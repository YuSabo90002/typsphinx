Template Named Dir Master
==========================

This master document sits inside a source directory named ``partials`` --
relocated here from ``tests/fixtures/template_named_dir_master/``'s
``_template``-named directory (Phase 54 plan 07, OUT-07: that directory
name is now a reserved output-directory collision, and the original
layout moved to the NEGATIVE
``tests/fixtures/template_prefix_reservation_gate/`` fixture instead).
It is the depth-1 case (one directory below the outdir root).
