Templates Path Absent Control Gate
====================================

Control fixture: no Sphinx Jinja override directory is configured at
all, yet the Typst template lives inside a directory spelled exactly
like that config value's own default. The build must succeed -- there
is nothing set to relate the template's directory against.
