Included Whole-Document Target
===============================

INCLUDED_BODY_MARKER_TEXT

This document IS reachable from the master toctree (via ``index``'s own
toctree), so the compiled master runs ``#include()`` on it, and a
whole-document reference to it can resolve to a real, working link
post-fix.
