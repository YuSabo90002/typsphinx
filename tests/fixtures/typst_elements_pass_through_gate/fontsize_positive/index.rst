Typst Elements Fontsize Positive Gate
======================================

This fixture exists solely to be compiled by
``tests/test_typst_elements_pass_through_gate.py`` (GATE-01, CONF-04,
SC#2). It is deliberately minimal -- a title and one short paragraph --
so the real ``typst.compile()`` this gate performs stays fast.
