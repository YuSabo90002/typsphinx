Typst Elements Unknown Key Negative Gate
=========================================

This fixture exists solely to be compiled (and to FAIL to compile) by
``tests/test_typst_elements_pass_through_gate.py`` (GATE-01, CONF-04,
SC#3). It is deliberately minimal -- a title and one short paragraph --
so the (expected-to-fail) build stays fast.
