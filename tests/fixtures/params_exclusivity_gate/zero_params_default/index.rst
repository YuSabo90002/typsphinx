Zero Params Default Gate
=========================

This document exists solely to be compiled by
``tests/test_params_exclusivity_gate.py`` (CONF-11, D-B, D-D). Its toctree
below makes ``toctree_numbered`` live on the pre-fix path (``extract_toctree_
options()`` reads the toctree node's own options regardless of whether it
has entries), so the gate proves the fix withholds it alongside every other
auto-derived key.

.. toctree::
   :numbered:
