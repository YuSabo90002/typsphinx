Package Params Gate Fixture
============================

This document exists solely to be compiled by
``tests/test_params_exclusivity_gate.py`` (CONF-11, D-B, D-C). It is
deliberately minimal -- a title and one short paragraph, no toctree -- so
the real ``typst.compile()`` this gate performs stays fast.
