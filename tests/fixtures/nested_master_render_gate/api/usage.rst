Usage
=====

This is the sibling document included from ``api/index`` via toctree.

It exists only to be a real, resolvable ``#include()`` target -- one
directory-relative path the fix under test must resolve correctly.
