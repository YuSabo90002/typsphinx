Mixed Prewrite Failures B
==========================

This is a minimal document used to prove that a build carrying TWO
different pre-write failure kinds -- a reserved-key case collision and a
CONF-17/A-01 violation -- refuses with ONE aggregated error naming both,
identical to fixture A's message once declaration order is normalized
out.
