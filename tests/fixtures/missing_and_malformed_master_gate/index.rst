Missing and Malformed Master Gate
==================================

This is the one valid master document in this fixture. It compiles cleanly
through the bundled default template so that the fixture's build failure is
attributable only to the other two, deliberately-bad ``typst_documents``
entries -- never to this master.

.. toctree::

   chapter1
