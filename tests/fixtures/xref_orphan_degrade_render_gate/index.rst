Master Document
===============

The master toctree flattens the included document into the compiled Typst
master. The orphan document is deliberately NOT listed here (it is marked
orphan), so it is never included and its anchors do not exist in the compiled
document.

.. toctree::

   included
