:orphan:

.. _orphan-target:

Orphan Target Section
=====================

This document is marked orphan, so Sphinx excludes it from every toctree. Its
body is written to its own file but never included into the compiled master, so
the anchor it emits does not exist in the master. A cross-reference to it from
an included document must therefore render as plain text rather than a dangling
label link.
