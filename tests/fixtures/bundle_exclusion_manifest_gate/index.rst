Bundle Exclusion Manifest Gate
===============================

This document exists solely to drive a real ``-b typst`` build whose
template bundle carries a nested asset and (at test time, injected into a
copy) VCS/OS metadata files that must not reach the published output.
