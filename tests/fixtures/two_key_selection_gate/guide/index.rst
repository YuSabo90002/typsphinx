Two Key Selection Gate — Report Nested
=======================================

A second master document, also selecting the "report" registry key, but
nested one directory below srcdir root -- this is OUT-06's proof that the
same key resolves to the identical root-absolute import string regardless
of wrapper nesting depth.
