Templates Path Collision Gate
==============================

This is a minimal document used to prove that a registry key whose
resolved template bundle directory collides with Sphinx's own
``templates_path`` refuses the build.
