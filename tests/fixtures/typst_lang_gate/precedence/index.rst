Typst Lang Gate Precedence Proof
==================================

This fixture exists solely to be built (source-only, no compile needed)
by ``tests/test_typst_lang_gate.py`` (GATE-01, CONF-07, SC#2). It is
deliberately minimal -- a title and one short paragraph.
