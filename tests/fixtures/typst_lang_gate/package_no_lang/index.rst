Typst Lang Gate Package No Lang Proof
========================================

This fixture exists solely to be built (source-only, no compile needed --
the existing package-only gate already proves this package path compiles
for real) by ``tests/test_typst_lang_gate.py`` (GATE-01, CONF-07, SC#3
package half). It is deliberately minimal and ASCII-only -- a title and
one short paragraph.
