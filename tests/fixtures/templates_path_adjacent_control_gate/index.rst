Templates Path Adjacent Control Gate
=====================================

Control fixture: the Typst template lives in a directory whose name
merely shares a prefix with the Sphinx template-override directory,
never the directory itself or a directory under it. The build must
succeed.
