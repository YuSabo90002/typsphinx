Reserved Key Case Prewrite Gate
=================================

This is a minimal document used to prove that a declared registry key
differing from the reserved built-in key only by case refuses the build
before any ``.typ`` file is written.
