Template Named Dir Master
==========================

This master document sits inside a source directory whose name collides
with the reserved ``_template`` basename that
``_validate_output_path_collisions()`` reserves at the outdir root
(Phase 54, 54-05). It is the depth-1 case (one directory below the
outdir root).
