Template Named Dir Master
==========================

This master document sits inside a source directory whose name collides
with the reserved ``_template`` basename that ``_write_template_file()``
writes at the outdir root. It is the depth-1 case (one directory below the
outdir root).
