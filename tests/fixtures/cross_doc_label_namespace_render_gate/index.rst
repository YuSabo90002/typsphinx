Master Document
===============

The master toctree flattens ``pagea`` and ``pageb`` into one compiled Typst
master. Both define a ``shared-topic`` section slug; per-document label
namespacing keeps them distinct.

.. toctree::

   pagea
   pageb
