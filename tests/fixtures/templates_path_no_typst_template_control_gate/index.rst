Templates Path No Typst Template Control Gate
================================================

Control fixture: ``templates_path`` is configured but no Typst template
is; the built-in registry key falls through to the packaged default,
whose bundle directory is never subjected to the containment test. The
build must succeed.
