Doctest Block Render Gate
=========================

Root master. It toctrees both context documents, so its own compile re-parses each of
them through ``#include()``.

.. toctree::

   context_a_paragraph
