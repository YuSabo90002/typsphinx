Real Source
============

This document's figure is an ordinary, source-root-relative image that
genuinely lives at ``<srcdir>/images/chart.png`` -- the file the converted
image in ``converted_source.rst`` currently destroys, pre-fix.

.. figure:: images/chart.png

   A real source image at the location the converted stand-in collides
   with.
