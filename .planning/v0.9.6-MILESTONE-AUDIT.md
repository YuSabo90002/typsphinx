---
milestone: v0.9.6
audited: 2026-09-20T13:05:00Z
status: gaps_found
scores:
  requirements: 3/5
  phases: 1/2
  integration: 7/7
  flows: 3/3
gaps:
  requirements:
    - id: "REL-15"
      status: "unsatisfied"
      phase: "Phase 75 — v0.9.6 Release Prep (prep-only)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 75 has not been planned or executed. STATE.md reads current_phase 75 / status planning / stopped_at 'Phase 74 complete, ready to plan Phase 75'. No 75-* phase directory exists. Measured on the tip: pyproject.toml line 7 still reads version = \"0.9.2\"; CHANGELOG.md still carries six bullets under ## [Unreleased] with no ## [0.9.6] section; the tail link block's [Unreleased] compare base is still v0.9.2...HEAD; README.md:348 Status line still reads v0.9.2; git ls-remote --tags origin carries no v0.9.6. This is expected mid-milestone, not a defect — REQUIREMENTS.md and ROADMAP constraints 1 and 9 hold REL-15's checkbox for /gsd-complete-milestone against observed publish evidence."
    - id: "REL-16"
      status: "unsatisfied"
      phase: "Phase 75 — v0.9.6 Release Prep (prep-only)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "No ### Known Limitations section exists in CHANGELOG.md and no decision record declines one. The question (NUM-01's per-master numref divergence, the converted-image rehome collision, the typst_documents duplicate-target cluster) is still open. Unlike REL-15, REL-16 legitimately closes INSIDE Phase 75 — it is a phase artifact, which is why that phase's REQUIREMENTS.md fence is line-scoped to REL-15 rather than whole-file."
  integration: []
  flows: []
tech_debt:
  - phase: 74-the-doctest-block-handler-its-real-compile-gate-and-the-docs
    items:
      - "Post-CI source drift: tests/test_docstring_rest_census_guard.py (351 lines) was committed at 03b0bdf3 AFTER the CI-green pushed tip e54d47d0. origin's branch head and CI run 35476044079 do not cover it. Phase 75 must re-push and re-dispatch CI before the merge PR."
      - "Five untracked scratch files at the repo root (probe_bogusxyz.typ, probe_none.typ, probe_pycon.typ, probe_python.typ, probe_text.typ) left from the D-01 language-tag compile probing. Untracked, so outside every scope fence, but they pollute git status at the milestone close."
      - "IN-01 (74-REVIEW.md, Info, unresolved): visit_literal_block / depart_literal_block Args: sections still read 'node: The literal block node' although both signatures were widened to nodes.literal_block | nodes.doctest_block."
      - "Phase 74 authored no CHANGELOG bullet of its own. TRN-01/TRN-02 are user-visible rendering fixes, so Phase 75 must write the ## [0.9.6] Fixed bullet from scratch rather than promoting one."
nyquist:
  compliant_phases: ["74-the-doctest-block-handler-its-real-compile-gate-and-the-docs"]
  partial_phases: []
  not_validated_phases: []
  missing_phases: ["75 (not started)"]
  overall: "compliant for every executed phase"
---

# Milestone v0.9.6 — Doctest block rendering and release — Audit

**Audited:** 2026-09-20 · **Status:** `gaps_found` · **Milestone base:** `6cc44f22` · **Tip:** `a3fe2102`

**Why `gaps_found`:** the milestone is **mid-flight**. Phase 75 (release prep) has not been planned
or executed, so REL-15 and REL-16 are unsatisfied and the FAIL gate forces this status. Every
requirement that belongs to an *executed* phase is satisfied and independently re-measured. This is
not a defect report on Phase 74 — it is an audit run one phase early.

## Requirements

| Req | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|-----|-------|-----------------|---------------------|-----------------|-------|
| TRN-01 | 74 | passed (SC1) | listed in 74-01, 74-05, 74-06, 74-07 | `[x]` | **satisfied** |
| TRN-02 | 74 | passed (SC2) | listed in 74-06, 74-07 | `[x]` | **satisfied** |
| QUA-14 | 74 | passed (SC3) | listed in 74-01, 74-04, 74-05, 74-06, 74-07 | `[x]` | **satisfied** |
| REL-15 | 75 | missing | missing | `[ ]` | **unsatisfied** (phase not run) |
| REL-16 | 75 | missing | missing | `[ ]` | **unsatisfied** (phase not run) |

**Orphan detection:** none. The traceability table maps exactly five REQ-IDs to the two milestone
phases; all three Phase 74 IDs appear in `74-VERIFICATION.md`'s requirements table, and the two
Phase 75 IDs are absent only because that phase has not run.

**Score:** 3/5 satisfied · 3/3 of the executed phase's requirements satisfied.

## Phases

| Phase | Plans | VERIFICATION.md | VALIDATION.md | SECURITY.md | REVIEW.md | Status |
|-------|-------|-----------------|---------------|-------------|-----------|--------|
| 74 — doctest_block handler, real-compile gate, docstring reST | 7/7 | `passed`, 4/4 must-haves, 0 overrides | `validated`, `nyquist_compliant: true`, `wave_0_complete: true` | `verified`, `threats_open: 0`, ASVS L1 | `issues_found`: 0 critical / 0 warning / 1 info | ✅ Complete |
| 75 — v0.9.6 Release Prep (prep-only) | 0/TBD | — | — | — | — | ⬜ Not started |

**Score:** 1/2 phases complete. No unverified *executed* phase — the one gap is a phase that has not
begun.

## Independent Re-measurement (this audit session, on tip `a3fe2102`)

| Check | Command | Result |
|-------|---------|--------|
| Full suite | `LC_ALL=C .venv/bin/python -m pytest -q` | `1569 passed, 1 skipped in 127.69s` |
| Types | `.venv/bin/python -m mypy typsphinx/` | `Success: no issues found in 9 source files` |
| Format | `black --check .` | `358 files would be left unchanged` |
| Lint | `ruff check .` | `All checks passed!` |
| Clean `-b typst` build | fresh `rm -rf` + `LANG=C LC_ALL=C sphinx-build -b typst docs/source <tmp>` | `build succeeded.` — **0** warnings (base `6cc44f22`: 5) |
| TRN-01 acceptance | `grep -c 'unknown node type: <doctest_block'` on that log | **0** (base: 2) |
| QUA-14 acceptance | `grep -c 'Unexpected indentation\|Block quote ends without a blank line'` | **0** (base: 3 attributed / 10 raw) |
| TRN-01 rendered shape | inspect `api/index.typ` | `compute_content_include_path` example inside a ` ```python ` fence, three `>>>` prompts and two outputs each on its own line |
| GATE-01 real compile | `pytest tests/test_doctest_block_render_gate.py -q` | `11 passed` — 3 masters, each driving a real `sphinx-build -b typstpdf` → `typst.compile()` |
| `@preview` invariant | `pytest tests/test_preview_version_sync.py` | 3 passed — still four packages, versions unchanged |
| Scope fence | `git diff --name-only 6cc44f22..HEAD -- typsphinx/` | exactly `typsphinx/pathfmt.py`, `typsphinx/translator.py` |

The suite count rose 1562 → **1569** since Phase 74's own verification, entirely from the seven
tests in `tests/test_docstring_rest_census_guard.py` added by the Nyquist validate-phase step.

## Integration

Cross-phase wiring checked by the integration checker and spot-confirmed here. With one work phase,
"cross-phase" means integration with the existing codebase and the handoff into Phase 75.

| Integration point | Mechanism | Status |
|-------------------|-----------|--------|
| `visit_doctest_block` → `visit_literal_block` | one-line delegation, `translator.py:2628-2653` / `2655-2665` | ✅ WIRED — single emission path, no double id-anchoring |
| Widened signatures | `nodes.literal_block \| nodes.doctest_block` on both visit and depart | ✅ WIRED — every existing call site still passes `literal_block` |
| Language fallback | `node.get("language", "") or ("python" if isinstance(node, nodes.doctest_block) else "")` | ✅ WIRED — applies only when the node carries no language, so an explicit tag is never clobbered (D-02) |
| List-item separator discipline | `in_list_item` / `list_item_needs_separator` shared by both node types | ✅ WIRED — context (b) fixtures prove it through a real compile |
| Captioned code blocks | `visit_container` matches `literal_block` children only | ✅ SAFE — `doctest_block` is a sibling under `FixedTextElement`, never captioned by the reST parser |
| Included (non-master) documents | codly / codly-languages `@preview` imports present in both the master template and the minimal included-document preamble | ✅ SAFE — a `doctest_block` in an `#include()`d file still resolves its codly imports |
| `@preview` three-place version sync | `writer.py` / `template_engine.py` / `templates/base.typ` | ✅ UNCHANGED — four packages, no version moved |

**Score:** 7/7 integration points wired. **No broken wiring, no orphaned export, no missing import.**

## End-to-End Flows

| Flow | Path | Status |
|------|------|--------|
| A `>>>` example in a docstring → `.typ` code block | autodoc/napoleon → docutils `Body.doctest()` → `visit_doctest_block` → `visit_literal_block` → codly-styled ` ```python ` fence | ✅ COMPLETE |
| A `>>>` example → compiled PDF | `sphinx-build -b typstpdf` → `typst.compile()` → `%PDF` magic, in all three GATE-01 masters | ✅ COMPLETE |
| Docs build warning ledger | clean `-b typst` of `docs/source` → 0 warnings, both message classes at zero, total not risen | ✅ COMPLETE |
| Release flow (tag → PyPI → GitHub Release) | Phase 75 + `/gsd-complete-milestone` | ⬜ NOT EXERCISED — phase not started |

**Score:** 3/3 of the flows this milestone's executed phase owns.

## Nyquist Coverage

| Phase | VALIDATION.md | `status` | `nyquist_compliant` | Verdict | Action |
|-------|---------------|----------|---------------------|---------|--------|
| 74 | exists | `validated` | `true` | **COMPLIANT** | none |
| 75 | absent | — | — | **MISSING** | runs as part of `/gsd-execute-phase 75` |

Every *executed* phase is Nyquist-compliant. Phase 75's VALIDATION.md is missing only because the
phase has not been planned.

## Tech Debt

### Phase 74

1. **Post-CI source drift — the one item that materially affects Phase 75.**
   `tests/test_docstring_rest_census_guard.py` (351 lines, 7 tests) landed at commit `03b0bdf3`,
   **after** the CI-green tip `e54d47d0` that `origin` still points at. `git diff --stat
   e54d47d0..HEAD -- . ':(exclude).planning'` shows exactly that one file. So CI run
   `35476044079` (12/12 jobs, including both `windows-latest` and both `macos-latest` lanes) was
   green on a tree that **does not contain this module**. The module passes locally on Linux, but
   it has never run on Windows or macOS — and it is a docs/docstring census guard, the class of
   test most likely to be path- or locale-sensitive. Phase 74's own SC4 was true when written;
   this is drift introduced afterwards by the Nyquist step. **Phase 75 must push the branch again
   and re-dispatch `gh workflow run CI --ref gsd/v0.9.6-doctest-block-rendering-and-release`
   before the merge PR** — which it would do anyway for the version bump, so the cost is zero as
   long as it is not skipped on the strength of the earlier green run.

2. **Five untracked scratch files at the repo root** — `probe_bogusxyz.typ`, `probe_none.typ`,
   `probe_pycon.typ`, `probe_python.typ`, `probe_text.typ`, left from the D-01 language-tag
   compile probing. Already recorded as an Info-level anti-pattern in `74-VERIFICATION.md`.
   Untracked, so outside every scope fence and every `git diff`, but they make `git status` dirty
   at the milestone close. `rm probe_*.typ`.

3. **IN-01 unresolved** (`74-REVIEW.md`, Info): `visit_literal_block` / `depart_literal_block`
   `Args:` sections still read `node: The literal block node` although both signatures now accept
   `nodes.literal_block | nodes.doctest_block`. A documentation nit, not a defect.

4. **No Phase 74 CHANGELOG bullet exists.** ROADMAP constraint 2 says the six carried
   `## [Unreleased]` bullets are promoted "together with this milestone's own bullets" — but Phase
   74 wrote none. TRN-01/TRN-02 are the first *user-visible rendering* change in three milestones
   (five of the six carried bullets explicitly say "This has no effect on installing or using
   typsphinx"), so `## [0.9.6]` needs a `### Fixed` bullet authored from scratch in Phase 75.

### Phase 75 handoff notes (staged correctly, but read these first)

- `pyproject.toml:7` = `version = "0.9.2"`, the sole version literal. ✅
- `## [Unreleased]` holds **exactly six** bullets — 1 Added, **4** Changed, 1 Fixed — matching
  ROADMAP constraint 2's measured census. ✅
- The tail link block's last line is `[Unreleased]: …/compare/v0.9.2...HEAD`; `grep` finds no
  `## [0.9.3]`, `## [0.9.4]` or `## [0.9.5]` section. ✅
- `README.md:348` reads `**Status**: Stable (v0.9.2) - Production ready`. ✅
- ⚠️ **`## [Unreleased]` also contains a `### Planned for Future Releases` sub-section**
  (`CHANGELOG.md:62-67`, five forward-looking items: BibTeX, glossary, index, pre-commit hooks,
  more Typst Universe templates). It is a standing roadmap list, **not** a release bullet, and must
  **stay under `## [Unreleased]`** when the six bullets are promoted. Neither the roadmap's
  constraint 2 nor the requirement text mentions it; promoting it into `## [0.9.6]` would publish
  five unimplemented features as shipped.

**Total: 4 items in Phase 74, plus 1 flagged handoff hazard for Phase 75.** None blocks the
milestone; item 1 is a required step rather than debt to defer.

---

_Audited 2026-09-20 · Phase 74 complete and independently re-measured; Phase 75 not started._
