---
phase: quick-260913-e7n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260913-e7n-close-issue-91-autoapi-footnote-signatur/260913-e7n-SUMMARY.md
autonomous: true
requirements: ["GH-91"]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "At execution-time HEAD, the minimal reStructuredText example from Issue #91 builds with `-b typstpdf` under LC_ALL=C and produces a non-empty PDF. This holds in two variants: `baz` defined in the same document, and `baz` defined in another document."
    - "The content file `index.typ` contains `#footnote(` and the label `<index:fn-f1>`, and the PDF text contains `Created with sphinx-autoapi`."
    - "No emitted `.typ` file in either variant contains the stray-plus shape, and the same regex does match the buggy line quoted in the issue (the negative control)."
    - "`typsphinx/` is byte-identical between tag v0.9.2 and execution-time HEAD, and between 4dfdd664 and execution-time HEAD. So the public comment can truthfully name 0.9.2, and the orchestrator's sphinx-autoapi measurement at 4dfdd664 still applies."
    - "Issue #91 ends CLOSED with stateReason COMPLETED and exactly one comment. The authenticated gh user wrote that comment, and it carries the verbatim body from <comment_body>."
    - "No tracked file in the repository changes except this quick task's SUMMARY.md, and the scratch projects live outside the repo tree."
  artifacts:
    - path: ".planning/quick/260913-e7n-close-issue-91-autoapi-footnote-signatur/260913-e7n-SUMMARY.md"
      provides: "Execution record: gate output, interpreter, emitted signature lines, comment URL, final issue state"
      contains: "status:"
  key_links:
    - from: "Task 1 gate output (PASS same, PASS xdoc, CONTROL_OK, SRC_UNCHANGED)"
      to: "Task 2 comment + close"
      via: "<precondition> on Task 2; any missing line halts before anything public happens"
      pattern: "SRC_UNCHANGED"
    - from: "<comment_body> block in this plan"
      to: "the posted GitHub comment"
      via: "Write tool -> $SCRATCH/comment.md -> gh issue comment 91 --body-file"
      pattern: "Neither symptom reproduces with typsphinx 0.9.2"
---

<objective>
Close GitHub Issue #91 ("[BUG] Transform autoapi generated files", filed 2025-11-21 against typsphinx 0.4.3). It no longer reproduces on the current release, 0.9.2. The issue reports two symptoms. First, a paragraph footnote is not emitted as a footnote and the file fails to compile. Second, the signature of `.. py:method:: foo(bar: baz | None = None)` gets a stray `+` inside a link call that fails to compile.

Purpose: the orchestrator measured on 2026-09-13 that both symptoms are gone at `main` @ 4dfdd664. That included a real sphinx-autoapi build of typsphinx's own package, which compiled to a 118-page PDF. The planner re-measured on the same day at the same HEAD. The minimal example and a cross-document variant both pass the exact gate command in Task 1's verify. Task 1 re-observes this live at execution time and fails closed. Task 2 posts one terse English comment and closes the issue, and runs only if Task 1 passed.

Output: one public comment on #91, the issue closed as completed, and the quick task's SUMMARY.md. No source, test, or docs change.

Tracer-first note: no code layer is modified, so there is no tracer slice to build. Task 1 is itself the end-to-end check of the whole pipeline: rST, then translator, then `.typ`, then typst-py compile, then PDF.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/yuta/Documents/typsphinx/CLAUDE.md
Read CLAUDE.md § Worktree-isolated execution and § NixOS development shell before running anything.

Measured facts (planner, 2026-09-13, main checkout @ 4dfdd664):
- `git diff --stat v0.9.2..HEAD -- typsphinx/` is empty, `pyproject.toml` has `version = "0.9.2"`, and tags `v0.9.0` and `v0.9.2` exist.
- With `typst_documents = [("index", "i91", "I91", "me")]`, the builder writes the wrapper `out/i91.typ`, the content file `out/index.typ`, `out/autoapi/foo.typ` and `out/i91.pdf`. The target must differ from the docname, or the output-path collision check fires. Use the content file, `index.typ`, for the content checks.
- Emitted signature, same-document variant: `raw("(") + emph(raw("bar")) + raw(":") + raw(" ") + link(<index:baz>, raw("baz")) + raw(" ") + raw("|") + ...`
- Emitted signature, cross-document variant: the same shape, except the link is wrapped as `context { let __tsx_body = [#{raw("baz")}]; if query(<autoapi_u2f_foo:baz>).len() > 0 { link(<autoapi_u2f_foo:baz>, __tsx_body) } else { __tsx_body } }`.
- The footnote is emitted as `[#footnote({par({text("Created with ") ... }) <index:fn-f1>]`.
- pypdf is in the `dev` extra, so `uv run python -c "import pypdf"` works after the worktree `uv sync --extra dev`. A bare `python` may resolve to an interpreter without it, so always go through `uv run`.
- `flake.nix`'s FHS sandbox ships `zlib`, which resolves the earlier Pillow/libz hazard, and `typsphinx/` does not import Pillow.
- `gh` is authenticated as the repo owner, with `repo` scope. Issue #91 was OPEN with zero comments at planning time.
</context>

<repro_project>
Build two scratch variants, `same` and `xdoc`, under `$SCRATCH` (see Task 1, step 2). Each variant has `src/conf.py`, `src/index.rst` and `src/autoapi/foo.rst`. Write the files with the Write tool, byte-for-byte, with no heredocs.

`src/conf.py` (identical in both variants):

```python
project = "I91"
author = "me"
typst_documents = [("index", "i91", "I91", "me")]
```

`same/src/index.rst`: the issue's own "Minimal reStructuredText Example", plus a `py:class:: baz` so the type cross-reference resolves in the same document.

```rst
API Reference
=============

This page contains auto-generated API reference documentation [#f1]_.

.. py:class:: baz

.. py:method:: foo(bar: baz | None = None)

.. toctree::
   :titlesonly:

   /autoapi/foo

.. [#f1] Created with `sphinx-autoapi <https://github.com/readthedocs/sphinx-autoapi>`_
```

`same/src/autoapi/foo.rst`:

```rst
foo
===

Foo module.
```

`xdoc/src/index.rst`: the issue's example exactly as reported, with no `py:class` here. `baz` lives in another document, like the issue's `../a/b.typ#baz` link.

```rst
API Reference
=============

This page contains auto-generated API reference documentation [#f1]_.

.. py:method:: foo(bar: baz | None = None)

.. toctree::
   :titlesonly:

   /autoapi/foo

.. [#f1] Created with `sphinx-autoapi <https://github.com/readthedocs/sphinx-autoapi>`_
```

`xdoc/src/autoapi/foo.rst`:

```rst
foo
===

.. py:class:: baz
```
</repro_project>

<comment_body>
Post this body verbatim as the single comment on #91. Write it to `$SCRATCH/comment.md` with the Write tool, without the surrounding fence:

```text
Thanks for the report. Neither symptom reproduces with typsphinx 0.9.2, the current release (checked on Linux):

- The minimal reStructuredText example from this issue builds with `sphinx-build -b typstpdf` and compiles to PDF.
- The footnote is emitted as a Typst `footnote(...)` and shows up as a footnote in the PDF.
- The `py:method` signature `foo(bar: baz | None = None)` is emitted with no stray `+`, both when `baz` is defined in the same document and when it is defined in another one.

A full sphinx-autoapi build also compiled to PDF.

Closing for now. If you still see either problem on 0.9.2 or later, please reopen and attach the generated `.typ` file.
```
</comment_body>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify both Issue #91 symptoms live at execution-time HEAD (fail-closed gate)</name>
  <files>$SCRATCH/same/**, $SCRATCH/xdoc/** (scratch only, OUTSIDE the repo tree, never committed); no repository file</files>
  <action>
Step 1, provision the worktree (CLAUDE.md § Worktree-isolated execution). Confirm you are in a worktree with `test -f .git`. Run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` from the worktree root, and run every later Python or Sphinx command via `uv run` from inside the worktree. Record the `home` and `version_info` lines of `.venv/pyvenv.cfg`. Run `uv run python -c "import typsphinx; print(typsphinx.__version__, typsphinx.__file__)"`. It must print `0.9.2` and a path under the worktree root (from `git rev-parse --show-toplevel`). A path under the main checkout means the editable install is bound to the wrong tree; treat that as a gate failure. If `Could not start dynamically linked executable` appears, the shim is not on PATH. That is an environment failure, not a product result: stop, and see step 5.

Step 2, choose the scratch dir. Set `SCRATCH` to your session scratchpad directory followed by `/issue91`, as an absolute path. It must NOT be inside the worktree or the main checkout. Assert this with a `case` test against `git rev-parse --show-toplevel` and against `/home/yuta/Documents/typsphinx`. Shell state does not persist between Bash calls, so re-set `SCRATCH` in every call and substitute the literal absolute path wherever this plan writes `$SCRATCH`. Delete any previous `$SCRATCH` first, then create both variants' files exactly as given in <repro_project>.

Step 3, build both variants. From inside the worktree, for each variant V in `same` and `xdoc`, run `LC_ALL=C uv run sphinx-build -b typstpdf "$SCRATCH/V/src" "$SCRATCH/V/out"`, redirecting stdout and stderr to `$SCRATCH/V/build.log`. Use absolute paths, because the `sphinx-build` shim walks up from the cwd, which is why you run from inside the worktree. Record each exit code and the count of lines that match WARNING in each log. Warnings are recorded, not gated.

Step 4, run the gate. Run the exact command in this task's automated verify. The gate passes only if the output contains all four lines: `PASS <same>`, `PASS <xdoc>`, `CONTROL_OK` and `SRC_UNCHANGED`. The gate covers the following:
- the build log reports success and the PDF is non-empty
- the content file has the footnote call, the `fn-f1` label and the `bar` parameter, which proves the signature was emitted at all
- the stray-plus regex finds nothing in any emitted `.typ`
- pypdf finds the footnote text in the PDF
- the same regex does match the buggy line quoted in the issue, so the negative check is not vacuous
- `typsphinx/` is unchanged versus both `v0.9.2` and `4dfdd664`

Also copy each variant's signature line (the line of `out/index.typ` containing `emph(raw("bar"))`) and the footnote line into your notes for the SUMMARY.

Step 5, fail closed. If any gate line is missing or any step above failed, STOP. Do not run Task 2, do not comment on the issue, and do not close it. Classify the failure in the SUMMARY. It is an environment failure if the evidence is a shim/ELF error, a ModuleNotFoundError, a missing shared library, or the typsphinx import bound to the main checkout. It is a product failure if the evidence is a build error in typsphinx code, a missing footnote or label, a stray-plus hit, missing PDF text, or a non-empty `typsphinx/` diff. Paste the raw evidence, and set the SUMMARY frontmatter to `status: blocked`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && SCRATCH=/ABSOLUTE/SCRATCH/issue91 && for V in "$SCRATCH/same" "$SCRATCH/xdoc"; do T="$V/out/index.typ"; grep -q 'build succeeded' "$V/build.log" && test -s "$V/out/i91.pdf" && grep -q '#footnote(' "$T" && grep -q '<index:fn-f1>' "$T" && grep -q 'emph(raw("bar"))' "$T" && ! grep -nE ',\s*\+|\(\s*\+' "$V"/out/*.typ "$V"/out/autoapi/*.typ && uv run python -c "import pypdf,sys; t=''.join(p.extract_text() for p in pypdf.PdfReader(sys.argv[1]).pages); sys.exit(0 if 'Created with sphinx-autoapi' in t else 1)" "$V/out/i91.pdf" && echo "PASS $V" || echo "FAIL $V"; done; printf '%s\n' 'link("../a/b.typ#baz",  + text("baz"))' | grep -qE ',\s*\+|\(\s*\+' && echo CONTROL_OK; git diff --quiet v0.9.2..HEAD -- typsphinx/ && git diff --quiet 4dfdd664..HEAD -- typsphinx/ && echo SRC_UNCHANGED</automated>
  </verify>
  <done>The gate output contains exactly `PASS …/same`, `PASS …/xdoc`, `CONTROL_OK` and `SRC_UNCHANGED`, with no `FAIL` line. `typsphinx.__version__` is `0.9.2` and `typsphinx.__file__` is under the worktree. `$SCRATCH` is outside both checkouts. If any of this fails, Task 2 is not run and the SUMMARY records `status: blocked` with classified raw evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Post the one verbatim comment on #91 and close it as completed (only after the Task 1 gate passed)</name>
  <files>$SCRATCH/comment.md (scratch only, outside the repo tree); GitHub Issue #91 (external)</files>
  <precondition>Task 1's gate output, from this same execution, contains PASS for both variants plus CONTROL_OK and SRC_UNCHANGED. If not, do not start this task.</precondition>
  <reversibility rating="costly">Closing can be undone by reopening. A public comment notifies subscribers by email, and deleting it later does not recall that notification.</reversibility>
  <action>
Step 1, write the body file. Using the Write tool, write the text inside the <comment_body> fence to `$SCRATCH/comment.md`, byte-for-byte, with nothing added and nothing removed. Then run the body-file checks in this task's verify, the first line of the verify block. The body must contain the key sentence, must have at most 10 lines, and the internal-vocabulary grep must print nothing. If a check fails, fix the file so it matches <comment_body> exactly. Do not reword the body.

Step 2, re-read the issue live before posting (mutable-scope authority, #3786). Run `gh issue view 91 --json state,comments --jq '[.state, (.comments|length)] | @tsv'`. The planning-time observation was OPEN with 0 comments. The live result decides what happens next:
- `OPEN` and `0`: proceed to step 3.
- `OPEN` and `1`, where that one comment's author login equals `gh api user --jq .login` and its body contains `Neither symptom reproduces with typsphinx 0.9.2`: a previous run already posted. Skip step 3 and go to step 4, so the comment is not posted twice.
- Anything else, meaning the issue is already closed, or someone else commented (the reporter may have added new information): STOP. Do not comment and do not close. Record the live JSON in the SUMMARY with `status: blocked`.

Step 3, post the comment. Run `gh issue comment 91 --body-file "$SCRATCH/comment.md"` and record the comment URL it prints. Use `--body-file`: the sandbox refuses command substitution inside `--body` or `--comment` arguments.

Step 4, close the issue. Run `gh issue close 91 --reason completed`. Do not pass `--comment`; the explanatory comment is already posted.

Step 5, confirm. Run the confirmation command in this task's verify, the second line of the verify block. The worktree must contain no change except the SUMMARY; check with `git status --porcelain`.
  </action>
  <verify>
    <automated>SCRATCH=/ABSOLUTE/SCRATCH/issue91 && grep -qF 'Neither symptom reproduces with typsphinx 0.9.2' "$SCRATCH/comment.md" && test "$(wc -l < "$SCRATCH/comment.md")" -le 10 && ! grep -niE 'doctest|milestone|phase|\.planning|gsd|todo|v0\.9\.3' "$SCRATCH/comment.md" && echo BODY_OK</automated>
    <automated>gh issue view 91 --json state,stateReason,comments --jq '[.state, .stateReason, (.comments|length), .comments[-1].author.login, (.comments[-1].body|contains("Neither symptom reproduces with typsphinx 0.9.2"))] | @tsv' ; gh api user --jq .login</automated>
  </verify>
  <done>`BODY_OK` is printed before posting. The confirmation prints `CLOSED`, `COMPLETED`, `1`, the author login and `true`, tab-separated, and the login matches the next line, the authenticated gh user. `git status --porcelain` in the worktree shows nothing except the quick task's SUMMARY.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor -> public GitHub (Issue #91) | Outward-facing and effectively permanent: a comment notifies subscribers by email, and the closure is visible to the reporter |
| scratch build dir -> repository tree | The repro projects and build outputs must never land in tracked files |
| planning-time observation -> execution-time action | The issue state and HEAD are mutable; acting on a stale observation could close an issue that has new reporter input, or claim a fix for code that changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-e7n-01 | Information disclosure | `$SCRATCH/comment.md` -> GitHub comment | medium | mitigate | The body comes verbatim from <comment_body>. Task 2's first verify line negative-greps for internal vocabulary and must print nothing before posting. |
| T-e7n-02 | Repudiation | issue close decision | medium | mitigate | Task 1 fails closed on four gate lines, including a negative control that proves the stray-plus regex is live. Task 2 carries a `<precondition>` on those lines. The SUMMARY records the raw gate output and the comment URL. |
| T-e7n-03 | Tampering | stale issue state | medium | mitigate | Task 2, step 2 re-reads the issue live and proceeds only on `OPEN`/`0`, or the resume case where our own identical comment is already there. A foreign comment or a closed state halts the run. |
| T-e7n-04 | Tampering | repository tree | low | mitigate | `$SCRATCH` is asserted to be outside both checkouts. `git status --porcelain` must show only the SUMMARY. |
| T-e7n-05 | Spoofing | gh identity | low | accept | `gh` is authenticated as the repo owner (measured at planning time). Task 2's confirmation compares the comment author against `gh api user`. |
| T-e7n-SC | Tampering | package installs | low | accept | No new package is installed. `uv sync --extra dev` installs only the existing `uv.lock` contents, and sphinx-autoapi is deliberately NOT installed. The comment's sphinx-autoapi sentence rests on the orchestrator's 2026-09-13 measurement at 4dfdd664, and `SRC_UNCHANGED` proves `typsphinx/` is identical to that point. |
</threat_model>

<verification>
- Task 1's gate prints `PASS …/same`, `PASS …/xdoc`, `CONTROL_OK` and `SRC_UNCHANGED`, with no `FAIL`.
- Task 2's body check prints `BODY_OK` before posting.
- `gh issue view 91 --json state,stateReason,comments` shows `CLOSED` and `COMPLETED`, with exactly one comment. The authenticated user wrote it, and it contains the key sentence.
- `git status --porcelain` in the worktree lists nothing except the SUMMARY.
</verification>

<success_criteria>
Issue #91 is closed as completed, with one terse, factual English comment that matches <comment_body>. That closure rests on a live, fail-closed re-measurement at execution-time HEAD, covering both reported symptoms in a same-document and a cross-document variant, and on proof that `typsphinx/` is unchanged since v0.9.2. No source, test, or docs file changed.
</success_criteria>

<output>
Create `/home/yuta/Documents/typsphinx/.planning/quick/260913-e7n-close-issue-91-autoapi-footnote-signatur/260913-e7n-SUMMARY.md` at that path inside your worktree.

Frontmatter: set `status: complete` when Task 2's confirmation passed. Set `status: blocked` if either task halted at its fail-closed or live-state gate, and state which gate halted it.

Body, at minimum:
- the `.venv/pyvenv.cfg` `home` and `version_info` lines, plus the `typsphinx.__version__` and `__file__` output
- the raw Task 1 gate output, and each variant's build exit code and WARNING count
- each variant's emitted signature line and footnote line
- the `gh issue comment` URL, and the raw output of the confirmation command
- the note that the sphinx-autoapi sentence in the comment rests on the orchestrator's 2026-09-13 measurement at 4dfdd664, which `SRC_UNCHANGED` keeps valid

Do not fix, file, or mention any other defect. Other pending todos are out of scope for this task.

Do not stage or commit any file other than this SUMMARY. The orchestrator commits the docs; if the quick workflow tells the executor to commit the SUMMARY in its worktree, commit only that one file.
</output>
