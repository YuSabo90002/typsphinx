---
phase: 260720-p2w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - QUICK-260720-p2w
must_haves:
  truths:
    - "CLAUDE.md documents how a gsd-executor provisions its environment when running under git-worktree isolation."
    - "The worktree-detection rule (.git is a FILE, not a directory) is stated."
    - "Both exact commands are present: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and running via `uv run`."
    - "The rationale (PEP-660 editable finder resolves to the main checkout, .venv is gitignored) is explained."
  artifacts:
    - CLAUDE.md
  key_links:
    - "New subsection sits inside CLAUDE.md and does not contradict the existing NixOS `sys.executable -m sphinx` guidance."
---

<objective>
Add a "Worktree-isolated execution" subsection to `CLAUDE.md` documenting how a gsd-executor (or any agent) must provision its environment when running under git-worktree isolation, so worktree-based execution is correct for this editable-install project.

Purpose: When `workflow.use_worktrees` is re-enabled for phases with parallel waves, an executor that edits files in a fresh worktree would otherwise have pytest import the unchanged main-tree package (because the main `.venv` holds a PEP-660 editable finder pinned to the main checkout's absolute path and `.venv` is gitignored). This subsection records the detection rule, the required provisioning steps, and the rationale so worktree execution binds `import typsphinx` to the worktree's own editable copy.

Output: An edited `CLAUDE.md` with a new subsection. No other files changed.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Worktree-isolated execution" subsection to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Edit `CLAUDE.md` ONLY. Add a new `### Worktree-isolated execution` subsection at the END of the existing `## Conventions & gotchas` section (immediately after the final CI bullet on line 78), matching the existing `###`-subsection style already used in the file (e.g. "### The `@preview` version-sync hazard"). Do not touch any other section, any source/test file, `.claude/gsd-core/**`, or any config. Do not change `workflow.use_worktrees` anywhere.

The subsection must contain the following, in prose + a short fenced `bash` block for the two commands (keep it concise, matching the file's tone):

1. Detection rule: state that you are running in an isolated git worktree when `.git` is a FILE (a `gitdir:` pointer), not a directory — checkable with `test -f .git`. Sequential main-tree execution has `.git` as a directory and needs none of this.

2. Required step 1 — provision the worktree's OWN virtualenv BEFORE running anything, with the exact command `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Explain that the `env -u …` unsets those two vars so `uv` creates a fresh worktree-local `.venv` rather than syncing into an already-activated main venv and re-pointing it.

3. Required step 2 — run ALL test/build commands via `uv run` (e.g. `uv run pytest …`). Note that inside the test fixtures Sphinx is still invoked as `sys.executable -m sphinx`, which under `uv run` resolves to the worktree venv's python, so `import typsphinx` binds to the worktree's editable copy. This complements — and must NOT contradict — the existing NixOS `sys.executable -m sphinx` guidance in the memory/notes.

4. Rationale (concise): the main `.venv` holds a PEP-660 editable finder (`__editable__.typsphinx-*.pth`) that resolves `import typsphinx` to the MAIN checkout's absolute path (`/…/typsphinx/typsphinx`), and `.venv` is gitignored so a fresh worktree has no venv of its own. Without the per-worktree `uv sync` + `uv run`, a worktree executor edits files in the worktree but pytest imports the UNCHANGED main-tree package → GATE-01 render gates stay RED after a correct fix, and later waves don't see earlier waves' changes. Verified 2026-07-20 that a worktree `uv sync` + `uv run` is isolated (main `.venv` untouched) and works under the NixOS sandbox.

5. Applicability note: `workflow.use_worktrees` is currently `false` (sequential main-tree execution needs none of this); this subsection matters when worktrees are re-enabled for phases with parallel waves.

Ensure the literal strings `uv sync --extra dev`, `VIRTUAL_ENV`, and `worktree` appear verbatim in the added text (the verify gate greps for them).
  </action>
  <verify>
    <automated>grep -q "uv sync --extra dev" CLAUDE.md && grep -q "VIRTUAL_ENV" CLAUDE.md && grep -q "worktree" CLAUDE.md && grep -q "uv run" CLAUDE.md && grep -q "Worktree-isolated execution" CLAUDE.md</automated>
  </verify>
  <done>
The `### Worktree-isolated execution` subsection is present in CLAUDE.md with: the detection rule (`.git` is a FILE), both commands (`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and running via `uv run`), and the PEP-660 editable-finder rationale. No file other than CLAUDE.md is modified (`git status --porcelain` shows only `CLAUDE.md`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edit to `CLAUDE.md`. No untrusted input, no code path, no package install, no config change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-p2w-01 | Tampering | CLAUDE.md guidance content | low | accept | Single-file prose edit; scope constraint (`CLAUDE.md` only) and the `git status --porcelain` check in `<done>` bound the blast radius. |
</threat_model>

<verification>
- `grep -q "uv sync --extra dev" CLAUDE.md && grep -q "VIRTUAL_ENV" CLAUDE.md && grep -q "worktree" CLAUDE.md` succeeds.
- `git status --porcelain` shows only `CLAUDE.md` modified.
</verification>

<success_criteria>
CLAUDE.md contains a new "Worktree-isolated execution" subsection covering the detection rule, both provisioning commands, and the rationale; no other files are changed.
</success_criteria>

<output>
Create `.planning/quick/260720-p2w-add-a-worktree-isolated-execution-subsec/260720-p2w-01-SUMMARY.md` when done.
</output>
