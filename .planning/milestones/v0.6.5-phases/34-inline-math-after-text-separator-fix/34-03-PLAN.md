---
phase: 34-inline-math-after-text-separator-fix
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - .planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md
autonomous: true
requirements: [MATH-01]

must_haves:
  truths:
    - "The full pytest suite shows NO failing test node ID that is absent from the pre-fix baseline recorded by Plan 01 — every remaining failure is a known NixOS-environmental failure, named and matched one-for-one (SC#5)"
    - "The two Plan 01 gate tests, which were in the RED set, are now in the passing set — the baseline comparison shows a strict improvement, never a regression"
    - "`black --check .`, `ruff check .` and `mypy typsphinx/` all exit 0 on the post-fix tree (matches the CI job set)"
    - "The full-corpus `-b typstpdf` regression gate is fatal-free, or its skip reason is recorded verbatim when the corpus is unavailable (SC#5)"
    - "The project's own documentation tree builds to PDF through `-b typstpdf` with a valid `%PDF` artifact — the dogfooding check (SC#5)"
    - "`34-GATE-EVIDENCE.md` carries a sweep section with one row per gate: command, verdict, and evidence pointer"
    - "Milestone invariants hold over the phase diff: zero new runtime dependencies, no `@preview` version bump, the four bundled package version strings unchanged across all four sync surfaces"
    - "Encoding edge (closing check): the PDF text-fidelity assertion's NFKC contract holds against the real rendered document, not only the synthetic fixture — the docs PDF still extracts its prose"
  artifacts:
    - .planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md
  key_links:
    - "Plan 01's `## Pre-fix full-suite baseline` failing-node-ID set → this plan's post-fix failing set (the comparison is what separates NixOS-environmental noise from a real regression)"
    - "`tests/test_preview_version_sync.py` → the four `@preview` version-sync surfaces (`writer.py`, `template_engine.py`, `templates/base.typ`, `examples/**/*.typ`)"
    - "`tests/test_corpus_gate.py` → the full-corpus `-b typstpdf` gate named explicitly by ROADMAP SC#5"
  prohibitions:
    - "A failing test may not be dismissed as environmental unless its node ID appears verbatim in Plan 01's pre-fix baseline"
    - "No source file, test file, or fixture may be edited by this plan — it is a measurement-only sweep; a real regression is escalated, not patched here"
    - "The corpus gate must not be reported as passing when it in fact skipped — a skip is recorded as a skip with its verbatim reason"
    - "No new runtime or dev dependency may be added, and no `@preview` package version string may change"
    - "Milestone invariants must be asserted mechanically over the diff, not from recollection"
---

<objective>
Close MATH-01 by proving nothing else regressed: run the full regression sweep against the fixed
tree, compare it to the pre-fix baseline Plan 01 recorded, and write the sweep verdict into the
phase evidence record.

Purpose: ROADMAP SC#5 names an explicit non-regression surface — display math, math in list items /
tables / captions, the three existing math modules, the full pytest suite, and the full-corpus
`-b typstpdf` gate. This project's sandbox has ~45 tests that fail for environmental reasons
unrelated to code correctness (project memory, RESEARCH.md "Environment Availability"), so "the
suite is green" is not a usable verdict here — the usable verdict is "the post-fix failing set is a
subset of the pre-fix failing set." That comparison is only possible because Plan 01 captured the
baseline before any source change.

Output: the `## Regression sweep` section of `34-GATE-EVIDENCE.md`, with a per-gate verdict table.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-inline-math-after-text-separator-fix/34-RESEARCH.md
@.planning/phases/34-inline-math-after-text-separator-fix/34-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

No new symbols. This plan only appends to
`.planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md`. The full Phase 34
artifact roster is in `34-01-PLAN.md` under "Artifacts this phase produces".

## Execution environment (MANDATORY — worktree-isolated execution is this repo's standing mode)

Detect isolation with `test -f .git` (a FILE means an isolated worktree). In a worktree, provision
FIRST with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run every
command through `uv run`. A sweep run against the main-tree package would measure the wrong code.

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + lint/type sweep, compared against the pre-fix baseline</name>
  <files>.planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md` — specifically the `## Pre-fix full-suite baseline` section written by Plan 01: the exact command, the counts, and the sorted failing-node-ID list this task compares against
    - `.planning/phases/34-inline-math-after-text-separator-fix/34-RESEARCH.md` — "Environment Availability", which documents the NixOS-sandbox caveat and the `sys.executable -m sphinx` requirement
    - `CLAUDE.md` — the canonical command set the CI job runs (`black --check .`, `ruff check .`, `mypy typsphinx/`, `pytest`)
    - `.planning/STATE.md` — "Milestone invariants (every phase)": zero new runtime dependencies; no `@preview` version bump; the four bundled package version strings unchanged across all four sync surfaces
  </read_first>
  <action>
Run the post-fix sweep and compare it mechanically to Plan 01's baseline. Edit no source, test, or
fixture file.

Step 1 — run the same full-suite command Plan 01 recorded: `uv run pytest -q --tb=no -rf`. Capture
the pass/fail/skip counts and the sorted set of failing test node IDs. Allow a long timeout; this
suite spawns many `sphinx-build` subprocesses.

Step 2 — compute the set difference against Plan 01's baseline set. Report three sets explicitly:
NEW failures (post-fix minus pre-fix — this set MUST be empty), FIXED (pre-fix minus post-fix —
expected to contain the two gate tests from `tests/test_inline_math_after_text_render_gate.py`), and
CARRIED (the intersection — the known NixOS-environmental failures). Do the comparison by writing
both sorted node-ID lists to scratch files and diffing them; do not eyeball it. If the NEW set is
non-empty, stop and escalate with the verbatim failure output — a real regression is not patched in
this plan.

Step 3 — run the CI command set exactly as `CLAUDE.md` specifies: `uv run black --check .`,
`uv run ruff check .`, `uv run mypy typsphinx/`. Record each exit status.

Step 4 — assert the milestone invariants mechanically over the phase diff (not from recollection).
Compute the phase diff against the milestone base (`git diff --stat <base>...HEAD`) and assert:
`pyproject.toml` and `uv.lock` are absent from the changed-file list (zero new dependencies); no
changed file is one of the four `@preview` sync surfaces (`typsphinx/writer.py`,
`typsphinx/template_engine.py`, `typsphinx/templates/base.typ`, `examples/**/*.typ`); and
`uv run pytest tests/test_preview_version_sync.py -q` passes.

Step 5 — append a `## Regression sweep — suite, lint, invariants` section to
`34-GATE-EVIDENCE.md` holding: the verbatim commands, the counts, the three set-difference lists
(NEW / FIXED / CARRIED) with the NEW list stated as empty when it is, the four lint/type exit
statuses, and the invariant assertions with the commands that produced them.
  </action>
  <verify>
    <automated>uv run black --check . && uv run ruff check . && uv run mypy typsphinx/ && uv run pytest tests/test_preview_version_sync.py -q && grep -c 'Regression sweep' .planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The NEW-failures set (post-fix failing node IDs minus Plan 01's pre-fix baseline) is empty, and the evidence file states this with both lists shown.
    - The FIXED set contains `tests/test_inline_math_after_text_render_gate.py::TestInlineMathAfterTextRenderGate::test_typstpdf_separates_inline_math_mitex_path` and `...::test_typstpdf_separates_inline_math_native_path`.
    - `uv run black --check .`, `uv run ruff check .`, and `uv run mypy typsphinx/` each exit 0, with the exit statuses recorded.
    - `uv run pytest tests/test_preview_version_sync.py -q` passes, and the phase diff's changed-file list contains none of `pyproject.toml`, `uv.lock`, `typsphinx/writer.py`, `typsphinx/template_engine.py`, `typsphinx/templates/base.typ`, or any path under `examples/`.
    - `git status --porcelain typsphinx/ tests/` prints nothing — this plan modified no code.
    - `34-GATE-EVIDENCE.md` contains the heading `## Regression sweep — suite, lint, invariants` and the three set labels `NEW`, `FIXED`, `CARRIED`.
  </acceptance_criteria>
  <done>The post-fix failing set is a strict subset of the pre-fix baseline, the CI command set is clean, the milestone invariants are mechanically asserted, and all of it is recorded in the evidence file.</done>
</task>

<task type="auto">
  <name>Task 2: Full-corpus gate and docs dogfooding build</name>
  <files>.planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md</files>
  <read_first>
    - `tests/test_corpus_gate.py` — the full-corpus `-b typstpdf` gate named by ROADMAP SC#5, including its own docstring describing the network dependency and its graceful-skip behavior when the corpus is unavailable
    - `.planning/phases/34-inline-math-after-text-separator-fix/34-RESEARCH.md` — "Validation Architecture" → "Sampling Rate" → the phase-gate row, which names the corpus gate as the phase's closing check
    - `docs/source/examples/basic.rst` — contains inline and display math inside a `.. code-block:: rst`, i.e. literal content, confirming the docs tree exercises the math role in prose elsewhere but not inside a literal block
    - `tox.ini` — the `docs-pdf` environment, for the canonical docs-PDF invocation this task reproduces directly (invoke sphinx as a module, never a compiled console script, per the NixOS caveat)
  </read_first>
  <action>
Run the two remaining SC#5 gates and record their verdicts. Edit no source, test, or fixture file.

Step 1 — full-corpus gate. Run `uv run pytest tests/test_corpus_gate.py -q -m slow` with a long
timeout. Record the outcome honestly in one of three shapes: PASSED (fatal-free, with the counts),
SKIPPED (with the verbatim skip reason — this gate is network-dependent and skips gracefully when
the corpus is unavailable), or FAILED (with the verbatim fatal, which is escalated, not patched
here). Never report a skip as a pass.

Step 2 — docs dogfooding build. Build the project's own documentation through the extension under
test: `uv run python -m sphinx -b typstpdf docs/source <scratch>/docs-pdf`. Assert the build exits
0, that stderr carries none of the strings `expected semicolon or line break`, `expected comma`, or
`Typst compilation failed`, and that the produced PDF exists, is non-empty, and starts with the
`%PDF` magic bytes. Record the PDF path, byte size, and page count.

Step 3 — encoding closing check. Extract the docs PDF's text with `pypdf` and confirm a prose
sentinel from the docs front page is present in the raw extracted text, and that
`unicodedata.normalize("NFKC", ...)` of the extracted text is non-empty and strictly longer than
zero characters. This closes the encoding edge against a real rendered document rather than only the
synthetic fixture.

Step 4 — append a `## Regression sweep — corpus gate and docs dogfooding` section to
`34-GATE-EVIDENCE.md` with a verdict table (gate, command, verdict, evidence), the verbatim corpus
gate outcome, and the docs PDF facts from steps 2-3.

Step 5 — append a `## Phase 34 verdict` section mapping each of the five ROADMAP SC#1-SC#5 criteria
to the specific evidence section that proves it, with a PASS / NOT-MET / HUMAN-NEEDED marker per
criterion. Follow the project's honest-verifier standing rule (STATE.md): abstain to
`human_needed` rather than assert a truth without direct evidence.
  </action>
  <verify>
    <automated>grep -c 'Regression sweep — corpus gate and docs dogfooding' .planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md && grep -c 'Phase 34 verdict' .planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md && grep -c 'SC#5' .planning/phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md</automated>
    <human-check>Open the docs PDF produced in step 2 and the fixture PDF from `tests/fixtures/inline_math_after_text_render_gate` (rebuild it to a scratch dir if needed). Confirm visually that (a) in the bullet-list item, the prose "Text before math", the rendered equation, and "text after." sit on one continuous line with normal word spacing — not split across lines, not overlapping, and with no visible Typst source such as `mi(` on the page; (b) the display equation inside the list item renders as a centred equation between its two prose paragraphs; and (c) the confval `:default:` field body reads as one continuous sentence with the equation inline. Report "approved" or describe what looks wrong.</human-check>
  </verify>
  <acceptance_criteria>
    - `34-GATE-EVIDENCE.md` contains the heading `## Regression sweep — corpus gate and docs dogfooding` with a verdict table whose corpus-gate row reads exactly one of PASSED / SKIPPED / FAILED, and carries the verbatim pytest output or skip reason.
    - The docs dogfooding build exited 0, its stderr contained none of the three fatal signature strings, and the produced PDF's first four bytes are `%PDF` with a recorded non-zero byte size and page count.
    - The docs PDF's `pypdf`-extracted text contains a recorded prose sentinel from the docs front page.
    - `34-GATE-EVIDENCE.md` contains a `## Phase 34 verdict` section with one row per ROADMAP criterion SC#1 through SC#5, each marked PASS, NOT-MET, or HUMAN-NEEDED and each citing the evidence section that supports it.
    - `git status --porcelain typsphinx/ tests/ docs/` prints nothing.
  </acceptance_criteria>
  <done>The corpus gate and the docs dogfooding build are recorded with honest verdicts, and every ROADMAP success criterion is mapped to its evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external corpus (Sphinx `doc/` tree, network-fetched) → `-b typstpdf` build | Third-party reST content crosses into the translator; this is the widest untrusted-input surface the project exercises |
| emitted `.typ` → `typst.compile()` | Generated source crosses into the Typst compiler |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-02 | Denial of Service | `typst.compile()` aborting a whole document on a juxtaposed emission, on a real third-party corpus rather than a synthetic fixture | high | mitigate | The full-corpus `-b typstpdf` gate is re-run against the fixed translator and its outcome recorded verbatim; a fatal is escalated, not patched here. |
| T-34-05 | Spoofing | A skipped gate being reported as a passing gate, creating false release confidence | medium | mitigate | The corpus-gate verdict is constrained to exactly one of PASSED / SKIPPED / FAILED, with the verbatim skip reason required when SKIPPED; the `## Phase 34 verdict` table uses HUMAN-NEEDED rather than asserting an unproven truth (STATE.md honest-verifier rule). |
| T-34-06 | Tampering | A real regression being masked by the ~45 known NixOS-environmental failures | high | mitigate | The verdict is a mechanical set difference against Plan 01's pre-fix baseline (captured before any source change), not a human judgement about which failures "look environmental". A non-empty NEW set stops the plan. |
| T-34-SC | Tampering | npm/pip/cargo supply chain | low | accept | Zero package installs in this phase (milestone invariant), asserted mechanically in Task 1 step 4 by the absence of `pyproject.toml` / `uv.lock` from the phase diff. |
</threat_model>

<verification>
- Post-fix failing node-ID set minus Plan 01's pre-fix baseline is empty.
- `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0.
- `uv run pytest tests/test_preview_version_sync.py -q` passes; the phase diff touches none of the four `@preview` sync surfaces, `pyproject.toml`, or `uv.lock`.
- The corpus gate outcome and the docs `-b typstpdf` PDF facts are recorded verbatim.
- `34-GATE-EVIDENCE.md` maps SC#1-SC#5 to evidence with an honest per-criterion marker.
</verification>

<success_criteria>
- No new test failure exists relative to the pre-fix baseline; the two gate tests moved from failing to passing (SC#5).
- The CI command set is clean on the post-fix tree (SC#5).
- The full-corpus `-b typstpdf` gate result is recorded honestly, and the project's own docs build to a valid PDF through the fixed translator (SC#5).
- The milestone invariants are asserted mechanically over the phase diff.
- Every ROADMAP success criterion SC#1-SC#5 is mapped to concrete evidence in `34-GATE-EVIDENCE.md`.
</success_criteria>

<output>
Create `.planning/phases/34-inline-math-after-text-separator-fix/34-03-SUMMARY.md` when done.
</output>
