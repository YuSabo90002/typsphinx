---
phase: 35-v0-6-5-release-prep
plan: 04
type: execute
wave: 3
depends_on: ["35-03"]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [REL-03]

must_haves:
  truths:
    - "ROADMAP SC#2: `CHANGELOG.md` carries a curated `## [0.6.5]` entry stating the inline-math fix in user-visible terms — what a reader could not build before and can build now."
    - "D-03: the entry has exactly two subsections, `### Fixed` and `### Verified`, preceded by a lead paragraph. It carries no Added, Changed, or Removed subsection, because nothing was added, changed, or removed at the user-facing level."
    - "D-01 and D-02: `### Fixed` is a single bullet whose one general sentence names representative contexts in parentheses (bullet-list items, definition-list terms, and the like), bundles the display-math-inside-a-list-item case into the same bullet because it is one and the same change from the user's side, and carries no breaking-change label."
    - "D-01 honesty constraint: the entry does not claim the top-level-paragraph shape was broken. That shape was measured green before the fix; what was actually red were bullet-list items, field bodies, definition-list terms, display math inside a list item, and a list item whose sole content is inline math."
    - "D-04: `### Verified` carries exactly three items — zero new runtime dependencies, the four bundled `@preview` version strings unchanged across all four sync surfaces, and the full-corpus `-b typstpdf` re-run fatal-free. The GATE-01 fixture's RED-to-GREEN record is deliberately absent, because test machinery is not user-visible."
    - "ROADMAP SC#2: the tail link block is rolled over — a `[0.6.5]:` release-tag line is added immediately above the `[0.6.4]:` line, and the file's last line advances the Unreleased compare range to name v0.6.5."
    - "No historical CHANGELOG entry is edited: the whole-plan diff on `CHANGELOG.md` removes exactly one line, the old Unreleased compare line."
    - "The `## [Unreleased]` heading at line 8 is left in place and empty, matching every prior release having fully drained it at bump time. The second, legacy `## [Unreleased]` heading further down the file (the historical Planned-for-Future-Releases block) is not touched or conflated with it."
  prohibitions:
    - statement: "MUST NOT describe the fix in terms broader than what the Phase 34 gate actually proves, and MUST NOT let the deliberately-deferred WR-01 defect vanish without a written record. Overstating the fix in a user-facing changelog is a transparency failure that no test in this repository can catch, and the deferral record lives in a separate plan whose output this entry must not implicitly contradict."
      category: transparency
      status: required
  artifacts:
    - path: "CHANGELOG.md"
      provides: "the curated `## [0.6.5]` release entry and the rolled-over tail link block"
      contains: "## [0.6.5]"
  key_links:
    - from: "CHANGELOG.md `## [0.6.5]` heading"
      to: "pyproject.toml `[project] version`"
      via: "the entry names the version plan 35-03 made true in the tree; a heading naming a version the tree does not declare would be a release-identity mismatch"
      pattern: "0\\.6\\.5"
    - from: "CHANGELOG.md tail link block"
      to: "the future `v0.6.5` git tag"
      via: "the `[0.6.5]:` line points at a releases/tag URL that only resolves once `/gsd-complete-milestone` pushes the tag; writing the link is prep, pushing the tag is publish"
      pattern: "releases/tag/v0\\.6\\.5"
---

<objective>
Write the curated `## [0.6.5]` entry into `CHANGELOG.md` and roll over the tail link block.

Purpose: discharges ROADMAP Phase 35 SC#2, both halves. The phase goal is that someone reading the
changelog can see, in their own terms, that a document which used to fail to compile now compiles —
so the entry's job is to state the user-visible change, not the implementation. D-01 through D-04
fix the entry's structure and content; this plan's job is to render those decisions faithfully and
resist both overstatement and the reflex to pad the entry with implementation detail.

The entry runs after plan 35-03 so its `### Verified` claims about dependencies and the `@preview`
surfaces describe the tree as it will actually ship, with the lockfile diff already measured.

Output: `CHANGELOG.md` carrying a `## [0.6.5]` section directly above `## [0.6.4]`, plus a `[0.6.5]:`
release-tag link and an advanced Unreleased compare range in the tail block.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-v0-6-5-release-prep/35-CONTEXT.md
@.planning/phases/35-v0-6-5-release-prep/35-RESEARCH.md
@.planning/phases/35-v0-6-5-release-prep/35-PATTERNS.md
@CLAUDE.md
</context>

## Environment prerequisite (read before Task 1)

If `.git` is a FILE you are in an isolated worktree. Provision it before running anything, then run
every command through `uv run`:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`

## Artifacts this phase produces

This plan creates **no new symbols** and touches exactly one file.

| Artifact | Kind | Change |
|----------|------|--------|
| `CHANGELOG.md` `## [0.6.5]` section | new document section | inserted between the top `## [Unreleased]` heading and `## [0.6.4] - 2026-07-28` |
| `CHANGELOG.md` `[0.6.5]:` tail link line | new line | inserted immediately above the existing `[0.6.4]:` line |
| `CHANGELOG.md` `[Unreleased]:` tail link line | existing last line | compare range advanced to name v0.6.5 |

## Hazard: `CHANGELOG.md` has two `## [Unreleased]` headings

Measured live at plan time: `grep -n '^## \[Unreleased\]$' CHANGELOG.md` returns **two** lines — line
8, the real Keep-a-Changelog Unreleased section at the top of the file, and line 831, a legacy
"Planned for Future Releases" block sitting just above the tail link block. Every instruction in this
plan that says "the Unreleased heading" means **line 8**. Never insert the new release section at the
second one, and never merge or delete the second one — editing historical CHANGELOG content is out of
scope for this phase.

<tasks>

<task type="auto">
  <name>Task 1: Insert the curated `## [0.6.5]` entry</name>

  <files>CHANGELOG.md</files>

  <read_first>
    - `CHANGELOG.md` — read lines 1-60. This covers the header, the top `## [Unreleased]` heading at
      line 8, the blank line 9, and the whole `## [0.6.4] - 2026-07-28` entry starting at line 10
      (lead paragraph plus Added, Changed, Removed, Fixed, and Verified subsections). The 0.6.4 entry
      is the wide-end model: read its Verified subsection closely, because its invariant sentence is
      the skeleton this entry reuses.
    - `CHANGELOG.md` — read lines 201-241, the `## [0.6.1]` entry. That is a small release with a lead
      paragraph plus Fixed plus Verified, and is the direct structural model for this entry's shape.
      Note the closing sentence of its lead paragraph about zero new runtime dependencies and the
      untouched `@preview` version-sync surface; that sentence is reused near-verbatim across 0.6.1,
      0.6.3, and 0.6.4.
    - `.planning/phases/35-v0-6-5-release-prep/35-CONTEXT.md` — decisions D-01, D-02, D-03, D-04 in
      full, and the `<specifics>` table rows for shapes that were already green before the fix versus
      shapes that were red. The distinction between those two rows is what keeps this entry honest.
    - `.planning/phases/35-v0-6-5-release-prep/35-RESEARCH.md` — section
      `### Pattern: CHANGELOG entry structure (small-release model — ## [0.6.1])`.
    - `.planning/phases/35-v0-6-5-release-prep/35-PATTERNS.md` — sections `### CHANGELOG.md` and
      `### CHANGELOG lead-paragraph register`.
    - `.planning/REQUIREMENTS.md` — the MATH-01 entry, for the requirement ID this entry cites.
  </read_first>

  <action>
    Insert a new section into `CHANGELOG.md` between the top `## [Unreleased]` heading (line 8, with
    its blank line 9 below it) and the `## [0.6.4] - 2026-07-28` heading. Leave `## [Unreleased]`
    itself in place and empty; every prior release fully drained it at bump time and this one does the
    same.

    Head the section `## [0.6.5] - <date>`, where `<date>` is the `date +%F` output on the day the
    edit lands. Match the exact heading punctuation the existing entries use.

    Write a lead paragraph of two to four sentences, following D-03's axis: a document that mixes
    prose and math no longer aborts the Typst compile, and the runtime change is confined to the math
    handlers in `typsphinx/translator.py`. Do not claim a single method changed — the measured diff for
    this milestone is `typsphinx/translator.py` alone at 45 insertions and 0 deletions, spanning both
    the inline and the display-math handler, and the entry must not narrow that to something it can be
    checked wrong on. Close the lead with the standing sentence this project reuses across 0.6.1,
    0.6.3, and 0.6.4: zero new runtime dependencies, and the bundled `@preview` version-sync surface
    untouched.

    Write `### Fixed` as exactly one bullet, per D-01 and D-02. The bullet's one general sentence
    describes inline math immediately after text (in bullet-list items, definition-list terms, and the
    like) no longer aborting the `typstpdf` compile, bundles the display-math-inside-a-list-item case
    into the same bullet because from the user's side it is one and the same change, and states that
    the fix covers both emission paths — the mitex default and the native path. Attach the requirement
    ID in trailing parentheses in the form the 0.6.1 and 0.6.4 entries use for their own IDs; the
    requirement here is MATH-01. Do not enumerate every failing context — D-01 prescribes the
    parenthetical shorthand precisely to avoid that. Do not add a label marking this as a
    compatibility break of any kind: this is a pure bug fix, and no user whose setup worked before is
    broken by it.

    Keep the entry honest on the point CONTEXT.md measured and the original backlog report got wrong:
    the top-level-paragraph shape, including the form where math immediately follows text with no
    intervening space, was already green before the fix. Do not write a sentence that implies plain
    paragraphs were failing. The shapes that were actually red are bullet-list items, collapsed field
    bodies, definition-list terms, display math inside a list item, and a list item whose sole content
    is inline math.

    Write `### Verified` as exactly three bullets, per D-04, in this order: zero new runtime
    dependencies across the full milestone diff; the four bundled `@preview` package version strings
    unchanged across all four sync surfaces; and the full-corpus `-b typstpdf` re-run over the Sphinx
    v9.1.0 `doc/` corpus fatal-free. Reuse the sentence skeleton from the 0.6.4 entry's Verified
    subsection for the first two, but drop its clause asserting zero changes under `typsphinx/` — that
    clause was true of the previous milestone and is false of this one. Do not list the GATE-01
    fixture's RED-to-GREEN record: D-04 excludes it because test machinery is not user-visible.

    Add no `### Added`, `### Changed`, or `### Removed` subsection. Nothing was added, changed, or
    removed at the user-facing level, and D-03 fixes this entry at two subsections.

    Do not edit any pre-existing entry, and do not touch the tail link block in this task — that is
    Task 2.
  </action>

  <verify>
    <automated>S=$(awk '/^## \[0\.6\.5\]/{f=1} /^## \[0\.6\.4\]/{f=0} f' CHANGELOG.md) &amp;&amp; printf '%s\n' "$S" | grep -qx '### Fixed' &amp;&amp; printf '%s\n' "$S" | grep -qx '### Verified' &amp;&amp; ! printf '%s\n' "$S" | grep -qxE '### (Added|Changed|Removed)' &amp;&amp; ! printf '%s\n' "$S" | grep -q 'BREAKING' &amp;&amp; [ "$(printf '%s\n' "$S" | awk '/^### Fixed/{f=1;next} /^### /{f=0} f' | grep -c '^- ')" = "1" ] &amp;&amp; [ "$(printf '%s\n' "$S" | awk '/^### Verified/{v=1;next} /^### /{v=0} v' | grep -c '^- ')" = "3" ] &amp;&amp; [ "$(grep -c '^## \[Unreleased\]$' CHANGELOG.md)" = "2" ] &amp;&amp; echo CHANGELOG_ENTRY_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## \[0\.6\.5\] - [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]$' CHANGELOG.md` returns
      exactly `1`.
    - `grep -m1 '^## \[[0-9]' CHANGELOG.md` outputs the `## [0.6.5]` heading — i.e. the new section is
      the first versioned section in the file, sitting above `## [0.6.4]`.
    - `awk 'NR==8' CHANGELOG.md` outputs exactly `## [Unreleased]`, and
      `grep -c '^## \[Unreleased\]$' CHANGELOG.md` returns `2` — the top heading did not move and the
      legacy one further down was not touched.
    - Within the `## [0.6.5]` section only: a line exactly equal to `### Fixed` is present, a line
      exactly equal to `### Verified` is present, and no line equal to `### Added`, `### Changed`, or
      `### Removed` is present.
    - Within the `### Fixed` subsection, `grep -c '^- '` returns exactly `1`.
    - Within the `### Verified` subsection, `grep -c '^- '` returns exactly `3`.
    - Within the `## [0.6.5]` section, `grep -c 'definition-list terms'` returns at least `1` and
      `grep -c 'MATH-01'` returns at least `1`.
    - Within the `## [0.6.5]` section, `grep -c 'BREAKING'` returns `0`.
    - Within the `## [0.6.5]` section, `grep -ci 'GATE-01'` returns `0` — D-04 excludes the fixture
      record.
    - Within the `## [0.6.5]` section, `grep -c 'zero changes under'` returns `0` — the 0.6.4 clause
      that is false for this milestone was not carried over.
    - `git diff -U0 CHANGELOG.md` for this task shows only added lines and zero removed lines.
  </acceptance_criteria>

  <done>
    A `## [0.6.5]` entry with a lead paragraph, a one-bullet Fixed subsection, and a three-bullet
    Verified subsection sits directly above `## [0.6.4]`, with no historical entry edited and no
    subsection D-03 excludes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Roll over the tail link block</name>

  <files>CHANGELOG.md</files>

  <read_first>
    - `CHANGELOG.md` — read the last 25 lines of the file. Measured live at plan time before Task 1's
      insertion: the `[0.6.4]:` release-tag line sits at line 842, the descending list of prior
      release-tag lines follows it, and the `[Unreleased]:` compare line is line 857, the file's last
      line. Task 1's insertion shifts every one of those numbers; locate both edit sites by content,
      never by the numbers recorded here.
    - `.planning/phases/35-v0-6-5-release-prep/35-RESEARCH.md` — section
      `### Pattern: tail link-block rollover (verified exact current lines)`, which gives the required
      edit and the URL template.
    - `.planning/phases/35-v0-6-5-release-prep/35-PATTERNS.md` — the tail-link-block paragraph under
      `### CHANGELOG.md`, which records that every existing line uses one literal owner/repo segment
      that must not be varied even if the working tree's git remote resolves elsewhere.
  </read_first>

  <action>
    Insert one new line into the tail link block, immediately above the existing `[0.6.4]:` line, of
    the same shape every existing release-tag line uses: the bracketed version `[0.6.5]`, a colon and
    space, then the project's releases-tag URL ending in `v0.6.5`. Copy the owner and repository
    segments character-for-character from the adjacent `[0.6.4]:` line rather than reconstructing the
    URL from the git remote — the historical lines are the authority for that literal.

    Then rewrite the file's last line, the Unreleased compare link, so its compare range starts at
    `v0.6.5` instead of the previous release while keeping the `...HEAD` suffix and the rest of the
    URL identical. There must be exactly one Unreleased compare line in the file when you are done.

    Do not reorder, reformat, or otherwise edit any other line in the tail block. Do not add a link
    line for a version that has no entry in the file.

    Note that the `[0.6.5]:` URL will not resolve until the `v0.6.5` tag is pushed, which happens at
    `/gsd-complete-milestone`, not here. Writing the link is prep; creating the tag is publish. Do not
    create or push any tag in this task.
  </action>

  <verify>
    <automated>grep -c '^\[0\.6\.5\]: https://github\.com/YuSabo90002/typsphinx/releases/tag/v0\.6\.5$' CHANGELOG.md | grep -qx 1 &amp;&amp; tail -n 1 CHANGELOG.md | grep -qE '^\[Unreleased\]: https://github\.com/YuSabo90002/typsphinx/compare/v0\.6\.5\.\.\.HEAD$' &amp;&amp; [ "$(grep -c '^\[Unreleased\]: ' CHANGELOG.md)" = "1" ] &amp;&amp; [ "$(grep -c '^\[Unreleased\]: .*v0\.6\.4\.\.\.HEAD$' CHANGELOG.md)" = "0" ] &amp;&amp; [ -z "$(git tag -l v0.6.5)" ] &amp;&amp; echo TAIL_LINKS_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^\[0\.6\.5\]: https://github\.com/YuSabo90002/typsphinx/releases/tag/v0\.6\.5$' CHANGELOG.md`
      returns exactly `1`.
    - `grep -n '^\[0\.6\.[45]\]: ' CHANGELOG.md` returns two consecutive line numbers with the 0.6.5
      line first — the new link sits immediately above the 0.6.4 link.
    - `tail -n 1 CHANGELOG.md` matches
      `^\[Unreleased\]: https://github\.com/YuSabo90002/typsphinx/compare/v0\.6\.5\.\.\.HEAD$`.
    - `grep -c '^\[Unreleased\]: ' CHANGELOG.md` returns exactly `1`, and
      `grep -c '^\[Unreleased\]: .*v0\.6\.4\.\.\.HEAD$' CHANGELOG.md` returns `0`.
    - `git diff --numstat CHANGELOG.md` for the whole plan reports exactly `1` deletion — the old
      Unreleased compare line — confirming no historical entry or link line was edited.
    - `git tag -l v0.6.5` prints nothing and `git ls-remote --tags origin v0.6.5` prints nothing.
  </acceptance_criteria>

  <done>
    The tail block carries a `[0.6.5]:` release-tag line directly above `[0.6.4]:`, the file's last
    line compares from v0.6.5 to HEAD, exactly one line in the whole file was removed, and no tag
    exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG prose -> the user's understanding of what shipped | Nothing in this repository machine-checks changelog prose, so an overstated or understated claim reaches readers unfiltered. |
| CHANGELOG entry -> future release-notes extraction | A todo filed by plan 35-02 proposes making this entry the GitHub Release body; a malformed section boundary would break that future extraction. |
| this plan -> the `v0.6.5` tag | The tail link block names a tag that does not exist yet. Writing the name must not slide into creating the tag. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-10 | Repudiation | the `### Verified` subsection | high | mitigate | Every one of the three Verified claims is independently re-measured and transcribed verbatim by plan 35-05 into `35-RELEASE-EVIDENCE.md`. A Verified bullet with no matching evidence section there is a phase-level gap, not a passing entry. |
| T-35-11 | Spoofing | the `### Fixed` subsection | medium | mitigate | The action pins the entry to the measured red-versus-green shape table from CONTEXT.md and forbids implying plain paragraphs were failing; acceptance criteria positively grep for the prescribed parenthetical and negatively grep for a compatibility-break label. |
| T-35-12 | Tampering | historical CHANGELOG entries and the legacy Unreleased heading | medium | mitigate | The whole-plan `git diff --numstat CHANGELOG.md` must report exactly one deletion, and the two-Unreleased-headings hazard is called out with a positional acceptance check on line 8. |
| T-35-13 | Elevation of privilege | the `v0.6.5` tag | high | mitigate | Task 2's automated verify and acceptance both assert `git tag -l v0.6.5` and `git ls-remote --tags origin v0.6.5` print nothing. Writing a link that names a tag is not creating one. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and edits one Markdown file. `35-RESEARCH.md` § Package Legitimacy Audit records the gate as not-applicable. |
</threat_model>

<verification>
- `grep -m1 '^## \[[0-9]' CHANGELOG.md` outputs the `## [0.6.5]` heading.
- `grep -c '^\[0\.6\.5\]: https://github\.com/YuSabo90002/typsphinx/releases/tag/v0\.6\.5$' CHANGELOG.md` returns 1.
- `tail -n 1 CHANGELOG.md` names the v0.6.5 compare range.
- `git diff --numstat CHANGELOG.md` reports exactly 1 deletion for the whole plan.
- `git diff --name-only` for the whole plan lists exactly one path: `CHANGELOG.md`.
- `git tag -l v0.6.5` and `git ls-remote --tags origin v0.6.5` both print nothing.
</verification>

<success_criteria>
ROADMAP Phase 35 SC#2 is discharged in both halves: `CHANGELOG.md` carries a curated `## [0.6.5]`
entry stating the inline-math fix in user-visible terms with the structure D-01 through D-04 fix, and
the tail link block is rolled over with a `[0.6.5]:` release-tag line added and the Unreleased compare
advanced to v0.6.5. No historical entry was edited and no tag was created.
</success_criteria>

<output>
Create `.planning/phases/35-v0-6-5-release-prep/35-04-SUMMARY.md` when done.
Reproduce the full `## [0.6.5]` section verbatim in the summary, and record the three `### Verified`
claims as a checklist — plan 35-05 must produce a matching evidence section for each one.
</output>
