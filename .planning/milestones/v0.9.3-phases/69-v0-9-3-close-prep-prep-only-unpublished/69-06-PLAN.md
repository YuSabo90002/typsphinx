---
phase: 69-v0-9-3-close-prep-prep-only-unpublished
plan: 06
type: execute
wave: 3
depends_on: ["69-02", "69-03", "69-04", "69-05"]
files_modified:
  - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md
  - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md
  - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-HANDOFF.md
autonomous: true
requirements: [REL-12]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC#1, observation 2 of 2: every probe of observation 1 is re-run at OBS2_AT, which is later than OBS1_AT, with the same positive controls, and the fence still holds. No v0.9.3 tag, PyPI release or GitHub Release exists, and no pull request has the milestone branch as its head (D-11).'
    - 'SC#1 phase-scoped fence: `git diff PHASE_BASE_SHA HEAD -- typsphinx/` is empty, and the same diff widened to every non-.planning path lists exactly CHANGELOG.md with zero deletions. That widened diff is the positive control.'
    - 'D-13 final tip: PUSHED_SHA from 69-CI-EVIDENCE.md is an ancestor of HEAD, and `git diff --name-only PUSHED_SHA HEAD` outside .planning/ is empty, so every commit after the single CI dispatch is planning-only. No second dispatch is warranted, and no release.yml run carries PUSHED_SHA.'
    - 'D-12 / SC#4, phase close: the live `sha256sum .planning/REQUIREMENTS.md` equals REQ_SHA256_BASE, and `git diff --name-only -- .planning/REQUIREMENTS.md` is empty. The REL-12 grep is byte-identical to the baseline, no commit since PHASE_BASE_SHA touched the file, and REQ_VERDICT_CLOSE = MATCH. REL-12 reads `- [ ]` and Pending, read directly from the file.'
    - 'D-09: the first 14 lines of 69-HANDOFF.md, before any `##` heading, state that the milestone publishes nothing: no tag, no PyPI upload, no GitHub Release, no version bump. They state that the update-pin.yml dispatch and the Read the Docs stable verification are not applicable, and that the one irreversible action is the REL-12 pull request.'
    - 'D-08: the handoff lists, in order, the concrete commands /gsd-complete-milestone runs. It merges origin/main into the canonical branch with `git merge --no-ff origin/main`, never a rebase, because `strict: true` requires it; re-syncs and records the merged ruff version, 0.16.x, consistent with NIX-01 per 67-CONTEXT D-03; pushes, and opens the pull request from gsd/v0.9.3-toolchain-and-dependency-update-repair to main. It then waits for the six named required checks on the updated head, and merges with `gh pr merge --merge`, citing #135 and #136.'
    - 'D-10 and D-11: the handoff names #139, #140, #141 and #142 with their measured states, left untouched and to be rebased by dependabot after REL-12. It records the 0.9.3 number as unclaimed, to be decided at the next milestone scoping, and says the next release-prep phase promotes the Unreleased bullets.'
    - 'SC#4 standalone handoff: 69-HANDOFF.md reproduces the third-observation procedure inline. It names both entry points (/gsd-execute-phase and /gsd-verify-work), gives the scratch backup of REQUIREMENTS.md, ROADMAP.md and STATE.md, the baseline digest, the REL-12 lines and `git checkout -- .planning/REQUIREMENTS.md`, and closes with a live fence observation.'
    - 'REL-12 is cited for coverage only; SUMMARY requirements-completed: [].'
  artifacts:
    - path: .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-HANDOFF.md
      provides: 'negative-first standalone handoff: ordered /gsd-complete-milestone steps, SC#1..SC#4 report, items recorded without acting, third-observation procedure, live fence observation'
      contains: 'publishes nothing'
    - path: .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md
      provides: 'observation 2 of 2, the phase-scoped typsphinx/ diff, commits after the CI dispatch'
      contains: 'OBS2_AT = '
    - path: .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md
      provides: 're-verification at phase close'
      contains: 'REQ_VERDICT_CLOSE = '
  key_links:
    - from: '69-CLOSEOUT-GUARD.md REQ_SHA256_BASE'
      to: '69-HANDOFF.md third-observation block'
      via: 'the digest written inline so the operator needs no other file after phase.complete-family tooling runs'
      pattern: 'REQ_SHA256_BASE = '
    - from: '69-PREFLIGHT-EVIDENCE.md MERGE_TREE, LOCK_CHECK_EXIT, PROTECTION_*'
      to: '69-HANDOFF.md branch-update step'
      via: 'cited as the pre-flight result, with the instruction to re-run merge-tree immediately before the real merge'
      pattern: 'MERGE_TREE = '
    - from: '69-CI-EVIDENCE.md PUSHED_SHA'
      to: 'the post-dispatch planning-only proof'
      via: 'git diff --name-only PUSHED_SHA HEAD outside .planning must be empty'
      pattern: 'PUSHED_SHA = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action: no push, tag, pull request, merge, release, workflow run, or comment on any pull request. The handoff records steps for /gsd-complete-milestone and executes none of them.'
      requirement_id: REL-12
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record REL-12 as satisfied: no checkbox flip, no Traceability change, no prose claiming the merge happened. A divergence found at close is reverted with git checkout and reported, never committed.'
      requirement_id: REL-12
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT write a command in the handoff that tags, releases, or dispatches the translations pin workflow, and MUST NOT treat 0.9.3 as a decided version.'
      requirement_id: REL-12
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT restate another evidence file verdict without citing its section, and MUST NOT copy a digest, SHA or count as a measurement without re-running the command here.'
      requirement_id: REL-12
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Close the phase's fence and write the milestone handoff:
- re-probe SC#1 at a genuinely later timestamp than observation 1
- prove with a positive control that nothing under `typsphinx/` changed across the phase
- prove that every commit after the single CI dispatch is planning-only (D-13)
- re-verify the REL-12 checksum guard at phase close (D-12)
- author `69-HANDOFF.md`: negative-first (D-09), with the ordered `/gsd-complete-milestone` steps
  including the mandatory branch update (D-08), the dependabot PRs and the version number recorded
  without acting (D-10, D-11), and the third-observation procedure reproduced inline (SC#4)

Purpose: this plan runs in wave 3, one wave after the CI dispatch (69-04), the green tree (69-03) and
the pre-flight (69-05). It is therefore the first plan that can read all their evidence, and its
observation is separated from observation 1 by two waves and a full CI run.

Output: `69-SC1-INVARIANTS.md` § observation 2; `69-CLOSEOUT-GUARD.md` § re-verification at phase
close; `69-HANDOFF.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CHANGELOG-EVIDENCE.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-GREEN-TREE-EVIDENCE.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CI-EVIDENCE.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PREFLIGHT-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and run project
tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line.

**Reserved name.** Never create `69-VERIFICATION.md`; `gsd-verifier` owns it and overwrites it
wholesale.
</worktree_provisioning>

<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.3 -->
<!-- planner-discipline-allow: gh release create|workflow run update-pin|git tag -a|git tag v -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
The version tag is named because the probes assert its absence. The release, pin-dispatch and tag
command shapes are forbidden in the handoff, and the gate asserts they are absent. `## HALT` is
written only on a failure path.

## Planning-time census (2026-09-13; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| REL-12 in REQUIREMENTS.md | checkbox at `:75` (`- [ ] **REL-12**: the milestone is merged to \`main\` via a PR, with no tag, no PyPI upload and no`); row `:147` (`\| REL-12 \| Phase 69 \| Pending \|`); informational `:164`, `:166` | `grep -n` |
| Required checks on `main` | `Test Python 3.12 on ubuntu-latest`, `Test Python 3.13 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`; `strict: true` | 69-PREFLIGHT-EVIDENCE.md (measured in wave 2) |
| Merge method | #135 and #136 are `Merge pull request` commits on `main`'s first-parent history | 69-CONTEXT D-08 |
| Main-checkout re-sync hazard | `uv sync --extra dev` is an exact sync and drops the docs extra in the main checkout; the documented restore is `--extra dev --extra docs` | 69-CONTEXT § Specific Ideas |
| Precedent handoff | `61-HANDOFF.md`, the unpublished-close shape; Phase 69 differs in that a PR to `main` is opened and merged | 69-RESEARCH § "Handoff shape to reproduce" |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: SC#1 observation 2 of 2, the phase-scoped typsphinx/ diff with its positive control, and the post-dispatch planning-only proof</name>
  <precondition>Plans 69-02 to 69-05 are merged into this worktree's HEAD. Their evidence files exist without a HALT heading, `69-CI-EVIDENCE.md` reads `SC3_CI_VERDICT = MET`, `69-GREEN-TREE-EVIDENCE.md` reads `SC3_LOCAL_VERDICT = MET`, and `69-PREFLIGHT-EVIDENCE.md` reads `MERGE_RC = 0`. Otherwise write `## HALT: wave 2 incomplete` naming the missing item, and stop.</precondition>
  <reversibility rating="reversible">Evidence appends recording read-only probes.</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md</files>
  <read_first>
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md (observation 1: `OBS1_AT`, the probes, § "Handoff to observation 2")
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md (`PHASE_BASE_SHA`)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CI-EVIDENCE.md (`PUSHED_SHA`, `RUN_ID`, § "D-13 final tip")
    - .planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md (observation 2, the scoped diff and the post-dispatch sections)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md D-11 and D-13
  </read_first>
  <action>
Append to `69-SC1-INVARIANTS.md`.

1. **`## Observation 2 of 2`.**
   - Record the head check and provisioning, then `BASE_69_06 = ` from `git rev-parse HEAD` (before any commit), then `OBS2_AT = ` from `date -u +"%Y-%m-%dT%H:%M:%SZ"`.
   - Re-run every probe of observation 1 in the same order, with the same positive controls, each as a command with its verbatim output. Use the same key names with an `OBS2_` prefix, for example `OBS2_REMOTE_V093_TAG`.
   - Quote `OBS1_AT` beside `OBS2_AT` and state the elapsed interval.
   - State that the fence held across the whole phase, including after the push and dispatch in 69-04.

2. **`## The phase-scoped typsphinx/ diff`.** Read `PHASE_BASE_SHA` from `69-CLOSEOUT-GUARD.md` and record:
   - `git diff "$PHASE_BASE_SHA" HEAD -- typsphinx/`, empty, key `PHASE_TYPSPHINX_DIFF = empty`
   - `git diff --numstat "$PHASE_BASE_SHA" HEAD -- . ':(exclude).planning'`, exactly one row, `CHANGELOG.md`, with a non-zero addition count and zero deletions, key `PHASE_PRODUCT_FILES = CHANGELOG.md`

   State why both are needed: the empty scoped diff is the claim, and the non-empty widened diff from the same anchor proves the anchor is real. This phase has no amended exception to the `typsphinx/` fence.

3. **`## Commits after the CI dispatch`.** Read `PUSHED_SHA` from `69-CI-EVIDENCE.md` and record:
   - `git merge-base --is-ancestor "$PUSHED_SHA" HEAD; echo "exit:$?"`, which must print `exit:0`
   - `git log --oneline "$PUSHED_SHA"..HEAD`
   - `git diff --name-only "$PUSHED_SHA" HEAD -- . ':(exclude).planning'`, empty, key `POST_DISPATCH_PRODUCT_FILES = 0`
   - `gh run list --workflow=release.yml --limit 20 --json headSha`, none at `PUSHED_SHA`
   - `gh run list --workflow=ci.yml --branch gsd/v0.9.3-toolchain-and-dependency-update-repair --event workflow_dispatch --limit 50 --json headSha,createdAt`, exactly one run at `PUSHED_SHA`

   State the D-13 reading: the single dispatch ran on the tip carrying every product-tree change of the phase, and every later commit is `.planning/`-only, which no CI job reads. D-13 permits a second dispatch only for a code-affecting change, so the post-dispatch commits are not a stale-tip violation.

4. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; S="$D/69-SC1-INVARIANTS.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; ks() { sed -n "s/^$1 = //p" "$S" | head -n 1; } &amp;&amp; g() { sed -n "s/^$1 = //p" "$D/69-CLOSEOUT-GUARD.md" | head -n 1; } &amp;&amp; c() { sed -n "s/^$1 = //p" "$D/69-CI-EVIDENCE.md" | head -n 1; } &amp;&amp; B="$(ks BASE_69_06)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; O1="$(ks OBS1_AT)" &amp;&amp; O2="$(ks OBS2_AT)" &amp;&amp; [ -n "$O1" ] &amp;&amp; [ -n "$O2" ] &amp;&amp; [[ "$O2" &gt; "$O1" ]] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; [ -z "$(git tag -l 'v0.9.3')" ] &amp;&amp; T="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.3')" = 0 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json)" = 200 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.3/json)" = 404 ] &amp;&amp; [ "$(gh release list --limit 20 --json tagName,isLatest --jq '.[] | select(.isLatest) | .tagName')" = v0.9.2 ] &amp;&amp; [ "$(gh pr list --head gsd/v0.9.3-toolchain-and-dependency-update-repair --state all --json number --jq 'length')" = 0 ] &amp;&amp; PB="$(g PHASE_BASE_SHA)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; [ -z "$(git diff "$PB" HEAD -- typsphinx/)" ] &amp;&amp; [ "$(git diff --name-only "$PB" HEAD -- . ':(exclude).planning')" = CHANGELOG.md ] &amp;&amp; P="$(c PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$P" HEAD &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha --jq '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; [ "$(ks POST_DISPATCH_PRODUCT_FILES)" = 0 ] &amp;&amp; for s in '## Observation 2 of 2' '## The phase-scoped typsphinx/ diff' '## Commits after the CI dispatch'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e "$S" -e "$D/69-CLOSEOUT-GUARD.md" -e "$D/69-HANDOFF.md" -e "$D/69-06-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$S"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_69_06` missing
- `OBS2_AT` empty or not later than `OBS1_AT`
- `pyproject.toml:7` not 0.9.2
- the v0.9.2 positive controls missing, or any v0.9.3 tag, PyPI release or pull request from the branch present
- the latest release not v0.9.2
- `PHASE_BASE_SHA` unreadable, a typsphinx/ change since it, or the widened diff not exactly CHANGELOG.md
- `PUSHED_SHA` unreadable, not an ancestor of HEAD, or followed by a product-tree change
- a release.yml run at `PUSHED_SHA`
- `POST_DISPATCH_PRODUCT_FILES` not 0
- another tracked file changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Observation 2 of 2` records `OBS2_AT`, later than `OBS1_AT`, with the elapsed interval, and repeats every probe of observation 1 with its positive control.
    - `## The phase-scoped typsphinx/ diff` shows the empty scoped diff and the widened `--numstat` diff listing exactly `CHANGELOG.md` with zero deletions.
    - `## Commits after the CI dispatch` shows `PUSHED_SHA` as an ancestor, the post-dispatch commit list, `POST_DISPATCH_PRODUCT_FILES = 0`, no release.yml run at `PUSHED_SHA`, one CI dispatch at it, and the D-13 reading.
  </acceptance_criteria>
  <done>The fence held across the whole phase at two separated timestamps. The `typsphinx/` diff is empty against a proven anchor, and the single CI run covers every product-tree change of the phase.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify the REL-12 closeout guard at phase close, reverting any divergence by hand</name>
  <precondition>Task 1 is committed with no HALT heading.</precondition>
  <reversibility rating="reversible">Evidence appends, plus — only on divergence — a `git checkout` restoring a tracked file to its committed state.</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md</files>
  <read_first>
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md (§ "Baseline", § "The lines under guard", § "Re-verification protocol (phase close)", § "For the operator running phase.complete")
    - .planning/REQUIREMENTS.md (the live REL-12 lines)
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md § "Re-verification at phase close" (the comparison-table shape)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md D-12
  </read_first>
  <action>
Append `## Re-verification at phase close` to `69-CLOSEOUT-GUARD.md`. Run exactly the commands the file's own protocol names, and record each with its output and a MATCH or DIVERGED verdict against the Baseline:
- `date -u +"%Y-%m-%dT%H:%M:%SZ"`, key `CLOSE_AT`
- `sha256sum .planning/REQUIREMENTS.md`, key `REQ_SHA256_CLOSE`, set in a two-row table beside `REQ_SHA256_BASE`
- `wc -l < .planning/REQUIREMENTS.md`, key `REQ_LINES_CLOSE`
- `git diff --name-only -- .planning/REQUIREMENTS.md`, empty
- `git log --oneline "$PHASE_BASE_SHA"..HEAD -- .planning/REQUIREMENTS.md`, empty: no commit in the phase touched the file
- `grep -n 'REL-12' .planning/REQUIREMENTS.md`, compared line by line with § "The lines under guard"

Write `REQ_VERDICT_CLOSE = MATCH` only if every comparison holds. Then quote REL-12's checkbox line and its Traceability row as read directly from the file (`- [ ]`, `Pending`), never inferred from any SUMMARY frontmatter. Every plan of this phase declares `requirements-completed: []`.

**On any divergence:**
- Record the verbatim `git diff -- .planning/REQUIREMENTS.md`.
- Run `git checkout -- .planning/REQUIREMENTS.md`, then re-run the checksum and the grep to show the Baseline restored.
- Record the sequence under `### Divergence detected and reverted`, write `REQ_VERDICT_CLOSE = REVERTED`, and report it. Never commit the divergence.

Close the section by confirming that § "For the operator running phase.complete" is present and unedited. The decisive third observation runs after this plan, outside any plan's reach, and 69-HANDOFF.md reproduces it.

Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; G="$D/69-CLOSEOUT-GUARD.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; k() { sed -n "s/^$1 = //p" "$G" | head -n 1; } &amp;&amp; PB="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(k REQ_SHA256_CLOSE)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(k REQ_LINES_CLOSE)" = "$(k REQ_LINES_BASE)" ] &amp;&amp; [ -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" ] &amp;&amp; [ -z "$(git log --format=%h "$PB"..HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; grep -qE '^- \[ \] \*\*REL-12\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-12 \| Phase 69 \| Pending \|' .planning/REQUIREMENTS.md &amp;&amp; [ "$(grep -c 'REL-12' .planning/REQUIREMENTS.md)" = "$(k REL12_HITS_BASE)" ] &amp;&amp; [ "$(k REQ_VERDICT_CLOSE)" = MATCH ] &amp;&amp; [ -n "$(k CLOSE_AT)" ] &amp;&amp; grep -qxF '## Re-verification at phase close' "$G" &amp;&amp; grep -qxF '## For the operator running phase.complete' "$G" &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -q '^## HALT' "$G"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- `PHASE_BASE_SHA` unreadable
- the live digest or `REQ_SHA256_CLOSE` differing from `REQ_SHA256_BASE`, or `REQ_LINES_CLOSE` differing from `REQ_LINES_BASE`
- a working-tree change to REQUIREMENTS.md, or any commit since the phase base touching it
- the REL-12 checkbox checked, or its row not Pending, or the hit count changed
- `REQ_VERDICT_CLOSE` not MATCH
- `CLOSE_AT` or either section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Re-verification at phase close` records `CLOSE_AT`, `REQ_SHA256_CLOSE` beside `REQ_SHA256_BASE`, `REQ_LINES_CLOSE`, the empty name-only diff, the empty `git log` over the file since `PHASE_BASE_SHA`, and the REL-12 grep compared line by line, each with a MATCH or DIVERGED verdict.
    - `REQ_VERDICT_CLOSE = MATCH`, and REL-12 is quoted as `- [ ]` and `Pending` from the file itself.
    - Any divergence is recorded under `### Divergence detected and reverted`, reverted with `git checkout -- .planning/REQUIREMENTS.md`, and never committed.
  </acceptance_criteria>
  <done>The REL-12 fence is re-verified at phase close with an explicit verdict, and the decisive third observation is left, fully specified, for the operator.</done>
</task>

<task type="auto">
  <name>Task 3: Author 69-HANDOFF.md: negative-first, the ordered /gsd-complete-milestone steps with the mandatory branch update, the items recorded without acting, and the third observation reproduced inline</name>
  <precondition>Tasks 1 and 2 are committed, and `REQ_VERDICT_CLOSE = MATCH`.</precondition>
  <reversibility rating="reversible">One new planning document. It records steps for a later command and performs none of them.</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-HANDOFF.md</files>
  <read_first>
    - .planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md (the unpublished-close shape: negative opening, per-SC report, "Before declaring the milestone closed", "Fence observation", "What this phase deliberately did not do")
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md D-06, D-08, D-09, D-10, D-11, D-12, D-13 and § "Specific Ideas"
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-RESEARCH.md § "Handoff shape to reproduce" (where this handoff must differ from 61's: a PR to `main` is opened and merged)
    - every evidence file of this phase: 69-CHANGELOG-EVIDENCE.md, 69-CLOSEOUT-GUARD.md, 69-SC1-INVARIANTS.md, 69-GREEN-TREE-EVIDENCE.md, 69-CI-EVIDENCE.md, 69-PREFLIGHT-EVIDENCE.md (the sections to cite)
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CONTEXT.md D-03 (NIX-01 after REL-12)
    - .planning/REQUIREMENTS.md (REL-12, quoted verbatim)
  </read_first>
  <action>
Create `69-HANDOFF.md`. Its first line is `# Phase 69: v0.9.3 Close Prep — Handoff for /gsd-complete-milestone`.

**The opening (D-09).** Lines 2 to 14 contain no `##` heading. In plain words they state:
- this milestone publishes nothing: no tag, local or remote; no PyPI upload; no GitHub Release; no version bump, since `pyproject.toml` stays `0.9.2`
- the `typsphinx-doc-translations` `update-pin.yml` dispatch and the Read the Docs `stable` verification are not applicable, because nothing is released
- the one irreversible action of the close is REL-12: a pull request from `gsd/v0.9.3-toolchain-and-dependency-update-repair` to `main`, opened and merged at `/gsd-complete-milestone`

This inverts `61-HANDOFF.md` on that last point: v0.9.1 opened no pull request, and this milestone does.

Then these sections, in order.

**1. `## What /gsd-complete-milestone does, in order` (D-08).** Numbered steps, each with its exact command and one line of why:

0. **Fence first.** Back up `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` to a scratch directory outside the repository. Then run the section 4 probes, and start only on a MATCH.

1. **Re-run the trial merge.**
   - `git fetch origin`, then `git merge-tree --write-tree HEAD origin/main; echo "exit:$?"`, which requires `exit:0`.
   - Cite 69-PREFLIGHT-EVIDENCE.md's `MERGE_TREE`, `LOCK_CHECK_EXIT` and `MERGED_RUFF_LOCK_VERSION` as the phase-time result. That tree SHA changes with every commit, so it is re-measured here.
   - If `origin/main` has moved past `ORIGIN_MAIN_SHA`, re-run the scratch `uv lock --check` exactly as 69-PREFLIGHT § "Merged lock" did.

2. **Merge `origin/main` into the branch.** In the main checkout, on `gsd/v0.9.3-toolchain-and-dependency-update-repair`, run `git merge --no-ff origin/main -m "Merge origin/main into gsd/v0.9.3-toolchain-and-dependency-update-repair"`. Never rebase the milestone's commits. The reason: `main`'s protection is `strict: true` (69-PREFLIGHT § "main protection"), so the pull request cannot merge until the branch is up to date. D-06 deferred this merge to here.

3. **Check the lock and re-sync.**
   - `uv lock --check`, then `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`. The docs extra is named because a bare `--extra dev` exact sync drops it from the main checkout.
   - `ruff --version` now reports the merged lock's version (0.16.x). That is consistent with NIX-01 per 67-CONTEXT D-03: NIX-01's binding sense is the version `uv.lock` pins.
   - If 69-PREFLIGHT recorded a `## FINDING: merged-tree lint`, name it here as the owner's decision before step 5.

4. **Push.** `git push --no-follow-tags origin gsd/v0.9.3-toolchain-and-dependency-update-repair`, a fast-forward from `PUSHED_SHA`. It also carries this phase's later `.planning/` commits.

5. **Open the pull request.** `gh pr create --base main --head gsd/v0.9.3-toolchain-and-dependency-update-repair --title <English title> --body-file <file>`. Title and body are English and terse. The body goes through a file, never through `$(…)`.

6. **Wait for the required checks.** Wait for all six to be green on the updated head, named literally: `Test Python 3.12 on ubuntu-latest`, `Test Python 3.13 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage` and `Build Package`. The pull_request trigger runs the whole twelve-job `ci.yml`, which should be green throughout. The six are the ones protection requires.

7. **Merge.** `gh pr merge <number> --merge`: a merge commit, never squash or rebase. Cite 69-PREFLIGHT § "Merge-method precedent", where #135 and #136 are `Merge pull request` commits.

8. **Observe what REL-12 requires, then close it.**
   - The merge commit is on `origin/main`.
   - `git show origin/main:pyproject.toml | sed -n 7p` reads `version = "0.9.2"`.
   - `git ls-remote --tags origin` shows v0.9.2 and no v0.9.3.
   - PyPI returns 404 for 0.9.3 and 200 for 0.9.2.
   - `gh release list` shows no v0.9.3.

   Only on those observations is REL-12's box checked, at `/gsd-complete-milestone`, never inside a plan.

9. **Not applicable.** No tag; no `release.yml` run; no PyPI upload; no GitHub Release; no `update-pin.yml` dispatch on `typsphinx-doc-translations`; no Read the Docs `stable` check. Read the Docs `latest` rebuilds from `main` and will then show the `## [Unreleased]` bullets, which needs no action.

**2. `## What this phase satisfied`.** Quote REL-12 verbatim from `.planning/REQUIREMENTS.md` and state that it stays open until step 8. Report each ROADMAP SC#1 to SC#4 as MET or NOT MET, citing each evidence file and section rather than re-deriving the verdict:
- SC#1: `69-SC1-INVARIANTS.md`, both observations and both diffs
- SC#2: `69-CHANGELOG-EVIDENCE.md` § "Fence assertions" and § "Pure-addition proof"
- SC#3: `69-GREEN-TREE-EVIDENCE.md` and `69-CI-EVIDENCE.md`, set side by side. The table carries the local suite summary, the `LC_ALL=C` summary, local ruff version versus `CI_RUFF_VERSION`, and the docs `_FINAL` counts versus the `_BASE` counts, plus `RUN_ID` with 12/12 and both `windows-latest` lanes named.
- SC#4: `69-CLOSEOUT-GUARD.md` § "Re-verification at phase close", and this file

Add the D-13 statement: the single dispatch ran on `PUSHED_SHA`, the tip carrying every product-tree change of the phase, and every later commit is `.planning/`-only (`69-SC1-INVARIANTS.md` § "Commits after the CI dispatch").

**3. `## Recorded without acting`.**
- **D-10:** a table of #139, #140, #141 and #142 with each one's state, base and title as measured in 69-PREFLIGHT § "Dependabot PRs" at `PR_CENSUS_AT`. They target `main`, this phase took no action on them, and dependabot rebases them once REL-12 changes `uv.lock`. Their disposition is ordinary dependency maintenance after REL-12.
- **D-11:** the `0.9.3` version number is unclaimed, not decided. No v0.9.3 tag exists and none is created. Whether the next published release is 0.9.3 is the next milestone's scoping, and the next release-prep phase promotes this phase's `## [Unreleased]` bullets into its versioned section, as Phase 63 did with Phase 61's.

**4. `## Before and after phase.complete-family tooling`.** Reproduce `69-CLOSEOUT-GUARD.md` § "For the operator running phase.complete" inline:
- Both entry points are named: `phase.complete` reached from `/gsd-execute-phase`, and `/gsd-verify-work`'s inline transition.
- The scratch backup of the three files comes before either.
- The probes follow, with the baseline digest (`REQ_SHA256_BASE`) and the REL-12 grep lines written out: `sha256sum .planning/REQUIREMENTS.md`, `git diff --name-only -- .planning/REQUIREMENTS.md` and `grep -n 'REL-12' .planning/REQUIREMENTS.md`.
- The reversion is `git checkout -- .planning/REQUIREMENTS.md`, and ROADMAP.md and STATE.md are diffed against the backup.
- The rule: reverted and reported, never committed.
- State that the flip has landed at seven release-prep closes, the last twice at the Phase 63 close, once through each entry point (`63-CLOSEOUT-GUARD.md`).

**5. `## Fence observation`.** Record live, at this task's own time:
- `git diff --name-only -- .planning/REQUIREMENTS.md`, empty
- `sha256sum .planning/REQUIREMENTS.md`, equal to `REQ_SHA256_BASE`
- `git tag -l 'v0.9.3'`, empty
- `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml uv.lock .planning/REQUIREMENTS.md`, empty

**6. `## What this phase deliberately did not do`.** A bullet list:
- no tag, local or remote
- no `release.yml` run
- nothing uploaded to PyPI
- no GitHub Release
- no pull request opened, merged or commented on
- no `origin/main` merged into the branch (D-06)
- no `update-pin.yml` dispatch
- no action on #139..#142
- no REQUIREMENTS.md checkbox or Traceability change
- one push and one `ci.yml` dispatch only, in 69-04 (D-13)

The handoff contains no command that tags, creates a release, or dispatches the translations pin workflow.

Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; H="$D/69-HANDOFF.md" &amp;&amp; G="$D/69-CLOSEOUT-GUARD.md" &amp;&amp; test -f "$H" &amp;&amp; k() { sed -n "s/^$1 = //p" "$G" | head -n 1; } &amp;&amp; O="$(head -n 14 "$H")" &amp;&amp; printf '%s\n' "$O" | grep -qi 'publishes nothing' &amp;&amp; for w in 'no tag' 'PyPI' 'GitHub Release' 'version bump' 'update-pin.yml' 'Read the Docs' 'not applicable' 'pull request'; do printf '%s\n' "$O" | grep -qiF -- "$w" || { echo "opening lacks: $w"; exit 1; }; done &amp;&amp; [ "$(printf '%s\n' "$O" | grep -cE '^#{2,6} ')" = 0 ] &amp;&amp; for n in 'Test Python 3.12 on ubuntu-latest' 'Test Python 3.13 on ubuntu-latest' 'Lint and Format Check' 'Type Check' 'Code Coverage' 'Build Package' 'git merge --no-ff origin/main' 'gh pr merge' '--merge' 'strict: true' '#135' '#136' '#139' '#140' '#141' '#142' 'unclaimed' '/gsd-execute-phase' '/gsd-verify-work' 'git checkout -- .planning/REQUIREMENTS.md' 'merge-tree' '--extra dev --extra docs' '69-PREFLIGHT-EVIDENCE.md' '69-CI-EVIDENCE.md' '69-GREEN-TREE-EVIDENCE.md' '69-CHANGELOG-EVIDENCE.md' '69-SC1-INVARIANTS.md' '69-CLOSEOUT-GUARD.md' '**REL-12**: the milestone is merged to `main` via a PR, with no tag, no PyPI upload and no'; do grep -qF -- "$n" "$H" || { echo "handoff lacks: $n"; exit 1; }; done &amp;&amp; grep -qF "$(k REQ_SHA256_BASE)" "$H" &amp;&amp; ! grep -qE 'gh release create|workflow run update-pin|git tag -a|git tag v' "$H" &amp;&amp; for s in '## What /gsd-complete-milestone does, in order' '## What this phase satisfied' '## Recorded without acting' '## Before and after phase.complete-family tooling' '## Fence observation' '## What this phase deliberately did not do'; do grep -qxF "$s" "$H" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.3')" ] &amp;&amp; test ! -e "$D/69-VERIFICATION.md" &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml uv.lock .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md)" ]</automated>
    <fails_when>Non-zero exit, or `opening lacks: <word>`, `handoff lacks: <text>` or `missing section: <name>` printed. Each of these makes it fail:
- the first 14 lines not stating that the milestone publishes nothing
- any of the negative items, the not-applicable pair or the pull request missing from those lines
- a `##` heading within those lines
- a required check name, the merge or pull-request commands, the strict reason, the precedent PRs, the dependabot PRs, the unclaimed version, either entry point, the reversion, the re-sync extras, an evidence citation or the verbatim REL-12 line missing
- the baseline digest absent
- a tag, release-creation or pin-dispatch command present
- a section missing
- the live REQUIREMENTS.md digest diverged
- a v0.9.3 tag locally
- `69-VERIFICATION.md` authored
- a product or tracking file modified</fails_when>
  </verify>
  <acceptance_criteria>
    - Lines 1 to 14 of `69-HANDOFF.md` state the negative before any `##` heading: no tag, PyPI upload, GitHub Release or version bump; `update-pin.yml` and Read the Docs `stable` not applicable; the REL-12 pull request is the one irreversible action (D-09).
    - `## What /gsd-complete-milestone does, in order` gives steps 0 to 9 with exact commands, including `git merge --no-ff origin/main` with the `strict: true` reason, the `--extra dev --extra docs` re-sync, the merged ruff version and NIX-01 note, the six required checks named literally, and `gh pr merge --merge` citing #135 and #136 (D-08).
    - `## What this phase satisfied` quotes REL-12 verbatim and reports SC#1 to SC#4, each citing its evidence section. The SC#3 local and CI results are set side by side, and the D-13 final-tip statement is included.
    - `## Recorded without acting` tabulates #139..#142 with measured states (D-10) and records 0.9.3 as unclaimed (D-11).
    - `## Before and after phase.complete-family tooling` names both entry points and gives the backup, the digest, the REL-12 lines and the reversion inline.
    - `## Fence observation` is live and matches the Baseline, and `## What this phase deliberately did not do` lists every non-action.
    - No command that tags, creates a release or dispatches the pin workflow appears anywhere in the file.
  </acceptance_criteria>
  <done>`69-HANDOFF.md` stands alone. It opens by stating that nothing is published, and it gives the operator every `/gsd-complete-milestone` step in order, the checks to wait for, the branch update the protection demands, the items left untouched, and the fence to run after the tooling.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's single spec-less edge item (REL-12, concurrency, **unresolved**) and the prohibition
recall (P1..P6) are accounted in `69-01-PLAN.md`. This plan carries P1, P2, P3 and P5. It carries
assumptions (f) and (g) of that edge into the handoff:
- `merge-tree` is re-run immediately before the real merge, because `origin/main` may move.
- The dependabot PR states are reported with their measurement time, not as standing facts.

It also carries guarantee (d): the decisive third observation of the checksum fence, reproduced
where the operator reaches it after every plan has finished. Phase accounting: 1 surfaced = 0 truths
+ 1 flagged + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → origin, GitHub API, PyPI (read-only) | observation 2's probes; nothing is written |
| `.planning/REQUIREMENTS.md` → `phase.complete`-family tooling after this plan | the flip lands outside every plan's reach |
| `69-HANDOFF.md` → the operator at `/gsd-complete-milestone` | read as instructions; a mis-framed step causes a real publish or a rebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-23 | Elevation of Privilege | a handoff read as licence to tag, release or dispatch the pin workflow for a milestone that publishes nothing (ASVS V4, L1, by analogy) | high | mitigate | Negative-first opening gated on its first 14 lines; step 9 lists the not-applicable actions; the verify asserts no tag, release-creation or pin-dispatch command anywhere in the file. |
| T-69-24 | Tampering | the REL-12 flip landing after this plan through either transition entry point | high | mitigate | Close-time re-verification with an explicit verdict, and the third observation reproduced inline with both entry points, a scratch backup, the digest and the `git checkout` reversion. |
| T-69-25 | Repudiation | an empty `typsphinx/` diff accepted from a wrong anchor (ASVS V7, L1) | high | mitigate | The scoped empty diff is paired with a widened diff from the same `PHASE_BASE_SHA` that must list exactly `CHANGELOG.md`. |
| T-69-26 | Repudiation | post-dispatch commits read as a stale-tip violation, or a product change slipping in after the dispatch | medium | mitigate | `git diff --name-only PUSHED_SHA HEAD` outside `.planning/` must be empty; the D-13 reading is written into both the invariants file and the handoff. |
| T-69-27 | Tampering | a rebase or squash at close rewriting the milestone's history, or merging before protection is satisfied | medium | mitigate | Step 2 names `git merge --no-ff origin/main` and forbids a rebase; step 7 names `gh pr merge --merge`; both cite measured precedent and the `strict: true` reason. |
| T-69-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins; the handoff's re-sync step does the same after the merge. |
</threat_model>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`69-HANDOFF.md`. It appends observation 2, the phase-scoped diff and the post-dispatch commits to
`69-SC1-INVARIANTS.md`, and the close-time re-verification to `69-CLOSEOUT-GUARD.md`. It modifies no
product-tree file and never edits `.planning/REQUIREMENTS.md`, except to revert a divergence. No
file named `69-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- Two SC#1 observations at separated timestamps, each remote probe positively controlled.
- The phase-scoped `typsphinx/` diff is empty against a proven anchor, and every post-dispatch commit
  is planning-only.
- The REL-12 digest matches the phase-head Baseline at close, and REL-12 is `[ ]` and Pending.
- `69-HANDOFF.md` opens negative-first, lists the ordered close steps with the strict-protection
  branch update, records #139..#142 and 0.9.3 without acting, and reproduces the third observation.
</verification>

<success_criteria>
1. ROADMAP SC#1: the tree is proven unpublished-shaped twice, with positive controls, and the
   `typsphinx/` fence is measured.
2. ROADMAP SC#4: the REL-12 checkbox is proven held at phase close by the recorded SHA-256, and
   `69-HANDOFF.md` is standalone, carrying the PR, the checks, the merge, the explicit no-publish
   statement and both not-applicable items (D-08..D-13).
</success_criteria>

<output>
Create `.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-06-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`.
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
- The first section states `OBS1_AT`, `OBS2_AT`, `PHASE_PRODUCT_FILES`,
  `POST_DISPATCH_PRODUCT_FILES`, `REQ_VERDICT_CLOSE` and the handoff path. It ends by reminding the
  orchestrator that the third fence observation, with a scratch backup first, is owed after
  `phase.complete` and again after `/gsd-verify-work`'s transition.
</output>
