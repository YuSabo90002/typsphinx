---
phase: 69-v0-9-3-close-prep-prep-only-unpublished
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md
  - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md
  - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/COVERAGE.md
autonomous: true
requirements: [REL-12]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'D-12 / SC#4 baseline at phase head: 69-CLOSEOUT-GUARD.md records `PHASE_BASE_SHA`, `REQ_SHA256_BASE`, `REQ_LINES_BASE`, `REL12_HITS_BASE` and `GUARD_AT` as bare key lines, before this plan commits anything. It also records the verbatim `grep -n REL-12 .planning/REQUIREMENTS.md` output, each hit classified as state-bearing (checkbox line, Traceability row) or informational. The recorded digest equals the live `sha256sum`.'
    - 'D-12: the guard names both transition entry points at which a release requirement has flipped: `phase.complete` reached from `/gsd-execute-phase`, and the inline transition of `/gsd-verify-work`. It instructs a scratch backup of REQUIREMENTS.md, ROADMAP.md and STATE.md before either runs, and prescribes `git checkout -- .planning/REQUIREMENTS.md` as the only response to a divergence: reverted and reported, never committed.'
    - 'REL-12 is measurably unmet at phase head: its checkbox line reads `- [ ] **REL-12**` and its Traceability row reads `| REL-12 | Phase 69 | Pending |`. This plan reads REQUIREMENTS.md and never edits it.'
    - 'SC#1, observation 1 of 2 (D-11: no v0.9.3 tag exists and none is created). pyproject.toml:7 reads `version = "0.9.2"`, and no commit since the milestone base changes a `version = ` line in pyproject.toml. No v0.9.3 tag exists locally or on origin, and `refs/tags/v0.9.2` appears once in the same unfiltered `git ls-remote` listing as the positive control. PyPI answers 200 for 0.9.2 and 404 for 0.9.3, the latest GitHub release is v0.9.2 and none is tagged v0.9.3, and no pull request has the milestone branch as its head.'
    - 'SC#1 release.yml probe: the listing contains the v0.9.2 publish run 33318905691 as the positive control, and no run created since 2026-09-02 or on a v0.9.3 ref.'
    - 'SC#1 constraint-13 fence: the diff from the milestone base (the merge-base of HEAD and origin/main) to HEAD is empty under typsphinx/. The same diff widened to every non-.planning path except CHANGELOG.md is non-empty: the positive control that the anchor is real.'
    - 'COVERAGE.md opens with `No external API integration: ` and a reason and records the detector result verbatim, and `check api-coverage.verify-pre` reports `"passed": true` for this phase directory.'
    - 'REL-12 is cited for coverage only; this plan SUMMARY declares requirements-completed: [].'
  artifacts:
    - path: .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md
      provides: 'phase-head REQUIREMENTS.md digest, line count, REL-12 hits, PHASE_BASE_SHA, and the close-time and post-transition procedures'
      contains: 'PHASE_BASE_SHA = '
    - path: .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md
      provides: 'fence observation 1 of 2 with positive controls, and the milestone typsphinx/ fence'
      contains: 'OBS1_AT = '
    - path: .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/COVERAGE.md
      provides: 'reasoned no-external-API declaration for the seal-time gate'
      contains: 'No external API integration: '
  key_links:
    - from: '.planning/REQUIREMENTS.md at phase head'
      to: '69-06 close-time re-verification and the operator third observation reproduced in 69-HANDOFF.md'
      via: 'REQ_SHA256_BASE compared byte-for-byte; a flip leaves wc -l unchanged, so the digest is the primary probe'
      pattern: 'REQ_SHA256_BASE = '
    - from: 'PHASE_BASE_SHA'
      to: '69-03 product-tree delta and 69-06 phase-scoped typsphinx/ diff'
      via: 'KEY = value line read with sed'
      pattern: 'PHASE_BASE_SHA = '
    - from: 'observation 1 (wave 1)'
      to: 'observation 2 (69-06, wave 3)'
      via: 'two UTC timestamps two waves and one CI dispatch apart'
      pattern: 'OBS1_AT = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action. Every command in this plan is a read, and it creates no tag, release, pull request, push or workflow run.'
      requirement_id: REL-12
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record REL-12 as satisfied: no checkbox flip, no Traceability status change, no prose claiming the merge happened. REQUIREMENTS.md is read and quoted, never edited.'
      requirement_id: REL-12
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record a negative probe without its positive control, or copy a digest, SHA or count from RESEARCH, CONTEXT or this plan as a measurement.'
      requirement_id: REL-12
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Record, at phase head and before any other plan's commit reaches the branch, the anchors this phase's
fence proof needs:
- the `.planning/REQUIREMENTS.md` checksum guard for REL-12 (D-12)
- the phase base SHA
- the first of two separated SC#1 observations that nothing is published, each remote negative
  paired with a positive control (D-11)
- the reasoned external-API declaration the seal-time gate reads

Purpose: a checksum recorded after another plan has run cannot prove the file was untouched at
phase head, and a fence observation taken beside its sibling is one observation, not two. This plan
is deliberately thin and early. It runs in parallel with 69-01, which touches only `CHANGELOG.md`
and its own evidence file.

Output: `69-CLOSEOUT-GUARD.md`, `69-SC1-INVARIANTS.md` (observation 1 of 2) and `COVERAGE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-RESEARCH.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Before running any command, provision this worktree with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then prefix project tooling with
`uv run` (`CLAUDE.md` § "Worktree-isolated execution"). This plan runs no test suite, but the rule is
unconditional.

**Evidence key lines.** Every `KEY = value` line carries the bare value only, with any explanation on
the next line: verify commands read values with `sed -n "s/^KEY = //p"` and compare them exactly.

**GSD tooling is not tracked.** `.claude/gsd-core/` is absent from a worktree. Call it by absolute
path: `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs` and
`node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/lib/api-coverage.cjs`.
</worktree_provisioning>

<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.3 -->
<!-- planner-discipline-allow: capability -->
The version tag is named because every probe asserts its absence. The word `capability` is named
because the declaration must not be a capability matrix.

## Planning-time census (2026-09-13, main checkout at `b2fea682`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `.planning/REQUIREMENTS.md` | `sha256` `02cb9deb614af2bd89e33adc5d0b640a489f85e616ba74aa1eb419e5b92a232a`, 172 lines | `sha256sum`, `wc -l` |
| `grep -n 'REL-12'` hits | 4. State-bearing: `:75` checkbox and `:147` Traceability row. Informational: `:164` phase-distribution row and `:166` coverage-only note | `grep -n` |
| Milestone base | `git merge-base HEAD origin/main` = `6181768f64b4cee62a77ac4e26c60c3c976cbb6e`; `origin/main` carries 4 commits the branch lacks (`7cc85d28`, `293f0c26`, `88088071`, `cf3305ce`) | `git merge-base`, `git log HEAD..origin/main` |
| Local tags | `v0.9.0`, `v0.9.2`; no `v0.9.3` | `git tag -l 'v0.9*'` |
| `release.yml` runs | newest is `33318905691` (2026-08-30, `v0.9.2`, success); none since | `gh run list --workflow=release.yml` |
| GitHub releases | `v0.9.2` is Latest | `gh release list` |
| PRs from the milestone branch | 0 | `gh pr list --head … --state all` |
| Flip history | landed at seven release-prep closes, the last twice at the Phase 63 close: via `/gsd-execute-phase`'s `phase.complete`, then via `/gsd-verify-work`'s inline transition. Each time it was reverted. | `63-CLOSEOUT-GUARD.md` § "Third observation" and § "Fourth observation"; ROADMAP constraint 14 |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: record the phase-head REQUIREMENTS.md closeout guard, PHASE_BASE_SHA and both operator procedures</name>
  <reversibility rating="reversible">One new planning document; no product-tree or requirement-state change.</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the REL-12 checkbox line, the Traceability row, and the two informational lines, read so the quotes are byte-accurate)
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md (the structure to mirror; § "Third observation" and § "Fourth observation" for the two entry points and the widened blast radius)
    - .planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md (the single-requirement shape)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md D-12 and § "Specific Ideas" second bullet
    - .planning/ROADMAP.md binding constraint 14
  </read_first>
  <action>
Create `69-CLOSEOUT-GUARD.md` with the heading `# Phase 69 — REL-12 Checkbox-Flip Closeout Guard` and a one-line statement: this file changes no requirement state, and `.planning/REQUIREMENTS.md` is read and quoted, never edited.

1. **`## Baseline`.** Record these before this plan's first commit, each command with its output, then its key line:
   - `git rev-parse HEAD`, key `PHASE_BASE_SHA`
   - `git log -1 --format='%H %s'`, where the subject must carry no `(69-0N)` plan scope
   - `sha256sum .planning/REQUIREMENTS.md`, key `REQ_SHA256_BASE` (digest only)
   - `wc -l < .planning/REQUIREMENTS.md`, key `REQ_LINES_BASE`
   - `grep -c 'REL-12' .planning/REQUIREMENTS.md`, key `REL12_HITS_BASE`
   - `date -u +"%Y-%m-%dT%H:%M:%SZ"`, key `GUARD_AT`

   Then cross-check against the planning-time values in this plan's census: digest `02cb9deb…a232a`, 172 lines, 4 hits.
   - If they agree, say so.
   - If they differ, record both values, run `git log --oneline -3 -- .planning/REQUIREMENTS.md` and `git diff "$(git log -1 --format=%H -- .planning/REQUIREMENTS.md)~1" -- .planning/REQUIREMENTS.md`, and quote the change.
   - If REL-12's checkbox is not `- [ ]` or its row is not `Pending`, write `## HALT: REL-12 already moved before phase head` and stop.
   - Otherwise keep your fresh values as the baseline, with the explanation beneath.

2. **`## The lines under guard`.** Paste the verbatim `grep -n 'REL-12' .planning/REQUIREMENTS.md` in a fenced block. Classify each hit:
   - state-bearing: the `- [ ] **REL-12**` checkbox line and the `| REL-12 | Phase 69 | Pending |` row
   - informational: the phase-distribution row and the coverage-only note

   Explain in two sentences why the whole-file SHA-256 is the primary probe:
   - a flip is line-count-neutral
   - Phase 63's flip moved three requirements at once, which a grep scoped to one ID would have missed

3. **`## Why this file exists`.** State the history from `63-CLOSEOUT-GUARD.md` and constraint 14, without inventing a count:
   - the flip has landed at seven release-prep closes
   - the last landing came twice at the Phase 63 close, once through `phase.complete` from `/gsd-execute-phase` and once through `/gsd-verify-work`'s inline transition
   - each flip was reverted
   - REL-12 is again a requirement meant to close only at `/gsd-complete-milestone`

4. **`## Re-verification protocol (phase close)`.** Give the exact commands plan 69-06 runs, and what each must show:
   - `sha256sum`, compared with `REQ_SHA256_BASE`
   - `wc -l`
   - `git diff --name-only -- .planning/REQUIREMENTS.md`, expected empty
   - `grep -n 'REL-12'`, expected byte-identical to § "The lines under guard"

5. **`## For the operator running phase.complete`.** This section applies after this phase's plans have finished, at whichever of the two entry points runs: `/gsd-execute-phase`'s `phase.complete` step, or `/gsd-verify-work`'s inline transition.
   - Before either runs, back up `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` to a scratch directory outside the repository.
   - After it runs, run the same probes as step 4, with the baseline digest and the grep lines written out inline so the reader needs no other file.
   - On any divergence, run `git checkout -- .planning/REQUIREMENTS.md`, re-run the probes to show a MATCH, and then diff ROADMAP.md and STATE.md against the backup.
   - The rule: reverted and reported, never committed, and no `/gsd-complete-milestone` step starts until the probes MATCH.

   State that `69-HANDOFF.md` reproduces this section.

6. **`## This task's own effect on .planning/REQUIREMENTS.md`.** Record `git diff --name-only -- .planning/REQUIREMENTS.md` and `git status --porcelain .planning/REQUIREMENTS.md`, both empty.

7. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; G="$D/69-CLOSEOUT-GUARD.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$G" | head -n 1; } &amp;&amp; PB="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; ! git log -1 --format=%s "$PB" | grep -qE '\(69-0[0-9]\)' &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(wc -l &lt; .planning/REQUIREMENTS.md)" = "$(k REQ_LINES_BASE)" ] &amp;&amp; [ "$(grep -c 'REL-12' .planning/REQUIREMENTS.md)" = "$(k REL12_HITS_BASE)" ] &amp;&amp; grep -qE '^- \[ \] \*\*REL-12\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-12 \| Phase 69 \| Pending \|' .planning/REQUIREMENTS.md &amp;&amp; [ -n "$(k GUARD_AT)" ] &amp;&amp; grep -qF '/gsd-verify-work' "$G" &amp;&amp; grep -qF '/gsd-execute-phase' "$G" &amp;&amp; grep -qF 'git checkout -- .planning/REQUIREMENTS.md' "$G" &amp;&amp; grep -qF "$(k REQ_SHA256_BASE)" "$G" &amp;&amp; for s in '## Baseline' '## The lines under guard' '## Why this file exists' '## Re-verification protocol (phase close)' '## For the operator running phase.complete'; do grep -qxF "$s" "$G" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md)" ] &amp;&amp; [ -z "$(git diff --name-only "$PB" -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md)" ] &amp;&amp; ! grep -q '^## HALT' "$G"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `PHASE_BASE_SHA` not 40 characters, not an ancestor of HEAD, or a plan-scoped `(69-0N)` commit
- the live digest, line count or REL-12 hit count differing from the recorded baseline keys
- the REL-12 checkbox not unchecked, or its row not Pending
- `GUARD_AT` empty
- either entry point, the reversion command or the baseline digest absent from the operator text
- REQUIREMENTS.md, ROADMAP.md or STATE.md modified since `PHASE_BASE_SHA`
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Baseline` holds `PHASE_BASE_SHA` (40 hex), `REQ_SHA256_BASE` (64 hex), `REQ_LINES_BASE`, `REL12_HITS_BASE` and `GUARD_AT` as bare key lines, each beside its command, plus a written cross-check against the planning-time values.
    - `## The lines under guard` quotes every `grep -n 'REL-12'` hit verbatim and classifies each as state-bearing or informational.
    - `## For the operator running phase.complete` names `/gsd-execute-phase` and `/gsd-verify-work`, instructs the scratch backup of REQUIREMENTS.md, ROADMAP.md and STATE.md, writes out the baseline digest and grep lines inline, and carries `git checkout -- .planning/REQUIREMENTS.md` with the rule "reverted and reported, never committed".
    - REQUIREMENTS.md, ROADMAP.md and STATE.md are unchanged in this worktree.
  </acceptance_criteria>
  <done>The phase-head digest, line count, REL-12 hits and PHASE_BASE_SHA are recorded, and both operator procedures are written with the two entry points named.</done>
</task>

<task type="auto">
  <name>Task 2: SC#1 fence observation 1 of 2, every remote negative paired with a positive control, plus the milestone typsphinx/ fence</name>
  <reversibility rating="reversible">One new planning document recording read-only probes; nothing is written to any remote.</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md</files>
  <read_first>
    - .planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md (the two-observation shape and its positive controls)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-RESEARCH.md § "Pattern 2: Positive-control remote probes" and § "Pitfall 1"
    - .planning/ROADMAP.md § Phase 69 SC#1, and binding constraints 2 and 13
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md D-11 and the measured-state list in `<domain>`
  </read_first>
  <action>
Create `69-SC1-INVARIANTS.md` with the heading `# Phase 69 — SC#1 Fence Invariants (unpublished-shaped tree)`.

1. **`## Observation 1 of 2`.** Open with `OBS1_AT = ` followed by the output of `date -u +"%Y-%m-%dT%H:%M:%SZ"`. Then record each probe as a command with its verbatim output, then its key line:
   - `sed -n 7p pyproject.toml`, key `VERSION_LINE`, which must be `version = "0.9.2"`
   - `git tag -l 'v0.9*'`, the local positive control: v0.9.0 and v0.9.2 present. Then `git tag -l 'v0.9.3'`, key `LOCAL_V093_TAGS = 0`
   - the unfiltered `git ls-remote --tags origin`. Derive two counts from that single listing: `REMOTE_V092_TAG = 1`, the lines ending `refs/tags/v0.9.2`, which is the positive control; and `REMOTE_V093_TAG = 0`, the lines containing `refs/tags/v0.9.3`. A filtered probe whose silence could mean an unreachable remote is not accepted.
   - `curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json`, key `PYPI_092_HTTP = 200` (positive control); the same for `0.9.3`, key `PYPI_093_HTTP = 404`
   - `gh release list --limit 20 --json tagName,isLatest,publishedAt`, verbatim. Key `GH_LATEST = v0.9.2`, the positive control, and key `GH_V093_RELEASES = 0`
   - `gh run list --workflow=release.yml --limit 20 --json databaseId,createdAt,headBranch,event,conclusion`, verbatim. `RELEASE_POSITIVE_CONTROL = 33318905691` must be present, and `RELEASE_RUNS_SINCE_START = 0` counts runs created after `2026-09-02` or on a `v0.9.3` ref
   - `gh pr list --state all --limit 5 --json number`, non-empty (positive control). Then `gh pr list --head gsd/v0.9.3-toolchain-and-dependency-update-repair --state all --json number,state`, key `BRANCH_PRS = 0`

   State plainly that the fence holds at phase head:
   - v0.9.2 is still the latest published release
   - no v0.9.3 tag exists locally or on origin, and nothing is on PyPI or GitHub Releases for 0.9.3
   - no pull request exists from the milestone branch

   D-11 records the 0.9.3 number as unclaimed, not decided, and this phase creates no tag.

2. **`## The typsphinx/ fence (constraint 13)`.**
   - Run `git fetch origin main`, then record `MILESTONE_BASE = ` with `git merge-base HEAD origin/main`.
   - Record `git diff --stat "$MILESTONE_BASE" HEAD -- typsphinx/`, which must be empty, key `TYPSPHINX_DIFF_FILES = 0`.
   - Record the widened positive control: `git diff --name-only "$MILESTONE_BASE" HEAD -- . ':(exclude).planning' ':(exclude)CHANGELOG.md'`, key `WIDENED_DIFF_FILES`, which must be non-zero. `CHANGELOG.md` is excluded because 69-01 edits it in parallel.
   - Record `git log --format=%h -G '^version = ' "$MILESTONE_BASE"..HEAD -- pyproject.toml`, which must print nothing, key `VERSION_BUMP_COMMITS = 0`.
   - Explain why the widened diff makes the empty scoped diff meaningful: a wrong anchor would also produce an empty diff.

3. **`## Handoff to observation 2`.** State that plan 69-06 owns observation 2, the phase-scoped `typsphinx/` diff from `PHASE_BASE_SHA`, and the post-dispatch commit check. It runs two waves and one CI dispatch later, so the two `OBS*_AT` stamps are genuinely separated. Name the sections 69-06 appends.

4. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; S="$D/69-SC1-INVARIANTS.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; ks() { sed -n "s/^$1 = //p" "$S" | head -n 1; } &amp;&amp; [ -n "$(ks OBS1_AT)" ] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; [ -n "$(git tag -l 'v0.9.2')" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.3')" ] &amp;&amp; T="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.3')" = 0 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json)" = 200 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.3/json)" = 404 ] &amp;&amp; [ "$(gh release list --limit 20 --json tagName,isLatest --jq '.[] | select(.isLatest) | .tagName')" = v0.9.2 ] &amp;&amp; [ "$(gh release list --limit 50 --json tagName --jq '[.[] | select(.tagName == "v0.9.3")] | length')" = 0 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json databaseId --jq '[.[] | select(.databaseId == 33318905691)] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headBranch,createdAt --jq '[.[] | select(.headBranch == "v0.9.3" or .createdAt &gt; "2026-09-02")] | length')" = 0 ] &amp;&amp; [ "$(gh pr list --state all --limit 5 --json number --jq 'length')" -ge 1 ] &amp;&amp; [ "$(gh pr list --head gsd/v0.9.3-toolchain-and-dependency-update-repair --state all --json number --jq 'length')" = 0 ] &amp;&amp; git fetch -q origin main &amp;&amp; MB="$(git merge-base HEAD origin/main)" &amp;&amp; [ "$(ks MILESTONE_BASE)" = "$MB" ] &amp;&amp; [ -z "$(git diff --name-only "$MB" HEAD -- typsphinx)" ] &amp;&amp; [ -n "$(git diff --name-only "$MB" HEAD -- . ':(exclude).planning' ':(exclude)CHANGELOG.md')" ] &amp;&amp; [ -z "$(git log --format=%h -G '^version = ' "$MB"..HEAD -- pyproject.toml)" ] &amp;&amp; for x in LOCAL_V093_TAGS REMOTE_V093_TAG GH_V093_RELEASES RELEASE_RUNS_SINCE_START BRANCH_PRS TYPSPHINX_DIFF_FILES VERSION_BUMP_COMMITS; do [ "$(ks "$x")" = 0 ] || { echo "key not 0: $x"; exit 1; }; done &amp;&amp; [ "$(ks REMOTE_V092_TAG)" = 1 ] &amp;&amp; [ "$(ks PYPI_092_HTTP)" = 200 ] &amp;&amp; [ "$(ks PYPI_093_HTTP)" = 404 ] &amp;&amp; [ "$(ks GH_LATEST)" = v0.9.2 ] &amp;&amp; for s in '## Observation 1 of 2' '## The typsphinx/ fence (constraint 13)' '## Handoff to observation 2'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml uv.lock .planning/REQUIREMENTS.md)" ]</automated>
    <fails_when>Non-zero exit, or `key not 0: <key>` / `missing section: <name>` printed. Each of these makes it fail:
- `OBS1_AT` empty
- `pyproject.toml:7` not the 0.9.2 assignment
- the v0.9.2 local or remote tag missing (positive control), or any v0.9.3 tag present
- PyPI not 200 for 0.9.2 or not 404 for 0.9.3
- the latest release not v0.9.2, or a v0.9.3 release present
- release.yml run 33318905691 absent (positive control), or a run since 2026-09-02 or on a v0.9.3 ref present
- the pull-request listing empty (positive control), or a pull request from the milestone branch present
- `MILESTONE_BASE` differing from the live merge-base
- a typsphinx/ change since it, or the widened diff empty
- a version-line commit in pyproject.toml
- a recorded key contradicting its probe
- a section missing
- a tracked product file or REQUIREMENTS.md changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Observation 1 of 2` opens with `OBS1_AT` and records every probe from action step 1 verbatim, each with its key.
    - Every negative has a positive control from the same source: the v0.9.2 tag line from the same `ls-remote`, PyPI 200 for 0.9.2, v0.9.2 as the latest release, run `33318905691` in the release.yml listing, and a non-empty pull-request listing.
    - `## The typsphinx/ fence (constraint 13)` records `MILESTONE_BASE`, `TYPSPHINX_DIFF_FILES = 0`, a non-zero `WIDENED_DIFF_FILES`, and `VERSION_BUMP_COMMITS = 0`.
    - `## Handoff to observation 2` names plan 69-06 and the sections it appends.
  </acceptance_criteria>
  <done>The first SC#1 observation is recorded with a positive control on every remote probe, and the milestone-wide `typsphinx/` fence is measured against a real anchor.</done>
</task>

<task type="auto">
  <name>Task 3: Write the reasoned no-external-API COVERAGE.md and prove the seal-time gate accepts it</name>
  <reversibility rating="reversible">One new planning document.</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/COVERAGE.md</files>
  <read_first>
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/COVERAGE.md (the declaration shape, whose opening line the gate accepts)
    - .planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md (the release-prep sibling)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-RESEARCH.md § "Standard Stack" (the tools the phase uses)
  </read_first>
  <action>
Create `COVERAGE.md` in the phase directory, following the Phase 65 shape: a reasoned declaration with no capability matrix.

1. The heading is `# Phase 69: External API Coverage Declaration`. The first body line starts `No external API integration: ` and continues with one sentence. That sentence says the phase:
   - edits `CHANGELOG.md` only
   - runs the project's own tooling (`uv`, `tox`, `pytest`, `black`, `mypy`, `ruff`, `sphinx-build`)
   - pushes the milestone branch once and dispatches the project's own `ci.yml` once
   - measures a trial merge with `git merge-tree`
   - writes planning evidence

2. Run the detector over this phase's scope and paste its JSON output verbatim in a fenced block. Build the scope from:
   - the concatenated bodies of every `69-0*-PLAN.md` in the phase directory
   - the output of `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs query roadmap.get-phase 69`

   Pipe that scope to `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/lib/api-coverage.cjs --json`. Whatever it returns is recorded as-is. If `detected` is true, name each signal and explain why it is not an integration. For example, `gh api` calls and the PyPI JSON endpoint are read-only probes of this project's own repository and of its own package page, and `Integration Test - basic` is a CI job name.

3. List what the phase's network use actually is:
   - `git fetch`/`ls-remote` and one fast-forward `git push` against this repository
   - `gh workflow run CI` once
   - read-only `gh run`, `gh release`, `gh pr` and `gh api .../branches/main/protection` calls
   - two `curl` status-code probes of `pypi.org/pypi/typsphinx/<version>/json`
   - `uv` fetching only what `uv.lock` pins

   `release.yml` and `update-pin.yml` are named in the plans only to forbid them.

4. State why the file exists: the seal-time `api-coverage.verify-pre` gate re-runs the detector over plan prose dense with `gh`, workflow and API wording, and this declaration meets a false positive with a reason rather than a fabricated matrix.

5. Run `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished` and paste its JSON. It must report `"passed": true` and `"none_declared": true`.

6. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; C="$D/COVERAGE.md" &amp;&amp; test -f "$C" &amp;&amp; grep -qE '^No external API integration: .{20,}' "$C" &amp;&amp; grep -qF '"detected"' "$C" &amp;&amp; ! grep -qiE '^\| *capability *\|' "$C" &amp;&amp; V="$(node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre "$D")" &amp;&amp; printf '%s\n' "$V" | grep -q '"passed": true' &amp;&amp; PB="$(sed -n 's/^PHASE_BASE_SHA = //p' "$D/69-CLOSEOUT-GUARD.md" | head -n 1)" &amp;&amp; [ -n "$PB" ] &amp;&amp; ! git diff --name-only "$PB" | grep -vxF -e "$D/69-CLOSEOUT-GUARD.md" -e "$D/69-SC1-INVARIANTS.md" -e "$C" -e "$D/69-02-SUMMARY.md"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- COVERAGE.md absent
- its declaration line missing or shorter than a sentence
- no verbatim detector JSON
- a capability-matrix table present
- the gate output lacking `"passed": true`
- `PHASE_BASE_SHA` unreadable
- any tracked file outside this plan's three files and SUMMARY changed since the phase base</fails_when>
  </verify>
  <acceptance_criteria>
    - `COVERAGE.md` opens with `No external API integration: ` followed by a one-sentence reason.
    - It holds the detector's JSON verbatim, run over the concatenated plan bodies and the ROADMAP Phase 69 section, and explains any signal the detector reports.
    - It lists the phase's actual network use and holds no capability table.
    - The pasted `check api-coverage.verify-pre` output reports `"passed": true`.
  </acceptance_criteria>
  <done>The seal-time API-coverage gate has a reasoned declaration it accepts, recorded with the real detector result.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's single spec-less edge item (REL-12, concurrency, **unresolved**) and the full prohibition
recall (P1..P6) are accounted in `69-01-PLAN.md` § "Spec-less edge-coverage accounting" and
§ "Prohibition recall accounting". This plan carries P1, P2 and P5.

This plan realises guarantee (d) of that edge. It records the phase-head digest that detects any
writer of `.planning/REQUIREMENTS.md`, including tooling that runs after every plan. Restated here
so no plan drops the edge silently. Phase accounting: 1 surfaced = 0 truths + 1 flagged + 0
dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → origin, GitHub API, PyPI (read-only) | `git ls-remote`, `gh release/run/pr list`, `curl` status probes; no write crosses |
| `.planning/REQUIREMENTS.md` → `phase.complete`-family tooling | the requirement state this phase must not change and that tooling has changed at seven prior closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-06 | Tampering | `phase.complete` (or `/gsd-verify-work`'s transition) flipping REL-12 against D-12 (ASVS V4, L1, by analogy) | high | mitigate | A phase-head digest, line count and quoted lines, recorded before any other plan's commit lands. Both entry points are named, with a scratch backup and a `git checkout` reversion procedure, and 69-06 reproduces all of it in the handoff. |
| T-69-07 | Repudiation | a vacuous remote negative from an unreachable source (ASVS V7, L1) | high | mitigate | Every negative is derived from the same listing as a positive control: v0.9.2 tag, PyPI 200, latest release v0.9.2, run 33318905691, and a non-empty pull-request listing. The verify re-runs every probe live. |
| T-69-08 | Elevation of Privilege | a probe becoming a write (tag, release, dispatch, pull request) | high | mitigate | Only read commands appear (`git tag -l`, `ls-remote`, `gh … list/view`, `gh api` GET, `curl -o /dev/null`); the plan pushes nothing and dispatches nothing. |
| T-69-09 | Repudiation | a fabricated capability matrix in COVERAGE.md | low | mitigate | A reasoned declaration with the verbatim detector output, and the seal gate's own verdict recorded. |
| T-69-SC | Tampering | package installs | low | accept | `uv sync --extra dev` installs only what `uv.lock` pins; no package is added. |
</threat_model>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates:
- `69-CLOSEOUT-GUARD.md`: baseline, lines under guard, and both operator procedures; 69-06
  appends the close-time re-verification.
- `69-SC1-INVARIANTS.md`: observation 1 of 2 and the milestone `typsphinx/` fence; 69-06 appends
  observation 2.
- `COVERAGE.md`.

It modifies no product-tree file and never edits `.planning/REQUIREMENTS.md`. No file named
`69-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- The recorded digest, line count and REL-12 hit count match the live file, and REL-12 is `[ ]` and
  Pending.
- Observation 1 holds on every probe, with a positive control on each remote one.
- The milestone `typsphinx/` diff is empty against a non-vacuous anchor.
- `check api-coverage.verify-pre` passes on `COVERAGE.md`.
</verification>

<success_criteria>
1. SC#4's phase-head half: the REL-12 fence baseline is recorded before any other plan's commit
   lands, with both operator procedures written (D-12).
2. SC#1's first observation: the tree is unpublished-shaped, and each remote probe is positively
   controlled (D-11).
3. The seal-time API-coverage gate is satisfied by a reasoned declaration.
</success_criteria>

<output>
Create `.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-02-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`.
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
- The first section states `PHASE_BASE_SHA`, `REQ_SHA256_BASE`, `REQ_LINES_BASE`,
  `REL12_HITS_BASE`, `OBS1_AT` and `MILESTONE_BASE`.
</output>
