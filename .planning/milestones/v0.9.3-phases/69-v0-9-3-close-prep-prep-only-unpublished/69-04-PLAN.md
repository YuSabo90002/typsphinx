---
phase: 69-v0-9-3-close-prep-prep-only-unpublished
plan: 04
type: execute
wave: 2
depends_on: ["69-01", "69-02"]
files_modified:
  - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CI-EVIDENCE.md
autonomous: true
requirements: [REL-12]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-14: immediately before the push, `git branch --list gsd/v0.9.3*` and `git ls-remote --heads origin gsd/v0.9.3*` are recorded. A decoy is never pushed; a decoy that carries unique commits halts the plan, because D-14 orders the canonical pointer to be advanced before the decoy is deleted, and that is a main-checkout action.'
    - 'The pushed tip carries every product-tree change of this phase. PUSHED_SHA is the canonical ref `gsd/v0.9.3-toolchain-and-dependency-update-repair`, whose CHANGELOG.md `## [Unreleased]` region holds the three bullets and 20 IDs, and whose pyproject.toml:7 is `version = "0.9.2"`. Nothing under typsphinx/ or .github/workflows/ differs from the merge-base with origin/main, and outside .planning/ it is identical to this worktree HEAD.'
    - 'D-13 push: the canonical ref is pushed with `git push --no-follow-tags origin gsd/v0.9.3-toolchain-and-dependency-update-repair`, as a fast-forward of the origin head (ORIGIN_BEFORE) re-measured just before, with no force, no tag and no other ref. Afterwards the origin head equals PUSHED_SHA and no tag points at it.'
    - 'D-13 dispatch: exactly one `gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair` is issued after the push. RUN_ID has headSha equal to PUSHED_SHA, event workflow_dispatch, workflowName CI, and createdAt later than PUSH_AT.'
    - 'D-13 / SC#3 CI half: the run is observed in the foreground to status completed, and all 12 job conclusions are transcribed in a numbered table. Test Python 3.12 on windows-latest, Test Python 3.13 on windows-latest, Test Python 3.12 on macos-latest and Test Python 3.13 on macos-latest are named individually. Every job is success, and a skipped, cancelled or missing job never counts as success.'
    - 'Constraint 7: the ruff verdict is quoted from the `Lint and Format Check` job, step `Run lint with tox`. CI_RUFF_VERSION, read from that job install output, equals the ruff version in uv.lock at PUSHED_SHA.'
    - 'D-13 single dispatch: the CI workflow list holds exactly one workflow_dispatch run at PUSHED_SHA, and the release.yml list holds none. Later commits touching only .planning/ warrant no second dispatch.'
    - 'Failure path: a non-success run is transcribed in full and kept. The executor stops at a blocking checkpoint, with no rerun, no second dispatch and no edit.'
    - 'REL-12 is cited for coverage only; SUMMARY requirements-completed: [].'
  artifacts:
    - path: .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CI-EVIDENCE.md
      provides: 'D-14 census, tip identity and fence, fast-forward push, one dispatch, the completed run with all 12 jobs, ruff verdict, dispatch-count and no-release guards, SC#3 CI verdict'
      contains: 'PUSHED_SHA = '
  key_links:
    - from: 'local ref gsd/v0.9.3-toolchain-and-dependency-update-repair'
      to: 'origin/gsd/v0.9.3-toolchain-and-dependency-update-repair'
      via: 'git push --no-follow-tags origin gsd/v0.9.3-toolchain-and-dependency-update-repair, fast-forward only; refs are shared between the worktree and the main checkout'
      pattern: 'ORIGIN_BEFORE = '
    - from: 'gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair'
      to: 'ci.yml twelve jobs on ubuntu, windows and macos'
      via: 'workflow_dispatch, since the push and pull_request triggers are scoped to main and develop'
      pattern: 'RUN_ID = '
    - from: 'uv.lock at PUSHED_SHA (ruff 0.15.x)'
      to: 'CI_RUFF_VERSION in the Lint and Format Check job'
      via: 'uv sync --extra dev --locked on the runner'
      pattern: 'CI_RUFF_VERSION = '
  prohibitions:
    - statement: 'MUST NOT create a tag, open or comment on a pull request, trigger release.yml or update-pin.yml, force-push, push any ref other than the canonical branch, or push a decoy.'
      requirement_id: REL-12
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT dispatch a second CI run, rerun jobs, or edit any file to turn a red job green; MUST NOT cite any run whose headSha differs from PUSHED_SHA as this phase green.'
      requirement_id: REL-12
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT move any ref of the main checkout from this worktree, other than deleting a decoy with zero unique commits, and MUST NOT merge origin/main into the branch (D-06).'
      requirement_id: REL-12
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Put this phase's tip on `origin` and obtain CI's verdict on it (D-13, ROADMAP SC#3 CI half):
- re-check the decoy branch immediately before the push (D-14)
- fast-forward the canonical milestone branch
- dispatch `ci.yml` exactly once on it and observe the run in the foreground until it completes
- transcribe every one of the 12 job conclusions, naming both `windows-latest` and both
  `macos-latest` lanes
- quote `ruff`'s verdict from the `Lint and Format Check` job

Purpose: CI holds lint authority and is the only route to Windows and macOS (ROADMAP constraint 7).
`origin` is about 120 commits behind, and its last CI run predates Phases 66 to 69 entirely. The
dispatched tip must carry the CHANGELOG edit, so this plan runs in wave 2, after 69-01 has merged.
It runs in parallel with 69-03 and 69-05, and it is the only plan in the phase that writes to a
remote.

Output: `69-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-RESEARCH.md
@.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-02-PLAN.md
@.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and run project
tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Refs are shared.** This worktree's HEAD is on its own worktree branch, but `refs/heads/gsd/v0.9.3-…`
is the same ref the main checkout has checked out. Push that ref by name; never `git branch -f`,
`git update-ref` or `git symbolic-ref` it from here.

**Waiting on CI.** Wait in the foreground: set the Bash tool's `timeout` to 600000 and never use
`run_in_background`. Wrap each watch in `timeout 590` so it returns inside the tool limit, then poll
`gh run view "$RUNID" --json status,conclusion`. Repeat these bounded foreground calls until `status`
is `completed`. Do not end the turn while the run is in progress.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line.
</worktree_provisioning>

<!-- planner-discipline-allow: gsd/v0.9.3-milestone -->
<!-- planner-discipline-allow: gsd/v0\.9\.3-milestone -->
<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.3 -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
The decoy name is named because the census looks for it and the gate requires it absent from
origin. `## HALT` is written only on a failure path.

## Planning-time census (2026-09-13, main checkout at `b2fea682`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| Local `gsd/v0.9.3*` refs | only `gsd/v0.9.3-toolchain-and-dependency-update-repair` (HEAD of the main checkout); no decoy | `git branch -vv` |
| Origin head of the canonical branch | `d9c7555323e843b5a389ed4354ce656d2fd05ca2`; local 118 commits ahead | `git ls-remote --heads`, `git rev-list --count` |
| Last CI runs on the branch | `34681968010` at `d9c75553` (Phase 65, 12/12 success); none since | 65-CI-EVIDENCE.md |
| `ci.yml` jobs | 12: `Test Python {3.12,3.13} on {ubuntu,windows,macos}-latest` (6), `Lint and Format Check` (step `Run lint with tox`), `Type Check`, `Code Coverage`, `Build Package`, `Integration Test - basic`, `Integration Test - advanced` | `.github/workflows/ci.yml` |
| Branch-lock ruff | 0.15.20 (`main` has 0.16.6 since #138; D-06 keeps the branch on its own lock) | `uv.lock`, 67-CONTEXT D-04 |
| `push.followTags` | unset at planning; `--no-follow-tags` is passed regardless | `git config` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: gate on wave 1, D-14 decoy census, tip identity and fence, fast-forward push of the canonical branch, dispatch CI exactly once</name>
  <precondition>`gh auth status` succeeds, and `git ls-remote --heads origin` returns at least one line. The canonical ref `gsd/v0.9.3-toolchain-and-dependency-update-repair` carries 69-01's edit: `git show gsd/v0.9.3-toolchain-and-dependency-update-repair:CHANGELOG.md` has three bold-lead bullets between `## [Unreleased]` and `## [0.9.2]`, and `git cat-file -e gsd/v0.9.3-toolchain-and-dependency-update-repair:.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md` succeeds. Otherwise write `## HALT: canonical tip lacks wave 1` and stop without pushing.</precondition>
  <reversibility rating="reversible">The branch already exists on origin and is advanced only by fast-forward, and a CI run publishes nothing. No tag, release or pull request is created. CONTEXT's Phase Boundary pre-authorises this push and dispatch as the standing prep-phase precedent (Phases 57, 61, 63).</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-02-PLAN.md Task 1 (the census, push and dispatch procedure this task mirrors)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md (the section structure)
    - .github/workflows/ci.yml (`name: CI`, the triggers and the twelve job names)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md D-06, D-13 and D-14
    - .planning/ROADMAP.md binding constraints 7, 9, 10 and 13
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line
   - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked; echo "exit:$?"`, which must print `exit:0`. Every `ci.yml` job begins with this locked sync, so a stale lock would fail all twelve lanes before any signal; on a non-zero exit, write `## HALT: lock not in sync` and never regenerate the lock.
   - before any commit: `BASE_69_04 = ` followed by the output of `git rev-parse HEAD`

2. **`## Wave-1 gate`.** Quote the precondition checks:
   - the bold-bullet count and the ID count over the canonical ref's `CHANGELOG.md` region
   - `git cat-file -e` of the guard file at the canonical ref

3. **`## Tip identity and fence`.** Record each of these; all must hold:
   - `PUSHED_SHA = ` followed by the output of `git rev-parse gsd/v0.9.3-toolchain-and-dependency-update-repair`, plus `git log --oneline -1 "$PUSHED_SHA"`
   - `git diff --name-only HEAD "$PUSHED_SHA" -- . ':(exclude).planning'`, empty: the pushed tree equals this worktree's product tree
   - `git show "$PUSHED_SHA:pyproject.toml" | sed -n 7p`, which must be `version = "0.9.2"`
   - after `git fetch origin main`: `git diff --name-only "$(git merge-base "$PUSHED_SHA" origin/main)" "$PUSHED_SHA" -- typsphinx .github/workflows`, empty (constraint 13; CI is unchanged)
   - `git merge-base --is-ancestor origin/main "$PUSHED_SHA"; echo "exit:$?"`, which must print `exit:1` (D-06)
   - `git tag --points-at "$PUSHED_SHA"`, empty
   - the ruff version in `git show "$PUSHED_SHA:uv.lock"`, key `LOCK_RUFF_VERSION`

4. **`## D-14 decoy census`.** Run this immediately before the push, with nothing but the step-5 measurements in between. Record `git branch --list 'gsd/v0.9.3*' -v` and `git ls-remote --heads origin 'gsd/v0.9.3*'`.
   - If `gsd/v0.9.3-milestone` exists on origin, write `## HALT: decoy on origin` and stop. It is never pushed, and its removal belongs to the owner.
   - If it exists locally, run `git merge-base --is-ancestor gsd/v0.9.3-milestone gsd/v0.9.3-toolchain-and-dependency-update-repair; echo "exit:$?"`:
     - `exit:0`: the canonical pointer already contains it, so D-14's advance-first step is satisfied. Delete it with `git branch -d gsd/v0.9.3-milestone` and record that.
     - Non-zero: the decoy carries commits the canonical branch lacks. Record `git log --oneline gsd/v0.9.3-toolchain-and-dependency-update-repair..gsd/v0.9.3-milestone`, write `## HALT: decoy carries unique commits`, and stop. D-14 requires the canonical pointer to be advanced first, and that happens in the main checkout, not from this worktree.

5. **`## Push`.**
   - Run `git fetch origin gsd/v0.9.3-toolchain-and-dependency-update-repair`, then record `ORIGIN_BEFORE = ` with the first field of `git ls-remote --heads origin refs/heads/gsd/v0.9.3-toolchain-and-dependency-update-repair`.
   - Record `git merge-base --is-ancestor "$ORIGIN_BEFORE" "$PUSHED_SHA"; echo "exit:$?"`, which must print `exit:0`. Otherwise write `## HALT: origin moved` and stop; a force-push is forbidden.
   - Record `git config --get push.followTags`, then `PUSH_AT = ` with the output of `date -u +%FT%TZ`.
   - Run `git push --no-follow-tags origin gsd/v0.9.3-toolchain-and-dependency-update-repair`: D-13's command, plus the guard against tags riding along. Record the output, including GitHub's pull-request hint, and do not act on that hint.
   - Record that `git ls-remote --heads origin refs/heads/gsd/v0.9.3-toolchain-and-dependency-update-repair` now prints `PUSHED_SHA`, and that `git ls-remote --tags origin` has no line containing `refs/tags/v0.9.3`.

6. **`## Dispatch`.**
   - Run `gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair` exactly once.
   - Locate the run with `gh run list --workflow=ci.yml --branch gsd/v0.9.3-toolchain-and-dependency-update-repair --event workflow_dispatch --limit 5 --json databaseId,headSha,status,createdAt,url`, selecting the row whose `headSha` equals `PUSHED_SHA` and whose `createdAt` is later than `PUSH_AT`.
   - If no such row is listed yet, that is registration lag. Repeat the list query a few seconds later; never dispatch again.
   - Record `RUN_ID = ` and `RUN_URL = `.

7. Write `69-CI-EVIDENCE.md` under the heading `# Phase 69 — CI Evidence (D-13, SC#3 CI half)`, using the section names above. Commit it. This plan's own commits land after the push and touch only `.planning/`, which no CI job reads.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; F="$D/69-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.3-toolchain-and-dependency-update-repair &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_69_04)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; [ "$(git ls-remote --heads origin "refs/heads/$BR" | cut -f1)" = "$P" ] &amp;&amp; OB="$(k ORIGIN_BEFORE)" &amp;&amp; [ -n "$OB" ] &amp;&amp; git merge-base --is-ancestor "$OB" "$P" &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; [ "$(printf '%s\n' "$HL" | grep -c 'gsd/v0\.9\.3-milestone')" = 0 ] &amp;&amp; [ -z "$(git tag --points-at "$P")" ] &amp;&amp; TL="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.3')" = 0 ] &amp;&amp; R="$(git show "$P:CHANGELOG.md" | awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}')" &amp;&amp; [ -n "$R" ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^- \*\*' | wc -l)" = 3 ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -oE '(TOX-0[1-4]|DEP-0[1-5]|NIX-0[1-8]|DOC-(19|20|21))' | sort -u | wc -l)" = 20 ] &amp;&amp; [ "$(git show "$P:pyproject.toml" | sed -n 7p)" = 'version = "0.9.2"' ] &amp;&amp; git fetch -q origin main &amp;&amp; MB="$(git merge-base "$P" origin/main)" &amp;&amp; [ -z "$(git diff --name-only "$MB" "$P" -- typsphinx .github/workflows)" ] &amp;&amp; { git merge-base --is-ancestor origin/main "$P"; [ $? = 1 ]; } &amp;&amp; [ -z "$(git diff --name-only HEAD "$P" -- . ':(exclude).planning')" ] &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json workflowName -q .workflowName)" = CI ] &amp;&amp; [ "$(gh run view "$RUNID" --json event -q .event)" = workflow_dispatch ] &amp;&amp; PA="$(k PUSH_AT)" &amp;&amp; [ -n "$PA" ] &amp;&amp; [[ "$(gh run view "$RUNID" --json createdAt -q .createdAt)" &gt; "$PA" ]] &amp;&amp; for s in '## Head check and provisioning' '## Wave-1 gate' '## Tip identity and fence' '## D-14 decoy census' '## Push' '## Dispatch'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e "$F" -e "$D/69-04-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_69_04` missing
- `PUSHED_SHA` not 40 characters, or the origin head not equal to it
- `ORIGIN_BEFORE` missing or not an ancestor of the pushed tip (a non-fast-forward)
- the decoy present on origin
- a tag pointing at the pushed tip, or a v0.9.3 tag on origin
- the pushed CHANGELOG.md region lacking three bullets and 20 IDs
- the pushed `pyproject.toml:7` not 0.9.2
- a typsphinx/ or workflow change since the merge-base with origin/main
- origin/main an ancestor of the pushed tip
- the pushed product tree differing from HEAD
- no `RUN_ID`, or the run's headSha, workflow, event or creation time not matching
- another tracked file changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## D-14 decoy census` records both listings immediately before the push, plus any zero-unique-commit decoy deletion with its ancestor result.
    - `## Tip identity and fence` records `PUSHED_SHA`, the empty product diff against HEAD, the 0.9.2 version line, the empty `typsphinx/` and workflow diff, the `exit:1` non-absorption check, no tag, and `LOCK_RUFF_VERSION`.
    - `## Push` records `ORIGIN_BEFORE` as an ancestor (`exit:0`), `PUSH_AT`, the verbatim `git push --no-follow-tags origin gsd/v0.9.3-toolchain-and-dependency-update-repair` output, and the origin head equal to `PUSHED_SHA`.
    - `## Dispatch` records exactly one `gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair`, plus `RUN_ID` and `RUN_URL`, with `headSha` equal to `PUSHED_SHA`.
  </acceptance_criteria>
  <done>The phase tip carrying the CHANGELOG edit is on origin as a fast-forward, with no decoy and no tag, and one CI run is dispatched against exactly that tip.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the run to completion in the foreground, transcribe all 12 jobs, name both windows-latest and both macos-latest lanes, and quote ruff's CI verdict</name>
  <precondition>The `RUN_ID` recorded by Task 1 resolves with `gh run view <id> --json databaseId`. If this executor resumes after returning mid-wait, it reuses that id; exactly one `workflow_dispatch` CI run on the branch carries `headSha` equal to `PUSHED_SHA`.</precondition>
  <reversibility rating="reversible">Read-only observation of a run already dispatched.</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CI-EVIDENCE.md (Task 1: `RUN_ID`, `PUSHED_SHA`, `LOCK_RUFF_VERSION`)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md § Job census, § windows-latest lanes, § macos-latest lanes, § ruff's verdict (the shape to mirror)
    - .github/workflows/ci.yml lines 10-72 (the matrix, the names, `Run lint with tox` at line 69)
  </read_first>
  <action>
1. **Wait in the foreground.**
   - Run `timeout 590 gh run watch "$RUNID" --interval 30` in a Bash call with `timeout` 600000 and no `run_in_background`.
   - After each return, read `gh run view "$RUNID" --json status,conclusion`.
   - If `status` is not `completed`, issue the same pair again. Do not end the turn and do not return control while it is in progress.
   - Overall cap: 90 minutes of wall clock. If it is reached, record `RUN_ID` and the current status in the evidence and return a blocking checkpoint. Never dispatch a second run.

2. **`## Run`.** Record `gh run view "$RUNID" --json status,conclusion,workflowName,headSha,url,createdAt,updatedAt` and the keys `RUN_HEAD_SHA` and `RUN_CONCLUSION`.

3. **`## Job census`.** Record `gh run view "$RUNID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'` as a numbered table covering every row. Keys: `JOB_COUNT = 12` and `NON_SUCCESS_JOBS = 0`.

   The census derived from `ci.yml`:
   - six `Test Python …` jobs (3 OS × 2 Python)
   - `Lint and Format Check`, `Type Check`, `Code Coverage` and `Build Package`
   - `Integration Test - basic` and `Integration Test - advanced`

4. **`## windows-latest lanes`** and **`## macos-latest lanes`.** Two sub-tables naming `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each with its conclusion.

5. **`## ruff's verdict`.** Take the `Lint and Format Check` job id from the run's jobs. From `gh run view --job <id> --log`, quote:
   - the `+ ruff==` line of the `Install dependencies` step, key `CI_RUFF_VERSION`, which must equal `LOCK_RUFF_VERSION`
   - the `Run lint with tox` step's `commands[0]> black --check .` line and its verdict
   - its `commands[1]> ruff check .` line and its verdict
   - `lint: OK`

   CI is the lint authority (constraint 7). The lint step of `ci.yml` is `Run lint with tox`; the differently-named lint step belongs to `release.yml`, which this plan never searches and never triggers. 69-03's local ruff run is set beside this one in 69-06, not here.

6. **`## Dispatch count and no release run`.** Record:
   - `gh run list --workflow=ci.yml --branch gsd/v0.9.3-toolchain-and-dependency-update-repair --event workflow_dispatch --limit 50 --json headSha`, showing exactly one run at `PUSHED_SHA`, key `DISPATCH_COUNT = 1`
   - `gh run list --workflow=release.yml --limit 20 --json headSha`, showing none at `PUSHED_SHA`, key `RELEASE_RUNS_AT_PUSHED = 0`

7. **`## D-13 final tip`.** State:
   - this run is D-13's single dispatch
   - `PUSHED_SHA` carries every product-tree change of the phase, the CHANGELOG edit
   - every later commit of this phase touches only `.planning/`, which no CI job reads, and plan 69-06 proves it with an empty product-tree diff
   - D-13 permits a second dispatch only for a code-affecting change, so none is warranted

8. **`## SC#3 CI verdict`.** Write `SC3_CI_VERDICT = MET` only when the run is completed and success, `JOB_COUNT = 12`, `NON_SUCCESS_JOBS = 0` and all four named lanes are success.
   - If any job is not success: transcribe every job literally and keep the run, do not rerun, dispatch again or edit anything, write `SC3_CI_VERDICT = NOT-MET`, and return a blocking checkpoint naming each failing job and step.

9. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; F="$D/69-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.3-toolchain-and-dependency-update-repair &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "$(gh run view "$RUNID" --json status -q .status)" = completed ] &amp;&amp; [ "$(gh run view "$RUNID" --json conclusion -q .conclusion)" = success ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '.jobs | length')" = 12 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.conclusion != "success")] | length')" = 0 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("windows-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("macos-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name == "Lint and Format Check" and .conclusion == "success")] | length')" = 1 ] &amp;&amp; for n in 'Test Python 3.12 on windows-latest' 'Test Python 3.13 on windows-latest' 'Test Python 3.12 on macos-latest' 'Test Python 3.13 on macos-latest' 'Lint and Format Check' 'Run lint with tox' 'Integration Test - basic' 'Integration Test - advanced'; do grep -qF "$n" "$F" || { echo "missing: $n"; exit 1; }; done &amp;&amp; CR="$(k CI_RUFF_VERSION)" &amp;&amp; [ -n "$CR" ] &amp;&amp; [ "$CR" = "$(k LOCK_RUFF_VERSION)" ] &amp;&amp; [ "$(git show "$P:uv.lock" | grep -A1 -xF 'name = "ruff"' | sed -n 's/^version = "\(.*\)"$/\1/p')" = "$CR" ] &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch "$BR" --event workflow_dispatch --limit 50 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; [ "$(grep -c 'Run linters' "$F")" = 0 ] &amp;&amp; [ "$(k JOB_COUNT)" = 12 ] &amp;&amp; [ "$(k NON_SUCCESS_JOBS)" = 0 ] &amp;&amp; [ "$(k DISPATCH_COUNT)" = 1 ] &amp;&amp; [ "$(k SC3_CI_VERDICT)" = MET ] &amp;&amp; for s in '## Run' '## Job census' '## windows-latest lanes' '## macos-latest lanes' "## ruff's verdict" '## Dispatch count and no release run' '## D-13 final tip' '## SC#3 CI verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing: <name>` / `missing section: <name>` printed. Each of these makes it fail:
- the run not completed, or its conclusion not success
- its headSha differing from `PUSHED_SHA`
- a job count other than 12, or any job not success
- a windows-latest or macos-latest count other than 2
- `Lint and Format Check` not success
- a lane, job or step name absent from the evidence
- `CI_RUFF_VERSION` empty or differing from the ruff version of `uv.lock` at the pushed tip
- more than one dispatched run at `PUSHED_SHA`, or a release.yml run at it
- the release-only lint step name present in the evidence
- a count or verdict key wrong
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Run` records `status`, `conclusion`, `workflowName`, `headSha` and `url`, and `## Job census` holds a numbered table of all twelve jobs, with `JOB_COUNT = 12` and `NON_SUCCESS_JOBS = 0`.
    - The two lane sub-tables name `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each success.
    - `## ruff's verdict` quotes the `Run lint with tox` step's black and ruff lines and `lint: OK`, with `CI_RUFF_VERSION` equal to `LOCK_RUFF_VERSION`.
    - `DISPATCH_COUNT = 1` and `RELEASE_RUNS_AT_PUSHED = 0`.
    - `## D-13 final tip` states why no second dispatch is warranted, and `SC3_CI_VERDICT = MET`.
  </acceptance_criteria>
  <done>One completed, all-green, twelve-job CI run on this phase's tip is transcribed job by job. Both Windows and both macOS lanes are named, and ruff's verdict comes from CI at the lock's own version.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's single spec-less edge item (REL-12, concurrency, **unresolved**) and the prohibition
recall (P1..P6) are accounted in `69-01-PLAN.md`. This plan carries P1, P5 and P6, and it realises
guarantees (b) and (c) of the edge: the one fast-forward push, and resuming on the recorded
`RUN_ID` rather than dispatching again.

It also carries assumption (e): the local canonical ref is assumed not to move between the census
and the push. If it does move, `PUSHED_SHA` records what was pushed, the run is bound to it by
`headSha`, and the empty product diff against HEAD shows whether the dispatched tree is this
phase's. Phase accounting: 1 surfaced = 0 truths + 1 flagged + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local refs → origin | a push publishes refs; a decoy, a force, another ref or a tag would publish something this phase must not |
| gh CLI → GitHub Actions | a dispatch starts remote work; the wrong workflow would begin a publish pipeline |
| CI log → committed evidence | quoted job-log lines enter the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-14 | Tampering | pushing the decoy, a force-push, another ref, or a tag riding along | high | mitigate | D-14 census immediately before the push, then `git push --no-follow-tags origin gsd/v0.9.3-toolchain-and-dependency-update-repair` with `ORIGIN_BEFORE` required as an ancestor. The verify requires no decoy on origin, no tag at the pushed tip and no v0.9.3 tag on origin. |
| T-69-15 | Elevation of Privilege | triggering `release.yml` or `update-pin.yml` (publish pipelines) | high | mitigate | Only `gh workflow run CI` is ever issued; the verify asserts no `release.yml` run carries `PUSHED_SHA`; a prohibition forbids every route. |
| T-69-16 | Repudiation | citing Phase 65's run `34681968010` or any other stale run, or a second dispatch masking a red first one | high | mitigate | `RUN_ID`'s `headSha` must equal `PUSHED_SHA` and its `createdAt` must follow `PUSH_AT`; exactly one dispatched run at that SHA; a red run is kept and escalated. |
| T-69-17 | Denial of Service | a stale `uv.lock` failing all twelve lanes before any signal (ASVS V14, L1) | medium | mitigate | `uv sync --extra dev --locked` must exit 0 before the push; drift halts, and the lock is never regenerated. |
| T-69-18 | Information Disclosure | CI log excerpts in committed evidence | low | accept | The repository and its Actions logs are public, and only tool-output lines (versions, lint verdicts) are quoted. |
| T-69-SC | Tampering | package installs | low | accept | Provisioning and the locked check install only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`69-CI-EVIDENCE.md`. Outside the repository it fast-forwards
`origin/gsd/v0.9.3-toolchain-and-dependency-update-repair` to `PUSHED_SHA` and creates one
`workflow_dispatch` CI run. It modifies no product-tree file. No file named `69-VERIFICATION.md` is
produced.
</artifacts_this_phase_produces>

<verification>
- The canonical branch is on origin at `PUSHED_SHA` as a fast-forward, with no decoy and no tag. The
  pushed tree carries the three CHANGELOG bullets and nothing under `typsphinx/` or the workflows
  changed.
- One `workflow_dispatch` CI run at `PUSHED_SHA` completed with all twelve jobs green, both
  `windows-latest` and both `macos-latest` lanes named, and ruff's verdict quoted from `Run lint with
  tox` at the lock's version.
- No `release.yml` run at `PUSHED_SHA`, and exactly one CI dispatch.
</verification>

<success_criteria>
ROADMAP SC#3's CI half: one fresh three-OS run dispatched on this phase's own tip, every job
conclusion transcribed, both `windows-latest` lanes named, and `ruff` green in `Lint and Format
Check` (D-13, D-14).
</success_criteria>

<output>
Create `.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-04-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`.
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
- The first section states `PUSHED_SHA`, `ORIGIN_BEFORE`, `RUN_ID`, `RUN_URL`, `JOB_COUNT`,
  `NON_SUCCESS_JOBS`, `CI_RUFF_VERSION` and `SC3_CI_VERDICT`. If the run was not all green, that
  section names the failing jobs first and states that SC#3 awaits the owner.
</output>
