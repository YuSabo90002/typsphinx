---
phase: 64-fhs-wrapper-and-command-shims-in-flake-nix
plan: 03
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX07-RENAME-EVIDENCE.md
  - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX08-ENV-EVIDENCE.md
autonomous: true
requirements: [NIX-07, NIX-08]
coupling_justified: ["64-02: both plans provision through the uv shim and share ~/.cache/uv, which uv locks internally; neither plan writes a file the other reads, and each renames or builds only inside its own worktree or scratch directory"]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-09: both tasks start with the same head check as 64-02: command -v ruff and command -v uv resolve to shim store paths whose bodies contain typsphinx-fhs-run, inherited from a session launched after 64-01 merged.'
    - 'NIX-07: all seven shim bodies exec through typsphinx-fhs-run with an absolute target (the walk variable for the six venv shims; the walk or a fixed /nix/store uv path for uv), and no body performs a PATH lookup of any tool name.'
    - 'NIX-07 (empty edge): with its .venv/bin/<tool> renamed, each of the six venv shims exits 127 inside a 30-second timeout, prints one typsphinx-shim: <tool>: line naming the start directory, and prints no version string, even though the main checkout runnable .venv/bin/ruff (positive control recorded) sits one directory tree away.'
    - 'NIX-07 (adjacency edge): each shim shares its name with its target, yet no renamed-target run hangs (rc 124) or recurses; every run returns 127 promptly, and every restored run prints the uv.lock version again.'
    - 'NIX-07 (encoding edge): a checkout path containing a space and a non-ASCII component is resolved, and reaches the target with the path intact through the sandbox.'
    - 'NIX-07 (ordering edge): the nearest .venv/bin/<tool> wins, the walk stops at the first .git boundary so an outer .venv is never reached from inside a checkout that has none, and for uv leg 1 (.venv/bin/uv) wins over leg 2 (the store uv) whenever it exists (D-02).'
    - 'NIX-07: the ruff rename case also holds when invoked from docs/ (start directory docs/, stopped at the worktree root).'
    - 'NIX-08: $HOME, TMPDIR, /etc and the locale are each measured host-side and through the shim, in this exact command-shim-from-an-outer-mkShell shape, and each gets a one-sentence statement.'
    - 'D-06: the load-bearing passthrough case is measured live: env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT survives into typsphinx-fhs-run (zero VIRTUAL_ENV or UV_PROJECT_ENVIRONMENT lines inside), and the environment-name diff host versus sandbox is recorded.'
    - 'D-08: REPRODUCES, MASKS and INCONCLUSIVE are defined and committed before the locale probe runs, and the evidence states exactly one finding backed by the four-cell matrix H-ja, H-C, S-ja, S-C.'
    - 'D-08: the locale-class probe is targeted (the four-cell Sphinx warning matrix plus TestNoLostDiagnostics under both locales through the pytest shim); the full suite is not run under a second locale, and no locale variable is pinned in any shim.'
    - 'D-05: the rename test and the environment measurement live only in these two evidence markdown files; no script under scripts/ and no tests/ file is added, and every probe file is created outside the repository and removed.'
    - 'The evidence never records full environment values: variable names only for the full diff, values only for the allowlisted variables.'
  artifacts:
    - path: .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX07-RENAME-EVIDENCE.md
      provides: 'NIX-07 shim-body inventory, rename proof with escape positive control, uv leg order, boundary/encoding/ordering probe'
      contains: 'typsphinx-shim:'
    - path: .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX08-ENV-EVIDENCE.md
      provides: 'NIX-08 definitions, environment measurement, four-cell locale matrix and the stated finding'
      contains: '**Finding:**'
  key_links:
    - from: 'a shim run from a nested worktree whose .venv/bin/<tool> is gone'
      to: 'the D-03 hard failure (exit 127)'
      via: 'the walk stopping at the worktree .git file before it can reach /home/yuta/Documents/typsphinx/.venv'
      pattern: 'typsphinx-shim:'
    - from: 'the caller environment (env -u …, LANG, HOME, TMPDIR)'
      to: 'the process inside typsphinx-fhs-run'
      via: 'bubblewrap passthrough plus the FHS /etc/profile (defaults only, never clobbers)'
      pattern: 'VIRTUAL_ENV'
  prohibitions:
    - statement: 'MUST NOT leave any .venv/bin target renamed, or the leg-1 probe script in place, when the task ends; every rename is restored and re-verified before the next case.'
      status: resolved
      verification: test
    - statement: 'MUST NOT write probe files or a fake .git boundary inside the repository, and MUST NOT add a script under scripts/ or a file under tests/ (D-05).'
      status: resolved
      verification: test
    - statement: 'MUST NOT record full environment values in any evidence file; tokens and keys may live in the session environment, and the evidence is committed.'
      status: resolved
      verification: test
    - statement: 'MUST NOT pin or normalize a locale variable in flake.nix, and MUST NOT run the full suite under a second locale (D-08).'
      status: resolved
      verification: test
    - statement: 'MUST NOT fold a MASKS or INCONCLUSIVE result into REPRODUCES, or omit it; masking is a legitimate finding and is written as such.'
      status: resolved
      verification: judgment
---

<objective>
Prove the two properties that no version smoke test can.

NIX-07: every shim resolves its target by absolute path and cannot recurse into itself or climb out of
its checkout. The proof is the rename-the-target experiment, run from inside a worktree nested in the
main checkout, whose runnable `.venv/bin/ruff` makes an escape observable.

NIX-08: the sandbox's `$HOME`, `TMPDIR`, `/etc` and locale behaviour is measured for this exact
invocation shape, and the reproduces-or-masks finding for the repository's locale-dependent CI-only
defect class is stated after terms defined in advance.

Purpose: SC#3's second half and SC#4's first half. Orchestrator note 1 is the reason NIX-07 must run
nested: an unbounded walk would resolve the main tree's binary silently. That binary reports the same
0.15.20, so only a positive-controlled failure can prove the walk is bounded. D-08 settles scope
(record, do not change behaviour; one targeted probe, not a second full suite), and D-05 settles form
(evidence markdown only).

Shape measured: the genuine D-09 shape, the inherited session PATH of a session launched after 64-01
merged. Each task's precondition enforces this.

Output: `64-NIX07-RENAME-EVIDENCE.md` and `64-NIX08-ENV-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-01-SUMMARY.md
@flake.nix
@CLAUDE.md
</context>

<!-- planner-discipline-allow: command -v -->
<!-- planner-discipline-allow: LC_ALL -->

## Flagged edge-probe assumption (unclassified row — surfaced, not silently dropped)

- **NIX-08 (unclassified).** The four-cell Sphinx warning-text matrix, plus `TestNoLostDiagnostics`
  under two locales, is taken as a sufficient proxy for "the project's known locale-dependent test
  class". That class is Sphinx localizing warning body text; Phase 52 anchored the historical
  instance. A future locale-dependent test of a different shape (for example, date or number
  formatting) is not covered by this probe, and the evidence says so.

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `64-NIX07-RENAME-EVIDENCE.md` | evidence file (NIX-07) | phase directory |
| `64-NIX08-ENV-EVIDENCE.md` | evidence file (NIX-08) | phase directory |
| `REPRODUCES` / `MASKS` / `INCONCLUSIVE` | defined finding vocabulary for the locale class, consumed by Phase 68 (DOC-19 / DOC-21) | `64-NIX08-ENV-EVIDENCE.md` |

No code symbol is created by this plan. Probe scripts and the scratch Sphinx project are transient,
live in the executor's session scratchpad outside the repository, and are deleted.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — NIX-07 rename proof from a nested worktree, with the escape positive control, uv leg order, and the boundary/encoding/ordering probe</name>
  <precondition>`command -v ruff` and `command -v uv` print `/nix/store/<hash>-ruff/bin/ruff` and `/nix/store/<hash>-uv/bin/uv`, and both files contain `typsphinx-fhs-run`. That means this session was launched after 64-01's `flake.nix` merged. If not, halt at a blocking-human gate: the maintainer opens a terminal in /home/yuta/Documents/typsphinx so direnv re-evaluates the flake, confirms `command -v ruff`, relaunches Claude Code from that shell, and re-runs /gsd-execute-phase 64.</precondition>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX07-RENAME-EVIDENCE.md</files>
  <read_first>
    - flake.nix (read-only: `venvWalk`, `venvShimNames`, `uvShim`, the D-03 stderr line and `exit 127`)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-02, D-03, D-05, D-09, § Specific Ideas on NIX-07's proof)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md § Pitfall 1, § Code Examples §5 (the rename procedure)
    - .planning/research/PITFALLS.md Pitfall 2 (self-recursion) and Pitfall 10 (version skew behind exit 0)
    - CLAUDE.md § "Worktree-isolated execution"
    - .planning/milestones/v0.9.2-phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md (the bold-labelled restore-command convention)
  </read_first>
  <action>
1. Head check and provisioning (D-09, CLAUDE.md).
   - Record `date -u +%FT%TZ`, `pwd -P` (it must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`: the nested location is what makes an escape observable), `test -f .git`, and `command -v ruff` and `command -v uv`.
   - Then run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

2. Shim bodies. For each of `uv tox ruff black mypy pytest sphinx-build`, record `command -v <name>` and the full `cat "$(command -v <name>)"` output; the scripts are short.
   - Each body must exec through `…/bin/typsphinx-fhs-run` with an absolute target: the walk variable for the six venv shims, and the walk or a literal `/nix/store/<hash>-uv-<version>/bin/uv` path for `uv`.
   - No body may perform a PATH lookup of any tool name. State this per body in the prose.

3. Escape positive control. Record `ls -l /home/yuta/Documents/typsphinx/.venv/bin/ruff`, and `/home/yuta/Documents/typsphinx/.venv/bin/ruff --version` with its exit code. At planning the main checkout's copy ran and printed 0.15.20. So an escape into the main checkout WOULD print a version line, which makes the rename result below discriminating.

4. The rename test, from the worktree root, for each of `tox ruff black mypy pytest sphinx-build`:
   - `mv .venv/bin/<t> .venv/bin/<t>.nix07-bak`
   - `timeout 30 <t> --version`, capturing stdout, stderr and rc separately, plus the wall time
   - restore immediately with `mv .venv/bin/<t>.nix07-bak .venv/bin/<t>`, then re-run `<t> --version`, which must print the `uv.lock` version again

   Expected for each renamed run:
   - rc 127
   - stderr is one `typsphinx-shim: <t>:` line naming this worktree's root as both the start directory and the stop directory
   - stdout is empty

   What counts as failure:
   - rc 124 means a hang
   - a version string on stdout means fall-through, whether to the main tree or anywhere else
   - any other rc is a finding

   Also run the `ruff` case from `docs/` (a guarded `cd docs`): the start directory is docs/ and the walk stops at the worktree root.

   Sequence the restores so they run even if a case fails. End with `ls -l .venv/bin/tox .venv/bin/ruff .venv/bin/black .venv/bin/mypy .venv/bin/pytest .venv/bin/sphinx-build` showing all six present.

5. uv leg order (D-02).
   - With no `.venv/bin/uv`, the xtrace `bash -x "$(command -v uv)" --version 2>&1 | grep '^+ exec'` names the store uv: leg 2.
   - Create `.venv/bin/uv` as a two-line executable sh script whose second line echoes `nix07-leg1-probe`. It is a probe script, not a link to any binary.
   - `uv --version` from the worktree root and from `docs/` must print `nix07-leg1-probe`: leg 1 wins when present, and its walk works from `docs/`.
   - Delete the probe script, then re-confirm `uv --version` prints the store uv's version.

6. Synthetic boundary, encoding and ordering probe, in a scratch directory OUTSIDE the repository (the executor's session scratchpad). Its path contains a space and a non-ASCII component, for example `<scratch>/nix07 probe/外側`.

   a. Boundary. Create:
      - `<outer>/.venv/bin/ruff`: an executable sh script printing `OUTER` followed by `"$0"`
      - `<outer>/inner/.git`: an empty file, standing in for a worktree's `.git` file
      - an empty `<outer>/inner/sub/`

      From `<outer>/inner/sub`, `ruff --version` must exit 127 with the not-found line. The walk stops at inner's `.git` and never reaches OUTER.

   b. Ordering and encoding. Add `<outer>/inner/.venv/bin/ruff`, printing `INNER` followed by `"$0"`. From `<outer>/inner/sub`, `ruff --version` must print `INNER` and the full path, space and non-ASCII component intact: the nearest `.venv` wins, and the quoting survived the sandbox.

   c. From `<outer>` itself, `ruff --version` must print `OUTER` with its intact path.

   Remove the whole scratch tree afterwards and record a `test ! -e` on it.

   Per D-05, none of these files enter the repository and none becomes a committed script.

7. Write `64-NIX07-RENAME-EVIDENCE.md`:
   - It opens with the measured-shape line: genuine D-09 shape, inherited session PATH.
   - Sections: `## Head check and provisioning`, `## Shim bodies`, `## Escape positive control`, `## Rename test`, `## uv leg order`, `## Boundary, encoding and ordering probe (outside the repository)`.
   - Include one results table with one row per rename case: tool, start dir, rc, stderr line, stdout bytes, wall seconds, restored version.

   Commit.
  </action>
  <verify>
    <automated>test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; for t in tox ruff black mypy pytest sphinx-build; do O="$(mktemp)"; E="$(mktemp)"; mv ".venv/bin/$t" ".venv/bin/$t.v"; timeout 30 "$t" --version &gt;"$O" 2&gt;"$E"; rc=$?; mv ".venv/bin/$t.v" ".venv/bin/$t"; [ "$rc" = 127 ] &amp;&amp; grep -q "^typsphinx-shim: $t:" "$E" &amp;&amp; [ ! -s "$O" ] || { echo "case failed: $t rc=$rc"; exit 1; }; done &amp;&amp; ruff --version | grep -Fx 'ruff 0.15.20' &amp;&amp; test ! -e .venv/bin/uv &amp;&amp; bash -x "$(command -v uv)" --version 2&gt;&amp;1 | grep -Eq 'typsphinx-fhs-run /nix/store/[a-z0-9]+-uv-[0-9.]+/bin/uv --version' &amp;&amp; for f in "$(command -v uv)" "$(command -v tox)" "$(command -v ruff)" "$(command -v black)" "$(command -v mypy)" "$(command -v pytest)" "$(command -v sphinx-build)"; do grep -q 'typsphinx-fhs-run' "$f" &amp;&amp; [ "$(grep -cE 'command -v|which |type -P' "$f")" = 0 ] || { echo "body failed: $f"; exit 1; }; done &amp;&amp; grep -q 'Escape positive control' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX07-RENAME-EVIDENCE.md &amp;&amp; grep -q 'nix07-leg1-probe' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX07-RENAME-EVIDENCE.md &amp;&amp; grep -q 'INNER' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX07-RENAME-EVIDENCE.md &amp;&amp; [ -z "$(git status --porcelain -- flake.nix flake.lock typsphinx tests scripts .github CLAUDE.md tox.ini pyproject.toml uv.lock)" ]</automated>
    <fails_when>non-zero exit; `case failed: <tool>` printed (an rc other than 127, a missing `typsphinx-shim:` line, or any stdout such as a version string from a fall-through); `ruff --version` not printing `ruff 0.15.20` after the restores; a leftover `.venv/bin/uv`; the uv xtrace line not naming a store uv; `body failed: <path>` printed (a shim body lacking the wrapper or containing a PATH lookup); the evidence missing the positive control, the leg-1 probe or the INNER result; a scope-fenced path modified</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records all seven shim bodies verbatim, each with an exec line through `typsphinx-fhs-run`.
    - The main checkout's `.venv/bin/ruff --version` output is recorded as the escape positive control.
    - The rename table has seven rows: six tools from the root, plus ruff from `docs/`. Every row shows rc 127, a `typsphinx-shim: <tool>:` stderr line, empty stdout, wall time under 30 seconds, and the restored version.
    - The uv section shows leg 2 (store path) without `.venv/bin/uv`, `nix07-leg1-probe` printed from the root and from `docs/` with the probe present, and leg 2 again after the probe is removed.
    - The scratch probe shows rc 127 from `inner/sub` before an inner `.venv` exists, `INNER <path>` after, `OUTER <path>` from `<outer>`, the path's space and non-ASCII component intact in the printed `$0`, and the scratch tree removed.
    - `.venv/bin` holds all six targets and no `uv` at the end; `git status --porcelain` over the scope-fenced paths is empty.
  </acceptance_criteria>
  <done>Every shim is shown to resolve by absolute path, bounded at its checkout root. A missing target fails loud at 127 rather than hanging, recursing, or escaping to the main checkout's runnable copy. uv's leg order, quoting and nearest-wins ordering are each proven.</done>
</task>

<task type="auto">
  <name>Task 2: NIX-08 — define reproduces/masks first, then measure $HOME, TMPDIR, /etc, the locale and D-06's passthrough, then the four-cell locale matrix</name>
  <precondition>The same session check as Task 1: `command -v sphinx-build` and `command -v pytest` print `/nix/store/…` shim files containing `typsphinx-fhs-run`, and this worktree's `.venv` exists.</precondition>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX08-ENV-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-06, D-08, § Specific Ideas on the NIX-08 record)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md § Summary point 3 (bind mounts, `etcBindEntries`, `/etc/profile` defaults), § Assumptions Log A1 and A4, § Code Examples §6
    - tests/test_state_guard_shapes_gate.py lines 778-845 (`_locale_invariant_anchors`, `TestNoLostDiagnostics` — read-only)
    - flake.nix (read-only; the passthrough and the absence of any locale variable)
    - .planning/ROADMAP.md "Binding constraints" 5a
  </read_first>
  <action>
1. Definitions first. Write the section `## Definitions (written before measuring)` into `64-NIX08-ENV-EVIDENCE.md` and COMMIT it before any locale probe runs. The commit order makes "defined before measured" checkable in `git log`. The definitions, verbatim in substance:

   - **The known class:** tests whose outcome depends on the process locale. Historically, Sphinx localizes a warning's BODY text under the maintainer's `LANG=ja_JP.UTF-8` while CI runs English. Phase 52 re-anchored `tests/test_state_guard_shapes_gate.py::TestNoLostDiagnostics` on the never-localized `file:line: WARNING:` prefix and the bracketed diagnostic tag, via `_locale_invariant_anchors()`.

   - **Matrix:** the warning body Sphinx emits for one fixed scratch document that is outside any toctree, captured four ways:
     - **H-ja:** host, maintainer environment, via the worktree's absolute `.venv/bin/python -m sphinx` (an absolute path, so no shim is involved)
     - **H-C:** host, with `LC_ALL=C LANG=C LANGUAGE=C`
     - **S-ja:** through the `sphinx-build` shim, maintainer environment
     - **S-C:** through the shim, with `LC_ALL=C LANG=C LANGUAGE=C` set by the caller

     All four runs start from inside the worktree, so the shim's walk finds this worktree's `.venv`. All four use the same Sphinx.

   - **Control:** H-ja ≠ H-C, meaning the locale axis actually changes Sphinx's text on the host. If H-ja = H-C, the finding is **INCONCLUSIVE**: record it and do not classify.

   - **REPRODUCES:** the control holds, S-ja = H-ja and S-C = H-C. Through the shim, the locale axis behaves exactly as on the host. So the class exists through the shim exactly as it does on the host: invisible under ja_JP.UTF-8, surfaced by the `LC_ALL=C` pre-check.

   - **MASKS:** the control holds, and S-ja ≠ H-ja or S-C ≠ H-C. The shim changes what the locale axis shows, so a shim run cannot stand in for a host run when reasoning about the class. Record which cell diverged and in which direction. Per CONTEXT § Specific Ideas, masking is a legitimate finding and is written as such.

2. Environment measurement, in this exact invocation shape. Take the wrapper path from a shim body: `FHS="$(grep -oE '/nix/store/[^ "]+/bin/typsphinx-fhs-run' "$(command -v ruff)" | head -1)`.

   a. Environment-name diff. Compare `env | cut -d= -f1 | sort` on the host with `"$FHS" /usr/bin/env | cut -d= -f1 | sort` inside. Record the added and removed NAMES only.

   b. Values, ONLY for this allowlist: `HOME`, `TMPDIR`, `LANG`, `LANGUAGE`, `LC_ALL`, `LC_MESSAGES`, `LOCALE_ARCHIVE`, `TZ`, `PWD`, `VIRTUAL_ENV`, `UV_PROJECT_ENVIRONMENT`, and the first four `PATH` entries. Take them host-side and inside. Never dump full values: tokens and keys may live in the session environment, and this file is committed.

   c. A caller-set probe variable containing a space and a non-ASCII word, `NIX08_PROBE='a b 日本'`, printed from inside.

   d. D-06's load-bearing case. Run `VIRTUAL_ENV=/nix08-probe UV_PROJECT_ENVIRONMENT=/nix08-probe env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT "$FHS" /usr/bin/env` and count the lines beginning `VIRTUAL_ENV=` or `UV_PROJECT_ENVIRONMENT=`. The count must be 0: the unsets survive into the sandbox.

   e. TMPDIR.
      - Record whether it is set in this session; at planning it was unset.
      - `TMPDIR=<scratch dir> "$FHS" /bin/sh -c 'printf %s "$TMPDIR"'` must print the same value.
      - `stat -c '%d %i' /tmp` host-side and inside must match (bound, not private).

   f. `$HOME`: its value host-side and inside must be identical, and `ls "$HOME/.cache/typst/packages/preview"` must work inside.

   g. `/etc`.
      - `"$FHS" /bin/sh -c 'ls -la /etc'`: record which entries are symlinks into `/.host-etc`. Expected from the nixpkgs source: resolv.conf, nsswitch.conf, hosts, passwd, group, localtime, zoneinfo, ssl/certs, ca-certificates, pki and fonts. Record which entries come from the FHS closure.
      - `readlink -f /lib64/ld-linux-x86-64.so.2` host-side (stub-ld) and inside (glibc).

   h. Locale mechanics. Host leg: `.venv/bin/python -c "import locale,sys,os; print(locale.setlocale(locale.LC_ALL,'')); print(locale.getlocale(locale.LC_MESSAGES)); print(sys.stdout.encoding); print(os.path.exists(os.environ.get('LOCALE_ARCHIVE','')))"`. Sandbox leg: `uv run python -c` with the same probe, through the uv shim. This covers RESEARCH A1: whether `LOCALE_ARCHIVE` is set and exists inside.

   Then write four one-sentence statements (CONTEXT § Specific Ideas): whether `$HOME` passes through unchanged; whether `TMPDIR` does; whether `/etc` does, and how; and whether the locale does.

3. Four-cell matrix. Build a scratch Sphinx project in the executor's session scratchpad, outside the repository:
   - `conf.py` containing only `project = "nix08"`
   - `index.rst` with a title
   - `orphan-probe.rst` with a title and not referenced from any toctree

   From the worktree root, build it four times with `-b dummy` into separate scratch output directories:
   - H-ja: `"$(pwd)/.venv/bin/python" -m sphinx -b dummy <src> <out1>`
   - H-C: the same, prefixed with `LC_ALL=C LANG=C LANGUAGE=C`
   - S-ja: `sphinx-build -b dummy <src> <out3>`
   - S-C: the same, prefixed with `LC_ALL=C LANG=C LANGUAGE=C`

   From each cell's stderr, capture the line containing `orphan-probe.rst` and `WARNING:`. Record all four lines verbatim in a table with the cells labelled H-ja, H-C, S-ja and S-C. Then write exactly one line, `**Finding:** REPRODUCES`, `**Finding:** MASKS (<cell> diverged: <direction>)` or `**Finding:** INCONCLUSIVE (<why>)`, applying the committed definitions mechanically.

   Remove the scratch project afterwards.

4. Targeted test nodes (D-08: this is not the full suite). Through the pytest shim, run `pytest tests/test_state_guard_shapes_gate.py -k TestNoLostDiagnostics -q` under the maintainer environment, then the same command prefixed with `LC_ALL=C LANG=C LANGUAGE=C`. Record both summary lines. The historical instance must stay fixed through the shim under both locales.

5. Add `## Consequence (for Phase 68, DOC-19 / DOC-21)`: two or three sentences on what the finding means for the maintainer's workflow, for example whether the `LC_ALL=C` pre-check before a CI dispatch still does its job through the shims. D-08 applies: this phase records the finding and changes no behaviour, pins nothing in the shims, and leaves `flake.nix` untouched. Per D-05, nothing becomes a script or a test. Commit.
  </action>
  <verify>
    <automated>FHS="$(grep -oE '/nix/store/[^ "]+/bin/typsphinx-fhs-run' "$(command -v ruff)" | head -1)" &amp;&amp; test -x "$FHS" &amp;&amp; [ "$(VIRTUAL_ENV=/nix08-probe UV_PROJECT_ENVIRONMENT=/nix08-probe env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT "$FHS" /usr/bin/env | grep -cE '^(VIRTUAL_ENV|UV_PROJECT_ENVIRONMENT)=')" = 0 ] &amp;&amp; [ "$("$FHS" /bin/sh -c 'printf %s "$HOME"')" = "$HOME" ] &amp;&amp; [ "$(stat -c '%d %i' /tmp)" = "$("$FHS" /bin/sh -c "stat -c '%d %i' /tmp")" ] &amp;&amp; pytest tests/test_state_guard_shapes_gate.py -k TestNoLostDiagnostics -q &amp;&amp; LC_ALL=C LANG=C LANGUAGE=C pytest tests/test_state_guard_shapes_gate.py -k TestNoLostDiagnostics -q &amp;&amp; F=.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX08-ENV-EVIDENCE.md &amp;&amp; grep -q '## Definitions (written before measuring)' "$F" &amp;&amp; N="$(git log --format=%H -- "$F")" &amp;&amp; [ "$(printf '%s\n' "$N" | grep -c .)" -ge 2 ] &amp;&amp; [ "$(grep -cE '^\*\*Finding:\*\* (REPRODUCES|MASKS|INCONCLUSIVE)' "$F")" = 1 ] &amp;&amp; for c in H-ja H-C S-ja S-C; do grep -q "$c" "$F" || exit 1; done &amp;&amp; [ "$(grep -cE '(TOKEN|API_KEY|SECRET)=' "$F")" = 0 ] &amp;&amp; [ "$(grep -c 'LC_ALL' flake.nix)" = 0 ] &amp;&amp; [ -z "$(git status --porcelain -- flake.nix flake.lock typsphinx tests scripts .github CLAUDE.md tox.ini pyproject.toml uv.lock)" ]</automated>
    <fails_when>non-zero exit; the wrapper path not extractable from the ruff shim; any `VIRTUAL_ENV=` or `UV_PROJECT_ENVIRONMENT=` line surviving the `env -u` into the sandbox; `$HOME` or the `/tmp` device and inode differing inside; either `TestNoLostDiagnostics` run failing; the definitions section absent or committed in the same single commit as the results (fewer than two commits touching the file); zero or more than one `**Finding:**` line; a matrix cell label missing; a token-, key- or secret-shaped assignment in the evidence; a locale variable present in `flake.nix`</fails_when>
  </verify>
  <acceptance_criteria>
    - `64-NIX08-ENV-EVIDENCE.md` contains `## Definitions (written before measuring)`, defining the known class, the four cells, the control, REPRODUCES, MASKS and INCONCLUSIVE. It is committed in an earlier commit than the measurement sections (`git log -- <file>` shows at least two commits).
    - The environment section records the name-only diff, the allowlisted values host-side and inside, the `NIX08_PROBE` value intact inside, the D-06 `env -u` count of 0, the TMPDIR state and passthrough, identical `/tmp` device and inode, identical `$HOME`, the `/etc` listing with its `/.host-etc` symlinks, and the loader path host-side and inside.
    - Four one-sentence statements cover `$HOME`, `TMPDIR`, `/etc` and the locale.
    - The matrix table has four rows, H-ja, H-C, S-ja and S-C, each with the verbatim `orphan-probe.rst … WARNING:` line. Exactly one `**Finding:**` line follows, derived from the definitions.
    - Both `TestNoLostDiagnostics` summary lines (maintainer locale and `LC_ALL=C`) are recorded as passing.
    - A consequence paragraph addressed to Phase 68 is present; `flake.nix` is untouched.
  </acceptance_criteria>
  <done>The sandbox's environment behaviour is measured rather than assumed, for this project's exact invocation shape. The locale-class effect is stated as REPRODUCES, MASKS or INCONCLUSIVE against definitions committed before the measurement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nested worktree → main checkout | the rename proof deliberately runs where an unbounded walk would reach a runnable binary |
| session environment → committed evidence | an environment dump crosses from a live session holding credentials into a git-tracked file |
| probe files → repository | a stray fake `.git` or `.venv/bin/<tool>` inside the repository would change shim resolution for everyone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-10 | Information Disclosure | environment measurement written to committed evidence | high | mitigate | full environment is diffed by NAME only; values only for a fixed allowlist of non-secret variables; the verify gate fails on any token-, key- or secret-shaped assignment in the file |
| T-64-11 | Tampering | rename test leaving a `.venv/bin` target renamed or the leg-1 probe script in place | medium | mitigate | each rename is restored inside the same step before its check; the task ends with a six-target `ls` and `test ! -e .venv/bin/uv`, both gated |
| T-64-12 | Tampering | probe files (fake `.git`, probe `.venv/bin/ruff`) created inside the repository | medium | mitigate | every probe lives in the executor's scratchpad outside the repository and is removed with a recorded `test ! -e`; `git status --porcelain` over tracked paths is gated empty (D-05) |
| T-64-13 | Repudiation | a MASKS or INCONCLUSIVE result reclassified after seeing the data | medium | mitigate | the definitions are committed before the probe runs and the gate requires at least two commits on the file and exactly one `**Finding:**` line |
</threat_model>

<verification>
- The rename proof runs from a nested worktree with a recorded positive control; all six venv shims exit 127 without a version or a hang; `uv`'s leg order is proven.
- The boundary, encoding and ordering probe is recorded outside the repository and cleaned up.
- The NIX-08 definitions precede the measurement in `git log`, and exactly one finding is stated.
- D-06's `env -u` passthrough is measured at 0 surviving lines; no full environment values are in the evidence.
</verification>

<success_criteria>
SC#3's second half is met: each shim resolves by an absolute, un-shadowable path and cannot recurse or escape, proven by a check that would catch bare-name resolution. SC#4's first half is met: `$HOME`, `TMPDIR`, `/etc` and the locale are measured in this project's exact shape, and the locale-class effect is stated as reproduced or masked.
</success_criteria>

<output>
Create `.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-03-SUMMARY.md` when done. Frontmatter declares `requirements-completed: [NIX-07, NIX-08]`.
</output>
