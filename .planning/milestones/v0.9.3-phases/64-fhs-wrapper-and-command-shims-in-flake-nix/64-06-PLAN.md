---
phase: 64-fhs-wrapper-and-command-shims-in-flake-nix
plan: 06
type: execute
wave: 5
depends_on: ["64-05"]
files_modified:
  - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md
autonomous: true
gap_closure: true
requirements: [NIX-01, NIX-02, NIX-03, NIX-04, NIX-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Session boundary (64-VALIDATION.md): every task runs only in a Claude Code session relaunched after 64-05 merged. The fhs-run embedded in the body of command -v ruff has /usr/lib/libz.so.1 in its root, and its store prefix equals the New fhs-run line of 64-LIBZ-FIX-EVIDENCE.md. In the stale session (root dgddrdfkvigqsv48k563szqc8w7xlw2g) that test is false, and the executor halts at a blocking-human gate before provisioning anything.'
    - 'D-09 / NIX-05: before provisioning, in a fresh worktree nested under /home/yuta/Documents/typsphinx/.claude/worktrees/ with .venv and .tox both absent, command -v for all seven shim names equals the New shim paths table of 64-LIBZ-FIX-EVIDENCE.md, reached purely by session inheritance. The direnv status RC lines are recorded, and one sentence answers reachability.'
    - 'D-04 / NIX-05: the only provisioning command is env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev, verbatim and with no interpreter pin. The .venv/pyvenv.cfg home and version_info are recorded beside the carried-in baseline interpreter (nix python3-3.13.13). No manual symlink and no ELF-patching step appears anywhere.'
    - 'NIX-05 (adjacency edge): typsphinx.__file__ and every recorded shim target lie under this worktree pwd -P, never under /home/yuta/Documents/typsphinx/.venv or /home/yuta/Documents/typsphinx/typsphinx.'
    - 'NIX-01 (regression): ruff --version through the shim prints exactly ruff 0.15.20, which is the uv.lock pin read at run time, and the shim resolves this worktree .venv/bin/ruff.'
    - 'Tracer: in the genuine shape, uv run python -S with this .venv site-packages inserted by hand imports PIL._imaging, and the bare pytest shim passes tests/test_converted_image_collision_render_gate.py, the exact path that failed in 64-02.'
    - 'NIX-04: one full-suite run through the bare pytest shim, under the maintainer locale with no locale variable (D-08), collects 1548 and reads 1543 passed, 5 skipped, with every skip itemised with its reason. Any other result is itemised node by node with its traceback and its attribution. The baseline is never adjusted, and a new baseline stands only with per-node attribution.'
    - 'NIX-02: tox -e lint, tox -e type, tox -e py312 and tox -e py313 each end with an <env>: OK line in this worktree. The py312 pytest header names Python 3.12, and every tox venv> line names the new uv shim path, so the nested entry reaches the new sandbox.'
    - 'NIX-02 (empty edge): .tox is absent before the first tox run, so every environment is provisioned cold inside the new sandbox, py312 included.'
    - 'NIX-02 (adjacency edge): each environment runs in its own invocation, in the fixed order lint, type, py312, py313, with its own recorded OK line; no result is inferred from another environment state in .tox.'
    - statement: 'NIX-02 (ordering edge): the NIX-02 outcome does not depend on the order the four environments run in, because each is self-contained (extras = dev, uv-venv-lock-runner).'
      verification: backstop
    - 'NIX-03: tox -e cov, tox -e docs-html and tox -e docs-pdf each end with an <env>: OK line. docs-html reports 3 warnings and docs-pdf 5, each from a build started with docs/_build removed. docs/_build/pdf/typsphinx.pdf is non-empty and begins %PDF (D-07: the @preview package count is recorded before and after).'
    - 'NIX-03 (adjacency edge): docs-html and docs-pdf share docs/_build, so each build command begins with rm -rf docs/_build, and neither build output nor warning count can be the other.'
    - 'NIX-03 (empty edge): the PDF check is test -s plus the %PDF magic bytes, taken from a build started with docs/_build absent; exit 0 alone never counts.'
    - statement: 'NIX-03 (ordering edge): the docs-pdf PDF and its warning count are independent of whether docs-html ran before it.'
      verification: backstop
    - 'Genuine residual: after the tox runs, 64-05 audit script, reproduced verbatim from its evidence and run through this session fhs-run over this worktree .venv, every .tox/<env> and their interpreter trees, reports no libz.so.1. Every soname it does report is either in 64-05 Residual verdicts table with that verdict cited or receives its own verdict here by the same rule.'
    - 'Every transcript in this plan evidence is the genuine D-09 shape (inherited session PATH; never nix develop, never direnv exec, never the main checkout), and the file says so at its top.'
    - 'The evidence ends with a Requirement closure table, one row each for NIX-01 to NIX-05, reading MET or NOT MET with the section that decides it; the table follows the transcripts and does not replace them.'
  artifacts:
    - path: .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md
      provides: 'the genuine-shape re-measurement after the zlib fix: head check, provisioning and interpreter provenance, NIX-01 regression, tracer, NIX-04 full suite, seven tox environments with a real PDF, genuine residual audit, requirement closure'
      contains: 'genuine D-09 shape'
  key_links:
    - from: 'the Claude Code session PATH (frozen at launch after 64-05 merged, direnv-derived in the main checkout)'
      to: 'this worktree Bash calls'
      via: 'environment inheritance: no direnv hook fires in non-interactive Bash, so the launch PATH, now carrying the libz rootfs shims, survives cd into the worktree'
      pattern: 'typsphinx-fhs-run'
    - from: 'the provisioning line'
      to: 'this worktree .venv (uv-managed cpython-3.14)'
      via: 'the uv shim second leg exec inside the new typsphinx-fhs-run, with the -u unsets preserved (D-02, D-06)'
      pattern: 'uv sync --extra dev'
    - from: 'tox inside the new sandbox'
      to: '.tox/<env>/bin/* and the uv-managed CPython 3.12'
      via: 'fork/exec namespace inheritance plus the nested uv shim entry'
      pattern: 'py312: OK'
  prohibitions:
    - statement: 'MUST NOT modify any tracked file other than 64-GAP-REMEASURE-EVIDENCE.md. flake.nix is included in that ban: a further rootfs change needs another session relaunch, so it belongs to a new gap-closure plan, and the finding is recorded here instead.'
      status: resolved
      verification: test
    - statement: 'MUST NOT add an interpreter pin to the provisioning line, and MUST NOT re-run any gate under a different interpreter to reach a baseline (D-04).'
      status: resolved
      verification: test
    - statement: 'MUST NOT adjust the 1543 / 5 or 3 / 5 baselines to match a measurement, and MUST NOT attribute a failure away without its traceback.'
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record a nix develop run, a direnv exec run or a main-checkout run as this plan evidence.'
      status: resolved
      verification: judgment
    - statement: 'MUST NOT set a locale variable for the NIX-04 run, and MUST NOT run the full suite a second time (D-08).'
      status: resolved
      verification: test
    - statement: 'MUST NOT use a manual symlink, an ELF-patching tool, a system-wide loader shim or nixpkgs own ruff package, and MUST NOT push or dispatch a workflow.'
      status: resolved
      verification: test
---

<objective>
Re-measure NIX-02, NIX-03 and NIX-04 in the genuine D-09 shape after 64-05's rootfs fix. In one fresh
worktree nested inside the main checkout, first confirm the session is the relaunched one (the
discriminator is `/usr/lib/libz.so.1` in the shim-embedded rootfs). Then run CLAUDE.md's single
provisioning line, the NIX-01 regression check, and the tracer: the exact path that failed in 64-02.
After that come the full suite (NIX-04) and all seven tox environments (NIX-02, NIX-03). All seven are
re-run, not just the three that failed, because the rootfs every environment runs in changed.

Purpose: these are the observations `64-VERIFICATION.md`'s three gaps ask for — "re-measured green in a
session relaunched after that edit". Decisions relied on:
- D-04: fresh worktree, one provisioning line, no interpreter pin
- D-07: docs-pdf must produce a real PDF inside the sandbox
- D-08: one full-suite run under the maintainer locale
- D-09: the head check, and reachability by session inheritance

Plan 64-05's diagnostics do not count here. The only evidence that counts is a transcript taken through
this session's inherited PATH.

**Shape measured, stated plainly:** this plan runs only in a Claude Code session launched after 64-05
merged, from a shell where direnv loaded the edited devShell. Each task's precondition enforces this. In
the older session the check is false (its rootfs has no `libz.so.1`), and the executor halts at a
blocking-human gate without provisioning anything. "The shim contains typsphinx-fhs-run" is true in
both sessions and is never used as the check.

Output: `64-GAP-REMEASURE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-VALIDATION.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-LIBZ-FIX-EVIDENCE.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-05-SUMMARY.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md
@CLAUDE.md
@tox.ini
</context>

<!-- planner-discipline-allow: libz.so.1 -->
<!-- planner-discipline-allow: NOT MET -->
`libz.so.1` is named in actions because the audit looks for it, and a gate negative-greps it only in the
recorded `GENUINE = {…}` set. `NOT MET` is one of the two words the closing table must use; the gate
requires that no row carries it.

## Carried-in baselines (re-measure; never inherit)

| Baseline | Value | Source |
|----------|-------|--------|
| Tests collected | **1548** | `64-NIX05-WORKTREE-EVIDENCE.md` § NIX-04 (re-counted 2026-09-11); no `tests/` or `typsphinx/` change since |
| Full suite, dev extra | **1543 passed, 5 skipped**, measured under nix python3-3.13.13 | `63-GREEN-TREE-EVIDENCE.md` § SC#4; the five skips are 4 myst-parser docs-extra gaps and 1 env-gated corpus report |
| Skip that should vanish | `tests/test_admonition_greyscale_pipeline.py:71` (Pillow gate) | `64-NIX05-WORKTREE-EVIDENCE.md` skip table, row 1 — caused by the same `libz.so.1` failure |
| `docs-html` / `docs-pdf` warnings | **3 / 5**, clean builds | `63-GREEN-TREE-EVIDENCE.md`; matched exactly in 64-02 |
| `ruff` pin | **0.15.20** | `uv.lock:1209-1210` |
| Worktree interpreter | uv-managed cpython-3.14.4 (`.venv`), uv cpython-3.12.13 (`.tox/py312`), nix python3-3.13.13 (`.tox/py313`) | `64-LIBZ-DIAGNOSIS.md` § 3; `64-LIBZ-FIX-EVIDENCE.md` provisioning section |
| New rootfs and shim paths | the `New fhs-run:` line and the `New shim paths` table | `64-LIBZ-FIX-EVIDENCE.md` |

## Flagged edge-probe assumptions (unclassified rows — surfaced, not silently dropped)

- **NIX-04 (unclassified).** "Environment-caused" is attributed by comparison with the carried-in
  baseline and its skip reasons. A failure absent from that baseline counts as environment-caused unless
  its traceback names product behaviour. That is a judgment, and the evidence must show the traceback it
  rests on.
- **Interpreter provenance (assumption, not a finding).** The worktree `.venv` runs uv cpython-3.14 while
  the 1543 / 5 baseline was taken under nix python3-3.13.13. `tox -e py313` matched the baseline exactly
  in 64-02, and the diagnostic full suite in 64-05 was taken under cp3.14. Any divergence that can be
  attributed to cp3.14 is recorded as such. It is never normalised away by pinning an interpreter.

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `64-GAP-REMEASURE-EVIDENCE.md` | evidence file (NIX-01..NIX-05 after the rootfs fix), with a `GENUINE = {…}` line and a `## Requirement closure` table | phase directory |

No code symbol and no tracked file other than the evidence is created by this plan.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — relaunched-session check, D-09 head check, the one provisioning line, NIX-01 regression, then the exact path that failed in 64-02 through the genuine shape</name>
  <precondition>The `/nix/store/<hash>-typsphinx-fhs-run/bin/typsphinx-fhs-run` path grepped from the body of `command -v ruff` has `/usr/lib/libz.so.1` in its root (`"$FHS" /bin/sh -c 'test -e /usr/lib/libz.so.1'` exits 0), and its `/nix/store/<hash>-typsphinx-fhs-run` prefix equals the `New fhs-run:` line in 64-LIBZ-FIX-EVIDENCE.md. That is true only in a session launched after 64-05 merged; the pre-relaunch root `dgddrdfkvigqsv48k563szqc8w7xlw2g` has no libz. If not, halt at a blocking-human gate: the maintainer opens a terminal in /home/yuta/Documents/typsphinx so direnv re-evaluates the flake, confirms the same test exits 0, relaunches Claude Code from that shell, and re-runs /gsd-execute-phase 64 --gaps-only.</precondition>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md</files>
  <read_first>
    - CLAUDE.md § "Worktree-isolated execution" (the provisioning line verbatim; the editable-install hazard)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-LIBZ-FIX-EVIDENCE.md (§ Session relaunch required before wave 5, § NIX-07 carry-over's `New shim paths` table, § GREEN — tracer, § Residual verdicts)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md (the analog: head-check shape, provisioning record, NIX-01 section, the failing node's traceback)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-02-SUMMARY.md § Issues Encountered (the harness refused literal xtrace invocations; the helper-script workaround)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-02, D-04, D-06, D-09)
    - flake.nix (read-only: `fhsRun`'s `targetPkgs`, `venvWalk`, `uvShim`)
    - uv.lock lines 1209-1210
  </read_first>
  <action>
1. **Session check (the precondition, recorded).**
   - Record `command -v ruff`, the `FHS` path grepped from its body, `"$FHS" /bin/sh -c 'test -e /usr/lib/libz.so.1'; echo "exit:$?"` (it must be `exit:0`), and the matching `New fhs-run:` line from 64-LIBZ-FIX-EVIDENCE.md.
   - State that the same check is false for the pre-relaunch root `dgddrdfkvigqsv48k563szqc8w7xlw2g`, citing 64-LIBZ-FIX-EVIDENCE.md's GREEN section.

2. **D-09 head check, before anything is provisioned.** Record:
   - `date -u +%FT%TZ`
   - `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git`
   - `test ! -e .venv` and `test ! -e .tox`
   - `command -v` for each of `uv tox ruff black mypy pytest sphinx-build`, each compared against 64-LIBZ-FIX-EVIDENCE.md's `New shim paths` table (all seven must match)
   - `echo "$DIRENV_DIR"` and `echo "$IN_NIX_SHELL"`
   - `direnv status 2>&1 | grep -E 'RC'`, taken from inside the worktree

   Write one sentence answering whether the shim PATH is reachable here before any direnv allow for this worktree path. Do not run a per-worktree direnv allow and do not wrap anything in `nix develop`; D-09 rejects both.

3. **Provision exactly as CLAUDE.md documents (D-04):** `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, with no interpreter flag.
   - Record `cat .venv/pyvenv.cfg`, naming its `home` and `version_info`, beside the baseline interpreter (nix python3-3.13.13, from 63-GREEN-TREE-EVIDENCE.md).
   - Record tree identity: `uv run python -c 'import typsphinx,os;print(os.path.realpath(typsphinx.__file__))'` must print `$(pwd -P)/typsphinx/__init__.py`. This proves the editable install binds this worktree, and that the `-u` unsets reached `uv` through the sandbox (D-06).
   - No manual symlink and no ELF-patching step happens anywhere (NIX-05).

4. **NIX-01 regression.**
   - `ruff --version` must print exactly `ruff 0.15.20`, compared against `uv.lock` read at run time.
   - Record the shim's `+ exec` xtrace line for `ruff --version`; it must name `$(pwd)/.venv/bin/ruff` inside this worktree. If the harness refuses a command containing a literal xtrace invocation, use 64-02's documented workaround: write the invocation into a helper script in the session scratchpad and run that script.
   - `ruff check .` must run to completion. Record its output and exit code.

5. **Tracer: the one path that failed, end to end in the genuine shape.**
   - Set `SP="$(ls -d "$(pwd)"/.venv/lib/python3.*/site-packages)"`, then run `uv run python -S -c "import sys; sys.path.insert(0, '$SP'); import PIL._imaging; print('PIL._imaging OK')"`. That path is session PATH → uv shim (leg 2) → new rootfs → uv-managed cp3.14 → Pillow, with `-S` making it import-order independent. It must print the OK line.
   - Then `pytest tests/test_converted_image_collision_render_gate.py -q -rs` through the bare pytest shim must show zero failed. This is the node 64-02 recorded failing with the `libz.so.1` ImportError.

6. Write `64-GAP-REMEASURE-EVIDENCE.md`.
   - Open it with the line `Measured shape: genuine D-09 shape — inherited session PATH of a session relaunched after 64-05 merged; no nix develop wrapper, no direnv exec.`
   - Then the sections `## Session check`, `## D-09 head check (session relaunched after 64-05 merged)`, `## Provisioning, interpreter provenance and tree identity`, `## NIX-01 — ruff (regression)` and `## Tracer — the path that failed in 64-02`.
   - Use the `$ <command>` + verbatim output + interpretive prose convention.

   Commit.
  </action>
  <verify>
    <automated>E6=.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md &amp;&amp; E5=.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-LIBZ-FIX-EVIDENCE.md &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; FHS="$(grep -oE '/nix/store/[a-z0-9]{32}-typsphinx-fhs-run/bin/typsphinx-fhs-run' "$(command -v ruff)" | head -n 1)" &amp;&amp; [ -n "$FHS" ] &amp;&amp; "$FHS" /bin/sh -c 'test -e /usr/lib/libz.so.1' &amp;&amp; grep -qFx "New fhs-run: ${FHS%/bin/typsphinx-fhs-run}" "$E5" &amp;&amp; ruff --version | grep -Fx 'ruff 0.15.20' &amp;&amp; test -x "$PWD/.venv/bin/ruff" &amp;&amp; ruff check . &amp;&amp; [ "$(uv run python -c 'import typsphinx,os;print(os.path.realpath(typsphinx.__file__))')" = "$(pwd -P)/typsphinx/__init__.py" ] &amp;&amp; SP="$(ls -d "$PWD"/.venv/lib/python3.*/site-packages)" &amp;&amp; uv run python -S -c "import sys; sys.path.insert(0, '$SP'); import PIL._imaging" &amp;&amp; pytest tests/test_converted_image_collision_render_gate.py -q &amp;&amp; grep -q 'genuine D-09 shape' "$E6" &amp;&amp; grep -q 'version_info' "$E6" &amp;&amp; grep -q 'Found RC' "$E6" &amp;&amp; grep -q 'PIL._imaging OK' "$E6" &amp;&amp; [ -z "$(git status --porcelain -- flake.nix flake.lock typsphinx tests scripts .github CLAUDE.md tox.ini pyproject.toml uv.lock)" ]</automated>
    <fails_when>non-zero exit; `pwd -P` outside `/home/yuta/Documents/typsphinx/.claude/worktrees/`; no rootfs path in the ruff shim, or `/usr/lib/libz.so.1` absent from its root (a stale session); the rootfs prefix not matching the `New fhs-run:` line; a version line other than `ruff 0.15.20`; `ruff check .` exiting non-zero; `typsphinx.__file__` resolving outside this worktree; the `-S` probe raising; the collision-gate file failing; the evidence missing the shape line, `version_info`, the direnv `Found RC` lines or the probe's OK line; a scope-fenced path modified</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records the session check (`exit:0` for the libz test, plus the matching `New fhs-run:` line) BEFORE the head check. The head check records ascending UTC timestamps, `.venv` and `.tox` absent, seven `command -v` paths each equal to 64-05's `New shim paths` table, and the `direnv status` RC lines, followed by the one-sentence reachability answer.
    - The provisioning command transcribed is exactly `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, and `.venv/pyvenv.cfg`'s `home` and `version_info` appear beside the baseline interpreter.
    - `typsphinx.__file__` resolves to `<pwd -P>/typsphinx/__init__.py`.
    - `ruff --version` prints `ruff 0.15.20`, the xtrace `+ exec` line names `<this worktree>/.venv/bin/ruff`, and `ruff check .`'s output and exit code are recorded.
    - The tracer section records the `-S` probe printing `PIL._imaging OK` and the collision-gate file with zero failed, both through the inherited PATH.
  </acceptance_criteria>
  <done>In a session proven to carry the fixed rootfs, a fresh nested worktree provisions with the one documented line, `ruff` still reports the `uv.lock` pin, and the exact path that failed in 64-02 now passes, all in the genuine D-09 shape.</done>
</task>

<task type="auto">
  <name>Task 2: NIX-04 — one full-suite run through the bare pytest shim, baseline gated literally, every skip and any divergence itemised</name>
  <precondition>The same session check as Task 1 holds (the ruff shim's rootfs has `/usr/lib/libz.so.1` and matches the `New fhs-run:` line), and this worktree's `.venv` exists from Task 1.</precondition>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md (Task 1's sections)
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-GREEN-TREE-EVIDENCE.md § SC#4 — full pytest suite (the 1543 / 5 baseline and its five itemised skips)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md § NIX-04 (the six-skip table and the failing traceback this run is compared against)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-LIBZ-FIX-EVIDENCE.md § Second-gap sweep (DIAGNOSTIC) (the cp3.14 diagnostic result)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-08: one full-suite run, no second locale)
  </read_first>
  <action>
1. **Run the full suite ONCE, through the bare `pytest` shim, under the maintainer's own locale.** Set no locale variable (D-08). Command: `pytest -q -rs`, with the Bash timeout at 600000 ms, or `run_in_background` and its completion notification if it would exceed that. Record:
   - the session header, including its `platform … Python 3.X` line
   - `collected <n> items` (1548 expected)
   - the summary line

2. **Compare against the carried-in baseline: 1543 passed, 5 skipped under the dev extra.**
   - Itemise every skip with its node or file:line and its reason, verbatim. Four should be the myst-parser docs-extra gap and one the env-gated corpus report.
   - Confirm the Pillow-gated skip at `tests/test_admonition_greyscale_pipeline.py:71`, row 1 of 64-02's table, is gone. A sixth skip is a finding, not noise.
   - If the summary is anything other than `1543 passed, 5 skipped`, add `## Baseline divergence`. It lists every differing node: for a failure, its node id and first traceback frame; for a skip, its reason. Each is attributed as environment-caused (loader, soname, locale, path, or a binary missing inside the sandbox) or not (product behaviour, or a cp3.14-specific behaviour visible in the traceback). The baseline itself is never adjusted. An environment-caused entry makes NIX-04 NOT MET in Task 3's closing table, and it is not fixed here: `flake.nix` is out of this plan's scope, and a rootfs change would force another relaunch.

3. Append `## NIX-04 — full suite (one run, maintainer locale)`, and `## Baseline divergence` only if it occurred. Commit.
  </action>
  <verify>
    <automated>E6=.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md &amp;&amp; FHS="$(grep -oE '/nix/store/[a-z0-9]{32}-typsphinx-fhs-run/bin/typsphinx-fhs-run' "$(command -v pytest)" | head -n 1)" &amp;&amp; "$FHS" /bin/sh -c 'test -e /usr/lib/libz.so.1' &amp;&amp; pytest --collect-only -q 2&gt;&amp;1 | grep -Eq '^1548 tests collected' &amp;&amp; OUT="$(pytest tests/test_admonition_greyscale_pipeline.py -q -rs 2&gt;&amp;1)" &amp;&amp; ! printf '%s' "$OUT" | grep -qF 'Pillow and typst-py are both required' &amp;&amp; grep -q '^## NIX-04' "$E6" &amp;&amp; grep -q 'collected 1548 items' "$E6" &amp;&amp; ( grep -qE '1543 passed, 5 skipped in ' "$E6" || grep -q '^## Baseline divergence' "$E6" ) &amp;&amp; [ "$(grep -c 'pytest -q -rs' "$E6")" -ge 1 ] &amp;&amp; [ -z "$(git status --porcelain -- flake.nix flake.lock typsphinx tests scripts .github CLAUDE.md tox.ini pyproject.toml uv.lock)" ]</automated>
    <fails_when>non-zero exit; the pytest shim's rootfs lacking `/usr/lib/libz.so.1`; the live collection count not reading `1548 tests collected`; the greyscale-pipeline file still reporting its Pillow-gated skip; the evidence lacking the NIX-04 section, the `collected 1548 items` line, or both the `1543 passed, 5 skipped` summary and a `## Baseline divergence` section; no `pytest -q -rs` transcript recorded; a scope-fenced path modified</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records exactly one full-suite run (`pytest -q -rs`) with its Python version header, `collected 1548 items` and its summary line.
    - Every skip appears with its node or file:line and its verbatim reason. The Pillow-gated skip from 64-02's table is stated absent.
    - If the summary is not `1543 passed, 5 skipped`, `## Baseline divergence` lists every differing node with its traceback frame or skip reason, and attributes each as environment-caused or not.
    - No locale variable is set in any command of this task.
  </acceptance_criteria>
  <done>The full suite runs once, through the fixed sandbox, and is compared literally against the carried-in baseline, with every skip and any divergence itemised and attributed.</done>
</task>

<task type="auto">
  <name>Task 3: NIX-02 and NIX-03 — all seven tox environments cold in the fixed sandbox, docs-pdf producing a real PDF, the genuine residual audit, then the requirement closure table</name>
  <precondition>The same session check as Task 1 holds (the tox shim's rootfs has `/usr/lib/libz.so.1` and matches the `New fhs-run:` line), and `test ! -e .tox` still holds in this worktree before the first tox run.</precondition>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md</files>
  <read_first>
    - tox.ini (every environment; `changedir = docs` for docs-html and docs-pdf; `requires = tox-uv-bare~=1.35` — read-only)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md § NIX-02 and § NIX-03 (the analog transcripts; the venv> line shape; the clean-build discipline)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-LIBZ-FIX-EVIDENCE.md (§ BEFORE residual audit for the script verbatim, § Residual verdicts, the `New shim paths` table)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-07; § Deferred Ideas on the cold `@preview` cache)
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-GREEN-TREE-EVIDENCE.md § docs builds (the 3 / 5 counts)
  </read_first>
  <action>
1. **NIX-02.** Record `test ! -e .tox` first: every environment is provisioned cold inside the fixed sandbox (the NIX-02 empty edge).
   - Run each environment in its own Bash call, with the timeout at 600000 ms or `run_in_background` and its completion notification.
   - Use this fixed order and record each environment separately: `tox -e lint`, `tox -e type`, `tox -e py312`, `tox -e py313`. Each must end with its `<env>: OK` line and `congratulations :)`; record the tail and the exit code.
   - For each environment, record that its `venv>` line names the `uv` shim path listed in 64-LIBZ-FIX-EVIDENCE.md's `New shim paths` table. That shows tox's nested `uv` entry lands in the fixed sandbox.
   - For `py312`, record the pytest header naming `Python 3.12`, its summary line, and `cat .tox/py312/pyvenv.cfg`. For `py313`, record its summary line and `.tox/py313/pyvenv.cfg`.

2. **NIX-03, in this fixed order.** The `rm -rf` belongs to each build command, not to a preamble, because the two builds share `docs/_build` (the NIX-03 adjacency edge).
   - `tox -e cov`: record its `TOTAL` line, its pytest summary and `cov: OK`.
   - `rm -rf docs/_build` followed by `tox -e docs-html`: record the `build succeeded` line and its warning count against the baseline of 3.
   - `rm -rf docs/_build` followed by `tox -e docs-pdf`: record the `Generated PDF:` line and the `build succeeded` line with its warning count against the baseline of 5. Then record `test -s docs/_build/pdf/typsphinx.pdf`, `head -c 4 docs/_build/pdf/typsphinx.pdf` (must be `%PDF`) and `stat -c %s` in bytes.
   - A warning count that differs from 3 or 5 is not silently accepted: record the differing warning lines verbatim and attribute them.
   - D-07: record `ls "$HOME/.cache/typst/packages/preview" | wc -l` before and after docs-pdf. If docs-pdf cannot reach its `@preview` packages, record the failing transcript and mark NIX-03 NOT MET. The fix belongs to a further gap-closure plan that edits `flake.nix` inside Phase 64, and this plan does not edit it.

3. **Genuine residual audit.** This applies the frame inversion to the environments this plan actually provisioned.
   - Copy the audit script verbatim from 64-LIBZ-FIX-EVIDENCE.md § BEFORE residual audit into the session scratchpad (D-05: never a tracked file).
   - Run it through this session's rootfs (`"$FHS" /bin/sh <script> <roots…>`). The roots are:
     - `$(pwd)/.venv`
     - every `$(pwd)/.tox/<env>`
     - the `realpath` of the directory above each distinct `pyvenv.cfg` `home`
   - Record `scanned=<n>`, every UNRESOLVED line, and the unique soname set on one line in exactly the form `GENUINE = {…}`. `libz.so.1` must not appear.
   - For every soname present, either cite its row and verdict in 64-LIBZ-FIX-EVIDENCE.md § Residual verdicts, or give it a new row by the same rule: owning objects, import names, an import search over `typsphinx/`, `tests/`, `docs/` and every provisioned site-packages, and the gate results above. A soname that a failing gate here forces makes the affected requirement NOT MET, and it becomes a new gap for the owner; there is no `flake.nix` edit here.

4. **Closing sections.**
   - `## NIX-05 — procedure summary`: one paragraph naming this fresh nested worktree, the session check and the head check first, the one provisioning line, then every gate, with no manual symlink or ELF-patch step anywhere and every tool target resolved inside this worktree.
   - A no-dispatch note: no workflow under `.github/workflows/` references nix or flake (64-LIBZ-FIX-EVIDENCE.md), so no push and no CI dispatch belong to this plan.
   - `## Requirement closure`: a table with one row each for NIX-01, NIX-02, NIX-03, NIX-04 and NIX-05, reading `MET` or `NOT MET`, each naming the section of this file that decides it. The table follows the transcripts and never replaces them.

   Commit.
  </action>
  <verify>
    <automated>E6=.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md &amp;&amp; FHS="$(grep -oE '/nix/store/[a-z0-9]{32}-typsphinx-fhs-run/bin/typsphinx-fhs-run' "$(command -v tox)" | head -n 1)" &amp;&amp; "$FHS" /bin/sh -c 'test -e /usr/lib/libz.so.1' &amp;&amp; tox -e lint &amp;&amp; tox -e py312 -- tests/test_converted_image_collision_render_gate.py &amp;&amp; rm -rf docs/_build &amp;&amp; tox -e docs-pdf &amp;&amp; test -s docs/_build/pdf/typsphinx.pdf &amp;&amp; [ "$(head -c 4 docs/_build/pdf/typsphinx.pdf)" = '%PDF' ] &amp;&amp; test -d .tox/py312 &amp;&amp; test -d .tox/py313 &amp;&amp; test -d .tox/cov &amp;&amp; ( for e in lint type py312 py313 cov docs-html docs-pdf; do grep -q "$e: OK" "$E6" || { echo "missing: $e"; exit 1; }; done ) &amp;&amp; grep -q 'Python 3.12' "$E6" &amp;&amp; [ "$(grep -c 'rm -rf docs/_build' "$E6")" -ge 2 ] &amp;&amp; grep -q '%PDF' "$E6" &amp;&amp; grep -E '^GENUINE = \{' "$E6" | tail -n 1 | grep -qvF 'libz.so.1' &amp;&amp; grep -q '^## Requirement closure' "$E6" &amp;&amp; ! grep -qE '^\| *NIX-0[1-5] *\|.*NOT MET' "$E6" &amp;&amp; grep -q 'NIX-05 — procedure summary' "$E6" &amp;&amp; [ -z "$(git status --porcelain -- flake.nix flake.lock typsphinx tests scripts .github CLAUDE.md tox.ini pyproject.toml uv.lock)" ]</automated>
    <fails_when>non-zero exit from the live `tox -e lint`, the live `tox -e py312` run on the collision-gate file, or the live `tox -e docs-pdf`; the tox shim's rootfs lacking `/usr/lib/libz.so.1`; the PDF missing, empty or not beginning with `%PDF`; `missing: <env>` printed for any of the seven environments; no `Python 3.12` header recorded; fewer than two clean-build commands recorded; no `GENUINE = {…}` line, or the last one naming libz.so.1; the closure table absent or any NIX-01..NIX-05 row reading NOT MET; the NIX-05 summary absent; a scope-fenced path modified</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records `test ! -e .tox` before the first tox run, then seven separately recorded environments in the stated order, each ending with its `<env>: OK` line. Each `venv>` line names the `New shim paths` uv entry.
    - `py312`'s pytest header names Python 3.12 and its `.tox/py312/pyvenv.cfg` is recorded. `py313`'s summary and `pyvenv.cfg` are recorded.
    - Each docs build command in the evidence begins with `rm -rf docs/_build`. The warning counts are compared against 3 and 5, and any difference is quoted and attributed.
    - `docs/_build/pdf/typsphinx.pdf` is non-empty, its first four bytes are `%PDF`, and its size in bytes and the before/after `@preview` counts are recorded.
    - A `GENUINE = {…}` line without `libz.so.1` is recorded, and every soname in it cites a 64-05 verdict or carries its own row.
    - `## Requirement closure` holds five rows, NIX-01 to NIX-05, each naming its deciding section.
  </acceptance_criteria>
  <done>All seven tox environments run to completion cold inside the fixed sandbox, with docs-pdf producing a real PDF. The provisioned environments carry no unresolved `libz.so.1` edge, and NIX-01 to NIX-05 each have a closure verdict backed by a genuine-shape transcript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session PATH → worktree | the shims reach the worktree only by inheritance from the launching session; a pre-relaunch session silently provides the unfixed rootfs |
| worktree → main checkout | the worktree is nested inside the main checkout, whose `.venv` holds a runnable nix-3.13 toolchain and a PEP-660 finder bound to the main tree |
| evidence → verifier | this file is the NIX-02/03/04 authority after the fix; a transcript from the wrong session, tree or shape would certify nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-23 | Repudiation | a measurement taken in the pre-relaunch session presented as the post-fix observation | medium | mitigate | every task's precondition tests `/usr/lib/libz.so.1` in the shim-embedded rootfs and matches the `New fhs-run:` line; each verify repeats the rootfs test live |
| T-64-24 | Tampering | measuring the main checkout instead of this worktree (editable-install hazard, nested location) | medium | mitigate | `typsphinx.__file__` identity gate; ruff xtrace path; every `command -v` compared against the `New shim paths` table |
| T-64-25 | Repudiation | a failure attributed away, or the baseline moved to match | medium | mitigate | the 1543 / 5 and 3 / 5 baselines are gated literally; any divergence needs a node-by-node section with tracebacks; the closure table must show every row MET for the plan's verify to pass |
| T-64-26 | Denial of Service | a cold tox provisioning exceeding one Bash call | low | mitigate | one environment per Bash call at a 600000 ms timeout, with `run_in_background` for any call that would exceed it |
</threat_model>

<verification>
- The session check, then the head check, then provisioning, then the gates, in that order, in one fresh nested worktree, in the genuine D-09 shape.
- `ruff 0.15.20` through the shim; the path that failed in 64-02 passes; the full suite reads 1543 passed and 5 skipped, or carries an attributed divergence section.
- Seven tox environments are OK; the PDF begins with `%PDF`; the genuine residual set carries no `libz.so.1`.
- `## Requirement closure` reads MET for NIX-01 to NIX-05, and `git status --porcelain` over the scope-fenced paths is empty.
</verification>

<success_criteria>
SC#1 and SC#2 are observed again, after the fix, in a session proven to carry it. The maintainer's documented commands, the full suite and every tox environment run to completion on this machine through the shims, with no environment-caused failure. A freshly created worktree still needs only the one documented provisioning line.
</success_criteria>

<output>
Create `.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-06-SUMMARY.md` when done. Frontmatter declares `requirements-completed:` listing exactly the NIX-01..NIX-05 rows that read MET in `## Requirement closure`. If any row reads NOT MET, the SUMMARY's first section states which row and why, and names the new gap for the owner.
</output>
