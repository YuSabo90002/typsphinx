---
phase: 68-documentation-follow-through-claude-md-tox-ini-flake-nix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLAUDEMD-EVIDENCE.md
autonomous: true
requirements: [DOC-19]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-08: CLAUDE.md line 11 reads exactly "Development uses `uv` for env/dependency management and `tox` (with `tox-uv`) as the task runner.", and the Conventions bullet is one line starting "- `tox.ini` pins `tox-uv~=1.35`" that keeps the ini-parser explanation (a single-line `requires` is split on commas, so `>=1.35,<2` breaks tox startup; `~=1.35` is the comma-free equivalent), names `tox-uv-bare` only as history tied to NixOS and the FHS shims, and no longer tells readers not to return to plain `tox-uv`.'
    - 'D-01 and D-03: the worktree provisioning recipe is byte-identical to the pre-phase CLAUDE.md. Everything from "When operating inside a worktree, provision" to end of file hashes identically before and after, so the `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` line, the `uv run pytest` line, list items 1-2 and the Applicability paragraph are untouched.'
    - 'D-01: a boundary paragraph sits inside "### Worktree-isolated execution" between the Detection rule and the provisioning sentence. It states the recipe is unchanged and mandatory, `flake.nix` does not substitute for it, on the maintainer NixOS machine the recipe itself runs through the shims, and the shims arrive by PATH inheritance from a session launched in the direnv-loaded main checkout, never by direnv in the worktree (whose `.envrc` is never allowed).'
    - 'D-04: a new "### NixOS development shell" subsection sits after the last Conventions bullet and immediately before "### Worktree-isolated execution". It names all seven shimmed commands, says each runs the tree own `.venv/bin/<tool>` inside an FHS sandbox, that a missing `.venv/bin/<tool>` fails loudly with exit 127 and a provisioning hint, that `uv` alone falls back to nixpkgs `uv` for bootstrapping, and that once `.venv` exists `uv --version` reports the `uv.lock` pin.'
    - 'D-03: the subsection states the launch prerequisite (Claude Code launched from a shell where direnv loaded the main checkout devShell) and gives the check `grep -q typsphinx-fhs-run "$(command -v uv)"`, scoped as NixOS-only (CI and non-NixOS contributors have no shims and need none).'
    - 'D-02: the subsection says no manual `ln -sf` / `patchelf` step exists or is needed, and that a `Could not start dynamically linked executable` error means the shim is not on PATH, not a code regression. Evidence transcribes both `git log -S` outputs and the `git grep` at base, each empty, and every `ln -sf` / `patchelf` line in CLAUDE.md after the edit lies inside the new subsection.'
    - 'D-05: the subsection says the sandbox passes `LANG` through unchanged, so warning text stays localised, CI runs in English, and a test asserting warning text should also be run under `LC_ALL=C` locally.'
    - 'D-06: the subsection says a fresh worktree `.venv` and the main checkout `.venv` may be on different interpreters that may differ in version, and tells the reader to compare both `pyvenv.cfg` `home` / `version_info` before comparing test counts. The subsection contains no dotted version number at all.'
    - 'D-07: CLAUDE.md carries operational facts only; the subsection points at the `flake.nix` header for the rationale, and neither the subsection nor the boundary paragraph explains the devShell shape, the falsified alternatives or the process-inheritance argument.'
    - '`## Conventions & gotchas` appears exactly once and unchanged (tests/test_toolchain_config_gate.py:164 cites it), and the only lines removed from CLAUDE.md are the old line 11 and the old Conventions bullet; everything else is additive.'
    - 'The pytest collected count in this worktree is identical before and after the edit (text-only change).'
  artifacts:
    - path: CLAUDE.md
      provides: 'D-08 line 11 and Conventions bullet; ### NixOS development shell subsection (D-02..D-07); D-01 boundary paragraph'
      contains: '### NixOS development shell'
    - path: .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLAUDEMD-EVIDENCE.md
      provides: 'head check, baseline, D-02 evidence at base, D-08 rewrite, subsection and boundary paragraph, gates after the edit'
      contains: 'BASE_68_01 = '
  key_links:
    - from: 'CLAUDE.md ### NixOS development shell'
      to: 'flake.nix header notes'
      via: 'one pointer sentence naming the flake.nix header (D-07)'
      pattern: 'flake.nix'
    - from: 'CLAUDE.md shim check'
      to: 'the uv shim on PATH'
      via: 'grep -q typsphinx-fhs-run "$(command -v uv)"'
      pattern: 'typsphinx-fhs-run'
    - from: 'boundary paragraph in ### Worktree-isolated execution'
      to: 'the unchanged provisioning recipe below it'
      via: 'placement immediately above "When operating inside a worktree, provision"'
      pattern: 'When operating inside a worktree, provision'
  prohibitions:
    - statement: 'MUST NOT alter, condition or replace the worktree provisioning recipe or its run-everything-via-uv-run instruction, and MUST NOT suggest the shims make provisioning optional (constraint 11, Pitfall 3).'
      requirement_id: DOC-19
      category: safety
      status: resolved
      verification: test
    - statement: 'MUST NOT state that worktree executors are unaffected by or unassisted by flake.nix on the maintainer NixOS machine; that reading was measured false (D-01, AMENDED 2026-09-12).'
      requirement_id: DOC-19
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT present the NixOS shim check or launch prerequisite as applying to CI or to non-NixOS contributors (D-03).'
      requirement_id: DOC-19
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT state an interpreter, uv or tool version as a permanent fact in the new subsection (D-06, D-04).'
      requirement_id: DOC-19
      category: transparency
      status: resolved
      verification: test
---

<objective>
Bring `CLAUDE.md` in line with what Phases 64 and 65 actually landed (DOC-19).

- **D-08:** line 11 and the `tox.ini` Conventions bullet name the landed `tox-uv~=1.35` pin. The ini-parser explanation stays, `tox-uv-bare` becomes history, and the do-not-revert instruction goes.
- **D-04, D-02, D-03, D-05, D-06, D-07:** a new `### NixOS development shell` subsection, placed immediately before `### Worktree-isolated execution`, carries the operational facts. These are the seven shims, exit 127, the `uv` bootstrap fallback, the launch prerequisite and the shim check, the absence of any manual step, locale, interpreters, and a pointer to `flake.nix`.
- **D-01:** one boundary paragraph above the provisioning recipe states, in the file's own words, that the recipe is unchanged and mandatory, and that on NixOS it runs through shims inherited from the launching session.
- **D-02:** the retirement of a manual `ln -sf` / `patchelf` step is vacuous. That is evidenced at base, then stated as one negative sentence.

The provisioning recipe stays byte-identical, and so does everything after it.

Output: the edited `CLAUDE.md`, and `68-CLAUDEMD-EVIDENCE.md` with verbatim transcripts and `KEY = value` lines.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-PATTERNS.md
@CLAUDE.md
@flake.nix
</context>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ln -sf -->
<!-- planner-discipline-allow: patchelf -->
`## HALT` is a heading the executor writes only on a failure path; the gates require its absence. `ln -sf` and `patchelf` are measured at base, where they must be absent, and are then written into the D-02 negative sentence on purpose.

## Planning-time census (2026-09-13: re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| Milestone branch tip | `3c9bfcee` (only `.planning/` changed since RESEARCH's `89416753`) | `git log --oneline 89416753..HEAD` |
| `CLAUDE.md:11` | `... and \`tox\` (with \`tox-uv-bare\`) as the task runner.` | `sed -n 11p` |
| `CLAUDE.md:77` | the `tox-uv-bare~=1.35` Conventions bullet, one line | `sed -n 77p` |
| `CLAUDE.md:73` | `## Conventions & gotchas`, cited by `tests/test_toolchain_config_gate.py:164` | `grep -rn` |
| `CLAUDE.md:80-96` | `### Worktree-isolated execution`, the last section in the file; the recipe starts at `:84` ("When operating inside a worktree, provision…") | `Read` |
| D-02 at base | `git log --oneline -S "ln -sf" -- CLAUDE.md`: 0 lines. `git log --oneline -S patchelf -- CLAUDE.md`: 0 lines. `git grep -nE 'ln -sf\|patchelf' -- ':!.planning'`: 0 hits. | session commands |
| uv shim | `command -v uv` gives `/nix/store/3vpzk25whpm7s8zq5flpk8gbpkggj9np-uv/bin/uv`; `grep -c typsphinx-fhs-run` on it gives 2 | session commands |
| Strict-shim message | `typsphinx-shim: <tool>: no executable .venv/bin/<tool> found walking up from $start (stopped at $dir); provision with: env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, exit 127 | `flake.nix:67-72` |
| Tokens absent from CLAUDE.md today | `typsphinx-fhs-run`, `LC_ALL`, `pyvenv`, `direnv`, and both manual-step words | `grep -n` (no hits) |

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `### NixOS development shell` | new CLAUDE.md subsection heading (named element) | `CLAUDE.md`, between the last `## Conventions & gotchas` bullet and `### Worktree-isolated execution` |
| NixOS boundary paragraph | new paragraph | `CLAUDE.md`, inside `### Worktree-isolated execution`, above "When operating inside a worktree, provision" |
| Rewritten line 11 and Conventions `tox.ini` bullet | edited lines | `CLAUDE.md` |
| `68-CLAUDEMD-EVIDENCE.md` | new evidence file. Sections: `## Head check and provisioning`, `## Baseline`, `## D-02 evidence at base`, `## D-08 rewrite`, `## NixOS subsection and boundary paragraph`, `## Gates after the edit`. Keys: `BASE_68_01`, `UV_SHIM_PATH`, `UV_SHIM_FHS_COUNT`, `PYVENV_HOME_68_01`, `PYVENV_VERSION_68_01`, `COLLECT_BEFORE_68_01`, `COLLECT_AFTER_68_01`, `LNSF_COMMITS`, `PATCHELF_COMMITS`, `LNSF_PATCHELF_GREP_HITS_BEFORE`, `NIXOS_SUBSECTION_HEADING` | phase directory |

<assumption_delta_decision>
Detector: `detected: true`, one signal, `pluralization` on the term "also", in the snippet "(DOC-19). It also states the boundary explicitly". Primary noun: none, because this phase changes no identity model, key or anchor; it edits prose. Decision: `no-change`. Rationale: the signal is a conjunction in the success-criterion text, not a second instance of any modelled thing.
</assumption_delta_decision>

## Flagged assumptions (spec-less fallback, surfaced and not auto-resolved)

- **Edge-probe row `DOC-19`:** category `unclassified`, status `unresolved`. No edge was auto-resolved or dismissed. Planner's reading, for the verifier to confirm or reject:
  - DOC-19's mechanically checkable edges are the ones Tasks 1-2 gate:
    - the recipe-tail byte identity
    - the two-line removal fence
    - the heading order
    - the token presence in the subsection and the boundary paragraph
    - the dotted-version absence
  - Whether the prose accurately reports the Phase 64/65 measurements is a judgment check at verify time, against `68-RESEARCH.md` § Measured Facts.
- **Prohibitions:** the four prohibitions in `must_haves.prohibitions` carry no wired-check descriptor (spec-less fallback), so each disposes as flagged-unverified downstream. The two `test`-tier ones are backed by this plan's gates, but no descriptor points at them.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: head check, baseline and D-02 evidence at base, then the D-08 rewrite of CLAUDE.md line 11 and the tox.ini Conventions bullet, gated end to end</name>
  <precondition>The session was launched from a shell in which direnv loaded the main checkout's devShell, so `grep -q typsphinx-fhs-run "$(command -v uv)"` succeeds inside this worktree. If it fails, write `## HALT: uv shim not on PATH` to the evidence and stop: this is an environment prerequisite, not a code regression.</precondition>
  <reversibility rating="reversible">Prose in a git-tracked file; one revert restores it.</reversibility>
  <files>CLAUDE.md, .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLAUDEMD-EVIDENCE.md</files>
  <read_first>
    - CLAUDE.md (whole file, 96 lines; lines 11, 73-78 and 80-96 are the edit and anchor sites)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md (D-02, D-08; § Phase Boundary edit footprint)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md (§ Current Text Inventory → CLAUDE.md; § Common Pitfalls 5)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-PATTERNS.md (§ CLAUDE.md; § Shared Patterns)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md (§ Head check: the evidence shape to mirror)
    - tox.ini (lines 1-11: the pin the bullet describes)
  </read_first>
  <action>
1. **`## Head check and provisioning`** (CLAUDE.md § Worktree-isolated execution; mandatory). Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`, which must print `exit:0`
   - `command -v uv` and `grep -c typsphinx-fhs-run "$(command -v uv)"`, recorded as `UV_SHIM_PATH` and `UV_SHIM_FHS_COUNT` (at least 1). On failure, apply the precondition's HALT.
   - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, the tail of its output
   - `BASE_68_01 = <git merge-base HEAD gsd/v0.9.3-toolchain-and-dependency-update-repair>`, measured here and never copied from executor metadata. Before any commit it must equal `git rev-parse HEAD`.

2. **`## Baseline`**, each command quoted.
   - Write `PYVENV_HOME_68_01` and `PYVENV_VERSION_68_01` from `sed -n 's/^home = //p;s/^version_info = //p' .venv/pyvenv.cfg`.
   - Write `COLLECT_BEFORE_68_01` as the number N from the last line of `uv run pytest --collect-only -q -p no:cacheprovider`, which reads "N tests collected".
   - Quote `sed -n '11p;73p;77p;80p' CLAUDE.md`.

3. **`## D-02 evidence at base`**, measured before any edit and transcribed verbatim (D-02):
   - `git log --oneline -S "ln -sf" "$BASE" -- CLAUDE.md`, then `LNSF_COMMITS = <line count>`
   - `git log --oneline -S patchelf "$BASE" -- CLAUDE.md`, then `PATCHELF_COMMITS = <line count>`
   - `git grep -nE 'ln -sf|patchelf' "$BASE" -- ':!.planning'`, then `LNSF_PATCHELF_GREP_HITS_BEFORE = <line count>`

   All three are expected to be 0. If any is non-zero, transcribe the hits, write `## HALT: D-02 premise changed`, and stop: the vacuity claim no longer holds.

   Add one sentence: the guidance never lived in `CLAUDE.md`; it lived only in the maintainer's out-of-repository memory.

4. **D-08 rewrite.** Edit exactly two lines of `CLAUDE.md` in place, and nothing else.
   - **Line 11.** Change only the parenthetical, so the line reads exactly: Development uses `uv` for env/dependency management and `tox` (with `tox-uv`) as the task runner.
   - **Line 77** stays one Markdown bullet on one line. It starts with ``- `tox.ini` pins `tox-uv~=1.35` `` and states, in this order:
     - (a) The pin uses `~=1.35`, not `>=1.35,<2`, and the comment in `tox.ini` explains why. Tox's ini parser splits a single-line `requires` on commas, so `>=1.35,<2` breaks tox's startup, and `~=1.35` is the comma-free equivalent.
     - (b) A short history. The pin was earlier `tox-uv-bare` (QUA-04, Phase 45.2) only because the `uv` bundled with `tox-uv` could not exec on NixOS. The NixOS FHS shims now handle that, which is why plain `tox-uv` is correct again.
   - The old bullet's closing instruction, which told readers not to go back to plain `tox-uv`, is deleted without replacement. Its "`-bare` is deliberate" rationale sentence is replaced by (b), not kept.
   - Do not add a pointer to the new subsection here; Task 2 adds that subsection.
   - Do not touch line 73's heading, the other bullets, or anything from line 80 on.

5. **`## D-08 rewrite`.**
   - Quote `sed -n '11p;77p' CLAUDE.md` after the edit.
   - Quote `git diff -U0 "$BASE" -- CLAUDE.md`, whose only removed lines must be the old line 11 and the old line 77.
   - Quote `awk '/^When operating inside a worktree, provision/{f=1} f' CLAUDE.md | sha256sum` next to the same pipeline run on `git show "$BASE:CLAUDE.md"`. They must be equal.

6. Write the evidence sections in the order named, with keys at column 0. Commit `CLAUDE.md` and the evidence together.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-CLAUDEMD-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_68_01)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; for v in "$(k PYVENV_HOME_68_01)" "$(k PYVENV_VERSION_68_01)" "$(k COLLECT_BEFORE_68_01)" "$(k UV_SHIM_PATH)"; do [ -n "$v" ] || { echo "missing evidence key"; exit 1; }; done &amp;&amp; [ "$(k LNSF_COMMITS)" = 0 ] &amp;&amp; [ "$(k PATCHELF_COMMITS)" = 0 ] &amp;&amp; [ "$(k LNSF_PATCHELF_GREP_HITS_BEFORE)" = 0 ] &amp;&amp; [ "$(git log --oneline -S 'ln -sf' "$B" -- CLAUDE.md | wc -l)" = 0 ] &amp;&amp; [ "$(git log --oneline -S patchelf "$B" -- CLAUDE.md | wc -l)" = 0 ] &amp;&amp; [ "$(sed -n 11p CLAUDE.md)" = 'Development uses `uv` for env/dependency management and `tox` (with `tox-uv`) as the task runner.' ] &amp;&amp; L="$(grep -F -- '- `tox.ini` pins `tox-uv~=1.35`' CLAUDE.md)" &amp;&amp; [ "$(printf '%s\n' "$L" | wc -l)" = 1 ] &amp;&amp; for t in '&gt;=1.35,&lt;2' '~=1.35' 'comma' 'tox-uv-bare' 'NixOS' 'FHS'; do printf '%s\n' "$L" | grep -qF -- "$t" || { echo "bullet lacks: $t"; exit 1; }; done &amp;&amp; ! grep -qF 'package is also deliberate' CLAUDE.md &amp;&amp; ! grep -qF '"simplify"' CLAUDE.md &amp;&amp; [ "$(grep -c '^## Conventions &amp; gotchas$' CLAUDE.md)" = 1 ] &amp;&amp; diff &lt;(git diff -U0 "$B" -- CLAUDE.md | grep '^-' | grep -v '^--- a/' | sed 's/^-//') &lt;(git show "$B:CLAUDE.md" | sed -n '11p;77p') &amp;&amp; [ "$(awk '/^When operating inside a worktree, provision/{f=1} f' CLAUDE.md | sha256sum)" = "$(git show "$B:CLAUDE.md" | awk '/^When operating inside a worktree, provision/{f=1} f' | sha256sum)" ] &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e CLAUDE.md -e "$F" -e "$D/68-01-SUMMARY.md" &amp;&amp; for s in '## Head check and provisioning' '## Baseline' '## D-02 evidence at base' '## D-08 rewrite'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing evidence key`, `bullet lacks: <token>` or `missing section: <name>` printed, or `diff` printing any line. Each of these makes it fail:
- not in a worktree, or the uv shim lacking `typsphinx-fhs-run`
- `BASE_68_01` absent or not an ancestor of HEAD
- any D-02 count not 0, or a live `git log -S` at base returning a commit
- line 11 not the exact target text
- zero or several `tox-uv~=1.35` Conventions bullets, or the bullet missing a required token
- the old `-bare` rationale or the do-not-revert instruction still present
- the Conventions heading count not 1
- a removed line other than the old 11 and 77
- the recipe tail hash differing from base
- a file outside CLAUDE.md and the evidence changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n 11p CLAUDE.md` prints exactly: Development uses `uv` for env/dependency management and `tox` (with `tox-uv`) as the task runner.
    - `grep -F -- '- `tox.ini` pins `tox-uv~=1.35`' CLAUDE.md` prints exactly one line. It contains `>=1.35,<2`, `~=1.35`, `comma`, `tox-uv-bare`, `NixOS` and `FHS`.
    - `grep -cF 'package is also deliberate' CLAUDE.md` prints 0, and `grep -cF '"simplify"' CLAUDE.md` prints 0.
    - `git diff -U0 BASE_68_01 -- CLAUDE.md` removes exactly the old line 11 and the old line 77.
    - `awk '/^When operating inside a worktree, provision/{f=1} f' CLAUDE.md | sha256sum` equals the same pipeline over `git show BASE_68_01:CLAUDE.md`.
    - The evidence records `LNSF_COMMITS = 0`, `PATCHELF_COMMITS = 0` and `LNSF_PATCHELF_GREP_HITS_BEFORE = 0`, each above its verbatim command transcript.
  </acceptance_criteria>
  <done>CLAUDE.md names the landed `tox-uv~=1.35` pin in both places, with `tox-uv-bare` only as history. The D-02 vacuity is evidenced at base, the recipe is provably untouched, and the slice is committed.</done>
</task>

<task type="auto">
  <name>Task 2: New ### NixOS development shell subsection (D-02..D-07) and the D-01 boundary paragraph, with the recipe byte-identical and the collected count unchanged</name>
  <precondition>Task 1 is committed: `68-CLAUDEMD-EVIDENCE.md` holds `BASE_68_01` and `## D-08 rewrite` with no HALT heading, and `grep -q typsphinx-fhs-run "$(command -v uv)"` still succeeds.</precondition>
  <reversibility rating="reversible">Additive prose in a git-tracked file.</reversibility>
  <files>CLAUDE.md, .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLAUDEMD-EVIDENCE.md</files>
  <read_first>
    - CLAUDE.md (as left by Task 1; lines 73-96)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md (D-01..D-07; § Specific Ideas "D-03 check shape")
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md (§ Measured Facts the New Text Must Report: the seven names, the strict-shim message and exit 127, the `uv` two legs, NIX-05 `Found RC allowed 1`, the pyvenv.cfg pair, the NIX-08 REPRODUCES matrix)
    - .planning/ROADMAP.md (§ Phase 68 SC#1 and its `AMENDED 2026-09-12` block; binding constraint 11)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md (PATH inheritance, `Found RC allowed 1`, worktree `.venv` interpreter)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX08-ENV-EVIDENCE.md (§ Four-cell matrix; Finding: REPRODUCES)
    - .planning/research/PITFALLS.md (§ Pitfall 3, line 53)
    - flake.nix (lines 63-101: the strict-shim message, `venvShimNames`, `uvShim`)
  </read_first>
  <action>
1. Re-run the head check (the precondition command, plus `test -f .git` and `pwd -P`), then `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Both are idempotent.

2. **Insert the subsection** (D-04) as new lines between the last Conventions bullet (the one beginning "- CI (") and the `### Worktree-isolated execution` heading. Its heading line is exactly `### NixOS development shell`. Follow the bold-lead-in style of the Worktree section (`**Label.** sentence.`, per 68-PATTERNS.md). Write in your own words, as operational facts only, in this order:
   - **Scope (D-03).** This applies only on the maintainer's NixOS machine. CI and non-NixOS contributors have no shims and need none. The phrase `non-NixOS` and the word `CI` must appear.
   - **What the shims are (D-04).**
     - On that machine, direnv loads `flake.nix`'s devShell in the main checkout, and it puts seven bare commands on PATH: `uv`, `tox`, `ruff`, `black`, `mypy`, `pytest` and `sphinx-build`, each written in backticks. These are the commands in § Commands.
     - Each walks up from the current directory to this checkout's own `.venv/bin/<tool>` and runs it inside an FHS sandbox (`typsphinx-fhs-run`). That is what lets generic-linux binaries that uv installs or downloads execute, together with everything they spawn.
     - A missing `.venv/bin/<tool>` fails loudly: a `typsphinx-shim:` line on stderr names the tool and prints the provisioning line as a hint, then exits 127. The shim never silently runs some other binary.
     - `uv` alone falls back, to nixpkgs' `uv`, so a fresh clone or worktree can run its first `uv sync`. Once `.venv` exists, `uv --version` reports the version `uv.lock` pins.
   - **Prerequisite and check (D-03).**
     - Launch Claude Code from a shell in which direnv has loaded the main checkout's devShell.
     - Before provisioning a worktree, confirm that `command -v uv` resolves to a `/nix/store/…` shim, by running exactly `grep -q typsphinx-fhs-run "$(command -v uv)"`.
     - A worktree never loads direnv itself; its shims come only from the launching session's PATH.
   - **No manual step (D-02).**
     - No manual `ln -sf` or `patchelf` step exists or is needed. These two words appear in this sentence and nowhere else in CLAUDE.md.
     - A `Could not start dynamically linked executable` error means the shim is not on PATH, because the session was not launched from the direnv-loaded checkout. It is not a code regression: relaunch from that shell.
   - **Locale (D-05).**
     - The sandbox passes the host's `LANG` through unchanged, so Sphinx warning text is localised inside it exactly as outside.
     - CI runs in English, so a test that asserts warning text should also be run under `LC_ALL=C` locally.
   - **Interpreters may differ (D-06).**
     - A fresh worktree's `.venv` is built on uv-managed CPython, while the main checkout's may be on nixpkgs' `python3`, and the versions may differ.
     - Compare both `.venv/pyvenv.cfg` `home` and `version_info` before comparing test counts between them.
     - Write no version number: the subsection must contain no digit-dot-digit sequence anywhere, so no interpreter, uv, tool or milestone version appears.
   - **Rationale pointer (D-07).** One sentence: why the development shell is built this way is recorded in the header notes of `flake.nix`. Include the word `header`.
   - **Out of scope for CLAUDE.md (D-07).** Do not explain why the devShell has its particular shape, the two alternative designs that were measured and rejected, or the process-inheritance argument; those belong to `flake.nix` alone.

3. **Insert the boundary paragraph** (D-01) inside `### Worktree-isolated execution`, as a new paragraph between the `**Detection rule:**` paragraph and the sentence "When operating inside a worktree, provision the worktree's own environment before running anything:". Use a bold lead-in, for example a NixOS-boundary label. In your own words it states:
   - The recipe below is unchanged and mandatory on every machine, and `flake.nix` does not substitute for it.
   - On the maintainer's NixOS machine the recipe itself runs through the shims described in the section above, because a worktree's `.venv` is built on a generic-linux interpreter that NixOS runs only inside the sandbox. The worktree therefore depends on the shims being present.
   - The shims reach a worktree only because they are inherited through PATH from a session launched in the direnv-loaded main checkout. direnv never loads in a worktree, and the worktree's own `.envrc` is never allowed there.
   - The words `flake.nix`, `mandatory`, `direnv`, `.envrc`, `NixOS`, `shim` and a form of `inherit` must appear.

   Do not touch the Detection rule line, the recipe sentence, the fenced recipe, list items 1-2, the Rationale paragraph or the Applicability paragraph. Everything from the recipe sentence to end of file must hash identically to `BASE_68_01`.

4. **`## NixOS subsection and boundary paragraph`.** Quote `awk '/^### NixOS development shell$/{f=1} /^### Worktree-isolated execution$/{f=0} f' CLAUDE.md` and `awk '/^### Worktree-isolated execution$/{f=1;next} /^When operating inside a worktree, provision/{f=0} f' CLAUDE.md` verbatim. Write `NIXOS_SUBSECTION_HEADING = ### NixOS development shell`.

5. **`## Gates after the edit`.** Quote each of these:
   - the heading line numbers from `grep -n -E '^## Conventions & gotchas$|^- CI \(|^### NixOS development shell$|^### Worktree-isolated execution$' CLAUDE.md`, which must be strictly increasing in that order
   - the recipe-tail `sha256sum` pair (base and now), which must be equal
   - `git diff -U0 "$BASE_68_01" -- CLAUDE.md | grep '^-' | grep -v '^--- a/'`, which must be only the old lines 11 and 77
   - `grep -nE 'ln -sf|patchelf' CLAUDE.md`, whose every hit lies inside the subsection
   - `COLLECT_AFTER_68_01`, from the same collect-only command as the baseline, which must equal `COLLECT_BEFORE_68_01`

6. Commit `CLAUDE.md` and the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-CLAUDEMD-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_68_01)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; [ "$(grep -c '^### NixOS development shell$' CLAUDE.md)" = 1 ] &amp;&amp; [ "$(grep -c '^### Worktree-isolated execution$' CLAUDE.md)" = 1 ] &amp;&amp; [ "$(grep -c '^## Conventions &amp; gotchas$' CLAUDE.md)" = 1 ] &amp;&amp; nc="$(grep -n '^## Conventions &amp; gotchas$' CLAUDE.md | cut -d: -f1)" &amp;&amp; ci="$(grep -n '^- CI (' CLAUDE.md | cut -d: -f1)" &amp;&amp; nx="$(grep -n '^### NixOS development shell$' CLAUDE.md | cut -d: -f1)" &amp;&amp; wt="$(grep -n '^### Worktree-isolated execution$' CLAUDE.md | cut -d: -f1)" &amp;&amp; [ "$nc" -lt "$ci" ] &amp;&amp; [ "$ci" -lt "$nx" ] &amp;&amp; [ "$nx" -lt "$wt" ] &amp;&amp; S="$(awk '/^### NixOS development shell$/{f=1} /^### Worktree-isolated execution$/{f=0} f' CLAUDE.md)" &amp;&amp; W="$(awk '/^### Worktree-isolated execution$/{f=1;next} /^When operating inside a worktree, provision/{f=0} f' CLAUDE.md)" &amp;&amp; for t in '`uv`' '`tox`' '`ruff`' '`black`' '`mypy`' '`pytest`' '`sphinx-build`' '.venv/bin/' 'FHS' '127' 'nixpkgs' 'uv.lock' 'uv --version' 'direnv' 'grep -q typsphinx-fhs-run "$(command -v uv)"' 'non-NixOS' 'CI' 'ln -sf' 'patchelf' 'Could not start dynamically linked executable' 'regression' 'LC_ALL=C' 'LANG' 'English' 'pyvenv.cfg' 'version_info' 'may differ' 'flake.nix' 'header'; do printf '%s\n' "$S" | grep -qF -- "$t" || { echo "subsection lacks: $t"; exit 1; }; done &amp;&amp; for t in 'flake.nix' 'mandatory' 'direnv' '.envrc' 'inherit' 'NixOS' 'shim'; do printf '%s\n' "$W" | grep -qF -- "$t" || { echo "boundary paragraph lacks: $t"; exit 1; }; done &amp;&amp; ! printf '%s\n' "$S" | grep -q '[0-9]\.[0-9]' &amp;&amp; ! printf '%s\n%s\n' "$S" "$W" | grep -qE 'shellHook|mkShell|namespace' &amp;&amp; [ "$(grep -cE 'ln -sf|patchelf' CLAUDE.md)" = "$(printf '%s\n' "$S" | grep -cE 'ln -sf|patchelf')" ] &amp;&amp; [ "$(grep '^\*\*Detection rule:\*\*' CLAUDE.md)" = "$(git show "$B:CLAUDE.md" | grep '^\*\*Detection rule:\*\*')" ] &amp;&amp; [ "$(awk '/^When operating inside a worktree, provision/{f=1} f' CLAUDE.md | sha256sum)" = "$(git show "$B:CLAUDE.md" | awk '/^When operating inside a worktree, provision/{f=1} f' | sha256sum)" ] &amp;&amp; diff &lt;(git diff -U0 "$B" -- CLAUDE.md | grep '^-' | grep -v '^--- a/' | sed 's/^-//') &lt;(git show "$B:CLAUDE.md" | sed -n '11p;77p') &amp;&amp; [ "$(sed -n 11p CLAUDE.md)" = 'Development uses `uv` for env/dependency management and `tox` (with `tox-uv`) as the task runner.' ] &amp;&amp; [ "$(uv run pytest --collect-only -q -p no:cacheprovider 2&gt;/dev/null | sed -n 's/^\([0-9][0-9]*\) tests\{0,1\} collected.*/\1/p' | tail -n 1)" = "$(k COLLECT_BEFORE_68_01)" ] &amp;&amp; [ "$(k COLLECT_AFTER_68_01)" = "$(k COLLECT_BEFORE_68_01)" ] &amp;&amp; [ "$(k NIXOS_SUBSECTION_HEADING)" = '### NixOS development shell' ] &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e CLAUDE.md -e "$F" -e "$D/68-01-SUMMARY.md" &amp;&amp; for s in '## NixOS subsection and boundary paragraph' '## Gates after the edit'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `subsection lacks: <token>`, `boundary paragraph lacks: <token>` or `missing section: <name>` printed, or `diff` printing a line. Each of these makes it fail:
- the three headings not each present once, or not in the order Conventions heading, CI bullet, NixOS subsection, Worktree section
- a required token missing from the subsection or the boundary paragraph
- any digit-dot-digit sequence in the subsection
- rationale vocabulary in either new region
- an `ln -sf` / `patchelf` line outside the subsection
- the Detection rule line or the recipe tail differing from base
- a removed line other than the old 11 and 77
- the live collected count differing from `COLLECT_BEFORE_68_01`
- a file outside CLAUDE.md and the evidence changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '^### NixOS development shell$' CLAUDE.md` prints one line, numbered between the `- CI (` bullet and `### Worktree-isolated execution`.
    - The subsection region contains all seven backticked command names, `grep -q typsphinx-fhs-run "$(command -v uv)"`, `127`, `nixpkgs`, `uv.lock`, `uv --version`, `non-NixOS`, `CI`, the D-02 sentence with `Could not start dynamically linked executable` and `regression`, `LC_ALL=C`, `LANG`, `English`, `pyvenv.cfg`, `version_info`, `may differ`, and a `flake.nix` header pointer.
    - `awk '/^### NixOS development shell$/{f=1} /^### Worktree-isolated execution$/{f=0} f' CLAUDE.md | grep -c '[0-9]\.[0-9]'` prints 0.
    - The region between `### Worktree-isolated execution` and "When operating inside a worktree, provision" contains `flake.nix`, `mandatory`, `direnv`, `.envrc`, `NixOS`, `shim` and a form of `inherit`.
    - The recipe-tail hash equals base. The only removed lines are the old 11 and 77. `COLLECT_AFTER_68_01` equals `COLLECT_BEFORE_68_01`.
  </acceptance_criteria>
  <done>CLAUDE.md describes the landed NixOS mechanism operationally and states the SC#1 boundary in its own words per the AMENDED reading. It instructs no manual step, the provisioning recipe is byte-identical, and the collected test count is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md → every future executor session | CLAUDE.md is loaded into every session and steers how executors provision; wrong text propagates into every later phase |
| phase evidence → verifier | the verifier reads recorded transcripts; a value not produced by a quoted command would mislead it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-01 | Tampering | CLAUDE.md provisioning recipe | medium | mitigate | A weakened or conditional recipe would make executors skip `uv sync`, reproducing Pitfall 3's misdiagnosed regressions. The recipe tail from "When operating inside a worktree, provision" to end of file must hash identically to `BASE_68_01`, and the only removed lines are the old 11 and 77. Both are checked in each task's verify. |
| T-68-02 | Repudiation | D-02 vacuity claim | low | mitigate | The claim rests on `git log -S` and `git grep` run at `BASE_68_01` and transcribed verbatim, and the verify re-runs both at base. |
| T-68-03 | Information Disclosure | evidence transcripts (worktree paths, store paths) | low | accept | The same paths already appear in committed Phase 64/65 evidence. No secret is read or recorded. |
| T-68-SC | Tampering | `uv sync --extra dev` during provisioning | low | accept | It installs only the pins already in `uv.lock`. No new package and no package-manager install task, so no legitimacy checkpoint applies. |
</threat_model>

<verification>
- Line 11 and the Conventions bullet name the landed `tox-uv~=1.35` pin, with `tox-uv-bare` as history only (D-08).
- The `### NixOS development shell` subsection carries D-02..D-07's operational facts and no version number. The boundary paragraph carries D-01.
- The provisioning recipe and everything after it are byte-identical to base, and `## Conventions & gotchas` is unchanged.
- The collected test count is unchanged in this worktree.
</verification>

<success_criteria>
DOC-19's CLAUDE.md half is delivered. The section describes the landed mechanism, instructs no manual shim step (the retirement is vacuous, and evidenced), and states the worktree boundary as the AMENDED SC#1 reading requires. The provisioning recipe stays mandatory and byte-identical. 68-04 re-reads this against the merged tree.
</success_criteria>

<output>
Create `.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-01-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`: DOC-19 closes in 68-04 on the merged tree.
- The first section states `BASE_68_01`, `NIXOS_SUBSECTION_HEADING`, `LNSF_COMMITS`, `PATCHELF_COMMITS`, `COLLECT_BEFORE_68_01` and `COLLECT_AFTER_68_01`, plus `PYVENV_HOME_68_01` / `PYVENV_VERSION_68_01`.
- If a HALT heading was written, that section says so first.
</output>
