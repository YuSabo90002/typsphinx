---
phase: 66-github-dependabot-yml-pip-uv-ecosystem
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md
autonomous: false
requirements: [DEP-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Wave-1 gate: 66-MAIN-PR-EVIDENCE.md records BASE_SHA, MILESTONE_COMMIT, CONFIG_BLOB, PR_COMMIT, PR_NUMBER and PR_RUN_ID, shows the six required checks as success, and carries no HALT heading; MILESTONE_COMMIT is an ancestor of this worktree HEAD.'
    - 'D-01 pre-merge gate: immediately before the owner is asked and again immediately before the merge, the PR is OPEN with headRefOid PR_COMMIT, origin/main still equals BASE_SHA (strict: true, so the PR is up to date), the six required checks read success, and HEAD:.github/dependabot.yml and PR_COMMIT:.github/dependabot.yml are the same blob CONFIG_BLOB.'
    - 'D-01: REL-12 stays conflict-free. git merge-tree --write-tree PR_COMMIT HEAD exits 0 before the merge, and git merge-tree --write-tree origin/main HEAD exits 0 after it.'
    - 'D-01: the merge happens only after the owner answers merge at the checkpoint:decision; OWNER_DECISION and DECIDED_AT are recorded.'
    - 'D-02: #123 and #128 are each snapshotted immediately before the merge with gh pr view --json number,state,closed,closedAt,comments,headRefName,headRefOid,updatedAt,labels, recorded verbatim under a UTC timestamp that is not later than MERGED_AT.'
    - 'D-01: the PR merges as a two-parent merge commit MERGE_SHA with parents BASE_SHA and PR_COMMIT, the style of #132 to #136, through gh pr merge --merge --match-head-commit PR_COMMIT. Afterwards MERGE_SHA is on origin/main, git diff BASE_SHA MERGE_SHA names only .github/dependabot.yml, and MERGE_SHA:.github/dependabot.yml is CONFIG_BLOB, so main carries package-ecosystem "uv".'
    - 'DEP-03 (encoding edge, continued): the post-merge main copy, the milestone copy and the PR copy are one blob SHA, CONFIG_BLOB.'
    - 'The Dependabot Updates run list is recorded verbatim before the merge and right after it, with MERGED_AT, so 66-03 can select runs created after the merge.'
    - 'Hold path: if the owner answers hold, OWNER_DECISION = hold is recorded under a HALT heading and nothing is merged.'
  artifacts:
    - path: .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md
      provides: 'pre-merge gate, REL-12 merge simulation, owner decision, D-02 pre-merge snapshot, merge record, post-merge main read, Dependabot run lists around the merge'
      contains: 'MERGE_SHA = '
  key_links:
    - from: 'owner answer at the checkpoint:decision'
      to: 'gh pr merge PR_NUMBER --merge --match-head-commit PR_COMMIT'
      via: 'OWNER_DECISION = merge recorded before the merge command runs'
      pattern: 'OWNER_DECISION = '
    - from: 'MERGE_SHA:.github/dependabot.yml on main'
      to: 'GitHub Dependabot service'
      via: 'dependabot reads the configuration from the default branch only (about-the-dependabot-yml-file.md:46)'
      pattern: 'MERGED_AT = '
  prohibitions:
    - statement: 'MUST NOT merge unless the owner answered merge at the checkpoint, and MUST NOT pass --admin, --auto, --squash, --rebase or --delete-branch to gh pr merge (D-01).'
      requirement_id: DEP-01
      category: safety
      status: resolved
      verification: test
    - statement: 'MUST NOT update, rebase, rebuild or force-push the PR branch when origin/main has moved or the head changed; that halts for the owner (D-01, strict branch protection).'
      requirement_id: DEP-01
      category: safety
      status: resolved
      verification: test
    - statement: 'MUST NOT close, merge, comment on, label, or send an @dependabot command to #123 or #128; they are snapshotted, never touched (D-02).'
      requirement_id: DEP-01
      category: values
      status: resolved
      verification: test
    - statement: 'MUST NOT revert the merge or open a revert pull request on the executor own initiative; post-merge dependabot behaviour is recorded and handed to the owner.'
      requirement_id: DEP-01
      category: values
      status: resolved
      verification: judgment
---

<objective>
Get the `uv` configuration onto `main`, the only branch dependabot reads, through the PR 66-01 opened, and only on the owner's go-ahead.

D-01 rates the merge one-way. Once `main` carries `package-ecosystem: "uv"`, dependabot acts on it: it may open `uv`-ecosystem PRs, and it may change #123/#128's state. A revert cannot undo what dependabot has already opened or closed. So the plan runs in three steps:
1. A read-only pre-merge gate, which also simulates REL-12's later merge.
2. A `checkpoint:decision` for the owner.
3. A task that, immediately before merging, takes D-02's snapshot of #123/#128. It then merges with a two-parent merge commit, the style #132 to #136 used, and proves `main` now holds the same blob as the milestone branch.

Output: `main` at `MERGE_SHA` carrying the uv entry, and `66-MAIN-PR-EVIDENCE.md` extended with the gate, the decision, the snapshot, the merge and the post-merge read.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-CONTEXT.md
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md
@.github/dependabot.yml
@CLAUDE.md
</context>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
`## HALT` is a heading the executor must be able to write on a failure path; the gates require its absence.

## Planning-time census (2026-09-12: re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| #123 | OPEN, `dependabot/pip/ruff-gte-0.15-and-lt-0.17`, head `1c905bb8`, `labels: []`, updated 2026-08-03T20:09:21Z | `gh pr view 123` |
| #128 | OPEN, `dependabot/pip/sphinx-typst-stack-12b5b89b5a`, head `000859f7`, `labels: []`, updated 2026-09-07T00:07:47Z, title "…in the sphinx-typst-stack group across 1 directory" | `gh pr view 128` |
| Recent merges into `main` | #132 to #136, each a two-parent merge commit ("Merge pull request #136 from …") | `gh pr list --state merged --base main`, `git rev-list --parents` |
| `delete_branch_on_merge` | `false` | `gh api repos/YuSabo90002/typsphinx` |
| Dependabot Updates runs | listable through `gh run list --workflow "Dependabot Updates"` (`dynamic/dependabot/dependabot-updates`); the latest were `pip in /. - Update #1559879825` and `pip in / for docutils - Update #1559879891`, both 2026-09-07T00:07Z, headSha `6181768f` | `gh run list --workflow 197391836` |
| `git merge-tree --write-tree` | supported (git 2.54.0) | `git --version` |

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `main` advanced to `MERGE_SHA` (merge of `PR_NUMBER`) | two-parent merge commit | GitHub |
| `66-MAIN-PR-EVIDENCE.md` sections `## Wave-1 gate`, `## Pre-merge gate`, `## REL-12 merge simulation`, `## Dependabot runs before the merge`, `## Owner decision`, `## D-02 pre-merge snapshot`, `## Merge`, `## Post-merge main`, `## Dependabot runs right after the merge`; keys `OWNER_DECISION`, `DECIDED_AT`, `MERGE_SHA`, `MERGED_AT` | evidence | phase directory |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: wave-1 gate and read-only pre-merge gate across both branches, with a REL-12 merge simulation and the pre-merge Dependabot run list</name>
  <precondition>`66-MAIN-PR-EVIDENCE.md` exists in this worktree with `BASE_SHA`, `MILESTONE_COMMIT`, `CONFIG_BLOB`, `PR_COMMIT`, `PR_NUMBER` and `PR_RUN_ID` lines, `## Required checks` and `## Handoff to 66-02` sections, and no HALT heading; `gh auth status` succeeds. Otherwise halt at a blocking-human gate: there is no green PR to merge.</precondition>
  <reversibility rating="reversible">Read-only: it records state and changes no ref.</reversibility>
  <files>.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md (all keys; `## Required checks`; `## Non-required job findings` if present)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-CONTEXT.md (D-01 and its reversibility note; D-02)
    - .github/dependabot.yml (the milestone copy)
    - CLAUDE.md, section "Worktree-isolated execution"
  </read_first>
  <action>
1. **Head check and provisioning** (CLAUDE.md, mandatory).
   - Record `test -f .git; echo "exit:$?"` and `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`.
   - Run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

2. **Wave-1 gate.**
   - Read each key with `sed -n 's/^KEY = //p' .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md | head -n 1`.
   - Record `git merge-base --is-ancestor "$MILESTONE_COMMIT" HEAD; echo "exit:$?"` (`exit:0`).
   - Record `grep -c '^## HALT' <that file>`, which must be 0.

3. **Pre-merge gate (D-01).**
   - Run `git fetch origin main chore/dependabot-uv-ecosystem`.
   - Record `git rev-parse origin/main`, which must equal `BASE_SHA`. If main moved, write `## HALT: main moved`, record both SHAs, and stop at a blocking-human checkpoint. Do not update, rebase or rebuild the PR branch.
   - Record `gh pr view "$PR_NUMBER" --json state,headRefOid,mergeable,mergeStateStatus`: state OPEN, and headRefOid equal to `PR_COMMIT`.
   - Record, for each of the six required contexts by exact name, its conclusion from `gh pr view "$PR_NUMBER" --json statusCheckRollup --jq '.statusCheckRollup[] | [.name, .conclusion] | @tsv'`. All six must read `SUCCESS`.
   - Record `git rev-parse HEAD:.github/dependabot.yml` and `git rev-parse "$PR_COMMIT:.github/dependabot.yml"`; both must equal `CONFIG_BLOB` (the DEP-03 encoding edge: blob identity, never a YAML comparison).
   - Record `git diff --stat "$PR_COMMIT" HEAD -- .github/dependabot.yml`, which must be empty.

4. **REL-12 merge simulation.** Record `git merge-tree --write-tree --name-only "$PR_COMMIT" HEAD; echo "exit:$?"`. It must print `exit:0`, meaning no conflict. With strict branch protection, the merge commit's tree equals `PR_COMMIT`'s tree, so this is the milestone's later merge into `main` as it will be.

5. **Dependabot Updates run list before the merge.** Record `date -u +%FT%TZ` and `gh run list --workflow "Dependabot Updates" --limit 10 --json databaseId,displayTitle,headSha,status,conclusion,createdAt` verbatim. Also record `gh pr list --state open --author app/dependabot --json number,title,headRefName,labels,createdAt,updatedAt` verbatim.

6. Restate 66-01's `## Non-required job findings`, if any, so the checkpoint can present them.

7. Append `## Wave-1 gate`, `## Pre-merge gate`, `## REL-12 merge simulation` and `## Dependabot runs before the merge`. Commit, then stop at Task 2.
  </action>
  <verify>
    <automated>F=.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_SHA)" &amp;&amp; BL="$(k CONFIG_BLOB)" &amp;&amp; MC="$(k MILESTONE_COMMIT)" &amp;&amp; PC="$(k PR_COMMIT)" &amp;&amp; N="$(k PR_NUMBER)" &amp;&amp; for v in "$B" "$BL" "$MC" "$PC" "$N"; do [ -n "$v" ] || { echo "missing evidence key"; exit 1; }; done &amp;&amp; git merge-base --is-ancestor "$MC" HEAD &amp;&amp; [ "$(git rev-parse HEAD:.github/dependabot.yml)" = "$BL" ] &amp;&amp; [ "$(git rev-parse "$PC:.github/dependabot.yml")" = "$BL" ] &amp;&amp; git merge-tree --write-tree --name-only "$PC" HEAD &gt;/dev/null &amp;&amp; S="$(awk '/^## Pre-merge gate/{f=1;next} /^## /{f=0} f' "$F")" &amp;&amp; printf '%s\n' "$S" | grep -qF "$B" &amp;&amp; printf '%s\n' "$S" | grep -qF "$PC" &amp;&amp; for c in 'Test Python 3.12 on ubuntu-latest' 'Test Python 3.13 on ubuntu-latest' 'Lint and Format Check' 'Type Check' 'Code Coverage' 'Build Package'; do printf '%s\n' "$S" | grep -qF "$c" || { echo "gate section missing: $c"; exit 1; }; done &amp;&amp; awk '/^## REL-12 merge simulation/{f=1;next} /^## /{f=0} f' "$F" | grep -q 'exit:0' &amp;&amp; grep -q '^## Dependabot runs before the merge' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing evidence key` or `gate section missing: <name>` printed. Each of these makes it fail:
- a missing key
- `MILESTONE_COMMIT` not an ancestor of HEAD
- the HEAD or PR blob differing from `CONFIG_BLOB`
- `git merge-tree` reporting a conflict (exit 1)
- the pre-merge gate section not naming `BASE_SHA`, `PR_COMMIT` and all six required checks
- the REL-12 simulation section lacking `exit:0`
- the pre-merge Dependabot run section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Pre-merge gate` shows origin/main equal to `BASE_SHA`, the PR OPEN at `PR_COMMIT`, all six required contexts `SUCCESS`, and both blobs equal to `CONFIG_BLOB`.
    - `## REL-12 merge simulation` records `git merge-tree --write-tree --name-only PR_COMMIT HEAD` with `exit:0`.
    - `## Dependabot runs before the merge` quotes the run list and the open dependabot PR list verbatim, under a UTC timestamp.
    - No ref was changed by this task.
  </acceptance_criteria>
  <done>Everything the owner needs to decide is measured and recorded, and nothing has been merged.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Owner go-ahead to merge the main-bound PR, a one-way door (D-01)</name>
  <read_first>
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md (`PR_URL`, `## Pre-merge gate`, `## REL-12 merge simulation`, any `## Non-required job findings`)
  </read_first>
  <decision>Merge PR `PR_NUMBER` (`chore/dependabot-uv-ecosystem` into `main`) now? Per D-01 this is a one-way door.</decision>
  <context>
Present `PR_URL`, the six required checks (all `SUCCESS`), any non-required findings, the blob identity (`CONFIG_BLOB` on the milestone branch and the PR head), the REL-12 simulation's `exit:0`, and that `origin/main` is still `BASE_SHA`.

Once `main` carries `package-ecosystem: "uv"`, dependabot acts on it.
- It is expected to open `uv`-ecosystem PRs, since ruff 0.16 and docutils 0.23 lie outside the current ranges.
- It may change #123/#128's state when the `pip` entry leaves `main`. GitHub's docs are silent on this, so it is unmeasured (D-02).
- A revert PR cannot undo PRs dependabot has already opened or closed.

Nothing else about the milestone changes: REL-12 still merges the milestone later, and nothing is tagged or released.
  </context>
  <options>
    <option id="merge">
      <name>Merge now</name>
      <pros>SC#1, the D-05 leg 2 observation, the DEP-03 behaviour read, and all of Phase 67 become reachable inside this milestone (D-01, ROADMAP constraint 2 AMENDED 2026-09-12).</pros>
      <cons>Irreversible in effect. Dependabot may open up to 5 uv PRs and may close #123/#128 mechanically.</cons>
    </option>
    <option id="hold">
      <name>Hold</name>
      <pros>Nothing reaches main, and #123/#128 stay exactly as they are.</pros>
      <cons>No uv-ecosystem PR can open, so SC#1, DEP-04 leg 2 and DEP-03's observation stay unreachable, and Phase 66 cannot close.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The owner's answer is exactly `merge` or `hold`, and Task 3 records it with its UTC time before doing anything else.
  </acceptance_criteria>
  <resume-signal>Reply "merge" or "hold".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: On "merge": re-assert the gate, snapshot #123/#128 (D-02), merge with a two-parent merge commit, and prove main carries the uv blob</name>
  <precondition>The owner answered Task 2. `66-MAIN-PR-EVIDENCE.md` holds Task 1's `## Pre-merge gate` with no HALT heading.</precondition>
  <reversibility rating="one-way">Once main carries the uv entry, dependabot opens PRs and may close #123/#128, and a revert PR cannot undo what it has already opened or closed (D-01).</reversibility>
  <files>.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md (keys; `## Pre-merge gate`)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-CONTEXT.md (D-01; D-02's snapshot field list)
  </read_first>
  <action>
1. **Record the decision.** Write `OWNER_DECISION = <merge|hold>` and `DECIDED_AT = <date -u +%FT%TZ>` under `## Owner decision`, quoting the owner's reply.
   - On `hold`: write `## HALT: owner hold`, commit, and return. Nothing is merged.

2. **Re-assert the gate** (the owner may have taken time).
   - Run `git fetch origin main`. `git rev-parse origin/main` must still equal `BASE_SHA`.
   - `gh pr view "$PR_NUMBER" --json state,headRefOid` must be OPEN at `PR_COMMIT`.
   - The six required contexts must still read `SUCCESS`.
   - On any difference: record it, write `## HALT: state changed after decision`, and stop at a blocking-human checkpoint. No merge, and no branch update.

3. **D-02 pre-merge snapshot, immediately before the merge.** For `n` in 123 and 128, record `date -u +%FT%TZ`, then `gh pr view "$n" --json number,state,closed,closedAt,comments,headRefName,headRefOid,updatedAt,labels`. Record the output verbatim in a fenced block under `## D-02 pre-merge snapshot`. Only read; never comment, close, label or `@dependabot`-command either PR.

4. **Merge (D-01).**
   - Record `MERGE_STARTED_AT = <date -u +%FT%TZ>`.
   - Run `gh pr merge "$PR_NUMBER" --merge --match-head-commit "$PR_COMMIT"`, with exactly those flags. This produces a two-parent merge commit, as #132 to #136 were merged. No `--admin`, `--auto`, `--squash`, `--rebase` or `--delete-branch`; the branch stays on origin.
   - Record the output verbatim.
   - If gh refuses (not mergeable, or the head moved), record the refusal and stop at a blocking-human checkpoint. Never retry with `--admin`.

5. **Post-merge main.** Record each of these:
   - `MERGE_SHA = <gh pr view "$PR_NUMBER" --json mergeCommit -q .mergeCommit.oid>`
   - `MERGED_AT = <gh pr view "$PR_NUMBER" --json mergedAt -q .mergedAt>`
   - after `git fetch origin main`: `git rev-list --parents -n 1 "$MERGE_SHA"`, which must print `MERGE_SHA BASE_SHA PR_COMMIT`
   - `git rev-parse origin/main`, which is `MERGE_SHA` unless something landed on top (record that, if so)
   - `git diff --name-only "$BASE_SHA" "$MERGE_SHA"`, which must be exactly `.github/dependabot.yml`
   - `git rev-parse "$MERGE_SHA:.github/dependabot.yml"`, which must be `CONFIG_BLOB`
   - `git show "$MERGE_SHA:.github/dependabot.yml" | sed -n 4p`, which must be the uv line
   - `git diff "$MERGE_SHA" HEAD -- .github/dependabot.yml`, which must be empty
   - `git merge-tree --write-tree --name-only origin/main HEAD; echo "exit:$?"`, which must print `exit:0`: REL-12 is still conflict-free

6. **The push CI run on main.** Record the `gh run list --workflow=ci.yml --branch main --event push --limit 5 --json databaseId,headSha,status,conclusion,createdAt` row whose headSha is `MERGE_SHA`, with its status as it stands. It is observational, not a gate; on registration lag, repeat the query.

7. **Dependabot runs right after the merge.** Record `date -u +%FT%TZ` and the same `gh run list --workflow "Dependabot Updates" --limit 10 --json databaseId,displayTitle,headSha,status,conclusion,createdAt` verbatim, noting which runs, if any, were created after `MERGED_AT`. Do not wait here: 66-03 polls.

8. Append `## Merge`, `## Post-merge main` and `## Dependabot runs right after the merge`. Commit.
  </action>
  <verify>
    <automated>F=.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_SHA)" &amp;&amp; BL="$(k CONFIG_BLOB)" &amp;&amp; PC="$(k PR_COMMIT)" &amp;&amp; N="$(k PR_NUMBER)" &amp;&amp; M="$(k MERGE_SHA)" &amp;&amp; MA="$(k MERGED_AT)" &amp;&amp; [ "$(k OWNER_DECISION)" = merge ] &amp;&amp; [ -n "$(k DECIDED_AT)" ] &amp;&amp; for v in "$B" "$BL" "$PC" "$N" "$M" "$MA"; do [ -n "$v" ] || { echo "missing evidence key"; exit 1; }; done &amp;&amp; git fetch -q origin main &amp;&amp; [ "$(gh pr view "$N" --json state,mergeCommit -q '[.state,.mergeCommit.oid] | join(" ")')" = "MERGED $M" ] &amp;&amp; [ "$(git rev-list --parents -n 1 "$M")" = "$M $B $PC" ] &amp;&amp; git merge-base --is-ancestor "$M" origin/main &amp;&amp; [ "$(git diff --name-only "$B" "$M")" = .github/dependabot.yml ] &amp;&amp; [ "$(git rev-parse "$M:.github/dependabot.yml")" = "$BL" ] &amp;&amp; [ "$(git rev-parse origin/main:.github/dependabot.yml)" = "$BL" ] &amp;&amp; git diff --quiet "$M" HEAD -- .github/dependabot.yml &amp;&amp; git merge-tree --write-tree --name-only origin/main HEAD &gt;/dev/null &amp;&amp; P="$(awk '/^## D-02 pre-merge snapshot/{f=1;next} /^## /{f=0} f' "$F")" &amp;&amp; printf '%s\n' "$P" | grep -Eq '"number": ?123' &amp;&amp; printf '%s\n' "$P" | grep -Eq '"number": ?128' &amp;&amp; SN="$(printf '%s\n' "$P" | grep -oE '[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' | sort | tail -n 1)" &amp;&amp; [ -n "$SN" ] &amp;&amp; [[ ! "$SN" &gt; "$MA" ]] &amp;&amp; grep -q '^## Post-merge main' "$F" &amp;&amp; grep -q '^## Dependabot runs right after the merge' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing evidence key` printed. Each of these makes it fail:
- `OWNER_DECISION` not `merge`, or any key missing
- the PR not MERGED at `MERGE_SHA`
- `MERGE_SHA` not having exactly the parents `BASE_SHA` and `PR_COMMIT`, or not being on origin/main
- the merge changing any file besides `.github/dependabot.yml`
- the merged or current main blob differing from `CONFIG_BLOB`
- the milestone copy differing from the merged copy
- `git merge-tree` of origin/main and HEAD conflicting
- the pre-merge snapshot lacking #123 or #128, or its latest timestamp being later than `MERGED_AT`
- the post-merge sections missing
- a HALT heading, which includes the hold path</fails_when>
  </verify>
  <acceptance_criteria>
    - `OWNER_DECISION = merge` and `DECIDED_AT` are recorded before `MERGE_STARTED_AT`.
    - `## D-02 pre-merge snapshot` holds both PRs' JSON verbatim, with the eight fields named in D-02 plus `number`, under timestamps not later than `MERGED_AT`.
    - The merge command is `gh pr merge <n> --merge --match-head-commit <PR_COMMIT>`, quoted with its output.
    - `MERGE_SHA` has the parents `BASE_SHA` and `PR_COMMIT` and lies on origin/main. Its diff from `BASE_SHA` is `.github/dependabot.yml` only, with the blob `CONFIG_BLOB`, and line 4 is the uv line.
    - `git merge-tree --write-tree origin/main HEAD` exits 0.
    - The Dependabot run list right after the merge is recorded with a timestamp.
  </acceptance_criteria>
  <done>main carries the uv entry as the same blob as the milestone branch, merged on the owner's go-ahead as a two-parent merge commit. #123 and #128 were snapshotted immediately before and left untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → `main` | the only irreversible action of the phase; branch protection and the owner decision both gate it |
| `main` → Dependabot service | the merged file is read by an external service that then opens and may close PRs |
| evidence ← PR JSON | snapshot JSON of #123/#128 enters the repository verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-06 | Elevation of Privilege | merging without the owner, or bypassing protection with `--admin` | high | mitigate | A `checkpoint:decision` with `gate="blocking-human"` precedes the merge. `OWNER_DECISION = merge` is required before the command runs, and the exact flag set is prescribed. The verify asserts two parents, `BASE_SHA` and `PR_COMMIT`, so a squash or rebase shows. |
| T-66-07 | Tampering | merging a head that changed after CI ran | high | mitigate | `--match-head-commit PR_COMMIT` makes GitHub refuse a moved head. The gate is re-asserted after the decision, and a moved `main` halts instead of updating the branch. |
| T-66-08 | Repudiation | a later state change on #123/#128 attributed to the wrong actor | medium | mitigate | D-02's snapshot is taken immediately before the merge, with UTC timestamps not later than `MERGED_AT`. 66-03 takes the post-merge snapshot and reads the close-event actor. |
| T-66-09 | Denial of Service | dependabot opening a burst of PRs after the merge | low | accept | Bounded by `open-pull-requests-limit: 5`. No auto-merge is configured anywhere, and nothing is merged by this phase. |
| T-66-SC | Tampering | `uv sync --extra dev` during provisioning | low | accept | installs only the pins already in `uv.lock`; no new package |
</threat_model>

<verification>
- The pre-merge gate, the REL-12 simulation and the owner's `merge` answer are recorded before the merge.
- The #123/#128 snapshot precedes the merge.
- `main` is at a two-parent merge commit whose only change is `.github/dependabot.yml`, and whose blob equals the milestone branch's.
</verification>

<success_criteria>
D-01 is complete: the configuration is on `main` through a separate PR changing only `.github/dependabot.yml`, merged after all six required checks were green and after the owner's go-ahead. The milestone branch holds the byte-identical blob, and REL-12's merge is proven conflict-free by `git merge-tree`. D-02's pre-merge half is recorded.
</success_criteria>

<output>
Create `.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-02-SUMMARY.md` when done.
- Its frontmatter `requirements-completed:` is `[]`: DEP-01 closes in 66-04 from dependabot's own output.
- Its first section states `OWNER_DECISION`, `MERGE_SHA`, `MERGED_AT`, and whether any Dependabot run was already created after the merge.
</output>
