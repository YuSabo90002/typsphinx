---
phase: 32-github-pages-teardown-irreversible
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md
autonomous: true
requirements: [CI-04]

must_haves:
  truths:
    - "Freshly re-taken in this phase (never cited from Phase 29/30/30.1/31): https://typsphinx.readthedocs.io/en/latest/ returns HTTP 200 over real HTTP"
    - "Freshly re-taken in this phase: the bare documentation root https://typsphinx.readthedocs.io/ returns HTTP 200 and its effective URL resolves to a versioned page (RTD-04 root resolution still holds)"
    - "Freshly re-taken in this phase: https://typsphinx.readthedocs.io/ja/latest/user_guide/builders.html returns HTTP 200 AND its body contains the Japanese string ビルダー AND its CJK character count is at least 200 AND at least 10x the CJK count of the same docname under /en/latest/ — content-level proof, not an HTTP-status or lang-attribute proxy (D-01)"
    - "Freshly re-taken in this phase: https://typsphinx.readthedocs.io/_/downloads/en/latest/pdf/ returns HTTP 200, its first four bytes are the PDF magic bytes, and its body is at least 500000 bytes (D-02)"
    - "Freshly re-taken in this phase: https://typsphinx.readthedocs.io/_/downloads/ja/latest/pdf/ returns HTTP 200, its first four bytes are the PDF magic bytes, and its body is at least 500000 bytes (D-02)"
    - "A single GATE VERDICT block in 32-EVIDENCE.md states GREEN or RED, derived only from the five checks above, and every check is recorded as a verbatim command plus verbatim output (Phase 29 D-15 format)"
    - "A pre-teardown baseline block records: the origin/gh-pages SHA from git ls-remote, the current HTTP status of https://YuSabo90002.github.io/typsphinx/, and PR #124's number/state/headRefOid — so Plan 03's after-state has a recorded before-state to be measured against"
  artifacts:
    - .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md
  key_links:
    - "GATE VERDICT (this plan) -> Plan 02 and Plan 03 entry: a RED verdict structurally blocks both teardown plans and escalates to the owner (D-03). This is the milestone's only no-undo action and this verdict is its sole standing gate."
    - "ja CJK-count ratio (this plan) -> I18N-01's surviving failure mode: the ja catalogs are 24.3% translated, so a healthy-looking 200 on a 0%-translated docname would read all-English. The probe targets user_guide/builders (65/65 translated) for exactly this reason."
  prohibitions:
    - statement: "MUST NOT cite, quote, or reuse evidence gathered in Phase 29, 30, 30.1 or 31 in place of a freshly-taken observation — SC#1 requires evidence gathered in this phase, and milestone invariant #4 requires a fresh fetch at execution time"
      status: active
    - statement: "MUST NOT proceed into Plan 02 or Plan 03, or write any teardown-enabling verdict, if any of the five gate checks fails — a red gate escalates to the owner and the teardown does not begin (D-03)"
      status: active
    - statement: "MUST NOT record a paraphrase, a summary, or a 'checked, looks good' in place of the verbatim command and its verbatim output"
      status: active
---

<objective>
Take the pre-teardown evidence gate that stands in front of this milestone's only irreversible
action. Freshly fetch — in this phase, over real HTTP — proof that Read the Docs is *currently*
serving English HTML, Japanese HTML (content-verified, not status-verified), the English PDF, the
Japanese PDF, and that the bare documentation root resolves. Record every observation verbatim and
close with a single GREEN/RED verdict.

Per D-03 this plan makes **zero** repository source changes. It only writes the phase's evidence
artifact. Plans 02 and 03 are structurally gated on this plan's verdict being GREEN.

Purpose: recovery cost if the teardown runs while RTD is not actually serving is total — the served
site and its caches are gone the moment Pages is off. This gate is why CI-04 was kept as its own
phase rather than folded into a neighbour (roadmap decision 2026-07-25).
Output: `.planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md` containing the five
gate checks, the pre-teardown baseline snapshot, and the GATE VERDICT block.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-github-pages-teardown-irreversible/32-CONTEXT.md
@.planning/phases/32-github-pages-teardown-irreversible/32-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols and paths newly created by Phase 32 (none of these exist before this phase; do not flag them
as drift against the current tree):

| Artifact | Kind | Created by |
|----------|------|-----------|
| `.planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md` | new file path | Plan 01 (this plan), appended by Plans 02 and 03 |
| `tests/test_readthedocs_config.py::test_docs_workflow_has_no_github_pages_deploy` | new test function | Plan 02 |
| `tests/test_readthedocs_config.py::test_docs_workflow_still_uploads_pdf_to_release` | new test function | Plan 02 |

No new decorators, classes, CLI flags, dataclass fields, or modules. Milestone invariant #3 holds:
nothing under `typsphinx/` is created or modified by this phase.

## Explicit Flagged Assumptions

The deterministic edge-coverage probe returned CI-04 as `unclassified` / `unresolved` — it produced
neither an explicit acceptance predicate nor a backstop. Surfaced here rather than auto-backstopped
or dropped:

- **A-01 (flagged, unresolved):** CI-04's edge behaviour was not classified by the probe. This plan
  set therefore derives CI-04's predicates directly from ROADMAP.md's three success criteria and
  CONTEXT.md's D-01…D-09, and encodes them as `must_haves.truths` across Plans 01–03. If a verifier
  finds a CI-04 behaviour not covered by any truth in this plan set, that is a genuine gap, not a
  probe artifact — escalate rather than assume coverage.
- **A-02 (flagged, carried from RESEARCH.md Assumptions Log A1):** "deleting the `gh-pages` branch
  does not by itself disable the GitHub Pages *feature*" is a pre-existing project assertion
  (ROADMAP.md, REQUIREMENTS.md manual step #7), not independently re-verified against GitHub's
  current product documentation. If it turns out branch deletion alone already 404s the site, the
  owner-manual step in Plan 03 becomes a no-op and its 404 check passes early — no harm either way.
- **A-03 (flagged, carried from RESEARCH.md Assumptions Log A2):** 404 propagation after Pages is
  disabled may lag behind the Settings action because Pages is CDN-fronted. Plan 03's retry posture
  exists for this; if propagation is instant the retries are simply unused.

<assumption_delta_decision>
The `assumption-delta` detector fired `pluralization` on the term **fallback**, from ROADMAP.md's
phrase "the PDF-or-documented-fallback".

- **Noun now primary:** the RTD-served PDF itself.
- **Decision:** `no-change`.
- **Rationale:** the "-or-documented-fallback" disjunction was resolved in Phase 29 — Branch A was
  confirmed (RTD reaches `packages.typst.org` and serves the `typstpdf`-produced PDF directly), so
  RTD-03's `releases/latest/download/` fallback path was never taken and there is no second
  representation to promote or add alongside. This phase's D-02 gate checks the RTD download URLs
  directly. Advisory only; nothing blocks.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Record the HTML half of the gate — en, ja (content-verified), and the documentation root</name>
  <files>.planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/32-github-pages-teardown-irreversible/32-CONTEXT.md` — D-01 (ja HTML goes all the way to content matching), D-04 (freshness), and the `<specifics>` block naming `user_guide/builders` (65/65 translated) and `examples/basic` (30/30) as the only safe probe targets
    - `.planning/phases/32-github-pages-teardown-irreversible/32-RESEARCH.md` — "Pattern 2: Verbatim command + output evidence log (Phase 29 D-15 format)", the exact recording shape this task must produce
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` — the established evidence-file register: a `## ` section per check, a fenced block holding `$ <command>` followed by its literal stdout, then a short verdict line
  </read_first>
  <action>
    Create `.planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md` with a title,
    a `**Gathered:**` date line, and a one-paragraph preamble stating that every observation in the
    file was taken during Phase 32 execution and that no observation is carried over from an earlier
    phase (SC#1, milestone invariant #4).

    Then add a `## Gate check 1 — RTD HTML liveness and Japanese content (D-01)` section holding the
    verbatim command and verbatim output of each of the following, run in this order. Record the
    real output; never a paraphrase.

    (a) English HTML liveness. Fetch `https://typsphinx.readthedocs.io/en/latest/` with `curl -sS -L`
    writing only `%{http_code}` and `%{url_effective}` to stdout. Required: `200`.

    (b) Documentation root resolution. Fetch the bare root `https://typsphinx.readthedocs.io/` the
    same way. Required: `200`, and the effective URL must be a versioned path — record it verbatim.
    This is the per-phase RTD-04 re-fetch the roadmap assigns to every phase after 29.

    (c) Japanese HTML content proof. Fetch
    `https://typsphinx.readthedocs.io/ja/latest/user_guide/builders.html` with `curl -sS -L`,
    recording the HTTP status separately, then pipe the same fetched body through a Python 3 stdlib
    one-liner that counts CJK characters with the regex character class covering Hiragana, Katakana
    and CJK Unified Ideographs, and prints the count. Record the count. Separately `grep -o` the
    body for the literal `ビルダー` and record the first match.

    (d) English same-docname control. Fetch
    `https://typsphinx.readthedocs.io/en/latest/user_guide/builders.html` and run the identical CJK
    counter over it. Record that count.

    Then write a short verdict line computing the ratio: check 1 passes only if status (a) is 200,
    status (b) is 200, status (c) is 200, the `ビルダー` match is present, the ja CJK count is at
    least 200, and the ja CJK count is at least 10 times the en CJK count. State the four numbers in
    the verdict line.

    Why the ratio and not a `lang="ja"` attribute check: per D-01 and I18N-01, the ja catalogs are
    only 24.3% translated and the surviving failure mode is a site that builds green, returns 200,
    and carries a correct `lang` attribute while rendering 100% English. The English documentation
    legitimately contains a handful of CJK strings (that is why `.readthedocs.yaml` installs
    `fonts-noto-cjk`), so an absolute-zero expectation on the English side would be wrong — the
    ratio is what falsifies the failure mode.

    Do not modify any file other than `32-EVIDENCE.md`. Do not run any git command that changes refs.
  </action>
  <acceptance_criteria>
    - `.planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md` exists and contains the heading `## Gate check 1 — RTD HTML liveness and Japanese content (D-01)`
    - The file contains four distinct fenced blocks, each opening with a line starting `$ curl` and followed by that command's literal output
    - The file contains the literal string `ビルダー` inside the recorded output of check (c)
    - The recorded ja CJK count is >= 200 and >= 10x the recorded en CJK count, and both numbers appear verbatim in the verdict line
    - The recorded HTTP status for `https://typsphinx.readthedocs.io/en/latest/`, `https://typsphinx.readthedocs.io/` and `https://typsphinx.readthedocs.io/ja/latest/user_guide/builders.html` is `200` in all three cases
    - `git status --porcelain` lists no modified path outside `.planning/phases/32-github-pages-teardown-irreversible/`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md && grep -q 'Gate check 1' .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md && grep -q 'ビルダー' .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md && test "$(git status --porcelain | grep -vc '^.. \.planning/phases/32-github-pages-teardown-irreversible/')" = "0"</automated>
  </verify>
  <done>32-EVIDENCE.md records, verbatim, that RTD currently serves English HTML, the documentation root, and a Japanese page whose CJK density proves it is genuinely translated rather than English-under-a-ja-URL. No repository source file was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Record the PDF half of the gate — English and Japanese downloads are still being served</name>
  <files>.planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/32-github-pages-teardown-irreversible/32-CONTEXT.md` — D-02: both PDFs are checked for *liveness only*; glyph and content fidelity are explicitly NOT re-verified here
    - `.planning/phases/32-github-pages-teardown-irreversible/32-RESEARCH.md` — "Don't Hand-Roll" table: use the four PDF magic bytes, not a PDF parser, for the liveness signature
    - `.planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md` — the file created by Task 1, to append to rather than overwrite
  </read_first>
  <action>
    Append a `## Gate check 2 — RTD PDF downloads still served (D-02)` section to
    `32-EVIDENCE.md`, again as verbatim command plus verbatim output.

    For each of these two URLs — English `https://typsphinx.readthedocs.io/_/downloads/en/latest/pdf/`
    and Japanese `https://typsphinx.readthedocs.io/_/downloads/ja/latest/pdf/` — download to a
    scratch path with `curl -sS -L` and record: the HTTP status code, the effective URL, the
    `content_type`, and `size_download`, using curl's `-w` formatting. Then record the first four
    bytes of each downloaded file via `head -c4` piped through a hex or literal dump, and record the
    on-disk byte size via `wc -c`.

    Record a page count for each PDF on a best-effort basis using the idiom this project already
    established in Phase 30.1 (`30.1-EVIDENCE.md` line ~3262): `uv run python -c` importing `pypdf`
    and printing `len(PdfReader(path).pages)`. If `pypdf` is not importable in this environment,
    record the literal line `page count: not measured (pypdf unavailable)` and continue — page count
    is corroborating, not load-bearing. The load-bearing bar is status, magic bytes and size.

    Then write a verdict line. Check 2 passes only if, for **both** PDFs: HTTP status is 200, the
    first four bytes are the PDF magic bytes, and the downloaded size is at least 500000 bytes.
    State both sizes and both page counts (or the not-measured marker) in the verdict line.

    Thresholds are Claude's discretion under D-02 and are set here as: >= 500000 bytes, and — when
    measurable — >= 40 pages. Both are deliberately far below the sizes these artifacts have
    historically had (Phase 30.1 measured the English PDF at 1710244 bytes and the Japanese at
    1929044 bytes / 94-95 pages), so the threshold catches an error page or a truncated response
    without becoming a brittle exact-match assertion against a `latest` artifact that tracks `main`.

    Do NOT re-verify glyph rendering, text content, or SHA equality against any local build. D-02 is
    explicit that fidelity was settled by Phase 29 (English) and Phase 30.1 (Japanese) and that
    `latest` tracks `main` so no stable comparison baseline exists. The only question this check
    asks is "is it still being served".

    Delete the scratch downloads when done; do not commit PDF binaries.
  </action>
  <acceptance_criteria>
    - `32-EVIDENCE.md` contains the heading `## Gate check 2 — RTD PDF downloads still served (D-02)`
    - Both `https://typsphinx.readthedocs.io/_/downloads/en/latest/pdf/` and `https://typsphinx.readthedocs.io/_/downloads/ja/latest/pdf/` appear verbatim in recorded `$ curl` command lines
    - The recorded HTTP status is `200` for both URLs, and the recorded `size_download` for each is >= 500000
    - The recorded first-four-bytes output for each PDF shows the PDF file signature, and the verdict line states both byte sizes
    - `git status --porcelain` shows no untracked `*.pdf` anywhere in the tree
    - `git status --porcelain` lists no modified path outside `.planning/phases/32-github-pages-teardown-irreversible/`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Gate check 2' .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md && grep -q '_/downloads/en/latest/pdf/' .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md && grep -q '_/downloads/ja/latest/pdf/' .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md && test -z "$(git status --porcelain | grep '\.pdf$')" && test "$(git status --porcelain | grep -vc '^.. \.planning/phases/32-github-pages-teardown-irreversible/')" = "0"</automated>
  </verify>
  <done>32-EVIDENCE.md records, verbatim, that both the English and Japanese PDF download URLs return a real PDF of plausible size right now. No fidelity re-verification was performed and no binary was committed.</done>
</task>

<task type="auto">
  <name>Task 3: Snapshot the pre-teardown baseline and write the GATE VERDICT</name>
  <files>.planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/32-github-pages-teardown-irreversible/32-CONTEXT.md` — D-03 (a red gate structurally blocks Plans 02/03 and escalates), D-04 (same-day validity), and the `<specifics>` block recording `origin/gh-pages` at `f97862dfea151dd904591a18d2ddbd0bf72fd851` as of 2026-07-27
    - `.planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md` — the two gate-check sections written by Tasks 1 and 2, which this task reads to derive the verdict
    - `.planning/ROADMAP.md` — Phase 32's three success criteria, which the verdict block must name explicitly
  </read_first>
  <action>
    Append two sections to `32-EVIDENCE.md`.

    First, `## Pre-teardown baseline (before-state for SC#2)`. Record, verbatim command plus output:
    (a) `git ls-remote origin` filtered to the pages ref, capturing the current `gh-pages` SHA;
    (b) the current HTTP status of `https://YuSabo90002.github.io/typsphinx/` via `curl` with
    `%{http_code}` and `%{url_effective}` — this is expected to be a live 200 at this point, and
    recording that live 200 is what makes Plan 03's 404 a measured transition rather than an
    unfalsifiable claim; (c) `gh pr view 124` in JSON emitting `number`, `state`, `isDraft`,
    `headRefName`, `headRefOid`, `baseRefName` — the draft PR opened per D-08/D-09 that Plan 03 will
    push onto. Note in prose that `headRefOid` recorded here is the **pre-teardown** head and must
    never be cited as SC#3's observed run (RESEARCH.md Pitfall 4).

    Second, `## GATE VERDICT (SC#1)`. Write a table with one row per gate check — en HTML, root
    resolution, ja HTML content, en PDF, ja PDF — each row carrying the observed value and PASS/FAIL.
    Close with a single line reading exactly `GATE VERDICT: GREEN` or `GATE VERDICT: RED`, plus the
    date the evidence was gathered, plus a sentence restating D-04's freshness rule: this full gate
    is valid for teardown on the same calendar day only; Plans 02 and 03 each re-confirm the four
    URL statuses at their head, and if execution crosses a day boundary this entire plan is re-run
    before the teardown continues.

    If ANY row is FAIL: write `GATE VERDICT: RED`, add an `## ESCALATION` section naming which check
    failed and its observed value, and stop. Do not write any remediation attempt, do not proceed,
    and surface the RED verdict in the plan summary as a blocker for the owner. Per D-03 a red gate
    means Plans 02 and 03 are not entered at all — this is the milestone's only action with no undo
    and it does not proceed on unverified hosting.
  </action>
  <acceptance_criteria>
    - `32-EVIDENCE.md` contains the headings `## Pre-teardown baseline (before-state for SC#2)` and `## GATE VERDICT (SC#1)`
    - The baseline section contains a recorded `git ls-remote` output line carrying a 40-character SHA next to `refs/heads/gh-pages`
    - The baseline section records the current HTTP status of `https://YuSabo90002.github.io/typsphinx/`
    - The baseline section records PR 124's `headRefOid` and states in prose that it is the pre-teardown head and is not SC#3 evidence
    - The file contains exactly one line matching `^GATE VERDICT: (GREEN|RED)$`
    - The verdict table has one row per gate check with an observed value and a PASS/FAIL cell, and no row is left blank
    - If the verdict line reads RED, an `## ESCALATION` section exists naming the failed check
    - `git status --porcelain` lists no modified path outside `.planning/phases/32-github-pages-teardown-irreversible/`
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -cE '^GATE VERDICT: (GREEN|RED)$' .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md)" = "1" && grep -q 'Pre-teardown baseline' .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md && grep -qE 'refs/heads/gh-pages' .planning/phases/32-github-pages-teardown-irreversible/32-EVIDENCE.md && test "$(git status --porcelain | grep -vc '^.. \.planning/phases/32-github-pages-teardown-irreversible/')" = "0"</automated>
  </verify>
  <done>A single unambiguous GREEN or RED verdict exists in 32-EVIDENCE.md, backed by a per-check table, and the pre-teardown before-state (branch SHA, live github.io status, PR head) is recorded so Plan 03's after-state is a measured transition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository -> Read the Docs (public HTTPS) | The gate's entire evidence base crosses this boundary; nothing about RTD's serving state is derivable from repo contents |
| this repository -> GitHub API / origin remote | `git ls-remote` and `gh pr view` read remote state that local refs can misreport |
| planning evidence -> irreversible teardown decision | A false GREEN here authorises an action with no undo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Denial of Service | Documentation hosting (all readers) | high | mitigate | The teardown is structurally gated behind this plan's GREEN verdict (D-03) and re-confirmed at the head of Plans 02 and 03 (D-04). A RED verdict stops the phase and escalates rather than proceeding. |
| T-32-02 | Spoofing | Japanese documentation site | medium | mitigate | An HTTP 200 plus a `lang="ja"` attribute can be served by a 100%-English page (I18N-01's live failure mode, catalogs 24.3% translated). Task 1 requires a CJK-density ratio against the same docname in English, on a 65/65-translated docname, not a status or attribute proxy. |
| T-32-03 | Repudiation | Gate evidence record | medium | mitigate | Verbatim command plus verbatim output (Phase 29 D-15 format) for every check, with an explicit prohibition on paraphrase and on citing earlier phases' evidence. The verdict table states observed values, not conclusions. |
| T-32-04 | Tampering | Remote branch / PR state read | low | mitigate | Baseline read via `git ls-remote origin` (live remote query) and `gh pr view`, never a local `git branch -a` listing which can be stale relative to origin. |
| T-32-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages in any ecosystem — RESEARCH.md's Package Legitimacy Audit records "Not applicable", and the only third-party dependency movement in the phase is the *removal* of `peaceiris/actions-gh-pages@v4` in Plan 02. No `[ASSUMED]` or `[SUS]` package exists to gate. |
</threat_model>

<verification>
- `32-EVIDENCE.md` exists and contains all four required headings (Gate check 1, Gate check 2, Pre-teardown baseline, GATE VERDICT).
- Exactly one `GATE VERDICT:` line, reading GREEN or RED.
- Every recorded check is a verbatim `$ command` line followed by its literal output — spot-check that no section says "verified" or "looks correct" without a command above it.
- `git status --porcelain` shows changes confined to `.planning/phases/32-github-pages-teardown-irreversible/`; no source file, no workflow file, and nothing under `typsphinx/` was touched.
- No `*.pdf` binary was committed.
</verification>

<success_criteria>
- SC#1 is discharged: freshly re-taken, in-phase evidence proves RTD is currently serving English HTML, Japanese HTML (content-verified), the English PDF, the Japanese PDF, and that the documentation root resolves.
- The gate's disposition is unambiguous and machine-readable (`GATE VERDICT: GREEN` or `GATE VERDICT: RED`).
- The pre-teardown before-state is recorded: `gh-pages` SHA, live `github.io` status, PR #124 head.
- Zero repository source changes were made by this plan.
</success_criteria>

<output>
Create `.planning/phases/32-github-pages-teardown-irreversible/32-01-SUMMARY.md` when done.

If the verdict is RED, the summary MUST lead with the blocker, name the failed check and its
observed value, and state that Plans 02 and 03 must not be executed.
</output>
