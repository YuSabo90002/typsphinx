---
phase: 31-published-url-cutover-repo-wide-link-guard
plan: 04
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - .planning/codebase/INTEGRATIONS.md
autonomous: true
requirements: [DOC-09]

must_haves:
  truths:
    - "INTEGRATIONS.md's Hosting section describes Read the Docs as the documentation host, naming the parent project, the Japanese translation project, and `.readthedocs.yaml` as the build manifest."
    - "INTEGRATIONS.md's Environment Configuration section states the language-resolution precedence `READTHEDOCS_LANGUAGE` > `SPHINX_LANGUAGE` > `\"en\"` and names `docs/source/conf.py`'s `_resolve_language()` as where it lives."
    - "INTEGRATIONS.md's CI section enumerates all five workflow files that exist in `.github/workflows/`, including `links.yml`."
    - "Every GitHub Action version recorded in INTEGRATIONS.md matches a version actually pinned in a workflow file, verified by grep at execution time rather than copied from the previous revision."
    - "INTEGRATIONS.md records the `typsphinx-doc-translations` repository as external infrastructure and is explicit that its submodule and pin-bump workflow live in that repository, not in this one."
    - "INTEGRATIONS.md's @preview version-synchronization section names every surface the guard actually watches and separately flags the unguarded one, rather than asserting a count that no longer holds."
    - "Every URL in the refreshed INTEGRATIONS.md returns a live page when fetched over real HTTP."
    - "The file's Analysis Date reflects the date of this refresh."
  prohibitions:
    - "Read the Docs must not be described as an API integration or an external service this project calls — it is a hosting platform. This project's `api-coverage` gate has a documented false-positive class triggered by exactly that phrasing (30.1-VERIFICATION.md override; STATE.md Accumulated Context)."
    - "INTEGRATIONS.md must not be carved out of links.yml's `.planning/` path exclusion — D-19 settled this; its URLs are verified once here by curl, not by ongoing CI."
    - "The GitHub Pages deploy step in docs.yml must be described as it currently exists, not as removed — its removal is Phase 32's work (CI-04) and describing it as already gone would make this file wrong in the other direction."
    - "No file under typsphinx/ may be modified (milestone invariant #3)."
    - "The unguarded fifth @preview pin site must be documented, not repaired — repairing it would touch the version-sync surface that milestone invariant #2 freezes."
    - "No workflow file may be edited by this plan; it describes the CI surface, it does not change it."
  artifacts:
    - ".planning/codebase/INTEGRATIONS.md"
  key_links:
    - "INTEGRATIONS.md -> the codebase map that /gsd-plan-phase loads for API/backend/integration phases. A stale hosting section here silently mis-briefs every future phase that touches CI or docs."
    - "`docs/source/conf.py::_resolve_language()` -> RTD's per-project Language setting. This is the seam that made the Japanese site render Japanese; the file currently documents the pre-Phase-29 world where only a CI env var existed."
---

<objective>
Bring `.planning/codebase/INTEGRATIONS.md` back into agreement with the repository it describes.

Purpose: SC#2 names this file alongside README.md and pyproject.toml, and D-18 chose a full
refresh over the ROADMAP-minimum patch. The file's Analysis Date is 2026-07-22 — before Phase 29
started — so its Hosting and Environment Configuration sections describe a project that no longer
exists: no Read the Docs anywhere, a language mechanism that has since been replaced, an action
version that no workflow uses, and no trace of the translations repository or of this phase's own
link-check workflow.

Output: a refreshed INTEGRATIONS.md whose every factual claim was re-measured against the tree at
execution time, and whose every URL was fetched over real HTTP.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-CONTEXT.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-RESEARCH.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-01-SUMMARY.md
</context>

## Environment briefing

**Worktree execution.** If `test -f .git` succeeds, provision with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and prefix commands with
`uv run`.

**lychee is never run locally (D-08).** URL verification here is `curl`, which is allowed.

**Measure, do not recall (milestone invariant #4).** Every anchor given below was measured on
2026-07-26 and is supplied so you can detect drift, not so you can skip measuring. Re-run each
command at execution time. Where your reading differs from the anchor, your reading wins and the
difference is a finding worth noting in the summary.

**This file is excluded from ongoing link watch by design (D-19).** Its URLs get exactly one
verification — the `curl` pass in Task 2. Get it right here; nothing downstream will catch it.

## Artifacts this phase produces

Symbols and paths created by Phase 31 (this plan's share marked ✱):

| Artifact | Kind | Plan |
|---|---|---|
| `.github/workflows/links.yml` | new file | 01 |
| workflow name `Link Check` / job id `link-check` | CI identifiers | 01 |
| `.planning/phases/31-.../31-EVIDENCE.md` | new file | 01, 05 |
| `.planning/phases/31-.../31-ABOUT-EVIDENCE.md` | new file | 02 |
| repository field `homepage` (GitHub metadata) | external state | 02 |
| `tests/test_no_stale_github_io_links.py` | new file | 03 |
| `test_readme_documentation_links_point_at_readthedocs` | new test function | 03 |
| `test_readme_deep_links_carry_language_version_prefix` | new test function | 03 |
| `test_readme_top_level_links_carry_no_version_segment` | new test function | 03 |
| `test_pyproject_documentation_url_points_at_readthedocs` | new test function | 03 |
| `REPO_ROOT` / `README_PATH` / `PYPROJECT_PATH` | new module constants | 03 |
| `.planning/phases/31-.../31-ISSUE-119-REPLY-DRAFT.md` | new file | 05 |

✱ This plan creates no new symbols — it rewrites the content of an existing file.

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the integration surface and rewrite the hosting, CI, and environment sections</name>

  <files>.planning/codebase/INTEGRATIONS.md</files>

  <read_first>
    - `.planning/codebase/INTEGRATIONS.md` — the whole 140-line file. You are rewriting most of it; read all of it first so section ordering and heading style survive the refresh.
    - `.readthedocs.yaml` — the build manifest. Note what it actually configures: `build.os: ubuntu-24.04`, Python 3.12, `apt_packages: [fonts-noto-cjk]`, a `build.jobs.build.pdf` override that runs `sphinx-build -b typstpdf` into a temp dir and copies only the PDF into `$READTHEDOCS_OUTPUT/pdf/`, `formats: [pdf]`, and a `python.install` step using `method: uv` with the `docs` extra. Its own comments explain why each exists — those reasons belong in the refreshed Hosting section.
    - `docs/source/conf.py` lines 45-60 — `_resolve_language()` and the `language = _resolve_language()` assignment. This is the seam the current file's "CI only: SPHINX_LANGUAGE" line predates.
    - `.github/workflows/docs.yml` — read in full. It still builds HTML and PDF, uploads both as artifacts, deploys to GitHub Pages via `peaceiris/actions-gh-pages@v4`, and attaches the PDF to tagged releases. Describe it as it is.
    - `.github/workflows/links.yml` — created by Plan 01. Read it to describe its trigger, permissions, and scope accurately.
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-04-SUMMARY.md` and `30.1-10-SUMMARY.md` — the translations-repository setup and its pin-bump workflow, as actually delivered.
    - `.planning/STATE.md` § Accumulated Context — the three owed post-merge flips, and the `api-coverage` false-positive note about how Read the Docs must be phrased.
  </read_first>

  <action>
Re-measure first, then rewrite. Run these and work from what they return:

- `ls .github/workflows/` — the workflow inventory. Anchor: five files after Plan 01 (`ci.yml`, `docs.yml`, `drift.yml`, `links.yml`, `release.yml`). The current file lists four and omits `links.yml`.
- `grep -rn 'uses:' .github/workflows/ | grep -oE '[a-z0-9-]+/[a-zA-Z0-9_-]+@[a-zA-Z0-9./]+' | sort -u` — the real action-version set. Anchor: `actions/checkout@v7` in every workflow. The current file claims `actions/checkout@v6`, which appears nowhere. Also present and currently undocumented: `actions/setup-python@v6` and `peaceiris/actions-gh-pages@v4`, both in `docs.yml`.

<!-- planner-discipline-allow: checkout@v6 -->
- `test -f .gitmodules` — anchor: **absent** in this repository. Phase 30.1's submodule and its pin-bump workflow live in the `typsphinx-doc-translations` repository, which submodules *this* one. Describing the submodule as living here would be wrong; it is external infrastructure this repository is consumed by.
- `ls docs/locale/ja 2>/dev/null` — anchor: **absent**. The catalogs moved out to the translations repository in Phase 30.1.

Then rewrite three sections.

**Hosting.** Currently a single line naming GitHub. It becomes the Read the Docs description: the
parent project serving English at `https://typsphinx.readthedocs.io/en/latest/`, the linked
Japanese translation project serving `https://typsphinx.readthedocs.io/ja/latest/`, the bare root
`https://typsphinx.readthedocs.io/` redirecting to the project's Default Version, and
`.readthedocs.yaml` as the sole place Read the Docs' build behavior is configured — nothing in
`conf.py` or the local tox tasks reaches it. Cover the PDF path specifically: Read the Docs' own
LaTeX pipeline is deliberately replaced by a `build.jobs.build.pdf` override so the published PDF
is the one `typstpdf` produced, and `fonts-noto-cjk` is installed because Typst's font fallback is
silent and a missing-glyph PDF would otherwise build green. Keep GitHub listed as the source-code
host — it did not stop being one.

Phrase all of this as *hosting and build platform*, never as an API this project calls or
integrates with. This project's `api-coverage` gate has a documented false-positive class that
fires on exactly that wording and has already forced one override
(`30.1-VERIFICATION.md § Acknowledged Gate Overrides`). Read the Docs' public HTTP endpoints were
read once, during verification, as one-off corroborating measurements — that is not an integration
and must not be written as one.

**CI Pipeline.** Extend the workflow list to all five files. Each gets one line describing what it
actually does now. `docs.yml`'s current line predates Phase 30 — re-derive it from the file:
it builds HTML and PDF, uploads both as artifacts, deploys to GitHub Pages on pushes to `main`,
and attaches the PDF to tagged releases. Describe the Pages deploy step as present, because it is;
its removal is Phase 32's work and pre-announcing it here would make this file wrong in the
opposite direction. `links.yml` gets a line naming its trigger (push and pull_request), its
advisory posture (never a required check), and its scope (the repository-wide file class Sphinx's
linkcheck builder cannot reach).

**Environment Configuration.** The "CI only: `SPHINX_LANGUAGE` (docs.yml, defaults to \"en\")"
line is stale twice over — no workflow in this repository sets that variable at all any more, and
the resolution rule changed. Replace it with the actual precedence:
`READTHEDOCS_LANGUAGE` > `SPHINX_LANGUAGE` > `"en"`, resolved by `_resolve_language()` in
`docs/source/conf.py`, where `READTHEDOCS_LANGUAGE` is injected by Read the Docs from each
project's own Language setting and `SPHINX_LANGUAGE` remains available as a local or CI override.
Also record `$READTHEDOCS_OUTPUT`, which `.readthedocs.yaml`'s PDF job depends on and which is
injected by the build environment rather than configured.

Leave the Data Storage, Authentication, Monitoring, Secrets, Intersphinx, and Dependabot sections
in place for now; Task 2 handles what remains.
  </action>

  <verify>
    <automated>bash -c 'set -e; F=.planning/codebase/INTEGRATIONS.md; for s in "readthedocs.io" "READTHEDOCS_LANGUAGE" "SPHINX_LANGUAGE" "_resolve_language" ".readthedocs.yaml" "links.yml" "ci.yml" "docs.yml" "drift.yml" "release.yml" "typsphinx-doc-translations" "READTHEDOCS_OUTPUT"; do grep -q -- "$s" "$F" || { echo "MISSING: $s"; exit 1; }; done; grep -q "checkout@v7" "$F"; test $(grep -c "checkout@v6" "$F") -eq 0; test $(ls .github/workflows/ | wc -l) -eq 5; echo "hosting/CI/environment sections refreshed"'</automated>
  </verify>

  <acceptance_criteria>
    - INTEGRATIONS.md contains `typsphinx.readthedocs.io`, `.readthedocs.yaml`, and `READTHEDOCS_OUTPUT`.
    - INTEGRATIONS.md contains the exact precedence chain `READTHEDOCS_LANGUAGE` > `SPHINX_LANGUAGE` > `"en"` and names `_resolve_language()`.
    - `grep -c 'SPHINX_LANGUAGE (docs.yml' .planning/codebase/INTEGRATIONS.md` returns `0` — the stale CI-only claim is gone.
    - INTEGRATIONS.md names all five files in `.github/workflows/`, and `ls .github/workflows/ | wc -l` returns `5`.
    - `grep -c 'checkout@v6' .planning/codebase/INTEGRATIONS.md` returns `0`; `grep -c 'checkout@v7' .planning/codebase/INTEGRATIONS.md` returns at least `1`.
    - INTEGRATIONS.md names `typsphinx-doc-translations` and states its submodule and pin-bump workflow live in that repository; `test ! -f .gitmodules` succeeds, confirming the claim.
    - INTEGRATIONS.md describes docs.yml's GitHub Pages deploy step as present.
    - `grep -icE 'readthedocs.*(api|endpoint|integration)' .planning/codebase/INTEGRATIONS.md` returns `0` — Read the Docs is described as a hosting platform, not an API integration.
    - `git diff --name-only HEAD~1` lists only `.planning/codebase/INTEGRATIONS.md`.
  </acceptance_criteria>

  <done>The three sections that Phase 29, Phase 30, and Phase 30.1 invalidated now describe the repository as it currently stands, with every claim re-derived from a command run at execution time.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the remaining sections, update the Analysis Date, and verify every URL over real HTTP</name>

  <files>.planning/codebase/INTEGRATIONS.md</files>

  <read_first>
    - `.planning/codebase/INTEGRATIONS.md` — as left by Task 1.
    - `tests/test_preview_version_sync.py` — read the module docstring and the path constants. It watches `typsphinx/writer.py`, `typsphinx/template_engine.py`, `typsphinx/templates/base.typ`, and every `.typ` under `examples/`. The docstring explains why `examples/` was added.
    - `docs/source/_typst/custom_template.typ` lines 35-50 — this file pins the same four `@preview` packages but is **not** watched by the sync guard. It is a carried Warning from Phase 30.1's review, recorded in STATE.md.
    - `.github/dependabot.yml` — verify the pip and github-actions ecosystem blocks against what the current INTEGRATIONS.md claims.
    - `docs/source/conf.py` — the `intersphinx_mapping` entries, to confirm the Intersphinx section.
  </read_first>

  <action>
Finish the refresh, then measure every URL.

**@preview version-synchronization section.** The current text says the four packages "must stay
synchronized across three files" and lists three synchronization points plus the test. That count
is stale in both directions. Rewrite it to state what the guard actually watches — `writer.py`,
`template_engine.py`, `templates/base.typ`, and every `.typ` under `examples/`, the last added at
the v0.6.3 close after a bundled sample shipped three milestones behind and could not compile — and
then, separately and explicitly, flag `docs/source/_typst/custom_template.typ` as a fifth site that
pins the same four packages and is **not** covered by the guard. Document it; do not repair it.
Repairing it would touch the version-sync surface that milestone invariant #2 freezes, and it is
already tracked as a carried Warning from Phase 30.1's review.

**APIs & External Services.** The current "None directly used" is still substantially right for the
package's runtime and should stay in that spirit. Extend it only to note that the *documentation
build and hosting* path now depends on external platforms — Read the Docs for hosting, Typst
Universe's package registry for the four `@preview` packages fetched at compile time. Keep the
distinction sharp: the `typsphinx` package itself calls nothing over the network at runtime, which
is the claim that section exists to make. Do not describe Read the Docs' HTTP endpoints as an
integration surface.

**Actions, Dependabot, Intersphinx.** Reconcile each against the files read above and correct any
drift. The Actions list needs the two currently-undocumented entries surfaced by Task 1's grep.

**Analysis Date.** Update both the header `**Analysis Date:**` and the closing
`*Integration audit: ...*` line to the execution date. Leaving either stale reproduces exactly the
defect this refresh exists to correct.

**URL verification (SC#2).** Extract every `http(s)` URL from the finished file with `grep -oE` and
fetch each one with `curl -s -o /dev/null -w "%{http_code}" -L`. Every one must return 200.
Transcribe the full URL/status list into the plan summary verbatim, command and output together
(Phase 29 D-15 convention). This is the only verification these URLs get — D-19 keeps this file
outside links.yml's ongoing watch, so a bad URL here has no second chance at detection.

If any URL returns non-200, fix the URL rather than recording the failure. A codebase note that
documents a dead link is the same defect class this phase exists to close.
  </action>

  <verify>
    <automated>bash -c 'set -e; F=.planning/codebase/INTEGRATIONS.md; grep -q "custom_template.typ" "$F"; grep -q "examples/" "$F"; D=$(date -u +%Y-%m-%d); grep -q "$D" "$F" || { echo "Analysis Date not updated to $D"; exit 1; }; N=0; for u in $(grep -oE "https?://[^ )>\"'\''\`,;]+" "$F" | sed -E "s/[.,)]+$//" | sort -u); do c=$(curl -s -o /dev/null -w "%{http_code}" -L --max-time 20 "$u"); echo "$c $u"; [ "$c" = "200" ] || exit 1; N=$((N+1)); done; echo "$N URLs verified 200"'</automated>
  </verify>

  <acceptance_criteria>
    - INTEGRATIONS.md names all four surfaces the `@preview` sync guard watches, including `examples/`.
    - INTEGRATIONS.md names `docs/source/_typst/custom_template.typ` and states it is not covered by the guard.
    - `grep -c 'across three files' .planning/codebase/INTEGRATIONS.md` returns `0`.
    - `git diff HEAD~2 -- docs/source/_typst/custom_template.typ` is empty — the unguarded file was documented, not edited.
    - `git diff HEAD~2 -- typsphinx/` is empty — milestone invariant #3 held.
    - Both the header `**Analysis Date:**` and the closing `*Integration audit: ...*` line carry today's UTC date, and they agree with each other.
    - Every distinct URL in INTEGRATIONS.md returns HTTP `200` under `curl -L`, and the full URL/status list appears verbatim in `31-04-SUMMARY.md`.
    - `grep -icE 'readthedocs.*(api|endpoint|integration)' .planning/codebase/INTEGRATIONS.md` returns `0`.
    - `git diff --name-only HEAD~1` lists only `.planning/codebase/INTEGRATIONS.md`.
  </acceptance_criteria>

  <done>The codebase note is accurate as of today across every section, its URL set has been fetched over real HTTP, and the two drift channels it was itself blind to — the unguarded fifth `@preview` pin site and the stale Analysis Date — are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INTEGRATIONS.md → every future GSD phase that loads the codebase map | A wrong claim here propagates silently into later planning |
| Documentation build → Read the Docs build environment | Third-party runner executes this repository's build commands with its own environment variables |
| Typst compile → `packages.typst.org` | The four `@preview` packages are fetched from an external registry at documentation build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-15 | Tampering | INTEGRATIONS.md's factual claims | medium | mitigate | Every claim is re-derived from a command run at execution time (`ls .github/workflows/`, the `uses:` grep, `test -f .gitmodules`) rather than edited from the previous revision. The acceptance criteria assert the command's result, not the prose. |
| T-31-16 | Information Disclosure | A codebase note enumerating secret names and CI surface | low | accept | Only secret *names* are recorded (`CODECOV_TOKEN`, `PYPI_API_TOKEN`, `TEST_PYPI_API_TOKEN`), never values, and all three are already public in the workflow files. This is unchanged from the existing document. |
| T-31-17 | Spoofing | URLs recorded in a codebase note that no ongoing check watches | medium | mitigate | D-19 removes this file from links.yml's watch, so Task 2's `curl` sweep is the sole verification. It is therefore made exhaustive and blocking: every distinct URL must return 200, a non-200 must be fixed rather than recorded, and the full transcript is required in the summary. |
| T-31-18 | Tampering | Documenting an unguarded `@preview` pin site without guarding it | medium | accept | `docs/source/_typst/custom_template.typ` can drift from `base.typ` undetected. Accepted here because closing it means touching the version-sync surface milestone invariant #2 freezes. Documenting it converts a silent gap into a tracked one; the repair belongs to a future milestone alongside the existing carried Warning. |
| T-31-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing and edits one Markdown file. No registry dependency is added or referenced. |
</threat_model>

<verification>
- INTEGRATIONS.md's Hosting, CI, and Environment Configuration sections match measurements taken at execution time.
- All five workflow files are enumerated; no action version appears that no workflow pins.
- The translations repository is described as external, corroborated by `test ! -f .gitmodules`.
- The `@preview` sync section names four guarded surfaces plus one flagged unguarded surface.
- Both date lines carry today's UTC date and agree.
- Every URL in the file returns 200, transcribed verbatim in the summary.
- `git diff` confirms nothing under `typsphinx/`, `.github/workflows/`, or `docs/source/_typst/` was modified.
</verification>

<success_criteria>
The third surface SC#2 names — the codebase notes — describes the repository that exists today
rather than the one that existed before this milestone began, and every URL it publishes has been
fetched over real HTTP in the one verification pass it will ever get.
</success_criteria>

<output>
Create `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-04-SUMMARY.md` when done.
</output>
