---
phase: 31-published-url-cutover-repo-wide-link-guard
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - README.md
  - pyproject.toml
  - tests/test_no_stale_github_io_links.py
autonomous: true
requirements: [DOC-09]

must_haves:
  truths:
    - "Every documentation URL in README.md addresses Read the Docs, and each one returns 200 when fetched over real HTTP."
    - "pyproject.toml's `Documentation` entry addresses the Read the Docs root, so the next PyPI release advertises a live documentation link instead of a README anchor."
    - "The README's documentation badge is Read the Docs' own build-status badge, so it turns red when the documentation build actually breaks."
    - "A one-line link to the Japanese documentation is reachable from the README's Documentation section, and it returns 200."
    - "Top-level documentation links carry no language or version segment while all 7 deep links carry `/en/latest/` — the two URL shapes coexist deliberately and never collapse into one another."
    - "A fresh repo-wide grep for the retired documentation host returns matches in CHANGELOG.md only; README.md and pyproject.toml yield zero, and the regression guard itself yields zero because it splits the literal on purpose."
    - "The 7 quick-link entries keep their existing order and their existing link labels; only the host and the path prefix change, so a reader's mental index of that list still holds."
    - "The rewrite of each file lands as a single commit, so no intermediate tree in this phase's history contains a mixture of retired-host and Read the Docs deep links within one file."
    - "`uv run pytest tests/test_no_stale_github_io_links.py` passes, and it fails loudly if any of the four invariants is broken later."
  prohibitions:
    - "pyproject.toml's `Homepage`, `Repository`, and `Issues` entries must not be changed — they already address GitHub correctly and are explicitly out of the rewrite set (D-11)."
    - "CHANGELOG.md must not be edited; its historical mention of the retired host is a deliberate record (D-03, Phase 24 D-02 precedent)."
    - "Deep links must not use `/en/stable/` — that version does not exist until the v0.6.4 tag builds, so burning it now would fail the 'alive over real HTTP, now' bar (D-10, RTD-06)."
    - "Top-level links must not be version-pinned — the bare root is what makes Phase 33's Default Version flip propagate without re-editing (D-11)."
    - "The link-target suffixes must not be guessed or tidied: `quickstart.html` is correct and `quick_start.html` returns 404 (RESEARCH.md Pitfall 4)."
    - "No file under typsphinx/ may be modified (milestone invariant #3)."
    - "No @preview package version may be changed, and the 4-surface version-sync guard must not be touched (milestone invariant #2)."
    - "The version bump and the CHANGELOG entry belong to Phase 33 — neither may be made here."
    - "Typing imports must not be 'modernized' (UP006/UP035 stay ignored until the pending todo lands — CLAUDE.md)."
  artifacts:
    - "tests/test_no_stale_github_io_links.py"
  key_links:
    - "README badge image URL -> app.readthedocs.org (a different host from the docs themselves); the badge click-through -> typsphinx.readthedocs.io. Conflating the two hosts breaks the badge."
    - "pyproject.toml `Documentation` -> PyPI's project sidebar. This is the only URL in this plan whose effect is invisible until a release ships, which is why it needs its own assertion rather than a visual check."
    - "The regression guard's split string literal -> the repo-wide grep in Plan 05. If the guard spelled the retired host whole, it would make its own success criterion permanently unverifiable."
---

<objective>
Rewrite every published documentation URL in `README.md` and `pyproject.toml` to Read the Docs,
and lock the result behind a hermetic regression guard.

Purpose: this is DOC-09's core. Ten occurrences of the retired host live in `README.md` and one
`Documentation` field in `pyproject.toml` points at a README anchor instead of a documentation
site. Seven of the ten are the deep links that 404'd for months and produced Issue #119. D-09
requires this to land *after* Plan 01's red negative control, so the mechanism's blindness is
disproved before the bug it detects is removed.

Output: a README whose documentation links all resolve, PyPI metadata that will advertise a live
documentation site on the next release, and `tests/test_no_stale_github_io_links.py` guarding four
invariants against silent drift.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-CONTEXT.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-RESEARCH.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-PATTERNS.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-01-SUMMARY.md
</context>

## Environment briefing

**Worktree execution is mandatory here — this plan runs pytest.** If `test -f .git` succeeds you
are in an isolated worktree. Provision it first:
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run every command through
`uv run`. Without this, pytest imports the *main* checkout's `typsphinx` and the new test file's
`REPO_ROOT` still resolves to the worktree — but the suite you think you ran is not the one that
ran. See CLAUDE.md § Worktree-isolated execution.

**lychee is never run locally (D-08).** This plan's HTTP verification uses `curl`, which is
allowed. The link-check job's green run is Plan 05's job, not this one's.

**Re-measure before rewriting (milestone invariant #4).** The counts below were measured on
2026-07-26 and are given so you can detect drift, not so you can skip measuring. Take a fresh
`grep -n` at execution time and reconcile: if the count differs, the tree changed and the
difference is the finding.

## Artifacts this phase produces

Symbols and paths created by Phase 31 (this plan's share marked ✱):

| Artifact | Kind | Plan |
|---|---|---|
| `.github/workflows/links.yml` | new file | 01 |
| workflow name `Link Check` / job id `link-check` | CI identifiers | 01 |
| `.planning/phases/31-.../31-EVIDENCE.md` | new file | 01, 05 |
| `.planning/phases/31-.../31-ABOUT-EVIDENCE.md` | new file | 02 |
| repository field `homepage` (GitHub metadata) | external state | 02 |
| ✱ `tests/test_no_stale_github_io_links.py` | new file | 03 |
| ✱ `test_readme_documentation_links_point_at_readthedocs` | new test function | 03 |
| ✱ `test_readme_deep_links_carry_language_version_prefix` | new test function | 03 |
| ✱ `test_readme_top_level_links_carry_no_version_segment` | new test function | 03 |
| ✱ `test_pyproject_documentation_url_points_at_readthedocs` | new test function | 03 |
| ✱ `REPO_ROOT` / `README_PATH` / `PYPROJECT_PATH` | new module constants | 03 |
| `.planning/phases/31-.../31-ISSUE-119-REPLY-DRAFT.md` | new file | 05 |

## Measured target values (verified 200 over real HTTP, 2026-07-26)

| Role | URL | Decision |
|---|---|---|
| Badge image | `https://app.readthedocs.org/projects/typsphinx/badge/?version=latest` | D-12 |
| Badge click-through / top-level links | `https://typsphinx.readthedocs.io/` | D-11 |
| Japanese documentation | `https://typsphinx.readthedocs.io/ja/latest/` | D-13 |
| Deep link 1 | `https://typsphinx.readthedocs.io/en/latest/installation.html` | D-10 |
| Deep link 2 | `https://typsphinx.readthedocs.io/en/latest/quickstart.html` | D-10 |
| Deep link 3 | `https://typsphinx.readthedocs.io/en/latest/user_guide/` | D-10 |
| Deep link 4 | `https://typsphinx.readthedocs.io/en/latest/user_guide/configuration.html` | D-10 |
| Deep link 5 | `https://typsphinx.readthedocs.io/en/latest/examples/` | D-10 |
| Deep link 6 | `https://typsphinx.readthedocs.io/en/latest/api/` | D-10 |
| Deep link 7 | `https://typsphinx.readthedocs.io/en/latest/contributing.html` | D-10 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite every documentation URL in README.md</name>

  <files>README.md</files>

  <read_first>
    - `README.md` lines 1-20 — the badge block. Six badges; only the documentation one changes. Read it to see the exact Markdown badge form used by the other five so the replacement matches house style.
    - `README.md` lines 258-290 — the `## Documentation` section: the lead sentence, the `Quick links:` label, and the seven-item list with its labels and order.
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-PATTERNS.md` § `README.md` — the before/after pairs for each of the three edit sites.
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-CONTEXT.md` — D-10, D-11, D-12, D-13.
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-RESEARCH.md` § Pitfall 4 — why the seven suffixes must be preserved exactly rather than tidied.
  </read_first>

  <behavior>
    - After the rewrite, extracting every `https://typsphinx.readthedocs.io/...` URL from README.md yields exactly 7 URLs containing `/en/latest/`, exactly 1 containing `/ja/latest/`, and exactly 3 containing neither.
    - The seven `/en/latest/` URLs end in these suffixes, in this order: `installation.html`, `quickstart.html`, `user_guide/`, `user_guide/configuration.html`, `examples/`, `api/`, `contributing.html`.
    - The list labels accompanying those seven links are unchanged from the current file.
    - The documentation badge's image URL is on host `app.readthedocs.org` and its click-through target is on host `typsphinx.readthedocs.io`.
    - The five non-documentation badges (CI, PyPI version, Python Support, License, Code style) are byte-identical to before.
  </behavior>

  <action>
Take a fresh measurement first: `grep -n` README.md for the retired documentation host. Measured
2026-07-26: 10 matching lines (8, 12, 267, 271, 272, 273, 274, 275, 276, 277) carrying 11 string
occurrences — line 267 carries two, because the retired host appears there once as the Markdown
link *text* and once as its target. That second occurrence is easy to miss with a
line-oriented replace, and leaving it behind fails this task's grep gate. Reconcile your fresh
count against 10 lines / 11 occurrences before editing.

<!-- planner-discipline-allow: github.io -->

There are three edit sites.

**Site 1 — the documentation badge, line 8 (D-12).** Replace the whole badge. The image becomes
Read the Docs' official build-status badge at
`https://app.readthedocs.org/projects/typsphinx/badge/?version=latest`; the click-through target
becomes `https://typsphinx.readthedocs.io/`; the alt text becomes `Documentation Status`, matching
Read the Docs' own convention. Note the two hosts differ — `app.readthedocs.org` serves the badge
image, `typsphinx.readthedocs.io` serves the documentation — and swapping them breaks it. The
static shields.io `docs-latest-blue` badge is dropped entirely: its whole defect was that it always
rendered blue regardless of build health, which is exactly the monitoring gap D-12 closes. Leave
the other five badges untouched.

**Site 2 — the header link row, line 12 (D-11).** The `Documentation` entry's target becomes the
bare root `https://typsphinx.readthedocs.io/`. The `Issue Tracker` and `PyPI` entries on the same
line already address the right places; do not touch them.

**Site 3 — the `## Documentation` section, lines 267-277 (D-10, D-11, D-13).** The lead sentence's
link text and its target both currently name the retired host; the text becomes
`typsphinx.readthedocs.io` and the target becomes `https://typsphinx.readthedocs.io/` — bare root,
because this is a top-level link. Each of the seven quick links then takes the form
`https://typsphinx.readthedocs.io/en/latest/<suffix>` with the suffix copied verbatim from the
current line. All seven suffixes were curl-verified 200 in this shape on 2026-07-26; do not
normalise or "tidy" any of them — `quickstart.html` is correct and the underscore variant returns
404. Preserve the list order and the seven link labels exactly; the labels are how a returning
reader finds a page.

Finally, add the Japanese documentation link (D-13). Place it in this same section so it is
discoverable next to the English documentation — one line, pointing at
`https://typsphinx.readthedocs.io/ja/latest/`, worded to make clear it is the Japanese-language
site. Exact placement within the section and exact wording are yours; keep it to one line.

<!-- planner-discipline-allow: typsphinx.readthedocs.io/ja/latest/ -->
<!-- planner-discipline-allow: app.readthedocs.org/projects/typsphinx/badge -->

Why two URL shapes coexist: top-level links deliberately carry no version segment so Read the Docs'
root redirect follows the project's Default Version, which Phase 33 flips from `latest` to `stable`
— those links update themselves. The seven deep links are pinned to `/en/latest/` because
`/en/stable/` does not exist until the v0.6.4 tag builds, and D-10 accepts that they stay on
`latest` permanently. This split is intentional, and Task 3 turns it into a guarded invariant.
  </action>

  <verify>
    <automated>bash -c 'set -e; test $(grep -oE "https://typsphinx\.readthedocs\.io/en/latest/[^)]+" README.md | wc -l) -eq 7; test $(grep -c "typsphinx.readthedocs.io/ja/latest/" README.md) -eq 1; test $(grep -oE "https://typsphinx\.readthedocs\.io/[^)]*" README.md | grep -vcE "/(en|ja)/") -eq 3; test $(grep -c "app.readthedocs.org/projects/typsphinx/badge" README.md) -eq 1; test $(grep -v "^#" README.md | grep -c "github\.io") -eq 0; for u in $(grep -oE "https://(typsphinx\.readthedocs\.io|app\.readthedocs\.org)[^)]*" README.md | sort -u); do c=$(curl -s -o /dev/null -w "%{http_code}" -L --max-time 20 "$u"); echo "$c $u"; [ "$c" = "200" ]; done; echo "README URLs rewritten and all live"'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -v '^#' README.md | grep -c 'github\.io'` returns `0`.
    - `grep -oE 'https://typsphinx\.readthedocs\.io/en/latest/[^)]+' README.md | wc -l` returns `7`.
    - Those seven, in file order, end with `installation.html`, `quickstart.html`, `user_guide/`, `user_guide/configuration.html`, `examples/`, `api/`, `contributing.html`.
    - `grep -oE 'https://typsphinx\.readthedocs\.io/[^)]*' README.md | grep -vcE '/(en|ja)/'` returns `3` — the badge click-through, the header link row, and the section lead sentence.
    - `grep -c 'typsphinx.readthedocs.io/ja/latest/' README.md` returns `1`.
    - `grep -c 'app.readthedocs.org/projects/typsphinx/badge' README.md` returns `1`.
    - `grep -c 'img.shields.io/badge/docs' README.md` returns `0` — the static documentation badge is gone.
    - `grep -c 'img.shields.io' README.md` returns `3` — the License, code-style, and pyversions badges survive.
    - `grep -c 'readthedocs.io/en/stable' README.md` returns `0`.
    - `grep -c 'quick_start' README.md` returns `0`.
    - Every distinct `typsphinx.readthedocs.io` or `app.readthedocs.org` URL in README.md returns HTTP `200` under `curl -L`.
    - `git diff --name-only HEAD~1` lists only `README.md`.
    - `git diff HEAD~1 -- README.md` shows no change to the `**Status**:` line — the version-sync guard's subject is untouched.
  </acceptance_criteria>

  <done>Every documentation link a README reader can click resolves to a live Read the Docs page, the documentation badge reflects real build health, the Japanese site is discoverable, and the two-shape URL split is in place exactly as D-10 and D-11 specify.</done>
</task>

<task type="auto">
  <name>Task 2: Point the PyPI Documentation metadata at Read the Docs</name>

  <files>pyproject.toml</files>

  <read_first>
    - `pyproject.toml` lines 54-58 — the `[project.urls]` table. Four entries; exactly one changes.
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-PATTERNS.md` § `pyproject.toml` — the before/after pair and the explicit note that Homepage/Repository/Issues are not rewrite targets.
    - `.planning/REQUIREMENTS.md` — REL-02, which depends on this field being right when Phase 33 publishes.
  </read_first>

  <action>
In `[project.urls]`, change the `Documentation` entry's value to `https://typsphinx.readthedocs.io/`
— the bare root, same shape and same reasoning as the README's top-level links (D-11): Read the
Docs' root redirect follows the Default Version, so Phase 33's `latest` → `stable` flip reaches
this field without another edit, and REL-02's "its Documentation metadata points at Read the Docs"
is satisfied by construction.

The current value addresses the repository README's anchor rather than a documentation site. It is
not a retired-host URL and so it does not appear in any host-based grep — which is precisely why it
survived: it renders as a working link on PyPI while delivering a reader to a README fragment
instead of documentation.

<!-- planner-discipline-allow: typsphinx#readme -->

Change nothing else in this file. `Homepage`, `Repository`, and `Issues` already address GitHub
correctly and are explicitly excluded from the rewrite set. The `version` field belongs to Phase 33
and must not move; the dependency tables and `[tool.*]` sections are out of scope.
  </action>

  <verify>
    <automated>bash -c 'set -e; uv run python -c "import tomllib;d=tomllib.load(open(\"pyproject.toml\",\"rb\"))[\"project\"][\"urls\"];assert d[\"Documentation\"]==\"https://typsphinx.readthedocs.io/\", d;assert d[\"Homepage\"]==\"https://github.com/YuSabo90002/typsphinx\";assert d[\"Repository\"]==\"https://github.com/YuSabo90002/typsphinx\";assert d[\"Issues\"]==\"https://github.com/YuSabo90002/typsphinx/issues\";print(\"project.urls OK\")"; c=$(curl -s -o /dev/null -w "%{http_code}" -L --max-time 20 https://typsphinx.readthedocs.io/); echo "$c documentation url"; test "$c" = "200"'</automated>
  </verify>

  <acceptance_criteria>
    - `tomllib` parses `pyproject.toml` and `project.urls.Documentation` equals `https://typsphinx.readthedocs.io/` exactly.
    - `project.urls.Homepage`, `project.urls.Repository`, and `project.urls.Issues` are byte-identical to their pre-task values.
    - `grep -v '^\s*#' pyproject.toml | grep -c 'typsphinx#readme'` returns `0`.
    - `curl -s -o /dev/null -w '%{http_code}' -L https://typsphinx.readthedocs.io/` returns `200`.
    - `git diff HEAD~1 -- pyproject.toml` touches exactly one line.
    - `git diff HEAD~1 -- pyproject.toml` shows no change to the `version` field.
    - `uv run pytest tests/test_readme_version_sync.py tests/test_extension.py -q` passes — the existing pyproject-parsing tests still hold.
  </acceptance_criteria>

  <done>The `Documentation` URL that PyPI will display for release 0.6.4 addresses the Read the Docs root and returns 200, with the three sibling URL entries provably untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the hermetic regression guard for the rewritten URLs</name>

  <files>tests/test_no_stale_github_io_links.py</files>

  <read_first>
    - `tests/test_readme_version_sync.py` — the structural analog to copy: module docstring naming the hazard being guarded, `REPO_ROOT = Path(__file__).resolve().parents[1]` plus `README_PATH`/`PYPROJECT_PATH` constants, module-level compiled regex constants with explanatory comments, small helper functions, and assertions carrying a message that explains what broke and why it matters. Note its guard-against-vacuous-pass idiom: it asserts the regex matched *something* before comparing, so a wording change fails loudly instead of silently passing.
    - `tests/test_preview_version_sync.py` — the other instance of this project's raw-text-parsing convention; confirms that guards parse file text directly rather than importing metadata.
    - `README.md` and `pyproject.toml` — as rewritten by Tasks 1 and 2; the guard must pass against them on first run.
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-PATTERNS.md` § `tests/test_no_stale_github_io_links.py` — the analog assignment and the substring-versus-regex guidance.
  </read_first>

  <behavior>
    - `test_readme_documentation_links_point_at_readthedocs` fails if README.md regains any URL on the retired documentation host.
    - `test_readme_deep_links_carry_language_version_prefix` fails if the count of `/en/latest/` deep links in README.md is not exactly 7, or if any of the 7 expected suffixes is missing.
    - `test_readme_top_level_links_carry_no_version_segment` fails if a Read the Docs URL that should be a bare root acquires a language or version segment — the invariant that keeps Phase 33's Default Version flip self-propagating.
    - `test_pyproject_documentation_url_points_at_readthedocs` fails if `project.urls.Documentation` stops addressing `typsphinx.readthedocs.io`.
    - All four run without network access and complete in well under a second.
  </behavior>

  <action>
Create `tests/test_no_stale_github_io_links.py` following `test_readme_version_sync.py`'s structure
exactly: module docstring, `REPO_ROOT`/`README_PATH`/`PYPROJECT_PATH` constants, module-level
regex constants with comments, then the four test functions named in the artifacts table above.
Parse raw file text (and `tomllib` for the TOML side, as `test_readme_version_sync.py` already
does); never import package metadata and never make a network call. The real-HTTP checks are a
separate instrument entirely — Plan 05's curl sweep and the Link Check CI job.

The module docstring must name the hazard concretely: seven README deep links pointed at a
documentation host that had been retired, they returned 404 for months, an external reporter filed
Issue #119, and Phase 31 rewrote them. That is the drift this module exists to catch a second time.

**Build the retired-host constant from two concatenated string fragments split across the dot**,
with a comment saying why. SC#2's claim is that a fresh repo-wide grep for the retired host finds
matches only in `CHANGELOG.md`. A guard that spelled the host whole would put a permanent match in
`tests/`, making its own success criterion unverifiable by grep and forcing every future check to
carry an exclusion list. Splitting the literal keeps the repo-wide grep clean and honest.

Apply `test_readme_version_sync.py`'s guard-against-vacuous-pass idiom throughout: before asserting
that something is absent, assert that the structure you are searching is present. A test that
passes because it found nothing to check is worse than no test — it is the exact failure mode that
let these links rot. Concretely: assert README.md contains at least one Read the Docs URL before
asserting it contains no retired-host URL.

For the deep-link test, define the seven expected suffixes as a module-level tuple in file order
and assert both the count and each suffix's presence, so a dropped or reordered link is caught, not
just a wrong host.

For the top-level test, extract every `typsphinx.readthedocs.io` URL, partition on whether it
carries a language segment, and assert the version-less partition is non-empty and that no member
of it contains a version-like segment. Give the assertion a message explaining the D-11 rationale —
that these links must stay version-less so the Default Version flip reaches them — because a future
reader "fixing" them to explicit versions would be undoing the design, not tidying it.

Run `black`, `ruff check`, and `mypy` over the new file before committing; CI runs all three and
this repository's line length is 88.
  </action>

  <verify>
    <automated>bash -c 'set -e; uv run pytest tests/test_no_stale_github_io_links.py -v; uv run black --check tests/test_no_stale_github_io_links.py; uv run ruff check tests/test_no_stale_github_io_links.py; uv run pytest -q -m "not slow"'</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_no_stale_github_io_links.py -v` reports 4 passed, 0 failed.
    - The four collected test ids are `test_readme_documentation_links_point_at_readthedocs`, `test_readme_deep_links_carry_language_version_prefix`, `test_readme_top_level_links_carry_no_version_segment`, and `test_pyproject_documentation_url_points_at_readthedocs`.
    - `grep -c 'github\.io' tests/test_no_stale_github_io_links.py` returns `0` — the retired-host literal is split, so a repo-wide grep does not hit the guard.
    - `grep -rl 'github\.io' --exclude-dir=.git --exclude-dir=.planning . | sort` lists `./CHANGELOG.md` and nothing else.
    - `grep -c 'requests\|urllib\|httpx\|socket' tests/test_no_stale_github_io_links.py` returns `0` — the guard is hermetic.
    - Temporarily reverting one README deep link to the retired host makes `test_readme_documentation_links_point_at_readthedocs` fail; restore the file afterwards. Record the observed failure output in the plan summary — an unexercised guard is an unproven guard.
    - `uv run black --check tests/test_no_stale_github_io_links.py` and `uv run ruff check tests/test_no_stale_github_io_links.py` both pass.
    - `uv run pytest -q -m "not slow"` shows no new failures relative to the pre-task baseline.
  </acceptance_criteria>

  <done>Four hermetic invariants — no retired host, exactly 7 correctly-prefixed deep links, version-less top-level links, and Read the Docs PyPI metadata — run on every `pytest` invocation, and the guard has been demonstrated to actually fail when its subject regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README reader → external documentation host | Every rewritten link sends a reader to a third-party hosting platform |
| PyPI package page → `project.urls.Documentation` | The URL is baked into published release metadata and cannot be corrected without a new release |
| README rendering context → badge image host | GitHub fetches the badge image from `app.readthedocs.org` on every repository page view |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-10 | Spoofing | The 11 rewritten documentation URLs | high | mitigate | Every target host is a first-party Read the Docs project provisioned by this milestone (Phases 29/30.1) under the owner's account, not a shortener or redirector. Each URL is curl-verified 200 in the task's own automated verify, and the whole set is re-verified independently in Plan 05 — two measurements, not one. |
| T-31-11 | Tampering | `project.urls.Documentation` baked into a published release | medium | mitigate | The value is asserted by `tomllib` parse rather than by eye, and three sibling URL fields are asserted byte-unchanged so an over-broad edit cannot ride along. A wrong value here is only correctable by a new release, which is why it gets a parser assertion and not a grep. |
| T-31-12 | Information Disclosure | Badge image fetched from `app.readthedocs.org` on every repo page view | low | accept | Standard badge mechanics, already accepted for the five existing shields.io badges. The host learns a viewer's IP and user agent; no repository or user data crosses. |
| T-31-13 | Denial of Service | Badge host outage renders the README badge broken | low | accept | A broken badge image is cosmetic and self-announcing. The tradeoff is deliberate: D-12 chose a live build-status badge over a permanently-blue static one precisely so the badge can report bad news. |
| T-31-14 | Tampering | A future edit silently reverts the URL rewrite | medium | mitigate | `tests/test_no_stale_github_io_links.py` runs on every `pytest` invocation and in CI, and Task 3 requires demonstrating a real failure before the guard is accepted. Link Check adds a second, network-level detection layer. |
| T-31-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan adds zero package-registry dependencies. The new test module uses only `pathlib`, `re`, and stdlib `tomllib`. |
</threat_model>

<verification>
- `grep -rl 'github\.io' --exclude-dir=.git --exclude-dir=.planning .` lists `./CHANGELOG.md` only.
- Every `typsphinx.readthedocs.io` and `app.readthedocs.org` URL in README.md returns 200 under `curl -L`.
- `tomllib` confirms `project.urls.Documentation` is the Read the Docs root and the three sibling entries are unchanged.
- `uv run pytest tests/test_no_stale_github_io_links.py -v` reports 4 passed, and the guard was demonstrated failing against a deliberately reverted link.
- `uv run pytest -q -m "not slow"` shows no new failures; `black --check` and `ruff check` pass on the new file.
</verification>

<success_criteria>
Every documentation URL this project publishes in its README and its package metadata addresses
Read the Docs and returns a live page over real HTTP, the deliberate two-shape URL split is
encoded as a guarded invariant rather than a convention, and the guard has been shown to fail when
its subject regresses.
</success_criteria>

<output>
Create `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-03-SUMMARY.md` when done.
</output>
