---
phase: 31-published-url-cutover-repo-wide-link-guard
plan: 05
type: execute
wave: 3
depends_on: ["31-02", "31-03", "31-04"]
files_modified:
  - .planning/phases/31-published-url-cutover-repo-wide-link-guard/31-EVIDENCE.md
  - .planning/phases/31-published-url-cutover-repo-wide-link-guard/31-ISSUE-119-REPLY-DRAFT.md
  - .planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md
  - .github/workflows/links.yml
autonomous: true
requirements: [DOC-09, DOC-10, CI-05]

must_haves:
  truths:
    - "A Link Check run against the rewritten tree concludes `success`, and its run URL and commit SHA are recorded — the positive half of the pair whose negative half Plan 01 captured."
    - "That green run and the red negative-control run are the same mechanism at the same settings, so the difference between them is attributable to the URL rewrite and to nothing else."
    - "A fresh repo-wide grep for the retired documentation host returns matches in CHANGELOG.md only — measured at execution time, not carried forward from any earlier tally."
    - "Every documentation URL across README.md, pyproject.toml's Documentation metadata, and .planning/codebase/INTEGRATIONS.md has been fetched over real HTTP in one consolidated pass and returned a live page."
    - "A close-reply draft for Issue #119 exists, is written in English, reports only the delivered fix, and has been checked against the full current issue thread rather than against the thread as it stood at planning time."
    - "Issue #119 is still open, and the close is recorded as a post-merge handoff alongside the milestone's other owed flips rather than left to look like a gap."
    - "The folded todo covering the 7 dead README deep links is annotated as resolved with the resolving commit identified."
    - "`gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '.required_status_checks.contexts'` still lists only ci.yml's jobs, so Link Check remains advisory in fact and not merely in intent."
    - statement: "A Link Check run that is cancelled or superseded mid-flight leaves no repository state behind — the job only reads and writes a job summary, never a commit, tag, issue, or tree file."
      verification: backstop
  prohibitions:
    - "Issue #119 must NOT be closed and no comment may be posted to it — D-15 defers the close to /gsd-complete-milestone and D-16 requires owner review of the draft before posting."
    - "The reply must not narrate the hosting migration, must not announce that the old URLs now 404, and must not re-explain the cause — D-17 limits it to the fulfillment report."
    - "The reply must not be phrased so as to read as blaming the reporter, and it must be terse (outward-facing-artifact convention, PR#98 lesson)."
    - "The todo file must not be moved or deleted inside a worktree — `worktree.cleanup-wave` blocks any branch containing a deletion, with no bypass, and would block this wave's merge."
    - "A red Link Check run must not be resolved by adding `continue-on-error`, by excluding a URL pattern, or by widening the path-exclusion set to cover a genuinely dead link."
    - "No file under typsphinx/ may be modified (milestone invariant #3)."
    - "No version bump and no CHANGELOG entry — both belong to Phase 33."
    - "CHANGELOG.md must not be edited; its historical mention of the retired host stays."
  artifacts:
    - ".planning/phases/31-published-url-cutover-repo-wide-link-guard/31-ISSUE-119-REPLY-DRAFT.md"
  key_links:
    - "Plan 01's red run <-> this plan's green run: the pair is the evidence. If links.yml were tuned between them in a way that changed what it scans, the pair stops being a controlled comparison — so any tuning must be recorded and justified."
    - "This plan's handoff list -> /gsd-complete-milestone. Four items are already owed there from Phases 29 and 30.1; #119's close and the todo file move join them. An unrecorded handoff is a dropped commitment."
---

<objective>
Close the phase: observe Link Check green on the rewritten tree, take the consolidated real-HTTP
measurement that SC#2 asks for, and prepare the Issue #119 close-reply that D-15 hands to milestone
close.

Purpose: SC#1 and SC#3 together ask for a *pair* of observations — the same mechanism red before
the rewrite and green after. Plan 01 took the first; this takes the second, and only now can the
difference be attributed to the rewrite. SC#2's occurrence count must come from a grep run here,
against the merged tree, not from any figure written down earlier in this phase.

Output: the green run transcribed into `31-EVIDENCE.md`, a consolidated URL/status table covering
all three surfaces SC#2 names, `31-ISSUE-119-REPLY-DRAFT.md` awaiting owner review, and an explicit
handoff record for the two items that leave this phase unfinished by design.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-CONTEXT.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-01-SUMMARY.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-02-SUMMARY.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-03-SUMMARY.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-04-SUMMARY.md
</context>

## Environment briefing

**lychee is never run locally (D-08).** The green observation happens by pushing the branch and
reading the GitHub Actions run. `curl` and `gh` are fine locally.

**Worktree execution.** If `test -f .git` succeeds, provision with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and prefix commands with
`uv run`.

**Do not move or delete any file.** `worktree.cleanup-wave` blocks any branch containing a
deletion, with no bypass (STATE.md; PROJECT.md D-13 precedent). A `git mv` of the todo file into
`completed/` registers as a deletion and would block this wave's merge. Task 3 annotates the todo
in place instead and hands the physical move to milestone close.

**links.yml is Plan 01's artifact.** Touch it here only if Task 1's run is red for a transient
reason and D-06's lenient posture needs tuning. Any edit must be recorded with the run that
motivated it.

## Artifacts this phase produces

Symbols and paths created by Phase 31 (this plan's share marked ✱):

| Artifact | Kind | Plan |
|---|---|---|
| `.github/workflows/links.yml` | new file | 01 |
| workflow name `Link Check` / job id `link-check` | CI identifiers | 01 |
| ✱ `.planning/phases/31-.../31-EVIDENCE.md` | new file | 01, 05 |
| `.planning/phases/31-.../31-ABOUT-EVIDENCE.md` | new file | 02 |
| repository field `homepage` (GitHub metadata) | external state | 02 |
| `tests/test_no_stale_github_io_links.py` | new file | 03 |
| `test_readme_documentation_links_point_at_readthedocs` | new test function | 03 |
| `test_readme_deep_links_carry_language_version_prefix` | new test function | 03 |
| `test_readme_top_level_links_carry_no_version_segment` | new test function | 03 |
| `test_pyproject_documentation_url_points_at_readthedocs` | new test function | 03 |
| `REPO_ROOT` / `README_PATH` / `PYPROJECT_PATH` | new module constants | 03 |
| ✱ `.planning/phases/31-.../31-ISSUE-119-REPLY-DRAFT.md` | new file | 05 |

## Flagged assumption (edge probe, unresolved)

**DOC-10 / unclassified** remains unresolved, carried forward here as the phase's one explicit
assumption. D-15 splits DOC-10 across the phase boundary on purpose: this phase delivers "About
set + resolving + close-reply drafted", and the close is a post-merge handoff. No defensible
acceptance criterion for the close half exists inside Phase 31's window because that half is
deliberately outside it. Task 3's handoff record is what keeps the split visible; verification must
read DOC-10 as owner-decided-split, not as a gap.

<tasks>

<task type="auto">
  <name>Task 1: Observe Link Check green on the rewritten tree and complete the evidence pair</name>

  <files>.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-EVIDENCE.md, .github/workflows/links.yml</files>

  <read_first>
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-EVIDENCE.md` — the negative-control record from Plan 01, including the empty section reserved for this run. Read the flagged-URL list so you can state precisely which entries disappeared and why.
    - `.github/workflows/links.yml` — the settings the negative control ran under. The green run must run under the same settings for the pair to mean anything.
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-CONTEXT.md` — D-06 (lenient posture, what may be tuned), D-08 (CI-only execution), D-09 (what the transcription must contain).
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-03-SUMMARY.md` and `31-04-SUMMARY.md` — what actually changed in Wave 2, so the attribution claim is grounded.
  </read_first>

  <action>
Push and observe, the same way Plan 01 did.

Push this branch with `git push -u origin HEAD`. Do not push to or force-push the phase branch
`gsd/v0.6.4-read-the-docs-migration`. Find the run with
`gh run list --workflow=links.yml --branch "$(git rev-parse --abbrev-ref HEAD)"`, watch it, and read
the conclusion out of `gh run view --json conclusion` rather than relying on `gh run watch`'s exit
status.

The expected conclusion is `success` — the mirror image of Plan 01, where red was the success
condition. Before recording it, read the run log and confirm the same four structural facts the
negative control established, because a green run that scanned nothing is worthless: the lychee step
produced a link summary rather than an argument error; `pyproject.toml` is among the scanned files;
no `.planning/` file, no `CHANGELOG.md`, and no `tests/fixtures/` file was scanned; and the total
link count is in the same order of magnitude as the negative control's. A sharp drop in total links
between the two runs means the scan surface changed, which would invalidate the comparison.

If the run is red, read the failure list and triage each entry before touching anything:

- **A transient** — 429, a timeout, a host that answered on retry — is tuned away inside D-06's
  lenient posture by adjusting `--max-retries`, `--retry-wait-time`, `--timeout`, or the `--accept`
  set in `links.yml`, then re-pushing. That push-observe loop is the sanctioned tuning method under
  D-08. Record each iteration with its run URL; do not retry silently until green.
- **A genuinely dead link** must be fixed at the source. Do not widen the path-exclusion set to
  cover it, do not add a URL-pattern exclusion, and do not add `continue-on-error`. All three
  produce a green job that has stopped detecting, which is the defect CI-05 exists to prevent.

Append to `31-EVIDENCE.md`, in the section Plan 01 reserved: the run URL, the conclusion, the
commit SHA the run evaluated, the lychee summary counts, and a short side-by-side against the
negative control — total links then and now, errors then and now, and the specific URLs that moved
from failing to passing. State plainly that both runs used the same `links.yml` settings, or, if
tuning was needed, exactly what changed and why that change cannot have masked a real failure.
  </action>

  <verify>
    <automated>bash -c 'set -e; B=$(git rev-parse --abbrev-ref HEAD); ID=$(gh run list --workflow=links.yml --branch "$B" --limit 1 --json databaseId --jq ".[0].databaseId"); test -n "$ID"; C=$(gh run view "$ID" --json conclusion --jq .conclusion); echo "run $ID conclusion=$C"; test "$C" = "success"; F=.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-EVIDENCE.md; grep -q "$ID" "$F"; echo "green run observed and transcribed"'</automated>
  </verify>

  <acceptance_criteria>
    - `gh run list --workflow=links.yml --branch "$(git rev-parse --abbrev-ref HEAD)" --limit 1 --json conclusion --jq '.[0].conclusion'` returns `success`.
    - The run log contains a lychee link summary with a zero error count and a non-zero total.
    - The green run's total link count is within the same order of magnitude as the negative control's — recorded as an explicit then/now comparison, so a collapsed scan surface cannot pass as a fix.
    - The run log shows `pyproject.toml` among the scanned files, and shows no scanned file under `.planning/`, no `CHANGELOG.md`, and no file under `tests/fixtures/`.
    - `31-EVIDENCE.md` now contains both runs: the red one with its 7 flagged deep links and the green one with zero errors, each with run URL, conclusion, and commit SHA.
    - `31-EVIDENCE.md` states whether `links.yml` changed between the two runs, and if it did, what changed and why the change cannot mask a real failure.
    - `grep -v '^\s*#' .github/workflows/links.yml | grep -c 'continue-on-error'` returns `0`.
    - `grep -v '^\s*#' .github/workflows/links.yml | grep -oE '[-]-exclude[a-z-]*' | grep -cx -- '--exclude'` returns `0`.
    - `grep -c 'scheme' .github/workflows/links.yml` returns `2` — the D-07 scheme restriction was not dropped while tuning.
    - `grep -c 'exclude-path' .github/workflows/links.yml` returns `3` — the exclusion set was not widened to hide a failure.
    - `gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '.required_status_checks.contexts'` output does not contain `link`.
  </acceptance_criteria>

  <done>The red/green pair is complete and recorded, both halves ran under the same settings, and the green half is provably green because the links resolve rather than because the scan stopped looking.</done>
</task>

<task type="auto">
  <name>Task 2: Take the consolidated fresh-grep and real-HTTP measurement for SC#2</name>

  <files>.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-EVIDENCE.md</files>

  <read_first>
    - `README.md` — as rewritten by Plan 03.
    - `pyproject.toml` `[project.urls]` — as rewritten by Plan 03.
    - `.planning/codebase/INTEGRATIONS.md` — as refreshed by Plan 04.
    - `.planning/REQUIREMENTS.md` § Milestone Invariants — invariant #4, which is why this measurement is taken here and not inherited from an earlier plan.
    - `.planning/STATE.md` § Accumulated Context — the recurring scoping lesson: a criterion phrased "anywhere under X" is checked by a repo-wide grep, never by grepping only the named files. Phase 27 and the v0.6.3 close were both bitten by the narrow read.
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-02-SUMMARY.md` — the About-field measurement, so this consolidated pass can reference rather than duplicate it.
  </read_first>

  <action>
Take the measurement fresh. Nothing in this task may cite a count written earlier in this phase —
the whole point of invariant #4 is that a remembered number is not evidence. Two of this project's
past misses came from grepping only the files a requirement named, so grep the tree, then narrow.

**Step 1 — repo-wide grep.** Run a recursive grep for the retired documentation host across the
whole tree with `.git` and `.planning` excluded, and record the verbatim output. The expected result
is matches in `CHANGELOG.md` only — deliberately preserved as a historical record (D-03, Phase 24
D-02 precedent). Anything else is a miss. Anchor for drift detection: before the rewrite there were
10 matching lines in `README.md` carrying 11 string occurrences, plus 1 in `CHANGELOG.md`; the
regression guard from Plan 03 splits its own literal so it does not appear.

<!-- planner-discipline-allow: github.io -->

Also grep for the retired `Documentation` value shape in `pyproject.toml` — the README-anchor form,
not a host — and confirm zero. It is invisible to a host-based grep, which is exactly how it
survived, so it needs its own check.

<!-- planner-discipline-allow: typsphinx#readme -->

**Step 2 — enumerate, then fetch.** Extract every distinct `http(s)` URL from the three surfaces
SC#2 names — `README.md`, `pyproject.toml`, `.planning/codebase/INTEGRATIONS.md` — with
`grep -oE`, deduplicate, and fetch each with
`curl -s -o /dev/null -w "%{http_code} %{url_effective}" -L`. Do not fetch a curated subset and do
not fetch only the URLs this phase changed: SC#2's bar is *every* documentation URL on those
surfaces, and a link this phase did not touch can be just as dead. Planning-time measurement of the
whole in-scope repository found 48 distinct URLs with exactly three failure classes, all of which
Plans 01 and 03 addressed; a fourth failure appearing here is a finding, not noise.

Every URL must return 200. If one does not, fix it at the source and re-measure — this is the
consolidated pass, so a recorded failure here is a shipped dead link.

**Step 3 — record.** Append to `31-EVIDENCE.md`: the verbatim grep commands and output, the full
URL/status/effective-URL table grouped by source file, the distinct-URL count, and a UTC timestamp.
Follow the Phase 29 D-15 convention — verbatim command plus verbatim output, never a prose summary.
Add a one-line statement that `CHANGELOG.md`'s surviving mention is intentional, so a later reader
does not file it as an oversight.

Cross-reference rather than duplicate Plan 02's About-field measurement and Plan 04's
INTEGRATIONS.md sweep: name their evidence files and state that this pass re-measured the same URLs
independently. Two independent readings of the same URL is corroboration; one reading copied twice
is not.
  </action>

  <verify>
    <automated>bash -c 'set -e; test $(grep -rl "github\.io" --exclude-dir=.git --exclude-dir=.planning . | grep -vc "^./CHANGELOG.md$") -eq 0; test $(grep -v "^\s*#" pyproject.toml | grep -c "typsphinx#readme") -eq 0; N=0; for u in $(grep -ohE "https?://[^ )>\"'\''\`,;]+" README.md pyproject.toml .planning/codebase/INTEGRATIONS.md | sed -E "s/[.,)]+$//" | sort -u); do c=$(curl -s -o /dev/null -w "%{http_code}" -L --max-time 25 "$u"); echo "$c $u"; [ "$c" = "200" ] || exit 1; N=$((N+1)); done; echo "$N distinct URLs verified 200 across README/pyproject/INTEGRATIONS"; grep -q "$(date -u +%Y-%m-%d)" .planning/phases/31-published-url-cutover-repo-wide-link-guard/31-EVIDENCE.md'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rl 'github\.io' --exclude-dir=.git --exclude-dir=.planning . | sort` outputs `./CHANGELOG.md` and nothing else.
    - `grep -v '^\s*#' pyproject.toml | grep -c 'typsphinx#readme'` returns `0`.
    - Every distinct URL extracted from `README.md`, `pyproject.toml`, and `.planning/codebase/INTEGRATIONS.md` returns HTTP `200` under `curl -L`, with zero exceptions.
    - `31-EVIDENCE.md` contains the verbatim grep commands and their output, not a paraphrase.
    - `31-EVIDENCE.md` contains the URL/status/effective-URL table grouped by source file, the distinct-URL count, and today's UTC date.
    - `31-EVIDENCE.md` states that `CHANGELOG.md`'s surviving mention is intentional and cites the decision.
    - `31-EVIDENCE.md` names `31-ABOUT-EVIDENCE.md` and `31-04-SUMMARY.md` and states that their URLs were re-measured independently here.
    - `uv run pytest -q -m "not slow"` passes — the tree this measurement describes is also a green tree.
  </acceptance_criteria>

  <done>SC#2 is discharged by a grep and a fetch run against the merged tree at this moment, covering all three named surfaces plus the whole repository, with every count and every status code traceable to a verbatim transcript.</done>
</task>

<task type="auto">
  <name>Task 3: Draft the Issue #119 close reply and record the post-merge handoffs</name>

  <files>.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-ISSUE-119-REPLY-DRAFT.md, .planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md</files>

  <read_first>
    - Issue #119's full current thread — fetch it live with `gh issue view 119 --json body,comments,state`. Read the whole thread, not a summary of it. The owner's existing reply already promises to fix the repository Website link and the README deep links, so the draft is a fulfillment report against a promise already made, not a fresh explanation.
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-CONTEXT.md` — D-15 (close deferred to milestone close), D-16 (draft, owner review, then post), D-17 (fulfillment report only).
    - `.planning/STATE.md` § Accumulated Context — the PR#98 lesson: read the whole thread before commenting on someone else's report, and answer any unanswered question before anything else. Also the four post-merge flips already owed to milestone close.
    - `.planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md` — the folded todo this phase resolves.
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` § "Phase 33 Handoff Precondition" — the existing handoff list this plan's items join.
  </read_first>

  <action>
Write the draft, then record what leaves the phase unfinished.

**Re-read the thread live before drafting.** Planning-time reading (2026-07-26): state OPEN, one
comment from the owner, no unanswered question from the reporter. If a new comment has appeared — in
particular any question — the reply answers it first. That ordering is the PR#98 lesson: a
thank-you posted over an open question reads as not having read the thread.

**Draft content (D-17).** The new documentation URL, and the fact that the repository's Website
field and the README links are now fixed. That is all. Do not narrate the hosting migration, do not
announce that the old URLs now return 404, and do not re-explain the cause — the owner's existing
reply already covered the cause, and repeating it makes the fulfillment report longer than the fix.

**Register (D-16, outward-facing convention).** English. Terse. Thank the reporter once, briefly.
Phrase nothing so that it could read as the reporter's mistake. Keep the explanation thin — a
reader wants the working link, not the reasoning.

Include in the draft file, above the reply text, a short block the owner can check at a glance: the
issue state at drafting time, the comment count, whether a new question appeared, and the URLs the
reply cites with their measured status codes from Task 2. Mark the reply text itself unmistakably as
awaiting owner review. Do not post it and do not close the issue; both are milestone-close work.

**Annotate the folded todo in place.** CONTEXT.md says to close
`2026-07-22-github-io-doc-links-404-missing-en-prefix` when the rewrite commit lands, and it landed
in Plan 03. Record the resolution *inside* the existing pending file: a status line marking it
resolved, the resolving commit SHA, the phase and plan that resolved it, and a note that the
physical move into `.planning/todos/completed/` is deferred to milestone close.

Do not `git mv` or `git rm` the file. `worktree.cleanup-wave` blocks any branch containing a
deletion with no bypass, and a rename registers as one — it would block this wave's merge. The
annotation is the substantive close; the file move is bookkeeping that costs nothing to defer and
is cheap to do in the main tree.

**Record the handoffs.** In the plan summary, under a `## Handoffs` heading, list the two items this
phase deliberately leaves for `/gsd-complete-milestone`, each with its reason and its decision
reference: closing Issue #119 after the README rewrite is visible on `main` (D-15), and moving the
resolved todo file into `completed/` (deletion-guard avoidance). Note that these join the four
already owed from Phases 29 and 30.1 — the two RTD Default-branch reverts to `main`, the
`.gitmodules` branch flip, and the Default Version flip from `latest` to `stable` — so milestone
close sees one list rather than six scattered notes.
  </action>

  <verify>
    <automated>bash -c 'set -e; D=.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-ISSUE-119-REPLY-DRAFT.md; test -f "$D"; grep -q "typsphinx.readthedocs.io" "$D"; grep -qi "awaiting owner review" "$D"; test $(grep -ci "github\.io" "$D") -eq 0; S=$(gh issue view 119 --json state --jq .state); echo "issue 119 state=$S"; test "$S" = "OPEN"; T=.planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md; test -f "$T"; grep -qi "resolved" "$T"; echo "draft prepared, issue still open, todo annotated in place"'</automated>
  </verify>

  <acceptance_criteria>
    - `31-ISSUE-119-REPLY-DRAFT.md` exists and contains the Read the Docs URL the reply cites.
    - The draft is marked as awaiting owner review, using text containing `awaiting owner review`.
    - The draft's reply text is English throughout.
    - The draft contains no mention of the retired host and no statement that the old URLs return 404 — D-17's exclusions hold.
    - The draft's reply text is at most 6 lines of prose — terseness is a stated requirement, so it is checked, not hoped for.
    - The draft records the issue state and comment count at drafting time, and states explicitly whether a new question appeared since planning.
    - `gh issue view 119 --json state --jq .state` returns `OPEN`.
    - `gh issue view 119 --json comments --jq '.comments | length'` returns the same count recorded in the draft — nothing was posted.
    - `.planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md` still exists at that path and now carries a resolved status line naming the resolving commit SHA, the phase, and the plan.
    - `git diff --diff-filter=D --name-only HEAD~1` is empty — no deletion was introduced, so the wave merge is not blocked.
    - `31-05-SUMMARY.md` contains a `## Handoffs` section listing both of this phase's deferred items with their decision references, and noting the four already owed from Phases 29 and 30.1.
  </acceptance_criteria>

  <done>The reply the owner will post is written, checked against the live thread, and held for review; the resolved todo is annotated where it lives; and every commitment this phase does not itself discharge is recorded in one place for milestone close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local `gh` CLI → a third party's public issue thread | A reply drafted here will be posted under the owner's identity to an external reporter |
| CI runner → arbitrary external hosts | The green Link Check run performs the same real-HTTP fan-out as the negative control |
| This phase → `/gsd-complete-milestone` | Two commitments cross the boundary as handoffs and are discharged by a different workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-19 | Repudiation | A green Link Check run treated as proof without checking what it scanned | high | mitigate | The green run's total link count is compared explicitly against the negative control's, and the same four structural facts are re-confirmed from the log. A green run whose scan surface collapsed fails the acceptance criteria rather than passing as a fix. |
| T-31-20 | Tampering | Resolving a red run by weakening the detector | high | mitigate | Three specific weakenings are named as prohibitions and each is enforced by a grep criterion: no `continue-on-error`, no bare URL-exclusion flag, and the path-exclusion count asserted to remain exactly 3. Tuning is confined to the retry, timeout, and accepted-status knobs D-06 sanctions, and every iteration must be recorded with its run URL. |
| T-31-21 | Information Disclosure | A public reply to an external reporter | medium | mitigate | D-17 confines the content to the fulfillment report; the draft is asserted to exclude the retired host and the old-URL-404 fact. Owner review precedes posting (D-16), so nothing reaches the thread unreviewed. |
| T-31-22 | Repudiation | A deferred commitment lost at the phase boundary | medium | mitigate | Both deferred items are written into a `## Handoffs` section alongside the four already owed from Phases 29 and 30.1, so milestone close reads one list. The handoff section's presence is an acceptance criterion, not a convention. |
| T-31-23 | Denial of Service | The wave merge blocked by an avoidable deletion | medium | mitigate | The todo is annotated in place rather than moved, and `git diff --diff-filter=D --name-only` is asserted empty. This sidesteps the known no-bypass deletion guard instead of discovering it at merge time. |
| T-31-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing. It runs `gh`, `curl`, `grep`, and the existing pytest suite only. |
</threat_model>

<verification>
- A Link Check run on the rewritten tree concludes `success`, with a zero error count and a total link count comparable to the negative control's, both runs recorded in `31-EVIDENCE.md` with URLs and SHAs.
- `links.yml` still carries no `continue-on-error`, no bare URL-exclusion flag, and exactly 3 path exclusions.
- A repo-wide grep for the retired host returns `./CHANGELOG.md` only.
- Every distinct URL across README.md, pyproject.toml, and INTEGRATIONS.md returns 200 in one consolidated pass, transcribed verbatim.
- `uv run pytest -q -m "not slow"` passes.
- Issue #119 is `OPEN` with an unchanged comment count; the draft exists, is English, is at most 6 lines, and is marked awaiting owner review.
- The folded todo is annotated in place; `git diff --diff-filter=D --name-only HEAD~1` is empty.
- `31-05-SUMMARY.md` carries a `## Handoffs` section with both deferred items and the four pre-existing ones.
</verification>

<success_criteria>
The mechanism CI-05 asked for is demonstrably green on the tree where every published documentation
URL now resolves, the pair of CI runs makes that claim falsifiable rather than asserted, SC#2's
counts and status codes all come from commands run against the merged tree, and the two parts of
DOC-10 that D-15 moved past the phase boundary leave with an explicit handoff instead of looking
like gaps.
</success_criteria>

<output>
Create `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-05-SUMMARY.md` when done,
including the `## Handoffs` section described in Task 3.
</output>
