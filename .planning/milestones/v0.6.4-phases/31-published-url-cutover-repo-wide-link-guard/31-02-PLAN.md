---
phase: 31-published-url-cutover-repo-wide-link-guard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-published-url-cutover-repo-wide-link-guard/31-ABOUT-EVIDENCE.md
autonomous: true
requirements: [DOC-10]

user_setup:
  - service: github-repository-settings
    why: "The repository's About -> Website field is the exact link the reporter of Issue #119 clicked and got a 404 from (D-14). It is repository metadata, not repository content, so no commit can set it."
    dashboard_config:
      - task: "Set About -> Website to https://typsphinx.readthedocs.io/"
        location: "GitHub -> YuSabo90002/typsphinx -> About (gear icon, top right of the repo page) -> Website"
        note: "FALLBACK ONLY. Measured 2026-07-26: the local gh token holds the `repo` scope and the account has `admin: true` on this repository, so `gh api repos/YuSabo90002/typsphinx -X PATCH -f homepage=...` sets this field without owner action. Perform this manually only if that call is refused."

must_haves:
  truths:
    - "`gh api repos/YuSabo90002/typsphinx --jq .homepage` returns `https://typsphinx.readthedocs.io/` — the field is no longer null."
    - "That exact value, fetched over real HTTP with curl following redirects, returns 200."
    - "A visitor landing on the repository page and clicking the Website link in the right-hand About panel reaches Read the Docs rather than a 404 — the precise action Issue #119 reported failing."
    - "The value carries no version segment, so Phase 33's Default Version flip from latest to stable propagates to it with no further edit."
  prohibitions:
    - "Issue #119 must NOT be closed in this phase — D-15 defers the close to /gsd-complete-milestone, after the README rewrite is visible on main."
    - "No comment may be posted to Issue #119 in this phase — D-16 requires draft, then owner review, then post."
    - "The Website value must not be a version-pinned URL such as an /en/latest/ form — D-11 requires the bare root precisely so it follows the Default Version."
    - "No file under typsphinx/ may be modified (milestone invariant #3)."
    - "Repository fields other than `homepage` must not be modified by the API call — no description, topics, visibility, or feature-toggle changes."
  artifacts:
    - ".planning/phases/31-published-url-cutover-repo-wide-link-guard/31-ABOUT-EVIDENCE.md"
  key_links:
    - "Repository `homepage` field -> the About panel's Website link -> RTD's root redirect -> the Default Version. Three hops, and the reported bug lived in the last one; the bare-root form is what keeps hop three self-updating."
---

<objective>
Set the repository's About → Website field to the Read the Docs root and prove over real HTTP that
the link a visitor clicks now resolves.

Purpose: this single field is what put101 clicked in Issue #119 and got a 404 from. D-14 puts it
first in the phase because it is independent of the README rewrite and repairs the reported symptom
the moment it is set — repository metadata takes effect immediately for every visitor, without
waiting for the milestone branch to merge.

Output: a non-null `homepage` on the repository and `31-ABOUT-EVIDENCE.md` carrying the read-back
and the curl transcript that satisfies ROADMAP criterion 4's second half.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-CONTEXT.md
@.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-RESEARCH.md
</context>

## Environment briefing

**Worktree execution.** If `test -f .git` succeeds you are in an isolated worktree; provision with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and prefix commands with
`uv run`. This plan runs no Python, but the rule stands.

**This plan changes GitHub state, not repository content.** Its only committed artifact is an
evidence file. Do not add repository-content changes here — the README and `pyproject.toml`
rewrites belong to Plan 03 and would collide.

**lychee is never run locally (D-08).** `curl` and `gh` are fine.

## Artifacts this phase produces

Symbols and paths created by Phase 31 (this plan's share marked ✱):

| Artifact | Kind | Plan |
|---|---|---|
| `.github/workflows/links.yml` | new file | 01 |
| workflow name `Link Check` / job id `link-check` | CI identifiers | 01 |
| `.planning/phases/31-.../31-EVIDENCE.md` | new file | 01, 05 |
| ✱ `.planning/phases/31-.../31-ABOUT-EVIDENCE.md` | new file | 02 |
| ✱ repository field `homepage` (GitHub metadata, not a repo file) | external state | 02 |
| `tests/test_no_stale_github_io_links.py` | new file | 03 |
| `test_readme_documentation_links_point_at_readthedocs` | new test function | 03 |
| `test_readme_deep_links_carry_language_version_prefix` | new test function | 03 |
| `test_readme_top_level_links_carry_no_version_segment` | new test function | 03 |
| `test_pyproject_documentation_url_points_at_readthedocs` | new test function | 03 |
| `REPO_ROOT` / `README_PATH` / `PYPROJECT_PATH` | new module constants | 03 |
| `.planning/phases/31-.../31-ISSUE-119-REPLY-DRAFT.md` | new file | 05 |

## Flagged assumption (edge probe, unresolved)

**DOC-10 / unclassified** remains unresolved and is carried forward rather than dropped. D-15
deliberately splits DOC-10 across the phase boundary: Phase 31 delivers "About set + resolving +
close-reply drafted"; the issue close itself is a post-merge handoff to
`/gsd-complete-milestone`. No defensible acceptance criterion for the close half can be written
inside Phase 31's window, because that half is intentionally outside it. Verification must treat
this as an owner-decided split, not a gap.

<tasks>

<task type="auto">
  <name>Task 1: Set the repository Website field to the Read the Docs root</name>

  <files>(no repository files — this task writes GitHub repository metadata)</files>

  <read_first>
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-CONTEXT.md` — D-11 (why the bare root and not a version-pinned URL) and D-14 (why this is set now rather than after the rewrite).
    - `.planning/ROADMAP.md` § Phase 31 — the "Owner-manual dependency" note and success criterion 4.
    - `pyproject.toml` `[project.urls]` — confirms `Homepage` stays pointed at GitHub; the Website field is the *documentation* link, a different thing from the package Homepage. Do not conflate them.
  </read_first>

  <action>
Record the before-state, then set the field, then read it back.

Capture the current value first with `gh api repos/YuSabo90002/typsphinx --jq '.homepage'`.
Measured at planning time on 2026-07-26 it was `null` — an unset field, which is why GitHub's
About panel fell back to the stale Pages URL that Issue #119 reported. Capture whatever it actually
is at execution time; do not assume the planning-time reading still holds.

Set it to exactly `https://typsphinx.readthedocs.io/` — bare root, trailing slash, no language
segment and no version segment. D-11 requires this form because Read the Docs' root redirects to
whatever the project's Default Version is, so Phase 33's `latest` → `stable` flip propagates here
automatically with zero re-editing. A version-pinned form would silently freeze this link on
`latest` forever.

Use `gh api repos/YuSabo90002/typsphinx -X PATCH -f homepage='https://typsphinx.readthedocs.io/'`.
Measured at planning time: the local token carries the `repo` scope and the account has
`admin: true` on this repository, so this call is expected to succeed. Send only the `homepage`
field — a PATCH carrying extra fields would rewrite repository settings this phase has no business
touching.

If the call is refused for scope or permission reasons, do not work around it and do not proceed
as though the field were set. Surface the refusal, and have the owner set About → Website to the
same value through the repository page's About gear icon; then continue to the read-back below.
CONTEXT.md D-14 anticipates the manual path; the API attempt simply removes the owner's need to
perform it when the token allows.

Read the value back with `gh api repos/YuSabo90002/typsphinx --jq '.homepage'` and confirm it
matches the intended string character for character, including the trailing slash.
  </action>

  <verify>
    <automated>bash -c 'set -e; V=$(gh api repos/YuSabo90002/typsphinx --jq ".homepage"); echo "homepage=$V"; test "$V" = "https://typsphinx.readthedocs.io/"; echo "About -> Website set correctly"'</automated>
  </verify>

  <acceptance_criteria>
    - `gh api repos/YuSabo90002/typsphinx --jq '.homepage'` returns exactly `https://typsphinx.readthedocs.io/`.
    - The returned value is non-null — the empty/unset case the field started in is closed.
    - The returned value contains no `/en/` segment and no `/latest/` segment.
    - `gh api repos/YuSabo90002/typsphinx --jq '{description,private,archived,has_issues}'` is unchanged from its pre-task reading — the PATCH touched only `homepage`.
    - No file in the working tree was modified by this task: `git status --porcelain` shows no change attributable to it.
  </acceptance_criteria>

  <done>The repository's About → Website field holds the Read the Docs bare root and reads back byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the Website link over real HTTP and record the evidence</name>

  <files>.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-ABOUT-EVIDENCE.md</files>

  <read_first>
    - `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-RESEARCH.md` § Code Examples — the exact curl invocation shape this project uses for RTD URL verification (`curl -s -o /dev/null -w "%{http_code}\n" -L <url>`).
    - `.planning/ROADMAP.md` § Phase 31 success criterion 4 — the second half ("resolves to the RTD root over real HTTP") is what this task discharges.
    - `.planning/STATE.md` § Accumulated Context — the honest-verifier rule (GATE-01): abstain rather than assert a truth without direct evidence.
  </read_first>

  <action>
Fetch the value the repository now advertises and record the transcript.

Do not curl a hardcoded string. Read the URL back out of the API
(`gh api repos/YuSabo90002/typsphinx --jq '.homepage'`) and fetch *that*, so the evidence proves
the link a visitor actually clicks resolves — not that a string you typed happens to resolve.

Fetch it twice: once following redirects to capture the final status and the landing URL
(`-L` plus `%{http_code}` and `%{url_effective}`), and once without following redirects to capture
the first-hop status and `Location` header. The two together document the full chain — Read the
Docs answers the bare root with a redirect to the Default Version, and criterion 4 is about a
visitor reaching a live page, so the redirect chain is part of the evidence rather than noise.

Also fetch `https://typsphinx.readthedocs.io/ja/latest/` in the same pass. It is not the Website
value, but it is the other half of the two-language site the About link fronts, and Plan 05's
sweep will want a second independent reading of it.

Write `31-ABOUT-EVIDENCE.md` containing: the before-value of `homepage`, the exact `gh api` PATCH
command issued (or a note that the owner set it manually and why), the read-back value, the two
curl invocations verbatim with their full output, the effective landing URL after redirects, and
the UTC timestamp of the measurement. Follow the Phase 29 D-15 convention — verbatim command plus
verbatim output, not a prose summary of what was observed.

Close the file with a short section stating explicitly what this plan does NOT discharge: Issue
#119 remains open by design (D-15), and the close-reply draft is Plan 05's deliverable. This keeps
the phase's verifier from reading a half-delivered DOC-10 as a gap.
  </action>

  <verify>
    <automated>bash -c 'set -e; U=$(gh api repos/YuSabo90002/typsphinx --jq ".homepage"); C=$(curl -s -o /dev/null -w "%{http_code}" -L --max-time 20 "$U"); E=$(curl -s -o /dev/null -w "%{url_effective}" -L --max-time 20 "$U"); echo "$C $U -> $E"; test "$C" = "200"; J=$(curl -s -o /dev/null -w "%{http_code}" -L --max-time 20 https://typsphinx.readthedocs.io/ja/latest/); echo "ja $J"; test "$J" = "200"; F=.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-ABOUT-EVIDENCE.md; test -f "$F"; grep -q "200" "$F"; echo "About link verified over real HTTP"'</automated>
  </verify>

  <acceptance_criteria>
    - Curling the value read back from the API, with redirects followed, returns HTTP `200`.
    - The recorded `url_effective` is a Read the Docs URL under `typsphinx.readthedocs.io`, not a GitHub Pages URL.
    - The no-redirect fetch's `Location` header is recorded, documenting the root-to-Default-Version hop.
    - `https://typsphinx.readthedocs.io/ja/latest/` returns `200` in the same measurement pass.
    - `31-ABOUT-EVIDENCE.md` contains the before-value, the command issued, the read-back value, both curl invocations verbatim with their outputs, and a UTC timestamp.
    - `31-ABOUT-EVIDENCE.md` states that Issue #119 remains open by D-15 design and names Plan 05 as the owner of the reply draft.
    - `gh issue view 119 --json state --jq .state` still returns `OPEN` — this plan did not close it.
    - `gh issue view 119 --json comments --jq '.comments | length'` is unchanged from its pre-task value — this plan posted no comment.
  </acceptance_criteria>

  <done>Criterion 4's second half is discharged with a real-HTTP transcript taken against the value the repository actually advertises, and the deliberately-deferred half of DOC-10 is documented in place so it cannot be misread as a gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local `gh` CLI → GitHub repository settings API | An authenticated, admin-scoped write to repository metadata |
| Public visitor → repository About panel → external documentation host | The link this plan sets is followed by untrusted third parties |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-06 | Tampering | `gh api repos/... -X PATCH` on repository settings | medium | mitigate | Send exactly one field (`homepage`) with a fixed literal value verified 200 at planning time. Read the value back and assert byte equality. Assert `description`/`private`/`archived`/`has_issues` are unchanged, so an over-broad PATCH cannot pass silently. |
| T-31-07 | Spoofing | The URL published in the About panel is followed by third parties | medium | mitigate | The value is a first-party Read the Docs project this milestone provisioned (Phases 29/30.1), not a redirector or shortener. It is curl-verified end to end, and the recorded `url_effective` is asserted to stay on `typsphinx.readthedocs.io` — a hijacked redirect would surface as an off-host landing URL. |
| T-31-08 | Repudiation | An owner-manual fallback path leaves no record of who set the field | low | mitigate | The evidence file records the before-value, the exact command issued, and — when the manual path is taken — that fact plus its reason, so the change is attributable either way. |
| T-31-09 | Information Disclosure | `gh` token with `repo` scope used for a settings write | low | accept | The token is the developer's existing local credential, already used throughout this milestone for `gh api` and `gh issue` reads. This plan introduces no new secret, no new storage, and no CI exposure. |
| T-31-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan adds zero package-registry dependencies and runs no installer. Nothing to audit. |
</threat_model>

<verification>
- `gh api repos/YuSabo90002/typsphinx --jq '.homepage'` returns the Read the Docs bare root.
- Curling that read-back value returns 200 and lands on `typsphinx.readthedocs.io`.
- The Japanese documentation root returns 200 in the same pass.
- `31-ABOUT-EVIDENCE.md` carries verbatim commands and outputs plus a UTC timestamp.
- Issue #119 is still `OPEN` with its comment count unchanged.
</verification>

<success_criteria>
The link a visitor clicks in the repository's About panel resolves to a live documentation page,
proven by fetching the value the API actually reports rather than a string typed into a plan — and
the half of DOC-10 that D-15 deliberately moved past the phase boundary is documented as deferred
rather than left to look missing.
</success_criteria>

<output>
Create `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-02-SUMMARY.md` when done.
</output>
