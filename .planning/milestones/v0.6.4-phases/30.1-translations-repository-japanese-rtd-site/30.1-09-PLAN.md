---
phase: 30.1-translations-repository-japanese-rtd-site
plan: 09
type: execute
wave: 8
depends_on: ["30.1-08"]
files_modified:
  - .planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md
  - .planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml
autonomous: true
gap_closure: true
requirements: [I18N-01]
must_haves:
  truths:
    - statement: "A run of the `Update translation source pin` workflow is triggered and observed to completion — not assumed, not left polling — with its run URL, conclusion, duration and per-step outcome list recorded, and with the `decide` step's `pin_moved` and `po_changed` outputs read from the run's own log rather than inferred"
      verification: backstop
    - statement: "The run pushes a commit to `typsphinx-doc-translations` whose diff MOVES the `typsphinx` gitlink: the record exhibits the actual hunk showing one `Subproject commit` SHA replaced by a different one, with the before value `dcc6a523fc2d16f3dae925e17d63a1a24318b6d7` and the after value equal to `origin/gsd/v0.6.4-read-the-docs-migration`'s tip at trigger time"
      verification: backstop
    - statement: "A run that changed only `POT-Creation-Date` headers is explicitly distinguished from a pin move and is recorded as NOT satisfying SC#3, per the criterion's own bar — the distinction is made by exhibiting the gitlink hunk, not by reading the commit message"
      verification: backstop
    - statement: "The ja Read the Docs project is observed rebuilding from the new translations-repository commit: a build whose `commit` field equals the pushed SHA reaches a finished state, and its raw log's submodule line is read and compared against the new gitlink value"
      verification: backstop
    - statement: "A real HTTP GET of https://typsphinx.readthedocs.io/ja/latest/contributing.html returns 200 and its served body contains BOTH the Plan 05 Translations sentence and the WR-01 `--recurse-submodules` correction — two independent probes for the same propagation, so a single cached or partial response cannot carry the claim alone"
      verification: backstop
    - "The record states explicitly that the English text on `/ja/latest/contributing.html` is correct Sphinx fallback behaviour for a 0/97-coverage catalog and is not a language failure — this page is SC#3's visible-delta probe, never an SC#1 language probe"
    - "A regression re-fetch of https://typsphinx.readthedocs.io/ja/latest/user_guide/builders.html confirms the rebuilt site still serves its Japanese msgstr, so a rebuild that silently lost the catalogs cannot pass as propagation"
    - "Any re-trigger is preceded by a recorded diagnosis and a recorded change; the number of trigger cycles and what changed between them is stated"
    - "No file is deleted, `30.1-VERIFICATION.md` is not modified, and nothing under `typsphinx/`, `docs/` or `tests/` is touched"
  prohibitions:
    - statement: "MUST NOT re-trigger the workflow speculatively. Every trigger after the first is preceded by a recorded diagnosis read out of the previous run's own log and a recorded change to both copies of the workflow file; at most three trigger cycles occur, and if the third still fails the outcome is recorded as an open defect with its diagnosis rather than retried further. A deterministic failure re-run unchanged is not evidence, it is noise, and it burns the one signal that distinguishes a real fix from a flake"
      status: required
      verification: "The evidence section lists each trigger in order with, for every trigger after the first, the diagnosis that motivated it and the diff that preceded it; the count of triggers is stated explicitly"
    - statement: "MUST NOT record SC#3's pin-move half as met on a green run conclusion, on a pushed commit, or on a commit message that says the pin moved. The criterion demands an observed run that MOVES THE GITLINK, and a `sphinx-intl update` run rewrites every catalog's `POT-Creation-Date` on every invocation — so a commit can exist, be green, and contain no pin movement at all. Only an exhibited `Subproject commit` diff hunk with two different SHAs settles it"
      status: required
      verification: "The evidence section contains the verbatim gitlink hunk from `git show` on the pushed commit, showing a removed and an added `Subproject commit` line with different SHAs"
  artifacts:
    - "30.1-EVIDENCE.md — sections `## Gap round — SC#3 re-triggered pin-bump run`, `## Gap round — SC#3 gitlink moved`, and `## Gap round — SC#3 propagation to /ja/latest/ and consolidated verdict` appended"
    - "A commit in `typsphinx-doc-translations` authored by the workflow itself, advancing the `typsphinx` gitlink (created by the run, not by the executor)"
  key_links:
    - "The new fetch step → `refs/remotes/origin/<branch>` exists → `git submodule update --remote typsphinx` resolves → the superproject's gitlink changes → `git diff -- typsphinx` is non-empty → the `decide` step sets `pin_moved=1` → the commit-and-push step runs. Six links, of which only the first was ever exercised before this plan; a failure at any of the remaining five is a finding, not a surprise"
    - "The push to `typsphinx-doc-translations` → RTD's webhook → a ja build → `/ja/latest/` serving the new source. A push made with the workflow's `GITHUB_TOKEN` suppresses further WORKFLOW triggers but not webhooks; if no build appears anyway, that is a measurable outcome with an owner-side fallback, not a reason to claim propagation"
    - "`contributing.po` at 0/97 → the new English sentences render in English on the Japanese site. That is the property that makes this page a usable visible-delta probe and simultaneously makes it useless as a language probe — conflating the two would either manufacture a failure or hide one"
---

<objective>
Exercise the repaired pin-bump workflow end to end and observe both halves of SC#3: a run that moves the
`typsphinx` gitlink, and the parent's source change arriving on `https://typsphinx.readthedocs.io/ja/`.

Purpose: run 30188423465 died at its fourth step, so the `decide` step, the commit-and-push step and the
header-only-churn discard step have never executed even once. They are unproven, not working. This plan
runs them for the first time and records what they actually do — including, if they misbehave, a
diagnosis from their own logs rather than a retry. SC#3 is the mechanism by which I18N-01's named failure
mode arrives through a different door: a Japanese site frozen at whatever commit it was created against,
building green forever.

Output: `30.1-EVIDENCE.md` sections carrying the observed run, the exhibited gitlink diff hunk, the ja
rebuild identified by commit, the two propagation probes, the regression re-fetch, and a consolidated
SC#3 verdict that says met or not met without hedging.

**Autonomy note:** `autonomous: true`. Triggering and observing GitHub Actions runs against
`typsphinx-doc-translations` and reading Read the Docs' public API are both things this phase's executors
have already done, recorded verbatim in `30.1-EVIDENCE.md`. Nothing here waits on the owner. If the
observation times out, that is recorded as an unobserved outcome with a named owner-side fallback — never
as a pass.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-CONTEXT.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VERIFICATION.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-08-SUMMARY.md
</context>

<gap_round_scope>
Part of the Phase 30.1 gap-closure round (Plans 07-11, waves 6-10). Plans 01-06 are complete and are not
modified, renumbered or re-run. `30.1-VERIFICATION.md` is input only and is never written.
Full artifact list: `30.1-07-PLAN.md` § `<artifacts_this_phase_produces>`.
</gap_round_scope>

<flagged_probe_assumptions>
Both unresolved spec-less edge-probe rows are enumerated in full in `30.1-07-PLAN.md` §
`<flagged_probe_assumptions>` (edges: 2 surfaced == 0 authored + 2 flagged; prohibitions: 10 kept == 10
authored + 0 flagged; canon breadcrumbs recorded there). The row this plan carries:

- **I18N-01 / `unclassified` — UNRESOLVED.** *What is assumed instead:* I18N-01's edge in this plan is
  **a green run that changes nothing substantive** — a workflow that completes successfully having
  rewritten only catalog header timestamps, leaving the site frozen while every signal reads healthy.
  This plan addresses it by demanding an exhibited gitlink diff hunk rather than a run conclusion.
  **If the assumption is wrong,** the gap is that no probe-derived edge predicate exists for I18N-01 and
  the exhibited hunk plus the two propagation probes are its entire edge surface.

One further assumption is settled empirically here rather than pre-solved: **RESEARCH assumption A3's
untested half** — that a push to the *translations* repository does rebuild the ja project. Plan 05 could
only test the parent-push half, because its pin-bump run never pushed. Task 3 measures the other half for
the first time; a negative result is recorded as a measurement with an owner-side fallback, not treated
as a failure of this plan.
</flagged_probe_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Trigger the repaired workflow and observe the run to completion</name>
  <files>.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md, .planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml</files>
  <read_first>
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#3 — observed pin-bump run" §§ 1-5 — the exact `gh workflow run` / `gh api` / `gh run view` invocations used previously, the per-step outcome format, and the failing step's log excerpt, so this task reuses the same recording shape rather than inventing one
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml` — the current, patched step sequence, including the `decide` step's exact output names `pin_moved` and `po_changed` and the header-filtering pipeline that produces them
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-RESEARCH.md` § "Common Pitfalls" Pitfall 1 — why `sphinx-intl update` rewrites `POT-Creation-Date` on every run and what a naive commit rule would therefore do
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-08-SUMMARY.md` — the live commit SHA in `typsphinx-doc-translations` carrying the patched workflow, so this task can confirm it is triggering the patched version rather than a stale one
  </read_first>
  <action>
    Trigger once, then wait properly.

    Before triggering, confirm the live repository actually carries the patched workflow: fetch the file
    from the remote and confirm the new fetch step is present. A trigger against a stale copy would
    reproduce the old failure and waste the observation.

    Trigger with `gh workflow run` against the `Update translation source pin` workflow in
    `YuSabo90002/typsphinx-doc-translations`, then resolve the run id — `gh workflow run` does not print
    one, so list the workflow's runs and take the newest whose creation time is after the trigger.

    **Wait and confirm; do not assume.** Poll the run's `status` with `gh run view --json` at roughly
    15-second intervals until it reports `completed`, with a hard ceiling of 10 minutes. The terminal
    condition is `status == "completed"`; the conclusion (`success` or `failure`) is then read from the
    same response. If the ceiling is reached with the run still queued or in progress, record the last
    observed status, the elapsed time and the run URL, and stop this plan there with the outcome recorded
    as UNOBSERVED — a timeout is a measurement, and Plan 10's work does not depend on this plan's result.

    When the run completes, record: the run URL, the conclusion, the start and end timestamps, the
    duration, and the per-step outcome list in the same shape Plan 05 used, so a reader can see at a
    glance which steps ran and which were skipped. Then read the `decide` step's own log and record
    `pin_moved` and `po_changed` as the values the log actually shows. If the job died before `decide`
    ran, say so exactly as Plan 05 did — those outputs do not exist for that run, and recording them as
    zero would be an invention.

    **On failure, diagnose from the log, then change something, then re-trigger — in that order.** Read
    the failing step's log verbatim and the log of the step immediately before it (that is where the
    previous failure's actual cause lived). Record both. Then apply the diagnosed change to BOTH copies
    of the workflow — the staged copy in this repository and the live copy in
    `typsphinx-doc-translations` — keeping them byte-identical and proving it from a fresh clone, and
    re-trigger. At most three trigger cycles. Each cycle records its diagnosis, its diff and its outcome.
    If the third cycle still fails, record the state as an open defect with the full diagnosis and stop;
    do not keep going.

    Likely-but-unproven failure points, named so a failure is recognised rather than puzzled over: the
    shallow submodule fetch may not satisfy `--remote` (the diagnosed change would be dropping the depth
    limit on that fetch, or setting the checkout step's fetch depth to full); the `.pot` regeneration step
    may fail on a missing docs dependency; the `sphinx-intl update` step may write to an unexpected path;
    the push step may be rejected. None of these has ever executed. Treat each as an ordinary finding.

    Append `## Gap round — SC#3 re-triggered pin-bump run` to `30.1-EVIDENCE.md` with every command and
    its output verbatim, in trigger order.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "
import pathlib,sys
ev=pathlib.Path('.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md').read_text(encoding='utf-8')
sec='## Gap round — SC#3 re-triggered pin-bump run'
ok=sec in ev
body=ev.split(sec)[-1]
need=['gh workflow run','conclusion','Advance submodule to latest tracked branch']
miss=[x for x in need if x not in body]
outcome=('pin_moved' in body) or ('UNOBSERVED' in body) or ('did not exist' in body) or ('never ran' in body)
print('section',ok,'MISSING',miss,'outcome_recorded',outcome)
sys.exit(0 if (ok and not miss and outcome) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `section True`, `MISSING []`, `outcome_recorded True` and exits 0
    - The evidence section records a pre-trigger confirmation that the live workflow file carries the new fetch step
    - The evidence section records the run URL, the conclusion, both timestamps, the duration and the full per-step outcome list
    - The `decide` step's `pin_moved` and `po_changed` values are recorded as read from the run's log, or their non-existence is stated explicitly because the job failed earlier — never invented as zeros
    - If any trigger failed: the failing step's log and the preceding step's log both appear verbatim, followed by the diagnosis and the diff applied before the next trigger
    - The number of trigger cycles is stated explicitly and is at most three; no trigger appears without either being the first or being preceded by a recorded diagnosis and change
    - If a re-trigger required a workflow change, the staged and live copies are re-proven byte-identical from a fresh clone
    - If the 10-minute ceiling was reached, the section records the last observed status, the elapsed time and an explicit UNOBSERVED outcome
    - `git status --porcelain typsphinx/ tests/ docs/ docs/locale/ pyproject.toml .readthedocs.yaml .github/` is empty
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VERIFICATION.md` is unmodified and no file is deleted
  </acceptance_criteria>
  <done>The repaired workflow has been run and watched to a terminal state, its per-step outcomes and `decide` outputs are recorded from its own log, and any failure carries a diagnosis rather than a retry.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the gitlink moved by exhibiting the diff hunk, not by reading the run's conclusion</name>
  <files>.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#3 — observed pin-bump run" § 6 — the fresh-clone confirmation shape used previously, and the recorded pre-state: tip `34647d25f6a4ab71db9df6635e13fc0c14a9ae2a`, gitlink `dcc6a523fc2d16f3dae925e17d63a1a24318b6d7`
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#3 precondition — pushed tree verified from a fresh clone" § 4 — the `git ls-files -s typsphinx` form that prints the `160000` gitlink mode and SHA
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "Gap round — parent milestone branch pushed" — the parent branch tip this pin should have advanced to
    - `.planning/ROADMAP.md` § "Phase 30.1" success criterion 3 — the exact bar: "demonstrated by an observed run that moves the pin"
  </read_first>
  <action>
    Skip only if Task 1 recorded an UNOBSERVED timeout or a third-cycle open defect; in that case record
    one sentence saying so and stop, without claiming or denying anything about the pin.

    Clone `typsphinx-doc-translations` fresh into a temporary directory — a clone made after the run, not
    a directory left over from Task 1 — and establish, from git objects rather than from the run's UI:

    - the repository's tip commit SHA, its author and its subject, compared against the pre-run tip
      `34647d25f6a4ab71db9df6635e13fc0c14a9ae2a`;
    - the full `git show --stat` of the new tip, so the shape of what changed is visible;
    - the **gitlink hunk itself**: run `git show` on the new commit restricted to the submodule path and
      record the output verbatim, showing the removed `Subproject commit` line and the added one. Two
      different SHAs on those two lines is the only thing that satisfies SC#3's pin-move half. Record
      both SHAs on their own line as a before/after pair;
    - `git ls-files -s typsphinx` on the new tip, showing mode `160000` and the new SHA.

    Then reconcile the new gitlink against the parent: it must equal `origin/gsd/v0.6.4-read-the-docs-migration`'s
    tip at the moment the run executed. Fetch that ref fresh and compare. If they differ, do not call it a
    failure reflexively — the parent may have advanced after the run started. Record both SHAs, state
    whether the recorded gitlink is an ancestor of the current parent tip, and say which explanation the
    evidence supports.

    **Distinguish the two outcomes explicitly, in writing:**
    - If the commit's diff touches only `locale/**` and shows no `Subproject commit` change, this is the
      `POT-Creation-Date`-churn outcome. Record it as **NOT satisfying SC#3**, quote the criterion, and
      state that the `decide` step's own `pin_moved` value should be `0` for this run — check whether it
      is, because a mismatch there would be a defect in the `decide` step's filter rather than in the pin.
    - If the diff shows the gitlink moving, say so and exhibit the hunk. Also record how many `locale/**`
      files changed alongside it and whether any change beyond `POT-Creation-Date` headers appears, since
      the parent gained new msgids in Plans 05 and 08 and those should now be merged into the catalogs.

    Append `## Gap round — SC#3 gitlink moved` to `30.1-EVIDENCE.md` with every command and its output
    verbatim.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "
import pathlib,sys,re
ev=pathlib.Path('.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md').read_text(encoding='utf-8')
sec='## Gap round — SC#3 gitlink moved'
if sec not in ev:
    print('section missing'); sys.exit(1)
body=ev.split(sec)[-1]
skipped=('UNOBSERVED' in body) or ('stopped' in body.lower() and len(body)<1200)
hunk=('-Subproject commit' in body and '+Subproject commit' in body)
churn=('POT-Creation-Date' in body)
verdict=('NOT satisf' in body) or ('satisfies' in body) or ('pin-move half' in body)
print('skipped',skipped,'gitlink_hunk',hunk,'churn_addressed',churn,'verdict',verdict)
sys.exit(0 if (skipped or (hunk and churn and verdict)) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0, printing either `skipped True` or `gitlink_hunk True` with `churn_addressed True` and `verdict True`
    - The evidence section records a fresh clone made after the run, its tip SHA, author and subject, and the comparison against `34647d25f6a4ab71db9df6635e13fc0c14a9ae2a`
    - The evidence section contains the verbatim `Subproject commit` diff hunk with a removed and an added line carrying different SHAs, plus a before/after SHA pair on its own line
    - The evidence section records `git ls-files -s typsphinx` showing mode `160000` and the new SHA
    - The new gitlink is reconciled against `origin/gsd/v0.6.4-read-the-docs-migration`, with both SHAs recorded and an ancestry statement where they differ
    - The `POT-Creation-Date`-only outcome is addressed in writing — either ruled out by the exhibited hunk, or recorded as the outcome and explicitly marked as NOT satisfying SC#3 with the `decide` step's `pin_moved` value cross-checked
    - The record states how many `locale/**` files changed and whether any change beyond header timestamps appears
    - `git status --porcelain typsphinx/ tests/ docs/ docs/locale/ pyproject.toml .readthedocs.yaml .github/` is empty
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VERIFICATION.md` is unmodified and no file is deleted
  </acceptance_criteria>
  <done>Whether the pin moved is settled from git objects in a fresh clone with the diff hunk exhibited, and the header-churn outcome is either ruled out by evidence or recorded as the outcome and marked as not satisfying the criterion.</done>
</task>

<task type="auto">
  <name>Task 3: Observe the ja rebuild and the parent's source change arriving on /ja/latest/, then state the SC#3 verdict</name>
  <files>.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#3 — propagation to /ja/latest/" §§ 1-5 — the exact builds-API call, the `curl` shape with status/effective-URL/size capture, the pre-change section structure of `contributing.html` (Feature Requests running straight into Community), and the recorded regression baseline for `builders.html` (`size_download=32376`, line 297, the matched msgstr)
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#1 — ja first-build log excerpts" — the raw-log URL form and the submodule excerpt shape to compare against
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "Gap round — SC#3 gitlink moved" — the pushed translations-repository commit SHA this task waits for the ja project to build
    - `docs/source/contributing.rst` — the two probe strings as they now stand in the parent: the Plan 05 Translations sentence and the WR-01 `--recurse-submodules` line
    - `.planning/ROADMAP.md` § "Phase 30.1" success criterion 3 and the STATE.md note that a `contributing` probe is a visible-delta probe and never a language probe
  </read_first>
  <action>
    Skip only if Task 2 recorded that the pin did not move; in that case record one sentence saying no
    propagation is expected, run the regression re-fetch anyway so the site's current state is on record,
    and write the SC#3 verdict as NOT MET with the reason.

    **Wait for the rebuild, with a stated terminal condition.** Poll
    `https://app.readthedocs.org/api/v3/projects/typsphinx-ja/builds/` at roughly 30-second intervals
    until a result appears whose `commit` equals the translations-repository SHA recorded in Task 2 AND
    whose `state.code` is `finished`, with a hard ceiling of 20 minutes. Record the poll count, the
    elapsed time, and the terminal response verbatim. If the ceiling is reached with no such build, record
    the last response verbatim, state that propagation is UNOBSERVED, and record the owner-side fallback
    as a named next step — a manual "Build version" trigger on the ja project's dashboard, which is
    web-UI work no command here is authorised to perform. Do not claim propagation from a build whose
    `commit` field does not match.

    When the matching build finishes, record its id, state, success flag and duration, then fetch its raw
    log over HTTP and record the submodule line verbatim — the SHA the build actually resolved. Compare
    it against the new gitlink value from Task 2 and state whether they agree. A build that finished
    against the old SHA is not propagation.

    **Then probe the served page, twice.** GET `https://typsphinx.readthedocs.io/ja/latest/contributing.html`
    following redirects, capturing status, effective URL and downloaded size, and assert on the captured
    body:
    - probe 1, the Plan 05 sentence beginning "Japanese translation catalogs are maintained in a separate
      repository" — the exact string SC#3's own bar names;
    - probe 2, the literal `--recurse-submodules` from the WR-01 correction, which did not exist anywhere
      until this round and therefore proves the served page is built from the *current* source rather than
      from any earlier commit that also contained probe 1.
    Record the matched lines with a few lines of surrounding context each. If probe 1 matches and probe 2
    does not, that is a finding worth stating plainly: the site advanced but not to the tip.

    Record, in the same section and not as a footnote, that `contributing.po` is 0/97 translated so both
    probe strings appear in English, that this is correct Sphinx fallback behaviour rather than a language
    failure, and that this page is therefore SC#3's visible-delta probe and never an SC#1 language probe.

    **Regression re-fetch.** GET `https://typsphinx.readthedocs.io/ja/latest/user_guide/builders.html` and
    assert its Japanese msgstr is still served. A rebuild that lost the catalogs would satisfy every
    propagation probe above while regressing the criterion the phase already passed; nothing else in this
    plan would notice.

    Close with `### Consolidated SC#3 verdict` inside the same appended section: the pin-move half, the
    propagation half, and SC#3 overall, each stated as MET or NOT MET with the evidence naming the
    section that carries it. Also record whether RESEARCH assumption A3's untested half — that a push to
    the translations repository does rebuild the ja project — is now measured, and what the measurement
    showed.

    Append all of this as `## Gap round — SC#3 propagation to /ja/latest/ and consolidated verdict`.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "
import pathlib,sys
ev=pathlib.Path('.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md').read_text(encoding='utf-8')
sec='## Gap round — SC#3 propagation to /ja/latest/ and consolidated verdict'
if sec not in ev:
    print('section missing'); sys.exit(1)
body=ev.split(sec)[-1]
need=['typsphinx-ja/builds','contributing.html','builders.html','0/97','Consolidated SC#3 verdict']
miss=[x for x in need if x not in body]
verdict=('MET' in body) or ('NOT MET' in body)
print('MISSING',miss,'verdict_present',verdict)
sys.exit(0 if (not miss and verdict) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `MISSING []` and `verdict_present True` and exits 0
    - The evidence section records the polling loop's terminal condition, poll count and elapsed time, and the terminal API response verbatim
    - The matching ja build is identified by id, `commit` field, state, success flag and duration, and its `commit` equals the translations-repository SHA from Task 2 — or the section records UNOBSERVED with the owner-side fallback named
    - The build's raw log submodule line is recorded verbatim and compared against the new gitlink value with an explicit agree-or-not statement
    - Probe 1 and probe 2 are both recorded with their matched lines and surrounding context, or a mismatch is stated plainly with what it implies
    - The 0/97 coverage of `contributing.po` and the visible-delta-versus-language-probe distinction are stated inside this same section
    - The regression re-fetch of `builders.html` is recorded with its status, size and matched Japanese msgstr
    - `### Consolidated SC#3 verdict` states the pin-move half, the propagation half and SC#3 overall, each MET or NOT MET, each naming the evidence section that carries it
    - The measured status of RESEARCH assumption A3's untested half is recorded
    - `git status --porcelain typsphinx/ tests/ docs/ docs/locale/ pyproject.toml .readthedocs.yaml .github/` is empty
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VERIFICATION.md` is unmodified and no file is deleted
  </acceptance_criteria>
  <done>The ja project is observed rebuilding from the commit the pin bump pushed, the parent's current source is observed on the served Japanese page by two independent probes, the previously-passing language probe is confirmed unregressed, and SC#3 carries a stated verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A scheduled, unattended GitHub Actions job with `contents: write` → `typsphinx-doc-translations`' `main` branch | The job authors and pushes a commit with no human in the loop. This plan is the first time that path executes |
| `typsphinx-doc-translations` push → RTD's build webhook → a public build container | A push in one repository causes code from another repository to be checked out and executed in a container this project cannot inspect |
| RTD's public v3/v2 endpoints and the served site → this record | Read-only, unauthenticated reads used as evidence |

## STRIDE Threat Register

ASVS L1, block on `high`. V2/V3/V4/V5/V6 remain not applicable. Threat IDs continue the phase's
`T-30.1-NN` sequence.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30.1-13 | Elevation of Privilege | The unattended `contents: write` job pushing to an external repository, executing for the first time past its fourth step | high | mitigate | The job's `permissions` scope was asserted unchanged in Plan 08 and no step that writes or pushes was modified. This plan observes rather than widens: the pushed commit's full `git show --stat` is recorded from an independent clone, so anything the job wrote beyond `typsphinx` and `locale/**` is visible in the record rather than assumed absent |
| T-30.1-21 | Tampering | A pin advanced to an unintended commit — a force-pushed or rewritten parent branch, or a race where the parent moved mid-run | medium | mitigate | Task 2 reconciles the new gitlink against `origin/gsd/v0.6.4-read-the-docs-migration` fetched fresh, records both SHAs, and states the ancestry relationship where they differ rather than assuming the newer value is correct |
| T-30.1-22 | Spoofing | A stale or cached HTTP response being read as propagation, or a build that finished against the OLD commit being counted as the rebuild | high | mitigate | The build is matched on its `commit` field, not on recency; the raw log's resolved submodule SHA is compared against the new gitlink; the page probe uses a string (`--recurse-submodules`) that did not exist anywhere before this round, so a cached pre-round response cannot satisfy it; effective URL and downloaded size are recorded on every fetch |
| T-30.1-23 | Denial of Service | Repeated speculative workflow triggers against an external repository | low | mitigate | At most three trigger cycles, each requiring a recorded diagnosis and a recorded change; a deterministic failure is recorded as an open defect rather than retried |
| T-30.1-17 | Information Disclosure | `gh` credentials in the executor's environment | medium | mitigate | Only workflow-trigger and run-read calls against the two known repositories, plus ordinary clones. No token is printed or written into any file; recorded transcripts are checked for credential material before being appended |
| T-30.1-06 | Information Disclosure | Public build logs and public repositories | low | accept | Both repositories and all logs are already public; the workflow references no secret. Phase 29's standing rule holds — no secret may ever be echoed into a workflow or `build.jobs` command |
| T-30.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan and no new package is introduced (milestone invariant #1). The workflow's own `uv pip install ./typsphinx[docs]` installs only what the submodule's `pyproject.toml` already declares — the same set CI already trusts — and `30.1-RESEARCH.md` § "Package Legitimacy Audit" records zero `[ASSUMED]`/`[SUS]`/`[SLOP]` rows, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
1. The live workflow file is confirmed patched before any trigger
2. A run is triggered and polled to a terminal state with a stated ceiling; a timeout is recorded as UNOBSERVED, never as a pass
3. Per-step outcomes and the `decide` step's `pin_moved` / `po_changed` values are recorded from the run's own log, or their non-existence is stated
4. Any re-trigger is preceded by a recorded diagnosis and a recorded change, with at most three cycles
5. The pin move is settled from a fresh clone by exhibiting the `Subproject commit` diff hunk with two different SHAs
6. The `POT-Creation-Date`-only outcome is explicitly ruled out or recorded as the outcome and marked NOT MET
7. The ja rebuild is matched on its `commit` field and its raw-log submodule SHA is compared against the new gitlink
8. Two independent propagation probes are asserted on the served page, one of which cannot be satisfied by any pre-round cached response
9. The previously-passing Japanese language probe is re-fetched and confirmed unregressed
10. A consolidated SC#3 verdict states both halves and the whole, MET or NOT MET, with evidence named

</verification>

<success_criteria>
- The repaired workflow is observed running past the step that previously killed it, with every step's outcome recorded
- The gitlink move is proven by an exhibited diff hunk, and a header-churn-only run would have been caught and recorded as a failure
- The ja site is observed rebuilding from the pushed commit and serving the parent's current source, proven by a string that did not exist before this round
- The Japanese content the phase already passed on is confirmed unregressed by the rebuild
- Every wait has a stated terminal condition and a stated meaning for its timeout
- SC#3 carries an unhedged verdict; no file is deleted; `30.1-VERIFICATION.md` is untouched
</success_criteria>

<output>
Create `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-09-SUMMARY.md` when done.

The SUMMARY must state the run URL and conclusion, the number of trigger cycles, the before/after gitlink
SHAs, the ja build id that carried the propagation, whether both page probes matched, and the SC#3
verdict — and must not describe SC#3 as met without an exhibited gitlink hunk.
</output>
