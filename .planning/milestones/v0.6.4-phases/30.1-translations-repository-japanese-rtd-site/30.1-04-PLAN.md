---
phase: 30.1-translations-repository-japanese-rtd-site
plan: 04
type: execute
wave: 3
depends_on: ["30.1-03"]
files_modified:
  - .planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md
autonomous: false
requirements: [I18N-01]
user_setup:
  - service: readthedocs
    why: "Creating the Japanese project, setting its Language, linking it under the English parent, and activating its versions have no .readthedocs.yaml representation and no API this phase is authorised to drive — they exist only in RTD's own database via its web UI (REQUIREMENTS.md § Owner-Manual Steps items 2, 3 and 4). RESEARCH Pitfall 4: these are three independent actions, not one"
    dashboard_config:
      - task: "Create a SEPARATE RTD project pointed at the `typsphinx-doc-translations` repository — not at `typsphinx`. Enter slug `typsphinx-ja`; if it is taken, pick any other free name and continue (D-09 — unlike the parent slug this one is never published)"
        location: "Read the Docs → Add project / Import a repository"
      - task: "Set Language = Japanese in THAT project's own Admin settings. This dropdown, and nothing in conf.py, is what makes RTD emit READTHEDOCS_LANGUAGE=ja at build time"
        location: "RTD → ja project → Admin → Settings → Language"
      - task: "Set that project's Default branch to `gsd/v0.6.4-read-the-docs-migration`, matching the parent project's current setting and the submodule's tracked branch — `main` carries no .readthedocs.yaml (measured 2026-07-26)"
        location: "RTD → ja project → Admin → Settings → Default branch"
      - task: "LINK the Japanese project under the ENGLISH PARENT's Admin → Translations. This is the step most likely to be missed: creating both projects without linking leaves two working but unswitchable sites, with no flyout language entry on either"
        location: "RTD → typsphinx (parent) project → Admin → Translations → Add translation"
      - task: "Activate versions on the Japanese project independently — translation projects inherit nothing from the parent, so a version active on the parent may be inactive here and /ja/latest/ can 404 after a successful build"
        location: "RTD → ja project → Versions"
      - task: "Leave the Japanese project's Default Version at `latest`. Do NOT set `stable` — it cannot exist before the v0.6.4 tag, and the flip is Phase 33's handoff (SC#5)"
        location: "RTD → ja project → Admin → Advanced settings → Default version"
must_haves:
  truths:
    - statement: "A real HTTP GET of https://typsphinx.readthedocs.io/ja/latest/user_guide/builders.html (the `ja` language segment is RTD-derived from the project's Admin Language via ISO 639-1 and is not configurable — D-10) returns 200 and its served body contains the literal msgstr taken from the relocated `user_guide/builders.po` (65/65 coverage) — so a Japanese project that builds green while rendering 100% English fails this criterion (SC#1)"
      verification: backstop
    - statement: "A second real HTTP GET of https://typsphinx.readthedocs.io/ja/latest/examples/basic.html returns 200 and its served body contains a literal msgstr from `examples/basic.po` (30/30 coverage) — an independent docname, so a single lucky page cannot carry SC#1 alone"
      verification: backstop
    - statement: "The ja project's first raw build log has been read end to end and answers, with recorded verbatim excerpts rather than inferences, three questions: whether the submodule checkout resolved the pinned commit (RESEARCH Open Question #1 / A4), whether post_create_environment removed and repopulated typsphinx/docs/locale, and whether the path-based python.install entry installed typsphinx successfully (RESEARCH assumption A2)"
      verification: backstop
    - statement: "Fetching the documentation ROOT over real HTTP still lands on a version that exists and serves real content — the RTD-04 standing invariant re-taken in this phase rather than cited from Phase 29"
      verification: backstop
    - statement: "RTD's flyout offers the en to ja switch from BOTH sites, owner-observed and recorded verbatim, and corroborated by the parent project's public translations record listing the ja project (SC#2). That parent-side record is what makes the ja project a registered translation OF the `typsphinx` parent rather than a standalone site (D-06)"
      verification: backstop
    - "30.1-EVIDENCE.md states the measured catalog coverage — 257/1058 msgids, 24.3%, with `api/index`, `contributing`, `changelog` and `user_guide/templates` at zero — inside the same section as the SC#1 pass, so the pass cannot be read as a claim that the site is fully translated"
    - "30.1-EVIDENCE.md records the ja project's Default Version as deliberately left at `latest`, and records the two-repository tag step as an explicit handoff into the release procedure — SC#5 is discharged by a recorded handoff, never by an assertion that `/ja/stable/` already serves"
    - "30.1-EVIDENCE.md records all THREE owed post-merge settings flips — the parent RTD project's Default branch, the ja RTD project's Default branch, and the translations repository's `.gitmodules` `branch` field — each marked not-yet-done"
    - "30.1-EVIDENCE.md distinguishes, item by item, what was machine-verified with a recorded command plus output from what is owner-reported; no RTD web-UI action is asserted as machine-verified"
  prohibitions:
    - statement: "MUST NOT present the Japanese site as translated or complete without recording the measured coverage alongside the pass. Reporting SC#1 green from a 100%-coverage probe docname while three quarters of the msgids are untranslated would be a literally true claim that reads as a false one, and the next reader who opens a zero-coverage page and finds English would be right to distrust the whole record"
      status: required
      verification: "Human review of the SC#1 section: the coverage figures and the four zero-coverage docnames appear in the same section as the pass, not in a footnote elsewhere"
    - statement: "MUST NOT record any RTD web-UI action — project creation, the Language setting, the parent-side Translations link, version activation, or Default Version — as machine-verified. Only the OUTCOME of those actions may be asserted, and only from a recorded fetch or a recorded log excerpt"
      status: required
      verification: "Human review of 30.1-EVIDENCE.md: every owner-manual item carries the literal `human_needed` or an explicit owner-reported marker, and no such item sits under a command-plus-output block as though the command confirmed it"
    - statement: "The SC#1 probe MUST NOT target `changelog`, `contributing`, `api/index` or `user_guide/templates`. All four are 0% translated and would render all-English on a perfectly healthy site, so a probe there produces a false negative that looks like a real defect — and swapping to a friendlier docname mid-execution to turn a red probe green would hide a genuine failure"
      status: required
      verification: "The recorded fetch URLs are `user_guide/builders` and `examples/basic` and no other docname; if a probe went red, the record shows the red result rather than a substituted docname"
  artifacts:
    - "30.1-EVIDENCE.md — sections `## SC#1 — /ja/latest/ serves real Japanese`, `## SC#1 — ja first-build log excerpts`, `## RTD-04 — documentation root re-fetched`, `## SC#2 — flyout and version activation (human_needed)`, `## SC#5 — /ja/stable/ handoff`, `## Handoffs`, and `## D-15 gate — replacement confirmed serving` appended"
  key_links:
    - "RTD Admin → Language = Japanese → the READTHEDOCS_LANGUAGE=ja environment variable → `_resolve_language()` in the submodule's unedited `docs/source/conf.py` → `language = \"ja\"` → Sphinx reading the copied catalogs. Five hops, none of them assertable from this repository; only the served bytes prove the chain"
    - "The parent's Admin → Translations link → the flyout's language entry on BOTH sites. Nothing fails without it: both projects build, both serve, and neither offers a switch. This is the single most-missed step in the phase (RESEARCH Pitfall 4)"
    - "Version activation on the ja project → whether `/ja/latest/` resolves at all. Translation projects inherit nothing, so a project whose build succeeded can still 404 on every URL"
---

<objective>
Stand up the Japanese Read the Docs project (owner-manual), then prove its **outcome** with real HTTP
fetches against actual translated strings — discharging I18N-01's serving half, re-taking the RTD-04
root invariant, and recording SC#2 and SC#5 honestly as what they are.

Purpose: I18N-01's named failure mode is a Japanese project that builds green while rendering 100%
English (milestone invariant #7). Nothing about the build status, the dashboard, or the project list
distinguishes that state from success — only bytes from the served page do. And RESEARCH Pitfall 4
splits what looks like one action into three that fail independently: created, linked, versions
activated.

Output: `30.1-EVIDENCE.md` carrying the two content probes verbatim, the first build log's answers to
three open questions, the root re-fetch, the owner-observed flyout, the SC#5 handoff, the three owed
post-merge flips, and — for Phase 30 to read — an unambiguous statement that the replacement is
confirmed serving.

**Autonomy note:** `autonomous: false` because Task 1 is a blocking owner-manual checkpoint. Tasks 2 and
3 are machine-executable and run only after the owner replies.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-CONTEXT.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-RESEARCH.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VALIDATION.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See `30.1-01-PLAN.md` § `<artifacts_this_phase_produces>` for the full phase-level list. This plan
creates no new file and no new repository symbol; it appends seven `##` sections to the existing
`30.1-EVIDENCE.md` and brings the RTD-side symbols listed there into existence through owner action —
the second RTD project (slug `typsphinx-ja` or any free name), its Japanese Language setting, its link
under the English parent, and its independently activated versions.
</artifacts_this_phase_produces>

<flagged_probe_assumptions>
Both unresolved spec-less edge-probe rows are enumerated in full in `30.1-01-PLAN.md` §
`<flagged_probe_assumptions>` (edges 2 == 0 authored + 2 flagged; prohibitions 7 kept == 7 authored + 0
flagged; canon breadcrumb recorded there). The row this plan carries, restated so the plan is
self-contained:

- **I18N-01 / `unclassified` — UNRESOLVED.** The probe could not classify it, and per §A it is not
  auto-`backstop`ped. *Assumption:* I18N-01's edge is a content-fidelity failure with no data boundary
  to probe, discharged here by served-content assertions against two independent 100%-coverage docnames
  rather than by any probe-derived predicate. **If wrong,** this plan's two fetches plus Plan 05's
  propagation evidence are I18N-01's entire edge surface.

Two RESEARCH-level unknowns are settled empirically by this plan rather than pre-solved, in the same
"read the raw log" shape Phase 29 used for its `@preview` egress question:
- **Open Question #1 / assumption A4** — whether RTD's clone depth prevents the pinned submodule commit
  from resolving. Task 2 reads the answer out of the first build log; a failure here is loud, not silent.
- **Assumption A2** — whether the non-editable path-based `python.install` behaves like the editable
  install `sphinx-doc-translations` uses. Task 2 reads the install lines. The correction, if one is
  needed, is a one-line manifest change decided from the log, not guessed at now.
</flagged_probe_assumptions>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Owner creates, configures and links the Japanese RTD project</name>
  <what-built>
    Plan 03 pushed `typsphinx-doc-translations` with everything a Read the Docs build needs: a submodule
    pinned to a commit that carries `.readthedocs.yaml` and the `_resolve_language()` seam, the 13
    Japanese catalogs, the ja build manifest, and the pin-bump workflow. What remains has no
    `.readthedocs.yaml` representation and no API this phase is authorised to drive — project creation,
    the Language dropdown, the parent-side Translations link, and version activation exist only in RTD's
    own database via its web UI. Tasks 2 and 3 verify their *outcome*; no test in this repository can
    assert the clicks.
  </what-built>
  <files>none — owner-manual actions in the Read the Docs dashboard; no file in this repository is touched by this task</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — § "Owner-Manual Steps" items 2, 3 and 4; § "Milestone Invariants" #7
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-RESEARCH.md` — § "Common Pitfalls" Pitfall 4 in full: created, linked, and versions-activated are three independent actions with three distinct symptoms
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-CONTEXT.md` — D-09 (the ja slug is not a decision; if `typsphinx-ja` is taken, pick any free name and continue — the D-02 stop-and-consult rule does NOT extend here), D-10 (`/ja/` is not configurable)
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-01-PLAN.md` — § `<planner_decisions>` PD-02, for why the ja project's Default branch must be the milestone branch and not `main`
    - `.planning/ROADMAP.md` — § "Phase 30.1" → "Owner-manual dependencies"
  </read_first>
  <action>
    Present the numbered dashboard steps in `<how-to-verify>` to the owner verbatim and BLOCK until they
    reply. Do not attempt to script or drive any of them — no manifest key and no command represents
    project creation, the Language dropdown, the parent-side Translations link, or version activation.

    When the owner replies, carry their reported values into Tasks 2 and 3 **verbatim**: do not
    normalise the build URL, do not summarise the raw-log excerpts, and do not fill in a value the owner
    left blank. An absent value is an absent value and is recorded as `human_needed`.

    Note the deliberate asymmetry with Phase 29: if the ja project slug `typsphinx-ja` is taken, the
    owner picks any other free name and continues (D-09). Unlike the parent slug, this string is never
    published — readers see `/ja/latest/` under the parent's own domain — so nothing downstream depends
    on it. Record whichever slug was actually used.

    If the owner reports that the first build FAILED, do not stop the phase: carry the failure forward.
    Task 2 reads the raw log and records the failure verbatim, which is exactly how RESEARCH's two open
    questions — submodule clone depth, and the install shape — were designed to be answered.
  </action>
  <verify>
    <human-check>Confirm the owner's reply contains every field named in `<resume-signal>`, that the raw-log excerpts are verbatim log text rather than paraphrases, and that the Translations link was confirmed from the PARENT project's Admin page rather than assumed. If any field is missing, ask for it rather than proceeding with a gap.</human-check>
  </verify>
  <acceptance_criteria>
    - The resume report states, verbatim: the ja project slug actually used; its Admin Language value; its Default branch value; its Default Version value; the list of activated versions; the first build's detail URL; and that build's final status
    - The resume report confirms the ja project was created against the `typsphinx-doc-translations` repository — not against `typsphinx`
    - The resume report confirms, as a separate line, that the ja project now appears under the ENGLISH PARENT's Admin → Translations list, observed on the parent's own page
    - The resume report contains three verbatim raw-log excerpts, labelled `submodule`, `post_create_environment` and `install`
    - The resume report states, per site, whether the RTD flyout on `/en/latest/` and on `/ja/latest/` offers the other language
    - Nothing in this task modifies any file in this repository
  </acceptance_criteria>
  <done>The owner has reported the ja project's slug, repository, Language, Default branch, Default Version and activated versions, confirmed the parent-side Translations link from the parent's own page, reported the first build's URL and status with three verbatim raw-log excerpts, and reported the flyout state on both sites.</done>
  <how-to-verify>
    Perform these in the Read the Docs web UI, in this order. Steps 4 and 5 are the two most-missed
    steps in this milestone — neither produces any symptom on a build page.

    1. **Add project.** Import the repository `YuSabo90002/typsphinx-doc-translations`. This is the
       **translations** repository, not `typsphinx`. Enter slug `typsphinx-ja`; if it is taken, pick any
       other free name and continue — this slug is never published.
    2. **Admin → Settings → Language = Japanese.** This dropdown, and nothing in `conf.py`, is what
       makes RTD emit `READTHEDOCS_LANGUAGE=ja` at build time. It is also what makes the URL segment
       `ja`: the segment is derived from this setting via ISO 639-1 and cannot be set from any file.
    3. **Admin → Settings → Default branch = `gsd/v0.6.4-read-the-docs-migration`.** Match the parent
       project's current setting. Measured 2026-07-26: the `main` branch carries no `.readthedocs.yaml`,
       so a project defaulting to `main` has nothing to build.
    4. **Link it under the PARENT.** Go to the **`typsphinx`** (English) project → Admin → Translations →
       add the Japanese project. Do this from the parent's page, not from the ja project's. Skipping this
       leaves two working but unswitchable sites: both build, both serve, and neither flyout offers a
       language entry.
    5. **Activate versions on the Japanese project.** Go to the ja project → Versions and activate the
       versions it should serve, starting with `latest`. Translation projects inherit **nothing** from
       the parent, so a version active on the parent may be inactive here — and an inactive `latest`
       means `/ja/latest/` 404s even though the build succeeded.
    6. **Admin → Advanced settings → Default version = `latest`.** Leave it there. `stable` cannot exist
       before the `v0.6.4` tag; the flip is Phase 33's handoff.
    7. Let the first build run and wait for it to finish. Open the build's detail page, note its URL, and
       click **View raw** for the plain-text log.
    8. In the raw log, copy three excerpts verbatim — do not search for a pre-decided string; read what
       the log actually says:
       (a) the lines where the `typsphinx` **submodule** is cloned or checked out, including the commit
           it resolved to;
       (b) the lines produced by the `post_create_environment` job — the removal, and the copy into
           `typsphinx/docs/locale`;
       (c) the lines where `typsphinx` itself is **installed** by the `python.install` step.
    9. Finally, open both `https://typsphinx.readthedocs.io/en/latest/` and
       `https://typsphinx.readthedocs.io/ja/latest/` in a browser and report, for each, whether the RTD
       flyout (bottom-right) shows a language section offering the other language.
  </how-to-verify>
  <resume-signal>
    Reply with a block containing: `ja_slug=`, `ja_repository=`, `admin_language=`, `default_branch=`,
    `default_version=`, `activated_versions=`, `linked_under_parent=` (yes/no, observed on the parent's
    Admin → Translations page), `build_url=`, `build_status=`, `flyout_on_en=` (yes/no),
    `flyout_on_ja=` (yes/no) — plus three fenced raw-log excerpt blocks labelled `submodule`,
    `post_create_environment` and `install`.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Probe /ja/latest/ for real translated strings, re-take the root invariant, read the first build log</name>
  <files>.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md</files>
  <read_first>
    - `docs/locale/ja/LC_MESSAGES/user_guide/builders.po` — read the msgstr entries directly and confirm the probe string used below is present verbatim, so the assertion keys on a real catalog value rather than a remembered one
    - `docs/locale/ja/LC_MESSAGES/examples/basic.po` — same, for the second probe string
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-CONTEXT.md` — § "Specifics" for the full per-file coverage table (257/1058 = 24.3%) and the explicit warning that `changelog`, `contributing`, `api/index` and `user_guide/templates` are at zero and would render all-English on a healthy site
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-RESEARCH.md` — § "Open Questions" #1 (clone depth vs. submodule pin resolution) and § "Assumptions Log" A2 (the path-based install shape) — the two questions this task's log excerpts answer
    - `.planning/ROADMAP.md` — § "Phase 30.1" success criterion 1 for the exact bar, and § "🚧 v0.6.4" → the "RTD-04 ownership" paragraph for why the documentation root is re-fetched in every phase rather than cited from Phase 29
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` — the file being appended to; read it so the append lands after Plan 03's sections without disturbing them
  </read_first>
  <action>
    Run three one-off live fetches with `curl` and record each command together with its full output,
    verbatim, appended to `30.1-EVIDENCE.md`. Alter nothing Plan 03 wrote.

    Fetch 1 — the primary content probe. GET
    `https://typsphinx.readthedocs.io/ja/latest/user_guide/builders.html` following redirects, capturing
    the body to a temp file and writing the status code, effective URL and downloaded size to stdout.
    Then assert on the captured body that it contains the exact msgstr for the source sentence
    "typsphinx provides two builders for different use cases." as it appears in
    `user_guide/builders.po` — transcribe that msgstr from the catalog, do not retype it from memory.
    Record the command, the status/URL/size line, and the matched line with a few lines of surrounding
    context. If Read the Docs serves this page under a directory-style URL instead, record the redirect
    and the effective URL actually used; either form is acceptable as long as the fetched body carries
    the string.

    Fetch 2 — the independent second probe. Same shape against
    `https://typsphinx.readthedocs.io/ja/latest/examples/basic.html`, asserting the msgstr for
    "Simple examples to get started with typsphinx." transcribed from `examples/basic.po`. Two
    independent docnames means a single fluke page cannot carry SC#1 on its own.

    Fetch 3 — the RTD-04 standing invariant, re-taken here rather than cited. GET
    `https://typsphinx.readthedocs.io/` following redirects, recording the full redirect chain (the
    status of each hop) plus the final effective URL and status.

    Write these as `## SC#1 — /ja/latest/ serves real Japanese` and
    `## RTD-04 — documentation root re-fetched`. Inside the SC#1 section, and **in the same section as
    the pass**, record the measured coverage: 257 of 1058 msgids translated (24.3%), with
    `user_guide/builders` at 65/65 and `examples/basic` at 30/30, and `api/index`, `contributing`,
    `changelog` and `user_guide/templates` at zero. State plainly that untranslated msgids fall back to
    English by Sphinx's normal behaviour, so English on those four pages is correct behaviour and not a
    defect — and that this is exactly why the probe targets the two docnames it does. Do not move this
    disclosure into a footnote or a later section.

    Then append `## SC#1 — ja first-build log excerpts`, recording the build's identity (detail URL,
    final status) exactly as the owner reported it, and the owner's three verbatim raw-log excerpts in
    fenced blocks. After each excerpt write one or two sentences of honest reading:
    - **Submodule** — did the checkout resolve the pinned commit Plan 03 recorded, and does the SHA in
      the log match it? This is RESEARCH Open Question #1 answered by measurement. Name the actual SHA
      found in the log; never assert a SHA that is not inside the recorded excerpt.
    - **`post_create_environment`** — did the removal run before the copy, and did the copy populate
      `typsphinx/docs/locale`? If the removal is absent from the log, say so: it is the mitigation for
      the stale compiled-catalog shadowing hazard and its absence is a finding, not a detail.
    - **Install** — what did the path-based pip entry actually do, and is there any import or autodoc
      error downstream of it? This is RESEARCH assumption A2 answered by measurement. If it failed, the
      correction is a `build.jobs.install` override running an editable install of `typsphinx[docs]` —
      record the failure and the proposed one-line correction rather than applying it silently.

    Also attempt to fetch the build's public raw log over HTTP from the reported detail URL, and record
    whichever way it goes. If retrievable, record the fetch command and then a scan of the full log for
    the strings `latexmk`, `pdflatex` and `.tex` with its count output — the same zero-LaTeX observation
    Phase 29 recorded for the parent, restated here for the ja manifest. If it is not retrievable,
    say so plainly and note that the excerpts rest on the owner's verbatim paste.

    If the network is unavailable in the execution environment, do NOT assert anything: record the
    attempted commands, their exact errors, mark the affected criteria `human_needed` with that reason,
    and stop. An honest abstention is required — never a passing claim without a recorded fetch. If a
    probe returns 200 but the Japanese string is absent, that is a **finding**: record the fetched body's
    relevant region verbatim and mark SC#1 failed. Do not substitute a different docname to make it pass.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib,re,subprocess,sys;po=pathlib.Path('docs/locale/ja/LC_MESSAGES/user_guide/builders.po').read_text(encoding='utf-8');po2=pathlib.Path('docs/locale/ja/LC_MESSAGES/examples/basic.po').read_text(encoding='utf-8');g=lambda t,k:re.search(r'msgid \"'+re.escape(k)+r'\"\n\s*msgstr \"([^\"]+)\"',t).group(1);s1=g(po,'typsphinx provides two builders for different use cases.');s2=g(po2,'Simple examples to get started with typsphinx.');u=lambda p:subprocess.run(['curl','-sS','-L','-w','\ncode=%{http_code} url=%{url_effective}\n',p],capture_output=True,text=True).stdout;b1=u('https://typsphinx.readthedocs.io/ja/latest/user_guide/builders.html');b2=u('https://typsphinx.readthedocs.io/ja/latest/examples/basic.html');b3=u('https://typsphinx.readthedocs.io/');ev=pathlib.Path('.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md').read_text(encoding='utf-8');chk=[('probe1_200','code=200' in b1),('probe1_ja',s1 in b1),('probe2_200','code=200' in b2),('probe2_ja',s2 in b2),('root_200','code=200' in b3),('sec_sc1','## SC#1 — /ja/latest/ serves real Japanese' in ev),('sec_log','## SC#1 — ja first-build log excerpts' in ev),('sec_root','## RTD-04 — documentation root re-fetched' in ev),('coverage_disclosed','257' in ev and '1058' in ev and 'user_guide/templates' in ev)];bad=[n for n,ok in chk if not ok];print('FAILED:',bad);sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `FAILED: []` and exits 0 — both Japanese probe strings are transcribed from the catalogs at check time and found in the served bodies, and all three fetches return 200
    - `30.1-EVIDENCE.md` § "SC#1 — /ja/latest/ serves real Japanese" contains both fetch commands verbatim, both `code=` / `url=` / `size=` output lines, and the matched Japanese line with surrounding context for each probe
    - That same section states `257`, `1058`, `24.3`, and names all four zero-coverage docnames, together with the sentence that untranslated msgids fall back to English by Sphinx's normal behaviour
    - The recorded probe URLs are `user_guide/builders` and `examples/basic` and no other docname
    - § "RTD-04 — documentation root re-fetched" records the full redirect chain and the final effective URL, and that URL names an existing version
    - § "SC#1 — ja first-build log excerpts" contains three fenced blocks holding the owner's verbatim excerpts, and each is followed by prose that names a token actually present inside that block — in particular a submodule SHA compared against the pin Plan 03 recorded
    - Either the raw-log HTTP fetch command and its output are recorded, or the section states plainly that the log was not retrievable and the excerpts rest on the owner's paste — one of the two, never neither
    - If any probe went red: the record shows the red result and the fetched body's relevant region verbatim, SC#1 is marked failed, and no substituted docname appears anywhere
    - `git diff -- .planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` shows only added lines
    - `git status --porcelain typsphinx/ docs/ docs/locale/ tests/ pyproject.toml .readthedocs.yaml .github/` is empty
  </acceptance_criteria>
  <done>Two independent 100%-coverage docnames are proven to serve their real Japanese msgstr values over live HTTP, the coverage is disclosed in the same breath, the documentation root still resolves, and the first build log has answered all three of its open questions from recorded text.</done>
</task>

<task type="auto">
  <name>Task 3: Record SC#2, SC#5, the three owed post-merge flips, and the D-15 gate</name>
  <files>.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` — § "SC#1" and § "RTD-04" from Task 2, which this task consolidates; § "D-15 gate — status after Wave 2" from Plan 03, whose remaining half this task closes
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VALIDATION.md` — § "Manual-Only Verifications", the SC#2 and SC#5 rows, for the exact evidence each expects
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-CONTEXT.md` — D-07 in full (lockstep tagging, the standing cost, and the fact that this phase discharges SC#5 by a recorded handoff rather than a serving URL)
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-01-PLAN.md` — § `<assumption_delta_decision>` (the release-set framing this handoff must use, and the explicit decision that Phase 33 owns carrying it into the publish procedure) and § `<planner_decisions>` PD-02 (the third owed flip)
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` — § "Phase 33 Handoff Precondition", so this phase's handoff section is written in the same shape and a Phase 33 reader finds both in one form
    - `.planning/STATE.md` — § "Accumulated Context": the standing honest-verifier rule (abstain to `human_needed` rather than assert a truth without direct evidence)
    - `.planning/ROADMAP.md` — § "Phase 30" → "Depends on", which is the statement this task's D-15 paragraph must satisfy
  </read_first>
  <action>
    Append four sections to `30.1-EVIDENCE.md`, altering nothing earlier plans or tasks wrote.

    **`## SC#2 — flyout and version activation (human_needed)`.** Record the owner's reported
    `flyout_on_en`, `flyout_on_ja`, `linked_under_parent` and `activated_versions` values verbatim,
    marked explicitly as owner-observed and **not** machine-verified. Then attempt one corroborating
    observation and record it whichever way it goes: fetch the parent project's public translations
    record from Read the Docs' v3 project data at
    `https://readthedocs.org/api/v3/projects/typsphinx/translations/` and record whether the ja project
    appears in it, with the command and the relevant portion of the response. Treat this strictly as
    corroborating evidence for an owner observation — it is a one-off read of a public record, not an
    integration, and nothing is committed that would fetch it again. If the fetch fails or the record is
    not public, say so plainly; SC#2 then rests on the owner's observation alone, which is what
    REQUIREMENTS.md already says it must.

    State explicitly, in prose, the failure mode this criterion exists to catch: creating both projects
    without the parent-side Translations link leaves two working but unswitchable sites, and nothing on
    either build page shows it.

    **`## SC#5 — /ja/stable/ handoff`.** Record: the ja project's Default Version is deliberately at
    `latest`; `stable` cannot exist before the `v0.6.4` tag because Read the Docs resolves `stable`
    against the tags of the repository each project builds, and `typsphinx-doc-translations` has no tag
    yet; therefore SC#5 is discharged by a recorded handoff, not by a serving URL, and `/ja/stable/` is
    explicitly **not** claimed to serve. Then write the handoff itself using the release-set framing:
    a typsphinx release tags every member of the set `{typsphinx, typsphinx-doc-translations}` with the
    same `vX.Y.Z`, and in the translations repository the submodule pin is bumped to the parent's release
    tag **before** its own tag is pushed. Name the consequence of skipping a member: `/ja/stable/` 404s
    rather than failing a build, so no CI signal exists. Note that the same wording is recorded in the
    translations repository's own `README.md` § "Release procedure", so the obligation lives with the
    repository that must honour it and not only in this repository's planning tree.

    **`## Handoffs`.** One list, each item marked not-yet-done and `human_needed`, so Phase 33 and the
    post-merge cleanup read a single place:
    1. Default Version `latest` → `stable` on the **parent** project, after the `v0.6.4` tag builds green
       (Phase 33; already recorded in `29-VERIFICATION.md` and restated here for completeness).
    2. Default Version and version activation re-checked on the **ja** project after the same tag —
       translation projects inherit nothing, so this is a second, separate action.
    3. Default branch `gsd/v0.6.4-read-the-docs-migration` → `main` on the **parent** RTD project, after
       the milestone merges.
    4. Default branch `gsd/v0.6.4-read-the-docs-migration` → `main` on the **ja** RTD project, after the
       milestone merges.
    5. `.gitmodules`' `branch` field in `typsphinx-doc-translations` → `main`, after the milestone
       merges, followed by one pin-bump run so the change takes effect. State the reason inline: while
       `main` carries no `.readthedocs.yaml`, tracking it would pin a tree Read the Docs refuses to
       build (measured 2026-07-26; PD-02).
    6. The release set from § SC#5, carried into the publish procedure by Phase 33.

    **`## D-15 gate — replacement confirmed serving`.** One self-contained paragraph, written so a
    Phase 30 reader needs nothing else from this file. State whether the Japanese site is now observed
    serving real Japanese prose (citing the SC#1 section by name and the two probe URLs), that the
    catalogs are relocated and proven byte-identical (citing Plan 03's fresh-clone section), and
    therefore whether Phase 30's deletions — the language switcher, its styling and `conf.py` wiring,
    `build_multilang.py`, the `docs/Makefile` locale and multilang targets, and `docs/locale/ja/` itself
    — are unblocked. If any part of SC#1 or SC#2 is open, say so and state plainly that the gate is
    **not** open; do not write a provisional pass. Note here, once, the deliberate and time-boxed
    divergence PD-01 creates: this repository's `docs/locale/ja/` and the translations repository's
    `locale/` will drift once Plan 05 adds new English source strings, and Phase 30's deletion commit is
    what closes it — no task in this phase may "fix" the drift by regenerating this repository's
    catalogs.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib,sys;t=pathlib.Path('.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md').read_text(encoding='utf-8');need=['## SC#2 — flyout and version activation (human_needed)','## SC#5 — /ja/stable/ handoff','## Handoffs','## D-15 gate — replacement confirmed serving','human_needed','release set','typsphinx-doc-translations','.gitmodules','gsd/v0.6.4-read-the-docs-migration','Phase 33','Phase 30'];miss=[s for s in need if s not in t];i=t.index('## Handoffs');tail=t[i:];items=[n for n in ['1.','2.','3.','4.','5.','6.'] if n in tail];print('MISSING:',miss,'HANDOFF_ITEMS:',len(items));sys.exit(1 if (miss or len(items)<6) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `MISSING: []` and `HANDOFF_ITEMS: 6`, and exits 0
    - § "SC#2" records the owner's four reported values verbatim, each marked owner-observed and carrying the literal `human_needed`, and states the two-working-but-unswitchable-sites failure mode in prose
    - § "SC#2" records either the corroborating public-record read with its command and relevant response portion, or a plain statement that it was unavailable — one of the two, never neither
    - § "SC#5" states that `/ja/stable/` is **not** claimed to serve, names the reason (no tag exists in the translations repository yet), and writes the handoff as a release set naming both repositories with the submodule bump ordered before the tag
    - § "Handoffs" contains exactly six numbered items, each marked not-yet-done and `human_needed`, including all three Default-branch/`.gitmodules` flips introduced by PD-02
    - § "D-15 gate — replacement confirmed serving" is a self-contained paragraph that cites the SC#1 section by name and both probe URLs, and states unambiguously whether Phase 30's deletions are unblocked — with no provisional pass if any part of SC#1 or SC#2 is open
    - That same paragraph records the PD-01 catalog divergence as deliberate and time-boxed, and states that no task in this phase may resolve it by regenerating this repository's catalogs
    - `git diff -- .planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` shows only added lines for this task
    - `git status --porcelain typsphinx/ docs/ docs/locale/ tests/ pyproject.toml .readthedocs.yaml .github/` is empty
  </acceptance_criteria>
  <done>SC#2 is recorded honestly as owner-observed with a corroborating read where one is available, SC#5 is discharged as a release-set handoff rather than a claimed URL, all six owed handoffs sit in one list, and Phase 30 has an unambiguous gate paragraph to read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RTD web UI (owner session) → RTD's project database | Owner-manual configuration crossing into a third-party system this repository cannot assert against. Its outcome is observable only through served bytes |
| RTD ja build container → the served `/ja/latest/` HTML | Where a language resolution failure becomes invisible: the build reports success either way, and only the served text distinguishes them |
| RTD build log (public) → this repository's evidence record | Unvalidated third-party text pasted into `.planning/`. It is recorded verbatim as evidence and never executed |

## STRIDE Threat Register

ASVS L1, block on `high`. Reused from `30.1-RESEARCH.md` § "Security Domain" plus this phase's own
register; V2/V3/V4/V5/V6 remain not applicable — this plan adds no authentication surface, no session,
no access control, no user input processing and no cryptography.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30.1-05 | Tampering | A stale compiled catalog inside the submodule checkout shadowing the copied `.po` files, so a green ja build serves stale or English text | high | mitigate | The manifest's `post_create_environment` removes `typsphinx/docs/locale` before repopulating it (Plan 01). This plan is where the mitigation is *verified*: Task 2 reads the removal and copy lines out of the real build log, and Task 2's content probes are the outcome-level check that would go red if it failed |
| T-30.1-08 | Spoofing | A 200 response serving a Read the Docs placeholder, a redirect to the English site, or a cached page being read as proof that Japanese is served | medium | mitigate | Every probe asserts a specific msgstr transcribed from the catalog at check time, records the effective URL after redirects, and uses two independent docnames. A placeholder, an English page, or a redirect to `/en/` all fail the string assertion |
| T-30.1-09 | Denial of Service | Version activation missed on the ja project, so `/ja/latest/` 404s despite a successful build; or Default Version pointing at a version with no build | medium | mitigate | Verified by outcome rather than by dashboard reading: Task 2's fetches would fail on a 404, and Task 3 records the activated-version list as owner-reported alongside the root re-fetch (RTD-04 standing invariant) |
| T-30.1-06 | Information Disclosure | RTD build logs are public for a public project; excerpts are pasted into `.planning/` | low | accept | Neither manifest nor workflow references a secret, so nothing in this build's log can be disclosed. Standing rule carried from Phase 29: no secret may ever be echoed into a `build.jobs` command |
| T-30.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. Zero new packages phase-wide (milestone invariant #1); `30.1-RESEARCH.md` § "Package Legitimacy Audit" records `sphinx-intl` and `pypdf` as pre-existing with verdict OK and no `[ASSUMED]`/`[SUS]`/`[SLOP]` rows, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
1. Both `/ja/latest/` probes return 200 and their served bodies contain msgstr values transcribed from the catalogs at check time
2. The probe docnames are `user_guide/builders` and `examples/basic` — never a zero-coverage docname
3. The measured coverage (257/1058, 24.3%, four docnames at zero) is recorded in the same section as the pass
4. The documentation root still resolves over real HTTP, with the redirect chain recorded (RTD-04 standing invariant)
5. The ja first-build log answers all three open questions from recorded verbatim text, each reading naming a token actually present in its excerpt
6. SC#2 is recorded as owner-observed with the literal `human_needed`, corroborated where a public record allows
7. SC#5 is a recorded release-set handoff; `/ja/stable/` is explicitly not claimed to serve
8. `## Handoffs` lists all six owed actions, each not-yet-done
9. `## D-15 gate — replacement confirmed serving` states unambiguously whether Phase 30 is unblocked
10. `git status --porcelain typsphinx/ docs/ docs/locale/ tests/ pyproject.toml .readthedocs.yaml .github/` is empty
</verification>

<success_criteria>
- The Japanese RTD project exists against the translations repository, with Language = Japanese, linked under the English parent, versions activated, Default Version `latest` — all owner-reported and recorded as such
- `/ja/latest/` is proven over live HTTP to serve real Japanese msgstr values on two independent 100%-coverage docnames
- The measured 24.3% coverage is disclosed alongside the pass, so the pass cannot be over-read
- The documentation root resolves, re-taken in this phase rather than cited
- The first build log has answered the submodule-resolution and install-shape questions from recorded text
- SC#2 and SC#5 are recorded honestly — owner-observed and handoff-discharged respectively, never asserted
- Phase 30 has one unambiguous paragraph telling it whether its deletions are unblocked
</success_criteria>

<output>
Create `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-04-SUMMARY.md` when done.

The SUMMARY must state the two probe URLs with their HTTP status and whether each Japanese string
matched, the measured coverage figure, the ja project's slug and Default Version, and — plainly — the
D-15 gate verdict. It must not restate an owner-reported web-UI value as though a command confirmed it.
</output>
