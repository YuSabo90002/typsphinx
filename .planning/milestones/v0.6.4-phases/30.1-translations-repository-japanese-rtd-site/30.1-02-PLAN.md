---
phase: 30.1-translations-repository-japanese-rtd-site
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml
  - tests/test_readthedocs_config.py
autonomous: true
requirements: [I18N-01, I18N-03]
must_haves:
  truths:
    - "The staged pin-bump workflow declares `permissions: contents: write` at the workflow level — the single least-privilege scope its `git push` needs, explicitly written rather than inherited from a repository-wide read-write default (T-30.1-01)"
    - "The staged workflow's triggers are exactly `schedule` and `workflow_dispatch`; it carries no `repository_dispatch` trigger, because wiring one would require a `.github/workflows/` change in this repository, which is outside this phase's file-touch list"
    - "The staged workflow's commit decision is true when the submodule gitlink moved OR when a `.po` diff survives the `POT-Creation-Date` header filter — a `.po`-only filter would leave a moved pin uncommitted, which is the exact staleness the workflow exists to prevent"
    - "The staged workflow contains no Transifex coupling: no `TX_TOKEN`, no `tx` CLI install, no `lock-translations` / `generate_templates` / `update.sh` invocation (D-08 forbids copying `sphinx-doc-translations`'s `main.yml` literally)"
    - "The staged workflow uses this repository's measured GitHub Actions conventions — `actions/checkout@v7` and `astral-sh/setup-uv@v7`, not the `@v4`/`@v6` pins named in RESEARCH.md's illustrative example"
    - "`tests/test_readthedocs_config.py`'s numbered no-language-flag block cites `D-04` and `D-05` of Phase 30.1 and names `typsphinx-doc-translations` as where the Japanese PDF actually comes from, in both its comment and its assertion message"
    - "Every executable line of `tests/test_readthedocs_config.py` is byte-unchanged: the `assert ` count stays at its measured 39, all four `def test_` functions remain, and the full suite is green afterwards"
  prohibitions:
    - statement: "The rationale amendment MUST NOT change, weaken, reorder, or delete any executable line of `tests/test_readthedocs_config.py`. Removing coverage under cover of a documentation edit is precisely the class milestone invariant #5 exists to catch, and here the assertion is still true and still useful — only its stated reason went stale"
      status: required
      verification: "`git diff -U0 -- tests/test_readthedocs_config.py` shows changed lines that are exclusively comment lines or lines inside the assertion's message string; the `assert ` count is still 39 and `def test_` is still 4; `uv run pytest -q` is green"
    - statement: "The pin-bump workflow MUST NOT be granted any permission scope beyond `contents: write`, and MUST NOT reference a personal access token or any repository secret. It pushes to its own repository, which the default token at `contents: write` already permits; a broader scope on a scheduled, unattended job hands it more authority than its one job needs"
      status: required
      verification: "The staged workflow's top-level `permissions` mapping equals exactly `{contents: write}`, and the file contains no `secrets.` reference at all"
  artifacts:
    - ".planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml — NEW (staged; workflow display name `Update translation source pin`)"
    - "tests/test_readthedocs_config.py — MODIFIED (comment and assertion-message text only)"
  key_links:
    - "`.gitmodules`' `branch` field → `git submodule update --remote typsphinx` → the pin actually moving. Without an explicit `branch`, `--remote` tracks whatever branch the submodule's HEAD pointed at when it was added (RESEARCH Pitfall 2), which for this milestone would silently be the wrong one"
    - "The workflow's `git push` → the translations repository's own RTD build → `/ja/latest/`. A push made with the default workflow token deliberately does not re-trigger GitHub Actions (loop prevention), but RTD rebuilds on the push regardless, because RTD watches the repository rather than the Actions run"
    - "`tests/test_readthedocs_config.py`'s no-language-flag block → the parent manifest's sole `sphinx-build`. The assertion stays true after this phase precisely because the Japanese PDF is produced by a different manifest in a different repository — the amendment records that, and changing the assertion would break a still-correct guard"
---

<objective>
Author the D-08 submodule pin-bump workflow (staged for `typsphinx-doc-translations`) and amend the one
collateral rationale in this repository that Phase 29 left stale (D-05).

Purpose: without the pin-bump workflow the Japanese site serves translations of a frozen English source
while every build reports success — I18N-01's failure mode arriving through a different door than
mistranslation. And without the D-05 amendment, a still-correct assertion in this repository carries a
justification this phase has just falsified, which is exactly the docs-versus-reality drift this
project's core value targets.

Output: one staged workflow file, and a text-only amendment to `tests/test_readthedocs_config.py` whose
executable lines are byte-unchanged.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-CONTEXT.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-RESEARCH.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-PATTERNS.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VALIDATION.md
</context>

<artifacts_this_phase_produces>
See `30.1-01-PLAN.md` § `<artifacts_this_phase_produces>` for the full phase-level list covering both
repositories, the RTD-side symbols, and the procedure symbols. This plan adds two of them:

- `.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml`
  — NEW, staged. Destined for `typsphinx-doc-translations/.github/workflows/update-pin.yml`. Its
  display name, **`Update translation source pin`**, is an interface: Plan 05 triggers it by that exact
  name with `gh workflow run`.
- `tests/test_readthedocs_config.py` — MODIFIED in this repository, text only.

The staged copy under `.planning/` is inert: GitHub reads workflows only from `.github/workflows/` at a
repository root, so nothing in this repository executes it.
</artifacts_this_phase_produces>

<flagged_probe_assumptions>
Both unresolved spec-less edge-probe rows (I18N-01 / `unclassified`, I18N-03 / `unclassified`) are
enumerated in full in `30.1-01-PLAN.md` § `<flagged_probe_assumptions>`, together with the
no-silent-drop equality tallies for edges (2 == 0 authored + 2 flagged) and prohibitions (7 kept == 7
authored + 0 flagged) and the canon-referral breadcrumb. The row bearing on this plan, restated so the
plan is self-contained:

- **I18N-01 / `unclassified` — UNRESOLVED.** The probe could not classify it and per §A it is not
  auto-`backstop`ped. This plan attempts no probe-derived predicate; it addresses the *staleness* half
  of I18N-01's failure mode structurally — a workflow that advances the pin — and leaves the
  served-content half entirely to Plan 04's live fetch and Plan 05's propagation evidence.
</flagged_probe_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Author the D-08 submodule pin-bump workflow (staged)</name>
  <files>.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml</files>
  <read_first>
    - `.github/workflows/drift.yml` (this repository, all of it) — the scheduled-job skeleton: an `on.schedule` + `on.workflow_dispatch` dual trigger, a top-level `permissions:` block written out explicitly, one job with a descriptive `name:`
    - `.github/workflows/ci.yml` lines 20-45 — this repository's standing runner-provisioning convention, repeated identically across 4 of its 5 jobs: `actions/checkout@v7`, then `astral-sh/setup-uv@v7` with `version: "latest"`, then `uv python install 3.12`. Match the action versions and step names exactly; RESEARCH.md's illustrative example names `@v4`/`@v6`, which are **not** this repository's measured pins
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-RESEARCH.md` — § "Code Examples" → "D-08 pin-bump GitHub Actions workflow" for the target step sequence; § "Common Pitfalls" Pitfall 1 for the `POT-Creation-Date` churn filter and its exact grep pipeline; § "Common Pitfalls" Pitfall 2 for why `.gitmodules` needs an explicit `branch`; § "Security Domain" for the `contents: write` scoping requirement
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-CONTEXT.md` — D-08 (the skeleton minus Transifex, and the explicit instruction not to copy `sphinx-doc-translations`'s `main.yml` literally); § "Claude's Discretion" (the trigger choice is planning's call)
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/Makefile` — if Plan 01 has already landed it, read it so the workflow's command sequence stays the same two verbs in the same order as the human entry point
    - `pyproject.toml` § `[project.optional-dependencies]` — confirm the `docs` extra provides `sphinx-intl`; the runtime `sphinx` dependency is what provides `sphinx-build`
  </read_first>
  <action>
    Create `.github/workflows/` inside the staging directory
    `.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/` and write
    `update-pin.yml` into it. It is destined for `typsphinx-doc-translations`; nothing in this
    repository runs it.

    Target state:

    - Workflow `name:` — `Update translation source pin`. Plan 05 triggers it by this exact display
      name, so treat it as an interface, not a label.
    - Triggers — `schedule` with cron `0 6 * * *` (daily, so the Japanese site is at most 24 hours
      behind the English source), plus `workflow_dispatch` with no inputs so a run can be started by
      hand for the SC#3 demonstration. Add **no** `repository_dispatch` trigger: RESEARCH resolved this
      discretion item — a parent-side dispatch would require a `.github/workflows/` edit in *this*
      repository, outside this phase's file-touch list, and would couple two milestone phases' commits.
      Comment that reasoning inline.
    - Top-level `permissions:` — a mapping with the single key `contents` set to `write`, written
      explicitly. Comment that the default workflow token at this scope suffices to push to the **same**
      repository, that no personal access token or repository secret is needed, and that the block is
      spelled out rather than inherited so a repository-wide read-write default cannot silently widen it.
    - One job with id `update-pin`, a descriptive `name:`, and `runs-on: ubuntu-latest`.
    - Steps, in order:
      1. `actions/checkout@v7` with `submodules: true` — without this the `typsphinx/` directory arrives
         empty and every later step fails.
      2. `astral-sh/setup-uv@v7` with `version: "latest"`, step name `Install uv`.
      3. `uv python install 3.12` via `run:`, step name `Set up Python`.
      4. `git submodule update --remote typsphinx` — the pin advance. Comment that the branch this
         follows comes from `.gitmodules`' `branch` field, not from whatever was checked out when the
         submodule was added, and that it is a different knob from RTD's Admin "Default branch" setting.
      5. Create a virtual environment with `uv venv`, then install the submodule with its docs extra via
         `uv pip install` of `./typsphinx[docs]`. Comment that this installs only what the submodule's
         own `pyproject.toml` already declares — the workflow introduces no package of its own.
      6. Regenerate the templates: invoke `sphinx-build` from the created environment's `bin` directory
         with `-M gettext`, source `typsphinx/docs/source`, build directory `typsphinx/docs/_build`.
      7. Merge into the catalogs: invoke `sphinx-intl update` from the same `bin` directory with
         `-p typsphinx/docs/_build/gettext`, `-d locale`, `-l ja`.
      8. A decision step with `id: decide` writing **two** outputs to `$GITHUB_OUTPUT`: `pin_moved` —
         `1` when `git diff --quiet -- typsphinx` reports a difference, else `0`; and `po_changed` — the
         count of `locale` diff lines that survive filtering out the `+++`/`---` file markers and any
         line mentioning the `POT-Creation-Date` header field. Comment the measured reason for the
         filter: `sphinx-intl update` calls `msgmerge`, which rewrites that header on every run even
         when no msgid or msgstr changed, so an unfiltered "any diff, commit" rule produces a
         content-free commit on every scheduled run.
      9. A commit-and-push step guarded by an `if:` that fires when **either** output is non-zero. Set
         `git config` user name and email to the `github-actions[bot]` identity, `git add` both
         `typsphinx` and `locale`, commit with a conventional-commit `chore:` subject naming both the
         pin advance and the catalog resync, and push. Comment the load-bearing subtlety: the OR is
         deliberate — a pin that moved with no msgid change **must still be committed**, because Read
         the Docs builds the submodule commit this repository has recorded, so a `.po`-only condition
         would leave the Japanese site permanently frozen at the first pin.
      10. A cleanup step guarded by the complementary `if:` (both outputs zero) running
         `git checkout -- locale` and echoing that only header churn was seen, so the working tree is
         left clean and the run's own log states plainly why nothing was pushed.

    Include no Transifex artefact: no token variable named for Transifex, no `tx` CLI installation, and
    no `lock-translations`, `generate_templates` or `update.sh` invocation. Add no matrix, no artifact
    upload, and no failure-reporting issue step — this repository's `drift.yml` has one, but D-08's
    failure mode is a job that does not run rather than a job that fails loudly, and an issue step would
    add an `issues: write` scope the workflow does not otherwise need.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import yaml,sys;p='.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml';raw=open(p,encoding='utf-8').read();d=yaml.safe_load(raw);trig=d.get('on',d.get(True));job=d['jobs']['update-pin'];steps=job['steps'];uses=[s.get('uses','') for s in steps];runs=' | '.join(s.get('run','') for s in steps);ifs=' | '.join(str(s.get('if','')) for s in steps);chk=[('name',d['name']=='Update translation source pin'),('schedule','schedule' in trig),('cron',trig['schedule'][0]['cron']=='0 6 * * *'),('dispatch','workflow_dispatch' in trig),('no_repo_dispatch','repository_dispatch' not in trig),('perms',d['permissions']=={'contents':'write'}),('checkout',any(u.startswith('actions/checkout@v7') for u in uses)),('submodules',any(s.get('with',{}).get('submodules') for s in steps if 'checkout' in s.get('uses',''))),('setup_uv',any(u.startswith('astral-sh/setup-uv@v7') for u in uses)),('no_old_pins','actions/checkout@v4' not in raw and 'actions/setup-python' not in raw),('remote','git submodule update --remote typsphinx' in runs),('gettext','-M gettext' in runs and 'typsphinx/docs/source' in runs),('intl','sphinx-intl update' in runs and '-d locale' in runs and '-l ja' in runs),('pin_out','pin_moved' in runs),('po_out','po_changed' in runs),('pot_filter','POT-Creation-Date' in runs),('or_guard','pin_moved' in ifs and 'po_changed' in ifs),('push','git push' in runs),('no_tx',not any(tok in raw for tok in ['TX_TOKEN','lock-translations','generate_templates','update.sh'])),('no_secrets','secrets.' not in raw)];bad=[n for n,ok in chk if not ok];print('FAILED:',bad);sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `FAILED: []` and exits 0
    - The workflow's top-level `permissions` mapping equals exactly `{contents: write}` — no `issues`, no `pull-requests`, no `packages`
    - The commit step's `if:` expression references **both** `pin_moved` and `po_changed` and fires when either is non-zero; the cleanup step's `if:` fires only when both are zero
    - The `decide` step's filter drops `+++`/`---` file markers and lines mentioning the `POT-Creation-Date` header before counting
    - `actions/checkout@v7` is present with `submodules: true`, `astral-sh/setup-uv@v7` is present, and the file contains neither `actions/checkout@v4` nor any `actions/setup-python` step
    - The file contains no Transifex artefact and no `secrets.` reference
    - The file contains an inline comment stating why the commit condition is an OR rather than a `.po`-only test
    - `git status --porcelain typsphinx/ docs/ pyproject.toml .readthedocs.yaml .github/` is empty — no workflow in **this** repository was added or changed; `git status --porcelain docs/locale/` is empty
  </acceptance_criteria>
  <done>A staged `update-pin.yml` advances the submodule against its declared tracked branch, resyncs the ja catalogs, and commits only on a real change — with least-privilege permissions and no Transifex coupling.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the superseded rationale in tests/test_readthedocs_config.py (D-05)</name>
  <files>tests/test_readthedocs_config.py</files>
  <read_first>
    - `tests/test_readthedocs_config.py` lines 255-285 — the numbered comment block and the two assertions it guards. Read the surrounding numbered-comment convention (one behaviour per numbered block, `pdf_commands` as the shared fixture-derived variable) so the amended text matches the file's own style
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-PATTERNS.md` — § "`tests/test_readthedocs_config.py:262-280` *(modified, this repository)*", which quotes the exact current text and states the instruction precisely: amend only the numbered comment and the assertion message's final clause; every line of executable code stays byte-identical
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-CONTEXT.md` — D-05 in full (what is superseded, what stays true, and why this edit belongs to Phase 30.1 rather than to Phase 30's deletion set), plus D-01/D-04 for why the Japanese PDF is now a shipped deliverable
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-RESEARCH.md` — § "State of the Art" → "Deprecated/outdated", which names this text as the one artefact still carrying the reversed framing
    - `.planning/REQUIREMENTS.md` — I18N-03's v1 entry, whose "Promoted from Future to v1 on 2026-07-26" wording is the authoritative statement the amended text must agree with
  </read_first>
  <action>
    Amend two pieces of **text** in `tests/test_readthedocs_config.py`, inside the numbered block that
    asserts the parent manifest carries exactly one `sphinx-build` invocation and that it bears no
    locale or language flag. Both pieces currently justify the assertion by claiming the CJK font
    package is not a step toward a Japanese PDF the project has postponed. That justification is now
    false: the Japanese PDF is a shipped v1 deliverable of this phase.

    Piece one — the numbered comment immediately above the list comprehension. Rewrite its trailing
    justification so it states the reason that is now true: the parent project's PDF build is English,
    and the Japanese PDF is produced by a **separate manifest in the `typsphinx-doc-translations`
    repository**, whose language is supplied by that project's own Read the Docs Admin Language setting
    through the `READTHEDOCS_LANGUAGE` environment variable — never by a flag on this manifest's
    `sphinx-build`. Cite `D-04` and `D-05` of Phase 30.1 as the decisions that changed the reason.

    Piece two — the trailing clause of the second assertion's message string. Apply the same
    substitution: the message should still explain *why* a language flag would be wrong on this
    manifest, but the explanation is now that the Japanese PDF ships from a different manifest in a
    different repository (Phase 30.1, D-04/D-05) rather than that it has been postponed. Keep the
    interpolated `{sole_command!r}` at the end of the message exactly where it is.

    Hard constraints on the diff:
    - Both `assert` statements, the list comprehension that builds `sphinx_build_indices`, the
      `sole_command` assignment, and all three boolean sub-conditions stay **byte-identical**. The
      assertion is still true and still useful; only its stated reason went stale. Measured baseline for
      the whole file: 39 occurrences of `assert ` and 4 `def test_` functions — both counts must be
      identical after the edit.
    - Do not renumber the block, do not add or remove a numbered block, and do not touch any other test
      in the file.
    - Do not modify the module docstring at the top of the file, and do not modify
      `test_language_seam_precedence` — its `html_context["language"]` assertions are **Phase 30's**
      collateral (repointed there when the switcher wiring is deleted), not this phase's.
    - Respect the file's formatting: `black` line length 88, `ruff check` clean.

    After the edit, run the whole test suite, not just this module — the amendment is text-only, so a
    red suite anywhere means something unintended moved. Invoke every tool through the module form
    (`uv run python -m pytest`, `uv run python -m black`, `uv run python -m ruff`) rather than the
    console-script form: this project's sandbox has been observed to fail on `uv run <compiled-binary>`,
    and the module form is the shape Phase 29 proved works here.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev >/dev/null 2>&1; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib,sys;t=pathlib.Path('tests/test_readthedocs_config.py').read_text(encoding='utf-8');keep=['sphinx_build_indices = [','assert len(sphinx_build_indices) == 1','sole_command = pdf_commands[sphinx_build_indices[0]]','{sole_command!r}'];chk=[('cites_d04','D-04' in t),('cites_d05','D-05' in t),('cites_phase','30.1' in t),('names_other_repo','typsphinx-doc-translations' in t),('no_stale_id',t.count('D-11')==0),('assert_count',t.count('assert ')==39),('test_count',t.count('def test_')==4),('cond_count',t.count('not in sole_command')==3),('exec_intact',all(s in t for s in keep))];bad=[n for n,ok in chk if not ok];print('FAILED:',bad);sys.exit(1 if bad else 0)" && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest -q && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m black --check tests/test_readthedocs_config.py && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m ruff check tests/test_readthedocs_config.py</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `FAILED: []`, the full pytest suite is green, and both `black --check` and `ruff check` exit 0
    - The file contains `D-04`, `D-05`, `30.1` and `typsphinx-doc-translations`, and zero occurrences of the superseded Phase 29 decision identifier the verify command counts
    - The `assert ` count is exactly 39 and the `def test_` count is exactly 4 — identical to the pre-edit measurement
    - `git diff -U0 -- tests/test_readthedocs_config.py` shows changed lines that are exclusively comment lines or lines inside the assertion's message string — no changed line contains `assert`, `=`, `for`, or `if` as executable syntax
    - `test_language_seam_precedence` is byte-unchanged, and the module docstring is byte-unchanged
    - `git status --porcelain typsphinx/ docs/ pyproject.toml .readthedocs.yaml .github/` is empty; `git status --porcelain docs/locale/` is empty
  </acceptance_criteria>
  <done>The stale rationale is replaced by the one that is now true, the assertion it guards is byte-identical, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → the translations repository's git history | A scheduled, unattended job holds a write-scoped token and pushes commits nobody reviews before they land |
| Parent repository's tracked branch → the submodule pin the workflow advances to | `--remote` follows a branch tip; whatever is at that tip becomes what RTD builds and serves as Japanese documentation |

## STRIDE Threat Register

ASVS L1, block on `high`. T-30.1-01 is carried from `30.1-RESEARCH.md` § "Security Domain"; the trigger
choice and secrets posture were re-checked against it during planning and add nothing new.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30.1-01 | Elevation of Privilege | The pin-bump workflow's `GITHUB_TOKEN` permissions | high | mitigate | A top-level `permissions:` block scoped to exactly `contents: write` is written into the workflow rather than inherited from a repository-wide read-write default; no personal access token and no repository secret is referenced; the only two actions used are the first-party `actions/checkout@v7` and this repository's already-trusted `astral-sh/setup-uv@v7`. Task 1's verify command asserts the mapping equality and the absence of any `secrets.` reference |
| T-30.1-02 | Tampering | The submodule pin advancing on a schedule to unreviewed parent content | medium | accept | The tracked branch lives in the same owner's repository and the same trust domain; the parent's branch-protection posture governs it and is unchanged by this phase. The tracked branch is named explicitly in `.gitmodules` rather than inherited from whatever was checked out at add time (RESEARCH Pitfall 2) |
| T-30.1-06 | Information Disclosure | GitHub Actions run logs are public for a public repository | low | accept | The workflow references no secret and echoes no credential; its loudest output is a diff-line count. Standing rule carried from Phase 29: no secret may ever be echoed into a `run:` command |
| T-30.1-SC | Tampering | npm/pip/cargo installs | low | accept | The only install this plan authors is `./typsphinx[docs]` — the submodule's own `pyproject.toml`, already trusted by this repository's CI. Zero new packages phase-wide (milestone invariant #1); `30.1-RESEARCH.md` § "Package Legitimacy Audit" records `sphinx-intl` and `pypdf` as pre-existing with verdict OK and no `[ASSUMED]`/`[SUS]`/`[SLOP]` rows, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
1. The staged `update-pin.yml` parses and satisfies all 20 structural assertions in Task 1's verify command
2. Its `permissions` mapping is exactly `{contents: write}` and it references no secret
3. Its commit condition is an OR over `pin_moved` and `po_changed`, with the `POT-Creation-Date` filter applied to the `.po` half
4. `tests/test_readthedocs_config.py` cites D-04/D-05 and `typsphinx-doc-translations`, with `assert ` still at 39 and `def test_` still at 4
5. The full `uv run pytest -q` suite is green, and `black --check` / `ruff check` pass on the amended file
6. `git status --porcelain typsphinx/ docs/ docs/locale/ pyproject.toml .readthedocs.yaml .github/` is empty
</verification>

<success_criteria>
- A staged pin-bump workflow exists that advances the submodule, resyncs the ja catalogs, and commits only on a real change
- Its permission scope is the minimum its push needs, written explicitly
- It carries no Transifex coupling and no `repository_dispatch` trigger
- The superseded rationale in this repository is replaced by the one that is now true, with every executable line byte-identical and the full suite green
- Nothing under `typsphinx/`, `docs/`, `docs/locale/` or this repository's `.github/` is modified
</success_criteria>

<output>
Create `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-02-SUMMARY.md` when done.

The SUMMARY must state the workflow's display name (Plan 05 depends on it), the cron and trigger set
chosen, and — for the test amendment — the pre- and post-edit `assert ` counts, so a reader can see at a
glance that no coverage moved.
</output>
