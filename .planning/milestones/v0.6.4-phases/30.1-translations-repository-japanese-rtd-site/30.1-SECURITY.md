---
phase: 30.1
slug: translations-repository-japanese-rtd-site
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: 2026-07-26
---

# Phase 30.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

Register origin: authored at plan time — all 11 PLAN.md files carry a parseable `<threat_model>`
block. Classification ran at ASVS L1 (grep depth) with `block_on: high`. Zero open threats at or
above threshold → short-circuit path (no auditor spawn required).

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| typsphinx ↔ typsphinx-doc-translations | New public repo pins the parent as a submodule; pin-bump workflow pushes back | Public source, gitlink SHA |
| Repos ↔ RTD build containers | `.readthedocs.yaml` `build.jobs` commands execute in an uninspectable remote container | Build commands, public artifacts |
| GitHub Actions (unattended) | `update-pin.yml` runs on schedule with `contents: write` | Scoped GITHUB_TOKEN, submodule pin |
| Local machine ↔ downloaded PDFs | Verification parses untrusted RTD-served PDFs with pypdf | PDF bytes (read-only, untracked) |
| Executor environment | `gh`/git credentials present during pushes and API reads | Credentials (never echoed/recorded) |

---

## Threat Register

Consolidated from the 11 plans' `<threat_model>` registers (per-plan duplicates of the same
threat ID merged; the strongest phrasing kept).

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-30.1-01 | Elevation of Privilege | Pin-bump workflow GITHUB_TOKEN | high | mitigate | `permissions:` scoped to exactly `contents: write` (grep-confirmed `update-pin.yml:21-26`); only first-party `actions/checkout@v7` + trusted `astral-sh/setup-uv@v7`; zero `secrets.` references | closed |
| T-30.1-02 | Tampering | Submodule pin advancing to unreviewed parent content | medium | accept | Same owner, same trust domain; tracked branch named explicitly in `.gitmodules` (`branch = gsd/v0.6.4-read-the-docs-migration`, evidence §pin) | closed |
| T-30.1-03 | Tampering | Silent Typst font fallback across 94-page ja PDF | high | mitigate | D-03 four-check bar all green: page count, extracted text, `/BaseFont` CJK enumeration (VERIFICATION.md), and owner visual glyph confirmation (30.1-UAT.md test 1: pass, 2026-07-26) | closed |
| T-30.1-04 | Spoofing | HTML error page / stale artifact read as the RTD PDF | medium | mitigate | Effective URL, byte size, SHA-256 and PDF signature recorded before treating bytes as PDF (EVIDENCE.md) | closed |
| T-30.1-05 | Tampering | Stale tracked `.mo` catalogs shadowing copied `.po` | high | mitigate | `post_create_environment` does `rm -rf` → `mkdir -p` → `cp -a` in order (grep-confirmed `.readthedocs.yaml:48-56`); removal/copy lines re-read from the real build log; content probes green | closed |
| T-30.1-06 | Information Disclosure | Public build logs / repos / artifacts | low | accept | Everything published is already public; no secret referenced or echoed (Phase 29 standing rule held phase-wide) | closed |
| T-30.1-07 | Repudiation | History rewrite leaving recorded pin dangling | medium | mitigate | Pin SHA + subject + author date recorded verbatim and re-observed from an independent clone; fast-forward-only pushes, no force flag | closed |
| T-30.1-08 | Spoofing | Placeholder/English/cached page read as ja being served | medium | mitigate | Probes assert specific catalog msgstr, record effective URL, use two independent docnames | closed |
| T-30.1-09 | Denial of Service | ja version not activated → `/ja/latest/` 404 | medium | mitigate | Verified by outcome — fetches would fail on 404; activated-version list recorded (RTD-04 invariant) | closed |
| T-30.1-10 | Spoofing | Cached `/ja/latest/` read as propagation | medium | mitigate | Probe string did not exist before the plan's own commit; effective URL recorded; producing build identified | closed |
| T-30.1-11 | Spoofing | English PDF served on the ja download path | high | mitigate | Diagnostic page-count check (93 en vs 94 ja) plus independent text comparison | closed |
| T-30.1-12 | Tampering | Parsing untrusted downloaded PDFs locally | low | accept | pypdf used for metadata/text only; nothing executed, interpolated, or committed; files stay outside the repo | closed |
| T-30.1-13 | Elevation of Privilege | Unattended `contents: write` job pushing to external repo | high | mitigate | Patch added read-only fetch + echoes only; permissions asserted unchanged; pushed commit's full `git show --stat` recorded from independent clone | closed |
| T-30.1-14 | Tampering | Authored commands in uninspectable remote build container | high | mitigate | Diagnostic block read-only (`fc-list`, `python -c`); no network fetch/pipe/package install permitted; marked temporary with removing plan named; live copy proven byte-identical | closed |
| T-30.1-15 | Spoofing | Stale/wrong PDF handed to owner as artifact under test | high | mitigate | In-session re-download with status/URL/size/SHA-256 recorded and compared to Plan 06's value; UAT expected-block pinned SHA-256 23885dcd… | closed |
| T-30.1-17 | Information Disclosure | `gh`/git credentials in executor environment | medium | mitigate | No token printed or written; recorded transcripts checked for credential material before append | closed |
| T-30.1-18 | Repudiation | Owner observation vs machine measurement conflated | high | mitigate | Owner-reported values recorded verbatim and attributed; disagreements keep both values with UNRESOLVED marker (e.g. `linked_under_parent` both-values record) | closed |
| T-30.1-19 | Tampering | `.gitmodules` `branch` value interpolated in privileged job | medium | mitigate | Read via `git config -f .gitmodules`, used only inside explicit refspec, verified with `git rev-parse --verify`; file writable only by someone already having push access | closed |
| T-30.1-20 | Tampering | Unreviewed change reaching translations repo via verification | high | mitigate | Verification clones are scratch dirs; fresh clone asserted tip carries only the deliberate workflow-patch commit | closed |
| T-30.1-21 | Tampering | Pin advanced to unintended commit (rewrite/race) | medium | mitigate | New gitlink reconciled against freshly fetched origin branch; both SHAs and ancestry recorded | closed |
| T-30.1-22 | Spoofing | Stale response / old-commit build counted as the rebuild | high | mitigate | Build matched on `commit` field; raw log's resolved submodule SHA compared to new gitlink; probe string new this round | closed |
| T-30.1-23 | Denial of Service | Repeated speculative workflow triggers on external repo | low | mitigate | Max three trigger cycles, each with recorded diagnosis; deterministic failure recorded as defect, not retried | closed |
| T-30.1-24 | Tampering | Font asset with unverified provenance entering a build | high | mitigate | No font binary committed or fetched from arbitrary URL; only distribution packages via `build.apt_packages` or fonts already on the container | closed |
| T-30.1-25 | Tampering | Doc-side change regressing the Phase 29-verified English PDF | high | mitigate | English artifact measured before/after (pages, text, NUL, fonts); full test suite + docs build run; otherwise `git status --porcelain docs/` asserted empty | closed |
| T-30.1-26 | Spoofing | Post-fix artifact byte-identical to pre-fix compared as new | high | mitigate | SHA-256 compared against pre-fix value; identity treated as a stopping finding (post-fix SHA 23885dcd… ≠ pre-fix 4eb8497e…) | closed |
| T-30.1-SC | Tampering | npm/pip/cargo supply chain | low | accept | Zero new packages phase-wide (milestone invariant #1); `sphinx-intl`/`pypdf` pre-existing, RESEARCH Package Legitimacy Audit verdict OK, no `[ASSUMED]`/`[SUS]`/`[SLOP]` rows | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-30.1-01 | T-30.1-02 | Submodule pin advances on schedule to unreviewed parent content — same owner, same trust domain, branch-protection posture unchanged; published SHA always reconstructable | plan-time register (owner-approved plans) | 2026-07-26 |
| AR-30.1-02 | T-30.1-06 | Both repositories, all build logs and artifacts are public by requirement; no secret exists in the surface | plan-time register (owner-approved plans) | 2026-07-26 |
| AR-30.1-03 | T-30.1-12 | pypdf parses untrusted downloaded PDFs read-only, outside the repository, output never executed | plan-time register (owner-approved plans) | 2026-07-26 |
| AR-30.1-04 | T-30.1-SC | No new language-registry package installed phase-wide; pre-existing deps audited OK | plan-time register (owner-approved plans) | 2026-07-26 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-07-26 | 26 | 26 | 0 | Claude (gsd-secure-phase, L1 short-circuit) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-07-26
