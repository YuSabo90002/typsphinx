---
phase: 30.1-translations-repository-japanese-rtd-site
plan: 10
subsystem: docs
tags: [typst, pdf, fonts, i18n, readthedocs, forensics]

requires:
  - phase: 30.1-09
    provides: "The re-triggered pin-bump run and the re-observed submodule gitlink move, which put a fresh, current Japanese PDF (pin 5297d38) in front of this plan to re-measure"
provides:
  - "A re-measured NUL map (10 occurrences, 発/単/釈, 96-page artifact `c67a3723...`) against current, non-superseded bytes"
  - "Per-occurrence font attribution: every NUL is CID `0x0000` (`.notdef`) tagged to `LibertinusSerif`, not to `HanaMinA` or any CJK-capable font"
  - "An in-container measurement (fc-list + typst.Fonts()) proving the RTD build container has multiple fontconfig-visible, Typst-enumerated fonts (including HanaMinA) that declare coverage for all three affected codepoints"
  - "A stated root cause: Typst's own font-selection/glyph-shaping stage fails to find or use a working glyph for these three specific codepoints, on a container that otherwise has abundant coverage for them — not a typsphinx/ code issue, not a font-availability gap"
  - "The 95-vs-96 page-count delta measured this round is ruled OUT as font-metrics reflow and fully attributed to Plan 05's new Translations section in contributing.rst; the separate, earlier 94-vs-95 delta is left explicitly open, not conflated"
  - "The English parent PDF measured with the identical scan: zero NUL bytes, page count unchanged at 93 — the defect does not reach the parent as currently built"
  - "The owner's recorded decision: option-b (custom typst_template with explicit font selection reached from docs/source/conf.py, affecting both PDFs), with accept_phase29_impact=yes and its basis, authorizing Plan 11's scope but not its outcome"
affects: [30.1-11]

tech-stack:
  added: []
  patterns: ["Per-occurrence PDF font/CMap forensics via pypdf's visitor_text hook, read-only pre_build container diagnostics via fc-list/typst.Fonts(), never installing a diagnostic dependency"]

key-files:
  created: []
  modified:
    - .planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md
    - .planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.readthedocs.yaml

key-decisions:
  - "Owner selected option-b (parent-side custom typst_template with explicit font selection, affecting both PDFs) and accepted the Phase 29 English-regression impact this incurs, per the scope consequences stated explicitly alongside the option before selection"
  - "The fix is recorded as authorized-to-attempt, not proven-to-work: Plan 11's own re-measurement of the Japanese artifact can legitimately still show the NUL defect and that is a valid, recordable outcome of this round, not a failure of this decision"

patterns-established:
  - "Root-cause forensics before any speculative fix: two independent evidence chains (content-stream CID attribution, in-container font-availability measurement) are required to converge on a root-cause statement, and an option set is only ever built from the resulting measurements"

requirements-completed: []

coverage: []

duration: ~55min (continuation portion; full plan including prior executor's Tasks 1-2 spans a longer session)
completed: 2026-07-26
status: complete
---

# Phase 30.1 Plan 10: SC#4 Root-Cause Forensics and Owner Fix Decision Summary

**Traced the Japanese PDF's 10 literal NUL bytes to a `.notdef` glyph tagged to `LibertinusSerif` at Typst's own font-selection stage — not a missing font — measured the RTD container has ample fontconfig-visible coverage for 発/単/釈, ruled out the 95-vs-96 page delta as font-metrics reflow (it's Plan 05's new Translations section), confirmed the English parent PDF does not share the defect, and recorded the owner's choice of option-b (parent-side explicit font selection via a custom `typst_template`, affecting both PDFs) as authorized-but-unproven.**

## Performance

- **Duration:** ~55 min (this continuation: recording the decision and closing Task 3; prior executor completed Tasks 1-2 and the evidence half of Task 3 in an earlier session)
- **Tasks:** 3 (Task 1 and Task 2 completed by a prior executor; Task 3's decision-recording half completed in this session)
- **Files modified:** 2 (`30.1-EVIDENCE.md`, `translations-repo/.readthedocs.yaml`)

## Accomplishments

- Re-downloaded and re-measured both the Japanese (`c67a3723...`, 96 pages, 10 NULs) and English (`8d1a79ff...`, 93 pages, 0 NULs) PDFs fresh in this session, superseding Plan 06's now-stale 95-page record
- Attributed every NUL occurrence to a specific font via `pypdf`'s content-stream visitor hook: all 10 are CID `0x0000` tagged to `LibertinusSerif` (the plain Latin body font, zero CJK coverage) — not `HanaMinA` or any CJK-capable embedded font
- Added a temporary, read-only `pre_build` diagnostic block to the ja `.readthedocs.yaml`, mirrored it into a live clone with byte-identity proven, triggered a real RTD build, and read the container's own `fc-list`/`typst.Fonts()` output: at least six fontconfig-visible families (including `HanaMinA` itself) declare coverage for 発/単/釈, and `typst.Fonts()` enumerates `HanaMinA` among 407 families it sees
- Stated the root cause: Typst's own font-selection/glyph-shaping stage fails for these three specific codepoints despite demonstrable, Typst-visible coverage — a selection/shaping failure, not a `typsphinx/` code defect and not a font-availability gap
- Ruled out the font-metrics-reflow hypothesis for the 95-vs-96 delta measured this round (fully attributed to Plan 05's new `contributing.rst` Translations section via a page-aligned diff); left the separate, earlier 94-vs-95 delta explicitly open and unconflated
- Measured the English parent shares no occurrence of the defect, and recorded the structural fact that `docs/source/conf.py` is shared byte-for-byte between both projects, which narrows which fix options can even be expressed as "ja-manifest-only"
- Recorded the owner's decision (`fix_option=option-b`, `accept_phase29_impact=yes`) with its basis, the owner's earlier contextual remark, and the explicit scope handoff to Plan 11 — without implementing anything

## Task Commits

Each task was committed atomically:

1. **Task 1: Re-measure text-layer defect on current artifact, attribute to font** - `d2b6bc8` (docs)
2. **Task 2: Measure RTD build container's own font situation** - `553bb3b` (docs)
3. **Task 3 (evidence half): State root cause, reconcile page count, present fix options** - `1814891` (docs)
4. **Task 3 (decision half): Record owner's SC#4 fix decision (option-b)** - `89b9c11` (docs)

**Plan metadata:** commit pending (this SUMMARY + STATE.md are the orchestrator's write, per this continuation's scope)

## Files Created/Modified

- `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` - Appended three sections: text-layer forensics on the current artifact, in-container font measurement, and root-cause/page-count/scope decision (including this session's owner-decision append)
- `.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.readthedocs.yaml` - Added a temporary, clearly-marked `build.jobs.pre_build` diagnostic block (fc-list inventory, per-codepoint charset queries, typst-py family enumeration, version prints); left in place for Plan 11 to remove alongside its fix

## Decisions Made

- **Owner chose option-b** — parent-side fix (custom `typst_template` with explicit font selection reached from `docs/source/conf.py`), affecting both the English and Japanese PDFs — and recorded `accept_phase29_impact=yes` on the basis of having been shown option-b's stated scope consequences (fresh English regression evidence required, a Phase 29 success criterion re-opened, a maintained template asset added, and the fix explicitly unproven) before selecting it. No additional free-text conditions were attached.
- The owner's earlier, unprompted remark — "そもそもの話として送り込んでるfontが適用されてないのが悪いやろ" ("fundamentally the problem is that the font we're shipping in isn't being applied") — is recorded as context consistent with, and plausibly motivating, the explicit-selection route, but is not treated as a fourth formal answer field.
- **This decision authorizes an attempt, not a proven outcome.** Plan 11's own re-measurement of the Japanese artifact after applying option-b can legitimately still show the NUL defect present — that is recorded as a valid possible result of Plan 11, not a failure of this decision-recording step.

## Deviations from Plan

None - plan executed exactly as written. This continuation's scope (recording the owner's decision, per the `<what_to_do>` instructions) matched Task 3's post-decision half precisely; no fix was implemented, no template/`conf.py`/manifest change was made, and `git status --porcelain typsphinx/ tests/ docs/ docs/locale/ pyproject.toml .readthedocs.yaml .github/` stayed clean throughout.

## Issues Encountered

None in this continuation. The prior executor's Task 2 documented (in the evidence file itself, not hidden) a first RTD build push that failed on a YAML-quoting mistake before a corrected push succeeded — that is prior-session work already recorded in `30.1-EVIDENCE.md` and is not re-litigated here.

## User Setup Required

None - no external service configuration required by this continuation. (Wave 9's owner decision itself was the `user_setup` dependency this plan's frontmatter declared, and it is now closed.)

## Next Phase Readiness

**Ready for Plan 11:** apply option-b — a custom `typst_template` (or equivalent `typst_elements`/template-mapping mechanism per Plan 11's own investigation of the `ELEMENTS_ALLOWLIST` constraint) adding explicit font selection, reached from `docs/source/conf.py` (this decision authorizes editing that file, reversing this plan's own read-only treatment of it for Plan 11 specifically) — then re-render both PDFs and re-measure against this round's baselines (Japanese `c67a3723...` 96 pages / 10 NULs; English `8d1a79ff...` 93 pages / 0 NULs). Plan 11 also removes Task 2's temporary `pre_build` diagnostic block from the ja `.readthedocs.yaml` in the same commit that applies the fix.

**Not yet resolved:** SC#4 and I18N-03 remain open — nothing has been fixed by this plan. This SUMMARY does not describe I18N-03 as resolved.

**Blockers/concerns for Plan 11:** the fix is explicitly unproven to work (Typst's internal selection/shaping mechanism for these three codepoints has not been further diagnosed — that would require reading Typst's own Rust source or an upstream report, both outside this project's control). Plan 11 must be prepared to record a negative result honestly if explicit selection does not bypass whatever internal Typst behavior is failing.

---
*Phase: 30.1-translations-repository-japanese-rtd-site*
*Completed: 2026-07-26*

## Self-Check: PASSED

- FOUND: `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-10-SUMMARY.md`
- FOUND: `d2b6bc8`, `553bb3b`, `1814891`, `89b9c11` (all four task commits present in `git log --oneline --all`)
