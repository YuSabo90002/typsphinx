---
phase: 29-rtd-build-establishment-english-parent-pdf-path-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .readthedocs.yaml
  - docs/source/conf.py
  - tests/test_readthedocs_config.py
autonomous: true
requirements: [RTD-01]
must_haves:
  truths:
    - "`.readthedocs.yaml` exists at the repository root, parses as YAML, and carries version 2 plus build.os, build.tools.python, sphinx.configuration and python.install (D-06 commit 1 shape)"
    - "Commit 1's `.readthedocs.yaml` declares NO `formats:` key and NO `build.jobs` key, so RTD's own LaTeX PDF pipeline is never activated before the typstpdf override exists (D-06)"
    - "`.readthedocs.yaml`'s python.install entry uses method uv with command sync and extras [docs], which is the mechanism that installs typsphinx itself from the checked-out commit rather than from a PyPI index (RTD-01 / RESEARCH Pitfall 2)"
    - "`.readthedocs.yaml`'s build.tools.python equals the python-version pinned in .github/workflows/docs.yml, so the D-12 PDF baseline comparison runs on the same Python minor on both sides"
    - "With both READTHEDOCS_LANGUAGE and SPHINX_LANGUAGE unset, docs/source/conf.py resolves `language` to \"en\" — identical to the pre-phase behavior, so the edit changes nothing locally or in .github/workflows/docs.yml"
    - "READTHEDOCS_LANGUAGE wins over SPHINX_LANGUAGE when both are set, and SPHINX_LANGUAGE still resolves when only it is set (the fallback layer is retained, not replaced)"
    - "docs/source/conf.py's html_context[\"language\"] still equals the resolved `language` value, so the seam is actually wired into the value its downstream consumer reads"
    - "`uv run python -m sphinx -b html docs/source <tmp>` exits 0 after the conf.py edit — the real documentation build still works"
    - "No file under typsphinx/ is modified by this plan (milestone invariant #3)"
    - "No new Python dependency is declared in pyproject.toml — PyYAML is consumed transitively via sphinx, exactly as PATTERNS.md measured (milestone invariant #1)"
  prohibitions:
    - statement: "No RTD project is created, no RTD dashboard setting is touched, and no RTD URL is asserted to serve anything by this plan — this plan is repo-side only"
      status: required
      verification: "Human review of the plan's commits: they touch only .readthedocs.yaml, docs/source/conf.py and tests/test_readthedocs_config.py; no VERIFICATION.md evidence claim is written here"
    - statement: "SPHINX_LANGUAGE is not renamed, removed, or made unreachable — it stays the fallback layer, because docs/build_multilang.py:44 is still its live producer and that file belongs to Phase 30"
      status: required
      verification: "Human review: `git diff -- docs/source/conf.py` still contains an os.getenv read of SPHINX_LANGUAGE, and docs/build_multilang.py is unchanged"
    - statement: "No committed test in this plan performs a network fetch — the suite stays hermetic (D-15's rationale applied to the local tests)"
      status: required
      verification: "Human review of tests/test_readthedocs_config.py: no urllib / requests / httpx / socket / curl usage anywhere in the file"
    - statement: "No Japanese RTD project, no Japanese slug, and no Japanese locale build target is created, named, or decided anywhere in this phase. Per D-03 the ja slug is deliberately left to Phase 30's own discussion, and per the deferred I18N-03 no Japanese PDF is produced — the seam landing here only makes ja's eventual first build resolve correctly"
      status: required
      verification: "Human review of the phase's full diff: no `ja` project name, slug, or locale target appears in `.readthedocs.yaml`; `docs/locale/ja/` and `docs/source/conf.py`'s `locale_dirs` are unchanged"
  artifacts:
    - "`.readthedocs.yaml` — NEW at repository root; commit-1 (HTML-only) shape"
    - "docs/source/conf.py — `_resolve_language()` module-level helper added; the `language` assignment at :51 now calls it"
    - "tests/test_readthedocs_config.py — NEW; `test_readthedocs_yaml_shape`, `test_build_python_matches_docs_workflow`, `test_language_seam_precedence`"
  key_links:
    - "`.readthedocs.yaml` python.install (method uv / command sync) → typsphinx installed from the checkout. This is the ONLY thing standing between the published docs and a stale PyPI wheel silently shadowing the working tree (SC#1's named failure mode)"
    - "`.readthedocs.yaml` build.tools.python → .github/workflows/docs.yml python-version. Not auto-derived; bound only by test_build_python_matches_docs_workflow. If they drift, D-12's baseline comparison silently stops being same-Python-minor"
    - "docs/source/conf.py `_resolve_language()` → module-level `language` → html_context[\"language\"] at :72. A helper that exists but is not called by the `language` assignment leaves the seam dead while looking present"
    - "READTHEDOCS_LANGUAGE (set only by RTD at build time, per the project's Admin Language setting) → `_resolve_language()`. Nothing in this repository sets it; Phase 30's ja project is its first real producer"
---

<objective>
Land D-06's **commit 1**: the HTML-only `.readthedocs.yaml`, the `conf.py` language seam, and the
local pytest file that binds both. This is the entire repo-side prerequisite for the owner creating
the RTD project in Plan 02 — nothing here touches RTD, and nothing here enables the PDF format.

Purpose: RTD refuses every build without a `.readthedocs.yaml`, and RTD's zero-config Sphinx build
does not install the project package — so without `python.install` the published docs would either
fail to import `typsphinx` or, worse, silently resolve a stale PyPI wheel (SC#1's exact failure
mode). The `language` seam lands here rather than in Phase 30 so the Japanese project's very first
build resolves correctly instead of needing a second pass.

Output: `.readthedocs.yaml` (HTML-only), a `_resolve_language()` seam in `docs/source/conf.py`, and
`tests/test_readthedocs_config.py` with three passing tests.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols and files created by Phase 29 (all four plans). Listed so a drift-verification pass can
tell a newly-created symbol from an unacknowledged change to an existing one.

**New files:**
- `.readthedocs.yaml` (repository root)
- `tests/test_readthedocs_config.py`
- `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md`
  (live-evidence record; created by Plan 02, appended to by Plans 03/04/05/06)

**New `.readthedocs.yaml` keys introduced (commit 1 in this plan, the rest in Plan 03):**
`version`, `build`, `build.os`, `build.tools`, `build.tools.python`, `sphinx`,
`sphinx.configuration`, `python`, `python.install`, `python.install[].method`,
`python.install[].command`, `python.install[].extras` — and, added by Plan 03:
`build.apt_packages`, `build.jobs`, `build.jobs.build`, `build.jobs.build.pdf`, `formats`.

**New Python symbols:**
- `docs/source/conf.py` → `_resolve_language()`
- `tests/test_readthedocs_config.py` → `REPO_ROOT`, `READTHEDOCS_YAML_PATH`, `CONF_PY_PATH`,
  `DOCS_WORKFLOW_PATH`, `_load_readthedocs_yaml()`, `_load_conf_module()`,
  `_extract_docs_workflow_python_version()`, `test_readthedocs_yaml_shape()`,
  `test_build_python_matches_docs_workflow()`, `test_language_seam_precedence()`, and (Plan 03)
  `test_readthedocs_yaml_pdf_override()`

**Deliberately NOT produced anywhere in Phase 29:** any change under `typsphinx/`; any new entry in
`pyproject.toml`'s dependency or optional-dependency tables; any committed script that downloads or
compares the RTD-built PDF (D-15); any committed test that performs a network fetch.
</artifacts_this_phase_produces>

<assumption_delta_decision>
The `assumption-delta scan` detector fired (`kind: pluralization`, terms `fallback` / `alongside` /
`second`) against this phase's scope. The transition it found is real: the `language` value goes from
being read from ONE env var to a two-layer chain.

- **Noun now primary:** the *resolved documentation language*, produced by `_resolve_language()`.
  `READTHEDOCS_LANGUAGE` becomes the preferred source; `SPHINX_LANGUAGE` becomes the fallback layer.
- **Decision: `add-alongside`.** `SPHINX_LANGUAGE` is deliberately retained rather than promoted away.
  Rationale: `docs/build_multilang.py:44` is still its live producer, and that file is **Phase 30's**
  to delete (I18N-02). Renaming or removing the layer here would couple this phase to Phase 30's
  deletion round and would break the existing local/CI override before its replacement is proven.
- **Accepted debt:** two env-var layers for one value, and a fallback layer whose only producer is
  scheduled for deletion.
- **What would force a later promote:** once Phase 30 removes `docs/build_multilang.py` and
  `tox.ini`'s `[testenv:docs-multilang]`, a repo-wide grep for `SPHINX_LANGUAGE` producers should
  return nothing but this seam — at which point the layer is dead configuration and the chain should
  collapse to `READTHEDOCS_LANGUAGE` → `"en"`. Recorded here as a Phase 30 follow-up observation, not
  as work this plan performs.
</assumption_delta_decision>

<flagged_probe_assumptions>
No `*-SPEC.md` exists for this phase, so the deterministic edge probe ran over RTD-01..RTD-04 in
fallback mode. It returned **6 applicable rows, 0 resolved, 6 unresolved**. Per the fallback protocol
an `unresolved` / `unclassified` row is never auto-`backstop`ped and never auto-dismissed — all 6 are
surfaced here as explicit planner assumptions. None was converted into a `must_haves` truth; the
truths above are derived from the ROADMAP success criteria and CONTEXT.md's D-01..D-15 instead.

1. **RTD-01 / `unclassified`** ("unclassified — review manually") — UNRESOLVED. Manual read: RTD-01's
   real edge is *install provenance* (local checkout vs. PyPI index), which is not an algorithmic
   edge family. Substantively addressed by SC#1's raw-log evidence in Plan 02, not by an edge
   predicate.
2. **RTD-02 / `unclassified`** — UNRESOLVED. RTD-02's real edge is *silent glyph substitution in a
   successful build*, a content-fidelity failure. Substantively addressed by D-12's four-check bar in
   Plan 05.
3. **RTD-03 / `unclassified`** — UNRESOLVED. RTD-03's real edge is *URL stability across releases*.
   Already measured (200, 1,678,961 bytes, 2026-07-25) and re-fetched in Plan 06.
4. **RTD-04 / `adjacency`** ("When two things are exactly equal or just touch, do they merge,
   collide, or separate?") — UNRESOLVED, and the category does not bite: RTD-04 compares no two
   quantities. Its nearest analogue — "Default Version names a version that has no build" — is an
   existence question, addressed by Plan 02's real-HTTP root fetch.
5. **RTD-04 / `empty`** ("What is the result for empty, single-element, or null input?") — UNRESOLVED,
   and this one is *substantively adjacent*: the single-element case for RTD-04 is "the project has
   exactly one active version (`latest`) and no `stable`", which is precisely this phase's state and
   precisely why Default Version must stay `latest`. Addressed by Plan 02's Default-Version truth;
   still recorded here as unresolved rather than laundered into a probe-derived truth.
6. **RTD-04 / `ordering`** ("When elements compare equal, is output order specified and stable?") —
   UNRESOLVED, and the category does not bite: RTD-04 produces no sorted output. The phase's one real
   ordering constraint is D-06's two-commit sequencing, expressed as a wave/dependency structure and
   as truths, not as an edge predicate.

Accounting: 6 probe rows == 0 authored into `must_haves` + 6 flagged assumptions. No silent drops.
</flagged_probe_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_readthedocs_config.py with three RED tests</name>
  <files>tests/test_readthedocs_config.py</files>
  <read_first>
    - `tests/test_readme_version_sync.py` — the primary style analog: module docstring naming the guarded hazard, `REPO_ROOT = Path(__file__).resolve().parents[1]` path constants, private `_extract_*` helper with an assertive guard against a vacuous pass, plain top-level `def test_*()` functions with no test class, descriptive f-string failure messages
    - `tests/test_preview_version_sync.py` — secondary analog for the raw-text-regex idiom against a non-Python target file
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-PATTERNS.md` — § `tests/test_readthedocs_config.py`: confirms the plain-function (not class-based) convention for file-shape tests, the confirmed PyYAML availability under `uv run`, and that this file introduces this suite's FIRST use of pytest's `monkeypatch`
    - `docs/source/conf.py` — read lines 1-100 to see the current `language` line at :51, the `html_context` consumer at :71-77, and the `sys.path.insert` / `tomllib` side effects that a full module load will execute
    - `.github/workflows/docs.yml` — read the `actions/setup-python` step to see the exact `python-version: "3.12"` line shape the regex must match
  </read_first>
  <action>
    Create `tests/test_readthedocs_config.py`, plain-function style, no test class, following
    `test_readme_version_sync.py`'s structure exactly.

    Module docstring: state that this module guards the RTD build manifest's shape and the
    `conf.py` language seam introduced for RTD-01/D-06, that PyYAML is available transitively via
    `sphinx` (so the suite must be run under `uv run`, per CLAUDE.md's standing execution mode) and
    is deliberately NOT added as a direct dependency, and that no test here performs a network
    fetch.

    Module-level constants: `REPO_ROOT = Path(__file__).resolve().parents[1]`,
    `READTHEDOCS_YAML_PATH = REPO_ROOT / ".readthedocs.yaml"`,
    `CONF_PY_PATH = REPO_ROOT / "docs" / "source" / "conf.py"`,
    `DOCS_WORKFLOW_PATH = REPO_ROOT / ".github" / "workflows" / "docs.yml"`.

    Helper `_load_readthedocs_yaml()` — assert `READTHEDOCS_YAML_PATH.exists()` with a descriptive
    message naming the file, then return `yaml.safe_load(...)`; assert the result is a dict so a
    scalar/None document cannot pass vacuously.

    Helper `_extract_docs_workflow_python_version()` — regex the raw text of `DOCS_WORKFLOW_PATH`
    for a `python-version:` line carrying a quoted `MAJOR.MINOR` value, with an assertive
    `assert match, "..."` guard (mirroring `_extract_readme_status_version`) so a reworded workflow
    fails loudly instead of comparing against nothing. Return the captured version string.

    Helper `_load_conf_module()` — load `docs/source/conf.py` as a module via
    `importlib.util.spec_from_file_location` under a private module name (e.g. `_p29_conf_probe`) +
    `importlib.util.module_from_spec` + `spec.loader.exec_module`, returning the module object. Do
    NOT register it in `sys.modules`. Load FRESH on every call so the module-level `language`
    assignment re-evaluates against the current environment. Docstring must note two things a
    reader will otherwise trip on: `conf.py` is not importable by plain `import` from the pytest
    path (confirmed — nothing under `tests/` does so today), and executing it has two side effects
    inherited from the existing file — a CWD-relative `sys.path.insert` and a `tomllib` read of
    `pyproject.toml` — both pre-existing and deliberately not changed by this phase.

    `test_readthedocs_yaml_shape()` — load the YAML and assert, each with its own descriptive
    message: `data["version"] == 2`; `build` is a dict; `build["os"]` is a non-empty string;
    `build["tools"]["python"]` is a string; `sphinx["configuration"] == "docs/source/conf.py"`;
    `python["install"]` is a non-empty list whose first entry has `method == "uv"`,
    `command == "sync"`, and an `extras` list containing `"docs"`.

    `test_build_python_matches_docs_workflow()` — compare `build.tools.python` against
    `_extract_docs_workflow_python_version()` and assert equality, with a failure message that
    states WHY they are bound: D-12's PDF baseline comparison is only same-Python-minor if RTD and
    `.github/workflows/docs.yml` provision the same interpreter minor. Compare the two parsed
    values against each other — never against a hardcoded expected version string.

    `test_language_seam_precedence(monkeypatch)` — this suite's first `monkeypatch` use; use
    `monkeypatch.delenv(name, raising=False)` and `monkeypatch.setenv`. Cover four combinations,
    asserting on both the freshly-loaded module's `language` attribute and a direct call to
    `_resolve_language()`: (a) both `READTHEDOCS_LANGUAGE` and `SPHINX_LANGUAGE` unset → `"en"`;
    (b) only `SPHINX_LANGUAGE` set to `"ja"` → `"ja"`; (c) only `READTHEDOCS_LANGUAGE` set to
    `"ja"` → `"ja"`; (d) `READTHEDOCS_LANGUAGE` set to `"ja"` and `SPHINX_LANGUAGE` set to `"fr"` →
    `"ja"`, i.e. RTD's layer wins. Add a final wiring assertion: for the freshly-loaded module,
    `module.html_context["language"] == module.language` — this is what catches a helper that
    exists but is never called by the `language` assignment.

    All three tests are expected to FAIL at the end of this task: `.readthedocs.yaml` does not exist
    yet and `_resolve_language` does not exist yet. That RED state is the point — it is what proves
    Tasks 2 and 3 actually satisfied the assertions rather than the assertions having been written
    to match whatever was produced. Commit this task as a `test(29-01): ...` commit and state in the
    commit body that the suite is intentionally RED until Task 3.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q > /tmp/p29-01-red.txt 2>&1; grep -q '3 failed' /tmp/p29-01-red.txt && echo RED_BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_readthedocs_config.py` exists and is collected by pytest (the run reports 3 collected items, not a collection error)
    - The pytest run above exits NON-zero and its summary line reports `3 failed` with `0 passed` — the RED baseline
    - `test_readthedocs_yaml_shape`'s failure message names `.readthedocs.yaml` (it fails on the missing file, not on an unrelated exception)
    - `test_language_seam_precedence`'s failure is an `AttributeError` / missing-`_resolve_language` failure, not a YAML or import error
    - The file defines `test_readthedocs_yaml_shape`, `test_build_python_matches_docs_workflow`, `test_language_seam_precedence` as plain top-level functions with no enclosing class: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py --collect-only -q` lists exactly those three node IDs with no `::Test` class segment
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib,re; t=pathlib.Path('tests/test_readthedocs_config.py').read_text(); assert not re.search(r'import (urllib|requests|httpx|socket)', t), 'network import found'; print('hermetic ok')"` prints `hermetic ok`
    - `git diff --name-only a616b97..HEAD -- pyproject.toml` is empty — no dependency was added for PyYAML
  </acceptance_criteria>
  <done>The three tests exist, are collected as plain functions, fail for the right reasons, and no dependency or network import was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Create the HTML-only .readthedocs.yaml (D-06 commit 1)</name>
  <files>.readthedocs.yaml</files>
  <read_first>
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md` — § "Pattern 1: Two-Commit Sequencing (D-06)" holds the exact commit-1 YAML; § "Standard Stack" holds the per-key rationale; § "Common Pitfalls" #1 and #2 explain why `formats:` must be absent here and why `python.install` is mandatory
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-PATTERNS.md` — § `.readthedocs.yaml`: house YAML style is 2-space indentation with sparse comments that explain *why*, borrowed from `.github/workflows/ci.yml` and `tox.ini:4-10`; and the explicit non-pattern warning against copying GitHub Actions or tox invocation shapes
    - `pyproject.toml` — read the `[project.optional-dependencies]` table to confirm the extra is named `docs` and that it is a classic optional-dependency group (so `extras:` is correct and `groups:` is not)
    - `tests/test_readthedocs_config.py` — the assertions this file must satisfy
  </read_first>
  <action>
    Create `.readthedocs.yaml` at the repository root with exactly the commit-1 (HTML-only) shape,
    2-space indentation:

    `version: 2`; a `build:` block with `os: ubuntu-24.04` and `tools:` → `python: "3.12"`; a
    `sphinx:` block with `configuration: docs/source/conf.py`; and a `python:` block with
    `install:` as a one-entry list whose entry is `method: uv`, `command: sync`, and `extras:` as a
    list containing `docs`.

    `ubuntu-24.04` is pinned explicitly rather than `ubuntu-lts-latest` so the OS image cannot
    advance without a corresponding repository commit — this phase's whole purpose is observing one
    specific build's log. `python: "3.12"` matches `.github/workflows/docs.yml`'s
    `actions/setup-python` pin so the D-12 PDF comparison runs on the same Python minor on both
    sides; `tests/test_readthedocs_config.py::test_build_python_matches_docs_workflow` binds them.

    Add short `#` comments explaining only the non-obvious *why*: (a) that RTD refuses builds
    without this file and that it is the only place RTD's build behavior is configured — nothing in
    `conf.py` or `tox.ini` reaches it; (b) that `python.install` exists because RTD's zero-config
    Sphinx build does NOT install the project package, so without it `conf.py`'s
    `extensions = [..., "typsphinx"]` would either fail to import or silently resolve a published
    wheel instead of this commit; (c) that the PDF format is deliberately absent in this commit and
    arrives in a separate one, so that if the PDF step fails its failure is isolated in a log with
    nothing else in it and the English site is already established and serving.

    Do NOT add `formats:`, `build.jobs`, or `build.apt_packages` here — those are Plan 03's commit.
    Do NOT reference `tox` anywhere in this file: RTD provisions its own environment via
    `python.install`, and nesting the repository's tox runner inside RTD's build would create a
    redundant second venv with no working locked-sync wiring (CLAUDE.md and RESEARCH.md § Common
    Pitfalls both call this out). Do NOT add a `commands:` / `build.commands` key — overriding the
    whole build would take the `sphinx:` key out of the picture.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py::test_readthedocs_yaml_shape tests/test_readthedocs_config.py::test_build_python_matches_docs_workflow -q</automated>
  </verify>
  <acceptance_criteria>
    - The pytest command above exits 0 with `2 passed`
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import yaml;d=yaml.safe_load(open('.readthedocs.yaml'));assert 'formats' not in d and 'jobs' not in d.get('build',{}) and 'apt_packages' not in d.get('build',{});print('commit-1 shape ok')"` prints `commit-1 shape ok`
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import yaml;d=yaml.safe_load(open('.readthedocs.yaml'));e=d['python']['install'][0];assert e['method']=='uv' and e['command']=='sync' and 'docs' in e['extras'];print('install ok')"` prints `install ok`
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib;t=pathlib.Path('.readthedocs.yaml').read_text();assert 'tox' not in t;print('no tox delegation')"` prints `no tox delegation`
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q 2>&1 | tail -3` still reports exactly `1 failed, 2 passed` — only the language-seam test remains RED
  </acceptance_criteria>
  <done>`.readthedocs.yaml` exists with the commit-1 HTML-only shape, both YAML tests pass, the PDF keys are absent, and the file does not delegate to tox.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the READTHEDOCS_LANGUAGE seam to docs/source/conf.py</name>
  <files>docs/source/conf.py</files>
  <behavior>
    - Both `READTHEDOCS_LANGUAGE` and `SPHINX_LANGUAGE` unset → resolved language is `"en"` (unchanged from today)
    - Only `SPHINX_LANGUAGE=ja` → `"ja"` (the existing local/CI override keeps working)
    - Only `READTHEDOCS_LANGUAGE=ja` → `"ja"` (RTD's per-project Language setting is honored)
    - Both set (`READTHEDOCS_LANGUAGE=ja`, `SPHINX_LANGUAGE=fr`) → `"ja"` (RTD's layer wins)
    - `html_context["language"]` equals the resolved `language` in every case (the seam is wired, not merely present)
  </behavior>
  <read_first>
    - `docs/source/conf.py` — the file being modified; read lines 1-100 for the existing seam at :51, its one-line `#` comment convention at :50, and the `html_context` / `html_sidebars` consumers at :71-89 (those are **Phase 30's** to delete, NOT this plan's — leave them byte-unchanged)
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-PATTERNS.md` — § `docs/source/conf.py`: the exact comment-style convention to preserve, and the explicit note that factoring the seam into a helper is a NEW pattern for this codebase (no `def` precedent exists in this file) justified purely by testability
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md` — § "Pattern 2: The `language` Seam" and § "Code Examples" → "`conf.py:51` seam"
    - `tests/test_readthedocs_config.py` — the precedence expectations this edit must satisfy
  </read_first>
  <action>
    In `docs/source/conf.py`, inside the existing `-- Internationalization (i18n) configuration --`
    block, replace the single-layer `language` assignment with a two-layer chain factored into a
    module-level helper:

    Add `def _resolve_language():` returning
    `os.getenv("READTHEDOCS_LANGUAGE", os.getenv("SPHINX_LANGUAGE", "en"))`, then assign
    `language = _resolve_language()`.

    Keep the existing one-line `#` comment convention directly above the assignment — no docstring
    block, no reformatting of neighbouring lines. The comment must name both env vars and the
    default, and say which one wins: `READTHEDOCS_LANGUAGE` is RTD's per-project Language setting;
    `SPHINX_LANGUAGE` is the pre-existing local/CI override retained as the fallback layer (its live
    producer, `docs/build_multilang.py`, belongs to Phase 30 and is untouched here); the resolved
    default is `"en"`.

    The leading underscore matters: Sphinx picks up known config names from this file's namespace,
    and a private helper name keeps it unambiguously not-a-config-value.

    This is a zero-behavior-change edit outside RTD — both env vars are unset locally and in
    `.github/workflows/docs.yml`, so the resolved value stays `"en"` exactly as today. Do NOT touch
    `locale_dirs`, `gettext_*`, `html_context`, `html_sidebars`, `html_css_files`, or anything under
    `typsphinx/`. Do NOT rename or remove `SPHINX_LANGUAGE`.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m sphinx -b html -q docs/source /tmp/p29-01-html && test -f /tmp/p29-01-html/index.html && echo HTML_BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q` exits 0 with `3 passed`
    - The Sphinx HTML build in the verify command exits 0 and `/tmp/p29-01-html/index.html` exists — `HTML_BUILD_OK` is printed
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import importlib.util as u;s=u.spec_from_file_location('_p','docs/source/conf.py');m=u.module_from_spec(s);s.loader.exec_module(m);print(m.language, m.html_context['language'])"` prints `en en` with both env vars unset
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib;t=pathlib.Path('docs/source/conf.py').read_text();assert 'SPHINX_LANGUAGE' in t and 'READTHEDOCS_LANGUAGE' in t and 'language = _resolve_language()' in t;print('seam wired')"` prints `seam wired`
    - `git diff a616b97..HEAD --name-only -- typsphinx/` is empty (milestone invariant #3)
    - `git diff a616b97..HEAD -- docs/build_multilang.py tox.ini` is empty — the fallback layer's producer and the multilang env are untouched
    - `git diff a616b97..HEAD -- docs/source/conf.py | grep -c '^+' ` reports 5 or fewer added lines — the edit stayed the minimal seam plus its comment, and did not reformat neighbours
    - Full suite is as green as before: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest -q 2>&1 | tail -3` reports the same failure set as the pre-plan baseline (the pre-existing `tests/test_integration_*.py` / `tests/test_examples_basic.py` sandbox failures are NOT regressions — record the before/after counts in the SUMMARY)
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run black --check docs/source/conf.py tests/test_readthedocs_config.py` and `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run ruff check docs/source/conf.py tests/test_readthedocs_config.py` both exit 0
  </acceptance_criteria>
  <done>The seam resolves per the four documented combinations, `html_context` still reads the resolved value, the real HTML build still succeeds, all three tests pass, and nothing under `typsphinx/` moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository commit → RTD build container | `.readthedocs.yaml` is maintainer-authored config executed by a third-party build orchestrator. Nothing untrusted crosses inbound; the manifest itself is the trusted artifact |
| PyPI index → RTD build environment | The boundary where a stale published wheel can shadow the working tree. `python.install` is the control that keeps the install on the local-checkout side |
| process environment → `conf.py` | `READTHEDOCS_LANGUAGE` / `SPHINX_LANGUAGE` are read at build time. Values select a locale catalog directory; they are not interpolated into shell, SQL, or file writes |

## STRIDE Threat Register

ASVS L1. Reused from `29-RESEARCH.md` § "Security Domain" rather than re-derived. V14 Configuration
is the one clearly-applicable category; V2/V3/V4/V5/V6 do not apply (no auth, no session, no
untrusted input parsed, no crypto).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Spoofing | `.readthedocs.yaml` `python.install` — a stale published wheel shadowing the working tree, so published docs describe code that is not in the commit | high | mitigate | `method: uv` + `command: sync` installs the workspace-root project from the checkout; asserted structurally by `test_readthedocs_yaml_shape` here and verified against the real raw build log by Plan 02 (SC#1) |
| T-29-06 | Tampering | `docs/source/conf.py` `_resolve_language()` — an env-var-selected locale path | low | accept | The resolved value only selects a Sphinx locale catalog under `docs/locale/`; it is never interpolated into a shell command, a path write, or a template render. The producer of `READTHEDOCS_LANGUAGE` is RTD's own build environment, which is already fully trusted to run `build.jobs` |
| T-29-07 | Information Disclosure | `.readthedocs.yaml` committed to a public repository, and RTD build logs public for a public project | medium | mitigate | The commit-1 manifest references no secret, no token, and no env var other than the RTD-provided ones; the acceptance criteria grep the file for tox delegation and the plan authors no `build.jobs` block at all in this commit. Standing rule for Plan 03: no secret may ever be echoed into a `build.jobs` command |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase — zero new Python/npm/crates packages (milestone invariant #1). `pypdf` and PyYAML are already available (`pyproject.toml` `dev` extra / transitively via `sphinx`). `29-RESEARCH.md` § "Package Legitimacy Audit" records "no packages to check"; the one new external name, the Ubuntu apt package `fonts-noto-cjk`, is outside the gate's ecosystems and is handled as T-29-02 in Plan 03 |
</threat_model>

<verification>
1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q` → `3 passed`
2. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest -q` → same failure set as the pre-plan baseline; record both counts verbatim in the SUMMARY
3. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m sphinx -b html -q docs/source /tmp/p29-01-html` → exit 0
4. `git diff a616b97..HEAD --name-only` lists exactly `.readthedocs.yaml`, `docs/source/conf.py`, `tests/test_readthedocs_config.py` (plus `.planning/` docs)
5. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run black --check .` and `uv run ruff check .` → exit 0
</verification>

<success_criteria>
- `.readthedocs.yaml` exists at the repository root in D-06's commit-1 (HTML-only) shape, with no `formats:`, no `build.jobs`, no `build.apt_packages`, and no reference to tox
- `.readthedocs.yaml`'s `build.tools.python` equals `.github/workflows/docs.yml`'s pinned `python-version`, bound by a test
- `docs/source/conf.py` resolves `language` through `_resolve_language()` with `READTHEDOCS_LANGUAGE` → `SPHINX_LANGUAGE` → `"en"` precedence, and `html_context["language"]` still reads the resolved value
- The resolved value with both env vars unset is `"en"` — measurably unchanged from the pre-phase state
- `tests/test_readthedocs_config.py` holds three passing, hermetic, plain-function tests
- Nothing under `typsphinx/` changed; no dependency was added; `docs/build_multilang.py` and `tox.ini` are untouched
</success_criteria>

<output>
Create `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-01-SUMMARY.md` when done.

Record in the SUMMARY, verbatim: the pre-plan and post-plan full-suite pass/fail counts (so "as green
as before" is an evidenced claim, not an assertion), and the resolved `language` value printed with
both env vars unset.
</output>
