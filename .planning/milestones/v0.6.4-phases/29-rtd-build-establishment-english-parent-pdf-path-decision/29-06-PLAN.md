---
phase: 29-rtd-build-establishment-english-parent-pdf-path-decision
plan: 06
type: execute
wave: 6
depends_on: ["29-04", "29-05"]
files_modified:
  - docs/source/index.rst
  - README.md
  - tests/test_readthedocs_config.py
  - .planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md
autonomous: true
requirements: [RTD-03]
must_haves:
  truths:
    - "This plan executes ONLY if Plan 04's `## Branch Decision` selected `branch-b`. If it selected `branch-a` or `stop-consult-owner`, the plan records a one-line skip note naming the recorded decision and performs no other work — in particular it makes no edit to docs/source/index.rst or README.md"
    - statement: "A real HTTP fetch of https://github.com/YuSabo90002/typsphinx/releases/latest/download/typsphinx.pdf follows its redirects to the current release's asset and returns 200 with a non-zero content length; the command, the redirect chain and the response are recorded verbatim in 29-VERIFICATION.md (SC#3 Branch B, RTD-03)"
      verification: backstop
    - "The recorded fetch demonstrates the `releases/latest/download/` form needs no per-release editing: the response is shown resolving against whatever the current release is, which is what satisfies RTD-03's 'stays correct across releases without editing' clause"
    - statement: "RTD serves no PDF for this version, evidenced by a recorded fetch of RTD's own download path returning something other than a PDF, and by the Downloads-menu observation Plan 04 recorded"
      verification: backstop
    - "docs/source/index.rst's Quick Links list gains one bullet linking to the Release PDF URL, matching the surrounding bullet style exactly (D-08)"
    - "README.md gains a small, self-contained additive block linking to the same URL — placed so it introduces no new github.io URL and occupies lines Phase 31's URL rewrite does not need to touch (D-08, D-09)"
    - "tests/test_readthedocs_config.py gains one test asserting the Release PDF URL is present in BOTH docs/source/index.rst and README.md, so a later rewrite of either file cannot silently drop the fallback (D-08's both-files requirement)"
    - "The documentation still builds after the content edits: a Sphinx HTML build of docs/source exits 0 and the new Quick Links bullet appears in the rendered index page"
    - "No file under typsphinx/ is modified, no dependency is added, and no existing README URL is rewritten — the URL cutover is Phase 31's work and this plan's edit is purely additive (D-09)"
  prohibitions:
    - statement: "This plan MUST NOT rewrite any existing published URL in README.md or pyproject.toml. The github.io → RTD cutover is Phase 31's (DOC-09), and touching those lines here would put two phases in conflict over the same text"
      status: required
      verification: "`git diff -- README.md` for this plan shows only added lines, and no line containing an existing github.io URL is modified or removed; `git status --porcelain pyproject.toml` is empty"
    - statement: "The fallback link MUST NOT be added to only one of the two files. D-08 requires both docs/source/index.rst and README.md, because the two audiences reach the project by different paths"
      status: required
      verification: "The added test asserts the URL is present in both files and fails if either is missing"
    - statement: "The fallback link MUST NOT be pinned to a specific release tag or version. Only the `releases/latest/download/` form satisfies RTD-03's edit-free-across-releases clause"
      status: required
      verification: "Human review of both edits: the URL contains `releases/latest/download/` and no version number or tag name"
    - statement: "Branch B MUST NOT be recorded as a regression or a failure of the PDF pipeline. The `typstpdf` CI regression gate and the tag-time PDF Release attachment are unaffected and MUST NOT be modified here"
      status: required
      verification: "`git status --porcelain .github/workflows/ tox.ini` is empty; the recorded verdict states the pipeline is unchanged"
  artifacts:
    - "docs/source/index.rst — one bullet added to the Quick Links list"
    - "README.md — one small additive block in the Documentation section"
    - "tests/test_readthedocs_config.py — `test_release_pdf_fallback_link_present` added"
    - "29-VERIFICATION.md — `## SC#3 Branch B — Release PDF fallback` and `## SC#3 Branch B — RTD-03 verdict` sections appended"
  key_links:
    - "`.github/workflows/docs.yml`'s tag-time 'Upload PDF to Release' step → the `typsphinx.pdf` Release asset → the `releases/latest/download/typsphinx.pdf` URL. That existing step is what makes the edit-free fallback URL work at all; it is NOT modified here, and if it ever stopped running the fallback link would silently start 404ing"
    - "docs/source/index.rst Quick Links bullet ↔ README.md's additive block — bound by the new presence test. D-08 requires both; nothing but the test keeps a later single-file rewrite from dropping one"
    - "Plan 04's recorded blocked-or-failed log excerpt → this plan's existence. Branch B is triggered by evidence, and the recorded excerpt is the trigger on file"
---

<objective>
**Branch B only.** Record that the Release PDF URL resolves over real HTTP, then land the fallback link
in both `docs/source/index.rst` and `README.md` as a small additive edit — discharging RTD-03.

Purpose: the registry fetch was shown blocked or failed in Plan 04's recorded log excerpt, so RTD serves
no PDF. This is not a regression — the PDF remains a `tox -e docs-pdf` CI regression artifact and a
tag-time Release asset — but readers must be able to reach it. The `releases/latest/download/` form is
the whole point: it resolves against whatever the current release is, so RTD-03's "stays correct across
releases without editing" clause is satisfied structurally rather than by a maintenance promise.

**Wave note:** this plan is wave 6 rather than wave 5 purely so that it and Plan 05 never hold
`29-VERIFICATION.md` open in the same wave. It is Plan 05's *alternative*, not its successor — exactly
one of the two does real work, and the other records a skip.

Output: `29-VERIFICATION.md` with the recorded fetch and the RTD-03 verdict, one bullet in
`docs/source/index.rst`, one small block in `README.md`, and one test binding both.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-PATTERNS.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See `29-01-PLAN.md` § `<artifacts_this_phase_produces>` for the full phase-level list. This plan's own
additions, and only on Branch B:

- `tests/test_readthedocs_config.py` → new function `test_release_pdf_fallback_link_present()`
- `29-VERIFICATION.md` → new sections `## SC#3 Branch B — Release PDF fallback` and
  `## SC#3 Branch B — RTD-03 verdict`
- Content-only additions to `docs/source/index.rst` and `README.md` — no new symbol

No new file, no new dependency, nothing under `typsphinx/`, and no change to `.github/workflows/` or
`tox.ini`.
</artifacts_this_phase_produces>

<flagged_probe_assumptions>
All 6 unresolved spec-less edge-probe rows are enumerated in `29-01-PLAN.md` §
`<flagged_probe_assumptions>` (6 rows == 0 authored into `must_haves` + 6 flagged assumptions, no silent
drops). The row bearing on this plan, restated so it is self-contained:

- **RTD-03 / `unclassified`** — UNRESOLVED. The probe could not classify RTD-03, and its algorithmic
  edge families do not fit: RTD-03's real edge is *URL stability across releases*. It was measured on
  2026-07-25 (200, 1,678,961 bytes against v0.6.3) and is re-fetched here rather than cited, because a
  measurement taken before the phase began is not evidence about the phase's own state.
</flagged_probe_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Fetch the Release PDF URL over real HTTP and record that RTD serves no PDF</name>
  <files>.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` — read § "Branch Decision" FIRST to confirm `branch-b` was selected, and § "SC#2" for the Downloads-menu observation Plan 04 recorded
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md` — D-08 (the exact URL and the both-files requirement), and § "Specifics" for the 2026-07-25 measurement (200, 1,678,961 bytes) this fetch re-takes
    - `.github/workflows/docs.yml` — read the tag-time "Upload PDF to Release" step to see what produces the `typsphinx.pdf` Release asset the fallback URL resolves to. Do NOT modify this file
  </read_first>
  <action>
    First read `29-VERIFICATION.md` § "Branch Decision". If it does not name `branch-b`, append a single
    `## SC#3 Branch B — SKIPPED` line quoting the recorded decision verbatim and stop — make no edit to
    `docs/source/index.rst`, `README.md` or `tests/`.

    Otherwise run two one-off live fetches and record each command with its full output.

    Fetch 1 — the fallback URL. Issue a redirect-following HEAD (or a body-discarding GET) against
    `https://github.com/YuSabo90002/typsphinx/releases/latest/download/typsphinx.pdf`, capturing every
    redirect hop's status code, the final status code, the final effective URL and the content length.
    Record the whole chain: the redirect chain is what demonstrates the `releases/latest/` indirection
    resolving to a concrete release asset, and that is precisely RTD-03's edit-free clause.

    Fetch 2 — confirm RTD serves no PDF. Issue a redirect-following request against RTD's own download
    path for this project's English latest version and record the status code and what came back. The
    expected observation on Branch B is that no PDF is served; record whatever is actually observed,
    including the case where something unexpected is served — that would contradict Plan 04's verdict
    and must be surfaced, not smoothed over.

    Append `## SC#3 Branch B — Release PDF fallback` to `29-VERIFICATION.md` without altering any
    existing section: both commands verbatim with their full output, the UTC timestamp, the content
    length returned by fetch 1, and one sentence naming which release the URL resolved to. Cross-note
    the Downloads-menu observation Plan 04 recorded, so the two agree on the record.

    If the network is unavailable, record the attempted commands and their exact errors and mark the
    RTD-03 fetch `human_needed` with that reason — then stop before making any content edit. The
    documentation must not claim a link works when nobody has fetched it.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w 'code=%{http_code} url=%{url_effective} size=%{size_download}\n' -L https://github.com/YuSabo90002/typsphinx/releases/latest/download/typsphinx.pdf</automated>
  </verify>
  <acceptance_criteria>
    - If Plan 04 recorded `branch-a` or `stop-consult-owner`: `29-VERIFICATION.md` contains `## SC#3 Branch B — SKIPPED` quoting the recorded decision, and `git status --porcelain docs/ README.md tests/` is empty
    - Otherwise the verify command prints `code=200` with a `size=` value greater than zero
    - `29-VERIFICATION.md` § "SC#3 Branch B — Release PDF fallback" records both fetch commands verbatim with their full output, including every redirect hop of fetch 1
    - The section names the concrete release the `releases/latest/download/` URL resolved to, taken from the recorded effective URL — not from memory
    - The section records fetch 2's result and states whether it agrees with Plan 04's Downloads-menu observation; a disagreement is recorded as a finding
    - `git status --porcelain typsphinx/ .github/workflows/ tox.ini pyproject.toml` is empty
    - `git diff -- .planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` shows only added lines
  </acceptance_criteria>
  <done>The fallback URL is proven to resolve by a recorded redirect-following fetch naming the release it landed on, and RTD's own absence of a PDF is recorded and reconciled with Plan 04's observation — or the plan is explicitly recorded as skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Add the fallback link to docs/source/index.rst and README.md, bound by one test</name>
  <files>docs/source/index.rst, README.md, tests/test_readthedocs_config.py</files>
  <read_first>
    - `docs/source/index.rst` — read the `Quick Links` section: three existing bullets of the form `- **Name**: <url>` (GitHub Repository / PyPI Package / Issue Tracker). The new bullet must match that style exactly
    - `README.md` — read the `## Documentation` section (its bold intro line and the `Quick links:` bullet list of github.io deep links, ending before `## Contributing`). Those github.io lines are **Phase 31's** to rewrite — do not touch them
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md` — D-08 (both files), D-09 (keep the README edit small and additive because Phase 31 rewrites the same file)
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md` — § "Code Examples" → "D-08 fallback block (Branch B only — index.rst Quick Links)" for the exact bullet wording
    - `tests/test_readthedocs_config.py` — the file being extended; reuse its `REPO_ROOT`-relative path-constant idiom and its plain-function, descriptive-failure-message style
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` — § "Branch Decision" (skip this task unless `branch-b`) and § "SC#3 Branch B — Release PDF fallback" for the fetch that justifies publishing the link
  </read_first>
  <action>
    Skip entirely if Task 1 recorded `## SC#3 Branch B — SKIPPED`.

    Edit 1 — `docs/source/index.rst`. Append one bullet to the end of the existing `Quick Links` list,
    matching the surrounding `- **Name**: <url>` style: a `Download PDF` entry noting it is typst-built
    and always current, pointing at
    `https://github.com/YuSabo90002/typsphinx/releases/latest/download/typsphinx.pdf`. Change nothing
    else in the file — no reordering of the existing bullets, no toctree change.

    Edit 2 — `README.md`. Add ONE self-contained line to the `## Documentation` section, placed after
    the existing `Quick links:` bullet list and before the next `##` heading, so it does not interleave
    with the github.io deep links Phase 31 rewrites. Word it as a bold-labelled PDF download line naming
    the same URL and noting the PDF is produced by typsphinx's own `typstpdf` builder and always
    resolves to the current release. It must introduce no github.io URL and must not be pinned to a
    version or tag — the `releases/latest/download/` form is the requirement. Keep it to a single added
    line plus at most one surrounding blank line: Phase 31 rewrites this same file and the two phases
    must not contend for the same text (D-09).

    Edit 3 — `tests/test_readthedocs_config.py`. Add one function,
    `test_release_pdf_fallback_link_present()`, following the file's existing conventions: a
    `REPO_ROOT`-relative path constant for `docs/source/index.rst` (README's constant may already exist
    in this suite's other modules — declare a local one here rather than importing across test
    modules), and an assertion that the exact `releases/latest/download/typsphinx.pdf` URL appears in
    both files, with a per-file descriptive failure message stating that D-08 requires both because the
    two audiences reach the project by different paths. Also assert the URL contains
    `releases/latest/download/` and no version number, so a later well-meaning edit cannot pin it to a
    tag. Give the test a docstring stating that it exists because Phase 31 rewrites `README.md` and this
    fallback link must survive that rewrite.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m sphinx -b html -q docs/source /tmp/p29-06-html && grep -q 'releases/latest/download/typsphinx.pdf' /tmp/p29-06-html/index.html && echo BRANCH_B_DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Skipped-branch case: `## SC#3 Branch B — SKIPPED` is present and `git status --porcelain docs/ README.md tests/` is empty
    - Otherwise the verify command prints `BRANCH_B_DOCS_OK`: the whole test file passes, the Sphinx HTML build exits 0, and the rendered `index.html` contains the fallback URL
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q` reports `5 passed`
    - `git diff -- README.md | grep -c '^-'` reports 0 — the README edit is purely additive and removed or altered no existing line
    - `git diff -- README.md | grep -c '^+'` reports 3 or fewer — the edit stayed small, per D-09
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib;u='releases/latest/download/typsphinx.pdf';[print(f,u in pathlib.Path(f).read_text(encoding='utf-8')) for f in ('README.md','docs/source/index.rst')]"` prints `True` for both files
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib,re;d=pathlib.Path('README.md').read_text(encoding='utf-8');new=[l for l in d.splitlines() if 'releases/latest/download' in l];assert new and not any(re.search(r'v?\d+\.\d+\.\d+',l) for l in new), new;print('unpinned ok')"` prints `unpinned ok`
    - `git status --porcelain typsphinx/ .github/workflows/ tox.ini pyproject.toml` is empty
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run black --check tests/test_readthedocs_config.py` and `uv run ruff check tests/test_readthedocs_config.py` both exit 0
  </acceptance_criteria>
  <done>Both files carry the unpinned `releases/latest/download/` link, the presence test binds them together and passes, the rendered index page shows the link, and no existing README line was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Record the RTD-03 verdict</name>
  <files>.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` — § "Branch Decision", § "SC#2", and § "SC#3 Branch B — Release PDF fallback" for the evidence being consolidated
    - `.planning/REQUIREMENTS.md` — the RTD-02 and RTD-03 texts, and § "Milestone Invariants" #7
    - `.planning/ROADMAP.md` — § "Phase 29" success criterion 3's Branch B clause, and the "RTD-03 is a conditional fallback" paragraph for the correct framing of what Branch B does and does not mean
  </read_first>
  <action>
    Skip entirely if Task 1 recorded `## SC#3 Branch B — SKIPPED`.

    Append `## SC#3 Branch B — RTD-03 verdict` to `29-VERIFICATION.md`, without altering any existing
    section.

    Record, each as its own line: that Plan 04's recorded excerpt showed the registry fetch blocked or
    failed (quote the excerpt's decisive line); that RTD therefore serves no PDF (cite Task 1's fetch 2
    and Plan 04's Downloads-menu observation); that the fallback URL was fetched over real HTTP and
    returned 200 with the recorded content length, resolving to the named release; and that the link is
    now published from both `docs/source/index.rst` and `README.md`, bound by
    `test_release_pdf_fallback_link_present`.

    Then record the framing explicitly, because it is easy to get wrong later: Branch B is **not** a
    failure of the PDF pipeline and **not** a regression. The `typstpdf` regression gate in
    `.github/workflows/docs.yml` and the tag-time PDF Release attachment are unchanged and still
    running; RTD-02 degrades to HTML-only on RTD by the owner's pre-agreed decision of 2026-07-25, and
    RTD-03's `releases/latest/download/` path is taken. State that no `.github/workflows/` or `tox.ini`
    file was modified by this phase.

    Close with the RTD-03 status: met, citing the recorded fetch — and the RTD-02 status: recorded as
    degraded-by-decision on Branch B, citing the branch decision, rather than as met or as failed.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib,sys;t=pathlib.Path('.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md').read_text(encoding='utf-8');skip='## SC#3 Branch B — SKIPPED' in t;need=['## SC#3 Branch B — RTD-03 verdict','releases/latest/download/typsphinx.pdf','RTD-02','RTD-03','test_release_pdf_fallback_link_present'];miss=[] if skip else [s for s in need if s not in t];print('skip',skip,'MISSING:',miss);sys.exit(1 if miss else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Skipped-branch case: `## SC#3 Branch B — SKIPPED` is present and this task added nothing
    - Otherwise the verify command prints `MISSING: []` and exits 0
    - The verdict section quotes Plan 04's decisive blocked-or-failed log line as the fallback's trigger
    - The verdict section cites Task 1's recorded 200 response and content length, and names the release the URL resolved to
    - The verdict section names both published locations and the test that binds them
    - The verdict section states explicitly that Branch B is not a regression, that `.github/workflows/` and `tox.ini` were not modified, and that RTD-02 is degraded-by-decision rather than met or failed
    - `git status --porcelain typsphinx/ .github/workflows/ tox.ini pyproject.toml` is empty
    - `git diff -- .planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` shows only added lines
  </acceptance_criteria>
  <done>RTD-03 is recorded as met on recorded evidence, RTD-02 is recorded as degraded-by-decision with its trigger quoted, and the unchanged CI pipeline is stated on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Releases CDN → readers | The published fallback artifact is served entirely outside this phase's build pipeline. The only control this repository holds is the URL form it publishes |
| documentation content → readers | An added hyperlink. No untrusted input is parsed and no code path is added |

## STRIDE Threat Register

ASVS L1; reused from `29-RESEARCH.md` § "Security Domain". V14 Configuration is the applicable category;
V5 does not apply — this plan adds documentation prose and one presence assertion.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-14 | Spoofing | A published documentation link that silently stops resolving — the exact bug class that motivated this milestone (seven dead README links unnoticed for months) | medium | mitigate | The URL is fetched over real HTTP before publication and the response recorded; the `releases/latest/download/` form removes per-release editing as a failure mode; `test_release_pdf_fallback_link_present` keeps a later single-file rewrite from dropping it. Phase 31's repo-wide link guard (CI-05) then covers ongoing reachability |
| T-29-15 | Tampering | A version-pinned fallback URL that goes stale as releases advance, serving an old PDF as current | medium | mitigate | The added test asserts the URL contains `releases/latest/download/` and carries no version number, so a later edit cannot pin it |
| T-29-16 | Repudiation | Branch B later being misread as a PDF-pipeline failure, prompting someone to "fix" the CI regression gate or the Release attachment | low | mitigate | Task 3 records the framing explicitly — degraded-by-decision, pipeline unchanged — and this plan's prohibitions forbid touching `.github/workflows/` or `tox.ini` |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. Zero new packages phase-wide (milestone invariant #1); `29-RESEARCH.md` § "Package Legitimacy Audit" records "no packages to check" |
</threat_model>

<verification>
1. `curl` fetch of the fallback URL → `code=200` with a non-zero size, redirect chain recorded verbatim
2. RTD's own download path fetched and recorded, reconciled against Plan 04's Downloads-menu observation
3. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q` → `5 passed`
4. Sphinx HTML build of `docs/source` exits 0 and the rendered `index.html` contains the fallback URL
5. `git diff -- README.md` shows only added lines, 3 or fewer
6. `git status --porcelain typsphinx/ .github/workflows/ tox.ini pyproject.toml` is empty
</verification>

<success_criteria>
- The `releases/latest/download/typsphinx.pdf` URL is proven to resolve by a recorded redirect-following fetch that names the release it landed on
- RTD serving no PDF is recorded and reconciled with Plan 04's observation
- The fallback link is published from both `docs/source/index.rst` and `README.md`, unpinned, additive, with no existing README line touched
- One test binds the link's presence in both files and the whole test file passes
- RTD-03 is recorded as met on evidence; RTD-02 is recorded as degraded-by-decision with its trigger quoted
- Nothing under `typsphinx/`, `.github/workflows/` or `tox.ini` changed; no dependency added
</success_criteria>

<output>
Create `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-06-SUMMARY.md` when done.

If the plan self-skipped because Branch A was selected, the SUMMARY must say so in its first line and
quote the recorded decision, so a reader does not mistake an empty plan for unfinished work.
</output>
