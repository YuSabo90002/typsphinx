---
phase: 29-rtd-build-establishment-english-parent-pdf-path-decision
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - tests/test_readthedocs_config.py
  - .readthedocs.yaml
  - .planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md
autonomous: true
requirements: [RTD-02]
must_haves:
  truths:
    - "`.readthedocs.yaml` declares `formats: [pdf]` AND a `build.jobs.build.pdf` override together — never one without the other, because `formats: [pdf]` alone activates RTD's own LaTeX pipeline and would ship an undogfooded PDF (RESEARCH Pitfall 1)"
    - "`build.jobs.build.pdf` runs `sphinx-build -b typstpdf` into a temporary directory and copies only `*.pdf` into `$READTHEDOCS_OUTPUT/pdf/` — the builder's output directory is never `$READTHEDOCS_OUTPUT` itself (D-04)"
    - "`build.jobs.build.pdf` creates `$READTHEDOCS_OUTPUT/pdf/` with an explicit mkdir BEFORE the copy, and the mkdir command appears earlier in the command list than the copy — the subdirectory is not pre-created by RTD (RESEARCH Pitfall 3)"
    - "`build.apt_packages` contains `fonts-noto-cjk`, because the English documentation itself contains CJK strings and typst-py's embedded fonts have no CJK coverage while Typst's fallback is silent (D-10)"
    - "No command in `build.jobs.build.pdf` delegates to this repository's tox environments — RTD provisions its own environment via `python.install` (CLAUDE.md, RESEARCH § Common Pitfalls)"
    - "The commit-1 keys are unchanged by this edit: `version`, `build.os`, `build.tools.python`, `sphinx.configuration` and `python.install` still satisfy `test_readthedocs_yaml_shape` and `test_build_python_matches_docs_workflow`"
    - "Running the manifest's own PDF command sequence locally (with `$READTHEDOCS_OUTPUT` pointed at a temp directory) leaves exactly one file, `typsphinx.pdf`, in the simulated download directory, while the builder's own output directory holds more than one file — proving the `*.pdf` filter is load-bearing rather than decorative (D-04, D-05)"
    - "A D-12 baseline for this commit is recorded in 29-VERIFICATION.md: the locally built PDF's page count, its full embedded `/BaseFont` list, the local interpreter version, and the commit SHA — so Plan 05's comparison has a dated, per-commit reference instead of a remembered number"
    - "No file under typsphinx/ is modified, and no new Python dependency is declared (milestone invariants #1 and #3)"
  prohibitions:
    - statement: "A PDF produced by RTD's own LaTeX pipeline MUST NOT be published or described as typsphinx's own `typstpdf` output. `formats: [pdf]` without the `build.jobs.build.pdf` override is the exact configuration that would cause this silently"
      status: required
      verification: "Human review of `.readthedocs.yaml`: `formats` and `build.jobs.build.pdf` are both present in the same commit; and Plan 04's raw-log read confirms zero latexmk / pdflatex / .tex lines"
    - statement: "The `typstpdf` builder's output directory MUST NOT be `$READTHEDOCS_OUTPUT/pdf/` — the builder writes many non-PDF files there and RTD's download area must receive only the PDF"
      status: required
      verification: "`test_readthedocs_yaml_pdf_override` asserts the sphinx-build command carries no reference to the RTD output variable; Task 3's local simulation shows a single file in the simulated download directory"
    - statement: "No secret, token, or credential may be referenced or echoed by any `build.jobs` command — RTD build logs are public for a public project"
      status: required
      verification: "Human review of the `build.jobs.build.pdf` block: the only variable referenced is the RTD-provided output path; no other environment variable, no secret name, no credential appears"
    - statement: "This plan MUST NOT add a Japanese-language PDF build, a Japanese locale target, or any second document target. `fonts-noto-cjk` is scoped strictly to the four CJK strings inside the English documentation and is not a step toward the deferred I18N-03"
      status: required
      verification: "Human review: `build.jobs.build.pdf` contains exactly one sphinx-build invocation, with no locale or language flag, and `docs/source/conf.py`'s `typst_documents` is unchanged"
  artifacts:
    - "`.readthedocs.yaml` — extended in place with `build.apt_packages`, `build.jobs.build.pdf` and a top-level `formats` list; commit-1 keys untouched"
    - "tests/test_readthedocs_config.py — `test_readthedocs_yaml_pdf_override` added"
    - "29-VERIFICATION.md — `## Pre-RTD Local Simulation of build.jobs.build.pdf` and `## D-12 Baseline (local, this commit)` sections appended"
  key_links:
    - "`formats: [pdf]` ↔ `build.jobs.build.pdf` — RTD's override REPLACES the default LaTeX step for that format, so the two must land in the same commit. Either alone is a defect: `formats` alone runs LaTeX; the override alone means RTD never runs the PDF step at all"
    - "`build.jobs.build.pdf`'s mkdir of `$READTHEDOCS_OUTPUT/pdf/` → the cp into it. `$READTHEDOCS_OUTPUT` expands to `<checkout>/_readthedocs/` and the format subdirectory is created by RTD's post-build ingestion, not before the job runs — so the cp fails without the mkdir"
    - "`build.apt_packages: [fonts-noto-cjk]` → typst-py's `scan-fonts` discovery → the CJK glyphs in the built PDF. Nothing in the build fails if this link is broken; the only symptom is substituted glyphs in a successful build, which is why Plan 05's D-12 check 3 enumerates embedded fonts"
    - "Task 3's recorded baseline (page count + `/BaseFont` list + commit SHA) → Plan 05's D-12 comparison. Without a recorded per-commit baseline, the comparison would rest on a remembered number"
---

<objective>
Land D-06's **commit 2**: add `formats: [pdf]`, the `build.jobs.build.pdf` typstpdf override, and
`build.apt_packages: [fonts-noto-cjk]` to `.readthedocs.yaml` — then prove the command sequence works
by running it locally before spending an RTD build cycle on it, and capture the D-12 baseline for this
exact commit.

Purpose: this is the commit whose raw build log answers the milestone's one genuinely open empirical
unknown. It lands only now, after `/en/latest/` is already established and serving (Plan 02), so a PDF
step failure is isolated in a log with nothing else in it and RTD-04 is never violated. `formats:` and
the override are used **together**: the override replaces the default LaTeX step for that format, so
`formats: [pdf]` alone would silently ship RTD's own LaTeX PDF instead of typsphinx's.

Output: the PDF-enabled `.readthedocs.yaml`, a fourth test binding its structure, a recorded local
simulation of the exact command sequence, and a recorded per-commit D-12 baseline.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-PATTERNS.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-01-SUMMARY.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See `29-01-PLAN.md` § `<artifacts_this_phase_produces>` for the full phase-level list. This plan's own
additions:

- `.readthedocs.yaml` keys introduced here: `build.apt_packages`, `build.jobs`, `build.jobs.build`,
  `build.jobs.build.pdf`, `formats`
- `tests/test_readthedocs_config.py` → new function `test_readthedocs_yaml_pdf_override()`
- `29-VERIFICATION.md` → new sections `## Pre-RTD Local Simulation of build.jobs.build.pdf` and
  `## D-12 Baseline (local, this commit)`

No new file, no new dependency, nothing under `typsphinx/`, and **no committed script that downloads
or compares the RTD-built PDF** (D-15).
</artifacts_this_phase_produces>

<flagged_probe_assumptions>
All 6 unresolved spec-less edge-probe rows are enumerated in `29-01-PLAN.md` §
`<flagged_probe_assumptions>` (6 rows == 0 authored into `must_haves` + 6 flagged assumptions, no
silent drops). The row bearing on this plan, restated so this plan is self-contained:

- **RTD-02 / `unclassified`** — UNRESOLVED. The probe could not classify RTD-02. Manual read: its real
  edge is *silent glyph substitution inside a successful build*, a content-fidelity failure rather
  than an algorithmic edge family. This plan encodes the structural half (the `fonts-noto-cjk`
  provisioning and the output-filtering shape); Plan 05's D-12 four-check bar addresses the content
  half. Not converted into a probe-derived truth.

Additionally, two RESEARCH.md assumptions are load-bearing for this plan and are flagged rather than
treated as settled:

- **A1** — `fonts-noto-cjk` is assumed to be a valid, installable Ubuntu apt package name on RTD's
  `ubuntu-24.04` image. Not independently apt-cache-verified. A wrong name fails `apt-get install`
  loudly in the public raw log, which Plan 04 already reads end to end — so the confirmation point is
  that log, and Plan 04's acceptance criteria require the apt line to be recorded.
- **A2** — `/tmp/` is assumed writable inside RTD's build sandbox for the intermediate output
  directory. Task 3 proves the sequence works locally; RTD's own sandbox is confirmed only by Plan
  04's log. A permission failure would surface immediately and visibly there.
</flagged_probe_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Add test_readthedocs_yaml_pdf_override (RED)</name>
  <files>tests/test_readthedocs_config.py</files>
  <read_first>
    - `tests/test_readthedocs_config.py` — the file being extended; reuse its existing `_load_readthedocs_yaml()` helper and its plain-function, descriptive-failure-message conventions rather than introducing new ones
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md` — § "Code Examples" → "Recommended `.readthedocs.yaml` (Commit 2, PDF-enabled state)" for the exact command list this test must accept; § "Common Pitfalls" #1 and #3 for the two failure modes being encoded; § "Anti-Patterns to Avoid"
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md` — D-04 (temp-dir-then-copy, and the measured 31-file output), D-05 (`typsphinx.pdf`), D-10 (`fonts-noto-cjk`)
  </read_first>
  <action>
    Add one function, `test_readthedocs_yaml_pdf_override()`, to `tests/test_readthedocs_config.py`.
    Reuse `_load_readthedocs_yaml()`. Keep the plain-function style and give every assertion its own
    descriptive failure message naming the decision it protects.

    Assert, in this order:

    1. Top-level `formats` is a list containing `"pdf"`, and `build["jobs"]["build"]["pdf"]` exists —
       with a failure message stating that these two must land together because `formats` alone
       activates RTD's own LaTeX pipeline.
    2. `build["apt_packages"]` is a list containing `"fonts-noto-cjk"` — message referencing D-10 and
       the silent-font-fallback failure mode.
    3. `build["jobs"]["build"]["pdf"]` is a non-empty list and every element is a string.
    4. Exactly one command in that list contains the substring `sphinx-build -b typstpdf`. Capture its
       index.
    5. That sphinx-build command does NOT reference the RTD output path variable
       (`READTHEDOCS_OUTPUT` must not appear in it) — message referencing D-04: the builder writes
       many non-PDF files into its output directory, so it must target a temporary directory.
    6. At least one command both creates a directory and references the RTD output variable together
       with a `pdf` path segment — the explicit mkdir that RTD does not perform for you. Capture its
       index.
    7. At least one command copies files whose name pattern ends in `.pdf` into the RTD output path —
       capture its index — and assert that command's pattern is a `*.pdf` glob rather than a recursive
       or whole-directory copy.
    8. Ordering: the mkdir command's index is lower than the copy command's index. State in the
       message that a copy before the mkdir fails with a cannot-create-regular-file error.
    9. No command in the list references this repository's tox runner — message stating RTD provisions
       its own environment through `python.install` and that nesting tox creates a redundant second
       venv with no working locked-sync wiring.
    10. Exactly one sphinx-build invocation exists in the whole list, and it carries no locale or
        language flag — guarding the D-11 boundary that `fonts-noto-cjk` is not a step toward a
        Japanese PDF.

    This test is expected to FAIL at the end of this task, because `.readthedocs.yaml` is still in its
    commit-1 shape. Commit as a `test(29-03): ...` commit whose body states the suite is intentionally
    RED until Task 2.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q > /tmp/p29-03-red.txt 2>&1; grep -q '1 failed, 3 passed' /tmp/p29-03-red.txt && echo RED_BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `RED_BASELINE_OK` — exactly one test fails (`test_readthedocs_yaml_pdf_override`) and the three tests from Plan 01 still pass
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py::test_readthedocs_yaml_pdf_override -q 2>&1 | grep -q 'formats'` — the failure message names the missing `formats` key rather than raising an unrelated KeyError with no explanation
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py --collect-only -q` lists exactly 4 node IDs, all plain functions with no `::Test` class segment
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run black --check tests/test_readthedocs_config.py` and `uv run ruff check tests/test_readthedocs_config.py` both exit 0
    - `git status --porcelain typsphinx/ .readthedocs.yaml pyproject.toml` is empty — only the test file changed
  </acceptance_criteria>
  <done>The fourth test exists, fails for the documented reason, and encodes D-04, D-10, D-11, Pitfall 1, Pitfall 3 and the ordering constraint as assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Extend .readthedocs.yaml with the PDF override (D-06 commit 2)</name>
  <files>.readthedocs.yaml</files>
  <read_first>
    - `.readthedocs.yaml` — the file being modified; its commit-1 keys must stay byte-unchanged
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md` — § "Code Examples" → "Recommended `.readthedocs.yaml` (Commit 2, PDF-enabled state)" holds the exact YAML; § "Common Pitfalls" #3 for the mkdir requirement; § "Code Examples" → "Uncertain spellings" for the `-d` doctrees flag rationale and the temp-directory naming discretion
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md` — D-04, D-05, D-10, D-11
    - `tox.ini` — read `[testenv:docs-pdf]` (`sphinx-build -b typstpdf source _build/pdf` with `changedir = docs`) as a **content** reference for what the PDF build logically does. Do NOT copy its invocation shape: RTD's `sphinx.configuration` and `build.jobs` take paths from the repository root, with no changedir and no tox wrapper
    - `docs/source/conf.py` — read `typst_documents` at :93-95 to confirm the PDF's target name is `typsphinx`, which is where the `typsphinx.pdf` filename comes from (D-05)
  </read_first>
  <action>
    Edit `.readthedocs.yaml` in place, adding three things and changing nothing else.

    Under the existing `build:` block, as siblings of `os:` and `tools:`, add:
    - `apt_packages:` as a list containing `fonts-noto-cjk`
    - `jobs:` → `build:` → `pdf:` as an ordered list of exactly four shell commands:
      1. create the intermediate doctrees directory: `mkdir -p /tmp/typst-pdf-build/doctrees`
      2. run the builder into a temporary output directory with the doctree cache separated:
         `sphinx-build -b typstpdf -d /tmp/typst-pdf-build/doctrees docs/source /tmp/typst-pdf-build/out`
      3. create RTD's download subdirectory, which RTD does not pre-create:
         `mkdir -p "$READTHEDOCS_OUTPUT/pdf/"`
      4. copy only the PDF: `cp /tmp/typst-pdf-build/out/*.pdf "$READTHEDOCS_OUTPUT/pdf/"`

    Add a new top-level `formats:` list containing `pdf`.

    Keep 2-space indentation and the existing comment density. Add short `#` comments for only the
    non-obvious *why*: (a) that the builder emits many files into its output directory — the PDF plus
    a `_template.typ`, one `.typ` per document, and a doctrees tree — so it must write to a temporary
    directory and only the PDF may be copied into RTD's download area; (b) that
    `$READTHEDOCS_OUTPUT` expands to a `_readthedocs/` directory whose format subdirectory is created
    by RTD's post-build ingestion, not before this job runs, hence the explicit mkdir; (c) that the
    English documentation itself contains CJK strings while typst-py's embedded fonts have no CJK
    coverage and Typst's font fallback is silent, so without the apt package the PDF renders
    substituted glyphs in a build that reports success; (d) that `formats` and this override are used
    together because the override replaces the default LaTeX step for that format.

    Do NOT change `version`, `build.os`, `build.tools.python`, `sphinx.configuration` or
    `python.install`. Do NOT override the `html` format — RTD's `sphinx:` key already drives it. Do NOT
    reference this repository's tox environments from any command. Do NOT reference any environment
    variable other than the RTD-provided output path — RTD build logs are public. Do NOT add a second
    sphinx-build invocation, a locale flag, or a language flag: `fonts-noto-cjk` is scoped to the four
    CJK strings in the English documentation and is not a step toward the deferred Japanese PDF.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 with `4 passed` — the new override test is green and the three commit-1 tests still pass
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import yaml;d=yaml.safe_load(open('.readthedocs.yaml'));c=d['build']['jobs']['build']['pdf'];assert len(c)==4, c;assert 'READTHEDOCS_OUTPUT' not in c[1];assert c[1].index('-b typstpdf')>0;print('job shape ok')"` prints `job shape ok`
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import yaml;d=yaml.safe_load(open('.readthedocs.yaml'));assert d['formats']==['pdf'];assert d['build']['apt_packages']==['fonts-noto-cjk'];assert d['sphinx']['configuration']=='docs/source/conf.py';e=d['python']['install'][0];assert (e['method'],e['command'])==('uv','sync');print('keys ok')"` prints `keys ok`
    - `git diff a616b97..HEAD -- .readthedocs.yaml | grep -c '^-'` reports 0 — the commit-2 edit is purely additive and removed no commit-1 line
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib;t=pathlib.Path('.readthedocs.yaml').read_text();assert 'tox' not in t;print('no tox delegation')"` prints `no tox delegation`
    - `git status --porcelain typsphinx/ docs/ pyproject.toml` is empty
  </acceptance_criteria>
  <done>`.readthedocs.yaml` carries `formats: [pdf]`, `build.apt_packages: [fonts-noto-cjk]` and a four-command `build.jobs.build.pdf` override with the mkdir before the copy; all four tests pass; the commit-1 keys are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Run the manifest's own PDF command sequence locally and record the D-12 baseline</name>
  <files>.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md</files>
  <read_first>
    - `.readthedocs.yaml` — the command list being simulated; read it so the simulation runs the same logical sequence rather than an approximation
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md` — D-12, D-13 (an exact font-list match is explicitly rejected as a bar), D-14, D-15 (this is a one-off hand-run sequence; **no script is committed**); § "Specifics" for the 2026-07-25 measurements this simulation re-takes for the current commit
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` — the file being appended to
    - `CLAUDE.md` — § "Worktree-isolated execution": the sandbox-compatible invocation is `uv run python -m sphinx`, not a bare `sphinx-build` console script
  </read_first>
  <action>
    Run the `build.jobs.build.pdf` command sequence locally, substituting a temporary directory for
    `$READTHEDOCS_OUTPUT` and the sandbox-compatible `uv run python -m sphinx` invocation for the
    `sphinx-build` console script (the console-script form is unreliable under this project's NixOS
    sandbox; the builder and its arguments are identical). Then record the result and the D-12
    baseline. Run these as one-off commands; **do not commit any script** — D-15 rejects a committed
    comparison artifact because the RTD-built PDF is unreachable from CI, so a committed script would
    look like a gate that never runs.

    Simulation: clear a fresh temp root, create the doctrees directory, build with
    `-b typstpdf -d <tmp>/doctrees docs/source <tmp>/out`, create `<tmp>/_readthedocs/pdf/`, copy
    `<tmp>/out/*.pdf` into it. Then capture: the count and full listing of `<tmp>/out`, and the
    listing of `<tmp>/_readthedocs/pdf/`. Do not compare against a remembered file count — record what
    this run actually produced. The assertion that matters is that the simulated download directory
    holds exactly `typsphinx.pdf` while the builder's own output directory holds more than one entry.

    D-12 baseline: with `pypdf` (already in the `dev` extra — add no dependency), read the built
    `typsphinx.pdf` and capture its page count, the full sorted list of embedded `/BaseFont` names,
    and the byte size. Also capture the local interpreter version, the resolved commit SHA
    (`git rev-parse HEAD`), and `.readthedocs.yaml`'s `build.tools.python` value.

    Append two sections to `29-VERIFICATION.md` without altering existing sections:
    `## Pre-RTD Local Simulation of build.jobs.build.pdf` — every command and its full output
    verbatim, plus one sentence on what it proves (the manifest's command sequence is syntactically
    and semantically sound before an RTD build cycle is spent on it, and the `*.pdf` filter is
    load-bearing).
    `## D-12 Baseline (local, this commit)` — the page count, the full `/BaseFont` list, the byte
    size, the commit SHA, the local interpreter version, and `build.tools.python`. Add an explicit
    note that the local interpreter minor may differ from RTD's provisioned `build.tools.python`, that
    the D-12 bar (page count and extracted text) is not expected to be interpreter-minor sensitive,
    and that if Plan 05 observes a mismatch it must be recorded verbatim rather than explained away.
    Add a second explicit note that per D-13 an exact embedded-font-list match is **not** the bar —
    several fonts in this list are host-provided; only CJK *coverage* is asserted, in Plan 05.
  </action>
  <verify>
    <automated>rm -rf /tmp/p29-03-sim && mkdir -p /tmp/p29-03-sim/doctrees && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m sphinx -b typstpdf -q -d /tmp/p29-03-sim/doctrees docs/source /tmp/p29-03-sim/out && mkdir -p /tmp/p29-03-sim/_readthedocs/pdf/ && cp /tmp/p29-03-sim/out/*.pdf /tmp/p29-03-sim/_readthedocs/pdf/ && test "$(ls -1 /tmp/p29-03-sim/_readthedocs/pdf/)" = "typsphinx.pdf" && test "$(ls -1 /tmp/p29-03-sim/out | wc -l)" -gt 1 && echo SIM_OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `SIM_OK`: the builder ran, exactly `typsphinx.pdf` landed in the simulated download directory, and the builder's own output directory held more than one entry
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pypdf;r=pypdf.PdfReader('/tmp/p29-03-sim/_readthedocs/pdf/typsphinx.pdf');print('pages',len(r.pages))"` prints a page count greater than 1
    - `29-VERIFICATION.md` contains the headings `## Pre-RTD Local Simulation of build.jobs.build.pdf` and `## D-12 Baseline (local, this commit)`
    - `29-VERIFICATION.md`'s baseline section contains the literal strings `pages`, `/BaseFont`, `build.tools.python`, and the output of `git rev-parse HEAD` for this commit — the baseline is dated and per-commit, not a remembered number
    - `29-VERIFICATION.md`'s baseline section contains an explicit note that per D-13 an exact embedded-font-list match is not the bar, and an explicit note about the possible local-vs-RTD interpreter-minor difference
    - `git status --porcelain` shows no new file outside `.planning/` — no comparison script was committed (D-15)
    - `git status --porcelain typsphinx/ tests/ docs/ pyproject.toml .readthedocs.yaml` is empty
    - Plan 02's and Task 1/2's existing sections of `29-VERIFICATION.md` are unchanged: `git diff -- .planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` shows only added lines
  </acceptance_criteria>
  <done>The manifest's exact PDF command sequence is proven to work locally, exactly one PDF reaches the simulated download directory, and a dated per-commit D-12 baseline (pages, fonts, size, SHA, interpreter) is recorded with D-13's and the interpreter-parity caveats stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.readthedocs.yaml` `build.jobs` → RTD build container shell | Maintainer-authored shell executed inside a third-party build sandbox. Not templated from any variable input, so there is no injection surface |
| `packages.typst.org` → RTD build container | New outbound network dependency: the four `@preview` Typst Universe packages fetched on a cold cache |
| Ubuntu apt mirror (RTD image) → RTD build container | New supply-chain trust point introduced by `build.apt_packages` |
| RTD build container → `$READTHEDOCS_OUTPUT/pdf/` → public download area | The published artifact boundary. Only `*.pdf` is permitted to cross it |

## STRIDE Threat Register

ASVS L1; reused from `29-RESEARCH.md` § "Security Domain" rather than re-derived. V14 Configuration is
the clearly-applicable category. V5 does not apply — `.readthedocs.yaml` is a static, maintainer-authored
config file and nothing this plan adds parses untrusted input.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering | The four `@preview` Typst packages fetched from `packages.typst.org` on a cold cache during `build.jobs.build.pdf` | medium | mitigate | Already mitigated at the milestone level: the four package versions are pinned by exact version across the repository's four version-sync surfaces (`writer.py`, `template_engine.py`, `templates/base.typ`, `examples/**/*.typ`), guarded by `tests/test_preview_version_sync.py`. This plan adds no new unpinned fetch and bumps no version (milestone invariant #2) |
| T-29-02 | Tampering | `build.apt_packages: [fonts-noto-cjk]` — a new supply-chain trust point (apt package fetched from RTD's build-image mirror) | low | accept | Ubuntu's own archive via RTD's image apt configuration; no third-party PPA or key is added, and the package is a font metapackage with no executable role in the build. A wrong or unavailable name fails `apt-get install` loudly in the public raw log, which Plan 04 reads end to end and records — see flagged assumption A1 |
| T-29-04 | Tampering | Silent Typst font fallback producing substituted glyphs in a PDF from a build that reports success | high | mitigate | Two layers: `build.apt_packages: [fonts-noto-cjk]` provisions CJK coverage (this plan), and Plan 05's D-12 four-check bar (page count, extracted text, embedded `/BaseFont` CJK-coverage enumeration, owner visual confirmation) verifies the content rather than the build status |
| T-29-10 | Tampering | Non-PDF build artifacts (`.typ` sources, `_template.typ`, doctree cache) leaking into RTD's public download area | medium | mitigate | D-04's temp-directory-then-filtered-copy shape, asserted structurally by `test_readthedocs_yaml_pdf_override` and demonstrated by Task 3's local simulation showing exactly one file in the simulated download directory |
| T-29-07 | Information Disclosure | `build.jobs` commands appear verbatim in RTD build logs, which are public for a public project | medium | mitigate | The four authored commands are `mkdir`, `sphinx-build` and `cp` only; the sole variable referenced is the RTD-provided output path. No secret, token or credential is named. Enforced by this plan's prohibition and by human review of the committed block |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan — zero new Python/npm/crates packages phase-wide (milestone invariant #1). `pypdf` used by Task 3 is already declared in the `dev` extra. `29-RESEARCH.md` § "Package Legitimacy Audit" records "no packages to check"; the one new external name is the Ubuntu apt package handled as T-29-02 |
</threat_model>

<verification>
1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readthedocs_config.py -q` → `4 passed`
2. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest -q` → same failure set as the pre-plan baseline; record both counts in the SUMMARY
3. The local simulation prints `SIM_OK` and its full output is recorded in `29-VERIFICATION.md`
4. `git diff a616b97..HEAD -- .readthedocs.yaml | grep -c '^-'` → 0 (the commit-2 edit is additive)
5. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run black --check .` and `uv run ruff check .` → exit 0
6. `git status --porcelain typsphinx/` empty
</verification>

<success_criteria>
- `.readthedocs.yaml` declares `formats: [pdf]`, `build.apt_packages: [fonts-noto-cjk]` and a four-command `build.jobs.build.pdf` override, in one additive commit, with the commit-1 keys untouched
- The override builds into a temporary directory and copies only `*.pdf` into `$READTHEDOCS_OUTPUT/pdf/`, creating that directory first, in that order
- No command in the override delegates to tox, references a secret, or adds a second document target
- `test_readthedocs_yaml_pdf_override` binds all of the above structurally, and all four tests pass
- The command sequence is proven to work by a local run whose output is recorded verbatim, before any RTD build cycle is spent
- A dated, per-commit D-12 baseline (page count, embedded `/BaseFont` list, byte size, commit SHA, interpreter version) is recorded, with D-13's and the interpreter-parity caveats stated
- No committed comparison script; nothing under `typsphinx/`; no new dependency
</success_criteria>

<output>
Create `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-03-SUMMARY.md` when done.

Record in the SUMMARY: the local simulation's actual output-directory file count (do not restate the
2026-07-25 figure), the baseline page count and font list, and the pre/post full-suite counts.
</output>
