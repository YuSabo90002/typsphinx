---
phase: 30-japanese-rtd-site-hand-rolled-machinery-orphan-removal
plan: 04
type: execute
wave: 2
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - .planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-EVIDENCE.md
autonomous: true
requirements: [I18N-02, DOC-08]
must_haves:
  truths:
    - "A repo-wide grep over every git-tracked file, run on the merged post-deletion tree and excluding only `CHANGELOG.md` and `.planning/` by standing precedent, returns ZERO hits for the switcher token set — down from a measured 37 hits across 9 files before the phase (ROADMAP SC#1)"
    - "The one measured grep false positive that must SURVIVE is still present: `tests/fixtures/confval_field_body_render_gate/index.rst`'s `.. confval:: html_sidebars` directive, an unrelated Sphinx-directive test fixture"
    - "The static-path token is scoped to `docs/source/conf.py` and is absent there, while all 7 measured occurrences in `examples/basic/conf.py`, `examples/advanced/conf.py`, `tests/fixtures/glob_image_render_gate/conf.py` and `tests/fixtures/static_asset_copy_render_gate/{conf.py,index.rst}` survive untouched — a literal repo-wide reading of that token would demand breaking two bundled examples and two unrelated fixtures"
    - "`docs/source/conf.py`'s region 1 and region 3 hash to `06f177f82fb153ca4971d258989e7ede2a4e5ffa018a5caaa8ab0a56e6f7b466` and `cd245215f80b2552dcba7b01d74a36de0ef0b2323df665e88390381c2cd5d169` on the merged tree — Phase 29's language seam and Phase 30.1's Typst font block are byte-unchanged (ROADMAP SC#2)"
    - "`git diff --stat` for the whole phase against its base shows zero paths under `typsphinx/` (milestone invariant #3)"
    - "A full `uv run python -m pytest` run on the merged tree is green — this run, not any deletion commit, is DOC-08's proof (ROADMAP SC#3)"
    - "`uv run tox -e docs-html` and `uv run tox -e docs-pdf` are both green on the merged tree, producing `docs/_build/html/index.html` and at least one PDF under `docs/_build/pdf/` — the two paths `.github/workflows/docs.yml` uploads and publishes (ROADMAP SC#5)"
    - "Every `tox -e <env>` string in `.github/workflows/docs.yml` resolves to a section that exists in `tox.ini`, and every `docs/_build/` path the workflow references is one the two tox environments actually produced — the caller/callee mismatch that would only surface at the milestone merge is closed by static cross-check plus a real build"
    - "The documentation root URL still resolves (RTD-04 standing invariant): a live fetch of `https://typsphinx.readthedocs.io/` returns 200 after redirect"
    - "`30-EVIDENCE.md` records every gate's exact command and verbatim output, and is named `30-EVIDENCE.md` rather than `30-VERIFICATION.md` — the latter is a reserved filename that `/gsd-verify-work` overwrites"
    - statement: "An actual `docs.yml` GitHub Actions run is observed green on the post-deletion tree (ROADMAP SC#5). The workflow triggers only on a push to `main`, a `v*` tag, or a pull request targeting `main`, and declares no `workflow_dispatch`; under `branching_strategy: milestone` no such event occurs until the milestone pull request is opened, so this is not observable inside the phase and is deferred to the milestone PR with the exact check named."
      verification: backstop
    - statement: "The published Read the Docs site stops serving the hand-rolled switcher markup. Measured before the phase: the live `/en/latest/` page contains one occurrence of the switcher wrapper class and one reference to the deleted stylesheet. Both should reach zero once Read the Docs rebuilds the merged branch, which cannot happen inside the phase because RTD builds the tracked branch, not a per-plan worktree branch."
      verification: backstop
    - statement: "Whether Furo's restored `ethical-ads` slot and variant selector render on the hosted site — both are `READTHEDOCS`-gated templates, measured absent in a local build, and their hosted behaviour depends on whether Read the Docs still injects that flag into the Jinja context under its Addons build."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT make the SC#1 grep pass by editing or deleting the unrelated files that legitimately contain the same Sphinx confval names. `examples/basic/conf.py`, `examples/advanced/conf.py`, the two render-gate fixtures, and the `confval` directive fixture are all real, working code with nothing to do with the language switcher; changing any of them to quiet a gate is pure gate-appeasement, and in the two bundled examples' case it would ship a broken sample — the precise defect class this milestone's predecessor found at its own close."
      status: required
      verification: "The 7 measured static-path occurrences across `examples/` and `tests/fixtures/` are still present and byte-unchanged, and the `confval` fixture line still exists; the gate's scoping is documented in `30-EVIDENCE.md` rather than achieved by deletion"
    - statement: "MUST NOT record a gate as passing on the strength of a prior phase's artefact or this phase's own SUMMARY files. Every criterion in the evidence record is a command run in this session with its verbatim output pasted; a citation is not a measurement. This is the standing honest-verifier rule — abstain to `human_needed` rather than assert an unevidenced truth."
      status: required
      verification: "Every section of `30-EVIDENCE.md` contains a command line and its captured output; the three `backstop` truths above are recorded as explicitly deferred with the reason and the exact future check, not as passes"
  artifacts:
    - ".planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-EVIDENCE.md — NEW (the phase's hand-run verification record)"
  key_links:
    - "`.github/workflows/docs.yml`'s `tox -e <env>` strings → `tox.ini`'s section list. An unchecked string reference across two files, exercised only when the workflow next runs; the cross-check in Task 2 is what converts it from a merge-time surprise into a phase-time gate"
    - "`tox -e docs-html` → `docs/_build/html/` → the workflow's HTML artifact `path` and the gh-pages `publish_dir`. Running the build and then asserting those exact directories exist is what proves the D-14 repoint landed on a real tree rather than a plausible-looking string"
    - "The merged tree → Read the Docs' next build of the tracked branch → `/en/latest/`. This is the only link the phase cannot close itself; it is recorded as a `backstop` truth with the exact post-merge check named, so the verifier abstains to `human_needed` rather than passing it silently"
---

<!-- planner-discipline-allow: multilang -->
<!-- planner-discipline-allow: html-ja -->
<!-- planner-discipline-allow: language-switcher -->
<!-- planner-discipline-allow: typsphinx_lang -->
<!-- planner-discipline-allow: custom.css -->
<!-- planner-discipline-allow: html_context -->
<!-- planner-discipline-allow: html_sidebars -->
<!-- planner-discipline-allow: html_static_path -->
<!-- planner-discipline-allow: build_multilang -->
<!-- planner-discipline-allow: docs-multilang -->
<!-- planner-discipline-allow: serve-multilang -->

<objective>
Run the phase's five success criteria as commands against the merged tree, and write the result into
a single evidence record.

Purpose: Plans 01-03 each proved their own blast radius in their own worktree. None of them could
prove the phase, because the phase's criteria are properties of the *merged* result — a repo-wide
grep is meaningless against a branch that carries one third of the deletions, and a caller/callee
cross-check between `docs.yml` and `tox.ini` needs both files' edits present at once. This plan is
where SC#1 through SC#5 are actually measured.

It also carries the phase's honesty burden. Three things this phase changes are only observable
after the milestone merges — an actual `docs.yml` run, the published page losing the switcher, and
whether Furo's restored ad slot renders on the hosted site. Each is recorded as a deferred
`backstop` truth with the exact future check named, so the verifier abstains to `human_needed`
rather than quietly counting a plausible inference as a pass.

Output: one new file, `30-EVIDENCE.md`. No source file is modified.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-CONTEXT.md
@.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-RESEARCH.md
@.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-PATTERNS.md
</context>

<execution_environment>
This plan runs in wave 2, after the three wave-1 branches have merged into the phase branch — two
of them by hand, because `worktree.cleanup-wave`'s deletion guard blocks any branch containing
deletions and has no bypass (Phase 27 precedent, PROJECT.md D-13). **Before starting, confirm the
merge actually happened**: all four switcher assets, both orphan documents, both collateral test
files, and `docs/locale/` must all be absent, and `tox.ini` / `docs.yml` / `docs/Makefile` must all
carry their edits. Measuring SC#1 against a partially-merged tree produces a green that means
nothing.

Provision the environment before running anything:
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run every command through
`uv run` in module form (`uv run python -m pytest`, `uv run --extra docs python -m sphinx`) — this
project's NixOS sandbox has been observed to fail on `uv run <compiled-binary>`.

Per D-03's standing form (carried from Phase 29 D-15 and reused by Phase 30.1): run these gates **by
hand**, paste the exact commands and their verbatim output into the evidence record, and commit no
gate script. A committed script that nothing in CI invokes looks like a gate that never runs.
</execution_environment>

<artifacts_this_phase_produces>
See `30-01-PLAN.md` § `<artifacts_this_phase_produces>` for the full phase-level list. This plan
contributes exactly one new path:

- `.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-EVIDENCE.md` — NEW.
  The phase's hand-run verification record. **The filename is load-bearing:** `30-VERIFICATION.md`
  is a reserved name that `/gsd-verify-work` writes and will overwrite, so evidence accumulated
  under that name is destroyed. Phase 30.1 used `30.1-EVIDENCE.md` for the same reason; follow it.

No source file, test, workflow or configuration file is created or modified by this plan.
</artifacts_this_phase_produces>

<flagged_probe_assumptions>
Both spec-less edge-probe rows are enumerated in full in `30-01-PLAN.md` §
`<flagged_probe_assumptions>`, with the no-silent-drop equality tallies (edges: 2 surfaced == 0
authored + 2 flagged; prohibitions: 8 kept == 8 authored + 0 flagged) and the canon-referral
breadcrumb. Both rows — **I18N-02 / `unclassified` / unresolved** and **DOC-08 / `unclassified` /
unresolved** — remain unresolved at the phase gate: neither was auto-backstopped and neither
contributed a probe-derived predicate. Every criterion measured here is derived from ROADMAP SC#1-5
and from measurements taken against this tree, and the evidence record must say so explicitly rather
than implying the probe covered them.
</flagged_probe_assumptions>

<decision_coverage>
Every decision in `30-CONTEXT.md`, with its disposition. `30-CONTEXT.md` was written before D-15
split the work, so most of its decisions belong to Phase 30.1, which has already executed.

- **D-01:** delivered by Phase 30.1 (complete, UAT passed) — the Japanese site ships a Japanese PDF; nothing owed in Phase 30.
- **D-02:** delivered by Phase 30.1 — local ja build feasibility was measured, not assumed; no Phase 30 action.
- **D-03:** delivered by Phase 30.1 — the ja PDF glyph gate ran by hand and passed the owner's visual confirmation; its *form* (run by hand, paste verbatim, commit no script) is reused by this plan's gates.
- **D-04:** delivered — I18N-03 was promoted to v1 and `REQUIREMENTS.md`'s Traceability table already carries the row; no Phase 30 action.
- **D-05:** already applied during Phase 30.1 — measured this session, the PDF no-language-flag docstring already cites Phase 30.1 D-04/D-05 and names `typsphinx-doc-translations`; Plan 02 Task 1 confirms it read-only and writes no edit.
- **D-06:** delivered by Phase 30.1 — the translations repository exists and builds the Japanese site; Plan 03 Task 2 depends on it and verifies its 13 catalogs live.
- **D-07:** deferred to Phase 33 and the standing release procedure — lockstep tagging across two repositories; no Phase 30 action.
- **D-08:** delivered by Phase 30.1 — the submodule pin-bump workflow exists in the translations repository.
- **D-09:** delivered by Phase 30.1 — the ja Read the Docs project slug was chosen and is never published.
- **D-10:** nothing to decide — the URL language segment is `ja` and is derived by Read the Docs from the project's Admin Language setting.
- **D-11:** implemented by Plan 03 Task 1 — both root orphans deleted whole with their collateral tests in one commit, nothing salvaged.
- **D-12:** implemented by Plan 01 Task 2 — the three i18n targets leave `docs/Makefile` for the translations repository.
- **D-13:** implemented by Plan 01 Task 2 — the Japanese-HTML target is deleted rather than left to fail silently.
- **D-14:** implemented by Plan 01 Task 1 — the gh-pages `publish_dir` is repointed and the deploy step itself survives for Phase 32.
- **D-15:** satisfied structurally — Phase 30.1 executed first and its Japanese site was observed serving, so the ordering constraint (do not delete before the replacement is confirmed working) held.
- **PD-01:** implemented by Plan 03 Task 2 — the local `docs/locale/` tree is deleted here, not regenerated, per STATE.md's carry-forward.
- **Claude's discretion on the stylesheet:** exercised in Plan 02 Task 1 — the stylesheet, the CSS-files list, and the static-path setting all go together, because all seven rules were switcher selectors and the emptied directory would make Sphinx warn.
- **Claude's discretion on the SC#1 grep rule:** exercised in this plan — the static-path token is scoped to `docs/source/conf.py`, and the exclusion list is widened past `30-CONTEXT.md`'s two entries to the 7 occurrences a fresh grep actually found.
- **Claude's discretion on Furo's default sidebar:** exercised in Plan 02 Task 2 — no further `conf.py` adjustment is made; the restored-default delta is measured and recorded as an accepted side effect instead.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Run the SC#1 token gate and the SC#2 seam proof, and open the evidence record</name>
  <files>.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-EVIDENCE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 30" — SC#1 and SC#2 verbatim, including SC#1's own exclusion
      clause (`CHANGELOG.md` and `.planning/**` by standing precedent D-02/D-10) and its two named
      survivors.
    - `.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-RESEARCH.md`
      § "Common Pitfalls" → Pitfall 1 — why the static-path token must be scoped to
      `docs/source/conf.py` rather than grepped repo-wide, with the four false-positive files it
      measured. Note the fresh grep taken at planning time found a fifth location, an `.rst` line in
      the static-asset render-gate fixture, so the survivor count is 7 occurrences, not 6.
    - `.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-PATTERNS.md`
      § "Shared Patterns" → "Repo-wide grep as verification gate, not a committed script" — the
      discipline this task follows.
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` — the sibling
      phase's evidence record. Match its structure and its level of verbatim-output discipline; this
      file is the model, not a template to be improvised around.
    - `.planning/STATE.md` § "Accumulated Context" → the honest-verifier rule (abstain to
      `human_needed` rather than assert a truth without direct evidence) and the recurring
      "anywhere under X implies a repo-wide grep at discovery time" scoping lesson.
  </read_first>
  <action>
    Before measuring anything, confirm the wave-1 merge is complete: the four switcher assets, both
    root orphan documents, both collateral test files and `docs/locale/` are all absent, and
    `tox.ini`, `.github/workflows/docs.yml` and `docs/Makefile` all carry their edits. If any is
    missing, stop — a gate run against a partially-merged tree is worse than no gate, because it
    produces a green that will be cited later.

    Then run three measurements and record each with its exact command and verbatim output.

    **Gate A — the SC#1 token sweep.** Grep every git-tracked file for the switcher token set:
    the multi-language build tokens, the Japanese-HTML make target, the switcher wrapper class, the
    session-storage key the deleted theme override wrote, the deleted stylesheet's filename, and the
    two deleted `conf.py` dict names. Scope the sweep with `git ls-files` rather than a filesystem
    walk — that is what makes "the repository" a definition rather than a guess, and it excludes
    every virtualenv, cache and build directory by construction. Exclude `CHANGELOG.md` and
    `.planning` by SC#1's own standing precedent, and filter out the one known-good survivor line,
    the `confval` directive in the field-body render-gate fixture. The expected result is zero
    lines. The pre-phase baseline, measured at planning time with the identical command, was 37
    lines across 9 files — record both numbers so the delta is visible.

    **Gate B — the scoped static-path token.** SC#1 also names the static-path setting, but that is a
    generic Sphinx confval name with 7 legitimate occurrences elsewhere in the repository. Assert two
    things separately: that the token is absent from `docs/source/conf.py`, and that all 7 other
    occurrences — two bundled example projects and two render-gate fixtures — are still present. Both
    halves matter: the first is the requirement, the second is the proof that the requirement was met
    by deleting the right line rather than by quieting the grep. Record the scoping decision and its
    reasoning in the evidence file explicitly, as an exercised discretion, so a later reader does not
    mistake the narrowed scope for an oversight.

    **Gate C — the SC#2 seam proof.** Hash the two anchored regions of `docs/source/conf.py` that
    must be byte-unchanged and compare them against their recorded pre-phase values: region 1 is the
    file start through the `# -- Options for HTML output` header line (it carries `templates_path`,
    Phase 29's `_resolve_language()` seam and its `language` assignment, and the locale/gettext
    block) and must hash to `06f177f82fb153ca4971d258989e7ede2a4e5ffa018a5caaa8ab0a56e6f7b466`;
    region 3 is the `# -- Options for typst/typstpdf output` header through end of file (Phase 30.1's
    font-config block and the `derive_typst_lang` re-derivation) and must hash to
    `cd245215f80b2552dcba7b01d74a36de0ef0b2323df665e88390381c2cd5d169`. Also confirm the phase's
    whole diff against its base touches nothing under `typsphinx/` (milestone invariant #3).

    Then create `30-EVIDENCE.md` with a section per gate, each carrying the command, the verbatim
    output, and a one-line verdict. Name the file exactly `30-EVIDENCE.md` — `30-VERIFICATION.md` is
    reserved for `/gsd-verify-work`, which overwrites it. Commit no gate script.
  </action>
  <verify>
    <automated>test -z "$(git ls-files -z -- . ':!CHANGELOG.md' ':!.planning' | xargs -0 grep -nE 'multilang|html-ja|language-switcher|typsphinx_lang|custom\.css|html_context|html_sidebars' | grep -v 'tests/fixtures/confval_field_body_render_gate/index\.rst:' || true)" && ! grep -q html_static_path docs/source/conf.py && test "$(grep -rn html_static_path examples tests | grep -c . || true)" -eq 7 && grep -q 'confval:: html_sidebars' tests/fixtures/confval_field_body_render_gate/index.rst && test "$(sed -n '1,/^# -- Options for HTML output/p' docs/source/conf.py | sha256sum | cut -d' ' -f1)" = "06f177f82fb153ca4971d258989e7ede2a4e5ffa018a5caaa8ab0a56e6f7b466" && test "$(sed -n '/^# -- Options for typst/,$p' docs/source/conf.py | sha256sum | cut -d' ' -f1)" = "cd245215f80b2552dcba7b01d74a36de0ef0b2323df665e88390381c2cd5d169" && test -z "$(git diff --name-only $(git merge-base HEAD main)..HEAD -- typsphinx/)" && test -f .planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-EVIDENCE.md && ! test -e .planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The token sweep over git-tracked files returns zero lines after filtering the one known-good survivor; both the zero and the pre-phase baseline of 37 lines across 9 files are recorded in `30-EVIDENCE.md`
    - `docs/source/conf.py` contains no static-path token, and exactly 7 occurrences survive across `examples/` and `tests/fixtures/`; the scoping decision and its reasoning are written into the evidence file as an exercised discretion
    - `tests/fixtures/confval_field_body_render_gate/index.rst` still contains its `.. confval:: html_sidebars` directive
    - `docs/source/conf.py`'s region 1 hashes to `06f177f82fb153ca4971d258989e7ede2a4e5ffa018a5caaa8ab0a56e6f7b466` and region 3 to `cd245215f80b2552dcba7b01d74a36de0ef0b2323df665e88390381c2cd5d169`
    - `git diff --name-only` across the whole phase lists no path under `typsphinx/`
    - `30-EVIDENCE.md` exists, carries one section per gate with the command and its verbatim output, and no file named `30-VERIFICATION.md` was created
    - No gate script was committed anywhere in the repository
  </acceptance_criteria>
  <done>SC#1 and SC#2 are measured on the merged tree with commands and verbatim output on the record, the survivors that had to survive are proven present, and the seam both Read the Docs projects depend on is proven byte-identical by hash.</done>
</task>

<task type="auto">
  <name>Task 2: Run the build, suite and workflow-consistency gates and close the evidence record</name>
  <files>.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-EVIDENCE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 30" — SC#3, SC#4 and SC#5 verbatim, including SC#5's clause
      about an observed CI run and the RTD-04 standing invariant about the documentation root.
    - `.github/workflows/docs.yml` on the merged tree, all of it — the exact `tox -e <env>` strings
      and every `docs/_build/` path it references. These are what the cross-check compares against.
    - `tox.ini` on the merged tree — the surviving section list.
    - `.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-02-SUMMARY.md`
      — its recorded ad-placement and variant-selector counts and the hosted-site question they left
      open, so the evidence record carries that thread forward rather than dropping it.
    - `.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-03-SUMMARY.md`
      — the collected-test counts before and after the orphan deletion, and the total deletion scope,
      which the evidence record restates for the owner's manual merge review.
    - `.planning/STATE.md` § "Blockers/Concerns" — the note that this project's phases are PDF /
      config / docs / hosting work rather than frontend UI, and the honest-verifier rule.
  </read_first>
  <action>
    Run four more measurements on the merged tree and append each to `30-EVIDENCE.md` with its exact
    command and verbatim output.

    **Gate D — the full suite (SC#3, SC#4).** Run the whole pytest suite. Record the pass count and
    compare it against the pre-phase count minus the tests that left with the two deleted orphan-doc
    test files, using the numbers Plan 03's SUMMARY recorded. The point is not that the suite is
    green — it is that the only tests missing are the ones whose subjects are gone.

    **Gate E — the documentation builds (SC#5).** Run both surviving tox documentation environments
    and confirm each produced the artefact the workflow expects: an `index.html` under
    `docs/_build/html/` and at least one PDF under `docs/_build/pdf/`. These are the exact two paths
    `.github/workflows/docs.yml` uploads, and the first is the one it publishes.

    **Gate F — workflow/task-runner consistency (SC#5).** Parse `docs.yml`, extract every
    `tox -e <env>` string from its `run:` values, and assert each names a section that exists in
    `tox.ini`. Then assert every `docs/_build/` path the workflow references is one Gate E just
    produced. Also re-assert the D-14 boundary on the merged tree: exactly one
    `peaceiris/actions-gh-pages` step, its `publish_dir` pointing at the HTML tree, and the
    `softprops/action-gh-release` step still present. This closes the failure mode where a workflow
    is internally broken but nothing notices until the milestone pull request runs it.

    **Gate G — the documentation root still resolves (RTD-04).** Fetch
    `https://typsphinx.readthedocs.io/` following redirects and record the final status code and
    effective URL. Measured at planning time: 200, redirecting to `/en/latest/`. Phrase this in the
    evidence record as a one-off corroborating read of a public URL, not as an API integration —
    this project's `api-coverage` gate has false-positived on RTD evidence prose before.

    Then close `30-EVIDENCE.md` with two sections that must not be written as passes:

    - **Deferred to the milestone pull request.** The observed `docs.yml` run required by SC#5.
      State the reason it cannot be observed in-phase: the workflow triggers only on a push to
      `main`, a `v*` tag, or a pull request targeting `main`, and declares no `workflow_dispatch` —
      and adding one would not help, because GitHub resolves manual dispatch against the default
      branch's copy of the workflow file. Name the exact future check: when the milestone pull
      request opens, the `Documentation` workflow's `build-docs` job must complete green with its
      HTML artifact uploaded from `docs/_build/html`.
    - **Deferred to the next Read the Docs build.** Two items. First, the published `/en/latest/`
      page must stop containing the switcher wrapper class and the deleted stylesheet reference —
      both measured present at planning time, one occurrence each. Second, whether Furo's restored
      ad slot and variant selector render on the hosted site, carried forward from Plan 02's local
      measurement. Name the exact future check for each: a fetch of `/en/latest/` grepped for those
      markers after Read the Docs has rebuilt the tracked branch.

    Both sections are recorded as open, with their checks named, not as inferences. Also restate in
    the evidence record the two accepted, deliberate losses this phase makes permanent — the
    browser-language auto-redirect at the documentation root, and the two `docs/usage.rst` sections
    with no counterpart under `docs/source/` — so neither can later read as an oversight.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest -q && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m tox -e docs-html && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m tox -e docs-pdf && test -f docs/_build/html/index.html && test -n "$(ls docs/_build/pdf/*.pdf 2>/dev/null)" && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import yaml,re,sys,pathlib,glob;raw=pathlib.Path('.github/workflows/docs.yml').read_text(encoding='utf-8');d=yaml.safe_load(raw);steps=d['jobs']['build-docs']['steps'];runs=[s.get('run','') for s in steps];envs=sorted(set(re.findall(r'tox -e ([A-Za-z0-9_.-]+)',' '.join(runs))));tox=pathlib.Path('tox.ini').read_text(encoding='utf-8');secs=set(re.findall(r'^\[([^]]+)\]',tox,re.M));paths=sorted(set(re.findall(r'docs/_build/[A-Za-z0-9_./*-]*',raw)));dep=[s for s in steps if s.get('uses','').startswith('peaceiris/actions-gh-pages')];missing=[e for e in envs if ('testenv:'+e) not in secs];badp=[p for p in paths if not glob.glob(p.rstrip('/'))];chk=[('envs_resolve',not missing),('envs_found',len(envs)>=2),('paths_exist',not badp),('deploy_one',len(dep)==1),('publish_dir',bool(dep) and dep[0]['with']['publish_dir']=='./docs/_build/html'),('release_kept',any(s.get('uses','').startswith('softprops/action-gh-release') for s in steps))];bad=[n for n,ok in chk if not ok];print('ENVS',envs,'PATHS',paths,'MISSING_ENVS',missing,'MISSING_PATHS',badp);print('FAILED:',bad);sys.exit(1 if bad else 0)" && test "$(curl -sS -o /dev/null -w '%{http_code}' -L https://typsphinx.readthedocs.io/)" = "200" && grep -q 'Deferred to the milestone pull request' .planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-EVIDENCE.md && grep -q 'Deferred to the next Read the Docs build' .planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-EVIDENCE.md</automated>
    <human-check>
      `workflow.human_verify_mode` is `end-of-phase`, so this is recorded here rather than as a
      blocking checkpoint task. At the milestone pull request, confirm two things that the phase
      cannot observe from inside itself:

      1. Open the milestone pull request against `main` and wait for the `Documentation` workflow.
         Its `build-docs` job must finish green, its `Build HTML documentation` step must run
         `tox -e docs-html`, and the `documentation-html` artifact must be uploaded from
         `docs/_build/html`. This is ROADMAP SC#5's "observed CI run" clause.
      2. After Read the Docs rebuilds the tracked branch, fetch
         `https://typsphinx.readthedocs.io/en/latest/` and confirm the switcher wrapper class and
         the deleted stylesheet reference are both gone — each measured at exactly one occurrence
         before the phase — while the Read the Docs flyout still offers the Japanese translation.
         Note the ad-placement and variant-selector counts on the hosted page at the same time; a
         non-zero count there is the accepted, documented side effect of deleting the sidebar
         override, not a regression to fix in this phase.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The full `uv run python -m pytest -q` suite is green, and the evidence record states the pass count alongside the pre-phase count and accounts for the difference entirely by the two deleted orphan-doc test files
    - Both `tox -e docs-html` and `tox -e docs-pdf` exit 0, and `docs/_build/html/index.html` plus at least one `docs/_build/pdf/*.pdf` exist afterwards
    - The workflow cross-check prints `FAILED: []` with an empty `MISSING_ENVS` and an empty `MISSING_PATHS`, and its `ENVS` / `PATHS` lines are pasted verbatim into the evidence record
    - Exactly one `peaceiris/actions-gh-pages` step survives on the merged tree with `publish_dir` at `./docs/_build/html`, and the `softprops/action-gh-release` step is present
    - A live fetch of `https://typsphinx.readthedocs.io/` returns 200; the evidence record phrases it as a one-off corroborating read of a public URL, not as an API integration
    - `30-EVIDENCE.md` contains a section headed `Deferred to the milestone pull request` and a section headed `Deferred to the next Read the Docs build`, each naming its exact future check and recorded as open rather than as a pass
    - The evidence record restates the two accepted deliberate losses — the browser-language auto-redirect and the two unsalvaged `docs/usage.rst` sections — and carries forward Plan 02's ad-placement and variant-selector counts with their unresolved hosted-site question
  </acceptance_criteria>
  <done>All five phase success criteria are measured on the merged tree with commands and verbatim output on the record, and the three things the phase genuinely cannot observe are written down as deferred with their checks named rather than inferred into passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The phase's own evidence record → every later reader who cites it | A gate recorded as passing becomes the basis for the milestone's irreversible Phase 32 teardown; a citation dressed as a measurement propagates |
| Public Read the Docs HTTP endpoint → this repository's verification record | An unauthenticated live fetch is the only observation available for the RTD-04 standing invariant |

## STRIDE Threat Register

ASVS L1, block on `high`. This plan writes no source code and installs nothing; its risks are
integrity-of-evidence rather than attacker-facing. Nothing below is `high`, so nothing blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-13 | Repudiation | The evidence record asserting an unmeasured pass | medium | mitigate | Every gate is a command with verbatim output pasted; the three genuinely unobservable items are authored as `backstop` truths that abstain to `human_needed` at verify time rather than passing silently, and are written into the record under explicit `Deferred` headings |
| T-30-14 | Tampering | Gate satisfied by editing unrelated files rather than by doing the work | medium | mitigate | Authored as a hard prohibition with a positive survivor check: the 7 legitimate static-path occurrences and the `confval` fixture line must all still be present, so a green achieved by deletion fails |
| T-30-15 | Tampering | Evidence destroyed by filename collision | low | mitigate | The record is named `30-EVIDENCE.md`; the verify asserts no `30-VERIFICATION.md` was created, that name being reserved for `/gsd-verify-work`, which overwrites it |
| T-30-16 | Information Disclosure | Unauthenticated fetch of a public documentation URL | low | accept | A public GET with no credential and no body; the recorded output is a status code and an effective URL. Standing rule carried from Phase 30.1: phrase Read the Docs reads as one-off corroboration, never as an integration |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing and touches no dependency list; `30-RESEARCH.md` § "Package Legitimacy Audit" records zero packages for the phase, so no `[ASSUMED]`/`[SUS]`/`[SLOP]` row and no legitimacy checkpoint is owed |
</threat_model>

<verification>
1. The wave-1 merge is confirmed complete before any gate runs
2. Gate A: the token sweep over git-tracked files returns zero lines (baseline 37 across 9 files)
3. Gate B: the static-path token is absent from `docs/source/conf.py` and all 7 legitimate occurrences elsewhere survive, as does the `confval` fixture line
4. Gate C: `conf.py` regions 1 and 3 hash to their recorded pre-phase values, and the phase diff touches nothing under `typsphinx/`
5. Gate D: the full pytest suite is green with the test-count delta fully accounted for
6. Gate E: both documentation tox environments are green and produced the two artefact paths
7. Gate F: every workflow `tox -e` string resolves to a real section, every referenced build path exists, and the D-14 deploy/release boundary holds
8. Gate G: the documentation root returns 200
9. `30-EVIDENCE.md` records all of the above verbatim plus two explicit `Deferred` sections and the two accepted losses
</verification>

<success_criteria>
- All five ROADMAP success criteria are measured on the merged tree, by command, with verbatim output
- The tokens that had to go are provably gone; the lookalikes that had to survive are provably present
- The cross-repository `conf.py` seam is proven byte-identical by hash
- The documentation still builds, the suite is still green, and the workflow is internally consistent with the task runner
- The three post-merge observations are recorded as deferred with their exact checks named, never inferred into passes
</success_criteria>

<output>
Create `.planning/phases/30-japanese-rtd-site-hand-rolled-machinery-orphan-removal/30-04-SUMMARY.md`
when done, alongside the `30-EVIDENCE.md` the tasks produce.

The SUMMARY must state, per gate, the one-line verdict and where in `30-EVIDENCE.md` the verbatim
output lives; the total phase deletion scope (files removed across Plans 02 and 03) so the owner's
manual merge past the `worktree.cleanup-wave` deletion guard is a review against a number; and the
three deferred observations with their named future checks, so the milestone's Phase 32 gate cannot
later cite this phase as having closed them.
</output>
