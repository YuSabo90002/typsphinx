---
phase: 33-v0-6-4-release-prep
plan: 04
type: execute
wave: 2
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - .planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md
  - .planning/phases/33-v0-6-4-release-prep/33-HANDOFF.md
autonomous: true
requirements: [REL-02]

must_haves:
  truths:
    - "`pyproject.toml`'s `Documentation` metadata value was fetched over real HTTP on the prepared tree and the recorded evidence shows the redirect chain and terminal status code, captured from a command re-run during this plan rather than quoted from any planning document."
    - "`git diff main..HEAD --stat -- typsphinx/` produces empty output, and the evidence file records that empty output as the invariant PASSING — the empty case is a positive result here, not a skipped or failed measurement."
    - "The evidence file records the milestone diff range re-measured at execution time: the merge-base commit and the commit count, both obtained by running the commands during this plan, because the count drifts as the branch grows (measured 254 at discussion, 256 at research, 258 at planning)."
    - "The evidence file asserts all three D-03 invariants with the verbatim command output backing each: zero new runtime dependencies, no `@preview` package version bump across the four version-sync surfaces, and zero changes under `typsphinx/`."
    - "`git tag -l v0.6.4` and `git ls-remote --tags origin v0.6.4` both return empty at the end of this phase, and no plan in this phase invoked a publish workflow — so an interrupted or partially-merged phase leaves zero irreversible published state (concurrency case)."
    - "The handoff checklist enumerates all eight owner/milestone-close items and is written in English, since it is a phase deliverable that merges to a public branch."
    - "Re-running the full evidence-collection command set produces the same verdicts — the assertions are re-derivable rather than one-shot observations (idempotency case)."
    - statement: "If this phase is interrupted mid-wave, the repository is left in a self-consistent state: no plan in this phase performs a partial mutation that another plan must complete for the tree to be valid, since wave-1 plans touch disjoint file sets and each commits its own complete change."
      verification: backstop
    - statement: "The live Read the Docs serving state observed during this plan reflects the moment of measurement only; it is recorded as a timestamped observation in the evidence file and deliberately kept out of CHANGELOG.md, because this project does not record facts for which it holds no re-verification mechanism (D-03)."
      verification: backstop
  prohibitions:
    - "Performing any irreversible publish action. This phase creates no git tag, pushes nothing to a release workflow, publishes nothing to PyPI, does not mark pull request 124 ready or merge it, does not close issue 119, and does not flip any Read the Docs Default Version or Default branch setting. SC#5's scope fence is absolute; every one of these belongs to `/gsd-complete-milestone` or to the owner."
    - "Asserting the post-tag half of REL-02 as satisfied. This phase structurally cannot make `typsphinx 0.6.4` live on PyPI or make `/en/stable/` and `/ja/stable/` serve the released version, because both require the tag that this phase does not create. Recording either as done, likely, or effectively true would be a false claim in the project's own verification record. A criterion this phase cannot meet is written as not met and handed off, never dressed up as met."
  artifacts:
    - .planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md
    - .planning/phases/33-v0-6-4-release-prep/33-HANDOFF.md
  key_links:
    - "`pyproject.toml` `[project.urls] Documentation` -> the live Read the Docs site (bound only by a real HTTP fetch; no test in this repository can assert it)"
    - "milestone diff range `main..HEAD` -> the three D-03 invariants asserted in the CHANGELOG `### Verified` section written by plan 33-02"
    - "`33-HANDOFF.md` -> `/gsd-complete-milestone` (the checklist is the interface between this prep phase and the publish step it deliberately does not perform)"
---

<objective>
Collect the evidence that the milestone invariants hold over the full milestone diff, re-verify
the published `Documentation` URL over real HTTP, and write the handoff checklist that carries
the half of REL-02 this phase cannot satisfy to the milestone-close command and the owner.

Purpose: Satisfies ROADMAP Phase 33 SC#3, SC#4, and SC#5. SC#5 is the phase's integrity
criterion — it exists so that a structurally unmeetable requirement is recorded as unmet and
handed off rather than quietly asserted.
Output: `33-RELEASE-EVIDENCE.md` (SC#3 + SC#4 evidence) and `33-HANDOFF.md` (SC#5 checklist).
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-v0-6-4-release-prep/33-CONTEXT.md
@.planning/phases/33-v0-6-4-release-prep/33-RESEARCH.md
@.planning/phases/33-v0-6-4-release-prep/33-01-SUMMARY.md
@.planning/phases/33-v0-6-4-release-prep/33-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan creates two new files and no code symbols:

| Artifact | Kind | Note |
|----------|------|------|
| `.planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md` | new file | SC#3 and SC#4 evidence record |
| `.planning/phases/33-v0-6-4-release-prep/33-HANDOFF.md` | new file | SC#5 publish + owner-manual handoff checklist |

**Filename constraint (do not rename these):** neither file may be called
`33-VERIFICATION.md`. That name is reserved by the `/gsd-verify-work` verifier, which overwrites
it wholesale — evidence accumulated there by a plan is destroyed when verification runs.
33-RESEARCH.md suggests `33-VERIFICATION.md` as the evidence location; that suggestion is
overridden here. Placement is explicitly Claude's discretion per CONTEXT.md.

No functions, classes, decorators, CLI flags, config keys, or struct fields are introduced.
Nothing under `typsphinx/` is touched.

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the Documentation metadata URL over real HTTP and record SC#3 evidence</name>

  <files>.planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md</files>

  <read_first>
    - `pyproject.toml` `[project.urls]` block — read the actual `Documentation` value from the
      prepared tree. Do not assume it; the whole point of SC#3 is that the value shipped in the
      wheel metadata is the one fetched.
    - `.planning/ROADMAP.md` § Phase 33 — success criterion 3, which requires a real HTTP fetch
      on the prepared tree, and identifies this as the half of REL-02 the phase can satisfy.
    - `.planning/phases/33-v0-6-4-release-prep/33-RESEARCH.md` — `## Summary` item 5, which
      records the research session's own fetch result, and the `## Validation Architecture`
      table's SC#3 row for the command form.
  </read_first>

  <action>
    Create `33-RELEASE-EVIDENCE.md` with a section for SC#3.

    Read the `Documentation` value out of `pyproject.toml` programmatically rather than by eye —
    parse it with `tomllib` through `uv run python` so the recorded URL is provably the one in
    the file. Phase 31 already set this value to the Read the Docs root; this phase makes no edit
    to it.

    Fetch that exact URL with `curl`, capturing both the un-followed response (to record the
    redirect status and `Location`) and the followed response (to record the terminal status code
    and effective URL). Use a form that prints the status code and effective URL, and record the
    verbatim command and its verbatim output in the evidence file. Do not paraphrase the result
    and do not reuse the research session's recorded figures — that observation is several days
    old and SC#3 requires the fetch to happen on the prepared tree.

    Record the result honestly. If the fetch does not reach a 2xx terminal status, write that
    down as SC#3 not met and stop rather than reporting a pass — this is the project's
    honest-verifier rule, and SC#5 exists precisely because this phase must not assert what it
    cannot show.

    Timestamp the observation. Note in the evidence file that this live serving observation stays
    out of `CHANGELOG.md` by D-03: the CHANGELOG records only facts with a standing
    re-verification mechanism, and a point-in-time HTTP fetch has none.
  </action>

  <verify>
    <automated>test -f .planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md &amp;&amp; grep -q 'SC#3' .planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md &amp;&amp; grep -q 'readthedocs.io' .planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md &amp;&amp; grep -qE '\b(200|30[0-9])\b' .planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md &amp;&amp; echo SC3_OK</automated>
  </verify>

  <acceptance_criteria>
    - `33-RELEASE-EVIDENCE.md` exists and contains an SC#3 section.
    - The section records the `Documentation` URL as parsed from `pyproject.toml`, and that value
      matches the file: `uv run python -c "import tomllib;print(tomllib.load(open('pyproject.toml','rb'))['project']['urls']['Documentation'])"`
      output appears verbatim in the evidence.
    - The section contains the verbatim `curl` command and its verbatim output, including a
      terminal HTTP status code and the effective URL.
    - The recorded terminal status is 2xx, or the section explicitly states SC#3 as NOT MET.
    - The section carries an ISO-8601 observation timestamp.
    - The section states that this observation is deliberately excluded from `CHANGELOG.md` per
      D-03.
    - `git diff main..HEAD -- pyproject.toml` shows no change to the `[project.urls]` block beyond
      the one Phase 31 already made — this task edited no metadata.
  </acceptance_criteria>

  <done>
    SC#3 is evidenced by a fresh, verbatim-recorded HTTP fetch of the exact URL parsed from the
    prepared tree's `pyproject.toml`, with an honest verdict.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assert the milestone invariants over the full diff and record SC#4 evidence</name>

  <files>.planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` § Milestone Invariants — invariants 1, 2, and 3 are the three
      this task asserts (zero new runtime dependencies, no `@preview` version bump across the
      four version-sync surfaces, no `typsphinx/` runtime code change). Invariant 4 is why every
      count in this task must be re-measured rather than quoted.
    - `.planning/ROADMAP.md` § Phase 33 — success criterion 4, which requires the assertion to
      span the full milestone diff, not this phase's diff.
    - `.planning/phases/33-v0-6-4-release-prep/33-PATTERNS.md` — the
      `### Milestone-invariant diff assertion` block, which lists the four commands.
    - `.planning/phases/33-v0-6-4-release-prep/33-01-SUMMARY.md` — the `typsphinx.__version__`
      probe output and the `uv.lock` numstat recorded by plan 33-01, cited here as SC#1 evidence.
    - `.planning/phases/33-v0-6-4-release-prep/33-02-SUMMARY.md` — the resolved CHANGELOG date and
      tail-block diff recorded by plan 33-02, cited here as SC#2 evidence.
  </read_first>

  <action>
    Append an SC#4 section to `33-RELEASE-EVIDENCE.md` asserting the three milestone invariants
    over the full milestone diff, with verbatim command output backing each.

    First re-measure the diff range itself: run `git merge-base main HEAD` and
    `git log --oneline main..HEAD | wc -l` and record both raw values. Do not carry forward any
    recorded commit count — it has already drifted three times across this phase's own artifacts
    (254 at discussion, 256 at research, 258 at planning) as completion commits accumulated.
    Milestone invariant 4 makes the discovery-time measurement the source of truth.

    Then assert each invariant:

    - Zero changes under `typsphinx/`: run `git diff main..HEAD --stat -- typsphinx/`. Empty
      output is the PASS condition. Record it explicitly as a pass, and state in the evidence
      that empty output here means the invariant held — an empty result must not be mistaken for
      a command that failed to run or a check that was skipped. To distinguish the two, also
      record the command's exit status and a positive control such as
      `git diff main..HEAD --stat -- pyproject.toml`, which must be non-empty, proving the diff
      range and pathspec machinery is actually working.
    - Zero new runtime dependencies: run `git diff main..HEAD -- pyproject.toml` and record the
      full diff. On the prepared tree the expected content is exactly two hunks — the
      `Documentation` URL line from Phase 31 and the version bump from plan 33-01 — with no line
      inside `dependencies` or `optional-dependencies` changed. Also record
      `git diff main..HEAD --stat -- uv.lock`, whose only expected change is the typsphinx
      self-entry version line.
    - No `@preview` package version bump: the four declaration surfaces are
      `typsphinx/writer.py`, `typsphinx/template_engine.py`, `typsphinx/templates/base.typ`, and
      `examples/**/*.typ`. The first three are already covered by the empty `typsphinx/` diff;
      additionally run `git diff main..HEAD --stat -- examples/` and run
      `tests/test_preview_version_sync.py`, recording both. Provision the environment first with
      `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and run the test through
      `uv run`, per this project's standing worktree-isolated execution mode.

    Then record the SC#1 and SC#2 evidence carried from plans 33-01 and 33-02 — the
    `typsphinx.__version__` probe output, the version-sync guard test results, the resolved
    CHANGELOG date, and the tail-block diff — so the evidence file is a single citable record for
    all four mechanically-provable criteria.

    Finally run the full test suite through `uv run` and record the counts. The research session
    measured 647 passed / 1 skipped as this phase's baseline. Record the actual result; a count
    at or above the baseline with zero failures is the pass condition. If any test fails, record
    the failure and stop — do not proceed to the handoff task with a red suite.

    Every claim in this section must be backed by output from a command you ran during this task.
    Do not cite a figure from CONTEXT.md, RESEARCH.md, or PATTERNS.md as evidence; those are
    inputs to be re-verified, not evidence.
  </action>

  <verify>
    <automated>test -z "$(git diff main..HEAD --stat -- typsphinx/)" &amp;&amp; test -n "$(git diff main..HEAD --stat -- pyproject.toml)" &amp;&amp; grep -q 'SC#4' .planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md &amp;&amp; grep -q "$(git merge-base main HEAD | cut -c1-7)" .planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest -q 2>&amp;1 | tail -3 &amp;&amp; echo SC4_OK</automated>
  </verify>

  <acceptance_criteria>
    - `33-RELEASE-EVIDENCE.md` contains an SC#4 section recording the merge-base commit SHA and
      the commit count, both re-measured during this task; the recorded SHA matches the current
      output of `git merge-base main HEAD`.
    - `git diff main..HEAD --stat -- typsphinx/` produces empty output, and the evidence file
      states in words that this empty result is the invariant PASSING.
    - The evidence file records a non-empty positive control (`git diff main..HEAD --stat --
      pyproject.toml`) demonstrating the diff range and pathspec are functioning.
    - The evidence file contains the full `git diff main..HEAD -- pyproject.toml` output and
      states that no line inside `dependencies` or `optional-dependencies` changed.
    - The evidence file records `git diff main..HEAD --stat -- examples/` output and a green
      result for `tests/test_preview_version_sync.py`.
    - The evidence file cites plan 33-01's `typsphinx.__version__` output (`0.6.4`) and plan
      33-02's resolved CHANGELOG date and tail-block diff.
    - `uv run python -m pytest -q` reports zero failures and a passed count at or above 647; the
      actual counts appear verbatim in the evidence file.
  </acceptance_criteria>

  <done>
    All three milestone invariants are asserted over a freshly re-measured full milestone diff
    with verbatim backing output, the empty-diff result is recorded as a pass alongside a working
    positive control, and the suite is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the SC#5 publish and owner-manual handoff checklist</name>

  <files>.planning/phases/33-v0-6-4-release-prep/33-HANDOFF.md</files>

  <read_first>
    - `.planning/ROADMAP.md` § Phase 33 — success criterion 5 and the "Owner-manual dependencies"
      paragraph. SC#5 is the criterion this task discharges, and its scope fence is absolute.
    - `.planning/REQUIREMENTS.md` — the REL-02 entry (including the standing two-repository
      tagging cost added by Phase 30's D-07), § Owner-Manual Steps items 4 and 5, and the
      Traceability note explaining that REL-02 is only half-satisfiable in this phase.
    - `.planning/phases/33-v0-6-4-release-prep/33-CONTEXT.md` § specifics — the eight numbered
      items already agreed as the checklist's content, and § Out of scope, which enumerates
      exactly what this phase must not do.
    - `.planning/STATE.md` § Blockers/Concerns and § Operator Next Steps — the three owed
      post-merge flips and the milestone-close items owed from earlier phases.
    - `.planning/phases/32-github-pages-teardown-irreversible/32-CONTEXT.md` — the section
      describing the gh-pages revival hazard, which is the basis for checklist item 7.
  </read_first>

  <action>
    Create `33-HANDOFF.md` as an English-language checklist. Write it in English directly rather
    than translating later: it is a phase deliverable that merges to a public branch, and D-05
    establishes English as the register for publicly-visible planning documents.

    Open the document by stating plainly which half of REL-02 this phase satisfied and which half
    it did not, and why the unsatisfied half is structurally out of reach here: `typsphinx 0.6.4`
    live on PyPI, and `/en/stable/` and `/ja/stable/` both serving that released version, both
    require the `v0.6.4` tag, which this phase deliberately does not create. State this as an
    unmet criterion handed off, not as a formality.

    Then enumerate the eight items, each with enough detail that the owner or
    `/gsd-complete-milestone` can act without re-deriving context, and each marked with its owner
    (milestone-close command versus human) and its ordering dependency:

    1. Mark pull request 124 ready for review and merge it, via `/gsd-complete-milestone`.
    2. Push the `v0.6.4` tag, which fires `release.yml`, publishing to PyPI and creating the
       GitHub Release. The Release body's single source is the `## [0.6.4]` CHANGELOG entry
       authored by plan 33-02.
    3. Bump the submodule and push a matching tag in the `typsphinx-doc-translations`
       repository. This is REL-02's standing cost from Phase 30's D-07: `/ja/stable/` resolves
       against the translations repository's own tags, so every release from now on tags two
       repositories, not one. Omitting this leaves `/ja/stable/` unresolvable while
       `/en/stable/` works.
    4. Perform the three owed post-merge flips: the parent Read the Docs project's Default
       branch to `main`, the Japanese project's Default branch to `main`, and the `.gitmodules`
       `branch` value to `main`. These are Phase 30.1 carry-forwards; the submodule currently
       tracks the milestone branch because `main` had neither `.readthedocs.yaml` nor the
       language-resolution seam at the time.
    5. After the tag build is green — and only after — flip the English project's Default
       Version from `latest` to `stable`, then re-confirm the Japanese project's independent
       version activation, verifying that `/ja/stable/` resolves to the same tag as
       `/en/stable/`. Translation projects do not inherit the parent's activated-version list,
       which is why this is a re-check and not an assumption. Until the tag builds green, Default
       Version stays `latest`.
    6. Close GitHub issue 119 using the reply draft at
       `.planning/phases/31-published-url-cutover-repo-wide-link-guard/31-ISSUE-119-REPLY-DRAFT.md`,
       after owner review. Phase 31's D-15 made this a post-merge handoff because closing while
       `main` still served dead links would promise an undelivered fix.
    7. Re-confirm with `git ls-remote` that no `gh-pages` branch exists on the origin remote.
       Phase 32's teardown is reversible by accident: a push to `main` before the milestone merge
       could let the retired Pages deploy action recreate the branch. Recommended, not blocking.
    8. Move the two resolved todos out of `.planning/todos/pending/` — the github.io 404 item
       resolved by Phase 31 and the orphan-docs item resolved by Phase 30.

    Include an explicit "not done in this phase, by design" section restating the scope fence: no
    tag, no release workflow run, no PyPI publish, no pull request merge, no issue closure, no
    Read the Docs setting change. Nothing in this plan or any other plan of Phase 33 performs any
    of these.

    Before finishing, confirm the fence held: `git tag -l v0.6.4` and
    `git ls-remote --tags origin v0.6.4` must both be empty. Record both outputs in the handoff
    document as proof that this phase created no irreversible state.
  </action>

  <verify>
    <automated>test -f .planning/phases/33-v0-6-4-release-prep/33-HANDOFF.md &amp;&amp; test "$(grep -cP '[\x{3040}-\x{30ff}\x{4e00}-\x{9fff}]' .planning/phases/33-v0-6-4-release-prep/33-HANDOFF.md)" -eq 0 &amp;&amp; test -z "$(git tag -l v0.6.4)" &amp;&amp; test -z "$(git ls-remote --tags origin v0.6.4)" &amp;&amp; grep -q 'typsphinx-doc-translations' .planning/phases/33-v0-6-4-release-prep/33-HANDOFF.md &amp;&amp; grep -qi 'stable' .planning/phases/33-v0-6-4-release-prep/33-HANDOFF.md &amp;&amp; echo SC5_OK</automated>
  </verify>

  <acceptance_criteria>
    - `33-HANDOFF.md` exists and contains zero CJK-matching lines (English-only, per D-05).
    - The document states explicitly which half of REL-02 is unmet and why it is structurally
      unreachable in this phase.
    - All eight checklist items are present and individually identifiable, each naming its owner
      (milestone-close command or human) and any ordering dependency.
    - The document names `typsphinx-doc-translations` and the two-repository tagging cost, names
      the three post-merge flips, names the `latest` to `stable` Default Version flip and its
      precondition, and references
      `31-ISSUE-119-REPLY-DRAFT.md` for the issue closure.
    - The document contains a "not done in this phase, by design" section listing the forbidden
      actions.
    - `git tag -l v0.6.4` produces empty output and `git ls-remote --tags origin v0.6.4` produces
      empty output, and both results are recorded verbatim in the document.
    - No file under `.git/refs/tags/` was created during this phase.
  </acceptance_criteria>

  <done>
    The handoff checklist is complete, English, honest about the unmet half of REL-02, and backed
    by recorded proof that no tag or publish action occurred.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prepared tree -> irreversible publish (tag, PyPI, Release, PR merge) | The single boundary this phase must not cross; everything past it is unrecoverable. |
| this repository -> live Read the Docs service | SC#3's fetch crosses to an external service; no test in this repository can assert its state. |
| evidence record -> future audits and the milestone close | Downstream decisions are made on the strength of what this file claims. |
| this repository -> `typsphinx-doc-translations` repository | REL-02's Japanese half resolves against a second repository's tags, outside this repository's control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-09 | Elevation of privilege | irreversible publish actions inside a prep-only phase | high | mitigate | The prohibition in `must_haves` forbids every publish action by name, and Task 3's acceptance criteria require `git tag -l v0.6.4` and `git ls-remote --tags origin v0.6.4` to be empty with both outputs recorded — the fence is asserted with evidence, not merely intended. Plan 33-02's Task 2 carries the same tag-emptiness check independently. |
| T-33-10 | Spoofing | evidence claims in `33-RELEASE-EVIDENCE.md` | high | mitigate | Every assertion must be backed by verbatim output from a command run during this plan; Task 2's action forbids citing figures from CONTEXT.md, RESEARCH.md, or PATTERNS.md as evidence, and the merge-base SHA in the file is cross-checked against a live `git merge-base` invocation in the verify command. |
| T-33-11 | Repudiation | recording an empty diff result ambiguously | medium | mitigate | Task 2 requires a non-empty positive control alongside the empty `typsphinx/` diff, so an empty result provably means "invariant held" rather than "the command or pathspec silently matched nothing". |
| T-33-12 | Information disclosure | evidence file destroyed by a name collision | medium | mitigate | Neither deliverable may be named `33-VERIFICATION.md`, which the verifier overwrites wholesale; the constraint is stated in the artifacts section and the filenames are fixed in `files_modified`. |
| T-33-13 | Tampering | stale external observation presented as current | medium | mitigate | Task 1 requires a fresh fetch on the prepared tree with an ISO-8601 timestamp and forbids reusing the research session's recorded figures; the observation is additionally kept out of CHANGELOG.md per D-03 because it has no standing re-verification mechanism. |
| T-33-SC | Tampering | package-manager installs | low | accept | This plan installs no new package; it provisions the existing pinned environment via `uv sync --extra dev` to run tests. `33-RESEARCH.md`'s `## Package Legitimacy Audit` records the gate as not-applicable, corroborated at plan time by an empty `git diff main..HEAD -- uv.lock` and zero changes to the dependency arrays. |
</threat_model>

<verification>
- `.planning/phases/33-v0-6-4-release-prep/33-RELEASE-EVIDENCE.md` and `33-HANDOFF.md` both exist; neither is named `33-VERIFICATION.md`.
- `git diff main..HEAD --stat -- typsphinx/` is empty and `git diff main..HEAD --stat -- pyproject.toml` is non-empty (positive control).
- The merge-base SHA recorded in the evidence file matches live `git merge-base main HEAD`.
- `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest -q` reports zero failures with at least 647 passed.
- `git tag -l v0.6.4` and `git ls-remote --tags origin v0.6.4` are both empty.
- `33-HANDOFF.md` contains zero CJK-matching lines and all eight checklist items.
</verification>

<success_criteria>
SC#3, SC#4, and SC#5 of ROADMAP Phase 33 are discharged: the `Documentation` metadata URL is
confirmed by a real HTTP fetch on the prepared tree; the milestone invariants are asserted over
the full milestone diff with verbatim evidence; and REL-02's remaining half is recorded as an
explicit handoff to `/gsd-complete-milestone` plus the owner, with no tag and no publish
performed in this phase.
</success_criteria>

<output>
Create `.planning/phases/33-v0-6-4-release-prep/33-04-SUMMARY.md` when done.
State plainly in the summary which success criteria are MET and which are HANDED OFF. Do not
report REL-02 as satisfied — report SC#1 through SC#4 as met and SC#5's subject matter as
deliberately unmet and transferred.
</output>
