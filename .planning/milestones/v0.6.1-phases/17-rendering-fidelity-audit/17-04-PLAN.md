---
phase: 17-rendering-fidelity-audit
plan: 04
type: execute
wave: 4
depends_on: ["17-03"]
files_modified:
  - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUD-01]
must_haves:
  truths:
    - "Every high-severity root cause — grouped per D-10 (one entry per node-kind + failure-mode, NOT per occurrence) — is registered as exactly one FID-01x (FID-01a, FID-01b, …) under REQUIREMENTS.md section Rendering Fidelity Audit, each listing its occurrence docnames/pages and a minimal repro (SC#4, D-11)."
    - "Medium/low issues stay fully recorded in the catalogue and are referenced from REQUIREMENTS.md section Future Requirements by a single pointer entry; no medium/low issue becomes a FID-01x (D-11)."
    - "The count of distinct FID-01x entries appended equals the count of distinct high-severity root-cause groups in the catalogue (Validation Architecture backlog-generation check; RESEARCH Pitfall 6)."
    - "FID-01a/FID-01b… lettering is assigned in a deterministic, documented order (catalogue root-cause order) so re-runs/appends reproduce the same lettering (ordering edge)."
    - "The append is valid even when zero high-severity issues are found — in that case no FID-01x is added and the catalogue records 'no high-severity issues; FID-01 backlog empty' explicitly (empty edge)."
    - "Existing requirement IDs in REQUIREMENTS.md (TODO-01, MAN-01, LEN-01, AUD-01, FID-01, GATE-03, DEG-03, XREF-02, CFG-01) are unchanged after the append (integrity)."
    - "All five mechanical checks pass: SC#1 coverage vs docname closure, SC#2 D-09 schema completeness, SC#3 out-of-scope classification, SC#4 FID-01x count == distinct high root-cause groups, and D-07 provenance freshness."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "FID-01a… high-severity backlog under section Rendering Fidelity Audit + single medium/low pointer under section Future Requirements"
      contains: "FID-01"
    - path: ".planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md"
      provides: "Finalized catalogue with root-cause grouping and recorded mechanical-check outcomes"
      contains: "root cause"
  key_links:
    - from: "17-AUDIT-CATALOGUE.md high-severity rows"
      to: "REQUIREMENTS.md section Rendering Fidelity Audit FID-01x entries"
      via: "root-cause grouping (D-10), one FID-01x per group"
      pattern: "FID-01[a-z]"
  prohibitions:
    - "Never renumber, delete, or rewrite existing requirement IDs when appending — the append is additive-only under the two designated sections, applied via scoped Edit (not a whole-file rewrite)."
    - "Never emit one FID-01x per occurrence — group by root cause first (D-10; RESEARCH Pitfall 6)."
    - "Never promote a medium/low issue to FID-01x — high only (D-11, SC#4)."
---

<objective>
Turn the human-confirmed catalogue into Phase 18's backlog: group high-severity rows by root cause (D-10), append them to `REQUIREMENTS.md` as `FID-01a`, `FID-01b`, … (high only, D-11/SC#4), add a single medium/low pointer under Future Requirements, and run the five mechanical consistency checks that certify the catalogue is complete and correct.

Purpose: Phase 18's fix plans and their real-compile regression fixtures start directly from these FID-01x entries and the catalogue's D-09 repro fields — the backlog must map 1:1 to handler-level fixes, and the audit's coverage/classification must be verified before it is called done.
Output: The `FID-01x` append to REQUIREMENTS.md, the finalized catalogue, and recorded mechanical-check outcomes.
</objective>

## Artifacts produced by this plan

- Append to `.planning/REQUIREMENTS.md` — `FID-01a`/`FID-01b`/… under section Rendering Fidelity Audit (high-severity root causes), plus a single medium/low pointer under section Future Requirements. Existing IDs untouched.
- Updated `.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md` — root-cause grouping + recorded mechanical-check outcomes.
- Ephemeral / NEVER committed: any throwaway consistency-check script.

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md
@.planning/phases/17-rendering-fidelity-audit/17-RESEARCH.md
@.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md
@tests/test_corpus_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group high-severity rows by root cause and append the FID-01x backlog to REQUIREMENTS.md</name>
  <read_first>
    - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md (the human-confirmed, severity-final issue table)
    - .planning/REQUIREMENTS.md section "Rendering Fidelity Audit" (the FID-01 append target) and section "Future Requirements"
    - .planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md decisions (D-09, D-10, D-11)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md</files>
  <action>
    In the catalogue, group the accepted high-severity rows by root cause — same node kind + same failure mode is ONE group even across N docnames (D-10; RESEARCH Pitfall 6). Record the grouping (a "Root cause" column or a grouped subsection) and assign FID-01x letters (a, b, …) in a fixed, documented order (catalogue root-cause order) so the lettering is reproducible (ordering edge).
    Then, using a SCOPED Edit (never a whole-file rewrite), append one `FID-01x` bullet per high-severity root-cause group under REQUIREMENTS.md section Rendering Fidelity Audit (right after the existing FID-01 entry), each stating: the root-cause node kind + failure mode, its occurrence docnames/pages (D-09 occurrence list), and a minimal repro pointer — shaped as Phase 18 fixture input. Under section Future Requirements, add a SINGLE pointer entry noting that medium/low fidelity issues are fully recorded in `17-AUDIT-CATALOGUE.md` as next-milestone candidates (D-11) — do not enumerate them as requirements. If zero high-severity issues were confirmed, add no FID-01x and instead record "no high-severity issues found; FID-01 backlog empty" in both the catalogue and a one-line note under the Rendering Fidelity Audit section (empty edge). Leave every pre-existing requirement ID (TODO-01, MAN-01, LEN-01, AUD-01, FID-01, GATE-03, DEG-03, XREF-02, CFG-01) byte-for-byte unchanged.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` section Rendering Fidelity Audit contains one `FID-01x` line per distinct high-severity root cause (or the explicit "backlog empty" note if none).
    - The count of `FID-01[a-z]` IDs equals the count of distinct high-severity root-cause groups recorded in the catalogue.
    - All nine pre-existing requirement IDs still present and unchanged; the append is additive-only.
    - Future Requirements has exactly one new pointer to the catalogue's medium/low issues (no per-issue enumeration).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import re; r=open('.planning/REQUIREMENTS.md').read(); ids=sorted(set(re.findall(r'FID-01[a-z]+', r))); print('FID-01x ids:', ids); assert all(x in r for x in ['TODO-01','MAN-01','LEN-01','AUD-01','FID-01','GATE-03','DEG-03','XREF-02','CFG-01']), 'existing id missing'; print('existing ids intact')"</automated>
  </verify>
  <done>High-severity root causes are registered as FID-01a… (high only, one per root cause) with the medium/low pointer added; existing requirement IDs are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Run the five mechanical consistency checks and record outcomes</name>
  <read_first>
    - .planning/phases/17-rendering-fidelity-audit/17-RESEARCH.md section "Validation Architecture" (the Req/SC -> Validation Map and the five mechanical checks)
    - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md (final issue table + progress tracker + provenance header)
    - tests/test_corpus_gate.py (re-run the corpus gate test for the D-07 freshness check)
  </read_first>
  <files>.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md</files>
  <action>
    Run the five mechanical checks from RESEARCH's Validation Architecture and record each outcome (pass/fail + evidence) in a "Validation" section of the catalogue: (1) SC#1 coverage — the progress tracker's docname set equals the corpus toctree closure (a fresh recursive `#include()` walk), zero "not yet audited" remaining. (2) SC#2 schema — every active issue row has all D-09 columns non-empty. (3) SC#3 classification — no active issue row's node-kind column equals a known out-of-scope kind and no description matches the autodoc-import / `py:meth` signatures (scope the grep to the active issue table, not the documented exclusion section, to avoid a self-invalidating match). (4) SC#4 backlog — the distinct `FID-01[a-z]` count in REQUIREMENTS.md equals the distinct high-severity root-cause group count. (5) D-07 freshness — re-run `pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow` and confirm the reported PDF size / empty unknown_visit catalogue still match the catalogue's provenance header (RESEARCH Pitfall 7). Also re-confirm no rasterized image or rebuilt PDF is staged for commit. Resolve any failing check before marking the phase done.
  </action>
  <acceptance_criteria>
    - The catalogue has a Validation section recording pass + evidence for all five checks.
    - `git status --porcelain` shows no `.png`/`.pdf`/scratch build artifact staged under the repo.
    - `pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow` passes and its PDF size/empty-catalogue match the provenance header.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -q</automated>
    <automated>test -z "$(git status --porcelain | grep -E '\.(png|pdf)$')" &amp;&amp; grep -qi "Validation" .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md &amp;&amp; echo "validation-recorded-no-image-artifacts"</automated>
  </verify>
  <done>All five mechanical checks pass and are recorded in the catalogue; no image/PDF artifacts are committed; provenance header matches a fresh build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit output → shared planning artifact (REQUIREMENTS.md) | the append edits a file other phases depend on; existing IDs must not be corrupted |
| audit process → corpus gate subprocess (freshness re-check) | subprocess re-run to validate provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Tampering | REQUIREMENTS.md append | medium | mitigate | Additive-only scoped `Edit` under the two designated sections (never a whole-file `Write`); a post-append check asserts all nine pre-existing requirement IDs are still present and unchanged. |
| T-17-01 | Tampering | corpus gate subprocess re-run | low | mitigate | Reuse `_run_corpus_sphinx_build`'s explicit-arg-list `subprocess.run([...], shell=False)` pattern; no shell interpolation (RESEARCH Security Domain V12). |
| T-17-02 | Information Disclosure | leftover rasterized images / rebuilt PDF | low | mitigate | `git status` check confirms no `.png`/`.pdf`/scratch build artifact is staged before finalizing (CONTEXT Deferred Ideas). |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — no package-manager installs in scope; audit tooling is already-installed `pypdf` + ad-hoc `nix-shell` system tools (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- FID-01a… append is high-only, one-per-root-cause (D-10/D-11); medium/low pointer added; existing IDs intact.
- All five mechanical checks pass and are recorded; provenance header matches a fresh build; no image/PDF committed.
</verification>

<success_criteria>
`REQUIREMENTS.md` carries a clean FID-01a… high-severity backlog (or an explicit empty-backlog note) that Phase 18 can consume 1:1 as handler-level fix plans, and the finalized `17-AUDIT-CATALOGUE.md` passes all five coverage/consistency checks.
</success_criteria>

<output>
Create `.planning/phases/17-rendering-fidelity-audit/17-04-SUMMARY.md` when done.
</output>
