---
phase: 17-rendering-fidelity-audit
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md
autonomous: true
requirements: [AUD-01]
must_haves:
  truths:
    - "Every docname in the corpus toctree closure has been visually reviewed page-by-page (each page rasterized and read via the Read tool) — the progress tracker records each as audited, none left 'not yet audited' (SC#1, D-01, D-02)."
    - "A docname with no divergence is recorded as 'audited — no issues' (a token distinct from 'not yet audited'), so a clean docname is never mis-read as unaudited and the catalogue can be finalized even if some docnames or the whole corpus yield zero candidates (empty edge; RESEARCH empty-catalogue pitfall)."
    - "Every candidate issue row carries docname + node kind, a source-vs-output description, a provisional severity (D-08), PDF page number, occurrence count, and a minimal repro snippet/source pointer (D-09), with cases Claude cannot classify flagged 'uncertain' (D-03)."
    - "No candidate row targets a known out-of-scope signature — graphviz/inheritance_diagram nodes, non-included-doc xref targets, or py:meth/autodoc-import warnings (SC#3)."
    - "Candidate rows are ordered deterministically (docname/toctree order, then page) so re-runs and resumed sessions reproduce the same catalogue ordering (ordering edge)."
  artifacts:
    - path: ".planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md"
      provides: "Populated candidate issue table (with uncertain flags + provisional severity) and a fully-completed per-docname progress tracker"
      contains: "Severity"
  key_links:
    - from: "each docname's rasterized PDF page range"
      to: "the same docname's -b html baseline (rST source as tiebreaker)"
      via: "visual cross-check for content/structure/meaning-bearing-style divergence (D-04, D-06)"
      pattern: "audited"
  prohibitions:
    - "Never mark a docname 'audited' from a text pre-filter's silence alone — every page gets an actual visual look; the pre-filter changes review ORDER/SPEED, never COVERAGE (SC#1; RESEARCH anti-pattern)."
    - "Never compare against an internet-hosted Sphinx render (www.sphinx-doc.org) — only the locally-built same-corpus -b html baseline (D-05)."
    - "Never flag a pure-appearance difference (color, font, margin, page break, sidebar/theme chrome) as a candidate — those are expected PDF-vs-HTML media differences (D-06)."
    - "Never commit rasterized page images — delete each docname's PNG batch after review (CONTEXT Deferred Ideas; RESEARCH Pitfall 1)."
---

<objective>
Run Claude's first pass (D-01): for every docname, rasterize its PDF page range, read the pages, cross-check against the `-b html` authority baseline (D-04), and record a candidate-issue list biased toward false positives (D-03). This is the phase's core discovery work and the multi-session bulk (D-02).

Purpose: Warnings only surface *dropped* content; silent mis-renders (compiles clean, no warning, yet diverges) are only findable by looking (SC#1). Claude builds the candidate list; the human confirms it in Plan 17-03.
Output: The populated candidate issue table + a completed per-docname progress tracker in `17-AUDIT-CATALOGUE.md`.
</objective>

## Artifacts produced by this plan

- Updated `.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md` — candidate issue rows (D-09 fields, uncertain flags, provisional severity) + every docname marked audited in the progress tracker.
- Ephemeral / NEVER committed: per-docname rasterized PNG batches (deleted after each docname), optional text-diff pre-filter scratch output.

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md
@.planning/phases/17-rendering-fidelity-audit/17-RESEARCH.md
@.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-docname visual first pass — rasterize, read, cross-check, record candidates</name>
  <read_first>
    - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md (the mapping table + progress tracker + D-09 schema from Plan 17-01; resume from the first "not yet audited" docname)
    - .planning/phases/17-rendering-fidelity-audit/17-RESEARCH.md §"Pattern 3" (text pre-filter), §"Common Pitfalls" (1, 3, 4, 5), §"Code Examples" (pdftoppm invocation)
    - .planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md §decisions (D-01, D-02, D-03, D-04, D-06, D-09)
  </read_first>
  <files>.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md</files>
  <action>
    Work docname-by-docname in toctree order, resuming from the first "not yet audited" entry in the progress tracker (D-02 resumability — the audit spans multiple sessions; re-run the Plan 17-01 environment smoke check at the start of each resumed session). For each docname: rasterize ONLY its page range with `nix-shell -p poppler-utils --run "pdftoppm -png -r 150 -f &lt;start&gt; -l &lt;end&gt; index.pdf &lt;scratch&gt;/page_images/page"` (batch strictly per-docname, ≤~20-30 pages, to protect the context budget — RESEARCH Pitfall 1), read every produced PNG via the Read tool (SC#1's literal page-by-page bar), and cross-check against that docname's `-b html` output as the authority (D-04), reading the `.rst` source only to confirm intent. Optionally use `pypdf.extract_text()` or the `-b text` `.txt` as a pre-filter to prioritize/skim order — but never as a substitute for the visual look.
    Judge divergence per D-06: IN scope = content loss/duplication/misordering, structural breakage (lists, tables, heading hierarchy), loss of meaning-bearing styling (code-literal rendered as prose, lost emphasis, links flattened to plain text, table column misalignment); OUT of scope = pure appearance (color/font/margin/page-break/theme-chrome). When unsure whether a divergence is in-scope or an acceptable media difference, record it as a candidate flagged "uncertain" — bias toward false positives, never a silent false negative (D-03). Record each candidate as a row with all D-09 fields (docname, node kind, source-vs-output description, provisional severity per D-08, PDF page, occurrence count, minimal repro snippet or corpus source-line pointer). Immediately after finishing a docname, update its progress-tracker entry to "audited — N issue(s)" or "audited — no issues" (never batch-defer — resumability depends on it being current) and delete that docname's PNG batch.
  </action>
  <acceptance_criteria>
    - After the pass, the progress tracker has zero "not yet audited" entries — every mapped docname reads "audited — no issues" or "audited — N issue(s)".
    - Every candidate row has all D-09 columns non-empty; uncertain candidates carry the "uncertain" flag.
    - No candidate row's source-vs-output description is purely about color/font/margin/page-break/sidebar (D-06 filter).
    - No rasterized PNG remains in the scratch directory after each docname is reviewed.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "not yet audited" .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md | grep -qx 0 &amp;&amp; echo "all-docnames-audited"</automated>
    <human-check>Every candidate row corresponds to a real content/structure/meaning divergence seen on a page image, not an appearance-only difference.</human-check>
  </verify>
  <done>Every docname is marked audited; a candidate issue list (biased false-positive, with uncertain flags and full D-09 fields) is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Classify candidates against the out-of-scope table and assign provisional severity</name>
  <read_first>
    - .planning/REQUIREMENTS.md §"Out of Scope" table (the SC#3 classification authority)
    - .planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md §decisions (D-06, D-08)
    - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md (the candidate rows from Task 1)
  </read_first>
  <files>.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md</files>
  <action>
    Sweep the accumulated candidate list once and classify each row against the REQUIREMENTS.md Out-of-Scope table (SC#3): remove any row whose node kind / docname is a known out-of-scope degradation — graphviz / inheritance_diagram placeholders, non-included-document cross-reference targets, and Sphinx-side autodoc-import / `py:meth` resolution warnings (these occur under `-b html` too and are builder-independent). Move removed rows into a short "Excluded (out-of-scope)" subsection with the exclusion reason, so the exclusion is auditable rather than silent. For every surviving row confirm a provisional severity per the D-08 rubric (high / medium / low), keeping "uncertain" flags intact for the human (D-03). Order the surviving rows deterministically (docname/toctree order, then PDF page) so the catalogue is reproducible across resumed sessions and later appends (ordering edge).
  </action>
  <acceptance_criteria>
    - No row in the active issue table has a node-kind column equal to `graphviz` or `inheritance_diagram`, nor a description matching the autodoc-import / `py:meth` warning signatures (rows for those live only in the Excluded subsection with a reason).
    - Every active row has a non-empty provisional severity of exactly `high`, `medium`, or `low`.
    - Active rows are sorted by (docname, PDF page) — verified stable on a re-sort.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import re,sys; t=open(sys.argv[1]).read(); import sys as s; body=t.split('Excluded')[0]; rows=[l for l in body.splitlines() if l.strip().startswith('|') and '---' not in l]; oos=[l for l in rows if re.search(r'inheritance.diagram|graphviz|py:meth', l)]; print('active out-of-scope rows:', len(oos)); sys.exit(0 if len(oos)==0 else 1)" .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md</automated>
  </verify>
  <done>The active issue table holds only in-scope silent mis-renders, each with a provisional severity; out-of-scope items are quarantined with reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit process → pdftoppm/nix-shell (rasterization) | page-range/path arguments flow into a shelled-out rasterizer |
| scratch page images → repo | rasterized PNGs could embed corpus content if accidentally committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | `pdftoppm`/`nix-shell` rasterization invocations | low | mitigate | Explicit arg-list `subprocess.run([...], shell=False)`; page ranges/paths are integers/fixed paths, never shell-interpolated (RESEARCH Security Domain V12). |
| T-17-02 | Information Disclosure | per-docname PNG batches in scratch | low | mitigate | Rasterize into a scratch dir outside the repo and delete each batch after review; never `git add` images (CONTEXT Deferred Ideas; RESEARCH Pitfall 1). |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — no package-manager installs; `pdftoppm` is an ad-hoc `nix-shell -p poppler-utils` system tool, never added to `pyproject.toml`. |
</threat_model>

<verification>
- Progress tracker fully consumed (zero "not yet audited"); a clean docname reads "audited — no issues".
- Every active candidate has full D-09 fields and a provisional severity; uncertain flags preserved.
- Out-of-scope degradations excluded (SC#3) with recorded reasons; active rows deterministically ordered.
</verification>

<success_criteria>
A complete, in-scope, deterministically-ordered candidate issue list exists — every docname visually audited page-by-page — ready for the human confirmation gate (Plan 17-03) to accept/reject and finalize severity.
</success_criteria>

<output>
Create `.planning/phases/17-rendering-fidelity-audit/17-02-SUMMARY.md` when done.
</output>
