---
phase: 17-rendering-fidelity-audit
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md
autonomous: false
requirements: [AUD-01]
must_haves:
  truths:
    - "The human has reviewed every candidate issue and recorded an accept/reject decision and a final severity (high/medium/low per D-08) for each accepted one (D-01a)."
    - "The human spot-checked a recorded sample of docnames/pages Claude marked clean, and the catalogue records the spot-check sample size and the resulting miss-rate (misses / sample size) (D-01a)."
    - "Every candidate previously flagged 'uncertain' (D-03) now has a resolved human disposition — no candidate remains in an undecided state after the gate."
  artifacts:
    - path: ".planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md"
      provides: "Human-confirmed issue table (accept/reject + final severity per row) plus a recorded spot-check sample size and miss-rate"
      contains: "miss-rate"
  key_links:
    - from: "Claude's candidate list (Plan 17-02)"
      to: "the human-confirmed issue set (the evidentiary bar for FID-01, Plan 17-04)"
      via: "human accept/reject + final severity + clean-set spot-check (D-01a)"
      pattern: "confirmed"
---

<objective>
The central human gate (D-01/D-01a): the human confirms Claude's entire candidate list and spot-checks a sample of pages Claude marked clean, so the finalized catalogue — and therefore Phase 18's FID-01 scope — rests on human-confirmed evidence, not Claude's first pass alone.

Purpose: This is the phase's genuine checkpoint; it cannot be self-approved by the executor. The human's confirmations are what scope FID-01 (Plan 17-04).
Output: An accept/reject decision + final severity per candidate, and a recorded clean-set spot-check (sample size + miss-rate), written into `17-AUDIT-CATALOGUE.md`.
</objective>

## Artifacts produced by this plan

- Updated `.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md` — per-row human disposition (accept/reject) + final severity, and a recorded spot-check sample size + miss-rate.

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md
@.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint — human confirms ALL candidates + spot-checks the clean set (D-01a)</name>
  <files>.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md</files>
  <read_first>
    - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md (the candidate issue table + progress tracker)
    - .planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md decisions (D-01, D-01a, D-03, D-08)
  </read_first>
  <what-built>
    Claude has completed the page-by-page first pass (Plan 17-02): every docname in the corpus is marked audited, and a candidate issue list — biased toward false positives, with "uncertain" flags (D-03) and provisional severities (D-08) — is recorded in `17-AUDIT-CATALOGUE.md`, with known out-of-scope degradations already excluded (SC#3).
  </what-built>
  <action>
    Blocking human-verify gate (no code produced). This is the phase's central human gate (D-01/D-01a) and MUST NOT be self-approved by the executor. Present the catalogue's active candidate table to the operator and pause; re-rasterize any specific docname's page range on request. Do not resume until the operator has ruled accept/reject + final severity on every candidate and reported the clean-set spot-check outcome per the how-to-verify steps and resume-signal below.
  </action>
  <acceptance_criteria>
    - The operator has issued an accept/reject decision + final severity for EVERY candidate row (D-01a — all, not a sample).
    - The operator reports a clean-set spot-check: how many "audited — no issues" pages were re-checked and how many misses were found.
  </acceptance_criteria>
  <how-to-verify>
    1. Open `.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md` and read the active issue table.
    2. For EACH candidate row (D-01a — confirm ALL, not a sample): decide accept or reject, and set the final severity (high / medium / low per the D-08 rubric). Pay special attention to rows flagged "uncertain". For any row you cannot judge from the description + repro snippet, ask Claude to re-rasterize that specific docname's page range (`nix-shell -p poppler-utils --run "pdftoppm -png -r 150 -f <start> -l <end> index.pdf out/page"`) and show the page.
    3. Spot-check the "clean" set: pick a handful of docnames the progress tracker marks "audited — no issues", have Claude re-render a few of their pages, and confirm nothing was missed. Note how many pages you sampled and how many misses you found (this is the D-01a miss-rate estimate).
    4. Tell Claude the per-row decisions/severities and the spot-check sample size + any misses, so it can record them.
  </how-to-verify>
  <verify>
    <human-check>Operator has ruled accept/reject + final severity on every candidate and reported the clean-set spot-check sample size + misses.</human-check>
  </verify>
  <resume-signal>Provide accept/reject + final severity for each candidate, and the clean-set spot-check sample size + misses; or list what still needs re-rendering.</resume-signal>
  <done>Operator has confirmed every candidate (accept/reject + final severity) and reported the clean-set spot-check outcome (D-01a).</done>
</task>

<task type="auto">
  <name>Task 2: Record the human confirmations and spot-check result into the catalogue</name>
  <read_first>
    - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md (the candidate rows to annotate)
    - .planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md §decisions (D-01a, D-08)
  </read_first>
  <files>.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md</files>
  <action>
    Apply the human's decisions from the checkpoint to `17-AUDIT-CATALOGUE.md`: mark each candidate row accepted or rejected and set its final severity (D-08); rejected rows move to a "Rejected candidates" subsection with the human's reason (kept for auditability, not deleted). Clear every remaining "uncertain" flag — after the gate no row is undecided (D-03 resolved). Add a "Human confirmation (D-01a)" section recording the clean-set spot-check: the sample size (pages/docnames re-checked) and the miss-rate (misses / sample size), so the evidentiary bar for FID-01 is itself auditable.
  </action>
  <acceptance_criteria>
    - Every active issue row carries an explicit accepted/rejected disposition and a final severity of exactly high/medium/low.
    - Zero rows still carry an "uncertain" flag.
    - The catalogue has a "Human confirmation (D-01a)" section stating the spot-check sample size and miss-rate.
  </acceptance_criteria>
  <verify>
    <automated>grep -qi "miss-rate\|miss rate" .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md &amp;&amp; ! grep -qi "uncertain" .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md &amp;&amp; echo "confirmations-recorded"</automated>
  </verify>
  <done>The catalogue reflects the human's accept/reject + final severity per row and records the clean-set spot-check sample size + miss-rate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude candidate list → human judgment | the human is the authority on accept/reject + severity; Claude only records the outcome |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04 | Repudiation | recorded human confirmations | low | mitigate | Record the confirmation outcome (per-row disposition, spot-check sample size + miss-rate) in the committed catalogue so the D-01a evidentiary bar is auditable later, not an unrecorded verbal approval. |
| T-17-02 | Information Disclosure | on-demand re-rasterized pages for the human | low | mitigate | Any re-render for the human uses the scratch dir outside the repo and is deleted after; never committed (CONTEXT Deferred Ideas). |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — no package-manager installs in this plan. |
</threat_model>

<verification>
- The human confirmed ALL candidates (accept/reject + final severity) and spot-checked the clean set (D-01a).
- All "uncertain" flags resolved; spot-check sample size + miss-rate recorded in the catalogue.
</verification>

<success_criteria>
The catalogue's issue set is human-confirmed and severity-final — a defensible evidentiary basis for enumerating the FID-01 high-severity backlog in Plan 17-04.
</success_criteria>

<output>
Create `.planning/phases/17-rendering-fidelity-audit/17-03-SUMMARY.md` when done.
</output>
