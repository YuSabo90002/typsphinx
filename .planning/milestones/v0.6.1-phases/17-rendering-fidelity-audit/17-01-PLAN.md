---
phase: 17-rendering-fidelity-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md
autonomous: true
requirements: [AUD-01]
must_haves:
  truths:
    - "The corpus PDF, HTML baseline, and text baseline are all built in-session from the SAME pinned corpus (tag v9.1.0, SHA cc7c6f435ad37bb12264f8118c8461b230e6830c) by reusing the tests/test_corpus_gate.py helpers unmodified (D-05)."
    - "The docname->page-range mapping covers pages 1..N of the compiled PDF with monotonic, non-overlapping ranges, and boundary pages (a heading landing exactly on a page break) are attributed to the correct docname (adjacency edge; RESEARCH Pitfall 2 spot-check)."
    - "17-AUDIT-CATALOGUE.md exists with a provenance header carrying THIS session's freshly-measured PDF byte size, page count, and an EMPTY unknown_visit catalogue — never Phase 15's stale 15,124,122-byte numbers (D-07; RESEARCH Pitfall 7)."
    - "The per-docname progress tracker lists every docname in the corpus toctree closure, each initialized to a 'not yet audited' token that is textually distinct from the 'audited' tokens, so a clean docname can never later read as unaudited (empty-input edge; D-02)."
  artifacts:
    - path: ".planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md"
      provides: "Provenance header + empty D-09 issue table + docname->page-range mapping + per-docname progress tracker"
      contains: "Provenance"
  key_links:
    - from: "index.pdf outline (pypdf)"
      to: "master index.typ #include() order (recursive walk)"
      via: "combine outline page numbers with the include()-order walk to assign one page range per docname"
      pattern: "#include\\("
  prohibitions:
    - "Never commit rasterized page images or a rebuilt corpus PDF/.typ tree to git — all build/rasterization output lives in a scratch directory outside the repo (CONTEXT Deferred Ideas)."
    - "Never copy Phase 15's provenance numbers into the header — re-measure this session's own build (RESEARCH Pitfall 7)."
---

<objective>
Stand up the audit infrastructure so the visual first pass (Plan 17-02) can run per-docname and resume across sessions. Build the three corpus artifacts (the `typstpdf` PDF under audit, the `-b html` authority baseline, the `-b text` pre-filter source) from the same pinned cached corpus, derive an exact docname->page-range mapping, and create the `17-AUDIT-CATALOGUE.md` shell (fresh provenance header + empty issue table + per-docname progress tracker).

Purpose: The audit is human-assisted (D-01) and multi-session (D-02); it needs a committed, resumable scaffold and an exact page map before any page is opened, or the page-by-page pass (SC#1) mis-attributes issues.
Output: `17-AUDIT-CATALOGUE.md` (skeleton), plus ephemeral scratch builds and a docname->page mapping recorded inside the catalogue.
</objective>

## Artifacts This Phase Produces

- `.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md` — the D-07 deliverable: provenance header, per-issue records (D-09), the docname->page-range mapping table, and the embedded per-docname progress tracker (D-02).
- Append to `.planning/REQUIREMENTS.md` — `FID-01a`, `FID-01b`, … high-severity root causes under §Rendering Fidelity Audit, plus a single medium/low pointer under §Future Requirements (produced in Plan 17-04).
- Ephemeral / NEVER committed: scratch `-b typstpdf` / `-b html` / `-b text` builds, rasterized page PNGs, and any throwaway mapping/pre-filter/validation helper scripts. No `typsphinx/` production symbols are created.

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md
@.planning/phases/17-rendering-fidelity-audit/17-RESEARCH.md
@.planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md
@tests/test_corpus_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke-check the environment and build the three corpus baselines</name>
  <read_first>
    - tests/test_corpus_gate.py (reuse `get_or_clone_corpus`, `wire_typsphinx_into_corpus_conf`, `_run_corpus_sphinx_build`, `resolve_corpus_tag` — do not reinvent subprocess plumbing)
    - .planning/phases/17-rendering-fidelity-audit/17-RESEARCH.md §"Environment Availability", §"Pattern 1", §"Common Pitfalls" (Pitfall 7)
    - .planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md §"Preamble: Corpus Provenance"
  </read_first>
  <files>(scratch only) &lt;scratch&gt;/17-audit/corpus_pdf_build/, &lt;scratch&gt;/17-audit/corpus_html_build/, &lt;scratch&gt;/17-audit/corpus_text_build/</files>
  <action>
    First run the RESEARCH-mandated smoke check before committing to the long audit (RESEARCH A2): `nix-shell -p poppler-utils --run "pdftoppm -v"` must succeed, and `pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -q` must pass (real `typst.compile()` + EMPTY unknown_visit catalogue, confirming Phase 16 landed). If `nix-shell` has no network to cache.nixos.org or the sandbox is in the ELF-exec-broken state (project memory `nixos-sandbox-test-env.md`), run these builds outside the sandbox per the phase's full-local-env fallback — do not degrade the audit.
    Then reuse `tests/test_corpus_gate.py` helpers (imported via `sys.path.insert(0, "tests"); import test_corpus_gate as tcg`): `tcg.get_or_clone_corpus(Path.home()/".cache"/"typsphinx-corpus-gate")` (cached, no re-clone), `tcg.wire_typsphinx_into_corpus_conf(doc_dir)` (idempotent), then `tcg._run_corpus_sphinx_build(b, doc_dir, out)` for `b` in `"typstpdf"`, `"html"`, `"text"` into three scratch dirs — the SAME cached tree for all three (D-05). HTML is the D-04 authority; text is the optional pre-filter source. Reconfirm returncode 0 for each; the doc-build-only-extension degradations (missing `dot`, `sphinxcontrib.websupport`) are expected and already REQUIREMENTS.md Out-of-Scope — not findings.
    Capture THIS build's fresh provenance for the header (D-07; NOT Phase 15's numbers, RESEARCH Pitfall 7): `index.pdf` byte size (`stat`), page count (`len(pypdf.PdfReader(index.pdf).pages)`), and the unknown_visit catalogue (must be empty). Record the corpus tag `resolve_corpus_tag()` and the clone SHA (`git -C &lt;clone&gt; rev-parse HEAD`).
  </action>
  <acceptance_criteria>
    - `nix-shell -p poppler-utils --run "pdftoppm -v"` returns 0 (or the fallback outside-sandbox path is documented in the SUMMARY).
    - `tests/test_corpus_gate.py::TestCorpusRenderGate` passes with an EMPTY unknown_visit catalogue.
    - `&lt;scratch&gt;/17-audit/corpus_pdf_build/index.pdf` starts with `%PDF` bytes; `corpus_html_build/` and `corpus_text_build/` each contain per-docname output files.
    - The measured PDF byte size differs from Phase 15's `15,124,122` (proves a fresh in-session measurement, not a copy — expected ~15,153,646 per RESEARCH).
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p poppler-utils --run "pdftoppm -v" &amp;&amp; python -c "import pypdf,sys; r=pypdf.PdfReader(sys.argv[1]); print('pages',len(r.pages))" &lt;scratch&gt;/17-audit/corpus_pdf_build/index.pdf</automated>
    <automated>pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -q</automated>
  </verify>
  <done>Three scratch builds exist from the same v9.1.0/cc7c6f4… corpus; fresh PDF byte size + page count + empty unknown_visit catalogue captured for the provenance header.</done>
</task>

<task type="auto">
  <name>Task 2: Derive and spot-check the docname->page-range mapping</name>
  <read_first>
    - .planning/phases/17-rendering-fidelity-audit/17-RESEARCH.md §"Pattern 2" (outline + #include()-order walk) and §"Common Pitfalls" (Pitfall 2 boundary spot-check)
    - &lt;scratch&gt;/17-audit/corpus_pdf_build/index.typ and its recursively-included per-docname `.typ` files
  </read_first>
  <files>(scratch only) a throwaway mapping helper + its printed table (recorded into the catalogue in Task 3)</files>
  <action>
    Build the exact docname->page-range table mechanically (never by eyeballing the in-body `#outline()` page — RESEARCH anti-pattern): walk `pypdf.PdfReader(index.pdf).outline` resolving each entry via `get_destination_page_number()`, and recursively walk the master `index.typ`'s `#include("…")` lines (toctree DFS order) to supply the docname for each heading slot. The first heading encountered for a docname is its start page; the next docname's start minus 1 is its end page. Pre-classify graphviz / inheritance_diagram docnames (identifiable from the corpus extension list, e.g. `usage/extensions/graphviz.rst`) as out-of-scope up front (SC#3; RESEARCH Pitfall 4) so they never enter the candidate list.
    Then spot-check the mapping against the adjacency risk (D-02 resumability depends on correct attribution): open the start page of at least the corpus-start, a mid-corpus, and the corpus-end docname, plus any caption-only toctree page, and confirm the visible title/opening content matches the mapped docname (RESEARCH Pitfall 2). Flag any range whose length is implausible relative to the docname's `.rst` line count.
  </action>
  <acceptance_criteria>
    - The mapping's page ranges are monotonic and non-overlapping and their union covers pages 1..N (N = the Task 1 page count).
    - Every docname in the recursive `#include()` closure appears exactly once in the mapping.
    - At least 3 boundary/start pages were visually spot-checked and matched their mapped docname; any mismatch is corrected before finalizing the table.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import sys; rows=[l for l in open(sys.argv[1]) if '|' in l]; print('mapping rows:',len(rows))" &lt;scratch&gt;/17-audit/mapping.txt</automated>
    <human-check>Spot-checked start pages (start/mid/end + a caption-only page) show the expected docname's opening content.</human-check>
  </verify>
  <done>An exact, spot-checked docname->page-range table exists covering the full PDF; graphviz/inheritance_diagram docnames flagged out-of-scope.</done>
</task>

<task type="auto">
  <name>Task 3: Write the 17-AUDIT-CATALOGUE.md skeleton (provenance + issue schema + progress tracker)</name>
  <read_first>
    - .planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md (the committed-report format precedent D-07 mirrors)
    - .planning/phases/17-rendering-fidelity-audit/17-CONTEXT.md §decisions (D-07, D-08, D-09, D-02)
  </read_first>
  <files>.planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md</files>
  <action>
    Create `17-AUDIT-CATALOGUE.md` as a single committed Markdown (D-07) with: (1) a Provenance header carrying Task 1's fresh numbers — corpus tag v9.1.0, clone SHA, `typst` 0.15.0, the exact `sphinx-build -b typstpdf`/`-b html` build commands, PDF byte size, page count, and "unknown_visit catalogue: EMPTY (confirms Phase 16 TODO-01/MAN-01 landed)". (2) The D-08 severity rubric verbatim (high = content lost/unreadable/grossly mis-structured; medium = meaning/distinction lost — code-literal-as-prose, lost emphasis, link flattened, column shift; low = finish sloppy). (3) An empty issue table whose columns are exactly the D-09 fields: docname, node kind, source-vs-output description, severity, PDF page, occurrence count, minimal repro snippet/source pointer, and an "uncertain?" flag (D-03). (4) The Task 2 docname->page-range mapping table. (5) A per-docname progress tracker listing every docname from the mapping, each set to a "not yet audited" token that is textually distinct from the "audited — no issues" and "audited — N issue(s)" tokens (D-02; the empty-input edge — a clean docname must never read as unaudited). Record the SC#3 out-of-scope exclusion list (graphviz/inheritance_diagram nodes, non-included-doc xrefs, py:meth/autodoc-import warnings) as a documented section so classification is unambiguous.
  </action>
  <acceptance_criteria>
    - `17-AUDIT-CATALOGUE.md` contains a Provenance section with corpus tag `v9.1.0` and SHA `cc7c6f435ad37bb12264f8118c8461b230e6830c` and this session's fresh PDF byte size.
    - The issue table header names all 8 D-09 columns; the table body is empty (no rows yet).
    - The progress tracker lists every mapped docname with a "not yet audited" token distinct from the audited tokens.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "cc7c6f435ad37bb12264f8118c8461b230e6830c" .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md &amp;&amp; grep -q "not yet audited" .planning/phases/17-rendering-fidelity-audit/17-AUDIT-CATALOGUE.md &amp;&amp; echo OK</automated>
  </verify>
  <done>The catalogue skeleton is committed with fresh provenance, the D-09 issue schema, the page mapping, and a full "not yet audited" progress tracker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit process → external CLIs (git, sphinx-build, pdftoppm, nix-shell) | subprocess invocations carrying path/docname/page-range arguments |
| pinned corpus clone → builds | fixed tag/SHA tree already vetted in Phase 15 — not untrusted input this phase introduces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | subprocess calls to sphinx-build / pdftoppm / nix-shell / git | low | mitigate | Invoke via explicit arg-list `subprocess.run([...], shell=False)` (never a shell-interpolated string of docnames/paths) — reuse the existing `_run_corpus_sphinx_build` / `get_or_clone_corpus` pattern (RESEARCH Security Domain V12). |
| T-17-02 | Information Disclosure | rasterized page images / rebuilt PDF in scratch | low | mitigate | Keep all build + rasterization output in a scratch dir outside the repo, never `git add`ed (CONTEXT Deferred Ideas). Corpus is public/MIT Sphinx docs — hygiene, not confidentiality. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — zero package-manager installs in scope; `pdftoppm`/`mutool` are ad-hoc `nix-shell -p` system tools never added to `pyproject.toml` (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- Smoke check green (`pdftoppm -v` + corpus gate test) or the outside-sandbox fallback documented.
- Three same-corpus builds present; fresh provenance measured (not Phase 15's numbers).
- Mapping monotonic, non-overlapping, full-coverage, boundary-spot-checked.
- Catalogue skeleton committed with provenance header, D-09 issue schema, page map, and a full "not yet audited" progress tracker.
</verification>

<success_criteria>
Plan 17-02 can begin the page-by-page visual pass immediately: for any docname it can look up an exact PDF page range, open the corresponding `-b html` baseline, and record findings into a schema-correct catalogue whose progress state survives session interruption.
</success_criteria>

<output>
Create `.planning/phases/17-rendering-fidelity-audit/17-01-SUMMARY.md` when done.
</output>
