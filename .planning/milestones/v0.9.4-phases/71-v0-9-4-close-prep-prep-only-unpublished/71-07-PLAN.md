---
phase: 71-v0-9-4-close-prep-prep-only-unpublished
plan: 07
type: execute
wave: 4
depends_on: ["71-01", "71-02", "71-03", "71-04", "71-05", "71-06"]
files_modified:
  - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md
  - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-HANDOFF.md
autonomous: true
requirements: [REL-13]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-09 / SC#4, phase close: the live `sha256sum .planning/REQUIREMENTS.md` equals REQ_SHA256_BASE, and `git diff --name-only -- .planning/REQUIREMENTS.md` is empty. No commit since PHASE_BASE_SHA touched the file, and REQ_VERDICT_CLOSE = MATCH. REL-13 reads `- [ ]` and Pending, read directly from the file.'
    - 'Edge (ordering): the `grep -n REL-13` output at close is compared line by line, in file order, with the same grep over REQUIREMENTS.md at PHASE_BASE_SHA, and the two are identical.'
    - 'Edge (encoding): the fence compares raw bytes by SHA-256, the primary probe, because a checkbox flip leaves `wc -l` unchanged.'
    - 'D-07: the first 14 lines of 71-HANDOFF.md, before any `##` heading, state that the milestone publishes nothing: no tag, no PyPI upload, no GitHub Release, no version bump. They state that the update-pin.yml dispatch and the Read the Docs stable check are not applicable, and record as a fact, not a step, that the daily update-pin.yml schedule moves the Japanese latest site onto the merged main without action while its stable site stays unchanged. They name the REL-13 pull request as the one irreversible action.'
    - 'D-06: the handoff lists, in order, the concrete commands /gsd-complete-milestone runs. First the fence backup, then a re-run of the trial merge, then a conditional branch update: `git merge --no-ff origin/main` only if origin/main moved past the merge-base, never a rebase, because of strict: true; otherwise a recorded no-op. Then the push, the PR from gsd/v0.9.4-typing-modernization to main, the six required checks named literally, and `gh pr merge <number> --merge`, citing #135, #136 and #143.'
    - 'SC#4 / D-07: the handoff lists the observations REL-13 is checked on after the merge: the merge commit on origin/main, pyproject.toml still 0.9.2, no v0.9.4 tag, PyPI 404 for 0.9.4, and no v0.9.4 Release.'
    - 'D-08 and D-12: the handoff records the 0.9.3 and 0.9.4 numbers as unclaimed, not decided. It states that the next release-prep phase promotes all four `## [Unreleased]` bullets, the Phase 61 to 63 mechanism. It names the open pull requests from 71-05''s census at PR_CENSUS_AT and from a live re-read, any dependabot one left untouched.'
    - 'SC#4 standalone: 71-HANDOFF.md reproduces the third-observation procedure inline. That covers both entry points (/gsd-execute-phase and /gsd-verify-work), the scratch backup of REQUIREMENTS.md, ROADMAP.md and STATE.md, the baseline digest, the REL-13 grep lines and `git checkout -- .planning/REQUIREMENTS.md`. It closes with a live fence observation, which also shows that the ten files 71-06 hashed are unchanged on this tip (D-11).'
    - 'The handoff contains no command that tags, creates a release or dispatches the translations pin workflow, and no plan authors 71-VERIFICATION.md.'
    - 'REL-13 is cited for coverage only; SUMMARY requirements-completed: [] (D-09).'
  artifacts:
    - path: .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-HANDOFF.md
      provides: 'negative-first standalone handoff: ordered /gsd-complete-milestone steps with the conditional branch update, SC#1..SC#4 report, items recorded without acting, third-observation procedure, live fence observation'
      contains: 'publishes nothing'
    - path: .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md
      provides: 're-verification at phase close'
      contains: 'REQ_VERDICT_CLOSE = '
  key_links:
    - from: '71-CLOSEOUT-GUARD.md REQ_SHA256_BASE'
      to: '71-HANDOFF.md third-observation block'
      via: 'the digest written inline so the operator needs no other file after phase.complete-family tooling runs'
      pattern: 'REQ_SHA256_BASE = '
    - from: '71-PREFLIGHT-EVIDENCE.md MERGE_TREE, MAIN_MOVED, PROTECTION_*, PR_CENSUS_AT'
      to: '71-HANDOFF.md conditional branch-update and merge steps'
      via: 'cited as the phase-time result, with the instruction to re-run merge-tree immediately before the PR'
      pattern: 'MERGE_TREE = '
    - from: '71-SC1-INVARIANTS.md BASE_71_06'
      to: 'the handoff fence observation'
      via: 'git diff --quiet BASE_71_06 HEAD over typsphinx/ and tests/ carries the D-11 masked-AST result to the final tip'
      pattern: 'BASE_71_06 = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action: no push, tag, pull request, merge, release, workflow run, or comment on any pull request. The handoff records steps for /gsd-complete-milestone and executes none of them.'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record REL-13 as satisfied: no checkbox flip, no Traceability change, no prose claiming the merge happened. A divergence found at close is reverted with git checkout and reported, never committed (D-09).'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT write a command in the handoff that tags, releases or dispatches the translations pin workflow, and MUST NOT treat 0.9.3 or 0.9.4 as a decided version (D-07, D-08).'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT restate another evidence file''s verdict without citing its section, and MUST NOT copy a digest, SHA or count as a measurement without re-running the command here.'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Close the phase's REL-13 fence and write the milestone handoff:
- re-verify the REL-13 checksum guard at phase close (D-09)
- author `71-HANDOFF.md` for `/gsd-complete-milestone`:
  - negative-first (D-07)
  - the ordered steps, with the conditional branch update that `strict: true` requires (D-06)
  - the post-merge observations REL-13 is checked on (SC#4)
  - the version numbers and open pull requests recorded without acting (D-08, D-12)
  - the third-observation procedure reproduced inline, closing with a live fence observation that
    carries D-11's masked-AST result to the final tip

Purpose: this plan runs in wave 4, after 71-06's close-tip measurements. It is therefore the first
plan that can read every other evidence file of the phase, and its guard re-verification is the
latest in-plan observation of `.planning/REQUIREMENTS.md`. The decisive third observation runs after
it, outside any plan, and the handoff hands it to the operator.

Output: `71-CLOSEOUT-GUARD.md` § "Re-verification at phase close", and `71-HANDOFF.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CHANGELOG-EVIDENCE.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-GREEN-TREE-EVIDENCE.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PREFLIGHT-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run project tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line. The verify fails on any key line in `71-CLOSEOUT-GUARD.md` containing an opening
parenthesis. Phase 69's executor appended explanations to five values although its plan said not to.

**Scratch files.** Parallel executors share the session scratchpad. Put any captured output under your
own `mktemp -d` directory with a `p7107_` prefix.

**Locale.** The host runs `LANG=ja_JP.UTF-8`; prefix `LC_ALL=C` to any `git` command whose message you
quote for comparison.

**Reserved name.** Never create `71-VERIFICATION.md`; `gsd-verifier` owns it and overwrites it
wholesale.
</worktree_provisioning>

<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: v0.9.4 -->
<!-- planner-discipline-allow: gh release create|workflow run update-pin|git tag -a|git tag v -->
<!-- plan-criteria-allow: R4 - the only match is the text git tag inside a quoted negative-grep alternation, not a pipeline stage; every real git call in this plan's verifies is captured into a variable first -->

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
The version tags are named because the probes assert their absence. The release, pin-dispatch and tag
command shapes are forbidden in the handoff, and the gate asserts they are absent. `## HALT` is
written only on a failure path.

## Planning-time census (2026-09-13; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| REL-13 in REQUIREMENTS.md | checkbox at `:24` (`- [ ] **REL-13**: Close prep only, unpublished: one CHANGELOG bullet under the existing …`), AMENDED block `:26-34`, Traceability row `:71` (`\| REL-13 \| Phase 71 \| Pending — coverage only; …`), coverage line `:75` | `grep -n` |
| Required checks on `main` | `Test Python 3.12 on ubuntu-latest`, `Test Python 3.13 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`; `strict: true` | `71-PREFLIGHT-EVIDENCE.md` (measured in wave 2) |
| Merge method | #135, #136 and #143 are `Merge pull request` commits on `main`'s first-parent history | `71-PREFLIGHT-EVIDENCE.md` |
| Main-checkout re-sync hazard | `uv sync --extra dev` is an exact sync and drops the docs extra in the main checkout; the restore is `--extra dev --extra docs` | `71-CONTEXT.md` `<code_context>` |
| ja site mechanism | `typsphinx-doc-translations`' `update-pin.yml` runs daily on a schedule and follows `main`'s tip; ja `stable` follows that repository's tags | `71-CONTEXT.md` `<domain>`, D-07 |
| Precedent handoff | `69-HANDOFF.md`: negative opening, ordered steps, per-SC report, items recorded without acting, inline third observation, fence observation, deliberate non-actions | `.planning/milestones/v0.9.3-phases/69-…/69-HANDOFF.md` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: re-verify the REL-13 closeout guard at phase close, reverting any divergence by hand</name>
  <precondition>Plans 71-01 to 71-06 are merged into this worktree's HEAD, their evidence files exist without a HALT heading, and `71-SC1-INVARIANTS.md` reads `D11_VERDICT = MET`. Otherwise write `## HALT: wave 3 incomplete` in `71-CLOSEOUT-GUARD.md` naming the missing item, and stop.</precondition>
  <reversibility rating="reversible">Evidence appends, plus, only on divergence, a `git checkout` restoring a tracked file to its committed state.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md</files>
  <read_first>
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md (§ "Baseline", § "The lines under guard", § "Re-verification protocol (phase close)", § "For the operator running phase.complete")
    - .planning/REQUIREMENTS.md (the live REL-13 lines)
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md § "Re-verification at phase close" (the comparison-table shape)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-09
  </read_first>
  <action>
Append `## Re-verification at phase close` to `71-CLOSEOUT-GUARD.md` (D-09). Run exactly the commands the file's own protocol names, and record each with its output and a MATCH or DIVERGED verdict against the Baseline:
- `date -u +"%Y-%m-%dT%H:%M:%SZ"`, key `CLOSE_AT`
- `sha256sum .planning/REQUIREMENTS.md`, key `REQ_SHA256_CLOSE`, set in a two-row table beside `REQ_SHA256_BASE`
- `wc -l < .planning/REQUIREMENTS.md`, key `REQ_LINES_CLOSE`
- `git diff --name-only -- .planning/REQUIREMENTS.md`, empty
- `git log --oneline "$PHASE_BASE_SHA"..HEAD -- .planning/REQUIREMENTS.md`, empty: no commit in the phase touched the file
- `grep -n 'REL-13' .planning/REQUIREMENTS.md`, compared line by line, in file order, with § "The lines under guard" (edge: ordering)

Write `REQ_VERDICT_CLOSE = MATCH` only if every comparison holds. Then quote REL-13's checkbox line and its Traceability row as read directly from the file (`- [ ]`, `Pending`), never inferred from any SUMMARY frontmatter. Every plan of this phase declares `requirements-completed: []`.

**On any divergence:**
- Record the verbatim `git diff -- .planning/REQUIREMENTS.md`.
- Run `git checkout -- .planning/REQUIREMENTS.md`, then re-run the checksum and the grep to show the Baseline restored.
- Record the sequence under `### Divergence detected and reverted`, write `REQ_VERDICT_CLOSE = REVERTED`, and report it. Never commit the divergence.

Close the section by confirming that § "For the operator running phase.complete" is present and unedited. The decisive third observation runs after this plan, outside any plan's reach, and `71-HANDOFF.md` reproduces it.

Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; G="$D/71-CLOSEOUT-GUARD.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; k() { sed -n "s/^$1 = //p" "$G" | head -n 1; } &amp;&amp; PB="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(k REQ_SHA256_CLOSE)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(wc -l &lt; .planning/REQUIREMENTS.md)" = "$(k REQ_LINES_BASE)" ] &amp;&amp; [ "$(k REQ_LINES_CLOSE)" = "$(k REQ_LINES_BASE)" ] &amp;&amp; [ -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" ] &amp;&amp; [ -z "$(git log --format=%h "$PB"..HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; grep -qE '^- \[ \] \*\*REL-13\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-13 \| Phase 71 \| Pending' .planning/REQUIREMENTS.md &amp;&amp; [ "$(grep -c 'REL-13' .planning/REQUIREMENTS.md)" = "$(k REL13_HITS_BASE)" ] &amp;&amp; RB="$(git show "$PB:.planning/REQUIREMENTS.md")" &amp;&amp; [ -n "$RB" ] &amp;&amp; [ "$(grep -n 'REL-13' .planning/REQUIREMENTS.md)" = "$(printf '%s\n' "$RB" | grep -n 'REL-13')" ] &amp;&amp; [ "$(k REQ_VERDICT_CLOSE)" = MATCH ] &amp;&amp; [ -n "$(k CLOSE_AT)" ] &amp;&amp; grep -qxF '## Re-verification at phase close' "$G" &amp;&amp; grep -qxF '## For the operator running phase.complete' "$G" &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$G" &amp;&amp; ! grep -q '^## HALT' "$G"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- `PHASE_BASE_SHA` unreadable or not an ancestor of HEAD
- the live digest or `REQ_SHA256_CLOSE` differing from `REQ_SHA256_BASE`, or the live or recorded line count differing from `REQ_LINES_BASE`
- a working-tree change to REQUIREMENTS.md, or any commit since the phase base touching it
- the REL-13 checkbox checked, its row not Pending, or the hit count changed
- the `grep -n REL-13` output differing, line by line, from the same grep at `PHASE_BASE_SHA`
- `REQ_VERDICT_CLOSE` not MATCH, or `CLOSE_AT` or either section missing
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Re-verification at phase close` records `CLOSE_AT`, `REQ_SHA256_CLOSE` beside `REQ_SHA256_BASE`, `REQ_LINES_CLOSE`, the empty name-only diff, the empty `git log` over the file since `PHASE_BASE_SHA`, and the REL-13 grep compared line by line, each with a MATCH or DIVERGED verdict.
    - `REQ_VERDICT_CLOSE = MATCH`, and REL-13 is quoted as `- [ ]` and `Pending` from the file itself.
    - Any divergence is recorded under `### Divergence detected and reverted`, reverted with `git checkout -- .planning/REQUIREMENTS.md`, and never committed.
  </acceptance_criteria>
  <done>The REL-13 fence is re-verified at phase close with an explicit verdict, and the decisive third observation is left, fully specified, for the operator.</done>
</task>

<task type="auto">
  <name>Task 2: Author 71-HANDOFF.md: negative-first, the ordered /gsd-complete-milestone steps with the conditional branch update, the items recorded without acting, the third observation reproduced inline, and a live fence observation</name>
  <precondition>Task 1 is committed, and `REQ_VERDICT_CLOSE = MATCH`.</precondition>
  <reversibility rating="reversible">One new planning document. It records steps for a later command and performs none of them.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-HANDOFF.md</files>
  <read_first>
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-HANDOFF.md (the structure to mirror: negative opening, ordered steps, per-SC report, "Recorded without acting", inline third observation, "Fence observation", "What this phase deliberately did not do")
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-05 to D-12, `<domain>` (the ja site mechanism) and § "Specific Ideas"
    - every evidence file of this phase: 71-CHANGELOG-EVIDENCE.md, 71-CLOSEOUT-GUARD.md, 71-SC1-INVARIANTS.md, 71-GREEN-TREE-EVIDENCE.md, 71-CI-EVIDENCE.md, 71-PREFLIGHT-EVIDENCE.md (the sections to cite)
    - .planning/REQUIREMENTS.md (REL-13 and its AMENDED block, quoted verbatim)
    - .planning/ROADMAP.md § "Phase 71" SC#4 and binding constraints 4 and 10
  </read_first>
  <action>
Create `71-HANDOFF.md`. Its first line is `# Phase 71: v0.9.4 Close Prep — Handoff for /gsd-complete-milestone`.

**The opening (D-07).** Lines 2 to 14 contain no `##` heading. In plain words they state:
- this milestone publishes nothing: no tag, local or remote; no PyPI upload; no GitHub Release; no version bump, since `pyproject.toml` stays `0.9.2`
- the `typsphinx-doc-translations` `update-pin.yml` dispatch and the Read the Docs `stable` check are not applicable, because nothing is released
- recorded as a fact, not a step: the daily `update-pin.yml` schedule moves the Japanese `latest` site onto the merged `main` within about a day with no action from anyone, and the Japanese `stable` site is unchanged, because it follows the translations repository's tags
- the one irreversible action of the close is REL-13: a pull request from `gsd/v0.9.4-typing-modernization` to `main`, opened and merged at `/gsd-complete-milestone`, as v0.9.3's REL-12 was with PR #143

Then these sections, in order.

**1. `## What /gsd-complete-milestone does, in order` (D-06).** Numbered steps, each with its exact command and one line of why:

0. **Fence first.** Back up `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` to a scratch directory outside the repository. Then run the section 4 probes, and start only on a MATCH.

1. **Re-run the trial merge.**
   - `git fetch origin`, then `git merge-tree --write-tree HEAD origin/main; echo "exit:$?"`, which requires `exit:0`.
   - Cite `71-PREFLIGHT-EVIDENCE.md`'s `MERGE_TREE`, `MAIN_MOVED`, `LOCK_CHECK_EXIT`, `MERGED_RUFF_LOCK_VERSION` and `TRIAL_MERGE_VERDICT` as the phase-time result. That tree SHA changes with every commit, so it is re-measured here.
   - Then `git merge-base --is-ancestor origin/main HEAD; echo "exit:$?"`. `exit:0` means `main` has not moved past the merge-base. `exit:1` means it moved: re-run the scratch `uv lock --check` and the merged-tree `ruff check .` and `black --check .` exactly as `71-PREFLIGHT-EVIDENCE.md` § "Merged lock" and § "Merged-tree lint" did. Under constraint 4 a moved `main`'s `ruff` count is authoritative, and a finding is the owner's decision before step 4.

2. **Conditional branch update (D-06).** Only if step 1 printed `exit:1`: in the main checkout, on `gsd/v0.9.4-typing-modernization`, run `git merge --no-ff origin/main -m "Merge origin/main into gsd/v0.9.4-typing-modernization"`. Never rebase the milestone's commits. Then run `uv lock --check`, then `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`; the docs extra is named because a bare `--extra dev` exact sync drops it from the main checkout. Re-check that `git merge-base --is-ancestor origin/main HEAD` now exits 0. Otherwise, record this step as a no-op with step 1's `exit:0` output. The reason: `main`'s protection is `strict: true` (`71-PREFLIGHT-EVIDENCE.md` § "main protection (D-06)"), so the pull request cannot merge until the branch is up to date.

3. **Push.** `git push --no-follow-tags origin gsd/v0.9.4-typing-modernization`, a fast-forward from `71-CI-EVIDENCE.md`'s `PUSHED_SHA`. It carries this phase's later `.planning/` commits, and the merge commit if step 2 ran.

4. **Open the pull request.** `gh pr create --base main --head gsd/v0.9.4-typing-modernization --title <English title> --body-file <file>`. Title and body are English and terse. The body goes through a file, never through a command substitution.

5. **Wait for the required checks.** Wait for all six to be green on the pull request head, named literally: `Test Python 3.12 on ubuntu-latest`, `Test Python 3.13 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage` and `Build Package`. The `pull_request` trigger runs the whole twelve-job `ci.yml`; the six are the ones protection requires.

6. **Merge.** `gh pr merge <number> --merge`: a merge commit, never squash or rebase. Cite `71-PREFLIGHT-EVIDENCE.md` § "Merge-method precedent (D-06)", where #135, #136 and #143 are `Merge pull request` commits.

7. **Observe what REL-13 requires, then close it** (SC#4, D-07).
   - The merge commit is on `origin/main`: `git log --first-parent -1 --format='%h %s' origin/main`.
   - `git show origin/main:pyproject.toml | sed -n 7p` reads `version = "0.9.2"`.
   - `git ls-remote --tags origin` shows `refs/tags/v0.9.2` and no v0.9.4 tag.
   - PyPI returns 404 for `0.9.4` and 200 for `0.9.2`, the positive control.
   - `gh release list` shows no v0.9.4 Release.

   Only on those five observations is REL-13's box checked, at `/gsd-complete-milestone`, never inside a plan.

8. **Not applicable.** No tag; no `release.yml` run; no PyPI upload; no GitHub Release; no `update-pin.yml` dispatch on `typsphinx-doc-translations`; no Read the Docs `stable` check. Read the Docs `latest` rebuilds from `main` and then shows the four `## [Unreleased]` bullets, which needs no action.

**2. `## What this phase satisfied`.** Quote REL-13's checkbox line verbatim from `.planning/REQUIREMENTS.md`, and state that its AMENDED block (D-02) governs the bullet: the Japanese-catalog clause is withdrawn. State that REL-13 stays open until step 7. Report each of ROADMAP SC#1 to SC#4 as MET or NOT MET, citing each evidence file and section rather than re-deriving the verdict:
- SC#1: `71-SC1-INVARIANTS.md`, both observations, the phase-scoped product diff, the code freeze on the close tip, and `D11_VERDICT`
- SC#2: `71-CHANGELOG-EVIDENCE.md` § "Fence assertions", § "Bullet content assertions" and § "Pure-addition proof", and `71-GREEN-TREE-EVIDENCE.md` § "Changelog page gate" (zero skipped)
- SC#3: `71-GREEN-TREE-EVIDENCE.md`, `71-CI-EVIDENCE.md` and `71-PREFLIGHT-EVIDENCE.md`, set side by side in one table:
  - the local suite summary and the `LC_ALL=C` summary
  - the local ruff version, `CI_RUFF_VERSION` and `MERGED_RUFF_LOCK_VERSION`
  - the docs `_FINAL` counts against the `_BASE` counts
  - `RUN_ID` with 12 of 12 jobs, both `windows-latest` and both `macos-latest` lanes named
  - the trial merge's `MERGE_RC`, `LOCK_CHECK_EXIT` and `MERGED_RUFF_EXIT`
- SC#4: `71-CLOSEOUT-GUARD.md` § "Re-verification at phase close", and this file

Add the D-10 statement: the single dispatch ran on `PUSHED_SHA`, the tip carrying every product-tree change of the phase, and every later commit is `.planning/`-only (`71-SC1-INVARIANTS.md` § "Commits after the CI dispatch").

**3. `## Recorded without acting`.**
- **D-12:** quote the open-PR census from `71-PREFLIGHT-EVIDENCE.md` § "Open pull requests (D-12)" with its `PR_CENSUS_AT` value, `OPEN_PRS` and `DEPENDABOT_OPEN_PRS`. Add a live re-read here, `gh pr list --state open --json number,title,author,headRefName,baseRefName`, with its `date -u +%FT%TZ`. Name any dependabot pull request with its state, left untouched. This phase took no action on any pull request. A dependabot `ruff` bump merged to `main` after this point is caught by step 1 (constraint 4).
- **D-08:** the `0.9.3` and `0.9.4` version numbers are both unclaimed, not decided. After this phase `## [Unreleased]` holds four bullets from two unpublished milestones: three from v0.9.3 and this phase's one. The next release-prep phase promotes all four into its versioned section, the mechanism by which Phase 63 promoted Phase 61's bullets. Whether the next published release is 0.9.3, 0.9.4 or another number belongs to that milestone's scoping.

**4. `## Before and after phase.complete-family tooling`.** Reproduce `71-CLOSEOUT-GUARD.md` § "For the operator running phase.complete" inline:
- Name both entry points: `phase.complete` reached from `/gsd-execute-phase`, and `/gsd-verify-work`'s inline transition.
- The scratch backup of the three files comes before either.
- Then the probes: `sha256sum .planning/REQUIREMENTS.md` against the baseline digest written out in full, `wc -l`, `git diff --name-only -- .planning/REQUIREMENTS.md`, and `grep -n 'REL-13' .planning/REQUIREMENTS.md` against the four baseline lines written out.
- The reversion is `git checkout -- .planning/REQUIREMENTS.md`, after which ROADMAP.md and STATE.md are diffed against the backup.
- The rule: reverted and reported, never committed, and no `/gsd-complete-milestone` step starts until the probes MATCH.
- State the history: the flip has landed at eight close-prep phases, most recently REL-12 at the Phase 69 close, caught by this third observation and reverted before any commit (`69-CLOSEOUT-GUARD.md` § "Third observation (after phase.complete, orchestrator)"; ROADMAP constraint 10).

**5. `## Fence observation`.** Record live, at this task's own time:
- `git diff --name-only -- .planning/REQUIREMENTS.md`, empty
- `sha256sum .planning/REQUIREMENTS.md`, equal to `REQ_SHA256_BASE`
- `git tag -l 'v0.9.3'` and `git tag -l 'v0.9.4'`, both empty
- `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml uv.lock .planning/REQUIREMENTS.md`, empty
- `git log --format=%H -1 -- typsphinx/ tests/`, equal to `CODE_FREEZE_ANCHOR`, and `git diff --quiet "$BASE_71_06" HEAD -- typsphinx/ tests/; echo "exit:$?"`, which must print `exit:0`, with `BASE_71_06` read from `71-SC1-INVARIANTS.md`. The ten files 71-06 hashed are the files on this tip, which carries D-11 to the final tip.

**6. `## What this phase deliberately did not do`.** A bullet list:
- no tag, local or remote
- no `release.yml` run
- nothing uploaded to PyPI
- no GitHub Release
- no version bump
- no pull request opened, merged or commented on
- no `origin/main` merged into the branch (D-05)
- no `update-pin.yml` dispatch
- no action on any open pull request (D-12)
- no REQUIREMENTS.md edit of any kind, checkbox, Traceability row or otherwise (D-02, D-09)
- no change under `typsphinx/`, `tests/`, `.github/`, `pyproject.toml` or `uv.lock`
- one push and one `ci.yml` dispatch only, in 71-04 (D-10)

The handoff contains no command that tags, creates a release or dispatches the translations pin workflow.

Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; H="$D/71-HANDOFF.md" &amp;&amp; G="$D/71-CLOSEOUT-GUARD.md" &amp;&amp; test -f "$H" &amp;&amp; k() { sed -n "s/^$1 = //p" "$G" | head -n 1; } &amp;&amp; O="$(head -n 14 "$H")" &amp;&amp; printf '%s\n' "$O" | grep -qi 'publishes nothing' &amp;&amp; for w in 'no tag' 'PyPI' 'GitHub Release' 'version bump' 'update-pin.yml' 'Read the Docs' 'stable' 'latest' 'daily' 'not applicable' 'pull request'; do printf '%s\n' "$O" | grep -qiF -- "$w" || { echo "opening lacks: $w"; exit 1; }; done &amp;&amp; [ "$(printf '%s\n' "$O" | grep -c '^##')" = 0 ] &amp;&amp; for n in 'Test Python 3.12 on ubuntu-latest' 'Test Python 3.13 on ubuntu-latest' 'Lint and Format Check' 'Type Check' 'Code Coverage' 'Build Package' 'git merge --no-ff origin/main' 'no-op' 'gh pr create --base main --head gsd/v0.9.4-typing-modernization' 'gh pr merge' '--merge' 'strict: true' '#135' '#136' '#143' 'unclaimed' '0.9.3' '0.9.4' 'Phase 63' 'constraint 4' '404' '/gsd-execute-phase' '/gsd-verify-work' 'git checkout -- .planning/REQUIREMENTS.md' 'merge-tree' '--extra dev --extra docs' '71-PREFLIGHT-EVIDENCE.md' '71-CI-EVIDENCE.md' '71-GREEN-TREE-EVIDENCE.md' '71-CHANGELOG-EVIDENCE.md' '71-SC1-INVARIANTS.md' '71-CLOSEOUT-GUARD.md' '**REL-13**: Close prep only, unpublished: one CHANGELOG bullet under the existing'; do grep -qF -- "$n" "$H" || { echo "handoff lacks: $n"; exit 1; }; done &amp;&amp; grep -qF "$(k REQ_SHA256_BASE)" "$H" &amp;&amp; PCT="$(sed -n 's/^PR_CENSUS_AT = //p' "$D/71-PREFLIGHT-EVIDENCE.md" | head -n 1)" &amp;&amp; [ -n "$PCT" ] &amp;&amp; grep -qF "$PCT" "$H" &amp;&amp; ! grep -qE 'gh release create|workflow run update-pin|git tag -a|git tag v' "$H" &amp;&amp; for s in '## What /gsd-complete-milestone does, in order' '## What this phase satisfied' '## Recorded without acting' '## Before and after phase.complete-family tooling' '## Fence observation' '## What this phase deliberately did not do'; do grep -qxF "$s" "$H" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.3')" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.4')" ] &amp;&amp; test ! -e "$D/71-VERIFICATION.md" &amp;&amp; B6="$(sed -n 's/^BASE_71_06 = //p' "$D/71-SC1-INVARIANTS.md" | head -n 1)" &amp;&amp; [ "${#B6}" = 40 ] &amp;&amp; git diff --quiet "$B6" HEAD -- typsphinx tests &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml uv.lock .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md)" ]</automated>
    <fails_when>Non-zero exit, or `opening lacks: <word>`, `handoff lacks: <text>` or `missing section: <name>` printed. Each of these makes it fail:
- the first 14 lines not stating that the milestone publishes nothing
- any of the negative items, the not-applicable pair, the daily-schedule fact or the pull request missing from those lines
- a `##` heading within those lines
- a required check name, the conditional merge and its no-op, the pull-request or merge commands, the strict reason, the three precedent PRs, the unclaimed versions, the Phase 63 mechanism, constraint 4, the 404 observation, either entry point, the reversion, the re-sync extras, an evidence citation or the verbatim REL-13 line missing
- the baseline digest or 71-05's census time absent
- a tag, release-creation or pin-dispatch command present
- a section missing
- the live REQUIREMENTS.md digest diverged, or a v0.9.3 or v0.9.4 tag locally
- `71-VERIFICATION.md` authored
- a typsphinx/ or tests/ change since `BASE_71_06`, or a product or tracking file modified</fails_when>
  </verify>
  <acceptance_criteria>
    - Lines 1 to 14 of `71-HANDOFF.md` state the negative before any `##` heading: no tag, PyPI upload, GitHub Release or version bump; `update-pin.yml` and Read the Docs `stable` not applicable; the daily schedule moving the Japanese `latest` site recorded as a fact; the REL-13 pull request as the one irreversible action (D-07).
    - `## What /gsd-complete-milestone does, in order` gives steps 0 to 8 with exact commands. They include the re-run trial merge with the constraint-4 branch, the conditional `git merge --no-ff origin/main` with its no-op case and the `strict: true` reason, and the `--extra dev --extra docs` re-sync. Then the push, `gh pr create --base main --head gsd/v0.9.4-typing-modernization`, the six required checks named literally, `gh pr merge --merge` citing #135, #136 and #143, and the five post-merge observations (D-06, SC#4).
    - `## What this phase satisfied` quotes REL-13 verbatim, notes its AMENDED block, and reports SC#1 to SC#4, each citing its evidence section. The SC#3 local, CI and trial-merge results are set side by side, and the D-10 final-tip statement is included.
    - `## Recorded without acting` carries the open-PR census with `PR_CENSUS_AT` and a live re-read (D-12), and records 0.9.3 and 0.9.4 as unclaimed with the four-bullet promotion (D-08).
    - `## Before and after phase.complete-family tooling` names both entry points and gives the backup, the digest, the REL-13 lines and the reversion inline.
    - `## Fence observation` is live, matches the Baseline, and shows the code unchanged since `BASE_71_06`; `## What this phase deliberately did not do` lists every non-action.
    - No command that tags, creates a release or dispatches the pin workflow appears anywhere in the file.
  </acceptance_criteria>
  <done>`71-HANDOFF.md` stands alone. It opens by stating that nothing is published, and it gives the operator every `/gsd-complete-milestone` step in order: the conditional branch update the protection demands, the checks to wait for, the merge, the five observations REL-13 closes on, the items left untouched, and the fence to run after the tooling.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`71-01-PLAN.md`. This plan carries P1, P2, P3 and P5, and it authors the `ordering` edge (the REL-13
grep compared line by line in file order) and the `encoding` edge (the raw-byte SHA-256 fence). It
carries residual assumptions (f) and (g) into the handoff: `merge-tree` is re-run immediately before
the PR because `origin/main` may move, and the open pull requests are reported with their measurement
times, not as standing facts. It also carries the concurrency guarantee that no plan writes
REQUIREMENTS.md, with the decisive third observation reproduced where the operator reaches it after
every plan has finished. Phase accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed + 0
dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/REQUIREMENTS.md` → `phase.complete`-family tooling after this plan | the flip lands outside every plan's reach |
| `71-HANDOFF.md` → the operator at `/gsd-complete-milestone` | read as instructions; a mis-framed step causes a real publish, a rebase, or a merge before protection is satisfied |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-31 | Elevation of Privilege | a handoff read as licence to tag, release or dispatch the pin workflow for a milestone that publishes nothing (ASVS V4, L1, by analogy) | high | mitigate | Negative-first opening gated on its first 14 lines; step 8 lists the not-applicable actions; the verify asserts no tag, release-creation or pin-dispatch command anywhere in the file. |
| T-71-32 | Tampering | the REL-13 flip landing after this plan through either transition entry point | high | mitigate | Close-time re-verification with an explicit verdict and a line-by-line grep comparison against `PHASE_BASE_SHA`, and the third observation reproduced inline with both entry points, a scratch backup, the digest and the `git checkout` reversion. |
| T-71-33 | Tampering | a rebase or squash at close rewriting the milestone's history, or a merge before protection is satisfied | medium | mitigate | Step 2 names `git merge --no-ff origin/main` conditionally and forbids a rebase; step 5 names the six required checks; step 6 names `gh pr merge --merge`; each cites measured precedent and the `strict: true` reason. |
| T-71-34 | Repudiation | a `main` that moved after the pre-flight (a dependabot `ruff` bump) merged without re-measurement | medium | mitigate | Step 1 re-runs `merge-tree` and, when `main` moved, the scratch lock check and merged-tree lint, with constraint 4 making that count authoritative. |
| T-71-35 | Repudiation | the D-11 masked-AST result not holding on the final tip | medium | mitigate | The fence observation requires `git diff --quiet BASE_71_06 HEAD` over `typsphinx/` and `tests/` and the last code commit equal to `CODE_FREEZE_ANCHOR`. |
| T-71-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins; the handoff's re-sync step does the same after any merge. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `71-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`71-HANDOFF.md` and appends `## Re-verification at phase close` to `71-CLOSEOUT-GUARD.md`, with the
keys `CLOSE_AT`, `REQ_SHA256_CLOSE`, `REQ_LINES_CLOSE` and `REQ_VERDICT_CLOSE`. It modifies no
product-tree file and never edits `.planning/REQUIREMENTS.md`, except to revert a divergence. No file
named `71-VERIFICATION.md` is produced. The orchestrator, not a plan, later appends the third
observation to `71-CLOSEOUT-GUARD.md`.
</artifacts_this_phase_produces>

<verification>
- The REL-13 digest matches the phase-head Baseline at close, the REL-13 grep is line-for-line
  identical, and REL-13 is `[ ]` and Pending.
- `71-HANDOFF.md` opens negative-first, lists the ordered close steps with the conditional
  strict-protection branch update and the five post-merge observations, records the version numbers
  and open pull requests without acting, reproduces the third observation, and closes with a live
  fence that includes the D-11 code check.
</verification>

<success_criteria>
ROADMAP SC#4: the REL-13 checkbox is proven held at phase close by the recorded SHA-256, and
`71-HANDOFF.md` is standalone. It carries the PR, the checks, the merge, the explicit no-publish
statement, both not-applicable items and the post-merge observations (D-06..D-09, D-12).
</success_criteria>

<output>
Create `.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-07-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-09).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run `phase.complete` or any other GSD state-writing verb.
- The first section states `REQ_VERDICT_CLOSE`, `CLOSE_AT` and the handoff path. It ends by reminding
  the orchestrator that the third fence observation, with a scratch backup first, is owed after
  `phase.complete` and again after `/gsd-verify-work`'s transition.
</output>
