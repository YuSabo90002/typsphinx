---
phase: 71-v0-9-4-close-prep-prep-only-unpublished
plan: 05
type: execute
wave: 2
depends_on: ["71-01", "71-02"]
files_modified:
  - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PREFLIGHT-EVIDENCE.md
autonomous: true
requirements: [REL-13]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-05: after `git fetch origin`, `git merge-tree --write-tree TRIAL_HEAD ORIGIN_MAIN_SHA` exits 0, and the tree SHA it prints is recorded as MERGE_TREE. TRIAL_HEAD is this worktree HEAD before any commit, and it carries the new CHANGELOG bullet.'
    - 'Edge (idempotency): re-running the same merge-tree command from the recorded inputs reproduces MERGE_TREE (MERGE_TREE_REPRODUCED = yes), and the verify recomputes it live.'
    - 'Edge (adjacency): when origin/main equals MILESTONE_BASE (MAIN_MOVED = no, nothing to absorb), the trial merge is a no-op: MERGE_TREE equals TRIAL_HEAD''s own tree and TRIAL_IS_NOOP = yes. When main has moved, MAIN_ONLY_COMMITS names the commits it carries, and any ruff or uv.lock change among them is flagged as constraint 4''s case.'
    - 'D-05: pyproject.toml and uv.lock are extracted from MERGE_TREE into a scratch directory outside the repository with `git archive`, and `uv lock --check` exits 0 there. The merged pyproject.toml:7 still reads `version = "0.9.2"`, and the merged uv.lock ruff version is recorded as MERGED_RUFF_LOCK_VERSION.'
    - 'D-05 / SC#3 / constraint 4: a scratch copy of MERGE_TREE is synced with `--locked` from its own uv.lock, and `ruff check .` and `black --check .` exit 0 there, the ruff that runs being MERGED_RUFF_LOCK_VERSION. A non-zero exit is a FINDING for the owner, never fixed here, and TRIAL_MERGE_VERDICT = MET only when the merge, the lock check, ruff and black all pass.'
    - 'D-05 non-committing: no merge commit is created and no ref of the branch moves. `git rev-list --merges BASE_71_05..HEAD` is empty, the merge-base of HEAD and origin/main still equals MILESTONE_BASE, and the scratch directories are removed.'
    - 'D-06 inputs: main protection is recorded live: `strict: true` and exactly six required contexts, namely Test Python 3.12 on ubuntu-latest, Test Python 3.13 on ubuntu-latest, Lint and Format Check, Type Check, Code Coverage and Build Package. The first-parent history of origin/main shows #135, #136 and #143 as Merge pull request commits.'
    - 'D-12 input: the open pull requests are read with `gh pr list --state open` at a recorded PR_CENSUS_AT, with a non-empty `--state all` listing as positive control, and DEPENDABOT_OPEN_PRS counts those opened by dependabot. No action is taken on any of them.'
    - 'REL-13 is cited for coverage only; SUMMARY requirements-completed: [] (D-09).'
  artifacts:
    - path: .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PREFLIGHT-EVIDENCE.md
      provides: 'trial merge, merged-lock check, merged-tree lint, main protection census, merge-method precedent, open-PR census, trial-merge verdict'
      contains: 'MERGE_TREE = '
  key_links:
    - from: 'TRIAL_HEAD and ORIGIN_MAIN_SHA'
      to: 'MERGE_TREE'
      via: 'git merge-tree --write-tree, which writes objects only and moves no ref'
      pattern: 'MERGE_RC = '
    - from: 'MERGE_TREE uv.lock'
      to: 'the conditional branch-update step in 71-HANDOFF.md (D-06)'
      via: 'uv lock --check in scratch and a locked scratch sync, proving the post-update lock valid before the operator reaches it'
      pattern: 'LOCK_CHECK_EXIT = '
    - from: '71-SC1-INVARIANTS.md MILESTONE_BASE'
      to: 'the no-op reading and the non-absorption check'
      via: 'KEY = value line read with sed'
      pattern: 'MILESTONE_BASE = '
  prohibitions:
    - statement: 'MUST NOT run `git merge`, `git rebase`, `git commit --amend` or any ref-moving command for the trial. It uses `git merge-tree --write-tree` only, and nothing from it is committed (D-05).'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT act on any open pull request, dependabot or otherwise: no merge, close, comment or rebase request (D-12).'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT copy the tree SHA, lock result or protection list from CONTEXT or RESEARCH; every value comes from a command run here. MUST NOT install any package outside a committed uv.lock.'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Measure, without committing anything to the branch, that the merge `/gsd-complete-milestone` may run
is safe (D-05): a merge of `origin/main` into the milestone branch is conflict-free, the merged
`uv.lock` is valid, and the merged tree lints cleanly at the merged lock's `ruff` version. Today
`origin/main` equals the merge-base, so the trial merge is expected to be a no-op, and it still runs.
It catches the case constraint 4 names: a `main` that moved, for instance with a dependabot `ruff`
bump, between planning and execution. The plan also records the live facts the handoff's
branch-update and merge steps depend on:
- `main`'s strict protection and its six required checks (D-06)
- the merge-commit method prior milestone PRs used, #135, #136 and #143 (D-06)
- the open pull requests at census time, left untouched (D-12)

Purpose: `main`'s protection is `strict: true`, so the REL-13 PR cannot merge until the branch is up
to date. This plan makes the conditional update step known-good before the operator reaches it.
Because the trial merge must include the CHANGELOG edit, it runs in wave 2, after 71-01, in parallel
with 71-03 and 71-04 and sharing no file with either.

Output: `71-PREFLIGHT-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-RESEARCH.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md
@.planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PREFLIGHT-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run project tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Scratch directories.** Parallel executors share the session scratchpad. Create one parent with
`mktemp -d`, record it as `SCRATCH_71_05`, and put every scratch tree, archive and captured output
under it with a `p7105_` prefix. Remove each scratch tree with `rm -rf` when its step finishes.

**Invoking uv on a scratch directory.** Run every `uv` command from the worktree root and point it at
the scratch tree with `uv --directory <scratch> …`. The `uv` shim resolves this worktree's own
`.venv/bin/uv` from the current directory, inside the FHS sandbox. Run from inside the scratch
directory, the shim would fall back to nixpkgs' `uv`.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line. The verify fails on any key line containing an opening parenthesis. Phase 69's executor
appended explanations to five values although its plan said not to.

**Locale.** The host runs `LANG=ja_JP.UTF-8`; prefix `LC_ALL=C` to any `git` command whose message you
quote for comparison.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
`## HALT` is written only on a failure path; the gates require its absence.

## Planning-time census (2026-09-13, main checkout at `d8679b01`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `origin/main` against the milestone base | `origin/main` = `main` = `d14ca458fd8cd6cd1374fb9de3b5f45d3a17cc2d` = `git merge-base HEAD origin/main`; the branch lacks no `main` commit | `git rev-parse`, `git merge-base` |
| Trial merge expectation | with `origin/main` equal to the merge-base, `git merge-tree --write-tree` returns HEAD's own tree with exit 0 | `git merge-tree` semantics |
| Branch lock | ruff 0.16.6 | `uv.lock` |
| `main` protection | `strict: true`; contexts `Test Python 3.12 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`, `Test Python 3.13 on ubuntu-latest` | `gh api repos/YuSabo90002/typsphinx/branches/main/protection` |
| Merge-method precedent | `9db2274c` (#135), `45962faa` (#136) and `58d578f2` (#143), all `Merge pull request` commits on `origin/main`'s first-parent history | `git log --first-parent` |
| Open PRs | none; dependabot runs on the `uv` ecosystem since v0.9.3 and can open `ruff` bumps against `main` | `gh pr list --state open`; ROADMAP constraint 4 |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: fetch, non-committing trial merge of origin/main reproduced from its inputs, and the merged lock checked in scratch</name>
  <precondition>71-01 and 71-02 are merged into this worktree's HEAD: the `CHANGELOG.md` region between `## [Unreleased]` and `## [0.9.2]` holds four bold-lead bullets, and `71-SC1-INVARIANTS.md` carries `MILESTONE_BASE`. Otherwise write `## HALT: wave 1 not merged` and stop.</precondition>
  <reversibility rating="reversible">`git merge-tree --write-tree` writes unreferenced objects only; no ref moves and the scratch directory is deleted.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PREFLIGHT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-RESEARCH.md § "Pattern 3: Non-committing trial merge" and § "Don't Hand-Roll"
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-05 and D-06
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PREFLIGHT-EVIDENCE.md (the transcript shape to mirror)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md (`MILESTONE_BASE`)
    - pyproject.toml lines 33-55 and uv.lock (the branch's own dev-extra ranges and ruff pin, for comparison)
    - .planning/ROADMAP.md binding constraint 4 of the v0.9.4 section
  </read_first>
  <action>
Create `71-PREFLIGHT-EVIDENCE.md` with the heading `# Phase 71 — REL-13 Update-Step Pre-Flight (D-05, non-committing)`. Open it with one paragraph stating that nothing in this file is committed to the branch except the file itself, and that no merge is performed.

1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line
   - `uv --version`
   - before any commit: `BASE_71_05 = ` and `TRIAL_HEAD = `, both from `git rev-parse HEAD`
   - `START_CANONICAL = ` from `git rev-parse gsd/v0.9.4-typing-modernization`
   - `SCRATCH_71_05 = ` followed by the path `mktemp -d` printed

2. **`## Fetch and divergence`.** Run `git fetch origin`, D-05's own command, and record:
   - `ORIGIN_MAIN_SHA = ` from `git rev-parse origin/main`
   - `git log --oneline "$TRIAL_HEAD".."$ORIGIN_MAIN_SHA"`, and the count of those commits, key `MAIN_ONLY_COMMITS`
   - `git rev-list --left-right --count "$ORIGIN_MAIN_SHA"...HEAD`
   - `git merge-base HEAD "$ORIGIN_MAIN_SHA"`, which must equal `MILESTONE_BASE` from `71-SC1-INVARIANTS.md`
   - `MAIN_MOVED = no` when `ORIGIN_MAIN_SHA` equals `MILESTONE_BASE`, `yes` otherwise

   If `main` moved, name each commit it carries, and state whether any changes `uv.lock` or the `ruff` pin in `pyproject.toml`. That is constraint 4's case, a dependabot `ruff` bump, and Task 2's lint count on the merged tree is then authoritative.

3. **`## Trial merge`.** Record `git merge-tree --write-tree "$TRIAL_HEAD" "$ORIGIN_MAIN_SHA"; echo "exit:$?"` verbatim.
   - Key `MERGE_RC` is the exit code. Key `MERGE_TREE` is the first output line, the tree SHA.
   - On a non-zero exit, quote the conflict listing, write `## HALT: trial merge is not clean`, and stop. The handoff's update step would not be conflict-free, and that decision belongs to the owner.
   - Run the same command a second time and record `MERGE_TREE_REPRODUCED = yes` when it prints the same tree (edge: idempotency).
   - Record `git rev-parse "$TRIAL_HEAD^{tree}"`, and `TRIAL_IS_NOOP = yes` when it equals `MERGE_TREE`, `no` otherwise. With `MAIN_MOVED = no` it must be `yes`: `origin/main` only touches the branch at the merge-base, so there is nothing to absorb (D-05, edge: adjacency).
   - State that this tree SHA is valid only for these two inputs and must be re-measured immediately before any real merge.

4. **`## Merged lock`.**
   - Create a scratch tree under `SCRATCH_71_05` and record its path.
   - Write `pyproject.toml` and `uv.lock` from `MERGE_TREE` into an archive file under `SCRATCH_71_05` with `git archive --format=tar -o <archive> "$MERGE_TREE" pyproject.toml uv.lock`, then extract it into the scratch tree with `tar -xf <archive> -C <scratch tree>`.
   - From the worktree root, record `uv --directory <scratch tree> lock --check; echo "exit:$?"` verbatim. Keys: `LOCK_CHECK_EXIT`, which must be 0, and `LOCK_CHECK_UV_VERSION`, from `uv --version`.
   - Record the merged facts, each as a command with its output:
     - `sed -n 7p <scratch tree>/pyproject.toml`, key `MERGED_VERSION_LINE`, which must be `version = "0.9.2"`
     - `grep -nE 'tox-uv|"ruff' <scratch tree>/pyproject.toml`
     - `grep -A1 -xF 'name = "ruff"' <scratch tree>/uv.lock`, key `MERGED_RUFF_LOCK_VERSION`, the version only
     - the same pair against this branch's own `uv.lock`, key `BRANCH_RUFF_LOCK_VERSION`
     - the bold-bullet count of the `CHANGELOG.md` region in `git show "$MERGE_TREE:CHANGELOG.md"`, key `MERGED_CHANGELOG_BOLD = 4`
   - Remove the scratch tree.

5. **`## Nothing moved`.** Record each of these:
   - `git rev-parse HEAD`, still equal to `TRIAL_HEAD` before this plan's commit
   - `git rev-parse gsd/v0.9.4-typing-modernization`, still equal to `START_CANONICAL` unless the orchestrator advanced it, in which case record both
   - `git status --porcelain`, which lists only this evidence file
   - `git merge-base HEAD "$ORIGIN_MAIN_SHA"`, still equal to `MILESTONE_BASE` (D-05)

   State that `merge-tree` added only unreferenced objects to the object store.

6. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; F="$D/71-PREFLIGHT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_71_05)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; TH="$(k TRIAL_HEAD)" &amp;&amp; [ "$TH" = "$B" ] &amp;&amp; OM="$(k ORIGIN_MAIN_SHA)" &amp;&amp; [ "${#OM}" = 40 ] &amp;&amp; git cat-file -e "$OM^{commit}" &amp;&amp; MB="$(k MILESTONE_BASE "$D/71-SC1-INVARIANTS.md")" &amp;&amp; [ "${#MB}" = 40 ] &amp;&amp; [ "$(git merge-base "$TH" "$OM")" = "$MB" ] &amp;&amp; [ "$(k MERGE_RC)" = 0 ] &amp;&amp; MT="$(git merge-tree --write-tree "$TH" "$OM")" &amp;&amp; [ "$MT" = "$(k MERGE_TREE)" ] &amp;&amp; [ "$(k MERGE_TREE_REPRODUCED)" = yes ] &amp;&amp; { [ "$OM" != "$MB" ] || { [ "$MT" = "$(git rev-parse "$TH^{tree}")" ] &amp;&amp; [ "$(k TRIAL_IS_NOOP)" = yes ] &amp;&amp; [ "$(k MAIN_MOVED)" = no ]; }; } &amp;&amp; PP="$(git show "$MT:pyproject.toml")" &amp;&amp; [ "$(printf '%s\n' "$PP" | sed -n 7p)" = 'version = "0.9.2"' ] &amp;&amp; UL="$(git show "$MT:uv.lock")" &amp;&amp; RV="$(printf '%s\n' "$UL" | grep -A1 -xF 'name = "ruff"' | sed -n 's/^version = "\(.*\)"$/\1/p')" &amp;&amp; [ -n "$RV" ] &amp;&amp; [ "$(k MERGED_RUFF_LOCK_VERSION)" = "$RV" ] &amp;&amp; MC="$(git show "$MT:CHANGELOG.md")" &amp;&amp; [ "$(printf '%s\n' "$MC" | awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' | grep -E '^- \*\*' | wc -l)" = 4 ] &amp;&amp; [ "$(k MERGED_CHANGELOG_BOLD)" = 4 ] &amp;&amp; S="$(mktemp -d)" &amp;&amp; X="$(mktemp)" &amp;&amp; git archive --format=tar -o "$X" "$MT" pyproject.toml uv.lock &amp;&amp; tar -xf "$X" -C "$S" &amp;&amp; uv --directory "$S" lock --check &amp;&amp; rm -rf "$S" "$X" &amp;&amp; [ "$(k LOCK_CHECK_EXIT)" = 0 ] &amp;&amp; [ -z "$(git rev-list --merges "$B"..HEAD)" ] &amp;&amp; [ "$(git merge-base HEAD "$OM")" = "$MB" ] &amp;&amp; for s in '## Head check and provisioning' '## Fetch and divergence' '## Trial merge' '## Merged lock' '## Nothing moved'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; DN="$(git diff --name-only "$B")" &amp;&amp; ! printf '%s\n' "$DN" | grep -vxF -e "$F" -e "$D/71-05-SUMMARY.md" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_71_05` missing, or `TRIAL_HEAD` differing from it
- `ORIGIN_MAIN_SHA` not a commit, or `MILESTONE_BASE` unreadable
- the merge-base of the two inputs differing from `MILESTONE_BASE`
- `MERGE_RC` not 0, the live `merge-tree` exiting non-zero, or printing a tree other than `MERGE_TREE`
- `MERGE_TREE_REPRODUCED` not yes
- with `origin/main` still at the milestone base, the merge tree differing from HEAD's own tree, or `TRIAL_IS_NOOP` or `MAIN_MOVED` keys contradicting that
- the merged `pyproject.toml:7` not 0.9.2
- the merged ruff version empty or differing from its key
- the merged CHANGELOG lacking the four bullets
- the live `uv lock --check` failing, or `LOCK_CHECK_EXIT` not 0
- a merge commit on the branch since the base, or origin/main absorbed
- another tracked file changed
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Fetch and divergence` records `ORIGIN_MAIN_SHA`, `MAIN_ONLY_COMMITS`, the merge-base equal to `MILESTONE_BASE`, and `MAIN_MOVED`.
    - `## Trial merge` records the verbatim `git merge-tree --write-tree` output with `MERGE_RC = 0`, `MERGE_TREE`, `MERGE_TREE_REPRODUCED = yes` and `TRIAL_IS_NOOP`, and states that the SHA must be re-measured before any real merge.
    - `## Merged lock` records the scratch path, `LOCK_CHECK_EXIT = 0` with `LOCK_CHECK_UV_VERSION`, `MERGED_VERSION_LINE`, `MERGED_RUFF_LOCK_VERSION`, `BRANCH_RUFF_LOCK_VERSION` and `MERGED_CHANGELOG_BOLD = 4`.
    - `## Nothing moved` shows HEAD and the canonical ref unmoved by the trial, only the evidence file in `git status`, and the merge-base with `origin/main` still `MILESTONE_BASE`.
  </acceptance_criteria>
  <done>The merge `/gsd-complete-milestone` may run is measured conflict-free and reproducible, with a valid merged lock, and the branch is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Merged-tree lint at the merged lock's versions, main's protection and merge-method precedent, and the open-PR census</name>
  <precondition>Task 1 is committed with `MERGE_RC = 0`, `LOCK_CHECK_EXIT = 0` and no HALT heading.</precondition>
  <reversibility rating="reversible">A scratch directory outside the repository, and read-only GitHub calls.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PREFLIGHT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PREFLIGHT-EVIDENCE.md (Task 1: `MERGE_TREE`, `MERGED_RUFF_LOCK_VERSION`, `MAIN_MOVED`)
    - tox.ini (the `lint` environment's two commands, which this task reproduces on the merged tree)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-05, D-06 (the six checks and the #135/#136/#143 precedent) and D-12
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PREFLIGHT-EVIDENCE.md § "Merged-tree lint" and § "main protection (D-08)" (the shape to mirror)
  </read_first>
  <action>
1. Re-run the head check, then `git fetch origin`.

2. **`## Merged-tree lint`.** D-05 and SC#3 require this: the trial merge must pass `ruff check .` on the merged tree, at the merged lock's `ruff`. Under constraint 4 that count is authoritative if `main` moved.
   - Create a second scratch tree under `SCRATCH_71_05`. Archive the whole `MERGE_TREE` into a file under `SCRATCH_71_05` with `git archive --format=tar -o <archive> "$MERGE_TREE"`, and extract it into the scratch tree.
   - Run `uv --directory <scratch tree> sync --locked --extra dev --no-install-project` from the worktree root. This is a hash-verified install from the merged `uv.lock` only.
   - Record `uv --directory <scratch tree> run --no-sync ruff --version`, key `MERGED_RUFF_RUN_VERSION`, which must equal `MERGED_RUFF_LOCK_VERSION`.
   - Record `uv --directory <scratch tree> run --no-sync ruff check .; echo "exit:$?"` verbatim, key `MERGED_RUFF_EXIT`.
   - Record `uv --directory <scratch tree> run --no-sync black --check .; echo "exit:$?"` verbatim, key `MERGED_BLACK_EXIT`.
   - Remove the scratch tree.
   - If either exit is non-zero, write a `## FINDING: merged-tree lint` section quoting each finding, write `TRIAL_MERGE_VERDICT = NOT-MET`, and return a blocking checkpoint naming the finding. Do not fix anything: a fix is a code change, which SC#1 forbids in this phase, and it would force a second CI dispatch (D-10).

3. **`## main protection (D-06)`.** Record `gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '.required_status_checks'` verbatim, with the keys:
   - `PROTECTION_STRICT`, which must be `true`
   - `PROTECTION_CONTEXTS_COUNT`, which must be `6`

   List the six context names exactly as printed. State why the handoff's branch-update step is conditional (D-06): under `strict: true` the PR cannot merge until the branch is up to date with `main`, so if `main` has moved by then it is merged in, and otherwise the step is a recorded no-op.

4. **`## Merge-method precedent (D-06)`.** Record `git log --first-parent --format='%h %s' origin/main`, filtered to the lines naming `Merge pull request #135`, `#136` and `#143`, key `MERGE_PRECEDENT_HITS = 3`, and `git log --first-parent --format='%h %s' -8 origin/main`. State that prior milestone PRs merged with merge commits, never squash or rebase.

5. **`## Open pull requests (D-12)`.**
   - Record `gh pr list --state all --limit 5 --json number`, which must be non-empty: the positive control.
   - Record `gh pr list --state open --json number,title,author,headRefName,baseRefName,updatedAt` verbatim.
   - Keys: `OPEN_PRS`, the count; `DEPENDABOT_OPEN_PRS`, the count whose author login is `app/dependabot`; and `PR_CENSUS_AT`, the `date -u +%FT%TZ` of the census.
   - If any pull request is open, tabulate it in a row starting `| #<n> |`, with title, head, base and updatedAt.
   - State that no merge, close, comment or rebase request was made (D-12). State that a dependabot `ruff` bump merged to `main` after this census is caught by the handoff's re-run of the trial merge.

6. **`## What the handoff can rely on`.** Summarise in one table, citing each key above:
   - conflict-free merge, and whether it is a no-op
   - valid merged lock
   - the merged ruff version and the merged-tree lint result
   - strict protection with its six checks
   - the merge-commit method
   - the PR census time and result

   Then write `TRIAL_MERGE_VERDICT = MET` only when `MERGE_RC`, `LOCK_CHECK_EXIT`, `MERGED_RUFF_EXIT` and `MERGED_BLACK_EXIT` are all 0.

7. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; F="$D/71-PREFLIGHT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_71_05)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; MT="$(k MERGE_TREE)" &amp;&amp; [ -n "$MT" ] &amp;&amp; S2="$(mktemp -d)" &amp;&amp; X="$(mktemp)" &amp;&amp; git archive --format=tar -o "$X" "$MT" &amp;&amp; tar -xf "$X" -C "$S2" &amp;&amp; uv --directory "$S2" sync --locked --extra dev --no-install-project -q &amp;&amp; [ "$(uv --directory "$S2" run --no-sync ruff --version | cut -d' ' -f2)" = "$(k MERGED_RUFF_LOCK_VERSION)" ] &amp;&amp; [ "$(k MERGED_RUFF_RUN_VERSION)" = "$(k MERGED_RUFF_LOCK_VERSION)" ] &amp;&amp; RX="$(uv --directory "$S2" run --no-sync ruff check . &gt;/dev/null 2&gt;&amp;1; echo $?)" &amp;&amp; BX="$(uv --directory "$S2" run --no-sync black --check . &gt;/dev/null 2&gt;&amp;1; echo $?)" &amp;&amp; rm -rf "$S2" "$X" &amp;&amp; [ "$RX" = 0 ] &amp;&amp; [ "$BX" = 0 ] &amp;&amp; [ "$(k MERGED_RUFF_EXIT)" = 0 ] &amp;&amp; [ "$(k MERGED_BLACK_EXIT)" = 0 ] &amp;&amp; [ "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '.required_status_checks.strict')" = true ] &amp;&amp; [ "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '.required_status_checks.contexts | sort | join("|")')" = 'Build Package|Code Coverage|Lint and Format Check|Test Python 3.12 on ubuntu-latest|Test Python 3.13 on ubuntu-latest|Type Check' ] &amp;&amp; [ "$(k PROTECTION_STRICT)" = true ] &amp;&amp; [ "$(k PROTECTION_CONTEXTS_COUNT)" = 6 ] &amp;&amp; git fetch -q origin main &amp;&amp; FP="$(git log --first-parent --format='%h %s' origin/main)" &amp;&amp; [ -n "$FP" ] &amp;&amp; [ "$(printf '%s\n' "$FP" | grep -E ' Merge pull request #(135|136|143) ' | wc -l)" = 3 ] &amp;&amp; [ "$(k MERGE_PRECEDENT_HITS)" = 3 ] &amp;&amp; [ "$(gh pr list --state all --limit 5 --json number --jq 'length')" -ge 1 ] &amp;&amp; [ -n "$(k PR_CENSUS_AT)" ] &amp;&amp; case "$(k OPEN_PRS)" in ''|*[!0-9]*) false ;; *) true ;; esac &amp;&amp; case "$(k DEPENDABOT_OPEN_PRS)" in ''|*[!0-9]*) false ;; *) true ;; esac &amp;&amp; [ "$(k TRIAL_MERGE_VERDICT)" = MET ] &amp;&amp; for s in '## Merged-tree lint' '## main protection (D-06)' '## Merge-method precedent (D-06)' '## Open pull requests (D-12)' '## What the handoff can rely on'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; DN="$(git diff --name-only "$B")" &amp;&amp; ! printf '%s\n' "$DN" | grep -vxF -e "$F" -e "$D/71-05-SUMMARY.md" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- `MERGE_TREE` missing, or the locked scratch sync failing
- the running ruff version differing from the merged lock's, or its key wrong
- the live ruff or black run on the merged tree exiting non-zero, or its recorded exit not 0
- `main` protection not strict, its contexts not exactly the six names, or the protection keys wrong
- the #135/#136/#143 merge-commit precedent not all three found, or its key wrong
- the pull-request listing empty (positive control), the census time missing, or a PR count not a number
- `TRIAL_MERGE_VERDICT` not MET
- a section missing, or another tracked file changed
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Merged-tree lint` records a locked scratch sync from the merged `uv.lock`, `MERGED_RUFF_RUN_VERSION` equal to `MERGED_RUFF_LOCK_VERSION`, and the verbatim `ruff check .` and `black --check .` outputs with `MERGED_RUFF_EXIT = 0` and `MERGED_BLACK_EXIT = 0`. A non-zero exit would carry a `## FINDING: merged-tree lint` section, and nothing is fixed.
    - `## main protection (D-06)` records `PROTECTION_STRICT = true`, `PROTECTION_CONTEXTS_COUNT = 6` and the six names verbatim.
    - `## Merge-method precedent (D-06)` records `MERGE_PRECEDENT_HITS = 3`.
    - `## Open pull requests (D-12)` records the positive control, the verbatim open-PR listing, `OPEN_PRS`, `DEPENDABOT_OPEN_PRS` and `PR_CENSUS_AT`, and states that no action was taken.
    - `## What the handoff can rely on` summarises every key, and `TRIAL_MERGE_VERDICT = MET`.
  </acceptance_criteria>
  <done>Every fact the handoff's branch-update and merge steps depend on is measured live: the merged-tree lint at the merged versions, strict protection with its six checks, the merge-commit method, and the open pull requests left untouched.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`71-01-PLAN.md`. This plan carries P1, P5 and P6, and it authors the `adjacency` edge (the no-op
merge when `origin/main` sits at the merge-base) and the `idempotency` edge (the reproduced tree).
It records the inputs for residual assumptions (f) and (g):
- `ORIGIN_MAIN_SHA` and `MERGE_TREE` hold only for this moment, so the handoff re-runs `merge-tree`
  immediately before the PR.
- The open-PR state holds only at `PR_CENSUS_AT`, so the handoff re-reads it.

Phase accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trial merge → branch refs | the pre-flight must measure the merge without creating it |
| merged `uv.lock` → scratch environment | packages are installed from a lock that may not be on this branch yet |
| worktree → GitHub API (read-only) | protection, pull-request and history reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-22 | Tampering | the pre-flight creating a merge commit or moving HEAD or the canonical ref, which would absorb `main` against D-05 | high | mitigate | Only `git merge-tree --write-tree` is used; `## Nothing moved` records HEAD and the canonical ref; the verify requires no merge commit since the base and a merge-base with `origin/main` still equal to `MILESTONE_BASE`. |
| T-71-23 | Tampering | installing packages outside a committed lock in the scratch lint (ASVS V14, L1) | medium | mitigate | The scratch environment is created with `uv sync --locked` from the merged `uv.lock`, which is hash-pinned; nothing is fetched with an ad-hoc spec. |
| T-71-24 | Repudiation | a stale tree SHA, lock result or protection list copied from CONTEXT or RESEARCH | medium | mitigate | A fresh `git fetch origin`, `ORIGIN_MAIN_SHA` recorded, and the verify recomputes `merge-tree` from the recorded inputs, re-runs `uv lock --check`, ruff and black live, and re-reads the protection. |
| T-71-25 | Elevation of Privilege | a census call becoming an action on an open pull request (D-12) | medium | mitigate | Only `gh pr list` is used; the prohibition names merge, close, comment and rebase request. |
| T-71-SC | Tampering | package installs | low | accept | The worktree sync and the scratch sync both install from committed locks only; no package is added to this branch. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `71-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`71-PREFLIGHT-EVIDENCE.md`, with the keys `BASE_71_05`, `TRIAL_HEAD`, `START_CANONICAL`,
`SCRATCH_71_05`, `ORIGIN_MAIN_SHA`, `MAIN_ONLY_COMMITS`, `MAIN_MOVED`, `MERGE_RC`, `MERGE_TREE`,
`MERGE_TREE_REPRODUCED`, `TRIAL_IS_NOOP`, `LOCK_CHECK_EXIT`, `LOCK_CHECK_UV_VERSION`,
`MERGED_VERSION_LINE`, `MERGED_RUFF_LOCK_VERSION`, `BRANCH_RUFF_LOCK_VERSION`,
`MERGED_CHANGELOG_BOLD`, `MERGED_RUFF_RUN_VERSION`, `MERGED_RUFF_EXIT`, `MERGED_BLACK_EXIT`,
`PROTECTION_STRICT`, `PROTECTION_CONTEXTS_COUNT`, `MERGE_PRECEDENT_HITS`, `OPEN_PRS`,
`DEPENDABOT_OPEN_PRS`, `PR_CENSUS_AT` and `TRIAL_MERGE_VERDICT`. It also creates scratch
directories outside the repository, all removed. It modifies no product-tree file, creates no commit
other than its evidence, and moves no ref. No file named `71-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- The trial merge of `origin/main` is clean and reproducible from the recorded inputs, a no-op while
  `main` sits at the merge-base, and the merged lock passes `uv lock --check`.
- The merged tree passes `ruff check .` and `black --check .` at the merged versions.
- `main`'s strict protection, its six checks, the three merge-commit precedents and the open pull
  requests are recorded live.
- Nothing was merged into, or moved on, the branch.
</verification>

<success_criteria>
The trial-merge half of ROADMAP SC#3 is met without committing anything to the branch (D-05), and
the facts D-06 and D-12 need are measured, so the handoff's conditional update step is known
conflict-free with a valid lock and a clean lint.
</success_criteria>

<output>
Create `.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-05-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-09).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run any GSD state-writing verb.
- The first section states `ORIGIN_MAIN_SHA`, `MAIN_MOVED`, `MERGE_RC`, `MERGE_TREE`,
  `TRIAL_IS_NOOP`, `LOCK_CHECK_EXIT`, `MERGED_RUFF_LOCK_VERSION`, `MERGED_RUFF_EXIT`,
  `MERGED_BLACK_EXIT`, `PROTECTION_STRICT`, the PR census and `TRIAL_MERGE_VERDICT`. If a FINDING or
  HALT section was written, it says so first.
</output>
