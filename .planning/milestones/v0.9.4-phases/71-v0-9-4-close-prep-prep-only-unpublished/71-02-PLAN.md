---
phase: 71-v0-9-4-close-prep-prep-only-unpublished
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md
  - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md
  - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/COVERAGE.md
autonomous: true
requirements: [REL-13]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'D-09 / SC#4 baseline at phase head: 71-CLOSEOUT-GUARD.md records PHASE_BASE_SHA, REQ_SHA256_BASE, REQ_LINES_BASE, REL13_HITS_BASE and GUARD_AT as bare key lines before this plan commits anything, plus the verbatim `grep -n REL-13 .planning/REQUIREMENTS.md` output with each hit classified as state-bearing or informational. The recorded digest equals the live `sha256sum`.'
    - 'D-09: PHASE_BASE_SHA lies after D-02''s AMENDED commit: the commit that added "AMENDED 2026-09-13 (Phase 71 discussion" to REQUIREMENTS.md is recorded as AMENDED_COMMIT and is an ancestor of PHASE_BASE_SHA.'
    - 'D-09: the guard names both entry points at which a release requirement has flipped, `phase.complete` reached from `/gsd-execute-phase` and the inline transition of `/gsd-verify-work`. It instructs a scratch backup of REQUIREMENTS.md, ROADMAP.md and STATE.md before either runs, and prescribes `git checkout -- .planning/REQUIREMENTS.md` as the only response to a divergence: reverted and reported, never committed.'
    - 'REL-13 is measurably unmet at phase head: its checkbox reads `- [ ] **REL-13**` and its Traceability row reads `| REL-13 | Phase 71 | Pending`. This plan reads REQUIREMENTS.md and never edits it (D-02).'
    - 'SC#1, observation 1 of 2, every remote negative paired with a positive control against v0.9.2 (D-11): pyproject.toml:7 reads `version = "0.9.2"`. No v0.9.3 or v0.9.4 tag exists locally or on origin, while `refs/tags/v0.9.2` appears once in the same unfiltered ls-remote listing. PyPI answers 200 for 0.9.2 and 404 for 0.9.3 and 0.9.4. The latest GitHub release is v0.9.2 and none is tagged v0.9.3 or v0.9.4, release.yml run 33318905691 is listed and no run since 2026-08-31 exists, and no pull request has the milestone branch as its head.'
    - 'Milestone fences: since MILESTONE_BASE (the merge-base of HEAD and origin/main) no commit changes a `version = ` line in pyproject.toml, nothing under .github/ changed (constraint 12), and the code diff lists exactly Phase 70''s ten converted files.'
    - 'D-11 part 1, observation 1: the last commit touching typsphinx/ or tests/ is CODE_FREEZE_ANCHOR e721ff899a981eafdad696ef1a9c93aaab41ece5, and the diff from it to HEAD over typsphinx/ and tests/ is empty. The same pathspec lists ten files across Phase 70''s own range (PHASE_BASE_SHA 697a1132 to the anchor), which is the positive control. The anchor carries the same code as Phase 70''s CI-tested PUSHED_SHA.'
    - 'Edge (empty): no negative probe in 71-SC1-INVARIANTS.md is recorded without a positive control from the same source, so an empty result is distinguishable from a broken probe.'
    - 'COVERAGE.md opens with `No external API integration: ` and a reason, records the detector result verbatim, and `check api-coverage.verify-pre` reports `"passed": true` for this phase directory.'
    - 'REL-13 is cited for coverage only; this plan SUMMARY declares requirements-completed: [].'
  artifacts:
    - path: .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md
      provides: 'phase-head REQUIREMENTS.md digest, line count, REL-13 hits, PHASE_BASE_SHA, AMENDED_COMMIT, and the close-time and post-transition procedures'
      contains: 'PHASE_BASE_SHA = '
    - path: .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md
      provides: 'fence observation 1 of 2 with positive controls, the milestone fences, and D-11 part 1 at phase head'
      contains: 'OBS1_AT = '
    - path: .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/COVERAGE.md
      provides: 'reasoned no-external-API declaration for the seal-time gate'
      contains: 'No external API integration: '
  key_links:
    - from: '.planning/REQUIREMENTS.md at phase head'
      to: '71-07 close-time re-verification and the operator third observation reproduced in 71-HANDOFF.md'
      via: 'REQ_SHA256_BASE compared byte for byte; a flip leaves wc -l unchanged, so the digest is the primary probe'
      pattern: 'REQ_SHA256_BASE = '
    - from: 'PHASE_BASE_SHA'
      to: '71-03 product-tree delta and 71-06 phase-scoped diff'
      via: 'KEY = value line read with sed'
      pattern: 'PHASE_BASE_SHA = '
    - from: 'MILESTONE_BASE and CODE_FREEZE_ANCHOR'
      to: '71-03, 71-04, 71-05 and 71-06 D-05 and D-11 checks'
      via: 'KEY = value lines read with sed'
      pattern: 'CODE_FREEZE_ANCHOR = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action. Every command in this plan is a read; it creates no tag, release, pull request, push or workflow run.'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record REL-13 as satisfied: no checkbox flip, no Traceability status change, no prose claiming the merge happened. REQUIREMENTS.md is read and quoted, never edited (D-02, D-09).'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record a negative probe without its positive control, or copy a digest, SHA or count from RESEARCH, CONTEXT or this plan as a measurement.'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Record, at phase head and before any other plan's commit reaches the branch, the anchors this phase's
fence proof needs:
- the `.planning/REQUIREMENTS.md` checksum guard for REL-13 (D-09), taken after D-02's AMENDED commit
- the phase base SHA
- the first of two separated SC#1 observations that nothing is published, each remote negative
  paired with a positive control against `v0.9.2` (D-11)
- the milestone fences, and D-11 part 1 at phase head: no code change since Phase 70's final code
  commit, against a positively controlled anchor
- the reasoned external-API declaration the seal-time gate reads

Purpose: a checksum recorded after another plan has run cannot prove the file was untouched at
phase head, and a fence observation taken beside its sibling is one observation, not two. This plan
is deliberately thin and early. It runs in parallel with 71-01, which touches only `CHANGELOG.md`
and its own evidence file.

Output: `71-CLOSEOUT-GUARD.md`, `71-SC1-INVARIANTS.md` (observation 1 of 2) and `COVERAGE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-RESEARCH.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Before running any command, provision this worktree with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`, then
prefix project tooling with `uv run` (`CLAUDE.md` § "Worktree-isolated execution"). This plan runs no
test suite, but the rule is unconditional, and the interpreter matches every other plan of the phase.

**Evidence key lines.** Every `KEY = value` line carries the bare value only, with any explanation on
the next line. The verify commands read values with `sed -n "s/^KEY = //p"` and compare them
exactly, and they fail on any key line that contains an opening parenthesis. Phase 69's executor
wrote explanations after values on five keys even though its plan said not to.

**Scratch files.** Parallel executors share the session scratchpad. Put every helper file and
captured output under your own `mktemp -d` directory, recorded as `SCRATCH_71_02`.

**Locale.** The host runs `LANG=ja_JP.UTF-8`. Prefix `LC_ALL=C` to any command whose English
message you quote for comparison.

**GSD tooling is not tracked.** `.claude/gsd-core/` is absent from a worktree. Call it by absolute
path: `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs` and
`node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/lib/api-coverage.cjs`.
</worktree_provisioning>

<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: v0.9.4 -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[34] -->
<!-- planner-discipline-allow: capability -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
The version tags are named because every probe asserts their absence. The word `capability` is named
because the declaration must not be a capability matrix. `## HALT` is written only on a failure path.

## Planning-time census (2026-09-13, main checkout at `d8679b01`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `.planning/REQUIREMENTS.md` | `sha256` `4d98e0287552d2dce8f45b7939dfcb0e729523c6869bfa1cd2d1d041119c5636`, 80 lines | `sha256sum`, `wc -l` |
| `grep -n 'REL-13'` hits | 4. State-bearing: `:24` checkbox and `:71` Traceability row (`\| REL-13 \| Phase 71 \| Pending — coverage only; …`). Informational: `:33` (AMENDED-block prose) and `:75` (the "Mapped to phases" coverage line) | `grep -n` |
| AMENDED commit | `5292a85b` (`docs(71): capture phase context`) added REL-13's AMENDED block | `git log -S` |
| Milestone base | `git merge-base HEAD origin/main` = `d14ca458fd8cd6cd1374fb9de3b5f45d3a17cc2d` = `origin/main` = `main`; the branch lacks no `main` commit | `git merge-base`, `git rev-parse` |
| Milestone code diff | exactly ten files: `tests/conftest.py`, `tests/test_bundle_layout_sweep_gate.py`, `tests/test_include_edge_derivation_unit.py`, `tests/test_include_ledger_removal_gate.py`, `typsphinx/__init__.py`, `typsphinx/builder.py`, `typsphinx/template_engine.py`, `typsphinx/template_registry.py`, `typsphinx/translator.py`, `typsphinx/writer.py`; `.github/` unchanged | `git diff --name-only` |
| D-11 anchor | `git log --format='%H %s' -1 -- typsphinx/ tests/` = `e721ff899a981eafdad696ef1a9c93aaab41ece5 chore: merge executor worktree (…)`; the diff from it to HEAD over those paths is empty; Phase 70's `PHASE_BASE_SHA` is `697a113221a8a267d7e8c6dd1f2b95672f9454d2`; Phase 70's CI-tested `PUSHED_SHA` `e70e31fb…` carries identical code | `git log`, `git diff --quiet` |
| Local tags | `v0.9.0`, `v0.9.2`; no `v0.9.3`, no `v0.9.4` | `git tag -l 'v0.9*'` |
| `release.yml` runs | newest is `33318905691` (2026-08-30, `v0.9.2`, success); none since | `gh run list --workflow=release.yml` |
| GitHub releases | `v0.9.2` is Latest | `gh release list` |
| PRs from the milestone branch | 0; open PRs: none | `gh pr list` |
| Flip history | landed at eight close-prep phases, most recently REL-12 at the Phase 69 close through `phase.complete`, caught by the third observation and reverted before any commit | ROADMAP constraint 10; `69-CLOSEOUT-GUARD.md` § "Third observation (after phase.complete, orchestrator)" |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: record the phase-head REQUIREMENTS.md closeout guard, PHASE_BASE_SHA after the AMENDED commit, and both operator procedures</name>
  <reversibility rating="reversible">One new planning document; no product-tree or requirement-state change.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the REL-13 checkbox line, its AMENDED block, the Traceability row and the coverage line, read so the quotes are byte-accurate)
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CLOSEOUT-GUARD.md (the structure to mirror, including § "Third observation (after phase.complete, orchestrator)")
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-02 and D-09, and § "Specific Ideas" second bullet
    - .planning/ROADMAP.md binding constraint 10 of the v0.9.4 section
  </read_first>
  <action>
Create `71-CLOSEOUT-GUARD.md` with the heading `# Phase 71 — REL-13 Checkbox-Flip Closeout Guard` and a one-line statement: this file changes no requirement state, and `.planning/REQUIREMENTS.md` is read and quoted, never edited (D-02: no Phase 71 plan writes it).

1. **`## Baseline`** (D-09). Record these before this plan's first commit, each command with its output, then its key line:
   - `git rev-parse HEAD`, key `PHASE_BASE_SHA`
   - `git log -1 --format='%H %s'`, where the subject must carry no `(71-0N)` plan scope
   - `git log --format=%H -1 -S 'AMENDED 2026-09-13 (Phase 71 discussion' -- .planning/REQUIREMENTS.md`, key `AMENDED_COMMIT`, then `git merge-base --is-ancestor "$AMENDED_COMMIT" "$PHASE_BASE_SHA"; echo "exit:$?"`, which must print `exit:0`: the phase head is after D-02's AMENDED commit, as D-09 requires
   - `sha256sum .planning/REQUIREMENTS.md`, key `REQ_SHA256_BASE`, the digest only
   - `wc -l < .planning/REQUIREMENTS.md`, key `REQ_LINES_BASE`
   - `grep -c 'REL-13' .planning/REQUIREMENTS.md`, key `REL13_HITS_BASE`
   - `date -u +"%Y-%m-%dT%H:%M:%SZ"`, key `GUARD_AT`

   Then cross-check against the planning-time values in this plan's census: digest `4d98e028…c5636`, 80 lines, 4 hits.
   - If they agree, say so.
   - If they differ, record both values, run `git log --oneline -3 -- .planning/REQUIREMENTS.md`, and quote the change from the previous commit that touched the file.
   - If REL-13's checkbox is not `- [ ]` or its Traceability row is not `Pending`, write `## HALT: REL-13 already moved before phase head` and stop.
   - Otherwise keep your fresh values as the baseline, with the explanation beneath.

2. **`## The lines under guard`.** Paste the verbatim `grep -n 'REL-13' .planning/REQUIREMENTS.md` in a fenced block. Classify each hit:
   - state-bearing: the `- [ ] **REL-13**:` checkbox line and the `| REL-13 | Phase 71 | Pending — coverage only; …` row
   - informational: the AMENDED-block prose line and the "Mapped to phases" coverage line

   Explain in two sentences why the whole-file SHA-256 is the primary probe:
   - a flip is line-count-neutral
   - Phase 63's flip moved three requirements at once, which a grep scoped to one ID would have missed

4. **`## Why this file exists`.** State the history from ROADMAP constraint 10 and `69-CLOSEOUT-GUARD.md`, without inventing a count:
   - `phase.complete`-family tooling has flipped the release requirement at eight close-prep phases, against an explicit CONTEXT decision each time
   - the most recent was REL-12 at the Phase 69 close, caught by the third observation after `phase.complete` and reverted before any commit
   - it has landed through both entry points, `phase.complete` from `/gsd-execute-phase` and `/gsd-verify-work`'s inline transition
   - REL-13 is again a requirement meant to close only at `/gsd-complete-milestone`, on the observed merge (D-09)

5. **`## Re-verification protocol (phase close)`.** Give the exact commands plan 71-07 runs, and what each must show:
   - `sha256sum`, compared with `REQ_SHA256_BASE`
   - `wc -l`
   - `git diff --name-only -- .planning/REQUIREMENTS.md`, expected empty
   - `grep -n 'REL-13'`, expected byte-identical to § "The lines under guard", compared line by line in file order

6. **`## For the operator running phase.complete`.** This section applies after this phase's plans have finished, at whichever of the two entry points runs: `/gsd-execute-phase`'s `phase.complete` step, or `/gsd-verify-work`'s inline transition.
   - Before either runs, back up `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` to a scratch directory outside the repository.
   - After it runs, run the same probes as step 5, with the baseline digest and the four grep lines written out inline so the reader needs no other file.
   - On any divergence, run `git checkout -- .planning/REQUIREMENTS.md`, re-run the probes to show a MATCH, and then diff ROADMAP.md and STATE.md against the backup.
   - The rule: reverted and reported, never committed, and no `/gsd-complete-milestone` step starts until the probes MATCH.

   State that `71-HANDOFF.md` reproduces this section.

7. **`## This task's own effect on .planning/REQUIREMENTS.md`.** Record `git diff --name-only -- .planning/REQUIREMENTS.md` and `git status --porcelain .planning/REQUIREMENTS.md`, both empty.

8. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; G="$D/71-CLOSEOUT-GUARD.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$G" | head -n 1; } &amp;&amp; PB="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; SJ="$(git log -1 --format=%s "$PB")" &amp;&amp; [ -n "$SJ" ] &amp;&amp; ! printf '%s\n' "$SJ" | grep -qE '\(71-0[0-9]\)' &amp;&amp; AC="$(git log --format=%H -1 -S 'AMENDED 2026-09-13 (Phase 71 discussion' -- .planning/REQUIREMENTS.md)" &amp;&amp; [ "${#AC}" = 40 ] &amp;&amp; [ "$(k AMENDED_COMMIT)" = "$AC" ] &amp;&amp; git merge-base --is-ancestor "$AC" "$PB" &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(wc -l &lt; .planning/REQUIREMENTS.md)" = "$(k REQ_LINES_BASE)" ] &amp;&amp; [ "$(grep -c 'REL-13' .planning/REQUIREMENTS.md)" = "$(k REL13_HITS_BASE)" ] &amp;&amp; grep -qE '^- \[ \] \*\*REL-13\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-13 \| Phase 71 \| Pending' .planning/REQUIREMENTS.md &amp;&amp; [ -n "$(k GUARD_AT)" ] &amp;&amp; grep -qF '/gsd-verify-work' "$G" &amp;&amp; grep -qF '/gsd-execute-phase' "$G" &amp;&amp; grep -qF 'git checkout -- .planning/REQUIREMENTS.md' "$G" &amp;&amp; [ "$(grep -cF "$(k REQ_SHA256_BASE)" "$G")" -ge 2 ] &amp;&amp; for s in '## Baseline' '## The lines under guard' '## Why this file exists' '## Re-verification protocol (phase close)' '## For the operator running phase.complete'; do grep -qxF "$s" "$G" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md)" ] &amp;&amp; [ -z "$(git diff --name-only "$PB" -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$G" &amp;&amp; ! grep -q '^## HALT' "$G"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `PHASE_BASE_SHA` not 40 characters, not an ancestor of HEAD, or a plan-scoped `(71-0N)` commit
- the AMENDED commit not found, differing from `AMENDED_COMMIT`, or not an ancestor of `PHASE_BASE_SHA`
- the live digest, line count or REL-13 hit count differing from the recorded baseline keys
- the REL-13 checkbox not unchecked, or its row not Pending
- `GUARD_AT` empty
- either entry point, the reversion command, or the baseline digest written out a second time missing from the file
- REQUIREMENTS.md, ROADMAP.md or STATE.md modified since `PHASE_BASE_SHA`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Baseline` holds `PHASE_BASE_SHA` (40 hex), `AMENDED_COMMIT` (40 hex, an ancestor of `PHASE_BASE_SHA`), `REQ_SHA256_BASE` (64 hex), `REQ_LINES_BASE`, `REL13_HITS_BASE` and `GUARD_AT` as bare key lines, each beside its command, plus a written cross-check against the planning-time values.
    - `## The lines under guard` quotes every `grep -n 'REL-13'` hit verbatim and classifies each as state-bearing or informational.
    - `## For the operator running phase.complete` names `/gsd-execute-phase` and `/gsd-verify-work`, instructs the scratch backup of REQUIREMENTS.md, ROADMAP.md and STATE.md, writes out the baseline digest and grep lines inline, and carries `git checkout -- .planning/REQUIREMENTS.md` with the rule "reverted and reported, never committed".
    - REQUIREMENTS.md, ROADMAP.md and STATE.md are unchanged in this worktree.
  </acceptance_criteria>
  <done>The phase-head digest, line count, REL-13 hits and PHASE_BASE_SHA are recorded after the AMENDED commit, and both operator procedures are written with the two entry points named.</done>
</task>

<task type="auto">
  <name>Task 2: SC#1 fence observation 1 of 2 with a positive control on every remote negative, the milestone fences, and D-11 part 1 at phase head</name>
  <reversibility rating="reversible">One new planning document recording read-only probes; nothing is written to any remote.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md</files>
  <read_first>
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md (the two-observation shape and its positive controls)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-RESEARCH.md § "The four SC#1 remote probes, each with a positive control" and § "D-11 part 1 — the code-freeze diff since Phase 70's final code commit"
    - .planning/ROADMAP.md § "Phase 71" SC#1, and binding constraint 12 of the v0.9.4 section
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-08 and D-11, and the measured-state list in `<domain>`
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PHASE_BASE_SHA`) and 70-CI-EVIDENCE.md (`PUSHED_SHA`, `RUN_ID`)
  </read_first>
  <action>
Create `71-SC1-INVARIANTS.md` with the heading `# Phase 71 — SC#1 Fence Invariants (unpublished-shaped tree)`.

1. **`## Observation 1 of 2`.** Open with `OBS1_AT = ` followed by the output of `date -u +"%Y-%m-%dT%H:%M:%SZ"`. Then record each probe as a command with its verbatim output, then its key line (D-11: each remote probe carries a positive control against the existing `v0.9.2`):
   - `sed -n 7p pyproject.toml`, key `VERSION_LINE`, which must be `version = "0.9.2"`
   - `git tag -l 'v0.9*'`, the local positive control, with v0.9.0 and v0.9.2 present; then `git tag -l 'v0.9.3'` and `git tag -l 'v0.9.4'`, keys `LOCAL_V093_TAGS = 0` and `LOCAL_V094_TAGS = 0`
   - the unfiltered `git ls-remote --tags origin`. Derive three counts from that single listing: `REMOTE_V092_TAG = 1`, the lines ending `refs/tags/v0.9.2`, which is the positive control; `REMOTE_V093_TAG = 0`; and `REMOTE_V094_TAG = 0`. A filtered probe whose silence could mean an unreachable remote is not accepted.
   - `curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json`, key `PYPI_092_HTTP = 200` (positive control); the same for `0.9.3` and `0.9.4`, keys `PYPI_093_HTTP = 404` and `PYPI_094_HTTP = 404`
   - `gh release list --limit 20 --json tagName,isLatest,publishedAt`, verbatim. Key `GH_LATEST = v0.9.2` (positive control), and keys `GH_V093_RELEASES = 0` and `GH_V094_RELEASES = 0`
   - `gh run list --workflow=release.yml --limit 20 --json databaseId,createdAt,headBranch,event,conclusion`, verbatim. `RELEASE_POSITIVE_CONTROL = 33318905691` must be present, and `RELEASE_RUNS_SINCE_V092 = 0` counts runs created after `2026-08-31` or on a `v0.9.3` or `v0.9.4` ref
   - `gh pr list --state all --limit 5 --json number`, non-empty (positive control); then `gh pr list --head gsd/v0.9.4-typing-modernization --state all --json number,state`, key `BRANCH_PRS = 0`

   State plainly that the fence holds at phase head: v0.9.2 is still the latest published release, no v0.9.3 or v0.9.4 tag exists locally or on origin, nothing is on PyPI or GitHub Releases for either number, and no pull request exists from the milestone branch. D-08 records both numbers as unclaimed, not decided, and this phase creates no tag.

2. **`## Milestone fences`.**
   - Run `git fetch origin main`, then record `MILESTONE_BASE = ` with `git merge-base HEAD origin/main` and `ORIGIN_MAIN_SHA_OBS1 = ` with `git rev-parse origin/main`.
   - Record `git log --format=%h -G '^version = ' "$MILESTONE_BASE"..HEAD -- pyproject.toml`, which must print nothing, key `VERSION_BUMP_COMMITS = 0`.
   - Record `git diff --name-only "$MILESTONE_BASE" HEAD -- .github/`, which must print nothing, key `WORKFLOW_DIFF_FILES = 0` (constraint 12: no workflow file is edited).
   - Record `git diff --name-only "$MILESTONE_BASE" HEAD -- typsphinx/ tests/`, key `MILESTONE_CODE_FILES` with the count, which must be 10: exactly Phase 70's converted files.

3. **`## Code freeze since Phase 70 (D-11 part 1), observation 1`.**
   - `CODE_FREEZE_ANCHOR = e721ff899a981eafdad696ef1a9c93aaab41ece5`, Phase 70's final code commit. Confirm it by measurement: `git log --format='%H %s' -1 -- typsphinx/ tests/` must print that SHA. If it prints another, a commit touched code after Phase 70; write `## HALT: code changed after Phase 70` with the output quoted, and stop.
   - `git merge-base --is-ancestor "$CODE_FREEZE_ANCHOR" HEAD; echo "exit:$?"`, which must print `exit:0`.
   - `git diff --stat "$CODE_FREEZE_ANCHOR" HEAD -- typsphinx/ tests/`, empty, key `CODE_FREEZE_DIFF_FILES = 0`.
   - The positive control: `git diff --name-only 697a113221a8a267d7e8c6dd1f2b95672f9454d2 "$CODE_FREEZE_ANCHOR" -- typsphinx/ tests/`, where `697a1132…` is Phase 70's `PHASE_BASE_SHA` read from `70-BASELINE-EVIDENCE.md`. It must list the same ten files as § "Milestone fences", key `ANCHOR_CONTROL_FILES = 10`. Explain that the same pathspec is non-empty across Phase 70's own range, so an empty result after the anchor is not a broken pathspec or a wrong anchor.
   - The Phase 70 CI cross-check: read `PUSHED_SHA` from `70-CI-EVIDENCE.md`, then record `git merge-base --is-ancestor "$CODE_FREEZE_ANCHOR" <that SHA>; echo "exit:$?"` and `git diff --quiet "$CODE_FREEZE_ANCHOR" <that SHA> -- typsphinx/ tests/; echo "exit:$?"`, both `exit:0`. Key `PHASE70_CI_TIP_SAME_CODE = yes`: the code on this branch is the code Phase 70's CI run tested.

4. **`## Handoff to observation 2`.** State that plan 71-06 owns observation 2, the phase-scoped diff from `PHASE_BASE_SHA`, the post-dispatch check, D-11 part 1 on the close tip, and D-11 part 2, the masked-AST re-run. It runs two waves and one CI dispatch later, so the two `OBS*_AT` stamps are genuinely separated. Name the sections 71-06 appends.

5. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; S="$D/71-SC1-INVARIANTS.md" &amp;&amp; A=e721ff899a981eafdad696ef1a9c93aaab41ece5 &amp;&amp; B70=697a113221a8a267d7e8c6dd1f2b95672f9454d2 &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; ks() { sed -n "s/^$1 = //p" "${2:-$S}" | head -n 1; } &amp;&amp; [ -n "$(ks OBS1_AT)" ] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; [ -n "$(git tag -l 'v0.9.2')" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.3')" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.4')" ] &amp;&amp; T="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.[34]')" = 0 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json)" = 200 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.3/json)" = 404 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.4/json)" = 404 ] &amp;&amp; [ "$(gh release list --limit 20 --json tagName,isLatest --jq '.[] | select(.isLatest) | .tagName')" = v0.9.2 ] &amp;&amp; [ "$(gh release list --limit 50 --json tagName --jq '[.[] | select(.tagName == "v0.9.3" or .tagName == "v0.9.4")] | length')" = 0 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json databaseId --jq '[.[] | select(.databaseId == 33318905691)] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headBranch,createdAt --jq '[.[] | select(.headBranch == "v0.9.3" or .headBranch == "v0.9.4" or .createdAt &gt; "2026-08-31")] | length')" = 0 ] &amp;&amp; [ "$(gh pr list --state all --limit 5 --json number --jq 'length')" -ge 1 ] &amp;&amp; [ "$(gh pr list --head gsd/v0.9.4-typing-modernization --state all --json number --jq 'length')" = 0 ] &amp;&amp; git fetch -q origin main &amp;&amp; MB="$(git merge-base HEAD origin/main)" &amp;&amp; [ "$(ks MILESTONE_BASE)" = "$MB" ] &amp;&amp; [ -z "$(git log --format=%h -G '^version = ' "$MB"..HEAD -- pyproject.toml)" ] &amp;&amp; [ -z "$(git diff --name-only "$MB" HEAD -- .github)" ] &amp;&amp; [ "$(ks CODE_FREEZE_ANCHOR)" = "$A" ] &amp;&amp; [ "$(git log --format=%H -1 -- typsphinx tests)" = "$A" ] &amp;&amp; git merge-base --is-ancestor "$A" HEAD &amp;&amp; [ -z "$(git diff --name-only "$A" HEAD -- typsphinx tests)" ] &amp;&amp; CL="$(git diff --name-only "$B70" "$A" -- typsphinx tests)" &amp;&amp; [ "$(printf '%s\n' "$CL" | wc -l)" = 10 ] &amp;&amp; [ "$(git diff --name-only "$MB" HEAD -- typsphinx tests)" = "$CL" ] &amp;&amp; P7="$(ks PUSHED_SHA .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CI-EVIDENCE.md)" &amp;&amp; [ "${#P7}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$A" "$P7" &amp;&amp; git diff --quiet "$A" "$P7" -- typsphinx tests &amp;&amp; for x in LOCAL_V093_TAGS LOCAL_V094_TAGS REMOTE_V093_TAG REMOTE_V094_TAG GH_V093_RELEASES GH_V094_RELEASES RELEASE_RUNS_SINCE_V092 BRANCH_PRS VERSION_BUMP_COMMITS WORKFLOW_DIFF_FILES CODE_FREEZE_DIFF_FILES; do [ "$(ks "$x")" = 0 ] || { echo "key not 0: $x"; exit 1; }; done &amp;&amp; [ "$(ks REMOTE_V092_TAG)" = 1 ] &amp;&amp; [ "$(ks PYPI_092_HTTP)" = 200 ] &amp;&amp; [ "$(ks PYPI_093_HTTP)" = 404 ] &amp;&amp; [ "$(ks PYPI_094_HTTP)" = 404 ] &amp;&amp; [ "$(ks GH_LATEST)" = v0.9.2 ] &amp;&amp; [ "$(ks RELEASE_POSITIVE_CONTROL)" = 33318905691 ] &amp;&amp; [ "$(ks MILESTONE_CODE_FILES)" = 10 ] &amp;&amp; [ "$(ks ANCHOR_CONTROL_FILES)" = 10 ] &amp;&amp; [ "$(ks PHASE70_CI_TIP_SAME_CODE)" = yes ] &amp;&amp; for s in '## Observation 1 of 2' '## Milestone fences' '## Code freeze since Phase 70 (D-11 part 1), observation 1' '## Handoff to observation 2'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$S" &amp;&amp; ! grep -q '^## HALT' "$S" &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml uv.lock .planning/REQUIREMENTS.md)" ]</automated>
    <fails_when>Non-zero exit, or `key not 0: <key>` / `missing section: <name>` printed. Each of these makes it fail:
- `OBS1_AT` empty, or `pyproject.toml:7` not the 0.9.2 assignment
- the v0.9.2 local or remote tag missing (positive control), or any v0.9.3 or v0.9.4 tag present
- PyPI not 200 for 0.9.2, or not 404 for 0.9.3 or 0.9.4
- the latest release not v0.9.2, or a v0.9.3 or v0.9.4 release present
- release.yml run 33318905691 absent (positive control), or a run since 2026-08-31 or on an unpublished ref present
- the pull-request listing empty (positive control), or a pull request from the milestone branch present
- `MILESTONE_BASE` differing from the live merge-base, a version-line commit, or a `.github/` change since it
- the anchor key wrong, the last code commit not the anchor, a code change after it, or the anchor-control list not ten files
- the milestone code diff differing from the anchor-control list
- the anchor not an ancestor of Phase 70's CI-tested tip, or the code differing between them
- a recorded key contradicting its probe, a section missing, a key line carrying a parenthesis, or a HALT heading
- a tracked product file or REQUIREMENTS.md changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Observation 1 of 2` opens with `OBS1_AT` and records every probe from action step 1 verbatim, each with its key.
    - Every negative has a positive control from the same source: the v0.9.2 tag line from the same `ls-remote`, PyPI 200 for 0.9.2, v0.9.2 as the latest release, run `33318905691` in the release.yml listing, and a non-empty pull-request listing.
    - `## Milestone fences` records `MILESTONE_BASE`, `ORIGIN_MAIN_SHA_OBS1`, `VERSION_BUMP_COMMITS = 0`, `WORKFLOW_DIFF_FILES = 0` and `MILESTONE_CODE_FILES = 10`.
    - `## Code freeze since Phase 70 (D-11 part 1), observation 1` records `CODE_FREEZE_ANCHOR`, `CODE_FREEZE_DIFF_FILES = 0`, `ANCHOR_CONTROL_FILES = 10` and `PHASE70_CI_TIP_SAME_CODE = yes`, each with its command.
    - `## Handoff to observation 2` names plan 71-06 and the sections it appends.
  </acceptance_criteria>
  <done>The first SC#1 observation is recorded with a positive control on every remote probe, and the code freeze since Phase 70 is measured against an anchor proven real by its own non-empty range.</done>
</task>

<task type="auto">
  <name>Task 3: Write the reasoned no-external-API COVERAGE.md and prove the seal-time gate accepts it</name>
  <reversibility rating="reversible">One new planning document.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/COVERAGE.md</files>
  <read_first>
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/COVERAGE.md (the declaration shape whose opening line the gate accepts)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-RESEARCH.md § "Standard Stack" and § "Pitfall 8" (the tools the phase uses and the detector's known false positive)
  </read_first>
  <action>
Create `COVERAGE.md` in the phase directory, following Phase 69's shape: a reasoned declaration with no capability matrix.

1. The heading is `# Phase 71: External API Coverage Declaration`. The first body line starts `No external API integration: ` and continues with one sentence. That sentence says the phase:
   - edits `CHANGELOG.md` only
   - runs the project's own tooling (`uv`, `tox`, `pytest`, `black`, `mypy`, `ruff`, `sphinx-build`)
   - pushes the milestone branch once and dispatches the project's own `ci.yml` once (D-10)
   - measures a trial merge with `git merge-tree` (D-05)
   - writes planning evidence

2. Run the detector over this phase's scope and paste its JSON output verbatim in a fenced block. Build the scope from:
   - the concatenated bodies of every `71-0*-PLAN.md` in the phase directory
   - the output of `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs query roadmap.get-phase 71`

   Pipe that scope to `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/lib/api-coverage.cjs --json`. Whatever it returns is recorded as-is. If `detected` is true, name each signal and explain why it is not an integration. For example, the `gh api` protection read and the PyPI JSON endpoint are read-only probes of this project's own repository and package page, and `Integration Test - basic` is a CI job name.

3. List what the phase's network use actually is:
   - `git fetch`/`ls-remote` and one fast-forward `git push` against this repository
   - `gh workflow run CI` once
   - read-only `gh run`, `gh release`, `gh pr` and `gh api .../branches/main/protection` calls
   - three `curl` status-code probes of `pypi.org/pypi/typsphinx/<version>/json`
   - `uv` fetching only what `uv.lock` pins

   `release.yml` and `update-pin.yml` are named in the plans only to forbid them.

4. State why the file exists: the seal-time `api-coverage.verify-pre` gate re-runs the detector over plan prose dense with `gh`, workflow and API wording, and this declaration meets a false positive with a reason rather than a fabricated matrix.

5. Run `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished` and paste its JSON. It must report `"passed": true`.

6. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; C="$D/COVERAGE.md" &amp;&amp; test -f "$C" &amp;&amp; grep -qE '^No external API integration: .{20,}' "$C" &amp;&amp; grep -qF '"detected"' "$C" &amp;&amp; ! grep -qiE '^\| *capability *\|' "$C" &amp;&amp; V="$(node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre "$D")" &amp;&amp; printf '%s\n' "$V" | grep -q '"passed": true' &amp;&amp; PB="$(sed -n 's/^PHASE_BASE_SHA = //p' "$D/71-CLOSEOUT-GUARD.md" | head -n 1)" &amp;&amp; [ -n "$PB" ] &amp;&amp; DN="$(git diff --name-only "$PB")" &amp;&amp; ! printf '%s\n' "$DN" | grep -vxF -e "$D/71-CLOSEOUT-GUARD.md" -e "$D/71-SC1-INVARIANTS.md" -e "$C" -e "$D/71-02-SUMMARY.md"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- COVERAGE.md absent
- its declaration line missing or shorter than a sentence
- no verbatim detector JSON
- a capability-matrix table present
- the gate output lacking `"passed": true`
- `PHASE_BASE_SHA` unreadable
- any tracked file outside this plan's three files and SUMMARY changed since the phase base</fails_when>
  </verify>
  <acceptance_criteria>
    - `COVERAGE.md` opens with `No external API integration: ` followed by a one-sentence reason.
    - It holds the detector's JSON verbatim, run over the concatenated plan bodies and the ROADMAP Phase 71 section, and explains any signal the detector reports.
    - It lists the phase's actual network use and holds no capability table.
    - The pasted `check api-coverage.verify-pre` output reports `"passed": true`.
  </acceptance_criteria>
  <done>The seal-time API-coverage gate has a reasoned declaration it accepts, recorded with the real detector result.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`71-01-PLAN.md` § "Spec-less edge-coverage accounting" and § "Prohibition recall accounting". This
plan carries P1, P2 and P5, and authors the `empty` edge: every negative probe here carries a
positive control. It also realises the concurrency guarantee that no plan writes
`.planning/REQUIREMENTS.md`: the phase-head digest it records detects any writer, including tooling
that runs after every plan. Phase accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed +
0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → origin, GitHub API, PyPI (read-only) | `git ls-remote`, `gh release/run/pr list`, `curl` status probes; no write crosses |
| `.planning/REQUIREMENTS.md` → `phase.complete`-family tooling | the requirement state this phase must not change, and that tooling has changed at eight prior closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-06 | Tampering | `phase.complete` (or `/gsd-verify-work`'s transition) flipping REL-13 against D-09 (ASVS V4, L1, by analogy) | high | mitigate | A phase-head digest, line count and quoted lines, recorded before any other plan's commit lands and after the AMENDED commit. Both entry points are named, with a scratch backup and a `git checkout` reversion procedure, and 71-07 reproduces all of it in the handoff. |
| T-71-07 | Repudiation | a vacuous remote negative from an unreachable source (ASVS V7, L1) | high | mitigate | Every negative is derived from the same listing as a positive control: v0.9.2 tag, PyPI 200, latest release v0.9.2, run 33318905691, and a non-empty pull-request listing. The verify re-runs every probe live. |
| T-71-08 | Repudiation | an empty code-freeze diff accepted from a wrong anchor or a broken pathspec | high | mitigate | The anchor is confirmed as the last code commit by measurement, the same pathspec lists ten files across Phase 70's own range, and the anchor carries the code Phase 70's CI run tested. |
| T-71-09 | Elevation of Privilege | a probe becoming a write (tag, release, dispatch, pull request) | high | mitigate | Only read commands appear (`git tag -l`, `ls-remote`, `gh … list/view`, `gh api` GET, `curl -o /dev/null`); the plan pushes nothing and dispatches nothing. |
| T-71-10 | Repudiation | a fabricated capability matrix in COVERAGE.md | low | mitigate | A reasoned declaration with the verbatim detector output, and the seal gate's own verdict recorded. |
| T-71-SC | Tampering | package installs | low | accept | `uv sync` installs only what `uv.lock` pins; no package is added. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `71-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates:
- `71-CLOSEOUT-GUARD.md`: baseline, lines under guard, and both operator procedures. Keys:
  `PHASE_BASE_SHA`, `AMENDED_COMMIT`, `REQ_SHA256_BASE`, `REQ_LINES_BASE`, `REL13_HITS_BASE`,
  `GUARD_AT`. 71-07 appends the close-time re-verification.
- `71-SC1-INVARIANTS.md`: observation 1 of 2, the milestone fences and D-11 part 1 at phase head.
  Keys: `OBS1_AT`, `VERSION_LINE`, `LOCAL_V093_TAGS`, `LOCAL_V094_TAGS`, `REMOTE_V092_TAG`,
  `REMOTE_V093_TAG`, `REMOTE_V094_TAG`, `PYPI_092_HTTP`, `PYPI_093_HTTP`, `PYPI_094_HTTP`,
  `GH_LATEST`, `GH_V093_RELEASES`, `GH_V094_RELEASES`, `RELEASE_POSITIVE_CONTROL`,
  `RELEASE_RUNS_SINCE_V092`, `BRANCH_PRS`, `MILESTONE_BASE`, `ORIGIN_MAIN_SHA_OBS1`,
  `VERSION_BUMP_COMMITS`, `WORKFLOW_DIFF_FILES`, `MILESTONE_CODE_FILES`, `CODE_FREEZE_ANCHOR`,
  `CODE_FREEZE_DIFF_FILES`, `ANCHOR_CONTROL_FILES`, `PHASE70_CI_TIP_SAME_CODE`. 71-06 appends
  observation 2 and the close-tip D-11 checks.
- `COVERAGE.md`.

It modifies no product-tree file and never edits `.planning/REQUIREMENTS.md`. No file named
`71-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- The recorded digest, line count and REL-13 hit count match the live file, the baseline is after
  the AMENDED commit, and REL-13 is `[ ]` and Pending.
- Observation 1 holds on every probe, with a positive control on each remote one.
- The milestone fences hold, and the code-freeze diff since Phase 70 is empty against a proven
  anchor.
- `check api-coverage.verify-pre` passes on `COVERAGE.md`.
</verification>

<success_criteria>
1. SC#4's phase-head half: the REL-13 fence baseline is recorded before any other plan's commit
   lands, with both operator procedures written (D-09).
2. SC#1's first observation: the tree is unpublished-shaped, each remote probe is positively
   controlled, and no code changed after Phase 70 (D-11 part 1).
3. The seal-time API-coverage gate is satisfied by a reasoned declaration.
</success_criteria>

<output>
Create `.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-02-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-09).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run any GSD state-writing verb.
- The first section states `PHASE_BASE_SHA`, `AMENDED_COMMIT`, `REQ_SHA256_BASE`,
  `REQ_LINES_BASE`, `REL13_HITS_BASE`, `OBS1_AT`, `MILESTONE_BASE` and `CODE_FREEZE_ANCHOR`.
</output>
