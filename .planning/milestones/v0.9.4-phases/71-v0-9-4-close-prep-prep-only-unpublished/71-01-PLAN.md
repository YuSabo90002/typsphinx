---
phase: 71-v0-9-4-close-prep-prep-only-unpublished
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CHANGELOG-EVIDENCE.md
autonomous: true
requirements: [REL-13]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-01: the `## [Unreleased]` → `### Changed` block of CHANGELOG.md holds exactly four bold-lead bullets, and the new one is the fourth, directly after the `flake.nix` bullet. Its bold span ends with `(QUA-09, QUA-11, QUA-12, DOC-22, DOC-23).**`, its bold lead carries a verb, its prose names both `Dict[str, Any]` and `dict[str, Any]`, and it contains "no effect on installing or using typsphinx".'
    - 'D-01: no version string of the two unpublished milestones (0.9.3 or 0.9.4, with or without a leading v) occurs anywhere in CHANGELOG.md.'
    - 'D-02: the new bullet contains neither `Japanese` nor `catalog` (case-insensitive) nor the two-letter Japanese language code as a word, and this plan does not write .planning/REQUIREMENTS.md.'
    - 'D-03: the new bullet carries exactly one occurrence of `byte-identical`, names no pytest or mypy figure and no QUA-12 leg, and every numeral in it outside the requirement IDs equals CORPUS_PROJECT_COUNT as read at execution time from 70-CORPUS-DOCS-BASE-EVIDENCE.md; 70-AFTER-RUNTIME-EVIDENCE.md reads LEG_D_VERDICT = MET.'
    - 'D-04: the edit is pure addition. The diff from BASE_71_01 has one hunk, zero deleted lines and exactly one added blank line, and its added non-blank lines are exactly the fourth bullet. The three existing bullets, the Planned block (equal SHA-256), the heading and link-reference counts and the final `[Unreleased]: …/compare/v0.9.2...HEAD` line are unchanged. There is no `### Verified` subsection and no lead paragraph, and scripts/extract_changelog_section.py is not run.'
    - 'SC#3 docs half (Claude discretion: clean builds): DOCS_HTML_WARN_BASE and DOCS_PDF_WARN_BASE are measured on the pre-edit tree, each immediately after `rm -rf docs/_build` and under LC_ALL=C. The post-edit clean builds report the same two counts with the same WARNING lines, and docs/_build/pdf/typsphinx.pdf begins with %PDF-.'
    - 'Edge (adjacency): the fourth bullet is preceded by exactly one blank line and followed by exactly one blank line and then `### Planned for Future Releases`, so MyST renders four list items rather than merging the new text into the third bullet or the heading.'
    - 'Edge (ordering): the four bullets read tox-uv, Dependabot, flake.nix, then the new bullet; the first three are byte-identical to the base, which the pure-addition proof implies.'
    - 'Every factual claim of the bullet is traced in `## Accuracy basis` to a named Phase 70 evidence file and section.'
    - 'REL-13 is cited for coverage only; this plan SUMMARY declares requirements-completed: [] (D-09).'
  artifacts:
    - path: CHANGELOG.md
      provides: 'the fourth `### Changed` bullet under the existing `## [Unreleased]` heading'
      contains: '(QUA-09, QUA-11, QUA-12, DOC-22, DOC-23).**'
    - path: .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CHANGELOG-EVIDENCE.md
      provides: 'plan base, pre-edit measurements, the Phase 70 figures cited, clean-build docs baselines, the bullet and its accuracy basis, pure-addition proof, fence and content assertions, post-edit docs counts'
      contains: 'DOCS_HTML_WARN_BASE = '
  key_links:
    - from: CHANGELOG.md
      to: docs/source/changelog.rst
      via: 'the include directive with :parser: myst_parser.sphinx_ makes the bullet Read the Docs latest text once REL-13 merges; a malformed bullet shows as a new docs warning'
      pattern: 'myst_parser.sphinx_'
    - from: '70-CORPUS-DOCS-BASE-EVIDENCE.md CORPUS_PROJECT_COUNT'
      to: 'the numeral in the bullet evidence sentence'
      via: 'read with sed at execution time and compared to every numeral in the bullet outside the requirement IDs'
      pattern: 'CORPUS_PROJECT_COUNT = '
    - from: '71-CHANGELOG-EVIDENCE.md DOCS_HTML_WARN_BASE and DOCS_PDF_WARN_BASE'
      to: '71-03 final-tree docs builds'
      via: 'KEY = value lines read with sed by plan 71-03'
      pattern: 'DOCS_PDF_WARN_BASE = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action: no tag, PyPI upload, GitHub Release, pull request, merge, release.yml or update-pin.yml run, PR comment, or push. The phase single push belongs to 71-04.'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT publish a reference to a version the project has not released, and MUST NOT describe the Japanese site or its translation mechanism (D-01, D-02). CHANGELOG.md is included wholesale into the published docs.'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT overstate what the milestone did: no user-facing effect, no pytest or mypy figures, no enumeration of the QUA-12 legs, and no number that was not transcribed from Phase 70 evidence at execution time (D-03).'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT change any file under typsphinx/, tests/, docs/, .github/, pyproject.toml or uv.lock, MUST NOT write .planning/REQUIREMENTS.md, and MUST NOT merge origin/main into the branch (D-05, D-11).'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Add this milestone's one CHANGELOG bullet under the existing `## [Unreleased]` → `### Changed`
heading (D-01..D-04), and prove with clean documentation builds, taken before and after the edit,
that the new prose renders through MyST without adding a warning.

Purpose: this is the phase's only product-tree change and the only text a reader of the published
changelog page sees from v0.9.4. Nothing is released, so the bullet joins v0.9.3's three under
`## [Unreleased]`, and the next release-prep phase promotes all four (D-08). The bullet names the
API-reference type-text change with the `Dict[str, Any]` → `dict[str, Any]` example (D-01), says
nothing about the Japanese site (D-02, the AMENDED clause), and carries exactly one evidence sentence
whose number is transcribed from Phase 70's evidence at execution time (D-03). Everything else in
the file stays byte-identical (D-04).

Output: the edited `CHANGELOG.md`, and `71-CHANGELOG-EVIDENCE.md` recording the plan base, the
pre-edit measurements, the Phase 70 figures the bullet cites, both clean-build docs baselines, the
accuracy basis, the pure-addition proof, the fence and content assertions and the post-edit docs
counts.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-RESEARCH.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution").
Before any command, provision this worktree's own environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`

Then run every project tool via `uv run`. `--python 3.13.13` is this plan's decision, not the
executor's. Every Phase 70 worktree measured on that interpreter (`VENV_VERSION_INFO = 3.13.13` in
`70-BASELINE-EVIDENCE.md`), and a fresh worktree's default uv-managed CPython 3.14 cannot import
Pillow inside the FHS sandbox (`libz.so.1`). The `docs` extra carries `myst-parser`, `furo` and
`sphinx-autodoc-typehints`, which both docs environments need.

**Evidence key lines.** Every `KEY = value` line carries the bare value only. Put any explanation on
the next line, never after the value, because the verify commands read values with
`sed -n "s/^KEY = //p"` and compare them exactly. Phase 69's executor wrote five keys such as
`DOCS_HTML_WARN_POST = 3 (equal to …)` although its plan said not to; the verify below fails on any
key line that contains an opening parenthesis.

**Scratch files.** Parallel executors share the session scratchpad. Put every helper file and
captured output under your own `mktemp -d` directory, recorded as `SCRATCH_71_01`, and give helper
scripts a `p7101_` prefix.

**Locale.** The host runs `LANG=ja_JP.UTF-8`. Run every counted docs build as
`LC_ALL=C uv run tox -e …`, so the quoted `build succeeded` and `WARNING` lines are English on both
sides of every comparison.

**Long runs stay in the foreground.** Every tox invocation runs in a Bash call with `timeout` set to
600000 ms and never with `run_in_background`.
</worktree_provisioning>

<!-- planner-discipline-allow: 0\.9\.[34] -->
<!-- planner-discipline-allow: japanese|catalog -->
<!-- planner-discipline-allow: ja -->
<!-- planner-discipline-allow: pytest|mypy -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-region-allow: 0\.9\.[34] -->
<!-- planner-region-allow: japanese|catalog -->
The version pattern, the Japanese-site words and the test-tool names are named because D-01, D-02
and D-03 forbid them in the bullet and the executor must know what to avoid. `## HALT` is a heading
written only on a failure path. The gates require the absence of all of them.

## Planning-time census (2026-09-13, main checkout at `d8679b01`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `## [Unreleased]` region | heading `CHANGELOG.md:8`, `### Changed` `:10`, three bold bullets starting `:12` (tox-uv), `:18` (Dependabot), `:28` (`flake.nix`, last line `:35`), blank `:36`, `### Planned for Future Releases` `:37`, `## [0.9.2]` `:44` | `grep -n`, awk region |
| Structural counts | 23 `^## \[` headings, 23 link-reference lines, 1324 lines; the last line is `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.2...HEAD` | `grep -cE`, `tail -n 1` |
| Version strings of the unpublished milestones | 0 hits in CHANGELOG.md | `grep -c` |
| Phase 70 figures | `CORPUS_PROJECT_COUNT = 167` at `70-CORPUS-DOCS-BASE-EVIDENCE.md:45`; `LEG_D_VERDICT = MET` at `70-AFTER-RUNTIME-EVIDENCE.md:360`, where the after-side manifest matched the base on the whole 167-entry list, including 21 projects whose non-zero exit is by design and identical on both sides; row H-018 of `70-DOCS-DIFF-EVIDENCE.md` traces `sphinx_metadata: Dict[str, Any]` → `sphinx_metadata: dict[str, Any]` to `typsphinx/template_engine.py:468-470` (`map_parameters`) | `grep -n` |
| Docs environments | `tox.ini` `docs-html` runs `sphinx-build -b html source _build/html`, `docs-pdf` runs `sphinx-build -b typstpdf source _build/pdf` (both `changedir = docs`, `extras = docs`); the PDF is `docs/_build/pdf/typsphinx.pdf`; `docs/_build` is gitignored | `tox.ini`, `git check-ignore` |
| Prior clean counts | 3 (docs-html) and 5 (docs-pdf) at Phase 69, on CPython 3.14; a cross-check only, never this plan's baseline | `69-CHANGELOG-EVIDENCE.md` |
| Publication | `docs/source/changelog.rst:1-2` includes `../../CHANGELOG.md` with `:parser: myst_parser.sphinx_` | `head -n 2` |
| House-style defect to avoid | Phase 69's review (`69-REVIEW.md` WR-01) flagged a bold lead with no verb | `69-REVIEW.md` |

Two extractors are used throughout:
- the region between `## [Unreleased]` and `## [0.9.2]`: `awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md`
- the fourth bullet of that region, up to its first blank line: pipe the region to `awk '/^- \*\*/{n++} n==4 && /^$/{exit} n==4'`

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: pre-edit measurements, the Phase 70 figures, clean-build docs baselines, then the bullet carried through MyST into a rendered page</name>
  <reversibility rating="reversible">A markdown addition under an existing heading; the next release-prep phase promotes it by editing a heading (D-08).</reversibility>
  <files>CHANGELOG.md, .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - CHANGELOG.md lines 1-50 (the file being modified: the `## [Unreleased]` block whose three bullets set the register)
    - docs/source/changelog.rst lines 1-3 (the include that publishes this edit)
    - tox.ini (the `docs-html` and `docs-pdf` environments)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md (`<decisions>` D-01 to D-04 and D-08; `<specifics>` first bullet, the draft bullet shape)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PATTERNS.md § "`CHANGELOG.md` (config, transform)"
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CORPUS-DOCS-BASE-EVIDENCE.md (the `CORPUS_PROJECT_COUNT` key line and the corpus section around it)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-RUNTIME-EVIDENCE.md § "Leg (b) — pytest after" and § "Post-flip manifest" (`LEG_D_VERDICT`)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOCS-DIFF-EVIDENCE.md (row H-018, the `map_parameters` example)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-STATIC-EVIDENCE.md § "Leg (a) — every converted file", 70-FLIP-EVIDENCE.md and 70-DOC22-EVIDENCE.md (the other claims the bullet may make)
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CHANGELOG-EVIDENCE.md (the evidence-file structure to mirror) and 69-REVIEW.md WR-01
  </read_first>
  <action>
Create `71-CHANGELOG-EVIDENCE.md` with the heading `# Phase 71 — CHANGELOG Evidence (D-01..D-04, SC#2)`.

1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `command -v uv`, then `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line from `<worktree_provisioning>`, with the tail of its output
   - before any commit: `BASE_71_01 = ` followed by the output of `git rev-parse HEAD`
   - `PYVENV_HOME_71_01 = ` and `PYVENV_VERSION_71_01 = `, the `home` and `version_info` values of `.venv/pyvenv.cfg`; the version must be `3.13.13`
   - `SCRATCH_71_01 = ` followed by the path `mktemp -d` printed

2. **`## Pre-edit measurements`.** Record each command with its output, then its key line:
   - `grep -cE '^## \[' CHANGELOG.md`, key `HEADINGS_BEFORE`
   - `grep -cE '^\[[^]]+\]: https' CHANGELOG.md`, key `LINKREFS_BEFORE`
   - the region extractor piped to `grep -cE '^- \*\*'`, key `UNRELEASED_BOLD_BEFORE`, which must be 3
   - the whole-file count of the two unpublished version strings (the dotted pattern the census row names), key `VERSION_LITERALS_BEFORE`, which must be 0
   - `awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' CHANGELOG.md | sha256sum`, key `PLANNED_SHA_BEFORE`, the digest field only
   - the verbatim `tail -n 1 CHANGELOG.md`, in a fenced block rather than a key line

   Add a short `## What this file is NOT` section. It states that `scripts/extract_changelog_section.py` is not run, because no versioned section exists for it to extract (D-04), and that this phase writes no `### Verified` subsection and no lead paragraph under `## [Unreleased]` (D-04, following Phase 69's own D-04). The next release-prep phase promotes all four bullets into its versioned section (D-08).

3. **`## Phase 70 figures the bullet cites`.** Run and quote verbatim, each in a fenced block:
   - `grep -n '^CORPUS_PROJECT_COUNT = ' .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CORPUS-DOCS-BASE-EVIDENCE.md`
   - `grep -n '^LEG_D_VERDICT = ' .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-RUNTIME-EVIDENCE.md`
   - `grep -n '^CORPUS_MANIFEST_SHA256_AFTER = ' .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-RUNTIME-EVIDENCE.md`
   - `grep -n 'map_parameters' .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOCS-DIFF-EVIDENCE.md`

   Then write `CORPUS_PROJECT_COUNT_70 = ` with the number only, and `LEG_D_VERDICT_70 = ` with the verdict only, which must be `MET`. If it is not `MET`, write `## HALT: Phase 70 leg (d) not MET` and stop. Under D-03, any number in the bullet comes from this section and nowhere else, not from this plan's census and not from RESEARCH.md.

4. **`## Docs baseline — pre-edit, clean builds`.** Take both baselines before touching `CHANGELOG.md`.
   - Run `rm -rf docs/_build`, then `LC_ALL=C uv run tox -e docs-html`, in the foreground.
   - Paste the log's final lines verbatim, including the literal `build succeeded` line, and every line containing `WARNING` in a fenced block.
   - Write `DOCS_HTML_WARN_BASE = ` with the number from `build succeeded, N warnings.`. If Sphinx prints `build succeeded.` with no count, the value is 0.
   - Repeat for `docs-pdf`, again with `rm -rf docs/_build` immediately before it, and write `DOCS_PDF_WARN_BASE = `. Record `head -c 5 docs/_build/pdf/typsphinx.pdf`, which must print `%PDF-`.

   An incremental rebuild under-reports warnings, which is why each counted build starts from an empty `docs/_build`.

5. **`## The bullet (tracer slice)`.** Edit `CHANGELOG.md`. After the last line of the `flake.nix` bullet, insert one blank line and then the new bullet. The blank line that already precedes `### Planned for Future Releases` stays the single separator after the new bullet (edge: adjacency). Nothing else in the file changes (D-04).
   - Shape (D-01): `- **` + a plain-language lead sentence that carries a verb + ` (QUA-09, QUA-11, QUA-12, DOC-22, DOC-23).**`, then prose. Continuation lines are indented two spaces and wrap near 100 columns, as the three bullets above do. Model the lead on `Contributor tooling returns to …`; a lead such as "Type annotations in typsphinx's source now use builtin generics" fits. A verbless lead is the defect `69-REVIEW.md` WR-01 recorded.
   - Content, bounded by Phase 70's evidence:
     - typsphinx's own type annotations moved from the `typing` aliases `Dict`, `List`, `Set` and `Tuple` to the builtin `dict`, `list`, `set` and `tuple`, with `Iterator` now imported from `collections.abc`; the linter now enforces that style (QUA-09, QUA-11), and the contributor notes in `CLAUDE.md` describe it (DOC-22)
     - the visible effect (D-01, DOC-23): the API reference now shows, for example, `dict[str, Any]` where it showed `Dict[str, Any]`
     - the sentence `This has no effect on installing or using typsphinx.` on its own (D-01)
     - exactly one evidence sentence (D-03): the Typst output typsphinx generates and its runtime behaviour are unchanged, and the `.typ` output is byte-identical across the test-fixture corpus of `CORPUS_PROJECT_COUNT_70` projects, written as digits from step 3
   - Leave out of the bullet:
     - any version number, with or without a leading `v`, because no release carries one (D-01)
     - any mention of the Japanese site, its translations, or the translation-pin workflow (D-02: the owner dropped that sentence, and the AMENDED blocks record why)
     - pytest or mypy figures, the words "leg" or "five measurements", ruff rule codes, phase numbers and run ids (D-03)
     - any numeral other than the project count; the verify rejects every other digit outside the requirement IDs
     - a second use of the word `byte-identical`
   - Write for a reader of the published changelog page, not in internal shorthand.

   Add `## Accuracy basis`, a table mapping every factual claim in the bullet to the evidence file and section that supports it:
   - the converted annotations: `70-AFTER-STATIC-EVIDENCE.md` § "Changed-file set" and § "Leg (a) — every converted file"
   - the linter enforcement: `70-FLIP-EVIDENCE.md`
   - the contributor notes: `70-DOC22-EVIDENCE.md`
   - the API-reference example: `70-DOCS-DIFF-EVIDENCE.md` row H-018
   - output and runtime behaviour unchanged: `70-AFTER-RUNTIME-EVIDENCE.md` § "Post-flip manifest" (`LEG_D_VERDICT = MET`) and § "Leg (b) — pytest after"

   In one sentence, explain why the evidence sentence says "byte-identical" rather than "every project compiles": the manifest includes projects whose non-zero exit is by design, identical on both sides.

6. **`## Docs render — tracer slice`.** Run `rm -rf docs/_build`, then `LC_ALL=C uv run tox -e docs-html` in the foreground. Paste the verbatim `build succeeded` line and all `WARNING` lines, and write `DOCS_HTML_WARN_TRACER = `.
   - It must equal `DOCS_HTML_WARN_BASE`, and the set of `WARNING` lines must match the baseline's.
   - If a new warning names the changelog page, the MyST in the bullet is malformed. Fix the bullet and re-run from the `rm -rf`.
   - If a new warning names any other page, write `## HALT: docs-html warning drift not caused by CHANGELOG.md` with the warning quoted, and stop. Never move the baseline.

7. Commit `CHANGELOG.md` and the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; F="$D/71-CHANGELOG-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = 3.13.13 ] &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_71_01)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; R="$(awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^### ' | tr '\n' '|')" = '### Changed|### Planned for Future Releases|' ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^- \*\*' | wc -l)" = 4 ] &amp;&amp; N="$(printf '%s\n' "$R" | awk '/^- \*\*/{n++} n==4 &amp;&amp; /^$/{exit} n==4')" &amp;&amp; J="$(printf '%s\n' "$N" | tr '\n' ' ' | tr -s ' ')" &amp;&amp; printf '%s\n' "$J" | grep -qF '(QUA-09, QUA-11, QUA-12, DOC-22, DOC-23).**' &amp;&amp; printf '%s\n' "$J" | grep -qiF 'no effect on installing or using typsphinx' &amp;&amp; [ "$(grep -c '0\.9\.[34]' CHANGELOG.md)" = 0 ] &amp;&amp; NS="$(git diff --numstat "$B" -- CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$NS" | cut -f2)" = 0 ] &amp;&amp; P1="$(awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' CHANGELOG.md | sha256sum)" &amp;&amp; BC="$(git show "$B:CHANGELOG.md")" &amp;&amp; [ -n "$BC" ] &amp;&amp; P0="$(printf '%s\n' "$BC" | awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' | sha256sum)" &amp;&amp; [ "$P1" = "$P0" ] &amp;&amp; [ "$(tail -n 1 CHANGELOG.md)" = "$(printf '%s\n' "$BC" | tail -n 1)" ] &amp;&amp; [ "$(k LEG_D_VERDICT_70)" = MET ] &amp;&amp; [ -n "$(k CORPUS_PROJECT_COUNT_70)" ] &amp;&amp; HB="$(k DOCS_HTML_WARN_BASE)" &amp;&amp; PW="$(k DOCS_PDF_WARN_BASE)" &amp;&amp; [ -n "$HB" ] &amp;&amp; [ -n "$PW" ] &amp;&amp; [ "$(k DOCS_HTML_WARN_TRACER)" = "$HB" ] &amp;&amp; dw() { rm -rf docs/_build &amp;&amp; O="$(mktemp)" &amp;&amp; LC_ALL=C uv run tox -e "$1" &gt;"$O" 2&gt;&amp;1 &amp;&amp; L="$(grep -oE 'build succeeded(, [0-9]+ warnings?)?\.' "$O" | tail -n 1)" &amp;&amp; [ -n "$L" ] &amp;&amp; W="$(printf '%s' "$L" | tr -cd '0-9')" &amp;&amp; printf '%s\n' "${W:-0}"; } &amp;&amp; [ "$(dw docs-html)" = "$HB" ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ docs/ .github/ pyproject.toml uv.lock .planning/REQUIREMENTS.md)" ] &amp;&amp; DN="$(git diff --name-only "$B")" &amp;&amp; ! printf '%s\n' "$DN" | grep -vxF -e CHANGELOG.md -e "$F" -e "$D/71-01-SUMMARY.md" &amp;&amp; for s in '## Head check and provisioning' '## Pre-edit measurements' '## What this file is NOT' '## Phase 70 figures the bullet cites' '## Docs baseline — pre-edit, clean builds' '## The bullet (tracer slice)' '## Accuracy basis' '## Docs render — tracer slice'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, the uv shim not on PATH, or `.venv` not on 3.13.13
- `BASE_71_01` missing or not an ancestor of HEAD
- the region's `###` headings other than `### Changed` then `### Planned for Future Releases`
- a bold-bullet count other than 4, or the fourth bullet lacking the five-ID bold span or the no-effect phrase
- any unpublished version string in CHANGELOG.md
- a deleted line in the CHANGELOG.md diff from the base, or no diff at all
- the Planned block digest or the final line differing from the base
- `LEG_D_VERDICT_70` not MET, or `CORPUS_PROJECT_COUNT_70` empty
- either baseline key empty, or the tracer count differing from `DOCS_HTML_WARN_BASE`
- the live clean docs-html build failing, printing no `build succeeded` line, or reporting a different count
- a change under typsphinx/, tests/, docs/, .github/, pyproject.toml, uv.lock or REQUIREMENTS.md
- another tracked file changed
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `71-CHANGELOG-EVIDENCE.md` holds `BASE_71_01` (40 hex), `PYVENV_VERSION_71_01 = 3.13.13`, `SCRATCH_71_01`, and the pre-edit keys `HEADINGS_BEFORE`, `LINKREFS_BEFORE`, `UNRELEASED_BOLD_BEFORE = 3`, `VERSION_LITERALS_BEFORE = 0` and `PLANNED_SHA_BEFORE`, plus the verbatim final line in a fenced block.
    - `## Phase 70 figures the bullet cites` quotes the four grep outputs and records `CORPUS_PROJECT_COUNT_70` and `LEG_D_VERDICT_70 = MET`.
    - `DOCS_HTML_WARN_BASE` and `DOCS_PDF_WARN_BASE` each come from a build preceded by `rm -rf docs/_build` on the pre-edit tree, run under `LC_ALL=C`, with the verbatim `build succeeded` line and the `WARNING` lines quoted; the PDF starts with `%PDF-`.
    - The region's `###` headings are exactly `### Changed` then `### Planned for Future Releases`, with four bold bullets; the fourth cites the five IDs inside its bold span, its bold lead has a verb, and it contains `no effect on installing or using typsphinx`.
    - `## Accuracy basis` maps every claim in the bullet to a named Phase 70 evidence file and section.
    - `DOCS_HTML_WARN_TRACER` equals `DOCS_HTML_WARN_BASE`, from a clean post-edit build.
  </acceptance_criteria>
  <done>Both docs baselines are taken from clean builds of the pre-edit tree, and the bullet renders through MyST into the changelog page with the warning count unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: The pure-addition proof, the fence and content assertions for D-01..D-04, and both docs environments rebuilt clean against the baseline</name>
  <reversibility rating="reversible">Evidence appends and, only if an assertion fails, a correction to the one added bullet; no version literal, tag or published artifact moves.</reversibility>
  <files>CHANGELOG.md, .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - CHANGELOG.md (the state after Task 1)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CHANGELOG-EVIDENCE.md (Task 1's keys and sections)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-01 to D-04 and § "Claude's Discretion"
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CHANGELOG-EVIDENCE.md § "Pure-addition proof" and § "Fence assertions" (the shape to mirror)
  </read_first>
  <action>
1. Re-run the head check and the provisioning line.

2. **`## Pure-addition proof`** (D-04). Paste the verbatim `git diff "$BASE_71_01" -- CHANGELOG.md`, then record:
   - `ADDED_LINES = ` and `REMOVED_LINES = `, from `git diff --numstat "$BASE_71_01" -- CHANGELOG.md`; the removed count must be 0
   - `DIFF_HUNKS = `, the number of `@@` lines in `git diff -U0 "$BASE_71_01" -- CHANGELOG.md`, which must be 1
   - `ADDED_BLANK_LINES = `, the number of added lines that are empty, which must be 1

   State that the added non-blank lines are exactly the fourth bullet, so the three existing bullets, the Planned block and everything below are unchanged byte for byte.

3. **`## Fence assertions`.** Record each as a command, its output, and a key line:
   - `HEADINGS_AFTER` and `LINKREFS_AFTER`, each equal to its `_BEFORE`
   - `UNRELEASED_BOLD_AFTER = 4`
   - `NO_EFFECT_PHRASES = 4`, counted case-insensitively over the region joined into one line
   - `VERSION_LITERALS_AFTER = 0`
   - `PLANNED_SHA_AFTER`, equal to `PLANNED_SHA_BEFORE`

   Also record, as commands with their outputs:
   - the verbatim final line, identical to the base's, and the count of `compare/v0.9.2...HEAD` lines, which must be 1: the `[Unreleased]` compare base stays `v0.9.2` (D-04)
   - `sed -n 7p pyproject.toml`, which must be `version = "0.9.2"`
   - the region's `###` heading list, and the empty lead-paragraph check `awk '/^### Changed$/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md | grep -c .`, which must be 0 (D-04)
   - `git status --porcelain typsphinx/ tests/ docs/ .github/ pyproject.toml uv.lock .planning/REQUIREMENTS.md`, which must be empty

4. **`## Bullet content assertions`.** Join the fourth bullet into one line with the extractor from the census, then record each check with its output and key:
   - `BULLET_ID_SPAN = present`, when the joined bullet contains the exact bold-span ending `(QUA-09, QUA-11, QUA-12, DOC-22, DOC-23).**` (D-01)
   - `TYPE_EXAMPLE = present`, when it contains both `Dict[str, Any]` and `dict[str, Any]` (D-01)
   - `JAPANESE_SITE_WORDS = 0`, the sum of a case-insensitive count of `Japanese` or `catalog` and a whole-word, case-insensitive count of the two-letter Japanese language code (D-02)
   - `BYTE_IDENTICAL_COUNT = 1` (D-03)
   - `TEST_TOOL_FIGURES = 0`, a case-insensitive count of the two test-tool names (D-03)
   - `BULLET_NUMERALS = `, the distinct numerals left after the requirement IDs are removed, which must equal `CORPUS_PROJECT_COUNT_70` (D-03)
   - `ADJACENCY = OK`, from a check that one blank line precedes the fourth bullet, and that exactly one blank line then `### Planned for Future Releases` follow it (edge: adjacency)

   Quote the first line of each of the four bullets in order: tox-uv, Dependabot, `flake.nix`, then the new one (edge: ordering).

   If any check fails, correct only the new bullet, re-run Task 1 step 6, and then re-run these checks. Never touch the other bullets.

5. **`## Discretion exercised`.** One line each:
   - the lead sentence chosen, and why it carries a verb
   - why the evidence sentence cites the project count
   - that `### Verified` and a lead paragraph are deliberately absent (D-04)

6. **`## Docs render — post-edit, clean builds`.** Build each environment in the foreground, running `rm -rf docs/_build` immediately before each:
   - `LC_ALL=C uv run tox -e docs-html`, key `DOCS_HTML_WARN_POST`
   - `LC_ALL=C uv run tox -e docs-pdf`, key `DOCS_PDF_WARN_POST`, then `head -c 5 docs/_build/pdf/typsphinx.pdf`, which must print `%PDF-`

   Quote each `build succeeded` line and its `WARNING` lines. Both counts must equal their `_BASE`, with the same set of `WARNING` lines. The halt rule of Task 1 step 6 applies.

7. Commit the evidence file, and `CHANGELOG.md` if step 4 corrected the bullet.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; F="$D/71-CHANGELOG-EVIDENCE.md" &amp;&amp; P70=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_71_01)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; BC="$(git show "$B:CHANGELOG.md")" &amp;&amp; [ -n "$BC" ] &amp;&amp; R="$(awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^### ' | tr '\n' '|')" = '### Changed|### Planned for Future Releases|' ] &amp;&amp; [ "$(awk '/^### Changed$/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md | grep -c .)" = 0 ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^- \*\*' | wc -l)" = 4 ] &amp;&amp; [ "$(printf '%s\n' "$R" | tr '\n' ' ' | tr -s ' ' | grep -oi 'no effect on installing or using typsphinx' | wc -l)" = 4 ] &amp;&amp; N="$(printf '%s\n' "$R" | awk '/^- \*\*/{n++} n==4 &amp;&amp; /^$/{exit} n==4')" &amp;&amp; [ -n "$N" ] &amp;&amp; J="$(printf '%s\n' "$N" | tr '\n' ' ' | tr -s ' ')" &amp;&amp; printf '%s\n' "$J" | grep -qF '(QUA-09, QUA-11, QUA-12, DOC-22, DOC-23).**' &amp;&amp; printf '%s\n' "$J" | grep -qF 'Dict[str, Any]' &amp;&amp; printf '%s\n' "$J" | grep -qF 'dict[str, Any]' &amp;&amp; [ "$(printf '%s\n' "$J" | grep -oi 'byte-identical' | wc -l)" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$J" | grep -ciE 'japanese|catalog')" = 0 ] &amp;&amp; [ "$(printf '%s\n' "$J" | grep -ciw 'ja')" = 0 ] &amp;&amp; [ "$(printf '%s\n' "$J" | grep -ciE 'pytest|mypy')" = 0 ] &amp;&amp; CPC="$(k CORPUS_PROJECT_COUNT "$P70/70-CORPUS-DOCS-BASE-EVIDENCE.md")" &amp;&amp; [ -n "$CPC" ] &amp;&amp; [ "$(k LEG_D_VERDICT "$P70/70-AFTER-RUNTIME-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(printf '%s\n' "$J" | sed -E 's/(QUA|DOC)-[0-9]+//g' | grep -oE '[0-9]+' | sort -u | tr '\n' ' ')" = "$CPC " ] &amp;&amp; [ "$(k BULLET_NUMERALS)" = "$CPC" ] &amp;&amp; [ "$(k CORPUS_PROJECT_COUNT_70)" = "$CPC" ] &amp;&amp; printf '%s\n' "$R" | awk '/^- \*\*/{n++; if(n==4){if(prev!="")bad=1; s=1}} {if(e==1){if($0!="### Planned for Future Releases")bad=1; e=2}} s==1 &amp;&amp; /^$/{s=0; e=1} {prev=$0} END{if(e!=2)bad=1; exit bad}' &amp;&amp; NS="$(git diff --numstat "$B" -- CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$NS" | cut -f2)" = 0 ] &amp;&amp; DU="$(git diff -U0 "$B" -- CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$DU" | grep '^@@' | wc -l)" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$DU" | grep -xF '+' | wc -l)" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$DU" | sed -n '/^+++ /d; s/^+//p' | grep -v '^$')" = "$(printf '%s\n' "$N" | grep -v '^$')" ] &amp;&amp; [ "$(grep -cE '^## \[' CHANGELOG.md)" = "$(printf '%s\n' "$BC" | grep -cE '^## \[')" ] &amp;&amp; [ "$(grep -cE '^\[[^]]+\]: https' CHANGELOG.md)" = "$(printf '%s\n' "$BC" | grep -cE '^\[[^]]+\]: https')" ] &amp;&amp; [ "$(tail -n 1 CHANGELOG.md)" = "$(printf '%s\n' "$BC" | tail -n 1)" ] &amp;&amp; [ "$(grep -cF 'compare/v0.9.2...HEAD' CHANGELOG.md)" = 1 ] &amp;&amp; [ "$(grep -c '0\.9\.[34]' CHANGELOG.md)" = 0 ] &amp;&amp; P1="$(awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' CHANGELOG.md | sha256sum)" &amp;&amp; P0="$(printf '%s\n' "$BC" | awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' | sha256sum)" &amp;&amp; [ "$P1" = "$P0" ] &amp;&amp; [ "$(k PLANNED_SHA_AFTER)" = "$(k PLANNED_SHA_BEFORE)" ] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; HB="$(k DOCS_HTML_WARN_BASE)" &amp;&amp; PW="$(k DOCS_PDF_WARN_BASE)" &amp;&amp; [ -n "$HB" ] &amp;&amp; [ -n "$PW" ] &amp;&amp; [ "$(k DOCS_HTML_WARN_POST)" = "$HB" ] &amp;&amp; [ "$(k DOCS_PDF_WARN_POST)" = "$PW" ] &amp;&amp; dw() { rm -rf docs/_build &amp;&amp; O="$(mktemp)" &amp;&amp; LC_ALL=C uv run tox -e "$1" &gt;"$O" 2&gt;&amp;1 &amp;&amp; L="$(grep -oE 'build succeeded(, [0-9]+ warnings?)?\.' "$O" | tail -n 1)" &amp;&amp; [ -n "$L" ] &amp;&amp; W="$(printf '%s' "$L" | tr -cd '0-9')" &amp;&amp; printf '%s\n' "${W:-0}"; } &amp;&amp; [ "$(dw docs-html)" = "$HB" ] &amp;&amp; [ "$(dw docs-pdf)" = "$PW" ] &amp;&amp; [ "$(head -c 5 docs/_build/pdf/typsphinx.pdf)" = '%PDF-' ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ docs/ .github/ pyproject.toml uv.lock .planning/REQUIREMENTS.md)" ] &amp;&amp; DN="$(git diff --name-only "$B")" &amp;&amp; ! printf '%s\n' "$DN" | grep -vxF -e CHANGELOG.md -e "$F" -e "$D/71-01-SUMMARY.md" &amp;&amp; for s in '## Pure-addition proof' '## Fence assertions' '## Bullet content assertions' '## Discretion exercised' '## Docs render — post-edit, clean builds'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- the heading order in the region wrong, or a non-blank line between `## [Unreleased]` and `### Changed`
- a bold-bullet count other than 4, or the no-effect phrase not present exactly four times
- the fourth bullet lacking the five-ID bold span or either half of the type example
- `byte-identical` not present exactly once in the fourth bullet
- a Japanese-site word, the language-code word or a test-tool name in the fourth bullet
- a numeral in the fourth bullet outside the IDs other than the live `CORPUS_PROJECT_COUNT`, or `LEG_D_VERDICT` not MET
- the blank-line structure around the fourth bullet wrong
- a deleted line, more than one hunk, other than one added blank line, or added non-blank lines differing from the fourth bullet
- the heading count, link-reference count, final line, compare-link count or Planned block digest differing from the base
- any unpublished version string in CHANGELOG.md, or `pyproject.toml:7` not the 0.9.2 assignment
- a post-edit docs key differing from its baseline, or a live clean build failing, printing no `build succeeded` line or counting differently
- the PDF not starting with `%PDF-`
- a change under typsphinx/, tests/, docs/, .github/, pyproject.toml, uv.lock or REQUIREMENTS.md, or another tracked file changed
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Pure-addition proof` holds the verbatim diff with `REMOVED_LINES = 0`, `DIFF_HUNKS = 1` and `ADDED_BLANK_LINES = 1`.
    - `## Fence assertions` records every key listed in action step 3 with its command; the heading and link-reference counts equal the base, `VERSION_LITERALS_AFTER = 0`, `PLANNED_SHA_AFTER` equals `PLANNED_SHA_BEFORE`, the compare link stays against `v0.9.2`, and `pyproject.toml:7` still reads `version = "0.9.2"`.
    - `## Bullet content assertions` records `BULLET_ID_SPAN = present`, `TYPE_EXAMPLE = present`, `JAPANESE_SITE_WORDS = 0`, `BYTE_IDENTICAL_COUNT = 1`, `TEST_TOOL_FIGURES = 0`, `BULLET_NUMERALS` equal to `CORPUS_PROJECT_COUNT_70`, and `ADJACENCY = OK`, and quotes the four bullets' first lines in order.
    - `## Discretion exercised` records the lead choice, the count citation and the absent `### Verified`.
    - `DOCS_HTML_WARN_POST` and `DOCS_PDF_WARN_POST` equal their clean pre-edit baselines, with the PDF starting `%PDF-`.
    - `scripts/extract_changelog_section.py` was not run, and only `CHANGELOG.md` and this evidence file changed.
  </acceptance_criteria>
  <done>The milestone's change is described by one bullet under the existing `## [Unreleased]` heading as pure addition, every D-01..D-04 constraint is measured, and both docs environments rebuilt clean match the clean pre-edit baseline.</done>
</task>

</tasks>

<flagged_assumptions>
## Spec-less edge-coverage accounting (6 probe-surfaced items, 0 silent drops)

This phase has no SPEC.md. The deterministic fallback probe ran over REL-13's text (which still
carries the pre-amendment ja-catalog clause; the AMENDED block governs, D-02) and returned six
items, all `unresolved`. Each is resolved against this phase's actual objects:

| Requirement | Category | Probe | Resolution | Verification | Authored in |
|---|---|---|---|---|---|
| REL-13 | adjacency | exactly equal or just touching | The new bullet sits between the `flake.nix` bullet and `### Planned for Future Releases`, separated from each by exactly one blank line. `origin/main` equal to the merge-base is the "just touch" case of the trial merge: the merge is a no-op, whose tree equals HEAD's tree. | explicit | 71-01 truths (bullet), 71-05 truths (trial merge) |
| REL-13 | empty | empty, single-element or null input | Every empty result (no tag, 404, no release, empty code diff) is paired with a positive control from the same source, so an empty result is distinguishable from a broken probe. | explicit | 71-02 and 71-06 truths |
| REL-13 | encoding | whose definition of equality | The REQUIREMENTS.md fence and the CHANGELOG checks compare raw bytes (`sha256sum`, `git diff`). The D-11 masked-AST check compares SHA-256 of `ast.dump` text computed by one interpreter on both sides, and raw bytes differ by design. | explicit | 71-06 and 71-07 truths |
| REL-13 | ordering | is output order specified | Bullet order is fixed: the three existing bullets keep their order and the new one is fourth. The CI job census is compared as a sorted name set against Phase 70's run. The REL-13 grep lines are compared line by line in file order. | explicit | 71-01, 71-04 and 71-07 truths |
| REL-13 | idempotency | what if it runs twice | `git merge-tree --write-tree` re-run from the recorded inputs reproduces `MERGE_TREE`. The CI dispatch never runs twice: a resumed executor reuses `RUN_ID`. The bullet count stays exactly four. | explicit | 71-05, 71-04 and 71-01 truths |
| REL-13 | concurrency | interrupted or parallel | Same-wave plans have disjoint files and never read a sibling's same-wave evidence. Only 71-04 writes to origin, as one fast-forward that halts rather than forcing, and no plan writes REQUIREMENTS.md, so the fence detects any other writer. | backstop | 71-04 truths |

**Residual assumptions, flagged and not guaranteed by any plan:**

- (e) The local canonical ref does not move between 71-04's census and its push. If it does,
  `PUSHED_SHA` records what was pushed, the run is bound to it by `headSha`, and 71-06's empty
  product diff from `PUSHED_SHA` to HEAD shows whether the dispatched tree is this phase's.
- (f) `origin/main` does not move between 71-05's pre-flight and `/gsd-complete-milestone`. The
  handoff therefore re-runs `git merge-tree --write-tree` immediately before the PR.
- (g) No dependabot pull request opens between 71-05's census and the close. The handoff re-reads
  the open-PR list at its own time and names any such PR, left untouched (D-12).

**Accounting:** 6 surfaced = 5 authored as explicit truths + 1 authored as a backstop marker + 0
dismissed + 0 dropped. The residual assumptions (e) to (g) are surfaced here in addition.

## Prohibition recall accounting (descriptor-less, never auto-dismissed)

Stage 1 over-produced against REL-13's text. The candidates were:

- a tag, a PyPI upload or a GitHub Release
- a version bump
- the pull request opened or merged inside this phase
- `release.yml` or `update-pin.yml` started
- a dependabot pull request touched
- `main` absorbed into the branch
- REL-13 recorded as satisfied, or REQUIREMENTS.md written
- an unpublished version number named or linked
- the bullet describing the Japanese site, against the AMENDED clause
- the bullet overstating the work or citing figures not transcribed from evidence
- stale or inherited evidence
- a force-push or a decoy push
- a `typsphinx/` or `tests/` change
- a workflow edit
- malformed markdown
- flaky tests

Stage 2 dropped two as routine engineering: malformed markdown, which the clean docs gates cover,
and flaky tests. No candidate was canon security or compliance, so no canon-referral breadcrumb
applies. Six bespoke prohibitions survive. Each is authored with no `check_*` descriptor, so each
disposes flagged-unverified downstream.

- **P1 (safety):** no irreversible or outward action beyond D-10's one push and one `ci.yml`
  dispatch. In plans 71-01, 71-02, 71-04, 71-05 and 71-07.
- **P2 (honesty):** REL-13 is never recorded as satisfied, and REQUIREMENTS.md is never written
  (D-02, D-09). In plans 71-02 and 71-07.
- **P3 (transparency):** no reference to an unpublished version and no Japanese-site claim in the
  bullet (D-01, D-02). In plans 71-01 and 71-07.
- **P4 (accuracy):** the bullet does not overstate, and its only number comes from Phase 70's
  evidence (D-03). In plan 71-01.
- **P5 (evidence integrity):** no inherited, paraphrased or fabricated evidence, and no negative
  probe without its positive control. In plans 71-02, 71-03, 71-04, 71-05, 71-06 and 71-07.
- **P6 (scope):** no `origin/main` absorption and no change under `typsphinx/`, `tests/`,
  `.github/`, `pyproject.toml` or `uv.lock` (D-05, D-11). In plans 71-01, 71-03, 71-04, 71-05 and
  71-06.

**Accounting:** 6 kept = 6 authored into `must_haves.prohibitions` + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` → `docs/source/changelog.rst` → Read the Docs `latest` | authored prose becomes a public page once REL-13 merges |
| authored prose → the project's factual record | the bullet is the account of what v0.9.4 changed; an unsupported claim misrepresents it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering | malformed MyST in the bullet publishing broken markup (ASVS V5, L1, marginal: the input is prose this project authors) | medium | mitigate | Both docs environments are built from an empty `docs/_build` before and after the edit, and counts and `WARNING` sets must be equal. The tracer proves the render chain first, and drift from another page halts instead of moving the baseline. |
| T-71-02 | Tampering | a heading, link or prose reference to a release that does not exist | high | mitigate | Whole-file count of the unpublished version strings is 0; heading and link-reference counts equal the base; the final line and the `v0.9.2...HEAD` compare link are unchanged; pure addition proven by one hunk with zero deletions. |
| T-71-03 | Repudiation | a recalled or incremental-build warning count recorded as the baseline | medium | mitigate | `rm -rf docs/_build` precedes every counted build, and the verify re-runs clean builds live against the recorded baseline instead of trusting the recorded post-edit numbers. |
| T-71-04 | Elevation of Privilege | scope escape into `typsphinx/`, `tests/`, `docs/`, `.github/`, `pyproject.toml`, `uv.lock` or REQUIREMENTS.md under cover of release prep | high | mitigate | An emptiness check over `git status --porcelain` for those paths, and a scope fence allowing only `CHANGELOG.md`, the evidence file and the SUMMARY to differ from `BASE_71_01`. |
| T-71-05 | Tampering | the bullet describing the Japanese site against D-02, citing test-tool figures, or carrying a number not taken from Phase 70's evidence | medium | mitigate | Case-insensitive word checks for the Japanese-site words and test-tool names, a numeral check against the live `CORPUS_PROJECT_COUNT`, and `## Accuracy basis` mapping each claim to a named evidence section. |
| T-71-SC | Tampering | package installs | low | accept | No new package is added. `uv sync --extra dev --extra docs --python 3.13.13` installs only what `uv.lock` pins; `71-RESEARCH.md` § "Package Legitimacy Audit" records zero added packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This is the phase-wide list; plans 71-02 to 71-07 repeat their own rows and point here.

**New files under `.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/`:**

| File | Created by | Extended by |
|------|------------|-------------|
| `71-CHANGELOG-EVIDENCE.md` | 71-01 | — |
| `71-CLOSEOUT-GUARD.md` | 71-02 (phase-head baseline and operator procedure) | 71-07 (close-time re-verification) |
| `71-SC1-INVARIANTS.md` | 71-02 (observation 1 of 2, milestone fences, D-11 part 1 at head) | 71-06 (observation 2 of 2, phase-scoped diff, post-dispatch proof, D-11 parts 1 and 2 on the close tip) |
| `COVERAGE.md` | 71-02 (reasoned no-external-API declaration) | — |
| `71-GREEN-TREE-EVIDENCE.md` | 71-03 | — |
| `71-CI-EVIDENCE.md` | 71-04 | — |
| `71-PREFLIGHT-EVIDENCE.md` | 71-05 (D-05 non-committing trial merge, D-06 and D-12 inputs) | — |
| `71-HANDOFF.md` | 71-07 | — |
| `71-01-SUMMARY.md` to `71-07-SUMMARY.md` | one per plan | — |

**Evidence keys this plan creates** (in `71-CHANGELOG-EVIDENCE.md`): `BASE_71_01`,
`PYVENV_HOME_71_01`, `PYVENV_VERSION_71_01`, `SCRATCH_71_01`, `HEADINGS_BEFORE`, `LINKREFS_BEFORE`,
`UNRELEASED_BOLD_BEFORE`, `VERSION_LITERALS_BEFORE`, `PLANNED_SHA_BEFORE`,
`CORPUS_PROJECT_COUNT_70`, `LEG_D_VERDICT_70`, `DOCS_HTML_WARN_BASE`, `DOCS_PDF_WARN_BASE`,
`DOCS_HTML_WARN_TRACER`, `ADDED_LINES`, `REMOVED_LINES`, `DIFF_HUNKS`, `ADDED_BLANK_LINES`,
`HEADINGS_AFTER`, `LINKREFS_AFTER`, `UNRELEASED_BOLD_AFTER`, `NO_EFFECT_PHRASES`,
`VERSION_LITERALS_AFTER`, `PLANNED_SHA_AFTER`, `BULLET_ID_SPAN`, `TYPE_EXAMPLE`,
`JAPANESE_SITE_WORDS`, `BYTE_IDENTICAL_COUNT`, `TEST_TOOL_FIGURES`, `BULLET_NUMERALS`, `ADJACENCY`,
`DOCS_HTML_WARN_POST`, `DOCS_PDF_WARN_POST`.

**Product tree:** exactly one file changes, `CHANGELOG.md`, and only by addition: one bullet
appended as the fourth under the existing `### Changed`. No new `## [...]` heading and no
link-reference line is added.

**Outside the repository:** 71-04 fast-forwards `origin/gsd/v0.9.4-typing-modernization` to
`PUSHED_SHA` and starts one `workflow_dispatch` run of `ci.yml`. 71-05 and 71-06 create scratch
directories outside the repository and remove them.

**Reserved name, never produced:** no plan authors `71-VERIFICATION.md`; `gsd-verifier` owns that
name and overwrites it wholesale.
</artifacts_this_phase_produces>

<verification>
- The `## [Unreleased]` region holds `### Changed` with exactly four bold-lead bullets, the fourth
  citing QUA-09, QUA-11, QUA-12, DOC-22 and DOC-23, above a byte-identical
  `### Planned for Future Releases`.
- The new bullet names the `Dict[str, Any]` → `dict[str, Any]` change, says it has no effect on
  installing or using typsphinx, carries one evidence sentence whose only number is Phase 70's
  project count, and says nothing about the Japanese site.
- No unpublished version string anywhere in `CHANGELOG.md`; heading count, link-reference count and
  final line equal the base; the diff from the base is pure addition.
- Both docs environments, built clean before and after the edit, report equal warning counts, and
  the PDF is real.
- Only `CHANGELOG.md` and `71-CHANGELOG-EVIDENCE.md` changed; `.planning/REQUIREMENTS.md` untouched.
</verification>

<success_criteria>
1. ROADMAP SC#2 (as AMENDED): the milestone's change is one bullet under the existing
   `## [Unreleased]` heading, in the house register, with no release section, no unpublished version
   link, the compare base left at `v0.9.2`, and no Japanese-site wording (D-01..D-04).
2. The docs half of SC#3: both docs environments build clean after the edit with the warning counts
   of the clean pre-edit baseline.
3. The bullet is accurate to the recorded Phase 70 evidence, claim by claim.
</success_criteria>

<output>
Create `.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-01-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`. REL-13 closes at `/gsd-complete-milestone`, never in a
  plan (D-09).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run `phase.complete` or any other GSD state-writing verb.
- The first section states `BASE_71_01`, the four docs keys (`_BASE` and `_POST` for both
  environments), the bullet count, `BULLET_NUMERALS`, and the bullet's bold lead quoted. If a HALT
  heading was written, that section says so first.
</output>
