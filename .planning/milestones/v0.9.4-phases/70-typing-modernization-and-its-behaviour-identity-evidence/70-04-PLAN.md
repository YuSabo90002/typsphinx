---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 04
type: execute
wave: 2
depends_on: ["70-01", "70-02"]
files_modified:
  - typsphinx/translator.py
  - tests/test_include_ledger_removal_gate.py
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md
autonomous: true
requirements: [QUA-11, QUA-12]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'The conversion starts only after the CLAUDE.md rewrite has merged. At this plan''s fork point, CLAUDE.md line 75 carries `collections.abc`, and the one commit touching CLAUDE.md since PHASE_BASE_SHA is an ancestor of HEAD.'
    - '`typsphinx/translator.py` and `tests/test_include_ledger_removal_gate.py` are converted by two ruff passes scoped to those two files while the config ignores are still present. Pass 1 is `ruff check <files> --select UP006,UP035 --fix`; pass 2 is `ruff check <files> --fix`. Afterwards `uv run ruff check <files> --select UP006,UP035` and `uv run ruff check <files>` both exit 0.'
    - 'Edge QUA-11/ordering: the import block of `tests/test_include_ledger_removal_gate.py` is left in the order ruff''s isort pass produces. `from collections.abc import Iterator` replaces the `typing` line, which vanishes, and bare `uv run ruff check .` (I001 enabled) exits 0.'
    - 'The masked-AST harness (the `Mask` NodeTransformer from 70-RESEARCH.md, which DELETES every ImportFrom of `typing` or `collections.abc` and nulls every annotation subtree) is recorded verbatim in a `~~~python mask-harness` block, with MASK_HARNESS_SHA256 over the extracted block. Every later automated use extracts and runs that exact text.'
    - 'SC#3 pilot, recorded before any automated use: the masked hash of each file equals the hash of `git show PHASE_BASE_SHA:<path>`. That holds for the shrink-in-place file (`typsphinx/translator.py`, PILOT_TRANSLATOR = EQUAL) and for the move-and-vanish file (`tests/test_include_ledger_removal_gate.py`, PILOT_LEDGER = EQUAL).'
    - 'Edge QUA-12/boundary: equality is exact, and one step away fails. A scratch copy with `class TypstTranslator(` renamed changes the hash (CONTROL_RENAME = DIFFER).'
    - 'Edge QUA-12/empty: the mask does not delete non-typing imports. A scratch copy of the ledger test with its `import textwrap` line removed changes the hash (CONTROL_IMPORT = DIFFER). The file whose `typing` import line vanishes entirely still hashes EQUAL to base.'
    - 'Every changed line in the two files, other than a blank line, carries a typing name (Dict, List, Set, Tuple, Iterator, their builtin forms, `typing` or `collections.abc`). No changed line contains `assert`. No `@preview` line appears in the diff.'
    - 'mypy stdout hashes equal to MYPY_STDOUT_SHA256_BEFORE, and the full pytest result equals PYTEST_RESULT_BEFORE, in this worktree on 3.13.13.'
  artifacts:
    - path: typsphinx/translator.py
      provides: 'Builtin generics; the typing import keeps Any and NamedTuple'
      contains: 'from typing import Any, NamedTuple'
    - path: tests/test_include_ledger_removal_gate.py
      provides: 'Builtin generics; Iterator from collections.abc'
      contains: 'from collections.abc import Iterator'
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md
      provides: 'The mask harness, its hash, the two-file pilot, the non-vacuity controls, and gates for the two converted files'
      contains: 'MASK_HARNESS_SHA256 = '
  key_links:
    - from: '~~~python mask-harness block'
      to: 'verify blocks of 70-05..70-08 and 70-10'
      via: 'sed extraction of the block, sha256 check against MASK_HARNESS_SHA256, then `uv run python -c "$H"`'
      pattern: '^~~~python mask-harness$'
  prohibitions:
    - statement: 'MUST NOT use sed or any text substitution on source, pass --preview or --unsafe-fixes, add `from __future__ import annotations`, rewrite any Optional/Union/`X | None`, or absorb MSG-06 (translator.py''s relative-path DEBUG logs stay as they are).'
      requirement_id: QUA-11
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT run ruff --fix or black in write mode on any file other than the two this plan owns, and MUST NOT add or edit any test assertion or add a new test.'
      requirement_id: QUA-12
      category: safety
      status: resolved
      verification: judgment
---

<objective>
The tracer of the conversion. This plan converts one shrink-in-place file (`typsphinx/translator.py`) and one move-and-vanish file (`tests/test_include_ledger_removal_gate.py`) for real, under the still-present ignores (ROADMAP constraints 1(ii) and 5). It then pilots the QUA-12 leg (a) masked-AST harness on both and proves the harness is not vacuous. That pilot is recorded before any automated use (SC#3).

Purpose: the four wave-3 conversion plans and 70-10 run the harness inside automated verify blocks. Research found that a mask which blanks import nodes in place gives false mismatches when a `typing` line moves or vanishes. So the harness is proven here, on both shapes and on real conversions, before anyone automates it. This plan runs alone in wave 2 for that reason.

Output: the two converted files, committed alone, and `70-MASK-PILOT-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every verify
command with the Bash tool `timeout` set to 600000. Run pytest under `LC_ALL=C`.

**Scratch.** Make one scratch directory `S` with `mktemp -d` outside the repository, and record
`SCRATCH_70_04 = <path>`.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value.

**Commits.** Stage files by explicit path, never `git add -A`. The conversion commit touches only
the two source files. On any failure path, write `## HALT: <reason>` into the evidence file, commit
it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: assert -->
<!-- planner-discipline-allow: @preview -->
<!-- planner-discipline-allow: --preview -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: convert translator.py and test_include_ledger_removal_gate.py with two scoped ruff passes, record the mask harness, and pilot it on both files with two non-vacuity controls</name>
  <precondition>At HEAD, `sed -n 75p CLAUDE.md` contains `collections.abc` and not `todos/pending`; `70-BASELINE-EVIDENCE.md` holds `PHASE_BASE_SHA` and `UP_FILES_MATCH_PLAN = YES` with no HALT heading; and `git diff --quiet "$PHASE_BASE_SHA" HEAD -- typsphinx/translator.py tests/test_include_ledger_removal_gate.py` exits 0. Otherwise write `## HALT: preconditions` and stop without converting.</precondition>
  <reversibility rating="reversible">Annotation and import rewrites in two git-tracked files, proven structure-preserving by the harness.</reversibility>
  <files>typsphinx/translator.py, tests/test_include_ledger_removal_gate.py, .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md</files>
  <read_first>
    - typsphinx/translator.py (line 9's typing import, line 510's `class TypstTranslator(`, and line 773's `str | List[str] | None`)
    - tests/test_include_ledger_removal_gate.py (lines 40-50: the import block)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md (§ Leg (a): the `Mask` class and `sk()` at lines 241-288, the measured EQUAL table, the non-vacuity control, and Pitfall 6)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md (Specific Ideas: what the mask covers and what it must not cover)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md (§ translator.py and § test_include_ledger_removal_gate.py)
  </read_first>
  <action>
1. Create `70-MASK-PILOT-EVIDENCE.md` with the heading `# Phase 70 — Mask Pilot and Tracer Conversion`. Before any commit, record `BASE_70_04 = ` from `git rev-parse HEAD`. Under `## Head check and provisioning`:
   - quote `pwd -P`, `test -f .git`, the shim check, the precondition commands, and the provisioning command
   - record `VENV_HOME = ` and `VENV_VERSION_INFO = `, which must equal 70-02's keys

2. **`## Conversion`.** With `P="typsphinx/translator.py tests/test_include_ledger_removal_gate.py"`:
   - Record `uv run ruff check $P --select UP006,UP035 --output-format=concise`, a non-zero finding count.
   - Run pass 1, `uv run ruff check $P --select UP006,UP035 --fix`. It rewrites usages and moves `Iterator`.
   - Run pass 2, `uv run ruff check $P --fix`. It uses the config rules, so F401 drops the now-unused names and I001 sorts the block. This was measured on a throwaway worktree during planning.
   - Record `uv run ruff check $P --select UP006,UP035` and `uv run ruff check $P`; both must exit 0.
   - No hand edit is expected in these two files. Never use `sed`, `--preview` or `--unsafe-fixes`.
   - Quote `git diff --stat -- $P` and the full `git diff -- tests/test_include_ledger_removal_gate.py`.
   - Commit only the two files, with the message `refactor(70-04): move translator.py and the ledger gate onto builtin generics (QUA-11)`.

3. **`## Mask harness`.** Write the harness into a block that opens with the line `~~~python mask-harness` and closes with `~~~`. It is RESEARCH.md's `Mask` class, unchanged in behaviour:
   - `visit_ImportFrom` returns `None` (deleting the node) when `node.module` is `typing` or `collections.abc`, and returns the node otherwise.
   - `visit_FunctionDef` and `visit_AsyncFunctionDef` both call `_mask_func`, which runs `generic_visit` and then sets `returns` and every `posonlyargs`, `args`, `kwonlyargs`, `vararg` and `kwarg` annotation to `None`.
   - `visit_AnnAssign` runs `generic_visit` and then sets `annotation` to `ast.Constant(value=None)`.

   Nothing else is masked, so docstrings, call arguments and every other node stay in the hash. The program reads the source from stdin, applies `Mask().visit`, calls `ast.fix_missing_locations`, and prints the SHA-256 hex digest of `ast.dump(tree, annotate_fields=True, include_attributes=False)`.

   Then:
   - Record `MASK_HARNESS_SHA256 = ` as the first field of `sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$F" | sed '1d;$d' | sha256sum`. That is exactly how every verify extracts it.
   - Define the helper as `H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$F" | sed '1d;$d')"` and `sk() { uv run python -c "$H"; }`, and quote that definition.
   - Add one paragraph on why the node is deleted rather than blanked (RESEARCH Pitfall 6), and one on what the leg does not prove on its own: import correctness is covered by legs (b) and (e).

4. **`## Pilot (SC#3)`.** For each of the two files, quote `git show "$PHASE_BASE_SHA:<path>" | sk` and `sk < <path>`. Then record:
   - `PILOT_TRANSLATOR = EQUAL` (shrink-in-place: the typing line keeps `Any, NamedTuple`)
   - `PILOT_LEDGER = EQUAL` (move and vanish: `Iterator` moves to `collections.abc` and the typing line disappears)

   If either differs, record `DIFFER`, write `## HALT: mask pilot`, and stop.

5. **`## Non-vacuity controls`.** Work only in copies under `$S`; never edit the tree.
   - Copy `typsphinx/translator.py` to `$S/rename.py` and replace the first `class TypstTranslator(` with `class TypstTranslatorZZZ(` using a `uv run python -c` string replace. Record `CONTROL_RENAME = DIFFER` if `sk < "$S/rename.py"` differs from `sk < typsphinx/translator.py`.
   - Copy `tests/test_include_ledger_removal_gate.py` to `$S/noimport.py` and remove its `import textwrap` line the same way. Record `CONTROL_IMPORT = DIFFER` if its hash differs from the file's.

   Either control being EQUAL means the mask is vacuous: write `## HALT: vacuous mask` and stop.

6. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-MASK-PILOT-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; [ "$(k VENV_VERSION_INFO)" = "$(k VENV_VERSION_INFO "$BE")" ] &amp;&amp; [ "$(sed -n 's/^home = //p' .venv/pyvenv.cfg)" = "$(k VENV_HOME "$BE")" ] &amp;&amp; C="$(git log --format=%H "$B"..HEAD -- CLAUDE.md)" &amp;&amp; [ "$(printf '%s\n' "$C" | grep -c .)" = 1 ] &amp;&amp; git merge-base --is-ancestor "$C" "$(k BASE_70_04)" &amp;&amp; H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$F" | sed '1d;$d')" &amp;&amp; [ -n "$H" ] &amp;&amp; [ "$(printf '%s\n' "$H" | sha256sum | cut -d' ' -f1)" = "$(k MASK_HARNESS_SHA256)" ] &amp;&amp; sk() { uv run python -c "$H"; } &amp;&amp; for f in typsphinx/translator.py tests/test_include_ledger_removal_gate.py; do a="$(sk &lt; "$f")" &amp;&amp; b="$(git show "$B:$f" | sk)" &amp;&amp; [ "${#a}" = 64 ] &amp;&amp; [ "$a" = "$b" ] || { echo "mask differs: $f"; exit 1; }; ! git diff --quiet "$B" HEAD -- "$f" || { echo "unchanged: $f"; exit 1; }; done &amp;&amp; S="$(k SCRATCH_70_04)" &amp;&amp; [ "$(sk &lt; "$S/rename.py")" != "$(sk &lt; typsphinx/translator.py)" ] &amp;&amp; [ "$(sk &lt; "$S/noimport.py")" != "$(sk &lt; tests/test_include_ledger_removal_gate.py)" ] &amp;&amp; [ "$(k PILOT_TRANSLATOR)" = EQUAL ] &amp;&amp; [ "$(k PILOT_LEDGER)" = EQUAL ] &amp;&amp; [ "$(k CONTROL_RENAME)" = DIFFER ] &amp;&amp; [ "$(k CONTROL_IMPORT)" = DIFFER ] &amp;&amp; uv run ruff check typsphinx/translator.py tests/test_include_ledger_removal_gate.py --select UP006,UP035 &amp;&amp; uv run ruff check typsphinx/translator.py tests/test_include_ledger_removal_gate.py &amp;&amp; grep -qxF 'from collections.abc import Iterator' tests/test_include_ledger_removal_gate.py &amp;&amp; ! grep -qE '^from typing import' tests/test_include_ledger_removal_gate.py &amp;&amp; grep -qxF 'from typing import Any, NamedTuple' typsphinx/translator.py &amp;&amp; for s in '## Head check and provisioning' '## Conversion' '## Mask harness' '## Pilot (SC#3)' '## Non-vacuity controls'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or a `mask differs:`, `unchanged:` or `missing section:` line printed. Each of these makes it fail:
- the interpreter differing from 70-02's
- the CLAUDE.md rewrite absent or not an ancestor of this plan's fork point
- the harness block empty or not hashing to `MASK_HARNESS_SHA256`
- either file's masked hash differing from base, or either file unconverted
- a scratch control hashing equal to the file (vacuous mask)
- a pilot or control key wrong
- ruff reporting UP006/UP035 or any config-rule finding on the two files
- the ledger gate lacking the `collections.abc` import or keeping a `typing` import
- translator.py's typing line not `from typing import Any, NamedTuple`
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - Both files carry zero UP006/UP035 findings, and `uv run ruff check` on them exits 0, with the config ignores still present.
    - `tests/test_include_ledger_removal_gate.py` has `from collections.abc import Iterator` and no `from typing import` line. `typsphinx/translator.py` line 9 is `from typing import Any, NamedTuple`.
    - The `~~~python mask-harness` block hashes to `MASK_HARNESS_SHA256`, and both pilot files hash EQUAL to `PHASE_BASE_SHA`.
    - `CONTROL_RENAME = DIFFER` and `CONTROL_IMPORT = DIFFER`.
  </acceptance_criteria>
  <done>The two files are on builtin generics, and the mask harness is recorded and proven on both import shapes with two non-vacuity controls, before any plan automates it.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the two conversions: the changed-line census, no assert or @preview line, black, repo-wide ruff, mypy stdout and the full pytest result against the baseline</name>
  <precondition>Task 1 is committed with both pilot keys EQUAL, both control keys DIFFER, and no HALT heading.</precondition>
  <reversibility rating="reversible">Measurement and evidence only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PYTEST_RESULT_BEFORE`, `MYPY_STDOUT_SHA256_BEFORE`)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md (§ Leg (c): the changed-line census form)
  </read_first>
  <action>
1. **`## Changed-line census`.** For the two files, quote each check and its result:
   - The `git diff "$PHASE_BASE_SHA" HEAD` added and removed lines, with headers and blank lines excluded. Every one matches `(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)`. Record `NON_TYPING_LINES_70_04 = 0`.
   - No changed line contains the word `assert`. Record `ASSERT_LINES_70_04 = 0`.
   - No changed line contains `@preview`.
   - `git diff --quiet "$PHASE_BASE_SHA" HEAD -- tests/test_authors_pipeline_stage_gate.py` exits 0.

2. **`## Gates`.** Quote each command with its exit code:
   - `uv run black --check typsphinx/translator.py tests/test_include_ledger_removal_gate.py` (check only)
   - `uv run ruff check .` (repo-wide, config rules)
   - `uv run mypy typsphinx/ 2>/dev/null`: record `MYPY_STDOUT_SHA256_70_04 = `, which must equal `MYPY_STDOUT_SHA256_BEFORE`
   - `LC_ALL=C uv run pytest -q -rs -p no:cacheprovider > "$S/pytest.out" 2>&1; echo "exit:$?"` must print `exit:0`. Record `PYTEST_RESULT_70_04 = ` from the last line with the same token extraction 70-02 used; it must equal `PYTEST_RESULT_BEFORE`.

3. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-MASK-PILOT-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; P="typsphinx/translator.py tests/test_include_ledger_removal_gate.py" &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -vE '^[+-][[:space:]]*$' | grep -qvE '(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)' &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -qw assert &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -qF '@preview' &amp;&amp; git diff --quiet "$B" HEAD -- tests/test_authors_pipeline_stage_gate.py &amp;&amp; uv run black --check $P &amp;&amp; uv run ruff check . &amp;&amp; [ "$(uv run mypy typsphinx/ 2&gt;/dev/null | sha256sum | cut -d' ' -f1)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; [ "$(k MYPY_STDOUT_SHA256_70_04)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; R="$(LC_ALL=C uv run pytest -q -p no:cacheprovider 2&gt;&amp;1 | tail -n 1 | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" &amp;&amp; [ -n "$R" ] &amp;&amp; [ "$R" = "$(k PYTEST_RESULT_BEFORE "$BE")" ] &amp;&amp; [ "$(k PYTEST_RESULT_70_04)" = "$R" ] &amp;&amp; [ "$(k NON_TYPING_LINES_70_04)" = 0 ] &amp;&amp; [ "$(k ASSERT_LINES_70_04)" = 0 ] &amp;&amp; for s in '## Changed-line census' '## Gates'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$(k BASE_70_04)" HEAD | grep -vxF -e typsphinx/translator.py -e tests/test_include_ledger_removal_gate.py -e "$F" -e "$D/70-04-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- a changed non-blank line without a typing name
- a changed line containing `assert` or `@preview`
- the authors pipeline gate changed
- black reporting either file, or `ruff check .` reporting anything
- the mypy stdout hash differing from base
- the full pytest result differing from `PYTEST_RESULT_BEFORE`
- a census key not 0
- a section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `NON_TYPING_LINES_70_04 = 0` and `ASSERT_LINES_70_04 = 0`; the diff has no `@preview` line; `tests/test_authors_pipeline_stage_gate.py` is unchanged.
    - `uv run black --check` on the two files and `uv run ruff check .` exit 0.
    - `MYPY_STDOUT_SHA256_70_04` equals `MYPY_STDOUT_SHA256_BEFORE`, and `PYTEST_RESULT_70_04` equals `PYTEST_RESULT_BEFORE`.
  </acceptance_criteria>
  <done>The tracer conversion is lint-green under the still-present ignores, changes only typing lines, and leaves mypy and the test suite exactly as they were.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruff --fix → source | an automated rewrite reaches product code |
| mask harness → every later equality verdict | a vacuous or wrong mask would certify any change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-08 | Tampering | a non-annotation change hidden inside the conversion | medium | mitigate | Masked hash equal to base, a changed-line census with no non-typing line, and the full pytest and mypy results equal to base. |
| T-70-09 | Repudiation | a vacuous mask certifying anything | high | mitigate | Two scratch controls (a class rename, a removed non-typing import) must change the hash; the harness is hash-pinned (`MASK_HARNESS_SHA256`) so later plans run exactly this text. |
| T-70-10 | Tampering | `--preview` or `--unsafe-fixes` widening the rewrite | medium | mitigate | Prohibited; only the two scoped passes run, and the diff census shows typing lines only. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan modifies
`typsphinx/translator.py` and `tests/test_include_ledger_removal_gate.py`, and creates
`70-MASK-PILOT-EVIDENCE.md`. That file holds the `Mask` harness block (`~~~python mask-harness`),
the `sk` helper definition, and the keys `BASE_70_04`, `SCRATCH_70_04`, `VENV_HOME`,
`VENV_VERSION_INFO`, `MASK_HARNESS_SHA256`, `PILOT_TRANSLATOR`, `PILOT_LEDGER`, `CONTROL_RENAME`,
`CONTROL_IMPORT`, `NON_TYPING_LINES_70_04`, `ASSERT_LINES_70_04`, `MYPY_STDOUT_SHA256_70_04` and
`PYTEST_RESULT_70_04`.
</artifacts_this_phase_produces>

<verification>
- Two real conversions, scoped and lint-green under the ignores.
- A hash-pinned mask harness, piloted EQUAL on both import shapes and shown non-vacuous, committed
  before wave 3 automates it.
</verification>

<success_criteria>
SC#3's pilot requirement is met, and the tracer proves the whole convert → mask → gate path end to
end, on one source file and one test file.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-04-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
