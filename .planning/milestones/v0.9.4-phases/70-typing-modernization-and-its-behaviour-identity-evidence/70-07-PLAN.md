---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 07
type: execute
wave: 3
depends_on: ["70-01", "70-02", "70-04"]
files_modified:
  - typsphinx/writer.py
  - typsphinx/__init__.py
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-WRITER-INIT-EVIDENCE.md
autonomous: true
requirements: [QUA-11, QUA-12]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - '`typsphinx/writer.py` and `typsphinx/__init__.py` are converted by the two scoped ruff passes while the config ignores are still present. Then exactly one hand edit is made, with the Edit tool: `typsphinx/__init__.py` line 15 changes from `from typing import Any, Dict` to `from typing import Any`, because ruff gates the `__init__.py` F401 fix behind --preview, which is never enabled.'
    - 'After the hand edit, `uv run ruff check <both> --select UP006,UP035` and `uv run ruff check <both>` both exit 0. `sed -n 15p typsphinx/__init__.py` prints exactly `from typing import Any`, and writer.py''s typing line is `from typing import Any`.'
    - 'Leg (a), automated: each file''s masked hash equals the hash of its `git show PHASE_BASE_SHA:<path>`, computed with the hash-pinned harness from `70-MASK-PILOT-EVIDENCE.md`.'
    - 'writer.py''s four `#import "@preview/…"` lines are byte-identical to base (ROADMAP constraint 5). Every changed non-blank line in the two files carries a typing name.'
    - 'Edge QUA-11/concurrency: this plan runs in parallel with 70-05, 70-06 and 70-08 on disjoint files. Its branch changes only these two files and its own evidence and SUMMARY.'
    - 'mypy stdout hashes equal to MYPY_STDOUT_SHA256_BEFORE, and the full pytest result equals PYTEST_RESULT_BEFORE.'
  artifacts:
    - path: typsphinx/__init__.py
      provides: 'The one hand-edited survivor line'
      contains: 'from typing import Any'
    - path: typsphinx/writer.py
      provides: 'Builtin generics throughout'
      contains: 'from typing import Any'
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-WRITER-INIT-EVIDENCE.md
      provides: 'Conversion transcript including the survivor and the hand edit, automated leg (a) hashes, changed-line census, gates'
      contains: 'INIT_LINE_15 = from typing import Any'
  key_links:
    - from: 'typsphinx/__init__.py:15'
      to: 'bare `ruff check .` after 70-09 removes the ignores'
      via: 'the hand edit removes the unused `Dict`, the only survivor ruff cannot fix without --preview'
      pattern: '^from typing import Any$'
  prohibitions:
    - statement: 'MUST NOT pass --preview or --unsafe-fixes to remove the `__init__.py` import, use sed on any source, edit an `@preview` version-sync line in writer.py, add `from __future__ import annotations`, or rewrite any Optional/Union/`X | None`.'
      requirement_id: QUA-11
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Convert `typsphinx/writer.py` and `typsphinx/__init__.py` onto builtin generics under the still-present ignores. This includes the single documented hand edit at `typsphinx/__init__.py:15` (ROADMAP constraint 5; QUA-11; SC#2's `__init__.py:15` clause). The plan proves leg (a) with the pinned harness and gates on mypy and the full suite.

Purpose: `__init__.py` is the one file where `ruff --fix` cannot finish. Ruff treats package imports as probable re-exports and withholds the F401 fix unless `--preview` is on. writer.py holds four of the twelve `@preview` version-sync lines.

Output: the two converted files, committed alone, and `70-CONV-WRITER-INIT-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every verify
command with the Bash tool `timeout` set to 600000. Run pytest under `LC_ALL=C`.

**Harness.** Extract it exactly as 70-04 defined:
`H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')"`, where `PE` is
`70-MASK-PILOT-EVIDENCE.md`. Check that `printf '%s\n' "$H" | sha256sum` equals
`MASK_HARNESS_SHA256`, then run it with `sk() { uv run python -c "$H"; }`. Never retype it.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value. `INIT_LINE_15 = from typing import Any` is the
one key whose value holds spaces; nothing may follow it on that line.

**Commits.** Stage files by explicit path, never `git add -A`. The conversion commit touches only
the two source files. On any failure path, write `## HALT: <reason>` into the evidence file, commit
it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: assert -->
<!-- planner-discipline-allow: @preview -->
<!-- planner-discipline-allow: --preview -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: convert writer.py and __init__.py with the two scoped ruff passes, record the __init__.py:15 survivor, make the one hand edit, and prove leg (a) for both</name>
  <precondition>`70-MASK-PILOT-EVIDENCE.md` holds `PILOT_TRANSLATOR = EQUAL`, `PILOT_LEDGER = EQUAL`, `CONTROL_RENAME = DIFFER` and `CONTROL_IMPORT = DIFFER` with no HALT heading, and its harness block hashes to `MASK_HARNESS_SHA256`. The single CLAUDE.md commit since `PHASE_BASE_SHA` is an ancestor of HEAD. `git diff --quiet "$PHASE_BASE_SHA" HEAD -- typsphinx/writer.py typsphinx/__init__.py` exits 0. Otherwise write `## HALT: preconditions` and stop.</precondition>
  <reversibility rating="reversible">Annotation and import rewrites plus one import-line hand edit, in two git-tracked files.</reversibility>
  <files>typsphinx/writer.py, typsphinx/__init__.py, .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-WRITER-INIT-EVIDENCE.md</files>
  <read_first>
    - typsphinx/__init__.py (lines 14-16 and every `Dict` use)
    - typsphinx/writer.py (line 10's typing import, lines 264-270 with the four `@preview` import strings, and every `Tuple` use)
    - .planning/research/STACK.md (the `__init__.py` F401 carve-out, and why `--preview` is refused)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md (§ typsphinx/__init__.py)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md (§ Mask harness)
  </read_first>
  <action>
1. Create `70-CONV-WRITER-INIT-EVIDENCE.md` with the heading `# Phase 70 — Conversion: writer.py and __init__.py`. Before any commit, record `BASE_70_07 = ` from `git rev-parse HEAD`. Then record `VENV_HOME = ` and `VENV_VERSION_INFO = ` after provisioning; both must equal 70-02's. Quote the precondition checks under `## Head check and provisioning`.

2. **`## Conversion`.** With `P="typsphinx/writer.py typsphinx/__init__.py"`:
   - Record `uv run ruff check $P --select UP006,UP035 --output-format=concise` and its non-zero count.
   - Run pass 1, `uv run ruff check $P --select UP006,UP035 --fix`, then pass 2, `uv run ruff check $P --fix`.
   - Quote what remains of `uv run ruff check $P --select UP006,UP035` and of `uv run ruff check $P`. Expect exactly the `__init__.py:15` survivor: one UP035 under `--select`, and one F401 with no fix offered under the config rules. Record `INIT_SURVIVOR_COUNT = ` with the number of remaining diagnostics, all of which must be on `typsphinx/__init__.py` line 15. Anything else remaining is a finding: write `## HALT: unexpected residue` and stop.

3. **`## Hand edit`.** Use the Edit tool to change `typsphinx/__init__.py` line 15 from `from typing import Any, Dict` to `from typing import Any`. This is the only manual line edit in the phase. Never use `sed`, `--preview` or `--unsafe-fixes`. Then:
   - record `INIT_LINE_15 = ` followed by `sed -n 15p typsphinx/__init__.py`
   - record that `uv run ruff check $P --select UP006,UP035` and `uv run ruff check $P` both exit 0
   - commit the two files alone, with the message `refactor(70-07): move writer.py and __init__.py onto builtin generics (QUA-11)`

4. **`## Leg (a)`.** For each file, quote the base hash and the current hash from `sk`. If each pair is equal, record `WRITER_MASK = EQUAL` and `INIT_MASK = EQUAL`. Any difference: write `## HALT: mask` and stop.

5. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CONV-WRITER-INIT-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; PE="$D/70-MASK-PILOT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = "$(k VENV_VERSION_INFO "$BE")" ] &amp;&amp; C="$(git log --format=%H "$B"..HEAD -- CLAUDE.md)" &amp;&amp; [ "$(printf '%s\n' "$C" | grep -c .)" = 1 ] &amp;&amp; git merge-base --is-ancestor "$C" "$(k BASE_70_07)" &amp;&amp; [ "$(k PILOT_LEDGER "$PE")" = EQUAL ] &amp;&amp; [ "$(k CONTROL_RENAME "$PE")" = DIFFER ] &amp;&amp; H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')" &amp;&amp; [ -n "$H" ] &amp;&amp; [ "$(printf '%s\n' "$H" | sha256sum | cut -d' ' -f1)" = "$(k MASK_HARNESS_SHA256 "$PE")" ] &amp;&amp; sk() { uv run python -c "$H"; } &amp;&amp; for f in typsphinx/writer.py typsphinx/__init__.py; do a="$(sk &lt; "$f")" &amp;&amp; [ "${#a}" = 64 ] &amp;&amp; [ "$a" = "$(git show "$B:$f" | sk)" ] || { echo "mask differs: $f"; exit 1; }; ! git diff --quiet "$B" HEAD -- "$f" || { echo "unchanged: $f"; exit 1; }; done &amp;&amp; [ "$(sed -n 15p typsphinx/__init__.py)" = 'from typing import Any' ] &amp;&amp; [ "$(k INIT_LINE_15)" = 'from typing import Any' ] &amp;&amp; [ "$(git show "$B:typsphinx/__init__.py" | sed -n 15p)" = 'from typing import Any, Dict' ] &amp;&amp; grep -qxF 'from typing import Any' typsphinx/writer.py &amp;&amp; [ "$(k WRITER_MASK)" = EQUAL ] &amp;&amp; [ "$(k INIT_MASK)" = EQUAL ] &amp;&amp; [ -n "$(k INIT_SURVIVOR_COUNT)" ] &amp;&amp; uv run ruff check typsphinx/writer.py typsphinx/__init__.py --select UP006,UP035 &amp;&amp; uv run ruff check typsphinx/writer.py typsphinx/__init__.py &amp;&amp; for s in '## Head check and provisioning' '## Conversion' '## Hand edit' '## Leg (a)'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or a `mask differs:`, `unchanged:` or `missing section:` line printed. Each of these makes it fail:
- the interpreter differing from 70-02's
- the CLAUDE.md rewrite not an ancestor
- the pilot keys wrong, or the harness not hashing to its key
- either file's masked hash differing from base, or either file unchanged
- `__init__.py` line 15 not exactly `from typing import Any`, or base line 15 not the documented survivor
- writer.py's typing line wrong
- a mask key wrong, or `INIT_SURVIVOR_COUNT` missing
- ruff still reporting anything on the two files
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n 15p typsphinx/__init__.py` prints `from typing import Any`, and base line 15 was `from typing import Any, Dict`.
    - `uv run ruff check typsphinx/writer.py typsphinx/__init__.py`, with and without `--select UP006,UP035`, exits 0.
    - Both masked hashes equal base's, with `WRITER_MASK = EQUAL` and `INIT_MASK = EQUAL`.
    - `## Conversion` records the `__init__.py:15` survivor before the hand edit, and nothing else remaining.
  </acceptance_criteria>
  <done>writer.py and __init__.py are on builtin generics, and the one survivor is closed by the one documented hand edit, with no --preview anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the conversion: changed-line census, writer.py's @preview lines byte-identical, black, repo-wide ruff, mypy stdout and the full pytest result against the baseline</name>
  <precondition>Task 1 is committed with both mask keys EQUAL and no HALT heading.</precondition>
  <reversibility rating="reversible">Measurement and evidence only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-WRITER-INIT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PYTEST_RESULT_BEFORE`, `MYPY_STDOUT_SHA256_BEFORE`)
    - tests/test_preview_version_sync.py (what the suite asserts about the `@preview` sites)
  </read_first>
  <action>
1. **`## Changed-line census`.** Over `git diff "$PHASE_BASE_SHA" HEAD` for the two files, with headers and blank lines excluded, quote each check:
   - Every added and removed line matches `(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)`. Record `NON_TYPING_LINES_70_07 = 0`.
   - No changed line contains `assert` or `@preview`.
   - The `git grep -n '@preview/' -- typsphinx/writer.py` output is byte-identical at `PHASE_BASE_SHA` and HEAD.

2. **`## Gates`.** Quote each command with its exit code:
   - `uv run black --check typsphinx/writer.py typsphinx/__init__.py`
   - `uv run ruff check .`
   - `uv run mypy typsphinx/ 2>/dev/null`, recording `MYPY_STDOUT_SHA256_70_07 = `, which must equal `MYPY_STDOUT_SHA256_BEFORE`
   - `LC_ALL=C uv run pytest -q -rs -p no:cacheprovider`, which must exit 0, recording `PYTEST_RESULT_70_07 = `, which must equal `PYTEST_RESULT_BEFORE`

3. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CONV-WRITER-INIT-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; P="typsphinx/writer.py typsphinx/__init__.py" &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -vE '^[+-][[:space:]]*$' | grep -qvE '(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)' &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -qwE 'assert|@preview' &amp;&amp; W="$(git grep -n '@preview/' HEAD -- typsphinx/writer.py | cut -d: -f2-)" &amp;&amp; [ -n "$W" ] &amp;&amp; [ "$(git grep -n '@preview/' "$B" -- typsphinx/writer.py | cut -d: -f2-)" = "$W" ] &amp;&amp; uv run black --check $P &amp;&amp; uv run ruff check . &amp;&amp; [ "$(uv run mypy typsphinx/ 2&gt;/dev/null | sha256sum | cut -d' ' -f1)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; [ "$(k MYPY_STDOUT_SHA256_70_07)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; R="$(LC_ALL=C uv run pytest -q -p no:cacheprovider 2&gt;&amp;1 | tail -n 1 | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" &amp;&amp; [ -n "$R" ] &amp;&amp; [ "$R" = "$(k PYTEST_RESULT_BEFORE "$BE")" ] &amp;&amp; [ "$(k PYTEST_RESULT_70_07)" = "$R" ] &amp;&amp; [ "$(k NON_TYPING_LINES_70_07)" = 0 ] &amp;&amp; for s in '## Changed-line census' '## Gates'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$(k BASE_70_07)" HEAD | grep -vxF -e typsphinx/writer.py -e typsphinx/__init__.py -e "$F" -e "$D/70-07-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- a changed non-blank line without a typing name, or containing `assert` or `@preview`
- writer.py's `@preview/` lines absent or differing from base
- black or ruff reporting anything
- the mypy stdout hash or the pytest result differing from base
- a key wrong
- a section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `NON_TYPING_LINES_70_07 = 0`, and writer.py's `@preview/` lines are byte-identical to base.
    - `uv run black --check` on both files and `uv run ruff check .` exit 0.
    - `MYPY_STDOUT_SHA256_70_07` and `PYTEST_RESULT_70_07` equal their base keys.
  </acceptance_criteria>
  <done>writer.py and __init__.py are lint-green under the ignores, the `@preview` sync lines are untouched, and mypy and the suite are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruff --fix and one hand edit → the extension's entry point and writer | `setup()` registers every builder and config value; the writer emits every file's imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-15 | Tampering | `--preview` enabled to clear the survivor, widening fixes project-wide | medium | mitigate | Prohibited; the survivor is closed by one Edit-tool change, recorded as `INIT_LINE_15`. |
| T-70-16 | Tampering | writer.py's `@preview` import strings | medium | mitigate | `@preview/` lines compared byte-for-byte with base; `tests/test_preview_version_sync.py` runs in the suite. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan modifies
`typsphinx/writer.py` and `typsphinx/__init__.py`, and creates `70-CONV-WRITER-INIT-EVIDENCE.md`,
with the keys `BASE_70_07`, `VENV_HOME`, `VENV_VERSION_INFO`, `INIT_SURVIVOR_COUNT`,
`INIT_LINE_15`, `WRITER_MASK`, `INIT_MASK`, `NON_TYPING_LINES_70_07`, `MYPY_STDOUT_SHA256_70_07`
and `PYTEST_RESULT_70_07`.
</artifacts_this_phase_produces>

<verification>
Both files carry zero UP006/UP035 findings. `__init__.py:15` reads `from typing import Any`, the
masked hashes equal base, and mypy, pytest and the `@preview` lines are unchanged.
</verification>

<success_criteria>
The one hand edit the phase allows is made and evidenced. This file-disjoint wave-3 conversion lands
lint-green, ready for 70-09's flip.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-07-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
