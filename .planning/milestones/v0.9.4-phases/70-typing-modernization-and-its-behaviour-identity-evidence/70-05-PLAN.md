---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 05
type: execute
wave: 3
depends_on: ["70-01", "70-02", "70-04"]
files_modified:
  - typsphinx/builder.py
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-BUILDER-EVIDENCE.md
autonomous: true
requirements: [QUA-11, QUA-12]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - '`typsphinx/builder.py` is converted by the two scoped ruff passes (pass 1 `--select UP006,UP035 --fix`, pass 2 `--fix` with the config rules) while the config ignores are still present. Afterwards `uv run ruff check typsphinx/builder.py --select UP006,UP035` and `uv run ruff check typsphinx/builder.py` both exit 0.'
    - 'builder.py''s typing import line reads `from typing import Any`, and its existing `from collections.abc import Iterator` line is untouched.'
    - 'Leg (a), automated: the masked hash of builder.py equals the hash of `git show PHASE_BASE_SHA:typsphinx/builder.py`, computed with the harness extracted from `70-MASK-PILOT-EVIDENCE.md` and checked against MASK_HARNESS_SHA256.'
    - 'Every changed non-blank line carries a typing name. The `@preview` lines are untouched. Docstring prose such as "Iterator of document names" and "Set of document names" stays byte-identical, since docstrings are inside the masked hash.'
    - 'Edge QUA-11/concurrency: this plan runs in parallel with 70-06, 70-07 and 70-08 on disjoint files. Its branch changes only builder.py and its own evidence and SUMMARY, measured with `git diff --name-only BASE_70_05 HEAD`.'
    - 'mypy stdout hashes equal to MYPY_STDOUT_SHA256_BEFORE, and the full pytest result equals PYTEST_RESULT_BEFORE.'
  artifacts:
    - path: typsphinx/builder.py
      provides: 'Builtin generics throughout'
      contains: 'from typing import Any'
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-BUILDER-EVIDENCE.md
      provides: 'Conversion transcript, automated leg (a) hash, changed-line census, gates'
      contains: 'BUILDER_MASK = EQUAL'
  key_links:
    - from: '70-MASK-PILOT-EVIDENCE.md ~~~python mask-harness'
      to: 'this plan''s leg (a) check'
      via: 'sed extraction, sha256 equal to MASK_HARNESS_SHA256, uv run python -c'
      pattern: 'MASK_HARNESS_SHA256'
  prohibitions:
    - statement: 'MUST NOT use sed or any text substitution on source, pass --preview or --unsafe-fixes, add `from __future__ import annotations`, rewrite any Optional/Union/`X | None`, edit an `@preview` version-sync line, or run ruff --fix or black in write mode on any file but typsphinx/builder.py.'
      requirement_id: QUA-11
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Convert `typsphinx/builder.py` onto builtin generics under the still-present ignores (ROADMAP constraints 1(ii) and 5; QUA-11). The plan also proves leg (a) with the harness 70-04 piloted, now used as an automated check, and gates on mypy and the full suite.

Purpose: this is one of four file-disjoint wave-3 conversions. The pyproject flip in 70-09 needs every file clean first, so that each merged state of the branch stays `ruff check .`-green.

Output: the converted builder.py, committed alone, and `70-CONV-BUILDER-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every verify
command with the Bash tool `timeout` set to 600000. Run pytest under `LC_ALL=C`.

**Harness.** Extract it exactly as 70-04 defined:
`H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')"`, where `PE` is
`70-MASK-PILOT-EVIDENCE.md`. Check that `printf '%s\n' "$H" | sha256sum` equals
`MASK_HARNESS_SHA256`, then run it with `sk() { uv run python -c "$H"; }`. Never retype it.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value.

**Commits.** Stage files by explicit path, never `git add -A`. The conversion commit touches only
`typsphinx/builder.py`. On any failure path, write `## HALT: <reason>` into the evidence file, commit
it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: assert -->
<!-- planner-discipline-allow: @preview -->
<!-- planner-discipline-allow: --preview -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: convert typsphinx/builder.py with the two scoped ruff passes and prove leg (a) with the pinned mask harness</name>
  <precondition>`70-MASK-PILOT-EVIDENCE.md` holds `PILOT_TRANSLATOR = EQUAL`, `PILOT_LEDGER = EQUAL`, `CONTROL_RENAME = DIFFER` and `CONTROL_IMPORT = DIFFER` with no HALT heading, and its harness block hashes to `MASK_HARNESS_SHA256`. The single CLAUDE.md commit since `PHASE_BASE_SHA` is an ancestor of HEAD. `git diff --quiet "$PHASE_BASE_SHA" HEAD -- typsphinx/builder.py` exits 0. Otherwise write `## HALT: preconditions` and stop.</precondition>
  <reversibility rating="reversible">Annotation and import rewrites in one git-tracked file, proven structure-preserving by the harness.</reversibility>
  <files>typsphinx/builder.py, .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-BUILDER-EVIDENCE.md</files>
  <read_first>
    - typsphinx/builder.py (lines 8-13: the imports; line 11 `from collections.abc import Iterator` stays)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md (§ Mask harness: the block, the key, and the `sk` definition)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md (§ typsphinx/builder.py)
  </read_first>
  <action>
1. Create `70-CONV-BUILDER-EVIDENCE.md` with the heading `# Phase 70 — Conversion: typsphinx/builder.py`. Before any commit, record `BASE_70_05 = ` from `git rev-parse HEAD`. Then record `VENV_HOME = ` and `VENV_VERSION_INFO = ` after provisioning; both must equal 70-02's. Quote the precondition checks under `## Head check and provisioning`.

2. **`## Conversion`.**
   - Record `uv run ruff check typsphinx/builder.py --select UP006,UP035 --output-format=concise` and its non-zero finding count.
   - Run pass 1, `uv run ruff check typsphinx/builder.py --select UP006,UP035 --fix`, then pass 2, `uv run ruff check typsphinx/builder.py --fix`. Never use `sed`, `--preview` or `--unsafe-fixes`.
   - Record that both `uv run ruff check typsphinx/builder.py --select UP006,UP035` and `uv run ruff check typsphinx/builder.py` exit 0.
   - Quote the typing and `collections.abc` import lines.
   - Commit builder.py alone, with the message `refactor(70-05): move builder.py onto builtin generics (QUA-11)`.

3. **`## Leg (a)`.** Quote `git show "$PHASE_BASE_SHA:typsphinx/builder.py" | sk` and `sk < typsphinx/builder.py`. If they are equal, record `BUILDER_MASK = EQUAL`. Otherwise record `DIFFER`, write `## HALT: mask`, and stop.

4. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CONV-BUILDER-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; PE="$D/70-MASK-PILOT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = "$(k VENV_VERSION_INFO "$BE")" ] &amp;&amp; C="$(git log --format=%H "$B"..HEAD -- CLAUDE.md)" &amp;&amp; [ "$(printf '%s\n' "$C" | grep -c .)" = 1 ] &amp;&amp; git merge-base --is-ancestor "$C" "$(k BASE_70_05)" &amp;&amp; [ "$(k PILOT_LEDGER "$PE")" = EQUAL ] &amp;&amp; [ "$(k CONTROL_RENAME "$PE")" = DIFFER ] &amp;&amp; H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')" &amp;&amp; [ -n "$H" ] &amp;&amp; [ "$(printf '%s\n' "$H" | sha256sum | cut -d' ' -f1)" = "$(k MASK_HARNESS_SHA256 "$PE")" ] &amp;&amp; sk() { uv run python -c "$H"; } &amp;&amp; f=typsphinx/builder.py &amp;&amp; a="$(sk &lt; "$f")" &amp;&amp; [ "${#a}" = 64 ] &amp;&amp; [ "$a" = "$(git show "$B:$f" | sk)" ] &amp;&amp; ! git diff --quiet "$B" HEAD -- "$f" &amp;&amp; [ "$(k BUILDER_MASK)" = EQUAL ] &amp;&amp; uv run ruff check "$f" --select UP006,UP035 &amp;&amp; uv run ruff check "$f" &amp;&amp; grep -qxF 'from typing import Any' "$f" &amp;&amp; grep -qxF 'from collections.abc import Iterator' "$f" &amp;&amp; for s in '## Head check and provisioning' '## Conversion' '## Leg (a)'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- the interpreter differing from 70-02's
- the CLAUDE.md rewrite not an ancestor
- the pilot keys wrong, or the harness not hashing to `MASK_HARNESS_SHA256`
- builder.py's masked hash differing from base, or builder.py unchanged
- ruff reporting UP006/UP035 or any config-rule finding
- the typing line not exactly `from typing import Any`, or the `collections.abc` line missing
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check typsphinx/builder.py --select UP006,UP035` and `uv run ruff check typsphinx/builder.py` exit 0.
    - builder.py has `from typing import Any` and `from collections.abc import Iterator`.
    - The masked hash of builder.py equals base's, computed with the hash-pinned harness, and `BUILDER_MASK = EQUAL`.
  </acceptance_criteria>
  <done>builder.py is on builtin generics and structurally identical to the base once annotations and typing imports are masked.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the conversion: changed-line census, no assert or @preview line, black, repo-wide ruff, mypy stdout and the full pytest result against the baseline</name>
  <precondition>Task 1 is committed with `BUILDER_MASK = EQUAL` and no HALT heading.</precondition>
  <reversibility rating="reversible">Measurement and evidence only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-BUILDER-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PYTEST_RESULT_BEFORE`, `MYPY_STDOUT_SHA256_BEFORE`)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md (§ Changed-line census and § Gates: the shape to mirror)
  </read_first>
  <action>
1. **`## Changed-line census`.** Over `git diff "$PHASE_BASE_SHA" HEAD -- typsphinx/builder.py`, with headers and blank lines excluded, quote each check:
   - Every added and removed line matches `(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)`. Record `NON_TYPING_LINES_70_05 = 0`.
   - No changed line contains `assert` or `@preview`.

2. **`## Gates`.** Quote each command with its exit code:
   - `uv run black --check typsphinx/builder.py`
   - `uv run ruff check .`
   - `uv run mypy typsphinx/ 2>/dev/null`, recording `MYPY_STDOUT_SHA256_70_05 = `, which must equal `MYPY_STDOUT_SHA256_BEFORE`
   - `LC_ALL=C uv run pytest -q -rs -p no:cacheprovider`, which must exit 0, recording `PYTEST_RESULT_70_05 = ` with 70-02's token extraction; it must equal `PYTEST_RESULT_BEFORE`

3. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CONV-BUILDER-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; P=typsphinx/builder.py &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -vE '^[+-][[:space:]]*$' | grep -qvE '(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)' &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -qwE 'assert|@preview' &amp;&amp; uv run black --check $P &amp;&amp; uv run ruff check . &amp;&amp; [ "$(uv run mypy typsphinx/ 2&gt;/dev/null | sha256sum | cut -d' ' -f1)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; [ "$(k MYPY_STDOUT_SHA256_70_05)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; R="$(LC_ALL=C uv run pytest -q -p no:cacheprovider 2&gt;&amp;1 | tail -n 1 | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" &amp;&amp; [ -n "$R" ] &amp;&amp; [ "$R" = "$(k PYTEST_RESULT_BEFORE "$BE")" ] &amp;&amp; [ "$(k PYTEST_RESULT_70_05)" = "$R" ] &amp;&amp; [ "$(k NON_TYPING_LINES_70_05)" = 0 ] &amp;&amp; for s in '## Changed-line census' '## Gates'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$(k BASE_70_05)" HEAD | grep -vxF -e "$P" -e "$F" -e "$D/70-05-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- a changed non-blank line without a typing name, or containing `assert` or `@preview`
- black or ruff reporting anything
- the mypy stdout hash differing from base
- the pytest result differing from `PYTEST_RESULT_BEFORE`
- a key wrong
- a section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `NON_TYPING_LINES_70_05 = 0`, and the diff has no `assert` or `@preview` line.
    - `uv run black --check typsphinx/builder.py` and `uv run ruff check .` exit 0.
    - `MYPY_STDOUT_SHA256_70_05` equals `MYPY_STDOUT_SHA256_BEFORE`, and `PYTEST_RESULT_70_05` equals `PYTEST_RESULT_BEFORE`.
    - `git diff --name-only BASE_70_05 HEAD` lists only builder.py, this evidence file and the SUMMARY.
  </acceptance_criteria>
  <done>builder.py's conversion is lint-green under the ignores and leaves mypy and the test suite exactly as they were.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruff --fix → typsphinx/builder.py | an automated rewrite reaches the builder's write loop and bundle copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-11 | Tampering | a non-annotation change in builder.py | medium | mitigate | Masked hash equal to base with the pinned harness; changed-line census; mypy stdout and full pytest equal to base. |
| T-70-12 | Tampering | the `@preview` version-sync lines | medium | mitigate | The census rejects any changed `@preview` line, and `tests/test_preview_version_sync.py` runs in the full suite. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan modifies
`typsphinx/builder.py` and creates `70-CONV-BUILDER-EVIDENCE.md`, with the keys `BASE_70_05`,
`VENV_HOME`, `VENV_VERSION_INFO`, `BUILDER_MASK`, `NON_TYPING_LINES_70_05`,
`MYPY_STDOUT_SHA256_70_05` and `PYTEST_RESULT_70_05`.
</artifacts_this_phase_produces>

<verification>
builder.py carries zero UP006/UP035 findings, its masked hash equals base, and mypy and pytest are
unchanged.
</verification>

<success_criteria>
One of the four file-disjoint wave-3 conversions lands lint-green, ready for 70-09's flip.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-05-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
