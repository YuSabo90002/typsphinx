---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 12
type: execute
wave: 5
depends_on: ["70-02", "70-03", "70-09"]
files_modified:
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOCS-DIFF-EVIDENCE.md
autonomous: true
requirements: [DOC-23]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-09: three clean builds of docs/source run in one detached worktree W, at one absolute path, each preceded by `rm -rf docs/_build` and each running the tox docs-html and docs-pdf commands under `LC_ALL=C`: A (base), B (base again), then C (the post-flip tip, after `git checkout --detach` in W and a fresh `uv sync`). Every build exits 0, and the PDF binary is never compared.'
    - 'Edge DOC-23/concurrency: A, B and C run one after another in the same worktree, never in parallel, and each is copied to scratch before the next `rm -rf docs/_build`. W is removed and pruned at the end of Task 1, and no branch is created for it.'
    - 'SC#4 control: A and B are identical in both outputs. `diff -rq --exclude=.doctrees` over the HTML is empty (DOCS_HTML_CONTROL = EQUAL), and the `.typ` manifests are identical (DOCS_TYP_CONTROL = EQUAL). So no A-vs-C line can be written off as nondeterminism; a non-empty control halts the plan.'
    - 'The base build A is also checked against 70-03''s wave-1 manifests (DOCS_BASE_CROSSCHECK). A DIFFER is listed file by file and reported to the owner in the SUMMARY''s first section. It never excuses any A-vs-C hunk.'
    - 'D-11 (as amended) location rule: every HTML file that differs between A and C is under `api/` or under `_modules/typsphinx/`, and every differing `.typ` is under `api/`. No file appears or vanishes. A file outside those places halts the plan for the owner.'
    - 'D-10: the docs `.typ` is compared here and never in the leg (d) corpus, and it is expected to differ.'
    - 'D-11 classification: every `diff -U0` hunk of both outputs (A vs C) is recorded verbatim and has exactly one classification row. It is TRACED only when it is a Dict/List/Set/Tuple rename or a typing/collections.abc import-line change that traces to a named converted source line, including any intersphinx link target or title attached to the renamed type and shape changes such as `Optional[Set[str]]` becoming `set[str] | None` (research Pitfall 9). UNTRACED_HUNKS = 0; any UNTRACED hunk halts for the owner.'
    - 'Edge DOC-23/empty: DOCS_HTML_HUNKS is greater than zero. Zero would mean the conversion never reached the API reference, which contradicts the phase goal, and halts the plan. A zero DOCS_TYP_HUNKS is recorded as an owner finding, because D-09 expected the dogfood `.typ` to change.'
    - 'Edge DOC-23/encoding: both comparisons are byte-level on raw files, with no text extraction, so markup, link targets and titles are inside the compared bytes.'
  artifacts:
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOCS-DIFF-EVIDENCE.md
      provides: 'Build exits, the A/B control, the base cross-check, the differing-file lists, raw diffs, the per-hunk D-11 classification table, DOC23_VERDICT'
      contains: 'DOC23_VERDICT = MET'
  key_links:
    - from: 'docs/source/api/index.rst autodoc + sphinx_autodoc_typehints + sphinx.ext.viewcode'
      to: 'api/index.html, api/index.typ and _modules/typsphinx/*.html'
      via: 'the converted annotations render into the API reference and the viewcode source mirror'
      pattern: 'DIFF_HTML: '
  prohibitions:
    - statement: 'MUST NOT classify any hunk as nondeterminism, noise or environment, drop `_modules/` from the HTML comparison, compare extracted text instead of raw bytes, or compare the PDF binary (D-09, D-11).'
      requirement_id: DOC-23
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT build through the main checkout''s `.venv`, create a branch for W, or leave W registered after Task 1.'
      requirement_id: DOC-23
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Take DOC-23's clean docs diff and record it as evidence (DOC-23; SC#4; ROADMAP constraint 7; D-09, D-10, D-11 as amended).

The plan builds the base twice and the post-flip tip once. All three builds run in one worktree at one path, each clean. It then:
- shows the base-vs-base control is empty for both outputs
- confines the differing files to D-11's locations
- classifies every hunk of both diffs against a converted annotation

Purpose: "only API-reference type text changed" is a measured claim, not an assumption. Building everything at one path, with a same-path control, rules out location and nondeterminism as explanations before any hunk is read.

Output: `70-DOCS-DIFF-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CORPUS-DOCS-BASE-EVIDENCE.md
@tox.ini
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). The docs builds run in a second, detached worktree
`W`, provisioned with its own
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
(run inside `W`), never through this worktree's `.venv` or the main checkout's. Never run `uv sync`
in the main checkout. Run every verify command with the Bash tool `timeout` set to 600000. Run
every Sphinx build under `LC_ALL=C`.

**Scratch.** Make one scratch directory `S` with `mktemp -d` outside the repository, and record
`SCRATCH_70_12 = <path>`. `W` is `$S/docs-wt`. The build copies `$S/A`, `$S/B` and `$S/C` stay until
this plan's last verify passes.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value. `DIFF_HTML: <path>` and `DIFF_TYP: <path>`
lines hold one path each. Append raw diffs with shell redirection between `~~~diff` and `~~~` fence
lines.

**Commits.** Stage the evidence file by explicit path. On any failure path, write
`## HALT: <reason>` into the evidence file, remove `W` if it exists, commit, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: UNTRACED -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: build base A, base B and post-flip C clean in one detached worktree, prove the A/B control empty, cross-check A against wave 1, and confine the A-vs-C differing files to D-11's locations</name>
  <precondition>`70-FLIP-EVIDENCE.md` exists at HEAD with no HALT heading. `70-CORPUS-DOCS-BASE-EVIDENCE.md` holds `DOCS_HTML_EXIT_BEFORE = 0` and `DOCS_PDF_EXIT_BEFORE = 0`, and its `PHASE_BASE_SHA_SEEN` equals 70-02's `PHASE_BASE_SHA`. Otherwise write `## HALT: preconditions` and stop.</precondition>
  <reversibility rating="reversible">Builds into scratch; W is detached and removed at the end of the task.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOCS-DIFF-EVIDENCE.md</files>
  <read_first>
    - tox.ini (`[testenv:docs-html]` and `[testenv:docs-pdf]`: `changedir = docs`, `sphinx-build -b html source _build/html`, `sphinx-build -b typstpdf source _build/pdf`)
    - docs/source/conf.py (lines 35-44: viewcode and intersphinx are enabled; line 114: `autodoc_typehints = "description"`)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md (D-09, D-10, D-11 and its AMENDED block)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md (Pitfalls 8 and 9; § Contradictions C1)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CORPUS-DOCS-BASE-EVIDENCE.md (the two docs manifest keys)
  </read_first>
  <action>
1. Create `70-DOCS-DIFF-EVIDENCE.md` with the heading `# Phase 70 — DOC-23 Docs Diff`. Before any commit, record:
   - `BASE_70_12 = ` and `AFTER_SHA = `, both from `git rev-parse HEAD`. This is the post-flip tip this worktree forked from.
   - `FLIP_COMMIT = `, the only line of `git log --format=%H "$PHASE_BASE_SHA"..HEAD -- pyproject.toml`, which must be an ancestor of `AFTER_SHA`.

2. **`## Worktree W`.**
   - Run `git worktree add --detach "$S/docs-wt" "$PHASE_BASE_SHA"`. Do not create a branch.
   - Provision inside `W`. Record `W_VENV_HOME = ` and `W_VENV_VERSION_INFO = `; both must equal 70-02's `VENV_HOME` and `VENV_VERSION_INFO`.

3. **`## Builds`.** Define one build round `R` as follows. In `W/docs`:
   - run `rm -rf _build`
   - run `LC_ALL=C uv run sphinx-build -b html source _build/html > "$S/R-html.log" 2>&1; echo "exit:$?"`
   - run `LC_ALL=C uv run sphinx-build -b typstpdf source _build/pdf > "$S/R-pdf.log" 2>&1; echo "exit:$?"`
   - run `cp -a _build "$S/R"`

   Run the rounds in this order:
   - round A at `PHASE_BASE_SHA`
   - round B, again at `PHASE_BASE_SHA`
   - `git -C "$S/docs-wt" checkout --detach "$AFTER_SHA"`, then re-run the same provisioning inside `W`, then round C

   Record `BUILD_EXITS = ` with the six exit codes in order (A-html A-pdf B-html B-pdf C-html C-pdf), all 0. Record each log's `grep -c WARNING` for information.

4. **`## Control (A vs B)`.**
   - Record `diff -rq --exclude=.doctrees "$S/A/html" "$S/B/html"`, which must be empty.
   - Build `.typ` manifests for `$S/A/pdf` and `$S/B/pdf` with 70-03's manifest command and compare them with `cmp`.
   - Record `DOCS_HTML_CONTROL = EQUAL` and `DOCS_TYP_CONTROL = EQUAL`.

   Either non-empty: record `DIFFER`, list every differing file, write `## HALT: control not empty`, remove `W`, and stop.

5. **`## Base cross-check`.** Compute A's HTML manifest (excluding `./.doctrees/`) and A's `.typ` manifest with 70-03's commands, and compare their SHA-256 with `DOCS_HTML_MANIFEST_SHA256_BEFORE` and `DOCS_TYP_MANIFEST_SHA256_BEFORE`.
   - If both are equal, record `DOCS_BASE_CROSSCHECK = EQUAL`.
   - Otherwise record `DIFFER`, list the differing manifest lines, and state that the owner is told in the SUMMARY. This never excuses any A-vs-C hunk.

6. **`## Differing files (A vs C)`.**
   - Record `diff -rq --exclude=.doctrees "$S/A/html" "$S/C/html"`. Any `Only in` line is a finding: write `## HALT: file appeared or vanished`, remove `W`, and stop.
   - For each differing file, write one `DIFF_HTML: <path relative to html/>` line.
   - Compare the A and C `.typ` manifests the same way, writing `DIFF_TYP: <relative path>` lines.
   - Record `DOCS_HTML_DIFF_FILES = ` and `DOCS_TYP_DIFF_FILES = ` as the counts.
   - Location rule (D-11 as amended): every `DIFF_HTML` path starts with `api/` or `_modules/typsphinx/`, and every `DIFF_TYP` path starts with `api/`. Any other path: write `## HALT: D-11 location`, remove `W`, and stop.

7. Remove `W` with `git worktree remove --force "$S/docs-wt" && git worktree prune`, and quote `git worktree list`. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-DOCS-DIFF-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; CE="$D/70-CORPUS-DOCS-BASE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; [ "$(k PHASE_BASE_SHA_SEEN "$CE")" = "$B" ] &amp;&amp; A="$(k AFTER_SHA)" &amp;&amp; [ "${#A}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$(k FLIP_COMMIT)" "$A" &amp;&amp; git merge-base --is-ancestor "$A" HEAD &amp;&amp; [ "$(k W_VENV_VERSION_INFO)" = "$(k VENV_VERSION_INFO "$BE")" ] &amp;&amp; [ "$(k W_VENV_HOME)" = "$(k VENV_HOME "$BE")" ] &amp;&amp; [ "$(k BUILD_EXITS)" = '0 0 0 0 0 0' ] &amp;&amp; S="$(k SCRATCH_70_12)" &amp;&amp; [ -z "$(diff -rq --exclude=.doctrees "$S/A/html" "$S/B/html")" ] &amp;&amp; tm() { (cd "$1" &amp;&amp; find . -type f -name '*.typ' -print0 | LC_ALL=C sort -z | xargs -0 sha256sum); } &amp;&amp; [ "$(tm "$S/A/pdf")" = "$(tm "$S/B/pdf")" ] &amp;&amp; [ "$(k DOCS_HTML_CONTROL)" = EQUAL ] &amp;&amp; [ "$(k DOCS_TYP_CONTROL)" = EQUAL ] &amp;&amp; case "$(k DOCS_BASE_CROSSCHECK)" in EQUAL|DIFFER) true ;; *) false ;; esac &amp;&amp; R="$(diff -rq --exclude=.doctrees "$S/A/html" "$S/C/html")" &amp;&amp; ! printf '%s\n' "$R" | grep -q '^Only in' &amp;&amp; [ "$(printf '%s\n' "$R" | grep -c ' differ$')" = "$(k DOCS_HTML_DIFF_FILES)" ] &amp;&amp; [ "$(grep -c '^DIFF_HTML: ' "$F")" = "$(k DOCS_HTML_DIFF_FILES)" ] &amp;&amp; [ "$(k DOCS_HTML_DIFF_FILES)" -gt 0 ] &amp;&amp; ! grep '^DIFF_HTML: ' "$F" | sed 's/^DIFF_HTML: //' | grep -qvE '^(api/|_modules/typsphinx/)' &amp;&amp; ! grep '^DIFF_TYP: ' "$F" | sed 's/^DIFF_TYP: //' | grep -qv '^api/' &amp;&amp; [ "$(grep -c '^DIFF_TYP: ' "$F" || true)" = "$(k DOCS_TYP_DIFF_FILES)" ] &amp;&amp; [ "$(tm "$S/A/pdf" | awk '{print $2}')" = "$(tm "$S/C/pdf" | awk '{print $2}')" ] &amp;&amp; [ "$(diff &lt;(tm "$S/A/pdf") &lt;(tm "$S/C/pdf") | grep -c '^&lt;' || true)" = "$(k DOCS_TYP_DIFF_FILES)" ] &amp;&amp; ! git worktree list | grep -qF "$S/docs-wt" &amp;&amp; for s in '## Worktree W' '## Builds' '## Control (A vs B)' '## Base cross-check' '## Differing files (A vs C)'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- the base pin disagreeing between wave-1 evidence files
- `AFTER_SHA` missing, or not descended from the flip commit
- W's interpreter differing from 70-02's
- any of the six builds not exiting 0
- the A/B HTML diff non-empty, or the A/B `.typ` manifests differing
- a control key not EQUAL, or the cross-check key missing
- a file appearing or vanishing between A and C
- the differing-file counts disagreeing with the rescan, or zero HTML files differing
- a `DIFF_HTML` path outside `api/` or `_modules/typsphinx/`, or a `DIFF_TYP` path outside `api/`
- the `.typ` file lists differing
- W still registered
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `BUILD_EXITS = 0 0 0 0 0 0`, from builds that each started with `rm -rf _build` in one worktree at one path.
    - `DOCS_HTML_CONTROL = EQUAL` and `DOCS_TYP_CONTROL = EQUAL`, confirmed by a rescan of `$S/A` against `$S/B`.
    - Every `DIFF_HTML` path is under `api/` or `_modules/typsphinx/`, and every `DIFF_TYP` path is under `api/`. No file appears or vanishes, and at least one HTML file differs.
    - `DOCS_BASE_CROSSCHECK` is recorded as EQUAL, or as DIFFER with the differing lines listed.
    - W is removed and pruned.
  </acceptance_criteria>
  <done>The three clean builds exist side by side, the control shows the base reproduces exactly, and the files that differ sit only where D-11 admits them.</done>
</task>

<task type="auto">
  <name>Task 2: Record every A-vs-C hunk verbatim and classify each against a converted annotation per D-11; any UNTRACED hunk goes to the owner</name>
  <precondition>Task 1 is committed with both control keys EQUAL, the location rule holding, and no HALT heading. The copies `$S/A` and `$S/C` still exist.</precondition>
  <reversibility rating="reversible">Evidence file only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOCS-DIFF-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOCS-DIFF-EVIDENCE.md (Task 1: the `DIFF_HTML` and `DIFF_TYP` lists)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md (D-11 and its AMENDED block)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md (Pitfall 9: `Optional[Set[str]]` against `set[str] | None`)
    - the converted source diff: `git diff "$PHASE_BASE_SHA" "$FLIP_COMMIT" -- typsphinx/` (every hunk must trace to a line here)
  </read_first>
  <action>
1. **`## Raw diffs`.** For each `DIFF_HTML` path, write a heading `### html: <path>`, then the output of `diff -U0 "$S/A/html/<path>" "$S/C/html/<path>"` between a `~~~diff` line and a `~~~` line, appended with shell redirection. Do the same for each `DIFF_TYP` path, under `### typ: <path>`, using `$S/A/pdf` and `$S/C/pdf`.

   Record:
   - `DOCS_HTML_HUNKS = ` (the number of `@@` header lines across the HTML diffs)
   - `DOCS_TYP_HUNKS = ` (the same across the `.typ` diffs)

   If `DOCS_HTML_HUNKS` is 0, write `## HALT: API reference unchanged` and stop. If `DOCS_TYP_HUNKS` is 0, add a `## Owner finding: docs .typ unchanged` section explaining what was checked, because D-09 expected a change.

2. **`## D-11 classification`.** Build one Markdown table with one row per hunk, numbered `H-001` upwards in the order of the raw diffs. The columns are:
   - `id`
   - `output` (`html` or `typ`)
   - `file`
   - `base lines` (from the `@@` header)
   - `before` (the changed tokens only, abridged)
   - `after` (the changed tokens only, abridged)
   - `traces to`
   - `class`

   `traces to` names the converted source line, as `typsphinx/<module>.py:<line after conversion>` from the source diff. For an `api/` hunk it also names the autodoc'd object (module, class, function and parameter or field).

   `class` is `TRACED` only when the hunk is one of these (D-11 as amended):
   - a `Dict`/`List`/`Set`/`Tuple` → `dict`/`list`/`set`/`tuple` rename in a signature, field or source line
   - a `typing` / `collections.abc` import-line change on a `_modules/typsphinx/` page
   - an intersphinx link target or title attached to the renamed type
   - a textual shape change caused by the rename, such as `Optional[Set[str]]` → `set[str] | None`

   Every other hunk is `UNTRACED`. Record `TRACED_HUNKS = ` and `UNTRACED_HUNKS = `.

3. **`## Verdict`.**
   - If `UNTRACED_HUNKS` is not 0: list each untraced hunk, write `## HALT: untraced hunks for the owner`, and stop. It is never recorded as nondeterminism, because the control was empty.
   - Otherwise record `DOC23_VERDICT = MET`.

   At end of phase, the owner reads the classification table (end-of-phase human check).

4. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-DOCS-DIFF-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; S="$(k SCRATCH_70_12)" &amp;&amp; HH=0 &amp;&amp; for p in $(sed -n 's/^DIFF_HTML: //p' "$F"); do HH=$((HH + $(diff -U0 "$S/A/html/$p" "$S/C/html/$p" | grep -c '^@@'))); done &amp;&amp; TH=0 &amp;&amp; for p in $(sed -n 's/^DIFF_TYP: //p' "$F"); do TH=$((TH + $(diff -U0 "$S/A/pdf/$p" "$S/C/pdf/$p" | grep -c '^@@'))); done &amp;&amp; [ "$HH" = "$(k DOCS_HTML_HUNKS)" ] &amp;&amp; [ "$TH" = "$(k DOCS_TYP_HUNKS)" ] &amp;&amp; [ "$HH" -gt 0 ] &amp;&amp; [ "$(grep -c '^@@ ' "$F")" = "$((HH + TH))" ] &amp;&amp; [ "$(grep -cE '^\| H-[0-9]{3} \|' "$F")" = "$((HH + TH))" ] &amp;&amp; [ "$(grep -E '^\| H-[0-9]{3} \|' "$F" | grep -cE '\| TRACED \|[[:space:]]*$')" = "$((HH + TH))" ] &amp;&amp; [ "$(k TRACED_HUNKS)" = "$((HH + TH))" ] &amp;&amp; [ "$(k UNTRACED_HUNKS)" = 0 ] &amp;&amp; [ "$(k DOC23_VERDICT)" = MET ] &amp;&amp; { [ "$TH" -gt 0 ] || grep -qxF '## Owner finding: docs .typ unchanged' "$F"; } &amp;&amp; for s in '## Raw diffs' '## D-11 classification' '## Verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$(k BASE_70_12)" HEAD | grep -vxF -e "$F" -e "$D/70-12-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- the hunk counts re-derived from `$S` differing from the keys, or zero HTML hunks
- the recorded `@@` lines, the table rows or the TRACED rows not equal to the total hunk count
- `UNTRACED_HUNKS` not 0, or `DOC23_VERDICT` not MET
- zero `.typ` hunks with no owner-finding section
- a section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
    <human-check>At end of phase, the owner reads `## D-11 classification` in `70-DOCS-DIFF-EVIDENCE.md` and confirms that each TRACED row's `traces to` names a converted annotation or typing import line.</human-check>
  </verify>
  <acceptance_criteria>
    - Every A-vs-C hunk of both outputs is recorded verbatim, and the count of recorded `@@` lines equals `DOCS_HTML_HUNKS + DOCS_TYP_HUNKS`, re-derived from the build copies.
    - The table has exactly one `H-NNN` row per hunk, all `TRACED`, each naming a converted source line. `UNTRACED_HUNKS = 0`.
    - `DOCS_HTML_HUNKS > 0`, and `DOC23_VERDICT = MET`.
  </acceptance_criteria>
  <done>The clean docs diff is recorded in full, and every hunk traces to a converted annotation or typing import, on an API-reference or viewcode page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build (intersphinx network fetch, autodoc) → evidence | the build pulls remote inventories; a remote change could masquerade as a conversion effect |
| W → shared git state | a leftover worktree or a branch would pollute 70-13's census |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-26 | Repudiation | a hunk written off as nondeterminism | medium | mitigate | The same-path A/B control must be empty, or the plan halts; every hunk needs a TRACED row naming a converted source line. |
| T-70-27 | Spoofing | an intersphinx inventory change between builds | low | mitigate | A, B and C run back to back in one worktree; the A/B control and the wave-1 cross-check expose any drift. |
| T-70-28 | Tampering | W left registered or on a branch | low | mitigate | Detached add, then `git worktree remove --force` and `prune`; the verify requires W absent from `git worktree list`. |
| T-70-SC | Tampering | package installs in W | low | accept | W installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan creates
`70-DOCS-DIFF-EVIDENCE.md`, with the keys `BASE_70_12`, `AFTER_SHA`, `FLIP_COMMIT`,
`SCRATCH_70_12`, `W_VENV_HOME`, `W_VENV_VERSION_INFO`, `BUILD_EXITS`, `DOCS_HTML_CONTROL`,
`DOCS_TYP_CONTROL`, `DOCS_BASE_CROSSCHECK`, `DOCS_HTML_DIFF_FILES`, `DOCS_TYP_DIFF_FILES`,
`DOCS_HTML_HUNKS`, `DOCS_TYP_HUNKS`, `TRACED_HUNKS`, `UNTRACED_HUNKS` and `DOC23_VERDICT`; the
`DIFF_HTML:` and `DIFF_TYP:` lines; the raw `~~~diff` blocks; and the H-NNN classification table.
</artifacts_this_phase_produces>

<verification>
The clean docs diff is confined to API-reference and viewcode pages, every hunk is classified as a
converted-annotation effect, and the base-vs-base control is empty on both outputs.
</verification>

<success_criteria>
SC#4's docs half and DOC-23: the one sanctioned visible change is recorded, and shown to be the only
one.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-12-SUMMARY.md` when done. Its first section states `DOC23_VERDICT`, the hunk counts and `DOCS_BASE_CROSSCHECK`, and names any owner finding. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
