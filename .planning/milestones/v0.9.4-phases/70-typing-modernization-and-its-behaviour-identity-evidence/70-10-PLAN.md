---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 10
type: execute
wave: 5
depends_on: ["70-01", "70-02", "70-04", "70-09"]
files_modified:
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-STATIC-EVIDENCE.md
autonomous: true
requirements: [QUA-09, QUA-11, QUA-12, DOC-22]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Measured on the post-flip tree in wave 5, never in a plan whose work it audits (ROADMAP constraint 1(iii)). `git diff --name-only PHASE_BASE_SHA HEAD` outside .planning/ lists exactly CLAUDE.md, pyproject.toml and the ten files named in 70-02''s `up-files` block.'
    - 'SC#2: pyproject.toml''s `[tool.ruff.lint]` section names neither UP006 nor UP035. A fresh `uv run ruff check . --select UP006,UP035` and bare `uv run ruff check .` exit 0, against the non-zero UP_TOTAL_BASE. No `preview` setting exists in pyproject.toml, and `--preview` appears in neither tox.ini nor ci.yml.'
    - 'Edge QUA-09/concurrency: the ruff version measured here equals RUFF_VERSION_BASE, so no dependabot bump moved the baseline mid-conversion (ROADMAP constraint 4).'
    - 'SC#2: no `from typing import` line under typsphinx/ or tests/ names Dict, List, Set, Tuple or Iterator. The same grep at PHASE_BASE_SHA is non-empty, as a positive control. `sed -n 15p typsphinx/__init__.py` prints `from typing import Any`.'
    - 'Edge QUA-11/adjacency and SC#2 counts: FUTURE_ANNOTATIONS, OPTIONAL, UNION and PIPE_NONE measured at HEAD equal their PHASE_BASE_SHA values, so no PEP 604 sweep or `__future__` import happened, and `str | list[str] | None` kept its pipes.'
    - 'Edge QUA-11/empty: the files whose only typing names were converted carry no `from typing import` line, and no typing import line is empty.'
    - 'SC#2: no `@preview` line appears in the milestone diff, and `typsphinx/templates/` is unchanged. `tests/test_authors_pipeline_stage_gate.py` is absent from the diff, with `ast.Dict` still at line 515.'
    - 'Leg (a) / SC#3: for every file in `up-files`, the masked hash at HEAD equals the hash of `git show PHASE_BASE_SHA:<path>`, computed with the harness pinned by MASK_HARNESS_SHA256. LEG_A_EQUAL_COUNT equals UP_FILE_COUNT_BASE. The commit that added the pilot evidence is an ancestor of every wave-3 conversion commit, so the pilot was recorded before the check was automated.'
    - 'Leg (c) / SC#3: `git diff PHASE_BASE_SHA HEAD -- tests/` names only the four converted test files. Every changed non-blank line carries a typing name, and no changed line contains `assert`. The result is read off `git diff`, not asserted.'
    - 'Leg (e) / SC#3: `uv run mypy typsphinx/ 2>/dev/null` stdout at HEAD hashes equal to MYPY_STDOUT_SHA256_BEFORE. The exit code is 0, MYPY_VERSION equals MYPY_VERSION_BASE, and the interpreter keys equal 70-02''s.'
    - 'SC#1: exactly one commit since PHASE_BASE_SHA touches CLAUDE.md, and it touches only CLAUDE.md. It is an ancestor of every conversion commit, and every conversion commit is an ancestor of the one flip commit. The flip commit''s name-status is `M pyproject.toml` plus the R100 todo rename. The todo is absent from pending/ and present in completed/, and HEAD has zero tracked references to its pending path outside .planning/.'
    - 'SC#1 / D-01, D-02, D-03: the CLAUDE.md Python bullet is byte-identical at the flip commit''s parent (the last pre-flip commit) and at HEAD. At both commits it carries the D-01 tokens and none of the D-02 or D-03 tokens, and the reading paragraph records that it is true at both.'
  artifacts:
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-STATIC-EVIDENCE.md
      provides: 'SC#1 history checks, SC#2 static checks, legs (a), (c) and (e) after-side, verdict keys'
      contains: 'SC1_VERDICT = MET'
  key_links:
    - from: '70-BASELINE-EVIDENCE.md and 70-MASK-PILOT-EVIDENCE.md keys'
      to: 'this plan''s equality checks'
      via: 'exact sed-key comparison, with the harness re-extracted and hash-checked'
      pattern: 'LEG_A_EQUAL_COUNT = '
  prohibitions:
    - statement: 'MUST NOT modify any file outside this plan''s evidence file, re-run any conversion, or record a verdict as MET when a measured value differs from its base key.'
      requirement_id: QUA-12
      category: transparency
      status: resolved
      verification: judgment
---

<assumption_delta_decision>
- Primary noun: the count of `Optional[` / `Union[` / `X | None` annotations named in SC#2.
- Decision: no-change. The detector matched the literal `Optional[` token in SC#2's counting
  clause. No field goes from required to optional and no identity model changes; this plan only
  checks that the count stays at its base value.
</assumption_delta_decision>

<objective>
Measure the static half of the after side on the post-flip tree:
- SC#2's static claims
- QUA-12 legs (a), (c) and (e)
- SC#1's history claims (DOC-22)

The last of these includes reading the CLAUDE.md bullet at the last pre-flip commit and at the post-flip tip against D-01, D-02 and D-03.

Purpose: this is the audit of 70-01 and 70-04 to 70-09, and it runs in a wave after all of them (ROADMAP constraint 1(iii)). It re-derives every value from commands, never from the audited plans' own keys, except the base keys it compares against.

Output: `70-AFTER-STATIC-EVIDENCE.md` with `SC1_VERDICT`, `SC2_VERDICT`, `LEG_A_VERDICT`, `LEG_C_VERDICT` and `LEG_E_VERDICT`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-FLIP-EVIDENCE.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every verify
command with the Bash tool `timeout` set to 600000.

**Harness.** Extract it exactly as 70-04 defined:
`H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')"`. Check its hash against
`MASK_HARNESS_SHA256`, then run it with `sk() { uv run python -c "$H"; }`.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value.

**Commits.** Stage the evidence file by explicit path. On any failure path, write
`## HALT: <reason>` into the evidence file, commit it, and stop. A measured difference is a finding
for the owner, never a MET verdict.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: assert -->
<!-- planner-discipline-allow: @preview -->
<!-- planner-discipline-allow: --preview -->
<!-- planner-discipline-allow: todos/pending/2026-07-22-modernize -->
<!-- planner-discipline-allow: UP006 -->
<!-- planner-discipline-allow: UP035 -->
<!-- planner-discipline-allow: from typing import -->
<!-- planner-discipline-allow: odernize -->
<!-- planner-discipline-allow: deferral -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: SC#2 static checks, then leg (a) over every census file and leg (c) over tests/, on the post-flip tree</name>
  <precondition>`70-FLIP-EVIDENCE.md` exists at HEAD with no HALT heading, and exactly one commit since `PHASE_BASE_SHA` touches pyproject.toml. Otherwise write `## HALT: flip not merged` and stop.</precondition>
  <reversibility rating="reversible">Read-only measurement and one evidence file.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-STATIC-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PHASE_BASE_SHA`, `RUFF_VERSION_BASE`, `UP_TOTAL_BASE`, `UP_FILE_COUNT_BASE`, the `up-files` block, the four SC#2 counts)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md (§ Mask harness, `MASK_HARNESS_SHA256`, and what the leg proves and does not prove)
    - .planning/ROADMAP.md (Phase 70 SC#2 and SC#3)
    - tests/test_authors_pipeline_stage_gate.py (lines 513-515)
  </read_first>
  <action>
1. Create `70-AFTER-STATIC-EVIDENCE.md` with the heading `# Phase 70 — After-side Static Evidence`. Before any commit, record `BASE_70_10 = ` from `git rev-parse HEAD`. Then record `VENV_HOME = ` and `VENV_VERSION_INFO = ` after provisioning; both must equal 70-02's. Record `RUFF_VERSION_AFTER = `, which must equal `RUFF_VERSION_BASE`.

2. **`## Changed-file set`.** Quote `git diff --name-only "$PHASE_BASE_SHA" HEAD -- . ':(exclude).planning' | LC_ALL=C sort`. It must equal CLAUDE.md, pyproject.toml and the ten `up-files` paths. Any other path is a finding: write `## HALT: unexpected changed file` and stop.

3. **`## SC#2 static checks`.** Quote each check with its result:
   - pyproject.toml's `[tool.ruff.lint]` section (from its header to the next `[` header) contains neither `UP006` nor `UP035`.
   - `uv run ruff check . --select UP006,UP035` and `uv run ruff check .` exit 0, set against `UP_TOTAL_BASE`.
   - No `preview =` line in pyproject.toml, and no `--preview` in `tox.ini` or `.github/workflows/ci.yml`.
   - The typing-import grep. At HEAD, `git grep -nE '^[[:space:]]*from typing import (.*[ ,])?(Dict|List|Set|Tuple|Iterator)([ ,]|$)' HEAD -- 'typsphinx/*.py' 'tests/*.py'` must be empty. The same grep at `PHASE_BASE_SHA` must be non-empty (positive control).
   - `sed -n 15p typsphinx/__init__.py` is exactly `from typing import Any`.
   - The four counts at HEAD, taken with the same `git grep -hoE … | wc -l` commands 70-02 used: `FUTURE_ANNOTATIONS_AFTER`, `OPTIONAL_AFTER`, `UNION_AFTER` and `PIPE_NONE_AFTER`, each equal to its `_BASE` key.
   - `tests/test_bundle_layout_sweep_gate.py`, `tests/test_include_edge_derivation_unit.py` and `tests/test_include_ledger_removal_gate.py` have no `from typing import` line.
   - The milestone diff for typsphinx/ and tests/ has no added or removed `@preview` line, and `git diff --quiet "$PHASE_BASE_SHA" HEAD -- typsphinx/templates` exits 0.
   - `git diff --quiet "$PHASE_BASE_SHA" HEAD -- tests/test_authors_pipeline_stage_gate.py` exits 0, and line 515 contains `ast.Dict`.

   Record `SC2_VERDICT = MET` only if every check holds.

4. **`## Leg (a) — every converted file`.** For each path in the `up-files` block, quote the base hash and the HEAD hash, with the verdict. Record `LEG_A_EQUAL_COUNT = ` with the number of EQUAL files, which must equal `UP_FILE_COUNT_BASE`.

   For the pilot ordering: take the commit that added the pilot evidence, `git log --diff-filter=A --format=%H "$PHASE_BASE_SHA"..HEAD -- <pilot evidence path>`, which must be one commit. It must be an ancestor of every commit returned by `git log --format=%H "$PHASE_BASE_SHA"..HEAD --` over the eight wave-3 files (builder.py, template_engine.py, template_registry.py, writer.py, `__init__.py`, and the three tests converted by 70-08). Record `PILOT_BEFORE_AUTOMATION = YES`.

   Add one sentence on what leg (a) leaves to legs (b) and (e): import correctness. Record `LEG_A_VERDICT = MET` if both hold.

5. **`## Leg (c) — tests/`.** Quote `git diff --name-only "$PHASE_BASE_SHA" HEAD -- tests/`, which must be exactly the four converted test files. Quote the per-file added and removed counts. Record:
   - `LEG_C_NON_TYPING_LINES = 0`: changed non-blank lines without a typing name
   - `LEG_C_ASSERT_LINES = 0`: changed lines containing `assert`
   - `LEG_C_VERDICT = MET`

6. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-AFTER-STATIC-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; PE="$D/70-MASK-PILOT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = "$(k VENV_VERSION_INFO "$BE")" ] &amp;&amp; [ "$(uv run ruff --version 2&gt;/dev/null)" = "$(k RUFF_VERSION_BASE "$BE")" ] &amp;&amp; UF="$(sed -n '/^~~~text up-files$/,/^~~~$/p' "$BE" | sed '1d;$d' | awk '{print $NF}' | LC_ALL=C sort)" &amp;&amp; [ "$(printf '%s\n' "$UF" | wc -l)" = "$(k UP_FILE_COUNT_BASE "$BE")" ] &amp;&amp; [ "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning' | LC_ALL=C sort)" = "$(printf '%s\n' CLAUDE.md pyproject.toml $UF | LC_ALL=C sort)" ] &amp;&amp; ! sed -n '/^\[tool\.ruff\.lint\]/,/^\[/p' pyproject.toml | grep -qE 'UP006|UP035' &amp;&amp; uv run ruff check . --select UP006,UP035 &amp;&amp; uv run ruff check . &amp;&amp; ! grep -qE '^[[:space:]]*preview[[:space:]]*=' pyproject.toml &amp;&amp; ! grep -qF -- '--preview' tox.ini .github/workflows/ci.yml &amp;&amp; RX='^[[:space:]]*from typing import (.*[ ,])?(Dict|List|Set|Tuple|Iterator)([ ,]|$)' &amp;&amp; [ -z "$(git grep -nE "$RX" HEAD -- 'typsphinx/*.py' 'tests/*.py')" ] &amp;&amp; [ -n "$(git grep -nE "$RX" "$B" -- 'typsphinx/*.py' 'tests/*.py')" ] &amp;&amp; [ "$(sed -n 15p typsphinx/__init__.py)" = 'from typing import Any' ] &amp;&amp; [ "$(git grep -hoE 'from __future__ import annotations' HEAD -- 'typsphinx/*.py' 'tests/*.py' | wc -l)" = "$(k FUTURE_ANNOTATIONS_BASE "$BE")" ] &amp;&amp; [ "$(git grep -hoE 'Optional\[' HEAD -- 'typsphinx/*.py' 'tests/*.py' | wc -l)" = "$(k OPTIONAL_BASE "$BE")" ] &amp;&amp; [ "$(git grep -hoE 'Union\[' HEAD -- 'typsphinx/*.py' 'tests/*.py' | wc -l)" = "$(k UNION_BASE "$BE")" ] &amp;&amp; [ "$(git grep -hoE '[|] None' HEAD -- 'typsphinx/*.py' 'tests/*.py' | wc -l)" = "$(k PIPE_NONE_BASE "$BE")" ] &amp;&amp; ! grep -qE '^from typing import' tests/test_bundle_layout_sweep_gate.py tests/test_include_edge_derivation_unit.py tests/test_include_ledger_removal_gate.py &amp;&amp; ! git diff "$B" HEAD -- typsphinx tests | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -qF '@preview' &amp;&amp; git diff --quiet "$B" HEAD -- typsphinx/templates tests/test_authors_pipeline_stage_gate.py &amp;&amp; sed -n 515p tests/test_authors_pipeline_stage_gate.py | grep -qF 'ast.Dict' &amp;&amp; H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')" &amp;&amp; [ -n "$H" ] &amp;&amp; [ "$(printf '%s\n' "$H" | sha256sum | cut -d' ' -f1)" = "$(k MASK_HARNESS_SHA256 "$PE")" ] &amp;&amp; sk() { uv run python -c "$H"; } &amp;&amp; n=0 &amp;&amp; for f in $UF; do a="$(sk &lt; "$f")" &amp;&amp; [ "${#a}" = 64 ] &amp;&amp; [ "$a" = "$(git show "$B:$f" | sk)" ] || { echo "mask differs: $f"; exit 1; }; n=$((n+1)); done &amp;&amp; [ "$n" = "$(k UP_FILE_COUNT_BASE "$BE")" ] &amp;&amp; [ "$(k LEG_A_EQUAL_COUNT)" = "$n" ] &amp;&amp; P0="$(git log --diff-filter=A --format=%H "$B"..HEAD -- "$PE")" &amp;&amp; [ "$(printf '%s\n' "$P0" | grep -c .)" = 1 ] &amp;&amp; for c in $(git log --format=%H "$B"..HEAD -- typsphinx/builder.py typsphinx/template_engine.py typsphinx/template_registry.py typsphinx/writer.py typsphinx/__init__.py tests/conftest.py tests/test_bundle_layout_sweep_gate.py tests/test_include_edge_derivation_unit.py); do git merge-base --is-ancestor "$P0" "$c" || { echo "automated before pilot: $c"; exit 1; }; done &amp;&amp; [ "$(git diff --name-only "$B" HEAD -- tests/ | LC_ALL=C sort)" = "$(printf '%s\n' $UF | grep '^tests/' | LC_ALL=C sort)" ] &amp;&amp; ! git diff "$B" HEAD -- tests/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -vE '^[+-][[:space:]]*$' | grep -qvE '(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)' &amp;&amp; ! git diff "$B" HEAD -- tests/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -qw assert &amp;&amp; for kk in SC2_VERDICT LEG_A_VERDICT LEG_C_VERDICT; do [ "$(k $kk)" = MET ] || { echo "verdict: $kk"; exit 1; }; done &amp;&amp; [ "$(k PILOT_BEFORE_AUTOMATION)" = YES ] &amp;&amp; [ "$(k LEG_C_NON_TYPING_LINES)" = 0 ] &amp;&amp; [ "$(k LEG_C_ASSERT_LINES)" = 0 ] &amp;&amp; for s in '## Changed-file set' '## SC#2 static checks' '## Leg (a) — every converted file' '## Leg (c) — tests/'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or a `mask differs:`, `automated before pilot:`, `verdict:` or `missing section:` line printed. Each of these makes it fail:
- the interpreter or the ruff version differing from base
- the changed-file set not exactly CLAUDE.md, pyproject.toml and the census files
- UP006/UP035 left in the lint section, or any ruff finding
- a `preview` setting, or `--preview` in tox.ini or ci.yml
- a banned typing name at HEAD, or no hit at base (a broken grep)
- `__init__.py:15` wrong
- an SC#2 count differing from base, or a typing import left in a vanish file
- an `@preview` diff line, or templates or the authors gate changed, or `ast.Dict` missing
- the harness hash wrong, or a masked hash differing
- the pilot commit not an ancestor of a wave-3 conversion commit
- the tests/ diff naming another file, or a non-typing or assert line changed
- a verdict or count key wrong
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - The changed-file set outside .planning/ is exactly CLAUDE.md, pyproject.toml and the census files.
    - `SC2_VERDICT = MET`, with every static check quoted and each `_AFTER` count equal to its `_BASE` key.
    - `LEG_A_EQUAL_COUNT` equals `UP_FILE_COUNT_BASE`, and `PILOT_BEFORE_AUTOMATION = YES`.
    - `LEG_C_NON_TYPING_LINES = 0`, `LEG_C_ASSERT_LINES = 0`, and the tests/ diff names only the converted test files.
  </acceptance_criteria>
  <done>SC#2 and legs (a) and (c) are measured MET on the post-flip tree, in a wave after the work they audit.</done>
</task>

<task type="auto">
  <name>Task 2: Leg (e) mypy after-side, SC#1's commit-order and same-commit checks, and the two-commit reading of CLAUDE.md:75 against D-01..D-03</name>
  <precondition>Task 1 is committed with `SC2_VERDICT = MET`, `LEG_A_VERDICT = MET` and `LEG_C_VERDICT = MET`, and no HALT heading.</precondition>
  <reversibility rating="reversible">Read-only measurement and one evidence file.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-STATIC-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md (D-01..D-04; Specific Ideas: the bullet is read at two commits)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOC22-EVIDENCE.md (the rewritten bullet and its token checks)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-FLIP-EVIDENCE.md (`PRE_FLIP_SHA`)
    - .planning/ROADMAP.md (Phase 70 SC#1)
  </read_first>
  <action>
1. **`## Leg (e) — mypy`.** Record `MYPY_VERSION_AFTER = ` from `uv run mypy --version 2>/dev/null`, which must equal `MYPY_VERSION_BASE`. Run `uv run mypy typsphinx/ 2>/dev/null > "$(mktemp)"` and record its exit code as `MYPY_EXIT_AFTER`, which must be 0. Record `MYPY_STDOUT_SHA256_AFTER = `, which must equal `MYPY_STDOUT_SHA256_BEFORE`. Quote the stdout verbatim beside the base block. Record `LEG_E_VERDICT = MET`.

2. **`## SC#1 — commit order`.** Quote each command and its result:
   - `CLAUDE_COMMIT`: the only line of `git log --format=%H "$PHASE_BASE_SHA"..HEAD -- CLAUDE.md`. `git show --name-only --format=` for it must print only `CLAUDE.md`.
   - `FLIP_COMMIT`: the only line of `git log --format=%H "$PHASE_BASE_SHA"..HEAD -- pyproject.toml`. `git show --name-status -M --format=` for it must be `M pyproject.toml` plus the R100 rename from `.planning/todos/pending/` to `.planning/todos/completed/`.
   - The conversion commits are `git log --format=%H "$PHASE_BASE_SHA"..HEAD --` over the ten census files. Put them in a table with, for each: `git merge-base --is-ancestor CLAUDE_COMMIT <c>` (exit 0) and `git merge-base --is-ancestor <c> FLIP_COMMIT` (exit 0).
   - `.planning/todos/completed/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` exists and the pending copy does not.
   - `git grep -n 'todos/pending/2026-07-22-modernize' HEAD -- . ':!.planning'` is empty, and the same grep at `FLIP_COMMIT^` prints one pyproject.toml line.

   Record `CLAUDE_COMMIT = `, `FLIP_COMMIT = ` and `CONVERSION_COMMIT_COUNT = `.

3. **`## SC#1 — CLAUDE.md read at two commits`.** Extract the line beginning `- **Python 3.12+ is required.**` from `git show FLIP_COMMIT^:CLAUDE.md` and from `git show HEAD:CLAUDE.md`. Quote both.
   - They must be byte-identical.
   - Each contains `dict[str, Any]`, `list[str]`, `collections.abc`, `Iterator` and `typing.Dict` (D-01).
   - Each contains none of `UP006`, `UP035`, `ignore`, `deferral`, `todos/pending` and `odernize` (D-02), and neither `__future__` nor `604` (D-03).

   Then write one reading paragraph for each commit:
   - At `FLIP_COMMIT^` the ignores are still present, and the bullet makes no claim about them. Every file already uses the shape it describes.
   - At HEAD ruff enforces that shape, and the text is unchanged and still makes no claim about ruff.

   Record `CLAUDE_BULLET_IDENTICAL = YES` and `SC1_VERDICT = MET`.

4. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-AFTER-STATIC-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; T=2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; [ "$(uv run mypy --version 2&gt;/dev/null)" = "$(k MYPY_VERSION_BASE "$BE")" ] &amp;&amp; uv run mypy typsphinx/ &gt;/dev/null 2&gt;&amp;1 &amp;&amp; [ "$(uv run mypy typsphinx/ 2&gt;/dev/null | sha256sum | cut -d' ' -f1)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; [ "$(k MYPY_STDOUT_SHA256_AFTER)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; [ "$(k MYPY_EXIT_AFTER)" = 0 ] &amp;&amp; C="$(git log --format=%H "$B"..HEAD -- CLAUDE.md)" &amp;&amp; [ "$(printf '%s\n' "$C" | grep -c .)" = 1 ] &amp;&amp; [ "$(git show --name-only --format= "$C")" = CLAUDE.md ] &amp;&amp; [ "$(k CLAUDE_COMMIT)" = "$C" ] &amp;&amp; FL="$(git log --format=%H "$B"..HEAD -- pyproject.toml)" &amp;&amp; [ "$(printf '%s\n' "$FL" | grep -c .)" = 1 ] &amp;&amp; [ "$(k FLIP_COMMIT)" = "$FL" ] &amp;&amp; [ "$(git show --name-status -M --format= "$FL" | LC_ALL=C sort)" = "$(printf 'M\tpyproject.toml\nR100\t.planning/todos/pending/%s\t.planning/todos/completed/%s\n' "$T" "$T" | LC_ALL=C sort)" ] &amp;&amp; UF="$(sed -n '/^~~~text up-files$/,/^~~~$/p' "$BE" | sed '1d;$d' | awk '{print $NF}')" &amp;&amp; CS="$(git log --format=%H "$B"..HEAD -- $UF)" &amp;&amp; [ -n "$CS" ] &amp;&amp; [ "$(printf '%s\n' "$CS" | wc -l)" = "$(k CONVERSION_COMMIT_COUNT)" ] &amp;&amp; for c in $CS; do git merge-base --is-ancestor "$C" "$c" &amp;&amp; git merge-base --is-ancestor "$c" "$FL" || { echo "order: $c"; exit 1; }; done &amp;&amp; test -f ".planning/todos/completed/$T" &amp;&amp; test ! -e ".planning/todos/pending/$T" &amp;&amp; [ -z "$(git grep -n 'todos/pending/2026-07-22-modernize' HEAD -- . ':!.planning')" ] &amp;&amp; [ "$(git grep -n 'todos/pending/2026-07-22-modernize' "$FL^" -- . ':!.planning' | wc -l)" = 1 ] &amp;&amp; L1="$(git show "$FL^:CLAUDE.md" | grep '^- \*\*Python 3\.12+ is required\.\*\*')" &amp;&amp; L2="$(git show HEAD:CLAUDE.md | grep '^- \*\*Python 3\.12+ is required\.\*\*')" &amp;&amp; [ -n "$L1" ] &amp;&amp; [ "$L1" = "$L2" ] &amp;&amp; for t in 'dict[str, Any]' 'list[str]' 'collections.abc' 'Iterator' 'typing.Dict'; do case "$L2" in *"$t"*) ;; *) echo "missing token: $t"; exit 1 ;; esac; done &amp;&amp; for t in UP006 UP035 ignore deferral 'todos/pending' odernize __future__ 604; do case "$L2" in *"$t"*) echo "forbidden token: $t"; exit 1 ;; esac; done &amp;&amp; [ "$(k CLAUDE_BULLET_IDENTICAL)" = YES ] &amp;&amp; [ "$(k LEG_E_VERDICT)" = MET ] &amp;&amp; [ "$(k SC1_VERDICT)" = MET ] &amp;&amp; for s in '## Leg (e) — mypy' '## SC#1 — commit order' '## SC#1 — CLAUDE.md read at two commits'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$(k BASE_70_10)" HEAD | grep -vxF -e "$F" -e "$D/70-10-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or an `order:`, `missing token:`, `forbidden token:` or `missing section:` line printed. Each of these makes it fail:
- the mypy version, exit code or stdout hash differing from base
- zero or several CLAUDE.md commits, or that commit touching another file
- zero or several flip commits, or the flip commit's name-status wrong
- a conversion commit not after CLAUDE_COMMIT or not before FLIP_COMMIT
- the todo not moved, a pending-path reference at HEAD, or no reference at the pre-flip commit
- the bullet differing between the two commits, or its D-01, D-02 or D-03 tokens wrong
- a key wrong
- a section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `MYPY_STDOUT_SHA256_AFTER` equals `MYPY_STDOUT_SHA256_BEFORE`, with `MYPY_EXIT_AFTER = 0` and matching versions. `LEG_E_VERDICT = MET`.
    - Every conversion commit descends from `CLAUDE_COMMIT` and is an ancestor of `FLIP_COMMIT`. `FLIP_COMMIT` holds exactly the pyproject edit and the R100 todo rename.
    - The CLAUDE.md bullet is byte-identical at `FLIP_COMMIT^` and HEAD, and it passes the D-01/D-02/D-03 token checks. `SC1_VERDICT = MET`.
  </acceptance_criteria>
  <done>SC#1 and leg (e) are measured MET: the rewrite came first, the todo left with the ignores, the bullet is true on both sides, and mypy prints the same bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audited plans' evidence → this audit | an audit that re-read the audited plans' own verdicts would certify nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-22 | Repudiation | a verdict copied from a conversion plan's own key | medium | mitigate | Every check is re-derived from git, ruff, mypy or the hash-pinned harness in this worktree; only `_BASE` keys are read. |
| T-70-23 | Repudiation | a broken grep reported as "zero hits" | medium | mitigate | Each negative grep has a positive control at `PHASE_BASE_SHA` or at the pre-flip commit. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan creates
`70-AFTER-STATIC-EVIDENCE.md`, with the keys `BASE_70_10`, `VENV_HOME`, `VENV_VERSION_INFO`,
`RUFF_VERSION_AFTER`, `FUTURE_ANNOTATIONS_AFTER`, `OPTIONAL_AFTER`, `UNION_AFTER`,
`PIPE_NONE_AFTER`, `SC2_VERDICT`, `LEG_A_EQUAL_COUNT`, `PILOT_BEFORE_AUTOMATION`,
`LEG_A_VERDICT`, `LEG_C_NON_TYPING_LINES`, `LEG_C_ASSERT_LINES`, `LEG_C_VERDICT`,
`MYPY_VERSION_AFTER`, `MYPY_EXIT_AFTER`, `MYPY_STDOUT_SHA256_AFTER`, `LEG_E_VERDICT`,
`CLAUDE_COMMIT`, `FLIP_COMMIT`, `CONVERSION_COMMIT_COUNT`, `CLAUDE_BULLET_IDENTICAL` and
`SC1_VERDICT`.
</artifacts_this_phase_produces>

<verification>
SC#1 and SC#2 are measured MET, and QUA-12 legs (a), (c) and (e) are MET, all on the post-flip tree
in wave 5.
</verification>

<success_criteria>
The static half of "nothing else changed" is proven by measurement, and DOC-22's ordering and
two-sided truth are read off git history.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-10-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
