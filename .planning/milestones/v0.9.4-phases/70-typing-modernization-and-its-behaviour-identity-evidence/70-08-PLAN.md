---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 08
type: execute
wave: 3
depends_on: ["70-01", "70-02", "70-04"]
files_modified:
  - tests/conftest.py
  - tests/test_bundle_layout_sweep_gate.py
  - tests/test_include_edge_derivation_unit.py
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-TESTS-EVIDENCE.md
autonomous: true
requirements: [QUA-11, QUA-12]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - '`tests/conftest.py`, `tests/test_bundle_layout_sweep_gate.py` and `tests/test_include_edge_derivation_unit.py` are converted by the two scoped ruff passes while the config ignores are still present. Afterwards `uv run ruff check <three> --select UP006,UP035` and `uv run ruff check <three>` both exit 0.'
    - 'conftest.py''s typing line reads `from typing import Any`. The other two files carry no `from typing import` line, because their only typing names were converted.'
    - 'Leg (a), automated: each file''s masked hash equals the hash of its `git show PHASE_BASE_SHA:<path>`, computed with the hash-pinned harness. That includes module-level annotated constants such as `EXCLUDED_SWEEP_PATHS` and files whose typing import vanished.'
    - 'Leg (c) for these files: every changed non-blank line carries a typing name, and no changed line contains `assert`. No pre-existing test assertion is edited and no test is added, so the collected count and the full pytest result equal the base values.'
    - '`tests/test_authors_pipeline_stage_gate.py` is untouched, and its line 515 still contains `ast.Dict`.'
    - 'Edge QUA-11/concurrency: this plan runs in parallel with 70-05, 70-06 and 70-07 on disjoint files. Its branch changes only these three files and its own evidence and SUMMARY.'
  artifacts:
    - path: tests/conftest.py
      provides: 'Builtin generics; typing keeps Any'
      contains: 'from typing import Any'
    - path: tests/test_bundle_layout_sweep_gate.py
      provides: 'Builtin generics; no typing import'
      contains: 'dict[str, str]'
    - path: tests/test_include_edge_derivation_unit.py
      provides: 'Builtin generics; no typing import'
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-TESTS-EVIDENCE.md
      provides: 'Conversion transcript, automated leg (a) hashes, leg (c) census, gates'
      contains: 'TESTS_MASK = EQUAL'
  key_links:
    - from: 'tests/conftest.py'
      to: 'every test module (pytest fixture discovery)'
      via: 'a broken import in conftest.py would error the whole collection, which the collected-count check catches'
      pattern: '^from typing import Any$'
  prohibitions:
    - statement: 'MUST NOT touch tests/test_authors_pipeline_stage_gate.py or any other test file, edit or add any assertion, add a test, use sed, or pass --preview or --unsafe-fixes.'
      requirement_id: QUA-12
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Convert the three remaining `tests/` files onto builtin generics under the still-present ignores (ROADMAP constraint 5; QUA-11). The plan proves leg (a) with the pinned harness, and leg (c) for these files: only import and annotation lines change, and no assertion is touched. 70-04 already converted the fourth test file, the `Iterator` move case.

Purpose: `conftest.py` is imported by every test module, so a wrong import here would change the collected count itself. The sweep and edge-derivation files are the other two cases where the `typing` import line vanishes.

Output: the three converted files, committed alone, and `70-CONV-TESTS-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every verify
command with the Bash tool `timeout` set to 600000. Run pytest under `LC_ALL=C`.

**Harness.** Extract it exactly as 70-04 defined:
`H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')"`, where `PE` is
`70-MASK-PILOT-EVIDENCE.md`. Check that `printf '%s\n' "$H" | sha256sum` equals
`MASK_HARNESS_SHA256`, then run it with `sk() { uv run python -c "$H"; }`. Never retype it.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value.

**Commits.** Stage files by explicit path, never `git add -A`. The conversion commit touches only
the three test files. On any failure path, write `## HALT: <reason>` into the evidence file, commit
it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: assert -->
<!-- planner-discipline-allow: @preview -->
<!-- planner-discipline-allow: --preview -->
<!-- planner-discipline-allow: from typing import -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: convert conftest.py, test_bundle_layout_sweep_gate.py and test_include_edge_derivation_unit.py with the two scoped ruff passes and prove leg (a) for all three</name>
  <precondition>`70-MASK-PILOT-EVIDENCE.md` holds `PILOT_TRANSLATOR = EQUAL`, `PILOT_LEDGER = EQUAL`, `CONTROL_RENAME = DIFFER` and `CONTROL_IMPORT = DIFFER` with no HALT heading, and its harness block hashes to `MASK_HARNESS_SHA256`. The single CLAUDE.md commit since `PHASE_BASE_SHA` is an ancestor of HEAD. `git diff --quiet "$PHASE_BASE_SHA" HEAD -- tests/conftest.py tests/test_bundle_layout_sweep_gate.py tests/test_include_edge_derivation_unit.py` exits 0. Otherwise write `## HALT: preconditions` and stop.</precondition>
  <reversibility rating="reversible">Annotation and import rewrites in three git-tracked test files, proven structure-preserving by the harness.</reversibility>
  <files>tests/conftest.py, tests/test_bundle_layout_sweep_gate.py, tests/test_include_edge_derivation_unit.py, .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-TESTS-EVIDENCE.md</files>
  <read_first>
    - tests/conftest.py (line 6's typing import and its `Dict` uses)
    - tests/test_bundle_layout_sweep_gate.py (line 42's typing import; line 87's `EXCLUDED_SWEEP_PATHS` annotated constant)
    - tests/test_include_edge_derivation_unit.py (line 25's typing import)
    - tests/test_authors_pipeline_stage_gate.py (lines 513-515: the `ast.Dict` that must stay untouched)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md (§ tests/conftest.py, …)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md (§ Mask harness)
  </read_first>
  <action>
1. Create `70-CONV-TESTS-EVIDENCE.md` with the heading `# Phase 70 — Conversion: tests/ remainder`. Before any commit, record `BASE_70_08 = ` from `git rev-parse HEAD`. Then record `VENV_HOME = ` and `VENV_VERSION_INFO = ` after provisioning; both must equal 70-02's. Quote the precondition checks under `## Head check and provisioning`.

2. **`## Conversion`.** With `P="tests/conftest.py tests/test_bundle_layout_sweep_gate.py tests/test_include_edge_derivation_unit.py"`:
   - Record `uv run ruff check $P --select UP006,UP035 --output-format=concise` and its non-zero count.
   - Run pass 1, `uv run ruff check $P --select UP006,UP035 --fix`, then pass 2, `uv run ruff check $P --fix`. Never use `sed`, `--preview` or `--unsafe-fixes`.
   - Record that both `uv run ruff check $P --select UP006,UP035` and `uv run ruff check $P` exit 0.
   - Quote the import block of each file.
   - Commit the three files alone, with the message `refactor(70-08): move the remaining tests/ files onto builtin generics (QUA-11)`.

3. **`## Leg (a)`.** For each file, quote the base hash and the current hash from `sk`. If all three pairs are equal, record `TESTS_MASK = EQUAL`. Any difference: write `## HALT: mask` and stop.

4. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CONV-TESTS-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; PE="$D/70-MASK-PILOT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = "$(k VENV_VERSION_INFO "$BE")" ] &amp;&amp; C="$(git log --format=%H "$B"..HEAD -- CLAUDE.md)" &amp;&amp; [ "$(printf '%s\n' "$C" | grep -c .)" = 1 ] &amp;&amp; git merge-base --is-ancestor "$C" "$(k BASE_70_08)" &amp;&amp; [ "$(k PILOT_LEDGER "$PE")" = EQUAL ] &amp;&amp; [ "$(k CONTROL_RENAME "$PE")" = DIFFER ] &amp;&amp; H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')" &amp;&amp; [ -n "$H" ] &amp;&amp; [ "$(printf '%s\n' "$H" | sha256sum | cut -d' ' -f1)" = "$(k MASK_HARNESS_SHA256 "$PE")" ] &amp;&amp; sk() { uv run python -c "$H"; } &amp;&amp; for f in tests/conftest.py tests/test_bundle_layout_sweep_gate.py tests/test_include_edge_derivation_unit.py; do a="$(sk &lt; "$f")" &amp;&amp; [ "${#a}" = 64 ] &amp;&amp; [ "$a" = "$(git show "$B:$f" | sk)" ] || { echo "mask differs: $f"; exit 1; }; ! git diff --quiet "$B" HEAD -- "$f" || { echo "unchanged: $f"; exit 1; }; done &amp;&amp; grep -qxF 'from typing import Any' tests/conftest.py &amp;&amp; ! grep -qE '^from typing import' tests/test_bundle_layout_sweep_gate.py &amp;&amp; ! grep -qE '^from typing import' tests/test_include_edge_derivation_unit.py &amp;&amp; [ "$(k TESTS_MASK)" = EQUAL ] &amp;&amp; uv run ruff check tests/conftest.py tests/test_bundle_layout_sweep_gate.py tests/test_include_edge_derivation_unit.py --select UP006,UP035 &amp;&amp; uv run ruff check tests/conftest.py tests/test_bundle_layout_sweep_gate.py tests/test_include_edge_derivation_unit.py &amp;&amp; for s in '## Head check and provisioning' '## Conversion' '## Leg (a)'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or a `mask differs:`, `unchanged:` or `missing section:` line printed. Each of these makes it fail:
- the interpreter differing from 70-02's
- the CLAUDE.md rewrite not an ancestor
- the pilot keys wrong, or the harness not hashing to its key
- any file's masked hash differing from base, or any file unchanged
- conftest.py's typing line not `from typing import Any`
- a typing import left in the sweep or edge-derivation file
- `TESTS_MASK` wrong
- ruff reporting anything on the three files
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check` of the three files, with and without `--select UP006,UP035`, exits 0.
    - `tests/conftest.py` has `from typing import Any`; the sweep and edge-derivation files have no `from typing import` line.
    - All three masked hashes equal base's, and `TESTS_MASK = EQUAL`.
  </acceptance_criteria>
  <done>The remaining test files are on builtin generics and structurally identical to the base under the mask.</done>
</task>

<task type="auto">
  <name>Task 2: Gate leg (c) for these files and the suite: census with no assert line, ast.Dict untouched, collected count and full result equal to base, black, repo-wide ruff, mypy</name>
  <precondition>Task 1 is committed with `TESTS_MASK = EQUAL` and no HALT heading.</precondition>
  <reversibility rating="reversible">Measurement and evidence only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-TESTS-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PYTEST_COLLECTED_BEFORE`, `PYTEST_RESULT_BEFORE`, `MYPY_STDOUT_SHA256_BEFORE`)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md (§ Leg (c))
  </read_first>
  <action>
1. **`## Leg (c) census`.** Over `git diff "$PHASE_BASE_SHA" HEAD` for the three files, with headers and blank lines excluded:
   - Quote the per-file added and removed line counts.
   - Every changed line matches `(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)`. Record `NON_TYPING_LINES_70_08 = 0`.
   - No changed line contains the word `assert`. Record `ASSERT_LINES_70_08 = 0`.
   - `git diff --quiet "$PHASE_BASE_SHA" HEAD -- tests/test_authors_pipeline_stage_gate.py` exits 0, and `sed -n 515p tests/test_authors_pipeline_stage_gate.py` contains `ast.Dict`.

2. **`## Gates`.** Quote each command with its exit code:
   - `uv run black --check` on the three files
   - `uv run ruff check .`
   - `uv run mypy typsphinx/ 2>/dev/null`, recording `MYPY_STDOUT_SHA256_70_08 = `, which must equal `MYPY_STDOUT_SHA256_BEFORE`
   - the unanchored collect count, recorded as `PYTEST_COLLECTED_70_08 = `, which must equal `PYTEST_COLLECTED_BEFORE`
   - `LC_ALL=C uv run pytest -q -rs -p no:cacheprovider`, which must exit 0, recording `PYTEST_RESULT_70_08 = `, which must equal `PYTEST_RESULT_BEFORE`

3. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CONV-TESTS-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; P="tests/conftest.py tests/test_bundle_layout_sweep_gate.py tests/test_include_edge_derivation_unit.py" &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -vE '^[+-][[:space:]]*$' | grep -qvE '(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)' &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -qwE 'assert|@preview' &amp;&amp; git diff --quiet "$B" HEAD -- tests/test_authors_pipeline_stage_gate.py &amp;&amp; sed -n 515p tests/test_authors_pipeline_stage_gate.py | grep -qF 'ast.Dict' &amp;&amp; uv run black --check $P &amp;&amp; uv run ruff check . &amp;&amp; [ "$(uv run mypy typsphinx/ 2&gt;/dev/null | sha256sum | cut -d' ' -f1)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; [ "$(k MYPY_STDOUT_SHA256_70_08)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; CC="$(LC_ALL=C uv run pytest --collect-only -q -p no:cacheprovider 2&gt;/dev/null | grep -oE '[0-9]+ tests? collected' | grep -oE '^[0-9]+')" &amp;&amp; [ -n "$CC" ] &amp;&amp; [ "$CC" = "$(k PYTEST_COLLECTED_BEFORE "$BE")" ] &amp;&amp; [ "$(k PYTEST_COLLECTED_70_08)" = "$CC" ] &amp;&amp; R="$(LC_ALL=C uv run pytest -q -p no:cacheprovider 2&gt;&amp;1 | tail -n 1 | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" &amp;&amp; [ -n "$R" ] &amp;&amp; [ "$R" = "$(k PYTEST_RESULT_BEFORE "$BE")" ] &amp;&amp; [ "$(k PYTEST_RESULT_70_08)" = "$R" ] &amp;&amp; [ "$(k NON_TYPING_LINES_70_08)" = 0 ] &amp;&amp; [ "$(k ASSERT_LINES_70_08)" = 0 ] &amp;&amp; for s in '## Leg (c) census' '## Gates'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$(k BASE_70_08)" HEAD | grep -vxF -e tests/conftest.py -e tests/test_bundle_layout_sweep_gate.py -e tests/test_include_edge_derivation_unit.py -e "$F" -e "$D/70-08-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- a changed non-blank line without a typing name, or containing `assert` or `@preview`
- the authors pipeline gate changed, or its line 515 lacking `ast.Dict`
- black or ruff reporting anything
- the mypy stdout hash differing from base
- the collected count or the full result differing from base
- a key wrong
- a section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `NON_TYPING_LINES_70_08 = 0` and `ASSERT_LINES_70_08 = 0`.
    - `tests/test_authors_pipeline_stage_gate.py` has no diff against base, and its line 515 contains `ast.Dict`.
    - `PYTEST_COLLECTED_70_08` equals `PYTEST_COLLECTED_BEFORE`, and `PYTEST_RESULT_70_08` equals `PYTEST_RESULT_BEFORE`.
    - `uv run black --check` on the three files and `uv run ruff check .` exit 0, and the mypy stdout hash equals base.
  </acceptance_criteria>
  <done>The tests/ conversion changes only import and annotation lines, touches no assertion, leaves `ast.Dict` alone, and leaves the collected and passed counts exactly as they were.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruff --fix → test modules | a rewritten test that asserts less would weaken the gates every later phase relies on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-17 | Tampering | a test assertion weakened or edited | high | mitigate | No changed line may contain `assert`; masked hashes equal base (assert expressions are inside the hash); collected and passed counts equal base. |
| T-70-18 | Tampering | `ast.Dict` corrupted by a blind rewrite | medium | mitigate | The file is outside this plan's set, and the verify requires an empty diff and `ast.Dict` at line 515. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan modifies
`tests/conftest.py`, `tests/test_bundle_layout_sweep_gate.py` and
`tests/test_include_edge_derivation_unit.py`, and creates `70-CONV-TESTS-EVIDENCE.md`, with the keys
`BASE_70_08`, `VENV_HOME`, `VENV_VERSION_INFO`, `TESTS_MASK`, `NON_TYPING_LINES_70_08`,
`ASSERT_LINES_70_08`, `MYPY_STDOUT_SHA256_70_08`, `PYTEST_COLLECTED_70_08` and
`PYTEST_RESULT_70_08`.
</artifacts_this_phase_produces>

<verification>
The three test files carry zero UP006/UP035 findings, their masked hashes equal base, no assertion
changed, and the suite's collected and passed counts are unchanged.
</verification>

<success_criteria>
The last file-disjoint wave-3 conversion lands lint-green, ready for 70-09's flip.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-08-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
