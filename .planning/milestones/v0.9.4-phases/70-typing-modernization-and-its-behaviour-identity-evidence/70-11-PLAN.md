---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 11
type: execute
wave: 5
depends_on: ["70-02", "70-03", "70-09"]
files_modified:
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-RUNTIME-EVIDENCE.md
autonomous: true
requirements: [QUA-12]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Measured on the post-flip tree in wave 5, in a worktree provisioned with the same lock, extras and interpreter as the base. VENV_HOME and VENV_VERSION_INFO equal 70-02''s (ROADMAP constraint 6).'
    - 'Leg (b) / SC#4: PYTEST_COLLECTED_AFTER equals PYTEST_COLLECTED_BEFORE, and PYTEST_RESULT_AFTER equals PYTEST_RESULT_BEFORE, from a full `LC_ALL=C` run that exits 0.'
    - 'Edge QUA-12/precision: both pytest values come from the same extraction as the base. The collected count uses the unanchored `[0-9]+ tests? collected` match, and the result uses the passed/failed/skipped/error/xfail/xpass tokens with warnings excluded, so the `=` decoration and the warning count cannot create or hide a difference.'
    - 'The skip reasons after the conversion equal the base skip reasons once line numbers are stripped (SKIP_REASONS_MATCH = YES), so an equal skip count cannot hide a changed reason.'
    - 'D-05 / leg (d) / SC#4: the corpus list enumerated from HEAD''s tests/ tree hashes to CORPUS_LIST_SHA256. It is built with 70-03''s corpus loop, unchanged, and the per-project manifest hashes to CORPUS_MANIFEST_SHA256_BEFORE, so every project''s exit status, `.typ` count and `.typ` bytes are identical.'
    - 'D-06: failing and zero-output projects are compared like every other project. D-08: a five-project pilot is timed on the after side before the full run, and CORPUS_EXTRAPOLATED_SECONDS_AFTER is recorded; above 1800 the plan halts for the owner and never narrows the corpus.'
    - 'Edge QUA-12/encoding: `.typ` equality is byte-level SHA-256 of each file inside each project digest, and the manifest equality is byte-level `cmp`. No normalisation is applied to either side.'
  artifacts:
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-RUNTIME-EVIDENCE.md
      provides: 'Legs (b) and (d) after-side, verdict keys'
      contains: 'LEG_D_VERDICT = MET'
  key_links:
    - from: '70-CORPUS-DOCS-BASE-EVIDENCE.md corpus-manifest and 70-03-PLAN.md <corpus_loop>'
      to: 'this plan''s after-side manifest'
      via: 'the identical loop over the identical list; compared by CORPUS_MANIFEST_SHA256_BEFORE'
      pattern: 'CORPUS_MANIFEST_SHA256_AFTER = '
  prohibitions:
    - statement: 'MUST NOT write off any pytest-count, skip-reason or corpus-digest difference as nondeterminism, environment or noise, or drop, reorder or re-scope a corpus project; every difference is recorded and halts the plan for the owner.'
      requirement_id: QUA-12
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Measure the runtime half of the after side on the post-flip tree: QUA-12 leg (b), pytest collected and passed counts, and leg (d), the byte-identical `.typ` corpus over D-05's list with D-06's failing projects kept and D-08's pilot run first.

Purpose: legs (a), (c) and (e) show the code is unchanged apart from annotations. These two legs show that what the code does is unchanged: the same tests collect and pass, and every fixture emits the same bytes.

Output: `70-AFTER-RUNTIME-EVIDENCE.md` with `LEG_B_VERDICT` and `LEG_D_VERDICT`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CORPUS-DOCS-BASE-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-03-PLAN.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every verify
command with the Bash tool `timeout` set to 600000. Run pytest and every Sphinx build under
`LC_ALL=C`.

**Scratch.** Make one scratch directory `S` with `mktemp -d` outside the repository, and record
`SCRATCH_70_11 = <path>`. Keep it until this plan's last verify passes.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value. Append long outputs with shell redirection
between tilde fences.

**Commits.** Stage the evidence file by explicit path. On any failure path, write
`## HALT: <reason>` into the evidence file, commit it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: leg (b) after side — collected count, full LC_ALL=C result and skip reasons on the post-flip tree, compared with the base keys</name>
  <precondition>`70-FLIP-EVIDENCE.md` exists at HEAD with no HALT heading, and pyproject.toml's `[tool.ruff.lint]` section names neither UP006 nor UP035. Otherwise write `## HALT: flip not merged` and stop.</precondition>
  <reversibility rating="reversible">Measurement and one evidence file.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-RUNTIME-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PYTEST_COLLECTED_BEFORE`, `PYTEST_RESULT_BEFORE`, the `pytest-skips-before` block, the VENV keys)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md (§ Leg (b); Assumption A2)
  </read_first>
  <action>
1. Create `70-AFTER-RUNTIME-EVIDENCE.md` with the heading `# Phase 70 — After-side Runtime Evidence`. Before any commit, record `BASE_70_11 = ` from `git rev-parse HEAD`. Record `VENV_HOME = ` and `VENV_VERSION_INFO = ` after provisioning; both must equal 70-02's. Quote the precondition checks.

2. **`## Leg (b) — pytest after`.**
   - Record `PYTEST_COLLECTED_AFTER = ` with 70-02's unanchored extraction.
   - Run `LC_ALL=C uv run pytest -q -rs -p no:cacheprovider > "$S/pytest-after.out" 2>&1; echo "exit:$?"`, which must print `exit:0`.
   - Record `PYTEST_RESULT_AFTER = ` with 70-02's token extraction from the last line, and `PYTEST_WARNINGS_AFTER = ` for information.
   - Quote the `SKIPPED` lines in a `~~~text pytest-skips-after` block.
   - Compare this block with `pytest-skips-before` after stripping `:<line>:` numbers with `sed -E 's/:[0-9]+:/:/'`. Record `SKIP_REASONS_MATCH = YES` if the sorted results are equal. Otherwise record `NO`, quote the difference, write `## HALT: skip reasons changed`, and stop.
   - Both counts must equal their base keys. Any difference: write `## HALT: leg (b)`, quote both summaries, and stop. Never attribute it to the environment.

   Record `LEG_B_VERDICT = MET`.

3. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-AFTER-RUNTIME-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; ! sed -n '/^\[tool\.ruff\.lint\]/,/^\[/p' pyproject.toml | grep -qE 'UP006|UP035' &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = "$(k VENV_VERSION_INFO "$BE")" ] &amp;&amp; [ "$(sed -n 's/^home = //p' .venv/pyvenv.cfg)" = "$(k VENV_HOME "$BE")" ] &amp;&amp; C="$(LC_ALL=C uv run pytest --collect-only -q -p no:cacheprovider 2&gt;/dev/null | grep -oE '[0-9]+ tests? collected' | grep -oE '^[0-9]+')" &amp;&amp; [ -n "$C" ] &amp;&amp; [ "$C" = "$(k PYTEST_COLLECTED_BEFORE "$BE")" ] &amp;&amp; [ "$(k PYTEST_COLLECTED_AFTER)" = "$C" ] &amp;&amp; S="$(k SCRATCH_70_11)" &amp;&amp; R="$(tail -n 1 "$S/pytest-after.out" | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" &amp;&amp; [ -n "$R" ] &amp;&amp; [ "$R" = "$(k PYTEST_RESULT_BEFORE "$BE")" ] &amp;&amp; [ "$(k PYTEST_RESULT_AFTER)" = "$R" ] &amp;&amp; [ "$(sed -n '/^~~~text pytest-skips-after$/,/^~~~$/p' "$F" | sed '1d;$d' | sed -E 's/:[0-9]+:/:/' | LC_ALL=C sort)" = "$(sed -n '/^~~~text pytest-skips-before$/,/^~~~$/p' "$BE" | sed '1d;$d' | sed -E 's/:[0-9]+:/:/' | LC_ALL=C sort)" ] &amp;&amp; [ "$(k SKIP_REASONS_MATCH)" = YES ] &amp;&amp; [ "$(k LEG_B_VERDICT)" = MET ] &amp;&amp; grep -qxF '## Leg (b) — pytest after' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- not in a worktree, or the flip not merged
- the interpreter differing from 70-02's
- the collected count empty or differing from base
- the recorded run's result tokens differing from `PYTEST_RESULT_BEFORE`
- the skip reasons differing from base once line numbers are stripped
- a key or the verdict wrong
- the section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `PYTEST_COLLECTED_AFTER` equals `PYTEST_COLLECTED_BEFORE`, and `PYTEST_RESULT_AFTER` equals `PYTEST_RESULT_BEFORE`, from a run that exited 0.
    - `SKIP_REASONS_MATCH = YES`, and `LEG_B_VERDICT = MET`.
  </acceptance_criteria>
  <done>The same tests collect, pass and skip, for the same reasons, after the conversion as before it.</done>
</task>

<task type="auto">
  <name>Task 2: Leg (d) after side: D-05 list identity, D-08 after-side pilot, and the full corpus with 70-03's loop, compared by manifest hash with D-06's failing projects kept</name>
  <precondition>Task 1 is committed with `LEG_B_VERDICT = MET`. `70-CORPUS-DOCS-BASE-EVIDENCE.md` holds `CORPUS_CONTROL = EQUAL`, and its `PHASE_BASE_SHA_SEEN` equals 70-02's `PHASE_BASE_SHA`. Otherwise write `## HALT: base corpus evidence` and stop.</precondition>
  <reversibility rating="reversible">Builds into scratch only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-RUNTIME-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-03-PLAN.md (`<corpus_loop>`: the loop reused here word for word)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CORPUS-DOCS-BASE-EVIDENCE.md (`CORPUS_LIST_SHA256`, `CORPUS_PROJECT_COUNT`, `CORPUS_MANIFEST_SHA256_BEFORE`, the `corpus-manifest` block)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md (D-05, D-06, D-08, D-10)
  </read_first>
  <action>
1. **`## Corpus list at HEAD (D-05)`.** Run `git ls-tree -r --name-only HEAD -- tests | grep -E '(^|/)conf\.py$' | sed 's#/conf\.py$##' | LC_ALL=C sort > "$S/corpus-list.txt"`. Its SHA-256 must equal `CORPUS_LIST_SHA256`, and its line count must equal `CORPUS_PROJECT_COUNT`. Record `CORPUS_LIST_SHA256_AFTER = `.

2. **`## D-08 pilot (after side)`.** Run 70-03's corpus loop over the first five entries into `$S/pilot`. Record:
   - `CORPUS_PILOT_SECONDS_AFTER = `
   - `CORPUS_EXTRAPOLATED_SECONDS_AFTER = ` (sum ÷ 5 × count, rounded up)

   Above 1800, write `## HALT: D-08 corpus time for the owner` and stop. Never narrow the corpus.

3. **`## Post-flip manifest`.** Run the corpus loop over the whole list into `$S/after`, with the manifest at `$S/corpus-after.txt`. Record:
   - `CORPUS_FULL_SECONDS_AFTER = `
   - `CORPUS_MANIFEST_SHA256_AFTER = `
   - the manifest in a `~~~text corpus-manifest-after` block
   - `cmp` of `$S/corpus-after.txt` against the `corpus-manifest` block extracted from 70-03's evidence, with its exit code

   Record `LEG_D_VERDICT = MET` only when the two hashes are equal. That means every project, including the failing and zero-output ones (D-06), has the same exit code, `.typ` count and `.typ` digest.

   On any inequality:
   - quote `diff` of the two manifests
   - for each differing project, rebuild it once more on this side, and quote `diff -r` of its `.typ` files against a fresh base build of the same project in a detached worktree at `PHASE_BASE_SHA`; provision that worktree with its own `uv sync` and remove it after with `git worktree remove --force` then `git worktree prune`
   - write `## HALT: leg (d) difference for the owner` and stop

   Never classify a difference as nondeterminism; 70-03's cross-path control already showed the base is reproducible.

4. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-AFTER-RUNTIME-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; CE="$D/70-CORPUS-DOCS-BASE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; [ "$(k PHASE_BASE_SHA_SEEN "$CE")" = "$(k PHASE_BASE_SHA "$BE")" ] &amp;&amp; [ "$(k CORPUS_CONTROL "$CE")" = EQUAL ] &amp;&amp; L="$(git ls-tree -r --name-only HEAD -- tests | grep -E '(^|/)conf\.py$' | sed 's#/conf\.py$##' | LC_ALL=C sort)" &amp;&amp; [ "$(printf '%s\n' "$L" | sha256sum | cut -d' ' -f1)" = "$(k CORPUS_LIST_SHA256 "$CE")" ] &amp;&amp; [ "$(k CORPUS_LIST_SHA256_AFTER)" = "$(k CORPUS_LIST_SHA256 "$CE")" ] &amp;&amp; S="$(k SCRATCH_70_11)" &amp;&amp; [ -s "$S/corpus-after.txt" ] &amp;&amp; [ "$(wc -l &lt; "$S/corpus-after.txt")" = "$(k CORPUS_PROJECT_COUNT "$CE")" ] &amp;&amp; MA="$(sha256sum &lt; "$S/corpus-after.txt" | cut -d' ' -f1)" &amp;&amp; [ "$MA" = "$(k CORPUS_MANIFEST_SHA256_BEFORE "$CE")" ] &amp;&amp; [ "$(k CORPUS_MANIFEST_SHA256_AFTER)" = "$MA" ] &amp;&amp; [ "$(sed -n '/^~~~text corpus-manifest-after$/,/^~~~$/p' "$F" | sed '1d;$d' | sha256sum | cut -d' ' -f1)" = "$MA" ] &amp;&amp; cmp -s "$S/corpus-after.txt" &lt;(sed -n '/^~~~text corpus-manifest$/,/^~~~$/p' "$CE" | sed '1d;$d') &amp;&amp; for d in $(cut -d' ' -f1 "$S/corpus-after.txt"); do o="$S/after/${d//\//__}"; dg="$( (cd "$o" &amp;&amp; find . -type f -name '*.typ' -print0 | LC_ALL=C sort -z | xargs -0 -r sha256sum) | sha256sum | cut -d' ' -f1)"; grep -qxE "$d exit=[0-9]+ typ=[0-9]+ digest=$dg" "$S/corpus-after.txt" || { echo "digest drift: $d"; exit 1; }; done &amp;&amp; [ "$(k CORPUS_EXTRAPOLATED_SECONDS_AFTER)" -le 1800 ] &amp;&amp; [ "$(k LEG_D_VERDICT)" = MET ] &amp;&amp; for s in '## Corpus list at HEAD (D-05)' '## D-08 pilot (after side)' '## Post-flip manifest'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$(k BASE_70_11)" HEAD | grep -vxF -e "$F" -e "$D/70-11-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or a `digest drift:` or `missing section:` line printed. Each of these makes it fail:
- the base corpus evidence not pinned to `PHASE_BASE_SHA`, or its control not EQUAL
- the list at HEAD differing from the base list
- the after manifest missing, of the wrong length, or hashing differently from the base manifest
- the evidence block or the byte-level `cmp` against the base block disagreeing
- a project's recorded digest not matching its `.typ` files on disk
- the after-side extrapolation above 1800
- the verdict not MET
- a section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `CORPUS_LIST_SHA256_AFTER` equals `CORPUS_LIST_SHA256`.
    - `CORPUS_MANIFEST_SHA256_AFTER` equals `CORPUS_MANIFEST_SHA256_BEFORE`, and `cmp` of the two manifests exits 0.
    - Every after-side project digest re-derives from the `.typ` files on disk.
    - `CORPUS_EXTRAPOLATED_SECONDS_AFTER` is at most 1800, and `LEG_D_VERDICT = MET`.
  </acceptance_criteria>
  <done>Every fixture in the corpus, failing ones included, emits byte-identical `.typ` with the same exit status after the conversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typsphinx builders → emitted `.typ` | the product output users compile; any byte change would be a behaviour change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-24 | Tampering | an emitted-output change hidden in an annotation rewrite | high | mitigate | Byte-level manifest equality over every project, with per-project digests re-derived from disk in the verify. |
| T-70-25 | Repudiation | a difference written off as flakiness | medium | mitigate | Any inequality halts for the owner; 70-03's cross-path control removes the path and time explanations up front. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan creates
`70-AFTER-RUNTIME-EVIDENCE.md`, with the keys `BASE_70_11`, `SCRATCH_70_11`, `VENV_HOME`,
`VENV_VERSION_INFO`, `PYTEST_COLLECTED_AFTER`, `PYTEST_RESULT_AFTER`, `PYTEST_WARNINGS_AFTER`,
`SKIP_REASONS_MATCH`, `LEG_B_VERDICT`, `CORPUS_LIST_SHA256_AFTER`, `CORPUS_PILOT_SECONDS_AFTER`,
`CORPUS_EXTRAPOLATED_SECONDS_AFTER`, `CORPUS_FULL_SECONDS_AFTER`, `CORPUS_MANIFEST_SHA256_AFTER`
and `LEG_D_VERDICT`, and the blocks `pytest-skips-after` and `corpus-manifest-after`.
</artifacts_this_phase_produces>

<verification>
Legs (b) and (d) are MET on the post-flip tree under the base's lock, extras and interpreter.
</verification>

<success_criteria>
SC#4's pytest and `.typ` halves: identical collected and passed counts, and byte-identical corpus
output compared over the list enumerated at the base.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-11-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
