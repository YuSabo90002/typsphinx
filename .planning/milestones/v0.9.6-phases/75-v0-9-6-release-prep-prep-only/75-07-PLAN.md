---
phase: 75-v0-9-6-release-prep-prep-only
plan: 07
type: execute
wave: 4
depends_on: ["75-01", "75-02", "75-03", "75-04", "75-05", "75-06"]
files_modified:
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CLOSEOUT-GUARD.md
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-SC5-INVARIANTS.md
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-HANDOFF.md
autonomous: true
requirements: [REL-15, REL-16]
# REL-15 is a coverage ID only (ROADMAP constraint 1 and the Phase 75 cross-cutting
# constraints): no plan closes it, no plan touches its checkbox, and it is checked at
# /gsd-complete-milestone against the four observations this plan's handoff enumerates.
# REL-16 is settled on the record by this phase, but its checkbox is moved by
# phase-completion tooling at close, never by a plan.
requirements-completed: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC5 fence observation 2: `sha256sum`, `wc -l` and the verbatim `grep -n ''REL-15''` transcript are re-taken at phase close and MATCH the baseline 75-01 recorded at phase head, with `git diff --name-only -- .planning/REQUIREMENTS.md` empty. REL-15''s checkbox is read directly out of the file as unchecked, never inferred.'
    - 'SC5 probe observation 2 of 2: every tag, PyPI and GitHub Release probe from observation 1 is repeated and still empty, each with its `v0.9.2` positive control still present — and the two observations are separated by the bump commit, the local green-tree runs, the trial merge, the push and the dispatched CI run rather than by wall-clock luck.'
    - 'SC5 scope fence: against the milestone base the `typsphinx/` diff is still exactly `typsphinx/pathfmt.py` and `typsphinx/translator.py`, which is Phase 74''s own diff and proves this phase added nothing there; and against this phase''s own base the product diff is exactly the five files of the bump commit. Both are paired with controls so neither can pass vacuously.'
    - 'SC5 handoff: `75-HANDOFF.md` is standalone — a reader needs no second file open — and enumerates every step `/gsd-complete-milestone` must execute, in order, each with an owner and an ordering note.'
    - 'The handoff records four facts that are easy to lose: the `## [0.9.6]` heading date is the prep date, may differ from the tag date and is not rewritten (D-05); `release.yml`''s `create-release` job has never run on a v0.9.x tag and a failure there is handled inside the release work rather than deferred; the `typsphinx-doc-translations` `update-pin.yml` dispatch is manual; and REL-16''s checkbox, unlike REL-15''s, legitimately closes with this phase.'
    - 'The handoff reproduces the fence''s re-verification commands inline, names the third observation — the one taken after `phase.complete`-family tooling including `/gsd-verify-work`''s inline transition has run — and carries the reversion recipe and the scratch backup paths.'
    - 'The Dependabot ordering note is keyed on 75-05''s freshly measured census, stated conditionally, and never on either figure recorded before this phase executed.'
  artifacts:
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-CLOSEOUT-GUARD.md
      provides: 'the phase-close re-verification section with its MATCH result, and the operator instructions for the third observation'
      contains: 'REQ_SHA256_CLOSE = '
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-SC5-INVARIANTS.md
      provides: 'probe observation 2 of 2, the scope fence with its controls, and SC5_VERDICT'
      contains: 'OBS2_AT = '
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-HANDOFF.md
      provides: 'the standalone ordered procedure /gsd-complete-milestone executes, the four observations REL-15 is checked on, and the SC1 to SC5 recap'
      contains: 'update-pin.yml'
  key_links:
    - from: '75-CLOSEOUT-GUARD.md REQ_SHA256_BASE'
      to: 'REQ_SHA256_CLOSE in the same file'
      via: 'the same sha256sum command re-run at phase close'
      pattern: 'REQ_SHA256_CLOSE = '
    - from: '75-HANDOFF.md'
      to: '/gsd-complete-milestone'
      via: 'an ordered checklist with an owner per step, read standalone by a different command'
      pattern: 'update-pin.yml'
    - from: '75-PREFLIGHT-EVIDENCE.md OPEN_PRS and DEPENDABOT_OPEN_PRS'
      to: '75-HANDOFF.md Dependabot ordering note'
      via: 'read with sed and written as a conditional rule keyed on a re-read at release time'
      pattern: 'OPEN_PRS = '
  prohibitions:
    - statement: 'MUST NOT edit, stage or commit `.planning/REQUIREMENTS.md`, and MUST NOT move REL-15''s or REL-16''s checkbox. On an unbidden change, run `git checkout -- .planning/REQUIREMENTS.md`, report it, and never commit it.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT create or push a tag, upload to PyPI, create a GitHub Release, open, comment on or merge a pull request, dispatch `update-pin.yml`, or dispatch a second CI run; the handoff describes these steps for a later command to execute.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT edit any file under `typsphinx/`, `tests/`, `.github/`, `docs/`, `flake.nix`, `tox.ini`, `pyproject.toml`, `uv.lock`, `README.md` or `CHANGELOG.md`; this plan writes only phase-evidence and handoff documents.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT build any `gh` or shell command body in the handoff by command substitution from file content; a body comes from a file path.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT write the discussion-time or research-time Dependabot count into the handoff as the operative figure; the ordering note cites 75-05''s measured census and instructs a re-read at release time.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
---

<objective>
Close the phase on the record (SC5) and hand it off:
- the REQUIREMENTS fence re-verified at phase close, MATCH against the phase-head baseline
- probe observation 2 of 2, separated from observation 1 by three intervening waves
- the scope fence with its controls, on both the milestone range and this phase's own range
- `75-HANDOFF.md`, standalone, enumerating every step `/gsd-complete-milestone` must execute
- the SC1 to SC5 roll-up and the REL-16 settlement record

Purpose: REL-15 must still read unchecked when this phase ends, and the flip that would change that
has landed at 9 of the 10 prior release-prep closes — so the fence's second observation happens here
and its third is written down for the operator. The handoff is what turns a prep-only phase into an
executable release.

Output: `75-CLOSEOUT-GUARD.md` (close section), `75-SC5-INVARIANTS.md` (observation 2),
`75-HANDOFF.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md
@.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` and
run project tooling through `uv run`.

**Evidence key lines.** Bare values only, `KEY = value` on its own line, explanation on the next.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_75_07`; helper prefix `p7507_`.

**No command substitution into a `gh` body.** This project's sandbox has refused that shape twice.
Anything the handoff tells a later operator to post comes from `--body-file <path>`.

**Commits.** Plain `git commit`, `.planning/` only. Never a GSD commit helper. Never write
`75-VERIFICATION.md`, which is a reserved name the verifier overwrites.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: gsd/v0\.9\.6-milestone -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[3456] -->

## Planning-time census (2026-09-20; context only — re-measure, never trust)

| Fact | Value at planning |
|------|-------------------|
| Milestone base | `6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b` |
| `typsphinx/` diff against it | exactly `typsphinx/pathfmt.py` and `typsphinx/translator.py`, both Phase 74's |
| Expected phase-scoped product diff | the five bump-commit files and nothing else |
| Prior handoff shapes | `46-HANDOFF.md`'s seven-item publish checklist (the genuine-publish precedent); `73-HANDOFF.md` and `71-HANDOFF.md` for the recap, "Recorded without acting" and fence-reproduction sections |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: fence observation 2 at phase close, probe observation 2 of 2, and the scope fence with its controls</name>
  <precondition>`75-CLOSEOUT-GUARD.md` records `REQ_SHA256_BASE`, `REQ_LINES_BASE` and `PHASE_BASE_SHA`, and `75-SC5-INVARIANTS.md` records `OBS1_AT`.</precondition>
  <reversibility rating="reversible">Every command is a read or an append to a phase-evidence file; `.planning/REQUIREMENTS.md` is read and never written.</reversibility>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-CLOSEOUT-GUARD.md, .planning/phases/75-v0-9-6-release-prep-prep-only/75-SC5-INVARIANTS.md</files>
  <read_first>
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CLOSEOUT-GUARD.md (the baseline keys and the re-verification protocol this task executes)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-SC5-INVARIANTS.md (observation 1's probes and keys, to repeat exactly)
    - .planning/REQUIREMENTS.md (the file under guard — read it, never write it)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CI-EVIDENCE.md (`PUSHED_SHA`, `SC4_CI_VERDICT`), 75-GREEN-TREE-EVIDENCE.md (`SC4_LOCAL_VERDICT`) and 75-PREFLIGHT-EVIDENCE.md (`TRIAL_MERGE_VERDICT`)
  </read_first>
  <action>
1. **Append `## Re-verification at phase close` to `75-CLOSEOUT-GUARD.md`.** Run and record, each
   verbatim:
   - `REQ_SHA256_CLOSE = ` from `sha256sum .planning/REQUIREMENTS.md`, first field only.
   - `REQ_LINES_CLOSE = ` from `wc -l < .planning/REQUIREMENTS.md`.
   - `git diff --name-only -- .planning/REQUIREMENTS.md`, expected to print nothing, and
     `git diff --name-only <PHASE_BASE_SHA> HEAD -- .planning/REQUIREMENTS.md`, also empty.
   - `grep -n 'REL-15' .planning/REQUIREMENTS.md`, pasted verbatim beside § "The lines under
     guard"'s transcript. Key `REL15_LINES_MATCH = yes` only when the two are byte-identical.
   - `grep -n 'REL-16' .planning/REQUIREMENTS.md`, recorded beside the expected-to-move callout.
     Note whether it has moved yet; at this point it should not have, because no plan edits the file
     and the tooling has not yet run.
   - The two direct reads SC5 requires: `REL15_CHECKBOX_AT_CLOSE = ` from
     `grep -m1 -E '^- \[.\] \*\*REL-15\*\*' .planning/REQUIREMENTS.md`, which must still show an
     unchecked box, and `REL15_TRACEABILITY_AT_CLOSE = ` from
     `grep -m1 -E '^\| REL-15 \|' .planning/REQUIREMENTS.md`, which must still read `Pending`.
   - `FENCE_CLOSE_VERDICT = MATCH` only when the digest, the line count, both empty diffs and the
     byte-identical REL-15 transcript all hold. Otherwise `MISMATCH`: run
     `git checkout -- .planning/REQUIREMENTS.md`, re-run every probe to show MATCH, record the whole
     sequence, and return a blocking checkpoint. Never commit a changed requirements file.
   - Diff `.planning/ROADMAP.md` and `.planning/STATE.md` against the scratch backups 75-01 recorded
     and transcribe both results; those two files have been mangled by the same tooling before, in
     ways a checkbox-only fence would not catch.

2. **Append `## Observation 2 of 2` to `75-SC5-INVARIANTS.md`.** Record `OBS2_AT = ` from
   `date -u +%FT%TZ` and repeat every probe from observation 1, with the same key names suffixed
   `_OBS2` where they would collide:
   - `git tag -l 'v0.9.6'` empty and `git tag -l 'v0.9.2'` non-empty.
   - `git ls-remote --tags origin`: `refs/tags/v0.9.2` present exactly once, no `v0.9.6`.
   - PyPI 404 for `0.9.6` and 200 for `0.9.2`.
   - `gh release list`: latest still `v0.9.2`, zero `v0.9.6` entries.
   - `gh run list --workflow=release.yml`: zero runs created after the v0.9.2 publish, with the same
     `RELEASE_POSITIVE_CONTROL` run id still resolving.
   - `git ls-remote --heads origin`: zero `gsd/v0.9.6-milestone` decoys, and the milestone branch's
     origin head recorded.
   - `gh pr list --head gsd/v0.9.6-doctest-block-rendering-and-release --state all`: zero.
   - `sed -n 7p pyproject.toml`, which now reads `version = "0.9.6"` — the one probe whose answer
     legitimately differs from observation 1 — and `VERSION_BUMP_COMMITS_OBS2 = ` from
     `git log --format=%h -G '^version = ' 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b..HEAD -- pyproject.toml`,
     which must be exactly 1: the bump commit, and only it.
   - Record `SEPARATION = `, a short `|`-joined list of what happened between the two observations —
     the bump commit, the local green-tree runs, the trial merge, the push and the dispatched CI run
     — with each item cited by its own evidence key, so the separation is structural rather than
     temporal.

3. **Append `## Scope fence` to `75-SC5-INVARIANTS.md`.** Record:
   - `MILESTONE_TYPSPHINX_DIFF = ` from
     `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b HEAD -- typsphinx`,
     `LC_ALL=C`-sorted and `|`-joined, which must be exactly
     `typsphinx/pathfmt.py|typsphinx/translator.py` — Phase 74's own diff, proving this phase added
     nothing under `typsphinx/`.
   - **Pathspec control:** `git diff --name-only <milestone base> HEAD -- typsphinx/translator.py`
     must be non-empty, proving the pathspec resolves to a real change rather than passing
     vacuously.
   - `PHASE_PRODUCT_DIFF = ` from
     `git diff --name-only <PHASE_BASE_SHA> HEAD -- . ':(exclude).planning'`, sorted and joined,
     which must be exactly
     `CHANGELOG.md|README.md|pyproject.toml|tests/test_changelog_page_gate.py|uv.lock` — the five
     files of the one commit and nothing else.
   - **Widened-diff control:** `git diff --name-only <PHASE_BASE_SHA> HEAD` without a pathspec must
     additionally list `.planning/` files, proving the range carries this phase's work and the fence
     is not measuring an empty range.
   - `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b HEAD -- .github flake.nix tox.ini`,
     empty, with the control `git ls-files -- .github/workflows/ci.yml flake.nix tox.ini` listing
     all three as tracked so the pathspecs resolve.
   - `POST_DISPATCH_PRODUCT_FILES = ` from
     `git diff --name-only <PUSHED_SHA> HEAD -- . ':(exclude).planning' | wc -l`, which must be 0:
     no product change landed after the CI run that tested the tree.
   - `SC5_VERDICT = MET` only when `FENCE_CLOSE_VERDICT` is `MATCH`, every observation-2 probe is
     empty with its control present, both diff sets are exactly as above, and
     `POST_DISPATCH_PRODUCT_FILES` is 0.

4. Commit both files.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; G="$D/75-CLOSEOUT-GUARD.md" &amp;&amp; S="$D/75-SC5-INVARIANTS.md" &amp;&amp; MB=6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b &amp;&amp; BR=gsd/v0.9.6-doctest-block-rendering-and-release &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; kg() { sed -n "s/^$1 = //p" "$G" | head -n 1; } &amp;&amp; ks() { sed -n "s/^$1 = //p" "${2:-$S}" | head -n 1; } &amp;&amp; PB="$(kg PHASE_BASE_SHA)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; LIVE="$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" &amp;&amp; [ "$LIVE" = "$(kg REQ_SHA256_BASE)" ] &amp;&amp; [ "$LIVE" = "$(kg REQ_SHA256_CLOSE)" ] &amp;&amp; [ "$(wc -l &lt; .planning/REQUIREMENTS.md)" = "$(kg REQ_LINES_CLOSE)" ] &amp;&amp; [ "$(kg REQ_LINES_CLOSE)" = "$(kg REQ_LINES_BASE)" ] &amp;&amp; [ -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" ] &amp;&amp; [ -z "$(git diff --name-only "$PB" HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; [ "$(kg REL15_LINES_MATCH)" = yes ] &amp;&amp; [ "$(kg FENCE_CLOSE_VERDICT)" = MATCH ] &amp;&amp; grep -qE '^- \[ \] \*\*REL-15\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-15 \| Phase 75 \| Pending' .planning/REQUIREMENTS.md &amp;&amp; [ -n "$(kg REL15_CHECKBOX_AT_CLOSE)" ] &amp;&amp; [ -n "$(kg REL15_TRACEABILITY_AT_CLOSE)" ] &amp;&amp; grep -qxF '## Re-verification at phase close' "$G" &amp;&amp; [ -n "$(ks OBS1_AT)" ] &amp;&amp; [ -n "$(ks OBS2_AT)" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.6')" ] &amp;&amp; [ -n "$(git tag -l 'v0.9.2')" ] &amp;&amp; TL="$(git ls-remote --tags origin)" &amp;&amp; [ -n "$TL" ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.[3456]')" = 0 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.6/json)" = 404 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json)" = 200 ] &amp;&amp; [ "$(gh release list --limit 50 --json tagName,isLatest --jq '.[] | select(.isLatest) | .tagName')" = v0.9.2 ] &amp;&amp; [ "$(gh release list --limit 50 --json tagName --jq '[.[] | select(.tagName == "v0.9.6")] | length')" = 0 ] &amp;&amp; RPC="$(ks RELEASE_POSITIVE_CONTROL)" &amp;&amp; [ -n "$RPC" ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json databaseId -q "[.[] | select(.databaseId == $RPC)] | length")" = 1 ] &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; [ "$(printf '%s\n' "$HL" | grep -c 'gsd/v0\.9\.6-milestone')" = 0 ] &amp;&amp; [ "$(gh pr list --head "$BR" --state all --json number --jq 'length')" = 0 ] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.6"' ] &amp;&amp; [ "$(git log --format=%h -G '^version = ' "$MB"..HEAD -- pyproject.toml | wc -l)" = 1 ] &amp;&amp; [ "$(ks VERSION_BUMP_COMMITS_OBS2)" = 1 ] &amp;&amp; TD="$(git diff --name-only "$MB" HEAD -- typsphinx | LC_ALL=C sort | paste -sd'|')" &amp;&amp; [ "$TD" = 'typsphinx/pathfmt.py|typsphinx/translator.py' ] &amp;&amp; [ "$(ks MILESTONE_TYPSPHINX_DIFF)" = "$TD" ] &amp;&amp; [ -n "$(git diff --name-only "$MB" HEAD -- typsphinx/translator.py)" ] &amp;&amp; PD="$(git diff --name-only "$PB" HEAD -- . ':(exclude).planning' | LC_ALL=C sort | paste -sd'|')" &amp;&amp; [ "$PD" = 'CHANGELOG.md|README.md|pyproject.toml|tests/test_changelog_page_gate.py|uv.lock' ] &amp;&amp; [ "$(ks PHASE_PRODUCT_DIFF)" = "$PD" ] &amp;&amp; [ -n "$(git diff --name-only "$PB" HEAD | grep '^\.planning/')" ] &amp;&amp; [ -z "$(git diff --name-only "$MB" HEAD -- .github flake.nix tox.ini)" ] &amp;&amp; [ "$(git ls-files -- .github/workflows/ci.yml flake.nix tox.ini | wc -l)" = 3 ] &amp;&amp; P="$(ks PUSHED_SHA "$D/75-CI-EVIDENCE.md")" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ "$(ks POST_DISPATCH_PRODUCT_FILES)" = 0 ] &amp;&amp; [ "$(ks SC5_VERDICT)" = MET ] &amp;&amp; for s in '## Observation 2 of 2' '## Scope fence'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$G" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$S" &amp;&amp; ! grep -q '^## HALT' "$G" &amp;&amp; ! grep -q '^## HALT' "$S"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail: not in a worktree; `PHASE_BASE_SHA` malformed or not an ancestor of HEAD; the live requirements digest differing from either the base or the close key, or the two line counts differing; `.planning/REQUIREMENTS.md` showing any change, in the working tree or against the phase base; `REL15_LINES_MATCH` not `yes` or `FENCE_CLOSE_VERDICT` not `MATCH`; REL-15's checkbox no longer reading unchecked, or its traceability row no longer reading `Pending`; either direct-read key empty; the close section heading missing; either observation timestamp absent; a local or remote `v0.9.6` tag existing, or the `v0.9.2` control absent; `git ls-remote --tags origin` returning nothing, which would make the remote zeros vacuous; PyPI not answering 404 for `0.9.6` or 200 for the control; the latest GitHub Release not `v0.9.2`, or a `v0.9.6` Release existing; `RELEASE_POSITIVE_CONTROL` absent or no longer resolving to a real `release.yml` run; a `gsd/v0.9.6-milestone` decoy on origin; any pull request already open or closed against the milestone branch; `pyproject.toml` line 7 not reading `version = "0.9.6"`; the milestone-range version-bump commit count not exactly 1, live or recorded; the `typsphinx/` diff not exactly the two Phase 74 files or disagreeing with its key; the pathspec control empty; the phase-scoped product diff not exactly the five bump files or disagreeing with its key; the widened-diff control listing no `.planning/` file; `.github/`, `flake.nix` or `tox.ini` changed since the milestone base, or the tracked-path control not listing all three; `PUSHED_SHA` malformed, or a product file changed after it; `POST_DISPATCH_PRODUCT_FILES` not 0; `SC5_VERDICT` not `MET`; either named section missing; a key line carrying an opening parenthesis; or a HALT heading in either file.</fails_when>
  </verify>
  <acceptance_criteria>
    - `REQ_SHA256_CLOSE` equals `REQ_SHA256_BASE` and both equal the live digest; `REQ_LINES_CLOSE` equals `REQ_LINES_BASE`; both requirements diffs are empty; `REL15_LINES_MATCH = yes`; `FENCE_CLOSE_VERDICT = MATCH`.
    - REL-15's checkbox reads unchecked and its traceability row reads `Pending`, both read directly out of the file and recorded as key lines.
    - Every observation-1 probe is repeated at `OBS2_AT` and still empty, each with its `v0.9.2` control present, and `SEPARATION` names the intervening work by evidence key.
    - `MILESTONE_TYPSPHINX_DIFF` is exactly `typsphinx/pathfmt.py|typsphinx/translator.py` with its pathspec control non-empty; `PHASE_PRODUCT_DIFF` is exactly the five bump files with its widened-diff control listing `.planning/` paths.
    - `POST_DISPATCH_PRODUCT_FILES = 0` and `SC5_VERDICT = MET`.
  </acceptance_criteria>
  <done>The fence held at its second observation, nothing irreversible exists, the scope fence holds with controls on both ranges, and no product file moved after the tree CI tested.</done>
</task>

<task type="auto">
  <name>Task 2: Write 75-HANDOFF.md — the standalone ordered procedure /gsd-complete-milestone executes</name>
  <precondition>`75-CI-EVIDENCE.md` records `SC4_CI_VERDICT`, `PUSHED_SHA` and `RUN_ID`, and `75-PREFLIGHT-EVIDENCE.md` records `PROTECTION_CONTEXTS`, `OPEN_PRS` and `DEPENDABOT_OPEN_PRS`.</precondition>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-HANDOFF.md</files>
  <read_first>
    - .planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md (the seven-item publish checklist with its Owner and Ordering lines — the only prior prep-only phase whose milestone actually published, and the structural model for items 1 to 7)
    - .planning/milestones/v0.9.5-phases/73-v0-9-5-close-prep-prep-only-unpublished/73-HANDOFF.md (the "What this phase satisfied", success-criteria recap, "Recorded without acting" and fence-reproduction sections)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-PREFLIGHT-EVIDENCE.md (`PROTECTION_CONTEXTS`, `PROTECTION_STRICT`, the merge-method reads, `OPEN_PRS`, `DEPENDABOT_OPEN_PRS`, `MAIN_MOVED`, `MERGE_TREE`)
    - .github/workflows/release.yml (the `validate` → `build` → `publish-pypi` → `create-release` job order and the `pypi` environment gate)
    - .planning/REQUIREMENTS.md REL-15 (quoted verbatim into the handoff)
  </read_first>
  <action>
Create `75-HANDOFF.md`, headed `# Phase 75 — Handoff to /gsd-complete-milestone`. It must be
readable standalone: every number it cites is written into it, not linked.

1. **`## What this phase satisfied`.** Quote REL-15 verbatim from `.planning/REQUIREMENTS.md` in a
   blockquote, then state plainly that it stays open until step 6 below runs, that no plan in this
   phase touched its checkbox, and that every SUMMARY of this phase declares
   `requirements-completed: []`. Then state that **REL-16 is different**: it closes inside this
   phase, its `### Known Limitations` section is in `## [0.9.6]`, and its checkbox may legitimately
   move at this phase's close — which is exactly why the fence is line-scoped to REL-15.

2. **`## Success criteria`.** Five rows, SC1 to SC5, each MET or not, each citing the evidence file
   and section that discharged it, with the load-bearing numbers inlined: the five-file bump commit
   SHA; the extractor's byte count and the digest equality; the two `### Known Limitations` headings;
   both pytest counts, both documentation warning integers and the linkcheck total; the CI run id
   with its job count; and the fence's two MATCHes.

3. **`## Checklist`.** Numbered steps, each with an **Owner** line and an **Ordering** line, in the
   order they must execute:
   1. **Open the pull request from `gsd/v0.9.6-doctest-block-rendering-and-release` to `main` and
      merge it.** Owner: `/gsd-complete-milestone`. Ordering: first; everything below depends on it.
      Name the required status checks verbatim from `75-PREFLIGHT-EVIDENCE.md`'s
      `PROTECTION_CONTEXTS`, state whether `strict` is set and what that implies (if `origin/main`
      has moved, merge it into the branch first and re-run CI), and record the observed merge method
      and its precedent. State that `75-PREFLIGHT-EVIDENCE.md`'s trial merge was clean at
      `MERGE_TREE`, that `MAIN_MOVED` read what it read, and that `origin/main` must be re-read at
      release time because it can move between then and now.
   2. **Push the `v0.9.6` tag on the merge commit.** Owner: `/gsd-complete-milestone`. Ordering:
      after step 1. State that this fires `.github/workflows/release.yml` and that no tag exists
      anywhere as of this phase's close, citing observation 2's probes.
   3. **Let `release.yml` run to completion: `validate`, `build`, `publish-pypi`, `create-release`.**
      Owner: `/gsd-complete-milestone`, with a human approval step. State plainly that the `pypi`
      GitHub Environment requires a **manual approval** and that waiting there is a gate, not a
      failure. State that `create-release` has real end-to-end evidence from the v0.7.1 and v0.8.0
      publishes but **has never run on a v0.9.x tag**, and that a failure there is handled inside the
      release work rather than deferred. Record that its body comes from
      `scripts/extract_changelog_section.py 0.9.6`, and that the published body must be checked
      byte-identical against that stdout — which is transcribed in `75-CHANGELOG-EVIDENCE.md`.
   4. **Dispatch `update-pin.yml` in `typsphinx-doc-translations`.** Owner: human plus
      `/gsd-complete-milestone`. Ordering: after step 3. State explicitly that this is **manual** and
      does **not** happen as a side effect of the parent repository's tag push, and that the
      second-repository tag is a standing per-release cost.
   5. **Confirm Read the Docs `stable` on both projects.** Owner: human, via the unauthenticated
      public API or real fetches. Both the `en` and the `ja` `stable` endpoints must report `0.9.6`.
      Note that publishing is what moves `stable`, so no default-version flip is expected or in
      scope.
   6. **Check REL-15's checkbox and its traceability row.** Owner: `/gsd-complete-milestone`.
      Ordering: last, and only against the **four observations** REL-15's own text names: the merge
      commit on `origin/main`'s first-parent history; `git ls-remote --tags origin` showing
      `v0.9.6`; a PyPI 200 for `0.9.6`; and `gh release list` showing a `v0.9.6` Release. Carry the
      standing warning: phase-completion tooling has flipped this row against an explicit decision
      at 9 of the 10 prior release-prep closes, so the flip must be the operator's, made here,
      against those four observations.
   7. **Do not re-date the `## [0.9.6]` heading.** Owner: nobody — this is a fact, not a step. The
      heading carries the **prep** authoring date and may differ from the tag date; precedent:
      `[0.9.2] - 2026-08-30` merged a day later, `[0.9.0] - 2026-08-17` tagged five days later.
      `/gsd-complete-milestone` does not rewrite it. Re-confirm the extractor instead.

4. **`## Recorded without acting`.** The Dependabot and open-pull-request census, quoted from
   `75-PREFLIGHT-EVIDENCE.md`'s `OPEN_PRS`, `DEPENDABOT_OPEN_PRS` and `PR_CENSUS_AT`, with a live
   re-read taken during this task — recorded as the key lines `HANDOFF_OPEN_PRS_LIVE = ` from
   `gh pr list --state open --json number --jq 'length'` and `HANDOFF_OPEN_PRS_AT = ` from
   `date -u +%FT%TZ` — and both transcribed side by side. Then the ordering rule, stated
   **conditionally**: if any Dependabot pull request is open when the release runs, order it after
   the milestone pull request, the order v0.9.3 used; if none is, there is no ordering question.
   State that the census drifted measurably within a single calendar day before this phase ran, so
   the operative reading is the one taken at release time.

5. **`## Deferred with this phase`.** The IN-01 pending todo filed by 75-02, by path; MSG-06 and
   QUA-08 still pending; NUM-01 disclosed but not fixed, its todo still pending after this phase.

6. **`## Before and after phase.complete-family tooling`.** Reproduce inline, so no second file need
   be open: `sha256sum .planning/REQUIREMENTS.md` with the digest to compare against;
   `wc -l .planning/REQUIREMENTS.md` with its number; `git diff --name-only -- .planning/REQUIREMENTS.md`
   expected empty; and `grep -n 'REL-15' .planning/REQUIREMENTS.md` with the expected-unchanged
   transcript pasted verbatim. Put REL-16's lines in a clearly separate **expected to move** callout.
   Name the third observation — after `phase.complete`-family tooling, including
   `/gsd-verify-work`'s inline transition, has run — as the one that actually catches the flip. Give
   the reversion recipe `git checkout -- .planning/REQUIREMENTS.md` and the scratch backup paths for
   `ROADMAP.md` and `STATE.md` from `75-CLOSEOUT-GUARD.md`.

7. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; H="$D/75-HANDOFF.md" &amp;&amp; G="$D/75-CLOSEOUT-GUARD.md" &amp;&amp; PF="$D/75-PREFLIGHT-EVIDENCE.md" &amp;&amp; CI="$D/75-CI-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$H}" | head -n 1; } &amp;&amp; test -s "$H" &amp;&amp; [ "$(wc -l &lt; "$H")" -ge 80 ] &amp;&amp; for s in '## What this phase satisfied' '## Success criteria' '## Checklist' '## Recorded without acting' '## Deferred with this phase' '## Before and after phase.complete-family tooling'; do grep -qxF "$s" "$H" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; for t in 'REL-15' 'REL-16' 'v0.9.6' 'release.yml' 'publish-pypi' 'create-release' 'pypi' 'update-pin.yml' 'typsphinx-doc-translations' 'readthedocs' 'stable' 'extract_changelog_section.py' 'gsd/v0.9.6-doctest-block-rendering-and-release' 'git checkout -- .planning/REQUIREMENTS.md' '/gsd-verify-work' 'Known Limitations' 'NUM-01' 'IN-01' 'Dependabot'; do grep -qF "$t" "$H" || { echo "missing term: $t"; exit 1; }; done &amp;&amp; grep -qiF 'manual' "$H" &amp;&amp; grep -qiF 'never run on a v0.9.x tag' "$H" &amp;&amp; grep -qiF 'prep' "$H" &amp;&amp; PC="$(k PROTECTION_CONTEXTS "$PF")" &amp;&amp; [ -n "$PC" ] &amp;&amp; printf '%s\n' "$PC" | tr '|' '\n' | while IFS= read -r c; do [ -n "$c" ] || continue; grep -qF "$c" "$H" || { echo "missing required check: $c"; exit 1; }; done &amp;&amp; RID="$(k RUN_ID "$CI")" &amp;&amp; [ -n "$RID" ] &amp;&amp; grep -qF "$RID" "$H" &amp;&amp; PS="$(k PUSHED_SHA "$CI")" &amp;&amp; [ "${#PS}" = 40 ] &amp;&amp; grep -qF "$PS" "$H" &amp;&amp; BC="$(k BUMP_COMMIT_SHA "$D/75-BUMP-EVIDENCE.md")" &amp;&amp; [ "${#BC}" = 40 ] &amp;&amp; grep -qF "$BC" "$H" &amp;&amp; SH="$(k REQ_SHA256_BASE "$G")" &amp;&amp; [ -n "$SH" ] &amp;&amp; grep -qF "$SH" "$H" &amp;&amp; grep -qF "$(k BACKUP_ROADMAP "$G")" "$H" &amp;&amp; grep -qF "$(k BACKUP_STATE "$G")" "$H" &amp;&amp; OP="$(k OPEN_PRS "$PF")" &amp;&amp; [ -n "$OP" ] &amp;&amp; LIVEOP="$(gh pr list --state open --json number --jq 'length')" &amp;&amp; [ "$(k HANDOFF_OPEN_PRS_LIVE)" = "$LIVEOP" ] &amp;&amp; [ -n "$(k HANDOFF_OPEN_PRS_AT)" ] &amp;&amp; ! grep -qE 'nine stale' "$H" &amp;&amp; TD="$(k TODO_PATH "$D/75-LEFTOVERS-EVIDENCE.md")" &amp;&amp; [ -n "$TD" ] &amp;&amp; grep -qF "$(basename "$TD")" "$H" &amp;&amp; ! grep -qE '\$\(' "$H" &amp;&amp; B="$(k BASE_75_07 "$D/75-SC5-INVARIANTS.md")" &amp;&amp; { [ -z "$B" ] || [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ]; } &amp;&amp; [ -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$H" &amp;&amp; ! grep -q '^## HALT' "$H"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME`, `missing term: TERM` or `missing required check: NAME` printed. Each of these makes it fail: not in a worktree; the handoff missing, empty or under 80 lines, which would mean it is not standalone; any of the six named sections absent; any of the required terms absent, including the `update-pin.yml` dispatch, the Read the Docs `stable` confirmation, the extractor, the reversion recipe, `/gsd-verify-work`, NUM-01, IN-01 and Dependabot; the handoff not saying the pin dispatch is manual, not carrying the never-run-on-a-v0.9.x-tag caveat, or not naming the heading date as the prep date; any of `main`'s required status-check contexts, read live from `75-PREFLIGHT-EVIDENCE.md`, absent from the handoff text; the CI run id, the pushed SHA, the bump commit SHA or the requirements digest absent; either scratch backup path absent; the live open-PR count absent from the text; the stale discussion-time branch figure written as the operative number; the IN-01 todo's filename absent; a `$(` command substitution present anywhere in the handoff, which is the shape this project's sandbox refuses for a posted body; a tracked product file changed by this plan; `.planning/REQUIREMENTS.md` changed; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `75-HANDOFF.md` carries all six named sections and is at least 80 lines, with every cited number written in rather than linked.
    - Its checklist names, in order: the PR to `main` with every required status-check context spelled out; the `v0.9.6` tag push; `release.yml`'s four jobs with the `pypi` environment's manual approval described as a gate; the manual `update-pin.yml` dispatch in `typsphinx-doc-translations`; the `en` and `ja` Read the Docs `stable` confirmation; and REL-15's checkbox flip against its four observations.
    - It records the prep-date fact, the `create-release`-never-run-on-a-v0.9.x-tag caveat, the filed IN-01 todo by filename, and NUM-01 as disclosed but unfixed.
    - Its Dependabot note quotes `75-PREFLIGHT-EVIDENCE.md`'s census, includes a live re-read, and states the rule conditionally rather than asserting a count.
    - It reproduces the fence's four commands inline with the recorded digest, the REL-16 expected-to-move callout, the reversion recipe and both scratch backup paths, and contains no `$(` command substitution.
  </acceptance_criteria>
  <done>A reader with only this file can execute the whole release, in order, and knows exactly which four observations close REL-15.</done>
</task>

<task type="auto">
  <name>Task 3: The REL-16 settlement record, the SC roll-up, and the operator's third-observation section in the guard</name>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-CLOSEOUT-GUARD.md, .planning/phases/75-v0-9-6-release-prep-prep-only/75-SC5-INVARIANTS.md</files>
  <read_first>
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CHANGELOG-EVIDENCE.md (`REL16_BRANCH`, `KNOWN_LIMITATIONS_AFTER` and § "REL-16: which branch was taken")
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md, 75-GREEN-TREE-EVIDENCE.md, 75-CI-EVIDENCE.md, 75-PREFLIGHT-EVIDENCE.md (the four verdict keys the roll-up quotes)
    - .planning/ROADMAP.md § "Phase 75" SC1 to SC5 (the literal wording each row is judged against)
    - CHANGELOG.md (the `## [0.9.6]` section, to re-read the settled `### Known Limitations` entry before recording the agreement)
  </read_first>
  <action>
1. **Append `## REL-16 settlement` to `75-SC5-INVARIANTS.md`.** SC3 requires the grep and the
   decision record to agree and the evidence to name which branch was taken. Record:
   - `KNOWN_LIMITATIONS_HEADINGS = ` from the **anchored** `grep -c '^### Known Limitations$' CHANGELOG.md`,
     which must be 2, and the two heading line numbers from `grep -n`. State in one sentence why the
     anchored form is used: the unanchored string also matches a prose mention elsewhere in the file
     and would give a misleading count.
   - `REL16_BRANCH_RECORDED = ` quoted from `75-CHANGELOG-EVIDENCE.md`'s `REL16_BRANCH`, and
     `REL16_AGREEMENT = yes` only when the anchored grep finding a new heading inside `## [0.9.6]`
     and the decision record's branch both say the section was written rather than declined.
   - The entry's own content check: the `### Known Limitations` block inside `## [0.9.6]` names
     NUM-01, carries a `Workaround:` line, and names neither of the two defects measured closed in
     v0.8.0 nor WR-02 or WR-03. Paste the block verbatim so a reader can judge it without opening
     `CHANGELOG.md`.
   - One paragraph recording the AMENDED candidate-set correction as the reason the set is NUM-01
     alone, and stating that `REQUIREMENTS.md` and `ROADMAP.md` keep their literal text, unedited.

2. **Append `## Success criteria roll-up` to `75-SC5-INVARIANTS.md`.** Five rows, SC1 to SC5, each
   with a verdict and the evidence key it reads: SC1 from `75-BUMP-EVIDENCE.md`'s
   `BUMP_COMMIT_FILES` and `CHANGELOG_GATE_SKIPS`; SC2 from `75-CHANGELOG-EVIDENCE.md`'s carried
   digests and `EXTRACT_MATCHES_SECTION`; SC3 from `REL16_AGREEMENT`; SC4 from
   `SC4_LOCAL_VERDICT`, `SC4_CI_VERDICT` and `TRIAL_MERGE_VERDICT`; SC5 from `SC5_VERDICT` and
   `FENCE_CLOSE_VERDICT`. Key `PHASE_VERDICT = MET` only when all five rows are MET.

3. **Append `## For the operator running phase.complete` to `75-CLOSEOUT-GUARD.md`** — or extend the
   section 75-01 created, if it already exists, so the file ends with one authoritative copy.
   Include: the third observation's four commands with their expected values inlined; the statement
   that `/gsd-verify-work`'s inline transition and `/gsd-execute-phase`'s tail both count as the
   tooling in question; the REL-16 expected-to-move callout; the reversion recipe; the instruction to
   diff `ROADMAP.md` and `STATE.md` against the recorded scratch backups as well; and a pointer to
   `75-HANDOFF.md` as the file that carries the release procedure itself. Key
   `THIRD_OBSERVATION_DOCUMENTED = yes`.

4. Commit both files.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; S="$D/75-SC5-INVARIANTS.md" &amp;&amp; G="$D/75-CLOSEOUT-GUARD.md" &amp;&amp; CF="$D/75-CHANGELOG-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; ks() { sed -n "s/^$1 = //p" "${2:-$S}" | head -n 1; } &amp;&amp; KL="$(grep -c '^### Known Limitations$' CHANGELOG.md)" &amp;&amp; [ "$KL" = 2 ] &amp;&amp; [ "$(ks KNOWN_LIMITATIONS_HEADINGS)" = 2 ] &amp;&amp; [ "$(ks REL16_BRANCH_RECORDED)" = known-limitations-section ] &amp;&amp; [ "$(ks REL16_BRANCH "$CF")" = known-limitations-section ] &amp;&amp; [ "$(ks REL16_AGREEMENT)" = yes ] &amp;&amp; NEW="$(awk '/^## \[/{if(f)exit} f; /^## \[0\.9\.6\]/{f=1}' CHANGELOG.md)" &amp;&amp; NKL="$(printf '%s\n' "$NEW" | awk '$0=="### Known Limitations"{f=1;next} f&amp;&amp;/^### /{exit} f' | grep -v '^$')" &amp;&amp; [ -n "$NKL" ] &amp;&amp; printf '%s\n' "$NKL" | grep -qF 'NUM-01' &amp;&amp; printf '%s\n' "$NKL" | grep -qF 'Workaround:' &amp;&amp; [ "$(printf '%s\n' "$NKL" | grep -ciE 'rehome|duplicate-target|WR-02|WR-03')" = 0 ] &amp;&amp; grep -qF 'NUM-01' "$S" &amp;&amp; grep -qF 'Workaround:' "$S" &amp;&amp; [ "$(ks SC5_VERDICT)" = MET ] &amp;&amp; [ "$(ks PHASE_VERDICT)" = MET ] &amp;&amp; [ "$(ks SC4_LOCAL_VERDICT "$D/75-GREEN-TREE-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(ks SC4_CI_VERDICT "$D/75-CI-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(ks TRIAL_MERGE_VERDICT "$D/75-PREFLIGHT-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(ks EXTRACT_MATCHES_SECTION "$CF")" = yes ] &amp;&amp; [ "$(ks BUMP_COMMIT_FILES "$D/75-BUMP-EVIDENCE.md")" = 'CHANGELOG.md|README.md|pyproject.toml|tests/test_changelog_page_gate.py|uv.lock' ] &amp;&amp; [ "$(ks THIRD_OBSERVATION_DOCUMENTED "$G")" = yes ] &amp;&amp; grep -qF '75-HANDOFF.md' "$G" &amp;&amp; grep -qF '/gsd-verify-work' "$G" &amp;&amp; grep -qF 'git checkout -- .planning/REQUIREMENTS.md' "$G" &amp;&amp; [ "$(grep -cxF '## For the operator running phase.complete' "$G")" = 1 ] &amp;&amp; for s in '## REL-16 settlement' '## Success criteria roll-up'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; grep -qE '^- \[ \] \*\*REL-15\*\*' .planning/REQUIREMENTS.md &amp;&amp; [ -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" ] &amp;&amp; [ -z "$(git status --porcelain -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$S" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$G" &amp;&amp; ! grep -q '^## HALT' "$S" &amp;&amp; ! grep -q '^## HALT' "$G"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail: not in a worktree; the anchored `### Known Limitations` heading count not exactly 2, live or recorded; either file's `REL16_BRANCH` key not reading `known-limitations-section`, or `REL16_AGREEMENT` not `yes`; the `### Known Limitations` block inside `## [0.9.6]` extracting empty, not naming NUM-01, lacking a `Workaround:` line, or naming the rehome collision, the duplicate-target cluster, WR-02 or WR-03; the settlement section not carrying the entry's own NUM-01 and workaround text, which would mean it was described rather than pasted; `SC5_VERDICT` or `PHASE_VERDICT` not `MET`; any of the three wave verdicts or `EXTRACT_MATCHES_SECTION` not reading its required value; `BUMP_COMMIT_FILES` not the five-file union; `THIRD_OBSERVATION_DOCUMENTED` not `yes`; the guard lacking the handoff pointer, the `/gsd-verify-work` mention or the reversion recipe; the operator section appearing other than exactly once, which would mean two divergent copies; either named section missing; REL-15's checkbox no longer unchecked; `.planning/REQUIREMENTS.md` changed; any uncommitted product change left behind; a key line carrying an opening parenthesis; or a HALT heading in either file.</fails_when>
  </verify>
  <acceptance_criteria>
    - `KNOWN_LIMITATIONS_HEADINGS = 2` from the anchored grep, with both line numbers recorded and the reason for anchoring stated.
    - `REL16_BRANCH_RECORDED` and `75-CHANGELOG-EVIDENCE.md`'s `REL16_BRANCH` both read `known-limitations-section`, and `REL16_AGREEMENT = yes`.
    - The `### Known Limitations` entry is pasted verbatim into the settlement section, names NUM-01, carries a `Workaround:` line, and names none of the two closed defects nor WR-02 / WR-03.
    - The roll-up has five rows citing `BUMP_COMMIT_FILES`, `EXTRACT_MATCHES_SECTION`, `REL16_AGREEMENT`, the three wave verdicts and `SC5_VERDICT`, and `PHASE_VERDICT = MET`.
    - `75-CLOSEOUT-GUARD.md` ends with exactly one `## For the operator running phase.complete` section, carrying the third observation's commands, the reversion recipe, the handoff pointer and `THIRD_OBSERVATION_DOCUMENTED = yes`.
  </acceptance_criteria>
  <done>REL-16's settlement is on the record with the CHANGELOG and the decision record agreeing, all five success criteria are rolled up, and the operator has one authoritative copy of the third-observation protocol.</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide spec-less probe accounting lives in `75-01-PLAN.md` § "Spec-less probe accounting". This
plan authors no edge row of its own.

**Row 6 restated, because this plan writes the settlement record.** REL-16's edge row came back
`unclassified` and stays **unresolved**. What this plan can prove is internal agreement: the
anchored grep over `CHANGELOG.md` and the decision record say the same thing, and the entry's
content matches D-10 and D-11. What it cannot prove by measurement is that the NUM-01-only candidate
set is the set REL-16's author meant by "the carried major defects" — the other two names in the
requirement text were measured closed in v0.8.0, and the reduction was an owner decision taken in
discussion. That is carried as an explicit assumption, not as a verified fact, and the corrective
window stays open until the tag is pushed.

**Assumption on the third fence observation.** It happens outside every plan, after
`phase.complete`-family tooling has run, so this plan can only write down the protocol and the
expected values — it cannot take the observation itself. `THIRD_OBSERVATION_DOCUMENTED = yes`
records that the instructions exist, not that the observation was made; the observation belongs to
whoever runs the close.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/REQUIREMENTS.md` → phase-completion tooling | the write this phase's whole fence exists to detect happens after every plan has finished |
| phase evidence → `/gsd-complete-milestone` | a different command, in a later session, executes the release from this handoff alone |
| prep-only phase → irreversible release actions | the handoff describes steps that must not be taken here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-30 | Tampering | REL-15's checkbox flipped by tooling after this plan finishes | high | mitigate | the close observation MATCHes here and the third observation's four commands, expected values and reversion recipe are written into both the guard and the handoff for the operator |
| T-75-31 | Repudiation | a handoff that depends on files the operator does not open, so a step is silently skipped | high | mitigate | the handoff is standalone: every required check context, SHA, run id and digest is inlined, and the verify asserts each is literally present |
| T-75-32 | Elevation of Privilege | a release command body assembled by shell command substitution | medium | mitigate | the handoff is negative-checked for `$(`, and every body it prescribes comes from `--body-file <path>` |
| T-75-33 | Repudiation | a scope fence that passes because its pathspec matches nothing | high | mitigate | both fences carry controls — a non-empty pathspec read and a widened diff listing `.planning/` paths |
| T-75-34 | Tampering | ROADMAP.md or STATE.md mangled by the same tooling, unnoticed because the fence only watches one file | medium | mitigate | both are backed up to scratch at phase head, diffed against those backups at close, and the handoff repeats the instruction for the third observation |
| T-75-35 | Spoofing | a Dependabot ordering rule keyed on a count that was already stale when written | medium | mitigate | the note quotes 75-05's measured census, adds a live re-read, states the rule conditionally, and the verify rejects the stale discussion-time figure as the operative number |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list, identical in every plan of this phase. Every symbol below is created by Phase 75
and did not exist at the phase base.

**New files under `.planning/phases/75-v0-9-6-release-prep-prep-only/`:** `75-CLOSEOUT-GUARD.md`,
`75-BASE-EVIDENCE.md`, `75-SC5-INVARIANTS.md`, `75-LEFTOVERS-EVIDENCE.md`, `COVERAGE.md`,
`75-BUMP-EVIDENCE.md`, `75-CHANGELOG-EVIDENCE.md`, `75-GREEN-TREE-EVIDENCE.md`,
`75-PREFLIGHT-EVIDENCE.md`, `75-CI-EVIDENCE.md`, `75-HANDOFF.md`, plus one `75-0N-SUMMARY.md` per
plan.

**New file under `.planning/todos/pending/`:** a record for IN-01 (D-14), the
`visit_literal_block` / `depart_literal_block` `Args:` text that still names only the literal block
node.

**New headings and lines in `CHANGELOG.md`:** `## [0.9.6] - <prep date>` and, inside it,
`### Added`, `### Changed`, `### Fixed`, `### Known Limitations` and `### Verified`; a fresh
`## [Unreleased]` heading placed above it, retaining only `### Planned for Future Releases`; and a
new tail link line `[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6`.

**New value in `tests/test_changelog_page_gate.py`:** the `RELEASE_VERSIONS` tuple member `"0.9.6"`.

**Changed version literals (not new symbols):** `pyproject.toml`'s `[project].version`, the
`typsphinx` stanza's `version` in `uv.lock`, and `README.md`'s `**Status**: Stable (vX.Y.Z)` line,
all moving `0.9.2` to `0.9.6`.

**Deletions (no new symbol):** the five untracked repository-root files `probe_bogusxyz.typ`,
`probe_none.typ`, `probe_pycon.typ`, `probe_python.typ` and `probe_text.typ`.
</artifacts_this_phase_produces>

<verification>
- The fence MATCHes at phase close and REL-15's checkbox reads unchecked, read directly out of the
  file.
- Every SC5 probe is empty at observation 2 with its control present, and the two observations are
  separated by three waves of recorded work.
- Both scope fences hold with their controls, and no product file changed after the tree CI tested.
- `75-HANDOFF.md` is standalone and carries every required check, SHA, run id and digest inline.
- The anchored `### Known Limitations` count is 2 and the decision record agrees; `PHASE_VERDICT = MET`.
</verification>

<success_criteria>
- SC5 is met: zero irreversible action, probed twice, the fence recorded and MATCHed, and a
  standalone handoff written.
- SC3's agreement clause is discharged: the grep and the decision record say the same thing, and the
  evidence names the branch taken.
- The operator has one authoritative copy of the third-observation protocol, which is the
  observation that actually catches the flip.
</success_criteria>

<output>
Create `.planning/phases/75-v0-9-6-release-prep-prep-only/75-07-SUMMARY.md` when done, declaring
`requirements-completed: []`.
</output>
