---
phase: 74-the-doctest-block-handler-its-real-compile-gate-and-the-docs
plan: 07
type: execute
wave: 5
depends_on: ["74-05", "74-06"]
files_modified:
  - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CI-EVIDENCE.md
autonomous: true
requirements: [TRN-01, TRN-02, QUA-14]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Evidence last (ROADMAP Phase 74 binding ordering): the first push and the CI dispatch run in wave 5, after 74-05 (SC1_VERDICT, SC3_VERDICT and D05_VERDICT MET) and 74-06 (SC4_LOCAL_VERDICT MET), and never alongside the work they audit.'
    - 'Constraint 10 branch census: immediately before the push, `git branch --list ''gsd/v0.9.6*'' -v` and `git ls-remote --heads origin ''refs/heads/gsd/v0.9.6*''` are recorded. A decoy is never pushed. A local decoy that is an ancestor of the canonical ref (zero unique commits) is deleted. A decoy carrying unique commits halts the plan before anything is deleted, because constraint 10''s fast-forward and HEAD re-point are main-checkout actions.'
    - 'PUSHED_SHA is the canonical ref gsd/v0.9.6-doctest-block-rendering-and-release. Outside .planning/ it equals this worktree''s HEAD, and it contains 74-TIP-EVIDENCE.md and 74-GATES-EVIDENCE.md. Against 6cc44f22 it changes only typsphinx/pathfmt.py and typsphinx/translator.py under typsphinx/, and nothing under .github/ or flake.nix. pyproject.toml reads version 0.9.2, and no tag points at it.'
    - 'SC4 first push: `git push --no-follow-tags -u origin gsd/v0.9.6-doctest-block-rendering-and-release` runs once, as a branch creation or a fast-forward of an ORIGIN_BEFORE re-measured just before. Afterwards the origin head equals PUSHED_SHA and the local branch tracks origin.'
    - 'SC4 CI: exactly one `gh workflow run CI --ref gsd/v0.9.6-doctest-block-rendering-and-release` is issued. RUN_ID has headSha PUSHED_SHA, event workflow_dispatch and a createdAt after PUSH_AT. It is waited on in the foreground until completed. Every job conclusion is transcribed, the two windows-latest and two macos-latest lanes are named individually, and every job is success.'
    - 'SC4 required checks: re-read at phase close, main''s required status checks give the same strict value and the same sorted contexts as REQUIRED_STRICT_HEAD and REQUIRED_CONTEXTS_HEAD in 74-BASE-EVIDENCE.md.'
    - 'Edge (dispatch idempotency): after an HTTP 5xx from gh workflow run, `gh run list` is read for a run at PUSHED_SHA created after PUSH_AT before any retry. A resumed executor reuses RUN_ID. The CI list holds exactly one workflow_dispatch run at PUSHED_SHA, and release.yml holds none.'
  artifacts:
    - path: .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CI-EVIDENCE.md
      provides: 'the wave-4 gate, tip identity and fence, decoy census, first push with tracking, one dispatch, the completed run with every job, the four named lanes, the tox lint step, required checks at close, and SC4_CI_VERDICT'
      contains: 'PUSHED_SHA = '
  key_links:
    - from: 'local ref gsd/v0.9.6-doctest-block-rendering-and-release'
      to: 'origin/gsd/v0.9.6-doctest-block-rendering-and-release'
      via: 'git push --no-follow-tags -u; refs are shared between this worktree and the main checkout'
      pattern: 'ORIGIN_BEFORE = '
    - from: 'gh workflow run CI --ref gsd/v0.9.6-doctest-block-rendering-and-release'
      to: 'ci.yml jobs on ubuntu, windows and macos'
      via: 'workflow_dispatch, since the push and pull_request triggers of ci.yml are scoped to main and develop'
      pattern: 'RUN_ID = '
  prohibitions:
    - statement: 'MUST NOT create or push a tag, force-push, push any ref other than the canonical branch, push or delete a decoy that carries unique commits, open or comment on a pull request, trigger release.yml or update-pin.yml, dispatch a second CI run, rerun jobs, or edit any file to turn a red job green.'
      requirement_id: TRN-01
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Put the phase tip on `origin` for the first time and obtain CI's three-OS verdict on it (SC4, ROADMAP
constraint 10):
- the branch census and decoy check, immediately before the push
- one push with tracking
- exactly one dispatched CI run, waited on in the foreground to completion, with every job transcribed
- `main`'s required checks re-read at phase close

Purpose: `ci.yml` runs on this branch only through `workflow_dispatch`, and only such a run reaches the
Windows and macOS lanes. It runs last because 74-05 and 74-06 must be MET first. Every later commit
of the phase touches only `.planning/`, which no CI job reads.

Output: `74-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md
@.planning/milestones/v0.9.5-phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --python 3.13.13`
and run project tooling through `uv run`.

**Refs are shared.** This worktree's HEAD is on its own worktree branch, but
`refs/heads/gsd/v0.9.6-doctest-block-rendering-and-release` is the same ref the main checkout has
checked out. Push that ref by name. Never run `git branch -f`, `git update-ref` or `git symbolic-ref`
on it from here, and never touch the main checkout's working tree, including its untracked
`probe_*.typ` files.

**Waiting on CI.** Wait in the foreground: set the Bash tool's `timeout` to 600000 and never use
`run_in_background`. Wrap each watch in `timeout 590` so it returns inside the tool limit, then read
`gh run view "$RUNID" --json status,conclusion`. Repeat these bounded foreground calls until `status`
is `completed`; do not end the turn or return control while the run is in progress. Commit `RUN_ID`
to the evidence before the first wait, so a resumed executor reuses it instead of dispatching again.

**Evidence key lines.** Bare values only; explanation on the next line. The verify fails on any key
line containing an opening parenthesis.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_74_07`; helper prefix `p7407_`.

**Commits.** Plain `git commit`. Never a GSD commit helper, which has created the
`gsd/vX.Y-milestone` decoy in every milestone since v0.9.1. Never write `74-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: gsd/v0\.9\.6-milestone -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[3456] -->

## Planning-time census (2026-09-20, HEAD `1deb7e4b`; re-measure, never trust)

| Fact | Value at planning |
|------|-------------------|
| Local `gsd/v0.9.6*` refs | only the canonical `gsd/v0.9.6-doctest-block-rendering-and-release`; STATE.md records the helper's decoy renamed at milestone start |
| Origin | nothing for `gsd/v0.9.6*`, so the push creates the branch |
| `ci.yml` | `name: CI`; six `Test Python {3.12,3.13} on {ubuntu,windows,macos}-latest` jobs plus `Lint and Format Check` (step `Run lint with tox`), `Type Check`, `Code Coverage`, `Build Package` and the integration jobs; 12 jobs in the reference run |
| Reference run | `REFERENCE_CI_RUN_ID` in 74-BASE-EVIDENCE.md (`35083828156`, Phase 73) |
| Required checks at the last close | strict `true`; `Build Package|Code Coverage|Lint and Format Check|Test Python 3.12 on ubuntu-latest|Test Python 3.13 on ubuntu-latest|Type Check` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: gate on wave 4, tip identity and fence, decoy census, the first push with tracking, and one CI dispatch</name>
  <precondition>`gh auth status` succeeds and `git ls-remote --heads origin` returns at least one line.</precondition>
  <reversibility rating="reversible">The push creates a new remote branch that can be deleted, and a CI run publishes nothing. No tag, release or pull request is created, and ROADMAP constraint 10 and SC4 pre-authorise this first push in Phase 74.</reversibility>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v0.9.5-phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md (the census, push and dispatch sections this mirrors)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md (`PHASE_BASE_SHA`, the head branch census, the required checks, `REFERENCE_CI_RUN_ID`)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-TIP-EVIDENCE.md and 74-GATES-EVIDENCE.md (the verdict keys gated on)
    - .github/workflows/ci.yml (triggers and job names)
    - .planning/ROADMAP.md § "v0.9.6" constraints 7 and 10, and § "Phase 74" SC4
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record:
   - `date -u +%FT%TZ`, `pwd -P` under `.claude/worktrees/`, `test -f .git; echo "exit:$?"`, and the shim check.
   - The provisioning line, then the same line with `--locked` appended and `echo "exit:$?"`, which must print `exit:0`. Every CI job starts with a locked sync, so a stale lock would fail every lane before any signal. On a non-zero exit write `## HALT: lock not in sync`; never regenerate the lock.
   - Before any commit, `BASE_74_07 = ` followed by `git rev-parse HEAD`, and `SCRATCH_74_07 = ` followed by the `mktemp -d` path.

2. **`## Wave-4 gate`.** Quote these keys, which must read as shown:
   - `SC1_VERDICT = MET`, `SC3_VERDICT = MET`, `D05_VERDICT = MET` and `D05_UNEXPLAINED_HUNKS = 0` from `74-TIP-EVIDENCE.md`
   - `SC4_LOCAL_VERDICT = MET` from `74-GATES-EVIDENCE.md`

   Otherwise write `## HALT: wave 4 not green` and stop without pushing.

3. **`## Tip identity and fence`.** Record each of these; all must hold:
   - `PUSHED_SHA = ` followed by `git rev-parse gsd/v0.9.6-doctest-block-rendering-and-release`, with `git log --oneline -1` of it
   - `git diff --name-only HEAD "$PUSHED_SHA" -- . ':(exclude).planning'`, empty
   - `git cat-file -e "$PUSHED_SHA:<phase dir>/74-TIP-EVIDENCE.md"` and the same for `74-GATES-EVIDENCE.md`, both exit 0: wave 4 is merged into the ref being pushed
   - `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b "$PUSHED_SHA" -- typsphinx`, exactly `typsphinx/pathfmt.py` and `typsphinx/translator.py`
   - `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b "$PUSHED_SHA" -- .github flake.nix`, empty
   - `git show "$PUSHED_SHA:pyproject.toml" | grep -m1 '^version = '`, which must be `version = "0.9.2"`
   - `git tag --points-at "$PUSHED_SHA"`, empty

4. **`## Decoy census (constraint 10)`.** Run this immediately before the push, with only step 5's measurements in between. Record `git branch --list 'gsd/v0.9.6*' -v` and `git ls-remote --heads origin 'refs/heads/gsd/v0.9.6*'`.
   - If `gsd/v0.9.6-milestone` exists on origin, write `## HALT: decoy on origin` and stop.
   - If it exists locally, record `git merge-base --is-ancestor gsd/v0.9.6-milestone gsd/v0.9.6-doctest-block-rendering-and-release; echo "exit:$?"`.
     - On `exit:0` it carries no unique commit: delete it with `git branch -d gsd/v0.9.6-milestone`, record that, and set `DECOY_ACTION = deleted-ancestor`.
     - Otherwise record `git log --oneline gsd/v0.9.6-doctest-block-rendering-and-release..gsd/v0.9.6-milestone`, write `## HALT: decoy carries unique commits`, and stop without deleting anything.
     - Constraint 10's order is: fast-forward the canonical ref to the decoy, re-point the main checkout's HEAD with `git symbolic-ref`, and only then delete the decoy. That order acts on the main checkout's HEAD, so the orchestrator performs it there and re-dispatches this plan.
   - If it exists nowhere, set `DECOY_ACTION = none-present`.

5. **`## Push`.**
   - `ORIGIN_BEFORE = ` followed by the first field of `git ls-remote --heads origin refs/heads/gsd/v0.9.6-doctest-block-rendering-and-release`, or `none` when that prints nothing, which is expected for the first push.
   - If it is a SHA, record `git merge-base --is-ancestor "$ORIGIN_BEFORE" "$PUSHED_SHA"; echo "exit:$?"`, which must be `exit:0`. Otherwise write `## HALT: origin moved`; never force.
   - Record `git config --get push.followTags`, then `PUSH_AT = ` followed by `date -u +%FT%TZ`.
   - Run `git push --no-follow-tags -u origin gsd/v0.9.6-doctest-block-rendering-and-release` once. Record the output, including GitHub's pull-request hint, and do not act on the hint.
   - Record the following:
     - the origin head now printing `PUSHED_SHA`
     - `git config --get branch.gsd/v0.9.6-doctest-block-rendering-and-release.remote` printing `origin`
     - `git ls-remote --tags origin` showing `refs/tags/v0.9.2` and no `v0.9.3`, `v0.9.4`, `v0.9.5` or `v0.9.6` tag

6. **`## Dispatch`.**
   - Run `gh workflow run CI --ref gsd/v0.9.6-doctest-block-rendering-and-release` exactly once.
   - Locate the run with `gh run list --workflow=ci.yml --branch gsd/v0.9.6-doctest-block-rendering-and-release --event workflow_dispatch --limit 5 --json databaseId,headSha,status,createdAt,url`, choosing the row whose `headSha` is `PUSHED_SHA` and whose `createdAt` is after `PUSH_AT`.
   - If no row is listed yet, that is registration lag: repeat the list query, and never dispatch again.
   - If the dispatch returned an HTTP 5xx, GitHub may still have created the run. Repeat the list query several times, recording each. Dispatch a second time only if every query shows no run at `PUSHED_SHA` created after `PUSH_AT`, and record why.
   - Record `RUN_ID = ` and `RUN_URL = `.

7. Write `74-CI-EVIDENCE.md` under `# Phase 74 — CI Evidence (first push, SC4 CI half)`, with the section names above, and commit it.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.6-doctest-block-rendering-and-release &amp;&amp; MB=6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; [ "$(k SC1_VERDICT "$D/74-TIP-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k SC3_VERDICT "$D/74-TIP-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k D05_VERDICT "$D/74-TIP-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k D05_UNEXPLAINED_HUNKS "$D/74-TIP-EVIDENCE.md")" = 0 ] &amp;&amp; [ "$(k SC4_LOCAL_VERDICT "$D/74-GATES-EVIDENCE.md")" = MET ] &amp;&amp; B="$(k BASE_74_07)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; git cat-file -e "$P:$D/74-TIP-EVIDENCE.md" &amp;&amp; git cat-file -e "$P:$D/74-GATES-EVIDENCE.md" &amp;&amp; HR="$(git ls-remote --heads origin "refs/heads/$BR")" &amp;&amp; [ "$(printf '%s\n' "$HR" | cut -f1)" = "$P" ] &amp;&amp; OB="$(k ORIGIN_BEFORE)" &amp;&amp; { [ "$OB" = none ] || git merge-base --is-ancestor "$OB" "$P"; } &amp;&amp; [ "$(git config --get "branch.$BR.remote")" = origin ] &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; [ "$(printf '%s\n' "$HL" | grep -c 'gsd/v0\.9\.6-milestone')" = 0 ] &amp;&amp; case "$(k DECOY_ACTION)" in none-present|deleted-ancestor) true ;; *) false ;; esac &amp;&amp; [ -z "$(git tag --points-at "$P")" ] &amp;&amp; TL="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.[3456]')" = 0 ] &amp;&amp; [ "$(git diff --name-only "$MB" "$P" -- typsphinx | LC_ALL=C sort | paste -sd'|')" = 'typsphinx/pathfmt.py|typsphinx/translator.py' ] &amp;&amp; [ -z "$(git diff --name-only "$MB" "$P" -- .github flake.nix)" ] &amp;&amp; [ -z "$(git diff --name-only HEAD "$P" -- . ':(exclude).planning')" ] &amp;&amp; PP="$(git show "$P:pyproject.toml")" &amp;&amp; [ "$(printf '%s\n' "$PP" | grep -m1 '^version = ')" = 'version = "0.9.2"' ] &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json workflowName -q .workflowName)" = CI ] &amp;&amp; [ "$(gh run view "$RUNID" --json event -q .event)" = workflow_dispatch ] &amp;&amp; PA="$(k PUSH_AT)" &amp;&amp; [ -n "$PA" ] &amp;&amp; [[ "$(gh run view "$RUNID" --json createdAt -q .createdAt)" &gt; "$PA" ]] &amp;&amp; for s in '## Head check and provisioning' '## Wave-4 gate' '## Tip identity and fence' '## Decoy census (constraint 10)' '## Push' '## Dispatch'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD | grep -vxF -e "$F" -e "$D/74-07-SUMMARY.md")" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- any 74-05 or 74-06 verdict not MET, or unexplained hunks non-zero
- `BASE_74_07` or `PUSHED_SHA` malformed, or the pushed ref lacking either wave-4 evidence file
- the origin head differing from `PUSHED_SHA`, or `ORIGIN_BEFORE` neither `none` nor an ancestor
- the branch not tracking origin, the decoy on origin, or `DECOY_ACTION` outside its two allowed values
- a tag at the pushed tip, the v0.9.2 tag missing on origin, or a v0.9.3 to v0.9.6 tag on origin
- the typsphinx/ set not the two files, .github or flake.nix changed, or the pushed product tree differing from HEAD
- the pushed version line not 0.9.2
- no `RUN_ID`, or the run's headSha, workflow, event or creation time not matching
- another tracked file changed, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Decoy census (constraint 10)` records both listings immediately before the push, and any ancestor-decoy deletion with its ancestry result.
    - `## Push` records `ORIGIN_BEFORE`, `PUSH_AT` and the verbatim `git push --no-follow-tags -u origin gsd/v0.9.6-doctest-block-rendering-and-release` output. The origin head equals `PUSHED_SHA`, and the branch tracks `origin`.
    - `## Dispatch` records exactly one `gh workflow run CI --ref gsd/v0.9.6-doctest-block-rendering-and-release`, with `RUN_ID` and `RUN_URL`, whose `headSha` is `PUSHED_SHA`.
  </acceptance_criteria>
  <done>The milestone branch is on origin at the green phase tip, with no decoy and no tag, and one CI run is dispatched against exactly that tip.</done>
</task>

<task type="auto">
  <name>Task 2: Wait for the run in the foreground to completion, transcribe every job, name the four windows and macos lanes, quote the tox lint step, and re-read the required checks at phase close</name>
  <precondition>The `RUN_ID` recorded by Task 1 resolves with `gh run view <id> --json databaseId`. A resumed executor reuses it.</precondition>
  <reversibility rating="reversible">Read-only observation of a run already dispatched, plus a read-only branch-protection query.</reversibility>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CI-EVIDENCE.md (Task 1: `RUN_ID`, `PUSHED_SHA`)
    - .planning/milestones/v0.9.5-phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md § "Job census" and § "Lint through tox" (the shape to mirror)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md (`REQUIRED_STRICT_HEAD`, `REQUIRED_CONTEXTS_HEAD`, `REFERENCE_CI_RUN_ID`, `REFERENCE_JOB_NAMES`)
    - .github/workflows/ci.yml lines 10-72
  </read_first>
  <action>
1. **Wait in the foreground.**
   - Run `timeout 590 gh run watch "$RUNID" --interval 30` in a Bash call with `timeout` 600000 and no `run_in_background`.
   - After each return, read `gh run view "$RUNID" --json status,conclusion`, and repeat until `status` is `completed`.
   - Overall cap: 90 minutes. If it is reached, record the status and return a blocking checkpoint. Never dispatch again.

2. **`## Run`.** Record `gh run view "$RUNID" --json status,conclusion,workflowName,headSha,url,createdAt,updatedAt`, with keys `RUN_HEAD_SHA` and `RUN_CONCLUSION`.

3. **`## Job census`.**
   - Record `gh run view "$RUNID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'` as a numbered table covering every row. Keys `JOB_COUNT` (measured) and `NON_SUCCESS_JOBS`, which must be 0.
   - Record the sorted job-name set, `gh run view "$RUNID" --json jobs --jq '[.jobs[].name] | sort | join("|")'`. Key `JOB_NAMES_MATCH_REFERENCE = yes` when it equals `REFERENCE_JOB_NAMES` in 74-BASE-EVIDENCE.md. Constraint 7 forbids any workflow edit, so a difference means `ci.yml` changed: write `## HALT: ci.yml job set changed`.

4. **`## windows-latest lanes`** and **`## macos-latest lanes`.** Two sub-tables naming `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each with its conclusion.

5. **`## Lint through tox`.** Take the `Lint and Format Check` job id from the run's jobs. From `gh run view --job <id> --log`, quote the `Run lint with tox` step's `black --check .` and `ruff check .` command lines and its `lint: OK` line. That shows CI ran the lint the worktree also ran.

6. **`## Dispatch count and no release run`.** Record:
   - `gh run list --workflow=ci.yml --branch gsd/v0.9.6-doctest-block-rendering-and-release --event workflow_dispatch --limit 50 --json headSha`, key `DISPATCH_COUNT`, the runs at `PUSHED_SHA`, which must be 1
   - `gh run list --workflow=release.yml --limit 20 --json headSha`, key `RELEASE_RUNS_AT_PUSHED`, which must be 0

7. **`## Required checks at phase close`.** Re-read `gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks` with `--jq .strict` and with `--jq '[.contexts[]] | sort | join("|")'`, keys `REQUIRED_STRICT_CLOSE` and `REQUIRED_CONTEXTS_CLOSE`. Key `REQUIRED_CHECKS_UNCHANGED = yes` when both equal the head values in `74-BASE-EVIDENCE.md`.

8. **`## SC4 CI verdict`.**
   - `SC4_CI_VERDICT = MET` only when the run is completed and success, `NON_SUCCESS_JOBS = 0`, `JOB_NAMES_MATCH_REFERENCE = yes`, all four named lanes are success, `DISPATCH_COUNT = 1`, `RELEASE_RUNS_AT_PUSHED = 0` and `REQUIRED_CHECKS_UNCHANGED = yes`.
   - If any job is not success, transcribe every job literally and keep the run. Do not rerun, dispatch again or edit anything. Write `SC4_CI_VERDICT = NOT-MET` and return a blocking checkpoint naming each failing job and step.

9. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.6-doctest-block-rendering-and-release &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; [ "$(gh run view "$RUNID" --json status -q .status)" = completed ] &amp;&amp; [ "$(gh run view "$RUNID" --json conclusion -q .conclusion)" = success ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; JC="$(gh run view "$RUNID" --json jobs -q '.jobs | length')" &amp;&amp; [ "$JC" -ge 1 ] &amp;&amp; [ "$JC" = "$(k JOB_COUNT)" ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.conclusion != "success")] | length')" = 0 ] &amp;&amp; JN="$(gh run view "$RUNID" --json jobs -q '[.jobs[].name] | sort | join("|")')" &amp;&amp; [ -n "$JN" ] &amp;&amp; [ "$JN" = "$(k REFERENCE_JOB_NAMES "$D/74-BASE-EVIDENCE.md")" ] &amp;&amp; for n in 'Test Python 3.12 on windows-latest' 'Test Python 3.13 on windows-latest' 'Test Python 3.12 on macos-latest' 'Test Python 3.13 on macos-latest'; do [ "$(gh run view "$RUNID" --json jobs -q "[.jobs[] | select(.name == \"$n\" and .conclusion == \"success\")] | length")" = 1 ] || { echo "lane: $n"; exit 1; }; grep -qF "$n" "$F" || { echo "missing: $n"; exit 1; }; done &amp;&amp; grep -qF 'Run lint with tox' "$F" &amp;&amp; grep -qF 'lint: OK' "$F" &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch "$BR" --event workflow_dispatch --limit 50 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; RC="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '[.contexts[]] | sort | join("|")')" &amp;&amp; [ -n "$RC" ] &amp;&amp; [ "$RC" = "$(k REQUIRED_CONTEXTS_HEAD "$D/74-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict)" = "$(k REQUIRED_STRICT_HEAD "$D/74-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(k NON_SUCCESS_JOBS)" = 0 ] &amp;&amp; [ "$(k JOB_NAMES_MATCH_REFERENCE)" = yes ] &amp;&amp; [ "$(k DISPATCH_COUNT)" = 1 ] &amp;&amp; [ "$(k RELEASE_RUNS_AT_PUSHED)" = 0 ] &amp;&amp; [ "$(k REQUIRED_CHECKS_UNCHANGED)" = yes ] &amp;&amp; [ "$(k SC4_CI_VERDICT)" = MET ] &amp;&amp; for s in '## Run' '## Job census' '## windows-latest lanes' '## macos-latest lanes' '## Lint through tox' '## Dispatch count and no release run' '## Required checks at phase close' '## SC4 CI verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or one of `lane: NAME`, `missing: NAME` or `missing section: NAME` printed. Each of these makes it fail:
- the run not completed, not success, or at a headSha other than `PUSHED_SHA`
- the job count zero or differing from `JOB_COUNT`, or any job not success
- the sorted job-name set differing from `REFERENCE_JOB_NAMES`
- any of the four windows or macos lanes absent, not success, or not named in the evidence
- the tox lint step or `lint: OK` not quoted
- more than one dispatched run at `PUSHED_SHA`, or a release.yml run at it
- the live required contexts or strict value differing from the phase-head record
- a count, match or verdict key wrong, a section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Job census` holds a numbered table of every job, with `NON_SUCCESS_JOBS = 0` and `JOB_NAMES_MATCH_REFERENCE = yes`.
    - The lane sub-tables name `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each success.
    - `## Lint through tox` quotes the `Run lint with tox` step's black and ruff lines and `lint: OK`.
    - `DISPATCH_COUNT = 1`, `RELEASE_RUNS_AT_PUSHED = 0`, `REQUIRED_CHECKS_UNCHANGED = yes` and `SC4_CI_VERDICT = MET`.
  </acceptance_criteria>
  <done>One completed, all-green CI run on the phase tip is transcribed job by job. Both Windows and both macOS lanes are named, and main's required checks are unchanged from phase head to phase close.</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide accounting lives in `74-01-PLAN.md`. This plan carries prohibition P5. The
dispatch-idempotency truth encodes this project's HTTP 5xx hazard. It is an execution guard beyond
the fourteen probe items and is not counted among them. The decoy-with-unique-commits halt keeps
constraint 10's ordering, which forbids deleting before the fast-forward and HEAD re-point: those two
steps act on the main checkout's HEAD, which a worktree executor must not re-point.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local refs → origin | a push publishes refs; a decoy, a force, another ref or a tag would publish what this phase must not |
| gh CLI → GitHub Actions | a dispatch starts remote work; the wrong workflow could begin a publish pipeline |
| CI log → committed evidence | quoted job-log lines enter the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-21 | Tampering | pushing a decoy, force-pushing, pushing another ref, or a tag riding along | high | mitigate | census immediately before the push; `--no-follow-tags -u` of the canonical ref only; the verify requires no decoy on origin, no tag at the tip, and no v0.9.3 to v0.9.6 tag on origin; prohibition P5 |
| T-74-22 | Elevation of Privilege | triggering `release.yml` or `update-pin.yml` | high | mitigate | only `gh workflow run CI` is issued; the verify asserts no release.yml run at `PUSHED_SHA`; prohibition P5 |
| T-74-23 | Repudiation | citing an older run as this phase's green, or masking a red run with a second dispatch | high | mitigate | `RUN_ID`'s headSha must equal `PUSHED_SHA` and its createdAt must follow `PUSH_AT`; exactly one dispatched run exists at that SHA; a red run is kept and escalated |
| T-74-24 | Tampering | required status checks changing mid-phase | medium | mitigate | head and close reads compared on strict and on the sorted contexts |
| T-74-25 | Denial of Service | a stale `uv.lock` failing every lane | medium | mitigate | the locked sync must exit 0 before the push; the lock is never regenerated |
| T-74-26 | Information Disclosure | CI log excerpts in evidence | low | accept | public repository and logs; only tool-output lines are quoted |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list (identical in every plan):
- `typsphinx/translator.py`: `TypstTranslator.visit_doctest_block(self, node: nodes.doctest_block) -> None` and `TypstTranslator.depart_doctest_block(self, node: nodes.doctest_block) -> None` (74-03); `visit_toctree` docstring blank lines (74-04).
- `typsphinx/pathfmt.py`: `quote_path` docstring blank line (74-04).
- `tests/test_doctest_block_render_gate.py` (74-02): class `TestDoctestBlockRenderGate`; constants `FIXTURE_DIR`, `MASTER_DOCNAMES`, `SHAPE_SENTINELS`, `UNKNOWN_DOCTEST_BLOCK`, `FENCE_OPEN`; helpers `_run_sphinx_build_typstpdf`, `_wrapper_pdf_path`, `_assert_pdf_magic`, `_source_doctest_block`, `_unknown_doctest_chunks`, `_read_typ`; module fixture `doctest_block_build`.
- `tests/fixtures/doctest_block_render_gate/{conf.py,index.rst,context_a_paragraph.rst,context_b_nonfirst_positions.rst}` (74-02).
- `tests/test_translator.py` (74-03): `test_doctest_block_defaults_to_python_fence`, `test_doctest_block_honours_existing_language`, `test_literal_block_without_language_keeps_bare_fence`, `test_doctest_block_shares_list_item_wrapper`.
- Evidence: `74-BASE-EVIDENCE.md`, `74-RED-EVIDENCE.md`, `74-QUA14-EVIDENCE.md`, `74-TIP-EVIDENCE.md`, `74-GATES-EVIDENCE.md`, `74-CI-EVIDENCE.md`, `COVERAGE.md`.

This plan creates `74-CI-EVIDENCE.md` with keys `BASE_74_07`, `SCRATCH_74_07`, `PUSHED_SHA`,
`DECOY_ACTION`, `ORIGIN_BEFORE`, `PUSH_AT`, `RUN_ID`, `RUN_URL`, `RUN_HEAD_SHA`, `RUN_CONCLUSION`,
`JOB_COUNT`, `NON_SUCCESS_JOBS`, `JOB_NAMES_MATCH_REFERENCE`, `DISPATCH_COUNT`,
`RELEASE_RUNS_AT_PUSHED`, `REQUIRED_STRICT_CLOSE`, `REQUIRED_CONTEXTS_CLOSE`,
`REQUIRED_CHECKS_UNCHANGED` and `SC4_CI_VERDICT`. Outside the repository it creates
`origin/gsd/v0.9.6-doctest-block-rendering-and-release` at `PUSHED_SHA`, sets its tracking, and
creates one `workflow_dispatch` CI run. It modifies no product-tree file.
</artifacts_this_phase_produces>

<verification>
- The canonical branch is on origin at `PUSHED_SHA`, with no decoy and no tag, and tracks origin.
- One dispatched CI run at `PUSHED_SHA` completed all green. The four Windows and macOS lanes are
  named, the job set equals the reference run's, and lint ran through tox.
- The required checks are unchanged from phase head to phase close.
</verification>

<success_criteria>
SC4's remote half: the milestone branch is on origin, and a completed, green three-OS CI run on the
phase tip is transcribed.
</success_criteria>

<output>
Create `.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-07-SUMMARY.md` when done.
Its first section states `PUSHED_SHA`, `RUN_ID`, `RUN_URL`, `JOB_COUNT`, `NON_SUCCESS_JOBS`,
`REQUIRED_CHECKS_UNCHANGED` and `SC4_CI_VERDICT`. If the run was not all green, that section names
the failing jobs first. Do not edit REQUIREMENTS.md, ROADMAP.md or STATE.md.
</output>
