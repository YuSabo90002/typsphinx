---
phase: 74-the-doctest-block-handler-its-real-compile-gate-and-the-docs
plan: 06
type: execute
wave: 4
depends_on: ["74-01", "74-02", "74-03", "74-04"]
files_modified:
  - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-GATES-EVIDENCE.md
  - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/COVERAGE.md
autonomous: true
requirements: [TRN-01, TRN-02, QUA-14]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Evidence last: the local gates and scope fence run in wave 4, after every product edit (waves 1-3), and audit 74-02''s RED_VERDICT, 74-03''s GREEN_VERDICT and 74-04''s QUA14_FIX_VERDICT, all MET.'
    - 'SC4 local gates: in the provisioned worktree venv (dev and docs extras, interpreter recorded), `uv run ruff check .`, `uv run black --check .`, `uv run mypy typsphinx/` and the full pytest suite pass on the phase tip. The suite passes twice, once plainly and once under LC_ALL=C. Each run reports zero failed and zero errors, its skip reasons are transcribed, and no test of tests/test_changelog_page_gate.py is skipped.'
    - 'SC4 @preview invariant: tests/test_preview_version_sync.py is green, typsphinx/templates/base.typ declares exactly four @preview packages, and none of the three declaration sites changed since 6cc44f22.'
    - 'SC4 scope fence (constraint 7), with a pathspec control and a widened-diff control: `git diff --name-only 6cc44f22..HEAD -- typsphinx/` lists exactly typsphinx/pathfmt.py and typsphinx/translator.py. Every translator.py hunk lies in the handler region (visit_literal_block through visit_definition_list) or inside visit_toctree''s docstring. pathfmt.py gains only empty lines, inside quote_path''s docstring.'
    - 'SC4 standing invariants: .github/ and flake.nix are absent from the milestone diff, with a control proving the pathspecs resolve. pyproject.toml and uv.lock are unchanged, so the version is still 0.9.2 and no runtime dependency is new. typsphinx/__init__.py is unchanged, so no typst_* config value is added.'
    - 'Zero pre-existing test edits: `git diff --name-status 6cc44f22..HEAD -- tests/` shows only added files plus tests/test_translator.py, whose numstat deletions are 0.'
    - 'COVERAGE.md in the phase directory holds exactly the line `No external API integration: typsphinx translator node handler, test fixture and docstring text only.`, and the api-coverage verify-pre gate result is recorded.'
  artifacts:
    - path: .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-GATES-EVIDENCE.md
      provides: 'the wave 1-3 verdict audit, both full pytest runs, the lint trio, the @preview invariant, the scope fence with controls, the test-edit census, and SC4_LOCAL_VERDICT'
      contains: 'SC4_LOCAL_VERDICT = '
    - path: .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/COVERAGE.md
      provides: 'the one-line no-external-API declaration for the api-coverage gate'
      contains: 'No external API integration: '
  key_links:
    - from: '74-GATES-EVIDENCE.md SC4_LOCAL_VERDICT'
      to: '74-07 wave gate'
      via: 'read with sed before the push'
      pattern: 'SC4_LOCAL_VERDICT = MET'
  prohibitions:
    - statement: 'MUST NOT edit, weaken, skip, xfail or delete any pre-existing test or assertion, or change any file, to make a local gate green; a red gate is recorded and escalated.'
      requirement_id: TRN-02
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT absorb NUM-01, MSG-06, WR-02, WR-03 or any other node still reaching unknown_visit, and MUST NOT change any typsphinx/ line other than the doctest_block handler and docstring text, nor any workflow file, flake.nix, pyproject.toml, uv.lock or typst_* config value.'
      requirement_id: TRN-01
      category: values
      status: resolved
      verification: judgment
---

<objective>
Discharge SC4's local half on the phase tip:
- the lint trio and the full pytest suite, twice (plain and `LC_ALL=C`)
- the `@preview` invariant
- constraint 7's scope fence with its controls
- the zero-pre-existing-test-edit census
- the waves 1-3 verdict audit
- `COVERAGE.md` for the api-coverage gate

Purpose: 74-07 pushes only a tip this plan has shown green locally, and CI does not replace these
checks. Lint, and the locale-dependent test class, are the defects this project has seen only in CI.

Output: `74-GATES-EVIDENCE.md` and `COVERAGE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-RED-EVIDENCE.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-QUA14-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. The `docs` extra lets `tests/test_changelog_page_gate.py` run
instead of skipping (ROADMAP constraint 11). Record `.venv/pyvenv.cfg` before comparing any count
with another environment. Worktree counts are compared with nothing here; they are recorded.

**Timeouts.** Run every docs build, every full pytest run and every verify block that contains one with the Bash tool's `timeout` set to 600000, in the foreground. The default two-minute limit can cut a build or the full suite short and leave a truncated log.

**Evidence key lines.** Bare values only; explanation on the next line.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_74_06`; helper prefix `p7406_`.

**Commits.** Plain `git commit`. Never a GSD commit helper or `74-VERIFICATION.md`. This plan changes
no product file. If a gate is red, record it verbatim, set the verdict `NOT-MET` and return a
blocking checkpoint. Never edit a test or source file to make it green.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: test_changelog_page_gate -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: audit waves 1-3, then run the lint trio, both full pytest runs and the @preview invariant on the tip</name>
  <precondition>74-RED-EVIDENCE.md reads `RED_VERDICT = MET` and `GREEN_VERDICT = MET`, and 74-QUA14-EVIDENCE.md reads `QUA14_FIX_VERDICT = MET`.</precondition>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-GATES-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-RED-EVIDENCE.md and 74-QUA14-EVIDENCE.md (verdict keys)
    - .planning/ROADMAP.md § "Phase 74" SC4 and § "v0.9.6" constraints 7 and 11
    - tests/test_preview_version_sync.py (the three declaration sites it checks)
    - CLAUDE.md § "The @preview version-sync hazard" and § "Worktree-isolated execution"
  </read_first>
  <action>
1. **`## Head check and provisioning`** in a new `74-GATES-EVIDENCE.md`, headed `# Phase 74 — Local Gates and Scope Fence (SC4 local half)`.
   - Record the head check, the provisioning line with its exit, and `PYVENV_HOME` and `PYVENV_VERSION_INFO`.
   - `BASE_74_06 = ` followed by `git rev-parse HEAD`, before any commit; `SCRATCH_74_06 = ` followed by the `mktemp -d` path.

2. **`## Wave 1-3 audit`.** Quote `RED_TREE_SHA`, `RED_VERDICT`, `GREEN_TREE_SHA` and `GREEN_VERDICT` from 74-RED-EVIDENCE.md, and `QUA14_FIX_VERDICT` from 74-QUA14-EVIDENCE.md. Every verdict must read `MET`. Otherwise write `## HALT: wave 1-3 verdict not MET` and stop.

3. **`## Lint trio`.** Run `uv run ruff check .`, `uv run black --check .` and `uv run mypy typsphinx/`, each output with its exit. Keys `RUFF_EXIT`, `BLACK_EXIT` and `MYPY_EXIT`.

4. **`## Full pytest`.**
   - Run `uv run pytest -rs -p no:cacheprovider` with stdout and stderr both redirected into `$S/p7406_pytest.txt`, then `echo "exit:$?"`.
   - Then run `LC_ALL=C uv run pytest -rs -p no:cacheprovider` into `$S/p7406_pytest-c.txt`. The C-locale run is the one matching CI's English locale.
   - For each run, record the final summary line verbatim and every `SKIPPED` line of the `-rs` summary verbatim.
   - Keys `FULL_PYTEST_EXIT` and `FULL_PYTEST_C_EXIT`; `FULL_PYTEST_PASSED` and `FULL_PYTEST_C_PASSED`, from `grep -oE '[0-9]+ passed'`; `FULL_PYTEST_FAILED` and `FULL_PYTEST_C_FAILED`, 0 when no `N failed` appears; `FULL_PYTEST_ERRORS`, 0 when no `N error` appears in either run.
   - Key `CHANGELOG_GATE_SKIPS`: the number of `SKIPPED` lines naming `tests/test_changelog_page_gate.py` across both runs, which must be 0 with the `docs` extra present.
   - These counts are recorded, not compared with the main checkout's. The interpreters are recorded on both sides (constraint 11).

5. **`## @preview invariant`.**
   - Run `uv run pytest tests/test_preview_version_sync.py -v -p no:cacheprovider` and record its output. Key `PREVIEW_SYNC_EXIT`.
   - Key `PREVIEW_PACKAGE_COUNT`: `grep -ho '@preview/[a-z-]*:' typsphinx/templates/base.typ | sort -u | wc -l`, which must be 4.
   - Record `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b HEAD -- typsphinx/templates typsphinx/writer.py typsphinx/template_engine.py`, which must be empty.

6. Commit the evidence. If any exit is non-zero, record it, set `SC4_LOCAL_VERDICT = NOT-MET` in Task 2, and return a blocking checkpoint naming the failing gate.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-GATES-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; [ "$(k RED_VERDICT "$D/74-RED-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k GREEN_VERDICT "$D/74-RED-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k QUA14_FIX_VERDICT "$D/74-QUA14-EVIDENCE.md")" = MET ] &amp;&amp; uv run ruff check . &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; O="$(LC_ALL=C uv run pytest -q -rs -p no:cacheprovider; echo "rc:$?")" &amp;&amp; printf '%s\n' "$O" | grep -qx 'rc:0' &amp;&amp; printf '%s\n' "$O" | grep -qE '[0-9]+ passed' &amp;&amp; ! printf '%s\n' "$O" | grep -qE '[0-9]+ (failed|errors?)\b' &amp;&amp; ! printf '%s\n' "$O" | grep -qE '^SKIPPED .*test_changelog_page_gate' &amp;&amp; uv run pytest tests/test_preview_version_sync.py -q -p no:cacheprovider &amp;&amp; [ "$(grep -ho '@preview/[a-z-]*:' typsphinx/templates/base.typ | sort -u | wc -l)" = 4 ] &amp;&amp; [ -z "$(git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b HEAD -- typsphinx/templates typsphinx/writer.py typsphinx/template_engine.py)" ] &amp;&amp; S="$(k SCRATCH_74_06)" &amp;&amp; test -s "$S/p7406_pytest.txt" &amp;&amp; test -s "$S/p7406_pytest-c.txt" &amp;&amp; for key in FULL_PYTEST_EXIT FULL_PYTEST_C_EXIT FULL_PYTEST_FAILED FULL_PYTEST_C_FAILED FULL_PYTEST_ERRORS CHANGELOG_GATE_SKIPS RUFF_EXIT BLACK_EXIT MYPY_EXIT PREVIEW_SYNC_EXIT; do [ "$(k "$key")" = 0 ] || { echo "nonzero: $key"; exit 1; }; done &amp;&amp; [ "$(k PREVIEW_PACKAGE_COUNT)" = 4 ] &amp;&amp; [ "$(k FULL_PYTEST_PASSED)" -ge 1 ] &amp;&amp; [ "$(k FULL_PYTEST_C_PASSED)" -ge 1 ] &amp;&amp; for s in '## Head check and provisioning' '## Wave 1-3 audit' '## Lint trio' '## Full pytest' '## @preview invariant'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$(k BASE_74_06)" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `nonzero: KEY` or `missing section: NAME` printed. Each of these makes it fail:
- not in a worktree, or a wave 1-3 verdict not MET
- ruff, black or mypy non-zero
- the live C-locale full suite exiting non-zero, reporting no passed tests, reporting a failure or error, or skipping a changelog page gate test
- the preview sync test failing, a base.typ @preview count other than 4, or a declaration site changed since 6cc44f22
- either saved pytest log missing
- a recorded exit, failure, error or skip key not 0, or the package count key not 4
- a product file changed by this plan, a section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `RED_VERDICT`, `GREEN_VERDICT` and `QUA14_FIX_VERDICT` are quoted as `MET`.
    - `uv run ruff check .`, `uv run black --check .` and `uv run mypy typsphinx/` exit 0.
    - Both full pytest runs exit 0 with 0 failed and 0 errors, and `CHANGELOG_GATE_SKIPS = 0`.
    - `tests/test_preview_version_sync.py` passes, `PREVIEW_PACKAGE_COUNT = 4`, and no declaration site changed since `6cc44f22`.
  </acceptance_criteria>
  <done>The phase tip is locally green on every gate CI runs, including the C-locale suite, and the @preview invariant holds.</done>
</task>

<task type="auto">
  <name>Task 2: The scope fence with pathspec and widened-diff controls, the test-edit census, COVERAGE.md, and SC4_LOCAL_VERDICT</name>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-GATES-EVIDENCE.md, .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/COVERAGE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-GATES-EVIDENCE.md (Task 1)
    - .planning/ROADMAP.md § "v0.9.6" constraint 7 and § "Phase 74" SC4 "Scope"
    - .planning/milestones/v0.9.5-phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md (a fence with controls, the shape to mirror)
  </read_first>
  <action>
All ranges below are `6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b..HEAD`, the milestone base per
constraint 10.

1. **`## Scope fence: typsphinx/`.**
   - Record `git diff --stat 6cc44f22..HEAD -- typsphinx/`.
   - Key `TYPSPHINX_DIFF_FILES`: `git diff --name-only 6cc44f22..HEAD -- typsphinx/`, `LC_ALL=C`-sorted and `|`-joined. It must be exactly `typsphinx/pathfmt.py|typsphinx/translator.py`.
   - **Pathspec control:** `git diff --name-only 6cc44f22..HEAD -- typsphinx/translator.py` must be non-empty, proving the pathspec matches a real change.
   - **Widened-diff control:** `git diff --name-only 6cc44f22..HEAD`, without a pathspec, must list files outside typsphinx/, such as the new test module and `.planning/` files. That proves the range carries this phase's work and the fence is not vacuous. Record the count of files per top-level directory.
   - **Region scope:**
     - Every translator.py hunk (`git diff -U0`, new-side start line) must lie in the handler region, from the line of `    def visit_literal_block` to the line before `    def visit_definition_list(`, or inside `visit_toctree`'s docstring, within 60 lines after `    def visit_toctree`.
     - Every pathfmt.py hunk must lie within 40 lines after `def quote_path`, and add only empty lines.
     - Tabulate each hunk with its region. Key `TRANSLATOR_HUNKS_OUTSIDE_SCOPE`, which must be 0.

2. **`## Standing invariants`.**
   - `git diff --name-only 6cc44f22..HEAD -- .github flake.nix` must be empty.
   - **Control:** `git ls-files -- .github/workflows/ci.yml flake.nix` must list both files, so the pathspecs resolve to tracked paths.
   - `git diff --name-only 6cc44f22..HEAD -- pyproject.toml uv.lock typsphinx/__init__.py` must be empty: no version bump, no new dependency, no new `typst_*` config value.
   - `git show HEAD:pyproject.toml | grep -m1 '^version = '` must read `version = "0.9.2"`. **Control:** the same read at `6cc44f22` reads the same line.
   - Keys `WORKFLOWS_FLAKE_UNTOUCHED = yes`, `PYPROJECT_UV_LOCK_INIT_UNTOUCHED = yes` and `VERSION_AT_CLOSE = 0.9.2`.

3. **`## Test-edit census`.**
   - Record `git diff --name-status 6cc44f22..HEAD -- tests/`. Every row must be `A` (added), except one `M` row for `tests/test_translator.py`.
   - `git diff --numstat 6cc44f22..HEAD -- tests/test_translator.py` must show 0 deletions.
   - Keys `PREEXISTING_TEST_DELETIONS = 0` and `MODIFIED_TEST_FILES = tests/test_translator.py`.

4. **`## API coverage declaration`.**
   - Write `COVERAGE.md` in the phase directory with exactly one line: `No external API integration: typsphinx translator node handler, test fixture and docstring text only.`
   - Run `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs` and record its JSON output verbatim. Key `API_COVERAGE_PASSED`, from its `passed` field.

5. **`## SC4 local verdict`.** `SC4_LOCAL_VERDICT = MET` only when:
   - every Task 1 key is 0, or 4 for the package count
   - `TRANSLATOR_HUNKS_OUTSIDE_SCOPE = 0`
   - the three invariant keys hold
   - `PREEXISTING_TEST_DELETIONS = 0`

   Otherwise write `NOT-MET` and a blocking checkpoint.

6. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-GATES-EVIDENCE.md" &amp;&amp; MB=6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; TD="$(git diff --name-only "$MB" HEAD -- typsphinx | LC_ALL=C sort | paste -sd'|')" &amp;&amp; [ "$TD" = 'typsphinx/pathfmt.py|typsphinx/translator.py' ] &amp;&amp; [ "$(k TYPSPHINX_DIFF_FILES)" = "$TD" ] &amp;&amp; [ -n "$(git diff --name-only "$MB" HEAD -- typsphinx/translator.py)" ] &amp;&amp; W="$(git diff --name-only "$MB" HEAD)" &amp;&amp; printf '%s\n' "$W" | grep -qxF tests/test_doctest_block_render_gate.py &amp;&amp; [ "$(printf '%s\n' "$W" | grep '^typsphinx/' | wc -l)" -eq 2 ] &amp;&amp; VL="$(grep -n '^    def visit_literal_block' typsphinx/translator.py | cut -d: -f1)" &amp;&amp; VD="$(grep -n '^    def visit_definition_list(' typsphinx/translator.py | cut -d: -f1)" &amp;&amp; VT="$(grep -n '^    def visit_toctree' typsphinx/translator.py | cut -d: -f1)" &amp;&amp; for c in $(git diff -U0 "$MB" HEAD -- typsphinx/translator.py | sed -n 's/^@@ -[0-9,]* +\([0-9]*\).*/\1/p'); do { [ "$c" -ge "$VL" ] &amp;&amp; [ "$c" -lt "$VD" ]; } || { [ "$c" -gt "$VT" ] &amp;&amp; [ "$c" -lt "$((VT + 60))" ]; } || { echo "translator hunk out of scope: $c"; exit 1; }; done &amp;&amp; QP="$(grep -n '^def quote_path' typsphinx/pathfmt.py | cut -d: -f1)" &amp;&amp; for c in $(git diff -U0 "$MB" HEAD -- typsphinx/pathfmt.py | sed -n 's/^@@ -[0-9,]* +\([0-9]*\).*/\1/p'); do [ "$c" -gt "$QP" ] &amp;&amp; [ "$c" -lt "$((QP + 40))" ] || { echo "pathfmt hunk out of scope: $c"; exit 1; }; done &amp;&amp; [ -z "$(git diff -U0 "$MB" HEAD -- typsphinx/pathfmt.py | grep -E '^[-+]' | grep -vE '^(\+\+\+|---) ' | grep -vx '+')" ] &amp;&amp; [ "$(k TRANSLATOR_HUNKS_OUTSIDE_SCOPE)" = 0 ] &amp;&amp; [ -z "$(git diff --name-only "$MB" HEAD -- .github flake.nix)" ] &amp;&amp; [ "$(git ls-files -- .github/workflows/ci.yml flake.nix | wc -l)" -eq 2 ] &amp;&amp; [ -z "$(git diff --name-only "$MB" HEAD -- pyproject.toml uv.lock typsphinx/__init__.py)" ] &amp;&amp; [ "$(git show HEAD:pyproject.toml | grep -m1 '^version = ')" = 'version = "0.9.2"' ] &amp;&amp; [ "$(git show "$MB":pyproject.toml | grep -m1 '^version = ')" = 'version = "0.9.2"' ] &amp;&amp; [ "$(k WORKFLOWS_FLAKE_UNTOUCHED)" = yes ] &amp;&amp; [ "$(k PYPROJECT_UV_LOCK_INIT_UNTOUCHED)" = yes ] &amp;&amp; [ "$(k VERSION_AT_CLOSE)" = 0.9.2 ] &amp;&amp; [ -z "$(git diff --name-status "$MB" HEAD -- tests | grep -vE '^A[[:space:]]' | grep -vE '^M[[:space:]]+tests/test_translator\.py$')" ] &amp;&amp; [ "$(git diff --numstat "$MB" HEAD -- tests/test_translator.py | cut -f2)" = 0 ] &amp;&amp; [ "$(k PREEXISTING_TEST_DELETIONS)" = 0 ] &amp;&amp; [ "$(cat "$D/COVERAGE.md")" = 'No external API integration: typsphinx translator node handler, test fixture and docstring text only.' ] &amp;&amp; [ "$(k API_COVERAGE_PASSED)" = true ] &amp;&amp; [ "$(k SC4_LOCAL_VERDICT)" = MET ] &amp;&amp; for s in '## Scope fence: typsphinx/' '## Standing invariants' '## Test-edit census' '## API coverage declaration' '## SC4 local verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `translator hunk out of scope: N`, `pathfmt hunk out of scope: N` or `missing section: NAME` printed. Each of these makes it fail:
- the typsphinx/ file set not exactly pathfmt.py and translator.py, or differing from its key
- the pathspec control empty, or the widened-diff control missing the new test module or counting other typsphinx/ files
- a translator.py or pathfmt.py hunk outside its region, or a non-empty or removed line in pathfmt.py
- .github or flake.nix changed, or the control not finding both tracked paths
- pyproject.toml, uv.lock or __init__.py changed, or a version line other than 0.9.2 at HEAD or at the base control
- a tests/ row other than an added file or test_translator.py modified, or a deletion in test_translator.py
- COVERAGE.md not exactly the declared line, or the api-coverage gate not passed
- any verdict or invariant key wrong, a section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only 6cc44f22..HEAD -- typsphinx/` is exactly `typsphinx/pathfmt.py` and `typsphinx/translator.py`, with both controls recorded, and `TRANSLATOR_HUNKS_OUTSIDE_SCOPE = 0`.
    - `.github/` and `flake.nix` are absent from the milestone diff, the control lists both paths as tracked, and `pyproject.toml` reads `version = "0.9.2"` with `uv.lock` and `typsphinx/__init__.py` unchanged.
    - Only added test files plus a deletion-free `tests/test_translator.py` appear under `tests/`.
    - `COVERAGE.md` holds exactly the declared line, and `API_COVERAGE_PASSED = true`.
    - `SC4_LOCAL_VERDICT = MET`.
  </acceptance_criteria>
  <done>The scope fence and the standing invariants hold with controls, no pre-existing test was edited, and SC4's local half is recorded MET for 74-07 to gate on.</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide accounting lives in `74-01-PLAN.md`. This plan carries prohibitions P3 and P4. It runs
in the same wave as 74-05, which it neither reads nor audits. Both audit only waves 1-3, and 74-07
gates on both.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local gates → push decision | a tip pushed without local proof defers every signal to CI |
| milestone diff → scope fence | a vacuous or mis-scoped diff command could pass a fence that is not holding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-18 | Tampering | scope creep in typsphinx/ or into workflows, flake.nix, pyproject.toml or uv.lock | high | mitigate | exact file-set equality, region-scoped hunk checks, pathspec and widened-diff controls, invariant reads at HEAD and at the base; prohibition P4 |
| T-74-19 | Denial of Service | a locale-dependent test that fails only in CI | medium | mitigate | the full suite also runs under `LC_ALL=C` and must be green |
| T-74-20 | Repudiation | a gate turned green by editing tests | high | mitigate | tests/ name-status census and zero deletions in test_translator.py; prohibition P3 |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list (identical in every plan):
- `typsphinx/translator.py`: `TypstTranslator.visit_doctest_block(self, node: nodes.doctest_block) -> None` and `TypstTranslator.depart_doctest_block(self, node: nodes.doctest_block) -> None` (74-03); `visit_toctree` docstring blank lines (74-04).
- `typsphinx/pathfmt.py`: `quote_path` docstring blank line (74-04).
- `tests/test_doctest_block_render_gate.py` (74-02): class `TestDoctestBlockRenderGate`; constants `FIXTURE_DIR`, `MASTER_DOCNAMES`, `SHAPE_SENTINELS`, `UNKNOWN_DOCTEST_BLOCK`, `FENCE_OPEN`; helpers `_run_sphinx_build_typstpdf`, `_wrapper_pdf_path`, `_assert_pdf_magic`, `_source_doctest_block`, `_unknown_doctest_chunks`, `_read_typ`; module fixture `doctest_block_build`.
- `tests/fixtures/doctest_block_render_gate/{conf.py,index.rst,context_a_paragraph.rst,context_b_nonfirst_positions.rst}` (74-02).
- `tests/test_translator.py` (74-03): `test_doctest_block_defaults_to_python_fence`, `test_doctest_block_honours_existing_language`, `test_literal_block_without_language_keeps_bare_fence`, `test_doctest_block_shares_list_item_wrapper`.
- Evidence: `74-BASE-EVIDENCE.md`, `74-RED-EVIDENCE.md`, `74-QUA14-EVIDENCE.md`, `74-TIP-EVIDENCE.md`, `74-GATES-EVIDENCE.md`, `74-CI-EVIDENCE.md`, `COVERAGE.md`.

This plan creates `74-GATES-EVIDENCE.md` with keys `BASE_74_06`, `SCRATCH_74_06`, `PYVENV_HOME`,
`PYVENV_VERSION_INFO`, `RUFF_EXIT`, `BLACK_EXIT`, `MYPY_EXIT`, `FULL_PYTEST_EXIT`,
`FULL_PYTEST_C_EXIT`, `FULL_PYTEST_PASSED`, `FULL_PYTEST_C_PASSED`, `FULL_PYTEST_FAILED`,
`FULL_PYTEST_C_FAILED`, `FULL_PYTEST_ERRORS`, `CHANGELOG_GATE_SKIPS`, `PREVIEW_SYNC_EXIT`,
`PREVIEW_PACKAGE_COUNT`, `TYPSPHINX_DIFF_FILES`, `TRANSLATOR_HUNKS_OUTSIDE_SCOPE`,
`WORKFLOWS_FLAKE_UNTOUCHED`, `PYPROJECT_UV_LOCK_INIT_UNTOUCHED`, `VERSION_AT_CLOSE`,
`PREEXISTING_TEST_DELETIONS`, `MODIFIED_TEST_FILES`, `API_COVERAGE_PASSED` and
`SC4_LOCAL_VERDICT`, and `COVERAGE.md`.
</artifacts_this_phase_produces>

<verification>
- Lint, type check and the full suite, plain and C-locale, are green on the tip, and the @preview
  invariant holds.
- The typsphinx/ fence lists only the two files, region-scoped, with pathspec and widened-diff
  controls; workflows, flake.nix, pyproject.toml, uv.lock and `__init__.py` are untouched.
- No pre-existing test lost a line, and COVERAGE.md satisfies the api-coverage gate.
</verification>

<success_criteria>
SC4's local-gates and scope bullets are met on the phase tip.
</success_criteria>

<output>
Create `.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-06-SUMMARY.md` when done.
Its first section states `FULL_PYTEST_PASSED`, `FULL_PYTEST_C_PASSED`, the skip reasons,
`TYPSPHINX_DIFF_FILES`, `VERSION_AT_CLOSE` and `SC4_LOCAL_VERDICT`. Do not edit REQUIREMENTS.md,
ROADMAP.md or STATE.md.
</output>
