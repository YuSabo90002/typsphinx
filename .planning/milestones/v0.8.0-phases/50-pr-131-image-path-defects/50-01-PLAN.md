---
phase: 50-pr-131-image-path-defects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/converted_image_collision_render_gate/conf.py
  - tests/fixtures/converted_image_collision_render_gate/index.rst
  - tests/fixtures/converted_image_collision_render_gate/converted_source.rst
  - tests/fixtures/converted_image_collision_render_gate/real_source.rst
  - tests/fixtures/converted_image_collision_render_gate/images/chart.png
  - tests/fixtures/converted_image_collision_render_gate/_static/chart.svg
  - tests/fixtures/converted_image_collision_render_gate/_static/converted_chart_stand_in.png
  - tests/test_converted_image_collision_render_gate.py
  - .planning/phases/50-pr-131-image-path-defects/50-RED-EVIDENCE.md
autonomous: true
requirements: [IMG-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A sibling fixture project reproduces the IMG-01 collision as one master wrapper over two content documents producing one PDF: one converted image rehomed onto images/chart.png and one real source image genuinely at <srcdir>/images/chart.png (D-10)."
    - "The two fixture PNGs are discriminated by pixel dimensions — 40x24 for the real source image, 16x64 for the converted stand-in — never by raw bytes, because Typst re-encodes what it embeds (D-09)."
    - "The pre-fix RED for IMG-01 is written down and observed against the unfixed builder before any implementation runs: the single compiled master.pdf's extracted-image size set holds exactly one entry, and only one of the two image files reaches the output tree (D-08)."
    - "The gate module pins the post-fix destination as the reserved top-level namespace path _typst_converted/images/chart.png, so the escape hatch it proves is a reserved namespace and not a write-order-dependent numeric suffix (D-02)."
    - "tests/fixtures/absolute_image_render_gate/, tests/test_absolute_image_render_gate.py and tests/test_builder.py are byte-unchanged by this plan — the new fixture is a SIBLING, never a mutation (D-12)."
  artifacts:
    - tests/fixtures/converted_image_collision_render_gate/conf.py
    - tests/fixtures/converted_image_collision_render_gate/index.rst
    - tests/fixtures/converted_image_collision_render_gate/converted_source.rst
    - tests/fixtures/converted_image_collision_render_gate/real_source.rst
    - tests/fixtures/converted_image_collision_render_gate/images/chart.png
    - tests/fixtures/converted_image_collision_render_gate/_static/chart.svg
    - tests/fixtures/converted_image_collision_render_gate/_static/converted_chart_stand_in.png
    - tests/test_converted_image_collision_render_gate.py
    - .planning/phases/50-pr-131-image-path-defects/50-RED-EVIDENCE.md
  key_links:
    - "FakeImageConverter in the new fixture's conf.py rewrites node['uri'] to <doctreedir>/images/chart.png — the exact basename the real source image already occupies — which is the single wire that makes _track_image()'s rehome collide at all."
    - "The gate module reads master.pdf, the wrapper's own output, because since Phase 47 only wrappers compile to PDF; reading index.pdf would find nothing."
    - "The two xfail(strict=True) markers are the ONLY link between this plan's RED and plan 50-02's GREEN; removing them is the whole of 50-02's permitted edit to this module."
  prohibitions:
    - "No expected value in any gate — neither the new D-10 gate authored here nor the three D-12-pinned assertions — is ever edited, weakened, or deleted to make an observed build result pass."
    - "The collision outcome must never become a function of document write order, so that two independent builds of the same project emit the identical path."
    - "An ordinary project with no collision and no escape must not silently receive a different emitted image path or a different copy destination than v0.7.1 produced."
---

<objective>
Close the Wave 0 validation gaps for Phase 50 and, per binding constraint #4, write down and OBSERVE
the pre-fix RED for IMG-01 against the unfixed builder — before any line of `typsphinx/builder.py`
changes (D-08, D-10).

This defect compiles fine today. "Does not compile" is therefore NOT an acceptable RED. The RED this
plan records is the D-08 shape: read out of the single compiled PDF that the one master wrapper
produces, asserting that today the extracted-image size set holds exactly ONE entry (the same picture
for both documents) and that only ONE of the two files was copied into the output tree.

Purpose: make plan 50-02's fix falsifiable. Without a recorded RED, a green gate after the fix proves
only that the code does what the code does (ROADMAP binding constraint #6, no laundered gates).
Output: a sibling fixture project, a render-gate test module whose two collision assertions are
recorded RED via `xfail(strict=True)`, and `50-RED-EVIDENCE.md` carrying the verbatim pre-fix failure.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-pr-131-image-path-defects/50-CONTEXT.md
@.planning/phases/50-pr-131-image-path-defects/50-RESEARCH.md
@.planning/phases/50-pr-131-image-path-defects/50-PATTERNS.md
@.planning/phases/50-pr-131-image-path-defects/50-VALIDATION.md
@CLAUDE.md
</context>

<worktree_protocol>
This project runs executors in isolated git worktrees as its STANDING execution mode (`CLAUDE.md`,
owner decision 2026-07-20). Before ANY command in this plan:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then run EVERY subsequent command through `uv run`. A bare `pytest` silently tests the MAIN checkout's
editable install, not this worktree — a correct fix will read as RED and a broken one as GREEN. Sphinx
subprocess builds inside test modules use `sys.executable -m sphinx`, never a console-script shim
(NixOS PATH-shadowing hazard).
</worktree_protocol>

<tasks>

<task type="auto">
  <name>Task 1: Build the D-10 sibling fixture — one master wrapper, two content documents, one collision</name>
  <files>tests/fixtures/converted_image_collision_render_gate/conf.py, tests/fixtures/converted_image_collision_render_gate/index.rst, tests/fixtures/converted_image_collision_render_gate/converted_source.rst, tests/fixtures/converted_image_collision_render_gate/real_source.rst, tests/fixtures/converted_image_collision_render_gate/images/chart.png, tests/fixtures/converted_image_collision_render_gate/_static/chart.svg, tests/fixtures/converted_image_collision_render_gate/_static/converted_chart_stand_in.png</files>
  <precondition>The worktree has its own provisioned venv: `uv run python -c "import typsphinx, pypdf, PIL, typst"` exits 0 after the worktree sync above. Halt and report if it does not.</precondition>
  <read_first>
    - tests/fixtures/absolute_image_render_gate/conf.py (the FakeImageConverter post-transform to copy; DO NOT edit this file — D-12 pins its consumer's assertions)
    - tests/fixtures/absolute_image_render_gate/index.rst (the figure-directive shape and the Phase 47 typst_documents de-collision comment to carry over)
    - tests/fixtures/cross_doc_label_namespace_render_gate/index.rst (this repo's master-toctree-over-two-content-documents shape)
    - typsphinx/builder.py lines 840-876 (`_track_image()` — the absolute-URI branch whose rehome target the fixture must collide with) and lines 1092-1129 (`copy_image_files()`)
    - .planning/phases/50-pr-131-image-path-defects/50-RESEARCH.md § Recommended Project Structure
  </read_first>
  <action>
Create a NEW sibling fixture directory `tests/fixtures/converted_image_collision_render_gate/`. It is a
sibling of `tests/fixtures/absolute_image_render_gate/`, never a mutation of it (D-12 pins that
fixture's current assertions).

`conf.py`: adapt the analog fixture's `FakeImageConverter` (a `SphinxTransform` subclass with
`default_priority = 200`, registered from `setup(app)` via `app.add_post_transform`). Two things
change from the analog. First, the conversion destination basename becomes `chart.png` so the rehome
target `images/chart.png` is the exact key the real source image already claims — that collision is
the entire point of the fixture (D-10). Second, the stand-in it copies is
`_static/converted_chart_stand_in.png`. Set `project = "Converted Image Collision Render Gate"`,
`author = "Test Author"`, `release = "1.0.0"`, `extensions = ["typsphinx"]`,
`html_static_path = ["_static"]`, and
`typst_documents = [("index", "master.typ", "Converted Image Collision Render Gate", "Test Author")]`
— a `master.typ` target, not `index`, because a target stem identical to the docname self-collides
under the Phase 47 two-layer split. Carry the analog's explanatory comment framing for that rename.
The transform must match only `.svg` uris so it converts `converted_source.rst`'s figure and leaves
`real_source.rst`'s ordinary PNG figure alone.

`index.rst`: the master. A title, a short prose paragraph naming what the fixture proves, and a
`.. toctree::` listing `converted_source` then `real_source`. No figure of its own.

`converted_source.rst`: a title and a `.. figure:: _static/chart.svg` whose uri the fake converter
rewrites to the absolute `<doctreedir>/images/chart.png`.

`real_source.rst`: a title and a `.. figure:: images/chart.png` — an ordinary, source-root-relative
image that genuinely lives at `<srcdir>/images/chart.png`. This is the file the converted image
currently destroys.

Assets. `_static/chart.svg` is any minimal valid SVG; it is only a trigger and is never decoded.
Generate the two PNGs with Pillow (already a dev dependency, `pyproject.toml:47`) and commit the
resulting binary files: `images/chart.png` at 40x24 pixels and
`_static/converted_chart_stand_in.png` at 16x64 pixels. Give them visibly different solid fill colors
as well. The dimensions are the D-09 discriminator — Typst re-encodes what it embeds, so a byte
comparison against the source PNG is not a stable assertion, but pixel dimensions survive embedding
and make the two extracted images self-identifying. Do not reuse the analog fixture's 1x1 stand-in:
a 1x1 image is a poor discriminator and both fixture images must be genuinely distinct.
  </action>
  <verify>
    <automated>rm -rf /tmp/img50-fixture-smoke && uv run python -c "from PIL import Image; a=Image.open('tests/fixtures/converted_image_collision_render_gate/images/chart.png'); b=Image.open('tests/fixtures/converted_image_collision_render_gate/_static/converted_chart_stand_in.png'); assert a.size==(40,24), a.size; assert b.size==(16,64), b.size; print('dims ok')" && uv run python -m sphinx -b typst tests/fixtures/converted_image_collision_render_gate /tmp/img50-fixture-smoke && test -f /tmp/img50-fixture-smoke/converted_source.typ && test -f /tmp/img50-fixture-smoke/real_source.typ && test -f /tmp/img50-fixture-smoke/master.typ && grep -q 'image(' /tmp/img50-fixture-smoke/converted_source.typ && grep -q 'image(' /tmp/img50-fixture-smoke/real_source.typ</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/converted_image_collision_render_gate/` contains exactly: `conf.py`, `index.rst`, `converted_source.rst`, `real_source.rst`, `images/chart.png`, `_static/chart.svg`, `_static/converted_chart_stand_in.png`.
    - `PIL.Image.open(...).size` is `(40, 24)` for `images/chart.png` and `(16, 64)` for `_static/converted_chart_stand_in.png` (D-09).
    - `uv run python -m sphinx -b typst tests/fixtures/converted_image_collision_render_gate <tmp>` exits 0 and writes `master.typ`, `index.typ`, `converted_source.typ`, `real_source.typ`.
    - Both `converted_source.typ` and `real_source.typ` contain an `image(` call; pre-fix both resolve to the same argument string, which Task 2's structural assertion records.
    - `git status --porcelain tests/fixtures/absolute_image_render_gate tests/test_absolute_image_render_gate.py tests/test_builder.py` prints nothing (D-12 fixed points untouched).
  </acceptance_criteria>
  <done>A sibling fixture project builds under `-b typst` and produces two content documents whose image references collide on `images/chart.png`, with the two PNGs distinguishable by pixel dimensions.</done>
</task>

<task type="auto">
  <name>Task 2: Author the D-08 render-gate module with both collision assertions marked xfail(strict=True)</name>
  <files>tests/test_converted_image_collision_render_gate.py</files>
  <precondition>Task 1's fixture directory exists and its `-b typst` smoke build exits 0; `typsphinx/builder.py` is still unmodified in this worktree (`git diff --stat -- typsphinx/builder.py` prints nothing).</precondition>
  <read_first>
    - tests/test_absolute_image_render_gate.py (the domain analog: skip guards, `_run_sphinx_build_typstpdf()` helper, the `master.pdf` wrapper-output convention, the stderr negative checks — read it, do not edit it)
    - tests/test_abbr_pep_separator_render_gate.py (the most recent render-gate scaffolding: `TYPST_AVAILABLE`/`PYPDF_AVAILABLE` split, the two-test structural/pypdf shape)
    - tests/test_pdf_render_gate.py lines 244-266 (the established `typst.compile()` + `pypdf.PdfReader` extraction scaffolding)
    - tests/fixtures/converted_image_collision_render_gate/ (all files created in Task 1)
    - .planning/phases/50-pr-131-image-path-defects/50-RESEARCH.md § Code Examples and § Common Pitfalls (Pitfall 3)
  </read_first>
  <action>
Create `tests/test_converted_image_collision_render_gate.py`. Follow this repository's per-module
render-gate convention: re-derive `_run_sphinx_build_typstpdf()` locally in this module using
`subprocess.run([sys.executable, "-m", "sphinx", "-b", "typstpdf", ...], capture_output=True,
text=True)`. Do NOT extract a shared `conftest.py` helper — no render-gate module in this repo shares
one, and this plan does not introduce that precedent. Add module-level `TYPST_AVAILABLE` and
`PYPDF_AVAILABLE` try/except-ImportError flags, a `converted_image_collision_render_gate_dir` fixture
returning `Path(__file__).parent / "fixtures" / "converted_image_collision_render_gate"`, and a
`temp_build_dir` fixture returning `tmp_path / "_build"`.

Class `TestConvertedImageCollisionRenderGate`, guarded at class level with
`@pytest.mark.skipif(not TYPST_AVAILABLE, ...)`. Three tests.

Test 1, `test_typstpdf_build_succeeds_without_image_warnings` — the CONTROL, no xfail marker. It
records that this defect is non-fatal: the build exits 0, `master.pdf` exists, is non-empty and
starts with the `%PDF` magic bytes, and stderr carries neither `are the same file` nor
`Image file not found`. Green both before and after the fix. It is the written proof that "does not
compile" was never available as a RED for IMG-01.

Test 2, `test_content_documents_emit_distinct_image_paths` — the structural RED, marked
`@pytest.mark.xfail(strict=True, reason=...)`. Read `converted_source.typ` and `real_source.typ`, and
require `real_source.typ` to contain the argument string `images/chart.png` inside its image call
while `converted_source.typ` contains `_typst_converted/images/chart.png` — the D-02 reserved
top-level namespace. Also require the two emitted image arguments to differ from each other.

Test 3, `test_pdf_embeds_both_distinctly_sized_images` — the D-08 embedded-image RED, marked
`@pytest.mark.xfail(strict=True, reason=...)` and additionally
`@pytest.mark.skipif(not PYPDF_AVAILABLE, ...)`. Open `master.pdf` with `pypdf.PdfReader`, build the
SET of `image_file.image.size` pairs over every page and every entry of `page.images`, skipping
entries whose `.image` is None, and require that set to equal `{(40, 24), (16, 64)}`. Assert on the
SET, never on `page.images[N]` by positional index — pypdf's extraction order follows Typst's own PDF
XObject enumeration, not doctree order (Pitfall 3). In the same test also require that
`<build>/_typst_converted/images/chart.png` exists and that `<build>/images/chart.png` is
byte-identical to the fixture's own `images/chart.png`, which is what proves the REAL source image —
not the converted stand-in — is the file that ends up at the ordinary location.

Write the module docstring and both xfail `reason=` strings in a tense that stays TRUE after plan
50-02 removes the two markers: describe the RED as recorded in `50-RED-EVIDENCE.md` and name 50-02 as
the plan that removes the markers. Do not write "currently xfail" or any present-tense claim about
marker presence — Phase 47 shipped exactly that staleness as a review Warning and it is still open.

Add a prominent module-docstring paragraph stating the contract that binds plan 50-02: the ONLY edit
permitted to this file downstream is the removal of the two `xfail` decorator lines. Every assertion
in it was derived from D-01/D-02/D-08/D-09/D-10 before the fix existed, and any change to an
assertion is a laundered gate under ROADMAP binding constraint #6.

**Mechanical requirement that makes that contract checkable.** Define the two RED reason strings as
module-level constants named `_RED_REASON_STRUCTURAL` and `_RED_REASON_EMBEDDED`, then write each
decorator as a SINGLE physical line of the form
`@pytest.mark.xfail(strict=True, reason=_RED_REASON_STRUCTURAL)`. Keeping the reason out of the
decorator keeps each line short enough that `black` will not wrap it across multiple physical lines,
which is what lets plan 50-02 prove — by filtering out every line containing `xfail` from both
versions and diffing the remainder — that its edit removed exactly two decorator lines and touched
nothing else. Do not inline the reason strings into the decorators.
  </action>
  <verify>
    <automated>uv run pytest tests/test_converted_image_collision_render_gate.py -rx -q 2>&1 | tail -5 && uv run pytest tests/test_converted_image_collision_render_gate.py -q 2>&1 | grep -qE '1 passed, 2 xfailed'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_converted_image_collision_render_gate.py -q` exits 0 and reports `1 passed, 2 xfailed`.
    - `grep -c 'xfail(strict=True' tests/test_converted_image_collision_render_gate.py` returns `2`, and `grep -c '^ *@pytest.mark.xfail(strict=True, reason=_RED_REASON_' tests/test_converted_image_collision_render_gate.py` also returns `2` — each decorator is one physical line, with its reason held in a module-level constant so `black` cannot wrap it.
    - `uv run black --check tests/test_converted_image_collision_render_gate.py` exits 0 without reformatting either decorator line.
    - `grep -c 'page.images\[' tests/test_converted_image_collision_render_gate.py` returns `0` (set comparison, not positional index — Pitfall 3).
    - `grep -c 'sys.executable' tests/test_converted_image_collision_render_gate.py` is at least `1`, and `grep -c 'sphinx-build' tests/test_converted_image_collision_render_gate.py` returns `0` (no console-script shim).
    - The literals `_typst_converted/images/chart.png`, `(40, 24)` and `(16, 64)` each appear in the module.
    - `git status --porcelain tests/test_absolute_image_render_gate.py tests/test_builder.py tests/fixtures/absolute_image_render_gate` prints nothing (D-12).
  </acceptance_criteria>
  <done>A render-gate module exists whose control test passes and whose two collision assertions are recorded as strict xfails against the unfixed builder.</done>
</task>

<task type="auto">
  <name>Task 3: Observe and record the verbatim pre-fix RED into 50-RED-EVIDENCE.md</name>
  <files>.planning/phases/50-pr-131-image-path-defects/50-RED-EVIDENCE.md</files>
  <precondition>`typsphinx/builder.py` is still unmodified in this worktree — `git diff --stat -- typsphinx/builder.py` prints nothing. If it does not, the RED is being recorded against already-fixed code: STOP and escalate.</precondition>
  <read_first>
    - tests/test_converted_image_collision_render_gate.py (created in Task 2)
    - .planning/ROADMAP.md lines 368-394 (binding constraints #4 and #6 — what a valid RED is for a non-fatal defect, and what a laundered gate is)
    - .planning/phases/50-pr-131-image-path-defects/50-CONTEXT.md § Verification obligations (D-08 through D-12)
  </read_first>
  <action>
Run the gate module with `--runxfail`, which makes the two xfail markers inert and surfaces the real
assertion failures. Capture the complete output verbatim.

Write `.planning/phases/50-pr-131-image-path-defects/50-RED-EVIDENCE.md` containing: the exact
commands run and their exit codes; the `git rev-parse HEAD` of the worktree and the `sha256sum` of
both `typsphinx/builder.py` and `tests/test_converted_image_collision_render_gate.py` at the moment of
measurement; the verbatim `--runxfail` failure output fenced as a transcript; and a short reading of
what the transcript shows — the extracted-image size set observed pre-fix (expected: a single-element
set), which of the two files landed at `<build>/images/chart.png`, and the absence of
`_typst_converted/` from the output tree entirely.

Then confirm the failures are the EXPECTED symptom and not an infrastructure artifact. An
`ImportError`, a fixture-resolution error, a non-zero `sphinx-build` return code, or a `pypdf` read
error is NOT the IMG-01 RED — it is a broken measurement. If any of those appear, fix the measurement
and re-run before writing the evidence; do not record a broken measurement as a RED. State this check
and its outcome explicitly in the evidence file.

Also record, from the same run, which single pixel-dimension pair the pre-fix PDF embeds. `write()`
iterates `sorted(docnames)`, so `converted_source` is tracked before `real_source` and the converted
stand-in is expected to be the winner, but record what was OBSERVED rather than what was predicted —
and if the observation disagrees with that prediction, record the disagreement rather than
reconciling it. Either way the set has one element, which is the assertion that matters.
  </action>
  <verify>
    <automated>if uv run pytest tests/test_converted_image_collision_render_gate.py --runxfail -q > /tmp/img50-red.txt 2>&1; then echo "UNEXPECTED PASS - no RED observed"; exit 1; fi; grep -qE '2 failed, 1 passed' /tmp/img50-red.txt && test -s .planning/phases/50-pr-131-image-path-defects/50-RED-EVIDENCE.md && grep -q 'runxfail' .planning/phases/50-pr-131-image-path-defects/50-RED-EVIDENCE.md && grep -q 'sha256' .planning/phases/50-pr-131-image-path-defects/50-RED-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_converted_image_collision_render_gate.py --runxfail -q` exits NON-ZERO and reports `2 failed, 1 passed`.
    - Neither failure is an `ImportError`, a fixture-resolution error, a non-zero `sphinx-build` return code, nor a `pypdf` read error; the evidence file states this check and its outcome.
    - `50-RED-EVIDENCE.md` contains: the `git rev-parse HEAD` value, the `sha256sum` of `typsphinx/builder.py`, the `sha256sum` of the gate module, and the verbatim `--runxfail` transcript.
    - `50-RED-EVIDENCE.md` names the single observed pixel-dimension pair present in the pre-fix `master.pdf` and records that `_typst_converted/` is absent from the pre-fix output tree.
    - `git diff --stat -- typsphinx/builder.py` prints nothing — the RED was measured against unfixed code.
  </acceptance_criteria>
  <done>The IMG-01 pre-fix RED is observed against the unfixed builder and recorded verbatim, with the measurement's own integrity checked and stated.</done>
</task>

</tasks>

<assumption_delta_decision>
**Signal received:** `{"kind":"pluralization","term":"also","snippet":"all. **IMG-01 is also a
regression in failure mode**, not merely a defect: the same project used to"}` — the ROADMAP Phase 50
goal line.

**Primary noun:** the **tracked image key** — the single string that is simultaneously the
`self.images` dict key, the `copy_image_files()` destination path under `outdir`, and the argument the
translator emits into `image(...)`. Changing it changes all three at once; that identity is why D-01
can hold D-12's three pinned assertions.

**Decision: `no-change`.**

**Rationale:** the detected "also" is prose emphasis in a goal sentence (IMG-01 is a defect *and* a
failure-mode regression), not a singular→plural, required→optional, or derived→chosen transition in
the identity model — so nothing is promoted or added alongside. Recorded honestly for the reader: this
phase DOES contain a genuine derived→chosen transition — the tracked image key stops being purely
derived (`relpath(uri, doctreedir)`, one formula, always) and becomes chosen from three candidates
(unchanged `rel_uri`; `_typst_converted/<rel_uri>` on a srcdir collision; `_typst_converted/<basename>`
on an outdir escape) — but that transition is already fully governed by locked decisions D-01, D-02
and D-05, so it needs no architectural question raised here and none is fabricated.
</assumption_delta_decision>

<flagged_assumptions>
The spec-less edge probe (`workflow.specless_probe_fallback`) returned BOTH requirement rows as
`unclassified` → `unresolved`. Per §A of the protocol an `unclassified` row is never auto-backstopped
and never silently dropped. Both are surfaced here as explicit flagged assumptions, **unverified**:

- **FA-01 (IMG-01, unresolved):** the probe could not classify IMG-01's edge category. The assumed
  edge set this phase actually covers is: converted-vs-source basename collision (covered, D-01/D-02),
  and no other. Converted-vs-converted collision is assumed already closed upstream by Sphinx's own
  `FilenameUniqDict.add_file()` numeric de-collision — measured in RESEARCH.md against installed
  Sphinx 9.1.0 (`sphinx/util/_files.py:36-38`) but **not** re-proved by any test this phase ships.
  Source-vs-source collision is assumed impossible by construction (two ordinary images at the same
  source-relative path are the same physical file). Neither assumption is verified by a gate here.

- **FA-02 (IMG-02, unresolved):** the probe could not classify IMG-02's edge category. The assumed
  edge set is: a `../`-prefixed `relpath` result of ARBITRARY depth (covered — `_escapes_outdir()`
  splits on `/` and tests segment membership, so depth is irrelevant), and a Windows cross-drive
  `ValueError` (covered by D-07's catch, but only ever exercised by a mocked `relpath`, never on a
  genuine two-drive filesystem — see `50-VALIDATION.md` § Manual-Only Verifications). A third shape,
  two DIFFERENT escaping images whose basenames coincide, is assumed not to occur and is **not**
  guarded: both would relocate to the same `_typst_converted/<basename>` key. Recorded as a residual,
  not closed.

**No-silent-drop equality (edge section):** probe-surfaced items = 2 (IMG-01, IMG-02); authored into
`must_haves` = 0; surfaced as flagged assumptions = 2. 2 = 0 + 2. ✓
</flagged_assumptions>

<prohibition_recall>
§B ran in-prompt (no compiled engine). Stage 1 over-produced 16 must-NOT candidates across IMG-01 and
IMG-02. Stage 2 dropped the routine-engineering candidates (restatements of the requirement itself,
of an already-locked D-decision, or of ordinary planning discipline) and kept the values/safety ones.

**Kept and authored** into `must_haves.prohibitions` (descriptor-less, in this plan and in 50-02 /
50-03), 3:
1. No gate's expected value is edited to match an observed result — covers both weakening the new
   D-10 gate and adjusting any of the three D-12-pinned strings.
2. The collision outcome must not become a function of document write order.
3. An ordinary, non-colliding, non-escaping project must not silently get a different emitted path or
   destination than v0.7.1 produced.

**Kept then canon-referral DROPPED**, 1:
4. "Must never write a copy destination outside `outdir`." Canon path-traversal / OWASP material —
   already owned by `<threat_model>` T-50-01 below, which carries the severity, the disposition and
   the mitigation. Breadcrumb recorded here rather than duplicated as a prohibition.

**No-silent-drop equality (prohibition section):** Stage-2 survivors = 4; authored into
`must_haves.prohibitions` = 3; canon-referral-dropped with breadcrumb = 1. 4 = 3 + 1. ✓
</prohibition_recall>

<artifacts_this_phase_produces>
Every symbol Phase 50 creates, across all three plans.

**Production symbols (`typsphinx/builder.py`, plan 50-02):**
- `RESERVED_IMAGE_NAMESPACE` — module-level `str` constant, value `"_typst_converted"`, placed
  immediately after `logger = logging.getLogger(__name__)`.
- No new function or method. `TypstBuilder._track_image()` is widened in place; `copy_image_files()`,
  `post_process_images()` and `_escapes_outdir()` are all unchanged (`_escapes_outdir()` gains a
  second, cross-domain call site only).

**New fixture directory (plan 50-01):** `tests/fixtures/converted_image_collision_render_gate/`
containing `conf.py` (defining the fixture-local `FakeImageConverter` `SphinxTransform` subclass and
its `setup(app)`), `index.rst`, `converted_source.rst`, `real_source.rst`, `images/chart.png` (40x24),
`_static/chart.svg`, `_static/converted_chart_stand_in.png` (16x64).

**New test module (plan 50-01):** `tests/test_converted_image_collision_render_gate.py` — module-level
`TYPST_AVAILABLE`, `PYPDF_AVAILABLE`, `_run_sphinx_build_typstpdf()`, fixtures
`converted_image_collision_render_gate_dir` and `temp_build_dir`; class
`TestConvertedImageCollisionRenderGate` with `test_typstpdf_build_succeeds_without_image_warnings`,
`test_content_documents_emit_distinct_image_paths`, `test_pdf_embeds_both_distinctly_sized_images`.

**New test functions appended to `tests/test_builder.py` (plan 50-03):**
`test_post_process_images_rehome_collision_relocates_silently`,
`test_post_process_images_rehome_escape_relocates_with_warning`,
`test_post_process_images_rehome_cross_drive_value_error_relocates`,
`test_copy_image_files_relocated_key_destination_stays_under_outdir`.

**New evidence artifacts under `.planning/phases/50-pr-131-image-path-defects/`:**
`50-RED-EVIDENCE.md` (50-01), `50-D11-BEFORE-MANIFEST.txt` / `50-D11-AFTER-MANIFEST.txt` /
`50-D11-EVIDENCE.md` (50-02), `50-BRANCH-EVIDENCE.md` (50-03). No file in this phase is named
`50-VERIFICATION.md` — that name is reserved by `/gsd-verify-work` and is overwritten wholesale.
</artifacts_this_phase_produces>

<reversibility>
  <decision rating="costly">The reserved namespace literal `_typst_converted` becomes part of the emitted output tree and of an emitted `image()` argument, so a later respelling changes user-visible output paths. Not one-way — no data loss, nothing published externally — and it is a LOCKED decision (D-02) that RESEARCH.md confirmed against two precedents, so no `checkpoint:decision` is inserted; re-litigating a locked decision is forbidden by this phase's constraints.</decision>
  <decision rating="reversible">Leaving the second-order reserved-namespace collision unguarded (RESEARCH.md Assumptions Log A1, the "Minimal" option). A later phase can add the hashed-fallback variant without changing any behavior of the non-colliding path. Flagged in 50-02 as an explicit recorded assumption.</decision>
  <decision rating="reversible">The exact D-06 warning wording. Pinned by a substring in one unit test; changing it is a one-line edit in two places.</decision>
  No `one-way` decision exists in this phase, so no `checkpoint:decision` task is emitted.
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party Sphinx extension → `TypstBuilder._track_image()` | An `ImageConverter`-family post-transform supplies an ABSOLUTE `node["uri"]`. The builder must treat its location as untrusted relative to its own assumptions — stock Sphinx always writes under `<doctreedir>/images/`, but nothing enforces that on a third-party transform. |
| `self.images` key → `copy_image_files()` filesystem write | The tracked key IS the destination path (`path.join(self.outdir, imguri)`), so any `..` segment surviving into the key becomes a write outside `outdir`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Tampering / Information Disclosure | `TypstBuilder._track_image()` → `copy_image_files()` `shutil.copy2(src, dest)` | high | mitigate | A `../`-prefixed `relpath` result escaping `doctreedir` must be detected by `_escapes_outdir(rel_uri)` and relocated under `RESERVED_IMAGE_NAMESPACE` BEFORE any destination is computed (D-05), so `path.join(self.outdir, imguri)` can never resolve outside `outdir`. Verified by `test_copy_image_files_relocated_key_destination_stays_under_outdir` and `test_post_process_images_rehome_escape_relocates_with_warning` (plan 50-03). ASVS L1 V5 / V12. |
| T-50-02 | Denial of Service | `path.relpath(resolved_uri, self.doctreedir)` on a Windows cross-drive pair | medium | mitigate | The `relpath()` call itself is wrapped in `try/except ValueError` — the guard must enclose the call, not follow it — and the exception routed into the same D-05 relocation (D-07). Verified by `test_post_process_images_rehome_cross_drive_value_error_relocates` (plan 50-03). |
| T-50-03 | Tampering | a real source directory at `<srcdir>/_typst_converted/...`, or two escaping images sharing a basename | low | accept | Compound-improbable second-order collision, left unguarded per RESEARCH.md Assumptions Log A1 (the "Minimal" option), matching this project's own `_template.typ` precedent. The hashed-fallback alternative (`sha1(resolved_uri)[:8]` prefix, a pure function of the source path so it stays write-order-independent) is recorded as the documented escape if this is ever measured to occur. |
| T-50-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero package-manager install tasks in this phase. `pypdf>=6.14,<7` and `pillow>=12.3,<13` are pre-existing dev dependencies (`pyproject.toml:46-47`), verified installed this session; the milestone invariant of zero new runtime dependencies is untouched. No `[ASSUMED]`/`[SUS]` package exists to gate. |

`security_block_on: high` — T-50-01 is the only `high` row and it is dispositioned `mitigate` with a
named test.
</threat_model>

<verification>
- `uv run pytest tests/test_converted_image_collision_render_gate.py -q` → `1 passed, 2 xfailed`.
- `uv run pytest tests/test_converted_image_collision_render_gate.py --runxfail -q` → non-zero exit,
  `2 failed, 1 passed`, with both failures being the IMG-01 symptom rather than an infrastructure error.
- `uv run pytest` (full suite) → no NEW failures relative to the pre-plan baseline. Record the
  baseline count first; the 45 known NixOS-environmental failures are unrelated to this plan.
- `uv run black --check .` and `uv run ruff check .` clean over the new files. (`ruff` ships as a
  generic-linux ELF that may not exec on this NixOS host — a known, filed toolchain limitation; if it
  cannot run locally, record that and take lint authority from CI, per Phase 45.2's precedent.)
- `git status --porcelain tests/test_absolute_image_render_gate.py tests/test_builder.py tests/fixtures/absolute_image_render_gate` → empty (D-12).
</verification>

<success_criteria>
1. A sibling fixture project exists at `tests/fixtures/converted_image_collision_render_gate/` that
   reproduces the IMG-01 collision as one master wrapper over two content documents (D-10), with two
   PNGs discriminated by pixel dimension (D-09).
2. `tests/test_converted_image_collision_render_gate.py` exists, its control test passes, and its two
   collision assertions are `xfail(strict=True)` against the unfixed builder.
3. `50-RED-EVIDENCE.md` records the verbatim `--runxfail` transcript, the measurement's own integrity
   check, and the observed single-element extracted-image size set (D-08).
4. `typsphinx/builder.py` is byte-unchanged by this plan.
5. All three D-12 fixed points are byte-unchanged.
</success_criteria>

<escalation_contract>
D-12 pins three assertions that must pass byte-unchanged: the emitted-path and copied-asset assertions
in `tests/test_absolute_image_render_gate.py`, and
`tests/test_builder.py::test_post_process_images_rehomes_absolute_uri`. **If any task in this phase
appears to require editing one of them, that is evidence D-01 was violated. STOP and escalate to the
owner. Do not adjust the expected string, do not rename the fixture, do not mark the test xfail.**
ROADMAP binding constraint #6 (no laundered gates) governs.
</escalation_contract>

<!-- planner-discipline-allow: xfail -->
<!-- planner-discipline-allow: could not rehome image URI -->
<!-- The two literals above are checked against subprocess stderr and against `git diff` output of a
     test file, never by grepping source the executor authors, so the comment-text self-invalidation
     hazard (#429) does not apply. -->

<output>
Create `.planning/phases/50-pr-131-image-path-defects/50-01-SUMMARY.md` when done.
</output>
