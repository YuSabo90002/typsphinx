---
phase: 51-two-layer-output-documentation
plan: 04
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - docs/source/user_guide/builders.rst
  - docs/source/user_guide/configuration.rst
  - docs/source/user_guide/templates.rst
autonomous: true
requirements: [DOC-14]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/source/user_guide/builders.rst no longer publishes a one-file-per-entry counting claim; its Output section states that each typst_documents entry writes a wrapper plus a content file, and that every document gets a content file whether or not it is named in typst_documents (51-RESEARCH.md Part A row 1)."
    - "Both `typst compile` walkthroughs in builders.rst, and its Development walkthrough's `open` line, name the wrapper filename their own configuration actually produces instead of a document-derived name (Part A rows 2, 4, 5; D-06)."
    - "builders.rst's Document Definitions paragraph states that the second tuple element names the WRAPPER only, and names the content files the shown configuration also emits (Part A row 3)."
    - "docs/source/user_guide/configuration.rst's typst_documents element-2 description matches `_escapes_outdir()`: a path is honoured relative to the output directory, and only a `..` segment, an absolute target, or a drive-qualified target is refused with a warning and a basename fallback (Part A row 6, OUT-01)."
    - "docs/source/user_guide/templates.rst's template-inspection walkthrough reads the wrapper, because content files carry no template application at all (Part A row 7)."
    - "builders.rst and configuration.rst each link to the new page rather than duplicating its contract (D-01)."
  artifacts:
    - path: docs/source/user_guide/builders.rst
      provides: "Corrected Output bullets, Manual Compilation, Document Definitions, Common Workflow, and a link to the new page"
      contains: "output_layout"
    - path: docs/source/user_guide/configuration.rst
      provides: "Corrected typst_documents element-2 contract and a link to the new page"
      contains: "output_layout"
    - path: docs/source/user_guide/templates.rst
      provides: "Corrected template-inspection walkthrough"
      contains: "output_layout"
  key_links:
    - from: "docs/source/user_guide/builders.rst"
      to: "docs/source/user_guide/output_layout.rst"
      via: ":doc:`output_layout` — same-directory bare form, in the Output section and in See Also (D-01: link, never duplicate)"
      pattern: "output_layout"
    - from: "docs/source/user_guide/configuration.rst"
      to: "docs/source/user_guide/output_layout.rst"
      via: ":doc:`output_layout` at the end of the corrected element-2 description and in See Also"
      pattern: "output_layout"
---

<objective>
Remove every falsified claim the two-layer split left in the three existing User Guide pages, and
point each of them at the new page instead of growing a second copy of its contract.

Purpose: D-04's repo-wide sweep, for the `docs/source/user_guide/` share of `51-RESEARCH.md` Part A —
rows 1, 2, 3, 4, 5 (`builders.rst`), row 6 (`configuration.rst`) and row 7 (`templates.rst`) — plus
D-01's requirement that these pages LINK to the new page rather than duplicate it.
Output: three corrected pages, each carrying a cross-reference to `output_layout`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/51-two-layer-output-documentation/51-CONTEXT.md
@.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md
@.planning/phases/51-two-layer-output-documentation/51-PATTERNS.md
@CLAUDE.md
</context>

<worktree_protocol>
This project runs executors in isolated git worktrees as its STANDING execution mode (`CLAUDE.md`,
owner decision 2026-07-20). Before ANY command in this plan:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then run EVERY subsequent command through `uv run`.

`docs/source/user_guide/output_layout.rst` is created by plan 51-01 in wave 1 and is present in this
worktree's base. The full docs HTML build proving every `:doc:` reference resolves is plan 51-06's
verification, after every wave-2 plan has merged.
</worktree_protocol>

<phase_scope_fence>
This phase changes **ZERO** lines under `typsphinx/`. It also does not touch
`docs/source/user_guide/output_layout.rst` (plans 51-01 / 51-03 / 51-06 own that file) or
`docs/source/user_guide/index.rst` (plan 51-01 owns it).
</phase_scope_fence>

<sweep_rows_owned_by_this_plan>
`51-RESEARCH.md` Part A is the closed, re-derived falsified-claim list for this phase. This plan owns
rows 1-7. Every row is FIXED; none is deferred.

| Row | Site | Verdict | Task |
|-----|------|---------|------|
| 1 | `docs/source/user_guide/builders.rst:39` — one-per-entry counting claim | FALSE | 1 |
| 2 | `docs/source/user_guide/builders.rst:61` — Manual Compilation `typst compile` | FALSE | 1 |
| 3 | `docs/source/user_guide/builders.rst:108-121` — Document Definitions element-2 paragraph | FALSE / INCOMPLETE | 1 |
| 4 | `docs/source/user_guide/builders.rst:156` — Development walkthrough `open` line | FALSE (pre-existing CONF-08 staleness) | 1 |
| 5 | `docs/source/user_guide/builders.rst:170` — Production Option 2 `typst compile` | FALSE | 1 |
| 6 | `docs/source/user_guide/configuration.rst:46-52` — element-2 path claim | FALSE | 2 |
| 7 | `docs/source/user_guide/templates.rst:453-462` — template-inspection walkthrough | FALSE | 3 |

Row 4 was flagged by the researcher as a possible scope call (a pre-existing CONF-08-era staleness,
not caused by the two-layer split). **Disposition: FIX.** D-04 scopes the sweep to every falsified
claim found repo-wide, not to split-caused ones only; the claim is measurably false as published, its
fix is one line, and this project has a recorded failure mode of closing an "anywhere under
`docs/source`" criterion against a convenient subset. Record the disposition in the SUMMARY.
</sweep_rows_owned_by_this_plan>

<tasks>

<task type="auto">
  <name>Task 1: builders.rst — the Output section, both compile walkthroughs, and Document Definitions</name>
  <files>docs/source/user_guide/builders.rst</files>
  <read_first>
    - `docs/source/user_guide/builders.rst` (full file — 190 lines; the four regions to change are the `Output` subsection under `typst Builder`, `Manual Compilation`, `Document Definitions`, and `Common Workflow`)
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part A rows 1-5 (verbatim current text, verdict, and the falsifying code citation for each), Part B §`_content_output_path` / `_wrapper_output_relpath` (the function-level model: which of the two files each name comes from), and Pitfall 2 (why a walkthrough with no `typst_documents` never resolves to a document-derived name since v0.7.1's CONF-08)
    - `docs/source/quickstart.rst` (the published `project = "My Project"` value these walkthroughs will name, so the two pages agree)
    - `.planning/phases/51-two-layer-output-documentation/51-PATTERNS.md` §"`:doc:` cross-reference form"
  </read_first>
  <action>
Four edits in `docs/source/user_guide/builders.rst`. Change nothing else on the page.

**1. The `Output` subsection under `typst Builder` (row 1).** Replace its middle bullet — the one
asserting a one-per-entry count — with two bullets that state the measured behaviour: each
`typst_documents` entry writes a wrapper at that entry's own target path plus a content file named
after the entry's source document; and every document in the project gets a content file, whether or
not it is named in `typst_documents`. Keep the existing first bullet. Replace the vague third bullet
about include files with a `:doc:`output_layout`` pointer sentence for the full contract and for which
file to compile.

**2. `Manual Compilation` (row 2).** The `typst compile` line names a document-derived file. Under this
walkthrough's implicit configuration — `typst_documents` unset — the wrapper is derived from `project`,
so per D-06 name the concrete file: with `project = "My Project"` (the value the published Quick Start
uses), the wrapper is `myproject.typ`, and the command compiles `build/typst/myproject.typ`. Add a
comment line inside the same `.. code-block:: bash` naming the `project` value the filename comes from,
and one sentence of prose above the block stating that the file to compile is the wrapper, not the
document-named file, with a `:doc:`output_layout`` reference.

**3. `Document Definitions` (row 3).** Rewrite the paragraph beneath the `typst_documents` code block.
It must state that the second tuple element names the WRAPPER file for that entry — and only the
wrapper — and that with the configuration shown directly above it the builders emit four `.typ` files,
not two: wrappers `main.typ` and `api-ref.typ`, plus content files `index.typ` and `api.typ` named
after the two source documents. Under the `typstpdf` builder only the wrappers become PDFs, so
`main.pdf` and `api-ref.pdf` and no others. Delete the trailing caveat sentence about the CLI
walkthroughs assuming a default identity configuration — it is describing a model that no longer
exists — and replace it with a `:doc:`output_layout`` pointer.

**4. `Common Workflow` -> `Development` (row 4).** The `open` line names a document-derived PDF. Under
this walkthrough's implicit configuration the PDF is named from `project`, so name it concretely the
same way task step 2 does, and add the same one-comment note inside the block.

**5. `Common Workflow` -> `Production`, Option 2 (row 5).** Same defect as step 2 and the same fix; keep
the `# Option 1` / `# Option 2` comments intact.

**6. `See Also`.** Add one `- :doc:`output_layout` - ...` entry, matching the existing list's dash and
description style, placed first so the layout contract is the first thing offered.
  </action>
  <acceptance_criteria>
    - `grep -c 'One file per document' docs/source/user_guide/builders.rst` returns 0.
    - `grep -c 'and it governs both' docs/source/user_guide/builders.rst` returns 0.
    - `grep -c 'build/typst/index.typ' docs/source/user_guide/builders.rst` returns 0.
    - `grep -c 'build/pdf/index.pdf' docs/source/user_guide/builders.rst` returns 0.
    - `grep -c 'build/typst/myproject.typ' docs/source/user_guide/builders.rst` returns 2 — the Manual Compilation walkthrough and Production Option 2.
    - `grep -c 'build/pdf/myproject.pdf' docs/source/user_guide/builders.rst` returns 1.
    - `grep -c 'output_layout' docs/source/user_guide/builders.rst` returns at least 3 — the Output section, the compile walkthroughs' prose, and See Also.
    - `grep -c 'wrapper' docs/source/user_guide/builders.rst` returns at least 4.
    - `awk '/^Document Definitions/,/^Builder-Specific Options/' docs/source/user_guide/builders.rst | grep -c 'index.typ'` returns at least 1, and the same slice contains `api.typ`, `main.typ` and `api-ref.typ`.
    - `git diff --name-only HEAD -- typsphinx/ | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_output_layout_docs_gate.py tests/test_docs_contract_claims_gate.py -q && ! grep -qE 'build/(typst/index\.typ|pdf/index\.pdf)|One file per document|and it governs both' docs/source/user_guide/builders.rst</automated>
  </verify>
  <done>builders.rst states the two-file-per-entry output shape, names the wrapper filename in all three CLI walkthroughs, describes element 2 as the wrapper's name with the content files named, and links to the new page in three places.</done>
</task>

<task type="auto">
  <name>Task 2: configuration.rst — the typst_documents element-2 contract (the OUT-01 reversal)</name>
  <files>docs/source/user_guide/configuration.rst</files>
  <read_first>
    - `docs/source/user_guide/configuration.rst` lines 23-80 (the `Typst Documents` subsection and the five-element tuple contract; element 2 is the falsified one) and lines 400-412 (the `See Also` list)
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part A row 6 (verbatim current text and the falsifying code citation) and Part B §`_resolve_target_stem` / §`_escapes_outdir` (the exact decision tree the corrected text must match, including that a bare separator is legitimate and only `..`, absolute and drive-qualified trip the guard)
    - `.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-08-SUMMARY.md` around line 212 (the verbatim record of this claim being handed forward to DOC-14)
    - `typsphinx/builder.py` lines 36-114 and 296-420 (read only — the predicates and the resolution order the prose must match)
  </read_first>
  <action>
One edit region in `docs/source/user_guide/configuration.rst`, plus a `See Also` entry. Do not touch
elements 1, 3, 4 or 5 of the tuple contract, and do not touch the declarative paragraph at lines 28-35
about the unset-`typst_documents` derivation — that paragraph is still true and is the register the
new page copies.

Rewrite tuple element 2. Keep the parts that are still true: a literal trailing `.typ` is stripped if
present and nothing else is, so a stem containing a period is preserved intact. Replace the sentence
denying path support with the measured contract: a path is honoured relative to the output directory,
so a target of `"manuals/guide.typ"` writes the wrapper to `manuals/guide.typ` under the output
directory. Add the refusal rule: a target with a `..` path segment, an absolute target, or a
drive-qualified target is refused with a build warning and written under its basename at the output
root instead; the drive-qualified test is a pure string-shape check, so a Windows-shaped target is
refused identically on every platform. Also state what element 2 does NOT govern: it names the entry's
wrapper file only; the entry's source document additionally gets its own content file, named after the
document. Rename the element's label from a filename-stem framing to one that says it names the
wrapper's output target, and close with a `:doc:`output_layout`` reference for the worked examples and
the refusal rules.

Add one `- :doc:`output_layout` - ...` entry to the page's `See Also` list, matching the existing
entries' dash-and-description style.

This is a deliberate reversal of what this page published for v0.7.1: OUT-01 (Phase 47) reversed Phase
44's D-05/D-06/D-07. Do not treat the existing sentence as a specification to preserve.
  </action>
  <acceptance_criteria>
    - `grep -c 'component is not supported' docs/source/user_guide/configuration.rst` returns 0.
    - `grep -c 'manuals/guide.typ' docs/source/user_guide/configuration.rst` returns at least 1.
    - `grep -c 'output_layout' docs/source/user_guide/configuration.rst` returns 2 — the element-2 pointer and the See Also entry.
    - `grep -c 'wrapper' docs/source/user_guide/configuration.rst` returns at least 2.
    - `awk '/^Typst Documents/,/^Template Configuration/' docs/source/user_guide/configuration.rst | grep -c 'v1.2-manual'` returns 1 — the still-true period-preservation example is retained, not collaterally deleted.
    - `sed -n '28,35p' docs/source/user_guide/configuration.rst | grep -c 'make_filename_from_project'` returns 1 — the still-true unset-derivation paragraph is byte-unchanged.
    - `git diff --name-only HEAD -- typsphinx/ | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_docs_contract_claims_gate.py tests/test_builder_output_stem.py -q && ! grep -q 'component is not supported' docs/source/user_guide/configuration.rst</automated>
  </verify>
  <done>The `typst_documents` element-2 description matches `_escapes_outdir()` exactly — path honoured, three shapes refused with a basename fallback — names the wrapper/content distinction, and links to the new page; the still-true neighbouring text is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: templates.rst — inspect the wrapper, because content files carry no template</name>
  <files>docs/source/user_guide/templates.rst</files>
  <read_first>
    - `docs/source/user_guide/templates.rst` lines 446-471 (the `Debugging Templates` section, its bash block, and `See Also`)
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part A row 7 (the falsifying citation: template application belongs exclusively to the wrapper) and Part C build 5 (this project's own docs build, where the wrapper carries `#show: project.with(...)` and the content file does not)
    - `typsphinx/writer.py` lines 215-320 (read only — `translate()`'s master/included branch and `render_wrapper()`, the source of the "no template in a content file" fact)
  </read_first>
  <action>
One edit region in `docs/source/user_guide/templates.rst`, inside `Debugging Templates`.

The walkthrough's inspection command reads a document-derived file, which since Phase 47 carries no
template application at all — so the step named "Check the generated template usage" currently
inspects the one file that can never show it. Rewrite the `cat` line to read the wrapper. Per D-06 name
a concrete file rather than a generic instruction: with `typst_documents` unset and
`project = "My Project"`, the wrapper is `myproject.typ`. Keep the `sphinx-build -b typst` line and the
`# Generate .typ files for inspection` comment unchanged; update the second comment so it states that
the template application lives in the wrapper.

Add one sentence of prose immediately below the block: the document-named file holds the body only and
carries no template application, so a template problem is always visible in the wrapper — with a
`:doc:`output_layout`` reference. Add a matching `- :doc:`output_layout` - ...` entry to the page's
`See Also` list.
  </action>
  <acceptance_criteria>
    - `grep -c 'cat build/typst/index.typ' docs/source/user_guide/templates.rst` returns 0.
    - `grep -c 'cat build/typst/myproject.typ' docs/source/user_guide/templates.rst` returns 1.
    - `grep -c 'output_layout' docs/source/user_guide/templates.rst` returns 2.
    - `grep -c 'Generate .typ files for inspection' docs/source/user_guide/templates.rst` returns 1 — the untouched comment is preserved.
    - `awk '/^Debugging Templates/,/^See Also/' docs/source/user_guide/templates.rst | grep -c 'wrapper'` returns at least 2.
    - `git diff --name-only HEAD -- typsphinx/ | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_docs_contract_claims_gate.py -q && ! grep -q 'cat build/typst/index.typ' docs/source/user_guide/templates.rst</automated>
  </verify>
  <done>The template-debugging walkthrough inspects the wrapper, states why the document-named file cannot show a template, and links to the new page.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol below is NET-NEW in Phase 51. Nothing here exists at HEAD, so drift verification must not
expect a prior definition. **This plan creates none of them** — it only references
`docs/source/user_guide/output_layout.rst` through `:doc:` cross-references.

**Documentation**
- `docs/source/user_guide/output_layout.rst` (new page, title `Output Layout`) — plans 51-01 / 51-03 / 51-06
- New subsection `Migrating from 0.7.x to 0.8.0` in `docs/source/changelog.rst` — plan 51-02

**Tests**
- `tests/test_output_layout_docs_gate.py` — classes `TestOutputLayoutBuildFileSets` and
  `TestPublishedOutputLayoutTextMatchesBuild`; helper `_run_sphinx_build`

**Test fixtures (new directories, each `conf.py` + `index.rst`)**
- `tests/fixtures/output_layout_bare_target_gate/`
- `tests/fixtures/output_layout_explicit_path_gate/`
- `tests/fixtures/output_layout_refused_parent_gate/`
- `tests/fixtures/output_layout_refused_absolute_gate/`
- `tests/fixtures/output_layout_refused_drive_gate/`

**Reused, NOT created** — `tests/fixtures/bld03_self_collision_gate/` and
`tests/fixtures/state_guard_three_master_gate/` already exist and must not be modified.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; blocking threshold `high`. This plan edits three reStructuredText files and changes zero
lines under `typsphinx/`. The register is short because the honest surface is small — it is not padded.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published documentation (Read the Docs, PyPI-linked) | Configuration-reference prose is copied by readers into their own `conf.py`. A claim that misstates a security control crosses this boundary unfiltered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-03 | Tampering | `docs/source/user_guide/configuration.rst` element-2 contract | medium | mitigate | The corrected text must state the OUT-02 escape guard, not only the OUT-01 relaxation. Task 2's action requires all three still-refused shapes (`..` segment, absolute, drive-qualified) to be named alongside the newly-honoured path behaviour, so a reader cannot conclude that any path value is now written anywhere. Asserted by the acceptance criteria and by the wrapper/refusal wording checks. |
| T-51-02 | Information Disclosure | corrected CLI walkthroughs | low | mitigate | Every published path stays relative to a build directory (`build/typst/myproject.typ`, `build/pdf/myproject.pdf`). No absolute local path or home directory is published. |
| T-51-SC | Tampering | package-manager installs | — | accept | Not applicable: this plan installs no npm/pip/cargo package and adds no dependency. No Package Legitimacy Audit is owed. |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_docs_contract_claims_gate.py tests/test_builder_output_stem.py tests/test_output_layout_docs_gate.py -q` — green.
- All six negative greps in the acceptance criteria return 0.
- `git diff --name-only HEAD -- typsphinx/` is empty.
</verification>

<success_criteria>
- Every one of `51-RESEARCH.md` Part A rows 1-7 is fixed; none is deferred.
- `builders.rst`, `configuration.rst` and `templates.rst` each carry at least one `:doc:`output_layout``
  cross-reference and no duplicated copy of the page's contract.
- The corrected `configuration.rst` element-2 text names both the OUT-01 relaxation and the three
  shapes OUT-02 still refuses.
- No line under `typsphinx/` changed.
</success_criteria>

<output>
Create `.planning/phases/51-two-layer-output-documentation/51-04-SUMMARY.md` when done.
</output>
