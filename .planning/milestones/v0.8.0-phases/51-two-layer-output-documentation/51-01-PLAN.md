---
phase: 51-two-layer-output-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/user_guide/output_layout.rst
  - docs/source/user_guide/index.rst
  - tests/test_output_layout_docs_gate.py
  - tests/fixtures/output_layout_bare_target_gate/conf.py
  - tests/fixtures/output_layout_bare_target_gate/index.rst
  - tests/fixtures/output_layout_explicit_path_gate/conf.py
  - tests/fixtures/output_layout_explicit_path_gate/index.rst
autonomous: true
requirements: [DOC-14]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader of docs/source/user_guide/output_layout.rst learns that one typst_documents entry writes TWO .typ files — a wrapper at the entry's target path and a docname-named content file — and that the wrapper is the file to compile (D-01, SC#1)."
    - "The page states, as plain prose inside the which-file-to-compile section and NOT as a note or warning admonition, that compiling a content file standalone succeeds and yields only that document's own body with no children, no error and no warning (D-08, SC#1)."
    - "The page carries the bare-target worked example (\"index\", \"manual\") naming manual.typ and index.typ, and the explicit-path worked example (\"index\", \"manuals/guide.typ\") naming manuals/guide.typ and index.typ, both taken verbatim from 51-RESEARCH.md Part C builds 1 and 2 (SC#2)."
    - "The new page is reachable from docs/source/user_guide/index.rst through BOTH its toctree AND its hand-written Main Topics definition list — two separate, unlinked lists (51-RESEARCH.md Part E)."
    - "A permanent pytest module tests/test_output_layout_docs_gate.py builds both worked-example fixtures with -b typst through a real sys.executable -m sphinx subprocess, asserts the exact emitted .typ file set, and separately reads the published page from disk and asserts it names those same filenames. It carries no typst-py import and never skips (D-10/D-11/D-12, SC#3)."
  artifacts:
    - path: docs/source/user_guide/output_layout.rst
      provides: "The two-layer output contract page — wrapper/content split, which file to compile, standalone-content behaviour, and the bare/explicit-path worked examples"
      contains: "Output Layout"
    - path: docs/source/user_guide/index.rst
      provides: "Toctree entry and Main Topics entry for the new page"
      contains: "output_layout"
    - path: tests/test_output_layout_docs_gate.py
      provides: "The SC#3 prose-vs-build gate — real -b typst builds plus a never-skipping published-text class"
      contains: "TestPublishedOutputLayoutTextMatchesBuild"
    - path: tests/fixtures/output_layout_bare_target_gate/conf.py
      provides: "51-RESEARCH.md Part C build 1's configuration, verbatim"
      contains: "typst_documents"
    - path: tests/fixtures/output_layout_explicit_path_gate/conf.py
      provides: "51-RESEARCH.md Part C build 2's configuration, verbatim"
      contains: "manuals/guide.typ"
  key_links:
    - from: "tests/test_output_layout_docs_gate.py"
      to: "docs/source/user_guide/output_layout.rst"
      via: "Path(...).read_text() literal-substring assertions in TestPublishedOutputLayoutTextMatchesBuild — the single wire that makes a published claim falsifiable"
      pattern: "output_layout.rst"
    - from: "tests/test_output_layout_docs_gate.py"
      to: "tests/fixtures/output_layout_bare_target_gate/"
      via: "_run_sphinx_build(fixture, tmp_path, \"typst\") then asserting (build/'manual.typ').exists() and (build/'index.typ').exists()"
      pattern: "_run_sphinx_build"
    - from: "docs/source/user_guide/index.rst"
      to: "docs/source/user_guide/output_layout.rst"
      via: "toctree entry AND a separate :doc:`output_layout` row in the hand-written Main Topics definition list"
      pattern: "output_layout"
  prohibitions:
    - statement: "MUST NOT publish any statement about the emitted file set, about which file to compile, or about a target-as-path outcome that a real `sphinx-build -b typst` of the configuration shown on the same page does not reproduce. A documentation page that is reassuring but false is the exact failure DOC-14 exists to end."
      category: transparency
---

<objective>
Create the two-layer output contract page and prove its first two claims against real builds, end to
end: fixture -> real `sphinx-build -b typst` subprocess -> emitted `.typ` file set -> the page's own
prose -> the page reachable from the User Guide index -> a permanent gate binding the prose back to
the build.

This is the phase's tracer. It touches every layer the phase will modify (a new fixture, a new gate
module, the new page, the User Guide index) on ONE worked example first, so that if the shape is
wrong it is wrong after one commit instead of after five.

Purpose: SC#1 (name the two layers, say which one to compile, state the standalone-content behaviour
as intended behaviour) and the first half of SC#2 (bare target at the output root, explicit path
where the user asked), each carried by SC#3's machine-checked half.
Output: `docs/source/user_guide/output_layout.rst`, its two index entries, the permanent gate module,
and two fixture projects that are literal copies of measured builds.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-two-layer-output-documentation/51-CONTEXT.md
@.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md
@.planning/phases/51-two-layer-output-documentation/51-PATTERNS.md
@.planning/phases/51-two-layer-output-documentation/51-VALIDATION.md
@CLAUDE.md
</context>

<worktree_protocol>
This project runs executors in isolated git worktrees as its STANDING execution mode (`CLAUDE.md`,
owner decision 2026-07-20). Before ANY command in this plan:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then run EVERY subsequent command through `uv run`. A bare `pytest` silently tests the MAIN checkout's
editable install, not this worktree. Sphinx subprocess builds inside test modules use
`sys.executable -m sphinx`, never a console-script shim (NixOS PATH-shadowing hazard).

`typst.compile()` does NOT work in this sandbox (`FileNotFoundError`, measured 2026-08-14). Do not
attempt a PDF-level check anywhere in this phase and do not read a `FileNotFoundError` from
`typst.compile()` as a behaviour change (`51-RESEARCH.md` Pitfall 3).
</worktree_protocol>

<phase_scope_fence>
This phase changes **ZERO** lines under `typsphinx/`. Every claim it publishes is a claim about code
that already shipped in Phases 47-50. If a task surfaces what looks like a code defect, record it as a
deferred item in the plan's SUMMARY; do not fold a code fix into this phase.
</phase_scope_fence>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "which file do I compile" — the bare-target path only</name>
  <precondition>The Phase 47/49 two-layer split is present at HEAD: `grep -c '_content_output_path\|_wrapper_output_relpath' typsphinx/builder.py` returns a non-zero count. Halt and report if it does not — this phase documents behaviour that must already have landed.</precondition>
  <files>tests/fixtures/output_layout_bare_target_gate/conf.py, tests/fixtures/output_layout_bare_target_gate/index.rst, tests/test_output_layout_docs_gate.py, docs/source/user_guide/output_layout.rst, docs/source/user_guide/index.rst</files>
  <read_first>
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part B (the `_resolve_target_stem` / `_content_output_path` / `_wrapper_output_relpath` model and the verbatim wrapper-report log line), Part C build 1, Part D (the gate precedent and its skip-guard hazard), Part E (the exact current content of the User Guide index)
    - `.planning/phases/51-two-layer-output-documentation/51-PATTERNS.md` §"docs/source/user_guide/output_layout.rst" (heading adornment scheme, config-example-then-consequence shape, the plain-prose register at `configuration.rst:28-35` that D-08 requires) and §"tests/test_output_layout_docs_gate.py"
    - `tests/test_quickstart_docs_gate.py` (full file — the two-class shape, `_run_sphinx_build()`, and the `TYPST_AVAILABLE` guard that must NOT be copied)
    - `tests/fixtures/quickstart_docs_gate/conf.py` and `tests/fixtures/quickstart_docs_gate/index.rst` (the fixture header-comment convention and a minimal source file)
    - `docs/source/user_guide/configuration.rst` lines 1-52 (heading adornments; the declarative no-admonition register at lines 28-35)
    - `docs/source/user_guide/index.rst` (both lists that need the new page)
    - `typsphinx/builder.py` lines 755-775 (the `typst: wrote N wrapper file(s) -- compile these: ...` message) and lines 960-1011 (`_content_output_path` / `_wrapper_output_relpath`)
  </read_first>
  <action>
Wire one path through every layer this phase touches, for the bare-target configuration only. No
other target shape, no other fixture, no batching.

**Fixture.** Create `tests/fixtures/output_layout_bare_target_gate/` with `conf.py` and `index.rst`.
`conf.py` carries the fixture header-comment convention from the analog: a comment stating its ONE
job (it is the built proof for the bare-target worked example on
`docs/source/user_guide/output_layout.rst`), and a do-not-merge note naming
`tests/fixtures/quickstart_docs_gate/` and `tests/fixtures/default_typst_documents_gate/` as
different fixtures with different jobs. Config values: `project`, `author`, `release`, `copyright`,
`extensions = ["typsphinx"]`, and the single load-bearing line
`typst_documents = [("index", "manual", "Title", "Author", "typst")]` — a literal copy of
`51-RESEARCH.md` Part C build 1. `index.rst` is a minimal titled document with one short paragraph
and NO toctree (Part C build 1 had none).

**Gate module.** Create `tests/test_output_layout_docs_gate.py` as a NEW module. Do not add methods to
`tests/test_docs_contract_claims_gate.py` — that module's docstring fences its own subject and
`51-RESEARCH.md` Part D records the fence. Module docstring states: DOC-14, SC#3, the subject
(published two-layer claims versus the emitted `.typ` file set of a real build), and that this module
deliberately carries no `typst-py` import guard because the precedent's guard is an import check while
the sandbox's failure is a compile failure, so an inherited guard would run and then fail (D-12,
`51-RESEARCH.md` Part D). Reproduce `_run_sphinx_build()` independently in this module per the suite's
own per-module-copy convention, invoking `sys.executable -m sphinx` with a list argument. Module
constants: `REPO_ROOT`, `FIXTURES_DIR`, `BARE_TARGET_FIXTURE_DIR`, `OUTPUT_LAYOUT_RST_PATH`.

Two classes, matching the precedent's split. `TestOutputLayoutBuildFileSets` runs the real `-b typst`
builds; give it one method now, `test_bare_target_emits_wrapper_and_content`, asserting the build
returns 0 and that `manual.typ`, `index.typ` and `_template.typ` all exist under the build dir, and
that `index.typ`'s text carries no template application while `manual.typ`'s does — the measured
wrapper/content discriminator from `51-RESEARCH.md` Part C build 5. Neither class carries
`@pytest.mark.skipif`, `@pytest.mark.slow`, or an `import typst`.
`TestPublishedOutputLayoutTextMatchesBuild` reads `OUTPUT_LAYOUT_RST_PATH` with
`Path(...).read_text(encoding="utf-8")`; give it one method now,
`test_page_names_the_bare_target_file_set`, asserting the page's text contains the literals `manual.typ`
and `index.typ`.

**Page.** Create `docs/source/user_guide/output_layout.rst`. Title `Output Layout` with the `=` / `-` /
`~` three-tier adornment scheme all three sibling User Guide pages use. This task writes exactly two
sections, both required by SC#1:

`Wrapper and Content Files` — states that a build writes two kinds of `.typ` file: one **wrapper** per
`typst_documents` entry, written at that entry's own target path, carrying the template application and
one `#include()` of its content file; and one **content** file per docname, named after the docname,
carrying the document body and no template. State that every docname gets a content file
unconditionally, whether or not it appears in `typst_documents`. Use the word "wrapper" — it is already
user-visible in the builder's own report line.

`Which File to Compile` — states that the wrapper is the file to compile, and that `-b typst` users can
read the wrapper names off the builder's own report line, quoting the literal `compile these:` fragment.
Then, as PLAIN PROSE in the same declarative register `configuration.rst:28-35` uses — NOT a `.. note::`,
NOT a `.. warning::`, and not framed as a limitation (D-08) — state the standalone-content behaviour:
compiling a content file directly succeeds and produces only that document's own body; the documents it
would have pulled in are absent, with no error and no warning, because nothing published the include
set that its guarded includes read. Cite nothing; state it as normal, well-defined behaviour.

Follow the config-example-then-consequence shape from `51-PATTERNS.md`: a `.. code-block:: python`
carrying the fixture's own `typst_documents` line, immediately followed by prose naming BOTH emitted
files by name (`manual.typ` the wrapper at the output root, `index.typ` the content file). Do not
render a `find`-style directory tree — no sibling page has one.

**Index.** Add `output_layout` to `docs/source/user_guide/index.rst`'s toctree AND add a matching
`:doc:`output_layout`` entry with a one-line description to its hand-written `Main Topics` definition
list. These are two separate lists and nothing keeps them in sync.
  </action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_output_layout_docs_gate.py -q` exits 0 and reports 2 passed, 0 skipped.
    - `uv run python -m pytest tests/test_output_layout_docs_gate.py -q -rs 2>&1 | grep -c SKIPPED` returns 0 — the module never skips (D-12).
    - `grep -c 'import typst' tests/test_output_layout_docs_gate.py` returns 0.
    - `grep -c 'typstpdf' tests/test_output_layout_docs_gate.py` returns 0 — the gate builds with `-b typst` only (D-12).
    - `grep -c 'sys.executable' tests/test_output_layout_docs_gate.py` returns at least 1, and `grep -c 'shell=True' tests/test_output_layout_docs_gate.py` returns 0.
    - `grep -c 'manual.typ' docs/source/user_guide/output_layout.rst` returns at least 1 and `grep -c 'index.typ' docs/source/user_guide/output_layout.rst` returns at least 1.
    - `grep -c 'compile these:' docs/source/user_guide/output_layout.rst` returns at least 1.
    - `grep -v '^ *\.\. ' docs/source/user_guide/output_layout.rst | grep -c 'wrapper' ` returns at least 3 — the user-facing vocabulary is present in prose, not only in a directive.
    - `grep -c '.. note::' docs/source/user_guide/output_layout.rst` returns 0 and `grep -c '.. warning::' docs/source/user_guide/output_layout.rst` returns 0 — D-08 forbids an admonition anywhere on this page as written by this task.
    - `grep -c 'output_layout' docs/source/user_guide/index.rst` returns exactly 2 — one toctree entry and one Main Topics entry.
    - `grep -c 'typst_documents = \[("index", "manual"' tests/fixtures/output_layout_bare_target_gate/conf.py` returns 1.
    - `git -C . diff --name-only HEAD -- typsphinx/ | wc -l` returns 0 — no line under `typsphinx/` changed.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_output_layout_docs_gate.py -q</automated>
  </verify>
  <done>A real `-b typst` build of the bare-target fixture emits `manual.typ` + `index.typ` + `_template.typ`; the new page names both files and says to compile the wrapper; the page is reachable from both User Guide index lists; and the gate binds the two together and never skips.</done>
</task>

<task type="auto">
  <name>Task 2: Expand to an explicit path target — the wrapper lands where the user asked</name>
  <files>tests/fixtures/output_layout_explicit_path_gate/conf.py, tests/fixtures/output_layout_explicit_path_gate/index.rst, tests/test_output_layout_docs_gate.py, docs/source/user_guide/output_layout.rst</files>
  <read_first>
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part C build 2 (the measured file set for `"manuals/guide.typ"`) and Part B §`_escapes_outdir` (why a plain path-bearing target is accepted)
    - `tests/test_output_layout_docs_gate.py` (as written by Task 1 — the constants and class names to extend)
    - `tests/fixtures/output_layout_bare_target_gate/conf.py` (the header-comment convention to mirror)
    - `docs/source/user_guide/output_layout.rst` (as written by Task 1 — the section to extend)
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` §"Don't Hand-Roll" (never reimplement the target-resolution decision tree inside the test)
  </read_first>
  <action>
Add the second worked example — an explicit path in the target — through the same three layers.

**Fixture.** Create `tests/fixtures/output_layout_explicit_path_gate/` with `conf.py` and `index.rst`,
same header-comment convention as the bare-target fixture, its stated ONE job being the explicit-path
worked example. Its single load-bearing config line is
`typst_documents = [("index", "manuals/guide.typ", "Title", "Author", "typst")]` — a literal copy of
`51-RESEARCH.md` Part C build 2. `index.rst` minimal and toctree-free, as in build 2.

**Gate.** Add `EXPLICIT_PATH_FIXTURE_DIR` to the module constants and one method
`test_explicit_path_target_writes_the_wrapper_under_its_path` to `TestOutputLayoutBuildFileSets`: the
build returns 0, `manuals/guide.typ` exists under the build dir, and `index.typ` exists at the build
dir ROOT — the content file's location is derived from the docname alone and is unaffected by where the
wrapper landed (Part C build 2's measured outcome). Assert file existence against the real build
output; do not reimplement the target-resolution logic in the test. Add
`test_page_names_the_explicit_path_file_set` to `TestPublishedOutputLayoutTextMatchesBuild` asserting
the page's text contains the literal `manuals/guide.typ`.

**Page.** Add a new section `Where the Wrapper Is Written` after `Which File to Compile`, with two
subsections at the `~` level. `A bare target` restates the Task 1 example as the output-root case: the
wrapper is written at the output root under the target's own stem. `A path in the target` carries a
`.. code-block:: python` with the explicit-path fixture's own `typst_documents` line, followed by prose
naming both emitted paths: the wrapper at `manuals/guide.typ` relative to the output directory, and the
content file still at `index.typ` at the output root. State explicitly that a path component in a
target is accepted and honoured relative to the output directory. Keep the two subsections in the
config-example-then-consequence shape and do not introduce an admonition.
  </action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_output_layout_docs_gate.py -q` exits 0 and reports 4 passed, 0 skipped.
    - `grep -c 'manuals/guide.typ' docs/source/user_guide/output_layout.rst` returns at least 2 — the config block and the prose naming the emitted wrapper path.
    - `grep -c 'manuals/guide.typ' tests/fixtures/output_layout_explicit_path_gate/conf.py` returns 1.
    - `grep -c 'Where the Wrapper Is Written' docs/source/user_guide/output_layout.rst` returns 1.
    - `grep -c '_resolve_target_stem\|_escapes_outdir\|posixpath' tests/test_output_layout_docs_gate.py` returns 0 — the gate asserts against the real emitted file set, never against a reimplementation of the builder's decision tree.
    - `grep -c '.. note::' docs/source/user_guide/output_layout.rst` returns 0 and `grep -c '.. warning::' docs/source/user_guide/output_layout.rst` returns 0.
    - `git -C . diff --name-only HEAD -- typsphinx/ | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_output_layout_docs_gate.py -q</automated>
  </verify>
  <done>A real `-b typst` build of the explicit-path fixture writes `manuals/guide.typ` and leaves `index.typ` at the output root; the page publishes exactly that outcome; the gate asserts both halves and still never skips.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol below is NET-NEW in Phase 51. Nothing here exists at HEAD, so drift verification must not
expect a prior definition.

**Documentation**
- `docs/source/user_guide/output_layout.rst` (new page, title `Output Layout`)
- New subsection `Migrating from 0.7.x to 0.8.0` in `docs/source/changelog.rst` (plan 51-02)

**Tests**
- `tests/test_output_layout_docs_gate.py` (new module)
  - class `TestOutputLayoutBuildFileSets`
  - class `TestPublishedOutputLayoutTextMatchesBuild`
  - helper `_run_sphinx_build`
  - constants `REPO_ROOT`, `FIXTURES_DIR`, `OUTPUT_LAYOUT_RST_PATH`, `BARE_TARGET_FIXTURE_DIR`,
    `EXPLICIT_PATH_FIXTURE_DIR`, `REFUSED_PARENT_FIXTURE_DIR`, `REFUSED_ABSOLUTE_FIXTURE_DIR`,
    `REFUSED_DRIVE_FIXTURE_DIR`, `SELF_COLLISION_FIXTURE_DIR`, `THREE_MASTER_FIXTURE_DIR`,
    `BUILDERS_RST_PATH`, `TEMPLATES_RST_PATH`
  - methods `test_bare_target_emits_wrapper_and_content`,
    `test_explicit_path_target_writes_the_wrapper_under_its_path`,
    `test_refused_parent_target_falls_back_to_basename`,
    `test_refused_absolute_target_falls_back_to_basename`,
    `test_refused_drive_qualified_target_falls_back_to_basename`,
    `test_self_collision_target_aborts_the_build_with_no_typ_files`,
    `test_three_master_project_emits_ten_typ_files`,
    `test_page_names_the_bare_target_file_set`,
    `test_page_names_the_explicit_path_file_set`,
    `test_page_quotes_the_verbatim_refusal_warning`,
    `test_page_states_the_collision_abort`,
    `test_page_states_the_shared_child_composition`,
    `test_helper_derived_wrapper_stem_matches_the_published_walkthroughs`

**Test fixtures (new directories, each `conf.py` + `index.rst` unless noted)**
- `tests/fixtures/output_layout_bare_target_gate/`
- `tests/fixtures/output_layout_explicit_path_gate/`
- `tests/fixtures/output_layout_refused_parent_gate/`
- `tests/fixtures/output_layout_refused_absolute_gate/`
- `tests/fixtures/output_layout_refused_drive_gate/`

**Reused, NOT created** — `tests/fixtures/bld03_self_collision_gate/` and
`tests/fixtures/state_guard_three_master_gate/` already exist and must not be modified.
</artifacts_this_phase_produces>

<assumption_delta_decision>
The `assumption-delta` detector returned `detected: true` with 2 signals. Both are false positives.

- Signal 1 — `pluralization` on "fallback", sourced from ROADMAP SC#2's phrase "refused with a warning
  and a safe fallback" (the target-refusal behaviour).
- Signal 2 — `pluralization` on "alongside", sourced from SC#2's "stated in old→new file names,
  alongside v0.7.1's own rename" (two renames sitting next to each other on a page).

Neither is a singular→plural identity-model transition. This phase introduces no second platform,
tenant, auth method, or source of truth, and defines no identity model at all.

- **Noun now primary:** none — this phase adds no new representation.
- **Decision:** `no-change`.
- **Rationale:** both detector signals are prose collocations in a documentation-only phase; no core
  assumption loses a monopoly here.
</assumption_delta_decision>

<flagged_assumptions>
The spec-less probe fallback fired for both sections (no SPEC exists for this phase). The deterministic
edge probe over DOC-14 returned exactly one row, and it is `unresolved`. It is surfaced here as an
explicit flagged assumption rather than auto-`backstop`ped or dropped.

| # | Requirement | Probe category | Probe question | Status | Assumption as flagged |
|---|---|---|---|---|---|
| 1 | DOC-14 | concurrency | "If interrupted or run in parallel, what is guaranteed?" | **unresolved** | The `concurrency` classification is a weak fit for a documentation requirement: DOC-14's deliverable is published prose plus one pytest gate, and neither has an interruption or parallelism semantic to guarantee. The nearest real-world reading — that the new gate must be safe under `pytest-xdist`-style parallel execution — is satisfied incidentally because every gate build writes into pytest's own per-test `tmp_path`, never into a shared directory. That is an observation, not a derived predicate, so no predicate is authored into `must_haves`. Flagged, not resolved. |

No-silent-drop equality holds: 1 probe-surfaced item == 0 authored into `must_haves` + 1 surfaced as a
flagged assumption.

**Prohibition recall (§B, in-prompt, two-stage).** Stage 1 over-produced ~11 must-NOT candidates for
DOC-14. Stage 2 dropped the routine-engineering ones (must not break the docs build; must not hardcode
a filename the helper computes; must not paraphrase a warning string; must not let the gate skip) —
all of these are owned by this plan's own acceptance criteria or by ordinary review. Two bespoke
transparency prohibitions survived and are authored descriptor-less: P1 in this plan's
`must_haves.prohibitions` (do not publish a claim a real build does not reproduce) and P2 in plan
51-04's (do not narrow the sweep without recording a reason). Canon-referral breadcrumb: path
traversal in a target name is canon security ground and is already a shipped code control (OUT-02,
Phase 47, `47-SECURITY.md`) owned by `/gsd-secure-phase` — not minted here.
</flagged_assumptions>

<threat_model>
ASVS level 1; blocking threshold `high`. This phase changes zero lines under `typsphinx/` and
introduces no input-handling, authentication, session, or cryptography surface. The register below is
short because the honest surface is small — it is not padded.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest process → `sphinx-build` subprocess | The new gate module spawns `sys.executable -m sphinx` on fixed, repo-controlled fixture paths. No user-controlled input crosses it. |
| repository → published documentation (PyPI / Read the Docs) | Prose written here is read by users and copied into their own `conf.py`. A false or unsafe instruction crosses this boundary unfiltered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Tampering | `_run_sphinx_build()` in `tests/test_output_layout_docs_gate.py` | low | accept | `subprocess.run` is called with a list argument, never `shell=True`, and every path argument is a repo-controlled constant or pytest's own `tmp_path`. Identical to the pattern every existing gate module in this suite already uses. Asserted by an acceptance criterion (`shell=True` count is 0). |
| T-51-02 | Information Disclosure | `docs/source/user_guide/output_layout.rst` worked examples | low | mitigate | Every path published on the page is relative to the output directory (`manual.typ`, `manuals/guide.typ`, `index.typ`), copied from `51-RESEARCH.md` Part C. No absolute local path, home directory, or sandbox-specific path is published. |
| T-51-SC | Tampering | package-manager installs | — | accept | Not applicable: this phase installs no npm/pip/cargo package and adds no runtime or dev dependency (`51-RESEARCH.md` §"Standard Stack"). No Package Legitimacy Audit is owed. |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_output_layout_docs_gate.py -q` — 4 passed, 0 skipped.
- `uv run python -m pytest -q -m "not slow"` — no regression in the existing suite.
- `git diff --name-only HEAD -- typsphinx/` is empty.
- `docs/source/user_guide/index.rst` names `output_layout` exactly twice.
</verification>

<success_criteria>
- The new page exists, names both layers, says the wrapper is the file to compile, and states the
  standalone-content behaviour as plain prose with no admonition.
- Two worked-example configurations are published, and both are built by the gate through a real
  `sphinx-build -b typst` subprocess with their emitted file sets asserted.
- The gate module carries no `typst-py` import and never skips.
- No line under `typsphinx/` changed.
</success_criteria>

<output>
Create `.planning/phases/51-two-layer-output-documentation/51-01-SUMMARY.md` when done.
</output>
