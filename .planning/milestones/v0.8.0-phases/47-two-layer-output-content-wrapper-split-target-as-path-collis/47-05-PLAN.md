---
phase: 47-two-layer-output-content-wrapper-split-target-as-path-collis
plan: 05
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - tests/test_desc_content_indent_render_gate.py
  - tests/test_figure_propagated_target_render_gate.py
  - tests/test_heading_depth_render_gate.py
  - tests/test_integration_nested_toctree.py
  - tests/test_paragraph_concat_render_gate.py
  - tests/test_static_asset_copy_gate.py
  - tests/test_absolute_image_render_gate.py
  - tests/test_confval_field_spacing_render_gate.py
  - tests/test_desc_signature_concat_render_gate.py
  - tests/test_integration_multi_doc.py
  - tests/test_ref_target_nested_list_render_gate.py
  - tests/test_target_label_render_gate.py
  - tests/test_citation_degradation_gate.py
  - tests/test_deflist_term_inline_children_gate.py
  - tests/test_epigraph_render_gate.py
  - tests/test_nested_figure_render_gate.py
  - tests/test_rubric_strong_nesting_render_gate.py
  - tests/fixtures/absolute_image_render_gate/conf.py
  - tests/fixtures/citation_degradation_gate/conf.py
  - tests/fixtures/confval_field_spacing_render_gate/conf.py
  - tests/fixtures/deflist_term_inline_children_concat/conf.py
  - tests/fixtures/desc_content_indent_render_gate/conf.py
  - tests/fixtures/desc_signature_concat_render_gate/conf.py
  - tests/fixtures/desc_signature_siblings_render_gate/conf.py
  - tests/fixtures/epigraph_render_gate/conf.py
  - tests/fixtures/figure_propagated_target_render_gate/conf.py
  - tests/fixtures/integration_multi_doc/conf.py
  - tests/fixtures/integration_multi_level/conf.py
  - tests/fixtures/integration_nested_toctree/conf.py
  - tests/fixtures/integration_sibling/conf.py
  - tests/fixtures/nested_figure_render_gate/conf.py
  - tests/fixtures/paragraph_concat_render_gate/conf.py
  - tests/fixtures/ref_target_nested_list_render_gate/conf.py
  - tests/fixtures/rubric_strong_nesting_render_gate/conf.py
  - tests/fixtures/static_asset_copy_render_gate/conf.py
  - tests/fixtures/target_label_render_gate/conf.py
autonomous: true
requirements: [COMP-01, COMP-02, BLD-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every group-B fixture project configures a `typst_documents` target whose resolved path does not `casefold()`-equal any docname's content path and is not shared with another entry."
    - "The multi-document and nested-toctree integration modules assert toctree `#include()` emission on the CONTENT file (rule R5) and full-document compiles on the wrapper (rule R3), because a toctree is emitted where its doctree is translated."
    - "All 17 group-B modules pass against the post-split emitter, and every changed expected path is traceable to the fixture's own `conf.py` target rather than to emitter output."
  artifacts:
    - tests/test_integration_nested_toctree.py
    - tests/test_integration_multi_doc.py
    - tests/test_absolute_image_render_gate.py
  key_links:
    - "toctree `include()` -> content file: the docname-to-docname relative path the translator emits becomes correct by construction once content files are always docname-derived; this is the mechanism these modules pin."
    - "fixture `conf.py` element [1] -> the module's compile target."
  prohibitions:
    - "A collision is never resolved by inventing a filename the user did not write."
    - "A written output filename is never case-folded, Unicode-normalized or transliterated; folding is comparison-only."
    - "The fifth typst_documents tuple element is never read or repurposed in this phase."
---

<objective>
Migrate corpus group B — 17 test modules and 19 fixture projects, including the multi-document and
nested-toctree integration suites — to the two-layer output shape, and de-collide every group-B
fixture target.

Purpose: group B carries the modules that exercise composition across documents, so it is the group
where the R5 rule (a toctree `#include()` is emitted into the CONTENT file, not the wrapper) is
load-bearing. Getting R5 wrong here would silently move an include assertion onto a file that has
exactly one include, and the test would keep passing while proving nothing.

Output: group B green against the post-split emitter, with every fixture de-collided.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-CONTEXT.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-EXPECTED-STRUCTURE.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `47-01-PLAN.md` section "Artifacts this phase produces". This plan creates no new symbols.

## Migration rules (authority: `47-EXPECTED-STRUCTURE.md` `## Corpus migration rules`)

| Rule | Assertion class | Reads today | Reads after the split | Why |
|------|-----------------|-------------|------------------------|-----|
| R1 | translator body markup, marker strings, code-mode leakage | `index.typ` | `index.typ` (content) — **UNCHANGED** | the translated body is emitted into the content file |
| R2 | template application: a `_template.typ` import, a `#show:` application, title/author parameters, the outline call | `index.typ` | `<target-stem>.typ` (wrapper) | the template application moved to the wrapper |
| R3 | `typst.compile()` of a complete document | `index.typ` | `<target-stem>.typ` (wrapper) | only the wrapper is a complete document |
| R4 | a `-b typstpdf` produced PDF | `index.pdf` | `<target-stem>.pdf` | `typstpdf` compiles wrappers only |
| R5 | toctree `include()` emission | `index.typ` | `index.typ` (content) — **UNCHANGED** | toctrees are emitted where the doctree is translated |

**Fixture de-collision rule.** For each fixture, read its `conf.py` `typst_documents` and compute
each entry's resolved target stem (strip only a literal trailing `.typ`). If that stem
`casefold()`-equals the entry's own docname, another docname in the same fixture, `_template`, or
another entry's stem, change element `[1]` to `"master.typ"` — or, where the fixture's own purpose
names its target, to a purpose-specific distinct name whose reason is recorded in that fixture's
`conf.py` comment. Never change elements `[0]`, `[2]`, `[3]` or `[4]`.

**Do not touch** the four CR-01 collision fixtures (`derived_docname_collision_gate`,
`derived_template_collision_gate`, `explicit_docname_collision_gate`,
`explicit_template_collision_gate`) — they belong to plan 47-09.

<tasks>

<task type="auto">
  <name>Task 1: Migrate the nested-toctree and layout cluster (6 modules, 7 fixtures)</name>
  <files>
    tests/test_desc_content_indent_render_gate.py, tests/test_figure_propagated_target_render_gate.py,
    tests/test_heading_depth_render_gate.py, tests/test_integration_nested_toctree.py,
    tests/test_paragraph_concat_render_gate.py, tests/test_static_asset_copy_gate.py,
    tests/fixtures/desc_content_indent_render_gate/conf.py, tests/fixtures/figure_propagated_target_render_gate/conf.py,
    tests/fixtures/integration_multi_level/conf.py, tests/fixtures/integration_nested_toctree/conf.py,
    tests/fixtures/integration_sibling/conf.py, tests/fixtures/paragraph_concat_render_gate/conf.py,
    tests/fixtures/static_asset_copy_render_gate/conf.py
  </files>
  <read_first>
    - .planning/phases/47-.../47-EXPECTED-STRUCTURE.md section `## Corpus migration rules`
    - tests/test_integration_nested_toctree.py in full (22 code-level `index.typ` references — the densest module in this group)
    - typsphinx/translator.py `visit_toctree` and `_compute_relative_include_path` (the R5 justification: an include path is docname-to-docname, and both endpoints are now content files)
    - each fixture's `conf.py` and the `.rst` files that define its toctree nesting
    - .planning/phases/47-.../47-02-SUMMARY.md
  </read_first>
  <action>
Provision the worktree first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`; run
everything through `uv run`.

Apply the fixture de-collision rule to the seven `conf.py` files, then migrate the six modules
against R1-R5.

The judgment that matters in this task is R5. `tests/test_integration_nested_toctree.py` and the
multi-level and sibling fixtures assert which document `#include()`s which, and at what relative
path. After the split those includes still live in the CONTENT file of the including document,
computed docname-to-docname exactly as before — the wrapper contributes exactly one additional
include, of its own master's content. So: an assertion about a toctree's include set stays on the
content file unchanged (R5), and a NEW assertion belongs on the wrapper stating it contains exactly
one `#include(` naming its master's content path. Add that wrapper assertion for each fixture in this
task; it is derived from COMP-02, not from observed output.

`tests/test_heading_depth_render_gate.py` asserts the `set heading(offset: heading.offset + 1)`
scoping that wraps toctree includes. That scoping is emitted by the translator into the content file
(R5) and is unaffected; only the compile target moves.

`tests/test_static_asset_copy_gate.py` asserts asset copying, which is driven by
`post_process_images` during the write phase. The shared write path introduced by 47-02 calls it once
per docname exactly as before; confirm the asserted asset set is unchanged and only repoint the
compile target.
  </action>
  <verify>
    <automated>uv run pytest tests/test_integration_nested_toctree.py tests/test_desc_content_indent_render_gate.py tests/test_figure_propagated_target_render_gate.py tests/test_heading_depth_render_gate.py tests/test_paragraph_concat_render_gate.py tests/test_static_asset_copy_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0.
    - For each of the seven fixtures, no entry's resolved stem `casefold()`-equals a docname, `_template`, or another entry's stem.
    - Each of the seven fixtures now has a test asserting its wrapper contains exactly one `#include(` and that the included path names its master's content file.
    - Every toctree include-set assertion still reads the content file and its expected paths are unchanged.
    - `git diff --stat typsphinx/` is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">Test and fixture edits only.</reversibility>
  <done>The nested-toctree cluster passes with toctree includes proven on content files and exactly one wrapper include per master.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the multi-document and reference cluster (6 modules, 7 fixtures)</name>
  <files>
    tests/test_absolute_image_render_gate.py, tests/test_confval_field_spacing_render_gate.py,
    tests/test_desc_signature_concat_render_gate.py, tests/test_integration_multi_doc.py,
    tests/test_ref_target_nested_list_render_gate.py, tests/test_target_label_render_gate.py,
    tests/fixtures/absolute_image_render_gate/conf.py, tests/fixtures/confval_field_spacing_render_gate/conf.py,
    tests/fixtures/desc_signature_concat_render_gate/conf.py, tests/fixtures/desc_signature_siblings_render_gate/conf.py,
    tests/fixtures/integration_multi_doc/conf.py, tests/fixtures/ref_target_nested_list_render_gate/conf.py,
    tests/fixtures/target_label_render_gate/conf.py
  </files>
  <read_first>
    - .planning/phases/47-.../47-EXPECTED-STRUCTURE.md section `## Corpus migration rules`
    - tests/test_integration_multi_doc.py in full
    - typsphinx/builder.py `get_target_uri` and its docstring (it stays docname-based by deliberate Phase 22 decision; the wrapper rename must not leak into it)
    - typsphinx/translator.py `_namespace_label` (every emitted label is namespaced by SOURCE docname, never by output filename)
    - each fixture's `conf.py`
  </read_first>
  <action>
Apply the fixture de-collision rule to the seven `conf.py` files, then migrate the six modules
against R1-R5.

The judgment that matters here is that cross-document labels are namespaced by SOURCE DOCNAME, never
by output filename. `get_target_uri` is deliberately docname-based and unchanged this phase, so every
`<docname:anchor>` label an assertion names keeps its exact current value even though the file it is
compiled from moved. Any temptation to rewrite a label expectation to follow the wrapper's name is
wrong and would break every cross-document link; do not do it, and if a label assertion fails, treat
it as a defect in the emitter rather than an expectation to update.

`tests/test_absolute_image_render_gate.py` asserts the PR #131 absolute-image behaviour. Image path
handling is Phase 50's scope (IMG-01, IMG-02) and must not be modified here — repoint only the
compile target and the PDF path, and leave every image path expectation at its current value.
  </action>
  <verify>
    <automated>uv run pytest tests/test_integration_multi_doc.py tests/test_absolute_image_render_gate.py tests/test_confval_field_spacing_render_gate.py tests/test_desc_signature_concat_render_gate.py tests/test_ref_target_nested_list_render_gate.py tests/test_target_label_render_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0.
    - `git diff tests/` shows no hunk that changes a `<docname:anchor>`-shaped label expectation.
    - `git diff tests/test_absolute_image_render_gate.py` shows no hunk that changes an image path expectation.
    - For each of the seven fixtures, no entry's resolved stem collides with a docname, `_template`, or another entry's stem.
    - `git diff --stat typsphinx/` is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">Test and fixture edits only.</reversibility>
  <done>The multi-document and reference cluster passes with every label and image expectation unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the remaining group-B render gates (5 modules, 5 fixtures)</name>
  <files>
    tests/test_citation_degradation_gate.py, tests/test_deflist_term_inline_children_gate.py,
    tests/test_epigraph_render_gate.py, tests/test_nested_figure_render_gate.py,
    tests/test_rubric_strong_nesting_render_gate.py,
    tests/fixtures/citation_degradation_gate/conf.py, tests/fixtures/deflist_term_inline_children_concat/conf.py,
    tests/fixtures/epigraph_render_gate/conf.py, tests/fixtures/nested_figure_render_gate/conf.py,
    tests/fixtures/rubric_strong_nesting_render_gate/conf.py
  </files>
  <read_first>
    - .planning/phases/47-.../47-EXPECTED-STRUCTURE.md section `## Corpus migration rules`
    - each of the five modules and its fixture `conf.py`
    - tests/test_citation_degradation_gate.py in full — it asserts graceful degradation of a citation link, which depends on `master_included_docnames`, a builder attribute this phase deliberately does not remove (Phase 48 owns it)
  </read_first>
  <action>
Apply the fixture de-collision rule and the R1-R5 classification to these five pairs.

`tests/test_citation_degradation_gate.py` exercises the build-time degrade decision driven by
`master_included_docnames`. That attribute and its computation stay exactly as they are this phase —
Phase 48 replaces the build-time decision with a compile-time guard, and Phase 49 replaces the
include graph. Do not adjust any degrade expectation; only repoint the compile target and the PDF
path per R3/R4.

Confirm at the end of this task that all 17 group-B modules pass together, and record in the plan
summary the count of files changed and the count of assertion values changed, which must be zero.
  </action>
  <verify>
    <automated>uv run pytest tests/test_citation_degradation_gate.py tests/test_deflist_term_inline_children_gate.py tests/test_epigraph_render_gate.py tests/test_nested_figure_render_gate.py tests/test_rubric_strong_nesting_render_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0.
    - All 17 group-B modules pass together: one `uv run pytest` invocation over this plan's `files_modified` test list exits 0.
    - `git diff tests/test_citation_degradation_gate.py` shows no hunk that changes a degrade expectation.
    - The plan summary records "assertion values changed: 0".
    - `git diff --stat typsphinx/` is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">Test and fixture edits only.</reversibility>
  <done>All 17 group-B modules and their 19 fixtures pass against the post-split emitter and no group-B fixture self-collides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture `conf.py` -> builder config | de-colliding each fixture target is the corpus-scale exercise of the trust boundary the product defends |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-02 | Tampering | wrapper vs. content physical path across the fixture corpus | high | mitigate | The de-collision rule is applied to every group-B fixture and asserted per fixture |
| T-47-09 | Tampering (test-integrity) | a laundered gate — an expected value quietly changed to match new output | high | mitigate | Acceptance criteria assert `git diff` contains no label, image-path or degrade-expectation change, and the summary records an explicit zero count |
| T-47-10 | Spoofing | a cross-document label following the wrapper name instead of the source docname | high | mitigate | Task 2 forbids rewriting label expectations; `get_target_uri` stays docname-based and `_namespace_label` is untouched |
| T-47-05 | Information Disclosure | build-time file generation | low | accept | No network or data-egress surface |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- Every module named in `files_modified` passes in one `uv run pytest` invocation.
- `uv run black --check .` and `uv run ruff check .` exit 0.
- `git diff --stat typsphinx/` is empty.
- The full suite is still knowingly RED until the remaining groups land; 47-09 is the phase's full-suite-green gate.
</verification>

<success_criteria>
- 17 group-B modules green against the post-split emitter.
- Toctree include assertions proven to live on content files; every wrapper proven to hold exactly one include.
- Zero label, image-path or degrade expectations changed; zero production files touched.
</success_criteria>

<output>
Create `.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-05-SUMMARY.md` when done
</output>
