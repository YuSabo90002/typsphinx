---
phase: 47-two-layer-output-content-wrapper-split-target-as-path-collis
plan: 03
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - tests/test_builder_output_stem.py
  - tests/test_out02_escape_target_gate.py
  - tests/fixtures/out02_escape_target_gate/
  - typsphinx/builder.py
autonomous: true
requirements: [OUT-01, OUT-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `typst_documents` target containing a path separator is treated as a path relative to the output directory and is NOT truncated to its basename — the unit expectation that asserted truncation is inverted, and the inversion is traceable to OUT-01's written reversal of Phase 44's D-06/D-07."
    - "A target that is empty, whitespace-only, non-`str`, or empty after stripping a literal trailing `.typ` still falls back to the docname with exactly one warning (OUT-01, edge/empty)."
    - "A non-ASCII target such as `マニュアル` is written byte for byte with no case folding, Unicode normalization or transliteration on the write side (OUT-01, edge/encoding, D-05)."
    - "A target containing `..`, an absolute target, and a drive-qualified target are each still refused with a warning and a safe fallback, proven by one real `sphinx-build` fixture per escape shape and by the surviving unit tests (OUT-02)."
    - "A repo-wide grep for a second, independent path-rejection site (`os.sep`, `os.altsep`, `isabs`, drive-letter detection) was run and its result recorded, closing RESEARCH.md Assumptions-Log A3 by measurement rather than by absence of a counter-example."
    - "No emitted file lands outside `outdir` for any of the three escape shapes, asserted by resolving every written path and checking it is under the build directory."
  artifacts:
    - tests/test_builder_output_stem.py
    - tests/test_out02_escape_target_gate.py
    - tests/fixtures/out02_escape_target_gate/conf.py
  key_links:
    - "`_escapes_outdir` -> the existing fallback-to-basename-with-warning block: OUT-02's control is the routing, not a new check."
    - "OUT-02 fixtures -> outdir containment: the assertion is on the resolved absolute path of every written file, not on the warning text alone."
  prohibitions:
    - "A collision is never resolved by inventing a filename the user did not write."
    - "A written output filename is never case-folded, Unicode-normalized or transliterated; folding is comparison-only."
    - "The fifth typst_documents tuple element is never read or repurposed in this phase."
---

<objective>
Move the unit expectations that Phase 44's D-05/D-06/D-07 pinned, and prove — with one real
`sphinx-build` fixture per escape shape — that OUT-02's security half survived the OUT-01 rewrite
intact.

Purpose: RESEARCH.md Pitfall 4 names this the single highest-risk regression site in the whole
phase, because all four conditions lived in one OR-expression and a naive reading of OUT-01 deletes
the security half with it. RESEARCH.md also records its own warning sign: a test asserting "any `/`
in a target name warns and truncates" that STILL passes after the change is itself a bug signal. This
plan makes the inversion explicit and adds the containment proof that a warning-text assertion alone
cannot give.

Output: `tests/test_builder_output_stem.py` with OUT-01's expectations moved and OUT-02's kept
verbatim as regression tests; a new `tests/test_out02_escape_target_gate.py` real-subprocess gate;
and a recorded answer to RESEARCH.md Assumptions-Log A3.

**Reversal notice.** OUT-01 deliberately REVERSES Phase 44's D-05, D-06 and D-07. The expectations
this plan changes were correct under those decisions and are being changed on purpose, not repaired.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-CONTEXT.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-RESEARCH.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `47-01-PLAN.md` section "Artifacts this phase produces". This plan creates
`tests/test_out02_escape_target_gate.py` and `tests/fixtures/out02_escape_target_gate/`, and
exercises the `_escapes_outdir` predicate created by 47-02.

<tasks>

<task type="auto">
  <name>Task 1: Move the OUT-01 expectations and keep the OUT-02 guard tests verbatim</name>
  <files>tests/test_builder_output_stem.py</files>
  <read_first>
    - tests/test_builder_output_stem.py (the whole file — every one of its ~25 tests, in particular lines 108-165 for the four guard cases, 166-235 for the degenerate and non-ASCII cases, and 304-330 for the directory-preserving relpath cases)
    - typsphinx/builder.py `_resolve_output_stem` and `_escapes_outdir` as rewritten by 47-02
    - .planning/phases/47-.../47-RESEARCH.md "Common Pitfalls" 4
    - .planning/phases/47-.../47-CONTEXT.md decision D-05 (case folding is comparison-only; the write side keeps the no-normalization rule)
  </read_first>
  <action>
Rewrite the expectations in `tests/test_builder_output_stem.py` that Phase 44's D-05/D-06/D-07
pinned, and leave every other test untouched.

Invert the two separator tests: the POSIX-separator case (`"sub/manual.typ"` on docname `"index"`)
must now resolve to `"sub/manual"` with NO warning, and the backslash-separator case
(`"sub\\manual.typ"`) must resolve to the same normalized `"sub/manual"` with no warning — a
Windows-authored target is still recognised as a path on POSIX, it is simply no longer refused.
Rename both test functions so their names state the new contract rather than a guard, and give each
a docstring naming OUT-01 and the Phase 44 decision it reverses.

Keep the three escape tests exactly as they are — parent traversal, absolute target, drive-qualified
target must each still resolve to the basename fallback. Do not rename them, do not relax their
assertions, and add a one-line docstring note to each recording that a still-passing assertion here
is confirming evidence for OUT-02, per RESEARCH.md's own warning sign.

Keep verbatim: the trailing-`.typ`-only stripping tests (including `v1.2-manual`), the
five-element-tuple tolerance test, the unlisted-docname and missing-config fallbacks, the
empty/whitespace/bare-`.typ`/short-tuple/non-`str` degenerate fallbacks, the warn-once test for a
path-bearing target with an empty basename, and the non-ASCII byte-preservation test.

Rewrite or remove the two `_directory_preserving_relpath` tests according to what 47-02 left in
place: if that helper is no longer called on any wrapper path, replace the two tests with one that
asserts the wrapper path for `("sub/index", "manual.typ", ...)` is `"manual"` at the output root —
NOT `"sub/manual"` — which is exactly the Phase 44 D-05 forcing OUT-01 removes. Add a companion
assertion that the same docname's CONTENT path is still `sub/index`.

Leave the two CR-01 collision tests at lines 333-366 alone; plan 47-09 owns their inversion.
  </action>
  <verify>
    <automated>uv run pytest tests/test_builder_output_stem.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_builder_output_stem.py -q` exits 0 except for the two CR-01 collision tests, which 47-09 owns and which may remain failing after this task.
    - `grep -c 'guards_posix_path_separator\|guards_backslash_path_separator' tests/test_builder_output_stem.py` returns 0 — both guard-named separator tests have been renamed to state the new contract.
    - The tests covering parent traversal, absolute targets and drive-qualified targets are still present and passing, with their function names unchanged.
    - `grep -c 'マニュアル' tests/test_builder_output_stem.py` returns at least 1 — the byte-preservation test survives.
    - Every changed expectation carries a docstring naming OUT-01 and the Phase 44 decision it reverses.
  </acceptance_criteria>
  <reversibility rating="reversible">Test expectations; changing them again costs one edit.</reversibility>
  <done>The stem-resolution unit suite states OUT-01's contract and still pins all three OUT-02 escape shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Add a real-sphinx-build gate with one fixture per escape shape and an outdir-containment assertion</name>
  <files>tests/test_out02_escape_target_gate.py, tests/fixtures/out02_escape_target_gate/conf.py, tests/fixtures/out02_escape_target_gate/index.rst</files>
  <read_first>
    - tests/test_typst_documents_collision_gate.py lines 18-72 (the `_run_sphinx_build` helper to copy per-module)
    - tests/fixtures/derived_docname_collision_gate/conf.py (the load-bearing-facts conf.py comment convention)
    - typsphinx/builder.py `_escapes_outdir` and the fallback block it routes to
    - .planning/phases/47-.../47-RESEARCH.md "Security Domain" (ASVS V5 and V12 rows)
  </read_first>
  <action>
Create `tests/fixtures/out02_escape_target_gate/` with a single docname `index` whose body carries
the marker `ESCAPE-GATE-MARKER`, and a `conf.py` that reads the escape shape from an environment
variable so one fixture directory serves all three shapes — a comment block must state which values
are load-bearing. Read `TYPSPHINX_ESCAPE_SHAPE` and map `traversal` to `"../escape.typ"`, `absolute`
to `"/tmp/escape.typ"` on POSIX and `"\\\\escape.typ"` on Windows via `os.name`, and `drive` to
`"C:\\escape.typ"`; default to `traversal` when unset.

Create `tests/test_out02_escape_target_gate.py` with its own copy of `_run_sphinx_build` (extended to
accept an `env` mapping) and a parametrized test over the three shapes. Per shape assert: the build
exits 0; the combined stdout+stderr contains a warning naming the offending target; the wrapper was
written at the basename fallback inside the build directory; and — the containment proof — every
regular file under the build directory resolves, via `Path.resolve()`, to a path that is under the
resolved build directory, so nothing escaped `outdir`. Also assert that no file named `escape.typ`
exists anywhere OUTSIDE the build directory that the test itself can reach, by checking the parent
of the build directory does not gain an `escape.typ`.

Mark the drive-qualified case to run on every platform — the check is a string-shape check, not a
filesystem behaviour, and D-05's platform-independence principle applies to it too.
  </action>
  <verify>
    <automated>uv run pytest tests/test_out02_escape_target_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_out02_escape_target_gate.py -q` exits 0 with three parametrized cases reported.
    - For each shape the test asserts `Path(f).resolve().is_relative_to(build_dir.resolve())` for every regular file under the build directory.
    - For each shape the combined build output contains a warning mentioning the configured target string.
    - `ls <build_dir>/../escape.typ` does not exist after any of the three runs.
    - `grep -c 'TYPSPHINX_ESCAPE_SHAPE' tests/fixtures/out02_escape_target_gate/conf.py` returns at least 1.
  </acceptance_criteria>
  <reversibility rating="reversible">A new test module and fixture; removable without touching production code.</reversibility>
  <done>Each of the three escape shapes is proven refused with a warning and a safe fallback in a real build, and nothing is written outside the output directory.</done>
</task>

<task type="auto">
  <name>Task 3: Close RESEARCH.md Assumptions-Log A3 by measurement</name>
  <files>typsphinx/builder.py, .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-RED-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/47-.../47-RESEARCH.md "Assumptions Log" row A3
    - typsphinx/builder.py in full
    - typsphinx/writer.py in full
    - typsphinx/translator.py — the image and include path-emission sites
  </read_first>
  <action>
RESEARCH.md's Assumptions-Log A3 records that a repo-wide grep for a SECOND, independent
path-rejection site was never exhaustively run, and that a missed site would let an escaping target
slip past OUT-02 undetected. Close it by measurement.

Run a repo-wide grep over `typsphinx/` for each of `os.sep`, `os.altsep`, `isabs`, `normpath`,
`relpath`, `basename`, and a drive-letter detection idiom (a two-character prefix test whose second
character is a colon). For every hit, record in `47-RED-EVIDENCE.md` under a new heading
"A3: second path-rejection site search": the file and line, what the site does, and whether it can
independently accept or reject a user-supplied target string. If a second independent rejection site
IS found, route it through the same `_escapes_outdir` predicate rather than leaving two rules, and
add a unit test pinning that the two sites agree on all three escape shapes. If none is found, record
that explicitly with the grep commands and their output so the assumption is closed by evidence
rather than silence.

Do not widen this into a general audit of path handling — image path rehoming is Phase 50's scope
(IMG-01, IMG-02) and must not be picked up opportunistically.
  </action>
  <verify>
    <automated>uv run pytest tests/test_builder_output_stem.py tests/test_out02_escape_target_gate.py tests/test_two_layer_output_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `47-RED-EVIDENCE.md` contains a heading `A3: second path-rejection site search` listing every grepped pattern, the commands run, and their output.
    - The recorded conclusion is one of exactly two: a named second site now routed through `_escapes_outdir` with a unit test naming both sites, or an explicit statement that no second site exists with the grep evidence quoted.
    - `uv run pytest tests/test_builder_output_stem.py tests/test_out02_escape_target_gate.py tests/test_two_layer_output_gate.py -q` exits 0 apart from the two CR-01 collision tests 47-09 owns.
    - `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` exit 0.
    - No change was made to image path handling: `git diff --stat` shows no hunk touching a `_track_image` or `copy_image_files` body.
  </acceptance_criteria>
  <done>Assumptions-Log A3 is closed by recorded measurement, and OUT-02 has exactly one rule in exactly one place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` -> `_resolve_output_stem` | a target string is the only user-controlled input to wrapper path resolution |
| resolved wrapper path -> `outdir` | the containment boundary OUT-02 defends |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering / Elevation of Privilege | `_escapes_outdir` and the fallback block it routes to | high | mitigate | Task 1 keeps the three unit guard tests verbatim; task 2 adds a real-build containment assertion resolving every written path against the build directory — a warning-text assertion alone cannot prove containment |
| T-47-07 | Tampering | an undiscovered second path-rejection site | high | mitigate | Task 3 closes RESEARCH.md Assumptions-Log A3 by exhaustive grep and routes any second site through the one predicate |
| T-47-08 | Elevation of Privilege | drive-qualified target on a POSIX runner | medium | mitigate | The drive check is a string-shape test asserted on every platform, so a Windows-authored `conf.py` is refused identically on Linux CI |
| T-47-05 | Information Disclosure | build-time file generation | low | accept | No network or data-egress surface |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `uv run pytest tests/test_builder_output_stem.py tests/test_out02_escape_target_gate.py -q` exits 0 apart from the two CR-01 tests 47-09 owns.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` exit 0.
- The full suite remains knowingly RED until 47-09; do not attempt broad repairs here.
</verification>

<success_criteria>
- OUT-01's separator expectations are inverted with a written rationale naming the Phase 44 reversal.
- All three OUT-02 escape shapes are pinned at both the unit and the real-build level, with an outdir-containment assertion.
- Assumptions-Log A3 is closed by recorded measurement.
</success_criteria>

<output>
Create `.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-03-SUMMARY.md` when done
</output>
