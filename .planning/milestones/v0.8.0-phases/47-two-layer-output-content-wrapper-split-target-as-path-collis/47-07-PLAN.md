---
phase: 47-two-layer-output-content-wrapper-split-target-as-path-collis
plan: 07
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - tests/test_desc_sig_space_render_gate.py
  - tests/test_inline_literal_overflow_render_gate.py
  - tests/test_paragraph_soft_newline_render_gate.py
  - tests/test_signature_typography_gate.py
  - tests/test_signature_typography_multi_signature_page_count_gate.py
  - tests/test_table_empty_caption_anchor_render_gate.py
  - tests/test_captioned_table_propagated_target_render_gate.py
  - tests/test_deflist_nested_definition_render_gate.py
  - tests/test_documented_params_contract_gate.py
  - tests/test_list_item_nested_block_render_gate.py
  - tests/test_rubric_option_concat_render_gate.py
  - tests/test_xref_orphan_degrade_render_gate.py
  - tests/test_codly_caption_listitem_leak_render_gate.py
  - tests/test_desc_break_marker_buffer_swap_gate.py
  - tests/test_field_body_typography_render_gate.py
  - tests/test_non_str_docname_gate.py
  - tests/test_signature_overflow_render_gate.py
  - tests/fixtures/captioned_table_propagated_target_render_gate/conf.py
  - tests/fixtures/codly_caption_listitem_leak_render_gate/conf.py
  - tests/fixtures/deflist_nested_definition_render_gate/conf.py
  - tests/fixtures/desc_break_marker_buffer_swap_gate/conf.py
  - tests/fixtures/desc_sig_space_render_gate/conf.py
  - tests/fixtures/documented_params_contract_gate/conf.py
  - tests/fixtures/field_body_typography_render_gate/conf.py
  - tests/fixtures/inline_literal_overflow_render_gate/conf.py
  - tests/fixtures/list_item_nested_block_render_gate/conf.py
  - tests/fixtures/non_str_docname_gate/conf.py
  - tests/fixtures/paragraph_soft_newline_render_gate/conf.py
  - tests/fixtures/rubric_option_concat_render_gate/conf.py
  - tests/fixtures/signature_overflow_render_gate/conf.py
  - tests/fixtures/signature_typography_gate/conf.py
  - tests/fixtures/table_empty_caption_anchor_render_gate/conf.py
  - tests/fixtures/xref_orphan_degrade_render_gate/conf.py
autonomous: true
requirements: [COMP-01, COMP-02, BLD-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every group-D fixture project configures a `typst_documents` target whose resolved path does not `casefold()`-equal any docname's content path and is not shared with another entry."
    - "Page-count and page-boundary assertions are made against the compiled WRAPPER, because only the wrapper carries the title page and outline that decide page indices."
    - "The published custom-template parameter contract gate still passes: the wrapper is the only file that applies a template, so the nine-parameter contract is asserted there."
    - "All 17 group-D modules pass against the post-split emitter, and every changed expected path is traceable to the fixture's own `conf.py` target rather than to emitter output."
  artifacts:
    - tests/test_signature_typography_multi_signature_page_count_gate.py
    - tests/test_documented_params_contract_gate.py
    - tests/test_non_str_docname_gate.py
  key_links:
    - "wrapper -> page indices: the title page and outline live only in the wrapper, so every page-count expectation is a wrapper-compile expectation."
    - "wrapper -> template parameter contract: the wrapper is the sole template application site after the split."
  prohibitions:
    - "A collision is never resolved by inventing a filename the user did not write."
    - "A written output filename is never case-folded, Unicode-normalized or transliterated; folding is comparison-only."
    - "The fifth typst_documents tuple element is never read or repurposed in this phase."
---

<objective>
Migrate corpus group D — 17 test modules and 16 fixture projects, including the page-count and
template-parameter-contract gates — to the two-layer output shape, and de-collide every group-D
fixture target.

Purpose: group D carries the assertions most sensitive to WHICH file is compiled. A page-count
expectation is meaningless against a content file, which has no title page and no outline; a
template-parameter-contract assertion is meaningless against a content file, which applies no
template at all. Getting the compile target right here is what keeps these gates honest.

Output: group D green against the post-split emitter, with every fixture de-collided.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-CONTEXT.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-EXPECTED-STRUCTURE.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `47-01-PLAN.md` section "Artifacts this phase produces". This plan creates no new symbols.

## Migration rules (authority: `47-EXPECTED-STRUCTURE.md` `## Corpus migration rules`)

| Rule | Assertion class | Reads today | Reads after the split | Why |
|------|-----------------|-------------|------------------------|-----|
| R1 | translator body markup, marker strings, code-mode leakage | `index.typ` | `index.typ` (content) — **UNCHANGED** | the translated body is emitted into the content file |
| R2 | template application: a `_template.typ` import, a `#show:` application, title/author parameters, the outline call | `index.typ` | `<target-stem>.typ` (wrapper) | the template application moved to the wrapper |
| R3 | `typst.compile()` of a complete document | `index.typ` | `<target-stem>.typ` (wrapper) | only the wrapper is a complete document |
| R4 | a `-b typstpdf` produced PDF | `index.pdf` | `<target-stem>.pdf` | `typstpdf` compiles wrappers only |
| R5 | toctree `include()` emission | `index.typ` | `index.typ` (content) — **UNCHANGED** | toctrees are emitted where the doctree is translated |

**Fixture de-collision rule.** For each fixture, read its `conf.py` `typst_documents` and compute
each entry's resolved target stem (strip only a literal trailing `.typ`). If that stem
`casefold()`-equals the entry's own docname, another docname in the same fixture, `_template`, or
another entry's stem, change element `[1]` to `"master.typ"` — or, where the fixture's own purpose
names its target, to a purpose-specific distinct name whose reason is recorded in that fixture's
`conf.py` comment. Never change elements `[0]`, `[2]`, `[3]` or `[4]`.

**Do not touch** the four CR-01 collision fixtures — they belong to plan 47-09.

<tasks>

<task type="auto">
  <name>Task 1: Migrate the page-count and typography cluster (6 modules, 5 fixtures)</name>
  <files>
    tests/test_desc_sig_space_render_gate.py, tests/test_inline_literal_overflow_render_gate.py,
    tests/test_paragraph_soft_newline_render_gate.py, tests/test_signature_typography_gate.py,
    tests/test_signature_typography_multi_signature_page_count_gate.py,
    tests/test_table_empty_caption_anchor_render_gate.py,
    tests/fixtures/desc_sig_space_render_gate/conf.py, tests/fixtures/inline_literal_overflow_render_gate/conf.py,
    tests/fixtures/paragraph_soft_newline_render_gate/conf.py, tests/fixtures/signature_typography_gate/conf.py,
    tests/fixtures/table_empty_caption_anchor_render_gate/conf.py
  </files>
  <read_first>
    - .planning/phases/47-.../47-EXPECTED-STRUCTURE.md section `## Corpus migration rules`
    - tests/test_signature_typography_multi_signature_page_count_gate.py in full — its assertions are page indices, which only exist against a compiled wrapper
    - .planning/STATE.md section "Blockers/Concerns" note that ZWSP poisons `pypdf` extraction and every compiled-PDF assertion must strip U+200B first
    - each fixture's `conf.py`
    - .planning/phases/47-.../47-02-SUMMARY.md
  </read_first>
  <action>
Provision the worktree first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`; run
everything through `uv run`.

Apply the fixture de-collision rule to the five `conf.py` files, then migrate the six modules against
R1-R5.

The judgment that matters here is R3 versus R1 for page-shaped claims. A page index, a page count, a
"this signature does not straddle a page boundary" claim, or an adjacency claim measured in extracted
PDF text is only meaningful against the compiled WRAPPER, because the title page and the outline that
shift every subsequent page index live in the wrapper alone. Repoint every such compile at the
wrapper and leave the expected page indices unchanged — the wrapper applies exactly the template the
master applied before the split, so the rendered page sequence is identical. If a page index genuinely
shifts, stop and record the before/after page sequence in the plan summary rather than adjusting the
expectation; a silently updated page index is a laundered gate.

Keep every U+200B stripping step exactly where it is.
  </action>
  <verify>
    <automated>uv run pytest tests/test_signature_typography_multi_signature_page_count_gate.py tests/test_signature_typography_gate.py tests/test_desc_sig_space_render_gate.py tests/test_inline_literal_overflow_render_gate.py tests/test_paragraph_soft_newline_render_gate.py tests/test_table_empty_caption_anchor_render_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0.
    - `git diff tests/test_signature_typography_multi_signature_page_count_gate.py` shows path changes only — no page index or page count expectation changed.
    - Every U+200B stripping step is still present: `grep -c '200B\|\\u200b' tests/test_signature_typography_gate.py` returns the same count as before the change.
    - For each of the five fixtures, no entry's resolved stem `casefold()`-equals a docname, `_template`, or another entry's stem.
    - `git diff --stat typsphinx/` is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">Test and fixture edits only.</reversibility>
  <done>Every page-shaped claim is measured against a compiled wrapper and its expected value is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the template-contract and propagated-target cluster (6 modules, 6 fixtures)</name>
  <files>
    tests/test_captioned_table_propagated_target_render_gate.py, tests/test_deflist_nested_definition_render_gate.py,
    tests/test_documented_params_contract_gate.py, tests/test_list_item_nested_block_render_gate.py,
    tests/test_rubric_option_concat_render_gate.py, tests/test_xref_orphan_degrade_render_gate.py,
    tests/fixtures/captioned_table_propagated_target_render_gate/conf.py,
    tests/fixtures/deflist_nested_definition_render_gate/conf.py, tests/fixtures/documented_params_contract_gate/conf.py,
    tests/fixtures/list_item_nested_block_render_gate/conf.py, tests/fixtures/rubric_option_concat_render_gate/conf.py,
    tests/fixtures/xref_orphan_degrade_render_gate/conf.py
  </files>
  <read_first>
    - .planning/phases/47-.../47-EXPECTED-STRUCTURE.md section `## Corpus migration rules`
    - tests/test_documented_params_contract_gate.py in full and tests/fixtures/documented_params_contract_gate/conf.py — it pins the published nine-parameter custom-template contract from `docs/source/user_guide/templates.rst`
    - typsphinx/writer.py `render_wrapper` as written by 47-02 (which parameters it passes and where `extract_toctree_options` feeds them)
    - tests/test_xref_orphan_degrade_render_gate.py — it depends on `master_included_docnames`, which this phase deliberately keeps (Phase 48 owns it)
  </read_first>
  <action>
Apply the fixture de-collision rule to the six `conf.py` files, then migrate the six modules against
R1-R5.

`tests/test_documented_params_contract_gate.py` asserts that the parameters typsphinx actually passes
to a custom template match the published contract. After the split the wrapper is the sole template
application site, so this is an R2/R3 module in full: its custom template, its emitted call and its
compile all move to the wrapper. The set of parameters passed must not change — `render_wrapper`
passes exactly what the master path passed, including the `toctree_*` parameters
`extract_toctree_options` supplies when the master has a toctree. If the parameter set differs, that
is a defect in 47-02 and must be reported, not accommodated by editing this gate.

`tests/test_xref_orphan_degrade_render_gate.py` pins the build-time degrade decision for a reference
into an `:orphan:` document. That decision is driven by `master_included_docnames`, which stays
exactly as it is this phase. Repoint only the compile target and the PDF path; do not adjust any
degrade expectation.

The three propagated-target modules assert that a standalone target preceding a captioned table, a
nested definition, or a list item emits its label. Labels are namespaced by SOURCE docname and the
body lives in the content file, so those are R1 assertions — leave their paths alone and repoint only
the compile.
  </action>
  <verify>
    <automated>uv run pytest tests/test_documented_params_contract_gate.py tests/test_xref_orphan_degrade_render_gate.py tests/test_captioned_table_propagated_target_render_gate.py tests/test_deflist_nested_definition_render_gate.py tests/test_list_item_nested_block_render_gate.py tests/test_rubric_option_concat_render_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0.
    - `git diff tests/test_documented_params_contract_gate.py` shows no hunk that adds, removes or renames a template parameter in the asserted set.
    - `git diff tests/test_xref_orphan_degrade_render_gate.py` shows no hunk that changes a degrade expectation.
    - The three propagated-target modules still read their label assertions from the content file.
    - For each of the six fixtures, no entry's resolved stem collides with a docname, `_template`, or another entry's stem.
    - `git diff --stat typsphinx/` is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">Test and fixture edits only.</reversibility>
  <done>The published template-parameter contract is asserted against the wrapper with an unchanged parameter set, and every propagated-target label assertion stays on the content file.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the remaining group-D gates and the malformed-docname gate (5 modules, 5 fixtures)</name>
  <files>
    tests/test_codly_caption_listitem_leak_render_gate.py, tests/test_desc_break_marker_buffer_swap_gate.py,
    tests/test_field_body_typography_render_gate.py, tests/test_non_str_docname_gate.py,
    tests/test_signature_overflow_render_gate.py,
    tests/fixtures/codly_caption_listitem_leak_render_gate/conf.py,
    tests/fixtures/desc_break_marker_buffer_swap_gate/conf.py, tests/fixtures/field_body_typography_render_gate/conf.py,
    tests/fixtures/non_str_docname_gate/conf.py, tests/fixtures/signature_overflow_render_gate/conf.py
  </files>
  <read_first>
    - .planning/phases/47-.../47-EXPECTED-STRUCTURE.md section `## Corpus migration rules`
    - tests/test_non_str_docname_gate.py in full and its fixture — it pins BLD-01's non-`str` docname handling, which used to raise a raw `TypeError` inside `_directory_preserving_relpath`
    - typsphinx/builder.py `TypstPDFBuilder.finish()` non-`str` docname guard
    - each remaining module and its fixture `conf.py`
  </read_first>
  <action>
Apply the fixture de-collision rule and the R1-R5 classification to these five pairs.

`tests/test_non_str_docname_gate.py` pins BLD-01: a `typst_documents` entry with a non-`str` docname
must be reported through the aggregate failure list rather than killing the build with a raw
`TypeError`. 47-02 removed `_directory_preserving_relpath` from the wrapper path, which is where that
`TypeError` originated, so re-verify the guard still fires: the new wrapper-path resolution and the
new content-path computation must each tolerate a non-`str` docname the same way, skipping the entry
rather than indexing or joining it. If the guard no longer fires, that is a defect in 47-02's path
computation and must be reported, not accommodated by weakening this gate.

`tests/test_desc_break_marker_buffer_swap_gate.py` and
`tests/test_codly_caption_listitem_leak_render_gate.py` assert translator-internal buffer behaviour
visible in the emitted body — R1, paths unchanged, compile target moved.

Confirm at the end of this task that all 17 group-D modules pass together, and record in the plan
summary the count of files changed and the count of assertion values changed, which must be zero.
  </action>
  <verify>
    <automated>uv run pytest tests/test_non_str_docname_gate.py tests/test_codly_caption_listitem_leak_render_gate.py tests/test_desc_break_marker_buffer_swap_gate.py tests/test_field_body_typography_render_gate.py tests/test_signature_overflow_render_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0.
    - All 17 group-D modules pass together: one `uv run pytest` invocation over this plan's `files_modified` test list exits 0.
    - `tests/test_non_str_docname_gate.py` still asserts that a non-`str` docname produces the aggregate `ExtensionError` and not a raw `TypeError`, with its expectation unchanged.
    - The plan summary records "assertion values changed: 0".
    - `git diff --stat typsphinx/` is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">Test and fixture edits only.</reversibility>
  <done>All 17 group-D modules and their 16 fixtures pass against the post-split emitter and no group-D fixture self-collides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture `conf.py` -> builder config | de-colliding each fixture target is the corpus-scale exercise of the trust boundary the product defends |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-02 | Tampering | wrapper vs. content physical path across the fixture corpus | high | mitigate | The de-collision rule is applied to every group-D fixture and asserted per fixture |
| T-47-09 | Tampering (test-integrity) | a laundered gate — a page index or template parameter quietly adjusted to match new output | high | mitigate | Acceptance criteria assert `git diff` contains no page-index and no template-parameter-set change; any genuine change must be stopped and written up |
| T-47-12 | Denial of Service | a non-`str` docname reaching the new path computation and raising a raw `TypeError` | medium | mitigate | Task 3 re-verifies the BLD-01 guard against the rewritten wrapper and content path computation |
| T-47-05 | Information Disclosure | build-time file generation | low | accept | No network or data-egress surface |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- Every module named in `files_modified` passes in one `uv run pytest` invocation.
- `uv run black --check .` and `uv run ruff check .` exit 0.
- `git diff --stat typsphinx/` is empty.
- The full suite is still knowingly RED until the residual plan lands; 47-09 is the phase's full-suite-green gate.
</verification>

<success_criteria>
- 17 group-D modules green against the post-split emitter.
- Every page-shaped and template-parameter claim measured against a compiled wrapper with unchanged expected values.
- BLD-01's non-`str` docname guard re-verified against the rewritten path computation.
</success_criteria>

<output>
Create `.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-07-SUMMARY.md` when done
</output>
