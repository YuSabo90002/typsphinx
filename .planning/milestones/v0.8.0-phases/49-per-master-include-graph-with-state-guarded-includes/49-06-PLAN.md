---
phase: 49-per-master-include-graph-with-state-guarded-includes
plan: 06
type: execute
wave: 5
depends_on: ["49-05"]
files_modified:
  - tests/fixtures/state_guard_numref_two_case_gate/
  - tests/test_state_guard_numref_gate.py
  - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
autonomous: false
requirements: [COMP-12]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full Sphinx `doc/` corpus compiles fatal-free through the PDF builder under the new composition: the existing GATE-02 corpus gate is run UNMODIFIED, the compiled PDF exists, is non-empty and starts with the PDF magic bytes, and the unsupported-node catalogue is empty — demonstrating that the multi-pass layout convergence the state-guarded design rests on holds beyond the small measured fixtures (COMP-12, SC#5)."
    - "A corpus convergence failure is treated as a DESIGN-LEVEL FINDING and escalated to the owner, not as a fixture bug. The corpus is not narrowed, the gate is not skipped or marked flaky, no partial write-time fallback is introduced, and the minimal failing shape is isolated and reported — per D-02 and ROADMAP binding constraint #5 (COMP-12, boundary)."
    - "The corpus gate's own runtime is recorded next to Phase 48's measured baseline, so a convergence cost regression is visible as a number rather than as a feeling. A cost change is recorded, not acted on — this phase has no cost-tier decision of its own."
    - "Open question #2 closes on a live two-master fixture, not by inference. Case (a): the same figure, reachable from BOTH the root document and a second master at DIFFERENT traversal positions, has its Sphinx-baked reference text extracted from each master's compiled PDF and compared against that master's own Typst-assigned caption number (D-01)."
    - "Case (b) — the sharper case research added — is measured too: a figure reachable ONLY through a non-root master gets no entry in Sphinx's figure-number mapping at all, because that mapping is populated by a single walk starting only from the root document, so the numbered reference silently falls back to the reference's own literal text with zero warning at any layer. The fixture proves the fallback rather than assuming it (D-01)."
    - "Both cases' extracted values are recorded VERBATIM in 49-EVIDENCE.md — the reference's rendered text and the target figure's Typst-assigned caption number, per master, per case — before any fix-or-document decision is written down (D-01)."
    - "The fix-or-document decision is recorded explicitly and follows D-01: a divergence is recorded as a documented limitation and handed forward to Phase 51 (docs) and Phase 52 (CHANGELOG), NOT fixed in this phase. A null result — no divergence — is recorded with the same evidence standard rather than left unstated (D-01)."
    - "The `:numref:` measurement does not silently expand this phase's scope: no renumbering mechanism is added, no figure-number override is emitted, and the phase's diff contains no change to figure emission (D-01)."
    - "The phase's standing green bar is re-measured at the end: the full test suite passes, the formatter, linter and type checker all pass, and the results are pasted verbatim (ROADMAP binding constraint #8)."
    - "All five of the phase's success criteria are walked one by one with the artifact and the measurement that discharges each, and any criterion not discharged is named as such rather than omitted."
    - { statement: "Typst's multi-pass layout converges for the corpus-scale state/context graph with no oscillation between passes. Convergence cannot be positively proven from a single green run — a green run shows convergence for this corpus at this version pin, not convergence in general — so this is recorded as an observed result with its scope stated, and the residual risk stays named in PROJECT.md rather than being marked closed.", verification: backstop }
  artifacts:
    - tests/fixtures/state_guard_numref_two_case_gate/conf.py
    - tests/test_state_guard_numref_gate.py
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
  key_links:
    - "tests/test_corpus_gate.py -> COMP-12: the existing GATE-02 gate is the vehicle, run unmodified against the new composition. Modifying it to make it pass would be the exact laundering D-02 forbids, so the gate's own file must be untouched by this phase."
    - "tests/fixtures/state_guard_numref_two_case_gate/ -> 49-EVIDENCE.md `## numref measurement`: both cases' extracted values are recorded before the decision paragraph is written, so the decision is a reading of the evidence rather than a framing of it."
    - "49-EVIDENCE.md `## numref measurement` -> Phase 51 and Phase 52: the divergence (or the null result) becomes a named documentation and CHANGELOG obligation, which is what keeps D-01's hand-forward from being tribal knowledge."
  prohibitions:
    - "A COMP-12 corpus convergence failure must not be worked around: the corpus must not be narrowed, and the gate must not be skipped, marked flaky, or left unrun. A convergence failure is escalated to the owner per D-02 and ROADMAP binding constraint #5."
    - "No expected value anywhere in this phase may be derived from the new emitter's own output. Every changed expected value traces to a written-first derivation from the fixture's own conf.py and .rst read literally plus the emission contract."
    - "This phase must not trade one silent omission for another: no configuration may end up producing a PDF that is missing content its own toctree names, with no error and no warning, and no reintroduced write-time include decision may sit alongside the compile-time state guard."
    - "The `:numref:` numbering behaviour must not be silently changed, renumbered, or hidden. Both measured cases are recorded verbatim and handed forward to Phase 51 and Phase 52 per D-01."
---

<objective>
Prove the state-guarded composition holds at real corpus scale, close open question #2 by live
measurement in both of its cases, and walk the phase's five success criteria to a recorded verdict.

Purpose: SC#5 has two halves and both are measurements rather than implementations. The corpus half is
the milestone's own named residual risk — the design rests on Typst's multi-pass layout convergence,
measured on the diamond, interleaving, outline and label cases but never at corpus scale — and ROADMAP
binding constraint #5 fixes in advance that a failure there is a design-level finding rather than a
fixture bug. The `:numref:` half is open question #2, which research sharpened from "the numbers might
differ" into two mechanically distinct cases, the second of which produces no number at all and no
warning anywhere.

This plan is NOT autonomous: D-02 requires a convergence failure to stop the phase and escalate to the
owner, and D-01's fix-or-document call is recorded here for the owner's confirmation.

Output: the two-case `:numref:` fixture and its measurement gate, three new sections in
`49-EVIDENCE.md`, and a success-criteria walk. No file under `typsphinx/` is touched, and
`tests/test_corpus_gate.py` is not modified.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-CONTEXT.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-RESEARCH.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-PATTERNS.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-VALIDATION.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files and identifiers **created** by Phase 49 (new — not drift; do not flag as unknown
symbols during source-grounding).

**New production symbols (all created in 49-04, all module-level in `typsphinx/translator.py`):**
`INCLUDE_STATE_KEY`, `make_include_edge_key`, `derive_master_edge_keys`,
`render_include_edge_state`, `render_include_guard`,
`TypstTranslator._toctree_entry_occurrences`, `TypstBuilder._build_include_edge_map`,
`TypstBuilder._master_include_edges`, and a new `edge_keys` keyword parameter on
`TypstWriter.render_wrapper`.

**New test modules:** `tests/test_state_guard_composition_gate.py`,
`tests/test_state_guard_shapes_gate.py`, `tests/test_include_edge_derivation_unit.py`,
`tests/test_include_ledger_removal_gate.py`, `tests/test_state_guard_numref_gate.py`

**New fixture projects under `tests/fixtures/`:** `state_guard_two_master_gate`,
`state_guard_mirror_pair_gate`, `state_guard_self_and_url_gate`, `state_guard_cycle_gate`,
`state_guard_selfref_gate`, `state_guard_glob_gate`, `state_guard_orphan_ref_gate`,
`state_guard_three_master_gate`, `state_guard_substring_key_gate`,
`state_guard_numref_two_case_gate`

**New planning artifacts:** `49-EVIDENCE.md`, `49-EXPECTED-STRUCTURE.md`, `49-RED-EVIDENCE.md`,
`49-SHAPES-RED-EVIDENCE.md`

**Symbols this phase DELETES:** the build-scoped include-dedup ledger attribute declared in
`TypstBuilder.init()`, its per-`write()` reset, and the toctree visitor's ledger lookup, membership
check, skipped-duplicate debug log and set-add write.

<!-- planner-discipline-allow: _included_docnames -->

## Edge coverage authored in this plan

Row 25 of the ledger in 49-01 (COMP-12 boundary = the 154-document corpus convergence and the
stop-and-escalate protocol at that boundary) is authored as a `must_haves.truths` entry above, and row 26
(COMP-12 precision) is carried as the backstop truth in 49-01 and restated here as this plan's own
backstop with its scope narrowed to what a single green run can and cannot show. No row assigned to this
plan is dropped.

## Flagged planner assumptions

- **A green corpus run shows convergence for this corpus at this version pin, not convergence in
  general.** The backstop truth above states that scope explicitly, and PROJECT.md's named residual risk
  should stay named rather than be marked closed on the strength of one run. Say so in the recorded
  evidence rather than overclaiming.
- **Case (b) of the `:numref:` measurement may render in a form research did not fully trace.** Research
  read the fallback mechanism in Sphinx's own post-transform and confirmed it returns the reference's own
  node content with no warning, but did not trace exactly what text that node carries in typsphinx's
  pipeline (the raw label, or the figure's caption title). The fixture must therefore RECORD whatever text
  appears rather than assert a predicted string, and the recorded text becomes the documented limitation's
  content.
- **The corpus gate is slow and is excluded from the fast suite.** It must be run explicitly with the slow
  marker selected; a plan that runs only the fast suite has not measured COMP-12 at all.

<tasks>

<task type="auto">
  <name>Task 1: Run the GATE-02 full-corpus gate unmodified against the new composition and record the result with its runtime</name>
  <files>
    .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
  </files>
  <read_first>
    - tests/test_corpus_gate.py (the whole module — the corpus clone-and-cache helper, the conf-wiring helper, and the fatal-free render gate that is COMP-12's vehicle; this file must NOT be modified by this phase)
    - .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md lines 219-330 (Phase 48's D-11 corpus measurement — the ~28.9s / 28.6s before-numbers this plan's runtime is recorded against, and the accepted-limit section's shape)
    - .planning/ROADMAP.md lines 379-384 (binding constraint #5 verbatim: GATE-02 is an explicit success criterion of this phase and a convergence failure is a design-level finding)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-CONTEXT.md `<decisions>` D-02 in full (the stop-and-escalate protocol, and the explicit statement that a partial write-time fallback is NOT available because it would restore the two-competing-mechanisms shape Phase 48 spent a phase deleting)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md as it stands after 49-05 (this task APPENDS a new section; it must not overwrite any of the five existing sections)
    - .planning/PROJECT.md lines 134-140 (the named residual risk this measurement addresses and the scope in which it can be said to be addressed)
  </read_first>
  <precondition>49-05 has landed and the full suite is green on this tree: `uv run pytest -q` exits 0, and the corpus cache directory exists or the gate's own clone helper can populate it.</precondition>
  <action>
Provision the worktree first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then
run every command through `uv run`. Confirm isolation by printing the resolved package path — a corpus run
against the main checkout's copy would measure the wrong tree entirely.

Run the existing corpus gate UNMODIFIED, with the slow marker selected, and time it. Do not edit
`tests/test_corpus_gate.py`, do not narrow the corpus, do not pass a reduced document set, and do not
add a skip or a flaky marker. The gate's own file must appear in no diff this phase produces — that is the
mechanical form of D-02's rule, and it is checked in this task's acceptance criteria.

Append a `## Corpus convergence measurement` section to `49-EVIDENCE.md` recording:

- The exact command line, the verbatim captured output, the exit status, and the wall-clock runtime.
- The compiled artifact's own properties as the gate asserts them: the PDF exists, is non-empty, and
  begins with the PDF magic bytes. Record the byte size.
- The unsupported-node catalogue from the build output, recorded verbatim, and whether it is empty.
- The runtime beside Phase 48's recorded corpus numbers, with the delta stated as a number and a
  percentage. State explicitly that this phase has no cost-tier decision of its own — Phase 48's D-11
  tiers governed Phase 48's own change and measured its bottom tier — so a cost change here is RECORDED,
  not acted on. If the runtime has grown enough to look material, say so plainly and leave the judgement
  to the owner rather than proposing a remediation.
- A closing paragraph stating the scope of what the green run shows: convergence for THIS corpus at THIS
  version pin, not convergence in general. Note that PROJECT.md's named residual risk therefore stays
  named rather than being marked closed, and that the version pin is the thing to re-verify on a
  dependency bump.

**If the gate FAILS, do not iterate on it.** Per D-02 and binding constraint #5 this is a design-level
finding. Instead:

- Record the verbatim failure in the same section, with the full output and the exit status.
- Isolate the MINIMAL failing shape: bisect the corpus down to the smallest document subset that still
  reproduces the failure, and record the subset plus the reproduction command. Bisecting to isolate a
  finding is not the same as narrowing the gate to make it pass — the gate itself stays untouched and the
  bisection result is reported, not committed as the new gate.
- Write a `## STOP — design-level finding` subsection naming the failing shape, the mechanism as far as it
  can be determined, and the fact that D-02 assigns the design call to the owner.
- Do NOT introduce a partial write-time fallback for the failing shapes. D-02 states plainly that this
  option is not available: it would restore the exact two-competing-mechanisms shape Phase 48 spent a
  phase deleting and would bring the silent-omission failure class back in a second location.
- Stop the plan there and surface the finding. Do not proceed to Task 2 or Task 3, and do not mark the
  phase complete.
  </action>
  <verify>
    <automated>uv run pytest tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error -m slow -q && uv run python -c "
import pathlib,subprocess,sys
t=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md').read_text(encoding='utf-8')
missing=[n for n in ['## Corpus convergence measurement'] if n not in t]
d=subprocess.run(['git','diff','--name-only','HEAD','--','tests/test_corpus_gate.py'],capture_output=True,text=True).stdout.strip()
if d: missing.append('corpus gate file was modified: '+d)
print(missing); sys.exit(1 if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour: the corpus gate run exits 0 with the slow marker selected, OR the plan stops with a recorded design-level finding — never a modified gate, a narrowed corpus, a skip or a flaky marker.
    - `tests/test_corpus_gate.py` appears in no diff this phase produces: the second half of the verify command exits 0.
    - `49-EVIDENCE.md` contains a `## Corpus convergence measurement` section with the exact command line, the verbatim output, the exit status and the wall-clock runtime, and none of the five earlier sections is overwritten.
    - The section records the compiled PDF's byte size and states that it begins with the PDF magic bytes.
    - The section records the unsupported-node catalogue verbatim and states whether it is empty.
    - The runtime is recorded beside Phase 48's numbers with the delta as both an absolute figure and a percentage, and the section states explicitly that a cost change is recorded rather than acted on in this phase.
    - The section's closing paragraph states the scope of the result — this corpus, this version pin — and that PROJECT.md's residual risk stays named rather than closed.
    - If the gate failed: the section carries the verbatim failure, the minimal failing document subset with its reproduction command, and a `## STOP — design-level finding` subsection naming the owner as the decision-maker; and no write-time fallback was introduced anywhere.
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>The full corpus compiles fatal-free under the new composition through the existing gate run unmodified, with its runtime recorded against Phase 48's baseline and the result's scope stated honestly — or the run failed and the phase stopped with an isolated minimal failing shape escalated to the owner per D-02, with no fallback introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Build the two-case numref fixture, measure both cases live, and record both readings verbatim before writing any decision</name>
  <files>
    tests/fixtures/state_guard_numref_two_case_gate/conf.py,
    tests/test_state_guard_numref_gate.py,
    .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
  </files>
  <read_first>
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md `## Fixture specification` entry 10 (the ONLY source of the fixture's docnames, figure names, captions, reference sites and traversal positions)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-RESEARCH.md "Common Pitfalls" 5 and "Open Questions" 1 in full (the two-case procedure, the exact mechanism — the figure-number mapping is populated by a single walk starting only from the root document, so a figure reachable only through another master gets no entry at all — and the fallback path that returns the reference's own node content with no warning)
    - .venv/lib/python3.13/site-packages/sphinx/environment/collectors/toctree.py lines 285-378 (the figure-number assignment walk and the root-document-only entry point)
    - .venv/lib/python3.13/site-packages/sphinx/domains/std/__init__.py lines 1087-1170 and 1395-1422 (the numbered-reference resolution and the number lookup that raises for an unassigned target, and the silent fallback that catches it)
    - typsphinx/translator.py the figure visit and depart handlers (confirming no numbering override is emitted, so Typst's own per-compile figure counter is the second, independent numbering authority)
    - tests/test_pdf_render_gate.py lines 150-262 (the build helper and the compile-plus-PDF-readback idiom)
    - tests/fixtures/template_named_dir_master/conf.py (the two-master `typst_documents` shape)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-CONTEXT.md `<decisions>` D-01 in full (the fix-or-document rule, including that a null result is recorded to the same evidence standard)
  </read_first>
  <precondition>Task 1 recorded a passing corpus gate: `49-EVIDENCE.md` contains a `## Corpus convergence measurement` section with no `## STOP — design-level finding` subsection.</precondition>
  <action>
Create `tests/fixtures/state_guard_numref_two_case_gate/` exactly as 49-EXPECTED-STRUCTURE.md
specification entry 10 fixes it, with figure numbering enabled in its configuration. The required shape,
restated so a divergence from the specification is visible:

- Two masters declared as well-formed four-tuple entries, with the root document being the first master.
- **Case (a) subject:** one document holding a named, captioned figure that is reachable from BOTH masters,
  at DIFFERENT traversal positions — reached through an intermediate document under the root master and
  directly under the second master, so its position in each master's traversal differs. A numbered
  reference to that figure appears in each master's own content, so each compiled PDF carries one.
- **Case (b) subject:** a second document holding its own named, captioned figure, toctree'd ONLY by the
  non-root master and reachable from the root document by no path at all. A numbered reference to it
  appears in the non-root master's own content.
- Enough filler content ahead of each figure that Typst's own per-compile figure counter and Sphinx's own
  project-wide numbering cannot coincide by accident — the fixture must be able to show a divergence if one
  exists, rather than producing the same number by luck.
- A load-bearing-properties comment block naming: that figure numbering must stay enabled; that the case
  (a) figure must stay reachable from both masters at DIFFERENT positions, because equal positions would
  dissolve case (a); that the case (b) figure must stay unreachable from the root document, because any
  path from the root assigns it a number and dissolves case (b); that both figures must keep their names,
  because the references target them; and that the intermediate document under the root master must stay,
  because it is what makes the two positions differ.

Author `tests/test_state_guard_numref_gate.py` as a MEASUREMENT gate rather than an assertion gate for the
numbering itself. Its docstring must say so: the numbering divergence is an open question this phase
closes by measurement, and D-01 fixes in advance that a divergence is documented rather than fixed here, so
this module asserts only what is invariant (the build succeeds, both PDFs exist, both references render
SOME text, the figures render with SOME Typst-assigned number) and EXTRACTS the rest for the record.

Per case, per master, extract and record: the numbered reference's rendered text as it appears in the
compiled PDF, and the target figure's own Typst-assigned caption number as it appears in the same PDF.
Capture the full Sphinx build warning list too, because case (b)'s defining property is that it produces no
warning — the absence must be recorded as an observation, not assumed.

For case (b), record whatever text actually appears rather than asserting a predicted string: research
traced the fallback mechanism but not the exact node content typsphinx's pipeline renders in that
position, and the recorded text becomes the documented limitation's content.

Then append a `## numref measurement` section to `49-EVIDENCE.md`. Write the extracted values FIRST, as a
table with one row per case per master carrying the reference's rendered text, the figure's Typst-assigned
number, and whether they agree — and only after that table is written, write the reading. This ordering is
deliberate: a decision written before the numbers is a framing of the evidence rather than a reading of it.

The reading must cover: whether case (a) diverges and by how much; whether case (b) produced a number at
all and what text the reference rendered instead; and whether any warning was emitted in either case. Then
record the fix-or-document decision, which D-01 already fixes: a divergence is recorded as a documented
limitation and handed forward to Phase 51 (documentation) and Phase 52 (CHANGELOG), NOT fixed in this
phase. State the mechanism in one paragraph so the later phases can write it up without re-deriving it:
Sphinx numbers project-wide from a single walk rooted at the root document, while Typst counts per compiled
wrapper, and no compile error exists to catch the difference. If the measurement shows NO divergence,
record that null result with the same evidence standard and state explicitly that D-01's hand-forward
therefore carries a null finding rather than nothing at all.

Add nothing to the production package. No renumbering mechanism, no numbering override on the emitted
figure, no change to figure emission of any kind — D-01 keeps that out of this phase, and the phase's diff
must show it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_state_guard_numref_gate.py -q && uv run python -c "
import pathlib,subprocess,sys
t=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md').read_text(encoding='utf-8')
missing=[n for n in ['## numref measurement'] if n not in t]
sec=t.split('## numref measurement')[1]
rows=[l for l in sec.splitlines() if l.strip().startswith('|') and '---' not in l]
if len(rows)<4: missing.append('numref table has fewer than 3 measurement rows')
d=subprocess.run(['git','diff','--name-only','HEAD','--','typsphinx/'],capture_output=True,text=True).stdout.strip()
if d: missing.append('production package modified: '+d)
print(missing); sys.exit(1 if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The fixture directory exists with a `conf.py`, figure numbering enabled, two well-formed master entries, and every document specification entry 10 names.
    - `grep -c 'Load-bearing properties' tests/fixtures/state_guard_numref_two_case_gate/conf.py` returns at least 1, and the block names the both-masters-different-positions property and the unreachable-from-root property.
    - Behaviour: `uv run pytest tests/test_state_guard_numref_gate.py -q` exits 0, and the build it drives exits 0 with both PDFs produced.
    - The case (b) figure is reachable from no path starting at the root document: the fixture's root master's toctree, transitively, does not name that document — asserted by the gate, not only by inspection.
    - `49-EVIDENCE.md` contains a `## numref measurement` section whose FIRST content is the extracted-values table, with one row per case per master carrying the reference's rendered text, the figure's Typst-assigned number and an agreement verdict.
    - The section records the full Sphinx warning list for the fixture and states explicitly whether any warning was emitted for either case.
    - The section records the fix-or-document decision as D-01 fixes it — documented limitation, handed to Phase 51 and Phase 52, not fixed here — or records a null result to the same evidence standard.
    - The production package is untouched by this task: the second half of the verify command exits 0, and no numbering override appears in the emitted figure output.
    - `uv run black --check tests/` exits 0 and `uv run python -m ruff check tests/` exits 0.
  </acceptance_criteria>
  <done>Open question #2 is closed by live measurement in both of its cases, with the extracted reference texts and Typst-assigned numbers recorded verbatim before any reading was written, the warning absence recorded as an observation, and D-01's documented-limitation hand-forward named against Phase 51 and Phase 52 — with no numbering mechanism added to the production package.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner review — the corpus convergence result, the numref decision, and the success-criteria walk</name>
  <what-built>
The phase is complete and green: the include decision now resolves at Typst compile time from a per-master
edge set each wrapper publishes, one shared content file serves every master that includes it, the
build-scoped ledger is gone and enforced gone by a committed gate, all seven degenerate shapes match their
plan-time-decided outcomes, the full Sphinx corpus compiles fatal-free under the new composition, and open
question #2 is closed by a live two-case measurement.

Two items need the owner's eyes rather than a test's, and both were fixed in advance as owner decisions:

1. **The corpus convergence result and its cost.** D-02 and ROADMAP binding constraint #5 assign the
   design call at this boundary to the owner. The measurement is recorded in `49-EVIDENCE.md`'s
   `## Corpus convergence measurement` section with its runtime beside Phase 48's baseline. Even on a pass,
   the owner should see the runtime delta and confirm that recording it (rather than acting on it) is the
   right call for this phase — this phase has no cost-tier decision of its own.

2. **The `:numref:` fix-or-document decision.** D-01 fixes the answer in advance — a divergence is
   documented and handed to Phase 51 and Phase 52, not fixed here — but the measured content of that
   limitation is new information, including case (b)'s rendered fallback text, which research could not
   predict. The owner should confirm the recorded limitation says what they want Phase 51 to document and
   Phase 52 to announce.

Before this checkpoint the executor also walks the phase's five success criteria one by one in the summary,
naming for each the artifact and the measurement that discharges it, and naming any criterion not
discharged rather than omitting it. The standing green bar is re-measured and pasted verbatim: the full test
suite, the formatter check, the linter check and the type checker.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md` —
   specifically `## Corpus convergence measurement` (the runtime and its delta against Phase 48's ~28.9s /
   28.6s baseline, and the scope paragraph) and `## numref measurement` (the extracted-values table FIRST,
   then the reading and the decision).

2. Confirm the corpus gate was run unmodified:
   `git diff --name-only HEAD~1 -- tests/test_corpus_gate.py` prints nothing, and the recorded command line
   selects the slow marker over the unmodified gate.

3. Re-run the two headline measurements yourself if you want them independently:
   - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
   - `uv run pytest tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error -m slow -q`
   - `uv run pytest tests/test_state_guard_composition_gate.py tests/test_state_guard_shapes_gate.py tests/test_include_ledger_removal_gate.py -q`

4. Confirm the phase's headline behaviour with your own eyes if you like: build
   `tests/fixtures/state_guard_two_master_gate` through the PDF builder and confirm BOTH PDFs contain the
   shared chapter — against the recorded pre-fix baseline in `49-RED-EVIDENCE.md` where one of them
   contained it zero times.

5. Read the success-criteria walk in the plan summary and confirm each of the five criteria has an artifact
   and a measurement behind it.

6. Decide the two items: (a) accept the corpus result and its recorded cost, or escalate; (b) accept the
   recorded `:numref:` limitation as the text Phase 51 documents and Phase 52 announces, or amend it.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or name which of the two items to change and how.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party corpus source (`conf.py` and `.rst` from the Sphinx documentation tree) → the emitter | The corpus gate builds a large external documentation tree this project does not author, which is the point: it exercises document shapes the fixtures do not |
| Emitted `.typ` → `typst.compile()` at corpus scale | The multi-pass layout convergence the state-guarded design rests on is exercised here at a scale no fixture reaches |

## STRIDE Threat Register

ASVS level 1, blocking threshold `high`. ASVS **V2 (Authentication)**, **V3 (Session Management)**,
**V4 (Access Control)** and **V6 (Cryptography)** are **structurally inapplicable** — typsphinx is a
local Sphinx/Typst document-generation tool with no network service, no authentication, no session and
no cryptographic surface. **V5 (Input Validation)** is the one real seam this phase introduces, mitigated
in 49-04 and verified in 49-05. Recorded rather than omitted, per the phase's security contract.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-03 | Denial of Service | Corpus-scale Typst multi-pass layout convergence over the new state/context graph | medium | mitigate | Measured here against the existing gate run unmodified, with the runtime recorded beside Phase 48's baseline. A convergence failure is escalated per D-02 with the minimal failing shape isolated, never worked around by narrowing the corpus |
| T-49-10 | Tampering | The corpus corpus clone, fetched from a third party and cached locally | low | accept | The gate's existing clone-and-cache helper is unchanged by this phase, pins a specific Sphinx version tag, and builds documentation only — no code from the corpus is executed beyond that documentation project's own configuration, which is the pre-existing accepted posture of this gate since it was introduced |
| T-49-11 | Information Disclosure | A numbered reference silently rendering unrelated text, misleading a reader of the generated PDF | medium | mitigate | Measured in both cases here rather than left latent, recorded verbatim, and handed forward as a documented limitation per D-01 so a reader of the documentation is warned even though no compile error exists to warn them |
| T-49-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no packages of any kind. `49-RESEARCH.md` §Package Legitimacy Audit records no new PyPI dependency and no new `@preview` package; ROADMAP binding constraint #7 forbids both |
</threat_model>

<verification>
- `uv run pytest tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error -m slow -q`
  exits 0, or the phase stopped with a recorded design-level finding.
- `git diff --name-only HEAD -- tests/test_corpus_gate.py` prints nothing.
- `uv run pytest tests/test_state_guard_numref_gate.py -q` exits 0.
- `git diff --name-only HEAD -- typsphinx/` prints nothing for this plan.
- `uv run pytest -q` exits 0, `uv run black --check .` exits 0, `uv run python -m ruff check .` exits 0,
  and `uv run python -m mypy typsphinx/` exits 0.
- `49-EVIDENCE.md` carries seven sections with none of the earlier ones overwritten.
</verification>

<success_criteria>
- The full Sphinx corpus compiles fatal-free through the PDF builder under the new composition, measured
  with the existing gate run unmodified, with a valid non-empty PDF and an empty unsupported-node
  catalogue.
- The corpus runtime is recorded beside Phase 48's baseline with the delta stated, and the result's scope —
  this corpus, this version pin — is stated honestly rather than overclaimed as general convergence.
- A convergence failure, if it occurs, stops the phase with the minimal failing shape isolated and
  escalated to the owner, with no narrowed corpus, no skipped gate and no write-time fallback.
- Open question #2 closes on a live two-master fixture in BOTH of its cases, with the extracted reference
  texts and Typst-assigned numbers recorded verbatim before any reading was written.
- The `:numref:` outcome is recorded as a documented limitation handed to Phase 51 and Phase 52 per D-01 —
  or as a null result to the same evidence standard — with no numbering mechanism added to the production
  package.
- The phase's five success criteria are walked one by one with the artifact and measurement that discharges
  each, and the owner has reviewed the corpus result and the `:numref:` decision.
</success_criteria>

<output>
Create `.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-06-SUMMARY.md` when done
</output>