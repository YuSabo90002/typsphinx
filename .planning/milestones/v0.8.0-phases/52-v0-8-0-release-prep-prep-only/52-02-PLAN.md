---
phase: 52-v0-8-0-release-prep-prep-only
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - CHANGELOG.md
  - docs/source/changelog.rst
  - tests/test_changelog_page_gate.py
autonomous: true
requirements: [REL-07]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`CHANGELOG.md` carries a curated `## [0.8.0]` entry whose bullets cover every one of this milestone's 23 delivered v1 requirements (COMP-01..12, OUT-01..03, XREF-03, XREF-04, BLD-02..04, IMG-01, IMG-02, DOC-14), cut at user-visible-change granularity with requirement IDs in trailing parentheses inside the bold span (D-07)."
    - "The lead paragraph's axis is the milestone goal itself — a `typst_documents` configuration declaring more than one master now produces a complete PDF for each of them — and the breaking-change declaration lives in the SECOND half of that same paragraph (D-05)."
    - "Breaking changes are marked exactly two ways: the lead-paragraph declaration, and a `**Breaking:**` prefix on each of the three affected bullets — the wrapper/content split, the target-as-path reversal, and the collision hard error, the last of which stops a build that used to succeed (D-04)."
    - "The `## [0.8.0]` entry has no `### Removed` heading, because the milestone diff of `typsphinx/__init__.py` contains zero `add_config_value` additions or removals — an empty section is not emitted (D-04)."
    - "`### Verified` carries the same three items as the `## [0.7.0]` and `## [0.7.1]` entries, unchanged in wording; SC#4's config invariant and SC#3's round-trip evidence are NOT promoted into it (D-06)."
    - "The two behaviours Phase 49 measured and Phase 51 documented appear inside the descriptive bullets rather than in a limitations section (D-02): (a) a content file compiled standalone yields only its own body, its state-guarded children absent, with no error or warning; (b) a document reachable from several masters renders once in each master's PDF, at that master's own traversal position, with its heading level varying per master."
    - "No limitations section and no defect disclosure for the four minor defects appears anywhere in the entry — they ship unfixed and are disclosed internally only, in `52-HANDOFF.md` (D-01)."
    - "The cross-reference role 51-CONTEXT D-07 excludes appears nowhere in the `## [0.8.0]` entry (ROADMAP SC#2's explicit exclusion, owner decision 2026-08-14)."
    - "The tail link block rolls over: a `[0.8.0]` release-tag line is inserted immediately above the `[0.7.1]` line, and the final `[Unreleased]` line's compare base advances to `v0.8.0...HEAD` (ROADMAP SC#2)."
    - "`docs/source/changelog.rst` was read IN FULL and confirmed to still render the repo-root file live through DOC-12's `.. include::` mechanism, and its \"Migrating from 0.7.x to 0.8.0\" section is confirmed to already carry all three `**Breaking:**` items — measured 2026-08-15 at lines 15, 36 and 57 — so no second hand edit is owed unless that measurement no longer holds (ROADMAP SC#2, RESEARCH Open Question 1)."
    - "`\"0.8.0\"` joins `RELEASE_VERSIONS` in `tests/test_changelog_page_gate.py` and the comment above it moves from 13 to 14 and from \"0.4.4 through 0.7.1\" to \"0.4.4 through 0.8.0\" — and this append lands STRICTLY AFTER the `## [0.8.0]` heading exists, because the gate asserts every listed release's content appears in the built page (RESEARCH Pitfall 2)."
    - "`uv run python scripts/extract_changelog_section.py 0.8.0` exits 0 with a non-empty body, satisfying `release.yml`'s `validate` job precondition — run as a precondition check, never as this plan's acceptance."
    - "Nothing under `typsphinx/` changed in this plan (D-01, prep-only fence)."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "the curated `## [0.8.0]` release entry and the rolled-over tail link block"
      contains: "## [0.8.0]"
    - path: "tests/test_changelog_page_gate.py"
      provides: "the RELEASE_VERSIONS tuple extended to 14 entries so the published page is held to the current bar"
      contains: "\"0.8.0\""
    - path: "docs/source/changelog.rst"
      provides: "the published changelog page, confirmed still delegating to the repo-root file"
      contains: "Migrating from 0.7.x to 0.8.0"
  key_links:
    - from: "CHANGELOG.md `## [0.8.0]` heading"
      to: "release.yml's `validate` job"
      via: "scripts/extract_changelog_section.py positionally extracts the section body and exits non-zero if the heading is missing or the body empty, failing the whole release before build/publish-pypi"
      pattern: "extract_changelog_section"
    - from: "CHANGELOG.md"
      to: "docs/source/changelog.rst"
      via: "DOC-12's `.. include::` of the repo-root file propagates the release history to the published page automatically, so only the gate's version tuple is hand-maintained"
      pattern: "\\.\\. include::"
    - from: "RELEASE_VERSIONS tuple"
      to: "the built HTML and PDF changelog pages"
      via: "TestChangelogPageContentCoverage asserts each listed release appears in the built page, which is why the tuple append must follow the heading, never precede it"
      pattern: "RELEASE_VERSIONS"
  prohibitions:
    - statement: "No git tag named v0.8.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged; a PR would also fire CI a second, unintended way and is the publish half's first step."
      status: unverified
      verification: flagged
    - statement: "No second tag is created on the typsphinx-doc-translations repository and its pin is not advanced."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed — the four minor defects this milestone's own reviews filed ship unfixed per D-01."
      status: unverified
      verification: flagged
    - statement: "No historical CHANGELOG entry is edited; only the new section and the two tail link-block lines move."
      status: unverified
      verification: flagged
    - statement: "REL-07's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped; the requirement stays Pending until the publish."
      status: unverified
      verification: flagged
    - statement: "No test gate is weakened — no skip guard loosened, no marker removed, no assertion deleted — to make a run report green."
      status: unverified
      verification: flagged
    - statement: "No figure is transcribed from the phase's CONTEXT or RESEARCH document into an acceptance criterion or an evidence file; every measured value is re-run live at execution time."
      status: unverified
      verification: flagged
    - statement: "No prohibition above is read as forbidding the two reversible actions D-08 depends on — a plain fast-forward push of the milestone branch to origin, and a workflow_dispatch of ci.yml on that branch."
      status: unverified
      verification: flagged
  flagged_assumptions:
    - statement: "Edge probe row (category: precision, REL-07) is UNRESOLVED. Planner reading: the phase's precision surface is the semantic-version ORDERING that two artifacts depend on — the CHANGELOG tail link block, where the `[0.8.0]` line must sit above `[0.7.1]` in descending release order and the `[Unreleased]` compare base must name the newest tag, and the `RELEASE_VERSIONS` tuple, whose append must land at the END so the sequence stays ascending. Neither artifact carries a machine-enforced ordering contract: nothing in the repository parses either as a version sequence, so a mis-ordered insert would be accepted silently. This plan's acceptance criteria pin the ordering positionally (line-adjacency and last-element assertions), which is the strongest check available without inventing a comparator, but no rounding/tie-breaking or comparator contract is specified anywhere and none is invented here. Recorded as an explicit flagged assumption, not auto-backstopped and not dismissed."
      status: unresolved
      verification: none
      source: "spec-less probe fallback section A — no 52-SPEC.md supplied a ## Edge Coverage section"
    - statement: "Edge probe row (category: boundary, REL-07) is UNRESOLVED and is OWNED BY PLAN 52-01, where the version-literal boundary 0.7.1 -> 0.8.0 lives. It is authored in full in 52-01's own flagged_assumptions block and is named here only so the pair is visible from this plan. It is not dropped."
      status: unresolved
      verification: none
      source: "spec-less probe fallback section A — no 52-SPEC.md supplied a ## Edge Coverage section"
---

<objective>
Author the curated `## [0.8.0]` CHANGELOG entry, roll the tail link block over, confirm the published
changelog page still renders live from the repo-root file, and extend the page gate's release tuple —
in that order, because the tuple append is only valid once the heading exists.

Purpose: discharges ROADMAP Phase 52 SC#2. The entry's shape is fixed by five locked decisions —
D-04 (two-way breaking marking, no `### Removed` heading), D-05 (the lead paragraph's axis is the
milestone goal, with the breaking declaration in its second half), D-06 (`### Verified` unchanged from
0.7.0/0.7.1), D-07 (bullets at user-visible-change granularity with requirement IDs), and D-02 (the
two documented behaviours ride inside descriptive bullets). D-01 keeps the four minor defects out of
the entry entirely.

Output: a `CHANGELOG.md` the release machinery can extract, a rolled-over tail link block, a confirmed
published page, and a 14-entry `RELEASE_VERSIONS` tuple.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-CONTEXT.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

The phase-level list lives in `52-01-PLAN.md` § "Artifacts this phase produces". This plan produces:

- `CHANGELOG.md` — a new `## [0.8.0] - <date>` heading with `### Added`, `### Changed`, `### Fixed`
  and `### Verified` subsections (no fifth subsection), plus two tail link-block lines: the new
  `[0.8.0]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.8.0` line and the edited
  `[Unreleased]` compare line.
- `tests/test_changelog_page_gate.py` — `"0.8.0"` appended to `RELEASE_VERSIONS` (13 → 14 entries),
  and the comment above it moved to 14 / "0.4.4 through 0.8.0".
- `docs/source/changelog.rst` — read-and-confirm only; no edit is expected.

<!-- planner-discipline-allow: numref -->
<!-- planner-discipline-allow: ### Removed -->
<!-- planner-discipline-allow: Known Limitations -->

<tasks>

<task type="auto">
  <name>Task 1: Read the published changelog page in full and resolve whether a third migration item is owed</name>
  <files>docs/source/changelog.rst</files>
  <precondition>Plan 52-01 has merged back, so HEAD carries `version = "0.8.0"` in `pyproject.toml`. This is an ordering dependency (tracer-first), not a data dependency — nothing in this task reads the version literal.</precondition>
  <reversibility rating="reversible">A documentation edit, if one turns out to be owed at all.</reversibility>
  <read_first>
    - `docs/source/changelog.rst` — ALL 343 lines, not merely the first 40. This is the point of the task: RESEARCH Open Question 1 was raised because a capped read saw only two `**Breaking:**` items under a heading whose own opening sentence says "three breaking changes".
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md` § "Pattern 2" and § "Open Questions" item 1.
    - `.planning/phases/51-two-layer-output-documentation/51-CONTEXT.md` § D-07 — binding on this phase: the cross-reference role it excludes appears in no published v0.8.0 surface.
    - `.planning/REQUIREMENTS.md` § "v1 Requirements" — the 23 delivered IDs Task 2's bullets must cover.
  </read_first>
  <action>
Read `docs/source/changelog.rst` in full and record two facts with the commands that produced them.

**Fact 1 — the delegation mechanism is intact.** DOC-12's design is that the published page
`.. include::`s the repo-root `CHANGELOG.md` (through MyST) rather than maintaining a second hand-written
release history, so a new entry propagates automatically. Confirm the include directive is still present
and still points at the repo-root file, and that no hand-maintained per-release history has crept back
onto the page. `tests/test_changelog_page_gate.py::TestPublishedChangelogPageDelegates` asserts exactly
this and always runs — run it and paste the transcript.

**Fact 2 — how many `**Breaking:**` items the "Migrating from 0.7.x to 0.8.0" section carries.** The
planner measured this section on 2026-08-15 and found **three** items, at lines 15 (the output shape),
36 (the target-as-path reversal) and 57 (the collision hard error, with a worked `ExtensionError`
transcript at line 75). That resolves RESEARCH Open Question 1: **no third item is owed and no hand edit
to this file is expected.** Re-measure it anyway with `grep -n 'Breaking' docs/source/changelog.rst`,
because the tree has moved since. If — and only if — the collision hard error is genuinely absent from
that section on the live tree, add a third item in the same shape as the two beside it: a bold
`**Breaking:**` lead, a one-sentence statement of the change, and a `.. code-block:: text` fragment
showing the `conf.py` a user has today and what it now produces. Adding it is IN scope (it is a
documentation edit, not a `typsphinx/` code change). If it is present, change nothing in this file and
record that plainly.

Record both facts in this task's SUMMARY with their commands and output verbatim. Do not edit
`CHANGELOG.md` in this task — that is Task 2, and keeping the two separate is what makes the migration-guide
reconciliation independently auditable.
  </action>
  <verify>
    <automated>uv run pytest tests/test_changelog_page_gate.py::TestPublishedChangelogPageDelegates -q && test "$(grep -c 'Breaking' docs/source/changelog.rst)" -ge 3 && grep -q 'Migrating from 0.7.x to 0.8.0' docs/source/changelog.rst</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_changelog_page_gate.py::TestPublishedChangelogPageDelegates -v` reports all tests passed with zero skips.
    - `grep -n 'Migrating from 0.7.x to 0.8.0' docs/source/changelog.rst` returns exactly one line.
    - The "Migrating from 0.7.x to 0.8.0" section contains three items whose lines begin with `- **Breaking:**`, covering the output shape, the target-as-path reversal, and the collision hard error respectively — either because they were already there (the expected outcome) or because the third was added by this task.
    - `grep -n 'collision' docs/source/changelog.rst` returns at least one line inside the "Migrating from 0.7.x to 0.8.0" section.
    - `git diff --name-only` lists either nothing, or exactly `docs/source/changelog.rst` and no other path.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>The published changelog page is confirmed to still delegate to the repo-root file, and the migration guide is confirmed (or made) to carry all three breaking-change items the `## [0.8.0]` entry will state.</done>
</task>

<task type="auto">
  <name>Task 2: Author the curated `## [0.8.0]` entry and roll the tail link block over</name>
  <files>CHANGELOG.md</files>
  <precondition>Task 1 has confirmed which breaking-change items the published migration guide states, so the bullets authored here can be written to agree with it rather than diverge from it.</precondition>
  <reversibility rating="reversible">Prose in a tracked markdown file; a single `git revert` restores it.</reversibility>
  <read_first>
    - `CHANGELOG.md` lines 1-100 — the `## [Unreleased]` block (whose body is a "Planned for Future Releases" list that stays exactly where it is, above the new heading) and the `## [0.7.1]` entry, which is the structural template: lead paragraph, then `### Added`, `### Changed`, `### Fixed`, `### Removed`, `### Verified`.
    - `CHANGELOG.md` — the last 30 lines, the tail link block, ending `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.7.1...HEAD`.
    - The `## [0.7.0]` entry's `### Verified` section — D-06 requires the same three items, unchanged in wording, so read both 0.7.0's and 0.7.1's and copy the settled text.
    - `.planning/REQUIREMENTS.md` § "v1 Requirements" — all 24 IDs and their one-line descriptions.
    - `.planning/PROJECT.md` § "Current Milestone: v0.8.0 multi-master composition" — the milestone goal sentence D-05 makes the lead paragraph's axis, and the "User-visible output-shape change" bullet instructing the entry to distinguish this change from v0.7.1's own rename.
    - `docs/source/user_guide/output_layout.rst` line 51 — the standalone-content-file prose D-02 item (a) folds into a bullet.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md` § "2. `CHANGELOG.md`" — the skeleton, the bullet vocabulary, and the exact two-line tail rollover.
  </read_first>
  <action>
Insert a new `## [0.8.0] - <date>` section immediately BELOW the `## [Unreleased]` block and ABOVE the
`## [0.7.1]` heading. Date it with today's UTC date from `date -u +%F`; `52-HANDOFF.md` carries a
checklist item to re-date it at the publish if the merge slips past midnight. Leave the
`## [Unreleased]` body untouched — its "Planned for Future Releases" list is not v0.8.0 material.

**Lead paragraph (D-05, D-04).** Two to four sentences. Its axis is the milestone goal itself: a
`typst_documents` configuration declaring more than one master now produces a complete PDF for each of
them. Put the breaking-change declaration in the SECOND half of the same paragraph, reusing v0.7.1's own
vocabulary shape — v0.7.1 wrote "**this patch release can break a working configuration**"; write the
minor-release equivalent and point the reader at the `### Changed` section and the "Migrating from 0.7.x
to 0.8.0" guide in the published documentation. Do not open with the output-shape change and do not tell
the release as a defect list; both framings were weighed and declined.

**Bullets (D-07, D-04, D-02).** Cut at user-visible-change granularity, requirement IDs trailing,
parenthesized and comma-separated INSIDE the bold span. Nine bullets, assigned as follows — the wording
is yours, the cut and the ID assignment are not:

Under `### Added`:
1. Multi-master composition — every master now gets its own complete PDF, built from a per-master
   include graph published as Typst `state` (COMP-05, COMP-06, COMP-07, COMP-09, COMP-10, COMP-12).
   Fold D-02 item (b) into this bullet: a document reachable from several masters renders once in each
   master's PDF, at that master's own traversal position, with its heading level varying per master.
2. Compile-time cross-reference degradation — a reference whose target label is absent from the
   compiling master degrades to plain text instead of aborting the compile (XREF-03, XREF-04).
3. The published documentation now describes the two-layer output — which file to compile, what a
   standalone content-file compile does, and target-as-path semantics (DOC-14).

Under `### Changed`:
4. `**Breaking:**` the output shape — one `typst_documents` entry now writes TWO files: a thin wrapper
   at the resolved target and a template-less `<docname>.typ` content file (COMP-01, COMP-02, COMP-11,
   OUT-03). Name the measured before/after file pair for a concrete config, agreeing with the migration
   guide Task 1 confirmed, and distinguish this change from v0.7.1's own `index.typ` → `<project>.typ`
   rename. Fold D-02 item (a) into this bullet: a content file compiled standalone yields only its own
   body, its state-guarded children absent, with no error or warning.
5. `**Breaking:**` the target-as-path reversal — a target containing a path separator is honoured
   as-is relative to the output directory (OUT-01, OUT-02). State it as a deliberate reversal of
   v0.7.1 Phase 44's D-05/D-06/D-07 and say that the security half is retained: a target escaping the
   output directory is still refused with a warning and a safe fallback.
6. `**Breaking:**` the collision hard error — a configuration whose wrapper target resolves onto a
   content file's own path now raises instead of silently dropping a master's body (BLD-02, BLD-03,
   BLD-04). Say plainly that this stops a build that used to succeed, for the most common configuration
   shape.

Under `### Fixed`:
7. A master that is also another master's toctree child now builds, and an included master no longer
   re-expands its template's title page and outline into the middle of the parent's body (COMP-03,
   COMP-04).
8. Prose written before and after a toctree keeps its position relative to the included content
   (COMP-08).
9. The two image-path defects — a converted image rehomed under the images directory no longer collides
   with a real source image of the same basename, and an absolute image URI outside the doctree
   directory no longer causes the copy step to write outside the output directory (IMG-01, IMG-02).

That accounts for all 23 delivered v1 requirements. REL-07 is the release itself and takes no bullet.

**Section discipline.** Exactly three `**Breaking:**` prefixes appear, on bullets 4, 5 and 6 — that is
D-04's second marking channel, and no new heading is invented for it. Emit NO fifth subsection: the
milestone diff of `typsphinx/__init__.py` contains zero `add_config_value` additions or removals, so
the removed-items heading v0.7.1 carried has no candidate here and an empty heading must not be
emitted. Emit no limitations heading either — D-01 keeps the four minor defects out of every published
surface, and D-02 deliberately routes the two documented behaviours into the descriptive bullets so
they read as intended behaviour rather than as warnings.

**`### Verified` (D-06).** Copy the same three items the `## [0.7.0]` and `## [0.7.1]` entries carry,
unchanged in wording: no new runtime dependencies across the full milestone diff; the four bundled
`@preview` package version strings unchanged across all four sync surfaces; the full-corpus Sphinx
`doc/` `-b typstpdf` re-run remains fatal-free. Do not add a fourth item for the config invariant and
do not add a fifth for the round-trip evidence — those live in this phase's own evidence artifacts.

**The cross-reference role excluded by 51-CONTEXT D-07 must not appear anywhere in this entry** — not
in a bullet, not in the lead paragraph, not in a parenthetical. It is classified as a bug for a later
milestone and is tracked only in `.planning/todos/pending/`.

**Tail link block.** Insert one new line immediately ABOVE the existing `[0.7.1]:` line:
`[0.8.0]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.8.0`. Then edit the final line so
its compare base reads `v0.8.0...HEAD` instead of `v0.7.1...HEAD`. Those are the only two lines in the
tail block that move; every historical release line stays byte-identical.

Finally, run `uv run python scripts/extract_changelog_section.py 0.8.0` as a PRECONDITION CHECK — it
must exit 0 with a non-empty body, which is what `release.yml`'s `validate` job will require before any
tag is ever pushed. Capture the transcript. Take no irreversible action.
  </action>
  <verify>
    <automated>uv run python scripts/extract_changelog_section.py 0.8.0 > /dev/null && test "$(awk '/^## \[0\.8\.0\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -c '\*\*Breaking:\*\*')" = "3" && test "$(awk '/^## \[0\.8\.0\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -c '### Removed')" = "0" && test "$(awk '/^## \[0\.8\.0\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -c 'numref')" = "0" && grep -q '^\[Unreleased\]: https://github.com/YuSabo90002/typsphinx/compare/v0\.8\.0\.\.\.HEAD$' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## \[0\.8\.0\] - ' CHANGELOG.md` returns `1`, and the date token matches `date -u +%F`'s output on the execution day.
    - `grep -n '^## \[' CHANGELOG.md | head -3` shows `## [Unreleased]` first, `## [0.8.0]` second and `## [0.7.1]` third — the new entry sits between them.
    - Within the `## [0.8.0]` section only (extracted with `awk '/^## \[0\.8\.0\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md`): `grep -c '^### '` returns `4`; the four headings are `### Added`, `### Changed`, `### Fixed`, `### Verified` in that order.
    - Within that same extracted section: `grep -c '### Removed'` returns `0`; `grep -ci 'Known Limitations'` returns `0`; `grep -c 'numref'` returns `0`.
    - Within that same extracted section: `grep -c '\*\*Breaking:\*\*'` returns exactly `3`.
    - Within that same extracted section, each of the 23 requirement IDs `COMP-01`, `COMP-02`, `COMP-03`, `COMP-04`, `COMP-05`, `COMP-06`, `COMP-07`, `COMP-08`, `COMP-09`, `COMP-10`, `COMP-11`, `COMP-12`, `OUT-01`, `OUT-02`, `OUT-03`, `XREF-03`, `XREF-04`, `BLD-02`, `BLD-03`, `BLD-04`, `IMG-01`, `IMG-02`, `DOC-14` appears at least once; `REL-07` appears zero times.
    - Within that same extracted section, the `### Verified` subsection's three bullets are byte-identical (modulo leading whitespace) to the `## [0.7.1]` entry's own three `### Verified` bullets.
    - `grep -c '^\[0\.8\.0\]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0\.8\.0$' CHANGELOG.md` returns `1`, and `grep -n '^\[0\.8\.0\]:\|^\[0\.7\.1\]:' CHANGELOG.md` shows the `[0.8.0]` line's number is exactly one less than the `[0.7.1]` line's.
    - `grep -c '^\[Unreleased\]: https://github.com/YuSabo90002/typsphinx/compare/v0\.8\.0\.\.\.HEAD$' CHANGELOG.md` returns `1`, and `grep -c 'compare/v0\.7\.1\.\.\.HEAD' CHANGELOG.md` returns `0`.
    - `git diff CHANGELOG.md` shows no deletion of any line belonging to a `## [0.7.1]` or earlier entry, and no change to any tail link line other than the inserted `[0.8.0]` line and the edited `[Unreleased]` line.
    - `uv run python scripts/extract_changelog_section.py 0.8.0` exits 0 and its stdout is non-empty.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>`CHANGELOG.md` carries a curated, extractable `## [0.8.0]` entry covering all 23 delivered v1 requirements with exactly three breaking bullets and four subsections, and the tail link block has rolled over.</done>
</task>

<task type="auto">
  <name>Task 3: Extend RELEASE_VERSIONS to 14 entries and prove the built page carries the new release</name>
  <files>tests/test_changelog_page_gate.py</files>
  <precondition>`CHANGELOG.md` already contains a `## [0.8.0]` heading with a non-empty body (Task 2 committed). If it does not, this append makes `TestChangelogPageContentCoverage` fail, because that class asserts every listed release's content actually appears in the built page — this is RESEARCH Pitfall 2, which has recurred at every prior curation phase.</precondition>
  <reversibility rating="reversible">A one-element tuple append and a comment edit in a test module.</reversibility>
  <read_first>
    - `tests/test_changelog_page_gate.py` lines 45-70 — the comment block ("The 13 releases the published page was frozen without (0.4.4 through 0.7.1, inclusive)") and the `RELEASE_VERSIONS` tuple, currently 13 entries ending `"0.7.1"`.
    - `tests/test_changelog_page_gate.py` — the `TestChangelogPageContentCoverage` and `TestChangelogIncludeCompilesToPdf` classes and their availability guards, so you can tell a genuine pass from a silent skip.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md` § "3. `tests/test_changelog_page_gate.py:49-63`".
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md` § "Pitfall 2".
  </read_first>
  <action>
Assert the precondition first: `grep -c '^## \[0\.8\.0\]' CHANGELOG.md` must return `1`. If it returns
`0`, stop — Task 2 has not landed and this append would turn the gate red for a reason that is not a
real defect.

Append `"0.8.0"` as the LAST element of the `RELEASE_VERSIONS` tuple, preserving the file's existing
one-entry-per-line formatting and the trailing comma. Then move the comment above it: the count goes
from 13 to 14, and the range text goes from "0.4.4 through 0.7.1" to "0.4.4 through 0.8.0". The tuple
and its comment must move together — a tuple that grows while its comment still claims 13 is exactly
the drift this comment exists to prevent.

Run the module with the docs extra:
`uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -v`. The `--extra docs` is
load-bearing: `TestChangelogPageContentCoverage` and `TestChangelogIncludeCompilesToPdf` are gated
behind `myst_parser` / `typst` availability, and running against the plain `dev` environment silently
SKIPS both without ever asserting that the new release reaches the built page. Check the JUnit XML's
`skipped` count, not the summary line — a summary line reading "N passed" while the two content-coverage
classes skipped is not evidence.

Change nothing else in this module: no other constant, no test body, no marker, no availability guard.
Take no irreversible action.
  </action>
  <verify>
    <automated>test "$(grep -c '^## \[0\.8\.0\]' CHANGELOG.md)" = "1" && uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -q --junit-xml="${TMPDIR:-/tmp}/52-02-page-gate.xml" && grep -q 'skipped="0"' "${TMPDIR:-/tmp}/52-02-page-gate.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/52-02-page-gate.xml" && grep -q 'errors="0"' "${TMPDIR:-/tmp}/52-02-page-gate.xml"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast,sys; src=open('tests/test_changelog_page_gate.py').read(); t=[n for n in ast.walk(ast.parse(src)) if isinstance(n,ast.Assign) and getattr(n.targets[0],'id','')=='RELEASE_VERSIONS'][0]; v=ast.literal_eval(t.value); print(len(v), v[-1])"` prints `14 0.8.0`.
    - The comment immediately above the tuple contains the literal `14 releases` and the literal `0.4.4 through 0.8.0`, and contains neither `13 releases` nor `0.4.4 through 0.7.1`.
    - `uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -v` produces a JUnit `testsuite` element with `skipped="0"`, `failures="0"` and `errors="0"`.
    - The `-v` transcript shows `TestChangelogPageContentCoverage` and `TestChangelogIncludeCompilesToPdf` tests as PASSED, not SKIPPED, by name.
    - `git diff tests/test_changelog_page_gate.py` touches only the comment block and the tuple — no test body, no marker, no availability guard, no other constant.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>`RELEASE_VERSIONS` carries 14 entries ending `"0.8.0"`, its comment agrees, and the page gate proves the new release's content actually reaches both the built HTML page and the compiled PDF, with zero skips.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` → `release.yml`'s `validate` and `create-release` jobs | The section body authored here becomes the published GitHub Release body, read by `scripts/extract_changelog_section.py` in a `contents: write` job. |
| `CHANGELOG.md` → `docs/source/changelog.rst` → the published documentation | A `.. include::` propagates this file's content to a public page on two Read the Docs projects. |
| the working tree → `origin` and PyPI (the prep/publish fence) | Everything this plan does must stay on the reversible side of that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-06 | Tampering | `scripts/extract_changelog_section.py`'s `version` argument in a `contents: write` release job | high | mitigate | The argument is used only for a string-equality comparison, never interpolated into a shell command and never `eval`'d — an invariant the script's own docstring states and `release.yml` preserves by passing every expression through `env:` rather than into `run:`. This plan EXERCISES the script and modifies neither it nor the workflow; `git diff --name-only` is asserted to list no `.github/` path. |
| T-52-07 | Repudiation | a published release body that misstates what shipped | high | mitigate | Acceptance asserts every one of the 23 delivered requirement IDs appears in the section and that exactly three breaking bullets exist; Task 1 independently confirms the migration guide states the same three breaking changes, so the two published surfaces cannot silently diverge. |
| T-52-08 | Tampering | historical CHANGELOG entries silently edited under cover of a large diff | medium | mitigate | Acceptance asserts `git diff CHANGELOG.md` deletes no line belonging to `## [0.7.1]` or earlier and changes no tail link line other than the two named ones. |
| T-52-09 | Tampering | a page gate that skips instead of asserting, and is read as green | high | mitigate | The gate is run with `--extra dev --extra docs` and its JUnit `skipped` count is asserted `0`; the `-v` transcript must show the two content-coverage classes as PASSED by name. No availability guard or marker may be edited. |
| T-52-03 | Tampering | the prep/publish fence | high | mitigate | No tag, tag push, PyPI upload, GitHub Release or PR is created by this plan. |
| T-52-SC | Tampering | npm/pip/cargo installs | medium | accept | No package is installed or added; `--extra docs` provisions only what `uv.lock` already pins. `52-RESEARCH.md` records the Package Legitimacy Gate as **not applicable** — zero candidate package names, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. |
</threat_model>

<verification>
- `uv run python scripts/extract_changelog_section.py 0.8.0` exits 0 with a non-empty body.
- The `## [0.8.0]` section carries exactly four `###` subsections and exactly three breaking bullets.
- All 23 delivered v1 requirement IDs appear in the section; `REL-07` does not.
- The tail link block carries the new `[0.8.0]` line directly above `[0.7.1]`, and `[Unreleased]` compares against `v0.8.0...HEAD`.
- `tests/test_changelog_page_gate.py` passes with `skipped="0"` under `--extra dev --extra docs`.
- `git diff --name-only -- typsphinx/` produces no output.
</verification>

<success_criteria>
- A curated, extractable `## [0.8.0]` entry exists, covering every delivered v1 requirement (SC#2).
- Both user-visible changes ROADMAP SC#2 names — the output-shape change and the target-as-path reversal — are called out explicitly, with the third breaking change (the collision hard error) beside them (SC#2, D-04).
- The tail link block has rolled over (SC#2).
- `docs/source/changelog.rst` is confirmed still rendering live from the repo-root file (SC#2).
- The page gate's release tuple is current and proven against the built page, with zero skips (SC#2).
</success_criteria>

<output>
Create `.planning/phases/52-v0-8-0-release-prep-prep-only/52-02-SUMMARY.md` when done.
</output>
