---
phase: 52-v0-8-0-release-prep-prep-only
plan: 04
type: execute
wave: 3
depends_on: ["52-02", "52-03"]
files_modified:
  - .planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md
autonomous: true
requirements: [REL-07]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A live CI run — dispatched on the post-bump, post-CHANGELOG, post-gate-test commit, not inherited from an earlier phase — reports `success` for every job: the six `{ubuntu, windows, macos}-latest` x `{3.12, 3.13}` test lanes plus Lint and Format Check, Type Check, Code Coverage, Build Package, and both Integration Test jobs (ROADMAP SC#3 toolchain half, D-08)."
    - "The commit CI proved green is the commit on `origin/gsd/v0.8.0-multi-master-composition` and carries all three of this phase's source changes: `version = \"0.8.0\"` in `pyproject.toml`, a `## [0.8.0]` heading in `CHANGELOG.md`, and the new `TestThreeMasterGate` method in `tests/test_state_guard_shapes_gate.py`."
    - "The Windows and macOS lanes — the lanes local execution structurally cannot see, and the ones that caught a real cp1252 defect at the v0.7.0 close, a real path-separator defect at the v0.7.1 close, and a real Windows-only OUT-02 defect in Phase 47 — are individually named with their conclusions."
    - "Lint and type authority sits with this run rather than with local execution, because `ruff` cannot execute on this machine at all: it ships as a generic-linux ELF the NixOS stub loader rejects (`.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md`) (D-08)."
    - "The push is a plain fast-forward to an already-existing origin branch and the dispatch targets `ci.yml`, never `release.yml` — both are reversible actions explicitly inside this phase's scope, and neither opens a pull request (D-08, prep/publish fence)."
    - "The commits-ahead figure is RE-MEASURED at execution time rather than transcribed: `52-CONTEXT.md` says 155, `52-RESEARCH.md` says 157, and the planner measured 159 at plan time — all three are stale by construction."
    - "No irreversible action is taken: `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` are both empty after the run, and no pull request exists."
  artifacts:
    - path: ".planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md"
      provides: "SC#3's toolchain-half authority — the pushed SHA, the dispatch, the run id and URL, and the full per-job conclusion table"
      contains: "Job conclusions"
  key_links:
    - from: "the pushed milestone-branch commit"
      to: "the dispatched ci.yml authority run"
      via: "`gh workflow run ci.yml --ref gsd/v0.8.0-multi-master-composition` after a fast-forward push; ci.yml's push trigger covers only `main` and `develop`, so a push to this branch starts nothing but Link Check and the run must be dispatched by hand and matched by headSha"
      pattern: "workflow_dispatch"
    - from: "the new TestThreeMasterGate method (plan 52-03)"
      to: "every OS lane of the authority run"
      via: "the gate is committed before this plan dispatches, so the dispatched matrix proves it green on Windows and macOS too — not only on the Linux machine that wrote it"
      pattern: "TestThreeMasterGate"
  prohibitions:
    - statement: "No git tag named v0.8.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch. The dispatch in this plan targets ci.yml only."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged; a PR would also fire CI a second, unintended way and is the publish half's first step."
      status: unverified
      verification: flagged
    - statement: "No second tag is created on the typsphinx-doc-translations repository and its pin is not advanced."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed — the four minor defects this milestone's own reviews filed ship unfixed per D-01."
      status: unverified
      verification: flagged
    - statement: "The push is never a force push; only a plain fast-forward to the existing origin branch is permitted."
      status: unverified
      verification: flagged
    - statement: "REL-07's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped; the requirement stays Pending until the publish."
      status: unverified
      verification: flagged
    - statement: "No CI failure is papered over: a red job is reported with its log excerpt and the plan stops, rather than being retried until it passes or excluded from the recorded table."
      status: unverified
      verification: flagged
    - statement: "No figure is transcribed from the phase's CONTEXT or RESEARCH document into an acceptance criterion or an evidence file; every measured value is re-run live at execution time."
      status: unverified
      verification: flagged
---

<objective>
Prove the post-bump tree green where local execution structurally cannot: push the merged tip to
`origin/gsd/v0.8.0-multi-master-composition`, dispatch `ci.yml` on it by hand, and record every job's
conclusion verbatim.

Purpose: discharges the toolchain half of ROADMAP Phase 52 SC#3 for pytest, lint and type. D-08 assigns
that authority to CI rather than to local runs for two measured reasons: `ci.yml`'s push trigger covers
only `main` and `develop`, so this milestone branch's CI runs exclusively via `workflow_dispatch`; and
`ruff` cannot execute on this machine at all. Pushing a branch and dispatching a workflow are reversible
and in scope; opening a pull request is not, and stays out.

Output: `52-CI-EVIDENCE.md` holding the pushed SHA, the dispatch, the run id and URL, and the complete
per-job conclusion table.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-CONTEXT.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

The phase-level list lives in `52-01-PLAN.md` § "Artifacts this phase produces". This plan produces one
planning artifact, `.planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md`, and changes
no source file.

<tasks>

<task type="auto">
  <name>Task 1: Push the merged tip, dispatch the authority CI run, and read every job</name>
  <precondition>HEAD already carries `version = "0.8.0"` in `pyproject.toml`, a `## [0.8.0]` heading in `CHANGELOG.md`, and the `test_three_masters_each_carry_their_full_include_set_in_pdf` method in `tests/test_state_guard_shapes_gate.py` — that is, plans 52-01, 52-02 and 52-03 have all merged back. `origin/gsd/v0.8.0-multi-master-composition` must be an ancestor of HEAD so a plain fast-forward push is possible. `gh` must be authenticated against this repository.</precondition>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md</files>
  <reversibility rating="costly">A fast-forward push to an existing origin branch is undoable only by a force push, which this project forbids — so treat it as costly rather than free. It is still firmly inside the reversible half of the prep/publish fence: nothing is tagged, published or merged, and CONTEXT D-08 names it in scope explicitly.</reversibility>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — provisioning is mandatory before any command.
    - `.github/workflows/ci.yml` — the trigger block (push on `main`/`develop` only, plus `workflow_dispatch`), the `os` x `python-version` matrix, and every job's `name:` — `Test Python ${{ matrix.python-version }} on ${{ matrix.os }}`, `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`, `Integration Test - basic`, `Integration Test - advanced`. READ ONLY; this file must not be edited.
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md` — the section structure to adapt. This milestone needs ONE run, not two: Phase 46's dual-run shape existed only because it had a separate Windows repair to check first, and there is no such repair here.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md` § "Pattern 4" — the exact command sequence and the job-name set.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-VALIDATION.md` § "Manual-Only Verifications" — the row describing what must be transcribed from the run.
  </read_first>
  <action>
**Do this before writing any file in this worktree.** The push must publish exactly the merged tip that
waves 1 and 2 produced — if an evidence commit lands first, the CI run covers a commit the phase never
intended to prove, and the "CI ran on this exact tree" claim becomes false.

Provision the worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

**Step 1 — confirm the tree is the one to prove.** `grep '^version = ' pyproject.toml` must read
`0.8.0`; `grep -c '^## \[0\.8\.0\]' CHANGELOG.md` must return `1`; and
`grep -c 'def test_three_masters_each_carry_their_full_include_set_in_pdf' tests/test_state_guard_shapes_gate.py`
must return `1`. If any is absent, STOP — waves 1 and 2 have not merged back and this plan is running
out of order.

**Step 2 — re-measure the branch position.** Run
`git rev-list --count origin/gsd/v0.8.0-multi-master-composition..HEAD` and
`git merge-base --is-ancestor origin/gsd/v0.8.0-multi-master-composition HEAD && echo fast-forward-ok`.
Record whatever they print. Do NOT copy `52-CONTEXT.md`'s 155 or `52-RESEARCH.md`'s 157 — the planner
measured 159 at plan time and it has moved again. If the ancestry check fails, STOP: the push would not
be a fast-forward and this plan may not force it.

**Step 3 — push.** `git push origin HEAD:refs/heads/gsd/v0.8.0-multi-master-composition`. Plain
fast-forward, never `--force`, never `--force-with-lease`. Record the pushed SHA with `git rev-parse HEAD`
and confirm `git rev-parse origin/gsd/v0.8.0-multi-master-composition` now equals it.

**Step 4 — dispatch and read.** `ci.yml` triggers on push only for `main` and `develop`, so this push
starts nothing but the separate Link Check workflow. Dispatch by hand:
`gh workflow run ci.yml --ref gsd/v0.8.0-multi-master-composition`. Poll
`gh run list --workflow=ci.yml --branch gsd/v0.8.0-multi-master-composition --limit 5 --json databaseId,headSha,event,status`
until a `workflow_dispatch` run whose `headSha` equals the pushed SHA appears; export its `databaseId`
as `RUN_ID`. Wait with `gh run watch "$RUN_ID"`, then read every job's conclusion with
`gh run view "$RUN_ID" --json jobs`.

This run is SC#3's authority for pytest, lint and type. **Every** job must be `success` — not only the
Windows lanes. If any job fails, do not paper over it: report the failure with its log excerpt and stop.
A retry is legitimate only after the cause is fixed; any fix commit must be pushed and a fresh run
dispatched, with BOTH runs recorded in the evidence file.

**Step 5 — write the evidence.** Create
`.planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md` in English with the headings
`# Phase 52 — CI Evidence`, `## The authority run`, `### Pre-push confirmation` (step 1's three greps
and step 2's two measurements, verbatim), `### Pushed SHA`, `### Push` (the push transcript),
`### Dispatch` (the dispatch command and the matching `gh run list` JSON), `### Job conclusions` (the
complete name/conclusion table from `gh run view --json jobs`, every job listed, none omitted), and
`### Why this run is the authority` (one paragraph restating D-08's split — CI covers the six-lane
matrix, the lint check and the type check; local covers the docs builds and the corpus gate, collected
by plan 52-05). Include the run's
`https://github.com/YuSabo90002/typsphinx/actions/runs/<id>` URL.

`ci.yml` must not be edited — this phase exercises it, never modifies it. Take no irreversible action:
no tag, no tag push, no PyPI upload, no GitHub Release, no pull request, and no dispatch of
`release.yml`.
  </action>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = "$(git rev-parse origin/gsd/v0.8.0-multi-master-composition)" && gh run view "$RUN_ID" --json jobs --jq '[.jobs[].conclusion]|unique|join(",")' | grep -qx 'success' && grep -q '^### Job conclusions' .planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse origin/gsd/v0.8.0-multi-master-composition` equals the SHA recorded as the pushed SHA, and that SHA's tree contains `version = "0.8.0"` in `pyproject.toml`, a `## [0.8.0]` heading in `CHANGELOG.md`, and `def test_three_masters_each_carry_their_full_include_set_in_pdf` in `tests/test_state_guard_shapes_gate.py`.
    - `gh run view "$RUN_ID" --json jobs --jq '[.jobs[].conclusion]|unique'` returns exactly `["success"]`.
    - `gh run view "$RUN_ID" --json event --jq '.event'` returns `workflow_dispatch`, and `gh run view "$RUN_ID" --json headSha --jq '.headSha'` equals the pushed SHA.
    - The recorded job table names all six matrix lanes individually — `Test Python 3.12 on ubuntu-latest`, `Test Python 3.12 on windows-latest`, `Test Python 3.12 on macos-latest`, and the three `3.13` equivalents — plus `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`, `Integration Test - basic`, and `Integration Test - advanced`.
    - `52-CI-EVIDENCE.md` contains the headings `## The authority run`, `### Pre-push confirmation`, `### Pushed SHA`, `### Push`, `### Dispatch`, `### Job conclusions`, and `### Why this run is the authority`.
    - The `### Pre-push confirmation` section contains a commits-ahead figure produced by a command shown in the same section; the figures `155` and `157` do NOT appear as this phase's own measurement.
    - `git diff --name-only -- .github/` produces no output — the workflow was exercised, not edited.
    - `gh pr list --head gsd/v0.8.0-multi-master-composition --json number --jq 'length'` returns `0`.
    - `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` both produce no output.
  </acceptance_criteria>
  <done>A live `workflow_dispatch` CI run on the exact merged tip reports `success` for every one of its twelve jobs, and its full transcript — pushed SHA, dispatch, run URL and per-job table — is recorded as SC#3's toolchain authority.</done>
</task>

<task type="auto">
  <name>Task 2: Record what this run does and does not cover, and re-assert the fence</name>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` — the filed, still-open reason lint authority sits with CI rather than locally.
    - `tox.ini` — the `env_list` whose `lint` environment is why a bare `tox` cannot be used as a local CI-parity check.
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md` — the `### No irreversible action` section shape to match.
    - `.planning/STATE.md` § "Blockers/Concerns" — the recorded history of Windows/macOS lanes catching real defects at the v0.7.0 and v0.7.1 closes and in Phase 47.
  </read_first>
  <action>
Append two sections to `52-CI-EVIDENCE.md`.

`### What this run does not cover` — state plainly that the dispatched matrix does NOT run the two docs
builds (`tox -e docs-html`, `tox -e docs-pdf`) or the full-corpus `-b typstpdf` GATE-02 gate, and CITE
plan 52-05 as the owner of that local half rather than duplicating its results. State that lint and type
authority sits here and not locally because `ruff` cannot execute on this machine at all — a filed,
still-open toolchain defect, not a validation gap — and that a bare `tox` therefore dies in its `lint`
environment at exit 127.

`### No irreversible action` — record, with a `date -u` timestamp and each command's verbatim output:
`git tag -l v0.8.0` (empty), `git ls-remote --tags origin v0.8.0` (empty),
`gh pr list --head gsd/v0.8.0-multi-master-composition --json number,state` (empty list), and
`gh run list --workflow=release.yml --limit 3 --json databaseId,createdAt,event` showing that no
`release.yml` run was started by this phase. Add one sentence naming exactly which two actions this plan
DID take — a plain fast-forward branch push and a `ci.yml` `workflow_dispatch` — and why both sit on the
reversible side of the fence per D-08.

Finally, note in the same section that any commit landing on the branch AFTER the recorded authority SHA
belongs to this phase's remaining evidence plans and touches only `.planning/`; plan 52-07 asserts that
mechanically, so state it here as a forward reference rather than asserting it yourself.

Never assert a command's result from memory — run it and paste the transcript.
  </action>
  <verify>
    <automated>grep -q '^### What this run does not cover' .planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md && grep -q '^### No irreversible action' .planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md && test -z "$(git tag -l v0.8.0)" && test -z "$(git ls-remote --tags origin v0.8.0)" && test "$(gh pr list --head gsd/v0.8.0-multi-master-composition --json number --jq 'length')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `52-CI-EVIDENCE.md` contains the headings `### What this run does not cover` and `### No irreversible action`.
    - The `### What this run does not cover` section names `tox -e docs-html`, `tox -e docs-pdf` and the full-corpus gate, and cites plan `52-05` as their owner without restating its figures.
    - The same section names `ruff` and states it cannot execute on this machine, citing the filed todo by filename.
    - The `### No irreversible action` section contains a `date -u` timestamp and the verbatim output of `git tag -l v0.8.0`, `git ls-remote --tags origin v0.8.0`, `gh pr list --head gsd/v0.8.0-multi-master-composition`, and a `release.yml` run listing showing none was started by this phase.
    - The same section names the two actions this plan took — the fast-forward push and the `ci.yml` dispatch — and forward-references plan 52-07 for the post-authority-SHA assertion.
    - `git diff --name-only -- typsphinx/` and `git diff --name-only -- .github/` both produce no output.
  </acceptance_criteria>
  <done>The CI evidence file states honestly what the authority run covers and what it does not, and records four independent observations that no irreversible action was taken.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the local working tree → `origin` | This plan is the only point in the phase where local state crosses to a remote. |
| GitHub Actions run status → this phase's green claim | The evidence recorded here is what SC#3's toolchain half rests on; a misread or partially-transcribed job table would launder a red lane into a green claim. |
| the working tree → PyPI / a git tag (the prep/publish fence) | Everything this plan does must stay on the reversible side of that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-14 | Repudiation | a CI run cited as authority that ran on a different commit than the phase's tree | high | mitigate | Acceptance asserts the run's `headSha` equals the pushed SHA, that the pushed SHA equals `origin/gsd/v0.8.0-multi-master-composition`, and that that SHA's tree carries all three of the phase's source changes. |
| T-52-15 | Repudiation | a partially transcribed job table hiding a red lane | high | mitigate | Acceptance asserts `[.jobs[].conclusion]|unique` returns exactly `["success"]` and that all twelve job names appear individually in the recorded table; the action forbids retrying-until-green and requires both runs be recorded if a retry happens. |
| T-52-16 | Elevation of privilege | dispatching `release.yml` instead of `ci.yml`, or opening a PR, either of which crosses the publish fence | high | mitigate | The dispatch command names `ci.yml` explicitly; acceptance asserts the run's `event` is `workflow_dispatch` on `ci.yml`, that `gh pr list` returns zero PRs, and that a `release.yml` run listing shows none started by this phase. |
| T-52-17 | Tampering | a force push rewriting origin history | high | mitigate | The action forbids `--force` and `--force-with-lease`; the ancestry check in step 2 must pass before the push, and the plan stops if it does not. |
| T-52-18 | Tampering | `.github/workflows/ci.yml` edited to make a lane pass | high | mitigate | `git diff --name-only -- .github/` asserted empty; the workflow is read-only for this phase. |
| T-52-03 | Tampering | the prep/publish fence | high | mitigate | Four independent absence observations are recorded with a `date -u` timestamp in Task 2. |
| T-52-SC | Tampering | npm/pip/cargo installs | medium | accept | No package is installed or added by this plan. `52-RESEARCH.md` records the Package Legitimacy Gate as **not applicable** — zero candidate package names, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. |
</threat_model>

<verification>
- The dispatched run's `event` is `workflow_dispatch` and its `headSha` equals the pushed SHA.
- `[.jobs[].conclusion]|unique` returns exactly `["success"]`.
- All twelve job names appear individually in the recorded table, including the three Windows and three macOS lanes.
- `gh pr list --head gsd/v0.8.0-multi-master-composition` returns an empty list.
- `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` are both empty.
- `git diff --name-only -- .github/` and `git diff --name-only -- typsphinx/` both produce no output.
</verification>

<success_criteria>
- The post-bump tree is proven green live on all six OS/Python lanes plus lint, type, coverage, build and both integration jobs (SC#3, toolchain half).
- The proved commit is the branch tip on `origin` and carries all three of this phase's source changes.
- The evidence states honestly what the run does not cover and cites plan 52-05 as the local half's owner.
- No irreversible action was taken, recorded on four independent observations (SC#5).
</success_criteria>

<output>
Create `.planning/phases/52-v0-8-0-release-prep-prep-only/52-04-SUMMARY.md` when done.
</output>
