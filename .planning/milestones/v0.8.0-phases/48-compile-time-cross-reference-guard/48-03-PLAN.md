---
phase: 48-compile-time-cross-reference-guard
plan: 03
type: execute
wave: 3
depends_on: ["48-02"]
files_modified:
  - typsphinx/translator.py
  - tests/test_citation_caption_dangling_label_gate.py
  - tests/test_translator.py
  - tests/test_label_existence_guard_unit.py
autonomous: true
requirements: [XREF-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A citation reference inside a captioned code block builds through `-b typstpdf` with exit 0 and no `does not exist in the document` error — the pre-fix fatal reproduced in 48-RED-EVIDENCE.md no longer occurs (XREF-04, D-05)."
    - "All three label-reference emission sites — `visit_reference`'s cross-document branch, `visit_citation`'s back-reference loop including its multi-target marker comprehension, and `visit_pending_xref`/`depart_pending_xref` — obtain their guard strings from the one shared helper and build no `context`/`query` string of their own (XREF-04, SC#2)."
    - "The two same-document `visit_reference` branches still emit the plain unguarded link form, asserted by an explicit negative unit test rather than by inspection (SC#4, D-06)."
    - "Two label namespaces that collide after sanitization produce one label, and the guard's demand side handles that by linking rather than aborting — the helper never ATTACHES a label, only queries one, so it introduces no new duplicate-label compile failure. This is asserted at string level here (the helper emits no attachment construct) AND characterized at compile level by `tests/fixtures/xref_label_collision_guard_gate/`, whose gate proves the consequence the string-level assertion cannot: a reference whose real target is absent renders as a working link to the colliding decoy. That false negative is an accepted limit of the guard, recorded in 48-04, not a defect closed here (XREF-04, edge/adjacency)."
    - "The guard is emitted with no input from builder state at all: a translator whose builder is a hand-built stub with no include-set attribute, and a project whose `typst_documents` is empty or made entirely of unusable entries, both emit the same guarded form (XREF-04, edge/empty)."
    - "Every guarded site passes a label already produced by `_namespace_label()`; the helper contains no call to a label-derivation or sanitization routine, so the demand side and the supply side cannot spell the same label differently (XREF-04, edge/encoding)."
    - "When a label is attached more than once, `query()` returns multiple hits; the guard reads only the result's length and never indexes into it, so its decision is order-independent and stable (XREF-04, edge/ordering)."
  artifacts:
    - typsphinx/translator.py
    - tests/test_label_existence_guard_unit.py
    - tests/test_citation_caption_dangling_label_gate.py
    - tests/test_translator.py
  key_links:
    - "_label_existence_guard -> visit_citation's backref loop: consumed as a value expression (open + already-computed body + close concatenated in Python), not as a streaming open/close pair — the citation site has no live child walk at that point."
    - "_label_existence_guard -> visit_pending_xref/depart_pending_xref: consumed as a streaming pair whose body is already markup, so the non-code-mode body spelling applies there."
    - "_namespace_label -> every guarded site: the label reaching the helper is always already namespaced and sanitized."
  prohibitions:
    - "typsphinx must not swallow or suppress Sphinx's own broken-reference diagnostics; after the build-time warning is deleted those Sphinx warnings are the only remaining build-time signal that a reference is broken."
    - "A reference whose target IS present must not render any differently to the reader than it did before this phase — no visual marking, bracket, or spacing distinguishes the degraded branch from the linked branch."
    - "No second, competing degrade decision may survive anywhere — not under a new name, not as a per-master ledger, not as a defensive getattr fallback."
---

<objective>
Expand the proven guard out from the tracer's single path to the two remaining label-reference
emission sites, and pin the shared helper's contract with direct unit tests.

Purpose: SC#2 requires every label-reference emission site to route through one shared guard rather
than carry its own derivation. Two sites remain after the tracer: `visit_citation`'s back-reference
loop, which is a currently-reachable real fatal (D-05, reproduced verbatim in `48-RED-EVIDENCE.md`),
and `visit_pending_xref`, whose RED is unconstructible but which D-04 requires be brought under the
guard as defence in depth rather than merely rewired. The helper itself gets the direct-unit-test
treatment `_reference_anchor_decision` already has, so its contract is asserted independently of any
end-to-end compile.

Output: two guarded emission sites, a new helper unit-test module carrying the D-06 negative
assertion and the XREF-04 structural site enumeration, and the citation-in-caption gate flipped from
strict xfail to green.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-compile-time-cross-reference-guard/48-CONTEXT.md
@.planning/phases/48-compile-time-cross-reference-guard/48-RESEARCH.md
@.planning/phases/48-compile-time-cross-reference-guard/48-PATTERNS.md
@.planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md
@.planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files and identifiers **created** by Phase 48 (new — not drift):

- `TypstTranslator._label_existence_guard()` and the `_LabelGuardStrings` `NamedTuple`
  (`typsphinx/translator.py`, created in 48-02)
- `TypstTranslator._reference_guard_close` (created in 48-02)
- `__tsx_body` — the emitted Typst `let`-bound identifier
- New test modules: `tests/test_xref_compile_time_guard_render_gate.py`,
  `tests/test_citation_caption_dangling_label_gate.py`,
  `tests/test_label_existence_guard_unit.py` (this plan)
- New fixtures: `tests/fixtures/xref_per_master_guard_gate/`,
  `tests/fixtures/citation_caption_dangling_label_gate/`,
  `tests/fixtures/xref_label_collision_guard_gate/`
- New planning artifacts: `48-EXPECTED-STRUCTURE.md`, `48-RED-EVIDENCE.md`, `48-EVIDENCE.md`

Symbols this phase **DELETES** (intended removals, not accidental):
`TypstBuilder._compute_master_included_docnames()`, `TypstBuilder.master_included_docnames` and its
`write()` call site, `_ReferenceAnchorDecision.degrade_xref_to_text`, `visit_reference`'s
cross-document degrade branch and its `logger.warning`, four unit tests in
`tests/test_master_include_set_predicate_gate.py`, and a `_StubBuilder` class attribute in
`tests/test_citation_degradation_gate.py`.

<!-- planner-discipline-allow: master_included_docnames -->
<!-- planner-discipline-allow: context { -->

## Review findings addressed in this plan

Revised after `48-REVIEWS.md`. What changed here and why:

| Finding | Severity | Change |
|---------|----------|--------|
| Fable LOW — the edge/adjacency truth overstates what string-level tests prove | LOW | Accepted. The truth is reworded to say exactly what each layer proves, and it now points at the real-compile collision gate authored in 48-01 for the consequence a string-level assertion cannot reach. Task 3's item 5 is reworded to match. |
| Sonnet MEDIUM — label-collision false negative is architecturally accepted, unverified | MEDIUM | Closed by the same real-compile fixture. Narrowed per the orchestrator's nuance: labels are `docname:id` and `_sanitize_label` is injective per character, so a cross-document collision additionally requires the DOCNAME segment to collide — the `/` → `_u2f_` route the fixture uses. Task 3 item 5's docstring must state that narrowing rather than the broader claim. |
| Sonnet LOW — `visit_pending_xref`'s hardcoded `#` prefix is preserved unfixed, and the guard's shape assumes a fixed prefix that may not match the surrounding mode if assumption A2 is wrong | LOW | Not fixed — D-04's scope is "brought under the guard, not audited for unrelated mode bugs", and research says "verify at implementation, do not fix speculatively". But the caveat is now recorded IN THE CODE: task 2's comment must name research assumption A2 explicitly as the argument's stated limit, so a future reader sees the condition under which the preserved behaviour becomes wrong. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route visit_citation's back-reference loop through the shared guard (D-05)</name>
  <files>
    typsphinx/translator.py,
    tests/test_citation_caption_dangling_label_gate.py
  </files>
  <read_first>
    - typsphinx/translator.py lines 3240-3300 (`visit_citation`'s buffer-swap label construction, the backref loop, the single-target `label_body`, the `label_expr` concatenation and the multi-target marker comprehension)
    - typsphinx/translator.py lines 2990-3010 (`_find_citing_reference`, the `document.findall` scan that disagrees with the walker)
    - typsphinx/translator.py lines 2620-2703 (`visit_caption` / `depart_caption` — the `SkipNode` in the captioned-code-block path that makes D-05 reachable; read it, do not change it)
    - typsphinx/translator.py — `_label_existence_guard` and `_LabelGuardStrings` as written by plan 48-02
    - .planning/phases/48-compile-time-cross-reference-guard/48-RESEARCH.md "Common Pitfalls" Pitfall 3 (this site does not stream — the guard is a value expression here) and Pitfall 5 (the reproduced fatal), plus "Code Examples" (the verified value-expression shape)
    - .planning/phases/48-compile-time-cross-reference-guard/48-PATTERNS.md section 2b
    - .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md failure mode 2 (the transcript this task turns green)
    - tests/test_citation_caption_dangling_label_gate.py (the strict xfails naming this plan)
  </read_first>
  <behavior>
    - Test 1: `sphinx-build -b typstpdf` over `tests/fixtures/citation_caption_dangling_label_gate/` exits 0, produces a real PDF, and its combined output contains no `does not exist in the document` text.
    - Test 2: the emitted content file for that fixture carries the guarded conditional around the citation back-reference, not a bare label link.
    - Test 3 (regression): the existing citation render and degradation gates stay green — a citation with one back-reference, a citation with two or more back-references, and a citation with none each render as before, with only the guard wrapping the link.
  </behavior>
  <reversibility rating="reversible">D-05 is rated reversible in `48-CONTEXT.md`: the change is confined to how the back-reference link expression is spelled at one site.</reversibility>
  <action>
Provision the worktree first (`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`) and
run every command through `uv run`.

In `visit_citation`, route **every** back-reference link expression through
`_label_existence_guard`. This site does not stream — the label content and the back-reference
targets are fully computed Python strings before any `add_text()` call — so the helper is consumed as
a value expression: concatenate `open_str`, the already-computed body, and `close_str` into one
Python string. Research verified this exact usage compiles, with no outer parentheses needed even
when the result is a multi-line function-call argument.

Two places take the guard, not one:

1. The single-target case, where the label body today is a bare link call taking the one back-reference
   target and the rendered label content. It becomes the guard's open string, the label content, and
   the guard's close string, concatenated. Pass `prefix=""` (this expression is built in code mode,
   inside the grid-cell expression) and the code-mode body spelling adopted by plan 48-02, since the
   label content was produced by a code-mode buffer-swap walk.
2. The multi-target marker comprehension, where each marker is today a bare link call taking one
   back-reference target and a bracketed ordinal. Each marker independently becomes a guarded
   expression built from the same helper with the same arguments and its own target label. D-05
   requires every back-reference target to be guarded, not only the single-target case — each marker
   is an independent reference to a possibly-never-emitted anchor.

Leave everything else in `visit_citation` untouched: the `_find_citing_reference` scan, the
`ref_node is None` continue (Phase 40.1's WR-01 guard), the `_reference_anchor_decision` eligibility
check, the citation's own definition anchor (that is an anchor ATTACHMENT — the supply side — and is
outside this phase's demand-side scope), and the body-cell code block. Do not change `visit_caption`.

Add a short comment above the guarded expressions recording why this site is guarded even though
SC#4 exempts same-document anchors: a back-reference target's presence depends on `visit_reference`
having actually run on the citing node, and the walker can be interrupted (the captioned-code-block
route) while the `findall` scan still finds the node — so unlike an ordinary same-document anchor,
this one is not guaranteed to exist. Cite D-05 and `48-RED-EVIDENCE.md` failure mode 2.

Then remove the strict-xfail markers naming this plan from
`tests/test_citation_caption_dangling_label_gate.py`, leaving the assertions themselves byte-unchanged.

Finish with `uv run black .`, `uv run ruff check .` and `uv run mypy typsphinx/`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_citation_caption_dangling_label_gate.py tests/test_citation_degradation_gate.py tests/test_citation_render_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_citation_caption_dangling_label_gate.py -q` exits 0 with no xfail and no XPASS.
    - Behavior: `sphinx-build -b typstpdf` over the citation-caption fixture exits 0 and the compiled PDF starts with the PDF magic bytes; the combined build output contains no `does not exist in the document` text.
    - `uv run pytest tests/test_citation_degradation_gate.py tests/test_citation_render_gate.py -q` exits 0 (no citation regression).
    - Source: `visit_citation` builds no guard string of its own — within the citation region, `sed -n '/def visit_citation/,/def depart_citation/p' typsphinx/translator.py | grep -c 'query(<'` returns 0, while `sed -n '/def visit_citation/,/def depart_citation/p' typsphinx/translator.py | grep -c '_label_existence_guard'` returns at least 2.
    - `uv run black --check .` exits 0, `uv run ruff check .` exits 0, `uv run mypy typsphinx/` exits 0.
  </acceptance_criteria>
  <done>Every citation back-reference — single-target and each multi-target marker — is emitted through the shared guard, and the reproduced captioned-code-block fatal no longer occurs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bring visit_pending_xref under the guard as defence in depth (D-04)</name>
  <files>
    typsphinx/translator.py,
    tests/test_translator.py
  </files>
  <read_first>
    - typsphinx/translator.py lines 4262-4304 (`visit_pending_xref` / `depart_pending_xref` in full — note the hardcoded prefix and the markup-mode bracket body form)
    - typsphinx/translator.py lines 4570-4620 (`_namespace_label`, and the `_sanitize_label` note in its docstring that records the one real fatal this path already produced)
    - typsphinx/translator.py — `_label_existence_guard` as written by plan 48-02
    - tests/test_translator.py lines 1960-2020 (the only two existing `pending_xref` tests)
    - .planning/phases/48-compile-time-cross-reference-guard/48-RESEARCH.md "Common Pitfalls" Pitfall 4 and "Open Questions" item 1 (verify at implementation, do NOT fix speculatively)
    - .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md the D-04 impossibility-argument section
    - .planning/phases/48-compile-time-cross-reference-guard/48-PATTERNS.md section 2c
  </read_first>
  <behavior>
    - Test 1: rendering a doctree containing a `pending_xref` with a non-empty reftarget emits the guarded conditional around the link, with the label derived exactly as before (the legacy dot/underscore transform, then namespacing with the current docname).
    - Test 2: rendering a `pending_xref` with an empty reftarget emits nothing at visit and nothing at depart, exactly as today.
    - Test 3: the emitted guard for this site uses the non-code-mode body spelling, because this site's children already stream into a markup bracket today and their emission must not change.
  </behavior>
  <reversibility rating="reversible">D-04 is rated reversible in `48-CONTEXT.md`.</reversibility>
  <action>
Replace this site's bare markup link-with-bracket-body emission with the shared guard's streaming
open/close pair, keeping the label derivation byte-identical to what is there now (the legacy
dot-and-underscore-to-hyphen transform applied to the reftarget, then `_namespace_label` with the
current docname).

`visit_pending_xref` emits the helper's open string and stashes its close string; `depart_pending_xref`
emits the stashed close string when the reftarget is non-empty, exactly where it emits the closing
bracket today. Use a dedicated slot (or reuse the reference slot only if the code proves the two can
never nest — prefer a dedicated slot; this site is unreachable through the normal pipeline and must
not be able to corrupt `visit_reference`'s state). Because this site's children already stream into a
markup bracket, pass the **non**-code-mode body spelling so their emission is unchanged.

On the hardcoded mode prefix: research flags it as "verify at implementation, do not fix
speculatively" and D-04's scope is "brought under the guard, not merely rewired" — not "audited for
unrelated mode bugs". Preserve today's behaviour exactly: pass the same unconditional prefix this
site emits now, and add a short comment recording four things: that the site does not compute the
mode-aware prefix its sibling `visit_reference` does; that this was noticed and deliberately left
unchanged as out of scope; that the site is unreachable through Sphinx's normal pipeline (citing
`48-RED-EVIDENCE.md`'s D-04 section); and — addressing a review concern (Sonnet LOW) — that the
argument rests on research **assumption A2**, which says no third-party extension was observed
emitting a `pending_xref` after `ReferencesResolver` runs, and which was NOT tested. Name A2
explicitly, and state the consequence in one clause: if A2 is wrong, the guard's fixed prefix here
may not match the surrounding mode. Do not introduce a mode-aware prefix here.

Add a docstring paragraph to `visit_pending_xref` recording D-04's finding in two sentences: Sphinx's
reference-resolving post-transform replaces every pending cross-reference node unconditionally before
the writer runs, so no such node survives to this handler through the normal pipeline; the guard is
applied anyway as defence in depth, because a future Sphinx version or an unusual extension
interaction is not ruled out by the four source shapes measured.

Update the two existing `pending_xref` unit tests in `tests/test_translator.py` to assert the guarded
emission. Derive the expected strings from the guard contract in `48-EXPECTED-STRUCTURE.md`, not from
running the new emitter. Add a third unit test for the empty-reftarget case asserting that nothing is
emitted, so the "only when reftarget is truthy" branch is pinned.

Finish with `uv run black .`, `uv run ruff check .` and `uv run mypy typsphinx/`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_translator.py -k pending_xref -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_translator.py -k pending_xref -q` exits 0 with at least 3 tests collected.
    - `uv run pytest tests/test_translator.py -q` exits 0.
    - Source: the pending-xref region builds no guard string of its own — `sed -n '/def visit_pending_xref/,/def _compute_relative_include_path/p' typsphinx/translator.py | grep -c 'query(<'` returns 0, while the same region's `grep -c '_label_existence_guard'` returns at least 1.
    - Source: the label derivation is unchanged — the pending-xref region still calls `_namespace_label` exactly once: `sed -n '/def visit_pending_xref/,/def depart_pending_xref/p' typsphinx/translator.py | grep -c '_namespace_label'` returns 1.
    - Source: `visit_pending_xref`'s docstring records the D-04 unreachability finding and the defence-in-depth rationale.
    - Source: the preserved-prefix comment names research assumption A2 by that identifier and states the consequence if it is wrong — `sed -n '/def visit_pending_xref/,/def depart_pending_xref/p' typsphinx/translator.py | grep -c 'A2'` returns at least 1.
    - `uv run black --check .` exits 0, `uv run ruff check .` exits 0, `uv run mypy typsphinx/` exits 0.
  </acceptance_criteria>
  <done>The fourth degradation site emits through the shared guard with its label derivation and mode prefix behaviour unchanged, and its unreachability finding is recorded in the code.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the helper contract, the D-06 exemption, and the XREF-04 site enumeration with direct unit tests</name>
  <files>
    tests/test_label_existence_guard_unit.py
  </files>
  <read_first>
    - tests/test_citation_degradation_gate.py lines 1039-1108 (`TestWr03EligibilityDecisionAgreesWithEmission` — the parametrized direct-unit-test convention for a shared derivation point, and the `_render_with_translator` / `_render_body` harness helpers it uses)
    - tests/test_citation_degradation_gate.py lines 560-660 (the `_MockConfig` / `_MockEnv` / `_StubBuilder` harness shape, post-48-02 edit — a stub builder with NO include-set attribute is exactly the empty-input case this task must exercise)
    - typsphinx/translator.py — `_label_existence_guard`, `_LabelGuardStrings`, and the two same-document `visit_reference` branches as they stand after plans 48-02 and 48-03 tasks 1-2
    - .planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md `## Guard contract` and `## Assertions that must NOT change`
    - .planning/phases/48-compile-time-cross-reference-guard/48-PATTERNS.md section 1 (the "unit-testing convention to imitate") and section 3 (the D-06 branches)
  </read_first>
  <action>
Create `tests/test_label_existence_guard_unit.py` — the direct unit gate on the shared helper's
contract, structurally mirroring `TestWr03EligibilityDecisionAgreesWithEmission`'s
parametrized-table style. Assert only values written down in `48-EXPECTED-STRUCTURE.md`'s
`## Guard contract`.

Cover, in named test classes:

1. **Contract shape.** For a representative label, both body spellings, and both prefix values, the
   returned pair's open string ends with the bound-identifier assignment and the correct body opener,
   and the close string begins with the matching body closer. The bound identifier is the one the
   contract fixes.
2. **Unbroken conditional (research Pitfall 1).** The close string contains the conditional's opening
   brace on the same line as its condition — assert with a regex over the close string that the
   condition and its brace are not separated by a newline, and separately assert the close string
   contains no newline at all. This is the one property whose violation is a hard Typst parse error.
3. **Single label spelling (edge/encoding).** The label the caller passes appears in the close string
   exactly twice — once in the existence query and once in the link call — and both occurrences are
   the identical string. Assert additionally, by source inspection of the helper's own body, that it
   calls no label-derivation or sanitization routine: the helper receives an already-namespaced label
   and never re-derives one.
4. **Order independence (edge/ordering).** The close string tests the query result's LENGTH and never
   indexes into it — assert the close string contains the length comparison and contains none of
   Typst's element-access forms (`.at(`, `.first(`, `[0]`). A label attached more than once therefore
   yields the same decision regardless of hit order.
5. **No attachment (edge/adjacency).** The guard is demand side only: neither returned string
   contains a label-attachment construct (a `label(` call, or a bare `<...>` immediately followed by
   a closing bracket). Two label namespaces that collide after sanitization therefore produce one
   label whose existence query is satisfied, and the guard links rather than aborting; the guard adds
   no new duplicate-label COMPILE failure of its own. State the reasoning in the test's docstring —
   and state, per a review finding (Fable LOW), exactly what this string-level test does and does not
   prove. It proves the helper cannot introduce a duplicate label. It does NOT prove the collision is
   harmless: the compile-level consequence is a false negative (a reference whose real target is
   absent linking to a colliding decoy), which is characterized by
   `tests/fixtures/xref_label_collision_guard_gate/` and its gate in
   `tests/test_xref_compile_time_guard_render_gate.py`, and accepted as a limit in 48-04. Cite that
   gate by name in the docstring so the two halves are traceable to each other. Note the narrowing
   the code supports: labels are namespaced `docname:id` and `_sanitize_label` maps each invalid
   character to a distinct `_u{codepoint:x}_` token, so a cross-document collision additionally
   requires the DOCNAME segment to collide — realistically only via `/` → `_u2f_`. Do not state the
   broader claim that any two ids colliding is sufficient.
6. **No builder input (edge/empty).** Render a small hand-built doctree containing a cross-document
   reference through a translator whose builder stub carries NO include-set attribute at all, and
   assert the emitted body contains the guarded form. Repeat with a stub whose `typst_documents` is
   an empty list. Both must emit the same guarded expression — the demand-side decision has no
   Python-side input.
7. **D-06 exemption, as an explicit negative assertion (SC#4).** Render two hand-built doctrees — one
   reference with a bare refid and no refuri, one reference with a `#`-prefixed internal refuri — and
   assert each emits the plain unguarded link form and that neither emitted body contains the guard's
   conditional. This is the assertion that stops a future executor over-applying the guard while
   touching `visit_reference`.
8. **XREF-04 site enumeration (SC#2/SC#3), structural.** Read `typsphinx/translator.py` and
   `typsphinx/builder.py` as text and assert: the guard's conditional appears in exactly one place in
   the whole package — inside the shared helper — so no site carries its own derivation; and no file
   under `typsphinx/` mentions the deleted builder include-set attribute. Use the package directory
   walked from the test file, not a hardcoded absolute path.

Mark this module `@pytest.mark.slow` only where it actually builds or compiles; the contract and
structural tests are pure and must stay in the fast suite.

Finish with `uv run black .` and `uv run ruff check .`, then run the phase-scoped suite from
`48-VALIDATION.md` and the quick suite.
  </action>
  <verify>
    <automated>uv run pytest tests/test_label_existence_guard_unit.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_label_existence_guard_unit.py -q` exits 0 with at least 8 tests collected.
    - The module asserts the close string is newline-free and that its conditional's brace shares a line with its condition.
    - The module asserts the guard's conditional appears exactly once across `typsphinx/*.py`, and that no file under `typsphinx/` mentions the deleted builder include-set attribute.
    - The module contains an explicit negative assertion that the two same-document reference branches emit no guard.
    - The edge/adjacency test's docstring names `tests/fixtures/xref_label_collision_guard_gate` and states the `docname:id` narrowing: `grep -c 'xref_label_collision_guard_gate' tests/test_label_existence_guard_unit.py` returns at least 1 and `grep -c '_u2f_' tests/test_label_existence_guard_unit.py` returns at least 1.
    - `uv run pytest tests/test_translator.py tests/test_citation_degradation_gate.py tests/test_master_include_set_predicate_gate.py tests/test_xref_orphan_degrade_render_gate.py -q` exits 0.
    - `uv run pytest -m "not slow" -q` exits 0.
    - `uv run black --check .` exits 0 and `uv run ruff check .` exits 0.
  </acceptance_criteria>
  <done>The helper's contract, the D-06 exemption and the single-derivation-point property are each pinned by a direct assertion rather than by inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` / `.rst` author input → emitted Typst source | User-authored ids, citation keys and cross-reference targets are interpolated into the guard expression at all three sites |
| Emitted `.typ` → `typst.compile()` | The Typst compiler consumes text this project generates |

## STRIDE Threat Register

ASVS level 1, blocking threshold `high`. ASVS **V2 (Authentication)**, **V3 (Session Management)**,
**V4 (Access Control)** and **V6 (Cryptography)** are **structurally inapplicable** — typsphinx is a
local Sphinx/Typst document-build tool with no network service, no authentication, no session and no
cryptographic surface (`48-RESEARCH.md` §Security Domain, `48-VALIDATION.md` §Security Domain).
**V5 (Input Validation)** is already covered by the existing `escape_typst_string` / `_sanitize_label`
machinery, unchanged by this phase.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | Label interpolated into the guard expression at all three emission sites | low | mitigate | Every site passes a label already produced by `_namespace_label()` → `_sanitize_label()`. Task 3 asserts by source inspection that the helper itself derives no label, and task 2 asserts the pending-xref site still routes its reftarget through the same derivation exactly once |
| T-48-02 | Denial of Service | Per-reference `query()` introspection pass at Typst compile time, now at three sites instead of one | low | accept | D-11's tiers are fixed before measurement; plan 48-04 takes the after-number against the recorded 28.93s / 28.56s baseline and applies the pre-fixed tiers without renegotiation |
| T-48-03 | Tampering | The pending-xref site's own state slot | low | mitigate | A dedicated close-string slot is used rather than sharing `visit_reference`'s, so an unreachable code path cannot corrupt the reachable one's state |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no packages. Binding constraint #7 forbids new runtime dependencies and a fifth `@preview` package |
</threat_model>

<verification>
- `uv run pytest tests/test_citation_caption_dangling_label_gate.py -q` exits 0 with no xfail.
- `uv run pytest tests/test_label_existence_guard_unit.py -q` exits 0.
- `uv run pytest -m "not slow" -q` exits 0.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all exit 0.
</verification>

<success_criteria>
- All three label-reference emission sites route through one shared guard (XREF-04, SC#2).
- The D-05 captioned-code-block fatal is closed on its committed pre-fix RED.
- The D-04 site is guarded as defence in depth with its unreachability finding recorded in code.
- The D-06 exemption is asserted explicitly, not merely observed (SC#4).
</success_criteria>

<output>
Create `.planning/phases/48-compile-time-cross-reference-guard/48-03-SUMMARY.md` when done
</output>
