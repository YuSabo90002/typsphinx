---
phase: 48-compile-time-cross-reference-guard
plan: 06
type: execute
wave: 6
depends_on: ["48-05"]
files_modified:
  - tests/fixtures/xref_whole_document_guard_gate/conf.py
  - tests/fixtures/xref_whole_document_guard_gate/index.rst
  - tests/fixtures/xref_whole_document_guard_gate/included.rst
  - tests/fixtures/xref_whole_document_guard_gate/orphan.rst
  - tests/test_whole_document_xref_unit.py
  - tests/test_xref_whole_document_guard_render_gate.py
  - .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md
autonomous: true
requirements: [XREF-03]
gap_closure: true
gap_ids: [G-48-4]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real `sphinx-build -b typstpdf` acceptance fixture exercises the whole-document reference path in both directions at once: one reference to a document the compiling master includes, and one to an `:orphan:` document it never includes."
    - "Every assertion in both new test modules is taken from `48-EXPECTED-STRUCTURE.md`'s whole-document section, never read off a build — binding constraint #6."
    - "Every assertion that must FLIP is recorded RED against the unfixed translator first, with its verbatim failure transcript pasted into `48-RED-EVIDENCE.md`, and lands committed as `xfail(strict=True)` (binding constraint #4's non-fatal amendment; this phase's own recorded convention)."
    - "Every assertion that is TRUE on the unfixed tree lands as a plain, non-xfail invariance guard and is labelled as such — including the one that pins the no-regression property that a relative link to a real file asset is never routed through the guard."
    - "The suite stays green at the end of this plan: the strict xfails are expected failures, not failures (binding constraint #8)."
  artifacts:
    - tests/fixtures/xref_whole_document_guard_gate/
    - tests/test_whole_document_xref_unit.py
    - tests/test_xref_whole_document_guard_render_gate.py
    - .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md
  key_links:
    - "`48-EXPECTED-STRUCTURE.md` §whole-document -> every regex and every count in both modules: each assertion carries a comment naming the sub-part it came from, so a reviewer can trace it without re-deriving."
    - "Fixture `conf.py`'s load-bearing-properties block -> the properties the gates actually depend on: the `:orphan:` marking and the single-toctree topology are what make the degrade half of the fixture real."
    - "RED transcript -> the strict xfail markers: each marker's reason string names the transcript section that recorded it."
  prohibitions:
    - "No change to `typsphinx/` in this plan. The whole point of the wave split is that these gates are proven RED against the UNFIXED emitter; `git status --porcelain typsphinx/` must print nothing."
    - "No assertion may be softened to make it pass pre-fix. A test that cannot be made to fail pre-fix is either an invariance guard (mark it so, in its own docstring) or it is not testing the defect."
    - "No copying of a fresh build's output into an expected block. If a needed value is missing from `48-EXPECTED-STRUCTURE.md`, derive it from first principles, add it there, and say so."
---

<objective>
Build the acceptance fixture for the whole-document reference path and record its pre-fix RED, so
the fix in plan 48-07 has a gate that provably fails before it and provably passes after it.

Purpose: G-48-4 is a "compiles fine, produces wrong output" defect — the build exits 0 today and the
PDF is valid; only the link is dead. Binding constraint #4's non-fatal amendment therefore requires
the RED assertion to be a structural / `pypdf` assertion named before implementation starts, and
this phase's own convention (`48-CONTEXT.md` § Established patterns) records pre-fix REDs as
`xfail(strict=True)` with the verbatim transcript in `48-RED-EVIDENCE.md`.

Output: `tests/fixtures/xref_whole_document_guard_gate/`, two new test modules committed with strict
xfails on every flipping assertion, and an appended RED transcript section.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md
@.planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md
@.planning/phases/48-compile-time-cross-reference-guard/48-05-SUMMARY.md
</context>

<execution_environment>
**Mandatory, not conditional** (`CLAUDE.md` § "Worktree-isolated execution"). Detect the worktree
with `test -f .git` (a FILE, not a directory), then:

- `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
- run EVERY subsequent command through `uv run`

Without this the worktree's pytest imports the UNCHANGED main-tree `typsphinx`, and a RED recorded
here would be a measurement of the wrong tree. The `docs` extra is not needed in this plan — no
documentation build happens here.
</execution_environment>

<tasks>

<task type="auto">
  <name>Task 1: Build the whole-document acceptance fixture and record what it emits today</name>
  <files>tests/fixtures/xref_whole_document_guard_gate/conf.py, tests/fixtures/xref_whole_document_guard_gate/index.rst, tests/fixtures/xref_whole_document_guard_gate/included.rst, tests/fixtures/xref_whole_document_guard_gate/orphan.rst</files>
  <read_first>
    - `48-EXPECTED-STRUCTURE.md` § whole-document, part 4 — the fixture design written down in plan
      48-05. Build what it specifies; if you must deviate, amend that artifact in the same commit and
      say why.
    - `tests/fixtures/xref_orphan_degrade_render_gate/` — all four files. This is the closest
      existing fixture (master + toctree'd document + `:orphan:` document) and the one to model on;
      the difference is that its references are anchored `:ref:`s and this one's are whole-document
      references.
    - `tests/fixtures/xref_per_master_guard_gate/conf.py` — the "Load-bearing properties -- do NOT
      touch any of these" comment-block convention, including its de-collision note (a
      `typst_documents` target whose resolved path casefold-equals a docname's own content path is a
      BLD-03 self-collision Phase 47's validator refuses).
  </read_first>
  <action>
    Create the four fixture files. Topology: docname `index` is the single `typst_documents` master,
    its target named so it does not casefold-collide with `index`'s own content file; `index.rst`
    carries a toctree listing `included` only; `orphan.rst` opens with the `:orphan:` field so Sphinx
    never warns about it and never pulls it into any toctree; `index.rst` prose carries exactly one
    whole-document reference to `included` and exactly one to `orphan`, written with the role that
    references a document as a whole rather than an anchor inside it.

    Give each of the three documents a distinctive uppercase body marker (the markers named in
    `48-EXPECTED-STRUCTURE.md` part 4) so the PDF page assertions in task 3 can identify a page by its
    extracted text rather than by position.

    Write the `conf.py` with a "Load-bearing properties -- do NOT touch any of these" block naming,
    at minimum: that `orphan.rst` must keep its orphan marking and must appear in no toctree (it is
    the degrade half of the fixture and adding it to a toctree collapses the fixture into a
    single-outcome case); that the two references in `index.rst` must stay whole-document references
    with no anchor (an anchor routes them down the already-fixed path and the fixture stops
    exercising this gap); and that the master's target must not casefold-equal `index`'s own content
    path.

    Then build it once and record what it emits TODAY: run `sphinx-build -b typstpdf` over the
    fixture into a scratch directory, invoked as `uv run python -m sphinx` (this repo's
    NixOS-sandbox-safe convention — never `uv run sphinx-build`), and capture the exit code, the
    emitted `index.typ`, and whether the compiled master PDF was produced. Confirm by reading the
    emitted `index.typ` that both references currently render as the plain string-url form — a link
    call whose first argument is a quoted relative path ending in the builder's `out_suffix` — and
    that neither content file carries any per-document self-anchor. Paste that transcript into the
    task's SUMMARY notes; task 3 folds it into `48-RED-EVIDENCE.md`.

    Do not add the fixture to any existing test module. Its consumers are the two modules tasks 2 and
    3 create.
  </action>
  <verify>
    <automated>uv run python -m sphinx -b typstpdf tests/fixtures/xref_whole_document_guard_gate /tmp/wd-gate-probe && grep -c 'link("' /tmp/wd-gate-probe/index.typ</automated>
  </verify>
  <acceptance_criteria>
    - All four fixture files exist under `tests/fixtures/xref_whole_document_guard_gate/`.
    - `grep -c ':orphan:' tests/fixtures/xref_whole_document_guard_gate/orphan.rst` returns 1.
    - `grep -c 'Load-bearing properties' tests/fixtures/xref_whole_document_guard_gate/conf.py` returns 1.
    - The probe build exits 0 and writes the master PDF (the defect is a dead link, not a compile failure — a non-zero exit here means the fixture is wrong, not that the defect is worse than diagnosed).
    - The emitted `index.typ` contains two string-url link calls whose arguments end in the builder's `out_suffix`, and `grep -c 'metadata(none) <included:' /tmp/wd-gate-probe/included.typ` returns 0.
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>A real two-outcome fixture exists, builds green today, and demonstrably emits the defect: both whole-document references are dead file links and no content file anchors itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fast offline unit gate for the resolver, the policy predicate and the self-anchor</name>
  <files>tests/test_whole_document_xref_unit.py</files>
  <behavior>
    Flipping (RED today, strict xfail on landing):
    - `_resolve_xref_docname` returns the target docname with an EMPTY anchor for a local
      whole-document refuri ending in the builder's `out_suffix` (today: `None`).
    - `_reference_anchor_decision` on a Sphinx-internal whole-document reference whose target is a
      known document yields that same pair rather than `None`.
    - A document translated with a builder-supplied current docname emits its own self-anchor exactly
      once, in the established zero-width `metadata` anchor form, immediately after the document's
      opening code-block brace.
    - The self-anchor token exists as ONE module-level constant, and both the definition site and the
      reference site derive their label from it through `_namespace_label` (D-13) — asserted
      structurally, resolved with `getattr` on the module so a missing constant fails the test
      instead of breaking collection.

    Invariance (TRUE today, plain non-xfail):
    - A hand-built doctree with NO builder docname emits byte-identical output — no self-anchor.
    - A reference that is not Sphinx-internal and whose target is not a known document is not routed
      through the guard (a relative link to a real file asset keeps its string-url form).
    - An anchored cross-document reference still resolves to its `(docname, anchor)` pair unchanged —
      the already-shipped path this gap must not disturb.
  </behavior>
  <read_first>
    - `48-EXPECTED-STRUCTURE.md` § whole-document, parts 1-3.
    - `tests/test_label_existence_guard_unit.py` — the fast-unit conventions of this phase, in
      particular `TestSingleDerivationPointStructural` (how a structural single-derivation assertion
      is written here) and how it scopes itself to `typsphinx/`.
    - `tests/test_citation_degradation_gate.py` lines ~600-660 — `_MockConfig` / `_MockEnv` /
      `_StubBuilder`, the locally-scoped stub shape this repo uses for translator unit tests, and its
      `out_suffix` / `current_docname` / `get_target_uri` attributes. Note it carries no `found_docs`;
      add one to YOUR OWN stub rather than editing that module's.
  </read_first>
  <action>
    Write a new fast (non-slow-marked) module holding the behaviours above. Build a locally-scoped
    stub builder in this module — do not import another gate module's stub and do not edit
    `tests/conftest.py` — carrying `out_suffix`, `current_docname`, `get_target_uri` and a
    `found_docs` set on its env.

    For the policy assertions, construct reference nodes the way Sphinx's own
    `sphinx.util.nodes.make_refnode` does: it sets `internal=True` on every reference it builds, and
    for a whole-document target it sets a `refuri` with no fragment. A hand-written relative rST link
    carries no such flag. Assert only the cases where BOTH options recorded in
    `48-EXPECTED-STRUCTURE.md` part 6 agree — an internal reference onto a known document is guarded;
    a non-internal reference onto an unknown target is not — and state in the module docstring that
    the divergent case (internal reference onto an UNKNOWN target) is asserted by exactly one
    option-specific test below, naming the option the owner chose.

    Add that one option-specific test: under option-a assert such a reference keeps the string-url
    form; under option-b assert it is routed through the guard against a label that can never exist,
    so it degrades to plain text. Mark it with the option id in its docstring.

    Every assertion that must flip: run it FIRST against the unfixed translator, capture the verbatim
    failure output, then mark the test `@pytest.mark.xfail(strict=True, reason=...)` with a reason
    string naming the `48-RED-EVIDENCE.md` section task 3 will paste it into. Every invariance
    assertion stays a plain test and says in its docstring that it is TRUE pre-fix and is a
    no-regression guard, not a flip — mislabelling one as an xfail would make it XPASS and fail the
    strict marker.

    Do not import the self-anchor token constant at module top level; resolve it with `getattr` on
    the translator module inside the test that asserts it, so the module still imports on the unfixed
    tree.
  </action>
  <verify>
    <automated>uv run pytest tests/test_whole_document_xref_unit.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_whole_document_xref_unit.py -q` exits 0 with every flipping test reported as `xfailed` and zero `XPASS`.
    - `grep -c 'xfail(strict=True' tests/test_whole_document_xref_unit.py` returns at least 4.
    - The module imports cleanly on the unfixed tree — `uv run python -c "import tests.test_whole_document_xref_unit"` from the repo root, or an equivalent collection-only run, succeeds.
    - Each invariance test's docstring says it is true pre-fix; each xfail's reason names a `48-RED-EVIDENCE.md` section.
    - The option-specific test names the owner's chosen option id in its docstring.
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>The resolver, the policy predicate and the self-anchor emission each have a fast, offline assertion that fails against the unfixed emitter for the right reason, and the no-regression properties are pinned by plain invariance guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Real sphinx-build → typst.compile() → pypdf render gate, recorded RED</name>
  <files>tests/test_xref_whole_document_guard_render_gate.py, .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md</files>
  <behavior>
    Flipping (RED today, strict xfail on landing), all over one class-scoped build of
    `tests/fixtures/xref_whole_document_guard_gate/`:
    - The emitted `index.typ` carries the fully-substituted D-07 guard expression for the included
      document's self-anchor label, and another for the orphan document's.
    - The emitted `index.typ` carries NO string-url link call naming either target's output file.
    - `included.typ` and `orphan.typ` each carry their own self-anchor exactly once.
    - The compiled master PDF carries ZERO URI actions whose target ends in the builder's
      `out_suffix` (today: two).
    - The compiled master PDF carries exactly one link annotation with a positional (non-string)
      destination, and the page that destination resolves to carries the included document's body
      marker in its extracted text.

    Invariance (TRUE today, plain non-xfail):
    - The build exits 0 and its output contains no dangling-label error text.
    - Both references' visible text is present in the extracted PDF text, whether linked or degraded
      (D-02: a degraded reference renders exactly the same visible text as the linked form).
    - The orphan document's body marker does NOT appear anywhere in the compiled master's text — it
      is never included, which is what makes the degrade half of the fixture real.
  </behavior>
  <read_first>
    - `48-EXPECTED-STRUCTURE.md` § whole-document, parts 3-5 — every regex and every count below
      comes from here.
    - `tests/test_xref_compile_time_guard_render_gate.py` — in full. Reuse its shape: the
      `_run_sphinx_build_typstpdf` subprocess helper (`sys.executable -m sphinx`, never
      `uv run sphinx-build`), the `_strip_raw_literals` scrubber, the class-scoped build fixture that
      deliberately does NOT assert the return code at fixture level, the `typst`/`pypdf` availability
      skips, and the `@pytest.mark.slow` class marker. Read `_link_annotation_dests`'s docstring
      closely: it records the measured reason a link to a NON-heading label yields an unnamed
      POSITIONAL destination rather than a recoverable name — that is exactly the case this gate
      asserts, so this module needs its own annotation helper rather than that one.
  </read_first>
  <action>
    Write the slow render-gate module. Give it its own annotation helper that classifies every
    `/Link` annotation across every page into three buckets — a string `/Dest` (named destination), a
    non-string `/Dest` (positional destination, carrying the page object it points at), and a `/A`
    action with `/S` equal to `/URI` (carrying the URI string) — and returns all three. This is the
    helper the counts above are asserted through; state in its docstring why the positional bucket
    exists, citing the measured non-heading-label behaviour the sibling module already records.

    For the destination-page assertion, resolve the positional destination's page object back to a
    page index through `pypdf` and extract that page's text. If the whole fixture lays out on a single
    page, the assertion is true but weak — say so in the module docstring rather than overclaiming,
    and record the measured page count in the RED evidence so the weakness is visible instead of
    hidden.

    Every assertion that must flip: run it FIRST against the unfixed translator, then mark it
    `@pytest.mark.xfail(strict=True, reason=...)`. Every invariance assertion stays plain and says in
    its docstring that it holds pre-fix. In particular, the build-exits-0 assertion is an invariance
    guard here, not a flip — this defect does not break the compile.

    Then append the RED record to `48-RED-EVIDENCE.md`: a new section carrying the fixture's topology
    in three lines, the task-1 probe transcript (exit code, the two string-url lines from
    `index.typ`, the absence of any self-anchor), the verbatim pre-fix pytest output for BOTH new
    modules with the strict xfail markers temporarily removed, the measured PDF page count, and a
    provenance header naming the interpreter, the installed `typst-py` and `pypdf` versions, and the
    HEAD SHA the RED was recorded against. Follow the existing file's header convention exactly. Then
    put the markers back and confirm the suite is green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_xref_whole_document_guard_render_gate.py tests/test_whole_document_xref_unit.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_xref_whole_document_guard_render_gate.py -q` exits 0 with every flipping test `xfailed` and zero `XPASS`.
    - The module carries `@pytest.mark.slow` on its render-gate class and skips cleanly when `typst` or `pypdf` is unavailable.
    - `48-RED-EVIDENCE.md` gained one section with a provenance header (interpreter, `typst-py` version, `pypdf` version, HEAD SHA) and the verbatim pre-fix failure output for both modules; `git diff` shows additions only.
    - The RED section states the measured page count of the fixture's compiled master and whether the destination-page assertion is strong or weak on that layout.
    - `uv run pytest -q` exits 0 over the whole suite (xfails are expected failures, not failures).
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>The gap has an end-to-end gate through real `sphinx-build` → `typst.compile()` → `pypdf` that fails against the unfixed emitter for exactly the diagnosed reason, its failure is on record verbatim, and the tree is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture `.rst` / `conf.py` → emitted Typst source | Exercised deliberately; the fixture is author-controlled input under test |
| Emitted `.typ` → `typst.compile()` | Exercised by the render gate; the compile must stay non-fatal throughout |

## STRIDE Threat Register

ASVS level 1, blocking threshold `high`. ASVS **V2 (Authentication)**, **V3 (Session Management)**,
**V4 (Access Control)** and **V6 (Cryptography)** are **structurally inapplicable** — typsphinx is a
local Sphinx/Typst document-build tool with no network service, no authentication, no session and no
cryptographic surface (`48-RESEARCH.md` § Security Domain). **V5 (Input Validation)** is covered by
the existing `escape_typst_string` / `_sanitize_label` machinery, unchanged by this plan.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-06-01 | Tampering | A gate that looks red but is measuring the wrong tree | high | mitigate | The mandatory per-worktree `uv sync` + `uv run` protocol, plus a HEAD SHA in the RED provenance header, so the transcript names the tree it was taken against |
| T-48-06-02 | Repudiation | A laundered expected value (read off a build, presented as derived) | medium | mitigate | Every assertion cites the `48-EXPECTED-STRUCTURE.md` sub-part it came from; `git status --porcelain typsphinx/` empty throughout proves no new emitter existed to read from |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — no packages are installed; `uv sync` resolves the already-pinned `dev` extra from the committed lock |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 over the whole suite.
- `uv run pytest tests/test_whole_document_xref_unit.py tests/test_xref_whole_document_guard_render_gate.py -q -rxX` reports every flipping test as `xfailed` and none as `XPASS`.
- `git status --porcelain typsphinx/` prints nothing.
- `48-RED-EVIDENCE.md` carries the new section with its provenance header.
</verification>

<success_criteria>
- The whole-document reference path has a real acceptance fixture exercising both outcomes in one
  build.
- Every flipping assertion is recorded RED against the unfixed emitter with a verbatim transcript
  (binding constraint #4's non-fatal amendment).
- Every expected value traces to `48-EXPECTED-STRUCTURE.md`, not to a build (binding constraint #6).
- The no-regression properties — hand-built doctrees unchanged, file-asset links unchanged, anchored
  cross-document references unchanged — are pinned by plain invariance guards, distinguishable at a
  glance from the flips.
- The tree closes green (binding constraint #8).
</success_criteria>

<output>
Create `.planning/phases/48-compile-time-cross-reference-guard/48-06-SUMMARY.md` when done
</output>
