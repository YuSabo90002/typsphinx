---
phase: 48-compile-time-cross-reference-guard
plan: 04
type: execute
wave: 4
depends_on: ["48-03"]
files_modified:
  - .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/
autonomous: true
requirements: [XREF-03, XREF-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full-corpus `-b typstpdf` compile time is measured after the guard lands and recorded in 48-EVIDENCE.md next to the 28.93s / 28.56s pre-fix baseline, with D-11's three tiers quoted verbatim BEFORE the number and applied without renegotiation."
    - "If the measured regression falls in D-11's middle tier, an improvement todo is filed naming the state-lookup remediation path; if it falls in the top tier, a blocker is raised against Phase 49's scope rather than the design being abandoned — and in EITHER case the record states in one sentence that the remediation is ADDITIVE scope for a phase already carrying its own requirement set, so the owner sees the coupling rather than inheriting it silently."
    - "D-09's output delta is measured, not asserted: the corpus is built twice with `-b typst` — once against the pre-guard translator, once as-shipped — and the citation back-reference marker counts are diffed and recorded. If the corpus exercises no citations, the record says so plainly instead of claiming verification."
    - "The label-collision false negative is recorded in 48-EVIDENCE.md as a measured, accepted limit with its narrowing (`docname:id` namespacing plus the `/` → `_u2f_` transform), a filed todo so it stays visible beyond this phase, and an explicit owner sign-off item in the phase's human check."
    - "`grep -rn \"non-included\\|degrade\" docs/source/` is re-run at implementation time and its result recorded, confirming that deleting the build-time warning changes no published contract (D-01)."
    - "SC#2's site enumeration is discharged by a repo-wide grep run at closeout — not against the files a requirement happens to name — per milestone invariant #4."
    - "The full pytest suite, `black`, `ruff` and `mypy` are all green before the phase closes (binding constraint #8)."
    - "XREF-03 and XREF-04 are checked off in REQUIREMENTS.md only after their evidence is recorded, not before."
  artifacts:
    - .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md
  key_links:
    - "D-11's pre-fixed tiers -> the measured after-number: the tiers are quoted in the evidence artifact above the measurement, so the reading order proves they were not renegotiated."
    - "48-EVIDENCE.md -> REQUIREMENTS.md: each checkbox flip cites the evidence section that discharges it."
  prohibitions:
    - "typsphinx must not swallow or suppress Sphinx's own broken-reference diagnostics; after the build-time warning is deleted those Sphinx warnings are the only remaining build-time signal that a reference is broken."
    - "A reference whose target IS present must not render any differently to the reader than it did before this phase — no visual marking, bracket, or spacing distinguishes the degraded branch from the linked branch."
    - "No second, competing degrade decision may survive anywhere — not under a new name, not as a per-master ledger, not as a defensive getattr fallback."
---

<objective>
Measure what the guard cost, against thresholds fixed before the measurement — then discharge the
phase's structural success criteria by repo-wide grep and close green.

Purpose: D-11 fixes the cost tiers first precisely so a measured regression cannot be rationalised
after the fact, and SC#4's second half makes "its cost is measured rather than assumed" part of the
requirement. Binding constraint #8 requires the phase to close on a full green suite, and milestone
invariant #4 requires "anywhere under X" criteria to be checked by a repo-wide grep rather than
against the files a requirement happens to name.

Output: `48-EVIDENCE.md` carrying the before/after cost record with its tier decision, the D-01
published-contract re-check, the SC#2/SC#3 grep transcripts, and the green-suite record; plus the two
requirement checkboxes flipped.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-compile-time-cross-reference-guard/48-CONTEXT.md
@.planning/phases/48-compile-time-cross-reference-guard/48-VALIDATION.md
@.planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files and identifiers **created** by Phase 48 (new — not drift):

- `TypstTranslator._label_existence_guard()`, `_LabelGuardStrings`,
  `TypstTranslator._reference_guard_close`, and the emitted `__tsx_body` identifier
  (`typsphinx/translator.py`)
- New test modules: `tests/test_xref_compile_time_guard_render_gate.py`,
  `tests/test_citation_caption_dangling_label_gate.py`,
  `tests/test_label_existence_guard_unit.py`
- New fixtures: `tests/fixtures/xref_per_master_guard_gate/`,
  `tests/fixtures/citation_caption_dangling_label_gate/`,
  `tests/fixtures/xref_label_collision_guard_gate/`
- New planning artifacts: `48-EXPECTED-STRUCTURE.md`, `48-RED-EVIDENCE.md`, `48-EVIDENCE.md`

Symbols this phase **DELETES** (intended removals, not accidental):
`TypstBuilder._compute_master_included_docnames()`, `TypstBuilder.master_included_docnames` and its
`write()` call site, `_ReferenceAnchorDecision.degrade_xref_to_text`, `visit_reference`'s
cross-document degrade branch and its `logger.warning`, four unit tests in
`tests/test_master_include_set_predicate_gate.py`, and a `_StubBuilder` class attribute in
`tests/test_citation_degradation_gate.py`.

<!-- planner-discipline-allow: master_included_docnames -->

## Review findings addressed in this plan

Revised after `48-REVIEWS.md`. What changed here and why:

| Finding | Severity | Change |
|---------|----------|--------|
| Sonnet MEDIUM — D-09's citation-marker regression is asserted backward-compatible-by-design but never verified against the full corpus; `test_corpus_gate.py` only asserts no fatal, which would not catch "an extra `[1]` marker now appears" | MEDIUM | New task 2 measures it, reusing the established two-build methodology from `tests/test_corpus_gate.py::test_empty_url_before_after` (a git worktree at the pre-guard commit + a `PYTHONPATH` override, both builds `-b typst`). Recorded as a one-off manual delta in 48-EVIDENCE.md, not as a new permanent wall-clock/count assertion — same reasoning D-11 gives for its own measurement. |
| Sonnet MEDIUM — performance risk is real and a top-tier result silently hands Phase 49 an unplanned obligation | MEDIUM | Task 1's middle-tier and top-tier actions now both require an explicit sentence naming the Phase 49 coupling as ADDITIVE scope needing owner attention, and the top-tier blocker goes into `STATE.md` where the next phase's planner reads it. Fable was silent on performance; as the consensus notes, that silence is not evidence against a phase-coupling argument. |
| Sonnet MEDIUM + Fable LOW — label-collision false negative deserves explicit owner sign-off | MEDIUM | Task 2 records the measured collision result as an accepted limit, files a todo so it survives the phase, and task 3's human check gains an explicit sign-off line. |
| Fable LOW — post-D-01 an `:orphan:`-target reference degrades with zero diagnostic | LOW | Surfaced, not reversed (owner-locked D-01). The `## D-01` section must now state the visibility loss in one sentence alongside the re-run grep, rather than recording only "no published contract changed". |

<tasks>

<task type="auto">
  <name>Task 1: Take the D-11 after-measurement against pre-fixed tiers and record the cost decision</name>
  <files>
    .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md,
    .planning/todos/pending/
  </files>
  <read_first>
    - .planning/phases/48-compile-time-cross-reference-guard/48-CONTEXT.md D-11 (the three tiers, and the instruction that they are fixed before the measurement)
    - .planning/phases/48-compile-time-cross-reference-guard/48-VALIDATION.md sections "Sampling Rate" and "Manual-Only Verifications" (the exact invocation and the recorded 28.93s / 28.56s baseline)
    - .planning/phases/48-compile-time-cross-reference-guard/48-RESEARCH.md assumption A3 (the baseline is machine-specific; the tiers are relative percentages)
    - tests/test_corpus_gate.py (confirm for yourself that it still carries no timing instrumentation, so the measurement stays a one-off manual record rather than becoming a flaky wall-clock assertion)
    - .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md (already carries plan 48-02's `## Body-mode measurement` section — append, do not overwrite)
  </read_first>
  <precondition>The full-corpus gate's cached Sphinx `doc/` corpus is present, or the gate is able to re-clone it: `tests/test_corpus_gate.py`'s corpus helper resolves without a network failure.</precondition>
  <action>
Provision the worktree first (`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`) and
run every command through `uv run`.

Append a `## D-11 compile-time cost` section to
`.planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md`. Write it in this order, and
in this order only, so the reading order itself proves the tiers were not renegotiated:

1. Quote D-11's three tiers verbatim from `48-CONTEXT.md` — under twenty percent, record the number
   and do nothing else; between twenty and one hundred percent, record it as an explicit finding and
   file an improvement todo; above one hundred percent, escalate it as a blocker attached to Phase
   49's scope. State that these were fixed at discussion time, before any measurement.
2. Quote the pre-fix baseline verbatim: 28.93s and 28.56s, and note research assumption A3 — the
   absolute numbers are specific to the measuring machine, so the after-number must be taken on the
   same machine for the ratio to mean anything, and the tiering logic is relative.
3. Run the corpus gate twice with wall-clock timing, using the invocation `48-VALIDATION.md` names,
   and paste both raw transcripts including the pytest-reported duration line.
4. Compute the percentage change from the mean of the two before-numbers to the mean of the two
   after-numbers, show the arithmetic, and state which tier it falls in.
5. Apply the tier without argument:
   - bottom tier: record only, and say so explicitly.
   - middle tier: additionally file a todo under `.planning/todos/pending/` naming the measured
     regression, the tier it landed in, and the remediation path D-11 already names — replacing the
     compile-time existence query with a lookup against the include-set state Phase 49 introduces.
     Follow the existing todo files' front-matter and section shape.
   - top tier: additionally record a blocker paragraph in the evidence artifact addressed to Phase
     49's scope, and add the same blocker to `STATE.md`'s Blockers/Concerns section. State
     explicitly that abandoning the design is not available, because binding constraint #1 makes
     Phase 49 depend on the guard.
   In the middle and top tier cases alike, add one explicit sentence — addressing a review concern
   (Sonnet MEDIUM) — that the remediation is **additive** scope for Phase 49, which already carries
   its own requirement set, and that this is a phase-coupling consequence the owner should see now
   rather than discover when Phase 49 is planned. Put that sentence in the todo (middle tier) or the
   `STATE.md` blocker (top tier), not only in the evidence artifact, so it reaches the next phase's
   planner through a channel that planner actually reads.

Do not add timing instrumentation to `tests/test_corpus_gate.py` and do not add a wall-clock
assertion anywhere — D-11 specifies a one-off manual record, and a timing assert would be flaky
across CI machines.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md').read_text(); sys.exit(0 if '## D-11 compile-time cost' in t and '28.93' in t and '28.56' in t else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `48-EVIDENCE.md` contains a `## D-11 compile-time cost` section quoting all three tiers before quoting any after-number.
    - It contains both baseline figures `28.93` and `28.56` and two pasted after-run transcripts with their pytest duration lines.
    - It shows the percentage arithmetic explicitly and names the resulting tier in one sentence.
    - If the middle tier was reached, a new file exists under `.planning/todos/pending/` naming the regression and the state-lookup remediation path; if the bottom tier was reached, the artifact says "record only" explicitly and no todo is filed.
    - If the middle or top tier was reached, the filed todo (middle) or the `STATE.md` blocker (top) contains an explicit sentence naming the remediation as additive Phase 49 scope — not only the evidence artifact.
    - `git diff --stat tests/test_corpus_gate.py` prints nothing (no timing instrumentation added).
    - `grep -Ec 'assert.*(elapsed|duration|time\.)' tests/test_corpus_gate.py` returns 0.
  </acceptance_criteria>
  <done>The guard's corpus-scale cost is measured and filed against thresholds that were written down before the measurement, with the tier applied mechanically and its Phase 49 coupling stated where the next planner will read it.</done>
</task>

<task type="auto">
  <name>Task 2: Measure D-09's citation-marker delta corpus-wide and record the label-collision limit as accepted</name>
  <files>
    .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md,
    .planning/todos/pending/
  </files>
  <read_first>
    - tests/test_corpus_gate.py lines 400-500 (`checkout_pre_xref01_translator`, `_run_corpus_sphinx_build`, `wire_typsphinx_into_corpus_conf`) and lines 509-575 (`test_empty_url_before_after` — the established two-build BEFORE/AFTER methodology: a git worktree at an older tree plus a `PYTHONPATH` override for the BEFORE subprocess only, both builds `-b typst`, cleanup in a `finally`)
    - typsphinx/translator.py `visit_citation`'s backref loop as it stands after plan 48-03 (the single-target `label_body`, the `label_expr` concatenation `text("[") + <body> + text("]")`, and the multi-target marker comprehension whose group opens with `text(" (")`)
    - .planning/phases/48-compile-time-cross-reference-guard/48-CONTEXT.md D-09 (the accepted behaviour change and why suppressing a same-document anchor was a category error)
    - .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md the collision-fixture baseline section (written by plan 48-01 task 1)
    - tests/test_xref_compile_time_guard_render_gate.py (the collision characterization test, green since plan 48-02)
    - .planning/todos/pending/ (any one existing file — copy its front-matter and section shape)
  </read_first>
  <precondition>The full-corpus gate's cached Sphinx `doc/` corpus is present, or the gate is able to re-clone it: `tests/test_corpus_gate.py`'s corpus helper resolves without a network failure.</precondition>
  <action>
Provision the worktree first (`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`) and
run every command through `uv run`.

**`## D-09 citation-marker delta`.** Append this section to `48-EVIDENCE.md`. It answers a review
concern (Sonnet MEDIUM): D-09 is asserted backward-compatible-by-design, but no gate in this phase
would catch "an extra back-reference marker now appears" — `test_corpus_gate.py` only asserts no
fatal. Measure the delta rather than resting on the same-document-anchor argument.

Use the methodology `tests/test_corpus_gate.py::test_empty_url_before_after` already establishes; do
not invent a new one, and do not add a new test to that module. Steps:

1. Resolve the pre-guard commit: take the earliest Phase 48 commit that touched
   `typsphinx/translator.py` (the project's commit convention makes it findable with
   `git log --reverse --format='%H %s' -- typsphinx/translator.py` and matching a subject naming
   `48-02`) and use its **parent** as `BEFORE_SHA`. Record the resolved sha and the subject line of
   the commit it precedes, so the reader can check the choice.
2. `git worktree add <tmp>/pre-48-guard $BEFORE_SHA`, then run the corpus build with
   `PYTHONPATH=<tmp>/pre-48-guard` set for that subprocess ONLY, so the pre-guard translator shadows
   the installed package for the BEFORE build and nothing else. Remove the worktree in a `finally`-
   equivalent step regardless of outcome.
3. Build the SAME wired corpus twice with `-b typst` (never `-b typstpdf` — the translate phase is
   what emits the markers, and a compile is both slower and able to fail for unrelated reasons).
4. Count, over all emitted `.typ` files in each output tree, three numbers and record all six:
   - total citation label cells: occurrences of the literal `text("[") + `;
   - LINKED citation label cells: pre-guard, occurrences of `text("[") + link(<`; post-guard,
     occurrences of `text("[") + context` — the guard replaced the bare link with the guarded
     expression, so the two builds have DIFFERENT signatures for the same concept and a naive
     single-signature grep would report a spurious collapse to zero. State this explicitly.
   - multi-target marker groups: occurrences of `+ text(" (") + (`.
5. Show the arithmetic and state the finding in one sentence: how many citation label cells gained a
   back-reference link that had none before, and how many marker groups appeared. Per D-09 an
   increase is the INTENDED fix, so an increase is recorded as expected, not as a regression; what
   would be a finding is a DECREASE (a marker disappearing), and the record must say so before
   showing the numbers.
6. If the corpus contains no citations at all — total citation label cells is zero in both builds —
   write that plainly and say the corpus does not exercise D-09, rather than reporting the zero delta
   as verification. In that case additionally record the counts from the project's own test corpus by
   running `uv run pytest tests/test_citation_render_gate.py tests/test_citation_degradation_gate.py -q`
   and noting that those gates are the only D-09 coverage.

Do not add a permanent count assertion anywhere. Like D-11's timing, this is a one-off manual record;
a corpus-dependent count assertion would drift with the external corpus (the same reasoning
`test_empty_url_before_after` gives for having no exact-magnitude assertion).

**`## Accepted limit — label-collision false negative`.** Append a second section. Paste the verbatim
`uv run pytest tests/test_xref_compile_time_guard_render_gate.py -q -k collision` transcript and the
emitted `index.typ` reference line from `tests/fixtures/xref_label_collision_guard_gate/`. Then state,
in four sentences: what the guard actually asks (does a label with this spelling exist in this
compile, not does the document I meant exist); the measured consequence (a reference whose real
target `a/b` is absent renders as a working link to the included decoy `a_u2f_b`); the narrowing
(labels are `docname:id` and `_sanitize_label` maps each invalid character to a distinct
`_u{codepoint:x}_` token, so this needs the DOCNAME segment to collide, realistically only via the
`/` → `_u2f_` transform); and the comparison to what was there before (the build-time mechanism
checked docname membership, not label existence, so this false-negative class is genuinely new to
this phase). Record it as ACCEPTED for Phase 48 and file a todo under `.planning/todos/pending/`
naming the class, the fixture that characterizes it, and the one obvious remediation direction
(carrying the target docname into the guard's decision rather than relying on label spelling alone),
so the limit survives past this phase's closeout.

Do not modify any file under `typsphinx/` or `tests/` in this task.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md').read_text(); need=['## D-09 citation-marker delta','## Accepted limit — label-collision false negative','a_u2f_b']; sys.exit(1 if [n for n in need if n not in t] else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `48-EVIDENCE.md` contains the headings `## D-09 citation-marker delta` and `## Accepted limit — label-collision false negative`.
    - The D-09 section records a resolved `BEFORE_SHA`, the two `-b typst` build invocations, six counts (three per build), the arithmetic, and a one-sentence finding; it states before showing the numbers that an increase is intended and a decrease is the finding.
    - The D-09 section states explicitly that the linked-cell signature differs between the two builds (`link(<` before, `context` after) and that a single-signature grep would report a spurious collapse.
    - If the corpus exercises no citations, the section says so plainly and does not present the zero delta as verification.
    - The accepted-limit section names the fixture, states the `docname:id` + `_u2f_` narrowing, and contrasts the new false-negative class with the build-time mechanism's docname-membership check.
    - A new file exists under `.planning/todos/pending/` naming the label-collision false-negative class and its characterizing fixture.
    - `git diff --stat tests/ typsphinx/` prints nothing (this task changes no code and no test).
    - `git worktree list` shows no leftover `pre-48-guard` worktree.
  </acceptance_criteria>
  <done>D-09's output delta is a measured transcript rather than an argument, and the guard's one new false-negative class is recorded as a measured, filed, accepted limit.</done>
</task>

<task type="auto">
  <name>Task 3: Discharge SC#2/SC#3 by repo-wide grep, re-check the published contract, and close the phase green</name>
  <files>
    .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/ROADMAP.md lines 588-639 (Phase 48's four success criteria, read literally — each grep below must discharge a named clause)
    - .planning/REQUIREMENTS.md lines 68-74 (XREF-03 and XREF-04's exact wording and their checkboxes)
    - .planning/phases/48-compile-time-cross-reference-guard/48-CONTEXT.md D-01's closing sentence ("Confirm this still holds at implementation time rather than assuming it") and the "Published contracts" section of `<canonical_refs>`
    - .planning/phases/48-compile-time-cross-reference-guard/48-VALIDATION.md "Manual-Only Verifications" (all three rows are discharged here)
    - .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md (append to the existing sections)
  </read_first>
  <action>
Append three further sections to `48-EVIDENCE.md`, each carrying the verbatim command and its
verbatim output.

**`## SC#2 — site enumeration`.** Run a repo-wide grep over `typsphinx/` for the emitted existence
query and for the shared helper's name, and paste both transcripts. Show that the query string
appears in exactly one definition — the shared helper — and that each of the three emission sites
appears as a caller. Enumerate the three sites by file and line in a small table, and add a fourth
row for the two same-document branches recording that they are deliberately unguarded per SC#4/D-06
with the line numbers where their exemption comments sit. Cross-reference the answer to open
question #1 recorded in `48-RED-EVIDENCE.md` — a fourth independent degradation site, not one already
routed through the shared anchor decision — and state that the answer, not an assumption, is what
determined the change made there.

**`## SC#3 — the build-time mechanism is gone`.** Run the repo-wide greps for the deleted builder
attribute, the deleted builder method, and the deleted decision field across `typsphinx/` AND across
`tests/`, and paste every transcript including the empty ones. Per milestone invariant #4 these are
repo-wide greps, not greps of the files the requirement names. Add a one-line statement that no
second degrade decision survives anywhere.

**`## D-01 — no published contract changed`.** Re-run the documentation-tree grep
`48-CONTEXT.md` names and paste its verbatim output, confirming the discussion-time result still
holds rather than assuming it. If it does NOT still hold, stop and record what changed instead of
proceeding — a published contract change is out of this phase's scope and would need the owner.

Then add one sentence recording the diagnostic-visibility consequence, addressing a review finding
(Fable LOW): with the build-time warning deleted, a reference to a target the author deliberately
marked `:orphan:` now degrades silently at every layer — Sphinx itself emits no warning for a
deliberately-orphaned target that resolved successfully, so no diagnostic remains anywhere. This is
owner-locked by D-01, whose own rationale measured that Sphinx already covers every other case; the
sentence exists so the loss is a stated consequence in the phase record rather than an unnoticed one.
Do not add a replacement diagnostic — D-01 forbids it, and the phase's prohibitions forbid a second
degrade decision under any name.

**`## Phase green gate`.** Run and paste, in order: the full pytest suite, `black --check`,
`ruff check`, and `mypy` over the package. All four must be green (binding constraint #8). Note the
pass/fail/xfail/skip counts.

Then update `.planning/REQUIREMENTS.md`: check off XREF-03 and XREF-04, and after each one add a
short parenthetical naming the evidence section that discharges it. Change nothing else in that file
— do not touch any other requirement's checkbox, wording or ordering.

Do not modify any file under `typsphinx/` in this task.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/</automated>
    <human-check>
      Open `48-EVIDENCE.md` and confirm, by reading rather than by trusting a grep: (1) the SC#2 table
      names three guarded emission sites plus the two deliberately-unguarded same-document branches,
      and the line numbers match the current tree; (2) the D-11 tiers are quoted ABOVE the measured
      number, not below it; (3) the D-04 answer recorded in `48-RED-EVIDENCE.md` reads as an
      enumerated impossibility argument rather than as a "could not reproduce" note. Then build the
      project's own documentation with `tox -e docs-pdf` and confirm the produced PDF still opens and
      its internal cross-reference links still navigate — the dogfooding check no automated assertion
      in this phase covers.

      OWNER SIGN-OFF (both cross-AI reviewers asked for this explicitly rather than leaving the
      residual risk implicit — see `48-REVIEWS.md` "Agreed Concerns" and "Divergent Views"):
      (4) Read `## Accepted limit — label-collision false negative` and confirm you accept it. The
      guard asks "does a label with this spelling exist in this compile", so a colliding label
      namespace can make an absent target render as a working link — a false-negative class the
      build-time mechanism did not have, because it checked docname membership rather than label
      existence. It is narrow (it needs the `docname` segment to collide, realistically only via the
      `/` → `_u2f_` transform) and it is characterized by a real compile, but it is real.
      (5) Read the `## D-11 compile-time cost` tier decision and, if it landed in the middle or top
      tier, confirm you accept the additive Phase 49 obligation it creates.
      (6) Read the D-01 visibility sentence and confirm you accept that a reference to an `:orphan:`
      target now degrades with no diagnostic at any layer.
      Reply with "accepted" for each, or name the one you want reopened.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exits 0; `uv run black --check .` exits 0; `uv run ruff check .` exits 0; `uv run mypy typsphinx/` exits 0.
    - `48-EVIDENCE.md` contains the four headings `## SC#2 — site enumeration`, `## SC#3 — the build-time mechanism is gone`, `## D-01 — no published contract changed` and `## Phase green gate`, each followed by a verbatim command line and its output.
    - The `## D-01` section contains, in addition to the grep transcript, an explicit sentence recording that a reference to an `:orphan:` target now degrades with no diagnostic at any layer, and no replacement diagnostic was added: `grep -rn 'logger.warning' typsphinx/translator.py | grep -c 'non-included'` returns 0.
    - `grep -rn master_included_docnames typsphinx/ tests/` exits 1, and that transcript is pasted in the SC#3 section.
    - The SC#2 section's table has at least four rows and names `typsphinx/translator.py` line numbers that resolve in the current tree.
    - `grep -c '\[x\] \*\*XREF-03' .planning/REQUIREMENTS.md` returns 1 and `grep -c '\[x\] \*\*XREF-04' .planning/REQUIREMENTS.md` returns 1.
    - `git diff .planning/REQUIREMENTS.md | grep -c '^[-+].*\*\*\(COMP\|OUT\|BLD\|IMG\|DOC\|REL\)-'` returns 0 (no other requirement's line was touched).
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>Every structural success criterion is discharged by a pasted repo-wide grep, the published-contract absence is re-measured rather than assumed, the suite and the three quality gates are green, and both requirements are checked off against named evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` / `.rst` author input → emitted Typst source | Unchanged by this plan; recorded for completeness |
| Emitted `.typ` → `typst.compile()` | Exercised at corpus scale by this plan's cost measurement |

## STRIDE Threat Register

ASVS level 1, blocking threshold `high`. ASVS **V2 (Authentication)**, **V3 (Session Management)**,
**V4 (Access Control)** and **V6 (Cryptography)** are **structurally inapplicable** — typsphinx is a
local Sphinx/Typst document-build tool with no network service, no authentication, no session and no
cryptographic surface (`48-RESEARCH.md` §Security Domain, `48-VALIDATION.md` §Security Domain).
**V5 (Input Validation)** is already covered by the existing `escape_typst_string` / `_sanitize_label`
machinery, unchanged by this phase.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | Label interpolated into emitted Typst source | low | mitigate | Already mitigated in plans 48-02 and 48-03; this plan re-verifies the single-derivation property by repo-wide grep in the SC#2 section |
| T-48-02 | Denial of Service | Per-reference `query()` introspection pass at Typst compile time | low | accept | Measured here at full-corpus scale against tiers fixed before the measurement; the middle and top tiers each have a pre-agreed action, so the acceptance is bounded rather than open-ended |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no packages. Binding constraint #7 forbids new runtime dependencies and a fifth `@preview` package; the corpus gate's cached clone is a pinned Sphinx source tree, not a package install |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 (full suite, including the `-m slow` corpus and render gates).
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all exit 0.
- `grep -rn master_included_docnames typsphinx/ tests/` exits 1.
- `48-EVIDENCE.md` carries the body-mode measurement (from plan 48-01), the D-11 cost record, the
  D-09 marker delta, the accepted label-collision limit, the SC#2/SC#3 grep transcripts, the D-01
  re-check and the green-gate record.
- `git worktree list` shows no leftover measurement worktree.
</verification>

<success_criteria>
- The guard's corpus-scale cost is a recorded finding measured against pre-fixed tiers, not a
  surprise (SC#4).
- D-09's output change is measured corpus-wide rather than argued from design.
- The guard's one new false-negative class is measured, filed and explicitly accepted by the owner.
- SC#2 and SC#3 are discharged by repo-wide greps pasted verbatim (milestone invariant #4).
- Deleting the build-time warning is confirmed to change no published contract (D-01).
- The phase closes green on the full suite plus `black` / `ruff` / `mypy` (binding constraint #8).
- XREF-03 and XREF-04 are checked off against named evidence.
</success_criteria>

<output>
Create `.planning/phases/48-compile-time-cross-reference-guard/48-04-SUMMARY.md` when done
</output>
