---
phase: 36-shared-emission-seam-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["36-02"]
files_modified:
  - tests/fixtures/inline_math_after_text_render_gate/index.rst
  - tests/fixtures/inline_math_pdf_text_mitex.golden.txt
  - tests/fixtures/inline_math_pdf_text_native.golden.txt
  - tests/test_inline_math_after_text_render_gate.py
  - typsphinx/translator.py
  - .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md
autonomous: true
requirements: [MATH-02]

must_haves:
  truths:
    - "D-04: RED lives in the emitted `.typ` (two blank lines after the math pre-fix, one post-fix); the compiled PDF carries an invariance assertion, not a RED one — planning re-measured on the real multi-construct fixture that pre-fix and post-fix extracted PDF text are identical on BOTH the mitex and native paths, so a PDF-text RED is impossible"
    - "D-05: the phase is verified against the corrected SC#3 — the redundant blank line is AFTER the math expression, between the math call and the following `parbreak()`, and the PDF's role is invariance rather than RED"
    - "D-06: the fix clears the flag rather than refraining from setting it — the single trailing statement in `visit_math_block` changes from setting `list_item_needs_separator` true to setting it false, which is sufficient for the `:label:` path precisely because it runs unconditionally AFTER `_emit_id_anchors` has already written the flag"
    - "D-07: MATH-02 lands in its own commit, after the decoupling commit, so SC#2's recorded diff is never contaminated by a byte-changing edit"
    - "Boundary edge (MATH-02): block math inside a list item is followed by exactly one blank line — not two (the pre-fix defect) and not zero (which would juxtapose the math call against the next sibling) — asserted as exact strings on the emitted `.typ` for the plain and `:label:` forms on both the mitex and native paths"
    - "Adjacency edge (MATH-02): the math call and its following sibling never abut with no separator, and the `:label:` id anchor and the math call never land on the same line — both asserted as absence of the zero-separator forms"
    - "Empty / single-element edge (MATH-02): a list item whose sole child is block math emits byte-identically across the fix, because with no following sibling the trailing flag has no consumer — pinned by an exact-string assertion on a new fixture construct rather than left unexercised"
    - "Ordering edge (MATH-02): the trailing flag reset executes after both `_emit_id_anchors` and the math emission, so the post-math flag state does not depend on whether the equation carried a `:label:` — proven by the plain and labelled forms producing identical blank-line counts"
    - "Encoding edge (MATH-02): the emitted-`.typ` assertions compare UTF-8-decoded text with exact Python string equality (code points, no normalization, no whitespace stripping), while the PDF-text invariance guard compares extracted text after the module's existing NFKC normalization step, because mitex renders math letters as Mathematical Alphanumeric Symbols that are not code-point-equal to the authored ASCII"
    - statement: "Precision edge (MATH-02): the fix introduces no lossy, rounded, or thresholded behaviour — the separator count is an exact small integer, not a computed quantity — so there is no precision contract to pin beyond the exact-string assertions already listed; no dedicated precision test exists and none is authored"
      verification: backstop
    - "The full {mitex, native} x {plain, labelled} matrix is exercised from ONE fixture via the existing `-D typst_use_mitex=0` override — no second fixture project is created"
    - "No existing exact-string assertion in `tests/` is regenerated from the fixed translator's output; the GREEN strings are derived by hand from the recorded pre-fix strings by removing exactly one newline, and that derivation is recorded"
  artifacts:
    - path: "tests/fixtures/inline_math_after_text_render_gate/index.rst"
      provides: "Construct H — a list item whose sole child is block math (the single-element edge)"
      contains: "Construct H"
    - path: "tests/fixtures/inline_math_pdf_text_mitex.golden.txt"
      provides: "pre-fix pypdf-extracted PDF text, mitex path"
    - path: "tests/fixtures/inline_math_pdf_text_native.golden.txt"
      provides: "pre-fix pypdf-extracted PDF text, native path"
    - path: "tests/test_inline_math_after_text_render_gate.py"
      provides: "SC#3 structural assertions and the D-04 PDF-text invariance guard"
    - path: "typsphinx/translator.py"
      provides: "the MATH-02 fix in visit_math_block's trailing separator bookkeeping"
  key_links:
    - from: "typsphinx/translator.py visit_math_block"
      to: "the next list-item sibling's leading separator check"
      via: "self.list_item_needs_separator — the single cross-cutting instance attribute both sides read"
      pattern: "list_item_needs_separator"
    - from: "tests/test_inline_math_after_text_render_gate.py"
      to: "tests/fixtures/inline_math_pdf_text_mitex.golden.txt"
      via: "exact equality between freshly extracted PDF text and the committed pre-fix baseline"
      pattern: "golden"
    - from: "tests/test_inline_math_after_text_render_gate.py"
      to: "the native emission path"
      via: "extra_args=('-D', 'typst_use_mitex=0') on the existing subprocess helper"
      pattern: "typst_use_mitex"
  prohibitions:
    - "MUST NOT derive the GREEN expected strings by running the FIXED translator and copying its output. Milestone invariant #4 names regenerating expected strings from the new code's own output as a violation, not a shortcut: a test written that way passes by construction and certifies nothing, while looking indistinguishable from a real gate. The GREEN strings are obtained by hand from the recorded pre-fix strings, and the derivation is written down before the fix is applied."
---

<objective>
Close MATH-02: block math inside a list item stops emitting a redundant second blank line, proven by
a structural assertion recorded RED against the unfixed translator and GREEN after a one-token change.

Purpose: this is the v0.6.5 Phase 34 review finding (WR-01), deferred out of that release only
because it looked like it would force re-deriving the GATE-01 fixture's expected strings immediately
before a tag. 36-CONTEXT.md measured that claim and found it wrong — the exact-string blast radius is
zero, because no assertion anywhere in `tests/` pins the shape AFTER a block-math expression. So the
work here is adding the missing assertion, not migrating old ones.

Per milestone invariant #4 the RED is structural, not a compile fatal: the defect compiles fine
today. Per D-04 the compiled PDF gets an invariance guard instead of a RED, because planning
re-measured on the real multi-construct fixture that the fix produces identical extracted text on
both emission paths — which turns "this change is inert" from a claim into a test.

Output: one new fixture construct, two committed PDF-text baselines, new assertions on the two
existing test methods plus one new invariance method, a one-token change in
`typsphinx/translator.py`, and the MATH-02 RED and GREEN sections appended to `36-GATE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-shared-emission-seam-cleanup/36-CONTEXT.md
@.planning/phases/36-shared-emission-seam-cleanup/36-RESEARCH.md
@.planning/phases/36-shared-emission-seam-cleanup/36-PATTERNS.md
@.planning/todos/pending/2026-07-29-visit-math-block-redundant-blank-line-in-list-items.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See the table in `36-01-PLAN.md` § "Artifacts this phase produces". This plan contributes the
"Construct H" fixture text, the two `*.golden.txt` PDF-text baselines, the module constants
`PDF_TEXT_BASELINE_MITEX` and `PDF_TEXT_BASELINE_NATIVE`, and the new test method
`test_block_math_pdf_text_is_invariant_across_the_math02_fix` on the existing class
`TestInlineMathAfterTextRenderGate`. The changed statement in `visit_math_block` is an edit to an
existing method, not a new symbol.

## Execution environment (MANDATORY — worktree-isolated execution is this repo's standing mode)

Detect isolation with `test -f .git` (a FILE means an isolated worktree). Provision with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` FIRST, then run everything through
`uv run`. Apply the NixOS shim once after `uv sync`:
`for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`.

## Measured facts handed down from planning (re-verify in your worktree; do not transcribe blindly)

Planning applied the candidate one-token change to a scratch copy of the tree, measured the result
with real `sphinx-build` runs, and reverted. These are the measured outcomes. Re-derive each from
your own pre-fix build before writing an assertion — but they tell you what to expect, and they
resolve 36-RESEARCH.md's flagged assumption A1 (the post-fix shape had been hand-traced, not run).

1. **Construct E, mitex.** Pre-fix, the math call is followed by three newlines before `parbreak()`;
   post-fix, by two. The GREEN string is the pre-fix string with exactly one newline removed.
2. **Construct G, mitex.** Same one-newline delta. Note the labelled equation's math payload is not
   the bare `G = m a` — the directive's content carries trailing blank lines into
   `node.astext()`, so the emitted call spans three source lines. Copy the payload verbatim from
   your own pre-fix build; do not retype it from the reST.
3. **Constructs E and G, native (`-D typst_use_mitex=0`).** Identical one-newline delta on the
   `$ ... $` forms.
4. **Construct H (new).** Pre-fix and post-fix emission are **byte-identical** — with no following
   sibling there is no consumer for the flag. This is the single-element edge, and its assertion is
   an invariance assertion, not a RED one.
5. **PDF extracted text.** Identical pre-fix and post-fix on both paths; page count identical.
6. **PDF bytes are NOT reproducible** — Typst embeds `CreationDate` and `ModDate`, so two builds of
   identical input seconds apart differ. Assert on extracted text only. Never assert PDF byte
   equality or byte size, and do not carry 36-CONTEXT.md's "22,855 bytes" figure into any assertion;
   it came from a different, single-construct scratch fixture during discussion.

## Scope fence — what this plan must not touch

36-RESEARCH.md's blast-radius table maps roughly thirty read/write sites of
`list_item_needs_separator`. Exactly one statement changes.

- Do NOT change `visit_math_block`'s LEADING separator check. It also fails to reset the flag, and
  that is pre-existing, harmless in context, and outside MATH-02's stated scope. Touching it risks
  changing the already-correct leading-side byte shape that the two existing pinned assertions in
  this very test module depend on.
- Do NOT change `_emit_id_anchors`' own body. MATH-02 only touches code that calls it.
- Do NOT generalise the change into `visit_math` (inline), `visit_Text`, or any other handler.
- Do NOT edit the three unit-test modules `tests/test_math_mitex.py`, `tests/test_math_native.py`,
  `tests/test_math_fallback.py` — they drive a bare translator with no list-item context and contain
  no newline-bearing assertions, so they are unaffected.

<tasks>

<task type="auto">
  <name>Task 1: Add Construct H and capture the pre-fix PDF-text baselines</name>
  <files>tests/fixtures/inline_math_after_text_render_gate/index.rst, tests/fixtures/inline_math_pdf_text_mitex.golden.txt, tests/fixtures/inline_math_pdf_text_native.golden.txt</files>
  <read_first>
    - `tests/fixtures/inline_math_after_text_render_gate/index.rst` — the seven existing constructs A through G, their prose-sentence-then-construct house style, and in particular Construct F (a list item whose sole content is INLINE math), which Construct H mirrors for BLOCK math
    - `tests/fixtures/inline_math_after_text_render_gate/conf.py` — confirms `typst_use_mitex` is deliberately unset so the native path is reached by CLI override from the same fixture
    - `tests/test_inline_math_after_text_render_gate.py` lines 218-260 — the existing `pypdf.PdfReader` extraction idiom and the NFKC-normalization comment explaining why mitex math letters are not code-point-equal to the source
    - `.planning/phases/36-shared-emission-seam-cleanup/36-RESEARCH.md` § "Fixture matrix for SC#3" and § "PDF invariance guard mechanics (D-04, Q6)"
  </read_first>
  <action>
Append one new construct to `tests/fixtures/inline_math_after_text_render_gate/index.rst`, after
Construct G, in the same house style (one short ASCII prose sentence naming what it exercises, then
the construct):

**Construct H — a list item whose sole content is display math.** A single-item bullet list whose
only child is a `.. math::` directive with a short ASCII body distinct from every other equation in
the file, so its emitted call is uniquely greppable. No `:label:`, no surrounding paragraph, nothing
after it inside the item. This is the block-math single-element edge: with no following sibling there
is nothing for the trailing separator flag to affect, so the construct's emission must be identical
before and after the MATH-02 fix. Construct F already covers the same edge for inline math; H is its
block counterpart and is the reason the edge-probe's `empty` row can be resolved as covered rather
than backstopped.

Then capture the two PDF-text baselines from the UNFIXED translator. Build the fixture with
`-b typstpdf` twice — once with no extra arguments (mitex, the default) and once with
`-D typst_use_mitex=0` (native) — read each resulting `index.pdf` with `pypdf.PdfReader`, join every
page's `extract_text()` with a newline exactly as the existing test module does, and write the two
results verbatim to `tests/fixtures/inline_math_pdf_text_mitex.golden.txt` and
`tests/fixtures/inline_math_pdf_text_native.golden.txt` with `encoding="utf-8"`. Put these two files
directly under `tests/fixtures/`, NOT inside the fixture's Sphinx source directory, so they can
never be mistaken for build input. Do not hand-edit either file; they are verbatim captures.

Record, for Task 3's evidence section, the exact commands used, the page count of each PDF, and the
character length of each extracted text. Do not record or assert either PDF's byte size — planning
measured that Typst embeds a creation timestamp, so two builds of identical input differ in bytes.

`typsphinx/` must stay untouched in this task: the baselines are only meaningful if they measure the
unfixed translator.
  </action>
  <verify>
    <automated>uv run pytest tests/test_inline_math_after_text_render_gate.py -q && test -s tests/fixtures/inline_math_pdf_text_mitex.golden.txt && test -s tests/fixtures/inline_math_pdf_text_native.golden.txt && grep -q 'Construct H' tests/fixtures/inline_math_after_text_render_gate/index.rst</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/inline_math_after_text_render_gate/index.rst` contains the literal `Construct H` and now holds exactly 3 lines beginning `.. math::` (E, G, and H): `grep -c '^ *\.\. math::' tests/fixtures/inline_math_after_text_render_gate/index.rst` prints `3`.
    - The fixture is still pure ASCII: `LC_ALL=C grep -n '[^[:print:][:space:]]' tests/fixtures/inline_math_after_text_render_gate/index.rst` prints nothing.
    - `uv run pytest tests/test_inline_math_after_text_render_gate.py -q` exits 0 — adding Construct H breaks none of the module's existing substring assertions.
    - Both baseline files exist, are non-empty, and are valid UTF-8: `uv run python -c "import pathlib,sys; [pathlib.Path(p).read_text(encoding='utf-8') for p in ('tests/fixtures/inline_math_pdf_text_mitex.golden.txt','tests/fixtures/inline_math_pdf_text_native.golden.txt')]"` exits 0.
    - Each baseline contains the prose sentinel `Text before block math.` — proving the extraction actually captured the document rather than an empty page.
    - The two baselines differ from each other (the two emission paths typeset math differently): `cmp -s tests/fixtures/inline_math_pdf_text_mitex.golden.txt tests/fixtures/inline_math_pdf_text_native.golden.txt` exits non-zero.
    - Neither baseline file lives inside the fixture's Sphinx source directory: `test ! -e tests/fixtures/inline_math_after_text_render_gate/inline_math_pdf_text_mitex.golden.txt` exits 0.
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>Construct H exists in the fixture, both pre-fix PDF-text baselines are captured verbatim from real compiles, the existing module is still green, and `typsphinx/` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the SC#3 structural assertions and the D-04 invariance guard, and record RED</name>
  <files>tests/test_inline_math_after_text_render_gate.py, .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md</files>
  <read_first>
    - `tests/test_inline_math_after_text_render_gate.py` in full — the module docstring, the sentinel and leak-signature constants, the `_run_sphinx_build_typstpdf` helper with its `extra_args` plumbing, and both existing test methods with their numbered assertion blocks (the new assertions extend these methods in the same numbered style)
    - `typsphinx/translator.py` lines 4014-4092 — `visit_math_block` in full: the `_emit_id_anchors` call, the leading separator check, the mitex-versus-native branch, the unconditional block separator, and the trailing bookkeeping statement that is the one thing Task 3 changes
    - `.planning/todos/pending/2026-07-29-visit-math-block-redundant-blank-line-in-list-items.md` — the original WR-01 record and both candidate fixes; note 36-CONTEXT.md corrected its blast-radius claim to zero
    - `.planning/milestones/v0.6.5-phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md` — the RED section shape being mirrored
    - `.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md` — the sections Plans 01 and 02 already wrote, so the new sections append rather than overwrite
  </read_first>
  <action>
First derive the strings, then write them, then record RED. Derivation order is the invariant-#4
discipline and is not optional.

**Derive.** Build the fixture with `-b typst` on the current (unfixed) tree, twice — mitex default
and `-D typst_use_mitex=0`. From each emitted `index.typ`, copy the exact region running from the
math call to the following `parbreak()` for Construct E and for Construct G, verbatim, including the
labelled equation's multi-line payload. Write those four pre-fix strings down. Then obtain the four
GREEN strings by removing exactly one newline from each — nothing else. Also copy Construct H's
emitted region verbatim; that one is used unchanged as an invariance assertion.

**Write.** Extend `tests/test_inline_math_after_text_render_gate.py`:

In `test_typstpdf_separates_inline_math_mitex_path`, append numbered assertions in the module's
existing style asserting, for Construct E and for Construct G: the GREEN string is present; the
two-blank-line pre-fix form is absent; and the zero-blank-line form — the math call immediately
followed by a single newline and `parbreak()` — is also absent. The pair of absence assertions is
what makes this a boundary check rather than a presence check: one step either side of the intended
count fails. Add one assertion pinning Construct H's emitted region exactly as measured.

In `test_typstpdf_separates_inline_math_native_path`, append the same set for the `$ ... $` forms.

Add module constants `PDF_TEXT_BASELINE_MITEX` and `PDF_TEXT_BASELINE_NATIVE` holding the two
baseline file paths (resolved via `Path(__file__).parent / "fixtures" / ...`), and add one new test
method `test_block_math_pdf_text_is_invariant_across_the_math02_fix` on the existing class. It builds
the fixture with `-b typstpdf` on both paths, extracts text with the module's existing `pypdf` idiom,
and asserts exact equality against each committed baseline, with a `difflib` rendering in the failure
message. Assert nothing about PDF byte size or byte equality. Keep the existing NFKC-normalization
assertion untouched — the invariance guard compares raw extracted text, and NFKC is only used for the
pre-existing math-letter sentinel check.

Every new assertion must carry a failure message that includes the emitted `.typ` (or the extracted
text), matching the module's existing convention, so a RED run is self-documenting.

**Record RED.** Run the module. It must fail, and it must fail on the new blank-line assertions.
Append `## RED — pre-fix run (SC#3, D-04, D-06)` to `36-GATE-EVIDENCE.md`, naming the commit measured
(at which `git status --porcelain typsphinx/` is empty), the date, and the commands. Add
sub-sections: `### Verbatim pytest failure output`; `### Pre-fix emitted regions per construct`
holding the four pre-fix strings and Construct H's region verbatim, both paths; and
`### GREEN string derivation` stating explicitly, for each of the four, that the GREEN string is the
pre-fix string with exactly one newline removed and that no string was taken from a fixed build.
Also record the PDF-text baseline capture commands, page counts, and text lengths from Task 1 under
`### PDF-text baseline capture (pre-fix)`.

`typsphinx/` must stay untouched in this task.
  </action>
  <verify>
    <automated>! uv run pytest tests/test_inline_math_after_text_render_gate.py -q -k "mitex_path or native_path" && uv run pytest tests/test_inline_math_after_text_render_gate.py -q -k "invariant" && grep -q '^## RED — pre-fix run' .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_inline_math_after_text_render_gate.py -q -k "mitex_path or native_path"` exits NON-zero, and the failure output names one of the new blank-line assertions — not a collection error, an `ImportError`, a missing file, or a skip. This is the recorded RED.
    - `uv run pytest tests/test_inline_math_after_text_render_gate.py -q -k "invariant"` exits 0 — the invariance guard is trivially green pre-fix, by construction, and its value is that it must STAY green in Task 3.
    - The module contains the new method: `uv run python -c "import ast,pathlib,sys; m=ast.parse(pathlib.Path('tests/test_inline_math_after_text_render_gate.py').read_text(encoding='utf-8')); names={n.name for n in ast.walk(m) if isinstance(n,ast.FunctionDef)}; sys.exit(0 if 'test_block_math_pdf_text_is_invariant_across_the_math02_fix' in names else 1)"` exits 0.
    - The module asserts nothing about PDF byte size: `uv run python -c "import pathlib,sys; s=pathlib.Path('tests/test_inline_math_after_text_render_gate.py').read_text(encoding='utf-8'); sys.exit(0 if s.count('st_size') == 2 else 1)"` exits 0 — the two pre-existing non-empty-file checks remain and no size comparison was added.
    - The evidence file contains `## RED — pre-fix run (SC#3, D-04, D-06)` and the sub-headings `### Verbatim pytest failure output`, `### Pre-fix emitted regions per construct`, `### GREEN string derivation`, and `### PDF-text baseline capture (pre-fix)`.
    - The evidence file's `### GREEN string derivation` section states for each of the four strings that exactly one newline was removed, and contains the phrase `no string was taken from a fixed build`.
    - `uv run black --check tests/test_inline_math_after_text_render_gate.py` and `uv run ruff check tests/test_inline_math_after_text_render_gate.py` exit 0.
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>The SC#3 assertions exist and are RED against the unfixed translator, the invariance guard is green, and the RED capture plus the GREEN-string derivation are recorded verbatim against a named commit.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the MATH-02 fix and record GREEN</name>
  <files>typsphinx/translator.py, .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md</files>
  <read_first>
    - `typsphinx/translator.py` lines 4014-4092 — `visit_math_block` in full, especially the comment block immediately above the trailing bookkeeping statement, which currently explains why the flag is set and must be rewritten to explain why it is cleared
    - `.planning/phases/36-shared-emission-seam-cleanup/36-RESEARCH.md` § "MATH-02 fix shape (D-06)" and § "Every `list_item_needs_separator` writer/reader" — the blast-radius table naming everything that must NOT change
    - `.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md` — the RED section written in Task 2, whose GREEN counterpart this task writes
    - `.planning/ROADMAP.md` § "Phase 36" success criterion 3 — the corrected SC#3 wording (blank line AFTER the math; PDF invariance rather than PDF RED) this task discharges
  </read_first>
  <action>
Change exactly one statement in `visit_math_block`: the trailing bookkeeping that marks the next
list-item sibling as needing a separator now clears `self.list_item_needs_separator` instead of
setting it. The guard around it (`if self.in_list_item:`) stays. Nothing else in the method changes.

This is D-06's option (a) with its completion: it clears the flag rather than merely refraining from
setting it, which is what makes it correct for a `:label:`-carrying equation. `_emit_id_anchors` sets
the flag BEFORE the math is emitted, so a fix that only deleted the trailing statement would leave
the flag set and the next sibling would still contribute its own leading newline. Because the
changed statement runs unconditionally after both the anchor emission and the math emission, it
overwrites whatever state the leading path left behind — one statement covers both the plain and the
labelled forms, on both emission paths.

Rewrite the comment block above the statement so it explains the new behaviour: the method already
emits its own unconditional block separator, so unlike every other block-level handler it must
CLEAR the shared flag rather than arm it; arming it stacks a second separator on top of one that was
already emitted. Reference MATH-02 and the Phase 34 review finding it closes.

Then run the module. The two path tests must flip RED to GREEN and the invariance guard must stay
green. Append `## GREEN — post-fix run (SC#3, D-04, D-06)` to `36-GATE-EVIDENCE.md`, naming the fix
commit SHA and the RED commit SHA from Task 2, with sub-sections: `### Verbatim pytest passing
output`; `### Post-fix emitted regions per construct` holding the same four regions plus Construct H
verbatim from a post-fix build, both paths, so the one-newline delta is visible side by side with the
RED capture; and `### PDF text-invariance result` recording that extracted text equals both committed
baselines on both paths, with the page counts, and stating explicitly that PDF bytes were NOT
compared because Typst embeds a creation timestamp. Close with a `### RED → GREEN verdict` table:
one row per assertion, naming the RED commit, the GREEN commit, and the observed transition.

Also record, in one sentence, the confirmation that Construct H's emitted region is unchanged across
the fix — the single-element edge behaved as predicted.

If the invariance guard fails, do NOT regenerate the baselines. Record the difference verbatim under
`### PDF text-invariance FAILED` and surface it: D-04's premise was measured, so a failure is a real
finding.
  </action>
  <verify>
    <automated>uv run pytest tests/test_inline_math_after_text_render_gate.py -q && uv run pytest tests/test_desc_rubric_decoupling_render_gate.py -q && grep -q '^## GREEN — post-fix run' .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_inline_math_after_text_render_gate.py -q` exits 0 — all three methods green.
    - The change is one statement: `git diff HEAD~1 -- typsphinx/translator.py | grep -c '^[+-][^+-]'` prints a number no greater than `2` for the code lines outside the rewritten comment block; the diff touches only `visit_math_block`.
    - `visit_math_block`'s trailing bookkeeping no longer arms the flag: `uv run python -c "import ast,pathlib,sys; m=ast.parse(pathlib.Path('typsphinx/translator.py').read_text(encoding='utf-8')); f=[n for n in ast.walk(m) if isinstance(n,ast.FunctionDef) and n.name=='visit_math_block'][0]; assigns=[a for a in ast.walk(f) if isinstance(a,ast.Assign) and any(isinstance(t,ast.Attribute) and t.attr=='list_item_needs_separator' for t in a.targets)]; sys.exit(0 if len(assigns)==1 and isinstance(assigns[0].value,ast.Constant) and assigns[0].value.value is False else 1)"` exits 0.
    - The leading separator check inside `visit_math_block` is unchanged: `git diff <the Plan 01 baseline sha> -- typsphinx/translator.py` shows no change to the method's leading `add_text` newline branch.
    - `_emit_id_anchors` is unchanged across the whole phase: `git diff <the Plan 01 baseline sha> -- typsphinx/translator.py` contains no hunk whose enclosing function is `_emit_id_anchors`.
    - `tests/test_math_mitex.py`, `tests/test_math_native.py`, and `tests/test_math_fallback.py` are unmodified: `git diff <the Plan 01 baseline sha> --name-only` lists none of them.
    - The evidence file contains `## GREEN — post-fix run (SC#3, D-04, D-06)`, `### Post-fix emitted regions per construct`, `### PDF text-invariance result`, and `### RED → GREEN verdict`, and the invariance section states that PDF bytes were deliberately not compared.
    - `uv run pytest tests/test_desc_rubric_decoupling_render_gate.py -q` exits 0 — the SC#2 golden is untouched by MATH-02, because the decoupling fixture contains no math.
    - `uv run black --check .`, `uv run ruff check .`, and `uv run mypy typsphinx/` all exit 0.
  </acceptance_criteria>
  <done>Block math inside a list item is followed by exactly one blank line on both emission paths and both forms; the PDF's extracted text is provably unchanged; the RED→GREEN transition is recorded verbatim against two named commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed by this plan) | A one-token whitespace-bookkeeping change plus test assertions, inside a document compiler processing trusted local `.rst`. No user input, no network, no credential, no deserialization of untrusted data, no new external data path. |

## STRIDE Threat Register

ASVS level 1, `block_on: high`. 36-RESEARCH.md § "Security Domain" measured that **no ASVS category
applies** here: V2, V3, V4, and V6 have no surface at all, and V5 Input Validation is discharged by
docutils' own rST parser upstream of `typsphinx`, unchanged by this plan. No threat is fabricated to
fill this block. The rows below are the two determinations that are genuinely this plan's to make.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-03-01 | Tampering | `visit_math_block` separator emission | low | mitigate | The flag being cleared is read by roughly thirty other handlers, so an over-broad edit could change separator behaviour for unrelated node types. Mitigated by scope: exactly one statement changes, an AST assertion pins that `visit_math_block` now holds exactly one assignment to the flag and that its value is false, and the full suite plus the full-corpus `-b typstpdf` gate (Plan 04) re-assert every other handler's emission. |
| T-36-SC | Tampering | dependency surface (`pyproject.toml`, `uv.lock`) | low | accept | No package-manager install occurs; zero new packages (36-RESEARCH.md § "Package Legitimacy Audit", milestone invariant #1). Plan 04 asserts `git diff` shows zero change to both files across the phase. |
</threat_model>

<verification>
1. `uv run pytest tests/test_inline_math_after_text_render_gate.py -q` exits 0 after Task 3 and exits non-zero after Task 2 (recorded RED).
2. `uv run pytest tests/test_desc_rubric_decoupling_render_gate.py -q` exits 0 — the decoupling gate is unaffected.
3. The AST assertion confirms exactly one flag assignment in `visit_math_block`, with a false value.
4. Extracted PDF text equals both committed pre-fix baselines on both emission paths.
5. `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all exit 0.
</verification>

<success_criteria>
- ROADMAP SC#3 (as corrected per D-05) is satisfied: block math inside a list item is followed by
  exactly one blank line, asserted structurally on the emitted `.typ`, on both the mitex and native
  paths and on both the plain and `:label:`-carrying forms, with the assertion recorded RED against
  the unfixed translator before the fix landed.
- The compiled PDF carries an invariance assertion, green before and after, proving the change inert.
- MATH-02 landed in its own commit, after the decoupling commit (D-07).
- No expected string anywhere was regenerated from the fixed translator's output.
</success_criteria>

<output>
Create `.planning/phases/36-shared-emission-seam-cleanup/36-03-SUMMARY.md` when done.
</output>
