---
phase: 36-shared-emission-seam-cleanup
plan: 04
type: execute
wave: 4
depends_on: ["36-03"]
files_modified:
  - .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md
  - .planning/todos/pending/2026-07-30-rubric-with-inline-markup-leaks-in-list-item-and-drops-par.md
autonomous: true
requirements: [ADM-06, MATH-02]

must_haves:
  truths:
    - "The full suite, the lint/type trio, and the full-corpus `-b typstpdf` gate are all run and recorded, with the pre-change baseline recorded alongside — SC#4's acceptance is `post-change failing node-ID set == the Plan 01 pre-change set`, never `zero failures`, because project memory records an environmental failure class in this sandbox"
    - "The test-migration census required by milestone invariant #5 is recorded: every touched test file and render-gate class is named, and the count of existing exact-string assertions that had to be re-derived is stated with the measurement that produced it"
    - "The claim that MATH-02's exact-string blast radius is zero is RE-MEASURED in this plan rather than inherited — 36-CONTEXT.md's own correction to the folded todo demonstrates that a stated blast radius can be wrong"
    - "D-02: the deferred `par()`-loss defect is carried as a filed todo whose text matches the post-decoupling code, and is routed to Phase 39, which owns `rubric`"
    - "Milestone invariant #1 holds: `pyproject.toml` and `uv.lock` are unchanged across the whole phase, and the four bundled `@preview` package versions are unchanged on every lockstep surface"
    - "`36-GATE-EVIDENCE.md` closes with a verdict table carrying one row per ROADMAP success criterion, each row naming the evidence section that discharges it"
    - "No file named `36-VERIFICATION.md` exists anywhere in the phase directory — that name is reserved by the verify stage and is overwritten wholesale"
  artifacts:
    - path: ".planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md"
      provides: "the regression sweep, the test-migration census, and the phase verdict table"
      contains: "Phase 36 verdict"
    - path: ".planning/todos/pending/2026-07-30-rubric-with-inline-markup-leaks-in-list-item-and-drops-par.md"
      provides: "the deferred par()-loss defect, retargeted at Phase 39 and corrected for the post-decoupling code"
      contains: "resolves_phase"
  key_links:
    - from: ".planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md § Pre-change full-suite baseline"
      to: ".planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md § Regression sweep"
      via: "set difference between the two recorded failing-node-ID sets — the only sound reading of SC#4 in this sandbox"
      pattern: "failing"
    - from: ".planning/todos/pending/2026-07-30-rubric-with-inline-markup-leaks-in-list-item-and-drops-par.md"
      to: "Phase 39"
      via: "resolves_phase front-matter key"
      pattern: "resolves_phase"
  prohibitions:
    - "MUST NOT narrow SC#4's definition of green — by excluding a test file, adding a skip or xfail marker, deselecting a node ID, or reporting the suite result with any test filtered out. If a test fails, it is either in the recorded pre-change baseline (and is therefore environmental and named as such) or it is a real regression to fix. Quietly shrinking the run so the number looks clean produces a green that certifies nothing, which is the one outcome that makes every other criterion in this phase worthless."
---

<objective>
Close SC#4: run every gate, prove the post-change failure set is identical to the pre-change one,
record the test-migration census milestone invariant #5 requires, and write the phase verdict.

Purpose: this phase's whole claim is "nothing changed except one blank line". That claim is only
worth as much as the sweep behind it. The sweep here is deliberately compared against the baseline
Plan 01 recorded on the untouched tree, not against an absolute zero, because project memory records
that a set of integration tests fail in this sandbox for provisioning reasons unrelated to any code
change — an absolute-zero criterion would either strand the phase or invite someone to quietly
delete tests until the number looked right.

Output: the regression-sweep, census, and verdict sections of `36-GATE-EVIDENCE.md`, and a corrected,
Phase-39-routed todo for the deferred `par()`-loss defect.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-shared-emission-seam-cleanup/36-CONTEXT.md
@.planning/phases/36-shared-emission-seam-cleanup/36-RESEARCH.md
@.planning/phases/36-shared-emission-seam-cleanup/36-01-SUMMARY.md
@.planning/phases/36-shared-emission-seam-cleanup/36-02-SUMMARY.md
@.planning/phases/36-shared-emission-seam-cleanup/36-03-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See the table in `36-01-PLAN.md` § "Artifacts this phase produces". This plan produces no new code
symbols. It writes the closing sections of `36-GATE-EVIDENCE.md` and edits an existing todo file.

## Execution environment (MANDATORY — worktree-isolated execution is this repo's standing mode)

Detect isolation with `test -f .git` (a FILE means an isolated worktree). Provision with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` FIRST, then run everything through
`uv run`. Apply the NixOS shim once after `uv sync`:
`for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`.
36-RESEARCH.md records that, with the shim applied, Phase 34's worktree executors reported full
suites byte-identical to the main tree — so do NOT pre-emptively exclude the integration test files;
only fall back to naming them as environmental if this specific worktree proves the shim insufficient,
and then only against the Plan 01 baseline, never by deselecting them from the run.

<tasks>

<task type="auto">
  <name>Task 1: Run and record the SC#4 regression sweep</name>
  <files>.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md` § "Pre-change full-suite baseline (SC#4)" — the recorded failing-node-ID set this sweep is compared against, and the recorded lint/type trio exit codes
    - `.planning/milestones/v0.6.5-phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md` § "Regression sweep — suite, lint, invariants" and § "Regression sweep — corpus gate and docs dogfooding" — the four-step shape (full suite, set-difference comparison, CI command set, mechanical invariant checks) and the corpus-gate command being reused verbatim
    - `tests/test_corpus_gate.py` — the `slow` marker and the graceful-skip behaviour when the cached Sphinx corpus is absent, so a skip is recorded honestly as a skip rather than reported as a pass
    - `tests/test_preview_version_sync.py` — the `@preview` lockstep invariant this sweep re-asserts
    - `.planning/ROADMAP.md` § "v0.7.0" binding constraints — milestone invariant #1 (zero new runtime dependencies, `@preview` count stays at four with no new lockstep site)
  </read_first>
  <action>
Append `## Regression sweep — suite, lint, invariants (SC#4)` to `36-GATE-EVIDENCE.md`, structured as
four numbered steps, each with its verbatim command and verbatim output.

Step 1 — the full suite, run with the SAME command Plan 01 used for the baseline
(`uv run pytest -q --tb=no -rf`), with its verbatim tail and the complete sorted set of failing test
node IDs.

Step 2 — the set-difference comparison against Plan 01's pre-change set. Record three explicit
groups: node IDs failing before and after (environmental, carried), node IDs failing only after
(regressions — any non-empty group here blocks the phase), and node IDs failing only before (fixed
incidentally). State the verdict as a sentence naming which groups are empty. Do not paraphrase the
baseline set; re-read it from the evidence file so the comparison is against the recorded artifact,
not against memory.

Step 3 — the CI command set verbatim from CLAUDE.md, each with its exit code and last output line:
`uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/`.

Step 4 — the milestone invariants, asserted mechanically over the whole phase diff rather than by
inspection. Record, with commands and output: that `git diff` between the Plan 01 baseline commit and
HEAD shows zero change to `pyproject.toml` and to `uv.lock`; that
`uv run pytest tests/test_preview_version_sync.py -q` exits 0; and the full `--name-only` list of
files the phase touched, with one sentence confirming that every entry is either a test file, a
fixture file, `typsphinx/translator.py`, or a planning artifact.

Then append `## Regression sweep — corpus gate (SC#4)` recording
`uv run pytest tests/test_corpus_gate.py -q -m slow` verbatim. This gate is `slow`-marked and must be
run explicitly. If it skips because the cached Sphinx corpus is unavailable, record the skip and its
reason verbatim and say plainly that the gate did not run — never report a skip as a pass.

If Step 2's "failing only after" group is non-empty, stop. Record it verbatim under a
`### SC#4 FAILED — new failures` sub-heading and surface it. Do not deselect, skip, or xfail anything
to make the number smaller.
  </action>
  <verify>
    <automated>grep -q '^## Regression sweep — suite, lint, invariants' .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md && grep -q '^## Regression sweep — corpus gate' .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/</automated>
  </verify>
  <acceptance_criteria>
    - The evidence file contains both new top-level headings and four numbered steps under the first one.
    - The recorded post-change failing node-ID set is compared explicitly against Plan 01's recorded set, and the "failing only after" group is stated to be empty (or, if not, the plan is stopped and the group is recorded verbatim).
    - `uv run black --check .`, `uv run ruff check .`, and `uv run mypy typsphinx/` each exit 0 and their exit codes appear in the file.
    - `git diff <the Plan 01 baseline sha> HEAD -- pyproject.toml uv.lock` prints nothing, and that command plus its empty output appear in the file.
    - `uv run pytest tests/test_preview_version_sync.py -q` exits 0.
    - `git diff <the Plan 01 baseline sha> HEAD --name-only` lists only paths under `tests/`, the single path `typsphinx/translator.py`, and paths under `.planning/`.
    - The corpus-gate section records either a real pass or an explicit skip with its verbatim reason; it never describes a skip as a pass.
    - No `xfail`, `skip`, or `deselect` marker was added anywhere in this phase: `git diff <the Plan 01 baseline sha> HEAD -- tests/ | grep -E '^\+.*(xfail|--deselect)'` prints nothing.
  </acceptance_criteria>
  <done>The full suite, lint/type trio, milestone invariants, and corpus gate are all run and recorded verbatim, and the post-change failure set is proven identical to the pre-change baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Record the test-migration census, correct the deferred todo, and write the phase verdict</name>
  <files>.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md, .planning/todos/pending/2026-07-30-rubric-with-inline-markup-leaks-in-list-item-and-drops-par.md</files>
  <read_first>
    - `.planning/todos/pending/2026-07-30-rubric-with-inline-markup-leaks-in-list-item-and-drops-par.md` in full — the existing record, whose `files:` block and Problem section describe the PRE-decoupling code (it names the dummy-node delegation as the mechanism) and whose front matter carries no `resolves_phase`
    - `.planning/phases/36-shared-emission-seam-cleanup/36-CONTEXT.md` § "Deferred Ideas" — the measured reproduction and the routing decision (Phase 39 owns `rubric`), plus the note that real-corpus incidence has NOT been measured
    - `typsphinx/translator.py` — the post-decoupling `visit_rubric` / `depart_rubric`, so the todo's description matches the code as it now stands
    - `.planning/ROADMAP.md` § "Phase 36" — all four success criteria, verbatim, for the verdict table
    - `.planning/milestones/v0.6.5-phases/34-inline-math-after-text-separator-fix/34-GATE-EVIDENCE.md` § "Phase 34 verdict" — the closing verdict table shape
    - `.planning/todos/pending/2026-07-29-visit-math-block-redundant-blank-line-in-list-items.md` — the MATH-02 todo this phase resolves, so its disposition can be stated
  </read_first>
  <action>
**Census.** Append `## Test-migration census (SC#4, milestone invariant #5)` to
`36-GATE-EVIDENCE.md`. Milestone invariant #5 says test migration is owned per phase and is never
deferred to a blanket closing pass, so the census must be a measurement, not a restatement.

Record: a table of every test file this phase created or modified, with the render-gate classes and
test methods in each and whether they are new or extended; and the count of PRE-EXISTING exact-string
assertions that had to be re-derived by hand. 36-CONTEXT.md's correction to the folded MATH-02 todo
claims that count is zero — RE-MEASURE it rather than inherit it, and record the command you used.
A defensible measurement is a search across `tests/` for newline-bearing exact-string assertions that
mention a math call or a bold wrapper, cross-checked against the phase diff to confirm that no such
pre-existing assertion line was modified. Whatever the count turns out to be, state the number, the
command that produced it, and — if any assertion WAS re-derived — the derivation for each, since
milestone invariant #4 forbids regenerating them from the new output.

Also record the two `@preview`-adjacent and dependency invariants by pointer to Task 1's Step 4
rather than repeating the output.

**Todo.** Update
`.planning/todos/pending/2026-07-30-rubric-with-inline-markup-leaks-in-list-item-and-drops-par.md`
so it describes the code as it now stands and is routed:

- Add `resolves_phase: 39` to the front matter (Phase 39 owns `rubric` and the admonition taxonomy).
- Correct the mechanism description: after this phase, `visit_rubric` / `depart_rubric` no longer
  reach the shared body through a dummy node — they hold their own copy — but per D-02 that copy uses
  the SAME three single-slot save attributes, so the leak is unchanged in behaviour and unchanged in
  cause. Say this explicitly, because a reader arriving after Phase 36 would otherwise conclude from
  the old text that the decoupling already fixed it.
- Correct the `files:` block to name the post-decoupling locations, and note that the same leak now
  exists in three copies rather than one shared body, so a fix must rename the slots per handler in
  all three.
- Keep the existing measured reproduction and the note that real-corpus incidence has not been
  measured; add that measuring it is part of picking the todo up.

**Verdict.** Append `## Diff scope` recording `git diff --stat` between the Plan 01 baseline commit
and HEAD, then `## Phase 36 verdict` — a table with one row per ROADMAP success criterion (SC#1
through SC#4), each row carrying the criterion in short form, a met/not-met marker, and a pointer to
the evidence section in this file that discharges it. Add a closing paragraph naming the disposition
of both requirements (ADM-06, MATH-02) and of the two folded todos: the MATH-02 todo is resolved by
this phase and should be filed to `todos/completed/` at phase close, and the `par()`-loss todo stays
pending, routed to Phase 39.

Do not create, and do not rename anything to, `36-VERIFICATION.md` — that filename is reserved by the
verify stage, which overwrites it wholesale.
  </action>
  <verify>
    <automated>grep -q '^## Test-migration census' .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md && grep -q '^## Phase 36 verdict' .planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md && grep -q '^resolves_phase: 39' .planning/todos/pending/2026-07-30-rubric-with-inline-markup-leaks-in-list-item-and-drops-par.md && test ! -e .planning/phases/36-shared-emission-seam-cleanup/36-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The evidence file contains the headings `## Test-migration census (SC#4, milestone invariant #5)`, `## Diff scope`, and `## Phase 36 verdict`.
    - The census section names every file in `git diff <the Plan 01 baseline sha> HEAD --name-only -- tests/`, with no omissions, and states a re-derived-assertion count together with the command that measured it.
    - The verdict table has exactly four criterion rows (SC#1..SC#4), and every row points at a heading that actually exists in the same file.
    - The todo's front matter contains `resolves_phase: 39`, and its body contains a sentence stating that the Phase 36 decoupling did NOT fix the leak and why (the shared save-slot names, per D-02).
    - The todo's body names the post-decoupling reality that the same slots are now used by three handler pairs.
    - `.planning/phases/36-shared-emission-seam-cleanup/36-VERIFICATION.md` does not exist.
    - The evidence file's final paragraph names both ADM-06 and MATH-02 and both todo dispositions.
    - `uv run pytest tests/test_desc_rubric_decoupling_render_gate.py tests/test_inline_math_after_text_render_gate.py -q` still exits 0 (this task changes no code, so this is a no-drift check).
  </acceptance_criteria>
  <done>The census is measured and recorded, the deferred todo describes the post-decoupling code and is routed to Phase 39, and the evidence file closes with a four-row verdict table pointing at real sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed by this plan) | This plan runs test and lint commands and writes two markdown files. It changes no code, adds no dependency, and reads no untrusted input. |

## STRIDE Threat Register

ASVS level 1, `block_on: high`. 36-RESEARCH.md § "Security Domain" measured that **no ASVS category
applies** to this phase, and this plan narrows that further by touching no code at all. No threat is
fabricated to fill this block. The two rows below are the determinations this plan actually makes.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-04-01 | Repudiation | `36-GATE-EVIDENCE.md` | medium | mitigate | Every downstream reader — the verifier, the milestone audit, and Phase 37/39's planners — treats this file as the record of what was actually run. An unrun-but-recorded command would be indistinguishable from a real one and would silently authorise later phases to build on an unverified base. Mitigated by requiring verbatim command-plus-output pairs (never paraphrase), named commit SHAs at every RED/GREEN boundary, and a set-difference comparison against an artifact recorded before any change existed rather than against recollection. |
| T-36-SC | Tampering | dependency surface (`pyproject.toml`, `uv.lock`) | low | mitigate | Asserted mechanically in Task 1 Step 4: `git diff` between the Plan 01 baseline commit and HEAD shows zero change to either file, and `tests/test_preview_version_sync.py` re-asserts the four `@preview` package versions across every lockstep surface. Zero packages were installed in this phase, so the Package Legitimacy Gate is not triggered (36-RESEARCH.md § "Package Legitimacy Audit"). |
</threat_model>

<verification>
1. `uv run pytest -q --tb=no -rf` post-change failing node-ID set equals the Plan 01 pre-change set.
2. `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all exit 0.
3. `uv run pytest tests/test_corpus_gate.py -q -m slow` recorded verbatim (pass or explicit skip).
4. `git diff <baseline> HEAD -- pyproject.toml uv.lock` prints nothing.
5. `test ! -e .planning/phases/36-shared-emission-seam-cleanup/36-VERIFICATION.md` exits 0.
</verification>

<success_criteria>
- ROADMAP SC#4 is satisfied: the re-derived-assertion count is measured (not inherited), the touched
  test files and render-gate classes are recorded as a census, and the full suite, the lint/type
  trio, and the full-corpus `-b typstpdf` gate are recorded with the pre-change baseline alongside.
- The phase's green is a set-equality against the recorded baseline, and no test was excluded,
  skipped, xfailed, or deselected to reach it.
- The deferred `par()`-loss defect is filed, corrected for the post-decoupling code, and routed to
  Phase 39 (D-02).
- `36-GATE-EVIDENCE.md` closes with a four-row verdict table; no `36-VERIFICATION.md` exists.
</success_criteria>

<output>
Create `.planning/phases/36-shared-emission-seam-cleanup/36-04-SUMMARY.md` when done.
</output>
