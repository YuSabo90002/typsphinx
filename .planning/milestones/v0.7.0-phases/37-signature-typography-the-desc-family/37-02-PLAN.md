---
phase: 37-signature-typography-the-desc-family
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/signature_break_and_arrow_gate/conf.py
  - tests/fixtures/signature_break_and_arrow_gate/index.rst
  - tests/test_signature_break_and_arrow_gate.py
  - .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-02.md
autonomous: true
requirements: [SIG-06, SIG-08]

must_haves:
  truths:
    - "A fixture Sphinx project exists that produces, in ONE build, a nested py:class::/py:method:: pair (the measured source of SIG-08's doubled break), a sibling body-less desc pair (the control that must NOT change), a return-annotated signature (SIG-06), an optional-group signature whose group has a following sibling (the D-11 defect), and the nested-optional printf shape (the D-11 non-regression control)."
    - "SIG-06 is proven on the COMPILED PDF's extracted text -- the arrow glyph is present and the ASCII two-character arrow is absent -- not merely on the emitted .typ, because the requirement is that a real glyph reaches the reader."
    - "SIG-08 is proven by an exact hand-derived count of parbreak() statements in the emitted .typ for the nested-desc fixture, plus a positional assertion that no two parbreak() statements are adjacent with nothing between them."
    - "The fixture carries the constructs D-12's resolution needs to be falsifiable: the nested-desc doubling case, the 'content follows the nested member' case that a depth-counter implementation would break, and the sibling body-less control. D-12 is resolved in 37-EMISSION-CONTRACT.md section 8 and implemented in plan 37-05; this plan writes the assertions that can disprove it."
    - "D-11 carries its own success criterion and its own fixture, and is NEVER folded into a SIG-05 assertion: the optional-group separator must land inside the bracket, matching Sphinx's own HTML rendering measured this session."
    - "The D-11 non-regression control (the nested-optional printf shape, whose desc_optional nodes are both last children) is asserted to be UNCHANGED by the fix -- it is labelled as a control so nobody converts it into the defect case."
    - "The D-11 explicit-concatenation half is asserted as a NON-REGRESSION: the current tree already joins the closing bracket and the following parameter with an explicit operator, so the assertion pins that behaviour rather than introducing a fix."
    - "Every expected string and every expected count is hand-derived from 37-EMISSION-CONTRACT.md, and each assertion is recorded RED (or, for the two controls, GREEN) against the pre-phase translator with verbatim output."
  artifacts:
    - tests/fixtures/signature_break_and_arrow_gate/conf.py
    - tests/fixtures/signature_break_and_arrow_gate/index.rst
    - tests/test_signature_break_and_arrow_gate.py
    - .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-02.md
  key_links:
    - "tests/test_signature_break_and_arrow_gate.py -> sys.executable -m sphinx (-b typst for the structural legs, -b typstpdf or typst.compile() for the SIG-06 glyph leg)"
    - "SIG-06 glyph assertion -> pypdf.extract_text() with U+200B stripped first (contract section 4.2 measured hazard)"
    - "SIG-08 count assertion -> 37-EMISSION-CONTRACT.md section 8 (the emission-position marker, not a depth counter)"
    - "D-11 assertions -> 37-EMISSION-CONTRACT.md section 6.1 (target and control table, both measured against Sphinx's own HTML writer)"
---

<objective>
Stand up the SIG-06 (real arrow glyph), SIG-08 (exactly one break) and D-11 (optional-group separator)
gates against the untouched translator: one fixture project carrying the nested-`desc`, sibling-`desc`,
return-arrow and optional-group shapes, and an assertion module whose expected counts and strings are
hand-derived from `37-EMISSION-CONTRACT.md`.

Purpose: D-11 is covered by no SIG requirement and must therefore carry its own criterion and its own
fixture rather than being smuggled into a SIG-05 assertion. SIG-08's fix lands alone in Wave 2 (before
the wrapper changes) precisely so its assertion has one variable, not two — this plan writes that
single-variable assertion.

Output: fixture project, gate module, and `37-GATE-EVIDENCE-02.md`. This plan does NOT touch `typsphinx/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md
@.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md
@.planning/phases/37-signature-typography-the-desc-family/37-RESEARCH.md
@.planning/phases/37-signature-typography-the-desc-family/37-PATTERNS.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run`.

Without step 1 the worktree's pytest imports the MAIN checkout's `typsphinx`, so gates stay RED after
a correct fix.
</environment_setup>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. This plan
contributes:
- `tests/fixtures/signature_break_and_arrow_gate/{conf.py,index.rst}`
- `tests/test_signature_break_and_arrow_gate.py`
- `.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-02.md`

None of these exist in the pre-phase tree.
</artifacts_this_phase_produces>

<flagged_assumptions>
The spec-less edge probe returned SIG-06's and SIG-08's rows as `unclassified`. Per the probe
protocol an `unclassified` row stays `unresolved` and is surfaced here, never auto-resolved and never
silently dropped:

- **SIG-06 (unclassified, unresolved):** the probe assigned no edge category to "renders a real arrow
  glyph rather than the ASCII form". The planner's reading is that its only real edge is *glyph
  identity in the reader's artifact* — presence in the emitted source proves nothing, so the assertion
  lives on the compiled PDF's extracted text. Recorded as an assumption because the probe did not
  derive it.
- **SIG-08 (unclassified, unresolved):** the probe assigned no edge category to "separated by exactly
  one break". The planner's reading is that "exactly one" has two distinct edges the requirement text
  conflates — a *count* (how many breaks in total) and an *adjacency* (never two in a row with nothing
  between) — and that only the adjacency form is falsifiable independently of fixture size. Both are
  asserted. Recorded as an assumption because the probe did not derive the split.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Create the signature_break_and_arrow_gate fixture project</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §6.1, §7, §8
    - `tests/fixtures/desc_bodyless_concat_render_gate/index.rst` and `conf.py` (the sibling body-less `desc` control shape this fixture must also carry)
    - `tests/fixtures/desc_signature_render_gate/index.rst` (the existing nested-optional `printf` shape)
    - `typsphinx/translator.py:4624-4667` (`visit_desc`/`depart_desc` — the handlers under assertion)
  </read_first>
  <files>tests/fixtures/signature_break_and_arrow_gate/conf.py, tests/fixtures/signature_break_and_arrow_gate/index.rst</files>
  <action>
Create a minimal fixture project mirroring `tests/fixtures/desc_bodyless_concat_render_gate/`:
`conf.py` with `extensions = ["typsphinx"]` and a `typst_documents` entry making `index` a master
document.

`index.rst` must carry exactly these constructs, each under its own distinctive section heading so
assertions can slice the emitted `.typ` by region:

1. **The SIG-08 defect case** — a `py:class::` with a one-line body containing a nested `py:method::`
   with its own one-line body, followed by another top-level directive. Measured on the current tree,
   this exact nesting emits two consecutive break statements with nothing between them; that is the
   defect the count assertion pins.
2. **The SIG-08 "content follows the nested member" case** — a second `py:class::` whose body holds a
   nested `py:method::` AND a trailing paragraph after it. This is the case a naive depth-counter
   implementation breaks (the method and the trailing paragraph would run together), so the fixture
   must contain it and Task 2 must assert the separation survives.
3. **The SIG-08 control** — two back-to-back body-less `confval`-style `desc` siblings, mirroring
   `tests/fixtures/desc_bodyless_concat_render_gate/index.rst`. These must keep exactly one break
   between them before and after the fix.
4. **The SIG-06 case** — a `py:function::` with an explicit return annotation.
5. **The D-11 defect case** — `py:function:: connect(host, port=8080, [timeout], **kwargs)` with a
   one-line body. Its `desc_optional` group HAS a following sibling, which is precisely why the
   separator is currently lost.
6. **The D-11 non-regression control** — `py:function:: printf(fmt[, args[, more]])`. Both
   `desc_optional` nodes here are last children, so neither gains a separator; this rendering must be
   byte-unchanged by the fix. Label it in an rST comment as the control.

Keep the file free of rubrics, admonitions and inline bold — Phase 39 owns those and their presence
would make a diff on this fixture ambiguous.

Build it through `-b typst` and record the emitted `index.typ` and the exact count of break statements
it contains, so Task 2 has the measured pre-phase baseline to derive expectations against.
  </action>
  <acceptance_criteria>
    - `uv run python -m sphinx -b typst tests/fixtures/signature_break_and_arrow_gate <tmp>` exits 0.
    - The emitted `index.typ` contains at least one position where two break statements are adjacent
      with nothing but a newline between them (the SIG-08 defect, present pre-phase).
    - A doctree dump shows at least one `desc` node that is a descendant of another `desc` node
      (construct 1) and at least one `desc_optional` node that HAS a following sibling (construct 5).
    - A doctree dump shows at least one `desc_optional` node that has NO following sibling
      (construct 6, the control).
    - `tests/fixtures/signature_break_and_arrow_gate/index.rst` contains no `.. rubric::` and no
      `**bold**` markup.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m sphinx -b typst tests/fixtures/signature_break_and_arrow_gate /tmp/sigbreak && test -s /tmp/sigbreak/index.typ</automated>
  </verify>
  <done>The fixture builds cleanly and provably contains the nested-`desc` defect shape, the content-follows-nested-member shape, the sibling body-less control, a return annotation, and both the D-11 defect and D-11 control optional groups.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the SIG-06 / SIG-08 / D-11 gate module, hand-derived and recorded RED</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §4.2 (the ZWSP extraction hazard), §6.1, §7, §8
    - `tests/test_desc_bodyless_concat_render_gate.py` in full (the structural break-count assertion pattern and the `-b typstpdf` helper variant this file clones)
    - `tests/test_pdf_render_gate.py` lines 141-178 and 731-810 (the `sys.executable -m sphinx` helper and the existing compiled-PDF arrow/optional-bracket assertions)
    - `typsphinx/translator.py:289-317` (`_emit_forced_break`) and `4624-4667` (`visit_desc`/`depart_desc`)
    - `typsphinx/translator.py:4810-4827` (`visit_desc_returns` — the single literal SIG-06 changes)
    - `typsphinx/translator.py:4964-4984` (`visit_desc_optional`/`depart_desc_optional` — the D-11 site)
  </read_first>
  <files>tests/test_signature_break_and_arrow_gate.py</files>
  <action>
Create the gate module in the shape of `tests/test_desc_bodyless_concat_render_gate.py`: fixture-dir
and `temp_build_dir` pytest fixtures, a `sys.executable -m sphinx` build helper (NEVER
`["uv", "run", "sphinx-build", ...]`), a `typst` availability skip on the compiled-PDF class only, and
separate test classes for the structural and the compiled legs.

**BINDING — ROADMAP SC#5 / milestone invariant #4:** every expected string and every expected count is
hand-derived from `37-EMISSION-CONTRACT.md` and from the doctree structure, never copied from
translator output. On a later mismatch, re-derive and record the correction with its reasoning in the
SUMMARY.

Assertions to write:

**SIG-08 (structural, on the emitted `.typ`):**
- an exact expected count of break statements for the whole fixture, derived by counting `desc`
  departures in the doctree and subtracting the nested-desc suppressions specified in contract §8 —
  state the derivation in the test docstring;
- a positional assertion that NO two break statements appear adjacent with only whitespace between
  them anywhere in the file (this is the requirement's actual wording, "exactly one break", and it is
  what fails pre-phase);
- the "content follows the nested member" assertion: in construct 2's region, the nested member's body
  and the trailing paragraph are separated by a break — this is the assertion that a depth-counter
  implementation would break, and it must pass BOTH before and after the fix;
- the sibling body-less control: construct 3's two `desc` siblings keep exactly one break between
  them, passing before and after.

**SIG-06 (compiled PDF):**
- build through the PDF path, extract text with `pypdf`, and — before any comparison — strip U+200B
  from the extracted text (contract §4.2: once ZWSP exists anywhere in a document, `pypdf` emits it
  spuriously at unrelated glyph boundaries; a test that skips the strip will flake);
- assert the arrow glyph is present in the extracted text and that the two-character ASCII arrow
  sequence is absent from the whole extracted text.

**D-11 (its own class, its own assertions — never merged into a SIG-05 check):**
- on the emitted `.typ` for construct 5's region: the separator expression is emitted between the last
  optional parameter and the closing bracket, i.e. the separator's index is greater than the optional
  parameter's index and less than the closing bracket's index;
- on the compiled PDF's extracted text (U+200B stripped): the target rendering from contract §6.1's
  table is present and the current defective rendering is absent;
- the explicit-concatenation NON-REGRESSION assertion: the closing bracket expression and the
  following parameter expression are joined by an explicit concatenation operator. Contract §6.2
  records that this already holds on the current tree — this assertion therefore passes pre-phase and
  exists to stop the rewrite regressing it. Label it as a non-regression control in its docstring so
  nobody "fixes" a defect that is not there;
- the D-11 CONTROL: construct 6's nested-optional rendering must be unchanged. Assert its exact
  expected rendering from contract §6.1's control row, and state in the docstring that this assertion
  passes before AND after the fix by design and must never be converted into the defect case.

Every assertion message names its requirement id (or `D-11`) and the shape it expected.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_signature_break_and_arrow_gate.py -v` collects cleanly and, against the
      untouched translator, reports: the SIG-08 adjacency and count assertions FAILED, the SIG-06
      glyph assertion FAILED, the D-11 placement and PDF-text assertions FAILED.
    - Against the same untouched translator it reports PASSED for: the SIG-08 content-follows-nested
      assertion, the SIG-08 sibling body-less control, the D-11 explicit-concatenation non-regression,
      and the D-11 nested-optional control.
    - Every extracted-PDF assertion in the module calls a U+200B-stripping normalisation before
      comparing; `grep -c '200[bB]' tests/test_signature_break_and_arrow_gate.py` is at least 1.
    - No D-11 assertion lives in a test function whose name mentions SIG-05, and no SIG-05 assertion
      appears in this module at all.
    - `git diff --name-only` for this task shows no path under `typsphinx/`.
    - `grep -c 'uv", "run"' tests/test_signature_break_and_arrow_gate.py` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_signature_break_and_arrow_gate.py -v 2>&1 | tee /tmp/sigbreak-red.txt; grep -q "failed" /tmp/sigbreak-red.txt &amp;&amp; grep -q "passed" /tmp/sigbreak-red.txt</automated>
  </verify>
  <done>SIG-06, SIG-08 and D-11 each have their own assertions, the four control assertions pass pre-phase, the defect assertions fail pre-phase, and every PDF-text comparison strips U+200B first.</done>
</task>

<task type="auto">
  <name>Task 3: Record the RED/GREEN split as evidence</name>
  <read_first>
    - `.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md` (the evidence-file convention)
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §6.1, §6.2, §8
  </read_first>
  <files>.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-02.md</files>
  <action>
Create `37-GATE-EVIDENCE-02.md` recording:

- the commit SHA of the untouched `typsphinx/translator.py` this evidence was captured against;
- the verbatim `uv run pytest tests/test_signature_break_and_arrow_gate.py -v` output;
- an explicit two-column table splitting every test node id into INTENTIONALLY-RED (must flip green in
  a named later plan) versus CONTROL-GREEN (must stay green throughout), with the plan number that
  owns each flip;
- the derivation of the SIG-08 expected break count: how many `desc` departures the fixture's doctree
  produces, which of them contract §8 suppresses, and therefore what the count must be after the fix;
- the pre-phase `index.typ` for this fixture quoted in full, with the adjacent-break position marked;
- the measured Sphinx-HTML target rendering for the D-11 case and the measured pre-phase typsphinx
  rendering side by side (both are in contract §6.1);
- an explicit note recording contract §6.2's correction: CONTEXT.md D-11's "cosmetic half" (a missing
  concatenation operator between the closing bracket and the following parameter) does NOT reproduce
  on the current tree; the obligation converts from a fix into a non-regression assertion, and no code
  change is required for it.

Do not write to a shared evidence file — plan 37-08 merges `37-GATE-EVIDENCE-01..04.md` at the end of
the phase; three sibling plans are running in this same wave.
  </action>
  <acceptance_criteria>
    - `37-GATE-EVIDENCE-02.md` exists, is non-empty, and contains a commit SHA plus verbatim pytest
      output.
    - It contains a RED-vs-CONTROL-GREEN table naming, for every RED node id, the plan number that
      flips it.
    - It states the arithmetic behind the expected break count, not just the number.
    - It records the contract §6.2 correction about CONTEXT.md D-11's cosmetic half explicitly.
    - `uv run black --check tests/test_signature_break_and_arrow_gate.py` and
      `uv run ruff check tests/test_signature_break_and_arrow_gate.py` both pass.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-02.md &amp;&amp; uv run black --check tests/test_signature_break_and_arrow_gate.py &amp;&amp; uv run ruff check tests/test_signature_break_and_arrow_gate.py</automated>
  </verify>
  <done>`37-GATE-EVIDENCE-02.md` records the SHA-anchored verbatim RED/GREEN split, the break-count derivation, the measured D-11 target-versus-current renderings, and the §6.2 correction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.rst` source → translator | Author-controlled build-time text |
| translator → emitted `.typ` string literal | Where unescaped text would become Typst syntax |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | Typst string-literal injection via unescaped signature text | medium | mitigate | The arrow glyph and the D-11 separator/bracket are hardcoded ASCII/escape literals carrying no user text; every user-text site continues to route through the unmodified `escape_typst_string`. No new escaping code. |
| T-37-02 | Information disclosure | none — no new network, file, subprocess, or dependency surface | low | accept | Test-only plan; the sole subprocess is `sys.executable -m sphinx` against a local fixture. |

ASVS L1: V5 Input Validation only, already mitigated project-wide. No high-severity threat. No package
installs, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- `uv run pytest tests/test_signature_break_and_arrow_gate.py -v` reports the expected RED set failing
  and the four control assertions passing against the untouched translator.
- `git diff --stat` shows no change under `typsphinx/`.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
</verification>

<success_criteria>
- SIG-06's assertion is a compiled-PDF glyph check with U+200B normalisation, not a `.typ` substring
  check.
- SIG-08's assertion is an exact hand-derived break count PLUS a no-adjacent-breaks positional check,
  with the derivation written down.
- D-11 has its own fixture constructs, its own test class, its own criterion, and a labelled
  non-regression control — and appears in no SIG-05 assertion.
- Every RED and every control is recorded verbatim against a named commit.
</success_criteria>

<output>
Create `.planning/phases/37-signature-typography-the-desc-family/37-02-SUMMARY.md` when done.
</output>
