---
phase: 37-signature-typography-the-desc-family
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_translator.py
  - tests/test_desc_signature_concat_render_gate.py
  - tests/test_rubric_option_concat_render_gate.py
  - tests/test_desc_sig_space_render_gate.py
  - tests/test_pdf_render_gate.py
  - tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ
  - .planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md
  - .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-04.md
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05, SIG-06]

must_haves:
  truths:
    - "Every pre-existing exact-string assertion this phase invalidates is migrated ONCE, in this plan, straight to its FINAL Phase 37 shape hand-derived from 37-EMISSION-CONTRACT.md -- never migrated twice, and never regenerated from the new translator's output (ROADMAP SC#5, milestone invariant #4)."
    - "golden.typ's 7 signature lines are re-derived by hand from the fixture's measured doctree plus the emission contract; every other line in the file stays byte-identical, so the diff itself is the evidence that Phase 37 touched only signatures (D-14 option 1)."
    - "The census separates files that ASSERT on signature bytes from files that merely MENTION the desc_* node names, and records for each entry which contract section drives the change and which plan flips it green."
    - "The rubric assertions inside tests/test_rubric_option_concat_render_gate.py and tests/test_translator.py are identified and left untouched -- rubric is Phase 39 territory, and touching it here would destroy the diff evidence Phase 36 established."
    - "The migrated assertions are recorded RED against the pre-phase translator, by node id, so the later implementation waves can be verified by set difference rather than by count."
  artifacts:
    - tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ
    - .planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md
    - .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-04.md
  key_links:
    - "every migrated expected string -> 37-EMISSION-CONTRACT.md sections 3, 5, 6, 7 (hand-derived)"
    - "golden.typ's 7 changed lines -> 37-EMISSION-CONTRACT.md section 9 (the worked derivation, including the measured doctree of all five desc_signature nodes)"
    - "37-TEST-CENSUS.md -> 37-EMISSION-CONTRACT.md section 11 (the verified blast-radius list this census extends and re-measures)"
---

<objective>
Migrate the pre-existing exact-string blast radius — nine assertions across five test modules plus the
seven signature lines of Phase 36's `golden.typ` — directly to their final Phase 37 shapes,
hand-derived from `37-EMISSION-CONTRACT.md`, and record the resulting RED set. Produce the file/class
census ROADMAP SC#5 requires.

Purpose: ROADMAP SC#5 owns test migration inside the phase that breaks it, and milestone invariant #4
forbids regenerating expected strings from the new code. Migrating once, up front, to the final shape
means every assertion in this repository is derived from the specification before the specification is
implemented — which is the only ordering under which "hand-derived" is verifiable.

Output: five migrated test modules, a hand-derived `golden.typ`, `37-TEST-CENSUS.md`, and
`37-GATE-EVIDENCE-04.md`. This plan does NOT touch `typsphinx/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md
@.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md
@.planning/phases/37-signature-typography-the-desc-family/37-RESEARCH.md
@.planning/phases/36-shared-emission-seam-cleanup/36-CONTEXT.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run`.
</environment_setup>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan contributes
`37-TEST-CENSUS.md` and `37-GATE-EVIDENCE-04.md`; every other file it touches already exists and is
edited in place.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the blast radius and write the SC#5 census</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §11 (the verified starting list — extend and re-measure it, do not transcribe it)
    - `.planning/ROADMAP.md` § "Phase 37" SC#5 and § "v0.7.0" milestone invariant #5
    - each file named in contract §11's three tables, read for its ACTUAL assertions (not grepped for node names)
  </read_first>
  <files>.planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md</files>
  <action>
Re-measure the blast radius against the current tree rather than inheriting contract §11's list — SC#5
asks for a recorded census, and Phase 36's own precedent is to re-measure rather than transcribe.
Contract §11 is the verified starting point; confirm every row and add anything it missed.

Method: for every test module and fixture that references a `desc_*` node name OR asserts on the
current signature wrapper shape, read the actual assertion and classify it into exactly one of three
buckets:

- **ASSERTS on signature bytes — will break.** Record: file, test function or fixture line, the exact
  current expected string, the contract section that specifies its replacement, and the plan number
  that flips it green.
- **MENTIONS the node names but asserts on something Phase 37 does not change — must stay green.**
  Record why it is safe (which handler it actually exercises).
- **CONDITIONALLY at risk — re-verify after implementation.** Record the specific condition.

Record counts per bucket and a total, and cross-check the total against the milestone's standing
measured blast radius (10 test files, 61 render-gate classes) — if this phase's numbers disagree with
that figure, say so explicitly rather than silently adopting either.

Call out by name the two rubric assertions that must NOT be touched: the `Structure Options` rubric
half of `tests/test_rubric_option_concat_render_gate.py` and `test_rubric_rendering` in
`tests/test_translator.py`. `rubric` is Phase 39 territory; Phase 36 deliberately decoupled it from
`desc_signature` so that restyling one does not touch the other, and editing a rubric assertion here
would destroy that evidence.

Also record the three `test_desc_signature_line_*_linebreak` functions in `tests/test_translator.py`
and `tests/test_desc_bodyless_concat_render_gate.py` in the conditional bucket, with the reason each
is expected to survive.
  </action>
  <acceptance_criteria>
    - `37-TEST-CENSUS.md` exists and contains three labelled buckets with per-bucket counts and a total.
    - Every "will break" row names: file, test function or fixture line number, current expected string,
      the driving `37-EMISSION-CONTRACT.md` section, and the plan number that flips it.
    - The census explicitly names the two rubric assertions as MUST-NOT-TOUCH.
    - The census was produced by reading assertions, not by grepping node names — the SUMMARY records
      at least one row where the two methods would have disagreed (a file that mentions a `desc_*` name
      but asserts on something else, or vice versa).
    - The census states whether its totals agree with the milestone's recorded 10-file / 61-class
      figure and, if not, why.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md &amp;&amp; grep -qi "must not touch\|must-not-touch" .planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md</automated>
  </verify>
  <done>`37-TEST-CENSUS.md` records the re-measured three-bucket census with per-row contract sections and owning plans, and protects the two rubric assertions by name.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the five test modules' exact-string assertions to their final shapes</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §3, §5, §6, §7, §11
    - `.planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md` (written in Task 1)
    - `tests/test_translator.py` at the four affected functions: `test_desc_signature_rendering` (~3355), `test_desc_with_annotation_and_name` (~3374), `test_desc_parameterlist` (~3401), `test_full_api_description_structure` (~3679)
    - `tests/test_desc_signature_concat_render_gate.py` (~lines 260-290)
    - `tests/test_rubric_option_concat_render_gate.py` (~lines 125-155)
    - `tests/test_desc_sig_space_render_gate.py` in full
    - `tests/test_pdf_render_gate.py::test_desc_signature_pdf_has_arrow_linebreak_brackets_and_inline` (~lines 731-810)
  </read_first>
  <files>tests/test_translator.py, tests/test_desc_signature_concat_render_gate.py, tests/test_rubric_option_concat_render_gate.py, tests/test_desc_sig_space_render_gate.py, tests/test_pdf_render_gate.py</files>
  <action>
**BINDING — ROADMAP SC#5 / milestone invariant #4:** every replacement string below is hand-derived
from `37-EMISSION-CONTRACT.md`. Copying whatever the new translator emits is forbidden and voids this
phase's evidence. If a migrated expectation later disagrees with reality, re-derive it from the
contract and record the correction and its reasoning in the SUMMARY — never paste output.

For each "will break" row in `37-TEST-CENSUS.md`, replace the expected string with its final Phase 37
shape derived from the contract:

- The four `tests/test_translator.py` functions build doctrees in-process and assert substrings on
  `translator.astext()`. Derive each replacement from contract §3 (the wrapper), §5.1 (`desc_name` and
  `desc_annotation`) and §5.2 (the parameter-name discriminator). Note that
  `test_desc_signature_rendering` attaches a bare `nodes.Text` directly to a `desc_signature` with no
  `desc_name` at all — derive its expectation from contract §4 (the monospace branch), not §5.1, and
  say so in a comment so a later reader does not "correct" it.
- `tests/test_desc_signature_concat_render_gate.py`'s two `.index(...)` lookups locate signature
  positions to compare ordering. Replace the located literal with the contract §3 + §5.1 form. Keep
  the ordering assertion's intent intact — this test is about position, not about wrapper shape, so
  prefer locating on the smallest stable substring the contract guarantees.
- `tests/test_rubric_option_concat_render_gate.py`: migrate ONLY the `--sep` lookup (that is an
  `.. option::` **desc_signature**). The `Structure Options` and `Trailing Heading` lookups are
  **rubrics** — leave them byte-identical. Add a comment at each rubric lookup stating it is Phase 39
  territory and deliberately unchanged.
- `tests/test_desc_sig_space_render_gate.py` asserts FID-08 spacing on the signature run. Re-derive its
  expectation from contract §4 (every `desc_sig_space` now routes through the monospace branch) while
  preserving the spacing property the test exists to protect — the test's subject is the SPACE, not
  the wrapper, so the migrated assertion must still fail if the space disappears.
- `tests/test_pdf_render_gate.py::test_desc_signature_pdf_has_arrow_linebreak_brackets_and_inline`:
  migrate the return-arrow assertion to contract §7's glyph, and add a U+200B strip to the extracted
  text before every comparison in that function (contract §4.2 — the fixture contains a dotted name,
  and once the break opportunity exists `pypdf` emits U+200B spuriously). Leave that function's
  DESC-02 sentinel, DESC-03 nested-bracket and DESC-04 inline assertions otherwise intact; contract
  §6.1's control row records that the nested-bracket rendering is unchanged by this phase.

Do not add new assertions here — new coverage belongs to plans 37-01..37-03. This task changes
expectations only.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_translator.py -k "desc" -v` reports the four migrated functions FAILED
      against the untouched translator, and `test_desc_signature_rendering`'s failure message shows the
      contract §4 form was expected.
    - `uv run pytest tests/test_desc_signature_concat_render_gate.py tests/test_desc_sig_space_render_gate.py -v`
      reports the migrated assertions FAILED.
    - `uv run pytest tests/test_rubric_option_concat_render_gate.py -v` reports the `--sep` assertion
      FAILED and the `Structure Options` / `Trailing Heading` rubric assertions still PASSING.
    - `uv run pytest tests/test_translator.py::test_rubric_rendering -v` PASSES (untouched).
    - `git diff tests/test_rubric_option_concat_render_gate.py` shows no change to any line containing
      a rubric title, and each rubric lookup gained a Phase-39 comment.
    - `git diff tests/test_pdf_render_gate.py` touches only
      `test_desc_signature_pdf_has_arrow_linebreak_brackets_and_inline`.
    - The three `test_desc_signature_line_*_linebreak` functions in `tests/test_translator.py` are
      unmodified.
    - `git diff --name-only` for this task shows no path under `typsphinx/`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_translator.py::test_rubric_rendering tests/test_rubric_option_concat_render_gate.py -v 2>&1 | tee /tmp/mig.txt; grep -q "failed" /tmp/mig.txt &amp;&amp; grep -q "passed" /tmp/mig.txt</automated>
  </verify>
  <done>All nine pre-existing exact-string assertions carry their final hand-derived Phase 37 expectations, the rubric assertions are provably untouched and still green, and the arrow test strips U+200B before comparing.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-derive golden.typ's seven signature lines and record the RED set</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §9 in full (the worked derivation, including the measured doctree of all five `desc_signature` nodes and the exact target lines), plus §3, §5, §6
    - `.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md` D-14 (the binding hand-derivation constraint)
    - `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` in full (90 lines)
    - `tests/fixtures/desc_rubric_decoupling_render_gate/index.rst`
    - `tests/test_desc_rubric_decoupling_render_gate.py` (the byte-identity gate that consumes the golden)
  </read_first>
  <files>tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ, .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-04.md</files>
  <action>
**BINDING — ROADMAP SC#5 / D-14, non-negotiable:** this file is edited IN PLACE by hand. Regenerating
it by running the new translator and copying its output is forbidden by milestone invariant #4 and
voids the phase's evidence. The new translator does not exist yet when this task runs, which is
precisely the point.

Apply contract §9's derivation: replace exactly the seven `desc_signature`-driven lines — the two-line
`connect` block, the three two-line `compile` blocks, and the one-line `--sep` block — with the target
lines the contract states, and change nothing else. Before writing, independently re-derive each
target line from contract §3 (wrapper), §5.1 (leaf `desc_name`), §5.2 rule 2 (each parameter's single
`desc_sig_name` is its first and is a leaf) and §6 (delimiters), and record that derivation
token-by-token in `37-GATE-EVIDENCE-04.md`. If your derivation disagrees with contract §9's stated
target line, STOP and report the discrepancy in the SUMMARY rather than silently adopting either — a
disagreement means the contract or the derivation is wrong, and finding that now is the whole value of
deriving twice.

Lines that must remain byte-identical, and whose unchanged state in the diff is the evidence that
Phase 37 touched only signatures: the three rubric lines (`Options`, `A Rubric In A List Item`,
`Trailing Heading`), the plain-bold regression-control paragraph, the `list({ ... })` bullet structure,
every body paragraph, every anchor, every `linebreak()`, every `parbreak()`, and the whole preamble.
The fixture contains no nested `desc`, so contract §8 does not touch it; and no signature in it
contains a period inside a signature text run, so contract §4.1's break opportunity must NOT appear
anywhere in the new golden — if one does, the injection scope is wrong, not the golden.

Then create `37-GATE-EVIDENCE-04.md` recording: the commit SHA; the token-by-token derivation of each
of the seven lines; the full `git diff` of `golden.typ` with a line count proving only 7 of the ~35
content-bearing lines changed; verbatim pytest output for all five migrated modules plus the byte-identity
gate; a RED-versus-STAYS-GREEN table by test node id naming the plan that flips each RED; and the
whole-suite `-m "not slow"` line with every intentional RED from this wave enumerated by node id.
  </action>
  <acceptance_criteria>
    - `git diff --numstat tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` shows exactly 7
      lines removed and 7 lines added.
    - `git diff tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` contains no change to any
      line holding a rubric title, the plain-bold control, a `list(`, a `par(`, an anchor, a
      `linebreak()`, a `parbreak()`, or any preamble line.
    - The new `golden.typ` contains no zero-width-break escape sequence anywhere.
    - `uv run pytest tests/test_desc_rubric_decoupling_render_gate.py -v` reports the byte-identity
      assertion FAILED against the untouched translator and the delegation assertion still PASSING.
    - `37-GATE-EVIDENCE-04.md` contains a token-by-token derivation for each of the seven lines, naming
      the contract section that produced each token.
    - `37-GATE-EVIDENCE-04.md` contains a RED-versus-STAYS-GREEN table naming, for every RED node id,
      the plan number that flips it.
    - `git diff --name-only` for this task shows no path under `typsphinx/`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(git diff --numstat tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ | awk '{print $1"/"$2}')" = "7/7" &amp;&amp; test -s .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-04.md</automated>
  </verify>
  <done>`golden.typ`'s seven signature lines carry hand-derived Phase 37 shapes with a 7-line diff, every other line is byte-identical, the derivation is recorded token-by-token, and the RED set is enumerated by node id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.rst` source → translator | Author-controlled build-time text |
| translator → emitted `.typ` string literal | Where unescaped text would become Typst syntax |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | Typst string-literal injection via unescaped signature text | medium | mitigate | No new emission site is created here; the migrated assertions pin the shapes produced by call sites that all reuse the unmodified `escape_typst_string`. |
| T-37-04 | Repudiation | An expected string fitted to the new code's output rather than derived from the specification would silently launder the phase's evidence | high | mitigate | This plan runs BEFORE any translator edit exists, so no output is available to copy; the derivation is recorded token-by-token in `37-GATE-EVIDENCE-04.md` and the 7/7 diff-shape assertion makes an over-broad edit mechanically detectable. |
| T-37-02 | Information disclosure | none — no new network, file, subprocess, or dependency surface | low | accept | Test-and-docs-only plan. |

ASVS L1: V5 Input Validation only, already mitigated project-wide. T-37-04 is the milestone's own
evidence-integrity threat (invariant #4) and is mitigated by ordering plus recorded derivation. No
package installs, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- The migrated assertions fail RED against the untouched translator; the rubric assertions, the
  delegation assertion, and the three `linebreak()`-counting functions stay green.
- `golden.typ`'s diff is exactly 7 lines changed.
- `git diff --stat` shows no change under `typsphinx/`.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
</verification>

<success_criteria>
- One migration, straight to the final shape, for every invalidated exact-string assertion.
- `golden.typ` hand-derived with a provable 7-line diff and no break-opportunity escape anywhere.
- A three-bucket census with per-row contract sections and owning plan numbers.
- The rubric assertions provably untouched and still green.
- The whole RED set enumerated by node id for later set-difference verification.
</success_criteria>

<output>
Create `.planning/phases/37-signature-typography-the-desc-family/37-04-SUMMARY.md` when done.
</output>
