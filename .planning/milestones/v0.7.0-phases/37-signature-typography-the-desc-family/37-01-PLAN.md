---
phase: 37-signature-typography-the-desc-family
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/signature_typography_gate/conf.py
  - tests/fixtures/signature_typography_gate/index.rst
  - tests/test_signature_typography_gate.py
  - .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-01.md
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05]

must_haves:
  truths:
    # --- goal-backward (this plan) ---
    - "A fixture Sphinx project exists that produces, in ONE -b typst build, every doctree shape SIG-01..SIG-05 must be judged on: desc_annotation, desc_addname, a leaf desc_name, a non-leaf (C++) desc_name, all eight measured desc_parameter shapes, an empty desc_parameterlist, an empty desc_addname, and a non-ASCII signature."
    - "Every SIG-01..SIG-05 assertion is written per sub-part (D-03), never as one blanket check over desc_parameter, and every expected string is derived from 37-EMISSION-CONTRACT.md rather than from any translator output."
    - "Each SIG-01..SIG-05 assertion is recorded RED against the pre-phase translator with its verbatim pytest failure output, and the RED is a structural mismatch on the emitted .typ -- never a compile failure (milestone invariant #4: the pre-phase output compiles fine)."
    # --- probe edges: SIG-01 ---
    - "SIG-01 empty: a desc_name, desc_addname or desc_annotation node with zero children emits zero bytes and no empty monospace call; a desc_signature carrying only a desc_name emits exactly one bold-monospace call and no parameter-list delimiters."
    - "SIG-01 encoding: signature text is escaped by escape_typst_string and ZWSP-injected after each U+002E, both operating on Python str code points -- not bytes, not grapheme clusters, and with no Unicode normalisation; a non-ASCII desc_name round-trips its code points unchanged into the emitted .typ."
    # --- probe edges: SIG-02 ---
    - "SIG-02 adjacency: a desc_addname immediately followed by a desc_name emits two separate expressions joined by the existing statement separator -- they never merge into a single monospace call, and the qualifier never absorbs the name."
    - "SIG-02 empty: a desc_addname with zero children (measured: the .. option:: --sep case) emits zero bytes and no separator, exactly as it does pre-phase."
    - "SIG-02 encoding: the zero-width break opportunity is emitted as the 8-character Typst escape sequence, not as a literal invisible U+200B byte, so the emitted .typ stays greppable and hand-derivable."
    - "SIG-02 ordering: desc_addname is always emitted before desc_name, matching doctree child order; two -b typst builds of the same fixture produce byte-identical .typ output."
    # --- probe edges: SIG-04 ---
    - "SIG-04 empty: a desc_parameterlist with zero desc_parameter children emits only the two delimiter calls with no separator between them; a desc_parameter with no desc_sig_name child (the bare keyword-only separator) emits no italic call at all."
    - "SIG-04 encoding: a parameter name is escaped and ZWSP-injected by the same code-point rules as desc_name; a non-ASCII parameter name emits the italic monospace form with its code points unchanged."
    # --- probe edges: SIG-05 ---
    - "SIG-05 adjacency: a closing optional-group bracket immediately followed by the next parameter is joined by an explicit concatenation operator, never juxtaposed; and the optional-group separator lands INSIDE the bracket, matching Sphinx's own HTML."
    - "SIG-05 empty: an empty parameter list emits no comma separator between its two delimiters."
    - statement: "Delimiter literals emitted by desc_parameterlist, desc_parameter and desc_optional are hardcoded ASCII that carry no user-supplied text, so no escaping path exists at those sites that could be bypassed."
      verification: backstop
    - "SIG-05 ordering: nested desc_optional brackets close in reverse open order (the measured printf(fmt[, args[, more]]) shape), and that order is stable across builds."

  artifacts:
    - tests/fixtures/signature_typography_gate/conf.py
    - tests/fixtures/signature_typography_gate/index.rst
    - tests/test_signature_typography_gate.py
    - .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-01.md

  key_links:
    - "tests/test_signature_typography_gate.py -> tests/fixtures/signature_typography_gate/ via a pytest fixture returning the directory, mirroring tests/test_desc_bodyless_concat_render_gate.py"
    - "tests/test_signature_typography_gate.py -> sys.executable -m sphinx -b typst (NEVER uv run sphinx-build -- NixOS stub-ld hazard)"
    - "every expected string in tests/test_signature_typography_gate.py -> 37-EMISSION-CONTRACT.md sections 3, 4, 5, 6 (hand-derived, ROADMAP SC#5)"

  prohibitions:
    - requirement_id: SIG-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT make any Phase 37 gate pass by copying the new translator's own output into a golden file or an expected string, and MUST NOT weaken, narrow, or delete a RED fixture so that it can no longer reach its red state -- the phase's evidence is void if the assertion was fitted to the output rather than derived from the specification."
    - requirement_id: SIG-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT silently drop a working cross-reference from a type annotation while making it look correctly styled -- a resolved type xref that loses its hyperlink but keeps its glyphs passes every substring assertion while removing navigation the reader had before."
    - requirement_id: SIG-07
      category: usability
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT inject invisible break characters so broadly that a reader copying a signature out of the PDF gets an unpasteable string -- the injection exists to prevent margin overflow, and its blast radius on copy-paste must be recorded and bounded, not discovered by a user."
    - requirement_id: SIG-02
      category: accessibility
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT reach monospace by selecting a font family by name -- doing so silently shadows the Japanese build's CJK fallback, which Typst reports as neither a warning nor an error, degrading a whole locale's output invisibly."
---

<objective>
Stand up the SIG-01..SIG-05 structural gate: one fixture Sphinx project exercising every signature
sub-part shape this phase must style, and a per-sub-part assertion module whose expected strings are
hand-derived from `37-EMISSION-CONTRACT.md` and recorded RED against the untouched translator.

Purpose: milestone invariant #4 makes RED structural for v0.7.0 — every defect in this phase compiles
fine today, so "it compiles" proves nothing. The assertions must exist, and must be proven to fail,
before `typsphinx/translator.py` receives a single edit.

Output: a fixture project, a gate module, and `37-GATE-EVIDENCE-01.md` recording the verbatim RED.
This plan does NOT touch `typsphinx/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md
@.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md
@.planning/phases/37-signature-typography-the-desc-family/37-RESEARCH.md
@.planning/phases/37-signature-typography-the-desc-family/37-PATTERNS.md
@.planning/phases/37-signature-typography-the-desc-family/37-VALIDATION.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Skipping step 1 makes the worktree's pytest import the MAIN checkout's `typsphinx` package, so gates
stay RED after a correct fix and later waves do not see earlier waves' changes.
</environment_setup>

<artifacts_this_phase_produces>
New symbols and files Phase 37 creates (so drift verification excludes them — none of these exist
in the pre-phase tree):

**Source (created in later plans, listed here for the whole phase):**
- `typsphinx.translator.SHARED_INDENT_STEP` — module-level constant (D-08)
- `TypstTranslator.in_signature_text` — new instance flag read by `visit_Text`
- `TypstTranslator._param_name_seen` — new per-`desc_parameter` instance flag
- `TypstTranslator._desc_break_marker` — new `depart_desc` emission-position marker
- Newly-implemented bodies for `visit_desc_name`, `visit_desc_annotation`, `visit_desc_sig_name`,
  `visit_desc_parameter` (all `pass` today)

**Tests and fixtures (this phase):**
- `tests/fixtures/signature_typography_gate/{conf.py,index.rst}` + `tests/test_signature_typography_gate.py` (this plan)
- `tests/fixtures/signature_break_and_arrow_gate/{conf.py,index.rst}` + `tests/test_signature_break_and_arrow_gate.py` (37-02)
- `tests/fixtures/signature_overflow_render_gate/{conf.py,index.rst}` + `tests/test_signature_overflow_render_gate.py` (37-03)
- `tests/fixtures/signature_page_boundary_render_gate/{conf.py,index.rst}` + `tests/test_signature_page_boundary_render_gate.py` (37-03)

**Planning artifacts:**
- `37-EMISSION-CONTRACT.md`, `37-GATE-EVIDENCE-01.md`..`-04.md`, `37-GATE-EVIDENCE.md`,
  `37-TEST-CENSUS.md`
</artifacts_this_phase_produces>

<assumption_delta_decision>
decision: no-change
primary_noun: unchanged
rationale: the assumption-delta detector fired on `kind: optional`, matched on the literal substring
"optional" inside the docutils node type name `desc_optional` (Sphinx's node for an optional-parameter
bracket group). That is a node identifier, not a required-to-optional field transition. No data model,
schema, or field optionality changes in this phase.
</assumption_delta_decision>

<flagged_assumptions>
The spec-less edge probe returned SIG-03's row as `unclassified` — it stays `unresolved` and is
surfaced here rather than auto-resolved:

- **SIG-03 (unclassified, unresolved):** the probe could not assign an edge category to "a
  `desc_annotation` renders in the same bold monospace as `desc_name`". The planner's reading is that
  SIG-03's only real edge is *sameness* — the two must be byte-identical in wrapper shape, not merely
  both bold — and Task 2 asserts exactly that. This is recorded as an assumption because the probe did
  not derive it; if a reviewer disagrees that byte-identical wrapper shape is the right reading of
  "the same bold monospace", the assertion is the thing to revisit.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Create the signature_typography_gate fixture Sphinx project</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` (all sections — this is the normative spec)
    - `.planning/phases/37-signature-typography-the-desc-family/37-RESEARCH.md` § "Pattern 2: D-05's discriminator rule" (the 8 measured parameter shapes)
    - `tests/fixtures/desc_rubric_decoupling_render_gate/conf.py` and `index.rst` (the fixture-project convention to mirror)
    - `tests/fixtures/desc_signature_render_gate/index.rst` (the existing C++/optional-bracket fixture)
  </read_first>
  <files>tests/fixtures/signature_typography_gate/conf.py, tests/fixtures/signature_typography_gate/index.rst</files>
  <action>
Create a minimal Sphinx fixture project mirroring `tests/fixtures/desc_rubric_decoupling_render_gate/`:
`conf.py` sets `project`, `author`, `release = "0.0.0"`, `extensions = ["typsphinx"]`, and
`typst_documents = [("index", "index", "Signature Typography Gate", "typsphinx tests")]`.

`index.rst` must produce, in ONE build, every doctree shape SIG-01..SIG-05 is judged on. Each
directive below is required; add a distinctive section heading above each so assertions can slice the
emitted `.typ` by region:

1. `py:class:: sphinx.builders.latex.LaTeXBuilder(app, env, *, extra=None, verbosity: int = 0)` with a
   one-line body — supplies `desc_annotation` ("class"), a dotted `desc_addname`
   (`sphinx.builders.latex.`), a leaf `desc_name`, the bare keyword-only separator, a defaulted
   parameter, and an annotated-plus-defaulted parameter.
2. Nested inside it, `py:method:: write_documents(docnames: set[str], *, force: bool = False) -> None`
   with a one-line body — supplies a generic subscript type and a `desc_returns`.
3. `py:class:: Foo` with a one-line body, then
   `py:function:: g(a: Foo | None = None, b: list[int] = [], c: "Bar" = None) -> Foo` — supplies a
   RESOLVED cross-reference inside a type annotation (measured this session: `Foo` becomes a
   `nodes.reference` child of the second `desc_sig_name`), a union type, a generic type, and a quoted
   forward reference (`desc_sig_literal_string`).
4. `py:function:: h(*args, **kwargs)` — supplies the star operator forms.
5. `cpp:function:: void cpp_probe(int x)` — supplies `desc_sig_keyword_type` and a NON-LEAF
   `desc_name` (measured: the C++ domain nests a `desc_sig_name` inside `desc_name`).
6. `py:function:: empty_params()` — supplies the empty-parameter-list edge.
7. `option:: --sep` with a one-line body — supplies a `desc_name` with an EMPTY sibling
   `desc_addname` (measured on the Phase 36 fixture).
8. A non-ASCII signature — a `py:function::` whose name and one parameter name both carry accented
   Latin characters that exist in DejaVu Sans Mono — supplying the SIG-01/SIG-04 encoding edge.

Do NOT add a `.. rubric::` or plain bold markup to this fixture; those belong to Phase 36's fixture
and Phase 39, and keeping them out means a diff on this file can never be confused for a rubric
regression.

Verify the fixture builds cleanly through `-b typst` before committing, and record the emitted
`index.typ` in the commit message body or task notes so Task 2 has the pre-phase baseline to write
RED against.
  </action>
  <acceptance_criteria>
    - `uv run python -m sphinx -b typst tests/fixtures/signature_typography_gate <tmp>` exits 0.
    - The emitted `index.typ` contains at least 8 distinct signature blocks (one per directive above).
    - A doctree dump of the built fixture shows at least one `desc_parameter` whose second
      `desc_sig_name` child has a `nodes.reference` descendant (the resolved-xref shape).
    - A doctree dump shows at least one `desc_name` whose children are NOT all `nodes.Text` (the C++
      non-leaf shape).
    - A doctree dump shows at least one `desc_addname` with zero children.
    - `tests/fixtures/signature_typography_gate/index.rst` contains no `.. rubric::` directive and no
      `**bold**` inline markup.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m sphinx -b typst tests/fixtures/signature_typography_gate /tmp/sigtypo && test -s /tmp/sigtypo/index.typ</automated>
  </verify>
  <done>The fixture project exists, builds cleanly under `-b typst`, and its doctree provably contains the resolved-xref, non-leaf-`desc_name`, empty-`desc_addname`, empty-parameter-list, and non-ASCII shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the per-sub-part SIG-01..SIG-05 gate module, hand-derived and recorded RED</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §3, §4, §5, §6 (the normative expected shapes)
    - `.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md` D-01 (the per-sub-part treatment table) and D-03 (per-sub-part assertions, never blanket)
    - `tests/test_desc_bodyless_concat_render_gate.py` (the render-gate module convention: fixture, `sys.executable -m sphinx` helper, `typst` availability skip)
    - `tests/test_pdf_render_gate.py` lines 141-178 (`_run_sphinx_build_typst`, and the documented reason it must never be `uv run sphinx-build`)
    - `typsphinx/translator.py:1282-1360` (`visit_literal`/`depart_literal` — the proven `raw(...)` + `escape_typst_string` emission every new call site reuses)
    - `typsphinx/translator.py:4669-4808` (the current `visit_desc_signature`/`depart_desc_signature` shape being asserted against)
  </read_first>
  <files>tests/test_signature_typography_gate.py</files>
  <action>
Create the gate module following `tests/test_desc_bodyless_concat_render_gate.py`'s shape: a pytest
fixture returning the fixture directory, a `temp_build_dir` fixture, a module-level
`_run_sphinx_build_typst` helper invoked as `sys.executable -m sphinx -b typst` (NEVER
`["uv", "run", "sphinx-build", ...]` — that hits the NixOS stub-ld hazard and false-fails), and one
test class holding the assertions.

**BINDING — ROADMAP SC#5 / milestone invariant #4:** every expected string below is hand-derived from
`37-EMISSION-CONTRACT.md`. Copying whatever the translator emits into an expected string is forbidden
and voids this phase's evidence. If an expectation later disagrees with reality, re-derive it from the
contract and record the correction and its reasoning in the plan SUMMARY — never paste output.

Write ONE assertion per signature sub-part (D-03 forbids a single blanket check over
`desc_parameter`). Slice the emitted `index.typ` into per-signature regions first (split on the
wrapper opening literal specified in contract §3) so an assertion about one sub-part cannot be
satisfied by bytes belonging to a different signature.

Assertions to write, each named for its requirement:

- **SIG-01:** the `LaTeXBuilder` and `cpp_probe` name sites each carry the bold-monospace form from
  contract §5.1/§5.2-rule-1; and no signature region contains the pre-phase text primitive call.
- **SIG-02:** the dotted qualifier `sphinx.builders.latex.` is emitted through the plain monospace
  primitive with NO enclosing bold call anywhere between the wrapper's content-block opening and the
  qualifier's own call; and the qualifier's emitted string carries the contract §4.1 escape after each
  period. Assert the qualifier and the name are two separate expressions (the SIG-02 adjacency edge).
- **SIG-03:** the `desc_annotation` site's wrapper shape is byte-identical to the `desc_name` site's
  wrapper shape in the same signature — compare the two extracted call prefixes for equality rather
  than asserting each separately, so "same as `desc_name`" is what is actually proven.
- **SIG-04:** per sub-part, over every parameter shape in the fixture: the parameter's own name
  carries the italic monospace form; its type annotation and its default value carry the plain
  monospace form and are NOT inside an italic call; the resolved-xref type annotation still emits a
  `link(` call with the monospace primitive inside it (this is the Pitfall 3 guard — a substring check
  for the type name alone would pass even if the hyperlink were silently dropped); and the bare
  keyword-only separator parameter emits no italic call at all.
- **SIG-05:** each of the five delimiter sites from contract §6's table is emitted through the
  monospace primitive; the empty-parameter-list signature emits its two delimiters with no comma
  separator between them; and nested optional brackets close in reverse open order.
- **Encoding edges:** the non-ASCII signature's name and parameter name round-trip their code points
  unchanged into the emitted `.typ`, and the escape helper introduced no mojibake.
- **Determinism (SIG-02 ordering edge):** build the fixture twice into two different temp dirs and
  assert the two `index.typ` files are byte-identical.

Every assertion message must name its requirement id and state what shape was expected, so a RED
failure is self-describing.

Mark the class with the existing `@pytest.mark.skipif(not TYPST_AVAILABLE, ...)` convention only if a
`typst.compile()` leg is added; the structural `.typ` assertions themselves need no compiler and must
NOT be skipped when `typst-py` is missing.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_signature_typography_gate.py -v` collects without error and reports
      every SIG-01..SIG-05 assertion as FAILED (not errored, not skipped) against the untouched
      `typsphinx/translator.py`.
    - Each failure message text contains the requirement id it belongs to.
    - The determinism assertion PASSES pre-phase (it is an invariance control, not a RED case).
    - `git diff --name-only` for this task shows no path under `typsphinx/`.
    - `grep -c 'uv", "run"' tests/test_signature_typography_gate.py` returns 0 and the module invokes
      `sys.executable` with `-m`, `sphinx`.
    - No expected string in the module was obtained by running the translator: the SUMMARY records,
      per assertion, which `37-EMISSION-CONTRACT.md` section it was derived from.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_signature_typography_gate.py -v 2>&1 | tee /tmp/sigtypo-red.txt; grep -qE "SIG-0[1-5]" /tmp/sigtypo-red.txt && grep -q "failed" /tmp/sigtypo-red.txt</automated>
  </verify>
  <done>The gate module exists, every SIG-01..SIG-05 assertion fails RED against the pre-phase translator with a self-describing message, the determinism control passes, and `typsphinx/` is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Record the RED evidence and the pre-change suite baseline</name>
  <read_first>
    - `.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md` (the evidence-file convention this mirrors, and the reason it is not named `37-VERIFICATION.md`)
    - `.planning/phases/37-signature-typography-the-desc-family/37-VALIDATION.md` § "Sampling Rate"
  </read_first>
  <files>.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-01.md</files>
  <action>
Create `37-GATE-EVIDENCE-01.md` recording, for the SIG-01..SIG-05 gate:

- the commit SHA of the untouched `typsphinx/translator.py` the RED was captured against;
- the VERBATIM pytest output of `uv run pytest tests/test_signature_typography_gate.py -v`, including
  every failure message — this is the phase's proof that RED was structural and pre-dates any code
  edit;
- the pre-change whole-suite baseline `uv run pytest -m "not slow" -q` result line, with an explicit
  note that the failure count includes exactly this plan's intentional REDs and listing them by node
  id, so a later wave can compare by set difference rather than by count;
- the pre-change lint/type trio results (`uv run black --check .`, `uv run ruff check .`,
  `uv run mypy typsphinx/`);
- the emitted pre-phase `index.typ` for the fixture, quoted in full, labelled as the BEFORE state;
- a one-line statement that this file is deliberately NOT named `37-VERIFICATION.md`, because that
  filename is reserved by the verify stage and is overwritten wholesale.

Do not consolidate with the other Wave 1 evidence files — plan 37-08 merges
`37-GATE-EVIDENCE-01..04.md` into `37-GATE-EVIDENCE.md` at the end of the phase. Writing to a shared
file here would collide with the three sibling plans running in the same wave.
  </action>
  <acceptance_criteria>
    - `.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-01.md` exists and is
      non-empty.
    - It contains a commit SHA, a verbatim pytest failure block, a `-m "not slow"` summary line, and
      the three lint/type command results.
    - It lists every intentionally-RED test node id explicitly (not just a count).
    - It contains the full pre-phase `index.typ` for `tests/fixtures/signature_typography_gate/`.
    - The three lint/type commands are recorded as passing (this plan adds only tests and docs; a lint
      failure here means the new test module is malformed).
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-01.md &amp;&amp; uv run black --check tests/test_signature_typography_gate.py &amp;&amp; uv run ruff check tests/test_signature_typography_gate.py</automated>
  </verify>
  <done>`37-GATE-EVIDENCE-01.md` records the SHA-anchored verbatim RED, the pre-change suite and lint baselines, the enumerated intentional-RED node ids, and the BEFORE `.typ`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.rst` / docstring source → translator | Author-controlled build-time text; the only external input this phase touches |
| translator → emitted `.typ` string literal | Where unescaped text would become Typst syntax |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | Typst string-literal injection via unescaped signature text (a parameter default containing a quote or backslash) | medium | mitigate | Every new monospace call site reuses `escape_typst_string` unmodified (contract §4 step 2, §5.1); no new escaping code is written, so "forgot to escape the new path" is closed by construction. Asserted by the non-ASCII / escaping legs in Task 2. |
| T-37-02 | Information disclosure | none — no new network, file, subprocess, or dependency surface | low | accept | This phase adds styling to already-parsed, already-escaped content. Milestone invariants #1/#5 keep the dependency count at zero new packages and the `@preview` count at four. |

ASVS L1: V5 Input Validation is the only partially-applicable category and is already mitigated
project-wide by `escape_typst_string`. No high-severity threat; nothing blocks. No package installs in
this phase, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- `uv run pytest tests/test_signature_typography_gate.py -v` fails RED on every SIG-01..SIG-05
  assertion against the untouched translator, and the determinism control passes.
- `uv run pytest -m "not slow" -q` differs from the recorded pre-change baseline by exactly this
  plan's intentional REDs, enumerated by node id.
- `git diff --stat` shows no change under `typsphinx/`.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
</verification>

<success_criteria>
- A fixture project exercising all eight measured parameter shapes plus the C++ non-leaf `desc_name`,
  the empty `desc_addname`, the empty parameter list and a non-ASCII signature builds cleanly.
- Per-sub-part SIG-01..SIG-05 assertions exist (never one blanket `desc_parameter` check), every
  expected string traced to a `37-EMISSION-CONTRACT.md` section in the SUMMARY.
- All of them are recorded RED, verbatim, against a named commit, in `37-GATE-EVIDENCE-01.md`.
- `typsphinx/` is untouched by this plan.
</success_criteria>

<output>
Create `.planning/phases/37-signature-typography-the-desc-family/37-01-SUMMARY.md` when done.
</output>
