---
phase: 42-captioned-table-drops-preceding-target-label
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-03.md
  - .planning/todos/pending/2026-08-03-table-whitespace-only-title-anchor-divergence.md
autonomous: true
requirements: [TBL-03]

must_haves:
  truths:
    - "Every `_emit_id_anchors(` call site in `typsphinx/translator.py` is enumerated with its owning visit/depart method and classified against one question: does a NON-cell-content anchor emission fire while a buffer-diverting flag `add_text` consults is still set? The classification is re-derived in this phase's own worktree, at a recorded commit — 42-RESEARCH.md § 5's table is reference material and must not be transcribed (D-06, D-07)."
    - "The image path is re-measured FORMALLY inside the sweep with a real build, covering both the bare `.. image::` site (`visit_image`) and the `.. figure::` site (`depart_figure`) — the discuss-phase and research-session measurements are inadmissible as phase evidence (D-07)."
    - "The sweep's result is recorded as evidence EITHER WAY, including a null result. An empty finding list is written out explicitly as a null result with the commands that produced it, never omitted (D-06)."
    - "If the sweep finds an image-path misrouting, it is fixed inside this phase (D-06's owner condition) and the finding is escalated in this plan's SUMMARY Deviations section rather than silently deferred. Any NON-image finding becomes a todo and is not fixed here."
    - "The D-08 whitespace-only-title divergence is filed as a todo carrying the negative-but-inconclusive probe result already measured, so the next investigator does not re-run the same trivial probe. Nothing is changed in production code for it (D-08)."
  artifacts:
    - .planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-03.md
    - .planning/todos/pending/2026-08-03-table-whitespace-only-title-anchor-divergence.md
  key_links:
    - "`add_text` -> `self.table_cell_content` is the ONLY buffer diversion in `typsphinx/translator.py` that a caller can trip without knowing: every other `self.in_*` flag governs a body-swap idiom fully contained within one child node's visit/depart pair, so it is always restored before the enclosing node's own depart-time anchor call could fire."
    - "The visit-side captioned pre-check (`isinstance(node.children[0], nodes.title)`) and the depart-side captioned check (`if self.table_caption:`, a truthiness test on the stripped rendered caption string set at the `_in_table_caption` consumer) are two different axes on the same question — the D-08 divergence."
  prohibitions:
    - "The sweep table must not be transcribed from 42-RESEARCH.md § 5. D-07 exists precisely because a prior measurement was ruled inadmissible; a table copied from research is the same failure with a different source."
    - "A null result must not be recorded as absence. If no image-path finding exists, the evidence file must say so in words, name the commands that established it, and state the commit it was measured at."
---

<objective>
Run the repo-wide sweep for the same misrouting class (D-06), formally re-take the image-path
measurement inside it (D-07), and file the D-08 whitespace-only-title divergence as a todo.

Purpose: the defect this phase fixes is "a non-cell-content anchor emission that fires while a
buffer-diverting flag is still set." The owner's condition is explicit and narrow — sweep
everywhere; a finding in the IMAGE path is fixed inside this phase, every other finding becomes a
todo; and the sweep's result is recorded as evidence either way, including a null result.

This plan runs in wave 1 against the UNFIXED tree on purpose: that is the state in which the
misrouting class exists, so the sweep can name `depart_table` as a live finding rather than as
history.

Output: `42-GATE-EVIDENCE-03.md` and one new pending todo. No production code changes unless the
sweep produces an image-path finding, in which case D-06's condition fires and the finding is
escalated.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-CONTEXT.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
CLAUDE.md's worktree-isolated execution is the STANDING mode. Before ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Assert the shim landed: `for t in uv ruff; do readlink -f ".venv/bin/$t"; done` must print
   `/nix/store/...` paths.
4. Run every subsequent command through `uv run`.
5. NOTE on the `<automated>` verify blocks in this plan: `&amp;&amp;` is XML-escaped
   `&&`. Unescape it before running the command in a shell. Same for `&amp;` on its own.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and classify every anchor-emission call site (the static half of the sweep)</name>
  <files>.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-03.md</files>
  <read_first>
    - `typsphinx/translator.py` lines 423-437 (`add_text`) — the single source of truth for which flags actually divert an append. Read it FIRST; everything else in the sweep is classified against it.
    - `typsphinx/translator.py` lines 481-552 (`_emit_id_anchors`) — confirm it emits through `add_text` and never through `self.body.append` directly, which is what makes a caller's flag state matter.
    - `typsphinx/translator.py` lines 3149-3197 (`visit_table`) and 3249-3368 (`depart_table`) — the safe call site and the bug site, for the contrast the table must draw.
    - `typsphinx/translator.py` lines 2480-2531 (`depart_figure`) and around line 3628 (`visit_image`) — the two image-path call sites.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-CONTEXT.md` § D-06 and § D-07 — the owner's exact condition and the inadmissibility rule.
  </read_first>
  <action>
Begin `42-GATE-EVIDENCE-03.md` under this phase's directory. Open it with the commit the sweep was
taken at (`git rev-parse HEAD`, `git log -1 --oneline`) and an explicit statement that the tree is
UNFIXED at this point — `depart_table`'s call ordering has not yet been changed by plan 42-04.

Run the three enumeration commands and paste each command and its FULL output verbatim:
enumerate every `_emit_id_anchors(` occurrence with line numbers; print `add_text`'s definition with
enough following lines to show its whole body; and enumerate every `self.in_<name> = True` and
`self.in_<name> = False` assignment with line numbers. Then, for each of the buffer-related
attributes those greps surface, establish which ones `add_text` actually branches on — quote the
relevant lines rather than asserting.

Build the classification table with one row per `_emit_id_anchors(` call site and these columns:
line, owning visit/depart method, whether the emission is the NODE'S OWN anchor or content nested
inside another node, which buffer-diverting flag (if any) is set at that call's moment, verdict
(SAFE / MISROUTED), and image-path classification (image-path / non-image). Derive each row by
reading the code at that line in THIS worktree — do not copy 42-RESEARCH.md § 5's table, which D-07
rules inadmissible as phase evidence.

Two distinctions the table must make explicitly, because they are the difference between a real
finding and a false positive:

- A call site whose node sits INSIDE a table cell (for example `visit_paragraph` for a paragraph
  nested in a cell) routes into `table_cell_content` correctly — that is the DESIRED behavior and
  is not a finding. The defect is specifically a parent node emitting its OWN trailing anchor while
  the buffer diversion it opened for its children is still active.
- A body-swap idiom that temporarily replaces `self.body` (the admonition-title swap, the
  figure-caption swap, the definition-list term/definition buffers) is not the same hazard: each is
  fully contained within one child's visit/depart pair and is restored before the enclosing node's
  own depart-time anchor call can fire. Say so, and cite the restore sites you observed.

Close the section with the finding list. If the only MISROUTED row is `depart_table`, say exactly
that. Every non-image finding, if any, gets a one-line entry naming the todo that will be filed for
it (D-06: todo only, not fixed here).
  </action>
  <acceptance_criteria>
    - `42-GATE-EVIDENCE-03.md` exists and opens with a 40-hex commit SHA and the statement that the tree is unfixed at that commit.
    - It contains the verbatim output of a grep enumerating `_emit_id_anchors(` call sites, with at least 20 numbered lines.
    - It contains `add_text`'s body quoted verbatim, and states in words that `self.in_table` is the only flag it branches on.
    - It contains a classification table with one row per enumerated `_emit_id_anchors(` call site, each carrying a SAFE or MISROUTED verdict and an image-path / non-image classification.
    - The table's `depart_table` row is verdict MISROUTED; the `visit_table` row is verdict SAFE with the reason that it fires before `self.in_table = True`.
    - The file states explicitly that a call site nested inside a table cell routing into `table_cell_content` is desired behavior and not a finding.
    - The file contains a finding list; if it is empty apart from `depart_table`, that is stated in words rather than left implicit.
    - `git status --porcelain typsphinx/` is empty at this task's commit.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-03.md; test -f "$F" &amp;&amp; grep -Eq '[0-9a-f]{40}' "$F" &amp;&amp; grep -q '_emit_id_anchors' "$F" &amp;&amp; grep -q 'add_text' "$F" &amp;&amp; grep -q 'MISROUTED' "$F" &amp;&amp; grep -q 'SAFE' "$F" &amp;&amp; grep -q 'depart_table' "$F" &amp;&amp; grep -q 'table_cell_content' "$F" &amp;&amp; test -z "$(git status --porcelain typsphinx/)"</automated>
  </verify>
  <done>The static half of the sweep is recorded: every anchor-emission call site enumerated from the live tree at a named unfixed commit, each classified SAFE or MISROUTED with an image-path flag, the two false-positive classes explicitly excluded, and a finding list written out.</done>
</task>

<task type="auto">
  <name>Task 2: Re-take the image-path measurement formally, with a real build (D-07)</name>
  <files>.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-03.md</files>
  <read_first>
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-03.md` — the file this task appends to; read task 1's classification so the measurement targets exactly the rows it flagged image-path.
    - `typsphinx/translator.py` around line 3628 (`visit_image`) and its `_emit_id_anchors` call — the bare-image call site being measured.
    - `typsphinx/translator.py` lines 2480-2531 (`depart_figure`) — the figure call site being measured.
    - `tests/fixtures/paragraph_propagated_target_render_gate/conf.py` — the minimal master-document `conf.py` shape to reuse for the throwaway probe project.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-CONTEXT.md` § D-06 — the owner's verbatim condition that an image-path finding IS fixed inside this phase.
  </read_first>
  <action>
Build a THROWAWAY Sphinx project under the scratchpad — not under `tests/`, and NOT committed to the
repository — containing a `conf.py` that makes `index` a master document plus an `index.rst` with
two shapes: a standalone target immediately before a bare `.. image::` directive, and a standalone
target immediately before a captioned `.. figure::`, each with a `:ref:` back to the target using
explicit link text. Reuse an existing in-repo image asset for the image file.

Run `uv run python -m sphinx -b typstpdf -q -E` over it and record, verbatim: the command, the exit
status, the full stderr, the emitted `.typ` lines carrying every `<index:...>` label definition and
every `link(<index:...>)` reference, and whether `index.pdf` was produced.

Append a section to `42-GATE-EVIDENCE-03.md` reporting this measurement. It must state the verdict
for each of the two image-path call sites separately — the bare-image site and the figure site — and
say in words whether an image-path misrouting exists. Paste the probe project's `conf.py` and
`index.rst` sources verbatim into the evidence file so the measurement is reproducible without the
project being committed.

Record the outcome either way. If NO image-path finding exists, write it as an explicit NULL RESULT
section: the verdict in words, the commands that established it, the commit it was measured at, and
a sentence stating that D-06's "an image-path finding is fixed inside this phase" condition is
therefore moot with nothing to fix. Do not simply omit the section.

If an image-path finding DOES exist, D-06's owner condition fires: it must be fixed inside this
phase. Record the finding in full, stop before making the change, and escalate it in this plan's
SUMMARY under Deviations with the exact call site and proposed minimal fix so the orchestrator can
route it. Do not defer it to a todo and do not fix it silently as part of this plan.

Note for the reader of the evidence file: the figure half of the image path is ALSO permanently
gated by plan 42-02's `tests/test_figure_propagated_target_render_gate.py`. That gate and this
measurement are independent — cross-reference it, do not depend on it.
  </action>
  <acceptance_criteria>
    - `42-GATE-EVIDENCE-03.md` gains a section reporting a real `-b typstpdf` build of a throwaway probe project, with the command, exit status, and stderr quoted verbatim.
    - The section reports a separate verdict for the bare `.. image::` site and for the `.. figure::` site.
    - The section contains the probe project's `conf.py` and `index.rst` sources verbatim.
    - The file states in words whether an image-path misrouting exists; if none does, the words "null result" appear and D-06's condition is stated as moot.
    - The throwaway probe project is not present anywhere under `tests/`, `typsphinx/`, `docs/`, or `examples/` in the committed tree.
    - If an image-path finding exists, this plan's SUMMARY Deviations section names the call site and the proposed minimal fix, and no production code was changed by this plan.
    - `git status --porcelain typsphinx/` is empty at this task's commit.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-03.md; test -f "$F" &amp;&amp; grep -q 'typstpdf' "$F" &amp;&amp; grep -q 'visit_image' "$F" &amp;&amp; grep -q 'depart_figure' "$F" &amp;&amp; grep -qi 'image path' "$F" &amp;&amp; grep -q 'D-06' "$F" &amp;&amp; grep -q 'D-07' "$F" &amp;&amp; test -z "$(git status --porcelain typsphinx/)" &amp;&amp; test -z "$(git status --porcelain tests/ docs/ examples/)"</automated>
  </verify>
  <done>The image path is re-measured formally with a real build inside this phase, with a separate verdict for the bare-image and figure call sites, the probe sources recorded verbatim, and the outcome written out either way — including an explicit null result if that is what was measured.</done>
</task>

<task type="auto">
  <name>Task 3: File the D-08 whitespace-only-title divergence as a pending todo</name>
  <files>.planning/todos/pending/2026-08-03-table-whitespace-only-title-anchor-divergence.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-03-captioned-table-drops-preceding-target-label.md` — the shape and tone to copy: YAML front matter (created / title / area / resolves_phase / roadmap_entry / source / files), a Problem section, a Status section, a Solution section holding breadcrumbs rather than a diagnosis, and an Acceptance checklist.
    - `typsphinx/translator.py` lines 3149-3197 (`visit_table`) — the visit-side structural captioned pre-check.
    - `typsphinx/translator.py` around lines 747-760 (the `_in_table_caption` consumer that assigns `self.table_caption` from the joined, stripped buffer) and lines 3300-3345 (the depart-side `if self.table_caption:` branch) — the two halves of the divergence.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md` Assumption A1 and Open Question 2 — the negative-but-inconclusive probe result that must be carried into the todo.
  </read_first>
  <action>
Create `.planning/todos/pending/2026-08-03-table-whitespace-only-title-anchor-divergence.md`. If the
phase executes on a later date, use that date as the filename prefix instead — the slug
`table-whitespace-only-title-anchor-divergence` is fixed, the date prefix follows the project's
`YYYY-MM-DD` todo convention.

Front matter: `created` as an ISO timestamp, `area: translator`, `resolves_phase: null`,
`roadmap_entry: null`, `source` naming this phase's D-08 decision, and a `files` list naming the two
divergent sites in `typsphinx/translator.py` with the line numbers you actually observe in this
worktree.

Problem section — state the divergence precisely, in these terms: `visit_table` decides "is this
table captioned?" STRUCTURALLY, by testing whether the node's first child is a `title` node, and
skips the unconditional `_emit_id_anchors` call when that is true; `depart_table` decides the same
question by TRUTHINESS of `self.table_caption`, which is assigned from the joined table-cell buffer
with surrounding whitespace stripped. The two axes disagree for a table that HAS a title node whose
rendered content strips to the empty string: the visit side treats it as captioned and skips the
anchor call, and the depart side treats it as caption-less and takes the branch that never calls
`_emit_id_anchors` at all. The table's ids are then anchored on neither path, so any reference to a
propagated target on such a table dangles and aborts the compile — the same class of failure TBL-03
fixes, reached by a different route.

Solution section — breadcrumbs only, explicitly NOT a diagnosis. Carry forward the probe already
run: a literal trailing-whitespace `.. table:: ` argument caused docutils to produce NO title child
at all, so the structural check returned false and the table anchored correctly through the
non-captioned path. Record that this is a data point AGAINST reachability but not a proof of
unreachability — constructs that were not tested include substitution references, the `replace`
directive, and raw markup that might yield a title node whose rendered text strips to empty while
the node itself still exists. State plainly that the next investigator should start from those
constructs and not re-run the trailing-whitespace probe.

Status section — filed by Phase 42 per D-08, deliberately NOT fixed there. The phase's scope fence
was call-ordering inside the table departure handler only (D-05); changing either captioned check
would be a semantics change outside that fence.

Acceptance checklist — determine whether any rST construct can produce a title node that renders to
an empty string; if reachable, decide which axis is authoritative and align the two checks; if
unreachable, close the todo with the reachability evidence recorded.
  </action>
  <acceptance_criteria>
    - A file matching `.planning/todos/pending/*-table-whitespace-only-title-anchor-divergence.md` exists with YAML front matter containing `area: translator` and `resolves_phase: null`.
    - Its Problem section names both sides of the divergence: the structural first-child-is-a-title check in `visit_table`, and the truthiness check on `self.table_caption` in `depart_table`.
    - It states the consequence: the table's ids are anchored on neither path.
    - It carries the negative-but-inconclusive probe result, naming the trailing-whitespace `.. table::` argument case and the fact that docutils produced no title child for it.
    - It names at least two untested constructs the next investigator should start from.
    - It states that Phase 42 deliberately did not fix it, citing D-08 and D-05's scope fence.
    - `git status --porcelain typsphinx/` is empty at this task's commit — filing this todo changes no code.
  </acceptance_criteria>
  <verify>
    <automated>F=$(ls .planning/todos/pending/*-table-whitespace-only-title-anchor-divergence.md); test -f "$F" &amp;&amp; grep -q 'area: translator' "$F" &amp;&amp; grep -q 'resolves_phase: null' "$F" &amp;&amp; grep -q 'visit_table' "$F" &amp;&amp; grep -q 'table_caption' "$F" &amp;&amp; grep -q 'D-08' "$F" &amp;&amp; grep -q 'D-05' "$F" &amp;&amp; test -z "$(git status --porcelain typsphinx/)"</automated>
  </verify>
  <done>The D-08 divergence is filed as a pending todo carrying an accurate description of both checks, the consequence, the already-measured negative-but-inconclusive probe result, the untested constructs to start from, and an explicit statement that Phase 42 left it unchanged.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created by Phase 42 as a whole (owner plan in brackets). The plan-review
source-grounding pass must treat every entry here as newly created by this phase, not as drift.

Fixtures and test modules:
- `tests/fixtures/captioned_table_propagated_target_render_gate/conf.py` [42-01]
- `tests/fixtures/captioned_table_propagated_target_render_gate/index.rst` [42-01]
- `tests/test_captioned_table_propagated_target_render_gate.py` [42-01]
- class `TestCaptionedTablePropagatedTargetRenderGate` [42-01]
- `tests/fixtures/figure_propagated_target_render_gate/conf.py` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/index.rst` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/image.png` [42-02]
- `tests/test_figure_propagated_target_render_gate.py` [42-02]
- class `TestFigurePropagatedTargetRenderGate` [42-02]

Production symbol:
- local `was_captioned` in `TypstTranslator.depart_table` (`typsphinx/translator.py`) [42-04]

rST label ids introduced by the new fixtures:
- `tbl-target`, `tbl-name`, `tbl-target-noname`, `tbl-target-li`, `tbl-name-li`, `tbl-target-a`,
  `tbl-target-b`, `tbl-name-two` [42-01]
- `fig-target`, `fig-name`, `fig-target-noname`, `fig-target-li`, `fig-name-li` [42-02]

Caption sentinels:
- `TBLTGTNAMEDSENTINEL`, `TBLTGTNONAMESENTINEL`, `TBLTGTLISTSENTINEL`, `TBLTGTTWOSENTINEL` [42-01]
- `FIGTGTNAMEDSENTINEL`, `FIGTGTNONAMESENTINEL`, `FIGTGTLISTSENTINEL` [42-02]

Evidence and planning artifacts (all under
`.planning/phases/42-captioned-table-drops-preceding-target-label/`):
- `42-GATE-EVIDENCE-01.md` — table-side RED, SC#1 + SC#5 [42-01]
- `42-GATE-EVIDENCE-02.md` — figure-side answer + permanent gate, SC#2 [42-02]
- `42-GATE-EVIDENCE-03.md` — repo-wide misrouting sweep, D-06 + D-07 [42-03]
- `42-GATE-EVIDENCE-04.md` — table-side GREEN + regression sweep, SC#3 [42-04]
- `42-GATE-EVIDENCE-05.md` — caption-less byte invariance, SC#4 + D-04 [42-05]
- `42-SC4-INVARIANTS.md` — Phase 41 SC#4 sweep re-measured over a range including Phase 42, with the
  change-site to RED manifest, SC#6 [42-06]
- `42-CLOSEOUT-GUARD.md` — REL-04/REL-05 checkbox-flip guard, SC#6 [42-06]

Todo:
- `.planning/todos/pending/<YYYY-MM-DD>-table-whitespace-only-title-anchor-divergence.md` — D-08 [42-03]

Modified (not created): `CHANGELOG.md` gains one bullet under `## [0.7.0]` -> `### Fixed` [42-06];
`typsphinx/translator.py` `depart_table` [42-04].
</artifacts_this_phase_produces>

<planner_assumptions>
Flagged planner assumptions for this plan (surfaced, never silently dropped):

- This plan is scheduled in wave 1, against the UNFIXED tree, so the sweep observes the misrouting
  class while it is live. Consequence the executor must honour: the line numbers recorded here are
  pre-fix line numbers and will shift once plan 42-04 lands. Record them as observed, and note the
  commit — do not "helpfully" adjust them.
- The spec-less edge probe's `encoding` row is the D-08 divergence and is authored as a BACKSTOP
  truth in plan 42-01; this plan is where it becomes a filed todo. Exactly one producer per
  predicate — do not restate the backstop truth here.
- 42-RESEARCH.md § 5 already reports a null image-path result. That expectation must NOT be allowed
  to pre-decide this plan's measurement: D-07 exists because a prior measurement was ruled
  inadmissible. If this plan's own measurement disagrees with research, the plan's measurement wins
  and the disagreement is recorded.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rST source -> translator | Author-controlled target ids and captions cross into emitted Typst markup. |
| emitted `.typ` -> `typst.compile()` | A label defined zero times but referenced once aborts the entire build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-07 | Denial of Service | any `_emit_id_anchors` call site firing while a buffer-diverting flag is set | medium | mitigate | A second instance of TBL-03's defect class elsewhere in `typsphinx/translator.py` would be an unfound build fatal. Mitigated by this plan's exhaustive call-site enumeration and per-site verdict, re-derived from the live tree at a named commit. |
| T-42-08 | Denial of Service | the D-08 whitespace-only-title branch | low | accept | Reachability is unproven and the only probe run so far argues against it. Accepted for this phase per D-08 and recorded as a pending todo carrying the probe result, so the acceptance is documented rather than silent. |
| T-42-09 | Repudiation | the sweep's own evidence record | medium | mitigate | A sweep whose table was transcribed from research would be unfalsifiable. Mitigated by requiring verbatim command output, a recorded commit SHA, and an explicit null-result section rather than an omission (D-06, D-07). |
| T-42-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package. 42-RESEARCH.md's Package Legitimacy Audit records "not applicable" — no `[ASSUMED]`/`[SUS]`/`[SLOP]` entry exists, so no legitimacy checkpoint is required. `git diff -- pyproject.toml uv.lock` must stay empty (milestone invariant #1), verified in plan 42-06's invariant sweep. |
</threat_model>

<verification>
- `42-GATE-EVIDENCE-03.md` contains a per-call-site classification table derived from the live tree at a named commit, with `depart_table` flagged MISROUTED and `visit_table` flagged SAFE.
- The image path carries a separate recorded verdict for the bare-image and figure call sites, from a real `-b typstpdf` build inside this phase.
- The sweep result is present either way; a null result is written out explicitly.
- A pending todo matching `*-table-whitespace-only-title-anchor-divergence.md` exists and carries the prior probe finding.
- `git status --porcelain typsphinx/` is empty across all three tasks — this plan changes no production code unless D-06's image-path condition fires, which is escalated rather than executed silently.
</verification>

<success_criteria>
- D-06 satisfied: a repo-wide sweep ran, its result is recorded as evidence either way including a null result, non-image findings (if any) are filed as todos, and an image-path finding (if any) is escalated for in-phase fixing rather than deferred.
- D-07 satisfied: the image-path measurement was re-taken formally inside the sweep with a real build; no discuss-phase or research-session measurement is transcribed as evidence.
- D-08 satisfied: the whitespace-only-title divergence is filed as a todo carrying the negative-but-inconclusive probe result, and nothing is changed for it.
</success_criteria>

<output>
Create `.planning/phases/42-captioned-table-drops-preceding-target-label/42-03-SUMMARY.md` when done.
</output>
