---
phase: 42-captioned-table-drops-preceding-target-label
plan: 05
type: execute
wave: 3
depends_on: ["42-04"]
files_modified:
  - .planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md
autonomous: true
requirements: [TBL-03]

must_haves:
  truths:
    - "The caption-less table path is byte-for-byte unchanged by the fix, proven by an EMPTY `diff` between two real `sphinx-build -b typst` runs — one at a named pre-fix commit and one at a named post-fix commit, each built from its own throwaway git worktree with its own venv (SC#4, D-04)."
    - "Both commit SHAs, both build commands with their exit statuses, and the verbatim (empty) diff output are recorded in `42-GATE-EVIDENCE-05.md`. The proof is the recorded diff, not a golden `.typ` committed to the repository."
    - "The production-code diff between the two named commits is isolated with a pathspec scoped to `typsphinx/`, because unrelated commits (evidence files, STATE updates, orchestrator tracking commits) sitting between them make an unscoped `git diff --stat` misleading — the trap Phase 36's own evidence file measured and explained."
    - "The byte-invariance build covers a caption-less table specifically: the caption-less control table in this phase's own new fixture and the pre-existing top-level control table in `tests/fixtures/table_in_list_item_render_gate/`."
  artifacts:
    - .planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md
  key_links:
    - "`depart_table`'s caption-less `else:` branch never called `_emit_id_anchors` before the fix and still does not: the moved call is guarded by `was_captioned`, so a caption-less table's emission path is unreachable from the change. The empty diff is the mechanical proof of that reasoning."
    - "Each throwaway worktree needs its own `uv sync --extra dev` plus the NixOS `uv`/`ruff` shim: the main `.venv`'s PEP-660 editable finder resolves `import typsphinx` to the MAIN checkout, so a worktree that skips provisioning would build BOTH sides with the same (post-fix) code and produce a meaningless empty diff."
  prohibitions:
    - "A golden `.typ` file must not be committed to the repository as the byte-invariance proof. The owner chose the two-build diff method explicitly over a committed golden, and over doing both."
    - "An empty diff must not be reported without proving the two sides were actually built by different code. The evidence file must show, for each worktree, that `import typsphinx` resolved to that worktree's own copy — otherwise an unprovisioned worktree yields an empty diff for the wrong reason."
---

<objective>
Prove the caption-less table path is byte-for-byte unchanged by the TBL-03 fix, using the two-build
diff method (SC#4, D-04).

Purpose: Phase 25's SC#2 established that the caption-less path stays byte-identical, and TBL-03's
requirement text restates it. The owner chose Phase 36's method — an empty `diff` between two real
builds at named pre-fix and post-fix commits, recorded in an evidence file — over a committed golden
file. The single most likely way to produce a false empty diff is an unprovisioned worktree in which
`import typsphinx` resolves to the main checkout for both sides; this plan makes disproving that a
recorded step rather than an assumption.

Output: `42-GATE-EVIDENCE-05.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-CONTEXT.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-04-SUMMARY.md
@CLAUDE.md
</context>

<worktree_provisioning>
CLAUDE.md's worktree-isolated execution is the STANDING mode. Before ANY command in this plan, in
THIS plan's own worktree:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Assert the shim landed: `for t in uv ruff; do readlink -f ".venv/bin/$t"; done` must print
   `/nix/store/...` paths.
4. Run every subsequent command through `uv run`.
5. NOTE on the `<automated>` verify blocks in this plan: `&amp;&amp;` is XML-escaped
   `&&`. Unescape it before running the command in a shell. Same for `&amp;` on its own.

Steps 1-3 must be repeated inside EACH of the two throwaway worktrees this plan creates, and every
command run inside them must go through that worktree's own `uv run`. This is not optional
bookkeeping — it is the difference between a real proof and a false one.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Build the caption-less fixtures from a pre-fix and a post-fix worktree and diff them</name>
  <files>.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md</files>
  <read_first>
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md` § 4 — the reusable four-step command sequence and the `git diff --stat` scoping trap.
    - `.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md` § "Post-decoupling diff" — the precedent this method comes from, including how it names the two commits and reports the empty diff.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-04.md` — the fix commit SHA to use as the post-fix side.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-01.md` — the RED-recording commit SHA, the natural pre-fix side (it is the last commit before any production change).
    - `tests/fixtures/table_in_list_item_render_gate/index.rst` — the pre-existing top-level control table, a caption-less shape that exists at BOTH commits and is therefore buildable from either side.
    - `CLAUDE.md` § "Worktree-isolated execution" — the per-worktree provisioning rationale (the PEP-660 editable finder hazard).
  </read_first>
  <action>
Pick the two named commits and record why each was chosen:

- PRE-FIX: plan 42-01's RED-recording commit (or any commit on this phase's history that is an
  ancestor of the fix and contains no production change). Verify with
  `git status --porcelain typsphinx/`-equivalent evidence, namely that
  `git diff --stat <PRE_FIX>..<POST_FIX> -- typsphinx/` attributes the change entirely to the fix.
- POST-FIX: plan 42-04's fix commit, the only commit in this phase that touches `typsphinx/`.

Create two throwaway git worktrees, one checked out at each SHA, under the scratchpad — never inside
the repository tree. Provision EACH one independently with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` followed by the `uv`/`ruff` shim
loop and the `readlink -f` assertion. Then, from inside each worktree and using that worktree's own
venv, prove the isolation before building anything: run a `uv run python` invocation that prints
`typsphinx.__file__` and confirm it points INSIDE that worktree, not into the main checkout. Record
both paths verbatim. Without this step an empty diff proves nothing.

Build the caption-less material from each worktree with
`uv run python -m sphinx -b typst -q -E <fixture-source> <build-dir>`. Build BOTH of these fixtures
from each side, because each covers a different caption-less shape:

- `tests/fixtures/table_in_list_item_render_gate` — the pre-existing top-level control table plus a
  list-item-nested caption-less table; this fixture exists identically at both commits.
- The caption-less control section of this phase's own
  `tests/fixtures/captioned_table_propagated_target_render_gate` — but note this fixture does NOT
  exist at commits before plan 42-01 landed. It exists at both of the chosen SHAs; confirm that by
  listing it in each worktree before building, and if the chosen pre-fix SHA predates it, say so and
  fall back to the `table_in_list_item_render_gate` build alone for that shape, recording the
  substitution rather than silently dropping it.

Then diff the emitted `.typ` files pairwise between the two build directories. Record each `diff`
command verbatim together with its output and exit status. The proof is empty output and exit 0.

Finally, record the production-code diff between the two commits, scoped with a pathspec:
`git diff --stat <PRE_FIX>..<POST_FIX> -- typsphinx/`. Also record the UNSCOPED
`git diff --stat <PRE_FIX>..<POST_FIX>` and explain, in one sentence, why it is larger — unrelated
evidence and tracking commits sit between the two SHAs, exactly the measurement trap Phase 36's own
evidence file called out.

Remove both throwaway worktrees when done (`git worktree remove`), and confirm
`git worktree list` shows none left behind.
  </action>
  <acceptance_criteria>
    - Two throwaway worktrees were created at two distinct 40-hex SHAs and both were removed afterwards; `git worktree list` at task end shows no scratchpad worktree.
    - For each worktree, the recorded `typsphinx.__file__` path lies inside that worktree's own directory, not inside the main checkout.
    - For each worktree, `readlink -f .venv/bin/uv` and `readlink -f .venv/bin/ruff` were recorded and resolve to `/nix/store/...` paths.
    - Both `uv run python -m sphinx -b typst -q -E` builds exited 0.
    - The pairwise `diff` of the emitted `index.typ` files produced EMPTY output with exit status 0.
    - `git diff --stat <PRE_FIX>..<POST_FIX> -- typsphinx/` shows `typsphinx/translator.py` and nothing else.
    - No `.typ` golden file was added anywhere under `tests/` or the repository tree.
    - `git status --porcelain typsphinx/ tests/` is empty — this plan changes no code and no fixture.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain typsphinx/ tests/)" &amp;&amp; test -z "$(git worktree list --porcelain | grep -i scratch || true)"</automated>
  </verify>
  <done>Two provisioned throwaway worktrees at named pre-fix and post-fix commits each built the caption-less fixtures with their own copy of typsphinx (proven by `typsphinx.__file__`), the pairwise `.typ` diff was empty at exit 0, the production diff was isolated with a `typsphinx/` pathspec, and both worktrees were cleaned up.</done>
</task>

<task type="auto">
  <name>Task 2: Write 42-GATE-EVIDENCE-05.md with the SC#4 proof</name>
  <files>.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md</files>
  <read_first>
    - `.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md` § "Post-decoupling diff" and § "SC#2 byte-identity diff (the proof)" — the exact section shape and tone to mirror.
    - `.planning/ROADMAP.md` § Phase 42 success criterion 4 — the criterion this file discharges.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-CONTEXT.md` § D-04 — the method the owner chose and the one they rejected.
  </read_first>
  <action>
Write `42-GATE-EVIDENCE-05.md` under this phase's directory, transcribing task 1's measurements
verbatim. Sections, in order:

1. THE TWO NAMED COMMITS. Each SHA with its `git log -1 --oneline` line and a one-sentence statement
   of what it is (the RED-recording commit that contains no production change; the fix commit that
   contains only the call-ordering move).

2. WORKTREE ISOLATION PROOF. For each throwaway worktree: the `git worktree add` command, the
   provisioning commands, the two `readlink -f` outputs, and the recorded `typsphinx.__file__` path.
   State in one sentence why this section exists — an unprovisioned worktree would resolve
   `import typsphinx` to the main checkout and both sides would be built by the SAME code, producing
   an empty diff that proves nothing.

3. BUILD COMMANDS. Each `uv run python -m sphinx -b typst -q -E` invocation verbatim, with its exit
   status, for both fixtures from both sides. If the caption-less control from this phase's own new
   fixture was unavailable at the pre-fix SHA, say so here and name the substitution made.

4. THE PROOF. Each `diff` command verbatim and its output. Report the empty output explicitly — write
   the word "(empty)" rather than leaving a blank region — together with exit status 0. State that
   this is SC#4's discharge and that no golden `.typ` was committed, per D-04.

5. PRODUCTION-DIFF ISOLATION. The pathspec-scoped `git diff --stat ... -- typsphinx/` output showing
   only `translator.py`, the unscoped `git diff --stat` output, and the one-sentence explanation of
   the difference.

6. VERDICT. A one-row table: SC#4 MET, with the section reference.
  </action>
  <acceptance_criteria>
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md` exists.
    - It contains two distinct 40-hex commit SHAs.
    - It contains at least two `readlink -f` results resolving into `/nix/store`, and at least two recorded `typsphinx.__file__` paths that differ from each other.
    - It contains at least one verbatim `diff` command whose reported output is explicitly marked empty and whose exit status is recorded as 0.
    - It contains a `git diff --stat` line scoped by `-- typsphinx/` showing `translator.py`.
    - It states that no golden `.typ` was committed, citing D-04.
    - It contains a verdict row naming SC#4.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md; test -f "$F" &amp;&amp; test "$(grep -Eo '[0-9a-f]{40}' "$F" | sort -u | wc -l)" -ge 2 &amp;&amp; grep -q 'nix/store' "$F" &amp;&amp; grep -q '__file__' "$F" &amp;&amp; grep -q 'diff' "$F" &amp;&amp; grep -q 'typsphinx/translator.py' "$F" &amp;&amp; grep -q 'D-04' "$F" &amp;&amp; grep -q 'SC#4' "$F" &amp;&amp; test -z "$(git status --porcelain typsphinx/ tests/)"</automated>
  </verify>
  <done>`42-GATE-EVIDENCE-05.md` records both named commits, the per-worktree isolation proof, every build command with its exit status, the explicitly-empty pairwise diff at exit 0, the pathspec-isolated production diff, and a verdict row discharging SC#4.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created by Phase 42 as a whole (owner plan in brackets). The plan-review
source-grounding pass must treat every entry here as newly created by this phase, not as drift.

Fixtures and test modules:
- `tests/fixtures/captioned_table_propagated_target_render_gate/conf.py` [42-01]
- `tests/fixtures/captioned_table_propagated_target_render_gate/index.rst` [42-01]
- `tests/test_captioned_table_propagated_target_render_gate.py` [42-01]
- class `TestCaptionedTablePropagatedTargetRenderGate` [42-01]
- `tests/fixtures/figure_propagated_target_render_gate/conf.py` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/index.rst` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/image.png` [42-02]
- `tests/test_figure_propagated_target_render_gate.py` [42-02]
- class `TestFigurePropagatedTargetRenderGate` [42-02]

Production symbol:
- local `was_captioned` in `TypstTranslator.depart_table` (`typsphinx/translator.py`) [42-04]

rST label ids introduced by the new fixtures:
- `tbl-target`, `tbl-name`, `tbl-target-noname`, `tbl-target-li`, `tbl-name-li`, `tbl-target-a`,
  `tbl-target-b`, `tbl-name-two` [42-01]
- `fig-target`, `fig-name`, `fig-target-noname`, `fig-target-li`, `fig-name-li` [42-02]

Caption sentinels:
- `TBLTGTNAMEDSENTINEL`, `TBLTGTNONAMESENTINEL`, `TBLTGTLISTSENTINEL`, `TBLTGTTWOSENTINEL` [42-01]
- `FIGTGTNAMEDSENTINEL`, `FIGTGTNONAMESENTINEL`, `FIGTGTLISTSENTINEL` [42-02]

Evidence and planning artifacts (all under
`.planning/phases/42-captioned-table-drops-preceding-target-label/`):
- `42-GATE-EVIDENCE-01.md` — table-side RED, SC#1 + SC#5 [42-01]
- `42-GATE-EVIDENCE-02.md` — figure-side answer + permanent gate, SC#2 [42-02]
- `42-GATE-EVIDENCE-03.md` — repo-wide misrouting sweep, D-06 + D-07 [42-03]
- `42-GATE-EVIDENCE-04.md` — table-side GREEN + regression sweep, SC#3 [42-04]
- `42-GATE-EVIDENCE-05.md` — caption-less byte invariance, SC#4 + D-04 [42-05]
- `42-SC4-INVARIANTS.md` — Phase 41 SC#4 sweep re-measured over a range including Phase 42, with the
  change-site to RED manifest, SC#6 [42-06]
- `42-CLOSEOUT-GUARD.md` — REL-04/REL-05 checkbox-flip guard, SC#6 [42-06]

Todo:
- `.planning/todos/pending/<YYYY-MM-DD>-table-whitespace-only-title-anchor-divergence.md` — D-08 [42-03]

Modified (not created): `CHANGELOG.md` gains one bullet under `## [0.7.0]` -> `### Fixed` [42-06];
`typsphinx/translator.py` `depart_table` [42-04].
</artifacts_this_phase_produces>

<planner_assumptions>
Flagged planner assumptions for this plan (surfaced, never silently dropped):

- The pre-fix side is assumed to be plan 42-01's RED-recording commit. If phase execution produces
  intervening commits that touch `typsphinx/` — it should not; plan 42-04's fix is scoped as the
  only such commit — the executor must pick the true last pre-fix commit instead and record why.
- This phase's own caption-less control table lives in a fixture created by plan 42-01, so it exists
  at both chosen SHAs only if the pre-fix SHA is at or after 42-01. The task carries an explicit
  fallback and a requirement to record the substitution rather than drop the shape.
- Both spec-less edge-probe rows (`empty`, `encoding`) are authored in plan 42-01 — exactly one
  producer per predicate, so they are not restated here.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository history -> throwaway worktree | Two different code states are checked out and executed; a provisioning miss silently collapses them into one. |
| emitted `.typ` -> the evidence record | The empty diff IS the proof; a diff taken against the wrong pair of builds is an unfalsifiable claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-11 | Spoofing | the byte-invariance proof itself | high | mitigate | An unprovisioned worktree resolves `import typsphinx` to the MAIN checkout via the PEP-660 editable finder, so both sides are built by identical code and the diff is empty for the wrong reason. Mitigated by requiring a recorded `typsphinx.__file__` per worktree, plus the recorded shim `readlink -f` results, as acceptance criteria. |
| T-42-12 | Repudiation | the recorded production diff | medium | mitigate | An unscoped `git diff --stat` between the two SHAs includes unrelated evidence and tracking commits, making the change look larger than it is. Mitigated by recording BOTH the `-- typsphinx/` pathspec-scoped diff and the unscoped one, with the discrepancy explained. |
| T-42-13 | Tampering | leftover throwaway worktrees | low | mitigate | Abandoned worktrees pointing at old SHAs can confuse later `git worktree` operations and the wave-cleanup guard. Mitigated by an explicit removal step and a `git worktree list` acceptance check. |
| T-42-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan runs `uv sync --extra dev` in throwaway worktrees against the repository's EXISTING pinned lockfile; it adds, removes, and upgrades nothing. 42-RESEARCH.md's Package Legitimacy Audit records "not applicable" — no `[ASSUMED]`/`[SUS]`/`[SLOP]` entry exists, so no legitimacy checkpoint is required. `git diff -- pyproject.toml uv.lock` must stay empty (milestone invariant #1), verified in plan 42-06's invariant sweep. |
</threat_model>

<verification>
- Two throwaway worktrees at distinct named SHAs, each independently provisioned, each proven to resolve `import typsphinx` to its own copy.
- Both `-b typst` builds exit 0; the pairwise `.typ` diff is empty at exit 0.
- `git diff --stat <PRE_FIX>..<POST_FIX> -- typsphinx/` lists only `typsphinx/translator.py`.
- No golden `.typ` committed; `git status --porcelain typsphinx/ tests/` empty.
- `git worktree list` shows no leftover scratch worktree.
</verification>

<success_criteria>
- SC#4 discharged: the caption-less table path is proven byte-for-byte unchanged by an empty two-build diff over named pre-fix and post-fix commits, recorded in an evidence file.
- D-04 satisfied: the proof is the recorded diff, not a committed golden file, and not both.
- The proof is falsifiable: the evidence file records enough per-worktree state that a reader can tell the two sides were built by different code.
</success_criteria>

<output>
Create `.planning/phases/42-captioned-table-drops-preceding-target-label/42-05-SUMMARY.md` when done.
</output>
