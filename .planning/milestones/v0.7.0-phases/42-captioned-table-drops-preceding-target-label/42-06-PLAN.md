---
phase: 42-captioned-table-drops-preceding-target-label
plan: 06
type: execute
wave: 3
depends_on: ["42-04"]
files_modified:
  - CHANGELOG.md
  - .planning/phases/42-captioned-table-drops-preceding-target-label/42-SC4-INVARIANTS.md
  - .planning/phases/42-captioned-table-drops-preceding-target-label/42-CLOSEOUT-GUARD.md
autonomous: true
requirements: [TBL-03]

must_haves:
  truths:
    - "The curated `## [0.7.0]` CHANGELOG entry gains a TBL-03 bullet under its existing `### Fixed` heading, at the same user-visible-change granularity and requirement-ID-in-parentheses shape Phase 41 already used for the MATH-02 bullet (SC#6)."
    - "Phase 41's SC#4 invariant sweep is re-measured over a SHA range that INCLUDES Phase 42's own commits, using the same BASE `51e02b6b61b314c99740883fb4bee7ce7b9be76b` that `41-SC4-INVARIANTS.md` used, with HEAD being Phase 42's tip rather than Phase 41's (SC#6)."
    - "The re-measurement is written as a NEW evidence file under this phase's own directory. No `41-*` artifact is edited, renamed, or appended to — that fence is an explicit user decision recorded in CONTEXT.md's Claude's Discretion section."
    - "The re-measured sweep includes a change-site to RED manifest row for Phase 42's own `depart_table` change, in the same column shape `40.1-NONREGRESSION.md` § 4 uses, so a future milestone's sweep can read this file the way Phase 41 read Phase 40.1's."
    - "REL-04 and REL-05 stay unchecked and Pending. Their pre-close state in `.planning/REQUIREMENTS.md` is recorded verbatim with a checksum, together with the exact revert procedure, because `phase.complete` auto-flipped exactly these four lines against an explicit decision during Phase 41."
    - "Milestone invariant #1 holds across Phase 42: `git diff <BASE>..HEAD -- pyproject.toml uv.lock` is empty, so no runtime or dev dependency was added — the mechanical discharge of this phase's supply-chain threat row."
  artifacts:
    - CHANGELOG.md
    - .planning/phases/42-captioned-table-drops-preceding-target-label/42-SC4-INVARIANTS.md
    - .planning/phases/42-captioned-table-drops-preceding-target-label/42-CLOSEOUT-GUARD.md
  key_links:
    - "`41-HANDOFF.md`'s 7-item publish checklist runs only AFTER this phase verifies; its item 6 is the REL-04/REL-05 flip hazard this plan's guard file makes falsifiable."
    - "`41-CONTEXT.md` D-11 defines the scope of the SC#4 invariant sweep being re-measured; `41-SC4-INVARIANTS.md` supplies the BASE SHA and the command shapes."
    - "TBL-03 is milestone invariant #4's second classic-`TypstError`-RED exception alongside CIT-01; the re-measured 'every node-handler change carries its recorded-RED GATE-01 fixture' clause is the one that actually grows when Phase 42 enters the range."
  prohibitions:
    - "No `41-*` artifact may be edited, appended to, or regenerated to carry Phase 42's reconciliation. The reconciliation is a new file under this phase's directory, by explicit user decision."
    - "The `## [0.7.0]` heading's existing date must not be changed without an explicit owner decision. The date question is a flagged open item, not a discretionary edit — a silent date change would rewrite a curated release artifact."
    - "REL-04 and REL-05 must not be checked off or moved to Complete by this phase. They are close-side work owned by `/gsd-complete-milestone`, and their text depends on a publish that has not happened."
---

<objective>
Reconcile Phase 41's release-prep artifacts with Phase 42's diff (SC#6), and arm the guard against
the REL-04/REL-05 auto-flip that hit Phase 41.

Purpose: Phase 41 produced the curated `## [0.7.0]` CHANGELOG entry and the SC#4 milestone-invariant
sweep against a tree that predates Phase 42. Both are now stale in a specific, bounded way: the
CHANGELOG has no TBL-03 line, and the sweep's "every node-handler change carries its recorded-RED
GATE-01 fixture" clause was measured over a SHA range ending before this phase's `depart_table`
change. The owner blocked the v0.7.0 publish on Phase 42 rather than resequencing, so Phase 42's
SC#6 owns closing both before `/gsd-complete-milestone` runs.

Output: one CHANGELOG bullet, `42-SC4-INVARIANTS.md`, and `42-CLOSEOUT-GUARD.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-CONTEXT.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-04-SUMMARY.md
@CLAUDE.md
</context>

<worktree_provisioning>
CLAUDE.md's worktree-isolated execution is the STANDING mode. Before ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Assert the shim landed: `for t in uv ruff; do readlink -f ".venv/bin/$t"; done` must print
   `/nix/store/...` paths.
4. Run every subsequent command through `uv run`.
5. NOTE on the `<automated>` verify blocks in this plan: `&amp;&amp;` is XML-escaped
   `&&`. Unescape it before running the command in a shell. Same for `&amp;` on its own.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Add the TBL-03 bullet to the curated 0.7.0 CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1-60 — the `## [Unreleased]` section, the `## [0.7.0]` heading and its release prose, and the `### Added` / `### Changed` / `### Fixed` / `### Verified` subsections. The MATH-02 bullet under `### Fixed` is the exact granularity and shape to match.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` — Phase 41's D-01 (bullet granularity: user-visible change grain, requirement ID in trailing parentheses) and D-02 (Keep-a-Changelog section semantics: a repaired compile-fatal defect is "Fixed").
    - `.planning/REQUIREMENTS.md` § TBL-03 — the requirement text the bullet must faithfully describe.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-04.md` — the landed fix, so the bullet describes what actually shipped rather than what was planned.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md` § 6 — the measured insertion point and the draft wording (a draft, not locked phrasing).
  </read_first>
  <action>
Insert exactly ONE new bullet into `CHANGELOG.md`, under the EXISTING `## [0.7.0]` entry's `### Fixed`
heading, immediately after the MATH-02 bullet and before the blank line preceding `### Verified`.
Do not create a new section, do not touch `## [Unreleased]`, and do not reorder or reword any
existing bullet.

Shape the bullet to match the MATH-02 bullet exactly: a bolded lead clause stating the user-visible
change with the requirement ID in trailing parentheses, an em-dash, then one sentence of detail.
The lead clause must say that a captioned table preceded by a standalone target no longer drops the
target's label, tagged `(TBL-03)`. The detail sentence must say that both the table's own
name-derived label and the propagated target's label are now emitted, so a reference to either
resolves instead of aborting the compile on a dangling label. Verify the wording against what
actually landed in plan 42-04 before writing it — 42-RESEARCH.md's draft is a suggestion, not locked
phrasing.

Do NOT change the `## [0.7.0]` heading's existing date. Whether that date should track Phase 42's
landing date instead of Phase 41's close date is an open owner question recorded in this plan's
flagged assumptions; changing a curated release artifact's date without a decision is out of scope.
Record the question in this plan's SUMMARY under Deviations so the orchestrator surfaces it before
`/gsd-complete-milestone` runs.

Keep the file's existing line-wrap discipline (the surrounding bullets wrap well before column 100).
  </action>
  <acceptance_criteria>
    - `CHANGELOG.md` contains the literal `(TBL-03)` exactly once.
    - `grep -n 'TBL-03' CHANGELOG.md` reports a line number that falls between the `### Fixed` heading line under `## [0.7.0]` and the following `### Verified` heading line.
    - The `## [Unreleased]` section is unchanged and still contains no bullets.
    - `git diff -- CHANGELOG.md` shows only additions (no deleted or reworded existing lines) apart from whitespace required to place the new bullet.
    - The `## [0.7.0]` heading line is byte-identical to its pre-task state, including its date.
    - The MATH-02 bullet is byte-identical to its pre-task state.
    - No file under `.planning/phases/41-v0-7-0-release-automation-release-prep/` is modified.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '(TBL-03)' CHANGELOG.md)" = "1" &amp;&amp; test "$(git diff --numstat -- CHANGELOG.md | awk '{print $2}')" = "0" &amp;&amp; test -z "$(git status --porcelain .planning/phases/41-v0-7-0-release-automation-release-prep/)" &amp;&amp; awk '/^## \[0.7.0\]/{z=1} z&amp;&amp;/^### Fixed/{f=NR} z&amp;&amp;/^### Verified/{v=NR} /TBL-03/{t=NR} END{exit !(f&amp;&amp;v&amp;&amp;t&amp;&amp;t>f&amp;&amp;t<v)}' CHANGELOG.md</automated>
  </verify>
  <done>The curated `## [0.7.0]` entry carries exactly one TBL-03 bullet under `### Fixed`, matching the MATH-02 bullet's granularity and shape; nothing else in the CHANGELOG changed, including the heading date; no Phase 41 artifact was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure Phase 41's SC#4 invariant sweep over a range including Phase 42</name>
  <files>.planning/phases/42-captioned-table-drops-preceding-target-label/42-SC4-INVARIANTS.md</files>
  <read_first>
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md` — read the WHOLE file. It supplies the BASE SHA, the command shapes, the invariant-by-invariant structure, and its own warning that the commit count is a moving target. READ ONLY; this file must not be edited.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` § D-11 — the scope definition of the sweep being re-measured.
    - `.planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md` § 4 — the change-site to RED manifest column shape to reuse.
    - `.planning/REQUIREMENTS.md` § "Milestone invariants" — the six invariants, including #4's amended wording naming TBL-03 as the second classic-RED exception.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-01.md` and `.../42-GATE-EVIDENCE-04.md` — the RED-recording commit and the fix commit that populate this phase's manifest row.
  </read_first>
  <action>
Write `42-SC4-INVARIANTS.md` as a NEW file under this phase's directory. Never edit, append to, or
regenerate any `41-*` artifact — that fence is an explicit user decision. Every number must be
transcribed from a command actually run in THIS worktree; `41-SC4-INVARIANTS.md`'s own figures are
now stale by construction and must be re-derived, not copied.

Use the SAME BASE as Phase 41: `51e02b6b61b314c99740883fb4bee7ce7b9be76b`. Re-derive it rather than
trusting it — record `git describe --tags <BASE>` and confirm it still resolves one commit past the
`v0.6.5` tag. HEAD is Phase 42's own tip in this worktree, not Phase 41's; record
`git rev-parse HEAD` and `git log --oneline <BASE>..HEAD | wc -l`, and note in one sentence that the
commit count is a moving target and this is a newer value than any recorded in Phase 41.

Prove Phase 42 is inside the range: run `git merge-base --is-ancestor` for plan 42-04's fix commit
against HEAD, and show that commit in `git log --oneline <BASE>..HEAD -- typsphinx/translator.py`.

Then re-measure each milestone invariant over `<BASE>..HEAD`, quoting every command and its output
verbatim:

- Invariant 1, zero new runtime dependencies: `git diff --stat <BASE>..HEAD -- pyproject.toml` and
  `... -- uv.lock`. Both must be empty for Phase 42's own contribution; state Phase 41's version-bump
  changes separately if they appear, and attribute each hunk rather than reporting a bare total.
- Invariant 2, the `@preview` package count stays at four with no new version-lockstep site: re-run
  the check over `writer.py`, `template_engine.py`, `templates/base.typ`, and `examples/**/*.typ`,
  and record the passing `tests/test_preview_version_sync.py` result.
- Invariant 4, every node-handler change carries its recorded-RED GATE-01 fixture: this is THE clause
  that grows. Enumerate `git log --oneline <BASE>..HEAD -- typsphinx/translator.py`, identify the
  Phase 42 commit(s), and account for each. Then produce the CHANGE-SITE TO RED MANIFEST table in
  `40.1-NONREGRESSION.md` § 4's column shape — change site (function/symbol), requirement, evidence
  file, RED form, RED provenance, pytest selector, and the commit the RED was recorded against —
  with one row for Phase 42's `depart_table` change. Its RED form is the classic
  `typst.compile()` fatal (milestone invariant #4's TBL-03 exception), its provenance is a real
  `sphinx-build -b typstpdf` over an in-repo fixture, and its recording commit is plan 42-01's,
  which is an ancestor of the fix commit. Add the GREEN-commit reference row alongside, as Phase 40.1
  did.
- Invariant 5, test migration owned per phase: record the test files this phase added or touched.
- Invariant 6, "anywhere under X" criteria checked by repo-wide grep: state whether any Phase 42
  criterion is of that shape and, if so, record the repo-wide grep rather than a file-scoped one.

Close with a verdict table, one row per invariant re-measured, and an explicit statement naming what
this file supersedes for the purposes of the pending publish: `41-SC4-INVARIANTS.md` remains the
record for the range it measured, and this file is the record for the range that includes Phase 42.
Do not describe `41-SC4-INVARIANTS.md` as wrong — it was correct for its range.
  </action>
  <acceptance_criteria>
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-SC4-INVARIANTS.md` exists.
    - It contains the BASE SHA `51e02b6b61b314c99740883fb4bee7ce7b9be76b` and a `git describe --tags` output for it.
    - It contains a `git rev-parse HEAD` value distinct from the BASE, and a commit count for `<BASE>..HEAD`.
    - It contains a `git merge-base --is-ancestor` proof that plan 42-04's fix commit is inside the range, and shows that commit in a `git log ... -- typsphinx/translator.py` listing.
    - It contains a change-site to RED manifest table with at least the columns change site, evidence file, RED form, RED provenance, pytest selector, and recording commit — with a row for `depart_table`.
    - It records `git diff --stat <BASE>..HEAD -- pyproject.toml` and `... -- uv.lock` outputs.
    - It contains a verdict table with one row per milestone invariant re-measured.
    - `git status --porcelain .planning/phases/41-v0-7-0-release-automation-release-prep/` is empty — no Phase 41 artifact was touched.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/42-captioned-table-drops-preceding-target-label/42-SC4-INVARIANTS.md; test -f "$F" &amp;&amp; grep -q '51e02b6b61b314c99740883fb4bee7ce7b9be76b' "$F" &amp;&amp; grep -q 'merge-base' "$F" &amp;&amp; grep -q 'depart_table' "$F" &amp;&amp; grep -q 'pyproject.toml' "$F" &amp;&amp; grep -q 'uv.lock' "$F" &amp;&amp; grep -q 'RED' "$F" &amp;&amp; grep -q '42-GATE-EVIDENCE-01' "$F" &amp;&amp; test -z "$(git status --porcelain .planning/phases/41-v0-7-0-release-automation-release-prep/)"</automated>
  </verify>
  <done>`42-SC4-INVARIANTS.md` re-measures the milestone invariants over a BASE-to-HEAD range proven to include Phase 42's fix commit, carries a change-site to RED manifest row for `depart_table` in Phase 40.1's column shape, and exists as a new file with every `41-*` artifact untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Arm the REL-04 / REL-05 checkbox-flip guard</name>
  <files>.planning/phases/42-captioned-table-drops-preceding-target-label/42-CLOSEOUT-GUARD.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the REL-04 and REL-05 requirement bullets and their Traceability rows. Record the line numbers you actually observe; the file changes between phases.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md` item 6 — the hazard as Phase 41 documented it, and the 7-item publish checklist this guard protects. READ ONLY.
    - `.planning/STATE.md` § "Phase 41 (previous position — COMPLETE)" — the record that `phase.complete` auto-flipped all four lines on 2026-08-03 against an explicit decision, and that the flip was measured against a pre-run copy and reverted before commit.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md` § 6 "REL-04/REL-05 checkbox-flip hazard" — the exact mitigation shape.
  </read_first>
  <action>
Write `42-CLOSEOUT-GUARD.md` as a new file under this phase's directory. Its job is to make the
post-close diff check falsifiable rather than remembered.

Record, verbatim and with the line numbers observed in this worktree, the current text of the four
lines at risk in `.planning/REQUIREMENTS.md`: the REL-04 requirement bullet's checkbox line, the
REL-05 requirement bullet's checkbox line, and the two Traceability rows for REL-04 and REL-05.
Alongside them record a `sha256sum .planning/REQUIREMENTS.md` value and the current TBL-03 lines
(its own checkbox bullet and its Traceability row), so the guard distinguishes a legitimate TBL-03
flip from an illegitimate REL flip.

Then write the procedure, as ordered, runnable steps:

1. After any `phase.complete`-family command runs and BEFORE committing, run
   `git diff -- .planning/REQUIREMENTS.md`.
2. If the diff touches the REL-04 or REL-05 checkbox bullets or their Traceability rows, that change
   is unintended: revert `.planning/REQUIREMENTS.md`, then re-apply by hand only the TBL-03 checkbox
   and Traceability-row flip that Phase 42 legitimately earns.
3. Re-run `git diff -- .planning/REQUIREMENTS.md` and confirm only TBL-03 lines changed.

State why REL-04 and REL-05 must stay unchecked: they are close-side work owned by
`/gsd-complete-milestone`, and REL-05's own text requires the publish to have executed while REL-04's
body swap is first exercised by that same tag push. Neither has happened; local and remote `v0.7.0`
tags are still empty.

Close with a short pointer section: `41-HANDOFF.md`'s 7-item publish checklist is still valid and
still un-executed, and runs only after Phase 42 verifies. Do not copy the checklist into this file
and do not modify `41-HANDOFF.md`.

This task changes no requirement state: REL-04 and REL-05 stay `[ ]` and Pending, and TBL-03's own
checkbox is NOT flipped here either — flipping it is the phase-close step the guard protects.
  </action>
  <acceptance_criteria>
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-CLOSEOUT-GUARD.md` exists.
    - It quotes the current REL-04 and REL-05 checkbox lines and their two Traceability rows verbatim, each with an observed line number.
    - It contains a `sha256sum` value for `.planning/REQUIREMENTS.md`.
    - It contains a three-step ordered procedure naming `git diff -- .planning/REQUIREMENTS.md` and the revert-then-reapply sequence.
    - It states why REL-04 and REL-05 must stay unchecked, referencing the un-executed publish.
    - `.planning/REQUIREMENTS.md` is UNCHANGED by this plan: `git status --porcelain .planning/REQUIREMENTS.md` is empty, REL-04 and REL-05 are still `- [ ]`, and their Traceability rows still read Pending.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md` is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/42-captioned-table-drops-preceding-target-label/42-CLOSEOUT-GUARD.md; test -f "$F" &amp;&amp; grep -q 'REL-04' "$F" &amp;&amp; grep -q 'REL-05' "$F" &amp;&amp; grep -q 'sha256' "$F" &amp;&amp; grep -q 'git diff -- .planning/REQUIREMENTS.md' "$F" &amp;&amp; test -z "$(git status --porcelain .planning/REQUIREMENTS.md .planning/phases/41-v0-7-0-release-automation-release-prep/)" &amp;&amp; grep -q '^- \[ \] \*\*REL-04\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^- \[ \] \*\*REL-05\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>`42-CLOSEOUT-GUARD.md` records the four at-risk lines verbatim with a file checksum, gives a three-step diff-check-and-revert procedure, explains why REL-04 and REL-05 stay unchecked, and leaves `.planning/REQUIREMENTS.md` and every Phase 41 artifact untouched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created by Phase 42 as a whole (owner plan in brackets). The plan-review
source-grounding pass must treat every entry here as newly created by this phase, not as drift.

Fixtures and test modules:
- `tests/fixtures/captioned_table_propagated_target_render_gate/conf.py` [42-01]
- `tests/fixtures/captioned_table_propagated_target_render_gate/index.rst` [42-01]
- `tests/test_captioned_table_propagated_target_render_gate.py` [42-01]
- class `TestCaptionedTablePropagatedTargetRenderGate` [42-01]
- `tests/fixtures/figure_propagated_target_render_gate/conf.py` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/index.rst` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/image.png` [42-02]
- `tests/test_figure_propagated_target_render_gate.py` [42-02]
- class `TestFigurePropagatedTargetRenderGate` [42-02]

Production symbol:
- local `was_captioned` in `TypstTranslator.depart_table` (`typsphinx/translator.py`) [42-04]

rST label ids introduced by the new fixtures:
- `tbl-target`, `tbl-name`, `tbl-target-noname`, `tbl-target-li`, `tbl-name-li`, `tbl-target-a`,
  `tbl-target-b`, `tbl-name-two` [42-01]
- `fig-target`, `fig-name`, `fig-target-noname`, `fig-target-li`, `fig-name-li` [42-02]

Caption sentinels:
- `TBLTGTNAMEDSENTINEL`, `TBLTGTNONAMESENTINEL`, `TBLTGTLISTSENTINEL`, `TBLTGTTWOSENTINEL` [42-01]
- `FIGTGTNAMEDSENTINEL`, `FIGTGTNONAMESENTINEL`, `FIGTGTLISTSENTINEL` [42-02]

Evidence and planning artifacts (all under
`.planning/phases/42-captioned-table-drops-preceding-target-label/`):
- `42-GATE-EVIDENCE-01.md` — table-side RED, SC#1 + SC#5 [42-01]
- `42-GATE-EVIDENCE-02.md` — figure-side answer + permanent gate, SC#2 [42-02]
- `42-GATE-EVIDENCE-03.md` — repo-wide misrouting sweep, D-06 + D-07 [42-03]
- `42-GATE-EVIDENCE-04.md` — table-side GREEN + regression sweep, SC#3 [42-04]
- `42-GATE-EVIDENCE-05.md` — caption-less byte invariance, SC#4 + D-04 [42-05]
- `42-SC4-INVARIANTS.md` — Phase 41 SC#4 sweep re-measured over a range including Phase 42, with the
  change-site to RED manifest, SC#6 [42-06]
- `42-CLOSEOUT-GUARD.md` — REL-04/REL-05 checkbox-flip guard, SC#6 [42-06]

Todo:
- `.planning/todos/pending/<YYYY-MM-DD>-table-whitespace-only-title-anchor-divergence.md` — D-08 [42-03]

Modified (not created): `CHANGELOG.md` gains one bullet under `## [0.7.0]` -> `### Fixed` [42-06];
`typsphinx/translator.py` `depart_table` [42-04].
</artifacts_this_phase_produces>

<planner_assumptions>
Flagged planner assumptions for this plan (surfaced, never silently dropped):

- OPEN OWNER QUESTION, deliberately not decided by the planner: the `## [0.7.0]` heading currently
  carries Phase 41's close date. Whether it should track Phase 42's actual landing date instead was
  raised by 42-RESEARCH.md (Assumption A3, Open Question 1) and never answered. This plan leaves the
  date untouched and requires the executor to re-surface the question in its SUMMARY Deviations
  section, so the orchestrator can put it to the owner before `/gsd-complete-milestone` runs. A
  stale date is cosmetic and does not block SC#6's mechanical requirement.
- The BASE SHA is assumed still to resolve to one commit past the `v0.6.5` tag. The task requires
  re-deriving it with `git describe --tags` rather than trusting the constant; if it no longer
  resolves that way, that is a finding to record, not a value to quietly substitute.
- The commit count over `<BASE>..HEAD` is a moving target on an actively developed branch. Phase 41
  recorded four different values for the same quantity across its own lifetime. The number recorded
  here is correct only for the moment it was measured, and the file must say so.
- Both spec-less edge-probe rows (`empty`, `encoding`) are authored in plan 42-01 — exactly one
  producer per predicate, so they are not restated here.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase tooling -> `.planning/REQUIREMENTS.md` | `phase.complete`-family commands write requirement state automatically and have twice overridden an explicit decision. |
| curated release artifacts -> the publish | `CHANGELOG.md`'s `## [0.7.0]` section becomes the GitHub Release body (REL-04); a wrong or missing line ships to users. |
| repository history -> the invariant sweep | The sweep's conclusions are only as valid as the SHA range it was measured over. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-14 | Tampering | `.planning/REQUIREMENTS.md` REL-04/REL-05 state | medium | mitigate | An automated checkbox flip would mark unreleased work as delivered, against an explicit user decision. Mitigated by `42-CLOSEOUT-GUARD.md`: the four at-risk lines recorded verbatim with a file checksum, plus a three-step diff-check-and-revert procedure run before any commit. |
| T-42-15 | Repudiation | a stale SC#4 invariant sweep | medium | mitigate | Publishing on a sweep measured over a range that excludes Phase 42 would assert an invariant nobody checked. Mitigated by re-measuring over a range proven by `git merge-base --is-ancestor` to contain the fix commit, in a NEW file that does not overwrite Phase 41's still-valid record for its own range. |
| T-42-16 | Information Disclosure | the GitHub Release body sourced from the CHANGELOG | low | accept | The section is curated prose describing shipped behavior; it contains no credential, endpoint, or internal path. Reviewed as part of task 1's read of the whole `## [0.7.0]` entry. |
| T-42-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package, and this is the plan that mechanically discharges the invariant for the whole phase: `git diff --stat <BASE>..HEAD -- pyproject.toml` and `... -- uv.lock` are recorded in `42-SC4-INVARIANTS.md`. 42-RESEARCH.md's Package Legitimacy Audit records "not applicable" — no `[ASSUMED]`/`[SUS]`/`[SLOP]` entry exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `grep -c '(TBL-03)' CHANGELOG.md` returns 1, and the line falls between `### Fixed` and `### Verified` inside `## [0.7.0]`.
- `git diff --numstat -- CHANGELOG.md` shows zero deleted lines.
- `42-SC4-INVARIANTS.md` proves plan 42-04's fix commit is inside `<BASE>..HEAD` and carries a change-site to RED manifest row for `depart_table`.
- `git diff --stat <BASE>..HEAD -- pyproject.toml uv.lock` is recorded and attributed hunk by hunk.
- `.planning/REQUIREMENTS.md` is unchanged by this plan; REL-04 and REL-05 are still `- [ ]` and Pending.
- No file under `.planning/phases/41-v0-7-0-release-automation-release-prep/` is modified.
</verification>

<success_criteria>
- SC#6 discharged: the curated `## [0.7.0]` CHANGELOG entry gains its TBL-03 line, and the SC#4 invariant sweep is re-measured over a SHA range that includes Phase 42.
- The reconciliation lives entirely in NEW files under this phase's directory; no `41-*` artifact is edited.
- The REL-04 / REL-05 auto-flip hazard is armed with a recorded pre-close state and an explicit revert procedure, rather than relying on someone remembering Phase 41 hit it.
- Milestone invariant #1 is mechanically discharged for Phase 42: no dependency was added.
</success_criteria>

<output>
Create `.planning/phases/42-captioned-table-drops-preceding-target-label/42-06-SUMMARY.md` when done.
</output>
