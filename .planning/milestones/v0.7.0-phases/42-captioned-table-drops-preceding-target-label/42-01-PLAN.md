---
phase: 42-captioned-table-drops-preceding-target-label
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/captioned_table_propagated_target_render_gate/conf.py
  - tests/fixtures/captioned_table_propagated_target_render_gate/index.rst
  - tests/test_captioned_table_propagated_target_render_gate.py
  - .planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-01.md
autonomous: true
requirements: [TBL-03]

must_haves:
  truths:
    - "Running `uv run pytest tests/test_captioned_table_propagated_target_render_gate.py -q` against the unfixed tree exits NON-zero, and its captured output contains the literal `does not exist in the document`."
    - "`git status --porcelain typsphinx/` is empty at the commit that adds the fixture and the test module — the RED is recorded against unfixed production code (SC#5, milestone invariant #4's TBL-03 classic-`TypstError` exception)."
    - "`42-GATE-EVIDENCE-01.md` records, per D-01 shape, the observed `node[\"ids\"]` and `node[\"names\"]` captured at `depart_table` call time, including the reversed chained-target ordering on the two-consecutive-targets shape (SC#1)."
    - "`42-GATE-EVIDENCE-01.md` contains the verbatim `sphinx-build -b typstpdf` stderr for the named-table shape, including the real `TypstError` line naming the dangling label (SC#1)."
    - "The zero/one/many axis of propagated ids is exercised by one fixture: ZERO (the caption-less control table carries no ids and is not figure-wrapped), ONE (`tbl-target`, `tbl-target-noname`, `tbl-target-li`), MANY (`tbl-target-a` and `tbl-target-b` on a single table, asserted by NAME and never by list position)."
    - statement: "Label-name equality between an emitted `[#metadata(none) <name>]` anchor and its `link(<name>, ...)` reference is byte equality over the `_sanitize_label` -> `_namespace_label` output, not a normalized/stripped comparison; the whitespace-only-title branch — where the visit-side structural check and the depart-side stripped-string check disagree about what counts as a caption — is fenced OUT of this phase's fix scope by D-08 and is recorded as a todo by plan 42-03."
      verification: backstop
  artifacts:
    - tests/fixtures/captioned_table_propagated_target_render_gate/conf.py
    - tests/fixtures/captioned_table_propagated_target_render_gate/index.rst
    - tests/test_captioned_table_propagated_target_render_gate.py
    - .planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-01.md
  key_links:
    - "`:ref:`/`:numref:` roles in `index.rst` -> `link(<index:NAME>, ...)` in the emitted `index.typ` -> `[#metadata(none) <index:NAME>]` anchors from `_emit_id_anchors`. The set difference (links minus anchors) IS the dangling-label detector."
    - "`typst_documents` in `conf.py` makes `index` a MASTER document. Without that entry the writer emits only a minimal import set, `TypstPDFBuilder.finish()` never calls `typst.compile()`, and the fixture cannot go RED at all."
    - "docutils' `PropagateTargets` transform moves a standalone `.. _label:` id onto the FOLLOWING body element's `node[\"ids\"]` — this is the only reason the table node carries the target's id."
  prohibitions:
    - "The RED must not be recorded against a tree that already contains the depart_table fix. An evidence file whose recording commit shows a non-empty `git status --porcelain typsphinx/`, or which post-dates plan 42-04's fix commit, is fabricated evidence rather than a RED."
    - "Expected assertion literals must not be regenerated from post-fix output. Every expected label name in the test module is derived by hand from the fixture's own rST ids plus the docname prefix, before the fix lands."
    - "A claim that the RED is a test-side defect must not be accepted on assertion. Any such claim must be accompanied by a re-run of the module against the same commit showing a different signature."
---

<objective>
Author the TBL-03 GATE-01 fixture and test module covering all four measured-failing shapes (D-01)
plus a caption-less control, and record the classic real-`TypstError` RED against the UNFIXED
`depart_table` before any production code is touched.

Purpose: SC#1 and SC#5. TBL-03 is milestone invariant #4's second classic-RED exception (alongside
CIT-01) because the defect fails the Typst compile today rather than compiling wrong. The RED
commit must strictly precede the fix commit, and the reproduction (minimal rST, verbatim Typst
error text, observed `node["ids"]`/`node["names"]` at `depart_table`) must be recorded as phase
evidence — the discuss-phase and research-session measurements are reference material only.

Output: a new dedicated fixture directory, a new render-gate module carrying the D-03 GREEN-side
structural assertions authored up front, and `42-GATE-EVIDENCE-01.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-CONTEXT.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-PATTERNS.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-VALIDATION.md
@CLAUDE.md
</context>

<worktree_provisioning>
CLAUDE.md's worktree-isolated execution is the STANDING mode. Before ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Assert the shim landed: `for t in uv ruff; do readlink -f ".venv/bin/$t"; done` must print
   `/nix/store/...` paths. A self-pointing symlink silently reintroduces ~45 environmental
   integration-test failures and looks like the shim ran.
4. Run every subsequent command through `uv run`.
5. NOTE on the `<automated>` verify blocks in this plan: `&amp;&amp;` is XML-escaped
   `&&`. Unescape it before running the command in a shell. Same for `&amp;` on its own.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Create the captioned-table propagated-target fixture (D-01's four shapes plus the caption-less control)</name>
  <files>tests/fixtures/captioned_table_propagated_target_render_gate/conf.py, tests/fixtures/captioned_table_propagated_target_render_gate/index.rst</files>
  <read_first>
    - `tests/fixtures/paragraph_propagated_target_render_gate/conf.py` — the exact conf shape to copy (project/author/release, `extensions = ["typsphinx"]`, master `typst_documents`, and the docstring convention that names the defect the fixture reproduces).
    - `tests/fixtures/paragraph_propagated_target_render_gate/index.rst` — the standalone `.. _label:`-target-directive-before-node shape and the references-back section.
    - `tests/fixtures/table_in_list_item_render_gate/index.rst` — the list-item-nested table shape and the "top-level control table proves byte-invariance" convention.
    - `tests/fixtures/captioned_table_render_gate/index.rst` and `.../conf.py` — the `:name:`/caption/`:numref:` + ALLCAPS-sentinel convention, and why `numfig = True` is set. Do NOT edit either file.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-PATTERNS.md` § "tests/fixtures/captioned_table_propagated_target_render_gate/index.rst (fixture data)" — the composed rST skeleton for the four shapes.
  </read_first>
  <action>
Create a NEW fixture directory `tests/fixtures/captioned_table_propagated_target_render_gate/`. Do
NOT extend `tests/fixtures/captioned_table_render_gate/` — that fixture backs the already-GREEN
`TestCaptionedTableRenderGate` class with exact-count sentinel assertions, and because a single
dangling label aborts the WHOLE document compile, adding a pre-fix-failing case there would fail an
unrelated already-shipped requirement's assertions throughout the RED-recording window. One fixture
per fixed defect class is the convention already followed by `paragraph_propagated_target_render_gate/`,
`desc_container_propagated_target_render_gate/`, and `rubric_propagated_target_render_gate/`.

`conf.py`: mirror `paragraph_propagated_target_render_gate/conf.py`. Set `project` to
"Captioned Table Propagated Target Render Gate", `author` to "typsphinx tests", `release` to
"0.0.0", `extensions` to the single entry `typsphinx`, and `typst_documents` to a single four-tuple
making `index` a master document. Set `numfig = True` (required for the `:numref:` on shape A;
matches `captioned_table_render_gate/conf.py`'s own reason). Give the module a docstring naming
TBL-03 / Phase 42, the mechanism (docutils' `PropagateTargets` moves a standalone target's id onto
the following table's `node["ids"]`; `depart_table`'s trailing `_emit_id_anchors` call fires while
`self.in_table` is still True so `add_text` routes the anchor into the `table_cell_content` buffer
that is discarded moments later), and the note that `index` MUST stay a master document or the
fixture cannot reach a real `typst.compile()` at all.

`index.rst`: one document containing exactly five tables, in this order, each with a distinct
ALLCAPS caption sentinel so a later PDF-text assertion can be added without editing the fixture:

- Shape A (D-01 shape 1, the reported case): standalone target `.. _tbl-target:` immediately
  followed by `.. table:: TBLTGTNAMEDSENTINEL` carrying `:name: tbl-name` and a two-column
  simple-table body.
- Shape B (D-01 shape 2): standalone target `.. _tbl-target-noname:` immediately followed by
  `.. table:: TBLTGTNONAMESENTINEL` with NO `:name:` option. docutils supplies an auto id as
  `ids[0]` here, so the human-authored target id is the one that lands in `ids[1:]`.
- Shape C (D-01 shape 3): a bullet-list item with lead-in text, then — indented inside that same
  item — `.. _tbl-target-li:` followed by `.. table:: TBLTGTLISTSENTINEL` carrying
  `:name: tbl-name-li`.
- Shape D (D-01 shape 4): two consecutive standalone targets `.. _tbl-target-a:` and
  `.. _tbl-target-b:` on adjacent lines, followed by `.. table:: TBLTGTTWOSENTINEL` carrying
  `:name: tbl-name-two`.
- Control (the byte-invariance control that rides along per D-01): under its own section heading,
  a `.. table::` with NO caption argument, no `:name:`, and no preceding target.

Then a "References back to the propagated targets" section emitting one reference per surviving
label: `:numref:` to `tbl-name`; `:ref:` with EXPLICIT link text to each of `tbl-target`,
`tbl-target-noname`, `tbl-target-li`, `tbl-target-a`, `tbl-target-b`, `tbl-name-li`, and
`tbl-name-two`. Explicit link text is mandatory for every `:ref:` — a bare `:ref:` to a captioned
table defaults its link text to that table's own caption, duplicating the sentinel.

Do not put any inline-literal snippet naming this fixture's own label ids into the fixture prose:
the module's dangling-label sweep strips `raw("...")` segments, but keeping such snippets out
removes the ambiguity entirely. Do not use the `:target:` figure option anywhere — that is a
different docutils mechanism and belongs to no shape here.
  </action>
  <acceptance_criteria>
    - `tests/fixtures/captioned_table_propagated_target_render_gate/conf.py` exists and contains the literals `extensions`, `typsphinx`, `typst_documents`, and `numfig = True`.
    - `tests/fixtures/captioned_table_propagated_target_render_gate/index.rst` contains all five target directives: `.. _tbl-target:`, `.. _tbl-target-noname:`, `.. _tbl-target-li:`, `.. _tbl-target-a:`, `.. _tbl-target-b:`.
    - `index.rst` contains all five caption sentinels: `TBLTGTNAMEDSENTINEL`, `TBLTGTNONAMESENTINEL`, `TBLTGTLISTSENTINEL`, `TBLTGTTWOSENTINEL` — and exactly one `.. table::` directive with no caption argument.
    - `index.rst` contains `:name: tbl-name`, `:name: tbl-name-li`, `:name: tbl-name-two`, and does NOT contain a `:name:` option on the shape-B table.
    - `uv run python -m sphinx -b typst -q -E` over the fixture exits 0 and the emitted `index.typ` contains `link(<index:tbl-target>` and `link(<index:tbl-target-b>` — proving the reference side that will dangle exists.
    - The emitted `index.typ` contains exactly 4 occurrences of `kind: table` — the four captioned shapes are figure-wrapped and the control table is not.
    - No file under `tests/fixtures/captioned_table_render_gate/` is modified.
  </acceptance_criteria>
  <verify>
    <automated>OUT=$(mktemp -d); uv run python -m sphinx -b typst -q -E tests/fixtures/captioned_table_propagated_target_render_gate "$OUT" &amp;&amp; grep -q 'link(<index:tbl-target>' "$OUT/index.typ" &amp;&amp; grep -q 'link(<index:tbl-target-b>' "$OUT/index.typ" &amp;&amp; grep -q 'link(<index:tbl-target-li>' "$OUT/index.typ" &amp;&amp; test "$(grep -c 'kind: table' "$OUT/index.typ")" = "4" &amp;&amp; test -z "$(git status --porcelain typsphinx/ tests/fixtures/captioned_table_render_gate/)"</automated>
  </verify>
  <done>The fixture directory exists with a master-document `conf.py` and an `index.rst` carrying D-01's four failing shapes plus the caption-less control; a real `-b typst` build emits the dangling references and exactly four figure-wrapped tables; no production source and no pre-existing fixture was touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the classic RED-to-GREEN render-gate module and commit it against unfixed source</name>
  <files>tests/test_captioned_table_propagated_target_render_gate.py</files>
  <read_first>
    - `tests/test_paragraph_propagated_target_render_gate.py` — read the WHOLE file. It is the exact skeleton to copy: module docstring shape, `TYPST_AVAILABLE` import guard, the `_run_sphinx_build_typstpdf` helper invoked as `sys.executable -m sphinx`, the `@pytest.mark.skipif` class decorator, and the five assertion families including the `raw("...")`-stripping regex sweep.
    - `typsphinx/translator.py` lines 423-437 (`add_text`) and 481-552 (`_emit_id_anchors`) — the emitted anchor form `[#metadata(none) <label>]` and the `_namespace_label` docname prefix that makes every label `index:...` in a single-doc fixture. READ ONLY; this task changes no production code.
    - `typsphinx/translator.py` lines 3249-3368 (`depart_table`) — the captioned branch's `figure(...)` self-anchor postfix and the bug-site `_emit_id_anchors` call. READ ONLY.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-PATTERNS.md` § "tests/test_captioned_table_propagated_target_render_gate.py (test, request-response)" — the condensed assertion families.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md` § 3 and § "Code Examples" — the classic-RED convention and the per-shape observed ids, including the reversed chained-target order.
  </read_first>
  <behavior>
    - Build the fixture once per class through `-b typstpdf` (never `-b typst`): the semantic label-resolution fatal only fires inside `TypstPDFBuilder.finish()`'s `typst.compile()` call.
    - Compile-clean: the subprocess returncode is 0, and stderr contains neither `does not exist in the document` nor `Typst compilation failed`.
    - Shape A: the emitted `index.typ` contains `[#metadata(none) <index:tbl-target>]` AND the figure self-anchor postfix ` <index:tbl-name>]`.
    - Shape B: the emitted `index.typ` contains `[#metadata(none) <index:tbl-target-noname>]`. No assertion may name the docutils auto id that owns `ids[0]` here — it is unstable.
    - Shape C: the emitted `index.typ` contains `[#metadata(none) <index:tbl-target-li>]` AND ` <index:tbl-name-li>]`.
    - Shape D: the emitted `index.typ` contains BOTH `[#metadata(none) <index:tbl-target-a>]` and `[#metadata(none) <index:tbl-target-b>]`, matched by name and never by list position (the chained-target order is reversed relative to source order).
    - D-03 no-duplicate-label: after stripping `raw("...")` segments, every `index:`-namespaced label DEFINITION occurrence (a `<index:NAME>` token NOT immediately preceded by `link(`) is collected into a list that preserves duplicates; no name occurs more than once.
    - No dangling same-document reference: the set of `link(<NAME>` names minus the set of `[#metadata(none) <NAME>]` anchor names is empty.
    - The caption-less control is not figure-wrapped: `kind: table` occurs exactly 4 times.
    - `index.pdf` exists, is non-empty, and starts with the `%PDF` magic bytes.
  </behavior>
  <action>
Create `tests/test_captioned_table_propagated_target_render_gate.py`, structurally near-identical to
`tests/test_paragraph_propagated_target_render_gate.py`, renamed for this fixture. This task adds a
test module ONLY — it must not touch anything under `typsphinx/`, because the RED must be recorded
against unfixed production code (SC#5, D-01).

Module docstring: name TBL-03 / Phase 42, quote the real fatal signature the unfixed tree produces
(the `TypstError` naming label `<index:tbl-target>` as not existing in the document), state the root
cause in the terms measured in RESEARCH.md § 1 — `depart_table`'s trailing
`_emit_id_anchors(node, skip_ids=set(node.get("ids", [])[:1]))` fires while `self.in_table` is still
True, `add_text` routes every append into `self.table_cell_content` while that flag is set, and that
buffer is deleted a few statements later, so the anchor is discarded rather than merely misplaced —
and state why the module drives `-b typstpdf` rather than `-b typst`.

Module scaffolding, copied from the analog: `re`/`subprocess`/`sys`/`pathlib.Path` imports, the
`try: import typst` / `TYPST_AVAILABLE` guard, a fixture returning
`Path(__file__).parent / "fixtures" / "captioned_table_propagated_target_render_gate"`, and a
`_run_sphinx_build_typstpdf(source_dir, build_dir)` helper that shells out via
`sys.executable -m sphinx` with `capture_output=True, text=True`. Never `uv run sphinx-build` and
never a bare `sphinx-build` — the NixOS-sandbox PATH-shadowing hazard is why every existing
render-gate module uses the `sys.executable` form.

Class `TestCaptionedTablePropagatedTargetRenderGate`, decorated
`@pytest.mark.skipif(not TYPST_AVAILABLE, ...)`. Give it a CLASS-SCOPED artifacts fixture (the
pattern `tests/test_pdf_render_gate.py` uses for `captioned_table_render_gate_artifacts`) that runs
the build ONCE and returns a small container exposing the `CompletedProcess`, the `index.typ` text
if it exists, and the `index.pdf` path. That fixture must NOT assert anything: pre-fix the build
fails and `index.pdf` never appears, and the RED must surface as named per-shape test failures
rather than as one fixture error.

Nine thin test methods over that single build, one behavior each, named so the RED output names the
failing shape: a compile-clean test; one test per D-01 shape A/B/C/D; the D-03 no-duplicate-label
test; the generic dangling-reference sweep; the caption-less control test; and the PDF magic-byte
test. Implement each against the `<behavior>` block above.

Two implementation details that are load-bearing and must not be simplified away. First, restrict
the duplicate-definition scan to `index:`-prefixed labels using a negative lookbehind on `link(` —
scanning every `<...>` token would pick up unrelated markup from the template preamble, and dropping
the lookbehind would count references as definitions. Second, strip `raw("...")` string segments
before every regex scan, exactly as the analog does.

Commit this task's work with a `test(42-01): ...` message. Before committing, confirm
`git status --porcelain typsphinx/` prints nothing.
  </action>
  <acceptance_criteria>
    - `tests/test_captioned_table_propagated_target_render_gate.py` exists and defines the class `TestCaptionedTablePropagatedTargetRenderGate` with nine test methods.
    - The module invokes Sphinx via `sys.executable` and the string `-b`, `typstpdf`; it contains neither the substring `uv run sphinx-build` nor a bare `sphinx-build` invocation.
    - The module contains all six expected anchor literals: `[#metadata(none) <index:tbl-target>]`, `[#metadata(none) <index:tbl-target-noname>]`, `[#metadata(none) <index:tbl-target-li>]`, `[#metadata(none) <index:tbl-target-a>]`, `[#metadata(none) <index:tbl-target-b>]`, and the figure postfix form ` <index:tbl-name>]`.
    - The module contains no literal naming a docutils auto id (no `index:id1`-style expectation for shape B).
    - `git status --porcelain typsphinx/` prints nothing at this task's commit — the RED is recorded against unfixed source.
    - `uv run pytest tests/test_captioned_table_propagated_target_render_gate.py -q` exits NON-zero, and its combined output contains the literal `does not exist in the document`. This is the expected failure signature; a run that fails with any other signature means the fixture or the module is wrong, not the production code.
    - `uv run black --check tests/test_captioned_table_propagated_target_render_gate.py` and `uv run ruff check tests/test_captioned_table_propagated_target_render_gate.py` both exit 0.
    - The rest of the suite is unaffected: `uv run pytest tests/test_pdf_render_gate.py -q` still passes.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain typsphinx/)" &amp;&amp; uv run black --check tests/test_captioned_table_propagated_target_render_gate.py &amp;&amp; uv run ruff check tests/test_captioned_table_propagated_target_render_gate.py &amp;&amp; uv run pytest tests/test_pdf_render_gate.py -q &amp;&amp; ! uv run pytest tests/test_captioned_table_propagated_target_render_gate.py -q &amp;&amp; uv run pytest tests/test_captioned_table_propagated_target_render_gate.py 2>&amp;1 | grep -q 'does not exist in the document'</automated>
  </verify>
  <done>The render-gate module exists and is RED against unfixed production source with the expected `does not exist in the document` signature; `git status --porcelain typsphinx/` is empty at its commit; the already-green captioned-table gate is unaffected; lint is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Record the RED and the observed depart_table ids in 42-GATE-EVIDENCE-01.md</name>
  <files>.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-01.md</files>
  <read_first>
    - `.planning/phases/40.1-citation-degradation-hardening/40.1-GATE-EVIDENCE-01.md` — the classic-compile-fatal RED evidence-file shape (recording commit, verbatim command, verbatim output, RED verdict).
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md` § 3 ("Recording the RED literally") and § "Code Examples" — the required contents and the reference values to compare against.
    - `typsphinx/translator.py` lines 3249-3368 (`depart_table`) — so the probe wraps the right method. READ ONLY.
    - `.planning/ROADMAP.md` § Phase 42 success criteria 1 and 5 — the two criteria this file discharges.
  </read_first>
  <action>
Write `42-GATE-EVIDENCE-01.md` under this phase's directory. Every number and every quoted line in
it must be transcribed from a command actually run in THIS worktree — nothing may be copied from
42-RESEARCH.md, 42-CONTEXT.md, or the discuss-phase measurements, all of which are explicitly
reference material and not admissible phase evidence.

Sections to produce, in order:

1. RECORDING COMMIT. `git rev-parse HEAD` and `git log -1 --oneline` (this is Task 2's commit), plus
   `git status --porcelain typsphinx/` showing empty output. State explicitly that no production
   code was changed.

2. THE MINIMAL REPRODUCTION (SC#1). Quote the shape-A rST from the fixture verbatim (the
   `.. _tbl-target:` target plus the `:name: tbl-name` captioned table plus the `:ref:` back to
   `tbl-target`), and record the verbatim command and full stderr of
   `uv run python -m sphinx -b typstpdf -q -E tests/fixtures/captioned_table_propagated_target_render_gate <build>`
   including the real `TypstError` line and its exit status.

3. THE OBSERVED IDS AT `depart_table` (SC#1). Write a THROWAWAY probe script under the scratchpad —
   do NOT commit it to the repository — that imports `typsphinx.translator.TypstTranslator`, wraps
   `depart_table` so it prints `node["ids"]` and `node["names"]` before delegating to the original,
   then drives a `-b typst` build of the fixture through Sphinx's Python entry point. Run it and
   record, as a table with one row per D-01 shape, the shape name, the observed `ids` list, and the
   observed `names` list. Explicitly call out the two-consecutive-targets row's ordering: the
   chained targets arrive REVERSED relative to source order, which is why every assertion in the
   module matches by name rather than by position. Paste the probe script source verbatim into the
   evidence file so the measurement is reproducible without the script being committed.

4. THE RED (SC#5). The verbatim command `uv run pytest tests/test_captioned_table_propagated_target_render_gate.py -v`
   and its full output, including the failure summary line and the assertion text. State which of
   the nine test methods are RED and which (if any) pass pre-fix.

5. VERDICT. A short table: SC#1 discharged by sections 2 and 3; SC#5 discharged by sections 1 and 4.
   State that the GREEN half is owned by plan 42-04 and will be recorded in `42-GATE-EVIDENCE-04.md`,
   and that the caption-less byte-invariance proof (D-04) is owned by plan 42-05.
  </action>
  <acceptance_criteria>
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-01.md` exists.
    - It contains the literal `does not exist in the document` inside the quoted `-b typstpdf` stderr.
    - It contains a 40-hex-character commit SHA on the recording-commit line, and a line showing `git status --porcelain typsphinx/` produced empty output.
    - It contains all four D-01 shape rows in the ids table, including the literals `tbl-target`, `tbl-target-noname`, `tbl-target-li`, `tbl-target-a`, and `tbl-target-b`.
    - It states, in words, that the chained-target ids for the two-consecutive-targets shape arrive reversed relative to source order.
    - It contains the verbatim probe script source, and the probe script itself is NOT present anywhere under `tests/`, `typsphinx/`, or `scripts/` in the committed tree.
    - `git status --porcelain typsphinx/` is still empty at this task's commit.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-01.md; test -f "$F" &amp;&amp; grep -q 'does not exist in the document' "$F" &amp;&amp; grep -Eq '[0-9a-f]{40}' "$F" &amp;&amp; grep -q 'tbl-target-noname' "$F" &amp;&amp; grep -q 'tbl-target-a' "$F" &amp;&amp; grep -q 'tbl-target-b' "$F" &amp;&amp; grep -q 'tbl-target-li' "$F" &amp;&amp; test -z "$(git status --porcelain typsphinx/)"</automated>
  </verify>
  <done>`42-GATE-EVIDENCE-01.md` records the recording commit with an empty production-source status, the verbatim Typst fatal, the per-shape `node["ids"]`/`node["names"]` observed at `depart_table` call time with the reversed-order caveat, the verbatim pytest RED, and a verdict table discharging SC#1 and SC#5.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created by Phase 42 as a whole (owner plan in brackets). The plan-review
source-grounding pass must treat every entry here as newly created by this phase, not as drift.

Fixtures and test modules:
- `tests/fixtures/captioned_table_propagated_target_render_gate/conf.py` [42-01]
- `tests/fixtures/captioned_table_propagated_target_render_gate/index.rst` [42-01]
- `tests/test_captioned_table_propagated_target_render_gate.py` [42-01]
- class `TestCaptionedTablePropagatedTargetRenderGate` [42-01]
- `tests/fixtures/figure_propagated_target_render_gate/conf.py` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/index.rst` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/image.png` [42-02]
- `tests/test_figure_propagated_target_render_gate.py` [42-02]
- class `TestFigurePropagatedTargetRenderGate` [42-02]

Production symbol:
- local `was_captioned` in `TypstTranslator.depart_table` (`typsphinx/translator.py`) [42-04]

rST label ids introduced by the new fixtures:
- `tbl-target`, `tbl-name`, `tbl-target-noname`, `tbl-target-li`, `tbl-name-li`, `tbl-target-a`,
  `tbl-target-b`, `tbl-name-two` [42-01]
- `fig-target`, `fig-name`, `fig-target-noname`, `fig-target-li`, `fig-name-li` [42-02]

Caption sentinels:
- `TBLTGTNAMEDSENTINEL`, `TBLTGTNONAMESENTINEL`, `TBLTGTLISTSENTINEL`, `TBLTGTTWOSENTINEL` [42-01]
- `FIGTGTNAMEDSENTINEL`, `FIGTGTNONAMESENTINEL`, `FIGTGTLISTSENTINEL` [42-02]

Evidence and planning artifacts (all under
`.planning/phases/42-captioned-table-drops-preceding-target-label/`):
- `42-GATE-EVIDENCE-01.md` — table-side RED, SC#1 + SC#5 [42-01]
- `42-GATE-EVIDENCE-02.md` — figure-side answer + permanent gate, SC#2 [42-02]
- `42-GATE-EVIDENCE-03.md` — repo-wide misrouting sweep, D-06 + D-07 [42-03]
- `42-GATE-EVIDENCE-04.md` — table-side GREEN + regression sweep, SC#3 [42-04]
- `42-GATE-EVIDENCE-05.md` — caption-less byte invariance, SC#4 + D-04 [42-05]
- `42-SC4-INVARIANTS.md` — Phase 41 SC#4 sweep re-measured over a range including Phase 42, with the
  change-site to RED manifest, SC#6 [42-06]
- `42-CLOSEOUT-GUARD.md` — REL-04/REL-05 checkbox-flip guard, SC#6 [42-06]

Todo:
- `.planning/todos/pending/<YYYY-MM-DD>-table-whitespace-only-title-anchor-divergence.md` — D-08 [42-03]

Modified (not created): `CHANGELOG.md` gains one bullet under `## [0.7.0]` -> `### Fixed` [42-06];
`typsphinx/translator.py` `depart_table` [42-04].
</artifacts_this_phase_produces>

<planner_assumptions>
Flagged planner assumptions for this plan (surfaced, never silently dropped):

- The spec-less edge probe's `empty` row is classified COVERED — the zero/one/many axis of
  propagated ids maps directly onto D-01's own shapes and is written as a plain truth above.
- The spec-less edge probe's `encoding` row is classified BACKSTOP — the only length/equality
  question in TBL-03's blast radius is the whitespace-only-title branch, which CONTEXT.md D-08
  fences OUT of this phase's fix scope (todo only, plan 42-03). Written as a structured
  `{ statement, verification: backstop }` truth above. Neither row is dismissed and neither is
  left unresolved.
- Shape B's `ids[0]` is a docutils auto id whose exact spelling depends on how many auto ids the
  document has already consumed. No assertion may name it. If the fixture is later reordered, the
  shape-B assertion stays valid because it names only the human-authored `tbl-target-noname`.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rST source -> translator | Author-controlled document text (standalone target ids, `:name:` values, captions) crosses into emitted Typst markup. |
| emitted `.typ` -> `typst.compile()` | A malformed label graph (dangling or duplicated) aborts the entire document build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Denial of Service | `depart_table` label emission (`typsphinx/translator.py`) | medium | mitigate | Build-time DoS: a dangling label aborts the whole compile. This plan makes the failure reproducible and permanently gated by `tests/test_captioned_table_propagated_target_render_gate.py`; the fix itself lands in plan 42-04. |
| T-42-02 | Denial of Service | same call site | medium | mitigate | The duplicate-label fatal (Phase 25 Critical Pitfall 3) must not be traded in. Mitigated by the `test_no_duplicate_label_definition` assertion authored in this plan (D-03) and by keeping `skip_ids={ids[0]}` unchanged (D-02). |
| T-42-03 | Tampering | author-controlled ids embedded in emitted Typst | low | accept | Ids already route through `_sanitize_label` (bug #10) and `_namespace_label`. This plan touches no production code at all, and plan 42-04 moves a call site without changing either helper (D-05). Canon input-validation control already in place. |
| T-42-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package. 42-RESEARCH.md's Package Legitimacy Audit records "not applicable" — no `[ASSUMED]`/`[SUS]`/`[SLOP]` entry exists, so no legitimacy checkpoint is required. `git diff -- pyproject.toml uv.lock` must stay empty across this phase (milestone invariant #1), verified in plan 42-06's invariant sweep. |
</threat_model>

<verification>
- `uv run pytest tests/test_captioned_table_propagated_target_render_gate.py -q` exits non-zero with the `does not exist in the document` signature (RED, recorded).
- `git status --porcelain typsphinx/` empty at the RED-recording commit.
- `uv run pytest tests/test_pdf_render_gate.py -q` passes — the pre-existing TBL-01/TBL-02 gate is uncontaminated.
- `uv run black --check .` and `uv run ruff check .` exit 0.
- `42-GATE-EVIDENCE-01.md` contains the verbatim fatal, the recording SHA, and the per-shape ids table.
</verification>

<success_criteria>
- SC#1 discharged: a minimal rST reproduces the failure, the Typst error text is captured verbatim, and the observed `node["ids"]`/`node["names"]` at `depart_table` are recorded for all four D-01 shapes.
- SC#5 discharged for the RED half: the fixture and module are committed while the bug still exists, with `git status --porcelain typsphinx/` empty at that commit, and the verbatim failure output is recorded.
- D-01 satisfied: all four measured-failing shapes plus the caption-less control live in one dedicated fixture.
- D-03's structural assertions exist in the module BEFORE any fix lands.
- No production source, and no pre-existing fixture or test module, is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/42-captioned-table-drops-preceding-target-label/42-01-SUMMARY.md` when done.
</output>
