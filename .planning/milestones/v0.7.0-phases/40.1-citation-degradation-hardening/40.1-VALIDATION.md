---
phase: 40.1
slug: citation-degradation-hardening
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-02
---

# Phase 40.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `40.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.4+ (project pin, `pyproject.toml` `[tool.pytest.ini_options]`) |
| **Config file** | `pyproject.toml` — `testpaths = ["tests"]`, markers `slow`/`integration`, `filterwarnings = ["error::DeprecationWarning", "error::PendingDeprecationWarning"]` |
| **Quick run command** | `uv run pytest tests/test_citation_degradation_gate.py -x` (new module, D-02) |
| **Full suite command** | `uv run pytest -m "not slow"` (fast tier) |
| **Slow / corpus gate** | `uv run pytest tests/test_corpus_gate.py -m slow` — D-06's non-regression evidence; must be **observed green at least once**, a skip does not satisfy it |
| **Estimated runtime** | fast tier ~60–120s; corpus gate is the long pole |

**Worktree note (CLAUDE.md § Worktree-isolated execution):** every command above runs through `uv run`,
after `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` in the worktree. This is
mandatory, not conditional — without it pytest imports the unchanged main-tree package and gates stay
RED after a correct fix.

---

## Sampling Rate

- **After every task commit:** `uv run pytest tests/test_citation_degradation_gate.py -x` **plus**
  `uv run pytest tests/test_citation_render_gate.py -x` (the frozen SC#5 control — cheap enough to run
  every commit, and it is the thing SC#5 measures)
- **After every plan wave:** `uv run pytest -m "not slow"`, plus `black --check .`, `ruff check .`,
  `mypy typsphinx/`
- **Before `/gsd-verify-work`:** full suite green (`uv run pytest`); corpus gate
  (`tests/test_corpus_gate.py -m slow`) observed green at least once; Phase 40's citation fixture
  `.typ` confirmed byte-identical to its pre-phase output (SC#5)
- **Max feedback latency:** ~30s for the per-commit pair

---

## Per-Task Verification Map

Task IDs assigned by the plan split (2026-08-02). D-03's warning→plan mapping, recorded explicitly in
every `40.1-*-PLAN.md`: `40.1-01` → WR-01 → `40.1-GATE-EVIDENCE-01.md`; `40.1-02` → WR-02 →
`40.1-GATE-EVIDENCE-02.md`; `40.1-03` → WR-03 → `40.1-GATE-EVIDENCE-03.md`; `40.1-04` covers no
warning and lands no source change, so it produces `40.1-NONREGRESSION.md` rather than a
`GATE-EVIDENCE-04` file (Phase 40's own precedent). Each warning lands in exactly one evidence file.

Every `RED` row's own task is the one that *records* the RED; the paired `GREEN` task lands the fix
under the same pytest selector, so the two map one-to-one without re-running anything.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| `40.1-01-T2` (RED) / `40.1-01-T3` (GREEN) | `40.1-01` | 1 | WR-01 | T-40.1-01 (continues T-40-03) | A backref whose citing `nodes.reference` cannot be located emits no `link()` to an unattached label; the resulting D-03 marker-shape changes (3→2 renumbered, 2→1, 1→0) are asserted | real-build RED→GREEN (`only:: never` fixture) + `typst.compile()` | `uv run pytest tests/test_citation_degradation_gate.py -k wr01 -x` | ✅ W0 (created by `40.1-01-T1`/`T2`) | ✅ green — `5 passed, 3 deselected` |
| `40.1-02-T1` (RED) / `40.1-02-T2` (GREEN) | `40.1-02` | 2 | WR-02 | T-40.1-03 (structural integrity) | An ids-less `nodes.target` between two citation definitions does not break the run; an ids-carrying target still does | structural `.count("grid(")` on the rendered body, directly-assembled doctree | `uv run pytest tests/test_citation_degradation_gate.py -k wr02 -x` | ✅ W0 (harness created by `40.1-02-T1`) | ✅ green — `3 passed, 5 deselected` |
| `40.1-03-T1` (RED) / `40.1-03-T2` (GREEN) | `40.1-03` | 3 | WR-03 | T-40.1-02 (continues T-40-03), T-40.1-06 | The D-14 eligibility judgement AND the anchor label are derived once; anchor-emitter and anchor-consumer cannot disagree; the degrade warning still fires exactly once | real-compile RED→GREEN, assembled doctree + StubBuilder, plus two invariant backstops added with the fix | `uv run pytest tests/test_citation_degradation_gate.py -k wr03 -x` | ✅ W0 (extended by `40.1-03-T1`) | ✅ green — `8 passed, 8 deselected` |
| `40.1-04-T1` | `40.1-04` | 4 | D-06 non-regression | T-40.1-04 | The predicate rewrite changes no byte of any non-citation reference across the whole Sphinx `doc/` corpus | full-corpus regression, **observed** green — a skip is recorded NOT SATISFIED | `uv run pytest tests/test_corpus_gate.py -m slow` | ✅ exists | ✅ green — `test_corpus_compiles_with_no_fatal_error PASSED` (1 passed, 1 skipped [unrelated env-gate], 3 deselected; `40.1-NONREGRESSION.md` §2.5) — OBSERVED, not skipped |
| `40.1-04-T1` | `40.1-04` | 4 | SC#5 control | T-40.1-04, T-40.1-07 | Phase 40's own citation fixture output is byte-identical, sha256-compared against the baseline `40.1-01-T1` captured before any translator byte changed | existing render gate, **unmodified** (D-02), plus an sha256 pair | `uv run pytest tests/test_citation_render_gate.py -x` | ✅ exists | ✅ green — `9 passed`; sha256 of `index.typ`/`second.typ` IDENTICAL to pre-phase baseline, independently re-derived (`40.1-NONREGRESSION.md` §3) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] **`40.1-01-T1`** — `tests/fixtures/citation_degradation_gate/` (D-02): `conf.py` + `index.rst`
      carrying WR-01's real-build `only:: never` shape, extended to three citations so all three D-03
      marker-shape transitions (3→2, 2→1, 1→0) are exercised — confirmed present on disk (`40.1-04`
      Task 1)
- [x] **`40.1-01-T2`** — `tests/test_citation_degradation_gate.py` (D-02): the new module plus its
      real-build half (`_run_sphinx_build_typstpdf`, `DegradationGateBuild`, `degradation_gate_build`,
      `_require_typ`, `_attached_anchor_tokens`, `_link_label_targets`, `_slice`,
      `_expected_namespace_label`) — confirmed present, 16 tests collected and green (`40.1-NONREGRESSION.md` §2.2)
- [x] **`40.1-02-T1`** — `_StubBuilder` and the assembled-doctree harness (`_make_document`,
      `_make_citation`, `_render_body`, `_compile_body`) inside the new module, for WR-02 / WR-03.
      Not reusable from `tests/test_citation_render_gate.py`: D-02 forbids editing that module, and
      its fixtures are real-build rather than stub-builder based — confirmed present and exercised by
      the WR-02/WR-03 selectors, both green
- [x] **`40.1-03-T1`** — `_make_citing_reference`, and (`40.1-03-T2`) `_render_with_translator` for
      the two invariant backstops — confirmed present and exercised, WR-03's 8 tests green
- [x] **`40.1-01-T1`** — SC#5 pre-phase sha256 baseline for `tests/fixtures/citation_render_gate/`'s
      emitted `index.typ` / `second.typ`, captured before any translator byte changes; re-derived and
      compared by `40.1-03-T2` and `40.1-04-T1` — `40.1-04` independently re-derived both hashes
      against the final merged tree: IDENTICAL to the pre-phase baseline (`40.1-NONREGRESSION.md` §3)
- [x] **`40.1-01-T2` / `40.1-02-T1` / `40.1-03-T1`** — the three `40.1-GATE-EVIDENCE-0N.md` files
      (D-03), one per plan, each carrying its warning's D-01 attempt list, provenance verdict and
      verbatim RED. `40.1-04` covers no warning and lands no source change, so it produces
      `40.1-NONREGRESSION.md` instead of a fourth GATE-EVIDENCE file — all four files confirmed to
      exist and the D-03 one-to-one mapping mechanically re-confirmed (`40.1-NONREGRESSION.md` §5)

*Existing infrastructure covers the two non-regression rows: `tests/test_corpus_gate.py` and
`tests/test_citation_render_gate.py` are re-run, never edited.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions | Outcome |
|----------|-------------|------------|-------------------|---------|
| D-01/D-08 provenance record | SC#4 | The *enumeration of attempts* is a written record, not an assertion — a test can prove a topology reproduces, it cannot prove the attempt list is complete | Each GATE-EVIDENCE file must list, per warning, every RST shape attempted, what the resolved doctree actually produced, and the resulting provenance verdict (real `sphinx-build` vs. assembled doctree) with its reason | **Confirmed for all three files.** `40.1-GATE-EVIDENCE-01.md` §4 records WR-01's attempt list (1 succeeded on first try, 2 more recorded as not-run with reasons) and §6 the verbatim classic-fatal RED. `40.1-GATE-EVIDENCE-02.md` §2 records WR-02's full five-attempt enumeration (all five verified not to produce the minimal topology) and §3 the verbatim structural RED. `40.1-GATE-EVIDENCE-03.md` §2 records WR-03's two-route attempt list (Route A a control-flow fact, Route B attempted against a real two-document build and found unreachable) and §3 the verbatim classic-fatal RED. All three carry an explicit provenance verdict with its reason. See `40.1-NONREGRESSION.md` §4 (the cross-phase manifest) and §5 (the mechanical one-to-one mapping check). |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s for the per-commit pair
- [ ] Corpus gate observed green (not skipped) at least once
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
