# Phase 40.1: Citation Degradation Hardening - Context

**Gathered:** 2026-08-02
**Status:** Ready for planning

<domain>
## Phase Boundary

Close the three graceful-degradation gaps `40-REVIEW.md` recorded in Phase 40's citation code
(WR-01, WR-02, WR-03), each under milestone invariant #4's RED-before-fix discipline, so a
citing-site topology Phase 40's fixture never constructs degrades quietly instead of emitting a
`link()` to a label nothing ever attaches — a Typst compile **fatal**, not a cosmetic defect.

**In scope:**

- `typsphinx/translator.py` only: `visit_citation`'s backref loop (2856-2862), `_citation_run_neighbour`
  (2644-2683), `_citing_reference_has_own_anchor` (2712-2742), and `visit_reference`'s D-14 guard
  (4210-4231, plus the `opens_wrapper` derivation at 4160-4191 which D-06 pulls into the new shared
  predicate).
- A new degradation test module and its own fixture directory, with the RED for each warning recorded
  before the fix lands, and the provenance of each RED (real `sphinx-build` vs. a directly-assembled
  doctree) established and written down per warning.
- Non-regression evidence: Phase 40's citation render-gate module, the full suite, the lint/type trio,
  the full-corpus `-b typstpdf` gate, and the byte-identity control on Phase 40's own citation fixture.

**Out of scope:**

- **IN-01** — `_find_citing_reference`'s full-document linear scan (O(citations × backrefs ×
  references)). Recorded as Info in `40-REVIEW.md`, explicitly no action.
- New requirement IDs. This phase hardens code CIT-01 / CIT-03 / CIT-04 already delivered; no
  requirement is added, removed, or reworded.
- Any Phase 40 locked decision (D-01..D-14 in `40-CONTEXT.md`). D-03's marker shapes, D-05's
  one-grid-per-run rule, D-07's uncited-entry rendering, and D-13's single label derivation point all
  stand — this phase makes the code apply them consistently, it does not revisit them.
- Phase 40's fixture directory and render-gate module. Frozen by D-02 below; they are SC#5's control.

</domain>

<decisions>
## Implementation Decisions

### RED provenance and placement

- **D-01: "A real `sphinx-build` cannot construct this topology" is established by enumerating the attempts, not by proving impossibility.** Per warning, build several RST shapes for real, measure each resolved doctree (`env.get_and_resolve_doctree`), and record what each one produced and why the target topology did not appear. Only after that list exists does the RED fall back to a directly-assembled doctree. Rejected: reading the docutils/Sphinx transform chain to prove the topology cannot be generated — high cost, and it bakes in a premise about today's Sphinx that WR-03 is precisely about not depending on. Also rejected: a single attempt then substitute — cheapest, but if the topology later turns out to be buildable, the recorded claim "not reproducible in a real build" is itself wrong.

- **D-02: Tests go in a new module with its own fixture directory, and Phase 40's are not touched.** `tests/test_citation_degradation_gate.py` plus `tests/fixtures/citation_degradation_gate/`. Rationale: SC#5 uses the byte-identity of Phase 40's fixture output as the control proving the change is confined to the degradation paths, and the cheapest way to keep that control honest is to not edit the thing being measured. Accepted cost: citation assertions now live in two modules (`tests/test_citation_render_gate.py` is 1,113 lines / 3 classes today), and any helper the new module wants from it must be duplicated or imported deliberately. Rejected: adding classes to the existing module, and splitting by evidence kind across the gate module and `tests/test_translator.py`.

- **D-03: RED evidence is recorded per plan, numbered the way Phase 40 numbered it.** `40.1-GATE-EVIDENCE-01.md`, `-02.md`, … one per plan, same convention as `40-GATE-EVIDENCE-01.md` (662 lines) and `-02.md`. Each file records, for every warning it covers, the enumerated RST attempts from D-01, the resulting provenance verdict, and the verbatim RED output. Rejected: one file with a section per warning (would make concurrent plans write to the same file under worktree-isolated execution). Consequence for planning: whoever splits this phase into plans must make the warning-to-plan mapping explicit, because the evidence filenames follow the plans, not the warnings.

- **D-04: The RED chases the real fatal wherever the fatal is reachable.** WR-01 and WR-03 both terminate in a `link()` to a label nothing attaches, so their RED is the classic `typst.compile()` failure with the verbatim error captured — reclaiming, for these two, the classic RED that the v0.7.0 GATE-01 amendment otherwise reserves to Phase 40 / CIT-01. This holds even when the topology comes from a directly-assembled doctree: the translator still produces a `.typ` and that `.typ` can still be compiled. WR-02 compiles cleanly by construction (it is a silent misalignment), so its RED is a structural assertion on the emitted `.typ` — the count of `grid(` openings across the citation run. Rejected: one structural form for all three, and layering both forms on every warning.

### WR-03 — removing the duplicated D-14 eligibility judgement

- **D-05: The D-14 judgement is extracted into one shared predicate that both call sites consult.** `visit_reference` (translator.py:4226) currently decides with `node.get("ids") and opens_wrapper and not next_is_target`; `_citing_reference_has_own_anchor` (2712-2742) re-derives the same answer from the `next_is_target` half alone. After this phase there is exactly one place where that condition is written, so the two cannot drift. Rejected: having the callee also check the remaining two conditions (the review's own second suggestion) — smaller, and it leaves `visit_reference`'s hot path untouched, but the condition still exists in two places and only a test would hold them together. Also rejected: recording the anchors actually emitted in a set and having `visit_citation` consult it — the strongest form in principle ("fact" rather than "matching judgement"), but it is visit-order dependent, and a forward reference (definition before citing site, a shape Phase 40's own probe carries) would query the set before the anchor was recorded.

- **D-06: The predicate derives its own inputs — it takes the node, not three booleans.** `refuri`, `refid`, `degrade_xref_to_text` and `next_is_target` are all derived inside it, and `visit_reference` uses the predicate's answer instead of its own local `opens_wrapper` computation (4191). Both the judgement and the derivation of its inputs then exist in exactly one place. Note `degrade_xref_to_text` reads `builder.master_included_docnames` (4162-4165), so the predicate is not a pure function of the node — it needs builder state, the same way `visit_reference` does today. **Consequence, stated at decision time: this rewrites a hot path taken by every link in the codebase, so D-14's non-regression obligation must be re-discharged for this phase — byte-identical emission for every reference that is not a citation target, with the full-corpus `-b typstpdf` gate as the evidence.** Rejected: a pure predicate taking three booleans (judgement in one place, derivation still in two — the drift just moves upstream). Also rejected: leaving the builder-dependent half out of the predicate, which would take back the one route by which `opens_wrapper` provably goes false today.

- **D-07: The predicate returns the anchor label too, not just a boolean.** Measured this session: the label string is also built independently at two sites — `_namespace_label(self._current_docname(), node["ids"][0])` in `visit_reference` (4227-4229) and `_namespace_label(docname, refid)` in `visit_citation` (2862). For a same-document backref the two coincide (D-08/D-13 explain why), but nothing in the code enforces it. Returning "if it anchors, this is the label" makes the link target and the attached anchor come out of one expression, closing a second drift source of the same class as WR-01/WR-03 in the same change. It is also the natural extension of Phase 40's D-13 (every emitted label has a single derivation point). Rejected: boolean only, on the argument that `_namespace_label`/`_sanitize_label` are already the single derivation point so equal arguments suffice — true, but "equal arguments" is exactly the unenforced invariant WR-03 is about. Also rejected: a separate label helper alongside the predicate (cleaner separation, two new helpers instead of one).

- **D-08: WR-03's RED measures both condition-breaking routes and takes whichever the real build can construct.** The two routes are `opens_wrapper` false (the `degrade_xref_to_text` path — an xref to a document not in `master_included_docnames`, which the review notes already proves `opens_wrapper` can be false for some references) and `ids` empty (unlikely under today's citation domain, which always populates `ids`, so it leans toward the assembled-doctree fallback). Both are attempted in a real build; the one that works becomes the RED and the other is written into D-01's attempt list. This is the same posture as D-01 rather than a second policy. Rejected: committing up front to either route alone.

### Claude's Discretion

The owner reviewed and declined to pin these two — they are the planner's/executor's call, within the
constraints recorded above:

- **Whether a skipped back-reference emits a Sphinx warning or degrades silently.** The nearest
  precedent is the footnote dangling-target guard, which warns and then skips
  (`40-CONTEXT.md` § Reusable Assets). Also unpinned, and load-bearing for the assertions: WR-01's fix
  makes filtering strictly more aggressive, and because `visit_citation` enumerates the **filtered**
  list, dropping a backref can change the marker's *shape* under D-03 — two backrefs falling to one
  turns `[Label] (1,2)` into a linked label with no parenthesised markers, and one falling to zero
  turns it into a plain non-linked label (D-07 of Phase 40). Whichever way this goes, the emitted shape
  for a degraded citation must be asserted, not left to chance.
- **How wide WR-02's fix is** — adding only the ids-less `nodes.target` case the review proposed, or
  generalising "a sibling that emits nothing" into a predicate so the same class of omission cannot
  recur. Measured caveat for either route: an ids-less `target` emits nothing into the body only
  outside list items — `visit_target` (3524-3557) still writes one newline and sets
  `list_item_needs_separator` when `self.in_list_item` is true, so "emits nothing" is not literally
  zero in every context.

Also at discretion, as usual: the shared predicate's name and signature, whether
`_citing_reference_has_own_anchor` survives as a thin delegator or is deleted at its call site, and
the exact shape of the structural `grid(` count assertion for WR-02.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The defects this phase closes
- `.planning/phases/40-citations-full-round-trip/40-REVIEW.md` — WR-01 (§ lines 60-111), WR-02
  (113-152), WR-03 (154-190), and IN-01 (192-203, explicitly out of scope). Each warning carries the
  reviewer's own proposed fix and the reason it was filed as WARNING rather than BLOCKER: none was
  reproduced against a real `sphinx-build`. That sentence is the origin of SC#4 and of D-01/D-08.
- `.planning/ROADMAP.md` § "Phase 40.1: Citation Degradation Hardening (INSERTED)" — the five success
  criteria. SC#4 fixes the provenance procedure; SC#5 fixes the byte-identity control that D-02 protects.

### Phase 40's locked decisions this phase must not re-open
- `.planning/phases/40-citations-full-round-trip/40-CONTEXT.md` — D-03 (label shapes by backref count),
  D-05/D-06 (one grid per run; a non-citation sibling breaks the run), D-07 (an uncited entry still
  renders with a plain label — the deliberate inverse of the footnote precedent), D-13 (every label
  namespaced by the citation's own `docname` through `_namespace_label`), D-14 (anchoring the citing
  site must not change the bytes of any non-citation reference). D-14's non-regression obligation is
  re-incurred by D-06 above.
- `.planning/milestones/v0.6.0-phases/14-footnotes-doctree-pre-pass/14-RESEARCH.md` — Pitfall 1:
  linking to a label that was never attached is a fatal compile abort, not a cosmetic issue. This is
  the hazard class WR-01 and WR-03 both sit in.

### Cross-phase constraint
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` D-11 — why these three
  were routed here instead of into the release-prep phase, and the two consequences: Phase 41's SC#4
  invariant sweep must cover this phase's handler changes, so **Phase 40.1 executes before Phase 41**.
- `.planning/STATE.md` § "Current Position" — the same execution-order note, plus the standing GATE-01
  bar and the v0.7.0 amendment to what "red" means (D-04 above reclaims the classic form for WR-01/WR-03).

### Project conventions
- `CLAUDE.md` § "Worktree-isolated execution" — the standing execution mode. Per-worktree
  `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and running everything through
  `uv run` are mandatory, not conditional.
- `CLAUDE.md` § "The `@preview` version-sync hazard" — this phase adds no package and moves no pin.

### Existing test surfaces this phase must keep green (and not edit)
- `tests/test_citation_render_gate.py` — Phase 40's render gate, 1,113 lines, three classes
  (`TestCitationRenderGateStructural`, `…RealCompile`, `…CompiledPdf`). Frozen by D-02.
- `tests/fixtures/citation_render_gate/` — `conf.py`, `index.rst`, `second.rst`. The `.typ` these
  produce is SC#5's byte-identity control.

</canonical_refs>

<code_context>
## Existing Code Insights

All line numbers measured against `typsphinx/translator.py` at this session's HEAD (2026-08-02).

### The three defect sites

- **WR-01 — `visit_citation`, 2856-2862.** The loop reads
  `if ref_node is not None and not self._citing_reference_has_own_anchor(ref_node): continue`, so a
  `None` from `_find_citing_reference` short-circuits the condition to `False`, skips the `continue`,
  and appends the namespaced label anyway. The reviewer's one-line fix is
  `if ref_node is None or not self._citing_reference_has_own_anchor(ref_node):`.
- **WR-02 — `_citation_run_neighbour`, 2644-2683.** Skips only `nodes.comment` and
  `nodes.system_message` (2679) when scanning for the next emitting sibling.
- **WR-03 — `visit_reference` 4226 vs `_citing_reference_has_own_anchor` 2712-2742.** Three conditions
  on one side, one on the other.

### Facts measured this session that the planner needs

- **An ids-less `nodes.target` is not unconditionally silent.** `visit_target`'s "original behavior"
  branch (3524-3557) writes the anchor only inside `if node.get("ids"):` (3548) — but *before* that it
  writes `"\n"` when `self.in_list_item and self.list_item_needs_separator` (3526-3527), and *after* it
  sets `self.list_item_needs_separator = True` when in a list item (3553-3554). So the "emits nothing"
  premise WR-02's fix rests on holds outside list items and is approximate inside them.
- **`opens_wrapper` is `bool(refuri or refid) and not degrade_xref_to_text` (4191)**, and
  `degrade_xref_to_text` is set at 4162-4165 from `builder.master_included_docnames` — a document not
  reachable from any master document degrades its xref to plain text. This is the one route by which
  `opens_wrapper` provably goes false today, and therefore D-08's first RED candidate for WR-03.
- **The D-14 anchor and the back-reference target are two independent `_namespace_label` calls** —
  4227-4229 (`_current_docname()`, `node["ids"][0]`) and 2862 (`docname`, `refid`). D-07 collapses them.

### Reusable assets

- `_namespace_label` / `_sanitize_label` (3576 / 3519) — the single derivation point every anchor and
  every link already shares. The new predicate's label half (D-07) routes through them, it does not
  replace them.
- `visit_footnote_reference` (2527) — the dangling-target guard that warns and then skips, i.e. the
  precedent for the degradation policy left at Claude's discretion above.
- `tests/test_citation_render_gate.py`'s helpers — `_attached_anchor_tokens` (232),
  `_definition_anchor_tokens` (259), `_own_id_anchor_tokens` (274), `_grid_span` (317),
  `_link_rect_x0_values` (473). Read them for the established assertion idioms; D-02 means the new
  module cannot edit them in place.

### Integration points

- `visit_reference` is on the hot path for every link in the codebase (toctree entries, `:ref:`,
  external URLs, figure targets). D-06 rewrites its eligibility computation, so the full-corpus
  `-b typstpdf` gate (`tests/test_corpus_gate.py`) is the load-bearing non-regression evidence, not a
  formality.
- Phase 41's SC#4 sweep reads this phase's GATE-EVIDENCE files (D-03) to prove every node-handler
  change in the milestone carries a recorded RED.

</code_context>

<specifics>
## Specific Ideas

- The owner interrupted the first question to ask for a plain explanation of WR-01/WR-02/WR-03 before
  choosing anything, and only then answered. The standing preference this confirms: **when a question
  rests on a defect the owner has not read the code for, explain the defect first — mechanism, what
  breaks, and how bad — and ask afterwards.** Do not treat a review's warning IDs as shared vocabulary.
- Both of the owner's selected areas were the ones with a *structural* answer (where evidence lives,
  where the judgement lives). The two areas left at discretion were the ones about degree (how loud,
  how wide). Consistent with Phase 40, where the owner picked the shapes and left the gutters to Claude.
- On WR-03 the owner took the strongest available form at every step — shared predicate, derivation
  inside it, label included — accepting in exchange a hot-path rewrite and a re-run of D-14's
  byte-identity proof. Read that as: for a defect class that is "two places silently disagree", the
  owner wants the disagreement made structurally impossible, not merely tested.

</specifics>

<deferred>
## Deferred Ideas

- **IN-01 — `_find_citing_reference`'s linear scan.** Recorded in `40-REVIEW.md` as Info with "no
  action required for this phase"; kept out of this one too. Becomes worth revisiting only if a
  citation-heavy corpus makes build time visible.
- **Generalising "a sibling that emits nothing" beyond citations.** If WR-02's fix ends up as a general
  predicate (Claude's discretion above), the same question exists for other run-adjacency and
  separator-protocol sites in the translator. Not this phase's scope — this phase touches the citation
  helper only.

### Reviewed Todos (not folded)

The todo matcher returned four candidates; none is folded — this phase is confined to the citation
degradation paths.

- `2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md` (score 0.9) — a Phase 37/38 `desc_*`
  seam. Already reviewed-and-declined in `40-CONTEXT.md` for the same reason; folding it would reopen
  Phase 37's completed golden file.
- `2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md` (score 0.9) — a Phase 37
  test-naming defect, unrelated to citations.
- `2026-07-22-add-sphinx-linkcheck-ci-job.md` (score 0.6) — deferred as future requirement LNK-01.
- `2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md` (score 0.6) — builder input hardening,
  unrelated to the translator.

</deferred>

---

*Phase: 40.1-Citation Degradation Hardening*
*Context gathered: 2026-08-02*
