---
phase: 40.1-citation-degradation-hardening
plan: 04
subsystem: translator
tags: [sphinx, typst, citations, non-regression, release-gate]

# Dependency graph
requires:
  - phase: 40.1-citation-degradation-hardening
    provides: "plans 40.1-01/02/03's landed fixes (WR-01 fail-closed backref filter, WR-02 run-neighbour skip-list widening, WR-03 shared _reference_anchor_decision predicate) and their three 40.1-GATE-EVIDENCE-0N.md files"
provides:
  - "40.1-NONREGRESSION.md: verbatim SC#5 gate-set output (frozen render gate, full suite, lint/type trio, full-corpus -b typstpdf gate OBSERVED PASSED not skipped), an independently re-derived SC#5 sha256 byte-identity verdict, and the change-site -> RED manifest Phase 41's SC#4 sweep consumes"
  - "40.1-VALIDATION.md's Per-Task Verification Map fully completed (all Status cells set from measured results, Wave 0 checkboxes ticked, Manual-Only Verifications outcome recorded) without touching its frontmatter status/nyquist_compliant/wave_0_complete keys"
  - "Mechanical (not assumed) confirmation that the D-03 warning->evidence-file mapping is one-to-one across all three GATE-EVIDENCE files"
affects: [41-v0-7-0-release-automation-release-prep]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Corpus-gate honesty check: distinguishing an OBSERVED PASSED (multi-second real build) from an instant pytest.skip by wall-clock time, so a skip is never mistaken for a pass"
    - "D-03 mapping verification via each evidence file's own title/RED-section, not a bare warning-ID grep (bare grep over-counts on legitimate sibling-warning prose mentions)"

key-files:
  created:
    - .planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md
  modified:
    - .planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md

key-decisions:
  - "No re-baselining: the SC#5 sha256 comparison is reported as an observed IDENTICAL verdict because it measured identical, not because a differing result would have been silently reconciled -- the plan's own instruction to report a regression finding if it ever differed was honored by there being nothing to report"
  - "Task split preserved as two atomic commits even though both tasks write into the same 40.1-NONREGRESSION.md file, by writing Task 1's sections first, committing, then appending Task 2's sections in a second commit -- mirroring the RED/evidence-append commit-pair convention plans 40.1-01/02/03 already established"

requirements-completed: [SC-04, SC-05]

coverage:
  - id: D1
    description: "The full-corpus -b typstpdf gate was run and its actual compile test (test_corpus_compiles_with_no_fatal_error) is OBSERVED PASSED -- 13.94s real build via a cache-hit, not an instant skip standing in for a pass"
    requirement: "SC-05"
    verification:
      - kind: unit
        ref: "tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error"
        status: pass
      - kind: manual_procedural
        ref: ".planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md §2.5 (verbatim output, cache-state confirmation, wall-clock reasoning distinguishing a real build from an instant skip)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Phase 40's citation render-gate module (frozen, D-02), the full test suite (slow included), and the lint/type trio are all green against the final merged tree"
    requirement: "SC-05"
    verification:
      - kind: unit
        ref: "tests/test_citation_render_gate.py (9 passed)"
        status: pass
      - kind: unit
        ref: "uv run pytest (799 passed, 1 skipped -- the same intentional Phase 15/SC#3 env-gate documented in 40-NONREGRESSION.md)"
        status: pass
      - kind: manual_procedural
        ref: "uv run black --check . / ruff check . / mypy typsphinx/ -- all exit 0, verbatim in 40.1-NONREGRESSION.md §2.4"
        status: pass
    human_judgment: false
  - id: D3
    description: "The .typ emitted for Phase 40's own citation fixture is byte-identical to its pre-phase sha256 baseline, independently re-derived on the final merged tree rather than transcribed from plan 40.1-03's own re-check"
    requirement: "SC-05"
    verification:
      - kind: manual_procedural
        ref: ".planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md §3 (fresh sha256 rebuild + comparison table, both files IDENTICAL)"
        status: pass
    human_judgment: true
    rationale: "The plan's own requirement is that this hash comparison be independently re-derived by THIS plan, not merely trusted from a prior plan's record -- a human-auditable distinction a passing test alone cannot certify."
  - id: D4
    description: "The change-site -> RED manifest maps every one of the phase's four translator change sites to the GATE-EVIDENCE file holding its RED, with form, provenance, pytest selector, and recording commit -- the cross-phase contract 41-CONTEXT.md D-11 names for Phase 41's SC#4 invariant sweep"
    requirement: "SC-04"
    verification:
      - kind: manual_procedural
        ref: ".planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md §4 (four-row manifest table)"
        status: pass
    human_judgment: true
    rationale: "SC#4's own requirement is that the manifest be a written cross-phase record a later phase's sweep reads, not an assertion a test can prove complete -- 40.1-VALIDATION.md already classifies this class of check (D-01/D-08 provenance) as Manual-Only."
  - id: D5
    description: "The D-03 warning->evidence-file mapping is mechanically re-confirmed one-to-one (each file's own title/RED section, not a bare ID grep which over-counts on legitimate sibling-warning prose) -- no warning's RED is recorded in two files or in none"
    requirement: "SC-04"
    verification:
      - kind: manual_procedural
        ref: ".planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md §5 (bare-grep over-count demonstrated, then the correct title/RED-section check performed)"
        status: pass
    human_judgment: false
  - id: D6
    description: "40.1-VALIDATION.md's Per-Task Verification Map is fully completed -- every Status cell set from a measured result, all Wave 0 checkboxes ticked, the Manual-Only Verifications outcome recorded -- while its frontmatter status/nyquist_compliant/wave_0_complete keys are left untouched for /gsd-validate-phase §6"
    requirement: "SC-04"
    verification:
      - kind: manual_procedural
        ref: ".planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md (Per-Task Verification Map, Wave 0 Requirements, Manual-Only Verifications sections; frontmatter diff shows only body changes)"
        status: pass
    human_judgment: false

duration: 35min
completed: 2026-08-02
status: complete
---

# Phase 40.1 Plan 04: SC#5 Non-Regression Close-Out and Cross-Phase Evidence Manifest Summary

**Ran all four SC#5 gates (Phase 40's frozen citation render gate, the full test suite, the lint/type trio, and the full-corpus `-b typstpdf` gate) against the final merged tree, with the corpus gate OBSERVED PASSED (not skipped), independently re-derived the SC#5 sha256 byte-identity verdict, and wrote the change-site → RED manifest Phase 41's SC#4 invariant sweep reads as a cross-phase contract.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-02T13:25:00Z (approx.)
- **Completed:** 2026-08-02T14:02:18Z
- **Tasks:** 2 completed
- **Files modified:** 2 (1 evidence file created, 1 validation strategy file completed)

## Accomplishments

- Provisioned the worktree environment from scratch (`uv sync --extra dev --extra docs`, then the NixOS `uv`/`ruff` shims — `ruff` required `patchelf --set-interpreter` to the Nix glibc loader since no system-wide `ruff` binary existed in this environment to symlink from directly, matching the class of fix `40.1-03-SUMMARY.md` recorded).
- Ran all seven SC#5-adjacent commands against the final merged tree (commit `6a732c10b6...`) with verbatim output captured: Phase 40's frozen citation render gate (9 passed), this phase's own degradation-gate module (16 passed, all three warnings), the full suite including slow tests (799 passed, 1 skipped — the same intentional Phase 15/SC#3 env-gate, not this phase's own gate), the lint/type trio (all clean), the `@preview` version-sync check (3 passed), and both dependency/production diffstats (`pyproject.toml`/`uv.lock` empty, `typsphinx/` names only `translator.py`).
- Ran the full-corpus `-b typstpdf` gate and confirmed its actual compile test — `test_corpus_compiles_with_no_fatal_error` — is **OBSERVED PASSED**, not a skip: a 13.94-second real build against a cache-hit corpus, distinguishable from an instant skip by wall-clock time alone. This discharges D-06's slow-half non-regression obligation on measured evidence rather than a paper-over.
- Independently re-derived the SC#5 byte-identity comparison by rebuilding Phase 40's frozen fixture fresh on this tree and computing sha256 of both `index.typ`/`second.typ` — **both IDENTICAL** to the pre-phase baseline plan `40.1-01` captured before any translator byte changed. This re-derivation was not transcribed from plan `40.1-03`'s own already-recorded re-check, per this plan's own instruction.
- Mechanically re-confirmed the D-03 warning→evidence-file mapping is one-to-one: a bare `grep -c "WR-0N"` over-counts (e.g. `40.1-GATE-EVIDENCE-03.md` mentions "WR-01" 9 times in prose describing its own baseline), so the correct check reads each file's own title and RED section — confirming WR-01/-02/-03 each land in exactly one file, with zero cross-contamination.
- Wrote the change-site → RED manifest `41-CONTEXT.md` D-11 names as the cross-phase contract Phase 41's SC#4 invariant sweep will read: four rows (`visit_citation`'s backref loop, `_citation_run_neighbour`, `_reference_anchor_decision` + `visit_reference`'s rewiring, and the deletion of `_citing_reference_has_own_anchor`), each carrying its warning, evidence file, RED form, RED provenance with reason, pytest selector, and recording commit.
- Recorded both Claude's-Discretion decisions (G1 silent degradation, G2 narrow WR-02 fix) with their locations and rationale, the three sub-discretion outcomes, and everything deliberately not done in this phase (IN-01, the deferred emit-nothing generalisation, the un-run docs dogfooding builds, the untouched Phase 40 locked decisions).
- Completed `40.1-VALIDATION.md`'s Per-Task Verification Map: every Status cell set from a measured result, all six Wave 0 Requirements checkboxes ticked, and the Manual-Only Verifications row's outcome filled — while leaving the frontmatter `status`/`nyquist_compliant`/`wave_0_complete` keys untouched, per the plan's explicit instruction that those belong to `/gsd-validate-phase` §6.

## Task Commits

Each task was committed atomically. Because both tasks write into the same `40.1-NONREGRESSION.md` file, Task 1's commit carries only its own sections (1–3: the seven SC#5 commands and the byte-identity comparison); Task 2's commit appends its own sections (4–9: the manifest, the mapping confirmation, the discretion record, the deliberately-not-done record) plus the `40.1-VALIDATION.md` completion:

1. **Task 1: Run the SC#5 gate set and the corpus gate, and prove byte-identity** — `083eceb` (docs)
2. **Task 2: Write the cross-phase evidence manifest and complete the validation map** — `3aedc7d` (docs)

## Files Created/Modified

- `.planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md` — the phase's close-out record (D-03 substitution for a fourth GATE-EVIDENCE file, per Phase 40's own precedent): verbatim SC#5 gate-set output, the independently re-derived byte-identity verdict, the change-site → RED manifest, the mechanical D-03 mapping check, the Claude's-Discretion record, and the deliberately-not-done record
- `.planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md` — Per-Task Verification Map completed (all Status cells, Wave 0 checkboxes, Manual-Only Verifications outcome); frontmatter unchanged

## Decisions Made

- **No re-baselining occurred, because none was needed:** both sha256 hashes matched the pre-phase baseline exactly. The plan's instruction to treat a differing hash as a regression finding rather than silently re-baselining was honored by there being nothing to reconcile — recorded explicitly in `40.1-NONREGRESSION.md` §3 rather than left implicit.
- **Task-commit split preserved despite shared file:** rather than writing the whole `40.1-NONREGRESSION.md` in one commit, the file was first written with only Task 1's sections, committed, then Task 2's sections were appended in a second commit alongside `40.1-VALIDATION.md`'s completion — mirroring the RED-commit / evidence-append-commit pairing plans `40.1-01`/`40.1-02`/`40.1-03` each already established, so each task's own acceptance criteria map to a distinct commit.

## Deviations from Plan

None — plan executed exactly as written. The worktree required the standard NixOS `uv`/`ruff` shim provisioning documented in `CLAUDE.md`; this session additionally needed the same `patchelf --set-interpreter` step for `ruff` that `40.1-03-SUMMARY.md` recorded (no system-wide `ruff` binary was reachable via `command -v` to symlink from directly in this environment) — this is the standing per-worktree NixOS setup, not a plan deviation.

## Issues Encountered

None beyond the standard NixOS shim provisioning noted above.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- ROADMAP SC#5 is discharged in full on observed evidence: Phase 40's citation render-gate module, the full suite, the lint/type trio, and the full-corpus `-b typstpdf` gate are all green, and Phase 40's own citation fixture output is proven byte-identical to its pre-phase baseline.
- ROADMAP SC#4 is closed out across all three warnings via the change-site → RED manifest in `40.1-NONREGRESSION.md` §4 — Phase 41's SC#4 invariant sweep has a manifest to read (per `41-CONTEXT.md` D-11), not a pile of GATE-EVIDENCE files to reconstruct from scratch.
- D-06's re-incurred non-regression obligation (both the fast half, discharged by plans `40.1-01`/`40.1-02`/`40.1-03`, and the slow half, discharged here) is fully closed on measured evidence — the full-corpus gate genuinely ran and passed, not skipped.
- No blockers or concerns carried forward. This phase lands no further source change; the next action on this thread is `/gsd-phase` inserting Phase 40.1 into `ROADMAP.md` (already recorded as separate work in `41-CONTEXT.md` D-11) and, eventually, Phase 41's own SC#4 sweep consuming this plan's manifest.

## Self-Check: PASSED

**Created/modified files verified on disk:**
- FOUND: `.planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md`
- FOUND: `.planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md`
- FOUND: `.planning/phases/40.1-citation-degradation-hardening/40.1-04-SUMMARY.md`

**Commits verified in git log:**
- FOUND: `083eceb` (Task 1)
- FOUND: `3aedc7d` (Task 2)

---
*Phase: 40.1-citation-degradation-hardening*
*Completed: 2026-08-02*
