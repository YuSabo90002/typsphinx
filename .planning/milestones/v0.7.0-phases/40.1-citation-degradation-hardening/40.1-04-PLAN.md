---
phase: 40.1-citation-degradation-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 40.1-03
files_modified:
  - .planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md
  - .planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md
autonomous: true
requirements:
  - SC-04
  - SC-05
must_haves:
  truths:
    - "The full-corpus `-b typstpdf` gate was RUN and observed PASSED — a skip is recorded as a non-result and does not close this phase."
    - "The full test suite, the lint/type trio, and Phase 40's frozen citation render gate are all green against the final phase tree."
    - "The `.typ` emitted for Phase 40's own citation fixture is byte-identical to its pre-phase baseline, sha256-compared against the value plan `40.1-01` recorded before any translator byte changed."
    - "Every one of WR-01 / WR-02 / WR-03 maps to exactly one GATE-EVIDENCE file, and every translator change site in this phase maps to a recorded RED — the manifest Phase 41's SC#4 invariant sweep consumes."
    - "No `@preview` package was added and no pin moved across the three declaration sites; no runtime or dev dependency was added."
  artifacts:
    - .planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md
    - .planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md
  key_links:
    - "plan `40.1-01`'s recorded pre-phase sha256 → this plan's post-phase rebuild → SC#5's byte-identity verdict"
    - "`40.1-GATE-EVIDENCE-01/02/03.md` → `40.1-NONREGRESSION.md`'s change-site → RED manifest → Phase 41 SC#4's invariant sweep"
    - "`tests/test_corpus_gate.py -m slow` → the D-06 hot-path non-regression obligation re-incurred by the predicate extraction"
---

<objective>
Discharge ROADMAP **SC#5** and the slow half of D-06's re-incurred non-regression obligation, and
leave behind the cross-phase manifest Phase 41 consumes.

This plan lands **no source change**. Its whole job is measurement and recording:

- Run the four things SC#5 names — Phase 40's citation render-gate module, the full suite, the
  lint/type trio, and the full-corpus `-b typstpdf` gate — and capture the verbatim outputs.
- Prove the `.typ` emitted for Phase 40's own citation fixture is **byte-identical** to its
  pre-phase baseline. `40-REVIEW.md` measured that this fixture reaches none of the three warning
  branches, so byte-identity there is the control proving the change is confined to the degradation
  paths. Plan `40.1-01` captured the baseline sha256 before any translator byte changed, precisely so
  this comparison is honest.
- Assemble `40.1-NONREGRESSION.md` — the phase's close-out record and, per `41-CONTEXT.md` D-11, a
  **cross-phase contract**: Phase 41's SC#4 invariant sweep reads this phase's GATE-EVIDENCE files to
  prove every node-handler change in the milestone carries a recorded RED. The manifest must therefore
  map every translator change site to the evidence file holding its RED, not just assert that REDs
  exist.

Per **D-03** this plan covers no warning and lands no source change, so it produces a non-regression
record rather than a `GATE-EVIDENCE-04` file — following Phase 40's own precedent
(`40-NONREGRESSION.md`). That substitution is a recorded planning decision, not an omission.

**The corpus gate must be observed green.** `40.1-VALIDATION.md` and `40.1-RESEARCH.md` both state
it: the gate `pytest.skip`s gracefully when the Sphinx corpus cannot be cloned, and a skip does
**not** satisfy this phase's non-regression obligation. If it skips, that is a non-result to report,
not a pass to record.

Purpose: prove the hot-path rewrite changed nothing it was not meant to change, and leave Phase 41 a
manifest it can sweep rather than a pile it must reconstruct.
Output: `40.1-NONREGRESSION.md` and a completed Per-Task Verification Map in `40.1-VALIDATION.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-citation-degradation-hardening/40.1-CONTEXT.md
@.planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md
@.planning/phases/40.1-citation-degradation-hardening/40.1-01-SUMMARY.md
@.planning/phases/40.1-citation-degradation-hardening/40.1-02-SUMMARY.md
@.planning/phases/40.1-citation-degradation-hardening/40.1-03-SUMMARY.md
@.planning/phases/40-citations-full-round-trip/40-NONREGRESSION.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md
@CLAUDE.md
</context>

---

## Warning → plan mapping (D-03, MANDATORY — read before starting)

| Plan | Warning(s) covered | Evidence file | RED form (D-04) | RED provenance (D-01) |
|------|--------------------|---------------|-----------------|-----------------------|
| `40.1-01` | **WR-01** | `40.1-GATE-EVIDENCE-01.md` | classic `typst.compile()` fatal | **real `sphinx-build`** (`.. only:: never` shape) |
| `40.1-02` | **WR-02** | `40.1-GATE-EVIDENCE-02.md` | structural `count("grid(")` | directly-assembled doctree (real build exhausted) |
| `40.1-03` | **WR-03** | `40.1-GATE-EVIDENCE-03.md` | classic `typst.compile()` fatal | directly-assembled doctree + StubBuilder (both D-08 routes exhausted) |
| `40.1-04` | *(none — lands no source change)* | `40.1-NONREGRESSION.md` | n/a | n/a |

Every one of WR-01 / WR-02 / WR-03 lands in **exactly one** plan's evidence file. Task 2 below
re-asserts this mapping mechanically rather than trusting it.

---

<assumption_delta_decision>
**Detector result:** `{"detected": true, "signals": [{"kind": "pluralization", "term": "second", "snippet": "second route."}]}`

**Literal trigger is a prose false positive** — the matched text is ROADMAP SC#3's phrase
*"WR-01's dangling-label hazard by a **second route**"*, describing a second way of reaching the same
bug, not a second instance of a modeled noun.

**Real decision recorded for the phase:**

- **Noun that becomes primary:** *the D-14 citing-site anchor decision* — "does this
  `nodes.reference` get its own bracket-attached anchor, and if so what is that anchor's label?"
- **Decision: `promote`** (landed in plan `40.1-03`). `TypstTranslator._reference_anchor_decision`
  becomes the single general representation; `_citing_reference_has_own_anchor` is deleted and
  `visit_reference`'s local derivation is replaced by the predicate's return values.
- **No `add-alongside` anywhere in this phase**, so no accepted debt of that kind is carried.
- **Invariant test is a backstop, not a substitute** — the refactor landed first (structural
  impossibility, per CONTEXT.md `<specifics>`), with
  `test_wr03_eligibility_decision_and_emitted_anchor_agree` and
  `test_wr03_xref_resolution_happens_once_per_reference` as guards that go red if a future phase
  reintroduces a disagreeing second derivation site.

**This plan's own delta:** `no-change` — it lands no source and introduces no representation.
</assumption_delta_decision>

---

## Spec-less probe fallback — VISIBLE SKIP (recorded, not silent)

**⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip, not
silent).** No `-SPEC.md` exists (`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`) and this phase declares no
requirement IDs — CIT-01 / CIT-03 / CIT-04 are Phase 40 requirements already Complete and are neither
re-opened nor re-claimed. With no requirement rows there is nothing to probe; no probe-derived edge
or prohibition predicates were generated and no requirement rows were fabricated to feed it.
`must_haves` are derived from the ROADMAP's five success criteria and CONTEXT.md's D-01..D-08.

---

## Artifacts this phase produces

Every symbol below is **new in phase 40.1**. Drift verification must not flag these as
pre-existing-but-missing.

**Production (`typsphinx/translator.py`, plan `40.1-03`):**

- `_ReferenceAnchorDecision` — module-level private `NamedTuple` with fields
  `refuri: str`, `refid: str`, `xref: Tuple[str, str] | None`, `degrade_xref_to_text: bool`,
  `opens_wrapper: bool`, `next_is_target: bool`, `eligible: bool`, `anchor_label: str | None`.
- `TypstTranslator._reference_anchor_decision(self, node: nodes.reference) -> _ReferenceAnchorDecision`
  — the single shared D-14 eligibility predicate (D-05/D-06/D-07). Silent: never logs.
- **Deleted** by the same plan: `TypstTranslator._citing_reference_has_own_anchor`.

**Tests (`tests/test_citation_degradation_gate.py`, new module, D-02):**
`_run_sphinx_build_typstpdf`, `DegradationGateBuild`, `degradation_gate_dir`,
`degradation_gate_build`, `_require_typ`, `_attached_anchor_tokens`, `_link_label_targets`, `_slice`,
`_expected_namespace_label`, `_StubBuilder`, `_make_document`, `_make_citation`, `_make_citing_reference`,
`_render_body`, `_render_with_translator`, `_compile_body`; test functions `test_wr01_*`,
`test_wr02_*`, `test_wr03_*`.

**Fixtures (new, D-02):** `tests/fixtures/citation_degradation_gate/conf.py`,
`tests/fixtures/citation_degradation_gate/index.rst`.

**Planning artifacts (new):** `40.1-GATE-EVIDENCE-01.md`, `40.1-GATE-EVIDENCE-02.md`,
`40.1-GATE-EVIDENCE-03.md`, `40.1-NONREGRESSION.md` (**this plan**).

**Frozen — re-run, never edit (D-02):** `tests/test_citation_render_gate.py`,
`tests/fixtures/citation_render_gate/`.

---

## Worktree provisioning (MANDATORY — run before any other command)

```bash
env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev
# NixOS: BOTH shims are required, resolved via `command -v` BEFORE any .venv/bin entry
# is on PATH. The uv shim alone is not optional -- measured 45 exit-127 failures without it
# (STATE.md, Phase 39). The corpus gate below drives subprocess sphinx builds, which is
# exactly the path the missing `uv` shim breaks.
for t in uv ruff; do ln -sf "$(command -v $t)" ".venv/bin/$t"; done
```

Every subsequent command in this plan runs through `uv run`.

---

## Out of scope for this plan (recorded so the omission is visible, not silent)

- **The docs dogfooding builds (`tox -e docs-html` / `docs-pdf`).** Not named by SC#5, and the
  full-corpus `-b typstpdf` gate is a strictly larger exercise of `visit_reference` than this
  project's own `docs/` tree. Deliberately not run here to keep the close-out to the four checks SC#5
  actually names.
- **IN-01** (`_find_citing_reference`'s linear scan) — recorded in `40-REVIEW.md` as Info with "no
  action required", and kept out of this phase by CONTEXT.md `<deferred>`. No performance measurement
  is taken and none is owed.

---

<tasks>

<task type="auto">
  <name>Task 1: Run the SC#5 gate set and the corpus gate, and prove byte-identity</name>
  <files>.planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md</files>
  <read_first>
    - .planning/phases/40.1-citation-degradation-hardening/40.1-GATE-EVIDENCE-01.md § "SC#5 pre-phase baseline" (the two sha256 values to reproduce, and the commit they were taken at)
    - .planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md § "Sampling Rate" and § "Manual-Only Verifications"
    - .planning/phases/40-citations-full-round-trip/40-NONREGRESSION.md (the close-out record shape to mirror)
    - tests/test_corpus_gate.py (its `pytest.skip` conditions, so a skip is recognised as a non-result rather than misread as a pass)
    - CLAUDE.md § "Worktree-isolated execution" and § "The `@preview` version-sync hazard"
  </read_first>
  <action>
Run the worktree provisioning block first. This task changes NO file under `typsphinx/` or `tests/`.

Run and capture the **verbatim** output of each of the following against the final phase tree. Record
each command, its exit status, and its summary line into `40.1-NONREGRESSION.md`:

1. `uv run pytest tests/test_citation_render_gate.py -x` — Phase 40's frozen citation render gate,
   unmodified (D-02).
2. `uv run pytest tests/test_citation_degradation_gate.py -v` — this phase's own module, all three
   warnings green together, with the per-test list captured so `-k wr01` / `-k wr02` / `-k wr03`
   coverage is readable from the record.
3. `uv run pytest` — the FULL suite including slow-marked tests. Capture the passed/skipped counts.
4. `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` — the lint/type trio,
   matching CI exactly.
5. `uv run pytest tests/test_corpus_gate.py -m slow -v` — the full-corpus `-b typstpdf` gate and
   D-06's load-bearing non-regression evidence, since `visit_reference` is on the hot path for every
   link in the codebase. **Capture the per-test result explicitly.** If the corpus clone fails on a
   transient network error, retry once. If it skips because the corpus is genuinely unavailable,
   record the verbatim skip reason and mark this item **NOT SATISFIED** in the record — a skip is not
   a pass, and this phase does not close on one. Do not paper over it and do not assert a green you
   did not observe.
6. `uv run pytest tests/test_preview_version_sync.py -x` — the three `@preview` declaration sites are
   in lockstep; this phase added no package and moved no pin.
7. `git diff --stat <phase-start-commit> -- pyproject.toml uv.lock` — must be empty; no runtime or
   dev dependency was added.

Then prove **SC#5 byte-identity**: rebuild the FROZEN fixture `tests/fixtures/citation_render_gate/`
with `-b typst` into a scratch directory, invoking Sphinx as `sys.executable -m sphinx` (never a bare
`sphinx-build`, never `uv run sphinx-build` — NixOS PATH shadowing). Compute the sha256 of `index.typ`
and `second.typ` and compare both against the values plan `40.1-01` recorded in
`40.1-GATE-EVIDENCE-01.md` § "SC#5 pre-phase baseline". Record both sides of both comparisons and the
verdict. **If either differs, do not re-baseline** — capture the unified diff of the two `.typ` files
into the record and report it as a regression finding rather than closing the phase.

Finally, record `git diff --stat <phase-start-commit> -- typsphinx/` so the record carries the exact
production blast radius of the phase (expected: `typsphinx/translator.py` only).
  </action>
  <verify>
    <automated>uv run pytest tests/test_citation_render_gate.py -x && uv run pytest tests/test_citation_degradation_gate.py -v && uv run pytest tests/test_corpus_gate.py -m slow -v</automated>
  </verify>
  <acceptance_criteria>
    - `40.1-NONREGRESSION.md` records all seven commands above with their verbatim summary lines and exit statuses.
    - The recorded `tests/test_corpus_gate.py -m slow` result shows `PASSED` for its corpus-compile test — if it shows `SKIPPED`, the record says **NOT SATISFIED** in those words and the phase is reported as not closable, not as green.
    - `uv run pytest` (full suite) reports zero failures.
    - `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` each exit 0, recorded.
    - `40.1-NONREGRESSION.md` carries both pre-phase and post-phase sha256 values for `index.typ` and `second.typ` built from `tests/fixtures/citation_render_gate/`, and an explicit byte-identity verdict.
    - `git diff --stat <phase-start-commit> -- pyproject.toml uv.lock` produces no output, quoted in the record.
    - `git diff --stat <phase-start-commit> -- typsphinx/` names `typsphinx/translator.py` and no other production file, quoted in the record.
    - `git status --porcelain typsphinx/ tests/` is empty — this task changes no code.
  </acceptance_criteria>
  <done>Every SC#5 check has been run against the final tree with verbatim output captured, the corpus gate's result is recorded as an observed pass or an explicit non-result, and Phase 40's fixture output is proven byte-identical to its pre-phase baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Write the cross-phase evidence manifest and complete the validation map</name>
  <files>.planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md, .planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/40.1-citation-degradation-hardening/40.1-GATE-EVIDENCE-01.md
    - .planning/phases/40.1-citation-degradation-hardening/40.1-GATE-EVIDENCE-02.md
    - .planning/phases/40.1-citation-degradation-hardening/40.1-GATE-EVIDENCE-03.md
    - .planning/phases/40.1-citation-degradation-hardening/40.1-VALIDATION.md (the Per-Task Verification Map whose Status column this task closes out)
    - .planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md D-11 (why these three warnings were routed here, and the two consequences for Phase 41's SC#4 sweep)
    - .planning/phases/40-citations-full-round-trip/40-NONREGRESSION.md (shape to mirror)
  </read_first>
  <action>
**(a) Write the change-site → RED manifest** into `40.1-NONREGRESSION.md`. Per `41-CONTEXT.md` D-11,
Phase 41's SC#4 invariant sweep reads this phase's GATE-EVIDENCE files to prove every node-handler
change in the milestone carries a recorded RED — so this manifest is a cross-phase contract, not
local bookkeeping. It must be a table with one row per translator change site, carrying: the site
(function or symbol), the warning it closes, the evidence file holding its RED, the RED's form
(classic `typst.compile()` fatal vs. structural), the RED's provenance (real `sphinx-build` vs.
directly-assembled doctree) with a one-line reason, the pytest selector that was RED, and the commit
the RED was recorded against. The four rows are: `visit_citation`'s backref loop (WR-01),
`_citation_run_neighbour` (WR-02), the new `_reference_anchor_decision` predicate plus `visit_reference`'s
rewiring (WR-03), and the deletion of `_citing_reference_has_own_anchor` (WR-03, same evidence file).

**(b) Verify the D-03 mapping, do not assume it.** Confirm all three GATE-EVIDENCE files exist and
that each one's RED section records exactly the warning this plan's mapping table assigns to it —
`-01.md` → WR-01, `-02.md` → WR-02, `-03.md` → WR-03. Read the RED section headings rather than
grepping bare warning IDs: an evidence file may legitimately mention a sibling warning in prose, so a
bare ID grep would over-count. Record the confirmation and its output. If a warning's RED is recorded
in two files or in none, fix the evidence files before writing the manifest.

**(c) Record the two Claude's-Discretion decisions and their locations** so a future maintainer finds
them without re-deriving: G1 — a skipped back-reference degrades **silently**, decided in
`40.1-01-PLAN.md` with its three-point rationale, and the obligation it carried (the emitted marker
shape for every degraded citation is asserted). G2 — WR-02's fix stays **narrow** (one literal
disjunct, no general emit-nothing predicate), decided in `40.1-02-PLAN.md`, with the measured caveat
that an ids-less target is inert only outside list items. Also record the three sub-discretion
outcomes: the predicate's name and eight-field return shape, the **deletion** of
`_citing_reference_has_own_anchor` rather than keeping a thin delegator, and the structural form of
WR-02's assertion (`count("grid(")` on the rendered body, with an ids-carrying-target negative
control bounding it).

**(d) Record what was deliberately NOT done**, so the omissions are visible: IN-01 (the linear scan,
Info, explicitly no action); generalising "a sibling that emits nothing" beyond citations (deferred
in CONTEXT.md); no new requirement ID added, removed or reworded; no Phase 40 locked decision
re-opened; Phase 40's fixture directory and render-gate module untouched; the docs dogfooding builds
not run and why.

**(e) Complete `40.1-VALIDATION.md`'s Per-Task Verification Map.** Replace the `TBD` Task ID and Plan
columns with the real values and set each Status from Task 1's measured results:

| Task ID | Plan | Wave | Requirement |
|---------|------|------|-------------|
| `40.1-01-T2` / `40.1-01-T3` | `40.1-01` | 1 | WR-01 |
| `40.1-02-T1` / `40.1-02-T2` | `40.1-02` | 2 | WR-02 |
| `40.1-03-T1` / `40.1-03-T2` | `40.1-03` | 3 | WR-03 |
| `40.1-04-T1` | `40.1-04` | 4 | D-06 non-regression |
| `40.1-04-T1` | `40.1-04` | 4 | SC#5 control |

Also tick the Wave 0 Requirements checkboxes that Task 1 measured as satisfied, and fill the
Manual-Only Verifications row's outcome (the D-01/D-08 provenance record — confirm each GATE-EVIDENCE
file carries its per-warning attempt list, resulting verdict, and verbatim RED). **Leave the
frontmatter `status`, `nyquist_compliant` and `wave_0_complete` keys alone** — those are
`/gsd-validate-phase` §6's to set, not this plan's.
  </action>
  <verify>
    <automated>D=.planning/phases/40.1-citation-degradation-hardening; for f in $D/40.1-GATE-EVIDENCE-01.md $D/40.1-GATE-EVIDENCE-02.md $D/40.1-GATE-EVIDENCE-03.md $D/40.1-NONREGRESSION.md; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done; grep -q 'WR-01' $D/40.1-GATE-EVIDENCE-01.md && grep -q 'WR-02' $D/40.1-GATE-EVIDENCE-02.md && grep -q 'WR-03' $D/40.1-GATE-EVIDENCE-03.md && echo "evidence mapping ok"</automated>
  </verify>
  <acceptance_criteria>
    - All four of `40.1-GATE-EVIDENCE-01.md`, `-02.md`, `-03.md` and `40.1-NONREGRESSION.md` exist in the phase directory.
    - `40.1-GATE-EVIDENCE-01.md` covers WR-01, `-02.md` covers WR-02 and `-03.md` covers WR-03 — confirmed by locating each file's RED section and reading which warning it records, not by a bare ID grep (an evidence file may legitimately mention a sibling warning in prose). The confirmation and its output are recorded in `40.1-NONREGRESSION.md`, and no warning's RED is recorded in two files or in none.
    - `40.1-NONREGRESSION.md` contains a change-site → RED manifest table with four rows (`visit_citation` backref loop, `_citation_run_neighbour`, `_reference_anchor_decision` + `visit_reference` rewiring, deletion of `_citing_reference_has_own_anchor`), each carrying warning ID, evidence file, RED form, RED provenance with reason, the RED pytest selector, and the recording commit.
    - `40.1-NONREGRESSION.md` records both Claude's-Discretion outcomes (G1 silent degradation, G2 narrow WR-02 fix) with where each was decided, plus the three sub-discretion outcomes (predicate name/return shape, deletion rather than delegator, WR-02's assertion form).
    - `40.1-NONREGRESSION.md` contains a "deliberately not done" section naming IN-01, the deferred emit-nothing generalisation, the absence of any requirement-ID change, the untouched Phase 40 locked decisions and fixture/render-gate, and the un-run docs dogfooding builds with the reason.
    - `40.1-VALIDATION.md`'s Per-Task Verification Map contains no remaining `TBD` in its Task ID or Plan columns, and every Status cell is set from a measured result rather than left `⬜ pending`.
    - `40.1-VALIDATION.md`'s frontmatter `status`, `nyquist_compliant` and `wave_0_complete` keys are unchanged by this task.
    - `git status --porcelain typsphinx/ tests/` is empty — this task changes no code.
  </acceptance_criteria>
  <done>The cross-phase manifest Phase 41's SC#4 sweep consumes is written, the D-03 warning→file mapping is mechanically confirmed one-to-one, the discretion outcomes and deliberate omissions are recorded, and `40.1-VALIDATION.md`'s verification map is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.rst` source → docutils/Sphinx doctree → `TypstTranslator` → emitted `.typ` → `typst.compile()` | Unchanged by this plan, which lands no source. Re-measured here across the whole Sphinx `doc/` corpus rather than a single fixture. |
| this phase's GATE-EVIDENCE files → Phase 41's SC#4 invariant sweep | A cross-phase contract (`41-CONTEXT.md` D-11): a later phase's success criterion depends on this phase's evidence filenames and contents. A missing or mis-mapped evidence file silently weakens Phase 41's proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-04 | Denial of Service (regression, not a new vulnerability class) | `visit_reference` — the hot path for every link in the codebase, rewritten by D-06's predicate extraction | high | mitigate | Full-corpus `-b typstpdf` gate (`tests/test_corpus_gate.py -m slow`) **run and observed PASSED**, plus the whole existing narrow render-gate suite (each asserting exact bytes for one `visit_reference` construct type) and the full test suite. A skip is recorded as NOT SATISFIED, never as a pass. |
| T-40.1-07 | Repudiation | the phase's own evidence record (`40.1-GATE-EVIDENCE-0N.md`, `40.1-NONREGRESSION.md`) | medium | mitigate | Every claim in the record is a captured verbatim command output with its exit status, and the SC#5 byte-identity verdict is an sha256 pair taken at two named commits rather than an assertion. The warning→file mapping is checked mechanically. This is what makes Phase 41's SC#4 sweep able to trust the files rather than re-derive them. |
| T-40.1-05 | Information Disclosure (V5 Input Validation) | label token derivation (`_namespace_label` / `_sanitize_label`) | low | accept | This plan lands no source change and touches no sanitizer. ASVS L1 requires no further control here. |
| T-40.1-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | Not applicable: no runtime or dev dependency is added and no package-manager install task exists in this phase (`40.1-RESEARCH.md` § "Package Legitimacy Audit"). Task 1 asserts this by diffing `pyproject.toml` and `uv.lock` against the phase-start commit and by re-running `tests/test_preview_version_sync.py`. |

Resolved config: `security_asvs_level: 1`, `security_block_on: high`. `T-40.1-01`, `T-40.1-02`,
`T-40.1-03` and `T-40.1-06` are carried by plans `40.1-01`, `40.1-03` and `40.1-02`.
</threat_model>

<verification>
- `uv run pytest` — full suite, zero failures, counts recorded.
- `uv run pytest tests/test_corpus_gate.py -m slow -v` — observed PASSED, not skipped; the verbatim result is in the record.
- `uv run pytest tests/test_citation_render_gate.py -x` — Phase 40's frozen SC#5 control, green and unmodified.
- `uv run pytest tests/test_citation_degradation_gate.py -v` — all three warnings' tests green together.
- `uv run pytest tests/test_preview_version_sync.py -x` — the three `@preview` declaration sites in lockstep.
- `uv run black --check .` / `uv run ruff check .` / `uv run mypy typsphinx/` — the lint/type trio, matching CI exactly.
- sha256 of `index.typ` and `second.typ` rebuilt from `tests/fixtures/citation_render_gate/` equal plan `40.1-01`'s pre-phase baseline.
- `git diff --stat <phase-start> -- pyproject.toml uv.lock` empty; `git diff --stat <phase-start> -- typsphinx/` names `translator.py` only.
- The warning→evidence-file mapping is one-to-one, checked mechanically.
</verification>

<success_criteria>
- ROADMAP SC#5 is met in full: Phase 40's citation render-gate module, the full suite, the lint/type trio and the full-corpus `-b typstpdf` gate are all green, and the `.typ` emitted for Phase 40's own citation fixture is byte-identical to its pre-phase baseline — the control proving the change is confined to the degradation paths.
- ROADMAP SC#4 is closed out across all three warnings: each RED is real, each provenance is recorded with its attempt list and reason, and the change-site → RED manifest makes that checkable in one table.
- D-06's re-incurred non-regression obligation is discharged on observed evidence, not on a skip.
- Phase 41's SC#4 invariant sweep has a manifest to read rather than a pile to reconstruct (`41-CONTEXT.md` D-11).
- Every deliberate omission — IN-01, the deferred emit-nothing generalisation, the un-run docs dogfooding builds — is recorded as visible rather than silent.
</success_criteria>

<output>
Create `.planning/phases/40.1-citation-degradation-hardening/40.1-04-SUMMARY.md` when done.
</output>