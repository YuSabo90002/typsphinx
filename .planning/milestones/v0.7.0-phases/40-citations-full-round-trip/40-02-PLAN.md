---
phase: 40-citations-full-round-trip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/charged-ieee/approach1/source/index.rst
  - examples/charged-ieee/approach2/source/index.rst
  - .planning/ROADMAP.md
  - .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-02.md
autonomous: true
requirements: [CIT-05]

must_haves:
  truths:
    - "Both `examples/charged-ieee/{approach1,approach2}/source/index.rst` are restored to the EXACT pre-removal content -- git blob `82831eb092b9f52cba8b1247b95f7e148f499bb2`, which is what both files held before commits `8bed1a3` and `c014a0b` stripped them, and which is the last state either file had other than the removal itself."
    - "The two samples are byte-identical to each other again (D-12): both `git hash-object` outputs are the same blob id, so the template wiring under `source/conf.py` is once more the samples' ONLY intended difference."
    - "The restore is verbatim, not an expansion (D-11): exactly one citing reference and exactly one References entry come back, the reworded CNN sentence reverts, and both approaches' five-line RST comment blocks explaining the missing citations are deleted. No further real paper is added, even though VGGNet / ResNet / EfficientNet are already named in the sample's prose."
    - "`tests/test_examples_charged_ieee_gate.py` is RE-RUN, never edited. It is RED after this restoration and before the handlers land -- the shipped samples stop building -- and that RED is CIT-05's own GATE-01 evidence on real shipped content rather than on a purpose-made fixture."
    - "ROADMAP.md Phase 40 SC#3's claim that a definition carries back-references to every citing location is corrected to the same-document scope D-08 establishes, and the correction is recorded as a dated bullet in the ROADMAP Evolution section -- the same handling Phase 36's SC#3 and Phase 39's D-12 received. The criterion is amended against measurement, never waived."
    - "This plan modifies no file under `typsphinx/` and no file under `tests/`."
  artifacts:
    - examples/charged-ieee/approach1/source/index.rst
    - examples/charged-ieee/approach2/source/index.rst
    - .planning/ROADMAP.md
    - .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-02.md
  key_links:
    - "examples/charged-ieee/{approach1,approach2}/source/index.rst -> tests/test_examples_charged_ieee_gate.py, which resolves the samples relative to the repository root and builds them LIVE; it is the only module in the suite that builds a shipped sample rather than a fixture."
    - "the restored citation definition -> the restored inline citing reference in the CNN paragraph; restoring one without the other produces either an uncited definition or a dangling reference, neither of which is what 8bed1a3/c014a0b removed."
    - "ROADMAP.md Phase 40 SC#3 -> 40-CONTEXT.md D-08; the roadmap text is the artifact /gsd-verify-work grades against, so leaving it uncorrected would grade the phase against a scope the owner explicitly narrowed."
  prohibitions:
    - requirement_id: CIT-05
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT expand, reword, modernise or otherwise improve the shipped sample beyond the exact reverse of the removal commits. The owner chose the smaller diff over the better demo so the commit reads as restoring what was broken; feature demonstration belongs in this phase's own fixtures, never in shipped user-facing sample content."
---

<objective>
Restore CIT-05: put the citation syntax that Phase 22.2 stripped out of both `examples/charged-ieee/`
approaches back, verbatim, so the two samples become byte-identical again; and land D-09's ROADMAP
correction so Phase 40's SC#3 states the same-document back-reference scope D-08 established rather
than the wider claim the roadmap was written with.

Purpose: an IEEE paper template shipping without a references section is the concrete defect CIT-05
closes. Restoring it BEFORE the handlers land also turns `tests/test_examples_charged_ieee_gate.py`
into CIT-05's own RED-to-GREEN gate on real shipped content, with zero code changes to that module --
its zero-warning helper and its build-exit-0 assertions fail today and pass after 40-03.

Output: two restored `.rst` files, a corrected ROADMAP SC#3 plus a Roadmap Evolution entry, and
`40-GATE-EVIDENCE-02.md`. This plan touches no file under `typsphinx/` and no file under `tests/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-citations-full-round-trip/40-CONTEXT.md
@.planning/phases/40-citations-full-round-trip/40-RESEARCH.md
@.planning/phases/40-citations-full-round-trip/40-PATTERNS.md
@.planning/phases/40-citations-full-round-trip/40-VALIDATION.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Skipping step 1 makes the worktree's pytest import the MAIN checkout's `typsphinx` package, so gates
report the wrong colour.
</environment_setup>

<flagged_assumptions>
The spec-less probe fallback is active for this phase. The full 11-row probe report, its disposition
table, the no-silent-drop equality statement and the prohibition-recall breadcrumbs are recorded once,
in `40-01-PLAN.md`'s `<flagged_assumptions>` block; they are not repeated here.

The one row that binds THIS plan is row 10 -- `CIT-05 / unclassified`, left **`unresolved`** and
flagged, never auto-covered, never auto-backstopped, never auto-dismissed. CIT-05 is a verbatim
content restore of two shipped `.rst` files: no input domain, no boundary, no ordering for the edge
taxonomy to reach. Its real correctness axis -- the diff being exactly the reverse of `8bed1a3` and
`c014a0b` and nothing more -- is carried as this plan's prohibition and as the byte-equality
acceptance criteria on Task 1, not as a probe-derived edge predicate.
</flagged_assumptions>

<artifacts_this_phase_produces>
Phase-wide list of every symbol Phase 40 creates, so the plan-review source-grounding pass does not
flag a newly-created symbol as drift. Every plan in this phase carries this same list.

**Source symbols (`typsphinx/translator.py`)** -- all in plan 40-03

- `visit_citation(self, node)` -- NEW method (run-scoped grid open plus this row's two cells).
- `depart_citation(self, node)` -- NEW method (body-cell close, row comma, run-scoped grid close).
- `visit_label(self, node)` -- NEW method, raises `nodes.SkipNode`; fires for citations only.
- `_citation_run_neighbour(self, node, offset)` -- NEW private helper (working name) for run-adjacency
  scanning, skipping siblings that emit nothing (`nodes.comment`, `nodes.system_message`).
- `visit_reference` / `depart_reference` -- EXISTING, gain the guarded own-`ids` bracket-wrap and its
  matching close plus label attachment (D-14).
- One NEW instance attribute slot in `__init__` carrying the D-14 bracket-wrap state between visit and
  depart (working name `_reference_own_anchor`); never a fourth consumer of the shared `_strong_was_*`
  slots (Phase 36 D-01/D-02).
- `_namespace_label`, `_sanitize_label`, `escape_typst_string`, `_emit_id_anchors`,
  `_visit_admonition`, `_depart_admonition`, `visit_footnote`, `visit_footnote_reference`,
  `depart_term` -- **NOT modified**; read as precedent, reused unchanged.

**Test symbols**

- `tests/fixtures/citation_render_gate/{conf.py,index.rst,second.rst}` -- NEW (plan 40-01).
- `tests/test_citation_render_gate.py` -- NEW (plan 40-01), with the standard render-gate scaffolding
  and layout helpers copied from `tests/test_rubric_indent_invariance.py`.
- The sentinel literals `CITORDERALPHA`, `CITORDERBRAVO`, `CITORDERCHARLIE`, `CITORDERDELTA`,
  `CITORDERECHO`, `CITNESTEDSENTINEL`, `CITBREAKONESENTINEL`, `CITBREAKTWOSENTINEL`,
  `CITSECONDDOCSENTINEL` -- NEW (plan 40-01).
- `tests/test_desc_break_marker_buffer_swap_gate.py` -- its Phase-40 sole-exception comment re-checked
  and updated only if stale (plan 40-04).
- `tests/test_examples_charged_ieee_gate.py` -- **re-run, never edited** (this plan and plan 40-04).
- `tests/test_corpus_gate.py` and `tests/test_preview_version_sync.py` -- **not modified**.

**Content**

- `examples/charged-ieee/approach1/source/index.rst` and
  `examples/charged-ieee/approach2/source/index.rst` -- restored to blob
  `82831eb092b9f52cba8b1247b95f7e148f499bb2` (this plan).

**Planning artifacts**

- `40-GATE-EVIDENCE-01.md` (plan 40-01), `40-GATE-EVIDENCE-02.md` (this plan),
  `40-NONREGRESSION.md` (plan 40-04).
- `.planning/ROADMAP.md` -- Phase 40 SC#3 corrected and a Roadmap Evolution entry added (this plan,
  D-09).

**Dependencies**

- None added; no `@preview` pin moves. Milestone invariants #1 and #2 hold.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Restore both charged-ieee samples to their exact pre-removal content</name>
  <read_first>
    - `examples/charged-ieee/approach1/source/index.rst` and
      `examples/charged-ieee/approach2/source/index.rst` in full -- the current, stripped state and
      the two differently-worded five-line RST comment blocks at the top of each.
    - The two removal commits in full: `git show 8bed1a3` and `git show c014a0b`. Both files held the
      SAME blob before removal, and the removal commit is the LAST change either file has had.
    - `.planning/phases/40-citations-full-round-trip/40-PATTERNS.md` § the restoration entry -- the
      three-part diff (delete the top comment block, revert the reworded CNN sentence, restore the
      trailing References section).
    - `.planning/phases/40-citations-full-round-trip/40-CONTEXT.md` D-11 and D-12.
    - `examples/charged-ieee/README.md` -- confirm it makes no claim about the samples lacking
      citations that would go stale with this restore.
  </read_first>
  <files>examples/charged-ieee/approach1/source/index.rst, examples/charged-ieee/approach2/source/index.rst</files>
  <action>
Restore both sample sources to the exact content they held before the removal. The pre-removal state
of BOTH files is one and the same git blob, `82831eb092b9f52cba8b1247b95f7e148f499bb2`, and the
removal commits are the last change either file has had -- so this restore is fully determined and
needs no hand-editing or manual reverse-patching.

Write that blob's content into each of the two paths. Verify afterwards with `git hash-object` that
both files hash to `82831eb092b9f52cba8b1247b95f7e148f499bb2`. That single check simultaneously
proves the verbatim restore (D-11) and the byte-identity of the two samples (D-12), and it proves
both top-of-file comment blocks are gone, because the pre-removal blob never contained either of them.

Do not add anything the blob does not contain. In particular do not add VGGNet, ResNet or
EfficientNet reference entries even though the sample's own prose already names those papers -- D-11
rejected exactly that expansion. Do not touch either approach's `source/conf.py`, its
`source/_templates` directory, or any other file in the examples tree: after this restore the
template wiring must be the samples' only remaining difference.

Then run `tests/test_examples_charged_ieee_gate.py` and CAPTURE its full output for Task 3. Expect it
to be RED: with the handlers not yet written, both approaches emit unknown-node warnings for the
citation and label node types and their `-b typstpdf` builds abort. Do not edit that module to make
it pass, do not add a skip marker, and do not add an xfail -- the RED is the point, and 40-04 re-runs
the same module unchanged to prove the GREEN flip.
  </action>
  <acceptance_criteria>
    - `git hash-object examples/charged-ieee/approach1/source/index.rst` outputs `82831eb092b9f52cba8b1247b95f7e148f499bb2`.
    - `git hash-object examples/charged-ieee/approach2/source/index.rst` outputs the same hash.
    - `diff examples/charged-ieee/approach1/source/index.rst examples/charged-ieee/approach2/source/index.rst` reports no difference and exits 0.
    - Each file contains exactly one citation definition line for the Krizhevsky key, exactly one inline citing reference to it in the CNN paragraph, and exactly one `References` section heading.
    - The first line of each file is the document title, not the start of an RST comment block.
    - `git diff --name-only -- examples/` lists exactly these two files and no others.
    - `uv run pytest tests/test_examples_charged_ieee_gate.py -v` is RED, and the failure text names either a non-zero `sphinx-build` exit or the presence of a build warning -- not a Python error and not a missing-file error.
    - `git diff --stat -- tests/ typsphinx/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>test "$(git hash-object examples/charged-ieee/approach1/source/index.rst)" = "82831eb092b9f52cba8b1247b95f7e148f499bb2" &amp;&amp; test "$(git hash-object examples/charged-ieee/approach2/source/index.rst)" = "82831eb092b9f52cba8b1247b95f7e148f499bb2" &amp;&amp; diff -q examples/charged-ieee/approach1/source/index.rst examples/charged-ieee/approach2/source/index.rst &amp;&amp; test -z "$(git diff --stat -- tests/ typsphinx/)"; uv run pytest tests/test_examples_charged_ieee_gate.py -v</automated>
  </verify>
  <done>Both shipped samples hash to the pre-removal blob, are byte-identical to each other, and the untouched examples gate is RED for a build reason.</done>
</task>

<task type="auto">
  <name>Task 2: Correct ROADMAP Phase 40 SC#3 to D-08's same-document scope and record the amendment</name>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 40: Citations — Full Round Trip" -- the Goal paragraph and all
      five success criteria, so the edit is scoped to SC#3 and the Goal's own back-reference wording
      is checked for the same overreach.
    - `.planning/ROADMAP.md` § "Roadmap Evolution" -- the section's preamble and the single existing
      2026-08-02 Phase 39 bullet, which fixes the bullet shape this entry must match.
    - `.planning/phases/40-citations-full-round-trip/40-CONTEXT.md` D-08 and D-09, and the "measured
      starting position" paragraph recording that `backrefs` are same-document only and that Sphinx's
      own HTML has the same limitation.
    - `.planning/STATE.md` § "Roadmap Evolution" (STATE.md's mirror log) -- confirm whether it needs a
      matching entry; the ROADMAP section's own preamble states the two are mirrors.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
Use scoped `Edit` operations only. Never rewrite `.planning/ROADMAP.md` with a whole-file `Write`:
the file holds every phase of every milestone and a whole-file write destroys everything outside the
diff window.

First edit -- Phase 40 SC#3. Its current text claims each definition carries back-references to every
citing location. Narrow that clause to the scope D-08 established and D-01 measured: back-references
to every citing location docutils itself recorded in the definition's own `backrefs`, which is
same-document only; a cross-document citing site gets a working forward link and no back-reference,
matching Sphinx's own HTML builder. Keep the rest of SC#3 exactly as it stands -- the forward
reference, the 2-or-more citations, the 2-or-more documents, and the namespace/sanitize clause about
a repeated key across documents are all still required and all still provable.

Second edit -- the Phase 40 Goal paragraph a few lines above. Check whether it repeats the same
unqualified claim. If it does, apply the same narrowing there; if it does not, leave it untouched and
say so in the summary rather than editing for symmetry.

Third edit -- add one dated bullet to the Roadmap Evolution section, following the shape of the
existing Phase 39 bullet exactly: the date, which phase and which criterion changed, the decision id
that drove it (D-08, recorded as D-09), the measurement that forced it (docutils populates `backrefs`
with same-document citing sites only, verified against a real two-document build; Sphinx HTML has the
same limitation and Sphinx LaTeX renders no back-references at all), and an explicit statement that
no requirement was added, removed, or re-assigned to a different phase. Do not mark the criterion
waived -- it is amended against measurement, which is the distinction this section exists to record.

Fourth -- if `.planning/STATE.md` carries a mirrored Roadmap Evolution log, note in the summary that
it needs the same bullet, but do NOT edit `STATE.md` in this plan; the phase-close workflow owns that
file and a concurrent edit here would conflict with it.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` § Phase 40 SC#3 no longer asserts an unqualified "every citing location" back-reference guarantee; its new wording names the same-document scope and names docutils' own `backrefs` as the source of truth.
    - SC#1, SC#2, SC#4 and SC#5 of Phase 40 are byte-unchanged.
    - The Roadmap Evolution section gains exactly one new dated bullet, and that bullet names Phase 40, SC#3, D-08/D-09, the measurement, and the statement that no requirement moved.
    - The pre-existing 2026-08-02 Phase 39 Roadmap Evolution bullet is byte-unchanged.
    - `git diff -- .planning/ROADMAP.md` shows changes confined to the Phase 40 detail block and the Roadmap Evolution section; no other phase's entry appears in the diff.
    - The total line count of `.planning/ROADMAP.md` increases (the evolution bullet is an addition) and no phase heading disappears -- `grep -c '^### Phase ' .planning/ROADMAP.md` is unchanged from before the edit.
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat -- .planning/ROADMAP.md &amp;&amp; git diff -- .planning/ROADMAP.md | grep -c '^-' &amp;&amp; test "$(grep -c '^### Phase ' .planning/ROADMAP.md)" = "$(git show HEAD:.planning/ROADMAP.md | grep -c '^### Phase ')"</automated>
  </verify>
  <done>Phase 40's SC#3 states the same-document back-reference scope, the amendment is recorded as a dated Roadmap Evolution bullet, and no other phase entry moved.</done>
</task>

<task type="auto">
  <name>Task 3: Record the shipped-sample GATE-01 RED evidence</name>
  <read_first>
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-01.md` -- the evidence
      file format: named commit, verbatim RED output, an explicit RED-versus-CONTROL split, and a
      closing no-source-change statement backed by `git diff --stat`.
    - `tests/test_examples_charged_ieee_gate.py` in full -- what each of its assertions actually
      checks, so the evidence file records WHICH assertion failed and why, rather than just pasting a
      traceback. Read it; do not edit it.
    - The captured output from Task 1's examples-gate run.
    - `.planning/phases/40-citations-full-round-trip/40-CONTEXT.md` D-11 and D-12 -- the two claims
      this evidence file must show were honoured.
  </read_first>
  <files>.planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-02.md</files>
  <action>
Write `40-GATE-EVIDENCE-02.md`, the shipped-sample half of this phase's GATE-01 RED. Record, against
a named commit hash of the untouched translator:

1. The commit hash, plus a statement that `typsphinx/` and `tests/` are both byte-unchanged by this
   plan, backed by the `git diff --stat -- typsphinx/ tests/` output.
2. The restoration proof: both `git hash-object` outputs equal to
   `82831eb092b9f52cba8b1247b95f7e148f499bb2`, the `diff` between the two samples showing no output,
   and the `git diff --stat -- examples/` showing exactly two files touched. State explicitly that
   this is what D-11's verbatim-restore and D-12's byte-identity claims reduce to, and that both
   top-of-file comment blocks disappeared as a consequence of restoring the blob rather than by a
   separate hand edit.
3. The verbatim `uv run pytest tests/test_examples_charged_ieee_gate.py -v` output, RED, with each
   failing assertion labelled by what it checks -- the zero-warning helper, the build exit code, the
   non-empty PDF, and for approach2 the template-provenance assertions.
4. The verbatim build output for at least one approach showing the two unknown-node warnings this
   phase removes, and the compile fatal that follows them.
5. An explicit statement that this module is re-run, never edited: its assertions are unchanged
   between this RED and the GREEN that 40-04 will record, which is what makes the flip meaningful.
6. An explicit statement of which tests actually EXECUTED versus which were skipped by the
   `TYPST_AVAILABLE` guard on this run -- a skip is not evidence.
7. The D-11 accepted cost, restated so a later reader does not mistake it for an oversight: with one
   entry and one citing site the restored sample exercises only D-03's single-back-reference shape.
   Neither the multi-marker shape nor D-05's widest-label alignment is visible here; both are proven
   by this phase's own fixture in 40-01.
  </action>
  <acceptance_criteria>
    - `.planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-02.md` exists and is non-empty.
    - It contains a named commit hash, both `git hash-object` outputs, the verbatim RED pytest output, and the verbatim unknown-node warning text from a real build.
    - It contains the executed-versus-skipped statement and the D-11 accepted-cost restatement.
    - It states that `tests/test_examples_charged_ieee_gate.py` was not edited, backed by `git diff --stat -- tests/` being empty.
    - `git diff --stat -- typsphinx/ tests/` is empty over this plan's commits.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-02.md &amp;&amp; grep -q '82831eb092b9f52cba8b1247b95f7e148f499bb2' .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-02.md &amp;&amp; test -z "$(git diff --stat -- typsphinx/ tests/)"</automated>
  </verify>
  <done>The shipped-sample RED is recorded verbatim against a named commit, with proof the gate module itself was never touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped sample `.rst` -> translator -> `typst.compile()` | The samples are the one place in this repo where a translator defect reaches a user by way of documentation they are told to copy. |
| removal-commit history -> restored working tree | The restore's correctness is defined entirely by a git blob; any hand-editing reintroduces the risk the blob check eliminates. |
| ROADMAP.md success criteria -> `/gsd-verify-work` | The roadmap text is the artifact the phase is graded against; an uncorrected criterion silently grades against the wrong scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | Citation label-token derivation -- an author-supplied key reaching a Typst `<label>` token unsanitized | high | mitigate | Not exercised by this plan's edit: the restored sample's single key is plain ASCII. The mitigation itself (route every label through the existing `_namespace_label`/`_sanitize_label`, D-13) lands in 40-03; the restored sample is one more real-content input proving it holds end to end. |
| T-40-02 | Denial of service | Separator miscounting merging or splitting a rendered reference list | high | mitigate | The restored sample has exactly one citation, so it cannot exercise a run boundary; that is D-11's accepted cost and is restated in the evidence file so nobody mistakes the sample for run coverage. Run boundaries are covered by 40-01's fixture. |
| T-40-03 | Denial of service | A citing reference emitting a link to a label that was never attached | high | mitigate | The restore reinstates the citing reference and its definition TOGETHER; the acceptance criteria assert exactly one of each, so a half-restore that would create a dangling reference fails the task rather than shipping. |
| T-40-SC | Tampering | npm/pip/cargo installs -- supply chain | high | mitigate | This plan installs nothing and this phase adds no dependency. `40-RESEARCH.md` § "Package Legitimacy Audit" records the phase as not applicable. Verified at phase close by `git diff -- pyproject.toml uv.lock` over the phase's commits being empty. |

ASVS L1: V5 Input Validation is the only applicable category -- a build-time Sphinx extension has no
runtime authentication, session, access-control or cryptography surface (`40-RESEARCH.md` § Security
Domain). Blocking threshold is `high`. All four threats are `high` and all four are `mitigate`, with
the implementation-side mitigations landing in 40-03. Nothing blocks this plan.
</threat_model>

<verification>
- Both sample files hash to `82831eb092b9f52cba8b1247b95f7e148f499bb2` and `diff` between them is
  empty.
- `uv run pytest tests/test_examples_charged_ieee_gate.py -v` is RED for a build reason, and the
  module itself is unmodified.
- `git diff -- .planning/ROADMAP.md` is confined to the Phase 40 detail block and the Roadmap
  Evolution section; `grep -c '^### Phase ' .planning/ROADMAP.md` is unchanged.
- `uv run pytest -m "not slow"` shows no NEW failure other than the examples gate and 40-01's gate
  module.
- `git diff --stat -- typsphinx/ tests/` is empty for this plan's commits.
</verification>

<success_criteria>
- Both shipped samples carry their citation syntax again, restored to the exact pre-removal blob, and
  are byte-identical to each other.
- Nothing was added to either sample beyond the reverse of the removal commits.
- The examples gate is RED and untouched, and that RED is recorded verbatim against a named commit.
- Phase 40's SC#3 states the same-document back-reference scope, with the amendment recorded as a
  dated Roadmap Evolution bullet rather than silently applied.
- No file under `typsphinx/` or `tests/` is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/40-citations-full-round-trip/40-02-SUMMARY.md` when done.
</output>
