---
phase: 40-citations-full-round-trip
plan: 05
type: execute
wave: 3
gap_closure: true
depends_on: [40-03]
files_modified:
  - tests/test_citation_render_gate.py
  - .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CIT-02, CIT-03, CIT-04, CIT-05]

must_haves:
  truths:
    - "All four remaining RED selectors in `tests/test_citation_render_gate.py` are RED because of defects in the gate module authored by 40-01, not because of defects in the handlers 40-03 landed. Each of the four diagnoses below was independently reproduced by the orchestrator against the merged tree before this plan was written; the executor re-reproduces each one before changing the line it blames."
    - "`_expected_own_id_anchors` and `_citing_site_own_anchors` call `env.get_and_resolve_doctree(docname, builder)` without the `tags=` keyword. Sphinx 9.1.0 emits `RemovedInSphinx11Warning` on that path and this project's own `pyproject.toml` escalates `error::PendingDeprecationWarning`, so both helpers raise before any citation assertion is reached. The deprecation branch in `sphinx/environment/__init__.py` assigns `tags = builder.tags` itself, so passing `tags=builder.tags` is behaviour-preserving by construction, not a guess."
    - "`_leading_columns` returns `len(line) - len(line.lstrip(' '))` -- the leading whitespace of the LINE holding the marker, not the column at which the MARKER starts. It was copied verbatim from `tests/test_rubric_indent_invariance.py`, where the marker is the first glyph on its line and the two quantities coincide. In a citation grid the label cell occupies the same physical line ahead of the sentinel, so the helper returns 0 for every entry's first line. As written, `test_layout_...` compares a line-leading-whitespace against a continuation-line indent and can never pass; its `assert alpha_column > 0` can never pass either. The test therefore has no ability to validate CIT-02 or D-05 at all, in either direction."
    - "The concat sub-check of `test_separator_...` requires a bare `link(` immediately after the `+` operator. D-14 -- mandated by this same phase, implemented by 40-03 Task 1 -- wraps a citation-derived citing site as `[#link(<...>, ...)#label(\"...\")]`. The emitted `text(\"Concat Term \") + [#link(<index:concat2000>, text(\"[Concat2000]\"))#label(\"index:id7\")]` satisfies SC#5's concat protocol in substance (operands `+`-joined, no operator left without a right operand); only the literal string shape the assertion demands is stale."
    - "Two comments in the module state that `visit_reference` is unmodified by this phase (lines 497 and 636 at the pre-fix commit). That was true when 40-01 authored them and is false once 40-03 Task 1 lands. They are corrected in place rather than left to rot."
    - "The corrected assertions still DISCRIMINATE: after the fixes are committed, the pre-40-03 translator (`8b22bf6eb74e9f8153757c53d18f2f06da7bcdae`) is checked back in over `typsphinx/translator.py` alone, the module is re-run, and every one of the nine tests is shown to be RED again. A corrected test that passes against the unfixed translator would be a laundered gate, and this re-proof is the only thing that rules that out."
    - "No absolute measurement is written into the module as a literal. The layout assertions stay relational -- continuation indent equals the sentinel's own start column, all five References sentinels share one column, that column is strictly greater than zero -- so the observed value 28 never appears anywhere in the file."
    - "`40-GATE-EVIDENCE-01.md` records each of the four corrections as an amendment against measurement, naming what was measured, what the assertion could and could not have detected before, and what it detects now. The RED it originally recorded is neither rewritten nor deleted."
    - "This plan modifies no file under `typsphinx/`, no file under `examples/`, and no test module other than `tests/test_citation_render_gate.py`."
  artifacts:
    - tests/test_citation_render_gate.py
    - .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "`_leading_columns` -> `_find_page_and_column` -> every sentinel-column assertion in `test_layout_...`; changing the measurement at one site and not the other silently leaves half the D-05 comparison measuring the wrong quantity."
    - "the concat assertion's expected string -> 40-03 Task 1's D-14 bracket-wrap; the test and the handler must agree on ONE shape, and the handler's shape is the one the phase's own decision record mandates."
    - "the RED re-proof -> commit 8b22bf6 (40-03's base); it is the last commit at which `typsphinx/translator.py` is byte-identical to the pre-phase translator, which is what makes it the correct oracle."
  prohibitions:
    - requirement_id: CIT-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT author any corrected expectation by reading what the current translator emits and transcribing it. Every corrected assertion stays relational or is derived from 40-CONTEXT.md's locked decisions. Concretely: the observed continuation column (28) must appear nowhere in the file, and no anchor, label or link-target token may be introduced as a string literal. A test rewritten to match observed output is the exact failure mode 40-01's own prohibition was minted to prevent, and it would be indistinguishable from a passing gate."
    - requirement_id: CIT-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT weaken, delete, skip, xfail or narrow any assertion in order to reach green. The permitted change is to make an assertion measure the quantity it always claimed to measure. If a corrected assertion still fails against the merged translator, that is a translator defect to report -- not an assertion to soften. Report it and halt rather than adjusting the expectation."
---

<objective>
Close the four gap selectors left RED after 40-03 by repairing the defects in the gate module that
40-01 authored, without changing what any assertion is trying to prove and without letting a
corrected assertion lose its ability to fail.

Purpose: the orchestrator independently reproduced all four failures against the merged tree and
confirmed by direct `pypdf` layout extraction that the rendering CIT-02 and D-05 describe is already
correct -- all five References entry bodies start at one shared column past the widest label, and every
wrapped continuation line aligns with them. The handlers are right; the gate cannot currently see it.
40-04's verification requires this module green across all eight selectors, so Wave 4 is blocked until
the module can measure what it claims to.

Output: a corrected `tests/test_citation_render_gate.py`, four amendments recorded in
`40-GATE-EVIDENCE-01.md`, and CIT-02/CIT-03/CIT-04/CIT-05 checked off in `.planning/REQUIREMENTS.md`.
This plan touches no file under `typsphinx/` and no other test module.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/40-citations-full-round-trip/40-CONTEXT.md
@.planning/phases/40-citations-full-round-trip/40-RESEARCH.md
@.planning/phases/40-citations-full-round-trip/40-VALIDATION.md
@.planning/phases/40-citations-full-round-trip/40-03-SUMMARY.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Skipping step 1 makes the worktree's pytest import the MAIN checkout's `typsphinx` package. This plan
judges a test by whether it goes green against the merged translator, so a provisioning miss produces
exactly the wrong verdict.

`git stash` is FORBIDDEN in this worktree -- `refs/stash` is shared across worktrees (#3542). Task 3's
RED re-proof uses `git checkout <sha> -- <path>` and restores with `git checkout HEAD -- <path>`.
</environment_setup>

<orchestrator_measurements>
Reproduced by the orchestrator on the merged tree at `191aefa`, before this plan was written. The
executor re-reproduces each before touching the line it blames; these are provided so a task is not
mistaken for speculation, not as a substitute for measuring.

`uv run python -m pytest tests/test_citation_render_gate.py -q` -> `4 failed, 5 passed`.

1. `test_link_citing_site_targets_match_definition_anchors_and_own_ids` — raises at
   `tests/test_citation_render_gate.py:275` inside `_expected_own_id_anchors`:
   `sphinx.deprecation.RemovedInSphinx11Warning: 'tags' will become a required keyword argument for
   global_toctree_for_doc() in Sphinx 11.0.`
2. `test_backref_markers_order_and_pdf_link_geometry` — raises at `:326` inside
   `_citing_site_own_anchors`, identical cause.
3. `test_separator_paragraph_concat_and_list_item_boundaries` — asserts
   `'text("Concat Term ") + link(<index:concat2000>,'` is in the concat region; the region actually
   holds `text("Concat Term ") + [#link(<index:concat2000>, text("[Concat2000]"))#label("index:id7")]`.
4. `test_layout_hanging_indent_and_widest_label_alignment` — `alpha_column` came back 0 and
   `continuation_column` 28.

`pypdf` `extraction_mode="layout"` over a freshly built `index.pdf`, References page (verbatim,
leading integer is each line's own leading-whitespace count):

```
   0 | '[Krizhevsky2012] (1,2)      CITORDERALPHA Krizhevsky, A., Sutskever, I., & Hinton, G. E. (2012).'
  28 | '                            ImageNet classification with deep convolutional neural networks. Advances'
  28 | '                            in neural information processing systems, 25. This entry body is padded'
   0 | '[Solo1998]                  CITORDERBRAVO Solo, J. (1998). A single-line reference entry.'
   0 | '[Never1999]                 CITORDERCHARLIE Never, N. (1999). An uncited reference entry - D-07's'
  28 | '                            plain, non-linked label case.'
   0 | '[Same2020]                  CITORDERDELTA Same, S. (2020). The duplicate-key entry, defined again in'
  28 | '                            second.rst - D-10's definition-side namespacing case.'
   0 | '[Concat2000]                CITORDERECHO Concat, C. (2000). A "quoted" reference with a cafe'
  28 | '                            character, exercising the existing escape_typst_string path.'
```

Read off that block: every entry's body begins at column 28, every continuation line begins at column
28, and the widest label `[Krizhevsky2012] (1,2)` occupies columns 0..27. That is exactly what CIT-02's
hanging indent and D-05's widest-label alignment assert. The value 28 is recorded HERE, in evidence,
and must NOT be transcribed into the test module.
</orchestrator_measurements>

<tasks>

<task type="auto">
  <name>Task 1: Repair the two deprecated-API helpers and the two stale visit_reference comments</name>
  <read_first>
    - `tests/test_citation_render_gate.py` lines 260-340 -- `_expected_own_id_anchors`,
      `_citing_site_own_anchors`, and the module docstring's claim at line 26 about
      `env.get_and_resolve_doctree`.
    - `.venv/lib/python3.13/site-packages/sphinx/environment/__init__.py`, the
      `get_and_resolve_doctree` signature and its deprecation branch -- confirm for yourself that the
      branch assigns `tags = builder.tags` when `tags` is omitted, which is what makes passing
      `tags=builder.tags` behaviour-preserving rather than a behaviour change.
    - `pyproject.toml` `[tool.pytest.ini_options] filterwarnings` -- the
      `error::PendingDeprecationWarning` line and the comment explaining why Sphinx's
      `RemovedInSphinxNNWarning` family needed it.
    - `tests/test_citation_render_gate.py` lines 490-500 and 630-640 -- the two comments asserting
      `visit_reference` is unmodified by this phase.
  </read_first>
  <files>tests/test_citation_render_gate.py</files>
  <action>
First reproduce, then fix. Run `uv run python -m pytest tests/test_citation_render_gate.py -q --tb=short`
and confirm with your own eyes that `test_link_...` and `test_backref_...` both terminate inside a
helper on `RemovedInSphinx11Warning`, before any citation assertion executes. Record the verbatim
text; Task 3 needs it.

Pass `tags=builder.tags` at both `env.get_and_resolve_doctree` call sites. Change nothing else about
either helper -- not what it iterates, not what it returns, not what the callers compare it against.
This is a call-signature repair, not an expectation change, and the SUMMARY must be able to say so
with a `git diff` that shows exactly two changed argument lists.

If the module docstring near line 26 describes the helper's use of that API, leave its meaning intact;
amend it only if it would otherwise read as false.

Then correct the two stale comments. Both currently tell a later reader that `visit_reference` is
unmodified by this phase and that the surrounding assertion is a pre-fix CONTROL. 40-03 Task 1
modified `visit_reference` to add D-14's guarded own-`ids` bracket-wrap, so both claims are now false.
Rewrite each to say what is actually true: `visit_reference`'s pre-existing refid and xref branches are
unchanged, and this phase ADDED the own-`ids` anchor on top of them. Do not delete the comments and do
not weaken the assertions they sit above.
  </action>
  <acceptance_criteria>
    - `grep -c 'tags=builder.tags' tests/test_citation_render_gate.py` is 2.
    - `grep -n 'get_and_resolve_doctree(' tests/test_citation_render_gate.py` shows no remaining call without a `tags=` keyword.
    - `uv run python -m pytest tests/test_citation_render_gate.py -k "link or backref" -q` no longer raises `RemovedInSphinx11Warning` from any helper; both selectors either pass or fail on a citation assertion.
    - No comment in the file still claims `visit_reference` is unmodified by this phase.
    - `git diff -- tests/test_citation_render_gate.py` for this task shows no change to any `assert` statement.
    - `uv run black --check tests/test_citation_render_gate.py` and `uv run ruff check tests/test_citation_render_gate.py` both pass.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'tags=builder.tags' tests/test_citation_render_gate.py)" = "2" &amp;&amp; ! grep -n 'get_and_resolve_doctree(' tests/test_citation_render_gate.py | grep -v 'tags=' | grep -q 'builder)' &amp;&amp; uv run black --check tests/test_citation_render_gate.py &amp;&amp; uv run ruff check tests/test_citation_render_gate.py; uv run python -m pytest tests/test_citation_render_gate.py -k "link or backref" -q --tb=short</automated>
  </verify>
  <done>Neither helper raises a deprecation error, both affected selectors reach their citation assertions, and no assert statement changed.</done>
</task>

<task type="auto">
  <name>Task 2: Make the layout and concat assertions measure what they claim to measure</name>
  <read_first>
    - `tests/test_citation_render_gate.py` lines 344-402 -- `_leading_columns`,
      `_line_after_marker`, `_find_page_and_column`, and every call site of `_find_page_and_column`
      in the module (find them all before changing anything; a helper corrected at one site and left
      at another leaves half a comparison measuring the wrong quantity).
    - `tests/test_rubric_indent_invariance.py` lines 165-200 -- the origin of `_leading_columns`, and
      the construct there in which the marker IS the first glyph on its line, so that the reason the
      copy is wrong HERE is understood rather than assumed.
    - `tests/test_citation_render_gate.py` lines 812-870 -- the whole `test_layout_...` body.
    - `tests/test_citation_render_gate.py` lines 623-660 -- the whole `test_separator_...` body and
      its three lettered sub-checks.
    - `.planning/phases/40-citations-full-round-trip/40-CONTEXT.md` D-05, D-06 and D-14.
    - This plan's `<orchestrator_measurements>` block.
  </read_first>
  <files>tests/test_citation_render_gate.py</files>
  <action>
**The layout half.** The defect is that the column of a SENTINEL is being read as the leading
whitespace of the LINE that holds it. On an entry's first line the label cell sits ahead of the
sentinel, so the answer is the label's column, not the body's.

Introduce a marker-column measurement -- the index at which the marker substring begins within its
line -- and use it wherever the module means "the column at which this entry's body starts". Keep a
leading-whitespace measurement for the CONTINUATION line, where it is the correct quantity because a
continuation line carries no label. Comparing a marker column against a continuation line's leading
whitespace is then an apples-to-apples comparison of two x positions.

Decide deliberately whether to correct `_find_page_and_column` in place or to add a sibling helper,
based on what its other call sites actually need, and state the reason in the helper's docstring. Do
not leave two helpers whose names do not distinguish which quantity they return.

After the correction the three claims `test_layout_...` always intended must hold and must be stated
relationally:
  - the wrapped continuation line of `CITORDERALPHA`'s entry begins at the same column as
    `CITORDERALPHA` itself (CIT-02's hanging indent);
  - that column is strictly greater than zero (the body sits past the label, not under it);
  - `CITORDERBRAVO`, `CITORDERCHARLIE`, `CITORDERDELTA` and `CITORDERECHO` all begin at that same
    column (D-05's single-grid, widest-label alignment).
Keep the D-06 counterpart exactly as authored: `CITBREAKONESENTINEL` and `CITBREAKTWOSENTINEL` are two
independently-aligned runs, so their columns may differ and only `> 0` is asserted of each.

No numeric column may be written into the file. The observed value is recorded in this plan's
evidence block and in `40-GATE-EVIDENCE-01.md`, and that is where it stays.

**The concat half.** Sub-check (b) of `test_separator_...` demands a bare `link(` immediately after
the `+`. D-14 wraps a citation-derived citing site in `[#link(<...>, ...)#label("...")]`, so the
demand contradicts a decision this same phase implemented. What SC#5's concat protocol actually
requires is that the term's leading text and the citing reference are joined by the concat operator
with no operator left without a right operand.

Rewrite the assertion to test that property against the region, tolerating the D-14 bracket-wrap:
the leading `text("Concat Term ")` is followed by the `+` operator, the operator's right operand is
the citing reference carrying the expected `concat2000` target, and no `+` in the region is left
without a right operand. Keep deriving the expected target through the module's existing
`_expected_namespace_label` helper -- never as a literal. Keep the assertion region-scoped via
`_slice`; a document-wide search would green on any unrelated construct.

Sub-checks (a) and (c) are untouched.
  </action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_citation_render_gate.py -q` reports 9 passed, 0 failed.
    - No integer column literal was introduced: `grep -n '28' tests/test_citation_render_gate.py` shows no occurrence used as an expected column, and the string `28` appears in no assertion.
    - No anchor, namespaced label or link-target token appears as a string literal in any assertion; every such value is still extracted from emitted output or computed via `_expected_namespace_label` / the translator's own helpers.
    - The D-06 counterpart still asserts only `> 0` for `CITBREAKONESENTINEL` and `CITBREAKTWOSENTINEL` and still permits their columns to differ.
    - `test_separator_...` still has all three lettered sub-checks and sub-checks (a) and (c) are byte-unchanged.
    - No test was deleted, renamed, skipped, xfailed or narrowed; `uv run python -m pytest tests/test_citation_render_gate.py --collect-only -q` still collects 9 tests, and each of the eight `-k` selectors `compile`, `link`, `namespace`, `separator`, `uncited`, `layout`, `backref`, `order` still selects at least one.
    - `uv run black --check tests/test_citation_render_gate.py` and `uv run ruff check tests/test_citation_render_gate.py` both pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_citation_render_gate.py -q --tb=short &amp;&amp; test "$(uv run python -m pytest tests/test_citation_render_gate.py --collect-only -q 2>/dev/null | grep -c '::')" = "9" &amp;&amp; for k in compile link namespace separator uncited layout backref order; do uv run python -m pytest tests/test_citation_render_gate.py -k "$k" --collect-only -q >/dev/null || exit 1; done &amp;&amp; uv run black --check tests/test_citation_render_gate.py &amp;&amp; uv run ruff check tests/test_citation_render_gate.py</automated>
  </verify>
  <done>All nine tests pass against the merged translator, with every assertion still relational and no observed value transcribed into the module.</done>
</task>

<task type="auto">
  <name>Task 3: Re-prove the corrected module still goes RED against the unfixed translator, then record the amendments</name>
  <read_first>
    - `.planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md` in full -- the format, and
      the RED it recorded. That record is appended to, never rewritten and never deleted.
    - `.planning/phases/40-citations-full-round-trip/40-02-SUMMARY.md` and the Roadmap Evolution bullet
      40-02 added -- the house style for an amendment made against measurement rather than waived.
    - `.planning/REQUIREMENTS.md` -- the CIT-01..CIT-06 checkbox list and the traceability table rows.
  </read_first>
  <files>.planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md, .planning/REQUIREMENTS.md</files>
  <action>
**Commit Tasks 1 and 2 before starting this task.** The re-proof below temporarily replaces
`typsphinx/translator.py` in your worktree; doing it with uncommitted test fixes risks losing them.

**The re-proof.** A corrected test is only worth something if it can still fail. Restore the
pre-40-03 translator over that one path, run the module, and restore:

```
git checkout 8b22bf6eb74e9f8153757c53d18f2f06da7bcdae -- typsphinx/translator.py
uv run python -m pytest tests/test_citation_render_gate.py -v      # capture verbatim
git checkout HEAD -- typsphinx/translator.py
```

Do NOT use `git stash` (forbidden here, #3542). After restoring, assert that
`git status --porcelain` is empty and `git diff HEAD --stat` is empty before continuing; if either is
not, stop and report rather than committing.

Expected: every one of the nine tests is RED against the unfixed translator. If ANY test passes there,
that test has lost its ability to discriminate -- halt, do not commit a green gate, and report which
test and why. Do not "fix" it by tightening it until it fails; report and stop.

Capture the verbatim RED output and the verbatim green output from Task 2 for the record.

**The amendments.** Append a new dated section to `40-GATE-EVIDENCE-01.md` -- do not touch anything
already in it. For each of the four corrections record: which test and which helper; the verbatim
failure text as measured; what the assertion could and could not have detected in its original form
(for the layout case, state explicitly that it could never have passed in EITHER direction and so was
incapable of validating CIT-02 or D-05 at all); what it detects now; and the evidence the corrected
form is not a transcription of observed output. Reproduce the `pypdf` layout block from this plan's
`<orchestrator_measurements>` as the measurement that justifies the layout amendment, and state that
the value 28 lives in this evidence file and nowhere in the test module. Close the section with the
re-proof: the nine RED results against `8b22bf6`, and an explicit statement that the module's ability
to fail was verified rather than assumed. Frame all four as amended against measurement, never waived.

**The requirements.** In `.planning/REQUIREMENTS.md`, tick CIT-02, CIT-03 and CIT-04 -- now proven by
the corrected gate -- and CIT-05, which `tests/test_examples_charged_ieee_gate.py` has proven green
since Wave 2 with the module provably never edited across the phase (`git diff ccb37b2..HEAD --
tests/test_examples_charged_ieee_gate.py` is empty); 40-03 left it unticked only out of caution. Move
the matching traceability rows from Pending to Complete. Use scoped `Edit` calls; never a whole-file
`Write`. Change no other requirement's state.
  </action>
  <acceptance_criteria>
    - The re-proof ran and its verbatim output is recorded; all nine tests were RED against `8b22bf6`'s `typsphinx/translator.py`.
    - After the re-proof, `git status --porcelain` is empty and `git diff --stat -- typsphinx/` against this plan's own commits is empty -- the temporary restore left nothing behind.
    - `40-GATE-EVIDENCE-01.md` gained a new dated amendment section covering all four corrections, and everything it recorded before this plan is byte-unchanged.
    - The amendment section states, for the layout case, that the original assertion could never have passed in either direction.
    - `.planning/REQUIREMENTS.md` has CIT-01..CIT-06 all ticked and all six traceability rows Complete; no other requirement's state changed.
    - `uv run python -m pytest -m "not slow"` shows no failure anywhere in the suite.
    - `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` all pass.
    - `git diff --stat -- typsphinx/ examples/` is empty for this plan's commits, and `tests/` shows only `tests/test_citation_render_gate.py`.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain)" &amp;&amp; test -z "$(git diff --stat -- typsphinx/ examples/)" &amp;&amp; uv run python -m pytest -m "not slow" -q --tb=short &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <done>The corrected module is proven still able to fail, the four amendments are recorded against measurement, and all six CIT requirements are ticked.</done>
</task>

</tasks>

<verification>
- `uv run python -m pytest tests/test_citation_render_gate.py -v` is green across all eight selectors,
  still collecting nine tests.
- The same module run against `8b22bf6`'s `typsphinx/translator.py` is RED across all nine.
- `uv run python -m pytest -m "not slow"` has no failure.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
- `git diff --stat -- typsphinx/ examples/ pyproject.toml uv.lock` is empty for this plan's commits.
- The only test module in this plan's diff is `tests/test_citation_render_gate.py`.
</verification>

<success_criteria>
- Four defective assertions now measure the quantity each always claimed to measure, and none was
  weakened, skipped or deleted to get there.
- The corrected module was proven still able to fail against the unfixed translator, not assumed to be.
- No observed measurement and no label token was transcribed into the test module.
- Each correction is recorded in `40-GATE-EVIDENCE-01.md` as an amendment against measurement, leaving
  the originally-recorded RED intact.
- All six CIT requirements are ticked with the evidence that supports each.
</success_criteria>

<output>
Create `.planning/phases/40-citations-full-round-trip/40-05-SUMMARY.md` when done.
</output>
