---
phase: 38-structural-indentation-info-fields
plan: 08
type: execute
wave: 5
depends_on: ["38-05", "38-06", "38-07"]
files_modified:
  - tests/test_signature_page_boundary_render_gate.py
  - tests/test_signature_typography_multi_signature_page_count_gate.py
  - .planning/phases/38-structural-indentation-info-fields/38-TEST-CENSUS.md
  - .planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE.md
autonomous: true
requirements: [IND-01, IND-02, IND-03, IND-04, IND-05, FLD-01, FLD-02, FLD-03]

must_haves:
  truths:
    - "Every page-count constant this phase moved is RE-MEASURED against the post-phase build, once, after every byte change has landed -- and each is recorded with its before value, its after value, and the reason it moved, so a page count is never confused with a regenerated expected string."
    - "The second folded todo is closed: the constant in the page-boundary gate that is named for a pre-phase value but holds a post-Phase-37 one is renamed to describe what it actually holds, and its sole consumer is updated in the same commit."
    - "D-08's whole-document claim is checked, not inherited: this project's own documentation is built to PDF and its page count recorded against the 97-to-87 figure the discussion session measured, with any difference explained rather than absorbed."
    - "The full-corpus slow gate is run explicitly before the phase is offered for verification, because it is excluded from the default run and is the milestone's standing practice."
    - "38-TEST-CENSUS.md is finalised against reality: every Bucket A row records whether it flipped at the plan that was predicted to own it, every Bucket B row records that it stayed green, every Bucket C row records its re-verified outcome, and every unpredicted break recorded by an implementation plan is folded in as an honest miss rather than silently absorbed."
    - "D-13's leave-in-place disposition and D-14's migration strategy are both restated at closeout with their outcome, so neither is left as a planning-time intention with no recorded result."
    - "The milestone's supply-chain invariants are verified mechanically at phase close: no runtime dependency was added, the preview package count is unchanged, and no new version-lockstep site exists."
    - "IND-05 is recorded as ASSERTED rather than implemented, with the reason -- under D-01 no nesting-depth counter exists, so the requirement's wording about a counter resetting is satisfied by proving depth cannot leak, not by building a counter to reset."
    - "The phase's aesthetic goal is checked by a human against a real rendered PDF, because 'the page shows structure' is not reducible to a numeric assertion -- the automated gates prove the mechanics, not that the result reads well."

  artifacts:
    - .planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE.md
    - .planning/phases/38-structural-indentation-info-fields/38-TEST-CENSUS.md

  key_links:
    - "the page-count re-measure -> 38-TEST-CENSUS.md Bucket D, the bucket that separates measurements from expected strings"
    - "the rename -> .planning/todos/pending/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md and its sole consumer test_page_count_does_not_inflate"
    - "the whole-document page-count check -> 38-EMISSION-CONTRACT.md section 4.4 and 38-CONTEXT.md D-08"
    - "the corpus gate -> tests/test_corpus_gate.py, marked slow and excluded from the default run, with a cached corpus at ~/.cache/typsphinx-corpus-gate/sphinx-v9.1.0/"
    - "the supply-chain check -> 38-RESEARCH.md's Package Legitimacy Audit (not applicable, no packages installed) and REQUIREMENTS.md milestone invariants 1 and 2"

  prohibitions:
    - requirement_id: IND-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT close this phase by silently correcting the census to match what actually happened -- a census that was wrong in a recorded way is more useful than one quietly fixed, because the record is what tells the next phase how far to trust its own predictions."
---

<objective>
Close Phase 38: re-measure every page count the phase moved, rename the misnamed page-count constant,
verify the measured whole-document consequence against this project's own documentation, run the
full-corpus gate, finalise the census against reality, and consolidate the phase's gate evidence.

Purpose: this phase's changes move page counts in both directions — the indent and the inline field
bodies shorten the document, the proportional-to-monospace move widens field-list lines — so a single
re-measure after every byte change has landed is the only honest point to re-pin them. Everything else
here is the phase's own record: what was predicted, what actually happened, and where the two differed.

Output: two re-measured test constants, a finalised census, a consolidated evidence file, and a human
sign-off on the rendered result. This plan makes no change to `typsphinx/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-structural-indentation-info-fields/38-EMISSION-CONTRACT.md
@.planning/phases/38-structural-indentation-info-fields/38-CONTEXT.md
@.planning/phases/38-structural-indentation-info-fields/38-TEST-CENSUS.md
@.planning/phases/38-structural-indentation-info-fields/38-VALIDATION.md
@.planning/phases/38-structural-indentation-info-fields/38-05-SUMMARY.md
@.planning/phases/38-structural-indentation-info-fields/38-06-SUMMARY.md
@.planning/phases/38-structural-indentation-info-fields/38-07-SUMMARY.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Building this project's own `docs/source` outside `tox` additionally needs the `docs` extra
(`sphinx-autodoc-typehints` and `sphinx-intl`), which the default dev environment does not install —
use `tox -e docs-pdf` for that step rather than a bare `sphinx-build`.
</environment_setup>

<artifacts_this_phase_produces>
See `38-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan renames
`EXPECTED_PAGE_COUNT_PRE_PHASE` in `tests/test_signature_page_boundary_render_gate.py` (the second
folded todo) and creates
`.planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE.md`. It creates no source
symbol.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the page counts, rename the misnamed constant, verify D-08's whole-document claim</name>
  <read_first>
    - `.planning/todos/pending/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md` (full file)
    - `tests/test_signature_page_boundary_render_gate.py` — the constant at line 109, the comment above it recording its full history, and its sole consumer `test_page_count_does_not_inflate`
    - `tests/test_signature_typography_multi_signature_page_count_gate.py` — its own page-count constant and its consuming assertion
    - `.planning/phases/38-structural-indentation-info-fields/38-TEST-CENSUS.md` Bucket D
    - `.planning/phases/38-structural-indentation-info-fields/38-EMISSION-CONTRACT.md` §4.4 (the measured 97-to-87 whole-document figure) and §5.6 (the monospace width note, the countervailing widening effect)
    - `.planning/phases/38-structural-indentation-info-fields/38-CONTEXT.md` D-08
  </read_first>
  <files>tests/test_signature_page_boundary_render_gate.py, tests/test_signature_typography_multi_signature_page_count_gate.py</files>
  <action>
Re-measure and re-pin both page-count constants, once, now that every byte change has landed.

For each: build its fixture, count the pages of the compiled PDF, and record the before value, the
after value, and the reason it moved. This phase pushes the count in two directions at once — the body
indent and the inline single-value field bodies shorten a document (the discussion session measured 97
pages to 87 on this project's own docs), while moving field-list parameter names and types from
proportional to monospace widens those lines and can push text onto more lines. State which effect
dominated for each fixture rather than asserting the direction.

A page count is a MEASUREMENT, not a hand-derived expected string. Re-measuring it against the
post-phase build is legitimate and required; pasting an emitted `.typ` fragment into a golden is not.
Keep that distinction explicit in the commit message and the SUMMARY, because the two operations look
superficially similar and only one is permitted under milestone invariant #4.

Close the second folded todo in the same commit: the page-boundary gate's constant is named for a
pre-phase value but has held a post-Phase-37 one since plan 37-09 re-pinned it, and this phase moves it
again. Rename it to describe what it actually holds, update its sole consumer, and extend the comment
above it with this phase's own re-measure so the value's provenance stays complete rather than being
replaced.

Then verify D-08's whole-document claim rather than inheriting it. Build this project's own
documentation to PDF through `tox -e docs-pdf` and record the resulting page count against the 97-to-87
figure. If it differs, explain the difference — a different environment, a docs change since the
measurement, or a real divergence — rather than absorbing it silently. If the build cannot run in this
environment, say so explicitly with the error and record the claim as unverified in this phase rather
than as confirmed.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_signature_page_boundary_render_gate.py tests/test_signature_typography_multi_signature_page_count_gate.py -v` PASSES.
    - Both page-count constants hold a value measured in this task, and the SUMMARY records before, after and the dominant reason for each.
    - The page-boundary gate's constant no longer carries a name that describes a pre-phase value; its sole consumer is updated in the same commit and `uv run ruff check .` reports no undefined name.
    - The comment above that constant records this phase's re-measure in addition to its existing history, rather than replacing it.
    - The SUMMARY states explicitly that a page count is a measurement and not a hand-derived expected string, and that no expected string was regenerated from the new code's output in this task.
    - The SUMMARY records the `tox -e docs-pdf` page count against the 97-to-87 figure with an explanation for any difference, or records the claim as unverified with the verbatim error that prevented the build.
    - `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_signature_page_boundary_render_gate.py tests/test_signature_typography_multi_signature_page_count_gate.py -v; uv run black --check . &amp;&amp; uv run ruff check .</automated>
  </verify>
  <done>Both page-count constants are re-measured once against the post-phase build with their movement explained, the misnamed constant is renamed with its provenance preserved, and D-08's whole-document claim is checked rather than inherited.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full-corpus gate and the milestone supply-chain checks</name>
  <read_first>
    - `tests/test_corpus_gate.py` — the slow marker, the cached corpus location, and what the gate actually asserts
    - `.planning/phases/38-structural-indentation-info-fields/38-VALIDATION.md` § "Sampling Rate" (the before-verification requirement)
    - `.planning/REQUIREMENTS.md` lines 237-256 (milestone invariants 1, 2 and 3)
    - `.planning/phases/38-structural-indentation-info-fields/38-RESEARCH.md` § "Package Legitimacy Audit"
  </read_first>
  <files>.planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE.md</files>
  <action>
Run the checks that only make sense once the whole phase has landed, and record them in the
consolidated evidence file.

**Full-corpus gate.** Run `uv run pytest tests/test_corpus_gate.py -m slow` explicitly — it is excluded
from the default run by the not-slow marker and is the milestone's standing practice before a phase is
offered for verification. Record the verbatim result and the runtime. If it surfaces a document shape
this phase's fixtures did not contain, that is a real finding: record it, and either fix it in this
phase or file it as a todo with the reason it is out of scope. Do not pass it over silently. Research's
Open Question 1 names the specific shape to watch for — a single-paragraph field body whose paragraph
contains something other than pure inline content — so check the corpus log for it explicitly.

**Supply-chain invariants (milestone invariants 1 and 2).** Verify mechanically and record each
command with its output:

- `git diff <phase-base-commit> -- pyproject.toml uv.lock` is empty — no runtime dependency was added.
- The count of preview package imports across `typsphinx/` is unchanged at four, and no new
  version-lockstep site exists. `tests/test_preview_version_sync.py` is the standing gate for the three
  declaration sites; run it and record the result.
- No new file was added under `typsphinx/` — this phase adds no template asset and no module.

**Whole-suite final state.** Run `uv run pytest -m "not slow" -q` and record the verbatim summary,
compared by set difference over node ids against plan 38-04's recorded pre-phase baseline. Every id that
flipped must be accounted for by a census row or a recorded miss. State the final count of failures,
which must be zero.

**Requirement verdict table.** Record one row per requirement — IND-01 through IND-05, FLD-01 through
FLD-03 — with the node id or ids that prove it and its verdict. For IND-05, record explicitly that it is
ASSERTED rather than implemented: under D-01 no nesting-depth counter exists, so the requirement's
wording about a counter resetting is satisfied by proving depth cannot leak, not by building a counter.
For IND-04, quote the closing SC#4 grep and compare it to the one plan 38-01 recorded at discovery time.
  </action>
  <acceptance_criteria>
    - `.planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE.md` exists and contains the verbatim full-corpus gate result with its runtime.
    - The corpus section explicitly addresses Research Open Question 1's shape, with either a finding or a statement that the corpus contains no instance of it.
    - The file records the dependency diff, the preview-package count, the preview version-sync gate result, and the no-new-file check, each with its command and output.
    - The file contains the whole-suite final state with a set-difference statement against plan 38-04's baseline and a final failure count of zero.
    - The file contains a requirement verdict table with one row per requirement id and a proving node id for each.
    - The IND-05 row states that it is asserted rather than implemented, with the reason.
    - The IND-04 row quotes the closing SC#4 grep and compares it to the discovery-time run.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_corpus_gate.py -m slow -q; uv run pytest tests/test_preview_version_sync.py -v; uv run pytest -m "not slow" -q</automated>
  </verify>
  <done>The full-corpus gate and every milestone supply-chain invariant are verified and recorded, and each of the eight requirements has a verdict backed by a named node id.</done>
</task>

<task type="auto">
  <name>Task 3: Finalise the census against reality and sign off the rendered result</name>
  <read_first>
    - `.planning/phases/38-structural-indentation-info-fields/38-TEST-CENSUS.md` (full file)
    - `.planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md` § "Finalisation against reality" — the shape to mirror, including its honest-miss section
    - `.planning/phases/38-structural-indentation-info-fields/38-05-SUMMARY.md`, `38-06-SUMMARY.md`, `38-07-SUMMARY.md` — every recorded set difference and every recorded census miss
    - `.planning/phases/38-structural-indentation-info-fields/38-VALIDATION.md` § "Manual-Only Verifications"
  </read_first>
  <files>.planning/phases/38-structural-indentation-info-fields/38-TEST-CENSUS.md</files>
  <action>
Append a finalisation section to the census, cross-checked against the three implementation plans'
SUMMARYs and the requirement verdict table from Task 2.

Record, per bucket:

- **Bucket A predicted versus actual.** One row per predicted node id: the plan predicted to own it, the
  plan and commit where it actually flipped, and whether they match. State the hit rate.
- **Bucket B.** Confirm each predicted stays-green file held throughout every wave, evidenced by the
  set-difference statements rather than by a fresh run.
- **Bucket C.** Record each conditional row's re-verified outcome and the plan whose decision settled it.
- **Bucket D.** Record each page count's before and after and the plan that re-measured it.
- **Misses.** Fold in every unpredicted break the implementation plans recorded, with the reason the
  census could not have predicted it. Do not silently amend an earlier bucket to make the prediction
  look right — a census that was wrong in a recorded way is more useful than one quietly fixed, and that
  record is what tells the next phase how far to trust its own predictions.

Restate the two dispositions that were taken at plan time so neither is left as an intention with no
result: D-13's stray break was left in place, and the test that pins its shape is still green; and
D-14's migration strategy was per-plan hand-derivation with the golden file hand-edited and confirmed by
rebuild rather than regenerated — state whether that actually held for the golden, quoting plan 38-05's
own evidence.

Finally, close the two folded todos: move each from the pending directory to wherever this project
files completed todos, or mark them resolved in place following whatever convention the repository
already uses for a closed todo, and record which convention was used.
  </action>
  <acceptance_criteria>
    - `38-TEST-CENSUS.md` contains a finalisation section with per-bucket predicted-versus-actual tables and a stated Bucket A hit rate.
    - Every unpredicted break recorded by plans 38-05, 38-06 or 38-07 appears in the misses section with a reason.
    - No earlier bucket row was edited to make a prediction look correct — verified by `git diff` on the census showing the finalisation section as an addition.
    - The census restates D-13's outcome with the still-green pinning test named, and D-14's outcome with plan 38-05's golden-file evidence quoted.
    - Both folded todos are closed following the repository's existing convention, and the SUMMARY names the convention used.
    - `uv run pytest -m "not slow" -q` reports zero failures.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -m "not slow" -q; git diff --stat .planning/phases/38-structural-indentation-info-fields/38-TEST-CENSUS.md</automated>
    <human-check>
Build this project's own documentation to PDF with `tox -e docs-pdf` and page through the API reference
section. Confirm, by eye:

1. A class's methods are visually recoverable as belonging to that class — the indent makes membership
   obvious at a glance rather than requiring the reader to track signatures.
2. A nested member's signature lines up with its parent's body text, not one step further right.
3. A Parameters block sits visibly inside its own description body and does not read as detached from
   it.
4. A `Returns:` line reads as one sentence with its value, and `Returns` / `Return type` / `Raises` are
   still visually separate entries rather than one run of text.
5. Parameter names and types in a field body are visibly monospace and visibly different from the bold
   `Parameters:` label.
6. Nothing runs past the right margin at the deepest nesting level present in the document.

Reply "approved" or describe what reads wrong. This is the phase goal's aesthetic half — the automated
gates prove the mechanics, not that the result reads well — and it blocks nothing automatically.
    </human-check>
  </verify>
  <done>The census records what was predicted against what happened, including every miss, both folded todos are closed, and the rendered result has been looked at by a human against the phase's stated goal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's own record → the next phase's planning | Where a silently corrected census becomes a false confidence signal |
| `pyproject.toml` / `uv.lock` / `@preview` imports → the shipped package | The supply-chain surface the milestone invariants protect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-SC | Tampering | Supply chain — a runtime dependency, a preview package, or a new version-lockstep site slipping in under this phase's work | high | mitigate | Verified mechanically in Task 2: an empty dependency diff against the phase base, an unchanged preview package count, the standing version-sync gate green, and a no-new-file check over the package directory. `38-RESEARCH.md`'s Package Legitimacy Audit records that this phase installs no packages, so no legitimacy checkpoint applies. |
| T-38-18 | Repudiation | The census silently corrected at closeout so its predictions look better than they were | medium | mitigate | The finalisation section is an append-only addition, verified by `git diff --stat`, and the misses section is mandatory. Carried as a `must_haves` prohibition. |
| T-38-19 | Denial of service | A corpus-wide document shape this phase's fixtures did not contain, breaking a real build only after release | high | mitigate | The full-corpus gate is run explicitly before verification and Research Open Question 1's specific shape is checked for by name; a finding is either fixed in-phase or filed with its reason. |
| T-38-02 | Denial of service | A page count re-pinned in a way that blurs into regenerating an expected string from the new code's output | high | mitigate | Bucket D separates measurements from expected strings, the re-measure happens once after all byte changes, and the SUMMARY must state the distinction explicitly. |

ASVS L1: V5 Input Validation only; this plan changes no code and adds no emission site. Blocking
threshold is `high`; all three `high` threats are `mitigate` with mitigations inside this plan. No
package install task exists, so the package-legitimacy checkpoint does not apply — its absence is
itself verified by the dependency diff above.
</threat_model>

<verification>
- Both page-count constants are re-measured, pass, and their movement is explained.
- The misnamed constant is renamed with its sole consumer updated and its provenance comment extended.
- `uv run pytest tests/test_corpus_gate.py -m slow` passes and its result is recorded.
- The supply-chain invariants are verified mechanically with commands and output recorded.
- `uv run pytest -m "not slow" -q` reports zero failures, verified by set difference against the
  pre-phase baseline.
- Every requirement id has a verdict row backed by a named node id.
- The census's finalisation section exists as an append, including a misses section.
- A human has looked at the rendered PDF against the six checks in the human-check block.
</verification>

<success_criteria>
- Every page count this phase moved is re-measured once, with its direction explained rather than
  assumed.
- Both folded todos are closed.
- D-08's whole-document claim is checked against a real build, or recorded as unverified with the
  reason.
- The full-corpus gate and every milestone supply-chain invariant pass and are recorded.
- The census records what was predicted against what happened, misses included.
- IND-05's assert-not-implement disposition and IND-04's closing grep are both on the record.
- The phase's aesthetic goal has been looked at, not only measured.
</success_criteria>

<output>
Create `.planning/phases/38-structural-indentation-info-fields/38-08-SUMMARY.md` when done.
</output>
