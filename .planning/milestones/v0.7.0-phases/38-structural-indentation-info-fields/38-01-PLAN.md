---
phase: 38-structural-indentation-info-fields
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/desc_content_indent_render_gate/conf.py
  - tests/fixtures/desc_content_indent_render_gate/index.rst
  - tests/test_desc_content_indent_render_gate.py
  - .planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE-01.md
autonomous: true
requirements: [IND-01, IND-02, IND-03, IND-04, IND-05, FLD-01]

must_haves:
  truths:
    # --- goal-backward (this plan) ---
    - "One fixture Sphinx project produces, in a single -b typst build, every doctree shape IND-01..IND-05 and FLD-01 must be judged on: a py:class:: containing a py:method:: containing a py:attribute:: (three levels), a field list inside the nested member, a sibling top-level py:function:: immediately after the nest, a body-less desc, a desc inside a list item, and a desc inside a table cell."
    - "Every IND/FLD-01 left-edge assertion is made with pypdf's extraction_mode=layout leading-space column counts on a really compiled PDF, and every expected .typ token is hand-derived from 38-EMISSION-CONTRACT.md sections 2 and 3 rather than from any translator output."
    - "Each assertion is recorded RED against the pre-phase translator with its verbatim pytest failure output, and the RED is structural -- the absence of the body wrapper's tokens and the equality of two left-edge columns -- never a compile failure, because the pre-phase output compiles fine (milestone invariant #4)."
    - "D-01's measured left-edge table is the acceptance target: class signature at the page margin, class body one step in, nested method signature EQUAL to the class body, nested method body one further step, class body resuming at its own step, sibling top-level desc back at the page margin."
    - "D-02 is honoured: no assertion in this plan pins a point value derived from re-valuing SHARED_INDENT_STEP; every indent assertion is RELATIVE (strictly greater, or equal), so the gate cannot be satisfied by changing the constant."
    - "D-11 is asserted, not assumed: a signature at a page boundary still keeps its body's first line on its own page, and a body crossing a page break keeps its indent on the following page."
    # --- probe edges: IND-01 ---
    - "IND-01 adjacency: a desc_signature and its own desc_content body are never emitted as one merged expression -- the body wrapper opens as a separate code-mode statement after the signature wrapper's closing bytes, and their compiled left-edge columns are strictly unequal."
    - "IND-01 empty: a desc whose desc_content has zero children still emits a well-formed wrapper pair around an empty content block and compiles; the body-less confval pair in tests/fixtures/desc_bodyless_concat_render_gate stays green."
    - "IND-01 ordering: two -b typst builds of this fixture produce byte-identical .typ output, and the body wrapper always opens after the signature's closing bytes and closes before depart_desc's break."
    # --- probe edges: IND-04 ---
    - "IND-04 adjacency: both new indent emission sites interpolate the SHARED_INDENT_STEP name rather than a numeric literal, and block_quote deliberately keeps Typst's own quote() default (measured 11.0pt) instead of being forced onto the shared 27.5pt step (D-04)."
    - "IND-04 empty: a document containing no desc and no field_list emits no indent wrapper at all and its .typ is byte-identical to the pre-phase build."
    - statement: "Two -b typst builds of the same source emit the indent constant's value at every consumer site in the same order and with identical bytes, so no site can observe a different indent value than another within one build."
      verification: backstop
    # --- probe edges: FLD-01 ---
    - "FLD-01 adjacency: the field-list wrapper opens strictly inside the enclosing desc_content wrapper and its compiled left-edge column is strictly greater than the enclosing body's; the two steps never collapse into one."
    - "FLD-01 empty: a desc_content containing ONLY a field list -- the body-less confval shape, no body paragraph -- still emits both wrappers, compiles, and keeps exactly one break between sibling confvals."
    - "FLD-01 ordering: field order in the emitted .typ follows doctree order (Parameters, Returns, Return type, Raises) and is stable across two builds."
    # --- flagged assumptions: unclassified probe rows, deliberately left unresolved ---
    - "FLAGGED ASSUMPTION (edge probe, IND-02, category unclassified): the probe could not classify this requirement into an edge category and it is carried forward UNRESOLVED rather than auto-backstopped. Planner's reading: IND-02's risk is cumulative-depth arithmetic, covered operationally by the three-level nesting assertion in this plan, but no probe-derived predicate is claimed for it."
    - "FLAGGED ASSUMPTION (edge probe, IND-03, category unclassified): carried forward UNRESOLVED. Planner's reading: IND-03's risk is the naive over-indent of a nested signature, covered operationally by the equality assertion (method signature column == class body column) in this plan, but no probe-derived predicate is claimed for it."
    - "FLAGGED ASSUMPTION (edge probe, IND-05, category unclassified): carried forward UNRESOLVED. Planner's reading: IND-05's risk is depth leaking across siblings, covered operationally by the sibling-top-level-desc assertion in this plan, but no probe-derived predicate is claimed for it -- and note D-01 records that IND-05 must be ASSERTED, not implemented, because no depth counter exists."

  artifacts:
    - tests/fixtures/desc_content_indent_render_gate/conf.py
    - tests/fixtures/desc_content_indent_render_gate/index.rst
    - tests/test_desc_content_indent_render_gate.py
    - .planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE-01.md

  key_links:
    - "tests/test_desc_content_indent_render_gate.py -> tests/fixtures/desc_content_indent_render_gate/ via a pytest fixture returning the directory, mirroring tests/test_signature_break_and_arrow_gate.py"
    - "tests/test_desc_content_indent_render_gate.py -> sys.executable -m sphinx -b typst (NEVER a uv run sphinx-build subprocess -- the NixOS stub-ld hazard restated in all three existing render-gate modules)"
    - "every expected .typ token -> 38-EMISSION-CONTRACT.md sections 2 and 3 (hand-derived, ROADMAP SC#5, milestone invariant #4)"
    - "every left-edge assertion -> pypdf extraction_mode=layout column counts (38-RESEARCH.md Pattern 2); the per-glyph position API is proven unusable on this project's PDFs"

  prohibitions:
    - requirement_id: IND-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT make any Phase 38 gate pass by copying the new translator's own output into a golden file or an expected string, and MUST NOT weaken, narrow, or delete a RED fixture so that it can no longer reach its red state -- the phase's evidence is void if the assertion was fitted to the output rather than derived from 38-EMISSION-CONTRACT.md."
    - requirement_id: IND-02
      category: usability
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT let accumulated indentation silently squeeze deeply-nested content past the right page margin -- a reader losing a code block, a table, or the tail of a long signature to overflow is a worse outcome than a flatter page, and the indent exists to make structure recoverable, not to consume the column."
---

<objective>
Stand up the IND-01..IND-05 + FLD-01 structural and left-edge gate: one fixture Sphinx project
exercising every nesting shape this phase must indent, and an assertion module whose expected `.typ`
tokens are hand-derived from `38-EMISSION-CONTRACT.md` §2/§3 and whose left-edge comparisons are read
from a really compiled PDF — all recorded RED against the untouched translator.

Purpose: milestone invariant #4 makes RED structural for v0.7.0. `visit_desc_content` and
`depart_desc_content` are both `pass` today, so the body sits flush with its signature and the output
compiles perfectly — "it compiles" proves nothing. The assertions must exist, and must be proven to
fail, before `typsphinx/translator.py` receives a single edit.

Output: a fixture project, a gate module, and `38-GATE-EVIDENCE-01.md` recording the verbatim RED plus
the SC#4 discovery grep. This plan does NOT touch `typsphinx/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-structural-indentation-info-fields/38-EMISSION-CONTRACT.md
@.planning/phases/38-structural-indentation-info-fields/38-CONTEXT.md
@.planning/phases/38-structural-indentation-info-fields/38-RESEARCH.md
@.planning/phases/38-structural-indentation-info-fields/38-PATTERNS.md
@.planning/phases/38-structural-indentation-info-fields/38-VALIDATION.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Skipping step 1 makes the worktree's pytest import the MAIN checkout's `typsphinx` package, so gates
report the wrong colour and later waves do not see earlier waves' changes.
</environment_setup>

<artifacts_this_phase_produces>
Phase-wide list of every symbol Phase 38 creates (read by the plan-review source-grounding pass so
newly-created symbols are not flagged as drift). Later plans reference this list rather than repeating
it.

**Source symbols (`typsphinx/translator.py`)**
- No new module-level constant. `SHARED_INDENT_STEP` already exists at line 29 (Phase 37 D-08); D-02
  locks its value.
- `visit_desc_content` / `depart_desc_content` — currently `pass`, gain bodies (plan 38-05).
- `visit_field_list` / `depart_field_list` — gain the second `pad` step (plan 38-06).
- `visit_field_body` — gains a single-`paragraph` classification branch (plan 38-06).
- `visit_paragraph` / `depart_paragraph` — gain a field-body fast-path that skips the `par(` wrapper
  (plan 38-06).
- `visit_literal_strong` / `visit_literal_emphasis` — replace the dummy-node delegation with their own
  leaf-emission bodies (plan 38-07).
- One new `TypstTranslator` instance attribute distinguishing a single-paragraph-unwrapped field body
  from a docutils-collapsed inline one, so `depart_field`'s FID-09 separator stays scoped to the
  latter (plan 38-06; working name `_field_body_unwrapped_paragraph`, final name is the executor's
  under D-12).
- `TypstTranslator._desc_break_marker` — existing attribute, changes shape from a bare buffer index to
  a buffer-identifying pair (plan 38-05, folded todo).

**Test symbols**
- `tests/fixtures/desc_content_indent_render_gate/` + `tests/test_desc_content_indent_render_gate.py`
  with helpers `_run_sphinx_build_typst`, `_layout_lines`, `_leading_columns` (this plan).
- `tests/fixtures/field_body_typography_render_gate/` +
  `tests/test_field_body_typography_render_gate.py` with helpers `_expected_bold_mono`,
  `_expected_italic_mono`, `_slice` (plan 38-02).
- `tests/fixtures/desc_break_marker_buffer_swap_gate/` +
  `tests/test_desc_break_marker_buffer_swap_gate.py` (plan 38-03).
- New assertions inside the existing `tests/test_signature_break_and_arrow_gate.py` (plan 38-03).
- `EXPECTED_PAGE_COUNT_PRE_PHASE` in `tests/test_signature_page_boundary_render_gate.py` is RENAMED
  (plan 38-08, folded todo).

**Planning artifacts**
- `38-EMISSION-CONTRACT.md` (authored at plan time), `38-TEST-CENSUS.md` (plan 38-04),
  `38-GATE-EVIDENCE-01.md` / `-02.md` / `-03.md` (wave 1), `38-GATE-EVIDENCE.md` (plan 38-08).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build the IND/FLD-01 nesting fixture</name>
  <read_first>
    - `.planning/phases/38-structural-indentation-info-fields/38-EMISSION-CONTRACT.md` §2 in full (the wrapper shape, the measured left-edge table, the empty-body case, the D-11 page-break property) and §3 (the field-list step)
    - `tests/fixtures/signature_break_and_arrow_gate/index.rst` (full file) and `tests/fixtures/signature_break_and_arrow_gate/conf.py` — the "one fixture, many labelled constructs, each with a docutils comment saying whether it is the defect case or a CONTROL" convention this fixture must mirror
    - `tests/fixtures/desc_bodyless_concat_render_gate/index.rst` — the body-less confval shape that must not regress
    - `tests/fixtures/field_list_in_list_item_render_gate/index.rst` — the `field_list`-inside-a-list-item shape §2.6 names as the control that settles the separator bookkeeping question
    - `.planning/phases/38-structural-indentation-info-fields/38-PATTERNS.md` § "The 'one fixture, many labeled constructs' convention"
  </read_first>
  <files>tests/fixtures/desc_content_indent_render_gate/conf.py, tests/fixtures/desc_content_indent_render_gate/index.rst</files>
  <action>
Create a minimal self-contained Sphinx project that produces, in ONE `-b typst` build, every doctree
shape IND-01..IND-05 and FLD-01 must be judged on. Mirror
`tests/fixtures/signature_break_and_arrow_gate/`'s conventions exactly: a `====` heading per
construct, a `..` docutils comment above each one stating whether it is a defect case or a CONTROL and
which requirement it serves, and a `conf.py` that lists `index` in `typst_documents` so the writer
emits the full template and the document really compiles.

Constructs the fixture must contain, each with a distinct, greppable identifier so an assertion can
locate its line without colliding with another construct's bytes:

1. **The three-level nest (IND-01/02/03).** A `py:class::` with a one-paragraph body, containing a
   `py:method::` with its own one-paragraph body, containing a `py:attribute::` with its own
   one-paragraph body. Per D-01 this single construct supplies IND-01 (body indented past its own
   signature), IND-02 (cumulative with depth) and IND-03 (the nested signature aligns with the parent
   body and receives no further step).
2. **Parent body resuming after the nest (IND-02).** The class body must continue with a further
   paragraph AFTER the nested method, so the "class body continues" row of D-01's measured table is
   reachable.
3. **The sibling top-level function (IND-05).** A top-level `py:function::` with a body paragraph,
   immediately after the three-level nest closes. Per D-01 this is IND-05's whole proof: depth cannot
   leak because the wrapper closes, not because a counter resets. Do not add a counter-shaped
   construct.
4. **A field list inside the nested member (FLD-01).** The `py:method::` carries a `:param:` /
   `:type:` / `:returns:` field list, so the field-list step nests inside the method body's step.
5. **The body-less CONTROL (§2.4).** Two back-to-back body-less `confval::` directives, mirroring
   `tests/fixtures/desc_bodyless_concat_render_gate/index.rst` — a `desc_content` with no children
   still gets a wrapper pair and must still compile, and exactly one break must separate the siblings.
6. **The list-item CONTROL (§2.6).** A `py:function::` nested inside a bullet-list item, so the
   block-visitor separator interaction is exercised and the executor of 38-05 has a real falsifier for
   the leading-newline question D-12 leaves open.
7. **The table-cell CONTROL (§2.1).** A `desc` inside a simple table cell, so the `add_text`-versus-
   `body.append` routing constraint has a falsifier rather than a comment.
8. **The page-boundary case (D-11, SIG-09).** Enough filler paragraphs before a final `py:class::`
   that its signature lands near a page boundary and its body crosses onto the next page. Set a
   deliberately short page geometry in `conf.py` via `typst_elements` so this is reproducible rather
   than incidental, and say so in a comment.
9. **A no-desc, no-field-list CONTROL (IND-04 empty).** A short section containing only ordinary
   paragraphs, so an assertion can prove that a document with neither construct emits no indent
   wrapper at all.
10. **A block quote (D-04).** One `..` -indented block quote with an attribution, so the assertion
    that `block_quote` is deliberately NOT converted to the shared step has a subject in the same
    build.

`conf.py` must set `project`, `author`, `release`, `extensions = ["typsphinx"]` and `typst_documents`,
and carry a header comment naming this fixture's requirements (IND-01..IND-05, FLD-01, D-04, D-11) and
each construct's role — matching the density of `signature_break_and_arrow_gate/conf.py`'s own header.
  </action>
  <acceptance_criteria>
    - `tests/fixtures/desc_content_indent_render_gate/conf.py` and `index.rst` both exist.
    - `index.rst` contains all ten constructs listed above, each preceded by a `..` docutils comment naming its requirement and whether it is a defect case or a CONTROL.
    - `conf.py` contains `extensions = ["typsphinx"]` and a `typst_documents` entry whose first element is `"index"`.
    - `uv run python -m sphinx -b typst tests/fixtures/desc_content_indent_render_gate <tmpdir>` exits 0 and writes an `index.typ`.
    - The emitted `index.typ` compiles: `uv run python -c "import typst,pathlib; typst.compile(str(pathlib.Path('<tmpdir>/index.typ')))"` exits 0.
    - The compiled PDF has more than one page (the D-11 page-boundary construct is reachable).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m sphinx -b typst tests/fixtures/desc_content_indent_render_gate /tmp/ind38fix &amp;&amp; uv run python -c "import typst; typst.compile('/tmp/ind38fix/index.typ')"</automated>
  </verify>
  <done>One fixture project builds and compiles, containing every nesting, control and page-boundary shape IND-01..IND-05 and FLD-01 are judged on.</done>
</task>

<task type="auto">
  <name>Task 2: Write the IND/FLD-01 gate module with hand-derived expectations</name>
  <read_first>
    - `.planning/phases/38-structural-indentation-info-fields/38-EMISSION-CONTRACT.md` §1.1 (the SC#4 grep form), §2 (all subsections), §3
    - `tests/test_signature_break_and_arrow_gate.py:1-140` — `_run_sphinx_build_typst`, `_extract_pdf_text`, the module docstring convention, and the structural-assert / compiled-PDF-assert split
    - `tests/test_pdf_render_gate.py:141-178` — the canonical `sys.executable -m sphinx` subprocess harness
    - `tests/test_signature_typography_gate.py:55-140` — `_slice` and `_extract_wrapped_call`, the region-isolation helpers that stop one construct's assertion from being satisfied by another construct's bytes
    - `.planning/phases/38-structural-indentation-info-fields/38-RESEARCH.md` § Pattern 2 and § Pitfall 3 (the layout-mode extraction technique and the per-glyph API that does not work)
  </read_first>
  <files>tests/test_desc_content_indent_render_gate.py</files>
  <action>
Write the gate module. Clone `tests/test_pdf_render_gate.py`'s `_run_sphinx_build_typst` subprocess
shape verbatim — `sys.executable`, `-m`, `sphinx`, `-b`, `typst` — and never a `uv run sphinx-build`
argv, per the NixOS PATH-shadowing hazard restated in all three existing render-gate modules. Add a
session-scoped fixture that builds the fixture once, compiles the `.typ` through `typst.compile()`,
and exposes both the emitted `.typ` text and the compiled PDF bytes.

Left-edge measurement technique, binding: use `pypdf`'s layout-mode text reconstruction —
`reader.pages[i].extract_text(extraction_mode="layout")` — and compare, per line,
`len(line) - len(line.lstrip(" "))`. Write two helpers: `_layout_lines(pdf_bytes, page_index)` and
`_leading_columns(layout_text, marker_substring)`. The per-glyph `visitor_text` callback reports
`x=0, y=0` on this project's PDFs and is unusable; a test built on it silently passes or fails on
zero regardless of the real position.
<!-- planner-discipline-allow: visitor_text -->

Every left-edge assertion must be RELATIVE — strictly greater, or exactly equal — never a pinned point
value, so the gate cannot be satisfied by re-valuing `SHARED_INDENT_STEP` (D-02).

The assertions, one test function each, each with a docstring naming its requirement and quoting the
contract section its expectation was derived from:

- **IND-01**: the class body paragraph's leading-space column is strictly greater than the class
  signature's.
- **IND-02**: the nested method's body column is strictly greater than the class body's; and the class
  body paragraph that RESUMES after the nested member is back at the class body's own column.
- **IND-03**: the nested method's own SIGNATURE column is exactly EQUAL to the class body's column —
  equality, not `>`. This is the assertion that fails if the naive implementation over-indents.
- **IND-05**: the sibling top-level `py:function::` after the three-level nest is at the same column as
  the top-level class signature, i.e. back at the page margin.
- **FLD-01**: the method's field-list line column is strictly greater than the method body's column.
- **IND-01 structural**: the emitted `.typ` contains the body wrapper's opening token
  `pad(left: 2.5em, {` and its closing token, and the opening token appears AFTER the class
  signature's closing bytes and BEFORE the class body's `par(` — a positional index assertion, not a
  bare substring check.
- **IND-04 structural**: the emitted `.typ` contains the shared step's value at both the `desc_content`
  and the `field_list` site; and, separately, a source-level check over `typsphinx/translator.py` with
  full-line and indented comments filtered out (`grep -vE '^[[:space:]]*#'`) finds exactly ONE numeric
  `em`-suffixed literal, the `SHARED_INDENT_STEP` assignment. Use the exact filtered form from
  contract §1.1; an unfiltered whole-file count is self-invalidating because this repo's docstrings and
  comments discuss `em` values.
- **IND-04 / D-04 non-conversion**: the block-quote construct still emits Typst's own
  `quote(block: true, {` form and is NOT wrapped in the shared step.
- **IND-04 empty**: the no-desc, no-field-list section's region of the `.typ` contains no indent
  wrapper token.
- **FLD-01 empty / IND-01 empty**: the two body-less `confval` siblings each emit a wrapper pair, the
  document compiles, and exactly one break statement separates them.
- **D-11 / SIG-09**: on the page-boundary construct, the signature and the first line of its body are
  on the same page, and the body's continuation on the following page has the SAME leading-space
  column as its first line.
- **Ordering / determinism**: two builds of the fixture into two different output directories produce
  byte-identical `.typ`.

Mark the compiled-PDF tests `@pytest.mark.slow` only if the project's existing render gates do so for
comparable work; check `tests/test_signature_break_and_arrow_gate.py` and follow whatever it does, so
the per-wave `-m "not slow"` sample keeps its stated ~45s budget.

Derive every expected `.typ` token from `38-EMISSION-CONTRACT.md` §2/§3. Do not run the translator and
paste what it printed — that is a milestone invariant #4 violation, not a shortcut.
  </action>
  <acceptance_criteria>
    - `tests/test_desc_content_indent_render_gate.py` exists and contains one test function per bullet listed in the action, each with a docstring naming its requirement id and the contract section its expectation came from.
    - The module builds Sphinx via a `sys.executable`-based argv; a repo grep shows the module contains no `sphinx-build` argv string.
    - The module contains `extraction_mode="layout"` and defines `_layout_lines` and `_leading_columns`.
    - Every left-edge assertion compares two measured columns (`>` or `==`); no test asserts a hard-coded point or column number.
    - `uv run pytest tests/test_desc_content_indent_render_gate.py -v` runs to completion (collection succeeds, no import or fixture errors) and reports at least the IND-01, IND-02, IND-03, IND-05 and FLD-01 tests as FAILED against the untouched translator.
    - No test in the module FAILS with a `TypstError` or any compile error — every failure is a structural or column-comparison mismatch (milestone invariant #4).
    - `uv run black --check .`, `uv run ruff check .` pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_desc_content_indent_render_gate.py -v; uv run black --check . &amp;&amp; uv run ruff check .</automated>
  </verify>
  <done>A gate module exists whose IND-01..IND-05 and FLD-01 assertions are hand-derived from the emission contract, measured with layout-mode column counts, and fail structurally against the pre-phase translator.</done>
</task>

<task type="auto">
  <name>Task 3: Record the verbatim RED and the SC#4 discovery grep</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-01.md` — the evidence-file shape this phase mirrors
    - `.planning/phases/38-structural-indentation-info-fields/38-EMISSION-CONTRACT.md` §1.1
    - `.planning/REQUIREMENTS.md` lines 237-256 (the six milestone invariants, notably #4 and #6)
  </read_first>
  <files>.planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE-01.md</files>
  <action>
Write the evidence file. It must contain, verbatim and unedited:

1. The full `uv run pytest tests/test_desc_content_indent_render_gate.py -v` output against the
   untouched translator, with a per-node-id table of RED versus CONTROL-GREEN and, for each RED, one
   sentence stating WHICH property failed — the wrapper token's absence, or two columns being equal
   where the contract requires strict inequality.
2. An explicit statement that no RED in this module is a compile failure, with the compile step's own
   exit status quoted, satisfying milestone invariant #4's structural-RED redefinition.
3. The SC#4 discovery grep required by milestone invariant #6, run repo-wide over `typsphinx/` at this
   point in time: the exact command, its full output, and a row-by-row disposition of every match
   stating whether it is a desc / field-list / block-quote indent. Contract §1.1 has the planning-time
   run; re-run it here and record any difference rather than copying the table.
4. The measured pre-phase left-edge columns for every marker line the gate compares, so the post-phase
   run has a baseline to diff against rather than only a pass/fail.
5. The two `pypdf` extraction results side by side for one marker line — the layout-mode column count
   that works, and the per-glyph position API's zero result that does not — so the technique choice is
   evidenced in this phase rather than inherited.

If any assertion in the module comes out GREEN against the untouched translator, do not adjust it into
red. Record it, state why it was already true pre-phase, and reclassify it in the file as a
non-regression control.
  </action>
  <acceptance_criteria>
    - `.planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE-01.md` exists.
    - It contains a fenced block holding the verbatim pytest output and a node-id table with a RED/CONTROL-GREEN column.
    - It contains the sentence-level cause for each RED and an explicit statement that none of them is a compile failure.
    - It contains the SC#4 repo-wide grep command, its verbatim output, and a disposition row per match.
    - It contains a table of pre-phase left-edge column counts per marker line.
    - It contains the side-by-side `pypdf` layout-mode versus per-glyph-position comparison for one marker line.
    - Any assertion that was green pre-phase is listed as a reclassified non-regression control with its reason, not silently omitted.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_desc_content_indent_render_gate.py -v 2>&amp;1 | tail -40; test -s .planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE-01.md</automated>
  </verify>
  <done>The RED is recorded verbatim with per-assertion causes, the SC#4 grep is recorded at discovery time, and the pre-phase column baseline exists for the post-phase diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.rst` fixture source → translator | Author-controlled build-time text; the only untrusted-input analog in a build-time tool |
| translator → emitted `.typ` → `typst.compile()` | Where an unescaped or mis-separated emission becomes a build fatal or a rendering defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | Typst string-literal injection via unescaped node text reaching an emission site | medium | accept | Unchanged by this plan — it adds no emission site at all, only fixtures and assertions. The project-wide `escape_typst_string` boundary is untouched; the two new emission sites this phase adds are handled by 38-05/38-07's own registers. |
| T-38-02 | Denial of service | A gate assertion fitted to the translator's output rather than derived from the contract, producing a green that proves nothing | high | mitigate | Every expected token is hand-derived from `38-EMISSION-CONTRACT.md` §2/§3 and the RED is recorded verbatim before any translator edit exists; the prohibition on output-fitted expectations is carried in `must_haves.prohibitions`. |
| T-38-03 | Information disclosure | A left-edge assertion built on `pypdf`'s per-glyph position API, which returns zeroes on this project's PDFs and therefore cannot detect the defect it claims to guard | medium | mitigate | Layout-mode extraction is mandated in the action and evidenced side-by-side against the zero-returning API in `38-GATE-EVIDENCE-01.md`. |
| T-38-SC | Tampering | Supply chain — a new package or `@preview` import slipping in under this phase's work | high | mitigate | No package install task exists in this phase and `38-RESEARCH.md`'s Package Legitimacy Audit records "not applicable — this phase installs no external packages". Verified at phase close by `git diff -- pyproject.toml uv.lock` and a `#import "@preview` count over `typsphinx/`, both recorded in 38-08. |

ASVS L1: V5 Input Validation is the only applicable category (build-time tool, no runtime auth,
session, access-control or cryptography surface). Blocking threshold is `high`; T-38-02 and T-38-SC are
both `high` and both `mitigate`, with their mitigations landing inside this plan and 38-08
respectively. Nothing blocks.
</threat_model>

<verification>
- `uv run pytest tests/test_desc_content_indent_render_gate.py -v` collects cleanly and reports the
  IND-01/02/03/05 and FLD-01 assertions RED against the untouched translator.
- Every RED is a structural or column-comparison mismatch; none is a compile failure.
- The fixture builds through `-b typst` and its `.typ` compiles through `typst.compile()`.
- `uv run pytest -m "not slow"` shows no NEW failure outside this module — this plan touches no
  existing test and no source file.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
</verification>

<success_criteria>
- One fixture exercises three-level nesting, a resumed parent body, a sibling top-level `desc`, a
  nested field list, a body-less `desc`, a list-item `desc`, a table-cell `desc`, a page-boundary
  `desc`, a block quote, and a no-desc control — in one build.
- Every IND/FLD-01 assertion exists, is hand-derived from `38-EMISSION-CONTRACT.md`, and is RED for a
  structural reason.
- The SC#4 repo-wide grep is recorded at discovery time per milestone invariant #6.
- The layout-mode measurement technique is evidenced in this phase, not inherited on trust.
- `typsphinx/` is not modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/38-structural-indentation-info-fields/38-01-SUMMARY.md` when done.
</output>
