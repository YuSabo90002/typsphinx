---
phase: 39-admonition-taxonomy-rubric-nesting
plan: 07
type: execute
wave: 3
depends_on: ["39-04", "39-05"]
files_modified:
  - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png
  - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md
autonomous: false
requirements: [ADM-04]

must_haves:
  truths:
    - "ADM-04 is discharged by an owner sign-off against a real rendered artifact, never by an automated assertion. It is the milestone's only [V] requirement and REQUIREMENTS.md's own legend marks it human-only; no mechanical stand-in is offered anywhere in this plan."
    - "The committed greyscale artifact is rendered from POST-FIX code -- after the bucket routing change of plan 39-05 has landed -- so what the owner looks at is the output this phase actually ships, not the pre-phase buckets."
    - "The artifact is committed into the phase directory at a stable path so the sign-off can reference it, and so a future reader can re-examine the evidence the decision was made on."
    - "The sign-off is recorded against the rendered artifact and NEVER against 39-CONTEXT.md's D-06 luminance table. That table was computed with the BT.709 relative-luminance weights while Pillow's desaturation uses the BT.601 luma weights; the two are close but not identical, so the rendered greys will not match the table's predictions and that difference is expected rather than a defect."
    - "No styling change is made in this plan (D-06). The claim under test is that the four icon shapes plus the existing 2pt accent stroke already carry the distinction -- which is exactly what ADM-04 words as the requirement."
    - "No fallback lever is chosen in advance (D-08). If the owner cannot distinguish the four kinds, the lever is chosen THEN, against the actual render, and the phase is expected to stop at this checkpoint."
    - "The two levers that exist are recorded for that conversation, along with the one that measurably does not: a dashed left edge cannot be specified, because the box's left edge takes only a thickness, a paint and a cap."
    - "This plan modifies no file under typsphinx/ and no file under tests/."
  artifacts:
    - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png
    - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md
  key_links:
    - "the render -> scripts/render_admonition_greyscale.py and tests/fixtures/admonition_greyscale_probe/, both created by plan 39-04"
    - "the render -> plan 39-05's bucket routing change; rendering before that plan lands would produce an artifact showing the pre-phase buckets"
    - "the sign-off -> ROADMAP SC#4, which requires both the render and the sign-off to be recorded in the phase artifacts"
    - "39-ADM04-SIGNOFF.md -> plan 39-08's phase-close checks, which read it rather than re-deriving the outcome"
  prohibitions:
    - requirement_id: ADM-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT substitute a computed luminance comparison for the owner's judgement. A pixel-difference or contrast-ratio assertion would produce a green that looks like verification while answering a different question than the one ADM-04 asks -- whether a reader can tell the four kinds apart -- and would let the phase close without anyone having looked."
    - requirement_id: ADM-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT pre-select the styling lever to apply if the sign-off fails. D-08 records that the lever is chosen against the actual render, because choosing in advance means choosing without the evidence; and a lever chosen in advance tends to get applied pre-emptively, which would make the render evidence of the fix rather than of the problem."
    - requirement_id: ADM-04
      category: accessibility
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT accept a distinction that survives only in colour. The whole point of ADM-04 is that the four title bands are mid-high-luminance pastels spanning about five percentage points and desaturate to near-identical greys; a sign-off given from a colour render, or given on the strength of remembered hues, answers nothing."
---

<objective>
Produce ADM-04's evidence and take it to the owner: render the four-bucket probe from post-fix code,
desaturate it, commit it into the phase directory, and stop at a blocking human checkpoint for the
sign-off ROADMAP SC#4 requires.

Purpose: ADM-04 is the milestone's only `[V]` requirement. The four title bands span about five
percentage points of luminance — the defect ADM-04 states, reproduced exactly by measurement — while
the left accent stroke spans about thirty-six and the four icons differ by shape. D-06 makes no
styling change and asserts that the icons and the stroke already carry the distinction. Whether they
actually do is a question only a person looking at the render can answer.

Output: `39-ADM04-GREYSCALE.png` and `39-ADM04-SIGNOFF.md`. This plan touches no source and no test.
It is expected to stop at the checkpoint if the owner cannot distinguish the four kinds — D-08
pre-agrees that outcome and pre-agrees that no fallback lever is chosen until then.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-RESEARCH.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-VALIDATION.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-04-SUMMARY.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-05-SUMMARY.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Step 1 is doubly load-bearing here: this worktree must sync from a `pyproject.toml` that already
carries the `pillow` entry plan 39-04 added, and its `typsphinx` must be the worktree's own copy
carrying plan 39-05's routing change. If either is stale the rendered artifact is evidence of the
wrong thing.
</environment_setup>

<artifacts_this_phase_produces>
See `39-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`39-ADM04-GREYSCALE.png` — the first committed binary artifact anywhere under `.planning/` in this
project — and `39-ADM04-SIGNOFF.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Render the committed greyscale artifact from post-fix code</name>
  <read_first>
    - `scripts/render_admonition_greyscale.py` (full file, created by plan 39-04) — its function
      signature, its command-line form, its single-page guard, and the docstring paragraph recording
      the BT.601-versus-BT.709 caveat.
    - `tests/fixtures/admonition_greyscale_probe/index.rst` (created by plan 39-04) — the six boxes
      the render will contain and the bucket each belongs to.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-05-SUMMARY.md` — confirmation that
      the bucket routing change has landed, which is the precondition for this render being evidence
      of the right thing.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` § "Greyscale
      distinguishability" — D-06's measured table and its explicit status as analytical scaffolding
      rather than a target.
  </read_first>
  <files>.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png</files>
  <action>
Before rendering, confirm the worktree really carries plan 39-05's change — run
`uv run pytest tests/test_admonition_bucket_render_gate.py -x` and require it green. A render taken
from a tree without that change would show the pre-phase buckets while claiming to evidence the
post-phase ones, and nothing downstream would catch it.

Then drive `scripts/render_admonition_greyscale.py` against
`tests/fixtures/admonition_greyscale_probe/`, writing the output to
`.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png`. Use the script's own
default PPI unless it produces an image too small to judge icon shape at normal viewing size, in which
case raise it and record the value used and why.

Also produce, alongside it in a temporary location, the un-desaturated colour render of the same
probe — not for commit, but so the checkpoint below can offer the owner the colour version as a
reference if they want to check that they are looking at the boxes they think they are. Record its
path in the checkpoint text.

Confirm before committing: the file exists, is non-empty, opens with Pillow in the single-channel
mode, has non-zero dimensions in both axes, and shows six boxes. If the probe grew past one page the
script raises rather than writing a partial artifact; if that happens, shorten the probe rather than
adding page-template handling.

**This task does not verify ADM-04.** It produces the artifact the verification will be performed on.
State that explicitly in the plan summary so no downstream reader mistakes a green render for a met
requirement.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_admonition_bucket_render_gate.py -x` is green in this worktree BEFORE the render is taken, and that ordering is recorded in the plan summary.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png` exists and is non-empty.
    - `uv run python -c "from PIL import Image; im = Image.open('.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png'); assert im.mode == 'L'; assert im.size[0] > 0 and im.size[1] > 0; print(im.mode, im.size)"` exits 0 and prints the mode and dimensions.
    - The PPI actually used is recorded in the plan summary, with a reason if it differs from the script's default.
    - The temporary colour render's path is recorded so the checkpoint can offer it.
    - The plan summary states in words that this task produced evidence and did not verify the requirement.
    - `git diff --stat -- typsphinx/ tests/` is empty for this plan's commits.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_admonition_bucket_render_gate.py -x &amp;&amp; uv run python -c "from PIL import Image; im = Image.open('.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png'); assert im.mode == 'L' and im.size[0] > 0 and im.size[1] > 0; print(im.mode, im.size)"</automated>
  </verify>
  <done>A single-channel render of the post-fix four-bucket probe is committed at a stable path, with the PPI and the colour reference recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner sign-off that the four kinds stay distinguishable without hue</name>
  <read_first>
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-VALIDATION.md` § "Manual-Only
      Verifications" — the single row this checkpoint discharges, and its carried caveat.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` § "Greyscale
      distinguishability" — D-06's measured luminance table and its status as analytical scaffolding,
      D-07's tooling decision, and D-08's decision that no fallback lever is chosen in advance.
    - `.planning/REQUIREMENTS.md` lines 120-123 — ADM-04's text and its `[V]` marking.
    - The artifact produced by Task 1, and the temporary colour render whose path Task 1 recorded.
  </read_first>
  <files>(no files modified — the owner's answer is recorded in 39-07-SUMMARY.md and consumed by Task 3)</files>
  <action>
Halt and present the material below to the owner. Do not self-approve and do not proceed on any
auto-advance setting: ADM-04 is the milestone's only `[V]` requirement and REQUIREMENTS.md's own legend
marks it human-only.

Offer no automated stand-in. Do not compute a contrast ratio, a pixel difference or a luminance
comparison and present it as supporting evidence — a number would answer a different question than the
one being asked and would anchor the judgement it is supposed to inform.

Record the owner's response verbatim in the plan summary. If they cannot distinguish the four kinds,
capture which pairs they could not tell apart and which lever they chose against the render, then stop
— do not begin the lever work inside this plan, and do not apply any styling change here.
  </action>
  <what-built>
`.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png` — a desaturated render
of a six-box probe compiled from the post-fix translator, containing one box from each of the four
buckets plus the two types this phase moved:

- note bucket — one `.. note::`
- success bucket — one `.. tip::` and one `.. seealso::` (seealso moved into this bucket, ADM-01)
- warning bucket — one `.. warning::`
- error bucket — one `.. error::` and one `.. attention::` (attention moved into this bucket, ADM-02)

Deliberately, **no styling change was made** (D-06). The claim you are being asked to judge is the one
ADM-04 itself words: that the distinction is carried by icon and border rather than by hue alone.

Context you should have before deciding:

- The four title bands were measured to span about **5.4 percentage points** of luminance — they are
  all mid-high-luminance pastels, which is precisely the defect ADM-04 names. Expect them to look
  similar. That is not the failure; that is the premise.
- The left accent stroke (2pt, accent at full saturation) spans about **35.9 points** — it is the
  strongest greyscale signal available.
- The four icons differ by **shape**, and their fills are baked into the source images rather than
  tinted by the box's accent, so they survive desaturation as shapes.
- The luminance numbers in 39-CONTEXT.md's D-06 table were computed with the BT.709 weights, while the
  desaturation you are looking at used Pillow's BT.601 weights. The two are close but not identical.
  **Judge the image, not the table** — the greys will not exactly match the predicted percentages, and
  that is expected.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png` at normal
   viewing size — not zoomed in. Zooming answers a different question than the one a reader of a PDF
   would be asking.
2. Without reading the body text, and without reference to the colour version, decide whether you can
   tell which box is which kind. The signals available to you are the icon shape and the left border.
3. Then check the two moved types specifically: does the `seealso` box read as the same kind as the
   `tip` box, and does the `attention` box read as the same kind as the `error` box? Grouping is what
   ADM-01 and ADM-02 asked for, so a render where they read as their own separate kinds is also a
   finding worth stating.
4. Optionally compare against the un-desaturated colour render whose path is recorded in the plan
   summary — but only after step 2, so the colour version does not prime the judgement.
5. If you cannot distinguish them: say so, and say what you would want changed. **No fallback lever
   was chosen in advance** (D-08) — that was deliberate, so the lever is picked now, against what you
   are actually looking at. Two levers exist: a per-bucket border thickness, and a per-bucket header
   band colour. A dashed border does **not** exist as an option — the box's left edge takes only a
   thickness, a paint and a cap, verified against the package sources. The phase is expected to stop
   here in that case; that is a planned outcome, not a failure of the plan.
  </how-to-verify>
  <resume-signal>Type "approved" to record the sign-off, or describe what you cannot distinguish and which lever you want considered.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the sign-off as a phase artifact</name>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 39" success criterion 4 — the exact wording of what must be
      recorded: the render and the sign-off, both in the phase artifacts.
    - `.planning/REQUIREMENTS.md` lines 106-130 — ADM-04's text and the `[V]` legend, so the record
      states what was judged in the requirement's own terms.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` D-06 and D-08 — the
      no-styling-change decision and the no-pre-agreed-lever decision, both of which the record cites.
    - The owner's answer from Task 2 verbatim.
  </read_first>
  <files>.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md</files>
  <action>
Write the sign-off record. It contains, in this order:

1. The requirement under judgement, quoted from REQUIREMENTS.md, with its `[V]` marking noted and a
   one-line statement that no automated assertion exists or was offered for it.
2. The artifact path, the commit it was rendered at, the PPI used, the probe fixture path, and the
   confirmation that the bucket routing change was verified green in the same worktree before the
   render was taken.
3. The caveat, restated: the sign-off was made against the rendered artifact and not against
   39-CONTEXT.md's D-06 luminance table, because that table used the BT.709 weights while the
   desaturation used BT.601.
4. The owner's answer, quoted verbatim rather than paraphrased, with the date.
5. The outcome, stated plainly as one of: the four kinds are distinguishable and ADM-04 is met with no
   styling change per D-06; or the owner could not distinguish them, in which case record exactly what
   they could not tell apart, which lever they chose against the render per D-08, and that the phase
   stops here pending that work — do not begin the lever work inside this plan.
6. A short note that the levers considered and their status were presented before the decision,
   including that a dashed border is not available because the box's left edge takes only a thickness,
   a paint and a cap.

If the outcome is that the owner could not distinguish the kinds, also file the follow-up as a pending
todo under `.planning/todos/pending/` naming the chosen lever and this phase, and state in the plan
summary that plan 39-08's phase close must not mark ADM-04 complete.
  </action>
  <acceptance_criteria>
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md` exists and contains all six numbered elements.
    - It quotes the owner's answer verbatim and carries a date.
    - It names the artifact path, the render commit and the PPI.
    - It states the BT.601-versus-BT.709 caveat explicitly.
    - It states one of the two outcomes unambiguously; the word "pending" does not appear as the final verdict.
    - If the outcome is negative, a pending todo file exists under `.planning/todos/pending/` naming the chosen lever, and no styling change was made in this plan: `git diff --stat -- typsphinx/` is empty.
    - `git diff --stat -- typsphinx/ tests/` is empty for this plan's commits in either outcome.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md &amp;&amp; grep -qi 'BT.601' .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md &amp;&amp; test -z "$(git diff --stat -- typsphinx/ tests/)"</automated>
  </verify>
  <done>ADM-04's outcome is recorded as a phase artifact, quoting the owner verbatim, with the render's provenance and the desaturation caveat attached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator → `.typ` → `typst.compile` → PNG → Pillow → committed artifact | The evidence chain. A break anywhere here yields a plausible-looking image that evidences the wrong build, the wrong colour space, or the wrong page. |
| committed artifact → human judgement → recorded verdict | The verification itself. This boundary is deliberately human and has no mechanical substitute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-10 | Spoofing | A verification artifact rendered from a tree that does not carry the bucket routing change, showing pre-phase buckets while presented as post-phase evidence | high | mitigate | Task 1 requires `tests/test_admonition_bucket_render_gate.py` green in the same worktree BEFORE the render, records that ordering, and records the render commit in the sign-off. The plan's `depends_on` places it after 39-05 rather than beside it. |
| T-39-15 | Repudiation | A `[V]` requirement closed by a computed stand-in — a contrast ratio or pixel diff — that greens without anyone having looked | high | mitigate | No automated assertion for ADM-04 exists anywhere in this plan; the only automated checks confirm the artifact was produced. The plan is non-autonomous and the checkpoint is blocking. Reinforced by the first `must_haves.prohibitions` entry. |
| T-39-16 | Tampering | A styling lever applied pre-emptively, turning the render into evidence of the fix rather than evidence of the problem | medium | mitigate | D-06 makes no styling change and D-08 pre-agrees no lever; both acceptance-criteria sets require `git diff --stat -- typsphinx/` empty in either outcome, and a negative outcome files a todo rather than starting the work. Reinforced by the second `must_haves.prohibitions` entry. |
| T-39-SC | Tampering | npm/pip/cargo installs — supply chain | high | mitigate | This plan installs nothing. `pillow` entered the dev extra in 39-04 behind its blocking legitimacy checkpoint. Verified by `git diff -- pyproject.toml` over this plan's commits being empty. |

ASVS L1: V5 Input Validation is the only applicable category for the translator itself — a build-time
Sphinx extension has no runtime authentication, session, access-control or cryptography surface
(39-RESEARCH.md § Security Domain). Blocking threshold is `high`. T-39-10, T-39-15 and T-39-SC are
`high` and all are `mitigate`, with T-39-10's and T-39-15's mitigations landing inside this plan.
Nothing blocks apart from the intended human checkpoint.
</threat_model>

<verification>
- `tests/test_admonition_bucket_render_gate.py` was green in this worktree before the render was
  taken, and that ordering is recorded.
- `39-ADM04-GREYSCALE.png` exists, opens in the single-channel mode with non-zero dimensions, and
  shows six boxes.
- The checkpoint was answered by a human and the answer is quoted verbatim in `39-ADM04-SIGNOFF.md`.
- `39-ADM04-SIGNOFF.md` states one unambiguous outcome and carries the render's provenance and the
  desaturation caveat.
- `git diff --stat -- typsphinx/ tests/` is empty for this plan's commits.
- No automated assertion anywhere in this plan claims to verify ADM-04.
</verification>

<success_criteria>
- A greyscale render of the post-fix four-bucket probe is committed at a stable path in the phase
  directory, with its provenance recorded.
- The owner's judgement is taken against that artifact, not against a predicted luminance table, and
  is recorded verbatim.
- No styling change was made and no fallback lever was chosen in advance.
- If the owner cannot distinguish the four kinds, the phase stops here with the chosen lever recorded
  as a pending todo — a planned outcome, not a failure.
</success_criteria>

<output>
Create `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-07-SUMMARY.md` when done.
</output>