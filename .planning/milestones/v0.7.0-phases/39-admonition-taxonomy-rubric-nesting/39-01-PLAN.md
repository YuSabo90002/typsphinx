---
phase: 39-admonition-taxonomy-rubric-nesting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/admonition_render_gate/index.rst
  - tests/test_admonition_bucket_render_gate.py
  - tests/test_pdf_render_gate.py
  - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-01.md
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03]

must_haves:
  truths:
    - "One fixture Sphinx project produces, in a single -b typst build, every one of the ten real admonition types plus a generic `.. admonition::` and a `.. topic::`, each carrying a distinct greppable body sentinel, so the bucket a type lands in is readable from the emitted .typ without counting global occurrences."
    - "Bucket membership is asserted REGION-SCOPED -- for each body sentinel, the clue-function name that opened the enclosing box is resolved from the emitted .typ and compared to a hand-derived expected name. No assertion counts global occurrences of a clue-function token, because adding or moving one type would silently re-value such a count."
    - "The three bucket moves are RED against the untouched translator for a STRUCTURAL reason, never a compile failure: seealso resolves to info where tip is expected (D-02), attention resolves to warning and danger resolves to danger where error is expected (D-03), the generic admonition resolves to clue where notify is expected (D-09), and the topic resolves to clue where abstract is expected (D-10)."
    - "The catalog-title change is RED in the same module: today only important and seealso carry a static title argument, so an assertion that every one of the ten types carries its sphinx.locale.admonitionlabels English title (D-04/D-05) fails on eight of them, and fails on seealso for the casing change from the current literal to the catalog's lowercase second word."
    - "Seven types are declared CONTROLS whose bucket does not move (note, warning, tip, important, caution, hint, error) and are asserted GREEN in both directions -- they must never be converted into defect cases."
    - "The compiled-PDF half of ADM-01/ADM-02 exists: the seealso, attention and danger body sentinels and their catalog titles survive into pypdf-extracted text from a real typst.compile(), alongside the module's existing literal-source-leak negative control."
    - "RESEARCH.md's claim that no fixture in this repository contains `danger` is recorded as CORRECTED: tests/fixtures/admonition_render_gate/index.rst already carries a danger construct, which is why this plan extends that fixture rather than creating a new one. seealso and attention are confirmed genuinely absent."
    - "This plan modifies no file under typsphinx/."
  artifacts:
    - tests/fixtures/admonition_render_gate/index.rst
    - tests/test_admonition_bucket_render_gate.py
    - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-01.md
  key_links:
    - "tests/test_admonition_bucket_render_gate.py -> tests/fixtures/admonition_render_gate/ and tests/fixtures/topic_line_block_render_gate/ via two session-scoped .typ-text pytest fixtures"
    - "tests/test_admonition_bucket_render_gate.py -> sys.executable -m sphinx (NEVER a bare sphinx-build or uv run sphinx-build argv -- the NixOS PATH-shadowing hazard restated in every render-gate module in this repo)"
    - "the region-scoping helper -> the body sentinels in the fixture .rst; a sentinel that is renamed in one file and not the other silently un-scopes every assertion built on it"
    - "tests/test_pdf_render_gate.py::TestAdmonitionPdfRenderGate -> the SAME extended fixture, so the .typ half and the compiled-PDF half cannot drift apart"
  prohibitions:
    - requirement_id: ADM-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT author an expected clue-function name or title string by reading what the translator currently emits. Every expectation in this module is derived from 39-CONTEXT.md's bucket table and from sphinx.locale.admonitionlabels read directly, so a wrong pre-phase mapping cannot be laundered into a green gate."
    - requirement_id: ADM-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT let a bucket assertion pass by matching a clue-function token anywhere in the document. A global substring or count assertion greens whenever ANY box in the file uses that function, which is exactly the failure mode that would let a mis-routed seealso ride on the real tip construct's token."
---

<objective>
Stand up the GATE-01 RED for the admonition half of this phase: extend the existing admonition
render-gate fixture into a full ten-type census with per-type body sentinels, and write a new
`.typ`-string gate module whose bucket and title expectations are hand-derived from 39-CONTEXT.md's
locked table (D-02, D-03, D-09, D-10) and from `sphinx.locale.admonitionlabels` (D-04, D-05).

Purpose: every one of these mis-routings compiles perfectly today. There is no fatal to capture, so
the RED must be structural — a box that opens with the wrong gentle-clues function, and a title
argument that is absent or carries the wrong casing. Recording that RED before any translator edit
exists is the milestone's GATE-01 bar.

Output: an extended fixture, `tests/test_admonition_bucket_render_gate.py`, new compiled-PDF
assertions inside the existing `TestAdmonitionPdfRenderGate`, and `39-GATE-EVIDENCE-01.md` recording
the verbatim RED/CONTROL split against a named commit. This plan does NOT touch `typsphinx/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-RESEARCH.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-PATTERNS.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-VALIDATION.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Skipping step 1 makes the worktree's pytest import the MAIN checkout's `typsphinx` package, so gates
report the wrong colour.
</environment_setup>

<assumption_delta_decision>
**decision: no-change.**

The orchestrator's advisory `assumption-delta` gate fired with `detected: true` on a single
`chosen`-family signal. Its matched term is the literal word `custom` inside the ROADMAP success
criterion's illustrative example `.. admonition:: Custom Title` — i.e. it matched a **directive
argument in an example**, not a singular→plural, required→optional, or derived→chosen architectural
transition.

Planning read every requirement, decision and research finding in this phase's scope for a genuine
transition of that shape and found none. The one identity-model change this phase does make — the
admonition title moving from a hardcoded Python literal to `sphinx.locale.admonitionlabels` (D-04) —
is a *source* change on an existing single-valued field, not a cardinality or optionality change, and
it is carried as threat `T-39-01` rather than as an assumption delta.

Recorded here so the fired gate leaves a visible trace rather than a silent drop.
</assumption_delta_decision>

<flagged_assumptions>
The spec-less probe fallback is active (no `39-SPEC.md` exists, so `## Edge Coverage` and
`## Prohibitions` are both absent). The orchestrator ran the deterministic edge probe; **all 7 rows
returned `unresolved`, 5 of them `unclassified`.** Per the protocol an `unclassified` row is never
auto-`covered`, never auto-`backstop`ped and never auto-dismissed — it is surfaced to the planner as a
flagged assumption. All 7 are therefore reproduced below and **none** was authored into `must_haves`
from the probe.

No-silent-drop equality: **7 probe-surfaced items == 0 authored into `must_haves` from the probe + 7
surfaced as flagged assumptions.**

| # | Req | Probe category | Probe question | Planning's honest read |
|---|-----|----------------|----------------|------------------------|
| 1 | ADM-01 | unclassified | (engine could not classify) | ADM-01 is a routing-table membership change with a closed, ten-element input domain. The shape axis the edge taxonomy reaches (boundaries, encoding, ordering) has no purchase on "which of four functions does this node class map to". The membership itself is asserted exhaustively in this plan — every one of the ten types has its own sentinel and its own expected function — which is a stronger statement than any edge category would have produced. Left `unresolved` deliberately; not converted into a `must_haves` truth from the probe. |
| 2 | ADM-02 | unclassified | (engine could not classify) | Same shape as row 1: `attention` and `danger` moving into the red bucket is a membership fact, not a boundary. Exhaustive per-type assertion in this plan covers it. Left `unresolved`. |
| 3 | ADM-03 | unclassified | (engine could not classify) | ADM-03's only genuinely open question is precedence — does a directive-supplied title still win over the newly-introduced catalog lookup. That is captured as a real prohibition (see `must_haves.prohibitions` in 39-05-PLAN.md) rather than as an edge, because it is an intent constraint, not a data shape. Left `unresolved`. |
| 4 | ADM-04 | adjacency | "When two things are exactly equal or just touch, do they merge, collide, or separate?" | Does not apply as posed. ADM-04's subject is whether four rendered boxes remain **visually** distinguishable after desaturation; there is no pair of values that can be "exactly equal or just touch" in a way a program could evaluate. The closest real analog — two buckets desaturating to indistinguishable greys — is precisely what the human checkpoint in 39-07-PLAN.md exists to judge, and D-08 pre-agrees the phase stops there if the owner cannot tell them apart. Left `unresolved`; deliberately given no automated stand-in. |
| 5 | ADM-04 | empty | "What is the result for empty, single-element, or null input?" | Does not apply as posed. The greyscale probe fixture is authored by this phase and always contains all four buckets by construction; there is no caller that can supply zero or one. The one genuine degenerate case — the probe growing past a single page, which would change `typst.compile(format="png")`'s return shape — is handled explicitly as an acceptance criterion in 39-04-PLAN.md, not as an edge predicate. Left `unresolved`. |
| 6 | ADM-04 | ordering | "When elements compare equal, is output order specified and stable?" | Does not apply as posed. Greyscale distinguishability is a per-box property with no comparison and no sort; there is nothing to order. Document order in the probe is fixed by the fixture `.rst` and is not a property under test. Left `unresolved`. |
| 7 | ADM-05 | concurrency | "If interrupted or run in parallel, what is guaranteed?" | Does not apply as posed. `TypstTranslator` is a synchronous single-pass docutils visitor: one document, one thread, no shared mutable state across documents, no interruption point. The only "parallelism" anywhere near this phase is pytest-xdist-style parallel *builds*, each in its own process and its own build directory. Note that the phase's actual defect (D-13) IS a state-lifetime bug — one handler's save slot clobbered by another's — but that is sequential nesting, not concurrency, and it is asserted directly in 39-02-PLAN.md. Left `unresolved`. |

**Prohibition recall (§B, run in-prompt — there is no compiled engine).** Stage 1 over-produced ~10
must-NOT candidates per requirement; Stage 2 dropped the routine-engineering ones. Survivors are
authored **descriptor-less** into the `must_haves.prohibitions:` sibling block of the plan they bind,
never into `truths`.

Canon-referral breadcrumbs (kept by Stage 2, then DROPPED without minting, per ADR-550 D6):

- *"MUST NOT interpolate externally-sourced title text into a Typst string literal without escaping"* —
  injection is canon; covered by `/gsd-secure-phase` and carried as threat `T-39-01` with a named
  one-line mitigation. Not minted as a prohibition.
</flagged_assumptions>

<artifacts_this_phase_produces>
Phase-wide list of every symbol Phase 39 creates or renames, so the plan-review source-grounding pass
does not flag a newly-created symbol as drift. Later plans reference this list rather than repeating
it.

**Source symbols (`typsphinx/translator.py`)** — all in plan 39-05 and 39-06

- New module-level import of `admonitionlabels` from `sphinx.locale` (plan 05). No new module-level
  constant and no new mapping table: D-04's catalog is keyed by the docutils node class name directly,
  verified byte-identical for all ten keys.
- `_visit_admonition` — gains the `admonitionlabels` lookup keyed on the node's class name (plan 05).
- `_depart_admonition` — its static-title branch gains an `escape_typst_string` call (plan 05).
- `visit_seealso`, `visit_danger`, `visit_attention`, `visit_admonition`, `visit_topic` — each changes
  the gentle-clues function name it passes (plan 05).
- `visit_important` and `visit_seealso` — their now-dead `custom_title=` keyword arguments are removed
  (plan 05).
- `visit_rubric` — its three save slots are renamed to `_rubric_was_in_paragraph`,
  `_rubric_was_in_list_item`, `_rubric_was_list_item_needs_separator` (plan 06).
- `depart_rubric` — its three `hasattr`/restore/`delattr` blocks read the same three renamed slots
  (plan 06).
- `visit_rubric` — its unconditional leading-newline append gains a guard so the rubric adds no
  separator that `_emit_id_anchors` has already emitted (plan 06).
- `visit_strong`, `depart_strong`, `visit_desc_signature`, `depart_desc_signature` — **NOT touched**.
  The `_strong_was_*` names stay exactly as they are (Phase 36 D-01/D-02; Phase 37's golden file is a
  fixed point).
- The gentle-clues base `clue` function disappears as a value passed anywhere in `typsphinx/`; the
  box-less `.. contents::` path is untouched.

**Test symbols**

- `tests/fixtures/admonition_render_gate/index.rst` — extended to a full ten-type census with per-type
  body sentinels (this plan).
- `tests/test_admonition_bucket_render_gate.py` — new module, with `_run_sphinx_build_typst`, two
  session-scoped `.typ`-text fixtures, and the region-scoping helper that resolves which clue function
  opened the box containing a given sentinel (this plan).
- New assertions inside the existing `tests/test_pdf_render_gate.py::TestAdmonitionPdfRenderGate`
  (this plan).
- `tests/fixtures/rubric_strong_nesting_render_gate/` (`conf.py` + `index.rst`) and
  `tests/test_rubric_strong_nesting_render_gate.py` (plan 02).
- A new newline-run assertion inside the existing
  `tests/test_desc_rubric_decoupling_render_gate.py` (plan 02).
- `tests/fixtures/rubric_indent_invariance_gate/` (`conf.py` + `index.rst`) and
  `tests/test_rubric_indent_invariance.py`, carrying its own copies of `_strip_zwsp`,
  `_layout_lines`, `_leading_columns`, `_find_page_and_column` (plan 03).
- `tests/fixtures/admonition_greyscale_probe/` (`conf.py` + `index.rst`),
  `scripts/render_admonition_greyscale.py` exposing `render_admonition_greyscale`, and
  `tests/test_admonition_greyscale_pipeline.py` with a `PILLOW_AVAILABLE` guard (plan 04).
- Four renamed test functions in `tests/test_admonitions.py`: the seealso, danger, attention and
  generic-admonition cases (plan 05). Two edited assertions in `tests/test_topics.py` (plan 05).
- `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` — regenerated by hand-derivation
  (plan 06).

**Config**

- `pyproject.toml` `[project.optional-dependencies] dev` gains a `pillow` entry (plan 04). No new
  entry in `[project] dependencies` — milestone invariant #1 holds. No `@preview` pin changes —
  milestone invariant #2 holds; `notify` and `abstract` are already in scope through the existing
  wildcard import.

**Planning artifacts**

- `39-GATE-EVIDENCE-01.md` (this plan), `39-GATE-EVIDENCE-02.md` (plan 02),
  `39-GATE-EVIDENCE-03.md` (plan 03), `39-GATE-EVIDENCE-04.md` (plan 08).
- `39-ADM04-GREYSCALE.png` and `39-ADM04-SIGNOFF.md` (plan 07) — the first committed binary artifact
  anywhere under `.planning/` in this project.
- `39-TEST-CENSUS.md` (plan 08).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the admonition render-gate fixture into a full ten-type census</name>
  <read_first>
    - `tests/fixtures/admonition_render_gate/index.rst` (full file) and
      `tests/fixtures/admonition_render_gate/conf.py` — the fixture being extended. It already carries
      note, warning, hint, danger, a nested note/warning pair, and a note containing a bullet list and
      a note containing a code block. **RESEARCH.md states no fixture in this repo contains `danger`;
      that is wrong and this file is the counter-example** — verified by repo-wide grep. `seealso` and
      `attention` are genuinely absent everywhere.
    - `tests/fixtures/topic_line_block_render_gate/index.rst` (full file) — the SENTINEL naming
      convention this fixture must match (`ADMONITIONNOTESENTINEL`-style, one uppercase run, no
      underscores), and the `.. admonition:: Custom *Title*` and `.. topic::` constructs this plan's
      module reads for ADM-03 and D-10 without editing them.
    - `tests/fixtures/preview_smoke/index.rst` lines 1-45 — it names `admonition_render_gate` in prose
      only, as the fixture whose constructs it deliberately does NOT duplicate. Extending
      `admonition_render_gate` therefore does not change `preview_smoke`'s meaning, but read it to
      confirm before editing.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` § "The bucket taxonomy"
      — the locked bucket table this fixture must make readable.
  </read_first>
  <files>tests/fixtures/admonition_render_gate/index.rst</files>
  <action>
Extend the existing fixture (do NOT create a new fixture directory) so that all ten real Sphinx
admonition types are present exactly once each, every one carrying a distinct body sentinel.

Present today and to be KEPT, gaining a sentinel in their body prose where they lack one: `note`,
`warning`, `hint`, `danger`. Keep every existing construct — the note-with-a-bullet-list, the
warning-with-a-code-block and the nested note/warning pair are pre-existing regression coverage for
the Phase 8.1 markup/code-mode fix and must not be removed or restructured.

To be ADDED, one `====`-underlined subsection each, in this order after the existing sections:
`tip`, `important`, `caution`, `seealso`, `attention`, `error`.

Sentinel naming, one per type, matching `topic_line_block_render_gate`'s convention (a single
uppercase run, no underscores, unique in the repository): `ADMONNOTESENTINEL`, `ADMONWARNINGSENTINEL`,
`ADMONTIPSENTINEL`, `ADMONIMPORTANTSENTINEL`, `ADMONCAUTIONSENTINEL`, `ADMONSEEALSOSENTINEL`,
`ADMONHINTSENTINEL`, `ADMONERRORSENTINEL`, `ADMONDANGERSENTINEL`, `ADMONATTENTIONSENTINEL`. Put each
sentinel in the FIRST paragraph of its admonition's body so the region-scoping helper in Task 2 can
resolve the nearest preceding clue-function open unambiguously.

Do not add a sentinel to the nested note/warning pair's bodies — that construct is deliberately about
nesting, and giving it sentinels would put two candidate regions in play for `note` and `warning`.
Instead, move the note and warning sentinels onto the two standalone note/warning constructs that
already lead the file, and add a `..` docutils comment on the nested pair recording that it carries no
sentinel on purpose.

Above each of the ten constructs put a `..` docutils comment naming: the requirement it serves, the
gentle-clues function it is expected to route to after this phase, and whether it is a DEFECT CASE or
a CONTROL. Per 39-CONTEXT.md's locked table the split is:

- CONTROL (bucket does not move): `note` to `info`, `warning` to `warning`, `tip` to `tip`,
  `important` to `warning`, `caution` to `warning`, `hint` to `tip`, `error` to `error`.
- DEFECT CASE: `seealso` to `tip` (D-02, currently `info`), `attention` to `error` (D-03, currently
  `warning`), `danger` to `error` (D-03, currently its own `danger`).

Also extend the file's header prose to state that the fixture is now read by TWO modules — the
pre-existing compiled-PDF leak gate in `tests/test_pdf_render_gate.py` and the new
`.typ`-string bucket gate — and that every type must keep exactly one sentinel-bearing construct.

Do not touch `tests/fixtures/admonition_render_gate/conf.py`; it already declares `typsphinx` and an
`index` master document.
  </action>
  <acceptance_criteria>
    - `tests/fixtures/admonition_render_gate/index.rst` contains exactly one directive occurrence of each of: `.. note::`, `.. warning::`, `.. tip::`, `.. important::`, `.. caution::`, `.. seealso::`, `.. hint::`, `.. error::`, `.. danger::`, `.. attention::` at the top level of the document, plus the pre-existing nested note/warning pair and the pre-existing bullet-list and code-block bodies.
    - Each of the ten sentinel strings appears exactly once in the file, and each appears in the first body paragraph of its own admonition.
    - Every one of the ten constructs is preceded by a `..` comment naming its requirement, its expected post-phase gentle-clues function, and the word CONTROL or the words DEFECT CASE.
    - The pre-existing note-with-bullet-list, warning-with-code-block and nested note/warning constructs are still present and unmodified apart from the added comment on the nested pair.
    - `uv run python -m sphinx -b typst tests/fixtures/admonition_render_gate /tmp/adm39fix` exits 0 and writes `index.typ`.
    - The emitted `index.typ` compiles through `typst.compile()` with no exception.
    - `uv run pytest tests/test_pdf_render_gate.py -k AdmonitionPdfRenderGate -x` is still green with the extended fixture (the pre-existing leak gate must not regress).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m sphinx -b typst tests/fixtures/admonition_render_gate /tmp/adm39fix &amp;&amp; uv run python -c "import typst; typst.compile('/tmp/adm39fix/index.typ')" &amp;&amp; uv run pytest tests/test_pdf_render_gate.py -k AdmonitionPdfRenderGate -x</automated>
  </verify>
  <done>One fixture builds and compiles, carrying all ten Sphinx admonition types with distinct per-type sentinels and an explicit CONTROL / DEFECT CASE label on each.</done>
</task>

<task type="auto">
  <name>Task 2: Write the bucket and catalog-title gate module, RED against the untouched translator</name>
  <read_first>
    - `tests/test_desc_content_indent_render_gate.py` lines 60-200 — `_run_sphinx_build_typst`
      (the `sys.executable -m sphinx` argv this module must clone verbatim) and the session-scoped
      `.typ`-text fixture shape.
    - `tests/test_signature_typography_gate.py` — its `_slice`-style region-isolation helper family,
      the in-repo precedent for scoping an assertion to one construct instead of asserting over the
      whole document.
    - `typsphinx/translator.py` lines 4337-4399 (`_visit_admonition` / `_depart_admonition`) — how the
      box opens and how the title argument is attached, so the region-scoping helper matches the real
      emitted shape rather than an assumed one.
    - `typsphinx/translator.py` lines 4400-4560 — the thirteen call sites, so each expected function
      name is checked against the current argument before it is written down as RED.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` § "The bucket taxonomy"
      and § "Admonition titles" (D-01 through D-05, D-09, D-10).
    - `tests/fixtures/topic_line_block_render_gate/conf.py` — needed to build that second fixture from
      this module.
  </read_first>
  <files>tests/test_admonition_bucket_render_gate.py</files>
  <action>
Create a new `.typ`-string gate module. Clone `_run_sphinx_build_typst` verbatim from
`tests/test_desc_content_indent_render_gate.py` — `sys.executable`, `-m`, `sphinx`, `-b`, `typst`,
source dir, build dir, captured output — and restate the NixOS PATH-shadowing rationale in its
docstring. Never invoke `sphinx-build` directly and never invoke it through `uv run`.

Provide two session-scoped fixtures returning emitted `.typ` text: one building
`tests/fixtures/admonition_render_gate`, one building `tests/fixtures/topic_line_block_render_gate`.
Each asserts the subprocess return code is 0 and that `index.typ` exists before returning its text.

Provide one region-scoping helper — working name `_clue_open_before` — that takes the emitted `.typ`
text and a sentinel string, locates that sentinel, scans BACKWARD for the nearest gentle-clues
box-open token, and returns just the function name that opened it. The box-open token's shape is the
one `_visit_admonition` emits: an identifier followed by an open paren followed by an open brace. The
helper raises `AssertionError` with the sentinel name when the sentinel is absent or when no box-open
precedes it, so a fixture rename fails as a clear structural assertion rather than an uncaught
exception. Every bucket assertion in this module goes through this helper. Do not write an assertion
that searches the whole document for a function token, and do not write an assertion that counts
occurrences of one — either form greens whenever any other construct in the file happens to use that
function.

Provide a second helper — working name `_title_arg_after` — that, given the emitted `.typ` and a
sentinel, returns the literal title argument attached to the enclosing box (the string argument
`_depart_admonition` appends after the body closes), or `None` when no title argument is present.

Write the DEFECT-CASE assertions, each RED today, each naming its decision in the failure message:

- The seealso sentinel's box opens with `tip` (D-02). RED today: `info`.
- The attention sentinel's box opens with `error` (D-03). RED today: `warning`.
- The danger sentinel's box opens with `error` (D-03). RED today: `danger`.
- In the topic fixture, the generic admonition's body sentinel's box opens with `notify` (D-09). RED
  today: `clue`.
- In the topic fixture, the topic body sentinel's box opens with `abstract` (D-10). RED today: `clue`.

Write the CONTROL assertions, GREEN in both directions, in a separately-named test whose docstring
says they must never be converted into defect cases: note opens `info`, warning opens `warning`, tip
opens `tip`, important opens `warning`, caution opens `warning`, hint opens `tip`, error opens
`error`.

Write the catalog-title assertions (D-04/D-05) as a table-driven test over all ten types, expecting
each type's English value from `sphinx.locale.admonitionlabels`. Derive the ten expected strings by
reading the catalog directly inside the test — importing `admonitionlabels` from `sphinx.locale` and
coercing each value with `str()` because the values are lazy i18n proxies, not plain strings — rather
than by transcribing them, so the test cannot drift from the catalog. Assert `_title_arg_after`
returns the quoted catalog value for each of the ten sentinels. This test is RED today for eight of
the ten (no title argument is emitted at all) and RED for seealso on casing; it is GREEN today only
for important. State that split in the module docstring.

Add one negative-direction assertion for D-10's consequence: in BOTH fixtures, no box that contains
one of this phase's sentinels opens with the gentle-clues base function, because after this phase no
call site passes it. Express it through `_clue_open_before` over the sentinel list — not as a
document-wide search — so it stays region-scoped like every other assertion here.

Apply the repo's optional-dependency convention: these are `.typ`-text assertions only, so this module
needs neither `typst` nor `pypdf` and must carry no `skipif` guard and no `slow` marker. Both are
reserved here for compiled-PDF work and for the network-dependent corpus gate respectively.
  </action>
  <acceptance_criteria>
    - `tests/test_admonition_bucket_render_gate.py` exists and collects cleanly under `uv run pytest tests/test_admonition_bucket_render_gate.py --collect-only`.
    - The module's subprocess helper builds its argv from `sys.executable` and the `-m sphinx` module form; the strings `sphinx-build` and `uv run` appear nowhere in any argv list in the file.
    - Running `uv run pytest tests/test_admonition_bucket_render_gate.py` against the untouched translator reports exactly five failing bucket assertions (seealso, attention, danger, generic admonition, topic) and a failing catalog-title assertion, and every failure is an equality/absence mismatch — no failure is a `sphinx-build` non-zero exit and none is a `typst.compile` error.
    - The CONTROL test passes against the untouched translator.
    - `_clue_open_before` raises `AssertionError` naming the sentinel when given a sentinel that is not in the text — provable by a self-check in the module or by a one-off `uv run python -c` invocation recorded in the evidence file.
    - The expected title strings are read from `sphinx.locale.admonitionlabels` inside the test rather than transcribed as literals; `grep -c 'admonitionlabels' tests/test_admonition_bucket_render_gate.py` is at least 1.
    - `uv run black --check tests/test_admonition_bucket_render_gate.py` and `uv run ruff check tests/test_admonition_bucket_render_gate.py` both pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_admonition_bucket_render_gate.py; uv run black --check tests/test_admonition_bucket_render_gate.py &amp;&amp; uv run ruff check tests/test_admonition_bucket_render_gate.py</automated>
  </verify>
  <done>A region-scoped bucket and catalog-title gate exists, is RED for exactly the five bucket moves and the eight missing titles, and is GREEN for the seven controls.</done>
</task>

<task type="auto">
  <name>Task 3: Add the compiled-PDF half for ADM-01/ADM-02 and record the RED evidence</name>
  <read_first>
    - `tests/test_pdf_render_gate.py` lines 30-70 (the `LEAK_SIGNATURES` constant and the fixtures-dir
      helpers) and lines 180-272 (`TestAdmonitionPdfRenderGate` in full) — the class this task extends
      and the exact sentinel-plus-negative-control shape to mirror.
    - `tests/test_pdf_render_gate.py` lines 1130-1180
      (`test_admonitiontitleregression_multichild`) — the naming rule that makes a test selectable by
      a `-k` substring, which the new methods must follow so 39-VALIDATION.md's per-requirement
      commands actually select them.
    - `.planning/phases/38-structural-indentation-info-fields/38-GATE-EVIDENCE-01.md` — the evidence
      file format (named commit, verbatim RED output, RED-versus-CONTROL split) this task's output
      must match.
    - `tests/fixtures/admonition_render_gate/index.rst` as extended by Task 1.
  </read_first>
  <files>tests/test_pdf_render_gate.py, .planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-01.md</files>
  <action>
Add compiled-PDF assertions for ADM-01 and ADM-02 into the existing `TestAdmonitionPdfRenderGate`
class, reusing that class's existing build-and-compile body rather than introducing a second
compile of the same fixture. If the class currently compiles inside its single test method, extract
that compile into a class- or session-scoped fixture returning extracted PDF text first, and leave the
existing leak assertion reading from it unchanged — the leak gate's behaviour must not move.

Add one new test method asserting that the seealso, attention and danger body sentinels all survive
into the pypdf-extracted text, and that the rendered header text for those three boxes reads as their
`sphinx.locale.admonitionlabels` English values. The header assertions are the RED half: today the
seealso box renders the current hardcoded literal with different casing, and the attention and danger
boxes render gentle-clues' own default titles derived from their function ids, so none of the three
expected strings is present. Reuse the module's existing `LEAK_SIGNATURES` constant for the negative
control; do not define a second copy.

Name the new method so a contiguous, case-insensitive substring of its node id selects it under a
`-k` expression tied to this phase's requirement, following the rule the existing multichild test's
docstring records: underscores break substring contiguity, so the selectable token must be written as
one unbroken run.

Then write `39-GATE-EVIDENCE-01.md` recording, against a named commit hash of the untouched
translator:

1. The verbatim `uv run pytest tests/test_admonition_bucket_render_gate.py` output, with each failing
   assertion labelled by the decision it enforces (D-02, D-03, D-04/D-05, D-09, D-10).
2. The verbatim `uv run pytest tests/test_pdf_render_gate.py -k AdmonitionPdfRenderGate` output.
3. An explicit RED-versus-CONTROL table naming the seven control types and stating that they are green
   pre-phase and must stay green post-phase.
4. The correction to RESEARCH.md's blast-radius claim: the repo-wide grep output showing
   `tests/fixtures/admonition_render_gate/index.rst` already contained a `danger` construct before
   this phase, and showing that `seealso` and `attention` were genuinely absent from every fixture.
5. A one-line statement that no file under `typsphinx/` was modified by this plan, with the
   `git diff --stat -- typsphinx/` output backing it.
  </action>
  <acceptance_criteria>
    - `tests/test_pdf_render_gate.py` gains exactly one new test method inside `TestAdmonitionPdfRenderGate`; no new module-level `LEAK_SIGNATURES`-style constant is introduced.
    - The new method's node id contains an unbroken, case-insensitive substring that a `-k` expression can select, and `uv run pytest tests/test_pdf_render_gate.py -k <that token> --collect-only` selects exactly that one test.
    - Running the new method against the untouched translator fails on the header-text expectations for seealso, attention and danger, and does NOT fail on the body sentinels.
    - The pre-existing literal-source-leak assertion still passes.
    - `39-GATE-EVIDENCE-01.md` exists and contains: a named commit hash, verbatim pytest output for both modules, the RED-versus-CONTROL table naming all seven controls, the corrected fixture grep, and the `git diff --stat -- typsphinx/` output showing no source change.
    - `uv run black --check tests/test_pdf_render_gate.py` and `uv run ruff check tests/test_pdf_render_gate.py` both pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_pdf_render_gate.py -k AdmonitionPdfRenderGate; uv run black --check tests/test_pdf_render_gate.py &amp;&amp; uv run ruff check tests/test_pdf_render_gate.py &amp;&amp; test -s .planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-01.md</automated>
  </verify>
  <done>ADM-01 and ADM-02 have a compiled-PDF assertion that is RED on titles today, and the whole admonition RED is recorded verbatim against a named commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.rst` fixture source → translator | Author-controlled build-time text; the only untrusted-input analog in a build-time tool |
| `sphinx.locale.admonitionlabels` → translator → emitted `.typ` | An EXTERNAL catalog now supplies a string that is interpolated into a Typst string literal (new this phase) |
| translator → emitted `.typ` → `typst.compile()` | Where an unescaped or mis-separated emission becomes a build fatal or a rendering defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `_depart_admonition`'s static-title branch — interpolates the title into a Typst string literal with no escaping, and D-04/D-05 change that title's source from a hardcoded Python literal to `sphinx.locale.admonitionlabels` | high | mitigate | Not this plan's edit; carried here because this plan authors the assertion that will observe it. The one-line mitigation lands in 39-05: route the branch through the project's existing `escape_typst_string` (`typsphinx/translator.py:32`) after coercing the lazy i18n proxy with `str()`. No second, title-specific escaping routine is introduced. |
| T-39-03 | Tampering | A gate expectation fitted to the translator's current output rather than derived from the locked bucket table and the catalog, producing a green that proves nothing | high | mitigate | Every expected function name comes from 39-CONTEXT.md's table and every expected title is read from `admonitionlabels` inside the test; the RED is recorded verbatim against a named commit before any translator edit exists. Reinforced by both `must_haves.prohibitions` entries. |
| T-39-06 | Spoofing | A region-unscoped bucket assertion greening because a DIFFERENT construct in the same document uses the expected function token | medium | mitigate | Every bucket assertion routes through `_clue_open_before`, which resolves the box that actually contains the sentinel. Document-wide substring and count assertions are prohibited by name in the action. |
| T-39-SC | Tampering | npm/pip/cargo installs — supply chain | high | mitigate | This plan installs nothing. 39-RESEARCH.md's Package Legitimacy Audit lists exactly one package for the whole phase (`pillow`, verdict SUS), and its blocking `checkpoint:human-verify` lives in 39-04 before the `pyproject.toml` edit. Verified at phase close by `git diff -- pyproject.toml` over this plan's commits being empty. |

ASVS L1: V5 Input Validation is the only applicable category — a build-time Sphinx extension has no
runtime authentication, session, access-control or cryptography surface (39-RESEARCH.md § Security
Domain). Blocking threshold is `high`. T-39-01, T-39-03 and T-39-SC are `high` and all three are
`mitigate`, with T-39-01's mitigation landing in 39-05 and T-39-SC's in 39-04. Nothing blocks this
plan.
</threat_model>

<verification>
- `uv run pytest tests/test_admonition_bucket_render_gate.py -v` collects cleanly and reports the five
  bucket assertions and the catalog-title assertion RED against the untouched translator.
- Every RED is a structural equality or absence mismatch; none is a `sphinx-build` failure and none is
  a `typst.compile()` error.
- The seven CONTROL types are green pre-phase.
- `uv run pytest tests/test_pdf_render_gate.py -k AdmonitionPdfRenderGate -v` shows the pre-existing
  leak assertion green and the new header-text assertions red.
- `uv run pytest -m "not slow"` shows no NEW failure outside these two modules.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
- `git diff --stat -- typsphinx/` is empty for this plan's commits.
</verification>

<success_criteria>
- One fixture carries all ten Sphinx admonition types, each with a unique sentinel and an explicit
  CONTROL / DEFECT CASE label.
- Every bucket assertion is region-scoped to the box containing its sentinel.
- The five bucket moves (D-02, D-03 twice, D-09, D-10) and the catalog-title change (D-04/D-05) are
  each recorded RED for a structural reason, against a named commit, before any translator edit
  exists.
- The compiled-PDF half of ADM-01/ADM-02 exists and shares the same fixture as the `.typ` half.
- RESEARCH.md's incorrect "no fixture contains danger" claim is corrected in the evidence file rather
  than silently worked around.
- `typsphinx/` is not modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-01-SUMMARY.md` when done.
</output>
