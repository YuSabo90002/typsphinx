---
phase: 39-admonition-taxonomy-rubric-nesting
plan: 08
type: execute
wave: 4
depends_on: ["39-05", "39-06", "39-07"]
files_modified:
  - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-TEST-CENSUS.md
  - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-04.md
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05]

must_haves:
  truths:
    - "The exact-string blast radius of this phase is migrated INSIDE the phase and recorded as a file/class census with per-entry counts, re-measured at close rather than inherited from the discussion-time estimate (SC#5, D-14)."
    - "The census records, per file, how many assertions were edited and how many were deliberately left untouched, and states why the untouched ones were untouched -- so a reader can tell a considered omission from an oversight."
    - "The full-corpus real-render gate ACTUALLY RAN and was green. It skips gracefully when the network is unavailable and a skip is NOT a pass; the census records the run's outcome, its duration and the resolved corpus tag, and the phase does not close on a skip."
    - "The gentle-clues pin is confirmed unchanged across every place the version-sync test watches. This phase changes which of the package's functions are called, not the pin, and the notify and abstract functions were already in scope through the existing wildcard import -- confirmed by running the sync test, not assumed."
    - "The project's own documentation build is exercised through the typstpdf builder, because this project dogfoods its own extension and its docs contain admonitions and rubrics."
    - "ROADMAP SC#3 reads as an invariance guard per D-12, and REQUIREMENTS.md's ADM entries reflect the outcome each requirement actually reached -- including ADM-04's, which is whatever the owner recorded in the sign-off and not an assumed pass."
    - "Milestone invariants are re-checked at close, not assumed: no new runtime dependency, no new @preview package, and no bumped @preview pin."
    - "This plan modifies no file under typsphinx/ and no file under tests/."
  artifacts:
    - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-TEST-CENSUS.md
    - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-04.md
  key_links:
    - "39-TEST-CENSUS.md -> the per-file counts recorded in 39-05-SUMMARY.md and 39-06-SUMMARY.md; the census cross-checks them rather than re-deriving from scratch"
    - "the corpus gate -> tests/test_corpus_gate.py, which is network-dependent and skips rather than fails when the corpus is unavailable"
    - "39-GATE-EVIDENCE-04.md -> the three earlier gate-evidence files, whose REDs it shows flipped"
    - "39-ADM04-SIGNOFF.md -> this plan's requirement-status update for ADM-04; the outcome is read from the sign-off, never assumed"
  prohibitions:
    - requirement_id: ADM-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT record a skipped corpus gate as a passed corpus gate. The gate skips gracefully when the network or the corpus is unavailable, which makes a skip visually indistinguishable from success in a summary line; SC#5 asks whether the corpus still renders, and only a real run answers that."
    - requirement_id: ADM-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT mark ADM-04 complete on the strength of the artifact existing. The requirement is met only if the owner's recorded sign-off says the four kinds are distinguishable; if it says otherwise, this plan records the phase as stopping there and leaves the requirement open."
    - requirement_id: ADM-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT report the census as a count without the reasons. A bare number of edited assertions cannot be audited; the value of the census is that a reader can check each untouched assertion was untouched for a stated reason rather than because nobody looked at it."
---

<objective>
Close the phase: re-measure and record the exact-string blast radius as a file/class census, run the
full-corpus real-render gate for real and record its outcome, confirm the milestone invariants and the
`@preview` pin at close rather than assuming them, and reconcile the roadmap and requirements records
with what each requirement actually reached.

Purpose: SC#5 asks two things — that this phase's exact-string migration happened inside the phase
with hand-derived expectations and a recorded census, and that the full-corpus gate is re-run green
after the admonition and rubric changes. The gate is network-dependent and skips gracefully offline,
so "green" has to mean it ran.

Output: `39-TEST-CENSUS.md` and `39-GATE-EVIDENCE-04.md`, plus the requirement-status reconciliation.
This plan touches no source and no test.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-RESEARCH.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-VALIDATION.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-05-SUMMARY.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-06-SUMMARY.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-07-SUMMARY.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Skipping step 1 makes every measurement in this plan a measurement of the MAIN checkout rather than of
this phase's work.
</environment_setup>

<artifacts_this_phase_produces>
See `39-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`39-TEST-CENSUS.md` and `39-GATE-EVIDENCE-04.md`. Neither name collides with the `39-VERIFICATION.md`
name reserved by the verification workflow.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure and record the exact-string migration census</name>
  <read_first>
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` D-14 — the
      discussion-time census and the instruction that it is a starting point to be re-taken, not
      trusted.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-RESEARCH.md` § "Blast-Radius
      Re-Take" — the planning-time re-measurement, including the per-line map of which of the
      eighteen in-process assertions actually moved and why the other thirteen did not.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-05-SUMMARY.md` and `39-06-SUMMARY.md`
      — the per-file edited/untouched counts those plans were required to record, which this census
      cross-checks.
    - `.planning/phases/38-structural-indentation-info-fields/38-TEST-CENSUS.md` — the census file
      format this project has already used once, so this one is comparable rather than novel.
  </read_first>
  <files>.planning/phases/39-admonition-taxonomy-rubric-nesting/39-TEST-CENSUS.md</files>
  <action>
Re-measure the blast radius against the finished tree and write the census.

Measure, do not recall: run repo-wide greps over `tests/` for the gentle-clues box-open forms and for
each of the ten admonition type names, and over the rubric-touching modules and fixtures named in
D-14. Record the commands you ran and their raw counts, so the measurement is reproducible.

The census table has one row per file, with columns: file path; role (in-process unit, real-compile
`.typ` gate, compiled-PDF gate, fixture, golden); assertions edited; assertions deliberately left
untouched; test functions renamed; and a reason column stating why the untouched ones are untouched.
The expected shape, to be confirmed rather than assumed:

- `tests/test_admonitions.py` — a small number of assertions edited across four functions, four
  functions renamed, and a materially larger number untouched because their bucket does not move and
  their English catalog title is byte-identical to the literal it replaced.
- `tests/test_topics.py` — two assertions edited; one untouched because the box-less contents path is
  not changed by this phase.
- `tests/test_pdf_render_gate.py` — new assertions added, none of the pre-existing four edited,
  because they check body sentinels and a directive-supplied title rather than catalog defaults.
- The five rubric-touching modules and five fixtures from D-14's list, each with its outcome recorded
  as confirmed-green or as re-derived, matching what plan 39-06 recorded.
- `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` — regenerated, with the diff's extent
  stated.

Add a short section recording the two measured regressions D-05 accepted as designed rather than as
bugs — the Japanese-build title changes on two types, and the English casing change on one — so a
future reader encountering them in a diff does not open a defect.

Add a section reconciling this census against the discussion-time and planning-time ones, naming any
place where the finished tree disagreed with either. If the raw counts match, say so explicitly;
"matched" is a finding, and stating it is what distinguishes a re-measurement from a copy.
  </action>
  <acceptance_criteria>
    - `39-TEST-CENSUS.md` exists and contains one row per affected file with all six columns populated.
    - Every count in the census is backed by a recorded command; the file contains the grep invocations used.
    - The reason column is non-empty for every row with a non-zero untouched count.
    - The accepted-regressions section names all three measured changes D-05 accepted.
    - The reconciliation section explicitly states, for the discussion-time and planning-time censuses, either matched or the specific disagreement.
    - The counts agree with those recorded in `39-05-SUMMARY.md` and `39-06-SUMMARY.md`, or the disagreement is called out and explained.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/39-admonition-taxonomy-rubric-nesting/39-TEST-CENSUS.md &amp;&amp; grep -qi 'reconcil' .planning/phases/39-admonition-taxonomy-rubric-nesting/39-TEST-CENSUS.md</automated>
  </verify>
  <done>The exact-string blast radius is re-measured against the finished tree and recorded per file with reasons, reconciled against both earlier censuses.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full-corpus gate for real, the full suite, and the docs dogfood build</name>
  <read_first>
    - `tests/test_corpus_gate.py` lines 1-60 — how the corpus is obtained (a shallow clone at the tag
      matching the installed Sphinx version), that the whole class is marked slow and excluded from
      the fast tier, and that it SKIPS rather than fails when the corpus is unavailable.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-VALIDATION.md` § "Sampling Rate" —
      the explicit rule that a skip is not a pass and that the gate must actually run at least once
      before the phase closes.
    - `tox.ini` — the `docs-html` and `docs-pdf` environments, the second of which drives this
      project's own documentation through the `typstpdf` builder.
    - `tests/test_preview_version_sync.py` — what it watches and where, so its green result is
      understood rather than merely observed.
    - `CLAUDE.md` § "The `@preview` version-sync hazard".
  </read_first>
  <files>.planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-04.md</files>
  <action>
Run the closing gates and capture each result verbatim into the evidence file.

1. The full-corpus real-render gate. Run it explicitly with its slow marker selected. It must ACTUALLY
   RUN — record the resolved corpus tag, the duration, and the pass/fail. If it skips, the phase does
   not close: report the skip reason, arrange network access or a cached corpus, and run it again.
   A skipped result recorded as a pass is the single failure mode this task exists to prevent.
2. The full test suite unfiltered, so every real-compile fixture this phase added or touched runs.
3. The fast tier, as the quick-regression cross-check.
4. The version-sync test, explicitly and by name, confirming the gentle-clues pin is untouched across
   every place it is declared. Record its green result rather than assuming it: this phase changed
   which of the package's functions are called, and the two newly-used functions were already in scope
   through the existing wildcard import, but that is a claim worth a green test behind it.
5. The lint, format and type trio.
6. The project's own documentation build through the Typst PDF environment, because this project
   dogfoods its own extension and its documentation contains both admonitions and rubrics. Record
   whether the produced PDF differs in page count from before the phase, and if it does, why — an
   admonition title changing length and a rubric separator shrinking are both legitimate causes.
7. The milestone invariants, each as a command and its output: the runtime dependency array
   unchanged; no new `@preview` package imported anywhere under `typsphinx/`; and the pinned
   gentle-clues version identical to its pre-phase value.

Write each command, its exit status and the salient part of its output into `39-GATE-EVIDENCE-04.md`,
under a heading naming what it proves.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_corpus_gate.py -m slow -v` ran and reported passed, not skipped; the evidence file records the resolved corpus tag, the duration, and the verbatim result line.
    - `uv run pytest` (unfiltered) is fully green and its summary line is recorded.
    - `uv run pytest -m "not slow"` is fully green.
    - `uv run pytest tests/test_preview_version_sync.py -x` is green and recorded by name.
    - `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` all pass and are recorded.
    - `uv run tox -e docs-pdf` (or the equivalent direct `sphinx-build -b typstpdf` over `docs/`) exits 0 and produces a PDF; the page count is recorded with an explanation for any change.
    - `git diff` over `pyproject.toml`'s runtime dependency array across the whole phase shows no addition; the evidence file records the diff.
    - A repo-wide count of `@preview` import lines under `typsphinx/` matches the pre-phase count, and the pinned gentle-clues version string is identical to its pre-phase value; both recorded.
    - The evidence file contains no line describing the corpus gate as passing on the basis of a skip.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_corpus_gate.py -m slow -v &amp;&amp; uv run pytest &amp;&amp; uv run pytest tests/test_preview_version_sync.py -x &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <done>Every closing gate ran for real and its verbatim result is recorded, including a corpus gate that actually executed rather than skipped.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the roadmap and requirement records with what each requirement reached</name>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 39" — all five success criteria, and specifically SC#3, whose
      wording D-12 corrects into an invariance guard. Check the current wording before deciding
      whether anything remains to change.
    - `.planning/REQUIREMENTS.md` lines 106-130 — the five ADM entries and their `[M]` / `[V]`
      markings.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md` — the owner's
      recorded outcome for ADM-04, which is the ONLY source for that requirement's status.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-01.md`, `-02.md` and
      `-03.md` — the three recorded REDs and the one recorded invariance guard, whose current state
      this task reports.
    - `.planning/STATE.md` § "Operator Next Steps" — the three Phase 39 notes recorded there, which
      should be updated or retired now that the phase has answered them.
  </read_first>
  <files>.planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-04.md</files>
  <action>
Append the reconciliation section to the evidence file.

For each of the five success criteria, state the criterion, name the artifact and the command that
discharges it, and give its status:

- SC#1 — the two bucket moves asserted on the emitted call and confirmed in the compiled PDF, with the
  RED recorded pre-phase. Name the gate-evidence file and the two modules.
- SC#2 — the generic admonition styled and titled, asserted on the emitted call and by the title
  surviving into extracted PDF text.
- SC#3 — confirm the roadmap wording now describes an invariance guard per D-12 and cite the Phase 36
  SC#3 precedent it follows. If the wording still reads as a RED-able claim, correct it now, and record
  the before and after text.
- SC#4 — read the outcome from the sign-off file. Do not infer it from the artifact existing. If the
  owner could not distinguish the four kinds, record the criterion as NOT met and the phase as stopped
  at that checkpoint, and do not mark ADM-04 complete anywhere.
- SC#5 — the census and the corpus gate, both from this plan's earlier tasks.

Then update `.planning/REQUIREMENTS.md`'s five ADM entries to the status each actually reached, and
update `.planning/STATE.md`'s Operator Next Steps to retire the three Phase 39 notes now answered —
the `SHARED_INDENT_STEP` consumption bar, the ADM-04 human-checkpoint expectation, and the
`--skip-ui` instruction — replacing them with whatever the next phase needs to know. Do not flip any
requirement checkbox that the evidence does not support; in particular ADM-04's status comes from the
sign-off and nowhere else.

Record, in the same section, the disposition of the two folded defects: the pending todo whose front
matter names this phase as its resolver should be moved to the completed location with a line naming
the commit that closed it, and the double-blank-line wart recorded in the rubric docstring should be
confirmed removed from that docstring since the repair landed.
  </action>
  <acceptance_criteria>
    - `39-GATE-EVIDENCE-04.md` contains a reconciliation section covering all five success criteria, each with its discharging artifact and command named.
    - SC#3's entry states the roadmap wording is an invariance guard per D-12 and cites Phase 36's SC#3 precedent; if a correction was needed, both the before and after text are recorded.
    - SC#4's entry quotes the outcome from `39-ADM04-SIGNOFF.md` and does not infer it from the artifact's existence.
    - `.planning/REQUIREMENTS.md`'s five ADM entries reflect the recorded outcomes; ADM-04's state matches the sign-off exactly.
    - `.planning/STATE.md`'s Operator Next Steps no longer carry the three now-answered Phase 39 notes.
    - The folded pending todo is moved out of `.planning/todos/pending/` with the closing commit named, and the rubric docstring no longer records the wart as deferred: `grep -c 'not fixed in this plan' typsphinx/translator.py` returns 0.
    - `uv run pytest -m "not slow"` is still green after the documentation edits (a sanity check that nothing under `tests/` or `typsphinx/` was touched by this task).
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-04.md &amp;&amp; uv run pytest -m "not slow"</automated>
  </verify>
  <done>All five success criteria are reconciled against real artifacts, the requirement and state records match what the evidence supports, and both folded defects are closed by reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network / upstream corpus → the full-corpus gate → the phase's close decision | The gate's graceful skip makes an unavailable corpus visually indistinguishable from a passing one in a summary |
| Recorded artifacts → the requirement status records | Where evidence is turned into a claim; an inferred status is where a phase closes on something nobody checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-17 | Repudiation | A skipped corpus gate recorded as a passed corpus gate, closing SC#5 on a run that never happened | high | mitigate | The gate's resolved corpus tag, duration and verbatim result line are all required in the evidence file, and the acceptance criteria state that the phase does not close on a skip. Reinforced by the first `must_haves.prohibitions` entry. |
| T-39-15 | Repudiation | ADM-04 marked complete because the artifact exists rather than because the owner said it was distinguishable | high | mitigate | The requirement's status is read from `39-ADM04-SIGNOFF.md` only, and the acceptance criteria forbid inferring it. Reinforced by the second `must_haves.prohibitions` entry. |
| T-39-SC | Tampering | npm/pip/cargo installs — supply chain, checked at close rather than assumed | high | mitigate | Task 2 re-checks the runtime dependency array, the `@preview` import count and the pinned gentle-clues version against their pre-phase values, and records each command with its output. The one package this phase added entered the dev extra in 39-04 behind a blocking human legitimacy checkpoint. |
| T-39-13 | Repudiation | A stale deferral note left in a docstring after the deferred repair landed, so the next reader believes the defect is still open | medium | mitigate | Task 3 requires the rubric docstring's deferred-repair sentence to be gone and the folded todo moved out of the pending directory with its closing commit named. |

ASVS L1: V5 Input Validation is the only applicable category for the translator itself — a build-time
Sphinx extension has no runtime authentication, session, access-control or cryptography surface
(39-RESEARCH.md § Security Domain). Blocking threshold is `high`. T-39-17, T-39-15 and T-39-SC are
`high` and all are `mitigate`, with every mitigation landing inside this plan. Nothing blocks.
</threat_model>

<verification>
- `uv run pytest tests/test_corpus_gate.py -m slow -v` ran and passed; its resolved tag, duration and
  result line are recorded, and the phase is not closing on a skip.
- `uv run pytest` unfiltered is fully green.
- `uv run pytest tests/test_preview_version_sync.py -x` is green — the pin is untouched.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
- The documentation dogfood build through the Typst PDF environment exits 0.
- The milestone invariants are re-checked with recorded commands: no new runtime dependency, no new
  `@preview` package, no bumped pin.
- All five success criteria are reconciled with named artifacts; ADM-04's status comes from the
  sign-off.
- `git diff --stat -- typsphinx/ tests/` is empty for this plan's commits.
</verification>

<success_criteria>
- The exact-string blast radius is re-measured at close and recorded per file with reasons, reconciled
  against both earlier censuses.
- The full-corpus real-render gate actually ran and was green, with its provenance recorded; a skip is
  not accepted as a pass.
- The gentle-clues pin and both milestone invariants are confirmed by command, not assumed.
- All five success criteria are reconciled against real artifacts, with SC#3 reading as an invariance
  guard per D-12 and SC#4 read from the owner's sign-off.
- Both folded defects are closed by reference, and no stale deferral note is left behind.
</success_criteria>

<output>
Create `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-08-SUMMARY.md` when done.
</output>
