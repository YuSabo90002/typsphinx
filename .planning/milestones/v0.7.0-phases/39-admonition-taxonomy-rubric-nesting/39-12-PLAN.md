---
phase: 39-admonition-taxonomy-rubric-nesting
plan: 12
type: execute
wave: 7
depends_on: ["39-11"]
files_modified:
  - tests/fixtures/admonition_greyscale_probe/index.rst
  - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png
  - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md
autonomous: false
gap_closure: true
gap_ids: [G-39-1]
requirements: [ADM-04]

must_haves:
  truths:
    - "ADM-04's sign-off is re-taken, not inherited. The artifact currently on record was rendered from code that folded attention, danger and error onto one function, so it cannot evidence a taxonomy in which they are three. A [V] requirement whose evidence shows something other than the shipped behaviour is not verified."
    - "The greyscale probe carries attention, danger and error as three SEPARATE adjacent boxes, so the owner is asked the adjacency question directly: once colour is removed, do the memo box's maroon title band and the error box's red title band merge, collide, or read as separate boxes? The sign-off records the verdict for that specific pair by name rather than only for the four groups collectively."
    - "The re-render is taken from post-change code. The routing change of plan 39-11 must be verified green in the SAME worktree before the render is produced, and the render commit is recorded in the sign-off."
    - "The probe stays exactly one page. typst-py's PNG export needs a page-number template in the output filename the moment a document exceeds one page (39-RESEARCH.md Pitfall 4) and this pipeline deliberately does not supply one; the render script raises rather than writing a partial artifact, and the correct response to that raise is to shorten the probe, never to add page-template handling."
    - "No automated assertion is offered for ADM-04 anywhere in this plan. The only automated checks confirm the artifact was produced and that it came from the right tree; the judgement itself is the owner's and the checkpoint is blocking."
    - "The new sign-off is recorded as a dated AMENDMENT to 39-ADM04-SIGNOFF.md. The 2026-08-02 plan-39-07 verdict and the owner's three verbatim Japanese quotations stay in the file; the amendment sits below them and states which verdict is now operative."
    - "No styling change is made in this plan and no fallback lever is chosen in advance, exactly as D-06 and D-08 govern. If the owner cannot distinguish the kinds, the lever is chosen then, against the actual render, and the gap stops at this checkpoint."
    - "The new taxonomy is EXPECTED to improve the greyscale verdict -- three distinct glyphs where there was one -- but that expectation is not stated to the owner as a conclusion and no number is computed to support it. Presenting a predicted improvement as evidence would anchor the judgement it is meant to inform."
    - "This plan modifies no file under typsphinx/ and no test module. The only file it touches under tests/ is the probe fixture's index.rst."
    - statement: "A probe document containing zero admonition boxes, or exactly one, is not a path this project ships and has no acceptance criterion here beyond the render pipeline's own single-page guard, which raises rather than writing a partial artifact. Recorded so a later reader knows the degenerate-input case was considered and deliberately left unasserted."
      verification: backstop
  artifacts:
    - tests/fixtures/admonition_greyscale_probe/index.rst
    - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png
    - .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md
  key_links:
    - "the render -> scripts/render_admonition_greyscale.py and tests/fixtures/admonition_greyscale_probe/, both created by plan 39-04 and reused unchanged apart from the fixture's box list"
    - "the render -> plan 39-11's routing change; rendering before it lands reproduces the folded artifact this plan exists to replace"
    - "the probe's three adjacent red-family boxes -> the ADM-04 adjacency question, which is the one the owner's original complaint was actually about"
    - "39-ADM04-SIGNOFF.md's amendment -> plan 39-13's close-out, which reads it rather than re-deriving the outcome, and must not close the gap on a negative verdict"
  prohibitions:
    - requirement_id: ADM-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT substitute a computed luminance comparison, contrast ratio or pixel difference for the owner's judgement. A number would produce a green that looks like verification while answering a different question than the one ADM-04 asks, and would let the gap close without anyone having looked at the new taxonomy."
    - requirement_id: ADM-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT reuse or re-commit the existing artifact. The committed PNG shows all three red-family types inside one box style; presenting it as evidence for a taxonomy in which they are three distinct styles would be evidence of the wrong thing while looking exactly like evidence of the right thing."
    - requirement_id: ADM-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT tell the owner that the new taxonomy is expected to improve the render before they have judged it. The expectation is real and is recorded in the gap, but stating it as a conclusion at the checkpoint anchors the very judgement the checkpoint exists to obtain."
    - requirement_id: ADM-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT overwrite the 2026-08-02 sign-off. It records a real owner verdict against a real artifact and remains the correct verdict for the phase as it was built; the new verdict is an amendment that supersedes it, and erasing the first would destroy the record of why a second was needed."
    - requirement_id: ADM-04
      category: accessibility
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT accept a distinction that survives only in colour. The three red-family accents are peach, red and maroon -- adjacent hues whose desaturated bands were already measured as near-identical; a sign-off given from the colour render, or from remembered hues, answers nothing about greyscale."
---

<objective>
Re-take ADM-04's evidence and its sign-off under the new taxonomy: extend the greyscale probe so
attention, danger and error appear as three separate adjacent boxes, re-render the artifact from
post-change code, and stop at a blocking human checkpoint for the owner's judgement.

Purpose: ADM-04 is the milestone's only `[V]` requirement, and the artifact on record was rendered
from code that folded the three red-family types onto one function. It is a faithful picture of the
phase as it shipped and a misleading picture of the phase as it will now ship. The owner's original
complaint — recorded in 39-UAT.md as 「うわ、デンジャーはgentle-clueのデンジャーに振った方が良かった
かも」 — was precisely about the red family reading as one thing, so the question ADM-04 asks has
materially changed and must be asked again.

Output: an extended probe fixture, a re-rendered `39-ADM04-GREYSCALE.png`, and a dated amendment to
`39-ADM04-SIGNOFF.md`. This plan touches no source and no test module. It is expected to stop at the
checkpoint if the owner cannot distinguish the kinds — D-08 pre-agrees that outcome and pre-agrees
that no fallback lever is chosen until then.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-UAT.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-RESEARCH.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-VALIDATION.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-04-SUMMARY.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-07-SUMMARY.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-11-SUMMARY.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Step 1 is doubly load-bearing here. This worktree must sync from a `pyproject.toml` carrying the
`pillow` entry plan 39-04 added, AND its `typsphinx` must be the worktree's own copy carrying plan
39-11's routing change. If either is stale the rendered artifact is evidence of the wrong thing, and
nothing downstream would catch it.
</environment_setup>

<artifacts_this_phase_produces>
New symbols created by THIS plan:

- A seventh box in `tests/fixtures/admonition_greyscale_probe/index.rst` — a `.. danger::` directive —
  and a re-ordering of the existing boxes so that error, danger and attention are contiguous.
- A regenerated `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png`
  (same path, new bytes — the previous artifact's history stays in git).
- A new dated section in `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md`
  headed as an amendment for gap G-39-1, containing its own numbered elements 1 through 6 mirroring
  the original sign-off's structure.

No new file, no new test function, no new fixture directory and no new script is created by this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the probe to the full red family and re-render from post-change code</name>
  <read_first>
    - `tests/fixtures/admonition_greyscale_probe/index.rst` in full — the six boxes it carries today
      (note, tip, seealso, warning, error, attention) and their body text lengths, which is what keeps
      the document to one page.
    - `tests/fixtures/admonition_greyscale_probe/conf.py` in full — its header comment recording the
      one-page requirement, why `typst_documents` must list `index` as a master document, and why the
      bundled default template is replaced by `_templates/minimal.typ`.
    - `tests/fixtures/admonition_greyscale_probe/_templates/minimal.typ` — the template's own header
      comment on opting out of the title page and table of contents.
    - `scripts/render_admonition_greyscale.py` in full — its signature, its command-line form, its
      single-page guard and the `RuntimeError` it raises, `DEFAULT_PPI`, and the BT.601-versus-BT.709
      paragraph in its module docstring.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-RESEARCH.md` § "Pitfall 4" — why the
      one-page constraint exists and why page-template handling is deliberately unsupported.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-11-SUMMARY.md` — confirmation that
      the routing change landed, which is the precondition for this render evidencing the right thing.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md` §2 — the provenance
      block the amendment will mirror: artifact path, render commit, PPI, probe fixture, and the
      recorded ordering of the pre-render gate check.
  </read_first>
  <files>tests/fixtures/admonition_greyscale_probe/index.rst, .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png</files>
  <action>
Add a `.. danger::` directive to the probe and re-order the boxes so the three red-family types —
error, danger and attention — are contiguous, in that order, at the end of the document. Adjacency is
the point: the question the owner is being asked is whether three near-hue title bands separate once
desaturated, and boxes separated by unrelated ones make that harder to judge than it is in a real
document. Keep the existing four non-red boxes in their current relative order ahead of them.

Give each box a one-line body that names its own directive in plain words, so the owner can check
their reading against the source without the render depending on long prose. Keep every body short:
seven boxes is one more than the fixture was sized for, and the one-page constraint is hard. Update
the fixture's header comment to record that it now carries seven boxes, that the three red-family
types are deliberately adjacent, and that the file is sized against a one-page limit.

Before rendering, confirm the worktree actually carries plan 39-11's change: run
`uv run pytest tests/test_admonition_bucket_render_gate.py tests/test_admonition_locale_title_precedence_gate.py -x`
and require both green. A render taken from a tree without the routing change would reproduce exactly
the folded artifact this plan exists to replace, and would look correct.

Then drive `scripts/render_admonition_greyscale.py` against the probe, writing to the existing path
`.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png`. Use the script's own
default PPI unless the icons are too small to judge shape at normal viewing size, in which case raise
it and record the value and the reason.

If the script raises its single-page error, shorten the probe bodies — or, as a last resort, drop one
of the four non-red boxes and record which and why. Do NOT add page-template handling to the script:
Pitfall 4 records that the pipeline deliberately does not support it, and a multi-page artifact that
silently shows only the first page is the exact failure mode the guard exists to prevent.

Also produce, in a temporary location outside the repository, the un-desaturated colour render of the
same probe. It is not committed. It exists so the checkpoint can offer it as a reference AFTER the
owner has judged the greyscale version. Record its path in the plan summary.

Confirm before committing: the PNG exists, is non-empty, opens with Pillow in the single-channel mode,
has non-zero dimensions on both axes, and shows seven boxes. **This task does not verify ADM-04.** It
produces the artifact the verification is performed on. State that in the plan summary in words, so no
downstream reader mistakes a successful render for a met requirement.
  </action>
  <acceptance_criteria>
    - `grep -c '^\.\. danger::' tests/fixtures/admonition_greyscale_probe/index.rst` returns 1, and `grep -c '^\.\. attention::\|^\.\. error::' tests/fixtures/admonition_greyscale_probe/index.rst` returns 2.
    - `grep -n '^\.\. error::\|^\.\. danger::\|^\.\. attention::' tests/fixtures/admonition_greyscale_probe/index.rst` shows the three on strictly increasing line numbers with no other directive line between them.
    - `uv run pytest tests/test_admonition_bucket_render_gate.py tests/test_admonition_locale_title_precedence_gate.py -x` is green in this worktree BEFORE the render, and that ordering is recorded in the plan summary.
    - `uv run pytest tests/test_admonition_greyscale_pipeline.py -v` is green — the pipeline smoke test still passes against the extended probe, including its one-page path.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png` exists, is non-empty, and its git status is modified rather than untracked — the artifact was regenerated at the existing path.
    - `uv run python -c "from PIL import Image; im = Image.open('.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png'); assert im.mode == 'L'; assert im.size[0] > 0 and im.size[1] > 0; print(im.mode, im.size)"` exits 0 and prints the mode and dimensions.
    - The rendered bytes differ from the committed artifact: `git diff --stat -- .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png` is non-empty.
    - The PPI used, the render commit SHA, and the temporary colour render's path are all recorded in the plan summary.
    - `git diff --stat -- typsphinx/` is empty, and the only path under `tests/` in this task's diff is the probe fixture's `index.rst`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_admonition_bucket_render_gate.py tests/test_admonition_locale_title_precedence_gate.py tests/test_admonition_greyscale_pipeline.py -x &amp;&amp; uv run python -c "from PIL import Image; im = Image.open('.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png'); assert im.mode == 'L' and im.size[0] > 0 and im.size[1] > 0; print(im.mode, im.size)" &amp;&amp; test -z "$(git diff --stat -- typsphinx/)"</automated>
  </verify>
  <done>A single-channel, one-page render of the seven-box probe with three adjacent red-family boxes is committed at the existing path, taken from a worktree proven to carry the routing change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner sign-off that the three red-family kinds separate in greyscale</name>
  <read_first>
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md` in full — the
      2026-08-02 verdict, its three verbatim Japanese quotations, the reconciliation paragraph
      explaining why Part 3 is operative, and §6's record of the two levers and the one that does not
      exist.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-VALIDATION.md` § "Manual-Only
      Verifications" — the single row this checkpoint re-discharges and its carried caveat.
    - `.planning/REQUIREMENTS.md` ADM-04 in full, including its `[V]` marker and the recorded
      "met on icon-shape grounds, luminance uniform" caveat now on the requirement itself.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` D-06, D-07 and D-08 —
      no styling change, the Pillow tooling decision, and that no fallback lever is chosen in advance.
    - The artifact produced by Task 1, and the temporary colour render whose path Task 1 recorded.
  </read_first>
  <files>(no files modified — the owner's answer is recorded in 39-12-SUMMARY.md and consumed by Task 3)</files>
  <action>
Halt and present the material below. Do not self-approve and do not proceed on any auto-advance
setting: ADM-04 is the milestone's only `[V]` requirement and REQUIREMENTS.md's own legend marks it
human-only.

Offer no automated stand-in. Do not compute a contrast ratio, a luminance difference or a pixel diff
between the memo and error bands and present it as supporting evidence. Do not tell the owner that the
new taxonomy is expected to improve the verdict — that expectation is recorded in the gap and is
probably right, and saying it here would anchor the judgement the checkpoint exists to obtain.

Record the owner's response verbatim, in their own language, with an accurate English rendering that
preserves the nuance rather than paraphrasing it — the discipline §4 of the existing sign-off already
established for this phase. If they cannot distinguish the kinds, capture which specific pairs, and
which lever they choose against the render, then stop. Do not begin the lever work inside this plan
and do not apply any styling change here.
  </action>
  <what-built>
`.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png` has been re-rendered
from the tree that now carries the new taxonomy. It contains seven boxes:

- note group — one `.. note::`
- success group — one `.. tip::` and one `.. seealso::`
- warning group — one `.. warning::`
- red family, adjacent at the end — one `.. error::`, one `.. danger::`, one `.. attention::`

What changed since the artifact you signed off on 2026-08-02: back then all three red-family types
were one box style. They are now three. `danger` uses the package's own danger style (peach accent,
lightning icon), `attention` uses its memo style (maroon accent, exclamation icon), and `error` is
unchanged (red accent, cross icon). Those hex values are measured from the pinned package sources and
are provenance only — no colour literal is emitted anywhere.

Deliberately, **no styling change was made** (D-06). The claim you are being asked to judge is the one
ADM-04 words: that the distinction is carried by icon and border rather than by hue alone.

Context you should have before deciding:

- The three red-family accents are adjacent hues. Their desaturated title bands were already measured
  as close together, and the four-group band spread was measured at about 5.4 percentage points.
  Expect the bands to look similar. That is not the failure; that is the premise ADM-04 states.
- The left accent stroke (2pt, accent at full saturation) spans about 35.9 points and is the strongest
  greyscale signal available.
- The icons differ by shape and their fills are baked into the source images rather than tinted by the
  box's accent, so they survive desaturation as shapes.
- The luminance numbers in 39-CONTEXT.md's D-06 table were computed with the BT.709 weights while this
  desaturation used Pillow's BT.601 weights. **Judge the image, not the table** — the greys will not
  match the predicted percentages, and that is expected.
- Your recorded 2026-08-02 verdict — met on icon shape, luminance uniform and carrying no signal —
  stands as the correct verdict for the phase as it was built. This checkpoint is not re-opening it;
  it is asking the same question of a different picture.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-GREYSCALE.png` at normal
   viewing size — not zoomed in. Zooming answers a different question than the one a reader of a PDF
   would be asking.
2. Without reading the body text and without looking at the colour version, decide whether you can
   tell which box is which kind. The signals available are icon shape and the left border.
3. Then the specific question this gap turns on: the last three boxes are the red family. Do the
   `error`, `danger` and `attention` boxes read as three separate kinds, or do any two of them merge?
   Name the pair if any two merge — the `attention` (maroon) and `error` (red) title bands are the
   pair most likely to, and a verdict that names them is more useful than a verdict that does not.
4. Also check the groups that did not change: does `seealso` still read as the same kind as `tip`, and
   are the four groups still tellable apart from each other?
5. Optionally compare against the un-desaturated colour render whose path is recorded in the plan
   summary — but only after steps 2 and 3, so colour does not prime the judgement.
6. If you cannot distinguish them: say so, and say what you would want changed. **No fallback lever
   was chosen in advance** (D-08) — deliberately, so the lever is picked now, against what you are
   actually looking at. Two levers exist: a per-bucket border thickness, and a per-bucket header band
   colour. A dashed border does **not** exist as an option — the box's left edge takes only a
   thickness, a paint and a cap, verified against the package sources. The gap is expected to stop
   here in that case; that is a planned outcome, not a failure of the plan.
  </how-to-verify>
  <resume-signal>Type "approved" to record the sign-off, or describe which kinds you cannot distinguish and which lever you want considered.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the re-taken sign-off as a dated amendment</name>
  <read_first>
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md` in full — its six
      numbered sections, so the amendment mirrors that structure rather than inventing a new one.
    - `.planning/ROADMAP.md` Phase 39 success criterion 4 — the exact wording of what must be
      recorded: the render and the sign-off, both in the phase artifacts.
    - `.planning/REQUIREMENTS.md` ADM-04 in full — so the record states what was judged in the
      requirement's own terms and so the plan summary can say whether the requirement's recorded
      caveat still holds.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` D-06 and D-08.
    - The owner's answer from Task 2, verbatim.
  </read_first>
  <files>.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md</files>
  <action>
Append a new dated section to the existing file. Do not edit, reword, renumber or delete anything
above it: the 2026-08-02 verdict, its three verbatim quotations and its reconciliation paragraph all
stay exactly as they are. Add a one-line pointer near the top of the file, immediately under the
existing status line, stating that the verdict below was amended under gap G-39-1 on the amendment's
date, and naming the heading to jump to. That pointer is the only change permitted above the new
section.

The amendment contains, in this order:

1. The requirement under judgement, quoted from REQUIREMENTS.md as it reads after plan 39-10's
   amendments, with its `[V]` marking noted and a one-line statement that no automated assertion
   exists or was offered for it.
2. Why the sign-off was re-taken: the previous artifact was rendered from code that folded the three
   red-family types onto one function, and gap G-39-1 reverses that. State plainly that the previous
   verdict was correct for the phase as built and is superseded rather than withdrawn.
3. Provenance: the artifact path, the commit it was rendered at, the PPI used, the probe fixture path
   and its new box count, and the confirmation that both routing gates were green in the same worktree
   before the render was taken.
4. The desaturation caveat, restated: the judgement was made against the rendered artifact and never
   against 39-CONTEXT.md's D-06 luminance table, because that table used BT.709 weights while the
   desaturation used BT.601.
5. The owner's answer, quoted verbatim in their own language with an accurate English rendering, and
   the date. If the answer names a specific pair that merged or separated, quote that part exactly —
   it is the answer to the adjacency question this gap turns on and it must not be smoothed into a
   general statement.
6. The outcome, stated unambiguously as one of: the kinds are distinguishable and ADM-04 remains met
   with no styling change per D-06, including whatever caveat the owner attaches; or the owner cannot
   distinguish some pair, in which case record exactly which pair, which lever they chose against the
   render per D-08, and that the gap stops here pending that work. The word "pending" must not be the
   final verdict.
7. A short note that the levers considered and their status were presented before the decision,
   including that a dashed border is not available because the box's left edge takes only a thickness,
   a paint and a cap.

If the outcome is negative, also file the follow-up as a pending todo under `.planning/todos/pending/`
naming the chosen lever, this phase and gap G-39-1, and state in the plan summary that plan 39-13 must
NOT mark the gap closed.
  </action>
  <acceptance_criteria>
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md` contains a new section whose heading names gap `G-39-1` and carries a date later than 2026-08-02 or equal to it with an explicit amendment marker.
    - The original verdict survives verbatim: `grep -c 'アイコンは異なるため区別はつく、明度は一緒として記録せよ' .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md` returns 1.
    - `git diff --numstat -- .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md` reports a deletion count no greater than 1 (the status line replaced by its pointer-bearing form), and the plan summary names that line.
    - The amendment contains all seven numbered elements above.
    - It quotes the owner's new answer verbatim and carries a date.
    - It names the artifact path, the render commit SHA and the PPI.
    - It states the BT.601-versus-BT.709 caveat explicitly.
    - It states one of the two outcomes unambiguously and the word `pending` does not appear as the final verdict.
    - If the outcome is negative, a pending todo file exists under `.planning/todos/pending/` naming the chosen lever and gap `G-39-1`.
    - `git diff --stat -- typsphinx/` is empty for this plan's commits, in either outcome.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md &amp;&amp; grep -q 'G-39-1' .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md &amp;&amp; grep -qi 'BT.601' .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md &amp;&amp; grep -q 'アイコンは異なるため区別はつく' .planning/phases/39-admonition-taxonomy-rubric-nesting/39-ADM04-SIGNOFF.md &amp;&amp; test -z "$(git diff --stat -- typsphinx/)"</automated>
  </verify>
  <done>ADM-04's re-taken outcome is recorded as a dated amendment quoting the owner verbatim, with the new render's provenance attached and the original verdict preserved intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator → `.typ` → `typst.compile` → PNG → Pillow → committed artifact | The evidence chain. A break anywhere yields a plausible image that evidences the wrong build, the wrong colour space or the wrong page. |
| committed artifact → human judgement → recorded verdict | The verification itself. Deliberately human, with no mechanical substitute. |
| the 2026-08-02 verdict on record → the amended verdict | Two verdicts on one requirement. Which is operative must be unambiguous from the file alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-32 | Spoofing | An artifact rendered from a tree without the routing change, showing the folded red family while presented as evidence for the sub-divided one — indistinguishable from correct evidence at a glance | high | mitigate | Task 1 requires both routing gates green in the SAME worktree BEFORE the render, records that ordering, records the render commit in the amendment, and gates on the PNG's bytes actually differing from the committed artifact. `depends_on` places this plan after 39-11 rather than beside it. |
| T-39-33 | Repudiation | A `[V]` requirement closed by a computed stand-in — a band-luminance delta between the memo and error styles — that greens without anyone having looked | high | mitigate | No automated assertion for ADM-04 exists anywhere in this plan; the automated checks only confirm the artifact was produced from the right tree. The plan is non-autonomous and the checkpoint is blocking. Reinforced by the first `must_haves.prohibitions` entry. |
| T-39-34 | Tampering | The existing artifact reused or re-committed unchanged, so the sign-off is re-taken against the picture it was meant to replace | high | mitigate | An acceptance criterion requires the PNG's bytes to differ from the committed version and requires the probe to carry the new box, so an unchanged artifact fails mechanically rather than by inspection. Reinforced by the second `must_haves.prohibitions` entry. |
| T-39-35 | Tampering | The predicted improvement stated to the owner as a conclusion at the checkpoint, anchoring the judgement the checkpoint exists to obtain | medium | mitigate | Task 2 forbids it explicitly and the `what-built` block deliberately states the premise (adjacent hues, similar bands) rather than the expectation. Reinforced by the third `must_haves.prohibitions` entry. |
| T-39-36 | Repudiation | The 2026-08-02 verdict overwritten, destroying the record of why a second sign-off was needed and leaving `39-07-SUMMARY.md`'s citations dangling | medium | mitigate | Task 3 appends and gates on the original Japanese verdict surviving verbatim and on a deletion count no greater than one named line. Reinforced by the fourth `must_haves.prohibitions` entry. |
| T-39-37 | Denial of service | A seven-box probe overflowing to two pages, where typst-py's PNG export silently yields only the first page under a filename claiming to be the whole document | medium | mitigate | The render script already raises rather than writing a partial artifact (Pitfall 4). Task 1 requires the pipeline smoke test green against the extended probe and instructs shortening the probe rather than adding page-template handling. |
| T-39-SC | Tampering | npm/pip/cargo installs — supply chain | high | mitigate | This plan installs nothing. `pillow` entered the dev extra in plan 39-04 behind its blocking legitimacy checkpoint and is unchanged here. Verified by `git diff -- pyproject.toml` being empty over this plan's commits. |

ASVS L1: V5 Input Validation is the only applicable category for the translator itself — a build-time
Sphinx extension has no runtime authentication, session, access-control or cryptography surface
(`39-RESEARCH.md` § Security Domain; `39-SECURITY.md` carries the phase's register, whose numbering
this table continues). Blocking threshold is `high`. T-39-32, T-39-33, T-39-34 and T-39-SC are `high`;
all four are `mitigate` and all four mitigations land inside this plan. Nothing blocks apart from the
intended human checkpoint.
</threat_model>

<verification>
- The probe carries seven boxes with error, danger and attention contiguous, and `tests/test_admonition_greyscale_pipeline.py` is green against it.
- Both routing gates were green in this worktree before the render was taken, and that ordering is recorded.
- `39-ADM04-GREYSCALE.png` opens in the single-channel mode with non-zero dimensions, its bytes differ from the previously committed artifact, and it shows seven boxes.
- The checkpoint was answered by a human and the answer is quoted verbatim in the amendment.
- `39-ADM04-SIGNOFF.md` still contains the 2026-08-02 verdict verbatim and now states unambiguously which verdict is operative.
- `git diff --stat -- typsphinx/` is empty for this plan's commits, and the only `tests/` path in the diff is the probe fixture's `index.rst`.
- No automated assertion anywhere in this plan claims to verify ADM-04.
</verification>

<success_criteria>
- A greyscale render of the seven-box probe, with the three red-family types adjacent, is committed at the existing path with its provenance recorded.
- The owner's judgement is taken against that artifact rather than a predicted luminance table, and is recorded verbatim, including a named verdict on the memo/error adjacency question.
- No styling change was made and no fallback lever was chosen in advance.
- The 2026-08-02 verdict survives intact and the file states unambiguously which verdict is now operative.
- If the owner cannot distinguish the kinds, the gap stops here with the chosen lever filed as a pending todo — a planned outcome, not a failure.
</success_criteria>

<output>
Create `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-12-SUMMARY.md` when done.
</output>
