---
phase: 39-admonition-taxonomy-rubric-nesting
plan: 05
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - typsphinx/translator.py
  - tests/test_admonitions.py
  - tests/test_topics.py
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03]

must_haves:
  truths:
    - "seealso emits the same gentle-clues function as hint and tip, so ADM-01's success bucket is one function (D-02). No accent colour argument is passed anywhere -- a bucket is expressed as a function name, never as a colour literal (D-01)."
    - "attention and danger both emit the same function as error, so ADM-02's red bucket is one function (D-03). The previously distinct danger function disappears from the codebase as a value passed by any call site."
    - "The generic `.. admonition::` emits the notify function (D-09) and a non-contents `.. topic::` emits the abstract function (D-10). The gentle-clues base clue function is no longer passed by any call site; the box-less `.. contents::` path is untouched."
    - "All ten real admonition types take their static title from sphinx.locale.admonitionlabels, looked up once inside the shared helper by the docutils node class name (D-04, D-05). todo_node keeps its own inert fallback because it is not a catalog key; the generic admonition and topic get no static title because they are not catalog keys either, so their directive-supplied titles keep flowing through the existing buffer swap."
    - "A directive-supplied title still wins over the catalog title. The dynamic title path is checked first in the depart helper and that precedence is unchanged."
    - "The static-title branch routes through the project's single escaping helper before interpolation, after coercing the lazy i18n proxy to a real string. No second, title-specific escaping routine is introduced (T-39-01)."
    - "Two measured regressions are accepted as designed, not treated as bugs (D-05): under a Japanese build `.. tip::` moves from the gentle-clues catalog title to Sphinx's untranslated one, and `.. note::` moves to Sphinx's own term; and seealso's English literal changes casing on its second word."
    - "The four now-misleading test function names in tests/test_admonitions.py are renamed in the same commit as their body edits, because a stale name is itself incorrect documentation (D-14)."
    - "The thirteen assertions in tests/test_admonitions.py whose bucket does not move and whose English title is byte-identical are NOT edited."
  artifacts:
    - typsphinx/translator.py
    - tests/test_admonitions.py
    - tests/test_topics.py
  key_links:
    - "_visit_admonition -> sphinx.locale.admonitionlabels, keyed on node.__class__.__name__; verified byte-identical for all ten keys, which is why no mapping table is needed"
    - "_depart_admonition's static-title branch -> escape_typst_string (typsphinx/translator.py:32), the project's single source of truth for Typst string-literal escaping"
    - "_depart_admonition's dynamic-title branch -> _pending_admonition_title, populated by visit_title's admonition-aware buffer swap; this path must keep priority over the catalog"
    - "the five changed call sites -> tests/test_admonition_bucket_render_gate.py's region-scoped assertions from plan 39-01, which flip from RED to GREEN as a result"
  prohibitions:
    - requirement_id: ADM-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT let the newly-introduced catalog title override a title the document author wrote. A generic admonition's whole purpose is its custom title; a lookup that silently wins over it would replace the author's words with a default and still pass every bucket assertion in this phase."
    - requirement_id: ADM-01
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT reach a bucket by passing an accent colour argument. D-01 makes a bucket a function name so that emitted admonitions carry no colour literals and stay coupled to the package theme; passing a colour would satisfy every assertion here while permanently enlarging the exact-string surface of every future admonition change."
    - requirement_id: ADM-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT leave a handler docstring describing the old bucket or the old title. Every docstring this change falsifies is corrected in the same commit -- a docstring that names the function a handler no longer emits is the same class of defect as the misleading test names D-14 requires renaming."
---

<objective>
Implement the admonition taxonomy: re-route the five call sites whose bucket moves, centralise the
title source on `sphinx.locale.admonitionlabels`, escape the static title, and migrate the five
in-process assertions and four test-function names the change falsifies.

Purpose: this is the phase's product change. ADM-01, ADM-02 and ADM-03 are all discharged here, and
the RED that plan 39-01 recorded — five region-scoped bucket assertions and a ten-type catalog-title
assertion — flips to GREEN.

Output: an edited `typsphinx/translator.py`, four renamed and five re-derived assertions in
`tests/test_admonitions.py`, and two re-derived assertions in `tests/test_topics.py`. This plan does
NOT touch `visit_rubric`, `depart_rubric`, `visit_strong` or `visit_desc_signature` — plan 39-06 owns
the rubric handler and Phase 37's `desc_signature` is a fixed point.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-RESEARCH.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-PATTERNS.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-VALIDATION.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-01-SUMMARY.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Skipping step 1 makes the worktree's pytest import the MAIN checkout's `typsphinx` package, so a
correct fix reports the wrong colour.
</environment_setup>

<artifacts_this_phase_produces>
See `39-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan adds the
`admonitionlabels` import to `typsphinx/translator.py`, changes the gentle-clues function name passed
by five call sites, removes two now-dead `custom_title=` keyword arguments, adds one
`escape_typst_string` call in `_depart_admonition`, and renames four test functions in
`tests/test_admonitions.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-route the five call sites whose bucket moves</name>
  <read_first>
    - `typsphinx/translator.py` lines 4400-4560 — every one of the thirteen `visit_X` admonition call
      sites in full, including their docstrings. Read the actual current argument at each site before
      changing it; several docstrings name a function the site no longer matches even today.
    - `typsphinx/translator.py` lines 4337-4399 (`_visit_admonition` / `_depart_admonition`) — the
      shared helper whose plumbing is NOT changed by this task, only the arguments reaching it.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` § "The bucket taxonomy"
      — the locked table, including D-01's rule that a bucket is a function name and never a colour
      argument, and the rejected alternatives with their measured reasons.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-01-SUMMARY.md` and
      `39-GATE-EVIDENCE-01.md` — the recorded RED this task must flip.
    - `tests/test_admonition_bucket_render_gate.py` (as created by plan 39-01) — the region-scoped
      assertions that will judge this task.
  </read_first>
  <behavior>
    - The seealso handler's emitted box opens with the same function `visit_hint` and `visit_tip` pass.
    - The danger handler's emitted box opens with the same function `visit_error` passes.
    - The attention handler's emitted box opens with the same function `visit_error` passes.
    - The generic admonition handler's emitted box opens with the gentle-clues notify function.
    - The non-contents branch of the topic handler's emitted box opens with the gentle-clues abstract function.
    - The seven control types (note, warning, tip, important, caution, hint, error) emit exactly the function they emit today.
    - The `.. contents::` topic branch returns early exactly as it does today and emits no box.
  </behavior>
  <files>typsphinx/translator.py</files>
  <action>
Change the gentle-clues function name passed at exactly five call sites, per 39-CONTEXT.md's locked
table. Change nothing else about how the box is opened — no keyword argument is added, and in
particular no accent colour argument is introduced anywhere (D-01).

- `visit_seealso`: pass the same function `visit_hint` and `visit_tip` already pass, instead of the
  one `visit_note` passes (D-02, ADM-01).
- `visit_danger`: pass the same function `visit_error` already passes, instead of its own
  same-named function (D-03, ADM-02).
- `visit_attention`: pass the same function `visit_error` already passes, instead of the one
  `visit_warning` passes (D-03, ADM-02).
- `visit_admonition`: pass the gentle-clues notify function instead of the base clue function (D-09,
  ADM-03).
- `visit_topic`, in its non-contents branch only: pass the gentle-clues abstract function instead of
  the base clue function (D-10). Leave the contents-branch early return, its id anchoring and
  `depart_topic`'s matching early return exactly as they are.

Rewrite every docstring this falsifies, in the same commit. That includes at minimum: `visit_seealso`,
`visit_danger`, `visit_attention`, `visit_admonition` and `visit_topic`, each of which currently names
the function it emits and in three cases explains a mapping that no longer holds. Replace the stale
mapping rationale with the decision that now governs — name D-02, D-03, D-09 or D-10 and state the
bucket. Also check `visit_hint`'s and `visit_attention`'s docstrings, which cite an earlier phase's
mapping research as their justification; that justification is superseded here.

No import change is needed for the two newly-used functions: the emitted template imports gentle-clues
with a wildcard, so `notify` and `abstract` are already in scope, and this phase does not touch the
pinned version in any of the places `tests/test_preview_version_sync.py` watches.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_admonition_bucket_render_gate.py -v` is fully green, including the five previously-RED bucket assertions and the seven CONTROL assertions.
    - `uv run pytest tests/test_preview_version_sync.py -x` is green — the pin is untouched.
    - No call to `_visit_admonition` anywhere in `typsphinx/` passes a keyword argument other than `custom_title`; `grep -nE '_visit_admonition\(' typsphinx/translator.py` shows only positional node plus function-name arguments and, where still present at this task's end, `custom_title=`.
    - The gentle-clues base clue function is no longer passed by any call site: `grep -vE '^\s*#' typsphinx/translator.py | grep -cE '_visit_admonition\([^)]*"clue"' ` returns 0.
    - The five edited handlers' docstrings each name the governing decision identifier.
    - `uv run mypy typsphinx/` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_admonition_bucket_render_gate.py tests/test_preview_version_sync.py -v &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <done>The five bucket moves are implemented, the seven controls are unchanged, and every docstring the change falsified is corrected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Source the static admonition title from the Sphinx locale catalog and escape it</name>
  <read_first>
    - `typsphinx/translator.py` lines 4337-4399 (`_visit_admonition` / `_depart_admonition` in full) —
      where the static title is stashed and where it is interpolated, and the comment explaining that
      the dynamic title takes precedence.
    - `typsphinx/translator.py` lines 25-64 (`escape_typst_string` in full, including the note that it
      is the single source of truth and that escaping order is significant).
    - `typsphinx/translator.py` lines 296-312 — the `_custom_admonition_title` attribute declaration
      and its inline comment, which names two of the literals this change removes.
    - `typsphinx/translator.py` lines 599-690 (`visit_title` / `depart_title`'s admonition-aware
      branch) — the dynamic-title buffer swap whose precedence must not change.
    - `typsphinx/translator.py` lines 4459-4492 (`visit_todo_node`) — its docstring explains that its
      static title is an inert fallback and that the node supplies its real title; that path must stay
      exactly as it is.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` § "Admonition titles"
      (D-04, D-05, and the note on `todo_node`) and `39-RESEARCH.md` § "Bucket Routing Table" (the
      verified fact that every catalog key is byte-identical to its docutils node class name, and why
      an explicit string coercion of the lazy proxy is load-bearing rather than defensive filler).
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-RESEARCH.md` § "Common Pitfalls"
      Pitfall 3 — the unescaped static-title branch and the one-line fix.
  </read_first>
  <behavior>
    - Each of the ten real admonition types emits a title argument whose value is that type's entry in the Sphinx locale catalog, looked up by the node's class name.
    - A todo node still emits its node-supplied title, and its handler's own fallback argument still reaches the helper unchanged because its class name is not a catalog key.
    - A generic admonition and a non-contents topic still emit their directive-supplied title; no static title is injected for either, because neither class name is a catalog key.
    - When a node supplies its own title, that title is emitted and the catalog value is not, at every type.
    - A title containing a double quote or a backslash is emitted through the escaping helper and does not break the emitted string literal.
  </behavior>
  <files>typsphinx/translator.py</files>
  <action>
Add a module-level import of `admonitionlabels` from `sphinx.locale`, placed with the other Sphinx
imports at the top of the file.

Inside `_visit_admonition`, before the static title is stashed, look the catalog up once by the node's
class name. When the lookup finds an entry, coerce it to a real string and use it as the static title,
overriding whatever the caller passed. When it does not, leave the caller's argument untouched. This
one edit at the shared helper is the whole of D-04/D-05's implementation and is deliberately chosen
over ten individual call-site edits, because every catalog key is byte-identical to its docutils node
class name — verified directly, and re-verify it in this task before relying on it.

Three consequences must be checked, not assumed, because they are what make the single edit correct:

- `todo_node` is not a catalog key, so its handler's own fallback argument survives and that path is
  unchanged.
- The generic admonition's and the topic's class names are not catalog keys, so no static title is
  injected for either and their directive-supplied titles keep flowing through `visit_title`'s buffer
  swap unchanged.
- The dynamic title still wins. `_depart_admonition` checks the pending dynamic title first; do not
  reorder that check, and add a regression assertion for it in Task 3.

The string coercion is load-bearing, not defensive: the catalog's values are Sphinx lazy i18n proxies,
not plain strings, and the next step performs string operations on the value.

Then remove the two now-dead `custom_title=` keyword arguments: `visit_important`'s and
`visit_seealso`'s. The catalog supplies both, and for seealso it supplies the different, correct
casing on the second word. Leave `visit_todo_node`'s argument in place.

Finally, in `_depart_admonition`'s static-title branch, route the value through the project's existing
`escape_typst_string` helper before interpolating it into the Typst string literal, coercing to `str`
first. Do not add a second, title-specific escaping routine — this is T-39-01's one-line mitigation
and the project's rule is one escaping helper.

Update the `_custom_admonition_title` attribute's inline comment, which currently names two hardcoded
literals that no longer exist, to say that the value now comes from the Sphinx locale catalog for the
ten real types and remains a caller-supplied fallback only for the todo path.
  </action>
  <acceptance_criteria>
    - `grep -c 'admonitionlabels' typsphinx/translator.py` is at least 2 (the import and the lookup).
    - `uv run pytest tests/test_admonition_bucket_render_gate.py -v` is fully green, including the ten-type catalog-title assertion that was RED before this plan.
    - `uv run pytest tests/test_pdf_render_gate.py -k AdmonitionPdfRenderGate -k AdmonitionTitleRegression -v` — run both selectors — is green, including plan 39-01's new header-text assertions and the pre-existing multi-child directive-title assertion.
    - A build with a directive-supplied admonition title still emits that title, not the catalog default: provable by `uv run pytest tests/test_admonitions.py -k title -v` staying green.
    - The escaping call is present in `_depart_admonition`'s static-title branch; a one-off `uv run python -c` round trip through the translator with a title containing a double quote emits a valid, compilable `.typ` — record that invocation and its output in the plan summary.
    - `uv run python -c "from sphinx.locale import admonitionlabels; import docutils.nodes as n; from sphinx import addnodes; assert all(k in admonitionlabels for k in ('note','warning','tip','important','caution','seealso','hint','error','danger','attention'))"` exits 0, re-verifying the key set this task depends on.
    - `uv run mypy typsphinx/` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_admonition_bucket_render_gate.py -v &amp;&amp; uv run pytest tests/test_pdf_render_gate.py -k AdmonitionTitleRegression -v &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <done>All ten types take their title from the Sphinx locale catalog through one lookup in the shared helper, the dynamic title still wins, and the static title is escaped through the project's single helper.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the falsified in-process assertions and rename the four misleading tests</name>
  <read_first>
    - `tests/test_admonitions.py` (full file, 434 lines) — every assertion, so the thirteen that must
      NOT be edited are identified positively rather than by omission.
    - `tests/test_topics.py` (full file) — its three clue-related assertions, including the box-less
      contents control that must stay as it is.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-RESEARCH.md` § "Blast-Radius
      Re-Take" — the line-by-line map of which of the eighteen assertions goes RED and why the other
      thirteen do not, and the instruction that the four affected test NAMES are themselves part of
      the migration.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` D-05 and D-14 — the two
      accepted measured regressions, and the rule that expected strings are re-derived by hand and
      never by copying failing output.
  </read_first>
  <files>tests/test_admonitions.py, tests/test_topics.py</files>
  <action>
Migrate exactly the assertions the change falsifies, and rename exactly the test functions the change
makes untrue. Derive every new expected string by hand from 39-CONTEXT.md's bucket table and from
`sphinx.locale.admonitionlabels` read directly; never by copying a failure message.

In `tests/test_admonitions.py`:

- The seealso test: its box-open assertion moves to the success bucket's function, and its title
  assertion moves to the catalog's English value, whose second word differs in casing from the current
  literal. Rename the function so its name states the new bucket.
- The danger test: its box-open assertion moves to the error bucket's function. Rename the function so
  its name states the new bucket rather than repeating the type name.
- The attention test: its box-open assertion moves to the error bucket's function. Rename the function
  so its name states the new bucket.
- The generic admonition test: its box-open assertion moves to the notify function. Rename the
  function so its name states the new function.
- Each of the four also carries a companion negative assertion asserting the markup-mode bracket form
  is absent for the same function name; update those companions to name the new function so they keep
  guarding the same property.
- Update each renamed function's docstring, which currently states the old mapping.
- Leave the other thirteen assertions untouched. Their buckets do not move and their English titles
  are byte-identical between the old hardcoded literal and the new catalog value.

Add one new test to the same file asserting the precedence property Task 2 must preserve: an
admonition node carrying its own title child emits that title and not the catalog value. Build it with
the file's existing document-construction idiom.

In `tests/test_topics.py`: the two assertions that a normal topic emits the base clue function move to
the abstract function. The third assertion — that the box-less `.. contents::` path emits no clue box
at all — is unaffected and must not be edited; add a comment recording that it was reviewed and left
alone deliberately.

Record in the plan summary the exact count of assertions edited and the exact count left untouched in
each file, so plan 39-08's census can be checked against it rather than re-derived.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_admonitions.py tests/test_topics.py -v` is fully green.
    - `uv run pytest tests/test_admonitions.py -k seealso -x`, `uv run pytest tests/test_admonitions.py -k "attention or danger" -x` and `uv run pytest tests/test_admonitions.py -k generic_admonition -x` each select at least one test and pass — proving the renames kept 39-VALIDATION.md's per-requirement selectors working.
    - Exactly four test functions are renamed in `tests/test_admonitions.py`; `git diff -- tests/test_admonitions.py` shows no other `def test_` line changed.
    - The new precedence test exists and fails if the catalog lookup is moved to override the dynamic title — provable by temporarily reordering the check in a scratch copy, recorded in the plan summary, and reverted.
    - `tests/test_topics.py`'s contents-path assertion is byte-unchanged apart from an added comment.
    - `uv run black --check tests/test_admonitions.py tests/test_topics.py` and `uv run ruff check tests/test_admonitions.py tests/test_topics.py` pass.
    - `uv run pytest -m "not slow"` is fully green.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_admonitions.py tests/test_topics.py -v &amp;&amp; uv run pytest tests/test_admonitions.py -k seealso -x &amp;&amp; uv run pytest tests/test_admonitions.py -k "attention or danger" -x &amp;&amp; uv run pytest tests/test_admonitions.py -k generic_admonition -x</automated>
  </verify>
  <done>Five assertions and four test names are migrated by hand-derivation, thirteen assertions are deliberately untouched, and a new test locks the directive-title precedence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `sphinx.locale.admonitionlabels` → translator → emitted `.typ` | An EXTERNAL catalog now supplies a string interpolated into a Typst string literal. This is the boundary this plan moves. |
| `.rst` directive argument → `visit_title` buffer swap → emitted `.typ` | The author-supplied title path, whose precedence over the catalog must not change |
| translator → emitted `.typ` → `typst.compile()` | Where an unescaped title becomes a build fatal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `_depart_admonition`'s static-title branch — interpolates the title into a Typst string literal with no escaping, and this plan changes that title's source from a hardcoded ASCII literal to the external `sphinx.locale.admonitionlabels` catalog | high | mitigate | Route the branch through the project's existing `escape_typst_string` (`typsphinx/translator.py:32`) after coercing the lazy i18n proxy with `str()` — Task 2. No second, title-specific escaping routine is introduced. Verified by a round trip through the translator with a quote-bearing title producing a compilable `.typ`. |
| T-39-12 | Spoofing | The catalog lookup silently overriding a directive-supplied title, replacing the author's words with a default while every bucket assertion still passes | high | mitigate | The dynamic-title check keeps priority in `_depart_admonition` and is not reordered; Task 3 adds a dedicated precedence test; reinforced by the first `must_haves.prohibitions` entry. |
| T-39-13 | Repudiation | A stale docstring or a stale test name asserting a mapping the code no longer has, so the next reader trusts documentation that is false | medium | mitigate | Every falsified docstring is rewritten in the same commit and the four misleading test names are renamed alongside their bodies (D-14); reinforced by the third `must_haves.prohibitions` entry. |
| T-39-SC | Tampering | npm/pip/cargo installs — supply chain | high | mitigate | This plan installs nothing and changes no `@preview` pin. `tests/test_preview_version_sync.py` is run as an explicit acceptance criterion rather than assumed green. Verified by `git diff -- pyproject.toml` over this plan's commits being empty. |

ASVS L1: V5 Input Validation is the only applicable category — a build-time Sphinx extension has no
runtime authentication, session, access-control or cryptography surface (39-RESEARCH.md § Security
Domain). Blocking threshold is `high`. T-39-01, T-39-12 and T-39-SC are `high`; all three are
`mitigate` and all three mitigations land inside this plan. Nothing blocks.
</threat_model>

<verification>
- `uv run pytest tests/test_admonition_bucket_render_gate.py -v` is fully green — every assertion
  plan 39-01 recorded RED has flipped, and every CONTROL is still green.
- `uv run pytest tests/test_pdf_render_gate.py -k AdmonitionPdfRenderGate -v` and
  `uv run pytest tests/test_pdf_render_gate.py -k AdmonitionTitleRegression -v` are both green.
- `uv run pytest tests/test_admonitions.py tests/test_topics.py -v` is fully green.
- `uv run pytest tests/test_preview_version_sync.py -x` is green — the gentle-clues pin is untouched.
- `uv run pytest -m "not slow"` is fully green.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
- `git diff -- typsphinx/translator.py` touches only the admonition zone, the import block and the
  `_custom_admonition_title` comment — `visit_rubric`, `depart_rubric`, `visit_strong`,
  `depart_strong`, `visit_desc_signature` and `depart_desc_signature` are byte-unchanged.
</verification>

<success_criteria>
- `seealso` is in the success bucket and `attention` and `danger` are in the error bucket, each
  expressed as a function name with no colour literal anywhere.
- The generic `.. admonition::` and the non-contents `.. topic::` each render as a distinct styled box,
  and the base clue function is passed by no call site.
- All ten real types take their title from `sphinx.locale.admonitionlabels` through a single lookup in
  the shared helper, with `todo_node`, the generic admonition and the topic correctly unaffected.
- A directive-supplied title still wins, locked by a dedicated test.
- The static title is escaped through the project's single escaping helper.
- Exactly five assertions and four test names are migrated by hand-derivation; the other thirteen are
  deliberately untouched.
</success_criteria>

<output>
Create `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-05-SUMMARY.md` when done.
</output>
