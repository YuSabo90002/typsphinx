---
phase: 41-v0-7-0-release-automation-release-prep
plan: 07
type: execute
wave: 3
depends_on: [41-04, 41-05, 41-06]
files_modified:
  - .planning/phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md
  - .planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md
autonomous: true
requirements: [REL-04, REL-05]

must_haves:
  truths:
    - "`git tag -l v0.7.0` and `git ls-remote --tags origin v0.7.0` BOTH produce EMPTY output at phase close, observed twice at two separate moments by two independent runs, with all four outputs transcribed verbatim (edge: empty, REL-05)."
    - "`41-RELEASE-EVIDENCE.md` carries one section per ROADMAP success criterion (SC#1 through SC#5), each stating what was proven, which evidence file proves it, and the verdict read FROM that file rather than re-derived — so no measured value is transcribed twice with a chance to drift."
    - "Every success criterion whose evidence file records a gap, a skip, or a NOT MET verdict is carried into `41-RELEASE-EVIDENCE.md` as exactly that, never upgraded to a pass at roll-up time."
    - "`41-HANDOFF.md` is a standalone checklist recording exactly what `/gsd-complete-milestone` will execute — merge, tag, `release.yml`, PyPI plus GitHub Release, and the standing second tag on the translations repository — with an Owner and an explicit Ordering dependency on every item."
    - "`41-HANDOFF.md` restates every excluded destructive action under a `Not done in this phase, by design` heading, and lists the close-side record work this phase deliberately did not do: flipping REL-04's and REL-05's checkboxes and Traceability rows, and filing the two todos this phase's own code work resolved."
    - "The four todos D-14 defers to v0.7.1+ are recorded in the handoff as still pending by design, so a close-side sweep does not mistake them for oversights."
  artifacts:
    - .planning/phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md
    - .planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md
  key_links:
    - "41-REL04-EVIDENCE.md -> 41-RELEASE-EVIDENCE.md SC#1"
    - "CHANGELOG.md + version lockstep -> 41-RELEASE-EVIDENCE.md SC#2"
    - "41-GREEN-TREE-EVIDENCE.md + 41-JA-GLYPH-BAR.md + 41-JA-GLYPHBAR-SIGNOFF.md -> 41-RELEASE-EVIDENCE.md SC#3"
    - "41-SC4-INVARIANTS.md -> 41-RELEASE-EVIDENCE.md SC#4"
    - "the fence probes -> 41-RELEASE-EVIDENCE.md SC#5 -> 41-HANDOFF.md 'Proof the fence held'"
  prohibitions:
    - statement: "MUST NOT take any irreversible release action — no `git tag`, no tag push, no `release.yml` trigger, no PyPI upload, no GitHub Release, no PR open or merge. This plan writes the checklist for those actions; it does not perform any of them."
      category: safety
      requirement_id: REL-05
    - statement: "MUST NOT flip REL-04's or REL-05's checkbox or Traceability row in `.planning/REQUIREMENTS.md` — both stay Pending until `/gsd-complete-milestone`. Watch for the known `phase.complete` hazard that auto-flips a deferred requirement against the CONTEXT decision: check the diff before committing and revert it if it fires."
      category: transparency
      requirement_id: REL-04
    - statement: "MUST NOT upgrade a gap, a skip, or a NOT MET verdict recorded in a sibling evidence file into a pass while rolling it up — the roll-up reads verdicts, it does not re-decide them."
      category: transparency
      requirement_id: REL-05
    - statement: "MUST NOT re-derive or re-type any measured value that already exists in a sibling evidence file — cite the file and quote it, so a transcription error cannot create a second, divergent number."
      category: transparency
      requirement_id: REL-05
---

<objective>
Close the phase: roll every success criterion up into `41-RELEASE-EVIDENCE.md` by citing the sibling
evidence files rather than re-deriving them, prove ROADMAP SC#5's fence held with two independent
observations of local and remote `v0.7.0` tag emptiness, and write the standalone `41-HANDOFF.md`
checklist recording exactly what `/gsd-complete-milestone` will execute.

Purpose: the prep/publish fence is absolute (Phase 33 and Phase 35 precedent). SC#5 requires proof of
an ABSENCE — that nothing irreversible happened — which no test can assert, so the proof is two
independent observations transcribed verbatim. And the publish half needs a checklist someone can
execute later without reconstructing this phase's reasoning.

Output: `41-RELEASE-EVIDENCE.md` (the SC#1-SC#5 roll-up, which is deliberately NOT named
`41-VERIFICATION.md` — that name is reserved by the verifier and would be clobbered) and
`41-HANDOFF.md`.

This plan takes NO irreversible action. It is the plan that writes down what the irreversible actions
will be, and then proves none of them happened.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<worktree_provisioning>
Worktree isolation is this project's STANDING execution mode (CLAUDE.md § "Worktree-isolated
execution"). Before ANY other command, run once from the worktree root:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do ln -sf "$(command -v $t)" ".venv/bin/$t"; done`

Then invoke EVERY command through `uv run`.

Before starting, confirm all wave-2 evidence files are present in this worktree:
`41-REL04-EVIDENCE.md`, `41-GREEN-TREE-EVIDENCE.md`, `41-SC4-INVARIANTS.md`, `41-JA-GLYPH-BAR.md`,
and `41-JA-GLYPHBAR-SIGNOFF.md`. If any is missing, the wave-2 merges have not landed here and the
roll-up would be built on absent evidence — stop and report it rather than writing around the gap.
</worktree_provisioning>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roll every success criterion up and take the first fence observation</name>
  <files>.planning/phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 41" — the five success criteria, to quote verbatim as each
      section's heading text.
    - `.planning/milestones/v0.6.5-phases/35-v0-6-5-release-prep/35-RELEASE-EVIDENCE.md` — the
      evidence-file shape: one `## SC#N: …` section per criterion, numbered `### Step N — …`
      subsections, and a closing `### SC#N verdict` paragraph. Note especially its § "SC#5: no
      irreversible action was taken", which is the section this task's fence observation mirrors.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md` — SC#1's
      evidence, produced by plan 41-01.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-GREEN-TREE-EVIDENCE.md` — SC#3's
      mechanical half, produced by plan 41-05, including its executed-versus-skipped statements.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPH-BAR.md` and
      `41-JA-GLYPHBAR-SIGNOFF.md` — SC#3's glyph-bar half and check 4's verdict, produced by plan
      41-04.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md` — SC#4's
      verdict table, produced by plan 41-06.
    - `CHANGELOG.md` § the `## [0.7.0]` entry and the tail link block, plus `pyproject.toml`,
      `README.md`, and `uv.lock` — SC#2's own subject matter, which this file measures directly
      because no sibling evidence file owns it.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` § "Claude's
      Discretion" last bullet — why this file is not named `41-VERIFICATION.md`.
  </read_first>
  <action>
Create `41-RELEASE-EVIDENCE.md`. Open it with a note stating explicitly that this file is
deliberately NOT named `41-VERIFICATION.md`, because that filename is reserved by the verifier and
would be overwritten, and that this follows the `35-RELEASE-EVIDENCE.md` precedent.

Then write one `## SC#N` section per ROADMAP success criterion, each opening with the criterion
quoted verbatim from ROADMAP.md, followed by:
- the evidence file(s) that discharge it, by path;
- the verdict READ from that file — quote the verdict sentence rather than restating it in your own
  words;
- a one-paragraph roll-up verdict for this file.

Per criterion:
- **SC#1** — cite `41-REL04-EVIDENCE.md`. Quote its SC#1 verdict and its statement that `release.yml`
  itself was not executed.
- **SC#2** — this file MEASURES this one directly, because no sibling owns it. Run and transcribe
  verbatim: `uv run python -c "import typsphinx; print(typsphinx.__version__)"`; the `[project]`
  version parsed from `pyproject.toml`; the README Status line; `git grep -n "0.7.0" -- uv.lock` for
  the typsphinx entry; the `## [0.7.0]` heading line with its line number; and the two tail
  link-block lines. State the verdict.
- **SC#3** — cite BOTH halves: `41-GREEN-TREE-EVIDENCE.md` for the full suite, lint/type trio,
  full-corpus gate, and both docs builds; and `41-JA-GLYPH-BAR.md` plus `41-JA-GLYPHBAR-SIGNOFF.md`
  for the four-check glyph bar. Quote each file's verdict. If the corpus gate SKIPPED, or if the
  glyph bar's check 4 came back NOT MET, carry that through as an open item — do not let a roll-up
  paragraph convert it into a pass.
- **SC#4** — cite `41-SC4-INVARIANTS.md` and reproduce its per-invariant verdict table by quotation.
  If any invariant is PARTIAL or NOT PROVEN, or if a single-hit handler was recorded NOT COVERED,
  carry it here as an open item with its handler name.
- **SC#5** — the fence. Run, and transcribe verbatim with a timestamp, BOTH of:
  `git tag -l v0.7.0` and `git ls-remote --tags origin v0.7.0`. Both must be EMPTY. Record this as
  "observation 1 of 2"; observation 2 is taken in Task 2, at a separate moment, and lives in
  `41-HANDOFF.md`. Also transcribe `git log --oneline -5` so the tree state at the moment of
  observation is on the record.

Close the file with a `## Phase verdict` table: one row per success criterion, its status
(PROVEN / PARTIAL / NOT PROVEN / OPEN), and the evidence path. Follow it with an explicit
`## Executed versus skipped` list naming every command this PHASE ran across all seven plans that did
NOT execute, with its reason — gathered by reading each sibling evidence file's own
executed-versus-skipped statement. A skip is not a pass, and the roll-up is where a skip would
otherwise disappear.

Commit with `docs(41-07): roll up the phase's success-criterion evidence (SC#1-SC#5)`.
  </action>
  <verify>
    <automated>uv run python -c "
import pathlib
d=pathlib.Path('.planning/phases/41-v0-7-0-release-automation-release-prep')
t=(d/'41-RELEASE-EVIDENCE.md').read_text(encoding='utf-8')
for needle in ['SC#1','SC#2','SC#3','SC#4','SC#5','41-REL04-EVIDENCE.md','41-GREEN-TREE-EVIDENCE.md','41-JA-GLYPH-BAR.md','41-JA-GLYPHBAR-SIGNOFF.md','41-SC4-INVARIANTS.md','Phase verdict','Executed versus skipped','observation 1']:
    assert needle in t, needle
assert '41-VERIFICATION.md' in t, 'the reserved-name note must be present'
print('roll-up complete')
" &amp;&amp; test -z "$(git tag -l v0.7.0)" &amp;&amp; test -z "$(git ls-remote --tags origin v0.7.0)"</automated>
  </verify>
  <acceptance_criteria>
    - `41-RELEASE-EVIDENCE.md` exists, opens with the reserved-name note explaining why it is not
      called `41-VERIFICATION.md`, and carries one `## SC#N` section per ROADMAP criterion with that
      criterion quoted verbatim.
    - SC#1, SC#3, and SC#4 each cite their sibling evidence file(s) by path and QUOTE the verdict
      from those files rather than restating it.
    - SC#2 carries its own directly-measured transcripts: `typsphinx.__version__`, the parsed
      `pyproject.toml` version, the README Status line, the `uv.lock` typsphinx entry, the
      `## [0.7.0]` heading with line number, and both tail link-block lines.
    - SC#5 carries observation 1: `git tag -l v0.7.0` and `git ls-remote --tags origin v0.7.0` both
      transcribed with EMPTY output and a timestamp, plus `git log --oneline -5`.
    - Any gap, skip, or NOT MET verdict recorded in a sibling file appears in the `## Phase verdict`
      table as PARTIAL / NOT PROVEN / OPEN — never as PROVEN.
    - The `## Executed versus skipped` list names every non-executed command across the phase with
      its reason.
    - `git diff --stat -- .planning/REQUIREMENTS.md` over this plan's commits is EMPTY.
  </acceptance_criteria>
  <done>Every success criterion has a section quoting its evidence file's own verdict, SC#2 is measured directly, the first fence observation is recorded, and no sibling gap was upgraded to a pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write the publish handoff checklist and take the second fence observation</name>
  <files>.planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md</files>
  <read_first>
    - `.planning/milestones/v0.6.5-phases/35-v0-6-5-release-prep/35-HANDOFF.md` — read it in full.
      This is the named precedent. Its structure: a `What this phase satisfied, and what it did not`
      opening restating the requirement text verbatim and mapping each ROADMAP success criterion to
      the plan or evidence file that satisfies it; a numbered `Checklist` where every item states an
      **Owner** (`/gsd-complete-milestone`, or **human** for console-only steps) and an explicit
      **Ordering** dependency on prior items; a `Not done in this phase, by design` bullet list
      restating every excluded destructive action; and a closing `Proof the fence held` section with
      verbatim tag-probe output taken as two independent observations at two separate moments.
    - `.planning/STATE.md` § "Shipped Milestone (v0.6.5 — archived)" — how the v0.6.5 publish half
      actually executed at close (PR merged, tag on the merge commit, release run, PyPI, GitHub
      Release, the translations-repo pin advance and its matching tag, and the RTD `stable`
      confirmation on both projects). This is the concrete sequence the checklist describes.
    - `.planning/REQUIREMENTS.md` § "Release and CI (REL)" and the Traceability table rows for REL-04
      and REL-05 — the checkbox and row that stay Pending until close.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` § "Out of scope",
      § D-14, and § "Folded Todos".
    - `.planning/todos/pending/` — list it, to name the exact files the close-side filing item refers
      to.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md` (from Task
      1) — the roll-up this handoff points back at.
  </read_first>
  <action>
Write `41-HANDOFF.md` following `35-HANDOFF.md`'s structure.

**Opening — what this phase satisfied, and what it did not.** Restate REL-04 and REL-05 verbatim from
`.planning/REQUIREMENTS.md`, then map each of ROADMAP's five success criteria to the plan and
evidence file that discharges it, citing `41-RELEASE-EVIDENCE.md`'s phase verdict table rather than
re-deriving statuses. State plainly that REL-05's publish half is NOT satisfied by this phase and is
what the checklist below covers.

**Checklist.** Numbered items, each with an explicit **Owner** and **Ordering** line:
1. Open the pull request and merge it to `main`. Owner: `/gsd-complete-milestone`. Ordering: first.
   Note the CI expectation and that `release.yml`'s `validate` job now also runs the CHANGELOG
   existence check plan 41-01 added — so a merge is the first moment that check exercises in anger.
2. Push the `v0.7.0` tag on the merge commit. Owner: `/gsd-complete-milestone`. Ordering: after 1.
   Note that this is the trigger for `release.yml`, and that the `validate` job will fail fast if
   `CHANGELOG.md` has no `## [0.7.0]` section — which it does, as SC#2 proves.
3. Let `release.yml` run to completion: `validate` -> `build` -> `publish-pypi` -> `create-release`.
   Owner: `/gsd-complete-milestone`, with a human approval step on the `pypi` environment. Ordering:
   after 2. Record the expectation that the GitHub Release body is now the curated `## [0.7.0]`
   CHANGELOG section plus the Installation block plus GitHub's own auto-generated portion — this is
   REL-04's first real-world exercise and the first release where the body is not a commit dump.
4. Advance the `typsphinx-doc-translations` submodule pin and push a matching `v0.7.0` tag there.
   Owner: `/gsd-complete-milestone` plus human. Ordering: after 1. This is the standing
   two-repository tagging cost adopted at v0.6.4 — `/ja/stable/` resolves against that repository's
   own tags.
5. Confirm Read the Docs `stable` is green at `v0.7.0` on BOTH projects (en and ja) and that both
   report 0.7.0 and serve their PDFs. Owner: human, via the RTD public API or real fetches. Ordering:
   after 3 and 4.
6. Flip REL-04's and REL-05's checkboxes and their Traceability rows in `.planning/REQUIREMENTS.md`
   from Pending to Complete. Owner: `/gsd-complete-milestone`. Ordering: after 3. State that Phase 41
   deliberately left both Pending, and carry the warning that `phase.complete` has been observed
   auto-flipping a deferred requirement's checkbox against a CONTEXT decision — check the diff before
   committing and revert if it fires.
7. File the two todos this phase's own code work resolved:
   `2026-07-29-release-notes-body-from-changelog-section.md` (REL-04, delivered by plan 41-01) and
   `2026-08-01-visit-desc-sig-name-docstring-unbalanced-asterisk-warning.md` (D-12, delivered by plan
   41-03), from `.planning/todos/pending/` to `.planning/todos/completed/`. Owner:
   `/gsd-complete-milestone`. Ordering: after 3. Note that Phase 41 filed the two ALREADY-fixed todos
   D-13 named, and left these two for the close side because REL-04's own close is close-side work.

**Not done in this phase, by design.** A bullet list restating every excluded action: no `git tag`,
no tag push, no `release.yml` trigger, no PyPI upload, no GitHub Release, no PR opened or merged, no
REQUIREMENTS.md checkbox or Traceability flip, no change under `docs/` (measured: zero lines changed
this milestone, and touching it drags in gettext-catalog follow-up in the translations repository),
no `typsphinx/` change other than D-12's docstring escape, and no revisiting of the version number.

**Deferred by decision, not oversight (D-14).** List the four todos deferred to v0.7.1+ by name —
the `sphinx-build -b linkcheck` CI job, the non-`str` docname `TypeError` in `TypstPDFBuilder.finish()`,
`derive_typst_lang()`'s duplicated warning block, and the typing modernization — with one line each
on why. State that the typing one is independently held back by `CLAUDE.md`'s standing instruction,
and that the linkcheck one was weighed and declined because `links.yml`'s repository-wide lychee
check already covers the single new link this release adds to `CHANGELOG.md`'s tail block.

**Proof the fence held.** Take observation 2 of 2 — a SECOND, independent run of `git tag -l v0.7.0`
and `git ls-remote --tags origin v0.7.0`, at a different moment from Task 1's observation 1.
Transcribe both outputs verbatim with a timestamp, and record the elapsed gap from observation 1.
Both must be EMPTY. Cite observation 1's location in `41-RELEASE-EVIDENCE.md` § SC#5. Two independent
observations at two separate moments is `35-HANDOFF.md`'s own convention and the reason it is two and
not one.

Before committing, run `git diff --stat -- .planning/REQUIREMENTS.md` and confirm it is empty. If
anything flipped a checkbox, revert it.

Commit with `docs(41-07): write the v0.7.0 publish handoff checklist (SC#5)`.
  </action>
  <verify>
    <automated>uv run python -c "
import pathlib
d=pathlib.Path('.planning/phases/41-v0-7-0-release-automation-release-prep')
t=(d/'41-HANDOFF.md').read_text(encoding='utf-8')
for needle in ['REL-04','REL-05','Owner','Ordering','Not done in this phase','Proof the fence held','typsphinx-doc-translations','observation 2','D-14','41-RELEASE-EVIDENCE.md']:
    assert needle in t, needle
import re
items=re.findall(r'^###\s*\d+\.', t, re.M)
assert len(items) &gt;= 7, len(items)
print('handoff complete:', len(items), 'checklist items')
" &amp;&amp; test -z "$(git tag -l v0.7.0)" &amp;&amp; test -z "$(git ls-remote --tags origin v0.7.0)" &amp;&amp; test -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `41-HANDOFF.md` exists and opens by restating REL-04 and REL-05 verbatim and mapping all five
      ROADMAP success criteria to the plan and evidence file that discharges each.
    - The checklist has at least 7 numbered items, and EVERY item states an Owner and an explicit
      Ordering dependency.
    - The checklist includes the standing second tag on `typsphinx-doc-translations` as its own item.
    - The checklist's REQUIREMENTS.md item carries the `phase.complete` auto-flip warning.
    - A `Not done in this phase, by design` section lists every excluded destructive action by name.
    - A D-14 section names all four deferred todos with a reason each.
    - `Proof the fence held` carries observation 2 with verbatim EMPTY output from both probes, a
      timestamp, the elapsed gap from observation 1, and a citation to observation 1's location.
    - `git tag -l v0.7.0` and `git ls-remote --tags origin v0.7.0` are both empty at the moment of
      the final commit.
    - `git diff --name-only -- .planning/REQUIREMENTS.md` is empty — no checkbox or Traceability row
      moved.
    - `git diff --stat -- typsphinx/ tests/ scripts/ .github/ CHANGELOG.md pyproject.toml uv.lock`
      over this plan's commits is EMPTY.
  </acceptance_criteria>
  <done>The handoff records every publish step with an owner and an ordering, names everything deliberately not done, and the fence is proven empty by a second independent observation.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Newly created symbols, files, and CI step names across ALL of Phase 41 (listed so the
plan-review-convergence source-grounding pass excludes them from drift verification):

**Files created**
- `scripts/extract_changelog_section.py`
- `tests/test_changelog_extraction.py`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPH-BAR.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPHBAR-SIGNOFF.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-GREEN-TREE-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md` (this plan)
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md` (this plan)
- `.planning/phases/41-v0-7-0-release-automation-release-prep/translations-repo/` (working clone,
  D-17 — NEVER committed, created and removed inside plan 41-04)

**Files relocated**
- `.planning/todos/completed/2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md`
- `.planning/todos/completed/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md`

**Functions / constants created**
- `scripts/extract_changelog_section.py`: `extract_section(changelog_text: str, version: str) -> str`,
  `REPO_ROOT`, `DEFAULT_CHANGELOG_PATH`, `_SECTION_HEADER_RE`; CLI positional `version`, optional
  `--changelog-path`

**Test functions created**
- `tests/test_changelog_extraction.py`: `test_extracts_real_version`,
  `test_section_terminates_at_next_version_heading`, `test_absent_version_fails`,
  `test_empty_section_fails`, `test_unreleased_headings_do_not_leak`, `test_changelog_path_override`,
  plus `_run_extractor` and `SCRIPT_PATH`

**CI step names created**
- `.github/workflows/release.yml`, `validate` job: `Verify CHANGELOG has a section for this version`

**Markdown anchors created**
- `CHANGELOG.md`: the `## [0.7.0]` heading and the `[0.7.0]:` link-reference line

**Explicitly NOT created by this plan**
- No change to any source, test, workflow, CHANGELOG, or REQUIREMENTS file. This plan writes exactly
  two markdown files.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sibling evidence files -> the phase verdict | Prior plans' self-reported verdicts become the phase's release-readiness claim |
| `origin` remote -> the fence probe | A network query becomes proof that nothing was published |
| this handoff -> `/gsd-complete-milestone` | A written checklist becomes the instruction set for irreversible actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-28 | Repudiation | A gap, skip, or NOT MET verdict in a sibling evidence file silently becoming PROVEN at roll-up | medium | mitigate | Each SC section QUOTES its sibling file's verdict rather than restating it, the `## Phase verdict` table carries PARTIAL / NOT PROVEN / OPEN statuses, and a phase-wide `## Executed versus skipped` list is assembled from each sibling's own statement. Reinforced by this plan's third `must_haves.prohibitions` entry. |
| T-41-29 | Tampering | A measured value diverging between an evidence file and the roll-up because it was retyped | medium | mitigate | The fourth `must_haves.prohibitions` entry forbids re-deriving any value a sibling file already holds; the roll-up cites and quotes. SC#2 is the one criterion measured directly here, precisely because no sibling owns it. |
| T-41-30 | Elevation of Privilege | An irreversible action having been taken somewhere in the phase without anyone noticing | medium | mitigate | SC#5's proof is an absence, so it is taken as TWO independent observations at two separate moments (`35-HANDOFF.md`'s own convention): observation 1 in `41-RELEASE-EVIDENCE.md` § SC#5, observation 2 in `41-HANDOFF.md` § "Proof the fence held", both probing local AND remote tags, both required EMPTY. Each of the seven plans additionally asserts tag emptiness in its own acceptance criteria. |
| T-41-31 | Tampering | `phase.complete` auto-flipping REL-04's or REL-05's checkbox against the CONTEXT decision | medium | mitigate | This is a KNOWN, previously-observed hazard, not a hypothetical. Both tasks assert `git diff --name-only -- .planning/REQUIREMENTS.md` is empty, the handoff's checklist item 6 carries the warning for the close side, and this plan's second `must_haves.prohibitions` entry names it with the instruction to check the diff and revert. |
| T-41-32 | Information Disclosure | Writing this evidence into the verifier-reserved filename and losing it | low | mitigate | The file is named `41-RELEASE-EVIDENCE.md`, following the `35-RELEASE-EVIDENCE.md` precedent, and opens with an explicit note recording why `41-VERIFICATION.md` was avoided — so a later editor does not "tidy" it into the reserved name. |
| T-41-SC | Tampering | npm/pip/cargo installs — supply chain | low | mitigate | This plan installs no new package. `41-RESEARCH.md` § "Package Legitimacy Audit" records the audit as not applicable because the phase adds none. Asserted by `git diff --stat -- pyproject.toml uv.lock` over this plan's commits being empty. |

**No `high`-severity threat was identified for this plan** (`security_asvs_level: 1`,
`security_block_on: high`). The register is complete, not empty: this plan writes two markdown files
and runs read-only git probes. It adds no authentication, session, access-control, cryptographic, or
external-input surface. Its risks are all integrity risks about a roll-up claiming more than its
sources support, and each is bound to a quotation, a doubled observation, or an asserted empty diff.
</threat_model>

<verification>
- `41-RELEASE-EVIDENCE.md` carries `## SC#1` through `## SC#5`, each quoting its criterion verbatim
  and its evidence file's own verdict, plus a `## Phase verdict` table and a phase-wide
  `## Executed versus skipped` list.
- `41-HANDOFF.md` carries at least 7 numbered checklist items, each with an Owner and an Ordering,
  including the translations-repository second tag and the REQUIREMENTS.md flip with its auto-flip
  warning.
- Both fence observations are recorded — observation 1 in `41-RELEASE-EVIDENCE.md` § SC#5,
  observation 2 in `41-HANDOFF.md` § "Proof the fence held" — each with `git tag -l v0.7.0` and
  `git ls-remote --tags origin v0.7.0` transcribed EMPTY, timestamped, and taken at separate moments.
- `git diff --name-only -- .planning/REQUIREMENTS.md` is empty.
- `git diff --stat -- typsphinx/ tests/ scripts/ .github/ CHANGELOG.md pyproject.toml uv.lock` over
  this plan's commits is empty.

**Honest-verifier note.** If a sibling evidence file records a gap, a skip, or a NOT MET verdict, the
roll-up records it as such and the phase closes with that criterion PARTIAL or OPEN. Reporting an
open criterion is correct; a clean phase verdict that its own evidence does not support is not.
</verification>

<success_criteria>
1. Every ROADMAP success criterion has a roll-up section quoting its criterion and its evidence
   file's own verdict, with SC#2 measured directly here.
2. No sibling gap, skip, or NOT MET verdict is upgraded to a pass, and a phase-wide
   executed-versus-skipped list exists.
3. SC#5's fence is proven by two independent observations of local AND remote tag emptiness at two
   separate moments.
4. `41-HANDOFF.md` records every publish step with an Owner and an Ordering, including the standing
   second tag on `typsphinx-doc-translations`.
5. `.planning/REQUIREMENTS.md` is untouched — REL-04 and REL-05 both stay Pending until
   `/gsd-complete-milestone`.
</success_criteria>

<output>
Create `.planning/phases/41-v0-7-0-release-automation-release-prep/41-07-SUMMARY.md` when done.
</output>