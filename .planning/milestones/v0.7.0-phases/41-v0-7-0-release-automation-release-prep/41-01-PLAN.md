---
phase: 41-v0-7-0-release-automation-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract_changelog_section.py
  - tests/test_changelog_extraction.py
  - .github/workflows/release.yml
  - .planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "Running the extractor for a real released version (0.6.5) exits 0 and prints that section's body to stdout, terminating at the next `^## [` heading line so no content from `## [0.6.4]` leaks in (edge: adjacency, REL-04)."
    - "Running the extractor for a version absent from CHANGELOG.md (9.9.9) exits non-zero, prints nothing to stdout, and names the requested version in its stderr message (edge: empty, REL-04)."
    - "A version heading whose section body is empty exits non-zero rather than emitting an empty release body (edge: empty, REL-04)."
    - "Extraction is positional — the first line matching the requested version's `^## [<version>]` heading, terminated by the next line matching `^## [` or EOF — never name-based, so the two identically-named `## [Unreleased]` headings in CHANGELOG.md cannot make the result for a numeric version order-dependent (edge: ordering, REL-04)."
    - "`.github/workflows/release.yml`'s `validate` job fails before `build`, `publish-pypi`, and `create-release` when the tag's version has no CHANGELOG section (D-09), proven by the job `needs:` graph recorded in the evidence file."
    - "`.github/workflows/release.yml`'s `create-release` job writes `release_notes.md` from the extractor's stdout, and the commit-dump generator is deleted rather than retained as a fallback branch (SC#1)."
  artifacts:
    - scripts/extract_changelog_section.py
    - tests/test_changelog_extraction.py
    - .planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md
  key_links:
    - "release.yml validate job -> scripts/extract_changelog_section.py (existence gate, D-09)"
    - "release.yml create-release job -> scripts/extract_changelog_section.py -> release_notes.md -> softprops/action-gh-release body_path"
    - "tests/test_changelog_extraction.py -> scripts/extract_changelog_section.py via subprocess.run (one implementation, exercised by both CI and pytest, D-06)"
  prohibitions:
    - statement: "MUST NOT take any irreversible release action while wiring release.yml — no `git tag`, no tag push, no `workflow_dispatch` trigger of the Release workflow, no PyPI upload, no GitHub Release, no PR open or merge."
      category: safety
      requirement_id: REL-04
    - statement: "MUST NOT flip REL-04's checkbox or its Traceability row in `.planning/REQUIREMENTS.md` — that is close-side work at `/gsd-complete-milestone`."
      category: transparency
      requirement_id: REL-04
    - statement: "MUST NOT keep a second, independently-implemented copy of the extraction logic inline in the workflow shell — one script, invoked by both jobs (D-06)."
      category: transparency
      requirement_id: REL-04
---

<objective>
Deliver REL-04 in full: a committed, pytest-covered `## [X.Y.Z]` CHANGELOG-section extractor (D-06),
wired into `.github/workflows/release.yml` in both the `validate` job (the fail-loud existence check,
D-09) and the `create-release` job (the release-notes body, replacing the commit dump), plus the SC#1
hand-run transcript (D-07) and the `generate_release_notes` / Installation-block retention (D-08).

Purpose: today `release.yml` never opens `CHANGELOG.md` at all. The GitHub Release body is a raw
commit dump — for v0.6.4 that was 296 dump lines inside a 308-line body — and a missing or malformed
CHANGELOG section is discovered only at tag time, after PyPI has already been published to. This plan
creates the link and moves the check ahead of the publish.

Output: `scripts/extract_changelog_section.py`, `tests/test_changelog_extraction.py`, two edits to
`.github/workflows/release.yml`, and `41-REL04-EVIDENCE.md`.

This plan takes NO irreversible action. It edits the workflow file; it never runs the workflow.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<worktree_provisioning>
Worktree isolation is this project's STANDING execution mode (CLAUDE.md § "Worktree-isolated
execution"). Before ANY other command in this plan, run, once, from the worktree root:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
   (STATE.md § Operator Next Steps: the `ruff` shim ALONE is not sufficient — the `uv` shim is also
   required, resolved via `command -v` before any `.venv/bin` entry is on `PATH`. Without both, 45
   integration tests fail with exit 127 and every gate reads falsely RED.)

Then invoke EVERY command in this plan through `uv run`. A bare `pytest` / `black` / `ruff` tests the
MAIN checkout's package and produces false results.
</worktree_provisioning>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-PATTERNS.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record the REL-04 extraction contract RED in a new pytest module</name>
  <files>tests/test_changelog_extraction.py</files>
  <read_first>
    - `tests/test_admonition_greyscale_pipeline.py` — the exact subprocess-invocation shape to copy
      (`SCRIPT_PATH = Path(__file__).resolve().parents[1] / "scripts" / "<name>.py"`, then
      `subprocess.run([sys.executable, str(SCRIPT_PATH), ...], capture_output=True, text=True)` with a
      failure message that interpolates both `result.stdout` and `result.stderr`). This is the D-06
      contract: the test exercises the SAME script CI runs, never an imported function.
    - `tests/test_readme_version_sync.py` — house style for module docstring, `REPO_ROOT`, named
      module-level `re.compile(...)` constants with a comment stating what they do NOT match, and
      assertion messages that name the fix.
    - `tests/test_preview_version_sync.py` — the second instance of that same regex-constant
      convention.
    - `CHANGELOG.md` — read lines 1-35 (the top `## [Unreleased]` at line 8, the `## [0.6.5]` entry
      at line 10 with its `### Fixed` / `### Verified` subsections, and `## [0.6.4]` at line 33) and
      the last 25 lines (the tail link-reference block). These are the real fixtures the tests assert
      against.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-PATTERNS.md` §
      "`tests/test_changelog_extraction.py`".
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` § "Pitfall 1".
  </read_first>
  <behavior>
    Six test functions, all invoking the not-yet-existing script through `subprocess.run`:

    - `test_extracts_real_version` — argv `["0.6.5"]`. Expect returncode 0; `result.stdout` non-empty;
      stdout contains the substrings `### Fixed`, `### Verified`, and `MATH-01`.
    - `test_section_terminates_at_next_version_heading` — argv `["0.6.5"]`. Expect stdout does NOT
      contain the string `0.6.4` and does NOT contain `Read the Docs` (a phrase unique to the
      `## [0.6.4]` entry, re-confirm it is present there before relying on it). This is the adjacency
      edge: the section must stop at the next `^## [` line.
    - `test_absent_version_fails` — argv `["9.9.9"]`. Expect returncode != 0; `result.stdout.strip()`
      empty; `"9.9.9"` present in `result.stderr`. The stderr-content assertion is what makes this
      test NON-VACUOUS: with the script absent, Python's own "can't open file" stderr does not
      contain the requested version, so this goes RED for the right reason rather than passing on the
      missing file.
    - `test_empty_section_fails` — writes a synthetic CHANGELOG under `tmp_path` holding
      `## [1.0.0]` immediately followed by `## [0.9.0]` with only blank lines between them, invokes
      with `--changelog-path` pointing at it and version `1.0.0`. Expect returncode != 0 and
      `1.0.0` named in stderr.
    - `test_unreleased_headings_do_not_leak` — writes a synthetic CHANGELOG under `tmp_path` that
      reproduces the real file's structure: a top `## [Unreleased]`, then `## [2.0.0]` with a body,
      then `## [1.0.0]`, then far below a SECOND `## [Unreleased]` carrying "Planned for Future
      Releases" bullets. Invoke for `2.0.0`. Expect returncode 0 and stdout containing neither
      `Planned for Future` nor any `1.0.0` body content. This is the ordering edge — it pins that
      the algorithm is positional, not name-based.
    - `test_changelog_path_override` — asserts `--changelog-path` is honoured (a synthetic file
      under `tmp_path` produces its own content, not the repo-root file's).

    No `pytest.mark.skipif` anywhere — the script is stdlib-only, so there is no optional dependency
    to skip on, and a skip here would let the module pass by never running
    (`test_admonition_greyscale_pipeline.py`'s own recorded reasoning).
  </behavior>
  <action>
Create `tests/test_changelog_extraction.py` implementing exactly the six behaviors above. Use
`SCRIPT_PATH = Path(__file__).resolve().parents[1] / "scripts" / "extract_changelog_section.py"` and a
module-private helper `_run_extractor(*args)` returning the `subprocess.CompletedProcess`, built on
`subprocess.run([sys.executable, str(SCRIPT_PATH), *args], capture_output=True, text=True)`. Write a
module docstring naming D-06 (one script, exercised by both CI and pytest) and D-10 (both directions
covered) and stating that the two `## [Unreleased]` headings in the real `CHANGELOG.md` are the quirk
these tests pin against. Follow `tests/test_readme_version_sync.py`'s assertion-message style: state
what was found, what was expected, and what to do about it.

Then RECORD THE RED. Run `uv run pytest tests/test_changelog_extraction.py -v` and capture the full
output. Confirm in that output that `test_absent_version_fails` and `test_empty_section_fails` are
among the FAILING tests — if either passes at this point the assertion is vacuous (a missing script
also exits non-zero), and the test must be strengthened until it fails for the behavioral reason
before you proceed. Save the captured RED output to the scratch location you will transcribe from in
Task 3; do not create the evidence file yet.

Commit with `test(41-01): add REL-04 changelog-extraction contract (RED)`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_changelog_extraction.py -v 2>&amp;1 | tee /tmp/p41-01-red.txt; grep -q "test_absent_version_fails" /tmp/p41-01-red.txt &amp;&amp; grep -qE "FAILED tests/test_changelog_extraction.py::test_absent_version_fails" /tmp/p41-01-red.txt</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_changelog_extraction.py` exists and contains `def test_extracts_real_version(`,
      `def test_section_terminates_at_next_version_heading(`, `def test_absent_version_fails(`,
      `def test_empty_section_fails(`, `def test_unreleased_headings_do_not_leak(`, and
      `def test_changelog_path_override(`.
    - `tests/test_changelog_extraction.py` contains `subprocess.run(` and `sys.executable` and does
      NOT contain `import extract_changelog_section` (the D-06 contract is subprocess, not import).
    - `tests/test_changelog_extraction.py` contains no `skipif`.
    - The captured RED output shows `FAILED` for `test_absent_version_fails` — i.e. its stderr
      assertion is behavioral, not satisfied by the script merely being absent.
    - `uv run black --check tests/test_changelog_extraction.py` and
      `uv run ruff check tests/test_changelog_extraction.py` both exit 0.
  </acceptance_criteria>
  <done>Six-case contract module exists, is lint-clean, and its RED is captured with the failure-direction cases failing behaviorally rather than vacuously.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the extraction script and turn the module GREEN</name>
  <files>scripts/extract_changelog_section.py</files>
  <read_first>
    - `scripts/render_admonition_greyscale.py` — the only other file under `scripts/` and the exact
      shape to copy: a long motivated module docstring naming the requirement it satisfies and the
      non-obvious pitfalls; `from __future__ import annotations` first; module-level named constants
      with a comment pointing back at the docstring's rationale; a top-level function taking explicit
      `Path`/`str` args that raises a descriptive `RuntimeError` on its one identified failure mode;
      and an `if __name__ == "__main__":` guard that does `import argparse` inside the guard, builds
      the parser, and calls the module's own testable function.
    - `tests/test_changelog_extraction.py` (just written) — the contract this must satisfy.
    - `tests/test_preview_version_sync.py` lines 38-43 — the named-regex-constant convention.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-PATTERNS.md` §
      "`scripts/extract_changelog_section.py`".
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` § "Pattern 2",
      § "Pitfall 1", § "Pitfall 2".
    - `CHANGELOG.md` — the real structure being parsed.
  </read_first>
  <action>
Create `scripts/extract_changelog_section.py`, stdlib-only (`from __future__ import annotations`,
`re`, `sys`, `pathlib.Path`; `argparse` imported inside the `__main__` guard). No third-party import
and no new dependency anywhere — the milestone invariant is zero new runtime dependencies, and
`41-RESEARCH.md` records that a markdown-AST library would violate it for a problem two existing
raw-regex precedents already solve.

Required surface:

- Module-level constant `REPO_ROOT = Path(__file__).resolve().parents[1]` and
  `DEFAULT_CHANGELOG_PATH = REPO_ROOT / "CHANGELOG.md"`.
- Module-level named constant `_SECTION_HEADER_RE = re.compile(r"^## \[(?P<version>[^\]]+)\]")`
  with a comment stating it matches EVERY `## [...]` heading including both `## [Unreleased]`
  headings, and that this is deliberate — the algorithm is positional, never name-based.
- `def extract_section(changelog_text: str, version: str) -> str:` — scan lines top-down for the
  FIRST line whose `_SECTION_HEADER_RE` match has `version` equal to the requested string; take every
  subsequent line up to (exclusive) the next line matching `_SECTION_HEADER_RE`, or EOF; strip
  leading/trailing blank lines from the result. Raise `RuntimeError` with a message naming the
  requested version and the CHANGELOG path when (a) no heading matches, or (b) the body is empty
  after stripping. Never return `""`.
- `if __name__ == "__main__":` guard with `argparse`: positional `version` (type `str`), optional
  `--changelog-path` (type `Path`, default `DEFAULT_CHANGELOG_PATH`). Read the file with
  `encoding="utf-8"`, call `extract_section`, and on success `print()` the body to stdout. On
  `RuntimeError`, print the exception message to `sys.stderr` and `sys.exit(1)` — an explicit
  one-line stderr message plus exit 1, NOT an uncaught traceback, because two CI shell steps consume
  this script and a clean annotation is what a GitHub Actions log needs.

Module docstring must name D-06 (committed + pytest-covered so a break surfaces on every CI run, not
only at tag time), D-09 (the same script runs in the `validate` job before any publish), D-10 (both
directions are tested), and the load-bearing gotcha readers must not "fix": the file carries TWO
headings named `Unreleased` — one at the top, one deep in the tail as a "Planned for Future Releases"
scratch area — and any attempt to special-case that name would mis-terminate a real version's
section. The positional rule is the correctness argument.

Security note to carry in the docstring (ASVS V5, T-41-01): the `version` argument is used ONLY for a
string equality comparison against text parsed out of the CHANGELOG; it is never interpolated into a
shell command, never used to build a filesystem path, and `--changelog-path` is the only path input.

Do not add a `mypy` step for this file — `tox.ini`'s `[testenv:type]` runs `mypy typsphinx/` and
structurally cannot reach `scripts/`. Write clean type hints anyway, matching the existing script's
style. `black --check .` and `ruff check .` DO run repo-wide and DO cover `scripts/` — treat any
failure there as a real blocker.

Then run `uv run pytest tests/test_changelog_extraction.py -v` and confirm all six pass. Commit with
`feat(41-01): extract the release-notes body from CHANGELOG.md`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_changelog_extraction.py -v &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/extract_changelog_section.py` exists and contains `def extract_section(`,
      `_SECTION_HEADER_RE`, `DEFAULT_CHANGELOG_PATH`, `--changelog-path`, and `sys.exit(1)`.
    - `scripts/extract_changelog_section.py` imports no third-party module: `uv run python -c "import ast,sys; m=ast.parse(open('scripts/extract_changelog_section.py').read()); mods=[n.module or '' for n in ast.walk(m) if isinstance(n,ast.ImportFrom)]+[a.name for n in ast.walk(m) if isinstance(n,ast.Import) for a in n.names]; assert all(x.split('.')[0] in {'__future__','re','sys','argparse','pathlib','typing'} for x in mods), mods"` exits 0.
    - `uv run pytest tests/test_changelog_extraction.py -v` reports 6 passed, 0 failed, 0 skipped.
    - `uv run python scripts/extract_changelog_section.py 0.6.5` exits 0 and prints a body containing
      `### Verified`.
    - `uv run python scripts/extract_changelog_section.py 9.9.9` exits non-zero, prints nothing on
      stdout, and prints a stderr line containing `9.9.9`.
    - `git diff --stat -- pyproject.toml uv.lock` over this plan's commits is empty (zero new
      dependencies).
    - `uv run black --check .` and `uv run ruff check .` both exit 0.
  </acceptance_criteria>
  <done>The extractor exists, is stdlib-only, is lint-clean repo-wide, and all six contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire release.yml (both jobs) and transcribe the SC#1 evidence</name>
  <files>.github/workflows/release.yml, .planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md</files>
  <read_first>
    - `.github/workflows/release.yml` — read the WHOLE file. The anchors you need: the `validate`
      job's `Extract version from tag` step (which sets `steps.version.outputs.version` and
      `steps.version.outputs.tag`) and the `Verify version matches pyproject.toml` step immediately
      after it around lines 50-59; the `create-release` job's `Generate release notes` step around
      lines 152-174; the `Create GitHub Release` step around lines 176-187; and the `needs:` chain
      `validate` -> `build` -> `publish-pypi` -> `create-release`.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-PATTERNS.md` § the two
      `release.yml` sections — the existing step's shape is the template: a `name:`, a `run: |` shell
      block, an `::error::`-prefixed annotation, and `exit 1`.
    - `.planning/milestones/v0.6.5-phases/35-v0-6-5-release-prep/35-RELEASE-EVIDENCE.md` — the
      evidence-file shape: one `##` section per criterion, numbered `### Step N — …` subsections
      holding VERBATIM command-plus-output blocks, and a closing `### verdict` paragraph.
    - `.planning/todos/pending/2026-07-29-release-notes-body-from-changelog-section.md` — the
      recorded problem statement and named failure mode this closes.
  </read_first>
  <action>
Make two edits to `.github/workflows/release.yml`.

**Edit A — the `validate` job (D-09).** Insert a new step named
`Verify CHANGELOG has a section for this version` immediately AFTER the existing
`Verify version matches pyproject.toml` step and BEFORE the `Run tests` step. Its shell block calls
`uv run python scripts/extract_changelog_section.py "${{ steps.version.outputs.version }}" >/dev/null`
and, on non-zero, echoes a `::error::` annotation naming the version and pointing at `CHANGELOG.md`,
then `exit 1`. Match the neighbouring step's convention: explicit `::error::` annotation before
`exit 1` so GitHub Actions renders an inline annotation. This position is the whole point of D-09 —
the `validate` job gates `build`, which gates `publish-pypi`, which gates `create-release`, so a
missing section now fails BEFORE the PyPI upload instead of after it.

**Edit B — the `create-release` job.** In the `Generate release notes` step, DELETE the entire
previous-tag lookup and commit-dump generator: the `PREV_TAG=$(git describe ...)` assignment, the
`if [ -n "$PREV_TAG" ]` / `else` / `fi` branch, the `## Changes since $PREV_TAG` and
`## Initial Release` heading writes, and the dump line itself. Replace all of it with a single
invocation writing to the same `release_notes.md` path the step already uses:
`uv run python scripts/extract_changelog_section.py "${TAG#v}" > release_notes.md`. Per SC#1 the
generator is REMOVED, not retained behind a conditional or an `||` fallback — there must be no
surviving code path that can still produce a commit dump.

Keep, byte-unchanged, the `Installation` block that follows (the four `echo` lines appending the
fenced `pip install typsphinx==${TAG#v}` snippet) — D-08. Make NO change at all to the
`Create GitHub Release` step: `body_path: release_notes.md` and `generate_release_notes: true` stay
exactly as they are, which per D-08 means GitHub's own 5-line auto-generated portion is APPENDED to
the curated body rather than replacing it.

<!-- planner-discipline-allow: git log -->
<!-- planner-discipline-allow: --pretty=format -->

Then create
`.planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md` with three
sections, every command and its output transcribed VERBATIM as run (never paraphrased, never
retyped from memory):

1. `## SC#1 — the extraction executed against the real file for a real version (D-07)` — the
   `uv run python scripts/extract_changelog_section.py 0.6.5` invocation with its full stdout, and
   the `9.9.9` invocation with its exit status and stderr. Record the exit codes explicitly.
2. `## SC#1 — the commit dump is removed, not fenced off` — the `git diff` of the
   `create-release` job's `Generate release notes` step over this plan's commits, plus the output of
   a repo-wide search over `.github/workflows/release.yml` showing zero surviving occurrences of the
   dump generator. State plainly that no fallback branch remains.
3. `## D-09 — the check runs before the publish` — the job `needs:` graph read straight out of
   `release.yml` (`validate` has no `needs:`; `build` needs `validate`; `publish-pypi` needs `build`;
   `create-release` needs `[build, publish-pypi]`), transcribed with line numbers, and one paragraph
   stating why that ordering is the mitigation for "published to PyPI but no GitHub Release".

Record honestly that the workflow itself was NOT executed — it cannot run outside a real tag push,
and triggering it is forbidden by this phase's fence. The job-graph reading is the proof, and saying
so is required: a skip is not a pass.

Commit with `ci(41-01): source the release body from the CHANGELOG section (REL-04)`.
  </action>
  <verify>
    <automated>uv run python -c "import re,sys; y=open('.github/workflows/release.yml').read(); assert 'scripts/extract_changelog_section.py' in y; assert y.count('scripts/extract_changelog_section.py')==2, y.count('scripts/extract_changelog_section.py'); assert 'Verify CHANGELOG has a section for this version' in y; assert 'body_path: release_notes.md' in y; assert 'generate_release_notes: true' in y; assert 'pip install typsphinx==' in y; import subprocess; print('ok')" &amp;&amp; test -s .planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release.yml` contains the step name
      `Verify CHANGELOG has a section for this version`, and that step appears at a line number
      GREATER than the `Verify version matches pyproject.toml` step and LESS than the `Run tests`
      step (assert the three line numbers in that order).
    - `.github/workflows/release.yml` contains exactly two invocations of
      `scripts/extract_changelog_section.py` — one in `validate`, one in `create-release`.
    - The `create-release` job's `Generate release notes` step no longer contains a previous-tag
      lookup or a commit-dump generator: `sed -n '/Generate release notes/,/Create GitHub Release/p' .github/workflows/release.yml | grep -vc 'extract_changelog_section'` shows no line invoking the git history log formatter, and `grep -c 'PREV_TAG' .github/workflows/release.yml` returns 0.
    - `.github/workflows/release.yml` still contains `body_path: release_notes.md`,
      `generate_release_notes: true`, and `pip install typsphinx==${TAG#v}` (D-08 retention).
    - `uv run python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/release.yml')); n=d['jobs']; assert 'needs' not in n['validate']; assert n['build']['needs']=='validate'; assert n['publish-pypi']['needs']=='build'; assert set(n['create-release']['needs'])=={'build','publish-pypi'}; print('job graph ok')"` exits 0 (if `yaml` is unavailable in the dev extra, assert the same four facts by reading the file and record which method was used).
    - `41-REL04-EVIDENCE.md` exists, is non-empty, and contains the headings `## SC#1`, `## D-09`, the
      literal exit codes of both hand-runs, and an explicit sentence recording that `release.yml`
      itself was not executed.
    - `git tag -l v0.7.0` returns empty and `git ls-remote --tags origin v0.7.0` returns empty after
      this plan's commits.
  </acceptance_criteria>
  <done>Both jobs call the one committed extractor, the commit dump has no surviving code path, the D-08 retentions are intact, and the SC#1 hand-run plus the job-graph proof are transcribed verbatim.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Newly created symbols, files, and CI step names across ALL of Phase 41 (listed here so the
plan-review-convergence source-grounding pass excludes them from drift verification):

**Files created**
- `scripts/extract_changelog_section.py`
- `tests/test_changelog_extraction.py`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPH-BAR.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPHBAR-SIGNOFF.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-GREEN-TREE-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/translations-repo/` (working clone,
  D-17 — NEVER committed, removed at the end of plan 41-04)

**Functions / constants created**
- `scripts/extract_changelog_section.py`: `extract_section(changelog_text: str, version: str) -> str`,
  `REPO_ROOT`, `DEFAULT_CHANGELOG_PATH`, `_SECTION_HEADER_RE`
- CLI surface: positional `version`, optional `--changelog-path`

**Test functions created**
- `tests/test_changelog_extraction.py`: `test_extracts_real_version`,
  `test_section_terminates_at_next_version_heading`, `test_absent_version_fails`,
  `test_empty_section_fails`, `test_unreleased_headings_do_not_leak`, `test_changelog_path_override`,
  plus module-private `_run_extractor` and constant `SCRIPT_PATH`

**CI step names created**
- `.github/workflows/release.yml`, `validate` job: `Verify CHANGELOG has a section for this version`
  (the existing `Generate release notes` step in `create-release` is re-bodied, not renamed)

**Markdown anchors created**
- `CHANGELOG.md`: the `## [0.7.0]` heading and the `[0.7.0]:` link-reference line
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git tag -> `release.yml` shell | The tag name (and the `workflow_dispatch` input) crosses into shell variables `TAG`/`VERSION` already, via pre-existing unmodified logic |
| `release.yml` shell -> `scripts/extract_changelog_section.py` argv | The version string crosses into a new Python process as a single argv element |
| `CHANGELOG.md` file content -> GitHub Release body | Repo file content becomes published prose |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | `scripts/extract_changelog_section.py` `version` CLI argument (ASVS V5, the phase's only input-validation surface) | low | mitigate | The version is used ONLY for a string-equality comparison against text parsed out of `CHANGELOG.md`; it is never interpolated into a shell command, never `eval`'d, and never used to build a filesystem path. `--changelog-path` is the only path input and defaults to `REPO_ROOT / "CHANGELOG.md"`. `release.yml` passes it as a single quoted argv element, matching the existing `Verify version matches pyproject.toml` step's convention. |
| T-41-02 | Tampering | The published GitHub Release body sourced from `CHANGELOG.md` | low | accept | `CHANGELOG.md` is a repo file editable only by maintainers through a PR; no external input reaches the extractor. The residual risk (a maintainer commits a malformed section) is handled by the D-09/D-10 fail-loud path rather than by content sanitisation. |
| T-41-03 | Elevation of Privilege | Publishing to PyPI before the release body is known good | medium | mitigate | D-09 moves the existence check into the `validate` job. The job graph `validate` -> `build` -> `publish-pypi` -> `create-release` is asserted mechanically in this plan's acceptance criteria, so the check provably runs before the upload. |
| T-41-04 | Repudiation | Claiming SC#1 satisfied without a real run | low | mitigate | D-07's hand-run is transcribed verbatim with exit codes into `41-REL04-EVIDENCE.md`, and the evidence file states explicitly that `release.yml` itself was not executed. |
| T-41-SC | Tampering | npm/pip/cargo installs — supply chain | low | mitigate | This plan installs nothing. `41-RESEARCH.md` § "Package Legitimacy Audit" records the audit as not applicable because the phase adds no package; the extractor is stdlib-only. Asserted mechanically: `git diff --stat -- pyproject.toml uv.lock` over this plan's commits is empty. |

**No `high`-severity threat was identified for this plan** (`security_asvs_level: 1`,
`security_block_on: high`). The register is complete, not empty: the four in-scope threats are all
`low`/`medium` because the phase adds no authentication, session, access-control, or cryptographic
surface, and its single input-validation surface (T-41-01) is a string compared against a repo file.
</threat_model>

<verification>
- `uv run pytest tests/test_changelog_extraction.py -v` — 6 passed, 0 skipped.
- `uv run pytest tests/ -m "not slow"` — no regression introduced by the new module.
- `uv run black --check .` and `uv run ruff check .` — both exit 0 (they cover `scripts/`).
- `uv run mypy typsphinx/` — unchanged and green (it does not reach `scripts/`; do not widen it).
- `git diff --stat -- pyproject.toml uv.lock` over this plan's commits — empty.
- `git tag -l v0.7.0` and `git ls-remote --tags origin v0.7.0` — both empty.
</verification>

<success_criteria>
1. One committed extraction script, invoked by both `release.yml` jobs and by pytest — no second
   implementation anywhere (D-06).
2. The existence check runs in `validate`, provably before `build`/`publish-pypi`/`create-release`
   (D-09), asserted from the parsed job graph.
3. The commit-dump generator has no surviving code path (SC#1), and `generate_release_notes: true`
   plus the Installation block are retained byte-unchanged (D-08).
4. Both D-10 directions are pinned by tests that fail for behavioral reasons, plus the three REL-04
   edge cases (adjacency / empty / ordering).
5. `41-REL04-EVIDENCE.md` carries the verbatim hand-run (D-07) and states what was not executed.
</success_criteria>

<output>
Create `.planning/phases/41-v0-7-0-release-automation-release-prep/41-01-SUMMARY.md` when done.
</output>
