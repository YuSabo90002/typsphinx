---
phase: 72-tox-e-linkcheck-and-root-toctree-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md
  - docs/source/index.rst
  - .planning/todos/pending/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md
  - .planning/todos/completed/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md
autonomous: true
requirements: [DOC-18]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'PHASE_BASE_SHA is this worktree HEAD recorded before any commit, and outside .planning/ its tree equals the milestone base 098a8ff64cf008822eef9dc69f75102ded3f7bc1 (constraint 6), so every base measurement is pre-edit.'
    - 'Base builds first (ROADMAP binding ordering): the clean HTML and -b typst builds of PHASE_BASE_SHA are taken in Tasks 1-2, before the DOC-18 edit of Task 3 is committed and before any DOC-24 edit exists.'
    - 'Constraint 7 positive control: a clean HTML build of PHASE_BASE_SHA run with LANG=C LC_ALL=C and no -q finds at least one `document is referenced in multiple toctrees` line on stdout; the count, every matching line and the English `build succeeded` line with its N are transcribed. A zero count halts the plan.'
    - 'The base Typst build records the root index.typ guard line of each of the five pages, the master wrapper include-edges state line and its SHA-256, and that none of the five root edge keys is in that state.'
    - 'The base sidebar-scoped link counts (the html.parser from 72-RESEARCH.md) and the phase-head remote reads (required status checks, main CI baseline run, branch census) are recorded for later plans.'
    - 'DOC-18 / D-08: one commit changes exactly docs/source/index.rst (the five duplicate entries removed) and renames the 2026-08-16 todo from todos/pending to todos/completed with identical content; nothing else is in that commit.'
    - 'Edge (DOC-18 boundary): git diff --numstat PHASE_BASE_SHA..HEAD for docs/source/index.rst is exactly 0 added and 5 removed, and the removed lines are exactly the five entries; the post-edit build count is 0 where the base count was positive.'
    - 'Edge (DOC-18 adjacency): the Getting Started, API Reference and Development toctrees, every :maxdepth: and :caption:, the blank lines separating the toctrees, docs/source/user_guide/index.rst, docs/source/examples/index.rst and docs/source/conf.py are unchanged.'
    - 'Edge (DOC-18 empty): after the edit the User Guide toctree holds exactly one entry, user_guide/index, and the Examples toctree exactly one, examples/index; neither toctree is left empty.'
    - 'D-09 is honoured at plan time: the phase is planned with --skip-ui and no UI hint line is added to ROADMAP.md.'
  artifacts:
    - path: .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md
      provides: 'PHASE_BASE_SHA, base HTML positive control and warning N, base Typst guard and edge-state record, base sidebar counts, required checks at head, main baseline CI run, head branch census, DOC-18 edit commit'
      contains: 'PHASE_BASE_SHA = '
    - path: docs/source/index.rst
      provides: 'root toctrees listing only user_guide/index and examples/index'
    - path: .planning/todos/completed/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md
      provides: 'the folded DOC-18 todo, moved per D-08'
  key_links:
    - from: 'docs/source/index.rst User Guide and Examples toctrees'
      to: 'docs/source/user_guide/index.rst:6-12 and docs/source/examples/index.rst:6-10'
      via: 'each section index becomes the only toctree listing its children'
      pattern: '^   user_guide/index$'
    - from: '72-BASE-EVIDENCE.md'
      to: '72-04 and 72-05'
      via: 'PHASE_BASE_SHA, BASE_MULTI_TOCTREE_COUNT, BASE_WARNING_COUNT and REQUIRED_CONTEXTS_HEAD are read back with sed'
      pattern: 'BASE_WARNING_COUNT = '
  prohibitions:
    - statement: 'MUST NOT substitute theme or sidebar configuration, a :maxdepth: change, suppress_warnings or a -q-quieted build for the toctree fix or its proof, and MUST NOT act on the examples parent divergence beyond re-measuring it (REQUIREMENTS Out of Scope).'
      requirement_id: DOC-18
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Take the phase's pre-edit baseline, then make the DOC-18 edit.

- Record `PHASE_BASE_SHA` and prove its product tree is the milestone base.
- Build `PHASE_BASE_SHA` clean, HTML and `-b typst`, under the C locale, as ROADMAP constraint 7's
  positive control ("base builds first").
- Read the phase-head remote facts SC#5 compares against at the close.
- Remove the five duplicate root toctree entries and move the folded todo in the same commit (D-08).

D-09 is honoured at plan time: this phase was planned with `--skip-ui` and ROADMAP.md gains no UI
hint line. The owner's discussion decisions D-01..D-07 are carried by 72-02, 72-03 and 72-05.

Purpose: SC#3 and SC#4 compare the tip against this base. The DOC-18 edit runs alongside QUA-13
(72-02), which the ROADMAP permits; it lands at the wave-1 merge, after this plan's own base builds.
The binding base-versus-tip comparison is taken later, in 72-04, from a single environment.

Output: `72-BASE-EVIDENCE.md`, the `index.rst` edit and the todo move.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run` (`CLAUDE.md` § "Worktree-isolated execution"). The `docs` extra
is needed because this plan runs `sphinx-build` with furo.

**Build shape.** Every build in this plan runs from this worktree's root, through this worktree's
venv, against an absolute source directory inside a `git archive` snapshot, into an absolute output
directory under your scratch directory. Never build into `docs/_build/` and never copy `docs/source/`
alone: `conf.py:22` locates `pyproject.toml` three directories above itself, so the snapshot must be
a full tree (72-RESEARCH.md Pitfall 1).

**Locale.** The host runs `LANG=ja_JP.UTF-8`. Prefix every `sphinx-build` and every grep of its
output with `LANG=C LC_ALL=C`. The English `build succeeded` line must be found in every log; if it
is missing, the output is localised and every count from it is void.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put any explanation on
the next line. The verify fails on any key line containing an opening parenthesis. Executors in
Phases 69 and 71 appended explanations to values although told not to; do not.

**Scratch.** Parallel executors share the session scratchpad. Put every log and helper under your
own `mktemp -d` directory, recorded as `SCRATCH_72_01`, and prefix helper files `p7201_`.

**Commits.** Plain `git commit`. Never a GSD commit helper. Never write `72-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: document is referenced in multiple toctrees -->
<!-- planner-discipline-allow: \ -->
The Sphinx message is named in actions because the executor must grep for it, and the gates count
it: positive on the base, zero on the edited tree.

## Planning-time measurements (2026-09-13, HEAD `704beaba`; context only, re-measure, never trust)

| Fact | Value at planning |
|------|-------------------|
| Merge-base of the branch with `main` | `098a8ff64cf008822eef9dc69f75102ded3f7bc1`, also origin `main` |
| Clean base HTML build, C locale | 5 `multiple toctrees` lines, all on stdout; `build succeeded, 3 warnings.` on stdout |
| Root `index.typ` | 12 `include(` lines; the five guarded by `index#0>` plus one of the five pages never fire |
| Master wrapper | `typsphinx.typ` (`conf.py:72-74`), one `#state("typsphinx:include-edges", ()).update((…))` line |
| Section-index includes | `user_guide/index.typ` guards `"user_guide/index#0>user_guide/configuration"` with `include("configuration.typ")`; `examples/index.typ` likewise |
| Base sidebar counts | 2 for each of the five pages, 1 for the control `user_guide/output_layout.html` |
| `docs/source/index.rst` | the lines to remove are `:43-45` and `:52-53` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: head check, PHASE_BASE_SHA, and the clean base HTML build with its positive control</name>
  <files>.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md § "Common Pitfalls" 1-2 and § "Code Examples" "Materialising a snapshot…" (the build shape this task runs)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-PATTERNS.md § "Evidence markdown files" (header, KEY = value block, Head check shape)
    - .planning/ROADMAP.md v0.9.5 constraints 6, 7 and 8, and Phase 72 SC#3
    - docs/source/conf.py lines 16-25 and 50-57 (the `__file__`-relative pyproject path and the environment-driven `language`)
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`; `pwd -P`, which must be under `/home/yuta/Documents/typsphinx/.claude/worktrees/`; `test -f .git; echo "exit:$?"`; `grep -c typsphinx-fhs-run "$(command -v uv)"`.
   - The provisioning line and its exit status.
   - `sed -n 's/^home = //p;s/^version_info = //p' .venv/pyvenv.cfg`, as keys `PYVENV_HOME` and `PYVENV_VERSION_INFO`.
   - `uv run python -c` printing `sphinx.__version__`, `docutils.__version__` and `furo.__version__`, as keys `SPHINX_VERSION`, `DOCUTILS_VERSION` and `FURO_VERSION`.
   - `printenv SPHINX_LANGUAGE READTHEDOCS_LANGUAGE; echo "exit:$?"`, which must print nothing but `exit:1`. Either variable would change the docs language (`conf.py` `_resolve_language()`); if one is set, write `## HALT: docs language overridden` and stop.
   - Before any commit: `PHASE_BASE_SHA = ` followed by `git rev-parse HEAD`, and `SCRATCH_72_01 = ` followed by the path `mktemp -d` printed.

2. **`## Base identity`.** Record `MILESTONE_BASE = 098a8ff64cf008822eef9dc69f75102ded3f7bc1`, then `git diff --name-only 098a8ff64cf008822eef9dc69f75102ded3f7bc1 "$PHASE_BASE_SHA" -- . ':(exclude).planning'`, which must be empty. If it is not, write `## HALT: base is not the milestone base` and stop.

3. **`## Base HTML build`.**
   - Materialise the base: `mkdir "$S/tree-base"`, then `git archive "$PHASE_BASE_SHA" | tar -x -C "$S/tree-base"`.
   - From the worktree root, with `"$S/html-base"` absent, run `LANG=C LC_ALL=C uv run sphinx-build -b html "$S/tree-base/docs/source" "$S/html-base"`, stdout to `$S/p7201_html-base.out` and stderr to `$S/p7201_html-base.err`, then `echo "exit:$?"`. Do not pass `-q`: the `multiple toctrees` message is info-level, and a quieted build could hide it (72-RESEARCH.md Open Question 1).
   - Record the full command in a fenced block under the sub-heading `### Build command`, so 72-04 can reuse it with only the tree and output paths changed.
   - `LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees'` over the `.out` file, key `BASE_MULTI_TOCTREE_COUNT`, and over the `.err` file, key `BASE_MULTI_TOCTREE_COUNT_STDERR`. Use `|| true` so a zero count does not abort your shell.
   - Transcribe every matching line verbatim.
   - The English summary line from `LANG=C LC_ALL=C grep -E '^build succeeded' "$S/p7201_html-base.out"`, verbatim. Key `BASE_WARNING_COUNT` is the integer in `build succeeded, N warnings.` or `build succeeded, 1 warning.`, and `0` when the line reads `build succeeded.`.
   - Transcribe the `WARNING:` and `ERROR:` lines of the `.err` file verbatim under `### Counted warnings`.
   - For `examples/basic` and `examples/advanced`, record the docname after `selecting:` in their message lines, as keys `BASE_CONSOLE_SELECTING_EXAMPLES_BASIC` and `BASE_CONSOLE_SELECTING_EXAMPLES_ADVANCED`. That token is only what Sphinx's log line prints; 72-04 interprets it. Do not describe it here as the parent Sphinx uses.
   - **Positive control (constraint 7):** `BASE_MULTI_TOCTREE_COUNT` must be at least 1. If it is 0, or the English `build succeeded` line is missing, write `## HALT: positive control failed` with both logs' tails and stop. A zero here would mean a broken grep or locale, not a fixed tree.

4. Write `72-BASE-EVIDENCE.md` under `# Phase 72 — Base Evidence (PHASE_BASE_SHA, positive control, DOC-18 edit)`, with the three section headings above, and commit it.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-BASE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; P="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$P" HEAD &amp;&amp; [ "$(k MILESTONE_BASE)" = 098a8ff64cf008822eef9dc69f75102ded3f7bc1 ] &amp;&amp; [ -z "$(git diff --name-only 098a8ff64cf008822eef9dc69f75102ded3f7bc1 "$P" -- . ':(exclude).planning')" ] &amp;&amp; S="$(k SCRATCH_72_01)" &amp;&amp; test -s "$S/p7201_html-base.out" &amp;&amp; C="$(LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees' "$S/p7201_html-base.out" || true)" &amp;&amp; [ "$C" -ge 1 ] &amp;&amp; [ "$C" = "$(k BASE_MULTI_TOCTREE_COUNT)" ] &amp;&amp; BL="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$S/p7201_html-base.out" || true)" &amp;&amp; [ -n "$BL" ] &amp;&amp; N="$(printf '%s\n' "$BL" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')" &amp;&amp; { [ -n "$N" ] || { [ "$BL" = 'build succeeded.' ] &amp;&amp; N=0; }; } &amp;&amp; [ "$N" = "$(k BASE_WARNING_COUNT)" ] &amp;&amp; [ -n "$(k PYVENV_VERSION_INFO)" ] &amp;&amp; [ -n "$(k SPHINX_VERSION)" ] &amp;&amp; [ -n "$(k BASE_CONSOLE_SELECTING_EXAMPLES_BASIC)" ] &amp;&amp; for s in '## Head check and provisioning' '## Base identity' '## Base HTML build' '### Build command' '### Counted warnings'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree under .claude/worktrees, or the FHS shim absent
- `PHASE_BASE_SHA` not 40 characters or not an ancestor of HEAD
- the base product tree differing from 098a8ff6 outside .planning
- the base stdout log missing, its English `multiple toctrees` count 0, or not equal to `BASE_MULTI_TOCTREE_COUNT`
- no English `build succeeded` line, or its N not equal to `BASE_WARNING_COUNT`
- interpreter, Sphinx version or console-selecting keys empty
- any product-tree file changed by this task
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `PHASE_BASE_SHA`, `MILESTONE_BASE`, `SCRATCH_72_01`, `PYVENV_HOME`, `PYVENV_VERSION_INFO`, `SPHINX_VERSION`, `DOCUTILS_VERSION` and `FURO_VERSION` are recorded as bare values.
    - `BASE_MULTI_TOCTREE_COUNT` is at least 1 and equals a fresh `LANG=C LC_ALL=C grep -c` of the recorded stdout log; every matching line is transcribed.
    - `BASE_WARNING_COUNT` equals the N parsed from the English `build succeeded` line, and the counted warnings are transcribed.
    - The build command is recorded verbatim under `### Build command`, without `-q`.
  </acceptance_criteria>
  <done>The pre-edit HTML baseline exists, with a non-zero C-locale `multiple toctrees` count as positive control and the warning count read from the English summary line.</done>
</task>

<task type="auto">
  <name>Task 2: Base -b typst build, base sidebar counts, and the phase-head remote reads</name>
  <files>.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md (Task 1: `PHASE_BASE_SHA`, `SCRATCH_72_01`, the build command)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md § "Code Examples" "Sidebar-scoped link count" (the parser to copy verbatim) and "Typst side: dead include() lines disappear…"
    - .planning/ROADMAP.md v0.9.5 constraints 3 and 6 (required status checks, the main baseline run, the branch census) and Phase 72 SC#4 and SC#5
  </read_first>
  <action>
1. **`## Base Typst build`.** From the worktree root, with `"$S/typst-base"` absent, run `LANG=C LC_ALL=C uv run sphinx-build -b typst "$S/tree-base/docs/source" "$S/typst-base"`, output to `$S/p7201_typst-base.log`, then `echo "exit:$?"`, which must print `exit:0`. Record:
   - `grep -c 'include("' "$S/typst-base/index.typ"`, key `BASE_ROOT_INCLUDE_LINES`
   - for each page P in `user_guide/configuration`, `user_guide/builders`, `user_guide/templates`, `examples/basic`, `examples/advanced`, the root line matched by `grep -F "\"index#0>P\""` in `index.typ`, verbatim
   - key `BASE_ROOT_DEAD_INCLUDES`, the number of those five root lines found, which should be 5
   - the master wrapper state line, `grep -F 'state("typsphinx:include-edges", ()).update(' "$S/typst-base/typsphinx.typ"`, verbatim, and `BASE_EDGE_STATE_SHA256` from `sha256sum` of exactly that line
   - key `BASE_DEAD_GUARD_EDGES_IN_STATE`, the number of the five strings `"index#0>P"` present in that state line, which must be 0: the root guards exist but never fire
   - the section-index lines guarding each page, from `user_guide/index.typ` and `examples/index.typ`, verbatim

2. **`## Base sidebar`.** Copy the parser from 72-RESEARCH.md § "Sidebar-scoped link count" verbatim to `$S/p7201_sidebar.py`; it is a scratch helper and is never committed. Run `uv run python "$S/p7201_sidebar.py" "$S/html-base/index.html"` and transcribe its six output lines. Keys:
   - `BASE_SIDEBAR_COUNTS`: the five pages' counts, space-separated, in the order configuration, builders, templates, basic, advanced
   - `BASE_SIDEBAR_CONTROL`: the `user_guide/output_layout.html` count
   - The base must show the defect: every one of the five counts must be at least 2. Otherwise write `## HALT: sidebar positive control failed` and stop.

3. **`## Phase-head remote reads`.**
   - Required checks: `gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict`, key `REQUIRED_STRICT_HEAD`, and the same call with `--jq '[.contexts[]] | sort | join("|")'`, key `REQUIRED_CONTEXTS_HEAD`, plus `REQUIRED_CONTEXT_COUNT_HEAD`.
   - Main's baseline CI run (constraint 6): `git ls-remote origin refs/heads/main`, key `MAIN_HEAD_AT_BASE`. Then `gh run list --workflow=ci.yml --branch main --event push --limit 10 --json databaseId,headSha,status,conclusion,createdAt`. Take the row whose `headSha` equals `MAIN_HEAD_AT_BASE` and record `MAIN_BASELINE_RUN_ID` and `MAIN_BASELINE_CONCLUSION`. If no such row exists, record both as `none` and say so on the next line. This is a record, not a gate.
   - Head branch census: `git branch --list 'gsd/v0.9.5*' -v` and `git ls-remote --heads origin 'gsd/v0.9.5*'`, verbatim. 72-06 re-measures both immediately before its push.

4. Commit the evidence update.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-BASE-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; P="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; S="$(k SCRATCH_72_01)" &amp;&amp; T="$S/typst-base" &amp;&amp; test -s "$T/index.typ" &amp;&amp; test -s "$T/typsphinx.typ" &amp;&amp; n=0 &amp;&amp; for p in user_guide/configuration user_guide/builders user_guide/templates examples/basic examples/advanced; do c="$(grep -cF "\"index#0&gt;$p\"" "$T/index.typ" || true)"; [ "$c" = 1 ] || { echo "root guard count for $p: $c"; exit 1; }; n=$((n+1)); done &amp;&amp; [ "$n" = "$(k BASE_ROOT_DEAD_INCLUDES)" ] &amp;&amp; SL="$(grep -F 'state("typsphinx:include-edges", ()).update(' "$T/typsphinx.typ")" &amp;&amp; [ "$(printf '%s\n' "$SL" | wc -l)" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$SL" | sha256sum | cut -d' ' -f1)" = "$(k BASE_EDGE_STATE_SHA256)" ] &amp;&amp; [ "$(printf '%s\n' "$SL" | grep -oE '"index#0&gt;(user_guide/configuration|user_guide/builders|user_guide/templates|examples/basic|examples/advanced)"' | wc -l)" = 0 ] &amp;&amp; [ "$(k BASE_DEAD_GUARD_EDGES_IN_STATE)" = 0 ] &amp;&amp; O="$(uv run python "$S/p7201_sidebar.py" "$S/html-base/index.html")" &amp;&amp; BC="$(for t in user_guide/configuration user_guide/builders user_guide/templates examples/basic examples/advanced; do printf '%s\n' "$O" | sed -n "s|^$t.html: count=\([0-9]*\) .*|\1|p"; done | tr '\n' ' ' | sed 's/ $//')" &amp;&amp; [ "$BC" = "$(k BASE_SIDEBAR_COUNTS)" ] &amp;&amp; for c in $BC; do [ "$c" -ge 2 ] || { echo "base sidebar count $c"; exit 1; }; done &amp;&amp; [ "$(k BASE_SIDEBAR_CONTROL)" = 1 ] &amp;&amp; [ -n "$(k REQUIRED_STRICT_HEAD)" ] &amp;&amp; [ "$(k REQUIRED_CONTEXTS_HEAD)" = "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '[.contexts[]] | sort | join("|")')" ] &amp;&amp; [ -n "$(k MAIN_HEAD_AT_BASE)" ] &amp;&amp; [ -n "$(k MAIN_BASELINE_RUN_ID)" ] &amp;&amp; for s in '## Base Typst build' '## Base sidebar' '## Phase-head remote reads'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or one of `root guard count for <page>: <n>`, `base sidebar count <n>` or `missing section: <name>` printed. Each of these makes it fail:
- the base Typst output missing index.typ or typsphinx.typ
- any of the five pages without exactly one root guard line, or the guard total differing from `BASE_ROOT_DEAD_INCLUDES`
- the edge-state line absent, duplicated, or its SHA-256 differing from `BASE_EDGE_STATE_SHA256`
- a root edge key for one of the five pages present in the state
- the sidebar counts re-read from the base index.html differing from `BASE_SIDEBAR_COUNTS`, any of them below 2, or the control not 1
- the recorded required contexts differing from a live re-read
- `MAIN_HEAD_AT_BASE` or `MAIN_BASELINE_RUN_ID` empty
- a product-tree file changed, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - Five root guard lines for the five pages are transcribed verbatim, `BASE_ROOT_DEAD_INCLUDES = 5` is re-derived from the base `index.typ`, and `BASE_DEAD_GUARD_EDGES_IN_STATE = 0`.
    - The include-edges state line is transcribed with `BASE_EDGE_STATE_SHA256`.
    - `BASE_SIDEBAR_COUNTS` holds five counts, each at least 2, and `BASE_SIDEBAR_CONTROL = 1`, re-derived by the scratch parser.
    - `REQUIRED_STRICT_HEAD`, `REQUIRED_CONTEXTS_HEAD`, `REQUIRED_CONTEXT_COUNT_HEAD`, `MAIN_HEAD_AT_BASE`, `MAIN_BASELINE_RUN_ID`, `MAIN_BASELINE_CONCLUSION` and the head branch census are recorded.
  </acceptance_criteria>
  <done>The Typst and sidebar sides of the pre-edit baseline and the phase-head remote facts are recorded, before any docs edit.</done>
</task>

<task type="auto">
  <name>Task 3: DOC-18 edit: remove the five duplicate root entries and move the folded todo in the same commit (D-08)</name>
  <files>docs/source/index.rst, .planning/todos/pending/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md, .planning/todos/completed/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md, .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md</files>
  <read_first>
    - docs/source/index.rst (the User Guide and Examples toctrees; at planning, lines 38-53)
    - docs/source/user_guide/index.rst lines 6-12 and docs/source/examples/index.rst lines 6-10 (the toctrees that keep owning the pages; read only)
    - .planning/todos/pending/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md (the folded todo, moved unchanged)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md D-08
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md § "Pattern 2" (the exact target state)
  </read_first>
  <action>
1. **The edit (DOC-18, owner decision 2026-09-13: the conventional hierarchy).** With the Edit tool, delete exactly these five lines from `docs/source/index.rst`, each a three-space indent followed by the docname:
   - `user_guide/configuration`, `user_guide/builders` and `user_guide/templates` from the User Guide toctree
   - `examples/basic` and `examples/advanced` from the Examples toctree

   Keep `   user_guide/index` and `   examples/index`, every `:maxdepth:` and `:caption:` line, the blank lines between toctrees, and the Getting Started, API Reference and Development toctrees. Do not touch `user_guide/index.rst`, `examples/index.rst` or `conf.py`, and add no theme, sidebar or `:maxdepth:` setting.

2. **The move (D-08).** `git mv .planning/todos/pending/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md .planning/todos/completed/`, with the content unchanged.

3. **One commit for both.** Stage exactly those two changes and run `git commit -m "docs(72-01): list only section indexes in the root toctrees (DOC-18)"`. That commit touches nothing else.

4. **`## DOC-18 edit`.** Record:
   - `DOC18_EDIT_COMMIT = ` followed by `git rev-parse HEAD`, then `git show --name-status --format= HEAD`, which must show exactly `M docs/source/index.rst` and one `R100` rename from the pending path to the completed path
   - `git diff --numstat "$PHASE_BASE_SHA" HEAD -- docs/source/index.rst`, which must read `0` added and `5` removed, and the `git diff -U0` removed lines verbatim
   - the two toctrees as they now read

5. **`## Post-edit sanity build`.** Per-task sampling only; the binding SC#3 pair is 72-04's. Run `git archive HEAD | tar -x -C "$S/tree-edit"`, then Task 1's HTML build command with the tree and output paths changed to `tree-edit` and `html-edit`. Keys:
   - `EDIT_MULTI_TOCTREE_COUNT`, which must be 0
   - `EDIT_WARNING_COUNT`, which must equal `BASE_WARNING_COUNT`
   - The English `build succeeded` line must be present. If any of the three fails, write `## HALT: post-edit build` with the lines and stop.

6. Commit the evidence in a second commit.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-BASE-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; P="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; I=docs/source/index.rst &amp;&amp; PEND=.planning/todos/pending/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md &amp;&amp; COMP=.planning/todos/completed/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md &amp;&amp; [ "$(git diff --numstat "$P" HEAD -- "$I" | cut -f1,2)" = "$(printf '0\t5')" ] &amp;&amp; [ "$(git diff -U0 "$P" HEAD -- "$I" | grep -E '^-   ' | sed 's/^-   //' | sort | tr '\n' ' ')" = 'examples/advanced examples/basic user_guide/builders user_guide/configuration user_guide/templates ' ] &amp;&amp; [ "$(grep -cE '^   user_guide/' "$I")" = 1 ] &amp;&amp; grep -qx '   user_guide/index' "$I" &amp;&amp; [ "$(grep -cE '^   examples/' "$I")" = 1 ] &amp;&amp; grep -qx '   examples/index' "$I" &amp;&amp; [ "$(git show "$P:$I" | grep -cE '^\.\. toctree::|^   :(maxdepth|caption):')" = "$(grep -cE '^\.\. toctree::|^   :(maxdepth|caption):' "$I")" ] &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- docs/source/user_guide/index.rst docs/source/examples/index.rst docs/source/conf.py)" ] &amp;&amp; E="$(k DOC18_EDIT_COMMIT)" &amp;&amp; [ "${#E}" = 40 ] &amp;&amp; NS="$(git show --name-status --format= "$E")" &amp;&amp; [ "$(printf '%s\n' "$NS" | wc -l)" = 2 ] &amp;&amp; printf '%s\n' "$NS" | grep -qx "$(printf 'M\tdocs/source/index.rst')" &amp;&amp; printf '%s\n' "$NS" | grep -qx "$(printf 'R100\t%s\t%s' "$PEND" "$COMP")" &amp;&amp; test ! -e "$PEND" &amp;&amp; [ "$(git rev-parse "$P:$PEND")" = "$(git rev-parse "HEAD:$COMP")" ] &amp;&amp; S="$(k SCRATCH_72_01)" &amp;&amp; test -s "$S/p7201_html-edit.out" &amp;&amp; [ "$(LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees' "$S/p7201_html-edit.out" || true)" = 0 ] &amp;&amp; LANG=C LC_ALL=C grep -qE '^build succeeded' "$S/p7201_html-edit.out" &amp;&amp; [ "$(k EDIT_MULTI_TOCTREE_COUNT)" = 0 ] &amp;&amp; [ "$(k EDIT_WARNING_COUNT)" = "$(k BASE_WARNING_COUNT)" ] &amp;&amp; for s in '## DOC-18 edit' '## Post-edit sanity build'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning' | grep -vxF "$I")" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- index.rst numstat other than 0 added and 5 removed, or the removed lines not exactly the five entries
- a toctree holding other than exactly `user_guide/index` or `examples/index` under the two captions
- the number of toctree, :maxdepth: and :caption: lines changing
- user_guide/index.rst, examples/index.rst or conf.py in the diff
- the DOC-18 commit not holding exactly the index.rst modification and one R100 rename of the todo, the pending file still present, or the moved todo's blob differing from the base
- the post-edit build log missing, carrying a `multiple toctrees` line, lacking the English `build succeeded` line, or its warning count differing from the base
- any product-tree file other than index.rst changed, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat PHASE_BASE_SHA HEAD -- docs/source/index.rst` is `0 5`, and the removed lines are exactly `user_guide/configuration`, `user_guide/builders`, `user_guide/templates`, `examples/basic` and `examples/advanced`.
    - `DOC18_EDIT_COMMIT` holds exactly `M docs/source/index.rst` and `R100` of the 2026-08-16 todo into `todos/completed/` (D-08), and the moved blob equals the base blob.
    - `EDIT_MULTI_TOCTREE_COUNT = 0` and `EDIT_WARNING_COUNT` equals `BASE_WARNING_COUNT`, from a clean C-locale build whose English summary line is present.
  </acceptance_criteria>
  <done>The root toctrees list only the section indexes, the folded todo is in `todos/completed/` in the same commit, and a clean post-edit build shows zero `multiple toctrees` messages at the base's warning count.</done>
</task>

</tasks>

<assumption_delta_decision>
The assumption-delta detector fired (`detected: true`) on the cues `also`, `second`, `multiple` and
`alongside`. All of them come from prose describing the duplicate toctree listing that this phase
*removes*, and from the sentence scheduling DOC-18 beside QUA-13. No identity model generalises:
nothing is turned from one into many.
- Noun that is primary: the root toctree entry (one per section).
- Decision: `no-change`. The phase deletes a duplication and adds no plural model.
- No accepted debt.
</assumption_delta_decision>

<flagged_assumptions>
**Phase-wide probe accounting** (spec-less fallback; no phase SPEC exists).

Edge coverage: 15 items surfaced, and every one is authored as a `must_haves.truths` entry.
- 72-02 authors the six QUA-13 edges: boundary, adjacency, empty, ordering, precision, and
  concurrency, the last as a backstop marker.
- 72-03 authors the four DOC-24 edges: adjacency, empty, encoding and ordering.
- This plan authors three DOC-18 edges: boundary, adjacency and empty.
- 72-04 authors the other two DOC-18 edges: ordering and precision.

Total: 15 = 14 explicit + 1 backstop + 0 unresolved + 0 dropped.

Prohibitions: six were kept, and each is authored descriptor-less and judgment-tier in the
`must_haves.prohibitions` of the plan it binds.
- P1, no ignore-style key or link edit to force a pass: 72-02, 72-05.
- P2, no pass counted from a run that did not itself meet SC#1, and no failed run deleted: 72-02,
  72-05.
- P3, linkcheck never in `env_list` or CI: 72-02.
- P4, no surface names linkcheck before it exists or claims a CI job: 72-03.
- P5, no configuration substitute for the fix, and no divergence fix: this plan and 72-04.
- P6, no `typsphinx/` or workflow change, force, tag, decoy, release trigger, PR or second
  dispatch: 72-05, 72-06.

Total: 6 kept = 6 authored + 0 dismissed. One canon candidate was referred and not minted: SSRF
through a committed doc URL when linkcheck runs is canon, covered by `/gsd-secure-phase`.

Residual planner assumptions:
- (a) `uv sync --python 3.13.13` resolves in a fresh worktree, as it did in Phases 70 and 71.
- (b) `gh` is authenticated in executor sessions.
- (c) The reading of D-01 with D-02 under the three-run ceiling is stated in 72-02 Task 2.
- (d) 72-04's rule for re-taking an HTML pair after an intersphinx inventory-fetch warning.

(c) and (d) preserve every attempt in the evidence and route anything else to the owner.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git archive snapshot → scratch build | the base tree is rebuilt outside the tracked tree; stale or incremental output could fake a baseline |
| Sphinx console → evidence | localised console text can turn a real count into a false zero |
| gh CLI → GitHub (read-only) | branch-protection and run-list reads; no write crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-01 | Repudiation | a C-locale grep silently reading localised output, a false zero | high | mitigate | `LANG=C LC_ALL=C` on every build and grep; the English `build succeeded` line is required; the base count must be at least 1, else the plan halts |
| T-72-02 | Tampering | an incremental or wrong-tree base build | medium | mitigate | full-tree `git archive` of `PHASE_BASE_SHA` into a fresh `mktemp -d`; the output directory is absent before each build; `PHASE_BASE_SHA` is proven equal to 098a8ff6 outside `.planning/` |
| T-72-03 | Tampering | the DOC-18 edit touching more than the five entries, a section index, or conf.py | medium | mitigate | numstat `0 5`, the exact removed-line set, and empty diffs for the two section indexes and conf.py in the verify |
| T-72-04 | Information Disclosure | branch-protection settings and run IDs in committed evidence | low | accept | public repository; only check names and run IDs are recorded |
| T-72-SC | Tampering | package installs during provisioning | low | accept | `uv sync` installs only what `uv.lock` pins; no new dependency |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list, owned here:
- `tox.ini` `[testenv:linkcheck]` (72-02); `docs/source/conf.py` gains a `linkcheck_*` key only
  under D-02 (72-02 or 72-05).
- `docs/source/index.rst` edited (this plan); `CLAUDE.md`, `README.md`,
  `docs/source/contributing.rst` each gain one line (72-03).
- `.planning/todos/completed/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md`
  moved from `todos/pending/` (this plan); `.planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md`
  gains a status note (72-03); `.planning/todos/pending/2026-09-13-html-and-typst-toctree-parents-diverge-after-dedup.md`
  only if 72-04 finds a surviving divergence.
- Evidence: `72-BASE-EVIDENCE.md` (this plan), `72-LINKCHECK-EVIDENCE.md` (72-02),
  `72-DOC24-EVIDENCE.md` (72-03), `72-TOCTREE-EVIDENCE.md` (72-04), `72-GATES-EVIDENCE.md` (72-05),
  `72-CI-EVIDENCE.md` (72-06). No `COVERAGE.md` is produced: the plan-time api-coverage detector,
  run over every plan body plus the ROADMAP Phase 72 section, returned `detected: false`.
- Outside the repository: `origin/gsd/v0.9.5-docs-link-check-and-navigation` created, and one
  `workflow_dispatch` CI run (72-06).

This plan's keys: `PHASE_BASE_SHA`, `MILESTONE_BASE`, `SCRATCH_72_01`, `PYVENV_HOME`,
`PYVENV_VERSION_INFO`, `SPHINX_VERSION`, `DOCUTILS_VERSION`, `FURO_VERSION`,
`BASE_MULTI_TOCTREE_COUNT`, `BASE_MULTI_TOCTREE_COUNT_STDERR`, `BASE_WARNING_COUNT`,
`BASE_CONSOLE_SELECTING_EXAMPLES_BASIC`, `BASE_CONSOLE_SELECTING_EXAMPLES_ADVANCED`,
`BASE_ROOT_INCLUDE_LINES`, `BASE_ROOT_DEAD_INCLUDES`, `BASE_EDGE_STATE_SHA256`,
`BASE_DEAD_GUARD_EDGES_IN_STATE`, `BASE_SIDEBAR_COUNTS`, `BASE_SIDEBAR_CONTROL`,
`REQUIRED_STRICT_HEAD`, `REQUIRED_CONTEXTS_HEAD`, `REQUIRED_CONTEXT_COUNT_HEAD`,
`MAIN_HEAD_AT_BASE`, `MAIN_BASELINE_RUN_ID`, `MAIN_BASELINE_CONCLUSION`, `DOC18_EDIT_COMMIT`,
`EDIT_MULTI_TOCTREE_COUNT`, `EDIT_WARNING_COUNT`. No file named `72-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- `PHASE_BASE_SHA` is recorded before any commit and equals the milestone base outside `.planning/`.
- The base's C-locale HTML build has a positive `multiple toctrees` count, the English summary line
  and its N; the base Typst build has five dead root guards and no matching edges.
- `docs/source/index.rst` loses exactly five lines in one commit that also moves the folded todo.
</verification>

<success_criteria>
The pre-fix baseline SC#3 and SC#4 compare against exists, with its positive control, taken before
any docs edit. DOC-18's edit has landed in the shape SC#3 names.
</success_criteria>

<output>
Create `.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-01-SUMMARY.md` when done.
Its first section states `PHASE_BASE_SHA`, `BASE_MULTI_TOCTREE_COUNT`, `BASE_WARNING_COUNT`,
`DOC18_EDIT_COMMIT` and `EDIT_MULTI_TOCTREE_COUNT`. Do not edit `.planning/REQUIREMENTS.md`,
`.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
