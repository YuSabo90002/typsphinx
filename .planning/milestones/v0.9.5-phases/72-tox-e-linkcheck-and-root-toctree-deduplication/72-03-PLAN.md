---
phase: 72-tox-e-linkcheck-and-root-toctree-deduplication
plan: 03
type: execute
wave: 2
depends_on: ["72-02"]
files_modified:
  - CLAUDE.md
  - README.md
  - docs/source/contributing.rst
  - .planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md
  - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-DOC24-EVIDENCE.md
autonomous: true
requirements: [DOC-24]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'QUA-13 before DOC-24 (ROADMAP binding ordering): before any surface is edited, tox.ini at this worktree HEAD already carries [testenv:linkcheck] from 72-02.'
    - 'DOC-24 / SC#2: `git grep -n ''tox -e docs-pdf'' -- '':!.planning''` is re-run at execution and recorded in full; every hit that lists the tox environments gains a linkcheck line in the same block, and every other hit has a recorded disposition, docs.yml and CHANGELOG.md included (D-06).'
    - 'D-05: the new line is last in its block and uses the neighbouring prefix: `tox -e linkcheck` in CLAUDE.md, `uv run tox -e linkcheck` in README.md and docs/source/contributing.rst. Its comment reads `# Check external links and anchors (needs network; not run by plain tox)`, which states both the network need and that plain tox does not run it.'
    - 'D-06: each edited file''s diff is exactly one added line and zero removed lines, so every existing line stays byte-identical. That includes CLAUDE.md''s `tox  # env_list: …` line, contributing.rst''s `# Run all tox environments …` comment, and both CLAUDE.md sync-hazard passages. No prose about linkcheck is added anywhere.'
    - 'D-07: the 2026-07-22 linkcheck todo stays in todos/pending/ and gains an appended status note, written after the environment landed. The note records that [testenv:linkcheck] exists (Phase 72, QUA-13), that the still-open part is the CI job, QUA-08, a Future requirement, and that a future pickup must plan a side PR to main (ROADMAP constraint 4). Its existing text and frontmatter are unchanged, and no new todo is filed.'
    - 'Edge (DOC-24 adjacency): in each block the new line''s `#` starts in the same column as its neighbours: 29 in CLAUDE.md, 28 in README.md, 31 in contributing.rst at planning time, re-measured with Python str.index. The line directly above it is unchanged.'
    - 'Edge (DOC-24 empty): a discovery grep returning zero hits is a broken discovery, not "nothing to edit"; the grep must return at least one hit, and the CLAUDE.md hit is the positive control.'
    - 'Edge (DOC-24 encoding): every added line is pure ASCII, so its byte, code-point and column counts agree. Column equality is measured in code points against neighbours that are themselves ASCII. CLAUDE.md''s non-ASCII line, the en dash in the py312–py313 comment, is outside the edited block and untouched.'
    - 'Edge (DOC-24 ordering): the new line is the last non-blank line of each block. In CLAUDE.md it follows `tox -e docs-pdf`, and in README.md and contributing.rst it follows the `uv run tox -e docs` line.'
  artifacts:
    - path: .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-DOC24-EVIDENCE.md
      provides: 'the discovery grep in full, a disposition per hit, per-file numstat and column proof, the post-edit grep, and the D-07 note diff'
      contains: 'DOC24_VERDICT = '
    - path: CLAUDE.md
      provides: 'tox -e linkcheck listed in § Commands'
      contains: 'tox -e linkcheck             # Check external links and anchors (needs network; not run by plain tox)'
  key_links:
    - from: 'CLAUDE.md, README.md and docs/source/contributing.rst tox blocks'
      to: 'tox.ini [testenv:linkcheck]'
      via: 'the listed command names an environment that exists at this HEAD'
      pattern: 'tox -e linkcheck'
  prohibitions:
    - statement: 'MUST NOT name tox -e linkcheck on any surface before tox.ini carries the environment, MUST NOT add any prose claiming linkcheck runs in CI or on a schedule (QUA-08 is deferred), and MUST NOT file a new todo for QUA-08 (D-07).'
      requirement_id: DOC-24
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Land DOC-24 and D-07. Every surface that lists the tox environments names `tox -e linkcheck`.
Discovery is by a fresh grep, and the placement, prefix, column and comment follow D-05, with every
existing line left byte-identical (D-06). The 2026-07-22 linkcheck todo gains its status note (D-07).

Purpose: this runs in wave 2 because QUA-13's environment must exist before any surface names it.
It shares no file with 72-01's DOC-18 edit or 72-02's `tox.ini`. The `contributing.rst` line it adds
is carried by 72-04's tip build, so one base/tip pair covers both docs edits.

Output: three one-line edits, the todo note, and `72-DOC24-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --python 3.13.13`
and run Python through `uv run` (`CLAUDE.md` § "Worktree-isolated execution"). This plan builds no
docs.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put any explanation on
the next line. The verify fails on any key line containing an opening parenthesis.

**Scratch.** Put helpers under your own `mktemp -d` directory, recorded as `SCRATCH_72_03`, and
prefix them `p7203_`.

**Commits.** Plain `git commit`. Never a GSD commit helper. Never write `72-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->

## Planning-time measurements (2026-09-13; context only, re-measure, never trust)

`git grep -n 'tox -e docs-pdf' -- ':!.planning'`:

| Hit | Reading at planning |
|-----|---------------------|
| `.github/workflows/docs.yml:36` | a CI step running `uv run tox -e docs-pdf`; not a listing, and a workflow file (constraint 3) |
| `CHANGELOG.md:921` | a bullet inside a released section; history, not a listing |
| `CLAUDE.md:33` | listing surface, last line of the docs block (`#` at column 29) |
| `README.md:262` | listing surface; block ends at `:263` `uv run tox -e docs` (`#` at column 28) |
| `docs/source/contributing.rst:124` | listing surface; block ends at `:125` (`#` at column 31) |

Supplementary grep `git grep -nE 'tox -e (lint|type|py312|docs-html|docs)( |$)' -- ':!.planning'`
adds no new file beyond `.github/workflows/ci.yml:70` (a CI step), `.github/workflows/docs.yml:33`
and `tests/test_toolchain_config_gate.py:16` (a docstring). None is a listing surface.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: gate on the landed environment, run the discovery grep, and add the linkcheck line to CLAUDE.md's docs block</name>
  <files>CLAUDE.md, .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-DOC24-EVIDENCE.md</files>
  <read_first>
    - CLAUDE.md § Commands (lines 10-36; the docs block is the last three lines of the fenced block)
    - tox.ini (must end with the `[testenv:linkcheck]` section from 72-02)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md D-05 and D-06
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md § "Pattern 3"
    - .planning/ROADMAP.md Phase 72 SC#2 and v0.9.5 constraint 11
  </read_first>
  <action>
1. **`## Head check and gate`.**
   - Record `date -u +%FT%TZ`, `pwd -P` under `.claude/worktrees/`, `test -f .git; echo "exit:$?"`, the shim check, and the provisioning line with its exit.
   - Before any commit, record `BASE_72_03 = ` followed by `git rev-parse HEAD`, and `SCRATCH_72_03 = ` followed by the path `mktemp -d` printed.
   - Record `grep -nx '\[testenv:linkcheck\]' tox.ini`. If it prints nothing, 72-02's environment has not landed. Write `## HALT: linkcheck environment absent` and stop, because naming it now would be false (ROADMAP ordering).

2. **`## Discovery`.**
   - Record `git grep -n 'tox -e docs-pdf' -- ':!.planning'` in full, and key `DOC24_GREP_HITS` with its line count. It must be at least 1; zero means a broken discovery, so write `## HALT: empty discovery`.
   - Record the supplementary `git grep -nE 'tox -e (lint|type|py312|docs-html|docs)( |$)' -- ':!.planning'` in full. It looks for a listing surface that omits `docs-pdf` (milestone invariant #4).

3. **The CLAUDE.md line (D-05).** Directly after the line `tox -e docs-pdf              # PDF via the typstpdf builder (dogfoods this extension)`, insert this single line: `tox -e linkcheck             # Check external links and anchors (needs network; not run by plain tox)`. That is `tox -e linkcheck`, thirteen spaces, then the comment. The wording is the CONTEXT reference wording, chosen under Claude's discretion. Change nothing else in the file (D-06). Commit with `git commit -m "docs(72-03): list tox -e linkcheck in CLAUDE.md (DOC-24)"`.

4. **`## CLAUDE.md`.** Record:
   - `git diff --numstat "$BASE_72_03" HEAD -- CLAUDE.md`, which must show 1 added and 0 removed
   - the `git diff -U1` hunk
   - `uv run python -c` printing, for the new line and the two lines above it, `line.index('#')` and `line.isascii()`. Key `CLAUDE_COMMENT_COLUMN`: the three columns must be equal and the new line ASCII.

5. Write `72-DOC24-EVIDENCE.md` under `# Phase 72 — DOC-24 Evidence (SC#2) and the D-07 todo note`, and commit.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-DOC24-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_72_03)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; git show "$B:tox.ini" | grep -qx '\[testenv:linkcheck\]' &amp;&amp; H="$(git grep -n 'tox -e docs-pdf' "$B" -- ':!.planning' | wc -l)" &amp;&amp; [ "$H" -ge 1 ] &amp;&amp; [ "$H" = "$(k DOC24_GREP_HITS)" ] &amp;&amp; [ "$(git diff --numstat "$B" HEAD -- CLAUDE.md | cut -f1,2)" = "$(printf '1\t0')" ] &amp;&amp; uv run python -c 'L=open("CLAUDE.md",encoding="utf-8").read().splitlines(); i=L.index("tox -e linkcheck             # Check external links and anchors (needs network; not run by plain tox)"); assert L[i-1].startswith("tox -e docs-pdf "), L[i-1]; assert L[i-2].startswith("tox -e docs-html "); c={x.index("#") for x in L[i-2:i+1]}; assert len(c)==1, c; assert L[i].isascii(); assert L[i+1]=="```", L[i+1]; print(c.pop())' &gt; /dev/null &amp;&amp; [ -n "$(k CLAUDE_COMMENT_COLUMN)" ] &amp;&amp; for s in '## Head check and gate' '## Discovery' '## CLAUDE.md'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning' | grep -vxF CLAUDE.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, a Python AssertionError or ValueError, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_72_03` missing or not an ancestor
- tox.ini at the base lacking the linkcheck section
- the discovery grep empty, or its count differing from `DOC24_GREP_HITS`
- the CLAUDE.md numstat not 1 added and 0 removed
- the exact new line absent, not directly after the docs-pdf line, not the last line of the fenced block, not column-aligned with both neighbours, or not ASCII
- a product-tree file other than CLAUDE.md changed, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - The discovery grep is recorded in full, with `DOC24_GREP_HITS` at least 1, and the supplementary grep is recorded.
    - `CLAUDE.md` gains exactly `tox -e linkcheck             # Check external links and anchors (needs network; not run by plain tox)` directly after the `tox -e docs-pdf` line, as the block's last line. Numstat is 1 and 0, and the `#` column equals its neighbours'.
  </acceptance_criteria>
  <done>One listing surface names the landed environment end to end, and the full hit set is on record.</done>
</task>

<task type="auto">
  <name>Task 2: Add the line to README.md and docs/source/contributing.rst, record a disposition for every hit, and re-grep</name>
  <files>README.md, docs/source/contributing.rst, .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-DOC24-EVIDENCE.md</files>
  <read_first>
    - README.md lines 250-265 (the development commands block)
    - docs/source/contributing.rst lines 108-127 (the "Using Tox" `.. code-block:: bash`, three-space indent)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-DOC24-EVIDENCE.md § Discovery (Task 1's hit list)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md D-05 and D-06
  </read_first>
  <action>
1. **README.md (D-05).** Directly after `uv run tox -e docs          # Build both HTML and PDF docs`, insert `uv run tox -e linkcheck     # Check external links and anchors (needs network; not run by plain tox)`: five spaces after the command, as `docs-html` has.

2. **docs/source/contributing.rst (D-05).** Directly after `   uv run tox -e docs          # Build both HTML and PDF`, insert three spaces followed by `uv run tox -e linkcheck     # Check external links and anchors (needs network; not run by plain tox)`. It is one more line inside the same code block. No blank line or indentation change around it, and no prose paragraph.

3. **Any further listing surface.** If Task 1's discovery or supplementary grep found a listing surface beyond `CLAUDE.md`, `README.md` and `contributing.rst`, give it the same treatment: last in its block, the neighbouring prefix, the same comment aligned to the neighbours' column. Record it. DOC-24 covers every listing surface, not only the three named at roadmap time.

4. Commit with `git commit -m "docs(72-03): list tox -e linkcheck in README and contributing guide (DOC-24)"`.

5. **`## Surfaces`.** For each edited file, record:
   - `git diff --numstat "$BASE_72_03" HEAD -- <file>`, which must show 1 and 0
   - the `-U1` hunk
   - the `line.index('#')` of the new line and its two neighbours, and `isascii()`

   Keys `README_COMMENT_COLUMN` and `CONTRIBUTING_COMMENT_COLUMN`.

6. **`## Dispositions`.** A table with one row per hit of both greps: file:line, the matched text, and a disposition. The disposition is one of:
   - `listing surface — linkcheck line added` (name the file)
   - `CI step that runs one environment; not a listing, and constraint 3 keeps workflow files out of the diff` (for `.github/workflows/*.yml`)
   - `released CHANGELOG history bullet; not a listing` (for `CHANGELOG.md`)
   - `test docstring; not a listing`
   - a stated reason for any other hit

   Every hit gets a row (D-06). Key `DOC24_LISTING_SURFACES`: the number of listing surfaces edited.

7. **`## Post-edit grep`.** Record `git grep -n 'tox -e linkcheck' -- ':!.planning'` in full. Key `DOC24_LINKCHECK_LINES`, which must equal `DOC24_LISTING_SURFACES`, one line per edited surface. Also record `git grep -n 'tox -e docs-pdf' -- ':!.planning'` again, with the same hit count as at discovery. Then `DOC24_VERDICT = MET`. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-DOC24-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_72_03)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; for f in README.md docs/source/contributing.rst; do [ "$(git diff --numstat "$B" HEAD -- "$f" | cut -f1,2)" = "$(printf '1\t0')" ] || { echo "numstat: $f"; exit 1; }; done &amp;&amp; uv run python -c 'import sys
def chk(p, new, above, nxt_ok):
    L=open(p,encoding="utf-8").read().splitlines(); i=L.index(new); assert L[i-1]==above, (p, L[i-1]); c={x.index("#") for x in L[i-3:i+1]}; assert len(c)==1, (p, c); assert L[i].isascii(); assert nxt_ok(L[i+1]), (p, L[i+1])
chk("README.md", "uv run tox -e linkcheck     # Check external links and anchors (needs network; not run by plain tox)", "uv run tox -e docs          # Build both HTML and PDF docs", lambda s: s=="```")
chk("docs/source/contributing.rst", "   uv run tox -e linkcheck     # Check external links and anchors (needs network; not run by plain tox)", "   uv run tox -e docs          # Build both HTML and PDF", lambda s: s=="")' &amp;&amp; grep -qx '   # Run all tox environments (tests, lint, type check, docs)' docs/source/contributing.rst &amp;&amp; grep -qx 'tox                          # env_list: py312, py313, lint, type, cov, docs' CLAUDE.md &amp;&amp; LS="$(k DOC24_LISTING_SURFACES)" &amp;&amp; [ "$LS" -ge 3 ] &amp;&amp; LL="$(git grep -n 'tox -e linkcheck' -- ':!.planning' | wc -l)" &amp;&amp; [ "$LL" = "$LS" ] &amp;&amp; [ "$LL" = "$(k DOC24_LINKCHECK_LINES)" ] &amp;&amp; [ "$(git grep -n 'tox -e docs-pdf' -- ':!.planning' | wc -l)" = "$(k DOC24_GREP_HITS)" ] &amp;&amp; for h in $(git grep -n 'tox -e docs-pdf' "$B" -- ':!.planning' | cut -d: -f2,3); do grep -qF "$h" "$F" || { echo "no disposition for $h"; exit 1; }; done &amp;&amp; [ "$(k DOC24_VERDICT)" = MET ] &amp;&amp; for s in '## Surfaces' '## Dispositions' '## Post-edit grep'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- .github tox.ini CHANGELOG.md docs/source/index.rst)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, a Python AssertionError or ValueError, or one of `numstat: <file>`, `no disposition for <hit>` or `missing section: <name>` printed. Each of these makes it fail:
- README.md or contributing.rst numstat not 1 added and 0 removed
- the exact new line absent, not directly after the `… tox -e docs` line, misaligned with its three neighbours above, not ASCII, or not the block's last line (a closing fence in README.md, a blank line in contributing.rst)
- contributing.rst's `# Run all tox environments` comment or CLAUDE.md's env_list comment changed
- fewer than three listing surfaces recorded, or the post-edit linkcheck line count not equal to the surfaces count
- the docs-pdf hit count changed
- a discovery hit with no disposition row
- the verdict not MET, a section missing, a workflow file, tox.ini, CHANGELOG.md or index.rst in this plan's diff, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `README.md` and `docs/source/contributing.rst` each gain exactly one line, the one named in the action, last in the block, with the `#` column equal to their neighbours'.
    - `## Dispositions` holds one row per discovery hit, including `.github/workflows/docs.yml:36` and `CHANGELOG.md:921`.
    - `DOC24_LINKCHECK_LINES` equals `DOC24_LISTING_SURFACES`, at least 3, and `DOC24_VERDICT = MET`.
  </acceptance_criteria>
  <done>Every surface listing the tox environments names `tox -e linkcheck`, found by grep, and every other hit carries its reason.</done>
</task>

<task type="auto">
  <name>Task 3: Append D-07's status note to the 2026-07-22 linkcheck todo, which stays in todos/pending</name>
  <files>.planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md, .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-DOC24-EVIDENCE.md</files>
  <read_first>
    - .planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md (whole file; frontmatter and Solution bullets stay untouched)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md D-07
    - .planning/REQUIREMENTS.md § Future Requirements (QUA-08)
    - .planning/ROADMAP.md v0.9.5 constraint 4
  </read_first>
  <action>
1. **The note (D-07).** Append to the end of the todo, after one blank line, a section headed `## Status note (Phase 72, v0.9.5)` with three bullets in English:
   - `tox.ini` now has the `[testenv:linkcheck]` environment the first Solution bullet asks for. It runs `sphinx-build -b linkcheck source _build/linkcheck` from `docs/`, and it is not in `env_list` (Phase 72, QUA-13).
   - The still-open part is the CI job that would run it. That is **QUA-08**, a Future requirement in `.planning/REQUIREMENTS.md`, deferred by the owner on 2026-09-13.
   - A future pickup must plan a side PR to `main` from the start, because GitHub runs `schedule` only on the default branch and `workflow_dispatch` only for a workflow file on it (ROADMAP v0.9.5 constraint 4). v0.9.3 did the same for `dependabot.yml`, in PR #137.

   Change no existing line and no frontmatter key, do not move the file, and file no new todo. The note must not claim any CI job exists.

2. Commit with `git commit -m "docs(72-03): status note on the linkcheck CI todo (D-07)"`.

3. **`## D-07 todo note`.** Record `git diff --numstat "$BASE_72_03" HEAD -- <todo>`, with 0 removed, the appended text verbatim, and `git ls-files .planning/todos/pending .planning/todos/completed | wc -l` before and after. That count must be unchanged by this task. Key `D07_NOTE = appended`. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-DOC24-EVIDENCE.md" &amp;&amp; T=.planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_72_03)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; test -f "$T" &amp;&amp; test ! -e .planning/todos/completed/2026-07-22-add-sphinx-linkcheck-ci-job.md &amp;&amp; [ "$(git diff --numstat "$B" HEAD -- "$T" | cut -f2)" = 0 ] &amp;&amp; N="$(git show "$B:$T" | wc -l)" &amp;&amp; [ "$(head -n "$N" "$T" | git hash-object --stdin)" = "$(git rev-parse "$B:$T")" ] &amp;&amp; NOTE="$(sed -n '/^## Status note (Phase 72, v0.9.5)$/,$p' "$T")" &amp;&amp; [ -n "$NOTE" ] &amp;&amp; printf '%s\n' "$NOTE" | grep -qF '[testenv:linkcheck]' &amp;&amp; printf '%s\n' "$NOTE" | grep -qF 'QUA-08' &amp;&amp; printf '%s\n' "$NOTE" | grep -qF 'side PR' &amp;&amp; printf '%s\n' "$NOTE" | grep -qF 'main' &amp;&amp; [ -z "$(git diff --name-only --diff-filter=A "$B" HEAD -- .planning/todos)" ] &amp;&amp; [ "$(k D07_NOTE)" = appended ] &amp;&amp; grep -qxF '## D-07 todo note' "$F" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- the todo missing from todos/pending, or present in todos/completed
- any removed line in its diff, or the base file not a byte-identical prefix of the new one
- the status-note heading absent, or the note lacking `[testenv:linkcheck]`, `QUA-08`, `side PR` or `main`
- a new file added under .planning/todos
- `D07_NOTE` not `appended`, the section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - The todo is still in `todos/pending/`, its base content is a byte-identical prefix of the new content, and it ends with `## Status note (Phase 72, v0.9.5)` naming `[testenv:linkcheck]`, QUA-08 and the side PR to `main`.
    - No todo file was added or moved by this plan.
  </acceptance_criteria>
  <done>The linkcheck todo records what landed and what stays open, written after the environment exists.</done>
</task>

</tasks>

<flagged_assumptions>
Accounting lives in `72-01-PLAN.md`. This plan authors the four DOC-24 edges (adjacency, empty,
encoding and ordering) and carries prohibition P4. The supplementary grep is the planner's addition
under milestone invariant #4. It adds discovery only, and any surface it finds is edited under the
same D-05 rule.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs surfaces → contributors | a listed command is taken as true; a false claim misleads every reader |
| CLAUDE.md → agent sessions | agents follow CLAUDE.md, including its two sync-hazard passages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-10 | Repudiation | a surface naming an environment that does not exist, or implying CI runs it | medium | mitigate | Task 1 halts unless `tox.ini` at the base carries the section; the comment states "not run by plain tox"; no prose is added (numstat 1 and 0) |
| T-72-11 | Tampering | an edit disturbing CLAUDE.md's `@preview` or `tox-uv` sync-hazard lines | medium | mitigate | CLAUDE.md numstat is exactly 1 added and 0 removed, and the env_list comment line is asserted byte-identical |
| T-72-12 | Repudiation | a listing surface missed because discovery trusted the roadmap's three | low | mitigate | fresh repo-wide grep plus a supplementary grep, a disposition row per hit, and post-edit line count equal to the surfaces edited |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The phase-wide list is in `72-01-PLAN.md`. This plan produces:
- one added line in each of `CLAUDE.md`, `README.md` and `docs/source/contributing.rst`
- the appended status note in `.planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md`
- `72-DOC24-EVIDENCE.md`, with keys `BASE_72_03`, `SCRATCH_72_03`, `DOC24_GREP_HITS`,
  `CLAUDE_COMMENT_COLUMN`, `README_COMMENT_COLUMN`, `CONTRIBUTING_COMMENT_COLUMN`,
  `DOC24_LISTING_SURFACES`, `DOC24_LINKCHECK_LINES`, `DOC24_VERDICT` and `D07_NOTE`
</artifacts_this_phase_produces>

<verification>
- The fresh `tox -e docs-pdf` grep is recorded, and each hit is either edited or dispositioned.
- Each listing surface gains exactly one aligned, ASCII line, last in its block.
- The linkcheck todo carries D-07's note and stays in `todos/pending/`.
</verification>

<success_criteria>
ROADMAP SC#2: every surface listing the tox environments names `tox -e linkcheck` in its neighbours'
form, found by grep rather than taken from a list, with every unedited hit's reason recorded.
</success_criteria>

<output>
Create `.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-03-SUMMARY.md` when done.
Its first section states `DOC24_GREP_HITS`, `DOC24_LISTING_SURFACES`, `DOC24_VERDICT` and
`D07_NOTE`. Do not edit REQUIREMENTS.md, ROADMAP.md or STATE.md.
</output>
