---
phase: 73-v0-9-5-close-prep-prep-only-unpublished
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CHANGELOG-EVIDENCE.md
autonomous: true
requirements: [REL-14]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-01: the `## [Unreleased]` region of CHANGELOG.md reads `### Added`, `### Changed`, `### Fixed`, `### Planned for Future Releases` in that order, the house order of the released `[0.9.0]` and `[0.8.0]` sections. `### Added` and `### Fixed` each hold exactly one bold-lead bullet, and the region holds six bold-lead bullets in total.'
    - 'D-02: each new bullet opens with a bold lead phrase that carries a verb and ends with its requirement IDs in trailing parentheses (`(QUA-13, DOC-24).**` and `(DOC-18).**`), carries the sentence "This has no effect on installing or using typsphinx." (the phrase occurs six times in the joined region), names no version string of an unpublished milestone (0.9.3, 0.9.4 or 0.9.5, with or without a leading v; whole-file count 0), and says nothing about the Japanese site or its catalogs.'
    - 'D-03: the `### Added` bullet names `tox -e linkcheck`, says it checks links including anchor targets, says it needs the network and is not part of "a plain `tox` run", and calls itself contributor tooling. It contains no word naming a CI job, workflow, schedule, weekly run, job or required check, no QUA-08, no numeral outside its requirement IDs, and no URL.'
    - 'D-04: the `### Fixed` bullet says that in the HTML documentation each User Guide and Examples page now appears once in the sidebar, nested under its section. It contains no page path, no numeral outside its requirement ID, no URL, and no mention of Read the Docs or of the `stable` or `latest` versions, so it cannot claim the fix is visible on the default documentation.'
    - 'D-05: the edit is pure addition. The diff from BASE_73_01 has exactly two hunks, zero deleted lines and exactly four added blank lines, and its added non-blank lines are exactly the two new headings and the two new bullets. The `### Changed` block (heading through its trailing blank line) and the `### Planned for Future Releases` block each hash to the same SHA-256 as at the base; the `## [` heading count, the link-reference count and the final `[Unreleased]: …/compare/v0.9.2...HEAD` line are unchanged; there is no `### Verified` subsection and no lead paragraph; scripts/extract_changelog_section.py is not run; CHANGELOG.md is the only product-tree file this plan changes.'
    - 'D-14: clean docs baselines are taken on the pre-edit tree at the phase base, each build preceded by `rm -rf docs/_build` and run under `LANG=C LC_ALL=C`: DOCS_HTML_WARN_BASE and DOCS_PDF_WARN_BASE, with zero `document is referenced in multiple toctrees` lines in both logs. Phase 72''s recorded pre-fix BASE_MULTI_TOCTREE_COUNT (at least 1) is the positive control that the same C-locale grep finds the message when it is present. The post-edit clean builds report the same two counts with identical WARNING lines and again zero multiple-toctrees lines, and the PDF begins with %PDF-.'
    - 'D-14: `tests/test_changelog_page_gate.py` runs on the edited tree in this worktree, provisioned with the docs extra, with 0 skipped and 0 failed.'
    - 'Edge (adjacency): `### Added` follows the blank line after `## [Unreleased]` and precedes `### Changed`; `### Fixed` follows the `### Changed` block''s trailing blank line and precedes `### Planned for Future Releases`. Each new subsection body is exactly a blank line, one bullet, and a blank line, so MyST renders two new list items without merging text into a neighbouring bullet or heading, and the `### Changed` block it touches on both sides is byte-identical.'
    - 'Edge (ordering): the subsection order follows Keep a Changelog (Added, Changed, Fixed), and the four `### Changed` bullets keep their order, which the Changed-block SHA-256 equality implies.'
    - 'Edge (encoding): equality of the untouched regions is byte identity by SHA-256 of the extracted text, not visual similarity.'
    - 'REL-14 is cited for coverage only; this plan SUMMARY declares requirements-completed: [] and this plan never writes .planning/REQUIREMENTS.md (D-08).'
  artifacts:
    - path: CHANGELOG.md
      provides: 'the `### Added` and `### Fixed` subsections under the existing `## [Unreleased]` heading'
      contains: '(QUA-13, DOC-24).**'
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CHANGELOG-EVIDENCE.md
      provides: 'plan base, pre-edit measurements, clean-build docs baselines with the multiple-toctrees census and its positive control, the two bullets and their accuracy basis, pure-addition proof, fence and content assertions, post-edit docs counts, the changelog page gate'
      contains: 'DOCS_HTML_WARN_BASE = '
  key_links:
    - from: CHANGELOG.md
      to: docs/source/changelog.rst
      via: 'the include directive with :parser: myst_parser.sphinx_ makes both bullets published text once REL-14 merges; a malformed bullet shows as a new docs warning'
      pattern: 'myst_parser.sphinx_'
    - from: '72-BASE-EVIDENCE.md BASE_MULTI_TOCTREE_COUNT'
      to: 'the zero multiple-toctrees readings at this phase base and tip'
      via: 'read with sed and quoted as the positive control for the LC_ALL=C grep'
      pattern: 'BASE_MULTI_TOCTREE_COUNT = '
    - from: '73-CHANGELOG-EVIDENCE.md DOCS_HTML_WARN_BASE and DOCS_PDF_WARN_BASE'
      to: '73-03 final-tree docs builds'
      via: 'KEY = value lines read with sed by plan 73-03 one wave later'
      pattern: 'DOCS_PDF_WARN_BASE = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action: no tag, PyPI upload, GitHub Release, pull request, merge, release.yml or update-pin.yml run, PR comment, push, or action on any Dependabot pull request. The phase single push and single CI dispatch belong to 73-04.'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT publish a reference to a version the project has not released, MUST NOT describe the Japanese site, MUST NOT claim or foreshadow a CI job, scheduled run or required check, and MUST NOT claim the sidebar fix is visible on the default stable documentation (D-02, D-03, D-04). CHANGELOG.md is included wholesale into the published docs.'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT overstate: no link count, no build or warning count, no page-path enumeration, and no claim that is not traced in the evidence file to a named Phase 72 evidence section (D-03, D-04).'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT change any product-tree file other than CHANGELOG.md (no edit under typsphinx/, tests/, docs/, .github/, pyproject.toml, uv.lock or tox.ini), MUST NOT write .planning/REQUIREMENTS.md, and MUST NOT merge origin/main into the branch (D-05, D-08).'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Add this milestone's two CHANGELOG bullets under the existing `## [Unreleased]` heading, each in a new
subsection that follows the house order of the released sections (D-01): `### Added` for the new
`tox -e linkcheck` environment (QUA-13, DOC-24) and `### Fixed` for the docs sidebar fix (DOC-18).
Prove with clean documentation builds, taken on the phase base before the edit and again after it,
that the new prose renders through MyST without adding a warning and that the `multiple toctrees`
class stays at zero (D-14).

Purpose: this is the phase's only product-tree change and the only text a reader of the published
changelog page sees from v0.9.5. Nothing is released, so both bullets join the four bullets v0.9.3 and
v0.9.4 left under `## [Unreleased]`, and the next release-prep phase promotes all six (D-11). The
bullets follow the register of the existing four (D-02), say exactly what D-03 and D-04 allow, and
everything else in the file stays byte-identical (D-05).

Output: the edited `CHANGELOG.md`, and `73-CHANGELOG-EVIDENCE.md` recording the plan base, the
pre-edit measurements, both clean-build docs baselines with the multiple-toctrees census and its
positive control, the accuracy basis of each claim, the pure-addition proof, the fence and content
assertions, the post-edit docs counts and the changelog page gate.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-RESEARCH.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution").
Before any command, provision this worktree's own environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`

Then run every project tool via `uv run`. `--python 3.13.13` is this plan's decision: every Phase 72
measurement ran on that interpreter (`PYVENV_VERSION_INFO = 3.13.13` in `72-BASE-EVIDENCE.md`), and a
fresh worktree's default uv-managed CPython 3.14 cannot import Pillow inside the FHS sandbox
(`libz.so.1`). The `docs` extra carries `myst-parser`, `furo` and `sphinx-autodoc-typehints`; without
it the changelog page gate skips silently.

**Evidence key lines.** Every `KEY = value` line carries the bare value only. Put any explanation on
the next line, never after the value, because the verify commands read values with
`sed -n "s/^KEY = //p"` and compare them exactly. Phase 69's executor once wrote keys with a trailing
explanation although its plan said not to, and Phase 71's executor caught the same slip only by
running the verify; the verify below fails on any key line that contains an opening parenthesis.

**Scratch files.** Parallel executors share the session scratchpad. Put every helper file and
captured output under your own `mktemp -d` directory, recorded as `SCRATCH_73_01`, and give every
file a `p7301_` prefix. The verify reads the build logs back from that directory by the exact names
this plan gives them.

**Locale.** The host runs `LANG=ja_JP.UTF-8`. Run every counted docs build as
`LANG=C LC_ALL=C uv run tox -e …`, so the `build succeeded` and `WARNING` lines are English on both
sides of every comparison, and grep them only under `LANG=C LC_ALL=C`.

**Long runs stay in the foreground.** Every tox and pytest invocation runs in a Bash call with the
`timeout` parameter set to 600000 and never with `run_in_background`. An executor that backgrounds a
long run returns early with no SUMMARY.

**No commits of tracking files.** Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or
`.planning/STATE.md`, and do not call any GSD state-writing verb or the GSD commit helper; commit with
plain `git commit`.
</worktree_provisioning>

<!-- planner-discipline-allow: 0\.9\.[345] -->
<!-- planner-discipline-allow: japanese|catalog|https?: -->
<!-- planner-discipline-allow: ja -->
<!-- planner-discipline-allow: CI|workflows?|scheduled?|weekly|jobs?|QUA-08 -->
<!-- planner-discipline-allow: required check|github actions -->
<!-- planner-discipline-allow: required check -->
<!-- planner-discipline-allow: user_guide/|examples/|\.rst|\.html -->
<!-- planner-discipline-allow: stable|latest -->
<!-- planner-discipline-allow: read ?the ?docs|readthedocs -->
<!-- planner-discipline-allow: [0-9]+ (skipped|failed) -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-region-allow: 0\.9\.[345] -->
<!-- planner-region-allow: japanese|catalog|https?: -->
<!-- planner-region-allow: stable|latest -->
The version pattern, the Japanese-site words, the CI vocabulary, the documentation-host words and
the page-path fragments are named because D-02, D-03 and D-04 forbid them in the bullets and the
executor must know what to avoid. `## HALT` is a heading written only on a failure path. The gates
require the absence of all of them inside the new bullets (the version pattern: anywhere in the file).

## Planning-time census (2026-09-14, main checkout at `9d276727`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `## [Unreleased]` region | heading `CHANGELOG.md:8`, blank `:9`, `### Changed` `:10`, four bold bullets starting `:12` (tox-uv), `:18` (Dependabot), `:28` (`flake.nix`), `:37` (builtin generics, last line `:45`), blank `:46`, `### Planned for Future Releases` `:47` | `grep -n`, `sed -n 1,50p` |
| Structural counts | 23 `^## \[` headings, 23 link-reference lines, 1334 lines; the last line is `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.2...HEAD` | `grep -cE`, `tail -n 1`, `wc -l` |
| Unpublished version strings | 0 hits for the dotted 0.9.3/0.9.4/0.9.5 pattern in CHANGELOG.md | `grep -cE` |
| Released-section order | `### Added` → `### Changed` → `### Fixed` (then `### Removed`, `### Verified`), e.g. `[0.9.0]` at `:124/:137/:175` | 73-CONTEXT `<domain>` |
| Docs environments | `tox.ini` `docs-html` runs `sphinx-build -b html source _build/html`, `docs-pdf` runs `sphinx-build -b typstpdf source _build/pdf`, `linkcheck` runs `sphinx-build -b linkcheck source _build/linkcheck`; all `changedir = docs`, `extras = docs`, `runner = uv-venv-lock-runner`; the PDF is `docs/_build/pdf/typsphinx.pdf` | `tox.ini:67-97` |
| Phase 72 docs figures | `BASE_MULTI_TOCTREE_COUNT = 5` (pre-fix positive control), `EDIT_MULTI_TOCTREE_COUNT = 0`, `BASE_WARNING_COUNT = 3` = `EDIT_WARNING_COUNT = 3` (HTML, CPython 3.13.13, Sphinx 9.1.0); a cross-check only, never this plan's baseline | `72-BASE-EVIDENCE.md` |
| Publication | `docs/source/changelog.rst:1-2` includes `../../CHANGELOG.md` with `:parser: myst_parser.sphinx_` | `head -n 3` |
| Changelog page gate | 6 tests; `TestChangelogPageContentCoverage` and `TestChangelogIncludeCompilesToPdf` skip when `myst_parser` is absent | `tests/test_changelog_page_gate.py:109-219` |
| House-style defect to avoid | a bold lead with no verb (Phase 69 review WR-01) | `71-CHANGELOG-EVIDENCE.md` § "Discretion exercised" |

Extractors used throughout (read them as shell one-liners; the verify uses the same ones):
- the region between `## [Unreleased]` and `## [0.9.2]`: `awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md`
- one new subsection's body, joined into one line: `awk '$0=="### Added"{f=1;next} f && /^### /{exit} f' CHANGELOG.md | tr '\n' ' ' | tr -s ' '` (and the same with `### Fixed`)
- the `### Changed` block, heading through its trailing blank line: `awk '/^### Changed$/{f=1;print;next} f && /^### /{exit} f'`
- the Planned block: `awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f'`
- a warning count from a build log: the number in the last C-locale `^build succeeded, N warnings.` line, or 0 for a bare `build succeeded.`
- a multiple-toctrees count from a build log: `LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees' <log>`

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: pre-edit measurements, clean-build docs baselines at the phase base with the multiple-toctrees census, then both subsections carried through MyST into a rendered page</name>
  <reversibility rating="reversible">A markdown addition under an existing heading; the next release-prep phase promotes it by editing a heading (D-11).</reversibility>
  <files>CHANGELOG.md, .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - CHANGELOG.md lines 1-60 (the file being modified: the `## [Unreleased]` block whose four bullets set the register) and lines 120-180 (the `[0.9.0]` section's `### Added` / `### Changed` / `### Fixed` order)
    - docs/source/changelog.rst lines 1-3 (the include that publishes this edit)
    - tox.ini lines 67-97 (the `docs-html`, `docs-pdf` and `linkcheck` environments)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md (`<decisions>` D-01 to D-05, D-11 and D-14; `<specifics>` for the draft bullet shapes)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PATTERNS.md § "`CHANGELOG.md` (config/documentation, CRUD pure-addition)" and § "`73-CHANGELOG-EVIDENCE.md`"
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md (the `BASE_MULTI_TOCTREE_COUNT` key line and § "Base HTML build")
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-TOCTREE-EVIDENCE.md § "Sidebar", § "Typst pair" and § "SC#3 verdict" (the sidebar claims)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-LINKCHECK-EVIDENCE.md § "tox.ini diff" and § "Run 1", and 72-DOC24-EVIDENCE.md § "Surfaces" (the linkcheck claims)
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CHANGELOG-EVIDENCE.md (the evidence-file structure to mirror, including § "What this file is NOT" and § "Discretion exercised")
  </read_first>
  <action>
Create `73-CHANGELOG-EVIDENCE.md` with the heading `# Phase 73 — CHANGELOG Evidence (D-01..D-05, D-14, SC#2)`.

1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `command -v uv`, then `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line from `<worktree_provisioning>`, with the tail of its output
   - before any commit: `BASE_73_01 = ` followed by the output of `git rev-parse HEAD`
   - `PYVENV_HOME_73_01 = ` and `PYVENV_VERSION_73_01 = `, the `home` and `version_info` values of `.venv/pyvenv.cfg`; the version must be `3.13.13`
   - `SCRATCH_73_01 = ` followed by the path `mktemp -d` printed

2. **`## Pre-edit measurements`.** Record each command with its output, then its key line:
   - `grep -cE '^## \[' CHANGELOG.md`, key `HEADINGS_BEFORE`
   - `grep -cE '^\[[^]]+\]: https' CHANGELOG.md`, key `LINKREFS_BEFORE`
   - the region extractor piped to `grep -cE '^- \*\*'`, key `UNRELEASED_BOLD_BEFORE`, which must be 4
   - the whole-file count of the unpublished version strings (the dotted pattern the census row names), key `VERSION_LITERALS_BEFORE`, which must be 0
   - the `### Changed` block extractor over `CHANGELOG.md` piped to `sha256sum`, key `CHANGED_SHA_BEFORE`, the digest field only
   - the Planned block extractor piped to `sha256sum`, key `PLANNED_SHA_BEFORE`, the digest field only
   - the verbatim `tail -n 1 CHANGELOG.md`, in a fenced block rather than a key line

   Add a short `## What this file is NOT` section. It states that `scripts/extract_changelog_section.py` is not run, because no versioned section exists for it to extract, and that this phase writes no `### Verified` subsection, no lead paragraph under `## [Unreleased]`, no versioned heading and no tail link (D-05). The next release-prep phase promotes all six bullets into its versioned section (D-11).

3. **`## Docs baseline — pre-edit, clean builds`** (D-14). Take both baselines before touching `CHANGELOG.md`, at the phase base.
   - Run `rm -rf docs/_build`, then `LANG=C LC_ALL=C uv run tox -e docs-html > "$SCRATCH_73_01/p7301_html-base.log" 2>&1; echo "exit:$?"`, in the foreground. The exit must be 0.
   - Quote from that log, verbatim: the `Running Sphinx v…` line, the final `build succeeded` line, and every line containing `WARNING`, in fenced blocks.
   - Write `DOCS_HTML_WARN_BASE = ` with the number from `build succeeded, N warnings.`; if Sphinx prints `build succeeded.` with no count, the value is 0.
   - Write `MULTI_TOCTREE_HTML_BASE = ` with the C-locale multiple-toctrees count of that log. It must be 0: DOC-18 landed in Phase 72, so the phase base already carries the fix.
   - Repeat for `docs-pdf`, again with `rm -rf docs/_build` immediately before it, logging to `$SCRATCH_73_01/p7301_pdf-base.log`. Write `DOCS_PDF_WARN_BASE = ` and `MULTI_TOCTREE_PDF_BASE = ` (must be 0). Copy the PDF to `$SCRATCH_73_01/p7301_base.pdf`, and record `head -c 5` of it, which must print `%PDF-`, as `PDF_MAGIC_BASE = `.
   - **Positive control.** Quote the `BASE_MULTI_TOCTREE_COUNT` line from `.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md` with `grep -n`, and write its value as `MULTI_TOCTREE_POSITIVE_CONTROL_72 = `. Explain in two sentences: the same `LANG=C LC_ALL=C grep -c` found that many messages on Phase 72's pre-fix base, so a zero here is a real absence, not a localised or broken grep; and the English `build succeeded` line found by the same C-locale grep in this log proves the console text is English.
   - If either base multiple-toctrees count is not 0, write `## HALT: multiple toctrees at the phase base` with the lines quoted, and stop: DOC-18 has regressed, which is the owner's decision.

   An incremental rebuild under-reports warnings, which is why each counted build starts from an empty `docs/_build`.

4. **`## The two subsections (tracer slice)`.** Edit `CHANGELOG.md` as two pure insertions (D-01, D-05):
   - **Insertion 1:** after the blank line that follows `## [Unreleased]`, and before `### Changed`, insert the line `### Added`, one blank line, the linkcheck bullet, and one blank line.
   - **Insertion 2:** after the blank line that ends the last `### Changed` bullet, and before `### Planned for Future Releases`, insert the line `### Fixed`, one blank line, the sidebar bullet, and one blank line.
   - Nothing else in the file changes: not the four `### Changed` bullets, not their heading, not the Planned block, not any versioned section, not the tail link block (D-05).
   - Both bullets share the register of the four `### Changed` bullets (D-02): `- **` + a plain-language lead sentence that carries a verb + a space + the requirement IDs in parentheses + `.**`, then prose. Continuation lines are indented two spaces and wrap near 100 columns. A verbless bold lead is the house-style defect the census row names. Each bullet carries the sentence `This has no effect on installing or using typsphinx.` and at most one evidence sentence, and is written for a reader of the published changelog page, not in internal shorthand.
   - **The `### Added` bullet (D-03).** Bold span ends with `(QUA-13, DOC-24).**`. A lead such as "A `tox -e linkcheck` environment checks the documentation's external links and anchors" fits. The prose must make three points: `tox -e linkcheck` runs Sphinx's link check over the documentation sources, including `#anchor` targets (the word `anchor` must appear); it needs the network and is not part of a plain `tox` run (write that literal phrase, with `tox` in backticks: "a plain `tox` run"); and it is contributor tooling (the word `contributor` must appear; it may say the environment is listed beside `docs-html` and `docs-pdf` in the contributor notes).
   - **The `### Fixed` bullet (D-04).** Bold span ends with `(DOC-18).**`. A lead such as "The HTML documentation's sidebar lists each User Guide and Examples page once" fits. The prose says that each User Guide and Examples page now appears once in the sidebar, nested under its section, instead of also being listed a second time beside it. The strings `sidebar`, `User Guide`, `Examples` and the word `once` must appear. Its one optional evidence sentence may say either or both of: Sphinx no longer reports those pages as referenced in multiple toctrees; the PDF still includes each page exactly once.
   - **Leave out of both bullets:**
     - any version number, with or without a leading `v`, because no release carries one (D-02)
     - any mention of the Japanese site, its translations or catalogs (D-02)
     - any digit outside the requirement IDs: no link count, no page count, no warning count (D-03, D-04; constraint 8)
     - any URL (so the new text adds nothing for `tox -e linkcheck` to check)
   - **Leave out of the `### Added` bullet** anything that names, implies or foreshadows a CI job, a GitHub Actions workflow, a schedule or weekly run, or a required check, and the ID of the deferred CI requirement (D-03; REL-14 was revised when that requirement was deferred).
   - **Leave out of the `### Fixed` bullet** the five pages' paths or file names, and any mention of Read the Docs or of its `stable` or `latest` versions: the default `stable` documentation stays on `v0.9.2` until the next published release, and the bullet must not claim otherwise (D-04; constraint 5).

   Add `## Accuracy basis`, a table mapping every factual claim in each bullet to the Phase 72 evidence file and section that supports it:
   - the environment exists, runs the linkcheck builder over `docs/source` and is not in `env_list`: `72-LINKCHECK-EVIDENCE.md` § "tox.ini diff"
   - it checks anchors and needs the network: `72-LINKCHECK-EVIDENCE.md` § "Run 1" and the `linkcheck` environment's own description in `tox.ini`
   - it is listed beside `docs-html` and `docs-pdf` in the contributor notes: `72-DOC24-EVIDENCE.md` § "Surfaces"
   - each User Guide and Examples page appears once in the sidebar, under its section: `72-TOCTREE-EVIDENCE.md` § "Sidebar"
   - no multiple-toctrees report: `72-TOCTREE-EVIDENCE.md` § "SC#3 verdict"
   - each page still reaches the PDF exactly once: `72-TOCTREE-EVIDENCE.md` § "Typst pair"

5. **`## Docs render — tracer slice`.** Run `rm -rf docs/_build`, then `LANG=C LC_ALL=C uv run tox -e docs-html > "$SCRATCH_73_01/p7301_html-tracer.log" 2>&1; echo "exit:$?"` in the foreground. Paste the verbatim `build succeeded` line and all `WARNING` lines, and write `DOCS_HTML_WARN_TRACER = ` and `MULTI_TOCTREE_HTML_TRACER = ` (must be 0).
   - `DOCS_HTML_WARN_TRACER` must equal `DOCS_HTML_WARN_BASE`, and the set of `WARNING` lines must match the baseline's.
   - If a new warning names the changelog page, the MyST in a bullet is malformed. Fix that bullet and re-run from the `rm -rf`.
   - If a new warning names any other page, write `## HALT: docs-html warning drift not caused by CHANGELOG.md` with the warning quoted, and stop. Never move the baseline.

6. Commit `CHANGELOG.md` and the evidence file with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; F="$D/73-CHANGELOG-EVIDENCE.md" &amp;&amp; P72=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = 3.13.13 ] &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; n() { l="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$1" | tail -n 1)"; [ -n "$l" ] || return 1; v="$(printf '%s\n' "$l" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')"; if [ -z "$v" ]; then [ "$l" = 'build succeeded.' ] || return 1; v=0; fi; printf '%s' "$v"; } &amp;&amp; m() { LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees' "$1" || true; } &amp;&amp; B="$(k BASE_73_01)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; BC="$(git show "$B:CHANGELOG.md")" &amp;&amp; [ -n "$BC" ] &amp;&amp; R="$(awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^### ' | tr '\n' '|')" = '### Added|### Changed|### Fixed|### Planned for Future Releases|' ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^- \*\*' | wc -l)" = 6 ] &amp;&amp; JA="$(awk '$0=="### Added"{f=1;next} f &amp;&amp; /^### /{exit} f' CHANGELOG.md | tr '\n' ' ' | tr -s ' ')" &amp;&amp; JF="$(awk '$0=="### Fixed"{f=1;next} f &amp;&amp; /^### /{exit} f' CHANGELOG.md | tr '\n' ' ' | tr -s ' ')" &amp;&amp; printf '%s\n' "$JA" | grep -qF '(QUA-13, DOC-24).**' &amp;&amp; printf '%s\n' "$JF" | grep -qF '(DOC-18).**' &amp;&amp; [ "$(grep -cE '0\.9\.[345]' CHANGELOG.md)" = 0 ] &amp;&amp; NS="$(git diff --numstat "$B" -- CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$NS" | cut -f2)" = 0 ] &amp;&amp; cb() { awk '/^### Changed$/{f=1;print;next} f &amp;&amp; /^### /{exit} f'; } &amp;&amp; pb() { awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f'; } &amp;&amp; [ "$(cb &lt; CHANGELOG.md | sha256sum)" = "$(printf '%s\n' "$BC" | cb | sha256sum)" ] &amp;&amp; [ "$(pb &lt; CHANGELOG.md | sha256sum)" = "$(printf '%s\n' "$BC" | pb | sha256sum)" ] &amp;&amp; [ "$(tail -n 1 CHANGELOG.md)" = "$(printf '%s\n' "$BC" | tail -n 1)" ] &amp;&amp; S="$(k SCRATCH_73_01)" &amp;&amp; [ -n "$S" ] &amp;&amp; for x in html-base pdf-base html-tracer; do test -s "$S/p7301_$x.log" || { echo "missing log: $x"; exit 1; }; [ "$(m "$S/p7301_$x.log")" = 0 ] || { echo "multiple toctrees in: $x"; exit 1; }; done &amp;&amp; HB="$(k DOCS_HTML_WARN_BASE)" &amp;&amp; PW="$(k DOCS_PDF_WARN_BASE)" &amp;&amp; [ -n "$HB" ] &amp;&amp; [ -n "$PW" ] &amp;&amp; [ "$(n "$S/p7301_html-base.log")" = "$HB" ] &amp;&amp; [ "$(n "$S/p7301_pdf-base.log")" = "$PW" ] &amp;&amp; [ "$(n "$S/p7301_html-tracer.log")" = "$HB" ] &amp;&amp; [ "$(k DOCS_HTML_WARN_TRACER)" = "$HB" ] &amp;&amp; for x in MULTI_TOCTREE_HTML_BASE MULTI_TOCTREE_PDF_BASE MULTI_TOCTREE_HTML_TRACER; do [ "$(k "$x")" = 0 ] || { echo "key not 0: $x"; exit 1; }; done &amp;&amp; PC="$(k BASE_MULTI_TOCTREE_COUNT "$P72/72-BASE-EVIDENCE.md")" &amp;&amp; [ "$PC" -ge 1 ] &amp;&amp; [ "$(k MULTI_TOCTREE_POSITIVE_CONTROL_72)" = "$PC" ] &amp;&amp; [ "$(head -c 5 "$S/p7301_base.pdf")" = '%PDF-' ] &amp;&amp; [ "$(git diff --name-only "$B" -- . ':(exclude).planning')" = CHANGELOG.md ] &amp;&amp; [ -z "$(git diff --name-only "$B" -- .planning/REQUIREMENTS.md)" ] &amp;&amp; for s in '## Head check and provisioning' '## Pre-edit measurements' '## What this file is NOT' '## Docs baseline — pre-edit, clean builds' '## The two subsections (tracer slice)' '## Accuracy basis' '## Docs render — tracer slice'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing log: NAME`, `multiple toctrees in: NAME`, `key not 0: KEY` or `missing section: NAME` printed. Each of these makes it fail:
- not in a worktree, the uv shim not on PATH, or `.venv` not on 3.13.13
- `BASE_73_01` missing or not an ancestor of HEAD
- the region's `###` headings other than Added, Changed, Fixed, Planned in that order, or a bold-bullet count other than 6
- the `### Added` bullet lacking `(QUA-13, DOC-24).**` or the `### Fixed` bullet lacking `(DOC-18).**`
- any unpublished version string in CHANGELOG.md
- a deleted line in the CHANGELOG.md diff from the base, or no diff at all
- the `### Changed` block digest, the Planned block digest or the final line differing from the base
- a base or tracer build log missing, or any of them carrying a multiple-toctrees line
- either baseline key empty, a log's `build succeeded` count differing from its key, no `build succeeded` line, or the tracer count differing from `DOCS_HTML_WARN_BASE`
- Phase 72's positive-control count below 1, or its recorded copy differing
- the base PDF copy not starting with `%PDF-`
- a product-tree file other than CHANGELOG.md changed, or REQUIREMENTS.md changed
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `73-CHANGELOG-EVIDENCE.md` holds `BASE_73_01` (40 hex), `PYVENV_VERSION_73_01 = 3.13.13`, `SCRATCH_73_01`, and the pre-edit keys `HEADINGS_BEFORE`, `LINKREFS_BEFORE`, `UNRELEASED_BOLD_BEFORE = 4`, `VERSION_LITERALS_BEFORE = 0`, `CHANGED_SHA_BEFORE` and `PLANNED_SHA_BEFORE`, plus the verbatim final line in a fenced block.
    - `DOCS_HTML_WARN_BASE` and `DOCS_PDF_WARN_BASE` each come from a build preceded by `rm -rf docs/_build` on the pre-edit tree, run under `LANG=C LC_ALL=C`, logged to `p7301_html-base.log` and `p7301_pdf-base.log`, with the verbatim `build succeeded` line and the `WARNING` lines quoted; `MULTI_TOCTREE_HTML_BASE = 0`, `MULTI_TOCTREE_PDF_BASE = 0`, `PDF_MAGIC_BASE = %PDF-`.
    - `MULTI_TOCTREE_POSITIVE_CONTROL_72` equals the live `BASE_MULTI_TOCTREE_COUNT` of `72-BASE-EVIDENCE.md`, with the two-sentence explanation.
    - The region's `###` headings are exactly `### Added`, `### Changed`, `### Fixed`, `### Planned for Future Releases`, with six bold bullets; the Added bullet's bold span ends `(QUA-13, DOC-24).**` and the Fixed bullet's ends `(DOC-18).**`.
    - `## Accuracy basis` maps every claim in both bullets to a named Phase 72 evidence file and section.
    - `DOCS_HTML_WARN_TRACER` equals `DOCS_HTML_WARN_BASE` and `MULTI_TOCTREE_HTML_TRACER = 0`, from a clean post-edit build.
  </acceptance_criteria>
  <done>Both docs baselines are taken from clean builds of the phase base with zero multiple-toctrees lines against a cited positive control, and both new subsections render through MyST into the changelog page with the warning count unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: The pure-addition proof, the fence and content assertions for D-01..D-05, both docs environments rebuilt clean against the baseline, and the changelog page gate</name>
  <reversibility rating="reversible">Evidence appends and, only if an assertion fails, a correction to one of the two added bullets; no version literal, tag or published artifact moves.</reversibility>
  <files>CHANGELOG.md, .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - CHANGELOG.md (the state after Task 1)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CHANGELOG-EVIDENCE.md (Task 1's keys and sections)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-01 to D-05 and § "Claude's Discretion"
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CHANGELOG-EVIDENCE.md § "Pure-addition proof", § "Fence assertions" and § "Bullet content assertions" (the shape to mirror)
    - tests/test_changelog_page_gate.py lines 100-230 (what the gate asserts and when it skips)
  </read_first>
  <action>
1. Re-run the head check and the provisioning line.

2. **`## Pure-addition proof`** (D-05). Paste the verbatim `git diff "$BASE_73_01" -- CHANGELOG.md`, then record:
   - `ADDED_LINES = ` and `REMOVED_LINES = `, from `git diff --numstat "$BASE_73_01" -- CHANGELOG.md`; the removed count must be 0
   - `DIFF_HUNKS = `, the number of `@@` lines in `git diff -U0 "$BASE_73_01" -- CHANGELOG.md`, which must be 2: one insertion before `### Changed`, one before `### Planned for Future Releases`
   - `ADDED_BLANK_LINES = `, the number of added lines that are empty, which must be 4

   State that the added non-blank lines are exactly `### Added`, the linkcheck bullet, `### Fixed` and the sidebar bullet, so every other line of the file is unchanged byte for byte.

3. **`## Fence assertions`.** Record each as a command, its output, and a key line:
   - `HEADINGS_AFTER` and `LINKREFS_AFTER`, each equal to its `_BEFORE`
   - `UNRELEASED_BOLD_AFTER = 6`
   - `NO_EFFECT_PHRASES = 6`, counted case-insensitively over the region joined into one line
   - `VERSION_LITERALS_AFTER = 0`
   - `CHANGED_SHA_AFTER`, equal to `CHANGED_SHA_BEFORE`: the `### Changed` block, which now has a new sibling on each side, is untouched (D-05)
   - `PLANNED_SHA_AFTER`, equal to `PLANNED_SHA_BEFORE`

   Also record, as commands with their outputs:
   - the verbatim final line, identical to the base's, and the count of `compare/v0.9.2...HEAD` lines, which must be 1: the `[Unreleased]` compare base stays `v0.9.2` (D-05)
   - `sed -n 7p pyproject.toml`, which must be `version = "0.9.2"`
   - the region's `###` heading list in order (edge: ordering), and the lead-paragraph check `awk '/^### /{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md | grep -c .`, which must be 0 (D-05)
   - `git status --porcelain typsphinx/ tests/ docs/ .github/ pyproject.toml uv.lock tox.ini .planning/REQUIREMENTS.md`, which must be empty

4. **`## Bullet content assertions`.** Join each new bullet into one line with the census extractor, then record each check with its output and key:
   - `ADDED_ID_SPAN = present`, when the Added bullet contains the exact bold-span ending `(QUA-13, DOC-24).**` (D-02)
   - `FIXED_ID_SPAN = present`, when the Fixed bullet contains `(DOC-18).**` (D-02)
   - `ADDED_REQUIRED_POINTS = present`, when the Added bullet contains `tox -e linkcheck`, `anchor`, `network`, the phrase "a plain `tox` run" and `contributor` (D-03)
   - `ADDED_CI_WORDS = 0`, a case-insensitive whole-word count, in the Added bullet, of the CI vocabulary the census allow-list names, plus the phrases for a required check and for GitHub's workflow product (D-03)
   - `FIXED_REQUIRED_POINTS = present`, when the Fixed bullet contains `sidebar`, `User Guide`, `Examples` and the word `once` (D-04)
   - `FIXED_PATH_FRAGMENTS = 0`, a count of page-path fragments (a section directory followed by a slash, or an `.rst` or `.html` suffix) in the Fixed bullet (D-04)
   - `FIXED_HOST_WORDS = 0`, a case-insensitive count, in the Fixed bullet, of the documentation host's name and of its two version names (D-04)
   - `BULLET_NUMERALS = none`, when neither bullet contains a digit after its requirement IDs are removed (D-03, D-04)
   - `JAPANESE_SITE_WORDS = 0`, over both bullets: a case-insensitive count of the language name and of `catalog`, plus a whole-word count of the two-letter language code (D-02)
   - `BULLET_URLS = 0`, a count of URL schemes over both bullets
   - `ADJACENCY = OK`, when each new subsection body is exactly one blank line, one bullet and one blank line (edge: adjacency)

   Quote the first line of all six bullets in order: the linkcheck bullet, the four `### Changed` bullets (tox-uv, Dependabot, `flake.nix`, builtin generics), then the sidebar bullet.

   If any check fails, correct only the failing new bullet, re-run Task 1 step 5, and then re-run these checks. Never touch the four `### Changed` bullets.

5. **`## Discretion exercised`.** One line each:
   - both lead sentences chosen, and why each carries a verb
   - the requirement IDs chosen for each bullet's parentheses, and why REL-14 is not cited in a bullet
   - which of the two optional evidence points the Fixed bullet uses, or that it uses neither
   - that `### Verified` and a lead paragraph are deliberately absent (D-05)

6. **`## Docs render — post-edit, clean builds`.** Build each environment in the foreground, running `rm -rf docs/_build` immediately before each:
   - `LANG=C LC_ALL=C uv run tox -e docs-html > "$SCRATCH_73_01/p7301_html-post.log" 2>&1`, keys `DOCS_HTML_WARN_POST` and `MULTI_TOCTREE_HTML_POST` (must be 0)
   - `LANG=C LC_ALL=C uv run tox -e docs-pdf > "$SCRATCH_73_01/p7301_pdf-post.log" 2>&1`, keys `DOCS_PDF_WARN_POST` and `MULTI_TOCTREE_PDF_POST` (must be 0); copy the PDF to `$SCRATCH_73_01/p7301_post.pdf` and record `PDF_MAGIC_POST = %PDF-`

   Quote each `build succeeded` line and its `WARNING` lines. Both counts must equal their `_BASE`, and the sorted C-locale `WARNING` lines of each post log must equal those of its base log; record `WARNING_LINES_IDENTICAL = yes`. The halt rule of Task 1 step 5 applies.

7. **`## Changelog page gate`** (D-14). Run `LC_ALL=C uv run pytest tests/test_changelog_page_gate.py -v -rs -p no:cacheprovider` in the foreground and quote each test line and the summary. Keys: `CHANGELOG_GATE_PASSED_73_01` (the passed count, extracted with an unanchored `grep -oE '[0-9]+ passed'`, because the summary line is wrapped in `=` decoration) and `CHANGELOG_GATE_SKIPPED_73_01`, which must be 0. A skip means the docs extra is missing: fix the provisioning and re-run; never record a skip as a pass.

8. Commit the evidence file, and `CHANGELOG.md` if step 4 corrected a bullet, with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; F="$D/73-CHANGELOG-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; n() { l="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$1" | tail -n 1)"; [ -n "$l" ] || return 1; v="$(printf '%s\n' "$l" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')"; if [ -z "$v" ]; then [ "$l" = 'build succeeded.' ] || return 1; v=0; fi; printf '%s' "$v"; } &amp;&amp; m() { LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees' "$1" || true; } &amp;&amp; B="$(k BASE_73_01)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; BC="$(git show "$B:CHANGELOG.md")" &amp;&amp; [ -n "$BC" ] &amp;&amp; R="$(awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^### ' | tr '\n' '|')" = '### Added|### Changed|### Fixed|### Planned for Future Releases|' ] &amp;&amp; [ "$(awk '/^### /{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md | grep -c .)" = 0 ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^- \*\*' | wc -l)" = 6 ] &amp;&amp; [ "$(printf '%s\n' "$R" | tr '\n' ' ' | tr -s ' ' | grep -oi 'no effect on installing or using typsphinx' | wc -l)" = 6 ] &amp;&amp; ss() { awk -v h="$1" '$0==h{f=1;next} f &amp;&amp; /^### /{exit} f{c++; if(c==1)a=$0; z=$0; if($0=="")e++; if($0 ~ /^- \*\*/)b++} END{exit !(c &gt;= 3 &amp;&amp; a=="" &amp;&amp; z=="" &amp;&amp; e==2 &amp;&amp; b==1)}' CHANGELOG.md; } &amp;&amp; ss '### Added' &amp;&amp; ss '### Fixed' &amp;&amp; JA="$(awk '$0=="### Added"{f=1;next} f &amp;&amp; /^### /{exit} f' CHANGELOG.md | tr '\n' ' ' | tr -s ' ')" &amp;&amp; JF="$(awk '$0=="### Fixed"{f=1;next} f &amp;&amp; /^### /{exit} f' CHANGELOG.md | tr '\n' ' ' | tr -s ' ')" &amp;&amp; printf '%s\n' "$JA" | grep -qF '(QUA-13, DOC-24).**' &amp;&amp; printf '%s\n' "$JA" | grep -qF 'tox -e linkcheck' &amp;&amp; printf '%s\n' "$JA" | grep -qi 'anchor' &amp;&amp; printf '%s\n' "$JA" | grep -qi 'network' &amp;&amp; printf '%s\n' "$JA" | grep -qF 'a plain `tox` run' &amp;&amp; printf '%s\n' "$JA" | grep -qi 'contributor' &amp;&amp; printf '%s\n' "$JA" | grep -qiF 'no effect on installing or using typsphinx' &amp;&amp; [ "$(printf '%s\n' "$JA" | grep -oiwE 'CI|workflows?|scheduled?|weekly|jobs?|QUA-08' | wc -l)" = 0 ] &amp;&amp; ! printf '%s\n' "$JA" | grep -qiE 'required check|github actions' &amp;&amp; printf '%s\n' "$JF" | grep -qF '(DOC-18).**' &amp;&amp; printf '%s\n' "$JF" | grep -qi 'sidebar' &amp;&amp; printf '%s\n' "$JF" | grep -qF 'User Guide' &amp;&amp; printf '%s\n' "$JF" | grep -qF 'Examples' &amp;&amp; printf '%s\n' "$JF" | grep -qiw 'once' &amp;&amp; printf '%s\n' "$JF" | grep -qiF 'no effect on installing or using typsphinx' &amp;&amp; ! printf '%s\n' "$JF" | grep -qE 'user_guide/|examples/|\.rst|\.html' &amp;&amp; [ "$(printf '%s\n' "$JF" | grep -oiwE 'stable|latest' | wc -l)" = 0 ] &amp;&amp; ! printf '%s\n' "$JF" | grep -qiE 'read ?the ?docs|readthedocs' &amp;&amp; for t in "$JA" "$JF"; do [ -z "$(printf '%s\n' "$t" | sed -E 's/(QUA|DOC)-[0-9]+//g' | grep -oE '[0-9]+')" ] || { echo "numeral in a new bullet"; exit 1; }; ! printf '%s\n' "$t" | grep -qiE 'japanese|catalog|https?:' || { echo "forbidden word in a new bullet"; exit 1; }; [ "$(printf '%s\n' "$t" | grep -ciw 'ja')" = 0 ] || { echo "language code in a new bullet"; exit 1; }; done &amp;&amp; NS="$(git diff --numstat "$B" -- CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$NS" | cut -f2)" = 0 ] &amp;&amp; DU="$(git diff -U0 "$B" -- CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$DU" | grep '^@@' | wc -l)" = 2 ] &amp;&amp; [ "$(printf '%s\n' "$DU" | grep -xF '+' | wc -l)" = 4 ] &amp;&amp; [ "$(printf '%s\n' "$DU" | sed -n '/^+++ /d; s/^+//p' | grep -v '^$')" = "$(printf '%s\n' "$R" | awk '$0=="### Added" || $0=="### Fixed"{print; f=1; next} f &amp;&amp; /^### /{f=0} f &amp;&amp; $0!=""')" ] &amp;&amp; [ "$(k DIFF_HUNKS)" = 2 ] &amp;&amp; [ "$(k REMOVED_LINES)" = 0 ] &amp;&amp; [ "$(k ADDED_BLANK_LINES)" = 4 ] &amp;&amp; [ "$(grep -cE '^## \[' CHANGELOG.md)" = "$(printf '%s\n' "$BC" | grep -cE '^## \[')" ] &amp;&amp; [ "$(grep -cE '^\[[^]]+\]: https' CHANGELOG.md)" = "$(printf '%s\n' "$BC" | grep -cE '^\[[^]]+\]: https')" ] &amp;&amp; [ "$(tail -n 1 CHANGELOG.md)" = "$(printf '%s\n' "$BC" | tail -n 1)" ] &amp;&amp; [ "$(grep -cF 'compare/v0.9.2...HEAD' CHANGELOG.md)" = 1 ] &amp;&amp; [ "$(grep -cE '0\.9\.[345]' CHANGELOG.md)" = 0 ] &amp;&amp; cb() { awk '/^### Changed$/{f=1;print;next} f &amp;&amp; /^### /{exit} f'; } &amp;&amp; pb() { awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f'; } &amp;&amp; [ "$(cb &lt; CHANGELOG.md | sha256sum)" = "$(printf '%s\n' "$BC" | cb | sha256sum)" ] &amp;&amp; [ "$(pb &lt; CHANGELOG.md | sha256sum)" = "$(printf '%s\n' "$BC" | pb | sha256sum)" ] &amp;&amp; [ "$(k CHANGED_SHA_AFTER)" = "$(k CHANGED_SHA_BEFORE)" ] &amp;&amp; [ "$(k PLANNED_SHA_AFTER)" = "$(k PLANNED_SHA_BEFORE)" ] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; S="$(k SCRATCH_73_01)" &amp;&amp; [ -n "$S" ] &amp;&amp; HB="$(k DOCS_HTML_WARN_BASE)" &amp;&amp; PW="$(k DOCS_PDF_WARN_BASE)" &amp;&amp; [ -n "$HB" ] &amp;&amp; [ -n "$PW" ] &amp;&amp; for x in html-post pdf-post; do test -s "$S/p7301_$x.log" || { echo "missing log: $x"; exit 1; }; [ "$(m "$S/p7301_$x.log")" = 0 ] || { echo "multiple toctrees in: $x"; exit 1; }; done &amp;&amp; [ "$(n "$S/p7301_html-post.log")" = "$HB" ] &amp;&amp; [ "$(n "$S/p7301_pdf-post.log")" = "$PW" ] &amp;&amp; [ "$(k DOCS_HTML_WARN_POST)" = "$HB" ] &amp;&amp; [ "$(k DOCS_PDF_WARN_POST)" = "$PW" ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -F 'WARNING' "$S/p7301_html-base.log" | sort)" = "$(LANG=C LC_ALL=C grep -F 'WARNING' "$S/p7301_html-post.log" | sort)" ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -F 'WARNING' "$S/p7301_pdf-base.log" | sort)" = "$(LANG=C LC_ALL=C grep -F 'WARNING' "$S/p7301_pdf-post.log" | sort)" ] &amp;&amp; [ "$(head -c 5 "$S/p7301_post.pdf")" = '%PDF-' ] &amp;&amp; O="$(mktemp)" &amp;&amp; LC_ALL=C uv run pytest tests/test_changelog_page_gate.py -q -rs -p no:cacheprovider &gt; "$O" 2&gt;&amp;1 &amp;&amp; grep -qE '[0-9]+ passed' "$O" &amp;&amp; ! grep -qE '[0-9]+ (skipped|failed)' "$O" &amp;&amp; [ "$(k CHANGELOG_GATE_SKIPPED_73_01)" = 0 ] &amp;&amp; [ "$(git diff --name-only "$B" -- . ':(exclude).planning')" = CHANGELOG.md ] &amp;&amp; [ -z "$(git diff --name-only "$B" -- .planning/REQUIREMENTS.md)" ] &amp;&amp; for s in '## Pure-addition proof' '## Fence assertions' '## Bullet content assertions' '## Discretion exercised' '## Docs render — post-edit, clean builds' '## Changelog page gate'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `numeral in a new bullet`, `forbidden word in a new bullet`, `language code in a new bullet`, `missing log: NAME`, `multiple toctrees in: NAME` or `missing section: NAME` printed. Each of these makes it fail:
- the heading order in the region wrong, or a non-blank line between `## [Unreleased]` and the first `###`
- a bold-bullet count other than 6, or the no-effect phrase not present exactly six times
- a new subsection body other than one blank line, one bullet and one blank line
- the Added bullet lacking its ID span or any of `tox -e linkcheck`, anchor, network, "a plain `tox` run", contributor, or the no-effect phrase; or carrying CI vocabulary
- the Fixed bullet lacking its ID span, sidebar, User Guide, Examples, once or the no-effect phrase; or carrying a page path, a documentation-host word or a version name of it
- a digit, a URL, a Japanese-site word or the language code in either new bullet
- a deleted line, other than two hunks, other than four added blank lines, or added non-blank lines differing from the two headings and two bullets
- a recorded diff key differing from its expectation
- the heading count, link-reference count, final line, compare-link count, `### Changed` digest or Planned digest differing from the base, or any unpublished version string in CHANGELOG.md
- `pyproject.toml:7` not the 0.9.2 assignment
- a post-edit log missing, carrying a multiple-toctrees line, or counting differently from its baseline; a post-edit key differing; the sorted WARNING lines differing from the base log's
- the post PDF copy not starting with `%PDF-`
- the live changelog page gate reporting no passed count, or any skipped or failed test, or its recorded skip key not 0
- a product-tree file other than CHANGELOG.md changed, or REQUIREMENTS.md changed
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Pure-addition proof` holds the verbatim diff with `REMOVED_LINES = 0`, `DIFF_HUNKS = 2` and `ADDED_BLANK_LINES = 4`.
    - `## Fence assertions` records every key listed in action step 3 with its command; the heading and link-reference counts equal the base, `VERSION_LITERALS_AFTER = 0`, `CHANGED_SHA_AFTER` equals `CHANGED_SHA_BEFORE`, `PLANNED_SHA_AFTER` equals `PLANNED_SHA_BEFORE`, the compare link stays against `v0.9.2`, and `pyproject.toml:7` still reads `version = "0.9.2"`.
    - `## Bullet content assertions` records `ADDED_ID_SPAN = present`, `FIXED_ID_SPAN = present`, `ADDED_REQUIRED_POINTS = present`, `ADDED_CI_WORDS = 0`, `FIXED_REQUIRED_POINTS = present`, `FIXED_PATH_FRAGMENTS = 0`, `FIXED_HOST_WORDS = 0`, `BULLET_NUMERALS = none`, `JAPANESE_SITE_WORDS = 0`, `BULLET_URLS = 0` and `ADJACENCY = OK`, and quotes the six bullets' first lines in order.
    - `## Discretion exercised` records both lead choices, the ID choices, the evidence-sentence choice and the absent `### Verified`.
    - `DOCS_HTML_WARN_POST` and `DOCS_PDF_WARN_POST` equal their clean pre-edit baselines, both post multiple-toctrees keys are 0, `WARNING_LINES_IDENTICAL = yes`, and `PDF_MAGIC_POST = %PDF-`.
    - `## Changelog page gate` quotes each test line with `CHANGELOG_GATE_SKIPPED_73_01 = 0`.
    - `scripts/extract_changelog_section.py` was not run, and among product-tree files only `CHANGELOG.md` changed.
  </acceptance_criteria>
  <done>The milestone's changes are described by two bullets in two new subsections under the existing `## [Unreleased]` heading as pure addition, every D-01..D-05 constraint is measured, both docs environments rebuilt clean match the clean phase-base baseline with zero multiple-toctrees lines, and the changelog page gate ran with nothing skipped.</done>
</task>

</tasks>

<assumption_delta_decision>
The assumption-delta detector (`gsd-tools query assumption-delta scan 73 --json`, run at plan time)
returned `detected: true` with one `pluralization` signal, the term `multiple` in the snippet
"with **zero** `multiple toctrees` messages." That is the name of a Sphinx console message whose count
this phase must keep at zero. It is not a second instance of any identity or storage model.
- **Primary noun:** none changes. No platform, auth method, tenant, region or source of truth is
  introduced; the phase edits one Markdown file and records evidence.
- **Decision:** `no-change`. The signal is lexical, and there is no model to promote or to add
  alongside.
- **Invariant test:** none suggested, because there is no generalised intent to encode.
</assumption_delta_decision>

<flagged_assumptions>
## Spec-less edge-coverage accounting (6 probe-surfaced items, 0 silent drops)

This phase has no SPEC.md. The deterministic fallback probe ran over REL-14's text and returned six
items, all `unresolved`. Each is resolved against this phase's actual objects:

| Requirement | Category | Probe | Resolution | Verification | Authored in |
|---|---|---|---|---|---|
| REL-14 | adjacency | exactly equal or just touching | `### Added` and `### Fixed` touch the `### Changed` block on both sides without altering it: each is separated by exactly one blank line and the Changed block's SHA-256 is unchanged. `origin/main` equal to the milestone base is the "just touch" case of the trial merge: the merge is a no-op whose tree equals HEAD's. | explicit | 73-01 truths (subsections), 73-05 truths (trial merge) |
| REL-14 | empty | empty, single-element or null input | Every empty result (no tag, 404, no release, empty milestone code diff, zero multiple-toctrees lines) is paired with a positive control. An unmoved `origin/main` makes the trial merge a no-op and the handoff's branch-update step a recorded no-op. | explicit | 73-02, 73-06 truths (probes); 73-01 truths (toctree control); 73-05 and 73-07 truths (no-op) |
| REL-14 | encoding | whose definition of equality | Byte identity by SHA-256: the untouched CHANGELOG regions and the whole of `.planning/REQUIREMENTS.md` are compared as digests, never by visual similarity or line count. | explicit | 73-01 and 73-07 truths |
| REL-14 | ordering | is output order specified | Keep a Changelog order Added, Changed, Fixed, then Planned; the CI job census compared as a sorted name set against Phase 72's run; the REL-14 grep compared line by line in file order. | explicit | 73-01, 73-04 and 73-07 truths |
| REL-14 | idempotency | what if it runs twice | `git merge-tree --write-tree` re-run from the recorded inputs reproduces `MERGE_TREE`; the fence probes re-run at close and MATCH; a resumed CI executor reuses `RUN_ID` and never dispatches again. | explicit | 73-05, 73-07 and 73-04 truths |
| REL-14 | concurrency | interrupted or parallel | Same-wave plans have disjoint files and never read a sibling's same-wave evidence. Only 73-04 writes to origin, as one fast-forward that halts rather than forcing. A Dependabot PR landing on `main` mid-phase is caught by the trial merge (D-07, D-09). No plan writes REQUIREMENTS.md, so the fence detects any other writer. | backstop | 73-04 truths |

**Residual assumptions, flagged and not guaranteed by any plan:**

- (a) The local canonical ref does not move between 73-04's census and its push. If it does,
  `PUSHED_SHA` records what was pushed, the run is bound to it by `headSha`, and 73-06's empty
  product diff from `PUSHED_SHA` to HEAD shows whether the dispatched tree is this phase's.
- (b) `origin/main` does not move between 73-05's pre-flight and `/gsd-complete-milestone`. The
  handoff therefore re-runs `git merge-tree --write-tree` immediately before the PR (D-07, D-10).
- (c) The open Dependabot pull requests do not change state between 73-05's census and the close.
  The handoff re-reads them at its own time and names any change (D-06).
- (d) The network is reachable for `tox -e linkcheck`, the PyPI probes and the `gh` calls. If not,
  the probes fail loudly and the linkcheck policy escalates to the owner (D-14).

**Accounting:** 6 surfaced = 5 authored as explicit truths + 1 authored as a backstop marker + 0
dismissed + 0 dropped. The residual assumptions (a) to (d) are surfaced here in addition.

## Prohibition recall accounting (descriptor-less, never auto-dismissed)

Stage 1 over-produced against REL-14's text. The candidates were:

- a tag, a PyPI upload or a GitHub Release
- a version bump
- the pull request opened or merged inside this phase
- `release.yml` or `update-pin.yml` started
- a Dependabot pull request merged, rebased, commented on or closed
- `main` absorbed into the branch
- REL-14 recorded as satisfied, or REQUIREMENTS.md written
- an unpublished version number named or linked
- a bullet describing the Japanese site
- a bullet claiming a CI job, schedule or required check that does not exist
- a bullet claiming the sidebar fix is visible on the default `stable` documentation
- a bullet overstating the work or citing counts that move with the docs
- stale or inherited evidence
- a force-push or a decoy push
- a product-tree change beyond `CHANGELOG.md`
- a workflow edit
- malformed markdown
- flaky link targets

Stage 2 dropped two as routine engineering: malformed markdown, which the clean docs gates cover, and
flaky link targets, which the linkcheck policy (D-14) covers. No candidate was canon security or
compliance, so no canon-referral breadcrumb applies. Six bespoke prohibitions survive. Each is
authored with no `check_*` descriptor, so each disposes flagged-unverified downstream.

- **P1 (safety):** no irreversible or outward action beyond the one push and one `ci.yml` dispatch
  (D-12). In plans 73-01, 73-02, 73-04, 73-05, 73-06 and 73-07.
- **P2 (honesty):** REL-14 is never recorded as satisfied, and REQUIREMENTS.md is never written
  (D-08). In plans 73-02 and 73-07.
- **P3 (transparency):** no reference to an unpublished version, no Japanese-site claim, no CI-job
  claim and no `stable`-visibility claim in a bullet (D-02, D-03, D-04). In plans 73-01 and 73-07.
- **P4 (accuracy):** the bullets do not overstate and carry no counts (D-03, D-04). In plan 73-01.
- **P5 (evidence integrity):** no inherited, paraphrased or fabricated evidence, and no negative
  probe without its positive control (D-13, D-14). In plans 73-02 to 73-07.
- **P6 (scope):** no `origin/main` absorption, no product-tree change beyond `CHANGELOG.md`, and no
  action on any Dependabot pull request (D-05, D-06, D-09). In plans 73-01, 73-03, 73-04, 73-05 and
  73-06.

**Accounting:** 6 kept = 6 authored into `must_haves.prohibitions` + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` → `docs/source/changelog.rst` → Read the Docs `latest` | authored prose becomes a public page once REL-14 merges |
| authored prose → the project's factual record | the bullets are the account of what v0.9.5 changed; an unsupported or forward-looking claim misrepresents it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Tampering | malformed MyST in a bullet publishing broken markup (ASVS V5, L1, marginal: the input is prose this project authors) | medium | mitigate | Both docs environments are built from an empty `docs/_build` before and after the edit, and counts and sorted `WARNING` sets must be equal; the changelog page gate runs with nothing skipped. The tracer proves the render chain first, and drift from another page halts instead of moving the baseline. |
| T-73-02 | Tampering | a heading, link or prose reference to a release that does not exist | high | mitigate | Whole-file count of the unpublished version strings is 0; heading and link-reference counts equal the base; the final line and the `v0.9.2...HEAD` compare link are unchanged; pure addition proven by two hunks with zero deletions. |
| T-73-03 | Repudiation | a bullet claiming a CI job or scheduled run that does not exist, or claiming the fix is on the default `stable` docs (REL-14 revision, constraint 5) | high | mitigate | Case-insensitive whole-word checks for the CI vocabulary in the Added bullet and for the documentation host and its version names in the Fixed bullet; `## Accuracy basis` maps each claim to a named Phase 72 evidence section. |
| T-73-04 | Repudiation | a localised or incremental-build reading recorded as the zero multiple-toctrees baseline | medium | mitigate | `rm -rf docs/_build` precedes every counted build; every build and grep runs under `LANG=C LC_ALL=C`; Phase 72's recorded pre-fix count is quoted as the positive control, and the English `build succeeded` line proves the log language. |
| T-73-05 | Elevation of Privilege | scope escape into another product-tree file or REQUIREMENTS.md under cover of release prep | high | mitigate | The verify requires the product-tree diff from `BASE_73_01` to be exactly `CHANGELOG.md` and REQUIREMENTS.md to be unchanged. |
| T-73-SC | Tampering | package installs | low | accept | No new package is added. `uv sync --extra dev --extra docs --python 3.13.13` and the tox environments install only what `uv.lock` pins; `73-RESEARCH.md` § "Package Legitimacy Audit" records zero added packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This is the phase-wide list; plans 73-02 to 73-07 repeat their own rows and point here.

**New files under `.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/`:**

| File | Created by | Extended by |
|------|------------|-------------|
| `73-CHANGELOG-EVIDENCE.md` | 73-01 | — |
| `73-CLOSEOUT-GUARD.md` | 73-02 (phase-head baseline and operator procedure) | 73-07 (close-time re-verification); the orchestrator later appends the third observation |
| `73-SC1-INVARIANTS.md` | 73-02 (observation 1 of 2 and the milestone fences) | 73-06 (observation 2 of 2, the close-tip milestone diff, the phase-scoped diff and the post-dispatch proof) |
| `COVERAGE.md` | 73-02 (reasoned no-external-API declaration) | — |
| `73-GREEN-TREE-EVIDENCE.md` | 73-03 | — |
| `73-CI-EVIDENCE.md` | 73-04 | — |
| `73-PREFLIGHT-EVIDENCE.md` | 73-05 (non-committing trial merge, protection, merge precedent, Dependabot census) | — |
| `73-HANDOFF.md` | 73-07 | — |
| `73-01-SUMMARY.md` to `73-07-SUMMARY.md` | one per plan | — |

**Evidence keys this plan creates** (in `73-CHANGELOG-EVIDENCE.md`): `BASE_73_01`,
`PYVENV_HOME_73_01`, `PYVENV_VERSION_73_01`, `SCRATCH_73_01`, `HEADINGS_BEFORE`, `LINKREFS_BEFORE`,
`UNRELEASED_BOLD_BEFORE`, `VERSION_LITERALS_BEFORE`, `CHANGED_SHA_BEFORE`, `PLANNED_SHA_BEFORE`,
`DOCS_HTML_WARN_BASE`, `MULTI_TOCTREE_HTML_BASE`, `DOCS_PDF_WARN_BASE`, `MULTI_TOCTREE_PDF_BASE`,
`PDF_MAGIC_BASE`, `MULTI_TOCTREE_POSITIVE_CONTROL_72`, `DOCS_HTML_WARN_TRACER`,
`MULTI_TOCTREE_HTML_TRACER`, `ADDED_LINES`, `REMOVED_LINES`, `DIFF_HUNKS`, `ADDED_BLANK_LINES`,
`HEADINGS_AFTER`, `LINKREFS_AFTER`, `UNRELEASED_BOLD_AFTER`, `NO_EFFECT_PHRASES`,
`VERSION_LITERALS_AFTER`, `CHANGED_SHA_AFTER`, `PLANNED_SHA_AFTER`, `ADDED_ID_SPAN`, `FIXED_ID_SPAN`,
`ADDED_REQUIRED_POINTS`, `ADDED_CI_WORDS`, `FIXED_REQUIRED_POINTS`, `FIXED_PATH_FRAGMENTS`,
`FIXED_HOST_WORDS`, `BULLET_NUMERALS`, `JAPANESE_SITE_WORDS`, `BULLET_URLS`, `ADJACENCY`,
`DOCS_HTML_WARN_POST`, `MULTI_TOCTREE_HTML_POST`, `DOCS_PDF_WARN_POST`, `MULTI_TOCTREE_PDF_POST`,
`PDF_MAGIC_POST`, `WARNING_LINES_IDENTICAL`, `CHANGELOG_GATE_PASSED_73_01`,
`CHANGELOG_GATE_SKIPPED_73_01`. Scratch files: `p7301_html-base.log`, `p7301_pdf-base.log`,
`p7301_base.pdf`, `p7301_html-tracer.log`, `p7301_html-post.log`, `p7301_pdf-post.log`,
`p7301_post.pdf` under `SCRATCH_73_01`.

**Product tree:** exactly one file changes, `CHANGELOG.md`, and only by addition: `### Added` with
one bullet before `### Changed`, and `### Fixed` with one bullet before
`### Planned for Future Releases`. No new `## [...]` heading and no link-reference line is added.

**Outside the repository:** 73-04 fast-forwards `origin/gsd/v0.9.5-docs-link-check-and-navigation`
to `PUSHED_SHA` and starts one `workflow_dispatch` run of `ci.yml`. 73-01, 73-03 and 73-05 create
scratch directories outside the repository.

**No new code symbol** (class, function, CLI flag or config value) is created by any plan of this
phase.

**Reserved name, never produced:** no plan authors `73-VERIFICATION.md`; `gsd-verifier` owns that
name and overwrites it wholesale.
</artifacts_this_phase_produces>

<verification>
- The `## [Unreleased]` region reads `### Added`, `### Changed`, `### Fixed`,
  `### Planned for Future Releases`; the two new subsections hold one bullet each, citing
  `(QUA-13, DOC-24)` and `(DOC-18)`, and the `### Changed` and Planned blocks are byte-identical.
- The new bullets say only what D-03 and D-04 allow, each says it has no effect on installing or
  using typsphinx, and neither carries a version, a count, a URL, a CI claim or a `stable` claim.
- No unpublished version string anywhere in `CHANGELOG.md`; heading count, link-reference count and
  final line equal the base; the diff from the base is pure addition in two hunks.
- Both docs environments, built clean before and after the edit, report equal warning counts and
  identical warning lines with zero multiple-toctrees lines, the PDF is real, and the changelog page
  gate runs with nothing skipped.
- Among product-tree files only `CHANGELOG.md` changed; `.planning/REQUIREMENTS.md` untouched.
</verification>

<success_criteria>
1. ROADMAP SC#2: the milestone's changes are two bullets under the existing `## [Unreleased]`
   heading, in the house register, with no release section, no unpublished version, the compare
   base left at `v0.9.2`, and no CI-job claim (D-01..D-05).
2. The docs half of SC#3 at the phase base: both docs environments build clean with zero
   multiple-toctrees lines, before and after the edit, with unchanged warning counts (D-14).
3. Every claim in both bullets is traced to a named Phase 72 evidence section.
</success_criteria>

<output>
Create `.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-01-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`. REL-14 closes at `/gsd-complete-milestone`, never in a
  plan (D-08).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run `phase.complete` or any other GSD state-writing verb.
- The first section states `BASE_73_01`, the four docs warning keys (`_BASE` and `_POST` for both
  environments), the multiple-toctrees keys, the bullet count, and both bullets' bold leads quoted.
  If a HALT heading was written, that section says so first.
</output>
