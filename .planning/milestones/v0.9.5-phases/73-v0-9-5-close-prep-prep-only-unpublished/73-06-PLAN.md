---
phase: 73-v0-9-5-close-prep-prep-only-unpublished
plan: 06
type: execute
wave: 3
depends_on: ["73-01", "73-02", "73-03", "73-04", "73-05"]
files_modified:
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md
autonomous: true
requirements: [REL-14]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-13 / SC#1, observation 2 of 2: every probe of observation 1 is re-run at OBS2_AT, later than OBS1_AT, with the same positive controls against v0.9.2, and the fence still holds. No v0.9.3, v0.9.4 or v0.9.5 tag exists locally or on origin, PyPI answers 404 for all three and 200 for 0.9.2, no GitHub Release exists for any of them, no release.yml run exists since 2026-08-31 or on an unpublished ref, and no pull request has the milestone branch as its head (D-11).'
    - 'D-13 on the close tip: `git diff --stat 098a8ff6..HEAD -- typsphinx/ .github/workflows/` is empty; the pathspec lists tracked files at HEAD; and the widened diff from the same base lists exactly Phase 72''s five product files plus CHANGELOG.md, the positive control that the base is real.'
    - 'SC#1 phase-scoped fence: `git diff PHASE_BASE_SHA HEAD` outside .planning/ lists exactly CHANGELOG.md with zero deletions, and no commit since the milestone base changes a `version = ` line in pyproject.toml.'
    - 'D-12 final tip: PUSHED_SHA from 73-CI-EVIDENCE.md is an ancestor of HEAD, and `git diff --name-only PUSHED_SHA HEAD` outside .planning/ is empty, so every commit after the single CI dispatch is planning-only. Exactly one workflow_dispatch CI run and no release.yml run carry PUSHED_SHA. The merge-base of HEAD and origin/main still equals MILESTONE_BASE.'
    - 'Edge (empty): every empty result in observation 2 and in the close-tip fences is paired with a positive control from the same source.'
    - 'REL-14 is cited for coverage only; SUMMARY requirements-completed: [] (D-08).'
  artifacts:
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md
      provides: 'observation 2 of 2, the D-13 milestone fences on the close tip, the phase-scoped product diff, the post-dispatch planning-only proof, and the SC#1 verdict'
      contains: 'SC1_VERDICT = '
  key_links:
    - from: '73-CI-EVIDENCE.md PUSHED_SHA'
      to: 'the post-dispatch planning-only proof'
      via: 'git diff --name-only PUSHED_SHA HEAD outside .planning must be empty'
      pattern: 'PUSHED_SHA = '
    - from: '73-CLOSEOUT-GUARD.md PHASE_BASE_SHA'
      to: 'the phase-scoped product diff'
      via: 'KEY = value line read with sed'
      pattern: 'PHASE_BASE_SHA = '
    - from: '72-GATES-EVIDENCE.md PRODUCT_DIFF_FILES'
      to: 'the close-tip widened-diff control'
      via: 'the recorded five files plus CHANGELOG.md, C-sorted, must equal the live widened diff'
      pattern: 'PRODUCT_DIFF_FILES = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action: every command is a read, and nothing is pushed, tagged, released, dispatched or commented.'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record a probe result, digest or count that was not computed here: observation 1''s values are only compared against, never transcribed as observation 2.'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT change any product-tree file; this plan appends evidence only.'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Close SC#1 on the phase's close tip (D-13):
- re-probe the unpublished shape at a genuinely later timestamp than observation 1, with the same
  positive controls, for all three unclaimed version numbers
- repeat the milestone-wide `typsphinx/` and `.github/workflows/` diff on the close tip, with the
  pathspec and widened-diff controls
- prove that nothing outside `CHANGELOG.md` and `.planning/` changed across the phase
- prove that every commit after the single CI dispatch is planning-only (D-12)

Purpose: this plan runs in wave 3, one wave after the CI dispatch (73-04), the green tree (73-03)
and the pre-flight (73-05). It is therefore the first plan that can read all their evidence, and its
observation is separated from observation 1 by two waves and a full CI run. It uses Phase 69's
lightweight scoped-plus-widened diff, not Phase 71's masked-AST re-run, because nothing under
`typsphinx/` changed in this milestone (D-13).

Output: `73-SC1-INVARIANTS.md` § observation 2, § the close-tip milestone fences, § the
phase-scoped product diff, § commits after the CI dispatch, and § the SC#1 verdict.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run project tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line. The verify fails on any key line containing an opening parenthesis.

**Scratch files.** Parallel executors share the session scratchpad. Put any captured output under
your own `mktemp -d` directory, recorded as `SCRATCH_73_06`, with a `p7306_` prefix.

**Locale.** The host runs `LANG=ja_JP.UTF-8`; prefix `LANG=C LC_ALL=C` to any `git` command whose
message you quote for comparison.

**Reserved name.** Never create `73-VERIFICATION.md`; `gsd-verifier` owns it and overwrites it
wholesale. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`,
and do not call any GSD state-writing verb or the GSD commit helper; commit with plain `git commit`.
</worktree_provisioning>

<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: v0.9.4 -->
<!-- planner-discipline-allow: v0.9.5 -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[345] -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
The version tags are named because the probes assert their absence. `## HALT` is written only on a
failure path.

## Planning-time census (2026-09-14; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| Milestone base | `098a8ff64cf008822eef9dc69f75102ded3f7bc1`, recorded as `MILESTONE_BASE` by 73-02 | `73-SC1-INVARIANTS.md` (wave 1) |
| Expected close-tip widened diff | Phase 72's five files (`CLAUDE.md`, `README.md`, `docs/source/contributing.rst`, `docs/source/index.rst`, `tox.ini`) plus this phase's `CHANGELOG.md` | `72-GATES-EVIDENCE.md` `PRODUCT_DIFF_FILES`, 73-01 |
| Anchors | `PHASE_BASE_SHA` (73-02), `PUSHED_SHA` and `RUN_ID` (73-04) | wave-1 and wave-2 evidence |
| Observation 1 | `OBS1_AT` and every probe key in `73-SC1-INVARIANTS.md` § "Observation 1 of 2" | 73-02 |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: SC#1 observation 2 of 2, every remote probe re-run with its positive control</name>
  <precondition>Plans 73-02 to 73-05 are merged into this worktree's HEAD: their evidence files exist without a HALT heading, `73-CI-EVIDENCE.md` reads `SC3_CI_VERDICT = MET`, `73-GREEN-TREE-EVIDENCE.md` reads `SC3_LOCAL_VERDICT = MET`, and `73-PREFLIGHT-EVIDENCE.md` reads `TRIAL_MERGE_VERDICT = MET`. Otherwise write `## HALT: wave 2 incomplete` naming the missing item, and stop.</precondition>
  <reversibility rating="reversible">Evidence appends recording read-only probes.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md</files>
  <read_first>
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md (observation 1: `OBS1_AT`, the probes, `RELEASE_POSITIVE_CONTROL`, § "Handoff to observation 2")
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md § "Observation 2 of 2" onward (the shape to mirror)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-11 and D-13
  </read_first>
  <action>
Append to `73-SC1-INVARIANTS.md`.

1. **`## Observation 2 of 2`** (D-13).
   - Record the head check and provisioning, then `BASE_73_06 = ` from `git rev-parse HEAD` before any commit, `SCRATCH_73_06 = ` from `mktemp -d`, and `OBS2_AT = ` from `date -u +"%Y-%m-%dT%H:%M:%SZ"`.
   - Re-run every probe of observation 1 in the same order, with the same positive controls, each as a command with its verbatim output. Use the same key names with an `OBS2_` prefix: `OBS2_VERSION_LINE`, `OBS2_LOCAL_V093_TAGS`, `OBS2_LOCAL_V094_TAGS`, `OBS2_LOCAL_V095_TAGS`, `OBS2_REMOTE_V092_TAG`, `OBS2_REMOTE_V093_TAG`, `OBS2_REMOTE_V094_TAG`, `OBS2_REMOTE_V095_TAG`, `OBS2_PYPI_092_HTTP`, `OBS2_PYPI_093_HTTP`, `OBS2_PYPI_094_HTTP`, `OBS2_PYPI_095_HTTP`, `OBS2_GH_LATEST`, `OBS2_GH_V093_RELEASES`, `OBS2_GH_V094_RELEASES`, `OBS2_GH_V095_RELEASES`, `OBS2_RELEASE_RUNS_SINCE_V092` and `OBS2_BRANCH_PRS`. The remote tag counts come from one unfiltered `git ls-remote --tags origin` listing, and the release.yml positive control is observation 1's `RELEASE_POSITIVE_CONTROL` run, still on `v0.9.2`.
   - Quote `OBS1_AT` beside `OBS2_AT` and state the elapsed interval.
   - State that the fence held across the whole phase, including after 73-04's push and dispatch, and that all three unpublished numbers remain unclaimed (D-11).

2. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; S="$D/73-SC1-INVARIANTS.md" &amp;&amp; BR=gsd/v0.9.5-docs-link-check-and-navigation &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; ks() { sed -n "s/^$1 = //p" "${2:-$S}" | head -n 1; } &amp;&amp; B="$(ks BASE_73_06)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; O1="$(ks OBS1_AT)" &amp;&amp; O2="$(ks OBS2_AT)" &amp;&amp; [ -n "$O1" ] &amp;&amp; [ -n "$O2" ] &amp;&amp; [[ "$O2" &gt; "$O1" ]] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; [ "$(ks OBS2_VERSION_LINE)" = 'version = "0.9.2"' ] &amp;&amp; [ -n "$(git tag -l 'v0.9.2')" ] &amp;&amp; for v in 3 4 5; do [ -z "$(git tag -l "v0.9.$v")" ] || { echo "local tag: v0.9.$v"; exit 1; }; done &amp;&amp; T="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.[345]')" = 0 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json)" = 200 ] &amp;&amp; for v in 3 4 5; do [ "$(curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/typsphinx/0.9.$v/json")" = 404 ] || { echo "pypi: 0.9.$v"; exit 1; }; done &amp;&amp; [ "$(gh release list --limit 20 --json tagName,isLatest --jq '.[] | select(.isLatest) | .tagName')" = v0.9.2 ] &amp;&amp; [ "$(gh release list --limit 50 --json tagName --jq '[.[] | select(.tagName == "v0.9.3" or .tagName == "v0.9.4" or .tagName == "v0.9.5")] | length')" = 0 ] &amp;&amp; RPC="$(ks RELEASE_POSITIVE_CONTROL)" &amp;&amp; [ -n "$RPC" ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json databaseId,headBranch --jq "[.[] | select(.databaseId == $RPC and .headBranch == \"v0.9.2\")] | length")" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headBranch,createdAt --jq '[.[] | select(.headBranch == "v0.9.3" or .headBranch == "v0.9.4" or .headBranch == "v0.9.5" or .createdAt &gt; "2026-08-31")] | length')" = 0 ] &amp;&amp; [ "$(gh pr list --state all --limit 5 --json number --jq 'length')" -ge 1 ] &amp;&amp; [ "$(gh pr list --head "$BR" --state all --json number --jq 'length')" = 0 ] &amp;&amp; for x in OBS2_LOCAL_V093_TAGS OBS2_LOCAL_V094_TAGS OBS2_LOCAL_V095_TAGS OBS2_REMOTE_V093_TAG OBS2_REMOTE_V094_TAG OBS2_REMOTE_V095_TAG OBS2_GH_V093_RELEASES OBS2_GH_V094_RELEASES OBS2_GH_V095_RELEASES OBS2_RELEASE_RUNS_SINCE_V092 OBS2_BRANCH_PRS; do [ "$(ks "$x")" = 0 ] || { echo "key not 0: $x"; exit 1; }; done &amp;&amp; [ "$(ks OBS2_REMOTE_V092_TAG)" = 1 ] &amp;&amp; [ "$(ks OBS2_PYPI_092_HTTP)" = 200 ] &amp;&amp; for v in 093 094 095; do [ "$(ks "OBS2_PYPI_${v}_HTTP")" = 404 ] || { echo "key: OBS2_PYPI_${v}_HTTP"; exit 1; }; done &amp;&amp; [ "$(ks OBS2_GH_LATEST)" = v0.9.2 ] &amp;&amp; grep -qxF '## Observation 2 of 2' "$S" &amp;&amp; [ -z "$(git diff --name-only "$B" -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$S" &amp;&amp; ! grep -q '^## HALT' "$S"</automated>
    <fails_when>Non-zero exit, or `local tag: TAG`, `pypi: VERSION`, `key not 0: KEY` or `key: KEY` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_73_06` missing, or `OBS2_AT` empty or not later than `OBS1_AT`
- `pyproject.toml:7` or its recorded copy not 0.9.2
- the v0.9.2 positive controls missing, or any v0.9.3, v0.9.4 or v0.9.5 tag, PyPI release, GitHub Release, recent release.yml run or pull request from the branch present
- the latest release not v0.9.2, or the recorded release.yml positive-control run absent or not on v0.9.2
- a recorded `OBS2_` key contradicting its probe, or the section missing
- a product-tree file changed since `BASE_73_06`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Observation 2 of 2` records `OBS2_AT`, later than `OBS1_AT`, with the elapsed interval, and repeats every probe of observation 1 with its positive control under an `OBS2_` key, for all three version numbers.
    - Every `OBS2_` negative is paired with the same positive control as in observation 1.
  </acceptance_criteria>
  <done>The unpublished shape is proven a second time, two waves and a full CI run after the first observation, with every remote probe positively controlled.</done>
</task>

<task type="auto">
  <name>Task 2: D-13 fences on the close tip, the phase-scoped product diff, the post-dispatch planning-only proof, and the SC#1 verdict</name>
  <precondition>Task 1 is committed with no HALT heading.</precondition>
  <reversibility rating="reversible">Evidence appends recording read-only probes.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md</files>
  <read_first>
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md (§ "Milestone fences (D-13)" of observation 1, and Task 1)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md (`PHASE_BASE_SHA`)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md (`PUSHED_SHA`, `RUN_ID`, § "D-12 final tip")
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-GATES-EVIDENCE.md (the `PRODUCT_DIFF_FILES` key line)
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md § "The phase-scoped typsphinx/ diff" and § "Commits after the CI dispatch" (the shape to mirror)
  </read_first>
  <action>
Append to `73-SC1-INVARIANTS.md`.

1. **`## Milestone fences on the close tip (D-13)`.** Read `MILESTONE_BASE` from observation 1's section and record:
   - `git diff --stat "$MILESTONE_BASE" HEAD -- typsphinx/ .github/workflows/`, empty, key `CLOSE_MILESTONE_CODE_WORKFLOW_DIFF = empty`
   - `git ls-tree -r --name-only HEAD -- typsphinx/ .github/workflows/`, non-empty, key `CLOSE_PATHSPEC_TRACKED_FILES` (the count)
   - `git diff --name-only "$MILESTONE_BASE" HEAD -- . ':(exclude).planning'`, C-sorted and space-joined as key `CLOSE_MILESTONE_PRODUCT_FILES`. It must equal Phase 72's `PRODUCT_DIFF_FILES` from `72-GATES-EVIDENCE.md` with `CHANGELOG.md` added, C-sorted: the five Phase 72 files and this phase's one.
   - `git log --format=%h -G '^version = ' "$MILESTONE_BASE"..HEAD -- pyproject.toml`, empty, key `CLOSE_VERSION_BUMP_COMMITS = 0`
   - after `git fetch origin main`, `git merge-base HEAD origin/main`, equal to `MILESTONE_BASE`, key `CLOSE_MAIN_ABSORBED = no`

   State why the scoped diff and the two controls are needed together, as observation 1 did.

2. **`## The phase-scoped product diff`.** Read `PHASE_BASE_SHA` from `73-CLOSEOUT-GUARD.md` and record:
   - `git diff --numstat "$PHASE_BASE_SHA" HEAD -- . ':(exclude).planning'`: exactly one row, `CHANGELOG.md`, with a non-zero addition count and zero deletions, key `PHASE_PRODUCT_FILES = CHANGELOG.md`
   - `git diff --name-only "$PHASE_BASE_SHA" HEAD -- typsphinx/ tests/ .github/ pyproject.toml uv.lock tox.ini docs/`, empty, key `PHASE_OTHER_PRODUCT_DIFF = empty`

3. **`## Commits after the CI dispatch`** (D-12). Read `PUSHED_SHA` from `73-CI-EVIDENCE.md` and record:
   - `git merge-base --is-ancestor "$PUSHED_SHA" HEAD; echo "exit:$?"`, which must print `exit:0`
   - `git log --oneline "$PUSHED_SHA"..HEAD`
   - `git diff --name-only "$PUSHED_SHA" HEAD -- . ':(exclude).planning'`, empty, key `POST_DISPATCH_PRODUCT_FILES = 0`
   - `gh run list --workflow=release.yml --limit 20 --json headSha`, none at `PUSHED_SHA`
   - `gh run list --workflow=ci.yml --branch gsd/v0.9.5-docs-link-check-and-navigation --event workflow_dispatch --limit 50 --json databaseId,headSha,createdAt`, exactly one run at `PUSHED_SHA`

   State the D-12 reading. The single dispatch ran on the tip carrying every product-tree change of the phase, and every later commit is `.planning/`-only, which no CI job reads, so the post-dispatch commits are not a stale-tip violation.

4. **`## SC#1 verdict`.** Write `SC1_VERDICT = MET` only when both observations hold with every positive control, `CLOSE_MILESTONE_CODE_WORKFLOW_DIFF = empty`, `CLOSE_MILESTONE_PRODUCT_FILES` matches, `CLOSE_VERSION_BUMP_COMMITS = 0`, `PHASE_PRODUCT_FILES = CHANGELOG.md`, `POST_DISPATCH_PRODUCT_FILES = 0` and `CLOSE_MAIN_ABSORBED = no`. Otherwise write `SC1_VERDICT = NOT-MET` with a HALT heading naming the failed item.

5. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; S="$D/73-SC1-INVARIANTS.md" &amp;&amp; P72=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; BR=gsd/v0.9.5-docs-link-check-and-navigation &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; ks() { sed -n "s/^$1 = //p" "${2:-$S}" | head -n 1; } &amp;&amp; B="$(ks BASE_73_06)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; MB="$(ks MILESTONE_BASE)" &amp;&amp; [ "$MB" = 098a8ff64cf008822eef9dc69f75102ded3f7bc1 ] &amp;&amp; CW="$(git diff --stat "$MB" HEAD -- typsphinx/ .github/workflows/)" &amp;&amp; [ -z "$CW" ] &amp;&amp; TF="$(git ls-tree -r --name-only HEAD -- typsphinx/ .github/workflows/)" &amp;&amp; [ -n "$TF" ] &amp;&amp; [ "$(printf '%s\n' "$TF" | wc -l)" = "$(ks CLOSE_PATHSPEC_TRACKED_FILES)" ] &amp;&amp; WD="$(git diff --name-only "$MB" HEAD -- . ':(exclude).planning')" &amp;&amp; W="$(printf '%s\n' "$WD" | LC_ALL=C sort | tr '\n' ' ' | sed 's/ $//')" &amp;&amp; E="$(printf '%s\n' $(ks PRODUCT_DIFF_FILES "$P72/72-GATES-EVIDENCE.md") CHANGELOG.md | LC_ALL=C sort | tr '\n' ' ' | sed 's/ $//')" &amp;&amp; [ -n "$E" ] &amp;&amp; [ "$W" = "$E" ] &amp;&amp; [ "$(ks CLOSE_MILESTONE_PRODUCT_FILES)" = "$E" ] &amp;&amp; VB="$(git log --format=%h -G '^version = ' "$MB"..HEAD -- pyproject.toml)" &amp;&amp; [ -z "$VB" ] &amp;&amp; git fetch -q origin main &amp;&amp; [ "$(git merge-base HEAD origin/main)" = "$MB" ] &amp;&amp; PB="$(ks PHASE_BASE_SHA "$D/73-CLOSEOUT-GUARD.md")" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; [ "$(git diff --name-only "$PB" HEAD -- . ':(exclude).planning')" = CHANGELOG.md ] &amp;&amp; NS="$(git diff --numstat "$PB" HEAD -- CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$NS" | cut -f2)" = 0 ] &amp;&amp; [ -z "$(git diff --name-only "$PB" HEAD -- typsphinx/ tests/ .github/ pyproject.toml uv.lock tox.ini docs/)" ] &amp;&amp; P="$(ks PUSHED_SHA "$D/73-CI-EVIDENCE.md")" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$P" HEAD &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha --jq '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch "$BR" --event workflow_dispatch --limit 50 --json headSha --jq '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; [ "$(ks CLOSE_MILESTONE_CODE_WORKFLOW_DIFF)" = empty ] &amp;&amp; [ "$(ks CLOSE_VERSION_BUMP_COMMITS)" = 0 ] &amp;&amp; [ "$(ks CLOSE_MAIN_ABSORBED)" = no ] &amp;&amp; [ "$(ks PHASE_PRODUCT_FILES)" = CHANGELOG.md ] &amp;&amp; [ "$(ks PHASE_OTHER_PRODUCT_DIFF)" = empty ] &amp;&amp; [ "$(ks POST_DISPATCH_PRODUCT_FILES)" = 0 ] &amp;&amp; [ "$(ks SC1_VERDICT)" = MET ] &amp;&amp; for s in '## Milestone fences on the close tip (D-13)' '## The phase-scoped product diff' '## Commits after the CI dispatch' '## SC#1 verdict'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$S" &amp;&amp; ! grep -q '^## HALT' "$S"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- `BASE_73_06` missing, or `MILESTONE_BASE` not 098a8ff6
- a non-empty diff over typsphinx/ and .github/workflows/ since the milestone base
- the pathspec listing no tracked file or differing from its key
- the close-tip widened diff differing from Phase 72's five files plus CHANGELOG.md, or its key differing
- a version-line commit since the base, or the merge-base with origin/main differing from `MILESTONE_BASE`
- `PHASE_BASE_SHA` unreadable, the phase product diff not exactly CHANGELOG.md with zero deletions, or any other product path changed since it
- `PUSHED_SHA` unreadable, not an ancestor of HEAD, or followed by a product-tree change
- a release.yml run at `PUSHED_SHA`, or other than one CI dispatch at it
- a recorded key contradicting its probe, `SC1_VERDICT` not MET, or a section missing
- a product-tree file changed since `BASE_73_06`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Milestone fences on the close tip (D-13)` records `CLOSE_MILESTONE_CODE_WORKFLOW_DIFF = empty`, `CLOSE_PATHSPEC_TRACKED_FILES`, `CLOSE_MILESTONE_PRODUCT_FILES` equal to Phase 72's five files plus `CHANGELOG.md`, `CLOSE_VERSION_BUMP_COMMITS = 0` and `CLOSE_MAIN_ABSORBED = no`, each with its command.
    - `## The phase-scoped product diff` shows the widened `--numstat` diff listing exactly `CHANGELOG.md` with zero deletions, and `PHASE_OTHER_PRODUCT_DIFF = empty`.
    - `## Commits after the CI dispatch` shows `PUSHED_SHA` as an ancestor, the post-dispatch commit list, `POST_DISPATCH_PRODUCT_FILES = 0`, no release.yml run at `PUSHED_SHA`, one CI dispatch at it, and the D-12 reading.
    - `SC1_VERDICT = MET`.
  </acceptance_criteria>
  <done>The fence held across the whole phase at two separated timestamps, the milestone changed nothing under `typsphinx/` or `.github/workflows/`, the phase changed nothing outside `CHANGELOG.md` and `.planning/`, and the single CI run covers every product-tree change.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`73-01-PLAN.md`. This plan carries P1, P5 and P6, and it authors the `empty` edge on the close tip.
It also carries residual assumption (a): if the canonical ref moved between 73-04's census and its
push, the empty product diff from `PUSHED_SHA` to HEAD recorded here shows whether the dispatched
tree is this phase's. Phase accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed + 0
dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → origin, GitHub API, PyPI (read-only) | observation 2's probes; nothing is written |
| recorded evidence → this phase's SC#1 verdict | a copied observation-1 value or a mistyped anchor would claim the fence without a measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-27 | Repudiation | observation 2 transcribed from observation 1 instead of re-run (ASVS V7, L1) | high | mitigate | `OBS2_AT` must be later than `OBS1_AT`, and the verify re-runs every remote probe live with its positive control before reading the recorded keys. |
| T-73-28 | Repudiation | an empty close-tip code-and-workflow diff accepted from a wrong base or a mistyped pathspec | high | mitigate | The pathspec must list tracked files, and the widened diff from the same base must equal Phase 72's recorded five files plus `CHANGELOG.md`. |
| T-73-29 | Repudiation | post-dispatch commits read as a stale-tip violation, or a product change slipping in after the dispatch | medium | mitigate | `git diff --name-only PUSHED_SHA HEAD` outside `.planning/` must be empty; exactly one CI dispatch and no release run at `PUSHED_SHA`; the D-12 reading is written into this file. |
| T-73-30 | Tampering | a product change beyond `CHANGELOG.md` across the phase | medium | mitigate | The phase-scoped diff from `PHASE_BASE_SHA` must list exactly `CHANGELOG.md` with zero deletions, and the named product paths must be unchanged. |
| T-73-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `73-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan appends to
`73-SC1-INVARIANTS.md` the sections `## Observation 2 of 2`,
`## Milestone fences on the close tip (D-13)`, `## The phase-scoped product diff`,
`## Commits after the CI dispatch` and `## SC#1 verdict`, with the keys `BASE_73_06`,
`SCRATCH_73_06`, `OBS2_AT`, the eighteen `OBS2_*` probe keys, `CLOSE_MILESTONE_CODE_WORKFLOW_DIFF`,
`CLOSE_PATHSPEC_TRACKED_FILES`, `CLOSE_MILESTONE_PRODUCT_FILES`, `CLOSE_VERSION_BUMP_COMMITS`,
`CLOSE_MAIN_ABSORBED`, `PHASE_PRODUCT_FILES`, `PHASE_OTHER_PRODUCT_DIFF`,
`POST_DISPATCH_PRODUCT_FILES` and `SC1_VERDICT`. It modifies no product-tree file. No file named
`73-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- Two SC#1 observations at separated timestamps, each remote probe positively controlled, for all
  three unclaimed version numbers.
- The milestone-wide `typsphinx/` and `.github/workflows/` diff is empty on the close tip against a
  proven pathspec and a proven base.
- The phase-scoped product diff is exactly `CHANGELOG.md`, and every post-dispatch commit is
  planning-only.
</verification>

<success_criteria>
ROADMAP SC#1 in full: the tree is proven unpublished-shaped twice with positive controls,
`pyproject.toml` still reads `0.9.2` with no version-bump commit, and across the whole milestone
`git diff --stat 098a8ff6..HEAD -- typsphinx/ .github/workflows/` is empty (D-13).
</success_criteria>

<output>
Create `.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-06-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-08).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run any GSD state-writing verb.
- The first section states `OBS1_AT`, `OBS2_AT`, `CLOSE_MILESTONE_CODE_WORKFLOW_DIFF`,
  `CLOSE_MILESTONE_PRODUCT_FILES`, `PHASE_PRODUCT_FILES`, `POST_DISPATCH_PRODUCT_FILES` and
  `SC1_VERDICT`. If a HALT heading was written, that section says so first.
</output>
