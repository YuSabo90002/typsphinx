---
phase: 73-v0-9-5-close-prep-prep-only-unpublished
plan: 07
type: execute
wave: 4
depends_on: ["73-01", "73-02", "73-03", "73-04", "73-05", "73-06"]
files_modified:
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-HANDOFF.md
autonomous: true
requirements: [REL-14]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-08 / SC#4, phase close: the live `sha256sum .planning/REQUIREMENTS.md` equals REQ_SHA256_BASE, `wc -l` equals REQ_LINES_BASE, `git diff --name-only -- .planning/REQUIREMENTS.md` is empty, and no commit since PHASE_BASE_SHA touched the file; REQ_VERDICT_CLOSE = MATCH. REL-14 reads `- [ ]` and Pending, read directly from the file.'
    - 'Edge (ordering): the `grep -n REL-14` output at close is compared line by line, in file order, with the same grep over REQUIREMENTS.md at PHASE_BASE_SHA, and the two are identical.'
    - 'Edge (encoding): the fence compares raw bytes by SHA-256, the primary probe, because a checkbox flip leaves `wc -l` unchanged.'
    - 'D-10: the first 14 lines of 73-HANDOFF.md, before any `##` heading, state that the milestone publishes nothing: no tag, no PyPI upload, no GitHub Release, no version bump. They mark the update-pin.yml dispatch and the Read the Docs stable check not applicable, and record as facts, not steps, that the daily update-pin.yml schedule moves the Japanese latest site onto the merged main with no action, that /en/latest/ rebuilds on the main push, and that stable stays on v0.9.2. They name the REL-14 pull request as the one irreversible action.'
    - 'D-10: the handoff lists, in order, the concrete commands /gsd-complete-milestone runs: the fence backup; a re-run of the trial merge; a conditional branch update (`git merge --no-ff origin/main` only if origin/main moved past 098a8ff6, never a rebase, because of strict: true; otherwise a recorded no-op); the push; the pull request from gsd/v0.9.5-docs-link-check-and-navigation to main with the body passed as a file; the six required checks named literally; `gh pr merge <number> --merge`, citing #143 and #145; the REL-14 observations; and only then the Dependabot pull requests.'
    - 'SC#4 / D-10: the handoff lists the observations REL-14 is checked on after the merge: the merge commit on origin/main, pyproject.toml still 0.9.2, no v0.9.3, v0.9.4 or v0.9.5 tag (with v0.9.2 as the positive control), PyPI 404 for 0.9.5 (and 0.9.3, 0.9.4) with 200 for 0.9.2, and no Release for any of them.'
    - 'D-06: the handoff names #146, #147, #148, #149 and #150 by number, package and version range from 73-05''s census at PR_CENSUS_AT and from a live re-read, states the order "milestone pull request first, Dependabot second", and the live count of comments and reviews by the gh account on each equals the census: this phase touched none of them.'
    - 'D-07: the handoff states that a Dependabot merge landing on main before the close is caught by the re-run trial merge, whose merged-tree ruff verdict is then authoritative, and that no docs build of main plus #150 is added.'
    - 'D-11: the handoff records 0.9.3, 0.9.4 and 0.9.5 as unclaimed, not decided, and states that `## [Unreleased]` now holds six bullets from three unpublished milestones (four under `### Changed`, plus this phase''s `### Added` and `### Fixed`) that the next release-prep phase promotes into its versioned section.'
    - 'D-15: this phase adds no UI-hint line to ROADMAP.md: the Phase 73 section of .planning/ROADMAP.md carries none, the UI-gate false positive having been handled at plan time with --skip-ui.'
    - 'SC#4 standalone: 73-HANDOFF.md reproduces the third-observation procedure inline: both entry points (/gsd-execute-phase and /gsd-verify-work), the scratch backup of REQUIREMENTS.md, ROADMAP.md and STATE.md, the baseline digest, the REL-14 grep lines and `git checkout -- .planning/REQUIREMENTS.md`. It closes with a live fence observation.'
    - 'The handoff contains no command that tags, creates a release or dispatches the translations pin workflow, and no plan authors 73-VERIFICATION.md.'
    - 'REL-14 is cited for coverage only; SUMMARY requirements-completed: [] (D-08).'
  artifacts:
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-HANDOFF.md
      provides: 'negative-first standalone handoff: ordered /gsd-complete-milestone steps with the conditional branch update, the REL-14 observations, the Dependabot order, SC#1..SC#4 report, items recorded without acting, third-observation procedure, live fence observation'
      contains: 'publishes nothing'
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md
      provides: 're-verification at phase close'
      contains: 'REQ_VERDICT_CLOSE = '
  key_links:
    - from: '73-CLOSEOUT-GUARD.md REQ_SHA256_BASE'
      to: '73-HANDOFF.md third-observation block'
      via: 'the digest written inline so the operator needs no other file after phase.complete-family tooling runs'
      pattern: 'REQ_SHA256_BASE = '
    - from: '73-PREFLIGHT-EVIDENCE.md MERGE_TREE, MAIN_MOVED, PROTECTION_*, PR_CENSUS_AT, PR_<n>_*'
      to: '73-HANDOFF.md conditional branch-update, merge and Dependabot steps'
      via: 'cited as the phase-time result, with the instruction to re-run merge-tree immediately before the pull request'
      pattern: 'MERGE_TREE = '
    - from: '73-CI-EVIDENCE.md PUSHED_SHA and RUN_ID'
      to: 'the handoff push step and SC#3 report'
      via: 'the push is a fast-forward from PUSHED_SHA; the run is cited by id'
      pattern: 'RUN_ID = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action: no push, tag, pull request, merge, release, workflow run, or comment on any pull request. The handoff records steps for /gsd-complete-milestone and executes none of them.'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record REL-14 as satisfied: no checkbox flip, no Traceability change, no prose claiming the merge happened. A divergence found at close is reverted with git checkout and reported, never committed (D-08).'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT write a command in the handoff that tags, releases or dispatches the translations pin workflow, and MUST NOT treat 0.9.3, 0.9.4 or 0.9.5 as a decided version (D-10, D-11).'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT restate another evidence file''s verdict without citing its section, and MUST NOT copy a digest, SHA or count as a measurement without re-running the command here.'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Close the phase's REL-14 fence and write the milestone handoff:
- re-verify the REL-14 checksum guard at phase close (D-08)
- author `73-HANDOFF.md` for `/gsd-complete-milestone`:
  - negative-first (D-10)
  - the ordered steps, with the conditional branch update that `strict: true` requires (D-10)
  - the post-merge observations REL-14 is checked on (SC#4)
  - the Dependabot pull requests #146 to #150, named and ordered after the milestone pull request
    (D-06), and the moved-main case (D-07)
  - the three unclaimed version numbers and the six-bullet promotion (D-11)
  - the third-observation procedure reproduced inline, closing with a live fence observation
- confirm that no UI-hint line was added to the ROADMAP (D-15)

Purpose: this plan runs in wave 4, after 73-06's close-tip measurements. It is therefore the first
plan that can read every other evidence file of the phase, and its guard re-verification is the
latest in-plan observation of `.planning/REQUIREMENTS.md`. The decisive third observation runs after
it, outside any plan, and the handoff hands it to the operator.

Output: `73-CLOSEOUT-GUARD.md` § "Re-verification at phase close", and `73-HANDOFF.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CHANGELOG-EVIDENCE.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-GREEN-TREE-EVIDENCE.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PREFLIGHT-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run project tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line. The verify fails on any key line in `73-CLOSEOUT-GUARD.md` containing an opening
parenthesis.

**Scratch files.** Parallel executors share the session scratchpad. Put any captured output under your
own `mktemp -d` directory with a `p7307_` prefix.

**Locale.** The host runs `LANG=ja_JP.UTF-8`; prefix `LANG=C LC_ALL=C` to any `git` command whose
message you quote for comparison.

**Reserved name.** Never create `73-VERIFICATION.md`; `gsd-verifier` owns it and overwrites it
wholesale. Do not edit `.planning/REQUIREMENTS.md` (except to revert a divergence),
`.planning/ROADMAP.md` or `.planning/STATE.md`, and do not call any GSD state-writing verb or the GSD
commit helper; commit with plain `git commit`.
</worktree_provisioning>

<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: v0.9.4 -->
<!-- planner-discipline-allow: v0.9.5 -->
<!-- planner-discipline-allow: gh release create|workflow run update-pin|git tag -a|git tag v -->
<!-- planner-discipline-allow: UI hint -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- plan-criteria-allow: R4 - the only match is the text git tag inside a quoted negative-grep alternation, not a pipeline stage; every real git call in this plan's verifies is captured into a variable first -->
The version tags are named because the probes assert their absence. The release, pin-dispatch and tag
command shapes are forbidden in the handoff, and the gate asserts they are absent. The ROADMAP
UI-hint marker is named because D-15 requires it absent. `## HALT` is written only on a failure path.

## Planning-time census (2026-09-14; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| REL-14 in REQUIREMENTS.md | checkbox at `:22` (`- [ ] **REL-14**: Close prep only, unpublished. CHANGELOG bullet(s) go under the existing …`), Traceability row `:57` (`\| REL-14 \| Phase 73 \| Pending \|`), coverage lines `:62` and `:65` | `grep -n` |
| Required checks on `main` | `Test Python 3.12 on ubuntu-latest`, `Test Python 3.13 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`; `strict: true` | `72-BASE-EVIDENCE.md`; `73-PREFLIGHT-EVIDENCE.md` (wave 2) |
| Merge method | #143 (`58d578f2`) and #145 (`383a07e9`) are `Merge pull request` commits on `main`'s first-parent history | `73-PREFLIGHT-EVIDENCE.md` |
| Dependabot | #146 `build` 1.5.0 → 1.6.1; #147 `ruff` 0.16.6 → 0.16.7; #148 `pypdf` 6.14.2 → 6.18.0; #149 `twine` 6.2.0 → 7.0.0; #150 `sphinx-autodoc-typehints` 3.0.1 → 3.13.6 (a `docs`-extra dependency); v0.9.3 precedent: #143, then #142 and #141 | 73-CONTEXT D-06; `73-PREFLIGHT-EVIDENCE.md` |
| Main-checkout re-sync hazard | `uv sync --extra dev` is an exact sync and drops the docs extra in the main checkout; the restore is `--extra dev --extra docs` | CLAUDE.md; ROADMAP constraint 12 |
| Public PR bodies | the sandbox refuses command substitution in `gh` bodies; pass the body as a file | project practice |
| Precedent handoff | `71-HANDOFF.md`: negative opening, ordered steps, per-SC report, items recorded without acting, inline third observation, fence observation, deliberate non-actions | `.planning/milestones/v0.9.4-phases/71-…/71-HANDOFF.md` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: re-verify the REL-14 closeout guard at phase close, reverting any divergence by hand</name>
  <precondition>Plans 73-01 to 73-06 are merged into this worktree's HEAD, their evidence files exist without a HALT heading, and `73-SC1-INVARIANTS.md` reads `SC1_VERDICT = MET`. Otherwise write `## HALT: wave 3 incomplete` in `73-CLOSEOUT-GUARD.md` naming the missing item, and stop.</precondition>
  <reversibility rating="reversible">Evidence appends, plus, only on divergence, a `git checkout` restoring a tracked file to its committed state.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md</files>
  <read_first>
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md (§ "Baseline", § "The lines under guard", § "Re-verification protocol (phase close)", § "For the operator running phase.complete")
    - .planning/REQUIREMENTS.md (the live REL-14 lines)
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md § "Re-verification at phase close" (the comparison-table shape)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-08
  </read_first>
  <action>
Append `## Re-verification at phase close` to `73-CLOSEOUT-GUARD.md` (D-08). Run exactly the commands the file's own protocol names, and record each with its output and a MATCH or DIVERGED verdict against the Baseline:
- `date -u +"%Y-%m-%dT%H:%M:%SZ"`, key `CLOSE_AT`
- `sha256sum .planning/REQUIREMENTS.md`, key `REQ_SHA256_CLOSE`, set in a two-row table beside `REQ_SHA256_BASE`
- `wc -l < .planning/REQUIREMENTS.md`, key `REQ_LINES_CLOSE`
- `git diff --name-only -- .planning/REQUIREMENTS.md`, empty
- `git log --oneline "$PHASE_BASE_SHA"..HEAD -- .planning/REQUIREMENTS.md`, empty: no commit in the phase touched the file
- `grep -n 'REL-14' .planning/REQUIREMENTS.md`, compared line by line, in file order, with § "The lines under guard" (edge: ordering)

Write `REQ_VERDICT_CLOSE = MATCH` only if every comparison holds. Then quote REL-14's checkbox line and its Traceability row as read directly from the file (`- [ ]`, `Pending`), never inferred from any SUMMARY frontmatter. Every plan of this phase declares `requirements-completed: []`.

**On any divergence:**
- Record the verbatim `git diff -- .planning/REQUIREMENTS.md`.
- Run `git checkout -- .planning/REQUIREMENTS.md`, then re-run the checksum and the grep to show the Baseline restored.
- Record the sequence under `### Divergence detected and reverted`, write `REQ_VERDICT_CLOSE = REVERTED`, and report it. Never commit the divergence.

Close the section by confirming that § "For the operator running phase.complete" is present and unedited. The decisive third observation runs after this plan, outside any plan's reach, and `73-HANDOFF.md` reproduces it.

Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; G="$D/73-CLOSEOUT-GUARD.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; k() { sed -n "s/^$1 = //p" "$G" | head -n 1; } &amp;&amp; PB="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(k REQ_SHA256_CLOSE)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(wc -l &lt; .planning/REQUIREMENTS.md)" = "$(k REQ_LINES_BASE)" ] &amp;&amp; [ "$(k REQ_LINES_CLOSE)" = "$(k REQ_LINES_BASE)" ] &amp;&amp; [ -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" ] &amp;&amp; RC="$(git log --format=%h "$PB"..HEAD -- .planning/REQUIREMENTS.md)" &amp;&amp; [ -z "$RC" ] &amp;&amp; grep -qE '^- \[ \] \*\*REL-14\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-14 \| Phase 73 \| Pending' .planning/REQUIREMENTS.md &amp;&amp; [ "$(grep -c 'REL-14' .planning/REQUIREMENTS.md)" = "$(k REL14_HITS_BASE)" ] &amp;&amp; RB="$(git show "$PB:.planning/REQUIREMENTS.md")" &amp;&amp; [ -n "$RB" ] &amp;&amp; [ "$(grep -n 'REL-14' .planning/REQUIREMENTS.md)" = "$(printf '%s\n' "$RB" | grep -n 'REL-14')" ] &amp;&amp; [ "$(k REQ_VERDICT_CLOSE)" = MATCH ] &amp;&amp; [ -n "$(k CLOSE_AT)" ] &amp;&amp; grep -qxF '## Re-verification at phase close' "$G" &amp;&amp; grep -qxF '## For the operator running phase.complete' "$G" &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$G" &amp;&amp; ! grep -q '^## HALT' "$G"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- `PHASE_BASE_SHA` unreadable or not an ancestor of HEAD
- the live digest or `REQ_SHA256_CLOSE` differing from `REQ_SHA256_BASE`, or the live or recorded line count differing from `REQ_LINES_BASE`
- a working-tree change to REQUIREMENTS.md, or any commit since the phase base touching it
- the REL-14 checkbox checked, its row not Pending, or the hit count changed
- the `grep -n REL-14` output differing, line by line, from the same grep at `PHASE_BASE_SHA`
- `REQ_VERDICT_CLOSE` not MATCH, or `CLOSE_AT` or either section missing
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Re-verification at phase close` records `CLOSE_AT`, `REQ_SHA256_CLOSE` beside `REQ_SHA256_BASE`, `REQ_LINES_CLOSE`, the empty name-only diff, the empty `git log` over the file since `PHASE_BASE_SHA`, and the REL-14 grep compared line by line, each with a MATCH or DIVERGED verdict.
    - `REQ_VERDICT_CLOSE = MATCH`, and REL-14 is quoted as `- [ ]` and `Pending` from the file itself.
    - Any divergence is recorded under `### Divergence detected and reverted`, reverted with `git checkout -- .planning/REQUIREMENTS.md`, and never committed.
  </acceptance_criteria>
  <done>The REL-14 fence is re-verified at phase close with an explicit verdict, and the decisive third observation is left, fully specified, for the operator.</done>
</task>

<task type="auto">
  <name>Task 2: Author 73-HANDOFF.md: negative-first, the ordered /gsd-complete-milestone steps with the conditional branch update and the Dependabot order, the items recorded without acting, the third observation reproduced inline, and a live fence observation</name>
  <precondition>Task 1 is committed, and `REQ_VERDICT_CLOSE = MATCH`.</precondition>
  <reversibility rating="reversible">One new planning document. It records steps for a later command and performs none of them.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-HANDOFF.md</files>
  <read_first>
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-HANDOFF.md (the structure to mirror: negative opening, ordered steps, per-SC report, "Recorded without acting", inline third observation, "Fence observation", "What this phase deliberately did not do")
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-06, D-07, D-10, D-11 and D-15, and the Phase Boundary
    - every evidence file of this phase: 73-CHANGELOG-EVIDENCE.md, 73-CLOSEOUT-GUARD.md, 73-SC1-INVARIANTS.md, 73-GREEN-TREE-EVIDENCE.md, 73-CI-EVIDENCE.md, 73-PREFLIGHT-EVIDENCE.md (the sections and keys to cite)
    - .planning/REQUIREMENTS.md (REL-14, quoted verbatim)
    - .planning/ROADMAP.md § "Phase 73" SC#4 and binding constraints 5, 10 and 13
  </read_first>
  <action>
Create `73-HANDOFF.md`. Its first line is `# Phase 73: v0.9.5 Close Prep — Handoff for /gsd-complete-milestone`.

**The opening (D-10).** Lines 2 to 14 contain no `##` heading. In plain words they state:
- this milestone publishes nothing: no tag, local or remote; no PyPI upload; no GitHub Release; no version bump, since `pyproject.toml` stays `0.9.2`
- the `typsphinx-doc-translations` `update-pin.yml` dispatch and the Read the Docs `stable` check are not applicable, because nothing is released
- recorded as facts, not steps: the daily `update-pin.yml` schedule moves the Japanese `latest` site onto the merged `main` with no action from anyone; `/en/latest/` rebuilds on the `main` push; `stable` stays on `v0.9.2` until the next published release (constraint 5)
- the one irreversible action of the close is REL-14: a pull request from `gsd/v0.9.5-docs-link-check-and-navigation` to `main`, opened and merged at `/gsd-complete-milestone`, as REL-13 was with PR #145

Then these sections, in order.

**1. `## What /gsd-complete-milestone does, in order` (D-10).** Numbered steps, each with its exact command and one line of why:

0. **Fence first.** Back up `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` to a scratch directory outside the repository. Then run the section 4 probes, and start only on a MATCH.

1. **Re-run the trial merge.**
   - `git fetch origin`, then `git merge-tree --write-tree HEAD origin/main; echo "exit:$?"`, which requires `exit:0`.
   - Cite `73-PREFLIGHT-EVIDENCE.md`'s `MERGE_TREE`, `MAIN_MOVED`, `LOCK_CHECK_EXIT`, `MERGED_RUFF_LOCK_VERSION` and `TRIAL_MERGE_VERDICT` as the phase-time result. That tree SHA changes with every commit, so it is re-measured here.
   - Then `git merge-base --is-ancestor origin/main HEAD; echo "exit:$?"`. `exit:0` means `main` has not moved past the milestone base `098a8ff6`. `exit:1` means it moved (for example a Dependabot pull request landed, D-07): re-run the scratch `uv lock --check` and the merged-tree `ruff check .` exactly as `73-PREFLIGHT-EVIDENCE.md` § "Merged lock" and § "Merged-tree lint" did. That `ruff` verdict is authoritative, and a finding is the owner's decision before step 4. No docs build of the merged tree is added (D-07).

2. **Conditional branch update (D-10).** Only if step 1 printed `exit:1`: in the main checkout, on `gsd/v0.9.5-docs-link-check-and-navigation`, run `git merge --no-ff origin/main -m "Merge origin/main into gsd/v0.9.5-docs-link-check-and-navigation"`. Never rebase the milestone's commits. Then run `uv lock --check`, then `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` (the docs extra is named because a bare `--extra dev` exact sync drops it from the main checkout), and re-check that `git merge-base --is-ancestor origin/main HEAD` now exits 0. Otherwise, record this step as a no-op with step 1's `exit:0` output. The reason: `main`'s protection is `strict: true` (`73-PREFLIGHT-EVIDENCE.md` § "main protection (D-09)"), so the pull request cannot merge until the branch is up to date.

3. **Push.** `git push --no-follow-tags origin gsd/v0.9.5-docs-link-check-and-navigation`, a fast-forward from `73-CI-EVIDENCE.md`'s `PUSHED_SHA`. It carries this phase's later `.planning/` commits, and the merge commit if step 2 ran.

4. **Open the pull request.** `gh pr create --base main --head gsd/v0.9.5-docs-link-check-and-navigation --title <English title> --body-file <file>`. Title and body are English and terse. The body goes through a file passed with `--body-file`, never through a command substitution.

5. **Wait for the required checks.** Wait for all six to be green on the pull request head, named literally: `Test Python 3.12 on ubuntu-latest`, `Test Python 3.13 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage` and `Build Package`. The `pull_request` trigger runs the whole `ci.yml`; the six are the ones protection requires.

6. **Merge.** `gh pr merge <number> --merge`: a merge commit, never squash or rebase. Cite `73-PREFLIGHT-EVIDENCE.md` § "Merge-method precedent (D-10)", where #143 and #145 are `Merge pull request` commits.

7. **Observe what REL-14 requires, then close it** (SC#4, D-10). Immediately after the merge, before any Dependabot merge:
   - The merge commit is on `origin/main`: `git fetch origin`, then `git log --first-parent -1 --format='%h %s' origin/main`.
   - `git show origin/main:pyproject.toml`, line 7, reads `version = "0.9.2"`.
   - `git ls-remote --tags origin` shows `refs/tags/v0.9.2` (positive control) and no v0.9.3, v0.9.4 or v0.9.5 tag.
   - PyPI returns 404 for `0.9.5`, and for `0.9.3` and `0.9.4`, and 200 for `0.9.2`, the positive control.
   - `gh release list` shows `v0.9.2` as Latest and no v0.9.3, v0.9.4 or v0.9.5 Release.

   Only on those five observations is REL-14's box checked, at `/gsd-complete-milestone`, never inside a plan.

8. **Only then, the Dependabot pull requests (D-06).** Name #146 (`build`), #147 (`ruff`), #148 (`pypdf`), #149 (`twine`) and #150 (`sphinx-autodoc-typehints`, a `docs`-extra dependency), each with its version range from the census. Write the order as the literal phrase "milestone pull request first, Dependabot second", as v0.9.3 did with #143 and then #142 and #141. Dependabot rebases the remaining `uv.lock`-only pull requests on its own after each merge, and each runs its own checks against `main`, which covers #150's documentation build (D-07).

9. **Not applicable.** No tag; no `release.yml` run; no PyPI upload; no GitHub Release; no `update-pin.yml` dispatch on `typsphinx-doc-translations`; no Read the Docs `stable` check. Read the Docs `latest` rebuilds from `main` and then shows the six `## [Unreleased]` bullets, which needs no action.

**2. `## What this phase satisfied`.** Quote REL-14's checkbox line verbatim from `.planning/REQUIREMENTS.md`, and state that REL-14 stays open until step 7. Report each of ROADMAP SC#1 to SC#4 as MET or NOT MET, citing each evidence file and section rather than re-deriving the verdict:
- SC#1: `73-SC1-INVARIANTS.md`, both observations, the close-tip milestone fences, the phase-scoped product diff, and `SC1_VERDICT`
- SC#2: `73-CHANGELOG-EVIDENCE.md` § "Pure-addition proof", § "Fence assertions", § "Bullet content assertions" and § "Changelog page gate", and `73-GREEN-TREE-EVIDENCE.md` § "Changelog page gate" (zero skipped)
- SC#3: `73-GREEN-TREE-EVIDENCE.md`, `73-CI-EVIDENCE.md` and `73-PREFLIGHT-EVIDENCE.md`, set side by side in one table:
  - the local suite summary and the `LC_ALL=C` summary
  - the local ruff version, `CI_RUFF_VERSION` and `MERGED_RUFF_LOCK_VERSION`
  - the docs `_FINAL` counts against the `_BASE` counts, and the zero multiple-toctrees readings
  - `LINKCHECK_TOTAL` and `LINKCHECK_WORKING` with `LINKCHECK_VERDICT`
  - `RUN_ID` with its `JOB_COUNT`, both `windows-latest` and both `macos-latest` lanes named
  - the trial merge's `MERGE_TREE`, `MERGE_RC`, `LOCK_CHECK_EXIT` and `MERGED_RUFF_EXIT`
  - the required contexts, unchanged since Phase 72's base
- SC#4: `73-CLOSEOUT-GUARD.md` § "Re-verification at phase close", and this file

Add the D-12 statement: the single dispatch ran on `PUSHED_SHA`, the tip carrying every product-tree change of the phase, and every later commit is `.planning/`-only (`73-SC1-INVARIANTS.md` § "Commits after the CI dispatch").

**3. `## Recorded without acting`.**
- **D-06:** quote the Dependabot census from `73-PREFLIGHT-EVIDENCE.md` § "Dependabot and open pull requests (D-06)" with its `PR_CENSUS_AT` value, `OPEN_PRS` and `DEPENDABOT_OPEN_PRS`, and each of #146 to #150 with package, version range and state. Add a live re-read here, `gh pr list --state open --json number,title,author,headRefName,baseRefName`, with its `date -u +%FT%TZ`, and for each of the five the live count of comments and reviews by the gh account (`GH_ACTOR`), which must equal the census. Name any change of state since the census, and any new pull request, left untouched. This phase took no action on any pull request.
- **D-07:** a Dependabot merge on `main` after the census is caught by step 1, and no docs build of `main` plus #150 is added.
- **D-11:** the `0.9.3`, `0.9.4` and `0.9.5` version numbers are all unclaimed, not decided. After this phase `## [Unreleased]` holds six bullets from three unpublished milestones: four under `### Changed`, plus this phase's `### Added` and `### Fixed`. The next release-prep phase promotes all of them into its versioned section; which number the next published release uses belongs to that milestone's scoping.

**4. `## Before and after phase.complete-family tooling`.** Reproduce `73-CLOSEOUT-GUARD.md` § "For the operator running phase.complete" inline:
- Name both entry points: `phase.complete` reached from `/gsd-execute-phase`, and `/gsd-verify-work`'s inline transition.
- The scratch backup of the three files comes before either.
- Then the probes: `sha256sum .planning/REQUIREMENTS.md` against the baseline digest written out in full, `wc -l`, `git diff --name-only -- .planning/REQUIREMENTS.md`, and `grep -n 'REL-14' .planning/REQUIREMENTS.md` against the four baseline lines written out.
- The reversion is `git checkout -- .planning/REQUIREMENTS.md`, after which ROADMAP.md and STATE.md are diffed against the backup; STATE.md's `current_phase_name` and ROADMAP.md's Status cell are hand-corrected if the tooling rewrote them.
- The rule: reverted and reported, never committed, and no `/gsd-complete-milestone` step starts until the probes MATCH.
- State the history: the flip has landed at 8 of the 9 prior release-prep closes and did not fire at Phase 71; one non-firing is not treated as a fix (ROADMAP constraint 10).

**5. `## Fence observation`.** Record live, at this task's own time:
- `git diff --name-only -- .planning/REQUIREMENTS.md`, empty
- `sha256sum .planning/REQUIREMENTS.md`, equal to `REQ_SHA256_BASE`
- `git tag -l 'v0.9.3'`, `git tag -l 'v0.9.4'` and `git tag -l 'v0.9.5'`, all empty
- `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml uv.lock .planning/REQUIREMENTS.md`, empty
- `git diff --name-only "$PUSHED_SHA" HEAD -- . ':(exclude).planning'`, empty, with `PUSHED_SHA` read from `73-CI-EVIDENCE.md`: the product tree CI tested is the product tree of this tip
- the Phase 73 section of `.planning/ROADMAP.md` carries no UI-hint line (D-15: the UI-gate false positive was handled at plan time with `--skip-ui`, and no hint line was added)

**6. `## What this phase deliberately did not do`.** A bullet list:
- no tag, local or remote
- no `release.yml` run
- nothing uploaded to PyPI
- no GitHub Release
- no version bump
- no pull request opened, merged or commented on
- no `origin/main` merged into the branch
- no `update-pin.yml` dispatch
- no action on any Dependabot pull request (D-06)
- no REQUIREMENTS.md edit of any kind, checkbox, Traceability row or otherwise (D-08)
- no product-tree change beyond `CHANGELOG.md`, and no `docs/source/conf.py` linkcheck key
- no UI-hint line added to the ROADMAP (D-15)
- one push and one `ci.yml` dispatch only, in 73-04 (D-12)

The handoff contains no command that tags, creates a release or dispatches the translations pin workflow.

Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; H="$D/73-HANDOFF.md" &amp;&amp; G="$D/73-CLOSEOUT-GUARD.md" &amp;&amp; PF="$D/73-PREFLIGHT-EVIDENCE.md" &amp;&amp; CE="$D/73-CI-EVIDENCE.md" &amp;&amp; test -f "$H" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$G}" | head -n 1; } &amp;&amp; [ "$(head -n 1 "$H")" = '# Phase 73: v0.9.5 Close Prep — Handoff for /gsd-complete-milestone' ] &amp;&amp; O="$(head -n 14 "$H")" &amp;&amp; printf '%s\n' "$O" | grep -qi 'publishes nothing' &amp;&amp; for w in 'no tag' 'PyPI' 'GitHub Release' 'version bump' 'update-pin.yml' 'Read the Docs' 'stable' 'latest' 'daily' 'not applicable' 'pull request'; do printf '%s\n' "$O" | grep -qiF -- "$w" || { echo "opening lacks: $w"; exit 1; }; done &amp;&amp; [ "$(printf '%s\n' "$O" | grep -c '^##')" = 0 ] &amp;&amp; for n in 'Test Python 3.12 on ubuntu-latest' 'Test Python 3.13 on ubuntu-latest' 'Lint and Format Check' 'Type Check' 'Code Coverage' 'Build Package' 'git merge --no-ff origin/main' 'no-op' 'gh pr create --base main --head gsd/v0.9.5-docs-link-check-and-navigation' '--body-file' 'gh pr merge' '--merge' 'strict: true' '#143' '#145' '#146' '#147' '#148' '#149' '#150' 'pypdf' 'twine' 'sphinx-autodoc-typehints' 'milestone pull request first, Dependabot second' 'unclaimed' 'next release-prep phase' '0.9.3' '0.9.4' '0.9.5' '404' '098a8ff6' '/gsd-execute-phase' '/gsd-verify-work' 'git checkout -- .planning/REQUIREMENTS.md' 'merge-tree' '--extra dev --extra docs' '73-PREFLIGHT-EVIDENCE.md' '73-CI-EVIDENCE.md' '73-GREEN-TREE-EVIDENCE.md' '73-CHANGELOG-EVIDENCE.md' '73-SC1-INVARIANTS.md' '73-CLOSEOUT-GUARD.md' '**REL-14**: Close prep only, unpublished. CHANGELOG bullet(s) go under the existing'; do grep -qF -- "$n" "$H" || { echo "handoff lacks: $n"; exit 1; }; done &amp;&amp; grep -qF "$(k REQ_SHA256_BASE)" "$H" &amp;&amp; PCT="$(k PR_CENSUS_AT "$PF")" &amp;&amp; [ -n "$PCT" ] &amp;&amp; grep -qF "$PCT" "$H" &amp;&amp; MTR="$(k MERGE_TREE "$PF")" &amp;&amp; [ -n "$MTR" ] &amp;&amp; grep -qF "$MTR" "$H" &amp;&amp; RID="$(k RUN_ID "$CE")" &amp;&amp; [ -n "$RID" ] &amp;&amp; grep -qF "$RID" "$H" &amp;&amp; ! grep -qE 'gh release create|workflow run update-pin|git tag -a|git tag v' "$H" &amp;&amp; for s in '## What /gsd-complete-milestone does, in order' '## What this phase satisfied' '## Recorded without acting' '## Before and after phase.complete-family tooling' '## Fence observation' '## What this phase deliberately did not do'; do grep -qxF "$s" "$H" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; for v in 3 4 5; do [ -z "$(git tag -l "v0.9.$v")" ] || { echo "local tag: v0.9.$v"; exit 1; }; done &amp;&amp; test ! -e "$D/73-VERIFICATION.md" &amp;&amp; A="$(k GH_ACTOR "$PF")" &amp;&amp; [ -n "$A" ] &amp;&amp; for p in 146 147 148 149 150; do [ "$(gh pr view "$p" --json comments,reviews -q "[.comments[], .reviews[] | select(.author.login == \"$A\")] | length")" = "$(k "PR_${p}_ACTOR_TOUCHES" "$PF")" ] || { echo "actor touches changed: #$p"; exit 1; }; done &amp;&amp; RM="$(awk '/^### Phase 73:/{f=1} f &amp;&amp; /^## Progress/{exit} f' .planning/ROADMAP.md)" &amp;&amp; [ -n "$RM" ] &amp;&amp; [ "$(printf '%s\n' "$RM" | grep -c 'UI hint' || true)" = 0 ] &amp;&amp; P="$(k PUSHED_SHA "$CE")" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml uv.lock .planning/REQUIREMENTS.md)" ]</automated>
    <fails_when>Non-zero exit, or `opening lacks: WORD`, `handoff lacks: TEXT`, `missing section: NAME`, `local tag: TAG` or `actor touches changed: #N` printed. Each of these makes it fail:
- the handoff's first line not the exact title
- the first 14 lines not stating that the milestone publishes nothing, missing any negative item, the not-applicable pair, the daily-schedule fact or the pull request, or carrying a `##` heading
- a required check name, the conditional merge and its no-op, the pull-request command or `--body-file`, the merge command, the strict reason, either precedent PR, any of #146 to #150 or the three named packages, the Dependabot order phrase, the unclaimed versions, the next release-prep promotion, the 404 observation, the milestone base, either entry point, the reversion, merge-tree, the re-sync extras, an evidence citation or the verbatim REL-14 line missing
- the baseline digest, 73-05's census time, the trial-merge tree or the CI run id absent
- a tag, release-creation or pin-dispatch command present
- a section missing
- the live REQUIREMENTS.md digest diverged, or a v0.9.3, v0.9.4 or v0.9.5 tag locally
- `73-VERIFICATION.md` authored
- a live count of comments and reviews by the gh account on #146 to #150 differing from the census
- the Phase 73 ROADMAP section missing or carrying a UI-hint line
- a product-tree change after `PUSHED_SHA`, or a product file or REQUIREMENTS.md modified</fails_when>
  </verify>
  <acceptance_criteria>
    - Lines 1 to 14 of `73-HANDOFF.md` state the negative before any `##` heading: no tag, PyPI upload, GitHub Release or version bump; `update-pin.yml` and Read the Docs `stable` not applicable; the daily schedule, the `latest` rebuild and `stable` staying on `v0.9.2` recorded as facts; the REL-14 pull request as the one irreversible action (D-10).
    - `## What /gsd-complete-milestone does, in order` gives steps 0 to 9 with exact commands: the re-run trial merge with the moved-main branch (D-07), the conditional `git merge --no-ff origin/main` with its no-op case and the `strict: true` reason, the `--extra dev --extra docs` re-sync, the push, `gh pr create --base main --head gsd/v0.9.5-docs-link-check-and-navigation` with `--body-file`, the six required checks named literally, `gh pr merge --merge` citing #143 and #145, the five post-merge REL-14 observations, then the Dependabot pull requests with the order "milestone pull request first, Dependabot second" (D-06, D-10, SC#4).
    - `## What this phase satisfied` quotes REL-14 verbatim and reports SC#1 to SC#4, each citing its evidence section. The SC#3 local, CI, linkcheck and trial-merge results are set side by side, and the D-12 final-tip statement is included.
    - `## Recorded without acting` carries the Dependabot census with `PR_CENSUS_AT` and a live re-read with the untouched counts (D-06), the moved-main note (D-07), and the three unclaimed version numbers with the six-bullet promotion (D-11).
    - `## Before and after phase.complete-family tooling` names both entry points and gives the backup, the digest, the REL-14 lines and the reversion inline.
    - `## Fence observation` is live, matches the Baseline, shows the product tree unchanged since `PUSHED_SHA`, and records the absent UI-hint line (D-15); `## What this phase deliberately did not do` lists every non-action.
    - No command that tags, creates a release or dispatches the pin workflow appears anywhere in the file.
  </acceptance_criteria>
  <done>`73-HANDOFF.md` stands alone. It opens by stating that nothing is published, and it gives the operator every `/gsd-complete-milestone` step in order: the conditional branch update the protection demands, the checks to wait for, the merge, the five observations REL-14 closes on, the Dependabot pull requests after the milestone, the items left untouched, and the fence to run after the tooling.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`73-01-PLAN.md`. This plan carries P1, P2, P3 and P5, and it authors the `ordering` edge (the REL-14
grep compared line by line in file order), the `encoding` edge (the raw-byte SHA-256 fence), the
`idempotency` edge (the fence probes re-run and MATCH) and the `empty` edge's handoff half (an
unmoved `main` makes the branch-update step a recorded no-op). It carries residual assumptions (b)
and (c) into the handoff: `merge-tree` is re-run immediately before the pull request because
`origin/main` may move, and the Dependabot pull requests are reported with their measurement times,
not as standing facts. It also carries the concurrency guarantee that no plan writes REQUIREMENTS.md,
with the decisive third observation reproduced where the operator reaches it after every plan has
finished. Phase accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/REQUIREMENTS.md` → `phase.complete`-family tooling after this plan | the flip lands outside every plan's reach |
| `73-HANDOFF.md` → the operator at `/gsd-complete-milestone` | read as instructions; a mis-framed step causes a real publish, a rebase, a merge before protection is satisfied, or a Dependabot merge ahead of the milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-31 | Tampering | the REL-14 flip landing after this plan through either transition entry point | high | mitigate | Close-time re-verification with an explicit verdict and a line-by-line grep comparison against `PHASE_BASE_SHA`, and the third observation reproduced inline with both entry points, a scratch backup, the digest and the `git checkout` reversion. |
| T-73-32 | Elevation of Privilege | a handoff read as licence to tag, release or dispatch the pin workflow for a milestone that publishes nothing (ASVS V4, L1, by analogy) | high | mitigate | Negative-first opening gated on its first 14 lines; step 9 lists the not-applicable actions; the verify asserts no tag, release-creation or pin-dispatch command anywhere in the file. |
| T-73-33 | Tampering | a rebase or squash at close rewriting the milestone's history, or a merge before protection is satisfied | medium | mitigate | Step 2 names `git merge --no-ff origin/main` conditionally and forbids a rebase; step 5 names the six required checks; step 6 names `gh pr merge --merge`; each cites measured precedent and the `strict: true` reason. |
| T-73-34 | Repudiation | a `main` that moved after the pre-flight (a Dependabot `ruff` bump) merged without re-measurement, or a Dependabot pull request merged ahead of the milestone (D-06, D-07) | medium | mitigate | Step 1 re-runs `merge-tree` and, when `main` moved, the scratch lock check and merged-tree `ruff check .`; step 8 comes after the merge and the REL-14 observations, with the order phrase written literally. |
| T-73-35 | Injection | a pull-request body built with command substitution from untrusted content | low | mitigate | Step 4 requires `--body-file`, never command substitution; the verify requires `--body-file` in the handoff. |
| T-73-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins; the handoff's re-sync step does the same after any merge. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `73-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`73-HANDOFF.md` and appends `## Re-verification at phase close` to `73-CLOSEOUT-GUARD.md`, with the
keys `CLOSE_AT`, `REQ_SHA256_CLOSE`, `REQ_LINES_CLOSE` and `REQ_VERDICT_CLOSE`. It modifies no
product-tree file and never edits `.planning/REQUIREMENTS.md`, except to revert a divergence. No file
named `73-VERIFICATION.md` is produced. The orchestrator, not a plan, later appends the third
observation to `73-CLOSEOUT-GUARD.md`.
</artifacts_this_phase_produces>

<verification>
- The REL-14 digest matches the phase-head Baseline at close, the REL-14 grep is line-for-line
  identical, and REL-14 is `[ ]` and Pending.
- `73-HANDOFF.md` opens negative-first, lists the ordered close steps with the conditional
  strict-protection branch update, the five post-merge observations and the Dependabot pull requests
  after the milestone, records the version numbers and pull requests without acting, reproduces the
  third observation, and closes with a live fence.
- No UI-hint line in the Phase 73 ROADMAP section.
</verification>

<success_criteria>
ROADMAP SC#4: the REL-14 checkbox is proven held at phase close by the recorded SHA-256, and
`73-HANDOFF.md` is standalone. It carries the pull request, the checks, the merge, the explicit
no-publish statement, both not-applicable items, the post-merge observations and the Dependabot order
(D-06, D-07, D-08, D-10, D-11, D-15).
</success_criteria>

<output>
Create `.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-07-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-08).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run `phase.complete` or any other GSD state-writing verb.
- The first section states `REQ_VERDICT_CLOSE`, `CLOSE_AT` and the handoff path. It ends by reminding
  the orchestrator that the third fence observation, with a scratch backup first, is owed after
  `phase.complete` and again after `/gsd-verify-work`'s transition.
</output>
