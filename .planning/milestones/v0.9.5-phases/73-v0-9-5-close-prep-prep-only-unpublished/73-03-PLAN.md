---
phase: 73-v0-9-5-close-prep-prep-only-unpublished
plan: 03
type: execute
wave: 2
depends_on: ["73-01", "73-02"]
files_modified:
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-GREEN-TREE-EVIDENCE.md
autonomous: true
requirements: [REL-14]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC#3 local half, run on this phase''s own merged tree. The worktree is provisioned with `--extra dev --extra docs --python 3.13.13`, `typsphinx.__file__` resolves inside the worktree, `myst_parser` imports, and the `.venv/pyvenv.cfg` `home` and `version_info` of both this worktree and the main checkout are recorded before any count is compared.'
    - 'D-05: the product-tree delta from PHASE_BASE_SHA (read from 73-CLOSEOUT-GUARD.md) to HEAD is exactly CHANGELOG.md with zero deletions, docs/source/conf.py is unchanged, and the merge-base of HEAD and origin/main still equals MILESTONE_BASE (read from 73-SC1-INVARIANTS.md), so no main commit was absorbed.'
    - 'The full pytest suite runs with `-rs`: FULL_FAILED = 0, every skip reason is transcribed, FULL_PASSED equals the passed count in the saved log, and COLLECTED_73_03 equals the live `--collect-only` count. A second full run under `LC_ALL=C` also has 0 failed. Phase 72''s PYTEST_PASSED and PYTEST_SKIPPED are set beside the summary, with the interpreter of each noted.'
    - '`black --check .`, `mypy typsphinx/` and `ruff check .` exit 0 in the worktree, and RUFF_LOCAL_VERSION equals the ruff version pinned in uv.lock. CI remains the lint authority, and 73-04 reads its verdict.'
    - 'The @preview and README version-sync family passes: tests/test_preview_version_sync.py, tests/test_readme_version_sync.py and tests/test_extension.py::test_version_matches_pyproject_toml.'
    - 'D-14: `tests/test_changelog_page_gate.py` runs with the docs extra present and reports 0 skipped, so the changelog page gate actually executed on the tree carrying both new subsections.'
    - 'D-14: both docs tox environments, built after `rm -rf docs/_build` and under `LANG=C LC_ALL=C`, report warning counts equal to 73-01''s clean phase-base DOCS_HTML_WARN_BASE and DOCS_PDF_WARN_BASE, with zero `document is referenced in multiple toctrees` lines, and the PDF begins with %PDF-.'
    - 'D-14: `tox -e linkcheck` runs from a clean output directory; the passing run''s output.json has every row `working` (working count equal to the total, read from the JSON, never from console text), in at most three runs. A transient row triggers a clean re-run; anything else, or a URL failing transiently on two runs, stops under a HALT heading for the owner. No `linkcheck_*` key is added to docs/source/conf.py, because CHANGELOG.md is the only product-tree file this phase authors (D-05).'
    - 'REL-14 is cited for coverage only; SUMMARY requirements-completed: [] (D-08).'
  artifacts:
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-GREEN-TREE-EVIDENCE.md
      provides: 'tree identity, product delta and non-absorption check, full suite twice (once under LC_ALL=C), format/type/lint, version-sync family, changelog page gate, clean-build docs against the phase-base baseline with the multiple-toctrees census, linkcheck runs and verdict, executed-versus-skipped table, SC#3 local verdict'
      contains: 'SC3_LOCAL_VERDICT = '
  key_links:
    - from: '73-CLOSEOUT-GUARD.md PHASE_BASE_SHA'
      to: 'the product-tree delta check'
      via: 'git diff --name-only PHASE_BASE_SHA HEAD outside .planning must print exactly CHANGELOG.md'
      pattern: 'PHASE_BASE_SHA = '
    - from: '73-SC1-INVARIANTS.md MILESTONE_BASE'
      to: 'the non-absorption check'
      via: 'git merge-base HEAD origin/main must still equal it'
      pattern: 'MILESTONE_BASE = '
    - from: '73-CHANGELOG-EVIDENCE.md DOCS_HTML_WARN_BASE and DOCS_PDF_WARN_BASE'
      to: 'DOCS_HTML_WARN_FINAL and DOCS_PDF_WARN_FINAL'
      via: 'equality of clean-build counts'
      pattern: 'DOCS_PDF_WARN_FINAL = '
  prohibitions:
    - statement: 'MUST NOT record a skipped test as a pass, an incremental-build or localised warning count as a measurement, a console link count in place of output.json, or a count from RESEARCH, CONTEXT, 73-01 or this plan in place of a run executed here.'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT edit any tracked file other than this plan evidence and SUMMARY (in particular, no linkcheck key in docs/source/conf.py), and MUST NOT merge origin/main into the branch or re-provision against main''s lock. A red gate is recorded under a HALT heading for the owner, never fixed here (D-05, D-14).'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Prove the phase's own merged tree green on runs executed here (ROADMAP SC#3, local half; D-14):
- the full pytest suite, run twice, once under `LC_ALL=C`
- `black --check .`, `mypy typsphinx/` and `ruff check .`
- the version-sync guard family
- the changelog page gate, with the docs extra actually present and 0 skipped
- both docs tox environments, built clean under the C locale, against the clean phase-base baseline
  73-01 recorded, with zero `multiple toctrees` lines
- `tox -e linkcheck` from a clean output directory, with every checked link `working`, classified
  under Phase 72's linkcheck failure policy (its CONTEXT decisions one to three) as D-14 reuses it

Purpose: SC#3 requires runs executed in this phase, not a prior phase's word, and the tree measured
must be this phase's own tip, not one that absorbed `main`. This plan runs in wave 2, after 73-01's
`CHANGELOG.md` edit and 73-02's guard have merged, and in parallel with 73-04 (CI) and 73-05
(pre-flight), with which it shares no file and whose evidence it does not read.

Output: `73-GREEN-TREE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-RESEARCH.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CHANGELOG-EVIDENCE.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision this worktree before any command:
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`, then
run everything via `uv run` (`CLAUDE.md` § "Worktree-isolated execution"). The docs extra is
required here: without `myst-parser` the build classes of `tests/test_changelog_page_gate.py` skip
silently, and the suite's green would say nothing about the changelog page. `--python 3.13.13` is
this plan's decision: it is the interpreter of every Phase 72 measurement, so the counts are
comparable, and a fresh worktree's default uv-managed CPython 3.14 cannot import Pillow inside the
FHS sandbox (`libz.so.1`).

**Long runs stay in the foreground.** Every pytest, tox and linkcheck invocation runs in a Bash call
with the `timeout` parameter set to 600000 and never with `run_in_background`. An executor that
backgrounds a long run returns early with no SUMMARY.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line. The verify fails on any key line containing an opening parenthesis.

**Scratch files.** Parallel executors share the session scratchpad. Put every captured output under
your own `mktemp -d` directory, recorded as `SCRATCH_73_03`, with the exact `p7303_` file names this
plan gives; the verify reads them back.

**Locale.** The host runs `LANG=ja_JP.UTF-8`. The second full run and every counted docs build run
under `LANG=C LC_ALL=C`, skip reasons are quoted from `LC_ALL=C` runs, and English Sphinx text is
grepped only under `LANG=C LC_ALL=C`.

**No tracking edits.** Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or
`.planning/STATE.md`, and do not call any GSD state-writing verb or the GSD commit helper; commit with
plain `git commit`.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: [0-9]+ skipped -->
<!-- planner-discipline-allow: [0-9]+ (failed|errors?) -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: ^linkcheck_ -->
`## HALT` is written only on a failure path; the gates require its absence. The `linkcheck_`
line-start pattern is named because the executor runs that exact count against
`docs/source/conf.py` and the gate requires it to be 0 (D-05: no linkcheck key is added).

## Planning-time census (2026-09-14; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| Phase 72 suite | `PYTEST_PASSED = 1547`, `PYTEST_SKIPPED = 1`, `PYTEST_FAILED = 0` on CPython 3.13.13 (nixpkgs `home`) | `72-GATES-EVIDENCE.md` |
| Main checkout interpreter | nixpkgs `python3-3.13.13` | `72-BASE-EVIDENCE.md` `PYVENV_HOME` |
| Changelog page gate | 6 tests; the build classes skip when `myst_parser` is absent; the PDF class also skips without `typst`, a core dependency and therefore present | `tests/test_changelog_page_gate.py:109-219` |
| Version-sync family | `tests/test_readme_version_sync.py`, `tests/test_preview_version_sync.py`, `tests/test_extension.py::test_version_matches_pyproject_toml` | `git ls-files` |
| Local ruff | 0.16.6 per `uv.lock` (Dependabot #147 proposes 0.16.7 on `main`, not on this branch) | `uv.lock`, `gh pr list` |
| Milestone base | `098a8ff6` = `origin/main` at planning: nothing to absorb | 73-CONTEXT `<domain>` |
| Docs and linkcheck environments | `docs-html` → `_build/html`, `docs-pdf` → `_build/pdf/typsphinx.pdf`, `linkcheck` → `_build/linkcheck/output.json`; `linkcheck` is not in `env_list` | `tox.ini:2, 67-97` |
| Phase 72 linkcheck | one run, `TIP_LINKCHECK_TOTAL = 95` = `TIP_LINKCHECK_WORKING`, `CONF_LINKCHECK_KEYS = 0`; a cross-check only, never a threshold (constraint 8) | `72-GATES-EVIDENCE.md`, `72-LINKCHECK-EVIDENCE.md` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: tree identity, product delta and non-absorption, then the full pytest suite on the merged tree</name>
  <precondition>73-01 and 73-02 are merged into this worktree's HEAD: `73-CHANGELOG-EVIDENCE.md`, `73-CLOSEOUT-GUARD.md` and `73-SC1-INVARIANTS.md` exist and none carries a HALT heading, and the `CHANGELOG.md` region between `## [Unreleased]` and `## [0.9.2]` holds `### Added`, `### Changed`, `### Fixed` and six bold-lead bullets. Otherwise write `## HALT: wave 1 not merged` and stop.</precondition>
  <reversibility rating="reversible">Evidence-file creation only.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md (`PHASE_BASE_SHA`)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md (`MILESTONE_BASE`)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CHANGELOG-EVIDENCE.md (the docs baselines and `BASE_73_01`)
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-GREEN-TREE-EVIDENCE.md (the section shape and the anti-stale-editable-install proof)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-GATES-EVIDENCE.md § "Local gate quartet" (`PYTEST_PASSED`, `PYTEST_SKIPPED`)
    - CLAUDE.md § "Worktree-isolated execution" and § "NixOS development shell" ("Interpreters may differ", "Locale")
  </read_first>
  <action>
Create `73-GREEN-TREE-EVIDENCE.md` with the heading `# Phase 73 — Local Green-Tree Evidence (SC#3, local half)`.

1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line from `<worktree_provisioning>`, and its tail
   - before any commit: `BASE_73_03 = ` followed by the output of `git rev-parse HEAD`
   - `SCRATCH_73_03 = ` followed by the path `mktemp -d` printed
   - `git log --oneline -8`

2. **`## Tree identity`.** Record:
   - `uv run python -c "import typsphinx, sys; print(typsphinx.__version__); print(typsphinx.__file__)"`. The path must lie inside this worktree, which proves the worktree's editable copy is measured rather than the main checkout's. Key `TYPSPHINX_FILE`.
   - `uv run python -c "import myst_parser; print(myst_parser.__version__)"`, proving the docs extra is present.
   - `PYVENV_HOME_73_03` and `PYVENV_VERSION_73_03`, from `.venv/pyvenv.cfg`; the version must be `3.13.13`.
   - `MAIN_PYVENV_HOME` and `MAIN_PYVENV_VERSION`, read-only from `/home/yuta/Documents/typsphinx/.venv/pyvenv.cfg`.

   State that counts are compared across trees only after noting any interpreter difference.

3. **`## Product-tree delta and non-absorption`** (D-05).
   - Read `PHASE_BASE_SHA` from `73-CLOSEOUT-GUARD.md`.
   - Record `git diff --stat "$PHASE_BASE_SHA" HEAD -- . ':(exclude).planning'`: exactly one file, `CHANGELOG.md`, with a non-zero insertion count and zero deletions. Key `PRODUCT_DELTA = CHANGELOG.md`.
   - Record `git diff --name-only "$PHASE_BASE_SHA" HEAD -- docs/source/conf.py`, empty.
   - Run `git fetch origin main`, then record `ORIGIN_MAIN_SHA_73_03 = ` from `git rev-parse origin/main`, and `git merge-base HEAD origin/main`, which must equal `MILESTONE_BASE` from `73-SC1-INVARIANTS.md`. Key `MAIN_ABSORBED = no`.
   - Also record `git merge-base --is-ancestor origin/main HEAD; echo "exit:$?"`, and explain it: `exit:0` means `origin/main` has not moved past the milestone base; `exit:1` means `main` moved and the branch did not absorb it, which is D-07's case for plan 73-05's trial merge. Either is consistent with this plan. A merge-base different from `MILESTONE_BASE` is what would breach it; in that case write `## HALT: main absorbed` and stop.

   Explain that the non-empty delta is the positive control on the anchor, and that the single file is why 73-01's docs comparison and this plan's apply to the same product tree.

4. **`## Full suite`.**
   - Record `uv run pytest --collect-only -q -p no:cacheprovider`, extracting the count unanchored: pipe it to `grep -oE '[0-9]+ tests? collected'`, then take the leading number. The summary line is wrapped in `=` decoration, so a line-anchored extraction is always empty. Key `COLLECTED_73_03`.
   - Run `uv run pytest -q -rs -p no:cacheprovider > "$SCRATCH_73_03/p7303_full.log" 2>&1; echo "exit:$?"` in one foreground call with the Bash `timeout` at 600000.
   - Quote the final summary line and the `SKIPPED` short-summary lines, verbatim.
   - Keys, each extracted with unanchored `grep -oE '[0-9]+ passed'` style patterns:
     - `FULL_SUMMARY`: the counts joined with `, `, such as `N passed, M skipped`
     - `FULL_PASSED`: the passed count
     - `FULL_FAILED`: failed plus errors, which must be 0
     - `FULL_SKIPPED`
   - Account for every skip by its printed reason.
   - Quote `PYTEST_PASSED` and `PYTEST_SKIPPED` from `72-GATES-EVIDENCE.md` beside `FULL_SUMMARY`, and state whether they agree on the same interpreter version. A difference is recorded and explained, never absorbed; only `CHANGELOG.md` changed since Phase 72.
   - On any failure, quote the failing test ids, write `## HALT: suite not green on the merged tree`, and stop. Fix nothing.

5. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; F="$D/73-GREEN-TREE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = 3.13.13 ] &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_73_03)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; PB="$(k PHASE_BASE_SHA "$D/73-CLOSEOUT-GUARD.md")" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; uv run python -c "import typsphinx,os,sys; sys.exit(0 if os.path.abspath(typsphinx.__file__).startswith(os.getcwd()+os.sep) else 1)" &amp;&amp; uv run python -c "import myst_parser" &amp;&amp; [ "$(git diff --name-only "$PB" HEAD -- . ':(exclude).planning')" = CHANGELOG.md ] &amp;&amp; NS="$(git diff --numstat "$PB" HEAD -- CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$NS" | cut -f2)" = 0 ] &amp;&amp; [ -z "$(git diff --name-only "$PB" HEAD -- docs/source/conf.py)" ] &amp;&amp; git fetch -q origin main &amp;&amp; MB="$(k MILESTONE_BASE "$D/73-SC1-INVARIANTS.md")" &amp;&amp; [ "$MB" = 098a8ff64cf008822eef9dc69f75102ded3f7bc1 ] &amp;&amp; [ "$(git merge-base HEAD origin/main)" = "$MB" ] &amp;&amp; [ "$(k MAIN_ABSORBED)" = no ] &amp;&amp; for v in PYVENV_HOME_73_03 PYVENV_VERSION_73_03 MAIN_PYVENV_HOME MAIN_PYVENV_VERSION TYPSPHINX_FILE ORIGIN_MAIN_SHA_73_03 FULL_SUMMARY FULL_SKIPPED SCRATCH_73_03; do [ -n "$(k "$v")" ] || { echo "missing key: $v"; exit 1; }; done &amp;&amp; [ "$(k FULL_FAILED)" = 0 ] &amp;&amp; case "$(k FULL_SUMMARY)" in *failed*|*error*) false ;; *passed*) true ;; *) false ;; esac &amp;&amp; S="$(k SCRATCH_73_03)" &amp;&amp; test -s "$S/p7303_full.log" &amp;&amp; PP="$(grep -oE '[0-9]+ passed' "$S/p7303_full.log" | tail -n 1 | cut -d' ' -f1)" &amp;&amp; [ -n "$PP" ] &amp;&amp; [ "$PP" = "$(k FULL_PASSED)" ] &amp;&amp; [ -z "$(grep -oE '[0-9]+ (failed|errors?)' "$S/p7303_full.log")" ] &amp;&amp; C="$(uv run pytest --collect-only -q -p no:cacheprovider 2&gt;&amp;1 | grep -oE '[0-9]+ tests? collected' | grep -oE '^[0-9]+')" &amp;&amp; [ -n "$C" ] &amp;&amp; [ "$(k COLLECTED_73_03)" = "$C" ] &amp;&amp; for s in '## Head check and provisioning' '## Tree identity' '## Product-tree delta and non-absorption' '## Full suite'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" -- . ':(exclude).planning')" ] &amp;&amp; [ -z "$(git diff --name-only "$B" -- .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing key: KEY` / `missing section: NAME` printed. Each of these makes it fail:
- not in a worktree, the shim absent, or `.venv` not on 3.13.13
- `BASE_73_03` or `PHASE_BASE_SHA` missing or not an ancestor
- `typsphinx.__file__` outside the worktree, or `myst_parser` not importable
- the product delta other than exactly CHANGELOG.md, a deletion in it, or docs/source/conf.py changed
- `MILESTONE_BASE` not 098a8ff6, the merge-base with origin/main differing from it, or `MAIN_ABSORBED` not no
- an interpreter, summary or scratch key empty
- `FULL_FAILED` not 0, `FULL_SUMMARY` containing failed or error or lacking passed
- the saved full-suite log missing, its passed count differing from `FULL_PASSED`, or it carrying a failed or error count
- `COLLECTED_73_03` differing from the live collect count
- a product-tree file or REQUIREMENTS.md changed since `BASE_73_03`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Tree identity` shows `typsphinx.__file__` inside the worktree, `myst_parser` importable, and both trees' `pyvenv.cfg` `home` and `version_info` keys, this worktree on 3.13.13.
    - `## Product-tree delta and non-absorption` shows the diff from `PHASE_BASE_SHA` listing exactly `CHANGELOG.md` with zero deletions, `docs/source/conf.py` unchanged, and `MAIN_ABSORBED = no` from a merge-base equal to `MILESTONE_BASE`, with the ancestry exit explained.
    - `## Full suite` quotes the verbatim summary and every skip reason; `FULL_FAILED = 0`; `FULL_PASSED` matches the saved log; `COLLECTED_73_03` equals the live collect count; Phase 72's counts sit beside `FULL_SUMMARY` with the comparison stated.
  </acceptance_criteria>
  <done>The tree measured is this worktree's own, its product delta from the phase base is `CHANGELOG.md` alone, `main` is not absorbed, and the full suite is green with every skip accounted for.</done>
</task>

<task type="auto">
  <name>Task 2: LC_ALL=C full suite, black, mypy and ruff, the version-sync family, and the changelog page gate with 0 skipped</name>
  <precondition>Task 1 is committed with `FULL_FAILED = 0` and no HALT heading.</precondition>
  <reversibility rating="reversible">Evidence-file appends only.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-GREEN-TREE-EVIDENCE.md (Task 1)
    - tests/test_changelog_page_gate.py lines 100-230 (its skip conditions and what the build classes assert)
    - tests/test_readme_version_sync.py, tests/test_preview_version_sync.py, and tests/test_extension.py (the `test_version_matches_pyproject_toml` test)
    - CLAUDE.md § "Commands" (the exact lint and type commands CI runs) and § "The `@preview` version-sync hazard"
  </read_first>
  <action>
1. Re-run the head check and the provisioning line.

2. **`## Full suite under LC_ALL=C`.** Run `LC_ALL=C uv run pytest -q -rs -p no:cacheprovider > "$SCRATCH_73_03/p7303_lcallc.log" 2>&1; echo "exit:$?"` in one foreground call with the Bash `timeout` at 600000. Quote the summary.
   - Keys: `LCALLC_SUMMARY`, `LCALLC_PASSED` and `LCALLC_FAILED`, which must be 0.
   - Explain in one sentence why the run is repeated: CI runs in English, and warning-text assertions have failed only on CI before (ROADMAP SC#3).
   - On a failure, quote the ids and write `## HALT: suite not green under LC_ALL=C`.

3. **`## Format, type and lint`.** Record each command with its output and exit, then its key:
   - `uv run black --check .; echo "exit:$?"`, key `BLACK_EXIT = 0`
   - `uv run mypy typsphinx/; echo "exit:$?"`, key `MYPY_EXIT = 0`
   - `uv run ruff --version`, key `RUFF_LOCAL_VERSION`, the version number only, which must equal the ruff version in `uv.lock`
   - `uv run ruff check .; echo "exit:$?"`, key `RUFF_LOCAL_EXIT = 0`

   State that CI's `Lint and Format Check` job holds lint authority and that plan 73-04 reads its verdict. This local run is additive.

4. **`## Version-sync family`.** Record the per-test outcome lines verbatim from:
   - `uv run pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v -p no:cacheprovider`
   - `uv run pytest tests/test_extension.py -k version_matches_pyproject_toml -v -p no:cacheprovider`

   Key `VERSION_SYNC_FAILED = 0`. Add one line on why this runs even though no version literal moves: it is the mechanism that would catch one moving, and constraint 2 keeps the `@preview` count at four with no version change.

5. **`## Changelog page gate`** (D-14). Run `LC_ALL=C uv run pytest tests/test_changelog_page_gate.py -v -rs -p no:cacheprovider` and quote each test line and the summary.
   - Keys: `CHANGELOG_GATE_SUMMARY` and `CHANGELOG_GATE_SKIPPED`, which must be 0.
   - A skip here means the docs extra is missing. Fix the provisioning and re-run the gate; never record a skip as a pass.

6. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; F="$D/73-GREEN-TREE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_73_03)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; [ "$(k LCALLC_FAILED)" = 0 ] &amp;&amp; case "$(k LCALLC_SUMMARY)" in *failed*|*error*) false ;; *passed*) true ;; *) false ;; esac &amp;&amp; S="$(k SCRATCH_73_03)" &amp;&amp; test -s "$S/p7303_lcallc.log" &amp;&amp; PP="$(grep -oE '[0-9]+ passed' "$S/p7303_lcallc.log" | tail -n 1 | cut -d' ' -f1)" &amp;&amp; [ -n "$PP" ] &amp;&amp; [ "$PP" = "$(k LCALLC_PASSED)" ] &amp;&amp; [ -z "$(grep -oE '[0-9]+ (failed|errors?)' "$S/p7303_lcallc.log")" ] &amp;&amp; [ "$(k BLACK_EXIT)" = 0 ] &amp;&amp; [ "$(k MYPY_EXIT)" = 0 ] &amp;&amp; [ "$(k RUFF_LOCAL_EXIT)" = 0 ] &amp;&amp; [ "$(k VERSION_SYNC_FAILED)" = 0 ] &amp;&amp; [ "$(k CHANGELOG_GATE_SKIPPED)" = 0 ] &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; uv run ruff check . &amp;&amp; RL="$(uv run ruff --version | cut -d' ' -f2)" &amp;&amp; [ "$RL" = "$(k RUFF_LOCAL_VERSION)" ] &amp;&amp; [ "$RL" = "$(grep -A1 -xF 'name = "ruff"' uv.lock | sed -n 's/^version = "\(.*\)"$/\1/p')" ] &amp;&amp; uv run pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q -p no:cacheprovider &amp;&amp; uv run pytest tests/test_extension.py -k version_matches_pyproject_toml -q -p no:cacheprovider &amp;&amp; O="$(mktemp)" &amp;&amp; LC_ALL=C uv run pytest tests/test_changelog_page_gate.py -q -rs -p no:cacheprovider &gt; "$O" 2&gt;&amp;1 &amp;&amp; grep -qE '[0-9]+ passed' "$O" &amp;&amp; ! grep -qE '[0-9]+ skipped' "$O" &amp;&amp; for s in '## Full suite under LC_ALL=C' '## Format, type and lint' '## Version-sync family' '## Changelog page gate'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- `LCALLC_FAILED` not 0, or its summary lacking passed or containing failed or error
- the saved LC_ALL=C log missing, its passed count differing from `LCALLC_PASSED`, or it carrying a failed or error count
- a recorded exit key not 0, or `CHANGELOG_GATE_SKIPPED` not 0
- a live black, mypy or ruff run reporting a finding
- the live ruff version differing from `RUFF_LOCAL_VERSION` or from the version pinned in uv.lock
- a version-sync test failing or deselected
- the changelog page gate printing no passed count, or any skipped
- a product-tree file changed since `BASE_73_03`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Full suite under LC_ALL=C` quotes the summary with `LCALLC_FAILED = 0` and `LCALLC_PASSED` matching the saved log.
    - `## Format, type and lint` records `BLACK_EXIT = 0`, `MYPY_EXIT = 0`, `RUFF_LOCAL_EXIT = 0` and `RUFF_LOCAL_VERSION` equal to `uv.lock`'s ruff, and names CI's `Lint and Format Check` as the authority.
    - `## Version-sync family` lists the three files' outcome lines, with `VERSION_SYNC_FAILED = 0`.
    - `## Changelog page gate` quotes each test line with `CHANGELOG_GATE_SKIPPED = 0`.
  </acceptance_criteria>
  <done>The merged tree passes the suite under the CI locale, the format, type and lint gates, the version-sync family and the changelog page gate, with nothing skipped where a skip would hide the changelog.</done>
</task>

<task type="auto">
  <name>Task 3: Both docs environments built clean against 73-01's phase-base baseline, tox -e linkcheck under the linkcheck failure policy, the executed-versus-skipped table, and the SC#3 local verdict</name>
  <precondition>Task 2 is committed with no HALT heading, and `73-CHANGELOG-EVIDENCE.md` carries non-empty `DOCS_HTML_WARN_BASE` and `DOCS_PDF_WARN_BASE`.</precondition>
  <reversibility rating="reversible">Evidence-file appends only; the linkcheck builder reads outbound links and writes only under the gitignored docs/_build.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CHANGELOG-EVIDENCE.md (§ "Docs baseline — pre-edit, clean builds" and § "Docs render — post-edit, clean builds", including their `WARNING` lines)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-GREEN-TREE-EVIDENCE.md (Tasks 1-2)
    - tox.ini lines 67-97 (the `docs-html`, `docs-pdf` and `linkcheck` environments)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md (its linkcheck failure policy, decisions one to three) and 72-GATES-EVIDENCE.md § "Tip linkcheck run 1" (the run and census shape to mirror)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-05 and D-14
    - .planning/ROADMAP.md § "Phase 73" SC#3 and binding constraints 7 and 8
  </read_first>
  <action>
1. Re-run the head check and the provisioning line.

2. **`## Docs builds on the merged tree`** (D-14). Build each environment in the foreground, running `rm -rf docs/_build` immediately before it:
   - `LANG=C LC_ALL=C uv run tox -e docs-html > "$SCRATCH_73_03/p7303_html.log" 2>&1; echo "exit:$?"`. Quote its `build succeeded` line and every `WARNING` line. Keys `DOCS_HTML_WARN_FINAL` (0 when Sphinx prints `build succeeded.` with no count) and `MULTI_TOCTREE_HTML_FINAL`, the C-locale count of `document is referenced in multiple toctrees` lines, which must be 0.
   - `LANG=C LC_ALL=C uv run tox -e docs-pdf > "$SCRATCH_73_03/p7303_pdf.log" 2>&1; echo "exit:$?"`, the same way. Keys `DOCS_PDF_WARN_FINAL` and `MULTI_TOCTREE_PDF_FINAL` (must be 0). Copy the PDF to `$SCRATCH_73_03/p7303_final.pdf`, record `head -c 5` of it as key `PDF_MAGIC` (must be `%PDF-`), and `ls -l` of it.

   Read `DOCS_HTML_WARN_BASE` and `DOCS_PDF_WARN_BASE` from `73-CHANGELOG-EVIDENCE.md` with the key reader and set each beside its final count.
   - Each pair must be equal, and the `WARNING` lines must match the baseline's apart from the worktree path prefix.
   - Note both interpreters, 73-01's worktree and this one. A count difference that traces to an interpreter difference rather than to `CHANGELOG.md` is reported under `## HALT: docs warning drift`, with the lines quoted. It is never absorbed into a new baseline.
   - A non-zero multiple-toctrees count is reported under `## HALT: multiple toctrees on the merged tree`.

3. **`## Linkcheck`** (D-14). Run up to three times, each from a clean output directory, in the foreground:
   - For run `i` (starting at 1): `rm -rf docs/_build/linkcheck`, then `uv run tox -e linkcheck > "$SCRATCH_73_03/p7303_linkcheck_run<i>.log" 2>&1; echo "exit:$?"`, then copy `docs/_build/linkcheck/output.json` to `$SCRATCH_73_03/p7303_linkcheck_run<i>_output.json`.
   - Read the JSON (one object per line) with `uv run python -c` and record keys `LINKCHECK_RUN_<i>_EXIT`, `LINKCHECK_RUN_<i>_TOTAL` (row count), `LINKCHECK_RUN_<i>_WORKING` (rows whose `status` is `working`) and `LINKCHECK_RUN_<i>_STATUS_CENSUS` (each status with its count, such as `working:N`). Quote every non-`working` row verbatim. Counts come from `output.json`, whose `status` field is not localised, never from console text (constraint 7).
   - **Classification**, following Phase 72's linkcheck failure policy as D-14 reuses it:
     - every row `working` and exit 0: this run passes; stop.
     - a non-`working` row that is plausibly transient (a timeout, a connection reset, a 5xx or 429 from a live host): run again from a clean directory, up to three runs in total.
     - the same URL failing on two runs: in Phase 72 that allowed a `linkcheck_*` timing key in `docs/source/conf.py` with a comment naming the failure. That route is **not available here**, because `CHANGELOG.md` is the only product-tree file this phase authors (D-05). Write `## HALT: recurring linkcheck failure` with the rows quoted, and stop for the owner.
     - anything not transient (a dead link, a missing anchor, a 404): write `## HALT: linkcheck failure` with the rows quoted, and stop for the owner. Never ignore it, never retry it into a pass, never add an ignore pattern.
   - After the passing run, record `LINKCHECK_RUNS` (runs made), `LINKCHECK_PASS_RUN` (equal to `LINKCHECK_RUNS`), `LINKCHECK_TOTAL`, `LINKCHECK_WORKING` (equal to `LINKCHECK_TOTAL`) and `LINKCHECK_VERDICT = PASS`. Quote Phase 72's `TIP_LINKCHECK_TOTAL` beside it as context only; the count moves with the docs and is never compared with a fixed number (constraint 8).
   - Record `grep -c '^linkcheck_' docs/source/conf.py || true`, which must be 0, and `git diff --name-only "$PHASE_BASE_SHA" -- docs/source/conf.py`, empty.

4. **`## Division of authority`.** State which file decides what:
   - `73-CHANGELOG-EVIDENCE.md` for the pre-edit versus post-edit comparison in one tree
   - this file for the suite, format, type, the local lint, the version-sync family, the changelog gate, the final-tree docs builds and the linkcheck verdict
   - `73-CI-EVIDENCE.md` (plan 73-04, running in parallel) for the three-OS matrix and the lint verdict
   - `73-PREFLIGHT-EVIDENCE.md` (plan 73-05, running in parallel) for the trial merge against `origin/main`

   This file does not read 73-04's or 73-05's results; plan 73-07 sets them side by side in the handoff.

5. **`## Executed versus skipped`.** Build a table of every gate this plan attempted, each with its real outcome: executed-green, executed-red, or not executable with the reason. Nothing is left implied.

6. **`## SC#3 local verdict`.** Write `SC3_LOCAL_VERDICT = MET` only when all of these hold:
   - `FULL_FAILED`, `LCALLC_FAILED` and the four exit keys are 0
   - `CHANGELOG_GATE_SKIPPED = 0`
   - both docs pairs are equal, both multiple-toctrees finals are 0, and `PDF_MAGIC` is `%PDF-`
   - `LINKCHECK_VERDICT = PASS`

   Otherwise write `SC3_LOCAL_VERDICT = NOT-MET` with a HALT heading.

7. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; F="$D/73-GREEN-TREE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; n() { l="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$1" | tail -n 1)"; [ -n "$l" ] || return 1; v="$(printf '%s\n' "$l" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')"; if [ -z "$v" ]; then [ "$l" = 'build succeeded.' ] || return 1; v=0; fi; printf '%s' "$v"; } &amp;&amp; m() { LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees' "$1" || true; } &amp;&amp; B="$(k BASE_73_03)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; PB="$(k PHASE_BASE_SHA "$D/73-CLOSEOUT-GUARD.md")" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; HB="$(k DOCS_HTML_WARN_BASE "$D/73-CHANGELOG-EVIDENCE.md")" &amp;&amp; PW="$(k DOCS_PDF_WARN_BASE "$D/73-CHANGELOG-EVIDENCE.md")" &amp;&amp; [ -n "$HB" ] &amp;&amp; [ -n "$PW" ] &amp;&amp; S="$(k SCRATCH_73_03)" &amp;&amp; [ -n "$S" ] &amp;&amp; test -s "$S/p7303_html.log" &amp;&amp; test -s "$S/p7303_pdf.log" &amp;&amp; [ "$(n "$S/p7303_html.log")" = "$HB" ] &amp;&amp; [ "$(n "$S/p7303_pdf.log")" = "$PW" ] &amp;&amp; [ "$(m "$S/p7303_html.log")" = 0 ] &amp;&amp; [ "$(m "$S/p7303_pdf.log")" = 0 ] &amp;&amp; [ "$(k DOCS_HTML_WARN_FINAL)" = "$HB" ] &amp;&amp; [ "$(k DOCS_PDF_WARN_FINAL)" = "$PW" ] &amp;&amp; [ "$(k MULTI_TOCTREE_HTML_FINAL)" = 0 ] &amp;&amp; [ "$(k MULTI_TOCTREE_PDF_FINAL)" = 0 ] &amp;&amp; [ "$(k PDF_MAGIC)" = '%PDF-' ] &amp;&amp; [ "$(head -c 5 "$S/p7303_final.pdf")" = '%PDF-' ] &amp;&amp; R="$(k LINKCHECK_RUNS)" &amp;&amp; PR="$(k LINKCHECK_PASS_RUN)" &amp;&amp; [ "$R" -ge 1 ] &amp;&amp; [ "$R" -le 3 ] &amp;&amp; [ "$PR" = "$R" ] &amp;&amp; [ "$(k "LINKCHECK_RUN_${PR}_EXIT")" = 0 ] &amp;&amp; J="$S/p7303_linkcheck_run${PR}_output.json" &amp;&amp; test -s "$J" &amp;&amp; C="$(uv run python -c 'import json,sys; r=[json.loads(l) for l in open(sys.argv[1]) if l.strip()]; print(len(r), sum(1 for x in r if x["status"]=="working"))' "$J")" &amp;&amp; [ "${C% *}" -ge 1 ] &amp;&amp; [ "${C% *}" = "${C#* }" ] &amp;&amp; [ "${C% *}" = "$(k LINKCHECK_TOTAL)" ] &amp;&amp; [ "${C#* }" = "$(k LINKCHECK_WORKING)" ] &amp;&amp; [ "${C% *}" = "$(k "LINKCHECK_RUN_${PR}_TOTAL")" ] &amp;&amp; [ "$(k LINKCHECK_VERDICT)" = PASS ] &amp;&amp; [ "$(grep -c '^linkcheck_' docs/source/conf.py || true)" = 0 ] &amp;&amp; [ -z "$(git diff --name-only "$PB" -- docs/source/conf.py)" ] &amp;&amp; [ "$(k SC3_LOCAL_VERDICT)" = MET ] &amp;&amp; for s in '## Docs builds on the merged tree' '## Linkcheck' '## Division of authority' '## Executed versus skipped' '## SC#3 local verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ docs/source/ CHANGELOG.md pyproject.toml uv.lock tox.ini .planning/REQUIREMENTS.md)" ] &amp;&amp; [ -z "$(git diff --name-only "$B" -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- either 73-01 baseline key empty, or the scratch key empty
- a docs log missing, printing no `build succeeded` line, counting differently from its baseline, or carrying a multiple-toctrees line
- a final docs key differing from its baseline, a multiple-toctrees key not 0, `PDF_MAGIC` not `%PDF-`, or the saved PDF not starting `%PDF-`
- `LINKCHECK_RUNS` outside 1 to 3, the pass run not the last run, or its exit not 0
- the passing run's saved output.json missing, empty, with a working count differing from its total, or disagreeing with the recorded totals
- `LINKCHECK_VERDICT` not PASS
- a `linkcheck_` key in docs/source/conf.py, or conf.py changed since the phase base
- `SC3_LOCAL_VERDICT` not MET
- a product file or REQUIREMENTS.md modified, or a product-tree file changed since `BASE_73_03`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Docs builds on the merged tree` quotes both `build succeeded` lines and their `WARNING` lines from builds preceded by `rm -rf docs/_build` and run under `LANG=C LC_ALL=C`. `DOCS_HTML_WARN_FINAL` and `DOCS_PDF_WARN_FINAL` equal 73-01's clean phase-base baselines, both `MULTI_TOCTREE_*_FINAL` keys are 0, and `PDF_MAGIC = %PDF-`.
    - `## Linkcheck` records every run with its exit, total, working count and status census read from its saved `output.json`, quotes any non-`working` row with its classification, and ends with `LINKCHECK_VERDICT = PASS`, `LINKCHECK_WORKING` equal to `LINKCHECK_TOTAL`, and `docs/source/conf.py` unchanged.
    - `## Division of authority` names the four evidence files and what each decides.
    - `## Executed versus skipped` lists every gate attempted with its real outcome.
    - `SC3_LOCAL_VERDICT = MET`.
  </acceptance_criteria>
  <done>SC#3's local half is closed on runs executed in this phase: suite, format, type, lint, version sync, changelog gate, both docs environments against a clean phase-base baseline with zero multiple-toctrees lines, and a clean linkcheck with every link working.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`73-01-PLAN.md`. This plan carries P5 and P6 and residual assumption (d), the network being
reachable for linkcheck. It reads 73-01's and 73-02's evidence one wave later, never a same-wave
sibling's, and it deliberately does not read 73-04's CI result or 73-05's pre-flight, which run in
parallel; plan 73-07 performs that comparison. Phase accounting: 6 surfaced = 5 explicit + 1
backstop + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree venv → every measurement | a wrongly provisioned venv measures the main checkout's editable install or silently skips the changelog gate |
| recorded counts → SC#3 verdict | a stale, incremental, localised or copied count would claim green without a run |
| linkcheck builder → external hosts | outbound requests to the documentation's own links; a flaky host must not be read as green or silently retried into a pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-11 | Repudiation | measuring the main checkout through its PEP-660 editable finder (ASVS V7, L1) | high | mitigate | The verify fails unless `typsphinx.__file__` resolves inside the worktree and `.venv` is on 3.13.13; provisioning is recorded as executed fact. |
| T-73-12 | Repudiation | a dev-only venv silently skipping the changelog page gate, or any skip read as a pass | medium | mitigate | `--extra docs` provisioning, a `myst_parser` import check, `-rs` skip reasons transcribed, `CHANGELOG_GATE_SKIPPED = 0` and a live re-run with no skipped line. |
| T-73-13 | Repudiation | an incremental or localised docs build under-reporting warnings or hiding multiple-toctrees lines | medium | mitigate | `rm -rf docs/_build` before every counted build, `LANG=C LC_ALL=C` on every build and grep, and the verify recomputes both counts from the saved logs against 73-01's clean baseline. |
| T-73-14 | Repudiation | a linkcheck failure retried into a pass or suppressed with a config key or ignore pattern | high | mitigate | Counts come from the saved `output.json`; at most three runs; a recurring or non-transient failure halts for the owner; the verify requires no `linkcheck_` key and an unchanged `docs/source/conf.py`. |
| T-73-15 | Tampering | the green measured on a tree that absorbed `main` | medium | mitigate | The merge-base with `origin/main` must still equal `MILESTONE_BASE`, and the product delta from `PHASE_BASE_SHA` must be exactly `CHANGELOG.md`. |
| T-73-SC | Tampering | package installs | low | accept | `uv sync --extra dev --extra docs --python 3.13.13` and the tox environments install only what `uv.lock` pins; no package is added. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `73-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`73-GREEN-TREE-EVIDENCE.md`, with the keys `BASE_73_03`, `SCRATCH_73_03`, `TYPSPHINX_FILE`,
`PYVENV_HOME_73_03`, `PYVENV_VERSION_73_03`, `MAIN_PYVENV_HOME`, `MAIN_PYVENV_VERSION`,
`PRODUCT_DELTA`, `ORIGIN_MAIN_SHA_73_03`, `MAIN_ABSORBED`, `COLLECTED_73_03`, `FULL_SUMMARY`,
`FULL_PASSED`, `FULL_FAILED`, `FULL_SKIPPED`, `LCALLC_SUMMARY`, `LCALLC_PASSED`, `LCALLC_FAILED`,
`BLACK_EXIT`, `MYPY_EXIT`, `RUFF_LOCAL_VERSION`, `RUFF_LOCAL_EXIT`, `VERSION_SYNC_FAILED`,
`CHANGELOG_GATE_SUMMARY`, `CHANGELOG_GATE_SKIPPED`, `DOCS_HTML_WARN_FINAL`, `DOCS_PDF_WARN_FINAL`,
`MULTI_TOCTREE_HTML_FINAL`, `MULTI_TOCTREE_PDF_FINAL`, `PDF_MAGIC`, the per-run
`LINKCHECK_RUN_<i>_EXIT`, `_TOTAL`, `_WORKING` and `_STATUS_CENSUS` keys, `LINKCHECK_RUNS`,
`LINKCHECK_PASS_RUN`, `LINKCHECK_TOTAL`, `LINKCHECK_WORKING`, `LINKCHECK_VERDICT` and
`SC3_LOCAL_VERDICT`. Scratch files under `SCRATCH_73_03`: `p7303_full.log`, `p7303_lcallc.log`,
`p7303_html.log`, `p7303_pdf.log`, `p7303_final.pdf`, and `p7303_linkcheck_run<i>.log` with
`p7303_linkcheck_run<i>_output.json` per run. It modifies no product-tree file. No file named
`73-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- The tree measured is the worktree's own on 3.13.13, its product delta from the phase base is
  `CHANGELOG.md`, and `main` is not absorbed.
- The full suite passes twice, once under `LC_ALL=C`; black, mypy and ruff pass; the version-sync
  family passes; the changelog page gate runs with 0 skipped.
- Both docs environments built clean match the clean phase-base baseline with zero multiple-toctrees
  lines, and the PDF is real.
- `tox -e linkcheck` passes with every checked link `working`, read from `output.json`, and
  `docs/source/conf.py` is unchanged.
</verification>

<success_criteria>
ROADMAP SC#3's local half is met on runs executed in this phase (D-14), with every figure taken from
a command quoted beside it.
</success_criteria>

<output>
Create `.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-03-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-08).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run any GSD state-writing verb.
- The first section states `FULL_SUMMARY`, `LCALLC_SUMMARY`, both interpreter pairs,
  `RUFF_LOCAL_VERSION`, `CHANGELOG_GATE_SUMMARY`, both docs `_FINAL` counts, both multiple-toctrees
  finals, `LINKCHECK_RUNS`, `LINKCHECK_TOTAL`, `LINKCHECK_VERDICT` and `SC3_LOCAL_VERDICT`. If a HALT
  heading was written, that section says so first.
</output>
