---
phase: 15-full-corpus-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - .planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md
autonomous: false
requirements:
  - GATE-02
user_setup:
  - service: non-sandbox execution environment
    why: "The corpus gate + before/after measurement require live network egress to github.com and the full (non-NixOS-sandbox) environment where `uv run`/compiled binaries work; in-sandbox the slow test pytest.skips (D-05) and cannot produce real numbers."
must_haves:
  truths:
    - "SC#2: the residual `unknown_visit` warnings are recorded as a frequency-ranked catalogue in the committed report"
    - "SC#3: the empty-URL before/after counts and their delta are recorded in the same committed report"
    - "**D-06: both measurements are recorded in a committed `15-CORPUS-REPORT.md` in the phase directory — no separate GitHub issue or backlog file — populated with CONCRETE numbers, not placeholders**"
    - "**D-07: the report's before/after numbers come from the reverted-XREF-01 `git worktree` measurement (Plan 02), holding the corpus constant**"
  artifacts:
    - .planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md
  key_links:
    - "the report's presence WITH concrete numbers (not placeholders) is itself part of the phase gate for SC#2/SC#3 (RESEARCH Validation §Phase gate)"
    - "the report preamble logs the resolved corpus tag + clone commit SHA for future diffability against a possibly force-moved tag"
---

<objective>
Run the GATE-02 corpus measurement OUTSIDE the NixOS sandbox (live network + full env),
then write both results into a committed `15-CORPUS-REPORT.md` in the phase directory (D-06):
the frequency-ranked `unknown_visit` catalogue (SC#2) and the empty-URL before/after counts +
delta (SC#3). A blocking human-verify checkpoint confirms the real corpus build completed with
no fatal error and the report holds concrete numbers, not placeholders.

Purpose: `15-CORPUS-REPORT.md` is the phase-completion artifact for SC#2/SC#3 and the next
milestone's backlog input (D-06). SC#1's fatal-free proof is the `slow` test from Plan 01; this
plan captures the measured evidence into the durable report.
Output: `.planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md`.
</objective>

## Artifacts this phase produces (created by THIS plan)

- `.planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md` — the D-06 report (no prior
  `*-REPORT.md` precedent in this project; new artifact shape). Two tables + a preamble; see the
  shape guidance in `15-PATTERNS.md §"Report Shape Precedent"`.

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-full-corpus-validation/15-CONTEXT.md
@.planning/phases/15-full-corpus-validation/15-RESEARCH.md
@.planning/phases/15-full-corpus-validation/15-PATTERNS.md
@.planning/phases/15-full-corpus-validation/15-01-PLAN.md
@.planning/phases/15-full-corpus-validation/15-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the measurement (non-sandbox) and write 15-CORPUS-REPORT.md (D-06)</name>
  <files>.planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md</files>
  <read_first>
    - tests/test_corpus_gate.py (Plans 01+02 — the SC#1 build/catalogue and the SC#3 before/after function this task drives)
    - .planning/phases/15-full-corpus-validation/15-PATTERNS.md §"Report Shape Precedent" (two-table shape: catalogue + before/after; preamble with resolved tag + SHA)
    - .planning/phases/15-full-corpus-validation/15-RESEARCH.md §Summary, §"Validation Architecture" (Phase gate: report must hold concrete numbers, not placeholders), §"Sampling Rate"
    - .planning/phases/15-full-corpus-validation/15-CONTEXT.md §"Env caveat" (must run outside the sandbox; in-sandbox it skips)
  </read_first>
  <action>
    NON-SANDBOX ENVIRONMENT REQUIRED (live network + full env). This task CANNOT produce real numbers
    inside the NixOS sandbox, where the corpus test `pytest.skip`s (D-05) — flag/hold if the executor
    is sandboxed.
    (1) Run the SC#1 gate to capture the SC#2 catalogue byproduct:
    `pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -v -s` — confirm it PASSES (no fatal
    `TypstCompilationError`; `index.pdf` with `%PDF` magic), and capture the frequency Counter
    deterministically from the `Unknown Visit Catalogue:` line that Plan 01's test already prints to stdout
    under `-s` (the `-s` flag above surfaces it; no temporary print or conditional logic is needed).
    (2) Run the SC#3 before/after measurement:
    `TYPSPHINX_CORPUS_REPORT=1 pytest tests/test_corpus_gate.py::test_empty_url_before_after -m slow -v -s`
    — capture the printed before / after empty-URL counts and delta.
    (3) Write `.planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md` (D-06) with:
    a preamble line recording the resolved corpus tag (`f"v{sphinx.__version__}"`) and the clone's
    `git rev-parse HEAD` SHA (reproducibility vs. a possibly force-moved tag);
    Table 1 — the frequency-ranked `unknown_visit` catalogue (node type | count), sorted descending,
    framed as the next milestone's backlog input;
    Table 2 — empty-URL warnings before (XREF-01 reverted) vs. after (as-shipped) + the delta, noting
    both builds were `-b typst` translate-only against the same corpus (D-07).
    Use CONCRETE numbers from the runs — NO placeholders / TBD / "to be measured" (RESEARCH Phase gate).
    Do not modify any `typsphinx/` source; this task only reads test output and writes the report.
  </action>
  <acceptance_criteria>
    - `.planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md` exists and is committed.
    - The report contains a frequency-ranked `unknown_visit` catalogue table with concrete integer counts (SC#2).
    - The report contains an empty-URL before/after table with concrete before, after, and delta integers (SC#3).
    - The report preamble records the resolved corpus tag and the clone commit SHA.
    - The report contains NO placeholder tokens (no "TBD", "to be measured", "<count>", "XXX").
    - No file under `typsphinx/` is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md && ! grep -niE 'TBD|to be measured|placeholder|<count>|XXX' .planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md</automated>
    <human-check>The two measurement commands were run outside the sandbox with network, SC#1 passed with no fatal error, and the report's two tables hold the real captured numbers.</human-check>
  </verify>
  <done>`15-CORPUS-REPORT.md` exists with the SC#2 frequency catalogue and the SC#3 before/after numbers as concrete integers, plus a tag+SHA preamble.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — confirm fatal-free corpus build + concrete report numbers</name>
  <files>.planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md</files>
  <what-built>The GATE-02 corpus gate was run outside the sandbox: SC#1 (`sphinx-build -b typstpdf` of Sphinx's real `doc/` tree) compiled with no fatal `TypstCompilationError`, and `15-CORPUS-REPORT.md` was populated with the SC#2 `unknown_visit` frequency catalogue and the SC#3 empty-URL before/after numbers.</what-built>
  <action>
    Blocking human-verify gate (no code produced). Pause and ask the operator to confirm the corpus
    measurement was genuinely run in a real (non-NixOS-sandbox) environment with network egress — NOT
    skipped — and that `15-CORPUS-REPORT.md` holds concrete numbers. Do not mark the phase complete
    until the operator approves per the how-to-verify steps and resume-signal below.
  </action>
  <how-to-verify>
    1. Confirm the SC#1 run happened in a real (non-NixOS-sandbox) environment with network:
       `pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -v` PASSED (it did not `pytest.skip`) — no `TypstCompilationError`, `index.pdf` produced with `%PDF` magic bytes.
    2. Open `.planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md` and confirm:
       - a preamble line with the resolved corpus tag (e.g. `v9.1.0`) and a clone commit SHA;
       - Table 1: a frequency-ranked `unknown_visit` catalogue with real integer counts;
       - Table 2: empty-URL before/after counts + delta with real integers, `after <= before`.
    3. Confirm no placeholders remain (no TBD / "to be measured").
  </how-to-verify>
  <verify>
    <human-check>Operator confirms SC#1 ran fatal-free outside the sandbox and the report's two tables hold concrete numbers, not placeholders.</human-check>
  </verify>
  <resume-signal>Type "approved" once the corpus build ran fatal-free outside the sandbox and the report holds concrete numbers, or describe what is missing.</resume-signal>
  <done>Operator approved: fatal-free real corpus build confirmed and `15-CORPUS-REPORT.md` holds concrete SC#2/SC#3 numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured test output → committed report | measured numbers transcribed from subprocess stdout into a durable doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Repudiation | report provenance (which corpus/commit produced the numbers) | low | mitigate | The report preamble records the resolved tag + clone commit SHA so the numbers are attributable and re-verifiable |
| T-15-01 | Tampering | git tag mutability (corpus content drift) | low | accept | SHA logging (above) makes a force-moved tag detectable on re-run; the report is a milestone-scoping input, not a security artifact |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new external packages introduced this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `test -f .planning/phases/15-full-corpus-validation/15-CORPUS-REPORT.md` succeeds.
- `grep -niE 'TBD|to be measured|placeholder|<count>|XXX'` on the report returns nothing (no placeholders).
- Human-verify checkpoint approved: SC#1 ran fatal-free outside the sandbox; report holds concrete SC#2/SC#3 numbers.
- `pytest -m "not slow" -q` remains green (no fast-suite regression).
</verification>

<success_criteria>
- `15-CORPUS-REPORT.md` committed with concrete SC#2 catalogue + SC#3 before/after numbers (D-06).
- The measurement was run outside the sandbox with network (real corpus, not a skip).
- D-06 and D-07 are fully realized: the report is the single committed artifact, its before/after numbers come from the reverted-XREF-01 worktree measurement.
</success_criteria>

<output>
Create `.planning/phases/15-full-corpus-validation/15-03-SUMMARY.md` when done.
</output>
