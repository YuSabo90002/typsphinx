---
phase: 11-issue-114-fatal-fixes-graceful-degrade-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typsphinx/translator.py
  - tests/test_translator.py
autonomous: true
requirements: [FIG-01, DEG-01, DEG-02]
must_haves:
  truths:
    - "A figure/image with a px width converts px->pt via the CSS-canonical 1px=0.75pt (a 200-pixel width becomes 150pt); no raw px unit ever reaches Typst (D-02)"
    - "Valid units (%, em, pt, cm, mm, in) pass through unchanged; pc converts to pt; a bare unitless number is treated as px (D-02)"
    - "Unknown/unconvertible units (ex, ch, rem, vw, vh, vmin, vmax, Q) emit exactly one logger.warning and drop the dimension so the image renders at natural size (D-02)"
    - "graphviz and inheritance_diagram each emit a visible bordered native-Typst placeholder block naming the node plus exactly one logger.warning, then raise nodes.SkipNode; no raw diagram source leaks (D-01)"
  artifacts:
    - "typsphinx/translator.py::_convert_length_to_typst"
    - "typsphinx/translator.py::_visit_graphical_placeholder"
    - "typsphinx/translator.py::visit_graphviz"
    - "typsphinx/translator.py::visit_inheritance_diagram"
    - "tests/test_translator.py length-conversion unit tests"
  key_links:
    - "visit_image width/height branches call _convert_length_to_typst and omit the attribute entirely when it returns None"
    - "_visit_graphical_placeholder reuses the module-level logger (translator.py:16), not a re-instantiated getLogger"
---

<objective>
Land the two independent, additive, non-state-machine fixes of Phase 11 inside
`typsphinx/translator.py`: the FIG-01 CSS length-unit converter (new `_convert_length_to_typst`
helper wired into `visit_image`) and the DEG-01/DEG-02 graceful-degrade placeholder (new
`_visit_graphical_placeholder` helper plus `visit_graphviz` / `visit_inheritance_diagram`
overrides). Both are new code in fresh regions of the file — no visitor-state surgery — grouped
because each reuses an idiom this exact file already proved correct (the `_compute_relative_image_path`
docstring/doctest helper convention; the `visit_index` skip idiom + `unknown_visit` warning idiom).

Purpose: FIG-01 fixes the fatal `width: 200px` compile abort (Issue #114); DEG-01/02 make
out-of-scope graphical nodes degrade with a visible placeholder instead of leaking source or
aborting — both prerequisites for the milestone's full-corpus real-compile validation.

Output: `_convert_length_to_typst`, its fast unit tests, `_visit_graphical_placeholder`,
`visit_graphviz`, `visit_inheritance_diagram`.

**Requirements coverage:** FIG-01 (per D-02), DEG-01 + DEG-02 (per D-01).
</objective>

## Artifacts this phase produces

Phase-wide manifest of every NEW symbol / file across all three Phase 11 plans (this plan creates the
first group; Plans 02 and 03 create the rest):

| Symbol / File | Plan | Kind |
|---|---|---|
| `typsphinx/translator.py::_convert_length_to_typst(self, value: str) -> str \| None` | 11-01 | new helper method |
| `_TYPST_PASSTHROUGH_UNITS` set constant | 11-01 | new constant |
| `typsphinx/translator.py::_visit_graphical_placeholder(self, node, node_label)` | 11-01 | new helper method |
| `typsphinx/translator.py::visit_graphviz` / `visit_inheritance_diagram` | 11-01 | new visitor methods |
| `tests/test_translator.py` length-conversion unit tests | 11-01 | new unit tests |
| `typsphinx/translator.py::__init__` state var `self._saved_body_for_figure_caption` | 11-02 | new state var |
| `visit_caption` / `depart_caption` buffer-swap (modified) | 11-02 | modified visitor |
| `depart_figure` code-block caption emission (modified) | 11-02 | modified visitor |
| `visit_reference` refid fallback branch (modified) | 11-02 | modified visitor |
| `tests/fixtures/figure_length_render_gate/{conf.py,index.rst}` | 11-03 | new fixture project |
| `tests/fixtures/figure_target_caption_render_gate/{conf.py,index.rst}` | 11-03 | new fixture project |
| `tests/fixtures/graphviz_degrade_render_gate/{conf.py,index.rst}` | 11-03 | new fixture project |
| `tests/test_pdf_render_gate.py::TestFigureLengthRenderGate` | 11-03 | new test class |
| `tests/test_pdf_render_gate.py::TestFigureCaptionRenderGate` | 11-03 | new test class |
| `tests/test_pdf_render_gate.py::TestGraphvizDegradeRenderGate` | 11-03 | new test class |

Node-class names confirmed live this session (resolves RESEARCH Assumption A1): `sphinx.ext.graphviz`
registers class `graphviz` and `sphinx.ext.inheritance_diagram` registers class `inheritance_diagram`
— so `visit_graphviz` / `visit_inheritance_diagram` are the correct dispatch method names.

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-issue-114-fatal-fixes-graceful-degrade-net/11-CONTEXT.md
@.planning/phases/11-issue-114-fatal-fixes-graceful-degrade-net/11-RESEARCH.md
@.planning/phases/11-issue-114-fatal-fixes-graceful-degrade-net/11-PATTERNS.md
@typsphinx/translator.py
@tests/test_translator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _convert_length_to_typst helper (FIG-01, D-02) with fast unit tests</name>
  <files>typsphinx/translator.py, tests/test_translator.py</files>
  <read_first>
    - `typsphinx/translator.py:1748-1776` — `_compute_relative_image_path`: copy its Args/Returns/Examples/Notes docstring shape and `str | None` annotation style (the pure-helper convention this file already uses).
    - `typsphinx/translator.py:8` (`import re` already present) and `:16` (module-level `logger`).
    - `11-RESEARCH.md` Pattern 2 + Code Examples §1 (the regex `(-?[0-9.]+)([a-zA-Zµ%]*)`, the passthrough set, the pc/px conversion, the warn+drop branch) and the "new pitfall found this session" note on the extended docutils CSS3 unit set.
    - `11-PATTERNS.md` section "_convert_length_to_typst()".
    - `11-CONTEXT.md` decision D-02 (the authoritative unit table).
    - `tests/test_translator.py:11-38` — the `simple_document` / `mock_builder` fixtures and the `TypstTranslator(simple_document, mock_builder)` instantiation pattern used by existing function-style tests.
  </read_first>
  <behavior>
    - `_convert_length_to_typst("200px")` -> `"150pt"` (1px = 0.75pt).
    - `_convert_length_to_typst("300")` (bare unitless, treated as px) -> `"225pt"`.
    - `_convert_length_to_typst("1pc")` -> `"12pt"` (1 pica = 12pt).
    - `_convert_length_to_typst("50%")` -> `"50%"` (passthrough).
    - `_convert_length_to_typst("3em")` -> `"3em"`; `"2in"` -> `"2in"`; `"5cm"`/`"3mm"`/`"12pt"` pass through unchanged.
    - `_convert_length_to_typst("1ex")` -> `None` and emits exactly one logger.warning; same for `ch`/`rem`/`vw`/`vh`/`vmin`/`vmax`/`Q` (generic unknown branch, no per-unit special-casing).
    - A malformed value that fails the regex -> `None` + one warning.
  </behavior>
  <action>
    Add a private method `def _convert_length_to_typst(self, value: str) -> str | None:` as a sibling
    of `_compute_relative_image_path` (place it near line 1748), plus a module-or-class-level
    `_TYPST_PASSTHROUGH_UNITS = {"%", "em", "pt", "cm", "mm", "in"}`. Parse `value` with a single
    `re.fullmatch(r"(-?[0-9.]+)([a-zA-Zµ%]*)", value)`; on no match, emit one `logger.warning` and
    return None. Split into number + unit. Per D-02: empty-unit OR px -> `f"{number * 0.75:g}pt"`;
    pc -> `f"{number * 12:g}pt"`; unit in the passthrough set -> return `value` unchanged; ANY other
    unit (ex, ch, rem, vw, vh, vmin, vmax, Q, and any future unit) -> emit exactly one
    `logger.warning` naming the offending unit and return None (implement as an allow-list fall-through,
    never a deny-list enumerating CSS3 units). Never positionally strip a fixed-width unit suffix (px,
    pc, pt, in, cm, mm are all 2 chars — the regex boundary split is the only robust approach). Give the
    method a full Args/Returns/Examples/Notes docstring matching `_compute_relative_image_path`, using
    the doctest examples from the behavior block above (docstrings are documentation only — this repo's
    pytest addopts do not run doctests; the authoritative check is the unit tests). Use the existing
    module-level `logger`; add no new imports. Then add function-style unit tests to
    `tests/test_translator.py` (name them `test_convert_length_*`) that instantiate
    `TypstTranslator(simple_document, mock_builder)` and assert every case in the behavior block,
    including a `caplog`-based assertion that exactly one warning fires for an unknown unit. Write these
    tests first (they must fail before the helper exists), then implement the helper to green.
  </action>
  <verify>
    <automated>grep -q "def _convert_length_to_typst" typsphinx/translator.py && pytest tests/test_translator.py -k convert_length -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def _convert_length_to_typst(self, value: str) -> str | None" typsphinx/translator.py` exits 0.
    - `pytest tests/test_translator.py -k convert_length -q` exits 0 with all new cases green.
    - The unknown-unit case returns None and the caplog assertion confirms exactly one warning.
    - `mypy typsphinx/` and `ruff check .` and `black --check typsphinx/ tests/` all pass.
  </acceptance_criteria>
  <done>The length converter exists, is fully type-annotated, matches the D-02 table exactly, and its fast unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire _convert_length_to_typst into visit_image (FIG-01, D-02)</name>
  <files>typsphinx/translator.py</files>
  <read_first>
    - `typsphinx/translator.py:1501-1546` — full `visit_image`, especially the verbatim width/height emission at `:1527-1533` being replaced (11-RESEARCH.md Code Examples §1 shows the exact current buggy lines) and the `_compute_relative_image_path` call + `in_figure` indentation branch that stay unchanged.
    - `11-PATTERNS.md` section "visit_image — replace fatal verbatim width/height emission".
    - `11-CONTEXT.md` D-02 (drop the dimension entirely on unconvertible units).
  </read_first>
  <action>
    Replace the two verbatim branches at translator.py:1527-1533. For width: call
    `converted = self._convert_length_to_typst(node["width"])` and emit `f", width: {converted}"`
    only when `converted is not None`; when it is None, emit nothing (the attribute is dropped so the
    image renders at its natural size, per D-02). Do the same for height with `node["height"]`. Leave
    everything else in `visit_image` (the `image(` opening, the `_compute_relative_image_path` call,
    the `in_figure` handling, and `depart_image`) untouched. Do not add a comment containing the raw
    pre-conversion unit string.
  </action>
  <verify>
    <automated>pytest tests/test_translator.py -k "image or convert_length" -q && pytest -m "not slow" -q</automated>
  </verify>
  <acceptance_criteria>
    - `visit_image` calls `_convert_length_to_typst` for both width and height and omits the attribute when the helper returns None.
    - No verbatim `node["width"]` / `node["height"]` interpolation remains in `visit_image`.
    - `pytest -m "not slow" -q` (full fast suite) stays green — no regression in existing image tests.
    - `mypy typsphinx/`, `ruff check .`, `black --check typsphinx/ tests/` pass.
  </acceptance_criteria>
  <done>Any :width:/:height: value routes through the converter; a px/pc/unitless value converts to pt, valid units pass through, and unconvertible units drop the dimension — no raw unit ever reaches the Typst output.</done>
</task>

<task type="auto">
  <name>Task 3: Add DEG-01/DEG-02 graceful-degrade placeholder (D-01)</name>
  <files>typsphinx/translator.py</files>
  <read_first>
    - `typsphinx/translator.py:2483-2493` — `visit_index` / `depart_index`: the "raise nodes.SkipNode, optional no-op depart" idiom.
    - `typsphinx/translator.py:2038-2049` — `unknown_visit`: the warning idiom (note it redundantly re-imports logging; the new helper must instead reuse the module-level `logger` at `:16`).
    - `11-RESEARCH.md` Pattern 4 (shared helper shape, rect() call, SkipNode) + Assumption A1 (node-class names — already resolved: `graphviz`, `inheritance_diagram`) + the Anti-Patterns list (do NOT reuse gentle-clues / _visit_admonition).
    - `11-PATTERNS.md` section "_visit_graphical_placeholder() + visit_graphviz / visit_inheritance_diagram".
    - `11-CONTEXT.md` D-01 (visible bordered native-Typst block naming the node; exactly one warning; no gentle-clues; no source leak).
  </read_first>
  <action>
    First reconfirm the node-class dispatch names in the executor's own environment via a one-line
    import check (from sphinx.ext.graphviz import graphviz; from sphinx.ext.inheritance_diagram import
    inheritance_diagram; assert their `__name__` values are `graphviz` and `inheritance_diagram`) so the
    visitor method names dispatch correctly. Then add, in the out-of-scope-node neighborhood near
    `visit_index` (~line 2483), a shared helper
    `def _visit_graphical_placeholder(self, node: nodes.Node, node_label: str) -> None:` that (1) emits
    exactly one `self`-level `logger.warning` naming `node_label` (reuse the module-level logger at
    translator.py:16 — do NOT re-instantiate `logging.getLogger` the way `unknown_visit` does), (2)
    emits a single visible bordered placeholder using native Typst `rect()` wrapping a `text(...)` whose
    content names the node (reader-legible wording in the spirit of the CONTEXT.md example, e.g. a
    bracketed "<node_label> diagram omitted" string) with a stroke, inset, and radius, followed by a
    blank-line separator, and (3) `raise nodes.SkipNode`. Per D-01 use Typst stdlib `rect()`/`box()`
    only — never the gentle-clues `clue`/`note`/`warning` functions and never `_visit_admonition` (a
    placeholder must not be visually confusable with a real admonition). Add
    `def visit_graphviz(self, node: nodes.Node) -> None:` -> `self._visit_graphical_placeholder(node, "graphviz")`
    and `def visit_inheritance_diagram(self, node: nodes.Node) -> None:` ->
    `self._visit_graphical_placeholder(node, "inheritance diagram")`. No `depart_*` methods are needed
    (SkipNode prevents the departure callback). Because the placeholder is a fixed static string and the
    children are never visited, no raw diagram source can leak.
  </action>
  <verify>
    <automated>grep -q "def _visit_graphical_placeholder" typsphinx/translator.py && grep -q "def visit_graphviz" typsphinx/translator.py && grep -q "def visit_inheritance_diagram" typsphinx/translator.py && pytest -m "not slow" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def visit_graphviz" typsphinx/translator.py` and `grep -q "def visit_inheritance_diagram" typsphinx/translator.py` and `grep -q "def _visit_graphical_placeholder" typsphinx/translator.py` all exit 0.
    - The helper reuses the module-level `logger` (no local `logging.getLogger` call inside it) and calls `raise nodes.SkipNode`.
    - The placeholder is emitted via native Typst `rect()` (no `clue`/gentle-clues call in the helper).
    - `pytest -m "not slow" -q` stays green; `mypy typsphinx/`, `ruff check .`, `black --check typsphinx/ tests/` pass.
  </acceptance_criteria>
  <done>graphviz and inheritance_diagram each render one visible bordered placeholder + exactly one warning then skip their children; the real-compile no-leak proof lands in Plan 11-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docutils doctree attribute -> Typst source | untrusted/malformed `:width:`/`:height:` strings and graphviz DOT/diagram-spec node content cross into generated Typst here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering / Denial of Service | `_convert_length_to_typst` + `visit_image` | high | mitigate | Fixed regex + allow-list validation; any unrecognized unit falls through to warn+drop and is never interpolated verbatim into Typst (the FIG-01 fatal-abort class, D-02) |
| T-11-02 | Information Disclosure | `visit_graphviz` / `visit_inheritance_diagram` | medium | mitigate | `raise nodes.SkipNode` before descending means raw DOT source / class-hierarchy spec children are never visited or emitted; placeholder text is a fixed static string (D-01) |
| T-11-SC | Tampering | pip installs | low | accept | Zero new packages introduced this phase (locked zero-dependency constraint); supply-chain surface unchanged, no legitimacy checkpoint required |
</threat_model>

<verification>
- `pytest -m "not slow" -q` green (fast unit suite including the new length-conversion tests).
- `grep -q` presence checks pass for `_convert_length_to_typst`, `_visit_graphical_placeholder`, `visit_graphviz`, `visit_inheritance_diagram`.
- `mypy typsphinx/`, `ruff check .`, `black --check typsphinx/ tests/` all clean.
- Real-compile proof of these behaviors is deferred to Plan 11-03's render gate (GATE-01).
</verification>

<success_criteria>
- `_convert_length_to_typst` implements the D-02 table exactly and is wired into both `visit_image` width/height branches with drop-on-None semantics.
- `visit_graphviz` / `visit_inheritance_diagram` emit a visible native-Typst placeholder + exactly one warning + SkipNode, sharing one helper, reusing the module-level logger.
- Fast unit suite green; type/lint/format clean.
</success_criteria>

<output>
Create `.planning/phases/11-issue-114-fatal-fixes-graceful-degrade-net/11-01-SUMMARY.md` when done.
</output>
