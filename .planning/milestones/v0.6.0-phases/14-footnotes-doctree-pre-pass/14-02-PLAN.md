---
phase: 14-footnotes-doctree-pre-pass
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - tests/fixtures/footnote_render_gate/conf.py
  - tests/fixtures/footnote_render_gate/index.rst
  - tests/test_pdf_render_gate.py
autonomous: true
requirements: [FN-01]
must_haves:
  truths:
    - "The footnote fixture compiles to a PDF via an UNCAUGHT real `typst.compile()` with no TypstCompilationError (GATE-01 empirical bar) — the single fixture covers single-ref, double-ref, inline-markup body, and footnote-inside-a-list-item in one master document (SC#1–4)"
    - "A footnote referenced ONCE renders its body sentinel EXACTLY ONCE in the extracted PDF text, with no floating body left at the docutils definition location (SC#1)"
    - "A footnote referenced from TWO paragraphs renders a marker at both sites while its body sentinel appears exactly once — the second citation reuses the placed note by label, no duplication (SC#2)"
    - "A footnote body containing `emph`/`literal` and markup-special characters (`@ # $ _ * < >`) renders correctly in the compiled PDF — proof the buffer-swap through the normal visitor chain preserved inline markup and escaping (SC#3)"
    - "A footnote cited from inside a bullet-list item compiles cleanly and its body sentinel is present (SC#4)"
    - "No LEAK_SIGNATURES token (`par({`, `text(\"`, `raw(\"`) appears in the extracted PDF text — the code-mode/markup leak negative control"
  artifacts:
    - "tests/fixtures/footnote_render_gate/conf.py"
    - "tests/fixtures/footnote_render_gate/index.rst"
    - "tests/test_pdf_render_gate.py::TestFootnoteRenderGate (class + class-scoped fixture + sentinel constants)"
  key_links:
    - "the class-scoped `footnote_render_gate_pdf_text` fixture runs `_run_sphinx_build_typst` → `typst.compile()` (NO try/except) → `pypdf` extract_text ONCE per class"
    - "body-sentinel `.count(...) == 1` asserts guard double-emission (SC#1/SC#2); the shared `LEAK_SIGNATURES` loop guards raw-source leak on every method"
    - "the fixture's real footnote emissions come from the Plan 14-01 translator handlers (Wave-2 dependency)"
---

<objective>
Deliver the GATE-01 real-compile acceptance gate for FN-01: a new fixture project
(`tests/fixtures/footnote_render_gate/`) and a `TestFootnoteRenderGate` class in
`tests/test_pdf_render_gate.py` that builds the fixture with `sphinx-build -b typst`, compiles the
emitted `.typ` with an UNCAUGHT real `typst.compile()`, and asserts (via `pypdf` text extraction)
single-reference body-once (SC#1), double-reference no-duplication (SC#2), inline-markup body
(SC#3), and footnote-inside-a-list-item (SC#4) — each method closing with the standing
`LEAK_SIGNATURES` negative control.

Purpose: The milestone's standing empirical bar (GATE-01) is a REAL `typst.compile()` outcome, never
string-agreement asserts alone. This proves the Plan 14-01 handlers produce compile-safe output —
including that the dangling-refid guard (D-08) and the def/reuse `set()` gate (D-03) hold under a
real compile.
Output: one fixture project + one render-gate class extending the existing GATE-01 suite.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-footnotes-doctree-pre-pass/14-CONTEXT.md
@.planning/phases/14-footnotes-doctree-pre-pass/14-RESEARCH.md
@.planning/phases/14-footnotes-doctree-pre-pass/14-PATTERNS.md
@.planning/phases/14-footnotes-doctree-pre-pass/14-01-SUMMARY.md
</context>

## Artifacts this plan produces

- `tests/fixtures/footnote_render_gate/conf.py` — minimal Sphinx project (single master `index`).
- `tests/fixtures/footnote_render_gate/index.rst` — one document combining SC#1–4 footnote shapes
  with distinctive ALLCAPS sentinels.
- `tests/test_pdf_render_gate.py::TestFootnoteRenderGate` — new class, plus the class-scoped
  `footnote_render_gate_pdf_text` fixture and the module-level sentinel constants.

## Decision Coverage

- **D-02: buffer-swap body render proven end-to-end** — SC#3 (inline markup + special chars survive) via Task 2.
- **D-03: first-cite defines / repeat-cite reuses, no body duplication** — SC#2 body-sentinel `.count(...) == 1` via Task 2.
- **D-04: Typst-native `footnote()` auto-numbering in document order** — proven by the real compile placing markers/notes via Task 2.
- **D-06: footnote-inside-a-list-item needs no special-casing beyond the separator convention** — SC#4 via Task 2.
- **D-08: dangling-refid warn+skip keeps the build non-fatal** — the uncaught `typst.compile()` gate would abort on any leaked bodyless call; Task 2 proves it does not.

<tasks>

<task type="auto">
  <name>Task 1: Create the footnote_render_gate fixture project</name>
  <files>tests/fixtures/footnote_render_gate/conf.py, tests/fixtures/footnote_render_gate/index.rst</files>
  <read_first>
    - tests/fixtures/topic_line_block_render_gate/conf.py (full — copy verbatim, changing only `project` and the `typst_documents` title)
    - tests/fixtures/topic_line_block_render_gate/index.rst (full — the "title + one section per concern + distinctive ALLCAPS sentinel per concern" structural shape)
    - .planning/phases/14-footnotes-doctree-pre-pass/14-PATTERNS.md (the fixture section — the four SC shapes to combine)
    - .planning/phases/14-footnotes-doctree-pre-pass/14-RESEARCH.md (Wave 0 Gaps + Verified Mechanism 2 — the RST footnote syntax `[#name]_` / `.. [#name]`, and that the t4/t5/t6/t7/t15 shape combination compiles cleanly)
  </read_first>
  <action>
    Write `conf.py` mirroring the topic fixture verbatim: `project = "Footnote Render Gate"`,
    `author = "Test Author"`, `release = "1.0.0"`, `extensions = ["typsphinx"]`, and a single-master
    `typst_documents = [("index", "index", "Footnote Render Gate", "Test Author")]` (index MUST be a
    master document so the writer emits the full template).

    Write `index.rst` as a top-level title followed by one section per success criterion, each carrying
    a distinctive ALLCAPS-no-space body sentinel so the render-gate `.count()` asserts are unambiguous
    (choose tokens like `FOOTNOTESINGLESENTINEL`, `FOOTNOTEDOUBLESENTINEL`, `FOOTNOTEMARKUPSENTINEL`,
    `FOOTNOTELISTSENTINEL`). Use real docutils footnote syntax (`[#name]_` reference + `.. [#name]`
    definition, and/or manual `[1]_` + `.. [1]`):
      - SC#1: a footnote referenced ONCE, its body carrying `FOOTNOTESINGLESENTINEL`.
      - SC#2: a footnote referenced from TWO different paragraphs, its body carrying
        `FOOTNOTEDOUBLESENTINEL` (must appear once despite two citations).
      - SC#3: a footnote whose body contains `*emphasis*`, `` `literal` ``, and the markup-special
        characters `@ # $ _ * < >`, plus `FOOTNOTEMARKUPSENTINEL`.
      - SC#4: a footnote cited from inside a bullet-list item, its body carrying `FOOTNOTELISTSENTINEL`.
    Place the footnote DEFINITIONS under a trailing `.. rubric:: Footnotes` so definitions follow their
    references in source order (mirrors the doctree shape 14-RESEARCH.md Mechanism 2 proved). Keep each
    body sentinel distinct from its reference-site marker so counts stay unambiguous.
  </action>
  <verify>
    <automated>rm -rf /tmp/fn_gate_check && sphinx-build -b typst tests/fixtures/footnote_render_gate /tmp/fn_gate_check && grep -q 'footnote({' /tmp/fn_gate_check/index.typ && grep -q 'footnote(<fn-' /tmp/fn_gate_check/index.typ</automated>
  </verify>
  <acceptance_criteria>
    - `conf.py` sets `extensions = ["typsphinx"]` and a single-master `typst_documents` entry for `index`.
    - `index.rst` contains all four sentinels (`FOOTNOTESINGLESENTINEL`, `FOOTNOTEDOUBLESENTINEL`,
      `FOOTNOTEMARKUPSENTINEL`, `FOOTNOTELISTSENTINEL`), a footnote cited from two paragraphs, a body
      with `*emphasis*`/`` `literal` `` + special chars, and a footnote cited inside a bullet-list item.
    - `sphinx-build -b typst tests/fixtures/footnote_render_gate <tmp>` exits 0 and the emitted
      `index.typ` contains both `footnote({` (definition form) and `footnote(<fn-` (reuse form) — proof
      the Plan 14-01 handlers rendered the fixture's footnotes.
  </acceptance_criteria>
  <done>The fixture project builds to `.typ` with both footnote emission forms present, covering all four SC shapes in one master document.</done>
</task>

<task type="auto">
  <name>Task 2: Add TestFootnoteRenderGate to test_pdf_render_gate.py</name>
  <files>tests/test_pdf_render_gate.py</files>
  <read_first>
    - tests/test_pdf_render_gate.py:40 (`LEAK_SIGNATURES = ("par({", 'text("', 'raw("')` — reuse UNCHANGED; note it deliberately does NOT include `footnote(`, which is legitimate output)
    - tests/test_pdf_render_gate.py:109 (`_run_sphinx_build_typst` signature)
    - tests/test_pdf_render_gate.py:942-983 (`topic_line_block_render_gate_pdf_text` — the exact class-scoped fixture shape: source dir, `tmp_path_factory.mktemp`, `_run_sphinx_build_typst`, uncaught `typst.compile(str(index_typ), output=...)`, `%PDF` magic check, `pypdf.PdfReader(...).extract_text()` join)
    - tests/test_pdf_render_gate.py:986-1121 (`TestTopicLineBlockRenderGate` — the `@pytest.mark.slow` + `@pytest.mark.skipif(not (TYPST_AVAILABLE and PYPDF_AVAILABLE), ...)` decorators, the thin-methods-share-one-fixture pattern, and the closing `for leaked_token in LEAK_SIGNATURES:` loop)
    - .planning/phases/14-footnotes-doctree-pre-pass/14-PATTERNS.md (the `TestFootnoteRenderGate` section — the four SC method mapping)
    - .planning/phases/14-footnotes-doctree-pre-pass/14-VALIDATION.md (the SC#1–4 test map rows)
  </read_first>
  <action>
    Add module-level sentinel constants matching the fixture tokens (e.g. `FOOTNOTE_SINGLE_SENTINEL =
    "FOOTNOTESINGLESENTINEL"`, and the double/markup/list equivalents), alongside the existing sentinel
    constants near the top of the module.

    Add a `@pytest.fixture(scope="class")` named `footnote_render_gate_pdf_text(tmp_path_factory)` that
    mirrors `topic_line_block_render_gate_pdf_text` exactly: `source_dir = Path(__file__).parent /
    "fixtures" / "footnote_render_gate"`, build once with `_run_sphinx_build_typst`, assert
    `result.returncode == 0`, assert `index.typ` exists, then `typst.compile(str(index_typ),
    output=str(pdf_output))` with NO try/except (the crux of GATE-01 — any fatal aborts here and fails
    every method loudly), assert the `%PDF` magic, and return the joined `pypdf` `extract_text()`. Depend
    only on `tmp_path_factory` to avoid a class-vs-function ScopeMismatch.

    Add `class TestFootnoteRenderGate` decorated with `@pytest.mark.slow` and
    `@pytest.mark.skipif(not (TYPST_AVAILABLE and PYPDF_AVAILABLE), reason=...)` (matching every existing
    GATE-01 class), with four thin methods sharing the class-scoped fixture, each ending with the
    standing `for leaked_token in LEAK_SIGNATURES: assert leaked_token not in full_text` negative control:
      - `test_single_reference_body_once` (SC#1): `full_text.count(FOOTNOTE_SINGLE_SENTINEL) == 1` — body
        present exactly once, no floating body at the definition location.
      - `test_double_reference_body_not_duplicated` (SC#2): `full_text.count(FOOTNOTE_DOUBLE_SENTINEL)
        == 1` while the reference site's distinctive text appears at both citing paragraphs — the reuse
        did not duplicate the body.
      - `test_body_inline_markup_and_special_chars` (SC#3): `FOOTNOTE_MARKUP_SENTINEL` present (and the
        emphasis/literal/special-char content extracted), proving the buffer-swap preserved inline markup.
      - `test_footnote_inside_list_item` (SC#4): `FOOTNOTE_LIST_SENTINEL` present — the list-item-cited
        footnote compiled cleanly.
    Give methods readable snake_case names; the whole class is runnable via `-k FootnoteRenderGate`.
    Preserve Python 3.10 typing conventions.
  </action>
  <verify>
    <automated>pytest tests/test_pdf_render_gate.py -k FootnoteRenderGate -x && ruff check tests/test_pdf_render_gate.py && black --check tests/test_pdf_render_gate.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_pdf_render_gate.py -k FootnoteRenderGate -x` — all four methods pass via a real,
      UNCAUGHT `typst.compile()` (no `TypstCompilationError`).
    - Source assertion: the class-scoped `footnote_render_gate_pdf_text` fixture calls `typst.compile(...)`
      with NO surrounding try/except and returns `pypdf`-extracted text.
    - Source assertion: SC#1 and SC#2 methods assert their body sentinel `.count(...) == 1` (double-emission
      guard); every method ends with the `LEAK_SIGNATURES` loop; the class carries `@pytest.mark.slow` +
      the `TYPST_AVAILABLE and PYPDF_AVAILABLE` skipif.
    - `LEAK_SIGNATURES` is reused unchanged (still `("par({", 'text("', 'raw("')`).
    - `ruff check tests/test_pdf_render_gate.py` and `black --check tests/test_pdf_render_gate.py` clean.
  </acceptance_criteria>
  <done>`TestFootnoteRenderGate` is green across all four SC methods; body-once + no-source-leak proven by a real `typst.compile()`; the GATE-01 suite is extended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture RST → generated Typst → real `typst.compile()` | The render gate is the boundary check that author-shaped footnote input compiles without a fatal abort |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Denial of Service (build-time) | A leaked bodyless/duplicate-label footnote call aborting the whole compile | high | mitigate | The UNCAUGHT `typst.compile()` in the class-scoped fixture is the verification that the Plan 14-01 D-08 guard + D-03 `set()` gate hold — any fatal fails the gate loudly (14-RESEARCH.md Pitfall 1, Assumption A1) |
| T-14-04 | Information Disclosure | Raw Typst source leaking into rendered PDF prose | medium | mitigate | Every method closes with the `LEAK_SIGNATURES` negative control (`par({`, `text("`, `raw("`), the established cross-phase leak guard |
| T-14-SC | Tampering | pip/package installs | n/a | accept | Zero new packages — pytest, typst-py, pypdf, docutils, sphinx already installed and exercised by the existing suite (14-RESEARCH.md Environment Availability) |
</threat_model>

<verification>
- `pytest tests/test_pdf_render_gate.py -k FootnoteRenderGate -x` — all four SC methods green via real compile.
- `pytest --cov=typsphinx --cov-report=term-missing` — full suite green (including the new class).
- CI parity: `black --check .`, `ruff check .` clean.
- No edits to `typsphinx/` runtime code, `writer.py`, `template_engine.py`, or `templates/base.typ`.
</verification>

<success_criteria>
- SC#1: single-reference body sentinel appears exactly once (no floating body).
- SC#2: double-reference body sentinel appears exactly once with a marker at both sites.
- SC#3: inline-markup + special-char body renders correctly.
- SC#4: footnote-inside-a-list-item compiles cleanly.
- All four proven by an uncaught real `typst.compile()`; no `LEAK_SIGNATURES` token in extracted text.
</success_criteria>

<output>
Create `.planning/phases/14-footnotes-doctree-pre-pass/14-02-SUMMARY.md` when done
</output>
