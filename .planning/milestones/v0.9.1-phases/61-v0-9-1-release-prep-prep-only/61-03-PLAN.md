---
phase: 61-v0-9-1-release-prep-prep-only
plan: 03
type: execute
wave: 2
depends_on: ["61-01", "61-02"]
files_modified:
  - .planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md
  - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md
autonomous: true
requirements: [REL-09]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC#3 as RETAINED and re-anchored by D-09: the milestone-final tree — not a bumped tree, because D-01 removes the bump — is proven green on runs EXECUTED IN THIS PHASE. The full pytest suite, black --check and mypy are run here and their verbatim tails recorded; a preceding phase''s word is never substituted for an executed run.'
    - 'D-09 / SC#3, the CI half: a 3-OS CI run is dispatched FRESH on the tip that INCLUDES this phase''s own CHANGELOG edit, with all 12 matrix jobs green and BOTH windows-latest lanes named individually. The Phase 60 close''s run is never cited as this phase''s evidence — SC#3''s whole point is that the green is observed here, not inherited.'
    - 'The tree measured is provably this worktree''s own: typsphinx.__file__ resolves inside the executing worktree, not the main checkout, so the stale-editable-install hazard CLAUDE.md documents cannot silently invalidate every measurement in this plan.'
    - 'D-01 holds under measurement on the merged tree: line 7 of pyproject.toml is still the 0.9.0 version assignment, line 347 of README.md is still the 0.9.0 Status line, and the whole version-sync guard family is RUN by name rather than reasoned about as un-failable.'
    - 'The product-tree delta from the phase base SHA is exactly one file, CHANGELOG.md — measured, not assumed. This is what makes 61-CHANGELOG-EVIDENCE.md''s docs-warning comparison applicable to the tree this plan measures, and it is a real positive control on the diff machinery rather than a bare empty-diff claim.'
    - 'D-13 AMENDED sequencing (carried forward from 57-CONTEXT): uv sync --extra dev --locked is confirmed clean BEFORE the dispatch, because every CI job begins with that same locked sync and a stale lock fails all 12 lanes before any test, lint or type signal exists.'
    - 'A skipped test is never recorded as a pass. The slow corpus gate''s per-test outcome is transcribed as printed — PASSED or SKIPPED — and a local ruff failure is recorded as deferred to CI with its reason, never as a green.'
    - 'REL-09 is cited by this plan as the phase''s only requirement ID for coverage purposes only. This plan does NOT close it and does not touch its checkbox; per D-08 it stays ``[ ]`` and carries forward unmet to the v0.9.2 milestone with its literal wording unchanged.'
  artifacts:
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md
  key_links:
    - 'the phase tip carrying 61-01''s CHANGELOG edit -> git push -> gh workflow run ci.yml --ref <branch> -> the 12-job 3-OS matrix: ci.yml''s push trigger is main and develop only, so workflow_dispatch is the ONLY route to a 3-OS run on this branch without opening a pull request, which the fence forbids.'
    - 'PHASE_BASE_SHA read from 61-CLOSEOUT-GUARD.md -> git diff --stat excluding .planning -> exactly one changed product file: the measurement that both bounds this phase''s product-tree footprint and proves the diff is non-vacuous.'
    - 'uv.lock -> the `uv sync --extra dev --locked` step every CI job begins with: a lock drift here fails all 12 lanes on the lock rather than on anything this phase did, which is why it is confirmed before the dispatch and not after.'
  prohibitions:
    - 'MUST NOT perform any irreversible action in this phase — no local or remote tag, no PyPI publish, no GitHub Release, no pull request, no trigger of release.yml. Dispatching ci.yml is a read-only test run and is the only workflow this plan may start.'
    - 'MUST NOT record inherited, paraphrased or fabricated evidence as this phase''s own. No prior CI run may be cited as this phase''s green; the 12-job conclusion list is transcribed literally and never paraphrased as "all jobs passed"; a skipped test is never recorded as a pass; and a build or suite that was not executed is never represented by a recalled number.'
---

<objective>
Prove the milestone-final tree green on runs executed inside this phase — the full pytest suite,
`black --check`, `mypy`, and the version-sync guard family locally — and then dispatch a fresh 3-OS
CI run on the tip that carries this phase's own CHANGELOG edit, recording all 12 job conclusions
verbatim with both `windows-latest` lanes named individually.

Purpose: D-09 retains ROADMAP SC#3 in full and re-anchors it. The phrase "the bumped tip" becomes
"the milestone-final tip" everywhere, because D-01 removes the bump — but the substance is unchanged
and the point is unchanged: the green is **observed here**, not inherited from Phases 59 and 60's
word. The most recent successful full CI run on this branch predates this phase entirely; CI has
never run against a tree containing this phase's CHANGELOG bullets.

Output: `61-GREEN-TREE-EVIDENCE.md` (the local half) and `61-CI-EVIDENCE.md` (the dispatch).

**Phase reframe (read before judging this plan against ROADMAP.md).** `61-CONTEXT.md` D-11 maps
ROADMAP Phase 61's success criteria: SC#1 is **DROPPED**, SC#2 is **REWORDED**, SC#3 is **RETAINED
and re-anchored** (this plan), SC#4 is **RETAINED in full**, SC#5 is **RETAINED and re-aimed**. There
is no version bump in this phase, so the tree-identity checks here assert that the version literals
are UNCHANGED — the opposite polarity to Phases 52 and 57, which asserted that a new version had
landed. That inversion is by design, not by omission.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-VALIDATION.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution";
`.planning/REQUIREMENTS.md` § "Standing constraints" #5). Before running ANY command in this plan,
provision this worktree's own virtual environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. This matters more in this plan than anywhere
else in the phase: without it, pytest imports the UNCHANGED main-tree package and the entire
green-tree proof is taken against the wrong tree. Task 1's `typsphinx.__file__` check exists
precisely to make that failure impossible to miss.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Prove tree identity and run the full pytest suite on the milestone-final tree</name>
  <files>.planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md lines 1-90 — the provisioning-and-tree-identity block to copy (the three-command tree confirmation and the absolute-`__file__` anti-stale-editable-install proof), and the full-pytest section shape.
    - .planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md lines 1-30 — the authority-split framing that states explicitly which evidence file is authoritative for what.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md § "Baseline" — the PHASE_BASE_SHA this task's product-tree delta measurement is anchored at.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md — the docs-warning comparison this plan does NOT repeat and instead cites; read it so the authority split names its real section headings.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md § "`61-GREEN-TREE-EVIDENCE.md`" — including the note that the version check here asserts 0.9.0 is UNCHANGED, a different assertion polarity from Phases 52 and 57.
    - CLAUDE.md § "Worktree-isolated execution" — the provisioning rationale this task records as executed fact.
  </read_first>
  <action>
    Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md` with a
    `# Phase 61 — Local Green-Tree Evidence (SC#3, local half, re-anchored per D-09)` heading.

    Under `## Provisioning and tree identity` record the provisioning command and its outcome, then
    the tree confirmation as four command-plus-output blocks:

    - `sed -n '7p' pyproject.toml`, which must still be the 0.9.0 version assignment. State explicitly
      that this asserts the version is **UNCHANGED** — the inverse of the assertion Phases 52 and 57
      made — because D-01 drops the bump;
    - `sed -n '347p' README.md`, still the 0.9.0 Status line;
    - a count of the requirement IDs cited inside the `## [Unreleased]` region of `CHANGELOG.md`,
      confirming this tree carries plan 61-01's bullets rather than a pre-edit CHANGELOG;
    - `uv run python -c` importing `typsphinx` and printing `__version__` and `__file__`. The
      absolute path must resolve INSIDE this worktree. This is the load-bearing
      anti-stale-editable-install proof: the main checkout's virtual environment holds a PEP-660
      editable finder pointing at the main tree, so a wrongly-provisioned run measures the wrong
      package and every result below would be meaningless.

    Under `## Product-tree delta from the phase base` read PHASE_BASE_SHA back from
    `61-CLOSEOUT-GUARD.md` § "Baseline" and record `git diff --stat` from that SHA to HEAD, restricted
    to exclude `.planning`. It must list exactly one file, `CHANGELOG.md`, with a nonzero insertion
    count and zero deletions. State both halves of what this proves: the phase's product-tree
    footprint is bounded to a single markdown file, **and** — because the diff is non-empty — the
    measurement is a real positive control rather than a vacuous empty-diff claim that would look
    identical if the anchor were wrong.

    Under `## Division of authority` state plainly which file is authoritative for what, following
    the Phase 52 precedent: `61-CHANGELOG-EVIDENCE.md` is authoritative for the docs-html and docs-pdf
    warning-count comparison against the 3 / 5 baseline, which is not repeated here because the
    product-tree delta measured above proves the two trees carry identical `CHANGELOG.md` content;
    this file is authoritative for the full pytest suite, format and type checks; and `61-CI-EVIDENCE.md`
    is authoritative for the 3-OS matrix and for lint, which CI owns on this project.

    Under `## SC#3 — full pytest suite` run `uv run pytest` — the FULL suite, not the fast subset —
    and paste its verbatim tail including the counts line. Then run
    `uv run pytest tests/test_corpus_gate.py -v` and paste its per-test outcome lines verbatim.
    Transcribe what is printed: a `PASSED` is a pass and a `SKIPPED` is a skip. A skipped test is never
    recorded as evidence of a pass — say which it was, and if it skipped, say why the corpus was
    unavailable.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run python -c "import typsphinx,os,sys; sys.exit(0 if os.path.abspath(typsphinx.__file__).startswith(os.getcwd()+os.sep) else 1)" &amp;&amp; [ "$(sed -n '7p' pyproject.toml)" = 'version = "0.9.0"' ] &amp;&amp; [ "$(sed -n '347p' README.md)" = '**Status**: Stable (v0.9.0) - Production ready' ] &amp;&amp; [ "$(awk '/^## \[Unreleased\]/,/^## \[0\.9\.0\]/' CHANGELOG.md | grep -oE '(PATH-01|IMG-0[4567]|MSG-0[2345])' | sort -u | wc -l)" = 9 ] &amp;&amp; [ "$(git diff --stat "$(grep -oE '^[0-9a-f]{40}$' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md | head -1)"..HEAD -- . ':(exclude).planning' | grep -c 'CHANGELOG.md')" = 1 ] &amp;&amp; [ -z "$(git diff --name-only "$(grep -oE '^[0-9a-f]{40}$' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md | head -1)"..HEAD -- . ':(exclude).planning' ':(exclude)CHANGELOG.md')" ] &amp;&amp; uv run pytest &amp;&amp; grep -q 'Division of authority' .planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md &amp;&amp; grep -q 'Product-tree delta from the phase base' .planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `61-GREEN-TREE-EVIDENCE.md` § "Provisioning and tree identity" holds four command-plus-output blocks, and the printed `typsphinx.__file__` is an absolute path whose prefix is the executing worktree directory, not the main checkout
    - the version check is recorded with an explicit statement that it asserts 0.9.0 is UNCHANGED, naming D-01 as the reason for the inverted polarity
    - `61-GREEN-TREE-EVIDENCE.md` § "Product-tree delta from the phase base" holds a `git diff --stat` anchored at the PHASE_BASE_SHA read back from `61-CLOSEOUT-GUARD.md`, listing exactly one file (`CHANGELOG.md`) with a nonzero insertion count and zero deletions, and states that the non-emptiness is the positive control
    - `git diff --name-only <PHASE_BASE_SHA>..HEAD` excluding `.planning` and `CHANGELOG.md` produces no output
    - `61-GREEN-TREE-EVIDENCE.md` § "Division of authority" names `61-CHANGELOG-EVIDENCE.md` as authoritative for the docs-warning comparison, this file for the suite and format/type checks, and `61-CI-EVIDENCE.md` for the OS matrix and lint
    - `uv run pytest` (the full suite, no `-m` filter) exits 0 and its verbatim tail including the counts line is in `61-GREEN-TREE-EVIDENCE.md` § "SC#3 — full pytest suite"
    - the corpus gate's per-test outcome lines are transcribed exactly as printed, with `PASSED` or `SKIPPED` stated explicitly and a skip never described as a pass
    - `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md` is empty — this task measures and records only
  </acceptance_criteria>
  <done>The tree under measurement is proven to be this worktree's own with its version literals unmoved and its product-tree delta bounded to `CHANGELOG.md` alone, and the full pytest suite is green with the corpus gate's real outcome transcribed rather than assumed.</done>
  <reversibility rating="reversible">Evidence-file creation only; no code, test or configuration change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the format, type and version-sync gates and record the lint authority split honestly</name>
  <files>.planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md § "SC#3 — format, lint and type" (lines 89-137) — the section shape, and how a local lint failure was recorded rather than hidden.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md § "Environment Availability" — the recorded history of `ruff` on this maintainer's host: re-measured working at the Phase 57 close, reproduced broken again at that same phase's later re-check, and the standing rule that CI holds lint authority.
    - .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md — the open todo this deferral cites, deliberately kept in `pending/`.
    - tests/test_readme_version_sync.py, tests/test_extension.py, tests/test_preview_version_sync.py — the version-sync guard family this task runs BY NAME rather than reasoning about as un-failable.
    - CLAUDE.md § "Commands" and § "The `@preview` version-sync hazard" — the canonical local command set and why the preview-version guard is part of the green bar even though this phase changes no package version.
  </read_first>
  <action>
    Append a `## SC#3 — format, type and version-sync gates` section to
    `61-GREEN-TREE-EVIDENCE.md`.

    Run and paste the verbatim output of `uv run black --check .` and `uv run mypy typsphinx/`, both
    expected green.

    Attempt `uv run ruff check .` and record what actually happens. `61-RESEARCH.md` records this as
    environment-dependent on this maintainer's host: a freshly-provisioned worktree pulls a
    generic-linux `ruff` wheel whose ELF the loader rejects, and the todo tracking it is deliberately
    still open. If it runs, paste its output. If it fails to execute, paste the failure verbatim and
    record it as **deferred to CI, with CI holding lint authority** per the standing project
    convention — naming the CI job that holds that authority. Do not present an unexecutable lint as
    a green, and do not silently omit it.

    Then run the version-sync guard family by name rather than reasoning that it cannot fail under
    D-01: `uv run pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v` plus
    the `test_version_matches_pyproject_toml` test in `tests/test_extension.py`. Paste the per-test
    outcome lines verbatim. State in one line why running them still matters: they are the mechanism
    that would catch a version literal moving, and asserting they are un-failable is exactly the kind
    of reasoning that lets a real drift through.

    Close the section with a `### Executed versus skipped` subsection listing, in a short table, every
    gate this plan attempted and its real outcome — executed-green, executed-red, or not-executable
    with its reason. Anything not executed appears here explicitly; nothing is left implied.
  </action>
  <verify>
    <automated>uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; uv run pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q &amp;&amp; uv run pytest tests/test_extension.py -k version_matches_pyproject_toml -q &amp;&amp; grep -q 'SC#3 — format, type and version-sync gates' .planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md &amp;&amp; grep -q 'Executed versus skipped' .planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `61-GREEN-TREE-EVIDENCE.md` § "SC#3 — format, type and version-sync gates" holds the verbatim green output of `uv run black --check .` and `uv run mypy typsphinx/`
    - the `uv run ruff check .` attempt is recorded with its real outcome: either its verbatim output, or its verbatim failure plus an explicit deferral to CI naming the CI job that holds lint authority — never presented as green and never omitted
    - the version-sync guard family is recorded as an executed run with per-test outcome lines: `tests/test_readme_version_sync.py`, `tests/test_preview_version_sync.py` and `tests/test_extension.py`'s version-versus-pyproject test all appear by name
    - the section states in one line why running the version-sync family still matters under D-01 rather than reasoning it un-failable
    - a `### Executed versus skipped` table lists every gate attempted with its real outcome, including anything not executable and its reason
    - `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md` is empty
  </acceptance_criteria>
  <done>The format and type gates are green on the milestone-final tree, the version-sync guard family is executed by name rather than assumed, and the local lint situation is recorded honestly with its authority explicitly assigned to CI.</done>
  <reversibility rating="reversible">Evidence-file appends only.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Dispatch a fresh 3-OS CI run on the tip carrying this phase's CHANGELOG edit and record all 12 job conclusions</name>
  <files>.planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE-RUN3.md lines 1-120 — the dispatch-and-capture command sequence and the literal 12-job transcription convention.
    - .planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md lines 1-40 — the single-run shape this phase follows, and the failure-honesty rule: report a failure with its log excerpt and stop, never silently retry or accept a non-green run.
    - .github/workflows/ci.yml — the `on:` block (push is `main` and `develop` only, plus `workflow_dispatch`), the 3-OS by 2-Python test matrix, and the five single-job definitions, so the recorded census is derived from the workflow rather than recalled.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md § "Pitfall 2" and § "Code Examples" — why a stale run must not be cited even though nothing under `typsphinx/` has changed, and the exact `uv sync --locked`-before-dispatch sequencing constraint carried forward from 57-CONTEXT D-13 AMENDED.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md § "`61-CI-EVIDENCE.md`" — the pre-dispatch confirmation pattern with its inverted expectation for this phase (confirm the tip DOES carry the new bullets) and the one-dispatch default.
  </read_first>
  <action>
    Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md` with a
    `# Phase 61 — 3-OS CI Evidence (SC#3, dispatch half, re-anchored per D-09)` heading.

    Under `## Pre-dispatch confirmation` record, as commands plus outputs: `git rev-parse HEAD` (the
    local tip), `git rev-parse --abbrev-ref HEAD` (the branch that will be dispatched), the count of
    requirement IDs cited inside the `## [Unreleased]` region of `CHANGELOG.md` — which must be
    nonzero, confirming the tip DOES carry this phase's own bullets, the inverse of the Phase 57
    pre-dispatch check — and `uv sync --extra dev --locked`, which must succeed. That last one is the
    sequencing constraint carried forward from 57-CONTEXT D-13 AMENDED: every CI job begins with the
    same locked sync across ten steps in four workflows, so a lock drift fails all 12 lanes on the
    lock before any test, lint or type signal exists. If it reports drift, stop and report; do not
    dispatch, and do not regenerate the lock, because a dependency change is outside this phase's
    scope entirely.

    State in the same section why a fresh dispatch is required even though nothing under `typsphinx/`
    has changed since the Phase 60 close: D-09 is explicit that SC#3's green is observed here, not
    inherited, and CI has never run against a tree containing this phase's CHANGELOG edit. Do not cite
    the Phase 60 close's run as this phase's evidence.

    Under `## Dispatch` push the current branch to `origin` and dispatch the CI workflow against that
    same ref with `gh workflow run ci.yml --ref <branch>`. `ci.yml`'s push trigger covers only `main`
    and `develop`, so `workflow_dispatch` is the only route to a 3-OS run on this branch without
    opening a pull request — which the fence forbids. Record every command and its output.

    Under `## Run` capture the run id with
    `gh run list --workflow=ci.yml --branch <branch> --limit 1`, wait for completion with
    `gh run watch <run-id>`, and record: the run URL, the head SHA the run was dispatched against, and
    the local tip SHA, so the two can be compared by a reader. Then record the FULL per-job conclusion
    list from `gh run view <run-id> --json jobs` reduced to conclusion and job name — transcribed
    literally, all 12 rows, never paraphrased as "all jobs passed". Add a separate
    `### Both windows-latest lanes` subsection naming each of the two Windows test jobs individually
    with its conclusion; both green is the milestone's own acceptance bar.

    Record the 12-job census derived from `.github/workflows/ci.yml` itself — six matrix test jobs
    across three operating systems and two Python versions, plus lint, type-check, coverage, build,
    and two integration jobs — and confirm the recorded run's job count matches it.

    **One dispatch is the default** per D-09: unlike Phase 57 there is no pre-bump/post-bump split to
    justify a second, and this phase lands no code-affecting change. Do not dispatch twice unless a
    genuine code-affecting change lands mid-phase.

    If a job fails, do not paper over it: record the failure with its log excerpt and stop. If the
    push or the dispatch cannot complete from this worktree, do not fabricate a result and do not cite
    an older run — record the exact commands the owner must run, mark the section
    `PENDING — owner dispatch required`, and leave the human check below to close it. A `PENDING`
    marker is an honest open item; a copied older run URL is not.
  </action>
  <verify>
    <automated>uv sync --extra dev --locked &amp;&amp; test -f .planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md &amp;&amp; grep -q 'Pre-dispatch confirmation' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md &amp;&amp; [ "$(awk '/^## \[Unreleased\]/,/^## \[0\.9\.0\]/' CHANGELOG.md | grep -oE '(PATH-01|IMG-0[4567]|MSG-0[2345])' | sort -u | wc -l)" = 9 ] &amp;&amp; grep -qE 'windows-latest|PENDING — owner dispatch required' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md uv.lock)" ]</automated>
    <human-check>Open the recorded run URL and confirm three things a local gate cannot: that the run's head SHA equals the local tip SHA recorded alongside it, that all 12 jobs concluded successfully with BOTH `windows-latest` lanes among them, and that the run is newer than this phase's own CHANGELOG commit rather than inherited from the Phase 60 close.</human-check>
  </verify>
  <acceptance_criteria>
    - `61-CI-EVIDENCE.md` § "Pre-dispatch confirmation" holds the local tip SHA, the branch name, a nonzero count of requirement IDs inside the `## [Unreleased]` region (confirming the dispatched tip carries this phase's bullets), and a successful `uv sync --extra dev --locked`
    - that section states why a fresh dispatch is required and explicitly declines to cite the Phase 60 close's run as this phase's evidence
    - `61-CI-EVIDENCE.md` § "Dispatch" records the push and the `gh workflow run ci.yml --ref <branch>` command with their outputs
    - `61-CI-EVIDENCE.md` § "Run" records the run URL, the dispatched head SHA and the local tip SHA, and the two are equal
    - the per-job conclusion list is transcribed literally with all 12 rows present; the phrase "all jobs passed" alone is not accepted in place of the list
    - a `### Both windows-latest lanes` subsection names each of the two Windows test jobs individually with its conclusion
    - the 12-job census recorded in the file is derived from `.github/workflows/ci.yml` and matches the recorded run's job count
    - exactly one dispatch is recorded, per D-09's one-dispatch default
    - if the dispatch could not complete, the section instead carries a `PENDING — owner dispatch required` marker with the exact commands, and no older run URL appears anywhere in the file
    - `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md uv.lock` is empty — in particular `uv.lock` is confirmed clean, never regenerated
  </acceptance_criteria>
  <done>A 3-OS CI run is dispatched fresh on the tip carrying this phase's own CHANGELOG edit, its run URL, dispatched head SHA and all 12 job conclusions are transcribed literally with both `windows-latest` lanes named, and no prior run is substituted — or the dispatch is explicitly marked pending with reproducible commands.</done>
  <reversibility rating="reversible">A branch push and a read-only test-workflow dispatch; no tag, no release, no publish, and nothing that cannot be undone by deleting a remote branch.</reversibility>
</task>

</tasks>

<flagged_assumptions>
## Spec-less edge-coverage accounting (pointer)

The phase's single probe-surfaced edge item — REL-09, category `unclassified`, status **`unresolved`**
— is surfaced in full in `61-01-PLAN.md` § "Spec-less edge-coverage accounting". It is honest to leave
unresolved because the probe ran against REL-09's literal PyPI-release text, which D-01, D-02 and D-08
place out of scope for this phase and this milestone. Restated here so no plan drops it silently.
Accounting for the phase: 1 surfaced = 0 authored into `truths` + 1 flagged assumption + 0 dismissed +
0 dropped.

## Prohibition recall accounting (pointer)

The full two-stage recall/precision accounting and the five kept, descriptor-less prohibitions are in
`61-01-PLAN.md` § "Prohibition recall accounting". This plan carries P1 (no irreversible action) and
P5 (no inherited, paraphrased or fabricated evidence) in its own `must_haves.prohibitions`.

## Time-dependent assumption carried from RESEARCH

`61-RESEARCH.md`'s assumption A2 records that one CI dispatch is expected to suffice, because D-09
makes a second warranted only if a plan lands a code-affecting change mid-phase and this phase's fence
excludes `typsphinx/` changes absolutely. If a second dispatch does become warranted, D-09 already
permits it — but it must be recorded as a second run in this file, never as a replacement for the
first.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local worktree → `origin` (branch push) → GitHub Actions | the only outbound write in this phase; it publishes commits and starts a test workflow. No tag, no release, no publish crosses it. |
| CI run result → the phase's SC#3 verdict | the recorded run is what the milestone's acceptance bar is judged on, so a stale, paraphrased or fabricated record is the failure mode this plan guards against |
| the worktree's virtual environment → every measurement in this plan | a wrongly-provisioned environment silently measures the main checkout instead of this worktree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-09 | Repudiation | a prior CI run cited as this phase's green, on the reasoning that nothing under `typsphinx/` has changed since (ASVS V7 Logging, L1) | high | mitigate | The pre-dispatch section records the local tip SHA and the count of this phase's own requirement IDs in the `## [Unreleased]` region, the run section records the dispatched head SHA beside the local tip so a reader can compare them, and an acceptance criterion forbids any older run URL appearing in the file. The human check re-confirms recency independently. |
| T-61-10 | Repudiation | measurements taken against the main checkout instead of this worktree, because of the PEP-660 editable finder CLAUDE.md documents (ASVS V7, L1) | high | mitigate | Task 1's automated gate imports `typsphinx` and fails unless `__file__` resolves inside the executing worktree; the provisioning command is recorded as executed fact in the evidence file. |
| T-61-11 | Repudiation | a skipped test or an unexecutable lint recorded as a pass (ASVS V7, L1) | medium | mitigate | The corpus gate's per-test outcome is transcribed as printed with `PASSED` and `SKIPPED` distinguished; the `ruff` attempt is recorded with its real outcome and an explicit deferral to CI naming the responsible job; an `### Executed versus skipped` table lists every attempted gate with its real outcome. |
| T-61-12 | Elevation of Privilege | the dispatch surface being one flag away from the publish surface — `gh workflow run` against `release.yml` instead of `ci.yml`, or a tag push riding along with the branch push | high | mitigate | The plan names `ci.yml` as the only workflow it may start, the prohibition block restates the fence, and the automated gate asserts `uv.lock` and every product-tree file are unmodified so nothing rides along with the push. Plan 61-04's second fence observation independently re-probes for a tag and a release after this dispatch has happened. |
| T-61-13 | Denial of Service | a stale `uv.lock` failing all 12 CI lanes at the locked sync before any real signal exists (ASVS V14 Configuration, L1) | medium | mitigate | `uv sync --extra dev --locked` is confirmed clean BEFORE the dispatch, per the sequencing constraint carried forward from 57-CONTEXT D-13 AMENDED; drift stops the task rather than being regenerated, since a dependency change is out of scope. |
| T-61-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external package: `uv sync` resolves the already-declared, already-locked dependency set and `pyproject.toml` is asserted unmodified. `61-RESEARCH.md` § "Package Legitimacy Audit" records the phase as introducing no new package. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `61-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. This plan creates
two of them: `.planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md` and
`.planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md`. It modifies no product-tree
file — in particular it does not touch `uv.lock`, which it only verifies. **No file named
`61-VERIFICATION.md` is produced or referenced as an output.**
</artifacts_this_phase_produces>

<verification>
- `typsphinx.__file__` resolves inside the executing worktree, proving the measurements are taken
  against this tree and not the main checkout
- line 7 of `pyproject.toml` and line 347 of `README.md` are unchanged, and the version-sync guard
  family is recorded as an executed run
- the product-tree delta from PHASE_BASE_SHA is exactly `CHANGELOG.md`, with a nonzero insertion count
  serving as the positive control
- `uv run pytest` (full suite), `uv run black --check .` and `uv run mypy typsphinx/` are green with
  verbatim tails recorded
- the corpus gate's real per-test outcome and the `ruff` attempt's real outcome are both recorded, with
  no skip or failure presented as a pass
- `61-CI-EVIDENCE.md` holds a run URL, matching dispatched and local tip SHAs, all 12 job conclusions
  transcribed literally, and both `windows-latest` lanes named individually — or an explicit
  `PENDING — owner dispatch required` marker with reproducible commands
- `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md uv.lock` is empty
- no file named `61-VERIFICATION.md` exists under the phase directory
</verification>

<success_criteria>
1. The milestone-final tree is proven green on runs executed inside this phase — full pytest suite,
   `black --check`, `mypy`, and the version-sync guard family — with every transcript verbatim. This is
   ROADMAP SC#3 as RETAINED and re-anchored by D-09, with "the bumped tip" read as "the
   milestone-final tip".
2. A 3-OS CI run is dispatched fresh on the tip that includes this phase's own CHANGELOG edit, with all
   12 job conclusions transcribed literally and both `windows-latest` lanes named individually.
3. No prior CI run is cited as this phase's evidence, and no skipped test or unexecutable lint is
   recorded as a pass.
4. The tree measured is proven to be the executing worktree's own, and its product-tree delta from the
   phase base is bounded to `CHANGELOG.md` with a non-vacuous positive control.
5. `uv.lock` is confirmed clean before the dispatch and left unmodified.
</success_criteria>

<output>
Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-03-SUMMARY.md` when done
</output>
