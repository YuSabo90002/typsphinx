---
phase: 61-v0-9-1-release-prep-prep-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md
autonomous: true
requirements: [REL-09]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'SC#2 as REWORDED by D-11: the milestone''s three defect families are described as user-visible prose under the EXISTING ``## [Unreleased]`` heading of CHANGELOG.md. There is no new versioned release heading for the skipped version, the extraction-script reproduction check is not attempted here, and the count of ``^## [`` headings is still 22 — byte-identical to the pre-edit measurement.'
    - 'D-03: the bullets are authored from scratch (the Phase 52 shape), not promoted from existing prose — ``## [Unreleased]`` held zero real change bullets before this plan and holds at least three after it, each a bold lead sentence carrying its requirement IDs in trailing parentheses per the house style settled since Phase 33.'
    - 'D-04: the tail link-reference block is untouched — the final line of CHANGELOG.md is still the ``[Unreleased]`` compare link against v0.9.0...HEAD, and the count of link-reference lines is still 22.'
    - 'D-01: no version literal moves. Line 7 of pyproject.toml still reads the 0.9.0 version assignment and line 347 of README.md still reads the 0.9.0 Status line, measured after the CHANGELOG edit lands.'
    - 'D-05: the inline-image blocker receives no public-surface disclosure — the count of ``^### Known Limitations`` subsections in CHANGELOG.md is still exactly 1 (the pre-existing one inside the 0.1.0b1 section), and no README or docs surface is touched by this plan.'
    - 'D-09 / SC#3 (docs half, RETAINED and re-anchored to the milestone-final tree): both docs tox environments are re-run AFTER the bullets land and their warning counts are compared against the measured baseline of 3 for docs-html and 5 for docs-pdf. A recalled baseline number is never substituted for an executed build.'
    - 'The edit is provably pure addition: a git diff of CHANGELOG.md against this plan''s own recorded base SHA contains no removed source line, so the ``### Planned for Future Releases`` subsection and every historical release section survive byte-identical.'
    - 'CONTEXT specific idea #1 is honoured: the path-diagnostics family''s prose names a path containing a quote character as an affected case, so the fixes are not misrepresented as affecting a single platform.'
    - 'REL-09 is cited by this plan as the phase''s only requirement ID for coverage purposes only. This plan does NOT close it and does not touch its checkbox; per D-08 it stays ``[ ]`` and carries forward unmet to the v0.9.2 milestone with its literal wording unchanged.'
  artifacts:
    - CHANGELOG.md
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md
  key_links:
    - 'CHANGELOG.md -> docs/source/changelog.rst (``.. include::`` with ``:parser: myst_parser.sphinx_``) -> tox -e docs-html / docs-pdf: the chain that makes every authored bullet a PUBLISHED docs page on Read the Docs latest, and the reason a malformed bullet surfaces as a docs-build warning rather than as a silent defect. This is the single most likely breakage point in this plan.'
    - 'REQUIREMENTS.md § PATH / IMG / MSG + the Phase 58/59/60 evidence files -> the authored bullet prose: the bullets'' factual authority. A claim the evidence files do not support is a fabricated release note.'
    - 'CHANGELOG.md tail link-reference block -> the published docs page: an added link for a tag that will never exist would render as a 404 on Read the Docs, which is why D-04 suspends the standing link-rollover step.'
  prohibitions:
    - 'MUST NOT perform any irreversible action in this phase — no local or remote tag, no PyPI publish, no GitHub Release, no pull request, no trigger of release.yml. The prep-only fence is absolute and has no amended exception in this phase.'
    - 'MUST NOT publish a link or a version reference the project cannot resolve. No release-tag link line for the skipped version, no versioned release heading for it, and no README or pyproject claim of a version absent from PyPI — CHANGELOG.md is included wholesale into the published docs, so a dead reference here becomes a dead reference readers hit.'
    - 'MUST NOT frame this milestone''s fixes as affecting a single platform, and MUST NOT describe the path-shape predicate change as the repair of a defect users were hitting when the requirement itself records it as unreachable from both real call sites. The prose must be accurate to the recorded measurements, not flattering to the release.'
---

<objective>
Author the milestone's three defect families as real, user-facing prose bullets under the existing
`## [Unreleased]` heading of `CHANGELOG.md`, and prove — by executing both documentation builds after
the bullets land — that the new prose reaches the published docs surface without introducing a single
new warning.

Purpose: this is the phase's only product-tree content change, and it is the one thing a reader of
this project's published changelog will actually see from the v0.9.1 milestone. Per D-02 no v0.9.1
release will ever exist, so these bullets sit under `## [Unreleased]` until the v0.9.2 release-prep
phase promotes them into its own versioned section (D-03). The `## [Unreleased]` block currently holds
**zero** real change bullets — only a `### Planned for Future Releases` list — so every bullet here is
authored from scratch (the Phase 52 shape), not promoted (the Phase 57 shape).

Output: the edited `## [Unreleased]` block in `CHANGELOG.md`, plus
`61-CHANGELOG-EVIDENCE.md` recording the before/after, the docs-warning comparison, and the negative
assertions that prove the fence held over the file.

**Phase reframe (read before judging this plan against ROADMAP.md).** `61-CONTEXT.md` D-11 maps
ROADMAP Phase 61's success criteria onto what this phase actually does: SC#1 (atomic bump to 0.9.1)
is **DROPPED**; SC#2 is **REWORDED** — the curated content is authored under `## [Unreleased]`, the
tail link block is untouched, and the `scripts/extract_changelog_section.py` reproduction check moves
to the v0.9.2 release-prep phase; SC#3 and SC#4 are **RETAINED**; SC#5 is **RETAINED and re-aimed**
at what v0.9.2 inherits. This plan implements the D-11-mapped SC#2 and the docs half of SC#3. It does
not bump a version and does not create a versioned release section.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-VALIDATION.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution";
`.planning/REQUIREMENTS.md` § "Standing constraints" #5). Before running ANY command in this plan,
provision this worktree's own virtual environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`

Both extras are required — `docs` carries `furo`, `myst-parser`, `sphinx-intl` and
`sphinx-autodoc-typehints`, without which neither docs tox environment can run. Then prefix every
subsequent command with `uv run`. Without this, the build imports the UNCHANGED main-tree package
and every measurement in this plan is taken against the wrong tree.
</worktree_provisioning>

<!-- planner-discipline-allow: quote character -->
<!-- The phrase above is the verification command's own grep regex AND the exact wording the
     executor must place in the CHANGELOG bullet per CONTEXT specific idea #1. Its gate is a
     POSITIVE presence check (count >= 1), not a negative absence check, so echoing it into the
     action body cannot invalidate anything. -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end slice — one defect-family bullet, wired from requirement text through MyST to a rendered docs page</name>
  <files>CHANGELOG.md, .planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - CHANGELOG.md lines 1-141 — the file being modified. Read the current `## [Unreleased]` block (heading at line 8, `### Planned for Future Releases` at line 10, five planned items, zero real change bullets) AND the whole `## [0.9.0]` section, whose `### Fixed` bullets at lines 105-124 are the house style this task must match.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md § "`CHANGELOG.md` (`## [Unreleased]` block)" — the verbatim bullet shape (bold lead sentence, requirement IDs in trailing parentheses inside the bold span, then 1-3 sentences of prose), the measured section vocabulary counts, and the exact tail-block line that must not move.
    - .planning/REQUIREMENTS.md § "PATH — path-shape predicate correctness" — PATH-01's full text INCLUDING its "Reachability, measured 2026-08-27" note, which is the factual constraint on how this bullet may be worded.
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md — the evidence backing any claim about the predicate's behaviour change.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md § `<decisions>` D-03, D-04, D-05 and § `<specifics>` items 1 and 2.
    - docs/source/changelog.rst lines 1-10 — the `.. include:: ../../CHANGELOG.md` with `:parser: myst_parser.sphinx_` that makes this edit a published docs change.
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md § "SC#3 — documentation builds" (lines 138-176) — the measured warning baseline (3 for docs-html, 5 for docs-pdf) and the "Baseline comparison" framing this task's evidence section reproduces.
  </read_first>
  <action>
    This is the phase's tracer slice: one bullet carried the whole way from requirement text, through
    MyST, into a rendered documentation page, with a real end-to-end verification — before the other
    two families are written. It is production-quality, not a draft: the bullet authored here is the
    final bullet and is not rewritten by Task 2.

    Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md` with a
    `# Phase 61 — CHANGELOG Evidence (SC#2 as REWORDED by D-11)` heading. Record, as its first
    section, this plan's own base SHA from `git rev-parse HEAD` and the pre-edit measurements taken
    before touching anything: the count of top-level release headings (expected 22), the count of
    link-reference lines (expected 22), the count of `Known Limitations` subsections (expected 1), the
    verbatim final line of the file, and the count of real change bullets currently under
    `## [Unreleased]` (expected 0, which is why D-03 calls this authoring from scratch rather than
    promotion). Also record, in a short `## What this file is NOT` note, that
    `scripts/extract_changelog_section.py` is deliberately NOT invoked in this phase — there is no
    versioned section for it to extract, and D-11 moves that reproduction check to the v0.9.2
    release-prep phase.

    Then author the FIRST of the three defect families, in the order CONTEXT specific idea #2 fixes
    (ROADMAP's own order): the `typst_documents` path-shape predicate, requirement **PATH-01**. Place
    it under a `### Fixed` subsection inside the existing `## [Unreleased]` block, ABOVE the existing
    `### Planned for Future Releases` subsection, leaving that subsection and its five entries exactly
    as they are. Follow the house style exactly: a bold lead sentence stating the user-visible change
    in plain language with `(PATH-01)` in trailing parentheses inside the bold span, then one to three
    sentences of prose. Never make an internal module or function name the subject of the lead
    sentence.

    **Accuracy constraint, load-bearing.** REQUIREMENTS.md records PATH-01 as unreachable from both of
    the predicate's real call sites — `_resolve_target_stem()` normalizes before calling and
    `_track_image()` passes a value that always carries a parent-directory segment — so it is
    contract hardening, not the repair of a defect users were hitting. Write the bullet to say what is
    true: the predicate now applies its absolute-path and drive-qualified checks to the normalized
    string, matching the idiom its sibling image-URI predicate already used, so a future third caller
    that does not pre-normalize cannot inherit the gap silently. Do not claim users were affected.

    Then run the documentation build and prove the chain end to end: provision the worktree per the
    provisioning block above, run `uv run tox -e docs-html`, and paste its verbatim tail — including
    the literal `build succeeded, N warnings.` line — into a `## Docs render — tracer slice` section
    of the evidence file, stating the count and comparing it to the measured baseline of 3.

    If the count is anything other than 3, do NOT accept it and do NOT adjust the baseline. Determine
    from the build output whether the new warning originates in the changelog page. If it does, the
    bullet's MyST is malformed — fix the bullet and re-run. If it does not, record the drift with its
    source file and message verbatim and HALT, reporting it rather than proceeding; the baseline was
    reconfirmed byte-identical at the Phase 57 close and no commit has touched `docs/source/` or
    `CHANGELOG.md` since the Phase 60 CI-authority commit, so a drift is a real finding.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs &amp;&amp; [ "$(grep -cE '^## \[' CHANGELOG.md)" = 22 ] &amp;&amp; [ "$(grep -cE '^\[[^]]+\]: https' CHANGELOG.md)" = 22 ] &amp;&amp; [ "$(tail -1 CHANGELOG.md)" = '[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.0...HEAD' ] &amp;&amp; [ "$(awk '/^## \[Unreleased\]/,/^## \[0\.9\.0\]/' CHANGELOG.md | grep -cE '^- \*\*')" -ge 1 ] &amp;&amp; [ "$(awk '/^## \[Unreleased\]/,/^## \[0\.9\.0\]/' CHANGELOG.md | grep -c 'PATH-01')" -ge 1 ] &amp;&amp; test -f .planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md &amp;&amp; uv run tox -e docs-html 2>&amp;1 | grep -q 'build succeeded, 3 warnings'</automated>
  </verify>
  <acceptance_criteria>
    - `61-CHANGELOG-EVIDENCE.md` exists and records this plan's base SHA as a 40-character hex string, plus all five pre-edit measurements (22 release headings, 22 link-reference lines, 1 `Known Limitations` subsection, the verbatim final line, 0 real change bullets under `## [Unreleased]`)
    - the evidence file contains a `## What this file is NOT` note stating that `scripts/extract_changelog_section.py` is not invoked this phase and why
    - `awk '/^## \[Unreleased\]/,/^## \[0\.9\.0\]/' CHANGELOG.md | grep -cE '^- \*\*'` returns at least 1, and that region contains the string `PATH-01`
    - the PATH-01 bullet's lead sentence is bold, carries its requirement ID in trailing parentheses inside the bold span, and does not name an internal module or function as its subject
    - the PATH-01 bullet does not assert that users were hitting the defect; it describes the predicate's contract being made consistent with its sibling, matching REQUIREMENTS.md's recorded reachability measurement
    - `grep -cE '^## \[' CHANGELOG.md` returns 22, `grep -cE '^\[[^]]+\]: https' CHANGELOG.md` returns 22, and `tail -1 CHANGELOG.md` is byte-identical to the recorded pre-edit final line
    - `uv run tox -e docs-html` exits 0 and its output contains the literal `build succeeded, 3 warnings.`
    - the evidence file's `## Docs render — tracer slice` section holds that verbatim `build succeeded` line from a build executed AFTER the bullet landed — not a recalled baseline number
    - `git status --porcelain typsphinx/ tests/` is empty (this task edits neither)
  </acceptance_criteria>
  <done>One defect-family bullet is authored under the existing `## [Unreleased]` heading in the file's own house style, and an executed documentation build proves it renders through MyST into the published docs page with the warning count unchanged from the measured baseline.</done>
  <reversibility rating="reversible">A markdown addition under an existing heading; promoting it into a versioned section later (D-03's stated mechanism) is a heading edit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Author the remaining two defect families and prove the whole edit is pure addition against both docs environments</name>
  <files>CHANGELOG.md, .planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - CHANGELOG.md — the current state after Task 1, including the bullet Task 1 authored (which this task must not rewrite).
    - .planning/REQUIREMENTS.md § "IMG — image URI safety" (IMG-04, IMG-05, IMG-06, IMG-07) and § "MSG — user-facing path quoting" (MSG-01 through MSG-05) — the requirement text every claim in these bullets must be traceable to.
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md — the IMG family's evidence, including the real `typst.compile()` gate that IMG-07 names.
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATH-QUOTING-EVIDENCE.md — the MSG-02..MSG-05 evidence, consolidated.
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md § D-01 AMENDED — records that the quoting helper closes an apostrophe by DOUBLING it rather than backslash-escaping; this is the measured basis for CONTEXT specific idea #1's framing constraint.
    - .planning/phases/58-repr-format-decoupling-test-side-only/58-REPR-CENSUS.md — MSG-01's census, needed to decide whether MSG-01's test-side decoupling earns a user-visible bullet at all.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md § "Claude's Discretion" — the discretion this task exercises (section assignment, granularity, whether `typsphinx/pathfmt.py` and MSG-01 earn bullets, whether a `### Verified` subsection is authored now).
    - tests/test_changelog_page_gate.py lines 45-70 — the `RELEASE_VERSIONS` tuple ending at `"0.9.0"`, which stays untouched because no versioned section is added.
  </read_first>
  <action>
    Author the remaining two defect families as bullets in the same `### Fixed` subsection under
    `## [Unreleased]`, in CONTEXT specific idea #2's order.

    **Family two — the absolute image URI (IMG-04, IMG-05, IMG-06, IMG-07).** The user-visible fact is
    that a Windows-shaped absolute image URI now compiles: the relocation key is built from a
    forward-slash-normalized basename so no separator from the original URI survives into the emitted
    image path value, the emitted path value is escaped as a syntax literal before interpolation, and
    the relocation basename is bounded to 255 UTF-8 bytes with the collision digest kept whole. Note
    in the prose that the two halves are coupled — neither alone closes the compile-time failure —
    because Typst refuses a backslash in an image path by value, not by syntax. Cite the IDs in
    trailing parentheses.

    **Family three — path-naming diagnostics (MSG-02, MSG-03, MSG-04, MSG-05).** The user-visible fact
    is that a path named in a diagnostic message now reads as it appears on disk: separators are no
    longer doubled, and the quoting no longer closes early on a path containing a quote character.
    **CONTEXT specific idea #1 binds this bullet:** the prose must name a path containing a quote
    character as an affected case, because the helper closes an apostrophe by doubling it — a POSIX
    path with an apostrophe was affected by the same family. Do not frame the family as belonging to
    one platform. Cite the IDs in trailing parentheses.

    Exercise the recorded discretion explicitly rather than silently: decide whether MSG-01's
    test-side decoupling and the new internal module earn user-visible bullets (both are internal by
    construction and this project's changelog has historically described user-visible behaviour), and
    record the decision and its one-line reason in the evidence file. Take the cheaper default on the
    `### Verified` subsection: do NOT author one this phase, and record in the evidence file that its
    authorship is left to the v0.9.2 release-prep phase to write against the whole 0.9.2 diff, per the
    discretion item. Leave `### Planned for Future Releases` and its five entries exactly as they are.

    Then append three sections to `61-CHANGELOG-EVIDENCE.md`.

    First, `## Pure-addition proof`: paste the verbatim output of a unified diff of `CHANGELOG.md`
    against the base SHA recorded in Task 1, and state that it contains no removed source line — which
    is what proves the `### Planned for Future Releases` subsection, every historical release section,
    and the tail link-reference block all survive byte-identical.

    Second, `## Fence assertions over CHANGELOG.md and the version literals`: record each as a command
    plus its output, never as a claim — the release-heading count (22, unchanged), the
    link-reference-line count (22, unchanged), the `Known Limitations` subsection count (1, unchanged,
    the pre-existing one inside the 0.1.0b1 section, per D-05), the verbatim final line of the file
    (unchanged, per D-04), the occurrence count of the skipped version string anywhere in the file
    (0, unchanged), line 7 of `pyproject.toml` (still the 0.9.0 version assignment, per D-01), line
    347 of `README.md` (still the 0.9.0 Status line, per D-01), and that
    `tests/test_changelog_page_gate.py`'s `RELEASE_VERSIONS` tuple is unmodified and still ends at the
    0.9.0 entry.

    Third, `## Docs render — full comparison against the 3 / 5 baseline`: run BOTH `uv run tox -e
    docs-html` and `uv run tox -e docs-pdf` on the tree that now carries all three families, and paste
    each build's verbatim `build succeeded, N warnings.` line, stating the counts and comparing them to
    the measured baseline of 3 and 5. The same halt rule as Task 1 applies: a count other than 3 or 5
    is investigated and reported, never accepted and never used to move the baseline.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^## \[' CHANGELOG.md)" = 22 ] &amp;&amp; [ "$(grep -cE '^### Known Limitations' CHANGELOG.md)" = 1 ] &amp;&amp; [ "$(grep -cE '^\[[^]]+\]: https' CHANGELOG.md)" = 22 ] &amp;&amp; [ "$(grep -c '0\.9\.1' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(tail -1 CHANGELOG.md)" = '[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.0...HEAD' ] &amp;&amp; [ "$(sed -n '7p' pyproject.toml)" = 'version = "0.9.0"' ] &amp;&amp; [ "$(sed -n '347p' README.md)" = '**Status**: Stable (v0.9.0) - Production ready' ] &amp;&amp; [ "$(awk '/^## \[Unreleased\]/,/^## \[0\.9\.0\]/' CHANGELOG.md | grep -cE '^- \*\*')" -ge 3 ] &amp;&amp; [ "$(awk '/^## \[Unreleased\]/,/^## \[0\.9\.0\]/' CHANGELOG.md | grep -oE '(PATH-01|IMG-0[4567]|MSG-0[2345])' | sort -u | wc -l)" = 9 ] &amp;&amp; [ "$(awk '/^## \[Unreleased\]/,/^## \[0\.9\.0\]/' CHANGELOG.md | grep -ci 'quote character')" -ge 1 ] &amp;&amp; [ "$(git diff "$(grep -m1 -oE '[0-9a-f]{40}' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md)" -- CHANGELOG.md | grep -cE '^-[^-]')" = 0 ] &amp;&amp; [ -z "$(git status --porcelain tests/ typsphinx/ docs/)" ] &amp;&amp; uv run tox -e docs-html 2>&amp;1 | grep -q 'build succeeded, 3 warnings' &amp;&amp; uv run tox -e docs-pdf 2>&amp;1 | grep -q 'build succeeded, 5 warnings'</automated>
  </verify>
  <acceptance_criteria>
    - the `## [Unreleased]` region of `CHANGELOG.md` contains at least 3 bold-lead change bullets and cites all nine of `PATH-01`, `IMG-04`, `IMG-05`, `IMG-06`, `IMG-07`, `MSG-02`, `MSG-03`, `MSG-04`, `MSG-05`
    - the path-diagnostics bullet's prose contains the phrase `quote character`, and no bullet frames the milestone's fixes as belonging to a single platform
    - `git diff <base SHA>..working tree -- CHANGELOG.md` contains no line beginning with a single `-` followed by a non-`-` character, proving pure addition
    - `grep -cE '^## \[' CHANGELOG.md` = 22, `grep -cE '^### Known Limitations' CHANGELOG.md` = 1, `grep -cE '^\[[^]]+\]: https' CHANGELOG.md` = 22, and the occurrence count of the skipped version string in `CHANGELOG.md` is 0 — every one of these byte-identical to the pre-edit measurement Task 1 recorded
    - `sed -n '7p' pyproject.toml` is the 0.9.0 version assignment and `sed -n '347p' README.md` is the 0.9.0 Status line
    - `git status --porcelain tests/ typsphinx/ docs/` is empty — `RELEASE_VERSIONS` is untouched, no `typsphinx/` file changed, and no docs source page was edited
    - `61-CHANGELOG-EVIDENCE.md` § "Pure-addition proof" holds the verbatim diff output, not a claim about it
    - `61-CHANGELOG-EVIDENCE.md` § "Fence assertions over CHANGELOG.md and the version literals" records each assertion as a command plus its output
    - `61-CHANGELOG-EVIDENCE.md` § "Docs render — full comparison against the 3 / 5 baseline" holds a verbatim `build succeeded, N warnings.` line from EACH of the two builds, both executed after all three families landed
    - the evidence file records the exercised discretion in one line each: whether MSG-01 and the new internal module earned bullets, and that the `### Verified` subsection is deliberately deferred to the v0.9.2 release-prep phase
  </acceptance_criteria>
  <done>All three defect families are described as user-visible prose under the existing `## [Unreleased]` heading, the edit is measurably pure addition with every version literal and the tail link block unmoved, and both documentation builds executed on the final tree match the recorded 3 / 5 warning baseline.</done>
  <reversibility rating="reversible">Markdown additions under an existing heading; no version literal, no tag, no published artifact moves.</reversibility>
</task>

</tasks>

<flagged_assumptions>
## Spec-less edge-coverage accounting (1 probe-surfaced item, 0 silent drops)

This phase has no `61-SPEC.md`, so the edge-coverage report came from the deterministic fallback
probe run over REL-09's literal `REQUIREMENTS.md` text. The probe returned exactly one item and left
it **`unresolved`**, category `unclassified`, verification `null`:

| Requirement | Category | Status | Verification | Disposition |
|---|---|---|---|---|
| REL-09 | unclassified | **unresolved** | null | **flagged assumption — surfaced here, not authored into `truths`, not dismissed** |

**Why `unresolved` is the honest state, and why no acceptance criterion is manufactured for it.**
The probe ran against REL-09's *literal* text, which describes a PyPI release with a curated
`## [0.9.1]` section, a version bump in lockstep across three files, and a GitHub Release body
sourced from the extraction script. D-01, D-02 and D-08 decided that none of that happens in this
phase or in this milestone: the version is never bumped, v0.9.1 is never published, and REL-09
carries forward unmet with its wording unchanged. An `unclassified` row stays `unresolved` under the
fallback protocol — never auto-resolved with a backstop marker, never auto-dismissed — and
fabricating an edge criterion for release mechanics that are deliberately out of scope would be
worse than recording the gap. It is recorded here, in the plan the phase leads with, so no downstream
reader mistakes the absence for an oversight.

**Accounting:** 1 surfaced = 0 authored into `must_haves.truths` + 1 surfaced as a flagged assumption
+ 0 dismissed + 0 dropped.

## Prohibition recall accounting (descriptor-less, never auto-dismissed)

Stage 1 over-produced against REL-09's text; Stage 2 dropped the routine-engineering candidates
("must not throw on a malformed bullet", "must not leave a temp artifact", "must not rewrite a
historical section" — the last already covered mechanically by this plan's pure-addition gate) as
edge-probe or code-review territory. No candidate was canon security/compliance, so no canon-referral
breadcrumb applies. Five bespoke values/safety/transparency prohibitions survived and are authored
descriptor-less (no `check_*` scalar, so each disposes flagged-unverified) across the phase's four
plans:

- **P1 (safety):** no irreversible action — no tag, no publish, no GitHub Release, no PR, no
  `release.yml` trigger. Authored in all four plans; the fence is phase-wide.
- **P2 (transparency):** no link or version reference the project cannot resolve. Authored in 61-01
  and 61-04.
- **P3 (honesty):** REL-09 must not be recorded as satisfied — no checkbox flip, no Traceability-row
  status change, no prose claiming a release occurred. Authored in 61-02 and 61-04.
- **P4 (accuracy):** the milestone's fixes must not be framed as platform-exclusive, and the
  path-shape predicate change must not be described as repairing a defect users were hitting.
  Authored in 61-01.
- **P5 (evidence integrity):** no inherited, paraphrased or fabricated evidence recorded as this
  phase's own — no prior CI run cited as this phase's green, no paraphrased job list, no baseline
  number substituted for an unexecuted build, no skipped test recorded as a pass. Authored in 61-02,
  61-03 and 61-04.

**Accounting:** 5 kept = 5 authored into `must_haves.prohibitions` + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` → `docs/source/changelog.rst` → Read the Docs `latest` | authored prose crosses from a repository file into a published, publicly-reachable page; this is the only outward-facing surface this plan writes to |
| authored prose → the milestone's factual record | the bullets become the project's account of what v0.9.1 fixed; a claim the evidence files do not support is a self-inflicted misrepresentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-01 | Tampering | a malformed MyST bullet publishing broken markup to Read the Docs (ASVS V5 Input Validation, L1 — marginal: the "input" is prose this project authors) | medium | mitigate | Both docs tox environments are executed AFTER the bullets land and their warning counts gated against the measured 3 / 5 baseline; a count other than 3 / 5 halts and is reported rather than accepted. The tracer task proves the render chain on one bullet before the other two are written. |
| T-61-02 | Tampering | a link-reference line for a tag that will never exist rendering as a 404 on the published docs page (`.github/workflows/links.yml` excludes `CHANGELOG.md` from the lychee scan, so CI would not catch it) | high | mitigate | D-04 suspends the standing link-rollover step; the tail line is gated byte-for-byte and the whole edit is gated as pure addition, so no link-reference line can be added or altered. |
| T-61-03 | Repudiation | a recalled baseline number recorded in place of an executed build, leaving the docs-warning claim unfalsifiable | medium | mitigate | Every warning count in the evidence file must be a verbatim `build succeeded, N warnings.` line from a build executed after the edit; the automated gate pipes the live build output through the comparison rather than reading a recorded number. |
| T-61-04 | Elevation of Privilege | scope escape — a version bump, a versioned release section, or a `typsphinx/` edit slipping in under cover of "release prep" | high | mitigate | Equality gates on both version literals, on the release-heading count, on the link-reference count and on the occurrence count of the skipped version string, plus an emptiness check over `git status --porcelain typsphinx/ tests/ docs/` in both tasks. |
| T-61-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. `61-RESEARCH.md` § "Package Legitimacy Audit" records the phase as introducing no external package: `pyproject.toml`'s dependency array and extras are untouched by every decision in `61-CONTEXT.md`. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every file below is created new by this phase. This is the phase-wide list; plans 61-02, 61-03 and
61-04 reference this section rather than repeating it.

**Under `.planning/phases/61-v0-9-1-release-prep-prep-only/`:**

- `61-CHANGELOG-EVIDENCE.md` — created by this plan (61-01)
- `61-CLOSEOUT-GUARD.md` — created by 61-02, re-verified and extended by 61-04
- `61-SC4-INVARIANTS.md` — created by 61-02 (observation 1), extended by 61-04 (observation 2 and the scoped diff)
- `COVERAGE.md` — created by 61-02
- `61-GREEN-TREE-EVIDENCE.md` — created by 61-03
- `61-CI-EVIDENCE.md` — created by 61-03
- `61-HANDOFF.md` — created by 61-04
- `61-01-SUMMARY.md`, `61-02-SUMMARY.md`, `61-03-SUMMARY.md`, `61-04-SUMMARY.md` — one per plan

**In the product tree:** exactly one file is modified, `CHANGELOG.md`, and only by addition inside
its existing `## [Unreleased]` block. The headings this phase adds bullets under are the existing
`## [Unreleased]` heading and a `### Fixed` subsection created beneath it. No other product-tree file
is created or modified by any plan in this phase.

**Reserved name — never produced:** no artifact in this phase may be named `61-VERIFICATION.md`. That
name is owned by `gsd-verifier` and is overwritten wholesale on phase close.
</artifacts_this_phase_produces>

<verification>
- `CHANGELOG.md`'s `## [Unreleased]` block holds at least three bold-lead change bullets covering the
  three defect families and citing all nine user-visible requirement IDs
- the whole edit is pure addition against this plan's recorded base SHA
- the release-heading count, the link-reference-line count, the `Known Limitations` count, the final
  line of the file and the occurrence count of the skipped version string are all byte-identical to
  the pre-edit measurements
- line 7 of `pyproject.toml` and line 347 of `README.md` are unchanged
- `uv run tox -e docs-html` reports 3 warnings and `uv run tox -e docs-pdf` reports 5 warnings, both
  executed after the bullets landed, both transcribed verbatim into `61-CHANGELOG-EVIDENCE.md`
- `git status --porcelain typsphinx/ tests/ docs/` is empty
- no file named `61-VERIFICATION.md` exists under the phase directory
</verification>

<success_criteria>
1. The milestone's three defect families are described as user-visible prose under the EXISTING
   `## [Unreleased]` heading, in the file's own house style, each carrying its requirement IDs — the
   D-11-mapped form of ROADMAP SC#2.
2. No versioned release section is created, no tail link-reference line is added or altered, and no
   version literal moves anywhere in the repository.
3. Both documentation builds are executed on the tree that carries the bullets and their warning
   counts match the measured 3 / 5 baseline, transcribed verbatim rather than recalled — the docs
   half of ROADMAP SC#3 as re-anchored by D-09.
4. The edit is measurably pure addition, so `### Planned for Future Releases` and every historical
   release section survive byte-identical.
5. The inline-image blocker receives no public-surface disclosure, and no bullet frames the
   milestone's fixes as platform-exclusive.
</success_criteria>

<output>
Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-01-SUMMARY.md` when done
</output>
