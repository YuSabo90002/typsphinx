---
phase: 61-v0-9-1-release-prep-prep-only
plan: 04
type: execute
wave: 3
depends_on: ["61-02", "61-03"]
files_modified:
  - .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md
  - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md
  - .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md
autonomous: true
requirements: [REL-09]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'D-10 / SC#4 (RETAINED in full), observation 2 of 2: the fence is re-probed at a genuinely LATER wall-clock timestamp than observation 1 — two waves and a full CI dispatch later — and still holds. Each negative probe carries the same positive control observation 1 used, so an unreachable source fails loudly rather than passing vacuously.'
    - 'D-10 / SC#4: a git diff scoped to typsphinx/ between the recorded PHASE_BASE_SHA and the phase tip is EMPTY, and that emptiness is backed by a real positive control — the same diff widened to the whole product tree lists exactly one file, CHANGELOG.md, with a nonzero insertion count. An empty-diff claim with no live control would look identical if the anchor were wrong.'
    - 'D-10 / D-08: the .planning/REQUIREMENTS.md checksum recorded at phase head is re-verified byte-for-byte at phase close, the three guarded REL-09-bearing lines are re-quoted and compared, and any divergence is REVERTED by hand and reported rather than committed.'
    - 'D-08: REL-09 is still an unchecked box with a Pending Traceability row at phase close. No plan in this phase touched it; it carries forward unmet to the v0.9.2 milestone with its literal wording — including its v0.9.1 version string — unchanged.'
    - 'D-12 / SC#5 (RETAINED and RE-AIMED by D-11): 61-HANDOFF.md opens by stating the negative — this milestone publishes nothing, and its /gsd-complete-milestone performs no tag, no PyPI publish and no GitHub Release — BEFORE any checklist item appears, inverting the opening polarity of seven consecutive prior handoffs.'
    - 'D-13: the three standing publish steps that must survive the milestone boundary are preserved as an INHERITANCE RECORD with the version written as a placeholder rather than hard-coded — the second-repository tag advanced by dispatching typsphinx-doc-translations'' own update-pin.yml rather than a hand-made clone-edit-push, the Read the Docs stable measurement for both projects, and the GitHub Release body being byte-identical to the extraction script''s stdout.'
    - 'D-06 / D-07: the inline-image blocker is named explicitly in the handoff with its todo path, its one-sentence shape and its measured status as a pre-existing defect rather than a regression, so the v0.9.2 milestone does not have to rediscover it.'
    - 'D-11: the handoff reports each ROADMAP success criterion in its D-11-mapped form — SC#1 as DROPPED rather than as MET or NOT-MET, SC#2 as REWORDED, SC#3 and SC#4 as RETAINED, SC#5 as RETAINED and RE-AIMED — citing each one''s own evidence artifact and section rather than restating its verdict.'
    - 'REL-09 is cited by this plan as the phase''s only requirement ID for coverage purposes only. This plan does NOT close it and does not touch its checkbox; per D-08 it stays ``[ ]`` and carries forward unmet to the v0.9.2 milestone with its literal wording unchanged.'
  artifacts:
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md
  key_links:
    - 'PHASE_BASE_SHA read back from 61-CLOSEOUT-GUARD.md § "Baseline" -> the scoped typsphinx/ diff and its whole-tree positive control: the anchor without which SC#4''s no-unintended-change claim is unfalsifiable.'
    - 'observation 1 timestamp (61-SC4-INVARIANTS.md, wave 1) -> observation 2 timestamp (this plan, wave 3): the separation SC#4 asks for, made measurable by two recorded wall-clock stamps rather than asserted.'
    - 'the phase-head checksum baseline -> the close-time re-verification -> the post-phase.complete protocol reproduced into 61-HANDOFF.md: the chain that carries the auto-flip defence past the last moment any plan can execute, which is exactly when the flip has historically landed.'
    - '61-HANDOFF.md -> the v0.9.2 release-prep phase and its /gsd-complete-milestone: the only carrier for three publish steps that are easy to lose and expensive to rediscover across a milestone boundary.'
  prohibitions:
    - 'MUST NOT perform any irreversible action in this phase — no local or remote tag, no PyPI publish, no GitHub Release, no pull request, no trigger of release.yml. 61-HANDOFF.md records the publish sequence as an inheritance record for a LATER milestone and is never a licence to execute it now.'
    - 'MUST NOT record REL-09 as satisfied. No checkbox flip, no Traceability-row status change, no prose in the handoff claiming a release occurred. A flip detected at close is reverted with git checkout and reported, never committed.'
    - 'MUST NOT publish a link or a version reference the project cannot resolve. The handoff''s inherited publish steps carry a version placeholder, not a hard-coded skipped version, so a future reader cannot copy a dead tag name out of it.'
    - 'MUST NOT record inherited, paraphrased or fabricated evidence as this phase''s own. Observation 2 is executed here at its own timestamp; the handoff cites each evidence file''s own section rather than restating or re-deriving its verdict.'
---

<objective>
Close the phase's fence proof and write the milestone's handoff. Re-probe the no-irreversible-action
fence at a genuinely later timestamp than observation 1, prove with a real positive control that
nothing under `typsphinx/` changed across the phase, re-verify the phase-head `REQUIREMENTS.md`
checksum and revert any flip by hand, and author `61-HANDOFF.md` — which must open by stating that
this milestone publishes nothing, before any checklist item appears.

Purpose: this plan runs two waves after the fence's first observation and one wave after the CI
dispatch, so its timestamps and its diff are taken at the phase's real end rather than at a
convenient moment. It also carries the one genuinely novel piece of work in this phase: seven
consecutive milestones have trained a handoff that opens as a publish checklist, and D-12 requires
this one to open as its own negation.

Output: `61-SC4-INVARIANTS.md` § observation 2 and the scoped diff; `61-CLOSEOUT-GUARD.md` §
close-time re-verification; and `61-HANDOFF.md`.

**Phase reframe (read before judging this plan against ROADMAP.md).** `61-CONTEXT.md` D-11 maps
ROADMAP Phase 61's success criteria: SC#1 (atomic bump to 0.9.1) is **DROPPED**, SC#2 is
**REWORDED**, SC#3 is **RETAINED and re-anchored**, SC#4 is **RETAINED in full** (this plan's first
two tasks), SC#5 is **RETAINED and RE-AIMED** at what the *v0.9.2* milestone inherits rather than at
what this milestone's `/gsd-complete-milestone` executes (this plan's third task). The handoff must
report SC#1 as DROPPED, not as met or unmet.
</objective>

<assumption_delta_decision>
## Assumption-Delta Architecture Checkpoint — disposition

**Detector result, verbatim:** `{"detected": true, "signals": [{"kind": "pluralization", "term":
"second", "snippet": "`/gsd-complete-milestone` must execute, including the standing
second-repository tag"}]}`

**Noun:** "the standing **second**-repository tag" — the `typsphinx-doc-translations` repository.

**Decision:** `no-change`.

**Rationale:** this is a lexical hit on an ordinal in an existing, long-standing fact of this
project's release procedure — the translations repository has been the second tag target for several
milestones and is advanced by dispatching that repository's own `update-pin.yml` — not a newly
introduced second case of anything. No identity model is generalizing: this phase introduces no new
platform, tenant, authentication method, region or source of truth, and in fact performs no publish
at all (D-02, D-12). The checkpoint is advisory and does not block; no architectural change is
invented to justify the signal, because a fabricated promotion would be worse than the false
positive.
</assumption_delta_decision>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-GREEN-TREE-EVIDENCE.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution";
`.planning/REQUIREMENTS.md` § "Standing constraints" #5). Before running ANY command in this plan,
provision this worktree's own virtual environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command that invokes project tooling with `uv run`.
</worktree_provisioning>

<!-- planner-discipline-allow: Latest -->
<!-- The word above is the marker the GitHub release listing prints against the current release, and
     it is the POSITIVE control (count == 1, proving the command reached GitHub) that makes the
     paired negative probe non-vacuous. It is part of the verification command's own grep regex. -->

<tasks>

<task type="auto">
  <name>Task 1: Record fence observation 2 of 2 and the phase-scoped typsphinx/ diff with a real positive control</name>
  <files>.planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md</files>
  <read_first>
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md — the file being modified. Read observation 1's recorded timestamp and probe outputs so observation 2 is directly comparable, and read its "Handoff to observation 2" section for the section name to append under.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md § "Baseline" — the PHASE_BASE_SHA this task's scoped diff is anchored at.
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md — the "hunk-level argument" escalation pattern used when an empty-diff assertion no longer suffices, and how Phase 57's own mid-phase `builder.py` fix broke its fence and had to be argued at hunk level.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md § `<decisions>` D-10 — in particular its note that, unlike Phase 57, this phase has NO amended exception for the `typsphinx/` diff, so a clean diff is the expected result and any hit is a real finding.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CI-EVIDENCE.md — the dispatched head SHA, so this task can state which commits post-date the CI run and confirm they are planning-only.
  </read_first>
  <action>
    Append to `.planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md`.

    Under `## Observation 2 of 2` re-run every probe observation 1 ran, in the same order and with the
    same positive controls, opening with `date -u +"%Y-%m-%dT%H:%M:%SZ"`: the local tag probe; the
    unfiltered remote tag listing with its two derived counts (one line for the v0.9.0 tag reference
    as the positive control, zero lines mentioning the skipped version's tag reference); the release
    listing with its `Latest` marker against `v0.9.0` as the positive control and no row naming the
    skipped version; and the release-workflow run listing. Record each as a verbatim command plus its
    verbatim output.

    Then state the separation explicitly: quote observation 1's timestamp beside observation 2's and
    give the elapsed interval. This observation is taken two waves later and after a full 3-OS CI
    dispatch has run, so the fence is shown to have held across the phase rather than only at its
    head.

    Under `## The typsphinx/ diff (SC#4)` read PHASE_BASE_SHA back from `61-CLOSEOUT-GUARD.md`
    § "Baseline" and record two diffs, both as verbatim commands plus outputs:

    - the scoped one, `git diff <PHASE_BASE_SHA>..HEAD -- typsphinx/`, which must be empty;
    - the widened one, `git diff <PHASE_BASE_SHA>..HEAD --stat` restricted to exclude `.planning`,
      which must list exactly one file, `CHANGELOG.md`, with a nonzero insertion count and zero
      deletions.

    State what the pair proves and why both are needed: the empty scoped diff is the SC#4 claim, and
    the non-empty widened diff is its **positive control** — an empty result from a wrong or
    non-existent anchor would look identical to a genuinely clean tree, so a live non-empty
    measurement from the same anchor is what makes the emptiness meaningful. Record explicitly that
    this phase has no amended exception for the `typsphinx/` fence — unlike Phase 57, where an
    owner-approved mid-phase `builder.py` message fix broke it and had to be argued at hunk level — so
    a clean diff is the expected result and any hit is a real finding to be reported, never explained
    away.

    Finally, under `## Commits after the CI dispatch`, read the dispatched head SHA from
    `61-CI-EVIDENCE.md` and record `git log --oneline <dispatched-sha>..HEAD` together with
    `git diff --name-only <dispatched-sha>..HEAD` restricted to exclude `.planning`, which must print
    nothing. This is what keeps the CI evidence valid at the phase's end: every commit landed after
    the dispatch is planning documentation only, so the tested product tree is still the phase tip's
    product tree.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; [ -z "$(git tag -l 'v0.9.1')" ] &amp;&amp; [ "$(git ls-remote --tags origin | grep -c 'refs/tags/v0\.9\.0$')" = 1 ] &amp;&amp; [ "$(git ls-remote --tags origin | grep -c 'refs/tags/v0\.9\.1')" = 0 ] &amp;&amp; [ "$(gh release list --limit 20 | grep -c 'Latest')" = 1 ] &amp;&amp; [ "$(gh release list --limit 20 | grep -c 'v0\.9\.1')" = 0 ] &amp;&amp; [ -z "$(git diff "$(grep -oE '^[0-9a-f]{40}$' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md | head -1)"..HEAD -- typsphinx/)" ] &amp;&amp; [ "$(git diff --name-only "$(grep -oE '^[0-9a-f]{40}$' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md | head -1)"..HEAD -- . ':(exclude).planning' | wc -l)" = 1 ] &amp;&amp; [ "$(git diff --name-only "$(grep -oE '^[0-9a-f]{40}$' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md | head -1)"..HEAD -- . ':(exclude).planning')" = 'CHANGELOG.md' ] &amp;&amp; grep -q 'Observation 2 of 2' .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md &amp;&amp; grep -q 'Commits after the CI dispatch' .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md &amp;&amp; [ "$(grep -cE '[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md)" -ge 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - `61-SC4-INVARIANTS.md` § "Observation 2 of 2" opens with a UTC timestamp and repeats all four probes from observation 1, each as a verbatim command plus output, each network-dependent one paired with the same positive control
    - the file contains at least two distinct UTC timestamps in `YYYY-MM-DDTHH:MM:SSZ` form, and observation 2's section quotes observation 1's timestamp beside its own with the elapsed interval stated
    - `61-SC4-INVARIANTS.md` § "The typsphinx/ diff (SC#4)" holds both diffs: the scoped one, empty, and the widened one listing exactly `CHANGELOG.md` with a nonzero insertion count and zero deletions
    - `git diff <PHASE_BASE_SHA>..HEAD -- typsphinx/` produces no output, and `git diff --name-only <PHASE_BASE_SHA>..HEAD` excluding `.planning` produces exactly the single line `CHANGELOG.md`
    - that section states in its own words that the non-empty widened diff is the positive control for the empty scoped one, and that this phase has no amended exception for the `typsphinx/` fence
    - `61-SC4-INVARIANTS.md` § "Commits after the CI dispatch" holds a `git log --oneline` from the dispatched head SHA and a `git diff --name-only` excluding `.planning` that produces no output, with the conclusion that every post-dispatch commit is planning documentation only
    - `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md uv.lock` is empty
  </acceptance_criteria>
  <done>The fence is proven to have held across the whole phase, not just at its head, with two genuinely separated timestamps and a positive control on every probe; and the `typsphinx/` diff is empty against a recorded anchor whose non-vacuousness is demonstrated by a live, non-empty widened measurement from the same anchor.</done>
  <reversibility rating="reversible">Evidence-file appends recording read-only probes; no write action is taken against any remote.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Re-verify the REQUIREMENTS.md closeout guard at phase close and revert any flip by hand</name>
  <files>.planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md</files>
  <read_first>
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md — the file being modified. Read its "Baseline" section (checksum, line count, timestamp, PHASE_BASE_SHA), its "The lines under guard" quotes, and its "Re-verification protocol (phase close)" section, which names the exact commands this task runs.
    - .planning/REQUIREMENTS.md § "REL — release" and § "Traceability" — the live state being compared against the recorded quotes.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md § `<decisions>` D-08 and D-10, and § `<specifics>` item 4 — the owner's explicit rejection of both rewriting REL-09 to name a later version and closing it as superseded, and the instruction that the response to a flip is reversion, not acceptance.
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md — the precedent's own re-verification wording.
  </read_first>
  <action>
    Append a `## Re-verification at phase close` section to
    `.planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md`, running exactly the
    commands the file's own re-verification protocol names and recording each as a verbatim command
    plus its output:

    - `date -u +"%Y-%m-%dT%H:%M:%SZ"`, so the close-time observation is timestamped;
    - `sha256sum .planning/REQUIREMENTS.md`, compared byte-for-byte against the recorded Baseline
      value and stated as MATCH or DIVERGED;
    - `wc -l .planning/REQUIREMENTS.md`, compared against the recorded Baseline line count;
    - `git diff --name-only -- .planning/REQUIREMENTS.md`, which must print nothing;
    - `grep -n 'REL-09' .planning/REQUIREMENTS.md`, compared line-for-line against the three recorded
      quotes and stated as byte-identical or not.

    State the verdict plainly: REL-09 is still an unchecked box, its Traceability row still reads
    Phase 61 and Pending, and no plan in this phase touched it. Per D-08 it carries forward unmet to
    the v0.9.2 milestone with its literal wording unchanged — including its `v0.9.1` version string,
    which the owner explicitly declined to rewrite and explicitly declined to close as superseded. The
    only inconsistency this leaves behind is a version number inside a requirement that has never been
    satisfied, which is accurate, because nothing was released.

    **If any comparison diverges**, do not proceed and do not commit the divergence. Record the full
    `git diff -- .planning/REQUIREMENTS.md` verbatim so the exact edit is on the record, run
    `git checkout -- .planning/REQUIREMENTS.md` to revert it, re-run the checksum and the three-line
    grep to confirm the reversion restored the Baseline byte-for-byte, and record the whole sequence
    under a `### Divergence detected and reverted` subsection together with the running count of
    consecutive occurrences. Then report it. The response to a flip is reversion and a report, never
    acceptance.

    Finally, append a `## For the operator running phase.complete` section restating — in commands, not
    prose — the three-command detection procedure and the one-command reversion, framed for the moment
    AFTER the phase-completion tooling has run. That moment is outside any plan's reach and is exactly
    where the flip has landed at five consecutive prior closes. Note that this same section is
    reproduced in `61-HANDOFF.md`, so an operator following the handoff reaches it without opening this
    file.
  </action>
  <verify>
    <automated>[ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(grep -oE '[0-9a-f]{64}' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md | head -1)" ] &amp;&amp; [ -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" ] &amp;&amp; grep -qE '^- \[ \] \*\*REL-09\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-09 \| Phase 61 \| Pending \|' .planning/REQUIREMENTS.md &amp;&amp; [ "$(grep -c 'REL-09' .planning/REQUIREMENTS.md)" = 3 ] &amp;&amp; grep -q 'Re-verification at phase close' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md &amp;&amp; grep -q 'For the operator running phase.complete' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `61-CLOSEOUT-GUARD.md` § "Re-verification at phase close" holds five command-plus-output blocks (timestamp, checksum, line count, name-only diff, three-line grep), each with an explicit MATCH or DIVERGED verdict against the recorded Baseline
    - the live `sha256sum .planning/REQUIREMENTS.md` equals the checksum recorded in the file's Baseline section
    - `git diff --name-only -- .planning/REQUIREMENTS.md` produces no output and `git status --porcelain .planning/REQUIREMENTS.md` is empty
    - `.planning/REQUIREMENTS.md` still matches `^- \[ \] \*\*REL-09\*\*` and `^\| REL-09 \| Phase 61 \| Pending \|`, and `grep -c 'REL-09'` over it returns exactly 3
    - the section states the verdict in words: REL-09 is unmet, carries forward to v0.9.2 with its literal wording unchanged, and was touched by no plan in this phase
    - if any comparison diverged, a `### Divergence detected and reverted` subsection holds the verbatim `git diff`, the `git checkout -- .planning/REQUIREMENTS.md` reversion, the post-reversion re-verification, and the updated running count — and the divergence is not committed
    - `61-CLOSEOUT-GUARD.md` § "For the operator running phase.complete" gives the detection commands and the reversion command for the moment after the completion tooling has run, and notes that the same section is reproduced in `61-HANDOFF.md`
  </acceptance_criteria>
  <done>The phase-head checksum and the three guarded REL-09-bearing lines are re-verified at phase close with an explicit verdict, any divergence is reverted by hand and recorded rather than shipped, and a post-`phase.complete` detection-and-reversion procedure is left where the operator will actually reach it.</done>
  <reversibility rating="reversible">Evidence-file appends, plus — only on divergence — a `git checkout` restoring a tracked file to its committed state.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Author 61-HANDOFF.md, opening with the negative and preserving the publish steps as an inheritance record</name>
  <files>.planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md</files>
  <read_first>
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md — the most recent structural precedent: the "what this phase satisfied and what it did not" section, the SC-by-SC list that cites each criterion's own evidence artifact rather than restating it, and the Owner/Ordering checklist item shape. Read it specifically to see the opening polarity this file must INVERT.
    - .planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md — the older sibling with the same skeleton and the "observation N of N" fence-probe placement inside the handoff itself.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md § `<decisions>` D-02, D-06, D-07, D-08, D-11, D-12, D-13 and § `<specifics>` item 3 — the decisions this document is the carrier for.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md § "`61-HANDOFF.md`" — the inherited skeleton, and the itemised list of exactly which piece must not be copied mechanically.
    - .planning/REQUIREMENTS.md § "REL — release" — REL-09's exact wording, to be quoted verbatim in the handoff.
    - .planning/todos/pending/2026-08-29-inline-image-in-paragraph-emits-unseparated-expression.md — the blocker's reproduction, root cause and solution sketch, which the handoff names so v0.9.2 does not rediscover it.
    - .planning/todos/pending/2026-08-04-release-create-job-missing-uv-verify-end-to-end.md — the release-workflow todo CONTEXT says is not folded here but should be re-offered at the v0.9.2 release-prep phase's handoff.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md § "Pitfall 4" — the exact failure mode this task is written to avoid.
  </read_first>
  <action>
    Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md`.

    **The opening is the one piece that must not be copied mechanically.** Every prior handoff this
    project has produced opens, implicitly through its checklist structure, as if a publish were
    imminent — because every prior milestone did publish. The very first section of this file, before
    any checklist item appears, must state the negative in plain words: this milestone's
    `/gsd-complete-milestone` performs no tag, no PyPI publish, no GitHub Release and no pull request;
    v0.9.1 is never published and the next published release is 0.9.2; and everything that follows is
    an inheritance record for the v0.9.2 release-prep phase, not a set of steps to execute now.

    Then a `## What this phase satisfied, and what it did not` section. Quote REL-09 verbatim from
    `.planning/REQUIREMENTS.md` and state that it remains open and unmet, carrying forward to v0.9.2
    with its literal wording — including its v0.9.1 version string — unchanged, because the owner
    explicitly declined both rewriting it and closing it as superseded. Then report each ROADMAP
    success criterion in its D-11-mapped form, citing that criterion's own evidence artifact and
    section rather than restating or re-deriving the verdict: SC#1 as **DROPPED** (state this
    explicitly rather than reporting met or unmet); SC#2 as **REWORDED**, citing
    `61-CHANGELOG-EVIDENCE.md`; SC#3 as **RETAINED and re-anchored**, citing
    `61-GREEN-TREE-EVIDENCE.md` and `61-CI-EVIDENCE.md`; SC#4 as **RETAINED in full**, citing
    `61-SC4-INVARIANTS.md` and `61-CLOSEOUT-GUARD.md`; SC#5 as **RETAINED and RE-AIMED**, citing this
    document itself.

    Then a `## What the v0.9.2 milestone inherits` section carrying the three standing publish steps
    that must survive the boundary because they are easy to lose and expensive to rediscover. Write
    each with its version as the placeholder `vX.Y.Z` rather than hard-coded to the skipped version,
    so no future reader can copy a dead tag name out of this file, and keep the Owner and Ordering
    framing the prior handoffs used — but relabelled as what to remember rather than what to do:

    - the second-repository tag for `typsphinx-doc-translations`, advanced by dispatching **that
      repository's own `update-pin.yml` workflow**, not by a hand-made clone-edit-push;
    - the Read the Docs `stable` measurement for both projects, which is doable with unauthenticated
      public API calls;
    - the GitHub Release body being byte-identical to `scripts/extract_changelog_section.py`'s stdout
      for the released version — the script that this phase deliberately did not invoke, because there
      is no versioned section for it to extract yet.

    Add a `## What v0.9.2 must also pick up` section naming, with paths: the inline-image blocker todo
    — a pre-existing defect measured against the v0.9.0 tag rather than a regression, whose shape is
    that an image node preceded by sibling content in the same paragraph or list item is emitted
    adjacent to the preceding expression, so Typst refuses it and no master document produces a PDF —
    and the `release.yml` `create-release` job todo, which CONTEXT declines to fold here because with
    no publish there is nothing to verify it against, and which should be re-offered at the v0.9.2
    release-prep phase's own handoff. Also record that the `## [Unreleased]` bullets this phase
    authored are promoted into the v0.9.2 versioned section together with the blocker fix's own bullet.

    Add a `## Before declaring the milestone closed` section reproducing, verbatim from
    `61-CLOSEOUT-GUARD.md` § "For the operator running phase.complete", the three detection commands
    and the one reversion command for the REL-09 auto-flip, with the count stated as five consecutive
    prior occurrences and the rule that the flip is reverted and reported, never accepted.

    Close with a `## Fence observation` section recording a live `git diff --name-only` restricted to
    `.planning/REQUIREMENTS.md` (expected empty) and a live `sha256sum .planning/REQUIREMENTS.md`
    matching the Baseline in `61-CLOSEOUT-GUARD.md`, following the prior handoffs' practice of placing
    a final observation inside the handoff itself.
  </action>
  <verify>
    <automated>test -f .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md &amp;&amp; [ "$(head -12 .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md | grep -ciE 'publishes nothing|no PyPI publish|performs no publish|is never published')" -ge 1 ] &amp;&amp; [ "$(head -12 .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md | grep -c '^### ')" = 0 ] &amp;&amp; grep -q 'vX.Y.Z' .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md &amp;&amp; grep -q 'update-pin.yml' .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md &amp;&amp; grep -q 'extract_changelog_section.py' .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md &amp;&amp; grep -q 'stable' .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md &amp;&amp; grep -q '2026-08-29-inline-image-in-paragraph-emits-unseparated-expression' .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md &amp;&amp; grep -q 'DROPPED' .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md &amp;&amp; grep -q 'REWORDED' .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md &amp;&amp; grep -q 'five consecutive' .planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(grep -oE '[0-9a-f]{64}' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md | head -1)" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.1')" ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md .planning/REQUIREMENTS.md)" ]</automated>
    <human-check>Read the first screenful of `61-HANDOFF.md` as if you were an operator about to close the milestone. Confirm it reads as a statement that nothing is published — not as a publish checklist whose first item happens to be missing. Seven consecutive prior handoffs trained the opposite reading, so this is the one thing a grep cannot confirm.</human-check>
  </verify>
  <acceptance_criteria>
    - `61-HANDOFF.md`'s first 12 lines state that this milestone publishes nothing (no tag, no PyPI publish, no GitHub Release, no pull request) and contain no `###` checklist heading — the negative precedes every checklist item
    - the file quotes REL-09 verbatim from `.planning/REQUIREMENTS.md` and states it remains open and unmet, carrying forward to v0.9.2 with its literal wording unchanged
    - the file reports SC#1 as `DROPPED` explicitly rather than as met or unmet, and reports SC#2 as `REWORDED`, SC#3 and SC#4 as retained, and SC#5 as retained and re-aimed, each citing its own evidence artifact by filename and section
    - the three inherited publish steps are present and name `update-pin.yml`, the Read the Docs `stable` measurement for both projects, and `scripts/extract_changelog_section.py`
    - every version reference inside the three inherited publish steps is the placeholder `vX.Y.Z`, not a hard-coded version
    - the file names `.planning/todos/pending/2026-08-29-inline-image-in-paragraph-emits-unseparated-expression.md` and describes the blocker as a pre-existing defect rather than a regression, and separately names the `release.yml` `create-release` todo as something to re-offer at the v0.9.2 handoff
    - the file contains a section reproducing the REL-09 auto-flip detection commands and the `git checkout -- .planning/REQUIREMENTS.md` reversion, with the count stated as **five consecutive** prior occurrences
    - the closing fence observation records a live empty `git diff --name-only -- .planning/REQUIREMENTS.md` and a live `sha256sum` matching `61-CLOSEOUT-GUARD.md`'s Baseline
    - `git tag -l 'v0.9.1'` is empty and `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md .planning/REQUIREMENTS.md` is empty
  </acceptance_criteria>
  <done>`61-HANDOFF.md` opens by stating that this milestone publishes nothing, reports every ROADMAP success criterion in its D-11-mapped form against its own evidence artifact, and preserves the three standing publish steps, the inline-image blocker and the auto-flip reversion procedure as an inheritance record for v0.9.2 with the version left as a placeholder.</done>
  <reversibility rating="costly">This document is the carrier for D-02 — v0.9.1 is never published. Reversing that decision later means publishing 0.9.1 from a tree that will by then contain the v0.9.2 milestone's work, so both the CHANGELOG section and the tag content would have to be reconstructed. The document itself is a file; the decision it records is not cheap to undo. No checkpoint is inserted: the owner made this exact choice in the discuss-phase session and the rating is carried forward from CONTEXT.md for the record, not to re-ask.</reversibility>
</task>

</tasks>

<flagged_assumptions>
## Spec-less edge-coverage accounting (pointer)

The phase's single probe-surfaced edge item — REL-09, category `unclassified`, status **`unresolved`**
— is surfaced in full in `61-01-PLAN.md` § "Spec-less edge-coverage accounting". It is honest to leave
unresolved because the probe ran against REL-09's literal PyPI-release text, which D-01, D-02 and D-08
place out of scope for this phase and this milestone; this plan is the one that carries that fact
forward into `61-HANDOFF.md` as an open inheritance. Restated here so no plan drops it silently.
Accounting for the phase: 1 surfaced = 0 authored into `truths` + 1 flagged assumption + 0 dismissed +
0 dropped.

## Prohibition recall accounting (pointer)

The full two-stage recall/precision accounting and the five kept, descriptor-less prohibitions are in
`61-01-PLAN.md` § "Prohibition recall accounting". This plan carries P1 (no irreversible action),
P2 (no unresolvable link or version reference), P3 (REL-09 must not be recorded as satisfied) and
P5 (no inherited, paraphrased or fabricated evidence) in its own `must_haves.prohibitions`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local worktree → `origin` / GitHub API (read-only probes) | observation 2's probes cross this boundary; all are reads and no write action is taken |
| `.planning/REQUIREMENTS.md` → the `phase.complete` tooling that runs after this plan | the requirement state this phase must not change, and which automated close tooling has changed anyway at five consecutive prior closes — outside any plan's execution window |
| `61-HANDOFF.md` → a future operator's actions at the v0.9.2 close | this document is read as instructions; a mis-framed one causes a real publish to be attempted from a milestone that must not publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-14 | Elevation of Privilege | a handoff whose checklist reads as steps to execute now, causing an operator to tag, publish or open a release for a milestone that must not publish (ASVS V4 Access Control, L1, by analogy: a document granting an action it was not meant to authorise) | high | mitigate | The opening section must state the negative before any checklist item; the automated gate asserts no `###` heading appears in the first 12 lines and that a no-publish statement does; the checklist is relabelled as an inheritance record; and a human check reads the first screenful specifically for polarity, which a grep cannot confirm. |
| T-61-15 | Tampering | the `phase.complete` auto-flip landing after this plan's last command and shipping a satisfied REL-09 against the phase's own decision (ASVS V4, L1) | high | mitigate | Close-time re-verification against the phase-head baseline with an explicit MATCH or DIVERGED verdict, a hand reversion path with `git checkout` recorded verbatim on divergence, and the same detection-and-reversion procedure reproduced into `61-HANDOFF.md` so the operator reaches it at the moment the flip actually lands. |
| T-61-16 | Repudiation | an empty `typsphinx/` diff accepted as proof when a wrong or missing anchor would produce the identical empty result (ASVS V7 Logging, L1) | high | mitigate | The scoped empty diff is paired with a live widened diff from the SAME anchor that must list exactly one file with a nonzero insertion count — a real positive control that fails if the anchor is wrong. The gate asserts both the emptiness and the single-file non-emptiness. |
| T-61-17 | Repudiation | two fence observations collapsed into effectively one instant, satisfying "probed twice at separated times" only nominally | medium | mitigate | Observation 2 runs two waves and a full CI dispatch after observation 1; both record a UTC wall-clock timestamp, the file must contain at least two distinct stamps, and the elapsed interval is stated. |
| T-61-18 | Information Disclosure | a dead version reference copied out of the handoff into a future release, publishing a link the project cannot resolve | medium | mitigate | Every version inside the three inherited publish steps is the placeholder `vX.Y.Z` rather than a hard-coded skipped version, gated by an acceptance criterion and an automated presence check. |
| T-61-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. `61-RESEARCH.md` § "Package Legitimacy Audit" records the phase as introducing no external package. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `61-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. This plan creates
one new file, `.planning/phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md`, and appends to two
files created by plan 61-02:
`.planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md` (observation 2 of 2, the
scoped `typsphinx/` diff, and the post-dispatch commit check) and
`.planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md` (the close-time
re-verification and the post-`phase.complete` operator procedure). It modifies no product-tree file
and does not edit `.planning/REQUIREMENTS.md` — which it reads, quotes, and reverts only if something
else has changed it. **No file named `61-VERIFICATION.md` is produced or referenced as an output.**
</artifacts_this_phase_produces>

<verification>
- `61-SC4-INVARIANTS.md` holds two fence observations at distinct UTC timestamps with the elapsed
  interval stated, each network-dependent probe carrying a positive control
- the scoped `typsphinx/` diff from PHASE_BASE_SHA to the phase tip is empty, and the widened diff from
  the same anchor lists exactly `CHANGELOG.md` with a nonzero insertion count
- every commit landed after the CI dispatch is planning documentation only, proven by a `git
  diff --name-only` excluding `.planning` that produces no output
- the `.planning/REQUIREMENTS.md` checksum at phase close matches the phase-head Baseline, the file is
  unmodified, and REL-09 is still an unchecked box with a Pending Traceability row
- `61-HANDOFF.md` opens with the negative before any checklist item, reports SC#1 as DROPPED and the
  rest in their D-11-mapped forms against their own evidence artifacts, and preserves the three
  inherited publish steps with a version placeholder
- `git tag -l 'v0.9.1'` is empty, no release for the skipped version exists, and
  `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md .planning/REQUIREMENTS.md`
  is empty
- no file named `61-VERIFICATION.md` exists under the phase directory
</verification>

<success_criteria>
1. The no-irreversible-action fence is probed twice at genuinely separated wall-clock timestamps, each
   negative result backed by a positive control proving the probe reached its source — ROADMAP SC#4,
   RETAINED in full per D-11.
2. The phase-scoped `typsphinx/` diff is empty against a recorded anchor, with a live non-empty widened
   diff from the same anchor as its positive control and an explicit statement that this phase has no
   amended exception to the fence.
3. The `REQUIREMENTS.md` checksum recorded at phase head is re-verified at phase close with an explicit
   verdict, and any divergence is reverted by hand and reported rather than committed. REL-09 remains
   an unchecked box with a Pending Traceability row.
4. `61-HANDOFF.md` opens by stating that this milestone publishes nothing, before any checklist item —
   inverting the opening polarity of seven consecutive prior handoffs.
5. The three standing publish steps, the inline-image blocker, the `release.yml` job todo, and the
   REL-09 auto-flip reversion procedure all survive the milestone boundary as an inheritance record
   with the version written as a placeholder — ROADMAP SC#5, RETAINED and RE-AIMED per D-11.
</success_criteria>

<output>
Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-04-SUMMARY.md` when done
</output>
