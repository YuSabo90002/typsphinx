---
phase: 60-one-delimiter-aware-path-quoting-helper-routed-everywhere
plan: 04
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - typsphinx/template_registry.py
  - tests/test_template_registry_path_quoting_gate.py
  - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md
autonomous: true
requirements: [MSG-05]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'MSG-05/D-06: the template value in typsphinx/template_registry.py''s CONF-17 violation message and in its existence-check message routes through quote_path(); the registry key on both lines keeps its current repr conversion (D-07).'
    - 'MSG-05/D-12 shape 1: resolve_template_registry() raised with a Windows-shaped str template produces an ExtensionError whose str() contains no run of two or more consecutive backslashes -- recorded RED (four doubled runs) against the unfixed tree first.'
    - 'MSG-05/D-12 shape 2: resolve_template_registry() raised with a pathlib.Path template that does not exist produces an ExtensionError whose str() contains no class-name wrapper -- the substring PosixPath does not appear -- because quote_path() normalizes through os.fspath() BEFORE inspecting any quote character. Recorded RED against the unfixed tree first; this shape has no existing test coverage at all.'
    - 'MSG-05/SC#3: the type-check message two lines above the two routed sites is measurably still on Python''s repr conversion -- it is reached precisely when the template value is NOT str and NOT os.PathLike, so routing it through a path helper would be actively wrong AND would raise TypeError on the exact values it exists to report.'
    - 'MSG-05/SC#3 falsification: the two existing assertions that pin the type-check message''s repr output for a list template and a bytes template stay green with ZERO edits -- they are the live regression proof that the excluded site was not accidentally routed.'
    - 'MSG-05 (edge: adjacency): the CONF-17 check and the existence check are structurally independent -- neither is an elif of the other -- so a template value that is both CONF-17-violating and nonexistent reports BOTH failures in one accumulated raise, each independently quoted, separated by the joiner rather than merged.'
    - 'MSG-05 (edge: empty): an empty template value is falsy, so the elif guard means neither routed site is reachable with it -- quote_path() is never called with an empty value from this module, and that unreachability is asserted rather than assumed.'
    - 'MSG-05 (edge: encoding): the isinstance guard immediately above the two routed sites guarantees the template reaching them is str or os.PathLike, so quote_path() can never receive a bytes, list or int value from this module; a pathlib.Path is normalized via os.fspath() so no class-name wrapper survives, and no Unicode normalization is applied to the value.'
    - 'MSG-05 (edge: ordering): the accumulated failures list order -- CONF-17 before existence for one key, and keys in registry iteration order -- is unchanged by this plan, and the joiner that renders it is untouched.'
    - 'Zero test edits: this plan adds one new test module and modifies no pre-existing test file at all.'
  artifacts:
    - tests/test_template_registry_path_quoting_gate.py
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md
    - 'typsphinx/template_registry.py :: two routed template interpolations (CONF-17 violation, existence check)'
  key_links:
    - 'typsphinx/template_registry.py -> typsphinx.pathfmt.quote_path: one added module-scope import line. This module currently avoids a cycle with builder.py only via a lazy function-scoped import; pathfmt.py imports nothing from typsphinx, so the new import is safe at module scope and adds no edge to that graph.'
    - 'the isinstance(template, (str, os.PathLike)) guard -> the two routed sites: the guard is what makes routing safe here, and it is why the type-check message in its own branch must stay unrouted.'
    - 'quote_path() -> os.fspath(): the ordering inside the helper (normalize first, inspect quote characters second) is what removes the pathlib class-name wrapper this plan''s shape-2 gate asserts against.'
  prohibitions:
    - 'The type-check message (the branch reached when the template value is NOT str and NOT os.PathLike) must NOT be routed. It will look like an oversight sitting two lines above two routed siblings; SC#3 makes leaving it alone a MEASURED pass criterion, and routing it would raise TypeError on the very list/bytes values it exists to report.'
    - 'The registry key on both routed lines must not be routed -- it is identifier-valued (D-07) and CONF-18 forbids a path separator in it, so it is never a filesystem location.'
    - 'The two existing assertions pinning the type-check message''s repr output for a list template and a bytes template must not be edited, relocated or weakened -- they are this phase''s falsification gate.'
    - 'This plan must not assert on any string emitted by typsphinx/builder.py or typsphinx/writer.py -- those belong to the sibling wave-2 plans, and a cross-module assertion would collide at merge even with disjoint files_modified.'
    - 'This plan must not touch tests/test_templates_path_collision_gate.py -- extending TestWindowsPathEscapingRegressionGuard is a one-plan privilege held by the builder.py wiring plan (D-11).'
    - 'The two routed messages'' wording must not be reworded or "improved" -- existing tests assert on the substrings CONF-17 and does not exist.'
    - 'No existing test assertion anywhere under tests/ is modified.'
    - 'No new runtime dependency, no new typst_* config value, and no typing-import modernization anywhere in this diff (ROADMAP constraint 14).'
---

<objective>
Close MSG-05: `typsphinx/template_registry.py`'s CONF-17 violation message and its existence-check
message route their `template` value through `quote_path()`, gated by a new test module recording
BOTH of D-12's independent RED shapes — the doubled backslash for a `str` template, and the leaked
`pathlib` class-name wrapper for a `Path` template — while the type-check message two lines above is
measurably left alone.

Purpose: this module is the phase's only site with a deliberate, adjacent EXCLUSION. Getting the
boundary right here is what SC#3 measures, and the excluded site's two existing guarding assertions
are the phase's falsification gate for accidental over-reach.

Output: the wired `typsphinx/template_registry.py`,
`tests/test_template_registry_path_quoting_gate.py`, and `60-04-EVIDENCE.md`.

**Wave note (D-09/D-11):** this is one of wave 2's THREE PARALLEL plans (`builder.py` / `writer.py` /
`template_registry.py`). It carries its own new test module and asserts only on
`template_registry.py`'s own output. It must not touch
`tests/test_templates_path_collision_gate.py` — that is the `builder.py` plan's exclusive privilege.

**Line numbers in this plan are anchors, not coordinates.** Re-measure with the discovery grep in
task 1; the description (branch + variable name) is the stable identity.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATTERNS.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-RESEARCH.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-VALIDATION.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 11). Before running ANY command in this plan, provision the worktree's own venv:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Without this, pytest imports the UNCHANGED
main-tree package and the gate stays RED after a correct fix. Do not degrade to main-tree execution.
</worktree_provisioning>

<tasks>

<task type="tracer">
  <name>Task 1: Run the discovery grep, then commit MSG-05's two RED shapes and the exclusion control</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md, tests/test_template_registry_path_quoting_gate.py</files>
  <read_first>
    - typsphinx/template_registry.py lines 380-440 (the whole `template` validation block: the isinstance guard and its type-check failure, then the `elif template:` branch with the CONF-17 check and the existence check — read the comments, which state why `str` AND `os.PathLike` are both accepted and why the two checks are structurally independent)
    - typsphinx/template_registry.py lines 137-176 (`_violates_conf17` — the `os.path.commonpath` arithmetic that decides the CONF-17 branch, needed to construct a shape that reaches it)
    - typsphinx/template_registry.py lines 440-460 (the accumulated `"; ".join(failures)` raise)
    - tests/test_template_registry.py lines 480-530 (the `pytest.raises(ExtensionError)` + `str(excinfo.value)` substring idiom, and the CONF-17 assertion form)
    - tests/test_template_registry.py lines 590-650 (the existence-check tests and their substring assertions)
    - tests/test_template_registry.py lines 820-900 (the list-typed and bytes-typed template tests whose `repr()` assertions are this phase's falsification gate, and the `pathlib.Path` control test proving a Path template is a supported shape — READ these, do NOT edit them)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md § D-05, D-06, D-07, D-11, D-12 and § "Specific Ideas" item 4
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-RESEARCH.md § "Pitfall 3" (the measured `PosixPath('…')` leak shape)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md § "Handoff to wave 2"
  </read_first>
  <action>
    FIRST create
    `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md`
    (per D-10 this exact name; `60-VERIFICATION.md` is reserved and must never be produced). Give it a
    `# Phase 60 Plan 04 — MSG-05 Evidence` heading, a `## Plan base SHA` section holding
    `git rev-parse HEAD` taken before any edit, and empty `## Discovery grep`,
    `## RED shape 1 — doubled backslash (str template)`,
    `## RED shape 2 — leaked class-name wrapper (Path template)`,
    `## Exclusion control (two-tree)`, `## GREEN` and `## Known gate gaps` sections.

    THEN run `grep -n "\!r" typsphinx/template_registry.py` and paste the verbatim output into
    `## Discovery grep` with the line numbers as measured RIGHT NOW. Classify each hit in one line
    under D-05's role rule: every registry-key interpolation in every form stays unrouted; the
    non-`dict` declared-value and raw-definition interpolations stay unrouted (non-path types); the
    sorted key lists stay unrouted; the type-check branch's `template` stays unrouted (the deliberate
    exclusion); and exactly two `template` interpolations route. Note that this module's `repr()` call
    inside its key sort is a SORT KEY, not a message interpolation, and is outside SC#2's scope
    entirely.

    THEN create `tests/test_template_registry_path_quoting_gate.py`. Its module docstring states that
    this is MSG-05's gate, that both routed sites raise `ExtensionError` so the only observable is
    `str(excinfo.value)` (D-12), that D-12 gives this site TWO independent RED shapes, and that it
    asserts ONLY on strings `typsphinx/template_registry.py` itself emits — never on a string emitted
    by `builder.py` or `writer.py`, which belong to the sibling wave-2 plans. Define a module-level
    `_assert_no_doubled_separator(message)` helper re-deriving the guard predicate with
    `re.findall(r"\\\\+", message)` locally (do not import from
    `tests/test_templates_path_collision_gate.py`, and do not touch that file). Write three classes:

    `TestRegistryTemplatePathQuoting` — RED shape 1. Two methods driving
    `resolve_template_registry(app.config, str(app.srcdir))` inside `pytest.raises(ExtensionError)`
    with `temp_sphinx_app`.
    (a) `test_conf17_violation_message_has_no_doubled_separator`: set
    `app.config.typst_document_templates = {"mykey": {"template": "C:\\Users\\runner\\base.typ"}}`.
    On a POSIX host the join of `srcdir` with that single backslash-bearing component leaves the
    parent directory equal to `srcdir`, so the CONF-17 branch fires; the existence check fires too, so
    both failures accumulate in one raise. Assert `str(excinfo.value)` contains the substring `CONF-17`
    and passes `_assert_no_doubled_separator`, and that the raw template value with its single
    separators is present in the message.
    (b) `test_existence_check_message_has_no_doubled_separator`: set the template to
    `"_typst/nested/C:\\Users\\runner\\base.typ"` — the forward-slash prefix moves the parent below
    `srcdir` so the CONF-17 branch does NOT fire and only the existence failure is raised. Assert
    `str(excinfo.value)` contains `does not exist`, passes `_assert_no_doubled_separator`, and does not
    contain `CONF-17`.
    Verify both shapes actually reach their intended branch when you run them; if one does not, read
    `_violates_conf17` and find the shape that does, then record the substitution and its reason in the
    evidence file rather than weakening the assertion.

    `TestRegistryPathLikeTemplateNoClassWrapper` — RED shape 2, which has NO existing coverage
    anywhere in the suite. Method
    `test_pathlike_template_existence_message_leaks_no_class_wrapper`: set
    `app.config.typst_document_templates = {"mykey": {"template": pathlib.Path("/some/path/_templates/nested/base.typ")}}`
    (an absolute `Path`, so the join returns it verbatim and its parent is outside `srcdir`, leaving
    only the existence failure). Assert `str(excinfo.value)` contains `does not exist`, contains the
    plain string form of the path, and does NOT contain the substring `PosixPath`. Give it a docstring
    recording that a `Path`-typed template is a deliberately supported shape (there is an existing
    control test proving it resolves), so its failure message leaking Python's internal class-name
    wrapper is a real user-facing defect and not a hypothetical.

    `TestRegistryTypeCheckMessageStaysReprQuoted` — SC#3's exclusion control. It must be GREEN both
    before and after the fix. Give its methods names containing the token
    `type_check_message_stays_repr_quoted` so the selector `-k type_check_message_stays_repr_quoted`
    picks up exactly these. Drive the same function with
    `{"bad_tpl": {"template": ["a", "b"]}}` and then with `{"bad_tpl": {"template": b"base.typ"}}`.
    For each: assert the raised error is an `ExtensionError` and NOT a `TypeError`, assert
    `str(excinfo.value)` contains `must be a path string`, and assert it contains the exact rendering
    Python's own repr produces for that value. Give the class a docstring recording that this branch is
    reached precisely when the value is NOT a path type, that routing it would both misrepresent a
    list or bytes value as a filesystem location AND raise `TypeError` on the very values it exists to
    report, and that SC#3 makes leaving it alone a measured pass criterion rather than an oversight.

    Run `uv run pytest tests/test_template_registry_path_quoting_gate.py -q` and capture the FULL
    verbatim output. Paste shape 1's failures into `## RED shape 1 — doubled backslash (str template)`,
    shape 2's failure into `## RED shape 2 — leaked class-name wrapper (Path template)`, and the
    already-passing exclusion control into `## Exclusion control (two-tree)` labelled as the pre-fix
    half — each with its command and the plan base SHA. Commit the gate and the evidence file
    together; the wiring is task 2.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; ! uv run pytest tests/test_template_registry_path_quoting_gate.py -q &amp;&amp; uv run pytest tests/test_template_registry_path_quoting_gate.py -q -k type_check_message_stays_repr_quoted &amp;&amp; grep -q 'Discovery grep' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md &amp;&amp; test -z "$(git status --porcelain typsphinx/)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_template_registry_path_quoting_gate.py` exists and `grep -c 'class Test' tests/test_template_registry_path_quoting_gate.py` returns 3
    - `uv run pytest tests/test_template_registry_path_quoting_gate.py -q` exits NON-ZERO with exactly 3 failures at this point in the plan
    - `uv run pytest tests/test_template_registry_path_quoting_gate.py -q -k type_check_message_stays_repr_quoted` exits 0 — the exclusion control is green BEFORE the fix
    - `grep -c 'PosixPath' tests/test_template_registry_path_quoting_gate.py` is at least 1
    - `grep -c 'render_wrapper' tests/test_template_registry_path_quoting_gate.py` returns 0 and `grep -c 'post_process_images' tests/test_template_registry_path_quoting_gate.py` returns 0
    - `git status --porcelain tests/` lists only the new file (no pre-existing test file is modified)
    - `60-04-EVIDENCE.md` § "Discovery grep" holds the verbatim `grep -n` output with a one-line classification per hit
    - both `## RED shape 1 …` and `## RED shape 2 …` sections hold verbatim failing transcripts
    - `git status --porcelain typsphinx/` is empty (no product file touched by this task)
  </acceptance_criteria>
  <done>Both of MSG-05's independent RED shapes are committed failing with verbatim transcripts, the exclusion control is committed already-green as the pre-fix half of a two-tree pin, and no product file has been touched.</done>
  <reversibility rating="reversible">One new test module plus a planning file; both deletable with no downstream effect.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route template_registry.py's two path-valued template interpolations through quote_path()</name>
  <files>typsphinx/template_registry.py</files>
  <read_first>
    - typsphinx/template_registry.py lines 1-40 (the module-scope import block the one new import line joins) and lines 80-100 (the lazy function-scoped `typsphinx.builder` import and its own docstring explaining the cycle it avoids — the reason `pathfmt.py` had to be a new leaf module)
    - typsphinx/template_registry.py lines 400-440 (the exact region this task edits — re-derive its line numbers from task 1's discovery grep)
    - typsphinx/pathfmt.py (the helper's signature and its `os.fspath()`-first ordering)
    - tests/test_template_registry_path_quoting_gate.py (the gate committed by task 1 — the contract this task must satisfy)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATTERNS.md § "typsphinx/template_registry.py wiring plan (MSG-05, wave 2)"
  </read_first>
  <behavior>
    - a Windows-shaped `str` template that violates CONF-17 raises `ExtensionError` whose message shows single separators
    - a Windows-shaped `str` template that merely does not exist raises `ExtensionError` whose message shows single separators
    - a nonexistent `pathlib.Path` template raises `ExtensionError` whose message contains no class-name wrapper
    - a `list` template still raises `ExtensionError` whose message contains Python's own repr of the list
    - a `bytes` template still raises `ExtensionError` whose message contains Python's own repr of the bytes
  </behavior>
  <action>
    Add one module-scope import to `typsphinx/template_registry.py`, in the existing import block:
    `from typsphinx.pathfmt import quote_path`. This is safe at module scope even though this module
    keeps its `typsphinx.builder` import function-scoped to avoid a cycle — `pathfmt.py` imports
    nothing from `typsphinx`, which is exactly why it had to be a new leaf module (ROADMAP constraint
    6). Do not move or alter the existing lazy import.

    Inside the `elif template:` branch, substitute the helper call for the repr conversion on the
    `template` value at exactly TWO sites: the CONF-17 violation failure (~:422) and the existence-check
    failure (~:433). Leave the registry `key` on both lines exactly as it is — it is identifier-valued
    (D-07), and CONF-18 already forbids a path separator in a registry key, so it is never a filesystem
    location.

    **Do NOT touch the type-check failure two lines above the `elif` (~:410).** It is reached precisely
    when the template value is neither `str` nor `os.PathLike` — a `list`, `bytes`, an `int` — so
    quoting it as a path would be actively wrong, and `quote_path()` would raise `TypeError` on the
    exact values that branch exists to report, replacing a clear configuration error with a crash. It
    will look like an oversight sitting two lines above two routed siblings; SC#3 makes leaving it
    alone a MEASURED pass criterion, and the two existing assertions pinning its repr output for a
    list template and a bytes template are the falsification gate. Do not add a comment claiming it is
    a TODO or a follow-up; if you add a comment at all, state that the exclusion is deliberate and
    cite MSG-05 and SC#3.

    Do not reword either routed message. Existing tests assert on the substrings `CONF-17` and
    `does not exist`, and the two checks must stay structurally independent — neither becomes an `elif`
    of the other, so a value that is both CONF-17-violating and nonexistent still reports both failures
    in one accumulated raise.

    Do not touch `typsphinx/builder.py` or `typsphinx/writer.py` — they belong to the sibling wave-2
    plans.

    Run the gate, the full suite, `black --check .`, `mypy typsphinx/`, and
    `uv run pytest tests/test_repr_census_guard.py -q`. If mypy objects at either routed call site
    because the template value's declared type is wider than the helper's, narrow at that call site
    (the surrounding `isinstance(template, (str, os.PathLike))` guard already establishes the fact) —
    never widen `quote_path()`'s signature.

    Paste the verbatim green transcripts into `## GREEN`, and complete the two-tree measurement in
    `## Exclusion control (two-tree)`: read `Plan base SHA` from the top of the evidence file, run
    `git checkout <plan base SHA> -- typsphinx/template_registry.py` to restore the pre-fix module
    while keeping the new test, run
    `uv run pytest tests/test_template_registry_path_quoting_gate.py -q -k type_check_message_stays_repr_quoted`
    capturing the output, then `git checkout HEAD -- typsphinx/template_registry.py` and run the
    identical command again, capturing that too. Paste both and state explicitly that the excluded
    site's behaviour is unchanged. Confirm `git status --porcelain typsphinx/` is clean before
    committing.
  </action>
  <verify>
    <automated>uv run pytest tests/test_template_registry_path_quoting_gate.py -q &amp;&amp; uv run pytest tests/test_template_registry.py -q &amp;&amp; uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; uv run pytest tests/test_repr_census_guard.py -q &amp;&amp; test -z "$(git status --porcelain typsphinx/)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_template_registry_path_quoting_gate.py -q` exits 0 with 0 skipped
    - `uv run pytest tests/test_template_registry.py -q` exits 0 with zero edits to that file
    - `uv run pytest -q` exits 0 (full suite green)
    - `grep -c 'quote_path(' typsphinx/template_registry.py` returns 2
    - `grep -c 'from typsphinx.pathfmt import quote_path' typsphinx/template_registry.py` returns 1
    - `grep -cE '\{template!r\}' typsphinx/template_registry.py` returns exactly 1 — the single surviving occurrence is the deliberately excluded type-check message
    - `grep -cE '\{key!r\}' typsphinx/template_registry.py` returns at least 12 — registry keys are untouched
    - `uv run pytest tests/test_repr_census_guard.py -q` exits 0
    - `uv run black --check .` exits 0 and `uv run mypy typsphinx/` exits 0
    - `git diff --name-only -- typsphinx/` lists only `typsphinx/template_registry.py`
    - `60-04-EVIDENCE.md` § "Exclusion control (two-tree)" holds two labelled transcripts and an explicit statement that the excluded site's behaviour is unchanged
    - `git status --porcelain typsphinx/` is empty (the temporary checkout was restored)
  </acceptance_criteria>
  <done>The two path-valued `template` interpolations route through `quote_path()`, the type-check message is measurably still on the repr conversion, the registry keys are untouched, and every gate plus the full suite, black, mypy and the census guard are green.</done>
  <reversibility rating="reversible">A one-line import plus two in-place substitutions.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Record MSG-05's RED-first ledger, the edge-unreachability proof, and the wave-3 handoff</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md</files>
  <read_first>
    - typsphinx/template_registry.py lines 400-445 (the `elif template:` guard whose falsiness makes the empty-value case structurally unreachable at both routed sites, and the two structurally independent checks)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md (the sections written by tasks 1 and 2)
    - tests/test_repr_census_guard.py lines 1-80 (what turns the AST guard RED, and why a quietly-appended allowlist entry is forbidden)
    - .planning/ROADMAP.md § "🚧 v0.9.1" constraints 1, 9 and 10
  </read_first>
  <action>
    Append a `## RED-first ledger` section naming MSG-05 and listing both RED shapes with the evidence
    section holding each recorded failure and the command that produced it, plus `## GREEN` for their
    pass and `## Exclusion control (two-tree)` for the unchanged excluded site.

    Append an `## Edge reachability` section recording, with the source lines it rests on: that the
    `elif template:` guard makes an empty template value falsy, so neither routed site is reachable
    with an empty value and `quote_path()` is never called with one from this module; that the
    `isinstance(template, (str, os.PathLike))` guard immediately above guarantees the value reaching
    the routed sites is `str` or `os.PathLike`, so a `bytes`, `list` or `int` value can never reach
    `quote_path()` from this module; and that the CONF-17 and existence checks are structurally
    independent (neither is an `elif` of the other), so a value that is both violating and nonexistent
    reports both failures in one accumulated raise with each independently quoted. Where a claim is
    behaviourally asserted by a method in the new gate module, name that method; where it is a
    structural read of the source, say so and paste the lines.

    Append a `## Zero test edits (measured)` section holding the verbatim output of
    `git diff --name-status <plan base SHA>..HEAD -- tests/`; every line must begin with `A`. Paste the
    green `uv run pytest tests/test_template_registry.py -q` tail and state that the two assertions
    pinning the excluded site's repr output for a list template and a bytes template are green
    UNMODIFIED — they are the phase's falsification gate for accidental over-reach. Paste the green
    `uv run pytest tests/test_repr_census_guard.py -q` tail and state that no entry was appended to
    that module's allowlist.

    Fill `## Known gate gaps` honestly. If every routed interpolation in this module has a behavioural
    gate, say so explicitly and name the gate for each. If any does not, name it and state what proves
    it instead. Do not leave an ungated site unnamed.

    Append a `## Wave-3 handoff` section giving the audit plan: the grep command scoped to this module,
    the count of `quote_path(` occurrences now in `typsphinx/template_registry.py`, the exact count of
    surviving `template` repr conversions (which must be 1, the deliberate exclusion) together with its
    current line number, and the list of identifier-valued names in this module that must still render
    through Python's repr conversion after the phase.

    Record `ruff check .` as deferred to CI with its reason (a freshly-provisioned worktree venv pulls
    a generic-linux `ruff` wheel whose ELF the loader rejects on this dev machine; CI holds lint
    authority).
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; grep -q 'RED-first ledger' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md &amp;&amp; grep -q 'Edge reachability' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md &amp;&amp; grep -q 'Zero test edits (measured)' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md &amp;&amp; grep -q 'Wave-3 handoff' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `60-04-EVIDENCE.md` § "RED-first ledger" names MSG-05 and lists BOTH RED shapes with the section holding each transcript
    - `60-04-EVIDENCE.md` § "Edge reachability" makes all three claims (empty unreachable, type guaranteed by the isinstance guard, the two checks structurally independent) and pastes the source lines each rests on
    - `60-04-EVIDENCE.md` § "Zero test edits (measured)" holds a `git diff --name-status` output in which every line begins with `A`, and states that the excluded site's two repr assertions are green unmodified
    - `60-04-EVIDENCE.md` § "Known gate gaps" either names each routed interpolation with its gate, or names any ungated one with what proves it instead
    - `60-04-EVIDENCE.md` § "Wave-3 handoff" gives the grep command, the `quote_path(` count, the surviving `template` repr-conversion count of 1 with its line number, and the stays-unrouted identifier list
    - the evidence file records `ruff check .` as deferred to CI with its reason
    - `git status --porcelain typsphinx/ tests/` is empty before the commit (this task edits neither)
  </acceptance_criteria>
  <done>MSG-05's RED-first ledger, its edge-reachability proof, its measured zero-test-edit result and its wave-3 handoff are recorded, with the deliberate exclusion documented as a measured pass criterion rather than an oversight.</done>
  <reversibility rating="reversible">Evidence-file appends only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user `conf.py` `typst_document_templates` definitions → `resolve_template_registry()` → `ExtensionError` | an arbitrarily-typed, arbitrarily-shaped user value crosses into path arithmetic and into a build-stopping error message |
| declared template path → `os.path.join(srcdir, template)` → `_violates_conf17()` / `os.path.isfile()` | a user-authored relative or absolute path crosses into filesystem-containment arithmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-10 | Denial of Service | `typsphinx/template_registry.py` type-check branch if accidentally routed (ASVS V5 Input Validation, L1) | high | mitigate | That branch is reached precisely when the value is a `list`, `bytes` or `int`; routing it would make `quote_path()` raise `TypeError` on the exact values the branch exists to report, replacing a clear configuration error with a crash. Mitigated by the explicit non-routing instruction, the surviving-repr-conversion count acceptance criterion, and `TestRegistryTypeCheckMessageStaysReprQuoted` driving both a list and a bytes template and asserting `ExtensionError` rather than `TypeError`. |
| T-60-11 | Information Disclosure | the CONF-17 and existence-check messages (ASVS V5, L1) | medium | mitigate | D-01's delimiter selection plus the removal of backslash doubling, and `quote_path()`'s `os.fspath()`-first ordering, which strips the `pathlib` class-name wrapper that today leaks a Python internal representation to a `conf.py` author. Gated by both RED shapes in `tests/test_template_registry_path_quoting_gate.py`. |
| T-60-12 | Tampering | registry keys accidentally routed (ASVS V5, L1) | low | mitigate | The explicit stays-unrouted instruction, the registry-key count acceptance criterion, the green AST census guard, and wave 3's SC#3 over-reach measurement one wave later. |
| T-60-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan; `60-RESEARCH.md` § "Package Legitimacy Audit" records the phase as stdlib-only with zero new packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `60-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. This plan
contributes: `tests/test_template_registry_path_quoting_gate.py` (`TestRegistryTemplatePathQuoting`,
`TestRegistryPathLikeTemplateNoClassWrapper`, `TestRegistryTypeCheckMessageStaysReprQuoted`); the two
routed `template` interpolations inside `typsphinx/template_registry.py`; and
`.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md`.
</artifacts_this_phase_produces>

<verification>
- `uv run pytest tests/test_template_registry_path_quoting_gate.py -q` — three classes green, 0 skipped
- `uv run pytest tests/test_template_registry.py -q` — green with zero edits to that file
- `uv run pytest -q` — full suite green
- `uv run pytest tests/test_repr_census_guard.py -q` — green
- `uv run black --check .` and `uv run mypy typsphinx/` — clean (`ruff` deferred to CI)
- `grep -c 'quote_path(' typsphinx/template_registry.py` returns 2 and exactly one `template` repr
  conversion survives — the deliberate exclusion
- `git diff --name-status <plan base SHA>..HEAD -- tests/` shows only added entries
- `60-04-EVIDENCE.md` holds the discovery grep, both RED shapes, the two-tree exclusion control, the
  GREEN transcripts, the RED-first ledger, the edge-reachability proof, the zero-test-edit measurement,
  the known gate gaps and the wave-3 handoff
</verification>

<success_criteria>
1. The `template` value in both the CONF-17 violation message and the existence-check message routes
   through `quote_path()`; the registry key on both lines does not.
2. Both of D-12's independent RED shapes were recorded FAILING against the unfixed tree first — the
   doubled backslash for a `str` template, and the leaked `pathlib` class-name wrapper for a `Path`
   template.
3. The type-check message is measurably still on Python's repr conversion, and the two existing
   assertions pinning its output for a list template and a bytes template are green with zero edits.
4. The empty-value and wrong-type edges are proven structurally unreachable at both routed sites, with
   the source lines that establish it recorded.
5. Zero existing test assertions modified, and the full suite, `black --check`, `mypy typsphinx/` and
   the AST census guard are green.
</success_criteria>

<output>
Create `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-SUMMARY.md` when done.
</output>
