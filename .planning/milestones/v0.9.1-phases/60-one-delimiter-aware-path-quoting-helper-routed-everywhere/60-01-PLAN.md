---
phase: 60-one-delimiter-aware-path-quoting-helper-routed-everywhere
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typsphinx/pathfmt.py
  - tests/test_pathfmt.py
  - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md
  - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/COVERAGE.md
autonomous: true
requirements: [MSG-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'MSG-02/D-01: quote_path(r"C:\Users\a") returns the 13-character string consisting of an apostrophe, C:\Users\a, and an apostrophe -- exactly one backslash per separator, where repr() of the same value doubles all three.'
    - 'MSG-02/D-01/57-REVIEW IN-01: quote_path("/home/O''Brien/x") wraps the value in DOUBLE quotes, so the embedded apostrophe cannot close the delimiter early; the returned string is byte-identical to repr() of the same value.'
    - 'MSG-02/D-01: a value containing BOTH quote characters is wrapped in single quotes with a single backslash inserted before each apostrophe and NO backslash inserted before any existing backslash -- quote_path("C:\\both''quotes\"here") differs from repr() of the same value in exactly the backslash-doubling, and in nothing else.'
    - 'MSG-02/D-01a: re.findall(r"\\\\+", quote_path(v)) is empty for every v whose own character sequence contains no two adjacent backslashes -- the both-quotes branch''s single inserted backslash never forms a run of two, so TestWindowsPathEscapingRegressionGuard._assert_no_doubled_separator stays green over quote_path output.'
    - 'MSG-02/D-04 (edge: empty): quote_path("") returns the two-character string of two apostrophes and does NOT raise -- deliberately unlike tests/_path_naming.py''s path_named_in(), which raises ValueError on an empty value.'
    - 'MSG-02/D-03 (edge: empty/null): quote_path(None) returns the bare four-character string None, with no delimiter of any kind, so a caller substituting it is byte-identical to the same site''s current repr conversion.'
    - 'MSG-02/D-03 (edge: encoding): quote_path accepts str and os.PathLike and normalizes a pathlib.Path through os.fspath() BEFORE inspecting any quote character, so no PosixPath(...) class wrapper can survive into a message; bytes, list and int each raise TypeError raised by quote_path itself, not by a downstream str operation.'
    - 'MSG-02 (edge: encoding): classification and quoting read Python str code points; no Unicode normalization, case folding, byte-length measurement or grapheme-cluster analysis is performed, so an NFC value and its NFD counterpart are each returned with their own code points intact and are not made equal.'
    - 'MSG-02 (edge: adjacency): quote_path never strips, reuses or collapses a quote character already present in the value -- a value that is itself a single apostrophe comes back wrapped in double quotes, a value that is itself a double quote comes back wrapped in single quotes, and a value that already looks quoted comes back wrapped again rather than unwrapped.'
    - 'MSG-02 (edge: ordering): quote_path is deterministic and order-preserving -- two calls with equal inputs return equal outputs, and the input''s character sequence appears in the output in its original order, the both-quotes branch''s inserted backslashes being the only characters added.'
    - statement: 'quote_path() is a pure function of one value with no module-level mutable state, no I/O and no global side effect, so an interrupted or concurrent call has no cross-call effect; this phase adds no concurrency test because there is no shared state to contend for.'
      verification: backstop
    - 'MSG-02/SC#1: typsphinx/pathfmt.py''s import block names no typsphinx module, proven both by an AST read of the source and by loading the file standalone in a FRESH interpreter (importlib.util.spec_from_file_location, never "import typsphinx.pathfmt") and observing that no typsphinx package module entered sys.modules.'
    - 'The phase base SHA is recorded at the top of 60-01-EVIDENCE.md before typsphinx/pathfmt.py exists, so waves 2 and 3 can reconstruct the pre-fix tree deterministically.'
    - 'tests/test_pathfmt.py is recorded FAILING against the unfixed tree (module absent) before typsphinx/pathfmt.py is created, and that transcript is pasted verbatim into 60-01-EVIDENCE.md.'
  artifacts:
    - typsphinx/pathfmt.py
    - tests/test_pathfmt.py
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/COVERAGE.md
    - 'typsphinx/pathfmt.py :: quote_path()'
  key_links:
    - 'typsphinx/pathfmt.py -> stdlib os ONLY: the zero-typsphinx-import property is what lets builder.py, writer.py and template_registry.py all import it without touching the existing builder->writer->template_registry module-scope import edges (ROADMAP constraint 6).'
    - 'quote_path() -> os.fspath() -> explicit isinstance(str) check: os.fspath(b"foo") returns b"foo" UNCHANGED, so the explicit check is the only thing that makes D-03''s bytes rejection real.'
    - 'quote_path() -> tests/_path_naming.py::path_named_in(): the two leaf modules are deliberate mirrors; path_named_in()''s repr()-form disjunct is what keeps the two MSG-01 sites green over D-01''s both-quotes branch with zero test edits.'
  prohibitions:
    - 'quote_path() is NOT added to typsphinx/__init__.py''s exports and gets no user-facing documentation (D-02, ROADMAP constraint 14) -- this is a bug-fix round with no new capability.'
    - 'quote_path() must not silently fall back to repr() for a rejected type; a bytes/list/int value raises TypeError so a future site cannot route a non-path value through the path helper with no gate noticing.'
    - 'quote_path() must not gain a caller-forced-delimiter keyword, a shell-escaping mode, or any Typst-string-literal escaping -- typsphinx/translator.py::escape_typst_string() owns that boundary and has a different correctness bar (CONTEXT Deferred Ideas).'
    - 'quote_path() must not be presented or used as a sanitizer: it must never be called to make a value safe for a shell, a Typst source literal, or a filesystem operation. It selects a delimiter for a human-readable diagnostic and nothing else.'
    - 'quote_path() must not drop, truncate, elide or transform the value''s own characters -- a reader must still be able to copy the path out of the message; the only characters it may add are its delimiters and the both-quotes branch''s apostrophe escapes.'
    - 'typsphinx/pathfmt.py must not import anything under typsphinx.* -- not even typsphinx.template_registry, which has the least restrictive existing import graph.'
    - 'No existing test assertion anywhere under tests/ is modified; this plan only adds new files.'
    - 'No new runtime dependency, no new typst_* config value, and no typing-import modernization (Dict/List to dict/list) anywhere in this diff (ROADMAP constraint 14).'
---

<objective>
Close MSG-02: a delimiter-aware path-quoting helper, `quote_path()`, exists in the new leaf module
`typsphinx/pathfmt.py` with zero `typsphinx`-internal imports, and its whole contract (D-01, D-01a,
D-03, D-04) is gated by a test module that was recorded RED against the unfixed tree first.

Purpose: this is the phase's tracer slice. Wave 1 is the only wave that can prove the phase's central
property end-to-end **without a build** — SC#1's two halves are properties of `quote_path()` itself
(never doubles a backslash; a literal apostrophe comes back unambiguously delimited) and are gated by
calling the function directly. Waves 2 and 3 expand horizontally from this proven slice; nothing in
them can be correct if this is wrong.

Output: `typsphinx/pathfmt.py`, `tests/test_pathfmt.py`, `60-01-EVIDENCE.md` carrying the phase base
SHA plus MSG-02's verbatim RED and green transcripts, and `COVERAGE.md`.

**Wave note (D-09):** this phase runs Wave 1 (this plan) → Wave 2 (plans 02, 03, 04 **in parallel**,
one per product module) → Wave 3 (plan 05, acceptance). The wave-3 audit sits one wave later than
what it audits on purpose — an auditing plan sharing a wave with its subject abstains from its own
success criterion.

**Tracer note (TRACER_MODE):** task 1 is the RED half of the slice and task 2 completes it. The RED
recording is a separate task from the implementation on purpose — ROADMAP constraint 1 requires the
failure to be recorded against the unfixed tree *before* the fix lands, which a single
write-test-and-implement task cannot produce.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATTERNS.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-RESEARCH.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-VALIDATION.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 11). Before running ANY command in this plan, provision the worktree's own venv:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Without this, pytest imports the UNCHANGED
main-tree package and the gate stays RED after a correct fix. Do not degrade to main-tree execution.
</worktree_provisioning>

<tasks>

<task type="tracer">
  <name>Task 1: Record the phase base SHA, write COVERAGE.md, and commit the full MSG-02 gate RED</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md, .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/COVERAGE.md, tests/test_pathfmt.py</files>
  <read_first>
    - tests/_path_naming.py (read in full — the leaf test-support module Phase 58 wrote as the deliberate mirror of this one: its module docstring shape, its `os.fspath()`-then-`isinstance(str)` two-step, and the ONE deliberate disagreement D-04 records)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md § D-01, D-01a, D-02, D-03, D-04, D-10, D-11
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATTERNS.md § "typsphinx/pathfmt.py (new leaf module, MSG-02)" and § "tests/test_pathfmt.py"
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-RESEARCH.md § "Pitfall 1" (os.fspath does not reject bytes) and § "Pitfall 3" (pathlib repr leak)
    - tests/test_templates_path_collision_gate.py lines 412-495 (`TestWindowsPathEscapingRegressionGuard`, its two Windows-shaped constants, and the `_assert_no_doubled_separator` regex D-01a reasons about — read it, do NOT edit it in this plan)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md (the evidence-file shape D-10 follows)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/COVERAGE.md (the declaration precedent to copy)
    - typsphinx/__init__.py lines 1-30 (READ THIS: it imports `typsphinx.builder` at module scope, which is why the leaf-import proof below must NOT be a plain `import typsphinx.pathfmt`)
  </read_first>
  <action>
    FIRST, before creating any product file, create
    `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md`.
    Per D-10 the per-plan evidence file uses exactly this name; the name `60-VERIFICATION.md` is
    RESERVED and overwritten wholesale by `gsd-verifier` and must never be produced by this phase.
    Give it a `# Phase 60 Plan 01 — MSG-02 Evidence` heading, a `## Phase base SHA` section whose body
    is the literal output of `git rev-parse HEAD` run in this worktree before any file is created
    (label the value `PHASE_BASE_SHA`), and empty `## MSG-02 RED`, `## MSG-02 GREEN`,
    `## D-01 byte-identity table`, and `## Leaf-import proof` sections.

    Also create `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/COVERAGE.md`
    in the exact form Phase 59 used: a first content line beginning `No external API integration:` and
    naming what this phase touches instead (four product files — one new leaf module plus three wired
    modules — four new test modules, one extended existing test class, and planning evidence; no
    external API, SDK, service or endpoint), the plan-time detector result recorded verbatim as
    `{"detected":false,"signals":[]}`, and a sentence recording that the file exists because the
    seal-time `api-coverage.verify-pre` gate re-runs the detector over the concatenated PLAN bodies
    and this project has a recorded history of that detector firing on ordinary engineering prose.

    THEN create `tests/test_pathfmt.py`. Its module docstring states that this is MSG-02's own gate,
    that it calls `quote_path()` DIRECTLY (no builder, no Sphinx app, no filesystem), and that every
    Windows shape in it is a hand-built string literal so the whole module runs on every CI lane
    including the non-Windows ones. Import the function as `from typsphinx.pathfmt import quote_path`.
    Write these classes and give every test a docstring naming the decision it gates:

    `TestQuotePathDelimiterSelection` — D-01's three branches, asserted as exact equality against
    hand-written expected strings (never against a re-derivation of the same rule):
    a value with no apostrophe wraps in apostrophes; a value containing an apostrophe and no double
    quote wraps in double quotes; a value containing both wraps in apostrophes with a single backslash
    inserted before each apostrophe and none inserted before any backslash. Use these five values:
    the raw Windows path `C:\Users\a`; `/home/O'Brien/x`; `/tmp/we"ird.png`; `/tmp/bo'th"quotes.png`;
    and the combined edge case `C:\both'quotes"here` (a backslash AND both quote characters).

    `TestQuotePathVersusRepr` — the byte-identity contract: for each of the five values above, assert
    that `quote_path(v)` equals `repr(v)` EXCEPT where `repr(v)` doubles a backslash, and record the
    difference explicitly. Concretely: assert `quote_path(v) == repr(v)` for the three values carrying
    no backslash, and assert `quote_path(v) != repr(v)` together with
    `quote_path(v) == repr(v).replace(chr(92) + chr(92), chr(92))` for the two backslash-bearing
    values. Build the two-backslash literal with `chr(92)` rather than a raw string so no acceptance
    grep in this phase can be tripped by the test source itself.

    `TestQuotePathNoDoubledSeparator` — D-01a: re-derive the guard predicate locally with
    `re.findall(r"\\\\+", ...)` (do NOT import `TestWindowsPathEscapingRegressionGuard`, and do not
    edit that class from this plan) and assert it returns an empty list for `quote_path()` applied to
    the Windows path, to `/home/O'Brien/x`, and to `C:\both'quotes"here`.

    `TestQuotePathTypeContract` — D-03: `quote_path(None)` equals the bare four-character string
    `None` with no delimiter; `quote_path(pathlib.Path("/some/path/_templates/nested"))` contains no
    occurrence of the substring `PosixPath` and equals the quoted form of the plain string; and
    `quote_path` raises `TypeError` for `b"base.typ"`, for `["a", "b"]`, and for `42`, with the raised
    message naming `quote_path` (use `pytest.raises(TypeError)` and assert on `str(excinfo.value)`).

    `TestQuotePathEdgeContract` — D-04 and the edge rows this phase resolved explicitly:
    `quote_path("")` equals two apostrophes and does not raise; a value that is exactly one apostrophe
    comes back wrapped in double quotes; a value that is exactly one double quote comes back wrapped in
    apostrophes; an already-apostrophe-wrapped value comes back wrapped again rather than unwrapped;
    two calls on equal inputs return equal outputs; and an NFC value (`ma\u00f1ana/guide`) and its NFD
    counterpart (`man\u0303ana/guide`) are each returned with their own code points intact and are NOT
    equal to each other — with a docstring recording that no Unicode normalization, case folding,
    byte-length measurement or grapheme-cluster analysis is performed.

    `TestPathfmtLeafModule` — SC#1's two halves. `test_import_block_names_no_typsphinx_module` parses
    `typsphinx/pathfmt.py` with `ast.parse` (locating it from `pathlib.Path(__file__).resolve().parent.parent`,
    never from the process working directory) and asserts that no `ast.Import` alias name and no
    `ast.ImportFrom` module name equals `typsphinx` or starts with `typsphinx.`.
    `test_module_loads_standalone_in_a_fresh_interpreter` runs a FRESH interpreter via
    `subprocess.run([sys.executable, "-c", script, str(module_path)])` where the script uses
    `importlib.util.spec_from_file_location` plus `spec.loader.exec_module` to load the file BY PATH,
    then asserts that no key in `sys.modules` equals `typsphinx` or starts with `typsphinx.`, then
    calls `quote_path` on a Windows-shaped value and prints a fixed success token; the test asserts
    the subprocess returncode is 0 and the token is in stdout.
    **Do NOT write the proof as `import typsphinx.pathfmt` plus a `sys.modules` scan** — measured this
    planning session: `typsphinx/__init__.py` imports `typsphinx.builder` at module scope, so that form
    fails even for a perfect leaf module and would prove the opposite of SC#1. Loading the file by path
    is what actually bypasses the package `__init__`.

    Run `uv run pytest tests/test_pathfmt.py -x` and capture the FULL verbatim output — at this point
    it is a collection error (`ModuleNotFoundError: No module named 'typsphinx.pathfmt'`), which is
    MSG-02's legitimate RED against the unfixed tree. Paste that transcript, the command that produced
    it and the `PHASE_BASE_SHA` it was taken at into the evidence file's `## MSG-02 RED` section.
    Commit the RED gate, the evidence file and `COVERAGE.md` together; the implementation is task 2.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; ! uv run pytest tests/test_pathfmt.py -x &amp;&amp; grep -q 'PHASE_BASE_SHA' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md &amp;&amp; grep -q 'MSG-02 RED' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md &amp;&amp; test ! -e typsphinx/pathfmt.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_pathfmt.py` exists and `grep -c 'class Test' tests/test_pathfmt.py` returns 6
    - `uv run pytest tests/test_pathfmt.py -x` exits NON-ZERO at this point in the plan
    - `typsphinx/pathfmt.py` does not exist at this point (`test ! -e typsphinx/pathfmt.py` succeeds)
    - `grep -c 'import typsphinx.pathfmt' tests/test_pathfmt.py` returns 0 — the leaf proof loads the file by path, never through the package
    - `grep -c 'spec_from_file_location' tests/test_pathfmt.py` is at least 1
    - `60-01-EVIDENCE.md` exists, its `## Phase base SHA` section holds the 40-character output of `git rev-parse HEAD` taken before any file was created, and its `## MSG-02 RED` section holds the verbatim failing transcript
    - `COVERAGE.md` exists in the phase directory and its first content line begins `No external API integration:`
    - no file named `60-VERIFICATION.md` exists anywhere under the phase directory
    - `git status --porcelain typsphinx/` is empty (no product file was touched by this task)
  </acceptance_criteria>
  <done>The full MSG-02 gate is committed RED with its verbatim transcript and the phase base SHA recorded, `COVERAGE.md` is in place, and no product file has been created.</done>
  <reversibility rating="reversible">A new test module plus two planning files; all deletable with no downstream effect.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typsphinx/pathfmt.py::quote_path() and turn the gate green</name>
  <files>typsphinx/pathfmt.py</files>
  <read_first>
    - tests/_path_naming.py lines 1-62 (the docstring style to mirror and the exact `os.fspath()` + `isinstance(value_str, str)` two-step idiom to copy, adapting the TypeError message to name this function)
    - tests/test_pathfmt.py (the gate committed by task 1 — the contract this task must satisfy, read it rather than re-deriving it)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATTERNS.md § "Reference delimiter algorithm"
    - typsphinx/builder.py lines 496-530 (`_conf17_violation_message`'s docstring — the "this function is the ONE place this is built, so a unit test can drive the real code" discipline `quote_path()` continues one level down)
    - pyproject.toml `[tool.mypy]` and `[tool.ruff]` sections (the type-checking and lint settings this new module must satisfy)
  </read_first>
  <behavior>
    - `quote_path(r"C:\Users\a")` returns apostrophe + `C:\Users\a` + apostrophe (three single backslashes, not six)
    - `quote_path("/home/O'Brien/x")` returns the value wrapped in double quotes
    - `quote_path('/tmp/we"ird.png')` returns the value wrapped in apostrophes
    - `quote_path('/tmp/bo\'th"quotes.png')` wraps in apostrophes and escapes only the apostrophe
    - `quote_path("")` returns two apostrophes; it does NOT raise
    - `quote_path(None)` returns the bare string `None`
    - `quote_path(pathlib.Path("/x/y"))` returns the quoted plain string, never a class-name wrapper
    - `quote_path(b"base.typ")`, `quote_path(["a", "b"])` and `quote_path(42)` each raise `TypeError`
  </behavior>
  <action>
    Create `typsphinx/pathfmt.py`. Its module docstring states, in the declarative decision-ID-citing
    style `tests/_path_naming.py` uses: that this is MSG-02's delimiter-aware path-quoting helper; that
    it is a LEAF module carrying ZERO `typsphinx`-internal imports because `builder.py` imports
    `writer.py` at module scope and `template_registry.py` avoids a cycle with `builder.py` only via a
    lazy function-scoped import, so any other placement creates an unconditional two-file import cycle
    the moment the other two import it back (ROADMAP constraint 6 — placement is forced, not
    stylistic); that it is the product-side counterpart of `tests/_path_naming.py`; and that D-04 is
    the ONE deliberate disagreement with that mirror (an empty value is quoted, not refused).

    Import `os` and nothing else. Do not import anything under `typsphinx`. Do not add a `typing`
    import unless mypy requires one, and if it does, use the modern `X | Y` union spelling — the
    `Dict`/`List` forms are frozen by ROADMAP constraint 14 and must not be introduced here.

    Define exactly one public function with the signature
    `def quote_path(value: str | os.PathLike[str] | None) -> str:` and a docstring stating D-01's
    three-branch rule, D-03's type contract and D-04's empty-value rule.

    Body, in this order: (1) return the bare four-character string `None` when `value is None` (D-03 —
    `writer.py`'s package-alone branch really does hand this function a `None`, so raising there would
    turn a supported build shape into a crash inside a debug log); (2) `value_str = os.fspath(value)`;
    (3) an EXPLICIT `isinstance(value_str, str)` check that raises `TypeError` naming `quote_path` and
    `type(value_str).__name__` when it fails — this step is load-bearing and must not be skipped:
    measured this planning session, `os.fspath(b"foo")` returns `b"foo"` UNCHANGED rather than raising,
    so `os.fspath()` alone does not enforce D-03's bytes rejection and the delimiter logic would crash
    later with a confusing native message instead of failing cleanly at the boundary; (4) D-01's three
    branches over `value_str` — no apostrophe present, wrap in apostrophes; apostrophe present and no
    double quote, wrap in double quotes; both present, wrap in apostrophes after replacing each
    apostrophe with a backslash followed by an apostrophe and touching no other character. Never
    replace, double or escape a backslash anywhere.

    The `os.fspath()` call must run BEFORE any quote-character inspection so a `pathlib.Path` is
    already its plain string form by the time a delimiter is chosen — otherwise the class-name wrapper
    leaks into the message and `template_registry.py`'s existence check keeps the exact defect plan 04
    is fixing.

    Do not add a caller-forced-delimiter keyword, a shell-escaping mode, or any second public function
    (CONTEXT Deferred Ideas). Do not touch `typsphinx/__init__.py`.

    Run the gate, then the full suite, `black --check .` and `mypy typsphinx/`. Paste the verbatim
    green transcript of `uv run pytest tests/test_pathfmt.py -q` into the evidence file's
    `## MSG-02 GREEN` section, and fill `## D-01 byte-identity table` with a five-row table giving,
    for each of the five values in `TestQuotePathVersusRepr`, the `quote_path()` output and the
    `repr()` output side by side, taken from a real `uv run python` invocation rather than copied from
    the research document.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pathfmt.py -q &amp;&amp; uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; grep -q 'MSG-02 GREEN' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pathfmt.py -q` exits 0 with 0 skipped
    - `uv run pytest -q` exits 0 (full suite green — nothing else moved)
    - `grep -c 'typsphinx' typsphinx/pathfmt.py` returns 0 for import lines: specifically `grep -nE '^(import|from) ' typsphinx/pathfmt.py` prints only lines naming `os`
    - `grep -c 'def ' typsphinx/pathfmt.py` returns 1
    - `uv run python -c "from typsphinx.pathfmt import quote_path as q; print(q(r'C:\Users\a'), q(chr(47)+'home'+chr(47)+chr(79)+chr(39)+'Brien'), q(''), q(None))"` prints the apostrophe-wrapped Windows path with exactly three backslashes, the double-quote-wrapped apostrophe value, two apostrophes, and the bare word None
    - `uv run python -c "from typsphinx.pathfmt import quote_path as q; q(b'x')"` exits non-zero with a `TypeError` whose message contains `quote_path`
    - `uv run black --check .` exits 0 and `uv run mypy typsphinx/` exits 0
    - `git diff --name-only -- tests/` lists no file that existed before this phase
    - `60-01-EVIDENCE.md` § "D-01 byte-identity table" has five rows, each with both a `quote_path()` column and a `repr()` column
  </acceptance_criteria>
  <done>`quote_path()` satisfies D-01, D-01a, D-03 and D-04; the MSG-02 gate is green; the full suite, black and mypy are clean; and the RED-then-green pair plus the byte-identity table are recorded.</done>
  <reversibility rating="reversible">One new leaf module with a single function; deleting the file restores the tree exactly.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Prove the leaf-import property in a fresh interpreter and close MSG-02's evidence</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md</files>
  <read_first>
    - typsphinx/__init__.py lines 1-30 (the module-scope `typsphinx.builder` import that makes a plain `import typsphinx.pathfmt` useless as a leaf proof)
    - tests/test_pathfmt.py (the `TestPathfmtLeafModule` class committed in task 1 — this task records its result, it does not rewrite it)
    - .planning/ROADMAP.md § "Phase 60" success criterion 1 (the exact wording SC#1 uses: "Its import block names no typsphinx module (read from the source, and proven by importing it standalone)")
    - tests/test_repr_census_guard.py lines 1-60 (the AST census guard whose green state is this phase's standing negative check)
  </read_first>
  <action>
    Record MSG-02's SC#1 proof in the evidence file's `## Leaf-import proof` section, in both of the
    forms SC#1's own wording names.

    First the SOURCE read: paste the verbatim output of `grep -nE '^(import|from) ' typsphinx/pathfmt.py`
    and state that it names only `os`.

    Second the STANDALONE load: run a fresh interpreter that loads the module BY FILE PATH via
    `importlib.util.spec_from_file_location` and `spec.loader.exec_module`, then prints the sorted list
    of `sys.modules` keys beginning with `typsphinx` and the result of one `quote_path()` call. Paste
    the verbatim command and its output; the printed list must be empty. Add one sentence recording WHY
    the obvious form is wrong — `typsphinx/__init__.py` imports `typsphinx.builder` at module scope, so
    a plain `import typsphinx.pathfmt` would pull the whole package in and make the `sys.modules` check
    fail for a module that is in fact a perfect leaf. This is a correction to the one-liner proposed in
    `60-RESEARCH.md` § "Phase Requirements → Test Map"; say so explicitly so a later reader does not
    reintroduce it.

    Then close MSG-02's record: append a `## RED-first ledger` section naming MSG-02 and pointing at
    the `## MSG-02 RED` and `## MSG-02 GREEN` sections; paste the verbatim tails of `uv run pytest -q`,
    `uv run black --check .`, `uv run mypy typsphinx/` and
    `uv run pytest tests/test_repr_census_guard.py -q` (the census guard must be green — this plan adds
    no test-side assertion the census tracks, so a RED there would mean the census was perturbed); and
    record `ruff check .` as deferred to CI with the reason (a freshly-provisioned worktree venv pulls a
    generic-linux `ruff` wheel whose ELF the loader rejects on this dev machine; CI holds lint
    authority).

    Finally append a `## Handoff to wave 2` section listing the exact import line the three wiring
    plans each add — `from typsphinx.pathfmt import quote_path` — and the one-sentence type contract
    they must respect: `quote_path()` accepts `str`, `os.PathLike` and `None` and raises `TypeError`
    for anything else, so a call site whose value can be an arbitrary user-config type must narrow it
    before calling.
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run pytest tests/test_repr_census_guard.py -q &amp;&amp; grep -q 'Leaf-import proof' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md &amp;&amp; grep -q 'RED-first ledger' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md &amp;&amp; grep -q 'Handoff to wave 2' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `60-01-EVIDENCE.md` § "Leaf-import proof" contains both the verbatim `grep -nE '^(import|from) '` output and the verbatim fresh-interpreter transcript, and the printed `typsphinx`-prefixed `sys.modules` list in that transcript is empty
    - that section states in one sentence why a plain `import typsphinx.pathfmt` is not a valid leaf proof for this package
    - `60-01-EVIDENCE.md` contains a `## RED-first ledger` section naming MSG-02 and the two sections holding its RED and GREEN
    - the evidence file records `ruff check .` as deferred to CI with its reason
    - `uv run pytest tests/test_repr_census_guard.py -q` exits 0
    - `60-01-EVIDENCE.md` § "Handoff to wave 2" contains the literal import line the three wiring plans add
    - `git status --porcelain typsphinx/` is empty (this task edits no product file)
  </acceptance_criteria>
  <done>SC#1 is proven in both the source-read and standalone-load forms, the research document's broken one-liner is corrected in the record, and MSG-02's RED-first ledger and wave-2 handoff are written.</done>
  <reversibility rating="reversible">Evidence-file appends only.</reversibility>
</task>

</tasks>

<assumption_delta_decision>
Detector fired: `{"kind":"pluralization","term":"another"}` on the phase-scope sentence "another
non-path type and quoting it as a path would be actively wrong" (the `template_registry.py:410`
exclusion rationale).

- **Primary noun:** unchanged — a path value rendered into a human-readable diagnostic. This phase
  introduces no second variant of any modeled noun.
- **Decision:** `no-change`. The signal fired on a prose enumeration of Python types (`list`,
  `bytes`, "another non-path type"), not on an identity model. This phase *reduces* variance rather
  than adding a variant: three modules that each independently decided how to quote a path now funnel
  through one function with one contract. `template_registry.py:410` is not a second quoting regime
  added alongside the first — it is the boundary that marks where the single regime does not apply,
  and SC#3 makes leaving it alone a measured pass criterion.
- **Accepted debt:** none.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user `conf.py` values (`typst_documents` targets, `typst_document_templates` template paths) → message construction → `quote_path()` | a user-authored, arbitrarily-typed and arbitrarily-shaped value crosses into a formatting helper with an explicit type contract |
| local filesystem path strings (resolved template paths, image URIs, output relpaths) → `quote_path()` → Sphinx's logging / `ExtensionError` surface | filesystem-derived text crosses into the build's diagnostic output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-01 | Tampering | `typsphinx/pathfmt.py::quote_path` type boundary (ASVS V5 Input Validation, L1) | medium | mitigate | D-03's explicit `isinstance(value_str, str)` check AFTER `os.fspath()` — measured: `os.fspath(b"foo")` returns `b"foo"` unchanged, so relying on `os.fspath()` alone leaves a `bytes` value to crash confusingly inside the delimiter logic. Gated by `TestQuotePathTypeContract`. |
| T-60-02 | Information Disclosure | message text a path value closes its own delimiter in (ASVS V5, L1) | medium | mitigate | D-01's delimiter selection: a value containing an apostrophe is wrapped in double quotes, and a value containing both is escaped, so the rendered message can never be read as ending its quote early and mis-attributing the remainder of the path to the surrounding sentence. Gated by `TestQuotePathDelimiterSelection`. |
| T-60-03 | Spoofing | `quote_path()` mistaken for a sanitizer at a future call site (ASVS V5, L1) | low | accept | Accepted with a control: the module docstring states it selects a delimiter for a human-readable diagnostic and is not a shell, Typst-literal or filesystem sanitizer, and `must_haves.prohibitions` records the must-NOT. Log/terminal-escape sanitization of an attacker-controlled filename is canon — covered by `/gsd-secure-phase`; not minted here. The values quoted originate from the local filesystem and `conf.py`, both already trusted in this extension's threat model (a malicious `conf.py` author already has arbitrary code execution via Sphinx's own extension loading). |
| T-60-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase. `60-RESEARCH.md` § "Package Legitimacy Audit" records the phase as stdlib-only with zero new packages, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Symbols and files newly created by Phase 60 (all five plans), listed here so the
plan-review-convergence source-grounding pass excludes them from drift verification.

**New product module and symbol (plan 01):**
- `typsphinx/pathfmt.py` — module
- `typsphinx/pathfmt.py :: quote_path()` — the phase's one new public symbol (deliberately NOT
  re-exported from `typsphinx/__init__.py`, D-02)

**New local binding in `typsphinx/builder.py` (plan 02):**
- `target_text` — the single local inside `_validate_output_path_collisions()` that both routed
  `target` sites read (see plan 02's `<flagged_assumptions>` for why this local exists)

**New test modules and their classes:**
- `tests/test_pathfmt.py` (plan 01) — `TestQuotePathDelimiterSelection`, `TestQuotePathVersusRepr`,
  `TestQuotePathNoDoubledSeparator`, `TestQuotePathTypeContract`, `TestQuotePathEdgeContract`,
  `TestPathfmtLeafModule`
- `tests/test_builder_path_quoting_gate.py` (plan 02) — `TestResolveTargetStemPathQuoting`,
  `TestTrackImageRehomeWarningPathQuoting`, `TestOutputPathCollisionMessagePathQuoting`,
  `TestBundleCopyMessagePathQuoting`, `TestBuilderIdentifierQuotingControl`
- `tests/test_writer_path_quoting_gate.py` (plan 03) — `TestWrapperDebugLogPathQuoting`,
  `TestWrapperDebugLogTemplateFileNone`
- `tests/test_template_registry_path_quoting_gate.py` (plan 04) —
  `TestRegistryTemplatePathQuoting`, `TestRegistryPathLikeTemplateNoClassWrapper`,
  `TestRegistryTypeCheckMessageStaysReprQuoted`

**New methods added to an EXISTING class** (`tests/test_templates_path_collision_gate.py`,
`TestWindowsPathEscapingRegressionGuard`, plan 02 ONLY, by addition — no existing method and no
`_assert_no_doubled_separator` is modified):
- `test_conf17_violation_message_disambiguates_embedded_single_quote`
- `test_templates_path_collision_message_disambiguates_embedded_single_quote`
- `test_bundle_destination_collision_message_disambiguates_embedded_single_quote`

**New planning artifacts:**
`.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md`,
`60-02-EVIDENCE.md`, `60-03-EVIDENCE.md`, `60-04-EVIDENCE.md`, `60-05-EVIDENCE.md`,
`60-PATH-QUOTING-EVIDENCE.md`, and `COVERAGE.md`. **No file named `60-VERIFICATION.md` is produced
or referenced as an output by any plan in this phase** (D-10 — that name is reserved and overwritten
wholesale by `gsd-verifier`).
</artifacts_this_phase_produces>

<verification>
- `uv run pytest tests/test_pathfmt.py -q` — all six classes green, 0 skipped
- `uv run pytest -q` — full suite green
- `uv run pytest tests/test_repr_census_guard.py -q` — green (the test-side `!r` census is unperturbed)
- `uv run black --check .` and `uv run mypy typsphinx/` — clean (`ruff` deferred to CI, per
  `60-VALIDATION.md` and the recorded NixOS ELF constraint)
- `grep -nE '^(import|from) ' typsphinx/pathfmt.py` names only `os`
- `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/` shows only added entries
- `60-01-EVIDENCE.md` holds the phase base SHA, MSG-02's verbatim RED, its verbatim GREEN, the five-row
  D-01 byte-identity table, both halves of the leaf-import proof, the RED-first ledger, and the wave-2
  handoff
</verification>

<success_criteria>
1. `typsphinx/pathfmt.py` exists, imports only `os`, and defines exactly one public function
   `quote_path()`.
2. `quote_path()` satisfies D-01's three delimiter branches, D-01a's no-doubled-run property, D-03's
   `None`/`os.PathLike`/`TypeError` contract, and D-04's empty-value rule — each proven by a direct
   call, never by a re-derivation of the rule.
3. The gate was recorded FAILING against the unfixed tree before `typsphinx/pathfmt.py` was created,
   and that transcript is in `60-01-EVIDENCE.md`.
4. SC#1's leaf property is proven in BOTH forms SC#1 names: an AST read of the import block, and a
   standalone load in a fresh interpreter that pulls in no `typsphinx` package module.
5. The full suite, `black --check` and `mypy typsphinx/` are green with zero existing test assertions
   modified.
</success_criteria>

<output>
Create `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-SUMMARY.md` when done.
</output>
