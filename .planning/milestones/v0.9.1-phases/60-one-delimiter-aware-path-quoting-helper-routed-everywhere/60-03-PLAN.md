---
phase: 60-one-delimiter-aware-path-quoting-helper-routed-everywhere
plan: 03
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - typsphinx/writer.py
  - tests/test_writer_path_quoting_gate.py
  - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md
autonomous: true
requirements: [MSG-04]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'MSG-04/D-06: the three path-valued interpolations in typsphinx/writer.py''s wrapper-render debug log -- wrapper_relative_dir, include_path and template_file -- route through quote_path(); the docname on the same line keeps its current repr conversion (D-07).'
    - 'MSG-04/D-12: the debug record, captured via caplog at DEBUG level while render_wrapper() is driven with a Windows-shaped wrapper_relative_dir and content_relative_path, contains no run of two or more consecutive backslashes -- recorded RED (eleven doubled runs) against the unfixed tree first.'
    - 'MSG-04/D-03/D-07 (edge: empty/null): on the package-alone build path -- typst_package configured with no custom template -- writer.py binds template_file to None, and quote_path(None) renders the bare four-character string None, keeping that debug line byte-identical to its current output. Proven by a two-tree measurement, not asserted.'
    - 'MSG-04 (edge: empty): a wrapper written to the outdir root passes an empty wrapper_relative_dir; it renders as two apostrophes, byte-identical to its current rendering, and never raises.'
    - 'MSG-04 (edge: encoding): every value routed at this site is a str or None -- wrapper_relative_dir and include_path are always str, and template_file is str-or-None -- so no bytes and no PathLike ever reaches quote_path() from this module, and no Unicode normalization is applied to any of the three.'
    - 'D-07 is confirmed rather than assumed: writer.py''s OTHER repr conversions (the entry-element fallback warning''s entry[0], value and default) are a docname and a title/author string resolved from config.project / config.author, so MSG-04''s restriction to the wrapper-render debug log is correct as written and those sites are untouched.'
    - 'Zero test edits: this plan adds one new test module and modifies no pre-existing test file at all.'
  artifacts:
    - tests/test_writer_path_quoting_gate.py
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md
    - 'typsphinx/writer.py :: three routed interpolations in render_wrapper()''s debug log'
  key_links:
    - 'typsphinx/writer.py -> typsphinx.pathfmt.quote_path: one added module-scope import line; pathfmt.py imports nothing from typsphinx, so the existing writer -> template_registry / template_engine / translator module-scope edges are untouched.'
    - 'render_wrapper()''s package-alone branch -> template_file = None -> quote_path(None): the one live build path that hands the helper a None; D-03''s bare-None rendering is what keeps this line byte-identical there.'
    - 'caplog.at_level("DEBUG") -> logger.debug in render_wrapper(): the only observable for this site -- no existing test drives this log at all, so the whole gate is new coverage.'
  prohibitions:
    - 'The None case must NOT be handled with a conditional inside the f-string -- D-03 already rejected that (it duplicates the invariant at the one site that has it and invites drift). quote_path(None) itself returns the bare string.'
    - 'The docname on the same debug line must not be routed -- it is identifier-valued (D-07) and routing it would misclassify a namespace name as a filesystem location.'
    - 'writer.py''s entry-element fallback warning (entry[0], value, default) must not be touched -- measured as docname / title / author values, not paths.'
    - 'This plan must not assert on any string emitted by typsphinx/builder.py or typsphinx/template_registry.py -- those belong to the sibling wave-2 plans, and a cross-module assertion would collide at merge even with disjoint files_modified.'
    - 'This plan must not touch tests/test_templates_path_collision_gate.py -- extending TestWindowsPathEscapingRegressionGuard is a one-plan privilege held by the builder.py wiring plan (D-11).'
    - 'The debug log''s message text must not be reworded, reordered or "improved" while routing it -- only the three interpolations change.'
    - 'No existing test assertion anywhere under tests/ is modified.'
    - 'No new runtime dependency, no new typst_* config value, and no typing-import modernization anywhere in this diff (ROADMAP constraint 14).'
---

<objective>
Close MSG-04: `typsphinx/writer.py`'s wrapper-render debug log routes its three path-valued
interpolations through `quote_path()`, gated by a new `caplog`-at-DEBUG test module that was recorded
RED against the unfixed tree first, plus a two-tree byte-identity proof for the package-alone
`None` case.

Purpose: this site is the phase's only logging-level target and its only live `None` path. No
existing test drives this debug log at all, so the whole gate is genuinely new coverage and nothing
here can force a test edit.

Output: the wired `typsphinx/writer.py`, `tests/test_writer_path_quoting_gate.py`, and
`60-03-EVIDENCE.md`.

**Wave note (D-09/D-11):** this is one of wave 2's THREE PARALLEL plans (`builder.py` / `writer.py` /
`template_registry.py`). It carries its own new test module and asserts only on `writer.py`'s own
output. It must not touch `tests/test_templates_path_collision_gate.py` — that is the `builder.py`
plan's exclusive privilege.

**Line numbers in this plan are anchors, not coordinates.** Re-measure with the discovery grep in
task 1; the description (function name + variable name) is the stable identity.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATTERNS.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-RESEARCH.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-VALIDATION.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 11). Before running ANY command in this plan, provision the worktree's own venv:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Without this, pytest imports the UNCHANGED
main-tree package and the gate stays RED after a correct fix. Do not degrade to main-tree execution.
</worktree_provisioning>

<tasks>

<task type="tracer">
  <name>Task 1: Run the discovery grep, then commit MSG-04's caplog gate RED and its None pin green</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md, tests/test_writer_path_quoting_gate.py</files>
  <read_first>
    - typsphinx/writer.py lines 277-345 (`render_wrapper`'s full signature and docstring, and the `compute_content_include_path` call that derives `include_path` from `wrapper_relative_dir`)
    - typsphinx/writer.py lines 345-420 (how `raw_template_path` and `typst_package` are derived from the resolved registry entry)
    - typsphinx/writer.py lines 495-516 (the package-alone `template_file = None` branch and the `logger.debug` call this plan routes)
    - typsphinx/writer.py lines 115-160 (`_entry_element_value` — the OTHER repr conversions in this module, which D-07 measured as docname / title / author values and which stay untouched)
    - typsphinx/writer.py lines 31-70 (`compute_content_include_path` — a `posixpath.relpath` of the content path against the wrapper dir, so a Windows-shaped `wrapper_relative_dir` yields a Windows-shaped `include_path`)
    - tests/test_template_registry.py lines 225-290 (the direct `writer.render_wrapper(...)` call precedent: `temp_sphinx_app` → hand-built `nodes.document` with `Reporter`/`states.Struct` → `TypstWriter(app.builder)` → a constructed `TemplateRegistryEntry` passed as `template_entry`)
    - tests/test_track_image_key_construction.py lines 60-100 (the `caplog.at_level(...)` + `[r for r in caplog.records if r.levelname == ...]` house idiom)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md § D-03, D-06, D-07, D-11, D-12 and § "Specific Ideas" item 3
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md § "Handoff to wave 2"
  </read_first>
  <action>
    FIRST create
    `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md`
    (per D-10 this exact name; `60-VERIFICATION.md` is reserved and must never be produced). Give it a
    `# Phase 60 Plan 03 — MSG-04 Evidence` heading, a `## Plan base SHA` section holding
    `git rev-parse HEAD` taken before any edit, and empty `## Discovery grep`,
    `## RED — wrapper-render debug log`, `## None pin (two-tree)`, `## GREEN`, `## D-07 confirmation`
    and `## Known gate gaps` sections.

    THEN run `grep -n "\!r" typsphinx/writer.py` and paste the verbatim output into
    `## Discovery grep` with the line numbers as measured RIGHT NOW. Classify each hit in one line
    under D-05's role rule: the entry-element fallback warning's three values are identifier / title /
    author (stay unrouted, D-07); the wrapper-render debug log's `docname` is identifier (stays
    unrouted); its `wrapper_relative_dir`, `include_path` and `template_file` are path-valued (route).
    State explicitly that this module has no `%r` occurrence and no hardcoded-delimiter interpolation.

    THEN create `tests/test_writer_path_quoting_gate.py`. Its module docstring states that this is
    MSG-04's gate, that the site is a `logger.debug` call so the only observable is `caplog` at DEBUG
    level (D-12), that no existing test drives this log at all so the whole module is new coverage,
    and that it asserts ONLY on strings `typsphinx/writer.py` itself emits — never on a string emitted
    by `builder.py` or `template_registry.py`, which belong to the sibling wave-2 plans. Define a
    module-level `_assert_no_doubled_separator(message)` helper re-deriving the guard predicate with
    `re.findall(r"\\\\+", message)` locally (do not import from
    `tests/test_templates_path_collision_gate.py`, and do not touch that file). Define a module-level
    document builder following `tests/test_template_registry.py`'s precedent: a `nodes.document` built
    with `Reporter("", 2, 4)` and `states.Struct()` settings, with one empty `nodes.section()` child.

    Write two classes:

    `TestWrapperDebugLogPathQuoting` — the RED gate. Method
    `test_wrapper_debug_log_has_no_doubled_separator_for_windows_shaped_paths`: build a `TypstWriter`
    from `temp_sphinx_app`'s builder, call
    `writer.render_wrapper(("index", "manual.typ", "T", "A"), doctree, wrapper_relative_dir, content_relative_path)`
    with `wrapper_relative_dir = "C:\\Users\\runner\\out\\sub"` and
    `content_relative_path = "C:\\Users\\runner\\out\\sub\\index.typ"` (hand-built raw literals, never
    `os.name`-gated), inside `caplog.at_level("DEBUG")`. Select the DEBUG records, find the one whose
    message starts with the wrapper-render sentence, assert it passes `_assert_no_doubled_separator`,
    and assert the raw `wrapper_relative_dir` value is still present in the message with its single
    separators. Pass no `template_entry` so the built-in registry entry is derived exactly as every
    existing direct caller does. Pre-fix this record carries eleven doubled runs.

    `TestWrapperDebugLogTemplateFileNone` — the byte-identity pin for D-03's live `None` path. Method
    `test_wrapper_debug_log_template_file_none_renders_bare_none_on_the_package_alone_path` (the name
    must contain the token `template_file_none` so the selector `-k template_file_none` picks it up —
    pytest keyword matching is case-sensitive and will not match the CamelCase class name): construct a
    `TemplateRegistryEntry` with `key="typst"`, `template=None`, a `package` value such as
    `"@preview/charged-ieee:0.1.3"` and a `template_function` name, pass it as `template_entry`, and
    drive `render_wrapper` with an EMPTY `wrapper_relative_dir` and `content_relative_path="index.typ"`
    inside `caplog.at_level("DEBUG")`. Assert the debug message contains the substring naming
    `template_file` followed by the bare four-character word `None` with no delimiter of any kind
    around it, and that the empty `wrapper_relative_dir` renders as two apostrophes. Give the method a
    docstring recording the measured reason this pin exists: `writer.py` binds `template_file` to
    `None` whenever a package is configured with no custom template, so a helper that raised on `None`
    would turn a supported build shape into a crash inside a debug log — the worst possible place to
    discover it. This method must be GREEN both before and after the fix.

    Run `uv run pytest tests/test_writer_path_quoting_gate.py -q` and capture the FULL verbatim output.
    Paste the failing method's transcript into `## RED — wrapper-render debug log` with its command and
    the plan base SHA; paste the already-passing `None` pin result into `## None pin (two-tree)`
    labelled as the pre-fix half. Commit the gate and the evidence file together; the wiring is task 2.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; ! uv run pytest tests/test_writer_path_quoting_gate.py -q &amp;&amp; uv run pytest tests/test_writer_path_quoting_gate.py -q -k template_file_none &amp;&amp; grep -q 'Discovery grep' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md &amp;&amp; test -z "$(git status --porcelain typsphinx/)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_writer_path_quoting_gate.py` exists and `grep -c 'class Test' tests/test_writer_path_quoting_gate.py` returns 2
    - `uv run pytest tests/test_writer_path_quoting_gate.py -q` exits NON-ZERO with exactly 1 failure at this point in the plan
    - `uv run pytest tests/test_writer_path_quoting_gate.py -q -k template_file_none` exits 0 — the `None` pin is green BEFORE the fix
    - `grep -c 'caplog.at_level' tests/test_writer_path_quoting_gate.py` returns 2
    - `grep -c 'test_templates_path_collision_gate' tests/test_writer_path_quoting_gate.py` returns 0 and `grep -c 'template_registry_path_quoting' tests/test_writer_path_quoting_gate.py` returns 0
    - `git status --porcelain tests/` lists only the new file (no pre-existing test file is modified)
    - `60-03-EVIDENCE.md` § "Discovery grep" holds the verbatim `grep -n` output with a one-line classification per hit
    - `60-03-EVIDENCE.md` § "RED — wrapper-render debug log" holds the verbatim failing transcript, and § "None pin (two-tree)" holds the pre-fix half
    - `git status --porcelain typsphinx/` is empty (no product file touched by this task)
  </acceptance_criteria>
  <done>MSG-04's caplog gate is committed RED with its verbatim transcript, the package-alone `None` pin is committed already-green as the pre-fix half of a two-tree measurement, and no product file has been touched.</done>
  <reversibility rating="reversible">One new test module plus a planning file; both deletable with no downstream effect.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route writer.py's wrapper-render debug log through quote_path()</name>
  <files>typsphinx/writer.py</files>
  <read_first>
    - typsphinx/writer.py lines 1-30 (the module-scope import block the one new import line joins)
    - typsphinx/writer.py lines 495-516 (the exact region this task edits — re-derive its line numbers from task 1's discovery grep)
    - typsphinx/pathfmt.py (the helper's signature: it accepts `str`, `os.PathLike` and `None`, and raises `TypeError` for anything else)
    - tests/test_writer_path_quoting_gate.py (the gate committed by task 1 — the contract this task must satisfy)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATTERNS.md § "typsphinx/writer.py wiring plan (MSG-04, wave 2)"
  </read_first>
  <behavior>
    - `render_wrapper(..., wrapper_relative_dir="C:\\Users\\runner\\out\\sub", content_relative_path="C:\\Users\\runner\\out\\sub\\index.typ")` emits a DEBUG record with no doubled backslash run
    - the same record still names the docname with its current repr conversion
    - the package-alone path emits `template_file=None` with the bare four-character word, unchanged
    - an empty `wrapper_relative_dir` emits two apostrophes, unchanged
  </behavior>
  <action>
    Add one module-scope import to `typsphinx/writer.py`, in the existing `typsphinx` import group:
    `from typsphinx.pathfmt import quote_path`. Nothing else in the import block changes.

    In `render_wrapper()`'s `logger.debug` call (~:510-514), substitute the helper call for the repr
    conversion on exactly three values: `wrapper_relative_dir`, `include_path` and `template_file`.
    Leave `docname` exactly as it is — it is identifier-valued (D-07) and routing it would misclassify
    a namespace name as a filesystem location.

    Do NOT add a conditional for the `None` case. `writer.py` binds `template_file` to `None` on the
    package-alone build path (the `typst_package and not raw_template_path` branch a few lines above),
    and D-03 puts the `None` handling inside `quote_path()` itself precisely so this call site stays a
    straight substitution — a conditional here would duplicate the invariant at the one site that has
    it and invite drift. `quote_path(None)` returns the bare four-character string, which keeps this
    line byte-identical for that build shape.

    Do not reword, reorder or reflow the debug sentence itself; only the three interpolations change.
    Do not touch `_entry_element_value()`'s fallback warning — its three values are a docname and a
    title/author string resolved from `config.project` / `config.author`, measured as
    identifier-valued (D-07), which is why MSG-04's restriction to this one log call is correct as
    written rather than an oversight. Do not touch `typsphinx/builder.py` or
    `typsphinx/template_registry.py` — they belong to the sibling wave-2 plans.

    Run the gate, the full suite, `black --check .`, `mypy typsphinx/`, and
    `uv run pytest tests/test_repr_census_guard.py -q`. If mypy objects because `template_file`'s
    declared type is wider than the helper's, narrow at this call site — never widen `quote_path()`'s
    signature.

    Paste the verbatim green transcripts into `## GREEN`, and complete the two-tree measurement in
    `## None pin (two-tree)`: read `Plan base SHA` from the top of the evidence file, run
    `git checkout <plan base SHA> -- typsphinx/writer.py` to restore the pre-fix module while keeping
    the new test, run `uv run pytest tests/test_writer_path_quoting_gate.py -q -k template_file_none -s`
    capturing the emitted debug line verbatim, then `git checkout HEAD -- typsphinx/writer.py` and run
    the identical command again, capturing that line too. Paste both and state explicitly that the two
    debug lines are byte-identical — if they are not, HALT, because D-03's `None` contract has not held
    and the package-alone build path has changed. Confirm `git status --porcelain typsphinx/` is clean
    before committing.
  </action>
  <verify>
    <automated>uv run pytest tests/test_writer_path_quoting_gate.py -q &amp;&amp; uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; uv run pytest tests/test_repr_census_guard.py -q &amp;&amp; grep -q 'byte-identical' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md &amp;&amp; test -z "$(git status --porcelain typsphinx/)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_writer_path_quoting_gate.py -q` exits 0 with 0 skipped
    - `uv run pytest -q` exits 0 (full suite green)
    - `grep -c 'quote_path(' typsphinx/writer.py` returns 3
    - `grep -c 'from typsphinx.pathfmt import quote_path' typsphinx/writer.py` returns 1
    - `grep -nE '\{(wrapper_relative_dir|include_path|template_file)!r\}' typsphinx/writer.py` prints nothing
    - `grep -cE '\{docname!r\}' typsphinx/writer.py` returns 1 — the docname on the same line is untouched
    - `grep -c 'if template_file is None' typsphinx/writer.py` returns 0 — no conditional was added at the call site
    - `uv run pytest tests/test_repr_census_guard.py -q` exits 0
    - `uv run black --check .` exits 0 and `uv run mypy typsphinx/` exits 0
    - `git diff --name-only -- typsphinx/` lists only `typsphinx/writer.py`
    - `60-03-EVIDENCE.md` § "None pin (two-tree)" holds two labelled debug lines and an explicit statement that they are byte-identical
    - `git status --porcelain typsphinx/` is empty (the temporary checkout was restored)
  </acceptance_criteria>
  <done>The three path-valued interpolations in `writer.py`'s wrapper-render debug log route through `quote_path()`, the docname is untouched, the package-alone `None` line is proven byte-identical across the pre-fix and post-fix trees, and every gate plus the full suite, black, mypy and the census guard are green.</done>
  <reversibility rating="reversible">A one-line import plus three in-place substitutions inside one f-string.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Confirm D-07's writer.py measurement and close MSG-04's evidence</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md</files>
  <read_first>
    - typsphinx/writer.py lines 115-160 (`_entry_element_value` — the fallback warning and how `value` / `default` are resolved from `config.project` / `config.author`)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md § D-07 (the measured claim this task re-confirms rather than restates)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md (the sections written by tasks 1 and 2)
    - .planning/ROADMAP.md § "🚧 v0.9.1" constraints 1, 9 and 10
  </read_first>
  <action>
    Fill `## D-07 confirmation` by re-measuring, not by restating. Read
    `_entry_element_value()`'s fallback warning and its two callers in `render_wrapper()`, and record
    in three or four lines: which argument each of `entry[0]`, `value` and `default` receives at each
    call site; that `default` is `config.project` at one call site and `config.author` at the other;
    and the conclusion that none of the three is a filesystem location under D-05's rule, so MSG-04's
    restriction to the wrapper-render debug log is correct as written. Paste the verbatim source lines
    the conclusion rests on.

    Append a `## RED-first ledger` section naming MSG-04 and pointing at `## RED — wrapper-render debug
    log` for its recorded failure and `## GREEN` for its pass, plus `## None pin (two-tree)` for the
    byte-identity measurement.

    Append a `## Zero test edits (measured)` section holding the verbatim output of
    `git diff --name-status <plan base SHA>..HEAD -- tests/`; every line must begin with `A`. Paste the
    green `uv run pytest tests/test_repr_census_guard.py -q` tail and state that no entry was appended
    to that module's allowlist.

    Fill `## Known gate gaps` honestly: `template_file`'s NON-`None` rendering has no behavioural
    backslash gate in this plan, because that value is built by `compute_template_import_path()` from a
    registry key and a basename and is therefore forward-slash-only by construction — there is no
    portable unit-test shape that puts a backslash in it. State that its routing is proven by the
    source route plus wave 3's repo-wide grep audit (SC#2), and that the `None` half IS gated. Do not
    leave this unnamed.

    Append a `## Wave-3 handoff` section giving the audit plan: the grep command scoped to this module,
    the count of `quote_path(` occurrences now in `typsphinx/writer.py`, and the exact list of
    interpolations in this module that must still render through Python's repr conversion after the
    phase (the wrapper-render `docname`, and the entry-element fallback warning's three values).

    Record `ruff check .` as deferred to CI with its reason (a freshly-provisioned worktree venv pulls
    a generic-linux `ruff` wheel whose ELF the loader rejects on this dev machine; CI holds lint
    authority).
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; grep -q 'D-07 confirmation' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md &amp;&amp; grep -q 'RED-first ledger' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md &amp;&amp; grep -q 'Zero test edits (measured)' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md &amp;&amp; grep -q 'Wave-3 handoff' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `60-03-EVIDENCE.md` § "D-07 confirmation" names all three of `entry[0]`, `value` and `default`, states what each receives at each of the two call sites, and pastes the source lines it rests on
    - `60-03-EVIDENCE.md` § "RED-first ledger" names MSG-04 and points at the sections holding its RED, its GREEN and its two-tree pin
    - `60-03-EVIDENCE.md` § "Zero test edits (measured)" holds a `git diff --name-status` output in which every line begins with `A`
    - `60-03-EVIDENCE.md` § "Known gate gaps" names `template_file`'s non-`None` rendering explicitly and states what proves it instead
    - `60-03-EVIDENCE.md` § "Wave-3 handoff" gives the grep command, the `quote_path(` count, and the four stays-unrouted interpolations
    - the evidence file records `ruff check .` as deferred to CI with its reason
    - `git status --porcelain typsphinx/ tests/` is empty before the commit (this task edits neither)
  </acceptance_criteria>
  <done>D-07's writer.py claim is re-measured rather than restated, and MSG-04's RED-first ledger, zero-test-edit proof, honestly-named gate gap and wave-3 handoff are recorded.</done>
  <reversibility rating="reversible">Evidence-file appends only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved output paths (`wrapper_relative_dir`, `include_path`) → `render_wrapper()`'s debug log → Python `logging` | build-derived filesystem path text crosses into developer-facing diagnostic output |
| resolved registry entry (`template`, `package`) → `template_file` (or `None`) → the same debug log | a `conf.py`-authored value, or a deliberate `None`, crosses into the same message |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-07 | Denial of Service | `typsphinx/writer.py::render_wrapper` debug log on the package-alone build path (ASVS V5 Input Validation, L1) | high | mitigate | `template_file` is `None` on a live, supported build path, so a helper that raised on `None` would abort every package-alone build inside a debug log. Mitigated by D-03's bare-`None` contract implemented inside `quote_path()` (never a call-site conditional), and gated by `TestWrapperDebugLogTemplateFileNone`, which is green before and after and proven byte-identical across both trees. |
| T-60-08 | Information Disclosure | the same debug log's path rendering (ASVS V5, L1) | low | mitigate | D-01's delimiter selection plus the removal of backslash doubling: a Windows-shaped wrapper directory and include path are shown with their real separators and cannot close their delimiter early. Severity is low because this is a DEBUG-level developer diagnostic, not a user-facing refusal. Gated by `TestWrapperDebugLogPathQuoting`. |
| T-60-09 | Tampering | the docname on the same debug line accidentally routed (ASVS V5, L1) | low | mitigate | The explicit stays-unrouted instruction, the docname count acceptance criterion, and wave 3's SC#3 over-reach measurement one wave later. |
| T-60-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan; `60-RESEARCH.md` § "Package Legitimacy Audit" records the phase as stdlib-only with zero new packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `60-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. This plan
contributes: `tests/test_writer_path_quoting_gate.py` (`TestWrapperDebugLogPathQuoting`,
`TestWrapperDebugLogTemplateFileNone`); the three routed interpolations inside
`typsphinx/writer.py::render_wrapper()`'s debug log; and
`.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md`.
</artifacts_this_phase_produces>

<verification>
- `uv run pytest tests/test_writer_path_quoting_gate.py -q` — both classes green, 0 skipped
- `uv run pytest -q` — full suite green
- `uv run pytest tests/test_repr_census_guard.py -q` — green
- `uv run black --check .` and `uv run mypy typsphinx/` — clean (`ruff` deferred to CI)
- `grep -c 'quote_path(' typsphinx/writer.py` returns 3 and the routed-value negative grep prints nothing
- `git diff --name-status <plan base SHA>..HEAD -- tests/` shows only added entries
- `60-03-EVIDENCE.md` holds the discovery grep, MSG-04's verbatim RED, the two-tree `None` pin, the
  GREEN transcripts, the D-07 confirmation, the RED-first ledger, the zero-test-edit measurement, the
  known gate gap and the wave-3 handoff
</verification>

<success_criteria>
1. `wrapper_relative_dir`, `include_path` and `template_file` in `writer.py`'s wrapper-render debug
   log route through `quote_path()`; the `docname` on the same line does not.
2. The debug record was recorded FAILING against the unfixed tree, with its doubled-backslash runs
   visible in the transcript, before the wiring landed.
3. The package-alone `template_file = None` line is proven byte-identical across the pre-fix and
   post-fix trees, and no conditional was added at the call site.
4. `_entry_element_value()`'s fallback warning is re-measured as identifier/title/author-valued and
   left untouched, confirming MSG-04's scope rather than assuming it.
5. Zero existing test assertions modified, and the full suite, `black --check`, `mypy typsphinx/` and
   the AST census guard are green.
</success_criteria>

<output>
Create `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-SUMMARY.md` when done.
</output>
