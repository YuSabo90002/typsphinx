---
phase: 60-one-delimiter-aware-path-quoting-helper-routed-everywhere
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - typsphinx/builder.py
  - tests/test_builder_path_quoting_gate.py
  - tests/test_templates_path_collision_gate.py
  - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md
autonomous: true
requirements: [MSG-03]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'MSG-03/D-06: every path-valued interpolation in typsphinx/builder.py routes through quote_path() -- the three 57-11 message builders'' hardcoded-delimiter values, the docname/target warnings, the v0.8.0-era output-path collision family, the image-rehome warning, and the bundle-copy I/O messages.'
    - 'MSG-03/D-06 AMENDED: the two target interpolations inside _validate_output_path_collisions() that D-06''s original enumeration missed are routed too -- the same semantic value D-08a already classifies as PATH-valued at the _resolve_target_stem() warning, merely read at a second call site.'
    - 'MSG-03/D-08a: _resolve_target_stem()''s path-refusal warning, driven with a Windows-shaped target through a direct call, contains no run of two or more consecutive backslashes -- recorded RED (three doubled runs) against the unfixed tree first.'
    - 'MSG-03/D-08c: _track_image()''s image-rehome warning, driven with a Windows-shaped absolute URI through post_process_images(), contains no run of two or more consecutive backslashes -- recorded RED against the unfixed tree first.'
    - 'MSG-03: _validate_output_path_collisions()''s ExtensionError message, driven with a backslash-bearing typst_documents target, contains no run of two or more consecutive backslashes -- for both the collision branch and the reserved-directory branch, each recorded RED first.'
    - 'MSG-03/D-08d: _copy_bundle_directory()''s never-copied ExtensionError, driven with hand-built Windows-shaped src_dir and dest_dir, contains no run of two or more consecutive backslashes -- recorded RED against the unfixed tree first.'
    - 'MSG-03/SC#4/D-12: the three 57-11 message builders are gated on the SINGLE-QUOTE half, not the backslash half -- a path containing a literal apostrophe comes back wrapped in double quotes so it cannot close its delimiter early. The backslash half of these three has been green since Phase 57, so a backslash-only assertion there would be tautologically green and prove nothing.'
    - 'MSG-03 (edge: adjacency): at every mixed f-string -- the reservation message''s docname beside its routed target, and the never-copied message''s registry key beside its routed template_filename -- routing one value leaves the other''s rendering byte-identical, and the two renderings stay separated by the message''s own literal text rather than merging.'
    - 'MSG-03 (edge: empty): an empty routed value renders as two apostrophes and never raises; _resolve_target_stem()''s own empty-fallback branch still returns the docname through its separate single warning, unchanged by this plan.'
    - 'MSG-03 (edge: encoding): every routed builder value is a str at the point of the call; the one routed position whose runtime type is user-controlled -- the raw typst_documents target inside _validate_output_path_collisions() -- is narrowed by an explicit isinstance check so a non-str target keeps its current repr rendering and never raises TypeError. No Unicode normalization is applied anywhere: a non-ASCII path survives byte-for-byte.'
    - 'MSG-03 (edge: ordering): the left-to-right order of interpolations inside every touched f-string is unchanged, and the failure-list joiner''s "; " ordering (content-file claims before typst_documents wrapper claims) is unchanged by this plan.'
    - 'Zero test edits: tests/test_templates_path_collision_gate.py is touched only by ADDING methods and one class constant to TestWindowsPathEscapingRegressionGuard; _assert_no_doubled_separator and every pre-existing method are byte-identical afterwards.'
    - 'tests/test_out02_escape_target_gate.py stays green with zero edits, proving MSG-01 did its job at the exact site D-08a routes.'
  artifacts:
    - tests/test_builder_path_quoting_gate.py
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md
    - 'typsphinx/builder.py :: 23 path-valued interpolations routed through quote_path()'
    - 'typsphinx/builder.py :: target_text local inside _validate_output_path_collisions()'
    - 'tests/test_templates_path_collision_gate.py :: three added methods on TestWindowsPathEscapingRegressionGuard'
  key_links:
    - 'typsphinx/builder.py -> typsphinx.pathfmt.quote_path: one added module-scope import line; builder.py already imports writer.py at module scope, and pathfmt.py imports nothing from typsphinx, so this adds no edge to the existing cycle-sensitive graph.'
    - '_validate_output_path_collisions() -> target_text local -> both routed target sites: ONE binding read at two message sites, so the isinstance narrowing cannot drift between them.'
    - 'tests/test_out02_escape_target_gate.py::path_named_in -> _resolve_target_stem()''s warning: the MSG-01 decoupling is what lets this site be rewired with zero test edits; its raw-value disjunct stays true under quote_path() output.'
    - 'tests/test_builder.py:598::path_named_in -> _track_image()''s rehome warning: the second MSG-01 site, same guarantee.'
  prohibitions:
    - 'template_registry.py:410 is NOT touched by this plan and no plan in this phase routes it -- it is reached precisely when the value is NOT path-shaped, and SC#3 makes leaving it alone a measured pass criterion.'
    - '_assert_no_doubled_separator and every pre-existing method of TestWindowsPathEscapingRegressionGuard must not be rewritten, reworded, or re-litigated -- 58-REPR-CENSUS.md''s third bucket names that helper as path-valued AND format-asserting BY DESIGN, and MSG-02''s own gate depends on it continuing to catch what it catches.'
    - 'Identifier-valued interpolations must not be routed: registry keys, docnames, whole config tuples and key lists keep their current repr conversion. Routing one would either crash on a non-path type or misrepresent a list or bytes value as a filesystem location.'
    - 'The delimiter-selection logic must not be inlined at any call site -- every routed value calls quote_path(); builder.py must not grow a second, local copy of the rule.'
    - 'No existing test assertion anywhere under tests/ is modified; the only touch to a pre-existing test file is the ADDITION of three methods and one class constant.'
    - 'This plan must not assert on any string emitted by typsphinx/writer.py or typsphinx/template_registry.py -- those belong to the sibling wave-2 plans, and a cross-module assertion would collide at merge even with disjoint files_modified.'
    - 'No new runtime dependency, no new typst_* config value, and no typing-import modernization anywhere in this diff (ROADMAP constraint 14).'
    - 'This phase must not silently grow into a general Windows-path sanitizer: reserved-character handling, device-name handling and length bounding are explicitly out of scope for v0.9.1 and must not be added under cover of a quoting change.'
---

<objective>
Close MSG-03: every path-valued interpolation in `typsphinx/builder.py` routes through
`quote_path()`, gated by a RED-recorded behavioural test at each reachable message family and by
three added single-quote methods on the existing `TestWindowsPathEscapingRegressionGuard`.

Purpose: `builder.py` carries the bulk of the phase's routed sites (23 interpolations across 16
lines) and is the only module whose gate can reuse the proven 57-11 regression-guard pattern. This
plan is one of wave 2's THREE PARALLEL plans (`builder.py` / `writer.py` / `template_registry.py`),
each carrying its own new test module and asserting only on its own module's output.

Output: the wired `typsphinx/builder.py`, `tests/test_builder_path_quoting_gate.py`, three added
methods on `TestWindowsPathEscapingRegressionGuard`, and `60-02-EVIDENCE.md`.

**Wave note (D-09/D-11):** this is Wave 2. This plan is the ONLY wave-2 plan permitted to touch
`tests/test_templates_path_collision_gate.py`, and it touches it by addition only — three parallel
plans each appending methods to one shared class is the exact merge hazard this project has already
paid for once. Plans 03 and 04 write their own new modules instead. Wave 3's audit runs one wave
later than what it audits, deliberately.

**Line numbers in this plan are anchors, not coordinates.** Phase 59 shifted every number
`REQUIREMENTS.md` and the ROADMAP record, and this plan's own edits will shift the later ones as it
works. Re-measure with the discovery grep in task 1 before every edit; the descriptions (function
name + variable name) are the stable identity.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATTERNS.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-RESEARCH.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-VALIDATION.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 11). Before running ANY command in this plan, provision the worktree's own venv:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Without this, pytest imports the UNCHANGED
main-tree package and the gate stays RED after a correct fix. Do not degrade to main-tree execution.
</worktree_provisioning>

<tasks>

<task type="tracer">
  <name>Task 1: Run the discovery grep, then commit MSG-03's behavioural gate RED at all five message families</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md, tests/test_builder_path_quoting_gate.py, tests/test_templates_path_collision_gate.py</files>
  <read_first>
    - typsphinx/builder.py lines 496-600 (the three 57-11 message builders and their docstrings)
    - typsphinx/builder.py lines 801-925 (`_resolve_target_stem` — note the path-refusal warning sits INSIDE `if isinstance(target, str):`, so its target is guaranteed `str`)
    - typsphinx/builder.py lines 1059-1213 (`_validate_output_path_collisions` in full — the `_claim` closure, the content-file loop, the `typst_documents` loop where `target = entry[1]`, and the failures joiner)
    - typsphinx/builder.py lines 597-640 (`_is_usable_typst_documents_entry` — READ THIS: it checks `len(entry) >= 2` and `isinstance(entry[0], str)` and does NOT check the type of `entry[1]`)
    - typsphinx/builder.py lines 1925-1950 (`_track_image`'s escape branch and the image-rehome warning)
    - typsphinx/builder.py lines 2171-2245 (`_copy_bundle_directory` — its four-parameter signature and the never-copied `ExtensionError`)
    - tests/test_templates_path_collision_gate.py lines 412-495 (`TestWindowsPathEscapingRegressionGuard` — the two Windows-shaped constants, `_assert_no_doubled_separator`, and the `test_registry_keys_stay_repr_quoted` control that is the shape for an identifier-stays-unrouted companion)
    - tests/test_track_image_key_construction.py lines 1-100 (`_build_single_image_document` plus the `temp_sphinx_app` → `TypstBuilder` → `builder.init()` → `caplog.at_level("WARNING")` → `post_process_images` house pattern)
    - tests/test_builder_output_stem.py lines 380-465 (the `types.SimpleNamespace(found_docs=...)` + `builder.config.typst_documents = [...]` + `pytest.raises(ExtensionError)` pattern for driving `_validate_output_path_collisions()`)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md § D-05, D-06 (including the AMENDED block), D-07, D-08, D-11, D-12
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md § "Handoff to wave 2" (the import line and the type contract)
  </read_first>
  <action>
    FIRST create
    `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md`
    (per D-10 this exact name; `60-VERIFICATION.md` is reserved and must never be produced). Give it a
    `# Phase 60 Plan 02 — MSG-03 Evidence` heading, a `## Plan base SHA` section holding
    `git rev-parse HEAD` taken before any edit, a `## Discovery grep` section, and empty
    `## RED — three 57-11 builders (single-quote half)`, `## RED — _resolve_target_stem`,
    `## RED — _track_image rehome warning`, `## RED — _validate_output_path_collisions`,
    `## RED — _copy_bundle_directory`, `## Non-str target characterization`, `## GREEN`, and
    `## Known gate gaps` sections.

    THEN run the discovery grep SC#2 names as the authority and paste its verbatim output into
    `## Discovery grep`, with the resolved line numbers as measured RIGHT NOW (they will differ from
    every number written in `REQUIREMENTS.md`, the ROADMAP and `60-CONTEXT.md` — those are stale
    coordinates, correct only as descriptions). Run all four:
    `grep -n "\!r" typsphinx/builder.py`, `grep -n "repr(" typsphinx/builder.py`,
    `grep -n "%r" typsphinx/builder.py`, and
    `grep -noE "'\{[a-zA-Z_.]+\}'" typsphinx/builder.py`. The fourth is the one that finds the three
    57-11 builders' hardcoded-delimiter sites, which carry the delimiter-selection defect without
    using a repr conversion at all — a plain repr-conversion grep would miss them entirely.

    THEN add exactly three methods and one class constant to
    `TestWindowsPathEscapingRegressionGuard` in `tests/test_templates_path_collision_gate.py`.
    Add the constant `SINGLE_QUOTE_SHAPED_PATH = "/home/O'Brien's Projects/_templates/nested"` beside
    the two existing Windows-shaped constants. Add
    `test_conf17_violation_message_disambiguates_embedded_single_quote`,
    `test_templates_path_collision_message_disambiguates_embedded_single_quote` and
    `test_bundle_destination_collision_message_disambiguates_embedded_single_quote`, each calling the
    REAL construction function (never a re-pasted f-string) with `SINGLE_QUOTE_SHAPED_PATH` in the
    path-valued position, and each asserting that the double-quote-delimited form of that value —
    a double quote, the value, a double quote — is a substring of the message. Give each a docstring
    naming D-12 and stating why the assertion is on the SINGLE-QUOTE half rather than the backslash
    half: these three sites stopped doubling backslashes in Phase 57, so a backslash-doubling
    assertion here would be tautologically green and prove nothing.
    **Add only. Do not modify `_assert_no_doubled_separator`, do not modify or rename any existing
    method, and do not modify the class docstring's existing sentences.**

    THEN create `tests/test_builder_path_quoting_gate.py`. Its module docstring states that this is
    MSG-03's behavioural gate for the inline (non-extracted) message sites in `typsphinx/builder.py`,
    that every Windows shape is a hand-built string literal so the module runs on every CI lane, and
    that it asserts ONLY on strings `typsphinx/builder.py` itself emits — never on a string emitted by
    `writer.py` or `template_registry.py`, which belong to the sibling wave-2 plans. Define a
    module-level helper `_assert_no_doubled_separator(message)` that re-derives the guard predicate
    with `re.findall(r"\\\\+", message)` locally (do not import the class from
    `tests/test_templates_path_collision_gate.py`). Write these classes:

    `TestResolveTargetStemPathQuoting` — builds a `TypstBuilder` from `temp_sphinx_app`, calls
    `builder._resolve_target_stem("index", "C:\\Users\\runner\\escape.typ")` inside
    `caplog.at_level("WARNING")`, selects the WARNING records, and asserts the path-refusal message
    passes `_assert_no_doubled_separator` and still contains the raw target with single separators.
    Pre-fix this message carries three doubled runs.

    `TestTrackImageRehomeWarningPathQuoting` — copies `tests/test_track_image_key_construction.py`'s
    `_build_single_image_document` document setup, drives
    `builder.post_process_images(doc)` for the URI `C:\Users\runner\assets\sub\image.png` inside
    `caplog.at_level("WARNING")`, and asserts the rehome WARNING passes
    `_assert_no_doubled_separator`. Do not assert on the relocation key's digest or suffix — Phase 59
    owns that value and this gate must stay green across it.

    `TestOutputPathCollisionMessagePathQuoting` — two methods, following
    `tests/test_builder_output_stem.py`'s `types.SimpleNamespace(found_docs=...)` pattern.
    (a) the collision branch: `found_docs={"index", "chapter1"}` and
    `typst_documents = [("index", r"manuals\guide.typ", "T", "A"), ("chapter1", r"manuals\guide.typ", "T", "A")]`,
    both entries resolving to the same wrapper path; assert `pytest.raises(ExtensionError)` and that
    `str(excinfo.value)` passes `_assert_no_doubled_separator`.
    (b) the reserved-directory branch: `found_docs={"index"}` and
    `typst_documents = [("index", r"_template\x.typ", "T", "A")]`; same two assertions.
    Both messages carry the raw target and are RED pre-fix. If either shape turns out not to reach
    its branch when you run it, do not weaken the assertion — read the function, find the shape that
    does reach it, and record the substitution and its reason in the evidence file.

    `TestBundleCopyMessagePathQuoting` — calls
    `builder._copy_bundle_directory(r"C:\Users\runner\project\_typst", r"C:\out\_template\mykey", "mykey", "base.typ")`
    directly. The source directory does not exist on a POSIX host, so the walk yields nothing, the
    template is never copied, and the never-copied `ExtensionError` fires with both Windows-shaped
    directory values in it; assert `pytest.raises(ExtensionError)` and that `str(excinfo.value)`
    passes `_assert_no_doubled_separator`.

    `TestBuilderIdentifierQuotingControl` — the falsification gate for task 2's type narrowing, and
    the adjacency control. It must be GREEN both before and after the fix. Give its two methods the
    names `test_identifier_quoting_control_none_target_still_raises_extension_error` and
    `test_identifier_quoting_control_int_target_still_raises_extension_error`, so the selector
    `-k identifier_quoting_control` picks up exactly these two and nothing else. Drive
    `_validate_output_path_collisions()` with `found_docs={"index"}` and
    `typst_documents = [("index", None, "T", "A")]`, then again with
    `typst_documents = [("index", 123, "T", "A")]`. For each: assert the raised error is an
    `ExtensionError` and NOT a `TypeError`, assert `str(excinfo.value)` contains the bare rendering
    of the non-str value (the word `None` / the digits `123`) exactly as Python's own repr produces
    it today, and assert the docname in the same message is apostrophe-delimited. Give the class a
    docstring recording the measured fact this control exists for: `_is_usable_typst_documents_entry`
    checks only that the entry has at least two elements and that its first element is a `str` — it
    does NOT constrain the type of the target — so an unconditional route of that value through
    `quote_path()` would turn a warned-about config typo into an unhandled `TypeError` on every build.

    Run `uv run pytest tests/test_builder_path_quoting_gate.py tests/test_templates_path_collision_gate.py -q`
    and capture the FULL verbatim output. Paste each family's failure into its own `## RED — …`
    section of the evidence file together with the command and the plan base SHA, and paste the
    `TestBuilderIdentifierQuotingControl` result (which must already be PASSING) into
    `## Non-str target characterization` labelled as the pre-fix half of a two-tree pin.
    Commit the RED gate and the evidence file together; the wiring is task 2.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; ! uv run pytest tests/test_builder_path_quoting_gate.py -q &amp;&amp; uv run pytest tests/test_builder_path_quoting_gate.py -q -k identifier_quoting_control &amp;&amp; grep -q 'Discovery grep' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md &amp;&amp; test -z "$(git status --porcelain typsphinx/)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_builder_path_quoting_gate.py` exists and `grep -c 'class Test' tests/test_builder_path_quoting_gate.py` returns 5
    - `uv run pytest tests/test_builder_path_quoting_gate.py -q` exits NON-ZERO with at least 5 failures at this point in the plan
    - `uv run pytest tests/test_builder_path_quoting_gate.py -q -k identifier_quoting_control` exits 0 — the control is green BEFORE the fix
    - `uv run pytest tests/test_templates_path_collision_gate.py -q -k disambiguates_embedded_single_quote` exits NON-ZERO with exactly 3 failures
    - `git diff -U0 -- tests/test_templates_path_collision_gate.py | grep -c '^-'` returns 1 (the single unified-diff header line only — no removed source line, proving the touch was pure addition)
    - `grep -c 'def _assert_no_doubled_separator' tests/test_templates_path_collision_gate.py` returns 1 and that method's body is unchanged
    - `grep -c 'template_registry' tests/test_builder_path_quoting_gate.py` returns 0 and `grep -c 'render_wrapper' tests/test_builder_path_quoting_gate.py` returns 0 — this plan asserts only on `builder.py` output
    - `60-02-EVIDENCE.md` § "Discovery grep" holds the verbatim output of all four grep commands with their line numbers as measured in this worktree
    - `60-02-EVIDENCE.md` has a populated `## RED — …` section for each of the five families
    - `git status --porcelain typsphinx/` is empty (no product file touched by this task)
  </acceptance_criteria>
  <done>The discovery grep is recorded, all five message families are committed RED with verbatim transcripts, the non-str-target control is committed already-green as the pre-fix half of its pin, and no product file has been touched.</done>
  <reversibility rating="reversible">One new test module plus three added methods and one constant on an existing class; both removable without touching product code.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route every path-valued interpolation in typsphinx/builder.py through quote_path()</name>
  <files>typsphinx/builder.py</files>
  <read_first>
    - typsphinx/builder.py lines 1-35 (the module-scope import block the one new import line joins)
    - typsphinx/builder.py lines 496-600, 801-925, 1059-1213, 1925-1950, 2171-2245 (every region this task edits — read each before editing it, and re-derive its line numbers from the task-1 discovery grep rather than trusting any number written in this plan)
    - typsphinx/pathfmt.py (the helper's own signature and docstring — its type contract is what forces the narrowing described below)
    - tests/test_builder_path_quoting_gate.py (the gate committed by task 1 — the contract this task must satisfy)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATTERNS.md § "typsphinx/builder.py wiring plan (MSG-03, wave 2)" (the full site list and the stays-unrouted list)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md § D-07 (the exact identifier-valued sites that must NOT be touched)
  </read_first>
  <behavior>
    - `builder._resolve_target_stem("index", "C:\\Users\\runner\\escape.typ")` emits a warning containing three single backslashes, not six
    - `builder.post_process_images(doc)` for a Windows-shaped absolute URI emits a rehome warning with no doubled backslash run
    - `_validate_output_path_collisions()` with two entries sharing a backslash-bearing target raises `ExtensionError` whose message has no doubled backslash run
    - `_validate_output_path_collisions()` with a `None` target still raises `ExtensionError` (never `TypeError`) and still renders the bare word `None`
    - `_copy_bundle_directory(...)` with Windows-shaped directory values raises `ExtensionError` whose message has no doubled backslash run
    - `_conf17_violation_message("mykey", "/home/O'Brien's Projects/_templates/nested", "/srcdir")` wraps the apostrophe-bearing path in double quotes
  </behavior>
  <action>
    Add one module-scope import to `typsphinx/builder.py`, in the existing `typsphinx` import group:
    `from typsphinx.pathfmt import quote_path`. Nothing else in the import block changes.

    Route these 23 interpolations, identified by enclosing function and variable name (re-measure the
    line numbers from task 1's discovery grep; the numbers below are anchors from a clean tree and
    WILL shift as you edit):

    In `_conf17_violation_message()` (~:524, :526): `resolved_path` and `srcdir`. Both currently sit
    inside hardcoded apostrophe delimiters in the f-string — **delete those surrounding apostrophe
    characters** and substitute the helper call, because the helper supplies its own delimiter.
    In `_templates_path_collision_message()` (~:558, :560, :561): `bundle_dir`, `raw_tp_entry` and
    `resolved_tp_entry`, same hardcoded-delimiter removal.
    In `_bundle_destination_collision_message()` (~:594): `dest_dir`, same.
    In `_resolve_target_stem()` (~:890): `target` and `fallback` (D-08a — `target` is the raw
    `typst_documents` target, which the escape predicate itself treats as path-bearing, and `fallback`
    is a surviving path component; both are guaranteed `str` here because this warning sits inside the
    `isinstance(target, str)` branch).
    In `_validate_output_path_collisions()`: `relpath` in the `_claim` closure (~:1135);
    `content_relpath` and `TEMPLATE_OUTPUT_DIR` (~:1157, :1158); `wrapper_relpath` and
    `TEMPLATE_OUTPUT_DIR` (~:1200, :1201); `relpath` in the failures joiner (~:1208); and the two
    `target` sites (~:1192, :1199) described in the next paragraph.
    In `_track_image()` (~:1943, :1944): `resolved_uri` and `key`. The `key` here is the relocation
    path (`_typst_converted/{digest}-{basename}` after Phase 59), NOT a registry key — D-08c names it
    explicitly as an MSG-03 site, so do not skip it while reading SC#3's "registry keys stay
    unrouted".
    In `_copy_bundle_directory()` (~:2232, :2241, :2242): `src_file`, `dest_file`,
    `template_filename`, `src_dir` and `dest_dir`. The registry `key` on the same lines stays as it
    is — this is a mixed site by design.
    `TEMPLATE_OUTPUT_DIR` is path-valued per D-08b (the message's own words name it as a path
    segment); its rendered output is identical either way, so routing it removes a judgement call from
    wave 3's audit rather than changing behaviour.

    **The two `target` sites inside `_validate_output_path_collisions()` need a type narrowing, and
    this is a planner-surfaced correction to D-06's AMENDED block — implement it exactly as written.**
    Measured this planning session: `_is_usable_typst_documents_entry()` admits an entry on
    `len(entry) >= 2` and `isinstance(entry[0], str)` alone, so `entry[1]` reaching these two message
    sites may be any type a user wrote in `conf.py` — `None`, an `int`, a `list`. `quote_path()`
    raises `TypeError` for those by D-03's deliberate contract, so an unconditional route here would
    convert a warned-about config typo into an unhandled crash on every build of such a project.
    Bind ONE local immediately after `target = entry[1]`:
    `target_text = quote_path(target) if isinstance(target, str) else repr(target)`
    and substitute `{target_text}` at BOTH message sites. One binding read at two sites is the same
    "one place" discipline the three extracted message builders already embody — it is not a
    duplicated inline conditional, and it is why the narrowing cannot drift between the two sites.
    Add a short comment above the binding recording the measured reason (the usability predicate does
    not constrain the target's type) and stating that the non-`str` rendering is deliberately
    unchanged from today's.

    Do NOT touch, in this file: `key`, `existing_key`, `declared_key` and `RESERVED_REGISTRY_KEY`
    anywhere they name a registry key; `docname` anywhere, including where it shares an f-string with
    a routed `target`; the whole-tuple `entry` in the unusable-entry warning; `doc_tuple` in
    `finish()`; and the two `"; ".join(...)` summary joiners keyed on a registry key. Do not touch
    `typsphinx/writer.py` or `typsphinx/template_registry.py` — they belong to the sibling wave-2
    plans.

    Update the three message builders' docstrings, which are part of the change surface: each
    currently documents its path-valued arguments as being quoted with an explicit hardcoded
    delimiter. Rewrite those argument descriptions to name `typsphinx.pathfmt.quote_path()` and state
    the property it adds — it selects a delimiter the value cannot close early — while keeping the
    existing sentence that explains why the function exists at all (so a unit test can drive the real
    construction code rather than a re-pasted format string). Do not paraphrase or "improve" any
    message TEXT: existing tests read that exact wording.

    Run the gate, the full suite, `black --check .`, `mypy typsphinx/`, and
    `uv run pytest tests/test_repr_census_guard.py -q`. If mypy objects at a routed call site because
    the value's declared type is wider than the helper's, narrow at that call site — never widen
    `quote_path()`'s signature.

    Paste the verbatim green transcripts into `## GREEN`, and re-run the identifier control, pasting
    its post-fix result into `## Non-str target characterization` as the second half of the two-tree
    pin, with an explicit statement that the two halves are byte-identical.
  </action>
  <verify>
    <automated>uv run pytest tests/test_builder_path_quoting_gate.py tests/test_templates_path_collision_gate.py -q &amp;&amp; uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; uv run pytest tests/test_repr_census_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_builder_path_quoting_gate.py -q` exits 0 with 0 skipped
    - `uv run pytest tests/test_templates_path_collision_gate.py -q` exits 0 with 0 skipped
    - `uv run pytest -q` exits 0 (full suite green)
    - `uv run pytest tests/test_out02_escape_target_gate.py tests/test_builder.py -q` exits 0 with zero edits to either file — the two MSG-01 sites prove the decoupling held
    - `grep -c 'quote_path(' typsphinx/builder.py` returns at least 20
    - `grep -c 'from typsphinx.pathfmt import quote_path' typsphinx/builder.py` returns 1
    - `grep -nE '\{(resolved_path|srcdir|bundle_dir|raw_tp_entry|resolved_tp_entry|dest_dir|target|fallback|relpath|content_relpath|wrapper_relpath|resolved_uri|src_file|dest_file|src_dir|template_filename|TEMPLATE_OUTPUT_DIR)!r\}' typsphinx/builder.py` prints nothing
    - `grep -c 'target_text' typsphinx/builder.py` returns 3 (one binding, two substitutions)
    - `grep -cE '\{docname!r\}' typsphinx/builder.py` returns at least 10 — docnames are untouched
    - `uv run pytest tests/test_repr_census_guard.py -q` exits 0
    - `uv run black --check .` exits 0 and `uv run mypy typsphinx/` exits 0
    - `git diff --name-only -- typsphinx/` lists only `typsphinx/builder.py`
  </acceptance_criteria>
  <done>All 23 path-valued interpolations in `builder.py` route through `quote_path()`, the identifier-valued ones are untouched, the two `target` sites are narrowed so a non-`str` config value still warns instead of crashing, the three docstrings name the helper, and every gate plus the full suite, black, mypy and the census guard are green.</done>
  <reversibility rating="reversible">A one-line import plus in-place substitutions inside f-strings and three docstring edits; each is individually revertible.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Record MSG-03's RED-first ledger, the zero-test-edit measurement, and the known gate gaps</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md (the five RED sections and the GREEN section written by tasks 1 and 2)
    - .planning/phases/58-repr-format-decoupling-test-side-only/58-REPR-CENSUS.md § "The pass-criterion census" and § "Third bucket" (the enumeration this plan's zero-test-edit claim is checked against)
    - tests/test_repr_census_guard.py lines 1-80 (what turns the AST guard RED, and why a quietly-appended allowlist entry is forbidden)
    - .planning/ROADMAP.md § "🚧 v0.9.1" constraints 1, 9 and 10
  </read_first>
  <action>
    Append a `## RED-first ledger` section naming MSG-03 and listing, for each of the five message
    families plus the three single-quote methods, the evidence section holding its recorded failure
    and the command that produced it. A green run that was never first recorded RED is evidence of
    nothing here, so every row must point at a real transcript.

    Append a `## Zero test edits (measured)` section holding the verbatim output of
    `git diff --name-status <plan base SHA>..HEAD -- tests/`. Every line must begin with `A` except
    the single `M` line for `tests/test_templates_path_collision_gate.py`; for that one line, also
    paste the verbatim output of `git diff -U0 -- tests/test_templates_path_collision_gate.py` and
    state that it contains no removed source line, so the touch was pure addition and no existing
    assertion changed. Paste the green `uv run pytest tests/test_repr_census_guard.py -q` tail and
    state that no entry was appended to that module's allowlist.

    Fill `## Known gate gaps` honestly. Name the routed interpolations in
    `_copy_bundle_directory()`'s copy-failure `except` branch: they are routed but have NO behavioural
    gate in this plan, because reaching them needs a `shutil.copy2` failure on a file that IS the
    resolved template, which is not constructible as a portable unit test. State that their proof is
    wave 3's repo-wide grep audit (SC#2) rather than a behavioural assertion, and that this is
    recorded rather than papered over. If any other routed site ended up without a behavioural gate,
    add it here with the same treatment — do not leave an ungated site unnamed.

    Append a `## Wave-3 handoff` section listing, for the audit plan: the four discovery-grep commands
    scoped to this module, the count of `quote_path(` occurrences now in `typsphinx/builder.py`, and
    the exact list of identifier-valued names in this module that must still render through Python's
    repr conversion after the phase (registry keys in every form, docnames, the whole-tuple config
    entry, the config doc-tuple, and the sorted key lists).

    Record `ruff check .` as deferred to CI with its reason (a freshly-provisioned worktree venv pulls
    a generic-linux `ruff` wheel whose ELF the loader rejects on this dev machine; CI holds lint
    authority).
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; grep -q 'RED-first ledger' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md &amp;&amp; grep -q 'Zero test edits (measured)' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md &amp;&amp; grep -q 'Known gate gaps' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md &amp;&amp; grep -q 'Wave-3 handoff' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `60-02-EVIDENCE.md` § "RED-first ledger" has one row per message family and per added single-quote method, each naming the section holding its transcript
    - `60-02-EVIDENCE.md` § "Zero test edits (measured)" holds the verbatim `git diff --name-status` output, in which the only non-`A` line is the one `M` for `tests/test_templates_path_collision_gate.py`
    - that section also holds the verbatim `git diff -U0` for that file, containing no removed source line
    - `60-02-EVIDENCE.md` § "Known gate gaps" names the copy-failure `except` branch explicitly and states that wave 3's grep audit is its proof
    - `60-02-EVIDENCE.md` § "Wave-3 handoff" lists the four grep commands, the `quote_path(` count, and the identifier-valued names that must stay unrouted
    - the evidence file records `ruff check .` as deferred to CI with its reason
    - `git status --porcelain typsphinx/ tests/` is empty before the commit (this task edits neither)
  </acceptance_criteria>
  <done>MSG-03's RED-first ledger, its measured zero-test-edit proof, its honestly-named gate gaps and its wave-3 handoff are all recorded in `60-02-EVIDENCE.md`.</done>
  <reversibility rating="reversible">Evidence-file appends only.</reversibility>
</task>

</tasks>

<flagged_assumptions>
**Planner-surfaced finding, folded into task 2 rather than deferred.**

`60-CONTEXT.md` D-06's AMENDED block and `60-RESEARCH.md` § "Divergence found" both instruct that
`_validate_output_path_collisions()`'s two `target` interpolations route through `quote_path()`
unconditionally. Measured against the live tree during this planning run:
`_is_usable_typst_documents_entry()` admits an entry on `len(entry) >= 2` and
`isinstance(entry[0], str)` alone — it places **no** constraint on the type of `entry[1]`. The value
therefore reaches both message sites as whatever the user wrote in `conf.py`, and both sites build
their string on **every** build of such a project (the `_claim` description is constructed
unconditionally, not only on collision). `quote_path()` raises `TypeError` for a non-`str`,
non-`PathLike`, non-`None` value by D-03's deliberate contract, so an unconditional route would turn
a today-warned config typo (`typst_documents = [("index", None, ...)]`) into an unhandled crash.

This is not a re-litigation of D-06's amendment — both sites still route, exactly as decided. It is
the type narrowing the amendment's own reasoning requires once the usability predicate's actual
contract is measured. Task 2 implements it as ONE `target_text` binding read at both sites, and task
1 commits `TestBuilderIdentifierQuotingControl` as its falsification gate: the control is green
before the fix and must stay byte-identically green after it.

The sibling site at `_resolve_target_stem()` needs no narrowing — its warning sits inside an
`isinstance(target, str)` branch, so its `target` is already guaranteed `str`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user `conf.py` `typst_documents` entries → `_validate_output_path_collisions()` / `_resolve_target_stem()` → output-path construction and diagnostics | an arbitrarily-typed, arbitrarily-shaped user value crosses into path construction and into a message |
| third-party post-transform `node["uri"]` → `_track_image()` → the rehome warning | an extension-authored absolute path string crosses into builder-owned state and into a message |
| installed-package / srcdir template bundle directory → `_copy_bundle_directory()` → `ExtensionError` | filesystem-derived directory paths cross into a build-stopping error message |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-04 | Denial of Service | `typsphinx/builder.py::_validate_output_path_collisions` target rendering (ASVS V5 Input Validation, L1) | high | mitigate | An unconditional route of the raw `typst_documents` target through a helper that raises `TypeError` on a non-`str` would abort every build of a project with a mistyped target. Mitigated by the `target_text` binding's explicit `isinstance(target, str)` narrowing, gated by `TestBuilderIdentifierQuotingControl` driving `None` and `123` targets and asserting `ExtensionError` rather than `TypeError`. |
| T-60-05 | Information Disclosure | every routed message in `typsphinx/builder.py` (ASVS V5, L1) | medium | mitigate | D-01's delimiter selection plus the removal of `repr()`'s backslash doubling: a Windows path is shown with its real separators and an apostrophe-bearing path cannot close its delimiter early, so the message cannot mis-attribute part of a path to the surrounding sentence. Gated per family by `tests/test_builder_path_quoting_gate.py` and by the three added single-quote methods. |
| T-60-06 | Tampering | identifier-valued interpolations accidentally routed (ASVS V5, L1) | medium | mitigate | The explicit stays-unrouted list in `<action>`, the `docname` count acceptance criterion, the green `tests/test_repr_census_guard.py` AST guard, and wave 3's SC#3 over-reach measurement one wave later. |
| T-60-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan; `60-RESEARCH.md` § "Package Legitimacy Audit" records the phase as stdlib-only with zero new packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `60-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. This plan
contributes: `tests/test_builder_path_quoting_gate.py` (`TestResolveTargetStemPathQuoting`,
`TestTrackImageRehomeWarningPathQuoting`, `TestOutputPathCollisionMessagePathQuoting`,
`TestBundleCopyMessagePathQuoting`, `TestBuilderIdentifierQuotingControl`); the three added methods
and the `SINGLE_QUOTE_SHAPED_PATH` constant on `TestWindowsPathEscapingRegressionGuard`; the
`target_text` local inside `typsphinx/builder.py::_validate_output_path_collisions()`; and
`.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md`.
</artifacts_this_phase_produces>

<verification>
- `uv run pytest tests/test_builder_path_quoting_gate.py -q` — five classes green, 0 skipped
- `uv run pytest tests/test_templates_path_collision_gate.py -q` — green, 0 skipped, including the
  three added single-quote methods and every pre-existing method
- `uv run pytest tests/test_out02_escape_target_gate.py tests/test_builder.py -q` — green with zero
  edits to either file
- `uv run pytest tests/test_repr_census_guard.py -q` — green
- `uv run pytest -q` — full suite green
- `uv run black --check .` and `uv run mypy typsphinx/` — clean (`ruff` deferred to CI)
- the routed-interpolation negative grep prints nothing, and `quote_path(` appears at least 20 times
  in `typsphinx/builder.py`
- `60-02-EVIDENCE.md` holds the discovery grep, five RED sections, the two-tree non-str-target pin,
  the GREEN transcripts, the RED-first ledger, the measured zero-test-edit proof, the known gate gaps
  and the wave-3 handoff
</verification>

<success_criteria>
1. Every path-valued interpolation the execution-time discovery grep finds in `typsphinx/builder.py`
   routes through `quote_path()`, including the two `target` sites D-06's enumeration missed.
2. Every identifier-valued interpolation in the same module is measurably untouched.
3. Each of the five message families was recorded FAILING against the unfixed tree before the wiring
   landed, in the shape its site actually admits — the single-quote half for the three 57-11 builders,
   the doubled backslash everywhere else.
4. A non-`str` `typst_documents` target still produces an `ExtensionError` with its current rendering
   and never a `TypeError`, proven by a control that is green both before and after.
5. `tests/test_templates_path_collision_gate.py` changed by pure addition, no other test file changed
   at all, and the full suite, `black --check`, `mypy typsphinx/` and the AST census guard are green.
</success_criteria>

<output>
Create `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-SUMMARY.md` when done.
</output>
