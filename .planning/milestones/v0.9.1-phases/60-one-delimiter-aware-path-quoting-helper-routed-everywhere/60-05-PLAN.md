---
phase: 60-one-delimiter-aware-path-quoting-helper-routed-everywhere
plan: 05
type: execute
wave: 3
depends_on: ["60-02", "60-03", "60-04"]
files_modified:
  - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md
  - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATH-QUOTING-EVIDENCE.md
  - .planning/todos/pending/
autonomous: true
requirements: [MSG-02, MSG-03, MSG-04, MSG-05]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC#2: a repo-wide grep run at execution time -- not any line list written before it -- is the discovery authority, and it shows no path-valued repr-conversion or hardcoded-delimiter interpolation left in typsphinx/builder.py, typsphinx/writer.py or typsphinx/template_registry.py.'
    - 'SC#2 scope discipline: the grep is run REPO-WIDE, not restricted to the three modules the requirements name; a path-valued site found in a fourth module is FILED AS A NEW REQUIREMENT and never fixed inside this phase.'
    - 'SC#3: the identifier-valued interpolations are measurably untouched -- registry keys in every form, docnames, the whole-tuple config entry, the config doc-tuple, and the sorted key lists all still render through Python''s repr conversion, with their counts recorded.'
    - 'SC#3: typsphinx/template_registry.py''s type-check message is measurably still on the repr conversion, and the two existing assertions pinning its output for a list template and a bytes template are green UNMODIFIED -- the phase''s falsification gate for accidental over-reach.'
    - 'SC#5: git diff --name-status between the phase base SHA and the phase tip, restricted to tests/, lists only added entries plus exactly one modified entry for tests/test_templates_path_collision_gate.py, and the unified diff for that one file contains no removed source line.'
    - 'SC#5: no test module enumerated in 58-REPR-CENSUS.md appears as modified in this phase''s tests/ diff, and tests/test_repr_census_guard.py is green with no entry appended to its allowlist.'
    - 'SC#5: the full pytest suite, black --check and mypy typsphinx/ are green on the phase tip, with zero skipped in the four new gate modules.'
    - 'SC#5: the 3-OS CI lane including windows-latest is dispatched FRESH on this phase''s own post-fix tip -- never inferred from a prior run -- with its run URL, dispatched head SHA and per-job conclusions recorded.'
    - 'ROADMAP constraint 1: every one of MSG-02..MSG-05 has a recorded local RED that preceded its green, and 60-PATH-QUOTING-EVIDENCE.md names the per-plan evidence section holding each one.'
    - 'D-10: 60-PATH-QUOTING-EVIDENCE.md is written by REFERENCING the four per-plan evidence files read-only; none of 60-01-EVIDENCE.md through 60-04-EVIDENCE.md is rewritten, appended to, or reformatted by this plan.'
  artifacts:
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATH-QUOTING-EVIDENCE.md
  key_links:
    - 'PHASE_BASE_SHA (recorded by plan 01) -> git diff --name-status against the phase tip: the mechanism that makes SC#5''s zero-test-edit claim a measurement rather than an assertion.'
    - '58-REPR-CENSUS.md -> this phase''s tests/ diff: the enumeration the zero-test-edit claim is CHECKED AGAINST (ROADMAP constraint 9), together with the live AST guard that re-derives it at run time.'
    - 'phase tip -> GitHub Actions ci.yml workflow_dispatch: the acceptance bar is that lane green on this phase''s own tip, dispatched fresh (ROADMAP constraint 10 -- CI is never first discovery).'
    - 'the four per-plan 60-0N-EVIDENCE.md files -> 60-PATH-QUOTING-EVIDENCE.md: reference-only consolidation, which is the single change that bought wave 2''s three-way parallelism.'
  prohibitions:
    - 'No file named 60-VERIFICATION.md is produced or referenced as an output -- that name is reserved and overwritten wholesale by gsd-verifier (D-10).'
    - 'The four per-plan evidence files are read-only to this plan; consolidation is by reference, never by rewriting, appending to, or reformatting them.'
    - 'A repo-wide grep hit in a fourth module (translator.py, template_engine.py, pdf.py, removed_config.py, __init__.py) must be FILED as a new requirement, never fixed here -- widening the grep''s scope mid-phase is exactly what CONTEXT.md''s Deferred Ideas forbids.'
    - 'template_registry.py''s type-check message must not be "finished off" in this plan. It will look like an oversight; SC#3 makes leaving it alone a measured pass criterion.'
    - 'CI is not first discovery: the local RED-then-green record must be complete for all four requirements before the 3-OS lane is dispatched.'
    - 'A stale or older CI run must never be cited as this phase''s acceptance; if the dispatch cannot complete from this worktree, record a PENDING marker with the exact commands rather than a substitute run URL.'
    - 'A skipped test result must never be recorded as a pass; the per-module skip census must show zero skipped in all four new gate modules.'
    - 'No product file and no test file is edited by this plan -- it measures and records only.'
---

<objective>
Close the phase's acceptance obligations: run SC#2's repo-wide discovery grep as the authority and
record that no path-valued interpolation is left in the three modules; measure SC#3's over-reach
criterion rather than asserting it; prove SC#5's zero-test-edit claim against `58-REPR-CENSUS.md` and
the live AST guard; consolidate the four per-plan evidence files by reference into
`60-PATH-QUOTING-EVIDENCE.md`; and dispatch the 3-OS CI lane fresh on the post-fix tip.

Purpose: this is Wave 3, and it runs one wave LATER than everything it audits, deliberately — an
auditing plan sharing a wave with its subject abstains from its own success criterion. Wave 2's three
plans each proved their own module; only a plan that runs after all three have merged can measure the
phase as a whole.

Output: `60-05-EVIDENCE.md` (this plan's own measurements) and `60-PATH-QUOTING-EVIDENCE.md` (the
reference-only consolidation).

**Wave note (D-09/D-10):** Wave 1 (plan 01) → Wave 2 (plans 02, 03, 04 in parallel) → Wave 3 (this
plan). Evidence files are per plan; this plan writes its own and consolidates the other four by
reference, never by rewriting them. **No file in this phase may be named `60-VERIFICATION.md`.**
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-VALIDATION.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md
@.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 11). Before running ANY command in this plan, provision the worktree's own venv:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Without this, pytest imports the UNCHANGED
main-tree package and every measurement in this plan is taken against the wrong tree.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Run SC#2's repo-wide discovery grep and SC#3's over-reach measurement</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md, .planning/todos/pending/</files>
  <read_first>
    - .planning/ROADMAP.md § "Phase 60" success criteria 2 and 3 (the exact wording the audit must satisfy, including that the execution-time grep and not any line list is the discovery authority)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md § "Wave-3 handoff" (builder.py's grep commands, `quote_path(` count and stays-unrouted list)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md § "Wave-3 handoff"
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md § "Wave-3 handoff"
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md § D-05, D-07, D-08, § "Deferred Ideas" (the fourth-module rule) and § "Specific Ideas" item 4
    - typsphinx/template_registry.py lines 400-440 (the deliberately excluded type-check message, to record its surviving line number)
  </read_first>
  <action>
    Create
    `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md`
    with a `# Phase 60 Plan 05 — Acceptance Evidence` heading and a `## Phase tip SHA` section holding
    `git rev-parse HEAD`.

    **SC#2 — repo-wide discovery grep, run REPO-WIDE.** Run each of these over the whole `typsphinx/`
    package, not only the three named modules (milestone invariant #4: an "anywhere under X" criterion
    is checked by a repo-wide grep at discovery time, never against the files a requirement happens to
    name):
    `grep -rn "\!r" typsphinx/`, `grep -rn "repr(" typsphinx/`, `grep -rn "%r" typsphinx/`, and
    `grep -rnoE "'\{[a-zA-Z_.]+\}'" typsphinx/`.
    Paste all four verbatim outputs into a `## SC#2 repo-wide discovery grep` section. Then classify
    every hit in the three in-scope modules under D-05's role rule ("does the reader read this value as
    a location on a filesystem, or as a name in a namespace?") in a table with columns
    module / line / value / classification / disposition, and state the conclusion: zero path-valued
    interpolations remain unrouted in `typsphinx/builder.py`, `typsphinx/writer.py` and
    `typsphinx/template_registry.py`.

    For any hit in a FOURTH module — `translator.py`, `template_engine.py`, `pdf.py`,
    `removed_config.py`, `__init__.py` — classify it the same way but do NOT fix it. The source todo's
    own census already classified `translator.py`'s master-docname and path-element interpolations as
    docname-valued despite their variable names, so the expected outcome is that nothing genuine turns
    up. If a genuinely path-valued site DOES turn up, write a new todo under `.planning/todos/pending/`
    naming the module, the line, the value, why it is path-valued under D-05, and that it is out of
    scope for Phase 60 because MSG-03/04/05 name three modules; record its filename in the evidence
    file. Widening this phase's scope mid-flight is exactly what CONTEXT.md's Deferred Ideas forbids.

    **SC#3 — over-reach measurement.** In a `## SC#3 over-reach measurement` section record, each as a
    verbatim command plus its output rather than a claim:
    the count of surviving repr conversions on registry keys in every form across the three modules;
    the count of surviving repr conversions on docnames; the surviving whole-tuple config `entry` and
    config `doc_tuple` interpolations; the surviving sorted-key-list interpolations; and — separately
    and by name — that `typsphinx/template_registry.py`'s type-check message still carries a repr
    conversion on its `template` value, giving its line number as measured now. State that this is a
    MEASURED PASS CRITERION, not an oversight: that branch is reached precisely when the value is not
    a path type, so routing it would misrepresent a list or bytes value as a filesystem location and
    would raise `TypeError` on the exact values it exists to report.

    Then run `uv run pytest tests/test_template_registry.py -q` and paste its green tail, stating that
    the two assertions pinning that message's repr output for a list template and a bytes template are
    green UNMODIFIED — they are the phase's falsification gate for accidental over-reach. Confirm with
    `git diff --name-status <phase base SHA>..HEAD -- tests/test_template_registry.py` that the file
    does not appear at all.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest tests/test_template_registry.py -q &amp;&amp; grep -q 'SC#2 repo-wide discovery grep' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md &amp;&amp; grep -q 'SC#3 over-reach measurement' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md &amp;&amp; test -z "$(git status --porcelain typsphinx/ tests/)"</automated>
  </verify>
  <acceptance_criteria>
    - `60-05-EVIDENCE.md` § "SC#2 repo-wide discovery grep" holds all four grep outputs, each produced by a command whose path argument is `typsphinx/` (the whole package), not a three-file list
    - that section holds a classification table covering every hit in the three in-scope modules, and states the zero-path-valued-remaining conclusion
    - `grep -rnE '\{(resolved_path|srcdir|bundle_dir|raw_tp_entry|resolved_tp_entry|dest_dir|target|fallback|relpath|content_relpath|wrapper_relpath|resolved_uri|src_file|dest_file|src_dir|template_filename|TEMPLATE_OUTPUT_DIR|wrapper_relative_dir|include_path|template_file)!r\}' typsphinx/` prints nothing
    - `grep -c 'quote_path(' typsphinx/builder.py` is at least 20, `grep -c 'quote_path(' typsphinx/writer.py` returns 3, and `grep -c 'quote_path(' typsphinx/template_registry.py` returns 2
    - `grep -cE '\{template!r\}' typsphinx/template_registry.py` returns exactly 1, and that line number is recorded in the evidence file
    - `60-05-EVIDENCE.md` § "SC#3 over-reach measurement" records each surviving identifier-valued class as a command plus its output, not as a prose claim
    - `uv run pytest tests/test_template_registry.py -q` exits 0 and `git diff --name-status <phase base SHA>..HEAD -- tests/test_template_registry.py` produces no output
    - if any fourth-module path-valued hit was found, a new file exists under `.planning/todos/pending/` naming it, and no product file outside the three in-scope modules was edited
    - `git status --porcelain typsphinx/ tests/` is empty (this task edits neither)
  </acceptance_criteria>
  <done>SC#2's repo-wide grep is recorded with a full classification and no path-valued interpolation left in the three modules, and SC#3's over-reach criterion is measured with commands and outputs rather than asserted — including the deliberately excluded type-check message and its two green, unmodified guarding assertions.</done>
  <reversibility rating="reversible">Evidence-file creation plus, conditionally, one new todo record; no code change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Measure SC#5's zero-test-edit claim and run the final local gate</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/58-repr-format-decoupling-test-side-only/58-REPR-CENSUS.md § "The pass-criterion census" and § "Third bucket" (the enumeration the claim is checked against, including the helper this phase must not rewrite)
    - tests/test_repr_census_guard.py lines 1-80 (the run-time AST re-derivation, its allowlist, and why quietly appending to it is forbidden)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md § "Phase base SHA" (the SHA every diff in this task is taken against)
    - .planning/ROADMAP.md § "🚧 v0.9.1" constraints 1, 9 and 10
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-VALIDATION.md § "Sampling Rate" and § "Manual-Only Verifications"
  </read_first>
  <action>
    Append a `## SC#5 zero test edits (measured)` section. Read `PHASE_BASE_SHA` from
    `60-01-EVIDENCE.md` and paste the verbatim output of
    `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/`. Every line must begin with `A` except
    exactly one `M` line for `tests/test_templates_path_collision_gate.py`. For that one file, paste
    the verbatim `git diff -U0 $PHASE_BASE_SHA..HEAD -- tests/test_templates_path_collision_gate.py`
    and state that it contains no removed source line, so the touch was pure addition and no existing
    assertion changed. If any OTHER file appears with `M` or `D`, HALT and record why — per ROADMAP
    constraint 9 that is a signal the census was incomplete, not a licence to edit.

    Cross-check against `58-REPR-CENSUS.md`: list each test module that census enumerates and confirm,
    with the diff output above, that none of them appears as modified in this phase's `tests/` diff.
    Record explicitly that `TestWindowsPathEscapingRegressionGuard._assert_no_doubled_separator` — the
    census's third-bucket entry, path-valued AND format-asserting BY DESIGN — is byte-identical, and
    paste the `git diff` proof that no line of it changed.

    Paste the green tail of `uv run pytest tests/test_repr_census_guard.py -q` and state that no entry
    was appended to `PASS_CRITERION_REPR_ALLOWLIST`; confirm with
    `git diff $PHASE_BASE_SHA..HEAD -- tests/test_repr_census_guard.py` producing no output.

    Append a `## Final local gate` section with the verbatim tails of `uv run pytest -q`,
    `uv run black --check .` and `uv run mypy typsphinx/`, all green, and a per-module skip census
    showing `0 skipped` for `tests/test_pathfmt.py`, `tests/test_builder_path_quoting_gate.py`,
    `tests/test_writer_path_quoting_gate.py` and
    `tests/test_template_registry_path_quoting_gate.py`. A skipped test is never recorded as a pass —
    a skip in any of these four means the worktree venv is wrong, since every one of them is a pure
    string, `caplog` or `ExtensionError` assertion needing no Windows host and no compiler. Record
    `ruff check .` as deferred to CI with its reason (a freshly-provisioned worktree venv pulls a
    generic-linux `ruff` wheel whose ELF the loader rejects on this dev machine; CI holds lint
    authority).

    Append a `## RED-first ledger (phase-wide)` section: one row per requirement MSG-02, MSG-03,
    MSG-04 and MSG-05, each naming the per-plan evidence file and the section within it that holds the
    recorded pre-fix failure, and the section holding its green. Any requirement without a real
    recorded RED is a phase failure under ROADMAP constraint 1 — do not fill a row with a green-only
    reference.
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; uv run pytest tests/test_repr_census_guard.py -q &amp;&amp; grep -q 'SC#5 zero test edits (measured)' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md &amp;&amp; grep -q 'RED-first ledger (phase-wide)' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md &amp;&amp; test -z "$(git diff --name-status $(grep -m1 -oE '[0-9a-f]{40}' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md)..HEAD -- tests/ | grep -v '^A' | grep -v 'test_templates_path_collision_gate')"</automated>
  </verify>
  <acceptance_criteria>
    - `60-05-EVIDENCE.md` § "SC#5 zero test edits (measured)" holds the verbatim `git diff --name-status` output whose only non-`A` line is the single `M` for `tests/test_templates_path_collision_gate.py`
    - that section holds the verbatim `git diff -U0` for that one file, containing no removed source line, and an explicit statement that `_assert_no_doubled_separator` is byte-identical
    - that section lists every test module enumerated in `58-REPR-CENSUS.md` and confirms none appears as modified
    - `git diff $PHASE_BASE_SHA..HEAD -- tests/test_repr_census_guard.py` produces no output and `uv run pytest tests/test_repr_census_guard.py -q` exits 0
    - `60-05-EVIDENCE.md` § "Final local gate" holds green tails for `uv run pytest -q`, `uv run black --check .` and `uv run mypy typsphinx/`, plus a per-module skip census showing `0 skipped` for all four new gate modules
    - the evidence file records `ruff check .` as deferred to CI with its reason
    - `60-05-EVIDENCE.md` § "RED-first ledger (phase-wide)" has exactly four rows, one per requirement, each naming a per-plan evidence file AND the section holding its recorded pre-fix failure
    - `git status --porcelain typsphinx/ tests/` is empty (this task edits neither)
  </acceptance_criteria>
  <done>SC#5's zero-test-edit claim is measured against the phase base SHA and `58-REPR-CENSUS.md` rather than asserted, the final local gate is green with a real skip census, and every requirement's RED has a named location.</done>
  <reversibility rating="reversible">Evidence-file appends only.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Consolidate the four per-plan evidence files by reference and dispatch the 3-OS CI lane fresh</name>
  <files>.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATH-QUOTING-EVIDENCE.md, .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md, 60-02-EVIDENCE.md, 60-03-EVIDENCE.md, 60-04-EVIDENCE.md (READ-ONLY — the four per-plan records this consolidation references; do not edit any of them)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md (the consolidated-evidence shape D-10 follows)
    - .github/workflows/ci.yml (the `workflow_dispatch` trigger and the `os` / `python-version` matrix whose `windows-latest` jobs are the acceptance bar)
    - .planning/ROADMAP.md § "🚧 v0.9.1" constraints 10 and 12 (CI is not first discovery; the milestone branch is `gsd/v0.9.1-windows-path-correctness` and is already pushed)
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-CONTEXT.md § D-10 (the read-only consolidation rule and the reserved-name prohibition)
  </read_first>
  <action>
    Create
    `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATH-QUOTING-EVIDENCE.md`.
    Per D-10 this file consolidates by REFERENCE: for each of MSG-02, MSG-03, MSG-04 and MSG-05 it
    gives the requirement, the plan that closed it, the per-plan evidence FILE PATH, and the section
    heading within that file holding the recorded RED and the recorded green — plus a one-line
    statement of what that RED actually was, in the shape D-12 assigned that site (the single-quote
    case for the three 57-11 message builders whose backslash half went green in Phase 57; the doubled
    backslash everywhere still on a repr conversion; the `caplog` DEBUG read for the writer site; and
    both the doubled backslash and the leaked class-name wrapper for the registry sites). Add sections
    referencing this plan's own `60-05-EVIDENCE.md` for SC#2's repo-wide grep, SC#3's over-reach
    measurement and SC#5's zero-test-edit measurement. **Do not copy transcripts across, do not rewrite
    any per-plan file, and do not append to one.** Confirm with `git status --porcelain` that
    `60-01-EVIDENCE.md` through `60-04-EVIDENCE.md` are untouched before committing. Also state in one
    line, at the top of the file, that no file in this phase is named `60-VERIFICATION.md` because
    that name is reserved and overwritten wholesale by `gsd-verifier`.

    THEN, and only after tasks 1 and 2 have recorded a complete local RED-then-green for all four
    requirements — ROADMAP constraint 10 makes CI the final confirmation, never the first discovery —
    push the current branch to `origin` and dispatch the CI workflow fresh against this phase's own
    tip: `gh workflow run ci.yml --ref <this branch>`. Do not reuse or cite a prior run; the acceptance
    bar is a run whose head SHA is this phase's post-fix tip.

    Wait for the run to finish (`gh run list --workflow=ci.yml --branch <branch> --limit 1` then
    `gh run watch <run-id>`), then record in `60-05-EVIDENCE.md` under a new `## SC#5 3-OS CI dispatch`
    section: the run URL, the head SHA the run was dispatched against, the local tip SHA so the two can
    be compared, and the conclusion of every matrix job with its OS and Python version, with an
    explicit line for each `windows-latest` job. Reference that section from
    `60-PATH-QUOTING-EVIDENCE.md`.

    If the push or the dispatch cannot complete from this worktree (no credentials, or this branch is
    not the one carrying the phase's merged waves), do not fabricate a result and do not cite an older
    run. Record the exact commands the owner must run and mark the section
    `PENDING — owner dispatch required`, then leave the human check below to close it. A `PENDING`
    marker is an honest open item; a copied older run URL is not.
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; test -f .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATH-QUOTING-EVIDENCE.md &amp;&amp; test ! -e .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-VERIFICATION.md &amp;&amp; grep -q '3-OS CI dispatch' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md &amp;&amp; grep -qE 'windows-latest|PENDING — owner dispatch required' .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md &amp;&amp; test -z "$(git status --porcelain .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-01-EVIDENCE.md .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-02-EVIDENCE.md .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-03-EVIDENCE.md .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-04-EVIDENCE.md)"</automated>
    <human-check>Confirm the recorded CI run URL was dispatched against this phase's own post-fix tip (the head SHA in the evidence file matches the phase tip), that every `windows-latest` matrix job concluded successfully, and that the run is newer than the phase's last commit rather than inferred from an earlier run.</human-check>
  </verify>
  <acceptance_criteria>
    - `60-PATH-QUOTING-EVIDENCE.md` exists and contains one referencing section per requirement (MSG-02, MSG-03, MSG-04, MSG-05), each naming a per-plan evidence file path and the section heading within it holding that requirement's RED and green
    - each of those four sections states in one line what the recorded RED actually was, in the shape D-12 assigned that site
    - `60-PATH-QUOTING-EVIDENCE.md` contains no pasted pytest transcript (consolidation is by reference)
    - `git status --porcelain` shows `60-01-EVIDENCE.md`, `60-02-EVIDENCE.md`, `60-03-EVIDENCE.md` and `60-04-EVIDENCE.md` as unmodified by this plan
    - no file named `60-VERIFICATION.md` exists anywhere under the phase directory
    - `60-05-EVIDENCE.md` § "SC#5 3-OS CI dispatch" contains either (a) a run URL, the dispatched head SHA, the local tip SHA, and a per-job conclusion line for every matrix job including each `windows-latest` job, or (b) a `PENDING — owner dispatch required` marker together with the exact commands to run
    - the recorded head SHA, when present, equals the local tip SHA recorded in the same section
    - no CI run older than this phase's own commits is cited as evidence
    - `git status --porcelain typsphinx/ tests/` is empty (this task edits neither)
  </acceptance_criteria>
  <done>The four per-plan evidence files are consolidated by reference into `60-PATH-QUOTING-EVIDENCE.md` without being touched, and the 3-OS lane has been dispatched fresh on the post-fix tip with per-job results recorded — or the dispatch is explicitly marked pending with reproducible commands, never silently omitted.</done>
  <reversibility rating="reversible">One new evidence file, one evidence-file append, and a branch push; no code change.</reversibility>
</task>

</tasks>

<flagged_assumptions>
## Spec-less edge-coverage accounting (15 probe-surfaced items, 0 silent drops)

This phase has no `60-SPEC.md`, so the edge-coverage report came from the deterministic fallback
probe. All 15 applicable items are authored into a plan's `must_haves.truths`; none was dismissed and
none was dropped.

| Requirement | Category | Status | Verification | Where authored |
|---|---|---|---|---|
| MSG-02 | adjacency | resolved | explicit | 60-01 `must_haves` + `TestQuotePathEdgeContract` (delimiter characters already present in the value are never stripped or reused) |
| MSG-02 | empty | resolved | explicit | 60-01 `must_haves` + `TestQuotePathEdgeContract` (D-04: empty renders as two apostrophes, deliberately unlike `path_named_in()`) |
| MSG-02 | encoding | resolved | explicit | 60-01 `must_haves` + `TestQuotePathTypeContract` / `TestQuotePathEdgeContract` (D-03's `str`/`os.PathLike`/`bytes` contract; `str` code points, no normalization) |
| MSG-02 | ordering | resolved | explicit | 60-01 `must_haves` + `TestQuotePathEdgeContract` (determinism and character-order preservation) |
| MSG-02 | concurrency | resolved | **backstop** | 60-01 `must_haves` structured marker — a pure function with no module-level mutable state and no I/O; the phase genuinely does not constrain a concurrency property, so no criterion is fabricated. A verifier that cannot confirm this with explicit evidence must abstain to `human_needed`, never pass silently. |
| MSG-03 | adjacency | resolved | explicit | 60-02 `must_haves` (the two mixed f-strings: docname beside routed target, registry key beside routed template filename) |
| MSG-03 | empty | resolved | explicit | 60-02 `must_haves` (empty routed value renders as two apostrophes and never raises; the empty-fallback branch is unchanged) |
| MSG-03 | encoding | resolved | explicit | 60-02 `must_haves` + `TestBuilderIdentifierQuotingControl` (the `isinstance` narrowing at the one user-typed position; no normalization) |
| MSG-03 | ordering | resolved | explicit | 60-02 `must_haves` (interpolation order and the failures-list joiner order unchanged) |
| MSG-04 | empty | resolved | explicit | 60-03 `must_haves` + `TestWrapperDebugLogTemplateFileNone` (D-03's live `None`; empty `wrapper_relative_dir`) |
| MSG-04 | encoding | resolved | explicit | 60-03 `must_haves` (every value at this site is `str` or `None`; no `bytes`, no `PathLike`, no normalization) |
| MSG-05 | adjacency | resolved | explicit | 60-04 `must_haves` + § "Edge reachability" (the two structurally independent checks report both failures, each independently quoted) |
| MSG-05 | empty | resolved | explicit | 60-04 `must_haves` + § "Edge reachability" (the `elif template:` guard makes an empty value structurally unreachable at both routed sites) |
| MSG-05 | encoding | resolved | explicit | 60-04 `must_haves` + `TestRegistryPathLikeTemplateNoClassWrapper` / `TestRegistryTypeCheckMessageStaysReprQuoted` |
| MSG-05 | ordering | resolved | explicit | 60-04 `must_haves` (accumulated failures order and the joiner unchanged) |

**Accounting:** 15 surfaced = 14 authored explicit + 1 authored backstop + 0 unresolved + 0 dismissed
+ 0 dropped.

## Prohibition recall accounting (descriptor-less, never auto-dismissed)

Stage 1 over-produced; Stage 2 dropped routine-engineering items ("must not mutate its input", "must
not raise on an empty value", "must not leak a file handle") as edge-probe or code-review territory.
Kept, bespoke, values/safety items are authored into `must_haves.prohibitions` across plans 01–05,
descriptor-less (no `check_*` scalar), so each disposes flagged-unverified:

- `quote_path()` must not be presented or used as a sanitizer (shell, Typst literal, filesystem).
- `quote_path()` must not drop, truncate, elide or transform the value's own characters — a reader
  must still be able to copy the path out of the message.
- The rollout must not reduce diagnostic fidelity for non-path values (the `template_registry.py`
  type-check exclusion; the `builder.py` non-`str` target narrowing).
- This phase must not silently grow into a general Windows-path sanitizer (reserved characters,
  device names, length bounds are all explicitly out of scope for v0.9.1).

**Canon-referral drops (breadcrumbs, not minted):** log-forging / terminal-escape sanitization of an
attacker-controlled filename crossing into terminal output is canon security — covered by
`/gsd-secure-phase`; not minted here. `60-RESEARCH.md` § "Security Domain" independently records that
this phase introduces no new attack surface relative to the status quo it replaces.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local worktree → `origin` remote (branch push + workflow dispatch) | the only outbound action in this phase; it publishes commits and triggers CI |
| recorded evidence → phase acceptance decision | the evidence files are what SC#1–SC#5 are judged on, so a fabricated, stale or copied entry is the failure mode this plan guards against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-13 | Repudiation | evidence measured against the wrong tree, or a stale CI run cited as this phase's acceptance (ASVS V7 Logging, L1) | medium | mitigate | Every diff is taken against `PHASE_BASE_SHA` read from `60-01-EVIDENCE.md`; the CI section records the dispatched head SHA alongside the local tip SHA so the two can be compared; a dispatch that cannot be completed is marked `PENDING` with reproducible commands rather than filled with an older run. |
| T-60-14 | Repudiation | a skipped gate recorded as a pass, hiding an unprovisioned worktree venv (ASVS V7, L1) | medium | mitigate | Task 2's per-module skip census requires an explicit `0 skipped` for all four new gate modules, and the plan records that every one of them is a pure string / `caplog` / `ExtensionError` assertion needing no Windows host and no compiler, so a skip can only mean the venv is wrong. |
| T-60-15 | Tampering | a per-plan evidence file rewritten during consolidation, destroying another plan's recorded RED (ASVS V7, L1) | medium | mitigate | D-10's read-only consolidation rule, an explicit `git status --porcelain` emptiness check over the four per-plan files in both the verify block and the acceptance criteria, and a prohibition on pasting transcripts into the consolidated file. |
| T-60-16 | Elevation of Privilege | scope widening under cover of an audit — fixing a fourth-module hit found by the repo-wide grep (ASVS V5, L1) | low | mitigate | The fourth-module rule: classify and FILE as a new requirement under `.planning/todos/pending/`, never fix here; enforced by an acceptance criterion that no product file outside the three in-scope modules was edited. |
| T-60-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan; `60-RESEARCH.md` § "Package Legitimacy Audit" records the phase as stdlib-only with zero new packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `60-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. This plan creates
no new symbol and no new test module; it creates
`.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-EVIDENCE.md` and
`.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-PATH-QUOTING-EVIDENCE.md`,
and — only if SC#2's repo-wide grep surfaces a genuinely path-valued site in a fourth module — one new
todo record under `.planning/todos/pending/`. **No file named `60-VERIFICATION.md` is produced or
referenced as an output.**
</artifacts_this_phase_produces>

<verification>
- `60-05-EVIDENCE.md` holds the phase tip SHA, SC#2's four repo-wide grep outputs plus their
  classification table, SC#3's measured over-reach commands and outputs, SC#5's zero-test-edit
  measurement, the final local gate with its skip census, the phase-wide RED-first ledger, and the
  3-OS CI dispatch record
- `60-PATH-QUOTING-EVIDENCE.md` references all four per-plan evidence files without modifying any of
  them, and contains no pasted transcript
- the routed-value negative grep over the whole `typsphinx/` package prints nothing
- exactly one `template` repr conversion survives in `typsphinx/template_registry.py`
- `uv run pytest -q`, `uv run black --check .`, `uv run mypy typsphinx/` and
  `uv run pytest tests/test_repr_census_guard.py -q` are all green on the phase tip
- `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/` shows only added entries plus the one
  pure-addition modification to `tests/test_templates_path_collision_gate.py`
- no file named `60-VERIFICATION.md` exists under the phase directory
</verification>

<success_criteria>
1. An execution-time, repo-wide grep — not any line list written before it — shows no path-valued
   interpolation left in `typsphinx/builder.py`, `typsphinx/writer.py` or
   `typsphinx/template_registry.py`, with every hit classified under D-05's role rule.
2. The rollout is proven not to have over-reached: identifier-valued interpolations still render
   through Python's repr conversion, `template_registry.py`'s type-check message is measurably still
   excluded, and its two guarding assertions are green unmodified.
3. Zero existing test assertions were modified across the whole phase, measured against the phase base
   SHA and cross-checked against `58-REPR-CENSUS.md` and the live AST census guard.
4. Every one of MSG-02..MSG-05 has a recorded local RED that preceded its green, each locatable by
   file and section from `60-PATH-QUOTING-EVIDENCE.md`.
5. The 3-OS CI lane, `windows-latest` included, is dispatched fresh on this phase's own post-fix tip
   with per-job results recorded — or explicitly marked pending with reproducible commands.
</success_criteria>

<output>
Create `.planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/60-05-SUMMARY.md` when done.
</output>
