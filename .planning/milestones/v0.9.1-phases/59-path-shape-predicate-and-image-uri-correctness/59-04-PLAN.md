---
phase: 59-path-shape-predicate-and-image-uri-correctness
plan: 04
type: execute
wave: 4
depends_on: ["59-03"]
files_modified:
  - tests/fixtures/windows_shaped_image_uri_gate/conf.py
  - tests/fixtures/windows_shaped_image_uri_gate/index.rst
  - tests/fixtures/windows_shaped_image_uri_gate/_static/converted_stand_in.png
  - tests/test_windows_image_uri_render_gate.py
  - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md
autonomous: true
requirements: [IMG-07]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'For a Windows-shaped absolute image URI, the emitted image("...") literal contains no backslash that is not the start of an escaped-quote sequence, and it contains an escaped double quote -- both asserted in one all-lane -b typst gate that needs no filesystem support for illegal filenames.'
    - 'A real typst.compile() driven by sphinx-build -b typstpdf produces a non-empty PDF starting with the %PDF magic bytes for a fixture whose raw image basename is sub\we"ird.png and whose normalized basename is we"ird.png.'
    - 'The copied destination image exists under outdir/_typst_converted/ with a normalized basename BEFORE the compile result is asserted, so a green compile can never be confused with a skipped copy.'
    - 'The compile gate skips only via a runtime tmp_path filesystem probe inside the test body, with the probe OSError recorded in the skip reason -- never via os.name and never via a collection-time skipif decorator.'
    - 'The compile-gate class is guarded by the TYPST_AVAILABLE import boolean, and a skipped result is recorded as a provisioning failure rather than a pass.'
  artifacts:
    - tests/fixtures/windows_shaped_image_uri_gate/conf.py
    - tests/fixtures/windows_shaped_image_uri_gate/index.rst
    - tests/fixtures/windows_shaped_image_uri_gate/_static/converted_stand_in.png
    - tests/test_windows_image_uri_render_gate.py
  key_links:
    - 'fixture post-transform node["uri"] -> _track_image() escape branch -> _build_relocation_key(): the fixture is what routes a Windows-shaped absolute URI into the key construction plan 02 rewrote.'
    - 'relocation key -> copy_image_files() -> outdir/_typst_converted/{digest}-we"ird.png: the copied file must exist for the compile to mean anything.'
    - 'node["uri"] -> visit_image() -> escape_typst_string() -> emitted image("...") literal -> typst.compile(): the full coupled path IMG-07 gates.'
  prohibitions:
    - 'The compile gate fixture URI is not backslash-only; its normalized basename carries a double quote, so IMG-04 alone cannot make the gate green.'
    - 'The compile gate does not branch on os.name; the skip decision is a measured tmp_path create wrapped in except OSError inside the test body.'
    - 'No skipif decorator references a fixture-scoped value such as tmp_path.'
    - 'No file whose name contains a backslash or a double quote is committed to the repository; such files are created only at build time under a temporary directory.'
    - 'No existing test assertion anywhere under tests/ is modified; only new test files and a new fixture directory are added.'
    - 'Neither typsphinx/builder.py nor typsphinx/translator.py is edited by this plan.'
---

<objective>
Close IMG-07: one gate in this milestone is a real `typst.compile()` proving a Windows-shaped
absolute image URI now compiles, plus D-04's POSIX-and-Windows-runnable string-shape sibling so the
`windows-latest` lane is not left uncovered by the compile gate's filesystem skip.

Purpose: ROADMAP constraint 5 makes this gate coupled to BOTH halves — Typst refuses a backslash in
an `image()` path by value, not by syntax — so it belongs in the wave after plans 02 and 03, not
beside them. ROADMAP constraint 4 independently forbids it from sharing a wave with either plan whose
emitted string it asserts on.

**Where this plan's RED comes from.** Because the roadmap forces this gate into the wave *after* both
fixes, a same-tree pre-fix RED is physically impossible for it. Its RED is recorded in plan 05 as a
**two-tree measurement** using the same committed gate — `git checkout $PHASE_BASE_SHA -- <product
file(s)>` reconstructs each of D-01's four tree combinations and re-runs this exact test. That is
strictly stronger than a one-shot pre-fix run: it also proves SC#2's "neither alone would have closed
it". This plan authors the gate; plan 05 records its RED and GREEN.

Output: the new fixture project `tests/fixtures/windows_shaped_image_uri_gate/` and
`tests/test_windows_image_uri_render_gate.py` carrying both gates.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-PATTERNS.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-VALIDATION.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-03-SUMMARY.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 11). Before running ANY command in this plan, provision the worktree's own venv:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. `typst` is a CORE dependency
(`pyproject.toml`), so a correctly-provisioned worktree venv has `TYPST_AVAILABLE` true — a
`skipped` line on the compile gate means the venv is wrong, not that the gate passed
(`59-CONTEXT.md` § Specific Ideas #4).
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Build the two-mode Windows-shaped image URI fixture project</name>
  <files>tests/fixtures/windows_shaped_image_uri_gate/conf.py, tests/fixtures/windows_shaped_image_uri_gate/index.rst, tests/fixtures/windows_shaped_image_uri_gate/_static/converted_stand_in.png</files>
  <read_first>
    - tests/fixtures/absolute_image_render_gate/conf.py (the full template: `FakeImageConverter(SphinxTransform)` with `default_priority = 200`, `apply()` rewriting `node["uri"]` and its `"*"` candidate, `shutil.copyfile` from a `_static/` stand-in PNG, `app.add_post_transform(...)` in `setup()`, and the `typst_documents = [("index", "master.typ", ...)]` de-collision comment)
    - tests/fixtures/absolute_image_render_gate/index.rst (the figure directive shape)
    - tests/fixtures/out02_escape_target_gate/conf.py (the established environment-variable-driven fixture-mode pattern this project already uses, read via `TYPSPHINX_ESCAPE_SHAPE`)
    - typsphinx/builder.py lines 1637-1790 (`_track_image` — why the rewritten URI must be absolute AND land outside `doctreedir` for the escape branch to fire)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md § D-01, D-02, D-03 and § Specific Ideas #1 and #3
  </read_first>
  <action>
    Create `tests/fixtures/windows_shaped_image_uri_gate/` modelled on
    `tests/fixtures/absolute_image_render_gate/`. Copy `_static/converted_stand_in.png` from that
    fixture verbatim (a valid 1x1 PNG, so the emitted `image()` call points at bytes Typst can
    decode). Write `index.rst` with a short prose body and a single `.. image:: _static/converted_stand_in.png`
    directive. Write `conf.py` with `project`, `author = "Test Author"`, `release`,
    `extensions = ["typsphinx"]`, `html_static_path = ["_static"]`, and
    `typst_documents = [("index", "master.typ", "Windows Shaped Image URI Gate", "Test Author")]` —
    the target stem must differ from the docname `"index"` to avoid the self-collision the analog's
    own comment explains.

    Define a `SphinxTransform` subclass named `WindowsShapedImageUriTransform` with
    `default_priority = 200`, registered via `app.add_post_transform(...)` in `setup(app)`. Its
    `apply()` reads the environment variable `TYPSPHINX_WIN_URI_MODE` (default `"string"`) and
    rewrites every image node's `uri` and, when a `candidates` key is present, its `"*"` candidate:

    In `"string"` mode it sets the URI to the hand-built Windows-shaped absolute string literal
    consisting of the drive prefix `C:`, backslash-separated components `Users`, `runner`, `assets`,
    `sub`, and the final component `we"ird.png` — one literal backslash per separator and one literal
    double quote in the basename. **No file is created in this mode.** That is deliberate: a double
    quote and a backslash are both illegal in an NTFS filename, so creating one would make the gate
    unrunnable on `windows-latest`, and `_track_image()` decides purely from the path shape — the file
    need not exist. `copy_image_files()` will log `Image file not found` and the build continues; that
    warning is expected in this mode.

    In `"file"` mode it creates a directory that is a SIBLING of `self.env.doctreedir` (append a
    distinguishing suffix to the doctreedir path) so the rewritten URI genuinely lands outside
    `doctreedir` and `path.relpath()` therefore carries a `..` segment into `_escapes_outdir()`.
    Inside it, `shutil.copyfile` the `_static/converted_stand_in.png` stand-in to a file whose
    basename is the single component `sub\we"ird.png` — one literal backslash and one literal double
    quote — and set the node URI to that file's absolute path. Per `59-CONTEXT.md` § Specific Ideas
    #3, the file must genuinely exist: `copy_image_files()` copies from the raw `resolved_uri` and
    skips a missing source with a warning, and a compile that then failed for "file not found" would
    be indistinguishable from a fixture bug.

    Record D-01's four-combination table in the `conf.py` module docstring so the reason for this
    exact basename shape travels with the fixture: raw basename `sub\we"ird.png`, normalized basename
    `we"ird.png`; unfixed tree emits a raw backslash and Typst refuses with "path must not contain a
    backslash"; key-normalization alone leaves a raw double quote and Typst refuses with "unclosed
    delimiter"; escaping alone leaves an escaped backslash which Typst still refuses BY VALUE; only
    both together compile. Note explicitly that a backslash-only fixture would be green with the key
    normalization alone and could therefore not prove SC#2.

    Commit no file whose own name contains a backslash or a double quote — those exist only at build
    time under the temporary build directory, never in the repository.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; test -f tests/fixtures/windows_shaped_image_uri_gate/conf.py &amp;&amp; test -f tests/fixtures/windows_shaped_image_uri_gate/index.rst &amp;&amp; test -s tests/fixtures/windows_shaped_image_uri_gate/_static/converted_stand_in.png &amp;&amp; rm -rf /tmp/win_uri_gate_smoke &amp;&amp; uv run python -m sphinx -b typst tests/fixtures/windows_shaped_image_uri_gate /tmp/win_uri_gate_smoke &amp;&amp; grep -q 'image(' /tmp/win_uri_gate_smoke/index.typ &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/windows_shaped_image_uri_gate/conf.py`, `index.rst` and `_static/converted_stand_in.png` all exist, and the PNG is byte-identical to `tests/fixtures/absolute_image_render_gate/_static/converted_stand_in.png`
    - `grep -c 'TYPSPHINX_WIN_URI_MODE' tests/fixtures/windows_shaped_image_uri_gate/conf.py` is at least 1
    - `grep -c 'add_post_transform' tests/fixtures/windows_shaped_image_uri_gate/conf.py` is 1
    - `grep -c 'master.typ' tests/fixtures/windows_shaped_image_uri_gate/conf.py` is at least 1
    - `git ls-files tests/fixtures/windows_shaped_image_uri_gate/ | grep -c '"'` is 0 and the same listing contains no path with a backslash
    - a `sphinx-build -b typst` run of the fixture in default (`string`) mode exits 0 and emits `index.typ` containing an `image(` call
    - `uv run pytest -q` exits 0 (adding a fixture project changes no existing test)
  </acceptance_criteria>
  <done>The fixture project exists in both modes, its default-mode build exits 0 and emits an `image(` call, and nothing with an illegal filename is committed.</done>
  <reversibility rating="reversible">A new fixture directory; deletable with no effect on any other test.</reversibility>
</task>

<task type="auto">
  <name>Task 2: All-lane -b typst string-shape gate on the emitted image() literal</name>
  <files>tests/test_windows_image_uri_render_gate.py</files>
  <read_first>
    - tests/test_templates_path_collision_gate.py lines 411-470 (`TestWindowsPathEscapingRegressionGuard` — the proven all-lane Windows-shaped-string pattern, including its docstring rule: call the ACTUAL product function, never a re-pasted f-string, and its `_assert_no_doubled_separator` regex helper)
    - tests/test_out02_escape_target_gate.py lines 40-95 (`_run_sphinx_build(source_dir, build_dir, builder, env)` — the richer helper with a parametrized builder argument and an `env` mapping MERGED on top of `os.environ`, which one module can use for both `-b typst` and `-b typstpdf`)
    - tests/fixtures/windows_shaped_image_uri_gate/conf.py (written in task 1)
    - typsphinx/translator.py lines 156-185 (`escape_typst_string()` — the real helper the expected fragments must be built from)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md § D-04
  </read_first>
  <action>
    Create `tests/test_windows_image_uri_render_gate.py` with a module docstring stating what each of
    its two classes proves and why they are separate: the string-shape gate runs on EVERY lane
    (including `windows-latest`) and needs no filesystem support for illegal filenames, while the
    compile gate can only run where the filesystem can hold a backslash-and-quote filename and
    therefore skips via a measured runtime probe. Carry its own local copy of the
    `_run_sphinx_build(source_dir, build_dir, builder, env=None)` helper in the shape
    `tests/test_out02_escape_target_gate.py` uses — `sys.executable -m sphinx`, `capture_output=True`,
    `text=True`, and an `env` mapping merged on top of a copy of `os.environ` so the subprocess still
    inherits `PATH` and the active venv. Do not import a sibling module's helper; every gate module in
    this suite carries its own.

    Add the class `TestWindowsShapedImageUriStringShape` with one test named
    `test_string_shape_emitted_image_literal_is_escaped_and_separator_free`. It runs the fixture
    through `-b typst` with `env={"TYPSPHINX_WIN_URI_MODE": "string"}`, asserts `returncode == 0`,
    reads the emitted content document `index.typ`, and extracts the contents of the first
    `image("...")` literal with a regex that honours backslash escapes inside the literal (a
    character class of "not a quote and not a backslash" alternated with "backslash followed by any
    character"), so a `\"` inside the path does not terminate the match early.

    Assert three properties of the extracted literal, each with an assertion message that echoes the
    literal verbatim so the transcript is self-describing:
    (1) every backslash inside it is immediately followed by a double quote — i.e. a search for a
    backslash NOT followed by a double quote finds nothing. This fails on the unfixed tree (raw
    separator backslashes) and also fails when only the escaping half is present (the separator
    backslash becomes a doubled backslash).
    (2) it contains an escaped double quote. This fails when only the key-normalization half is
    present (the quote is still raw).
    (3) it contains neither the drive fragment `C:` nor the fragment `Users`, proving the whole raw
    URI did not survive as the "basename".
    Build the expected escaped fragment by calling the real `escape_typst_string()` imported from
    `typsphinx.translator` on the normalized basename, never by pasting an escaped literal into this
    module — the `TestWindowsPathEscapingRegressionGuard` discipline: a re-pasted string keeps passing
    after a product regression.

    Add no `TYPST_AVAILABLE` guard and no skip of any kind to this class — it never calls
    `typst.compile()` and never creates a file with an illegal name, so it must run unconditionally on
    all three OS lanes. That is precisely what makes this phase's Windows-lane claim mean something
    despite the compile gate's probe-skip.
  </action>
  <verify>
    <automated>uv run pytest tests/test_windows_image_uri_render_gate.py -k string_shape -q &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_windows_image_uri_render_gate.py -k string_shape -q` exits 0 with 1 passed and 0 skipped
    - `grep -c 'skipif' tests/test_windows_image_uri_render_gate.py` counts only occurrences attached to the compile-gate class (0 at this point in the plan)
    - `grep -c 'escape_typst_string' tests/test_windows_image_uri_render_gate.py` is at least 1
    - `grep -c 'TYPSPHINX_WIN_URI_MODE' tests/test_windows_image_uri_render_gate.py` is at least 1
    - `uv run pytest -q` exits 0 and `uv run black --check .` exits 0
  </acceptance_criteria>
  <done>The all-lane string-shape gate passes with 0 skips and asserts all three properties on a literal extracted from a real `-b typst` build.</done>
  <reversibility rating="reversible">One new test module.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Real typst.compile() gate with a runtime filesystem probe-skip</name>
  <files>tests/test_windows_image_uri_render_gate.py, .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</files>
  <read_first>
    - tests/test_absolute_image_render_gate.py (the whole file: the `TYPST_AVAILABLE` import guard, `_run_sphinx_build_typstpdf`, the class-level `skipif`, and the assertion tail — returncode, absence of compile-fatal substrings in stderr, the emitted `.typ` path assertion, the copied-asset `.exists()` assertion, and the `master.pdf` non-empty + `%PDF` magic-byte check)
    - tests/fixtures/windows_shaped_image_uri_gate/conf.py (written in task 1 — the `"file"` mode branch)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-RESEARCH.md § "Common Pitfalls" Pitfall 1 (a `skipif` decorator is evaluated at collection time, before `tmp_path` exists) and § "Code Examples" → "D-03 filesystem probe idiom"
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md § D-02, D-03 and § Specific Ideas #3, #4, #5
  </read_first>
  <action>
    Add to `tests/test_windows_image_uri_render_gate.py` a module-level `TYPST_AVAILABLE` boolean set
    by a guarded `import typst` in a `try`/`except ImportError`, and a class
    `TestWindowsShapedImageUriCompileGate` decorated with
    `pytest.mark.skipif(not TYPST_AVAILABLE, ...)`. That is the ONLY permitted skip decorator here:
    it reads a module-level, import-time boolean, which is exactly what `skipif` is designed for.

    Add one test named `test_compile_windows_shaped_absolute_image_uri_produces_pdf`, taking `tmp_path`.
    Its FIRST statements are D-03's measured probe: attempt to create a file under `tmp_path` whose
    basename contains both a backslash and a double quote, wrapped in `except OSError as e:
    pytest.skip(...)` whose reason includes the caught error text. This must be inside the test body,
    not a decorator, because decorators are evaluated at collection time before `tmp_path` is
    instantiated. Do not branch on `os.name` anywhere: that asserts a belief about the platform where
    a two-line probe measures the actual constraint.

    Then run the fixture through `-b typstpdf` with `env={"TYPSPHINX_WIN_URI_MODE": "file"}` — the
    `typstpdf` builder specifically, because a compile fatal only aborts on
    `TypstPDFBuilder.finish()`'s compile path. Assert in this order:

    1. `result.returncode == 0`, with stdout and stderr echoed in the assertion message.
    2. The copied destination image EXISTS: exactly one file under `<build>/_typst_converted/` whose
       name ends with the normalized basename `we"ird.png`, found by globbing the directory. Assert
       this BEFORE any compile assertion — per `59-CONTEXT.md` § Specific Ideas #3, a green compile is
       not evidence unless the source file was actually copied; otherwise a "file not found" failure
       would be chasing the fixture rather than the defect.
    3. `Image file not found` does not appear in the captured output (the `"file"` mode fixture creates
       a real file, so this warning would mean the fixture, not the product, is broken).
    4. The Typst refusal texts `path must not contain a backslash` and `unclosed delimiter` do not
       appear in the captured output, and neither does `Typst compilation failed` — a fatal inside
       `finish()` is logged, not raised, so returncode alone is not enough.
    5. The emitted `index.typ` contains an `image("...")` literal whose extracted contents pass the
       same escaped-and-separator-free assertions task 2 defined; factor those two assertions into one
       module-level helper both classes call, so the string-level claim and the compile-level claim
       cannot drift apart.
    6. `master.pdf` exists, is non-empty, and its first four bytes are the `%PDF` magic bytes.

    Then append to `59-WINDOWS-URI-EVIDENCE.md` under `## IMG-07 four-combination table` a
    `### GREEN (post-fix, both halves present)` subsection carrying the verbatim passing pytest output
    for `-k compile`, the measured emitted literal, and the resolved name of the copied destination
    file. Record explicitly that the run reported `0 skipped` — a `skipped` line means the worktree
    venv lacks `typst` or the filesystem rejected the probe, and either reads as a pass in a summary
    line while proving nothing (`59-CONTEXT.md` § Specific Ideas #4). Leave the RED half of this
    section empty with a one-line pointer to plan 05, which reconstructs all four tree combinations.
  </action>
  <verify>
    <automated>uv run pytest tests/test_windows_image_uri_render_gate.py -q 2>&amp;1 | tee /dev/stderr | grep -qE '2 passed' &amp;&amp; uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; grep -q 'GREEN (post-fix, both halves present)' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_windows_image_uri_render_gate.py -q` reports 2 passed and 0 skipped on this dev machine
    - `uv run pytest tests/test_windows_image_uri_render_gate.py -k compile -q` exits 0 with 0 skipped
    - `grep -c 'pytest.skip' tests/test_windows_image_uri_render_gate.py` is at least 1 (the in-body probe) and `grep -c 'os.name' tests/test_windows_image_uri_render_gate.py` is 0
    - `grep -c 'TYPST_AVAILABLE' tests/test_windows_image_uri_render_gate.py` is at least 2 (the definition and the class decorator)
    - the compile test asserts the copied destination file exists at a source line ABOVE the line asserting the PDF magic bytes
    - `59-WINDOWS-URI-EVIDENCE.md` § "IMG-07 four-combination table" contains a GREEN subsection recording `0 skipped`
    - `uv run pytest -q` exits 0 and `uv run black --check .` exits 0
  </acceptance_criteria>
  <done>A real `typst.compile()` produces a `%PDF`-magic PDF for the Windows-shaped fixture, the copied asset is asserted present first, the skip is a measured runtime probe, and the GREEN transcript with `0 skipped` is recorded.</done>
  <reversibility rating="reversible">Test-only additions plus an evidence append.</reversibility>
</task>

</tasks>

<flagged_assumptions>
**IMG-07 / `unclassified` — UNRESOLVED, surfaced not dropped.** The deterministic edge probe returned
`{"requirement_id":"IMG-07","category":"unclassified","status":"unresolved","probe":"unclassified —
review manually"}`. Reviewed manually: IMG-07's requirement text is an existence obligation ("at
least one gate in this milestone is a real `typst.compile()`"), which has no natural
empty/boundary/precision edge to enumerate. The nearest genuine edge is *coverage of the lane on
which the gate cannot run* — `windows-latest`, where the fixture's filename is illegal. **That edge
is addressed, but by D-04 rather than by a criterion minted against IMG-07**: task 2's all-lane
string-shape gate runs unconditionally on every lane and asserts the same two literal properties the
compile gate asserts, through one shared helper. No further criterion is minted for this row, and it
stays `unresolved` rather than being auto-resolved with a backstop.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party post-transform `node["uri"]` → builder key → `copy_image_files()` write | the fixture drives a real, arbitrarily-shaped absolute path through the whole write path |
| emitted `.typ` string literal → `typst.compile()` | Typst's own parser and path resolver are the enforcement point this gate observes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-03 | Tampering | emitted `image("...")` literal reaching `typst.compile()` (ASVS V5 Input Validation) | medium | mitigate | This plan is the verification of that mitigation, not a new one: a real compile plus an all-lane string-shape assertion prove the escaped, separator-free literal Typst accepts. |
| T-59-01 | Tampering | `copy_image_files()` destination under `outdir` (ASVS V12 File and Resources) | medium | mitigate | The compile gate asserts the copied asset lands under `<build>/_typst_converted/`, inside `outdir` — an incidental containment observation over the same write sink `_escapes_outdir()` guards. |
| T-59-06 | Denial of Service | a silently skipped gate reported as a pass | low | mitigate | The compile gate's skip is a measured runtime probe with the error text in its reason, the class is `TYPST_AVAILABLE`-guarded, and the acceptance criteria require an explicit `0 skipped` in the recorded transcript. |
| T-59-SC | Tampering | package-manager installs (npm/pip/cargo) | low | accept | No package-manager install task exists in this plan; `59-RESEARCH.md` § "Package Legitimacy Audit" records the phase as stdlib-only with zero new packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `59-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. Newly created by
THIS plan: the fixture project `tests/fixtures/windows_shaped_image_uri_gate/` (`conf.py` with the
transform class `WindowsShapedImageUriTransform` and the `TYPSPHINX_WIN_URI_MODE` environment switch,
`index.rst`, `_static/converted_stand_in.png`) and `tests/test_windows_image_uri_render_gate.py` with
the classes `TestWindowsShapedImageUriStringShape` and `TestWindowsShapedImageUriCompileGate`.
</artifacts_this_phase_produces>

<verification>
- `uv run pytest tests/test_windows_image_uri_render_gate.py -q` — 2 passed, 0 skipped on this dev machine
- `uv run pytest -q` — full suite green
- `uv run black --check .` and `uv run mypy typsphinx/` — clean
- `git ls-files tests/fixtures/windows_shaped_image_uri_gate/` — no committed filename contains a backslash or a double quote
- `git diff --stat <PHASE_BASE_SHA>..HEAD -- tests/` shows added files only
- `59-WINDOWS-URI-EVIDENCE.md` § "IMG-07 four-combination table" carries the GREEN transcript with an explicit `0 skipped`
</verification>

<success_criteria>
1. A real `typst.compile()` driven through `sphinx-build -b typstpdf` produces a non-empty
   `%PDF`-magic PDF for a fixture whose raw image basename is `sub\we"ird.png`.
2. The copied destination image is asserted present before the compile result is asserted.
3. The compile gate skips only via an in-body `tmp_path` probe with the `OSError` text in its reason;
   nothing branches on `os.name`.
4. The all-lane `-b typst` string-shape gate passes unconditionally on every lane and asserts, via a
   helper shared with the compile gate, that the emitted literal is escaped and separator-free.
5. Zero existing test assertions modified; no filename containing a backslash or a double quote is
   committed to the repository.
</success_criteria>

<output>
Create `.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-04-SUMMARY.md` when done.
</output>
