---
phase: 59-path-shape-predicate-and-image-uri-correctness
plan: 05
type: execute
wave: 5
depends_on: ["59-04"]
files_modified:
  - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md
autonomous: true
requirements: [IMG-04, IMG-05, IMG-07]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The unfixed tree fails the compile gate with Typst's own words `path must not contain a backslash`, quoted verbatim in 59-WINDOWS-URI-EVIDENCE.md."
    - 'The key-normalization half alone fails the compile gate with Typst’s `unclosed delimiter` refusal, and the escaping half alone fails with the same backslash refusal as the unfixed tree -- so neither alone would have closed it (SC#2).'
    - 'Only the tree carrying both halves compiles, completing D-01’s four-combination table with four recorded, verbatim transcripts.'
    - 'git diff --name-status between the phase base SHA and the phase tip, restricted to tests/, lists only added entries -- no modified and no deleted test file anywhere under tests/.'
    - 'No test module enumerated in 58-REPR-CENSUS.md appears in this phase’s tests/ diff.'
    - 'The full pytest suite, black --check and mypy typsphinx/ are all green on the phase tip, with zero skipped in the five new gate modules on this dev machine.'
    - 'The 3-OS CI lane including windows-latest is dispatched fresh on this phase’s own post-fix tip, and its run URL plus per-OS job results are recorded in the evidence file.'
    - 'The working tree is clean of the temporary git checkouts used for the four-combination measurement before the plan commits.'
  artifacts:
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md
  key_links:
    - 'PHASE_BASE_SHA (recorded by plan 01) -> git checkout of typsphinx/builder.py and typsphinx/translator.py: the mechanism that reconstructs each of the four tree combinations without a second worktree or a second venv.'
    - 'tests/test_windows_image_uri_render_gate.py (plan 04) -> all four combinations: the SAME committed gate is re-run against each tree, so the four transcripts are comparable.'
    - 'phase tip -> GitHub Actions ci.yml workflow_dispatch: the acceptance bar is that lane green on this phase own tip, dispatched fresh.'
  prohibitions:
    - 'The evidence file is never named 59-VERIFICATION.md -- that name is reserved by gsd-verifier and overwritten wholesale.'
    - 'A skipped test result is never recorded as a pass; a skip on the compile gate means the venv or the filesystem probe failed.'
    - 'The full suite is not run against a partially-reverted tree; only tests/test_windows_image_uri_render_gate.py is run for the four combinations.'
    - 'CI is not the first discovery: the local RED-then-green record is complete before the 3-OS lane is dispatched.'
    - 'No existing test assertion anywhere under tests/ is modified, and no product file is edited by this plan.'
    - 'The temporary git checkouts are restored before committing; no reverted product file is committed.'
---

<objective>
Close the phase's acceptance obligations: record D-01's four-combination RED/GREEN table that proves
SC#2's "neither alone would have closed it", measure SC#5's zero-test-edit claim against Phase 58's
census rather than claiming it, and dispatch the 3-OS CI lane fresh on this phase's own post-fix tip.

Purpose: ROADMAP constraint 5 forces IMG-07's gate into the wave after both fixes, so its RED cannot
be a same-tree pre-fix run. This plan supplies the stronger substitute — the same committed gate
re-run against four reconstructed trees — using the two-tree device D-09 already established for
PATH-01 and 57-11 / 58 D-05(b) established for this project. It also finalizes the evidence file that
ROADMAP constraint 1 makes the phase's actual deliverable alongside the code.

Output: a complete `59-WINDOWS-URI-EVIDENCE.md`. No product file and no test file is edited.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-VALIDATION.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-04-SUMMARY.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 11). Before running ANY command in this plan, provision the worktree's own venv:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Do not degrade to main-tree execution.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Reconstruct all four trees and record D-01's four-combination table</name>
  <files>.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md (the `PHASE_BASE_SHA` recorded by plan 01 and the existing PATH-01 / IMG-04 / IMG-06 / IMG-05 sections)
    - tests/test_windows_image_uri_render_gate.py (the gate this task re-runs against four trees)
    - tests/fixtures/windows_shaped_image_uri_gate/conf.py (the `TYPSPHINX_WIN_URI_MODE` switch)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md § D-01 (the four-combination table), D-05 (why a backslash-only fixture would make SC#2 unprovable) and § Specific Ideas #5
    - .planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md (the two-tree evidence shape this follows)
  </read_first>
  <action>
    Read `PHASE_BASE_SHA` from the top of `59-WINDOWS-URI-EVIDENCE.md`. Confirm
    `git status --porcelain` is clean before starting; if it is not, HALT.

    Reconstruct each of D-01's four trees in turn by checking out the pre-fix version of zero, one, or
    both product files from the phase base, running the measurement, and restoring immediately. The
    four combinations and their expected outcomes are:

    (A) **unfixed** — `git checkout $PHASE_BASE_SHA -- typsphinx/builder.py typsphinx/translator.py`;
    expected: Typst refuses with `path must not contain a backslash`.
    (B) **key normalization only** — restore both to `HEAD`, then
    `git checkout $PHASE_BASE_SHA -- typsphinx/translator.py`; expected: Typst refuses with
    `unclosed delimiter`.
    (C) **escaping only** — restore both to `HEAD`, then
    `git checkout $PHASE_BASE_SHA -- typsphinx/builder.py`; expected: Typst refuses with
    `path must not contain a backslash` again, because it refuses a backslash BY VALUE and an escaped
    backslash still decodes to one.
    (D) **both** — restore both to `HEAD`; expected: the document compiles.

    For each combination take TWO measurements. First run
    `uv run pytest tests/test_windows_image_uri_render_gate.py -q` and capture its verbatim output.
    Run ONLY that module — the other new gate modules import symbols that do not exist on a reverted
    `builder.py` and would produce collection errors that say nothing about IMG-07. Second, run the
    fixture directly with `uv run python -m sphinx -b typstpdf tests/fixtures/windows_shaped_image_uri_gate
    <a fresh temp dir>` with `TYPSPHINX_WIN_URI_MODE=file` in the environment, and capture stderr
    verbatim — this guarantees Typst's own refusal text lands in the evidence rather than only a
    pytest assertion summary (SC#2 names that text explicitly, and `59-CONTEXT.md` § Specific Ideas #5
    requires the exact words, not a paraphrase). Also record, for each combination, the emitted
    `image("...")` literal read out of that build's `index.typ`.

    After EVERY combination run `git checkout HEAD -- typsphinx/builder.py typsphinx/translator.py`
    and confirm `git status --porcelain typsphinx/` is empty before moving on.

    Write the results into `59-WINDOWS-URI-EVIDENCE.md` under `## IMG-07 four-combination table`, as a
    summary table with the columns tree / emitted literal / Typst outcome, followed by the four
    verbatim transcripts under `### (A) unfixed`, `### (B) key normalization only`,
    `### (C) escaping only`, `### (D) both`. Directly under the table, state the SC#2 conclusion in
    one sentence: neither half alone produced a compiling document, so the two are genuinely coupled
    on this fixture. If any combination's measured outcome differs from the expectation above, do NOT
    edit the expectation to match — record the measurement, mark the row DIVERGENT, and HALT for the
    owner, because a divergence means either the fixture is not exercising the escape branch or
    `59-CONTEXT.md`'s measured table is wrong.

    Finally confirm `git status --porcelain` shows only the evidence file as modified, and commit.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; test -z "$(git status --porcelain typsphinx/)" &amp;&amp; grep -q 'path must not contain a backslash' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md &amp;&amp; grep -q 'unclosed delimiter' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md &amp;&amp; uv run pytest tests/test_windows_image_uri_render_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `59-WINDOWS-URI-EVIDENCE.md` § "IMG-07 four-combination table" contains four subsections labelled (A) unfixed, (B) key normalization only, (C) escaping only, (D) both
    - the file contains the verbatim string `path must not contain a backslash` at least twice (combinations A and C) and `unclosed delimiter` at least once (combination B)
    - the file records the emitted `image("...")` literal for each of the four combinations
    - the file states in one sentence that neither half alone produced a compiling document
    - `git status --porcelain typsphinx/` is empty and `uv run pytest tests/test_windows_image_uri_render_gate.py -q` exits 0 with 0 skipped after the measurement
    - `git diff --name-only HEAD~1..HEAD` for this task's commit lists only the evidence file
  </acceptance_criteria>
  <done>All four trees are measured with the same committed gate, Typst's verbatim refusal texts are recorded for the two failing-by-backslash trees and the unclosed-delimiter tree, and the working tree is restored.</done>
  <reversibility rating="reversible">Evidence-file appends only; the product checkouts are transient and restored.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Measure SC#5's zero-test-edit claim and record the final local gate</name>
  <files>.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/58-repr-format-decoupling-test-side-only/58-REPR-CENSUS.md (the enumeration the zero-test-edit claim is checked against — ROADMAP constraint 9: a plan finding it must edit a test is a signal the census was incomplete, not a licence)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md (the `PHASE_BASE_SHA`)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-VALIDATION.md § "Manual-Only Verifications" (the zero-test-edit and RED-first rows this task closes)
  </read_first>
  <action>
    Run `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/` and record the verbatim output in
    `59-WINDOWS-URI-EVIDENCE.md` under `## SC#5 acceptance` → `### Zero test edits (measured)`. Every
    line must begin with `A` (added). A single `M` or `D` line is a halt condition, not something to
    explain away: cross-check that path against `58-REPR-CENSUS.md`, record which census row it
    corresponds to, and stop for the owner. Also run `git diff --numstat $PHASE_BASE_SHA..HEAD --
    tests/` and record it, so the added-lines-only shape is visible numerically as well as by status.

    Then record the phase's final local gate, each command with its verbatim tail:
    `uv run pytest -q` (full suite), `uv run black --check .`, and `uv run mypy typsphinx/`. Record
    `ruff check .` as DEFERRED TO CI with the one-line reason from `59-VALIDATION.md` — it is not
    runnable on this NixOS dev machine and CI is the lint authority for this project.

    Separately record a per-module skip census for the five new gate modules
    (`tests/test_path_shape_predicate_gate.py`, `tests/test_track_image_key_construction.py`,
    `tests/test_copy_image_files_name_too_long.py`, `tests/test_image_literal_escaping_gate.py`,
    `tests/test_windows_image_uri_render_gate.py`): run each with `-q` and record its passed/skipped
    counts. Every module must report `0 skipped` on this dev machine. A non-zero skip count is a
    provisioning or fixture failure, never a pass — `typst` is a core dependency, so a skipped compile
    gate means the worktree venv is wrong.

    Finally, add a `### RED-first ledger` subsection to `## SC#5 acceptance` listing all five
    requirements with, for each, the evidence section holding its recorded failure and one line naming
    the mechanism: PATH-01, IMG-04, IMG-06 and IMG-05 were recorded RED on the unfixed tree before
    their fix landed in the same plan; IMG-07's RED is the four-combination two-tree reconstruction in
    task 1, because ROADMAP constraint 5 forces its gate into the wave after both fixes and a
    same-tree pre-fix RED is therefore impossible for it.
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; grep -q 'Zero test edits (measured)' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md &amp;&amp; grep -q 'RED-first ledger' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md &amp;&amp; test -z "$(git diff --name-status $(grep -m1 -oE '[0-9a-f]{40}' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md)..HEAD -- tests/ | grep -v '^A')"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/` produces only lines beginning with `A`, and that verbatim output appears in the evidence file
    - the evidence file records the verbatim tails of `uv run pytest -q`, `uv run black --check .` and `uv run mypy typsphinx/`, all green
    - the evidence file records `ruff check .` as deferred to CI with its reason
    - the evidence file contains a per-module skip census showing `0 skipped` for all five new gate modules
    - the evidence file contains a `### RED-first ledger` naming all five requirement IDs and, for each, the section holding its recorded failure
    - `uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>SC#5's zero-test-edit claim is measured rather than asserted, the final local gate is green and recorded, and every requirement's RED has a named location in the evidence file.</done>
  <reversibility rating="reversible">Evidence-file appends only.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Dispatch the 3-OS CI lane fresh on the post-fix tip and record the result</name>
  <files>.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</files>
  <read_first>
    - .github/workflows/ci.yml (the `workflow_dispatch` trigger and the `os` / `python-version` test matrix whose `windows-latest` jobs are the acceptance bar)
    - .planning/ROADMAP.md § "🚧 v0.9.1 — Windows path correctness (ACTIVE)" constraints 10 and 12 (CI is not first discovery; the milestone branch is `gsd/v0.9.1-windows-path-correctness` and is already pushed by Phase 58)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md (the local RED-then-green record that must be complete first)
  </read_first>
  <action>
    Only after tasks 1 and 2 have recorded a complete local RED-then-green — ROADMAP constraint 10
    makes CI the final confirmation, never the first discovery — push the current branch to `origin`
    and dispatch the CI workflow fresh against this phase's own tip:
    `gh workflow run ci.yml --ref <this branch>`. Do not reuse or cite a prior run; the acceptance bar
    is a run whose head SHA is this phase's post-fix tip.

    Wait for the run to finish (`gh run list --workflow=ci.yml --branch <branch> --limit 1` then
    `gh run watch <run-id>`), then record in `59-WINDOWS-URI-EVIDENCE.md` under `## SC#5 acceptance` →
    `### 3-OS CI dispatch`: the run URL, the head SHA the run was dispatched against, the conclusion
    of every matrix job with its OS and Python version, and an explicit line for each `windows-latest`
    job. Also record the local tip SHA so the two can be compared.

    If the push or the dispatch cannot complete from this worktree (no credentials, or the branch is
    not the one carrying the phase's merged waves), do not fabricate a result and do not cite an older
    run. Record the exact commands the owner must run and mark the section
    `PENDING — owner dispatch required`, then leave the human check below to close it. A `PENDING`
    marker is an honest open item; a copied older run URL is not.
  </action>
  <verify>
    <automated>grep -q '3-OS CI dispatch' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md &amp;&amp; grep -qE 'windows-latest|PENDING — owner dispatch required' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</automated>
    <human-check>Confirm the recorded CI run URL was dispatched against this phase's own post-fix tip (the head SHA in the evidence file matches the phase tip), that every `windows-latest` matrix job concluded successfully, and that the run is newer than the phase's last commit rather than inferred from an earlier run.</human-check>
  </verify>
  <acceptance_criteria>
    - `59-WINDOWS-URI-EVIDENCE.md` § "SC#5 acceptance" contains a `### 3-OS CI dispatch` subsection
    - that subsection records either (a) a run URL, the dispatched head SHA, and a per-job conclusion line for every matrix job including each `windows-latest` job, or (b) a `PENDING — owner dispatch required` marker together with the exact commands to run
    - the recorded head SHA, when present, equals the local tip SHA also recorded in the same subsection
    - no CI run older than the phase's own commits is cited as evidence
    - `git status --porcelain` shows only the evidence file as modified before the commit
  </acceptance_criteria>
  <done>The 3-OS lane has been dispatched fresh on the post-fix tip with its per-job results recorded, or the dispatch is explicitly marked pending with reproducible commands — never silently omitted.</done>
  <reversibility rating="reversible">Evidence-file append plus a branch push; no code change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local worktree → `origin` remote (branch push + workflow dispatch) | the only outbound action in this phase; it publishes commits and triggers CI |
| recorded evidence → phase acceptance decision | the evidence file is what SC#1–SC#5 are judged on, so a fabricated or stale entry is the failure mode this plan guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-07 | Repudiation | evidence recorded from the wrong tree, or a stale CI run cited as this phase's acceptance | medium | mitigate | Every transcript records the SHA it was taken at; the four-combination measurement restores the working tree and verifies `git status --porcelain typsphinx/` empty after each step; a dispatch that cannot be completed is marked `PENDING` rather than filled with an older run. |
| T-59-06 | Denial of Service | a skipped gate reported as a pass, hiding an unprovisioned venv | medium | mitigate | Task 2's per-module skip census requires an explicit `0 skipped` for all five new gate modules, and the plan states that `typst` is a core dependency so a skipped compile gate means the venv is wrong. |
| T-59-08 | Tampering | a partially-reverted product file accidentally committed during the four-combination measurement | medium | mitigate | `git checkout HEAD -- typsphinx/builder.py typsphinx/translator.py` after every combination, an emptiness check on `git status --porcelain typsphinx/`, and an acceptance criterion that the task's commit touches only the evidence file. |
| T-59-SC | Tampering | package-manager installs (npm/pip/cargo) | low | accept | No package-manager install task exists in this plan; `59-RESEARCH.md` § "Package Legitimacy Audit" records the phase as stdlib-only with zero new packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `59-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. This plan creates
no new symbol and no new file; it completes
`.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md` with
its `## IMG-07 four-combination table` and `## SC#5 acceptance` sections.
</artifacts_this_phase_produces>

<verification>
- `59-WINDOWS-URI-EVIDENCE.md` contains all five requirement sections, the four-combination table with
  four verbatim transcripts, the zero-test-edit measurement, the per-module skip census, the RED-first
  ledger, and the CI dispatch record
- `uv run pytest -q`, `uv run black --check .` and `uv run mypy typsphinx/` all green on the phase tip
- `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/` shows only `A` entries
- `git status --porcelain typsphinx/` is empty (no reverted product file left behind)
</verification>

<success_criteria>
1. D-01's four-combination table is recorded with four verbatim transcripts, including Typst's own
   `path must not contain a backslash` and `unclosed delimiter` refusals, proving neither half alone
   closes the compile failure.
2. SC#5's zero-test-edit claim is measured with `git diff --name-status` against the phase base and
   cross-checked against `58-REPR-CENSUS.md`, not asserted.
3. All five new gate modules report `0 skipped`; the full suite, `black --check` and
   `mypy typsphinx/` are green, with `ruff` explicitly deferred to CI.
4. The 3-OS CI lane is dispatched fresh on this phase's own post-fix tip with per-job results
   recorded, or is explicitly marked `PENDING — owner dispatch required` with reproducible commands.
5. The working tree is clean of the temporary reverts, and this plan edits no product or test file.
</success_criteria>

<output>
Create `.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-05-SUMMARY.md` when done.
</output>
