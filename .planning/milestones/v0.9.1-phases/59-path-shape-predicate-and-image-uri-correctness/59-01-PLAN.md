---
phase: 59-path-shape-predicate-and-image-uri-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typsphinx/builder.py
  - tests/test_path_shape_predicate_gate.py
  - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md
  - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/COVERAGE.md
autonomous: true
requirements: [PATH-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - '_escapes_outdir() called directly returns True for the driveless-absolute stem \manuals\guide and the UNC stem \\srv\share\g (both False before this plan).'
    - 'The pre-fix False for those two shapes is quoted verbatim in 59-WINDOWS-URI-EVIDENCE.md, recorded before the product edit.'
    - 'Both production call sites (_resolve_target_stem, _track_image) classify all five documented shapes identically before and after the fix, shown by a two-tree measurement in the evidence file.'
    - '_escapes_outdir("") returns False and _escapes_outdir("guide") returns False -- the normalize-then-decide rewrite leaves the empty and single-component cases unchanged.'
    - '_escapes_outdir() compares Python str code points after a literal backslash-to-slash replacement; no Unicode normalization, byte-length, or grapheme-cluster comparison is applied, so "mañana/guide" (NFC) and "man\u0303ana/guide" (NFD) both classify False.'
    - 'The phase base commit SHA is recorded at the top of 59-WINDOWS-URI-EVIDENCE.md before any product file is edited, so later plans can reconstruct the pre-fix tree deterministically.'
  artifacts:
    - tests/test_path_shape_predicate_gate.py
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/COVERAGE.md
    - 'typsphinx/builder.py :: _escapes_outdir() rewritten to normalize-then-decide'
  key_links:
    - '_escapes_outdir() -> _is_drive_qualified(): both terms must read the same normalized string, matching _is_absolute_image_uri().'
    - '_resolve_target_stem() -> _escapes_outdir(): the call site pre-normalizes, so its returned stem must not change for any shape.'
    - '_track_image() -> _escapes_outdir(): the call site passes a relpath() result that always carries "..", so its branch selection must not change for any shape.'
  prohibitions:
    - 'No existing test assertion anywhere under tests/ is modified; only new test files are added.'
    - 'The !r interpolations in builder.py warning messages are not rewritten in this phase -- MSG-03 owns them in Phase 60.'
    - "PATH-01's gate does not route through _resolve_target_stem() or _track_image(); those call sites appear only in the separately-named characterization pin."
    - 'No typing-import modernization (Dict/List to dict/list) is performed anywhere in this diff.'
    - 'The ".." segment term of _escapes_outdir() is not weakened, removed, or reordered.'
---

<objective>
Close PATH-01: `_escapes_outdir()` applies its absolute-path and drive-qualified checks to the
backslash-normalized string, matching the idiom `_is_absolute_image_uri()` already ships, and the
hardening is proven to have changed nothing at either production call site.

Purpose: this is the phase's thinnest complete vertical slice. It touches every layer the rest of
the phase repeats — a new gate module, a product edit in `typsphinx/builder.py`, and the phase's
evidence record — for one requirement, end to end, before any horizontal expansion. It also creates
`59-WINDOWS-URI-EVIDENCE.md` and pins the phase base SHA, which plans 02–05 all depend on.

Output: `tests/test_path_shape_predicate_gate.py` (RED gate + characterization pin), the rewritten
`_escapes_outdir()`, and `59-WINDOWS-URI-EVIDENCE.md` carrying the phase base SHA, PATH-01's verbatim
pre-fix failure, and the two-tree call-site measurement.

**Tracer note (TRACER_MODE):** the leading `type="tracer"` task is the RED half of that slice and
task 2 completes it. The RED recording is a separate task from the product edit on purpose —
ROADMAP constraint 1 requires the failure to be recorded against the unfixed tree *before* the fix
lands, which a single write-test-and-fix task cannot produce.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-PATTERNS.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-VALIDATION.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 11). Before running ANY command in this plan, provision the worktree's own venv:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Do not degrade to main-tree execution.
</worktree_provisioning>

<tasks>

<task type="tracer">
  <name>Task 1: Record the phase base SHA and PATH-01's verbatim RED (direct-call gate)</name>
  <files>.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md, .planning/phases/59-path-shape-predicate-and-image-uri-correctness/COVERAGE.md, tests/test_path_shape_predicate_gate.py</files>
  <read_first>
    - typsphinx/builder.py lines 39-240 (`RESERVED_IMAGE_NAMESPACE`, `_is_drive_qualified`, `_is_absolute_image_uri`, `_escapes_outdir` and its docstring)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-PATTERNS.md § "tests/test_path_shape_predicate_gate.py (new)"
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md § D-09, D-10, D-11
    - .planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md (the evidence-file shape D-11 follows)
    - .planning/phases/58-repr-format-decoupling-test-side-only/COVERAGE.md (the declaration precedent to copy)
    - tests/test_path_naming_predicate.py (house `@pytest.mark.parametrize` convention)
  </read_first>
  <action>
    FIRST, before touching any product file, create
    `.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md`
    (per D-11 this exact filename — the `59-VERIFICATION.md` name is reserved and overwritten
    wholesale by `gsd-verifier`). Give it a `# Phase 59 — Windows URI Evidence` heading, a
    `## Phase base SHA` section whose body is the literal output of `git rev-parse HEAD` run in this
    worktree before any product edit (label it `PHASE_BASE_SHA`), and empty `## PATH-01`,
    `## IMG-04 / IMG-06`, `## IMG-05`, `## IMG-07 four-combination table`, and `## SC#5 acceptance`
    sections for later plans to fill.

    Also create `.planning/phases/59-path-shape-predicate-and-image-uri-correctness/COVERAGE.md`
    containing a reasoned external-API declaration in the exact form Phase 58 used: a first line
    `No external API integration: <reason>` naming what this phase touches instead (two product
    files, five new test modules, one new test fixture project, and planning evidence — no external
    API, SDK, service, or endpoint), plus the plan-time detector result recorded verbatim
    (`{"detected":false,"signals":[]}`) and a sentence explaining that the file exists because the
    seal-time `api-coverage.verify-pre` gate re-runs the detector over the concatenated PLAN bodies
    and has a recorded history of firing on this project's prose.

    THEN create `tests/test_path_shape_predicate_gate.py` with a module docstring stating that
    PATH-01's gate calls `_escapes_outdir()` DIRECTLY (ROADMAP constraint 8: an integration test
    routed through either production call site is tautologically green before and after the fix and
    proves nothing), and a class `TestEscapesOutdirDirectCall` containing exactly two tests named
    `test_escapes_outdir_direct_driveless_absolute_is_true` and
    `test_escapes_outdir_direct_unc_is_true`. Each imports `_escapes_outdir` from `typsphinx.builder`
    and calls it directly on a hand-built Windows-shaped raw string literal — the driveless-absolute
    stem is `r"\manuals\guide"` and the UNC stem is `r"\\srv\share\g"` — asserting `True` in both
    cases. Do not import, instantiate, or reference `TypstBuilder`, `_resolve_target_stem` or
    `_track_image` in this class. Do not add any `os.name` branch: this is a string-shape decision on
    every platform, per this module's own D-05 platform-independence principle.

    Run the gate, capture the full verbatim pytest output (both failing assertions, including the
    `assert False is True` lines and the shape literals), and paste it into the evidence file's
    `## PATH-01` section under a `### RED (pre-fix, direct call)` heading, with the command that
    produced it and the phase base SHA it was taken at. Commit the RED gate and the evidence file
    together; the fix arrives in task 2.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; ! uv run pytest tests/test_path_shape_predicate_gate.py -k escapes_outdir_direct -x &amp;&amp; grep -q 'PHASE_BASE_SHA' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md &amp;&amp; grep -q 'RED (pre-fix, direct call)' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_path_shape_predicate_gate.py` exists and contains `def test_escapes_outdir_direct_driveless_absolute_is_true` and `def test_escapes_outdir_direct_unc_is_true`
    - `uv run pytest tests/test_path_shape_predicate_gate.py -k escapes_outdir_direct -x` exits NON-ZERO with exactly 2 failed tests and 0 skipped
    - `grep -c 'TypstBuilder' tests/test_path_shape_predicate_gate.py` returns 0 at this point in the plan
    - `59-WINDOWS-URI-EVIDENCE.md` exists, contains a `## Phase base SHA` section whose recorded SHA equals `git rev-parse HEAD` taken before any edit to `typsphinx/builder.py`, and contains the verbatim two-failure pytest transcript
    - `COVERAGE.md` exists in the phase directory and its first content line begins `No external API integration:`
    - `typsphinx/builder.py` is unchanged by this task (`git diff --stat -- typsphinx/builder.py` is empty)
  </acceptance_criteria>
  <done>The direct-call gate is committed RED with 2 failures, the phase base SHA and the verbatim failure transcript are recorded in `59-WINDOWS-URI-EVIDENCE.md`, and no product file has been touched.</done>
  <reversibility rating="reversible">Adding a new test module and an evidence file; both are deletable with no downstream effect.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite _escapes_outdir() to normalize first, then decide</name>
  <files>typsphinx/builder.py</files>
  <read_first>
    - typsphinx/builder.py lines 86-240 (the whole predicate cluster: `_is_drive_qualified`, `_is_absolute_image_uri` with its `normalized = resolved_uri.replace("\\", "/")` idiom at 193-194, and `_escapes_outdir`'s docstring + body at 197-238)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-RESEARCH.md § "Code Examples" → "PATH-01: _escapes_outdir() normalize-then-decide rewrite"
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-PATTERNS.md § "typsphinx/builder.py — PATH-01"
  </read_first>
  <behavior>
    - `_escapes_outdir(r"\manuals\guide")` returns True (was False)
    - `_escapes_outdir(r"\\srv\share\g")` returns True (was False)
    - `_escapes_outdir("manuals/guide")` returns False (unchanged)
    - `_escapes_outdir("../escape")` returns True (unchanged)
    - `_escapes_outdir("/abs/manual")` returns True (unchanged)
    - `_escapes_outdir("C:manual")` returns True (unchanged)
    - `_escapes_outdir("")` returns False (unchanged)
  </behavior>
  <action>
    In `typsphinx/builder.py`, rewrite the body of `_escapes_outdir(stem: str) -> bool` to bind one
    local `normalized = stem.replace("\\", "/")`, derive `segments = normalized.split("/")` from it,
    and pass `normalized` (not `stem`) to BOTH `posixpath.isabs(...)` and `_is_drive_qualified(...)`.
    This is per D-09 and mirrors `_is_absolute_image_uri()`'s already-shipped single-`normalized`
    idiom at `builder.py:193-194`. The `".." in segments` term keeps its current position and meaning
    — the OR order stays `".." in segments or posixpath.isabs(...) or _is_drive_qualified(...)`.

    Keep the existing multi-line comment block above the return verbatim (the measured
    `ntpath.isabs("/abs/manual") is False` reasoning, 47-10/T2) — it is still exactly why
    `posixpath` is used here. Extend the comment with one sentence recording that both shape terms
    now read the normalized string per PATH-01, naming `_is_absolute_image_uri()` as the sibling idiom.

    Extend the docstring's existing `Examples:` doctest block with the two shapes that newly classify
    True. The docstring is a plain (non-raw) triple-quoted string and the surrounding prose already
    uses doubled backslash escapes, so write the new doctest lines using the same escaping convention
    the file already uses for backslash-bearing literals — verify by reading the rendered value of
    `typsphinx.builder._escapes_outdir.__doc__` that each rendered `>>>` line is a syntactically valid
    Python call whose argument is the intended one-backslash / two-backslash stem, and that the file
    still compiles without a `SyntaxWarning` for an invalid escape sequence.

    Do not touch `_is_drive_qualified`, `_is_absolute_image_uri`, `_resolve_target_stem`,
    `_track_image`, `copy_image_files`, or any warning message string in this file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_path_shape_predicate_gate.py -k escapes_outdir_direct -x &amp;&amp; uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_path_shape_predicate_gate.py -k escapes_outdir_direct -x` exits 0 with 2 passed, 0 skipped
    - `uv run pytest -q` exits 0 (full suite green — no pre-existing test changed behavior)
    - `grep -c 'normalized' typsphinx/builder.py` is at least 2 (the `_is_absolute_image_uri` binding and the new `_escapes_outdir` binding)
    - `uv run python -c "from typsphinx.builder import _escapes_outdir as e; print(e(r'\manuals\guide'), e(r'\\srv\share\g'), e('manuals/guide'), e(''), e('../escape'))"` prints `True True False False True`
    - `uv run black --check .` exits 0 and `uv run mypy typsphinx/` exits 0
    - `git diff -- tests/` shows no modification to any file that existed before this phase
  </acceptance_criteria>
  <done>`_escapes_outdir()` decides on the normalized string, the two RED tests are green, and the full suite, black and mypy are clean.</done>
  <reversibility rating="reversible">A three-line predicate change in one function; revertible by restoring the two `stem` arguments.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Characterization pin at both call sites + two-tree byte-identical measurement</name>
  <files>tests/test_path_shape_predicate_gate.py, .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</files>
  <read_first>
    - typsphinx/builder.py lines 608-700 (`_resolve_target_stem` — note it normalizes `stem` BEFORE calling `_escapes_outdir`)
    - typsphinx/builder.py lines 1637-1790 (`_track_image` — the `_is_absolute_image_uri` gate, the `relpath` + `_escapes_outdir` call, and the three key branches)
    - tests/test_builder.py lines 505-600 (`test_post_process_images_rehome_escape_relocates_with_warning` — the house pattern for driving `post_process_images()` over a hand-built `nodes.image` with `temp_sphinx_app`)
    - tests/conftest.py lines 40-90 (`temp_sphinx_app` fixture)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md § D-09, D-10
  </read_first>
  <action>
    Append two classes to `tests/test_path_shape_predicate_gate.py`.

    `TestEscapesOutdirEdgeShapes` — direct calls covering the edge-probe rows this phase resolved
    explicitly for PATH-01: `test_characterization_empty_and_single_component_shapes` asserts
    `_escapes_outdir("")` is False and `_escapes_outdir("guide")` is False; and
    `test_characterization_unicode_normalization_form_is_irrelevant` asserts that the NFC string
    `"ma\u00f1ana/guide"` and the NFD string `"man\u0303ana/guide"` both classify False, with a
    docstring recording the contract: classification reads Python `str` code points after a literal
    backslash-to-slash replacement, and depends only on the ASCII characters `/`, `\`, `..` and a
    leading drive letter — no Unicode normalization, byte-length, or grapheme-cluster comparison is
    performed.

    `TestEscapesOutdirCallSiteCharacterization` — D-10's opposite-routing pin: it runs THROUGH both
    production call sites, because "the hardening changed no live behaviour" is a claim about the
    call sites and nothing else. Define one module-level shape table with five labelled entries —
    `driveless-absolute` = `r"\manuals\guide"`, `unc` = `r"\\srv\share\g"`, `drive-qualified` =
    `"C:manual"`, `posix-absolute` = `"/abs/manual"`, `ordinary-relative` = `"manuals/guide"` — and
    drive it with a single `@pytest.mark.parametrize` per call site, in the house convention.

    `test_characterization_resolve_target_stem_shapes` builds a `TypstBuilder` from the
    `temp_sphinx_app` fixture and calls `builder._resolve_target_stem("index", shape + ".typ")` for
    each shape, asserting the RETURNED STEM equals the expected value recorded in the table. Assert
    only on the return value — never on the warning text, which interpolates `{target!r}` and is
    MSG-03's site in Phase 60 (ROADMAP constraint 4).

    `test_characterization_track_image_escape_branch_shapes` builds a minimal `nodes.image` node for
    each shape with `.png` appended (following `tests/test_builder.py`'s `Reporter`/`states.Struct`
    document setup), calls `builder.post_process_images(doc)`, and asserts on TWO branch-selection
    observables only: whether `node["uri"]` starts with `f"{RESERVED_IMAGE_NAMESPACE}/"`, and the
    COUNT of emitted WARNING records captured with `caplog.at_level("WARNING")`. Do not assert on the
    key's suffix, the digest, or any warning message text — plan 02 changes the key value and Phase 60
    changes the message quoting, and this pin must stay green across both.

    Then record the two-tree measurement in the evidence file's `## PATH-01` section under
    `### Characterization: byte-identical at both call sites`. Read `PHASE_BASE_SHA` from the top of
    the evidence file, run `git checkout $PHASE_BASE_SHA -- typsphinx/builder.py` to restore the
    pre-fix predicate while keeping the new tests, run
    `uv run pytest tests/test_path_shape_predicate_gate.py -k characterization -q` and capture the
    verbatim output, then `git checkout HEAD -- typsphinx/builder.py` to restore the fixed predicate
    and run the identical command again, capturing that output too. Paste both transcripts and state
    explicitly that the two are byte-identical (and, if they are not, HALT — a difference means the
    hardening changed live behaviour and PATH-01's reachability note is wrong). Confirm
    `git status --porcelain typsphinx/builder.py` is clean before committing.
  </action>
  <verify>
    <automated>uv run pytest tests/test_path_shape_predicate_gate.py -q &amp;&amp; uv run pytest -q &amp;&amp; grep -q 'byte-identical' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md &amp;&amp; test -z "$(git status --porcelain typsphinx/builder.py)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_path_shape_predicate_gate.py -k characterization -q` exits 0 with 0 skipped
    - `uv run pytest tests/test_path_shape_predicate_gate.py -q` exits 0 (direct-call gate + edge shapes + characterization all green)
    - `grep -c 'caplog.at_level' tests/test_path_shape_predicate_gate.py` is at least 1
    - `59-WINDOWS-URI-EVIDENCE.md` § PATH-01 contains two labelled transcripts (pre-fix tree, post-fix tree) for the same `-k characterization` command and an explicit statement that they are byte-identical
    - `git status --porcelain typsphinx/builder.py` is empty (the temporary checkout was restored)
    - `uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>The characterization pin passes identically against the pre-fix and post-fix trees, both transcripts are recorded in the evidence file, and the working tree is clean.</done>
  <reversibility rating="reversible">Test-only additions plus an append to the evidence file.</reversibility>
</task>

</tasks>

<assumption_delta_decision>
Detector fired: `{"kind":"pluralization","term":"alongside"}` on the ROADMAP Phase 59 sentence
"`translator.py` is a disjoint file and parallel-safe alongside it".

- **Primary noun:** unchanged — a path-shape string. No second variant of any modeled noun is
  introduced by this phase.
- **Decision:** `no-change`. The signal fired on plan-scheduling prose (which files may run in
  parallel worktrees), not on an identity model. This phase adds no second platform, tenant, auth
  method, or source of truth; `_escapes_outdir()` and `_is_absolute_image_uri()` remain two callers
  of the one already-primary `posixpath.isabs(normalized) or _is_drive_qualified(normalized)` idiom,
  and PATH-01 *reduces* the divergence between them rather than adding a variant.
- **Accepted debt:** none.
</assumption_delta_decision>

<flagged_assumptions>
Edge-probe accounting for this phase (9 probe-surfaced items, 0 silent drops): 6 resolved explicit
(authored into `must_haves.truths` across plans 01 and 02), 3 unresolved/unclassified and flagged
below. No item is auto-resolved with a backstop.

| Requirement | Category | Status | Where surfaced |
|---|---|---|---|
| PATH-01 | empty | resolved (explicit) | 59-01 must_haves + `test_characterization_empty_and_single_component_shapes` |
| PATH-01 | encoding | resolved (explicit) | 59-01 must_haves + `test_characterization_unicode_normalization_form_is_irrelevant` |
| IMG-06 | boundary | resolved (explicit) | 59-02 must_haves + `-k length_bound` |
| IMG-06 | empty | resolved (explicit) | 59-02 must_haves + `-k length_bound` |
| IMG-06 | encoding | resolved (explicit) | 59-02 must_haves + `-k length_bound` |
| IMG-06 | precision | resolved (explicit) | 59-02 must_haves + `-k length_bound` |
| IMG-04 | unclassified | **UNRESOLVED — flagged** | 59-02 § Flagged Assumptions |
| IMG-05 | unclassified | **UNRESOLVED — flagged** | 59-03 § Flagged Assumptions |
| IMG-07 | unclassified | **UNRESOLVED — flagged** | 59-04 § Flagged Assumptions |

The three `unclassified` rows carry the probe text "unclassified — review manually" and are NOT
auto-resolved by rule. Each owning plan states what the unclassified edge would be if it were
classified, and why the plan does not mint a criterion for it.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `typst_documents` target string (user `conf.py`) → `_resolve_target_stem()` → `path.join(self.outdir, ...)` | a user-authored, arbitrarily-shaped path string crosses into an output-path construction |
| third-party post-transform `node["uri"]` → `_track_image()` → `self.images` | an extension-authored absolute path string crosses into builder-owned state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-01 | Tampering | `typsphinx/builder.py::_escapes_outdir` (ASVS V12 File and Resources — outdir containment, OUT-02) | medium | mitigate | Apply `posixpath.isabs(...)` and `_is_drive_qualified(...)` to the backslash-normalized string so a driveless-absolute or UNC stem is classified as escaping. Strictly more restrictive than today: the `".."` term is unchanged and no shape that currently returns True can start returning False. |
| T-59-01b | Tampering | `typsphinx/builder.py::_resolve_target_stem` fallback path (ASVS V12) | low | accept | The call site pre-normalizes, so this plan cannot change its behaviour; the characterization pin in task 3 is the accepted-risk control that proves it. |
| T-59-SC | Tampering | package-manager installs (npm/pip/cargo) | low | accept | No package-manager install task exists in this phase. `59-RESEARCH.md` § "Package Legitimacy Audit" records the phase as stdlib-only with zero new packages, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Symbols and files newly created by Phase 59 (all five plans), listed here so the plan-review
source-grounding pass excludes them from drift verification:

**New symbols in `typsphinx/builder.py`** (plan 02): `_bound_relocation_component`,
`_build_relocation_key`, `MAX_PATH_COMPONENT_BYTES`.

**New symbols in `typsphinx/translator.py`** (plan 03): the local name `escaped_uri` inside
`visit_image()`.

**New test modules and their classes:**
- `tests/test_path_shape_predicate_gate.py` — `TestEscapesOutdirDirectCall`,
  `TestEscapesOutdirEdgeShapes`, `TestEscapesOutdirCallSiteCharacterization` (plan 01)
- `tests/test_track_image_key_construction.py` — `TestRelocationKeyNoBackslash`,
  `TestRelocationKeyLengthBound` (plan 02)
- `tests/test_copy_image_files_name_too_long.py` — `TestCopyImageFilesNameTooLong` (plan 02)
- `tests/test_image_literal_escaping_gate.py` — `TestImageLiteralEscaping` (plan 03)
- `tests/test_windows_image_uri_render_gate.py` — `TestWindowsShapedImageUriStringShape`,
  `TestWindowsShapedImageUriCompileGate` (plan 04)

**New fixture project** (plan 04): `tests/fixtures/windows_shaped_image_uri_gate/` containing
`conf.py` (with the transform class `WindowsShapedImageUriTransform` and the
`TYPSPHINX_WIN_URI_MODE` environment switch), `index.rst`, and
`_static/converted_stand_in.png`.

**New planning artifacts:**
`.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md`
(plan 01, appended by plans 02–05) and
`.planning/phases/59-path-shape-predicate-and-image-uri-correctness/COVERAGE.md` (plan 01).
</artifacts_this_phase_produces>

<verification>
- `uv run pytest tests/test_path_shape_predicate_gate.py -q` — all classes green, 0 skipped
- `uv run pytest -q` — full suite green
- `uv run black --check .` and `uv run mypy typsphinx/` — clean (`ruff` is deferred to CI per
  `59-VALIDATION.md`; it is not runnable on this dev machine)
- `git diff --stat <PHASE_BASE_SHA>..HEAD -- tests/` shows one added file and no modified lines in
  any pre-existing test module
- `59-WINDOWS-URI-EVIDENCE.md` contains: the phase base SHA, PATH-01's verbatim pre-fix failure
  transcript, and two byte-identical characterization transcripts
</verification>

<success_criteria>
1. `_escapes_outdir(r"\manuals\guide")` and `_escapes_outdir(r"\\srv\share\g")` both return `True`,
   proven by a test that calls the predicate directly and never through a production call site.
2. The pre-fix `False` for those two shapes is quoted verbatim in `59-WINDOWS-URI-EVIDENCE.md`,
   recorded at the phase base SHA before `typsphinx/builder.py` was edited.
3. `_resolve_target_stem()` and `_track_image()` classify all five documented shapes identically
   before and after, shown by two byte-identical transcripts in the evidence file.
4. The full suite, `black --check` and `mypy typsphinx/` are green, with zero existing test
   assertions modified.
</success_criteria>

<output>
Create `.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-01-SUMMARY.md` when done.
</output>
