---
phase: 46-v0-7-1-release-prep-prep-only
plan: 06
type: execute
wave: 4
depends_on: ["46-04", "46-05"]
files_modified:
  - .planning/phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md
  - .planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md
  - .planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md
  - .planning/todos/completed/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md
autonomous: true
requirements: [REL-06, REL-04]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No irreversible action was taken at any point in the phase: `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` are both empty, measured as **two independent observations at two separate moments** — one in `46-RELEASE-EVIDENCE.md` and a fresh one in `46-HANDOFF.md`, timestamped apart (SC#5)."
    - "An interruption at any wave leaves the release unpublished: no tag exists locally or on `origin`, nothing was uploaded to PyPI, no GitHub Release was created, and no pull request was opened or merged — so the phase is restartable without an undo step."
    - "A standalone `46-HANDOFF.md` exists covering merge → tag → `release.yml` (with an explicit item to observe `create-release` succeed, which is what closes REL-04) → PyPI + GitHub Release → the second tag on `typsphinx-doc-translations` → the Read the Docs `stable` measurement on both projects (SC#5)."
    - "The roll-up evidence file is named `46-RELEASE-EVIDENCE.md`, **not** `46-VERIFICATION.md` — that name is reserved by `/gsd-verify-work` and is overwritten wholesale when the verifier runs (D-15)."
    - "Every one of the 12 records in `.planning/todos/pending/` is dispositioned in writing: `2026-08-11-windows-path-separator-breaks-contract-claims-gate` is filed to `todos/completed/` because D-22 resolved it, and every other record is explicitly deferred with its reason recorded (D-16)."
    - "The two `TypstBuilder._track_image()` defect records stay in `todos/pending/` and are named in `46-HANDOFF.md`; no `### Known Limitations` CHANGELOG section was added and no GitHub issue was filed (D-27)."
    - "`.planning/REQUIREMENTS.md` is unedited by this phase, and `.planning/STATE.md` carries no correction about PR #131 — D-17 was retracted and D-28 confirms `STATE.md` was right (D-26, D-28)."
  prohibitions:
    - statement: "MUST NOT report REL-04 complete, or write any artifact that a later reader could take as reporting it complete. Its acceptance evidence is a real tag push whose `create-release` job runs to completion. v0.7.0 reported this requirement's mechanism as done on the strength of a correct workflow file and the release then failed at exactly that job; repeating that would launder an unproven automation into a closed requirement and hide a known publishing risk from the next release."
      status: unverified
  artifacts:
    - path: ".planning/phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md"
      provides: "the SC#1–SC#5 roll-up citing the four sibling evidence files, plus fence observation 1 of 2"
      contains: "SC#5"
    - path: ".planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md"
      provides: "the publish checklist for /gsd-complete-milestone, plus fence observation 2 of 2"
      contains: "typsphinx-doc-translations"
    - path: ".planning/todos/completed/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md"
      provides: "the D-22 record filed as resolved"
      contains: "contract-claims"
  key_links:
    - from: "46-RELEASE-EVIDENCE.md"
      to: "46-CI-EVIDENCE.md / 46-BUMP-EVIDENCE.md / 46-GREEN-TREE-EVIDENCE.md / 46-SC4-INVARIANTS.md / 46-REL04-EVIDENCE.md"
      via: "the roll-up cites each sibling's own verdict language verbatim rather than re-deriving its findings"
      pattern: "46-(CI|BUMP|GREEN-TREE|SC4|REL04)"
    - from: "46-HANDOFF.md checklist item for `release.yml`"
      to: "REL-04"
      via: "the item that says to watch `create-release` run to completion is the step that closes the requirement; until it runs, REL-04 carries forward"
      pattern: "create-release"
---

<objective>
Close the phase: roll the five sibling evidence files into one SC#1–SC#5 record, write the publish
handoff for `/gsd-complete-milestone`, prove the prep/publish fence held as two independent
observations, and disposition every pending todo in writing.

Purpose: discharges ROADMAP Phase 46 SC#5. The fence is absolute (the Phase 33/35/41 precedent) and
must be proved by *absence* across two namespaces — local refs and `origin` — because a pytest
assertion would be vacuous and would never reach the remote. The handoff exists because the publish
half is structurally outside this phase, and because REL-04 closes only when someone watches
`create-release` succeed on a real tag push.

Output: `46-RELEASE-EVIDENCE.md`, `46-HANDOFF.md`, a filed D-22 todo record, and a written disposition
for every remaining pending todo.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-CONTEXT.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `46-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan creates
`.planning/phases/46-…/46-RELEASE-EVIDENCE.md` and `.planning/phases/46-…/46-HANDOFF.md`, and moves
one todo record from `todos/pending/` to `todos/completed/`.

## Probe coverage accounting (spec-less fallback)

No phase SPEC exists, so the edge and prohibition predicates were derived by the spec-less probe
fallback and authored into plan `must_haves`. The accounting below is recorded here so the totals stay
auditable across the plan set. **No item was dropped silently.**

### Edge coverage — 9 probe-surfaced items, 9 authored, 0 unresolved

| # | Requirement | Category | Verification | Authored in |
|---|-------------|----------|--------------|-------------|
| 1 | REL-06 | adjacency | explicit | 46-03 `must_haves.truths` — the `## [Unreleased]` / `## [0.7.1]` / `## [0.7.0]` boundary |
| 2 | REL-06 | empty | explicit | 46-05 `must_haves.truths` — extractor exit 0 non-empty, exit 1 with no section |
| 3 | REL-06 | ordering | explicit | 46-03 `must_haves.truths` — `RELEASE_VERSIONS` gains `"0.7.1"` only after the heading exists |
| 4 | REL-06 | concurrency | explicit | this plan's `must_haves.truths` — an interruption at any wave leaves the release unpublished |
| 5 | REL-04 | adjacency | explicit | 46-05 `must_haves.truths` — the extracted body carries no text from either neighbouring section |
| 6 | REL-04 | empty | explicit | 46-05 `must_haves.truths` — the `validate` job's non-emptiness call shape exercised by hand |
| 7 | REL-04 | ordering | explicit | 46-05 `must_haves.truths` — stdout byte-identical to the blank-stripped source region |
| 8 | REL-04 | idempotency | explicit | 46-05 `must_haves.truths` — two invocations byte-identical, neither creates a tag |
| 9 | REL-04 | concurrency | **backstop** | 46-05 `must_haves.truths` — REL-04 stays open regardless of where the phase stops |

Item 9 is a structured `{ statement, verification: backstop }` marker: it asserts the *absence* of a
closure claim across artifacts, which no single command can confirm. At verify time it abstains to
`human_needed` rather than passing silently.

### Prohibitions — 3 kept after the two-stage recall, 3 authored, 0 dropped silently

| Kept prohibition | Authored in |
|---|---|
| Understating a user-facing break in the release notes | 46-03 `must_haves.prohibitions` |
| Dropping or anonymising the first external contributor's credit | 46-03 `must_haves.prohibitions` |
| Reporting REL-04 complete without its acceptance evidence | this plan's `must_haves.prohibitions` |

Stage-2 precision dropped nine routine-engineering candidates (version/artifact mismatch, gate
weakening, skip-reported-as-pass, green-claimed-from-memory, merge content loss, fence breach,
`typsphinx/` edit, silent `_track_image` "fix", clobbering `46-VERIFICATION.md`) — each is already
owned by an edge truth, a threat-register row, or an explicit acceptance criterion in this plan set,
so minting a prohibition would duplicate rather than add. No canon-referral drop was needed: none of
the candidates is OWASP / prototype-pollution / path-traversal / injection / GDPR / fairness canon.

<!-- planner-discipline-allow: ### Known Limitations -->
<!-- planner-discipline-allow: 46-VERIFICATION.md -->

<tasks>

<task type="auto">
  <name>Task 1: Roll the sibling evidence into `46-RELEASE-EVIDENCE.md` and take fence observation 1 of 2</name>
  <files>.planning/phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 46: v0.7.1 Release Prep (prep-only)" — SC#1 through SC#5 verbatim; each becomes a section heading.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md`, `46-BUMP-EVIDENCE.md`, `46-GREEN-TREE-EVIDENCE.md`, `46-SC4-INVARIANTS.md`, `46-REL04-EVIDENCE.md` — the five siblings this file cites.
    - `.planning/milestones/v0.7.0-phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md` — the heading structure and the three conventions to carry forward: cite siblings rather than re-derive them, split SC#5's fence proof into two independent observations, and close with an "Executed versus skipped" section.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md` § "Release-evidence file" — including the verbatim filename-note paragraph to adapt.
  </read_first>
  <action>
Create `46-RELEASE-EVIDENCE.md` in the phase directory, written entirely in English. **Do not name it
`46-VERIFICATION.md`** — that name is reserved by `/gsd-verify-work`, which overwrites it wholesale
when it runs, so this roll-up would be clobbered (D-15). Open the file with a short filename-note
paragraph saying exactly that, adapted from the `41-RELEASE-EVIDENCE.md` precedent, and with the
provisioning note naming `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

Structure the body as one section per success criterion, each headed with the criterion's own wording
from `ROADMAP.md`:

- `## SC#1` — the version-literal lockstep. Cite `46-BUMP-EVIDENCE.md` and quote its own verdict
  language verbatim; do not re-run its commands or restate its figures in your own words.
- `## SC#2` — the curated `## [0.7.1]` entry, the tail link-block rollover, and the matching
  `docs/source/changelog.rst` section. Record how D-02's three breakage markings appear (the lead
  paragraph's plain statement, the three `**Breaking:**` prefixes, and the `### Removed` section) and
  that D-07's CONF-08 callout carries both measured facts, quoting `44-GATE-EVIDENCE-03.md` § 7.
- `## SC#3` — split into `### CI authority (D-11, D-23 run 2)` citing `46-CI-EVIDENCE.md`,
  `### Local half` citing `46-GREEN-TREE-EVIDENCE.md`, and `### SC#3 roll-up verdict`. Note that
  D-23 run 1 also lives in the CI evidence file and is what proves D-22's Windows repair, which is
  not locally reproducible.
- `## SC#4` — cite `46-SC4-INVARIANTS.md` for the D-21 sweep and `46-REL04-EVIDENCE.md` for REL-04's
  two preconditions, then a `### SC#4 roll-up verdict`. State plainly, in this section, that **REL-04
  remains open** and that its acceptance evidence can only be generated by a real tag push at
  `/gsd-complete-milestone`.
- `## SC#5: no irreversible action taken — the fence, observation 1 of 2` — take the observation
  **now**, freshly: run `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1`, record both
  commands, both (empty) outputs, and the UTC timestamp with `date -u`. Follow with a
  `### SC#5 (observation 1) verdict`. State that observation 2 is taken separately, later, in
  `46-HANDOFF.md`, and that it must be a fresh measurement rather than a copy of this one.
- `## Phase verdict` — a per-criterion PROVEN / PARTIAL / `human_needed` table, honest in both
  directions.
- `## Executed versus skipped` — state plainly what was not run and why: `tox -e py312` cannot
  provision on this machine, a bare `tox` dies in its `lint` environment, and the Windows behaviour of
  D-22's repair is provable only on CI. Do not gloss a gap.

Apply the honest-verifier rule throughout: abstain to `human_needed` rather than assert a truth
without direct evidence, and never transcribe a result from memory.

Take no irreversible action.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md && test ! -e .planning/phases/46-v0-7-1-release-prep-prep-only/46-VERIFICATION.md && grep -q 'observation 1 of 2' .planning/phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md && grep -q 'REL-04 remains open' .planning/phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md && grep -q '^## Executed versus skipped' .planning/phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md && test -z "$(git tag -l v0.7.1)" && test -z "$(git ls-remote --tags origin v0.7.1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md` exists, is written entirely in English, and opens with a filename-note paragraph explaining why it is not named `46-VERIFICATION.md`.
    - No file named `46-VERIFICATION.md` exists in the phase directory.
    - The file contains the headings `## SC#1`, `## SC#2`, `## SC#3`, `## SC#4`, `## SC#5: no irreversible action taken — the fence, observation 1 of 2`, `## Phase verdict`, and `## Executed versus skipped`.
    - The file cites all five sibling evidence files by name (`46-CI-EVIDENCE.md`, `46-BUMP-EVIDENCE.md`, `46-GREEN-TREE-EVIDENCE.md`, `46-SC4-INVARIANTS.md`, `46-REL04-EVIDENCE.md`) and quotes each one's own verdict language rather than re-deriving it.
    - The SC#5 section records both `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` with empty outputs and a `date -u` timestamp, and states that observation 2 is a separate, later measurement in `46-HANDOFF.md`.
    - The SC#4 section contains the sentence-level statement that REL-04 remains open until a real tag push runs `create-release` to completion.
    - `## Executed versus skipped` names `tox -e py312`, the bare-`tox` `lint` failure, and the CI-only provability of the Windows repair.
    - `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` both produce no output.
  </acceptance_criteria>
  <done>A single roll-up records SC#1–SC#5 against the five sibling evidence files, carries fence observation 1 of 2 with its own timestamp, and states REL-04's openness and every skipped check in writing.</done>
</task>

<task type="auto">
  <name>Task 2: Write `46-HANDOFF.md` and take fence observation 2 of 2</name>
  <files>.planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md</files>
  <read_first>
    - `.planning/milestones/v0.7.0-phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md` — the full heading structure and the opening-paragraph convention to adapt.
    - `.planning/REQUIREMENTS.md` § "Release" — REL-04 and REL-06 verbatim, to quote in the "What this phase satisfied" section.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md` — task 1's roll-up, which this document cites rather than duplicates.
    - `.planning/todos/pending/2026-08-10-rehomed-converted-image-collides-with-srcdir-images-dir.md` and `.planning/todos/pending/2026-08-10-track-image-rehome-escapes-outdir-for-non-doctreedir-abs-uri.md` — the two D-27 defects this document must name.
    - `.planning/STATE.md` § "Shipped Milestone (v0.7.0 — archived)" — the two-repository tagging record and the Read the Docs `stable` measurement shape, for checklist items 4 and 5.
  </read_first>
  <action>
Create `46-HANDOFF.md` in the phase directory, in English, following `41-HANDOFF.md`'s structure:

Title it `# Phase 46: v0.7.1 Release Prep (prep-only) — Publish & Owner-Manual Handoff Checklist`.
Open with the adapted convention paragraph: this document is the explicit hand-off for the half of
REL-06 this phase structurally cannot satisfy, written in English directly as a phase deliverable that
merges to a public branch, following the `41-HANDOFF.md` precedent.

`## What this phase satisfied, and what it did not` — quote REL-04 and REL-06 verbatim from
`REQUIREMENTS.md`, then map SC#1–SC#5 to a verdict, citing `46-RELEASE-EVIDENCE.md` rather than
re-deriving. State explicitly that REL-06's publish half and **all** of REL-04's acceptance evidence
are owed.

`## Checklist` with seven numbered items, each naming an **Owner** (`/gsd-complete-milestone`, and a
human where a browser or an approval is genuinely required) and an **Ordering** dependency on the
items before it:

1. Open the pull request and merge it to `main`.
2. Push the `v0.7.1` tag on the merge commit.
3. Let `release.yml` run to completion: `validate` → `build` → `publish-pypi` → `create-release`.
   **Watch `create-release` succeed and record the run id — this is the step that closes REL-04.**
   Note the v0.7.0 precedent: run `30848860064`'s `create-release` failed at `uv: command not found`,
   the workflow was fixed on `main` afterwards, and the fix has never been exercised end to end.
   Note also that `publish-pypi` waits on owner approval of the `pypi` environment.
4. Advance the `typsphinx-doc-translations` submodule pin and push a matching `v0.7.1` tag there —
   the standing two-repository tagging cost adopted at the v0.6.4 close. Record D-13's correction here
   so nobody looks for work that does not exist: that repository contains no `conf.py` and no `.typ`
   template, so there is nothing to remove in it; only the pin and the tag move.
5. Confirm Read the Docs `stable` is green on BOTH projects (en and ja) and reports `0.7.1`.
6. Flip REL-04's and REL-06's checkboxes and Traceability rows in `REQUIREMENTS.md` — **only after
   item 3 actually succeeded.** Add the standing warning: `phase.complete` has a recorded habit of
   auto-flipping REL rows against a CONTEXT decision (caught in Phase 41, pre-empted in Phase 42), so
   diff `REQUIREMENTS.md` before committing the close.
7. Re-date the `## [0.7.1]` CHANGELOG heading if the publish lands on a different UTC day than the one
   this phase wrote, and confirm `scripts/extract_changelog_section.py 0.7.1` still returns the
   intended body before the tag is pushed.

`## Not done in this phase, by design` — the prep-only fence and what it cost: D-03's declined
fail-loud shim for the removed `typst_authors` (the silent-loss failure mode is real and measured);
D-27's two `TypstBuilder._track_image()` defects, **named by their record filenames**, both of which
ship in v0.7.1 unfixed with no `### Known Limitations` CHANGELOG section and no GitHub issue, by owner
decision after the full counter-case was argued; and the stale module docstring in
`scripts/extract_changelog_section.py` claiming `CHANGELOG.md` carries two `## [Unreleased]` headings
when a direct count returns one — left unedited because this phase permits exactly two non-planning
code edits, along with the now-narrower `EXCLUDED_CLAIM_PAGES` reason string for
`docs/source/changelog.rst`, which the D-09 migration section outgrew.

`## Deferred by decision, not oversight` — point at task 3's todo ledger rather than duplicating it.

`## Proof the fence held` — take fence observation **2 of 2** here, as a **fresh** measurement, not a
copy of task 1's: run `git tag -l v0.7.1`, `git ls-remote --tags origin v0.7.1`, and `date -u`, and
record all three verbatim. State that observation 1 lives in `46-RELEASE-EVIDENCE.md` and that the two
were taken at separate moments, and show both timestamps so the separation is visible.

Take no irreversible action — this document describes the publish, it does not perform it.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md && grep -q 'typsphinx-doc-translations' .planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md && grep -q 'create-release' .planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md && grep -q 'observation 2 of 2' .planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md && grep -q 'rehomed-converted-image-collides-with-srcdir-images-dir' .planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md && grep -q 'track-image-rehome-escapes-outdir-for-non-doctreedir-abs-uri' .planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md && test -z "$(git tag -l v0.7.1)" && test -z "$(git ls-remote --tags origin v0.7.1)"</automated>
  </verify>
  <acceptance_criteria>
    - `46-HANDOFF.md` exists, is written entirely in English, and carries the headings `## What this phase satisfied, and what it did not`, `## Checklist`, `## Not done in this phase, by design`, `## Deferred by decision, not oversight`, and `## Proof the fence held`.
    - The checklist has seven numbered items, each naming an Owner and an Ordering dependency, in the order merge → tag → `release.yml` → translations-repo pin and tag → Read the Docs `stable` → requirement checkboxes → CHANGELOG re-date check.
    - Item 3 states in its own text that observing `create-release` succeed is what closes REL-04, and names run `30848860064` as the v0.7.0 failure it repairs.
    - Item 6 carries the `phase.complete` auto-flip warning and instructs a diff of `REQUIREMENTS.md` before committing the close.
    - The `## Not done in this phase, by design` section names both `_track_image` todo record filenames verbatim and states that no `### Known Limitations` CHANGELOG section and no GitHub issue were created.
    - The `## Proof the fence held` section contains fresh `git tag -l v0.7.1`, `git ls-remote --tags origin v0.7.1` and `date -u` transcripts, both outputs empty, and its timestamp differs from the one recorded in `46-RELEASE-EVIDENCE.md`.
    - The document quotes REL-04 and REL-06 verbatim from `REQUIREMENTS.md` and states that REL-04's acceptance evidence is entirely owed.
    - `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` both produce no output.
  </acceptance_criteria>
  <done>A standalone publish checklist exists covering all six owner-manual steps plus the CHANGELOG re-date check, names the deliberately-deferred defects, and carries the second of the two independent fence observations.</done>
</task>

<task type="auto">
  <name>Task 3: Disposition every pending todo and file the D-22 record as completed</name>
  <files>.planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md, .planning/todos/completed/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md</files>
  <read_first>
    - `.planning/todos/pending/` — list all 12 records and read each one's header.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-CONTEXT.md` § `<deferred>` → "Reviewed Todos (not folded)" — the enumerated ledger with each record's recorded reason, and D-16's amendment that the ledger is 12 records, not 10.
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/` SUMMARY files — to settle whether `2026-08-04-documented-custom-template-parameter-contract-is-wrong-and-t` is delivered.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/` SUMMARY files — same question for `2026-08-04-typst-documents-title-author-elements-ignored`.
    - `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-05-SUMMARY.md` — the target-name collision guard, which bears on `2026-08-04-duplicate-typst-documents-target-silently-drops-a-master`.
  </read_first>
  <action>
Move `.planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md` to
`.planning/todos/completed/` with `git mv`, and append a short resolution note to the record naming
plan 46-01, the `as_posix()` repair site, and D-23 run 1's run id as the evidence that the two
`windows-latest` lanes went green. This record is the one exception to D-16's blanket deferral,
because D-22 resolved it inside this phase.

For the two records whose delivering phase has already shipped —
`2026-08-04-documented-custom-template-parameter-contract-is-wrong-and-t` (DOC-13, Phase 45.1) and
`2026-08-04-typst-documents-title-author-elements-ignored` (CONF-09, Phase 44.2) — settle the filing
question CONTEXT.md flagged for the planner rather than carrying it again. Read the delivering phase's
SUMMARY files and apply one rule: if the record's described defect is demonstrably delivered by that
phase's shipped work, `git mv` the record to `todos/completed/` with a note citing the delivering plan;
if it is not, leave it in `todos/pending/` and record the deferral reason. Do not guess — cite the
SUMMARY line you relied on either way.

For `2026-08-04-duplicate-typst-documents-target-silently-drops-a-master`, re-measure its status
against Phase 44 plan 44-05's target-name collision guard before carrying it again, and record what
you found. If it is still reachable, defer it with that reason; if 44-05 closed it, file it.

Every remaining record is deferred with its reason recorded (D-16). Add a
`## Deferred by decision, not oversight` section to `46-HANDOFF.md` — or, if task 2 already created
that heading, fill it — listing each remaining pending record by filename with a one-line reason.
Reasons that already exist in `46-CONTEXT.md` § `<deferred>` should be carried forward, not reinvented:

- `2026-07-22-add-sphinx-linkcheck-ci-job` — Future requirement LNK-01; `links.yml`'s repo-wide
  lychee check already covers the links this release adds.
- `2026-07-22-modernize-typing-imports-drop-up006-up035-ignore` — forbidden by `CLAUDE.md` and by the
  milestone's own binding constraint until the todo itself lands.
- `2026-08-04-release-create-job-missing-uv-verify-end-to-end` — this **is** REL-04; stays pending
  until a real tag push runs `create-release` to completion.
- `2026-08-05-a-master-that-is-also-a-toctree-child-is-unrepresentable` — a `typst_documents`
  modelling defect, unrelated to release prep.
- `2026-08-05-shared-document-silently-dropped-from-all-but-first-master` — the
  per-build-not-per-master include-dedup ledger defect.
- `2026-08-10-rehomed-converted-image-collides-with-srcdir-images-dir` and
  `2026-08-10-track-image-rehome-escapes-outdir-for-non-doctreedir-abs-uri` — deferred by owner
  decision (D-27), not by impossibility; the code is on `main` and ships in v0.7.1.
- `2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos` — a `flake.nix`-side toolchain repair in the
  same family as QUA-04; does not block SC#3, which takes lint authority from CI.

Finally, confirm two negatives in writing rather than assuming them: `.planning/REQUIREMENTS.md` is
unedited by this phase (D-26 — PR #131 gets no requirement ID and coverage stays 19/19 mapped with
zero orphans), and `.planning/STATE.md` needs **no** correction about PR #131 — D-17 asserted otherwise
and was retracted; D-28 measured PR #131 as `MERGED` at `9b2b76b`, so `STATE.md` was right all along.

Take no irreversible action.
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md && test ! -e .planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md && test -f .planning/todos/pending/2026-08-04-release-create-job-missing-uv-verify-end-to-end.md && test -f .planning/todos/pending/2026-08-10-rehomed-converted-image-collides-with-srcdir-images-dir.md && test -f .planning/todos/pending/2026-08-10-track-image-rehome-escapes-outdir-for-non-doctreedir-abs-uri.md && test -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" && grep -q '^## Deferred by decision, not oversight' .planning/phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/completed/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md` exists and carries a resolution note naming plan 46-01, the `as_posix()` repair, and D-23 run 1's run id.
    - `.planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md` no longer exists.
    - `.planning/todos/pending/2026-08-04-release-create-job-missing-uv-verify-end-to-end.md` still exists.
    - Both `_track_image` records still exist in `.planning/todos/pending/`.
    - Every file remaining in `.planning/todos/pending/` is named in `46-HANDOFF.md`'s `## Deferred by decision, not oversight` section with a one-line reason.
    - The three records whose status CONTEXT.md flagged for re-measurement (`documented-custom-template-parameter-contract-is-wrong-and-t`, `typst-documents-title-author-elements-ignored`, `duplicate-typst-documents-target-silently-drops-a-master`) each have a recorded finding citing the SUMMARY line relied on, and each is either filed to `todos/completed/` or left pending with that finding as its reason.
    - `git diff --name-only -- .planning/REQUIREMENTS.md` produces no output.
    - `git diff -- .planning/STATE.md` contains no change to any sentence about PR #131.
    - `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` both produce no output.
  </acceptance_criteria>
  <done>Every record in the 12-item todo ledger has a written disposition, the D-22 record is filed as completed with its evidence, and the two "unchanged by design" facts (`REQUIREMENTS.md`, `STATE.md`) are confirmed rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's artifacts → `/gsd-complete-milestone` | The handoff checklist is the only instruction the publish step receives; an omitted item is an omitted release step. |
| the local repository and `origin` → the outside world | The fence proof asserts that nothing crossed into PyPI, a GitHub Release, or a tag namespace. |
| requirement status records → the next milestone's planning | A requirement wrongly marked complete removes it from view permanently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-03 | Tampering | prep/publish fence | high | mitigate | Two independent observations at two separate moments, in two different files, each with its own `date -u` timestamp; both `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` asserted empty in `<verify>` on all three tasks. |
| T-46-14 | Information disclosure | reporting REL-04 complete without acceptance evidence | high | mitigate | Both artifacts state the boundary in writing; the REL-04 todo record is asserted still pending; `REQUIREMENTS.md` is asserted unedited; and checklist item 6 carries the `phase.complete` auto-flip warning with an explicit diff-before-commit instruction. |
| T-46-15 | Repudiation | `46-VERIFICATION.md` clobbering the roll-up | medium | mitigate | The file is named `46-RELEASE-EVIDENCE.md` and its absence-of-the-reserved-name is asserted in `<verify>`; a filename-note paragraph records the reason for the next reader. |
| T-46-16 | Information disclosure | the two `_track_image()` defects vanishing without a written record | medium | accept | Owner decision D-27 after the full counter-case (regression in failure mode, non-exotic reachability, silent wrong output, the `### Known Limitations` precedent, and an otherwise-empty public issue tracker) was on the table. Mitigated to the extent chosen: both records stay in `todos/pending/` and are named by filename in `46-HANDOFF.md`. Not disclosed externally, by decision. |
| T-46-SC | Tampering | npm/pip/cargo installs | medium | accept | This plan installs nothing; it writes planning artifacts and moves one file. RESEARCH.md records the Package Legitimacy Gate as not applicable for the phase — zero packages evaluated, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. |
</threat_model>

<verification>
- `46-RELEASE-EVIDENCE.md` and `46-HANDOFF.md` both exist; no `46-VERIFICATION.md` exists.
- Both files record `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` as empty, at two different `date -u` timestamps.
- Both files state REL-04 as open, and the REL-04 todo record is still in `todos/pending/`.
- The D-22 todo record is in `todos/completed/`; both `_track_image` records are still pending.
- `git diff --name-only -- .planning/REQUIREMENTS.md` is empty.
</verification>

<success_criteria>
- SC#1–SC#5 are rolled up in one file that cites its five siblings rather than re-deriving them.
- A standalone publish handoff exists with seven owner-attributed, ordered checklist items.
- The prep/publish fence is proved by two independent observations taken at separate moments.
- Every one of the 12 todo records has a written disposition; exactly one moved to `completed/`.
- REL-04 is reported **open** everywhere, and `REQUIREMENTS.md` and `STATE.md` are untouched.
</success_criteria>

<output>
Create `.planning/phases/46-v0-7-1-release-prep-prep-only/46-06-SUMMARY.md` when done.
</output>
