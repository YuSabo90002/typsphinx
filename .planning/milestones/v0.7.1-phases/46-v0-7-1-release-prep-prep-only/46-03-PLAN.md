---
phase: 46-v0-7-1-release-prep-prep-only
plan: 03
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - CHANGELOG.md
  - docs/source/changelog.rst
  - tests/test_changelog_page_gate.py
autonomous: true
requirements: [REL-06]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`CHANGELOG.md` carries a `## [0.7.1]` section whose lead paragraph states plainly that this patch release can break a working configuration, and whose CONF-10 / CONF-11 / CONF-12 bullets each carry a `**Breaking:**` prefix (D-02 a and b)."
    - "A `### Removed` section — the first in this CHANGELOG's history — holds the `typst_authors` removal together with the fact that 0.7.0's documentation announced removal in a future major release (D-02 c, D-04)."
    - "The compressed PR #131 bullet keeps the user-visible symptom, drops the internal mechanism, and credits `@christianwehe` in its trailing parentheses (D-24, D-25)."
    - "`CHANGELOG.md` still contains exactly one `## [Unreleased]` heading after the curation, carrying only the `### Planned for Future Releases` list; the new `## [0.7.1]` heading sits between that heading and `## [0.7.0]`, and `scripts/extract_changelog_section.py 0.7.1` returns the 0.7.1 body alone with no text drawn from either neighbouring section."
    - "`tests/test_changelog_page_gate.py`'s `RELEASE_VERSIONS` gains `\"0.7.1\"` only in a tree where the `## [0.7.1]` CHANGELOG heading already exists, and the gate's content-coverage classes run rather than skip, and pass."
    - "`docs/source/changelog.rst` gains a `Migrating from 0.7.0 to 0.7.1` section covering the `typst_authors` route change, the nine-parameter `params` enumeration, and the `lang` argument requirement, each with a before/after code fragment (D-09)."
    - "`tests/test_docs_contract_claims_gate.py` still reports 8 passed with zero skips after the migration section lands — the D-22 / D-09 interaction is checked, not assumed."
  prohibitions:
    - statement: "MUST NOT let the `## [0.7.1]` entry understate a user-facing break. CONF-10 removes a live config value whose absence Sphinx never warns about, CONF-11 changes what a declared `params` dict means, and CONF-12 turns an undeclared `lang` into a hard compile error. The version number carries no warning at all (D-01 kept `0.7.1` over `0.8.0`), so the CHANGELOG is the only surface a reader has; an under-stated entry means someone upgrades a patch release and silently loses author metadata or breaks a working build."
      status: unverified
    - statement: "MUST NOT drop or anonymise the first external contribution this project has ever shipped. `@christianwehe`'s credit must survive into the GitHub Release body, which is generated from this exact CHANGELOG section by `scripts/extract_changelog_section.py`; reducing the attribution to an issue number would render the contribution anonymous on the published release notes."
      status: unverified
  artifacts:
    - path: "CHANGELOG.md"
      provides: "the curated `## [0.7.1]` release entry, its `### Removed` section, and the rolled-over tail link block"
      contains: "## [0.7.1]"
    - path: "docs/source/changelog.rst"
      provides: "the hand-written `Migrating from 0.7.0 to 0.7.1` section with before/after fragments"
      contains: "Migrating from 0.7.0 to 0.7.1"
    - path: "tests/test_changelog_page_gate.py"
      provides: "the release-coverage tuple extended to the new release"
      contains: "0.7.1"
  key_links:
    - from: "CHANGELOG.md `## [0.7.1]` heading"
      to: "scripts/extract_changelog_section.py"
      via: "the extractor's positional algorithm bounds the section between this heading and the next `## [` heading; this text becomes the GitHub Release body at /gsd-complete-milestone"
      pattern: "## \\[0\\.7\\.1\\]"
    - from: "CHANGELOG.md"
      to: "docs/source/changelog.rst"
      via: "`.. include:: ../../CHANGELOG.md` with `:parser: myst_parser.sphinx_` — release history propagates automatically; only the Migration Guides tail is hand-maintained"
      pattern: "include:: \\.\\./\\.\\./CHANGELOG\\.md"
    - from: "CHANGELOG.md `## [0.7.1]` section"
      to: "tests/test_changelog_page_gate.py `RELEASE_VERSIONS`"
      via: "the gate asserts every listed release appears in the built page, so the tuple entry is only valid once the section exists"
      pattern: "\"0\\.7\\.1\""
    - from: "CHANGELOG.md tail link block"
      to: "the future `v0.7.1` git tag"
      via: "the `[0.7.1]:` line points at a releases/tag URL that only resolves once /gsd-complete-milestone pushes the tag — writing the link is prep, pushing the tag is publish"
      pattern: "releases/tag/v0\\.7\\.1"
---

<objective>
Curate the `## [0.7.1]` CHANGELOG entry, roll over the tail link block, add the matching
"Migrating from 0.7.0 to 0.7.1" section to the published changelog page, and extend the page gate's
release tuple.

Purpose: discharges ROADMAP Phase 46 SC#2. Because D-01 kept the version at `0.7.1`, the version
number carries no warning — so this entry is the only surface that can tell a user this patch release
may break their working configuration. D-02 therefore marks the breakage three ways at once; D-04
puts the "we promised a major release and removed it in a patch" fact in the `### Removed` bullet,
where it reaches the GitHub Release body; D-24/D-25 fold in the milestone's sixth user-visible change,
PR #131, with its contributor's credit.

Output: a `CHANGELOG.md` the release automation can extract verbatim, a current published changelog
page, and a page gate that knows about the new release.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-CONTEXT.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `46-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan owns
five of its rows: the `## [0.7.1]` heading, the `### Removed` section under it, the `[0.7.1]:` tail
link-block line, the `Migrating from 0.7.0 to 0.7.1` section in `docs/source/changelog.rst`, and the
`"0.7.1"` entry in `RELEASE_VERSIONS`.

<!-- planner-discipline-allow: QUA-01 -->
<!-- planner-discipline-allow: QUA-02 -->
<!-- planner-discipline-allow: QUA-03 -->
<!-- planner-discipline-allow: QUA-04 -->
<!-- planner-discipline-allow: ### Known Limitations -->

<tasks>

<task type="auto">
  <name>Task 1: Write the curated `## [0.7.1]` entry and roll over the tail link block</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — provisioning is mandatory.
    - `CHANGELOG.md` lines 1-90 — the single `## [Unreleased]` heading at line 8, the merged Issue #130 bullet plan 46-01 left under it, and the `## [0.7.0]` entry (lines 17-73) which is the structural model: lead paragraph, `### Added` / `### Changed` / `### Fixed`, `### Verified`, bullets in the `- **Bold summary (REQ-ID, REQ-ID)** — description` form running 3-5 lines each.
    - `CHANGELOG.md`'s last 25 lines — the tail link block: a most-recent-first flat list of `[X.Y.Z]: https://github.com/YuSabo90002/typsphinx/releases/tag/vX.Y.Z` lines terminated by `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.7.0...HEAD`.
    - `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md` § 7 — the quotable CONF-08 source text with the measured `index.typ` → `quickstartdefaultgate.typ` pair and the untemplated→templated structure change. Quote it; do not re-derive it (D-07).
    - `.planning/REQUIREMENTS.md` § "v1 Requirements" — the verbatim wording of TBL-04, TBL-05, FIG-01, TOC-01, CONF-08, CONF-09, CONF-10, CONF-11, CONF-12, BLD-01, DOC-11, DOC-12, DOC-13.
    - `git show origin/main:CHANGELOG.md` — PR #131's own 13-line `### Fixed` bullet, the compression source for D-24.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md` § `CHANGELOG.md` — the section-order template, the identifier-parenthesis convention, and the explicit note that `### Removed` has no in-repo analog.
  </read_first>
  <action>
Provision first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`; run Python
through `uv run`.

Restructure the top of `CHANGELOG.md` so the single `## [Unreleased]` heading at line 8 retains only
its `### Planned for Future Releases` subsection and its five items, and a new `## [0.7.1]` section
sits immediately below it and immediately above `## [0.7.0]`. Date the heading with today's UTC date
in `## [0.7.1] - YYYY-MM-DD` form (`date -u +%F`). Move the Issue #130 bullet plan 46-01 parked under
`## [Unreleased]` into the new section's `### Fixed`, compressed as described below — do not leave a
copy behind. The transition shape to copy is this file's own existing `## [Unreleased]` →
`## [0.7.0]` boundary.

Write a lead paragraph of 2-4 sentences on the D-06 axis — **"the configuration the documentation
promises actually takes effect"** — which is the project's own core-value sentence and covers the
largest share of this milestone at once (CONF-08, CONF-09, DOC-13, CONF-11, CONF-12). In the same
paragraph, state plainly that this patch release can break a working configuration (D-02 a). Do not
open with the warning: D-02 a only requires it be present, and leading with it would bury the fact
that this release mostly repairs things that were broken.

Then write exactly **eight** bullets, cut at user-visible-change granularity with requirement IDs in
trailing parentheses (D-05), distributed across the five sections below (D-10). Each bullet follows
the house form and runs 3-5 lines, matching `## [0.7.0]`'s own bullets.

| # | Section | Subject | IDs in trailing parentheses | Facts the bullet must carry |
|---|---------|---------|------------------------------|------------------------------|
| 1 | `### Added` | `typst_documents` now has a default, so following the Quick Start produces a PDF | CONF-08, DOC-11 | Both measured consequences for a project that never set `typst_documents`, quoted from `44-GATE-EVIDENCE-03.md` § 7: the emitted filename changes from `index.typ` to a name derived from `project` (measured `quickstartdefaultgate.typ`), **and** the emitted body changes from untemplated to fully templated (D-07). |
| 2 | `### Changed` | An explicit `typst_documents` entry's title and author reach the rendered PDF | CONF-09 | Entry `[2]` (title) and `[3]` (author) now override `project` / `author`, as Sphinx's LaTeX builder does; they were silently ignored before, so a project that set them sees its rendered title/author change. |
| 3 | `### Changed` | **Breaking:** a declared `typst_template_function` `params` dict is now the complete parameter set | CONF-11 | Prefix the bullet text with `**Breaking:**` (D-02 b). A project that declares `params` today must now enumerate every parameter it wants; typsphinx no longer merges its own values into a declared dict, so a partial dict silently changed the render before and is now exclusive. |
| 4 | `### Changed` | **Breaking:** the auto-derived `lang` reaches every non-package template route, and the published contract now matches what typsphinx passes | CONF-12, DOC-13 | Prefix with `**Breaking:**`. A custom template that does not declare a `lang` parameter now fails the compile with `unexpected argument: lang`; the documented custom-template parameter contract was corrected to the nine parameters typsphinx actually passes. |
| 5 | `### Fixed` | Nested tables and figures no longer destroy the enclosing structure, and an empty-titled caption still anchors its ids | TBL-04, TBL-05, FIG-01, TOC-01 | Four rendering-structure repairs in one bullet: a table nested in a `list-table` cell no longer replaces the outer table's body; a figure nested in a figure no longer drops the outer caption; a captioned table whose title renders empty still anchors its ids so a surviving reference resolves; and a document reached through a `toctree` renders its headings one level deeper than its parent, so the PDF outline nests instead of being flat. |
| 6 | `### Fixed` | Absolute image URIs from Sphinx's image converter or downloader no longer abort the Typst compile | Issue #130, PR #131, @christianwehe | The D-24 compression: keep the user-visible fact — building with an image-conversion extension (`sphinxcontrib.rsvgconverter`, `sphinxcontrib.inkscapeconverter`, `sphinx.ext.imgconverter`) or a downloaded remote image copied no image at all and made the Typst compile abort with "file not found". Drop the internal mechanism entirely (`os.path.join()` swallowing its first argument once the second is absolute; the bogus `../..` depth prefix) — that belongs in the PR, not the release notes. Credit `@christianwehe` inside the trailing parentheses alongside the issue and PR identifiers (D-25); this CHANGELOG has no `Thanks`/attribution phrase precedent, so use the existing identifier slot rather than inventing one. Target 3-5 lines, against the source bullet's 13. |
| 7 | `### Fixed` | A malformed docname fails with an actionable typsphinx error, and the published changelog page is current | BLD-01, DOC-12 | A non-`str` docname reaching `TypstPDFBuilder.finish()` now raises an actionable typsphinx error instead of a raw `TypeError` from deep inside the builder; and the published documentation's changelog page, frozen at 0.4.0 for two years, now carries every release. |
| 8 | `### Removed` | **Breaking:** the `typst_authors` config value is removed | CONF-10 | Prefix with `**Breaking:**`. State the D-04 fact plainly: 0.7.0's documentation announced removal in a future **major** release, and this patch release removes it. State the measured consequence: `typst_authors` is now an unregistered `conf.py` variable, which Sphinx ignores without any warning, so a project that still sets it loses its author information silently. Point the reader at the migration guide for the rewrite. Do **not** describe a deprecation shim — none exists, by D-03. |

`### Removed` is the first such section in this file's history (D-02 c); there is no in-repo analog, so
follow Keep a Changelog's own convention for its shape only. Place it after `### Fixed` and before
`### Verified`.

Close with a `### Verified` section carrying the same three items as `## [0.6.5]` and `## [0.7.0]`,
with the first one scoped to runtime (D-08): "No new **runtime** dependencies across the full
milestone diff" — the wording matters, because the milestone diff does add `myst-parser` to the
`docs` extra, and an unscoped claim would be false to anyone who diffs `uv.lock`. The other two are
the four bundled `@preview` package version strings unchanged across all four sync surfaces, and the
full-corpus `-b typstpdf` re-run remaining fatal-free. Plan 46-05 produces the evidence these three
lines assert; do not invent figures here.

Deliberately **absent** from the entry, and this is not an oversight: QUA-01 (a docstring fix),
QUA-02 (a warning-block refactor), QUA-03 (planning-record hygiene) and QUA-04 (the `tox-uv` →
`tox-uv-bare` dev-extra swap, D-19) carry no user-visible change and get no bullet. Neither is there a
`### Known Limitations` section for the two `TypstBuilder._track_image()` defects — D-27 places their
disclosure in `46-HANDOFF.md` only, after the full counter-case was argued and declined.

**Tail link block rollover.** Two edits at the end of the file: insert
`[0.7.1]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.7.1` immediately above the
`[0.7.0]:` line, and change the final line's compare base from `v0.7.0...HEAD` to `v0.7.1...HEAD`.
Do not edit any historical entry or any other link line.

Take no irreversible action: no `git tag`, no tag push, no PyPI upload, no GitHub Release, no PR.
`REQUIREMENTS.md` is not touched by this plan (D-26) — PR #131 gets no requirement ID.
  </action>
  <verify>
    <automated>uv run python scripts/extract_changelog_section.py 0.7.1 > "${TMPDIR:-/tmp}/46-03-body.txt" && test -s "${TMPDIR:-/tmp}/46-03-body.txt" && test "$(grep -c '^## \[Unreleased\]' CHANGELOG.md)" = "1" && test "$(grep -c '^## \[0.7.1\]' CHANGELOG.md)" = "1" && test "$(grep -c '^### Removed$' CHANGELOG.md)" = "1" && test "$(grep -c '\*\*Breaking:\*\*' CHANGELOG.md)" -ge "3" && grep -q 'christianwehe' "${TMPDIR:-/tmp}/46-03-body.txt" && grep -q 'runtime' "${TMPDIR:-/tmp}/46-03-body.txt" && grep -q 'releases/tag/v0.7.1' CHANGELOG.md && grep -q 'compare/v0.7.1\.\.\.HEAD' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## \[0.7.1\]' CHANGELOG.md` returns `1`, and the heading line matches the `## [0.7.1] - YYYY-MM-DD` form.
    - `grep -c '^## \[Unreleased\]' CHANGELOG.md` returns `1`; the lines between that heading and the `## [0.7.1]` heading consist only of the `### Planned for Future Releases` heading and its five list items.
    - The `## [0.7.1]` section contains all five headings `### Added`, `### Changed`, `### Fixed`, `### Removed`, `### Verified`, and exactly 8 top-level bullets across the first four of them.
    - `grep -c '^### Removed$' CHANGELOG.md` returns `1` (the first occurrence in the file's history).
    - At least three bullets inside the `## [0.7.1]` section carry a `**Breaking:**` prefix, and they are the CONF-10, CONF-11 and CONF-12 bullets.
    - The `## [0.7.1]` section contains every one of these identifier strings: `CONF-08`, `DOC-11`, `CONF-09`, `CONF-11`, `CONF-12`, `DOC-13`, `TBL-04`, `TBL-05`, `FIG-01`, `TOC-01`, `Issue #130`, `PR #131`, `christianwehe`, `BLD-01`, `DOC-12`, `CONF-10`.
    - The `## [0.7.1]` section contains none of `QUA-01`, `QUA-02`, `QUA-03`, `QUA-04`.
    - The `### Verified` item about dependencies scopes its claim to runtime (the word `runtime` appears in it).
    - The CONF-08 bullet contains both `index.typ` and `quickstartdefaultgate.typ`.
    - `uv run python scripts/extract_changelog_section.py 0.7.1` exits 0 and prints a non-empty body whose last line is the last line of the 0.7.1 section — it contains no text from `## [0.7.0]` and none from `## [Unreleased]`.
    - `CHANGELOG.md` contains `[0.7.1]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.7.1` on the line immediately above the `[0.7.0]:` line, and its final link line reads `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.7.1...HEAD`.
    - `git diff --stat CHANGELOG.md` shows no line removed from any section at or below `## [0.7.0]`.
    - `git tag -l v0.7.1` produces no output.
  </acceptance_criteria>
  <done>`CHANGELOG.md` carries a curated, triple-marked `## [0.7.1]` entry the extractor returns cleanly, with the tail link block rolled over and no historical entry disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Add the "Migrating from 0.7.0 to 0.7.1" section to the published changelog page</name>
  <files>docs/source/changelog.rst</files>
  <read_first>
    - `docs/source/changelog.rst` lines 1-70 — the `.. include:: ../../CHANGELOG.md` directive with `:parser: myst_parser.sphinx_`, the `Migration Guides` heading, and the existing `Migrating from 0.6.x to 0.7.0` and `Migrating from 0.5.x to 0.6.x` sections. The latter already uses the `**Breaking:**` prefix — that is the vocabulary to reuse.
    - `docs/source/user_guide/templates.rst` — the documented custom-template parameter contract, and the `.. code-block:: python` / `.. code-block:: typst` directives this docs corpus already uses. Match those directives; do not invent a lexer name.
    - `docs/source/_typst/custom_template.typ` lines 60-80 — the nine-parameter declaration including `lang`, which is the shape a reader must copy.
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md` § D-F — the rationale (LaTeX parity) this section must carry, per D-04's split.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md` § `docs/source/changelog.rst` — including the explicit note that code fragments have no in-repo analog in a Migrating section.
  </read_first>
  <action>
Insert a new subsection titled `Migrating from 0.7.0 to 0.7.1` directly under the `Migration Guides`
heading and **above** `Migrating from 0.6.x to 0.7.0`, matching the page's most-recent-first ordering.
Underline it with `~` characters to the same length as the title, as the sibling sections do.

Open with one or two sentences of prose noting that this patch release carries three breaking
configuration changes, and that each item below shows the rewrite. Then write exactly three items,
each prefixed `**Breaking:**` and each carrying a before/after fragment. Use `.. code-block:: python`
for `conf.py` fragments and `.. code-block:: typst` for Typst template fragments — both directives are
already in use in `docs/source/user_guide/templates.rst`, so no new lexer is introduced. Every
existing Migrating section on this page is prose-bullets-only; code fragments enter this page here for
the first time, so this establishes the pattern rather than following one.

Item 1 — `typst_authors` is removed. Show the old `conf.py` line setting `typst_authors`, then the
replacement through `typst_template_function`'s `params` route, placing the same author dictionary at
`params["authors"]`. Carry the D-04 **rationale** here (the fact itself lives in the CHANGELOG's
`### Removed` bullet): the value was pure sugar over the `params` route — Phase 44.2 D-06 measured the
two routes producing a byte-identical `authors:` value, differing only in the order of named arguments
in the emitted call, which is semantically irrelevant in Typst — and removing it brings the config
surface to LaTeX parity (45.1 D-F). State the failure mode a reader needs to know: a leftover
`typst_authors` is an unregistered `conf.py` variable, which Sphinx ignores without warning, so author
information is lost silently rather than loudly.

Item 2 — a declared `params` dict is now the complete parameter set (CONF-11). Show a `conf.py`
fragment that declares a partial `params` today, then the corrected fragment enumerating all nine
parameters by name: ``title``, ``authors``, ``date``, ``papersize``, ``fontsize``, ``lang``,
``toctree_maxdepth``, ``toctree_numbered``, ``toctree_caption``. The point the fragment must convey —
which prose alone does not — is "write all nine".

Item 3 — a custom template must declare `lang` (CONF-12). Show the Typst template signature line a
reader has today without `lang`, the exact error a build now fails with
(`unexpected argument: lang`), and the corrected signature declaring `lang` with a default. The
shipped `docs/source/_typst/custom_template.typ` already declares all nine and is the reference shape.

Do not touch the `.. include::` directive, and do not hand-write any release history — the release
entries reach this page automatically through the include, which is why the migration section is the
only hand-written surface here.

After the edit, re-run `uv run pytest tests/test_docs_contract_claims_gate.py -v`. Two facts govern
this check. First, the premise correction: `docs/source/changelog.rst` **already** satisfies
`_page_makes_contract_claim()` today via its existing "Migrating from 0.5.x to 0.6.x" section, and the
module is 8/8 green on POSIX right now — so this section adds more matching text to a page that
already passes, and does not "newly" make it claim. Any sequencing rationale that says the page
currently makes no claim is false. Second, the page is listed in `EXCLUDED_CLAIM_PAGES` with a reason
string describing it as a historical release-note migration guide; that string is now narrower than
the page's content, but **do not edit it** — this phase permits exactly two non-planning code edits
(plan 46-01's `as_posix()` repair and task 3's `RELEASE_VERSIONS` append), and this would be a third.
Record the observation for `46-HANDOFF.md` § "Not done in this phase, by design" instead. If the
module goes red, stop and report rather than editing the module to make it green.
  </action>
  <verify>
    <automated>grep -q 'Migrating from 0.7.0 to 0.7.1' docs/source/changelog.rst && test "$(grep -c '\*\*Breaking:\*\*' docs/source/changelog.rst)" -ge "5" && grep -q 'code-block:: python' docs/source/changelog.rst && grep -q 'code-block:: typst' docs/source/changelog.rst && uv run pytest tests/test_docs_contract_claims_gate.py -q --junit-xml="${TMPDIR:-/tmp}/46-03-claims.xml" && grep -q 'skipped="0"' "${TMPDIR:-/tmp}/46-03-claims.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/46-03-claims.xml"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/source/changelog.rst` contains a `Migrating from 0.7.0 to 0.7.1` title with a `~`-underline of matching length, positioned between the `Migration Guides` heading and `Migrating from 0.6.x to 0.7.0`.
    - The new section contains exactly three items, each beginning with `**Breaking:**`.
    - The new section contains at least one `.. code-block:: python` directive and at least one `.. code-block:: typst` directive.
    - The new section names all nine published parameters: `title`, `authors`, `date`, `papersize`, `fontsize`, `lang`, `toctree_maxdepth`, `toctree_numbered`, `toctree_caption`.
    - The new section contains the literals `typst_authors`, `typst_template_function`, `params`, and `unexpected argument: lang`.
    - `git diff docs/source/changelog.rst` shows no change to the `.. include::` directive lines and no change to any pre-existing Migrating section.
    - `uv run pytest tests/test_docs_contract_claims_gate.py` produces a JUnit `testsuite` element with 8 tests, `skipped="0"`, `failures="0"`, `errors="0"`.
    - `git diff --name-only tests/` produces no output for this task (the gate module is unchanged here).
  </acceptance_criteria>
  <done>The published changelog page carries a live migration guide for this release with runnable before/after fragments for all three breaking changes, and the contract-claims gate is still fully green on POSIX after the edit.</done>
</task>

<task type="auto">
  <name>Task 3: Extend `RELEASE_VERSIONS` and prove the page gate in a docs-extras environment</name>
  <precondition>`CHANGELOG.md` already contains a `## [0.7.1]` heading with a non-empty body (task 1 of this plan is complete) — the gate asserts every listed release actually appears in the built page, so the tuple entry is invalid before the section exists.</precondition>
  <files>tests/test_changelog_page_gate.py</files>
  <read_first>
    - `tests/test_changelog_page_gate.py` lines 1-70 — the module docstring's honest-skip guards, the `MYST_PARSER_AVAILABLE` / `TYPST_AVAILABLE` flags, and the `RELEASE_VERSIONS` tuple at lines 49-63 with the comment above it naming "The 12 releases … (0.4.4 through 0.7.0, inclusive)".
    - `tests/test_changelog_page_gate.py`'s `TestChangelogPageContentCoverage` and `TestChangelogIncludeCompilesToPdf` classes — the `@pytest.mark.slow` and `skipif` decorators that must survive unchanged.
    - `pyproject.toml` lines 45-60 — the `docs` extra, which is where `myst-parser` lives (not the `dev` extra).
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md` § "Pitfall 2".
  </read_first>
  <action>
Append `"0.7.1",` to the `RELEASE_VERSIONS` tuple, immediately after `"0.7.0",`. Update the comment
directly above the tuple so its count reads 13 rather than 12 and its range reads through `0.7.1`
rather than through `0.7.0`; leave the rest of that comment's wording intact. Change nothing else in
the module — in particular do not weaken, remove, or narrow the `@pytest.mark.slow` markers or the
`skipif` guards on the two content-coverage classes: the honest skip is deliberate design, and this
task's job is to make those classes *run*, not to make them stop skipping by deletion.

The gate must then be proved in an environment that actually has `myst-parser`. It lives in the
`docs` extra, not `dev`, so running the module against the plain dev `.venv` silently **skips**
`TestChangelogPageContentCoverage` and `TestChangelogIncludeCompilesToPdf` — the module then passes
without ever asserting that the release tuple reaches the built page or the compiled PDF. Run it with
both extras present: `uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -v`,
and emit a JUnit report so the skip count is machine-checkable rather than eyeballed from the summary
line.

Record the summary line and the JUnit skip/failure counts in the plan SUMMARY. A run reporting
"all green" without checking the skip count has not proved anything about `RELEASE_VERSIONS`.

Take no irreversible action.
  </action>
  <verify>
    <automated>grep -q '"0.7.1",' tests/test_changelog_page_gate.py && uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -q --junit-xml="${TMPDIR:-/tmp}/46-03-page.xml" && grep -q 'skipped="0"' "${TMPDIR:-/tmp}/46-03-page.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/46-03-page.xml"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_changelog_page_gate.py` contains `"0.7.1",` on the line immediately following `"0.7.0",` inside `RELEASE_VERSIONS`.
    - The comment above the tuple names 13 releases and a range ending at `0.7.1`.
    - `git diff tests/test_changelog_page_gate.py` shows changes only inside the `RELEASE_VERSIONS` tuple and its preceding comment — no decorator, guard, import, or test body is modified.
    - `uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py` produces a JUnit `testsuite` element with `skipped="0"`, `failures="0"` and `errors="0"`.
    - The run's node list includes at least one test from `TestChangelogPageContentCoverage` and at least one from `TestChangelogIncludeCompilesToPdf`, both with no `skipped` element.
    - `grep -c '^## \[0.7.1\]' CHANGELOG.md` returns `1` at the moment this task runs (the ordering dependency held).
  </acceptance_criteria>
  <done>`RELEASE_VERSIONS` knows about `0.7.1`, and the gate's content-coverage classes actually executed — proved by a zero skip count, not by a green summary line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` `## [0.7.1]` section → the published GitHub Release body | `scripts/extract_changelog_section.py` copies this text verbatim into the release notes at `/gsd-complete-milestone`; whatever it does or does not say is what users read. |
| `origin/main`'s contributor-authored bullet → this repository's release notes | External content is compressed and re-published under this project's name. |
| `docs/source/changelog.rst` → the Read the Docs `stable` build (en and ja) | The migration guide becomes published documentation on two hosted projects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-04 | Information disclosure | the `## [0.7.1]` entry understating a breaking change | high | mitigate | D-02's triple marking is asserted mechanically: the lead paragraph's plain statement, at least three `**Breaking:**` prefixes on the CONF-10/11/12 bullets, and a `### Removed` section; plus an acceptance criterion requiring every affected requirement ID to be present in the section. |
| T-46-05 | Repudiation | dropping or anonymising `@christianwehe`'s attribution during D-24's compression | medium | mitigate | The extractor's own output for `0.7.1` is grepped for `christianwehe` in `<verify>`, so the credit is asserted on the text that actually becomes the release body — not merely on the file. |
| T-46-01 | Tampering | historical CHANGELOG entries disturbed by the restructure | medium | mitigate | Acceptance asserts `git diff --stat CHANGELOG.md` removes no line at or below `## [0.7.0]`, and that the tail link block gains exactly one line and changes exactly one. |
| T-46-10 | Tampering | a test gate weakened to make it pass | high | mitigate | Task 3 forbids touching the `skipif`/`slow` guards and asserts `git diff` is confined to the tuple and its comment; the zero-skip JUnit assertion means the gate must actually run rather than be made to stop skipping by deletion. |
| T-46-02 | Tampering | `typsphinx/` source | high | mitigate | This plan's `files_modified` contains no `typsphinx/` path; acceptance on task 2 asserts `git diff --name-only tests/` is empty for that task, and the phase-level fence is re-asserted in plan 46-05's sweep. |
| T-46-SC | Tampering | npm/pip/cargo installs | medium | accept | `uv run --extra dev --extra docs` installs only what `uv.lock` already pins; no package is added. RESEARCH.md records the Package Legitimacy Gate as not applicable — zero packages evaluated, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. |
</threat_model>

<verification>
- `uv run python scripts/extract_changelog_section.py 0.7.1` exits 0 with non-empty output containing `christianwehe`.
- `grep -c '^## \[Unreleased\]' CHANGELOG.md` returns 1; `grep -c '^## \[0.7.1\]' CHANGELOG.md` returns 1; `grep -c '^### Removed$' CHANGELOG.md` returns 1.
- `uv run pytest tests/test_docs_contract_claims_gate.py` → 8 tests, zero skips, zero failures.
- `uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py` → zero skips, zero failures.
- `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` are both empty.
</verification>

<success_criteria>
- `CHANGELOG.md` carries a curated eight-bullet `## [0.7.1]` entry with a triple-marked breaking-change disclosure and a rolled-over tail link block.
- `docs/source/changelog.rst` carries a `Migrating from 0.7.0 to 0.7.1` section with before/after fragments for all three breaking changes.
- `RELEASE_VERSIONS` includes `"0.7.1"` and the page gate's content-coverage classes ran and passed with zero skips.
- No historical entry was edited, no gate was weakened, and no irreversible action was taken.
</success_criteria>

<output>
Create `.planning/phases/46-v0-7-1-release-prep-prep-only/46-03-SUMMARY.md` when done.
</output>
