---
phase: 45-documentation-currency-carried-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - docs/source/conf.py
  - docs/source/changelog.rst
  - .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md
  - .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-include-shape.md
autonomous: false
requirements: [DOC-12]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The published changelog page renders every release section of CHANGELOG.md — all 12 previously-missing versions (0.4.1, 0.4.2, 0.4.3, 0.4.4, 0.5.0, 0.6.0, 0.6.1, 0.6.2, 0.6.3, 0.6.4, 0.6.5, 0.7.0) appear in the built HTML."
    - "Both `sphinx-build -b html` and `sphinx-build -b typstpdf` over `docs/source` exit 0 with the include directive in place."
    - "The rendered changelog page carries exactly one heading whose text is `Changelog` — the included Markdown's own H1 does not collide with or duplicate the rST page title (DOC-12 adjacency)."
    - statement: "If `../../CHANGELOG.md` is absent or unreadable at build time the docs build fails loudly with a docutils include error rather than silently rendering an empty changelog page (DOC-12 empty-input edge)."
      verification: backstop
    - "`myst-parser>=5.0` appears in `[project.optional-dependencies].docs` and the `[project].dependencies` list still holds exactly its three pre-phase entries."
    - "A pre-phase docs-build warning baseline exists on disk with a named commit SHA, so `build clean` can be judged as a delta rather than as an absolute zero (neither tox docs env passes `-W`)."
  artifacts:
    - "pyproject.toml — `myst-parser>=5.0` line inside the `docs` extra"
    - "uv.lock — regenerated so tox-uv's `uv-venv-lock-runner` resolves myst-parser for the `docs` extras env"
    - "docs/source/conf.py — `myst_parser` registered in `extensions`"
    - "docs/source/changelog.rst — the `.. include:: ../../CHANGELOG.md` / `:parser: myst_parser.sphinx_` delegation, replacing the hand-maintained release history"
    - ".planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md"
    - ".planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-include-shape.md"
  key_links:
    - "docs/source/changelog.rst include → repo-root CHANGELOG.md — the single source both REL-04's extractor and the docs page read; this link is what makes Phase 46's 0.7.1 entry a one-line addition"
    - "docs/source/conf.py `extensions` → `myst_parser` registration → the `:parser: myst_parser.sphinx_` dotted path resolving inside a Sphinx build"
    - ".readthedocs.yaml `python.install.method: uv / extras: [docs]` → myst-parser present in the published RTD build without any RTD-side change"
  prohibitions:
    - "myst-parser is not added to `[project].dependencies` — the standing zero-new-runtime-dependency invariant holds."
    - "`docs/source/user_guide/templates.rst` is not edited — its four-parameter contract is Phase 45.1 / DOC-13."
    - "CHANGELOG.md is not converted away from Markdown, and is not edited at all by this plan."
    - "No `## [0.7.1]` section is added to CHANGELOG.md — Phase 46 owns it."
    - "`scripts/extract_changelog_section.py` and `.github/workflows/release.yml` are not modified."
---

<objective>
Prove DOC-12's delegation mechanism end-to-end: `docs/source/changelog.rst` stops hand-maintaining
release history and instead includes the repository's `CHANGELOG.md`, parsed as Markdown by
myst-parser, so the published page carries every release and a future release needs zero doc edits
(D-01). This is the phase's tracer — the only item with real technical risk, and it must survive both
`tox -e docs-html` (furo) and `tox -e docs-pdf` (typsphinx dogfooding itself).

Purpose: close the drift channel that let the published changelog page sit 12 releases stale, and
make Phase 46's `0.7.1` entry a one-line `CHANGELOG.md` addition rather than a re-derivation
(ROADMAP SC#2).
Output: a myst-parser `docs`-extra dependency, a registered parser, a delegating changelog page, a
pre-phase warning baseline, and a recorded include-shape decision.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-CONTEXT.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-RESEARCH.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-PATTERNS.md
@CLAUDE.md
</context>

<worktree_protocol>
Executors run in an isolated git worktree (`.git` is a FILE, not a directory). Before running ANY
command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`
2. Run every subsequent command through `uv run` (e.g. `uv run python -m sphinx …`).

Both extras are required here: `dev` for pytest/black/ruff/mypy, `docs` for furo,
sphinx-autodoc-typehints and (after task 3) myst-parser. A bare `sphinx-build` or bare `pytest`
imports the UNCHANGED main-tree package and produces misleading results.

Sphinx is invoked as `python -m sphinx` (never a resolved `sphinx-build` binary) — the same rule
`tests/test_default_typst_documents_gate.py::_run_sphinx_build` documents, to sidestep the NixOS
sandbox PATH-shadowing hazard.
</worktree_protocol>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-phase docs-build warning baseline</name>
  <files>.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md</files>
  <read_first>
    - tox.ini (the `docs-html` / `docs-pdf` env `commands` — confirm neither passes `-W`)
    - docs/source/conf.py (the extensions list and the typst_documents master entry, unmodified at this point)
    - docs/source/changelog.rst (the page as it stands today, before any edit)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-RESEARCH.md (§ Common Pitfalls, Pitfall 2)
  </read_first>
  <precondition>`typst-py` is importable in the provisioned environment (`uv run python -c "import typst"` succeeds) — the `-b typstpdf` half of the baseline cannot be captured without it.</precondition>
  <action>
    This task runs BEFORE any file in `docs/` or `pyproject.toml` is touched. RESEARCH Pitfall 2
    establishes that neither tox docs env passes `-W`, so exit code 0 does not mean zero warnings;
    ROADMAP SC#2's "build clean" bar can only be judged as a delta against a pre-phase capture, and
    pre-existing warnings elsewhere in the docs build are outside this phase's fence.

    Create a scratch output root with `mktemp -d`. Run two builds against the untouched tree:
    `uv run python -m sphinx -b html docs/source <scratch>/html` and
    `uv run python -m sphinx -b typstpdf docs/source <scratch>/pdf`, capturing stdout and stderr of
    each to separate files. Do not use `-W`, `-q`, or `-n` — the goal is to record exactly what a
    normal build says today.

    Write the evidence file with a machine-greppable header block followed by verbatim warning
    text. The header block must contain these four lines, each on its own line, in this order and
    exactly this key shape (the wave-2 delta task greps them):
    `- baseline_sha: <full HEAD sha>`, `- html_warning_count: <N>`, `- pdf_warning_count: <N>`,
    `- changelog_attributable_warning_count: <N>`.
    Counts are the number of lines matching `WARNING:` in each build's combined output;
    the changelog-attributable count is the subset of those lines whose text mentions `changelog`.
    Below the header, reproduce every `WARNING:` line verbatim under an `## HTML build` and a
    `## typstpdf build` section, and record the exit code of each build.

    Also record, in a short `## Reading` section, the interpretation rule this phase will use: the
    phase's own bar is `changelog_attributable_warning_count == 0` after the changes land, and
    `html_warning_count` / `pdf_warning_count` must not exceed the baseline values.
  </action>
  <verify>
    <automated>test -s .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md && grep -qE '^- baseline_sha: [0-9a-f]{7,40}$' .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md && grep -qE '^- html_warning_count: [0-9]+$' .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md && grep -qE '^- pdf_warning_count: [0-9]+$' .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md && grep -qE '^- changelog_attributable_warning_count: [0-9]+$' .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: the evidence file contains all four header lines matching `^- baseline_sha: [0-9a-f]{7,40}$`, `^- html_warning_count: [0-9]+$`, `^- pdf_warning_count: [0-9]+$`, `^- changelog_attributable_warning_count: [0-9]+$`.
    - Source: the file contains both an `## HTML build` and a `## typstpdf build` section, each recording that build's exit code.
    - CLI: `git status --porcelain -- docs/ pyproject.toml uv.lock` prints nothing after this task — the baseline is measured against an untouched tree.
    - Behavior: `baseline_sha` equals `git rev-parse HEAD` at the time the builds ran.
  </acceptance_criteria>
  <done>A committed evidence file records, against a named SHA, every WARNING line both docs builds emit today plus the three counts the wave-2 delta check compares against.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package-legitimacy gate for myst-parser before the pyproject.toml edit</name>
  <what-built>
    Nothing yet — this is the package-legitimacy gate that must clear before `myst-parser` is added
    to `pyproject.toml`.

    Background the executor must present: the automated package-legitimacy seam returned a `SUS`
    verdict for `myst-parser` with the single reason `unknown-downloads` — its downloads API call
    returned null, not a low number. Research cross-checked independently (pypistats via WebSearch,
    ~1.5M downloads/week, ~7.1M/month) and found the flag to be a data-availability artifact rather
    than an adverse signal. `myst-parser` is the executablebooks/Jupyter Book family's Markdown
    parser for Sphinx; source repo `github.com/executablebooks/MyST-Parser`; latest release 5.1.0
    uploaded 2026-05-13; `requires_dist` for 5.1.0 is `docutils<0.23,>=0.20` and `sphinx<10,>=8`,
    both intersecting this repo's pins cleanly. The 4.x line caps `sphinx<9` / `docutils<0.22`,
    which this repo's `sphinx>=9.1,<10` / `docutils>=0.21,<0.23` violate — hence the `>=5.0` floor.

    Reversibility rating for the decision this gate covers: **costly** (not one-way). Inside this
    phase the change is one line plus a lock regeneration. It becomes costly only once v0.7.1 ships,
    because the `docs` extra is published package metadata and reverting after release needs another
    release. No `checkpoint:decision` is inserted for a costly rating.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/myst-parser/ and confirm the project page loads, the latest
       version is 5.x, and the listed source repository is `executablebooks/MyST-Parser`.
    2. Open https://github.com/executablebooks/MyST-Parser and confirm it is an active, non-archived
       repository under the `executablebooks` organisation.
    3. Confirm you accept adding `myst-parser>=5.0` to `[project.optional-dependencies].docs` in
       `pyproject.toml` — the `docs` extra only, never `[project].dependencies`.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the pyproject.toml edit, or describe the concern.</resume-signal>
</task>

<task type="tracer">
  <name>Task 3: End-to-end changelog delegation — one path from CHANGELOG.md to both rendered outputs</name>
  <files>pyproject.toml, uv.lock, docs/source/conf.py, docs/source/changelog.rst, .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-include-shape.md</files>
  <read_first>
    - pyproject.toml (the `[project].dependencies` block and the `[project.optional-dependencies].docs` block — the docs extra's lower-bound-only pin house style)
    - docs/source/conf.py (the `extensions` list at lines 35-42)
    - docs/source/changelog.rst (the full current page — the sections to keep, correct, and delete)
    - CHANGELOG.md lines 1-12 (the preamble the include must skip, and the first heading it must start at)
    - docs/source/index.rst (the Development toctree that lists `changelog`)
    - .readthedocs.yaml (confirm `python.install` uses `uv sync` with the `docs` extra, so no RTD-side change is needed)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-RESEARCH.md (§ Architecture Patterns Pattern 1; § Open Questions 1 and 2)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-PATTERNS.md (§ `pyproject.toml` docs extra; § `docs/source/changelog.rst`)
  </read_first>
  <reversibility rating="costly">Adding `myst-parser` to the published `docs` extra is one line plus `uv lock` inside this phase, but it becomes part of v0.7.1's published package metadata at release; reverting after that needs another release. Cleared by the preceding blocking-human legitimacy gate.</reversibility>
  <action>
    Wire ONE path — repo-root `CHANGELOG.md` through myst-parser through `docs/source/changelog.rst`
    through both builders — end to end. Four edits, then a real build, then a recorded shape decision.

    **1. `pyproject.toml`.** Add the entry `myst-parser>=5.0` to the `docs` list under
    `[project.optional-dependencies]`, matching the lower-bound-only house style of the three
    entries already there (`furo>=2024.0`, `sphinx-autodoc-typehints>=1.0`, `sphinx-intl>=2.0`) —
    no upper cap. Do not touch `[project].dependencies`; per D-01 the zero-new-runtime-dependency
    invariant is untouched. The `>=5.0` floor is not negotiable: the 4.x line's own metadata caps
    `sphinx<9` and `docutils<0.22`, both violated by this repo's pins.

    **2. `uv.lock`.** Regenerate with `uv run uv lock` (or `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv lock`)
    and commit it, exactly as the existing three docs-extra packages already are. tox's docs envs
    use `runner = uv-venv-lock-runner` with `extras = docs`, so without a regenerated lock the
    `docs-html`/`docs-pdf` environments will not see the new package.

    **3. `docs/source/conf.py`.** Add `"myst_parser"` to the `extensions` list. RESEARCH assumption
    A1 records this as defensive registration — every worked example in the official docs shows it
    present even for the include-only use case, and the parser module reads Sphinx `app.config` for
    MyST settings. Place it before `"typsphinx"` so the list stays grouped third-party-then-own.
    There are zero `.md` files under `docs/`, so registering the parser adds no new documents and
    no orphan-document warnings. Do not set `myst_enable_extensions`, `myst_heading_anchors`, or any
    other `myst_*` config value — defaults only, so no raw-HTML or anchor-generation surface is opened.

    **4. `docs/source/changelog.rst`.** Replace the hand-maintained release history with the
    delegation. Concretely: keep the page title `Changelog` and its two framing paragraphs; delete
    every hand-maintained per-release section (each `Version X.Y.Z` heading and its body, including
    the one carrying the stale current-release marker) and delete the stale per-version support-status
    section outright — the one at lines 133-138 claiming v0.3.x is the current stable release (D-06:
    it is a per-release hand-update site, precisely the drift channel D-01 closes).
    Keep `Migration Guides`, `Deprecation Policy`, `Upcoming Features`, `Versioning`,
    `Release Process` and `See Also` in place for now — plan 45-02 corrects their contents.
    In place of the deleted release history, add a level-2 section titled `Release History`
    containing the include: directive `.. include:: ../../CHANGELOG.md` with the two options
    `:parser: myst_parser.sphinx_` and `:start-line: 7`.

    The `:start-line: 7` value is 0-based (docutils convention), so the included range begins at
    CHANGELOG.md file line 8, which is the `## [Unreleased]` heading — skipping the file's own
    `# Changelog` H1 and the Keep-a-Changelog/SemVer attribution paragraph, both of which the rST
    page already carries. Plan 45-02 pins CHANGELOG.md lines 1-7 as byte-invariant so this offset
    stays correct after that plan's edits, which is how RESEARCH Pitfall 1's ordering hazard is
    discharged without a re-measurement.

    **5. Build both, and settle the shape empirically.** Run
    `uv run python -m sphinx -b html docs/source <scratch>/html` and
    `uv run python -m sphinx -b typstpdf docs/source <scratch>/pdf`, capturing output. Two questions
    RESEARCH left open can only be settled here:
    (a) whether the included fragment's heading levels nest sanely under the toctree offset Phase
    44.1 applies to included documents; (b) whether CommonMark's shortcut-reference resolution of
    the bracketed version headings against CHANGELOG.md's own tail link-definition block renders
    acceptably as linked headings.

    If either build emits a warning attributable to the changelog page, iterate on the include's
    shape within this task using this ordered ladder, and stop at the first rung that produces zero
    changelog-attributable warnings: (i) the shape above; (ii) move the include into a deeper rST
    section so the fragment's headings nest one further level down; (iii) add `:end-before:` bounded
    at the tail link-definition block, accepting the loss of linked headings. Do NOT reach for
    `suppress_warnings` — a suppressed warning is not a clean build.

    Record the outcome in `45-GATE-EVIDENCE-01-include-shape.md`: the final directive verbatim, which
    ladder rung it is, the answers measured for (a) and (b), and both builds' exit codes and
    changelog-attributable warning counts. Also record the two consequences the phase must carry
    forward: every line the include newly surfaces renders untranslated on the `ja` site until the
    separate `typsphinx-doc-translations` catalogs are regenerated (out of this repo's scope, flag at
    milestone close); and the `.readthedocs.yaml` `uv sync --extras docs` path means no RTD-side
    change is needed.
  </action>
  <verify>
    <automated>uv run python - <<'PY'
import pathlib, re, subprocess, sys, tempfile
out = tempfile.mkdtemp()
r = subprocess.run([sys.executable, "-m", "sphinx", "-b", "html", "docs/source", out + "/h"], capture_output=True, text=True)
assert r.returncode == 0, r.stdout + r.stderr
noisy = [ln for ln in (r.stdout + r.stderr).splitlines() if "WARNING" in ln and "changelog" in ln]
assert not noisy, "changelog-attributable warnings: " + repr(noisy)
html = pathlib.Path(out, "h", "changelog.html").read_text(encoding="utf-8")
missing = [v for v in ["0.4.1","0.4.2","0.4.3","0.5.0","0.6.0","0.6.1","0.6.2","0.6.3","0.6.4","0.6.5","0.7.0"] if v not in html]
assert not missing, "missing from changelog.html: " + repr(missing)
titles = re.findall(r"<h[1-6][^>]*>\s*Changelog", html)
assert len(titles) == 1, "expected exactly one Changelog heading, found " + str(len(titles))
p = subprocess.run([sys.executable, "-m", "sphinx", "-b", "typstpdf", "docs/source", out + "/p"], capture_output=True, text=True)
assert p.returncode == 0, p.stdout + p.stderr
noisy = [ln for ln in (p.stdout + p.stderr).splitlines() if "WARNING" in ln and "changelog" in ln]
assert not noisy, "changelog-attributable warnings (typstpdf): " + repr(noisy)
pdf = pathlib.Path(out, "p", "typsphinx.pdf")
assert pdf.exists() and pdf.read_bytes()[:4] == b"%PDF", "typstpdf did not produce a valid PDF"
print("OK both builders")
PY</automated>
  </verify>
  <acceptance_criteria>
    - Source: `docs/source/changelog.rst` contains the line `.. include:: ../../CHANGELOG.md` and the option line `:parser: myst_parser.sphinx_`.
    - Source: `uv run python -c "import tomllib,pathlib; d=tomllib.loads(pathlib.Path('pyproject.toml').read_text()); assert any(e.startswith('myst-parser') for e in d['project']['optional-dependencies']['docs']); assert len(d['project']['dependencies'])==3; print('ok')"` prints `ok`.
    - Source: `docs/source/conf.py`'s `extensions` list contains `"myst_parser"`.
    - Source: `grep -c 'myst-parser' uv.lock` is greater than 0 (the lock was regenerated).
    - Behavior: `uv run python -m sphinx -b html docs/source <tmp>` exits 0, and the resulting `changelog.html` contains every one of `0.4.1`, `0.4.2`, `0.4.3`, `0.5.0`, `0.6.0`, `0.6.1`, `0.6.2`, `0.6.3`, `0.6.4`, `0.6.5`, `0.7.0`. (`0.4.4` is backfilled in plan 45-02 and is asserted there.)
    - Behavior: `changelog.html` contains exactly one heading element whose text is `Changelog`.
    - Behavior: `uv run python -m sphinx -b typstpdf docs/source <tmp>` exits 0 and writes `typsphinx.pdf` whose first four bytes are the `%PDF` magic.
    - CLI: `grep -c 'Version 0.4.0 (Current)' docs/source/changelog.rst` returns 0.
    - CLI: `grep -c 'Development Status' docs/source/changelog.rst` returns 0.
    - Source: `45-GATE-EVIDENCE-01-include-shape.md` names the final directive verbatim, the ladder rung chosen, and the measured answers to the heading-nesting and linked-heading questions.
  </acceptance_criteria>
  <done>The published changelog page is generated from `CHANGELOG.md` by myst-parser, both builders render it, all release sections present in CHANGELOG.md today reach the rendered HTML, and the include's final shape is recorded with the evidence that chose it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → build environment | A new build-time dependency (`myst-parser`) enters the `docs` extra and is installed by RTD and by CI's docs lane |
| repo-root file → Sphinx source tree | `.. include:: ../../CHANGELOG.md` reads a path OUTSIDE `docs/source/`, crossing the documented source root |
| CHANGELOG.md content → rendered HTML/PDF | Markdown authored in the repo is parsed by a new parser and rendered into published output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-SC | Tampering | `pyproject.toml` docs extra — `myst-parser` install | medium | mitigate | Package legitimacy gate ran (RESEARCH § Package Legitimacy Audit: `SUS` traced to a null downloads-API response, contradicted by ~1.5M weekly downloads); a blocking-human checkpoint precedes the `pyproject.toml` edit; the floor is pinned `>=5.0`; `uv.lock` is regenerated and committed so the resolved artifact is reviewable |
| T-45-01 | Information Disclosure | `.. include:: ../../CHANGELOG.md` path traversal out of `docs/source/` | low | accept | The path is a fixed literal in a repo-tracked file, never user- or build-input-derived; the target is first-party and already public on GitHub. Nothing outside the repository is reachable |
| T-45-02 | Denial of Service | Docs build when `CHANGELOG.md` is missing (e.g. a partial checkout, or the `ja` submodule build) | low | mitigate | docutils' `include` fails loudly with a build error rather than rendering an empty page; recorded as a backstop truth in `must_haves` and re-checked at verify time |
| T-45-03 | Tampering | myst-parser rendering raw HTML from Markdown into published pages | low | accept | No `myst_*` config value is set, so `html_admonition` / `html_image` / raw-HTML extensions stay off at their defaults, and `CHANGELOG.md` is first-party repo-tracked content, not user submission |
| T-45-04 | Spoofing | RTD `ja` build resolving a different `CHANGELOG.md` through the translations-repo submodule | low | accept | Out of this repository's phase scope (CONTEXT `<deferred>`); recorded in the include-shape evidence for the milestone close rather than mitigated here |
</threat_model>

<flagged_assumptions>
- **RESEARCH Open Question 1 (MEDIUM confidence):** whether the included Markdown's `###`/`####`
  subsection depth reads sensibly in the PDF outline once combined with Phase 44.1's relative
  heading-depth offset. No compile-fatal risk was found (every node type has a handler). Resolution
  step: task 3's real `-b typstpdf` build, answer recorded in `45-GATE-EVIDENCE-01-include-shape.md`.
- **RESEARCH Open Question 2 (MEDIUM confidence):** CommonMark resolves the bracketed version
  headings as shortcut reference links against CHANGELOG.md's own tail link-definition block, so
  every version heading becomes a link. Mechanically fine; visual acceptability is a judgement call.
  Resolution step: task 3 inspects the rendered output; ladder rung (iii) is the documented remedy.
- **RESEARCH assumption A1:** `myst_parser` may not strictly need to be in `extensions` for the
  include-only use case. Registering it is a harmless no-op if unnecessary and a loud failure if
  omitted-and-necessary; task 3 registers it and the real build settles it either way.
</flagged_assumptions>

<verification>
- `uv run python -m sphinx -b html docs/source <tmp>` exits 0; `changelog.html` carries every release section CHANGELOG.md holds today and exactly one `Changelog` heading.
- `uv run python -m sphinx -b typstpdf docs/source <tmp>` exits 0 and produces a `%PDF`-magic file.
- Zero WARNING lines mentioning `changelog` in either build's output.
- `[project].dependencies` still holds exactly three entries.
- `uv run pytest tests/test_changelog_extraction.py -v` green — CHANGELOG.md was not touched by this plan, so REL-04's extractor surface is provably undisturbed.
- `uv run black --check .` and `uv run ruff check .` clean.
</verification>

<success_criteria>
- ROADMAP SC#2 (mechanism half): the published changelog page is generated from `CHANGELOG.md`, so a future release is a one-line `CHANGELOG.md` addition with zero `changelog.rst` edits.
- No stale current-release marker remains on the page.
- Both docs builders render the page, measured live, not inferred.
</success_criteria>

## Artifacts this phase produces

New symbols created by phase 45 (all four plans) — exclude these from drift/source-grounding checks:

- `pyproject.toml` — new dependency entry `myst-parser>=5.0` in `[project.optional-dependencies].docs`
- `docs/source/conf.py` — new `extensions` member `"myst_parser"`
- `docs/source/changelog.rst` — new `Release History` section and the `.. include:: ../../CHANGELOG.md` directive with options `:parser: myst_parser.sphinx_` and `:start-line: 7`
- `CHANGELOG.md` — new `## [0.4.4] - 2026-07-05` section and new `[0.4.4]:` link-reference line
- `tests/test_changelog_page_gate.py` — new test module; classes `TestPublishedChangelogPageDelegates`, `TestChangelogPageContentCoverage`, `TestChangelogIncludeCompilesToPdf`
- `tests/fixtures/changelog_include_gate/conf.py`, `tests/fixtures/changelog_include_gate/index.rst`, `tests/fixtures/changelog_include_gate/changelog.rst` — new fixture project
- `tests/test_quickstart_docs_gate.py` — new test module; classes `TestQuickstartFirstPdfGate`, `TestPublishedQuickstartTextMatchesBuild`
- `tests/fixtures/quickstart_docs_gate/conf.py`, `tests/fixtures/quickstart_docs_gate/index.rst` — new fixture project mirroring the published Quick Start
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-include-shape.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-02-docs-build-clean.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-qua03-comment-scan.md`

<output>
Create `.planning/phases/45-documentation-currency-carried-hygiene/45-01-SUMMARY.md` when done.
</output>
