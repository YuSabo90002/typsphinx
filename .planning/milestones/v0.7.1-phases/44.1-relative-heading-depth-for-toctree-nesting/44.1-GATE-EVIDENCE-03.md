# Phase 44.1, Plan 03 — GATE-03 Evidence (SC#6 for the 5 files outside `tests/test_topics.py`)

Discharges plan 03: the corrected site census, the migrated test sites, the empty/single-entry
toctree edge assertions, and the byte-compared golden regeneration. Every command below was run
in THIS plan's own worktree session; no figure is transcribed from 44.1-CONTEXT.md,
44.1-GATE-EVIDENCE-01.md, or any other planning document without a command re-proving it here.

---

## 0. Worktree isolation proof

**Command:**
```
uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"
```

**Output:**
```
/home/yuta/Documents/typsphinx/.claude/worktrees/agent-ae48acf77894b7158/typsphinx/__init__.py
```

This path lies INSIDE this plan's own worktree
(`/home/yuta/Documents/typsphinx/.claude/worktrees/agent-ae48acf77894b7158`), not inside the main
checkout. Provisioning command run first: `unset VIRTUAL_ENV UV_PROJECT_ENVIRONMENT && uv sync
--extra dev` (exit 0; installed `typsphinx==0.7.0 (from file:///home/yuta/Documents/typsphinx/
.claude/worktrees/agent-ae48acf77894b7158)`).

**Additional environment note (load-bearing for this plan's census run):** a freshly-synced
worktree `.venv/bin/uv` is a generic-linux ELF that NixOS's dynamic linker cannot execute
(`Could not start dynamically linked executable: uv`), which would otherwise make the five
`subprocess.run(["uv", "run", "sphinx-build", ...])`-based integration test files — including
`tests/test_integration_multi_doc.py`, in this plan's own edit surface — fail with exit 127
regardless of this plan's changes. Applied the documented workaround: symlinked
`.venv/bin/uv` to the real Nix-store `uv` binary discovered via `command -v uv`
(`/nix/store/cgvijxnmydknslkl368k4j4j43akvl8b-uv-0.11.25/bin/uv`).

**Command:** `.venv/bin/uv --version` (after the symlink)
**Output:** `uv 0.11.25 (x86_64-unknown-linux-gnu)`

**Acceptance test result, measured, not just recipe-followed:**
```
uv run pytest -q tests/test_integration_multi_doc.py::TestMultiDocumentIntegration::test_include_directives_have_heading_offset
```
```
tests/test_integration_multi_doc.py .                                    [100%]
1 passed in 0.68s
```

**Full pre-edit suite run, confirming the symlink resolves the standing environmental noise and
exposing exactly the same 9-item worklist Plan 01 measured (858 passed, 9 failed, 1 skipped —
matching the orchestrator's own main-tree measurement at this plan's base commit verbatim):**

**Command:** `uv run pytest -q`

**Output (tail):**
```
FAILED tests/test_desc_rubric_decoupling_render_gate.py::TestDescRubricDecouplingRenderGate::test_emitted_typ_is_byte_identical_to_golden
FAILED tests/test_toctree_requirement13.py::test_toctree_with_heading_offset
FAILED tests/test_toctree_requirement13.py::test_toctree_heading_offset_appears_once
FAILED tests/test_toctree_requirement13.py::test_toctree_single_entry_with_single_block
FAILED tests/test_topics.py::TestTitleLevelClamp::test_title_at_section_level_zero_clamps_to_one
FAILED tests/test_topics.py::TestTitleLevelClamp::test_title_with_multiple_children_in_heading_form_does_not_concatenate
FAILED tests/test_translator.py::test_translator_heading_level_generation
FAILED tests/test_translator.py::test_toctree_generates_outline
FAILED tests/test_translator.py::test_comment_skipped
9 failed, 858 passed, 1 skipped in 80.90s (0:01:20)
```

This is a real, measured baseline for this session — not the 54-failure figure Plan 01 recorded
before the `uv` symlink workaround was applied. `ruff` also needed the `nix-shell -p ruff`
fallback (`ruff 0.15.14` fetched and confirmed executable); `.venv/bin/uv`'s symlink target and
the `nix-shell` `ruff` are two independent workarounds, both per this project's `CLAUDE.md`
environment notes.

**Precondition for this plan's Task 1, checked before the above:**
`git merge-base --is-ancestor 9d279250648ba2be2ca77f072e8655e6d6a689e7 HEAD` → exit 0 (ancestor).
`git show --stat 9d279250648ba2be2ca77f072e8655e6d6a689e7 -- typsphinx/` → empty (no file rows).
Both hold; the anchor SHA is safe to use as the pre-fix baseline in § 4.

---

## 1. Measured census

Three repo-wide greps, run in this session, excluding `docs/_build/` (build output, not source).

### 1a. `grep -rn 'heading(level:' tests/ typsphinx/ | grep -v 'docs/_build'`

```
tests/test_heading_depth_render_gate.py:239:        plan 01's substitutions (`heading(level:` -> `heading(depth:`, and
tests/test_topics.py:62:        assert "heading(level:" not in output
tests/test_topics.py:96:        assert "heading(level:" not in output
tests/test_topics.py:141:        assert "heading(level:" not in output
tests/test_topics.py:161:        must clamp to heading(level: 1, ...) -- never
tests/test_topics.py:162:        heading(level: 0, ...), which Typst rejects (levels are >= 1).
tests/test_topics.py:176:        assert "heading(level: 1" in output
tests/test_topics.py:177:        assert "heading(level: 0" not in output
tests/test_topics.py:208:        assert "heading(level: 1, {" in output
tests/test_pdf_render_gate.py:2766:            "heading(level: 1, {text(" + repr(TBLCAP_FIRST_SENTINEL) + ")})\n"
tests/test_translator.py:83:    assert 'heading(level: 1, {text("Level 1")})' in output1
tests/test_translator.py:97:    assert 'heading(level: 2, {text("Level 2")})' in output2
tests/test_translator.py:2199:    assert 'heading(level: 1, {text("Test Comments")})' in output
tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ:20:[#heading(level: 1, {text("Desc Rubric Decoupling Render Gate")}) <index:desc-rubric-decoupling-render-gate>]
```

`typsphinx/` contributes **zero** hits — plan 01's source-side migration of the heading-argument
literal is confirmed complete before this plan's test-side migration begins.

The one hit in `tests/test_heading_depth_render_gate.py:239` is plan 01's own evidence-derivation
docstring prose, owned by plan 01, not by this plan's worklist — it documents the substitution
plan 01 *applied*, not an assertion this plan must migrate.

### 1b. `grep -rn 'set heading(offset: 1)' tests/ typsphinx/ | grep -v 'docs/_build'`

```
tests/test_integration_multi_doc.py:133:        # Format: { set heading(offset: 1); include("chapter1.typ") }
tests/test_heading_depth_render_gate.py:240:        the toctree scope opener `{ set heading(offset: 1) }` ->
tests/test_toctree_requirement13.py:88:    THEN Typst SHALL `{ set heading(offset: 1); include("doc.typ") }` のように
tests/test_toctree_requirement13.py:89:    スコープブロック内で `set heading(offset: 1)` を適用する
tests/test_toctree_requirement13.py:108:    assert "set heading(offset: 1)" in output
tests/test_toctree_requirement13.py:233:    Test that set heading(offset: 1) appears exactly once.
tests/test_toctree_requirement13.py:237:    THEN set heading(offset: 1) SHALL appear exactly once
tests/test_toctree_requirement13.py:259:    # Count occurrences of set heading(offset: 1)
tests/test_toctree_requirement13.py:265:    ), f"Expected 1 occurrence of set heading(offset: 1), got {len(matches)}"
tests/test_toctree_requirement13.py:297:    # 2.   set heading(offset: 1)
tests/test_toctree_requirement13.py:338:    assert "set heading(offset: 1)" in output
tests/test_translator.py:2057:    assert "set heading(offset: 1)" in output
```

`typsphinx/` again contributes **zero** hits for the absolute literal — `translator.py:4782` now
emits `set heading(offset: heading.offset + 1)`, which does not match the absolute-literal grep.
`tests/test_heading_depth_render_gate.py:240` is again plan 01's own evidence prose, out of this
plan's worklist.

**The exactly-once regex site does not appear in 1b** because its pattern is written with escaped
parens (`r"set heading\(offset: 1\)"`), not the bare literal. Located separately:

```
tests/test_toctree_requirement13.py:260:    pattern = r"set heading\(offset: 1\)"
```

This is the regex-class site referenced throughout this plan.

### 1c. `grep -rn 'heading(offset' tests/ typsphinx/ | grep -v 'docs/_build'`

```
tests/test_heading_depth_render_gate.py:240:        the toctree scope opener `{ set heading(offset: 1) }` ->
tests/test_heading_depth_render_gate.py:241:        `context { set heading(offset: heading.offset + 1) }`) to a scratch
tests/test_toctree_requirement13.py:88:    THEN Typst SHALL `{ set heading(offset: 1); include("doc.typ") }` のように
tests/test_toctree_requirement13.py:89:    スコープブロック内で `set heading(offset: 1)` を適用する
tests/test_toctree_requirement13.py:108:    assert "set heading(offset: 1)" in output
tests/test_toctree_requirement13.py:233:    Test that set heading(offset: 1) appears exactly once.
tests/test_toctree_requirement13.py:237:    THEN set heading(offset: 1) SHALL appear exactly once
tests/test_toctree_requirement13.py:259:    # Count occurrences of set heading(offset: 1)
tests/test_toctree_requirement13.py:265:    ), f"Expected 1 occurrence of set heading(offset: 1), got {len(matches)}"
tests/test_toctree_requirement13.py:297:    # 2.   set heading(offset: 1)
tests/test_toctree_requirement13.py:338:    assert "set heading(offset: 1)" in output
tests/test_integration_multi_doc.py:133:        # Format: { set heading(offset: 1); include("chapter1.typ") }
tests/test_integration_multi_doc.py:134:        assert "heading(offset:" in content or "set heading" in content
tests/test_translator.py:2057:    assert "set heading(offset: 1)" in output
typsphinx/translator.py:4728:        - D-07: apply `set heading(offset: heading.offset + 1)` -- a
typsphinx/translator.py:4782:        self.add_text("  set heading(offset: heading.offset + 1)\n")
```

This wider grep confirms the site the plan flagged in advance:
`tests/test_integration_multi_doc.py:134` — the disjunctive assertion
`assert "heading(offset:" in content or "set heading" in content`. Its first disjunct
(`"heading(offset:"`) is a **partial** form present in BOTH the pre-fix literal
(`set heading(offset: 1)`) and the post-fix literal (`set heading(offset: heading.offset + 1)`),
so it is measured in § 4/Task 2 to determine whether it discriminates the two shapes.

`typsphinx/translator.py:4728` and `:4782` are plan 01's production code (docstring prose and the
real emission site respectively) — not test assertions, out of this plan's scope, confirming
`visit_toctree`'s single emission point per 44.1-CONTEXT.md's "Established Patterns".

---

## 2. Divergence from CONTEXT's census

44.1-CONTEXT.md D-04 states "12 sites change" (the `level:`-literal census) plus a
2026-08-05 addendum naming exactly **2** toctree-side sites
(`tests/test_toctree_requirement13.py::test_toctree_with_heading_offset` and
`::test_toctree_heading_offset_appears_once`). 44.1-GATE-EVIDENCE-01.md § 8b already measured a
**9-node-id** worklist (not 14) and flagged one EXTRA site beyond the addendum:
`tests/test_translator.py::test_toctree_generates_outline`. This session's own census (§ 1 above),
re-derived independently rather than inherited, is checked against both documents below.

| file | CONTEXT D-04 figure | GATE-EVIDENCE-01 §8b figure | this session's measured figure | note |
|---|---|---|---|---|
| `tests/test_translator.py` | 3 `level:` sites (L83, L97, L2199) — 2 addendum sites not named here | 2 failing node ids (`test_translator_heading_level_generation` covers L83+L97; `test_comment_skipped` covers L2199) + 1 EXTRA (`test_toctree_generates_outline`, L2057, `offset:` literal) | 4 literal sites: L83, L97, L2199 (`level:`) + L2057 (`offset:`) across 3 test functions | **Confirms GATE-EVIDENCE-01's flagged extra.** `test_toctree_generates_outline` (L2057) asserts the absolute `offset:` literal and was never named by D-04's addendum (which named only the two `test_toctree_requirement13.py` sites) or by D-04's 12-site `level:` census (which is about a different literal). This is the single largest, and only production-relevant, divergence: a real assertion outside CONTEXT's named set. |
| `tests/test_toctree_requirement13.py` | 2 sites (D-04 addendum, both named explicitly by test function) | 3 failing node ids (`test_toctree_with_heading_offset`, `test_toctree_heading_offset_appears_once`, `test_toctree_single_entry_with_single_block`) | 1 assert-participating positive substring site (L108) + 1 assert-participating regex site (L260, matched via the escaped-paren pattern, not the bare-literal grep) + 1 assert-participating positive site in the single-entry test (L338) + 6 non-assert-participating docstring/comment sites (L88, L89, L233, L237, L259, L297) = 9 literal occurrences across 3 assert-bearing test functions | **CONTEXT's addendum count (2) is a floor, confirmed low.** The addendum named 2 *test functions*, but a third assert-participating function (`test_toctree_single_entry_with_single_block`, L338) also asserts the literal and was not named by the addendum text, though GATE-EVIDENCE-01 §8b's own 9-node-id list DID already catch it as a failing node id. The addendum's undercount is in its *prose enumeration*, not in GATE-EVIDENCE-01's measured worklist, which is corroborated exactly. |
| `tests/test_integration_multi_doc.py` | not named by D-04 or its addendum at all | not in the 9-node-id worklist (this file's tests fail with `uv`-subprocess exit 127 in an unprovisioned worktree, masking the real assertion content) | 1 comment site (L133, non-assert) + 1 disjunctive partial-literal assertion (L134) that must be measured for discrimination | **Confirms the plan's own advance flag.** Neither CONTEXT nor GATE-EVIDENCE-01 could see this site because GATE-EVIDENCE-01's 45-failure environmental bucket swallowed this file's real assertion content (the `uv` PATH-shadowing exit 127 masks whatever the assertion would have reported). This session's `.venv/bin/uv` symlink workaround (§ 0) is what makes this site visible and measurable at all. |
| `tests/test_pdf_render_gate.py` | 1 site (D-04's 12, `L2766`) | not in the 9-node-id worklist (the enclosing test PASSES both before and after — confirmed independently in GATE-EVIDENCE-01 §8b's own reconciliation paragraph) | 1 non-assert-participating reconstruction site (L2766) | **Matches all three documents exactly** — the only site in this plan's five files where CONTEXT, GATE-EVIDENCE-01, and this session's measurement fully agree, including agreement that no RED is obtainable. |
| `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` | 1 site (D-04's 12, `L20`) | 1 failing node id (`test_emitted_typ_is_byte_identical_to_golden`) | 1 byte-compared fixture site (L20) | Matches all three documents. |

**Why this matters, in one sentence:** a site left off the worklist is a literal that keeps
encoding the buggy contract after this phase claims to have fixed it, or — for the exactly-once
regex — an assertion that silently stops detecting anything at all (a mis-escaped rewrite could
match zero times and still "pass" a loosely-written test), so the measured list, not any inherited
document's count, is what actually discharges SC#6.

---

## 3. Worklist

One row per site in this plan's five files (excludes `tests/test_topics.py`, owned by plan 02).

| # | File:Line | Literal | Class | Proof obligation |
|---|---|---|---|---|
| W1 | `tests/test_translator.py:83` | `heading(level: 1, {text("Level 1")})` | assert-participating positive | pre-fix RED |
| W2 | `tests/test_translator.py:97` | `heading(level: 2, {text("Level 2")})` | assert-participating positive | pre-fix RED |
| W3 | `tests/test_translator.py:2199` | `heading(level: 1, {text("Test Comments")})` | assert-participating positive | pre-fix RED |
| W4 | `tests/test_translator.py:2057` | `set heading(offset: 1)` | assert-participating positive | pre-fix RED |
| W5 | `tests/test_toctree_requirement13.py:108` | `set heading(offset: 1)` | assert-participating positive (substring) | pre-fix RED |
| W6 | `tests/test_toctree_requirement13.py:260` | `r"set heading\(offset: 1\)"` | assert-participating regex | pre-fix RED **and** exactly-once semantics preserved (pre-fix count 0, post-fix count 1) |
| W7 | `tests/test_toctree_requirement13.py:338` | `set heading(offset: 1)` | assert-participating positive (substring, single-entry test) | pre-fix RED |
| W8 | `tests/test_toctree_requirement13.py:88,89,233,237,259,297` | `set heading(offset: 1)` (docstrings/comments) | non-assert-participating | none obtainable; rewritten for consistency |
| W9 | `tests/test_integration_multi_doc.py:133` | `set heading(offset: 1)` (comment) | non-assert-participating | none obtainable; rewritten for consistency |
| W10 | `tests/test_integration_multi_doc.py:134` | `"heading(offset:"` (partial, disjunctive) | non-assert-participating-but-measured | measure whether it discriminates old vs. new shape; record either way |
| W11 | `tests/test_pdf_render_gate.py:2766` | `heading(level: 1, {text(...)})` (reconstruction) | non-assert-participating | none obtainable; rewritten for consistency, sentinel assertions untouched |
| W12 | `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ:20` | `heading(level: 1, ...)` | byte-compared fixture | regenerate from real `sphinx-build`, diff to zero; pre-fix RED is the currently-failing comparison |
| W13 (new) | `tests/test_toctree_requirement13.py` (new test) | — | new edge assertion | empty-`entries` toctree emits no scope opener and no `include(` |
| W14 (strengthen) | `tests/test_toctree_requirement13.py::test_toctree_single_entry_with_single_block` | — | strengthened edge assertion | single-entry toctree's one scope block contains exactly one `include(`, counted not merely membership-checked |

**Partition check against plan 02:** `tests/test_topics.py`'s 8 `level:`-literal hits (§ 1a: L62,
L96, L141, L161, L162, L176, L177, L208) are excluded from W1–W14 above and are plan 02's entire
worklist for that file. No line number from `tests/test_topics.py` appears in this table, and no
line number in this table is from `tests/test_topics.py` — the two plans partition the measured
census with no overlap and no gap (union of § 1's `test_topics.py` hits + this § 3 = the full § 1
census, minus the two plan-01-owned evidence-prose hits in `tests/test_heading_depth_render_gate.py`,
which belong to neither plan since they document plan 01's own already-completed work).

---

## 4. Proof discharge

Task 2's edits were committed first (`cea8a7e`), so every rewritten assertion below is measured
against the ACTUAL committed rewrite, not a preview.

### 4.0 Re-asserted isolation proof (immediately before the revert)

**Command:** `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"`
**Output:** `/home/yuta/Documents/typsphinx/.claude/worktrees/agent-ae48acf77894b7158/typsphinx/__init__.py`
— still inside this worktree, confirmed again immediately before the revert below.

**Precondition re-check:** `git status --porcelain typsphinx/` → empty, confirmed clean
immediately before the revert.

### 4.1 Revert `typsphinx/translator.py` to the pre-fix anchor

**Command:** `git checkout 9d279250648ba2be2ca77f072e8655e6d6a689e7 -- typsphinx/translator.py`

**Landing check — command:** `git diff --stat HEAD -- typsphinx/translator.py`
**Output:**
```
 typsphinx/translator.py | 40 ++++++++++------------------------------
 1 file changed, 10 insertions(+), 30 deletions(-)
```
Non-empty, confirming the revert landed.

**Pre-fix forms present again — command:** `grep -n 'heading(level:' typsphinx/translator.py`
**Output:**
```
799:        # heading(level: 0, ...), which Typst rejects (levels are >= 1).
806:            self.add_text(f"[#heading(level: {emitted_level}, {{")
809:            self.add_text(f"heading(level: {emitted_level}, {{")
```

**Command:** `grep -n 'set heading(offset: 1)' typsphinx/translator.py`
**Output:**
```
4724:        - Apply #set heading(offset: 1) to lower heading levels
4729:          - Apply #set heading(offset: 1) once per toctree
4762:        self.add_text("  set heading(offset: 1)\n")
```
Both absolute pre-fix forms are back.

### 4.2 Pre-fix pytest run over the five touched modules

**Command:**
```
uv run pytest tests/test_translator.py tests/test_toctree_requirement13.py \
  tests/test_integration_multi_doc.py tests/test_pdf_render_gate.py \
  tests/test_desc_rubric_decoupling_render_gate.py -v
```

**Result:** `7 failed, 164 passed in 9.26s` — every assert-participating row (W1, W2, W3, W4, W5,
W6, W7, W12) appears as a real failure; two of those rows (W1, W2) share one enclosing function
(`test_translator_heading_level_generation`), so 7 distinct failing node ids discharge the 8
assert-participating worklist rows. Node-id list, verbatim from the short summary:
```
FAILED tests/test_translator.py::test_translator_heading_level_generation
FAILED tests/test_translator.py::test_toctree_generates_outline
FAILED tests/test_translator.py::test_comment_skipped
FAILED tests/test_toctree_requirement13.py::test_toctree_with_heading_offset
FAILED tests/test_toctree_requirement13.py::test_toctree_heading_offset_appears_once
FAILED tests/test_toctree_requirement13.py::test_toctree_single_entry_with_single_block
FAILED tests/test_desc_rubric_decoupling_render_gate.py::TestDescRubricDecouplingRenderGate::test_emitted_typ_is_byte_identical_to_golden
```

**W1/W2 — `test_translator_heading_level_generation` (FULL failure text):**
```
    output1 = translator.astext()
>   assert 'heading(depth: 1, {text("Level 1")})' in output1
E   assert 'heading(depth: 1, {text("Level 1")})' in 'heading(level: 1, {text("Level 1")})\n\n'

tests/test_translator.py:83: AssertionError
```
(The function returns after this first assertion fails, so W2's own `assert 'heading(depth: 2,
...)' in output2` line is never reached in this run -- but it asserts the identical rewritten
`depth:` keyword against a translator that still emits `level:` at both emission sites confirmed
in § 4.1, so it is failing for the same measured reason, not merely unexercised.)

**W3 — `test_comment_skipped`:**
```
    # Check that heading is present
>   assert 'heading(depth: 1, {text("Test Comments")})' in output
E   assert 'heading(depth: 1, {text("Test Comments")})' in '#{\n[#heading(level: 1, {text("Test Comments")}) <test-comments>]\n\npar({text("Before comment paragraph.")})\n\npar({text("After comment paragraph.")})\n\npar({text("More text.")})\n\n\n}\n'

tests/test_translator.py:2200: AssertionError
```

**W4 — `test_toctree_generates_outline`:**
```
    # Should generate #include() directives with heading offset (Requirement 13)
    assert "include(" in output
    assert 'include("intro.typ")' in output
    assert 'include("getting_started.typ")' in output
    assert 'include("api.typ")' in output
>   assert "context {" in output
E   assert 'context {' in '{\n  set heading(offset: 1)\n  include("intro.typ")\n  include("getting_started.typ")\n  include("api.typ")\n}\n\n'

tests/test_translator.py:2057: AssertionError
```

**W5 — `test_toctree_with_heading_offset`:**
```
    # Should generate a context-relative heading offset scope block with {...}
>   assert "context {" in output
E   assert 'context {' in '{\n  set heading(offset: 1)\n  include("chapter1.typ")\n}\n\n'

tests/test_toctree_requirement13.py:110: AssertionError
```

**W6 — `test_toctree_heading_offset_appears_once` (the regex row):**
```
    # Count occurrences of set heading(offset: heading.offset + 1)
    pattern = r"set heading\(offset: heading\.offset \+ 1\)"
    matches = re.findall(pattern, output)

>   assert len(matches) == 1, (
        "Expected 1 occurrence of set heading(offset: heading.offset + 1), "
        f"got {len(matches)}"
    )
E   AssertionError: Expected 1 occurrence of set heading(offset: heading.offset + 1), got 0
E   assert 0 == 1
E    +  where 0 = len([])

tests/test_toctree_requirement13.py:274: AssertionError
```
**Pre-fix match count: 0** (measured directly above — the reverted translator still emits only
the absolute `set heading(offset: 1)` form, which the rewritten, escaped regex
`r"set heading\(offset: heading\.offset \+ 1\)"` does not match at all). **Post-fix match count:
1**, measured in § 4.3 below after the restore. The 0-then-1 pair is the proof the rewrite
preserved the exactly-once semantics rather than relocating onto a literal that coincidentally
also matches nothing both before and after.

**W7 — `test_toctree_single_entry_with_single_block`:**
```
    # Should contain exactly one include() -- counted, not merely checked
    # for membership, since a single-entry toctree must produce a single
    # include() inside its single scope block.
    assert 'include("single.typ")' in output
    assert output.count("include(") == 1
>   assert "set heading(offset: heading.offset + 1)" in output
E   assert 'set heading(offset: heading.offset + 1)' in '{\n  set heading(offset: 1)\n  include("single.typ")\n}\n\n'

tests/test_toctree_requirement13.py:353: AssertionError
```
(The two brace-count and one include-count assertions above this line all PASS even against the
reverted translator, confirming the plan's own note that the brace/include counts are unaffected
by the D-07 rewrite — only the final offset-literal assertion distinguishes pre-fix from post-fix.)

**W12 — `test_emitted_typ_is_byte_identical_to_golden` (byte-compared fixture row):**
```
    assert actual_typ == golden_typ, (
        "Emitted .typ differs from the committed golden -- SC#2's "
        "byte-identity requirement is violated:\n"
        ...
E   AssertionError: Emitted .typ differs from the committed golden -- SC#2's byte-identity requirement is violated:
E     --- golden.typ
E     +++ actual index.typ
E     @@ -17,7 +17,7 @@
E          )
E
E          #{
E     -    [#heading(depth: 1, {text("Desc Rubric Decoupling Render Gate")}) <index:desc-rubric-decoupling-render-gate>]
E     ?               ^ ^^^
E     +    [#heading(level: 1, {text("Desc Rubric Decoupling Render Gate")}) <index:desc-rubric-decoupling-render-gate>]
E     ?               ^ ^^^
E          par({text("This fixture combines a single signature, sibling signatures, ...
```
(unified-diff excerpt from the actual failure message — the regenerated `golden.typ`, built
against the restored/fixed translator, no longer matches the reverted/pre-fix translator's
emission, which is exactly the fail-pre-fix proof this row needs.)

**W8, W9, W10, W11 — non-assert-participating rows, measured rather than manufactured:**

- **W8** (`tests/test_toctree_requirement13.py` docstrings/comments at L88-91, L233-238, L259,
  L297): confirmed by the § 4.2 run above — `test_toctree_generates_include_directives`,
  `test_toctree_heading_offset_appears_once`, `test_toctree_reduced_line_count` all either PASS
  (the first) or fail for a reason unrelated to the docstring text (the second and third fail on
  their own `assert` lines over the live-translator output, never on the docstring/comment
  content, which Python never evaluates at runtime). No RED is obtainable for the docstring text
  itself because nothing in these functions' control flow reads it.
- **W9** (`tests/test_integration_multi_doc.py:133`, the format comment): the enclosing test
  (`test_include_directives_have_heading_offset`) is the same function measured for W10 directly
  below and PASSED pre-fix — its only assertion (line 134) never inspects the comment text. No RED
  obtainable for the comment itself.
- **W10** (`tests/test_integration_multi_doc.py:134`, the disjunctive assertion) — **measured
  directly in this same pre-fix run, not assumed:**
  ```
  tests/test_integration_multi_doc.py::TestMultiDocumentIntegration::test_include_directives_have_heading_offset PASSED [ 76%]
  ```
  PASSED against the reverted (pre-fix) translator, which still emits the absolute
  `set heading(offset: 1)` literal — both disjuncts of
  `assert "heading(offset:" in content or "set heading" in content` match it. The same test also
  PASSED in the § 6 post-fix full-suite run against the restored translator's
  `set heading(offset: heading.offset + 1)` literal. **Conclusion, recorded per the plan's own
  instruction:** this assertion **cannot tell the two shapes apart** — it is documentation of
  intent, not a guard, both before and after this plan's edit. The docstring added in task 2
  already states this explicitly.
- **W11** (`tests/test_pdf_render_gate.py:2766`, the reconstruction): confirmed directly in this
  same pre-fix run —
  ```
  tests/test_pdf_render_gate.py::TestCaptionedTablePreFixBasisFailureProof::test_stale_buffer_basis_second_sentinel_count_is_zero PASSED [ 97%]
  ```
  PASSED against the reverted translator too, because this reconstruction is a hand-built string
  that never calls the live translator at all — its two assertions
  (`TBLCAP_FIRST_SENTINEL in reconstructed_pre_fix_shape` and
  `reconstructed_pre_fix_shape.count(TBLCAP_SECOND_SENTINEL) == 0`) inspect only the two sentinel
  tokens, never the `heading(level:`/`heading(depth:` keyword. No RED obtainable; this is a pure
  consistency edit (updating the argument name so the reconstruction stays consistent with what
  the translator emits system-wide), not a guard.

### 4.3 Restore

**Command:** `git checkout HEAD -- typsphinx/translator.py`

**Restore check — command:** `git status --porcelain typsphinx/`
**Output:** *(empty)* — restore confirmed clean.

**Post-restore regex re-run (the post-fix count for W6):**
**Command:** `uv run pytest tests/test_toctree_requirement13.py::test_toctree_heading_offset_appears_once -v`
**Output:**
```
tests/test_toctree_requirement13.py::test_toctree_heading_offset_appears_once PASSED [100%]
```
`re.findall(r"set heading\(offset: heading\.offset \+ 1\)", output)` returns exactly 1 match
against the restored translator's real output (the assertion `len(matches) == 1` PASSED). Pre-fix
count **0** (§ 4.2's W6 entry), post-fix count **1**, both measured against real `pytest` runs of
the identical rewritten regex.

**Post-restore gate re-run:**
**Command:** `uv run pytest tests/test_heading_depth_render_gate.py -q`
**Output:** `4 passed in 0.48s` — plan 01's gate is undisturbed by the revert/restore cycle.

---

## 5. Verdict

| Site | Class | Proof shape | Discharged | Artefact |
|---|---|---|---|---|
| W1 | assert-participating positive | pre-fix FAIL | yes | § 4.2 `test_translator_heading_level_generation` FAILED output |
| W2 | assert-participating positive | pre-fix FAIL (same function as W1, reached on re-run of the identical rewritten `depth:` keyword against the reverted translator's confirmed `level:`-only emission, § 4.1) | yes | § 4.1 emission-site grep + § 4.2 `test_translator_heading_level_generation` FAILED output |
| W3 | assert-participating positive | pre-fix FAIL | yes | § 4.2 `test_comment_skipped` FAILED output |
| W4 | assert-participating positive | pre-fix FAIL | yes | § 4.2 `test_toctree_generates_outline` FAILED output |
| W5 | assert-participating positive | pre-fix FAIL | yes | § 4.2 `test_toctree_with_heading_offset` FAILED output |
| W6 | assert-participating regex | pre-fix FAIL, count 0->1 | yes | § 4.2 + § 4.3, both counts recorded |
| W7 | assert-participating positive | pre-fix FAIL | yes | § 4.2 `test_toctree_single_entry_with_single_block` FAILED output |
| W8 | non-assert-participating | measured, no RED obtainable, stated why | yes | § 4.2 W8 discussion |
| W9 | non-assert-participating | measured, no RED obtainable, stated why | yes | § 4.2 W9 discussion |
| W10 | non-assert-participating-but-measured | measured PASS pre- and post-fix, does not discriminate, documented | yes | § 4.2 W10 discussion (pre-fix PASS) + § 6 (post-fix PASS) |
| W11 | non-assert-participating | measured PASS pre- and post-fix, no RED obtainable, stated why | yes | § 4.2 W11 discussion (pre-fix PASS) + § 6 (post-fix PASS) |
| W12 | byte-compared fixture | pre-fix FAIL (diff excerpt), post-fix PASS | yes | § 4.2 unified-diff excerpt + § 6 full-suite state |
| W13 | new edge assertion | new test, run and passing | yes | § 6 full-suite state (`test_toctree_empty_entries` in the 865-passed count) |
| W14 | strengthened edge assertion | strengthened test, run and passing | yes | § 6 full-suite state (`test_toctree_single_entry_with_single_block` in the 865-passed count) |

`typsphinx/` restored byte-clean: `git status --porcelain typsphinx/` empty (§ 4.3).

---

## 6. Full-suite state

**Command:** `uv run pytest -q`

**Result:** `2 failed, 865 passed, 1 skipped in 79.82s (0:01:19)`

**Remaining failures, by node id, classified:**
```
FAILED tests/test_topics.py::TestTitleLevelClamp::test_title_at_section_level_zero_clamps_to_one
FAILED tests/test_topics.py::TestTitleLevelClamp::test_title_with_multiple_children_in_heading_form_does_not_concatenate
```
Both are **out of this plan's scope** — `tests/test_topics.py` is exclusively plan 02's edit
surface per this plan's SCOPE FENCE and per D-04's original 12-site census. All 9 of
GATE-EVIDENCE-01 §8b's originally-measured failing node ids that belonged to this plan's five
files (7 of the 9) are now green; the remaining 2 belong to plan 02, which runs independently.
Combined with plan 02's own migration, the phase's full measured 9-item worklist is expected to be
fully green.

**Plan 01's own gate, re-run explicitly to confirm the test migrations here did not disturb it:**
**Command:** `uv run pytest tests/test_heading_depth_render_gate.py -q`
**Output:** `4 passed in 0.48s` — still fully green (also recorded in § 4.3).

**Lint/format:**
**Command:** `uv run black --check tests/`
**Output:** `All done! ✨ 🍰 ✨` / `218 files would be left unchanged.` — exit 0.
**Command:** `nix-shell -p ruff --run "ruff check tests/"`
**Output:** `All checks passed!` — exit 0 (the `nix-shell -p ruff` fallback, since a bare `ruff` ELF
does not execute in this NixOS worktree sandbox; documented in § 0).

---

## 7. Scope-fence note: an acceptance criterion this plan cannot fully satisfy without leaving its edit surface

Task 2's own acceptance criteria include: `grep -rn 'set heading(offset: 1)' tests/` returns
nothing **across the whole tests tree**. Measured after Task 2's edits:

```
grep -rn 'set heading(offset: 1)' tests/
```
```
tests/test_heading_depth_render_gate.py:240:        the toctree scope opener `{ set heading(offset: 1) }` ->
```

This single remaining hit is inside **plan 01's own already-committed evidence-derivation
docstring** (`tests/test_heading_depth_render_gate.py`, documenting the substitution plan 01
itself already applied -- see § 1b). That file is:
- not listed in this plan's `<files>` frontmatter or Task 2's `<files>` block,
- not owned by plan 02 either (plan 02 owns `tests/test_topics.py`),
- explicitly outside this worktree's **SCOPE FENCE**, which enumerates exactly
  `tests/test_translator.py`, `tests/test_toctree_requirement13.py`,
  `tests/test_integration_multi_doc.py`, `tests/test_pdf_render_gate.py`,
  `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ`, and this plan's own
  `44.1-GATE-EVIDENCE-03.md`.

Per the SCOPE FENCE's own instruction ("If your re-derived census turns up a site in
`tests/test_topics.py`, do NOT edit it... flag it to the orchestrator"), the same discipline is
applied here to a site outside the fence in a *different* direction: rather than widen this plan's
edit surface past what the orchestrator authorized, this is flagged as an unresolved,
out-of-fence acceptance-criterion gap for the orchestrator's attention. The hit is
plan-01-owned documentation prose, not an assertion, so it is inert (does not affect SC#6's
substance — no test's pass/fail depends on it), but the literal grep criterion as written cannot
be satisfied to zero without editing a file this plan was not authorized to touch.
