---
phase: 44.1-relative-heading-depth-for-toctree-nesting
plan: 03
type: execute
wave: 2
depends_on: ["44.1-01"]
files_modified:
  - tests/test_translator.py
  - tests/test_toctree_requirement13.py
  - tests/test_integration_multi_doc.py
  - tests/test_pdf_render_gate.py
  - tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ
  - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-03.md
autonomous: true
requirements: [TOC-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC#6 (D-04 positive-assertion category + D-04's addendum): every remaining site that encodes the pre-TOC-01 emitted shape as a literal — in `tests/test_translator.py`, `tests/test_toctree_requirement13.py`, `tests/test_integration_multi_doc.py`, `tests/test_pdf_render_gate.py` and the byte-compared golden fixture — is migrated deliberately, each edit traceable to TOC-01."
    - "The site census is re-derived by a repo-wide grep in this plan's own session and supersedes 44.1-CONTEXT.md's D-04 addendum figure where the two disagree. CONTEXT's addendum names two toctree-side sites; a planning-time grep found more, so the measured list — not the document's number — is the worklist."
    - "D-07's addendum obligation: the exactly-once semantics of the toctree scope opener survives the rewrite — the regex asserting a single occurrence matches the literal `visit_toctree` now emits, byte for byte, and still asserts exactly one match."
    - "Every rewritten assertion whose literal participates in an assert expression is re-proved FAILING against plan 01's pre-fix anchor commit. The one site whose literal participates in no assert expression is recorded as such, with the measurement proving it, rather than being given a manufactured RED."
    - "The byte-compared golden fixture `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` is REGENERATED from a real `sphinx-build -b typst` run of its own fixture project and proved byte-identical to that run's output — never hand-edited to the value the assertion wants."
    - "D-05 holds: every migrated site stays a unit or fixture-comparison test; none is promoted to a build-and-compile test."
    - "Edge (empty / single element): a toctree with zero entries emits no scope block at all — `visit_toctree` raises `nodes.SkipNode` before any text is added — and a toctree with exactly one entry emits exactly one scope block containing exactly one `include()`. Both are asserted, not assumed."
  artifacts:
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-03.md
  key_links:
    - "the regex in the exactly-once toctree test -> the byte-exact literal `visit_toctree` emits"
    - "the committed golden `.typ` -> the live output of a real `sphinx-build` of its own fixture"
  prohibitions:
    - statement: "MUST NOT transcribe a value observed in a post-fix run into an assertion or into a golden fixture. Every literal written here is produced by a command run in this plan's own session — the golden file by regenerating it from a real build and diffing, the regex by matching it against the translator's actual output — and the command and its raw output are recorded next to it."
      status: unverified
      verification: test
---

<objective>
Migrate every remaining site outside `tests/test_topics.py` that hard-codes the pre-TOC-01 emitted
shape, and re-derive the census that says which sites those are.

This plan owns the census correction. 44.1-CONTEXT.md's D-04 addendum states that two sites in
`tests/test_toctree_requirement13.py` assert the toctree scope opener. A repo-wide grep at planning
time found more than two, and found a site in `tests/test_translator.py` that the addendum does not
mention at all. The addendum's *policy* is right and binding; its *count* is a floor, not a ceiling.
Deriving the list by grep rather than by transcription is the standing project rule for
"anywhere"-shaped criteria, and it applies here.

Purpose: SC#6 names five files. Four of them carry positive assertions that a keyword sweep handles,
one carries a regex whose exactly-once semantics must survive a literal change, one carries a
hand-built reconstruction whose literal is asserted by nothing, and one is a byte-compared golden
fixture that must be regenerated from a real build rather than edited. Those are four different kinds
of edit with four different proof obligations, and this plan keeps them apart.

Output: the corrected census, the migrated sites, the empty/single-entry edge assertions, and
`44.1-GATE-EVIDENCE-03.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-CONTEXT.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-PATTERNS.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-01-SUMMARY.md
@CLAUDE.md
</context>

<worktree_provisioning>
Mandatory, same as plans 01 and 02, and load-bearing here for two separate reasons: task 2
regenerates a golden fixture from a real `sphinx-build`, and task 3 measures against a reverted
`typsphinx/translator.py`. Both are meaningless if `import typsphinx` resolves outside this worktree.

- `test -f .git` confirms an isolated worktree.
- `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
- Everything through `uv run`.
- `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"`
  must print a path inside this worktree. Record it as § 0 of `44.1-GATE-EVIDENCE-03.md`.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the full site census by grep and record where it exceeds CONTEXT's figure</name>

  <files>.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-03.md</files>

  <precondition>
    Plan 01's test-only anchor commit is reachable from HEAD and changes no path under `typsphinx/`.
    Read the 40-hex SHA from `44.1-GATE-EVIDENCE-01.md` § 5, then assert both
    `git merge-base --is-ancestor &lt;SHA&gt; HEAD` succeeds and `git show --stat &lt;SHA&gt; -- typsphinx/`
    lists no file. Halt if either fails.
  </precondition>

  <read_first>
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-01.md
      (§ 5 for the anchor SHA, § 8 for plan 01's measured worklist of invalidated assertions)
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-CONTEXT.md `<decisions>`
      D-04 including its 2026-08-05 addendum, and `<specifics>` item 4 (the census figures this task
      must check rather than trust)
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-PATTERNS.md — the sections
      for `tests/test_translator.py`, `tests/test_toctree_requirement13.py`, `tests/test_pdf_render_gate.py`
      and the golden fixture, including PATTERNS' explicit caveat about the reconstruction site
    - the ROADMAP.md § "Phase 44.1" Success Criteria block, SC#6 verbatim (it names five files by path
      and line, and those line numbers are pre-migration — treat them as pointers, not as the census)
  </read_first>

  <action>
Derive the census here, in this session. Record every command and its raw output.

<!-- planner-discipline-allow: heading(level: -->
<!-- planner-discipline-allow: set heading(offset: 1) -->

Run two repo-wide greps over `tests/` and `typsphinx/`, excluding `docs/_build/` (build output, not
source):

- the old absolute heading argument literal `heading(level:`
- the old absolute toctree scope literal `set heading(offset: 1)`

Also run a third, wider grep for `heading(offset` to catch assertions written against a partial form
of the toctree literal — the planning-time scan found one such site in
`tests/test_integration_multi_doc.py` whose assertion still passes after the fix but whose adjacent
comment documents the superseded shape.

Record all three hit lists verbatim in `44.1-GATE-EVIDENCE-03.md` § 1 "Measured census". Exclude
`tests/test_topics.py` from this plan's worklist — plan 02 owns that file — but keep its hits in the
recorded census so the phase total is complete and the two plans can be shown to partition the set
without overlap or gap.

Classify every hit in this plan's files into one of four classes and record which:

- **assert-participating positive** — the literal appears inside an assert expression and the assertion
  is satisfied by its presence. Proof obligation: pre-fix RED.
- **assert-participating regex** — the literal appears inside a regex whose match count is asserted.
  Proof obligation: pre-fix RED *and* preservation of the exactly-once semantics.
- **byte-compared fixture** — the literal lives in a file compared byte-for-byte against live output.
  Proof obligation: regenerate from a real build and diff to zero; the pre-fix RED is the currently
  failing comparison.
- **non-assert-participating** — the literal appears in a docstring, a comment, or a hand-built string
  that no assertion inspects for it. Proof obligation: *none is obtainable*; record the measurement
  that shows the enclosing test passes both before and after the edit, and say so plainly.

Then compare against 44.1-CONTEXT.md. Its `<specifics>` item 4 gives per-file occurrence counts and
its D-04 addendum names two toctree-side sites. Record, in § 2 "Divergence from CONTEXT's census", a
per-file table of *CONTEXT's figure* against *this session's measured figure*, with an explicit note on
every row that differs. Do not smooth over a divergence and do not adopt CONTEXT's number: the
measured list is the worklist. State in one sentence why this matters — a site left off the worklist
is a literal that keeps encoding the buggy contract, or an assertion that silently stops detecting
anything.

Finally record § 3 "Worklist", the ordered list of edits this plan will make, one row per site with
its class and proof obligation.
  </action>

  <verify>
    <automated>grep -rn 'heading(level:' tests/ typsphinx/ | grep -v 'docs/_build' | tee /dev/stderr | wc -l</automated>
    <automated>grep -rn 'set heading(offset: 1)' tests/ typsphinx/ | grep -v 'docs/_build' | tee /dev/stderr | wc -l</automated>
    <automated>grep -rn 'heading(offset' tests/ typsphinx/ | grep -v 'docs/_build' | tee /dev/stderr | wc -l</automated>
    <automated>uv run pytest -q 2>&1 | tail -25</automated>
  </verify>

  <acceptance_criteria>
    - `44.1-GATE-EVIDENCE-03.md` § 1 contains the raw output of all three greps run in this session,
      each preceded by its command line.
    - `grep -rl 'heading(level:' typsphinx/` and `grep -rl 'set heading(offset: 1)' typsphinx/` both
      return nothing in § 1 — plan 01's source-side migration is confirmed complete before this plan's
      test-side migration begins.
    - Every hit in this plan's five files is classified into exactly one of the four classes, and the
      class counts sum to the measured hit count for those files.
    - § 2 contains a per-file table comparing CONTEXT's figure with the measured figure, and every
      differing row carries an explicit note.
    - § 3 lists every edit this plan will make, one row per site, each with its class and proof
      obligation.
    - The union of § 1's `tests/test_topics.py` hits and § 3's worklist covers every hit in the census
      with no site appearing in both — the two plans partition the set.
    - No file under `tests/` or `typsphinx/` is modified by this task.
  </acceptance_criteria>

  <done>
    The phase's full site census is measured rather than inherited, its divergence from CONTEXT's
    figure is recorded row by row, every site carries a class and a proof obligation, and this plan's
    worklist provably partitions the census with plan 02's.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate the sites, regenerate the golden fixture, and assert the empty/single-entry edge</name>

  <files>
    tests/test_translator.py,
    tests/test_toctree_requirement13.py,
    tests/test_integration_multi_doc.py,
    tests/test_pdf_render_gate.py,
    tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ
  </files>

  <read_first>
    - tests/test_translator.py lines 75-100, 260-310, 2020-2060, 2190-2205 — the three heading-argument
      assertions, the toctree-offset assertion, and the two keyword-agnostic `"heading(" not in …`
      table-caption guards that must be left alone
    - tests/test_toctree_requirement13.py in full — the `simple_document` / `mock_builder` fixtures, the
      substring assertion, the exactly-once regex, the single-entry brace-count test, the line-count
      test, and every docstring naming the old scope literal
    - tests/test_integration_multi_doc.py lines 120-145 — the comment documenting the emitted format and
      the disjunctive assertion beside it
    - tests/test_pdf_render_gate.py lines 2495-2505 (the sentinel constants) and 2750-2796 (the
      reconstruction and the two assertions that actually inspect it)
    - tests/test_desc_rubric_decoupling_render_gate.py lines 30-50 and 270-310 — how `golden.typ` is
      consumed: `actual_typ == golden_typ` on text read with `encoding="utf-8"`, with a unified diff in
      the failure message
    - tests/fixtures/desc_rubric_decoupling_render_gate/conf.py and index.rst — the fixture project the
      golden is regenerated from
    - typsphinx/translator.py lines 4740-4800 — the post-plan-01 `visit_toctree`, so the regex and the
      substring assertions are written against the bytes it actually emits
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-03.md § 3
  </read_first>

  <action>
Work § 3's worklist row by row. Each edit is traceable to TOC-01 and to D-04's owner sign-off; note
that traceability where a docstring or comment is being rewritten anyway.

<!-- planner-discipline-allow: heading(level: -->
<!-- planner-discipline-allow: set heading(offset: 1) -->

**`tests/test_translator.py` — assert-participating positives.** Three assertions over the heading
argument name and one over the toctree scope literal. Change the argument name in the first three to
the relative form the translator now emits. For the fourth, replace the old absolute scope literal with
the byte-exact literal `visit_toctree` now emits — read it from the source, do not reconstruct it from
memory. Leave the two keyword-agnostic `"heading(" not in …` table-caption guards untouched: they are
argument-name-agnostic by design and are part of SC#7's evidence, so changing them would weaken the
phase's own proof.

**`tests/test_toctree_requirement13.py` — assert-participating substring, regex, and prose.**

- The substring assertions that check the scope opener are present must now check the two-part shape:
  the `context` keyword opening the scope, and the relative increment expression inside it. Under D-07
  the literal being asserted changes entirely rather than by one keyword, so treat each as a new
  assertion over a new literal, not as a find-and-replace.
- The exactly-once regex must be rewritten to match the new literal byte for byte, with every Typst
  punctuation character escaped for `re` — parentheses, the colon, the dot in the offset field access,
  and the plus sign. **The exactly-once semantics must survive**: the assertion still requires exactly
  one match, and its failure message still names the literal it counted. This is D-07's addendum
  obligation and it is the one place a sloppy escape would silently turn an exact count into a
  zero-match count that happens to raise a clear error — so verify the regex matches by running it
  against real translator output, not by inspection.
- The brace-count test asserting a single scope block must keep asserting exactly one opening and one
  closing brace. Check by running it: the new opener adds a keyword before the brace but not a second
  brace, so the counts should be unchanged — confirm that rather than assume it.
- The line-count test asserting the emitted block is between five and six non-empty lines must keep its
  bounds. Confirm by running it.
- Every docstring and comment in this file that quotes the old absolute scope literal is rewritten to
  quote the new one, including the requirement-tracing prose written in Japanese. After this task no
  occurrence of the old literal may remain in the file.

**Add the empty/single-entry edge assertions to this file** (it is the natural home — it already owns
the toctree unit tests and the `simple_document`/`mock_builder` fixtures):

- A test that a toctree node whose `entries` list is empty produces **no output at all**: `visit_toctree`
  raises `nodes.SkipNode` before adding any text, so after catching the `SkipNode` the translator's
  `astext()` must contain neither the scope opener nor any `include(`. Assert both.
- Strengthen the existing single-entry test so it asserts the *single* scope block contains exactly one
  `include(` — count it, do not merely check membership.

**`tests/test_integration_multi_doc.py` — non-assert-participating comment.** The comment documenting
the emitted format quotes the superseded shape. Rewrite it to the new shape. Then measure the adjacent
disjunctive assertion: it checks for a partial form of the literal, so it may well still pass against
the new emission. Whatever the measurement shows, record it — and if it passes for both the old and the
new shape, add a note in the docstring saying so, because an assertion that cannot tell the two apart
is documentation, not a guard.

**`tests/test_pdf_render_gate.py` — non-assert-participating reconstruction.** The hand-built pre-fix
shape at the site SC#6 names is a literal string standing in for output an *unrelated* table-caption
bug produced; the two assertions around it inspect only the caption sentinels, never the heading
argument name. Update the argument name so the reconstruction stays consistent with what the translator
emits system-wide, and extend the surrounding comment to say that the argument name was migrated by
TOC-01 and that the reconstruction still faithfully reproduces the table-caption bug shape it exists to
represent. Do not touch the two sentinel assertions or the sentinel constants.

**`tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` — byte-compared fixture. Regenerate,
do not edit.** This file is compared with `actual_typ == golden_typ` against a live build, so a
hand-edited value would be a transcription of what the assertion wants rather than a capture of what
the code produces. Run `sphinx-build -b typst` over `tests/fixtures/desc_rubric_decoupling_render_gate`
into a scratch directory using the NixOS-safe `sys.executable -m sphinx` form (or `uv run python -m
sphinx`), read the emitted `.typ` with `encoding="utf-8"`, and write it to `golden.typ` verbatim. Then
`diff` the regenerated golden against the build output and require an empty diff, and `git diff` the
golden and require the change to be confined to the heading argument name on the single line SC#6 names
— any other line moving means the regeneration captured an unrelated drift and must be investigated
before it is committed.

Run `uv run pytest` over each touched file as you go, and lint/format at the end.
  </action>

  <verify>
    <automated>test -z "$(grep -rn 'heading(level:' tests/test_translator.py tests/test_toctree_requirement13.py tests/test_integration_multi_doc.py tests/test_pdf_render_gate.py tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ)" &amp;&amp; echo "OK: old heading literal absent"</automated>
    <automated>test -z "$(grep -rn 'set heading(offset: 1)' tests/test_translator.py tests/test_toctree_requirement13.py tests/test_integration_multi_doc.py)" &amp;&amp; echo "OK: old scope literal absent"</automated>
    <automated>uv run pytest tests/test_toctree_requirement13.py tests/test_translator.py tests/test_integration_multi_doc.py -q</automated>
    <automated>uv run pytest tests/test_desc_rubric_decoupling_render_gate.py tests/test_pdf_render_gate.py -q</automated>
    <automated>uv run ruff check tests/ &amp;&amp; uv run black --check tests/</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn 'heading(level:' tests/test_translator.py tests/test_toctree_requirement13.py
      tests/test_integration_multi_doc.py tests/test_pdf_render_gate.py
      tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` returns nothing.
    - `grep -rn 'set heading(offset: 1)' tests/` returns nothing across the whole tests tree — the old
      absolute scope literal survives nowhere, in assertions, docstrings or comments.
    - `grep -c 'heading.offset' tests/test_toctree_requirement13.py` returns at least `3` — the
      substring assertion, the regex, and the single-entry assertion all reference the new form.
    - `grep -c 'len(matches) == 1' tests/test_toctree_requirement13.py` returns at least `1` — the
      exactly-once semantics survived, and running that test proves the regex actually matches once
      rather than zero times.
    - `tests/test_toctree_requirement13.py` contains a test asserting an empty-`entries` toctree
      produces no scope opener and no `include(`, and its single-entry test counts `include(`
      occurrences rather than testing membership: `grep -c 'count("include(' tests/test_toctree_requirement13.py`
      returns at least `1`.
    - `grep -c 'heading(" not in' tests/test_translator.py` is unchanged from its pre-task value — the
      two keyword-agnostic table-caption guards were not touched.
    - `grep -c 'TBLCAP_FIRST_SENTINEL' tests/test_pdf_render_gate.py` and
      `grep -c 'TBLCAP_SECOND_SENTINEL' tests/test_pdf_render_gate.py` are unchanged — the
      reconstruction's own assertions and constants are untouched.
    - `git diff --numstat tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` shows exactly
      one line added and one line removed, and `git diff` on that file shows the change confined to the
      heading argument name.
    - A `diff` between the regenerated `golden.typ` and the live `sphinx-build` output of its own
      fixture is empty, and that diff command and its output are recorded.
    - `uv run pytest tests/test_toctree_requirement13.py tests/test_translator.py
      tests/test_integration_multi_doc.py tests/test_desc_rubric_decoupling_render_gate.py
      tests/test_pdf_render_gate.py -q` exits 0.
    - `uv run ruff check tests/` and `uv run black --check tests/` exit 0.
  </acceptance_criteria>

  <done>
    Every site on § 3's worklist carries the literal the translator now emits; the exactly-once regex
    still matches exactly once against real output; the empty and single-entry toctree edges are
    asserted; the byte-compared golden was regenerated from a real build and diffs to zero against it;
    and every touched test file is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Discharge the proof obligations against the pre-fix anchor commit</name>

  <files>.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-03.md</files>

  <precondition>
    `git status --porcelain typsphinx/` is empty before this task starts — the pre-fix measurement
    below reverts one file under `typsphinx/` and restores it, and an unclean starting state would make
    the restore unverifiable.
  </precondition>

  <read_first>
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-03.md § 3
      (the worklist and its per-site proof obligations)
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-01.md § 5
      (the anchor SHA)
    - .planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md
      §§ 1-2 (the house pattern for naming a pre-fix commit and proving the measurement really ran
      against pre-fix code)
    - CLAUDE.md § "Worktree-isolated execution"
  </read_first>

  <action>
Discharge every row of § 3's worklist and record the result. These are unit and fixture-comparison
tests over the translator's emitted string, so reverting a single file reproduces pre-fix emission
exactly and no second worktree is needed.

Inside this plan's own provisioned worktree:

1. Re-assert the isolation proof from `<worktree_provisioning>` and record its output. If
   `typsphinx.__file__` resolves outside this worktree, the revert below changes nothing that pytest
   imports and every figure produced here is void.
2. `git checkout <anchor SHA> -- typsphinx/translator.py`
3. Confirm the revert landed: `git diff --stat HEAD -- typsphinx/translator.py` must be non-empty, and
   a grep of the reverted file must show the pre-fix absolute forms present again. Record both.
4. Run the five touched test modules and capture the full output.
5. `git checkout HEAD -- typsphinx/translator.py`
6. Confirm the restore: `git status --porcelain typsphinx/` must be empty again. Record it. Do not
   write the verdict until this passes.

Per class:

- **assert-participating positive** and **assert-participating regex** rows: the rewritten assertion
  must appear as a FAILURE in step 4's output. Record the node id and the failure text. For the regex
  row, additionally record that the pre-fix match count was zero (so the exactly-once assertion failed
  by counting nothing) and that the post-fix count is one — the two numbers together are what proves
  the rewrite preserved the semantics rather than merely relocating the literal. If a row expected to
  fail passes, stop: that assertion does not distinguish pre-fix from post-fix and must be reworked in
  task 2 before this task can complete.
- **byte-compared fixture** row: record that the golden comparison test fails against the reverted
  translator (the emitted `.typ` no longer matches the regenerated golden) and passes against the
  restored one. This is the strongest of the four proofs and needs no special handling.
- **non-assert-participating** rows: no RED is obtainable, and none may be manufactured. Record instead
  the measurement that establishes it — the enclosing test passes both before and after the edit — and
  name, for each such site, which assertions the enclosing test actually makes and why none of them
  inspects the migrated literal. State plainly that these are consistency edits keeping the repository's
  depiction of emitted output truthful, not guards.

Write all of this into `44.1-GATE-EVIDENCE-03.md` § 4 "Proof discharge", one subsection per row, each
carrying its command and raw output. Add § 5 "Verdict": a row-by-row table of site, class, proof shape,
discharged yes/no, and the artefact that discharges it.

Then run the full suite (`uv run pytest -q`) and record its state in § 6. Combined with plan 02's work
this should be green; if anything still fails, list it by node id and say whether it belongs to this
phase's scope or to something else. Also re-run the resolved-level gate from plan 01 explicitly and
record that it is still green — the test migrations must not have disturbed it.
  </action>

  <verify>
    <automated>test -z "$(git status --porcelain typsphinx/)" &amp;&amp; echo "OK: typsphinx/ restored clean"</automated>
    <automated>uv run pytest tests/test_heading_depth_render_gate.py -q</automated>
    <automated>uv run pytest -q 2>&1 | tail -25</automated>
  </verify>

  <acceptance_criteria>
    - `44.1-GATE-EVIDENCE-03.md` § 4 contains, for every row of § 3, the command run and its raw
      output — no summarised or paraphrased result.
    - § 4 records the worktree isolation proof output taken immediately before the revert, the revert
      landing (non-empty `git diff --stat` against the reverted file), and the restore
      (`git status --porcelain typsphinx/` empty afterwards).
    - Every row classified `assert-participating positive` or `assert-participating regex` appears as an
      actual pytest FAILURE in § 4's captured pre-fix run, quoted by node id.
    - The regex row additionally records the pre-fix match count (`0`) and the post-fix match count
      (`1`) with the commands that produced both.
    - The `byte-compared fixture` row records the golden comparison failing pre-fix and passing
      post-fix, with the unified-diff excerpt from the failure message.
    - Every `non-assert-participating` row names the assertions its enclosing test actually makes and
      states in one sentence why no RED is obtainable — no such row claims a RED.
    - § 5's verdict table has one row per site with `discharged: yes` in every row.
    - `git status --porcelain typsphinx/` is empty at the end of this task.
    - `uv run pytest tests/test_heading_depth_render_gate.py -q` exits 0 — plan 01's gate is undisturbed.
    - § 6 records the full-suite state with any remaining failure listed by node id and classified as
      in-scope or out-of-scope.
  </acceptance_criteria>

  <done>
    Every site on this plan's worklist has a discharged, recorded proof appropriate to its class: an
    actual pre-fix failure for the assertions, a regenerate-and-diff for the golden fixture, and an
    explicit "no RED obtainable, here is why" measurement for the two sites whose literal no assertion
    inspects. `typsphinx/` is restored byte-clean and plan 01's gate is still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a transcribed literal -> the bytes the translator actually emits | A hand-written regex or golden line can diverge from real output while still looking correct |
| `git checkout <sha> -- typsphinx/translator.py` -> the working tree | A transient revert of production source inside a worktree the merge step will read |
| test process -> `import typsphinx` | An editable-install finder can resolve the import outside this worktree, detaching the measurement from the revert |
| a census inherited from a document -> the census in the repository | A site absent from the inherited list keeps encoding the superseded contract, invisibly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44.1-10 | Spoofing | the exactly-once regex in the toctree test | high | mitigate | A mis-escaped regex matches zero times; the exactly-once assertion then fails loudly, but a regex that matches once *for the wrong reason* would pass while counting the wrong thing. Mitigated by running the regex against real translator output and by recording the pre-fix count (`0`) beside the post-fix count (`1`). |
| T-44.1-11 | Tampering | `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` | high | mitigate | Hand-editing a byte-compared golden to the value the assertion wants converts a real gate into a tautology. Mitigated by regenerating it from a live `sphinx-build`, diffing regenerated-vs-build to zero, and requiring `git diff --numstat` to show exactly one line changed. |
| T-44.1-12 | Tampering | the transient `typsphinx/translator.py` revert in task 3 | high | mitigate | A revert left in place would be merged and would silently undo plan 01. Mitigated by an empty `git status --porcelain typsphinx/` as both a task precondition and an acceptance criterion, and again in `<verification>`. |
| T-44.1-13 | Repudiation | the site census | high | mitigate | A census inherited from a planning document was already measured low at planning time. Mitigated by re-deriving it with three greps in this session, recording the raw output, and requiring an explicit per-file divergence table rather than silent adoption. |
| T-44.1-14 | Repudiation | the pre-fix measurement | high | mitigate | The `typsphinx.__file__` isolation proof is captured immediately before the revert and stored in § 4; without it a measurement in an unprovisioned environment would report a pre-fix run that never happened. |
| T-44.1-SC | Tampering | package-manager installs | low | accept | This plan installs no package and does not modify `pyproject.toml`; no legitimacy checkpoint is applicable. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and paths created by this plan:

- `tests/test_toctree_requirement13.py`: a new test asserting an empty-`entries` toctree emits nothing
  (the empty edge), and a strengthened single-entry assertion counting `include(` occurrences
- `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-03.md`
- `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` is regenerated, not created

Created by sibling plans and referenced here: `tests/test_heading_depth_render_gate.py` and
`44.1-GATE-EVIDENCE-01.md` (plan 01); `_section_title_output` in `tests/test_topics.py` and
`44.1-GATE-EVIDENCE-02.md` (plan 02); `44.1-GATE-EVIDENCE-04.md` (plan 04); `emitted_depth` in
`typsphinx/translator.py` (plan 01).
</artifacts_this_phase_produces>

<flagged_assumptions>
1. **CONTEXT's D-04 addendum census is a floor, not a ceiling.** A planning-time repo-wide grep found
   assertion sites over the absolute-offset literal beyond the two the addendum names, plus one in a
   file the addendum does not mention. Task 1 re-derives the census and records the divergence per file.
   If a verifier reads SC#6's file-and-line list literally, it should read § 2 first.
2. **Two sites can produce no RED, by construction.** The hand-built reconstruction and the format
   comment carry the migrated literal in text no assertion inspects. This plan records the measurement
   proving that rather than manufacturing a failure, and marks them explicitly as consistency edits.
3. **One surviving assertion may be unable to tell the old shape from the new.** The disjunctive
   assertion beside the format comment checks a partial form of the literal. Task 2 measures whether it
   discriminates and records the answer; if it does not, that is documented at the site rather than
   quietly relied on as a guard.
</flagged_assumptions>

<verification>
- `grep -rn 'set heading(offset: 1)' tests/ typsphinx/` returns nothing.
- `grep -rn 'heading(level:' tests/ typsphinx/` returns nothing (plan 02 clears the remaining file).
- `uv run pytest -q` exits 0 across the whole suite.
- `uv run pytest tests/test_heading_depth_render_gate.py -q` exits 0 — plan 01's gate is undisturbed.
- `git status --porcelain typsphinx/` is empty — no production source is left modified by this plan.
- `git diff --numstat tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` shows a one-line
  change.
- `uv run ruff check . && uv run black --check .` exit 0.
- `44.1-GATE-EVIDENCE-03.md` § 5's verdict table shows `discharged: yes` for every site, and § 2
  records the census divergence.
</verification>

<success_criteria>
- SC#6 met for the five files this plan owns: every site migrated deliberately, traceable to TOC-01,
  with a discharged proof appropriate to its class.
- D-07's addendum obligation met: the toctree-side assertions target the new literal and the
  exactly-once semantics survive, proved by a measured pre-fix count of zero and post-fix count of one.
- The census is measured, its divergence from CONTEXT's figure is recorded, and this plan's worklist
  provably partitions the census with plan 02's.
- The byte-compared golden is a capture of real output, not a transcription.
- The empty and single-element toctree edges are asserted rather than assumed.
- D-05 holds: nothing was promoted to a build-and-compile test.
</success_criteria>

<output>
Create `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-03-SUMMARY.md` when done.
Record in it: the corrected census and its divergence from CONTEXT's figure, the per-site proof shapes
and their discharge, the golden regeneration diff, and the full-suite state.
</output>