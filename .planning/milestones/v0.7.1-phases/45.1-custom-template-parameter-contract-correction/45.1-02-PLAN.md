---
phase: 45.1-custom-template-parameter-contract-correction
plan: 02
type: execute
wave: 2
depends_on: ["45.1-01"]
files_modified:
  - tests/fixtures/params_exclusivity_gate/partial_params_template/conf.py
  - tests/fixtures/params_exclusivity_gate/partial_params_template/index.rst
  - tests/fixtures/params_exclusivity_gate/partial_params_template/chapter.rst
  - tests/fixtures/params_exclusivity_gate/partial_params_template/_templates/partial.typ
  - tests/fixtures/params_exclusivity_gate/zero_params_default/conf.py
  - tests/fixtures/params_exclusivity_gate/zero_params_default/index.rst
  - tests/fixtures/params_exclusivity_gate/package_params/conf.py
  - tests/fixtures/params_exclusivity_gate/package_params/index.rst
  - tests/test_params_exclusivity_gate.py
autonomous: true
requirements: [CONF-11]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-B: exclusivity holds uniformly on all three routes named by SC#7 — an explicit typst_template, a typst_package, and the bundled default template — proven by three separate real builds emitting the #show rule call, not by reading the merge code"
    - "D-B: on the typst_template route a params declaration naming one key emits exactly that one named argument; title, authors and date are absent from the emitted call even though the pre-fix union supplied them"
    - "D-B: on the typst_package route an auto-derived value produced by typst_template_mapping is absent from the emitted call whenever params is present, so the package path needs no special case (this is what makes D-C's documentation-only stance sufficient)"
    - "D-D: on the bundled-default route an empty params dict emits a call with zero named arguments even though typsphinx/templates/base.typ would happily accept all nine"
    - "D-K/SC#3: every assertion added here is proved failing against the pre-fix tree at the recorded pre-45.1-01 commit, in a detached checkout — never reconstructed only from the post-fix emission"
    - "Every fixture under tests/fixtures/params_exclusivity_gate/ declares typst_template_function with a params key, so its expected emission is independent of the CONF-12 lang change landing in parallel in wave 2"
  artifacts:
    - path: "tests/fixtures/params_exclusivity_gate/partial_params_template/conf.py"
      provides: "typst_template route with a single-key params declaration colliding with nothing"
      contains: "typst_template_function"
    - path: "tests/fixtures/params_exclusivity_gate/zero_params_default/conf.py"
      provides: "bundled-default route with an empty params dict"
      contains: "params"
    - path: "tests/fixtures/params_exclusivity_gate/package_params/conf.py"
      provides: "typst_package route where typst_template_mapping supplies a key that params does not"
      contains: "typst_package"
    - path: "tests/test_params_exclusivity_gate.py"
      provides: "The three-route exclusivity matrix on top of plan 01's tracer case"
      min_lines: 200
  key_links:
    - from: "tests/test_params_exclusivity_gate.py"
      to: "tests/fixtures/params_exclusivity_gate/package_params/"
      via: "sys.executable -m sphinx -b typst subprocess build asserting the mapped key is absent"
      pattern: "package_params"
    - from: "45.1-GATE-EVIDENCE-02.md"
      to: "the pre-45.1-01 commit"
      via: "a detached git worktree at the recorded SHA, built with the same fixtures"
      pattern: "git worktree add"
---

<objective>
Prove D-B's exclusivity on the two remaining routes SC#7 names — `typst_package` and the bundled
default — plus the partial-`params` case on the `typst_template` route, and prove each assertion
RED against the pre-fix tree.

Purpose: plan 45.1-01 proved the thinnest slice (`params: {}` on one custom-template route). SC#7
requires the rule to hold "on the `typst_template`, `typst_package` and bundled-default routes
alike". The package route matters most: D-C's decision to close the measured
`TypstError: unexpected argument: papersize` fatal by documentation rather than code is only sound
if a declared `params` genuinely suppresses every auto-derived and merged key there.

Output: three fixture projects, the exclusivity matrix in the gate module, and a recorded pre-fix
RED proof taken against the real pre-fix commit rather than reconstructed.
</objective>

## Artifacts this phase produces

**Added by this plan**
- `tests/fixtures/params_exclusivity_gate/partial_params_template/` — new fixture project
- `tests/fixtures/params_exclusivity_gate/zero_params_default/` — new fixture project
- `tests/fixtures/params_exclusivity_gate/package_params/` — new fixture project
- `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-02.md` — the pre-fix RED record for the three routes

**Removed by this plan**
- Nothing. This plan adds gate coverage only; it changes no production symbol.

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the three remaining route fixtures and record their pre-fix RED</name>
  <precondition>The pre-45.1-01 commit SHA — the tree state before plan 45.1-01's implementation commit — is resolvable from git history and can be checked out into a detached worktree; if `git log --oneline` cannot identify it, halt and ask rather than reconstructing the pre-fix basis from the post-fix emission.</precondition>
  <files>tests/fixtures/params_exclusivity_gate/partial_params_template/conf.py, tests/fixtures/params_exclusivity_gate/partial_params_template/index.rst, tests/fixtures/params_exclusivity_gate/partial_params_template/chapter.rst, tests/fixtures/params_exclusivity_gate/partial_params_template/_templates/partial.typ, tests/fixtures/params_exclusivity_gate/zero_params_default/conf.py, tests/fixtures/params_exclusivity_gate/zero_params_default/index.rst, tests/fixtures/params_exclusivity_gate/package_params/conf.py, tests/fixtures/params_exclusivity_gate/package_params/index.rst</files>
  <read_first>
    - `tests/fixtures/params_exclusivity_gate/zero_params_template/conf.py` and `_templates/zero_param.typ` — plan 45.1-01's fixture, the shape these three copy
    - `tests/fixtures/package_only_config_gate/conf.py` in full — the existing `@preview/charged-ieee:0.1.4` package-route fixture, including its `typst_template_mapping = {"project": "title"}` single-key mapping and the header comment explaining why `typst_template` must never be set alongside `typst_package` in such a fixture
    - `typsphinx/templates/base.typ` lines 39-50 — the nine-parameter signature the `zero_params_default` fixture routes onto
    - `typsphinx/template_engine.py` `render()` as changed by plan 45.1-01
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md` `<specifics>` items 3, 4 and 5 — the measured pre-fix emissions these fixtures must reproduce
  </read_first>
  <action>
    Create three fixture projects under `tests/fixtures/params_exclusivity_gate/`.

    `partial_params_template/` — the `typst_template` route with a non-empty, partial declaration.
    `conf.py` sets `typst_template = "_templates/partial.typ"`,
    `typst_elements = {"papersize": "a4"}`, a `typst_documents` entry making `index` a master, and
    `typst_template_function = {"name": "project", "params": {"subtitle": "A Gate Subtitle"}}`.
    `index.rst` carries a toctree with `:maxdepth: 2` and `:caption: Partial Contents` pointing at
    `chapter.rst`. `_templates/partial.typ` declares `subtitle` and the trailing positional body
    and nothing else — deliberately no `title`, no `authors`, no `date`. Under the pre-fix union
    this build receives seven-plus named arguments against a one-parameter template and aborts;
    post-fix it receives `subtitle` alone.

    `zero_params_default/` — the bundled-default route. `conf.py` sets no `typst_template` and no
    `typst_package`, sets `typst_elements = {"papersize": "a4", "fontsize": "11pt"}`, a
    `typst_documents` entry making `index` a master, and
    `typst_template_function = {"name": "project", "params": {}}`. `index.rst` carries a toctree
    with `:numbered:`. This one compiles both pre- and post-fix, because `templates/base.typ`
    declares defaults for all nine — its RED is structural, not a compile fatal: pre-fix the
    emitted call carries the full derived set, post-fix it carries zero named arguments. Record it
    as a structural-RED case, which is the amended definition of RED this project adopted in
    v0.7.0 and which STATE.md's "Standing GATE-01 bar" paragraph describes.

    `package_params/` — the `typst_package` route with a mapping-supplied key that `params` does
    not name. `conf.py` sets `typst_package = "@preview/charged-ieee:0.1.4"`,
    `typst_template_mapping = {"project": "title"}`, a `typst_documents` entry making `index` a
    master, and `typst_template_function = {"name": "ieee", "params": {"abstract": "...",
    "paper-size": "a4"}}` — with **no** `title` key in `params`. Do not set `typst_template` here;
    the header comment in `tests/fixtures/package_only_config_gate/conf.py` explains why that would
    route the fixture off the package-alone path. Pre-fix the emitted call carries three named
    arguments including the mapped `title`; post-fix it carries the two declared keys and no
    `title`. This is the fixture that discharges D-C: it shows the package path needs no code
    special case once `params` is authoritative.

    Every fixture here declares a `params` key, deliberately: that makes each one's expected
    emission independent of the CONF-12 `lang` change landing in parallel in plan 45.1-03. Do not
    add a fixture without `params` to this directory.

    Then record RED. Create a detached worktree at the pre-45.1-01 commit
    (`git worktree add <scratch-dir> <sha>`), copy these three fixture directories into it,
    provision it with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, and run
    `sphinx-build -b typstpdf` over each. Capture, verbatim, each run's exit code and each emitted
    `index.typ`'s `#show:` region, and write all three into
    `45.1-GATE-EVIDENCE-02.md` in the phase directory with the command lines and the SHA shown.
    Remove the scratch worktree afterwards. Do not reconstruct the pre-fix basis by editing the
    post-fix emission by hand.
  </action>
  <verify>
    <automated>test -f .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-02.md && grep -qE 'partial_params_template' .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-02.md && grep -qE 'zero_params_default' .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-02.md && grep -qE 'package_params' .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-02.md</automated>
  </verify>
  <acceptance_criteria>
    - All three fixture directories exist, each with a `conf.py` and an `index.rst`, and `partial_params_template/` additionally has `chapter.rst` and `_templates/partial.typ`
    - `grep -L 'params' tests/fixtures/params_exclusivity_gate/*/conf.py` returns no output — every fixture in the directory declares a `params` key
    - `grep -c 'typst_template' tests/fixtures/params_exclusivity_gate/package_params/conf.py` returns a count that comes only from `typst_template_mapping` and `typst_template_function`, with no bare `typst_template =` assignment (verify with `! grep -Eq '^typst_template[[:space:]]*=' tests/fixtures/params_exclusivity_gate/package_params/conf.py`)
    - `45.1-GATE-EVIDENCE-02.md` records, for each of the three fixtures, the pre-fix commit SHA, the exact command line, the exit code, and the verbatim emitted `#show:` region
    - The recorded `partial_params_template` run has a non-zero exit code; the recorded `zero_params_default` and `package_params` runs each show an emitted `#show:` region carrying named arguments that the post-fix run will not carry
  </acceptance_criteria>
  <done>Three fixture projects covering the remaining SC#7 routes exist and their pre-fix behaviour is recorded from a real build of the real pre-fix tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the three-route exclusivity matrix into the gate module</name>
  <files>tests/test_params_exclusivity_gate.py</files>
  <read_first>
    - `tests/test_params_exclusivity_gate.py` as created by plan 45.1-01 — extend it, do not start a second module
    - `tests/test_package_only_config_gate.py` — the `_show_rule_call_region()` helper and the class-scoped `build` fixture pattern, and the module docstring's rule that no assertion matches Typst compiler error text
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-02.md` — the recorded pre-fix emissions each new assertion must contradict
  </read_first>
  <behavior>
    - `partial_params_template`: the emitted `#show: project.with(` region contains exactly one named argument, `subtitle`, and the build compiles to a valid PDF
    - `partial_params_template`: `title`, `authors`, `date`, `papersize`, `toctree_maxdepth` and `toctree_caption` are each absent as named arguments in that region
    - `zero_params_default`: the emitted region contains zero named arguments and the build compiles to a valid PDF, even though the inlined `templates/base.typ` declares nine parameters
    - `package_params`: the emitted `#show: ieee.with(` region contains exactly the two declared keys and no `title`, and the build compiles to a valid PDF
    - Subset invariant: for a `params` declaration whose key set is a strict subset of the auto-derived key set, the emitted key set equals the `params` key set exactly — the assertion that goes red if the additive union ever returns
  </behavior>
  <action>
    Extend `tests/test_params_exclusivity_gate.py` with one test class per fixture, each following
    the module's existing class-scoped `build` fixture shape. Use `-b typstpdf` where the criterion
    is "it compiles" and `-b typst` where the criterion is only about emitted source text, matching
    the split `tests/test_package_only_config_gate.py`'s module docstring describes.

    Add an explicit subset-invariant test named for what it guards: given a `params` key set that
    is a strict subset of the auto-derived key set, assert set equality between the emitted named
    arguments and the `params` keys — not containment. Its docstring must state that it is the
    assumption-delta guard for D-B, and that it goes red the moment a per-key merge is reintroduced.

    Every assertion added here must correspond to a recorded row in `45.1-GATE-EVIDENCE-02.md`.
    Where the recorded pre-fix run compiled successfully (`zero_params_default`, `package_params`),
    say so in the test docstring and name the assertion a structural-RED case rather than implying
    a compile fatal. Match no Typst compiler error text.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest tests/test_params_exclusivity_gate.py -q && uv run pytest -q && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_params_exclusivity_gate.py -q` exits 0 and collects at least four test classes (the tracer case plus the three routes)
    - The module contains a test whose name identifies it as the subset invariant and whose body asserts set equality between the emitted named-argument names and the declared `params` keys
    - A fresh `-b typst` build of `tests/fixtures/params_exclusivity_gate/package_params/` emits an `#show: ieee.with(` region in which `title` does not appear as a named argument
    - A fresh `-b typst` build of `tests/fixtures/params_exclusivity_gate/zero_params_default/` emits a `#show: project.with(` line immediately followed by a `)` line
    - Two consecutive `-b typst` builds of `tests/fixtures/params_exclusivity_gate/partial_params_template/` into different output directories produce byte-identical `index.typ` files (`cmp` exits 0) — the CONF-11 ordering edge
    - `uv run pytest -q`, `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` all exit 0
  </acceptance_criteria>
  <done>SC#7's "on the `typst_template`, `typst_package` and bundled-default routes alike" claim is backed by three real builds, each contradicting a recorded pre-fix emission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → Typst Universe | `package_params/` fetches `@preview/charged-ieee:0.1.4` over the network during `typst.compile()` |
| pre-fix evidence → gate assertion | A RED proof reconstructed from the post-fix tree rather than taken from the real pre-fix commit is circular and proves nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45.1-12 | Repudiation | the RED proof for the three new routes | medium | mitigate | The proof runs against a detached `git worktree` at the recorded pre-45.1-01 SHA, with the SHA, command line and verbatim output written into `45.1-GATE-EVIDENCE-02.md`; the task carries a `<precondition>` that halts if the SHA cannot be identified. |
| T-45.1-14 | Denial of Service (build) | network fetch of `@preview/charged-ieee:0.1.4` during the `package_params` compile | low | accept | The same fetch is already a standing test-time cost from `tests/fixtures/package_only_config_gate/` (Phase 22.2); the pin is unchanged and no new package is introduced. |
| T-45.1-SC | Tampering | third-party package fetch during tests | low | accept | No npm/pip/cargo install task exists in this plan, so no package-legitimacy audit is owed. The Typst Universe pin `@preview/charged-ieee:0.1.4` is reused verbatim from an in-tree fixture rather than newly selected. |
</threat_model>

<verification>
- `uv run pytest tests/test_params_exclusivity_gate.py -q` exits 0
- `uv run pytest -q` exits 0 over the whole suite
- `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0
- `45.1-GATE-EVIDENCE-02.md` records a real pre-fix run for each of the three new fixtures
</verification>

<success_criteria>
- SC#7's three-route claim is proven by build, not by reading the merge code
- D-C's documentation-only stance is backed by the `package_params` fixture showing the package route needs no special case
- SC#3's RED-before-GREEN rule holds for every assertion added
</success_criteria>

<output>
Create `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-02-SUMMARY.md` when done
</output>
