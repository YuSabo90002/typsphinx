---
phase: 45.1-custom-template-parameter-contract-correction
plan: 05
type: execute
wave: 4
depends_on: ["45.1-01", "45.1-02", "45.1-03", "45.1-04"]
files_modified:
  - docs/source/user_guide/templates.rst
  - docs/source/user_guide/configuration.rst
  - docs/source/examples/advanced.rst
autonomous: true
requirements: [DOC-13]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-A: templates.rst publishes the default parameter set as nine named parameters plus the trailing positional body, each with its emission condition — title/authors/date unconditional on the non-package path, papersize/fontsize only when set in typst_elements, lang auto-derived on every non-package route, and the three toctree_* keys only when the master carries a toctree"
    - "D-A: templates.rst states that the authors parameter is always a Typst array and never a bare string, and that a string source is split on commas"
    - "D-B/D-E: configuration.rst's precedence model describes exclusivity, not a per-key merge — when a params key is declared the auto-derived set is discarded entirely, and stage 1's own rules become subordinate to the case where params is absent"
    - "D-E: the note recommending migration to the params route for the authors parameter alone is gone — under exclusivity that advice silently drops the other eight parameters"
    - "D-C: templates.rst's Configuration-Based Templates section states that a typst_package configuration requires typst_template_function's params, and deprecates the function-name-only form as documented misuse rather than a defect typsphinx handles in code"
    - "D-G: all eight example templates and all four typst_template_function configuration examples across templates.rst and examples/advanced.rst are verified by a real build, not by reading"
    - "D-J/SC#4: the standing consequence — adding a template parameter is a breaking change for a correctly-written custom template — is recorded in prose wherever the contract is documented"
    - "DOC-13 / edge: empty — the published contract states what a master with no toctree and no typst_elements receives, so a reader can tell which of the nine are conditional and which are not"
  prohibitions:
    - "Do NOT build a test that holds the contract's surfaces in lockstep — not over the emission sites, not over templates/base.typ, templates.rst and docs/source/_typst/custom_template.typ. The owner declined it explicitly even though tests/test_preview_version_sync.py is the established in-repo precedent for that exact shape; record the standing consequence in prose instead"
    - "Do NOT correct the published contract by prose alone and call the phase done — every claim added to these three pages must correspond to behaviour a build in this phase actually produced, and any claim that cannot be built must be removed rather than softened"
  artifacts:
    - path: "docs/source/user_guide/templates.rst"
      provides: "The nine-parameter default contract, the deprecation of the function-name-only package form, the standing breaking-change consequence, and five corrected example templates"
      contains: "toctree_maxdepth"
    - path: "docs/source/user_guide/configuration.rst"
      provides: "The exclusivity-based precedence model replacing the two-stage per-key merge"
    - path: "docs/source/examples/advanced.rst"
      provides: "Three corrected example templates and two corrected configuration examples"
  key_links:
    - from: "docs/source/user_guide/templates.rst Standard Parameters"
      to: "typsphinx/templates/base.typ lines 39-50"
      via: "the published parameter list mirroring the bundled nine-parameter signature, name for name and default for default"
      pattern: "toctree_caption"
    - from: "docs/source/user_guide/configuration.rst precedence section"
      to: "typsphinx/template_engine.py render()'s exclusive branch"
      via: "the prose describing wholesale replacement rather than per-key override"
      pattern: "params"
---

<objective>
Publish the corrected custom-template parameter contract (DOC-13) across the three documentation
surfaces that carry it, and prove every example on those pages by a real build.

Purpose: `templates.rst` publishes four parameters where typsphinx passes up to nine, and four of
the page's five example templates are measured to fail. `configuration.rst` describes a two-stage
per-key merge that CONF-11 has just replaced with wholesale replacement, and its closing note
recommends a migration that now silently drops eight parameters. `examples/advanced.rst` carries
three more under-declared templates and two configuration examples whose behaviour changed.

Output: the nine-parameter contract with per-parameter emission conditions, the exclusivity-based
precedence model, the deprecation of the function-name-only package form, the standing
breaking-change consequence in prose, and twelve build-verified examples.
</objective>

## Artifacts this phase produces

**Added by this plan**
- `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-05.md` — the D-G build record for all twelve documented examples

**Removed by this plan**
- The four-parameter Standard Parameters list in `docs/source/user_guide/templates.rst`
- The two-stage per-key precedence model in `docs/source/user_guide/configuration.rst` (the section beginning at the "Precedence" paragraph) and the closing note recommending migration to the params route for the authors parameter alone
- Every remaining prose mention of the config value removed in plan 45.1-04, across all three pages

**Deliberately NOT produced**
- A lockstep sync test over the contract's surfaces. D-J declines it. `docs/source/_typst/custom_template.typ` therefore remains the unguarded fourth lockstep site the v0.6.4 review flagged, and that standing consequence is recorded in prose by this plan rather than closed by a test.

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-01-SUMMARY.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-03-SUMMARY.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the nine-parameter contract, the package deprecation, and the standing consequence in templates.rst</name>
  <files>docs/source/user_guide/templates.rst</files>
  <read_first>
    - `docs/source/user_guide/templates.rst` in full — the Configuration-Based Templates section (from line 35), the Custom Template Files walkthrough and its Basic Structure example (line 144), Template Parameters / Standard Parameters (lines 186-192), Custom Parameters and its configuration example plus template (lines 194-226), Wrapping External Packages (line 237), and the Examples section's Minimal (line 272) and Academic Paper (line 287) templates
    - `typsphinx/templates/base.typ` lines 39-50 — the bundled nine-parameter signature, name for name and default for default
    - `docs/source/_typst/custom_template.typ` lines 64-75 — the repository's own nine-parameter declaration, the second already-correct surface
    - `typsphinx/template_engine.py` `map_parameters()` and `_convert_to_authors_tuple()` — the emission conditions and the comma split
    - `typsphinx/writer.py` lines 290-331 as changed by plan 45.1-03 — the three merge sites that decide which of the nine appear
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md` `<specifics>` item 3 (the full measured emitted call) and item 7 (the authors array shape and the comma split)
  </read_first>
  <action>
    Replace the four-item Standard Parameters list with the measured default set: nine named
    parameters plus the trailing positional `body`. State each parameter's emission condition
    explicitly, because a reader needs to know which are conditional: `title`, `authors` and `date`
    arrive unconditionally on every non-package route; `papersize` and `fontsize` only when the
    corresponding key is set in `typst_elements`; `lang` on every non-package route, auto-derived
    from Sphinx's `language`, with an explicit `typst_elements` value winning; and
    `toctree_maxdepth`, `toctree_numbered` and `toctree_caption` only when the master document
    carries a toctree. Name the defaults from `typsphinx/templates/base.typ:39-50` so a reader can
    copy a declaration that matches. Say in the same section what a master with no toctree and no
    `typst_elements` receives, so the conditional set is unambiguous.

    Document the `authors` shape: always a Typst array, never a bare string, and a string source is
    split on commas — a single name becomes a one-element array, and "Alice Smith, Bob Jones"
    becomes a two-element array. Every example on this page that calls a join over `authors` is
    therefore correct against the array; the dictionary-valued form is gone as of plan 45.1-04.

    Record the standing consequence per D-J and SC#4, in prose, in this section: adding a template
    parameter is a breaking change for a correctly-written custom template, because Typst rejects a
    named argument the function never declared. State that this is what makes the next parameter
    addition a deliberate decision. Do not build a test to enforce it — D-J declined that, and
    `docs/source/_typst/custom_template.typ` stays an unguarded lockstep site by that decision.

    Rewrite the Configuration-Based Templates section per D-C: a `typst_package` configuration
    requires `typst_template_function` in its dict form with a `params` key, and the
    function-name-only form is deprecated. Explain why in one sentence — typsphinx cannot introspect
    a third-party Typst Universe function's signature, so passing it the default set is misuse
    rather than a bug — and state the observable outcome for a reader who ignores the rule: the
    build reaches a real Typst compile failure on an undeclared argument. This is documentation
    only; no code change accompanies it.

    Then correct the five example templates. Basic Structure (line 144) and Wrapping External
    Packages (line 237) are presented as complete templates for the default configuration: declare
    all nine plus the trailing body. Custom Parameters (line 214) is already correct as of CONF-11 —
    it pairs with the configuration example above it that declares `params`, so under exclusivity
    exactly those three named parameters arrive; add a sentence saying so and cross-reference the
    precedence section in `configuration.rst`. Minimal (line 272) and Academic Paper (line 287) keep
    their short signatures and instead gain the `typst_template_function` `params` declaration that
    makes each valid — for Minimal, a `params` dict naming only `title`; for Academic Paper, one
    naming `title`, `authors`, `abstract` and `keywords`. That pairing is the clearest teaching of
    CONF-11 available on this page.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run tox -e docs-html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'toctree_maxdepth' docs/source/user_guide/templates.rst` is at least 1, and `toctree_numbered`, `toctree_caption`, `papersize`, `fontsize` and `lang` each appear at least once in the Standard Parameters section
    - The Standard Parameters section names exactly nine parameters plus `body`, and each of the nine carries a stated emission condition
    - The page states that a master with no toctree and no `typst_elements` receives only the unconditional subset
    - The page states that the authors value is a Typst array and that a string source is split on commas
    - The page contains a sentence recording that adding a template parameter is a breaking change for a correctly-written custom template
    - The Configuration-Based Templates section states that a package configuration requires the params key and deprecates the function-name-only form
    - `grep -rn 'typst_authors' docs/source/user_guide/templates.rst | wc -l` returns `0`
    - `uv run tox -e docs-html` exits 0 with no new Sphinx warning attributable to this file
  </acceptance_criteria>
  <done>The page a reader copies from publishes the parameter set typsphinx actually passes, with each parameter's condition, the package-route rule, and the standing consequence.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite configuration.rst's precedence model onto exclusivity and correct advanced.rst</name>
  <files>docs/source/user_guide/configuration.rst, docs/source/examples/advanced.rst</files>
  <read_first>
    - `docs/source/user_guide/configuration.rst` lines 55-75 (Phase 44.2's rationale sentence, which CONF-11 vindicates) and lines 195-270 in full — the "Precedence" two-stage model, the target-key rule and its two bullet cases, the package-mapping side-door paragraph, and the closing note
    - `docs/source/examples/advanced.rst` in full — the configuration example at line 32 with its author-details block, the custom_ieee wrapping template at line 61, the styled template at line 144, the styled configuration at line 186, and the bibliography template at line 238
    - `typsphinx/template_engine.py` `render()`'s exclusive branch as changed by plan 45.1-01, and `map_parameters()` as changed by plan 45.1-04
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md` decisions D-B, D-E and D-F
  </read_first>
  <action>
    Rewrite `configuration.rst`'s precedence section so the top-level rule is exclusivity, not a
    merge. Lead with the question that now decides everything: is a `params` key present in
    `typst_template_function`'s dict form? If it is, those parameters are the complete set — the
    auto-derived `title`/`authors`/`date`, the `typst_elements` allowlist merge and the three
    `toctree_*` keys are all discarded, on the `typst_template`, `typst_package` and bundled-default
    routes alike, and an empty `params` dict passes nothing at all. If it is not, the existing
    stage-1 rules apply and stay factually correct — the target-key rule, its two bullet cases, and
    the package-mapping side-door paragraph all keep their content but become subordinate to the
    absent-`params` case rather than being stage one of two.

    Keep and re-anchor the sentence at lines 63-68 — "a user who has named both the template
    function and its arguments has already made a more specific decision than either". Phase 44.2
    published that rationale while documenting the mechanism as per-key override; it is now
    literally what the code does, and the section should say so.

    Delete the closing note that recommends migrating to the params route for the author parameter
    alone. Under exclusivity that advice is actively harmful: migrating for one key silently drops
    the other eight. Replace it with a warning that names the failure mode — a partial migration
    renders with the template's own defaults, an empty title and no author, rather than erroring —
    and points a reader who wants one extra parameter at declaring the full set they need. Remove
    every remaining prose mention of the config value plan 45.1-04 deleted, including the seed
    survival condition in the numbered summary near line 60, which no longer has a subject.

    In `examples/advanced.rst`, correct the two configuration examples and three templates. The
    configuration example at line 32 still declares the removed author-details value: move that
    author structure into the `typst_template_function` `params` dict directly above it, matching
    the migration plan 45.1-04 applied to `examples/charged-ieee/approach1/conf.py`. The styled
    configuration at line 186 declares only `primary-color` in `params`, which under exclusivity
    means the styled template at line 144 would never receive the `title` it declares — add `title`
    to that `params` dict and add a sentence explaining why it is now required. The custom_ieee
    wrapping template at line 61 and the bibliography template at line 238 are presented as complete
    templates for a default configuration: declare all nine plus the trailing body in each, matching
    the list Task 1 publishes.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run tox -e docs-html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'typst_authors' docs/source/ | wc -l` returns `0`
    - `configuration.rst`'s precedence section leads with the presence of the params key as the deciding question, and states that an empty params dict passes nothing
    - `configuration.rst` no longer contains a recommendation to prefer the params route for the author parameter alone; the replacement text names the partial-migration failure mode
    - `configuration.rst` retains the "more specific decision than either" sentence and states that it now describes the mechanism, not only the intent
    - `advanced.rst`'s styled configuration example declares `title` in its params dict
    - `advanced.rst`'s custom_ieee and bibliography templates each declare the nine parameters plus the trailing body
    - `uv run tox -e docs-html` exits 0 with no new Sphinx warning attributable to either file
  </acceptance_criteria>
  <done>The precedence model published matches the code, the harmful migration advice is gone, and advanced.rst's examples are consistent with exclusivity.</done>
</task>

<task type="auto">
  <name>Task 3: Build-verify all twelve documented examples and record the result</name>
  <precondition>Network access is available for `typst.compile()` to fetch the Typst Universe `@preview` packages that three of the twelve examples import; without it the package-importing examples cannot be verified and the task must halt rather than record them as passing.</precondition>
  <files>.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-05.md</files>
  <read_first>
    - `docs/source/user_guide/templates.rst` and `docs/source/examples/advanced.rst` as rewritten by Tasks 1 and 2 — the twelve examples to extract
    - `tests/test_package_only_config_gate.py`'s `_run_sphinx_build()` helper — the `sys.executable -m sphinx` invocation form to reuse in the scratch harness
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md` decision D-G and `<specifics>` item 2, which records that four of the five templates.rst examples fail today
  </read_first>
  <action>
    D-G requires verification by build, not by reading, and SC#2 asks for it against enumerated
    emission sites. Build a throwaway harness under the session scratch directory — not under
    `tests/` — that, for each of the twelve documented examples, materializes a minimal Sphinx
    project from the example's own text and runs `sphinx-build -b typstpdf` over it via
    `sys.executable -m sphinx`.

    The twelve are: five example templates in `templates.rst` (Basic Structure, the Custom
    Parameters template, Wrapping External Packages, Minimal, Academic Paper), three in
    `examples/advanced.rst` (custom_ieee, styled, bibliography), and four `typst_template_function`
    configuration examples (the Configuration-Based Templates block and the Custom Parameters block
    in `templates.rst`, and the two configuration blocks in `examples/advanced.rst`). Pair each
    template with the configuration the page presents alongside it, so the harness exercises the
    combination a reader would actually copy — a template verified against a configuration the page
    does not show proves nothing about the page.

    Give each example a master document with a toctree and a `typst_elements` setting wherever the
    page's own configuration does not already supply one, so the conditional half of the contract is
    live rather than dormant. That is the SC#1 shape: a template declaring exactly the published
    parameters, built over a master that has both a toctree and a `typst_elements` setting.

    Record every run in `45.1-GATE-EVIDENCE-05.md`: the example's page and section name, the exact
    command line, the exit code, and the emitted `#show:` region. A non-zero exit means the example
    is still wrong — fix the page and re-run rather than recording the failure as a known issue.

    Do not promote the harness into `tests/`. A permanent module that builds every documented
    example is one step from the multi-surface lockstep test D-J declined; the deliverable here is
    the recorded evidence, and the permanent regression coverage for the contract lives in
    `tests/test_params_exclusivity_gate.py` and `tests/test_documented_params_contract_gate.py`
    instead.
  </action>
  <verify>
    <automated>test -f .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-05.md && test "$(grep -cE '^\| *[0-9]+ *\|' .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-05.md)" -ge 12</automated>
  </verify>
  <acceptance_criteria>
    - `45.1-GATE-EVIDENCE-05.md` records twelve numbered entries, one per documented example, each with its page, section name, command line, exit code and emitted `#show:` region
    - Every recorded exit code is `0`
    - At least one recorded entry is a template declaring exactly the nine published parameters, built over a master carrying both a toctree and a `typst_elements` setting, with an exit code of `0` — SC#1's literal shape
    - No file was added under `tests/` by this task (`git status --porcelain tests/` is empty)
    - `uv run tox -e docs-html` and `uv run tox -e docs-pdf` both exit 0
  </acceptance_criteria>
  <done>Every example a reader can copy from these three pages has been built, and the record says so with exit codes and emitted output rather than with prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published documentation → user's configuration | These three pages are copy-paste source material; a wrong example is a defect delivered to every reader |
| documented example → real Typst compile | The only boundary that can tell a correct example from a plausible one |
| scratch verification harness → the test suite | A harness promoted into `tests/` becomes the lockstep test the owner declined |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45.1-10 | Tampering | a documented example template that does not compile | high | mitigate | Four of the five `templates.rst` examples are measured broken today, which is the defect this phase exists to close. Mitigated by Task 3: every one of the twelve is built with `-b typstpdf` and its exit code recorded, and a non-zero exit forces a page fix rather than a recorded caveat. |
| T-45.1-11 | Information disclosure (misdirection) | the closing note recommending a partial migration to the params route | medium | mitigate | Under exclusivity that advice silently drops eight parameters and renders an empty title with no author. Mitigated by deleting the note and replacing it with text that names the failure mode. |
| T-45.1-16 | Repudiation | a prose-only correction with no build behind it | medium | mitigate | Recorded as a plan prohibition and enforced by Task 3's evidence file: every claim added to these pages corresponds to a recorded build. |
| T-45.1-17 | Tampering | scratch harness promoted into the permanent suite | low | mitigate | An acceptance criterion asserts `git status --porcelain tests/` is empty after Task 3, keeping D-J's decision intact. |
| T-45.1-SC | Tampering | Typst Universe package fetch during example verification | low | accept | No npm/pip/cargo install task exists in this plan. Three documented examples import `@preview/charged-ieee:0.1.4`, the pin already in the tree; the task carries a `<precondition>` requiring network access rather than silently recording an unverified pass. |
</threat_model>

<verification>
- `grep -rn 'typst_authors' docs/source/ | wc -l` returns `0`
- `uv run tox -e docs-html` exits 0
- `uv run tox -e docs-pdf` exits 0
- `45.1-GATE-EVIDENCE-05.md` records twelve builds, all with exit code 0
- `git status --porcelain tests/` is empty

<!-- planner-discipline-allow: typst_authors -->
</verification>

<success_criteria>
- SC#1's shape is exercised and recorded: a template declaring exactly the published parameters, over a master with a toctree and a `typst_elements` setting, builds clean
- SC#4's standing consequence is recorded in prose where the contract is documented
- D-G's twelve examples are each verified by build
- D-J holds: no lockstep sync test was created
</success_criteria>

<output>
Create `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-05-SUMMARY.md` when done
</output>
