---
phase: 45.1-custom-template-parameter-contract-correction
plan: 07
type: execute
wave: 6
depends_on: ["45.1-05", "45.1-06"]
files_modified:
  - docs/source/user_guide/configuration.rst
  - tests/test_docs_contract_claims_gate.py
  - .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-07-CROSS-PAGE-CONTRACT-SWEEP.md
  - .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [DOC-13, CONF-10, CONF-11, CONF-12]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- The gap: VERIFICATION.md truth 2b (phase-goal-level, cross-page) ---
    - "DOC-13 truth 2b: the published documentation and typsphinx's shipped behaviour agree in both directions across the reader-visible doc set, not merely within templates.rst — docs/source/user_guide/configuration.rst's Document Language route-scope prose states the D-I rule (auto-derived lang reaches the bundled default, an explicit typst_template and a <srcdir>/base.typ shadow alike; it is withheld only when typst_package is configured) and no longer publishes the pre-CONF-12 claim that derivation is withheld on the typst_template or base.typ routes"
    - "DOC-13: a reader who follows templates.rst:222-225's own cross-reference into configuration.rst's Document Language section reads the same rule on both pages — the cross-reference target agrees with the referring page and with typsphinx/template_engine.py:353"
    - "DOC-13: configuration.rst states that declaring a lang parameter in a custom template is mandatory on every non-package route, naming the real failure mode (a Typst unexpected-argument abort) rather than presenting it as an opt-in, so the reader is not told the harm tests/test_typst_lang_gate.py::TestPreFixBasisFailureProof::test_undeclared_argument_basis_raises proves is real cannot happen"
    # --- The recurrence hole: SC#2's enumeration was single-page ---
    - "DOC-13: a permanent automated guard (tests/test_docs_contract_claims_gate.py) enumerates every reader-visible docs page that publishes a route-scope claim about lang or template-parameter emission and fails when a page makes a claim the shipped predicate contradicts — so a stale claim on a page nobody thought to read cannot recur silently, which is the exact way this gap escaped plan 06's templates.rst-only SC#2 enumeration"
    - "DOC-13: the guard's claim-page set is closed by assertion, not by an author's memory — a newly claim-bearing docs page fails the guard until it is reviewed into the declared page list, and a declared exclusion that no longer makes a claim also fails, so neither the inclusion list nor the exclusion list can rot"
    - "DOC-13 / edge: adjacency (verification: explicit) — cross-page agreement is proved by set equality in both directions between the withheld-route set the docs publish and the withheld-route set the code computes from TemplateEngine.uses_bundled_default_template(), not by containment in either direction"
    - "DOC-13 / edge: empty (verification: explicit) — the guard cannot pass vacuously: it asserts the discovered claim-page set is non-empty, and its forbidden-claim detector is proved fail-first against the verbatim pre-fix sentence recorded in 45.1-REVIEW.md CR-01, so a detector that matches nothing fails instead of certifying agreement"
    - statement: "DOC-13 / edge: ordering (verification: backstop) — the guard reports its discovered claim pages and per-route verdicts in a deterministic sorted order so repeated runs produce the same output, and the published contract still promises no particular emitted-parameter order; the emitted-order half is already discharged by tests/test_documented_params_contract_gate.py::TestDocumentedParamsContractGateOrdering::test_two_consecutive_typst_builds_are_byte_identical (shipped by plan 06, passing)"
      verification: backstop
    # --- CONF-12 edges (one new cheap assertion; one already discharged) ---
    - "CONF-12 / edge: adjacency (verification: explicit) — the two route inputs touching at once is decided, not left implicit: a configuration setting BOTH typst_package AND typst_template withholds lang, because the shipped predicate is the negation of typst_package alone; asserted directly against TemplateEngine.uses_bundled_default_template() over all four route combinations, a case no existing fixture covers (tests/fixtures/typst_lang_gate/package_no_lang/conf.py deliberately excludes it)"
    - "CONF-12 / edge: empty (verification: explicit) — an absent, empty or malformed Sphinx language value emits no lang argument and the build still succeeds; already discharged by tests/test_typst_lang_gate.py::TestMalformedLanguage and ::TestNullElementsDoesNotAbort (shipped by plan 03, passing) — cited as existing evidence, no new work planned"
    - statement: "CONF-12 / edge: ordering (verification: backstop) — the position of the lang argument within the emitted show-rule call is deterministic across repeated builds of the same configuration and is not a promise the docs make; carried by the same byte-identical-builds evidence as the DOC-13 ordering row"
      verification: backstop
    # --- CONF-11 edges (both already discharged by plans 01-02) ---
    - "CONF-11 / edge: adjacency (verification: explicit) — the boundary between an absent params key and a present-but-empty one is decided and tested: presence of the key is the predicate, so params: {} counts as specified; already discharged by tests/test_params_exclusivity_gate.py's zero-params fixtures (shipped by plans 01-02, passing) — cited as existing evidence, no new work planned"
    - "CONF-11 / edge: empty (verification: explicit) — params: {} passes nothing at all and a zero-named-parameter template compiles; already discharged by tests/test_params_exclusivity_gate.py (shipped by plans 01-02, passing) — cited as existing evidence, no new work planned"
    - statement: "CONF-11 / edge: ordering (verification: backstop) — when a declared params dict and the auto-derived set would both name a key, there is no merge order to specify because the declared set replaces the auto-derived set wholesale; the residual ordering question is only the emitted argument sequence, carried by the same byte-identical-builds evidence"
      verification: backstop
    # --- Bookkeeping (VERIFICATION.md's recommended, non-truth item) ---
    - "CONF-10, CONF-11, CONF-12: .planning/REQUIREMENTS.md's traceability table and checklist agree with the independently verified implementation state before Phase 46 reads them — the three rows read Complete rather than Pending, and DOC-13's row reads Complete rather than Gaps Found once this plan's own gates pass"
  prohibitions:
    - "MUST NOT soften, hedge or omit the breaking-change consequence while correcting the prose: the corrected section must state that an existing custom template omitting lang on a non-package route now fails to compile. A correction that reads as reassurance rather than as a warning re-creates the exact trap the reader fell into, in gentler words."
    - "MUST NOT resolve the documentation/code contradiction by changing shipped behaviour. Reverting or narrowing CONF-12/D-I in typsphinx/ so the stale prose becomes true again would silently reopen a locked one-way decision and discard plan 03's verified work; the prose is what is wrong."
    - "MUST NOT record a requirement as Complete, or edit any executed plan or SUMMARY, to make the phase look closed ahead of its evidence. The traceability flip is only legitimate after this plan's own gates pass, and plans 45.1-01 through 45.1-06 and their SUMMARYs are the executed record — they stay byte-unchanged."
  artifacts:
    - path: "docs/source/user_guide/configuration.rst"
      provides: "The Document Language section's route-scope prose, restated on the D-I rule and reachable from templates.rst's own cross-reference"
      contains: "Scope: every non-package route"
    - path: "tests/test_docs_contract_claims_gate.py"
      provides: "The permanent cross-page contract-claim guard: page-set closure, route-scope claim agreement against the shipped predicate, and a fail-first proof of the detector"
      min_lines: 120
    - path: ".planning/phases/45.1-custom-template-parameter-contract-correction/45.1-07-CROSS-PAGE-CONTRACT-SWEEP.md"
      provides: "The cross-page extension of plan 06's SC#2 enumeration — every reader-visible page enumerated with its per-page verdict, plus the guard that replaces the manual sweep"
    - path: ".planning/REQUIREMENTS.md"
      provides: "The traceability table and checklist flipped to match the verified implementation state"
  key_links:
    - from: "docs/source/user_guide/templates.rst Standard Parameters (lang bullet)"
      to: "docs/source/user_guide/configuration.rst Document Language section"
      via: "the :doc:`configuration` cross-reference at templates.rst:222-225, whose target must state the same rule as the referring page"
      pattern: "Document Language"
    - from: "tests/test_docs_contract_claims_gate.py"
      to: "typsphinx/template_engine.py uses_bundled_default_template()"
      via: "the withheld-route set derived by calling the shipped predicate over four route configurations, compared to the withheld-route set the docs publish"
      pattern: "uses_bundled_default_template"
    - from: "tests/test_docs_contract_claims_gate.py"
      to: "docs/source/user_guide/configuration.rst, docs/source/user_guide/templates.rst, docs/source/examples/advanced.rst"
      via: "a declared claim-page list asserted equal to the pages a scan of docs/source/**/*.rst discovers"
      pattern: "REVIEWED_CLAIM_PAGES"
  flagged_assumptions:
    - "CONF-10 / edge probe row `unclassified` (status: unresolved, never dismissed): the deterministic edge probe could not classify CONF-10 (\"the typst_authors config value is removed\") into any shape category and emitted the placeholder probe \"unclassified — review manually\". Planner judgment: a pure removal has no data-shape edge to resolve — there is no input whose empty/adjacent/ordered case matters once the config value does not exist. The removal itself is already verified (repo-wide grep returns zero references; VERIFICATION.md SC#8 ✓ VERIFIED). No work is planned for this row and it is NOT dismissed — it is carried here as an explicit unresolved assumption for the verifier to accept or challenge."
---

<objective>
Close the one gap VERIFICATION.md found: `docs/source/user_guide/configuration.rst`'s Document
Language section still publishes the pre-CONF-12 route-scope rule, so the published doc set
contradicts both `typsphinx/template_engine.py:353` and the corrected `templates.rst` on the same
behaviour — and does so on the page `templates.rst` itself sends the reader to. Then close the hole
that let it escape: plan 06's SC#2 two-way agreement proof read `templates.rst` and nothing else.

Purpose: the phase goal is worded about "the published documentation", not about one page. A reader
who trusts the stale claim and omits `lang` from a custom template gets the exact
`unexpected argument: lang` abort this phase exists to eliminate —
`tests/test_typst_lang_gate.py::TestPreFixBasisFailureProof::test_undeclared_argument_basis_raises`
is a passing test proving that harm is real, not hypothetical.

Output: corrected route-scope prose (prose only, no behaviour change), a permanent cross-page
claim guard so this class of gap cannot recur silently, the cross-page sweep record, and the
REQUIREMENTS.md bookkeeping flip VERIFICATION.md asked for.

**Decision fences this plan holds.** D-J (owner) declined a lockstep sync test over the contract's
*parameter-name declaration surfaces* — the emission sites, `typsphinx/templates/base.typ`,
`templates.rst`, and `docs/source/_typst/custom_template.typ`, which stays the unguarded fourth
lockstep site the v0.6.4 review flagged. The guard this plan adds is on a different axis: it checks
**route-scope claim correctness** (which routes the docs say receive or are denied a parameter,
against the shipped predicate) and **claim-page enumeration closure**. It adds no parameter-name
comparison across those four declaration surfaces and touches `custom_template.typ` not at all, so
D-J is not reopened. The rejected `..args` argument sink stays the deferred option of record. No code
under `typsphinx/` is touched: CONF-10, CONF-11 and CONF-12 are verified satisfied in code, and the
one-way decisions from plans 01-04 stay closed.
</objective>

## Source Coverage Audit

| Source | Item | Covered by |
|---|---|---|
| GOAL (ROADMAP Phase 45.1) | "A reader who writes a custom template from the published documentation gets a build that works" — read across the whole published doc set | Task 1 (the false route-scope prose) + Task 2 (the recurrence guard) |
| REQ | DOC-13 — published contract and shipped behaviour agree in both directions | Tasks 1, 2; Task 3 flips its traceability row |
| REQ | CONF-11 — declared `params` is the complete set | Already satisfied in code by plans 01-02 (VERIFICATION.md SC#7 ✓). Task 3 only flips its bookkeeping row; no code work planned |
| REQ | CONF-12 — `lang` on every non-package route | Already satisfied in code by plan 03 (VERIFICATION.md SC#9 ✓). Task 1 documents it; Task 2 adds the four-route predicate assertion; Task 3 flips its row |
| REQ | CONF-10 — `typst_authors` removed | Already satisfied by plan 04 (VERIFICATION.md SC#8 ✓, zero repo-wide references). Task 3 only flips its bookkeeping row |
| RESEARCH | — | No RESEARCH.md exists for this phase (research skipped for the milestone by owner decision 2026-08-04, and skipped again in `--gaps` mode). Nothing to cover |
| CONTEXT | D-A (publish the measured default set) | Already delivered by plan 05 in `templates.rst`; Task 1 makes `configuration.rst` agree with it |
| CONTEXT | D-E (rewrite the precedence model onto exclusivity) | Already delivered by plan 05 (`configuration.rst:199-272`, VERIFICATION.md artifacts table). Untouched here |
| CONTEXT | D-I (`lang` on every non-package route) | Task 1 publishes the rule; Task 2 guards it against drift |
| CONTEXT | D-J (no lockstep sync test) | Honoured — see the fence in `<objective>`; the guard is on the claim-correctness axis, not the name-lockstep axis |
| CONTEXT | Deferred: `..args` sink, `typsphinx-doc-translations` workaround, migration guidance | Correctly excluded — deferred items of record (`deferred-items.md`, `45.1-HANDOFF.md`), not gaps |
| VERIFICATION missing #1 | Rewrite the route-scope subsection and its heading | Task 1 |
| VERIFICATION missing #2 | Re-run SC#2's enumeration across every claim-publishing page | Task 2 |
| VERIFICATION recommended | REQUIREMENTS.md traceability lag | Task 3 |

No source item is MISSING. No item is deferred by this plan.

## Artifacts this phase produces

**Added by this plan**

- `tests/test_docs_contract_claims_gate.py` — new permanent guard module. New symbols:
  - module constants `REVIEWED_CLAIM_PAGES`, `EXCLUDED_CLAIM_PAGES`, `ROUTE_CONFIGS`,
    `CODE_WITHHELD_ROUTES`, `DERIVATION_TOKENS`, `WITHHOLDING_TOKENS`, `PRE_FIX_FALSE_CLAIM`
  - module helpers `_iter_rst_pages()`, `_page_makes_contract_claim()`, `_claim_sentences()`,
    `_withheld_routes_claimed()`, `_routes_affirmed()`, `_withheld_routes_from_code()`
  - test classes `TestContractClaimPageEnumerationIsClosed`,
    `TestLangRouteScopeClaimsMatchShippedPredicate`, `TestForbiddenClaimDetectorIsFailFirst`
- `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-07-CROSS-PAGE-CONTRACT-SWEEP.md`
  — the cross-page extension of plan 06's SC#2 record
- New RST sub-subsection heading `Scope: every non-package route` in
  `docs/source/user_guide/configuration.rst`

**Modified by this plan**

- `docs/source/user_guide/configuration.rst` — Document Language section prose only
- `.planning/REQUIREMENTS.md` — four checklist markers and four traceability rows
- `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md`
  — **append-only**: one trailing cross-reference line pointing at the sweep record

**Deliberately NOT touched**

- Any file under `typsphinx/` — CONF-10/11/12 are verified satisfied in code
- `45.1-01-PLAN.md` … `45.1-06-PLAN.md` and every `45.1-*-SUMMARY.md` — the executed record stays
  byte-unchanged
- `docs/source/_typst/custom_template.typ` — D-J's unguarded fourth lockstep site, left standing
- `CHANGELOG.md` / `docs/source/changelog.rst` — `45.1-HANDOFF.md` Item 2 owns that debt for Phase 46

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-VERIFICATION.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-REVIEW.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md
@CLAUDE.md
</context>

<!-- planner-discipline-allow: Scope: default template only -->
<!-- planner-discipline-allow: applies **only** when the bundled default template -->
<!-- planner-discipline-allow: When your project uses the bundled default template -->
<!-- planner-discipline-allow: If you use a custom template or package and want the same behavior -->

<tasks>

<task type="tracer">
  <name>Task 1: Correct configuration.rst's Document Language route-scope prose end-to-end</name>
  <precondition>`typsphinx/template_engine.py`'s `uses_bundled_default_template()` still returns the negation of `self.typst_package` (no resolution-provenance half), so the rule this task publishes is the rule the shipped code applies; if the predicate has changed, halt rather than documenting a rule the code no longer follows.</precondition>
  <reversibility rating="reversible">A prose-only edit to one published page; reverting is a single-commit revert with no behaviour, API or config consequence.</reversibility>
  <files>docs/source/user_guide/configuration.rst</files>
  <read_first>
    - `docs/source/user_guide/configuration.rst` lines 274-350 in full — the file being edited: the `Paper Size and Format` cross-reference into `Document Language`, the `Automatic derivation` sub-subsection, the route-scope sub-subsection that is wrong, and the `Precedence and known limitation` sub-subsection that is correct and must survive untouched
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-REVIEW.md` CR-01 — the code review's already-drafted replacement prose, including the proposed heading `Scope: every non-package route`; start from it rather than re-inventing the wording
    - `typsphinx/template_engine.py` lines 353-395 — `uses_bundled_default_template()` and its docstring: the shipped truth this prose must match, including why the `typst_package` guard survives
    - `docs/source/user_guide/templates.rst` lines 203-250 — the page that is already correct and must stay consistent, especially the `lang` bullet at 222-225 (which cross-references this section by name) and the package-route paragraph at 247-250
    - `docs/source/conf.py` lines 96-104 — this repository's own dogfooded explicit-`typst_template` build and the comment recording that the workaround was removed
    - `tests/test_typst_lang_gate.py` `TestPreFixBasisFailureProof::test_undeclared_argument_basis_raises` — the passing test proving the failure mode this prose must warn about
  </read_first>
  <action>
    Rewrite the route-scope prose inside `configuration.rst`'s `Document Language` section so it
    states the D-I rule, and nothing weaker. Three specific edits, all inside lines 296-328; the
    `Precedence and known limitation` sub-subsection at 330-348 is factually correct under D-I and
    must be left as it is.

    First, the `Automatic derivation` sub-subsection's opening sentence currently begins
    "When your project uses the bundled default template" and so frames derivation as
    bundled-default-only before the reader reaches the scope subsection at all. Reframe it to state
    that `lang` is derived automatically from Sphinx's own `language` setting on every non-package
    template route. Keep the derivation algorithm paragraph (before the first underscore, hyphen or
    at-sign, lowercased), its three `ja` / `zh_CN` / `pt_BR` examples, and the malformed-value
    warning paragraph exactly as they are — those are still accurate.

    Second, replace the sub-subsection titled "Scope: default template only" — the title is itself
    false now — with `Scope: every non-package route`, per CR-01. Its body must state: automatic
    derivation applies on every route except `typst_package`; the bundled default template, an
    explicit `typst_template`, and a `<srcdir>/base.typ` shadow of the bundled template all receive
    the auto-derived `lang` argument unconditionally; it is withheld only when `typst_package` is
    configured, because typsphinx never introspects a third-party Typst Universe function's
    signature and would otherwise hand it an argument it never declared. Delete the sentence
    beginning "Automatic derivation applies **only** when the bundled default template", the
    enumeration of `typst_template` / `typst_package` / a neighbouring `base.typ` file as routes
    where derivation does not happen, and the "a template that does not declare a `lang` parameter
    would receive an argument it never asked for" rationale — that rationale is exactly the one D-I
    retired for non-package routes, and it now survives only as the `typst_package` justification.

    Third, replace the closing paragraph that begins "If you use a custom template or package and
    want the same behavior". Declaring `lang` in a custom template is no longer an opt-in: on every
    non-package route it is **mandatory**, and an existing custom template that omits it fails to
    compile with a Typst `unexpected argument: lang` abort the next time it is built. Say that
    plainly, name that failure string so a reader who has already hit it can find this page by
    searching for it, and point at `templates.rst`'s nine-parameter contract with a `:doc:` role for
    the full parameter list. Re-scope the `typst_elements` opt-in advice to the one route where it
    is still needed — `typst_package` — since on that route typsphinx withholds `lang` and the
    reader's own wrapping template must declare it and receive the value through `typst_elements`.

    Two structural constraints. Do NOT rename the `Document Language` section itself: its title is
    an implicit RST hyperlink target used by this same file's `Paper Size and Format` paragraph and
    is named in `templates.rst`'s cross-reference, so renaming it breaks both. And when you change a
    sub-subsection title, resize its `~` underline to at least the new title's length, or Sphinx
    emits a title-underline-too-short warning and `tox -e docs-pdf` stops being clean.

    Prose only. Do not change any file under `typsphinx/`, do not add or remove a config value, and
    do not edit `templates.rst` — it is already correct and this task's job is to make the
    cross-reference target agree with it.

    Worktree note: this task's verify runs a real docs build, so provision the worktree first with
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and run every command through
    `uv run`, per `CLAUDE.md`'s worktree-isolated execution rules.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && ! grep -Fq 'Scope: default template only' docs/source/user_guide/configuration.rst && ! grep -Fq 'applies **only** when the bundled default template' docs/source/user_guide/configuration.rst && ! grep -Fq 'When your project uses the bundled default template' docs/source/user_guide/configuration.rst && ! grep -Fq 'If you use a custom template or package and want the same behavior' docs/source/user_guide/configuration.rst && grep -Fq 'Scope: every non-package route' docs/source/user_guide/configuration.rst && grep -Fq 'unexpected argument: lang' docs/source/user_guide/configuration.rst && grep -Fq '`Document Language`_' docs/source/user_guide/configuration.rst && uv run tox -e docs-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'Scope: default template only' docs/source/user_guide/configuration.rst` returns 0
    - `grep -Fc 'applies **only** when the bundled default template' docs/source/user_guide/configuration.rst` returns 0
    - `grep -Fc 'When your project uses the bundled default template' docs/source/user_guide/configuration.rst` returns 0
    - `grep -Fc 'If you use a custom template or package and want the same behavior' docs/source/user_guide/configuration.rst` returns 0
    - `grep -Fc 'Scope: every non-package route' docs/source/user_guide/configuration.rst` returns at least 1, and the following line is a `~` underline at least as long as that title
    - The new subsection body names all three non-package routes as receiving the derived value — the bundled default, an explicit ``typst_template``, and a ``<srcdir>/base.typ`` shadow — and names ``typst_package`` as the only route where it is withheld, with the signature-introspection reason
    - `grep -Fc 'unexpected argument: lang' docs/source/user_guide/configuration.rst` returns at least 1, in a sentence stating that an existing custom template omitting the parameter fails to compile
    - The section heading `Document Language` is unchanged and `grep -Fc '`Document Language`_' docs/source/user_guide/configuration.rst` still returns at least 1, so the in-page implicit hyperlink and `templates.rst`'s cross-reference both still resolve
    - The `Precedence and known limitation` sub-subsection is byte-unchanged: `git diff docs/source/user_guide/configuration.rst` shows no hunk touching the explicit-`typst_elements`-wins paragraph, its `language = "ja"` / `typst_elements = {"lang": "de"}` example, or the region-subtag limitation
    - `git diff --name-only` lists exactly `docs/source/user_guide/configuration.rst` for this task — no file under `typsphinx/`, no test file, no other docs page
    - `uv run tox -e docs-pdf` exits 0 and produces a real PDF, with no new Sphinx warning relative to the three pre-existing `visit_toctree` docstring-indentation warnings plan 06 recorded
  </acceptance_criteria>
  <done>A reader following `templates.rst`'s cross-reference into `configuration.rst`'s Document Language section reads the same rule the shipped predicate applies, is told that declaring `lang` is mandatory on non-package routes and what happens if they skip it, and the repository's own dogfooded PDF build of that page is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the permanent cross-page contract-claim guard and record the sweep</name>
  <precondition>`45.1-REVIEW.md` CR-01 still quotes the pre-fix route-scope sentence verbatim, so the guard's fail-first proof can use the real pre-fix text as its known violation instead of a reconstruction; if that quotation is absent, take the text from `git show` of the pre-Task-1 revision of `configuration.rst` rather than inventing a paraphrase.</precondition>
  <reversibility rating="reversible">A new, additive test module plus a new planning record; deleting both restores the prior state exactly.</reversibility>
  <files>tests/test_docs_contract_claims_gate.py, .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-07-CROSS-PAGE-CONTRACT-SWEEP.md</files>
  <read_first>
    - `tests/test_documented_params_contract_gate.py` in full — the sibling gate this module joins: its module-docstring convention for stating which assertion is RED-proved and which is a control, its `frozenset` published-name constant, and its set-equality-not-containment discipline
    - `tests/test_quickstart_docs_gate.py` lines 1-60 — the project's established pattern for a never-skipping test that binds published prose to measured behaviour, including how it resolves `REPO_ROOT` and reads a docs page
    - `typsphinx/template_engine.py` lines 202-232 (the `TemplateEngine.__init__` signature: `template_path`, `template_name`, `search_paths`, `parameter_mapping`, `typst_package`, `typst_template_function`, `typst_package_imports`) and lines 353-395 (`uses_bundled_default_template()`) — the guard derives its route truth by constructing this class and calling that predicate, so it cannot drift from the code
    - `docs/source/user_guide/configuration.rst` as corrected by Task 1, `docs/source/user_guide/templates.rst` lines 203-250, and `docs/source/examples/advanced.rst` lines 160-240 — the three pages that publish route-scope or parameter-exclusivity claims today
    - `docs/source/changelog.rst` line 27 — a historical release note that mentions the `typst_elements` key set; the reason the guard needs a reviewed exclusion list rather than a bare scan
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md` Task 2 — plan 06's single-page SC#2 record this sweep extends, including its two directional statements
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-VERIFICATION.md` — the `missing:` list this task's second item closes, and the explanation of why the single-page enumeration missed it
    - `tests/fixtures/typst_lang_gate/package_no_lang/conf.py` — its comment records that the both-`typst_package`-and-`typst_template` combination is deliberately NOT exercised by that fixture, which is why the four-route predicate assertion here is new coverage
  </read_first>
  <behavior>
    - Given the docs tree as corrected by Task 1, the guard passes.
    - Given the verbatim pre-fix route-scope sentence, the forbidden-claim detector flags it — proved by feeding that string to the detector directly, so a detector that silently matches nothing fails instead of certifying agreement.
    - Given a docs page that starts publishing a route-scope claim and is not in the reviewed page list, the page-closure assertion fails.
    - Given a page in the reviewed exclusion list that no longer publishes any claim, the closure assertion also fails, so exclusions cannot rot.
    - Given the four route configurations, the withheld-route set computed from `uses_bundled_default_template()` is exactly `{typst_package-alone, typst_package + typst_template}`, and the docs' published withheld-route set equals it — set equality in both directions.
    - Given zero discovered claim pages, the guard fails rather than passing vacuously.
  </behavior>
  <action>
    Write `tests/test_docs_contract_claims_gate.py` as a permanent, never-skipping guard (no
    `typst-py` dependency, no `sphinx-build` subprocess — it reads published prose and calls one
    Python predicate, so it is fast enough to run on every suite invocation). Follow the sibling gate
    module's docstring convention: state in the module docstring what class of gap this guard exists
    to prevent, name `45.1-VERIFICATION.md`'s truth 2b as the failure it is the regression lock for,
    and state plainly that its subject is published prose rather than emitted output.

    Derive the truth from code, never from prose. Build a `ROUTE_CONFIGS` mapping of the four route
    combinations onto real `TemplateEngine` constructions — bundled default (no `template_path`, no
    `typst_package`), explicit `typst_template` (`template_path` set), `<srcdir>/base.typ` shadow
    (`template_name`/`search_paths` set), and `typst_package` set both alone and together with
    `template_path` — call `uses_bundled_default_template()` on each, and reduce the results to
    `CODE_WITHHELD_ROUTES`, the set of route names where the derived parameter is withheld. Assert
    that set equals the two `typst_package` combinations: this is the CONF-12 adjacency edge, the
    both-inputs-set case no existing fixture covers, and it must be an assertion rather than a
    comment. The predicate reads only `self.typst_package`, so no template file needs to exist on
    disk for these constructions.

    Then close the enumeration hole. `_iter_rst_pages()` walks every `*.rst` under `docs/source/`.
    `_page_makes_contract_claim()` returns true for a page that mentions at least one of the nine
    published parameter names as an RST inline literal AND at least one route token
    (`typst_template`, `typst_package`, `base.typ`, or the bundled-default wording).
    `TestContractClaimPageEnumerationIsClosed` asserts three things: the discovered set is non-empty
    (a vacuous scan is a failure, not a pass); the discovered set minus `EXCLUDED_CLAIM_PAGES`
    equals the declared `REVIEWED_CLAIM_PAGES`; and every entry in `EXCLUDED_CLAIM_PAGES` is still
    among the discovered pages, so a stale exclusion fails too. Give each exclusion an inline reason
    — `docs/source/changelog.rst` is a historical release record, not a live contract claim — and
    make the failure message tell a future author exactly what to do: review the new page's claims
    against the code, then add it to one list or the other. Report discovered pages in sorted order
    so the message is stable across runs.

    `TestLangRouteScopeClaimsMatchShippedPredicate` is the two-way, cross-page agreement check that
    plan 06 performed for `templates.rst` alone. For each reviewed page, split the prose into
    sentences, keep the sentences that mention derivation (`derive`/`derived`/`derivation`/
    `auto-derived`) together with a route token, and classify each as affirming or withholding using
    `WITHHOLDING_TOKENS` (negated-applicability wording, exclusive-scope wording, `withheld`,
    `never`). Direction one: the union of routes any page claims the parameter is withheld on must
    equal `CODE_WITHHELD_ROUTES` — nothing the docs deny is something the code actually does.
    Direction two: the union of routes the pages affirm must cover every route where the predicate
    returns true — nothing the code does goes unpublished. Assert both as set equality on a single
    `==` between two sets, not as containment in either direction, and put the offending page and
    sentence in the assertion message.

    `TestForbiddenClaimDetectorIsFailFirst` is what keeps this guard honest. Store the verbatim
    pre-fix sentence CR-01 quotes as `PRE_FIX_FALSE_CLAIM`, feed it to the same classifier the page
    walk uses, and assert it is detected as a withholding claim naming a route the code does not
    withhold — i.e. the guard would have gone red against `configuration.rst` as it stood before
    Task 1. This is the module's RED basis, taken against real pre-fix text rather than a
    post-fix observation. Also assert the affirmative detector fires on a known-good sentence, so
    neither half of the classifier can rot into a no-op. Say in the docstring that the RED basis is
    a synthetic known-violation string sourced from the recorded pre-fix text, not a build against
    the pre-fix tree — the same honesty the sibling module applies to its own axes.

    Keep the guard on the claim-correctness axis. Do not add any assertion comparing parameter-name
    lists across `typsphinx/templates/base.typ`, `templates.rst` and
    `docs/source/_typst/custom_template.typ` — that is the lockstep sync test D-J declined, and this
    module must not become it by accretion. Note that boundary in the module docstring so a later
    reader does not extend it across the fence.

    Then write `45.1-07-CROSS-PAGE-CONTRACT-SWEEP.md` as the evidence record: name every `*.rst`
    page under `docs/source/` the scan discovered, give each a verdict (publishes claims and agrees /
    publishes claims and disagrees / excluded with its reason / no claims), state the two directional
    conclusions with the concrete route sets shown so a reader can check rather than trust, and state
    explicitly that the manual sweep is now backed by the permanent guard whose test IDs you name.
    Record why plan 06's SC#2 record did not catch this — its enumeration was scoped to
    `templates.rst` and never read `configuration.rst` — and paste the live command lines with their
    exit codes, dated to this session, rather than inheriting a result. Finally, append exactly one
    trailing cross-reference line to `45.1-CONTRACT-ENUMERATION.md` pointing at the sweep record;
    append only, and change no existing line in that file.

    Run everything through `uv run` after `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync
    --extra dev`, per `CLAUDE.md`. Record in the sweep document that in the main tree a human runs
    pytest as `.venv/bin/python -m pytest` instead, because an outer `uv run` there produces spurious
    integration-test failures. Respect line length 88 and the project's ruff configuration; in this
    executor's sandbox `ruff` may need `nix-shell -p ruff --run "ruff check ."`, the workaround plans
    01-06 recorded.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest tests/test_docs_contract_claims_gate.py -q && uv run pytest -q && uv run black --check . && uv run mypy typsphinx/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_docs_contract_claims_gate.py -q` exits 0 and collects at least six tests across the three named classes, with zero skips (the module has no `skipif` on `typst-py`)
    - `uv run pytest -q` exits 0 over the whole suite, and its passed count is at least the 980 plan 06's closeout recorded plus this module's new tests
    - `uv run black --check .` and `uv run mypy typsphinx/` exit 0; `ruff check .` reports no findings
    - The module computes its withheld-route set by calling `TemplateEngine.uses_bundled_default_template()`, verified by `grep -c 'uses_bundled_default_template' tests/test_docs_contract_claims_gate.py` returning at least 1 — the route truth is read from code, not transcribed
    - The module asserts the withheld-route set for a configuration setting BOTH `typst_package` and an explicit template, a combination absent from `tests/fixtures/typst_lang_gate/`
    - The two directional assertions each compare two sets with `==`; `grep -cE 'issubset|issuperset' tests/test_docs_contract_claims_gate.py` returns 0, so neither direction is satisfied by containment alone
    - Temporarily appending a sentence to a docs page asserting that derivation does not apply on the explicit-`typst_template` route makes `uv run pytest tests/test_docs_contract_claims_gate.py -q` fail; reverting it makes it pass again — recorded in the sweep document with both exit codes
    - `TestForbiddenClaimDetectorIsFailFirst` passes the verbatim pre-fix sentence through the same classifier the page walk uses and asserts it is flagged, so the detector is proved to have teeth rather than assumed to
    - The page-closure test fails when a discovered claim page is in neither the reviewed nor the excluded list, and also fails when an excluded page no longer makes a claim — both directions asserted, not just the first
    - Every entry in the exclusion list carries an inline reason naming why it is not a live contract claim
    - `grep -c 'custom_template.typ' tests/test_docs_contract_claims_gate.py` returns 0 outside the docstring sentence that records the D-J fence, and the module contains no parameter-name list comparison across `base.typ` / `templates.rst` / `custom_template.typ`
    - `45.1-07-CROSS-PAGE-CONTRACT-SWEEP.md` lists every `*.rst` page under `docs/source/` with a per-page verdict, states both directional conclusions with the concrete route sets shown, names the guard's test IDs, and explains why plan 06's `templates.rst`-scoped enumeration missed this page
    - `git diff .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md` shows only added lines at the end of the file and zero removed or modified lines
    - `git status --porcelain .planning/phases/45.1-custom-template-parameter-contract-correction/` shows no modification to any `45.1-0[1-6]-PLAN.md` or `45.1-*-SUMMARY.md`
  </acceptance_criteria>
  <done>The class of gap that produced this closure round — a contract claim published on a page the agreement proof never read — now fails a permanent, fail-first-proved test, and the cross-page sweep that proves the current doc set clean is recorded with live command output.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the REQUIREMENTS.md traceability lag to match the verified state</name>
  <precondition>`.planning/REQUIREMENTS.md` still carries CONF-10, CONF-11 and CONF-12 as `Pending` and DOC-13 as `Gaps Found` in the traceability table with their checklist markers unticked; if another process has already flipped them, report that as a finding in the SUMMARY rather than re-editing.</precondition>
  <reversibility rating="reversible">Planning-record bookkeeping; a single revert restores the prior rows.</reversibility>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 100-180 — the four checklist entries to tick: CONF-11 at 106, CONF-12 at 122, CONF-10 at 137, DOC-13 at 169
    - `.planning/REQUIREMENTS.md` lines 288-320 — the traceability table rows for DOC-13 / CONF-10 / CONF-11 / CONF-12 and the coverage tally that must stay at 18/18 with zero orphans
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-VERIFICATION.md` — the "Note on REQUIREMENTS.md bookkeeping" paragraph and the Requirements Coverage table: the independent evidence that authorises each flip
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md` Item 3 — the record that this lag was expected at hand-off and whose step was to close it
  </read_first>
  <action>
    Flip only what the evidence supports, and only after Tasks 1 and 2 are green — the prohibition
    against recording a requirement Complete ahead of its evidence is the reason this task is last
    rather than first.

    In the requirement checklist, change the leading marker from unticked to ticked for exactly four
    entries: CONF-11, CONF-12, CONF-10 and DOC-13. Change no word of any requirement's text — the
    marker only.

    In the traceability table, set the Status cell to `Complete` for the four `Phase 45.1` rows:
    DOC-13 (currently `Gaps Found`), CONF-10, CONF-11 and CONF-12 (currently `Pending`). Leave the
    REL-06 and REL-04 rows alone — Phase 46 owns those, and REL-04's row carries its own
    "closes at `/gsd-complete-milestone`" qualifier which must survive verbatim. Leave the coverage
    tally at 18 total / 18 mapped / 0 unmapped: this task changes statuses, not coverage.

    Use scoped edits, one row and one marker at a time. Do not rewrite the file wholesale — it
    carries the full milestone requirement history and a whole-file write would destroy entries
    outside the four lines this task touches.

    For each of the four, state in the plan SUMMARY which evidence closed it: CONF-11 by
    `tests/test_params_exclusivity_gate.py` across all three routes, CONF-12 by
    `tests/test_typst_lang_gate.py` plus the narrowed predicate, CONF-10 by the zero-hit repo-wide
    grep and the migrated shipped sample, and DOC-13 by Task 1's corrected page plus Task 2's
    permanent guard and sweep record. If any one of those is not green at the time you edit, do not
    flip that row — report it.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\| (DOC-13|CONF-10|CONF-11|CONF-12) \| Phase 45\.1 \| Complete \|$' .planning/REQUIREMENTS.md)" = "4" && test "$(grep -cE '^- \[x\] \*\*(DOC-13|CONF-10|CONF-11|CONF-12)\*\*' .planning/REQUIREMENTS.md)" = "4" && ! grep -qE '^\| (DOC-13|CONF-10|CONF-11|CONF-12) \| Phase 45\.1 \| (Pending|Gaps Found) \|' .planning/REQUIREMENTS.md && grep -qE '^\| REL-04 \| Phase 46 \| Pending' .planning/REQUIREMENTS.md && grep -q 'v1 requirements: 18 total' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| (DOC-13|CONF-10|CONF-11|CONF-12) \| Phase 45\.1 \| Complete \|$' .planning/REQUIREMENTS.md` returns exactly 4
    - `grep -cE '^- \[x\] \*\*(DOC-13|CONF-10|CONF-11|CONF-12)\*\*' .planning/REQUIREMENTS.md` returns exactly 4
    - No `Phase 45.1` row remains `Pending` or `Gaps Found`
    - The REL-06 and REL-04 rows are byte-unchanged, including REL-04's "closes at `/gsd-complete-milestone`" qualifier
    - The coverage block still reads 18 total / 18 mapped / 0 unmapped
    - `git diff --stat .planning/REQUIREMENTS.md` shows at most 8 changed lines (four markers plus four table rows) and zero deletions of requirement text
    - `git diff --name-only` for this task lists only `.planning/REQUIREMENTS.md`
    - The SUMMARY names the closing evidence for each of the four requirements individually
  </acceptance_criteria>
  <done>Phase 46 reads a traceability table that agrees with the verified implementation state, with each flip attributable to named evidence.</done>
</task>

</tasks>

<planner_assumptions>
**Flagged, unresolved (never dismissed) — surfaced for the verifier to accept or challenge.**

1. **CONF-10's edge-probe row is `unclassified`.** The deterministic probe emitted
   "unclassified — review manually" for CONF-10 and produced no shape category. Planner judgment: a
   requirement whose content is the *removal* of a config value has no data-shape edge to resolve —
   once `typst_authors` does not exist, there is no input whose empty, adjacent or ordered case
   matters. The removal is already verified independently (repo-wide grep returns zero references;
   VERIFICATION.md SC#8 ✓ VERIFIED; the shipped sample migrated). No work is planned for this row.
   It is carried as unresolved rather than dismissed, because dismissing it would be the silent drop
   the probe protocol exists to prevent.

No other probe-surfaced item is unresolved: the remaining nine are authored into `must_haves.truths`
(six with `verification: explicit`, three as `verification: backstop` markers). Equality holds —
10 probe-surfaced items == 9 authored + 1 flagged assumption.
</planner_assumptions>

<assumption_delta_decision>
**Noun now primary:** unchanged — the *published custom-template parameter contract* (and the
reader's own custom template as its consumer) remains the phase's primary noun, exactly as it was at
plans 01-06.

**Decision:** `no-change`.

**Rationale:** the three detector signals are incidental to this closure round — `pluralization` fired
on the word "also" in a sentence about the `toctree_*` parameters, and `chosen` fired twice on the
word "custom" in the phase title and on "chosen" in "the fix route was chosen at the phase
discussion". None of them marks a new entity, a pluralized identity, or a swapped primary noun. This
plan corrects prose so the docs describe already-shipped behaviour and adds a test guard; it
introduces no new domain object, renames nothing, and does not change what the contract is *about*.
Manufacturing an identity-model change here would be fabrication.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published documentation → a reader's own `conf.py` and custom template | A false claim crossing this boundary is executed as configuration advice and produces a hard Typst compile abort in a build typsphinx never sees |
| one docs page → another docs page's cross-reference | A page corrected in isolation can still send a reader into a stale claim on its cross-reference target — the exact shape of this gap |
| shipped predicate (`typsphinx/template_engine.py`) → prose asserting what it does | Prose can drift from the predicate silently, because nothing mechanically compares them |
| planning record (`REQUIREMENTS.md` traceability) → Phase 46's reading of it | A status flipped ahead of its evidence certifies work nobody measured |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45.1-20 | Tampering (documentation integrity) | `docs/source/user_guide/configuration.rst` Document Language route-scope prose | high | mitigate | Task 1 replaces the false claim, its heading and the stale opt-in advice, with four negative-grep gates on the retired literals and positive gates on the D-I rule and the named failure string; proved by a real `tox -e docs-pdf` exit 0 rather than by reading |
| T-45.1-21 | Repudiation | a single-page agreement proof standing in for a doc-set-wide claim | high | mitigate | Task 2 replaces the one-shot manual enumeration with a permanent guard whose claim-page set is closed by assertion in both directions (a new claim page fails; a rotted exclusion fails), so "the docs agree" can no longer be asserted for one page and read as all pages |
| T-45.1-22 | Repudiation | a prose guard that matches nothing and passes vacuously | high | mitigate | `TestForbiddenClaimDetectorIsFailFirst` feeds the verbatim recorded pre-fix sentence to the same classifier the page walk uses and requires it to be flagged; the closure test additionally requires a non-empty discovered page set, and the acceptance criteria require an observed red from a temporarily reintroduced false claim |
| T-45.1-23 | Repudiation | `REQUIREMENTS.md` status flipped ahead of the evidence | medium | mitigate | Task 3 is ordered last, its `<precondition>` asserts the pre-flip state, its action forbids flipping any row whose gate is not green, and its acceptance criteria require per-requirement evidence attribution in the SUMMARY |
| T-45.1-24 | Tampering | the executed record (plans/SUMMARYs 01-06, `45.1-CONTRACT-ENUMERATION.md`) rewritten to look clean | medium | mitigate | The sweep record is a NEW file; only a trailing cross-reference line is appended to the enumeration record, gated by a `git diff` acceptance criterion allowing added lines only; a further criterion requires `git status` to show no modification to any `45.1-0[1-6]-PLAN.md` or `45.1-*-SUMMARY.md` |
| T-45.1-SC | Tampering | third-party package fetch during this plan's gates | low | accept | This plan adds **no** package to `pyproject.toml` and installs nothing new: `uv sync --extra dev` resolves the already-declared, already-pinned dev dependencies, so no package-legitimacy audit is applicable and no legitimacy checkpoint is inserted. `tox -e docs-pdf` fetches the four already-pinned Typst Universe `@preview` packages, whose pins `tests/test_preview_version_sync.py` asserts across the three in-repo declaration sites; unchanged by this plan |

**ASVS level 1, blocking threshold high.** The three `high` rows are all mitigated with mechanical
gates in this plan's own acceptance criteria; none is accepted or transferred. This closure round is
documentation prose plus one read-only test module — there is no untrusted input, no network surface,
no authentication, authorization, session, secret or persistence surface in scope, so no ASVS L1
control beyond documentation-integrity and evidence-integrity applies. Nothing is padded to fill the
register.
</threat_model>

<verification>
- `uv run tox -e docs-pdf` exits 0 with no new Sphinx warning beyond the three pre-existing
  `visit_toctree` docstring-indentation warnings
- `uv run pytest tests/test_docs_contract_claims_gate.py -q` exits 0 with zero skips
- `uv run pytest -q` exits 0 over the whole suite
- `uv run black --check .`, `ruff check .` and `uv run mypy typsphinx/` all exit 0
- The four retired literals return 0 hits in `docs/source/user_guide/configuration.rst`; the D-I rule
  and the `unexpected argument: lang` failure string are present
- Temporarily reintroducing a withholding claim about the explicit-`typst_template` route into any
  reviewed docs page turns the guard red; reverting turns it green — both exit codes recorded in
  `45.1-07-CROSS-PAGE-CONTRACT-SWEEP.md`
- `git status --porcelain` shows no modification to any file under `typsphinx/`, to any
  `45.1-0[1-6]-PLAN.md`, or to any `45.1-*-SUMMARY.md`
- `.planning/REQUIREMENTS.md` shows four `Complete` rows for Phase 45.1 and four ticked checklist
  markers, with the coverage tally still 18/18 and zero orphans
</verification>

<success_criteria>
- VERIFICATION.md truth 2b holds: the reader-visible doc set and the shipped predicate agree in both
  directions, and the page `templates.rst` cross-references states the same rule as `templates.rst`
- Both of VERIFICATION.md's `missing:` items are closed — the prose is corrected, and the agreement
  proof now spans every claim-publishing page and is enforced by a permanent, fail-first-proved test
- DOC-13 moves from partially satisfied to satisfied without any change to `typsphinx/`, without
  reopening a one-way decision from plans 01-04, and without crossing D-J's declined-lockstep fence
- The bookkeeping lag VERIFICATION.md flagged is closed before Phase 46 reads the traceability table
</success_criteria>

<output>
Create `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-07-SUMMARY.md` when done
</output>
