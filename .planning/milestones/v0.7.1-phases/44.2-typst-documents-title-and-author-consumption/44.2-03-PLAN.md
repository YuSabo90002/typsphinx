---
phase: 44.2-typst-documents-title-and-author-consumption
plan: 03
type: execute
wave: 3
depends_on: ["44.2-01", "44.2-02"]
files_modified:
  - docs/source/user_guide/configuration.rst
  - .planning/REQUIREMENTS.md
  - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md
autonomous: true
requirements: [CONF-09]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC#4 (D-09): the corpus partition is proved by a machine-checkable AST walk re-derived and run in this plan's own session — not by prose, and not by transcribing a figure from any planning document."
    - "SC#4 baseline (D-08, corrected): the walk run against the phase's pre-change tree reproduces 108 `conf.py` files, 111 entries, 107 four-tuples, 3 five-tuples, 6 divergent-`entry[2]` entries (one of them inert), 0 divergent-`entry[3]` entries, 72 inline non-`conf.py` occurrences, and 2 files combining `typst_documents` with `typst_authors`. ROADMAP's 104/5/99 and CONTEXT.md D-08's 83/5/0 are BOTH superseded and are named as superseded in the evidence."
    - "SC#4 delta: the walk run against the post-change tree differs from the baseline by exactly this phase's own additions, each enumerated by file and attributed to the plan that added it — including the divergent `entry[3]` plan 02 introduced in `tests/fixtures/template_named_dir_master/conf.py`, which moves the divergent-author count off zero."
    - "SC#4 partition (D-08's three conditions): an entry produces unchanged output when `entry[2] == project` AND `entry[3] == author` AND `typst_authors` is unset. Where measurement refines that condition it is stated rather than smoothed over — the third condition is conservative, because a file setting `typst_authors` alongside a custom parameter mapping that omits `\"author\"` is also unchanged."
    - "SC#4 measured difference (D-07): each entry that fails the three conditions is BUILT with `-b typst` against the pre-change production files and again against the post-change ones, and its emitted title and author lines are recorded verbatim for both. No in-repo `conf.py` is edited to make its title match `project`; the changed output IS the evidence."
    - "SC#4 inert site: `tests/fixtures/non_str_docname_gate/conf.py`'s second entry is reported by the walk as divergent and is recorded as CONFIRMED INERT with the reason — its docname is an integer, so no real document write can ever match it — rather than being silently omitted or silently counted as a live change."
    - "SC#5 (D-10): `docs/source/user_guide/configuration.rst` states that the fifth element is accepted and ignored, so no element of the published five-element contract is left silently inert after this phase."
    - "SC#5 corrected evidence: the documentation does NOT repeat D-10's false claim that no in-repo `conf.py` carries a fifth element. Three do — `docs/source/conf.py`, `examples/charged-ieee/approach1/conf.py` and `examples/charged-ieee/approach2/conf.py` — and the walk in this plan re-confirms it."
    - "D-11: `configuration.rst` publishes the resolution order decided here — entry value first, then `config.project`/`config.author`; an empty string is a value, not a fallback signal; `entry[3]` beats `typst_authors`; `typst_template_function`'s `params` beat everything — and both `typst_authors` sections carry the forward notice that the setting is fully replaceable by `typst_template_function`'s `params` and is slated for removal in a future major release."
    - "D-06: `.planning/REQUIREMENTS.md` § Future Requirements gains a new entry `CONF-10` for the `typst_authors` removal, carrying the byte-identical-replacement measurement, the surface it must touch, and the note that `examples/charged-ieee/approach1/conf.py`'s `typst_authors` block moves into its own `typst_template_function` dict when that phase lands. No deprecation warning is emitted in code by this phase."
    - "SC#6: at phase close the full pytest suite, `black --check .`, `ruff check .`, `mypy typsphinx/`, `tests/test_preview_version_sync.py` and `tests/test_corpus_gate.py` are all green, and a docs HTML build reports no warning naming `configuration.rst`."
    - statement: "The precedence text published in `configuration.rst` matches the precedence the code actually implements, rule for rule, with each published rule traceable to a passing test from plan 02's matrix. Prose accuracy is a human judgement that no assertion can settle; the automated half only confirms the required phrases are present."
      verification: backstop
  artifacts:
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md
    - .planning/REQUIREMENTS.md
    - docs/source/user_guide/configuration.rst
  key_links:
    - "`phase_base_sha` recorded in `44.2-GATE-EVIDENCE-01.md` § 0 -> this plan's baseline corpus walk and its pre-change builds"
    - "plan 02's `Corpus delta introduced by this plan` note -> this plan's delta reconciliation"
    - "the published precedence rules in `configuration.rst` -> the passing matrix tests in `tests/test_entry_metadata_precedence.py` that enforce each of them"
  prohibitions:
    - statement: "MUST NOT edit `docs/source/user_guide/templates.rst`. Its `title` claim becomes true when this phase lands, but VERIFYING that claim is Phase 45.1 SC#2's job — `git diff --stat docs/source/user_guide/templates.rst` must be empty across every commit of this plan. This is the phase's sharpest boundary because this plan is the one editing the neighbouring documentation file."
      status: unverified
      verification: test
    - statement: "MUST NOT absorb the adjacent `high`-severity duplicate-**target** defect (`.planning/todos/pending/2026-08-04-duplicate-typst-documents-target-silently-drops-a-master.md`). It shares the `typst_documents` surface and will surface during the corpus walk; it is a different defect, its todo file is not edited, and no requirement, documentation sentence or evidence claim about it is written here."
      status: unverified
      verification: judgment
    - statement: "MUST NOT remove `typst_authors`, register a deprecation, or emit any runtime warning for it. D-06 defers the removal precisely because v0.7.1 is a patch release already carrying two user-visible changes; adding a third breaking change here would contradict the owner decision that created this phase's boundary. The output is a Future Requirement entry and documentation prose only."
      status: unverified
      verification: test
    - statement: "MUST NOT edit any `conf.py`, any file under `typsphinx/`, or any test file. D-07 requires divergent in-repo entries to be LEFT AS-IS so their changed output becomes the evidence; editing one to make its title match `project` would destroy exactly the measurement SC#4 needs."
      status: unverified
      verification: test
    - statement: "MUST NOT publish ROADMAP's 104/5/99 figures or CONTEXT.md D-08's 83/5/0 figures as if current, and MUST NOT repeat D-10's claim that no in-repo `conf.py` carries a fifth element. All three are measured wrong; each is named as superseded rather than silently replaced."
      status: unverified
      verification: test
    - statement: "MUST NOT record a figure, a count or a verdict copied from `44.2-CONTEXT.md`, `44.2-RESEARCH.md`, `44.2-PATTERNS.md` or `ROADMAP.md` instead of produced by a command run in this plan's own session. RESEARCH.md's corrected numbers are a target to REPRODUCE, not a value to transcribe; if the reproduction disagrees, the measurement wins and the disagreement is reported."
      status: unverified
      verification: test
    - statement: "MUST NOT name any plan-authored artifact `44.2-VERIFICATION.md` — that name is written by `/gsd-verify-work` and silently clobbers plan-authored files. Use `44.2-GATE-EVIDENCE-03.md`."
      status: unverified
      verification: test
    - statement: "MUST NOT edit the CONF-09 traceability row's status in `.planning/REQUIREMENTS.md`. Phase completion owns that flip; a plan setting it early has produced an incorrect record on this project before."
      status: unverified
      verification: test
---

<objective>
Close the phase's two remaining obligations: prove what the change actually did to this repository's
own corpus, and make the published contract true in the document that publishes it.

Plans 01 and 02 made the behaviour real and asserted. This plan measures its blast radius against the
repo's 111 in-tree entries (D-08, D-09), records the deliberate handling of every entry that changes
(D-07), writes the accepted-and-ignored statement that stops the fifth element from being silently
inert (D-10, SC#5), publishes the precedence order and the `typst_authors` forward notice
(D-11), and files the `typst_authors` removal as a Future Requirement rather than performing it
(D-06).

Purpose: CONF-09's own text is "no element of the published five-element contract is left silently
inert." Elements 3 and 4 stop being inert by code; element 5 stops being silently inert only by being
documented as ignored. And SC#4's claim that the rest of the corpus is unchanged is worth nothing as
prose — it has to be a walk anyone can re-run, whose baseline reproduces a stated number and whose
delta is attributed file by file.

Output: `44.2-GATE-EVIDENCE-03.md` carrying the re-derived AST walk, its baseline and post-change
runs, and the before/after builds of every changed site; the amended `configuration.rst`; and the new
`CONF-10` Future Requirement.

This plan runs in its own wave rather than beside plan 02 for a measured reason: plan 02 adds a
divergent `entry[3]` to `tests/fixtures/template_named_dir_master/conf.py`, which changes the very
census this plan reports. Their `files_modified` do not overlap, but their content does — the exact
shape that lets two correct plans collide at merge.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-CONTEXT.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-RESEARCH.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-PATTERNS.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-01-SUMMARY.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-02-SUMMARY.md
@CLAUDE.md
</context>

<worktree_provisioning>
Identical to plans 01 and 02 and equally mandatory. `test -f .git`, then
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then everything through
`uv run python -m …` (never `uv run <compiled-binary>` on this NixOS sandbox). Prove the binding with
`uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"`
and record the path in `44.2-GATE-EVIDENCE-03.md` § 0. If it resolves into the main checkout, every
measurement in this plan is void.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: The corpus partition, re-derived and measured on both sides of the change (SC#4)</name>

  <precondition>
    `44.2-GATE-EVIDENCE-01.md` exists and its § 0 carries a 40-hex `phase_base_sha` recorded by plan
    01 BEFORE that plan's first commit, and `44.2-GATE-EVIDENCE-02.md` § 2 carries plan 02's
    `Corpus delta introduced by this plan` note. Assert both before starting; a baseline walk cannot
    be run without the SHA and the delta cannot be reconciled without the note. If either is missing,
    halt rather than reconstructing the SHA by guesswork — `git log` ordering is not a substitute for
    a recorded pre-change anchor.
  </precondition>

  <files>.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md</files>

  <read_first>
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-01.md
      § 0 — the `phase_base_sha`.
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-02.md
      § 2 — plan 02's corpus-delta note.
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-RESEARCH.md
      § "Common Pitfalls" Pitfall 1 (the corrected numbers table and the walk skeleton), Pitfall 2
      (the inert `non_str_docname_gate` entry and why it must be reported rather than omitted or
      counted) and Pitfall 5 (the three `conf.py` files that DO carry a fifth element).
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-CONTEXT.md D-08 and
      D-09 — the three-condition definition of "unchanged" and the decision to prove the partition by
      static walk with real builds only for the failing set.
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-01.md — the
      house evidence style (verbatim command, verbatim output, explicit reconciliation of a measured
      figure against a planning document's predicted figure).
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-PATTERNS.md
      § "SC#4 AST-walk evidence (script)" — the instruction to inline the script in the evidence file
      rather than adding a standalone script file, and the numbers it must reproduce.
    - typsphinx/writer.py and typsphinx/template_engine.py — the post-change state, so the before/after
      builds are set up against the right two files.
  </read_first>

  <action>
**Step A — write the walk fresh.**

Write a Python AST-walk script that, given a set of `conf.py` sources, reports for each:
the parsed `typst_documents` entries with their arity; whether `entry[2]` equals the file's `project`
value and `entry[3]` equals its `author` value (comparing by AST `Name` reference OR by literal-value
equality, since some files write `project` as a name and others as a literal); whether `typst_authors`
is set; and whether `typst_template_mapping` is set and whether it maps `"author"`. Exclude any path
under `.venv`. Separately count inline (non-`conf.py`) `typst_documents = [` occurrences across the
tree.

Derive it fresh from the skeleton in the research document — do not hunt for a stale copy. Inline the
finished script verbatim in `44.2-GATE-EVIDENCE-03.md` § 1 as a fenced block, with the exact command
that runs it. The evidence file IS the reproducible artifact; do not add a standalone script file to
the repo, which has never carried a `scripts/` directory.

**Step B — the baseline run.**

Run the walk against the PRE-CHANGE tree, using the `phase_base_sha` from
`44.2-GATE-EVIDENCE-01.md` § 0. Enumerate that tree's `conf.py` set with
`git ls-tree -r <phase_base_sha> --name-only` and read each file's content with
`git show <phase_base_sha>:<path>` — no `git archive`, no checkout, no working-tree mutation.

That run must reproduce: **108** `conf.py` files setting `typst_documents`, **111** parsed entries,
**107** four-tuples, **3** five-tuples, **6** entries with a divergent `entry[2]` (one of them inert),
**0** entries with a divergent `entry[3]`, **72** inline non-`conf.py` occurrences, and **2** files
combining `typst_documents` with `typst_authors`. If your run produces a different number, STOP and
reconcile before writing anything down — a discrepancy is itself diagnostic (an uncommitted fixture,
a walk that followed `.venv`, a different definition of "sets `typst_documents`"). Report the
reconciliation; do not adjust the walk until it agrees.

Record the baseline table in § 2, and immediately beneath it a `Superseded figures` note stating that
ROADMAP § Phase 44.2's "104 entries / 5 divergent / other 99 identical" and CONTEXT.md D-08's
"83 entries / 5 divergent / 0 five-tuples" are both superseded by this measurement, and which of them
each figure replaces. Name them; do not silently substitute.

**Step C — the post-change run and the delta.**

Run the same walk against the working tree at HEAD. Record the table in § 3, then a line-by-line
delta against § 2 in which every difference is attributed to the plan that caused it: plan 01's two
new fixture `conf.py` files (which add entries, add divergent titles, and add divergent authors), and
plan 02's divergent fourth element on the second entry of
`tests/fixtures/template_named_dir_master/conf.py` (which moves the divergent-author count off zero,
exactly as plan 02's handoff note says). Inline-occurrence growth from this phase's own new test
modules is attributed the same way. Any difference you cannot attribute is a finding — report it
rather than absorbing it into a count.

**Step D — the partition, and the entries that fail it.**

State D-08's three-condition definition of "unchanged output" — `entry[2] == project` AND
`entry[3] == author` AND `typst_authors` unset — and partition the baseline corpus by it in § 4.
Where measurement refines the condition, say so plainly: the third condition is conservative, because
a file that sets `typst_authors` alongside a custom parameter mapping omitting `"author"` is also
unchanged (plan 01's control-group evidence measured exactly that). Report both the strict-condition
partition and the measured one, and say which is being used as SC#4's claim.

For the satisfying set, the argument is D-09's and is not built: identical values entering
`map_parameters()` produce identical output, and the walk itself establishes the value equality. Say
that explicitly rather than implying a build happened.

For the failing set — every pre-existing entry that fails the conditions — take a real measurement.
Restore the two production files to their pre-change state with
`git checkout <phase_base_sha> -- typsphinx/writer.py typsphinx/template_engine.py`, build each
failing site with `sys.executable -m sphinx -b typst` into a temporary directory, capture its emitted
title and author lines, then restore the current files with
`git checkout HEAD -- typsphinx/writer.py typsphinx/template_engine.py` and rebuild each site the
same way. Record both sides verbatim in § 5 as a per-site before/after table. Verify with
`git status --porcelain typsphinx/` that the tree is clean again before committing anything; leaving
a reverted production file behind would silently un-do the phase.

If a site cannot be built in this sandbox (a missing example dependency, an environmental failure of
the kind CLAUDE.md documents), record the exact failure and fall back to measuring that site's
resolved values by calling the resolution helper and `map_parameters()` directly with that file's
own configuration — and say which sites needed the fallback and why. Do not drop a site.

**Step E — the inert site.**

`tests/fixtures/non_str_docname_gate/conf.py`'s second entry will be reported divergent by the walk.
Record it in § 6 as CONFIRMED INERT with the reason measured, not asserted: its `entry[0]` is an
integer, and the writer's master-document check compares that element against a real docname string,
so no document write can ever match it. Show the comparison that establishes this. Do not omit it
(an auditor re-running the walk would see an unexplained extra) and do not count it as a live change
(that overstates the blast radius). This is why the divergent-title count is 6 while the live count
is 5.
  </action>

  <verify>
    <automated>test -f .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md &amp;&amp; grep -c 'phase_base_sha\|108\|111' .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md</automated>
    <automated>test -z "$(git status --porcelain typsphinx/)" &amp;&amp; echo "OK: production tree clean after before/after builds"</automated>
    <automated>test -z "$(git diff --name-only -- typsphinx/ tests/ '*conf.py')" &amp;&amp; echo "OK: no source, test or conf.py edited by this task"</automated>
    <automated>uv run python -m pytest -q 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `44.2-GATE-EVIDENCE-03.md` § 1 contains the complete AST-walk script as a fenced block plus the
      exact command that runs it, and no standalone `.py` script file was added to the repo:
      `git status --porcelain --untracked-files=all -- '*.py' | grep -v '^?? tests/' | wc -l` returns
      `0`.
    - § 2 reports the baseline run against `phase_base_sha` with all eight figures, and reproduces
      108 / 111 / 107 / 3 / 6 / 0 / 72 / 2 — or, if it does not, § 2 contains a reconciliation naming
      each differing figure and the cause.
    - § 2 contains a `Superseded figures` note that names BOTH ROADMAP's 104/5/99 and CONTEXT.md
      D-08's 83/5/0 explicitly as superseded.
    - § 3 reports the post-change run and a delta in which every differing figure is attributed to a
      named plan and a named file, including `tests/fixtures/template_named_dir_master/conf.py` for
      the divergent-author count.
    - § 4 states D-08's three conditions verbatim and reports both the strict-condition partition and
      the measured refinement, and says which one SC#4's claim rests on.
    - § 5 contains a per-site before/after table for every entry failing the conditions, with the
      emitted title and author lines verbatim on both sides and the build command shown, or an
      explicit fallback note for any site that could not be built.
    - § 6 records `tests/fixtures/non_str_docname_gate/conf.py`'s second entry as confirmed inert with
      the measurement that establishes it.
    - `git status --porcelain typsphinx/` is empty and `git diff --name-only -- typsphinx/ tests/` is
      empty — the before/after builds left the tree exactly as they found it.
    - `uv run python -m pytest -q` is green, or every failure is individually attributed with a command
      output and none is attributable to this task.
  </acceptance_criteria>

  <reversibility rating="reversible">
    D-08 and D-09 are rated reversible in CONTEXT.md — a definition in this phase's evidence, and a
    method whose widening to real builds later costs only time. This task writes evidence, not code.
  </reversibility>

  <done>
    A re-derived AST walk, inlined in the evidence file, reproduces the corrected pre-change corpus
    figures against the recorded `phase_base_sha`, reports the post-change delta with every difference
    attributed to a plan and a file, partitions the corpus by D-08's three conditions with its
    measured refinement stated, and carries a verbatim before/after build for every site that changes
    — with the one inert divergent site named as inert and the tree left clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: The published contract made true — element 5 accepted-and-ignored, and the precedence order (SC#5, D-10, D-11)</name>

  <files>
    docs/source/user_guide/configuration.rst,
    .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md
  </files>

  <read_first>
    - docs/source/user_guide/configuration.rst — the WHOLE file. In particular the five-element
      contract block with its numbered list of the five tuple positions, the "Author Information" /
      "Detailed Format" section, and the later worked example that combines `typst_documents`,
      `typst_package`, `typst_template_function` and `typst_authors`. PATTERNS.md cites these near
      lines 24-46, 144-166 and 260-289 — locate them by reading, not by line number.
    - docs/source/user_guide/templates.rst — **READ ONLY.** Its `title` claim becomes true when this
      phase lands, and Phase 45.1 SC#2 owns verifying it. Reading it is required so the two documents'
      wording does not contradict; editing it is forbidden.
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-CONTEXT.md D-10 and
      D-11 — what the fifth element gets said about it, and which rules the precedence text must
      publish.
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-RESEARCH.md
      § "Common Pitfalls" Pitfall 5 — the corrected fifth-element evidence and its recommendation to
      NAME the three `conf.py` files rather than state a count.
    - tests/test_entry_metadata_precedence.py (written by plan 02) — every rule published here must
      correspond to a test in that module; read it so the prose and the assertions agree rule for
      rule.
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md
      § 3 — this task's own walk output, to cite the fifth-element files from a measurement rather
      than from RESEARCH.md's prose.
  </read_first>

  <action>
Amend `docs/source/user_guide/configuration.rst` in three places. Match the file's existing reST
conventions — the same heading underline characters, the same `.. code-block:: python` idiom, the
same double-backtick literal style — and keep every existing sentence that is still true.

**1. The five-element contract's numbered list.**

Items 3 and 4 (the title and author positions) gain the resolution order, stated as a rule the reader
can act on:

- the entry's value is used when present;
- when the element is absent or `None`, `project` / `author` are used instead;
- an empty string is a VALUE, not a request to fall back — it renders as an empty title or empty
  author;
- a non-string element produces a build warning and then falls back;
- for the author position specifically: the entry value takes precedence over `typst_authors`, and
  `typst_template_function`'s `params` take precedence over everything.

Item 5 (the document class) gains the accepted-and-ignored statement. It must literally contain the
phrase `accepted and ignored`. State plainly that typsphinx reads nothing from this position today,
that a five-element tuple is valid and behaves identically to a four-element one, and — corrected per
this plan's own walk — that real five-element tuples exist in this repository, naming
`docs/source/conf.py` and both `examples/charged-ieee` configs. Do NOT write that no in-repo `conf.py`
carries a fifth element; that claim is measured false. Cite the fifth-element files from § 3 of this
plan's evidence file, not from a planning document.

**2. The `typst_authors` "Detailed Format" section.**

Append two things after the existing example: the new precedence (an entry's author element takes
precedence over this setting, as does `typst_template_function`'s `params`), and the forward notice —
that `typst_authors` is fully replaceable by `typst_template_function`'s `params` for authors, that
the two routes were measured to produce a byte-identical authors value differing only in the order of
named arguments in the emitted call (which is semantically irrelevant in Typst), and that the setting
is slated for removal in a future major release. The notice must literally contain the phrase
`slated for removal`. Use whatever admonition directive the file already uses elsewhere; do not
introduce a new directive type.

No deprecation warning is emitted in code — D-06 defers the removal precisely so this patch release
does not carry a third breaking change. The documentation says what is coming; the code says nothing.

**3. The worked example that uses `typst_authors`.**

Add a short forward-pointer near that block's author-details comment, referring the reader to the
notice added in step 2. Do not duplicate the full notice, and do not restructure the example.

**Then verify the docs still build.** Run a Sphinx HTML build of `docs/source` into a temporary
directory with `uv run python -m sphinx -b html`, capture the output, and confirm no warning names
`configuration.rst`. Record the command, the tail of its output and the grep result in
`44.2-GATE-EVIDENCE-03.md` § 7.

Finally, write a rule-to-test traceability table into § 8: one row per rule published in step 1, each
naming the test node id from `tests/test_entry_metadata_precedence.py` (or
`tests/test_document_metadata_render_gate.py` for the empty-string rule) that enforces it. A
published rule with no enforcing test is a finding — report it rather than dropping the rule or
inventing a test here.
  </action>

  <verify>
    <automated>grep -c 'accepted and ignored' docs/source/user_guide/configuration.rst</automated>
    <automated>grep -c 'slated for removal' docs/source/user_guide/configuration.rst</automated>
    <automated>grep -c 'typst_template_function' docs/source/user_guide/configuration.rst</automated>
    <automated>test -z "$(git diff --stat -- docs/source/user_guide/templates.rst)" &amp;&amp; echo "OK: templates.rst untouched"</automated>
    <automated>uv run python -m sphinx -b html docs/source /tmp/44.2-docs-html 2>&1 | tail -20</automated>
    <automated>uv run python -m sphinx -b html docs/source /tmp/44.2-docs-html2 2>&1 | grep -c 'configuration.rst' || true</automated>
    <human-check>
Read `docs/source/user_guide/configuration.rst`'s five-element contract section and both
`typst_authors` sections, then read `tests/test_entry_metadata_precedence.py`. Confirm each published
rule matches an asserted test rule-for-rule, and that the prose reads as guidance a user can act on
rather than as an internal changelog. Prose accuracy is a human judgement; the automated checks above
only confirm the required phrases are present.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `grep -c 'accepted and ignored' docs/source/user_guide/configuration.rst` returns at least `1`.
    - `grep -c 'slated for removal' docs/source/user_guide/configuration.rst` returns at least `1`.
    - `grep -c 'docs/source/conf.py' docs/source/user_guide/configuration.rst` returns at least `1` and
      `grep -c 'charged-ieee' docs/source/user_guide/configuration.rst` returns at least `1` — the
      corrected fifth-element evidence names the real files.
    - The five-element contract's numbered items 3 and 4 mention `typst_authors` and
      `typst_template_function` and state that an empty string is a value.
    - `git diff --stat -- docs/source/user_guide/templates.rst` is empty.
    - `git diff --name-only` for this task lists only `docs/source/user_guide/configuration.rst` and
      the evidence file.
    - `uv run python -m sphinx -b html docs/source <tmpdir>` exits 0 and its captured output contains
      no warning line naming `configuration.rst`.
    - `44.2-GATE-EVIDENCE-03.md` § 7 records the docs build command and its output tail, and § 8
      contains a rule-to-test table with a real test node id in every row, or an explicit finding for
      any rule that has none.
  </acceptance_criteria>

  <reversibility rating="reversible">
    D-10 and D-11 are rated reversible in CONTEXT.md — documentation text, and giving the fifth
    element meaning later does not conflict with having documented it as ignored today.
  </reversibility>

  <done>
    `configuration.rst` publishes the resolution order for the title and author positions, states that
    the fifth element is accepted and ignored while naming the real five-element configs in this
    repository, and carries the `typst_authors` forward-removal notice with its byte-identical
    replacement measurement — with the docs still building clean, `templates.rst` untouched, and every
    published rule traced to a passing test.
  </done>
</task>

<task type="auto">
  <name>Task 3: File the typst_authors removal as CONF-10 (D-06), and close the phase gate (SC#6)</name>

  <files>
    .planning/REQUIREMENTS.md,
    .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md
  </files>

  <read_first>
    - .planning/REQUIREMENTS.md — the whole file. Specifically § "Future Requirements" and its
      "Carried from earlier milestones" list (the entry style to match: a bolded ID followed by a
      one-line description, occasionally with a rationale clause), the CONF-09 entry itself, and the
      § Traceability table.
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-CONTEXT.md D-06 — the
      full measured basis for the removal being lossless, the emitted-diff evidence, and the list of
      surfaces the removal phase must touch.
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-CONTEXT.md
      `<deferred>` — the affected surface list to carry into the entry, and the related deferred
      defect (`typst_authors` plus the bundled template producing no PDF) that belongs with the same
      removal.
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-01-SUMMARY.md — plan 01's
      measured statement of how far D-05 already narrowed `typst_authors`, which the entry should
      carry so the future phase starts from a measurement rather than a memory.
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VALIDATION.md
      § "Sampling Rate" — the exact phase-gate command set.
  </read_first>

  <action>
<!-- planner-discipline-allow: CONF-10 -->

**Step A — file CONF-10.**

Add a new entry to `.planning/REQUIREMENTS.md` § "Future Requirements", using the ID **CONF-10**
(verified free: the highest existing CONF identifier in the planning tree is CONF-09). Place it under
the existing "Carried from earlier milestones" list or in a clearly-labelled sibling grouping —
whichever keeps the section's existing structure intact. Match the file's existing entry style.

The entry must carry, in compact form:

- what it removes: the `typst_authors` config value.
- why it is lossless: `typst_authors` is sugar over `typst_template_function["params"]["authors"]`,
  and rendering the same author dictionary through both routes was measured to produce a
  byte-identical authors value — the only difference being the order of named arguments in the
  emitted call, which is semantically irrelevant in Typst.
- why it is not done here: it is a breaking change and v0.7.1 is a patch release already carrying two
  user-visible changes (CONF-08's output-filename rename and CONF-09's title/author change), both of
  which REL-06 requires the CHANGELOG to call out.
- how far Phase 44.2 already narrowed it: after D-05's reorder, `typst_authors` is authoritative only
  on a package-alone build whose custom parameter mapping omits `"author"`. Take this from plan 01's
  measured summary, not from memory.
- the surface the removal must touch: the config registration in `typsphinx/__init__.py`, the
  override block in `typsphinx/template_engine.py`, both `typst_authors` sections in
  `docs/source/user_guide/configuration.rst`, the `typst_authors` mention in
  `docs/source/examples/advanced.rst`, and the `typst_authors` tests in
  `tests/test_template_engine.py` and `tests/test_package_only_config_gate.py`.
- the migration it owes: `examples/charged-ieee/approach1/conf.py`'s `typst_authors` block moves into
  that file's already-present `typst_template_function` dictionary.
- the deferred defect that travels with it: `typst_authors` combined with the bundled default template
  produces no PDF at all, because the template's document-author assignment receives an array of
  dictionaries. D-05 incidentally unbreaks the case where the entry also supplies an author, but a
  project setting only `typst_authors` stays broken; the fix belongs with this removal.

Do NOT touch the CONF-09 traceability row's status — phase completion owns that flip, and a plan
setting it early has produced an incorrect record on this project before. Do not add CONF-10 to the
Traceability table either: that table tracks this milestone's requirements, and CONF-10 is
deliberately outside it.

**Step B — the phase gate (SC#6).**

Run the full gate and record each command with its verbatim output in `44.2-GATE-EVIDENCE-03.md`
§ 9: `uv run python -m pytest -q`, `uv run black --check .`, `uv run ruff check .`,
`uv run mypy typsphinx/`, `uv run python -m pytest tests/test_preview_version_sync.py -q` and
`uv run python -m pytest tests/test_corpus_gate.py -q`. Any failure is either repaired here or
attributed individually, with proof, as a pre-existing environmental failure of this sandbox — and
"environmental" without proof is not an available attribution.

**Step C — the phase fence proof.**

Record in § 10, each with the command that produced it:

- `docs/source/user_guide/templates.rst` is byte-unchanged across the whole phase, measured from
  `phase_base_sha` to `HEAD` — not merely across this plan.
- nothing under `.planning/todos/pending/` was edited, so the duplicate-target defect was neither
  absorbed nor closed.
- no `conf.py` outside this phase's own new fixtures was edited except
  `tests/fixtures/template_named_dir_master/conf.py`'s divergent author (D-07 holds: divergent
  in-repo entries were left as-is and their changed output is the evidence).
- `typst_authors` still exists as a registered config value with no deprecation warning attached
  (D-06 holds: the removal was filed, not performed).
- the `@preview` package version literals in `typsphinx/writer.py`, `typsphinx/template_engine.py`
  and `typsphinx/templates/base.typ` are unchanged from `phase_base_sha`.

**Step D — the phase-level roll-up.** Write § 11 as a one-table roll-up of SC#1 through SC#6 for the
whole phase: the criterion, the plan and task that discharged it, and the evidence section that
proves it. Every row must point at a section of a `44.2-GATE-EVIDENCE-NN.md` file or a passing test
node id — no row may point at a planning document.
  </action>

  <verify>
    <automated>grep -c 'CONF-10' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c 'typst_authors' .planning/REQUIREMENTS.md</automated>
    <automated>uv run python -m pytest -q 2>&1 | tail -20</automated>
    <automated>uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy typsphinx/</automated>
    <automated>uv run python -m pytest tests/test_preview_version_sync.py tests/test_corpus_gate.py -q</automated>
    <automated>uv run python -c "import typsphinx, inspect, re; src=inspect.getsource(typsphinx.setup); print('typst_authors registered:', 'typst_authors' in src)"</automated>
    <automated>test -z "$(git diff --name-only -- .planning/todos/pending/)" &amp;&amp; echo "OK: duplicate-target todo untouched"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'CONF-10' .planning/REQUIREMENTS.md` returns at least `1`, and the new entry sits under
      § "Future Requirements".
    - The CONF-10 entry text mentions `typst_template_function`, `examples/charged-ieee/approach1`,
      `typsphinx/__init__.py` and `typsphinx/template_engine.py`.
    - `grep -n 'CONF-09' .planning/REQUIREMENTS.md` shows the Traceability row still reading
      `Pending`, and `git diff -- .planning/REQUIREMENTS.md` contains no change to that row.
    - `CONF-10` does NOT appear in the § Traceability table:
      `sed -n '/^## Traceability/,$p' .planning/REQUIREMENTS.md | grep -c 'CONF-10'` returns `0`.
    - `uv run python -m pytest -q` is green, or every failure is listed in § 9 with an individual
      evidence-backed attribution and none is attributable to this phase.
    - `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` and
      `uv run python -m pytest tests/test_preview_version_sync.py tests/test_corpus_gate.py -q` all
      exit 0, with verbatim output in § 9.
    - `git diff --stat <phase_base_sha>..HEAD -- docs/source/user_guide/templates.rst` is empty.
    - `git diff --name-only <phase_base_sha>..HEAD -- .planning/todos/pending/` is empty.
    - `git diff --name-only <phase_base_sha>..HEAD -- '*conf.py'` lists only this phase's own new
      fixture configs and `tests/fixtures/template_named_dir_master/conf.py`.
    - `git diff <phase_base_sha>..HEAD -- typsphinx/templates/base.typ` is empty, and no `@preview`
      version literal changed in `typsphinx/writer.py` or `typsphinx/template_engine.py`.
    - `44.2-GATE-EVIDENCE-03.md` § 11 contains a six-row SC#1–SC#6 roll-up in which every row cites an
      evidence-file section or a test node id, and no row cites a planning document.
  </acceptance_criteria>

  <reversibility rating="reversible">
    D-06 is rated reversible in CONTEXT.md — it is a Future Requirements entry, not a code change.
    Reverting is deleting one list item.
  </reversibility>

  <done>
    `typst_authors`' removal is filed as `CONF-10` under Future Requirements, carrying its
    lossless-replacement measurement, its full affected surface, its example migration and the broken
    bundled-template case that travels with it — while the setting itself stays registered and
    undeprecated in code. The phase gate is green with raw output on record, all four scope fences are
    proven from `phase_base_sha` to `HEAD`, and SC#1 through SC#6 each point at a measurement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository `conf.py` files -> the AST-walk script | Repository-owned Python is PARSED, never executed. `ast.parse` reads source text and builds a tree; no `conf.py` in the corpus is imported or `exec`ed by the walk. |
| `git show <sha>:<path>` -> the walk's input | Historical file content is read from the object database rather than by mutating the working tree. |
| working tree -> temporary production-file revert during the before/after builds | The only step in this phase that puts the tree into a knowingly-inconsistent state. |
| published documentation -> reader's `conf.py` | A rule stated in `configuration.rst` becomes a rule users write configuration against. |
| planning claim -> recorded evidence | A figure asserted in a planning document versus a figure produced by a command in this session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44.2-12 | Elevation of privilege | the corpus AST walk over 100+ `conf.py` files | medium | mitigate | A walk that `import`ed or `exec`ed each `conf.py` would run 100+ arbitrary Python files, several of them example configurations, as a side effect of counting tuples. The walk uses `ast.parse` on source text only and never executes a corpus file; § 1's inlined script is the auditable proof. |
| T-44.2-13 | Tampering | the temporary production-file revert in task 1 step D | medium | mitigate | Reverting `writer.py` and `template_engine.py` to `phase_base_sha` for the "before" builds leaves the phase's own change absent if the restore is skipped. The restore is an explicit instructed step, `git status --porcelain typsphinx/` being empty is both a task verify and an acceptance criterion, and the full suite is re-run afterwards. |
| T-44.2-14 | Repudiation | figures recorded in `44.2-GATE-EVIDENCE-03.md` | high | mitigate | Three separate figures in this phase's own source artifacts were measured wrong (ROADMAP's 104/5/99, D-08's 83/5/0, D-10's zero-fifth-elements claim), so transcription is a demonstrated failure mode here rather than a hypothetical one. Every figure carries its command and raw output; the baseline must REPRODUCE a stated number before anything is written; superseded figures are named rather than silently replaced; a plan prohibition forbids transcription. |
| T-44.2-15 | Information disclosure | published precedence rules in `configuration.rst` | low | accept | The document describes configuration semantics only. It names three in-repo file paths as fifth-element examples, all of which are already public in this repository. No credential, host, internal endpoint or private path is published. |
| T-44.2-16 | Denial of service | before/after builds across the failing-entry set plus a docs HTML build | low | accept | A bounded number of `-b typst` builds (no compile) over already-present fixtures, plus one HTML docs build. Comparable to the existing corpus gate's cost and run once. |
| T-44.2-17 | Spoofing | none identified | low | accept | This plan has no authentication, session, authorization or cryptographic surface. ASVS V2/V3/V4/V6 do not apply. V5 applies only to the already-trusted `conf.py` input, handled as parse-don't-execute (T-44.2-12). |
| T-44.2-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; `pyproject.toml` is not in `files_modified` for any plan in this phase, and RESEARCH.md's Package Legitimacy Audit records the phase as installing no external packages. No `[ASSUMED]`/`[SUS]` legitimacy checkpoint applies. |

**ASVS level 1, block on high.** The one `high`-severity threat (T-44.2-14) is dispositioned
`mitigate`. No threat is accepted at `high` or above.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and paths created by THIS plan (exclude from drift verification):

- `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md`
  (sections § 0 through § 11, including the inlined AST-walk script)
- `.planning/REQUIREMENTS.md` — a new Future Requirement identifier **`CONF-10`** (the
  `typst_authors` removal). Verified free at planning time: the highest existing CONF identifier in
  the planning tree is CONF-09.
- `docs/source/user_guide/configuration.rst` — new prose only (the resolution-order rules on the
  title and author positions, the `accepted and ignored` statement on the fifth position, the
  `slated for removal` forward notice in both `typst_authors` locations). No new directive type and
  no new document.

Created elsewhere in the phase (listed so this plan's readers do not flag them as drift): plan 01's
`_resolve_entry_element` and module-level `logger` in `typsphinx/writer.py`,
`tests/test_document_metadata_render_gate.py`, `tests/fixtures/entry_title_author_render_gate/`,
`tests/fixtures/entry_empty_metadata_render_gate/`, `44.2-GATE-EVIDENCE-01.md`; plan 02's
`tests/test_entry_metadata_precedence.py`, `tests/test_multi_master_metadata_no_leak.py`,
`tests/test_entry_metadata_route_uniformity.py`, the divergent `entry[3]` in
`tests/fixtures/template_named_dir_master/conf.py`, and `44.2-GATE-EVIDENCE-02.md`.
</artifacts_this_phase_produces>

<flagged_assumptions>
1. **The edge probe abstained.** The deterministic edge probe over CONF-09 returned one row,
   `unclassified` / `unresolved`, `applicable: 1, resolved: 0`, with zero explicit and zero backstop
   verifications. It produced no coverage and is NOT treated as having produced any. The edge
   predicates in this phase's plans were lifted by hand from ROADMAP § Phase 44.2 SC#1–SC#6 and
   CONTEXT.md D-01/D-02's element matrix. Recorded here, and in plans 01 and 02, as an explicit
   flagged assumption.
2. **One truth in this plan is a `backstop`.** Whether `configuration.rst`'s published precedence
   prose genuinely matches the implemented precedence is a human judgement — the automated half only
   confirms required phrases are present. At verify time that truth abstains to `human_needed`
   rather than passing silently; the `<human-check>` in task 2 is the intended discharge.
3. **The baseline reproduction may not reproduce.** The plan requires the pre-change walk to produce
   108/111/107/3/6/0/72/2 before anything is written down, but that target came from a research
   session, not from this executor's own run. If it does not reproduce, the instruction is to
   reconcile and report — never to adjust the walk until it agrees. A non-reproducing baseline is a
   real finding about one of the two measurements.
4. **D-08's third condition is measurably conservative.** `typst_authors` being set does not by
   itself mean an entry's output changes: a file that also sets a custom parameter mapping omitting
   `"author"` is unchanged (plan 01's control group measured exactly that). The plan requires both
   the strict partition and the measured refinement to be reported, and requires the evidence to say
   which one SC#4's claim rests on, rather than quietly using the more favourable number.
5. **`CONF-10` is a planner-chosen identifier.** It was verified free against the whole planning tree
   at planning time. If a concurrent branch has since claimed it, the executor should pick the next
   free CONF identifier and say so in the summary rather than reusing one.
</flagged_assumptions>

<verification>
- `44.2-GATE-EVIDENCE-03.md` contains § 0 through § 11: the isolation proof, the inlined walk script,
  the baseline run with its superseded-figures note, the post-change run with an attributed delta,
  the partition with its measured refinement, the per-site before/after build table, the inert-site
  record, the docs-build result, the rule-to-test table, the phase gate output, the fence proofs and
  the SC#1–SC#6 roll-up.
- `grep -c 'accepted and ignored' docs/source/user_guide/configuration.rst` and
  `grep -c 'slated for removal' docs/source/user_guide/configuration.rst` each return at least `1`.
- `grep -c 'CONF-10' .planning/REQUIREMENTS.md` returns at least `1`, and the CONF-09 traceability
  row is unmodified.
- `uv run python -m pytest -q`, `uv run black --check .`, `uv run ruff check .`,
  `uv run mypy typsphinx/` and
  `uv run python -m pytest tests/test_preview_version_sync.py tests/test_corpus_gate.py -q` are all
  green.
- `git diff --stat <phase_base_sha>..HEAD -- docs/source/user_guide/templates.rst` is empty.
- `git diff --name-only <phase_base_sha>..HEAD -- .planning/todos/pending/` is empty.
- `git status --porcelain typsphinx/` is empty — the before/after builds left no reverted file behind.
</verification>

<success_criteria>
- SC#4 met: the corpus partition is a re-runnable walk whose baseline reproduces a stated figure set,
  whose delta is attributed file by file, and whose failing set carries verbatim before/after builds.
- SC#5 met: the fifth element is documented as accepted and ignored, with corrected evidence naming
  the real five-element configs — so no element of the published contract is silently inert.
- D-10 and D-11 met: the precedence order and the `typst_authors` forward-removal notice are
  published in the document that publishes the contract.
- D-06 met: the removal is filed as `CONF-10` with its measured basis, and is NOT performed.
- D-07 met: no in-repo `conf.py` was edited to hide a change; the changed output is the evidence.
- D-08 and D-09 met: the corrected three-condition definition is used and stated, and the partition is
  proved by static walk with real builds only for the entries that fail it.
- SC#6 met for the phase: suite, format, lint, type, `@preview` sync and corpus gate all green, with
  all four scope fences proven from `phase_base_sha` to `HEAD`.
</success_criteria>

<output>
Create `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-03-SUMMARY.md` when
done. Record in it: the baseline and post-change corpus figures with the delta attribution, the
per-site before/after title and author values, the `CONF-10` entry text, and the SC#1–SC#6 roll-up
table.
</output>
