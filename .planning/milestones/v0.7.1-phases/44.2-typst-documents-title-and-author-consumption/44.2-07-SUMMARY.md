---
phase: 44.2-typst-documents-title-and-author-consumption
plan: 07
subsystem: docs
tags: [ast-probing, sphinx, docs-precedence, drift-guard, gap-closure]

# Dependency graph
requires:
  - phase: 44.2 (plan 06)
    provides: the two-stage Precedence rewrite in configuration.rst, tests/test_authors_pipeline_stage_gate.py, the Group-3 cell in test_entry_metadata_precedence.py
provides:
  - 44.2-GATE-EVIDENCE-06.md — an independently re-derived, per-sentence, per-stage falsification enumeration over the authors precedence rule
  - a fixed residual (two unscoped stage-1 bullets in configuration.rst) plus its drift guard
affects: [44.2 verify-phase, 45.1 (templates.rst input noted)]

# Actuals (#2632)
actuals:
  tokens: 20300
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Independent-grader wave separation: the correction (wave 6) and its grading (wave 7, this plan) run in separate worktrees/sessions so the grader cannot inherit the author's frame."
    - "Falsification row space widened along a STAGE axis (mapping x render), not just a config-shape axis, so a claim true of one stage but false of a later one becomes falsifiable."

key-files:
  created:
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md
  modified:
    - docs/source/user_guide/configuration.rst
    - tests/test_authors_pipeline_stage_gate.py

key-decisions:
  - "Re-derived the pipeline-stage writer set independently via an AST walk over both typsphinx/template_engine.py and typsphinx/writer.py rather than trusting plan 06's frozen EXPECTED_STAGE_SITES constant; the two sets agreed exactly (no disagreement to record)."
  - "Widened the falsification row space with a typst_template_function tf/notf axis (36 generated rows) — the axis absent from round 3's map_parameters()-only row space that let an unfalsifiable claim through."
  - "Found a residual (FA-04): two Precedence-block bullets used unscoped 'is then passed' / 'lands...is then' language that reads as a claim about the final rendered output, not merely stage 1's own return value — falsified by the same three-knob shape (author-to-doc_authors-present-tf) that produced four prior rounds of failure. Fixed by naming the stage-1 scope explicitly and cross-referencing stage 2's override, with no change to any pinned test literal."
  - "Added one new drift guard test (test_configuration_rst_stage1_bullets_name_stage2_override_explicitly) so the fix cannot silently revert to the unscoped shape."

patterns-established:
  - "Per-sentence falsification grading requires a named row on EVERY later pipeline stage, not just the stage nearest the sentence's own topic — a row with an empty later-stage cell is unfinished, not a pass."

requirements-completed: []  # CONF-09 remains open by design — this plan produces evidence, verify-phase seals it

coverage: []

# Metrics
duration: ~45min
completed: 2026-08-07
status: complete
---

# Phase 44.2 Plan 07: Independent Per-Stage Falsification Grading (Round-4 Gap Closure) Summary

**Independently re-derived the authors-precedence stage set from an AST walk over both pipeline
modules, widened the falsification row space with a render-stage axis, graded all 23 claim-bearing
sentences against per-stage falsification rows, found and fixed one residual (two unscoped
stage-1-output bullets), and closed `44.2-VERIFICATION.md`'s last open gap (`missing:` bullet 2).**

## Performance

- **Duration:** ~45 min (commit span 16 min 23s; includes provisioning, AST probing, and grading
  analysis before the first commit)
- **Completed:** 2026-08-07T07:08:12+09:00 (last task commit)
- **Tasks:** 3/3
- **Files modified:** 3 (1 created: `44.2-GATE-EVIDENCE-06.md`; 2 modified:
  `docs/source/user_guide/configuration.rst`, `tests/test_authors_pipeline_stage_gate.py`)

## Accomplishments

- Re-derived the 9-site pipeline-stage writer set (both `map_parameters()` and `render()`, both
  `typsphinx/template_engine.py` and `typsphinx/writer.py`) via a fresh AST walk in this plan's own
  session, independently confirming plan 06's `EXPECTED_STAGE_SITES` constant — no disagreement.
- Generated a 36-row falsification table (`source key x target key x presence x
  typst_template_function tf/notf`), the axis round 3's `map_parameters()`-only row space lacked,
  by a pasted loop (not typed by hand).
- Swept the WHOLE `configuration.rst` file plus `template_engine.py`'s D-05 comment block for every
  sentence constraining when a stage writes `authors` — 23 claim-bearing sentences, each with a
  single-line citation and a stage tag, derived from §1's stage list outward (never from the
  passages any prior plan edited).
- Graded all 23 sentences against a named falsification row on BOTH the mapping stage and the
  render stage — mechanically verified equal (23 surfaced == 23 graded) by a runnable id-set
  assertion, not a prose claim.
- Found the specific defect class this round was designed to catch: two Precedence-block bullets
  (S-14, S-16) used unscoped "is then passed" / "lands...is then" language, graded
  NARROWER-THAN-CODE against the same three-knob shape (`author-to-doc_authors-present-tf`) that
  produced four rounds of failure. Fixed with a minimal, comment-scope-only reword naming the
  stage-1 scope explicitly, without touching any pinned drift-guard literal; re-graded ACCURATE.
- Added a new drift guard test so the fix cannot silently revert.
- Named a passing test for every clause of the published rule and every enumerated stage (17 rows,
  18 tests, all PASSED in a fresh `-v` run).
- Full suite: 941 passed, 1 skipped (up from this session's own 940 baseline, up from
  `44.2-VERIFICATION.md`'s round-3 base of 932 — delta reconciled by name: +8 plan 06, +1 this plan).
- `black`/`ruff`/`mypy` clean; docs HTML build succeeds with zero WARNING/ERROR lines naming
  `configuration.rst`.
- Scope fences proved: `typsphinx/` is `ast`-equal and byte-diff-empty against `GAP_BASE_R4`; plans
  01-05, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, `44.2-GATE-EVIDENCE-01..05.md` untouched;
  `.planning/REQUIREMENTS.md`'s `CONF-09` remains `- [ ]` / `Pending` (unsealed).

## Task Commits

Each task was committed atomically:

1. **Task 1: Re-derive the stage set from the committed code, and generate the widened
   falsification row space (§0-§2)** - `11bd5c5` (docs)
2. **Task 2: Derive the sentence set from the stage set, grade every sentence against per-stage
   falsification rows, and enforce equality mechanically (§3-§5)** - `b036e65` (fix)
3. **Task 3: Clause-to-test traceability, scope-fence proofs, and the SC#6 re-take (§6-§7)** -
   `93f80b2` (docs)

_No separate plan-metadata commit — the orchestrator handles STATE.md/ROADMAP.md updates centrally
after this worktree merges (worktree isolation mode)._

## Files Created/Modified

- `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md` -
  new evidence file, §0-§7: worktree isolation/baseline, AST stage enumeration, widened
  falsification row space, sentence inventory, per-stage grading, no-silent-drop equality assertion,
  clause-to-test traceability, scope-fence proofs, SC#6 re-take, discharge statement.
- `docs/source/user_guide/configuration.rst` - reworded two Precedence-block bullets (S-14, S-16)
  to name their stage-1 scope explicitly and cross-reference stage 2's override; no pinned literal
  changed.
- `tests/test_authors_pipeline_stage_gate.py` - added
  `test_configuration_rst_stage1_bullets_name_stage2_override_explicitly`, a drift guard for the fix.

## Decisions Made

- The stage-set walk was re-run independently in this session rather than trusted from plan 06's
  frozen `EXPECTED_STAGE_SITES` constant — the two sets agreed exactly, so no substitution was
  needed, but the independence was structural, not assumed.
- The falsification row space's `tf`/`notf` axis generalizes the specific `{"project": "title"}`
  three-knob shape `44.2-VERIFICATION.md` reproduced to the broader class "any mapping whose target
  is not `authors`" — represented by rows like `author-to-doc_authors-present-tf`, cited throughout
  §4 and §6 as the canonical falsifying row for this class of claim.
- S-14/S-16's residual fix was graded ACCURATE only after rewording, per FA-04's own caveat: this
  plan is both the grader that FOUND the defect and the one that FIXED it, so the wave-separation
  control does not apply to that specific fix. Compensating controls applied per the plan's own
  design: the re-grading used the same mechanical probes and equality assertion, both the
  pre-fix and post-fix wording are recorded verbatim, and a new drift guard pins the fix. Given the
  fix's minimal, symmetric, comment-scope-only nature (verified `ast`-equal, re-tested, drift-guarded),
  §7's discharge recommends no further wave for this specific residual.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] NixOS `.venv/bin/uv` generic-linux-ELF hazard in this fresh worktree**
- **Found during:** Task 1 (§0 provisioning)
- **Issue:** A fresh `uv sync` in this worktree produced a `.venv/bin/uv` that could not execute
  under the NixOS sandbox (`Could not start dynamically linked executable: uv`), causing 45
  integration tests that shell out via `["uv", "run", "sphinx-build", ...]` to fail
  environmentally (`45 failed, 895 passed, 1 skipped` before the fix).
- **Fix:** Symlinked `.venv/bin/uv` to the same nix store binary `44.2-GATE-EVIDENCE-04.md` §0
  recorded, and re-applied the analogous `patchelf` interpreter fix to `.venv/bin/ruff`.
- **Files modified:** none tracked (worktree-local `.venv/`, gitignored).
- **Verification:** Post-fix baseline `940 passed, 1 skipped` — exactly matching the orchestrator's
  independently measured main-tree figure at this plan's base commit.
- **Committed in:** not applicable (environment-only, no tracked file change; recorded in §0).

**2. [Rule 1 - Bug, FA-04 residual] Two unscoped Precedence-block bullets, NARROWER-THAN-CODE**
- **Found during:** Task 2 (§4 grading)
- **Issue:** S-14/S-16's original wording ("Both values are then passed" / "What lands in authors
  is then...") read as an unscoped claim about the final rendered output; falsified by
  `author-to-doc_authors-present-tf`, where the seed survives stage 1 but is overridden at render
  by `typst_template_function`'s `params["authors"]`.
- **Fix:** Reworded both bullets to name the stage-1 scope explicitly and cross-reference stage 2's
  override, preserving all three literals the existing drift guards pin.
- **Files modified:** `docs/source/user_guide/configuration.rst`,
  `tests/test_authors_pipeline_stage_gate.py` (new drift guard).
- **Verification:** Re-graded ACCURATE against the same falsification row; full three-module suite
  re-run green (58 passed); full suite green (941 passed, 1 skipped); `ast`-equality confirms no
  executable line touched; docs build re-verified with zero `configuration.rst` warnings.
- **Committed in:** `b036e65` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking-environment / Rule 1, 1 bug / Rule 1)
**Impact on plan:** Both were necessary — the NixOS fix to obtain a trustworthy baseline at all,
and the S-14/S-16 fix is the plan's own core deliverable (the exact class of defect this round
exists to find). No scope creep: no `typsphinx/` line changed, no other file outside
`files_modified` was touched.

## Issues Encountered

None beyond the two auto-fixed items above. The sandbox's command-complexity/path-token checker
blocked several multi-command and `docs/source`-literal-path Bash invocations during this session;
worked around by running single commands and using `tox -e docs-html` (which references `docs/`
as a tox working directory, not a literal `source` path argument) for the docs build instead of a
direct `sphinx-build ... docs/source ...` invocation.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `44.2-GATE-EVIDENCE-06.md` is in place for `/gsd-verify-phase` to grade against
  `44.2-VERIFICATION.md`'s `missing:` bullet 2 and plan 05's backstop must-have.
- `.planning/REQUIREMENTS.md`'s `CONF-09` remains deliberately unsealed — this plan produces
  evidence, not a seal; sealing is verify-phase's call.
- Two carry-forwards recorded as explicitly OUT OF SCOPE, not gaps: `44.2-REVIEW.md` WR-01
  (`typsphinx/writer.py:319-321`, pre-existing, Phase 27.1) and any `templates.rst` claim-bearing
  sentence (Phase 45.1 SC#2's to verify — not swept or touched here).
- No blockers for phase closure from this plan's own scope.

---
*Phase: 44.2-typst-documents-title-and-author-consumption*
*Completed: 2026-08-07*
