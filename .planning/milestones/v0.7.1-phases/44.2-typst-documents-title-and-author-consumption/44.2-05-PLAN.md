---
phase: 44.2-typst-documents-title-and-author-consumption
plan: 05
type: execute
wave: 5
depends_on: ["44.2-01", "44.2-02", "44.2-03", "44.2-04"]
files_modified:
  - docs/source/user_guide/configuration.rst
  - typsphinx/template_engine.py
  - tests/test_params_authors_writers.py
  - tests/test_entry_metadata_precedence.py
  - tests/test_template_engine.py
  - .planning/REQUIREMENTS.md
  - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md
autonomous: true
gap_closure: true
requirements: [CONF-09]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-05/D-11: the published rule is stated over the ASSIGNMENT TARGET and is true on every measured row -- `typst_authors`' seed at `params[\"authors\"]` survives if and only if no entry of the active `parameter_mapping` has the target key `\"authors\"` with its source key present in the passed `sphinx_metadata`. The source key need not be `\"author\"`; its presence is neither necessary nor sufficient."
    - "D-11: every site that states the rule states it in target-key terms -- `docs/source/user_guide/configuration.rst`'s Precedence paragraph and its element-4 bullet, `typsphinx/template_engine.py`'s D-05 reorder comment, `tests/test_entry_metadata_precedence.py`'s module docstring / Group 2 banner / two cell docstrings, and `tests/test_template_engine.py`'s `test_typst_authors_through_pipeline_produces_native_array_of_dicts` docstring."
    - "D-06: `.planning/REQUIREMENTS.md`'s `CONF-10` Future Requirement entry -- authored by this phase under D-06 and the fifth defect site, outside plan 04's four-file scope -- states the same target-key rule. Its edit is confined to the `CONF-10` bullet: `CONF-09`'s checkbox and the Traceability table are byte-identical."
    - "SC#2 restored, per clause: each clause of the corrected rule is pinned by a named, passing test in `tests/test_entry_metadata_precedence.py` Group 2 -- including the two shapes with zero prior coverage anywhere in the suite: `\"author\"` routed to a target other than `\"authors\"` (seed survives AND the mapped value lands under the other key, simultaneously), and a non-author source key routed INTO `\"authors\"` (seed destroyed by a non-author value)."
    - "A rule one predicate short of the code now FAILS in CI, not at a third verify round: `tests/test_params_authors_writers.py` generates its row set mechanically from the code's own axes (source keys x target keys x source-presence) and asserts each row against an independently-written predicate transcribing the published sentence, so a narrower published rule cannot be transcribed into that predicate without turning rows red."
    - "The writer set is pinned structurally: an AST enumeration asserts exactly which sites in `map_parameters()` assign `params[...]`, so a future fourth writer of `params[\"authors\"]` fails a test that names the published rule as the thing to re-derive."
    - "D-05: the production change is comment-only. `ast.dump(ast.parse(...))` of `typsphinx/template_engine.py` is unchanged from the gap base, and every added or removed line under `typsphinx/` begins with `#`. `map_parameters()`'s behaviour is correct and stays untouched."
    - "The historical record is intact: `44.2-01/02/03/04-PLAN.md`, every `-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, and `44.2-GATE-EVIDENCE-01/02/03/04.md` are byte-identical to the gap base. The correction is recorded FORWARD in `44.2-GATE-EVIDENCE-05.md`."
    - "SC#6 still holds: full `uv run python -m pytest`, `black --check .`, `ruff check .`, `mypy typsphinx/` are green against the pre-change baseline measured in the same session, and a docs HTML build emits no WARNING or ERROR line naming `configuration.rst`."
    - statement: "Every published sentence that constrains when `params[\"authors\"]` is written is enumerated in `44.2-GATE-EVIDENCE-05.md` with (a) a verdict against the generated row table and (b) the named row that was the closest falsification attempt for that sentence. A verdict with no named falsification row is not permitted. Whole-file prose accuracy is a judgement no single assertion settles -- and plan 04's identical enumeration passed while shipping the defect precisely because its verdicts were graded against a probe whose rows came from the prose being graded."
      verification: backstop
  artifacts:
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md
    - tests/test_params_authors_writers.py
    - docs/source/user_guide/configuration.rst
    - typsphinx/template_engine.py
    - tests/test_entry_metadata_precedence.py
    - tests/test_template_engine.py
    - .planning/REQUIREMENTS.md
  key_links:
    - "`map_parameters()`'s three writers of `params[\"authors\"]` (the seed, the mapping loop's `params[template_key]`, the non-package back-fill) -> the target-key iff-rule -> the AST writer enumeration in `tests/test_params_authors_writers.py`"
    - "the mapping loop's `params[template_key] = value` -- the ONLY site that can overwrite the seed -> the four new named Group 2 cells covering a source routed away from `\"authors\"` and a non-author source routed into it"
    - "the generated source x target x presence row table -> the published sentence in `configuration.rst` -> the independently-written `_seed_survives()` predicate the matrix asserts against"
    - "`.planning/REQUIREMENTS.md`'s `CONF-10` entry -> the same target-key rule -> the CONF-10-confined-diff gate that keeps `CONF-09`'s row untouched"
  prohibitions:
    - statement: "MUST NOT publish any exhaustive quantifier over CONFIG SHAPES for the survives-case (of the form 'exactly N configurations/shapes/routes'), and MUST NOT hedge one with 'usually', 'typically', 'most often', or 'in practice'. Two rounds of this phase have now shipped a quantifier over config shapes that a reachable shape falsified. The only exhaustive statement permitted is the iff over the mapping predicate itself; individual config shapes may be given as ILLUSTRATIONS of that rule, never as an enumeration of it. A hedge is worse than a wrong quantifier because it cannot be falsified by any test."
      status: unverified
      verification: test
    - statement: "MUST NOT transcribe `44.2-REVIEW.md` WR-01's literal suggested sentence, nor `44.2-VERIFICATION.md`'s `missing:` bullet 1 phrasing. Both are keyed on the SOURCE key `\"author\"` and both mispredict a measured row: WR-01's wording says the seed survives whenever the mapping does not send `\"author\"` to `\"authors\"`, which is false for a mapping that sends some OTHER key there. Copying either would make this the third consecutive round of a rule one predicate short. Their FINDINGS are correct and are closed by this plan; only their suggested wordings are rejected."
      status: unverified
      verification: test
    - statement: "MUST NOT change any executable line under `typsphinx/`. This is a text-accuracy repair inside a patch release that already carries two user-visible behaviour changes; a runtime change smuggled in under a comment fix would ship untested behaviour under a docs-only banner. `map_parameters()` has been correct throughout this phase -- only its description keeps landing short. Comment lines only, `ast.parse`-verified."
      status: unverified
      verification: test
    - statement: "MUST NOT weaken, delete, rename, `xfail`, `skip`, or loosen any existing assertion in `tests/test_entry_metadata_precedence.py`, `tests/test_template_engine.py`, or any other module to accommodate the corrected prose. The repair ADDS coverage; collected test counts go up, never down. Deleting the narrow cells that documented the narrow claim would destroy the control the new general rows are read against."
      status: unverified
      verification: test
    - statement: "MUST NOT edit, amend, or re-word `44.2-GATE-EVIDENCE-01/02/03/04.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, any `44.2-0[1234]-PLAN.md`, or any `-SUMMARY.md` to make the two earlier overstated rules disappear. They are the record of what was published, reviewed and measured across three rounds; erasing them would destroy the only evidence of the failure mode this plan exists to break. The correction is recorded FORWARD."
      status: unverified
      verification: test
    - statement: "MUST NOT name any plan-authored artifact `44.2-VERIFICATION.md`. That filename is owned and overwritten wholesale by `/gsd-verify-phase`; a plan writing evidence there loses it silently on the next verify run. Use `44.2-GATE-EVIDENCE-05.md`."
      status: unverified
      verification: test
    - statement: "MUST NOT edit `.planning/REQUIREMENTS.md` outside the `CONF-10` Future Requirement bullet. In particular MUST NOT flip `CONF-09`'s `- [ ]` checkbox or touch the Traceability table -- `phase.complete` has a recorded habit of flipping such rows early on this project, and a plan doing it by hand would make the phase look sealed while a gap is open."
      status: unverified
      verification: test
    - statement: "MUST NOT 'correct' the accurate package-ROUTING sentences elsewhere in the repo as collateral: `typsphinx/template_engine.py`'s `uses_bundled_default_template()` docstring, `tests/test_entry_metadata_route_uniformity.py`, `tests/test_package_only_config_gate.py`, `tests/test_package_template_routing.py`, `tests/test_examples_charged_ieee_gate.py`, both fixture `conf.py` files, `typsphinx/writer.py`, `typsphinx/builder.py`. They describe template-file routing, a different subject, and are correct. MUST NOT edit `docs/source/user_guide/templates.rst` -- Phase 45.1 SC#2's to verify, not this phase's to rewrite."
      status: unverified
      verification: test
    - statement: "MUST NOT write the matrix's expected value by calling the code under test. `_seed_survives()` is a transcription of the PUBLISHED sentence into Python and must be readable as such; deriving it from `map_parameters()`'s output would make the matrix a tautology that passes for any rule, including the two already measured false."
      status: unverified
      verification: test
---

<objective>
Close the gap `44.2-VERIFICATION.md` records at 5/7 (ROADMAP SC#2 and the plan 03/04 backstop
truth, one root cause): the rule typsphinx publishes for when `typst_authors` survives is still
narrower than the rule its code implements.

This phase has now published a wrong version of that rule twice, and both failures share one
cause: **the search set for the correction was derived from the vocabulary of the prose being
corrected (the mapping SOURCE key `"author"`) rather than from the code's own vocabulary (the
assignment TARGET `params["authors"]`).** Round 1 said the seed survives only on a package-alone
build; round 2 said it survives on exactly the two configurations whose mapping carries no
`"author"` key. Both were falsified by shapes nobody enumerated because nobody was enumerating over
the right axis.

The rule, measured (D-05): the `typst_authors` seed at `params["authors"]` survives **iff no entry
of the active `parameter_mapping` has the target key `"authors"` with its source key present in
`sphinx_metadata`**. `{"author": "doc_authors"}` keeps the seed *and* passes the mapped value under
`doc_authors`; `{"project": "authors"}` destroys the seed with the project name despite never
mentioning `"author"` at all.

Purpose: state that rule at every site that states it (D-11, plus the D-06 `CONF-10` entry), give
every clause of it a named test (SC#2), and make the next drift fail in CI rather than at a fourth
verify round -- by generating the row table mechanically from the code's axes instead of from the
prose.

Output: one new test module carrying the writer enumeration and the generated matrix; six corrected
text sites plus the `CONF-10` entry; four new named Group 2 cells; and
`44.2-GATE-EVIDENCE-05.md`, whose per-sentence enumeration records a named falsification attempt
per verdict.

Scope fence: no runtime behaviour change (`map_parameters()` is correct and stays untouched, D-05);
no `typst_authors` removal (still `CONF-10`, D-06); no `templates.rst` edit (Phase 45.1 SC#2's); no
`.planning/REQUIREMENTS.md` edit outside the `CONF-10` bullet.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-REVIEW.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-CONTEXT.md
@typsphinx/template_engine.py
@docs/source/user_guide/configuration.rst
@tests/test_entry_metadata_precedence.py
</context>

<measured_grounding>
Every figure below was measured in this planning session by direct execution against the shipped
code at `GAP_BASE = 6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c`. Re-derive rather than trust -- but do
not skip §3, which is what makes the two currently-open "suggested fixes" unusable.

## 1. Complete enumeration of writers to `params["authors"]`, derived from the assignment target

`ast.parse` over `TemplateEngine.map_parameters()`, collecting every `Assign` whose target is a
`Subscript` on `Name(id="params")`:

| Kind | Slice | Line (at `GAP_BASE`) | Guard | Value |
|---|---|---|---|---|
| W1 | literal `"authors"` | 472 | `if self.typst_authors:` | `list[dict]` built from `typst_authors` |
| W2 | dynamic `template_key` | 486 | fires for `"authors"` iff some mapping entry has TARGET `"authors"` AND its SOURCE key is in `sphinx_metadata` | `_convert_to_authors_tuple(sphinx_metadata[src])` |
| W3 | literal `"authors"` | 497 | `if not self.typst_package` AND `"authors" not in params` | `()` |
| -- | literal `"title"` / `"date"` | 495 / 499 | same back-fill guard | `""` / `None` |
| -- | dynamic `key` | 518 | `typst_elements` loop, gated by `ELEMENTS_ALLOWLIST` | cannot reach `"authors"` |

Measured: `sorted(ELEMENTS_ALLOWLIST) == ['fontsize', 'lang', 'papersize']`, and an unknown key
raises `ExtensionError`. W3 cannot replace a seed -- its `"authors" not in params` guard means it
only fills an absent key (measured: a non-package engine with no `typst_authors` and a mapping
targeting only `"title"` yields `params["authors"] == ()`). **W2 is therefore the only overwriter,
and the rule is a property of W2's guard alone.**

## 2. The generated truth table (`uv run python`, live)

All rows: `typst_authors={"Jane Doe": {"organization": "MIT"}}`,
`sphinx_metadata={"project": "P", "author": "Explicit Entry Author", "release": "1.0"}` unless noted.

| # | `parameter_mapping` | survives? | resulting `params` (authors-relevant) |
|---|---|---|---|
| A | unset -> `DEFAULT_PARAMETER_MAPPING` | no | `authors=('Explicit Entry Author',)` |
| B | `{"project": "title"}` | yes | `authors=<seed>` |
| C | `{"project": "title", "author": "authors"}` | no | `authors=('Explicit Entry Author',)` |
| D | `{"project": "title", "author": "doc_authors"}` | **yes** | `authors=<seed>` AND `doc_authors=('Explicit Entry Author',)` |
| E | `{"project": "title", "author": "writer"}` | **yes** | `authors=<seed>` AND `writer=('Explicit Entry Author',)` |
| F | `{"project": "authors"}` -- no `"author"` key at all | **no** | `authors=('P',)` -- the project name lands in `authors` |
| G | `{"release": "authors", "project": "title"}` | **no** | `authors=('1.0',)` |
| H | `{"project": "title", "author": "authors"}`, `"author"` absent from metadata | yes | `authors=<seed>` |
| I | `typst_package` set, `parameter_mapping` left `None` (`__init__` resolves `{}`) | yes | `authors=<seed>` |

Rows D, E, F and G have **zero** test coverage anywhere in the suite.
`tests/test_config_template_mapping.py` already exercises `{"author": "doc_authors"}` as a realistic
custom-template config (Requirement 8.4); only the combination with `typst_authors` is untested.

## 3. Both currently-open suggested fixes are STILL frame-bound -- do NOT transcribe either

- `44.2-REVIEW.md` WR-01's "Fix:" proposes: *`typst_authors` survives as the sole source whenever
  the active mapping does not send `"author"` to the template key `"authors"`*. **Measured false on
  row F**: `{"project": "authors"}` does not send `"author"` anywhere (there is no `"author"` key at
  all) yet the seed is destroyed.
- `44.2-VERIFICATION.md`'s `missing:` bullet 1 is correct as far as it goes but is stated as a
  repair to the source-key framing, and its companion test bullet asks only for a
  `{"author": "<non-authors-key>"}` cell (rows D/E). It does not reach F/G.

WR-01's and the verification's *findings* are correct and are closed here. Their *wordings* are not
used, and §5 of the evidence file records why with the falsifying row.

## 4. Occurrence sweep, derived from the target key (repo, `GAP_BASE`)

The sweep that matters is "every prose site that constrains when `params["authors"]` is written",
not "every site containing the previous round's qualifier". Measured hits that state the rule:
`docs/source/user_guide/configuration.rst` (Precedence paragraph 186-203, element-4 bullet 49-58),
`typsphinx/template_engine.py` (D-05 comment 428-453), `tests/test_entry_metadata_precedence.py`
(module docstring 14-20, Group 2 banner 184-194, two cell docstrings 303-307 and 326-333),
`tests/test_template_engine.py` (docstring paragraph 941-951), `.planning/REQUIREMENTS.md`
(`CONF-10` entry, the sentence run beginning on line 191).

Measured NOT instances (they describe template-file routing, a different subject, and are correct):
`typsphinx/template_engine.py:367`, `tests/test_entry_metadata_route_uniformity.py` (5 occurrences,
plus its line 172 pointer, which only names where the precedence matrix lives),
`tests/test_package_only_config_gate.py`, `tests/test_package_template_routing.py`,
`tests/test_examples_charged_ieee_gate.py`, both fixture `conf.py` files, `typsphinx/writer.py`,
`typsphinx/builder.py`. Also measured NOT instances: `docs/source/user_guide/configuration.rst:322`
and `examples/charged-ieee/README.md:152`, which point at the rule rather than restating it, and
`docs/source/examples/advanced.rst:42`, which is a bare config example. Task 1 re-runs this sweep
rather than trusting the list.
</measured_grounding>

<assumption_delta_decision>
The `assumption-delta` detector fired (`detected: true`; `pluralization` on "fallback", `chosen` on
"chosen order") over this phase's ROADMAP section. Advisory, recorded here, non-blocking.

- **The noun that is now primary:** *the mapping entry whose TARGET key is `"authors"`*. Two rounds
  modelled the identity of this decision as "the config shape / template route" (round 1) and then
  "the mapping's source key `"author"`" (round 2). Both are now demoted to derived details of
  particular variants: a package build with no mapping is one way to have no entry targeting
  `"authors"`; `"author"` being absent from the mapping is another; and neither is necessary.
- **Decision: `promote`.** The general representation (the target-key predicate) becomes the primary
  statement everywhere, and the two previously-primary framings survive only as illustrations under
  it. Adding the new cases alongside the old rule -- which is what round 2 did, appending a second
  config shape to a shape enumeration -- is exactly what let a third shape stay invisible.
- **Companion invariant test, accepted:** `tests/test_params_authors_writers.py`'s generated matrix
  is the invariant that encodes the generalized intent ("every row of the source x target x
  presence cross product agrees with the published predicate"). It goes red the instant a future
  change reintroduces a source-key or route assumption, so the regression cannot land silently.
</assumption_delta_decision>

<flagged_assumptions>
Surfaced, not silently dropped, per the spec-less probe fallback protocol (this phase has no
SPEC.md, so no `## Edge Coverage` / `## Prohibitions` section exists to lift from).

**FA-01 -- CONF-09's edge classification is `unclassified` / `unresolved`.** The deterministic edge
probe returned `{applicable: 1, resolved: 0, unresolved: 1}` for CONF-09 with
`probe: "unclassified -- review manually"`. Per protocol an `unclassified` row stays `unresolved`,
is never auto-`backstop`ped and is never silently dropped, so no probe-derived edge predicate was
authored into `must_haves` from it. Every truth above was derived goal-backward from the two FAILED
truths in `44.2-VERIFICATION.md` and from the measured code. Consequence accepted knowingly: if
CONF-09 carries a data-shape edge the probe would have surfaced (encoding, ordering, adjacency),
this plan does not cover it. Judged acceptable because this plan's scope is text accuracy plus test
rows over an already-shipped, already-verified mechanism (SC#1/SC#3/SC#4/SC#5/SC#6 all verified).
No-silent-drop equality for edges: 1 surfaced == 0 authored + 1 flagged assumption.

**FA-02 -- prohibition probe: 15 raw candidates, 8 dropped as routine engineering, 0 canon
referrals, 7 authored.** Stage-2 drops with breadcrumbs: docs-build health, test-collection
correctness, dependency/version hygiene, `@preview` lockstep, rst list well-formedness, wrap-width
conformance, commit granularity, and evidence-file section completeness -- all normal correctness
or hygiene owned by the acceptance criteria and by SC#6's gate, not values/safety items. **No
OWASP / injection / path-traversal / prototype-pollution / GDPR / fairness candidate survived Stage
2** for this scope (prose, comments and tests; no new input handling, no new parsing of untrusted
data), so there is no canon-referral breadcrumb to `/gsd-secure-phase`. The seven kept prohibitions
are bespoke project-hazard items, authored descriptor-less and disposing flagged-unverified.
No-silent-drop equality for prohibitions: 15 surfaced == 7 authored + 8 dropped with breadcrumbs.

**FA-03 -- the source-presence half of the rule is asserted at API level only.** The rule's second
conjunct (the source key must be present in `sphinx_metadata`) is unreachable from a real
`sphinx-build`, because `writer.py` always supplies `"author"` via `_resolve_entry_element`. Its
test is therefore a direct `map_parameters()` call. If a future change makes `writer.py` omit a key
conditionally, this plan's rows pin the resulting behaviour but nothing pins that the writer still
supplies it.

**FA-04 -- the drift guards are literal-presence assertions, not semantic ones.** No test can prove
an English paragraph is accurate. `tests/test_params_authors_writers.py` pins that
`configuration.rst` and the D-05 comment still carry the two illustrating mapping literals and the
target-key phrasing, so a revert to a source-key framing goes red -- but a *new* wrong sentence
added beside them would not. That residual is what §3 of the evidence file (falsification-per-row
enumeration) exists to catch, and it is a human-reviewable artifact, not an automated one.
</flagged_assumptions>

<source_audit>
| SOURCE | ID | Item | Task | Status |
|---|---|---|---|---|
| GOAL | -- | Entry `[2]`/`[3]` take effect with a defined and tested precedence against `typst_authors` (the residual: the *definition* is still narrower than the code) | 1, 2 | COVERED |
| REQ | CONF-09 | "a defined and tested precedence against `typst_authors`" -- the only clause `44.2-VERIFICATION.md` leaves unmet | 1, 2, 3 | COVERED |
| RESEARCH | -- | Pattern 2 (the D-05 reorder as a minimal block move) -- already landed by plan 01; this plan asserts it is untouched | 1 (comment-only gate) | COVERED |
| RESEARCH | -- | Pitfall 3 (do NOT re-implement D-05 as a per-entry override) -- the corrected comment records why it cannot be built that way | 1 | COVERED |
| RESEARCH | -- | Package Legitimacy Audit -- no package-manager install task exists in this plan; nothing to gate | -- | N/A |
| RESEARCH | -- | Architectural Responsibility Map -- this plan writes only prose, comments and tests; no tier assignment changes | -- | N/A |
| CONTEXT | D-05 | `entry[3]` beats `typst_authors`; the mechanism is an assignment reorder, not a branch | 1, 2 | COVERED |
| CONTEXT | D-06 | `typst_authors` removal filed as Future Requirement `CONF-10` -- this plan corrects that entry's text | 2 | COVERED |
| CONTEXT | D-11 | `configuration.rst` publishes the precedence rules decided here | 1, 2 | COVERED |
| CONTEXT | D-01/D-02/D-03/D-04/D-07/D-08/D-09/D-10 | Landed and verified by plans 01-03 (SC#1/SC#3/SC#4/SC#5 all VERIFIED in `44.2-VERIFICATION.md`); untouched here | -- | COVERED (prior plans) |

No `⚠ MISSING` rows. `api-coverage` detector returned `{"detected": false}` over this phase's scope
-- no external API integration, so no `COVERAGE.md` is produced. The schema-push gate finds no ORM
file patterns in scope.
</source_audit>

<artifacts_this_phase_produces>
Newly-created symbols and paths introduced by this plan. Drift verification must exclude these from
"does it already exist in the source" checks -- none of them exists before this plan runs.

**New file:**
- `tests/test_params_authors_writers.py`
- `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md`

**New module-level names in `tests/test_params_authors_writers.py`:**
- Constants: `REPO_ROOT`, `TEMPLATE_ENGINE_PATH`, `CONFIGURATION_RST_PATH`, `TYPST_AUTHORS`,
  `SEED`, `BASE_METADATA`, `SOURCE_KEYS`, `TARGET_KEYS`
- Helpers: `_seed_survives`, `_map_parameters_ast`, `_params_assignments`, `_map_parameters_region`
- Tests: `test_map_parameters_writes_params_from_exactly_these_sites`,
  `test_typst_elements_cannot_write_the_authors_key`,
  `test_non_package_back_fill_cannot_replace_a_typst_authors_seed`,
  `test_default_mapping_targets_the_authors_key`,
  `test_authors_seed_survival_matrix` (parametrized over the generated cross product),
  `test_configuration_rst_documents_both_target_key_shapes`,
  `test_d05_comment_states_the_target_key_rule`

**New test functions in `tests/test_entry_metadata_precedence.py` (Group 2):**
- `test_map_parameters_author_mapped_to_non_authors_target_keeps_typst_authors`
- `test_map_parameters_author_mapped_to_arbitrary_target_keeps_typst_authors`
- `test_map_parameters_non_author_source_mapped_to_authors_destroys_typst_authors`
- `test_map_parameters_release_mapped_to_authors_destroys_typst_authors`

**No new production symbols.** No new fixture directory, no new `conf.py`, no new helper under
`typsphinx/`, no new config value, no new dependency. Every other file in `files_modified` is edited
in place.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One end-to-end path for the target-key rule — writer enumeration, generated matrix, code comment, published paragraph</name>

  <precondition>The worktree has its own environment: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` has been run, and `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"` prints a path inside THIS worktree, not `/home/yuta/Documents/typsphinx/typsphinx`. If it prints the main checkout, stop — the editable finder is resolving to the main tree and every gate below would measure the wrong files. Plans 03 and 04 both hit the NixOS generic-linux-ELF hazard on `.venv/bin/uv` and `.venv/bin/ruff`; the recorded fix is in `44.2-GATE-EVIDENCE-04.md` §0 (symlink `.venv/bin/uv` to the nix-store copy; `patchelf --set-interpreter` on `.venv/bin/ruff`). Apply it and record what you applied.</precondition>

  <reversibility rating="reversible">A comment edit, a documentation paragraph and one new test module; a single `git revert` restores the prior wording with no data, schema or API consequence.</reversibility>

  <files>tests/test_params_authors_writers.py, typsphinx/template_engine.py, docs/source/user_guide/configuration.rst</files>

  <read_first>
    - `/tmp/claude-1000/-home-yuta-Documents-typsphinx/1f48eec0-ac70-4a24-afea-5b128097be50/scratchpad/44.2-orchestrator-measurement.md` — the orchestrator's own live measurement, produced before this plan existed. It supersedes the suggested-fix wording in BOTH `44.2-VERIFICATION.md` and `44.2-REVIEW.md`.
    - `typsphinx/template_engine.py` lines 418-522 — `map_parameters()` in full: the `params: Dict[str, Any] = {}` annotation, the D-05 comment block being replaced (428-453), the `if self.typst_authors:` seed, the mapping loop with its `if sphinx_key in sphinx_metadata:` guard and its `_convert_to_authors_tuple` special case, the `if not self.typst_package:` back-fill, and the `typst_elements` loop with its `ELEMENTS_ALLOWLIST` raise. This is the sole source of truth for the corrected wording; the defect exists twice over because the replacement text was written without enumerating over this method's assignment targets.
    - `typsphinx/template_engine.py` lines 77-90 (`ELEMENTS_ALLOWLIST`) and 196-243 (`DEFAULT_PARAMETER_MAPPING` and `__init__`'s three mapping-resolution branches).
    - `typsphinx/template_engine.py` lines 358-386 — `uses_bundled_default_template()`'s docstring. Line 367 uses the package-routing qualifier ACCURATELY and about a different subject; it must survive verbatim, which is why this file's negative gate is region-scoped to `map_parameters()` and never file-wide.
    - `docs/source/user_guide/configuration.rst` lines 163-214 — the Author Information section: the `typst_authors` example, the Precedence paragraph being replaced (186-203), and the `.. note::` forward-removal block (205-213) that must stay untouched.
    - `tests/test_preview_version_sync.py` lines 1-60 — this project's established cross-file consistency-test idiom (`REPO_ROOT = Path(__file__).resolve().parents[1]`, path constants, a docstring naming the hazard). The new module mirrors it.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-PATTERNS.md` lines 172-208 — the SC#2 matrix pattern, which explicitly prescribes discrete named functions over `@pytest.mark.parametrize` for `tests/test_entry_metadata_precedence.py`. Read it before writing the new module so the deviation recorded in its docstring is a deliberate one.
    - `44.2-GATE-EVIDENCE-04.md` §1 and §4 — the artifacts that FAILED to catch the residual defect. §1's probe never constructed a falsifying shape, so §4 rated all 17 sentences accurate against it. Understand that failure before designing anything here.
  </read_first>

  <behavior>
    The new module is a CHARACTERIZATION and DRIFT-GUARD module: every assertion passes against
    unmodified production code, because the code has been right throughout and only its description
    keeps landing short. Do not expect a RED-then-GREEN transition from the Python. The RED half of
    this task is the documentation gate, recorded against the pre-fix tree per this project's
    amended GATE-01 RED definition (a structural assertion defined before the change, since the
    defect compiles and tests fine).

    - `test_map_parameters_writes_params_from_exactly_these_sites`: AST-walks `map_parameters()` and
      collects every `Assign` whose target is a `Subscript` on `Name(id="params")`. Expects the
      multiset of literal-string slices to be exactly `["authors", "title", "authors", "date"]` in
      source order (two writers of `"authors"`: the seed and the back-fill) and the set of dynamic
      `Name` slices to be exactly `{"template_key", "key"}`.
    - `test_typst_elements_cannot_write_the_authors_key`: `"authors" not in ELEMENTS_ALLOWLIST`, and
      `map_parameters({...}, typst_elements={"authors": "x"})` raises `ExtensionError`.
    - `test_non_package_back_fill_cannot_replace_a_typst_authors_seed`: a non-package engine with
      `parameter_mapping={"project": "title"}` yields `params["authors"] == ()` with no
      `typst_authors`, and the seed (never `()`) with `typst_authors` set.
    - `test_default_mapping_targets_the_authors_key`:
      `TemplateEngine.DEFAULT_PARAMETER_MAPPING["author"] == "authors"`.
    - `test_authors_seed_survival_matrix`: for every cell of the generated cross product
      `SOURCE_KEYS x TARGET_KEYS x {source present, source absent}`, the seed survives exactly when
      `_seed_survives(mapping, metadata)` says it should; when it does not survive,
      `params["authors"] == (metadata[source],)`; when it survives with a non-`"authors"` target and
      the source present, `target in params` as well.
    - `test_configuration_rst_documents_both_target_key_shapes` and
      `test_d05_comment_states_the_target_key_rule`: drift guards — the published paragraph and the
      code comment each still carry the target-key phrasing and both illustrating mapping literals.
    - Pre-fix RED for the text half: `grep -n` against `git show <GAP_BASE>:docs/source/user_guide/configuration.rst`
      and `git show <GAP_BASE>:typsphinx/template_engine.py` returns the exhaustive-quantifier
      sentence at each site; the same greps against the working tree after this task return nothing.
  </behavior>

  <action>
Set `GAP_BASE=6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c` (the tree this plan was written against). Before anything else, assert it is an ancestor of `HEAD` with `git merge-base --is-ancestor $GAP_BASE HEAD`. If it is not, recompute it as the commit this task builds on, use that value everywhere below and in every hardcoded SHA in the `<verify>` blocks, and record the substitution and its reason in the evidence file in task 3 — never silently proceed with a base that does not match.

Record the pre-fix RED first, before editing anything. Run `git show $GAP_BASE:docs/source/user_guide/configuration.rst` and `git show $GAP_BASE:typsphinx/template_engine.py` through `grep -n` for the exhaustive-quantifier sentence at each site (`configuration.rst:192` and `template_engine.py:441`), and keep the verbatim line-numbered output for task 3's evidence file.

Then re-run the occurrence sweep yourself rather than trusting `<measured_grounding>` §4, and derive it from the assignment target: search the repo for prose that constrains when `params["authors"]` is written — start from `grep -rn 'typst_authors' --include=*.py --include=*.rst --include=*.md .` excluding `.planning/phases/`, then read each hit and classify it as "states the rule" or "mentions the setting". Record the classified list; task 3 §3 enumerates against it. If you find a site `<measured_grounding>` §4 does not list, correct it there and treat it as in scope for task 2.

**(1) Create `tests/test_params_authors_writers.py`.** This module's job is to make the rule executable and to generate its own row set, so a rule one predicate short of the code cannot pass. Mirror `tests/test_preview_version_sync.py`'s idiom: a module docstring naming the hazard, `REPO_ROOT = Path(__file__).resolve().parents[1]`, then path constants.

The module docstring must state: (i) that the enumeration axis is the ASSIGNMENT TARGET `params["authors"]`, not any config shape or mapping source key; (ii) the rule — the seed survives iff no entry of the active `parameter_mapping` has the target key `"authors"` with its source key present in the passed `sphinx_metadata`; (iii) that this phase published two rules derived from the prose's own vocabulary and both were falsified, which is why this module generates its rows from the code's axes instead; and (iv) a deliberate-deviation note: `44.2-PATTERNS.md` prescribes discrete named functions over `@pytest.mark.parametrize` for the sibling matrix module, and this module deviates on purpose, because a hand-enumerated row set is exactly what let the falsifying row be omitted twice — the sibling module keeps the discrete convention and gains its own named cells in task 2.

Module-level constants, exactly these names: `TEMPLATE_ENGINE_PATH = REPO_ROOT / "typsphinx" / "template_engine.py"`; `CONFIGURATION_RST_PATH = REPO_ROOT / "docs" / "source" / "user_guide" / "configuration.rst"`; `TYPST_AUTHORS = {"Jane Doe": {"organization": "MIT"}}`; `SEED = [{"name": "Jane Doe", "organization": "MIT"}]`; `BASE_METADATA = {"project": "P", "author": "Explicit Entry Author", "release": "1.0"}`; `SOURCE_KEYS = ("author", "project", "release")`; `TARGET_KEYS = ("authors", "doc_authors", "writer")`.

Helper `_seed_survives(mapping, metadata)` returns `not any(target == "authors" and source in metadata for source, target in mapping.items())`. Its docstring must say, in these terms, that it is the PUBLISHED sentence transcribed into Python and must stay readable as such — deriving it from `map_parameters()`'s output would make the matrix a tautology that passes for any rule, including the two already measured false. `TARGET_KEYS` covers three cases on purpose: `"authors"` itself, `"doc_authors"` (which the mapping loop special-cases for tuple conversion), and `"writer"` (which it does not), so surviving cannot be an artifact of the conversion special case.

Helpers `_map_parameters_ast()` (parse `TEMPLATE_ENGINE_PATH` and return the `FunctionDef` named `map_parameters`) and `_params_assignments()` (walk it, return two lists: literal-string slices in source order, and dynamic `Name` slice ids) and `_map_parameters_region()` (return the source text of `map_parameters()` from `TEMPLATE_ENGINE_PATH`, sliced by `lineno`/`end_lineno` of the `FunctionDef`).

Then the seven tests named in `<artifacts_this_phase_produces>`, with the behavior given in `<behavior>`. Specific requirements:

- `test_map_parameters_writes_params_from_exactly_these_sites` asserts the literal-slice list equals `["authors", "title", "authors", "date"]` and the dynamic-slice set equals `{"template_key", "key"}`. Its assertion message must name the consequence, not just the mismatch: a new writer of `params[...]` appeared in `map_parameters()`, so the published precedence rule in `docs/source/user_guide/configuration.rst`, the D-05 comment, and this module's `_seed_survives` must be re-derived before this expectation is changed.
- `test_authors_seed_survival_matrix` is parametrized over the generated cross product with readable ids of the form `source-to-target-present` / `source-to-target-absent`. Build each cell's mapping as `{source: target}` and each cell's metadata as `BASE_METADATA` or `BASE_METADATA` minus the source key. Construct `TemplateEngine(parameter_mapping=mapping, typst_authors=TYPST_AUTHORS)` with no `typst_package`. Assert `(params["authors"] == SEED) is _seed_survives(mapping, metadata)`; then, when the seed did not survive, `params["authors"] == (metadata[source],)`; and when it survived with `target != "authors"` and `source in metadata`, `target in params` (assert presence only — do not assert the mapped value's shape, because the loop tuple-converts only when the source is `"author"` or the target is `"authors"`/`"doc_authors"`, and that conversion is a separate concern).
- `test_configuration_rst_documents_both_target_key_shapes` reads `CONFIGURATION_RST_PATH`, collapses every run of whitespace to a single space (so re-wrapping the paragraph cannot break the guard), and asserts the normalized text contains all three of `no entry of the active mapping targets ``authors```, `{"author": "doc_authors"}` and `{"project": "authors"}`. Its docstring must state that the latter two are exactly the shapes the two earlier published rules mispredicted, so a revert to a source-key framing goes red here.
- `test_d05_comment_states_the_target_key_rule` reads `_map_parameters_region()`, normalizes whitespace the same way, and asserts it contains `target key "authors"` and both illustrating literals.

**(2) `typsphinx/template_engine.py` — replace the D-05 reorder rationale comment.** The block to replace begins at the comment line that starts `# CONF-09 (Phase 44.2, D-05): typst_authors now SEEDS "authors"` and ends at the line reading `# surviving case; that was measured false.` (currently lines 428-453). Leave the `#`-only separator line and everything after it — the `A dedicated "explicit entry[3] wins" override cannot be built` paragraph and the native-`list[dict]` paragraph — completely unchanged; they are accurate and load-bearing. Write exactly this in its place, preserving the existing eight-space indentation and the file's ~72-column comment width:

    # CONF-09 (Phase 44.2, D-05): typst_authors now SEEDS "authors"
    # FIRST, before the mapping loop below -- superseding its
    # previous position as an unconditional override that ran LAST.
    #
    # The rule below is enumerated from the ASSIGNMENT TARGET, not
    # from any config shape. Exactly three sites in this method can
    # write params["authors"]:
    #   W1  this seed, guarded by `if self.typst_authors`;
    #   W2  the mapping loop's `params[template_key] = value`, which
    #       reaches "authors" only when some mapping entry's TARGET
    #       is literally "authors" AND its SOURCE key is present in
    #       the sphinx_metadata dict passed in;
    #   W3  the `if not self.typst_package` back-fill, guarded by
    #       `"authors" not in params`, so it can only fill an absent
    #       key and can never replace a seed.
    # The typst_elements loop cannot reach the key at all:
    # ELEMENTS_ALLOWLIST is papersize/fontsize/lang and an unknown
    # key raises. W2 is therefore the only overwriter.
    #
    # Hence: the seed survives IFF no entry of self.parameter_mapping
    # has the target key "authors" with its source key present in
    # sphinx_metadata. The SOURCE key does not have to be "author",
    # and its presence is neither necessary nor sufficient --
    # {"author": "doc_authors"} keeps the seed in "authors" AND
    # passes the mapped value under "doc_authors", while
    # {"project": "authors"} destroys the seed with the project name
    # despite never mentioning "author".
    #
    # The condition is the MAPPING, not the template route: no
    # self.typst_package test exists in this method outside W3's
    # back-fill guard. A package build with parameter_mapping left
    # None survives only because __init__ resolves it to an EMPTY
    # dict, which trivially targets nothing.
    #
    # Every row is pinned by a named test in
    # tests/test_entry_metadata_precedence.py Group 2 and by the
    # generated matrix in tests/test_params_authors_writers.py. Two
    # earlier versions of this comment stated the rule over the
    # SOURCE key instead of the target; both were measured false.

This is a comment-only edit. Do not touch `params: Dict[str, Any] = {}`, the `if self.typst_authors:` block, the mapping loop, the back-fill guard, or the elements loop — the mechanism is correct and is what the corrected text describes.

**(3) `docs/source/user_guide/configuration.rst` — replace the Precedence paragraph.** Replace lines 186-203 in full: the block whose first line begins with the bold `**Precedence.**` run-in and whose last line reads `both.`, sitting between the `typst_authors` code-block example and the `.. note::` directive at line 205. Write exactly this in its place (per D-11, whose obligation is that this file publish the precedence the code implements, and per D-05, whose ordering it describes):

    **Precedence.** One question decides this, and it is not about the
    template route: **does the active parameter mapping send some Sphinx
    metadata key to the template parameter named** ``authors``? typsphinx
    seeds ``authors`` from ``typst_authors`` first and then applies the
    mapping, so whatever the mapping writes into ``authors`` replaces that
    seed -- and nothing else in typsphinx replaces it.

    The default mapping sends ``author`` to ``authors``, so on an ordinary
    build the entry's own author element (the fourth position in
    `Typst Documents`_ above) -- or the ``author`` fallback it resolves to
    -- wins.

    ``typst_authors`` therefore survives as the sole source of ``authors``
    if and only if no entry of the active mapping targets ``authors`` with
    a source key the build actually supplies. It is the *target* key that
    decides, not whether ``"author"`` appears in the mapping at all:

    * A mapping that sends ``"author"`` somewhere other than ``authors``
      -- for example ``typst_template_mapping = {"author": "doc_authors"}``,
      for a custom template whose function names its author parameter
      differently -- leaves ``authors`` alone. Both values are then passed:
      ``typst_authors`` as ``authors``, and the entry's author value as
      ``doc_authors``.
    * A mapping that sends some other key to ``authors`` -- for example
      ``typst_template_mapping = {"project": "authors"}`` -- replaces the
      seed even though it never mentions ``"author"``. What lands in
      ``authors`` is then the project name, in the same shape an author
      value would take.

    A ``typst_package`` build with ``typst_template_mapping`` not set at
    all reaches the surviving case by a side door rather than by a rule of
    its own: typsphinx then passes only what was explicitly mapped, and
    nothing was, so the mapping is empty and can target nothing.
    Package-based and template-based builds -- including the bundled
    default template -- are governed by the one rule above;
    ``typst_package`` affects only which mapping is in force, never
    whether the seed is replaced once a mapping is in force.

    ``typst_template_function``'s dict-form ``params`` take precedence over
    both.

Keep a blank line before and after the bulleted list so docutils parses it as a list rather than folding it into the paragraph. Do not touch the `.. note::` block that follows (the forward-removal notice is D-11's other half and is correct). Do not use `44.2-REVIEW.md`'s suggested wording or `44.2-VERIFICATION.md`'s `missing:` phrasing — `<measured_grounding>` §3 records the row that falsifies the first and the coverage the second does not reach.

Do not touch `tests/test_entry_metadata_precedence.py` in this task; its named cells are task 2's.
  </action>

  <verify>
    <automated>
uv run python -m pytest tests/test_params_authors_writers.py -q &&
uv run python -m pytest tests/test_params_authors_writers.py -q -k authors_seed_survival_matrix &&
! grep -qF 'exactly the two configurations' docs/source/user_guide/configuration.rst &&
uv run python -c 'import re,sys;t=re.sub(r"\s+"," ",open(sys.argv[1],encoding="utf-8").read());[sys.exit("MISSING: "+n) for n in sys.argv[2:] if n not in t]' docs/source/user_guide/configuration.rst 'no entry of the active mapping targets ``authors``' '{"author": "doc_authors"}' '{"project": "authors"}' &&
! awk '/^    def map_parameters\(/{f=1} /^    def generate_package_import\(/{f=0} f' typsphinx/template_engine.py | grep -qF 'exactly two config shapes' &&
awk '/^    def map_parameters\(/{f=1} /^    def generate_package_import\(/{f=0} f' typsphinx/template_engine.py | grep -qF 'target key "authors"' &&
awk '/^    def map_parameters\(/{f=1} /^    def generate_package_import\(/{f=0} f' typsphinx/template_engine.py | grep -qF '{"project": "authors"}' &&
test "$(grep -c 'package-alone' typsphinx/template_engine.py)" = "1" &&
uv run python -c "import ast,subprocess;o=subprocess.check_output(['git','show','6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c:typsphinx/template_engine.py']).decode();n=open('typsphinx/template_engine.py',encoding='utf-8').read();assert ast.dump(ast.parse(o))==ast.dump(ast.parse(n)),'executable code changed by a comment-only task'"
    </automated>
  </verify>

  <acceptance_criteria>
    - `uv run python -m pytest tests/test_params_authors_writers.py -q` passes with zero failures, and the collected node count is at least 24 (7 test functions, one of them parametrized into 18 cells).
    - `uv run python -m pytest tests/test_params_authors_writers.py --collect-only -q` lists parametrized ids that include a cell whose source key is `project` and whose target key is `authors`, and a cell whose source key is `author` and whose target key is `writer` — the two shapes with no prior coverage anywhere in the suite.
    - `tests/test_params_authors_writers.py` defines a module-level `_seed_survives` whose body contains no call to `map_parameters`, `TemplateEngine`, or any other production entry point — verified by reading the function.
    - `test_map_parameters_writes_params_from_exactly_these_sites` asserts the literal-slice list equals `["authors", "title", "authors", "date"]` and the dynamic-slice set equals `{"template_key", "key"}`, and its assertion message names `configuration.rst`, the D-05 comment and `_seed_survives` as the things to re-derive.
    - NEGATIVE: `grep -c 'exactly the two configurations' docs/source/user_guide/configuration.rst` is `0`.
    - POSITIVE (whitespace-normalized, so a re-wrap cannot fool it): `docs/source/user_guide/configuration.rst` contains all three of `no entry of the active mapping targets ``authors```, `{"author": "doc_authors"}`, `{"project": "authors"}`. Verified RED against `<GAP_BASE>` by the same command before the edit.
    - NEGATIVE, region-scoped (file-wide would be wrong — line 367's use is accurate and about template-file routing): the `map_parameters()` region extracted by `awk '/^    def map_parameters\(/{f=1} /^    def generate_package_import\(/{f=0} f' typsphinx/template_engine.py` contains no occurrence of `exactly two config shapes`.
    - POSITIVE, same region: it contains `target key "authors"` and `{"project": "authors"}`.
    - CONTROL, whole file: `grep -c 'package-alone' typsphinx/template_engine.py` is `1` — the region gate removed nothing from `uses_bundled_default_template()`'s accurate docstring.
    - `ast.dump(ast.parse(...))` of the working-tree `typsphinx/template_engine.py` equals that of `git show <GAP_BASE>:typsphinx/template_engine.py` — the production edit changed comments only.
    - The verbatim pre-fix `grep -n` output against `<GAP_BASE>` for both production sites, and the classified occurrence sweep, are captured and available for task 3.
    - `git status --porcelain` shows no modification to `tests/test_entry_metadata_precedence.py` from this task.
  </acceptance_criteria>

  <done>
The target-key rule exists on one wired path end to end: enumerated from the assignment target in
an executable AST test, generated into a row table that includes both previously-uncovered shapes,
stated in the D-05 comment that explains the mechanism, and published in the paragraph a user
configures against — with the production diff proven comment-only and the accurate package-routing
docstring elsewhere in the same file intact.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand — four named Group 2 cells for the uncovered rows, and the five remaining text sites including the CONF-10 entry</name>

  <files>tests/test_entry_metadata_precedence.py, tests/test_template_engine.py, docs/source/user_guide/configuration.rst, .planning/REQUIREMENTS.md</files>

  <read_first>
    - `typsphinx/template_engine.py` lines 418-522 as they now stand — re-read the mapping loop and its `_convert_to_authors_tuple` special case before writing each cell's expectation. Every assertion below must be derived from the code, not transcribed from this plan's table.
    - `tests/test_entry_metadata_precedence.py` lines 1-35 (module docstring), 183-196 (Group 2 banner), and 197-350 (all six existing Group 2 cells) — for the module's discrete-function-per-cell convention, its assertion ordering style, and the two cell docstrings being replaced.
    - `tests/test_config_template_mapping.py` — READ ONLY, the whole `typst_template_mapping` section. It already exercises `{"author": "doc_authors"}` and `{"author": "doc_author"}` as realistic custom-template configurations (Requirement 8.4, "different parameter names"); this is why the new rows are realistic user configurations rather than invented edge cases. Do not edit it.
    - `tests/test_template_engine.py` lines 910-1005 — `test_typst_authors_through_pipeline_produces_native_array_of_dicts` (only its docstring's third paragraph changes; the body stays byte-identical) and the test immediately after it, so the edit stops at the right boundary.
    - `docs/source/user_guide/configuration.rst` lines 34-66 — the numbered five-element contract. Only item 4 changes; item 5's accepted-and-ignored statement is plan 03's verified SC#5 deliverable and must not move.
    - `.planning/REQUIREMENTS.md` lines 183-212 — the `CONF-10` Future Requirement entry authored by this phase under D-06, and the `### Open todos not scoped here` heading that bounds it. Also read lines 93-104 (`CONF-09`) and the `## Traceability` table so you can see exactly what must NOT change.
    - `tests/test_entry_metadata_route_uniformity.py` lines 1-32 and 165-175 — READ ONLY. Its package-route sentences describe route A4's own empty-mapping behaviour, and its line 172 only names where the precedence matrix lives. Neither is an instance of this defect. Confirm that yourself, then leave the file untouched.
  </read_first>

  <behavior>
    Four more characterization cells, one per previously-uncovered row, each passing against
    unmodified production code. Each pins one clause of the rule task 1 published, in the sibling
    module's discrete named-function convention (D-05).

    - `test_map_parameters_author_mapped_to_non_authors_target_keeps_typst_authors`: engine with
      `parameter_mapping={"project": "title", "author": "doc_authors"}`, `typst_authors` set, no
      package, fed `BASE_METADATA`. Expect `params["authors"]` to be the `typst_authors` list of
      dicts AND `params["doc_authors"] == ("Explicit Entry Author",)` — both land simultaneously.
      This is the row `44.2-REVIEW.md` WR-01 reproduced.
    - `test_map_parameters_author_mapped_to_arbitrary_target_keeps_typst_authors`: same, with
      `"author"` mapped to `"writer"`. Expect the seed to survive and `params["writer"]` to be
      present. Proves the survival is not an artifact of `"doc_authors"`' tuple-conversion special
      case.
    - `test_map_parameters_non_author_source_mapped_to_authors_destroys_typst_authors`: engine with
      `parameter_mapping={"project": "authors"}` — no `"author"` key at all — and `typst_authors`
      set. Expect `params["authors"] == ("P",)`: the project name, not any author value, and not
      the seed. This is the row that falsifies both currently-open suggested fixes.
    - `test_map_parameters_release_mapped_to_authors_destroys_typst_authors`: engine with
      `parameter_mapping={"release": "authors", "project": "title"}` and `typst_authors` set.
      Expect `params["authors"] == ("1.0",)`. A second non-author source, inside a mapping that
      also maps something else, so the previous cell's result cannot be read as an artifact of a
      single-entry mapping.
  </behavior>

  <action>
**(1) `tests/test_entry_metadata_precedence.py` — four more Group 2 cells**, appended after `test_map_parameters_author_absent_from_metadata_keeps_typst_authors` and before the `Group 3` banner comment, in the module's existing shape (module-level `def`, one cell per function, a docstring naming the decision and the row, no `@pytest.mark.parametrize` — see `44.2-PATTERNS.md`). All four use `typst_authors={"Jane Doe": {"organization": "MIT", "email": "jane@mit.edu"}}` and `sphinx_metadata={"project": "P", "author": "Explicit Entry Author", "release": "1.0"}`, matching the cells above them.

`test_map_parameters_author_mapped_to_non_authors_target_keeps_typst_authors` — `parameter_mapping={"project": "title", "author": "doc_authors"}`, no package. Assert in this order: `engine.parameter_mapping["author"] == "doc_authors"`; `isinstance(params["authors"], list)`; `params["authors"][0]["name"] == "Jane Doe"`; `params["doc_authors"] == ("Explicit Entry Author",)`. Its docstring must state that the decisive thing is the mapping's TARGET key, that `"author"` being an active source key is neither necessary nor sufficient, that BOTH values land in `params` simultaneously, and that `tests/test_config_template_mapping.py` already exercises this mapping as a realistic custom-template configuration — only the combination with `typst_authors` had never been tested, which is why a published rule keyed on the source key could ship twice without any assertion going red.

`test_map_parameters_author_mapped_to_arbitrary_target_keeps_typst_authors` — `parameter_mapping={"project": "title", "author": "writer"}`, no package. Assert `isinstance(params["authors"], list)`; `params["authors"][0]["name"] == "Jane Doe"`; `"writer" in params`. Its docstring must say this is the companion control for the cell above: `"doc_authors"` is special-cased by the mapping loop's author-tuple conversion and `"writer"` is not, so surviving cannot be an artifact of that special case.

`test_map_parameters_non_author_source_mapped_to_authors_destroys_typst_authors` — `parameter_mapping={"project": "authors"}`, no package. Assert `"author" not in engine.parameter_mapping`; then `params["authors"] == ("P",)`; then `params["authors"] != [{"name": "Jane Doe", "organization": "MIT", "email": "jane@mit.edu"}]`. Its docstring must state that this is the row that falsifies both currently-open suggested fixes — the mapping mentions `"author"` nowhere, yet the seed is destroyed, and what lands in `authors` is the project name rather than any author value — and that a template declaring an `authors` parameter would therefore render the project name as its author.

`test_map_parameters_release_mapped_to_authors_destroys_typst_authors` — `parameter_mapping={"release": "authors", "project": "title"}`, no package. Assert `"author" not in engine.parameter_mapping`; then `params["authors"] == ("1.0",)`; then `params["title"] == "P"`. Its docstring must say it is a second non-author source inside a mapping that also maps something else, so the previous cell's result cannot be read as an artifact of a single-entry mapping.

**(2) `tests/test_entry_metadata_precedence.py` — the module docstring's Group 2 bullet (lines 14-20).** Replace those seven lines with exactly this, keeping the surrounding two-space list indentation:

    - Precedence inside `map_parameters()` (D-05): the `typst_authors` seed
      at `params["authors"]` survives if and only if no entry of the active
      `parameter_mapping` has the target key `"authors"` with its source key
      present in the passed `sphinx_metadata`. The decisive thing is the
      mapping's TARGET, not its source key and not the template route --
      `{"author": "doc_authors"}` keeps the seed and passes the mapped value
      under `doc_authors`, while `{"project": "authors"}` destroys the seed
      with the project name despite never mentioning `"author"`. One cell
      below per row; the writer enumeration this rule is derived from lives
      in `tests/test_params_authors_writers.py`.

**(3) `tests/test_entry_metadata_precedence.py` — the Group 2 banner (lines 184-194).** Replace that comment block, including both `# ---` rule lines, with exactly this:

    # ---------------------------------------------------------------------------
    # Group 2: precedence inside TemplateEngine.map_parameters() (D-05).
    #
    # The full condition, stated over the ASSIGNMENT TARGET: the typst_authors
    # seed at params["authors"] survives iff no entry of the active
    # parameter_mapping has the target key "authors" with its source key
    # present in the passed sphinx_metadata. The mapping loop's
    # params[template_key] assignment is the only site that can overwrite the
    # seed; the non-package back-fill is guarded by "authors" not in params,
    # and self.typst_package is never tested. One cell below per row of that
    # rule, including a source key routed AWAY from "authors" and a non-author
    # source key routed INTO it.
    # ---------------------------------------------------------------------------

**(4) `tests/test_entry_metadata_precedence.py` — two existing cell docstrings.** Replace the docstring of `test_map_parameters_custom_mapping_with_author_beats_typst_authors` (lines 303-307) with exactly:

    """D-05: the control proving the overwrite fires on the mapping's
    TARGET key being "authors", not on the mapping being
    DEFAULT_PARAMETER_MAPPING. A CUSTOM mapping that routes "author" to
    "authors" still overwrites the typst_authors seed, with no package
    involved at all. Read against
    test_map_parameters_author_mapped_to_non_authors_target_keeps_typst_authors
    below, which changes only the target key and flips the outcome."""

Replace the docstring of `test_map_parameters_author_absent_from_metadata_keeps_typst_authors` (lines 326-333) with exactly:

    """D-05: pins the source-presence half of the rule -- the mapping loop
    can only overwrite a key whose source it can read. A default-mapping
    engine (whose "author" entry does target "authors") still keeps the
    typst_authors seed when the passed sphinx_metadata carries no
    "author" key at all. This shape is not reachable from a real
    sphinx-build -- writer.py always supplies "author" via
    _resolve_entry_element -- but map_parameters() is a public method and
    the D-05 comment states this half of the condition, so it gets a test
    pinning the public contract."""

Leave every other docstring and every assertion in this module byte-identical.

**(5) `tests/test_template_engine.py` — the third paragraph of `test_typst_authors_through_pipeline_produces_native_array_of_dicts`'s docstring (lines 941-951).** Replace only that paragraph; leave the first two paragraphs and every line of the test body byte-identical (the engine construction, the metadata dict, all six assertions, and the double-formatting regression guard). Write exactly:

        typst_authors remains authoritative exactly when no entry of the
        active `parameter_mapping` targets the template key "authors"
        with a source key present in the passed metadata -- the decisive
        thing is the mapping's TARGET key, not its source key and not the
        presence of a package. This test exercises one such shape (an
        explicit mapping that targets nothing but "title", here alongside
        a package, the same shape
        tests/fixtures/package_only_config_gate/conf.py carries), so it
        still pins what it exists to pin: typst_authors reaches render()
        as a native array of dictionaries and never as a quoted string
        (the double-formatting regression guard below is unchanged). The
        full matrix lives in tests/test_entry_metadata_precedence.py
        Group 2 and tests/test_params_authors_writers.py.

**(6) `docs/source/user_guide/configuration.rst` — item 4 of the five-element contract (lines 49-58).** Replace that bullet with exactly this, keeping the file's three-space list continuation indent (per D-11):

    4. **Author** -- resolved the same way as the title: a present value wins
       (including ``""``); an absent, ``None``, or non-``str`` value falls back
       to ``author``, with a build warning for the non-``str`` case. Whether
       this value also beats ``typst_authors`` is decided by one thing only --
       whether the active parameter mapping sends ``"author"`` to the template
       parameter named ``authors``, which is what the default mapping does.
       `Author Information`_ below states the full rule, including the two
       cases this summary does not cover: a mapping that sends ``"author"``
       to a different parameter, and a mapping that sends a different key to
       ``authors``. ``typst_template_function``'s dict-form ``params`` take
       precedence over *both* -- a user who has named both the template
       function and its arguments has already made a more specific decision
       than either.

Leave items 1, 2, 3 and 5 untouched.

**(7) `.planning/REQUIREMENTS.md` — the `CONF-10` Future Requirement entry only.** This is the fifth defect site, outside plan 04's four-file scope and outside its occurrence census, and it still carries the ORIGINAL round-1 framing. It is in scope here because this phase authored that entry under D-06; the fence is that the edit must be confined to the `CONF-10` bullet. Replace the sentence run that begins `Phase 44.2 already narrowed the setting's own scope:` (mid line 191) and ends with the clause immediately before `The removal must touch:` (mid line 199) with the following prose, re-wrapped to the file's existing ~100-column width and two-space continuation indent so the bullet stays one flowing paragraph:

    Phase 44.2 already narrowed the setting's own scope: after its D-05 reorder, the `typst_authors`
    seed at `params["authors"]` survives if and only if no entry of the active `parameter_mapping`
    has the target key `"authors"` with its source key present in the passed `sphinx_metadata` --
    the mapping's TARGET decides, not its source key and not the template route. Two in-repo
    configurations reach the surviving case for different reasons: the fixture that deliberately
    sets a custom mapping targeting only `"title"` (`44.2-01-SUMMARY.md`), and
    `examples/charged-ieee/approach1/conf.py`, which sets `typst_package` with
    `typst_template_mapping` left completely unset, so `TemplateEngine.__init__` resolves the
    mapping to an *empty* dict that can target nothing (`44.2-GATE-EVIDENCE-03.md` § 4-5). Two
    further shapes the earlier wording mispredicted are now pinned by named tests: a mapping that
    routes `"author"` to some other template parameter keeps the seed and passes both values, and a
    mapping that routes a non-author key into `"authors"` destroys the seed with a non-author value
    (`44.2-GATE-EVIDENCE-05.md`).

Do not change `CONF-09`'s `- [ ]` checkbox, its text, the `## Traceability` table, or any other line of the file. `phase.complete` has a recorded habit of flipping such rows early on this project; a plan doing it by hand would make the phase look sealed while a gap is open.

Do not edit `tests/test_entry_metadata_route_uniformity.py`, `tests/test_package_only_config_gate.py`, `tests/test_package_template_routing.py`, `tests/test_examples_charged_ieee_gate.py`, `tests/test_config_template_mapping.py`, either fixture `conf.py`, `typsphinx/writer.py`, `typsphinx/builder.py`, or `docs/source/user_guide/templates.rst` — see `<measured_grounding>` §4.
  </action>

  <verify>
    <automated>
uv run python -m pytest tests/test_entry_metadata_precedence.py tests/test_template_engine.py tests/test_params_authors_writers.py -q &&
uv run python -m pytest tests/test_entry_metadata_precedence.py -q --collect-only | grep -c 'test_map_parameters' | grep -qx 10 &&
! grep -nE 'key AND|SECOND conjunct|trigger is the presence of the|survives on BOTH' tests/test_entry_metadata_precedence.py &&
! grep -qF 'has no "author" key' tests/test_template_engine.py &&
! grep -qF 'configurations where it does not' docs/source/user_guide/configuration.rst &&
uv run python -c 'import re,sys;t=re.sub(r"\s+"," ",open(sys.argv[1],encoding="utf-8").read());[sys.exit("MISSING: "+n) for n in sys.argv[2:] if n not in t]' docs/source/user_guide/configuration.rst 'the two cases this summary does not cover' &&
! grep -qF 'remains authoritative only on a' .planning/REQUIREMENTS.md &&
! grep -qF 'package-alone' .planning/REQUIREMENTS.md &&
uv run python -c 'import re,sys;t=re.sub(r"\s+"," ",open(sys.argv[1],encoding="utf-8").read());[sys.exit("MISSING: "+n) for n in sys.argv[2:] if n not in t]' .planning/REQUIREMENTS.md "the mapping's TARGET decides" 'no entry of the active `parameter_mapping`' &&
test "$(git diff 6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c -- .planning/REQUIREMENTS.md | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -c 'CONF-09')" = "0" &&
uv run python -c "import re,subprocess;d=subprocess.check_output(['git','diff','-U0','6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c','--','.planning/REQUIREMENTS.md']).decode();src=open('.planning/REQUIREMENTS.md',encoding='utf-8').read().splitlines();lo=next(i for i,l in enumerate(src,1) if l.startswith('- **CONF-10**'));hi=next(i for i,l in enumerate(src,1) if i>lo and (l.startswith('### ') or l.startswith('## ')));bad=[m.group(1) for m in re.finditer(r'^@@ -\d+(?:,\d+)? \+(\d+)',d,re.M) if not (lo-1)<=int(m.group(1))<hi];assert not bad, 'REQUIREMENTS.md diff touches lines outside the CONF-10 entry: '+repr(bad)" &&
test "$(grep -c 'package-alone' tests/test_entry_metadata_route_uniformity.py)" = "5" &&
test -z "$(git diff --name-only 6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c -- tests/test_entry_metadata_route_uniformity.py tests/test_config_template_mapping.py tests/test_package_only_config_gate.py docs/source/user_guide/templates.rst typsphinx/writer.py typsphinx/builder.py)" &&
uv run python -c "import subprocess;o=subprocess.check_output(['git','show','6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c:tests/test_template_engine.py']).decode();n=open('tests/test_template_engine.py',encoding='utf-8').read();f=lambda s:[l for l in s.splitlines() if l.strip().startswith(('def ','assert ','engine','params','result','body','sphinx_metadata','formatted','authors'))];assert f(o)==f(n),'a non-docstring line changed in test_template_engine.py'"
    </automated>
  </verify>

  <acceptance_criteria>
    - `uv run python -m pytest tests/test_entry_metadata_precedence.py -q` reports 25 passed (21 pre-existing plus the 4 added here) and zero failures.
    - `uv run python -m pytest tests/test_entry_metadata_precedence.py --collect-only -q` lists exactly 10 node ids containing `test_map_parameters`.
    - All four new functions exist at module level with the exact names `test_map_parameters_author_mapped_to_non_authors_target_keeps_typst_authors`, `test_map_parameters_author_mapped_to_arbitrary_target_keeps_typst_authors`, `test_map_parameters_non_author_source_mapped_to_authors_destroys_typst_authors`, `test_map_parameters_release_mapped_to_authors_destroys_typst_authors`.
    - The first asserts BOTH `params["authors"][0]["name"] == "Jane Doe"` AND `params["doc_authors"] == ("Explicit Entry Author",)` in the same test body — the "two values land simultaneously" shape.
    - The third asserts `"author" not in engine.parameter_mapping` AND `params["authors"] == ("P",)`; the fourth asserts `"author" not in engine.parameter_mapping` AND `params["authors"] == ("1.0",)`.
    - NEGATIVE: `grep -nE 'key AND|SECOND conjunct|trigger is the presence of the|survives on BOTH' tests/test_entry_metadata_precedence.py` returns nothing.
    - NEGATIVE: `grep -c 'has no "author" key' tests/test_template_engine.py` is `0`; `grep -c 'configurations where it does not' docs/source/user_guide/configuration.rst` is `0`.
    - POSITIVE (whitespace-normalized): `docs/source/user_guide/configuration.rst` contains `the two cases this summary does not cover`.
    - NEGATIVE: `grep -c 'remains authoritative only on a' .planning/REQUIREMENTS.md` is `0` and `grep -c 'package-alone' .planning/REQUIREMENTS.md` is `0`.
    - POSITIVE (whitespace-normalized): `.planning/REQUIREMENTS.md` contains both `the mapping's TARGET decides` and ``no entry of the active `parameter_mapping```.
    - FENCE: every hunk in `git diff -U0 <GAP_BASE> -- .planning/REQUIREMENTS.md` starts inside the `CONF-10` bullet's line span, and no added or removed line in that diff contains `CONF-09`. `CONF-09`'s `- [ ]` checkbox and the `## Traceability` table are byte-identical to `<GAP_BASE>`.
    - CONTROL: `grep -c 'package-alone' tests/test_entry_metadata_route_uniformity.py` is still exactly `5`, and `git diff --name-only <GAP_BASE> --` for `tests/test_entry_metadata_route_uniformity.py`, `tests/test_config_template_mapping.py`, `tests/test_package_only_config_gate.py`, `docs/source/user_guide/templates.rst`, `typsphinx/writer.py`, `typsphinx/builder.py` is empty.
    - Every `def`/`assert`/statement line of `tests/test_template_engine.py` is byte-identical to `<GAP_BASE>` — only docstring lines changed there.
    - No pre-existing assertion was deleted, renamed, `xfail`-ed or `skip`-ped in any module; the collected count of `tests/test_entry_metadata_precedence.py` and `tests/test_template_engine.py` each went up or stayed the same, never down.
  </acceptance_criteria>

  <done>
Every row of the corrected rule has a named, passing cell in the sibling module's own convention —
including the two shapes that had zero coverage anywhere in the suite and the two that falsify the
open suggested fixes — and all five remaining text sites, including the `CONF-10` Future
Requirement entry authored under D-06, state the rule over the assignment target, with the accurate
package-routing sentences elsewhere in the repo provably untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the correction with a falsification-graded enumeration, and re-take the phase gate</name>

  <files>.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md</files>

  <read_first>
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-04.md` — read §1 and §4 in full first. §1's probe constructed six shapes, none of them falsifying; §4 then graded all 17 sentences ACCURATE against that probe and shipped the defect. This file is the specification of what NOT to reproduce: an enumeration is worthless if its authority is a row set derived from the prose being graded.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md` sections 0-2 — the established evidence-file shape for this phase: a worktree-isolation proof first, then command/output pairs with verbatim output and no transcribed figures.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md` — the `gaps:` block this file discharges (both failed truths, one root cause) and the SC#6 command set the phase is measured with.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-REVIEW.md` WR-01 — for §5's record of why its literal suggested sentence was not used.
    - `docs/source/user_guide/configuration.rst`, `typsphinx/template_engine.py`, `tests/test_entry_metadata_precedence.py`, `tests/test_template_engine.py`, `tests/test_params_authors_writers.py` and `.planning/REQUIREMENTS.md` as they now stand — §3's enumeration must be produced by reading all six, not by recalling what tasks 1-2 intended to write.
  </read_first>

  <action>
Before changing anything, record the pre-change baseline for this session so an environmental failure cannot be mistaken for a regression: run the full `uv run python -m pytest -q` against `git stash`-clean state or, if that is impractical, against `<GAP_BASE>` in a scratch checkout, and note the counts. The orchestrator measured 904 passed / 1 skipped / 0 failed at `<GAP_BASE>` with `black`/`ruff`/`mypy` clean; this project's NixOS sandbox is known to fail ~45 integration tests unless the ELF workaround from `44.2-GATE-EVIDENCE-04.md` §0 is applied, so compare against the baseline you measured, never against an assumption that any failure is yours.

Create `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md`. Do NOT name it `44.2-VERIFICATION.md` — that name is owned and overwritten wholesale by `/gsd-verify-phase`. Every command output must be pasted verbatim from a command run in this task's own session; transcribe no figure from this plan, from `44.2-RESEARCH.md`, or from `44.2-VERIFICATION.md`.

Sections, in order:

§0 Worktree isolation and provisioning — the resolved `typsphinx.__file__` path, the sync command used, any NixOS ELF workaround applied, the pre-change full-suite baseline counts, and `GAP_BASE` with the output of `git merge-base --is-ancestor` proving it is an ancestor of `HEAD` (or the substituted value and why).

§1 Writer enumeration, from the assignment target — paste the verbatim output of an `ast`-based probe over `TemplateEngine.map_parameters()` that lists every `Assign` whose target is a `Subscript` on `params`, with its slice and line number, plus `sorted(ELEMENTS_ALLOWLIST)` and the `ExtensionError` raised by an unknown `typst_elements` key. State which of those sites can reach `params["authors"]` and under exactly what guard. **This section, not any config-shape list, is the authority for §2 and §3.**

§2 The generated row table — a runnable `uv run python` probe whose rows are the CROSS PRODUCT of the source keys present in a realistic `sphinx_metadata` and the target keys `authors` / `doc_authors` / `writer`, times source-present and source-absent, with `typst_authors` set. Print for each row: the mapping, the resolved `parameter_mapping`, `params["authors"]`, whether the seed survived, and any other key the mapping wrote. Paste the verbatim output. The row set must be **generated by the loop, not typed by hand** — that is the whole point, and typing it by hand is how the falsifying row was omitted twice. Confirm the table contains a row whose source is not `"author"` and whose target is `"authors"`, and a row whose source is `"author"` and whose target is not `"authors"`.

§3 Every published sentence, enumerated and falsification-graded — walk all six touched files plus any additional site task 1's sweep found, and list EVERY sentence that constrains when `params["authors"]` is written, each with the file, its line number, the sentence quoted, a verdict of ACCURATE or NARROWER-THAN-CODE against §2, **and a `FALSIFY-ROW:` column naming the §2 row that was the closest attempt to falsify that sentence, with the observed value that shows it does not.** A verdict with no named falsification row is not permitted and must be treated as an unfinished row, not as a pass. For any sentence making a universally quantified or exhaustive claim, the falsification row must be one whose shape the sentence's own wording would predict wrongly if the sentence were one predicate short. If any sentence comes out NARROWER-THAN-CODE, fix it and re-run §2 and §3 rather than recording it as an accepted defect.

§4 Clause-to-test traceability — one row per clause of the published rule (the mapping's target must be `"authors"`; the source key must be present; a source routed away from `"authors"` keeps the seed AND passes its own key; a non-author source routed into `"authors"` destroys the seed; `typst_package` affects only which mapping is in force; the back-fill cannot replace a seed; `typst_elements` cannot reach the key), each naming the exact test function that pins it and the verbatim `pytest -q` line proving it passes. This table is what SC#2's "each rule has a test" now rests on.

§5 Why neither open suggested fix was transcribed — quote `44.2-REVIEW.md` WR-01's proposed sentence and `44.2-VERIFICATION.md`'s `missing:` bullet 1, name the §2 row that falsifies WR-01, and show that row's observed value. Record that both findings were correct and are closed here; only their wordings were one predicate short. Also record, in one paragraph, the methodological through-line: rounds 1 and 2 both derived their search set from the prose being corrected, and this round derived it from the assignment target instead.

§6 Scope-fence proofs — the `git diff <GAP_BASE> -- typsphinx/` output in full with the assertion that every `+`/`-` line begins with `#` and the `ast.parse` equality result; the `git diff -U0 <GAP_BASE> -- .planning/REQUIREMENTS.md` hunk headers with the `CONF-10` line span showing containment, plus proof that no diff line names `CONF-09`; and `git diff --name-only <GAP_BASE>` showing that none of `44.2-01/02/03/04-PLAN.md`, any `-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, or `44.2-GATE-EVIDENCE-01/02/03/04.md` appears.

§7 SC#6 re-take — verbatim output of the full `uv run python -m pytest -q` run, `black --check .`, `ruff check .`, `mypy typsphinx/` (invoked as `uv run python -m <tool>` if a compiled launcher fails to exec), and a docs HTML build, with the grep proving no WARNING or ERROR line names `configuration.rst`. Report the new full-suite count as measured and state the delta against both the 904 the orchestrator measured at `<GAP_BASE>` and the pre-change baseline recorded in §0.

Close with a one-paragraph discharge statement naming the two failed truths from `44.2-VERIFICATION.md`, the seven corrected sites, and the rows now pinned.

Do not touch `.planning/REQUIREMENTS.md` in this task — task 2 owns its only permitted edit. Do not edit any `44.2-0[1234]-PLAN.md`, any `-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, or `44.2-GATE-EVIDENCE-01/02/03/04.md`.
  </action>

  <verify>
    <automated>
uv run python -m pytest -q &&
uv run python -m black --check . &&
uv run python -m ruff check . &&
uv run python -m mypy typsphinx/ &&
test -f .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md &&
grep -qF 'FALSIFY-ROW' .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md &&
! grep -qiE 'NARROWER-THAN-CODE[^|]*\| *open' .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md &&
test -z "$(git diff --name-only 6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c -- .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-REVIEW.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-01.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-02.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-04.md)" &&
test -z "$(git diff --name-only 6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c -- .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-01-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-02-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-03-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-04-PLAN.md)" &&
! (git diff 6b2ecfba61597c89bdf6e0f12d1ccd58a17e9e3c -- typsphinx/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-][[:space:]]*#' | grep -q .) &&
DOCS_OUT="$(uv run --extra dev --extra docs python -m sphinx -b html docs/sourc? "$(mktemp -d)" 2>&1)" &&
printf '%s' "$DOCS_OUT" | grep -q 'build succeeded' &&
! printf '%s' "$DOCS_OUT" | grep -E 'WARNING|ERROR' | grep -q 'configuration.rst'
    </automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md` exists and contains all eight headings §0-§7 described in the action.
    - §1 contains verbatim `ast` probe output naming every `params[...]` assignment site in `map_parameters()` with its slice, and `sorted(ELEMENTS_ALLOWLIST)`.
    - §2's row table is produced by a pasted, runnable loop; it contains at least one row whose source key is not `"author"` with target `"authors"`, and at least one row whose source key is `"author"` with a target other than `"authors"`; both rows' observed `params["authors"]` values are shown.
    - §3 enumerates at least one sentence per touched file, and EVERY row carries a file path, a line number, a quoted sentence, a verdict token of `ACCURATE` or `NARROWER-THAN-CODE`, and a non-empty `FALSIFY-ROW:` naming a §2 row. No row is left with an open `NARROWER-THAN-CODE` verdict and no row has an empty falsification column.
    - §4's table has one row per clause, and every row names a test function that appears in the combined `--collect-only -q` output of `tests/test_entry_metadata_precedence.py` and `tests/test_params_authors_writers.py`.
    - §5 quotes both open suggested fixes verbatim and names the §2 row that falsifies WR-01's, with that row's observed value.
    - `uv run python -m pytest -q` passes with zero failures; the reported passed count is recorded verbatim in §7 alongside the delta against both the 904 measured at `<GAP_BASE>` and the §0 pre-change baseline.
    - `black --check .`, `ruff check .` and `mypy typsphinx/` all exit `0`.
    - The docs HTML build prints `build succeeded`, and no line matching `WARNING` or `ERROR` contains `configuration.rst`.
    - `git diff --name-only <GAP_BASE>` includes NONE of `44.2-VERIFICATION.md`, `44.2-REVIEW.md`, `44.2-GATE-EVIDENCE-01/02/03/04.md`, `44.2-01/02/03/04-PLAN.md`, or any `-SUMMARY.md`.
    - `git diff <GAP_BASE> -- typsphinx/` contains no added or removed line that does not begin (after leading whitespace) with `#`.
    - No file named `44.2-VERIFICATION.md` was created or modified by this plan.
  </acceptance_criteria>

  <done>
The correction is recorded in a durably-named evidence file whose enumeration is graded by named
falsification attempts against a mechanically generated row table, the phase gate is green against
a baseline measured in the same session, the historical artifacts are untouched, and the production
diff is proven comment-only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs -> user's `conf.py` | `configuration.rst` is the authority a user configures against; a wrong rule here becomes a wrong configuration in every downstream project, with no error to signal it. A user who reads the round-2 rule and sets `typst_template_mapping = {"project": "authors"}` gets the project name rendered as the document's author |
| in-code comment -> future maintainer's change | the D-05 comment is the only in-repo explanation of the seed-then-overwrite ordering; a wrong condition invites a "simplifying" change that breaks a route |
| planning record -> future phase scoping | `.planning/REQUIREMENTS.md`'s `CONF-10` entry is the input to the removal phase; a wrong precedence claim there propagates into work that has not been planned yet |
| plan-authored artifact name -> `/gsd-verify-phase`'s reserved output | `44.2-VERIFICATION.md` is written by the verifier and clobbers same-named plan output |
| test expectation -> the code it tests | a matrix whose expected value is computed by calling the code under test is a tautology that would have passed for both already-falsified rules |

## STRIDE Threat Register (ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44.2.5-01 | Information Disclosure | `docs/source/user_guide/configuration.rst` precedence guidance | high | mitigate | Every published clause is derived from §1's writer enumeration and pinned in §4; §3 grades each sentence with a NAMED falsification row rather than a match against a prose-derived probe — the exact hole that let rounds 1 and 2 ship. Rated `high` (not `medium` as in plan 04) because this same boundary has now failed twice and the current wording actively mispredicts a documented, tested configuration shape |
| T-44.2.5-02 | Tampering | `typsphinx/template_engine.py` (a production file edited by a text-accuracy plan) | medium | mitigate | Task 1 asserts `ast.parse` equality against `<GAP_BASE>` and task 3 asserts every `+`/`-` line under `typsphinx/` is a comment line; a behaviour change cannot ride along |
| T-44.2.5-03 | Tampering | existing assertions in the touched test modules | medium | mitigate | Task 2 asserts the collected `test_map_parameters` node count rises to 10 and that every non-docstring line of `tests/test_template_engine.py` is byte-identical to `<GAP_BASE>`; a weakened, deleted or `xfail`-ed assertion fails the gate |
| T-44.2.5-04 | Tampering | `.planning/REQUIREMENTS.md` (`CONF-09`'s checkbox and the Traceability table) | medium | mitigate | Task 2 asserts every diff hunk starts inside the `CONF-10` bullet's line span and that no diff line names `CONF-09` — the recorded early-flip hazard cannot fire from this plan |
| T-44.2.5-05 | Repudiation | phase evidence trail (`GATE-EVIDENCE-01..04`, `REVIEW`, `VERIFICATION`, all four prior PLANs and SUMMARYs) | medium | mitigate | Task 3 asserts `git diff --name-only <GAP_BASE>` lists none of them; the correction is recorded FORWARD in `44.2-GATE-EVIDENCE-05.md`, never by amending the record of what was published |
| T-44.2.5-06 | Spoofing | the generated matrix's expected values | medium | mitigate | `_seed_survives()` transcribes the published sentence and is prohibited from calling any production entry point; the acceptance criteria require reading its body to confirm it contains no such call, so a tautological matrix cannot pass as evidence |
| T-44.2.5-07 | Denial of Service | docs HTML build (a malformed rst bullet list could break the published page) | low | mitigate | Task 3 runs a real `sphinx -b html` build and requires `build succeeded` with no WARNING/ERROR line naming `configuration.rst` |
| T-44.2.5-SC | Tampering | supply chain / package installs | low | accept | This plan installs nothing. `uv sync --extra dev --extra docs` resolves only already-pinned `pyproject.toml` extras; no dependency is added, removed or version-bumped, and `pyproject.toml`/`uv.lock` are absent from `files_modified`. No `## Package Legitimacy Audit` is required because there is no package-manager install task |

One threat is rated `high` (T-44.2.5-01) and is dispositioned `mitigate` with the mitigations named
above; nothing is left `accept`-ed at or above `security_block_on: high`.
</threat_model>

<verification>
Phase-level checks after all three tasks:

1. `uv run python -m pytest -q` — full suite green; count recorded against both the 904 measured at
   `<GAP_BASE>` and the session's own pre-change baseline.
2. `uv run python -m black --check .`, `uv run python -m ruff check .`,
   `uv run python -m mypy typsphinx/` — all clean.
3. Docs HTML build succeeds with no WARNING/ERROR naming `configuration.rst`.
4. `uv run python -m pytest tests/test_entry_metadata_precedence.py --collect-only -q` lists 10
   `test_map_parameters` node ids; `tests/test_params_authors_writers.py` collects at least 24.
5. `grep -c 'package-alone'` is `0` in `.planning/REQUIREMENTS.md`; `1` in
   `typsphinx/template_engine.py` (the accurate `uses_bundled_default_template()` sentence); `5` in
   `tests/test_entry_metadata_route_uniformity.py` (accurate route-A4 sentences).
6. `git diff <GAP_BASE> -- typsphinx/` contains only comment-line changes.
7. Every hunk of `git diff -U0 <GAP_BASE> -- .planning/REQUIREMENTS.md` lies inside the `CONF-10`
   bullet; no diff line names `CONF-09`.
8. `44.2-GATE-EVIDENCE-05.md` exists, every §3 row carries a `FALSIFY-ROW:` entry, and no file named
   `44.2-VERIFICATION.md` was written by this plan.
</verification>

<success_criteria>
- The rule published in `docs/source/user_guide/configuration.rst`, stated in `typsphinx/template_engine.py`'s
  D-05 comment, restated in the two test modules' prose, and recorded in `.planning/REQUIREMENTS.md`'s
  `CONF-10` entry is the same rule, is stated over the assignment target `params["authors"]`, and is
  true on every measured row A-I — the plan 03/04 backstop must-have that has now failed twice.
- SC#2's "each rule has a test" holds for the rule as implemented: every clause is pinned by a named
  passing cell, including the four rows (`"author"` routed to `doc_authors` and to `writer`;
  `project` and `release` routed into `authors`) that had zero coverage anywhere in the suite.
- A rule one predicate short of the code fails in CI: `tests/test_params_authors_writers.py`
  generates its rows from the code's own axes and grades them against an independently written
  transcription of the published sentence, and an AST enumeration fails if a fourth writer of
  `params[...]` appears in `map_parameters()`.
- The production diff is comment-only, the `.planning/REQUIREMENTS.md` diff is confined to the
  `CONF-10` bullet, the phase's historical record is byte-identical, and the full SC#6 gate is green.
- `44.2-GATE-EVIDENCE-05.md` records the writer enumeration, the generated row table, a
  falsification-graded enumeration of every published sentence, the clause-to-test traceability, and
  why neither open suggested fix was transcribed.
</success_criteria>

<output>
Create `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-05-SUMMARY.md` when
done.
</output>
