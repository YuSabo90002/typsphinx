---
phase: 44-typst-documents-default-derivation-builder-input-hardening
plan: 03
type: execute
wave: 3
depends_on: [44-01, 44-02]
files_modified:
  - .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md
autonomous: true
requirements: [CONF-08]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The output-filename rename is measured, not assumed: `44-GATE-EVIDENCE-03.md` records, from two real builds of the same no-`typst_documents` project on two different trees, the exact pre-change emitted filename (`index.typ`) and the exact post-change pair (`quickstartdefaultgate.typ` plus `quickstartdefaultgate.pdf`), each with its byte size and the verbatim command that produced it (ROADMAP SC#4)."
    - "The two builds are proven to have run against genuinely different copies of typsphinx: each throwaway worktree's resolved `typsphinx.__file__` is recorded, both paths lie inside their own worktree, the two paths differ, and neither is the main checkout — without this the pre-change side would be built by post-change code through the main venv's editable finder and the whole record would be vacuous."
    - "The record covers BOTH halves of the user-visible change per D-05 — the filename rename AND the content change: the pre-change `index.typ` is `@preview` imports plus body with no template applied, the post-change `<slug>.typ` carries the shared-template import and the template function call, with the emitted head of each file pasted verbatim rather than described."
    - "The pre-change `-b typstpdf` build exited 0, wrote zero PDF files, and emitted exactly one typsphinx warning; the post-change `-b typstpdf` build exited 0, wrote one PDF, and emitted no such warning — both stated as recorded command output, not as claims."
    - "The measured difference is attributable to this phase alone: a pathspec-scoped `git diff --stat <PRE>..<POST> -- typsphinx/` recorded in the evidence file lists only `typsphinx/__init__.py` and `typsphinx/builder.py`."
    - statement: "The recorded pair is sufficient as Phase 46's CHANGELOG source text for the affected user — someone who never set `typst_documents` and who includes the old `index.typ` from their own Typst file — because the record states in quotable prose both what the emitted filename becomes and that the emitted file's structure changes from body-only to fully templated."
      verification: backstop
    - "Every throwaway worktree created for this measurement is removed afterwards, proven by a recorded `git worktree list` that names neither of them."
  prohibitions:
    - statement: "MUST NOT let the output-filename change and the output-content change reach a release unrecorded. A user who never set `typst_documents` gets a differently-named and differently-structured artifact after upgrading a patch release; shipping that without a measured record for the CHANGELOG would be exactly the silent user-facing change the owner accepted the cost of only on condition it be called out."
      category: transparency
    - statement: "MUST NOT paste into 44-GATE-EVIDENCE-03.md any figure recalled from a planning document as if it were measured here. `44-CONTEXT.md` already carries a 373-byte measurement and an 8209-byte measurement from a different project; this file records only what its own commands produced, and if a number differs from CONTEXT's the measured one wins and the divergence is stated."
      category: transparency
  artifacts:
    - path: ".planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md"
      provides: "The SC#4 measured before/after record — two named commits, two isolation proofs, four builds, both halves of the change, and the Phase 46 CHANGELOG source text"
      contains: "quickstartdefaultgate"
  key_links:
    - from: ".planning/phases/44-.../44-GATE-EVIDENCE-01.md"
      to: ".planning/phases/44-.../44-GATE-EVIDENCE-03.md"
      via: "the 40-hex RED commit SHA recorded by plan 44-01 is this plan's pre-change side"
      pattern: "RED commit"
    - from: ".planning/phases/44-.../44-GATE-EVIDENCE-03.md"
      to: "Phase 46 CHANGELOG"
      via: "a quotable source-text block naming the old filename, the new filename, and the structure change"
      pattern: "CHANGELOG source text"
---

<objective>
Measure the user-visible change CONF-08 causes, on two real trees, and hand the measured pair to
Phase 46 as CHANGELOG source text.

Purpose: the owner accepted the output-filename rename as a known cost of CONF-08 on the explicit
condition that it be called out in the CHANGELOG, and ROADMAP SC#4 requires it be **measured, not
assumed**. D-05 widens what must be recorded: the change is not only a rename. Before this phase, an
unset-config `-b typst` build emitted `index.typ` as `@preview` imports plus body with **no template
applied**, because `_is_master_document('index')` was False; after it, the derived entry makes
`root_doc` a master, so the emitted file is fully templated as well as renamed. A user who includes
the old `index.typ` from their own Typst file is affected by both halves.

Output: `44-GATE-EVIDENCE-03.md` — two named commits, a per-worktree isolation proof, four real
builds, the byte sizes and emitted heads of both `.typ` files, the PDF counts on both sides, a
pathspec-scoped production diff, and a quotable CHANGELOG source-text block.
</objective>

<probe_findings>
Spec-less probe fallback is active for this phase. All five edge items are accounted for in plans
44-01 (four authored into `must_haves.truths`) and 44-02 (one flagged assumption); this plan adds one
`backstop` truth of its own — the sufficiency of the recorded pair as Phase 46's source text is a
judgment a verifier cannot confirm from command output alone, so it is authored as a structured
`{ statement, verification: backstop }` marker and will abstain to `human_needed` rather than pass
silently if the verifier cannot confirm it.

**Prohibition probe.** Stage 1's transparency candidate for CONF-08 — that the rename plus content
change must not reach a release unrecorded — survives Stage 2 and is authored in
`must_haves.prohibitions` above. It is bespoke, not canon: no security or compliance tool owns
"a patch release silently renamed the user's build output".
</probe_findings>

<artifacts_this_phase_produces>
Every symbol Phase 44 creates. The plan-review-convergence source-grounding pass reads this list to
exclude newly-created symbols from drift verification. Items marked **[this plan]** are created here.

**New production symbols**
- None. This plan changes no code and no test. It creates one markdown evidence file **[this plan]**
  and creates and then removes two throwaway, untracked git worktrees.
- `typsphinx.builder._default_typst_documents(config)` (plan 44-01); the `isinstance(docname, str)`
  guard and the reworded opt-out message inside `TypstPDFBuilder.finish()` (plan 44-02)

**No new runtime dependency.** `pyproject.toml` and `uv.lock` are not modified by this phase.

**New test modules** (none in this plan)
- `tests/test_default_typst_documents_derivation.py`, `tests/test_default_typst_documents_gate.py`
  (plan 44-01); `tests/test_non_str_docname_gate.py`,
  `tests/test_empty_typst_documents_optout_gate.py` (plan 44-02)

**New fixture directories** (none in this plan)
- `tests/fixtures/default_typst_documents_gate/`,
  `tests/fixtures/explicit_typst_documents_wins_gate/` (plan 44-01);
  `tests/fixtures/non_str_docname_gate/`, `tests/fixtures/empty_typst_documents_optout_gate/`
  (plan 44-02)

**New evidence files** (all under the phase directory; never named `44-VERIFICATION.md`, which
`/gsd-verify-work` overwrites wholesale)
- `44-GATE-EVIDENCE-03.md` — SC#4 measured before/after record + Phase 46 CHANGELOG source text
  **[this plan]**
- `44-GATE-EVIDENCE-01.md` (plan 44-01), `-02.md` (plan 44-02), `-04.md` (plan 44-04)
</artifacts_this_phase_produces>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-CONTEXT.md
@.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-01.md
@.planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the pre-change side on a tree that does not contain this phase's change</name>
  <precondition>`44-GATE-EVIDENCE-01.md` records a 40-hex RED commit SHA under its `## 2. RED commit` heading, and `git show --name-only <that SHA>` lists `tests/fixtures/default_typst_documents_gate/conf.py` and no path beginning `typsphinx/`.</precondition>
  <files>.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md</files>
  <read_first>
    - `.planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md`
      §§ 1-3 (the two-build worktree-isolation method this task reuses: `git worktree add --detach`,
      per-worktree `uv sync` with `VIRTUAL_ENV`/`UV_PROJECT_ENVIRONMENT` unset, the `uv` shim, and
      the `typsphinx.__file__` isolation proof)
    - `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-01.md`
      §§ 1-2 (the pre-change measurement plan 44-01 already recorded, and the RED commit SHA this
      task consumes — read it, then RE-MEASURE rather than transcribe)
    - `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-CONTEXT.md`
      `<decisions>` D-05 and `<specifics>` §1 (what the record must cover, and the figures from a
      DIFFERENT project that must not be copied into this file)
    - `CLAUDE.md` § "Worktree-isolated execution" (why each throwaway worktree needs its own
      `uv sync` and why every command must go through `uv run`)
  </read_first>
  <action>
    **(a) Resolve and verify the two named commits.** Read the 40-hex RED commit SHA from
    `44-GATE-EVIDENCE-01.md` § "2. RED commit" — call it PRE. Take POST as this worktree's current
    `git rev-parse HEAD`. Before using them, verify each, and paste each command with its output into
    the evidence file:

    - `git log -1 --oneline <PRE>` and `git log -1 --oneline <POST>`, plus both full 40-hex SHAs.
    - `git show --name-only <PRE>` lists no path beginning `typsphinx/` — the pre-change side really
      predates every production change in this phase.
    - `git ls-tree -r --name-only <PRE> | grep default_typst_documents_gate` lists the fixture's
      `conf.py` and `index.rst` — the fixture exists on BOTH sides, so the same project is built
      twice. If it does not, stop and surface the problem rather than substituting another fixture.
    - In the current tree, `grep -c 'def _default_typst_documents' typsphinx/builder.py` is 1 and
      `grep -c 'isinstance(docname, str)' typsphinx/builder.py` is 1 — POST contains both waves'
      production changes.
    - `git diff --stat <PRE>..<POST> -- typsphinx/` lists only `typsphinx/__init__.py` and
      `typsphinx/builder.py`. Also record the unscoped `git diff --stat <PRE>..<POST>` and state in
      one sentence why it is larger (this phase's own tests, fixtures and evidence files landed
      between the two commits).

    **(b) Create and provision the pre-change worktree.** `git worktree add --detach <scratch>/pre-wt <PRE>`,
    then `(cd <scratch>/pre-wt && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev)`.
    If `uv run` inside the throwaway worktree cannot resolve `uv` itself, create the shim the way
    `42-GATE-EVIDENCE-05.md` § 2 records (`ln -sf <resolved uv path> .venv/bin/uv`) and record both
    the shim command and its `readlink -f` output. Then record the isolation proof:
    `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"`
    — the printed path must lie inside `<scratch>/pre-wt`, not inside the main checkout and not
    inside this plan's own agent worktree. Paste the command and its exact output.

    **(c) Build the same project twice from the pre-change worktree.** From inside `<scratch>/pre-wt`,
    run `uv run python -m sphinx -b typstpdf -E tests/fixtures/default_typst_documents_gate <scratch>/pre-pdf`
    and `uv run python -m sphinx -b typst -E tests/fixtures/default_typst_documents_gate <scratch>/pre-typ`.
    Record for each: the verbatim command, the exit status, the complete typsphinx warning text
    emitted (there should be exactly one on the `typstpdf` side — quote it), `ls -la` of the build
    directory, the count of `.pdf` files found by a recursive glob, the exact byte size of the
    emitted `.typ`, and its first 20 lines verbatim. The head must show the `@preview` imports and
    the body with no template call — that is D-05's content half, pre-change.

    **(d) Write sections 1-3 of the evidence file.** Create
    `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md`
    with `## 1. The two named commits`, `## 2. Pre-change worktree isolation proof`, and
    `## 3. Pre-change builds`, containing everything recorded above. Open the file with a one-
    paragraph statement of what it discharges (ROADMAP SC#4, D-05 both halves) and an explicit note
    that every figure in it was produced by a command run in this task's own session — and that where
    a number differs from `44-CONTEXT.md`'s (which measured a different project, `My Cool Project`),
    the measured number here wins and the divergence is stated rather than hidden.

    Leave `<scratch>/pre-wt` in place for now if Task 2 benefits from it, but note that Task 2 owns
    removing every throwaway worktree and recording the cleanup.
  </action>
  <verify>
    <automated>test -f .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md &amp;&amp; grep -c 'typsphinx/__init__.py' .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md &amp;&amp; grep -Ec '[0-9a-f]{40}' .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md</automated>
  </verify>
  <acceptance_criteria>
    - `44-GATE-EVIDENCE-03.md` exists and contains the headings `## 1. The two named commits`,
      `## 2. Pre-change worktree isolation proof` and `## 3. Pre-change builds`
    - The file contains at least two distinct 40-hex SHAs (`grep -Eo '[0-9a-f]{40}' <file> | sort -u | wc -l` is at least 2)
    - The file records a `typsphinx.__file__` path containing the substring `pre-wt`, proving the
      pre-change build ran against the throwaway worktree's own copy rather than the main checkout
    - The file records the pre-change `-b typstpdf` exit status as 0 together with a PDF count of 0
    - The file contains `index.typ` together with an explicit byte size, and a verbatim 20-line head
      of that file showing `@preview` and no template call
    - The file contains the output of `git diff --stat <PRE>..<POST> -- typsphinx/` listing exactly
      `typsphinx/__init__.py` and `typsphinx/builder.py`
    - `git show --name-only <the PRE SHA recorded in the file>` lists no path beginning `typsphinx/`
  </acceptance_criteria>
  <done>
    The pre-change half of SC#4 is measured on a tree that provably does not contain this phase's
    change — proven by the recorded `typsphinx.__file__` path and by the pathspec-scoped production
    diff — and both the emitted filename and the emitted file's untemplated structure are recorded
    verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Measure the post-change side and write the Phase 46 CHANGELOG source text</name>
  <files>.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md</files>
  <read_first>
    - `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md`
      §§ 1-3 as written by Task 1 (the PRE/POST SHAs and the pre-change figures to pair against)
    - `.planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md`
      §§ 5-7 (the production-diff isolation section, the cleanup record, and the verdict-table shape
      to mirror)
    - `.planning/REQUIREMENTS.md` REL-06 (what Phase 46 owes: a curated `## [0.7.1]` CHANGELOG entry
      that explicitly calls out CONF-08's output-filename change — this task's source text is its
      input)
  </read_first>
  <action>
    **(a) Create and provision the post-change worktree.** `git worktree add --detach <scratch>/post-wt <POST>`
    where POST is the SHA recorded in § 1, then
    `(cd <scratch>/post-wt && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev)`,
    applying the same `uv` shim if needed. Record the isolation proof exactly as Task 1 did:
    `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"`
    must print a path inside `<scratch>/post-wt`. Append `## 4. Post-change worktree isolation proof`
    with both recorded paths side by side and one sentence stating that they differ and that neither
    is the main checkout — this is what makes the pairing in § 5 meaningful rather than vacuous.

    **(b) Build the same project twice from the post-change worktree.** Run
    `uv run python -m sphinx -b typstpdf -E tests/fixtures/default_typst_documents_gate <scratch>/post-pdf`
    and `uv run python -m sphinx -b typst -E tests/fixtures/default_typst_documents_gate <scratch>/post-typ`.
    Append `## 5. Post-change builds` recording, for each: the verbatim command, the exit status, the
    complete list of typsphinx warnings emitted (there should be none of the nothing-to-compile kind
    — state that explicitly), `ls -la` of the build directory, the count of `.pdf` files, the exact
    byte size of `quickstartdefaultgate.typ`, its first 20 lines verbatim showing the shared-template
    import and the template function call, and the first four bytes of `quickstartdefaultgate.pdf`.

    **(c) Pair the two sides.** Append `## 6. The measured before/after pair` with a two-column table
    whose rows are: emitted `.typ` filename; emitted `.typ` byte size; template applied (yes/no);
    number of `.pdf` files written; emitted `.pdf` filename; typsphinx warnings emitted. Every cell
    must be a figure recorded in §§ 3 or 5 of this same file, not a figure recalled from anywhere
    else. Below the table, state in two sentences what a user who never set `typst_documents`
    experiences on upgrade — the artifact is renamed, and its structure changes from body-only to
    fully templated, which matters specifically to anyone who includes the old file from their own
    Typst source.

    **(d) Write the Phase 46 hand-off.** Append `## 7. CHANGELOG source text for Phase 46 (REL-06)`
    containing a fenced markdown block written as CHANGELOG prose — a `### Changed` bullet naming
    the old and new filenames measured above, and a second bullet naming the structure change — plus
    one sentence stating that Phase 46 should quote or adapt this block rather than re-derive it, and
    that the underlying figures live in §§ 3, 5 and 6 of this file. Keep the block quotable: no
    placeholders, no `TBD`, no figure that is not in this file.

    **(e) Clean up and close.** Remove both throwaway worktrees with
    `git worktree remove --force <path>`, then record `git worktree list` verbatim under
    `## 8. Cleanup` and state that neither `pre-wt` nor `post-wt` appears in it. Also record
    `git status --porcelain typsphinx/ tests/` as empty — this plan changed no code and no test.
    Finish with `## 9. Verdict`, a one-row table mapping ROADMAP SC#4 to the sections that discharge
    it and a MET/NOT-MET status, plus a short "not owned by this file" list naming SC#1/SC#2
    (plan 44-01), SC#3 (plan 44-02) and SC#5 (plan 44-04).
  </action>
  <!-- planner-discipline-allow: pre-wt|post-wt -->
  <!-- planner-discipline-allow: pre-wt -->
  <!-- planner-discipline-allow: post-wt -->
  <!-- planner-region-allow: pre-wt|post-wt -->
  <verify>
    <automated>grep -c 'quickstartdefaultgate.pdf' .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md &amp;&amp; grep -c 'CHANGELOG source text' .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-03.md &amp;&amp; ! git worktree list | grep -Eq 'pre-wt|post-wt'</automated>
    <human-check>Read § 7's fenced CHANGELOG block. Confirm it reads as prose a release note can quote directly — it names both the old and the new emitted filename, says the emitted file is now fully templated, and contains no placeholder. This is the `backstop` truth in this plan's `must_haves`: whether the record is *sufficient* for Phase 46 is a judgment, not a command output.</human-check>
  </verify>
  <acceptance_criteria>
    - `44-GATE-EVIDENCE-03.md` contains the headings `## 4. Post-change worktree isolation proof`,
      `## 5. Post-change builds`, `## 6. The measured before/after pair`,
      `## 7. CHANGELOG source text for Phase 46 (REL-06)`, `## 8. Cleanup` and `## 9. Verdict`
    - The file records a `typsphinx.__file__` path containing `post-wt`, distinct from the `pre-wt`
      path recorded in § 2
    - The file contains `quickstartdefaultgate.typ` together with an explicit byte size, and
      `quickstartdefaultgate.pdf`
    - The § 6 table has a row for the emitted `.typ` filename whose two cells are `index.typ` and
      `quickstartdefaultgate.typ`, and a row for the PDF count whose two cells are `0` and `1`
    - § 7 contains a fenced block with at least two `### Changed`-style bullets and no occurrence of
      `TBD`
    - `git worktree list` names neither `pre-wt` nor `post-wt`
    - `git status --porcelain typsphinx/ tests/` produces no output
    - `uv run python -m pytest -q` (full suite) exits 0 in the main plan worktree — this plan changed
      nothing, so a red suite here means a wave-2 regression, not a plan failure
  </acceptance_criteria>
  <done>
    Both halves of CONF-08's user-visible change are measured on two provably different trees, paired
    in one table, and handed to Phase 46 as quotable CHANGELOG source text; both throwaway worktrees
    are removed and the removal is recorded.
  </done>
</task>

</tasks>

<threat_model>
Security enforcement is active at **OWASP ASVS level 1**, blocking on **high** severity. Reused from
`44-RESEARCH.md` § "Security Domain". This plan changes no production code; its threat surface is the
measurement procedure itself.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| named git commit → throwaway worktree → installed package | The measurement's whole validity rests on the pre-change side executing pre-change code. A worktree without its own `uv sync` resolves `import typsphinx` to the MAIN checkout through the main venv's editable finder, so the boundary is real and silently crossable. |
| scratch worktree paths → `git worktree remove` | Created state outside the repository's tracked tree that must be removed rather than left behind. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-04 | Spoofing | The pre-change measurement silently produced by post-change code (an unprovisioned worktree resolving `import typsphinx` to the main checkout) | high | mitigate | Per-worktree `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, plus a recorded `typsphinx.__file__` isolation proof for each side whose two paths must differ and must each lie inside their own worktree. Both are acceptance criteria, not advice. The behavioural control is independent: a post-change-code "pre-change" build would write a PDF, and § 3 asserts a PDF count of 0. |
| T-44-05 | Repudiation | A figure transcribed from `44-CONTEXT.md` presented as measured here, making the record unfalsifiable | medium | mitigate | Authored as a `must_haves` prohibition and as an explicit statement in the evidence file's opening paragraph; the divergence rule (measured number wins, divergence stated) makes a mismatch visible instead of quietly resolved in favour of the planning document. |
| T-44-02 | Tampering | Path traversal through a `typst_documents` target name | high | mitigate | Unchanged and untouched by this plan; the existing `_resolve_output_stem` guard remains the control. Recorded here only so the register is complete across the phase. Canon security — also breadcrumbed to `/gsd-secure-phase`. |
| T-44-SC | Tampering | Supply chain — package-manager installs | low | accept | The per-worktree `uv sync` installs from this repository's own committed `uv.lock` at the named commit; no new or changed dependency is introduced anywhere in this phase, and `pyproject.toml`/`uv.lock` are unmodified. Plan 44-04 asserts that rather than assuming it. |
</threat_model>

<verification>
- `44-GATE-EVIDENCE-03.md` contains sections 1 through 9 and at least two distinct 40-hex SHAs.
- Two distinct `typsphinx.__file__` paths are recorded, one per throwaway worktree.
- The § 6 table pairs `index.typ` / 0 PDFs against `quickstartdefaultgate.typ` / 1 PDF, with byte
  sizes for both `.typ` files and a template-applied yes/no row.
- `git worktree list` names neither throwaway worktree; `git status --porcelain typsphinx/ tests/`
  is empty.
- `uv run python -m pytest -q` still exits 0 in the plan worktree.
</verification>

<success_criteria>
- The output-filename rename is measured, not assumed, on two real builds of the same project across
  two named commits (ROADMAP SC#4).
- The record covers BOTH the rename and the content change (D-05).
- The measurement is proven non-vacuous by a per-side `typsphinx.__file__` isolation proof and by a
  pathspec-scoped production diff naming only the two files this phase changed.
- Phase 46 receives a quotable CHANGELOG source-text block rather than a set of figures it would have
  to re-derive.
</success_criteria>

<output>
Create `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-03-SUMMARY.md` when done
</output>
