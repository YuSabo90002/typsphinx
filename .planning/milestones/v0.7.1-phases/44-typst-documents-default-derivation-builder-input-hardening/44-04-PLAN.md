---
phase: 44-typst-documents-default-derivation-builder-input-hardening
plan: 04
type: execute
wave: 3
depends_on: [44-01, 44-02]
files_modified:
  - .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md
autonomous: true
requirements: [CONF-08, BLD-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every existing test that encoded the old `[]`-default is accounted for by name: the audit re-runs the repo-wide `conf.py` census and the `temp_sphinx_app` blast-radius grep — including the `write_doc(` call pattern the research pass omitted, which is why one affected file was missed there — and records a per-file verdict of CHANGED or VERIFIED-NO-CHANGE for every test module that references that fixture (ROADMAP SC#5, traceability half)."
    - "`tests/test_config.py` is recorded as a deliberate, verified no-op with its passing pytest output as the proof rather than omitted as an implicit non-event: its two `temp_sphinx_app`-based assertions check only attribute presence and list-ness, both of which still hold under a derived default."
    - "The full suite is green with no `-m` filter applied anywhere, so `tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error` actually ran — its selection is proven by name in recorded output rather than assumed (ROADMAP SC#5, full-corpus gate)."
    - "`uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` each exit 0 on the post-change tree, with the exit status of each recorded (ROADMAP SC#5)."
    - "This phase introduced no runtime dependency and no new `@preview` version-lockstep site: the diff of `pyproject.toml` and `uv.lock` across the phase is empty and `tests/test_preview_version_sync.py` passes (milestone invariant: zero new runtime dependencies)."
    - "Every file this phase added or modified is enumerated in one table with the requirement id it serves and the plan that produced it, so no change reaches the release absorbed silently."
    - "Typing imports were not modernized anywhere in this phase's diff: the `List`/`Set`/`Tuple` imports in `typsphinx/builder.py` are unchanged, and `ruff` still reports clean with `UP006`/`UP035` ignored (`CLAUDE.md` standing prohibition, independently restated in REQUIREMENTS.md)."
  prohibitions:
    - statement: "MUST NOT report SC#5 satisfied by silence. A file that needed no change must appear in the audit as an audited, verified no-op with its proof attached; omitting it because nothing happened is indistinguishable from never having looked, which is the exact failure this success criterion exists to prevent."
      category: transparency
    - statement: "MUST NOT relax, skip, xfail, deselect or otherwise suppress any existing test to make the phase gate green. A red test means either the production change is wrong or the test encoded the superseded contract and its update is a deliberate, recorded SC#5 item with a measured reason — a suppression is neither."
      category: transparency
  artifacts:
    - path: ".planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md"
      provides: "The SC#5 record: the re-measured repo-wide audit, the per-file CHANGED / VERIFIED-NO-CHANGE table, the full-suite and lint/type gate transcripts, and the untouched-manifest proof"
      contains: "VERIFIED-NO-CHANGE"
  key_links:
    - from: ".planning/phases/44-.../44-GATE-EVIDENCE-01.md § 4"
      to: ".planning/phases/44-.../44-GATE-EVIDENCE-04.md"
      via: "plan 44-01's per-plan test-update record is folded into the repo-wide audit as the CHANGED rows"
      pattern: "CHANGED"
    - from: "tests/test_corpus_gate.py"
      to: ".planning/phases/44-.../44-GATE-EVIDENCE-04.md"
      via: "the full-corpus render gate's node id appears in the recorded full-suite output, proving it was selected"
      pattern: "test_corpus_compiles_with_no_fatal_error"
---

<objective>
Discharge ROADMAP SC#5: prove that every existing test which encoded the old `[]`-default was updated
deliberately and traceably, and that the full suite, the lint/type trio and the full-corpus
`-b typstpdf` gate are green on the post-change tree.

Purpose: SC#5 is not satisfied by silence. `44-RESEARCH.md` concluded that no existing test needed a
change, and that conclusion was **wrong in one place** — its blast-radius grep searched for
`.translate()`, `.write(`, `app.build(`, `writer.translate` and `_is_master_document`, but not for
`write_doc(`, so it missed `tests/test_builder.py`, whose two output-file assertions read
`index.typ` from a `temp_sphinx_app` whose `conf.py` omits `typst_documents`. That miss is exactly
why the audit must be re-measured here rather than trusted, and why each verdict must carry its own
proof.

Output: `44-GATE-EVIDENCE-04.md` — the re-run census, the corrected blast-radius grep, a per-file
CHANGED / VERIFIED-NO-CHANGE table with proofs, the phase's complete added/changed file inventory
mapped to requirement ids, and the recorded phase-gate run.
</objective>

<probe_findings>
Spec-less probe fallback is active for this phase. All five edge items are accounted for in plans
44-01 (four authored into `must_haves.truths`) and 44-02 (one flagged assumption for BLD-01's
`unclassified` row); this plan authors none of its own — it is the phase's audit and gate plan, and
adding synthetic edge truths here would double-count them.

**Prohibition probe.** Stage 1 over-produced against the SC#5 surface; Stage 2 kept the two
transparency items authored in `must_haves.prohibitions` above (audit-by-silence, and
suppression-instead-of-repair) and dropped the routine-engineering candidates (must not leave a
scratch directory behind; must not run the suite from the wrong venv — both are ordinary hygiene the
worktree provisioning step already owns). No canon security item surfaced for this plan's surface.
</probe_findings>

<artifacts_this_phase_produces>
Every symbol Phase 44 creates. The plan-review-convergence source-grounding pass reads this list to
exclude newly-created symbols from drift verification. Items marked **[this plan]** are created here.

**New production symbols**
- None. This plan changes no code, no test and no fixture; it creates one markdown evidence file
  **[this plan]**.
- `typsphinx.builder._default_typst_documents(config)` (plan 44-01); the `isinstance(docname, str)`
  guard and the reworded opt-out message inside `TypstPDFBuilder.finish()` (plan 44-02)

**No new runtime dependency.** `pyproject.toml` and `uv.lock` are not modified by this phase — a
fact this plan asserts rather than assumes.

**New test modules** (none in this plan)
- `tests/test_default_typst_documents_derivation.py`, `tests/test_default_typst_documents_gate.py`
  (plan 44-01); `tests/test_non_str_docname_gate.py`,
  `tests/test_empty_typst_documents_optout_gate.py` (plan 44-02)

**New fixture directories** (none in this plan)
- `tests/fixtures/default_typst_documents_gate/`,
  `tests/fixtures/explicit_typst_documents_wins_gate/` (plan 44-01);
  `tests/fixtures/non_str_docname_gate/`, `tests/fixtures/empty_typst_documents_optout_gate/`
  (plan 44-02)

**Existing files modified across the phase**
- `typsphinx/builder.py` (plans 44-01 and 44-02), `typsphinx/__init__.py` (plan 44-01),
  `tests/test_builder.py` (plan 44-01)

**New evidence files** (all under the phase directory; never named `44-VERIFICATION.md`, which
`/gsd-verify-work` overwrites wholesale)
- `44-GATE-EVIDENCE-04.md` — SC#5 audit + phase gate **[this plan]**
- `44-GATE-EVIDENCE-01.md` (plan 44-01), `-02.md` (plan 44-02), `-03.md` (plan 44-03)
</artifacts_this_phase_produces>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-CONTEXT.md
@.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-VALIDATION.md
@.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-01.md
@.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-02.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the repo-wide test audit and record a verdict for every affected file</name>
  <files>.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md</files>
  <read_first>
    - `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-RESEARCH.md`
      § "Common Pitfalls" Pitfall 2 (the blast-radius grep whose pattern list omitted `write_doc(`)
      and § "Validation Architecture" → "Wave 0 Gaps" (the four items SC#5 requires be recorded)
    - `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-01.md`
      § 4 (plan 44-01's own record of which tests it changed and why — fold it in, do not restate it
      from memory)
    - `tests/conftest.py` lines 44-79 (`temp_sphinx_app`'s inline `conf.py`, which omits
      `typst_documents` and is the premise of the whole blast radius)
    - `tests/test_config.py` lines 1-20 (the two `temp_sphinx_app`-based assertions that must be
      recorded as a verified no-op)
    - `tests/test_builder_output_stem.py` lines 1-80 (every test assigns
      `builder.config.typst_documents` explicitly — the reason this module is unaffected)
    - `.planning/ROADMAP.md` § "Phase 44" SC#5 (the exact wording being discharged)
  </read_first>
  <action>
    Provision this worktree FIRST: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`,
    then run every command through `uv run`.

    Create
    `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md`
    and fill sections 1-4, pasting every command with its verbatim output. Re-measure everything;
    do not transcribe a count from `44-CONTEXT.md` or `44-RESEARCH.md`, and where a re-measured
    number differs from a planning document's, record the divergence explicitly and let the measured
    number stand.

    **`## 1. Repo-wide `conf.py` census.`** Run
    `grep -rl "typst_documents" --include=conf.py . | wc -l` and a loop that prints any of those
    files which does not actually assign the setting at column 0. Record both outputs. State the
    conclusion this supports: no pre-existing fixture's output filename changes, because every
    `conf.py` in the repo that mentions the setting also sets it — except the two this phase added
    deliberately (the unset fixture and the explicit-empty opt-out fixture), which must be listed
    by name as intentional exceptions.

    **`## 2. `temp_sphinx_app` blast radius, re-measured with the corrected pattern set.`** List the
    test modules that reference `temp_sphinx_app` with a per-file reference count. Then grep those
    modules for the full-pass drivers — `app.build(`, `.translate()`, `builder.write(`, and
    critically `write_doc(`, the pattern the research pass omitted. Record which files match and
    which do not, and state plainly that adding `write_doc(` to the pattern set is what surfaces
    `tests/test_builder.py`, so the research conclusion "no existing test needs a change" was
    incomplete rather than merely conservative.

    **`## 3. Per-file verdict table.`** One row per test module that references `temp_sphinx_app`,
    plus `tests/test_pdf_generation.py` and `tests/test_builder_output_stem.py`. Columns: file,
    verdict (`CHANGED` or `VERIFIED-NO-CHANGE`), reason, proof. Every `VERIFIED-NO-CHANGE` row's
    proof must be a command output pasted in this file — the module's own passing pytest run, and
    for `tests/test_builder_output_stem.py` and `tests/test_pdf_generation.py` also a grep count
    showing every test assigns `builder.config.typst_documents` explicitly. `tests/test_config.py`
    gets its own explicit row: its two `temp_sphinx_app`-based assertions check only attribute
    presence and list-ness, both still true under the derived default. The `CHANGED` rows are
    whatever plan 44-01 actually changed — read them out of `44-GATE-EVIDENCE-01.md` § 4 and
    reproduce the measured reason there, with the current `git log --oneline -- tests/test_builder.py`
    entry as the proof.

    **`## 4. Phase file inventory.`** One table listing every file this phase added or modified,
    from `git diff --name-status <PHASE_BASE>..HEAD`, with a requirement id (CONF-08 or BLD-01) and
    the producing plan for each row. Take PHASE_BASE as the parent of the RED commit SHA recorded in
    `44-GATE-EVIDENCE-01.md` § 2 (that SHA followed by `^`), and verify with
    `git log -1 --oneline <PHASE_BASE>` that its subject does not carry a `44-` scope; record that
    check. Any row you cannot map to a requirement id is a finding, not a formatting problem —
    surface it in the section text rather than leaving the cell blank.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; grep -c 'VERIFIED-NO-CHANGE' .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md &amp;&amp; grep -c 'write_doc(' .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md &amp;&amp; uv run python -m pytest tests/test_config.py tests/test_builder.py tests/test_builder_output_stem.py tests/test_pdf_generation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `44-GATE-EVIDENCE-04.md` exists and contains the headings `## 1. Repo-wide`, `## 2.`,
      `## 3. Per-file verdict table` and `## 4. Phase file inventory`
    - § 2 records `write_doc(` as part of the pattern set and names `tests/test_builder.py` as the
      file it surfaces
    - § 3's table contains at least one `CHANGED` row and at least six `VERIFIED-NO-CHANGE` rows,
      and every row's proof cell references command output present in this same file
    - § 3 contains a row whose file cell is `tests/test_config.py` with verdict
      `VERIFIED-NO-CHANGE`
    - § 4's table lists `typsphinx/builder.py`, `typsphinx/__init__.py` and `tests/test_builder.py`
      among its rows, and every row carries either `CONF-08` or `BLD-01`
    - `grep -Eo '[0-9a-f]{40}' .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md | sort -u | wc -l` is at least 1 (the PHASE_BASE resolution is recorded with real SHAs)
    - `uv run python -m pytest tests/test_config.py tests/test_builder.py tests/test_builder_output_stem.py tests/test_pdf_generation.py -q` exits 0
  </acceptance_criteria>
  <done>
    Every test module that could have encoded the old `[]`-default carries a recorded verdict with
    its own proof — none is omitted as an implicit non-event — and the phase's complete
    added/changed file inventory is mapped to requirement ids.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run and record the phase gate — full suite, corpus gate, lint, types, manifests</name>
  <files>.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md</files>
  <read_first>
    - `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md`
      §§ 1-4 as written by Task 1 (the PHASE_BASE SHA to reuse)
    - `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-VALIDATION.md`
      § "Sampling Rate" (the exact phase-gate command set SC#5 names)
    - `tests/test_corpus_gate.py` (the class and method whose node id must appear in the recorded
      full-suite output, proving it was selected rather than filtered out)
    - `tests/test_preview_version_sync.py` (the `@preview` lockstep guard that must still pass)
    - `pyproject.toml` `[tool.pytest.ini_options]` and `tox.ini` (confirm no `-m` filter is
      configured anywhere, so the full run really does include the slow corpus gate)
  </read_first>
  <action>
    Run every command through `uv run` in this already-provisioned worktree. Append sections 5-7 to
    `44-GATE-EVIDENCE-04.md`, pasting each command with its verbatim output and its exit status.

    **`## 5. Full suite.`** Run `uv run python -m pytest -q` and record the command, the final
    summary line, and the exit status. This repository configures no `-m` filter in
    `pyproject.toml`, `tox.ini` or CI, so this run includes the slow full-corpus gate; prove that
    rather than assert it by additionally running
    `uv run python -m pytest tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error -q`
    and recording its own passing output and node id. Also record
    `uv run python -m pytest tests/test_preview_version_sync.py -q` — the phase must not have
    introduced a new `@preview` version-lockstep site.

    If ANY test is red, do not proceed to section 6 and do not suppress it. Diagnose it: either the
    production change from wave 1 or 2 is wrong (fix belongs in that plan's surface, so stop and
    surface the failure), or the test encoded the superseded contract and its update is a new SC#5
    item that must be made deliberately, with the measured failure output recorded in § 3's table as
    an additional `CHANGED` row carrying its reason. Never `skip`, `xfail`, deselect, or weaken an
    assertion to reach green.

    **`## 6. Lint and types.`** Run `uv run black --check .`, `uv run ruff check .` and
    `uv run mypy typsphinx/`, recording each command's output and exit status separately. If a
    console-script invocation cannot be resolved in this sandbox, fall back to the equivalent
    `uv run python -m black --check .` / `uv run python -m ruff check .` /
    `uv run python -m mypy typsphinx/` form and record that you did so and why. Add one sentence
    confirming that `ruff` is still passing with `UP006`/`UP035` in its ignore list and that this
    phase's diff modernized no typing import — the standing prohibition in `CLAUDE.md`.

    **`## 7. Manifests and verdict.`** Record `git diff --stat <PHASE_BASE>..HEAD -- pyproject.toml uv.lock`
    and state that its emptiness is the zero-new-runtime-dependency proof (and that this is also the
    reason `44-RESEARCH.md` records the Package Legitimacy Audit as not applicable — no package
    manager install occurs anywhere in this phase). Record
    `git diff --stat <PHASE_BASE>..HEAD -- typsphinx/` and confirm it names only
    `typsphinx/__init__.py` and `typsphinx/builder.py`. Close with a verdict table mapping each of
    ROADMAP SC#1 through SC#5 to the evidence file and section that discharges it — SC#1 and SC#2 to
    `44-GATE-EVIDENCE-01.md`, SC#3 to `-02.md`, SC#4 to `-03.md`, SC#5 to this file — each with a
    MET / NOT-MET status. A criterion this plan cannot see the evidence for is NOT-MET with the
    reason stated, never MET by assumption.
  </action>
  <verify>
    <automated>uv run python -m pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; grep -c 'test_corpus_compiles_with_no_fatal_error' .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-GATE-EVIDENCE-04.md</automated>
  </verify>
  <acceptance_criteria>
    - `44-GATE-EVIDENCE-04.md` contains the headings `## 5. Full suite`, `## 6. Lint and types` and
      `## 7. Manifests and verdict`
    - § 5 records the verbatim final summary line of `uv run python -m pytest -q` together with its
      exit status, and contains the node id
      `tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error`
    - § 5 records a passing `tests/test_preview_version_sync.py` run
    - § 6 records three separate commands with their exit statuses, and states that no typing import
      was modernized in this phase's diff
    - § 7 records `git diff --stat <PHASE_BASE>..HEAD -- pyproject.toml uv.lock` producing no output
    - § 7 records `git diff --stat <PHASE_BASE>..HEAD -- typsphinx/` naming only
      `typsphinx/__init__.py` and `typsphinx/builder.py`
    - § 7's verdict table has one row per ROADMAP success criterion SC#1 through SC#5, each naming
      the evidence file and section that discharges it and carrying a MET or NOT-MET status
    - `uv run python -m pytest -q` exits 0
    - `uv run black --check .` exits 0, `uv run ruff check .` exits 0, `uv run mypy typsphinx/`
      exits 0
    - `git status --porcelain typsphinx/ tests/` produces no output (this plan changed no code and
      no test)
  </acceptance_criteria>
  <done>
    The full suite — including the full-corpus `-b typstpdf` gate, proven selected by node id —
    plus `black`, `ruff` and `mypy` are recorded green on the post-change tree, the manifests are
    recorded untouched, and every ROADMAP success criterion is mapped to the evidence that
    discharges it.
  </done>
</task>

</tasks>

<threat_model>
Security enforcement is active at **OWASP ASVS level 1**, blocking on **high** severity. Reused from
`44-RESEARCH.md` § "Security Domain". This plan changes no production code; it is the phase's audit
and gate, so its threat surface is the integrity of the audit itself and the supply-chain claim it
asserts.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's diff → the release | Everything this phase changed passes through this audit on its way to Phase 45's documentation and Phase 46's release prep. A change that escapes the inventory escapes the CHANGELOG. |
| `pyproject.toml` / `uv.lock` → the published wheel | The zero-new-runtime-dependency milestone invariant is only real if it is measured; this plan is where it is measured for this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-SC | Tampering | Supply chain — an unnoticed dependency addition or version bump riding along in the phase diff | low | mitigate | § 7 records `git diff --stat <PHASE_BASE>..HEAD -- pyproject.toml uv.lock` and requires it to be empty, and `tests/test_preview_version_sync.py` is run explicitly so a new `@preview` version-lockstep site cannot enter unnoticed. `44-RESEARCH.md` records the Package Legitimacy Audit as not applicable because this phase runs no package-manager install; this section is what converts that from a claim into a measurement. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package is introduced, so no legitimacy checkpoint is required. |
| T-44-06 | Repudiation | An audit that reports SC#5 satisfied without having looked — a file omitted from the verdict table is indistinguishable from a file that was never examined | medium | mitigate | The verdict table requires a row per module with a proof cell that must reference command output present in the same file, and both `must_haves.prohibitions` forbid audit-by-silence and suppression-instead-of-repair. The acceptance criteria count rows rather than trusting narrative. |
| T-44-01 | Denial of Service | A non-`str` docname reaching `posixpath.dirname` | medium | mitigate | Mitigated in plan 44-02; this plan re-runs its gate as part of the full suite, so a regression in the guard surfaces here. |
| T-44-02 | Tampering | Path traversal through a `typst_documents` target name | high | mitigate | Unchanged and untouched by this phase; the existing `_resolve_output_stem` guard remains the control and its own tests run in the recorded full suite. Canon security — also breadcrumbed to `/gsd-secure-phase`. |
</threat_model>

<verification>
- `44-GATE-EVIDENCE-04.md` contains sections 1 through 7, a per-file verdict table with a proof for
  every row, a phase file inventory mapped to requirement ids, and a SC#1-SC#5 verdict table.
- `uv run python -m pytest -q` exits 0 and the recorded output shows the full-corpus gate's node id.
- `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` all exit 0.
- `git diff --stat <PHASE_BASE>..HEAD -- pyproject.toml uv.lock` produces no output.
- `git status --porcelain typsphinx/ tests/` produces no output.
</verification>

<success_criteria>
- Every existing test that encoded the old `[]`-default is updated deliberately, with each change
  traceable to CONF-08 rather than absorbed silently, and every unaffected module recorded as an
  audited verified no-op with its proof (ROADMAP SC#5, traceability half).
- The full suite, `black`, `ruff`, `mypy` and the full-corpus `-b typstpdf` gate are green and
  recorded (ROADMAP SC#5, gate half).
- The phase's complete added/changed file inventory is mapped to CONF-08 / BLD-01, so Phase 45's
  documentation work and Phase 46's CHANGELOG have a single authoritative list.
- The zero-new-runtime-dependency invariant is measured, not assumed.
</success_criteria>

<output>
Create `.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-04-SUMMARY.md` when done
</output>