---
phase: 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare
plan: 05
type: execute
wave: 4
depends_on: ["45.2-03", "45.2-04"]
files_modified:
  - .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md
  - .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md
  - pyproject.toml
  - tox.ini
  - uv.lock
autonomous: true
requirements: [QUA-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real CI run on gsd/v0.7.1-bug-fix-round, whose head SHA contains this phase's pyproject.toml and tox.ini changes, has completed, and its result is recorded verbatim rather than argued from the workflow file (SC#4)"
    - "The before and after of tox's existing uv-sync> log line are recorded side by side: on the pre-phase CI run it resolved a binary under .venv/bin, and on this phase's run it resolves the astral-sh/setup-uv-installed binary instead — proving CI reaches uv by a different route than the local build does (SC#4, D-17)"
    - ".github/workflows/ci.yml is byte-unchanged — SC#4's evidence is the change in an existing log line, not a new assertion step, because adding one would move .github/ outside this phase's fence (D-17)"
    - "This phase's own diff, taken against the pre-phase base SHA recorded in the evidence file's Step 0, touches no path under typsphinx/, changes no entry of [project] dependencies, changes no @preview package version, and changes pyproject.toml only inside the dev extra (SC#7)"
    - "The ruff generic-linux-ELF defect is filed as a todo describing the measured symptom and the candidate NixOS-local repairs, and is not fixed and not reframed as an environmental false positive (D-03)"
    - "The project memory pytest-no-outer-uv-run-in-main-tree is rewritten only after Step 7 records SC#3 as MET; if SC#3 is NOT MET the memory is left in place and the reason is recorded (D-12)"
  artifacts:
    - path: ".planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md"
      provides: "the routed-out ruff defect, filed rather than fixed"
    - path: ".planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md"
      provides: "Steps 8 and 9 plus the closing verdict, one bullet per success criterion"
      contains: "## Closing verdict"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "the CI uv-sync> log line"
      via: "astral-sh/setup-uv@v7 runs before uv run tox, so shutil.which resolves the runner-installed binary once the wheel binary is gone"
      pattern: "uv-sync>"
  prohibitions:
    - "Never create .venv/bin/uv or .venv/bin/uvx as a symlink or copy pointing at a nix-store or system uv (D-11), in either tree. SC#2 requires those paths to be absent, and the closing verdict certifies that."
    - "Never modify .planning/MILESTONES.md, .planning/RETROSPECTIVE.md, anything under .planning/milestones/, .planning/PROJECT.md line 951, .planning/STATE.md line 425 or .planning/STATE.md line 587 (D-08). Those six records describe REL-04's release-job failure — a different, real bug — and are the recorded justification for an open requirement."
    - "Never edit .github/workflows/ci.yml. D-17 makes the change in an existing log line the evidence; adding a permanent assertion step would move .github/, outside this phase's fence, and SC#7 is asserted mechanically over this phase's diff."
    - "Never attempt to repair the ruff generic-linux-ELF defect on this machine, and never reframe it as an environmental false positive in the closing verdict (D-03). Every available local repair is NixOS-local — the category D-18 declined. File it; do not fix it."
    - "Never write a MET verdict for a success criterion whose evidence step is absent or empty. A criterion with no transcript is recorded as NOT VERIFIED in those words. This phase exists because the project believed unverified claims about its own toolchain."
---

<objective>
Close the phase: prove on a real CI run that the repaired tree stays green and reaches `uv` by a
different route than the local build does, assert this phase's fence mechanically over its own diff,
file the routed-out defects as todos, retire the project memory the fix invalidates, and write the
closing verdict.

Purpose: SC#4 is the criterion v0.7.0 got wrong with REL-04 — a mechanism reported as done on the
strength of the workflow file rather than a run. This phase must not repeat that. SC#7 is the fence
Phase 46's SC#3 depends on: Phase 46 measures the tree this phase hands it, so "nothing else moved"
has to be a mechanical assertion over the diff, not a claim.

Output: `45.2-TOOLCHAIN-EVIDENCE.md` gains Step 8 (SC#4), Step 9 (SC#7) and a closing verdict
section; one or more new todos; and a rewritten project memory.
</objective>

<wave_and_isolation_contract>
This plan runs in a worktree in wave 4, alone. Waves 1 through 3 have merged back, so at plan start
your worktree `HEAD` is the milestone branch tip and already contains every commit of this phase.

**Task 1 pushes before it commits, and that ordering is load-bearing.** `git push origin
HEAD:refs/heads/gsd/v0.7.1-bug-fix-round` at plan start pushes exactly the milestone tip, which is
what SC#4 needs on the remote. If you commit first, the push carries an unmerged worktree commit
onto the shared branch. Do the push as the first action of Task 1, before any file is written.

The only main-tree command this plan may issue is a read-only `git -C /home/yuta/Documents/typsphinx
rev-parse HEAD` to confirm the two trees agree. Plan 04 already took every main-tree measurement.
Provision your own worktree per `CLAUDE.md` and run local tooling with `uv run` inside it.
</wave_and_isolation_contract>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md
@.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger CI on the branch and record Step 8 — the uv-sync log line before and after (SC#4)</name>
  <precondition>`gh auth status` reports an authenticated account with access to the `YuSabo90002/typsphinx` repository, and `git rev-parse HEAD` in this worktree resolves to a commit whose `pyproject.toml` contains the new dev-extra requirement name. If `gh` is not authenticated, HALT and surface it — SC#4 must not be argued from the workflow file.</precondition>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md, pyproject.toml, tox.ini, uv.lock</files>
  <read_first>
    - `.github/workflows/ci.yml` — the `on:` block (`push: [main, develop]`, `pull_request: [main, develop]`, `workflow_dispatch`) and the job step order `actions/checkout@v7` then `astral-sh/setup-uv@v7` then `uv python install` then `uv sync --extra dev --locked` then `uv run tox -e <env>`. That ordering is what SC#4 asserts, and the file must not be edited.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-04 (CI run 31287786840's Lint job proves today's green does not validate `tox.ini`), D-16 (the branch is not push-triggered, so all three prior branch runs are `workflow_dispatch`; `gh workflow run CI --ref gsd/v0.7.1-bug-fix-round` matches that precedent, covers the Windows and macOS lanes, and needs no PR), D-17 (the evidence is the before/after of the existing `uv-sync>` line and `ci.yml` gains no assertion step) and D-18 (a red CI caused by this phase's own changes is fixed inside this phase).
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — Step 6, whose local `uv-sync>` line is the local-side counterpart of what this step measures on CI.
  </read_first>
  <action>
    **First action, before writing anything:** run `git rev-parse HEAD`, confirm
    `git show HEAD:pyproject.toml` carries the new dev-extra requirement name and
    `git show HEAD:tox.ini` carries the new `requires` line, then push with
    `git push origin HEAD:refs/heads/gsd/v0.7.1-bug-fix-round`. Record the pushed SHA. The branch
    already exists on `origin`, so this is an update, not a create.

    **Capture the before half.** Fetch the Lint job log of CI run `31287786840` (main, 2026-08-09)
    with `gh run view 31287786840 --log`, and extract its `lint: uv-sync>` line and its
    `lint: freeze>` line with the freeze output. Those two lines together are D-04's measurement:
    the sync resolved a binary under `.venv/bin`, and the freeze shows the tox environment contained
    neither linter, yet both lint commands ran anyway because `uv run tox` had put `.venv/bin` on
    `PATH`. If that run's logs have expired, say so explicitly and substitute the most recent
    pre-phase CI run on `main`, naming which run was used and why.

    **Trigger the after half.** Run `gh workflow run CI --ref gsd/v0.7.1-bug-fix-round`. Do NOT open
    a pull request — that would entangle with Phase 46's release PR. Poll for the new run with
    `gh run list --workflow=CI --branch gsd/v0.7.1-bug-fix-round --limit 5 --json
    databaseId,headSha,event,status,conclusion,createdAt`, confirm its `headSha` equals the SHA you
    pushed, and wait for completion with `gh run watch` or repeated `gh run view`.

    **Record the after half.** From the completed run's Lint job log, extract the `lint: uv-sync>`
    line and the `lint: freeze>` line. Two things must have changed and both must be called out
    explicitly: the resolved `uv` path is no longer under `.venv/bin`, and the freeze output now
    lists the two linters because `extras = dev` installs them into the tox environment. Record each
    job's conclusion — every OS and Python lane, plus lint, type and cov.

    **Apply D-18 to the result.** If any job is red:
    - If the failure is attributable to this phase's own change — dependency resolution, the
      lockfile, tox provisioning, or a tool now running inside a tox environment that previously
      never ran there — fix it in this phase in `pyproject.toml`, `tox.ini` or `uv.lock`, re-push,
      re-dispatch, and record each iteration. Phase 46 must not inherit a red tree. Note that
      `ruff check .` now genuinely executes inside `.tox/lint` on CI, where the Linux runner has a
      working `ruff` — so a `ruff` finding on CI is a real lint finding in this repository's source,
      is this phase's to fix, and is NOT the local generic-linux-ELF defect D-03 routes out.
    - If the failure is demonstrably unrelated, for example a pre-existing Windows-only issue,
      isolate it, record it in Step 8 with the verbatim error, and file it as a todo in Task 2.

    Append everything as `## Step 8 — CI evidence (SC#4)`, with the before and after log lines in
    adjacent fenced blocks and a short paragraph naming what the single changed line proves.
  </action>
  <verify>
    <automated>gh run list --workflow=CI --branch gsd/v0.7.1-bug-fix-round --limit 1 --json headSha,conclusion,event &amp;&amp; grep -c '^## Step 8' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --heads origin gsd/v0.7.1-bug-fix-round` returns a SHA equal to the SHA Step 8 records as pushed.
    - `gh run list --workflow=CI --branch gsd/v0.7.1-bug-fix-round --limit 1 --json headSha,conclusion,event` reports `event` of `workflow_dispatch`, a `headSha` matching the pushed SHA, and a non-null `conclusion`.
    - Step 8 contains two `uv-sync>` lines in separate fenced blocks, one from the pre-phase run and one from this phase's run, and the two resolved paths differ.
    - The pre-phase `uv-sync>` path contains `.venv/bin`; this phase's `uv-sync>` path does not.
    - Step 8 contains both `freeze>` outputs, and this phase's freeze output lists `black` and `ruff` while the pre-phase one does not.
    - Step 8 records a conclusion for every job in the run, not only the lint job.
    - `git diff --name-only $BASE..HEAD -- .github/` is empty, where `$BASE` is the pre-phase SHA recorded in Step 0.
    - If any job was red, Step 8 records the classification (caused by this phase / unrelated) with the verbatim error, and for the former the fix applied and the re-run's conclusion.
  </acceptance_criteria>
  <done>A `workflow_dispatch` CI run on `gsd/v0.7.1-bug-fix-round` at the pushed SHA has completed, the before and after `uv-sync>` and `freeze>` lines are recorded side by side, `ci.yml` is unedited, and any red job is either fixed in-phase or isolated and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the SC#7 fence over the phase diff and file the routed-out defects as todos</name>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md, .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md</files>
  <read_first>
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — Step 0, which records the main tree's `git rev-parse HEAD` at wave-1 start. That SHA is this phase's pre-phase base and the left-hand side of every diff in this task.
    - `.planning/ROADMAP.md` § Phase 45.2 SC#7 — its four clauses: `[project] dependencies` untouched, `typsphinx/` untouched, the `@preview` count still four, and `pyproject.toml`'s only change inside the `dev` extra.
    - `tests/test_preview_version_sync.py` — the existing guard asserting the four `@preview` versions agree across `writer.py`, `template_engine.py` and `templates/base.typ`. It is the mechanical check for SC#7's third clause; do not write a new one.
    - `.planning/todos/pending/2026-08-04-release-create-job-missing-uv-verify-end-to-end.md` — an existing todo in this directory, for the filename convention, frontmatter shape and expected level of detail.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-03 and the `<deferred>` section's ruff entry: the measured symptom, the three candidate NixOS-local repairs, and that `nixpkgs#ruff` is 0.15.14, inside the project's `ruff>=0.15,<0.16` floor.
  </read_first>
  <action>
    **Part A — assert SC#7 mechanically.** Let `BASE` be the pre-phase SHA recorded in Step 0. Run
    and record verbatim:
    - `git diff --stat $BASE..HEAD` — the full phase diff, so the reader sees every file it touched.
    - `git diff --name-only $BASE..HEAD -- typsphinx/` — must be empty.
    - `git diff --name-only $BASE..HEAD -- .github/ README.md docs/` — must be empty.
    - `git diff -U0 $BASE..HEAD -- pyproject.toml` — read the hunk headers and confirm every changed
      line falls inside the `[project.optional-dependencies] dev` list. Record the hunk headers.
    - A `tomllib` comparison of `[project] dependencies` parsed from `git show $BASE:pyproject.toml`
      against the working tree, asserting the two lists are equal. This is stronger than a grep: it
      proves the runtime dependency set is identical, not merely that the lines look alike.
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest tests/test_preview_version_sync.py -q`
      — the existing three-way guard. Green means the count is still four and all three declaration
      sites still agree.

    Append as `## Step 9 — SC#7 fence assertion`, with a clause-by-clause table: SC#7 clause, the
    command that checks it, the observed result, and MET or NOT MET.

    **Part B — file the routed-out defects.** Create
    `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` following the
    conventions of the existing todo you read. It must record: the measured symptom, that
    `.venv/bin/ruff` is a generic-linux ELF whose interpreter is `/lib64/ld-linux-x86-64.so.2` and
    that invoking it dies with the loader error; that nothing named `ruff` resolves outside the venv,
    so the standard lint command `CLAUDE.md` documents has never run on this machine; that after
    Phase 45.2 the `lint` tox environment reaches and passes `black --check .` and then stops at the
    lint step; the three candidate repairs, all NixOS-local — a nixpkgs-provided binary in
    `flake.nix`, patchelf on the wheel, or system `nix-ld` — and that `nixpkgs#ruff` is 0.15.14,
    inside the project's `ruff>=0.15,<0.16` floor; that CI is unaffected because the Linux runner has
    a working binary; and that D-03 routed this out of Phase 45.2 because every local repair is in
    the category D-18 declined. Name `SEED-003` as its natural companion. Do not propose a fix
    decision — this is a filing, not a plan.

    If Plan 04's Step 6 recorded a `docs-pdf` failure whose cause is outside the dependency and
    `tox.ini` scope (D-19), or Task 1 isolated an unrelated CI failure (D-18), file each as its own
    todo in the same directory with a descriptive `2026-08-11-<slug>.md` name and cross-reference it
    from Step 8 or Step 9. If there is nothing else to file, write one sentence in Step 9 saying so
    explicitly rather than leaving the reader to infer it.
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md &amp;&amp; grep -c '^## Step 9' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest tests/test_preview_version_sync.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only $BASE..HEAD -- typsphinx/` is empty and `git diff --name-only $BASE..HEAD -- .github/ README.md docs/` is empty.
    - The `tomllib` comparison of `[project] dependencies` at `$BASE` and at `HEAD` reports the two lists equal, and Step 9 records both lists.
    - Every hunk header recorded from `git diff -U0 $BASE..HEAD -- pyproject.toml` falls inside the `dev` extra's line range, and Step 9 states the line range it was compared against.
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest tests/test_preview_version_sync.py -q` exits 0.
    - Step 9's table has one row per SC#7 clause, four rows total, each with an explicit MET or NOT MET.
    - `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` exists, is at least 15 lines, names the loader-error symptom, names all three candidate repairs, states the nixpkgs version and the project's version floor, and states that CI is unaffected.
    - `git diff --name-only $BASE..HEAD -- .planning/MILESTONES.md .planning/RETROSPECTIVE.md .planning/milestones/ .planning/PROJECT.md` is empty across the entire phase.
  </acceptance_criteria>
  <done>Step 9 carries a four-row SC#7 clause table with mechanical evidence for each, and every routed-out defect is filed as a todo with its measured symptom and candidate repairs.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the invalidated project memory and write the closing verdict</name>
  <precondition>`45.2-TOOLCHAIN-EVIDENCE.md` contains a `## Step 7` section with an explicit SC#3 disposition sentence. D-12 makes the memory rewrite conditional on that disposition, so it must exist before this task runs.</precondition>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</files>
  <read_first>
    - `/home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/pytest-no-outer-uv-run-in-main-tree.md` — the memory to rewrite. Note its frontmatter keys (`name`, `description`, `metadata`), that its body is written in Japanese like its sibling memory files, and that its closing paragraph about worktree execution remains correct after this phase and must survive.
    - `/home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/MEMORY.md` — the index, whose one-line entry for this memory carries the same claim and must be updated in step with the file.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — Step 7's SC#3 disposition, which decides whether this task rewrites the memory at all, and Steps 1 through 9, which the closing verdict indexes.
    - `.planning/milestones/v0.7.0-phases/41-v0-7-0-release-automation-release-prep/41-GREEN-TREE-EVIDENCE.md` — its closing verdict section (roughly lines 409-441): one bullet per success criterion, naming which Step proves it, ending in an explicit overall sentence. That is the shape to reproduce.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-12 (why the rewrite waits for SC#3) and D-11 (the shim prohibition the verdict certifies).
  </read_first>
  <action>
    **Part A — the memory (D-12).** Read Step 7's SC#3 disposition first and branch on it.

    If SC#3 is MET: rewrite `pytest-no-outer-uv-run-in-main-tree.md` in place. Keep the filename —
    renaming it would break the `MEMORY.md` index link and any wiki-style references to it — and
    keep the body in the language it is currently written in, so it stays consistent with its
    sibling memory files and with the index entry. Rewrite the content to record that the
    prohibition on running the suite through an outer `uv run` in the main checkout was lifted by
    QUA-04 on this date; that the cause was a generic-linux `uv` wheel binary at `.venv/bin/uv`
    which the outer `uv run` put on `PATH` ahead of the working nix-store build, so the tests
    shelling out to a subprocess resolved the unrunnable copy; and that the binary is no longer
    installed. Give the post-fix absolute counts from Step 7 so the record is measured rather than
    asserted. Keep the still-true operational notes: a sync of the dev extra is still a prerequisite
    because `uv run` alone does not install it, and the worktree guidance at the end of the file is
    unchanged and still correct. Update the `description` frontmatter field and the matching
    one-line entry in `MEMORY.md` so the index no longer advertises a prohibition that no longer
    exists.

    If SC#3 is NOT MET: do not touch the memory. D-12's whole point is that rewriting it early would
    leave a wrong procedure in place if the fix turned out incomplete. Record in the closing verdict,
    in one sentence, that the memory was deliberately left unchanged and why.

    **Part B — the closing verdict.** Append `## Closing verdict` to `45.2-TOOLCHAIN-EVIDENCE.md`.
    Write one bullet per success criterion, SC#1 through SC#7, each naming the Step that proves it
    and ending in an explicit disposition. Use the word MET only where a transcript exists; use the
    words NOT MET or NOT VERIFIED otherwise, and say which evidence is missing. Specifically:
    - SC#1 — Step 6's per-environment table. If `lint` stopped at the lint step, say so and name the
      todo filed in Task 2; SC#1 closes on provisioning and command execution both being reached.
    - SC#2 — Step 5, including the non-symlink checks and the second-sync result.
    - SC#3 — Step 7's disposition, quoted rather than re-derived.
    - SC#4 — Step 8's before/after `uv-sync>` lines and the run conclusion.
    - SC#5 — the current `tox.ini` `requires` line and the surviving rewritten comment.
    - SC#6 — Step 4's sweep inventory and the assertion that the REL-04 records are byte-unchanged.
    - SC#7 — Step 9's four-clause table.

    Also add a short subsection recording that the shim prohibited by D-11 was never applied in
    either tree during this phase, with the check that shows it, and a subsection listing every todo
    filed by this phase with a one-line reason each.

    Finish with a working-tree cleanliness check in the shape `41-GREEN-TREE-EVIDENCE.md` uses:
    `git status --porcelain` both scoped and unscoped, recorded verbatim.
  </action>
  <verify>
    <automated>grep -c '^## Closing verdict' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; grep -c 'SC#7' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The closing verdict contains exactly seven success-criterion bullets, labelled SC#1 through SC#7, each naming a Step number and each ending in one of MET, NOT MET or NOT VERIFIED.
    - No bullet reads MET without naming a Step that exists in the file: for each `SC#N ... Step M` bullet, `grep -c "^## Step M" 45.2-TOOLCHAIN-EVIDENCE.md` returns 1.
    - The closing verdict contains a subsection recording the D-11 shim check for both trees, with the command that produced it.
    - The closing verdict lists every todo file this phase created, and each listed path exists under `.planning/todos/pending/`.
    - If Step 7 recorded SC#3 as MET: `/home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/pytest-no-outer-uv-run-in-main-tree.md` has a modification time newer than the phase start, its `description` frontmatter field no longer states the prohibition, and `MEMORY.md`'s entry for it matches the new description.
    - If Step 7 recorded SC#3 as NOT MET: that memory file is byte-unchanged, and the closing verdict says in one sentence that it was deliberately left alone and why.
    - The closing verdict ends with verbatim `git status --porcelain` output, scoped and unscoped.
    - `grep -c 'false positive' 45.2-TOOLCHAIN-EVIDENCE.md` — any occurrence is inside a quotation of the old record or an explicit statement that the framing was wrong, never a claim by this phase about the ruff defect.
  </acceptance_criteria>
  <done>The project memory is either rewritten with measured post-fix counts or deliberately left in place with the reason recorded, and the closing verdict carries a per-criterion disposition backed by an existing Step, the D-11 shim check, the todo inventory and a working-tree cleanliness check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local worktree → `origin` | This plan pushes to the shared milestone branch, which other agents and the developer read. |
| local worktree → GitHub Actions | `gh workflow run` dispatches a workflow that executes with repository credentials on GitHub-hosted runners. |
| CI logs → committed evidence | Runner log output is transcribed verbatim into a committed artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45.2-15 | Tampering | the shared `gsd/v0.7.1-bug-fix-round` branch | high | mitigate | The push is the first action of Task 1, before any file is written, so it carries exactly the merged milestone tip and never an unmerged worktree commit. The pushed SHA is recorded and re-checked against `git ls-remote` and against the CI run's `headSha`, so a push of the wrong commit is detected rather than assumed away. |
| T-45.2-02 | Spoofing | `gh workflow run` | low | accept | Dispatch uses the developer's already-authenticated `gh` credentials against the project's own existing workflow. No new secret is created, no workflow permission is changed, and `ci.yml` is asserted byte-unchanged across the phase. |
| T-45.2-16 | Tampering | `.github/workflows/ci.yml` | high | mitigate | D-17 forbids adding an assertion step; the prohibition is encoded and checked by `git diff --name-only $BASE..HEAD -- .github/` being empty, so a workflow edit cannot ship under cover of "improving the evidence". |
| T-45.2-04 | Information disclosure | CI logs transcribed into the evidence file | medium | mitigate | Before committing, scan the transcribed log excerpts for anything matching a credential shape (`gh[pousr]_`, `pypi-`, `AKIA`, `Bearer `) and redact it. Copy only the `uv-sync>`, `freeze>`, command and conclusion lines rather than whole job logs. |
| T-45.2-03 | Repudiation | the closing verdict | high | mitigate | Every MET bullet must name a Step that actually exists in the file, and the acceptance criteria check that correspondence mechanically; a criterion with no transcript is recorded as NOT VERIFIED in those words. |
| T-45.2-17 | Tampering | the project memory store | medium | mitigate | The rewrite is gated on Step 7's recorded SC#3 disposition (D-12), so an incomplete fix cannot silently retire the workaround that is still needed. The `MEMORY.md` index entry is updated in the same task, so the index and the file cannot diverge. |
</threat_model>

<artifacts_this_phase_produces>
New symbols created by Phase 45.2 (listed in every plan so drift verification excludes them):

- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — new file, created by Plan 01; this plan appends `## Step 8`, `## Step 9` and `## Closing verdict`.
- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/COVERAGE.md` — new file, written at plan time.
- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-0{1..5}-SUMMARY.md` — new files, one per plan.
- `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` — new todo filed by this plan (D-03).
- `.planning/todos/pending/2026-08-11-<slug>.md` — conditional new todos filed by this plan for a D-19 out-of-scope `docs-pdf` cause and for a D-18 unrelated CI failure, if either occurred.
- `pyproject.toml` `[project.optional-dependencies] dev` gains the requirement name `tox-uv-bare` — new symbol, created by Plan 02.
- `tox.ini` gains an `extras` key on `[testenv]`, `[testenv:lint]`, `[testenv:type]` and `[testenv:cov]`, and a `runner` key on `[testenv:cov]` — new keys, created by Plan 02.
</artifacts_this_phase_produces>

<verification>
- A completed `workflow_dispatch` CI run exists on `gsd/v0.7.1-bug-fix-round` at a SHA containing this phase's changes, and its before/after `uv-sync>` and `freeze>` lines are recorded.
- `.github/workflows/ci.yml` is byte-unchanged across the whole phase.
- SC#7's four clauses each have a mechanical check with a recorded result.
- The routed-out defects are filed as todos rather than fixed or reframed.
- The project memory is retired only on a proven SC#3, and `MEMORY.md` agrees with it.
- The closing verdict gives every success criterion an explicit disposition backed by an existing Step.
</verification>

<success_criteria>
- SC#4 is proven by a real run, not argued from the workflow file — the mistake v0.7.0 made with REL-04.
- SC#7 is asserted mechanically over this phase's own diff, so Phase 46 inherits a fence it can rely on.
- Every routed-out defect leaves the phase as a filed todo with its measured symptom, and none is reframed as an environmental false positive.
- The evidence file closes with a per-criterion verdict that cannot claim MET without a transcript.
</success_criteria>

<output>
Create `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-05-SUMMARY.md` when done.
</output>
