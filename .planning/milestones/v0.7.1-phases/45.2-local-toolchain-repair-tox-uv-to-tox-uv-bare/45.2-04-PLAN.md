---
phase: 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare
plan: 04
type: execute
wave: 3
depends_on: ["45.2-02", "45.2-03"]
files_modified:
  - .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md
autonomous: true
requirements: [QUA-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After a clean uv sync --extra dev in /home/yuta/Documents/typsphinx, neither .venv/bin/uv nor .venv/bin/uvx exists, and importlib.metadata.distribution('uv') raises PackageNotFoundError — checked against the live environment, not against the lockfile (SC#2)"
    - "tox -e lint, tox -e type, tox -e docs-html and tox -e docs-pdf all start, provision, and reach their commands on the maintainer's machine with no TOX_UV_PATH set and no other environment override, with every transcript recorded verbatim (SC#1)"
    - "The full pytest suite run under an outer uv run pytest in the main tree has an absolute failure count strictly lower than Plan 01's pre-fix baseline, and none of the remaining failures is an exit-127 or dynamically-linked-executable failure of the uv binary (SC#3, D-14)"
    - "Running uv sync --extra dev a second time against the same tree does not reinstall a uv or uvx executable into .venv/bin — the post-fix state is stable under repetition, not an artifact of one particular sync"
    - "Every before/after comparison in the census is made over sorted sets of pytest node ids rather than over a raw diff of console output, so a change in collection order cannot masquerade as a change in results"
  artifacts:
    - path: ".planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md"
      provides: "Post-fix Steps 5, 6 and 7 with verbatim main-tree transcripts and the before/after deltas"
      contains: "## Step 5"
  key_links:
    - from: "uv.lock"
      to: "/home/yuta/Documents/typsphinx/.venv/bin"
      via: "uv sync --extra dev provisions the main-tree environment from the regenerated lock and produces no uv executable"
      pattern: "PackageNotFoundError"
    - from: "tox.ini"
      to: ".tox/lint"
      via: "extras = dev makes uv-venv-lock-runner install black and ruff into the tox environment rather than the run borrowing them from .venv/bin via PATH"
      pattern: "extras = dev"
  prohibitions:
    - "Never create /home/yuta/Documents/typsphinx/.venv/bin/uv or .venv/bin/uvx as a symlink or copy pointing at a nix-store or system uv (D-11). SC#2 requires those paths to be absent; re-applying the shim here would make this plan certify its own contradiction."
    - "Never modify .planning/MILESTONES.md, .planning/RETROSPECTIVE.md, anything under .planning/milestones/, .planning/PROJECT.md line 951, .planning/STATE.md line 425 or .planning/STATE.md line 587 (D-08). Those six records describe REL-04's release-job failure — a different, real bug — and are the recorded justification for an open requirement."
    - "Never set TOX_UV_PATH, prepend a directory to PATH, or otherwise override the environment for any SC#1 measurement. SC#1 explicitly requires the runs to succeed with no environment override; an override turns the proof into a demonstration of the workaround this phase declined."
    - "Never attempt to repair the ruff generic-linux-ELF defect, and never reframe it as an environmental false positive so that tox -e lint can be reported green (D-03). Record the stop verbatim and let Plan 05 file the todo."
    - "Never record a measurement that was not actually run, and never paraphrase command output. Any step that could not be completed says NOT SATISFIED or NOT RUN in those words with the reason. This phase exists because the project believed unverified claims about its own toolchain."
---

<objective>
Prove the repair on the machine it was made for: the maintainer's main checkout.

Purpose: Plan 02 proved the fix end to end inside a fresh worktree, which is a clean-room result.
SC#1, SC#2 and SC#3 are claims about `/home/yuta/Documents/typsphinx` specifically — the tree whose
`.venv` has carried a generic-linux `uv` ELF that NixOS cannot exec, and against which Plan 01
recorded the pre-fix baseline. This plan re-syncs that environment from the now-fixed lock and
takes the after half of every measurement.

Output: `45.2-TOOLCHAIN-EVIDENCE.md` gains Step 5 (SC#2 after-half plus the idempotency check),
Step 6 (SC#1 — the full tox matrix), and Step 7 (SC#3 — the post-fix census and its delta against
Plan 01's baseline).

This plan makes NO source edit. It is measurement only.
</objective>

<wave_and_isolation_contract>
**Read this before running any command.** This plan is where D-13's main-tree requirement and
`CLAUDE.md`'s standing worktree isolation have to be reconciled most carefully.

1. **Provision your own worktree first, per `CLAUDE.md`:**
   `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` with the worktree as the
   working directory. You need it for your own tooling. You must NOT measure it — a worktree
   `.venv` is a clean-room environment, and Plan 02 already reported that result.
2. **Every measurement command targets the main tree explicitly**, via
   `--directory /home/yuta/Documents/typsphinx` (for `uv run`), `--project
   /home/yuta/Documents/typsphinx` (for `uv sync`), or an absolute path under
   `/home/yuta/Documents/typsphinx/.venv/bin/`. Never use `cd`.
3. **Every main-tree command is prefixed with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT`.**
   Without those unsets, `uv` re-points the already-activated worktree venv at the main project and
   the measurement is meaningless — and worse, it can leave the main tree in a state that is
   neither the pre-fix nor the post-fix one.
4. **This plan is the ONLY plan in wave 3.** That is the structural guarantee that no concurrent
   agent mutates the shared main-tree `.venv`/`.tox` while the census runs. Waves 1 and 2 have
   merged back before this plan starts, so the main tree's working files already carry Plan 02's
   `pyproject.toml`, `tox.ini` and `uv.lock` — the `<precondition>` on Task 1 asserts exactly that
   before anything is measured.
5. **Nothing this plan writes to the main tree is git-tracked.** The only main-tree side effects
   are `/home/yuta/Documents/typsphinx/.venv` and `/home/yuta/Documents/typsphinx/.tox`, both
   gitignored. The one git-tracked file this plan writes lives in your worktree.
</wave_and_isolation_contract>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md
@.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-sync the main tree and record Step 5 — SC#2 after-half plus the second-sync check</name>
  <precondition>The main tree already carries Plan 02's change: `git -C /home/yuta/Documents/typsphinx show HEAD:pyproject.toml` contains `tox-uv-bare` and `git -C /home/yuta/Documents/typsphinx show HEAD:tox.ini` contains `requires = tox-uv-bare~=1.35`. If either check fails, wave 2 has not merged back and this plan must not run — HALT rather than measuring a half-applied tree.</precondition>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — Steps 0 through 4, in particular Step 1's pre-fix environment census, which this task's Step 5 is the after-half of. Match its formatting and continue its numbering.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-11 (the shim is prohibited), D-13 (why the main tree is the measurement target) and `<specifics>` item 7 (the exact pre-fix facts, so the after-half is a like-for-like comparison).
    - `.planning/ROADMAP.md` § Phase 45.2 SC#2 — the criterion's own wording: checked with `importlib.metadata`, not by looking at the lockfile.
  </read_first>
  <action>
    Append `## Step 5 — Post-fix environment census (SC#2)` to `45.2-TOOLCHAIN-EVIDENCE.md`.

    Record the main tree's `git rev-parse HEAD` first, so this step's provenance is separable from
    Plan 01's.

    Then run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --project
    /home/yuta/Documents/typsphinx` and record its verbatim output — in particular the lines
    showing which distributions were removed. The pre-fix census recorded `uv` and the meta package
    present; the sync output is where their removal becomes visible.

    Then record verbatim output for each of the following, mirroring Step 1's checks exactly so the
    before/after reads as a pair:
    - `ls -la /home/yuta/Documents/typsphinx/.venv/bin/` filtered to any entry whose name begins
      with `uv` — the expected result is no such entry. If the listing is empty, say so in words
      next to the empty block rather than leaving a bare empty fence.
    - `test -e` and `test -L` on both `.venv/bin/uv` and `.venv/bin/uvx`, recording the exit codes.
      Both must be absent AND not symlinks — the second check is what proves the D-11 shim was not
      applied to manufacture the result.
    - `/home/yuta/Documents/typsphinx/.venv/bin/python -c` attempting
      `importlib.metadata.distribution('uv')` and printing the exception type name. SC#2 requires
      this check specifically, rather than reading `uv.lock`.
    - The same `importlib.metadata` census over `tox`, `tox-uv` and `tox-uv-bare`, so the delta
      against Step 1's four-name census is complete and explicit.
    - `command -v uv` and `file` on what it resolves to — the working nix-store build must still be
      on PATH. Removing the wheel binary must not have removed `uv` from the machine.

    Then run the sync a SECOND time, unchanged, and record its output plus a repeat of the
    `test -e` checks on both paths. A dependency repair that only holds until the next `uv sync` is
    not a repair; this is the check that the post-fix state is stable under repetition rather than
    an artifact of one particular invocation. State the result explicitly in prose: whether a
    second sync reinstalls a `uv` executable into `.venv/bin`.

    Under each block write one short paragraph tying the output to SC#2. Do not paraphrase output
    into prose.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --project /home/yuta/Documents/typsphinx &amp;&amp; test ! -e /home/yuta/Documents/typsphinx/.venv/bin/uv &amp;&amp; test ! -e /home/yuta/Documents/typsphinx/.venv/bin/uvx &amp;&amp; test ! -L /home/yuta/Documents/typsphinx/.venv/bin/uv &amp;&amp; grep -c '^## Step 5' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e /home/yuta/Documents/typsphinx/.venv/bin/uv` and `test ! -e /home/yuta/Documents/typsphinx/.venv/bin/uvx` both succeed, and `test ! -L` succeeds for both paths.
    - `/home/yuta/Documents/typsphinx/.venv/bin/python -c "import importlib.metadata as m; m.distribution('uv')"` exits non-zero and the recorded exception type name is `PackageNotFoundError`.
    - `/home/yuta/Documents/typsphinx/.venv/bin/python -c "import importlib.metadata as m; print(m.version('tox-uv-bare'))"` prints a `1.35.x` version.
    - `command -v uv` still resolves to a path under `/nix/store` — the machine still has a working `uv`.
    - Step 5 records the second `uv sync --extra dev` run and, after it, a repeat of both `test -e` checks, with the outcome stated in words.
    - `grep -c '^## Step 5' 45.2-TOOLCHAIN-EVIDENCE.md` returns 1.
    - Step 5 contains a 40-hex-character commit SHA for the main tree.
    - `git -C /home/yuta/Documents/typsphinx status --porcelain` is empty, or contains only untracked/ignored build artefacts — the sync changed no git-tracked main-tree file.
  </acceptance_criteria>
  <done>The main tree's environment is re-synced from the fixed lock, `.venv/bin/uv` and `.venv/bin/uvx` are absent and are not symlinks, no `uv` distribution is installed, and a second sync is recorded as not reinstating either binary.</done>
</task>

<task type="auto">
  <name>Task 2: Record Step 6 — the full tox matrix on the maintainer's machine (SC#1)</name>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — Step 2, the pre-fix tox transcript this step is the after-half of.
    - `tox.ini` — the post-fix state carried by the main tree: `requires = tox-uv-bare~=1.35`, seven environments, four with `extras = dev` and three with `extras = docs`.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-03 (the `ruff` defect is routed out, is NOT reframed, and gets a todo rather than a fix), D-19 (a `docs-pdf` failure outside the dependency/`tox.ini` scope is isolated and recorded, and SC#1 then closes on "provisioning and command execution both reached"), and `<specifics>` item 5 (the measured tool-by-tool table: `black`, `mypy`, `pytest`, `sphinx-build` all run; `ruff` and `uv` do not).
    - `.planning/ROADMAP.md` § Phase 45.2 SC#1 — including its statement that the `docs-pdf` run is the one this phase must not skip, because Phase 46's SC#3 depends on it.
  </read_first>
  <action>
    Append `## Step 6 — Post-fix tox matrix (SC#1)` to `45.2-TOOLCHAIN-EVIDENCE.md`.

    Run each of these against the MAIN TREE, recording the exact command, the verbatim combined
    output and the numeric exit code captured with `echo $?`:
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx tox -e type`
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx tox -e lint`
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx tox -e docs-html`
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx tox -e docs-pdf`

    Before the first one, record `env | grep -c TOX_UV_PATH` output as proof that no environment
    override is in play. SC#1 requires all four to work with no override; a transcript taken with
    one set proves nothing.

    For every run, capture and call out the `uv-sync>` line tox prints. It names the `uv` binary tox
    resolved. Pre-fix that line pointed into `.venv/bin`; post-fix it must resolve a binary outside
    `.venv/bin` via `shutil.which("uv")`. That single line changing is half of the phase's thesis,
    and D-17 makes the CI counterpart of it SC#4's evidence.

    Expected outcomes, all of which must be recorded as observed rather than as expected:
    - `type` exits 0.
    - `lint` reaches and passes `black --check .`, then stops at `ruff check .` with the
      generic-linux-ELF loader error. Record it verbatim. Do NOT repair it, do NOT add
      `allowlist_externals`, and do NOT describe it as an environmental false positive. It is defect
      ③, D-03 routes it out of this phase, and Plan 05 files the todo. In the explanatory paragraph,
      state plainly that SC#1 closes for `lint` on provisioning and command execution both being
      reached, with the `ruff` stop named as a separate defect.
    - `docs-html` and `docs-pdf` exercise `extras = docs`, which was already correct before this
      phase, and are the environments most likely to work unchanged. If `docs-pdf` fails for a
      reason demonstrably outside the dependency and `tox.ini` scope, apply D-19: isolate the cause,
      record it in this step with the verbatim error, and note that Plan 05 files it as a todo. Do
      not chase it.

    Close the step with a per-environment verdict table: environment, exit code, whether
    provisioning was reached, whether commands were reached, and the SC#1 disposition (MET, or MET
    with a named out-of-scope stop, or NOT MET). Use the words NOT MET if that is the result.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx tox -e type &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx tox -e docs-pdf &amp;&amp; grep -c '^## Step 6' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Step 6 contains four distinct fenced command blocks, one per environment, each followed by a verbatim output block and a recorded numeric exit code.
    - `tox -e type` is recorded with exit code 0.
    - Step 6 records the `uv-sync>` line for at least one environment, and that line's `uv` path is NOT under `/home/yuta/Documents/typsphinx/.venv/bin`.
    - Step 6 records the output of `env | grep -c TOX_UV_PATH` as 0.
    - The `lint` transcript shows `black --check .` being executed by tox rather than failing with a missing-executable error, and any subsequent failure is attributed in prose to the `ruff` loader defect with a pointer to the todo Plan 05 files.
    - Step 6 ends with a per-environment verdict table containing an explicit SC#1 disposition for each of the four environments.
    - `git -C /home/yuta/Documents/typsphinx status --porcelain -- tox.ini pyproject.toml uv.lock typsphinx/ tests/` is empty — the matrix run changed no tracked file.
    - `git status --porcelain` in the worktree shows only the evidence file modified.
  </acceptance_criteria>
  <done>All four environments have verbatim main-tree transcripts with exit codes, the `uv-sync>` resolution is shown to come from outside `.venv/bin`, no environment override was used, and each environment has an explicit SC#1 disposition.</done>
</task>

<task type="auto">
  <name>Task 3: Record Step 7 — post-fix full-suite census and the before/after delta (SC#3)</name>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — Step 3, the pre-fix census. It carries the baseline absolute counts, the two sorted failing-node-id sets, and the explicit rule that all before/after comparison is over sorted node-id sets rather than over raw console output. Follow that rule.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-14 (the census is the full suite's absolute counts on both sides, because a 5-module-only measurement would not show whether other failures hide behind the same cause) and D-09 (the five `uv run`-invoking modules are the regression detector).
    - `.planning/ROADMAP.md` § Phase 45.2 SC#3 — including its note that "45-as-a-count has never been verified against this cause".
    - `/home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/pytest-no-outer-uv-run-in-main-tree.md` — the standing memory whose procedure this measurement is about to invalidate. Do not rewrite it here; Plan 05 owns that, after this step proves SC#3 (D-12).
  </read_first>
  <action>
    Append `## Step 7 — Post-fix full-suite census (SC#3)` to `45.2-TOOLCHAIN-EVIDENCE.md`.

    Run the same two commands Step 3 ran, against the main tree, so the comparison is like-for-like:
    - Measurement run:
      `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx pytest -q --tb=no -rf`
    - Control run: the absolute interpreter path
      `/home/yuta/Documents/typsphinx/.venv/bin/python -m pytest -q --tb=no -rf`, with no outer
      `uv run` wrapping it.

    Record for each: the verbatim final summary line, the full `FAILED ...` short-summary list,
    total passed/failed/skipped/errors, and wall time.

    Then write `### Pass/fail delta against the Plan 01 baseline`, presenting the comparison the way
    `41-GREEN-TREE-EVIDENCE.md` presents its pass-count delta — a table with the pre-fix number, the
    post-fix number and the difference, for each of passed, failed, skipped, errors, and for both
    the measurement and the control run.

    Then write `### Node-id set delta`. Extract the failing node ids from both post-fix runs, sort
    them, and compare against Step 3's sorted sets with `comm` or an equivalent set operation. Never
    diff raw console output: pytest does not guarantee stable ordering between runs, so an ordering
    change would otherwise read as a results change. State explicitly:
    - which node ids failed pre-fix and pass post-fix (the population SC#3 claims),
    - which node ids fail on both sides (pre-existing failures unrelated to this defect),
    - which node ids fail only post-fix (regressions — if this set is non-empty, say so plainly and
      investigate before writing a verdict),
    - whether every node id in the first set belongs to one of the five modules
      `tests/test_examples_basic.py` and
      `tests/test_integration_{basic,advanced,multi_doc,nested_toctree}.py`.

    Finally, grep both post-fix outputs for the exit-127 signature and the
    dynamically-linked-executable message and record the count. SC#3 is about a specific failure
    cause disappearing, not merely about a smaller number; a lower count with the same cause still
    present is NOT SC#3 being met, and must be written up in those words.

    Close with an explicit SC#3 disposition: MET, or NOT MET with the reason. Include the honesty
    statement in the shape `41-GREEN-TREE-EVIDENCE.md` uses: if the post-fix measurement run had
    shown the same failures as the pre-fix run, this section would say NOT MET in those words.
  </action>
  <verify>
    <automated>grep -c '^## Step 7' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; grep -c '^### Node-id set delta' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; grep -c '^### Pass/fail delta' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Step 7 contains two pytest summary lines, each matching `grep -Eq '[0-9]+ (passed|failed)'`, one for the outer-`uv run` measurement run and one for the direct-interpreter control run.
    - The `### Pass/fail delta against the Plan 01 baseline` table gives pre-fix, post-fix and difference figures for passed, failed, skipped and errors, for both runs.
    - The `### Node-id set delta` section reports all four sets named in the action, each as an explicit count, and states whether the post-fix-only failure set is empty.
    - The post-fix measurement run's failure count is strictly lower than the pre-fix measurement run's failure count recorded in Step 3; if it is not, Step 7 says NOT MET in those words with the reason.
    - The count of exit-127 / dynamically-linked-executable occurrences in the post-fix measurement output is recorded, and Step 7 states in prose whether it is zero.
    - Step 7 ends with an explicit SC#3 disposition sentence using the word MET or the words NOT MET.
    - `git -C /home/yuta/Documents/typsphinx status --porcelain -- tests/ typsphinx/` is empty — the census changed no source file.
  </acceptance_criteria>
  <done>The post-fix census is recorded with absolute counts, the pass/fail delta and the sorted node-id set delta against Plan 01's baseline are both present, the residual exit-127 count is stated, and SC#3 carries an explicit disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree executor → main-tree environment | This plan re-provisions `/home/yuta/Documents/typsphinx/.venv` from outside the main tree. The main tree is the maintainer's working environment, not a disposable sandbox. |
| measurement → success claim | Step 5, 6 and 7 transcripts are the sole evidence for SC#1, SC#2 and SC#3. Whatever is written here is what downstream Phase 46 and the verifier trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45.2-03 | Repudiation | `45.2-TOOLCHAIN-EVIDENCE.md` Steps 5-7 | high | mitigate | Each step records the main tree's `git rev-parse HEAD`, the exact command, verbatim output and an explicitly captured `echo $?`. The `test ! -L` checks on both `uv` paths make a shim-manufactured SC#2 result detectable, and the residual exit-127 count makes a "smaller number, same cause" outcome impossible to report as MET. |
| T-45.2-07 | Elevation of privilege | `/home/yuta/Documents/typsphinx/.venv/bin` | medium | mitigate | The D-11 prohibition forbids re-introducing a `uv` executable there; the acceptance criteria assert absence AND non-symlink status for both `uv` and `uvx`, so the prohibition is checked rather than merely stated. |
| T-45.2-14 | Denial of service | main-tree `.venv` | medium | mitigate | The `<precondition>` halts the plan if wave 2 has not merged back, preventing a half-applied sync. `uv sync` is idempotent and Task 1 proves it by running it twice; an interrupted run is recovered by re-running the same command. `command -v uv` is asserted still resolvable so the machine keeps a working `uv` after the wheel binary is removed. |
| T-45.2-04 | Information disclosure | committed transcripts | low | mitigate | Before committing, scan the evidence file for anything matching a credential shape (`gh[pousr]_`, `pypi-`, `AKIA`, `Bearer `) and redact it. |
| T-45.2-06 | Tampering | main-tree `.tox` | low | accept | `tox` runs create `.tox/`, which is gitignored (`.gitignore:31`); the acceptance criteria assert the main tree's tracked status stays clean. |
</threat_model>

<artifacts_this_phase_produces>
New symbols created by Phase 45.2 (listed in every plan so drift verification excludes them):

- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — new file, created by Plan 01; this plan appends `## Step 5`, `## Step 6` and `## Step 7`.
- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/COVERAGE.md` — new file, written at plan time.
- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-0{1..5}-SUMMARY.md` — new files, one per plan.
- `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` — new todo filed by Plan 05 (D-03).
- `.planning/todos/pending/2026-08-11-<slug>.md` — conditional new todo filed by Plan 05 only if `tox -e docs-pdf` fails for a cause outside this phase's dependency/`tox.ini` scope (D-19).
- `pyproject.toml` `[project.optional-dependencies] dev` gains the requirement name `tox-uv-bare` — new symbol, created by Plan 02.
- `tox.ini` gains an `extras` key on `[testenv]`, `[testenv:lint]`, `[testenv:type]` and `[testenv:cov]`, and a `runner` key on `[testenv:cov]` — new keys, created by Plan 02.
</artifacts_this_phase_produces>

<verification>
- `45.2-TOOLCHAIN-EVIDENCE.md` carries Steps 5, 6 and 7 with verbatim main-tree transcripts.
- SC#2 is proven on the live environment via `importlib.metadata`, with the non-symlink check making the D-11 shim detectable, and the second sync recorded.
- SC#1 has a per-environment verdict table across `type`, `lint`, `docs-html` and `docs-pdf`, taken with no environment override.
- SC#3 has an absolute before/after census, a sorted node-id set delta, a residual exit-127 count and an explicit disposition.
- No git-tracked file in either tree was modified except the evidence file.
</verification>

<success_criteria>
- SC#1, SC#2 and SC#3 each carry an explicit MET or NOT MET disposition backed by verbatim main-tree output.
- The post-fix state is shown stable under a second `uv sync`.
- The comparison methodology is set-based, so an ordering change cannot be mistaken for a results change.
- D-03's `ruff` stop and any D-19 `docs-pdf` cause are recorded as out-of-scope, not repaired and not reframed.
</success_criteria>

<output>
Create `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-04-SUMMARY.md` when done.
</output>
