---
phase: 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare
plan: 03
type: execute
wave: 2
depends_on: ["45.2-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - tests/test_pdf_render_gate.py
  - tests/test_preview_smoke_gate.py
  - tests/test_epigraph_render_gate.py
  - tests/test_heading_depth_render_gate.py
  - tests/test_admonition_bucket_render_gate.py
  - tests/test_admonition_locale_title_precedence_gate.py
  - tests/test_authors_pipeline_stage_gate.py
  - tests/test_package_template_routing.py
  - .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md
autonomous: true
requirements: [QUA-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A repo-wide grep performed at execution time — not a file list copied from CONTEXT.md — is the authoritative inventory of wrong-diagnosis sites, and its full hit list including in-scope/out-of-scope disposition per hit is recorded in 45.2-TOOLCHAIN-EVIDENCE.md (SC#6, milestone invariant #4)"
    - "Every live record that frames the failures as uv being absent from PATH, or as an unfixable NixOS environmental false positive, is corrected; the diagnosis text in the test docstrings is moved to past tense with an added rationale rather than rewritten, because that text is factually correct (D-06)"
    - "ROADMAP.md § Phase 45.2 and REQUIREMENTS.md § QUA-04 both say 5 rather than 13 test modules, SC#1 acknowledges that the lint environment reaches black and stops at ruff, SC#5 names the extras lines alongside the requires rename, and SC#6's premise about the docstrings is corrected (D-05, D-03, D-02, D-06, D-10)"
    - "STATE.md carries one sentence distinguishing this milestone's two unrelated exit-127 causes: the local .venv/bin/uv ELF mismatch closed by QUA-04, and the create-release setup-uv omission that is REL-04 (D-08)"
    - "The five modules that genuinely invoke uv as a subprocess are byte-unchanged, so the regression detector for this defect class survives (D-09)"
    - "A record that sits in a file that is partly in scope and partly out of scope separates rather than merges: .planning/STATE.md is edited only in its Phase-45.2-diagnosis region, and its two REL-04 release-job records remain byte-identical in the same file"
    - "A named candidate file that turns out to hold zero in-scope hits is recorded in the evidence file as an explicit zero-hit finding with the grep that produced it, never silently skipped; and a docstring whose wording has drifted from the canonical text halts that file's edit for individual reading rather than being force-matched"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 45.2 goal and SC#1/SC#3/SC#5/SC#6 amended per D-10"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QUA-04 body amended per D-05/D-02/D-03"
    - path: ".planning/STATE.md"
      provides: "corrected diagnosis framing plus D-08's two-causes sentence"
    - path: "tests/test_pdf_render_gate.py"
      provides: "the canonical _run_sphinx_build_typst docstring, tense-corrected, which the other modules clone"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the same three factual corrections must land identically in both siblings"
      pattern: "QUA-04"
  prohibitions:
    - "Never create .venv/bin/uv or .venv/bin/uvx as a symlink or copy pointing at a nix-store or system uv (D-11). SC#2 requires those paths to be absent after the fix; re-applying the shim out of habit invalidates the phase's own verification."
    - "Never modify .planning/MILESTONES.md, .planning/RETROSPECTIVE.md, anything under .planning/milestones/, .planning/PROJECT.md line 951, .planning/STATE.md line 425 or .planning/STATE.md line 587 (D-08). Those six records describe REL-04's release-job failure — a different, real bug whose wording is correct — and they are the recorded justification for a requirement this milestone is trying to close. A mechanical repo-wide sweep that deletes them destroys that justification."
    - "Never rewrite the diagnosis inside the test docstrings. D-06 measured that text as factually correct; only its tense is stale. Replacing the explanation with a new one destroys a correct record on the pretext of correcting it."
    - "Never edit PROJECT.md's HTML-commented prior footers or any archived milestone record to make them agree with today's understanding (D-07). They are dated records of what was believed at the time; rewriting them destroys the audit trail rather than correcting it."
    - "Never normalize the five modules that invoke uv as a subprocess onto sys.executable -m sphinx (D-09). They are SC#3's evidence and the project's only regression detector for this defect class."
---

<!-- planner-discipline-allow: create-release -->
<!-- planner-discipline-allow: command not found -->
<!-- planner-discipline-allow: false positives -->

<objective>
Correct every live record of the wrong diagnosis, and amend `ROADMAP.md` and `REQUIREMENTS.md`
where they are factually wrong about this phase.

Purpose: the project has carried a wrong explanation of these failures for several milestones —
`uv` framed as "not on PATH" when it is on PATH and is simply the wrong build, and 45 pytest
failures framed as unfixable NixOS environmental false positives when they are a fixable dependency
defect. That framing forced PR #131's review to compare against a `main` baseline instead of
reading absolute counts. Two further corrections belong to the phase's own paperwork: the
"13 test modules" figure is a miscount of 5, and SC#6's stated premise about the test docstrings is
itself wrong.

Output: an execution-time grep inventory recorded as evidence, tense-corrected docstrings across
the modules that inventory finds, and amended `ROADMAP.md`, `REQUIREMENTS.md` and `STATE.md`.
</objective>

<wave_and_isolation_contract>
This plan runs in a worktree in wave 2, in parallel with Plan 02. Plan 02 owns `pyproject.toml`,
`tox.ini` and `uv.lock`; this plan owns `.planning/` and `tests/`. There is no file overlap.

**This plan issues NO command against `/home/yuta/Documents/typsphinx`.** Plan 01 recorded the
pre-fix baseline there and Plan 04 will take the post-fix measurement there; a stray `uv sync` or
`pytest` run against the main tree from here would contaminate both. Provision your own worktree
per `CLAUDE.md` (`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`) and run
everything with `uv run` inside the worktree.

**On editing `STATE.md` and `ROADMAP.md` from a worktree.** The standing worktree instruction "do
not modify STATE.md or ROADMAP.md" exists to stop executors racing the orchestrator on *tracking*
writes — frontmatter, `## Current Position`, progress percentages, plan checkboxes. This plan's
deliverable is a *prose content correction* in different regions of those files, mandated by D-06,
D-08 and D-10. Make every edit with the `Edit` tool as a scoped replacement, never with `Write`,
and touch none of the tracking regions. If the orchestrator's post-wave write conflicts, the
orchestrator resolves it.
</wave_and_isolation_contract>

<assumption_delta_decision>
The assumption-delta detector fired `pluralization` on this phase's scope. Interpreted against the
phase's actual subject matter, the real singular-to-plural transition is this: the project's records
assume there is ONE `exit 127` cause. There are two, and this milestone carries both.

- **Noun now primary:** the *cause* of an `exit 127` / `Could not start dynamically linked
  executable` failure — no longer a single implied cause, but an enumerated set of two.
- **Decision: `promote`.** The generalized noun becomes primary in `STATE.md`: the record names
  cause (a) the local `.venv/bin/uv` generic-linux-ELF mismatch, closed by QUA-04, and cause (b)
  the `create-release` job's missing `astral-sh/setup-uv` step, which is REL-04 and remains open.
  The previously-singular framing is demoted to naming cause (a) specifically rather than standing
  for the whole category. This is D-08's mandated sentence, and Task 3 implements it.
- **Why not `add-alongside`:** leaving the old singular framing in place and appending a note about
  a second cause is exactly what makes SC#6's repo-wide grep dangerous — an executor sweeping for
  the singular framing would hit cause (b)'s records and delete the justification for an open
  requirement.
- **Companion invariant:** the suggested contract test — a guard asserting the six REL-04 record
  sites stay byte-unchanged across any future diagnosis sweep — is NOT added as a test file.
  `45.2-CONTEXT.md` `<deferred>` already declines test-infrastructure expansion in a repair phase.
  It is encoded instead as Task 1's and Task 3's acceptance criteria over `git diff`.
</assumption_delta_decision>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md
@.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the discovery-time sweep and record the authoritative hit inventory</name>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-06 (what is actually wrong versus what only goes stale), D-07 (the sweep reaches live records only), D-08 (the six REL-04 sites and why a mechanical sweep is dangerous), and the `<canonical_refs>` section "Records that must NOT be swept by SC#6's grep".
    - `.planning/ROADMAP.md` § Phase 45.2 SC#6 — the criterion's own wording: a repo-wide grep for the framing at discovery time, milestone invariant #4, "not against a list of files this criterion happens to name".
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — the running evidence file created by Plan 01; continue its Step numbering and formatting.
  </read_first>
  <action>
    Run the sweep now, at execution time. SC#6 requires the grep to be authoritative — the file
    lists in `45.2-CONTEXT.md` and `45.2-PATTERNS.md` are a starting inventory, not a closed set,
    and the planner already found one concrete discrepancy that proves it (see below).

    Run at least these five patterns over the repository, excluding `.git`, `.venv`, `.tox`,
    `_build` and `node_modules`, and record every hit as `path:line` with enough surrounding text
    to judge it:
    - `Could not start dynamically linked`
    - `command not found`
    - `not on PATH`
    - `non-Nix`
    - `false positive`

    Then build a disposition table in the evidence file with one row per hit and exactly one of
    three dispositions:
    - **CORRECT** — a live record carrying the wrong diagnosis or the stale tense; Task 2 or Task 3
      edits it.
    - **OUT-OF-SCOPE (REL-04)** — one of the six records naming the v0.7.0 `create-release` job
      failure: `.planning/MILESTONES.md:43`, `.planning/PROJECT.md:951`, `.planning/STATE.md:425`,
      `.planning/STATE.md:587`, `.planning/RETROSPECTIVE.md:369`,
      `.planning/milestones/v0.7.0-REQUIREMENTS.md:226`. Their wording is correct and they are the
      recorded reason REL-04 is open in v0.7.1. Left byte-unchanged.
    - **OUT-OF-SCOPE (archived record)** — anything under `.planning/milestones/`,
      `.planning/RETROSPECTIVE.md`, or a `<!-- Prior: ... -->` HTML-commented footer in
      `.planning/PROJECT.md`. These are dated records of what was believed at the time (D-07).

    Two findings the planner measured on 2026-08-11 that the inventory must reconcile explicitly:
    - `tests/test_heading_depth_render_gate.py` carries the same diagnosis prose at roughly line 53
      but phrases it as "a stray non-Nix package-manager", with no literal `uv` token — so it is
      absent from `45.2-CONTEXT.md` D-05's list of eight, and a `grep -l "uv" tests/` would never
      find it. This is the concrete reason SC#6 mandates a grep over a file list.
    - `tests/test_readthedocs_config.py`'s `uv` mentions at roughly lines 13, 15 and 115 are
      procedural and remain true after this phase (a PyYAML availability note, a standing
      invocation instruction, and an assertion on a Read the Docs config value). Judge it on its
      text, and if it holds no wrong-diagnosis hit, record it as an explicit ZERO-HIT row naming
      the grep that produced the zero — do not silently drop a candidate from the inventory.

    The same zero-hit rule applies to `.planning/PROJECT.md`: the planner measured that its three
    NixOS mentions all sit inside `<!-- Prior: ... -->` commented footers and that its current
    uncommented footers carry no diagnosis framing, so the expected disposition for PROJECT.md is
    zero CORRECT rows. Verify that independently and record it as a zero-hit finding either way.

    Append the whole inventory as `## Step 4 — SC#6 discovery-time sweep` in
    `45.2-TOOLCHAIN-EVIDENCE.md`, with the exact grep commands in fenced blocks above the table.
  </action>
  <verify>
    <automated>grep -c '^## Step 4' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; grep -c 'OUT-OF-SCOPE (REL-04)' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Step 4' 45.2-TOOLCHAIN-EVIDENCE.md` returns 1.
    - Step 4 contains the five grep commands verbatim in fenced blocks, and a disposition table in which every hit carries exactly one of `CORRECT`, `OUT-OF-SCOPE (REL-04)`, `OUT-OF-SCOPE (archived record)`.
    - All six REL-04 paths appear in the table with the `OUT-OF-SCOPE (REL-04)` disposition: `grep -c 'OUT-OF-SCOPE (REL-04)' 45.2-TOOLCHAIN-EVIDENCE.md` returns at least 6.
    - Step 4 contains an explicit row or sentence for `tests/test_heading_depth_render_gate.py` reconciling it against `45.2-CONTEXT.md` D-05's list of eight.
    - Step 4 contains an explicit ZERO-HIT finding for at least `.planning/PROJECT.md`, naming the grep that produced the zero, rather than omitting the file.
    - The number of rows in the disposition table equals the total number of grep hits reported; no hit is present in the raw output but absent from the table.
    - `git status --porcelain` shows only the evidence file modified by this task — the sweep read, it did not edit.
  </acceptance_criteria>
  <done>`45.2-TOOLCHAIN-EVIDENCE.md` Step 4 holds the execution-time grep commands, the complete hit list, and a disposition for every hit including explicit zero-hit findings.</done>
</task>

<task type="auto">
  <name>Task 2: Move the test-docstring diagnosis to past tense across every module the sweep found</name>
  <files>tests/test_pdf_render_gate.py, tests/test_preview_smoke_gate.py, tests/test_epigraph_render_gate.py, tests/test_heading_depth_render_gate.py, tests/test_admonition_bucket_render_gate.py, tests/test_admonition_locale_title_precedence_gate.py, tests/test_authors_pipeline_stage_gate.py, tests/test_package_template_routing.py</files>
  <read_first>
    - `tests/test_pdf_render_gate.py` lines 150-172 — the canonical `_run_sphinx_build_typst` docstring, and lines 208-226 — the SAME prose repeated as an inline `#` comment block in the same file. Both occurrences need the same edit.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-PATTERNS.md` — sections "`tests/test_pdf_render_gate.py::_run_sphinx_build_typst`" (which quotes the canonical text verbatim) and "7 clone modules" (which explicitly warns not to blind-copy one file's exact string over the others without checking for wording drift).
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-06 (the correction is tense and rationale, NOT the diagnosis) and D-09 (the five real-`uv`-invoking modules are never touched).
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — Step 4's inventory from Task 1, which is the authoritative edit list. Where it disagrees with this task's `<files>` line, the inventory wins: add any module it found that is missing here, and skip any listed here that it dispositioned out of scope.
  </read_first>
  <action>
    The text in these docstrings is factually correct. D-06 measured it: "a stray non-Nix `uv`
    binary installed into `.venv/bin` (shadowing the correct Nix-provided `uv` earlier on PATH for
    subprocess children) makes `["uv", "run", ...]` exit 127" is an accurate description of the
    defect. Do NOT rewrite the explanation. What goes stale is only the tense — it describes a
    hazard that no longer exists after this phase.

    For each site the Task 1 inventory dispositioned `CORRECT`:
    1. Read the actual wording at that site first. The eight modules are clones of one another but
      the prose has drifted — some use single backticks, some double backticks, some say
      "package-manager" instead of naming `uv`, and the sentence is split across different line
      breaks in each file. Never apply a canned replacement string; edit each site against the text
      you just read.
    2. Move the hazard description into past tense.
    3. Append the rationale in `45.2-CONTEXT.md` D-06's own words: the cause was removed by QUA-04
      (2026-08-10), and `sys.executable -m sphinx` is kept because it depends on no PATH resolution
      at all — which is a better reason than the hazard ever was, and remains true regardless of
      what is installed in `.venv/bin`.
    4. If a site's wording has drifted so far that the past-tense edit is not obvious, stop editing
      that file and record the wording in the SUMMARY for a human read rather than force-fitting.

    `tests/test_pdf_render_gate.py` contains the prose twice — once as the `_run_sphinx_build_typst`
    docstring and once as an inline `#` comment block roughly 60 lines further down. Edit both.

    Do not touch `tests/test_examples_basic.py` or any of
    `tests/test_integration_{basic,advanced,multi_doc,nested_toctree}.py`. Those five genuinely
    invoke `uv` as a subprocess and are SC#3's regression detector: if the `uv` wheel ever returns
    to the `dev` extra, those five modules are what goes red. D-09 records this so a future reviewer
    does not "fix" the inconsistency.

    These are comment and docstring edits only. No executable line of any test changes.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest --collect-only -q tests/ &gt;/dev/null &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m compileall -q tests/</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD~1 -- tests/` lists only modules the Task 1 inventory dispositioned `CORRECT`, and lists none of `tests/test_examples_basic.py`, `tests/test_integration_basic.py`, `tests/test_integration_advanced.py`, `tests/test_integration_multi_doc.py`, `tests/test_integration_nested_toctree.py`.
    - `git diff HEAD~1 -- tests/ | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -cvE '^[+-]\s*(#|\*|"""|'"'''"'|[A-Za-z(`\[])'` — every changed line is inside a comment or a docstring; no changed line is an executable statement. Confirm by reading the diff: it must contain no added or removed `import`, `def`, `assert`, `subprocess.run` or `return` line.
    - `grep -c 'QUA-04' tests/test_pdf_render_gate.py` returns at least 2 — both occurrences in that file carry the new rationale.
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest --collect-only -q tests/` exits 0 and collects the same number of tests as before the edit.
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run black --check tests/` exits 0 — the reflowed comment blocks still satisfy the 88-column formatter.
    - Every edited site still contains the substantive diagnosis (the PATH-shadowing mechanism and the exit-127 symptom); none was replaced by a shorter note that drops the explanation.
  </acceptance_criteria>
  <done>Every `CORRECT`-dispositioned docstring and comment site reads in past tense with the QUA-04 rationale appended, the five real-`uv`-invoking modules are byte-unchanged, and collection plus formatting are green.</done>
</task>

<task type="auto">
  <name>Task 3: Amend ROADMAP.md, REQUIREMENTS.md and STATE.md</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` line 359 (the Phase 45.2 bullet in the phase list) and lines 909-981 (§ "Phase 45.2: Local Toolchain Repair" — the goal paragraphs and SC#1 through SC#7). Both carry the "13 test modules" figure.
    - `.planning/REQUIREMENTS.md` lines 192-209 — the QUA-04 body, which carries the same "13 test modules" figure and the same diagnosis prose.
    - `.planning/STATE.md` line 349 — the sentence ending "the 45 are the known NixOS `uv` exit-127 false positives", and lines 425 and 587, which are REL-04 records that must remain byte-identical.
    - `.planning/STATE.md` line 71 — the Phase 45.2 row of the phase table, a ROADMAP-derived summary that may repeat the same figures; judge it against the Task 1 inventory.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-05, D-06, D-08, D-10, and D-01/D-02/D-03 for the substance of the SC#1 and SC#5 amendments.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-PATTERNS.md` — section "`.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`", which quotes the QUA-04 body verbatim and identifies the sibling-document relationship.
  </read_first>
  <action>
    Use the `Edit` tool for every change in this task. Never use `Write` on these three files — a
    whole-file rewrite destroys content outside the diff window and, for `STATE.md` and
    `ROADMAP.md`, races the orchestrator's own tracking writes.

    **`ROADMAP.md` § Phase 45.2 — four amendments (D-10).**
    - The goal paragraph and the phase-list bullet at line 359 both say the defect makes 13 test
      modules fail. The real number is 5: `tests/test_examples_basic.py` and the four
      `tests/test_integration_{basic,advanced,multi_doc,nested_toctree}.py` modules are the only
      ones that actually execute `["uv", "run", "sphinx-build", …]`. 13 is what a bare
      `grep -l "uv" tests/` returns; the other matches are prose mentions in docstrings. The 5-file
      set matches every archived measurement (`26-01-SUMMARY.md`, `22.1-01-SUMMARY.md`). Correct
      both sites, and say in one clause where the 13 came from so the miscount is not re-derived.
    - SC#1 currently claims `tox -e lint` completes. It cannot on this machine: the `lint`
      environment now reaches `black --check .` and passes it, then stops at `ruff check .` because
      `.venv/bin/ruff` is a generic-linux ELF NixOS has no loader for. That is defect ③, routed out
      by D-03 and filed as a todo by Plan 05. Amend SC#1 so it closes on provisioning and command
      execution both being reached, with the `ruff` stop named as an out-of-scope, separately-filed
      defect rather than a failure of this phase.
    - SC#3 says 13 modules; change to 5, and keep the rest of the criterion — including the census
      being taken over the full suite on both sides (D-14) — intact.
    - SC#5 currently names only the `requires` rename. Add that `tox.ini` also gains `extras` lines
      on `[testenv]`, `[testenv:lint]`, `[testenv:type]` and `[testenv:cov]`, because `runner =
      uv-venv-lock-runner` provisions from `uv.lock` via `uv sync` and never consults a `deps` list
      (D-02).
    - SC#6's premise is wrong. It implies the affected test docstrings are misdiagnosed; D-06
      measured them as correct — only their tense goes stale. Amend the criterion to say the
      docstrings are tense-corrected with an added rationale, and that the genuinely wrong record is
      `STATE.md`'s framing of the failures as unfixable environmental false positives. Also record
      SC#6's scope boundary explicitly: live records only, with the REL-04 family and every archived
      record out of scope (D-07, D-08).
    - Leave SC#2, SC#4 and SC#7 unamended. SC#7 in particular stands untouched by design — D-02 was
      chosen over the PEP 735 alternative precisely to keep `pyproject.toml`'s only change inside
      the `dev` extra, and Phase 46's SC#3 depends on that fence.

    **`REQUIREMENTS.md` § QUA-04 — the same corrections in the sibling document.** Change the
    "13 test modules" figure to 5 with the same explanatory clause. Add the second half of the
    diagnosis: `tox.ini`'s `deps` lists are inert under `uv-venv-lock-runner`, so `[testenv]`,
    `lint`, `type` and `cov` never contained their own tools, which is why CI's green did not
    validate `tox.ini` at all. Note that the `ruff` generic-linux-ELF defect is a separate,
    NixOS-local problem routed out of QUA-04 and tracked as a todo. Do not change QUA-04's checkbox
    state and do not touch any other requirement entry.

    **`STATE.md` — two edits.** First, at line 349, the phrase framing the 45 failures as "the
    known NixOS `uv` exit-127 false positives" is the genuinely wrong record D-06 identifies: it
    presents a fixable dependency defect as an unfixable environmental one. Replace that framing
    with an accurate one naming the real cause — a generic-linux `uv` wheel binary at
    `.venv/bin/uv` that NixOS cannot exec, shadowing the working nix-store `uv` for subprocess
    children — and note that it is closed by QUA-04. Keep the surrounding PR-#131 review context
    and the `45F/776P → 45F/779P` figures; they are a dated measurement and remain accurate as a
    record of what was counted.

    Second, add D-08's distinguishing sentence at or near the same location: this milestone carries
    two unrelated `exit 127` causes — (a) the local `.venv/bin/uv` ELF mismatch, closed by QUA-04,
    and (b) the v0.7.0 `create-release` job calling `uv run` with no `astral-sh/setup-uv` step,
    which is REL-04 and remains open. This sentence is what stops a future mechanical sweep from
    conflating them.

    Do not touch `STATE.md` lines 425 and 587. Both describe cause (b) and their wording is
    correct. Do not touch `STATE.md`'s frontmatter, `## Current Position`, or any progress figure —
    the orchestrator owns those.
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only HEAD~1 -- .planning/MILESTONES.md .planning/RETROSPECTIVE.md .planning/milestones/ .planning/PROJECT.md)" &amp;&amp; test "$(git diff HEAD~1 -- .planning/STATE.md | grep '^-' | grep -c 'create-release')" = "0" &amp;&amp; test "$(git diff HEAD~1 -- .planning/STATE.md | grep '^-' | grep -c 'command not found')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD~1` lists exactly `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` and `.planning/STATE.md`.
    - `git diff --name-only HEAD~1 -- .planning/MILESTONES.md .planning/RETROSPECTIVE.md .planning/milestones/ .planning/PROJECT.md` is empty — four of the six REL-04 sites are in files this plan never touched at all.
    - `git diff HEAD~1 -- .planning/STATE.md | grep '^-' | grep -c 'create-release'` returns 0 and the same pipeline for `command not found` returns 0 — the other two REL-04 sites, which live inside a file this task does edit, were not removed or reworded.
    - `grep -c 'false positives' .planning/STATE.md` returns 0.
    - `grep -c 'REL-04' .planning/STATE.md` increases by at least 1 relative to `git show HEAD~1:.planning/STATE.md` — D-08's distinguishing sentence names the other cause explicitly.
    - `grep -c '13 test modules' .planning/ROADMAP.md` returns 0 and `grep -c '13 test modules' .planning/REQUIREMENTS.md` returns 0.
    - `grep -c 'QUA-04' .planning/REQUIREMENTS.md` is unchanged from `git show HEAD~1:.planning/REQUIREMENTS.md` — the requirement was amended, not duplicated or renumbered.
    - `git diff HEAD~1 -- .planning/STATE.md` contains no hunk touching the frontmatter block or the `## Current Position` section.
    - `node .claude/gsd-core/bin/gsd-tools.cjs query state.load` exits 0 — `STATE.md` still parses after the edit.
    - `.planning/ROADMAP.md` § Phase 45.2 still contains seven success criteria, numbered 1 through 7, and SC#7's text is byte-identical to `git show HEAD~1:.planning/ROADMAP.md`'s.
  </acceptance_criteria>
  <done>`ROADMAP.md` and `REQUIREMENTS.md` state 5 modules rather than 13, SC#1/SC#5/SC#6 are amended per D-01/D-02/D-03/D-06, `STATE.md` names the real cause and distinguishes it from REL-04's, and all six REL-04 records are provably byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-wide grep → destructive edit | A mechanical sweep over the whole repository decides which recorded text gets rewritten. A pattern that is too broad destroys correct records; one that is too narrow leaves the wrong diagnosis in place. |
| worktree executor → shared orchestrator artifacts | `STATE.md` and `ROADMAP.md` are written by the orchestrator between waves and by this plan inside a worktree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45.2-09 | Tampering | the six REL-04 record sites | high | mitigate | Task 1 dispositions every grep hit before any edit, Task 3 edits only `STATE.md`'s Phase-45.2-diagnosis region, and the automated verify asserts over `git diff` that no removed line in `STATE.md` carries the REL-04 wording while four of the six sites live in files this plan never opens. |
| T-45.2-10 | Repudiation | archived milestone records | medium | mitigate | D-07's scope boundary is encoded as a prohibition and as the `OUT-OF-SCOPE (archived record)` disposition; the acceptance criteria assert `.planning/milestones/`, `.planning/RETROSPECTIVE.md` and `.planning/PROJECT.md` are untouched, preserving the audit trail of what was believed when. |
| T-45.2-11 | Tampering | test executable lines | medium | mitigate | Task 2 is a comment/docstring-only edit; the acceptance criteria require the diff to contain no added or removed executable statement, and gate on `pytest --collect-only` returning an unchanged collection count plus `black --check tests/` staying green. |
| T-45.2-12 | Denial of service | `STATE.md` parseability | low | mitigate | `state.load` is required to exit 0 after the edit, so a malformed frontmatter or broken structure cannot ship. |
| T-45.2-13 | Information disclosure | committed record text | low | accept | All edited text is project-internal planning prose already committed to this repository; no credential or private data is introduced. |
</threat_model>

<artifacts_this_phase_produces>
New symbols created by Phase 45.2 (listed in every plan so drift verification excludes them):

- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — new file, created by Plan 01; this plan appends `## Step 4`.
- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/COVERAGE.md` — new file, written at plan time.
- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-0{1..5}-SUMMARY.md` — new files, one per plan.
- `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` — new todo filed by Plan 05 (D-03).
- `.planning/todos/pending/2026-08-11-<slug>.md` — conditional new todo filed by Plan 05 only if `tox -e docs-pdf` fails for a cause outside this phase's dependency/`tox.ini` scope (D-19).
- `pyproject.toml` `[project.optional-dependencies] dev` gains the requirement name `tox-uv-bare` — new symbol, created by Plan 02.
- `tox.ini` gains an `extras` key on `[testenv]`, `[testenv:lint]`, `[testenv:type]` and `[testenv:cov]`, and a `runner` key on `[testenv:cov]` — new keys, created by Plan 02.
</artifacts_this_phase_produces>

<verification>
- `45.2-TOOLCHAIN-EVIDENCE.md` Step 4 holds an execution-time grep inventory with a disposition for every hit and explicit zero-hit findings.
- Every `CORRECT`-dispositioned site is edited; every `OUT-OF-SCOPE` site is byte-unchanged.
- `ROADMAP.md` and `REQUIREMENTS.md` agree with each other on the corrected figures.
- The five `uv run`-invoking test modules are byte-unchanged.
- `pytest --collect-only`, `black --check tests/` and `state.load` are all green.
</verification>

<success_criteria>
- SC#6 is satisfied on its own terms: the sweep was a repo-wide grep run at execution time, not a file list, and its boundary is recorded rather than assumed.
- D-05, D-06, D-07, D-08 and D-10 are each implemented and individually checkable in the diff.
- The audit trail is intact: no archived record and no REL-04 record was rewritten.
</success_criteria>

<output>
Create `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-03-SUMMARY.md` when done.
</output>
