# Phase 45.2: Local Toolchain Repair — tox-uv to tox-uv-bare - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-10
**Phase:** 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare
**Areas discussed:** Record-correction scope (SC#6), Forward workflow guidance, CI evidence (SC#4),
Measurement protocol and evidence file

---

## Record-correction scope (SC#6)

Opened with two discovery-time measurements that contradicted `ROADMAP.md`: only 5 test modules
actually invoke `uv` (not 13), and the 8 prose-mention docstrings carry a *correct* diagnosis
rather than the misdiagnosis SC#6 assumes.

| Option | Description | Selected |
|--------|-------------|----------|
| Correct in CONTEXT, amend ROADMAP in-phase | Record the correction in CONTEXT.md; downstream follows it; ROADMAP wording fixed during execution | ✓ |
| Pause and run `/gsd-phase` now | Rewrite ROADMAP SC#3/SC#6 and QUA-04 before writing CONTEXT | |
| Keep SC wording, widen census to all 13 | Do not call "13" an error; census all uv-mentioning modules | |

**User's choice:** Correct in CONTEXT, amend ROADMAP in-phase → D-10

| Option | Description | Selected |
|--------|-------------|----------|
| Live records only | STATE.md, PROJECT.md current footer, the 8 docstrings; archives untouched | ✓ |
| Live records + annotation in archives | Add a "Corrected 2026-08-10 (QUA-04)" line to ~10 archived files | |
| Rewrite repo-wide | Align every occurrence with current understanding | |

**User's choice:** Live records only → D-07

| Option | Description | Selected |
|--------|-------------|----------|
| Fence the REL-04 records only | Name the six sites out of scope in CONTEXT.md | |
| Fence + distinguishing sentence in STATE.md | Also record that this milestone carries two unrelated exit-127 causes | ✓ |

**User's choice:** Fence + distinguishing sentence → D-08

**Notes:** The user asked twice for clarification here — first for a fuller explanation of the
question, then specifically for what "the Bug B records" referred to. The six passages were quoted
verbatim (`MILESTONES.md:43`, `STATE.md:421-428`, `STATE.md:587`, `PROJECT.md:951`,
`RETROSPECTIVE.md:369`, `v0.7.0-REQUIREMENTS.md:226`) before the choice was made. The concern is
structural: SC#6 mandates a repo-wide grep, and `command not found` / `exit 127` matches all six.

| Option | Description | Selected |
|--------|-------------|----------|
| Do not normalize the 5 modules | Keep `["uv", "run", …]` as SC#3 evidence and regression detector | ✓ |
| Normalize all 13 onto `sys.executable -m sphinx` | Remove PATH dependence entirely | |
| Do not normalize but file a todo | Leave for a future decision | |

**User's choice:** Do not normalize → D-09

| Option | Description | Selected |
|--------|-------------|----------|
| Past tense + updated rationale | Cause removed by QUA-04; pattern kept for PATH-independence | ✓ |
| One-line annotation only | Append "Resolved by QUA-04 (2026-08-10)" | |
| Leave untouched | Diagnosis is correct, so out of SC#6's scope | |

**User's choice:** Past tense + updated rationale → D-06

---

## Scope of the repair (emergent — not in the original area list)

Measuring past `tox -e lint --notest` (the limit `46-CONTEXT.md` `<specifics>` item 6 declared)
surfaced two further defects: `tox.ini`'s `deps` being ignored by `uv-venv-lock-runner`, and
`ruff` shipping the same unrunnable generic-linux ELF as `uv`.

| Option | Description | Selected |
|--------|-------------|----------|
| Fix ① + ②, route ③ (ruff) out | Both config defects have cross-platform fixes; ruff has only NixOS-local workarounds | ✓ |
| Fix ①②③ | Also repair ruff locally, meeting SC#1 literally | |
| Fix ① only | Dependency rename as ROADMAP declares; ②③ both become todos | |

**User's choice:** Fix ① + ②, route ③ out → D-01, D-03

**Notes:** The user challenged the first framing — asking whether `black` was affected as well as
`ruff`. It is not. A scan of every executable in `.venv/bin` found exactly three ELF binaries
(`uv`, `uvx`, `ruff`), all Rust programs; `black` and `mypy` are Python packages whose
"compiled: yes" refers to mypyc extension modules that the Nix CPython loads fine. The corrected
statement is that `tox -e lint` reaches and passes `black --check .` and then fails at
`ruff check .`. This correction was made before the scope choice was taken.

| Option | Description | Selected |
|--------|-------------|----------|
| `extras = dev` | Only `tox.ini` moves; SC#7 stands unamended | ✓ |
| PEP 735 `[dependency-groups]` | Finer-grained, uv's recommended shape; requires an SC#7 amendment | |
| Let the researcher decide | Record intent only | |

**User's choice:** `extras = dev` for this phase; capture the `[dependency-groups]` split as a
seed → D-02, `SEED-003`

**Notes:** The user asked for the options to be explained again with concrete diffs before choosing.
Measured inputs to the decision: `.tox/lint` holds 25 distributions today; the `dev` extra adds 13
direct packages plus transitive deps to every tox environment.

| Option | Description | Selected |
|--------|-------------|----------|
| Close the CI false-green in the same phase | Fixing ② makes CI use in-environment tools | ✓ (derived) |
| Record only | Leave CI relying on `uv run`'s PATH | |

**User's choice:** Deferred pending the scope decision, then resolved as a derived consequence of
choosing ② → D-04

---

## Forward workflow guidance

| Option | Description | Selected |
|--------|-------------|----------|
| Prohibit the `ln -sf` shim in CLAUDE.md | Durable, reaches future sessions | |
| Prohibit in CONTEXT.md only | This phase's executors only; CLAUDE.md untouched | ✓ |

**User's choice:** CONTEXT.md only → D-11

**Notes:** The shim appears in no live configuration file — only archived plans
(`35-0*-PLAN.md`, `28-VERIFICATION.md`) and verbal executor instructions.

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite the project memory after the phase verifies | Update once `uv run pytest` is proven green | ✓ |
| Leave it and decide manually later | No CONTEXT record | |

**User's choice:** Rewrite after verification → D-12

---

## CI evidence (SC#4)

Measured first: `ci.yml` triggers on `push: [main, develop]`, `pull_request: [main, develop]`,
`workflow_dispatch` — so pushing the milestone branch triggers nothing.

| Option | Description | Selected |
|--------|-------------|----------|
| Push then `workflow_dispatch` | Matches the three prior branch CI runs; covers Windows/macOS; no PR | ✓ |
| Open a draft PR | Auto-runs on every push; entangles with Phase 46's release PR | |
| Reuse Phase 46's CI run | Less work; SC#4 cannot close in this phase | |

**User's choice:** Push then `workflow_dispatch` → D-16

| Option | Description | Selected |
|--------|-------------|----------|
| Transcribe the before/after of an existing log line | tox prints the resolved `uv` path; no CI change | ✓ |
| Add a permanent assertion step to `ci.yml` | Catches future regressions; moves `.github/` | |

**User's choice:** Transcribe existing log → D-17

| Option | Description | Selected |
|--------|-------------|----------|
| Fix a red CI inside this phase | Do not hand Phase 46 a red tree | ✓ |
| Isolate unrelated failures and proceed | Only for failures demonstrably outside this phase's changes | |

**User's choice:** Fix in-phase → D-18

---

## Measurement protocol and evidence file

| Option | Description | Selected |
|--------|-------------|----------|
| Capture the baseline once in the main tree, pre-phase | The measurement is environmental; per-worktree capture would not compare | ✓ |
| Each executor captures in its worktree | Standard GATE-01 shape; each `uv sync` reinstalls the broken binary | |

**User's choice:** Main tree, before any plan → D-13

**Notes:** Verified at discussion time that `.venv/bin/uv` is the raw 63,949,040-byte ELF with no
shim applied — an uncontaminated as-is state available right now.

| Option | Description | Selected |
|--------|-------------|----------|
| Full-suite absolute counts, before and after | Settles whether "45" was ever this cause | ✓ |
| The 5 modules only | Faster; cannot show hidden failures | |
| Both | Strongest; longest runtime | |

**User's choice:** Full-suite absolute counts → D-14

| Option | Description | Selected |
|--------|-------------|----------|
| `45.2-TOOLCHAIN-EVIDENCE.md` | Names the content; matches `41-GREEN-TREE-EVIDENCE.md` | ✓ |
| `45.2-GATE-EVIDENCE.md` | Matches Phase 39/40 naming; emphasises RED-first | |

**User's choice:** `45.2-TOOLCHAIN-EVIDENCE.md` → D-15

| Option | Description | Selected |
|--------|-------------|----------|
| Isolate an unrelated `docs-pdf` failure and proceed | Record cause in the evidence file, file a todo | ✓ |
| Fix `docs-pdf` regardless of cause | Certain for Phase 46; unbounded scope | |

**User's choice:** Isolate and proceed → D-19

---

## Claude's Discretion

- The exact `tox.ini` edit shape for D-02 — which environments get `extras = dev`, and whether the
  dead `deps` blocks are deleted or kept with an explanatory comment.
- The version-specifier form for `tox-uv-bare` in `pyproject.toml` (SC#5 constrains `tox.ini` only).
- Whether `uv.lock` is regenerated via `uv lock` or falls out of `uv sync`.
- The exact wording of the corrected docstrings and the `STATE.md` sentences.
- Plan/wave decomposition and where the D-10 roadmap amendment lands within it.

## Deferred Ideas

- The `ruff` generic-linux-ELF defect — every repair is a NixOS-local workaround (D-03).
- `SEED-003` — split the `dev` extra into PEP 735 `[dependency-groups]`.
- `docs/source/contributing.rst:117-128` lists Python versions (`py39`, `py310`, `py311`) that are
  not in `tox.ini`'s `env_list`.
- A guard asserting every `tox.ini` environment declares tools via `extras`/`dependency_groups`
  rather than the inert `deps`.
- Three keyword-matched pending todos reviewed and not folded; all remain covered by Phase 46's
  D-16.
