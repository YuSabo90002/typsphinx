---
phase: 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare
verified: 2026-08-11T00:54:28Z
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 45.2: Local Toolchain Repair — tox-uv to tox-uv-bare Verification Report

**Phase Goal:** The task runner this project documents as its own actually runs on the maintainer's
machine, and the test suite passes under the invocation `CLAUDE.md` mandates for every
worktree-isolated executor. The repair is one dependency name (`tox-uv` → `tox-uv-bare`), plus
correcting the project's own recorded (wrong) diagnosis of these failures.
**Verified:** 2026-08-11T00:54:28Z
**Status:** passed
**Re-verification:** No — initial verification

## Method

This phase's own evidence lives in `45.2-TOOLCHAIN-EVIDENCE.md` (per D-15, deliberately not in this
file). Rather than trusting that document's or the five SUMMARYs' claims, every Success Criterion
below was **independently re-measured live against the current main tree** (`/home/yuta/Documents/typsphinx`,
HEAD `49c0ae3`) during this verification pass — direct filesystem checks, direct `tox`/`pytest`
invocations, and direct `gh run view`/`gh run list` calls against GitHub's API, not a re-read of the
evidence file's transcripts. Every number below is this verification's own, not copied from
`45.2-TOOLCHAIN-EVIDENCE.md`.

## Goal Achievement

### Observable Truths (Success Criteria SC#1–SC#7, ROADMAP.md § Phase 45.2, as amended by D-01/D-02/
D-05/D-06/D-10)

| # | Truth (amended wording per D-10 where applicable) | Status | Evidence |
|---|---|---|---|
| SC#1 | `tox -e type`/`docs-html`/`docs-pdf` complete end-to-end with no `TOX_UV_PATH` override; `tox -e lint` reaches and passes `black --check .`, then stops at `ruff check .` (named, out-of-scope NixOS ELF defect, not counted as failure) | ✓ VERIFIED | Ran all four locally, live, this session: `tox -e type` → `Success: no issues found in 6 source files`, exit 0. `tox -e lint` → `black --check .` passes (`All done! 251 files would be left unchanged.`), `ruff check .` fails with `Could not start dynamically linked executable: .../.tox/lint/bin/ruff` / `exit 127` — matches D-03's named, routed-out defect exactly. `tox -e docs-html` → `build succeeded`, exit 0. `tox -e docs-pdf` → `Generated PDF: .../docs/_build/pdf/typsphinx.pdf`, `build succeeded`, exit 0 (the environment the phase named as "must not skip"). |
| SC#2 | `.venv/bin/uv`/`.venv/bin/uvx` absent after clean `uv sync --extra dev`; no installed `uv` distribution (checked via `importlib.metadata`, not the lockfile) | ✓ VERIFIED | `test -e .venv/bin/uv` → absent; `test -e .venv/bin/uvx` → absent. `importlib.metadata.distribution('uv')` → raises `PackageNotFoundError`. `tox-uv-bare` resolves `1.35.2`. `command -v uv` resolves the nix-store binary. `uv.lock` contains `name = "tox-uv-bare"` and no `tox-uv`/`uv` package block. |
| SC#3 | The 5 `uv`-invoking test modules' pytest failures are gone under an outer `uv run pytest`, measured over the full suite | ✓ VERIFIED | `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest -q` → **984 passed, 5 skipped, 0 failed** (989 collected), run to completion live this session. Exactly the module count is now 5, confirmed via `grep -lE '^\s*"uv",\s*$' tests/*.py` → `test_examples_basic.py` + the four `test_integration_*.py`, matching the corrected count (not 13). |
| SC#4 | CI stays green and reaches `uv` by a different route (`astral-sh/setup-uv@v7`), observed on a real CI run of this phase's commit | ✓ VERIFIED | `gh run view 31445582363` (workflow_dispatch, headSha `2fb6c6f`, this phase's fix commit): 10/12 jobs `success` (Lint, Type, Build, Coverage, both Integration jobs, 4/6 non-Windows Test Python jobs); the 2 red jobs are both `windows-latest` Test Python legs, independently confirmed unrelated (see below). Lint job log line directly fetched: `lint: uv-sync> /opt/hostedtoolcache/uv/0.12.3/x86_64/uv sync …` — resolves the `astral-sh/setup-uv`-installed binary, not `.venv/bin/uv`. Pre-phase run `31287786840` (main, 2026-08-09) log directly fetched for comparison: `lint: uv-sync> .venv/bin/uv sync …` — confirms the single-line-change evidence D-17 describes is real, not asserted from the workflow file. |
| SC#5 | `tox.ini`'s `requires` moves to `tox-uv-bare~=1.35`, comma-splitting comment rewritten (not deleted); `extras = dev` added to `[testenv]`/`lint`/`type`/`cov` | ✓ VERIFIED | `tox.ini:11` reads `requires = tox-uv-bare~=1.35`, preceded by a 9-line comment explaining the comma-splitting hazard for the new package name. All four environments (`[testenv]`, `[testenv:lint]`, `[testenv:type]`, `[testenv:cov]`) carry `extras = dev`; the pre-`[testenv]` comment explains why `deps` was inert. |
| SC#6 | Wrong diagnosis corrected wherever recorded (live records only); REL-04 sites and archived records left byte-unchanged | ✓ VERIFIED | `STATE.md` (former line 349 area) now reads "corrected 2026-08-11 (QUA-04, D-06): the 45 are a fixable dependency defect, not an unfixable environmental artifact" plus the added two-causes sentence distinguishing QUA-04 from REL-04. `git diff <phase-start>..HEAD -- .planning/MILESTONES.md .planning/RETROSPECTIVE.md .planning/PROJECT.md .planning/milestones/` is empty — all six D-08-named REL-04 sites and every archived record untouched. All 5 `uv`-invoking-module docstrings (well, the 6 grep-confirmed sites across the render-gate test files) moved to past tense with a QUA-04 rationale, diagnosis text itself preserved per D-06 (spot-checked `test_pdf_render_gate.py`). ROADMAP.md/REQUIREMENTS.md read "5" module count and the SC#1 amendment, matching D-05/D-10 exactly. |
| SC#7 | No runtime surface moves: `[project] dependencies` untouched, `typsphinx/` untouched, `@preview` count stays 4, `pyproject.toml`'s only change stays inside `dev` extra | ✓ VERIFIED | `git diff <phase-start>..HEAD -- pyproject.toml` shows exactly one hunk, `tox-uv>=1.35,<2` → `tox-uv-bare>=1.35,<2`, inside the `dev` extra. `git diff <phase-start>..HEAD --stat -- typsphinx/` and `-- .github/ README.md docs/` are both empty. |

**Score:** 7/7 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `45.2-TOOLCHAIN-EVIDENCE.md` | Steps 0-9 + closing verdict, D-15 | ✓ VERIFIED | 2075 lines, all 10 sections present; every transcript I spot-checked (CI logs, tox runs, pytest counts, .venv state) reproduces independently |
| `pyproject.toml` | `dev` extra → `tox-uv-bare` | ✓ VERIFIED | Confirmed live |
| `tox.ini` | `requires` rename + `extras = dev` × 4 + `package = editable` | ✓ VERIFIED | Confirmed live, and the `package = editable` change (a real, this-phase-discovered CI fix, D-18) verified present with its own explanatory comment |
| `uv.lock` | Regenerated, no `tox-uv`/`uv` blocks | ✓ VERIFIED | Confirmed live |
| `.planning/ROADMAP.md`, `REQUIREMENTS.md`, `STATE.md` | SC#1/#3/#5/#6 amendments, "5" module count, corrected diagnosis | ✓ VERIFIED | Confirmed live |
| 6 test docstrings | Past tense, QUA-04 rationale | ✓ VERIFIED | Spot-checked `test_pdf_render_gate.py` (both docstring and inline comment) |
| 2 filed todos (`ruff` NixOS ELF, Windows path-separator) | Exist, correctly scoped | ✓ VERIFIED | Both present in `.planning/todos/pending/`, content matches phase claims |
| Project memory `pytest-no-outer-uv-run-in-main-tree.md` | Retired, prohibition lifted, measured counts quoted | ✓ VERIFIED | Read directly; matches Step 7's counts (984/5/0), MEMORY.md index entry updated to match |

### Data-Flow / Wiring Spot-Checks

All 5 plans' `must_haves.artifacts` pass via `gsd_run query verify.artifacts` (14/14 artifacts across
the 5 plans, `all_passed: true` for each plan). No stubs, no orphans — every artifact this phase
claims to have produced exists on disk and its content matches what the plan specified.

### Behavioral Spot-Checks (live re-execution, this verification session)

| Behavior | Command | Result | Status |
|---|---|---|---|
| `.venv/bin/uv`/`uvx` absent, no `uv` distribution | `test -e`, `importlib.metadata` | absent, `PackageNotFoundError` | ✓ PASS |
| `tox -e type` completes | `uv run tox -e type` | exit 0, "Success: no issues found" | ✓ PASS |
| `tox -e lint` reaches `black` then stops at `ruff` (named defect) | `uv run tox -e lint` | black passes; ruff exit 127 (NixOS ELF, as documented) | ✓ PASS |
| `tox -e docs-html` completes | `uv run tox -e docs-html` | `build succeeded`, exit 0 | ✓ PASS |
| `tox -e docs-pdf` completes, produces PDF | `uv run tox -e docs-pdf` | `Generated PDF: .../typsphinx.pdf`, exit 0 | ✓ PASS |
| Full pytest suite under outer `uv run pytest` | `uv run pytest -q` | 984 passed, 5 skipped, 0 failed | ✓ PASS |
| CI run at fix commit is green except 2 named-unrelated jobs | `gh run view 31445582363` | 10/12 success; both failures are `windows-latest` Test Python, unrelated path-separator bug | ✓ PASS |
| CI's `uv-sync>` line resolves the setup-uv binary, not `.venv/bin` | `gh run view --log` on both the pre-phase run and the fix-commit run | `.venv/bin/uv` before → `/opt/hostedtoolcache/uv/0.12.3/.../uv` after | ✓ PASS |
| Only 5 modules actually invoke `uv` as a subprocess | `grep -lE '^\s*"uv",\s*$' tests/*.py` | exactly 5 files | ✓ PASS |
| REL-04 / archived record sites byte-unchanged | `git diff` over the 4 protected paths | empty diff | ✓ PASS |
| No debt markers in phase-touched files | `grep -nE "TBD|FIXME|XXX"` over 9 phase-touched files | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| QUA-04 | 45.2-01..05 (all 5) | Task runner runs locally; pytest passes under `uv run`; wrong diagnosis corrected | ✓ SATISFIED | All 7 SCs independently re-verified live (table above); `REQUIREMENTS.md` line 192 body matches the corrected (5-module, fixable-defect) diagnosis |

No orphaned requirements — Phase 45.2 maps to exactly QUA-04 in both `REQUIREMENTS.md`'s table and
every plan's `requirements:` frontmatter field.

### Anti-Patterns Found

None blocking. See "Non-Blocking Finding" below for one warning-level item the phase's own code
review already surfaced and left unresolved.

## Non-Blocking Finding (does not affect SC#1–#7 disposition)

**`CLAUDE.md` still names the removed `tox-uv` package (code review WR-01, confirmed still present
at verification time).** `CLAUDE.md:11` reads `tox (with tox-uv)` and `CLAUDE.md:77` reads `tox.ini
pins tox-uv~=1.35`; both are now factually wrong — `tox.ini` pins `tox-uv-bare~=1.35`, confirmed live.
This is a genuine, currently-live inaccuracy in the project's own auto-loaded onboarding document
(the same class of drift risk WR-01 named: a future agent could "fix" `tox.ini` back toward `tox-uv`,
or misinform a user).

**Assessed against the phase's own SCs: this is correctly out of scope, not a gap.**
- It does not bear on SC#5, which is scoped explicitly to `tox.ini`'s own `requires` line and
  comment, not to `CLAUDE.md`.
- It does not bear on SC#6, whose grep patterns (`Could not start dynamically linked`, `command not
  found`, `not on PATH`, `non-Nix`, `false positive`) target the *uv-exit-127 misdiagnosis*, not a
  stale package-name reference — `CLAUDE.md`'s text is not a "wrong diagnosis," it is simply outdated
  after the rename, a different category the sweep was never asked to catch.
- It does not bear on SC#7, which fences `pyproject.toml`/`tox.ini`/`uv.lock`/`typsphinx/`, and does
  not require editing `CLAUDE.md`.

**One process gap, worth noting for the phase owner:** the code review flagged this as WR-01, and it
was not fixed and — unlike the `ruff` and Windows-path-separator findings — **not filed as a todo
either**. It is a two-line fix. Recommend filing it (or fixing it directly, since it is a trivial
two-string edit with no scope-fence implications) before or shortly after this phase closes, so it
doesn't silently persist as the project's own onboarding doc contradicting its own `tox.ini`.

### Execution Process Note (informational, not a gap)

Plan 45.2-02's checkpoint continuation resumed inside Plan 45.2-03's worktree after 02's own worktree
was reclaimed (recorded in both plans' own SUMMARYs). I independently confirmed the two plans'
commits (`e603f36`/`1140839` for 02; `18ffa14`/`0efd3d8`/`228f288` for 03) touch disjoint file sets —
Plan 02 only ever staged `pyproject.toml`/`tox.ini`/`uv.lock`; Plan 03 only ever staged
`.planning/`/`tests/*` files — matching each plan's declared `files_modified`. No cross-contamination
found.

### Human Verification Required

None. Every Success Criterion was independently re-measurable and re-measured live this session —
tox invocations, pytest runs, `.venv` filesystem state, and real `gh run view`/`gh run list` calls
against GitHub's API for the CI criterion (not argued from the workflow file, matching SC#4's own
requirement).

### Gaps Summary

No gaps. All 7 Success Criteria (as amended by D-01/D-02/D-05/D-06/D-10) are independently confirmed
true on the live main tree, not merely claimed by SUMMARY.md or `45.2-TOOLCHAIN-EVIDENCE.md`. The
phase goal — "the task runner this project documents as its own actually runs" — holds: `tox -e
type`/`lint`(to its named `ruff` stop)/`docs-html`/`docs-pdf` all ran to completion live during this
verification, and the full pytest suite passes cleanly (984/5/0) under the exact `uv run pytest`
invocation `CLAUDE.md` mandates. CI is proven green on the real fix commit via direct `gh` API calls,
with the one still-red category (Windows path-separator, 2 jobs) independently confirmed unrelated
and already filed as a todo. The project's wrong diagnosis is corrected in every live record and
left untouched in every REL-04/archived record, exactly as SC#6 requires.

The one non-blocking finding (`CLAUDE.md`'s stale `tox-uv` references, WR-01) does not bear on any
SC and is recommended for a quick follow-up fix or todo filing, not a blocker to phase completion.

---

_Verified: 2026-08-11T00:54:28Z_
_Verifier: Claude (gsd-verifier)_
