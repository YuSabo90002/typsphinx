---
phase: 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare
plan: 05
subsystem: infra
tags: [tox, uv, ci, github-actions, packaging, nixos, memory-retirement]

# Dependency graph
requires:
  - phase: 45.2-03
    provides: "SC#6 discovery-time record-correction sweep (Step 4)"
  - phase: 45.2-04
    provides: "Steps 5-7: main-tree post-fix proof, SC#1/SC#2/SC#3 all MET"
provides:
  - "45.2-TOOLCHAIN-EVIDENCE.md Steps 8-9 and the closing verdict: SC#4 (real CI dispatch, before/after uv-sync> line) and SC#7 (mechanical dev-extra-only fence) both MET"
  - "A real tox.ini fix (package = wheel -> package = editable) for a newly-exposed, this-phase-caused CI failure: non-editable typsphinx installs were silently dropping typsphinx/templates/base.typ under CI's uv 0.12.3"
  - "Two filed todos: the D-03 ruff generic-linux-ELF defect (plus the sibling tox -e py312 NixOS interpreter-download defect), and a newly-discovered, unrelated Windows-only path-separator bug in tests/test_docs_contract_claims_gate.py"
  - "The project memory pytest-no-outer-uv-run-in-main-tree.md retired (D-12), recording the prohibition as lifted with measured post-fix counts"
affects: [46]

# Actuals (#2632)
actuals:
  tokens: 11900
  tasks: 3
  commits: 4

# Tech tracking
tech-stack:
  added: []
  patterns: ["tox-uv-bare/uv-venv-lock-runner's `package` config key controls --no-editable vs editable installs per tox environment; `package = editable` (the runner's own default) sidesteps non-editable-install packaging gaps entirely, matching what an outer `uv sync --extra dev` already does"]

key-files:
  created:
    - .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md
    - .planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md
  modified:
    - tox.ini
    - .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md
    - /home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/pytest-no-outer-uv-run-in-main-tree.md (outside repo)
    - /home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/MEMORY.md (outside repo)

key-decisions:
  - "Fixed a newly-exposed, this-phase-caused CI failure in-phase per D-18's own named category ('a tool now running inside a tox environment that previously never ran there'): [testenv]'s `package = wheel` (present since the project's first commit, unrelated to tox-uv/deps-extras) was building non-editable typsphinx installs that silently dropped templates/base.typ under CI's uv 0.12.3 (not under this machine's uv 0.11.25). Fixed by switching to `package = editable` in tox.ini only -- SC#7's pyproject.toml-only-inside-dev-extra fence stays untouched, since the fix never touches pyproject.toml."
  - "Classified the Windows-only test_docs_contract_claims_gate.py failure as demonstrably unrelated (D-18's second bullet) rather than fixed: the test computes Path.relative_to() (backslash on Windows) against forward-slash literal sets, added the day before this phase by Phase 45.1 plan 07 -- its first-ever real Windows CI exercise, unrelated to tox-uv/deps-extras/package=editable. Isolated and filed as a todo instead of touched."
  - "The Type Check 'fetch failed' (astral-sh/setup-uv's version-metadata fetch) occurred once and did not recur on a second dispatch with no code change -- recorded in Step 8 as a transient network flake, no todo filed (non-reproducible, non-actionable, outside this repository's control)."
  - "Rewrote the project memory (D-12) because Step 7's SC#3 disposition was MET -- quoted verbatim in the closing verdict rather than re-derived. Kept the memory's filename and Japanese-language body; updated its description frontmatter and MEMORY.md's matching index entry in the same edit."

patterns-established:
  - "When a newly-unmasked-by-this-phase CI failure's root cause is genuinely orthogonal to this phase's own fence (a Python packaging quirk of a pre-existing tox.ini `package = wheel` setting, not the tox-uv->tox-uv-bare/deps->extras change itself), the correct fix lives in whichever file the fence still permits (here: tox.ini, not pyproject.toml) -- the fence constrains WHERE the fix lands, not WHETHER a newly-exposed genuine defect gets fixed in-phase."

requirements-completed: [QUA-04]

coverage:
  - id: D1
    description: "A real workflow_dispatch CI run on gsd/v0.7.1-bug-fix-round, at a SHA carrying this phase's pyproject.toml/tox.ini changes, completed and its before/after uv-sync>/freeze> lines are recorded verbatim (SC#4, not argued from the workflow file)"
    requirement: "QUA-04"
    verification:
      - kind: manual_procedural
        ref: "live gh run view/gh workflow run transcripts in 45.2-TOOLCHAIN-EVIDENCE.md ## Step 8"
        status: pass
    human_judgment: false
  - id: D2
    description: "SC#7's four clauses (typsphinx/ untouched, .github/README.md/docs/ untouched, @preview three-way guard passes, pyproject.toml's only change stays inside the dev extra as both a diff-hunk check and a tomllib parsed-data comparison) are each asserted mechanically with a recorded command and result"
    requirement: "QUA-04"
    verification:
      - kind: unit
        ref: "env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest tests/test_preview_version_sync.py -q (3 passed); git diff --name-only/-U0 commands and tomllib comparison transcribed in 45.2-TOOLCHAIN-EVIDENCE.md ## Step 9"
        status: pass
    human_judgment: false
  - id: D3
    description: "The newly-exposed package=wheel templates-packaging CI failure (23 pytest failures across py312/py313/cov) is fixed in-phase via tox.ini's package = editable, verified locally and by a third CI dispatch turning all affected jobs green"
    requirement: "QUA-04"
    verification:
      - kind: integration
        ref: "CI run 31445582363: Code Coverage + 4/6 Test Python jobs green (was failure in runs 31444258982/31444883849); local env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run tox -e py313/cov reproduce green"
        status: pass
    human_judgment: false
  - id: D4
    description: "The ruff generic-linux-ELF defect (D-03, plus the sibling tox -e py312 NixOS interpreter-download defect) and the newly-discovered Windows-only path-separator defect are filed as todos, not fixed and not reframed as environmental false positives"
    requirement: "QUA-04"
    verification:
      - kind: unit
        ref: "test -f .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md; test -f .planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md"
        status: pass
    human_judgment: false
  - id: D5
    description: "The project memory pytest-no-outer-uv-run-in-main-tree.md is retired only because Step 7 recorded SC#3 as MET (quoted, not re-derived), with post-fix measured counts and MEMORY.md's matching index entry updated in the same edit"
    requirement: "QUA-04"
    verification:
      - kind: manual_procedural
        ref: "memory file mtime 2026-08-11 09:30 (newer than phase start 2026-08-10); description field no longer states the prohibition; MEMORY.md entry matches"
        status: pass
    human_judgment: false

duration: ~38min
completed: 2026-08-11
status: complete
---

# Phase 45.2 Plan 05: Local Toolchain Repair — Close Summary

**Dispatched a real CI run proving SC#4, discovered and fixed a genuine (though this-phase-unmasked) packaging bug the same run exposed, asserted SC#7's dev-extra-only fence mechanically over the whole phase's diff, filed the two routed-out/unrelated defects as todos, and retired the now-obsolete project memory with measured post-fix counts.**

## Performance

- **Duration:** ~38 min
- **Started:** 2026-08-11T08:56:47+09:00 (first CI dispatch)
- **Completed:** 2026-08-11T09:34:00+09:00
- **Tasks:** 3/3
- **Files modified:** 4 (in-repo: `tox.ini`, `45.2-TOOLCHAIN-EVIDENCE.md`, 2 new todos; plus 2 out-of-repo memory files)

## Accomplishments

- **Task 1 (SC#4):** Pushed the milestone tip first (per the plan's own load-bearing ordering), then dispatched `workflow_dispatch` CI three times. The first two dispatches (pre-fix, headSha `36b964d`) revealed a genuine, this-phase-caused failure the `extras = dev` fix newly unmasked: `[testenv]`'s pre-existing `package = wheel` setting (unrelated to `tox-uv`/`deps`, present since the project's very first commit) was silently dropping `typsphinx/templates/base.typ` from non-editable installs under CI's `uv` (`0.12.3`), causing 23 pytest failures across `py312`/`py313`/`cov` — a defect that had always been masked because those environments' pytest previously fell back to the outer editable `.venv` via the same PATH-shadowing mechanism D-04 diagnosed for `lint`. Fixed with a scoped `tox.ini`-only change (`package = wheel` → `package = editable`), verified locally, then re-pushed and re-dispatched: the third CI run (`31445582363`, headSha `2fb6c6f`) shows every job this phase's change touches green, plus the D-17-predicted `uv-sync>` line change (`.venv/bin/uv` before, `astral-sh/setup-uv`'s installed binary after) and the `freeze>` line now genuinely listing `black`/`ruff`.
- **A second, unrelated failure surfaced on the third dispatch:** both `windows-latest` legs fail on `tests/test_docs_contract_claims_gate.py`, a brand-new test (added the day before this phase) whose `Path.relative_to()` renders backslash paths on Windows against forward-slash literal comparison sets — its first-ever real Windows CI exercise, demonstrably unrelated to this phase's own change (D-18's second bullet). Isolated, recorded verbatim in Step 8, and filed as its own todo rather than fixed.
- **Task 2 (SC#7):** Asserted the fence mechanically: `git diff --name-only` shows `typsphinx/` and `.github/`/`README.md`/`docs/` both empty across the whole phase; a `tomllib` comparison (not text-grep) proves `[project].dependencies` byte-identical as parsed data between the pre-phase base and `HEAD`; the `pyproject.toml` diff is exactly one hunk at line 38, inside the `dev` extra's line range (34-48); and `tests/test_preview_version_sync.py` passes (`3 passed`). Filed two todos: the D-03 `ruff` generic-linux-ELF defect (with the sibling `tox -e py312` NixOS interpreter-download defect Plan 02 discovered, folded in as a second section — same root cause), and the newly-discovered Windows path-separator defect.
- **Task 3 (D-12, closing verdict):** Step 7's SC#3 disposition (MET) gated the memory rewrite — quoted verbatim rather than re-derived. Rewrote `pytest-no-outer-uv-run-in-main-tree.md` in place (filename and Japanese body preserved): the prohibition is now recorded as lifted by QUA-04, with the historical cause in the past tense and Step 7's own measured post-fix counts (984 passed / 5 skipped / 0 failed, zero regressions) quoted, the still-true operational notes (dev-extra sync prerequisite, worktree guidance) preserved, and `MEMORY.md`'s matching index entry updated in the same edit. Wrote the closing verdict: all seven success criteria MET, each naming the Step that proves it; the D-11 shim-absence check reproduced for this worktree directly (matching the main tree's Step 5 recording); the two-todo inventory; the main-tree/worktree HEAD-agreement check; and a verbatim `git status --porcelain` (scoped and unscoped) as the file's final content.

## Task Commits

Each task was committed atomically (Task 1 required two commits — the fix, then the evidence write):

1. **Task 1a: Fix the newly-exposed CI failure** - `2fb6c6f` (fix)
2. **Task 1b: Record Step 8 — CI before/after evidence (SC#4)** - `f790f6a` (docs)
3. **Task 2: Assert SC#7 fence and file routed-out defects** - `f40da5f` (docs)
4. **Task 3: Retire the project memory, write the closing verdict** - `5cc7ad2` (docs)

_No separate plan-metadata commit for this worktree's own contribution: this plan runs in worktree isolation, and per the parallel-execution contract STATE.md/ROADMAP.md updates are owned by the orchestrator after the wave merges. The final metadata commit below covers only this SUMMARY.md._

## Files Created/Modified

- `tox.ini` - `[testenv]`'s `package = wheel` / `wheel_build_env = .pkg` replaced with `package = editable` (the `uv-venv-lock-runner`'s own default), with a comment explaining the CI-discovered packaging gap this fixes
- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` - appended `## Step 8`, `## Step 9`, `## Project memory retirement (D-12)`, `## Closing verdict`
- `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` - new todo (D-03), with the sibling `tox -e py312` interpreter-download finding folded in
- `.planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md` - new todo (D-18, discovered this plan)
- `/home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/pytest-no-outer-uv-run-in-main-tree.md` - rewritten in place (outside the repository, does not merge via git)
- `/home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/MEMORY.md` - one-line index entry updated to match (outside the repository)

## Decisions Made

- **Fixed the templates-packaging CI failure in-phase rather than filing it as a todo,** per D-18's own explicit named category: "a tool now running inside a tox environment that previously never ran there." The root cause (a pre-existing `package = wheel` setting silently dropping package data under a newer `uv`) predates this phase and is unrelated to `tox-uv`/`deps`/`extras`, but the *symptom* was newly exposed by this phase's own `extras = dev` fix (previously masked by the exact PATH-shadowing mechanism D-04 diagnosed for `lint`). The fix stays entirely inside `tox.ini`, so SC#7's `pyproject.toml`-only-inside-`dev`-extra fence is never touched — confirmed mechanically in Step 9 after the fact.
- **Classified the Windows path-separator failure as unrelated rather than fixed,** because it is verifiably orthogonal: a brand-new test (added the day before this phase, by a different plan in a different phase) hitting its first-ever real Windows CI exercise, with a root cause (`Path.relative_to()`'s platform-dependent separator) that has nothing to do with `tox-uv`, `deps`/`extras`, or `package = wheel`/`editable`. D-18's second bullet names exactly this category.
- **No todo filed for the transient "fetch failed" Type Check error** — it did not recur on a second dispatch with no code change in between, and its cause (a third-party GitHub Action's own network fetch) is outside this repository's control and non-actionable as a filed defect. Recorded verbatim in Step 8 instead, with the reasoning stated explicitly in Step 9 per this plan's own honesty requirement.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug, in-phase per D-18] `[testenv]`'s `package = wheel` silently drops `typsphinx/templates/base.typ` under CI's `uv`**
- **Found during:** Task 1, first and second CI dispatches
- **Issue:** `Code Coverage` and all six `Test Python 3.1{2,3} on {ubuntu,windows,macos}-latest` jobs failed with `FileNotFoundError: Default template not found ... Package installation may be corrupted.` — a non-editable typsphinx install (`--no-editable --reinstall-package typsphinx`, driven by `[testenv]`'s pre-existing, this-phase-unrelated `package = wheel` setting) was missing package data under CI's `uv` (`0.12.3`), though not under this machine's `uv` (`0.11.25`). Root-caused by reproducing a full `tox -e py313` run locally against the *unmodified* pre-fix `tox.ini` (984 passed, no `FileNotFoundError`) before writing any fix, and by inspecting `tox_uv/_run_lock.py`'s `package` config handling to confirm `package = editable` was a safe, supported, in-fence alternative.
- **Fix:** `tox.ini`'s `[testenv]` `package = wheel` / `wheel_build_env = .pkg` → `package = editable`.
- **Files modified:** `tox.ini`
- **Verification:** Local `tox -e py313` full suite → 984 passed/5 skipped; `tox -e type`/`tox -e lint` unaffected; `tox -e cov -- -k "test_load_default_template or test_get_default_template_path"` → 2 passed (previously the failing assertion). Re-pushed and re-dispatched CI (run `31445582363`): `Code Coverage` and all four non-Windows `Test Python` jobs flip to green.
- **Committed in:** `2fb6c6f` (Task 1's first commit)

---

**Total deviations:** 1 auto-fixed bug (in-phase per D-18's own named category, scoped to `tox.ini` only, SC#7 fence unaffected)
**Impact on plan:** No scope creep — the fix's root cause is genuinely unrelated to `tox-uv`/`deps`/`extras`, but D-18 explicitly names "a tool now running inside a tox environment that previously never ran there" as this phase's own to fix, and the fix landed entirely inside the file the fence still permits.

## Issues Encountered

None beyond the deviation recorded above. The Windows path-separator failure and the transient network fetch failure were both correctly classified as out-of-scope on first encounter (no wasted fix attempts) per D-18's own worked example.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 45.2 closes with all seven success criteria MET, each backed by a Step in `45.2-TOOLCHAIN-EVIDENCE.md`. Phase 46's own SC#3 (which reads local `tox` output and this branch's CI runs) inherits a genuinely green tree for everything this phase's fence covers, plus two honestly filed, unfixed defects (`ruff`/`py312` NixOS-local, Windows path-separator) that are explicitly NOT this milestone's concern.
- The project memory `pytest-no-outer-uv-run-in-main-tree.md` no longer prescribes the `.venv/bin/python -m pytest` workaround as mandatory — future sessions can use `uv run pytest` in the main tree directly, provided `uv sync --extra dev` has run first.
- No blockers for Phase 46. The two filed todos are independent, non-blocking follow-ups.

---
*Phase: 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare*
*Completed: 2026-08-11*

## Self-Check: PASSED

- FOUND: `tox.ini`
- FOUND: `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md`
- FOUND: `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md`
- FOUND: `.planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md`
- FOUND: `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-05-SUMMARY.md`
- FOUND: `/home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/pytest-no-outer-uv-run-in-main-tree.md`
- FOUND: commit `2fb6c6f` (Task 1a)
- FOUND: commit `f790f6a` (Task 1b)
- FOUND: commit `f40da5f` (Task 2)
- FOUND: commit `5cc7ad2` (Task 3)
