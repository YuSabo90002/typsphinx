---
phase: 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md
autonomous: true
requirements: [QUA-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "45.2-TOOLCHAIN-EVIDENCE.md records the main tree's pre-fix state with verbatim command output: .venv/bin/uv is a regular ELF file (not a symlink), command -v uv resolves a /nix/store path, and importlib.metadata reports uv, tox, tox-uv and tox-uv-bare all installed (D-13, SC#2 before-half)"
    - "45.2-TOOLCHAIN-EVIDENCE.md records a verbatim pre-fix transcript of tox -e lint and tox -e docs-pdf run in /home/yuta/Documents/typsphinx with no TOX_UV_PATH and no other environment override, including the exit code (SC#1 before-half)"
    - "45.2-TOOLCHAIN-EVIDENCE.md records absolute pre-fix pytest counts for the full suite under an outer uv run pytest in the main tree, plus the same suite under .venv/bin/python -m pytest as the control, with the sorted set of failing node ids for both (SC#3 before-half, D-14)"
    - statement: "No other agent mutated /home/yuta/Documents/typsphinx/.venv or /home/yuta/Documents/typsphinx/.tox while this plan's measurements ran — guaranteed structurally by this plan being the sole plan in wave 1, and by every other plan's executor keeping VIRTUAL_ENV and UV_PROJECT_ENVIRONMENT unset so its own uv sync writes only its worktree .venv"
      verification: backstop
  artifacts:
    - path: ".planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md"
      provides: "Pre-fix baseline sections Step 0, Step 1 and Step 2 with verbatim transcripts"
      contains: "## Step 0"
      min_lines: 90
  key_links:
    - from: ".planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md"
      to: "/home/yuta/Documents/typsphinx/.venv/bin/uv"
      via: "recorded byte size, file type and ELF interpreter of the pre-fix binary"
      pattern: "ld-linux-x86-64"
  prohibitions:
    - "Never create /home/yuta/Documents/typsphinx/.venv/bin/uv or .venv/bin/uvx as a symlink or copy pointing at a nix-store or system uv (D-11). SC#2 requires those paths to be absent after the fix; applying the shim out of habit destroys this phase's own verification and silently invalidates the baseline recorded here."
    - "Never modify .planning/MILESTONES.md, .planning/RETROSPECTIVE.md, anything under .planning/milestones/, .planning/PROJECT.md line 951, .planning/STATE.md line 425 or .planning/STATE.md line 587 (D-08). Those six records describe REL-04's release-job failure — a different, real bug — and are the recorded justification for an open requirement."
    - "Never set TOX_UV_PATH, prepend a directory to PATH, or otherwise override the environment when capturing the pre-fix tox transcript. SC#1 requires the failure to be observed under the maintainer's real environment; an override produces a transcript that proves nothing."
    - "Never record a measurement that was not actually run, and never paraphrase command output. Every transcript in the evidence file is verbatim, and any step that could not be run says NOT RUN in those words with the reason. This phase exists because the project believed unverified claims about its own toolchain."
---

<objective>
Capture the pre-fix baseline of the maintainer's machine, once, in the MAIN TREE, before any file
in this phase changes.

Purpose: this phase's every success criterion is a before/after comparison. D-13 (locked user
decision) states the baseline is captured once in the main tree because what is being measured is
the *environment*, not the code — each worktree runs its own `uv sync`, which reinstalls the broken
binary and produces numbers that cannot be compared. `<specifics>` item 7 of `45.2-CONTEXT.md`
verified on 2026-08-10 that the main tree is an uncontaminated pre-fix baseline right now:
`.venv/bin/uv` is a 63,949,040-byte generic-linux ELF, not a symlink. That state is perishable —
the first `uv sync` run against a fixed `pyproject.toml` destroys it forever.

Output: `45.2-TOOLCHAIN-EVIDENCE.md` (new) carrying Step 0 (preconditions and provenance),
Step 1 (SC#2 before-half), and Step 2 (SC#1 and SC#3 before-halves), each with verbatim transcripts.

This plan makes NO source edit of any kind. It is measurement only.
</objective>

<wave_and_isolation_contract>
**Read this before running any command. It reconciles D-13 with `CLAUDE.md`'s standing
worktree-isolation mandate, and it is load-bearing for every measurement in this plan.**

`CLAUDE.md` § "Worktree-isolated execution" makes worktree isolation the standing execution mode,
so this plan's executor is almost certainly running inside an isolated git worktree (`.git` is a
FILE, not a directory). D-13 nonetheless requires the baseline to be taken against the MAIN TREE
at `/home/yuta/Documents/typsphinx`, because that is the environment the phase repairs.

Both are satisfied at once as follows:

1. **Provision your own worktree normally, per `CLAUDE.md`.** Run
   `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` with the worktree as the
   working directory. This creates the worktree's own `.venv`. It does NOT touch the main tree.
   You need it so your own tooling works; you must NOT measure it.
2. **Every measurement command in this plan targets the main tree explicitly** by passing
   `--directory /home/yuta/Documents/typsphinx` (for `uv run`) or `--project
   /home/yuta/Documents/typsphinx` (for `uv sync`), or by invoking an absolute path under
   `/home/yuta/Documents/typsphinx/.venv/bin/`. Never use `cd`.
3. **Every main-tree command is prefixed with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT`.**
   Without those unsets, `uv` re-points the already-activated venv at the wrong project and the
   measurement is meaningless.
4. **Nothing this plan writes to the main tree is git-tracked.** The only main-tree side effects
   are `/home/yuta/Documents/typsphinx/.venv` and `/home/yuta/Documents/typsphinx/.tox`, both
   gitignored (`.gitignore:31` for `.tox`). The single git-tracked file this plan creates,
   `45.2-TOOLCHAIN-EVIDENCE.md`, is written inside your worktree and merges back normally.
5. **This plan is the ONLY plan in wave 1.** That is deliberate: it is the structural guarantee
   that no concurrent agent mutates the shared main-tree `.venv`/`.tox` mid-measurement. Do not
   run any other plan's work here.
</wave_and_isolation_contract>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md
@.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 45.2-TOOLCHAIN-EVIDENCE.md and record Step 0 (provenance) + Step 1 (SC#2 before-half)</name>
  <precondition>/home/yuta/Documents/typsphinx/.venv/bin/uv exists and is a regular file, not a symlink — the D-11 shim has not been applied and the main tree is still an uncontaminated pre-fix baseline. Check with `test -f /home/yuta/Documents/typsphinx/.venv/bin/uv &amp;&amp; test ! -L /home/yuta/Documents/typsphinx/.venv/bin/uv`. If it is a symlink, or absent, HALT — the baseline D-13 requires no longer exists and the phase cannot proceed as planned.</precondition>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-PATTERNS.md` — section "`45.2-TOOLCHAIN-EVIDENCE.md` (new evidence-doc)" gives the seven-part structure to follow, derived from the archived `41-GREEN-TREE-EVIDENCE.md`.
    - `.planning/milestones/v0.7.0-phases/41-v0-7-0-release-automation-release-prep/41-GREEN-TREE-EVIDENCE.md` — lines 1-11 (title + scope banner), 15-65 (preconditions section), 69-111 (a "Step N" section: verbatim command block, verbatim output block, explanatory paragraph). This is the shape analog D-15 designates.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-13, D-15 and `<specifics>` items 5 and 7 (the exact pre-fix facts this task must independently re-measure rather than transcribe).
  </read_first>
  <action>
    Create `45.2-TOOLCHAIN-EVIDENCE.md` in the phase directory. The filename is fixed by D-15 and
    must NOT be `45.2-VERIFICATION.md` — that name belongs to gsd-verifier and gets clobbered.

    Write a title naming Phase 45.2 / QUA-04, and a scope banner stating which success criteria the
    file speaks to (SC#1 through SC#7) and what it does NOT speak to (the ruff generic-linux-ELF
    defect, which D-03 routes out of this phase, and REL-04's release-job failure, which is a
    different bug).

    Then write `## Step 0 — Provenance and preconditions`, recording verbatim output for: the main
    tree's `git rev-parse HEAD` and `git rev-parse --abbrev-ref HEAD`; `git status --porcelain` for
    the main tree; and confirmation that the worktree-vs-main-tree distinction was honoured by
    printing the value of `pwd` alongside the absolute main-tree path used for every measurement.

    Then write `## Step 1 — Pre-fix environment census (SC#2 before-half)`, recording verbatim
    output for each of the following, run with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT` and
    absolute paths under `/home/yuta/Documents/typsphinx`:
    - `ls -l` and `stat -c '%s %F'` on `.venv/bin/uv` and `.venv/bin/uvx` — record the byte size and
      that the type is "regular file", not "symbolic link".
    - `file` on `.venv/bin/uv`, `.venv/bin/uvx` and `.venv/bin/ruff` — record the ELF interpreter
      string for each.
    - `.venv/bin/uv --version` and `.venv/bin/ruff --version` — record the failure text verbatim.
    - `command -v uv` and `file` on whatever it resolves to — record the nix-store path and its
      interpreter, showing the working build sits further down the same PATH.
    - `.venv/bin/python -c` printing, for each of the four distribution names `uv`, `tox`, `tox-uv`
      and `tox-uv-bare`, the installed version from `importlib.metadata.version`, or the exception
      type name when it raises. Per `<specifics>` item 7 all four are expected present pre-fix,
      with `tox-uv-bare` already there transitively — which is why the fix REMOVES a package rather
      than adding one. Record whatever is actually observed, not what is expected.

    Under each fenced output block write one short paragraph connecting the output to SC#2's
    before-half. Do not paraphrase output into prose; the fenced blocks are the evidence.

    Do NOT run `uv sync` in this task. Observing the current state is the point.
  </action>
  <verify>
    <automated>test -f .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; grep -c '^## Step 0' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; grep -c '^## Step 1' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; grep -c 'ld-linux-x86-64' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-VERIFICATION.md` succeeds — the evidence went to the D-15 filename, not the verifier's.
    - `grep -c '^## Step 0' 45.2-TOOLCHAIN-EVIDENCE.md` returns 1 and `grep -c '^## Step 1' 45.2-TOOLCHAIN-EVIDENCE.md` returns 1.
    - The Step 0 section contains a 40-hex-character commit SHA (`grep -Eq '[0-9a-f]{40}'`).
    - The Step 1 section contains the literal `ld-linux-x86-64` at least twice — once for `.venv/bin/uv` and once for `.venv/bin/ruff` — proving the ELF interpreter was actually captured for both.
    - The Step 1 section contains an `importlib.metadata` census listing all four of `uv`, `tox`, `tox-uv`, `tox-uv-bare` with either a version string or an exception type name for each.
    - `test -f /home/yuta/Documents/typsphinx/.venv/bin/uv` still succeeds after this task — the task observed, it did not repair.
    - Every fenced output block in Steps 0 and 1 is non-empty; no block contains an ellipsis standing in for output that was not captured.
  </acceptance_criteria>
  <done>`45.2-TOOLCHAIN-EVIDENCE.md` exists with Step 0 and Step 1 populated by verbatim main-tree output, and the main tree's `.venv/bin/uv` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Record Step 2 — pre-fix tox transcript (SC#1 before-half)</name>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</files>
  <read_first>
    - `tox.ini` — the current on-disk state: `requires = tox-uv~=1.35` at line 11, `runner = uv-venv-lock-runner` on every environment, and `deps =` blocks on `[testenv]`, `[testenv:lint]`, `[testenv:type]`, `[testenv:cov]` while `[testenv:docs-html]`, `[testenv:docs-pdf]`, `[testenv:docs]` use `extras = docs`.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — `<specifics>` item 2 (the measured `tox -e lint` failure shape) and item 8 (`tox` runs create `.tox/` only; `.tox/` is gitignored at `.gitignore:31`).
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — the file created by Task 1, so the new section continues its numbering and formatting.
  </read_first>
  <action>
    Append `## Step 2 — Pre-fix tox transcript (SC#1 before-half)` to `45.2-TOOLCHAIN-EVIDENCE.md`.

    Run each of the following against the MAIN TREE and record the exact command, the verbatim
    combined stdout/stderr, and the numeric exit code (capture it explicitly with `echo $?`; do not
    infer it):
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx tox -e lint`
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx tox -e docs-pdf`

    `docs-pdf` is included deliberately: SC#1 names it as the environment this phase must not skip,
    because Phase 46's SC#3 depends on it and it has never been exercised end to end.

    Also run the direct-binary form `/home/yuta/Documents/typsphinx/.venv/bin/tox -e lint` from the
    main tree and record it separately, so the transcript distinguishes "fails under an outer
    `uv run`" from "fails when invoked directly" — both are expected to fail identically pre-fix,
    since `tox-uv` reaches the binary through `uv.find_uv_bin()`, which searches `.venv/bin` first
    and consults no environment variable.

    Record explicitly, in prose next to the transcripts, that `TOX_UV_PATH` was NOT set and that no
    directory was prepended to `PATH` for any of these runs — print `env | grep -c TOX_UV_PATH` and
    record its output as the proof. SC#1 requires the failure observed under the real environment.

    If a run unexpectedly succeeds, record it as observed and say so plainly; do not retry until it
    fails. If `tox` cannot start at all (rather than starting and failing), that is itself the
    measurement — record the startup error verbatim.

    Finish the section with one paragraph naming which SC#1 clause each transcript is the
    before-half of.
  </action>
  <verify>
    <automated>grep -c '^## Step 2' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; grep -c 'docs-pdf' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Step 2' 45.2-TOOLCHAIN-EVIDENCE.md` returns 1.
    - Step 2 contains three distinct fenced command blocks: one for `tox -e lint` under `uv run`, one for `tox -e docs-pdf` under `uv run`, and one for the direct `.venv/bin/tox -e lint` invocation.
    - Each of the three has an accompanying recorded numeric exit code captured via `echo $?`, present as a digit in the file.
    - Step 2 contains the recorded output of `env | grep -c TOX_UV_PATH`, proving no environment override was in play.
    - `git status --porcelain -- tox.ini pyproject.toml uv.lock` is empty — this task changed no configuration file.
    - `git check-ignore -q .tox` succeeds (or `.tox` does not exist), confirming the tox runs left nothing git-tracked behind.
  </acceptance_criteria>
  <done>Step 2 records verbatim pre-fix transcripts and exit codes for `tox -e lint` (two invocation forms) and `tox -e docs-pdf` from the main tree, with the absence of `TOX_UV_PATH` proven by recorded output.</done>
</task>

<task type="auto">
  <name>Task 3: Record Step 3 — pre-fix full-suite pytest census (SC#3 before-half, D-14)</name>
  <files>.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-CONTEXT.md` — D-14 (the census is the FULL suite's absolute counts, before and after, under an outer `uv run pytest`; a 5-module-only measurement would not show whether other failures hide behind the same cause) and D-09 (the five `uv run`-invoking modules are the regression detector and are never normalized away).
    - `tests/test_examples_basic.py` — the `subprocess.run(["uv", "run", "sphinx-build", ...])` idiom at roughly lines 105-109, 134-138 and 165-169. This and the four `tests/test_integration_{basic,advanced,multi_doc,nested_toctree}.py` modules are the only five that genuinely invoke `uv` as a subprocess.
    - `/home/yuta/.claude/projects/-home-yuta-Documents-typsphinx/memory/pytest-no-outer-uv-run-in-main-tree.md` — the standing project memory. It claims 45 failures under an outer `uv run pytest` and `946 passed / 1 skipped` under `.venv/bin/python -m pytest`. `.planning/STATE.md:349` records a different pair (`45F/776P`). This task's job is to settle which absolute numbers are real today; do not carry either claim forward unverified.
    - `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — the running evidence file.
  </read_first>
  <action>
    Append `## Step 3 — Pre-fix full-suite pytest census (SC#3 before-half)` to
    `45.2-TOOLCHAIN-EVIDENCE.md`.

    First ensure the main tree's environment is synced in its current (pre-fix) shape, so `pytest`
    is importable: run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --project
    /home/yuta/Documents/typsphinx`. This is expected to leave the broken binary in place because
    the fix has not landed yet — record `ls -l /home/yuta/Documents/typsphinx/.venv/bin/uv`
    immediately afterwards to prove the baseline survived the sync.

    Then run BOTH of the following over the whole suite and record each one's verbatim final
    summary line plus its full `FAILED ...` short-summary list:
    - Measurement run (the invocation `CLAUDE.md` mandates, and the one D-14 names):
      `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run --directory /home/yuta/Documents/typsphinx pytest -q --tb=no -rf`
    - Control run (the workaround the project memory currently prescribes):
      `/home/yuta/Documents/typsphinx/.venv/bin/python -m pytest -q --tb=no -rf` executed with the
      main tree as the working directory via `uv run --directory` is NOT appropriate here — invoke
      the absolute interpreter path directly so no outer `uv run` wraps it.

    Record for each run: total passed, total failed, total skipped, total errors, and wall time.
    Then write a `### Failing node-id sets` subsection containing, for each run, the failing test
    node ids extracted from the `FAILED` lines and sorted with `sort`. Compare the two sets and
    state in prose (a) how many node ids are in the measurement run but not the control, and (b)
    whether every one of those belongs to one of the five modules named in `<read_first>`. That
    difference is the population SC#3 claims this phase eliminates.

    Because pytest's collection order is not guaranteed stable between runs, every comparison in
    this section and in the post-fix census must be made over SORTED SETS of node ids, never over a
    raw diff of console output. State that rule explicitly in the section so the post-fix census
    follows it.

    The suite is large; note the wall time for each run so the post-fix comparison is like-for-like.
    Do not truncate the `FAILED` list — if it is long, that is the measurement.

    Close the section with a short honesty statement in the shape `41-GREEN-TREE-EVIDENCE.md` uses:
    state that if the measurement run had shown zero failures, this section would say so in those
    words and SC#3's premise would be recorded as already satisfied.
  </action>
  <verify>
    <automated>grep -c '^## Step 3' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md &amp;&amp; grep -c '^### Failing node-id sets' .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Step 3' 45.2-TOOLCHAIN-EVIDENCE.md` returns 1 and `grep -c '^### Failing node-id sets' 45.2-TOOLCHAIN-EVIDENCE.md` returns 1.
    - Step 3 contains two distinct pytest summary lines, each matching `grep -Eq '[0-9]+ (passed|failed)'`, one for the outer-`uv run` measurement run and one for the direct-interpreter control run.
    - Step 3 records `ls -l /home/yuta/Documents/typsphinx/.venv/bin/uv` output taken AFTER the pre-fix `uv sync`, showing the binary still present — proof the baseline was not accidentally repaired mid-measurement.
    - Step 3 states, as a numeric count, how many failing node ids appear in the measurement run but not in the control run.
    - Step 3 contains an explicit sentence establishing that before/after comparison is over sorted node-id sets, not over raw console output.
    - `git status --porcelain -- tests/ typsphinx/ tox.ini pyproject.toml uv.lock` is empty — the census changed no source file.
  </acceptance_criteria>
  <done>Step 3 records both pre-fix censuses with absolute counts and sorted failing node-id sets, the delta between them is stated numerically, and the sorted-set comparison rule is written into the file for the post-fix run to follow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree executor → main-tree filesystem | This plan issues commands that write `/home/yuta/Documents/typsphinx/.venv` and `.tox` from outside the main tree. Both are gitignored; no git-tracked main-tree path is written. |
| command output → committed evidence file | Verbatim transcripts of local commands are committed to the repository, so whatever the shell printed becomes a published artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45.2-03 | Repudiation | `45.2-TOOLCHAIN-EVIDENCE.md` | medium | mitigate | Every step records `git rev-parse HEAD`, the exact command, and verbatim output with an explicitly captured `echo $?` exit code; the acceptance criteria reject ellipsis-truncated blocks, so a paraphrased or fabricated measurement fails the task gate. |
| T-45.2-04 | Information disclosure | committed transcripts | low | mitigate | Before committing, scan the evidence file for anything matching a credential shape (`gh[pousr]_`, `pypi-`, `AKIA`, `Bearer `) and redact it. Absolute paths under `/home/yuta` are already present throughout `.planning/` and are not treated as sensitive. |
| T-45.2-05 | Denial of service | main-tree `.venv` | low | accept | The pre-fix `uv sync` in Task 3 rebuilds the main-tree environment in its existing shape. If interrupted it can be re-run idempotently; nothing git-tracked depends on it. |
| T-45.2-06 | Tampering | main-tree `.tox` | low | accept | `tox` runs create `.tox/`, which is gitignored (`.gitignore:31`) and was measured in `<specifics>` item 8 to leave `git status` clean. No mitigation needed beyond the acceptance criterion asserting it. |
</threat_model>

<artifacts_this_phase_produces>
New symbols created by Phase 45.2 (listed in every plan so drift verification excludes them):

- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` — new file, created by this plan (Plan 01), appended by Plans 03, 04 and 05.
- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/COVERAGE.md` — new file, written at plan time.
- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-0{1..5}-SUMMARY.md` — new files, one per plan.
- `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` — new todo filed by Plan 05 (D-03).
- `.planning/todos/pending/2026-08-11-<slug>.md` — conditional new todo filed by Plan 05 only if `tox -e docs-pdf` fails for a cause outside this phase's dependency/`tox.ini` scope (D-19).
- `pyproject.toml` `[project.optional-dependencies] dev` gains the requirement name `tox-uv-bare` — new symbol, created by Plan 02.
- `tox.ini` gains an `extras` key on `[testenv]`, `[testenv:lint]`, `[testenv:type]` and `[testenv:cov]` — new keys, created by Plan 02.
</artifacts_this_phase_produces>

<verification>
- `45.2-TOOLCHAIN-EVIDENCE.md` exists in the phase directory with Steps 0, 1, 2 and 3.
- No file under `typsphinx/`, `tests/`, `tox.ini`, `pyproject.toml` or `uv.lock` was modified by this plan: `git diff --name-only` over this plan's commits lists only the evidence file and the plan's SUMMARY.
- `/home/yuta/Documents/typsphinx/.venv/bin/uv` is still the pre-fix regular ELF file when this plan returns.
</verification>

<success_criteria>
- SC#1's before-half, SC#2's before-half and SC#3's before-half are each recorded as verbatim main-tree transcripts with explicitly captured exit codes and absolute counts.
- The sorted-node-id-set comparison rule is written into the evidence file for the post-fix census (Plan 04) to follow.
- The main tree remains an uncontaminated pre-fix baseline when this plan returns.
</success_criteria>

<output>
Create `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-01-SUMMARY.md` when done.
</output>
