---
phase: 63-v0-9-2-release-prep-prep-only
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md
  - .planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md
autonomous: true
requirements: [REL-09, REL-11]
requirements-completed: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC#3 / REL-11 / D-16: a 63-CLOSEOUT-GUARD.md records, at phase head and inside this plan''s own worktree, the four baseline commands as commands with their literal output — sha256sum of .planning/REQUIREMENTS.md, wc -l of the same file, the UTC timestamp, git rev-parse HEAD as PHASE_BASE_SHA, and grep -n for REL-09 with every hit quoted verbatim including its line number.'
    - 'D-16: the fence is a THREE-observation protocol, and the guard file itself carries all three. Observation one is this plan''s baseline; observation two is plan 63-04''s close-time re-verification; observation three is re-run once more AFTER phase.complete-family tooling has run, and the guard file carries a "For the operator running phase.complete" section holding that third protocol verbatim, because that observation runs outside any plan''s reach and is the one that historically catches the flip.'
    - 'D-16: the response to a detected divergence is written into the guard file as revert-and-report, never accept-and-commit — ``git checkout -- .planning/REQUIREMENTS.md`` followed by reporting it. Five consecutive prior release-prep closes had the release checkbox auto-flipped against the CONTEXT''s explicit decision; Phase 61 was the first to hold, using exactly this procedure.'
    - 'Pitfall 6: the guarded-line shape is RE-MEASURED against the current .planning/REQUIREMENTS.md rather than copied from 61-CLOSEOUT-GUARD.md. The grep returns three hits, and the guard file states explicitly which of them are treated as state-bearing (the checkbox line and the Traceability row) and which is informational-only (the third hit, which opens a multi-line prose paragraph rather than being a single terse line as Phase 61''s third hit was). The whole-file SHA-256 covers all three regardless of that classification.'
    - 'SC#5 observation 1 of 2: a 63-SC5-INVARIANTS.md records the local tag probe, the remote tag probe, the publish probe and the release-workflow probe, each with the positive control that proves the probe reached its source. This is the FIRST of two observations that SC#5 requires to be separated by intervening waves; observation two belongs to plan 63-04 in wave 3.'
    - 'Every remote probe is unfiltered-then-counted, never a bare filtered query whose silence is indistinguishable from a network failure: one unfiltered ``git ls-remote --tags origin`` fetch yields BOTH a positive-control count against a tag known to exist and the negative assertion count against the release tag, derived from that same single fetch.'
    - 'The milestone-invariant-sweep question that 61-SC4-INVARIANTS.md deferred to this phase is resolved IN WRITING here rather than left as a silent absence: the sweep is assigned to plan 63-01 Task 2, is run against the v0.9.0 anchor with a widened-scope positive control, and is recorded in 63-CHANGELOG-EVIDENCE.md. This plan records that assignment and the anchor identity; it does not re-run the sweep and does not assert its result, because 63-01 executes in the same wave and its artifact is not yet readable from this worktree.'
    - 'PHASE_BASE_SHA is recorded as the FIRST command of this plan, before any file is created or committed in this worktree, so it names the phase''s own base commit rather than this plan''s tip. The recorded commit''s subject is quoted alongside it, and it does not carry a Phase 63 commit scope.'
    - 'A COVERAGE.md declaration exists, records the plan-time external-API detector result verbatim, and explains in reasoned prose why this phase''s gh-dense and release-pipeline-dense plan bodies are a false-positive surface for a seal-time re-run — rather than fabricating a capability matrix for an API this phase does not touch.'
    - 'D-16: REL-09 is cited by this plan as a coverage ID ONLY. This plan reads .planning/REQUIREMENTS.md and quotes it; it never edits it. 63-02-SUMMARY.md frontmatter declares ``requirements-completed: []`` for REL-09.'
  artifacts:
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md
    - .planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md
  key_links:
    - '63-CLOSEOUT-GUARD.md PHASE_BASE_SHA -> plan 63-03''s product-tree delta and plan 63-04''s scoped typsphinx/ diff: this one recorded value is the anchor two later plans read against. A wrong anchor makes both of their empty results vacuous, which is why 63-04 pairs the scoped diff with a same-anchor widened diff as its positive control.'
    - '.planning/REQUIREMENTS.md REL-09 checkbox -> phase.complete-family tooling -> the milestone-close reader and /gsd-complete-milestone: an auto-flip here misrepresents the release''s true state to a future reader. The SHA-256 fence is the only measure that has ever caught it, and its third observation runs after the tooling, outside any plan.'
    - 'the unfiltered git ls-remote and gh release list fetches -> their positive-control counts: without a control, an unreachable network endpoint produces the same empty result as a genuinely empty remote, and the fence proof becomes unfalsifiable.'
  prohibitions:
    - 'MUST NOT perform any irreversible action in this phase — no git tag (local or remote), no tag push, no PyPI upload, no GitHub Release creation, no pull request, no update-pin.yml dispatch on typsphinx-doc-translations, no Read the Docs re-flip, and no trigger of release.yml by any route (tag push or workflow_dispatch). Each of those executes at /gsd-complete-milestone and is RECORDED in 63-HANDOFF.md, never performed.'
    - 'MUST NOT touch anything under typsphinx/. SC#5 git diff over the phase proves that path empty, and any edit there is a scope escape dressed as release prep.'
    - 'MUST NOT modify .planning/REQUIREMENTS.md at all — not its REL-09 checkbox, not its Traceability row, not a single byte. This plan reads and quotes that file and nothing more. If tooling flips it, the flip is reverted with ``git checkout -- .planning/REQUIREMENTS.md`` and reported — never accepted, never committed. No SUMMARY.md in this phase declares REL-09 in requirements-completed.'
    - 'MUST NOT record inherited, paraphrased, recalled or fabricated evidence as this phase own. Phase 61 baseline values are NOT copied forward; every value in the guard file is measured fresh in this worktree, and no probe is recorded without the positive control that proves it reached its source.'
---

<objective>
Record the phase-head half of the two mechanisms that keep this prep-only phase honest: the SHA-256
closeout fence around REL-09's checkbox, and the first of the two waves-separated observations that
prove no irreversible action has been taken.

Purpose: REL-09's checkbox has been auto-flipped to `[x]` by `phase.complete`-family tooling at five
consecutive prior release-prep closes, against the CONTEXT's explicit decision each time. Phase 61
was the first to hold, and the only thing that made the difference was a recorded checksum baseline
re-verified at three separate moments. This plan records that baseline and the anchor commit two
later plans read their diffs against, then takes fence observation one — with a positive control on
every remote probe, because a probe that cannot reach its source produces the same silence as a
genuinely clean remote.

Output: `63-CLOSEOUT-GUARD.md` (baseline, the lines under guard, the re-verification protocol, and
the post-`phase.complete` operator protocol), `63-SC5-INVARIANTS.md` (observation 1 of 2, with the
milestone anchor and the sweep-ownership resolution), and `COVERAGE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-VALIDATION.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution";
ROADMAP milestone constraint 12). It is mandatory and not conditional on parallelism. Before running
ANY command in this plan, provision this worktree's own virtual environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. This plan runs no pytest and no build, but the
provisioning is unconditional and the worktree identity it establishes is what makes
`git rev-parse HEAD` in Task 1 the phase's own base rather than the main checkout's tip.

**This plan executes in wave 1 alongside plan 63-01, in a separate worktree.** It therefore cannot
read anything 63-01 writes, and must not assert that any 63-01 artifact exists. Where this plan needs
to reference 63-01's work it records the *assignment* in writing, never a measured result.
</worktree_provisioning>

<!-- planner-discipline-allow: Latest -->
<!-- planner-discipline-allow: positive control -->
<!-- planner-discipline-allow: Observation 1 of 2 -->
<!-- planner-discipline-allow: Handoff to observation 2 -->
<!-- planner-discipline-allow: milestone-invariant sweep -->
<!-- planner-discipline-allow: REL-09 -->
<!-- planner-discipline-allow: state-bearing -->
<!-- planner-discipline-allow: For the operator running phase.complete -->
<!-- planner-discipline-allow: git checkout -- .planning/REQUIREMENTS.md -->
<!-- planner-discipline-allow: never triggered -->
<!-- planner-discipline-allow: zero packages -->
<!-- planner-discipline-allow: release.yml -->
<!-- Every literal above appears in a POSITIVE presence check (`-ge 1`, `-ge 3`, `= 1`) over an
     evidence file this plan writes, never in an absence check, so echoing it into an action body
     cannot invalidate anything. `Latest` is the publish probe's own positive control and must be
     named in the action for the executor to record it; the section headings are the shapes the
     evidence file is required to carry. -->

<tasks>

<task type="auto">
  <name>Task 1: Record the phase-head REQUIREMENTS.md checksum fence, the PHASE_BASE_SHA anchor, and the three-observation protocol including the post-`phase.complete` operator half</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md</files>
  <precondition>This worktree carries no Phase 63 commit yet — `git log --format=%s -1` must not name a `63-` commit scope. If it does, the value recorded as PHASE_BASE_SHA would be this plan's own tip rather than the phase's base, and two later plans would scope their diffs against the wrong anchor. Halt and report rather than recording a wrong anchor.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full — the file being guarded (and never edited). Read REL-09's checkbox bullet, its Traceability row, and the phase-mapping-notes paragraph that also names it, so the guarded-line classification below is made against what the file actually contains rather than against a recalled shape.
    - `.planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md` in full — the procedure D-16 reuses verbatim, including its "Baseline", "The lines under guard", "Re-verification protocol (phase close)", "Post-close detection and reversion (after phase.complete runs)" and "For the operator running phase.complete" sections. This is the template; only the phase number and the freshly-measured values change.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-16 in full.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md` § "Common Pitfalls" Pitfall 6 and § "Code Examples" § "The closeout-guard fence (D-16/REL-11)" — including its explicit statement that the research session's own numbers are a cross-check and never this plan's baseline.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md` § "`63-CLOSEOUT-GUARD.md`" — the section skeleton and the verbatim baseline-block shape.
    - `.planning/ROADMAP.md` § "Phase Details → Phase 63" SC#3, and § "🚧 v0.9.2" milestone constraint 9.
  </read_first>
  <action>
    Create `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md`, following
    `61-CLOSEOUT-GUARD.md`'s section skeleton with the phase number resolved and every value
    re-measured fresh in this worktree.

    Open the file with a one-paragraph statement that this task changes NO requirement state: REL-09
    stays an unchecked box and its Traceability row stays Pending; `.planning/REQUIREMENTS.md` is
    read and quoted here and never edited.

    **`## Baseline`.** Run, as the very first commands of this task and before creating any file:
    `git rev-parse HEAD`, `git log --format='%H %s' -1`, `git rev-parse --abbrev-ref HEAD`,
    `sha256sum .planning/REQUIREMENTS.md`, `wc -l .planning/REQUIREMENTS.md`, and
    `date -u` in ISO-8601 Zulu form. Record each as a command with its literal output. Name the
    `git rev-parse HEAD` value explicitly as **PHASE_BASE_SHA**, in its own labelled line, because
    plans 63-03 and 63-04 read that label back out of this file to scope their diffs. Quote the
    anchor commit's subject beside it so a later reader can confirm by eye that it predates this
    phase's own work. Also record this worktree's directory name, as `61-CLOSEOUT-GUARD.md` does, so
    the baseline's provenance is unambiguous.

    Do NOT copy Phase 61's digest, line count or line numbers. Do not copy the cross-check values
    printed in `63-RESEARCH.md` either — that research states in its own comment that they are a
    cross-check and not this plan's baseline. Measure fresh.

    **`## The lines under guard`.** Run `grep -n 'REL-09' .planning/REQUIREMENTS.md` and quote every
    hit verbatim, with its line number, in its own labelled subsection. Then resolve Pitfall 6
    explicitly in writing rather than by copying Phase 61's three-hit shape: state which hits are
    treated as **state-bearing** — the requirement bullet's checkbox line and the Traceability row,
    the two lines whose content would change if the checkbox flipped — and which is
    **informational-only**, being the opening line of a multi-line prose paragraph rather than a
    single terse line as Phase 61's third hit was. State in the same paragraph that the classification
    changes nothing about detection coverage, because the whole-file SHA-256 covers all three
    regardless, and that the classification exists only so the close-time re-verification compares the
    right things line-for-line. A guard file that quotes a line shape the current file does not
    contain is itself the defect Pitfall 6 names.

    **`## Why this file exists`.** State the measured history in two or three sentences:
    `phase.complete`-family tooling has auto-flipped the release requirement to checked against the
    CONTEXT's explicit decision at five consecutive release-prep closes; Phase 61 was the first to
    hold, using this procedure; and the flip is reverted and reported, never accepted and never
    committed.

    **`## Re-verification protocol (phase close)`.** Reproduce the exact command triad the close-time
    plan will run — `sha256sum` compared against the recorded Baseline digest,
    `git diff --name-only` scoped to `.planning/REQUIREMENTS.md` expecting no output, and the
    `grep -n 'REL-09'` comparison expecting byte-identical hits — with the expected values inlined so
    the comparison needs no second file open.

    **`## Post-close detection and reversion (after phase.complete runs)`** and **`## For the operator
    running phase.complete`.** Reproduce the same triad a third time, addressed to the operator, with
    the reversion command spelled out and the rule stated plainly: a detected flip is reverted with
    `git checkout -- .planning/REQUIREMENTS.md` and reported; it is never accepted and never
    committed. State that this third observation is the decisive one precisely because it runs after
    the tooling and outside any plan's reach, and note that `63-HANDOFF.md` will point an operator
    here so this section is reachable without opening the guard file separately.

    **`## This task's own effect on .planning/REQUIREMENTS.md`.** Record
    `git diff --name-only -- .planning/REQUIREMENTS.md` and `git status --porcelain
    .planning/REQUIREMENTS.md`, both expected empty, proving this task read the file and changed
    nothing.
  </action>
  <verify>
    <automated>test -f .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md &amp;&amp; [ "$(grep -c 'PHASE_BASE_SHA' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md)" -ge 1 ] &amp;&amp; [ "$(grep -oE '\b[0-9a-f]{40}\b' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md | head -1 | wc -c)" = 41 ] &amp;&amp; [ "$(grep -oE '\b[0-9a-f]{64}\b' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md | head -1)" = "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" ] &amp;&amp; [ "$(grep -c "$(wc -l &lt; .planning/REQUIREMENTS.md) .planning/REQUIREMENTS.md" .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'For the operator running phase.complete' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'git checkout -- .planning/REQUIREMENTS.md' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'state-bearing' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'REL-09' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md)" -ge 3 ] &amp;&amp; [ "$(grep -c '^- \[ \] \*\*REL-09\*\*' .planning/REQUIREMENTS.md)" = 1 ] &amp;&amp; [ "$(grep -c '| REL-09 | Phase 63 | Pending |' .planning/REQUIREMENTS.md)" = 1 ] &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md)" ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/)" ] &amp;&amp; [ ! -f .planning/phases/63-v0-9-2-release-prep-prep-only/63-VERIFICATION.md ]</automated>
  </verify>
  <acceptance_criteria>
    - `63-CLOSEOUT-GUARD.md` exists and its `## Baseline` section records six commands with their literal output: `git rev-parse HEAD`, `git log --format='%H %s' -1`, `git rev-parse --abbrev-ref HEAD`, `sha256sum`, `wc -l` and the UTC timestamp
    - the recorded 64-hex digest equals the live `sha256sum .planning/REQUIREMENTS.md` value at the time of verification, and the recorded line count equals the live `wc -l` value
    - a line labelled `PHASE_BASE_SHA` carries a 40-character hex commit id, with the anchor commit's subject quoted beside it, and that subject does not name a `63-` commit scope
    - `## The lines under guard` quotes all three `grep -n 'REL-09'` hits verbatim with their line numbers, and classifies them explicitly into state-bearing and informational-only with a stated reason — no line shape is quoted that the current `.planning/REQUIREMENTS.md` does not contain
    - `.planning/REQUIREMENTS.md` still contains exactly one unchecked REL-09 requirement bullet and exactly one `| REL-09 | Phase 63 | Pending |` Traceability row
    - the file contains a `Re-verification protocol` section, a `Post-close detection and reversion` section, and a `For the operator running phase.complete` section, and the reversion command `git checkout -- .planning/REQUIREMENTS.md` appears at least once
    - `git status --porcelain .planning/REQUIREMENTS.md` is empty — this task read and quoted the file and changed nothing
    - `git status --porcelain typsphinx/` is empty
    - no file named `63-VERIFICATION.md` exists under the phase directory
  </acceptance_criteria>
  <done>The checksum fence around REL-09's checkbox is recorded fresh at phase head with a labelled PHASE_BASE_SHA anchor, its guarded lines are classified against the file's actual current shape rather than a copied one, and all three observation protocols — including the decisive post-`phase.complete` one — are written into the guard file itself.</done>
  <reversibility rating="reversible">A new planning document recording read-only measurements; no product-tree change and no requirement state change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Record SC#5 fence observation 1 of 2 with a positive control on every remote probe, and resolve the milestone-invariant-sweep ownership question in writing</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md</files>
  <precondition>`gh auth status` succeeds and `git ls-remote --heads origin` returns at least one line — both remote probes below are only meaningful if the remote is reachable. If either fails, halt and report; do not record an unreachable-endpoint silence as a clean fence.</precondition>
  <read_first>
    - `.planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md` in full — the file being mirrored, including its "Observation 1 of 2" structure, its four probe subsections with their positive controls, its "Milestone anchor (recorded, not swept)" section, its "The milestone-invariant sweep — resolved in writing, not left as a silent absence" section, and its "Handoff to observation 2" section.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md` § "Code Examples" § "SC#5's tag/publish probe pair, with positive controls" — the exact command shapes, and its explicit warning against a bare filtered remote query.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md` § "Anti-Patterns to Avoid" (the first two bullets) and § "Open Questions" item 2.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md` § "`63-SC5-INVARIANTS.md`" — the section skeleton, the positive-control pattern, and the warning that the widened diff's expected file list for this phase is five files, not Phase 61's single file.
    - `.planning/ROADMAP.md` § "Phase Details → Phase 63" SC#5, and § "🚧 v0.9.2" milestone constraint 8.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<specifics>` items 5 and 6, and § "Claude's Discretion" bullets 3 and 4.
  </read_first>
  <action>
    Create `.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md`, mirroring
    `61-SC4-INVARIANTS.md`'s structure with the SC number resolved to SC#5 and the version token
    resolved to 0.9.2. Every value is measured now, in this worktree; nothing is copied forward.

    Open with `## Observation 1 of 2` and record its UTC timestamp. Then the four probes, each as a
    labelled subsection holding the command and its literal output:

    **Local tag probe.** `git tag -l 'v0.9.2'`, expected empty. Pair it with the same command shape
    against a tag known to exist, `git tag -l 'v0.9.0'`, as the positive control — an empty result
    from the first is only a finding if the second is non-empty.

    **Remote tag probe, unfiltered with positive control.** Fetch the tag list ONCE with
    `git ls-remote --tags origin` redirected to a file, then derive both counts from that single
    fetch: a count of references matching the 0.9.0 tag as the POSITIVE CONTROL (expected at least
    1, because that tag is known to exist) and a count matching the 0.9.2 tag as the NEGATIVE
    ASSERTION (expected 0). Never issue a bare filtered `git ls-remote --tags origin 'v0.9.2'` as
    the sole probe: its silence is indistinguishable from a network failure, which is precisely the
    failure this two-count-from-one-fetch shape exists to eliminate.

    **Publish probe.** `gh release list --limit 20` redirected to a file, then two counts from that
    one fetch: a count of the `Latest` marker as the POSITIVE CONTROL (expected 1 — some release IS
    marked latest) and a count of the 0.9.2 version string as the NEGATIVE ASSERTION (expected 0).
    Additionally run `gh release view v0.9.2` and record its literal not-found message and non-zero
    exit.

    **Release-workflow probe.** `gh run list --workflow=release.yml --limit 5` and record the output
    literally, stating that no run corresponds to a 0.9.2 tag push. This is a READ-ONLY listing;
    under no circumstance is that workflow dispatched, by tag push or by `workflow_dispatch`.

    Close the observation with an `### Observation 1 verdict` paragraph stating what the four probes
    together establish and naming the wave this observation was taken in, so the waves-separation
    SC#5 requires is legible from the file itself.

    **`## Milestone anchor (recorded, not swept)`.** Record the anchor's identity and reachability:
    `git rev-parse v0.9.0`, `git rev-list --count v0.9.0..HEAD`, and `git diff --stat v0.9.0..HEAD --
    typsphinx/` reduced to its summary tail. These establish that the milestone anchor is real and
    non-trivially distant, which is the precondition for every scoped diff in this phase being a
    finding rather than an artifact.

    **`## The milestone-invariant sweep — resolved in writing`.** `61-SC4-INVARIANTS.md` explicitly
    deferred to this phase the question of which plan runs the sweep backing the release entry's
    dependency and `@preview` claims. Resolve it here in writing: the sweep is owned by plan 63-01
    Task 2, is run as four targeted measurements against the `v0.9.0` anchor with a widened-scope
    positive control, and is recorded in `63-CHANGELOG-EVIDENCE.md` under its own
    milestone-invariant-sweep heading. State plainly that **this plan does not re-run the sweep and
    does not assert its result**, because 63-01 executes in the same wave in a separate worktree and
    its artifact is not readable from here — recording an unmeasured result would be exactly the
    inherited-evidence failure this phase's prohibitions forbid.

    **`## Handoff to observation 2`.** State that observation 2 belongs to plan 63-04 in wave 3, that
    SC#5 requires the two observations to be separated by intervening waves rather than by wall-clock
    luck, and that observation 2 additionally carries the scoped `typsphinx/` diff against
    PHASE_BASE_SHA paired with a same-anchor widened diff as its positive control — whose expected
    non-empty result for this phase is the five files the bump commit and the tuple edit touch, not
    Phase 61's single-file result.
  </action>
  <verify>
    <automated>test -f .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md &amp;&amp; [ -z "$(git tag -l 'v0.9.2')" ] &amp;&amp; [ -n "$(git tag -l 'v0.9.0')" ] &amp;&amp; git ls-remote --tags origin &gt; /tmp/63-tags.txt &amp;&amp; [ "$(grep -c 'refs/tags/v0\.9\.0' /tmp/63-tags.txt)" -ge 1 ] &amp;&amp; [ "$(grep -c 'refs/tags/v0\.9\.2' /tmp/63-tags.txt)" = 0 ] &amp;&amp; gh release list --limit 20 &gt; /tmp/63-releases.txt &amp;&amp; [ "$(grep -c 'Latest' /tmp/63-releases.txt)" -ge 1 ] &amp;&amp; [ "$(grep -c 'v0\.9\.2' /tmp/63-releases.txt)" = 0 ] &amp;&amp; [ "$(grep -c 'Observation 1 of 2' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md)" -ge 1 ] &amp;&amp; [ "$(grep -ci 'positive control' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md)" -ge 3 ] &amp;&amp; [ "$(grep -c 'Handoff to observation 2' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md)" -ge 1 ] &amp;&amp; [ "$(grep -ci 'milestone-invariant sweep' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md)" -ge 1 ] &amp;&amp; [ "$(grep -cE '20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md)" -ge 1 ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ .planning/REQUIREMENTS.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `63-SC5-INVARIANTS.md` exists, opens with an `## Observation 1 of 2` heading, and carries at least one ISO-8601 Zulu timestamp
    - the local tag probe records `git tag -l 'v0.9.2'` as empty AND its positive control `git tag -l 'v0.9.0'` as non-empty, both as commands with literal output
    - the remote tag probe derives two counts from ONE unfiltered `git ls-remote --tags origin` fetch: at least 1 reference matching the 0.9.0 tag (positive control) and exactly 0 matching the 0.9.2 tag (negative assertion). No bare filtered remote query appears as a sole probe anywhere in the file
    - the publish probe derives two counts from ONE `gh release list --limit 20` fetch: at least 1 `Latest` marker (positive control) and exactly 0 rows matching the 0.9.2 version; `gh release view v0.9.2` is additionally recorded with its literal not-found message and non-zero exit
    - the release-workflow probe records `gh run list --workflow=release.yml --limit 5` output literally, and no command anywhere in this plan dispatches that workflow
    - the phrase `positive control` appears at least three times, once per remote-reachable probe
    - `## Milestone anchor (recorded, not swept)` records `git rev-parse v0.9.0`, a non-zero `git rev-list --count v0.9.0..HEAD`, and the `git diff --stat v0.9.0..HEAD -- typsphinx/` summary tail
    - the milestone-invariant-sweep section names plan 63-01 Task 2 as the owner, names `63-CHANGELOG-EVIDENCE.md` as where the result lands, and states explicitly that this plan neither re-runs the sweep nor asserts its result
    - `## Handoff to observation 2` names plan 63-04 and wave 3, and states the widened-diff positive control's expected five-file result rather than Phase 61's single-file result
    - `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md` is empty
  </acceptance_criteria>
  <done>The first of SC#5's two waves-separated fence observations is recorded with four probes, each paired with a control that would fail loudly if the probe never reached its source, and the sweep-ownership question Phase 61 deferred here is answered in writing rather than left as a silent absence.</done>
  <reversibility rating="reversible">Read-only probes and a new planning document; nothing is written to any remote and no workflow is dispatched.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Write the external-API coverage declaration for this phase's gh-dense and release-pipeline-dense plan bodies</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md</files>
  <read_first>
    - `.planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md` in full — the analog being followed, including its verbatim detector-result block, its "What this phase's `gh` usage actually is" section, and its "Why this file exists" reasoning about the seal-time re-run.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<domain>` — the phase boundary this declaration describes.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md` § "Package Legitimacy Audit" — the recorded fact that this phase installs, adds or upgrades zero packages.
  </read_first>
  <action>
    Create `.planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md`, following
    `61-COVERAGE.md`'s shape with this phase's own facts.

    State plainly that there is no external API integration: this phase edits four product-tree files
    and one test file, runs the project's own test, lint, type and documentation tooling, dispatches
    the project's own CI workflow once, and writes planning documents. No third-party API client,
    SDK, HTTP integration or webhook is introduced, and no `typsphinx/` source file is touched.

    Record the plan-time detector result verbatim, in a fenced block, exactly as it was returned over
    this phase's scope before any plan existed: `{"detected": false, "signals": []}`. State that the
    result was produced by the deterministic detector run by the plan-phase orchestrator over the
    phase scope, and that it is transcribed here rather than re-derived.

    Add a section explaining what this phase's `gh` usage actually is: read-only probes against this
    project's own GitHub repository (`gh release list`, `gh release view`, `gh run list`,
    `gh run view`, `gh run watch`) plus one `gh workflow run ci.yml --ref <branch>` dispatch of this
    project's own continuous-integration workflow. That is the project's own CI tooling, not a
    third-party service integration. State explicitly that `release.yml` is **named and recorded** in
    this phase's plans and in `63-HANDOFF.md` but is **never triggered**, by tag push or by
    `workflow_dispatch`.

    Add a section explaining why the file exists: the seal-time coverage gate re-runs the same
    detector over the concatenated PLAN bodies rather than over the phase scope document, and this
    phase's plan prose is unavoidably dense with `gh`, workflow, release-pipeline and PyPI vocabulary
    — exactly the false-positive surface a verb-plus-noun detector matches on. Note that Phase 57 hit
    this precise case, where a seal-time re-run matched self-referentially on prose inside its own
    declaration. This file exists so that such a re-run meets a reasoned declaration rather than a
    fabricated capability matrix, which for an API this phase does not touch would be worse than none.

    Record, in one line, that `63-RESEARCH.md` § "Package Legitimacy Audit" measured this phase as
    installing, adding or upgrading zero packages, so there is no package-legitimacy checkpoint owed
    anywhere in the phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md &amp;&amp; [ "$(wc -l &lt; .planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md)" -ge 20 ] &amp;&amp; [ "$(grep -c '"detected"' .planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'release.yml' .planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md)" -ge 1 ] &amp;&amp; [ "$(grep -ci 'never triggered' .planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md)" -ge 1 ] &amp;&amp; [ "$(grep -ci 'zero packages' .planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md)" -ge 1 ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ .planning/REQUIREMENTS.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `COVERAGE.md` exists, is at least 20 lines, and contains no fabricated capability matrix for any external API
    - it records the plan-time detector result verbatim in a fenced block, including the `"detected"` key with a false value and an empty signals list
    - it names `release.yml` and states that the workflow is recorded but never triggered, by tag push or by `workflow_dispatch`
    - it distinguishes this project's own CI tooling (`gh workflow run ci.yml`, `gh run view`, `gh release list`) from a third-party service integration, in prose
    - it states in one line that this phase installs, adds or upgrades zero packages, citing `63-RESEARCH.md` § "Package Legitimacy Audit"
    - `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md` is empty
  </acceptance_criteria>
  <done>A reasoned external-API coverage declaration exists for this phase, recording the plan-time detector result verbatim and pre-empting a seal-time false positive over plan prose that cannot avoid release-pipeline vocabulary.</done>
  <reversibility rating="reversible">A new planning document; no product-tree change and no external effect.</reversibility>
</task>

</tasks>

<flagged_assumptions>
## Spec-less accounting

The phase-wide edge-coverage and prohibition-recall accounting is authored once, in
`63-01-PLAN.md` § `<flagged_assumptions>`. This plan does not repeat it.

Relevant here: the single **unresolved** row is REL-11's, classified `unclassified` by the
deterministic probe. It is surfaced as a flagged assumption rather than authored into
`must_haves.truths` and rather than being auto-resolved with a backstop marker — REL-11 is a process
guard over a checkbox in a planning document, not a behavioural requirement with an input domain, so
there is no adjacency, emptiness, ordering or encoding edge to author for it that would not be
manufactured. Its actual verification is this plan's Task 1 (baseline) and Task 2 (observation 1),
plan 63-04's close-time re-verification, and the operator's post-`phase.complete` third observation
recorded inside `63-CLOSEOUT-GUARD.md`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/REQUIREMENTS.md` → `phase.complete`-family tooling → the milestone-close reader and `/gsd-complete-milestone` | project-state metadata crosses from a file this plan only reads into automated tooling that has historically mutated it against an explicit decision |
| this worktree → `origin` and the GitHub API (`git ls-remote`, `gh release list`, `gh run list`) | read-only probes cross a network boundary whose failure mode is silence, which is indistinguishable from a clean result without a control |
| `63-CLOSEOUT-GUARD.md`'s PHASE_BASE_SHA → plans 63-03 and 63-04's diff scoping | one recorded value becomes the anchor two later plans read every diff against; a wrong anchor makes their empty results vacuous |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-07 | Tampering | REL-09's checkbox silently flipped by `phase.complete`-family tooling, misrepresenting the release's true state to a future reader or to `/gsd-complete-milestone` | high | mitigate | The three-observation SHA-256 fence, with the third observation deliberately placed AFTER the tooling has run — the moment the flip has landed at five consecutive prior closes. The guard file carries the operator-facing protocol so the decisive observation is reachable outside any plan, and the response is written as revert-and-report, never accept-and-commit. |
| T-63-08 | Repudiation | a vacuous fence proof — an unreachable network endpoint producing the same empty output as a genuinely clean remote, leaving the no-tag / no-publish claim unfalsifiable | high | mitigate | Every remote probe is unfiltered-then-counted, with a positive control derived from the SAME fetch: a `v0.9.0` tag reference count and a `Latest` release-marker count, each of which would fail loudly if the probe never reached its source. A precondition additionally halts the task if `gh auth status` or `git ls-remote --heads origin` fails. |
| T-63-09 | Repudiation | a wrong PHASE_BASE_SHA — this plan's own tip recorded instead of the phase's base — making plans 63-03 and 63-04's scoped diffs empty for the wrong reason | medium | mitigate | The anchor is captured as the first command of Task 1, before any file is created or committed; a precondition halts if this worktree already carries a Phase 63 commit; the anchor commit's subject is quoted beside the SHA; and plan 63-04 pairs its scoped diff with a same-anchor widened diff whose expected non-empty result would expose a bad anchor. |
| T-63-10 | Elevation of Privilege | a probe escalating into an action — `gh run list --workflow=release.yml` becoming `gh workflow run release.yml` while looking for a job's result | high | mitigate | The prohibitions block forbids triggering that workflow by any route; the release-workflow probe is specified as a read-only listing; and no command in any task of this plan writes to a remote. Plan 63-03's CI dispatch names `ci.yml` explicitly and reads its lint verdict from `ci.yml`'s own job, removing the incentive to look inside the release workflow at all. |
| T-63-11 | Tampering | a guard file quoting a line shape the current `.planning/REQUIREMENTS.md` does not contain, so the close-time comparison compares nothing | medium | mitigate | Pitfall 6 is resolved explicitly in Task 1: the grep is re-run fresh, all three hits are quoted verbatim with line numbers, and the state-bearing / informational-only classification is stated with its reason rather than copied from Phase 61's differently-shaped file. |
| T-63-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. `63-RESEARCH.md` § "Package Legitimacy Audit" records the phase as installing, adding or upgrading zero packages; this plan touches no dependency declaration at all. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The phase-wide list is authored once, in `63-01-PLAN.md` § `<artifacts_this_phase_produces>`. This
plan contributes three of its entries:

- `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md` — created here, re-verified
  and extended by plan 63-04, and read once more by the operator after `phase.complete`
- `.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md` — created here with
  observation 1 of 2 and the milestone anchor; extended by plan 63-04 with observation 2 and the
  scoped/widened `typsphinx/` diff
- `.planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md` — created here

Nothing under `typsphinx/`, and no product-tree file at all, is created or modified by this plan.

**Reserved name — never produced:** no artifact in this phase may be named `63-VERIFICATION.md`.
That name is owned by `gsd-verifier` and is overwritten wholesale on phase close (D-19).
</artifacts_this_phase_produces>

<verification>
- `63-CLOSEOUT-GUARD.md` carries a freshly-measured SHA-256, line count, UTC timestamp and labelled
  PHASE_BASE_SHA, all matching live values at verification time
- the three `REL-09` grep hits are quoted verbatim with line numbers and classified into
  state-bearing and informational-only with a stated reason
- the guard file carries the re-verification protocol, the post-close reversion protocol and the
  operator-facing third-observation protocol, with the reversion command spelled out
- `.planning/REQUIREMENTS.md` is unmodified — `git status --porcelain` over it is empty, REL-09's
  checkbox is still unchecked and its Traceability row still reads Pending
- `63-SC5-INVARIANTS.md` records observation 1 of 2 with four probes and a positive control on each
  remote-reachable one, plus the milestone anchor and the sweep-ownership resolution
- no command in this plan writes to any remote and `release.yml` is never dispatched
- `COVERAGE.md` records the plan-time detector result verbatim with reasoned prose and no fabricated
  capability matrix
- `git status --porcelain typsphinx/` is empty
- no file named `63-VERIFICATION.md` exists under the phase directory
</verification>

<success_criteria>
1. The REL-09 checkbox fence is recorded at phase head with values measured fresh in this worktree,
   a labelled PHASE_BASE_SHA anchor, and all three observation protocols including the decisive
   post-`phase.complete` one — ROADMAP SC#3 and REL-11's phase-head half.
2. The guarded-line classification is made against the file's actual current shape, resolving
   Pitfall 6 in writing rather than inheriting Phase 61's differently-shaped three hits.
3. SC#5 fence observation 1 of 2 is recorded, with every remote probe carrying a positive control
   derived from the same fetch, so an empty result is a genuine finding rather than a network
   silence.
4. The milestone-invariant-sweep ownership question that Phase 61 deferred here is answered in
   writing, naming plan 63-01 Task 2 and its evidence file, without asserting an unmeasured result.
5. `.planning/REQUIREMENTS.md` is read and quoted and never edited, and no irreversible action is
   taken by any command in this plan.
</success_criteria>

<output>
Create `.planning/phases/63-v0-9-2-release-prep-prep-only/63-02-SUMMARY.md` when done.

Its frontmatter MUST declare `requirements-completed: []` for REL-09. REL-09 is cited by this plan
for coverage only; it closes at `/gsd-complete-milestone`, not here. The v0.9.1 audit found three of
Phase 61's four plans wrongly declaring `[REL-09]`, contradicting the correctly-unmet checkbox — that
is the specific defect this instruction exists to prevent. REL-11's own state is reported from the
guard file's recorded observations, never inferred from frontmatter.
</output>
</content>
