---
phase: 63-v0-9-2-release-prep-prep-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - README.md
  - CHANGELOG.md
  - tests/test_changelog_page_gate.py
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md
autonomous: true
requirements: [REL-09, REL-10]
requirements-completed: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC#1: the version moves to 0.9.2 in ONE commit touching all four of pyproject.toml, uv.lock, README.md and CHANGELOG.md together. Line 7 of pyproject.toml is the sole hand-edited version literal; uv.lock is regenerated by ``uv lock`` and never hand-edited (D-17); line 347 of README.md reads the 0.9.2 Status line; ``uv lock --check`` and ``uv sync --extra dev --locked`` both exit 0 against the bumped tree; and the version-sync guard trio (test_extension.py::test_version_matches_pyproject_toml, test_readme_version_sync.py, test_preview_version_sync.py) is green.'
    - 'REL-10 / D-20: the CHANGELOG edit is performed in the load-bearing four-step order — relocate the scratch block under a FRESH, EMPTY ``## [Unreleased]`` placed ABOVE the existing heading FIRST, rename the old heading to the 0.9.2 versioned heading SECOND, roll the tail link block THIRD, run scripts/extract_changelog_section.py FOURTH and read its stdout. The extractor is executed and its stdout transcribed verbatim into 63-CHANGELOG-EVIDENCE.md, never summarised and never reasoned about.'
    - 'D-03 / D-05: the new inline-image separator bullet leads the ``### Fixed`` list and cites IMG-08, IMG-09 and IMG-10 in trailing parentheses; the three promoted bullets follow in the roadmap order PATH-01, then IMG-04..IMG-07, then MSG-02..MSG-05. The new bullet describes the user-visible shape (an image not first in its container refused the file and produced no PDF for any master), never the internal mechanism.'
    - 'D-04: the three pre-existing bullets are promoted VERBATIM, not rewritten. The MSG bullet keeps its explicit statement that a POSIX path containing an apostrophe was affected by the same defect family, so the release is never framed as a Windows-exclusive fix. The only permitted edit is trimming a clause the new lead paragraph makes literally redundant.'
    - 'D-02: the string for the completed-but-never-published version appears ZERO times anywhere in CHANGELOG.md — no versioned heading for it, no tail link reference for it, and no mention of it in the lead paragraph or in any bullet.'
    - 'D-06: a ``### Verified`` subsection is written with exactly three bullets, each backed by a run recorded in this phase or in Phase 62. The third bullet states the TEST-05 real-typst.compile() gate result (16 previously-failing plus 9 must-keep-passing image shapes, 18/18 masters compiling); the full-corpus re-run sentence nine prior entries carried is NOT copied, because this milestone did not run that corpus.'
    - 'D-07: the section vocabulary is ``### Fixed`` and ``### Verified`` only — no Added, Changed, Removed or Known Limitations subsection is created inside the new section, and the file-wide count of Known Limitations subsections stays exactly 1 (the pre-existing one inside 0.1.0b1).'
    - 'D-11: RELEASE_VERSIONS in tests/test_changelog_page_gate.py gains the 0.9.2 entry and its preceding comment is updated in the same edit (15 releases through 0.9.0 becomes 16 through 0.9.2). The proof is a run carrying the docs extra with every test reported PASSED and none reported skipped — a dev-extra-only run silently skips both content-coverage classes and proves nothing.'
    - 'D-09: README.md is edited on line 347 only. Its Known Limitations list at line 289 keeps exactly its two original entries and no upgrade banner is added.'
    - 'D-12: no migration guide is written for this release. 0.9.2 breaks nothing and needs no rewrite from anyone, and the one prior patch release with no breaking change (0.6.5) has no guide either — a "nothing to do" guide would be the first of its kind in the file. Enforced mechanically rather than by intent: no plan in this phase touches docs/source/changelog.rst, and every task in this plan gates ``git status --porcelain docs/`` empty.'
    - 'D-10: no GitHub Security Advisory, no PyPI yank of the published 0.9.0, and no README upgrade banner. Each is outward-facing and irreversible and is excluded by the phase fence independent of whether it would be justified; each is recorded as a declined idea in 63-HANDOFF.md rather than silently dropped, and the decision to take any of them belongs at or after /gsd-complete-milestone.'
    - 'REL-09 edge (adjacency): the two placeholder headings that exist transiently during step 1 do not merge or collide. After the edit exactly ONE unreleased-placeholder heading remains, it is the FIRST ``^## [`` heading in the file, the second ``^## [`` heading is the new 0.9.2 versioned heading, and the heading immediately terminating the 0.9.2 section is ``## [0.9.0] - 2026-08-17`` — the positional terminator the extractor stops at. The file-wide ``^## [`` heading count moves from 22 to exactly 23.'
    - 'REL-09 edge (empty): the fresh placeholder heading is deliberately EMPTY of release bullets — the region between it and the 0.9.2 heading carries the relocated scratch block and its five entries and ZERO bold-lead change bullets — while the extractor stdout for 0.9.2 is non-empty (a file of non-zero byte length).'
    - 'REL-09 edge (ordering): when several bullets sit in one ``### Fixed`` list the output order is specified and stable — within the 0.9.2 section the first bold-lead bullet cites IMG-08, and the line numbers of the IMG-08, PATH-01, IMG-04 and MSG-02 citations are strictly ascending in that order.'
    - 'REL-10 edge (empty): an empty or missing extracted body fails LOUDLY rather than passing as nothing to report. scripts/extract_changelog_section.py raises on both a missing and an empty section and exits 1 with the version named on stderr; this plan relies on that behaviour, never suppresses it with a discarded exit code, and asserts a non-empty stdout file explicitly.'
    - 'REL-10 edge (encoding): byte-for-byte identity is defined concretely as ``diff`` exiting 0 with empty output between the extractor stdout and the 0.9.2 section body sliced out of CHANGELOG.md by awk and trimmed of leading and trailing blank lines. It is a comparison over UTF-8 bytes, never a normalised or whitespace-insensitive one. The identical comparison run against the pre-existing 0.6.5 section is the positive control and must also exit 0 — it was measured exit 0 (1299 bytes on both sides) while authoring this plan.'
    - 'D-16: REL-09 is cited by this plan as a coverage ID ONLY. This plan does not close it, does not touch its checkbox, and 63-01-SUMMARY.md frontmatter declares ``requirements-completed: []`` for it.'
  artifacts:
    - pyproject.toml
    - uv.lock
    - README.md
    - CHANGELOG.md
    - tests/test_changelog_page_gate.py
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md
  key_links:
    - 'CHANGELOG.md -> scripts/extract_changelog_section.py -> the future GitHub Release body: the extractor selects purely by POSITION (first ``## [<version>]`` heading, everything up to the next ``## [`` heading of any name), so the physical location of the scratch block relative to the renamed heading decides what a published release body contains. This is the single most likely breakage point in this plan, and it is why the relocation strictly precedes the rename.'
    - 'CHANGELOG.md -> docs/source/changelog.rst (``.. include::`` with ``:parser: myst_parser.sphinx_``) -> tox -e docs-html / docs-pdf -> Read the Docs: every word authored here is a published docs page, so a malformed MyST construct surfaces as a docs-build warning rather than as a silent defect.'
    - 'pyproject.toml:7 -> uv lock -> uv.lock self-package stanza -> every ``uv sync --extra dev --locked`` step in ci.yml and release.yml: a bump that omits the lockfile regeneration fails all twelve CI lanes and both release.yml install steps before any test runs. This is the shape currently killing every dependabot PR.'
    - 'tests/test_changelog_page_gate.py RELEASE_VERSIONS -> a real ``-b html`` build of docs/source -> the rendered changelog.html and the compiled changelog PDF: the tuple is the assertion set both content-coverage classes iterate, and both classes are gated on importing myst_parser, which lives in the docs extra only.'
  prohibitions:
    - 'MUST NOT perform any irreversible action in this phase — no git tag (local or remote), no tag push, no PyPI upload, no GitHub Release creation, no pull request, no update-pin.yml dispatch on typsphinx-doc-translations, no Read the Docs re-flip, and no trigger of release.yml by any route (tag push or workflow_dispatch). Each of those executes at /gsd-complete-milestone and is RECORDED in 63-HANDOFF.md, never performed.'
    - 'MUST NOT touch anything under typsphinx/. SC#5 git diff over the phase proves that path empty, and any edit there is a scope escape dressed as release prep.'
    - 'MUST NOT modify .planning/REQUIREMENTS.md REL-09 checkbox, its Traceability row, or record REL-09 as satisfied anywhere. If tooling flips it, the flip is reverted with ``git checkout -- .planning/REQUIREMENTS.md`` and reported — never accepted, never committed. No SUMMARY.md in this phase declares REL-09 in requirements-completed.'
    - 'MUST NOT publish a version reference the project cannot resolve. No versioned heading and no tail link reference is created for the version that was completed but never published, and the lead paragraph never names it — CHANGELOG.md is included wholesale into the published docs, so a reference here becomes a reference readers hit.'
    - 'MUST NOT frame this release as Windows-exclusive, and MUST NOT write a ``### Verified`` bullet asserting a claim this milestone did not measure. Specifically, the full-corpus typstpdf re-run sentence carried by nine prior entries is not copied, because this phase does not run that corpus.'
    - 'MUST NOT record inherited, paraphrased, recalled or fabricated evidence as this phase own. No recalled warning count substitutes for an executed build, no skipped test is recorded as a pass, and no probe is recorded without the positive control that proves it reached its source.'
---

<objective>
Bump the tree to 0.9.2 across `pyproject.toml`, `uv.lock` and `README.md` in one commit that also
carries the CHANGELOG's structural promotion, curate the accumulated `## [Unreleased]` content into a
single versioned `## [0.9.2]` section covering both the Windows-path work and this milestone's
inline-image blocker fix, extend `RELEASE_VERSIONS`, and prove the result by **running**
`scripts/extract_changelog_section.py` and reading its stdout rather than reasoning that the edit was
correct.

Purpose: this plan owns SC#1 and SC#2 in full, and is the only plan in the phase that writes to the
product tree. The extracted body it produces is byte-for-byte what
`/gsd-complete-milestone` will publish as the GitHub Release body, so an ordering mistake here is not
a cosmetic defect — it is a scratch block in a published release note. `CHANGELOG.md` is also
included wholesale into `docs/source/changelog.rst`, so every word authored here is simultaneously a
release artifact and a published Read the Docs page.

Output: the bumped version literals, the curated `## [0.9.2]` section with its tail link, the
extended `RELEASE_VERSIONS` tuple, and `63-CHANGELOG-EVIDENCE.md` carrying the pre-edit measurements,
the extractor's verbatim stdout, the byte-identity proof with its positive control, the
milestone-invariant sweep, and D-20's three named greps recorded as command plus output.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-VALIDATION.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution";
ROADMAP milestone constraint 12). It is mandatory and not conditional on parallelism. Before running
ANY command in this plan, provision this worktree's own virtual environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Without this, the build imports the UNCHANGED
main-tree package and every measurement in this plan is taken against the wrong tree.

**Task 3 additionally needs the `docs` extra** and invokes it explicitly as
`uv run --extra dev --extra docs pytest …`. That invocation mutates this worktree's `.venv` by
installing `myst_parser`. That is correct and required here — but it is also why this plan never
reports a full-suite count: after Task 3 the four `myst_parser`-gated skips would run, and the
resulting count would not be comparable to the phase's 1543-passed / 5-skipped baseline. The
full-suite measurement belongs to plan 63-03, which deliberately provisions `--extra dev` only.
</worktree_provisioning>

<!-- planner-discipline-allow: 0\.9\.1 -->
<!-- planner-discipline-allow: ^## \[0\.9\.1\] -->
<!-- planner-discipline-allow: ^\[0\.9\.1\]: -->
<!-- planner-discipline-allow: Planned for Future Releases -->
<!-- planner-discipline-allow: SKIPPED -->
<!-- planner-discipline-allow: ^## \[ -->
<!-- planner-discipline-allow: ^## \[Unreleased\] -->
<!-- planner-discipline-allow: ^\[[^]]\+\]: https -->
<!-- planner-discipline-allow: ^### Known Limitations -->
<!-- planner-discipline-allow: ^### Verified -->
<!-- planner-discipline-allow: IMG-08 -->
<!-- planner-discipline-allow: TEST-05 -->
<!-- planner-discipline-allow: @preview -->
<!-- planner-discipline-allow: full-corpus -->
<!-- planner-discipline-allow: Milestone-invariant sweep -->
<!-- planner-discipline-allow: Byte-for-byte identity -->
<!-- Every literal from the second group is a POSITIVE presence or equality check
     (heading counts of 23 and 10, link-reference count of 23, a Known-Limitations count
     of exactly 1, at-least-one occurrences of the requirement IDs and the evidence-section
     headings), not an absence check, so echoing it into an action body cannot invalidate
     anything. The one genuine absence check in that group is `full-corpus`, whose zero-count
     assertion is scoped to the new CHANGELOG section rather than to this plan file, and whose
     literal the action must name in order to forbid copying it.
     Each literal above is part of a verification command's own grep regex. Two of them
     (the scratch-block heading text, and the version string that must never appear) are also
     content the executor must physically move or physically avoid, so naming them in the action
     body is unavoidable. The greps target CHANGELOG.md and the extractor's stdout, never this
     plan file. -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end release slice — version literal through lockfile, README and the four-step CHANGELOG promotion, to the extractor's own stdout</name>
  <files>pyproject.toml, uv.lock, README.md, CHANGELOG.md, .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md</files>
  <precondition>The `v0.9.0` tag is present and reachable in this worktree (`git rev-parse --verify v0.9.0` exits 0) — it is the anchor every milestone-scoped diff in this phase reads against. Halt and report if it is absent.</precondition>
  <read_first>
    - `CHANGELOG.md` lines 1-46 — the file being modified, in its current state: heading `## [Unreleased]` at line 8, `### Fixed` at line 10 with the three bullets at lines 12-36, `### Planned for Future Releases` at line 38 with five entries, and `## [0.9.0] - 2026-08-17` at line 45 (the terminator the extractor stops at today).
    - `CHANGELOG.md` lines 381-403 — the `## [0.6.5]` section. This is the measured structural template (D-01, CONTEXT specific idea 1): a lead paragraph naming the defect, scoping the runtime change, and stating the zero-new-dependency fact, then `### Fixed`, then `### Verified`, and nothing else. Same defect class (a compile-blocking separator), same release class (patch, no breaking change).
    - `CHANGELOG.md` last 8 lines — the tail link-reference block, ending with the `[Unreleased]` compare link against `v0.9.0...HEAD`, plus the topmost `[0.9.0]:` release-tag line immediately above which the new link is inserted.
    - `scripts/extract_changelog_section.py` — read the module docstring in full. It documents the purely positional algorithm and the two-identically-named-headings gotcha REL-10 turns on. `_SECTION_HEADER_RE` matches EVERY `## [...]` heading with no name filtering, and `extract_section()` raises (exit 1, version named on stderr) on both a missing and an empty section.
    - `pyproject.toml` line 7 and `README.md` line 347 — the two hand-edited literals, currently both reading 0.9.0.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md` §§ "`pyproject.toml:7`", "`uv.lock`", "`README.md:347`", "`CHANGELOG.md` — new `## [0.9.2]` section", "`CHANGELOG.md` — the 4-step structural edit" — the exact diff shapes, the verbatim 0.6.5 template, and the current-live bullets to promote.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md` § "Architecture Patterns" Patterns 1 and 2, and § "Common Pitfalls" Pitfall 2 — the transcript-verified bump sequence and the exact consequence of swapping steps 1 and 2.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-01 through D-05, D-07, D-08, D-09, D-17, D-20, and § `<specifics>` items 1-4.
    - `.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-VERIFICATION.md` — the authority for every factual claim the new bullet makes about the image fix: 18 masters producing PDFs including the image-free `index`, 30 gate tests passing, the fix confined to a 9-line pure insertion in `typsphinx/translator.py`.
    - `.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md` — the recorded RED run against the unfixed tree, including Typst's verbatim refusal text, which is the authority for naming the compile error in the bullet.
    - `.planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md` — the provenance of the three bullets D-04 promotes verbatim, and the evidence-file structure to follow.
    - `docs/source/changelog.rst` lines 1-10 — the `.. include:: ../../CHANGELOG.md` with `:parser: myst_parser.sphinx_` that makes this edit a published docs change.
  </read_first>
  <action>
    This is the phase's tracer slice: one release path carried from the version literal, through the
    lockfile and the README, through the whole four-step CHANGELOG promotion, all the way to the
    extractor's own stdout — the far end of the release-body stack — before any expansion work is
    written. It is production-quality, not a draft: everything authored here is final and is not
    rewritten by Tasks 2 or 3.

    **Before touching anything**, create
    `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md` with a
    `# Phase 63 — CHANGELOG and Version-Bump Evidence (SC#1, SC#2, REL-10)` heading and a
    `## This plan's base SHA` section holding `git rev-parse HEAD`, plus a
    `## Pre-edit measurements` section recording each as a command with its output: the count of
    `^## \[` headings (expect 22), the count of `^## \[Unreleased\]` headings (expect 1), the count of
    tail link-reference lines matching `^\[[^]]\+\]: https` (expect 22), the count of
    `^### Known Limitations` subsections (expect 1), the count of `^### Verified` subsections
    (expect 9), the verbatim final line of the file, the verbatim content of `pyproject.toml` line 7,
    the verbatim content of `README.md` line 347, and the `uv.lock` self-package `version` line near
    line 1467.

    **The version-literal lockstep (SC#1, Pattern 1).** Hand-edit `pyproject.toml` line 7 so the
    version assignment reads `0.9.2` — this is the sole hand-edited version literal in the
    repository. Hand-edit `README.md` line 347 so the Status line reads `Stable (v0.9.2) - Production
    ready`; no other `README.md` line is touched, and in particular the `## Known Limitations` list
    around line 289 keeps exactly its two existing entries and gains no upgrade banner (D-09). Then
    run `uv lock` to regenerate the lockfile's self-package stanza — `uv.lock` is NEVER hand-edited
    (D-17); expect an `Updated typsphinx v0.9.0 -> v0.9.2` line. Then run
    `uv sync --extra dev --locked` (expect an uninstall/install pair naming both versions),
    `uv lock --check` (exit 0), and read the version back through the real import path with
    `uv run python -c` importing `typsphinx` and printing `__version__` — it derives from installed
    metadata, not from the pyproject literal, so this step is load-bearing rather than decorative.

    **The four-step CHANGELOG edit — the order is load-bearing, not cosmetic (REL-10, D-20,
    Pattern 2).**

    Step one, FIRST: relocate the `### Planned for Future Releases` scratch block and its five
    entries out from under the existing `## [Unreleased]` heading, and place it under a brand-new,
    otherwise-EMPTY `## [Unreleased]` heading inserted ABOVE the existing one. After this step two
    identically-named headings exist transiently and the new one carries only the relocated block.

    Step two, SECOND: rename the old (now second) `## [Unreleased]` heading to a versioned heading
    naming 0.9.2 with a dash and today's date, following the file's own `## [0.6.5] - 2026-07-29`
    line shape. Renaming before relocating would leave the scratch block physically between the
    renamed heading and `## [0.9.0]`, and the extractor's positional scan would capture it verbatim
    into the published release body — that is the exact failure REL-10 exists to prevent.

    Immediately below the new versioned heading, author the lead paragraph (D-01, D-08). Model it on
    the `## [0.6.5]` lead paragraph read above: name the defect in plain user-facing language, scope
    where the runtime change lives, and state that there are zero new runtime dependencies and that
    the bundled `@preview` version-sync surface is untouched. It must carry, in exactly one sentence,
    the disclosure D-08 requires: a project built with the published 0.9.0 whose document places an
    image anywhere but first in its container produced no PDF for any master, and 0.9.0 users should
    upgrade. It must NEVER name the completed-but-never-published version (D-02) — describe the
    Windows-path work as part of 0.9.2's own content. It must not frame the release as
    Windows-exclusive.

    Then author the new `### Fixed` bullet for the inline-image separator and place it FIRST in the
    list, above the three promoted bullets (D-03, D-05). Follow the house style settled since Phase
    33: a bold lead sentence stating the user-visible change with `(IMG-08, IMG-09, IMG-10)` in
    trailing parentheses inside the bold span, then one to three sentences of prose. Describe the
    user-visible shape, not the mechanism: an image node preceded by any sibling content in the same
    container was emitted adjacent to the preceding code-mode expression, so Typst refused the file
    and the `typstpdf` builder raised an extension error and wrote no PDF for **any** master document
    in the project — including masters containing no image at all. Summarise the container set rather
    than enumerating all sixteen shapes: mid-sentence, in a list item, a table cell, a
    definition-list body, an admonition, a footnote, a field-list body, a section title, or a
    figure's legend. Every factual claim must be traceable to `62-VERIFICATION.md` or
    `62-RED-EVIDENCE.md`; the CHANGELOG must not describe a mechanism the Phase 62 amendments
    falsified.

    The three existing bullets are carried along by the rename and are promoted VERBATIM (D-04) —
    they are not rewritten. The MSG bullet's clause naming a POSIX path containing an apostrophe as
    affected by the same defect family is specifically preserved: it is the measured reason this
    release is not Windows-exclusive. The only permitted edit is trimming a clause the new lead
    paragraph makes literally redundant, and if such a trim is made, record it with its one-line
    reason in the evidence file. The `### Verified` subsection is Task 2's work and is not written
    here; no `### Added`, `### Changed`, `### Removed` or `### Known Limitations` subsection is
    created anywhere in the new section (D-07).

    Step three, THIRD: the tail link block. Insert a release-tag link line for 0.9.2 immediately
    above the current topmost `[0.9.0]:` line, using the same
    `https://github.com/YuSabo90002/typsphinx/releases/tag/` prefix the neighbouring lines use.
    Advance the final `[Unreleased]` compare link's base from `v0.9.0` to `v0.9.2`, keeping it as the
    last line of the file. This link points at a tag that will not exist until
    `/gsd-complete-milestone`; that is expected and is the state every prior release-prep phase ended
    in — `links.yml`'s lychee scan excludes `CHANGELOG.md`.

    Step four, FOURTH: run `uv run python scripts/extract_changelog_section.py 0.9.2` redirected to a
    file, and READ its stdout. Record in the evidence file, under
    `## The extractor, run and read (REL-10, D-20)`: the command, its exit code, the byte length of
    its output, and D-20's three named greps each as a command plus its literal output — that no
    versioned heading exists anywhere in `CHANGELOG.md` for the never-published version, that no tail
    link reference exists for it either, and that the extracted body contains zero occurrences of the
    scratch-block heading text `Planned for Future Releases`. Verbatim transcription of the full
    stdout is Task 3's consolidation work; this step records the run and the three greps.

    Finally, land `pyproject.toml`, `uv.lock`, `README.md` and `CHANGELOG.md` in **one** commit —
    `git show --name-only` on it must list all four together. Do NOT commit `pyproject.toml`
    separately from `CHANGELOG.md`; a partial bump is the exact shape currently killing every
    dependabot PR and would fail at the install step of the release workflow's validate and build
    jobs on the eventual real tag push, before any test runs.
  </action>
  <verify>
    <automated>[ "$(sed -n '7p' pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; [ "$(sed -n '347p' README.md)" = '**Status**: Stable (v0.9.2) - Production ready' ] &amp;&amp; uv lock --check &amp;&amp; uv sync --extra dev --locked &amp;&amp; [ "$(uv run python -c 'import typsphinx; print(typsphinx.__version__)')" = '0.9.2' ] &amp;&amp; [ "$(grep -c '^## \[' CHANGELOG.md)" = 23 ] &amp;&amp; [ "$(grep -c '^## \[Unreleased\]' CHANGELOG.md)" = 1 ] &amp;&amp; [ "$(grep -m1 '^## \[' CHANGELOG.md)" = '## [Unreleased]' ] &amp;&amp; [ "$(grep '^## \[' CHANGELOG.md | sed -n '2p' | cut -d' ' -f1-2)" = '## [0.9.2]' ] &amp;&amp; [ "$(grep '^## \[' CHANGELOG.md | sed -n '3p')" = '## [0.9.0] - 2026-08-17' ] &amp;&amp; [ "$(grep -c '^\[[^]]\+\]: https' CHANGELOG.md)" = 23 ] &amp;&amp; [ "$(grep -c '0\.9\.1' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(grep -c '^## \[0\.9\.1\]' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(grep -c '^\[0\.9\.1\]:' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(grep -c '^\[0\.9\.2\]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0\.9\.2$' CHANGELOG.md)" = 1 ] &amp;&amp; [ "$(tail -1 CHANGELOG.md)" = '[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.2...HEAD' ] &amp;&amp; [ "$(grep -c '^### Known Limitations' CHANGELOG.md)" = 1 ] &amp;&amp; [ "$(awk '/^## \[Unreleased\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | grep -c '^- \*\*')" = 0 ] &amp;&amp; [ "$(awk '/^## \[Unreleased\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | grep -c '^### Planned for Future Releases')" = 1 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | grep -cE '^### (Added|Changed|Removed|Known Limitations)')" = 0 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | grep -m1 '^- \*\*' | grep -c 'IMG-08')" = 1 ] &amp;&amp; uv run python scripts/extract_changelog_section.py 0.9.2 &gt; /tmp/63-extracted.md &amp;&amp; [ -s /tmp/63-extracted.md ] &amp;&amp; [ "$(grep -c 'Planned for Future Releases' /tmp/63-extracted.md)" = 0 ] &amp;&amp; uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q &amp;&amp; test -f .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md &amp;&amp; [ -z "$(git status --porcelain typsphinx/ docs/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '7p' pyproject.toml` is exactly `version = "0.9.2"` and `sed -n '347p' README.md` is exactly `**Status**: Stable (v0.9.2) - Production ready`
    - `uv lock --check` exits 0, `uv sync --extra dev --locked` exits 0, and `uv run python -c 'import typsphinx; print(typsphinx.__version__)'` prints `0.9.2`
    - `git diff` of `README.md` against this task's recorded base SHA touches exactly one line, line 347; the `## Known Limitations` list at line 289 still holds its two original entries
    - `grep -c '^## \[' CHANGELOG.md` returns 23 (was 22) and `grep -c '^## \[Unreleased\]' CHANGELOG.md` returns 1
    - the first `^## \[` heading in the file is `## [Unreleased]`, the second begins `## [0.9.2]`, and the third is exactly `## [0.9.0] - 2026-08-17` — the positional terminator the extractor stops at
    - the region between the placeholder heading and the 0.9.2 heading contains exactly one `^### Planned for Future Releases` line and ZERO `^- \*\*` bold-lead change bullets
    - `grep -c '^\[[^]]\+\]: https' CHANGELOG.md` returns 23 (was 22), `grep -c '^\[0\.9\.2\]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0\.9\.2$' CHANGELOG.md` returns 1, and `tail -1 CHANGELOG.md` is exactly `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.2...HEAD`
    - `grep -c '0\.9\.1' CHANGELOG.md` returns 0 — the never-published version appears nowhere in the file, as a heading, as a link reference, or in prose
    - inside the 0.9.2 section, `grep -cE '^### (Added|Changed|Removed|Known Limitations)'` returns 0, and the file-wide `^### Known Limitations` count is still 1
    - the FIRST `^- \*\*` bullet inside the 0.9.2 section cites `IMG-08`, and the section also cites `IMG-09` and `IMG-10`
    - the 0.9.2 lead paragraph contains exactly one sentence stating that a project built with 0.9.0 produced no PDF for any master when an image was not first in its container, and recommending that 0.9.0 users upgrade
    - `uv run python scripts/extract_changelog_section.py 0.9.2` exits 0, its stdout file is non-empty, and `grep -c 'Planned for Future Releases'` over that stdout returns 0
    - `uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q` reports 0 failed
    - `63-CHANGELOG-EVIDENCE.md` records this task's base SHA as a 40-character hex string plus all nine pre-edit measurements, each as a command with its literal output
    - `63-CHANGELOG-EVIDENCE.md` § "The extractor, run and read (REL-10, D-20)" records the extractor command, its exit code, its output byte length, and D-20's three greps each as a command with its literal output
    - `git status --porcelain typsphinx/ docs/` is empty — the empty `docs/` half is also D-12's mechanical enforcement: no migration guide is added to `docs/source/changelog.rst`, because 0.9.2 breaks nothing and the one prior no-breaking-change patch release has none either
    - the commit produced by this task lists exactly `CHANGELOG.md`, `README.md`, `pyproject.toml` and `uv.lock` under `git show --name-only`
  </acceptance_criteria>
  <done>The version literal, the regenerated lockfile, the README status line and the four-step CHANGELOG promotion land together in one commit, and the extractor — run for real, not reasoned about — produces a non-empty body that terminates at the 0.9.0 heading and carries no scratch-block content.</done>
  <reversibility rating="reversible">Local file edits and one local commit; no tag, no push, no published artifact. Reverting is a `git revert` of a single commit plus `uv lock`.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the milestone-invariant sweep against the v0.9.0 anchor and author the `### Verified` subsection from what it measured</name>
  <files>CHANGELOG.md, .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - `CHANGELOG.md` — the current state after Task 1, including the new versioned section and the bullet Task 1 authored, which this task must not rewrite.
    - `CHANGELOG.md` lines 381-403 (the `## [0.6.5]` section) and the `### Verified` subsection of the `## [0.9.0]` section — the two measured templates for the three-bullet shape, including the exact wording of the `@preview` bullet that this task reuses verbatim.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-06 in full, including its **explicit warning to the planner** about the third bullet, and § "Claude's Discretion" bullet 4 (targeted greps versus a full sweep, and the requirement that the sweep's positive control be real).
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md` § "Open Questions" item 2 — why the sweep is re-measured fresh against this milestone's own anchor and never copied forward.
    - `.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-VERIFICATION.md` lines 25-80 — the authority for the third bullet: 18 masters producing PDFs, 30 gate tests passing, the real `typst.compile()` basis of the gate.
    - `.planning/REQUIREMENTS.md` § "TEST — regression gate" (TEST-05) and § "IMG — inline image separator" (IMG-08) — the 16-failing / 9-must-keep-passing shape counts the third bullet states.
    - `.planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md` § "The milestone-invariant sweep — resolved in writing, not left as a silent absence" — the section this phase inherits the question from, and the shape of resolving it in writing.
  </read_first>
  <action>
    Run the milestone-invariant sweep against this milestone's own anchor, the `v0.9.0` tag, and
    author the `### Verified` subsection from what it actually measured. No bullet may be carried on
    prose.

    **The sweep, four targeted measurements** (Claude's Discretion bullet 4 permits targeted greps
    rather than a full sweep; each is recorded as a command with its literal output in a
    `## Milestone-invariant sweep (D-06 bullets 1 and 2)` section of the evidence file):

    First, the dependency arrays. Diff `pyproject.toml` across `v0.9.0..HEAD`. After Task 1's bump
    this diff must contain exactly one removed line and one added line, both the `version`
    assignment on line 7 — nothing inside `[project.dependencies]` or
    `[project.optional-dependencies]`. Measured while this plan was written, the same diff at the
    pre-bump tree was completely empty, so a single version line-pair is the expected post-bump
    result.

    Second, the lockfile. Diff `uv.lock` across the same range. The only change must be the
    self-package `version` stanza near line 1467; no dependency pin moves, because `uv lock` without
    an upgrade flag preserves existing resolutions. Measured pre-bump, this diff was also empty.

    Third, the four bundled `@preview` package version strings. Diff
    `typsphinx/writer.py`, `typsphinx/template_engine.py`, `typsphinx/templates/base.typ` and
    `examples/` across the same range and count the diff lines mentioning `@preview` — expect zero.
    Measured while this plan was written: zero. Record the four current version strings verbatim
    alongside, read out of `typsphinx/templates/base.typ`.

    Fourth, the positive control, which is what makes the three empty results a finding rather than
    an artifact of an unreachable anchor: run the same-anchor diff with a WIDENED scope over
    `typsphinx/` and record its non-empty `--stat` tail plus `git rev-list --count v0.9.0..HEAD`.
    Measured while this plan was written: 5 files, 408 insertions, 58 deletions, 217 commits. If the
    widened diff comes back empty the anchor is wrong or unreachable — halt and report rather than
    recording three vacuous zeros as proof.

    **Author the `### Verified` subsection** immediately after the `### Fixed` list inside the 0.9.2
    section, with exactly three bullets (D-06):

    Bullet one states zero new runtime **and** dev dependencies across this milestone's diff, backed
    by the first two sweep measurements.

    Bullet two reuses, verbatim, the wording the 0.6.5 and 0.9.0 entries already carry: the four
    bundled `@preview` package version strings unchanged across all four sync surfaces, naming
    `writer.py`, `template_engine.py`, `templates/base.typ` and `examples/**/*.typ`. Backed by the
    third sweep measurement.

    Bullet three states the TEST-05 gate result: the 16 previously-failing and 9 must-keep-passing
    image shapes bound by a real `typst.compile()` gate, with 18 of 18 master documents compiling.
    **Do not copy the sentence nine prior entries used about a full-corpus Sphinx `doc/` typstpdf
    re-run remaining fatal-free** — this milestone did not run that corpus, and D-06 forbids
    substituting an unmeasured claim for a measured one. This is the deliberate break in that
    streak, and the evidence file records it as such in one line.

    Add nothing else to the section: `### Fixed` and `### Verified` are the whole vocabulary (D-07).
    Record in the evidence file, in one line each, which evidence file and section backs each of the
    three bullets, so a reader can re-run rather than trust.
  </action>
  <verify>
    <automated>[ "$(git rev-list --count v0.9.0..HEAD)" -gt 0 ] &amp;&amp; [ -n "$(git diff --stat v0.9.0..HEAD -- typsphinx/)" ] &amp;&amp; [ "$(git diff v0.9.0..HEAD -- pyproject.toml | grep -cE '^\+[^+]')" = 1 ] &amp;&amp; [ "$(git diff v0.9.0..HEAD -- pyproject.toml | grep -cE '^-[^-]')" = 1 ] &amp;&amp; [ "$(git diff v0.9.0..HEAD -- pyproject.toml | grep -cE '^\+version = ')" = 1 ] &amp;&amp; [ "$(git diff v0.9.0..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ examples/ | grep -c '@preview')" = 0 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | grep -c '^### Verified')" = 1 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/,0' CHANGELOG.md | awk '/^### Verified/{f=1;next} f&amp;&amp;/^#/{exit} f' | grep -c '^- ')" = 3 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | grep -c 'full-corpus')" = 0 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | grep -c 'TEST-05')" -ge 1 ] &amp;&amp; [ "$(grep -c '^### Verified' CHANGELOG.md)" = 10 ] &amp;&amp; [ "$(grep -c '0\.9\.1' CHANGELOG.md)" = 0 ] &amp;&amp; uv run python scripts/extract_changelog_section.py 0.9.2 &gt; /tmp/63-extracted.md &amp;&amp; [ -s /tmp/63-extracted.md ] &amp;&amp; [ "$(grep -c 'Planned for Future Releases' /tmp/63-extracted.md)" = 0 ] &amp;&amp; [ "$(grep -c 'Milestone-invariant sweep' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md)" -ge 1 ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ docs/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --count v0.9.0..HEAD` is greater than 0 and `git diff --stat v0.9.0..HEAD -- typsphinx/` is NON-empty — the positive control proving the anchor is real and reachable, recorded verbatim in the evidence file
    - `git diff v0.9.0..HEAD -- pyproject.toml` contains exactly one added and one removed source line, and the added line is a `version = ` assignment — nothing inside either dependency array
    - `git diff v0.9.0..HEAD -- uv.lock` changes only the self-package `version` stanza; the evidence file records the diff verbatim
    - `git diff v0.9.0..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ examples/ | grep -c '@preview'` returns 0, and the evidence file records the four current version strings read out of `typsphinx/templates/base.typ`
    - the 0.9.2 section contains exactly one `^### Verified` line and exactly three `^- ` bullets beneath it
    - the file-wide `^### Verified` count is 10 (was 9)
    - the 0.9.2 section contains zero occurrences of `full-corpus` — the sentence nine prior entries carried is not copied — and at least one occurrence of `TEST-05`
    - the third `### Verified` bullet states 18 of 18 masters compiling and the 16-failing / 9-must-keep-passing shape counts, both traceable to `62-VERIFICATION.md`
    - `grep -c '0\.9\.1' CHANGELOG.md` still returns 0
    - `63-CHANGELOG-EVIDENCE.md` contains a `## Milestone-invariant sweep (D-06 bullets 1 and 2)` section recording all four measurements as commands with literal output, and one line naming the evidence file and section backing each of the three `### Verified` bullets
    - the extractor still exits 0 with non-empty stdout carrying zero `Planned for Future Releases` occurrences
    - `git status --porcelain typsphinx/ docs/` is empty
  </acceptance_criteria>
  <done>The three `### Verified` bullets are authored from four freshly-executed measurements against the `v0.9.0` anchor, each with a named evidence source, and the sweep's positive control proves the anchor was reachable so the three empty results are genuine findings.</done>
  <reversibility rating="reversible">Markdown additions inside a section this plan created in the same wave; no version literal, no tag, no published artifact moves.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Extend `RELEASE_VERSIONS`, prove it in an environment that actually runs the coverage classes, and consolidate the byte-identity evidence</name>
  <files>tests/test_changelog_page_gate.py, .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - `tests/test_changelog_page_gate.py` lines 1-70 — the file being modified: the module docstring stating both content classes drive a real Sphinx build and are `@pytest.mark.slow`, the `myst_parser` import guard, the `RELEASE_VERSIONS` tuple currently ending at the 0.9.0 entry, and its preceding comment currently reading "The 15 releases the published page was frozen without (0.4.4 through 0.9.0, inclusive)".
    - `tests/test_changelog_page_gate.py` lines 140-260 — the two content-coverage classes, their `@pytest.mark.skipif` guards on `MYST_PARSER_AVAILABLE`, `test_rendered_page_carries_every_release`, `test_build_emits_no_changelog_warnings`, and the PDF class's `pypdf` text extraction.
    - `pyproject.toml` lines 40-60 — the `dev` and `docs` optional-dependency groups, confirming `myst-parser` lives in `docs` only. This is why a `--extra dev`-only run proves nothing here.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md` § "Architecture Patterns" Pattern 3 and § "Common Pitfalls" Pitfall 3 — the exact invocation shape and the failure mode it closes.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md` § "`tests/test_changelog_page_gate.py:50-66`" — the measured diff shape from commit `dcee0201`, the identical precedents at `0c784c48` and `075c07d0`, and the comment-count update the same edit must carry.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-11 and D-20.
    - `CHANGELOG.md` — the final state after Tasks 1 and 2, for the byte-identity comparison.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md` — this file's current state, to append to rather than overwrite.
  </read_first>
  <action>
    Extend the release-coverage tuple, prove it in an environment that actually runs the classes that
    assert it, and consolidate the evidence file.

    **The tuple edit (D-11).** Append the 0.9.2 entry to `RELEASE_VERSIONS` in
    `tests/test_changelog_page_gate.py`, immediately after the 0.9.0 entry, matching the existing
    quoting and trailing-comma style exactly. In the same edit, update the tuple's preceding comment
    so its count and its upper bound both move: "15 releases … through 0.9.0" becomes "16 releases …
    through 0.9.2". A stale comment beside a changed tuple is its own defect, and every release-prep
    phase since Phase 46 has made this pair of edits together.

    **The proof, in the only environment that runs the assertion (Pitfall 3).** Both
    content-coverage classes are guarded on importing `myst_parser`, which lives in the `docs` extra
    only. This worktree was provisioned with `--extra dev`, so a bare
    `uv run pytest tests/test_changelog_page_gate.py` would report the two classes as skipped, exit
    0, and prove nothing. Run instead
    `uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -v` and assert on the
    reported counts, not on the exit code: the summary line must report a non-zero passed count and
    a **zero** skipped count. Transcribe the verbatim summary line into the evidence file under
    `## RELEASE_VERSIONS proof (D-11), run with the docs extra`, and state in one line which of the
    two permitted routes this evidence took (the local docs-extra run, not the dispatched CI docs
    job). A transcript showing any test as skipped is never recorded as proof of this edit.

    This run performs a real `-b html` build of the whole `docs/source` tree and a real typstpdf
    compile of the changelog, so it is also the first place a malformed MyST construct in the new
    section would surface — `test_build_emits_no_changelog_warnings` asserts on changelog-page
    warnings directly. If it fails on a warning traceable to the new prose, fix the prose and re-run;
    do not adjust the test.

    **Consolidate the evidence (D-20, REL-10).** Append three sections to
    `63-CHANGELOG-EVIDENCE.md`.

    First, `## The extracted body, verbatim`: run the extractor once more against the final tree and
    paste its **complete stdout verbatim** inside a fenced block — D-20's binding requirement is
    transcription, never summary. Record the command, the exit code and the output byte length beside
    it.

    Second, `## Byte-for-byte identity, with its positive control`: prove the extracted body is
    byte-identical to the section as it sits in `CHANGELOG.md`. The comparison method is stated
    concretely rather than left as a word: slice the section body out of `CHANGELOG.md` with `awk`
    from the 0.9.2 heading to the next `^## \[` heading, trim leading and trailing blank lines with
    `sed` and `tac`, and `diff` that against the extractor's stdout — an empty diff and exit 0 is the
    assertion, over UTF-8 bytes and with no whitespace-insensitive comparison anywhere. Then run the
    identical pipeline against the pre-existing 0.6.5 section as the POSITIVE CONTROL, proving the
    comparison method itself is sound and that an empty diff means agreement rather than a broken
    pipeline. Both results are recorded as commands with their literal output. The 0.6.5 control was
    measured exit 0 with 1299 bytes on each side while this plan was written; if it comes back
    non-empty, the pipeline is broken and the 0.9.2 result must not be trusted.

    Third, `## Fence assertions over the final tree`: record each as a command plus its output — the
    `^## \[` heading count (23), the tail link-reference count (23), the `^### Known Limitations`
    count (1), the `^### Verified` count (10), the occurrence count of the never-published version
    string across the whole file (0), the verbatim final line of the file, `pyproject.toml` line 7,
    `README.md` line 347, and the `git show --name-only` file list of the bump commit — which must be
    exactly `CHANGELOG.md`, `README.md`, `pyproject.toml` and `uv.lock`, proving SC#1's
    one-commit-four-files requirement rather than asserting it.

    Also record, in one line, that `63-01-SUMMARY.md`'s frontmatter must declare
    `requirements-completed: []` for REL-09: this plan cites REL-09 for coverage only, closes
    nothing, and does not touch its checkbox (D-16).
  </action>
  <verify>
    <automated>[ "$(grep -c '^    "0\.9\.2",$' tests/test_changelog_page_gate.py)" = 1 ] &amp;&amp; [ "$(sed -n '/^RELEASE_VERSIONS = ($/,/^)$/p' tests/test_changelog_page_gate.py | grep -c '^    "')" = 16 ] &amp;&amp; [ "$(grep -c 'The 16 releases the published page was frozen without' tests/test_changelog_page_gate.py)" = 1 ] &amp;&amp; [ "$(grep -c '0\.4\.4 through 0\.9\.2' tests/test_changelog_page_gate.py)" = 1 ] &amp;&amp; uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -v &gt; /tmp/63-pagegate.txt 2&gt;&amp;1 &amp;&amp; [ "$(grep -c 'SKIPPED' /tmp/63-pagegate.txt)" = 0 ] &amp;&amp; [ "$(grep -c 'PASSED' /tmp/63-pagegate.txt)" -ge 6 ] &amp;&amp; uv run python scripts/extract_changelog_section.py 0.9.2 &gt; /tmp/63-e.md &amp;&amp; awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | sed -e '/./,$!d' | tac | sed -e '/./,$!d' | tac &gt; /tmp/63-s.md &amp;&amp; diff /tmp/63-e.md /tmp/63-s.md &amp;&amp; uv run python scripts/extract_changelog_section.py 0.6.5 &gt; /tmp/63-e65.md &amp;&amp; awk '/^## \[0\.6\.5\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | sed -e '/./,$!d' | tac | sed -e '/./,$!d' | tac &gt; /tmp/63-s65.md &amp;&amp; diff /tmp/63-e65.md /tmp/63-s65.md &amp;&amp; [ "$(git show --name-only --format= "$(git log --format=%H -1 -- pyproject.toml)" | grep -v '^$' | LC_ALL=C sort | tr '\n' ' ')" = 'CHANGELOG.md README.md pyproject.toml uv.lock ' ] &amp;&amp; [ "$(grep -c 'Byte-for-byte identity' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md)" -ge 1 ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ docs/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `RELEASE_VERSIONS` in `tests/test_changelog_page_gate.py` holds exactly 16 quoted entries, the last of which is the 0.9.2 entry, and its preceding comment reads "The 16 releases the published page was frozen without (0.4.4 through 0.9.2, inclusive)"
    - `uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -v` exits 0, its transcript shows at least 6 tests reported PASSED, and reports **zero** tests skipped — a transcript with any skipped test is not accepted as proof of the tuple edit
    - `63-CHANGELOG-EVIDENCE.md` § "RELEASE_VERSIONS proof (D-11), run with the docs extra" holds the verbatim pytest summary line and states in one line that the local docs-extra route was used rather than the dispatched CI docs job
    - `63-CHANGELOG-EVIDENCE.md` § "The extracted body, verbatim" holds the extractor's complete stdout transcribed verbatim, not summarised, alongside the command, exit code and byte length
    - the `diff` between the extractor's 0.9.2 stdout and the awk-sliced, blank-line-trimmed 0.9.2 section body is empty and exits 0
    - the identical pipeline run against the pre-existing 0.6.5 section — the positive control — also produces an empty diff and exit 0, and both results are recorded as commands with their literal output
    - `63-CHANGELOG-EVIDENCE.md` § "Fence assertions over the final tree" records nine assertions as commands with output: heading count 23, link-reference count 23, Known Limitations count 1, Verified count 10, never-published-version occurrence count 0, the verbatim final line, `pyproject.toml` line 7, `README.md` line 347, and the bump commit's file list
    - `git show --name-only --format=` on the most recent commit touching `pyproject.toml`, sorted under `LC_ALL=C`, is exactly `CHANGELOG.md README.md pyproject.toml uv.lock`
    - the evidence file records in one line that `63-01-SUMMARY.md` declares `requirements-completed: []` for REL-09
    - `git status --porcelain typsphinx/ docs/` is empty
  </acceptance_criteria>
  <done>The release-coverage tuple carries the new version and is proven by a run in which the two coverage classes actually executed, and the extracted release body is proven byte-identical to the section on disk by a comparison method whose own soundness is demonstrated by a positive control against an existing section.</done>
  <reversibility rating="reversible">A one-entry tuple append plus evidence-file writes; no published artifact and no irreversible action.</reversibility>
</task>

</tasks>

<flagged_assumptions>
## Spec-less edge-coverage accounting (6 probe-surfaced items, 0 silent drops)

This phase has no `63-SPEC.md`, so both `EDGE_ABSENT` and `PROHIB_ABSENT` hold and the deterministic
fallback probe ran over REL-09, REL-10 and REL-11's literal `REQUIREMENTS.md` text. It returned six
items, all `unresolved` with `verification: null`. This is the **phase-wide** accounting; plans
63-02, 63-03 and 63-04 reference this section rather than repeating it.

| Requirement | Category | Resolution | Verification | Where authored |
|---|---|---|---|---|
| REL-09 | adjacency | resolved | explicit | `must_haves.truths` of this plan — the transient two-placeholder-heading state and the 0.9.0 terminator boundary |
| REL-09 | empty | resolved | explicit | `must_haves.truths` of this plan — the deliberately empty placeholder versus the non-empty extracted body |
| REL-09 | ordering | resolved | explicit | `must_haves.truths` of this plan — D-03's specified, stable `### Fixed` bullet order |
| REL-10 | empty | resolved | explicit | `must_haves.truths` of this plan — the extractor's own loud-failure path, never suppressed |
| REL-10 | encoding | resolved | explicit | `must_haves.truths` of this plan — byte-for-byte named concretely as a `diff` over UTF-8 bytes, with a positive control |
| REL-11 | unclassified | **unresolved** | null | **flagged assumption — surfaced below, not authored into `truths`, not dismissed, not backstopped** |

**Why REL-11's row stays `unresolved`.** The probe classified it `unclassified` and the fallback
protocol is explicit that an `unclassified` row is never auto-resolved with a backstop marker and
never auto-dismissed. REL-11 is not a behavioural requirement with an input domain: it is a process
guard asserting that a checkbox in a planning document does not move, verified by comparing a
SHA-256 across three timestamped observations. There is no adjacency, emptiness, ordering or encoding
edge to author for it that would not be manufactured. It is surfaced here so no downstream reader
mistakes the absence for an oversight, and its actual verification lives in plan 63-02 (baseline and
observation 1) and plan 63-04 (close-time re-verification), with the decisive third observation
recorded in `63-CLOSEOUT-GUARD.md` for the operator running `phase.complete`.

**Accounting:** 6 surfaced = 5 authored into `must_haves.truths` + 1 surfaced as a flagged
assumption + 0 dismissed + 0 dropped.

## Prohibition recall accounting (descriptor-less, never auto-dismissed)

Stage 1 over-produced adversarially against REL-09, REL-10 and REL-11's text. Stage 2 dropped the
routine-engineering candidates — must not leave temp artifacts, must not hand-edit the lockfile
(already mechanically gated by `uv lock --check`), must not break the markdown, must not rewrite a
historical release section (already gated by this plan's structural equality assertions) — as edge-probe
or code-review territory.

**Canon-referral breadcrumb (dropped by rule, ADR-550 D6):** one surviving candidate — "the version
argument must not become an injection or path-traversal vector" — is canon security and is already
addressed at source by `scripts/extract_changelog_section.py`'s own ASVS V5 note (the argument is used
only for string equality against text parsed out of `CHANGELOG.md`; it is never interpolated into a
shell command, never `eval`'d, and never used to build a path). Not minted.

Six bespoke values / safety / honesty / transparency prohibitions survived and are authored
**descriptor-less** (no `check_*` scalar, so each disposes flagged-unverified) across the phase's
four plans:

- **P1 (safety):** no irreversible action — no tag local or remote, no tag push, no PyPI upload, no
  GitHub Release, no PR, no `update-pin.yml` dispatch, no Read the Docs re-flip, no `release.yml`
  trigger by any route. Authored in all four plans; the fence is phase-wide.
- **P2 (honesty):** REL-09 must not be recorded as satisfied — no checkbox flip, no Traceability-row
  change, no `requirements-completed: [REL-09]` in any SUMMARY frontmatter, no prose claiming the
  release happened. A tooling flip is reverted and reported, never committed. Authored in all four
  plans.
- **P3 (accuracy):** the release must not be framed as Windows-exclusive, and no `### Verified`
  bullet may assert a claim this milestone did not measure. Authored in 63-01 and 63-04.
- **P4 (transparency):** no version reference the project cannot resolve — no heading and no tail
  link for the completed-but-never-published version, and no mention of it in published prose.
  Authored in 63-01 and 63-04.
- **P5 (evidence integrity):** no inherited, paraphrased, recalled or fabricated evidence recorded as
  this phase's own — no prior CI run cited as this phase's green, no recalled warning count
  substituted for an executed clean build, no skipped test recorded as a pass, no probe recorded
  without its positive control. Authored in all four plans.
- **P6 (scope integrity):** nothing under `typsphinx/` is touched. Authored in all four plans.

**Accounting:** 6 kept = 6 authored into `must_haves.prohibitions` + 0 dismissed + 0 dropped
(+ 1 canon-referral breadcrumb, dropped by rule).
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` → `scripts/extract_changelog_section.py` → the future GitHub Release body | curated prose crosses from a repository file into a body that `release.yml`'s `create-release` job will publish verbatim at `/gsd-complete-milestone`; the selection is positional, so physical layout decides published content |
| `CHANGELOG.md` → `docs/source/changelog.rst` → Read the Docs `latest` | the same prose crosses into a publicly-reachable documentation page on every docs build |
| `pyproject.toml:7` → `uv lock` → `uv.lock` → every `uv sync --extra dev --locked` step in `ci.yml` and `release.yml` | the version literal crosses from a hand-edited source into the install precondition of twelve CI lanes and both release-workflow install steps |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-01 | Tampering | the scratch block leaking into the published GitHub Release body, because `_SECTION_HEADER_RE` selects by position and never by heading name (ASVS V5, L1 — marginal: the "input" is prose this project authors) | high | mitigate | The four-step edit order relocates the block under a fresh empty placeholder BEFORE the rename; the extractor is then executed for real and its stdout grepped for the scratch-block heading text with an expected count of 0, in Task 1 and again in Tasks 2 and 3 on the final tree. |
| T-63-02 | Tampering | a malformed MyST construct in the new lead paragraph, the new bullet, or the new `### Verified` subsection publishing broken markup to Read the Docs | medium | mitigate | Task 3's docs-extra page-gate run performs a real `-b html` build of the whole `docs/source` tree and a real typstpdf compile, and `test_build_emits_no_changelog_warnings` asserts on changelog-page warnings directly. Plan 63-03 then re-measures both docs environments from a clean build against the 3 / 5 baseline. |
| T-63-03 | Spoofing | publishing a version reference the project can never resolve — a versioned heading or a tail link for the completed-but-never-published version — on a page readers actually reach (`links.yml`'s lychee scan excludes `CHANGELOG.md`, so CI would not catch it) | high | mitigate | Three equality gates in every task: zero occurrences of that version string file-wide, zero versioned headings for it, zero tail link references for it, all asserted after each edit. |
| T-63-04 | Elevation of Privilege | a partial bump — `pyproject.toml` moved without `uv.lock` regenerated — breaking every `uv sync --extra dev --locked` step in CI and failing the release workflow's install step before any test runs (the shape currently killing every dependabot PR) | high | mitigate | `uv lock` is run rather than the lockfile hand-edited; `uv lock --check` and `uv sync --extra dev --locked` must both exit 0; and Task 3 asserts the bump commit's `git show --name-only` list is exactly the four files, so a split commit fails the gate. |
| T-63-05 | Repudiation | a `### Verified` bullet asserting a claim this milestone did not measure — specifically the full-corpus typstpdf sentence nine prior entries carried — leaving the release notes unfalsifiable | medium | mitigate | D-06's third bullet is replaced by the TEST-05 gate result; Task 2 gates on zero occurrences of the inherited sentence's distinctive phrase inside the new section, and each bullet's backing evidence file and section is named in the evidence file. |
| T-63-06 | Repudiation | a skipped test recorded as a pass, so the release-coverage tuple edit is cited as proven by a run in which the asserting classes never executed | medium | mitigate | The proof command carries the `docs` extra explicitly and the gate asserts on reported PASSED and SKIPPED counts rather than on the exit code. |
| T-63-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. `63-RESEARCH.md` § "Package Legitimacy Audit" records the phase as installing, adding or upgrading zero packages; `pyproject.toml`'s dependency arrays are read but never edited, and Task 2 measures that fact against the `v0.9.0` anchor. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol and file below is created new by this phase. This is the phase-wide list; plans 63-02,
63-03 and 63-04 reference this section rather than repeating it. The plan-review-convergence
source-grounding pass reads this section to exclude newly-created symbols from drift verification.

**New content inside existing product-tree files:**

- `CHANGELOG.md` — a new `## [0.9.2]` versioned section heading with a lead paragraph, a `### Fixed`
  subsection (one newly-authored bullet plus the three promoted verbatim), and a `### Verified`
  subsection with three bullets; a new, empty `## [Unreleased]` heading above it carrying the
  relocated `### Planned for Future Releases` block; and a new `[0.9.2]:` release-tag link line in
  the tail block, with the `[Unreleased]` compare base advanced. Created by 63-01.
- `tests/test_changelog_page_gate.py` — a new `"0.9.2"` entry appended to the `RELEASE_VERSIONS`
  tuple, with its preceding comment's count and upper bound updated in the same edit. Created by
  63-01.
- `pyproject.toml`, `uv.lock`, `README.md` — one changed version literal each; no new symbol.

**New files under `.planning/phases/63-v0-9-2-release-prep-prep-only/`:**

- `63-CHANGELOG-EVIDENCE.md` — created by 63-01
- `63-CLOSEOUT-GUARD.md` — created by 63-02, re-verified and extended by 63-04
- `63-SC5-INVARIANTS.md` — created by 63-02 (observation 1), extended by 63-04 (observation 2 and the
  scoped/widened diff)
- `COVERAGE.md` — created by 63-02
- `63-GREEN-TREE-EVIDENCE.md` — created by 63-03
- `63-CI-EVIDENCE.md` — created by 63-03
- `63-HANDOFF.md` — created by 63-04
- `63-01-SUMMARY.md`, `63-02-SUMMARY.md`, `63-03-SUMMARY.md`, `63-04-SUMMARY.md` — one per plan

**Nothing under `typsphinx/` is created or modified by any plan in this phase.**

**Reserved name — never produced:** no artifact in this phase may be named `63-VERIFICATION.md`.
That name is owned by `gsd-verifier` and is overwritten wholesale on phase close (D-19).
</artifacts_this_phase_produces>

<verification>
- `pyproject.toml` line 7, `uv.lock`'s self-package stanza and `README.md` line 347 all read 0.9.2,
  and the four files land in one commit
- `uv lock --check`, `uv sync --extra dev --locked` and the version-sync guard trio are all green
- `CHANGELOG.md` carries exactly one empty `## [Unreleased]` heading first, the new `## [0.9.2]`
  section second, and `## [0.9.0] - 2026-08-17` third; heading count 23, link-reference count 23,
  `### Verified` count 10, `### Known Limitations` count 1
- the never-published version string occurs zero times anywhere in `CHANGELOG.md`
- `scripts/extract_changelog_section.py 0.9.2` exits 0 with non-empty stdout carrying zero
  scratch-block occurrences, transcribed verbatim into `63-CHANGELOG-EVIDENCE.md`
- the extracted body is byte-identical to the section on disk, proven by an empty `diff`, with the
  same pipeline against `## [0.6.5]` as the positive control
- `RELEASE_VERSIONS` holds 16 entries and its coverage classes ran PASSED with zero skipped under the
  `docs` extra
- `git status --porcelain typsphinx/ docs/` is empty
- no file named `63-VERIFICATION.md` exists under the phase directory
</verification>

<success_criteria>
1. The version moves to 0.9.2 in one commit touching `pyproject.toml`, `uv.lock`, `README.md` and
   `CHANGELOG.md` together, with `uv lock --check`, `uv sync --extra dev --locked` and the
   version-sync guard trio all green — ROADMAP SC#1.
2. `CHANGELOG.md` carries a single curated `## [0.9.2]` section covering both the Windows-path work
   and the inline-image blocker fix, in `### Fixed` + `### Verified` vocabulary only, with the
   inline-image bullet leading and the three promoted bullets verbatim behind it, and with the
   never-published version named nowhere.
3. The scratch block was relocated under a fresh empty placeholder BEFORE the rename, and the
   extractor was executed and its stdout read — non-empty, scratch-block-free, and byte-identical to
   the section on disk under a comparison whose soundness is shown by a positive control — ROADMAP
   SC#2 and REL-10.
4. `RELEASE_VERSIONS` gains the new entry with its comment updated, proven by a run in which both
   content-coverage classes actually executed rather than skipped.
5. Nothing under `typsphinx/` is touched, no irreversible action is taken, and REL-09's checkbox is
   neither read as satisfied nor moved.
</success_criteria>

<output>
Create `.planning/phases/63-v0-9-2-release-prep-prep-only/63-01-SUMMARY.md` when done.

Its frontmatter MUST declare `requirements-completed: []` for REL-09. REL-09 is cited by this plan
for coverage only; it closes at `/gsd-complete-milestone`, not here. The v0.9.1 audit found three of
Phase 61's four plans wrongly declaring `[REL-09]`, contradicting the correctly-unmet checkbox — that
is the specific defect this instruction exists to prevent.
</output>
</content>
</invoke>
