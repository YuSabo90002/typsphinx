---
phase: 22.4-readme-status-configuration-options-known-limitations-docs
verified: 2026-07-23T00:30:00+09:00
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 22.4: README 記述の実測乖離解消 — Verification Report

**Phase Goal:** `README.md`（および同種の乖離を持つ `CLAUDE.md` / `pyproject.toml` ruff-ignore コメント）の
全記述が、リポジトリの実測と一致すること。検証機構を持てない数値クレームは削除、Configuration Options は
部分列であることを明示、能力/制限/Status/謝辞は実測から再導出。フルテキストパス（台帳限定ではない）。
ソース挙動変更ゼロ。
**Verified:** 2026-07-23T00:30:00+09:00
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria #1–6)

| # | Truth (SC) | Status | Evidence |
|---|---|---|---|
| 1 | No unverifiable numeric claim survives (`413 tests` x2, `93% overall`, `80%+ code coverage` all gone); machine-guarded numbers (`mitex:0.2.7`, `codly:1.3.0`, Requirements block) byte-unchanged | ✓ VERIFIED | `grep -nE '413 tests\|93% overall\|80%\+ code coverage' README.md` → 0 matches. `grep -nE 'mitex:0\.2\.7\|codly:1\.3\.0' README.md` → 2 matches (:23, :25), unchanged per `git diff`. README:37-39 Requirements block byte-identical (diff shows no touch). |
| 2 | Configuration Options worded as explicit partial list, 6 entries (5 existing + `typst_documents`), links to `docs/source/user_guide/configuration.rst` not the orphan; github.io links (:274-284, D-17) left as-is pending separate todo | ✓ VERIFIED | README.md:201 lead-in: "Below are the main configuration options. This is not the complete set — see [docs/source/user_guide/configuration.rst]...". `grep -c '^- \`typst_' README.md` → 6. `grep -n 'docs/configuration.rst' README.md` → 0 matches. `grep -n 'docs/source/user_guide/configuration.rst'` → 1 match. `grep -c 'yusabo90002.github.io' README.md` → 10 (unchanged count), `grep -c 'yusabo90002.github.io/en/'` → 0 (no `/en/` added, D-17 held). |
| 3 | Capability/status claims match source: citations out of Features, Glossary limitation removed, Status reads `Stable (v0.6.1)` never 0.6.2, Acknowledgments names GSD not Kiro; Bibliography stays listed | ✓ VERIFIED | `grep -n 'and citations' README.md` → 0. `grep -nE 'Glossary generation\|Glossary.*not yet supported' README.md` → 0. `grep -n 'Citation' README.md` → 1 (Known Limitations, :264). `grep -n 'Stable (v0.6.1)' README.md` → 1 (:316); `grep -nE 'v0\.5\.0\|v0\.6\.2' README.md` → 0. `grep -n 'Kiro' README.md` → 0; `grep -n 'GSD' README.md` → 1 (:308). Bibliography line (:263) untouched per diff. |
| 4 | `CLAUDE.md` (4 locations) and `pyproject.toml:122-123` ruff-ignore comment state the measured floor (py312/py313); `UP006`/`UP035` ignores remain in force | ✓ VERIFIED | `grep -n 'py310' CLAUDE.md` → 0; `grep -n 'py312' CLAUDE.md` → 3 matches (:28,:29,:78). `grep -n 'Python 3.10+' CLAUDE.md` → 0; `grep -n 'Python 3.12+' CLAUDE.md` → 1 (:75). `grep -n 'Python 3.10+ support' pyproject.toml` → 0. `grep -nE '"UP006"\|"UP035"' pyproject.toml` → 2 (values unchanged, only trailing comments rewritten — confirmed via `git diff`). |
| 5 | Full-text pass: every remaining README claim checked against codebase; anything found beyond the ledger fixed or filed as pending todo; D-15/D-16 exist as pending todo files | ✓ VERIFIED | See "Full-Text Ledger Walk" section below — all ~55 audited rows dispositioned. D-15 todo: `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` (exists, `file:line` evidence, `## Problem`/`## Solution`). D-16 todo: `.planning/todos/pending/2026-07-22-delete-orphan-docs-configuration-rst.md` (exists, same format). Plus D-17/D-18/D-21 and a linkcheck-CI proposal filed beyond the original ledger (see below) — confirming the pass exceeded ledger-only scope. |
| 6 | Scope fence held: no file under `typsphinx/` modified, no `typst_*` config added/removed, no `docs/` file deleted/re-homed, no github.io→RTD migration, no `CHANGELOG.md` rewrite | ✓ VERIFIED | `git diff --stat d0eab08..HEAD -- typsphinx/ docs/ CHANGELOG.md` → empty. `git diff --name-only d0eab08..HEAD` → only `README.md`, `CLAUDE.md`, `pyproject.toml`, `.planning/todos/pending/*.md` (6 new + 1 append-edited), `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, 3 SUMMARY.md files. `docs/configuration.rst` confirmed still present at 526 lines (not deleted). |

**Score:** 6/6 truths verified (0 present-but-behavior-unverified)

### Full-Text Ledger Walk (SC#5 / DOC-05 — the check that cannot be grepped)

Walked every row of RESEARCH.md's ~55-claim audit table against the current `README.md`:

**Fixed in this phase (11 ledger-covered + off-ledger discoveries):**
- :30 citations dropped from Features — confirmed (D-10)
- :223/:243 test-count numbers removed — confirmed (D-01)
- :246 coverage % removed — confirmed (D-02)
- :299 `80%+ coverage` guideline removed — confirmed (D-04)
- :203-211 Configuration Options reworded + link repointed — confirmed (D-05/D-06/D-12)
- :133-142 Custom Templates example: `papersize`/`fontsize`/`lang` dict deleted — confirmed (D-18, off-ledger discovery #1's README-side half)
- :148-155 Template Parameter Mapping example direction flipped to match implementation — confirmed (D-19, off-ledger discovery #2)
- :265-268 Glossary limitation removed, Citation added — confirmed (D-13)
- :270 dangling "project documentation" reference removed — confirmed (D-20, off-ledger discovery #4)
- :315 Kiro → GSD — confirmed (D-09)
- :323 Status v0.5.0 → v0.6.1 — confirmed (D-11)

**Deliberately unchanged-and-correct (verified via `git diff`, zero touch):** CI/PyPI/License/black/Documentation badges, Requirements block (:37-39), install/build commands, entry-point auto-discovery note, mitex/codly `@preview` versions, rST example block, Multi-Document Projects toctree example, Third-Party Extensions mermaid example, Project Structure tree, Contributing section, License section, Acknowledgments' Sphinx/Typst/mitex/codly/gentle-clues credits, Version History pointer, `examples/basic/`+`examples/advanced/` bullet (D-14), github.io Quick Links 7 URLs (D-17, unchanged pending separate todo).

**Deliberately left as-is, evaluated not silently missed (discovery #6, low severity):** README:20 "Markdown documents" phrasing and README:194 (was :198) "unknown_visit() ... extracts text content" — both RESEARCH-classified as minor/non-misleading, recorded rather than fixed. Confirmed both strings are still present verbatim in current README.md, matching the SUMMARY's disposition.

**Filed as pending todos (off-ledger discoveries, beyond the original 3-item todo and the 14-row discussion ledger):**
- discovery #1/#5 → `2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md` (D-18's source-side half — `typst_elements` non-mapped keys + `typst_toctree_defaults` non-reflection)
- discovery #3 → `2026-07-22-github-io-doc-links-404-missing-en-prefix.md` (D-17)
- discovery #4's docs-link independent finding → `2026-07-22-user-guide-configuration-phantom-config-names.md` (D-21)
- D-15 → `2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md`
- D-16 → `2026-07-22-delete-orphan-docs-configuration-rst.md`
- Bonus (RESEARCH Task F recommendation) → `2026-07-22-add-sphinx-linkcheck-ci-job.md`
- Existing RTD-migration todo append-corrected (its false "links are all measured valid" premise) — confirmed original content intact, correction is a dated addendum section, not a rewrite.

**Independent spot-read beyond the ledger:** Re-read the full current README.md end-to-end (see cat output captured during verification) looking for any claim neither RESEARCH.md nor the SUMMARYs mention. No additional discrepancy found — every remaining factual claim (badges, install commands, feature list, Requirements floor, toctree/mermaid examples, Project Structure tree, Contributing/License/Acknowledgments) matches its source per the ledger's own "一致" (match) disposition, independently re-checked here via the same `grep`/`git diff` commands rather than trusting the ledger's prior conclusion.

**Disposition completeness:** every ledger row is now one of {fixed, unchanged-and-correct, deliberately-left-as-is-recorded, filed-as-todo} — no row was silently dropped.

### Owner Rulings Held (adversarial checks)

| Ruling | Check | Result |
|---|---|---|
| D-17: 7 github.io sub-page links unchanged (404s, but todo-only) | `grep -c 'yusabo90002.github.io' README.md` = 10 (unchanged); `grep -c '/en/' README.md` = 0; `git diff` shows zero touch to :268-278 URLs | ✓ HELD |
| D-18: `papersize`/`fontsize`/`lang` removed from Custom Templates example, but Configuration Options list still names `typst_elements` | `grep -n 'typst_elements' README.md` → 1 match in Configuration Options list (:206), description text unchanged ("Template parameters (paper size, fonts, etc.)"); the code example (:133-136) has the dict deleted | ✓ HELD |
| D-03/D-14: `@preview` version strings and `examples/basic/`+`examples/advanced/` bullet byte-unchanged | `git diff d0eab08..HEAD -- README.md` shows no `-`/`+` lines touching :23, :25, or the Example projects bullet (:242) | ✓ HELD |
| D-08: `UP006`/`UP035` still ignored in `pyproject.toml`; only comments changed | `git diff d0eab08..HEAD -- pyproject.toml` shows only the two trailing `#` comments changed; the `ignore = [...]` array values are untouched (`"UP035",`/`"UP006",` lines unchanged) | ✓ HELD |

### Deferred Work Filed, Not Done

- D-15 (typing modernization): `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` exists, with `file:line` evidence across `pyproject.toml:122-123` and 5 `typsphinx/*.py` import lines, `## Problem`/`## Solution` sections. `typsphinx/**` confirmed untouched (`git diff --stat d0eab08..HEAD -- typsphinx/` empty).
- D-16 (orphan `docs/configuration.rst` deletion): `.planning/todos/pending/2026-07-22-delete-orphan-docs-configuration-rst.md` exists with the 526-line count, wrong-package-name evidence, and toctree-orphan confirmation. `docs/configuration.rst` confirmed still present, 526 lines, `docs/**` confirmed untouched (`git diff --stat d0eab08..HEAD -- docs/` empty).

### Todo Quality

All 6 new pending todo files (`2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md`, `2026-07-22-delete-orphan-docs-configuration-rst.md`, `2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md`, `2026-07-22-user-guide-configuration-phantom-config-names.md`, `2026-07-22-github-io-doc-links-404-missing-en-prefix.md`, `2026-07-22-add-sphinx-linkcheck-ci-job.md`) follow the established frontmatter format (`created`/`title`/`area`/`files` array of `path:line (description)` entries) and body structure (`## Problem`/`## Solution`), each carrying real measured `file:line` evidence (verified by reading all 6 files directly) rather than bare assertions.

The RTD-migration todo (`2026-07-21-move-documentation-hosting-to-read-the-docs.md`) was confirmed appended-to, not rewritten: its original `files:` frontmatter, Problem narrative, and Solution section (including the still-`TBD` decision-pending note, a pre-existing characteristic of this todo predating Phase 22.4) remain byte-present; a dated "2026-07-22 追記" section was added correcting the falsified premise "実測上リンクは全て有効" and cross-referencing the new D-17 todo.

**Observation (not a gap):** the originating todo `2026-07-22-readme-overall-content-review-stale-claims.md` (the one CONTEXT.md's "Folded Todos" section says this phase's body absorbed) is still present under `.planning/todos/pending/` rather than moved to a completed/closed location. This phase's scope and the ROADMAP Success Criteria do not require closing that todo file as part of phase execution — prior phases' pattern of moving closed todos out of `pending/` appears to be an orchestrator/STATE.md-level housekeeping step, not a plan-level deliverable. Recorded here for the orchestrator's awareness, not counted as a gap.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `README.md` | All D-01–D-21 prose fixes applied | ✓ VERIFIED | `git diff d0eab08..HEAD -- README.md` shows exactly the expected 8 hunks, nothing more, nothing less |
| `CLAUDE.md` | 4 Python-version locations corrected | ✓ VERIFIED | `git diff d0eab08..HEAD -- CLAUDE.md` shows exactly 2 hunks (4 line changes) |
| `pyproject.toml` | ruff-ignore comments rewritten, array values untouched | ✓ VERIFIED | `git diff d0eab08..HEAD -- pyproject.toml` shows exactly 1 hunk (2 comment lines) |
| `.planning/todos/pending/*.md` (6 new + 1 appended) | D-15/D-16/D-17/D-18/D-21 + linkcheck proposal filed; RTD todo corrected | ✓ VERIFIED | All 6 new files exist with correct format; RTD todo append confirmed |

### Behavioral Spot-Checks / Regression

| Check | Command | Result | Status |
|---|---|---|---|
| Full test suite | `uv run pytest -q` | `588 passed, 1 skipped` | ✓ PASS (matches expected count; main tree, no worktree-environmental exclusions needed) |
| Preview version-sync gate | `uv run pytest tests/test_preview_version_sync.py -q` | `2 passed` | ✓ PASS |
| Format | `uv run black --check .` | `All done! 159 files would be left unchanged.` (exit 0) | ✓ PASS |
| Types | `uv run mypy typsphinx/` | `Success: no issues found in 6 source files` (exit 0) | ✓ PASS |
| Lint | `uv run ruff check .` (worked directly on main tree, no NixOS binary hazard here) | `All checks passed!` | ✓ PASS |
| Lint (fallback, also run for cross-check) | `nix-shell -p ruff --run "ruff check ."` | `All checks passed!` | ✓ PASS |

No environmental ruff failure occurred in this verification pass (this is the main checkout, not a worktree — the documented NixOS worktree dynamic-linker hazard does not apply here). Both invocation methods agree, so no discrepancy to reconcile.

### Anti-Patterns Found

`grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER" README.md CLAUDE.md pyproject.toml` → 0 matches. No debt markers introduced in any of the three phase-scoped files.

### Honesty Check on Verification Ceiling

Per RESEARCH.md Task F and VALIDATION.md, this phase deliberately added no new automated test (DOC-01's remedy is deletion, not a re-measurable assertion; the one genuinely valuable automation — link reachability — is correctly filed as a separate `sphinx-build -b linkcheck` CI todo rather than shoehorned in here). This is not flagged as missing coverage. Cross-checked all three SUMMARY.md files for overclaiming: none claim automated test coverage beyond grep-assertions and the full-suite regression check; 22.4-01-SUMMARY.md's "Manual-Review Quotes" section and 22.4-VALIDATION.md's "Manual-Only Verifications" table are both honestly labeled as human-judgment items, consistent with what was actually possible to verify mechanically.

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|---|---|---|---|
| DOC-01 | Unverifiable numeric claims removed, machine-guarded numbers kept | ✓ SATISFIED | See SC#1 above |
| DOC-02 | Configuration Options partial-list wording + link repoint | ✓ SATISFIED | See SC#2 above |
| DOC-03 | Capability/limitation/status/methodology alignment | ✓ SATISFIED | See SC#3 above |
| DOC-04 | CLAUDE.md + pyproject.toml Python-version floor correction | ✓ SATISFIED | See SC#4 above |
| DOC-05 | Full-text pass, off-ledger discoveries fixed/filed, D-15/D-16 filed | ✓ SATISFIED | See SC#5 / Full-Text Ledger Walk above |

No orphaned requirements found — REQUIREMENTS.md's "README Accuracy (Phase 22.4)" section lists exactly DOC-01 through DOC-05, and all 3 plans' frontmatter `requirements-completed` fields collectively cover all 5.

### Human Verification Required

None. All 6 ROADMAP Success Criteria, the full-text ledger walk, the owner rulings, the deferred-work filing, todo quality, and the no-regression suite are all verifiable via `grep`/`git diff`/direct file reads/test execution — consistent with VALIDATION.md's own assessment that this phase's Manual-Only items (Configuration Options tone, full-text-pass completeness, GSD-naming factual safety) were already discharged during plan execution and independently re-confirmed here.

### Gaps Summary

None. All 6 Success Criteria verified true against the actual current state of `README.md`, `CLAUDE.md`, and `pyproject.toml`; the full-text pass was independently re-walked (not merely trusted from SUMMARY.md); all 4 adversarial owner-ruling checks held; both deferred items (D-15, D-16) exist as properly-formatted pending todos with real evidence; the scope fence (`typsphinx/`, `docs/`, `CHANGELOG.md`) is confirmed untouched; and the full regression suite (pytest/black/mypy/ruff) passes clean with zero source-behavior change.

---

_Verified: 2026-07-23T00:30:00+09:00_
_Verifier: Claude (gsd-verifier)_
