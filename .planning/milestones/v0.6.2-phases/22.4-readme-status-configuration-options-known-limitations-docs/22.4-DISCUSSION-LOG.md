# Phase 22.4: README 記述の実測乖離解消 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-22
**Phase:** 22.4-README 記述の実測乖離解消
**Areas discussed:** 数値クレームの扱い, Configuration Options の掲載範囲, CLAUDE.md のスコープ, Acknowledgments（討議中にオーナーが追加提起）, Features 節（全文再検証で新規検出）

---

## Todo の折り込み

| Option | Description | Selected |
|--------|-------------|----------|
| README 全体見直し (score 0.9) | 本フェーズの本体。テスト数 413→589、Status v0.5.0→0.6.1、Configuration Options 5件→登録12件 | ✓ |
| RTD 移行 todo (score 0.5) | github.io リンク 7 本の扱いと競合する | |
| citation ノード未対応 todo (score 0.2) | Known Limitations への追記のみ折り込む案 | |

**User's choice:** README 全体見直しのみ
**Notes:** 残り 2 件は reviewed-but-not-folded として CONTEXT.md の Deferred に記録。ただし citation の「未対応であることを README に書く」部分は、全文再検証で Features:30 の矛盾が実測検出されたため結果的に D-10/D-13 でカバーされた。

---

## 領域選択

| Option | Description | Selected |
|--------|-------------|----------|
| 数値クレームの扱い | 413 tests ×2箇所、93% coverage | ✓ |
| Status 行のバージョン | v0.5.0 → 実測 0.6.1、Phase 23 で 0.6.2 へ | |
| Configuration Options の掲載範囲 | 5 件掲載 vs 登録 12 件 | ✓ |
| docs リンク先の確定 | 孤児 `docs/configuration.rst` / github.io 7 本 | |
| Known Limitations の実測更新 | Glossary 対応済み、Citation 未掲載 | |
| CLAUDE.md の stale 記述をスコープに含めるか | Python 3.10+ 記述 4 箇所 | ✓ |

**User's choice:** 数値クレーム / Configuration Options / CLAUDE.md スコープ の 3 領域
**Notes:** 未選択の 3 領域は Claude 裁量へ。方針を明示して確認を取り、D-11/D-12/D-13 として記録。

---

## 数値クレームの扱い

### テスト数（README:223, :243）

| Option | Description | Selected |
|--------|-------------|----------|
| 実測値に書き換える | 413 → 589 | |
| 数値を落とす | 件数の記載自体を削る。乖離が原理的に発生しなくなる | ✓ |
| 数値を残し CI で検証する | README の数値と `pytest --collect-only` の一致をテスト/CI で担保 | |

**User's choice:** 数値を落とす
**Notes:** 413 → 577（todo 起票時）→ 589（本日実測）と数日で二度ずれた実績が決め手。

### カバレッジ（README:246）

| Option | Description | Selected |
|--------|-------------|----------|
| テスト数と同じ方針に揃える | 上の選択をカバレッジにも適用 | |
| 実測して書き換える | `pytest --cov` の値に更新 | |
| 数値を落とす | 行ごと削る | ✓ |

**User's choice:** 数値を落とす

### 削除の適用範囲

| Option | Description | Selected |
|--------|-------------|----------|
| テスト数・カバレッジだけ | `@preview` バージョン（sync テストで担保）と Requirements 節（pyproject と一致）は残す | ✓ |
| 検証のない数値すべて | Requirements 節の 3 件も対象に入る | |

**User's choice:** テスト数・カバレッジだけ

### Maintain 80%+ code coverage（README:300）

| Option | Description | Selected |
|--------|-------------|----------|
| そのまま残す | 目標値は実測クレームと別物 | |
| 削除する | `fail_under` 等の強制設定がなく検証できない | ✓ |

**User's choice:** 削除する

---

## Configuration Options の掲載範囲

| Option | Description | Selected |
|--------|-------------|----------|
| 全 12 件を載せる | 登録済みを全列挙。設定を増減するたび README 更新が要る | |
| 部分列だと明示する | 「主要なもの」と分かる文言にし、全一覧は docs へ誘導 | ✓ |
| 列挙をやめ docs へのポインタにする | README 単体では何が設定できるか分からなくなる | |

**User's choice:** 部分列だと明示する

### 部分列の中身

| Option | Description | Selected |
|--------|-------------|----------|
| 現状 5 件 + typst_documents | todo が名指しした欠落だけを埋める（計 6 件） | ✓ |
| 現状 5 件のまま | 選定は変えず文言だけ追加 | |
| 選定を Claude に任せる | 使用頻度と docs 構成を見て 5〜7 件を選ぶ | |

**User's choice:** 現状 5 件 + typst_documents

---

## CLAUDE.md のスコープ

| Option | Description | Selected |
|--------|-------------|----------|
| このフェーズで直す | README と同じ実測乖離として 4 箇所を修正 | ✓ |
| 別作業にする | フェーズ名通り README に限定 | |
| このフェーズで直す（記述のみ） | ruff 設定には触れない | |

**User's choice:** このフェーズで直す

### ruff UP006/UP035 ignore の扱い

| Option | Description | Selected |
|--------|-------------|----------|
| 記述だけ直す（ignore は維持） | 根拠文言のみ修正、ソース無変更 | |
| ignore を外して typing を更新 | 4 ファイルの Dict/List/Set/Tuple を書き換え | |
| 記述だけ直し、更新は todo に退避 | 上の 1 を実施した上で typing 更新を pending todo に起票 | ✓ |

**User's choice:** 記述だけ直し、更新は todo に退避

---

## Acknowledgments（オーナーが討議中に提起）

裁量項確認の場でオーナーが `README.md:315` の `Kiro-style Spec-Driven Development` が実態と合っていないと指摘。実測で `.kiro` 不在・GSD 運用を確認（Kiro 参照は README:315 のみ、`CHANGELOG.md:601` は 0.4.x の履歴なので対象外）。

| Option | Description | Selected |
|--------|-------------|----------|
| GSD に差し替える | 方法論名を実態に合わせる | ✓ |
| 方法論の記述を落とす | Claude Code のみ残す | |
| 行ごと削除する | Acknowledgments から丸ごと削る | |

**User's choice:** GSD に差し替える
**Notes:** この指摘を受けて全文再検証を継続し、Features:30 と examples の 2 件を追加検出した。

---

## Features 節 / Example projects（全文再検証で新規検出）

### Features:30 の citations

| Option | Description | Selected |
|--------|-------------|----------|
| citations を取る | `visit_citation` 不在。Known Limitations の追記と整合させる | ✓ |
| Claude に任せる | 実測に合う文言を Claude が確定 | |

**User's choice:** citations を取る

### Example projects（README:245）

| Option | Description | Selected |
|--------|-------------|----------|
| charged-ieee を追記 | 3 つのサンプルを列挙 | |
| 現状のまま | basic / advanced の 2 つのみ維持 | ✓ |

**User's choice:** 現状のまま

---

## Claude's Discretion

未選択領域について方針を提示し、オーナー確認を経て確定した 3 項（CONTEXT.md の D-11/D-12/D-13）。

- **Status 行** — README:323 を `Stable (v0.6.1)` に。0.6.2 の先行記載はしない。Phase 23 のバンプ作業に README:323 を含める申し送り。
- **docs リンク先** — README:205 を `docs/source/user_guide/configuration.rst` へ張り替え。孤児 `docs/configuration.rst` の削除は todo へ退避。github.io リンク 7 本は現状維持。
- **Known Limitations** — Glossary を削除（`translator.py:4324` で対応済み）、Citation を追加、Bibliography は維持。

## Deferred Ideas

- typing の modernize（`UP006`/`UP035` ignore 撤去 + 4 ファイル書き換え）— pending todo に起票
- 孤児 `docs/configuration.rst` の削除 — pending todo に起票
- RTD 移行（既存 todo）— github.io リンクの一括変更は移行作業側で
- `visit_citation` の実装（既存 todo）— 本フェーズは記述の是正のみ
