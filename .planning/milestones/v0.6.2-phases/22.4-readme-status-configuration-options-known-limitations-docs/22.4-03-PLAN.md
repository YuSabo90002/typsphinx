---
phase: 22.4-readme-status-configuration-options-known-limitations-docs
plan: 03
type: execute
wave: 2
depends_on: ["22.4-02"]
files_modified:
  - CLAUDE.md
  - pyproject.toml
autonomous: true
requirements: [DOC-04]
must_haves:
  truths:
    - "`CLAUDE.md` の 4 箇所の Python バージョン記述が実測（`requires-python = \">=3.12\"` / `tox.ini` の `env_list = py312, py313` / CI の `['3.12','3.13']` / classifiers 3.12・3.13 のみ）と一致する (D-07)"
    - "`pyproject.toml` の ruff ignore コメントが、ignore を維持している**正直な理由**を述べている — 3.10 互換のためではなく、typing modernize を専用パスへ意図的に先送りしているため (D-08)"
    - "`UP006` / `UP035` の ignore は ignore リストに残っており、`typsphinx/` のソースは 1 行も変わっていない (D-08)"
    - "コメントが指す先送り先が実在する — Plan 02 が起票した D-15 の todo ファイルをパスで参照する (D-08, D-15)"
  artifacts:
    - "CLAUDE.md（修正済み・新規ファイルなし）"
    - "pyproject.toml（コメント行のみ修正・新規ファイルなし）"
  key_links:
    - "`CLAUDE.md` / `pyproject.toml` のコメント → `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md`（Plan 02 が作成。存在しなければ dangling reference になるため wave 2 で直列化している）"
    - "`CLAUDE.md` の記述 ↔ `pyproject.toml` / `tox.ini` / `.github/workflows/ci.yml` の実測（唯一の正解源）"
  prohibitions:
    - statement: "`typsphinx/` 配下のファイルを 1 バイトも変更しない。typing の modernize は本フェーズでは**行わない**（D-15 の todo へ退避済み）"
      status: required
      verification: "git diff --stat 240142a..HEAD -- typsphinx/ が空"
    - statement: "`pyproject.toml` の `[tool.ruff.lint] ignore` リストから `UP006` / `UP035` を外さない — 変更するのは同じ行のコメント文言だけ (D-08)"
      status: required
      verification: "grep -cE '\"UP006\"|\"UP035\"' pyproject.toml が 2"
    - statement: "`pyproject.toml` の `requires-python` / `version` / `classifiers` / `dependencies` を変更しない。バージョンバンプは Phase 23 の仕事"
      status: required
      verification: "grep -c 'version = \"0.6.1\"' pyproject.toml が 1、grep -c 'requires-python = \">=3.12\"' pyproject.toml が 1"
    - statement: "`tox.ini` と `.github/workflows/ci.yml` を変更しない — これらは実測の正解源であって修正対象ではない"
      status: required
      verification: "git diff --stat 240142a..HEAD -- tox.ini .github/ が空"
    - statement: "`CHANGELOG.md` の過去バージョンエントリを書き換えない（旧 Python フロアへの言及は履歴として正しい）"
      status: required
      verification: "git diff --stat 240142a..HEAD -- CHANGELOG.md が空"
    - statement: "`CLAUDE.md` の §Worktree-isolated execution 節を変更しない（本フェーズと無関係、かつ実行モードの正解源）"
      status: required
      verification: "grep -c 'Worktree-isolated execution' CLAUDE.md が 2 以上、grep -c 'UV_PROJECT_ENVIRONMENT' CLAUDE.md が変更前と同数"
    - statement: "`README.md` を変更しない（本フェーズで README を編集するのは Plan 01 のみ）"
      status: required
      verification: "本プランのコミットに README.md が含まれない"
---

<objective>
`CLAUDE.md` の 4 箇所と `pyproject.toml` の ruff ignore コメントを実測に合わせる。README と同種の
stale claim だが編集面が完全に disjoint なため独立プランとし、コメントが参照する D-15 todo が
確実に存在するよう Plan 02 の後段（wave 2）に置く。

Purpose: `CLAUDE.md` は AI エージェントの作業指針そのものであり、誤った Python フロアを述べている
限り後続フェーズのエージェントが「3.10 互換のため」という存在しない制約に従い続ける。ruff ignore の
コメントも同様に、実態でない理由を根拠として流通させている。

Output: 修正済みの `CLAUDE.md` と `pyproject.toml`（コメント行のみ）。ソース変更ゼロ、
ruff ignore の値も不変。
</objective>

## Artifacts this phase produces

このプランが **新規に作成するファイルは無い**。`CLAUDE.md` と `pyproject.toml` を in-place で修正するのみ。
新しいシンボル・クラス・関数・CLI フラグ・設定値も一切導入しない。`pyproject.toml` の変更は
`[tool.ruff.lint] ignore` 配列内の**行末コメント文言のみ**であり、配列の要素は不変。

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-CONTEXT.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-RESEARCH.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-VALIDATION.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-02-SUMMARY.md
@CLAUDE.md

**⚠ CLAUDE.md 自身の Python バージョン記述は stale であり、本プランの修正対象そのものである。**
プロジェクト規約としての CLAUDE.md には従うが、Python バージョンに関する記述だけは
`pyproject.toml` / `tox.ini` / `.github/workflows/ci.yml` を権威として扱うこと。

**Worktree 前提（CLAUDE.md §Worktree-isolated execution）:** `test -f .git` が真なら最初に
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` を実行し、`ruff` / `black` /
`pytest` は必ず `uv run` 経由で呼ぶ。

**座標は信用しない。** RESEARCH.md が挙げる `CLAUDE.md:28/:29/:75/:78` と `pyproject.toml:122-123` は
2026-07-22 のスナップショット。対象箇所は `grep -n` で現在位置を引き直してから編集すること。
</context>

<!-- 以下は acceptance criteria が 0 件を要求する文字列リテラルであり、
     本プラン内で対象箇所を一意に指し示すために不可避に登場する。 -->
<!-- planner-discipline-allow: py310 -->
<!-- planner-discipline-allow: Python 3.10+ -->
<!-- planner-discipline-allow: Python 3.10+ support -->

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md の Python バージョン記述 4 箇所を実測に合わせる (D-07/D-08)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` — §Commands の tox コメント 2 行と §Conventions &amp; gotchas の 2 行。現在位置は `grep -n 'py310\|Python 3.10' CLAUDE.md` で引き直す
    - `pyproject.toml` — `requires-python` の値と `classifiers` の Python エントリ（実測の正解源）
    - `tox.ini` — `env_list` の実値（実測の正解源）
    - `.github/workflows/ci.yml` — `python-version` マトリクスの実値（実測の正解源）
    - `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` — Plan 02 が作成した D-15 の todo。本タスクでパス参照するため存在を確認する
    - `.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-RESEARCH.md` — Task D（原文引用と修正後文言案、および ignore が残っている「正直な理由」の調査結果）
  </read_first>
  <action>
    まず `test -f .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` で
    Plan 02 の成果物を確認する（存在しなければ停止して報告 — 参照先の無いコメントを書かないため）。

    `CLAUDE.md` の 4 箇所を実測値に直す (D-07)。実測は `requires-python = ">=3.12"`、
    `tox.ini` の `env_list = py312, py313, lint, type, cov, docs`、CI の `python-version: ['3.12', '3.13']`、
    classifiers も 3.12 / 3.13 のみ。

    1. §Commands の tox 一括実行を説明するコメント行 → `# Everything, matrixed across py312–py313`
    2. その次行、`tox` コマンドの行末コメント → `# env_list: py312, py313, lint, type, cov, docs`
       （`tox.ini` の実値をそのまま写す。既存の `tox` コマンド本体と桁揃えは維持する）
    3. §Conventions &amp; gotchas の先頭箇条書き（Python 互換性要件と ruff ignore の根拠を述べている行）
       → 実測フロアを述べ、ignore が残っている理由を**正直に**書き換える。目標文言:
       `- **Python 3.12+ is required.** ruff intentionally ignores \`UP006\`/\`UP035\` (the \`Dict\`/\`List\` → \`dict\`/\`list\` upgrades) — this is a deliberate deferral, not a compatibility constraint; the modernization pass is filed at \`.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md\`. Don't "modernize" typing imports until that todo lands.`
       技術的正当化を捏造しないこと — RESEARCH Task D の調査では、`Dict`/`List` を使い続ける
       技術的必然性は存在せず、フロアを 3.12 に引き上げた際（Phase 6, v0.5.0）に ignore を外し損ねた
       メンテナンスの取りこぼしが実態である (D-08)。
    4. §Conventions &amp; gotchas の CI マトリクスを述べる箇条書き → マトリクス表記を `py312–py313` に直す。
       同じ行の後段（`drift.yml` の週次再解決と `release.yml` の PyPI 公開）は**変更しない**。

    **触ってはいけないもの:** §Worktree-isolated execution 節は完全に不変（本フェーズと無関係で、
    かつ実行モードの正解源）。§Overview / §Architecture / §Configuration surface / §Tests も不変。
    `typsphinx/` のソースも当然不変 — 本タスクは散文のみ (D-08)。
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md &amp;&amp; test "$(grep -c 'py310' CLAUDE.md)" = "0" &amp;&amp; test "$(grep -c 'Python 3.10+' CLAUDE.md)" = "0" &amp;&amp; test "$(grep -c 'py312' CLAUDE.md)" -ge 2 &amp;&amp; test "$(grep -c 'Python 3.12+' CLAUDE.md)" -ge 1 &amp;&amp; test "$(grep -c '2026-07-22-modernize-typing-imports-drop-up006-up035-ignore' CLAUDE.md)" -ge 1 &amp;&amp; test "$(grep -c 'Worktree-isolated execution' CLAUDE.md)" -ge 2 &amp;&amp; test "$(grep -c 'UV_PROJECT_ENVIRONMENT' CLAUDE.md)" -ge 3 &amp;&amp; test -z "$(git diff --name-only 240142a..HEAD -- typsphinx/ tox.ini .github/)" &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'py310' CLAUDE.md` が **0**、`grep -c 'Python 3.10+' CLAUDE.md` が **0**（D-07）
    - `grep -c 'py312' CLAUDE.md` が **2 以上**（§Commands の 2 行 + CI マトリクス行）
    - `grep -c 'Python 3.12+' CLAUDE.md` が **1 以上**
    - `grep -c '2026-07-22-modernize-typing-imports-drop-up006-up035-ignore' CLAUDE.md` が **1 以上** かつ同名ファイルが `test -f` で存在する（dangling reference でないこと、D-08/D-15）
    - `grep -c 'Worktree-isolated execution' CLAUDE.md` が **2 以上**、`grep -c 'UV_PROJECT_ENVIRONMENT' CLAUDE.md` が **3 以上**（§Worktree 節のアンチリグレッション）
    - `grep -c 'drift.yml' CLAUDE.md` が **1**、`grep -c 'release.yml' CLAUDE.md` が **1**（CI 箇条書きの後段を消していないこと）
    - `git diff --name-only 240142a..HEAD -- typsphinx/ tox.ini .github/` が空
  </acceptance_criteria>
  <done>CLAUDE.md の Python 記述が実測と一致し、ruff ignore の根拠が捏造されていない実態の説明になり、参照先の todo が実在する。</done>
</task>

<task type="auto">
  <name>Task 2: pyproject.toml の ruff ignore コメントを実態に合った理由に書き換える (D-08)</name>
  <files>pyproject.toml</files>
  <read_first>
    - `pyproject.toml` — `[tool.ruff.lint]` の `ignore` 配列全体。`UP035` と `UP006` の 2 行の現行コメント文言と、周囲の他の ignore 行（`E501` / `T201` / `B017` / `UP028` / `N802` / `A001` / `F841`）の書式を確認する。現在位置は `grep -n 'UP035\|UP006' pyproject.toml` で引き直す
    - `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` — 参照先。存在を確認する
    - `.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-RESEARCH.md` — Task D の「正直な理由」の結論
  </read_first>
  <action>
    `[tool.ruff.lint] ignore` 配列の `UP035` 行と `UP006` 行について、**行末コメントの文言だけ**を
    書き換える。配列の要素（`"UP035",` / `"UP006",`）と他の全 ignore 行は 1 文字も変えない (D-08)。

    現行コメントは両行とも括弧で 3.10 互換を理由として挙げているが、これは実態と一致しない
    （`requires-python = ">=3.12"` である以上、組み込みジェネリクスは何の制約もなく使える）。
    実態は「typing modernize を専用パスへ意図的に先送りしている」であり、その先送り先は
    Plan 02 が起票した todo である。目標文言:

    - `UP035` 行のコメント → `# typing.Dict/List/Set deprecation; modernization deferred (see .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md)`
    - `UP006` 行のコメント → `# Use dict instead of Dict; same deferral as UP035 above`

    括弧内で 3.10 互換を理由に挙げる表現は両行から完全に除去すること。

    **触ってはいけないもの:** `[project]` の `version` / `requires-python` / `classifiers` /
    `dependencies` / `optional-dependencies` / `entry-points`、`[tool.ruff.lint] select`、
    `[tool.mypy]`、`[tool.pytest.ini_options]` — いずれも不変。バージョンバンプは Phase 23 の仕事であり
    本フェーズでは行わない。

    書き換え後、フォーマッタ・リンタの回帰が無いことを確認する:
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run ruff check .` と
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run black --check .` がともに exit 0 であること。
    ignore を実際に外していないので `ruff check` の結果は変化しないはずであり、変化したら回帰である。
  </action>
  <verify>
    <automated>test "$(grep -c 'Python 3.10+ support' pyproject.toml)" = "0" &amp;&amp; test "$(grep -cE '"UP006"|"UP035"' pyproject.toml)" = "2" &amp;&amp; test "$(grep -c '2026-07-22-modernize-typing-imports-drop-up006-up035-ignore' pyproject.toml)" -ge 1 &amp;&amp; test -f .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md &amp;&amp; test "$(grep -c 'version = "0.6.1"' pyproject.toml)" = "1" &amp;&amp; test "$(grep -c 'requires-python = ">=3.12"' pyproject.toml)" = "1" &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run ruff check . &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run black --check . &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Python 3.10+ support' pyproject.toml` が **0**（D-08）
    - `grep -cE '"UP006"|"UP035"' pyproject.toml` が **2**（ignore は維持、D-08）
    - `grep -c '2026-07-22-modernize-typing-imports-drop-up006-up035-ignore' pyproject.toml` が **1 以上** かつ同名ファイルが存在する
    - `grep -c 'version = "0.6.1"' pyproject.toml` が **1**、`grep -c 'requires-python = ">=3.12"' pyproject.toml` が **1**（バージョン系不変）
    - `uv run ruff check .` が exit 0、`uv run black --check .` が exit 0
    - `git diff --stat 240142a..HEAD -- typsphinx/` が空
  </acceptance_criteria>
  <done>ruff ignore コメントが実態に即した先送り理由を述べ、ignore 自体は維持され、lint/format が green のまま。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （該当なし） | 本プランは `CLAUDE.md` の散文と `pyproject.toml` の行末コメントのみを編集する。ビルド設定の実効値（依存関係・エントリポイント・lint ルール）は一切変化せず、実行されるコードパスも増減しない |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| （なし） | — | — | — | — | 変更対象は散文とコメントのみ。依存関係の追加・削除・バージョン変更はなく（サプライチェーン面は不変）、入力を解析するコードも、ネットワーク到達先も追加されない。`ruff` の `ignore` 配列を意図せず緩めることだけが理論上の attack surface 変化になり得るが、これは `grep -cE '"UP006"\|"UP035"' pyproject.toml` == 2 と `uv run ruff check .` exit 0 の 2 段で機械的に封じてある。ASVS L1 の V2/V3/V4/V5/V6 のいずれも適用対象を持たない。捏造した脅威を並べるより、適用なしと明記するのが誠実な結論である |

**Package installs:** なし。依存関係の宣言（`dependencies` / `optional-dependencies`）は不変であり、
`uv.lock` も再生成しない。`T-22.4-SC` サプライチェーン脅威も適用外。
</threat_model>

<verification>
全タスク完了後、worktree で以下を実行する（`test -f .git` が真なら先に
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`）。

1. `grep -c 'py310' CLAUDE.md` → 0 / `grep -c 'py312' CLAUDE.md` → 2 以上
2. `grep -c 'Python 3.10+' CLAUDE.md` → 0
3. `grep -c 'Python 3.10+ support' pyproject.toml` → 0
4. `grep -cE '"UP006"|"UP035"' pyproject.toml` → 2
5. `test -f .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` → 真（CLAUDE.md / pyproject.toml の参照先が実在）
6. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run ruff check .` → exit 0
7. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run black --check .` → exit 0
8. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run mypy typsphinx/` → フェーズ開始前と同じ結果
9. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest` → フェーズ開始前と同じパス件数
10. スコープフェンス: `git diff --stat 240142a..HEAD -- typsphinx/ docs/ tox.ini .github/ CHANGELOG.md` → 出力なし
</verification>

<success_criteria>
- ROADMAP SC#4（`CLAUDE.md` 4 箇所と `pyproject.toml` の ruff ignore コメントが実測フロアを述べ、`UP006`/`UP035` の ignore は維持される）が満たされる
- ROADMAP SC#6（スコープフェンス）が `git diff --stat` で確認できる
- lint / format / type / test がすべてフェーズ開始前と同じ結果
</success_criteria>

<output>
Create `.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-03-SUMMARY.md` when done.

SUMMARY には必ず含めること:
- `CLAUDE.md` の 4 箇所の修正前／修正後の文面
- `pyproject.toml` の `UP035` / `UP006` 行の修正前／修正後の文面
- `ruff check` / `black --check` / `mypy` / `pytest` の結果（フェーズ開始前との比較）
</output>
