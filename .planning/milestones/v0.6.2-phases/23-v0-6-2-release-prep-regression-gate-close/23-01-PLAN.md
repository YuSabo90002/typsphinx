---
phase: 23-v0-6-2-release-prep-regression-gate-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - README.md
  - tests/test_readme_version_sync.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "pyproject.toml [project].version reads exactly 0.6.2 and remains the sole version literal in the file (SC#1)"
    - "uv.lock's typsphinx self-entry reads version = \"0.6.2\" and `uv sync --extra dev --locked` exits 0 (SC#1)"
    - "README.md line 316 reads `**Status**: Stable (v0.6.2) - Production ready` (SC#1, D-13)"
    - "tests/test_readme_version_sync.py exists and passes, asserting README's Status version equals pyproject.toml's version by comparing the two parsed values against each other, never against a hardcoded literal (D-13)"
    - "The new sync test is a genuine drift guard, not a tautology: reverting README's Status version alone makes it fail RED (D-13)"
    - "The uv.lock diff contains no dependency-range change and no unexplained transitive resolution change beyond the typsphinx self-entry version and an optional lockfile revision counter"
    - statement: "The new sync test's failure message names the concrete remediation (update README's Status line in lockstep with the version bump), so a future failure is actionable without reading the test source"
      verification: backstop
  artifacts:
    - "tests/test_readme_version_sync.py — new module with REPO_ROOT, README_PATH, PYPROJECT_PATH, _STATUS_LINE_RE, _extract_readme_status_version(), _extract_pyproject_version(), test_readme_status_version_matches_pyproject()"
    - "pyproject.toml — version literal at [project].version bumped to 0.6.2"
    - "uv.lock — regenerated, typsphinx self-entry at ~line 1379 bumped to 0.6.2"
    - "README.md — Status line at :316 bumped to Stable (v0.6.2)"
  key_links:
    - "README.md:316 Status line ↔ pyproject.toml [project].version — the new binding enforced by tests/test_readme_version_sync.py; every future release must move both in lockstep"
    - "pyproject.toml [project].version ↔ uv.lock typsphinx self-entry — NOT auto-derived at read time; requires an explicit `uv lock` run or `uv sync --locked` reports drift"
  prohibitions:
    - statement: "No git tag named v0.6.2 (or any other tag) is created or pushed by this plan"
      status: required
      verification: "`git tag --list 'v0.6.2'` returns empty"
    - statement: "No PyPI upload, no GitHub Release creation, no manual or scripted trigger of .github/workflows/release.yml"
      status: required
      verification: "`git diff v0.6.1..HEAD -- .github/workflows/release.yml` is empty; no `gh release`, `twine`, `uv publish`, or `git push --tags` command is run"
    - statement: "No behavior change under typsphinx/ — this plan touches no package source file"
      status: required
      verification: "`git diff --name-only` for this plan's commits lists no path under typsphinx/"
    - statement: "No modernization of typing imports and no change to the UP006/UP035 ruff ignores while pyproject.toml is open"
      status: required
      verification: "`git diff v0.6.1..HEAD -- pyproject.toml` shows no change to [tool.ruff] ignore list"
    - statement: "No change to tox.ini's deliberate `tox-uv~=1.35` pin"
      status: required
      verification: "`git diff --name-only` for this plan's commits does not list tox.ini"
---

<objective>
Bump typsphinx from 0.6.1 to 0.6.2 across the three declaration surfaces that carry a version literal
(`pyproject.toml`, `uv.lock`, `README.md`), and add the D-13 sync test that permanently binds the README
Status line to `pyproject.toml`'s version so it can never again go stale across releases.

Purpose: satisfies ROADMAP SC#1 and closes the Phase 22.4 D-11 hand-off. The README Status line sat at
v0.5.0 through two releases; per D-13 this is stopped by a mechanism, not by human attention.

Output: `pyproject.toml` at 0.6.2, a regenerated `uv.lock`, `README.md:316` reading `Stable (v0.6.2)`,
and a new `tests/test_readme_version_sync.py`.

`requirements: []` is correct and deliberate: Phase 23 is a release/close phase with no requirement IDs
mapped to it (ROADMAP `**Requirements**` reads "(none — release/close phase; all v1 requirements are
delivered by Phases 19–22.3)"). The v0.6.2 ledger's 23 IDs are covered by plan 23-03 (CHANGELOG), which
carries them in its own `requirements` field.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-CONTEXT.md
@.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-RESEARCH.md
@.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-PATTERNS.md
@.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 23-01 share)

| Artifact | Kind | Symbols defined |
|---|---|---|
| `tests/test_readme_version_sync.py` | new file | `REPO_ROOT`, `README_PATH`, `PYPROJECT_PATH`, `_STATUS_LINE_RE`, `_extract_readme_status_version()`, `_extract_pyproject_version()`, `test_readme_status_version_matches_pyproject()` |
| `pyproject.toml` | modified | `[project].version` → `0.6.2` |
| `uv.lock` | regenerated | `typsphinx` self-entry `version` → `0.6.2` |
| `README.md` | modified | Status line at `:316` |

Produced by sibling plans (listed for completeness): `23-VERIFICATION.md` evidence record (23-02),
`CHANGELOG.md` `## [0.6.2]` section with `### Fixed` / `### Removed` / `### Verified` (23-03).

<assumption_delta_decision>
noun: n/a · decision: no-change · rationale: detector fired on the prose word "also" in a CHANGELOG
section list; no core assumption changes in a release-prep phase.
</assumption_delta_decision>

<specless_probe_note>
spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). `must_haves.truths`
are derived directly from ROADMAP SC#1–SC#5 and CONTEXT.md decisions D-13/D-14.
</specless_probe_note>

<tasks>

<task type="auto">
  <name>Task 1: Bump the version literal in pyproject.toml, README.md, and uv.lock</name>
  <files>pyproject.toml, README.md, uv.lock</files>
  <read_first>
    - `pyproject.toml` (the whole `[project]` table — confirm `version = "0.6.1"` is at line 7 and that no
      other version literal exists in the file; `requires-python`, `dependencies`, `[tool.ruff]`, and
      `[tool.pytest.ini_options]` must all stay byte-identical)
    - `README.md` lines 305–320 (the Version History section and the two-line footer block; line 316 is the
      Status line, line 317 is the dependency-floor footer that must NOT change)
    - `uv.lock` around line 1370–1390 (the `[[package]] name = "typsphinx"` self-entry with its own
      `version` field and its `dependencies` list)
    - `CLAUDE.md` § "Worktree-isolated execution" (mandatory provisioning convention for this project)
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-RESEARCH.md` § "`uv.lock`
      Regeneration" (why the lockfile self-entry does not follow from the pyproject edit) and
      § "Common Pitfalls" Pitfall 2
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-PATTERNS.md` §§ `pyproject.toml`,
      `uv.lock`, `README.md`
  </read_first>
  <action>
    Provision the worktree first, before any other command:
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Run every subsequent command in
    this plan via `uv run`. This is mandatory per CLAUDE.md, not conditional on parallelism.

    Then make exactly three edits, in this order:

    1. `pyproject.toml` line 7: change `version = "0.6.1"` to `version = "0.6.2"`. Change nothing else in
       the file. Specifically do NOT touch `requires-python = ">=3.12"`, the `dependencies` array
       (`sphinx>=9.1,<10`, `docutils>=0.21,<0.23`, `typst>=0.15.0,<0.16`), the `[tool.ruff]` `ignore` list
       (the `UP006`/`UP035` entries are a deliberate deferral recorded in
       `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md`), or
       `[tool.pytest.ini_options]`.

    2. `README.md` line 316: change `**Status**: Stable (v0.6.1) - Production ready` to
       `**Status**: Stable (v0.6.2) - Production ready`. Per D-14 the adjacent line 317
       (`**Python**: 3.12+ | **Sphinx**: 9.1+ | **Typst**: 0.15+`) and the Requirements section at lines
       37–39 are explicitly out of scope — they were measured in sync with `pyproject.toml` during Phase
       22.4 and must be left byte-identical. Also leave the README's `github.io` documentation links alone:
       their 404s are an owner-adjudicated deferral (22.4 D-17) to the Read the Docs migration.

    3. Regenerate the lockfile. Run `uv lock` (not a bare `uv sync`): `uv lock` is the minimal single-purpose
       command that rewrites only `uv.lock`, keeping "did the lock regenerate" separate from "did the venv
       install". Then verify with `uv sync --extra dev --locked`, which fails loudly rather than silently
       re-locking if drift remains. Do not hand-edit `uv.lock`.

    After `uv lock`, inspect `git diff -- uv.lock`. The expected shape is the `typsphinx` self-entry's
    `version` moving `0.6.1` → `0.6.2`, plus at most a cosmetic bump of the lockfile's own `revision`
    metadata counter. If any *transitive* dependency's resolved version also changed (an upstream patch
    release landing inside an existing range), that is incidental upstream drift: record it verbatim in the
    SUMMARY under a heading naming it as observed lockfile drift, but do not treat it as blocking — SC#4's
    invariant is about typsphinx's own declared runtime dependencies and the `@preview` versions, not
    transitive resolution. If a *direct* dependency range changed, stop: that is a real SC#4 violation and
    means an unintended edit reached `pyproject.toml`.

    Scope fence (ROADMAP SC#5, absolute): do not create or push any git tag, do not run `uv publish`,
    `twine`, `gh release create`, or `git push --tags`, and do not edit or invoke
    `.github/workflows/release.yml`. All publish steps belong to `/gsd-complete-milestone`.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import tomllib;print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])"` prints exactly `0.6.2`
    - `grep -c '^version = "0.6.1"' pyproject.toml` returns 0
    - `README.md` contains the exact line `**Status**: Stable (v0.6.2) - Production ready`
    - `README.md` still contains the exact line `**Python**: 3.12+ | **Sphinx**: 9.1+ | **Typst**: 0.15+`
    - `git diff -- README.md` shows exactly one changed line
    - `uv.lock` contains `version = "0.6.2"` on the line immediately following its `name = "typsphinx"` line
    - `uv sync --extra dev --locked` exits 0
    - `git diff v0.6.1..HEAD -- pyproject.toml` shows no added or removed line inside the `dependencies = [` array and no change to `requires-python`
    - `git status --porcelain tox.ini` is empty
    - `git tag --list 'v0.6.2'` returns empty
  </acceptance_criteria>
  <verify>
    <automated>uv sync --extra dev --locked &amp;&amp; test "$(git tag --list 'v0.6.2')" = "" &amp;&amp; uv run python -c "import tomllib,pathlib,sys;v=tomllib.load(open('pyproject.toml','rb'))['project']['version'];r=pathlib.Path('README.md').read_text(encoding='utf-8');lock=pathlib.Path('uv.lock').read_text(encoding='utf-8');errs=[];errs.append('pyproject:'+v) if v!='0.6.2' else None;errs.append('readme-status') if '**Status**: Stable (v0.6.2) - Production ready' not in r else None;errs.append('readme-footer-changed') if '**Python**: 3.12+ | **Sphinx**: 9.1+ | **Typst**: 0.15+' not in r else None;errs.append('uvlock-self-entry') if 'name = \"typsphinx\"\nversion = \"0.6.2\"' not in lock else None;print('ERRORS:',errs);sys.exit(1 if errs else 0)"</automated>
  </verify>
  <done>
    `pyproject.toml` reads 0.6.2, `README.md:316` reads `Stable (v0.6.2) - Production ready` with line 317
    unchanged, `uv.lock`'s typsphinx self-entry reads 0.6.2, and `uv sync --extra dev --locked` exits 0.
    No tag exists, no publish command was run, no file under `typsphinx/` was touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests/test_readme_version_sync.py — the README ↔ pyproject drift guard (D-13)</name>
  <files>tests/test_readme_version_sync.py</files>
  <read_first>
    - `tests/test_preview_version_sync.py` (full file, 106 lines — the template named by D-13: module
      docstring shape that states the hazard and cites the decision ID, `REPO_ROOT =
      Path(__file__).resolve().parents[1]` plus module-level path constants, a commented module-level
      compiled regex, one `_extract_*` helper per file, plain module-level test functions with no `Test*`
      class wrapper, full-sentence docstrings explaining *why* the assertion matters, and f-string failure
      messages that name the exact file context)
    - `tests/test_extension.py` lines 79–95 (`test_version_matches_pyproject_toml` — the proven
      `tomllib.load(open(path, "rb"))["project"]["version"]` parsing precedent in this repo)
    - `README.md` line 316 (the exact prose the regex must match)
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-RESEARCH.md` § "Version-Sync Test
      (D-13) — Design", its § "Code Examples" new-sync-test skeleton (authoritative, usable near-verbatim),
      and § "Common Pitfalls" Pitfall 5
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-PATTERNS.md`
      § `tests/test_readme_version_sync.py`
  </read_first>
  <action>
    Create the new module `tests/test_readme_version_sync.py`. Do NOT append to
    `tests/test_preview_version_sync.py` — that module's own docstring scopes it narrowly to the four-package
    `@preview` hazard, and this project's convention is one small single-purpose module per drift hazard
    (`test_builder_output_stem.py`, `test_target_name_render_gate.py`). The new filename mirrors the
    `test_<subject>_version_sync.py` naming shape.

    Take RESEARCH.md § "Code Examples" § "New sync test skeleton" as the starting text; it is already
    correct against the current file contents. Required contents:

    - A module docstring in the analog's shape: state the hazard concretely (the README Status line drifted
      stale relative to `pyproject.toml` across two prior releases, sitting at v0.5.0 through v0.6.0 and
      v0.6.1), name both files, state what is asserted and why it prevents recurrence, and cite `D-13` the
      same way the analog cites `D-03`.
    - Module-level constants: `REPO_ROOT = Path(__file__).resolve().parents[1]`, `README_PATH = REPO_ROOT /
      "README.md"`, `PYPROJECT_PATH = REPO_ROOT / "pyproject.toml"`. Use the module-level constants style of
      `test_preview_version_sync.py`, not `test_extension.py`'s inline per-test path construction.
    - A module-level compiled regex named `_STATUS_LINE_RE` matching the README Status prose:
      `r"\*\*Status\*\*:\s*Stable \(v(?P<version>\d+\.\d+\.\d+)\)"`, with a short comment above it noting it
      targets README prose (parens plus a leading `v`), not the `#import "@preview/name:version"` syntax the
      analog's `_PREVIEW_IMPORT_RE` targets. Do not copy that regex.
    - `_extract_readme_status_version()` — reads `README_PATH.read_text(encoding="utf-8")`, searches with
      `_STATUS_LINE_RE`, and guards with `assert match, "..."` naming the file and asking whether the Status
      line's wording changed. This guard is the vacuous-pass defence (the analog uses a second top-level
      test for this because it compares a four-item set; here one value per file makes a single
      assert-with-message sufficient). Returns `match.group("version")`, a plain `str`.
    - `_extract_pyproject_version()` — `tomllib.load` on `PYPROJECT_PATH` opened in binary mode, returning
      `data["project"]["version"]`. Use `tomllib`, not a regex: a stdlib parser for this exact field is
      already proven in `tests/test_extension.py`.
    - One plain module-level test function `test_readme_status_version_matches_pyproject()` with a
      full-sentence docstring. It compares the two parsed values against each other — never against any
      hardcoded expected version string — so the test survives future joint bumps and fails only on genuine
      divergence. Its f-string failure message must name both observed values and the concrete remediation:
      update the README Status line in lockstep with any version bump.
    - Import only `re`, `tomllib`, and `pathlib.Path`. Add no dependency; `tomllib` is stdlib on the
      project's `>=3.12` floor. Do not modernize or restructure typing imports anywhere.
    - Keep to black's 88-column line length.

    D-14 fences the scope: assert the Status line's release version only. Do not parse or assert
    `README.md:37-39` (Requirements section) or `README.md:317` (the `**Python** | **Sphinx** | **Typst**`
    footer) — those were measured in sync during Phase 22.4 and a future same-shaped test for them is an
    explicitly deferred idea, not this phase's work.

    Then prove the test is a real drift guard rather than a tautology: temporarily rewrite the README Status
    line's version to a different value, run the new test and confirm it fails, then restore `README.md`
    exactly and confirm `git diff -- README.md` matches Task 1's single-line change. The verify command below
    performs this RED proof and its own restore.

    Lint the new file to match CI: `uv run python -m black --check tests/test_readme_version_sync.py` and
    `uv run python -m ruff check tests/test_readme_version_sync.py`. If the compiled-binary ELF hazard
    documented in the project's NixOS notes bites (`uv run ruff` failing to start), fall back to
    `nix-shell -p ruff --run "ruff check tests/test_readme_version_sync.py"`. `mypy typsphinx/` is a no-op
    for this plan since no package source is touched.

    Scope fence (ROADMAP SC#5): still no tag, no publish, no GitHub Release, no `release.yml` interaction.
  </action>
  <acceptance_criteria>
    - `tests/test_readme_version_sync.py` exists and contains `def test_readme_status_version_matches_pyproject(`
    - The file contains `def _extract_readme_status_version(` and `def _extract_pyproject_version(`
    - The file contains `REPO_ROOT = Path(__file__).resolve().parents[1]`
    - The file contains `import tomllib`
    - `grep -c '"0\.6\.2"' tests/test_readme_version_sync.py` returns 0 — the test must compare the two parsed values against each other, never against a hardcoded expected version
    - `uv run python -m pytest tests/test_readme_version_sync.py -v` exits 0 with 1 passed
    - RED proof: with README's Status version alone changed to a different value, `uv run python -m pytest tests/test_readme_version_sync.py -q` exits non-zero; afterwards `README.md` is restored byte-for-byte
    - `uv run python -m black --check tests/test_readme_version_sync.py` exits 0
    - `uv run python -m ruff check tests/test_readme_version_sync.py` exits 0 (or the documented `nix-shell -p ruff` fallback exits 0)
    - `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v` exits 0 (3 passed)
    - `git tag --list 'v0.6.2'` returns empty
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v &amp;&amp; uv run python -c "import pathlib,subprocess,sys;p=pathlib.Path('README.md');orig=p.read_text(encoding='utf-8');p.write_text(orig.replace('Stable (v0.6.2)','Stable (v0.4.9)'),encoding='utf-8');r=subprocess.run([sys.executable,'-m','pytest','tests/test_readme_version_sync.py','-q']);p.write_text(orig,encoding='utf-8');print('RED_PROOF_RC:',r.returncode);sys.exit(0 if r.returncode else 1)" &amp;&amp; test "$(grep -c '^\*\*Status\*\*: Stable (v0.6.2) - Production ready$' README.md)" = "1" &amp;&amp; test "$(grep -c '0\.4\.9' README.md)" = "0" &amp;&amp; test "$(grep -c '"0\.6\.2"' tests/test_readme_version_sync.py)" = "0" &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <done>
    `tests/test_readme_version_sync.py` exists, passes green against the bumped files, fails RED when the
    README Status version alone is perturbed, is clean under black and ruff, and hardcodes no expected
    version string. `README.md` is byte-identical to its post-Task-1 state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository working tree → published release artifact | Version metadata written here is consumed unmodified by `/gsd-complete-milestone` → `release.yml` → PyPI. A wrong or inconsistent value here becomes an immutable published fact. |
| PyPI index → `uv lock` resolver | `uv lock` re-resolves against a remote index; the resolved set is written into the lockfile that will ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | Version literals across `pyproject.toml`, `uv.lock`, `README.md` | medium | mitigate | Task 1 bumps all three in one change; Task 2's `tests/test_readme_version_sync.py` makes README↔pyproject divergence a hard CI failure; `uv sync --extra dev --locked` makes lock↔pyproject divergence a hard failure |
| T-23-02 | Elevation of Privilege | `.github/workflows/release.yml` (tag-triggered PyPI publish) | high | mitigate | ROADMAP SC#5 scope fence encoded in `must_haves.prohibitions` and restated in both task actions: no tag creation/push, no `uv publish`/`twine`/`gh release`, no workflow edit. Asserted by `git tag --list 'v0.6.2'` returning empty |
| T-23-03 | Tampering | `uv.lock` transitive resolution | medium | mitigate | Task 1 requires inspecting `git diff -- uv.lock` and recording any transitive change verbatim in the SUMMARY; a *direct* dependency-range change is a stop condition. Direct deps remain range-pinned in `pyproject.toml`, which this plan does not alter |
| T-23-04 | Spoofing | `tests/test_readme_version_sync.py` as a false assurance | low | mitigate | The test is required to compare two parsed values (never a hardcoded literal, asserted by a grep) and to be RED-proven by perturbing README before it is accepted — a test that cannot fail is worse than no test |
| T-23-SC | Tampering | pip/uv package installation | low | accept | No package-manager install task exists in this phase; `23-RESEARCH.md` § "Package Legitimacy Audit" records the gate as not applicable (zero new runtime or dev dependencies). `uv lock`/`uv sync` re-resolve only the already-declared, range-pinned dependency set |
</threat_model>

<verification>
- `uv sync --extra dev --locked` exits 0
- `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v` → 3 passed
- `uv run python -m pytest -q -m "not slow" --ignore=tests/test_integration_advanced.py --ignore=tests/test_integration_basic.py --ignore=tests/test_integration_multi_doc.py --ignore=tests/test_integration_nested_toctree.py --ignore=tests/test_examples_basic.py` shows no NEW failure versus the pre-plan baseline (those five files fail environmentally inside worktrees — a known non-regression documented in the project's NixOS notes; do not spend turns diagnosing them)
- `git tag --list 'v0.6.2'` returns empty
- `git diff --name-only v0.6.1..HEAD -- typsphinx/` contains no file changed by this plan
</verification>

<success_criteria>
- SC#1 satisfied for its version-bump half: `pyproject.toml` at 0.6.2, `uv.lock` regenerated in lockstep, `uv sync --locked` green
- D-13 satisfied: `README.md:316` reads `Stable (v0.6.2)` and a new test binds it to `pyproject.toml`
- D-14 respected: the sync test covers the Status line only
- SC#5 held: no tag, no publish, no Release, no `release.yml` interaction
</success_criteria>

<output>
Create `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-01-SUMMARY.md` when done.
Include the observed `git diff --stat -- uv.lock` and a verbatim note of any transitive resolution change.
</output>
