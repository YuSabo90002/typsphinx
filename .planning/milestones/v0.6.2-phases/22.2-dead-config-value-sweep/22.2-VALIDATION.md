---
phase: 22.2
slug: dead-config-value-sweep
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-22
---

# Phase 22.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `22.2-RESEARCH.md` § Validation Architecture (lines 629-678).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (config in `pyproject.toml`, `[tool.pytest.ini_options]`) |
| **Config file** | `pyproject.toml:73-90` — `testpaths = ["tests"]`, markers `slow`/`integration`, `filterwarnings = ["error::DeprecationWarning", …]` |
| **Quick run command** | `uv run pytest tests/test_template_engine.py tests/test_config_other_options.py tests/test_documentation_configuration.py -x` |
| **Full suite command** | `uv run pytest` |
| **Estimated runtime** | ~15s quick · ~4–8 min full (GATE-01 fixtures download `@preview` packages) |

> **Worktree note (CLAUDE.md § Worktree-isolated execution):** executors run in isolated git
> worktrees. Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
> first, then run **every** command above via `uv run`. Without this, pytest imports the
> unchanged main-tree package and gates stay red after a correct fix.

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/test_template_engine.py tests/test_config_other_options.py tests/test_documentation_configuration.py -x` (fast, no network)
- **After every plan wave:** Run `uv run pytest -m "not slow" -x` **plus** the new GATE-01 module explicitly (`uv run pytest tests/test_<new>_gate.py -v`)
- **Before `/gsd-verify-work`:** Full suite must be green — including `tests/test_corpus_gate.py` and `tests/test_preview_version_sync.py`, which must stay **untouched and green** per the milestone invariant (no `@preview` bump, 3-way version-sync surface unchanged)
- **Max feedback latency:** ~15 seconds (quick command)

---

## Per-Task Verification Map

*Seeded as `draft` — task IDs are populated after `/gsd-plan-phase` emits PLAN.md files.
The requirement→behavior→command rows below are the authoritative contract; the planner maps
them onto task IDs.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | CONF-01 | — | N/A | unit | `uv run pytest tests/test_config_other_options.py -x` — the two `typst_output_dir` tests (141-179) are **deleted**, not made to pass | ✅ (edit) | ⬜ pending |
| TBD | TBD | TBD | CONF-01 | — | N/A | unit | `uv run pytest tests/test_documentation_configuration.py -x` — `required_configs` (line 46) drops `typst_output_dir` | ✅ (edit) | ⬜ pending |
| TBD | TBD | TBD | CONF-01 | — | N/A | unit | `uv run pytest tests/test_template_engine.py -k typst_author_params -x` — `test_typst_author_params_backward_compatibility` (line 717) **deleted** | ✅ (delete) | ⬜ pending |
| TBD | TBD | TBD | CONF-02 (BUG-A) | — | N/A | unit | new GATE module: emitted `.typ` contains **no** `_template.typ` import when `typst_package` is set alone | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | CONF-02 (BUG-B) | — | N/A | unit | new GATE module: `#show: ieee.with(...)` carries **no** unmapped `title`/`authors`/`date` when `typst_template_mapping` is unset | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | CONF-02 (BUG-C) | — | N/A | integration (GATE-01) | new GATE module: real `typst.compile()` succeeds — a `str`-vs-`array` `authors` type error surfaces here | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | CONF-02 (BUG-E) | — | N/A | unit | new precedence test: explicit `typst_template_function["params"]` wins over a **colliding** auto-derived key | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | CONF-02 (BUG-F) | — | N/A | unit | new GATE module: package-only master emits all four `@preview` imports + `codly-init` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | CONF-02 (non-regression) | — | N/A | unit | `uv run pytest tests/test_template_engine.py -k render -x` — existing render tests (460-576) stay green **unmodified**, proving D-08's swap is a no-op for non-colliding keys | ✅ | ⬜ pending |
| TBD | TBD | TBD | CONF-02 (D-12) | — | N/A | integration | `examples/charged-ieee/approach1` builds under `-b typstpdf`, exit 0, non-empty PDF | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | CONF-03 | — | N/A | integration | setting `typst_package` **changes emitted output** vs. not setting it (GATE-01 module) | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | CONF-03 | — | N/A | unit | setting `typst_authors` changes `map_parameters()`'s returned `params["authors"]` — asserted **through** the pipeline, not by calling `_format_authors_with_details()` directly | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | CONF-03 | — | N/A | unit | `typst_template` / `typst_template_function` output-change coverage — audit `tests/test_template_engine.py:580-628` during planning; extend only if a gap is found | ✅ (audit) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/fixtures/<new-fixture-name>/` — minimal `charged-ieee`-alone Sphinx project, `typst_package` set, **no** `typst_template` (D-09)
- [ ] `tests/test_<new>_gate.py` — GATE-01 module with **named** BUG-A / BUG-B / BUG-C / BUG-E / BUG-F assertions plus a recorded pre-fix-basis failure proof (D-09)
- [ ] A test that builds `examples/charged-ieee/approach1` end-to-end and asserts success (D-12) — **no existing test exercises any bundled `examples/` project as a live build**
- [ ] Extension of `tests/test_template_engine.py`'s `TestTypstAuthorsConfig` to assert **through** `map_parameters()`. Current tests call the dead-code `_format_authors_with_details()` directly, so they would keep passing even if D-07's wiring hits the double-formatting trap (RESEARCH Pitfall #2)
- [ ] A BUG-E precedence test with a **colliding** key — every current dict-format test uses non-overlapping names (`abstract` / `index-terms` / `paper-size`), so none can observe the inversion

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Rendered PDF of the `charged-ieee` fixture is visually a two-column IEEE paper | CONF-02 | Visual fidelity is not asserted by "compiles with zero errors"; the phase's automated contract stops at a clean compile | Build the D-09 fixture with `-b typstpdf`, open the PDF, confirm two-column IEEE layout with the author block rendered from `typst_authors` dicts |
| `### Removed` note is carried into the `[0.6.2]` CHANGELOG entry | CONF-01 | Phase 23 curates the CHANGELOG; this phase only **stages** the note | Confirm the staged note names **both** `typst_output_dir` and `typst_author_params` (ROADMAP line 376 currently names only the first) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s (quick command)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
