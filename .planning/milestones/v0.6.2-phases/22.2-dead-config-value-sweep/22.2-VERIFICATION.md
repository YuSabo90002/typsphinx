---
phase: 22.2-dead-config-value-sweep
verified: 2026-07-21T22:01:44Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 22.2: Dead Config-Value Sweep Verification Report

**Phase Goal:** Two user-facing config values were registered and documented but never affected
output — `typst_output_dir` did nothing at all, and the entire `typst_package` (Typst Universe) path
failed to compile. Fix both, and land a config→output regression fixture so registration-only
assertions can no longer hide a dead feature.
**Verified:** 2026-07-21T22:01:44Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP §Phase 22.2 Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `typst_output_dir` (and `typst_author_params`, folded in under D-07/D-13) is gone from every named surface, and a `### Removed` note is staged for the `[0.6.2]` CHANGELOG | ✓ VERIFIED | `grep -rn typst_output_dir` / `typst_author_params` across `typsphinx/__init__.py`, `docs/configuration.rst`, `tests/test_config_other_options.py`, `tests/test_documentation_configuration.py` (`required_configs` list), `examples/advanced/conf.py`, `examples/advanced/README.md`, `CLAUDE.md`, `tests/test_template_engine.py` all return zero hits. ROADMAP.md Phase 23 SC#2 already carries the corrected sentence naming **both** removals (`.planning/ROADMAP.md:423`). Only surviving hit is `CHANGELOG.md:489` — a historical `[0.4.4]`-era entry, deliberately untouched per CONTEXT.md's Deferred Ideas ("historical release notes should stay accurate about what shipped"). |
| 2 | A `typst_package`-alone project builds under `-b typstpdf` and compiles with ZERO Typst errors via a real `typst.compile()`; BUG-A..BUG-D (plus BUG-E/F) demonstrably fixed; fixture fails against pre-fix code | ✓ VERIFIED | Ran `tests/test_package_only_config_gate.py` live (not trusted from SUMMARY): 12/12 pass, including a real `-b typstpdf` compile producing a valid `%PDF`-magic, non-empty PDF (verified by opening it — see below). Independently reconstructed the pre-fix code by checking out the commit before `8ceb521` (BUG-A's fix) into a scratch worktree, copied the *current* test+fixture in unmodified, and reran: it fails with the exact pre-fix defect (`sphinx.errors.ExtensionError: ... file not found (searched at .../\_template.typ)`) — proving the fixture is a genuine red→green regression test, not vacuous. `TestPreFixBasisFailureProof` (3 tests) additionally proves each of BUG-A/B/C raises under a real `typst.compile()` when reconstructed from the post-fix master. |
| 3 | `docs/source/examples/advanced.rst` package examples corrected (important-note + modern-cv example removal) | ✓ VERIFIED | Read the file directly: no `modern-cv` occurrence remains (only a stale Japanese `.po` translation catalog msgid, out of this SC's named scope). The "Custom Template Wrapping" important-note text (`typsphinx emits a build warning naming both config values, ignores the typst_package setting, and honours typst_template`) matches the actual `builder.py:539-547` D-03 code exactly (confirmed by reading `builder.py` directly). |
| 4 | A config→output regression fixture asserts a config value CHANGES THE EMITTED OUTPUT | ✓ VERIFIED | `TestConfigOutputDifferenceMatrix` (3 tests, run live) proves `typst_package`, `typst_authors`, and `typst_template_function["params"]` each provably change emitted `.typ` text against an unmodified baseline — not merely "registered." |
| 5 | Zero new runtime deps, no `@preview` version bump, 3-way version-sync surface untouched | ✓ VERIFIED | `git diff 62c3e03..HEAD -- pyproject.toml uv.lock` is empty (no dependency changes across the whole phase). `@preview` versions identical across `writer.py`/`template_engine.py`/`templates/base.typ` (all `codly:1.3.0`, `codly-languages:0.1.10`, `mitex:0.2.7`, `gentle-clues:1.3.1`) and `tests/test_preview_version_sync.py` passes. |

**Score:** 5/5 truths verified (0 present, behavior-unverified)

### Requirement Traceability (CONF-01, CONF-02, CONF-03)

REQUIREMENTS.md currently shows CONF-01/02/03 as unchecked (`- [ ]`). Per the verification brief, this
is deliberate orchestrator state (one executor prematurely marked CONF-02 complete before BUG-A was
fixed; the orchestrator reverted it) and NOT evidence of a gap. Deciding from the codebase directly:

| Requirement | Description | Status | Evidence |
|---|---|---|---|
| **CONF-01** | `typst_output_dir` removed from every surface, `### Removed` CHANGELOG note staged | ✓ SATISFIED | See Truth #1 above — zero remaining references on any named surface; `typst_author_params` (folded in per D-07) also fully removed, including its ctor arg, both `getattr` call sites, the dead legacy branch, and its backward-compat test. |
| **CONF-02** | `typst_package` path works end-to-end (BUG-A/B/C/D) | ✓ SATISFIED | See Truth #2 above — live-run real-compile gate passes, pre-fix-basis failure independently reproduced by this verifier (not just trusted from SUMMARY), both bundled `charged-ieee` samples (`approach1` package-alone, `approach2` custom-template) build and compile for real (`tests/test_examples_charged_ieee_gate.py`, 2/2 pass). |
| **CONF-03** | Config→output regression fixture, registration-only assertions insufficient | ✓ SATISFIED | See Truth #4 above — `TestConfigOutputDifferenceMatrix` live-verified; the two pre-existing registration-only test files (`test_config_other_options.py`, `test_documentation_configuration.py`) had their `typst_output_dir`-specific assertions actually deleted (not just left green), closing the exact escape CONF-03 names. |

**Recommendation:** REQUIREMENTS.md's CONF-01/02/03 checkboxes should be flipped to `- [x]` — they are
genuinely satisfied by the codebase, not merely claimed.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `typsphinx/__init__.py` | `typst_output_dir`/`typst_author_params` registrations removed | ✓ VERIFIED | Confirmed via direct read — 8 `add_config_value` calls remain, neither dead config present |
| `typsphinx/writer.py` | Package-alone routing passes `template_file=None` (D-01) | ✓ VERIFIED | `package_for_engine = None if raw_template_path else typst_package` (line 179); `template_file = None` when package-alone (line 236-237) |
| `typsphinx/builder.py` | Narrowed early-return + both-set warning (D-03) | ✓ VERIFIED | `if typst_package and raw_template_path: logger.warning(...)` and `if typst_package and not raw_template_path: return` (lines 539-551) |
| `typsphinx/template_engine.py` | Essential imports hoisted (D-02/BUG-F), package-path injection stopped (D-05/BUG-B), `typst_authors` wired (D-07/BUG-C), precedence inverted (D-08/BUG-E) | ✓ VERIFIED | Confirmed via `tests/test_template_engine.py` (69/69 pass) including `TestEssentialImportHoist`, `TestPackagePathParameterSuppression`, `TestTypstAuthorsConfig`, `TestTypstTemplateFunctionDictFormat` (colliding-key precedence test) |
| `tests/fixtures/package_only_config_gate/` | New minimal `charged-ieee`-alone Sphinx project (D-09) | ✓ VERIFIED | Exists, builds, compiles for real |
| `tests/test_package_only_config_gate.py` | GATE-01 module, per-defect named assertions, pre-fix-basis proof, diff matrix | ✓ VERIFIED | 12/12 tests pass live; pre-fix-basis independently reproduced by this verifier against actual pre-fix commit |
| `tests/test_examples_charged_ieee_gate.py` | Both bundled samples build end-to-end (D-12) | ✓ VERIFIED | 2/2 pass live; `approach2` test proves the package was actually used (not silently falling back to `base.typ`) |
| `docs/source/examples/advanced.rst` | Modern-cv example removed, important-note correct | ✓ VERIFIED | Confirmed by direct read |
| `examples/charged-ieee/approach1/conf.py`, `approach2/conf.py` | Repaired to match post-fix behavior | ✓ VERIFIED | Both build/compile for real per the gate test |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `writer.py` package-alone branch | `template_engine.render(template_file=None)` | D-01 routing | ✓ WIRED | Confirmed by reading code + `tests/test_package_template_routing.py` (3/3 pass) |
| `builder.py` both-set warning | `logger.warning` | D-03 | ✓ WIRED | `test_both_configured_warns_once_and_template_wins` passes |
| `map_parameters()` | `_format_authors_with_details()` | D-07 (typst_authors wiring) | ✓ WIRED | `test_typst_authors_through_pipeline_produces_native_array_of_dicts` — asserts THROUGH the pipeline, not by calling the formatter directly (per VALIDATION.md Wave-0 requirement, closing the double-formatting-trap risk) |
| `render()` precedence order | `typst_template_function["params"]` wins on collision | D-08 | ✓ WIRED | `test_explicit_template_function_params_win_on_colliding_key` + live `test_bug_e_explicit_title_wins_over_metadata` both pass |

### Behavioral Spot-Checks / Real-Compile Evidence (this verifier's own execution, not SUMMARY claims)

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test suite | `uv run python -m pytest` | 583 passed, 1 skipped, 0 failed | ✓ PASS — matches orchestrator's claimed count exactly |
| CONF-02 gate module | `uv run python -m pytest tests/test_package_only_config_gate.py -v` | 12/12 passed | ✓ PASS |
| Charged-ieee bundled samples | `uv run python -m pytest tests/test_examples_charged_ieee_gate.py -v` | 2/2 passed | ✓ PASS |
| Pre-fix-basis reproduction | Checked out parent-of-`8ceb521` into scratch worktree, ran the CURRENT (post-fix) test file unmodified against it | `assert 2 == 0` failure with `TypstError: file not found (searched at .../_template.typ)` — exactly BUG-A | ✓ PASS (proves the fixture is a real regression test) |
| Visual PDF confirmation | Built `tests/fixtures/package_only_config_gate` via `-b typstpdf`, opened the resulting PDF | Title, IEEE-style author block (name/department/organization/location/email all rendered from the `typst_authors` dict), Abstract/Index Terms sections — matches `charged-ieee` template conventions | ✓ PASS (discharges the VALIDATION.md "Manual-Only Verification" item — performed directly by this verifier rather than deferred) |
| Lint / types / format | `nix-shell -p ruff --run "ruff check ."`, `uv run mypy typsphinx/`, `uv run black --check .` | All clean | ✓ PASS |
| Dependency diff | `git diff 62c3e03..HEAD -- pyproject.toml uv.lock` | Empty | ✓ PASS |

### Anti-Patterns Found

None. `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` across all 4 phase-touched `typsphinx/*.py` files
returns zero hits.

### Known Open Items (from the verification brief — assessed, not gaps)

1. **No `visit_citation`/`visit_label` translator handler.** Plans 22.2-02 and 22.2-06 removed
   citation constructs from the bundled `charged-ieee` samples rather than adding support, with a
   clear top-of-file RST comment explaining why. There is no REQUIREMENTS.md entry for citation-node
   support, and this phase's scope (CONF-01/02/03) never claimed to add it — correctly out of scope,
   not a gap.
2. **`tests/test_examples_basic.py` NixOS-worktree environmental failure.** Confirmed via direct
   execution: passes 15/15 on this (main-tree) environment, matching the environment notes'
   expectation. Correctly logged in `deferred-items.md` as a pre-existing, unrelated, worktree-only
   issue — not a regression introduced by this phase.
3. **`uv sync --extra dev --extra docs` vs `--extra dev`** — a documentation-completeness nit for
   CLAUDE.md's Commands section, unrelated to this phase's requirements; not a CONF-01/02/03 gap.
4. **`docs/locale/ja/LC_MESSAGES/examples/advanced.po`** still has a stale `modern-cv` msgid/msgstr
   pair (translation catalog, not the English source `advanced.rst` SC#3 explicitly names). Minor,
   informational only — does not block the phase goal.
5. **`.planning/STATE.md`** is stale (still shows "Plan 1 of 6", "0/6 plans (0%)") despite ROADMAP.md
   correctly showing 6/6 plans complete with all boxes checked. This is the known GSD tooling gap
   (plan-phase §13b does not update STATE.md) documented in the user's own memory notes — not a
   codebase/goal-achievement issue.

None of the five items above rise to a gap against CONF-01/02/03 or the five ROADMAP success criteria.

### Human Verification Required

None. The one item VALIDATION.md flagged as manual-only (visual confirmation of the two-column IEEE
layout and author-block rendering) was performed directly by this verifier by building the fixture and
reading the resulting PDF — see the Behavioral Spot-Checks table above. No open items remain.

### Gaps Summary

No gaps. All 5 ROADMAP success criteria for Phase 22.2 are verified against the actual codebase (not
SUMMARY.md claims): `typst_output_dir`/`typst_author_params` are fully removed from every named
surface, the `typst_package`-alone path builds and compiles for real (independently confirmed to fail
against the pre-fix code by this verifier, not merely asserted), the bundled samples and docs are
corrected, a config→output regression fixture closes the CONF-03 escape, and the milestone invariants
(no new deps, no `@preview` bump, version-sync surface untouched) hold. CONF-01/02/03 are genuinely
satisfied despite their unchecked REQUIREMENTS.md boxes — that checkbox state is stale orchestrator
bookkeeping, not a defect.

---

_Verified: 2026-07-21T22:01:44Z_
_Verifier: Claude (gsd-verifier)_
