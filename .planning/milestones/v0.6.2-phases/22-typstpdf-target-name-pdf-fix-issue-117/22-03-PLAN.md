---
phase: 22-typstpdf-target-name-pdf-fix-issue-117
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - tests/test_corpus_gate.py
  - tests/fixtures/cross_doc_label_namespace_render_gate/conf.py
  - tests/test_cross_doc_label_namespace_render_gate.py
  - tests/test_examples_basic.py
  - examples/advanced/README.md
  - examples/charged-ieee/README.md
  - docs/source/user_guide/builders.rst
  - docs/source/user_guide/configuration.rst
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PDF-01

must_haves:
  truths:
    - "tests/test_corpus_gate.py's GATE-02 render assertion targets the corpus's configured target name rather than its source docname, so the slow/network corpus gate stays green after the fix instead of failing on a missing artifact (D-12)."
    - "tests/fixtures/cross_doc_label_namespace_render_gate/conf.py declares a target name that differs from its master docname, so the existing cross-document label-namespacing gate now also exercises a renamed master."
    - "That gate still compiles with no 'occurs multiple times' and no 'does not exist in the document' diagnostic — the automated, real-compile proof that leaving get_target_uri docname-based does not break cross-document references into or out of a renamed master."
    - "docs/source/user_guide/builders.rst no longer presents a non-identity typst_documents example next to CLI walkthroughs that assume docname-named outputs; the page states which filenames each configuration actually produces."
    - "docs/source/user_guide/configuration.rst states that typst_documents element 2 governs BOTH the emitted .typ and the compiled .pdf filename, that only a literal trailing '.typ' is stripped, and that a path component is not supported."
    - "ROADMAP.md Phase 22 SC#2 no longer asserts that a correct .typ filename mapping already existed — the corrected wording records that doc_tuple[1] was read nowhere and that per D-01 this phase fixes both filenames."
    - "ROADMAP.md Phase 23 SC#2 records the D-09 hand-off: the [0.6.2] CHANGELOG entry must call out the output-filename change as user-visible, not bury it as an internal fix."
    - "The five modules named by D-11 (test_builder.py, test_builder_requirement13.py, test_integration_nested_toctree.py, test_config_template_mapping.py, test_config_toctree_defaults.py) are NOT edited — RESEARCH.md measured that none consume tests/roots/test-basic, so D-11 resolves to a regression guard rather than an assertion update."
    - "tests/test_examples_basic.py asserts the target-derived artifact for examples/basic (whose typst_documents target is 'basic-example.typ'), not the source-docname-derived one — the D-11 species finding that the post-merge gate caught, because both CONTEXT.md and RESEARCH.md scoped their blast-radius sweep to tests/ + docs/ and never looked at examples/."
    - "The three example projects whose emitted filenames move (examples/basic -> basic-example, examples/advanced -> advanced-example, examples/charged-ieee approach1+2 -> paper) have READMEs that name the artifacts the builders actually produce."
  artifacts:
    - "tests/test_corpus_gate.py — updated GATE-02 PDF assertion, failure message, comment block and method docstring"
    - "tests/fixtures/cross_doc_label_namespace_render_gate/conf.py — non-identity typst_documents target"
    - "tests/test_cross_doc_label_namespace_render_gate.py — updated PDF-artifact assertion matching the fixture's new target, plus an absence assertion for the docname-named PDF"
    - "tests/test_examples_basic.py — updated .typ artifact assertions matching examples/basic's declared target name"
    - "examples/advanced/README.md, examples/charged-ieee/README.md — corrected output-artifact filenames"
  key_links:
    - "wire_typsphinx_into_corpus_conf's injected typst_documents target -> TypstBuilder._resolve_output_stem -> the corpus gate's asserted PDF path"
    - "cross_doc_label_namespace_render_gate/conf.py target -> renamed master .typ -> typst.compile() -> the namespaced cross-doc labels emitted by translator.py:_namespace_label, which remain docname-keyed"
  prohibitions:
    - "tests/test_builder.py, tests/test_builder_requirement13.py, tests/test_integration_nested_toctree.py, tests/test_config_template_mapping.py and tests/test_config_toctree_defaults.py must not be modified — they are the unmodified-and-green regression guard (D-11)."
    - "The ~60 identity-mapping typst_documents entries elsewhere under tests/fixtures/ must not be changed; only the cross_doc_label_namespace_render_gate fixture becomes non-identity."
    - "docs/configuration.rst (the top-level, non-toctree'd copy) must not be edited — its line 43 contract becomes TRUE with this fix, so there is nothing to correct."
    - "typsphinx/ source files must not be modified by this plan; all fixes land in tests, docs, examples prose, and the roadmap."
    - "The conf.py files under examples/ must not be changed — examples/basic ('basic-example.typ'), examples/advanced ('advanced-example.typ') and examples/charged-ieee approach1+2 ('paper') already declare the target names their READMEs should describe. The code was wrong, not the configs; editing a conf.py to match a stale assertion would re-encode the bug."
    - "ROADMAP.md must be edited with scoped replacements only — never rewritten wholesale, which would destroy phase entries outside the diff window."
---

<objective>
Close the three consumer surfaces the fix moves, and correct the two documents that assert something
false about it.

Purpose: the fix in Plan 01 changes real emitted filenames in three places outside the new gate —
the slow corpus gate (which currently passes *because of* the bug), the docs dogfood build, and any
prose that hardcodes docname-named artifacts. RESEARCH.md also found that no existing fixture pairs
`target != docname` with a cross-document reference, so the `get_target_uri`-stays-docname-based
decision has no automated proof; this plan gives it one at near-zero cost by making one existing
multi-doc fixture non-identity.

Output: an updated corpus gate, a cross-document render gate that now also covers a renamed master,
corrected user-guide prose, and a ROADMAP whose Phase 22 SC#2 stops asserting a false premise and
whose Phase 23 SC#2 carries the D-09 CHANGELOG hand-off.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-typstpdf-target-name-pdf-fix-issue-117/22-CONTEXT.md
@.planning/phases/22-typstpdf-target-name-pdf-fix-issue-117/22-RESEARCH.md
@.planning/phases/22-typstpdf-target-name-pdf-fix-issue-117/22-01-SUMMARY.md
@CLAUDE.md

**Worktree provisioning (MANDATORY, per CLAUDE.md).** Run
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` first, then run every command
through `uv run`.

**Planner decisions recorded here (RESEARCH.md left both open):**

- *Open question 1 — `docs/source/user_guide/builders.rst`.* **Fix it in this phase.** The page is
  self-contradictory today and becomes actively misleading once the fix ships: it shows a
  non-identity `typst_documents` example and then walks the reader through opening docname-named
  artifacts. The phase already crosses the "strictly builder.py + tests" boundary via D-12, the fix
  is prose-only, and deferring it means shipping a doc that tells users the wrong filename. Task 3
  also extends `docs/source/user_guide/configuration.rst`, the *built* configuration reference, for
  the same reason. `docs/configuration.rst` (top-level, not reachable from `docs/source/index.rst`'s
  toctree) is deliberately left alone: its line 43 already documents element 2 as the output `.typ`
  filename, which this fix makes true.
**ORCHESTRATOR DEVIATION added after plan-checker approval (Wave 1 post-merge gate).** Task 3 is new.
The post-merge fast-suite run on the merged Wave 1 code turned two tests in
`tests/test_examples_basic.py` RED. Root cause: `examples/basic/conf.py` declares
`("index", "basic-example.typ", ...)` — a **fourth** `target != docname` entry that neither
CONTEXT.md's "all 62 entries enumerated" blast-radius table nor RESEARCH.md's sweep found, because
both scoped themselves to `tests/` + `docs/` and never looked under `examples/`. `examples/advanced`
(`advanced-example.typ`) and `examples/charged-ieee` approach1+2 (`paper`) are also non-identity, but
have no tests — only stale README prose. The two RED tests encode the pre-fix filename, so this is
the D-11 species and belongs to this plan's "close the consumer surfaces the fix moves" charter.
`examples/basic/README.md` already documents `basic-example.typ`, which independently confirms the
config was right and the builder was wrong. Task 3 runs BEFORE Task 4 because Task 4's `<automated>`
check includes the full fast suite, which cannot go green until Task 3 lands.

- *Open question 2 — cross-document-reference-into-renamed-master coverage.* **Automate it.** This
  project's standing GATE-01 bar is a real `typst.compile()` proof, not reasoning, and the
  `get_target_uri` non-regression argument currently rests on code reading alone. Task 2 takes
  RESEARCH.md's cheapest route: flip one existing multi-doc + cross-`:ref:` fixture to a non-identity
  target rather than building a new project.
</context>

## Artifacts this phase produces

New symbols created by THIS phase (do not treat as drift):

| Symbol | Kind | Location | Plan |
|---|---|---|---|
| `TypstBuilder._resolve_output_stem` | private method | `typsphinx/builder.py` | 22-01 |
| `tests/test_builder_output_stem.py` (+ its 18 `test_resolve_output_stem_*` functions) | test module | `tests/` | 22-01 |
| `tests/test_target_name_render_gate.py` (+ `target_name_render_gate_source_dir`, `TestTargetNameRenderGate`, `test_typstpdf_emits_target_named_artifacts_and_not_docname_named`) | test module | `tests/` | 22-02 |

This plan creates no new symbols — it edits existing files only.

<tasks>

<task type="auto">
  <name>Task 1: Move the corpus gate's PDF assertion off the docname-named artifact (D-12 companion)</name>

  <files>tests/test_corpus_gate.py</files>

  <read_first>
    - `tests/test_corpus_gate.py` lines 130-155 — `wire_typsphinx_into_corpus_conf`, which appends `typst_documents = [('index', 'sphinx-corpus', ...)]` to the cloned corpus's `conf.py` as an f-string at test runtime. This is the third `target != docname` entry in the repo; CONTEXT.md's static-grep blast-radius table missed it. Confirm the injected target string yourself before editing the assertion — the assertion must match whatever `wire_typsphinx_into_corpus_conf` actually writes.
    - `tests/test_corpus_gate.py` lines 280-345 — `TestCorpusRenderGate.test_corpus_compiles_with_no_fatal_error`: its method docstring (~line 287), the SC#1 comment block (~lines 324-329, which currently documents the docname-based naming as intentional and cites stale `builder.py:544/560` line numbers), the `pdf_path` assignment (~line 330), the failure message (~line 332), and the size + magic-bytes tail.
    - `typsphinx/builder.py` — `_resolve_output_stem` from Plan 01, to confirm how `'sphinx-corpus'` (extension-less) resolves.
    - `.planning/phases/22-typstpdf-target-name-pdf-fix-issue-117/22-RESEARCH.md` § "Common Pitfalls" Pitfall 2 — the full account of why this assertion is passing by bug today.
  </read_first>

  <action>
This test currently passes *because of* the bug this phase fixes: the corpus's injected
`typst_documents` target is `'sphinx-corpus'` while the asserted artifact is named after the source
docname. Post-fix the compiled artifact moves to the target-derived name, and this `@pytest.mark.slow`
network-dependent test would break silently — nobody runs it in the fast loop.

Change `pdf_path` (around line 330) to point at the artifact named after the target that
`wire_typsphinx_into_corpus_conf` injects — read that function first and use its literal target
value, appending the `.pdf` suffix. Update the assertion's failure message on the following line so
it names the same artifact it is now looking for; the message must stay actionable (it should still
mention checking that the `typst_documents` wiring landed and that no fatal `TypstCompilationError`
occurred, with `result.stdout` / `result.stderr` interpolated). Leave the `st_size > 0` and
PDF-magic-bytes assertions structurally unchanged — only the path variable feeds them.

Rewrite the SC#1 comment block above the assignment (around lines 324-329). Its current text
documents docname-based naming as the intended, permanent behavior and cites `builder.py:544/560`,
which are stale line numbers. Replace it with text that states the post-Phase-22 contract: the
emitted artifact is named from the `typst_documents` target via
`TypstBuilder._resolve_output_stem`, and its existence — not `returncode == 0` alone — is the
GATE-02 pass/fail signal, because `TypstPDFBuilder.finish()` logs a fatal
`TypstCompilationError` as an ERROR rather than raising it out of the subprocess. Do not cite
absolute line numbers in the replacement text; name the method instead, so the comment does not go
stale again.

Update the method docstring (around line 287) the same way — it currently promises a docname-named
compiled artifact.

Do not touch `wire_typsphinx_into_corpus_conf` itself, the corpus clone/caching machinery, the
`unknown_visit` catalogue assertions, or the `@pytest.mark.slow` marker. This is a
naming-alignment edit only.

<!-- planner-discipline-allow: index.pdf -->
  </action>

  <verify>
    <automated>uv run pytest tests/test_corpus_gate.py --collect-only -q && uv run black --check tests/test_corpus_gate.py && uv run ruff check tests/test_corpus_gate.py && test "$(grep -c 'index\.pdf' tests/test_corpus_gate.py)" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'index\.pdf' tests/test_corpus_gate.py` returns 0.
    - `grep -c 'sphinx-corpus.pdf' tests/test_corpus_gate.py` is at least 2 (the path assignment and the failure message).
    - `grep -c 'builder.py:544' tests/test_corpus_gate.py` returns 0 — the stale line-number citation is gone.
    - `grep -c '_resolve_output_stem' tests/test_corpus_gate.py` is at least 1 — the rewritten comment names the method instead of a line number.
    - `grep -c 'pytest.mark.slow' tests/test_corpus_gate.py` is unchanged from before the edit.
    - `git diff tests/test_corpus_gate.py | grep -c '^[-+].*def wire_typsphinx_into_corpus_conf'` returns 0 — the conf-injection helper was not modified.
    - `uv run pytest tests/test_corpus_gate.py --collect-only -q` exits 0.
    - `uv run black --check tests/test_corpus_gate.py` and `uv run ruff check tests/test_corpus_gate.py` both exit 0.
  </acceptance_criteria>

  <done>
    The corpus gate asserts the target-derived PDF artifact, its failure message and SC#1 comment
    describe the post-fix contract without stale line numbers, and the docname-named artifact is
    referenced nowhere in the file. Full green requires the network + `-m slow` run listed in
    `<verification>`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Give the get_target_uri non-regression a real-compile proof via a renamed master in the cross-doc label fixture</name>

  <files>tests/fixtures/cross_doc_label_namespace_render_gate/conf.py, tests/test_cross_doc_label_namespace_render_gate.py</files>

  <read_first>
    - `tests/fixtures/cross_doc_label_namespace_render_gate/conf.py` — currently `typst_documents = [("index", "index", "Cross-Doc Namespace Gate", "Test Author")]`, an identity mapping. This is the cheapest existing multi-document + cross-`:ref:` fixture in the corpus.
    - `tests/fixtures/cross_doc_label_namespace_render_gate/index.rst`, `pagea.rst`, `pageb.rst` — the toctree master and the two children that carry the cross-document `:ref:` under test. `pagea` and `pageb` are NOT `typst_documents` entries, so per D-02 their emitted filenames do not move.
    - `tests/test_cross_doc_label_namespace_render_gate.py` — the whole file. Concerns (1) through (3) assert on `pagea.typ` / `pageb.typ` content and must remain untouched; concern (4) at lines 222-230 asserts the compiled master PDF exists, is non-empty, and starts with the PDF magic bytes.
    - `typsphinx/translator.py` lines 3200-3210 (`_namespace_label`) and 3240-3283 (`_resolve_xref_docname`) — the round-trip this task proves is intact. Read-only.
    - `typsphinx/builder.py` lines 142-153 — `get_target_uri`, which Plan 01 deliberately left docname-based. This task is the automated proof that decision is correct.
  </read_first>

  <action>
Make this existing gate double as the renamed-master cross-reference proof, without building a new
fixture project.

In `tests/fixtures/cross_doc_label_namespace_render_gate/conf.py`, change the `typst_documents`
entry's second element from the identity `"index"` to the non-identity value `"namespace-gate"`,
leaving the source name `"index"`, the title, and the author unchanged. Extend the module docstring
with a sentence explaining the second purpose the fixture now serves: its master's target name
differs from its docname, so a build of this project exercises a cross-document `:ref:` into and out
of a RENAMED master and would surface a Typst `label ... does not exist` fatal if `get_target_uri`
had been made target-name-aware.

In `tests/test_cross_doc_label_namespace_render_gate.py`, update concern (4) so `pdf_output` points
at `temp_build_dir / "namespace-gate.pdf"`, and update its failure message to name that artifact.
Add, in the same concern block, an assertion that `temp_build_dir / "index.pdf"` does NOT exist,
with a message stating that the master's compiled artifact must follow the fixture's
`typst_documents` target name (D-08 clean break, no compatibility shim). Leave the `st_size > 0` and
PDF-magic assertions structurally unchanged.

Also extend the module docstring's numbered concern list with a fifth item recording that this gate
now additionally proves the Phase 22 `get_target_uri` decision: labels stay namespaced by SOURCE
docname (`pagea:` / `pageb:` prefixes, asserted in concerns 1-3) even though the master's OUTPUT
filename is `namespace-gate`, and the compile is fatal-free — which is exactly what would break if
the docname-to-URI mapping had followed the write-path rename.

Do NOT touch concerns (1), (2) or (3): `pagea.typ` and `pageb.typ` are toctree-included children
with no `typst_documents` entry, so per D-02 their filenames and their emitted namespaced labels are
unchanged. The `<pagea:shared-topic>` / `<pageb:shared-topic>` / `pagea:local-a` assertions must
still pass verbatim — if any of them turns red, the fix has leaked the output filename into the
label namespace and Plan 01 must be corrected, not this test.
  </action>

  <verify>
    <automated>uv run pytest tests/test_cross_doc_label_namespace_render_gate.py -q && uv run black --check tests/test_cross_doc_label_namespace_render_gate.py tests/fixtures/cross_doc_label_namespace_render_gate/conf.py && uv run ruff check tests/test_cross_doc_label_namespace_render_gate.py tests/fixtures/cross_doc_label_namespace_render_gate/conf.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '"namespace-gate"' tests/fixtures/cross_doc_label_namespace_render_gate/conf.py` returns 1.
    - `grep -c 'namespace-gate.pdf' tests/test_cross_doc_label_namespace_render_gate.py` is at least 2.
    - `grep -c 'assert not (temp_build_dir / "index.pdf").exists()' tests/test_cross_doc_label_namespace_render_gate.py` returns 1.
    - `grep -c 'pagea:shared-topic' tests/test_cross_doc_label_namespace_render_gate.py` is unchanged from before the edit — the label-namespace assertions were not weakened.
    - `uv run pytest tests/test_cross_doc_label_namespace_render_gate.py -q` exits 0 with 1 passed (not skipped, on this project's local env where `typst` 0.15.0 is installed).
    - The test output contains no `occurs multiple times` and no `does not exist in the document` diagnostic — both are already asserted inside the test body and remain asserted.
    - `git diff --name-only` for this task lists exactly `tests/fixtures/cross_doc_label_namespace_render_gate/conf.py` and `tests/test_cross_doc_label_namespace_render_gate.py`.
    - `uv run black --check` and `uv run ruff check` on both files exit 0.
  </acceptance_criteria>

  <done>
    A real `typst.compile()` of a multi-document project whose master's target name differs from its
    docname succeeds, its cross-document `:ref:` still resolves to the correct source-docname-keyed
    label, and the compiled artifact carries the target-derived name while the docname-derived one is
    absent. The `get_target_uri`-stays-docname-based decision now has automated proof rather than
    code-reading alone.
  </done>
</task>

<task type="auto">
  <name>Task 3: Realign the examples/ consumer surface — the blast-radius entries CONTEXT.md and RESEARCH.md missed</name>

  <files>tests/test_examples_basic.py, examples/advanced/README.md, examples/charged-ieee/README.md</files>

  <read_first>
    - `examples/basic/conf.py` around line 30 — `typst_documents = [("index", "basic-example.typ", ...)]`. A non-identity target that neither CONTEXT.md's blast-radius table nor RESEARCH.md's sweep enumerated, because both scoped themselves to `tests/` + `docs/`.
    - `examples/basic/README.md` lines 30-57 — note that it ALREADY documents `_build/typst/basic-example.typ` as the output. The README was right and the builder was wrong; this is the proof that updating the assertion (not the config) is correct. Do not edit this file.
    - `tests/test_examples_basic.py` — the whole `TestBasicExampleBuild` class. `test_build_generates_typ_file` (docstring ~line 127, path ~line 144, assertion messages ~lines 145-146) and `test_generated_typ_is_valid` (path ~line 166) both build `examples/basic` with `-b typst` and then look for the source-docname-derived artifact. These two are RED on the merged Wave 1 code — the post-merge gate caught them.
    - `examples/advanced/conf.py` around line 36 — target `"advanced-example.typ"`.
    - `examples/advanced/README.md` lines 55-70 — the "output files" list naming the master artifact by source docname, and the sentence just below it describing which file carries the `#include()` directives. Line ~217 of the same file already shows the `('index', 'advanced-example.typ', ...)` config, so this page is self-contradictory in the same way `builders.rst` is.
    - `examples/charged-ieee/approach1/conf.py` and `approach2/conf.py` around line 15 — both declare the extension-less target `"paper"`.
    - `examples/charged-ieee/README.md` lines ~100-115 — two `typst compile` command examples naming the source-docname-derived `.typ`.
    - `typsphinx/builder.py` — `_resolve_output_stem` from Plan 01, to confirm how `"basic-example.typ"` (suffix present) and `"paper"` (extension-less) each resolve.
  </read_first>

  <action>
**Deviation on record.** This task did not exist when the plan-checker approved this plan. The
Wave 1 post-merge test gate turned `tests/test_examples_basic.py::TestBasicExampleBuild::test_build_generates_typ_file`
and `::test_generated_typ_is_valid` RED. Diagnosis: not a defect in Plan 01 and not a merge conflict
— `examples/basic/conf.py` is a **fourth** `target != docname` entry, and these two tests encode the
pre-fix (buggy) filename. This is exactly the D-11 species ("tests that encode the buggy behavior are
updated, not worked around"); the sweep that produced the blast-radius table never looked under
`examples/`.

**(a) `tests/test_examples_basic.py`.** In `test_build_generates_typ_file`, change the built-artifact
path from the source-docname-derived name to the target-derived `basic-example.typ`, and update both
assertion messages and the method docstring to name that artifact. In `test_generated_typ_is_valid`,
change its artifact path the same way. Additionally assert in `test_build_generates_typ_file` that
the source-docname-derived `.typ` is NOT present, with a message citing the D-08 clean break — that
half is what makes the test fail against the pre-fix builder rather than passing for the wrong
reason. Do not touch `TestBasicExampleStructure`, `TestBasicExampleConfiguration`,
`TestBasicExampleReadme`, or the `subprocess.run` invocation shape.

**(b) `examples/advanced/README.md`.** In the build-output list under the `-b typst` walkthrough,
change the master-document entry from the source-docname-derived `.typ` to `advanced-example.typ`,
and update the following sentence (which names the file carrying the `#include()` directives) to
match. Leave the `chapter1.typ` / `chapter2.typ` entries alone — they are toctree-included children
with no `typst_documents` entry, so per D-02 their filenames do not move. Leave the `#include(...)`
code sample further down the page unchanged for the same reason.

**(c) `examples/charged-ieee/README.md`.** Both `typst compile` examples name the source-docname-derived
`.typ`; both approaches declare the extension-less target `"paper"`, so the emitted file is
`paper.typ`. Update both command lines. Do not change the `typst_package` / template prose.

Do NOT edit any `conf.py` under `examples/`. Their target names are the intended configuration; the
builder was the thing that ignored them.
  </action>

  <verify>
    <automated>uv run pytest tests/test_examples_basic.py -q && uv run black --check tests/test_examples_basic.py && uv run ruff check tests/test_examples_basic.py</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_examples_basic.py -q` exits 0 with all tests passing (the two previously-RED tests included).
    - `grep -c 'basic-example.typ' tests/test_examples_basic.py` is at least 4 (two path assignments plus assertion messages/docstring).
    - `grep -c '"typst" / "index.typ"' tests/test_examples_basic.py` returns 0.
    - `tests/test_examples_basic.py` contains an assertion that the source-docname-derived `.typ` does not exist (grep for `should not` or `not .*exists()` in `test_build_generates_typ_file`).
    - `git status --porcelain examples/basic/conf.py examples/advanced/conf.py examples/charged-ieee/approach1/conf.py examples/charged-ieee/approach2/conf.py` produces no output — no example config was edited.
    - `git status --porcelain examples/basic/README.md` produces no output — it already documented the correct filename.
    - `grep -c 'advanced-example.typ' examples/advanced/README.md` is at least 3 (the pre-existing config sample plus the two corrected prose references).
    - `grep -c 'chapter1.typ' examples/advanced/README.md` is unchanged from before the edit — included children were not touched.
    - `grep -c 'paper.typ' examples/charged-ieee/README.md` returns 2.
    - `uv run black --check tests/test_examples_basic.py` and `uv run ruff check tests/test_examples_basic.py` both exit 0.
  </acceptance_criteria>

  <done>
    The `examples/` consumer surface matches the shipped behavior: the basic-example build test asserts
    the target-derived artifact in both directions, and the advanced and charged-ieee READMEs name the
    files their own `typst_documents` targets actually produce. No example `conf.py` was changed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Correct the user-guide prose, ROADMAP Phase 22 SC#2, and record the D-09 CHANGELOG hand-off</name>

  <files>docs/source/user_guide/builders.rst, docs/source/user_guide/configuration.rst, .planning/ROADMAP.md</files>

  <read_first>
    - `docs/source/user_guide/builders.rst` — the whole page. Note the `typst_documents` example under "Document Definitions" (around lines 104-112), which declares non-identity targets `"main"` and `"api-ref"`, versus the CLI walkthroughs at lines 61, 147 and 161, which hardcode docname-named artifacts. That contradiction is invisible today only because the pre-fix builder ignored the target name.
    - `docs/source/user_guide/configuration.rst` lines 20-41 — the `typst_documents` reference, whose element-2 description currently reads "Output filename (without extension)" with no statement about the `.pdf`, about suffix stripping, or about path components.
    - `docs/configuration.rst` lines 36-60 — the top-level copy, NOT reachable from `docs/source/index.rst`'s toctree. Read it to confirm the deliberate non-edit: its line 43 already says element 2 is the output `.typ` filename, which this fix makes true.
    - `.planning/ROADMAP.md` lines 233-249 — the Phase 22 entry, whose SC#2 asserts the fix should be "consistent with the already-correct `.typ` filename mapping".
    - `.planning/ROADMAP.md` lines 290-312 — the Phase 23 entry, whose SC#2 currently mentions the Issue #117 fix without flagging it as user-visible.
    - `.planning/phases/22-typstpdf-target-name-pdf-fix-issue-117/22-CONTEXT.md` § `<domain>` — the measurement that `doc_tuple[1]` is read nowhere, which is why SC#2's premise is false.
  </read_first>

  <action>
Three prose corrections. Every ROADMAP edit must be a scoped replacement of the specific lines
named — never a whole-file rewrite, which would destroy phase entries outside the diff window.

**(a) `docs/source/user_guide/builders.rst`.** Add a short note (three to four lines of prose,
no new code-block) immediately after the "Document Definitions" `typst_documents` example, stating
that the second tuple element is the output filename stem and that it governs both the emitted
`.typ` and the compiled `.pdf`; that with the example shown the builders therefore emit
`main.typ` / `main.pdf` and `api-ref.typ` / `api-ref.pdf`; and that the CLI walkthroughs elsewhere on
this page assume the default identity configuration, where the source name and the target name
agree. Do NOT change the three CLI code-blocks at lines 61, 147 and 161 — with an identity target
their artifact names are correct, and rewriting them would make the page inconsistent with the
quickstart. The whole fix is the added note.

**(b) `docs/source/user_guide/configuration.rst`.** Rewrite item 2 of the "Each tuple contains"
list (currently "Output filename (without extension)") to state four facts: it is the output
filename stem; it governs both the emitted `.typ` and, under the `typstpdf` builder, the compiled
`.pdf`; a literal trailing `.typ` is stripped if present, and nothing else is — so a stem containing
a period such as `v1.2-manual` is preserved intact; and a path component is not supported, with a
path-bearing value producing a build warning and the file being written under its basename next to
the source document. Keep the surrounding list numbering and the five-element example above it
intact.

**(c) `.planning/ROADMAP.md`.** Replace Phase 22's SC#2 line. Its current premise — "consistent with
the already-correct `.typ` filename mapping" — is factually wrong: `doc_tuple[1]` was read nowhere in
the package, so the `.typ` side was equally broken and no correct mapping existed. Replace it with a
criterion stating that `TypstPDFBuilder.finish()` and both `write_doc()` sites derive the output
filename from the `typst_documents` target element via one shared helper
(`TypstBuilder._resolve_output_stem`), so the compiled `.pdf` and the emitted `.typ` always agree —
and append a short parenthetical recording that the original wording was corrected in this phase
because it asserted a mapping that never existed, and that per D-01 the phase scope is both
filenames. Then replace Phase 23's SC#2 line to carry the D-09 hand-off explicitly: the curated
`## [0.6.2]` CHANGELOG entry must present the Issue #117 target-name fix as a **user-visible output
filename change** — users whose CI references a docname-named build artifact must be able to see it —
rather than burying it among internal fixes.

Leave `docs/configuration.rst` (the top-level copy) unedited. Record in the plan summary that this
was a deliberate non-change: it is not reachable from `docs/source/index.rst`'s toctree, and its
element-2 description becomes accurate rather than inaccurate once this phase ships.
  </action>

  <verify>
    <automated>uv run python -c "import pathlib,sys; t=pathlib.Path('.planning/ROADMAP.md').read_text(); sys.exit(0 if 'already-correct' not in t else 1)" && uv run sphinx-build -b html -q -W --keep-going docs/source /tmp/typsphinx-docs-lint-check 2>&1 | tail -5; uv run pytest -m "not slow" -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'already-correct' .planning/ROADMAP.md` returns 0.
    - `grep -c '_resolve_output_stem' .planning/ROADMAP.md` is at least 1 (the rewritten Phase 22 SC#2).
    - `grep -c 'user-visible' .planning/ROADMAP.md` is at least 1 (the rewritten Phase 23 SC#2 carrying the D-09 hand-off).
    - `grep -c '^### Phase ' .planning/ROADMAP.md` equals the count taken before the edit — no phase entry was lost to a wholesale rewrite.
    - `grep -c 'main.typ' docs/source/user_guide/builders.rst` is at least 1 and `grep -c 'api-ref' docs/source/user_guide/builders.rst` is at least 2 (the original example plus the new note).
    - `git diff docs/source/user_guide/builders.rst | grep -c '^-.*typst compile build/typst'` returns 0 — the three CLI code-blocks were not rewritten.
    - `grep -c 'v1.2-manual' docs/source/user_guide/configuration.rst` returns 1.
    - `git status --porcelain docs/configuration.rst` produces no output — the top-level copy is untouched.
    - `git diff --name-only` for this task lists exactly `docs/source/user_guide/builders.rst`, `docs/source/user_guide/configuration.rst` and `.planning/ROADMAP.md`.
    - `uv run pytest -m "not slow" -q` exits 0.
  </acceptance_criteria>

  <done>
    The built user guide states which filenames each `typst_documents` configuration produces and no
    longer contradicts itself; ROADMAP Phase 22 SC#2 describes the fix truthfully; ROADMAP Phase 23
    SC#2 carries the D-09 obligation that the `[0.6.2]` CHANGELOG present this as a user-visible
    output-filename change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream Sphinx corpus clone → `wire_typsphinx_into_corpus_conf`'s appended `conf.py` text | The slow corpus gate clones an external repository at a resolved tag and executes its `conf.py`. This boundary pre-dates this phase and is unchanged by it; the helper that performs the append is explicitly out of scope for Task 1. |
| documentation prose → user's `conf.py` | Wrong prose leads users to configure a target name and then look for the wrong artifact — a correctness/UX failure, not a security one. Listed because it is the actual risk this plan's Task 3 addresses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-06 | Repudiation | `tests/test_corpus_gate.py` GATE-02 assertion | medium | mitigate | The assertion currently passes *because of* the bug under fix, so post-fix it would fail on a missing artifact and — being `@pytest.mark.slow` and network-dependent — could be silently dismissed as an environment flake, destroying the gate's evidentiary value. Task 1 realigns the assertion AND rewrites its comment/docstring so the recorded contract matches what the builder does, with no stale line-number citations to go wrong again. |
| T-22-07 | Tampering | `tests/fixtures/cross_doc_label_namespace_render_gate/` label-namespacing assertions | medium | mitigate | Flipping the fixture to a non-identity target could mask a genuine label regression if the concern (1)-(3) assertions were relaxed to accommodate it. Task 2 forbids touching them and requires `grep -c 'pagea:shared-topic'` to be unchanged, so the namespacing proof is strengthened, never weakened. |
| T-22-08 | Information Disclosure | `.planning/ROADMAP.md` scoped edits | low | mitigate | A whole-file `Write` on ROADMAP.md would silently delete phase entries outside the diff window. Task 3 mandates scoped replacement and the acceptance criteria pin the `### Phase ` heading count before and after. |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages. RESEARCH.md § "Package Legitimacy Audit" records the gate as not applicable; no `pyproject.toml` dependency edit is in scope. |

ASVS level 1; block-on threshold is `high`. No threat here reaches `high`.
</threat_model>

<verification>
1. `uv run pytest tests/test_cross_doc_label_namespace_render_gate.py -q` — the renamed-master cross-reference proof.
2. `uv run pytest -m "not slow" -q` — the full fast suite, including the five D-11 regression modules, which must be green while remaining unmodified.
3. **Phase gate, network required:** `uv run pytest tests/test_corpus_gate.py -k TestCorpusRenderGate -m slow -q` must be green with the corpus cached at `~/.cache/typsphinx-corpus-gate`. Task 1's fast `<automated>` check is a source-assertion + collection check only; this is the run that actually proves D-12's corpus half.
4. **Phase gate, dogfood docs build:** `tox -e docs-pdf` must be green and must emit `docs/_build/pdf/typsphinx.typ` and `docs/_build/pdf/typsphinx.pdf` (not docname-named artifacts) — the D-12 verification for `docs/source/conf.py`'s extension-less target `"typsphinx"`. CI needs no change: `tox.ini` passes no filename and `.github/workflows/docs.yml` globs `docs/_build/pdf/*.pdf`.
5. `uv run pytest tests/test_examples_basic.py -q` — the examples/ consumer surface (Task 3), green in both directions.
6. `git status --porcelain examples/basic/conf.py examples/advanced/conf.py examples/charged-ieee/approach1/conf.py examples/charged-ieee/approach2/conf.py` must be empty — no example config was edited to accommodate a stale assertion.
7. `git status --porcelain tests/test_builder.py tests/test_builder_requirement13.py tests/test_integration_nested_toctree.py tests/test_config_template_mapping.py tests/test_config_toctree_defaults.py` must be empty — the D-11 regression guard: RESEARCH.md measured that none of these consume `tests/roots/test-basic`, so they must pass UNMODIFIED. A red result there means Plan 01's D-02 fallback or identity-mapping normalization is wrong, not that these tests were.
</verification>

<success_criteria>
- The corpus gate asserts the target-derived artifact and is green under `-m slow` with network.
- `tox -e docs-pdf` emits `typsphinx.typ` / `typsphinx.pdf` (D-12).
- A real compile proves a cross-document `:ref:` into a renamed master still resolves.
- `tests/test_examples_basic.py` is green and asserts the target-derived artifact in both directions; the three non-identity example projects' READMEs name the files their configs actually produce, and no example `conf.py` was edited.
- The built user guide states the actual output filenames for a given `typst_documents` configuration.
- ROADMAP Phase 22 SC#2 is factually correct; Phase 23 SC#2 carries the D-09 CHANGELOG obligation.
- The five D-11 modules are unmodified and green.
</success_criteria>

<output>
Create `.planning/phases/22-typstpdf-target-name-pdf-fix-issue-117/22-03-SUMMARY.md` when done.
</output>
