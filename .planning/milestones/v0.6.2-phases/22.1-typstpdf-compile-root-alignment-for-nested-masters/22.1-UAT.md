---
status: complete
phase: 22.1-typstpdf-compile-root-alignment-for-nested-masters
source: [22.1-VERIFICATION.md]
started: 2026-07-21T16:03:03Z
updated: 2026-07-22T19:40:00Z
---

## Current Test

[testing complete]

## Tests

<!-- Round 1 (2026-07-21) — tests 1-3. Gap G-22.1-2 from test 2 was closed by plan 22.1-03. -->

### 1. Unplanned `typsphinx/writer.py` scope expansion

expected: Reviewer accepts the deviation as a genuine, same-failure-class, honestly-documented Rule-1
bug fix that is a structural prerequisite for the phase goal — or explicitly rejects it.

Verifier's mechanical findings (all confirmed, none in dispute):

- The bug is real and of the same class as PDF-02: `_write_template_file()` always writes
  `_template.typ` at the outdir root, so a nested master's bare `"_template.typ"` reference resolved
  to `outdir/api/_template.typ`, a file never written.
- Root-level masters are provably unaffected: `_compute_relative_include_path("_template", "index")`
  hits the `current_dir == "."` branch and returns `"_template"` unchanged, so the computed value is
  byte-identical to the old hardcoded `"_template.typ"`.
- The milestone `@preview` version-sync invariant is NOT violated: the diff is confined to
  `translate()`'s template-file computation; the `@preview` import declarations (writer.py lines
  92–100) are untouched, and `tests/test_preview_version_sync.py` passes 2/2.

What makes it a judgment call: it contradicts CONTEXT.md's locked scope statement ("All edits land in
`typsphinx/pdf.py` + `typsphinx/builder.py`") **and** Plan 02's own `<verification>` step 6, which
explicitly asserts `git status --porcelain typsphinx/writer.py` produces no output. The plan predicted
zero diff on this file; there is a diff.

why_human: Whether a correct, necessary fix that breaches a locked scope fence is acceptable is a
policy decision, not a mechanically verifiable fact.

result: pass
reviewer_note: |
  Accepted. During review the reviewer independently probed whether a nested master could resolve a
  root-level `_template.typ` at all. Empirically confirmed against the real build
  (`sphinx-build -b typstpdf tests/fixtures/nested_master_render_gate`): `_template.typ` stays at the
  outdir root, `outdir/api/index.typ` emits `#import "../_template.typ": project` alongside
  `include("usage.typ")` / `image("../logo.png")`, and `api/index.pdf` is produced. With
  `root_dir=self.outdir` unchanged, `../_template.typ` from `outdir/api/` resolves to
  `outdir/_template.typ` — inside the compile root — so no template duplication or relocation is
  needed at any nesting depth. This confirms the writer.py deviation is exactly the structural
  prerequisite claimed: without it the pdf.py/builder.py fix alone leaves nested masters unable to
  resolve their template.

### 2. SC#2 failure-mode drift — is the pre-fix proof still adequate?

expected: Reviewer accepts that the standing pre-fix-basis test failing on a Typst root-boundary
rejection of `../_template.typ` (rather than the literal sibling-include "file not found") still
discharges SC#2's intent — or requires an additional test that proves the include/image basis
divergence in isolation.

The substantive gap the verifier identified: `test_outdir_root_compile_basis_still_fails` now trips on
the companion `../_template.typ` reference **before** ever reaching the sibling `include("usage.typ")`
/ `image("../logo.png")` that PDF-02 canonically names. The failure is real, non-vacuous, and the error
was transcribed verbatim — but **no standing test independently proves the sibling-include/image
failure mode broke pre-fix and is now fixed via a red-then-green pair.** Test 1 exercises include/image
resolution only on the success path, post-fix.

ROADMAP SC#2 wording: "That fixture **fails** against the pre-fix code (the test demonstrably
reproduces the file-not-found break, not just passes vacuously)."

Note the assertion is also tolerantly worded — `"not found" in lowered or "escape" in lowered` — so it
would accept either failure class in future runs.

why_human: Whether a differently-shaped but equally-real failure discharges SC#2's intent is a
judgment call about evidentiary precision.

result: issue
reported: "追加テストを要求する"
severity: major

### 3. Six judgment-tier prohibitions across both plans

expected: Reviewer confirms the verifier's judgment-tier assessment (all 6 held, no violations found)
or overrides it.

All 6 `must_haves.prohibitions` (3 per plan) are descriptor-less prose statements with no explicit
verification tier, so they default to judgment tier and are NOT independently machine-checked beyond
the verifier's own reasoning.

why_human: Judgment-tier prohibitions require a non-authoritative LLM-judge verdict to be flagged for
human review per protocol — never silently passed.

result: pass

<!-- Round 2 (2026-07-22) — raised by the post-gap-closure re-verification. Both come from
     22.1-REVIEW.md, which ran for the first time in this pass. Neither breaks a ROADMAP SC. -->

### 4. Triage CR-01 — `writer.py` template-import path malformed for a docname containing a literal `_template` segment

expected: A human decides whether this must be fixed before Phase 22.1 / v0.6.2 closes, or is
acceptable to file as a tracked follow-up backlog item / phase.

`TypstWriter.translate()` (`typsphinx/writer.py:163-165`) computes the template import path by reusing
`TypstTranslator._compute_relative_include_path("_template", docname)`. When a master's docname
contains a directory segment literally named `_template` (e.g. docname `_template/index`), the
relativization logic mistakes the fake `"_template"` sentinel target for a real shared path component
and returns `"."`, which becomes a malformed import (`"..typ"` / `"../.typ"`) instead of a reference to
the real `outdir/_template.typ`.

Reproduced independently **twice**: once by the code reviewer against the installed package, and again
by the verifier directly against `typsphinx.translator.TypstTranslator._compute_relative_include_path`
(`fn(Dummy(), "_template", "_template/index") == "."`). It is not a false positive.

Why it is not a mechanical phase failure: no ROADMAP success criterion, plan must-have, or fixture in
this phase uses a docname containing `_template` — the gate fixture uses `api/index`. The bug is
nonetheless in code **this phase introduced**, is untested, and is tracked by no backlog item.

why_human: Whether an untriaged critical-severity code-review finding, in code this phase introduced,
blocks phase closure is a policy decision, not a mechanically verifiable fact.

result: issue
reported: "今直す"
severity: blocker

### 5. Confirm WR-01 / WR-02 as accepted residual risk

expected: Reviewer confirms both are acceptable lower-severity residual risk to track as backlog
items rather than phase blockers — or requires follow-up action now.

- **WR-01** (`typsphinx/builder.py`): D-04's "a build can no longer succeed while silently producing
  no PDF for a broken master" claim is incomplete — the pre-existing missing-`.typ`-file branch does
  not feed the new `failures` / `ExtensionError` tracking, so `finish()` can still exit 0 while
  silently omitting a configured master's PDF.
- **WR-02** (`tests/test_nested_master_render_gate.py`): the SC#2 / G-22.1-2 tests assert on literal
  `typst-py` error-message substrings (`"escape"`, `"not found"`, `"usage.typ"`, `"_template.typ"`),
  which are not a contractual API. A `typst-py` wording change could break them for reasons unrelated
  to typsphinx. Currently passing against typst-py 0.15.0 — forward-looking risk only.

why_human: Both are judgment calls about acceptable residual risk versus required immediate
remediation; neither breaks a ROADMAP success criterion or plan must-have.

result: skipped
reason: "Deferred follow-up: バックログへ"

## Deferred Follow-Ups

<!-- Captured per #1921: deferred follow-ups are NOT gaps and never spawn a fix plan. -->

- test: 5
  idea: "バックログへ"
  deferred_at: 2026-07-22
  covers: [WR-01, WR-02]
  backlog: ".planning/todos/pending/2026-07-22-wr01-silent-missing-pdf-wr02-typst-error-substring-coupling.md"

## Summary

total: 5
passed: 2
issues: 2
pending: 0
skipped: 1
blocked: 0

## Gaps

- gap_id: G-22.1-2
  truth: "A standing test independently proves the sibling-`include()`/`image()` failure mode PDF-02 names: it breaks on the pre-fix compile basis and passes post-fix, as a red-then-green pair — not merely as a side effect of the `../_template.typ` root-boundary rejection."
  status: resolved
  resolved_by: "22.1-03 (plan 03, gap_closure) — tests/test_nested_master_render_gate.py now carries test_sibling_include_fails_at_outdir_root_and_resolves_in_place (red+green pair isolating the include/image basis), and test_outdir_root_compile_basis_still_fails was tightened to a single pinned root-escape assertion. Module collects 4 tests, all passing."
  reason: "User reported: 追加テストを要求する — the reviewer declined to accept the `../_template.typ` root-boundary rejection as sufficient discharge of SC#2 and requires the include/image basis divergence proven in isolation."
  severity: major
  test: 2
  root_cause: >-
    The emitted nested master carries TWO independent outdir-root-relative references, and the
    earlier one masks the one PDF-02 names. `outdir/api/index.typ` line 7 is
    `#import "../_template.typ": project`; the sibling `include("usage.typ")` is line 33 and
    `image("../logo.png")` line 40. The pre-fix-basis reproduction copies the file verbatim to the
    outdir root and compiles with `root=outdir`, so `../_template.typ` now points ABOVE the compile
    root and Typst's root-boundary check aborts at line 7 — before the compiler ever reaches the
    include/image references. The test therefore proves the basis divergence via the template
    reference only; the include/image failure mode is never exercised in the red direction.
  artifacts:
    - path: "tests/test_nested_master_render_gate.py"
      issue: >-
        `test_outdir_root_compile_basis_still_fails` (line 235) copies the master verbatim, so the
        `../_template.typ` root escape short-circuits the reproduction. Its assertion is also
        tolerantly worded (`"not found" in lowered or "escape" in lowered`), so it would keep
        passing on either failure class and cannot detect the drift itself.
  missing:
    - >-
      A standing test that isolates the include/image basis divergence: copy the emitted
      `api/index.typ` to the outdir root, neutralize ONLY the template reference (rewrite
      `#import "../_template.typ"` to `#import "_template.typ"`, which is correct from the outdir
      root), then `typst.compile(copy, root=outdir)` and assert the failure names the sibling
      `usage.typ` specifically — asserting on the include/image target, not on a tolerant
      substring that either failure class would satisfy.
    - >-
      The green half of the pair: assert the same references resolve when the master is compiled at
      its real location `outdir/api/index.typ` (may reuse or extend the existing SC#1 test), so the
      red-then-green pair is explicit rather than implied.
  diagnosis_note: >-
    Remediation verified feasible during diagnosis, not merely hypothesized. Running the neutralized
    copy against the real build produced exactly the canonical PDF-02 error:
    `file not found (searched at <outdir>/usage.typ)` — the sibling-include break, with no template
    reference involved.

- gap_id: G-22.1-4
  truth: "`TypstWriter.translate()` emits a correct, resolvable `_template.typ` import for a master at ANY docname, including one whose directory portion is literally named `_template`."
  status: resolved
  resolved_by: "22.1-04 (plan 04, gap_closure) — typsphinx/writer.py now computes the import via TypstWriter._compute_template_import_path(docname), a staticmethod deriving the path purely from len(PurePosixPath(docname).parent.parts) upward segments, with no string-equality dependence on the reserved `_template` basename. Pinned by tests/test_template_import_path.py: a 7-case parametrized matrix (the 3 previously-broken `_template`-directory docnames plus the 4 already-correct anti-regression fence rows) and a real-compile render gate over tests/fixtures/template_named_dir_master/ (sphinx-build -b typst then typst.compile(master, root=outdir), asserting %PDF bytes). translator.py untouched; @preview declarations byte-identical; test_nested_master_render_gate.py still 4/4. Re-review of phase 22.1 reports 0 critical findings."
  reason: "User reported: 今直す — fix before Phase 22.1 / v0.6.2 closes rather than deferring to backlog. Source: CR-01 (critical) in 22.1-REVIEW.md."
  severity: blocker
  test: 4
  root_cause: >-
    `typsphinx/writer.py:163-165` computes the template import by calling
    `TypstTranslator._compute_relative_include_path("_template", docname)` — passing the literal
    string `"_template"` as a FAKE target docname. When the master's directory portion is itself
    named `_template`, that sentinel collides with a real path component and the helper's
    same-directory / common-parent logic resolves it as a path-to-itself, returning `"."` or a
    bare `"../"` chain with an EMPTY final component. `writer.py` then appends `".typ"` to that,
    producing a malformed import. Reproduced directly in this session against the installed
    package:
    `_template/index` -> `"."` -> `"..typ"`;
    `_template/sub/index` -> `"../"` -> `"../.typ"`;
    `_template/a/b/index` -> `"../../"` -> `"../../.typ"`.
    Non-`_template` docnames are unaffected (`index` -> `_template.typ`, `api/index` ->
    `../_template.typ`, `a/b/index` -> `../../_template.typ`, and even `a/_template/index` ->
    `../../_template.typ`, all correct) — which is why the `api/index` gate fixture never caught it.
  artifacts:
    - path: "typsphinx/writer.py"
      issue: >-
        Lines 163-165 reuse a docname-relativization helper with a synthetic sentinel target. The
        helper is correct for real docname pairs; the defect is the sentinel's namespace collision
        with a real directory named `_template`, which the helper cannot distinguish.
  missing:
    - >-
      Replace the sentinel-based computation with a direct depth calculation from the master's own
      directory to the outdir root, where `_write_template_file()` actually writes the file — e.g.
      `"../" * docname.count("/") + "_template.typ"`. Verified in this session to produce the
      correct result for every case above INCLUDING the three broken ones
      (`_template/index` -> `../_template.typ`, `_template/sub/index` -> `../../_template.typ`)
      while remaining byte-identical to today's output for `index`, `api/index`, `a/b/index` and
      `a/_template/index` — so it is a strict repair with no behavioural change on working paths.
    - >-
      A regression test pinning the `_template`-named-directory case, since no existing fixture or
      test exercises a docname whose directory portion is literally `_template`. Must assert the
      emitted import is `../_template.typ` (not `..typ`), and should cover at least the depth-1 and
      depth-2 forms.
  diagnosis_note: >-
    Root cause independently verified by the orchestrator in this session by executing
    `TypstTranslator._compute_relative_include_path` directly against the installed package, and the
    proposed depth-based remediation was executed against the same case matrix before being written
    here — neither is hypothesized. Originally surfaced as CR-01 by gsd-code-reviewer and
    independently re-reproduced by gsd-verifier, so this is a three-way confirmed finding.
