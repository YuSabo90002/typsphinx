---
phase: 22.1-typstpdf-compile-root-alignment-for-nested-masters
verified: 2026-07-22T20:00:00Z
status: passed
score: 5/5 ROADMAP success criteria verified; 38/38 plan-level must_haves.truths verified across all four plans (29 from plans 01-03 + 9 from plan 04 gap closure)
behavior_unverified: 0
overrides_applied: 0

re_verification:
  previous_status: human_needed
  previous_score: "5/5 ROADMAP success criteria (no caveats); 29/29 plan-level must_haves (plans 01-03); 2 open human_verification items (CR-01 triage, WR-01/WR-02 confirmation)"
  gaps_closed:
    - "G-22.1-4 / CR-01 (blocker): TypstWriter's template-import computation repaired. Independently re-verified in this pass: typsphinx/writer.py::TypstWriter._compute_template_import_path is a @staticmethod computing `len(PurePosixPath(docname).parent.parts)` upward '../' segments + '_template.typ' -- no string-equality dependence on the reserved '_template' basename. All 7 case-matrix rows reproduced directly against the installed package (fence rows byte-identical to pre-fix output; repaired rows now correct). tests/test_template_import_path.py: 9/9 passed on direct re-run (7-case parametrized matrix + depth-invariant restatement + real-compile render gate, run un-skipped since typst-py 0.15.0 is installed here)."
  gaps_remaining: []
  regressions: []
---

# Phase 22.1: typstpdf Compile-Root Alignment for Nested Masters Verification Report

**Phase Goal:** `-b typstpdf` and `-b typst` resolve relative paths on the same basis, so a master at a
nested docname (`api/index`) compiles to PDF with its `#include()`s and images intact. The minimal fix
creates the temporary file in the same directory as the master's own `.typ` (leaving `root` at outdir);
no output file moves.
**Verified:** 2026-07-22
**Status:** passed
**Re-verification:** Yes — after gap closure (Plan 04 / `G-22.1-4`, CR-01)

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

All 5 were VERIFIED in the prior pass with no caveats and are re-confirmed here by direct regression
re-run (not re-derived from scratch, per the re-verification optimization — no code in
`typsphinx/pdf.py` or `typsphinx/builder.py` changed since that pass; `typsphinx/writer.py` changed
only in the template-import computation, which is what plan 04 targets).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC#1 | Nested-master fixture (`api/index`) with sibling `#include()` + image compiles to PDF under `-b typstpdf` with zero Typst errors, via real `typst.compile()` | ✓ VERIFIED | `test_typstpdf_nested_master_resolves_include_and_image` — re-ran directly in this session, PASSED. |
| SC#2 | Fixture **fails** against pre-fix code (demonstrably reproduces the file-not-found break) | ✓ VERIFIED | `test_sibling_include_fails_at_outdir_root_and_resolves_in_place` — re-ran directly, PASSED. Gap `G-22.1-2` closure from plan 03 confirmed unchanged (`git diff c6d9b30 HEAD -- tests/test_nested_master_render_gate.py tests/fixtures/nested_master_render_gate/` produces no output — untouched by this gap-closure round). |
| SC#3 | `-b typstpdf` and `-b typst` + manual `typst compile` resolve identically — no basis divergence | ✓ VERIFIED | `test_typst_builder_output_compiles_manually` — re-ran directly, PASSED. |
| SC#4 | Output locations unchanged; root-level masters keep working exactly as before | ✓ VERIFIED | `typsphinx/pdf.py` / `typsphinx/builder.py` untouched by plan 04 (`git diff c6d9b30 HEAD --stat` — neither file appears). `index` docname's template-import path unchanged (`_template.typ`, confirmed by the anti-regression fence, `fence-root` row). |
| SC#5 | Zero new runtime deps, no `@preview` bump, 3-way version-sync surface untouched | ✓ VERIFIED | `git status --porcelain pyproject.toml` empty. `tests/test_preview_version_sync.py` re-ran 2/2 passed. `git diff c6d9b30 HEAD -- typsphinx/writer.py \| grep -A3 -B3 "codly\|mitex\|gentle-clues"` produces no output — the `@preview` import-declaration block is byte-identical across the gap-closure commits. |

**Score:** 5/5 ROADMAP success criteria verified — no caveats remaining.

### Gap Closure Verification (`G-22.1-4` / CR-01)

Independently re-derived, not taken from `22.1-04-SUMMARY.md`'s claims:

- **Read `typsphinx/writer.py` directly.** `TypstWriter._compute_template_import_path(docname)` is a
  `@staticmethod`: `depth = len(PurePosixPath(docname).parent.parts); return "".join(["../"] * depth) +
  "_template.typ"`. No call to `TypstTranslator._compute_relative_include_path` with a `"_template"`
  sentinel remains anywhere in the file (`grep -n '"_template"' typsphinx/writer.py` finds only the
  literal filename `_template.typ`, never the fake-docname sentinel). `translate()` calls it directly:
  `template_file = TypstWriter._compute_template_import_path(docname)`.
- **Reproduced all 7 case-matrix rows directly against the installed package** (not from the test file,
  a standalone `python -c` invocation): `index`→`_template.typ`, `api/index`→`../_template.typ`,
  `a/b/index`→`../../_template.typ`, `a/_template/index`→`../../_template.typ` (all four
  anti-regression-fence rows, byte-identical to documented pre-fix output), plus `_template/index`→
  `../_template.typ`, `_template/sub/index`→`../../_template.typ`, `_template/a/b/index`→
  `../../../_template.typ` (all three previously-broken rows, now correct). All 7 assertions passed.
- **`tests/test_template_import_path.py` re-run directly: 9/9 passed**, including the real-compile
  render gate (`TestTemplateNamedDirMasterRenderGate::test_template_named_dir_master_resolves_and_compiles`,
  run un-skipped since `typst-py` 0.15.0 is installed in this environment) — proves the outdir-root
  `_template.typ` file / `_template/` directory coexistence, depth-correct emitted `#import` lines at
  both depths, and real `typst.compile(master, root=outdir)` success (`%PDF` magic bytes) at both
  nesting depths.
- **`typsphinx/translator.py` untouched.** `git diff c6d9b30 HEAD -- typsphinx/translator.py` produces
  no output — confirmed empty, honoring `22.1-CONTEXT.md` line 44's "no `translator.py` change" fence.
- **`tests/test_nested_master_render_gate.py` and its fixture untouched and still 4/4.** `git diff
  c6d9b30 HEAD -- tests/test_nested_master_render_gate.py tests/fixtures/nested_master_render_gate/`
  produces no output; re-ran the module directly: `4 passed`. Plan 03's `G-22.1-2` closure is intact.
- **Scope fence honored.** `git diff c6d9b30 HEAD --stat` shows exactly the files the plan declared:
  `typsphinx/writer.py` (modified), `tests/test_template_import_path.py` (new),
  `tests/fixtures/template_named_dir_master/{conf.py,_template/index.rst,_template/sub/index.rst}`
  (new), plus phase-tracking docs (`ROADMAP.md`, `22.1-REVIEW.md`, `22.1-UAT.md`,
  `22.1-04-SUMMARY.md`). No other `typsphinx/` file appears in the diff.
- **No new error-substring coupling introduced.** `grep -in "error\|escape\|not found"
  tests/test_template_import_path.py` finds only docstring prose and `except ImportError` — no
  assertion pins a `typst-py` error-message substring, honoring the plan's WR-02-avoidance prohibition.
- **Regression, independently re-run:** `tests/test_preview_version_sync.py` 2/2 passed;
  `tests/test_nested_toctree_paths.py` 10/10 passed (Issue #5 relativization helper unaffected);
  `mypy typsphinx/` clean (6 source files); `black --check .` clean (153 files); `ruff check .` clean;
  full suite `uv run python -m pytest -q` → **560 passed, 1 skipped, 0 failed** (matches the
  orchestrator-reported gate result exactly).

**Conclusion: `G-22.1-4` / CR-01 is genuinely closed.** Every claim in `22.1-04-SUMMARY.md` was
independently reproduced against the actual code and test run in this session, not accepted on the
SUMMARY's word.

### Plan-Level `must_haves.truths` — Plan 04 (gap closure)

All 9 truths declared in `22.1-04-PLAN.md` frontmatter checked directly:

1. Depth-based computation with no sentinel string-equality dependence — VERIFIED (code read directly).
2. `_template/index` and `_template/sub/index` emit correct depth-1/depth-2 references — VERIFIED (case matrix + render gate, both re-run).
3. Anti-regression fence: 4 already-correct docnames byte-identical — VERIFIED (reproduced directly).
4. Standing parametrized unit test with fence/repaired labels — VERIFIED (9 tests, `ids=` visibly label `fence-*`/`repaired-*`).
5. Real-compile render gate proving both depths compile to valid PDF — VERIFIED (re-ran, un-skipped, PASSED).
6. Structural precondition (`_template.typ` file + `_template/` dir coexist) pinned — VERIFIED (read test assertions directly).
7. `translator.py` unchanged — VERIFIED (`git diff` empty).
8. Milestone invariant: zero new deps, `@preview` untouched, version-sync 2/2 — VERIFIED.
9. `test_nested_master_render_gate.py` still exactly 4 tests, all passing — VERIFIED (re-ran directly).

Plans 01-03's 29 previously-verified truths were spot-checked for regression only: full suite matches
the stated 560/1/0 baseline, `pdf.py`/`builder.py` untouched by this round, Phase 22 target-name gate
(`test_target_name_render_gate.py`) included in the green full-suite run. No regressions found.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `typsphinx/writer.py` | `_compute_template_import_path` staticmethod, depth-based | ✓ VERIFIED | Read directly; matches plan's `<behavior>` table exactly; `translate()` calls it. |
| `tests/test_template_import_path.py` | Case matrix + render gate | ✓ VERIFIED | 9/9 passed on direct re-run. |
| `tests/fixtures/template_named_dir_master/` | Two masters under a literal `_template` dir | ✓ VERIFIED | 3 files present, tracked by git, exercised by the render gate. |
| `typsphinx/translator.py` | Unchanged | ✓ VERIFIED | `git diff c6d9b30 HEAD` empty. |
| `typsphinx/pdf.py`, `typsphinx/builder.py` | Unchanged since prior verification | ✓ VERIFIED | Neither appears in `git diff c6d9b30 HEAD --stat`. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Master docname | Emitted `#import` line in `outdir/<docname>.typ` | `TypstWriter._compute_template_import_path(docname)` called in `translate()` | ✓ WIRED | Confirmed by direct code read and render-gate re-run. |
| `sphinx-build -b typst` on `template_named_dir_master` | `typst.compile(master, root=outdir)` → `%PDF` bytes | Render gate's real-compile assertions | ✓ WIRED | Re-ran directly, PASSED at both depths. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| New gap-closure module, all 9 tests | `uv run pytest tests/test_template_import_path.py -v` | `9 passed` | ✓ PASS |
| Plan-03 render gate, undisturbed | `uv run pytest tests/test_nested_master_render_gate.py -v` | `4 passed` | ✓ PASS |
| Version-sync invariant | `uv run pytest tests/test_preview_version_sync.py -q` | `2 passed` | ✓ PASS |
| Issue #5 relativization helper unaffected | `uv run pytest tests/test_nested_toctree_paths.py -q` | `10 passed` | ✓ PASS |
| Anti-regression fence + repair, standalone reproduction | `python -c "from typsphinx.writer import TypstWriter as W; ..."` (all 7 cases) | all assertions passed, printed confirmation | ✓ PASS |
| Full suite | `uv run python -m pytest -q` | `560 passed, 1 skipped` | ✓ PASS |
| Types | `uv run mypy typsphinx/` | `Success: no issues found in 6 source files` | ✓ PASS |
| Lint/format | `uv run black --check .` / `uv run ruff check .` | both clean | ✓ PASS |
| Scope fence | `git diff c6d9b30 HEAD --stat` | only declared files changed | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| PDF-02 | 22.1-01, 22.1-02, 22.1-03, 22.1-04 | `sphinx-build -b typstpdf` resolves `#include()`/`image()` on the master's own directory basis, matching `-b typst` + manual `typst compile` | ✓ SATISFIED | All 5 ROADMAP success criteria verified with no remaining caveats. `.planning/REQUIREMENTS.md:49` marks PDF-02 `[x]` complete, `:96` marks Phase 22.1 "Complete" — matches. No orphaned requirements. |

### Anti-Patterns Found

None blocking. `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER" typsphinx/writer.py
tests/test_template_import_path.py` finds nothing in the files this gap-closure round modified/created.

### Fresh Code Review Triage (`22.1-REVIEW.md`, post-gap-closure)

The fresh review (committed `e013f22`) found **0 critical, 3 warnings, 3 info**:

- **CR-01: verified fixed** by this re-verification pass (see Gap Closure Verification above) — no
  longer a finding.
- **WR-01** (missing-`.typ` branch doesn't feed `failures` tracking) and **WR-02** (typst-py
  error-substring coupling): both explicitly **deferred by the human** ("バックログへ") and tracked at
  `.planning/todos/pending/2026-07-22-wr01-silent-missing-pdf-wr02-typst-error-substring-coupling.md`
  (confirmed present on disk). Per the re-verification instructions, these are closed for this phase
  and are not re-raised as gaps or human-verification items.
- **WR-03** (new finding — `_resolve_output_stem`'s traversal guard doesn't neutralize a bare `".."`
  or `"."` target in `typsphinx/builder.py:190-219`): read the code directly and confirmed the finding
  is accurate (`is_guarded` checks `".." in segments` but not `"." in segments`, and the `".."` fallback
  computed via `path.basename("..")` is byte-identical to the rejected input, so the guard's own log
  message is misleading). **This does not affect any Phase 22.1 success criterion** — it concerns
  `typst_documents` target-name sanitization (a Phase 22 concern, `_resolve_output_stem`), not the
  compile-root/relative-path-basis alignment this phase's 5 SCs cover, and none of this phase's
  fixtures or plans touch `builder.py`. It is also not currently exploitable per the review's own
  analysis (every call site concatenates the suffix as a string rather than `path.join`-ing it). Noted
  here for traceability; not a phase blocker, not routed to human_verification, per this pass's stated
  high bar for raising new items.
- **IN-01/IN-02/IN-03**: pre-existing/cosmetic, unescalated by the reviewer, no phase-goal impact.

### Human Verification Required

None. Both previously open human-verification items are resolved:
1. **CR-01 triage** — human ruled "今直す" (fix now); the fix (plan 22.1-04) is independently verified
   correct and complete in this pass.
2. **WR-01/WR-02 residual-risk confirmation** — human deferred both to backlog
   (`.planning/todos/pending/2026-07-22-wr01-silent-missing-pdf-wr02-typst-error-substring-coupling.md`),
   closing the item for this phase.

WR-03 (new finding from the fresh review) does not affect any declared success criterion or must-have
and is not independently escalated to human verification (see Fresh Code Review Triage above).

### Gaps Summary

No must-have truth, artifact, or key link FAILED. Both gaps from prior verification rounds
(`G-22.1-2` and `G-22.1-4`) are genuinely closed, independently re-confirmed against the actual code
and a fresh local test run rather than accepted from SUMMARY.md claims. All 5 ROADMAP success criteria
verified with no caveats. The full test suite matches the stated 560-passed/1-skipped/0-failed
baseline exactly, and lint/format/type gates are clean. `typsphinx/translator.py` remains untouched
throughout the phase, honoring the CONTEXT.md scope fence. The milestone's zero-new-deps /
`@preview`-version-sync invariant holds. The one new finding from the fresh code review (WR-03) is a
pre-existing Phase-22-era robustness gap in output-name sanitization, unrelated to this phase's
compile-root-alignment goal and not currently exploitable — noted for traceability, not blocking.

---

_Verified: 2026-07-22_
_Verifier: Claude (gsd-verifier)_
