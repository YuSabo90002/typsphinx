---
phase: 22.1-typstpdf-compile-root-alignment-for-nested-masters
plan: 02
type: execute
wave: 2
depends_on:
  - 22.1-01
files_modified:
  - tests/fixtures/nested_master_render_gate/conf.py
  - tests/fixtures/nested_master_render_gate/logo.png
  - tests/fixtures/nested_master_render_gate/api/index.rst
  - tests/fixtures/nested_master_render_gate/api/usage.rst
  - tests/test_nested_master_render_gate.py
autonomous: true
requirements:
  - PDF-02

must_haves:
  truths:
    - "tests/fixtures/nested_master_render_gate/ is a self-contained Sphinx project whose only master is at the nested docname api/index, declared as typst_documents = [('api/index', 'index', ...)] — target name equal to the docname's basename, so the Phase 22 target-name rename is deliberately NOT exercised (D-07)."
    - "api/index.rst toctrees the sibling api/usage, so the emitted api/index.typ contains include(\"usage.typ\") — a sibling reference resolvable only from outdir/api/, not from the outdir root."
    - "api/index.rst references an image that lives at the project root, so the emitted api/index.typ contains image(\"../logo.png\") — a reference that crosses a directory boundary upward while staying inside outdir (D-07)."
    - "sphinx-build -b typstpdf on the fixture exits 0, writes outdir/api/index.pdf non-empty with the four-byte PDF magic prefix, and its stderr reports no missing-file compilation fatal (GATE-01, PDF-02 SC#1)."
    - "The same build copies the root-level image asset to outdir/logo.png, which is what makes the upward ../ reference resolvable."
    - "A standing test reproduces the PRE-FIX compile basis by copying the emitted api/index.typ to the outdir ROOT and compiling that copy with typst.compile(copy, root=outdir); it fails, and the raised error names a path under the outdir root rather than under outdir/api — the permanent, bidirectional proof that the gate is not passing vacuously (PDF-02 SC#2, D-08(a))."
    - "The real Typst error text produced by that pre-fix-basis reproduction, including the path Typst searched at, is transcribed verbatim into the test module docstring under a 'Confirmed both directions' record, matching every existing render gate's convention (D-08(a))."
    - "A separate sphinx-build -b typst on the same fixture emits outdir_typst/api/index.typ, and calling typst.compile(that path, root=outdir_typst) directly — exactly what a user running typst compile by hand does — returns bytes with the PDF magic prefix, pinning cross-builder equivalence (PDF-02 SC#3, D-09)."
    - "Sphinx is invoked as sys.executable -m sphinx via subprocess in every build, never as a resolved sphinx-build binary (NixOS sandbox PATH-shadowing hazard)."
    - "Every real-compile test in the module is guarded by @pytest.mark.skipif(not TYPST_AVAILABLE), matching tests/test_glob_image_render_gate.py."
    - "tests/fixtures/integration_nested_toctree/ and tests/roots/test-basic/ are not modified — the new gate is a dedicated fixture (D-06)."
  artifacts:
    - "tests/fixtures/nested_master_render_gate/conf.py — declares the single nested master and the extension"
    - "tests/fixtures/nested_master_render_gate/api/index.rst — the nested master: toctrees the sibling, references the root-level image"
    - "tests/fixtures/nested_master_render_gate/api/usage.rst — the sibling include target"
    - "tests/fixtures/nested_master_render_gate/logo.png — root-level image asset forcing an upward ../ reference"
    - "tests/test_nested_master_render_gate.py — the GATE-01 module: success gate, pre-fix-basis failure proof, cross-builder equivalence"
  key_links:
    - "conf.py typst_documents ('api/index','index') -> TypstPDFBuilder.write_doc -> outdir/api/index.typ -> finish() -> compile_typst_file_to_pdf(outdir/api/index.typ, root_dir=outdir) -> typst resolves include(\"usage.typ\") to outdir/api/usage.typ and image(\"../logo.png\") to outdir/logo.png -> outdir/api/index.pdf"
    - "TypstBuilder.post_process_images -> self.images -> copy_image_files() -> outdir/logo.png — without the copy, the upward image reference fails for a reason unrelated to PDF-02"
  prohibitions:
    - "MUST NOT report build success while a configured master's PDF is missing — the gate must assert the artifact exists and carries the PDF magic prefix, never merely that the process exited 0."
    - "MUST NOT discard the underlying compiler error text when proving the pre-fix direction — the transcribed docstring record must carry the real Typst message and the path it searched at, not a paraphrase, so a future reader can tell a genuine regression from an unrelated failure."
    - "MUST NOT weaken the gate into a string-agreement assertion on the emitted .typ. A real typst.compile() must run in both directions; asserting only that the .typ contains the expected include()/image() text would pass against the pre-fix builder and prove nothing (standing GATE-01 bar)."
---

<objective>
Add the GATE-01 real-compile regression gate for PDF-02: a dedicated fixture whose only master sits
at the nested docname `api/index`, proving `-b typstpdf` now resolves its sibling `#include()` and its
upward `image()` reference — and proving, permanently, that the pre-fix compile basis does not.

Purpose: ROADMAP Phase 22.1 SC#1 requires a nested-master fixture that compiles to PDF with zero
Typst errors via a real `typst.compile()`; SC#2 requires that the same fixture demonstrably fails
against the pre-fix basis rather than passing vacuously; SC#3 requires that `-b typst` plus a manual
`typst compile` resolves identically. This plan delivers all three in one module. The phase's whole
detection hole was that nothing in the corpus ever placed a master below the outdir root — this gate
closes it.

Output: `tests/fixtures/nested_master_render_gate/` and `tests/test_nested_master_render_gate.py`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-typstpdf-compile-root-alignment-for-nested-masters/22.1-CONTEXT.md
@.planning/phases/22.1-typstpdf-compile-root-alignment-for-nested-masters/22.1-RESEARCH.md
@.planning/phases/22.1-typstpdf-compile-root-alignment-for-nested-masters/22.1-PATTERNS.md
@.planning/phases/22.1-typstpdf-compile-root-alignment-for-nested-masters/22.1-01-SUMMARY.md
@CLAUDE.md

**Worktree provisioning (MANDATORY, per CLAUDE.md "Worktree-isolated execution").** Before running
anything, run:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

then run **every** subsequent command through `uv run` (e.g. `uv run pytest ...`). Without this the
worktree's pytest imports the UNCHANGED main-tree `typsphinx` package, so Plan 01's fix is invisible
and this gate stays red for the wrong reason.

**Exception to the `uv run` rule, inside the test code itself:** the Sphinx builds this gate drives
are spawned as `subprocess.run([sys.executable, "-m", "sphinx", ...])` — never `uv run sphinx-build`
and never a resolved `sphinx-build` binary. Under `uv run pytest`, `sys.executable` is already the
worktree venv's interpreter, so the subprocess imports the worktree's editable `typsphinx`. This is
the established convention in every existing render gate (NixOS sandbox PATH-shadowing hazard).

**Project conventions (CLAUDE.md):** Python 3.10+ compatibility required; ruff ignores `UP006`/`UP035`
— do not modernize typing imports. Line length 88 (black owns wrapping).
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Symbols and files that do **not** exist before this phase. Drift verification must treat these as
newly created, not as unresolved references to existing code.

**Plan 02 (this plan):**

| Kind | Name / path | Notes |
|------|-------------|-------|
| fixture dir | `tests/fixtures/nested_master_render_gate/` | new self-contained Sphinx project |
| fixture file | `tests/fixtures/nested_master_render_gate/conf.py` | `root_doc = "api/index"`, `typst_documents = [("api/index", "index", ...)]` |
| fixture file | `tests/fixtures/nested_master_render_gate/api/index.rst` | the nested master |
| fixture file | `tests/fixtures/nested_master_render_gate/api/usage.rst` | the sibling include target |
| fixture asset | `tests/fixtures/nested_master_render_gate/logo.png` | root-level PNG, copied from `tests/fixtures/glob_image_render_gate/_static/pic.png` |
| test module | `tests/test_nested_master_render_gate.py` | new GATE-01 module |
| pytest fixture | `nested_master_render_gate_dir` | module-local |
| pytest fixture | `temp_build_dir` | module-local (typstpdf output) |
| pytest fixture | `temp_build_dir_typst` | module-local (`-b typst` output, D-09) |
| helper | `_run_sphinx_build(source_dir, build_dir, builder)` | module-local |
| test class | `TestNestedMasterRenderGate` | |
| test | `test_typstpdf_nested_master_resolves_include_and_image` | SC#1 |
| test | `test_outdir_root_compile_basis_still_fails` | SC#2 / D-08(a) |
| test | `test_typst_builder_output_compiles_manually` | SC#3 / D-09 |

**Plan 01 (sibling plan, listed here so drift checks see the full phase surface):**
`typsphinx.pdf.compile_typst_file_to_pdf`; `from sphinx.errors import ExtensionError` in
`typsphinx/builder.py`; the aggregate message
`typstpdf: {N} master document(s) failed to compile to PDF: ...`;
`tests/test_pdf_generation.py::TestTypstPDFBuilder::test_finish_compiles_master_at_its_own_directory`.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions — cross-reference only (owned by Plan 01)

Both phase-level records live in `22.1-01-PLAN.md` and are **not** restated here. This section is a
pointer so a reader of Plan 02 alone knows why these tests exist and where the reasoning is kept.

- **Assumption-delta record:** the promote/demote decision for the compile basis (the master's own
  directory becomes primary; the outdir root is demoted to `root_dir`) is owned by
  `22.1-01-PLAN.md` → `<assumption_delta_decision>`. Read it there; it is not duplicated in this plan.

- **FA-01 (`category: concurrency`, status: unresolved)** is a phase-level flagged assumption against
  PDF-02, stated in full in `22.1-01-PLAN.md` → `<flagged_assumptions>`. What Plan 02 does and does
  not do about it:

  - **Exercised here (the guaranteed half).**
    `test_typstpdf_nested_master_resolves_include_and_image` asserts `result.returncode == 0` as a
    *primary* signal. That assertion is only meaningful because of D-04's attempt-all-then-raise
    change — under the pre-fix swallow a build that produced no PDF also exited 0. It is therefore the
    single-master instance of FA-01's guaranteed half ("every master attempted; masters that compile
    still get their `.pdf` written").
  - **NOT exercised here.** This fixture is deliberately single-master (D-07), so it does not test
    multi-master *sequencing*. The genuine multi-master assertions — every master attempted, one
    aggregated raise naming each failure, surviving masters' `.pdf`s still on disk — are Plan 01
    Task 3's `test_builder_attempts_every_master_then_raises` and
    `test_builder_still_writes_pdf_for_masters_that_compile`.
  - **Not closed by this plan.** FA-01's interruption gap (a SIGINT mid-`open(..., "wb")` can leave a
    truncated `.pdf`, undetected on the next build) and its concurrent-build gap (two builds sharing
    one `outdir`; no locking, last-writer-wins) stay **deferred**. Nothing in this plan adds atomic
    write-then-rename or an outdir lock, and no test here should be read as evidence for either.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Create the nested-master render-gate fixture project</name>

  <files>tests/fixtures/nested_master_render_gate/conf.py, tests/fixtures/nested_master_render_gate/logo.png, tests/fixtures/nested_master_render_gate/api/index.rst, tests/fixtures/nested_master_render_gate/api/usage.rst</files>

  <read_first>
    - `tests/fixtures/glob_image_render_gate/conf.py` — the canonical render-gate `conf.py` shape:
      `project` / `author` / `release`, `extensions = ["typsphinx"]`, an explanatory head comment
      naming the bug under test, and a `typst_documents` entry making the document a real master.
    - `tests/fixtures/glob_image_render_gate/index.rst` — how an existing gate fixture references an
      image and explains itself in prose.
    - `tests/fixtures/integration_nested_toctree/` (`conf.py`, `index.rst`, `chapter1/index.rst`,
      `chapter1/section1.rst`) — the existing nested-subdirectory + sibling-toctree layout this
      fixture is modeled on. **Do not modify it** (D-06).
    - `typsphinx/translator.py` `_compute_relative_include_path` (around line 2928) and
      `_compute_relative_image_path` (around line 3039) — confirm the emitted forms for
      `current_docname = "api/index"`: a toctree entry `usage` becomes `include("usage.typ")`, and a
      source-root-relative image URI `logo.png` becomes `image("../logo.png")`.
    - `typsphinx/builder.py` `copy_image_files()` (around line 558) — it copies `srcdir/<uri>` to
      `outdir/<uri>`, which is what puts the root-level image at `outdir/logo.png`.
  </read_first>

  <action>
Create a new, self-contained Sphinx project at `tests/fixtures/nested_master_render_gate/` with
exactly four files. It isolates the nested-master condition and nothing else (D-07).

**`conf.py`** — model the head comment on `tests/fixtures/glob_image_render_gate/conf.py`: explain
that this fixture reproduces the PDF-02 compile-root divergence, i.e. that `-b typstpdf` used to
compile a temporary copy of the master placed at the outdir root while the translator emits
docname-relative paths, so a master below the outdir root could not resolve its own siblings.
Contents:

- `project = "Nested Master Render Gate"`, `author = "Test Author"`, `release = "1.0.0"`.
- `extensions = ["typsphinx"]`.
- `root_doc = "api/index"` — making the nested document the project's root document is what keeps the
  fixture to a single master with no root-level document at all, so nothing in the project accidentally
  sits at the outdir root and masks the bug.
- `typst_documents = [("api/index", "index", "Nested Master Render Gate", "Test Author")]`.
  The target name `index` equals the docname's basename **on purpose** (D-07): the Phase 22
  target-name rename must NOT be exercised here, so that when this gate goes red the cause is
  unambiguously PDF-02. Add an inline comment saying exactly that.

With that entry, `_resolve_output_stem("api/index")` returns `index` and
`_directory_preserving_relpath("api/index", "index")` returns `api/index`, so the build writes
`outdir/api/index.typ` and `outdir/api/index.pdf`.

**`api/index.rst`** — the nested master. It needs a title, a short prose paragraph explaining what
the fixture proves, a `.. toctree::` whose single entry is the bare sibling name `usage`, and an
image directive referencing the project-root asset as a source-root-relative path: `.. image:: /logo.png`
(the leading `/` means "relative to the Sphinx source root", which Sphinx resolves to the URI
`logo.png`). Those two constructs are the whole point of the fixture:

- the toctree entry emits `#include("usage.typ")` — resolvable from `outdir/api/` only;
- the image emits `image("../logo.png")` — an upward directory crossing that still stays inside
  `outdir`, which is what proves `root=self.outdir` was not narrowed.

**`api/usage.rst`** — the sibling include target. A title plus one or two short paragraphs of plain
text; no directives, no images, nothing that could fail for its own reasons.

**`logo.png`** — copy `tests/fixtures/glob_image_render_gate/_static/pic.png` verbatim to
`tests/fixtures/nested_master_render_gate/logo.png` (it is an already-committed 68-byte real PNG, a
zero-new-risk precedent). Do not generate a new image and do not add an `html_static_path` setting;
the asset deliberately lives at the project root so the emitted reference must climb one level.

Keep every `.rst` body minimal — no admonitions, no code blocks, no math, no cross-references. Any
extra construct is another way for this gate to go red for a reason that is not PDF-02.

Do not touch `tests/fixtures/integration_nested_toctree/` or `tests/roots/test-basic/`.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run python -m sphinx -b typst tests/fixtures/nested_master_render_gate /tmp/nmrg-typ &amp;&amp; grep -q 'include("usage.typ")' /tmp/nmrg-typ/api/index.typ &amp;&amp; grep -q 'image("../logo.png"' /tmp/nmrg-typ/api/index.typ &amp;&amp; test -f /tmp/nmrg-typ/api/usage.typ &amp;&amp; test -f /tmp/nmrg-typ/logo.png &amp;&amp; echo FIXTURE_OK</automated>
  </verify>

  <acceptance_criteria>
    - Files exist: `tests/fixtures/nested_master_render_gate/conf.py`,
      `.../api/index.rst`, `.../api/usage.rst`, `.../logo.png`.
    - Source: `grep -c 'root_doc = "api/index"' tests/fixtures/nested_master_render_gate/conf.py`
      outputs `1`.
    - Source: `grep -c '("api/index", "index"' tests/fixtures/nested_master_render_gate/conf.py`
      outputs `1`.
    - Asset: `cmp tests/fixtures/nested_master_render_gate/logo.png tests/fixtures/glob_image_render_gate/_static/pic.png`
      exits 0 (byte-identical copy of the existing committed PNG).
    - CLI: `uv run python -m sphinx -b typst tests/fixtures/nested_master_render_gate /tmp/nmrg-typ`
      exits 0.
    - Behavior: `/tmp/nmrg-typ/api/index.typ` exists and contains both `include("usage.typ")` and
      `image("../logo.png"` — the two docname-relative references the gate depends on.
    - Behavior: `/tmp/nmrg-typ/api/usage.typ` and `/tmp/nmrg-typ/logo.png` both exist — the include
      target was written into the nested directory and the root-level asset was copied.
    - Behavior: `/tmp/nmrg-typ/index.typ` does NOT exist — the fixture has no root-level document
      that could mask the nested condition.
    - Unmodified: `git status --porcelain tests/fixtures/integration_nested_toctree tests/roots`
      produces no output.
  </acceptance_criteria>

  <done>
    A four-file Sphinx project exists at `tests/fixtures/nested_master_render_gate/` whose sole
    master is at docname `api/index` with target name `index`, whose emitted `api/index.typ` carries
    a sibling `include("usage.typ")` and an upward `image("../logo.png")`, and whose root-level PNG
    is copied into the output tree.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the GATE-01 render-gate module with the success gate, the pre-fix-basis failure proof, and the cross-builder equivalence check</name>

  <files>tests/test_nested_master_render_gate.py</files>

  <read_first>
    - `tests/test_glob_image_render_gate.py` — read in full (191 lines). This is the canonical
      GATE-01 template: the module docstring convention (root cause / fix / "Confirmed both
      directions"), the `try: import typst / TYPST_AVAILABLE` guard (lines 56-61), the
      `*_render_gate_dir` and `temp_build_dir` pytest fixtures (lines 64-73), the
      `subprocess.run([sys.executable, "-m", "sphinx", "-b", ...])` helper (lines 76-99), the
      `@pytest.mark.skipif(not TYPST_AVAILABLE)` class guard (lines 102-105), and the assertion
      shape — `returncode`, stderr substring guards, artifact existence, non-empty size, four-byte
      PDF magic (lines 135-191).
    - `tests/fixtures/nested_master_render_gate/` as created in Task 1.
    - `typsphinx/pdf.py` `compile_typst_file_to_pdf` and `typsphinx/builder.py`
      `TypstPDFBuilder.finish()` as delivered by Plan 01 — the behavior under test.
    - `.planning/phases/22.1-typstpdf-compile-root-alignment-for-nested-masters/22.1-CONTEXT.md`
      D-06 / D-07 / D-08 / D-09, and `22.1-RESEARCH.md` "GATE-01 render-gate skeleton (D-06)".
  </read_first>

  <action>
Create `tests/test_nested_master_render_gate.py`, modeled structurally on
`tests/test_glob_image_render_gate.py`.

**Module preamble.** Copy the import block and availability guard verbatim: `subprocess`, `sys`,
`shutil`, `from pathlib import Path`, `import pytest`, and the
`try: import typst / TYPST_AVAILABLE = True / except ImportError: TYPST_AVAILABLE = False` block.

**Pytest fixtures.** `nested_master_render_gate_dir` returning
`Path(__file__).parent / "fixtures" / "nested_master_render_gate"`; `temp_build_dir` returning
`tmp_path / "_build"`; `temp_build_dir_typst` returning `tmp_path / "_build_typst"` (a separate
output tree so the `-b typst` build in the D-09 test cannot be contaminated by the `-b typstpdf`
build's `.pdf` artifacts).

**Build helper.** `_run_sphinx_build(source_dir, build_dir, builder)` running
`subprocess.run([sys.executable, "-m", "sphinx", "-b", builder, str(source_dir), str(build_dir)],
capture_output=True, text=True)`. Parameterize the builder name rather than hard-coding `typstpdf`,
because this module drives both builders. Docstring it with the same `sys.executable -m sphinx`
rationale the existing gates carry.

**Test class** `TestNestedMasterRenderGate`, guarded with
`@pytest.mark.skipif(not TYPST_AVAILABLE, reason="typst-py is required for the nested-master render gate")`,
containing three tests:

1. `test_typstpdf_nested_master_resolves_include_and_image` (SC#1). Build the fixture with
   `-b typstpdf` into `temp_build_dir`. Assert: `result.returncode == 0` (now a trustworthy primary
   signal because Plan 01's D-04 change raises on compile failure — unlike the older gates, which
   predate it and could only trust artifacts); `"file not found" not in result.stderr`;
   `"Typst compilation failed" not in result.stderr`. Then assert the emitted
   `temp_build_dir/"api"/"index.typ"` exists, that `temp_build_dir/"api"/"usage.typ"` exists, and
   that `temp_build_dir/"logo.png"` exists (the copied asset that makes the upward reference
   resolvable). Finally assert `temp_build_dir/"api"/"index.pdf"` exists, has `st_size > 0`, and its
   first four bytes equal the ASCII magic prefix that opens a PDF file. Every assertion carries an
   f-string message including `result.stdout` / `result.stderr` so a failure is diagnosable, matching
   the existing gates.

2. `test_outdir_root_compile_basis_still_fails` (SC#2, D-08a). This is the bidirectional half, and it
   is a **standing** test rather than a one-time manual verification, so the gate can never start
   passing vacuously after a future refactor. Build the fixture with `-b typst` into
   `temp_build_dir_typst`, then reproduce the pre-fix compile basis exactly: `shutil.copy2` the
   emitted `api/index.typ` to a file at the **outdir root** of that same build tree (name it
   something that cannot collide with a real artifact, e.g. `_prefix_basis_copy.typ`), and call
   `typst.compile(str(that_copy), root=str(temp_build_dir_typst))` inside `pytest.raises(Exception)`.
   Assert the captured exception's string mentions a missing file and contains the outdir-root path
   segment rather than the `api/` segment for the sibling include — i.e. the compiler searched at the
   outdir root, which is precisely what the pre-fix `NamedTemporaryFile(dir=root_dir)` placement
   caused. Keep the assertion tolerant of Typst's exact wording (match on a lowercase-normalized
   substring such as `not found`), but DO capture and assert on the searched path, because that is
   the discriminating detail.

   **Then transcribe the real error.** Run this test once, take the actual exception text it produced
   (including the path Typst reports searching at) and paste it verbatim into the module docstring's
   "Confirmed both directions" record, exactly as
   `tests/test_glob_image_render_gate.py:42-47` does. A paraphrase is not acceptable — the point of
   the record is that a future reader can distinguish a genuine PDF-02 regression from an unrelated
   failure.

3. `test_typst_builder_output_compiles_manually` (SC#3, D-09). Build the fixture with `-b typst` into
   `temp_build_dir_typst`, assert `returncode == 0`, then call
   `typst.compile(str(temp_build_dir_typst / "api" / "index.typ"), root=str(temp_build_dir_typst))`
   directly — reproducing exactly what a user running `typst compile` by hand does — and assert the
   returned bytes start with the PDF magic prefix. Docstring it with the D-09 rationale: after the
   fix the two builders compile the same file at the same location, so equivalence is structural;
   this test pins that structural property so a future refactor reintroducing an intermediate copy is
   caught.

**Module docstring.** Follow the existing gates' convention and cover, in order: what this gate
proves (PDF-02 / Phase 22.1); the root cause (`TypstPDFBuilder.finish()` read the master into a
string, `compile_typst_to_pdf()` wrote it to a `NamedTemporaryFile(dir=root_dir)` at the outdir root,
and Typst resolves relative paths against the file being compiled while the translator emits them
docname-relative — the two bases coincide only for a master at the outdir root, which is why the
whole existing corpus passed); the fix (`compile_typst_file_to_pdf()` compiles the master's own `.typ`
at its real location, so the divergence is structurally impossible); why the fixture is shaped the
way it is (target name equals the docname's basename so the Phase 22 rename is not confounded); and
the "Confirmed both directions" record carrying the verbatim pre-fix Typst error from test 2. Also
state that the gate drives the full `-b typstpdf` path on purpose, and that `returncode == 0` is a
meaningful primary signal here only because Phase 22.1's D-04 change made compile failures raise.

Do not add this module to any `slow` marker — it must run in the default fast suite.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest tests/test_nested_master_render_gate.py -v</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `tests/test_nested_master_render_gate.py`.
    - Test: `uv run pytest tests/test_nested_master_render_gate.py -v` exits 0 and collects exactly
      3 tests, all passing (not skipped — typst-py 0.15.0 is installed in this environment).
    - Test: `uv run pytest "tests/test_nested_master_render_gate.py::TestNestedMasterRenderGate::test_typstpdf_nested_master_resolves_include_and_image" -q` exits 0.
    - Test: `uv run pytest "tests/test_nested_master_render_gate.py::TestNestedMasterRenderGate::test_outdir_root_compile_basis_still_fails" -q` exits 0 — the pre-fix basis still fails, so the success gate is not passing vacuously.
    - Test: `uv run pytest "tests/test_nested_master_render_gate.py::TestNestedMasterRenderGate::test_typst_builder_output_compiles_manually" -q` exits 0.
    - CLI: `uv run python -m sphinx -b typstpdf tests/fixtures/nested_master_render_gate /tmp/nmrg-pdf`
      exits 0 and `/tmp/nmrg-pdf/api/index.pdf` exists with `head -c 4 /tmp/nmrg-pdf/api/index.pdf`
      printing the four-byte PDF magic prefix.
    - Source: `grep -c '\[sys.executable' tests/test_nested_master_render_gate.py` outputs `1` — the
      single `subprocess.run` argv starts with the running interpreter.
    - Source: `grep -c '"sphinx-build"' tests/test_nested_master_render_gate.py` outputs `0` — no
      subprocess argv passes a resolved binary name. (The grep targets the quoted-argument form
      deliberately: the module's own prose may legitimately mention the command by name, and a
      file-wide unquoted grep would be self-invalidating.)
    - Source: `grep -c 'Confirmed both directions' tests/test_nested_master_render_gate.py` outputs
      `1`, and the module docstring contains a transcribed Typst error string carrying a searched
      path — verify by reading the docstring, not by grep alone.
    - Source: `grep -c 'skipif' tests/test_nested_master_render_gate.py` is at least `1`.
    - Full suite: `uv run pytest -m "not slow" -q` exits 0.
    - Lint/format: `uv run black --check tests/test_nested_master_render_gate.py` and
      `uv run ruff check tests/test_nested_master_render_gate.py` exit 0.
  </acceptance_criteria>

  <done>
    `tests/test_nested_master_render_gate.py` proves, with real `typst.compile()` runs in both
    directions, that a master at docname `api/index` compiles to `api/index.pdf` under `-b typstpdf`
    with its sibling include and upward image reference intact, that reproducing the pre-fix
    outdir-root compile basis still fails with a missing-file error naming the outdir-root path, and
    that the `-b typst` output compiles identically under a manual `typst.compile()`. The real
    pre-fix error is transcribed verbatim into the module docstring.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture `conf.py` → `sphinx-build` subprocess | The fixture's `conf.py` is executed as Python by every build this gate spawns. It is repo-committed test data authored in this plan, not user input. |
| fixture source tree (`srcdir`) → build output tree (`outdir`) | The build copies the fixture's root-level `logo.png` into `outdir`; the gate then reads artifacts back from `outdir` only. |
| build `outdir` → `typst.compile(input, root=outdir)` | Tests 2 and 3 call the compiler directly on files inside a pytest `tmp_path` build tree, with `root` bounding path resolution to that tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.1-07 | Tampering | the pre-fix-basis copy written into the build output tree by test 2 | low | mitigate | The copy is written into a pytest `tmp_path`-scoped build directory (never the repo, never a shared location) under a leading-underscore name (`_prefix_basis_copy.typ`) that cannot collide with any docname-derived artifact the builders emit, and the whole tree is discarded by pytest after the test. Task 2 names both constraints explicitly. |
| T-22.1-08 | Tampering | the gate's `image("../logo.png")` reference climbing out of the compile `root` | low | mitigate | The reference climbs exactly one level, from `outdir/api/` to `outdir/`, and `root` is `outdir` — so it stays strictly inside the boundary. This is a deliberate, bounded exercise of Typst's root-escape check: the gate proves the boundary stays open for a legitimate upward reference, and would go red if a future change narrowed `root` to the master's own directory. Do not deepen the traversal (`../../`), which would test the compiler's boundary rather than PDF-02. |
| T-22.1-09 | Denial of Service | subprocess Sphinx builds hanging or leaking processes in CI | low | accept | Each test spawns one short-lived `sys.executable -m sphinx` build over a four-file project with `capture_output=True`; this is the identical pattern every existing render gate in the suite already uses, at the same cost. No new timeout mechanism is introduced. |
| T-22.1-10 | Information Disclosure | absolute local build paths embedded in assertion failure messages and in the transcribed docstring record | low | accept | The transcription is required by D-08(a) and by the project's standing render-gate convention; the paths are `tmp_path`-scoped build directories from the author's own machine, carry no credential or personal data beyond a home-directory name, and are the discriminating detail that makes the record useful. Same posture as every existing gate's docstring. |
| T-22.1-SC | Tampering | npm/pip/cargo installs (supply chain) | high | accept | Zero packages installed by this plan — `pyproject.toml` is untouched (milestone invariant) and the only new binary artifact is a byte-identical copy of an already-committed 68-byte PNG from `tests/fixtures/glob_image_render_gate/_static/pic.png`, verified with `cmp` in the acceptance criteria. No `[ASSUMED]`/`[SUS]` package exists to gate on. |
</threat_model>

<verification>
Run from the worktree root, after `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`:

1. `uv run pytest tests/test_nested_master_render_gate.py -v` — exits 0, 3 tests passed, 0 skipped.
2. `uv run python -m sphinx -b typstpdf tests/fixtures/nested_master_render_gate /tmp/nmrg-pdf` — exits 0,
   and `/tmp/nmrg-pdf/api/index.pdf` exists and opens with the PDF magic prefix.
3. `uv run pytest -m "not slow" -q` — exits 0 (full fast suite, including Plan 01's changes).
4. `uv run pytest -m slow -q` — exits 0 (the full-corpus regression gate; Plan 01's D-04 change makes
   this gate more likely to surface a real failure, so run it at least once before phase close).
5. `uv run black --check . && uv run ruff check .` — both exit 0.
6. `git status --porcelain tests/fixtures/integration_nested_toctree tests/roots typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ pyproject.toml`
   produces no output.
</verification>

<success_criteria>
- `tests/fixtures/nested_master_render_gate/` exists with the four-file shape and its only master at
  docname `api/index` with target name `index`.
- `-b typstpdf` on the fixture exits 0 and produces `api/index.pdf` with the PDF magic prefix.
- The pre-fix compile basis (a copy of the master at the outdir root) still fails with a missing-file
  error naming an outdir-root path, and that real error is transcribed verbatim into the module
  docstring under "Confirmed both directions".
- `typst.compile()` run by hand on the `-b typst` output of the same fixture succeeds — no basis
  divergence remains between the two builders.
- Sphinx is invoked only as `sys.executable -m sphinx`; every real-compile test is `TYPST_AVAILABLE`-guarded.
- `tests/fixtures/integration_nested_toctree/` and `tests/roots/` are unmodified.
- Fast suite and slow corpus gate both green; black / ruff clean.
</success_criteria>

<output>
Create `.planning/phases/22.1-typstpdf-compile-root-alignment-for-nested-masters/22.1-02-SUMMARY.md` when done.
</output>
