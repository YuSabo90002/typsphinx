---
phase: 22.3-typstpdf-builder-warning-hardening
verified: 2026-07-22T13:33:55Z
status: human_needed
score: 24/25 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Exercise tests/test_missing_and_malformed_master_gate.py and tests/test_nested_master_render_gate.py under a real parallel test runner (e.g. `pytest-xdist -n auto`, once added as a dependency) to confirm no shared-state collision between the two GATE-01 fixtures under concurrent execution."
    expected: "Both gate modules pass unchanged under parallel execution; no artifact written by one test collides with another (per-test tmp_path isolation holds under real concurrency, not just by structural argument)."
    why_human: "22.3-03-PLAN.md's own must_haves marks this truth `verification: backstop` and its `<flagged_assumptions>` states pytest-xdist is not a project dependency, so the concurrency guarantee is asserted structurally (per-test tmp_path, read-only fixture source, leading-underscore artifact names) rather than exercised directly. Per the honest-verifier rule, a backstop truth that cannot be confirmed with explicit behavioral evidence must abstain to human_needed rather than being silently counted as passed. This is a minor, non-blocking item — it does not affect any ROADMAP SC#1–5 truth, all of which have direct evidence below."
---

# Phase 22.3: typstpdf Builder Warning Hardening Verification Report

**Phase Goal:** The two warnings the Phase 22.1 `/gsd-code-review` raised are closed.
`TypstPDFBuilder.finish()` no longer reports a successful build while silently emitting no PDF for a
configured master, and the nested-master render gate stops depending on `typst-py`'s uncontracted
error wording.
**Verified:** 2026-07-22T13:33:55Z
**Status:** human_needed (one non-blocking backstop item; all 5 ROADMAP success criteria independently
confirmed with direct evidence, including two live revert-and-restore reproductions)
**Re-verification:** No — initial verification

## Summary

I did not take SUMMARY.md or REVIEW-FIX.md claims at face value. I read the actual `finish()` body,
the `_is_master_document()` guard, both fixture files, and all three test modules; ran the full
non-slow suite (566 passed, 23 deselected, 0 failed — matching the claimed post-phase baseline
exactly); ran two independent structural AST probes; and, most importantly, **independently
reproduced both pre-fix failure modes myself** rather than trusting the SUMMARYs' transcripts:

1. Reverted `typsphinx/writer.py`'s CR-01 guard (`if doc_tuple and doc_tuple[0] == docname:` →
   `if doc_tuple[0] == docname:`) and re-ran the gate: both tests failed with the exact
   `IndexError: tuple index out of range` the code review reported, confirming the crash is real and
   the fix genuinely closes it. Restored byte-identically (md5 `858aa4fe373a2dfc5b03cb1f3289f632`,
   matching the value recorded in `22.3-REVIEW-FIX.md`; `git diff --stat` empty afterward).
2. Reverted both `failures.append(...)` sites plan 22.3-01 added to `typsphinx/builder.py` and
   re-ran the affected unit tests plus the GATE-01 subprocess gate: all 5 tests failed, reproducing
   WR-01's defect verbatim — `build succeeded, 2 warnings` with `returncode == 0` while two configured
   masters produced no PDF. Restored byte-identically (`git diff --stat` empty afterward; full suite
   re-confirmed green at 566/23/0).

Both code-review findings (CR-01 Critical, WR-01 Warning) and the Info item are genuinely resolved in
the tree, not just claimed. All ROADMAP SC#1–5 hold. The only unresolved item is a single
`verification: backstop` truth in plan 22.3-03 that cannot be confirmed by direct evidence (no
`pytest-xdist` in this project) — routed to human verification per the honest-verifier rule, not
treated as a gap, since it does not bear on any ROADMAP success criterion.

## Goal Achievement — ROADMAP Success Criteria (authoritative contract)

| # | Success Criterion | Status | Evidence |
|---|---|---|---|
| SC#1 | Missing-`.typ` branch (`builder.py`) aligned with compile-failure path; docstring's "no silent success" claim matches implementation | ✓ VERIFIED | Read `typsphinx/builder.py:922-934` — the guard now assigns a `found_docs`-discriminated `message`, logs it, and calls `failures.append((docname, message))` before `continue`. Docstring (`builder.py:875-886`) explicitly names all three failure kinds. Confirmed by AST structural probe (exit 0) and by `tests/test_pdf_generation.py::TestPDFErrorHandling::test_builder_appends_failure_for_missing_typ_file` / `test_builder_appends_failure_for_unknown_docname` (both pass). **Independently reproduced RED**: reverting the two `failures.append` sites made these tests fail with the exact pre-fix "silent success" symptom (`returncode == 0`, no PDF for `ghost`). |
| SC#2 | Malformed-`doc_tuple` skip treated consistently with the missing-`.typ` branch (not left asymmetric) | ✓ VERIFIED | `builder.py:912-916`: `if not doc_tuple:` now appends `(repr(doc_tuple), "malformed typst_documents entry")` before `continue` — same `failures` list, same aggregate raise. Detection surface unwidened (`grep -c 'if not doc_tuple:'` = 1, per D-06). Confirmed by `test_builder_appends_failure_for_malformed_entry_but_not_short_entry` (pass) and by the subprocess gate's `(): malformed typst_documents entry` assertion (pass). |
| SC#3 | Behavioral-vs-documentary decision made at discuss; if behavioral, staged for Phase 23 CHANGELOG | ✓ VERIFIED | `22.3-CONTEXT.md` D-01 records the decision was made **behavioral** at `/gsd-discuss-phase 22.3` (not left open). The exact CHANGELOG sentence is staged verbatim in both `22.3-01-SUMMARY.md` and `22.3-03-SUMMARY.md` under "Handoff Obligations" for Phase 23 to author — correctly out of this phase's scope to actually edit `CHANGELOG.md`. |
| SC#4 | `tests/test_nested_master_render_gate.py` no longer asserts on `typst-py` error-message literals (`"escape"`, `"not found"`, `"usage.typ"`, `"_template.typ"`) | ✓ VERIFIED | Measured directly: `grep -n '"escape"'` → 0 matches anywhere in the file. Every remaining occurrence of `"not found"` / `"file not found"` (lines 19, 125, 266, 412) is inside a docstring or a `# NOTE` comment, explicitly labeled "historical, non-contract record" / "an upstream rewording is expected NOT to break this test" — never inside an asserted expression. The removed PASS-path assertion (`"file not found" not in result.stderr`, the code-review Warning) is confirmed gone; replaced by the typsphinx-authored `"Typst compilation failed" not in result.stderr`. Both AST probes for the 3-part ablation (D-08/Pitfall 3: `.count("image(\"../logo.png\")")` and `.replace(...)` call nodes) pass, proving the GREEN ablation half is a real text substitution, not prose about one. |
| SC#5 | New behavior pinned by a test that fails against pre-fix code; zero new runtime deps; no `@preview` bump; 3-way version-sync surface untouched | ✓ VERIFIED | **Independently reproduced, not just read**: (a) unit-level — reverted both `failures.append` sites in `builder.py`; the 3 Task-1 unit tests plus both subprocess-gate tests failed (5 failed), reproducing the exact silent-success symptom; restored byte-identically. (b) CR-01/write-phase — reverted the `_is_master_document` guard in `writer.py`; both gate tests failed with `IndexError: tuple index out of range` (the code review's exact reproduction); restored byte-identically, md5 matches `22.3-REVIEW-FIX.md`'s recorded `858aa4fe373a2dfc5b03cb1f3289f632`. Invariants: `git diff` across every phase commit (`4d621da`..`8f94df9`) touches `typsphinx/translator.py`, `typsphinx/template_engine.py`, `typsphinx/templates/base.typ`, `pyproject.toml`, `uv.lock`, `typsphinx/__init__.py` — **zero** diff on all six. `@preview` versions (codly 1.3.0, codly-languages 0.1.10, mitex 0.2.7, gentle-clues 1.3.1) identical across `writer.py`/`template_engine.py`/`templates/base.typ`. `uv run pytest -q -m "not slow"` → **566 passed, 23 deselected, 0 failed** (my own run, main tree — matches `22.3-REVIEW-FIX.md`'s claimed post-fix count exactly). `tests/test_preview_version_sync.py tests/test_corpus_gate.py` → 6 passed, 1 skipped (pre-existing, unrelated skip). |

**Score:** 5/5 ROADMAP success criteria VERIFIED with direct evidence, including two independent live revert-and-restore reproductions of the pre-fix defects.

## Code-Review Findings — Independently Verified

| Finding | Severity | Status | Evidence |
|---|---|---|---|
| CR-01 (`_is_master_document()` unguarded `doc_tuple[0]` crashes write phase) | Critical | ✓ RESOLVED (verified live) | `typsphinx/writer.py:63-64` now reads `if doc_tuple and doc_tuple[0] == docname:`. I reverted this to the pre-fix form and re-ran the gate: both tests failed with `IndexError: tuple index out of range` at exactly the reported line — the crash is real and the fix closes it. Restored byte-identically (md5 `858aa4fe373a2dfc5b03cb1f3289f632`). The gate fixture was also extended with `chapter1.rst` (a second, non-master, toctree-only document not in `typst_documents`) so the scan is forced to traverse to the malformed `()` entry — confirmed present and referenced from `index.rst`'s toctree, and confirmed absent from `conf.py`'s `typst_documents`. |
| WR-01 (review) (stale `"file not found" not in result.stderr` assertion, PASS-path test) | Warning | ✓ RESOLVED | Assertion confirmed removed from `test_typstpdf_nested_master_resolves_include_and_image`; replaced with a comment explaining why no such assertion belongs there, plus the typsphinx-authored `"Typst compilation failed" not in result.stderr` check. |
| IN-01 (missing precondition symmetry for non-ASCII docname) | Info | ✓ RESOLVED | `tests/test_pdf_generation.py:361` now asserts `"日本語ドキュメント" not in temp_sphinx_app.env.found_docs` symmetrically with the `"ghost"` precondition. |

## Per-Plan `must_haves` Results

### Plan 22.3-01 (WR-01 core)

| Truth | Status | Evidence |
|---|---|---|
| D-01/D-05: missing-`.typ`/malformed entries raise `ExtensionError` | ✓ VERIFIED | Confirmed by code read + tests + independent revert-and-RED reproduction |
| D-02: both new failure kinds share `failures` list, single aggregate raise | ✓ VERIFIED | AST probe confirms exact control-flow shape |
| D-04 adjacency: fallback text byte-identical, `test_target_name_render_gate.py` unedited | ✓ VERIFIED | `git diff` across the whole phase range on that file is empty; fallback string `Master document not found: {typ_file}` confirmed byte-identical |
| D-04 shared append (both branches → one append) | ✓ VERIFIED | AST structural probe from the plan itself, re-run by me, exits 0 |
| WR-01/empty edge (`()` fails, `("valid",)` doesn't, empty list ≠ per-entry failure) | ✓ VERIFIED | `test_builder_appends_failure_for_malformed_entry_but_not_short_entry` passes; whole-project early return (`"No documents defined..."`) unchanged |
| WR-01/ordering edge | ✓ VERIFIED | `test_aggregate_message_lists_failures_in_typst_documents_order` passes |
| WR-01/encoding edge (non-ASCII docname) | ✓ VERIFIED | `test_builder_appends_failure_for_unknown_docname` passes with `日本語ドキュメント` |
| D-02 wording (`"master document(s) failed:"`) | ✓ VERIFIED | `grep -c` = 1; old `"to PDF: {summary}"` = 0 |
| D-06 scope fence (`if not doc_tuple:` unwidened; non-str docname deferred) | ✓ VERIFIED | `grep -c` = 1; todo filed at `.planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md` (confirmed present) |
| Prohibition: docstring never weakened | ✓ VERIFIED | Docstring strengthens the claim, does not weaken it |
| Prohibition: no new opt-in config / compat shim | ✓ VERIFIED | `git diff` on `typsphinx/__init__.py`, `pyproject.toml`, `uv.lock` across the whole phase is empty |

### Plan 22.3-02 (WR-02)

| Truth | Status | Evidence |
|---|---|---|
| D-10: zero `typst-py` diagnostic-string assertions | ✓ VERIFIED | Measured directly — 0 matches for `"escape"`; all `"not found"` occurrences are in docstrings/comments, never in an asserted expression |
| D-10 diagnostics preserved for humans only | ✓ VERIFIED | `red_half_error` / `exc_info.value` only appear in failure-message f-strings |
| D-08/D-09: ablation GREEN half produces real `b"%PDF"` bytes | ✓ VERIFIED | Two AST probes (count + replace call nodes) pass; `grep -c 'b"%PDF"'` = 4 |
| Pitfall 3: image path rewrite is mandatory, not optional | ✓ VERIFIED | Both AST probes pass, ruling out a parts-1+3-only (non-compiling) ablation |
| D-09: RED-only differential fixed by structural precondition + byte-identity | ✓ VERIFIED | `test_outdir_root_compile_basis_still_fails` passes; `read_bytes()` count ≥ 2 |
| Prohibition: no de-coupling to a bare raises-only check | ✓ VERIFIED | Filesystem/emitted-source preconditions plus the mandatory GREEN ablation remain — not a tolerant `pytest.raises(Exception)` alone |

### Plan 22.3-03 (D-11 GATE-01 + SC#5 subprocess proof)

| Truth | Status | Evidence |
|---|---|---|
| D-11: mixed fixture (valid/unknown-docname/malformed) exits non-zero | ✓ VERIFIED | `test_mixed_bad_entries_fail_build_but_good_master_still_compiles` passes; independently reproduced RED on reverted builder |
| D-02 end-to-end: valid master still gets its PDF | ✓ VERIFIED | Asserted and passing; confirmed in my own RED run too (`index.pdf` present pre-revert-of-CR01, absent-of-signal only for `ghost`) |
| Gate reads only typsphinx-authored stderr | ✓ VERIFIED | `grep -c 'TypstError'` = 0, `grep -c 'Extension error!'` = 0 |
| ROADMAP SC#5 RED/GREEN transcript pair | ✓ VERIFIED | Present in `22.3-03-SUMMARY.md`; independently reproduced by me as well (not merely trusted) |
| WR-01/empty, adjacency, ordering edges (subprocess level) | ✓ VERIFIED | Both gate tests pass with the specific assertions the plan requires |
| WR-02/concurrency edge (`verification: backstop`) | ⚠️ **human_needed (backstop, unconfirmed)** | Plan itself marks this `verification: backstop` and its `<flagged_assumptions>` states `pytest-xdist` is not a dependency, so no real parallel run was exercised. Per the honest-verifier rule this must abstain to human_needed rather than being silently counted VERIFIED. `git status --porcelain tests/` empty after the suite is confirmed (partial evidence), but genuine concurrent-execution safety is not directly tested. |

## Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| WR-01 | 22.3-01, 22.3-03 | `finish()` no longer silently succeeds for missing/malformed masters | ✓ SATISFIED | See SC#1/SC#2/SC#5 above |
| WR-02 | 22.3-02 | Render gate decoupled from `typst-py` wording | ✓ SATISFIED | See SC#4 above |

No orphaned requirements — `REQUIREMENTS.md` maps only WR-01/WR-02 to Phase 22.3, both claimed by a plan and both satisfied.

## Anti-Patterns Found

None. Scanned `typsphinx/builder.py`, `typsphinx/writer.py`, all three test modules, and the new
fixture files for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` — zero matches.

## Deviations / Notable Observations (non-blocking)

- **`.planning/STATE.md`, `.planning/ROADMAP.md`'s progress table, and `.planning/REQUIREMENTS.md`'s
  WR-01/WR-02 checkboxes are stale** — they still show Phase 22.3 as "In Progress" / "executing" /
  "Not started" even though all 3 plans are complete and the code review is resolved. This is a known
  GSD tracking artifact (project memory: `gsd-state-planned-phase-noop`), not a code defect. It does
  not affect the phase-goal verification above, but the orchestrator should update these files when
  closing this phase.
- **Source-of-record todo not yet closed.** `.planning/todos/pending/2026-07-22-wr01-silent-missing-pdf-wr02-typst-error-substring-coupling.md`
  is explicitly staged by both `22.3-01-SUMMARY.md` and `22.3-03-SUMMARY.md` to be closed "at
  `/gsd-verify-work`, after both WR-01 and WR-02 verify green." Both are now confirmed green — this
  closure is an outstanding operational action for the orchestrator, not a code gap.

## Human Verification Required

### 1. WR-02/concurrency edge — real parallel-execution safety of the two new/edited GATE-01 modules

**Test:** Run `tests/test_missing_and_malformed_master_gate.py` and
`tests/test_nested_master_render_gate.py` under a real parallel test runner (e.g. install
`pytest-xdist` and run `pytest -n auto`), or otherwise manually confirm no shared build-directory
state exists between concurrent test workers.
**Expected:** Both modules pass unchanged; no artifact collision between workers.
**Why human:** The plan itself marks this truth `verification: backstop` and documents in
`<flagged_assumptions>` that `pytest-xdist` is not a project dependency, so this was asserted
structurally (per-test `tmp_path`, leading-underscore artifact names, read-only fixture source) but
never exercised under genuine concurrency. Per the honest-verifier rule, an un-confirmable backstop
truth must be surfaced rather than silently marked passed. This does not affect any ROADMAP SC and is
low materiality — the per-test `tmp_path` pattern is a well-established pytest idiom used consistently
across every other GATE-01 module in this suite.

---

_Verified: 2026-07-22T13:33:55Z_
_Verifier: Claude (gsd-verifier)_
