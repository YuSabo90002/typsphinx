---
phase: 22.3-typstpdf-builder-warning-hardening
plan: 03
type: execute
wave: 2
depends_on: ["22.3-01"]
files_modified:
  - tests/fixtures/missing_and_malformed_master_gate/conf.py
  - tests/fixtures/missing_and_malformed_master_gate/index.rst
  - tests/test_missing_and_malformed_master_gate.py
autonomous: true
requirements: [WR-01]
user_setup: []

must_haves:
  truths:
    - "D-11: a real `sphinx-build -b typstpdf` subprocess run over a project whose `typst_documents` contains one valid master, one docname absent from `found_docs`, and one malformed empty entry exits NON-ZERO — the suite's first must-fail subprocess assertion, inverting the polarity every sibling GATE-01 module asserts."
    - "D-02 attempt-all-then-raise survives end-to-end: in that same failing build the valid master still gets its `.pdf` written on disk, proving no bad entry short-circuits the loop."
    - "The gate's pass/fail reads only typsphinx-authored strings from stderr — the aggregate `ExtensionError` body — never a `typst-py` diagnostic and never Sphinx-core's own CLI banner formatting."
    - "ROADMAP SC#5: the gate is demonstrated to FAIL against the pre-fix `finish()` and to PASS against the fixed one, with both transcripts recorded, and `git diff --stat typsphinx/builder.py` is empty afterwards so the temporary reversal left nothing behind."
    - "WR-01/empty edge (probe WR-01/empty): the malformed empty entry `()` is reported as a failure identified by `repr(doc_tuple)`, while the fixture's non-empty entries are not — a falsy per-entry value is a failure, whereas an empty `typst_documents` LIST is a different, unchanged code path that this fixture deliberately does not exercise."
    - "WR-01/adjacency edge (probe WR-01/adjacency): the unknown-docname entry produces the `found_docs`-discriminating message and the aggregate does NOT contain the fallback text `Master document not found`, proving end-to-end that D-04's two branches never collide."
    - "WR-01/ordering edge (probe WR-01/ordering): the aggregate reports exactly 2 failures and names them in `typst_documents` iteration order."
    - { statement: "WR-02/concurrency edge — the gate is safe under interruption and parallel execution: the fixture source directory is read-only input, every build artifact is written under a per-test pytest `tmp_path`, and no state is shared with any sibling gate module. Verified by `git status --porcelain tests/` being empty after the suite; a genuine `pytest -n` parallel run is not exercised because `pytest-xdist` is not a project dependency.", verification: backstop }
  prohibitions:
    - "The gate must never assert on a `typst-py` diagnostic string or on Sphinx-core's CLI traceback banner formatting. Both are uncontracted upstream presentation surfaces, and coupling to them here would recreate — in a brand-new module — the exact defect WR-02 is removing from a sibling module in the same phase."
    - "The subprocess must never be invoked through a PATH-resolved `sphinx-build` binary or through `uv run sphinx-build`. Under the documented NixOS sandbox that resolves a different interpreter than the one running pytest, which would silently test the wrong installed package while appearing to pass."
  artifacts:
    - "tests/fixtures/missing_and_malformed_master_gate/conf.py — the first fixture in this suite that deliberately encodes BAD configuration"
    - "tests/fixtures/missing_and_malformed_master_gate/index.rst — the one valid master"
    - "tests/test_missing_and_malformed_master_gate.py — the GATE-01 module carrying the suite's first must-fail subprocess assertion"
  key_links:
    - "`_run_sphinx_build` must be a module-local copy invoked as `sys.executable -m sphinx` — each GATE-01 module in this suite defines its own; none are shared or imported across modules"
    - "the aggregate `ExtensionError` message produced by `TypstPDFBuilder.finish()` (plan 22.3-01) is the only stderr contract this gate reads; a change to that wording must update this gate in the same commit"
    - "the fixture's `ghost` entry is only a `found_docs` miss for as long as no `ghost.rst` exists and no toctree references it — that absence is the measurement, not an oversight"
---

<objective>
Pin WR-01's new behavior with a GATE-01 real-`sphinx-build` fixture (D-11) and discharge ROADMAP
SC#5's pre-fix must-fail requirement at the subprocess level.

A new minimal Sphinx project encodes all three conditions at once — one valid master, one
`typst_documents` docname that is not a real Sphinx document (D-04), and one malformed empty entry
(D-05/D-07) — so a single build proves both halves of D-02's attempt-all-then-raise contract: the
build fails, and the valid master's PDF is still on disk. This is the suite's first must-fail
subprocess assertion; every existing GATE-01 module asserts `returncode == 0`.

Plan 22.3-01 already produced one pre-fix must-fail proof at the unit level (its four tests were
committed RED). This plan produces the second, independent proof at the CLI level, matching the
22.1 D-08 precedent of demonstrating a pre-fix failure two ways.

Purpose: the unit tests construct `TypstPDFBuilder` directly; only a real `sphinx-build` subprocess
proves that the failure actually reaches the process exit code a CI pipeline reads.
Output: a new fixture directory, a new gate module, and a recorded RED/GREEN transcript pair.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-CONTEXT.md
@.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-RESEARCH.md
@.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-PATTERNS.md
@.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-01-SUMMARY.md
@CLAUDE.md
</context>

<line_number_warning>
`22.3-CONTEXT.md` and `.planning/REQUIREMENTS.md` §WR-01 cite `builder.py` coordinates that have
drifted. The real ones (RESEARCH § "Pitfall 1", verified twice) are: `finish()` def **867**,
docstring **868-886**, whole-project-empty early return **894-898** (a DIFFERENT code path, not this
phase's target), `failures` declaration **902**, malformed guard **912-916**, missing-`.typ` guard
**922-924**, aggregate raise **939-948**. Plan 22.3-01 has already edited these, so the current file
differs again — Task 3 must locate the two `failures.append(...)` call sites by reading the file,
never by line number.
</line_number_warning>

<worktree_setup>
Worktree isolation is the standing execution mode (`workflow.use_worktrees: true`,
`worktree.baseRef: "head"`). Before running ANY command:

```
env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev
```

Then run every command through `uv run`. This is doubly load-bearing in this plan: Task 3's
must-fail proof temporarily edits `typsphinx/builder.py`, and without a worktree-local editable
install the subprocess would import the unchanged main-tree package — making the reversal invisible
and the "proof" meaningless in BOTH directions.

Never invoke a compiled binary directly under `uv run` (NixOS sandbox). Inside the tests the Sphinx
subprocess is spawned as `sys.executable -m sphinx`, which under `uv run pytest` resolves to the
worktree venv's interpreter and therefore to the worktree's editable `typsphinx`.

Baseline after plan 22.3-01: `uv run pytest -q -m "not slow"` → **564 passed, 23 deselected**.
</worktree_setup>

<artifacts_produced>
## Artifacts this phase produces (new symbols — plan 22.3-03)

Newly created here; they do not exist anywhere in the tree before this plan:

- `tests/fixtures/missing_and_malformed_master_gate/` (directory)
- `tests/fixtures/missing_and_malformed_master_gate/conf.py`
- `tests/fixtures/missing_and_malformed_master_gate/index.rst`
- `tests/test_missing_and_malformed_master_gate.py`, containing:
  - `TYPST_AVAILABLE`, `FIXTURES_DIR`, `GATE_FIXTURE_DIR` (module constants)
  - `_run_sphinx_build` (module-local helper — deliberately a per-module copy, not an import)
  - `TestMissingAndMalformedMasterGate` (class)
  - `TestMissingAndMalformedMasterGate.test_mixed_bad_entries_fail_build_but_good_master_still_compiles`
  - `TestMissingAndMalformedMasterGate.test_aggregate_message_discriminates_unknown_docname_from_missing_file`

Sibling plans in this phase produce: the four new tests in `tests/test_pdf_generation.py`
(`test_builder_appends_failure_for_missing_typ_file`,
`test_builder_appends_failure_for_unknown_docname`,
`test_builder_appends_failure_for_malformed_entry_but_not_short_entry`,
`test_aggregate_message_lists_failures_in_typst_documents_order`),
`.planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md`, and the new
locals `fully_neutralized_text` / `fully_neutralized_copy` / `red_half_error` / `root_usage_copy`
inside `tests/test_nested_master_render_gate.py`.
</artifacts_produced>

<discretion_record>
CONTEXT.md leaves the fixture name and layout for D-11 to the plan. **Choice made here:** one
combined fixture at `tests/fixtures/missing_and_malformed_master_gate/` holding all three conditions
simultaneously — adopting RESEARCH § "Open Questions" #1's recommendation. Rationale: it mirrors the
single-fixture-multiple-assertions house style of `tests/fixtures/package_only_config_gate/`, and,
more importantly, only a fixture that mixes a GOOD master with the bad entries can prove D-02's
attempt-all-then-raise contract end-to-end. Two smaller single-condition fixtures were rejected
because neither would exercise the mixed case, which is the interesting one.
</discretion_record>

<tasks>

<task type="auto">
  <name>Task 1: Create the missing_and_malformed_master_gate fixture (D-11, D-04, D-05)</name>
  <files>tests/fixtures/missing_and_malformed_master_gate/conf.py, tests/fixtures/missing_and_malformed_master_gate/index.rst</files>

  <read_first>
    - `tests/fixtures/package_only_config_gate/conf.py` — the house header-comment convention: a
      block comment naming each defect class the fixture pins, and, per condition, why it is written
      the way it is. Copy that discipline, not its content.
    - `tests/fixtures/nested_master_render_gate/` — the directory layout to mirror (`conf.py` plus
      `.rst` sources only; no template overrides, no package config, no assets).
    - `typsphinx/builder.py` — the post-22.3-01 `finish()` loop, so the fixture's three entries map
      onto exactly the three branches it must exercise.
    - `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-PATTERNS.md`
      § "`tests/fixtures/missing_and_malformed_master_gate/`".
  </read_first>

  <action>
Create `tests/fixtures/missing_and_malformed_master_gate/` with exactly two files.

`conf.py` — a minimal Sphinx project: `project`, `author`, `release`, `copyright`,
`extensions = ["typsphinx"]`, and a `typst_documents` list with exactly three entries in this order:

1. `("index", "index", project, author)` — the one VALID master. Its `.typ` is generated and it must
   still produce `index.pdf` even though the build overall fails (D-02 attempt-all-then-raise).
2. `("ghost", "ghost", project, author)` — a docname that is deliberately NOT a document in this
   project. There must be no `ghost.rst` file and it must not be reachable via any toctree. This
   exercises D-04's `docname not in self.env.found_docs` branch.
3. `()` — a malformed empty entry, exercising the D-05/D-07 branch, reported by `repr(doc_tuple)`.

Set no other `typst_*` config value. In particular set no `typst_template`, no `typst_package`, and
no `typst_template_function` — the valid master must compile through the bundled default template so
this gate stays a pure WR-01 gate and cannot go red because of an unrelated template or package
interaction.

Write a header comment block in the style of `package_only_config_gate/conf.py` stating: this is the
suite's first fixture deliberately encoding BAD configuration; the build is EXPECTED to fail with a
non-zero exit; entry order is load-bearing because the aggregate error renders failures in
`typst_documents` iteration order; and `ghost` must never be given an `.rst` file or added to a
toctree, because its absence from `env.found_docs` is precisely what the gate measures.

`index.rst` — a single small master document (a title plus a short paragraph) that compiles cleanly
under the default template. No toctree entry for `ghost`. Keep it minimal: every construct it uses
is a construct that could independently break this gate.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run python -c "import pathlib; ns={}; exec(compile(pathlib.Path('tests/fixtures/missing_and_malformed_master_gate/conf.py').read_text(),'conf.py','exec'), ns); d=ns['typst_documents']; assert len(d)==3, d; assert d[0][0]=='index', d; assert d[1][0]=='ghost', d; assert d[2]==(), d; assert 'typst_template' not in ns and 'typst_package' not in ns; print('fixture conf OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f tests/fixtures/missing_and_malformed_master_gate/conf.py` and `test -f tests/fixtures/missing_and_malformed_master_gate/index.rst` both succeed
    - `test ! -e tests/fixtures/missing_and_malformed_master_gate/ghost.rst` succeeds — the unknown docname must have no source file
    - `grep -c 'toctree' tests/fixtures/missing_and_malformed_master_gate/index.rst` returns 0
    - The `uv run python -c ...` check in `<verify>` prints `fixture conf OK`
    - `ls tests/fixtures/missing_and_malformed_master_gate/` lists exactly two entries
    - `uv run black --check tests/fixtures/missing_and_malformed_master_gate/conf.py` exits 0 and `uv run ruff check tests/fixtures/missing_and_malformed_master_gate/conf.py` exits 0
    - `git diff --name-only --cached; git status --porcelain` shows only the two new fixture files as additions
  </acceptance_criteria>

  <done>
The fixture directory contains exactly `conf.py` and `index.rst`, its `typst_documents` holds the
three ordered entries (valid / unknown-docname / empty tuple), no other `typst_*` value is set, no
`ghost.rst` exists, and the header comment records why each condition is written the way it is.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the GATE-01 must-fail gate module (D-11, D-02, D-04, D-07)</name>
  <files>tests/test_missing_and_malformed_master_gate.py</files>

  <read_first>
    - `tests/test_package_only_config_gate.py` lines 46-96 — the module-local `_run_sphinx_build`
      helper, the `TYPST_AVAILABLE` try/except import guard, the `FIXTURES_DIR` / `GATE_FIXTURE_DIR`
      constants, and the `@pytest.mark.skipif(not TYPST_AVAILABLE, ...)` class decorator. Copy this
      shape; note that no `slow` marker is applied to gate modules in this suite, so do not add one.
    - `tests/test_nested_master_render_gate.py` lines 186-226 — the sibling copy of the same helper
      plus the module/class docstring conventions used by GATE-01 modules.
    - `typsphinx/builder.py` — the post-22.3-01 aggregate `ExtensionError` f-string and both new
      guard messages. Those exact typsphinx-authored substrings are the only stderr contract this
      gate may read.
    - `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-RESEARCH.md`
      § "Architecture Patterns, Pattern 1" and its "Live confirmation of the post-fix ExtensionError
      shape" example (which records a real observed `returncode` of 2 — informative, NOT a contract).
  </read_first>

  <action>
Create `tests/test_missing_and_malformed_master_gate.py`.

Module docstring: state that this is a GATE-01 real-`sphinx-build` regression gate for WR-01
(Phase 22.3, D-11); that it is the suite's FIRST must-fail subprocess gate, inverting the
`returncode == 0` polarity every sibling gate asserts; that the fixture's build is expected to fail
by design; and that only typsphinx-authored strings may be read from stderr — never a `typst-py`
diagnostic and never Sphinx-core's own CLI banner text, both of which are uncontracted upstream
presentation surfaces (the same coupling class WR-02 removes from
`tests/test_nested_master_render_gate.py` in this same phase).

Copy the module-local `_run_sphinx_build` helper verbatim in shape from
`tests/test_package_only_config_gate.py`, including its docstring explaining the
`sys.executable -m sphinx` invocation and the NixOS PATH-shadowing hazard. Do not import a helper
from a sibling test module — every GATE-01 module in this suite carries its own copy.

Define `FIXTURES_DIR` and `GATE_FIXTURE_DIR` (pointing at
`tests/fixtures/missing_and_malformed_master_gate`), the `TYPST_AVAILABLE` import guard, and a class
`TestMissingAndMalformedMasterGate` decorated with the standard skipif.

Two test methods, both taking `tmp_path` and building into a subdirectory of it (never into a shared
or repo-relative path):

1. `test_mixed_bad_entries_fail_build_but_good_master_still_compiles` — run the fixture through
   `-b typstpdf`, then assert, each with a message embedding both `result.stdout` and
   `result.stderr`:
   - `result.returncode != 0`. Assert inequality, not `== 2`: the specific code is Sphinx CLI's own
     convention, not a typsphinx contract. Add a comment recording that this is the suite's first
     must-fail assertion so a future maintainer does not "correct" its polarity.
   - the aggregate message's typsphinx-authored fragment `master document(s) failed` appears in
     `result.stderr`, and it is preceded by the count `2` (two bad entries, one good master).
   - `index.typ` exists in the build directory — the valid master was written before the failure.
   - `index.pdf` exists in the build directory — D-02's attempt-all-then-raise survives end to end;
     a good master still gets its PDF even though the build as a whole fails.
   - no `ghost.typ` and no `ghost.pdf` exist in the build directory.

2. `test_aggregate_message_discriminates_unknown_docname_from_missing_file` — run the same build (a
   second run into a distinct `tmp_path` subdirectory; do not share a build directory between tests)
   and assert on the aggregate body only:
   - it names `ghost` and contains D-04's discriminating fragment `is not a known Sphinx document`.
   - it does NOT contain the fallback fragment `Master document not found` — the fixture's unknown
     docname must take the new branch, never the fallback, proving the two D-04 branches do not
     collide (the WR-01/adjacency probe).
   - it contains `(): malformed typst_documents entry` — D-07's `repr` identifier for the empty
     entry.
   - the position of the `ghost` fragment precedes the position of the `()` fragment, matching
     `typst_documents` iteration order (the WR-01/ordering probe).
   - it does NOT contain `failed to compile to PDF` — D-02's broadened wording is in effect.

Every assertion must carry an explanatory message. No assertion anywhere in the module may read a
Typst compiler diagnostic or Sphinx's traceback banner.

Then run the module and confirm it is GREEN against the fixed builder from plan 22.3-01.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest tests/test_missing_and_malformed_master_gate.py -v</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_missing_and_malformed_master_gate.py -v` exits 0 with both tests passing
    - `grep -c 'result.returncode != 0' tests/test_missing_and_malformed_master_gate.py` returns at least 1
    - `grep -c 'result.returncode == 0' tests/test_missing_and_malformed_master_gate.py` returns 0 — the polarity is inverted throughout
    - `grep -c 'returncode == 2' tests/test_missing_and_malformed_master_gate.py` returns 0 — the exact exit code is not treated as a contract
    - `grep -c 'sys.executable' tests/test_missing_and_malformed_master_gate.py` returns at least 1 and `grep -c 'sphinx-build' tests/test_missing_and_malformed_master_gate.py` returns 0 outside docstring prose — the subprocess is never a PATH-resolved binary
    - `grep -c 'is not a known Sphinx document' tests/test_missing_and_malformed_master_gate.py` returns at least 1
    - `grep -c '(): malformed typst_documents entry' tests/test_missing_and_malformed_master_gate.py` returns at least 1
    - `grep -c 'index.pdf' tests/test_missing_and_malformed_master_gate.py` returns at least 1 — attempt-all-then-raise is actually asserted
    - `grep -c 'TypstError' tests/test_missing_and_malformed_master_gate.py` returns 0 and `grep -c 'Extension error!' tests/test_missing_and_malformed_master_gate.py` returns 0 — no upstream presentation surface is read
    - `uv run black --check tests/test_missing_and_malformed_master_gate.py` exits 0 and `uv run ruff check tests/test_missing_and_malformed_master_gate.py` exits 0
    - `git status --porcelain tests/fixtures/` shows no modification to the fixture (the build wrote only into `tmp_path`)
  </acceptance_criteria>

  <done>
The gate module exists with its own `_run_sphinx_build` helper, both tests pass against the fixed
builder, the must-fail polarity is inverted and documented, D-04's discrimination and D-07's `repr`
identifier and D-02's ordering and broadened wording are all asserted, and no assertion reads a
`typst-py` or Sphinx-CLI presentation string.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the SC#5 pre-fix must-fail proof for the gate (D-11, ROADMAP SC#5)</name>
  <files>typsphinx/builder.py (temporarily reverted, then restored byte-identical — NOT a committed change), .planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-03-SUMMARY.md</files>

  <read_first>
    - `typsphinx/builder.py` — the current post-22.3-01 `finish()`. Locate the two
      `failures.append(...)` calls that plan 22.3-01 added inside the loop (one in the
      `if not doc_tuple:` guard, one in the `if not path.exists(typ_file):` guard) by reading the
      code, not by line number.
    - `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-01-SUMMARY.md` — the unit-level
      RED transcript already recorded there. This task produces the second, independent proof; the
      SUMMARY for this plan must cross-reference it.
    - `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-CONTEXT.md` § D-11 and
      `.planning/ROADMAP.md` § Phase 22.3 SC#5.
  </read_first>

  <action>
Demonstrate that the new gate fails against the pre-fix `finish()` and passes against the fixed one,
then restore the tree exactly. Follow this sequence without deviation:

1. Copy `typsphinx/builder.py` to a scratch path OUTSIDE the repository working tree (not under
   `tests/`, not under `.planning/`) so the restore is a byte-for-byte file copy rather than a
   re-edit. Record that path in the SUMMARY.
2. Temporarily revert ONLY the two `failures.append(...)` call sites added by plan 22.3-01: delete
   those two lines so each guard is again just its `logger.warning(...)` followed by `continue`.
   Leave the aggregate message, the docstring, and the D-04 message split alone — they do not
   determine whether the gate fails, and touching them widens the reversal beyond what is being
   proved.
3. Run `uv run pytest tests/test_missing_and_malformed_master_gate.py -v`. It MUST exit non-zero.
   Capture the transcript, specifically the assertion that fires and the observed `returncode` value
   of `0` — that observed `0` is the WR-01 defect itself: the build reported success while producing
   no PDF for two configured masters.
4. Restore `typsphinx/builder.py` from the scratch copy made in step 1.
5. Confirm the restore was exact: `git diff --stat typsphinx/builder.py` must produce no output.
6. Re-run `uv run pytest tests/test_missing_and_malformed_master_gate.py -v`. It MUST exit 0.
7. Run the full non-slow suite and compare against the expected count.

Transcribe both transcripts into `22.3-03-SUMMARY.md` under an explicit
`## SC#5 pre-fix must-fail proof (subprocess half)` heading, stating for each: the command run, the
exit status, and the deciding assertion. Cross-reference the unit-level half already recorded in
`22.3-01-SUMMARY.md`, so the SUMMARY pair together discharge the 22.1 D-08 "prove the pre-fix
failure two ways" precedent.

Also record in the SUMMARY, carried forward from `22.3-01-PLAN.md`'s hand-offs: the drafted Phase 23
`[0.6.2]` CHANGELOG sentence (D-03), and the obligation to close
`.planning/todos/pending/2026-07-22-wr01-silent-missing-pdf-wr02-typst-error-substring-coupling.md`
at `/gsd-verify-work` once both WR-01 and WR-02 verify green.

Commit nothing from step 2; the only committed change in this task is the SUMMARY.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest -q -m "not slow" &amp;&amp; git diff --stat typsphinx/builder.py &amp;&amp; test -z "$(git diff --stat typsphinx/builder.py)"</automated>
  </verify>

  <acceptance_criteria>
    - The recorded RED transcript shows `uv run pytest tests/test_missing_and_malformed_master_gate.py -v` exiting non-zero against the reverted builder, with the failing assertion being the `returncode != 0` check and the observed value being `0`
    - The recorded GREEN transcript shows the same command exiting 0 after the restore
    - `git diff --stat typsphinx/builder.py` produces no output — the temporary reversal left nothing behind
    - `grep -c 'failures.append' typsphinx/builder.py` returns 3 — the same value plan 22.3-01 established
    - `uv run pytest -q -m "not slow"` exits 0 and reports **566 passed, 23 deselected** (564 after plan 22.3-01, plus this plan's two gate tests; plan 22.3-02 adds no test function). Any other count is a regression and must be explained.
    - `uv run pytest tests/test_corpus_gate.py tests/test_preview_version_sync.py -x` exits 0 AND `git diff --name-only` lists neither file, nor `typsphinx/writer.py`, nor `typsphinx/template_engine.py`, nor `typsphinx/templates/base.typ`, nor any file under `tests/fixtures/` other than the two created in Task 1
    - `git status --porcelain` shows a clean tree apart from this plan's intended new files
    - `22.3-03-SUMMARY.md` contains the heading `## SC#5 pre-fix must-fail proof (subprocess half)`, both transcripts, the D-03 CHANGELOG sentence, and the source-todo closure obligation
  </acceptance_criteria>

  <done>
The gate is proven to fail against pre-fix code and pass against fixed code, `typsphinx/builder.py`
is byte-identical to its committed state, the full non-slow suite is green at 566 passed /
23 deselected, and the SUMMARY records both transcripts plus both hand-off obligations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` → typsphinx builder | The fixture's deliberately-bad `typst_documents` values cross into build orchestration. `conf.py` is fully-trusted arbitrary Python by construction; this fixture exercises malformed shapes, not untrusted input. |
| build process → CI exit code | The process exit status is the only signal a CI pipeline or a downstream PDF consumer receives. This gate asserts on exactly that signal. |
| test suite → Sphinx / typst-py presentation surfaces | stderr carries three intermixed authorships: typsphinx's own messages, Sphinx-core's CLI banner, and typst-py's diagnostics. Only the first is a contract this gate may read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.3-08 | Denial of Service (availability by omission) | end-to-end `sphinx-build -b typstpdf` exit status | high | mitigate | The gate asserts `result.returncode != 0` for a project that produces no PDF for two configured masters, and Task 3 proves the assertion actually discriminates by showing it fails against pre-fix code. Without the pre-fix proof the gate could be vacuously green. |
| T-22.3-09 | Tampering (test-suite integrity) | the new gate module's stderr assertions | high | mitigate | Assertions read only typsphinx-authored aggregate-message fragments; asserting on `typst-py` text or Sphinx's CLI banner is prohibited in `must_haves.prohibitions` and gated by acceptance criteria that grep for their absence. This prevents the new module from re-introducing, in the same phase, the coupling WR-02 removes elsewhere. |
| T-22.3-10 | Tampering (working-tree integrity) | Task 3's temporary revert of `typsphinx/builder.py` | medium | mitigate | The revert is bracketed by a file copy outside the working tree and closed by `git diff --stat typsphinx/builder.py` producing no output, so an incomplete restore fails the task rather than silently shipping a half-reverted builder. |
| T-22.3-11 | Spoofing (wrong package under test) | `sys.executable -m sphinx` subprocess resolution | medium | mitigate | Mandatory worktree provisioning plus `uv run` ensures the subprocess imports the worktree's editable `typsphinx`; a PATH-resolved `sphinx-build` is prohibited and grep-gated. Without this, Task 3's revert would be invisible to the subprocess and the SC#5 proof would be fabricated. |
| T-22.3-SC | Tampering (supply chain) | npm/pip/cargo installs | high | accept | Not applicable — zero packages installed, zero runtime dependencies added (milestone invariant, ROADMAP SC#5). `22.3-RESEARCH.md` § "Package Legitimacy Audit" records the gate as skipped by its own trigger condition. `uv sync --extra dev` installs only already-locked project dev dependencies. |

ASVS level 1; block-on threshold `high`. T-22.3-08 and T-22.3-09 are both `high` and both mitigated
within this plan. T-22.3-SC is `high` by template but has no applicable surface.
</threat_model>

<verification>
1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` — worktree provisioning (mandatory first step)
2. `uv run pytest tests/test_missing_and_malformed_master_gate.py -v` → 0, both tests passing
3. Task 3's RED transcript exists and shows the same command exiting non-zero against the reverted builder
4. `git diff --stat typsphinx/builder.py` → no output
5. `uv run pytest -q -m "not slow"` → **566 passed, 23 deselected**
6. `uv run pytest tests/test_corpus_gate.py tests/test_preview_version_sync.py -x` → 0, both files unedited
7. `uv run black --check .` → 0; `uv run ruff check .` → 0; `uv run mypy typsphinx/` → 0
8. `git diff --name-only` lists only the two new fixture files and the new gate module — in particular NOT `typsphinx/translator.py`, NOT `typsphinx/builder.py`, NOT `typsphinx/writer.py`, NOT `typsphinx/template_engine.py`, NOT `typsphinx/templates/base.typ`, NOT `tests/test_target_name_render_gate.py`
</verification>

<success_criteria>
- ROADMAP SC#1 and SC#2 hold at the CLI level, not only in unit tests: a build with an unknown-docname master and a malformed entry exits non-zero, and the valid master still gets its PDF
- ROADMAP SC#5: the gate demonstrably fails against pre-fix code, and both the RED and GREEN transcripts are recorded in the SUMMARY; combined with plan 22.3-01's unit-level RED transcript this discharges the 22.1 D-08 "prove it two ways" precedent
- ROADMAP SC#5 invariants: zero new runtime deps, no `@preview` bump, the three-way version-sync surface untouched, `tests/test_corpus_gate.py` green with no fixture regenerated
- `typsphinx/` has zero net changes in this plan
</success_criteria>

<flagged_assumptions>
- **Expected suite counts are cumulative and assume the phase's plans land in order.** 560 (research
  baseline) → 564 after 22.3-01's four unit tests → 566 after this plan's two gate tests; 22.3-02
  adds no test function. If the observed count differs, reconcile it against which plans have merged
  before treating it as a regression.
- **The fixture's valid master compiles through the bundled default template**, which imports four
  `@preview` packages. The first run in a cold environment may fetch them; every sibling GATE-01
  fixture has the same property and the local cache is warm. If a network-cold environment makes
  this gate flaky, that is an environmental issue shared with the existing gates, not a defect in
  this plan — record it rather than weakening the assertions.
- **`pytest-xdist` is not a project dependency**, so the WR-02/concurrency probe's parallel-execution
  guarantee is asserted structurally (per-test `tmp_path`, read-only fixture source) and carried as
  a `backstop` truth rather than exercised directly.
</flagged_assumptions>

<output>
Create `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-03-SUMMARY.md` when done,
containing the `## SC#5 pre-fix must-fail proof (subprocess half)` heading with both transcripts, the
D-03 CHANGELOG sentence carried forward, and the source-todo closure obligation.
</output>
