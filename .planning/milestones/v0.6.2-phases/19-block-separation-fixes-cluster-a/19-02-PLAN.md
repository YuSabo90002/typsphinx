---
phase: 19-block-separation-fixes-cluster-a
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - typsphinx/translator.py
  - tests/test_desc_signature_concat_render_gate.py
  - tests/fixtures/desc_signature_siblings_render_gate/conf.py
  - tests/fixtures/desc_signature_siblings_render_gate/index.rst
  - tests/test_rubric_option_concat_render_gate.py
  - tests/fixtures/rubric_option_concat_render_gate/conf.py
  - tests/fixtures/rubric_option_concat_render_gate/index.rst
autonomous: true
requirements: [FID-03, FID-04]

must_haves:
  truths:
    - "Multiple sibling desc_signatures (overloads / alias groups / multi-option directives) render with a linebreak() between siblings (stacked on separate lines), and the emitted .typ compiles to a valid %PDF (FID-03, D-01)."
    - "A desc with exactly ONE desc_signature (the overwhelming common case) is byte-for-byte unchanged — no linebreak() is emitted for a lone signature (the per-desc _is_first_desc_signature flag stays True through the only signature) [edge: FID-03 unclassified — single-element/cardinality input; also resolves the assumption-delta 'multiple' pluralization signal]."
    - "A rubric option-group heading directly adjacent to the following option/field renders with a linebreak() separating them instead of merging onto it, and the emitted .typ compiles to a valid %PDF (FID-04, D-01) [edge: FID-04 adjacency — a rubric that just touches the next option SEPARATES, not merges]."
    - "A rubric at true end-of-document (nothing follows the trailing linebreak()) compiles cleanly with no visible artifact (RESEARCH Site 3) [edge: FID-04 empty — end-of-input]."
    - "Each gate (the new FID-03 method in the extended module, and the new FID-04 module) FAILS against the pre-fix translator (the linebreak() token is absent) and PASSES post-fix (D-05 / GATE-01)."
    - "The full ~684-page corpus gate stays green after both fixes (Pitfall 4)."
  artifacts:
    - "typsphinx/translator.py :: self._is_first_desc_signature (new per-desc instance flag, set True in visit_desc); FID-03 leading-linebreak() in visit_desc_signature; FID-04 linebreak() in depart_rubric"
    - "tests/test_desc_signature_concat_render_gate.py (EXTENDED with a new FID-03 sibling-signature test method)"
    - "tests/fixtures/desc_signature_siblings_render_gate/{conf.py,index.rst} (new fixture: a py:function with 2+ signature lines + one lone signature)"
    - "tests/test_rubric_option_concat_render_gate.py (new GATE-01 module, FID-04)"
    - "tests/fixtures/rubric_option_concat_render_gate/{conf.py,index.rst} (new fixture: a .. rubric:: immediately followed by an .. option::)"
  key_links:
    - "FID-03 uses a NEW scalar per-desc flag self._is_first_desc_signature (mirrors the existing per-desc_signature _is_first_desc_signature_line one level up), reset True in visit_desc — a scalar is safe because a desc's own signature children are fully processed before its desc_content (which may hold nested desc children) is entered (RESEARCH Site 2, nested-desc note)."
    - "FID-04 depart_rubric emits linebreak() UNCONDITIONALLY (rubric always needs separation from what follows; verified harmless at end-of-document) — replaces the cosmetic self.body.append(\"\\n\")."
    - "Both fixes go through _emit_forced_break (from Plan 01), whose unconditional trailing newline prevents the linebreak()-abuts-strong({ 'expected semicolon or line break' fatal (Pitfall 1)."
  prohibitions:
    - statement: "MUST NOT introduce any new runtime dependency — pyproject.toml [project.dependencies] stays unchanged (milestone invariant)."
      status: flagged-unverified
      verification: manual
    - statement: "MUST NOT bump any @preview package version (codly / codly-languages / mitex / gentle-clues)."
      status: flagged-unverified
      verification: manual
    - statement: "MUST NOT modify the 3-way version-sync surface — writer.py / template_engine.py / templates/base.typ stay untouched."
      status: flagged-unverified
      verification: manual
    - statement: "MUST NOT regress the full ~684-page corpus gate (fatal-free, unknown_visit catalogue empty)."
      status: flagged-unverified
      verification: manual
  flagged_assumptions:
    - "[edge FID-04 ordering — unresolved] The probe 'when elements compare equal, is output order specified and stable?' does NOT apply to a rubric-then-option sequence: there is no comparison or sort — a rubric and its following option/field emit strictly in doctree (source) order, which is inherently stable. Carried as a flagged assumption, not silently dropped; no automated ordering check authored because no ordering semantics exist for this construct."
---

<objective>
Land the two `linebreak()` (stacked-line) fixes on the shared helper from Plan 01: sibling
`desc_signature`s (FID-03) and the `rubric` option-group heading (FID-04). Both render via
`strong()` delegation and both need a real Typst line break — not a cosmetic `\n` — between the
signature/heading and what follows, per the `-b html` authority (D-01).

Purpose: FID-03 and FID-04 are the two "stacked lines" members of Cluster A. FID-03 stacks
overload/alias/multi-option signatures; FID-04 puts the rubric heading on its own line ahead of
the first following option/field. Both reuse `_emit_forced_break("linebreak()")` (D-03), whose
unconditional trailing newline (Pitfall 1) is exactly what these SIBLING-boundary sites need —
the in-repo `visit_desc_signature_line` precedent would fail here verbatim.
Output: FID-03 + FID-04 fixes; the `desc_signature` gate module extended with a sibling-signature
method (+ fixture); a new rubric/option gate module (+ fixture); corpus gate confirmed green.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-block-separation-fixes-cluster-a/19-CONTEXT.md
@.planning/phases/19-block-separation-fixes-cluster-a/19-RESEARCH.md
@.planning/phases/19-block-separation-fixes-cluster-a/19-PATTERNS.md
@.planning/phases/19-block-separation-fixes-cluster-a/19-01-SUMMARY.md
</context>

## Artifacts this phase produces

The source-grounding / drift pass MUST treat these as newly-created. This plan's new symbols:
`self._is_first_desc_signature` (new per-`desc` instance flag, reset in `visit_desc`); the FID-03
leading-`linebreak()` in `visit_desc_signature`; the FID-04 `linebreak()` in `depart_rubric`; new
module `tests/test_rubric_option_concat_render_gate.py`; new fixtures
`tests/fixtures/rubric_option_concat_render_gate/{conf.py,index.rst}` and
`tests/fixtures/desc_signature_siblings_render_gate/{conf.py,index.rst}`; a new test method in
`tests/test_desc_signature_concat_render_gate.py`. (Full phase list is in 19-01-PLAN.md.) The
`_emit_forced_break` helper this plan consumes was created in Plan 01.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FID-03 (sibling desc_signatures) linebreak fix + extend the desc-signature gate</name>
  <read_first>
    - typsphinx/translator.py — `visit_desc`/`depart_desc` (~4299-4324; add the flag reset in `visit_desc`), `visit_desc_signature`/`depart_desc_signature` (~4326-4371; note the existing per-signature `_is_first_desc_signature_line` reset at ~4337), and `visit_desc_signature_line` (~4392-4410, the one-level-down precedent). The `_emit_forced_break` helper from Plan 01 (~261).
    - .planning/phases/19-block-separation-fixes-cluster-a/19-RESEARCH.md — "Site 2 — FID-03" (the new per-desc flag, the `linebreak()` insertion, the single-signature byte-unchanged guarantee, and the nested-`desc` scalar-flag safety argument).
    - .planning/phases/19-block-separation-fixes-cluster-a/19-PATTERNS.md — the FID-03 fix pattern + the "Test analog note".
    - tests/test_desc_signature_concat_render_gate.py — the module to EXTEND (read the existing method + `_run_sphinx_build_typstpdf` to reuse; add a new method, do not disturb the existing GATE-02 method).
    - tests/fixtures/desc_signature_concat_render_gate/{conf.py,index.rst} — the fixture-project template for the new sibling-signature fixture.
  </read_first>
  <behavior>
    - Given a `.. py:function::` directive with 2+ signature lines (sibling desc_signatures), the emitted `index.typ` contains a `linebreak()` between the sibling `strong({...})` signature blocks; pre-fix it contains none (RED→GREEN).
    - Given a `.. py:function::` with a single signature, no `linebreak()` is emitted for that lone signature (byte-unchanged; cardinality edge).
    - The emitted `index.typ` compiles via real `typst.compile()` to a file beginning with `%PDF`, with no "expected semicolon or line break" fatal (the helper's unconditional trailing newline).
  </behavior>
  <files>typsphinx/translator.py, tests/test_desc_signature_concat_render_gate.py, tests/fixtures/desc_signature_siblings_render_gate/conf.py, tests/fixtures/desc_signature_siblings_render_gate/index.rst</files>
  <action>
    Apply FID-03 (D-01: line break). In `visit_desc` (~4299-4316), after `_emit_id_anchors(node)`,
    initialise a NEW per-desc flag `self._is_first_desc_signature = True` (mirrors the existing
    per-`desc_signature` `_is_first_desc_signature_line` flag one level up). In `visit_desc_signature`
    (~4326), BEFORE creating the dummy strong, call `self._emit_forced_break("linebreak()")` when
    `not self._is_first_desc_signature`, then set `self._is_first_desc_signature = False`. Leave
    `depart_desc_signature`'s trailing `self.body.append("\n")` untouched (it already works as the LAST
    separator; the NEXT signature now supplies its own LEADING break). Keep the flag a plain scalar (not
    a stack) — safe because a `desc`'s own `desc_signature` children are fully processed before its
    `desc_content` (which may hold nested `desc`s) is entered (RESEARCH Site 2). A lone-signature `desc`
    emits zero extra bytes.

    Create `tests/fixtures/desc_signature_siblings_render_gate/{conf.py,index.rst}` (copy the fixture
    shape from `desc_signature_concat_render_gate`, but plainer — no intersphinx needed). `index.rst`:
    a `.. py:function::` with 2+ signature lines (e.g. `compile(source)` / `compile(source, filename)` /
    `compile(source, filename, symbol)`) producing sibling `desc_signature`s that pre-fix concatenate to
    one running line; plus a separate single-signature `.. py:function::` to exercise the lone-signature
    byte-unchanged edge.

    EXTEND `tests/test_desc_signature_concat_render_gate.py` with a NEW test method (do not modify the
    existing GATE-02 method) pointing at the new fixture via its own fixture-dir fixture. The method
    builds `-b typstpdf`, asserts `returncode == 0` and `"Typst compilation failed" not in stderr`, reads
    `index.typ`, asserts the `linebreak()` token is present between the sibling signatures (D-05), asserts
    the lone-signature construct emits NO `linebreak()` for its single signature, and asserts `index.pdf`
    begins with `%PDF`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_desc_signature_concat_render_gate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_desc_signature_concat_render_gate.py -x` exits 0 (both the existing GATE-02 method and the new FID-03 method).
    - GATE-01 / D-05 red→green proven: the new method FAILS against the pre-FID-03 translator (no
      `linebreak()` between the sibling signatures in `index.typ`) and PASSES after the edit; both
      outcomes recorded in the SUMMARY.
    - The new method asserts the `linebreak()` separator token is present between sibling signatures,
      the single-signature construct is unchanged, and `index.pdf` begins with `%PDF`.
    - The scalar `_is_first_desc_signature` flag is reset in `visit_desc` (not per-signature).
  </acceptance_criteria>
  <done>Sibling desc_signatures render `linebreak()`-stacked; a lone signature is byte-unchanged; the extended gate passes and the new method would fail pre-fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FID-04 (rubric/option heading) linebreak fix + new gate</name>
  <read_first>
    - typsphinx/translator.py — `visit_rubric`/`depart_rubric` (~4655-4677; `depart_rubric` appends only `"\n"` today, ~4677) and its `strong()` delegation. The `_emit_forced_break` helper (~261).
    - .planning/phases/19-block-separation-fixes-cluster-a/19-RESEARCH.md — "Site 3 — FID-04" (both corpus sub-cases collapse to one `depart_rubric` fix; the unconditional `linebreak()`; end-of-document harmlessness).
    - .planning/phases/19-block-separation-fixes-cluster-a/19-PATTERNS.md — the FID-04 fix pattern.
    - tests/test_rubric_propagated_target_render_gate.py — an existing rubric-focused gate module to reference for the `.. rubric::` fixture shape.
    - tests/test_desc_signature_concat_render_gate.py — GATE-01 module shape to copy for the new module (`_run_sphinx_build_typstpdf` verbatim).
  </read_first>
  <behavior>
    - Given a `.. rubric::` immediately followed by an `.. option::` (or a directive-option "Options" heading followed by a field), the emitted `index.typ` contains a `linebreak()` between the rubric's `strong({...})` and the following option/field; pre-fix it contains none (RED→GREEN).
    - The emitted `index.typ` compiles via real `typst.compile()` to a file beginning with `%PDF`.
    - A rubric at end-of-document (nothing after it) still compiles cleanly (end-of-input edge).
  </behavior>
  <files>typsphinx/translator.py, tests/test_rubric_option_concat_render_gate.py, tests/fixtures/rubric_option_concat_render_gate/conf.py, tests/fixtures/rubric_option_concat_render_gate/index.rst</files>
  <action>
    Apply FID-04 (D-01: heading on its own line). In `depart_rubric` (~4671-4677), after the
    `depart_strong` delegation, REPLACE the trailing `self.body.append("\n")` with
    `self._emit_forced_break("linebreak()")`, unconditionally (a rubric always needs separation from
    what follows; RESEARCH Site 3 verified this is harmless at true end-of-document — no compile error,
    no visible artifact). One fix at `depart_rubric` covers BOTH audit sub-cases (rubric→option and the
    "Options" heading→first field), since both render the following construct via `strong()`.

    Create `tests/fixtures/rubric_option_concat_render_gate/{conf.py,index.rst}` (copy the fixture-project
    shape; plain `.rst`, no `man` builder or intersphinx needed). `index.rst`: a `.. rubric::` immediately
    followed by an `.. option::` — mirror `man/sphinx-quickstart.rst`'s "Structure Options"/`--sep`
    pattern that pre-fix merges onto one line.

    Create `tests/test_rubric_option_concat_render_gate.py`: copy imports + `_run_sphinx_build_typstpdf`
    (verbatim) + fixtures from `test_desc_signature_concat_render_gate.py`. One test method: build, assert
    `returncode == 0` and `"Typst compilation failed" not in stderr`, read `index.typ`, assert the
    `linebreak()` token is present between the rubric and the following option (D-05), assert `index.pdf`
    begins with `%PDF`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_rubric_option_concat_render_gate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_rubric_option_concat_render_gate.py -x` exits 0.
    - GATE-01 / D-05 red→green proven: FAILS against the pre-FID-04 translator (no `linebreak()`
      between the rubric and the following option in `index.typ`), PASSES after the edit; both outcomes
      recorded in the SUMMARY.
    - The test asserts the `linebreak()` separator token is present between the rubric and the following
      option/field, and that `index.pdf` begins with `%PDF`.
    - `depart_rubric` emits the break unconditionally (no end-of-document guard was added).
  </acceptance_criteria>
  <done>A rubric heading renders `linebreak()`-separated from the following option/field; the new render-gate passes and would fail pre-fix; end-of-document rubrics still compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .rst source → Sphinx parser → docutils doctree → TypstTranslator | Untrusted authored text, already parsed/validated/resolved by Sphinx before the translator runs. No new input path — only which fixed break tokens are emitted for already-trusted node content changes. |
| emitted .typ → typst.compile() (pdf.py) | Offline in-process compile via pinned `typst-py` 0.15.0. No network, no external command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | translator.py `strong()`/`text()` emission around desc_signature & rubric | low | mitigate | Both fixes emit only the FIXED literal token `linebreak()` — never user-controlled content. Document string content still flows through the UNCHANGED `escape_typst_string()` helper; no interpolation into break-token strings → no new code-mode injection surface (RESEARCH §Security Domain). |
| T-19-02 | Information Disclosure / DoS | typst.compile() of emitted .typ | low | accept | Offline translation, no new external input path, pinned compiler. No plausible new surface from a separator change. |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | ZERO new packages installed this phase (milestone invariant; RESEARCH "Package Legitimacy Audit: Not applicable"). No supply-chain surface; no legitimacy checkpoint required. |

**No-new-surface finding (ASVS L1, block on `high`):** offline doctree→markup codegen; no
auth/session/access-control/crypto surface; V5 input-validation gains no new path. Highest
severity in scope is `low` — nothing meets the `high` block bar.
</threat_model>

<verification>
- `uv run pytest tests/test_desc_signature_concat_render_gate.py tests/test_rubric_option_concat_render_gate.py -x` green.
- `uv run pytest tests/test_translator.py -q` green (per-wave structural-regression sampling).
- **Phase blast-radius gate (Pitfall 4):** `uv run pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -q` green (~14s) after this plan.
- **D-06:** structural `.typ` token assert + real `%PDF` compile only — no rasterization; pypdf adjacency check not required.
- Milestone invariants held: no new dep, no `@preview` bump, version-sync surface untouched (edits in `translator.py` only).
</verification>

<success_criteria>
- FID-03: sibling desc_signatures render `linebreak()`-stacked; lone signature byte-unchanged.
- FID-04: rubric heading renders `linebreak()`-separated from the following option/field.
- Both gates fail pre-fix and pass post-fix (D-05); corpus gate green.
</success_criteria>

<output>
Create `.planning/phases/19-block-separation-fixes-cluster-a/19-02-SUMMARY.md` when done.
</output>
