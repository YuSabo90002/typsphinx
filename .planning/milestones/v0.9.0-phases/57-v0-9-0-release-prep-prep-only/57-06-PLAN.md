---
phase: 57-v0-9-0-release-prep-prep-only
plan: 06
type: execute
wave: 2
depends_on: ["57-01", "57-03", "57-04"]
files_modified:
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md
autonomous: true
requirements: [REL-08]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full local pytest suite runs on the POST-bump tree — not on the preceding phases' word — and reports zero failures and zero errors, with its summary line and JUnit `testsuite` attributes transcribed verbatim (ROADMAP SC#3)."
    - "`black --check .`, `ruff check .` and `mypy typsphinx/` are each run locally on the post-bump tree and their transcripts recorded. Local `ruff` runnability was re-measured this milestone and works; that is recorded as a fact with its transcript rather than repeated as an impossibility from a stale premise. These local runs are an additive pre-flight — the dispatched CI run (plan 57-05) keeps lint/type/matrix authority under D-13."
    - "Both documentation tox environments build on the post-bump tree, and their warning counts are recorded against the measured pre-existing baseline rather than reported as a bare success (ROADMAP SC#3)."
    - "`tests/test_corpus_gate.py`'s outcome is recorded as PASSED or as SKIPPED **with its skip reason quoted** — never conflated. A `pytest.skip` is not evidence: the module skips rather than fails when the corpus is unavailable, so a green summary line containing a skip proves nothing about the full-corpus claim in the release section's `### Verified` block."
    - "A locally-built wheel is inspected for the bundled template-directory file, giving a local copy of SC#3's built-wheel content check alongside the CI-side one — because an editable install never packs a wheel and therefore cannot detect a narrowed package-data glob."
    - "Every command is run inside the executor's own provisioned worktree via `uv run`, so the suite imports this worktree's editable copy rather than the main checkout's; the provisioning transcript is recorded."
    - "What was deliberately NOT run locally is stated plainly — a bare `tox` and `tox -e py312` — with the reason for each, so a reader cannot mistake the local half for a full-matrix result."
    - "Nothing under `typsphinx/`, `tests/`, `docs/` or `.planning/REQUIREMENTS.md` changed in this plan; it runs and records, it does not edit."
  artifacts:
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md"
      provides: "SC#3's local half — the full suite, the lint/type/format trio, both docs builds, the corpus gate's honest outcome, and a local built-wheel content check"
      contains: "SC#3"
  key_links:
    - from: "the post-bump tree"
      to: "the full local pytest suite and both docs tox environments"
      via: "`uv run` inside a per-worktree `.venv` provisioned by `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`; without it the editable finder resolves imports to the main checkout and the run measures the wrong tree"
      pattern: "uv run"
    - from: "pyproject.toml's package-data glob"
      to: "a locally built wheel's file list"
      via: "`uv build` followed by a zipfile namelist inspection; the editable install used by every other local command never packs a wheel, so inspection of the glob is not evidence"
      pattern: "uv build"
    - from: "tests/test_corpus_gate.py"
      to: "the release section's full-corpus `### Verified` claim"
      via: "the gate skips rather than fails when the corpus is unavailable, so the claim is only supported by a recorded PASSED outcome; a SKIPPED outcome must be reported as such and the claim qualified"
      pattern: "test_corpus_gate"
  prohibitions:
    - statement: "No git tag named v0.9.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged."
      status: unverified
      verification: flagged
    - statement: "No test gate is weakened — no skip guard loosened, no marker removed, no assertion deleted, no test deselected — to make a run report green."
      status: unverified
      verification: flagged
    - statement: "A pytest.skip is never reported as a pass; the corpus gate's outcome is recorded as PASSED or SKIPPED with its reason, never conflated."
      status: unverified
      verification: flagged
    - statement: "No source, test, documentation or requirements file is edited by this plan; it runs commands and records their output."
      status: unverified
      verification: flagged
    - statement: "No local lint/type/matrix result is presented as SC#3's authority; that authority belongs to the dispatched CI run under D-13."
      status: unverified
      verification: flagged
    - statement: "REL-08's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped."
      status: unverified
      verification: flagged
    - statement: "No warning count or baseline figure is transcribed from a planning document; each is measured by the run it describes."
      status: unverified
      verification: flagged
  flagged_assumptions: []
---

<objective>
Prove the post-bump tree green on everything the dispatched CI matrix does not cover, and record it
verbatim.

Purpose: discharges ROADMAP Phase 57 SC#3's local half. CI holds the pytest / lint / type authority
under D-13, but `ci.yml` has no documentation-build job, no full-corpus gate and no local wheel
inspection — so both `tox` docs environments, `tests/test_corpus_gate.py` and a locally built wheel's
contents are collected here, on the tree as it stands after the bump, the CHANGELOG curation and the
migration guide. The corpus gate is the one place in this phase where an honest SKIPPED must not be
laundered into a PASSED: it skips rather than fails when the corpus is unavailable, and the release
section's `### Verified` block makes a full-corpus claim that only a real PASSED supports.

Output: `57-GREEN-TREE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-level list, repeated in every plan of this phase so each is readable standalone.

**Source tree (modified files and new symbols):**

| Artifact | Plan | Shape |
|---|---|---|
| `pyproject.toml` line 7 | 57-01 | the `version` key's value becomes the string `0.9.0` |
| `README.md` line 347 | 57-01 | the Status line reads `**Status**: Stable (v0.9.0) - Production ready` |
| `uv.lock` | 57-01 | regenerated; its first-party `typsphinx` block carries the new version |
| `CHANGELOG.md` — new `## [0.9.0] - <date>` section (lead paragraph + `### Added` / `### Changed` / `### Fixed` / `### Removed` / `### Verified`) | 57-03 | new section; `## [Unreleased]` keeps only its `### Planned for Future Releases` list |
| `CHANGELOG.md` tail link block | 57-03 | one inserted `[0.9.0]:` release-tag line above the existing topmost release line, and the `[Unreleased]` compare base advanced |
| `tests/test_changelog_page_gate.py` — `RELEASE_VERSIONS` gains a `"0.9.0"` entry (14 → 15) and its count/range comment moves | 57-03 | tuple + comment edit only |
| `docs/source/changelog.rst` — new `Migrating from 0.8.x to 0.9.0` subsection under `Migration Guides` | 57-04 | new prose section, deliberately ungated (D-07) |
| `docs/source/installation.rst`, `docs/source/contributing.rst`, `docs/source/examples/advanced.rst`, `examples/basic/README.md`, `examples/advanced/README.md` | 57-04 | **conditional** — edited ONLY if the execution-time D-10 discovery grep returns a hit; commit `70e24958` already corrected all five, so the expected diff is empty |
| `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` | 57-09 | dated evidence annotation appended; the record **stays in `pending/`** by owner decision |

**No new todo record is created by this phase.**

**Planning artifacts:**

| Path (under `.planning/phases/57-v0-9-0-release-prep-prep-only/`) | Plan |
|---|---|
| `57-BUMP-EVIDENCE.md` | 57-01 |
| `57-CLOSEOUT-GUARD.md` | 57-01 (baseline) — re-verified by 57-08 and 57-09 |
| `COVERAGE.md` | 57-01 |
| `57-CI-EVIDENCE.md` | 57-02 (pre-bump check run) → 57-05 (post-bump authority run) |
| `57-CHANGELOG-EVIDENCE.md` | 57-03 |
| `57-MIGRATION-EVIDENCE.md` | 57-04 |
| `57-GREEN-TREE-EVIDENCE.md` | 57-06 |
| `57-GOAL-CLAIM-EVIDENCE.md` | 57-07 |
| `57-SC4-INVARIANTS.md` | 57-08 |
| `57-HANDOFF.md` | 57-09 |

**Reserved name — never produced:** `57-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: Run the full local suite and the format/lint/type trio on the post-bump tree</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md</files>
  <precondition>Wave 1 has merged back: `grep -c '^version = "0\.9\.0"$' pyproject.toml` returns 1 and `grep -c '^## \[0\.9\.0\]' CHANGELOG.md` returns 1. If either returns 0, this plan is running out of order and the run would measure the pre-bump tree.</precondition>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — the mandatory provisioning and `uv run` rule, and the reason: without it the editable finder resolves `import typsphinx` to the MAIN checkout, so the suite measures a tree this plan did not build.
    - `pyproject.toml` `[tool.pytest.ini_options]` — the marker configuration and any default deselection, so the suite is invoked the way the project intends.
    - `.planning/STATE.md` — the measured green baseline recorded at the Phase 56 close (suite counts, and the note that `black`/`ruff`/`mypy` were clean), so a regression is distinguishable from a pre-existing condition. Also the standing instruction to run the lint/type trio at every gate, not pytest alone.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md` § "Pitfall 2" — the re-measured local `ruff` status and the instruction never to assert its impossibility without a fresh transcript.
  </read_first>
  <action>
Provision first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, capturing the
transcript. Then run every command through `uv run`.

**Step 1 — confirm the tree.** Run `grep -n '^version = ' pyproject.toml`,
`grep -c '^## \[0\.9\.0\]' CHANGELOG.md` and
`uv run python -c "import typsphinx; print(typsphinx.__version__, typsphinx.__file__)"`. The printed
`__file__` path must lie inside this worktree, not the main checkout. Record all three. If the path
points elsewhere, STOP: the provisioning did not isolate and everything below would measure the wrong
tree.

**Step 2 — the full suite.** Run
`uv run pytest -v --junit-xml="${TMPDIR:-/tmp}/57-06-suite.xml"` and capture the complete summary line
plus the JUnit `testsuite` element's attributes (`tests`, `failures`, `errors`, `skipped`, `time`).
Zero failures and zero errors is the bar; the skip count is recorded as a number, not waved past.
If any test fails, report it with its node id and traceback and STOP — do not deselect it, do not
loosen a marker, do not modify the test.

**Step 3 — the format/lint/type trio.** Run each separately and capture each transcript with its exit
code: `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/`. Record what actually
happens. `ruff` was re-measured working on this machine during this milestone's research, so do not
write "ruff cannot run here" without a fresh failing transcript backing that specific claim on this
specific day; equally, if it does fail today, record the failure honestly rather than assuming the
research finding still holds. In either case state that this trio is an additive local pre-flight and
that lint and type authority sits with the dispatched CI run under D-13, on the independent grounds
that no local run reaches the Windows or macOS lanes.

Do NOT run a bare `tox` (its `env_list` includes environments this machine cannot provision) and do
NOT run `tox -e py312` (the standalone CPython 3.12 download is an ELF the NixOS stub loader rejects).
Both omissions get recorded explicitly in Task 3.

Edit no file in this task other than by creating the evidence file in Task 3 — this task runs and
captures.
  </action>
  <verify>
    <automated>uv run pytest -q --junit-xml="${TMPDIR:-/tmp}/57-06-suite.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/57-06-suite.xml" && grep -q 'errors="0"' "${TMPDIR:-/tmp}/57-06-suite.xml" && uv run black --check . && uv run mypy typsphinx/ && uv run python -c "import typsphinx, sys; sys.exit(0 if typsphinx.__version__ == '0.9.0' else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - The JUnit `testsuite` element from the full-suite run carries `failures="0"` and `errors="0"`; its `tests` and `skipped` counts are recorded as numbers.
    - `uv run python -c "import typsphinx; print(typsphinx.__version__, typsphinx.__file__)"` prints `0.9.0` and a `__file__` path inside this worktree.
    - `uv run black --check .` exits 0 and `uv run mypy typsphinx/` exits 0, each with its transcript captured.
    - `uv run ruff check .` was run and its exit code and output captured verbatim; no claim about local ruff availability is made without that transcript.
    - No pytest invocation in this task used `-k`, `-m 'not ...'`, `--deselect` or `--ignore` to exclude a test.
    - `git diff --name-only` lists no source, test or documentation path for this task.
  </acceptance_criteria>
  <done>The full suite passes on the post-bump tree with zero failures and zero errors, the format and type gates pass, the lint gate's real behaviour today is transcribed rather than assumed, and the imported package is proven to be this worktree's copy.</done>
</task>

<task type="auto">
  <name>Task 2: Build both documentation environments, run the full-corpus gate, and inspect a locally built wheel</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - `tox.ini` — the `docs-html` and `docs-pdf` environment definitions and their dependency sets, so the two builds are invoked the way the project intends.
    - `tests/test_corpus_gate.py` — the `slow` marker, the corpus-availability guard, and the exact skip path, so a skip can be reported with its real reason instead of a guess.
    - `.github/workflows/ci.yml` — the Build Package job's wheel-content verification step, so the local copy checks for the same target file rather than inventing one.
    - `.planning/STATE.md` — the Phase 56 close's measured documentation-build warning baseline (`docs-html` and `docs-pdf` warning counts), so this run's counts are comparable rather than reported in isolation.
  </read_first>
  <action>
Continue in the same provisioned worktree; run everything through `uv run`.

**Step 1 — both documentation builds.** Run `uv run tox -e docs-html` and then `uv run tox -e docs-pdf`,
capturing each full transcript including its `build succeeded` line and its warning count. Compare
each count against the baseline recorded at the previous phase close and state whether it matches; if
a new warning appears, quote it and name the page it comes from. Do not report a bare "succeeded" —
the warning count is the part that catches a regression introduced by the new migration-guide section
or the promoted CHANGELOG prose.

**Step 2 — the full-corpus gate, reported honestly.** Run
`uv run pytest tests/test_corpus_gate.py -v` and capture the complete transcript. The outcome is
**exactly one of** PASSED or SKIPPED, and whichever it is must be written as such:

- If PASSED, record it and note that the release section's full-corpus `### Verified` claim is
  supported by this run.
- If SKIPPED, quote the skip reason verbatim from the transcript, state explicitly that a
  `pytest.skip` is not evidence, and record the full-corpus claim as **not locally re-proven this
  phase** with a pointer to whichever earlier phase last proved it. Do NOT loosen the availability
  guard, do not force the marker, and do not describe the skip as a pass.

If the corpus is fetchable and the skip is only a matter of the marker's default deselection, re-run
with the marker explicitly selected and record that invocation too — but never edit the module.

**Step 3 — the local built-wheel content check.** Run `uv build` and capture its transcript, then
inspect the produced wheel for the bundled template-directory file the CI Build Package job checks
for. Use an inline Python read of the wheel's `zipfile` namelist rather than unpacking into the tree,
and print both a pass/fail line and the matching entry names. Record which wheel filename was
inspected. This is a **local copy** of SC#3's built-wheel content check; the CI-side one is captured
by plan 57-05 and neither replaces the other. Remove or leave the `dist/` output as the repository's
`.gitignore` dictates — it must not appear in `git status --porcelain` as an untracked addition to be
committed.

Edit no source, test or documentation file.
  </action>
  <verify>
    <automated>uv run tox -e docs-html && uv run tox -e docs-pdf && uv run pytest tests/test_corpus_gate.py -q --junit-xml="${TMPDIR:-/tmp}/57-06-corpus.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/57-06-corpus.xml" && grep -q 'errors="0"' "${TMPDIR:-/tmp}/57-06-corpus.xml" && uv build && uv run python -c "import glob,sys,zipfile; w=sorted(glob.glob('dist/*.whl'))[-1]; n=zipfile.ZipFile(w).namelist(); t='typsphinx/templates/README.md'; print('OK' if t in n else 'FATAL', w); sys.exit(0 if t in n else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Both `uv run tox -e docs-html` and `uv run tox -e docs-pdf` exit 0 and their `build succeeded` lines with warning counts are captured; each count is compared in prose against the previous close's recorded baseline.
    - The corpus-gate transcript is captured in full and its outcome is written as PASSED or as SKIPPED-with-quoted-reason; the evidence file contains the literal `A pytest.skip is not evidence` in the paragraph describing it.
    - `tests/test_corpus_gate.py` is byte-unchanged: `git diff --name-only -- tests/` produces no output.
    - `uv build` produced a wheel, and an inline `zipfile` namelist inspection of it printed a pass line naming the bundled template-directory file and the wheel filename.
    - `git status --porcelain` shows no untracked `dist/` artifact staged for commit.
    - `git diff --name-only -- typsphinx/ tests/ docs/` produces no output.
  </acceptance_criteria>
  <done>Both documentation environments build with their warning counts compared against the recorded baseline, the full-corpus gate's outcome is recorded as PASSED or as an honestly-labelled SKIPPED, and a locally built wheel is proven to carry the bundled template-directory file.</done>
</task>

<task type="auto">
  <name>Task 3: Write the local green-tree evidence record</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - `.planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md` — the heading structure and evidence register to match.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-VALIDATION.md` § "Sampling Rate" — the phase-gate bar this file discharges, including the corpus-gate honesty rule.
  </read_first>
  <action>
Write `.planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md` in English with
the heading `# Phase 57 — Local Green-Tree Evidence (SC#3, local half)` and these sections:

- `## Provisioning and tree identity` — the `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync
  --extra dev` transcript, Task 1 Step 1's three commands with output, and one sentence stating why
  the `__file__` path check matters on this project.
- `## SC#3 — full pytest suite` — the complete summary line and the JUnit `testsuite` attributes,
  with the skip count stated as a number and not glossed.
- `## SC#3 — format, lint and type` — the three transcripts with their exit codes, plus one paragraph
  recording the current local `ruff` behaviour as measured today and stating that lint and type
  authority nonetheless belongs to the dispatched CI run under D-13, because no local run reaches the
  Windows or macOS lanes.
- `## SC#3 — documentation builds` — both `tox` transcripts, both warning counts, and the comparison
  against the previous close's baseline with any new warning quoted and attributed to a page.
- `## SC#3 — full-corpus gate` — the complete transcript and an unambiguous outcome line reading
  either `Outcome: PASSED` or `Outcome: SKIPPED — <verbatim reason>`. Include the sentence
  `A pytest.skip is not evidence.` and, if skipped, state that the release section's full-corpus
  claim was not locally re-proven this phase and name the phase that last proved it.
- `## SC#3 — built-wheel content check (local copy)` — the `uv build` transcript, the inspected wheel
  filename, the namelist check's output, and one sentence stating that the CI-side copy is recorded
  in `57-CI-EVIDENCE.md` by plan 57-05 and that neither replaces the other.
- `## Executed versus skipped` — a plain list of what was NOT run locally and why: a bare `tox`
  (`env_list` includes environments this machine cannot provision) and `tox -e py312` (standalone
  CPython 3.12 is an ELF the NixOS stub loader rejects), with the statement that full-matrix
  authority belongs to CI per D-13.
- `## Division of authority` — three lines naming which claim is proven where: CI for the six-lane
  matrix, lint and type; this file for both documentation builds, the corpus gate and the local
  wheel; `57-GOAL-CLAIM-EVIDENCE.md` (plan 57-07) for the multi-template PDF claim.

Never assert a command's result from memory — every section quotes a transcript produced in Task 1 or
Task 2. Where a result is genuinely unavailable, record `human_needed` honestly rather than inferring
it.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md && grep -q '^## SC#3 — full pytest suite' .planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md && grep -q '^## SC#3 — full-corpus gate' .planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md && grep -q 'A pytest.skip is not evidence' .planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md && grep -Eq '^Outcome: (PASSED|SKIPPED)' .planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md && grep -q '^## Executed versus skipped' .planning/phases/57-v0-9-0-release-prep-prep-only/57-GREEN-TREE-EVIDENCE.md && test -z "$(git diff --name-only -- typsphinx/ tests/ docs/ .planning/REQUIREMENTS.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `57-GREEN-TREE-EVIDENCE.md` exists, is written entirely in English, and contains the headings `## Provisioning and tree identity`, `## SC#3 — full pytest suite`, `## SC#3 — format, lint and type`, `## SC#3 — documentation builds`, `## SC#3 — full-corpus gate`, `## SC#3 — built-wheel content check (local copy)`, `## Executed versus skipped`, and `## Division of authority`.
    - The file contains a line matching `^Outcome: (PASSED|SKIPPED)` and the literal `A pytest.skip is not evidence.`
    - The `## SC#3 — full pytest suite` section contains a JUnit `testsuite` attribute string including `failures="0"` and `errors="0"`.
    - The `## SC#3 — documentation builds` section contains two `build succeeded` lines and a stated warning count for each, compared in prose against a baseline figure.
    - The `## SC#3 — format, lint and type` section contains three distinct command transcripts and a sentence assigning authority to CI.
    - The `## Executed versus skipped` section names both `tox -e py312` and a bare `tox` with a reason for each.
    - The `## Division of authority` section names `57-CI-EVIDENCE.md` and `57-GOAL-CLAIM-EVIDENCE.md`.
    - `git diff --name-only -- typsphinx/ tests/ docs/ .planning/REQUIREMENTS.md` produces no output.
  </acceptance_criteria>
  <done>SC#3's local half is on disk as verbatim transcripts — full suite, format/lint/type, both documentation builds with their warning counts, the corpus gate with an unambiguous PASSED-or-SKIPPED outcome line, and a locally built wheel's content check — with an explicit statement of what was not run and where each claim's authority lives.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the executor's worktree `.venv` → what `import typsphinx` resolves to | Without per-worktree provisioning the suite measures the main checkout, so a green result would describe a tree this plan did not build. |
| a `pytest.skip` → a claim of proof | The corpus gate skips rather than fails when its input is unavailable; conflating the two would put an unproven claim into the published `### Verified` block. |
| the built wheel → PyPI at the publish | An editable install cannot detect a narrowed package-data glob; only a packed wheel can. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-31 | Repudiation | a skipped corpus gate reported as a pass | high | mitigate | The evidence file must carry an `Outcome: PASSED` or `Outcome: SKIPPED — <reason>` line and the literal statement that a skip is not evidence, both asserted in `<verify>` and acceptance; a prohibition forbids conflating them. |
| T-57-32 | Spoofing | a green suite measured against the main checkout instead of this worktree | high | mitigate | The `import typsphinx` `__file__` path is asserted to lie inside this worktree, and the version read-back is asserted in `<verify>`. |
| T-57-33 | Tampering | a test deselected, a marker loosened or a guard relaxed to force green | high | mitigate | Acceptance asserts no `-k`, `-m 'not ...'`, `--deselect` or `--ignore` was used and that `git diff --name-only -- tests/` is empty; a prohibition names each weakening route. |
| T-57-34 | Tampering | a narrowed package-data glob shipping a wheel missing bundled template files | high | mitigate | The local `uv build` plus zipfile namelist inspection is asserted in `<verify>`; the CI-side copy in plan 57-05 is an independent second observation. |
| T-57-35 | Repudiation | a documentation build reported as a bare success, hiding a new warning | medium | mitigate | Acceptance requires both warning counts to be stated and compared in prose against the previous close's baseline, with any new warning quoted and attributed. |
| T-57-36 | Repudiation | local lint presented as SC#3's authority | medium | mitigate | Both the action and the evidence file must state that CI holds lint/type/matrix authority under D-13; a prohibition forbids the contrary framing. |
| T-57-SC | Tampering | npm/pip/cargo installs | low | accept | Only `uv sync --extra dev` and `uv build` run, both against the existing lock and the existing manifest; no package is added. `57-RESEARCH.md` records the Package Legitimacy Gate as not applicable. |
</threat_model>

<verification>
- The full-suite JUnit result carries `failures="0"` and `errors="0"`.
- `uv run black --check .` and `uv run mypy typsphinx/` exit 0; `uv run ruff check .` is run and its result transcribed.
- Both `uv run tox -e docs-html` and `uv run tox -e docs-pdf` exit 0 with their warning counts recorded.
- The corpus gate's outcome is recorded as PASSED or SKIPPED-with-reason, never conflated.
- A locally built wheel is proven to carry the bundled template-directory file.
- `git diff --name-only -- typsphinx/ tests/ docs/ .planning/REQUIREMENTS.md` produces no output.
</verification>

<success_criteria>
- The post-bump tree is proven green locally on the full suite, the format and type gates, and both documentation builds (SC#3 local half).
- The full-corpus gate's outcome is recorded honestly, with a skip labelled as such and the dependent `### Verified` claim qualified if it skipped.
- SC#3's built-wheel content check has a local copy alongside the CI-side one.
- What was not run locally, and where each claim's authority lives, is stated plainly.
</success_criteria>

<output>
Create `.planning/phases/57-v0-9-0-release-prep-prep-only/57-06-SUMMARY.md` when done.
</output>
