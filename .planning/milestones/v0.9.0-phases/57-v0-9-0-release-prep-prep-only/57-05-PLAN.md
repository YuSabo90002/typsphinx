---
phase: 57-v0-9-0-release-prep-prep-only
plan: 05
type: execute
wave: 2
depends_on: ["57-01", "57-02", "57-03", "57-04", "57-10"]
files_modified:
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md
autonomous: true
requirements: [REL-08]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A live CI run — dispatched on the post-bump, post-CHANGELOG, post-migration-guide commit, not inherited from an earlier phase and not the pre-bump check run — reports `success` for every one of its jobs: the six `{ubuntu, windows, macos}-latest` x `{3.12, 3.13}` test lanes plus Lint and Format Check, Type Check, Code Coverage, Build Package, and both Integration Test lanes. This run is SC#3's authority for pytest, lint and type (D-12 run 2, D-13)."
    - "The commit CI proved green carries all four Wave-1 changes: the bumped version in `pyproject.toml`, a `## [0.9.0]` heading in `CHANGELOG.md`, the appended `RELEASE_VERSIONS` entry in `tests/test_changelog_page_gate.py`, and the `Migrating from 0.8.x to 0.9.0` section in `docs/source/changelog.rst` — each confirmed by its own grep before the push."
    - "Edge probe (category: ordering, REL-08) RESOLVED explicit: the `uv.lock` regeneration commit is a strict ancestor of the SHA this run is dispatched against, and `uv lock --check` exits 0 at that SHA — so the 10 `uv sync --extra dev --locked` steps across the four workflow files can install and a real test/lint/type signal is produced. Order matters absolutely here: dispatching before the lockfile lands fails at install and yields no signal at all, which is exactly how dependabot PRs #128 and #123 are failing."
    - "The Build Package job's \"Verify wheel carries the template bundle\" step ran on this exact commit and passed — SC#3's built-wheel content check, discharged by the CI run rather than by a separate local build, with its step output captured. The editable install used everywhere else in CI cannot detect a narrowed package-data glob because it never packs a wheel."
    - "Both dispatches are cross-referenced in one evidence file: run 1's SHA and job table beside run 2's, with an explicit statement of what changed between them, so a failure appearing only in run 2 is attributable to this phase's own edits and a failure present in both is not."
    - "The push is a plain fast-forward to an already-existing origin branch and the dispatch targets `ci.yml`, never `release.yml` — both reversible actions explicitly inside this phase's scope, and neither opens a pull request (D-12, prep/publish fence)."
    - "If any job fails, the failure is reported with its log excerpt and the plan stops; a retry is legitimate only after the cause is fixed, and every dispatch is recorded — the precedent at the previous close needed three runs (8/12, then 11/12, then 12/12) before acceptance, so run 2 must not be assumed final."
  artifacts:
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md"
      provides: "SC#3's toolchain-half authority — the pushed SHA, the dispatch, the run id and URL, the full per-job conclusion table, the wheel-verification step output, and the cross-reference against run 1"
      contains: "Run 2"
  key_links:
    - from: "the merged Wave-1 tip (bump + CHANGELOG + page gate + migration guide)"
      to: "the dispatched ci.yml authority run"
      via: "`gh workflow run ci.yml --ref gsd/v0.9.0-per-document-templates` after a fast-forward push; ci.yml's push and pull_request triggers cover only `main`/`develop`, so the run must be dispatched by hand and matched by headSha"
      pattern: "workflow_dispatch"
    - from: "uv.lock regenerated by plan 57-01"
      to: "every job's first step in this dispatch"
      via: "`uv sync --extra dev --locked` opens each job; the lock commit must be a strict ancestor of the dispatched SHA or the install fails before any signal exists — the ordering edge this phase resolves explicitly"
      pattern: "--locked"
    - from: "pyproject.toml's package-data glob"
      to: "the Build Package job's wheel-content verification step"
      via: "the step unzips the built wheel and asserts a bundled template-directory file is present; the editable install used everywhere else in CI never packs a wheel and so cannot detect a narrowed glob"
      pattern: "Verify wheel carries the template bundle"
  prohibitions:
    - statement: "No git tag named v0.9.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch. The dispatch in this plan targets ci.yml only."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged; a PR would also fire CI a second, unintended way and is the publish half's first step."
      status: unverified
      verification: flagged
    - statement: "No tag is created on the typsphinx-doc-translations repository and its pin is not advanced."
      status: unverified
      verification: flagged
    - statement: "The push is never a force push; only a plain fast-forward to the existing origin branch is permitted."
      status: unverified
      verification: flagged
    - statement: "No workflow file under .github/workflows/ is edited — this phase exercises the release machinery, never modifies it."
      status: unverified
      verification: flagged
    - statement: "No test gate is weakened, no marker removed and no skip guard loosened to turn a red job green."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed."
      status: unverified
      verification: flagged
    - statement: "REL-08's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped."
      status: unverified
      verification: flagged
    - statement: "No job conclusion is summarized or omitted; the complete job table is transcribed."
      status: unverified
      verification: flagged
  flagged_assumptions: []
---

<objective>
Dispatch and read D-12's **run 2** — the post-bump authority run — on the merged Wave-1 tip, and
cross-reference it against run 1 so that any new failure is attributable to this phase's own edits.

Purpose: discharges ROADMAP Phase 57 SC#3's toolchain half. Under D-13 the dispatched run is the
authority for pytest, `black`, `ruff`, `mypy` and the full py312–py313 x three-OS matrix, on D-12's
independent grounds: the Windows and macOS lanes are structurally unreachable from a local run and
have caught real defects at two consecutive closes. The run also carries the Build Package job's
wheel-content verification step, which is SC#3's built-wheel content check.

Output: `57-CI-EVIDENCE.md`'s `## Run 2` half — pushed SHA, dispatch, run id, URL, full job table,
the wheel-verification step output, and the two-run cross-reference.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-VALIDATION.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-01-SUMMARY.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-level list, repeated in every plan of this phase so each is readable standalone.

**Source tree (modified files and new symbols):**

| Artifact | Plan | Shape |
|---|---|---|
| `pyproject.toml` line 7 | 57-01 | the `version` key's value becomes the string `0.9.0` |
| `README.md` line 347 | 57-01 | the Status line reads `**Status**: Stable (v0.9.0) - Production ready` |
| `uv.lock` | 57-01 | regenerated; its first-party `typsphinx` block carries the new version |
| `CHANGELOG.md` — new `## [0.9.0] - <date>` section (lead paragraph + `### Added` / `### Changed` / `### Fixed` / `### Removed` / `### Verified`) | 57-03 | new section; `## [Unreleased]` keeps only its `### Planned for Future Releases` list |
| `CHANGELOG.md` tail link block | 57-03 | one inserted `[0.9.0]:` release-tag line above the existing topmost release line, and the `[Unreleased]` compare base advanced |
| `tests/test_changelog_page_gate.py` — `RELEASE_VERSIONS` gains a `"0.9.0"` entry (14 → 15) and its count/range comment moves | 57-03 | tuple + comment edit only |
| `docs/source/changelog.rst` — new `Migrating from 0.8.x to 0.9.0` subsection under `Migration Guides` | 57-04 | new prose section, deliberately ungated (D-07) |
| `docs/source/installation.rst`, `docs/source/contributing.rst`, `docs/source/examples/advanced.rst`, `examples/basic/README.md`, `examples/advanced/README.md` | 57-04 | **conditional** — edited ONLY if the execution-time D-10 discovery grep returns a hit; commit `70e24958` already corrected all five, so the expected diff is empty |
| `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` | 57-09 | dated evidence annotation appended; the record **stays in `pending/`** by owner decision |

**No new todo record is created by this phase.**

**Planning artifacts:**

| Path (under `.planning/phases/57-v0-9-0-release-prep-prep-only/`) | Plan |
|---|---|
| `57-BUMP-EVIDENCE.md` | 57-01 |
| `57-CLOSEOUT-GUARD.md` | 57-01 (baseline) — re-verified by 57-08 and 57-09 |
| `COVERAGE.md` | 57-01 |
| `57-CI-EVIDENCE.md` | 57-02 (pre-bump check run) → 57-05 (post-bump authority run) |
| `57-CHANGELOG-EVIDENCE.md` | 57-03 |
| `57-MIGRATION-EVIDENCE.md` | 57-04 |
| `57-GREEN-TREE-EVIDENCE.md` | 57-06 |
| `57-GOAL-CLAIM-EVIDENCE.md` | 57-07 |
| `57-SC4-INVARIANTS.md` | 57-08 |
| `57-HANDOFF.md` | 57-09 |

**Reserved name — never produced:** `57-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: Prove the lockfile ordering, push the merged Wave-1 tip, dispatch the authority run and read every job</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md</files>
  <precondition>All four Wave-1 plans have merged back, so HEAD carries the bumped version literal, a `## [0.9.0]` heading, the appended `RELEASE_VERSIONS` entry and the `Migrating from 0.8.x to 0.9.0` section. `origin/gsd/v0.9.0-per-document-templates` — which plan 57-02 fast-forwarded to the pre-bump tip — must still be an ancestor of HEAD so this push is also a plain fast-forward. `gh` is authenticated against this repository.</precondition>
  <reversibility rating="costly">A fast-forward push to an existing origin branch is undoable only by a force push, which this project forbids. It remains firmly inside the reversible half of the prep/publish fence: nothing is tagged, published or merged, and CONTEXT D-12 names it in scope explicitly.</reversibility>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — provisioning is mandatory before any command.
    - `.github/workflows/ci.yml` — the trigger block, the `os` x `python-version` matrix, every job's `name:`, the `uv sync --extra dev --locked` step that opens each job, and the Build Package job's wheel-content verification step. READ ONLY; this file must not be edited.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md` — run 1's recorded SHA, run id and job table, written by plan 57-02, so run 2 can be cross-referenced against it rather than described in isolation.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md` § "Pattern 4" and § "Pattern 5" — the dispatch sequence, the job-name set, and the wheel-verification step's exact shape.
    - `.planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md` — the recorded precedent that the previous close needed three dispatches (8/12 → 11/12 → 12/12) before acceptance.
  </read_first>
  <action>
**Do the push before writing any file in this worktree.** The push must publish exactly the merged
Wave-1 tip. If an evidence commit lands first, the authority run covers a commit the phase never
intended to prove and the "CI ran on this exact tree" claim becomes false.

Provision the worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

**Step 1 — confirm the tree is the one to prove.** Run and record each of these four greps:
`grep -n '^version = ' pyproject.toml` must show the new release value;
`grep -c '^## \[0\.9\.0\]' CHANGELOG.md` must return `1`;
`grep -c '"0\.9\.0",' tests/test_changelog_page_gate.py` must return `1`; and
`grep -c '^Migrating from 0\.8\.x to 0\.9\.0$' docs/source/changelog.rst` must return `1`. If any is
absent, STOP — Wave 1 has not merged back and this plan is running out of order.

**Step 2 — prove the ordering edge explicitly.** This is the single most dangerous ordering in the
phase. Run `uv lock --check` and confirm exit 0. Then identify the commit that last touched
`uv.lock` with `git log -1 --format='%H %cI %s' -- uv.lock`, and prove it is a strict ancestor of the
SHA about to be dispatched with
`git merge-base --is-ancestor "$(git log -1 --format=%H -- uv.lock)" HEAD && echo lock-precedes-dispatch`.
Record all three transcripts. Also re-run `grep -c locked .github/workflows/*.yml` and record its
output — count it here rather than transcribing a figure. If `uv lock --check` exits non-zero, or if
the ancestry check fails, STOP and do not dispatch: every CI job opens with
`uv sync --extra dev --locked`, so the install would fail before any test, lint or type signal
exists, and the run would measure nothing at all.

**Step 3 — re-measure the branch position.** Run
`git rev-list --count origin/gsd/v0.9.0-per-document-templates..HEAD` and
`git merge-base --is-ancestor origin/gsd/v0.9.0-per-document-templates HEAD && echo fast-forward-ok`.
Record whatever they print. Do NOT copy any figure from `57-CONTEXT.md`, `57-RESEARCH.md` or a
sibling plan. If the ancestry check fails, STOP: the push would not be a fast-forward and this plan
may not force it.

**Step 4 — push.** `git push origin HEAD:refs/heads/gsd/v0.9.0-per-document-templates`. Plain
fast-forward, never `--force`, never `--force-with-lease`. Record the pushed SHA with
`git rev-parse HEAD` and confirm `git rev-parse origin/gsd/v0.9.0-per-document-templates` now equals
it.

**Step 5 — dispatch and read.** `gh workflow run ci.yml --ref gsd/v0.9.0-per-document-templates`. Poll
`gh run list --workflow=ci.yml --branch gsd/v0.9.0-per-document-templates --limit 5 --json databaseId,headSha,event,status`
until a `workflow_dispatch` run whose `headSha` equals the pushed SHA appears — note that run 1's
entry is also in this list, so match on `headSha`, never on position. Export its `databaseId` as
`RUN_ID_2`. Wait with `gh run watch "$RUN_ID_2"`, then read every job's conclusion with
`gh run view "$RUN_ID_2" --json jobs`.

**Step 6 — capture the wheel-content check specifically.** SC#3 names Phase 54's built-wheel content
check by hand. It lives inside the Build Package job as a step that unzips the built wheel and
asserts a bundled template-directory file is present — the editable install used everywhere else in
CI never packs a wheel, so nothing else in the matrix can detect a narrowed package-data glob. Pull
that job's log with `gh run view "$RUN_ID_2" --log --job "$BUILD_JOB_ID"` (obtain `BUILD_JOB_ID` from
the `--json jobs` output) and capture the verification step's own output lines verbatim.

**Every job must be `success` — not only the Windows and macOS lanes.** If any job fails, do not
paper over it and do not weaken a gate: report the failure with `gh run view "$RUN_ID_2" --log-failed`
and stop. A retry is legitimate only after the cause is fixed; any fix commit must be pushed and a
fresh run dispatched, with EVERY run recorded in the evidence file. The previous close needed three
dispatches before acceptance — do not assume run 2 is the final one.

`ci.yml` must not be edited. Take no irreversible action: no tag, no tag push, no PyPI upload, no
GitHub Release, no pull request, and no dispatch of `release.yml`.
  </action>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = "$(git rev-parse origin/gsd/v0.9.0-per-document-templates)" && uv lock --check && git merge-base --is-ancestor "$(git log -1 --format=%H -- uv.lock)" HEAD && gh run view "$RUN_ID_2" --json jobs --jq '[.jobs[].conclusion]|unique|join(",")' | grep -qx 'success' && test -z "$(git tag -l v0.9.0)" && test -z "$(git ls-remote --tags origin v0.9.0)" && test -z "$(git diff --name-only -- typsphinx/ .github/)"</automated>
  </verify>
  <acceptance_criteria>
    - All four Step-1 greps returned their expected values and are recorded verbatim — the dispatched SHA carries the bump, the release section, the coverage-tuple entry and the migration guide.
    - `uv lock --check` exits 0, and `git merge-base --is-ancestor "$(git log -1 --format=%H -- uv.lock)" HEAD` exits 0 — the lockfile commit strictly precedes the dispatched SHA.
    - `git rev-parse origin/gsd/v0.9.0-per-document-templates` equals `git rev-parse HEAD` after the push.
    - `gh run view "$RUN_ID_2" --json jobs --jq '[.jobs[].conclusion]|unique|join(",")'` outputs exactly `success`.
    - The job list contains at least 12 entries, including a `windows-latest` lane and a `macos-latest` lane by name, plus jobs named for lint, type checking, coverage, build and both integration lanes.
    - `RUN_ID_2` differs from run 1's id recorded in `57-CI-EVIDENCE.md`, and its `headSha` differs from run 1's.
    - The Build Package job's wheel-verification step output is captured verbatim and reports success.
    - `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` both produce no output after the run.
    - `git diff --name-only -- typsphinx/ .github/` produces no output.
  </acceptance_criteria>
  <done>The merged Wave-1 tip is on origin behind a lockfile commit proven to precede it, a `workflow_dispatch` CI run completed all-green against exactly that SHA across all twelve jobs including both cross-platform lanes, and the built-wheel content check's own step output is captured.</done>
</task>

<task type="auto">
  <name>Task 2: Write the authority-run half of the CI evidence and cross-reference both dispatches</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md` — run 1's complete section, including its placeholder `## Run 2 — post-bump authority run` heading, which this task replaces in place.
    - `.planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md` — the job-table transcription format to reproduce.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-VALIDATION.md` § "Authority note" — the exact division of authority between CI and local runs that this section must restate.
  </read_first>
  <action>
Replace the `Written by plan 57-05.` placeholder line under `## Run 2 — post-bump authority run` in
`.planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md` with these subsections, in
English. Do not modify run 1's section.

- `### Pre-push confirmation` — Task 1 Step 1's four greps with their verbatim output, so the reader
  can see exactly which changes the dispatched commit carried.
- `### Ordering proof (the D-13 sequencing constraint)` — Step 2's `uv lock --check` transcript, the
  `git log -1 ... -- uv.lock` output, the `merge-base --is-ancestor` result, and the live
  `grep -c locked .github/workflows/*.yml` output with its measured total. Add one paragraph naming
  this the **ordering edge case** the phase resolves explicitly: the lockfile commit must strictly
  precede the dispatched SHA, because every CI job opens with `uv sync --extra dev --locked` and a
  stale lockfile fails the install before any test, lint or type signal exists — the measured failure
  mode of the two open dependabot pull requests. State that the step count was counted here rather
  than transcribed from a planning document.
- `### Branch position` — Step 3's two commands and their output, with a sentence recording that the
  figures were re-measured.
- `### Pushed SHA` and `### Push` — `git rev-parse HEAD`, the push transcript, and the
  `git rev-parse origin/...` confirmation.
- `### Dispatch` — the `gh workflow run` command and the matching `gh run list` JSON that identified
  `RUN_ID_2` by `headSha`, with one sentence noting that run 1 also appears in that list so the match
  was made on `headSha` rather than position.
- `### Job conclusions` — the complete name-and-conclusion table from `gh run view --json jobs`,
  **every job listed, none omitted**, with the Windows and macOS lanes individually visible, plus the
  run's `https://github.com/YuSabo90002/typsphinx/actions/runs/<id>` URL.
- `### Built-wheel content check (SC#3)` — the Build Package job's wheel-verification step output
  verbatim, and one sentence explaining why the editable install used elsewhere in CI cannot stand in
  for it.
- `### Why this run is the authority` — one paragraph restating D-13's split: CI covers the six-lane
  matrix, `black`/`ruff` lint and the type check; local runs cover both docs tox environments, the
  full-corpus gate and the multi-template PDF gate, collected by plans 57-06 and 57-07. State that
  local `ruff` runnability was re-measured this milestone and is an additive pre-flight only — it
  does not move authority off CI, because the Windows and macOS lanes are unreachable locally and
  have caught real defects at two consecutive closes.

Then add a final top-level section `## Cross-reference — what changed between the two runs`, holding:
a two-column table of run 1 versus run 2 (run id, headSha, event, conclusion, failing-job count); a
one-line statement of what the SHA range between them contains
(`git log --oneline <run1-sha>..<run2-sha>`, pasted); and one explicit sentence of attribution — a
job failing in BOTH runs is a pre-existing defect of the phases that preceded this one, and a job
failing only in run 2 is attributable to this phase's own edits. If every job in both runs is
`success`, say so plainly rather than leaving the attribution rule hypothetical.

If more than two dispatches were needed, record every one of them with its own job table under a
numbered subsection, in the order they ran, rather than reporting only the final green one.

Never assert a command's result from memory — run it and paste the transcript. Where a result is
genuinely unavailable, record `human_needed` honestly.
  </action>
  <verify>
    <automated>grep -q '^### Ordering proof (the D-13 sequencing constraint)' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md && grep -q '^### Built-wheel content check (SC#3)' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md && grep -q '^## Cross-reference — what changed between the two runs' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md && test "$(grep -c 'Written by plan 57-05\.' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md)" = "0" && test "$(grep -c 'actions/runs/' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md)" -ge "2"</automated>
  </verify>
  <acceptance_criteria>
    - `57-CI-EVIDENCE.md` contains the headings `### Pre-push confirmation`, `### Ordering proof (the D-13 sequencing constraint)`, `### Branch position`, `### Pushed SHA`, `### Push`, `### Dispatch`, `### Job conclusions`, `### Built-wheel content check (SC#3)`, `### Why this run is the authority`, and `## Cross-reference — what changed between the two runs`.
    - The placeholder line written by plan 57-02 no longer appears.
    - The `### Ordering proof` section contains a `uv lock --check` transcript, a `git merge-base --is-ancestor` result, a live `grep -c locked` output, and the literal `ordering edge case`.
    - The `### Job conclusions` section for run 2 names both `windows-latest` and `macos-latest` and lists at least 12 rows, all reading `success`.
    - The `### Built-wheel content check (SC#3)` section contains the verification step's own output lines.
    - The file contains at least two distinct `https://github.com/YuSabo90002/typsphinx/actions/runs/` URLs — one per dispatch.
    - The `## Cross-reference` section contains a `git log --oneline` range output and an explicit attribution sentence.
    - Run 1's section is byte-unchanged: `git diff -U0 -- .planning/phases/57-v0-9-0-release-prep-prep-only/57-CI-EVIDENCE.md | grep -c '^-'` returns `2` (the diff header and the single removed placeholder line).
    - `git diff --name-only -- typsphinx/ .github/ .planning/REQUIREMENTS.md` produces no output.
  </acceptance_criteria>
  <done>SC#3's toolchain-half authority is on disk: run 2's full job table with both cross-platform lanes green, the wheel-content step output, the explicit lockfile-precedes-dispatch ordering proof, and a two-run cross-reference that makes any failure attributable.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local milestone branch → `origin` | A push publishes commits; on this project it is reversible only by a force push, which is forbidden. |
| the workflow *selector* (`ci.yml` vs `release.yml`) | Dispatching the wrong workflow crosses the prep/publish fence irreversibly. |
| `uv.lock` at the dispatched SHA → every job's install step | A lockfile that does not precede the dispatch yields a run that measures nothing while appearing to have run. |
| the built wheel → PyPI at the publish | The wheel-content verification step is the only place in CI where the packaged artifact, rather than the editable install, is inspected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-24 | Tampering | dispatching `release.yml` instead of `ci.yml` | high | mitigate | The action names `ci.yml` at every step; prohibitions forbid a `release.yml` dispatch; acceptance asserts both tag probes are still empty after the run. |
| T-57-25 | Repudiation | claiming SC#3 green on a run that covered a different tree | high | mitigate | Four pre-push greps are recorded, `RUN_ID_2` is matched by `headSha` against the pushed SHA, `<verify>` asserts `origin/<branch>` equals HEAD, and acceptance requires run 2's id and headSha to differ from run 1's. |
| T-57-26 | Denial of service | a stale lockfile producing an install failure and no test signal | high | mitigate | `uv lock --check` and a `merge-base --is-ancestor` ordering proof are hard preconditions in `<verify>` and acceptance; the action instructs STOP rather than dispatch on failure. |
| T-57-27 | Tampering | a narrowed package-data glob shipping a wheel missing bundled template files | high | mitigate | The Build Package job's wheel-verification step is captured verbatim as SC#3's built-wheel content check; the editable install cannot substitute for it, and that is stated in the evidence. |
| T-57-28 | Tampering | a workflow file or test gate edited to turn a red job green | high | mitigate | `git diff --name-only -- .github/` asserted empty; prohibitions forbid weakening any gate; the action requires reporting a failure with its log rather than working around it. |
| T-57-29 | Repudiation | reporting only the final green dispatch and omitting earlier red ones | medium | mitigate | The action requires every dispatch recorded with its own job table in run order, citing the previous close's three-run history as precedent. |
| T-57-30 | Repudiation | REL-08 reported complete before the publish | high | mitigate | Prohibition plus `git diff --name-only -- .planning/REQUIREMENTS.md` asserted empty; `57-CLOSEOUT-GUARD.md` holds the digest baseline and plans 57-08 / 57-09 re-verify it. |
| T-57-SC | Tampering | npm/pip/cargo installs | low | accept | Nothing is installed beyond the existing lock's contents; `57-RESEARCH.md` records the Package Legitimacy Gate as not applicable — zero candidate packages. |
</threat_model>

<verification>
- `git rev-parse origin/gsd/v0.9.0-per-document-templates` equals `git rev-parse HEAD` after the push.
- `uv lock --check` exits 0 and the `uv.lock` commit is a strict ancestor of the dispatched SHA.
- `gh run view "$RUN_ID_2" --json jobs --jq '[.jobs[].conclusion]|unique|join(",")'` outputs exactly `success`.
- The Build Package job's wheel-verification step output is captured verbatim.
- Both dispatches appear in `57-CI-EVIDENCE.md` with distinct run URLs and a cross-reference table.
- `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` are both empty after the run.
- `git diff --name-only -- typsphinx/ .github/ .planning/REQUIREMENTS.md` produces no output.
</verification>

<success_criteria>
- D-12's post-bump authority run is all-green across all twelve jobs, including the Windows and macOS lanes, on the merged Wave-1 tip (SC#3 toolchain half).
- SC#3's built-wheel content check is discharged with its own step output captured.
- The D-13 ordering constraint is proven explicitly rather than assumed: the lockfile commit strictly precedes the dispatched SHA.
- Both dispatches are recorded and cross-referenced so any failure is attributable.
- No irreversible action was taken; the fence is still empty.
</success_criteria>

<output>
Create `.planning/phases/57-v0-9-0-release-prep-prep-only/57-05-SUMMARY.md` when done.
</output>
