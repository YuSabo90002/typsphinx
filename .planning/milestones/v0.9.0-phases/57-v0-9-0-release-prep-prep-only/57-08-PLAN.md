---
phase: 57-v0-9-0-release-prep-prep-only
plan: 08
type: execute
wave: 3
depends_on: ["57-05", "57-06", "57-07"]
files_modified:
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md
autonomous: true
requirements: [REL-08]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The milestone-diff sweep is anchored at the prior release tag and every figure in it is re-measured live at execution time — the shortstat, the commit count, the merge-base and the per-file diffs — never transcribed from `57-CONTEXT.md`, `57-RESEARCH.md` or a sibling plan (D-15)."
    - "The `### Verified` claim \"no new runtime dependencies\" is argued at HUNK level rather than by an empty diff, because `pyproject.toml` is NOT unchanged this milestone: its whole diff is quoted, shown to be confined to the packaging package-data glob, and shown to touch zero lines under the dependency arrays (D-15)."
    - "That claim carries a REAL positive control, not a restatement: the same command form is shown to produce non-empty output over a historical range that genuinely did change a dependency line, proving the detector can see a dependency change rather than being vacuously silent."
    - "The `@preview` package count invariant is verified live AND falsified live: the count command is shown returning four on the current tree, and the version-sync guard is shown turning RED against a deliberately perturbed working tree before being restored — with `git status --porcelain` asserted empty afterwards, so the perturbation left nothing behind."
    - "Phase 52's \"no new `typst_*` config value\" assertion is explicitly NOT carried forward: the milestone diff shows one config value removed and one added, and that fact is recorded as belonging to the CHANGELOG's own `### Removed` bullet and lead paragraph rather than to `### Verified` (D-15)."
    - "SC#4's fence proof is scoped to THIS PHASE's own diff, not the whole milestone: `git diff <phase-start-SHA>..HEAD -- typsphinx/` is empty, where the phase-start SHA is read from `57-BUMP-EVIDENCE.md`'s recorded measurement. The whole-milestone `typsphinx/` diff is deliberately large — it is the milestone's own content — and asserting it empty would be wrong."
    - "The prep/publish fence is observed and timestamped a second time, at a moment separated from plan 57-01's observation: `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` both produce no output (ROADMAP SC#4, observation 2 of 3)."
    - "The `REQUIREMENTS.md` checksum is re-verified against the baseline `57-CLOSEOUT-GUARD.md` recorded at phase head, and `git diff --name-only -- .planning/REQUIREMENTS.md` is empty — catching the `phase.complete` auto-flip that has fired at four consecutive release-prep closes rather than shipping it (ROADMAP SC#4)."
    - "Edge probe (category: adjacency, REL-08) RESOLVED explicit: the two candidate diff anchors are measured to coincide — `git merge-base origin/main HEAD` equals `git rev-parse origin/main`, both are ancestors of HEAD, and the `.planning`-excluded shortstat is identical at both anchors — so the sweep's conclusions do not depend on which anchor was chosen. If the two shortstats diverge at execution time, BOTH are recorded side by side with the divergence named, rather than one being silently preferred."
    - "Nothing under `typsphinx/`, `tests/` or `docs/` changed in this plan, and `.planning/REQUIREMENTS.md` is untouched; it measures and records."
  artifacts:
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md"
      provides: "SC#4's whole discharge — the milestone-diff invariant sweep with its hunk-level dependency argument and live positive controls, the phase-scoped fence proof, the second tag observation, and the REQUIREMENTS.md checksum re-verification"
      contains: "SC#4"
  key_links:
    - from: "git diff over the milestone range"
      to: "the CHANGELOG's `### Verified` three items"
      via: "the sweep is what those three published claims rest on; the first now needs a hunk-level argument because the manifest is not an empty diff this milestone"
      pattern: "package-data"
    - from: "the phase-start SHA recorded in 57-BUMP-EVIDENCE.md"
      to: "the fence proof's diff range"
      via: "SC#4's `no unintended typsphinx/ change` is about THIS phase, so the range starts at the phase's own head; using the milestone anchor instead would assert something deliberately false"
      pattern: "phase-start SHA"
    - from: "57-CLOSEOUT-GUARD.md's sha256sum baseline"
      to: "the state of .planning/REQUIREMENTS.md at this point in the phase"
      via: "a digest comparison plus an empty-name-only diff; the auto-flip has fired at four consecutive release-prep closes and is caught here rather than at the close"
      pattern: "sha256sum"
  prohibitions:
    - statement: "No git tag named v0.9.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered, nothing is uploaded to PyPI, and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged."
      status: unverified
      verification: flagged
    - statement: "No tag is created on the typsphinx-doc-translations repository and its pin is not advanced."
      status: unverified
      verification: flagged
    - statement: "REL-08's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped; if a diff against the baseline digest appears, it is reverted by hand and reported, never accepted."
      status: unverified
      verification: flagged
    - statement: "No figure is transcribed from 57-CONTEXT.md, 57-RESEARCH.md or a sibling plan into this sweep; every value is produced by a command shown beside it."
      status: unverified
      verification: flagged
    - statement: "Phase 52's `no new typst_* config value` assertion is not copied forward; this milestone removed one config value and added another."
      status: unverified
      verification: flagged
    - statement: "No positive control is a restatement of the invariant it claims to test; each must be an observation that would differ if the detector were vacuous."
      status: unverified
      verification: flagged
    - statement: "Any working-tree perturbation made to falsify a guard is reverted in the same task, and `git status --porcelain` is empty before the task's commit."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/, tests/ or docs/ is changed by this plan."
      status: unverified
      verification: flagged
  flagged_assumptions: []
---

<objective>
Discharge ROADMAP Phase 57 SC#4: sweep the milestone diff for the invariants the published
`### Verified` block asserts, prove the prep-only fence held over this phase's own diff, take the
second separated tag observation, and re-verify the `REQUIREMENTS.md` checksum against the baseline
recorded at phase head.

Purpose: two things make this sweep different from the previous release's. First, the manifest is not
an empty diff this milestone — the packaging package-data glob widened — so "no new runtime
dependency" needs a hunk-level argument instead of an absence, and it needs a positive control that
would fail if the check were vacuous. Second, SC#4 here is a **fence** criterion rather than an
invariant-sweep criterion, so its "no unintended `typsphinx/` change" clause is scoped to this
phase's own diff: the whole-milestone `typsphinx/` diff is deliberately large, and asserting it empty
would be asserting something false.

Output: `57-SC4-INVARIANTS.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-level list, repeated in every plan of this phase so each is readable standalone.

**Source tree (modified files and new symbols):**

| Artifact | Plan | Shape |
|---|---|---|
| `pyproject.toml` line 7 | 57-01 | the `version` key's value becomes the string `0.9.0` |
| `README.md` line 347 | 57-01 | the Status line reads `**Status**: Stable (v0.9.0) - Production ready` |
| `uv.lock` | 57-01 | regenerated; its first-party `typsphinx` block carries the new version |
| `CHANGELOG.md` — new `## [0.9.0] - <date>` section (lead paragraph + `### Added` / `### Changed` / `### Fixed` / `### Removed` / `### Verified`) | 57-03 | new section; `## [Unreleased]` keeps only its `### Planned for Future Releases` list |
| `CHANGELOG.md` tail link block | 57-03 | one inserted `[0.9.0]:` release-tag line above the existing topmost release line, and the `[Unreleased]` compare base advanced |
| `tests/test_changelog_page_gate.py` — `RELEASE_VERSIONS` gains a `"0.9.0"` entry (14 → 15) and its count/range comment moves | 57-03 | tuple + comment edit only |
| `docs/source/changelog.rst` — new `Migrating from 0.8.x to 0.9.0` subsection under `Migration Guides` | 57-04 | new prose section, deliberately ungated (D-07) |
| `docs/source/installation.rst`, `docs/source/contributing.rst`, `docs/source/examples/advanced.rst`, `examples/basic/README.md`, `examples/advanced/README.md` | 57-04 | **conditional** — edited ONLY if the execution-time D-10 discovery grep returns a hit; commit `70e24958` already corrected all five, so the expected diff is empty |
| `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` | 57-09 | dated evidence annotation appended; the record **stays in `pending/`** by owner decision |

**No new todo record is created by this phase.**

**Planning artifacts:**

| Path (under `.planning/phases/57-v0-9-0-release-prep-prep-only/`) | Plan |
|---|---|
| `57-BUMP-EVIDENCE.md` | 57-01 |
| `57-CLOSEOUT-GUARD.md` | 57-01 (baseline) — re-verified by 57-08 and 57-09 |
| `COVERAGE.md` | 57-01 |
| `57-CI-EVIDENCE.md` | 57-02 (pre-bump check run) → 57-05 (post-bump authority run) |
| `57-CHANGELOG-EVIDENCE.md` | 57-03 |
| `57-MIGRATION-EVIDENCE.md` | 57-04 |
| `57-GREEN-TREE-EVIDENCE.md` | 57-06 |
| `57-GOAL-CLAIM-EVIDENCE.md` | 57-07 |
| `57-SC4-INVARIANTS.md` | 57-08 |
| `57-HANDOFF.md` | 57-09 |

**Reserved name — never produced:** `57-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: Sweep the milestone diff and argue the three `### Verified` invariants at hunk level, each with a real positive control</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md</files>
  <precondition>All Wave-1 and Wave-2 plans have merged back, so the tree carries the bump, the CHANGELOG section, the migration guide and every evidence file. The prior release tag resolves locally and is an ancestor of HEAD.</precondition>
  <read_first>
    - `CHANGELOG.md`'s new `### Verified` block — the three claims this sweep exists to support, in their exact published wording.
    - `pyproject.toml` lines 60-85 — the `[tool.setuptools]` and `[tool.setuptools.package-data]` sections, and the `[project]` dependency arrays, so the hunk-level argument names real section boundaries.
    - `CLAUDE.md` § "The `@preview` version-sync hazard" — the three declaration sites the second `### Verified` item counts, and the note that the sync test asserts all three agree.
    - `tests/test_preview_version_sync.py` — the three test functions, so the live falsification in Step 4 perturbs something the guard actually reads.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md` § "Pattern 6" — the exact command set, the quoted manifest hunk, and the one-removed/one-added config-value finding.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md` § D-15 — why the anchor is the prior release tag, why the config-value assertion does not carry forward, and the instruction that a positive control must be real.
  </read_first>
  <action>
Provision first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Run everything
through `uv run`.

**Step 1 — anchors, and the adjacency question.** Run and capture, each with its command line:
`git rev-parse v0.8.0^{commit}`; `git rev-parse origin/main`; `git merge-base origin/main HEAD`;
`git merge-base --is-ancestor v0.8.0 HEAD && echo tag-is-ancestor`;
`git merge-base --is-ancestor origin/main HEAD && echo main-is-ancestor`;
`git rev-list --count v0.8.0..HEAD`;
`git diff v0.8.0..HEAD --stat -- . ':(exclude).planning' | tail -1`; and
`git diff "$(git rev-parse origin/main)"..HEAD --stat -- . ':(exclude).planning' | tail -1`.

State explicitly whether `git merge-base origin/main HEAD` equals `git rev-parse origin/main` (that
is, whether the two candidate anchors coincide) and whether the two shortstats are identical. This is
the **adjacency edge case** the phase resolves: when two anchors sit at exactly the same point, the
sweep's conclusions must not depend on which one was picked. If the two shortstats are identical,
record that the anchors coincide and that the choice is therefore immaterial. **If they differ**,
record BOTH shortstats side by side, name the commits in the gap
(`git log --oneline "$(git merge-base origin/main HEAD)"..v0.8.0` and the reverse), and say which
anchor the rest of the sweep uses and why — do not silently prefer one.

**Step 2 — the hunk-level runtime-dependency argument.** Run `git diff v0.8.0..HEAD -- pyproject.toml`
and quote the ENTIRE output. Then argue from it in prose: name the section the hunk falls in, note
that it is the whole manifest diff for the milestone, and state that it touches zero lines under
`[project]`'s dependency array or under `[project.optional-dependencies]`. Support that with a
targeted command whose output you paste — extract the dependency array from both sides and diff them,
e.g. by comparing `git show v0.8.0:pyproject.toml` and the working file through the same
`sed -n '/^dependencies = \[/,/^\]/p'` range — and show the result is empty.

**The positive control for this claim must be real.** An empty targeted diff proves nothing unless
the same command form can be shown to produce output when a dependency genuinely moved. Find a
historical range in this repository where a dependency line did change — locate one with
`git log --oneline -L '/^dependencies = \[/,/^\]/:pyproject.toml' | head -40` — and run the identical
extraction-and-diff form across that range, pasting its non-empty output. Record both, and one
sentence stating that the control shows the detector is discriminating rather than silent.

**Step 3 — the config-value delta, recorded but NOT promoted.** Run
`git diff v0.8.0..HEAD -- typsphinx/__init__.py | grep add_config_value` and paste the output. It
shows one config value removed and one added — this milestone's headline feature is the addition.
Record explicitly that the previous release-prep phase's "no new `typst_*` config value" assertion is
therefore **not carried forward**, that this fact belongs in the CHANGELOG's own `### Removed` bullet
and lead paragraph (plan 57-03) rather than in `### Verified`, and that copying the old assertion
forward unexamined would have published a false statement.

**Step 4 — the `@preview` invariant, verified and then falsified.** First verify: run
`grep -c "@preview" typsphinx/templates/base.typ` and paste the count, then
`grep -n "@preview" typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ`
and paste the full listing so the three declaration sites and their version strings are visible.
Then run `uv run pytest tests/test_preview_version_sync.py -v` and paste the transcript.

Then falsify, to prove the guard is load-bearing rather than merely currently-green. Perturb ONE
version string in ONE of the three declaration sites in the working tree, re-run
`uv run pytest tests/test_preview_version_sync.py -v`, and confirm it turns RED with the mismatch
named. **Immediately restore** with `git checkout -- <that file>`, re-run the guard to confirm it is
green again, and run `git status --porcelain` to confirm the working tree is clean. Paste all four
transcripts — green, perturbed-RED, restored-green, and the clean status. If the perturbation does
NOT turn the guard red, that is a real finding about the guard, not a formality to skip: record it
and escalate.

Do not perturb anything else, do not leave a perturbation in place, and do not commit while a
perturbation exists.

Nothing under `typsphinx/`, `tests/` or `docs/` may remain changed when this task ends. Take no
irreversible action.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor v0.8.0 HEAD && test "$(grep -c '@preview' typsphinx/templates/base.typ)" = "4" && uv run pytest tests/test_preview_version_sync.py -q && diff <(git show v0.8.0:pyproject.toml | sed -n '/^dependencies = \[/,/^\]/p') <(sed -n '/^dependencies = \[/,/^\]/p' pyproject.toml) && test -z "$(git status --porcelain -- typsphinx/ tests/ docs/)"</automated>
  </verify>
  <acceptance_criteria>
    - Every figure in the sweep is accompanied by the command that produced it in the same section; no figure appears without one.
    - The anchor section states explicitly whether `git merge-base origin/main HEAD` equals `git rev-parse origin/main`, and whether the two `.planning`-excluded shortstats are identical; if they differ, both are recorded with the intervening commits named.
    - `git diff v0.8.0..HEAD -- pyproject.toml` is quoted in full, and the dependency-array extraction diff produces no output.
    - A positive control for the dependency check is recorded: the same extraction-and-diff form run across a historical range produces NON-EMPTY output, pasted verbatim.
    - `git diff v0.8.0..HEAD -- typsphinx/__init__.py | grep add_config_value` output is pasted, and the record states that the prior phase's "no new `typst_*` config value" assertion is not carried forward.
    - `grep -c "@preview" typsphinx/templates/base.typ` returns `4`, and the three declaration sites' `@preview` lines are listed with their version strings.
    - Four `tests/test_preview_version_sync.py` transcripts are recorded in order: green, RED under a deliberate one-string perturbation with the mismatch named, green again after restore, and a `git status --porcelain` showing a clean tree.
    - `git status --porcelain -- typsphinx/ tests/ docs/` produces no output at task end.
  </acceptance_criteria>
  <done>The milestone-diff sweep is complete with every figure measured live, the runtime-dependency claim argued at hunk level and backed by a control that proves the check is discriminating, the config-value delta recorded and deliberately kept out of `### Verified`, and the `@preview` guard proven load-bearing by a live falsification that left the tree clean.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the fence over this phase's own diff, take the second tag observation, and re-verify the REQUIREMENTS.md checksum</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md</files>
  <read_first>
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-BUMP-EVIDENCE.md` § "Phase-head anchor re-measurement" — the recorded **phase-start SHA**, which is the left side of this task's fence-proof diff range.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md` — the `sha256sum` baseline, REL-08's verbatim guarded lines, and the re-verification protocol naming this plan as one of its two checkpoints.
    - `.planning/ROADMAP.md` § "Phase 57" success criterion 4 — the exact clauses: no local or remote tag, no publish, probed and recorded twice at separated times, no unintended `typsphinx/` change, and the checksum.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md` § "Pattern 6" (the fence-proof scope correction) and § "Pitfall 5" (the auto-flip's four-close history).
  </read_first>
  <action>
**Step 1 — read the phase-start SHA from the record, not from memory.** Extract it from
`57-BUMP-EVIDENCE.md`'s anchor section and echo it. If it is absent or ambiguous there, STOP and
report — reconstructing it by guesswork would make the fence proof meaningless.

**Step 2 — the fence proof, correctly scoped.** Run `git diff <phase-start-SHA>..HEAD -- typsphinx/`
and confirm it produces NO output. Also run `git diff <phase-start-SHA>..HEAD --stat -- . ':(exclude).planning'`
and paste the full result: the only source paths listed must be the release surfaces this phase
deliberately touched — the manifest, the lockfile, the repository README, the changelog, the
published changelog page, and the page-gate test module (plus, only if plan 57-04's live grep found a
hit, the prose file it fixed). Name each listed path and why it is there. Any path you cannot account
for is a finding: report it rather than absorbing it.

Record explicitly, in one sentence, why this range and not the milestone range: SC#4's fence clause is
about what THIS PHASE changed, and the whole-milestone `typsphinx/` diff is deliberately large — it is
the milestone's own content — so asserting that empty would assert something false. Paste
`git diff v0.8.0..HEAD --stat -- typsphinx/ | tail -1` alongside, to show the contrast concretely.

**Step 3 — the second fence observation.** Run `date -u`, then `git tag -l v0.9.0` and
`git ls-remote --tags origin v0.9.0`, and paste all three. Both probes must produce no output. Also
run `gh release list --limit 5` and paste it, confirming no release exists for this version, and
`gh run list --workflow=release.yml --limit 5` confirming the release workflow has not run. This is
observation 2 of 3; plan 57-01 took the first at phase head and plan 57-09 takes the third at the
handoff, at a third separated time.

**Step 4 — re-verify the closeout guard.** Run `sha256sum .planning/REQUIREMENTS.md` and compare it
against the baseline digest recorded in `57-CLOSEOUT-GUARD.md`; run
`git diff --name-only -- .planning/REQUIREMENTS.md` and confirm it is empty; and re-quote REL-08's
checkbox line from the live file to confirm it is byte-identical to the guarded quote.

If the digest differs or the diff is non-empty, the `phase.complete` auto-flip has fired. Do not
accept it: revert by hand with `git checkout -- .planning/REQUIREMENTS.md`, re-run both checks,
record the whole incident — what changed, when, and the revert — in the evidence file, and report it
in the plan summary. This has fired at four consecutive release-prep closes; treat a clean result as
the outcome to verify, not the outcome to assume.

Take no irreversible action. Do not edit `.planning/REQUIREMENTS.md` for any reason other than
reverting an unintended change back to the guarded baseline.
  </action>
  <verify>
    <automated>PS=$(grep -oE '\b[0-9a-f]{40}\b' .planning/phases/57-v0-9-0-release-prep-prep-only/57-BUMP-EVIDENCE.md | head -1) && test -n "$PS" && test -z "$(git diff "$PS"..HEAD -- typsphinx/)" && test -z "$(git tag -l v0.9.0)" && test -z "$(git ls-remote --tags origin v0.9.0)" && test -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" && sha256sum -c <(awk '/^[0-9a-f]{64}  \.planning\/REQUIREMENTS\.md$/{print; exit}' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md)</automated>
  </verify>
  <acceptance_criteria>
    - The phase-start SHA used in the fence-proof range is quoted from `57-BUMP-EVIDENCE.md`, not reconstructed.
    - `git diff <phase-start-SHA>..HEAD -- typsphinx/` produces no output.
    - `git diff <phase-start-SHA>..HEAD --stat -- . ':(exclude).planning'` is pasted in full and every listed source path is named with a reason; no unaccounted path remains.
    - `git diff v0.8.0..HEAD --stat -- typsphinx/ | tail -1` is pasted alongside, showing a non-empty milestone-scope diff, with one sentence explaining why the two ranges differ.
    - `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` both produce no output, recorded with a `date -u` timestamp distinct from plan 57-01's.
    - `gh release list` and `gh run list --workflow=release.yml` outputs are pasted and show no release and no release-workflow run for this version.
    - `sha256sum .planning/REQUIREMENTS.md` matches the baseline digest in `57-CLOSEOUT-GUARD.md`, and `git diff --name-only -- .planning/REQUIREMENTS.md` produces no output.
    - REL-08's checkbox line quoted from the live file is byte-identical to the guarded quote in `57-CLOSEOUT-GUARD.md`.
  </acceptance_criteria>
  <done>The prep-only fence is proven over this phase's own diff with every touched path accounted for, the tag and release probes are empty at a second separated timestamp, and the `REQUIREMENTS.md` digest still matches the baseline recorded at phase head.</done>
</task>

<task type="auto">
  <name>Task 3: Write the SC#4 invariants and fence record</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md</files>
  <read_first>
    - `.planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md` — the heading structure and register to adapt, noting the two scope differences this milestone introduces (the non-empty manifest diff, and the phase-scoped rather than milestone-scoped fence proof).
    - `CHANGELOG.md`'s `### Verified` block as published by plan 57-03 — so each section of this file is labelled with the claim it supports.
  </read_first>
  <action>
Write `.planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md` in English with the
heading `# Phase 57 — SC#4 Invariants and Fence Proof` and these sections, each holding the verbatim
transcripts produced in Tasks 1 and 2:

- `## Anchors, and the adjacency question` — Task 1 Step 1's eight commands and their output, the
  explicit statement of whether the two candidate anchors coincide, and the divergence protocol if
  they did not. Label this the **adjacency edge case** this phase resolves.
- `## Verified item 1 — no new runtime dependencies (hunk-level argument)` — the full manifest diff,
  the dependency-array extraction diff, the prose argument, and the historical positive control with
  its non-empty output. One sentence stating that this claim could be proven by an empty diff at every
  previous release and cannot be this time.
- `## Verified item 2 — the `@preview` package count and version lockstep` — the count, the three-site
  listing, and the four-transcript falsification sequence (green, RED, green, clean status) with one
  sentence stating that a green gate is not by itself proof that a gate is load-bearing.
- `## Verified item 3 — the full-corpus re-run` — a pointer to `57-GREEN-TREE-EVIDENCE.md`'s
  `Outcome:` line rather than a restatement, and, if that outcome was SKIPPED, an explicit note that
  the published claim is not locally re-proven this phase.
- `## Not carried forward — the config-value assertion` — the `add_config_value` diff output and the
  statement that the prior phase's assertion does not apply to this milestone.
- `## SC#4 fence — this phase's own diff` — the phase-start SHA quoted from `57-BUMP-EVIDENCE.md`,
  the empty `typsphinx/` diff, the full `--stat` listing with every path accounted for, and the
  contrasting milestone-scope figure with the one-sentence explanation of the scope difference.
- `## SC#4 fence — observation 2 of 3` — the `date -u` stamp, both tag probes, the `gh release list`
  and `gh run list --workflow=release.yml` outputs, and one sentence naming `57-BUMP-EVIDENCE.md` and
  `57-HANDOFF.md` as the other two observations.
- `## SC#4 — REQUIREMENTS.md closeout guard, re-verified` — the digest comparison, the empty
  name-only diff, the re-quoted REL-08 checkbox line, and — if the auto-flip fired — the full
  incident record and revert transcript. State plainly that REL-08 remains open and closes at
  `/gsd-complete-milestone`.
- `## Method note` — one short paragraph recording that every figure in this file was produced by a
  command shown beside it, that no value was transcribed from `57-CONTEXT.md`, `57-RESEARCH.md` or a
  sibling plan, and that both positive controls are observations that would differ if their detector
  were vacuous rather than restatements of the invariants they test.

Never assert a command's result from memory. Where a result is genuinely unavailable, record
`human_needed` honestly.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md && grep -q '^## Anchors, and the adjacency question' .planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md && grep -q '^## SC#4 fence — this phase.s own diff' .planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md && grep -q '^## SC#4 fence — observation 2 of 3' .planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md && grep -q '^## SC#4 — REQUIREMENTS.md closeout guard, re-verified' .planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md && grep -q '^## Method note' .planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md && grep -q 'adjacency edge case' .planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md && test -z "$(git diff --name-only -- typsphinx/ tests/ docs/ .planning/REQUIREMENTS.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `57-SC4-INVARIANTS.md` exists, is written entirely in English, and contains all nine headings listed in the action.
    - It contains the literal `adjacency edge case` and an explicit statement of whether the two anchors coincide.
    - The runtime-dependency section contains both an empty targeted diff and a non-empty positive-control output.
    - The `@preview` section contains four distinct pytest transcripts in the order green / RED / green / clean-status.
    - The fence section contains a 40-character phase-start SHA quoted from `57-BUMP-EVIDENCE.md` and an empty `typsphinx/` diff for that range, alongside a non-empty milestone-scope figure.
    - The observation-2 section carries a `date -u` timestamp differing from the one in `57-BUMP-EVIDENCE.md`, and names `57-HANDOFF.md` as the third observation's owner.
    - The closeout-guard section states that REL-08 remains open and closes at `/gsd-complete-milestone`.
    - `git diff --name-only -- typsphinx/ tests/ docs/ .planning/REQUIREMENTS.md` produces no output.
  </acceptance_criteria>
  <done>SC#4's whole discharge is on disk: the anchor adjacency resolved, all three `### Verified` claims argued from live commands with real positive controls, the config-value assertion explicitly retired, the fence proven over this phase's own diff with every path accounted for, a second separated tag observation, and the `REQUIREMENTS.md` digest re-verified against its phase-head baseline.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the milestone diff → the published `### Verified` claims | Three statements shipped to users rest entirely on this sweep; an unexamined claim carried forward from a prior release would publish a falsehood. |
| this phase's own diff → the prep-only fence | A source change riding in under release-prep work would ship unreviewed by the phase that owns the code. |
| `.planning/REQUIREMENTS.md` → the milestone-close record | An auto-flipped checkbox asserts a publish that has not happened. |
| the working tree during a falsification test | A perturbation left in place, or committed, would corrupt the release tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-42 | Repudiation | a published `### Verified` claim supported by a vacuous check | high | mitigate | The runtime-dependency claim is argued at hunk level from the quoted whole diff and backed by a historical positive control that produces non-empty output; the `@preview` claim is backed by a live RED falsification. Both are asserted in acceptance. |
| T-57-43 | Repudiation | the prior release's config-value assertion copied forward unexamined | high | mitigate | The `add_config_value` diff is pasted and the record must state the assertion is not carried forward; a prohibition names it. |
| T-57-44 | Repudiation | the fence proof scoped to the wrong range, asserting something deliberately false | high | mitigate | The phase-start SHA is read from `57-BUMP-EVIDENCE.md` and the contrasting milestone-scope figure is pasted beside it with an explanation; `<verify>` extracts the SHA from the record rather than accepting a literal. |
| T-57-45 | Tampering | a working-tree perturbation left behind or committed after a falsification test | high | mitigate | The action requires an immediate restore, a re-run proving green, and a clean `git status --porcelain`; `<verify>` asserts the clean status and a prohibition forbids committing while a perturbation exists. |
| T-57-46 | Repudiation | REL-08 auto-flipped and shipped | high | mitigate | The digest is re-verified against `57-CLOSEOUT-GUARD.md` and the name-only diff asserted empty in `<verify>`; the action specifies a hand revert and a full incident record if it fired. |
| T-57-47 | Tampering | the prep/publish fence crossed | high | mitigate | Both tag probes plus `gh release list` and `gh run list --workflow=release.yml` are recorded at a second separated timestamp; prohibitions enumerate every forbidden action. |
| T-57-48 | Repudiation | a figure transcribed from a planning document rather than measured | medium | mitigate | The `## Method note` section is required, and acceptance asserts every figure appears beside its own command. |
| T-57-SC | Tampering | npm/pip/cargo installs | low | accept | Only `uv sync --extra dev` runs, against the existing lock; no package is added. `57-RESEARCH.md` records the Package Legitimacy Gate as not applicable. |
</threat_model>

<verification>
- The prior release tag is an ancestor of HEAD and both candidate anchors' shortstats are recorded.
- The manifest's whole milestone diff is quoted and the dependency-array extraction diff is empty, with a non-empty historical positive control beside it.
- `grep -c "@preview" typsphinx/templates/base.typ` returns `4`, and the version-sync guard is shown green, RED under perturbation, and green again after restore, with a clean working tree.
- `git diff <phase-start-SHA>..HEAD -- typsphinx/` produces no output.
- `git tag -l v0.9.0`, `git ls-remote --tags origin v0.9.0`, `gh release list` and `gh run list --workflow=release.yml` all show no v0.9.0 artefact.
- `sha256sum .planning/REQUIREMENTS.md` matches the baseline and `git diff --name-only -- .planning/REQUIREMENTS.md` is empty.
</verification>

<success_criteria>
- All three published `### Verified` claims are supported by live commands, with the dependency claim argued at hunk level and both invariant checks backed by real positive controls (SC#4, D-15).
- The anchor adjacency question is resolved explicitly rather than assumed.
- The prior phase's config-value assertion is retired with its measurement recorded.
- The prep-only fence is proven over this phase's own diff, with every touched path accounted for.
- The second separated tag observation and the `REQUIREMENTS.md` digest re-verification are on disk.
</success_criteria>

<output>
Create `.planning/phases/57-v0-9-0-release-prep-prep-only/57-08-SUMMARY.md` when done.
</output>
