---
phase: 57-v0-9-0-release-prep-prep-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - uv.lock
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-BUMP-EVIDENCE.md
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md
  - .planning/phases/57-v0-9-0-release-prep-prep-only/COVERAGE.md
autonomous: true
requirements: [REL-08]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`pyproject.toml` line 7 is the sole release-version literal in the manifest; `README.md`'s Status line and `uv.lock`'s own first-party `typsphinx` block moved to `0.9.0` with it in one edit set, and no second hardcoded version string was introduced (ROADMAP SC#1)."
    - "`uv run python -c \"import typsphinx; print(typsphinx.__version__)\"` prints `0.9.0` — the editable-install `.dist-info`/`.pth` metadata was regenerated by `uv sync`, not merely the manifest literal edited (ROADMAP SC#1)."
    - "`uv lock --check` and `uv sync --extra dev --locked` both exit 0 on the post-bump tree, which is the D-13 sequencing precondition every later CI dispatch depends on: 10 `uv sync --locked` steps across `ci.yml`/`release.yml`/`docs.yml`/`drift.yml` fail at install — before any test, lint or type signal exists — against a stale lockfile."
    - "The three version-sync guard modules (`tests/test_extension.py::test_version_matches_pyproject_toml`, `tests/test_readme_version_sync.py`, `tests/test_preview_version_sync.py`) are green on the post-bump tree with zero skips."
    - "`scripts/extract_changelog_section.py` — the ONE extractor `release.yml`'s `validate` and `create-release` jobs both call — is proven live on this tree in BOTH directions (exit 0 on an already-published version, exit 1 on an absent one) before plan 57-03 authors the `## [0.9.0]` section it will read."
    - "The prep/publish fence is observed and timestamped at phase head: `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` both produce no output (ROADMAP SC#4, first of three separated observations; 57-08 and 57-09 carry the other two)."
    - "A `sha256sum .planning/REQUIREMENTS.md` baseline and REL-08's exact checkbox line and Traceability row are recorded verbatim in `57-CLOSEOUT-GUARD.md` at phase head, so the `phase.complete` auto-flip — which has fired against this requirement shape at four consecutive release-prep closes — is caught and reverted rather than shipped (ROADMAP SC#4)."
    - "Every anchor figure this phase depends on — the `v0.8.0` commit SHA, the `origin/main` merge-base, the commits-ahead count and the `.planning`-excluded shortstat — is RE-MEASURED live at execution time with its command shown, never transcribed from `57-CONTEXT.md` or `57-RESEARCH.md`, both of which carry figures already known stale."
    - "Nothing under `typsphinx/` changed in this plan — the prep-only fence holds, and `54.1-REVIEW.md` WR-01 and WR-02 both ship unfixed by decision (D-09, D-10)."
    - "`COVERAGE.md` exists in the phase directory as a reasoned, matrix-free no-external-API declaration, so the seal-time `api-coverage.verify-pre` re-run over this phase's `gh run view` / `git ls-remote` / `release.yml`-dense PLAN bodies cannot block on this project's standing false-positive shape."
  artifacts:
    - path: "pyproject.toml"
      provides: "the sole version literal for the v0.9.0 release"
      contains: "0.9.0"
    - path: "README.md"
      provides: "the human-readable release status line, moved in lockstep"
      contains: "Stable (v0.9.0)"
    - path: "uv.lock"
      provides: "the regenerated lock whose first-party typsphinx entry carries the new version, satisfying D-13's sequencing precondition for every later CI dispatch"
      contains: "0.9.0"
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-BUMP-EVIDENCE.md"
      provides: "SC#1's verbatim transcripts plus the phase-head anchor re-measurement and the first fence observation"
      contains: "SC#1"
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md"
      provides: "the REQUIREMENTS.md checksum baseline and REL-08's verbatim lines that SC#4 requires be recorded at the phase's start"
      contains: "REL-08"
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/COVERAGE.md"
      provides: "the reasoned external-API coverage declaration the seal-time gate accepts in place of a fabricated matrix"
      contains: "No external API integration"
  key_links:
    - from: "pyproject.toml `[project] version`"
      to: "typsphinx.__version__"
      via: "importlib.metadata.version(\"typsphinx\") reads the installed .dist-info, which only `uv sync` regenerates — editing the manifest literal alone does not move it"
      pattern: "importlib\\.metadata"
    - from: "pyproject.toml `[project] version`"
      to: "README.md Status line"
      via: "tests/test_readme_version_sync.py regex-parses the Status line and compares it against tomllib's parse of pyproject.toml, so the two can never be edited independently"
      pattern: "Stable \\(v0\\.9\\.0\\)"
    - from: "uv.lock"
      to: "every dispatched CI job"
      via: "10 `uv sync --extra dev --locked` steps across ci.yml (6), release.yml (2), docs.yml (1) and drift.yml (1) refuse a lockfile that disagrees with the manifest and fail before any test runs — D-13's hard sequencing constraint, and the exact failure mode killing dependabot PRs #128 and #123 right now"
      pattern: "--locked"
    - from: "CHANGELOG.md `## [X.Y.Z]` headings"
      to: "release.yml's `validate` job"
      via: "scripts/extract_changelog_section.py is the ONE committed, pytest-covered extractor both `validate` and `create-release` call; this plan proves it runs against this tree in both directions before plan 57-03 authors the section it will read"
      pattern: "extract_changelog_section"
  prohibitions:
    - statement: "No git tag named v0.9.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged."
      status: unverified
      verification: flagged
    - statement: "No tag is created on the typsphinx-doc-translations repository and its pin is not advanced."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed — 54.1-REVIEW WR-01 and WR-02 both ship unfixed by decision D-09 and D-10."
      status: unverified
      verification: flagged
    - statement: "REL-08's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped; the requirement stays open until the publish."
      status: unverified
      verification: flagged
    - statement: "No test gate is weakened — no skip guard loosened, no marker removed, no assertion deleted — to make a run report green."
      status: unverified
      verification: flagged
    - statement: "No figure is transcribed from 57-CONTEXT.md or 57-RESEARCH.md into an acceptance criterion or an evidence file; every measured value is re-run live at execution time."
      status: unverified
      verification: flagged
    - statement: "No prohibition above is read as forbidding the two reversible actions D-12 depends on — a plain fast-forward push of the milestone branch to origin, and a workflow_dispatch of ci.yml on that branch."
      status: unverified
      verification: flagged
  flagged_assumptions: []
---

<objective>
The phase's tracer: prove the whole release-surface path end to end on one thin slice — the version
literal moves to `0.9.0` across all three surfaces, the lockfile is regenerated so every later CI
dispatch has something installable, the editable-install metadata is regenerated so
`typsphinx.__version__` actually reports the new value, the three version-sync guard modules stay
green, and the release machinery's own extractor still runs against this tree in both directions —
with the prep/publish fence, the `REQUIREMENTS.md` closeout guard and every anchor figure recorded
live before anything is edited.

Purpose: discharges ROADMAP Phase 57 SC#1, lays down SC#4's first fence observation and its
`REQUIREMENTS.md` checksum baseline, and satisfies D-13's hard sequencing precondition — the
regenerated, committed `uv.lock` that plans 57-02 and 57-05 both dispatch against. This is the
phase's tracer because it is the only slice touching every layer the phase will modify — the release
manifest, the lockfile, the installed package metadata, the pytest guard tier, and the release
machinery consumer — end to end, on one path, for keeps. Nothing here is a stub.

Output: a post-bump tree whose manifest, lockfile, README status line and installed metadata all
agree; `57-BUMP-EVIDENCE.md`; `57-CLOSEOUT-GUARD.md`; and `COVERAGE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-level list, repeated in every plan of this phase so each is readable standalone.

**Source tree (modified files and new symbols):**

| Artifact | Plan | Shape |
|---|---|---|
| `pyproject.toml` line 7 | 57-01 | the `version` key's value becomes the string `0.9.0` |
| `README.md` line 347 | 57-01 | the Status line reads `**Status**: Stable (v0.9.0) - Production ready` |
| `uv.lock` | 57-01 | regenerated; its first-party `typsphinx` block carries the new version |
| `CHANGELOG.md` — new `## [0.9.0] - <date>` section (lead paragraph + `### Added` / `### Changed` / `### Fixed` / `### Removed` / `### Verified`) | 57-03 | new section; `## [Unreleased]` keeps only its `### Planned for Future Releases` list |
| `CHANGELOG.md` tail link block | 57-03 | one inserted `[0.9.0]:` release-tag line above the existing topmost release line, and the `[Unreleased]` compare base advanced |
| `tests/test_changelog_page_gate.py` — `RELEASE_VERSIONS` gains a `"0.9.0"` entry (14 → 15) and its count/range comment moves | 57-03 | tuple + comment edit only |
| `docs/source/changelog.rst` — new `Migrating from 0.8.x to 0.9.0` subsection under `Migration Guides` | 57-04 | new prose section, deliberately ungated (D-07) |
| `docs/source/installation.rst`, `docs/source/contributing.rst`, `docs/source/examples/advanced.rst`, `examples/basic/README.md`, `examples/advanced/README.md` | 57-04 | **conditional** — edited ONLY if the execution-time D-10 discovery grep returns a hit; commit `70e24958` already corrected all five, so the expected diff is empty |
| `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` | 57-09 | dated evidence annotation appended; the record **stays in `pending/`** by owner decision |

**No new todo record is created by this phase.** The 56-REVIEW filing exists at
`.planning/todos/completed/2026-08-16-stale-version-prerequisites-and-dead-config-link-in-published-docs.md`;
the CONTEXT's "re-file the missing todo" item was retracted as a method artifact.

**Planning artifacts (evidence family, adapted from Phase 52's):**

| Path (under `.planning/phases/57-v0-9-0-release-prep-prep-only/`) | Plan |
|---|---|
| `57-BUMP-EVIDENCE.md` | 57-01 |
| `57-CLOSEOUT-GUARD.md` | 57-01 (baseline) — re-verified by 57-08 and 57-09 |
| `COVERAGE.md` | 57-01 |
| `57-CI-EVIDENCE.md` | 57-02 (pre-bump check run) → 57-05 (post-bump authority run) |
| `57-CHANGELOG-EVIDENCE.md` | 57-03 |
| `57-MIGRATION-EVIDENCE.md` | 57-04 |
| `57-GREEN-TREE-EVIDENCE.md` | 57-06 |
| `57-GOAL-CLAIM-EVIDENCE.md` | 57-07 |
| `57-SC4-INVARIANTS.md` | 57-08 |
| `57-HANDOFF.md` | 57-09 |

**Reserved name — never produced:** no artifact in this phase may be named `57-VERIFICATION.md`.
`gsd-verifier` owns and clobbers that exact filename (46-CONTEXT D-15).

<assumption_delta_decision>
`gsd-tools query assumption-delta scan 57 --json` returned `detected: true` with one
`pluralization` signal on the term `second`, from the ROADMAP prose "including the standing
**second**-repository tag".

- **Noun now primary:** the *release target repository* — a release tags two repositories, the
  parent and `typsphinx-doc-translations`.
- **Decision:** `no-change`. This is not a live identity-model transition in Phase 57. The
  second-repository tag was **promoted** to a standing release cost at the v0.6.4 close (D-07) and
  has been discharged at every close since; `57-HANDOFF.md` (plan 57-09) records it as an ordinary
  checklist item, not as a newly discovered plurality. This phase makes no modelling decision and
  changes nothing under `typsphinx/`.
- **What would force a later promote:** a third publication target (a mirror index, or a second
  translation repository) would turn the hand-maintained two-item checklist into a set that needs
  enumeration rather than repetition. Nothing in the v0.9.0 scope adds one.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end release-surface slice — record the fence, the closeout guard's anchors and the live anchor figures, then move the version across all three surfaces and regenerate the lock and the editable install</name>
  <files>pyproject.toml, README.md, uv.lock</files>
  <precondition>The working tree is on the milestone branch `gsd/v0.9.0-per-document-templates` with no uncommitted source changes, `gh` is authenticated against this repository, and `git tag -l v0.9.0` is empty before any edit is made. If a `v0.9.0` tag already exists locally or on origin, STOP — the publish half has already run and this phase must not proceed.</precondition>
  <reversibility rating="reversible">A manifest literal, a lockfile entry and a README status line are all plain tracked-file edits; reverting is one `git revert` plus one `uv sync`.</reversibility>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — provisioning is MANDATORY on this project, not conditional on parallelism. Without `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` followed by `uv run` for every command, pytest imports the UNCHANGED main-tree package and the guard tests report against the wrong tree.
    - `pyproject.toml` lines 1-80 — the `[project]` table (the `version` key at line 7, `requires-python` at line 10, the `sphinx` pin at line 28), the `dependencies` array, the optional-dependency extras, and the `[tool.setuptools.package-data]` hunk at lines 70-77 that plan 57-08's sweep must account for.
    - `README.md` lines 340-352 — the `**Status**` line at line 347 and the `**Python**` / `**Sphinx**` / `**Typst**` line below it, which must NOT move.
    - `typsphinx/__init__.py` lines 1-30 — the `importlib.metadata.version("typsphinx")` derivation and its `PackageNotFoundError` fallback. READ ONLY; this file must not be edited.
    - `tests/test_readme_version_sync.py` — the assertion coupling the two literals.
    - `scripts/extract_changelog_section.py` — the extractor `release.yml`'s `validate` and `create-release` jobs both call, and its documented no-side-effects property.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md` § "`pyproject.toml` / `README.md` / `uv.lock`".
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md` § "Pattern 1" and § "Pitfall 5".
  </read_first>
  <action>
Provision the worktree FIRST, before any other command:
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Then run every subsequent command
through `uv run`. Use the bare `uv` on PATH, never `.venv/bin/uv`, which does not exist under
`tox-uv-bare`.

**Step 1 — observe the fence and re-measure every anchor, before editing anything.** Run and capture
verbatim, each with its own command line and a `date -u` stamp beside the fence pair:
`git tag -l v0.9.0` (expect no output); `git ls-remote --tags origin v0.9.0` (expect no output);
`git rev-parse v0.8.0^{commit}` (the sweep anchor — the `^{commit}` peel is required in case the tag
is annotated); `git merge-base origin/main HEAD`; `git rev-parse origin/main`;
`git merge-base --is-ancestor origin/main HEAD && echo ancestor`;
`git rev-list --count origin/gsd/v0.9.0-per-document-templates..HEAD`;
`git rev-list --count v0.8.0..HEAD`; and
`git diff v0.8.0..HEAD --stat -- . ':(exclude).planning' | tail -1`. Record whatever these commands
print. Do NOT copy the commits-ahead figures from `57-CONTEXT.md` (188) or `57-RESEARCH.md` (190) —
the planner measured 192 at plan time and it moves with every commit. If
`git merge-base --is-ancestor origin/main HEAD` fails, STOP and report: `origin/main` is no longer an
ancestor and the "nothing to merge in" premise this phase rests on has changed.

Also capture `git rev-parse HEAD` as the **phase-start SHA** and label it as such — plan 57-08's
fence proof is scoped to `<phase-start-SHA>..HEAD`, not to the whole milestone diff, because the
milestone's own `typsphinx/` diff is deliberately large.

**Step 2 — move the three version surfaces in one edit set.** In `pyproject.toml`, set the `[project]`
table's `version` key on line 7 to the string `0.9.0`. This is the repository's sole release-version
literal — `typsphinx.__version__` derives from it indirectly through
`importlib.metadata.version("typsphinx")`, never from a second hardcoded string, and none may be
added. In `README.md`, set line 347 to `**Status**: Stable (v0.9.0) - Production ready`; change only
the version token and leave the `**Python**` / `**Sphinx**` / `**Typst**` line below it untouched.
Do not touch `requires-python` (line 10) or the `sphinx` pin (line 28) — those are the truth source
plan 57-04's D-10 verification greps against.

**Step 3 — regenerate the lock and the installed metadata.** Run `uv lock`, which rewrites `uv.lock`'s
own first-party `typsphinx` block (the one under `name = "typsphinx"` with
`source = { editable = "." }`). No other line in `uv.lock` should move: this phase adds and removes no
dependency. If `uv lock` introduces or drops any third-party package, STOP and escalate — SC#4's
"no new runtime dependency" invariant exists precisely to catch that, and `57-RESEARCH.md`'s Package
Legitimacy Audit disposition assumes zero candidate packages.

Then run `uv sync --extra dev --locked`. This does two jobs at once: `--locked` fails loudly on any
lock/manifest disagreement, which is the acceptance bar CONTEXT names for the regeneration procedure,
and the sync regenerates the editable-install metadata (the `typsphinx-0.9.0.dist-info/` directory and
the `__editable__.typsphinx-0.9.0.pth` / `__editable___typsphinx_0_9_0_finder.py` pair, whose
filenames literally embed the version). Editing the manifest alone does NOT change what
`importlib.metadata.version()` returns; this step is what does. **This regenerated, committed
lockfile is D-13's hard sequencing precondition** — plans 57-02 and 57-05 both dispatch CI runs whose
every job opens with `uv sync --extra dev --locked`, 10 such steps across four workflow files; a
stale lockfile fails at install and produces no test, lint or type signal at all, which is exactly
how dependabot PRs #128 and #123 are dying right now.

**Step 4 — prove the slice end to end.** Read the version back with
`uv run python -c "import typsphinx; print(typsphinx.__version__)"`, expecting `0.9.0`. Run
`uv lock --check`, expecting exit 0. Then prove the release machinery's own consumer path is live on
this tree by running `uv run python scripts/extract_changelog_section.py 0.8.0` — an already-published
section — and confirming exit 0 with a non-empty body, then
`uv run python scripts/extract_changelog_section.py 9.9.9` and confirming exit 1 with a message on
stderr. That pair is the extractor's own liveness proof in both directions: it shows the reader
`release.yml`'s `validate` job calls both finds a real section and rejects a missing one, BEFORE plan
57-03 authors the `## [0.9.0]` section it will read next.

Do not edit any other file. `CHANGELOG.md` and `tests/test_changelog_page_gate.py` belong to plan
57-03; `docs/source/changelog.rst` belongs to 57-04. Nothing under `typsphinx/` may change (D-09,
D-10, prep-only fence). Take no irreversible action of any kind: no tag, no tag push, no upload, no
release, no pull request, no dispatch of `release.yml`.
  </action>
  <verify>
    <automated>uv lock --check && uv sync --extra dev --locked && uv run python -c "import typsphinx, sys; sys.exit(0 if typsphinx.__version__ == '0.9.0' else 1)" && uv run python scripts/extract_changelog_section.py 0.8.0 > /dev/null && ! uv run python scripts/extract_changelog_section.py 9.9.9 > /dev/null 2>&1 && test -z "$(git tag -l v0.9.0)" && test -z "$(git ls-remote --tags origin v0.9.0)" && test -z "$(git diff --name-only -- typsphinx/)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "0\.9\.0"$' pyproject.toml` returns `1`.
    - `grep -c '^version = "0\.8\.0"$' pyproject.toml` returns `0`.
    - `grep -c 'Stable (v0\.9\.0) - Production ready' README.md` returns `1`.
    - `grep -c 'requires-python = ">=3\.12"' pyproject.toml` returns `1` and `grep -c 'sphinx>=9\.1,<10' pyproject.toml` returns `1` — the D-10 truth source is untouched.
    - `grep -A2 'name = "typsphinx"' uv.lock` shows the new version and `source = { editable = "." }`.
    - `uv lock --check` exits 0 and `uv sync --extra dev --locked` exits 0.
    - `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints exactly `0.9.0`.
    - `uv run python scripts/extract_changelog_section.py 0.8.0` exits 0 and writes a non-empty body to stdout; `uv run python scripts/extract_changelog_section.py 9.9.9` exits non-zero with a message on stderr.
    - `diff <(git show HEAD:pyproject.toml | sed -n '/^dependencies = \[/,/^\]/p') <(sed -n '/^dependencies = \[/,/^\]/p' pyproject.toml)` produces no output — the runtime dependency array is byte-identical to the pre-bump HEAD's.
    - `git diff --name-only` lists exactly `README.md`, `pyproject.toml`, `uv.lock` — no fourth source path, and in particular nothing under `typsphinx/`.
    - `git diff --name-only -- typsphinx/` produces no output.
    - `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` both produce no output.
  </acceptance_criteria>
  <done>All three version surfaces read the new release, the lock and manifest agree under `--locked`, the installed package metadata reports `0.9.0` on import, the release machinery's extractor runs against this tree in both its success and its failure direction, the fence is observed empty, and the phase-start SHA plus every anchor figure is measured live.</done>
</task>

<task type="auto">
  <name>Task 2: Run the version-sync guard battery and record SC#1's evidence with the live anchor re-measurement</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-BUMP-EVIDENCE.md</files>
  <read_first>
    - `tests/test_extension.py` around `def test_version_matches_pyproject_toml` — what the drift guard actually compares (a `tomllib` parse against `importlib.metadata`, not a hardcoded literal).
    - `tests/test_preview_version_sync.py` — the four-surface `@preview` lockstep guard and its three test functions.
    - `CLAUDE.md` § "The `@preview` version-sync hazard" — the three declaration sites plan 57-08's sweep will count.
    - `.planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-BUMP-EVIDENCE.md` — the exact heading structure and evidence register to match.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md` § "Evidence artifacts" — the verbatim-transcription culture, and the rule that a `pytest.skip` is not evidence.
  </read_first>
  <action>
Run the three guard modules and capture their output verbatim, each with its own summary line:
`uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml -v`,
`uv run pytest tests/test_readme_version_sync.py -v`, and
`uv run pytest tests/test_preview_version_sync.py -v`. All must pass with zero skips.

Additionally run `uv run ruff check .` as a cheap local pre-flight and record its transcript. This is
NOT a substitute for the dispatched CI runs — CI keeps lint/type/test authority under D-13 on D-12's
independent grounds (the Windows and macOS lanes, which no local run reproduces) — but the premise
`57-CONTEXT.md`'s D-13 originally gave for local ruff being impossible was re-measured false at
research time, so record what the command actually does today rather than repeating the stale claim.
If it fails, that is a real lint break: report it and stop rather than proceeding to a CI dispatch.

Do NOT run a bare `tox`, and do NOT run `tox -e py312`: standalone-CPython-3.12 downloads on this
NixOS machine hit the stub-loader rejection. Full-matrix, lint and type authority belongs to the
dispatched CI runs collected by plans 57-02 and 57-05 (D-12, D-13), not to this plan.

Then write `.planning/phases/57-v0-9-0-release-prep-prep-only/57-BUMP-EVIDENCE.md` in English, with
these headings, following `52-BUMP-EVIDENCE.md`'s structure:

- a provisioning note naming `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and
  the `uv run` convention, per `CLAUDE.md`;
- `## SC#1 — version-literal lockstep`, holding the before and after values of the three surfaces
  (`pyproject.toml` line 7, `README.md` line 347, `uv.lock`'s `typsphinx` entry), plus the `uv lock`,
  `uv sync --extra dev --locked`, `uv lock --check` and `import typsphinx` read-back transcripts;
- `## D-13 sequencing precondition`, stating in one paragraph — with the `uv lock --check` exit code
  as its evidence — that the regenerated lockfile is committed BEFORE either CI dispatch, that 10
  `uv sync --locked` steps across `ci.yml` (6), `release.yml` (2), `docs.yml` (1) and `drift.yml` (1)
  depend on it, and that the step count is 10 rather than the eleven `57-CONTEXT.md` states. Verify
  the count yourself with `grep -c locked .github/workflows/*.yml` and paste the output; do not
  transcribe either figure;
- `## Release-machinery consumer path`, holding both `scripts/extract_changelog_section.py`
  transcripts from Task 1 — the exit-0 run and the exit-1 run — and one sentence stating that this
  proves the reader is live BEFORE plan 57-03 authors the section it will read;
- `## Guard tests`, holding all three pytest transcripts verbatim plus the combined JUnit-XML run's
  `testsuite` attributes, and the `uv run ruff check .` transcript with one sentence recording that
  local ruff runnability is an additive pre-flight and does not move authority off CI;
- `## Phase-head anchor re-measurement`, holding every command from Task 1 Step 1 with its own live
  output — the peeled `v0.8.0` commit SHA, the `origin/main` merge-base and rev-parse pair, the
  ancestry check, both commits-ahead counts, the `.planning`-excluded shortstat, and the labelled
  **phase-start SHA** — plus one explicit sentence stating these were RE-RUN at execution time and
  that `57-CONTEXT.md`'s and `57-RESEARCH.md`'s figures were deliberately not transcribed;
- `## SC#4 — fence observation 1 of 3`, holding the `git tag -l v0.9.0` and
  `git ls-remote --tags origin v0.9.0` transcripts with a `date -u` timestamp, and one sentence
  naming `57-SC4-INVARIANTS.md` (plan 57-08) and `57-HANDOFF.md` (plan 57-09) as the owners of the
  other two separated observations;
- `## Executed versus skipped`, stating plainly that a bare `tox` and `tox -e py312` were not run and
  why, and that lint / type / full-matrix authority belongs to CI per D-13.

Never assert a command's result from memory — run it and paste the transcript. Where a result is
genuinely unavailable, record `human_needed` honestly rather than inferring it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q --junit-xml="${TMPDIR:-/tmp}/57-01-guards.xml" && grep -q 'skipped="0"' "${TMPDIR:-/tmp}/57-01-guards.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/57-01-guards.xml" && grep -q 'errors="0"' "${TMPDIR:-/tmp}/57-01-guards.xml" && grep -q '^## SC#1' .planning/phases/57-v0-9-0-release-prep-prep-only/57-BUMP-EVIDENCE.md && grep -q '^## D-13 sequencing precondition' .planning/phases/57-v0-9-0-release-prep-prep-only/57-BUMP-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The combined guard run produces a JUnit `testsuite` element carrying `skipped="0"`, `failures="0"` and `errors="0"`, with at least 5 tests (1 version-match + 1 readme-sync + 3 preview-sync).
    - `57-BUMP-EVIDENCE.md` exists, is written entirely in English, and contains the headings `## SC#1 — version-literal lockstep`, `## D-13 sequencing precondition`, `## Release-machinery consumer path`, `## Guard tests`, `## Phase-head anchor re-measurement`, `## SC#4 — fence observation 1 of 3`, and `## Executed versus skipped`.
    - The evidence file contains the literals `uv sync --extra dev --locked`, `uv lock --check`, `extract_changelog_section.py`, and `phase-start SHA`.
    - The `## D-13 sequencing precondition` section contains the verbatim output of a `grep -c locked .github/workflows/*.yml` run made in that section, and states the measured total.
    - The `## Phase-head anchor re-measurement` section contains the string `git merge-base` and a commits-ahead figure produced by a command shown in the same section; `grep -c '188 commits\|190 commits\|192 commits' 57-BUMP-EVIDENCE.md` returns `0` — no figure is presented as this phase's own measurement without its command beside it.
    - The `## SC#4 — fence observation 1 of 3` section contains a `date -u` timestamp and names `57-SC4-INVARIANTS.md` and `57-HANDOFF.md` as the owners of the other two observations.
    - The `## Guard tests` section contains a `ruff` transcript and a sentence stating CI retains authority.
    - The `## Executed versus skipped` section names `tox -e py312` and a bare `tox`, and states for each why it was not run.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>All five version-sync guard assertions pass with zero skips on the post-bump tree, the D-13 sequencing precondition is recorded with its own live census, and SC#1's transcripts — including the live anchor re-measurement and an honest statement of what was not run locally — are on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Record the REQUIREMENTS.md closeout guard baseline and write the reasoned external-API coverage declaration</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md, .planning/phases/57-v0-9-0-release-prep-prep-only/COVERAGE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 126-136 — REL-08's checkbox line and the italic note that it stays open through every plan of Phase 57, plus the Traceability row for REL-08 wherever it appears in that file (locate it with `grep -n 'REL-08' .planning/REQUIREMENTS.md`).
    - `.planning/ROADMAP.md` § "Phase 57" success criterion 4 — the exact wording of the checksum requirement.
    - `.planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md` — the exact declaration form and register to match; a reasoned prose declaration with NO capability matrix, which the seal-time gate accepts.
    - `.planning/STATE.md` — the standing note that `api-coverage.verify-pre` false-positives on prose describing compile / render / API-read evidence.
  </read_first>
  <action>
**Part A — the closeout guard.** Write
`.planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md` in English, following the
`42-CLOSEOUT-GUARD.md` mechanism this project adopted at the v0.7.0 close. It must carry:

- `## Baseline`, holding the verbatim output of `sha256sum .planning/REQUIREMENTS.md` and
  `wc -l .planning/REQUIREMENTS.md`, each with a `date -u` stamp and the phase-start SHA from Task 1;
- `## The lines under guard`, quoting REL-08's checkbox line and its Traceability row **verbatim,
  byte for byte**, each with its `grep -n` line number, obtained by running
  `grep -n 'REL-08' .planning/REQUIREMENTS.md` and quoting the matching lines from the file;
- `## Why this file exists`, stating in one short paragraph that `phase.complete` has auto-flipped
  the release requirement against an explicit CONTEXT decision at four consecutive release-prep
  closes, that ROADMAP SC#4 requires this checksum precisely so the flip is caught rather than
  shipped, and that REL-08 closes at `/gsd-complete-milestone` on the publish, never in this phase;
- `## Re-verification protocol`, giving the exact two commands a later plan must run —
  `sha256sum .planning/REQUIREMENTS.md` compared against the baseline digest, and
  `git diff --name-only -- .planning/REQUIREMENTS.md` expected empty — and naming plan 57-08 and plan
  57-09 as the two re-verification points, with the instruction to revert by hand
  (`git checkout -- .planning/REQUIREMENTS.md`) and report if either check fails.

Do NOT edit `.planning/REQUIREMENTS.md` in this task or in any task of this phase. Reading and
quoting it is the entire interaction.

**Part B — the coverage declaration.** This file was already seeded at plan time, because the
detector fires on the finished PLAN bodies and the seal-time re-run needs the declaration present.
Read it first; reconcile it against the tree as it now stands and complete anything missing rather
than blindly overwriting a correct file. If it is absent, write it from scratch to the same
specification.

`.planning/phases/57-v0-9-0-release-prep-prep-only/COVERAGE.md` is a reasoned declaration, in
English, following `52-*/COVERAGE.md`'s shape. Its first content line must be a single sentence
beginning with the literal `No external API integration:` followed by a one-line reason naming what
this phase touches instead — release-surface files (the manifest, the lockfile, `README.md`,
`CHANGELOG.md`), one data constant in an existing test module, two documentation prose sections, and
planning artifacts — plus the fact that the only remote surfaces read are this repository's own
GitHub Actions run status and its own git remote refs, through the already-authenticated `gh` and
`git` clients.

Then add a short `**Why this declaration exists.**` paragraph recording that the detector was run
over the phase scope at plan time and returned `{"detected":false,"signals":[]}`, and that this file
exists for the seal-time re-run over the PLAN bodies, which are dense with `gh run view`,
`git ls-remote` and `release.yml` prose — the exact false-positive shape `STATE.md` names.

Finally add a `**No capability matrix is supplied**` paragraph stating that a reasoned declaration
with no rows is the correct form here because there is no external API being integrated, and that
inventing a table would fabricate rows for capabilities that do not exist.

Do not invent a capability matrix. Do not add table rows to `COVERAGE.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md && test -f .planning/phases/57-v0-9-0-release-prep-prep-only/COVERAGE.md && grep -q 'No external API integration:' .planning/phases/57-v0-9-0-release-prep-prep-only/COVERAGE.md && test "$(grep -c '^| ' .planning/phases/57-v0-9-0-release-prep-prep-only/COVERAGE.md)" = "0" && grep -q 'REL-08' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md && test -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" && sha256sum -c <(awk '/^[0-9a-f]{64}  \.planning\/REQUIREMENTS\.md$/{print; exit}' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md)</automated>
  </verify>
  <acceptance_criteria>
    - `57-CLOSEOUT-GUARD.md` exists, is written in English, and contains the headings `## Baseline`, `## The lines under guard`, `## Why this file exists`, and `## Re-verification protocol`.
    - `57-CLOSEOUT-GUARD.md` contains a line matching the `sha256sum` output shape `^[0-9a-f]{64}  \.planning/REQUIREMENTS\.md$`, and `sha256sum -c` against that line succeeds.
    - `57-CLOSEOUT-GUARD.md` quotes REL-08's checkbox line verbatim and names both plan 57-08 and plan 57-09 as re-verification points.
    - `git diff --name-only -- .planning/REQUIREMENTS.md` produces no output — the guarded file was read, never written.
    - `COVERAGE.md` exists, is written in English, and contains a line beginning with the literal `No external API integration:` followed by a reason on the same line.
    - `COVERAGE.md` contains the literal `{"detected":false,"signals":[]}`.
    - `grep -c '^| ' .planning/phases/57-v0-9-0-release-prep-prep-only/COVERAGE.md` returns `0` — no capability-matrix table rows were fabricated.
    - No file named `57-VERIFICATION.md` exists in the phase directory.
  </acceptance_criteria>
  <done>The `REQUIREMENTS.md` checksum baseline and REL-08's verbatim guarded lines are on disk with a re-verification protocol naming its two later checkpoints, and a reasoned, matrix-free external-API declaration is in the phase directory in the form the seal-time gate accepts.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pyproject.toml` version literal → the published artifact's identity | The literal edited here becomes the PyPI package version and the git tag name at `/gsd-complete-milestone`. |
| `uv.lock` → every CI job and the release build | All 10 `uv sync --locked` steps across four workflow files provision from this lockfile; a silently-moved dependency set reaches the published wheel. |
| the working tree → `origin`, PyPI and the GitHub Release (the prep/publish fence) | Everything this phase does must stay on the reversible side of that boundary. |
| `.planning/REQUIREMENTS.md` → the milestone-close record | An auto-flipped checkbox asserts a publish that has not happened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-01 | Spoofing | version identity across `pyproject.toml` / `uv.lock` / `README.md` / installed `.dist-info` | high | mitigate | All four are asserted to agree: `uv lock --check`, `uv sync --extra dev --locked`, the `importlib.metadata` read-back, and `tests/test_readme_version_sync.py`. A tree whose declared version differs from its installed metadata would publish an artifact that does not match the reviewed tree. |
| T-57-02 | Tampering | `uv.lock` dependency set silently moving during regeneration | high | mitigate | Acceptance asserts `git diff --name-only` lists exactly three source paths and that the `[project] dependencies` array is byte-identical to the pre-bump HEAD's; the action instructs STOP-and-escalate on any package add/drop; plan 57-08 re-asserts it mechanically against the `v0.8.0` anchor. |
| T-57-03 | Tampering | the prep/publish fence | high | mitigate | No tag, tag push, PyPI upload, GitHub Release or PR is created; `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` are asserted empty in `<verify>` and in acceptance, and recorded with a `date -u` timestamp as observation 1 of 3. |
| T-57-04 | Repudiation | evidence transcribed from a prior planning document instead of re-measured | high | mitigate | Acceptance asserts the anchor section shows its own commands and that no stale commits-ahead figure appears as this phase's own measurement. |
| T-57-05 | Repudiation | REL-08 reported complete before the publish by a closeout auto-flip | high | mitigate | `57-CLOSEOUT-GUARD.md` records a `sha256sum` baseline and REL-08's verbatim lines at phase head; `<verify>` re-checks the digest and asserts `git diff --name-only -- .planning/REQUIREMENTS.md` is empty; plans 57-08 and 57-09 re-verify. |
| T-57-06 | Elevation of privilege | a `typsphinx/` source change riding in under a release-prep phase | medium | mitigate | `git diff --name-only -- typsphinx/` asserted empty in `<verify>` and in two separate tasks' acceptance criteria. |
| T-57-SC | Tampering | npm/pip/cargo installs | medium | accept | `uv sync --extra dev --locked` installs only what the existing lock already pins; no package is added. `57-RESEARCH.md` records the Package Legitimacy Gate as **not applicable** — zero candidate package names, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. If `uv lock` produces an unexpected new transitive dependency the action instructs STOP-and-escalate rather than proceeding. |
</threat_model>

<verification>
- `uv lock --check` exits 0 and `uv sync --extra dev --locked` exits 0.
- `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints `0.9.0`.
- The guard battery reports `skipped="0"`, `failures="0"` and `errors="0"`.
- Both `scripts/extract_changelog_section.py` runs behave as specified (exit 0 / exit non-zero).
- `git diff --name-only` lists exactly `README.md`, `pyproject.toml`, `uv.lock` plus the three planning artifacts.
- `git diff --name-only -- typsphinx/` and `git diff --name-only -- .planning/REQUIREMENTS.md` both produce no output.
- `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` are both empty.
</verification>

<success_criteria>
- `pyproject.toml` is the sole release-version literal, with `README.md` and `uv.lock` moved in lockstep (SC#1).
- The editable-install metadata was regenerated, so `typsphinx.__version__` reports `0.9.0` (SC#1).
- All three version-sync guard modules pass with zero skips (SC#1).
- The regenerated lockfile is committed, satisfying D-13's sequencing precondition for both later CI dispatches.
- Every anchor figure and the phase-start SHA were measured live and recorded with their commands (SC#4 precondition).
- The fence is observed empty and timestamped at phase head (SC#4, observation 1 of 3).
- `57-BUMP-EVIDENCE.md`, `57-CLOSEOUT-GUARD.md` and `COVERAGE.md` are on disk; no file named `57-VERIFICATION.md` exists.
</success_criteria>

<output>
Create `.planning/phases/57-v0-9-0-release-prep-prep-only/57-01-SUMMARY.md` when done.
</output>
