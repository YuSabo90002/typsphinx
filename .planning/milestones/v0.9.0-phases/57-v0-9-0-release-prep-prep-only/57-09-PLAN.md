---
phase: 57-v0-9-0-release-prep-prep-only
plan: 09
type: execute
wave: 4
depends_on: ["57-08"]
files_modified:
  - .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md
autonomous: true
requirements: [REL-08]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`57-HANDOFF.md` is standalone and complete: a reader with only that file and the repository can execute the publish without opening a PLAN or SUMMARY. Every checklist item names its Owner and its Ordering dependency on the items before it (ROADMAP SC#5)."
    - "The checklist covers, in order: open and merge the pull request against the default branch; push the release tag, which is what fires the release workflow; watch that workflow to completion through its validate, build, publish and create-release jobs including the human approval gate on the publishing environment; verify the package index and the GitHub Release; advance the second repository's pin by dispatching that repository's OWN pin-update workflow rather than by a manual clone-edit-push, then tag it; and measure Read the Docs `stable` on both projects (ROADMAP SC#5)."
    - "The handoff states that the GitHub Release body must be byte-identical to `scripts/extract_changelog_section.py 0.9.0`'s output, and names the check that proves it — the same comparison made at the previous two closes (ROADMAP SC#5)."
    - "**REL-08 remains open**, in those words, with the confirmation that `git diff --name-only -- .planning/REQUIREMENTS.md` is empty over this phase's entire history and that the checksum still matches `57-CLOSEOUT-GUARD.md`'s baseline. It closes at `/gsd-complete-milestone`, on the publish, never here."
    - "Every deferral the handoff carries is verified by LISTING the todo ledger directories, never by grepping their contents, and the file states which check was run. The rule exists because a content grep cannot see a slug that appears only in a filename — the method artifact that produced this phase's own retracted \"missing record\" claim."
    - "`54.1-REVIEW.md` WR-02 is named in the handoff as a known, deliberately undisclosed limitation (D-09): the pre-write validation resolves `templates_path` against the source directory rather than the config directory, so a `-c`/`--confdir` project still walks into the republication hole, and the shipped CHANGELOG sentence reads as unconditional. The reviewer's own recommended minimum remediation is recorded as declined, not silently dropped."
    - "`54.1-REVIEW.md` WR-01 is named as shipping unfixed behind the prep-only fence (D-10), with its todo record's existence verified by directory listing."
    - "The `ruff` toolchain todo is ANNOTATED with this milestone's re-measurement and KEPT in `pending/` by owner decision — not closed — because the defect was environment-dependent and no commit explains the repair, so closing it would erase the record if it recurs. Its `## Acceptance` section, which asks for a durable intentional fix, is left intact."
    - "All nine pending ledger records are dispositioned in the handoff with one line each, and the record about the release workflow's own end-to-end verification is explicitly flagged for owner decision on whether it belongs in `completed/` — the same flag raised at the previous close and evidently unactioned, so it is raised again with the measurement that would settle it rather than repeated as an open question."
    - "The prep/publish fence is observed and timestamped a THIRD time, at a moment separated from the observations in `57-BUMP-EVIDENCE.md` and `57-SC4-INVARIANTS.md` (ROADMAP SC#4)."
  artifacts:
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md"
      provides: "the standalone publish checklist /gsd-complete-milestone reads, the SC#1-#5 disposition, the deferral register, and the third fence observation"
      contains: "REL-08 remains open"
    - path: ".planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md"
      provides: "the toolchain record, annotated with this milestone's re-measurement and deliberately kept open"
      contains: "2026-08"
  key_links:
    - from: "57-HANDOFF.md"
      to: "/gsd-complete-milestone"
      via: "the handoff is the checklist that command reads; it describes the publish and never performs it, so every item names an Owner and an Ordering dependency"
      pattern: "gsd-complete-milestone"
    - from: "the second repository's pin"
      to: "its own pin-update workflow"
      via: "a workflow_dispatch of that repository's committed workflow, confirmed working live, so the same reviewed catalog-regeneration and no-content-free-commit logic applies every time — never a manual clone, edit and push"
      pattern: "workflow_dispatch"
    - from: "the curated `## [0.9.0]` CHANGELOG section"
      to: "the GitHub Release body"
      via: "create-release calls scripts/extract_changelog_section.py and appends an installation block; SC#5 requires the published body to be byte-identical to that output, which is verified after the release exists"
      pattern: "extract_changelog_section"
  prohibitions:
    - statement: "No git tag named v0.9.0 is created locally or pushed to any remote during this phase; the handoff DESCRIBES the tag push, it does not perform it."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged; item 1 of the checklist describes it as the publish half's first step."
      status: unverified
      verification: flagged
    - statement: "No tag is created on the typsphinx-doc-translations repository, its pin is not advanced, and its pin-update workflow is not dispatched by this phase."
      status: unverified
      verification: flagged
    - statement: "REL-08's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped; if a change appears it is reverted by hand and the incident recorded."
      status: unverified
      verification: flagged
    - statement: "The ruff toolchain todo is not moved to completed/ and its Acceptance section is not weakened; only a dated evidence annotation is appended."
      status: unverified
      verification: flagged
    - statement: "No ledger record's existence is claimed on the basis of a content grep; every one is verified by listing the directory, and the file states which check was run."
      status: unverified
      verification: flagged
    - statement: "No `### Known Limitations` CHANGELOG section is added and no GitHub issue is filed for WR-02; D-09 declined both and the handoff records the decline instead."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/, tests/ or docs/ is changed by this plan."
      status: unverified
      verification: flagged
  flagged_assumptions: []
---

<objective>
Close the phase: disposition the todo ledger by directory listing, annotate the one record the owner
decided to keep open, and write the standalone publish checklist `/gsd-complete-milestone` reads —
with REL-08 explicitly still open and the fence observed a third time.

Purpose: discharges ROADMAP Phase 57 SC#5 and takes SC#4's third separated observation. Two lessons
shape this plan. First, a completion narrative is not proof a record exists — and this phase's own
CONTEXT proved the sharper version of that when its "missing todo" claim turned out to be a method
artifact of a content grep. So every deferral this handoff carries is verified by LISTING the ledger
directory, and the file says which check was run. Second, `phase.complete` has auto-flipped the
release requirement at four consecutive release-prep closes; the handoff is where the final
confirmation that it did not fire this time belongs.

Output: an annotated toolchain todo record still in `pending/`, and `57-HANDOFF.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-08-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-level list, repeated in every plan of this phase so each is readable standalone.

**Source tree (modified files and new symbols):**

| Artifact | Plan | Shape |
|---|---|---|
| `pyproject.toml` line 7 | 57-01 | the `version` key's value becomes the string `0.9.0` |
| `README.md` line 347 | 57-01 | the Status line reads `**Status**: Stable (v0.9.0) - Production ready` |
| `uv.lock` | 57-01 | regenerated; its first-party `typsphinx` block carries the new version |
| `CHANGELOG.md` — new `## [0.9.0] - <date>` section (lead paragraph + `### Added` / `### Changed` / `### Fixed` / `### Removed` / `### Verified`) | 57-03 | new section; `## [Unreleased]` keeps only its `### Planned for Future Releases` list |
| `CHANGELOG.md` tail link block | 57-03 | one inserted `[0.9.0]:` release-tag line above the existing topmost release line, and the `[Unreleased]` compare base advanced |
| `tests/test_changelog_page_gate.py` — `RELEASE_VERSIONS` gains a `"0.9.0"` entry (14 → 15) and its count/range comment moves | 57-03 | tuple + comment edit only |
| `docs/source/changelog.rst` — new `Migrating from 0.8.x to 0.9.0` subsection under `Migration Guides` | 57-04 | new prose section, deliberately ungated (D-07) |
| `docs/source/installation.rst`, `docs/source/contributing.rst`, `docs/source/examples/advanced.rst`, `examples/basic/README.md`, `examples/advanced/README.md` | 57-04 | **conditional** — edited ONLY if the execution-time D-10 discovery grep returns a hit; commit `70e24958` already corrected all five, so the expected diff is empty |
| `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` | 57-09 | dated evidence annotation appended; the record **stays in `pending/`** by owner decision |

**No new todo record is created by this phase.** The 56-REVIEW filing already exists in
`.planning/todos/completed/`; the CONTEXT's "re-file the missing todo" item was retracted as a method
artifact.

**Planning artifacts:**

| Path (under `.planning/phases/57-v0-9-0-release-prep-prep-only/`) | Plan |
|---|---|
| `57-BUMP-EVIDENCE.md` | 57-01 |
| `57-CLOSEOUT-GUARD.md` | 57-01 (baseline) — re-verified by 57-08 and 57-09 |
| `COVERAGE.md` | 57-01 |
| `57-CI-EVIDENCE.md` | 57-02 (pre-bump check run) → 57-05 (post-bump authority run) |
| `57-CHANGELOG-EVIDENCE.md` | 57-03 |
| `57-MIGRATION-EVIDENCE.md` | 57-04 |
| `57-GREEN-TREE-EVIDENCE.md` | 57-06 |
| `57-GOAL-CLAIM-EVIDENCE.md` | 57-07 |
| `57-SC4-INVARIANTS.md` | 57-08 |
| `57-HANDOFF.md` | 57-09 |

**Reserved name — never produced:** `57-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: Disposition the todo ledger by directory listing, and annotate the toolchain record the owner decided to keep open</name>
  <files>.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` in full — its frontmatter, its `## Problem` section quoting the original failure signature, and its `## Acceptance` section, which asks for a durable intentional fix rather than an environment-dependent one. That section is why the record stays open.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md` `<deferred>` § "Reviewed Todos (not folded)" — the per-record disposition reasons already recorded, and the flag raised about the release-workflow verification record.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md` `<specifics>` item 9 including its RETRACTED block — the method artifact this task's verification rule exists to prevent.
    - `.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-REVIEW.md` § WR-01 and § WR-02 — the two findings shipping unfixed, so the handoff names them accurately.
    - `.planning/STATE.md` — the v0.7.1 close record showing whether the release workflow's create-release job completed successfully, which is the measurement that would settle the flagged record's disposition.
  </read_first>
  <action>
Provision first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

**Step 1 — census the ledger by LISTING, never by grepping content.** Run
`ls -1 .planning/todos/pending/ | sort` and `ls -1 .planning/todos/completed/ | sort` and capture both
outputs verbatim. Then, for each record the handoff will name, confirm its existence with a filename
match — for example `ls -1 .planning/todos/completed/ | grep <slug>` — and record which check you ran.

This method rule is not ceremony. This phase's own CONTEXT asserted a filed record did not exist,
having searched with a content grep; the record was on disk all along, with the slug present only in
the filename and never repeated in the body. A narrative is not proof, and neither is the wrong kind
of search. State in your output, for every existence claim you make, that it was settled by a
directory listing.

**Step 2 — annotate the toolchain record and KEEP it in `pending/`.** Append a new dated section to
`.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` — do not rewrite
any existing section, do not touch the frontmatter's `severity` or `resolves_phase`, and do not weaken
the `## Acceptance` criteria.

The new section records, with live transcripts you produce now rather than figures copied from
research: the current `file .venv/bin/ruff` output showing the interpreter path; the
`uv run ruff --version` output; and the `uv run ruff check .` output with its exit code. Then state,
in prose: that the failure signature quoted in the record's own `## Problem` section no longer
reproduces on this machine as of today; that no commit is known to explain the repair, so the change
is environment-dependent rather than the durable intentional fix `## Acceptance` asks for; and that
the owner decided during this phase to keep the record OPEN for exactly that reason — closing it
would erase the record if the condition recurs after a toolchain or environment update.

Do NOT move the file to `.planning/todos/completed/`. Do NOT change its status or severity.

**Step 3 — settle the flagged record with a measurement rather than re-raising a question.** One
pending record concerns the release workflow's own end-to-end verification, which the previous close
believed was discharged at the v0.7.1 publish. That flag was raised at the previous release-prep phase
and evidently went unactioned. Do not simply repeat it. Instead, gather the measurement that would
settle it — read `STATE.md`'s v0.7.1 release record for whether the `create-release` job completed
successfully and whether the published body was compared against the extractor's output — and record
that measurement beside the flag. Then leave the disposition itself to the owner: this phase does not
move ledger records except the one Step 2 annotates, and it does not decide a prior milestone's
closure on its own authority. The handoff (Task 2) carries the flag with its measurement attached.

Do not create any new todo record. Do not edit any other file under `.planning/todos/`.
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md && test "$(ls -1 .planning/todos/completed/ | grep -c 'stale-version-prerequisites-and-dead-config-link-in-published-docs')" = "1" && test "$(ls -1 .planning/todos/pending/ | grep -c 'ruff-generic-linux-elf-unrunnable-on-nixos')" = "1" && test "$(ls -1 .planning/todos/completed/ | grep -c 'ruff-generic-linux-elf-unrunnable-on-nixos')" = "0" && grep -q '^## Acceptance' .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md && test "$(git diff --name-only -- .planning/todos/ | wc -l)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `ls -1 .planning/todos/pending/ | sort` and `ls -1 .planning/todos/completed/ | sort` were both run and their outputs captured verbatim.
    - The toolchain record remains in `.planning/todos/pending/` and does NOT appear in `.planning/todos/completed/`.
    - The toolchain record gained a new dated section containing a `file .venv/bin/ruff` transcript, a `ruff --version` transcript, and a `ruff check .` transcript with its exit code, plus prose stating the owner's decision to keep it open and why.
    - The toolchain record's `## Acceptance` section is byte-unchanged: `git diff -U0 -- .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md | grep -c '^-'` returns `1` (the diff header only) — the change is purely additive.
    - `git diff --name-only -- .planning/todos/` lists exactly one path.
    - No file was created under `.planning/todos/`.
    - The 56-REVIEW filing is confirmed present in `completed/` by a filename match, and that check is the one recorded — not a content grep.
    - The measurement bearing on the flagged release-workflow record is captured from `STATE.md`'s v0.7.1 release record, without the record itself being moved.
  </acceptance_criteria>
  <done>Both ledger directories are censused by listing, the toolchain record carries a dated, transcript-backed annotation and stays in `pending/` with its acceptance criteria intact, and the one flagged record has its settling measurement attached without this phase deciding its disposition.</done>
</task>

<task type="auto">
  <name>Task 2: Write the standalone publish checklist</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md</files>
  <read_first>
    - `.planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md` in full — the document shape SC#5 asks for: the standalone-reader preamble, the "What this phase satisfied, and what it did not" section quoting the requirement verbatim and dispositioning each success criterion by citation rather than restatement, and the numbered checklist where each item names its Owner and Ordering dependency.
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md` — the seven-item checklist this one descends from, including the Read the Docs measurement item and the second-repository item.
    - `.planning/REQUIREMENTS.md` lines 126-136 — REL-08's text, to quote verbatim.
    - `.planning/ROADMAP.md` § "Phase 57" success criteria 1-5 — the five criteria to disposition.
    - `.github/workflows/release.yml` — the job sequence, which job validates the changelog section, which job publishes to the package index behind an environment approval, and which job creates the GitHub Release. READ ONLY.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md` § "Pattern 7" — the second-repository pin-update workflow's dispatch route, confirmed working live, and the note that it does not itself create a tag there.
    - Every evidence artifact this phase produced, so each success criterion is dispositioned by citation: `57-BUMP-EVIDENCE.md`, `57-CI-EVIDENCE.md`, `57-CHANGELOG-EVIDENCE.md`, `57-MIGRATION-EVIDENCE.md`, `57-GREEN-TREE-EVIDENCE.md`, `57-GOAL-CLAIM-EVIDENCE.md`, `57-SC4-INVARIANTS.md`, `57-CLOSEOUT-GUARD.md`.
  </read_first>
  <action>
Write `.planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md` in English, with the heading
`# Phase 57: v0.9.0 Release Prep (prep-only) — Publish & Owner-Manual Handoff Checklist`, and these
sections.

**Preamble.** One paragraph stating that this document is the standalone publish checklist
`/gsd-complete-milestone` reads for this milestone, and that a reader with only this file and the
repository can execute the publish without opening a PLAN or SUMMARY file.

**`## What this phase satisfied, and what it did not`.** Quote REL-08 verbatim from
`.planning/REQUIREMENTS.md`, as a blockquote. Then one line per success criterion SC#1 through SC#5,
each reading MET or NOT MET and **citing** the evidence artifact and section that proves it rather
than restating the evidence. Then, in its own paragraph and in these words: **REL-08 remains open.**
It closes at `/gsd-complete-milestone`, on the publish, not here — with the confirmation that
`git diff --name-only -- .planning/REQUIREMENTS.md` is empty over this phase's entire history and that
`sha256sum .planning/REQUIREMENTS.md` still matches the baseline in `57-CLOSEOUT-GUARD.md`. Run both
commands now and paste them.

**`## Checklist`.** A numbered list; each item names its **Owner** and its **Ordering** dependency on
the items before it. The items, in order:

1. Open the pull request against the default branch and merge it. Owner: `/gsd-complete-milestone`.
   Ordering: first — everything below depends on the merge commit existing.
2. Push the release tag on the merge commit. Owner: `/gsd-complete-milestone`. Ordering: after 1.
   State that this is the action that fires the release workflow, and that nothing before this point
   in the milestone was irreversible.
3. Watch the release workflow to completion, job by job. Owner: `/gsd-complete-milestone`, with an
   owner-manual approval step. Ordering: after 2. Name each job in sequence, name the environment
   approval gate that blocks publication, and call out the release-creation job specifically as the
   one to watch closely — it is the job that failed at the v0.7.0 close and had to be repaired by
   hand. Also state that the validation job runs `scripts/extract_changelog_section.py` against the
   new version and will abort the whole release before any upload if the section is missing or empty.
4. Verify the package index and the GitHub Release. Owner: owner-manual. Ordering: after 3. Include
   the byte-identity check SC#5 requires: the published Release body's leading lines must be
   byte-identical to `uv run python scripts/extract_changelog_section.py 0.9.0`'s stdout — give the
   exact comparison command shape and say that a non-empty `diff` output is a failure, not a
   formatting difference. Also list the three assets the Release should carry.
5. Advance the second repository's pin, then tag it. Owner: `/gsd-complete-milestone` for the
   dispatch, owner-manual for the tag timing. Ordering: after 3. State plainly that the pin is
   advanced by dispatching that repository's OWN pin-update workflow — give the `gh workflow run`
   command shape and the polling command — and NOT by a manual clone, edit and push, so the same
   reviewed catalog-regeneration and no-content-free-commit logic applies. Note that the workflow
   does not itself create a tag there; the tag is a separate subsequent step once the pin commit
   lands.
6. Measure Read the Docs `stable` on both projects. Owner: owner-manual — flag explicitly that this
   is outside `/gsd-complete-milestone`'s own reach. State what to measure: that the root URL resolves
   to the stable path, the `stable` version identifier on each project, that both report the new
   version, and that both PDFs are served.

**`## Deferrals carried forward`.** One subsection per item, each stating what it is, why it ships
unfixed, and where its record lives — with the record's existence confirmed by a directory listing
from Task 1, and the listing check named:

- **WR-02** (`54.1-REVIEW.md`) — the pre-write validation resolves Sphinx's `templates_path` against
  the source directory rather than the config directory, so a project using `-c`/`--confdir` still
  walks into the republication hole, and `_copy_used_template_bundles()` has no `templates_path`
  awareness of its own either. Record explicitly that D-09 shipped this SILENT: no CHANGELOG
  carve-out, no `### Known Limitations` section, no GitHub issue — and that the reviewer's own
  recommended minimum remediation was exactly the carve-out that was declined. Note that the shipped
  sentence therefore reads as unconditional, which is a stronger consequence than merely omitting a
  caveat. This is the third consecutive release to decline a limitations section.
- **WR-01** (`54.1-REVIEW.md`) — the "Custom template not found" warning fires three times instead of
  two for one narrow configuration shape; it needs a source change and stays behind the prep-only
  fence (D-10).
- **The nine pending ledger records** — a compact table with one disposition line each, taken from
  the census in Task 1, including the toolchain record's annotate-and-keep-open decision and the
  flagged release-workflow record with the settling measurement attached and its disposition left to
  the owner.

**`## Closeout guard`.** The third fence observation and the final `REQUIREMENTS.md` verification,
written by Task 3.

**`## What this phase deliberately did not do`.** A short list: no tag, no release-workflow run, no
package-index upload, no GitHub Release, no pull request, no second-repository tag or pin advance.
State that the two reversible remote actions taken — a fast-forward push of the milestone branch and
two CI workflow dispatches — are named in scope by D-12 and are not part of the publish.

Cite; do not restate. Where an evidence artifact already carries a transcript, point at its file and
section rather than copying it in.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md && grep -q 'REL-08 remains open' .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md && grep -q '^## Checklist' .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md && grep -q '^## Deferrals carried forward' .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md && grep -q 'extract_changelog_section.py 0.9.0' .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md && test "$(grep -c '\*\*Owner:\*\*' .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md)" -ge "6" && test "$(grep -c '\*\*Ordering:\*\*' .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md)" -ge "6" && test -z "$(git diff --name-only -- typsphinx/ tests/ docs/ .planning/REQUIREMENTS.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `57-HANDOFF.md` exists, is written entirely in English, and contains the headings `## What this phase satisfied, and what it did not`, `## Checklist`, `## Deferrals carried forward`, `## Closeout guard`, and `## What this phase deliberately did not do`.
    - REL-08 is quoted verbatim as a blockquote, and the file contains the exact phrase `REL-08 remains open`.
    - Each of SC#1 through SC#5 has its own disposition line reading MET or NOT MET and citing a named evidence file and section.
    - The checklist has at least 6 numbered items, each carrying a `**Owner:**` and an `**Ordering:**` line.
    - The checklist item for the GitHub Release contains the literal `extract_changelog_section.py 0.9.0` and a comparison command whose failure condition is stated as non-empty `diff` output.
    - The checklist item for the second repository names a `gh workflow run` dispatch of that repository's own pin-update workflow and states explicitly that a manual clone-edit-push is not the route.
    - The Read the Docs item is marked owner-manual and flagged as outside `/gsd-complete-milestone`'s reach.
    - The deferrals section names WR-02 and WR-01, records D-09's decline including the reviewer's recommended minimum, and carries a table of nine pending ledger records with one disposition line each.
    - Every existence claim about a ledger record states that it was settled by a directory listing.
    - `git diff --name-only -- typsphinx/ tests/ docs/ .planning/REQUIREMENTS.md` produces no output.
  </acceptance_criteria>
  <done>A standalone publish checklist exists that a reader with only it and the repository can execute — every item with an Owner and an Ordering dependency, every success criterion dispositioned by citation, every deferral named with its record verified by listing, and REL-08 stated to remain open.</done>
</task>

<task type="auto">
  <name>Task 3: Take the third fence observation and record the final closeout-guard verification</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md</files>
  <read_first>
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md` — the baseline digest, the guarded REL-08 lines, and the re-verification protocol naming this plan as its second checkpoint.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md` § "SC#4 fence — observation 2 of 3" — the second observation's timestamp, so this third one is demonstrably separated from it.
    - `.planning/ROADMAP.md` § "Phase 57" success criterion 4 — the "probed and recorded, twice, at separated times" wording, which this phase exceeds with three observations.
  </read_first>
  <action>
Fill in `57-HANDOFF.md`'s `## Closeout guard` section with live transcripts produced now, at a moment
separated from the earlier observations.

**Step 1 — the third fence observation.** Run `date -u`, then `git tag -l v0.9.0`,
`git ls-remote --tags origin v0.9.0`, `gh release list --limit 5`, and
`gh run list --workflow=release.yml --limit 5`. Paste all five outputs. Both tag probes must produce
no output, no release may exist for this version, and the release workflow must show no run for it.
Add one sentence naming the timestamps of the other two observations, quoted from
`57-BUMP-EVIDENCE.md` and `57-SC4-INVARIANTS.md`, so the separation is visible rather than asserted.

**Step 2 — the final closeout-guard verification.** Run `sha256sum .planning/REQUIREMENTS.md` and
compare it against the baseline digest in `57-CLOSEOUT-GUARD.md`. Run
`git diff --name-only -- .planning/REQUIREMENTS.md` and confirm it is empty. Run
`git log --oneline -- .planning/REQUIREMENTS.md` restricted to this phase's own SHA range (the
phase-start SHA is recorded in `57-BUMP-EVIDENCE.md`) and confirm it lists no commit. Re-quote REL-08's
checkbox line from the live file and confirm it is byte-identical to the guarded quote. Paste all
four.

If any of these shows a change, the `phase.complete` auto-flip has fired. Revert by hand with
`git checkout -- .planning/REQUIREMENTS.md`, re-run all four checks, and record the incident in full —
what changed, when, and the revert — in this section. Report it in the plan summary too. This has
fired at four consecutive release-prep closes; a clean result is what you verify, not what you assume.

**Step 3 — state the phase's own boundary.** Close the section with one paragraph: the phase is
complete when this file is written, REL-08 is still open, and the next action is
`/gsd-complete-milestone`, which executes the checklist above. Nothing in this phase may be read as
having performed any checklist item.

Do not edit `.planning/REQUIREMENTS.md` for any reason other than reverting an unintended change back
to the guarded baseline.
  </action>
  <verify>
    <automated>grep -q '^## Closeout guard' .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md && test -z "$(git tag -l v0.9.0)" && test -z "$(git ls-remote --tags origin v0.9.0)" && test -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" && sha256sum -c <(awk '/^[0-9a-f]{64}  \.planning\/REQUIREMENTS\.md$/{print; exit}' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md) && grep -q 'gsd-complete-milestone' .planning/phases/57-v0-9-0-release-prep-prep-only/57-HANDOFF.md</automated>
    <human-check>Read `57-HANDOFF.md` end to end as if you had never seen this phase, and confirm you could execute the publish from it alone — every step has an owner, the ordering is unambiguous, and nothing requires opening a PLAN or SUMMARY file. This is the one judgement in the phase that no assertion can express; `57-VALIDATION.md` lists it as a Manual-Only verification. Walk each item against `52-HANDOFF.md` and `46-HANDOFF.md`, and confirm in particular that the second-repository item advances the pin by dispatching that repository's own workflow rather than by hand.</human-check>
  </verify>
  <acceptance_criteria>
    - `57-HANDOFF.md`'s `## Closeout guard` section contains a `date -u` timestamp distinct from the ones recorded in `57-BUMP-EVIDENCE.md` and `57-SC4-INVARIANTS.md`, and quotes both of those timestamps for comparison.
    - `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` both produce no output, pasted verbatim.
    - `gh release list --limit 5` and `gh run list --workflow=release.yml --limit 5` outputs are pasted and show no artefact for this version.
    - `sha256sum .planning/REQUIREMENTS.md` matches `57-CLOSEOUT-GUARD.md`'s baseline digest, `git diff --name-only -- .planning/REQUIREMENTS.md` is empty, and `git log --oneline <phase-start-SHA>..HEAD -- .planning/REQUIREMENTS.md` lists no commit.
    - REL-08's checkbox line quoted from the live file is byte-identical to the guarded quote.
    - The section closes by naming `/gsd-complete-milestone` as the next action and stating that no checklist item was performed by this phase.
    - `git diff --name-only -- typsphinx/ tests/ docs/` produces no output.
  </acceptance_criteria>
  <done>The prep-only fence is observed empty a third time at a demonstrably separated moment, the `REQUIREMENTS.md` digest and REL-08's guarded lines are unchanged across the phase's entire history, and the handoff closes by naming the next action without having taken any part of it.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `57-HANDOFF.md` → `/gsd-complete-milestone`'s execution | Every irreversible action of the milestone is taken from this checklist; an omitted or mis-ordered step causes a partial publish. |
| the todo ledger → the only surviving record of deferred findings | D-09 makes the ledger and this handoff the sole record of a real, undisclosed limitation. |
| `.planning/REQUIREMENTS.md` → the milestone-close record | An auto-flipped checkbox asserts a publish that has not happened. |
| the second repository's pin → the published translated documentation | Advancing it by hand bypasses the reviewed catalog-regeneration logic its own workflow applies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-49 | Repudiation | REL-08 reported complete before the publish | high | mitigate | Task 3 re-verifies the digest, the name-only diff and the phase-range log against `57-CLOSEOUT-GUARD.md`, all asserted in `<verify>`; the handoff must state `REL-08 remains open` in those words; a hand-revert and full incident record are specified if it fired. |
| T-57-50 | Repudiation | a deferral's record claimed to exist on the strength of a narrative or a content grep | high | mitigate | Task 1 censuses both ledger directories by listing and every existence claim must name the listing check; `<verify>` asserts the two specific records' presence and absence by filename match. |
| T-57-51 | Information disclosure | the WR-02 limitation disappearing from the record entirely | medium | mitigate | D-09's decline is recorded in the handoff's deferrals section with the reviewer's own recommended minimum named, and the over-broad-sentence consequence stated; acceptance asserts the section names it. |
| T-57-52 | Tampering | a ledger record closed to tidy the ledger, erasing a recurrence-prone defect | high | mitigate | The toolchain record is asserted still in `pending/` and absent from `completed/`; acceptance requires the edit to be purely additive with `## Acceptance` byte-unchanged; a prohibition forbids the move. |
| T-57-53 | Tampering | the prep/publish fence crossed while writing the handoff that describes crossing it | high | mitigate | The third observation records both tag probes plus `gh release list` and `gh run list --workflow=release.yml`; prohibitions enumerate every forbidden action and state that the handoff describes the tag push rather than performing it. |
| T-57-54 | Elevation of privilege | the second repository's pin advanced by a manual push, bypassing its reviewed workflow | medium | mitigate | The checklist item is required to name the `gh workflow run` dispatch route and to state explicitly that a manual clone-edit-push is not the route; a prohibition forbids this phase dispatching it at all. |
| T-57-55 | Repudiation | a Release body diverging from the curated CHANGELOG section | medium | mitigate | The checklist item carries the byte-identity comparison SC#5 requires and states that non-empty `diff` output is a failure, not a formatting difference. |
| T-57-SC | Tampering | npm/pip/cargo installs | low | accept | Only `uv sync --extra dev` runs, against the existing lock; no package is added. `57-RESEARCH.md` records the Package Legitimacy Gate as not applicable. |
</threat_model>

<verification>
- The toolchain record remains in `.planning/todos/pending/` and is absent from `completed/`, with a purely additive annotation.
- `git diff --name-only -- .planning/todos/` lists exactly one path.
- `57-HANDOFF.md` contains the exact phrase `REL-08 remains open`, at least six checklist items each with an Owner and an Ordering line, and the byte-identity check for the Release body.
- `git tag -l v0.9.0`, `git ls-remote --tags origin v0.9.0`, `gh release list` and `gh run list --workflow=release.yml` all show no v0.9.0 artefact, at a third separated timestamp.
- `sha256sum .planning/REQUIREMENTS.md` matches the phase-head baseline and `git diff --name-only -- .planning/REQUIREMENTS.md` is empty.
- `git diff --name-only -- typsphinx/ tests/ docs/` produces no output.
</verification>

<success_criteria>
- `57-HANDOFF.md` is standalone and complete, with every checklist item naming its Owner and Ordering dependency (SC#5).
- The second-repository item advances the pin by dispatching that repository's own workflow, and the Release-body byte-identity check is specified (SC#5).
- Every deferral is named with its record verified by directory listing, and D-09's decline is recorded with the declined recommendation (D-09).
- The toolchain record is annotated with live transcripts and deliberately kept open (owner decision).
- The fence is observed empty a third time and the `REQUIREMENTS.md` guard holds; REL-08 remains open (SC#4).
</success_criteria>

<output>
Create `.planning/phases/57-v0-9-0-release-prep-prep-only/57-09-SUMMARY.md` when done.
</output>
