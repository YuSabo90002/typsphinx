---
phase: 56-per-document-template-documentation
plan: 03
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - docs/source/user_guide/output_layout.rst
  - docs/source/user_guide/builders.rst
  - tests/test_output_layout_docs_gate.py
  - tests/test_hand_compile_root_gate.py
autonomous: true
requirements: [DOC-15]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-03: `docs/source/user_guide/output_layout.rst` is the canonical page for output layout — it describes one bundle per USED registry key, copied wholesale to a per-key directory under the output tree, and states that a key declared but named by no `typst_documents` entry has no bundle copied"
    - "D-03: the page states that nothing under the output directory is deleted, so a file removed from a source bundle can survive at the destination across an incremental rebuild (Phase 54 D-01) — the docs do not promise a cleanup that does not happen"
    - "SC#4: the published three-master file-count rule matches the real build — `output_layout.rst` states NINE root-level `.typ` files for a three-master project over six documents, and the per-key bundle beside them, which is exactly what `tests/test_output_layout_docs_gate.py::test_three_master_project_emits_ten_typ_files` already measures from a real `sphinx-build -b typst`"
    - "The prose fix and the prose-binding assertion move together: `tests/test_output_layout_docs_gate.py::test_page_states_the_shared_child_composition` asserts the corrected count clause in the SAME commit that corrects the page, so the suite is never RED between them"
    - "`docs/source/user_guide/builders.rst`'s two-entry walkthrough publishes the corrected root-level file count for that example plus the bundle file one directory down, and no longer claims a root-level shared template file"
    - "D-03 (amended, measured twice): the hand-compile note published in `output_layout.rst`'s `Which File to Compile` section is CONDITIONAL — a wrapper whose `typst_documents` target carries a path component needs an explicit project root pointing at the output directory, and a bare-target wrapper sitting at the output root does not, because Typst's default project root is the compiled file's own directory"
    - "Both branches of that conditional are pinned by a real `typst.compile()` gate, not published on the strength of prose review: the bare-target wrapper compiles with no explicit root, and the nested-target wrapper fails without one and succeeds with the output directory passed as root"
    - "The note states why neither builder is affected: `typsphinx/pdf.py` passes the output directory as the project root on every compile it performs, so the hazard is invisible to anyone who only uses the `typst` and `typstpdf` builders"
    - "Both documentation builds stay green: `uv run tox -e docs-html` and `uv run tox -e docs-pdf` both succeed after every edit in this plan"
  prohibitions:
    - "No file under `typsphinx/` is modified"
    - "The hand-compile note does not claim an explicit root is required for the bare-target example the same page already walks through — that claim is measurably false"
    - "No file under `tests/` is edited as a stale-prose sweep target; the only test edits here are the count-clause assertion and the new root gate"
    - "`docs/source/changelog.rst` and `CHANGELOG.md` are not edited"
    - "No `@preview` package version is quoted into any documentation page"
    - "No new RST directive is introduced that has no precedent in this corpus"
  artifacts:
    - path: "docs/source/user_guide/output_layout.rst"
      provides: "the per-key bundle-directory layout story replacing the single-root-template story, the corrected three-master count rule, the no-deletion caveat, and the conditional hand-compile root note in `Which File to Compile`"
      contains: "Which File to Compile"
    - path: "docs/source/user_guide/builders.rst"
      provides: "the corrected file count for the two-entry walkthrough"
    - path: "tests/test_output_layout_docs_gate.py"
      provides: "the count-clause prose assertion updated to the corrected number, moved in the same commit as the prose"
    - path: "tests/test_hand_compile_root_gate.py"
      provides: "a real `sphinx-build -b typst` plus `typst.compile()` gate pinning BOTH branches of the conditional root rule — bare target without a root, nested target with and without one"
  key_links:
    - from: "tests/test_output_layout_docs_gate.py::test_three_master_project_emits_ten_typ_files"
      to: "docs/source/user_guide/output_layout.rst's three-master count rule"
      via: "test_page_states_the_shared_child_composition, the prose-side twin of that build-side measurement — the two must now agree on the same number"
    - from: "typsphinx/writer.py's root-absolute template import construction"
      to: "output_layout.rst's `Which File to Compile` hand-compile note"
      via: "tests/test_hand_compile_root_gate.py, which compiles a real wrapper of each target shape and records which one needs a root"
---

<objective>
Make `output_layout.rst` describe the output tree that actually ships, and publish the hand-compile
consequence with both of its measured branches pinned by a test.

Purpose: this page is the phase's proof that eyeball review does not hold this documentation set —
its file-count rule has been provably wrong since Phase 54 while a test asserting the WRONG number
sat green. Correcting it requires moving the prose and the assertion together, and the `--root`
consequence Phase 54 handed forward is narrower than it was handed forward as, so publishing it
without exercising the nested case would open exactly the hole this phase closes.

Output: a corrected `output_layout.rst` and `builders.rst`; an updated count-clause assertion; a new
real-compile gate covering both root branches.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-per-document-template-documentation/56-CONTEXT.md
@.planning/phases/56-per-document-template-documentation/56-RESEARCH.md
@.planning/phases/56-per-document-template-documentation/56-PATTERNS.md
@.planning/phases/56-per-document-template-documentation/56-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the single-root-template story with the per-key bundle-directory story</name>
  <files>docs/source/user_guide/output_layout.rst</files>
  <read_first>
    - `docs/source/user_guide/output_layout.rst` in full (168 lines) — specifically the `Wrapper and Content Files` paragraph at 31-35, the `Targets That Stop the Build` claim list at 119-122, and the no-files-written sentence at 139-141
    - `typsphinx/builder.py` lines 745-800 (`_reserves_template_prefix`, the reserved output directory) and 1985-2010 plus 2145-2180 (the bundle copy and its destination-collision refusal) — the measured behaviour the prose must match
    - `typsphinx/writer.py` lines 75-235 — `TEMPLATE_OUTPUT_DIR` and `compute_template_import_path()`, which builds the wrapper's root-absolute import
    - `tests/test_output_layout_docs_gate.py` — `TestPublishedOutputLayoutTextMatchesBuild` (the class that reads this page) and `test_three_master_project_emits_ten_typ_files`, which already asserts the real build's nine-file root set plus the bundled template one directory down
    - `.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-CONTEXT.md` § D-01 — no deletion happens under `outdir`
  </read_first>
  <action>
<!-- planner-discipline-allow: _template.typ -->
Rewrite the three places on this page that still describe a single shared template file written at
the output root under the reserved basename `_template.typ`. That basename no longer exists as an
output artifact; the shipped layout is one directory per USED registry key, holding that key's whole
bundle, under the reserved `_template/` output directory.

  1. **`Wrapper and Content Files`, the three-file paragraph (currently 31-35).** The shown
     configuration writes `manual.typ` and `index.typ` at the output root, plus the template bundle
     for the registry key that entry uses — for that example, the reserved `typst` key — one directory
     down at `_template/typst/`, holding the resolved template file and every other file that sat
     beside it. Keep the closing point that the wrapper will not compile without it.
  2. **`Targets That Stop the Build`, the claim list (currently 119-122).** A path can be claimed by
     any of three things: the reserved `_template/` output directory typsphinx writes template
     bundles into, any document's own content file, or any other entry's own wrapper. Say
     `directory`, not `file`, and keep the existing sentence structure otherwise.
  3. **The no-files-written sentence (currently 139-141).** A build that fails this check leaves no
     `.typ` files behind and no template bundle either.

Then add, at the end of the `Wrapper and Content Files` section, two short paragraphs stating the
bundle rule in full, because this page is now canonical for it (D-03):
  - one bundle per registry key that some `typst_documents` entry actually names, copied wholesale —
    the resolved template file's own parent directory and everything in it — to that key's own
    directory under the reserved output directory; a key declared in `typst_document_templates` that
    no entry names has nothing copied; and the built-in `typst` key is handled by the same rule, with
    no special case;
  - nothing under the output directory is deleted between builds, so a file removed from a source
    bundle can survive at its destination across an incremental rebuild.

Cross-reference `configuration.rst`'s registry subsection with the existing
`See Also` entry's `:doc:` form rather than restating the registry schema here.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest tests/test_output_layout_docs_gate.py -x -q && uv run tox -e docs-html && uv run tox -e docs-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_output_layout_docs_gate.py -x -q` exits 0
    - `grep -c '_template/' docs/source/user_guide/output_layout.rst` is at least 3
    - `grep -c 'wholesale' docs/source/user_guide/output_layout.rst` is at least 1
    - `uv run tox -e docs-html` and `uv run tox -e docs-pdf` both report `build succeeded`
    - `git diff --stat typsphinx/` is empty
  </acceptance_criteria>
  <reversibility rating="reversible">Prose on one page; superseded by rewriting the same paragraphs.</reversibility>
  <done>The page describes per-key bundle directories, the unused-key rule, the built-in key's lack of special-casing and the no-deletion caveat, and no longer names a root-level shared template file in any of the three places.</done>
</task>

<task type="auto">
  <name>Task 2: Correct both published file counts and the assertion that pins one of them — one commit</name>
  <files>docs/source/user_guide/output_layout.rst, docs/source/user_guide/builders.rst, tests/test_output_layout_docs_gate.py</files>
  <read_first>
    - `docs/source/user_guide/output_layout.rst` lines 143-161 — the `Documents Shared by Several Masters` section and its closing count rule
    - `tests/test_output_layout_docs_gate.py` lines 351-400 — `test_three_master_project_emits_ten_typ_files`, which measures the real build's NINE-file root set plus `_template/typst/base.typ`, and lines 455-480 — `test_page_states_the_shared_child_composition`, whose literal count-clause assertion contradicts it
    - `docs/source/user_guide/builders.rst` lines 113-131 — the two-entry walkthrough and its file-count sentence
    - `tests/fixtures/state_guard_three_master_gate/` — the fixture the build-side test drives, so the corrected prose describes that exact project
  </read_first>
  <action>
<!-- planner-discipline-allow: writes ten -->
<!-- planner-discipline-allow: _template.typ -->
Both published file counts are stale for the same reason and are corrected together with the one
assertion that pins the prose, in a single commit, so the suite is never RED in between.

  1. **`output_layout.rst`'s three-master rule (currently 154-161).** The rule as published says a
     build writes one wrapper per `typst_documents` entry, one content file per document, and — unless
     the entry is on the package route — the reserved `_template.typ`, concluding that a three-master
     project over six documents writes ten `.typ` files and nine on the package route. Rewrite it so
     the root-level count is what the build actually produces: three wrappers plus six content files
     is **nine** root-level `.typ` files, with the template bundle for each USED key beside them one
     directory down rather than at the root; on the package route no bundle is copied at all, so the
     root-level count is unchanged at nine. Make the sentence explicit that the count being stated is
     the ROOT-LEVEL count, since the bundle now contributes files at a different depth.
  2. **`tests/test_output_layout_docs_gate.py::test_page_states_the_shared_child_composition`.**
     Its second assertion currently requires the literal clause containing the word `ten`. Update
     that literal to the exact corrected clause the rewritten prose publishes, and update the
     docstring and the failure message to match. Keep the existing design note explaining why the
     assertion quotes the whole clause rather than the bare number word — that reasoning still holds
     for the new number. This assertion and the prose above MUST land in the same commit; a commit
     containing only one of them leaves a previously-green test RED.
  3. **`builders.rst`'s two-entry walkthrough (currently 122-130).** It publishes five `.typ` files
     for a two-entry example: two wrappers, two content files, and a root-level shared template.
     Correct it to four root-level files — wrappers `main.typ` and `api-ref.typ`, content files
     `index.typ` and `api.typ` — plus the bundle for the registry key both entries use, one directory
     down under the reserved output directory. Leave the surrounding sentences about which files the
     `typstpdf` builder turns into PDFs, and the `:doc:` link to `output_layout`, untouched.
  </action>
  <verify>
    <automated>uv run pytest tests/test_output_layout_docs_gate.py -q && uv run tox -e docs-html && uv run tox -e docs-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_output_layout_docs_gate.py -q` exits 0 with every test in `TestPublishedOutputLayoutTextMatchesBuild` passing
    - `grep -c 'writes ten' docs/source/user_guide/output_layout.rst` is 0
    - `grep -c 'writes ten' tests/test_output_layout_docs_gate.py` is 0
    - `grep -c 'nine' docs/source/user_guide/output_layout.rst` is at least 1
    - `grep -c 'five ' docs/source/user_guide/builders.rst` is 0
    - `git log -1 --name-only` lists both `docs/source/user_guide/output_layout.rst` and `tests/test_output_layout_docs_gate.py` — the prose and its assertion landed in one commit
    - `uv run tox -e docs-html` and `uv run tox -e docs-pdf` both report `build succeeded`
  </acceptance_criteria>
  <done>Both pages publish the real root-level counts, the prose-binding assertion quotes the corrected clause, and the two changes are in a single commit.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the conditional hand-compile root rule and pin both of its branches by real compile</name>
  <files>docs/source/user_guide/output_layout.rst, tests/test_hand_compile_root_gate.py</files>
  <read_first>
    - `docs/source/user_guide/output_layout.rst` lines 37-52 (`Which File to Compile`) and lines 54-109 (`Where the Wrapper Is Written`, whose `A bare target` and `A path in the target` subsections define the two shapes the rule branches on, the latter using the target `"manuals/guide.typ"`)
    - `typsphinx/pdf.py` lines 120-155 — `typst.compile(typ_path, root=root_dir)`, the unconditional root pass that makes both builders immune
    - `typsphinx/writer.py` lines 190-235 — `compute_template_import_path()`, which produces the root-absolute import the rule is about
    - `tests/test_user_template_relative_asset_gate.py` lines 1-80 — the `TYPST_AVAILABLE` guard, the `_run_sphinx_build` per-module helper copy, and the class-scoped `build` fixture shape this new module follows
    - `tests/test_output_layout_docs_gate.py` lines 82-105 — the canonical `_run_sphinx_build` helper text to copy near-verbatim (this repository copies it per module rather than sharing it)
    - `.planning/phases/56-per-document-template-documentation/56-RESEARCH.md` § "The `--root` Claim, Empirically Corrected" — the two reproductions, including the exact failure the nested case produces without a root
  </read_first>
  <action>
Publish the hand-compile consequence in `output_layout.rst`'s existing `Which File to Compile`
section (D-03), written conditionally on the target's shape — because the unconditional form is
measurably false for the very example that section already walks through.

The note states: the wrapper imports its template by a project-root-absolute path, so hand-running
the Typst compiler resolves that import against the project root. Typst's default project root is the
directory containing the file being compiled. For a bare target — the `manual.typ` example this
section already uses — that directory already IS the output directory, so no root option is needed.
For a target carrying a path component, as the `A path in the target` subsection above shows, the
wrapper sits in a subdirectory and the default root no longer contains the template bundle, so the
compile must be given the output directory as the project root explicitly. Add one closing sentence
recording that neither the `typst` nor the `typstpdf` builder is affected either way, because
typsphinx passes the output directory as the project root on every compile it performs — which is why
this is worth writing down at all. Reference the two target shapes by their existing section names
using the corpus's implicit reference form (`` `A bare target`_ `` and `` `A path in the target`_ ``).
Show the concrete command form in a `.. code-block:: text` block, one line per branch, following this
page's existing use of `code-block:: text` for the warning examples.

Then create `tests/test_hand_compile_root_gate.py`, pinning BOTH branches. Module docstring cites
Phase 56 and D-03's post-research amendment, and states that publishing the conditional rule without
exercising the nested branch would leave the same review hole this phase exists to close. Structure:
  - `import typst` inside a `try`/`except ImportError` producing a `TYPST_AVAILABLE` flag, and a
    class-level `@pytest.mark.skipif(not TYPST_AVAILABLE, ...)` — this module legitimately carries the
    guard because it performs a real compile, unlike the static gates;
  - a `_run_sphinx_build` copy, near-verbatim from `tests/test_output_layout_docs_gate.py`, invoked as
    `sys.executable -m sphinx` and never as a resolved `sphinx-build` binary;
  - a `_build_source_tree(tmp_path, target)` helper writing a minimal Sphinx project at runtime — a
    `conf.py` setting `project`, `author`, `extensions = ["typsphinx"]` and a single-entry
    `typst_documents` whose target is the parameter, plus a one-heading `index.rst`. Built at runtime
    rather than committed, so the two variants cannot drift apart;
  - `TestBareTargetWrapperNeedsNoExplicitRoot`: build with target `"manual"`, assert the wrapper
    exists at the output root, assert its text contains the root-absolute template import, then call
    `typst.compile(<outdir>/manual.typ)` with NO root argument and assert it returns non-empty output
    rather than raising;
  - `TestNestedTargetWrapperNeedsTheOutdirAsRoot`: build with target `"manuals/guide.typ"`, assert the
    wrapper exists at that nested path, then assert `typst.compile(<wrapper>)` with no root RAISES,
    and that `typst.compile(<wrapper>, root=<outdir>)` returns non-empty output. Assert the raised
    error's text mentions the template file typsphinx could not find, so a future unrelated failure
    cannot masquerade as this one;
  - a `TestPublishedRootRuleIsConditional` class with no `typst-py` dependency that reads
    `output_layout.rst` and asserts the `Which File to Compile` region names both target shapes and
    does NOT state the root option unconditionally — assert the region contains a phrase scoping the
    requirement to a target with a path component.
  </action>
  <verify>
    <automated>uv run pytest tests/test_hand_compile_root_gate.py tests/test_output_layout_docs_gate.py -q && uv run tox -e docs-html && uv run tox -e docs-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_hand_compile_root_gate.py -q` exits 0 and collects at least 5 tests, none of them skipped in this environment (`typst-py` resolves here — measured)
    - `awk '/Which File to Compile/,/Where the Wrapper Is Written/' docs/source/user_guide/output_layout.rst | grep -c 'path component'` is at least 1
    - `grep -c 'A path in the target' docs/source/user_guide/output_layout.rst` is at least 2 — the section heading plus the new note's reference to it
    - `grep -c 'sphinx-build' tests/test_hand_compile_root_gate.py` is 0 — the helper invokes the interpreter module form, never a resolved binary
    - `uv run pytest -q` exits 0 with 0 failed
    - `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0
    - `uv run tox -e docs-html` and `uv run tox -e docs-pdf` both report `build succeeded`
  </acceptance_criteria>
  <done>The hand-compile rule is published scoped to the target shape it actually applies to, and both branches — bare target compiling with no root, nested target failing without one and succeeding with it — are proven by real `typst.compile()` calls against real builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime-generated `conf.py` → `sphinx-build` subprocess | `_build_source_tree()` writes a `conf.py` under `tmp_path` and Sphinx executes it |
| build output `.typ` → `typst.compile()` | the compiler reads the generated wrapper and follows its root-absolute import |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-07 | Tampering | the runtime-generated `conf.py` executed by Sphinx | medium | mitigate | the `conf.py` body is a literal written by the test, parameterized only by the two target strings the test itself supplies; nothing from the environment, the repository or an external source is interpolated into it, and the subprocess argument list is fixed |
| T-56-08 | Tampering | `typst.compile(..., root=<outdir>)` in the nested-target test | low | mitigate | the root passed is the pytest-managed `tmp_path` build directory the test just created; no repository path and no absolute path outside `tmp_path` is ever passed as a project root, so the compiler's file access stays inside the temporary tree |
| T-56-09 | Information Disclosure | compiler error text asserted in the nested-target test | low | accept | the asserted text is the compiler's own file-not-found message over `tmp_path` paths, which pytest already prints for every failing test in this suite |
| T-56-SC | Tampering | npm/pip/cargo installs | low | accept | this plan installs no package; `typst-py` is an existing dev dependency already used by four gate modules, and `56-RESEARCH.md` § "Package Legitimacy Audit" measured that the phase adds no dependency |
</threat_model>

<verification>
- `uv run pytest -q` — full suite green, 0 failed
- `uv run pytest tests/test_output_layout_docs_gate.py tests/test_hand_compile_root_gate.py -q` — green
- `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` — all clean
- `uv run tox -e docs-html && uv run tox -e docs-pdf` — both `build succeeded`
- `git diff --stat typsphinx/ docs/source/changelog.rst CHANGELOG.md` — empty
</verification>

<success_criteria>
- `output_layout.rst` describes per-key bundle directories, the unused-key rule and the no-deletion caveat, and names no root-level shared template file.
- Both published file counts match the real build, and the prose-binding assertion moved in the same commit as the prose.
- The hand-compile root rule is published conditionally and both of its branches are pinned by real compiles.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan:

- **New test module:** `tests/test_hand_compile_root_gate.py`
- **New test classes:** `TestBareTargetWrapperNeedsNoExplicitRoot`, `TestNestedTargetWrapperNeedsTheOutdirAsRoot`, `TestPublishedRootRuleIsConditional`
- **New helper functions:** `_run_sphinx_build()` (a per-module copy, per this repository's convention), `_build_source_tree()`
- **New module-level constants:** `TYPST_AVAILABLE`, `OUTPUT_LAYOUT_RST_PATH` (module-local)
- **Modified existing symbol:** `tests/test_output_layout_docs_gate.py::TestPublishedOutputLayoutTextMatchesBuild::test_page_states_the_shared_child_composition` — updated in place, not created

<output>
Create `.planning/phases/56-per-document-template-documentation/56-03-SUMMARY.md` when done.
</output>
