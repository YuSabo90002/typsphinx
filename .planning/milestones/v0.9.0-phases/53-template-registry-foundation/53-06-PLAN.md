---
phase: 53-template-registry-foundation
plan: 06
type: execute
wave: 5
depends_on: ["53-03", "53-05"]
gap_closure: true
files_modified:
  - typsphinx/builder.py
  - tests/test_registry_prewrite_validation_gate.py
  - tests/fixtures/conf14_prewrite_bad_last_gate/conf.py
  - tests/fixtures/conf14_prewrite_bad_last_gate/index.rst
  - tests/fixtures/conf14_prewrite_bad_last_gate/alpha.rst
  - tests/fixtures/conf14_prewrite_bad_last_gate/beta.rst
  - tests/fixtures/conf14_prewrite_bad_first_gate/conf.py
  - tests/fixtures/conf14_prewrite_bad_first_gate/index.rst
  - tests/fixtures/conf14_prewrite_bad_first_gate/aaa_bad.rst
  - tests/fixtures/conf14_prewrite_bad_first_gate/zzz_good.rst
  - tests/fixtures/conf14_prewrite_control_gate/conf.py
  - tests/fixtures/conf14_prewrite_control_gate/index.rst
  - tests/fixtures/conf14_prewrite_control_gate/four.rst
  - tests/fixtures/conf14_prewrite_control_gate/five.rst
  - .planning/phases/53-template-registry-foundation/53-06-RED-EVIDENCE.md
autonomous: true
requirements: [CONF-14, TPL-04]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A real `sphinx-build -b typst` over a multi-master config whose `typst_documents` names an unregistered registry key leaves ZERO `.typ` files anywhere under the build directory -- no `_template.typ`, no content file for any docname, no wrapper file (ROADMAP SC#3; 53-VERIFICATION.md gap `missing` item 2)."
    - "The zero-output guarantee holds for BOTH master orders -- the offending master sorting FIRST in `sorted(docnames)` and sorting LAST -- and the two builds raise a byte-identical `ExtensionError` message, which is SC#3's 'a multi-master config with a bad entry fails the same way regardless of which wrapper would have been written first'."
    - "`resolve_registry_key()` is reached for every usable `typst_documents` entry from inside `write()`, after `self._document_template_registry = resolve_template_registry(...)` and before `prepare_writing()`, so a bad element [4] is refused in the SAME up-front pass CONF-15/CONF-16/CONF-17/CONF-18 and D-08 already get (53-VERIFICATION.md gap `missing` item 1; D-03's `_validate_output_path_collisions()` precedent)."
    - "The up-front pass is a NO-OP for an untouched `conf.py`: with `typst_documents` populated and every entry either four-element (TPL-04) or naming the literal `typst`, it raises nothing, logs nothing, and the emitted `.typ` bytes are unchanged -- ROADMAP SC#2's byte-identity, already ✓ VERIFIED in 53-VERIFICATION.md truth #2, stays verified."
    - "An absent or empty `typst_documents`, and any entry that fails `_is_usable_typst_documents_entry()` (falsy, fewer than two elements, or a non-`str` docname), are skipped by the up-front pass without raising -- it never converts builder.py:120's tolerate-and-skip contract into a build failure (CONF-14 empty edge; probe fallback §A)."
    - "Declaring `typst_document_templates` at all does not perturb output for entries that do not name a user-defined key: a config declaring one unused user key still emits the same `.typ` set as one declaring none (TPL-03 ordering edge; probe fallback §A)."
    - statement: "If a build is interrupted mid-`write()`, or `write()` is ever driven by more than one worker, no partially-validated registry can be observed: validation runs on the single `write()` thread before `prepare_writing()`, and `write()` writes documents in a sequential `for` loop over `sorted(docnames)`."
      verification: backstop
  artifacts:
    - typsphinx/builder.py
    - tests/test_registry_prewrite_validation_gate.py
    - tests/fixtures/conf14_prewrite_bad_last_gate/conf.py
    - tests/fixtures/conf14_prewrite_bad_first_gate/conf.py
    - tests/fixtures/conf14_prewrite_control_gate/conf.py
    - .planning/phases/53-template-registry-foundation/53-06-RED-EVIDENCE.md
  key_links:
    - "`write()`'s new pass must filter entries through the module-level `_is_usable_typst_documents_entry()` (builder.py:120), the four-site single source of truth for 'can this entry produce a wrapper'. A fifth inline spelling of that question is exactly the drift BLD-03 closed, and its own docstring instructs that a genuinely different usability question gets a NEW named predicate instead."
    - "The regression must drive a real `sphinx-build` subprocess. `test_resolve_registry_key_bad_key_fails_identically_regardless_of_master_order` already passes against an in-memory registry while the partial-write defect is live -- an in-memory call can never observe which files reached disk, which is precisely why the suite never saw this."
    - "The new pass must sit between the existing `resolve_template_registry()` call (builder.py:737) and `prepare_writing()` (builder.py:742). `prepare_writing()` calls `_write_template_file()`, which writes `_template.typ` immediately -- one line later and the zero-output guarantee is already broken."
  prohibitions:
    - "MUST NOT weaken, skip, xfail, delete, or rewrite any existing test to make this plan pass. The suite is green at HEAD (1232 passed, 5 skipped, measured by the verifier); a red test after this change means the production change is wrong."
    - "MUST NOT downgrade any registry failure at the new call site to a `logger.warning` or a tolerate-and-skip. CONF-14 stops the build; the milestone's core value is that a documented configuration actually takes effect rather than silently degrading."
    - "MUST NOT satisfy the zero-output guarantee by writing files and then deleting them on failure. The guarantee is that nothing is ever written, which is what survives a crash between the write and the cleanup."
---

<objective>
Close ROADMAP Phase 53 Success Criterion #3, the single ✗ FAILED criterion in `53-VERIFICATION.md`.

CONF-14 -- a `typst_documents` entry whose element [4] names a key absent from the resolved registry
-- is the one registry validation that never got the up-front treatment CONF-15/CONF-16/CONF-17/
CONF-18 and D-08 all received. `resolve_registry_key()` is reached only from
`_write_typst_files()`'s per-docname wrapper loop (builder.py:1121), which runs strictly AFTER that
docname's own content file and after every earlier-sorted docname's content and wrapper files have
already hit disk. The verifier reproduced this live, twice, and measured that the SET of `.typ` files
surviving a failed build differs with the offending master's sort position -- verbatim the
order-dependent failure the phase goal text says the once-per-build placement exists to prevent.

Purpose: make "no output file is written when validation fails" structural for CONF-14, exactly as
`_validate_output_path_collisions()`'s own docstring already claims it is for the collision class,
and pin it with a regression that drives the real `write()` path in both master orders.
Output: `TypstBuilder._validate_registry_key_references()`, its `write()` call site, a new
subprocess+unit gate module, three fixture projects, and a recorded pre-fix RED transcript.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/53-template-registry-foundation/53-CONTEXT.md
@.planning/phases/53-template-registry-foundation/53-VERIFICATION.md
@.planning/phases/53-template-registry-foundation/53-REVIEW.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-first end-to-end gate for CONF-14 partial output, then the up-front validation pass</name>
  <precondition>The worktree has been provisioned with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"` prints a path inside THIS worktree, not the main checkout. Per CLAUDE.md § "Worktree-isolated execution" this is mandatory; without it a correct fix still measures RED because pytest imports the main tree's editable package.</precondition>
  <files>tests/fixtures/conf14_prewrite_bad_last_gate/conf.py, tests/fixtures/conf14_prewrite_bad_last_gate/index.rst, tests/fixtures/conf14_prewrite_bad_last_gate/alpha.rst, tests/fixtures/conf14_prewrite_bad_last_gate/beta.rst, tests/fixtures/conf14_prewrite_bad_first_gate/conf.py, tests/fixtures/conf14_prewrite_bad_first_gate/index.rst, tests/fixtures/conf14_prewrite_bad_first_gate/aaa_bad.rst, tests/fixtures/conf14_prewrite_bad_first_gate/zzz_good.rst, tests/test_registry_prewrite_validation_gate.py, typsphinx/builder.py, .planning/phases/53-template-registry-foundation/53-06-RED-EVIDENCE.md</files>
  <read_first>
    - `typsphinx/builder.py` lines 695-760 — `write()` as it stands: `_validate_output_path_collisions()` at 728, the `resolve_template_registry()` call at 737-739, `prepare_writing()` at 742, `_master_include_edges` at 756, then the `for docname in sorted(docnames)` loop. This is the exact region being edited; read it before touching it.
    - `typsphinx/builder.py` lines 517-564 — `_validate_output_path_collisions()`'s docstring. This is the source of truth for the invariant being extended: "Runs ONCE, called from `write()` at the very top -- before `prepare_writing()` ... so 'no output file is written when any collision is found' (D-02) is structural rather than a promise". The new method's docstring must state the same property for CONF-14 and cite this method by name.
    - `typsphinx/builder.py` lines 120-171 — `_is_usable_typst_documents_entry()`, the four-site single source of truth, and its instruction that a different usability question gets a new named predicate rather than an extension of that one.
    - `typsphinx/builder.py` lines 1085-1133 — `_write_typst_files()`'s content write (1068-1072), the lazy registry fallback (1106-1109), and the per-wrapper `resolve_registry_key()` call at 1121-1123. Needed to understand why the per-wrapper call is retained.
    - `typsphinx/template_registry.py` lines 333-389 — `resolve_registry_key()`'s full signature, both raise paths, and the exact wording of its CONF-14 message.
    - `tests/test_collision_validator_gate.py` lines 1-130 — the subprocess gate shape being mirrored: the module docstring convention, the per-module `_run_sphinx_build()` helper invoked as `sys.executable -m sphinx`, the `_no_typ_files_written()` rglob helper, and the assertion style. Do not import this module's helpers; this suite's convention is one copy per gate module.
    - `tests/fixtures/bld02_duplicate_target_gate/conf.py` — the fixture-conf.py convention: a header comment naming the plan, the requirement, and every load-bearing property that must not be edited.
    - `.planning/phases/53-template-registry-foundation/53-VERIFICATION.md` — the frontmatter `gaps:` block (the two `missing:` items are this task's literal contract) and the "Behavioral Spot-Checks" table rows recording which `.typ` files each of the two live builds left on disk.
    - `.planning/phases/53-template-registry-foundation/53-CONTEXT.md` — D-03 (accumulate-then-raise-once through an independent `ExtensionError`, never merged into the collision validator), D-05 (validation covers every declared key so order-independence holds trivially), D-06 (a present-but-non-`str` element [4] is the same CONF-14 class of error), and D-09.
    - `CLAUDE.md` § "Worktree-isolated execution" — why every command in this task is `uv run`.
  </read_first>
  <behavior>
    - Test 1 (`bad master sorts LAST`): a `sphinx-build -b typst` over `tests/fixtures/conf14_prewrite_bad_last_gate` exits non-zero, its combined stdout+stderr contains `not a registered typst_document_templates key`, and `list(Path(build_dir).rglob("*.typ"))` is empty.
    - Test 2 (`bad master sorts FIRST`): the same three assertions hold for `tests/fixtures/conf14_prewrite_bad_first_gate`, whose offending master sorts first in `sorted(docnames)` AND is declared first in `typst_documents` — both orders inverted relative to fixture 1.
    - Test 3 (order-independence of the message): the registry-key error text extracted from build 1 and from build 2 is byte-identical. Both fixtures name the same bad key and declare the same registry, so the message is the same string or the assertion is meaningless.
    - Pre-fix expectation (the RED): tests 1 and 2 fail on the `rglob` assertion (not on the exit code — the build already fails loudly today), and the surviving file sets DIFFER between the two fixtures. That difference is the recorded evidence.
  </behavior>
  <action>
Build the fixtures first, then the gate module, capture RED, then implement.

**Fixture `tests/fixtures/conf14_prewrite_bad_last_gate/`.** `conf.py` sets `project`, `author`,
`release`, `copyright`, `extensions = ["typsphinx"]`, then
`typst_document_templates` with exactly one user key `good` whose definition carries only
`package` set to `@preview/example:0.1.0` — package-only so it trips neither CONF-15's xor, nor
CONF-17's path arithmetic, nor D-08's existence check (both of those run only under `if template:`),
and so the fixture needs no template file on disk. Then `typst_documents` with two entries in this
order: `alpha` targeting `alpha_out.typ` with fifth element `good`, and `beta` targeting
`beta_out.typ` with fifth element `nope`. `index.rst` is the root document and carries a `toctree`
listing `alpha` and `beta` so neither master trips Sphinx's "not included in any toctree" warning.
`alpha.rst` and `beta.rst` each carry a title and one body line. Sorted docnames are
`alpha`, `beta`, `index`, so the offending master is written second — the verifier's live build A.
Head `conf.py` with a comment block naming plan 53-06, ROADMAP SC#3, and the load-bearing
properties: the bad key spelling `nope` and the registry key spelling `good` must match fixture 2
byte-for-byte or the message-identity assertion stops meaning anything; the docnames must keep their
alphabetical relationship to each other.

**Fixture `tests/fixtures/conf14_prewrite_bad_first_gate/`.** Identical `typst_document_templates`
and identical bad-key spelling. `typst_documents` declares the BAD entry first: `aaa_bad` targeting
`aaa_bad_out.typ` with fifth element `nope`, then `zzz_good` targeting `zzz_good_out.typ` with fifth
element `good`. Docnames `aaa_bad.rst`, `zzz_good.rst`, plus `index.rst` with a toctree listing both.
Sorted docnames are `aaa_bad`, `index`, `zzz_good`, so the offending master is written first — the
verifier's live build B. Both the write order and the declaration order are inverted relative to
fixture 1; that double inversion is what makes the byte-identical-message assertion load-bearing.

**Gate module `tests/test_registry_prewrite_validation_gate.py`.** Module docstring states: plan
53-06, closes `53-VERIFICATION.md`'s SC#3 gap, and paraphrases the pre-fix RED recorded in
`53-06-RED-EVIDENCE.md`. Carry its own private `_run_sphinx_build(source_dir, build_dir, builder)`
returning the `subprocess.CompletedProcess`, invoked as `sys.executable -m sphinx -b <builder>
<src> <out>` with `capture_output=True, text=True` — never a resolved `sphinx-build` binary, per
this suite's NixOS-sandbox rule. Carry its own `_typ_files(build_dir)` returning a sorted list of
`Path.rglob("*.typ")` results, or an empty list when the directory does not exist; return the LIST,
not a bool, so a failure message can name the survivors. Add no `skipif` on `typst` availability —
these builds use `-b typst` only and never compile. Class `TestRegistryKeyPreWriteGate` holds the
three behaviors above, each build writing into its own `tmp_path` subdirectory. For test 3, extract
the message by locating the substring `typst_documents entry names registry key` in each build's
combined output and comparing the remainder of that line.

**RED capture.** Run the module against the UNFIXED builder and record, verbatim, into
`.planning/phases/53-template-registry-foundation/53-06-RED-EVIDENCE.md`: the base commit SHA
(`git rev-parse HEAD`), the full `uv run pytest` failure output, and for each of the two fixtures a
literal `find <build_dir> -name '*.typ'` style listing of the surviving files. State explicitly that
the two surviving sets differ, and name them — that difference IS the order-dependence the fix
removes, and it is the milestone's standing GATE-01 RED-first bar (binding constraint #6) for this
change. Do not proceed to the implementation until both transcripts are written down.

**Implementation.** Add `_validate_registry_key_references(self) -> None` to `TypstBuilder`, defined
immediately after `_validate_output_path_collisions()` so the two pre-write validators sit together.
It reads `getattr(self.config, "typst_documents", []) or []`, skips every entry for which
`_is_usable_typst_documents_entry(entry)` is False (no warning — the collision validator already
emits the one per-build warning for those, per its own docstring), and for every remaining entry
calls `resolve_registry_key(self._document_template_registry, entry)`, discarding the return value
and letting the `ExtensionError` propagate. Its docstring must (a) state that it runs once, from
`write()`, before `prepare_writing()`, so "no `.typ` file is written when a registry-key reference is
bad" is structural, citing `_validate_output_path_collisions()` by name as the precedent; (b) record
that iteration is in `typst_documents` declaration order and the FIRST offending entry raises —
failures are deliberately NOT accumulated across entries, because `resolve_registry_key()` owns
CONF-14's message text and accumulating would mint a second, divergent message shape; declaration
order is fixed for a given `conf.py`, so the raise is byte-identical across runs (D-03); (c) record
that the per-wrapper `resolve_registry_key()` call in `_write_typst_files()` (builder.py:1121)
DELIBERATELY STAYS as the data-flow lookup that hands `render_wrapper()` its resolved
`TemplateRegistryEntry` — it is an idempotent dict lookup, it is load-bearing for the several
existing tests that drive `write_doc()`/`_write_typst_files()` directly without ever calling
`write()` (the same reason the lazy registry fallback at builder.py:1106 exists), and after this
change it can no longer be the FIRST place a bad key is noticed in a real build. No memoization is
introduced; a second per-entry cache would be new state for no behavioural gain.

Call it from `write()` on the line immediately after `self._document_template_registry =
resolve_template_registry(...)` ends and before the `logger.info("preparing documents... ")` /
`prepare_writing(docnames)` pair. `resolve_registry_key` is already imported at builder.py:22-26;
add no import. Note in the call-site comment that this pass covers EVERY `typst_documents` entry,
including entries whose docname is not in this build's `docnames` set — that is deliberate and
matches D-05's "validation covers every declared key, not only keys referenced by an entry being
written", which is what makes order-independence hold trivially.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry_prewrite_validation_gate.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry_prewrite_validation_gate.py -q` exits 0 with 3 passed.
    - `uv run python -c "import inspect; from typsphinx.builder import TypstBuilder as B; s=inspect.getsource(B.write); assert 'resolve_registry_key' in s or '_validate_registry_key_references' in s, s"` exits 0 — the validation is reachable from `write()`'s own source region, not only from the write loop.
    - `uv run python -c "import inspect; from typsphinx.builder import TypstBuilder as B; w=inspect.getsource(B.write); assert w.index('_validate_registry_key_references') < w.index('prepare_writing'), 'validation must precede prepare_writing()'"` exits 0.
    - `uv run python -c "import inspect; from typsphinx.builder import TypstBuilder as B; s=inspect.getsource(B._validate_registry_key_references); assert '_is_usable_typst_documents_entry' in s"` exits 0 — the shared predicate is used, not a fifth inline spelling.
    - After a `uv run python -m sphinx -b typst tests/fixtures/conf14_prewrite_bad_last_gate <tmp>` run, the command exits non-zero and `find <tmp> -name '*.typ'` prints nothing.
    - After the same command against `tests/fixtures/conf14_prewrite_bad_first_gate`, it exits non-zero and `find <tmp> -name '*.typ'` prints nothing.
    - `.planning/phases/53-template-registry-foundation/53-06-RED-EVIDENCE.md` exists, names the pre-fix commit SHA, and lists two DIFFERENT surviving-file sets, one per fixture.
    - `uv run pytest tests/test_template_registry.py -q` exits 0 with 57 passed — the existing registry suite is untouched and unaffected.
  </acceptance_criteria>
  <done>A `sphinx-build` whose `typst_documents` names an unregistered registry key leaves zero `.typ` files on disk, in both master orders, with a byte-identical message; the pre-fix partial-output transcript for both orders is recorded and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the up-front pass is a no-op for ordinary configs and for skipped entries</name>
  <files>tests/fixtures/conf14_prewrite_control_gate/conf.py, tests/fixtures/conf14_prewrite_control_gate/index.rst, tests/fixtures/conf14_prewrite_control_gate/four.rst, tests/fixtures/conf14_prewrite_control_gate/five.rst, tests/test_registry_prewrite_validation_gate.py</files>
  <read_first>
    - `typsphinx/builder.py` — `_validate_registry_key_references()` as Task 1 left it, and its `write()` call site.
    - `typsphinx/builder.py` lines 120-171 — `_is_usable_typst_documents_entry()`'s three False cases (falsy entry, fewer than two elements, non-`str` first element) and its tolerate-and-skip contract.
    - `typsphinx/template_registry.py` lines 333-381 — `resolve_registry_key()`'s `len(entry) > 4` branch, which is what makes a four-element tuple resolve to the built-in key without touching element [4] (TPL-04).
    - `tests/conftest.py` — the `temp_sphinx_app` fixture (a `SphinxTestApp` over a minimal generated srcdir) used by the in-process half of this task.
    - `tests/test_collision_predicate_completeness_gate.py` lines 362-382 — `TestIsUsableTypstDocumentsEntryUnit`, the in-repo precedent for an in-process unit class living inside a `_gate.py` module alongside subprocess gate classes, importing the symbol inside the test body.
    - `.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` — the SC#2 byte-identity baseline this task must not disturb; read its "Post-change summary verdict" section so the criterion being protected is concrete.
    - `.planning/phases/53-template-registry-foundation/53-CONTEXT.md` — D-05 and TPL-04's contract.
  </read_first>
  <behavior>
    - Test 4 (control build): `sphinx-build -b typst` over `tests/fixtures/conf14_prewrite_control_gate` exits 0 and every one of `_template.typ`, `index.typ`, `four.typ`, `four_out.typ`, `five.typ`, `five_out.typ` exists under the build directory.
    - Test 5 (empty list): a directly-constructed `TypstBuilder` with `typst_documents` set to the empty list and a resolved registry in place returns from `_validate_registry_key_references()` without raising.
    - Test 6 (unusable entries, parametrized): the same call with `typst_documents` set to a list holding an empty tuple, a one-element tuple, and a tuple whose first element is an `int` returns without raising — each of `_is_usable_typst_documents_entry()`'s three False cases.
    - Test 7 (TPL-04 pass-through): the same call with a four-element entry and with a five-element entry whose fifth element is the literal `typst` both return without raising.
    - Test 8 (two bad entries, determinism): the same call with two entries both naming unregistered keys raises once, the message names the FIRST-declared offending key, and two consecutive calls produce byte-identical message strings.
  </behavior>
  <action>
**Fixture `tests/fixtures/conf14_prewrite_control_gate/`.** `conf.py` declares
`typst_document_templates` with one user key `good` carrying only `package` set to
`@preview/example:0.1.0` — declared but referenced by NO entry, which is the point: declaring a
registry must not perturb output. `typst_documents` holds three entries: a FOUR-element entry for
docname `four` targeting `four_out.typ` (TPL-04's absent element [4]); a FIVE-element entry for
docname `five` targeting `five_out.typ` whose fifth element is the literal `typst`; and a
one-element tuple holding only `index`, which `_is_usable_typst_documents_entry()` rejects and every
write-phase site tolerates and skips. `index.rst` is the root with a toctree listing `four` and
`five`; `four.rst` and `five.rst` each carry a title and one body line. Head `conf.py` with a
comment naming plan 53-06, ROADMAP SC#2, and the load-bearing properties: the four-element entry
must stay four-element, the five-element entry's fifth element must stay the literal reserved key,
and the one-element entry must stay unusable — each pins a different no-op path through the new
validation pass.

**Test 4** goes in `TestRegistryKeyPreWriteGate` alongside Task 1's three, reusing the module's
`_run_sphinx_build()` and asserting `returncode == 0` plus the six expected files. Assert on the
sorted result of the module's `_typ_files()` helper so an unexpected extra file is reported by name
rather than passing silently.

**Tests 5-8** go in a new class `TestValidateRegistryKeyReferencesUnit` in the same module,
following the in-repo convention of importing `TypstBuilder` and `resolve_template_registry` inside
the test bodies. Each test uses the `temp_sphinx_app` fixture, constructs `TypstBuilder(app,
app.env)`, assigns `builder._document_template_registry = resolve_template_registry(builder.config,
str(app.srcdir))`, sets `builder.config.typst_documents` to the case under test, and calls
`builder._validate_registry_key_references()`. Tests 5-7 assert the call returns `None` and raise
nothing; parametrize test 6 over the three unusable shapes so each False case is its own named case,
matching this suite's one-assertion-per-named-case granularity. Test 8 wraps the call in
`pytest.raises(ExtensionError)` twice and asserts the two message strings are equal and that the
message contains the repr of the first-declared bad key and not the second's — this pins the
declaration-order/first-raise decision Task 1 recorded, so a later refactor toward accumulation is a
deliberate, test-visible change rather than a silent one.

Do not touch `tests/test_template_registry.py`, `tests/test_collision_validator_gate.py`, or any of
the files asserting the root `_template.typ`. Nothing in this task edits `typsphinx/`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry_prewrite_validation_gate.py -v && uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry_prewrite_validation_gate.py -q` exits 0 with at least 8 passed (3 from Task 1 plus test 4 plus the parametrized 5-8 set).
    - `uv run pytest tests/ -q` reports `0 failed` — the standing green-at-every-phase-boundary constraint (milestone binding constraint #2).
    - `uv run python -m sphinx -b typst tests/fixtures/conf14_prewrite_control_gate <tmp>` exits 0 and `find <tmp> -name '*.typ' | sort` lists exactly `_template.typ`, `five.typ`, `five_out.typ`, `four.typ`, `four_out.typ`, `index.typ`.
    - `git diff --name-only` for this task lists no file under `typsphinx/`.
    - `grep -rl "_template\.typ" tests/ | wc -l` returns the same count as before this plan (re-measure it, do not hardcode); none of those files appears in this plan's diff.
  </acceptance_criteria>
  <done>An ordinary populated `conf.py` — four-element tuples, explicit `typst` fifth elements, an unusable entry, and a declared-but-unreferenced registry — builds to exactly the same `.typ` set it did before, and every skip path through the new validation pass is pinned by a named test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` → `TypstBuilder.write()` | User-authored Python config values (`typst_documents`, `typst_document_templates`) cross into the builder untyped — Sphinx validates only that `typst_document_templates` is a `dict`, never its keys, values, or the shape of `typst_documents` tuples. |
| `TypstBuilder.write()` → filesystem under `outdir` | Every `.typ` path this phase can cause to be written or NOT written. The gap being closed is entirely about which side of a validation failure this boundary is crossed on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-06-01 | Tampering | `TypstBuilder._validate_registry_key_references()` | medium | mitigate | The new pass performs dict lookups only — a registry key never reaches `os.path.join`, `mkdir`, or `open` in this phase. Refusing an unregistered key before `prepare_writing()` is what stops a key from reaching Phase 54's `_template/<key>/` write site at all; the key-shape denylist that guards that site stays exactly the seven locked cases (D-02). |
| T-53-06-02 | Denial of Service | partial `.typ` output surviving a failed build | medium | mitigate | A failed build currently leaves an order-dependent subset of `.typ` files on disk, which a later `typst compile` or a CI artifact step can consume as if it were a complete build. Mitigated by hoisting validation above `prepare_writing()` so zero files are written, pinned by the `rglob("*.typ") == []` assertion in both master orders. |
| T-53-06-03 | Information disclosure | `resolve_registry_key()`'s `ExtensionError` message | low | accept | The message embeds the user's own registry keys and the offending value via `repr()`. These are values the user authored in their own `conf.py` and the message is required by CONF-14 to name the registered keys; no build-machine paths or environment values are added. |
| T-53-06-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan. Zero new runtime dependencies is a standing milestone invariant; this plan adds only stdlib-level code and test fixtures, so the supply-chain gate has no install site to guard. |

Canon referral: path traversal via a registry key that later becomes a directory name is canon
security ground owned by `/gsd-secure-phase` and by Phase 54's own bundle-write threat model; it is
not minted as a bespoke prohibition here.
</threat_model>

<verification>
1. `uv run pytest tests/test_registry_prewrite_validation_gate.py -v` — all tests pass.
2. `uv run pytest tests/ -q` — `0 failed`.
3. Two manual `uv run python -m sphinx -b typst` runs over the two failing fixtures, each followed by
   `find <outdir> -name '*.typ'` printing nothing.
4. One manual run over the control fixture exiting 0 with all six `.typ` files present.
5. `53-06-RED-EVIDENCE.md` records the pre-fix commit SHA and two DIFFERENT surviving-file sets.
</verification>

<success_criteria>
- ROADMAP SC#3's "each fires once per build and order-independently ... following
  `_validate_output_path_collisions()`'s 'runs once, at the very top of `write()`' precedent" is true
  for CONF-14, not only for CONF-15/CONF-16/CONF-17/CONF-18 and D-08.
- Both `missing:` items in `53-VERIFICATION.md`'s SC#3 gap are discharged: the `write()` call site
  exists, and the regression drives the real `write()` path in both master orders asserting zero
  `.typ` files.
- ROADMAP SC#2 stays ✓ VERIFIED: the full suite is green and the control fixture emits its expected
  `.typ` set unchanged.
</success_criteria>

## Artifacts this phase produces

**New production symbol**

- `TypstBuilder._validate_registry_key_references(self) -> None` — `typsphinx/builder.py`, defined
  immediately after `_validate_output_path_collisions()`. Iterates `config.typst_documents` in
  declaration order, skips entries failing `_is_usable_typst_documents_entry()`, and calls
  `resolve_registry_key(self._document_template_registry, entry)` for the rest. Raises the
  `ExtensionError` `resolve_registry_key()` raises, unchanged.

**Changed behaviour (no signature change)**

- `TypstBuilder.write()` — gains one call to `_validate_registry_key_references()` between the
  existing `resolve_template_registry()` assignment and `prepare_writing()`. A build whose
  `typst_documents` names an unregistered registry key now fails before any `.typ` file exists,
  where it previously failed mid-loop with an order-dependent partial output set.
- `TypstBuilder._write_typst_files()` — UNCHANGED. Its `resolve_registry_key()` call at
  builder.py:1121 is deliberately retained as the data-flow lookup feeding `render_wrapper()`.

**New test module and test names** — `tests/test_registry_prewrite_validation_gate.py`

- `_run_sphinx_build(source_dir, build_dir, builder)` (module-private helper)
- `_typ_files(build_dir)` (module-private helper)
- `TestRegistryKeyPreWriteGate::test_conf14_bad_key_sorting_last_writes_no_typ_files`
- `TestRegistryKeyPreWriteGate::test_conf14_bad_key_sorting_first_writes_no_typ_files`
- `TestRegistryKeyPreWriteGate::test_conf14_message_identical_across_both_master_orders`
- `TestRegistryKeyPreWriteGate::test_control_config_builds_unchanged_typ_set`
- `TestValidateRegistryKeyReferencesUnit::test_empty_typst_documents_does_not_raise`
- `TestValidateRegistryKeyReferencesUnit::test_unusable_entries_are_skipped_without_raising`
- `TestValidateRegistryKeyReferencesUnit::test_four_element_and_explicit_typst_entries_do_not_raise`
- `TestValidateRegistryKeyReferencesUnit::test_two_bad_entries_raise_once_naming_the_first`

**New fixture projects**

- `tests/fixtures/conf14_prewrite_bad_last_gate/` — `conf.py`, `index.rst`, `alpha.rst`, `beta.rst`
- `tests/fixtures/conf14_prewrite_bad_first_gate/` — `conf.py`, `index.rst`, `aaa_bad.rst`, `zzz_good.rst`
- `tests/fixtures/conf14_prewrite_control_gate/` — `conf.py`, `index.rst`, `four.rst`, `five.rst`

**New planning artifact**

- `.planning/phases/53-template-registry-foundation/53-06-RED-EVIDENCE.md` — pre-fix commit SHA,
  verbatim pytest failure output, and the two differing surviving-`.typ` sets. Deliberately NOT named
  `53-VERIFICATION.md`, which `gsd-verifier` reserves and would clobber (D-12).

## Spec-less probe fallback — edge disposition (§A/§C)

Phase 53 has no SPEC, so the deterministic edge probe was run over its nine requirement IDs
(15 applicable, 0 pre-resolved). Eight of the nine requirements are already ✓ VERIFIED on disk, so
each row below is dispositioned rather than blindly re-authored. **15 surfaced = 5 authored new
(3 here, 2 in 53-07) + 6 already-covered-upstream + 4 flagged assumptions.** No silent drops.

| # | Req / category | Disposition |
|---|---|---|
| 1 | TPL-01 / adjacency | Already covered upstream — `53-03-PLAN.md` `must_haves` truth "Two registry keys that are byte-equal cannot both exist ... two that are equal only after casefolding collide and are rejected — they never silently merge"; re-measured at `53-VERIFICATION.md` truth #4 (✓ VERIFIED). |
| 2 | TPL-01 / empty | Already covered upstream — `53-03-PLAN.md` truth "A registry declaring exactly one key has no other registered key to compare against"; plus `53-02`'s `test_empty_typst_document_templates_resolves_to_only_the_typst_key`. `53-VERIFICATION.md` truth #1 (✓ VERIFIED). |
| 3 | TPL-01 / ordering | Covered upstream (`53-03-PLAN.md`'s `sorted()` byte-identity truth) but **RE-OPENED** by 53-07's sort-key change → re-authored as a new `must_haves` truth in `53-07-PLAN.md`. |
| 4 | TPL-03 / adjacency | Already covered upstream — `53-03-PLAN.md` truth on D-04 (`Typst`/`TYPST` pass as ordinary keys); the resulting Phase 54 bundle-directory collision is a recorded Deferred Idea in `53-CONTEXT.md`. `53-VERIFICATION.md` CONF-16 row (✓ SATISFIED). |
| 5 | TPL-03 / empty | Already covered upstream — `53-01`'s `53-RED-EVIDENCE.md` Shape D (nothing set) and `53-02`'s `test_default_config_resolves_registry_with_only_the_typst_key`. `53-VERIFICATION.md` truth #2 (✓ VERIFIED). |
| 6 | TPL-03 / ordering | **Authored here** — `must_haves` truth: declaring an unused user key does not perturb the emitted `.typ` set, and the up-front pass is a no-op for an untouched `conf.py`. Verified by Task 2's control fixture. |
| 7 | TPL-04 / unclassified | **UNRESOLVED — flagged assumption.** The probe could not classify an edge category for TPL-04. Not auto-resolved and not dismissed. See `## Flagged assumptions` below. |
| 8 | TPL-05 / unclassified | **UNRESOLVED — flagged assumption.** See below. |
| 9 | CONF-14 / empty | **Authored here** — `must_haves` truth: absent/empty `typst_documents` and entries failing `_is_usable_typst_documents_entry()` are skipped without raising. (The empty-REGISTRY half is already covered upstream by `53-03`'s "the message lists `['typst']`, never an empty list".) Verified by Task 2 tests 5-6. |
| 10 | CONF-14 / encoding | Already covered upstream — `53-03-PLAN.md` truth "Registry-key lookup ... is exact `str` equality, never case-folded", test at `tests/test_template_registry.py:678`. `53-VERIFICATION.md` CONF-14 row. |
| 11 | CONF-15 / unclassified | **UNRESOLVED — flagged assumption.** See below. |
| 12 | CONF-16 / unclassified | **UNRESOLVED — flagged assumption.** See below. |
| 13 | CONF-17 / concurrency | Covered upstream by a `53-03-PLAN.md` truth whose "no `.typ` file is written when validation fails" half was **falsified for CONF-14** by `53-VERIFICATION.md`'s SC#3 row → **authored here** as a `backstop`-verification `must_haves` marker for the interrupted/parallel half, with the zero-output half carried by this plan's explicit truths 1-3. |
| 14 | CONF-18 / empty | Already covered upstream — `53-03-PLAN.md`'s single-key truth (case 7 cannot fire on a one-key registry; the other six still apply). `53-VERIFICATION.md` truth #4 (✓ VERIFIED). |
| 15 | CONF-18 / encoding | Covered upstream (`53-03-PLAN.md`'s `str.casefold()`-not-NFC truth) but **EXTENDED** by 53-07's non-`str` key path → re-authored as a new `must_haves` truth in `53-07-PLAN.md`. |

## Flagged assumptions

Surfaced by the probe, deliberately left `unresolved` rather than auto-resolved or dismissed
(§A forbids auto-dismissal, and an `unclassified` row must never be auto-resolved with `backstop`):

- **TPL-04 (unclassified).** The probe returned no edge category. TPL-04 is ✓ SATISFIED per
  `53-VERIFICATION.md` and its four-element/explicit-`typst` equivalence is re-pinned by this plan's
  Task 2 test 7 — but no probe-derived edge obligation was generated, so none is claimed.
- **TPL-05 (unclassified).** Same. TPL-05 is ✓ SATISFIED (shared-key identity live-confirmed,
  `r1 is r2 → True`); no probe-derived edge obligation is claimed.
- **CONF-15 (unclassified).** Same. ✓ SATISFIED; the non-`dict` definition robustness gap adjacent to
  it is handled as a `53-REVIEW.md` finding in `53-07-PLAN.md`, not as a probe edge.
- **CONF-16 (unclassified).** Same. ✓ SATISFIED.

## Deferred pointer (not a task)

`53-REVIEW.md` **CR-01** — a bare drive-qualified registry key (`C:` / `C:foo`) is accepted — is
recorded here as a pointer only. `53-VERIFICATION.md` classifies it `ℹ️ INFO — NOT a gap`;
`53-CONTEXT.md`'s Deferred Ideas locks `< > : " | ? *` as accepted in Phase 53 by design; and
`53-03-PLAN.md`'s own `must_haves.prohibitions` forbids an eighth denylist case. It is NOT planned in
this phase and no task may drift into it.

<output>
Create `.planning/phases/53-template-registry-foundation/53-06-SUMMARY.md` when done
</output>
