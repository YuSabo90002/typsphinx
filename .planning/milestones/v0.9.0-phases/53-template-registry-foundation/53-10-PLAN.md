---
phase: 53-template-registry-foundation
plan: 10
type: execute
wave: 8
depends_on: ["53-05", "53-08", "53-09"]
gap_closure: true
files_modified:
  - .planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md
autonomous: true
requirements: [TPL-01, TPL-03, TPL-04, TPL-05, CONF-14, CONF-15, CONF-16, CONF-17, CONF-18]
user_setup:
  - service: github
    why: "SC#5 requires the milestone branch on origin plus a completed 3-OS CI run dispatched through the GitHub API. The branch is already on origin at a stale SHA; this plan advances it and dispatches a fresh run."
    env_vars: []
    dashboard_config:
      - task: "The `gh` CLI must already be authenticated with push access to this repository's `origin`. Verify with `gh auth status` before the push task; no new credential is created by this plan."
        location: "local `gh` CLI configuration"

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`git ls-remote --heads origin gsd/v0.9.0-per-document-templates` returns the SHA this plan measured with `git rev-parse gsd/v0.9.0-per-document-templates` at push time -- not the `48c957cd` measured at planning time, and not the executing worktree's own HEAD (SC#5)."
    - "A CI workflow run over `gsd/v0.9.0-per-document-templates` reaches status `completed` with conclusion `success`, its `event` is `workflow_dispatch`, and its `headSha` equals the SHA this plan pushed. The run is identified by matching all three of name `CI`, event `workflow_dispatch` and that `headSha` -- never by 'the newest run on the branch', because every push to this branch also fires the separate `Link Check` workflow (measured: `gh run list` on this branch shows four `push`-event `Link Check` runs and two `workflow_dispatch` `CI` runs)."
    - "`gh run view <run-id> --json jobs` shows conclusion `success` for both `windows-latest` `test` legs and both `macos-latest` `test` legs, transcribed verbatim into the evidence artifact (SC#5)."
    - "STALENESS ASSERTION, checked at the moment the evidence is recorded and stated in the artifact: `git log <ci_run_head>..gsd/v0.9.0-per-document-templates -- typsphinx/ tests/` is EMPTY. This is the check that would have caught the previous round's failure, where a green run at head `d1eff100` was cited while four later commits -- `c9d1eb3b`, `512a211b`, `8d45e0b5`, `eb69904f` -- had changed `typsphinx/` and `tests/` (re-measured during planning; the prior verification counted three, the fourth is a `tests/` commit)."
    - "POSITIVE CONTENT ASSERTION on the certified SHA, so the evidence proves WHICH code CI ran rather than only that a run was green: `git show <pushed_sha>:typsphinx/template_registry.py` contains the plan-53-08 container-guard message text, and `git show <pushed_sha>:.planning/REQUIREMENTS.md` contains `| TPL-01 | Phase 53 | Complete |`. Both are recorded in the artifact."
    - "Documentation-only drift after the certified head is named explicitly and permitted: this plan's own evidence commit, the executor SUMMARY, and the phase-tracking commits all touch only `.planning/`, so they move the branch tip past the certified head WITHOUT invalidating it. The artifact states this in words and states the inverse rule: any commit touching `typsphinx/` or `tests/` after the certified head invalidates the evidence and requires a re-push and a re-dispatch."
    - "The artifact records the previous round's stale evidence honestly rather than overwriting it: the existing Run 1 / Run 2 sections stay, and the new run is appended as a dated section that states why Run 2 no longer certifies the shipping code."
    - "No pull request is opened; no branch is renamed, merged, rebased, force-pushed or deleted. The stale local `gsd/v0.9.0-milestone` still resolves to `aed773c9807ab871468b1b2a7e1ec36b54e82907`."
  artifacts:
    - .planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md
  key_links:
    - "The ref to push and to measure is the BRANCH BY NAME, `gsd/v0.9.0-per-document-templates` -- never `HEAD`. Worktree isolation is this project's standing execution mode (CLAUDE.md), so an executor's `git rev-parse HEAD` returns its OWN worktree branch tip, which is not the milestone branch and does not contain the wave-1 merges. Every SHA in this plan is measured with `git rev-parse gsd/v0.9.0-per-document-templates` and every push names that ref explicitly."
    - "A push alone runs no CI. `.github/workflows/ci.yml` lines 3-8 scope `push` and `pull_request` to `main`/`develop`; `workflow_dispatch` is the only trigger available on a feature branch. The required sequence is push, then `gh workflow run CI --ref gsd/v0.9.0-per-document-templates`, then poll, then capture -- skipping the dispatch produces only a `Link Check` run and no test coverage at all."
    - "Lint coverage for this phase comes ONLY from the dispatched run's `Lint and Format Check` job. `ruff` cannot execute locally in this sandbox (a generic-linux ELF that NixOS cannot exec, recorded as Future requirement QUA-06), so a local pytest pass is never lint evidence -- state this in the artifact as 53-05 already does."
    - "This plan runs in the LAST wave and depends on both 53-08 and 53-09 precisely because the previous round failed by certifying a tip that later code commits overtook. Nothing in this plan may run before those two have merged into the milestone branch; the staleness assertion is what makes that ordering falsifiable rather than assumed."
  prohibitions:
    - "MUST NOT record any value that a command run in this plan did not produce. Run IDs, job names, conclusions, head SHAs, timestamps and durations are transcribed from `gh`/`git` output; none may be reconstructed, estimated, or carried over from `53-VERIFICATION.md`, this plan, or the existing evidence artifact."
    - "MUST NOT certify a CI head that any `typsphinx/` or `tests/` commit post-dates. If the staleness assertion fails at recording time, the correct action is to push the new tip and dispatch again, recording both attempts -- never to record the earlier run with a caveat."
    - "MUST NOT force-push, rename, merge, rebase or delete any branch, and MUST NOT open a pull request. If a push is rejected or reports an unexpected divergence, halt and report rather than forcing."
    - "MUST NOT re-dispatch repeatedly to escape a genuine failure. If a `test` leg concludes other than `success`, record the lane, the job name and the failing log excerpt as a finding and stop; a defect that only the Windows or macOS lane can see is exactly what SC#5 exists to surface."
---

<objective>
Close SC#5, the single scored gap in `53-VERIFICATION.md`, on evidence that certifies the code that
actually ships.

Measured during this planning session, independently of the verifier: `origin`'s
`gsd/v0.9.0-per-document-templates` is at `48c957cd` while the local branch tip is `344b9510`, 24
commits ahead. The only green `CI` run on the branch is `31875707734` at head `d1eff100`, and
`git log d1eff100..HEAD -- typsphinx/ tests/` lists four commits that post-date it: `c9d1eb3b`,
`512a211b`, `8d45e0b5` and `eb69904f`. One of them, `8d45e0b5`, is a Windows cross-drive `ValueError`
guard -- precisely the class of change the `windows-latest` lane exists to exercise -- and it has never
run there. Plan 53-08 adds two more `typsphinx/`+`tests/` commits on top.

The previous round did the mechanics correctly and still failed, because the CI work ran before the
last code landed. This plan therefore runs in the LAST wave, depends on both 53-08 and 53-09, measures
the branch tip at execution time rather than trusting any SHA written here, and records an explicit
staleness assertion as an acceptance criterion instead of the weaker "a run succeeded".

Its `requirements` field lists all nine of the phase's IDs deliberately: the dispatched run is the only
cross-platform evidence the phase has, and ROADMAP SC#4 states outright that CONF-18's
reserved-device-name and case-collision failures are structurally invisible to a local Linux-only run.

Purpose: make "the milestone branch is on origin with CI over the code that ships" a falsifiable,
re-checkable fact rather than an artifact that silently ages out.
Output: a new dated section appended to `53-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/53-template-registry-foundation/53-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local CI-parity gates and the pre-push tip measurement</name>
  <precondition>The worktree has been provisioned with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"` prints a path inside THIS worktree. Additionally, plans 53-08 and 53-09 must already be merged into `gsd/v0.9.0-per-document-templates`: `git log gsd/v0.9.0-per-document-templates --oneline -20` shows their commits. If it does not, halt -- this plan is wave 2 for exactly that reason.</precondition>
  <files>.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md</files>
  <read_first>
    - `.github/workflows/ci.yml` lines 1-30 -- the trigger scoping (`push`/`pull_request` limited to `main`/`develop`, `workflow_dispatch` always available) and the matrix (`os: [ubuntu-latest, windows-latest, macos-latest]` x `python-version: ['3.12','3.13']`, giving six `test` job legs, plus `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package` and two `Integration Test` jobs -- twelve in total).
    - `.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md` in full -- the artifact this task extends. Its Run 1 / Run 2 sections are the format to follow and must not be edited or deleted; the new run is appended.
    - `CLAUDE.md` § "Worktree-isolated execution" -- why every command runs through `uv run`, and why an executor's own HEAD is not the milestone branch tip.
    - `CLAUDE.md` § "Commands" -- the exact local gate set CI mirrors: `pytest`, `black --check .`, `ruff check .`, `mypy typsphinx/`.
    - `.planning/STATE.md` § "Active Milestone (v0.9.0)" -- the corrected branch name and the note that `gsd/v0.9.0-milestone` is a stale local branch carrying zero milestone commits, which must be left untouched.
  </read_first>
  <action>
Do not push anything in this task. Its job is to make a wasted CI run unlikely and to establish the
SHA the next task will certify.

Run the four CI-parity gates locally and transcribe each result: `uv run pytest tests/ -q`,
`uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/`. Record the measured outcome
of each, including a failure. If `ruff` cannot execute (the recorded NixOS generic-linux ELF hazard,
Future requirement QUA-06), transcribe the actual error text and state plainly that lint coverage for
this phase therefore comes from the dispatched run's `Lint and Format Check` job -- do not claim a
local lint pass that did not happen.

Pre-empt the locale-dependent failure class this project has been bitten by before: run
`LC_ALL=C uv run pytest tests/ -q` as well and confirm the pass count matches the ambient-locale run.
A divergence is a finding to report, not something to work around; it means a test asserts on a
gettext-translated string and would fail on a differently-localized CI runner for reasons unrelated to
the code.

Measure the branch tip with `git rev-parse gsd/v0.9.0-per-document-templates` -- by ref name, never
`git rev-parse HEAD`, which in an isolated worktree returns this executor's own branch. Record that SHA
as the candidate. Confirm with `git log gsd/v0.9.0-per-document-templates --oneline -20` that plan
53-08's and plan 53-09's commits are present; if they are not, halt and report rather than pushing an
incomplete tip.

Confirm the working tree is clean apart from this artifact: `git status --porcelain` should list only
`.planning/` paths.

Open a new dated section in `53-CI-EVIDENCE.md` headed for this gap-closure round, recording: the
candidate SHA and the command that produced it, the four local gate results verbatim, both locale runs'
summary lines, and the `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` value measured
right now (the stale SHA the push is about to advance). Leave the existing Run 1 and Run 2 sections
untouched.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` exits 0 and its summary line is transcribed into the new section.
    - `LC_ALL=C uv run pytest tests/ -q` exits 0 with the same passed count as the ambient-locale run, and both summary lines are transcribed.
    - `uv run black --check .` exits 0 and `uv run mypy typsphinx/` exits 0, both transcribed.
    - The `uv run ruff check .` outcome is transcribed verbatim -- either a clean pass or the exact execution error, never an unmeasured claim.
    - The new section records a 40-hex-character candidate SHA and states it came from `git rev-parse gsd/v0.9.0-per-document-templates`.
    - `git log gsd/v0.9.0-per-document-templates --oneline -20 | grep -c '53-08'` returns at least 1 and `| grep -c '53-09'` returns at least 1 -- both wave-1 plans have merged into the branch being certified.
    - The new section records the pre-push `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` value.
    - The existing `## Run 1` and `## Run 2` headings are still present in the file.
  </acceptance_criteria>
  <done>The four CI-parity gates and the locale control are measured and recorded, and the exact milestone-branch SHA that CI will certify is fixed by measurement rather than by assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Push the measured tip, dispatch CI over it, and capture per-lane conclusions</name>
  <reversibility rating="reversible">The branch is already published on `origin` at `48c957cd`; this advances an existing public ref with a fast-forward push and dispatches a re-runnable workflow. No new door is opened, no history is rewritten, and the publication decision itself is already locked as ROADMAP binding constraint #9 -- so no checkpoint is inserted, per the rule against re-litigating a decision the owner has already made.</reversibility>
  <files>.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md` -- the section Task 1 opened, and the Run 2 section whose `gh run view ... --json` capture shape this task reproduces.
    - `.planning/ROADMAP.md` § "Phase 53" SC#5 -- the literal required sequence and the literal required evidence: a `git ls-remote --heads origin` hit plus at least one completed CI run including the `windows-latest` and `macos-latest` lanes, produced by `workflow_dispatch`.
    - `.github/workflows/ci.yml` -- the twelve job names, so the capture can be checked for completeness rather than only for the two named lanes.
  </read_first>
  <action>
Push the milestone branch by name: `git push origin gsd/v0.9.0-per-document-templates`. Push that ref
only -- not the executing worktree's own branch, not `--all`, not `--force`, and open no pull request
(the owner chose dispatch over a draft PR). Leave the stale local `gsd/v0.9.0-milestone` exactly where
it is. If the push is rejected or reports an unexpected divergence, halt and report rather than forcing.

Confirm the ref landed with `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` and
transcribe the line verbatim. Assert that the SHA it reports equals Task 1's candidate SHA; if it does
not, something moved the branch between the measurement and the push -- re-measure, re-push, and record
both attempts rather than proceeding.

Dispatch: `gh workflow run CI --ref gsd/v0.9.0-per-document-templates`. A push alone runs no CI on this
branch; only the dispatch does.

Poll with `gh run list --branch gsd/v0.9.0-per-document-templates --limit 10 --json databaseId,name,event,status,conclusion,headSha,createdAt` until the run matching ALL THREE of name `CI`, event
`workflow_dispatch` and `headSha` equal to the pushed SHA reaches status `completed`. Match on those
three fields, never on recency: every push to this branch also fires the separate `Link Check`
workflow, so the newest run on the branch is routinely not the one being waited on. The v0.8.0 and
53-05 precedents both ran about six minutes; bound the polling at roughly twenty minutes and report a
timeout as a finding rather than proceeding on an incomplete run.

Capture `gh run view <run-id> --json status,conclusion,headSha,event,createdAt,updatedAt,url,displayTitle`
and `gh run view <run-id> --json jobs -q '.jobs[] | {name, conclusion}'`, and transcribe both verbatim
into the new section alongside the run ID, the run URL and the computed duration.

State explicitly, in the artifact, how many of the six `Test Python … on …` legs concluded `success`,
and name the four platform-specific legs individually: `Test Python 3.12 on windows-latest`,
`Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest`, `Test Python 3.13 on
macos-latest`.

If any lane concludes other than `success`, record the failing lane, the job name and the relevant
`gh run view <run-id> --log-failed` excerpt, and report it as a finding. Do not re-dispatch hoping for
a different result; a lane that only fails on Windows or macOS is the exact signal SC#5 exists to
surface, and the previous round's honest two-run record is the precedent to follow.
  </action>
  <verify>
    <automated>git ls-remote --heads origin | grep -c 'gsd/v0.9.0-per-document-templates'</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` returns exactly one line whose SHA equals Task 1's candidate SHA, and that line is transcribed verbatim into the artifact.
    - The artifact records a numeric run ID, the run URL, `"event": "workflow_dispatch"`, and a `headSha` equal to the pushed SHA.
    - The artifact contains the verbatim `gh run view <run-id> --json jobs` output, and that output shows `"conclusion":"success"` for all four of `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`.
    - The artifact states explicitly how many of the six `Test Python … on …` legs and how many of the twelve total jobs concluded `success`.
    - `gh pr list --head gsd/v0.9.0-per-document-templates` returns no pull request opened by this plan.
    - `git rev-parse gsd/v0.9.0-milestone` still returns `aed773c9807ab871468b1b2a7e1ec36b54e82907`, and `git branch --list 'gsd/v0.9.0-milestone'` still lists it.
    - `git reflog show gsd/v0.9.0-per-document-templates | head -5` shows no rebase or reset entry created by this plan.
  </acceptance_criteria>
  <done>The measured milestone-branch tip is on `origin` and a `workflow_dispatch` CI run over exactly that SHA has completed, with all twelve per-job conclusions transcribed verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the evidence is not stale, and record the rule that keeps it that way</name>
  <files>.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/53-template-registry-foundation/53-VERIFICATION.md` -- the frontmatter `gaps:` block. Its four `missing:` items are this plan's literal contract, and its `reason:` field is the description of the failure mode this task exists to make impossible to repeat.
    - `.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md` -- the sections Tasks 1 and 2 wrote, plus the `## Summary` and `## Audit` sections at the end of the existing artifact, whose per-success-criterion verdict format this task extends.
    - `.planning/ROADMAP.md` § "Phase 53" -- the five success criteria verbatim; the audit checks the evidence against each criterion's own wording, not against a paraphrase.
  </read_first>
  <action>
Re-measure, at this moment, three facts and record each with the command that produced it.

**Fact 1 -- remote agreement.** `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` equals
the head SHA the CI run executed against.

**Fact 2 -- the staleness assertion.** `git log <ci_run_head>..gsd/v0.9.0-per-document-templates -- typsphinx/ tests/`
produces no output. Record the exact command with the SHA substituted, and record its empty result.
This is the check whose absence let the previous round cite a run at `d1eff100` while four later
commits had already changed `typsphinx/` and `tests/`; run it, do not assume it.

**Fact 3 -- positive content proof.** Prove WHICH code was certified, not merely that a run was green:
`git show <ci_run_head>:typsphinx/template_registry.py` contains plan 53-08's container-guard message
text, and `git show <ci_run_head>:.planning/REQUIREMENTS.md` contains the corrected TPL-01 traceability
row. Record both greps and their counts.

Then write, in the artifact and in plain words, the currency rule that governs this evidence going
forward:

- Commits landing after the certified head that touch only `.planning/`, `docs/` or `CHANGELOG.md` are
  documentation-only and do NOT invalidate it. Name the ones this plan itself will create: its own
  evidence commit, the executor SUMMARY, and the phase-tracking commits. This is why the branch tip
  will legitimately sit ahead of the certified head by the time anyone reads the artifact.
- Any commit touching `typsphinx/` or `tests/` after the certified head DOES invalidate it. The
  required response is named explicitly: push the new tip, dispatch a fresh run, and append a new
  dated section -- not annotate the old one with a caveat.
- Name the merge hazard by name: this project runs executors in isolated worktrees as its standing
  mode, so a worktree merge moves the milestone tip after a plan finishes. Anyone re-checking this
  evidence must re-run Fact 2 against the tip as it stands at that moment rather than trusting the
  recorded verdict.

Finally, extend the artifact's audit with a per-success-criterion verdict for SC#1 through SC#5 as it
now stands, citing for each the artifact section or the measurement that supports it, and stating
plainly that the previous round's Run 2 no longer certifies the shipping code even though it remains
an accurate record of what it did certify. Do not delete or rewrite the Run 1 / Run 2 sections; the
honest history is part of the evidence.

Re-measure the standing invariants rather than trusting an earlier plan's SUMMARY:
`grep -rl "_template\.typ" tests/ | wc -l`, `uv run pytest tests/ -q`, and
`uv run pytest tests/test_preview_version_sync.py -q`. Record all three as measured output.

Create no file at `.planning/phases/53-template-registry-foundation/53-VERIFICATION.md`; that name is
reserved by the verifier and is overwritten wholesale on the next verify run (D-12).
  </action>
  <verify>
    <automated>uv run pytest tests/ -q &amp;&amp; uv run pytest tests/test_preview_version_sync.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The artifact records the command `git log <ci_run_head>..gsd/v0.9.0-per-document-templates -- typsphinx/ tests/` with the real SHA substituted, and records that it produced no output.
    - The artifact records `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` returning the CI run's head SHA.
    - `git show <ci_run_head>:typsphinx/template_registry.py | grep -c 'must be a dict mapping registry key to definition'` returns 1, and the count is recorded in the artifact.
    - `git show <ci_run_head>:.planning/REQUIREMENTS.md | grep -c '| TPL-01 | Phase 53 | Complete |'` returns 1, and the count is recorded in the artifact.
    - The artifact contains a currency-rule paragraph that names `.planning/`, `docs/` and `CHANGELOG.md` as non-invalidating and `typsphinx/` and `tests/` as invalidating, and that names re-push plus re-dispatch as the required response.
    - The artifact contains one verdict line per success criterion SC#1 through SC#5, each citing the section or measurement that supports it.
    - The artifact states that the previous round's Run 2 (head `d1eff100`) no longer certifies the shipping code, and the `## Run 1` and `## Run 2` headings are still present.
    - `grep -rl "_template\.typ" tests/ | wc -l` returns 32 and the value is recorded.
    - `uv run pytest tests/ -q` exits 0 and `uv run pytest tests/test_preview_version_sync.py -q` exits 0, both transcribed.
    - `test -e .planning/phases/53-template-registry-foundation/53-VERIFICATION.md` -- the file may exist because the verifier wrote it, but `git diff --name-only` must not list it as modified by this plan.
  </acceptance_criteria>
  <done>SC#5's evidence certifies a named SHA, proves by content which code that SHA carries, asserts no `typsphinx/` or `tests/` commit post-dates it, and publishes the rule for deciding when it goes stale again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository -> `origin` | A push advances a published branch. Already public at `48c957cd`, so this is an update rather than a first publication, but a pushed ref cannot be cleanly un-published. |
| GitHub Actions run output -> `53-CI-EVIDENCE.md` | Run IDs, job names, head SHAs and conclusions are transcribed into an artifact that the verifier, the milestone audit and the release record all treat as ground truth. |
| certified head SHA -> later commits on the same branch | The evidence's validity depends on a fact that decays: every subsequent commit is a chance for the artifact to become quietly wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-10-01 | Repudiation | `53-CI-EVIDENCE.md` currency | high | mitigate | Evidence that certifies a head the shipping code has moved past reads as a green phase while an untested Windows-specific guard ships -- this already happened once in this phase. Mitigated by Task 3's `git log <ci_run_head>..<branch> -- typsphinx/ tests/` emptiness assertion, by the positive `git show`-plus-grep content proof of what the certified SHA carries, by placing this plan in the last wave behind both code plans, and by publishing the invalidation rule in the artifact itself. |
| T-53-10-02 | Tampering | `53-CI-EVIDENCE.md` recorded values | medium | mitigate | A run ID, conclusion or SHA reconstructed from a planning document rather than measured would certify nothing. Mitigated by acceptance criteria demanding verbatim `gh run view --json` output, a `git ls-remote` line, and SHA equality between the pushed tip and the run's `headSha`. |
| T-53-10-03 | Tampering | the `origin` branch namespace | medium | mitigate | A force-push, rename or delete could make the evidence line up artificially. Mitigated by an explicit prohibition, by re-asserting that `gsd/v0.9.0-milestone` still resolves to its recorded SHA, and by a reflog check showing no rebase or reset entry. |
| T-53-10-04 | Spoofing | run identification | medium | mitigate | Selecting "the newest run on the branch" would pick up the `Link Check` workflow that every push fires, certifying a run that executes no tests. Mitigated by requiring a three-field match on name `CI`, event `workflow_dispatch` and `headSha`. |
| T-53-10-05 | Information disclosure | evidence artifact committed to the repository | low | mitigate | The artifact records hashes, run IDs and public URLs only. No credential, token or environment secret is transcribed; `gh auth status` is used as a precondition check and its output is not pasted in. |
| T-53-10-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. The dispatched CI run resolves dependencies from the repository's own pinned configuration, unchanged by this phase, so no `## Package Legitimacy Audit` is required. |

Canon referral: credential handling and CI supply-chain hardening are canon security ground owned by
`/gsd-secure-phase`; not minted as bespoke prohibitions here.
</threat_model>

<verification>
1. `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` returns the certified head SHA.
2. `gh run view <run-id> --json jobs` shows `success` for both `windows-latest` and both `macos-latest` `test` legs.
3. `git log <ci_run_head>..gsd/v0.9.0-per-document-templates -- typsphinx/ tests/` is empty.
4. `git show <ci_run_head>:typsphinx/template_registry.py` and `git show <ci_run_head>:.planning/REQUIREMENTS.md` both carry the wave-1 changes.
5. `uv run pytest tests/ -q` exits 0; `uv run pytest tests/test_preview_version_sync.py -q` exits 0; `grep -rl "_template\.typ" tests/ | wc -l` returns 32.
6. No PR opened; `gsd/v0.9.0-milestone` unchanged; no force-push.
</verification>

<success_criteria>
SC#5 is met on evidence that names the SHA it certifies, proves by content that that SHA carries plan
53-08's guards and plan 53-09's tracking correction, and asserts at recording time that no `typsphinx/`
or `tests/` commit post-dates it -- the assertion whose absence made the previous round's evidence
stale.
</success_criteria>

## Artifacts this phase produces

**Changed files**

- `.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md` -- gains a new dated
  gap-closure-round section carrying: the local CI-parity gate results, both locale runs, the pushed
  SHA and its `git ls-remote` confirmation, the run ID / URL / event / head SHA / duration, the verbatim
  twelve-job conclusion capture, the three re-measured currency facts, the published invalidation rule,
  and an extended per-success-criterion audit. The existing Run 1 and Run 2 sections are preserved.

**Deliberately produced nothing else**

- No code, no test, no fixture. No file is created at `53-VERIFICATION.md`, which the verifier reserves
  and overwrites wholesale (D-12).

**No tracer task in this plan.** The tracer for this plan set lives in `53-08-PLAN.md`. This plan wires
no layers -- it measures, pushes, dispatches and records -- so a nominal `type="tracer"` here would be a
label without a slice.

## Spec-less probe fallback

This plan owns no probe rows. All 15 surfaced edges derive from the phase's nine code requirements and
are dispositioned once, in `53-08-PLAN.md` § "Spec-less probe fallback -- edge disposition (§A/§C)"
(3 authored new + 8 already-covered-upstream + 4 flagged assumptions = 15, no silent drops). This plan
changes no code and re-opens none of them.

<output>
Create `.planning/phases/53-template-registry-foundation/53-10-SUMMARY.md` when done
</output>
