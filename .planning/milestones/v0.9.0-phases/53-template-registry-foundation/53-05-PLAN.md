---
phase: 53-template-registry-foundation
plan: 05
type: execute
wave: 4
depends_on: ["53-01", "53-02", "53-03", "53-04"]
files_modified:
  - .planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md
  - .planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md
autonomous: true
requirements: [TPL-03, TPL-04]
user_setup:
  - service: github
    why: "SC#5 requires the milestone branch on origin plus a completed 3-OS CI run dispatched through the GitHub API."
    env_vars: []
    dashboard_config:
      - task: "The `gh` CLI must already be authenticated with push access to this repository's `origin`. Verify with `gh auth status` before the push task; no new credential is created by this plan."
        location: "local `gh` CLI configuration"
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "For every one of the four existing configuration shapes, each emitted `.typ` file's post-change SHA-256 equals the pre-change SHA-256 plan 53-01 recorded for that same file (TPL-03, SC#2)."
    - "Where PDFs were compiled, each shape's post-change page count equals its pre-change page count; where the `-b typst` hash-only fallback was taken, the artifact records that the page-count half was unobtainable and names the reason."
    - "A four-element `typst_documents` tuple and the same tuple with a fifth element of the literal `\"typst\"` produce byte-identical `.typ` output after the change (TPL-04)."
    - "`53-RED-EVIDENCE.md` records both the pre-change and the post-change commit SHA, each produced by `git rev-parse` at its own measurement time."
    - "`gsd/v0.9.0-per-document-templates` is present in `git ls-remote --heads origin` output (SC#5)."
    - "At least one CI workflow run over `gsd/v0.9.0-per-document-templates` reaches status `completed`, and `gh run view <id> --json jobs` shows conclusion `success` for both the `windows-latest` and the `macos-latest` legs of the `test` job (SC#5)."
    - "The CI run is produced by `workflow_dispatch`; no pull request is opened, and no branch is renamed, merged, rebased, force-pushed, or deleted."
    - "No file is created or written at `.planning/phases/53-template-registry-foundation/53-VERIFICATION.md` — that name is reserved by the verifier (D-12)."
  artifacts:
    - .planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md
    - .planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md
  key_links:
    - "Lint, type and cross-platform coverage for this phase come ONLY from the dispatched CI run — `ruff` cannot execute locally (generic-linux ELF on NixOS). A local pytest pass is never lint evidence."
    - "CONF-18's reserved-device-name and case-collision predicates are platform-independent string-shape checks, so the Windows and macOS lanes confirm CI plumbing and packaging on those platforms rather than exercising a platform-specific code path."
  prohibitions:
    - "Evidence in 53-RED-EVIDENCE.md and 53-CI-EVIDENCE.md must be produced by running the commands and transcribing their real output; hashes, page counts, run IDs, job names, conclusions and commit SHAs must never be reconstructed, estimated, or carried over from a planning document."
---

<objective>
Close SC#2 and SC#5 on measured evidence.

SC#2: re-run plan 53-01's exact procedure at the post-change commit and diff every emitted `.typ`
file's SHA-256 against the recorded pre-change value, across all four existing configuration shapes,
plus the TPL-04 four-element-versus-fifth-element comparison. Per D-12 this is a one-off evidence
artifact, not a new golden-file pytest gate — the standing regression net that asserts the root
`_template.typ` is what pins behaviour, and it must still pass unmodified.

SC#5: push `gsd/v0.9.0-per-document-templates` to `origin` and land a completed CI run over it whose
`windows-latest` and `macos-latest` legs both conclude `success`. The run is produced by
`workflow_dispatch`, because `.github/workflows/ci.yml:3-8` scopes the `push` and `pull_request`
triggers to `main`/`develop` only, so a feature-branch push alone runs no CI.

Purpose: make the "this phase changes no output" claim falsifiable by identity, and get the milestone
branch onto `origin` in the first phase — the discipline whose absence cost v0.7.0 two defects.
Output: a complete `53-RED-EVIDENCE.md` and a new `53-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/53-template-registry-foundation/53-CONTEXT.md
@.planning/phases/53-template-registry-foundation/53-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan |
|---|---|---|
| `.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` | evidence artifact | 53-01 (pre), 53-05 (post) |
| `.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md` | evidence artifact | 53-05 |
| `typsphinx/template_registry.py`, `TemplateRegistryEntry`, `resolve_template_registry`, `resolve_registry_key`, `RESERVED_REGISTRY_KEY` | new module and surface | 53-02 |
| `_WINDOWS_RESERVED_NAMES`, `_is_windows_reserved_name`, `_has_case_collision`, `_violates_conf17`, `_validate_registry_key_shape` | private predicates | 53-03 |
| `typst_document_templates` | Sphinx config value | 53-02 |
| `TypstBuilder._document_template_registry` | builder attribute | 53-02 |
| `TypstWriter.render_wrapper(..., template_entry=...)` | new keyword parameter | 53-02 |
| `TemplateResolution.path` | new dataclass field | 53-04 |
| `tests/test_template_registry.py` | new test module | 53-02, 53-03 |

<tasks>

<task type="auto">
  <name>Task 1: Post-change byte-identity measurement across all four shapes, diffed against the baseline</name>
  <files>.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` — plan 53-01's pre-change section: the recorded commit SHA, the builder chosen by the live `typst.compile()` probe, the four fixture paths, the per-file SHA-256 values, and the TPL-04 baseline.
    - `.planning/phases/53-template-registry-foundation/53-RESEARCH.md` § Q6 — the six-step procedure, so the post-change run is procedurally identical to the pre-change one.
    - `.planning/phases/53-template-registry-foundation/53-CONTEXT.md` § D-12 — the reserved-filename constraint and why no golden pytest gate is added.
    - `./CLAUDE.md` — worktree provisioning (`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`) and the requirement that every command run through `uv run`. Without it the post-change build would measure the unchanged main-tree package and the whole comparison would be meaningless.
  </read_first>
  <precondition>`.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` exists and contains plan 53-01's pre-change SHA-256 lines and its recorded builder choice.</precondition>
  <action>
    Provision the worktree per CLAUDE.md, then record `git rev-parse HEAD` as the post-change commit SHA
    — measured, never copied.

    Re-run the live `typst.compile()` probe. If the pre-change run took the PDF path and this run cannot,
    or vice versa, record the divergence explicitly and fall back to comparing only what both runs can
    produce: the `.typ` file hashes. Do not silently switch paths.

    Rebuild all four shapes into fresh scratch directories using the same builder and the same fixture
    paths 53-01 used — `tests/fixtures/documented_params_contract_gate`,
    `tests/fixtures/typst_lang_gate/package_no_lang`,
    `tests/fixtures/params_exclusivity_gate/zero_params_default`, and `tests/roots/test-basic`. Compute
    the SHA-256 of every emitted `.typ` file, and where PDFs exist read each page count with
    `pypdf.PdfReader(...)` and `len(reader.pages)`.

    Diff, per shape: the sorted `.typ` file LIST must match (a file appearing or disappearing is a
    failure just as much as a changed hash), each file's SHA-256 must match its pre-change counterpart
    exactly, and each PDF's page count must match. Repeat plan 53-01 Task 2's TPL-04 comparison at the
    post-change commit: the four-element tuple tree and the explicit-fifth-element tree must be
    byte-identical to each other.

    Append a post-change section to `53-RED-EVIDENCE.md` recording the post-change commit SHA, the
    probe outcome, the four shapes' file lists with hashes, the page counts, the TPL-04 comparison, and
    an explicit per-shape verdict. If any hash differs, record the divergence verbatim — the file, both
    hashes, and a diff of the two `.typ` files — and stop rather than adjusting the procedure. A
    divergence means the phase changed output, which contradicts its goal, and it is a finding, not an
    obstacle to work around.

    Do not create or write `53-VERIFICATION.md`; that name is reserved by the verifier and would be
    clobbered (D-12).
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `53-RED-EVIDENCE.md` contains a post-change section with a 40-hex-character commit SHA distinct from the pre-change one, and states it was produced by `git rev-parse HEAD`.
    - The post-change section records, per shape, the sorted `.typ` file list, one SHA-256 per file, and an explicit verdict stating whether every hash matched its pre-change counterpart.
    - All four fixture paths appear in the post-change section, matching the four in the pre-change section.
    - A post-change `TPL-04 equivalence` section states whether the four-element and five-element trees are byte-identical.
    - `grep -c '^[0-9a-f]\{64\}' .planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` returns at least twice the pre-change count.
    - `test -e .planning/phases/53-template-registry-foundation/53-VERIFICATION.md` returns non-zero.
    - `uv run pytest tests/ -q` exits 0, and `grep -rl "_template\.typ" tests/ | wc -l` returns the same count as at phase start.
  </acceptance_criteria>
  <done>SC#2's identity claim is measured end to end at named commits, across four shapes plus the TPL-04 comparison, with an explicit per-shape verdict recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Push the milestone branch and land a completed 3-OS CI run</name>
  <files>.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md</files>
  <read_first>
    - `.github/workflows/ci.yml` — the trigger scoping at lines 3-8 (`push` and `pull_request` limited to `main`/`develop`, `workflow_dispatch` always available) and the matrix at lines 16-18 (`os: [ubuntu-latest, windows-latest, macos-latest]`, two Python versions, giving six `test` job legs).
    - `.planning/ROADMAP.md` § "Phase 53" SC#5 and § binding constraint #9 — the branch name and the required evidence shape.
    - `.planning/phases/53-template-registry-foundation/53-RESEARCH.md` § Q8 — the measured v0.8.0 precedent (`workflow_dispatch` run over a milestone branch with no PR open) and the recommended command sequence.
    - `.planning/STATE.md` § "Active Milestone (v0.9.0)" — the corrected branch name and the note that the originally-named `gsd/v0.9.0-milestone` is a stale local branch carrying zero milestone commits.
  </read_first>
  <precondition>`gh auth status` reports an authenticated account with push access to this repository's `origin` remote, and `git remote get-url origin` resolves.</precondition>
  <action>
    Push the milestone branch by name: `git push origin gsd/v0.9.0-per-document-templates`. Push that
    ref only. Do not push the executing worktree's own branch, do not force-push, do not open a pull
    request (the owner chose dispatch over a draft PR), and do not rename, merge, rebase or delete any
    branch — in particular leave the stale local `gsd/v0.9.0-milestone` exactly where it is. SC#5 was
    retargeted to the branch that actually carries the milestone's commits; nothing is being reconciled.
    If the push is rejected or reports an unexpected divergence, halt and report rather than forcing.

    Confirm the branch landed with `git ls-remote --heads origin` and transcribe the matching line
    verbatim, including the SHA.

    Dispatch CI: `gh workflow run CI --ref gsd/v0.9.0-per-document-templates`. Then poll — do not
    sleep-loop blindly — with `gh run list --branch gsd/v0.9.0-per-document-templates --limit 5` until
    the dispatched run's status is `completed`. The v0.8.0 precedent ran about six minutes, so bound the
    polling at roughly twenty minutes and report a timeout as a finding rather than proceeding on an
    incomplete run.

    Capture per-lane evidence with
    `gh run view <run-id> --json jobs -q '.jobs[] | {name, conclusion}'` and transcribe the full output
    verbatim into `53-CI-EVIDENCE.md`, alongside the run ID, the run URL, the triggering event, the head
    SHA the run executed against, and the total duration.

    Write `53-CI-EVIDENCE.md` with those sections plus an explicit statement of whether both the
    `windows-latest` and `macos-latest` legs of the `test` job concluded `success`. Note in the artifact
    that lint and type coverage for this phase come from this run alone, because `ruff` cannot execute
    on this machine (generic-linux ELF under NixOS) — a local pytest pass is never lint evidence.

    If any lane concludes other than `success`, record the failing lane, the job name, and the relevant
    log excerpt, and report it as a finding. Do not re-dispatch repeatedly hoping for a different
    result.
  </action>
  <verify>
    <automated>git ls-remote --heads origin | grep -c 'gsd/v0.9.0-per-document-templates'</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --heads origin | grep -c 'gsd/v0.9.0-per-document-templates'` returns 1.
    - `53-CI-EVIDENCE.md` exists and records a numeric run ID, the run URL, the triggering event `workflow_dispatch`, and the head SHA.
    - `53-CI-EVIDENCE.md` contains the verbatim `gh run view <id> --json jobs` output, and that output shows `conclusion` `success` for a job whose name contains `windows-latest` and for a job whose name contains `macos-latest`.
    - The artifact states explicitly how many of the six `test` job legs concluded `success`.
    - `git branch --list 'gsd/v0.9.0-milestone'` still returns that branch, and `git rev-parse gsd/v0.9.0-milestone` still returns `aed773c9807ab871468b1b2a7e1ec36b54e82907` — nothing was renamed, merged or deleted.
    - `gh pr list --head gsd/v0.9.0-per-document-templates` returns no open pull request opened by this plan.
  </acceptance_criteria>
  <reversibility rating="costly">Pushing the milestone branch to `origin` makes its history visible on the remote; it can be updated but a pushed ref is not cleanly un-published. The decision itself is already the owner's, locked as ROADMAP binding constraint #9 and Phase 53 SC#5, so this is recorded rather than gated — no checkpoint is inserted, because inserting one would re-litigate a locked decision.</reversibility>
  <done>The milestone branch is on `origin` and a `workflow_dispatch` CI run over it has completed, with the Windows and macOS lane conclusions transcribed verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Audit both evidence artifacts against the literal success criteria</name>
  <files>.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` — the completed artifact, both sections.
    - `.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md` — the artifact Task 2 wrote.
    - `.planning/ROADMAP.md` § "Phase 53" — the five success criteria verbatim; this audit checks each artifact against the criterion's own wording, not against a paraphrase.
    - `.planning/phases/53-template-registry-foundation/53-VALIDATION.md` § "Validation Sign-Off" — the checklist this audit discharges.
  </read_first>
  <action>
    Read both evidence artifacts and check each against the literal text of the success criterion it
    serves.

    For SC#2, confirm the artifact covers all four named shapes — `typst_template` set, `typst_package`
    set, `typst_template_function` set, and nothing set — and the four-element-versus-fifth-element
    comparison, and that every claim is backed by a transcribed hash or page count rather than a
    statement.

    For SC#5, confirm the artifact carries a `git ls-remote --heads origin` hit and per-lane
    conclusions for both `windows-latest` and `macos-latest`.

    Re-measure the standing invariants rather than trusting earlier plans' SUMMARYs: run
    `grep -rl "_template\.typ" tests/ | wc -l` and compare against the count at phase start; run
    `uv run pytest tests/ -q`; run `uv run pytest tests/test_preview_version_sync.py -q`. Record all
    three as measured output.

    Append an "Audit" section to `53-CI-EVIDENCE.md` recording, per success criterion, whether the
    evidence on file satisfies its literal wording, and listing any criterion that does not. Report a
    shortfall as a finding — do not close it by rewriting the criterion or by adding an assertion to the
    artifact that no command produced.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q &amp;&amp; uv run pytest tests/test_preview_version_sync.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `53-CI-EVIDENCE.md` contains an `Audit` section with one verdict line per success criterion SC#1 through SC#5.
    - The audit section records the re-measured `grep -rl "_template\.typ" tests/ | wc -l` value and states whether it equals the phase-start count.
    - The audit section records that `uv run pytest tests/ -q` exited 0 and that `uv run pytest tests/test_preview_version_sync.py -q` exited 0, with the measured summary lines transcribed.
    - Every SC verdict cites the artifact and section that supports it, or names it as a shortfall.
    - No file exists at `.planning/phases/53-template-registry-foundation/53-VERIFICATION.md`.
  </acceptance_criteria>
  <done>Both evidence artifacts are audited against the criteria's literal wording, with any shortfall named rather than papered over.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository → `origin` | A push publishes the milestone branch's history. Author-controlled, but irreversible in the sense that a pushed ref cannot be cleanly un-published. |
| GitHub Actions run output → evidence artifact | Run IDs, job names and conclusions are transcribed into an artifact later phases and the verifier trust as ground truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-04 | Tampering | `53-RED-EVIDENCE.md` / `53-CI-EVIDENCE.md` | medium | mitigate | Every value is transcribed from a command executed in this plan; the acceptance criteria require countable SHA-256 lines, a numeric run ID, and verbatim `gh run view` JSON, so an artifact written from prose fails the gate. |
| T-53-10 | Tampering | the `origin` branch namespace | medium | mitigate | The push targets exactly one named ref with no force flag; the acceptance criteria re-assert that the stale `gsd/v0.9.0-milestone` still exists at its measured SHA, so a rename, merge or deletion turns a check red. |
| T-53-11 | Information Disclosure | evidence artifacts committed to the repository | low | mitigate | The artifacts record hashes, page counts, run IDs and public URLs only. No credential, token or environment secret is transcribed; `gh auth status` output is used as a precondition check and is not pasted into an artifact. |
| T-53-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. The dispatched CI run resolves dependencies from the repository's own pinned configuration, unchanged by this phase. |
</threat_model>

<verification>
- `uv run pytest tests/ -q` exits 0.
- `uv run pytest tests/test_preview_version_sync.py -q` exits 0.
- `grep -rl "_template\.typ" tests/ | wc -l` unchanged from phase start.
- `git ls-remote --heads origin` shows `gsd/v0.9.0-per-document-templates`.
- `gh run view <id> --json jobs` shows `success` for the `windows-latest` and `macos-latest` legs.
- `53-RED-EVIDENCE.md` carries both a pre-change and a post-change section with matching hashes.
- No file exists at `53-VERIFICATION.md`.
</verification>

<success_criteria>
SC#2 and SC#5 are met on measured evidence: byte-identical output proven by identity across four
shapes plus the TPL-04 comparison, and the milestone branch on `origin` with a completed 3-OS CI run
including the Windows and macOS lanes.
</success_criteria>

<output>
Create `.planning/phases/53-template-registry-foundation/53-05-SUMMARY.md` when done.
</output>
