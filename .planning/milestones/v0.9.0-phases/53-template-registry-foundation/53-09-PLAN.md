---
phase: 53-template-registry-foundation
plan: 09
type: execute
wave: 7
depends_on: ["53-03", "53-07"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TPL-01, TPL-05, CONF-16]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` records TPL-01, TPL-05 and CONF-16 as delivered in BOTH of the two places it tracks status: the v1 Requirements checkbox list (`- [x] **TPL-01**`, `- [x] **TPL-05**`, `- [x] **CONF-16**`) and the Traceability table (`| TPL-01 | Phase 53 | Complete |`, `| TPL-05 | Phase 53 | Complete |`, `| CONF-16 | Phase 53 | Complete |`). Correcting only one of the two surfaces leaves the file self-contradictory and is not a close."
    - "Exactly six lines change. `git diff --numstat -- .planning/REQUIREMENTS.md` reports `6\\t6`, and no requirement other than TPL-01, TPL-05 and CONF-16 has its state altered -- in particular TPL-02, CONF-19, OUT-04..07, BLD-05, BLD-06, XREF-05, BLD-07..09, IMG-03, DOC-15..17 and REL-08 all keep the status they hold at HEAD."
    - "The counts move consistently in both surfaces: `grep -c '^- \\[x\\] \\*\\*' .planning/REQUIREMENTS.md` goes 6 -> 9, `grep -c '^- \\[ \\] \\*\\*'` goes 20 -> 17, `grep -c '| Complete |'` goes 6 -> 9, `grep -c '| Pending |'` goes 20 -> 17. A mismatch between the checkbox count and the table count means one surface was missed."
    - "TPL-01's bullet wraps onto a second physical line. Only the line carrying the `- [ ]` marker changes; its continuation line stays byte-identical, so the wrapped bullet is not orphaned (the recorded `update-plan-progress` wrapped-line hazard, one surface over)."
    - "No prose in the file is rewritten: the `**Per-phase totals:**` line, the `**Coverage:**` block (26 total / 26 mapped / 0 unmapped / 0 duplicated), the Future Requirements section, the Out of Scope table and the closing provenance line are all untouched. Requirement counts do not change -- only three requirements' delivery status does."
  artifacts:
    - .planning/REQUIREMENTS.md
  key_links:
    - "The authority for this correction is measured, not asserted: `53-03-SUMMARY.md`'s `requirements-completed` list names TPL-01, TPL-05 and CONF-16, and `53-VERIFICATION.md`'s Requirements Coverage table independently re-confirmed all three ✓ SATISFIED by live measurement (shared-key identity `r1 is r2 -> True` for TPL-01/TPL-05; a live `ExtensionError` on a user-declared `\"typst\"` key for CONF-16). The executor re-reads both before editing rather than trusting this plan's summary of them."
    - "This is a documentation-tracking correction with ZERO code change. `.planning/REQUIREMENTS.md` is the only file this plan may touch; a diff naming any path under `typsphinx/` or `tests/` means the plan went out of scope, and it would additionally invalidate plan 53-10's CI-head staleness assertion, which permits only `.planning/`-shaped commits after the certified head."
  prohibitions:
    - "MUST NOT flip any requirement's status that this phase did not measure complete. This project has a recorded, four-times-repeated hazard of tracking automation flipping a deliberately-deferred requirement against an owner decision; the `6\\t6` numstat criterion exists specifically to make that visible in review rather than discovered at milestone close."
    - "MUST NOT restructure, reflow, reorder or re-wrap `.planning/REQUIREMENTS.md`. Six marker edits, nothing else -- a reflowed file hides the three real changes inside a large diff and defeats the numstat check."
---

<objective>
Close the ⚠ WARNING row `53-VERIFICATION.md` records against `.planning/REQUIREMENTS.md`: TPL-01,
TPL-05 and CONF-16 are still marked not-done even though `53-03-SUMMARY.md` lists all three under
`requirements-completed` and this session's verification independently re-confirmed all three
functionally satisfied by live measurement.

The staleness is in **two** places, and both must be corrected or the file stays self-contradictory:
the v1 Requirements checkbox list near the top, and the Traceability table near the bottom. The
verifier's Anti-Patterns row cites only the table rows; the checkbox list is equally stale and is
included here deliberately.

Purpose: make `.planning/REQUIREMENTS.md` an accurate record of delivered scope before milestone
close, so the v0.9.0 audit reads Phase 53's real 9/9 rather than a 6/9 that no longer reflects the
code. No code changes.
Output: six single-line edits in one file.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/53-template-registry-foundation/53-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark TPL-01, TPL-05 and CONF-16 delivered in both tracking surfaces</name>
  <precondition>The worktree has been provisioned with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` per CLAUDE.md § "Worktree-isolated execution". No `uv run` command is strictly required by this task, but the worktree must be provisioned before any verification command is attempted.</precondition>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full -- both tracking surfaces. The checkbox list at lines 15, 29 and 37 (TPL-01's bullet wraps onto line 16; TPL-05's and CONF-16's are single-line) and the Traceability table rows at lines 155, 159 and 162. Line numbers are the values measured at HEAD `344b9510`; locate each line by its requirement ID, never by line number alone, in case an earlier wave-1 plan shifted the file.
    - `.planning/phases/53-template-registry-foundation/53-03-SUMMARY.md` -- its `requirements-completed` front-matter list, which is the in-phase authority naming all three IDs. Confirm all three are present before editing; if any is absent, halt and report rather than editing on this plan's say-so.
    - `.planning/phases/53-template-registry-foundation/53-VERIFICATION.md` § "Requirements Coverage" -- the three rows reading `✓ SATISFIED (code); ⚠️ REQUIREMENTS.md tracking stale`, and § "Anti-Patterns Found"'s final row, which names lines 155/159/162. This is the independent live re-measurement that makes the correction a record of fact rather than a claim.
    - `.planning/phases/53-template-registry-foundation/53-CONTEXT.md` § "Deferred Ideas" -- confirmation that nothing in this phase's deferred set maps to a requirement ID, so no deferred item can be swept up by this edit.
  </read_first>
  <action>
Make exactly six single-line edits in `.planning/REQUIREMENTS.md`. Use a scoped replacement per line;
do not rewrite the file.

**Checkbox list, `## v1 Requirements`.** Change the leading marker from unchecked to checked on the
bullet whose text begins `**TPL-01**: User can define named template definitions`, on the bullet
`**TPL-05**: Several \`typst_documents\` entries can share one registry key`, and on the bullet
`**CONF-16**: Defining \`"typst"\` in the registry stops the build`. TPL-01's bullet wraps onto a
second physical line beginning `typst_document_templates`; that continuation line must stay
byte-identical -- only the marker line changes. Change no bullet text.

**Traceability table, `## Traceability`.** In the `| Requirement | Phase | Status |` table, change the
third cell from the pending value to `Complete` in the `TPL-01`, `TPL-05` and `CONF-16` rows, all three
of which name `Phase 53`. Leave the `Requirement` and `Phase` cells untouched and leave every other row
exactly as it stands -- including `| TPL-02 | Phase 54 | ... |`, `| CONF-19 | Phase 54 | ... |` and
`| REL-08 | Phase 57 | ... |`, none of which this phase delivers.

**Change nothing else.** The `**Per-phase totals:**` line still reads Phase 53 -> 9, because the
requirement-to-phase mapping is unchanged; the `**Coverage:**` block still reads 26 total, 26 mapped,
0 unmapped, 0 duplicated, because no requirement was added, removed or re-mapped; the Future
Requirements section, the Out of Scope table and the closing provenance line are all untouched. This
edit records delivery status only.

Before committing, run `git diff --numstat -- .planning/REQUIREMENTS.md` and confirm it reports exactly
six insertions and six deletions, then read `git diff -- .planning/REQUIREMENTS.md` and confirm every
one of the six changed lines names TPL-01, TPL-05 or CONF-16. If the numstat is anything other than
`6	6`, revert and redo the edits one line at a time -- a larger diff means an editor reflowed
something.
  </action>
  <verify>
    <automated>git diff --numstat -- .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*TPL-01\*\*' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^- \[x\] \*\*TPL-05\*\*' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^- \[x\] \*\*CONF-16\*\*' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^| TPL-01 | Phase 53 | Complete |$' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^| TPL-05 | Phase 53 | Complete |$' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^| CONF-16 | Phase 53 | Complete |$' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^- \[x\] \*\*' .planning/REQUIREMENTS.md` returns 9 and `grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md` returns 17.
    - `grep -c '| Complete |' .planning/REQUIREMENTS.md` returns 9 and `grep -c '| Pending |' .planning/REQUIREMENTS.md` returns 17 (both were 6 and 20 respectively at HEAD `344b9510`, measured during planning).
    - `git diff --numstat -- .planning/REQUIREMENTS.md` reports exactly `6	6`.
    - `git status --porcelain` lists `.planning/REQUIREMENTS.md` and no other path -- in particular no path under `typsphinx/` or `tests/`.
    - `grep -c '| REL-08 | Phase 57 | Pending |' .planning/REQUIREMENTS.md` returns 1 -- the requirement this project's tracking automation has flipped against an owner decision at four consecutive release-prep closes is untouched here.
    - `git diff -- .planning/REQUIREMENTS.md | grep '^[+][^+]' | grep -c 'TPL-01\|TPL-05\|CONF-16'` returns 6 -- every added line names one of the three corrected requirements, so the `6	6` numstat cannot be satisfied by an unrelated pair of edits.
  </acceptance_criteria>
  <done>Both of `.planning/REQUIREMENTS.md`'s tracking surfaces agree that TPL-01, TPL-05 and CONF-16 are delivered, exactly six lines changed, and no other requirement's state moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase SUMMARY / VERIFICATION claims -> `.planning/REQUIREMENTS.md` | A requirement's recorded status is read downstream by the milestone audit and by `/gsd-complete-milestone` as ground truth for what shipped. A status written from a claim rather than from a measurement propagates silently to the release record. |
| editing tool -> `.planning/REQUIREMENTS.md` | A whole-file rewrite or reflow can alter lines outside the intended three requirements without any reviewer noticing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-09-01 | Tampering | `.planning/REQUIREMENTS.md` status cells | medium | mitigate | An edit could flip a requirement the phase did not deliver -- a hazard this project has recorded firing four times against REL-shaped requirements. Mitigated by the `6	6` numstat criterion, per-ID positive greps in both surfaces, and an explicit criterion re-asserting that `REL-08` is untouched. |
| T-53-09-02 | Repudiation | requirement delivery record | medium | mitigate | A status written from this plan's prose rather than from the phase's own evidence would be unfalsifiable later. Mitigated by requiring the executor to re-read `53-03-SUMMARY.md`'s `requirements-completed` list and `53-VERIFICATION.md`'s Requirements Coverage rows, and to halt if either fails to name all three IDs. |
| T-53-09-03 | Tampering | surrounding prose (totals, coverage block) | low | mitigate | A reflow could silently change the coverage arithmetic. Mitigated by the numstat bound and by a positive grep on the `**Per-phase totals:**` line. |
| T-53-09-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan; it touches one Markdown file. No `## Package Legitimacy Audit` is required. |

Canon referral: no security-relevant code path is touched -- this plan edits a planning document and
runs no build. Supply-chain and injection concerns are canon ground owned by `/gsd-secure-phase`; not
minted as bespoke prohibitions here.
</threat_model>

<verification>
1. Both tracking surfaces agree for TPL-01, TPL-05 and CONF-16 (six positive greps above).
2. `git diff --numstat -- .planning/REQUIREMENTS.md` reports `6	6`.
3. `git status --porcelain` names no path under `typsphinx/` or `tests/`.
4. The checkbox counts and the table counts move together: 6 -> 9 checked, 20 -> 17 unchecked, 6 -> 9 Complete, 20 -> 17 Pending.
</verification>

<success_criteria>
`53-VERIFICATION.md`'s `.planning/REQUIREMENTS.md` ⚠ WARNING row is closed on both tracking surfaces,
with a six-line diff that makes any accidental scope creep visible at a glance.
</success_criteria>

## Artifacts this phase produces

**Changed files**

- `.planning/REQUIREMENTS.md` -- six single-line status edits. No new symbols, no new files, no code.

**Deliberately produced nothing else**

- No new test, no new fixture, no new `.planning/` artifact. This plan's entire deliverable is the
  corrected record; a SUMMARY is produced by the executor as usual.

**No tracer task in this plan.** `TRACER_MODE` is on for this plan set and the tracer lives in
`53-08-PLAN.md`, which is the plan with layers to wire end to end. This plan changes one Markdown
file and has no path to trace; adding a nominal `type="tracer"` here would be the label-only
"layer cake disguised as slices" the tracer guidance rejects.

## Spec-less probe fallback

This plan owns no probe rows. All 15 surfaced edges derive from the phase's nine code requirements and
are dispositioned once, in `53-08-PLAN.md` § "Spec-less probe fallback -- edge disposition (§A/§C)"
(3 authored new + 8 already-covered-upstream + 4 flagged assumptions = 15, no silent drops). This plan
changes no code and therefore re-opens none of them; authoring a duplicate row here would create a
second producer for a predicate that already has one.

<output>
Create `.planning/phases/53-template-registry-foundation/53-09-SUMMARY.md` when done
</output>
