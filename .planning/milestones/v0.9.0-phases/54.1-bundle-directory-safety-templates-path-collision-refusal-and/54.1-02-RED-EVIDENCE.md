# Phase 54.1 Plan 02 — RED Evidence

Recorded against commit `9cc529b794563e57f2f070f93f0be9f141f8740e` (the pre-fix tree — no
documentation file has been touched yet; this evidence was captured before Task 2 or Task 3 of this
plan made any edit).

**Policed scope (D-12):** exactly `docs/source/`, `README.md`, and `examples/`. `tests/` and
`tests/fixtures/` are deliberately NOT policed (measured basis: zero files under `tests/` set both
a Sphinx `templates_path` value and a Typst `typst_template` value).

**Command:**

```
uv run pytest tests/test_docs_template_layout_gate.py -v
```

**Result:** `2 failed, 1 passed in 0.03s` — Test 1 (`test_no_typst_template_assignment_inside_jinja_dir`)
and Test 2 (`test_every_surviving_jinja_dir_mention_names_templates_path`) both RED; the fail-first
control (`test_patterns_have_teeth`) GREEN, as required by the plan's acceptance criteria.

## Test 1 — narrow, assignment-shaped (verbatim failure tail)

```
E       AssertionError: found a Typst-template configuration assignment pointing inside Sphinx's own templates_path directory (`_templates/`) -- move the template to a Typst-owned directory (`_typst/`) instead:
E         README.md:164: typst_template = '_templates/custom.typ'
E         docs/source/examples/advanced.rst:117: typst_template = "_templates/custom_ieee.typ"
E         docs/source/examples/advanced.rst:216: typst_template = "_templates/styled.typ"
E         docs/source/examples/advanced.rst:254: typst_template = "_templates/default.typ"
E         docs/source/quickstart.rst:100: typst_template = "_templates/custom.typ"
E         docs/source/user_guide/configuration.rst:117: typst_template = "_templates/custom.typ"
E         docs/source/user_guide/templates.rst:91: typst_template = "_templates/custom.typ"
E         docs/source/user_guide/templates.rst:168: typst_template = "_templates/custom.typ"
E         docs/source/user_guide/templates.rst:321: typst_template = "_templates/minimal.typ"
E         docs/source/user_guide/templates.rst:351: typst_template = "_templates/academic.typ"
E         examples/advanced/README.md:85: typst_template = '_templates/custom.typ'
E         examples/advanced/README.md:247: typst_template = '_templates/custom.typ'
E         examples/advanced/_templates/custom.typ:5: //   typst_template = '_templates/custom.typ'
E         examples/advanced/conf.py:70: typst_template = "_templates/custom.typ"
E         examples/charged-ieee/approach2/conf.py:25: typst_template = "_templates/_template.typ"
```

15 offending `path:line` pairs.

## Test 2 — broad, line-scoped (verbatim failure tail, full offending-path list)

```
E       AssertionError: found a mention of Sphinx's Jinja directory (`_templates`) that does not identify itself as Sphinx's own templates_path config value on the same line -- either annotate the line with `templates_path`, or if it is recommending a Typst template location, move it to `_typst/`:
E         README.md:164: typst_template = '_templates/custom.typ'
E         docs/source/examples/advanced.rst:62: **_templates/custom_ieee.typ**:
E         docs/source/examples/advanced.rst:107: bibliography: bibliography("_templates/refs.bib"),
E         docs/source/examples/advanced.rst:117: typst_template = "_templates/custom_ieee.typ"
E         docs/source/examples/advanced.rst:124: to ``custom_ieee.typ`` in your ``_templates/`` directory; typsphinx's
E         docs/source/examples/advanced.rst:127: ``_templates/`` path. The template file itself is written to the output
E         docs/source/examples/advanced.rst:130: ``"_templates/refs.bib"``, matching where the copy lands, not the bare
E         docs/source/examples/advanced.rst:172: **_templates/styled.typ**:
E         docs/source/examples/advanced.rst:216: typst_template = "_templates/styled.typ"
E         docs/source/examples/advanced.rst:254: typst_template = "_templates/default.typ"
E         docs/source/quickstart.rst:100: typst_template = "_templates/custom.typ"
E         docs/source/user_guide/configuration.rst:117: typst_template = "_templates/custom.typ"
E         docs/source/user_guide/templates.rst:91: typst_template = "_templates/custom.typ"
E         docs/source/user_guide/templates.rst:92: # Every file in _templates/ is automatically copied, including custom.typ itself
E         docs/source/user_guide/templates.rst:98: _templates/
E         docs/source/user_guide/templates.rst:110: // _templates/custom.typ
E         docs/source/user_guide/templates.rst:123: Create a file ``_templates/custom.typ``:
E         docs/source/user_guide/templates.rst:168: typst_template = "_templates/custom.typ"
E         docs/source/user_guide/templates.rst:321: typst_template = "_templates/minimal.typ"
E         docs/source/user_guide/templates.rst:351: typst_template = "_templates/academic.typ"
E         examples/advanced/README.md:20: ├── _templates/             # Custom template directory
E         examples/advanced/README.md:64: - `_build/typst/_template.typ` - Template imported by the wrapper (here, your `_templates/custom.typ`)
E         examples/advanced/README.md:85: typst_template = '_templates/custom.typ'
E         examples/advanced/README.md:233: # parameter -- _templates/custom.typ declares all three.
E         examples/advanced/README.md:247: typst_template = '_templates/custom.typ'
E         examples/advanced/README.md:299: 1. Try modifying the custom template in `_templates/custom.typ`
E         examples/advanced/_templates/custom.typ:5: //   typst_template = '_templates/custom.typ'
E         examples/advanced/conf.py:58: # its `project()` function. `_templates/custom.typ` below declares all three;
E         examples/advanced/conf.py:70: typst_template = "_templates/custom.typ"
E         examples/advanced/index.rst:85: ├── _templates/             # Custom templates (optional)
E         examples/charged-ieee/approach2/conf.py:21: # NOTE: typst_package is intentionally NOT set here. _templates/_template.typ
E         examples/charged-ieee/approach2/conf.py:25: typst_template = "_templates/_template.typ"
```

31 offending `path:line` pairs — this is the full discovery-time repo-wide grep this phase's SC#2
rests on, superseding the D-08 CONTEXT.md floor (which found the same set except one comment
`examples/advanced/_templates/custom.typ:5`, recorded separately as D-13).

## Test 3 — control / fail-first

`test_patterns_have_teeth` PASSED — both `TYPST_TEMPLATE_IN_JINJA_DIR_RE` and
`UNEXPLAINED_JINJA_DIR_RE` were exercised against inline synthetic known-violating and
known-clean strings only (no repository file read), proving the patterns have teeth before any
documentation edit is made.

## Why it is RED on this tree

Every offending line above still names the discouraged Sphinx-Jinja-directory string
(`_templates/`) as the location of a Typst template — the exact layout this phase's WR-01 fix
(Tasks 2 and 3) retires in favor of `_typst/`. No documentation file has been edited when this
evidence was captured: `git status --porcelain docs/ README.md examples/` (run immediately after
this test run, before any edit) produced no output.
