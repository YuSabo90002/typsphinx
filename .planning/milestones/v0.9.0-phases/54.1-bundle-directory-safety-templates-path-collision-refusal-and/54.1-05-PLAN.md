---
phase: 54.1-bundle-directory-safety-templates-path-collision-refusal-and
plan: 05
type: execute
wave: 3
depends_on: ["54.1-01", "54.1-02", "54.1-03", "54.1-04"]
files_modified:
  - tests/test_prewrite_failure_aggregation_gate.py
  - tests/fixtures/mixed_prewrite_failures_a_gate/conf.py
  - tests/fixtures/mixed_prewrite_failures_a_gate/index.rst
  - tests/fixtures/mixed_prewrite_failures_a_gate/other.rst
  - tests/fixtures/mixed_prewrite_failures_a_gate/aardvark.typ
  - tests/fixtures/mixed_prewrite_failures_a_gate/_typst/other.typ
  - tests/fixtures/mixed_prewrite_failures_b_gate/conf.py
  - tests/fixtures/mixed_prewrite_failures_b_gate/index.rst
  - tests/fixtures/mixed_prewrite_failures_b_gate/other.rst
  - tests/fixtures/mixed_prewrite_failures_b_gate/aardvark.typ
  - tests/fixtures/mixed_prewrite_failures_b_gate/_typst/other.typ
  - CHANGELOG.md
  - .planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-05-EVIDENCE.md
autonomous: true
requirements: [WR-01, CR-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CR-01 ordering edge — when one build carries failures of MORE THAN ONE kind, they are accumulated into a single `ExtensionError` in a documented, deterministic order, and two projects declaring the same violations in INVERTED declaration order produce the same message once each project's own source directory name is normalized out; D-03's byte-identical guarantee therefore holds across declaration order, not merely across repeated runs of one project"
    - "The v0.9.0 `Unreleased` CHANGELOG carries the breaking-change entry for the new pre-write refusals, naming the configuration that now fails, the reason, and the exact migration — a user whose working v0.8.x configuration stops building must learn WHERE to move the template, not only that it broke"
    - "The phase closes green: the full pytest suite passes modulo exactly the 7 pre-existing `tests/test_state_guard_shapes_gate.py` failures, `black`/`ruff`/`mypy` all pass, and this repository's own dogfooding project — which sets `templates_path` AND points its Typst template at a separate directory, the NON-colliding shape — still builds in both builders"
    - "Milestone invariant #9 holds: the milestone branch `gsd/v0.9.0-per-document-templates` is present on `origin`"
  artifacts:
    - path: "tests/test_prewrite_failure_aggregation_gate.py"
      provides: "the cross-kind aggregation and declaration-order-independence gate for the pre-write pass"
    - path: "tests/fixtures/mixed_prewrite_failures_a_gate/conf.py"
      provides: "one project carrying a CONF-17/A-01 violation AND a reserved-key case collision, in one declaration order"
    - path: "tests/fixtures/mixed_prewrite_failures_b_gate/conf.py"
      provides: "the same two violations in the inverted declaration order, for the message-identity assertion"
    - path: "CHANGELOG.md"
      provides: "the v0.9.0 `Unreleased` breaking-change entry for the new pre-write refusals"
      contains: "templates_path"
    - path: ".planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-05-EVIDENCE.md"
      provides: "the phase-boundary green evidence: full suite, lint/format/type, both dogfooding builds, milestone-branch presence, and the message-quality read"
  key_links:
    - from: "tests/test_prewrite_failure_aggregation_gate.py"
      to: "typsphinx/builder.py::TypstBuilder._validate_used_template_paths"
      via: "two real `sphinx-build` subprocess builds whose normalized aggregate messages are compared for identity"
      pattern: "_run_sphinx_build"
    - from: "CHANGELOG.md"
      to: "typsphinx/builder.py::TypstBuilder._validate_used_template_paths"
      via: "the `Unreleased` entry announces the migration this method's refusal forces"
      pattern: "templates_path"
  prohibitions:
    - requirement_id: WR-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT ship a build-breaking refusal without a user-facing migration announcement that names WHERE to move the template — a v0.8.x user whose working configuration stops building must be told the remedy, not only that the layout is wrong"
---

<objective>
Close the phase: prove that the pre-write pass aggregates failures of DIFFERENT kinds into one
deterministic, declaration-order-independent message; announce the breaking migration in the v0.9.0
`Unreleased` CHANGELOG; and record the phase-boundary green evidence.

Purpose: plans 01, 03 and 04 each proved their own failure kind in isolation. D-03's guarantee is
about the aggregate — one raise, every offender named, byte-identical across runs. A build carrying
two DIFFERENT kinds is the case none of the earlier plans exercises, and it is the one where an
ordering regression would hide. Separately, D-01's refusal is `costly` by the owner's own rating:
the migration it forces is announced once, and this is where.

Output: a cross-kind aggregation gate with an inverted-declaration-order fixture pair; the
`Unreleased` breaking-change entry; and `54.1-05-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-CONTEXT.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-VALIDATION.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-01-SUMMARY.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-03-SUMMARY.md
@CLAUDE.md
</context>

## Execution environment (MANDATORY — read before any command)

Worktree-isolated execution is this project's STANDING mode. Before running anything in your
worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run EVERY
subsequent command through `uv run …`. Without this, pytest imports the UNCHANGED main-tree package
and gates stay RED after a correct fix.

## Baseline: what "green" means in this phase

The full suite has **exactly 7 pre-existing failures** in `tests/test_state_guard_shapes_gate.py`
(they read an archived `.planning/` path), recorded in
`.planning/phases/53-template-registry-foundation/deferred-items.md`. They predate Phase 53 and are
**NOT** a regression this phase causes. Do NOT fix them and do NOT report them as new RED. Task 3's
evidence file must state their count explicitly so a reader can tell a clean close from a regression.

## Scope fence (do NOT touch)

- **WR-02 is Phase 57's, not this plan's.** Do NOT add a CHANGELOG entry for the
  `typst_template_assets` removal or for the asset-list-to-wholesale-copy behaviour change. This
  plan writes ONE new entry, covering only the pre-write refusals this phase introduces.
- Do NOT reorganize, re-curate, or re-word any existing CHANGELOG entry, including the
  `<srcdir>/base.typ` shadow-route entry Phase 54 wrote. Phase 57 curates; this plan appends.
- Do NOT bump the version number anywhere. Phase 57 is the release-prep phase.
- **WR-03 / WR-04 / IN-01** are out of this phase entirely.
- Typing-import modernization is FORBIDDEN. The `@preview` count stays four with no fourth
  version-lockstep site — neither new fixture template declares a `@preview` import.

<tasks>

<task type="auto">
  <name>Task 1: Prove cross-kind aggregation is one raise, in a declaration-order-independent order</name>

  <read_first>
    - `typsphinx/builder.py` — `_validate_used_template_paths()` as plans `54.1-01` and `54.1-03`
      left it: the single `failures` list, the order in which the three branches append to it, and
      the terminal aggregate raise with its prefix and `"; ".join(...)` summary. The order the
      branches append in IS the order this gate documents; read it rather than assuming it.
    - `tests/test_registry_prewrite_validation_gate.py` — the bad-first/bad-last fixture-pair
      methodology and `_extract_registry_key_message()`, the model for comparing two builds' messages
      rather than merely asserting substrings.
    - `tests/test_conf17_prewrite_gate.py` and `tests/test_reserved_key_case_prewrite_gate.py` —
      `CONF17_PREWRITE_MARKER` and `RESERVED_KEY_CASE_MARKER`, the two sentence fragments this gate
      locates. Copy the constant VALUES; do not import across test modules (this suite's convention
      is per-module copies).
    - `tests/fixtures/conf17_prewrite_gate/conf.py` and
      `tests/fixtures/reserved_key_case_prewrite_gate/conf.py` — the two violation shapes to combine.
    - `.planning/phases/54.1-.../54.1-01-RED-EVIDENCE.md` — for the pre-fix commit SHA the RED
      measurement uses, and the swap-and-restore procedure plan `54.1-04` recorded.
  </read_first>

  <files>
    tests/fixtures/mixed_prewrite_failures_a_gate/conf.py,
    tests/fixtures/mixed_prewrite_failures_a_gate/index.rst,
    tests/fixtures/mixed_prewrite_failures_a_gate/other.rst,
    tests/fixtures/mixed_prewrite_failures_a_gate/aardvark.typ,
    tests/fixtures/mixed_prewrite_failures_a_gate/_typst/other.typ,
    tests/fixtures/mixed_prewrite_failures_b_gate/conf.py,
    tests/fixtures/mixed_prewrite_failures_b_gate/index.rst,
    tests/fixtures/mixed_prewrite_failures_b_gate/other.rst,
    tests/fixtures/mixed_prewrite_failures_b_gate/aardvark.typ,
    tests/fixtures/mixed_prewrite_failures_b_gate/_typst/other.typ,
    tests/test_prewrite_failure_aggregation_gate.py,
    .planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-05-EVIDENCE.md
  </files>

  <behavior>
    - Building fixture A refuses with ONE `ExtensionError` whose aggregate prefix reports a failure
      count of 2, and whose body contains both the CONF-17/A-01 sentence fragment and the
      reserved-key case sentence fragment.
    - Building fixture B — the same two violations declared in inverted order — refuses with an
      aggregate message that is identical to A's once each project's own source-directory name is
      normalized out.
    - Both refused builds leave zero `.typ` files under their build directories.
  </behavior>

  <action>
    Create two fixture trees, `tests/fixtures/mixed_prewrite_failures_a_gate/` and
    `tests/fixtures/mixed_prewrite_failures_b_gate/`, carrying the SAME two violations in inverted
    declaration order:

    - A registry declaring two keys. One key is spelled so it folds onto the reserved built-in key
      but is not equal to it (the reserved-key case collision), with its template in the `_typst`
      subdirectory. The other key is named `aardvark` and points at `aardvark.typ` at the
      source-tree ROOT — a CONF-17/A-01 violation. Neither fixture sets `templates_path`, so the
      `templates_path` branch contributes nothing and the aggregate carries exactly the two kinds
      under test.
    - Two `typst_documents` entries, one per docname (`index`, `other`), one referencing each key,
      with distinct target filenames so `_validate_output_path_collisions()` is not tripped.
    - Fixture B is byte-identical to A except that BOTH the registry mapping's key insertion order
      and the `typst_documents` list order are reversed.
    - Head both `conf.py` files with the "load-bearing properties — do NOT touch any of these"
      block, naming: the two violations, that the two fixtures must stay identical except for
      declaration order, and that the key spellings are asserted verbatim by the gate module.
    - Both `.typ` templates in each fixture are real and compilable, exposing a `project` function
      with the parameter surface `writer.py` passes, with NO `@preview` imports.

    Create `tests/test_prewrite_failure_aggregation_gate.py`. Carry its own copies of
    `_run_sphinx_build()` and `_typ_files()`, and its own copies of the two marker-constant VALUES.
    Add a helper that, given a build's combined output and the fixture directory it was built from,
    extracts the aggregate sentence and replaces every occurrence of that fixture's own directory
    name with a fixed placeholder token — this is what makes the two messages comparable despite
    living at different absolute paths. Write the three tests from `<behavior>`, asserting message
    IDENTITY between the two normalized extractions (`assert a == b`), not merely that both contain
    the same substrings.

    Give the module a docstring stating: WR-01/CR-01 and D-03; that this is the only gate exercising
    MORE THAN ONE failure kind in one build; and the documented append order the pre-write pass uses
    (read it from the implementation and state it, so a later reordering is a visible, intentional
    change rather than a silent one).

    **RED transcript.** Measure this gate against this phase's pre-fix production file using the same
    swap-and-restore procedure plan `54.1-04` recorded: take the pre-fix commit SHA from
    `54.1-01-RED-EVIDENCE.md`, write that commit's version of the production builder over the working
    copy (`git show <SHA>:typsphinx/builder.py > typsphinx/builder.py`), run the gate, capture the
    output verbatim, restore (`git checkout -- typsphinx/builder.py`), and confirm with
    `git status --porcelain typsphinx/` that nothing remains modified. Open
    `54.1-05-EVIDENCE.md` with this transcript: the SHA, the procedure, the exact command, the
    pass/fail counts and the verbatim failure tail.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest tests/test_prewrite_failure_aggregation_gate.py -x && git status --porcelain typsphinx/ | wc -l | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_prewrite_failure_aggregation_gate.py` reports at least **3 passed, 0 failed**.
    - `uv run python -c "import subprocess,sys,tempfile,pathlib;\nouts={}\nfor f in ['mixed_prewrite_failures_a_gate','mixed_prewrite_failures_b_gate']:\n    d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','sphinx','-b','typst','tests/fixtures/'+f,d],capture_output=True,text=True); o=r.stdout+r.stderr\n    assert r.returncode!=0, (f,o)\n    assert 'has a parent directory that is srcdir itself, or an ancestor of srcdir' in o, (f,o)\n    assert \"differs from the built-in 'typst' registry key only by case\" in o, (f,o)\n    assert not list(pathlib.Path(d).rglob('*.typ')), f\n    outs[f]=o.replace(f,'FIXTURE')\nprint('OK')"` prints `OK`.
    - The gate asserts message IDENTITY between the two normalized extractions — `grep -cE 'assert .*==' tests/test_prewrite_failure_aggregation_gate.py` returns at least `1`, and the identity assertion's failure message shows both strings.
    - The aggregate prefix reports a failure count of `2` for both fixtures; assert this in the test by reading the count out of the prefix, not by counting substrings.
    - `uv run python -c "import pathlib,sys; a=(pathlib.Path('tests/fixtures/mixed_prewrite_failures_a_gate/conf.py')).read_text(); b=(pathlib.Path('tests/fixtures/mixed_prewrite_failures_b_gate/conf.py')).read_text(); sys.exit(0 if a!=b else 1)"` exits 0 — the two fixtures genuinely differ.
    - `grep -c '@preview' tests/fixtures/mixed_prewrite_failures_a_gate/aardvark.typ tests/fixtures/mixed_prewrite_failures_a_gate/_typst/other.typ tests/fixtures/mixed_prewrite_failures_b_gate/aardvark.typ tests/fixtures/mixed_prewrite_failures_b_gate/_typst/other.typ` reports `0` for all four.
    - `.planning/phases/54.1-.../54.1-05-EVIDENCE.md` opens with a RED section naming the pre-fix SHA carried over from `54.1-01-RED-EVIDENCE.md`, the swap-and-restore procedure, and a verbatim failure tail.
    - `git status --porcelain typsphinx/` produces no output at the end of the task.
  </acceptance_criteria>

  <done>
    A build carrying two DIFFERENT pre-write failure kinds raises once, naming both, with a failure
    count of 2 and zero written files; the message is identical across inverted declaration order
    after path normalization; and the assertion is recorded RED against the pre-fix production file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Announce the migration in the v0.9.0 Unreleased CHANGELOG</name>

  <read_first>
    - `CHANGELOG.md` lines 1-30 — the Keep-a-Changelog header, the existing `## [Unreleased]` block
      and its `### Changed` section, and specifically the `<srcdir>/base.typ` shadow-route entry
      Phase 54's plan `54-03` wrote. That entry is the precedent for tone, length, and structure:
      a bold one-line headline naming the change and its requirement id, then the reason in one
      clause, then the exact migration, then what happens if the user does nothing.
    - `CHANGELOG.md`'s `## [0.8.0]` section header and its first paragraph — the house style for
      describing a breaking change in a minor release.
    - `.planning/phases/54.1-.../54.1-CONTEXT.md` D-01's reversibility note — the precise scope of
      the migration ("every v0.8.x project that put its Typst template inside a directory on
      `templates_path`") and the fact that this announcement is the only place it is made.
    - `docs/source/conf.py` lines around 40-50 and 90-100 — the non-colliding pairing the entry
      should point users at, since it is what this repository's own configuration does.
  </read_first>

  <files>CHANGELOG.md</files>

  <action>
    Add ONE new entry to the existing `### Changed` section under `## [Unreleased]`, immediately
    after the existing shadow-route entry, following that entry's structure exactly.

    The entry must cover all three refusals this phase introduces, as one coherent breaking change
    ("template layout is now validated before anything is written"), and must contain, in this order:

    1. A bold headline naming the change and citing both requirement ids (WR-01, CR-01).
    2. The reason in one clause: the resolved template's parent directory is copied wholesale to the
       output as that registry key's bundle, so a template inside a directory Sphinx already treats
       as its own would republish that directory into public build output.
    3. The three configurations that now stop the build, each in one sentence: (a) a used registry
       key whose resolved template bundle directory is, contains, or is contained by any entry of
       Sphinx's `templates_path`; (b) a resolved template whose parent directory is the source
       directory itself or an ancestor of it — now caught before any file is written rather than at
       the end of the build; (c) a declared registry key differing from the built-in key only by
       case.
    4. The migration, stated as an instruction: move the Typst template into a directory that is not
       on `templates_path` and update the template configuration to match. Name `_typst/` as the
       directory this project uses, and note that Sphinx's own directory keeps its own meaning and
       should stay — the two coexist, which is what this repository's own documentation build does.
    5. What happens if the user does nothing: the build fails with a message naming the offending
       registry key, its resolved bundle directory, and the colliding entry. This is a hard failure
       by design — the rejected alternative, warning and skipping that key's bundle copy, leaves the
       wrapper importing a template file that was never written, so the emitted `.typ` tree cannot
       compile while the build reports success.

    Do not add any other entry, do not touch any existing entry, and do not change the version
    number. Phase 57 curates this section; this plan authors one entry into it.
  </action>

  <verify>
    <automated>uv run python -c "import pathlib; t=pathlib.Path('CHANGELOG.md').read_text(); u=t.split('## [Unreleased]')[1].split('## [0.8.0]')[0]; assert 'templates_path' in u; assert 'WR-01' in u and 'CR-01' in u; assert '_typst' in u; assert 'typst_template_assets' not in u; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - The `## [Unreleased]` section of `CHANGELOG.md` contains the strings `templates_path`, `WR-01`, `CR-01`, and `_typst` — verified by the command in `<verify>`, which prints `OK`.
    - `uv run python -c "import pathlib; u=pathlib.Path('CHANGELOG.md').read_text().split('## [Unreleased]')[1].split('## [0.8.0]')[0]; assert 'typst_template_assets' not in u; print('OK')"` prints `OK` — WR-02 was not folded in.
    - `git diff -- CHANGELOG.md | grep -cE '^-[^-]'` returns `0` — the entry was appended; no existing line was removed or reworded.
    - `git diff -- CHANGELOG.md | grep -cE '^\+.*## \[0\.9'` returns `0` — no version header was added; Phase 57 owns the version bump.
    - `grep -n '^## \[Unreleased\]' CHANGELOG.md` returns exactly one line, and `grep -c '^### Changed' CHANGELOG.md` is unchanged from its pre-task value — no duplicate section was created.
    - The new entry states the migration as an instruction naming the target directory, not only as a diagnosis. Read it back and confirm a reader learns where to move the template.
  </acceptance_criteria>

  <done>
    The v0.9.0 `Unreleased` CHANGELOG carries one new breaking-change entry covering all three new
    pre-write refusals, with the migration stated as an instruction; no existing entry was touched
    and no version number moved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the phase-boundary green evidence</name>

  <read_first>
    - `.planning/phases/54.1-.../54.1-VALIDATION.md` § Sampling Rate and § Manual-Only Verifications —
      the exact phase-gate command list this task discharges, and the one manual read it names.
    - `.planning/phases/53-template-registry-foundation/deferred-items.md` — the 7 pre-existing
      `tests/test_state_guard_shapes_gate.py` failures, so the evidence file can state their count
      and cite the record rather than presenting them as new.
    - `tox.ini` — the `docs-html` and `docs-pdf` environments and what each actually runs, so the
      evidence records the real commands.
    - `.planning/ROADMAP.md` lines 451-472 — milestone invariants #9 and #11, the two this task
      measures directly.
    - `.planning/phases/54.1-.../54.1-05-EVIDENCE.md` as Task 1 left it — you are appending sections
      to an existing file, not creating a new one.
  </read_first>

  <files>.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-05-EVIDENCE.md</files>

  <action>
    Run the phase-boundary gate and record every result verbatim into `54.1-05-EVIDENCE.md`, one
    section per item, each carrying the exact command and its real output (or an honest excerpt with
    the totals line intact). Do not summarise a command you did not run.

    1. **Full suite.** `uv run pytest -q`. Record the totals line. State the failure count and
       confirm it is exactly the 7 `tests/test_state_guard_shapes_gate.py` tests, listing their node
       ids, and cite `deferred-items.md`. Any other failure is a regression and blocks the phase.
    2. **Locale pre-check.** Re-run the phase's own new gate modules under `LC_ALL=C` —
       `LC_ALL=C uv run pytest tests/test_templates_path_collision_gate.py tests/test_conf17_prewrite_gate.py tests/test_reserved_key_case_prewrite_gate.py tests/test_docs_template_layout_gate.py tests/test_prewrite_failure_aggregation_gate.py -q`.
       This project has a recorded class of defects that appear only in CI because of locale, and
       this is the cheapest way to pre-empt one.
    3. **Lint / format / types.** `uv run black --check .`, `uv run ruff check .`,
       `uv run mypy typsphinx/`. Record each exit status. `ruff` in particular has been a
       CI-only failure class here — run it, do not assume it.
    4. **Version-sync invariant (#11).** `uv run pytest tests/test_preview_version_sync.py -q`, plus
       `git diff --stat <pre-fix SHA>..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ` to show the three-way lockstep surface is untouched across the whole phase.
    5. **Dogfooding builds.** `tox -e docs-html` and `tox -e docs-pdf`. These build this repository's
       own documentation, which sets `templates_path` AND points its Typst template at a separate
       directory — the NON-colliding shape. Both must succeed: they are the proof the new refusal
       does not fire on a correct configuration.
    6. **Milestone invariant #9.** `git ls-remote --heads origin gsd/v0.9.0-per-document-templates`.
       Record the output. If it is empty, that is a finding to surface, not something to fix inside
       this plan.
    7. **Requirement closure.** State, with the gate command that proves each, that WR-01 and CR-01
       are both closed: the collision gate, the documentation-layout gate, the CONF-17 pre-write
       gate, the reserved-key case gate, and the cross-kind aggregation gate.

    Then perform the one manual-only verification `54.1-VALIDATION.md` names: read the rendered
    refusal message out of a real build's captured stderr and confirm it reads as an INSTRUCTION —
    that a user hitting it learns where to move the template, not only that the current location is
    wrong. Paste the rendered message verbatim into the evidence file under its own heading and
    record the judgement.
  </action>

  <verify>
    <automated>uv run pytest -q 2>&1 | tail -5 && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/ && LC_ALL=C uv run pytest tests/test_templates_path_collision_gate.py tests/test_conf17_prewrite_gate.py tests/test_reserved_key_case_prewrite_gate.py tests/test_docs_template_layout_gate.py tests/test_prewrite_failure_aggregation_gate.py -q 2>&1 | tail -5 && tox -e docs-html && tox -e docs-pdf && git ls-remote --heads origin gsd/v0.9.0-per-document-templates</automated>
    <human-check>Read the refusal message pasted verbatim into `54.1-05-EVIDENCE.md` under the "Message quality" heading. Confirm it names the registry key, the resolved bundle directory, and the colliding `templates_path` entry, AND tells the reader where to move the template (`_typst/`). If it reads as a diagnosis without a remedy, that is a finding to report — the wording is Claude's Discretion, but "instruction, not only diagnosis" is a CONTEXT.md requirement.</human-check>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q` totals line is recorded verbatim in the evidence file, and the failure count is exactly `7`, all in `tests/test_state_guard_shapes_gate.py`, with their node ids listed.
    - `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` each exit 0, with the exit status recorded.
    - The `LC_ALL=C` re-run of the five new gate modules passes, and its totals line is recorded.
    - `uv run pytest tests/test_preview_version_sync.py -q` passes, and the recorded `git diff --stat` over `typsphinx/writer.py`, `typsphinx/template_engine.py` and `typsphinx/templates/base.typ` across the phase shows zero changed lines.
    - `tox -e docs-html` and `tox -e docs-pdf` both exit 0, with their final lines recorded — the non-colliding dogfooding configuration still builds in both builders.
    - `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` output is recorded; a non-empty result satisfies milestone invariant #9, an empty result is recorded as a finding.
    - The evidence file contains a "Message quality" heading with the refusal message pasted verbatim from a real build's captured stderr, followed by the judgement.
    - The evidence file names WR-01 and CR-01 and, for each, the gate command that proves it closed.
  </acceptance_criteria>

  <done>
    `54.1-05-EVIDENCE.md` carries the full-suite totals with the 7 known failures accounted for and
    cited, the locale re-run, the three CI-matching gates, the version-sync invariant, both
    dogfooding builds, the milestone-branch check, the per-requirement closure map, and the verbatim
    refusal message with its readability judgement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this release → every downstream v0.8.x project | A refusal introduced here breaks builds that work today. The CHANGELOG is the only channel through which that migration is communicated. |
| maintainer-authored `conf.py` → `TypstBuilder.write()` | Two failure kinds now cross this boundary in one build; the aggregate's determinism is what makes the failure reportable and reproducible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54.1-14 | Repudiation | an unannounced breaking change | high | mitigate | The `Unreleased` breaking-change entry names the three configurations that now fail, the reason, and the exact migration including the target directory. D-01 records that this announcement is made once and cannot be un-announced; Task 2 is where it is made. |
| T-54.1-15 | Tampering | a non-deterministic aggregate message | medium | mitigate | The cross-kind gate asserts message IDENTITY across inverted declaration order after path normalization, so an ordering regression is caught rather than surfacing as an intermittently-different error text. |
| T-54.1-16 | Denial of Service | the new refusal firing on a correct configuration | medium | mitigate | Both dogfooding builds (`tox -e docs-html`, `tox -e docs-pdf`) run this repository's own documentation, which sets `templates_path` AND points its Typst template at a separate directory; their success is the proof the guard does not over-match. |
| T-54.1-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed in this phase (binding constraint #11); `54.1-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. |

**ASVS level 1, block on `high`.** T-54.1-14 is the one `high` item and is dispositioned `mitigate`
by Task 2. Canon breadcrumb: generic information-disclosure and path-traversal hardening is canon —
covered by `/gsd-secure-phase` and the registers in plans `54.1-01` and `54.1-03`; not minted as a
bespoke prohibition.
</threat_model>

## Artifacts this phase produces

Created by THIS plan (excluded from drift verification — none exists on the pre-fix tree):

- `tests/test_prewrite_failure_aggregation_gate.py` — new test module, with its own copies of the
  subprocess helper, the two marker-constant values, and a fixture-path-normalizing message extractor
- `tests/fixtures/mixed_prewrite_failures_a_gate/`, `tests/fixtures/mixed_prewrite_failures_b_gate/` — new fixture directories
- `.planning/phases/54.1-.../54.1-05-EVIDENCE.md` — new evidence artifact
- one new `### Changed` entry inside `CHANGELOG.md`'s existing `## [Unreleased]` section

Created by sibling plans in this phase (also new, also excluded): `_paths_related()`,
`TypstBuilder._validate_used_template_paths()`, `_conf17_violation_message()`,
`tests/test_templates_path_collision_gate.py`, `tests/test_docs_template_layout_gate.py`,
`tests/test_conf17_prewrite_gate.py`, `tests/test_reserved_key_case_prewrite_gate.py`,
`tests/fixtures/templates_path_collision_gate/`,
`tests/fixtures/templates_path_collision_multi_gate/`,
`tests/fixtures/templates_path_adjacent_control_gate/`,
`tests/fixtures/templates_path_absent_control_gate/`,
`tests/fixtures/templates_path_no_typst_template_control_gate/`,
`tests/fixtures/templates_path_default_documents_gate/`, `tests/fixtures/conf17_prewrite_gate/`,
`tests/fixtures/reserved_key_case_prewrite_gate/`, `examples/advanced/_typst/`,
`examples/charged-ieee/approach2/source/_typst/`, `54.1-01-RED-EVIDENCE.md`,
`54.1-02-RED-EVIDENCE.md`, `54.1-03-RED-EVIDENCE.md`, `54.1-04-RED-EVIDENCE.md`.

<verification>
- `uv run pytest tests/test_prewrite_failure_aggregation_gate.py` — at least 3 passed.
- `uv run pytest -q` — green modulo exactly the 7 pre-existing `tests/test_state_guard_shapes_gate.py` failures.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` — exit 0.
- `LC_ALL=C` re-run of the five new gate modules — passes.
- `tox -e docs-html`, `tox -e docs-pdf` — exit 0.
- `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` — recorded.
- `CHANGELOG.md`'s `## [Unreleased]` names `templates_path`, `WR-01`, `CR-01` and `_typst`, and does
  not mention the Phase 57 asset-list removal.
</verification>

<success_criteria>
- A build carrying two different pre-write failure kinds raises once, naming both, with a message
  identical across inverted declaration order after path normalization (D-03).
- The breaking migration is announced in the v0.9.0 `Unreleased` CHANGELOG as an instruction naming
  the target directory (D-01's `costly` reversibility, discharged).
- The phase closes green on the full suite (modulo the 7 known pre-existing failures), the three
  CI-matching gates, the locale re-run, and both dogfooding builds.
- Milestone invariants #9 (branch pushed) and #11 (zero new deps, `@preview` count four, no fourth
  lockstep site) are measured and recorded, not assumed.
- WR-01 and CR-01 are each mapped to the gate command that proves them closed.
</success_criteria>

<output>
Create `.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-05-SUMMARY.md` when done.
</output>