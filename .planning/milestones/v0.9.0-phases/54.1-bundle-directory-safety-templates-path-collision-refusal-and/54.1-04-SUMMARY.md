---
phase: 54.1-bundle-directory-safety-templates-path-collision-refusal-and
plan: 04
subsystem: testing
tags: [sphinx, builder, templates_path, extensionerror, pre-write-validation, gate-tests]

# Dependency graph
requires:
  - phase: 54.1-bundle-directory-safety-templates-path-collision-refusal-and (plan 01)
    provides: "_paths_related(), TypstBuilder._validate_used_template_paths(), and the tracer gate module tests/test_templates_path_collision_gate.py this plan extends"
provides:
  - "A multi-relation fixture and test class asserting all three of D-02's path relations (is / is-contained-by / contains) refuse in ONE build, with D-03's accumulate-then-raise-once aggregation and sorted() key ordering pinned by index"
  - "Three control fixtures pinning D-02's non-refusing edges: an adjacency false-positive tripwire, and the two measured non-colliding shapes (no templates_path set; templates_path set but no Typst template configured)"
  - "A synthesized-entry fixture proving D-05: an unset typst_documents still routes the built-in \"typst\" key through the checked key set via _default_typst_documents()'s callable default"
  - "54.1-04-RED-EVIDENCE.md: two RED sections (multi-relation aggregation/ordering, synthesized-entry) measured against the same pre-fix commit as 54.1-01, via the swap-and-restore procedure"
affects: ["54.1-05 (cross-kind aggregation, CHANGELOG entry, needs this plan's edge-case coverage complete before final verification)"]

# Actuals (#2632)
actuals:
  tokens: 13600
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Aggregate-message extraction via a capturing regex (AGGREGATE_PREFIX_RE) rather than a fixed marker substring, because the failure count digit varies per build -- modelled on _extract_registry_key_message() but generalized for a variable prefix."
    - "Ordering assertions located by str.find() INDEX comparison (strictly increasing), not substring presence, so an ordering regression is actually caught rather than silently passing."
    - "Control fixtures asserting both exit code 0 AND the exact produced .typ file set, never exit-0-only -- matches test_registry_prewrite_validation_gate.py's established control shape."

key-files:
  created:
    - tests/fixtures/templates_path_collision_multi_gate/conf.py
    - tests/fixtures/templates_path_collision_multi_gate/index.rst
    - tests/fixtures/templates_path_collision_multi_gate/_templates/alpha.typ
    - tests/fixtures/templates_path_collision_multi_gate/_templates/nested/beta.typ
    - tests/fixtures/templates_path_collision_multi_gate/_typst/gamma.typ
    - tests/fixtures/templates_path_adjacent_control_gate/conf.py
    - tests/fixtures/templates_path_adjacent_control_gate/index.rst
    - tests/fixtures/templates_path_adjacent_control_gate/_templatesx/custom.typ
    - tests/fixtures/templates_path_absent_control_gate/conf.py
    - tests/fixtures/templates_path_absent_control_gate/index.rst
    - tests/fixtures/templates_path_absent_control_gate/_templates/custom.typ
    - tests/fixtures/templates_path_no_typst_template_control_gate/conf.py
    - tests/fixtures/templates_path_no_typst_template_control_gate/index.rst
    - tests/fixtures/templates_path_default_documents_gate/conf.py
    - tests/fixtures/templates_path_default_documents_gate/index.rst
    - tests/fixtures/templates_path_default_documents_gate/_templates/custom.typ
    - .planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-04-RED-EVIDENCE.md
  modified:
    - tests/test_templates_path_collision_gate.py

key-decisions:
  - "The multi-relation fixture uses ONE docname (\"index\") carrying THREE typst_documents entries with three distinct target filenames, rather than three separate .rst documents -- per the plan's own \"OR\" option, sidesteps _validate_output_path_collisions() entirely (D-04's own docstring explicitly allows same-docname/different-target entries) without needing extra fixture files."
  - "Both the absent-templates_path control fixture and the synthesized-entry fixture's conf.py were written to contain ZERO literal occurrences of the config-value names their own acceptance criteria grep for (\"templates_path\" and \"typst_documents\" respectively) -- including in comments -- by describing the config values in prose rather than naming them, so the grep assertions are genuinely meaningful rather than accidentally self-defeating."
  - "The aggregate-message helper (_extract_aggregate_message) matches on a capturing regex (AGGREGATE_PREFIX_RE) rather than a fixed marker string, because the failure count digit varies per build -- the count assertion reads match.group(1) directly rather than counting substrings, per the plan's explicit instruction."

patterns-established:
  - "Ordering-by-index assertion for accumulate-then-raise-once error messages: locate each expected substring's position with str.find() and assert positions are strictly increasing, rather than merely asserting presence."

requirements-completed: []

coverage:
  - id: D1
    description: "All three of D-02's path relations (is / is-contained-by / contains) refuse in ONE build across three registry keys, with D-03's aggregated, sorted()-ordered ExtensionError naming all three keys, a correct failure count, and each key's own sentence naming its own bundle directory and colliding entry."
    requirement: "WR-01"
    verification:
      - kind: integration
        ref: "tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate"
        status: pass
    human_judgment: false
  - id: D2
    description: "The three shapes D-02 says must NEVER refuse (adjacency false positive, no templates_path set, templates_path set but no Typst template) build successfully with the expected .typ file set and no collision marker in output."
    requirement: "WR-01"
    verification:
      - kind: integration
        ref: "tests/test_templates_path_collision_gate.py::TestNonCollidingShapesControlGate"
        status: pass
    human_judgment: false
  - id: D3
    description: "An unset typst_documents still refuses on a colliding global Typst template, proving the synthesized entry's key (D-05) reaches the checked key set."
    requirement: "WR-01"
    verification:
      - kind: integration
        ref: "tests/test_templates_path_collision_gate.py::test_default_documents_collision_refuses_build"
        status: pass
    human_judgment: false

# Metrics
duration: 12min
completed: 2026-08-16
status: complete
---

# Phase 54.1 Plan 04: WR-01 Edge and Control Cases Summary

**Five new fixtures and thirteen new test methods pin all three of D-02's path relations (with D-03's aggregation/ordering), all three of D-02's non-refusing shapes, and D-05's synthesized-entry coverage — closing the edge-case surface `54.1-01`'s tracer slice left open.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-16T11:36:00+09:00
- **Completed:** 2026-08-16T11:47:40+09:00
- **Tasks:** 3
- **Files modified:** 18 (1 modified, 17 created)

## Accomplishments
- `tests/fixtures/templates_path_collision_multi_gate/` — one build exercising all three of D-02's path relations at once across registry keys `alpha`/`beta`/`gamma` (equality, is-contained-by, contains), plus a `TestMultiRelationAggregationGate` class (5 tests) asserting the refusal, the sorted-key-order aggregate message (located by index, not substring presence), the exact failure count (`3`, read from the aggregate prefix's captured digit), each key's own bundle-directory/entry naming, and zero surviving `.typ` files.
- Three control fixtures (`templates_path_adjacent_control_gate`, `templates_path_absent_control_gate`, `templates_path_no_typst_template_control_gate`) plus a `TestNonCollidingShapesControlGate` class (3 tests) pinning D-02's adjacency false-positive tripwire and its two measured non-colliding shapes — each asserting exit 0, the exact `.typ` set, and the absence of the collision marker.
- `tests/fixtures/templates_path_default_documents_gate/` — proves D-05: an unset `typst_documents` still routes the built-in `"typst"` registry key through the checked key set via `_default_typst_documents()`'s callable default, refusing on a colliding global `typst_template`. Two new tests (refusal + zero-`.typ`-survivors).
- `54.1-04-RED-EVIDENCE.md` — two RED sections (multi-relation aggregation/ordering, synthesized-entry), both measured via the swap-and-restore procedure against the same pre-fix commit (`9cc529b7`) `54.1-01-RED-EVIDENCE.md` used, with `git status --porcelain typsphinx/` confirmed clean after each restoration.
- Two cross-plan fixture-safety sweeps (no template at any fixture's source-tree root; no fixture registry key case-folding onto the reserved `"typst"` key) run over every fixture this plan created and recorded pass, guarding against the CONF-17 hoist and reserved-key case check plan `54.1-03` lands in the same wave.

## Task Commits

Each task was committed atomically:

1. **Task 1: Assert all three D-02 relations and D-03's single aggregated, sorted raise** - `e444b718` (test)
2. **Task 2: Assert the three shapes that must NOT refuse** - `4c672095` (test)
3. **Task 3: Assert the synthesized typst_documents entry is checked** - `0886facb` (test)

**Plan metadata:** (this commit) — SUMMARY.md, per worktree-mode conventions STATE.md/ROADMAP.md are NOT touched here; the orchestrator owns those writes after the wave merges.

## Files Created/Modified
- `tests/test_templates_path_collision_gate.py` - Extended with `MULTI_FIXTURE_DIR`/control/`DEFAULT_DOCUMENTS_FIXTURE_DIR` constants, `AGGREGATE_PREFIX_RE` + `_extract_aggregate_message()`, `TestMultiRelationAggregationGate` (5 tests), `TestNonCollidingShapesControlGate` (3 tests), and two module-level synthesized-entry tests.
- `tests/fixtures/templates_path_collision_multi_gate/{conf.py,index.rst,_templates/alpha.typ,_templates/nested/beta.typ,_typst/gamma.typ}` - Multi-relation fixture: two `templates_path` entries, three registry keys each exercising a distinct D-02 relation, one docname with three `typst_documents` entries carrying distinct targets.
- `tests/fixtures/templates_path_adjacent_control_gate/{conf.py,index.rst,_templatesx/custom.typ}` - Sibling-directory-prefix control (the false positive a `str.startswith()` containment test would produce).
- `tests/fixtures/templates_path_absent_control_gate/{conf.py,index.rst,_templates/custom.typ}` - D-02 bullet 1's measured shape control: no Jinja override directory set at all.
- `tests/fixtures/templates_path_no_typst_template_control_gate/{conf.py,index.rst}` - D-02 bullet 2's measured shape control: `templates_path` set, no Typst template configured.
- `tests/fixtures/templates_path_default_documents_gate/{conf.py,index.rst,_templates/custom.typ}` - D-05's synthesized-entry fixture: `typst_documents` deliberately unset.
- `.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-04-RED-EVIDENCE.md` - Two RED transcripts (multi-relation, synthesized-entry) plus the fixture-safety sweep results.

## Decisions Made
- The multi-relation fixture uses one docname with three `typst_documents` entries carrying distinct target filenames rather than three separate `.rst` documents — the plan's own alternative option, and it exactly matches the `files_modified` list in the plan's frontmatter (only `index.rst`, no per-key `.rst` files).
- Both the absent-`templates_path` control fixture and the synthesized-entry fixture's `conf.py` files were drafted with zero literal occurrences of the config-value names their own acceptance criteria `grep` for (`templates_path`, `typst_documents`) — including in header comments — by describing the config values in prose. Verified with `grep -c` returning `0` for both before committing.
- `_extract_aggregate_message()` matches via a capturing regex (`AGGREGATE_PREFIX_RE`) rather than a fixed marker substring, since the failure count digit varies by build; the count assertion reads `match.group(1)` directly rather than counting substrings, per the plan's explicit instruction to "read the number out of the aggregate prefix rather than counting substrings."

## Deviations from Plan

None — plan executed exactly as written. All three tasks' acceptance criteria (aggregate ordering/count, control build/`.typ`-set/marker-absence checks, fixture-safety sweeps, `grep -c` zero-occurrence checks) passed as specified; no auto-fixes were needed.

## Issues Encountered
- `ruff` is a generic-linux ELF binary that cannot execute directly inside this NixOS sandbox (`uv run ruff` fails to start). Worked around with `nix run nixpkgs#ruff -- check <files>`, which reported "All checks passed!" for every file this plan touched — a previously-documented environment hazard (not a defect in this plan's code), consistent with `54.1-01-SUMMARY.md` and `54.1-02-SUMMARY.md`'s own notes.
- `black --check` initially flagged minor reformatting on `tests/test_templates_path_collision_gate.py` after each of the first two tasks' edits (line-wrapping differences in newly-appended assertions). Ran `uv run black tests/test_templates_path_collision_gate.py` both times and re-verified the full gate module still passed afterward — cosmetic only, no assertion logic changed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- WR-01's edge-case surface is now fully asserted: all three of D-02's path relations (with D-03's aggregation/ordering), both of D-02's measured non-colliding shapes, the adjacency false-positive tripwire, and D-05's synthesized-entry coverage.
- `uv run pytest tests/test_templates_path_collision_gate.py` — **12 passed**, well over the plan's "at least 10" bar.
- `uv run pytest -q` (full suite) — **1309 passed, 5 skipped, 0 failed** — no pre-existing failures reproduced on this worktree's base commit (matching `54.1-01-SUMMARY.md`'s own observation that the previously-documented 7 `tests/test_state_guard_shapes_gate.py` failures did not reproduce here either).
- `git status --porcelain typsphinx/` is clean at plan end — every RED-evidence swap was fully restored.
- Every fixture this plan created is verified safe against the CONF-17 hoist and reserved-key case check plan `54.1-03` lands in the same wave (both fixture-safety sweeps pass, recorded in `54.1-04-RED-EVIDENCE.md`).
- `.planning/REQUIREMENTS.md` intentionally NOT updated for WR-01 from this plan alone, matching `54.1-01-SUMMARY.md`'s own precedent: WR-01's text requires both the runtime refusal (`54.1-01`) and the documentation half (`54.1-02`), and this plan's edge-case coverage is additive to `54.1-01`'s slice, not a standalone requirement closure. The orchestrator (or a later plan) should close WR-01 once all of `54.1-01`, `54.1-02`, and this plan's coverage are accounted for.
- No blockers.

## Self-Check: PASSED

All 17 claimed created files verified present on disk:
`tests/fixtures/templates_path_collision_multi_gate/{conf.py,index.rst,_templates/alpha.typ,_templates/nested/beta.typ,_typst/gamma.typ}`,
`tests/fixtures/templates_path_adjacent_control_gate/{conf.py,index.rst,_templatesx/custom.typ}`,
`tests/fixtures/templates_path_absent_control_gate/{conf.py,index.rst,_templates/custom.typ}`,
`tests/fixtures/templates_path_no_typst_template_control_gate/{conf.py,index.rst}`,
`tests/fixtures/templates_path_default_documents_gate/{conf.py,index.rst,_templates/custom.typ}`,
`.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-04-RED-EVIDENCE.md`.
All 3 claimed task commit hashes (`e444b718`, `4c672095`, `0886facb`) verified present via `git log --oneline`.

---
*Phase: 54.1-bundle-directory-safety-templates-path-collision-refusal-and*
*Completed: 2026-08-16*
