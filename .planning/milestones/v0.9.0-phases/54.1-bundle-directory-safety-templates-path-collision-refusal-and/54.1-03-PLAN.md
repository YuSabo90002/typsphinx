---
phase: 54.1-bundle-directory-safety-templates-path-collision-refusal-and
plan: 03
type: execute
wave: 2
depends_on: ["54.1-01"]
files_modified:
  - typsphinx/builder.py
  - tests/test_conf17_prewrite_gate.py
  - tests/test_reserved_key_case_prewrite_gate.py
  - tests/fixtures/conf17_prewrite_gate/conf.py
  - tests/fixtures/conf17_prewrite_gate/index.rst
  - tests/fixtures/conf17_prewrite_gate/other.rst
  - tests/fixtures/conf17_prewrite_gate/custom.typ
  - tests/fixtures/reserved_key_case_prewrite_gate/conf.py
  - tests/fixtures/reserved_key_case_prewrite_gate/index.rst
  - tests/fixtures/reserved_key_case_prewrite_gate/_typst/other.typ
  - .planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-03-RED-EVIDENCE.md
autonomous: true
requirements: [CR-01]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CR-01: a global Typst template naming a bare filename at the source root stops the build with ZERO `.typ` files on disk — the A-01/CONF-17 guard now runs in the same pre-write position as `_validate_output_path_collisions()` and `_validate_registry_key_references()`, not only at `finish()`"
    - "D-05: the hoisted check reaches the SYNTHESIZED built-in `\"typst\"` registry key — the key `resolve_template_registry()` appends after its declared-key validation loop, and therefore the one key CONF-17 never validated"
    - "D-06: the existing `finish()`-side A-01/CONF-17 guard in `_copy_used_template_bundles()` STAYS — the duplication is deliberate, because many existing tests drive `write_doc()`/`_write_typst_files()` and `finish()` directly without ever calling `write()`, and on that path the pre-write pass never runs"
    - "D-07: a declared registry key differing from the reserved built-in key only by case is refused pre-write, routed through the existing `_collision_key()` folding — never a second case-folding primitive"
    - "CR-01 adjacency edge — a declared key and the reserved built-in key that compare EQUAL under `_collision_key()`'s folding but UNEQUAL under `==` are treated as a collision; CONF-18 does not catch this because it compares declared keys only against each other and never against the synthesized key"
    - "CR-01 empty edge — the hoisted check fires for a project that declares NO registry keys at all (the registry then holds only the synthesized built-in key), and it fires for a key referenced only by a `typst_documents` entry whose docname is NOT in this build's `docnames` set, which is the same order-independence property `_validate_registry_key_references()` was given"
    - "The CONF-17/A-01 message text is built in ONE place and used by both the pre-write and the `finish()`-side site, so the two accepted duplicate guards cannot drift apart in wording"
    - statement: "CR-01 concurrency edge — the pre-write validation pass runs exactly once per `write()` call, in the process that calls `write()`, before `prepare_writing()` and before any per-docname work; `TypstBuilder.write()` performs its own sequential per-docname loop and forks no parallel writer, so a refusal is raised once and never once-per-worker. No source artifact settles Sphinx's parallel-write interaction for this builder, so this is recorded as a backstop rather than an explicit acceptance criterion."
      verification: backstop
  artifacts:
    - path: "typsphinx/builder.py"
      provides: "the hoisted A-01/CONF-17 branch and the reserved-key case-collision branch, both feeding `_validate_used_template_paths()`'s single `failures` list, plus a shared `_conf17_violation_message()` used by both guard sites"
      contains: "def _conf17_violation_message"
    - path: "tests/test_conf17_prewrite_gate.py"
      provides: "real `sphinx-build` subprocess gate for the hoisted CONF-17/A-01 check, including the zero-`.typ`-files property and the restricted-docnames order-independence case"
    - path: "tests/test_reserved_key_case_prewrite_gate.py"
      provides: "real `sphinx-build` subprocess gate for the reserved-key case collision"
    - path: ".planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-03-RED-EVIDENCE.md"
      provides: "verbatim pre-fix RED transcripts for both CR-01 gates (binding constraint #6)"
  key_links:
    - from: "typsphinx/builder.py::TypstBuilder._validate_used_template_paths"
      to: "typsphinx/template_registry.py::_violates_conf17"
      via: "the existing CONF-17 predicate imported and called unchanged on the RESOLVED template path — never a second copy of its containment logic"
      pattern: "_violates_conf17\\("
    - from: "typsphinx/builder.py::TypstBuilder._validate_used_template_paths"
      to: "typsphinx/builder.py::TypstBuilder._collision_key"
      via: "the reserved-key case comparison routes through the single existing case-folding primitive"
      pattern: "_collision_key\\("
    - from: "typsphinx/builder.py::TypstBuilder._copy_used_template_bundles"
      to: "typsphinx/builder.py::_conf17_violation_message"
      via: "the surviving finish()-side guard raises with the shared message builder instead of its own inline f-string"
      pattern: "_conf17_violation_message\\("
  prohibitions:
    - requirement_id: CR-01
      category: integrity
      status: resolved
      verification: judgment
      statement: "MUST NOT record RED evidence reconstructed, remembered, or written after the production change — every transcript in the RED-EVIDENCE file must come from a real command run against the named pre-fix commit"
---

<objective>
Close CR-01 by expanding the proven pre-write skeleton with two more failure kinds: the hoisted
A-01/CONF-17 check (which today runs only at `finish()`, after every content and wrapper `.typ`
file is already on disk) and the reserved-key case collision (which CONF-18 structurally cannot
catch, because it compares declared keys only against each other and never against the synthesized
built-in key).

Purpose: this is the one path that breaks the invariant `_validate_output_path_collisions()`,
`_validate_registry_key_references()`, and
`test_template_prefix_reservation_gate.py::test_no_typ_file_written_after_refusal` all establish. A
one-line configuration mistake currently writes a full, broken output tree and only then fails.

Output: two new branches inside `TypstBuilder._validate_used_template_paths()` feeding its existing
single `failures` list; a shared `_conf17_violation_message()` so the pre-write and `finish()`-side
guards cannot drift apart; two new subprocess gate modules with fixtures; and verbatim pre-fix RED
transcripts.

**This is an expansion of plan `54.1-01`'s tracer slice, not a new architecture.** The key-set
derivation, per-key `TemplateEngine` resolution, `sorted()` iteration, accumulation, and
`ExtensionError` prefix all already exist and are proven end to end. This plan adds two `failures.append(...)`
sites and their evidence.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-CONTEXT.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-PATTERNS.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-01-SUMMARY.md
@CLAUDE.md
</context>

## Execution environment (MANDATORY — read before any command)

Worktree-isolated execution is this project's STANDING mode. Before running anything in your
worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run EVERY
subsequent command through `uv run …`. Without this, pytest imports the UNCHANGED main-tree package
and gates stay RED after a correct fix.

## Baseline: what "green" means in this phase

The full suite currently has **exactly 7 pre-existing failures** in
`tests/test_state_guard_shapes_gate.py` (they read an archived `.planning/` path), recorded in
`.planning/phases/53-template-registry-foundation/deferred-items.md`. They predate Phase 53 and are
**NOT** a regression this phase causes. Do NOT fix them and do NOT report them as new RED.

## Scope fence (do NOT touch)

- **WR-03** — a declared key with neither `template` nor `package` sharing the reserved key's shadow
  search path. Explicitly out of this phase, even though the code you are editing is adjacent to it.
- **WR-04** — the duplicate local re-import of `TEMPLATE_OUTPUT_DIR` inside
  `_copy_used_template_bundles()`. You WILL be editing a few lines away from it. Leave it exactly as
  it is.
- **IN-01** — the private cross-module helper import in `_copy_used_template_bundles()`. Leave it.
- Do NOT remove the `finish()`-side A-01/CONF-17 guard (D-06). Its duplication is a deliberate
  decision, not an oversight: removing it silently drops A-01 protection for every test that drives
  `write_doc()`/`_write_typst_files()` and `finish()` without calling `write()` — the same callers
  the lazy registry fallback exists for.
- Do NOT change the wording of the CONF-17/A-01 sentence. Existing tests read it. The shared message
  builder must reproduce today's text byte-for-byte.
- Typing-import modernization is FORBIDDEN. The `@preview` count stays four with no fourth
  version-lockstep site.

<tasks>

<task type="auto">
  <name>Task 1: Record RED — CONF-17 hoist and reserved-key case fixtures and gates, against the pre-fix tree</name>

  <read_first>
    - `tests/test_registry_prewrite_validation_gate.py` in full — the `_run_sphinx_build()`
      subprocess helper (`sys.executable -m sphinx`), the `_typ_files()` helper that returns the LIST
      so a failure message names survivors, the marker-constant convention, and the
      `test_conf14_bad_key_sorting_first/last_writes_no_typ_files` +
      `test_control_config_builds_unchanged_typ_set` methodology.
    - `tests/test_template_prefix_reservation_gate.py:150-170` — `test_no_typ_file_written_after_refusal`,
      the exact sibling property shape ROADMAP SC#3 names as the model for CR-01's new test.
    - `tests/fixtures/conf14_prewrite_bad_last_gate/conf.py` — the minimal fixture shape and the
      "load-bearing properties — do NOT touch any of these" header-comment convention.
    - `typsphinx/template_registry.py:320-360` and `:455-470` — the declared-key validation loop
      (where CONF-16/CONF-17/CONF-18 run) and the synthesized built-in key appended AFTER it, which
      is the precise code-level root cause CR-01 names.
    - `typsphinx/builder.py:1615-1645` — the existing `finish()`-side A-01/CONF-17 raise, so the new
      gate's marker constant matches the message text that already exists.
    - `.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-01-RED-EVIDENCE.md`
      — the RED-EVIDENCE artifact shape.
  </read_first>

  <files>
    tests/fixtures/conf17_prewrite_gate/conf.py,
    tests/fixtures/conf17_prewrite_gate/index.rst,
    tests/fixtures/conf17_prewrite_gate/other.rst,
    tests/fixtures/conf17_prewrite_gate/custom.typ,
    tests/fixtures/reserved_key_case_prewrite_gate/conf.py,
    tests/fixtures/reserved_key_case_prewrite_gate/index.rst,
    tests/fixtures/reserved_key_case_prewrite_gate/_typst/other.typ,
    tests/test_conf17_prewrite_gate.py,
    tests/test_reserved_key_case_prewrite_gate.py,
    .planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-03-RED-EVIDENCE.md
  </files>

  <behavior>
    - CONF-17 gate test 1: `sphinx-build -b typst` on the CONF-17 fixture exits non-zero and the
      combined output contains the A-01/CONF-17 marker sentence.
    - CONF-17 gate test 2: after that refused build, no `.typ` file exists anywhere under the build
      directory. RED today — today the refusal happens at `finish()`, so a full `.typ` tree survives.
    - CONF-17 gate test 3 (order independence): invoking `sphinx-build` with an explicit filename
      argument that restricts the build to ONE docname still refuses, even when the offending key is
      referenced only by the `typst_documents` entry for the OTHER docname — the same
      order-independence property `_validate_registry_key_references()` was given.
    - Reserved-key gate test 1: a project declaring a registry key that differs from the reserved
      built-in key only by case exits non-zero with the case-collision marker sentence.
    - Reserved-key gate test 2: after that refused build, no `.typ` file exists under the build
      directory.
  </behavior>

  <action>
    Create `tests/fixtures/conf17_prewrite_gate/` as a minimal real Sphinx source tree following
    `tests/fixtures/conf14_prewrite_bad_last_gate/`'s shape:

    - `custom.typ` at the source-tree ROOT — a real, compilable template exposing a `project`
      function with the parameter surface `writer.py` passes (`title`, `authors`, `date`, `body`),
      with NO `@preview` imports. Its location at the root is the whole point: its parent directory
      IS `srcdir`, which is exactly what CONF-17/A-01 forbids.
    - `conf.py` sets `project`/`author`/`release`/`copyright`, `extensions = ["typsphinx"]`, and the
      GLOBAL Typst template config value naming that bare filename. It declares NO
      `typst_document_templates` at all — so the resolved registry holds only the SYNTHESIZED
      built-in key, which is the key `resolve_template_registry()`'s declared-key loop never
      validates and which CR-01 names as the root cause. It declares a `typst_documents` list with
      two entries, one per docname, so test 3 can restrict the build to one of them. Head the file
      with the "load-bearing properties — do NOT touch any of these" comment block naming: the
      template must stay at the source-tree root; no registry may be declared; and both docnames are
      load-bearing for the restricted-build test.
    - `index.rst` and `other.rst` — a title and one paragraph each.

    Create `tests/fixtures/reserved_key_case_prewrite_gate/`:

    - `_typst/other.typ` — the same minimal template shape, in a proper subdirectory so this fixture
      trips ONLY the case collision and not CONF-17.
    - `conf.py` declares a `typst_document_templates` mapping whose single key differs from the
      reserved built-in registry key only by capitalisation, with that key's `template` pointing at
      `_typst/other.typ`, plus a `typst_documents` entry naming `index` and referencing that key.
      Head it with the load-bearing-properties comment naming the capitalisation as the point of the
      fixture.
    - `index.rst`.

    Create `tests/test_conf17_prewrite_gate.py` and `tests/test_reserved_key_case_prewrite_gate.py`.
    Each carries its own verbatim copy of `_run_sphinx_build()` and `_typ_files()` (per-module
    copies are this suite's convention — never a cross-module import), and defines its own marker
    constant:

    - `CONF17_PREWRITE_MARKER` — the existing A-01/CONF-17 sentence fragment
      `has a parent directory that is srcdir itself, or an ancestor of srcdir`, copied verbatim from
      the message that already exists at the `finish()`-side guard.
    - `RESERVED_KEY_CASE_MARKER` — the fragment the new case-collision message will use:
      `differs from the built-in 'typst' registry key only by case`.

    Extend `_run_sphinx_build()` in the CONF-17 module with an optional trailing-filenames argument
    so test 3 can invoke `sphinx-build -b typst <src> <out> index.rst`, restricting the build's
    docname set. Write the five tests described in `<behavior>`. Give each module a docstring
    stating CR-01, the relevant decisions (D-04/D-05/D-06 for the CONF-17 module, D-07 for the
    reserved-key module), and citing `54.1-03-RED-EVIDENCE.md` by path.

    Then, BEFORE touching `typsphinx/builder.py`, run BOTH gate modules against the tree as it stands
    and record the results verbatim into `54.1-03-RED-EVIDENCE.md`, following
    `54-01-RED-EVIDENCE.md`'s shape: the real pre-fix commit SHA from `git rev-parse HEAD` in this
    worktree, one section per finding (CONF-17 hoist / reserved-key case), each with a fixture
    description, the exact command line, the pass/fail counts, and a verbatim failure tail. For the
    CONF-17 module the tail must SHOW the surviving `.typ` files — that list is the evidence CR-01
    is about. Binding constraint #6: every line must come from a command you actually ran, in this
    task, before any production edit.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest tests/test_conf17_prewrite_gate.py tests/test_reserved_key_case_prewrite_gate.py 2>&1 | tail -60; test -s .planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-03-RED-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_conf17_prewrite_gate.py tests/test_reserved_key_case_prewrite_gate.py` reports at least **3 failed** at this point (the two zero-`.typ`-files tests and the reserved-key refusal test are RED against the pre-fix tree).
    - `test -f tests/fixtures/conf17_prewrite_gate/custom.typ` succeeds, and `uv run python -c "import pathlib,sys; p=pathlib.Path('tests/fixtures/conf17_prewrite_gate/custom.typ'); sys.exit(0 if p.parent.name=='conf17_prewrite_gate' else 1)"` exits 0 — the template really sits at the source-tree root.
    - `grep -c 'typst_document_templates' tests/fixtures/conf17_prewrite_gate/conf.py` returns `0` — the fixture declares no registry, so only the synthesized built-in key exists.
    - `grep -c 'CONF17_PREWRITE_MARKER' tests/test_conf17_prewrite_gate.py` returns at least `2`, and `grep -c 'RESERVED_KEY_CASE_MARKER' tests/test_reserved_key_case_prewrite_gate.py` returns at least `2`.
    - `grep -c '@preview' tests/fixtures/conf17_prewrite_gate/custom.typ tests/fixtures/reserved_key_case_prewrite_gate/_typst/other.typ` reports `0` for both files.
    - `.planning/phases/54.1-.../54.1-03-RED-EVIDENCE.md` exists, is non-empty, contains the 40-character SHA `git rev-parse HEAD` printed in this worktree before any `typsphinx/` edit, contains both literal command strings, and its CONF-17 section lists the surviving `.typ` files from the pre-fix run.
    - `git status --porcelain typsphinx/` produces no output — no production file was touched in this task.
  </acceptance_criteria>

  <done>
    Both CR-01 fixtures and both gate modules exist, both gates are measured RED against the pre-fix
    tree with the surviving-`.typ`-file list captured verbatim, and `typsphinx/` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hoist the A-01/CONF-17 check into the pre-write pass, behind one shared message</name>

  <read_first>
    - `typsphinx/builder.py` — the `_validate_used_template_paths()` method plan `54.1-01` added,
      in full: its key-set derivation, per-key `TemplateEngine` construction, `failures` list and
      terminal raise. You are adding a branch to code that already exists; read it before writing.
    - `typsphinx/builder.py:1590-1665` — `_copy_used_template_bundles()`'s existing A-01/CONF-17
      raise, verbatim, including its exact wording, and the `WR-04` local re-import a few lines away
      that you must leave alone.
    - `typsphinx/template_registry.py:137-176` — `_violates_conf17()`, the predicate to import and
      call unchanged.
    - `typsphinx/template_engine.py:55-95` — `TemplateResolution.source` and `.path`.
  </read_first>

  <files>typsphinx/builder.py</files>

  <behavior>
    - A resolved template whose parent directory IS `srcdir`, or is an ancestor of `srcdir`, produces
      a failure appended to `_validate_used_template_paths()`'s existing `failures` list.
    - The refusal happens before `prepare_writing()`, so zero `.typ` files reach disk.
    - The synthesized built-in registry key is covered — it is in the key set derived from
      `typst_documents`, including the entries `_default_typst_documents()` synthesizes.
    - The `finish()`-side guard still raises for callers that reach `_copy_used_template_bundles()`
      without going through `write()`, with byte-identical message text.
  </behavior>

  <action>
    Add a module-level function `_conf17_violation_message(key: str, resolved_path: str, srcdir: str) -> str`
    to `typsphinx/builder.py`, placed with the other module-level helpers. It returns the CONF-17/A-01
    sentence **byte-for-byte identical to the string `_copy_used_template_bundles()` builds today** —
    copy that f-string, do not paraphrase it, do not "improve" it. Existing tests read this text.
    Docstring: state that it exists because the same sentence is now raised from two deliberately
    duplicated guard sites (D-06) and one shared builder is what stops them drifting.

    Replace the inline f-string inside `_copy_used_template_bundles()`'s existing A-01 raise with a
    call to `_conf17_violation_message(...)`. The guard itself STAYS exactly where it is and keeps
    raising immediately (D-06). Change nothing else in that method — in particular, leave the local
    `TEMPLATE_OUTPUT_DIR` re-import (WR-04) and the private cross-module import (IN-01) untouched.

    In `_validate_used_template_paths()`, inside the existing per-key loop and immediately after the
    `resolve_template()` call, add the hoisted check: when
    `_violates_conf17(str(resolution.path), str(self.srcdir))` is True, append
    `(key, _conf17_violation_message(key, str(resolution.path), str(self.srcdir)))` to the SAME
    `failures` list the `templates_path` branch already uses, then `continue` to the next key —
    a template that already violates CONF-17 has nothing useful to say about `templates_path`
    containment, and reporting both for one key is noise. Do NOT raise inside the loop: the
    `finish()`-side guard's immediate-raise shape is correct THERE and wrong here, because D-03
    requires every offending key to be named in one message.

    Unlike the `templates_path` branch, this check does NOT skip
    `resolution.source == "default"` — the packaged default's parent directory is inside the
    installed package and can never be `srcdir` or its ancestor, so the predicate simply returns
    False for it and no special-case is warranted. Say so in a short comment so a later reader does
    not "fix" the asymmetry between the two branches.

    Extend `_validate_used_template_paths()`'s docstring to state that it now covers two failure
    kinds, that the CONF-17/A-01 kind is a HOIST of a check that also still runs at `finish()`
    (D-06, with the reason: tests that drive `write_doc()`/`_write_typst_files()` and `finish()`
    directly never enter `write()`), and that the hoisted check is the first place the SYNTHESIZED
    built-in key is ever CONF-17-validated, because `resolve_template_registry()` appends that key
    after its declared-key validation loop.
  </action>

  <verify>
    <automated>uv run pytest tests/test_conf17_prewrite_gate.py -x && uv run pytest tests/test_templates_path_collision_gate.py tests/test_template_prefix_reservation_gate.py tests/test_registry_prewrite_validation_gate.py -q && uv run black --check typsphinx/builder.py && uv run ruff check typsphinx/builder.py && uv run mypy typsphinx/</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_conf17_prewrite_gate.py` reports **3 passed, 0 failed**.
    - `grep -c 'def _conf17_violation_message' typsphinx/builder.py` returns `1`.
    - `grep -c '_conf17_violation_message(' typsphinx/builder.py` returns at least `3` (definition plus both call sites).
    - `awk '/def _copy_used_template_bundles/,0' typsphinx/builder.py | grep -c '_violates_conf17('` returns `1` — the `finish()`-side guard is still present (D-06).
    - `awk '/def _validate_used_template_paths/,/^    def [a-z]/' typsphinx/builder.py | grep -c '_violates_conf17('` returns `1` — the hoisted check calls the SAME predicate, not a re-derived copy.
    - `grep -c 'def _violates_conf17' typsphinx/*.py` returns `1` — no second copy of the predicate was written.
    - `uv run python -c "import subprocess,sys,tempfile,pathlib; d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','sphinx','-b','typst','tests/fixtures/conf17_prewrite_gate',d],capture_output=True,text=True); out=r.stdout+r.stderr; assert r.returncode!=0, out; assert 'has a parent directory that is srcdir itself, or an ancestor of srcdir' in out, out; assert not list(pathlib.Path(d).rglob('*.typ')), list(pathlib.Path(d).rglob('*.typ')); print('OK')"` prints `OK`.
    - `uv run pytest tests/test_template_prefix_reservation_gate.py tests/test_registry_prewrite_validation_gate.py tests/test_templates_path_collision_gate.py -q` passes — the existing pre-write guards and plan `54.1-01`'s gate are unaffected.
    - `git diff -- typsphinx/builder.py | grep -E '^[-+].*TEMPLATE_OUTPUT_DIR' | wc -l` returns `0` — WR-04 was not folded in.
    - `uv run black --check typsphinx/builder.py`, `uv run ruff check typsphinx/builder.py`, `uv run mypy typsphinx/` all exit 0.
  </acceptance_criteria>

  <done>
    A bare-filename global Typst template at the source root refuses the build with zero `.typ`
    files, the synthesized built-in key is CONF-17-validated for the first time, both guard sites
    share one message builder, and the `finish()`-side guard is intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refuse a declared registry key that collides with the reserved key only by case</name>

  <read_first>
    - `typsphinx/builder.py:490-575` — `_collision_key()` in full: what it normalizes (backslashes to
      forward slashes, `posixpath.normpath`, then `casefold`), and its docstring's statement that the
      folding is unconditional on every platform with no `sys.platform` branch.
    - `typsphinx/builder.py` — `_validate_used_template_paths()` as it stands after Task 2, so the
      new branch joins the existing `failures` list rather than adding a second raise.
    - `typsphinx/template_registry.py:38` — `RESERVED_REGISTRY_KEY`, and `:330-350` — CONF-16's
      "cannot be redeclared" check on the literal key and CONF-18's declared-keys-against-each-other
      comparison, so you can see precisely why neither catches a case variant.
    - `typsphinx/builder.py:1645-1670` — the `finish()`-side bundle-destination collision check,
      which is where this collision surfaces today (and only when both keys are used).
  </read_first>

  <files>typsphinx/builder.py</files>

  <behavior>
    - A registry declaring a key that folds to the reserved built-in key's folded form, but is not
      equal to it under `==`, produces a failure appended to the existing `failures` list.
    - The refusal happens pre-write, so zero `.typ` files reach disk.
    - The literal reserved key itself never reports a collision with itself.
    - The check is scoped to DECLARED keys, so it fires whether or not any `typst_documents` entry
      references the offending key.
  </behavior>

  <action>
    In `_validate_used_template_paths()`, before the per-used-key loop, add the reserved-key
    case-collision branch. Iterate `sorted(self._document_template_registry)` and, for each key that
    is NOT equal to `RESERVED_REGISTRY_KEY` under `==` but whose `self._collision_key(key)` equals
    `self._collision_key(RESERVED_REGISTRY_KEY)`, append a failure to the SAME `failures` list.
    Route the comparison exclusively through `_collision_key()` — D-07 forbids minting a second
    `.casefold()`/`.lower()` helper for this.

    The message must name the declared key, name the reserved built-in key, state that both resolve
    to the same bundle destination under the output-path folding this project applies on every
    platform, state that CONF-18 does not catch it because CONF-18 compares declared keys only
    against each other and never against the synthesized built-in key, and tell the user what to do
    (rename the declared key to something that does not fold to the reserved key, or drop it and
    configure the built-in key through the global Typst template settings). It must contain the
    exact fragment `differs from the built-in 'typst' registry key only by case`, which is the
    marker constant Task 1's gate module asserts.

    **Scope, stated deliberately and recorded in the docstring:** this sub-check iterates the whole
    resolved registry — every DECLARED key — not the reference-derived used-key set the two path
    checks use. That is not the widening CONTEXT.md's Claude's Discretion ruled against: that ruling
    is about running the PATH checks on declared-but-unreferenced keys, and it stands. This is a
    key-NAME collision whose sibling CONF-18 is already declaration-scoped, and the built-in key is
    always present in the registry, so a declared case variant is always wrong regardless of whether
    any entry references it — which is exactly what ROADMAP SC#4 asks for. Write that distinction
    into the docstring so a later reader does not collapse the two scopes.

    Update `_validate_used_template_paths()`'s docstring to record that the method now carries three
    failure kinds into one accumulated, `sorted()`-ordered raise, and name each kind's scope
    (reference-derived for the two path checks; declaration-derived for the key-name collision).
  </action>

  <verify>
    <automated>uv run pytest tests/test_reserved_key_case_prewrite_gate.py -x && uv run pytest -q 2>&1 | tail -20 && uv run black --check typsphinx/builder.py && uv run ruff check typsphinx/builder.py && uv run mypy typsphinx/</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_reserved_key_case_prewrite_gate.py` reports **2 passed, 0 failed**.
    - `awk '/def _validate_used_template_paths/,/^    def [a-z]/' typsphinx/builder.py | grep -c '_collision_key('` returns at least `2` — the comparison routes through the existing primitive on both sides.
    - `grep -c 'def _collision_key' typsphinx/builder.py` returns `1` and `grep -cE 'def .*(casefold|lower).*key' typsphinx/builder.py` returns `0` — no second case-folding primitive was minted.
    - `awk '/def _validate_used_template_paths/,/^    def [a-z]/' typsphinx/builder.py | grep -c 'RESERVED_REGISTRY_KEY'` returns at least `1`.
    - `uv run python -c "import subprocess,sys,tempfile,pathlib; d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','sphinx','-b','typst','tests/fixtures/reserved_key_case_prewrite_gate',d],capture_output=True,text=True); out=r.stdout+r.stderr; assert r.returncode!=0, out; assert \"differs from the built-in 'typst' registry key only by case\" in out, out; assert not list(pathlib.Path(d).rglob('*.typ')), list(pathlib.Path(d).rglob('*.typ')); print('OK')"` prints `OK`.
    - `uv run pytest -q` is green apart from exactly the 7 known `tests/test_state_guard_shapes_gate.py` failures.
    - `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all exit 0.
    - `git diff --stat` shows no change to `typsphinx/writer.py`, `typsphinx/template_engine.py`, `typsphinx/templates/base.typ` or `typsphinx/template_registry.py`.
  </acceptance_criteria>

  <done>
    A declared registry key that folds onto the reserved built-in key refuses the build pre-write
    with zero `.typ` files, routed through the single existing case-folding primitive, and the whole
    suite is green modulo the 7 known pre-existing failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer-authored `conf.py` → `TypstBuilder.write()` | `typst_template` and `typst_document_templates` keys cross into path arithmetic and output-path derivation. |
| `TypstBuilder.write()` → `<outdir>` | Every content and wrapper `.typ` file is written here. A guard that runs after this point cannot un-write what it finds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54.1-07 | Information Disclosure | the synthesized built-in `"typst"` registry key, whose CONF-17/A-01 violation is discovered only at `finish()` | high | mitigate | Hoist the A-01/CONF-17 check into `_validate_used_template_paths()` so a template whose parent directory is `srcdir` (or an ancestor of it) — i.e. a bundle that would republish the whole source tree — refuses before `prepare_writing()`, leaving zero written files. |
| T-54.1-08 | Tampering | `<outdir>/_template/<key>/` destination derivation for a declared key that folds onto the reserved key | medium | mitigate | Detect the fold-equal/`==`-unequal key pair pre-write through the single existing `_collision_key()` primitive, so two registry keys can never race for one bundle destination; declaration-scoped, matching CONF-18's own scope. |
| T-54.1-09 | Repudiation | the two deliberately duplicated CONF-17 guard sites drifting apart in wording | medium | mitigate | One shared `_conf17_violation_message()` builds the sentence for both sites; the existing text is reproduced byte-for-byte so no test assertion changes. |
| T-54.1-10 | Denial of Service | a partially-written `.typ` tree left behind by a late refusal | medium | mitigate | The pre-write position structurally guarantees zero written files: Sphinx's `Builder.build()` calls `write()` then `finish()` with no exception boundary between them, so a raise inside `write()` prevents `finish()` entirely. |
| T-54.1-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed in this phase (binding constraint #11); `54.1-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. |

**ASVS level 1, block on `high`.** T-54.1-07 is the one `high` item and is dispositioned `mitigate`
by Task 2. Canon breadcrumb: generic path-traversal and information-disclosure hardening is canon —
covered by `/gsd-secure-phase` and this register; not minted as a bespoke prohibition.
</threat_model>

## Artifacts this phase produces

Created by THIS plan (excluded from drift verification — none exists on the pre-fix tree):

- `typsphinx/builder.py` — new module-level function `_conf17_violation_message(key, resolved_path, srcdir)`
- `typsphinx/builder.py` — two new failure branches inside `TypstBuilder._validate_used_template_paths()`
- `tests/test_conf17_prewrite_gate.py`, `tests/test_reserved_key_case_prewrite_gate.py` — new test modules
- `CONF17_PREWRITE_MARKER`, `RESERVED_KEY_CASE_MARKER` — new module-level constants
- `tests/fixtures/conf17_prewrite_gate/`, `tests/fixtures/reserved_key_case_prewrite_gate/` — new fixture directories
- `.planning/phases/54.1-.../54.1-03-RED-EVIDENCE.md` — new evidence artifact

Created by sibling plans in this phase (also new, also excluded): `_paths_related()`,
`TypstBuilder._validate_used_template_paths()`, `tests/test_templates_path_collision_gate.py`,
`tests/test_docs_template_layout_gate.py`, `tests/fixtures/templates_path_collision_gate/`,
`tests/fixtures/templates_path_collision_adjacent_gate/`,
`tests/fixtures/templates_path_collision_multi_gate/`,
`tests/fixtures/templates_path_no_entries_gate/`,
`tests/fixtures/templates_path_no_template_gate/`,
`tests/fixtures/templates_path_default_documents_gate/`,
`tests/fixtures/mixed_prewrite_failures_gate/`, `examples/advanced/_typst/`,
`examples/charged-ieee/approach2/source/_typst/`, `54.1-01-RED-EVIDENCE.md`,
`54.1-02-RED-EVIDENCE.md`, `54.1-04-RED-EVIDENCE.md`, `54.1-05-EVIDENCE.md`.

<verification>
- `uv run pytest tests/test_conf17_prewrite_gate.py tests/test_reserved_key_case_prewrite_gate.py` — 5 passed.
- `uv run pytest -q` — green modulo exactly the 7 pre-existing `tests/test_state_guard_shapes_gate.py` failures.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` — exit 0.
- `54.1-03-RED-EVIDENCE.md` names the real pre-fix SHA and lists the surviving `.typ` files from the
  pre-fix CONF-17 run.
</verification>

<success_criteria>
- CR-01 is closed on both halves: a bare-filename global Typst template at the source root and a
  declared registry key colliding with the reserved key only by case are BOTH refused before any
  `.typ` file is written (ROADMAP SC#3 and SC#4).
- The hoisted check reaches the synthesized built-in key (D-05) and both guard sites share one
  message (D-06); the `finish()`-side guard is intact.
- The reserved-key comparison routes through the single existing case-folding primitive (D-07).
- Both gates were recorded RED against the pre-fix tree before the production change (binding
  constraint #6), with the surviving-`.typ`-file list captured.
- No new runtime dependency; `@preview` count unchanged; no typing-import modernization; WR-03,
  WR-04 and IN-01 untouched.
</success_criteria>

<output>
Create `.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-03-SUMMARY.md` when done.
</output>
