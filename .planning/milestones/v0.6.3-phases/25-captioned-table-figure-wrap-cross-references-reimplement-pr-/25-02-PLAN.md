---
phase: 25-captioned-table-figure-wrap-cross-references-reimplement-pr
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - tests/test_pdf_render_gate.py
  - tests/fixtures/captioned_table_render_gate/conf.py
  - tests/fixtures/captioned_table_render_gate/index.rst
autonomous: true
requirements: [TBL-01, TBL-02]

must_haves:
  truths:
    - "A real `sphinx-build -b typst` + `typst.compile()` of a 2-captioned-table document produces a valid %PDF in which EACH caption sentinel appears exactly once (SC#4 stale-buffer + no double-emission)."
    - "The compiled captioned-table PDF contains no rendered section heading above the tables, and the emitted index.typ contains `figure(` with `kind: table` (SC#1)."
    - "A captioned table that also sets `:width:` compiles green with both its caption sentinel and its width wrap present (SC#3)."
    - "A `:numref:`/`:ref:` to a captioned table compiles green: the emitted .typ contains `link(<` and no `link(\"\",`, with no dangling/duplicate-label fatal (SC#5, TBL-02, D-03)."
    - "A captioned `csv-table` and a captioned `list-table` each figure-wrap and keep their caption (D-05)."
    - { statement: "The gate is fail-pre-fix: a reconstructed double-anchored ids[0] Typst source makes typst.compile() raise, and a reconstructed stale-buffer basis fails the sentinel-count==1 check (durable red->green proof, D-06)", verification: backstop }
  artifacts:
    - tests/fixtures/captioned_table_render_gate/conf.py
    - tests/fixtures/captioned_table_render_gate/index.rst
    - tests/test_pdf_render_gate.py
  key_links:
    - "index.typ figure(...) <label> <-> the `:numref:`/`:ref:` link(<label>, ...): a real compile is the only place the double-anchor / dangling-label fatal (Critical Pitfall 3) is observable."
    - "sentinel-per-caption + full_text.count(...) == 1 <-> the 2nd-table stale-buffer bug: a single-table fixture cannot expose it (D-06)."
  prohibitions:
    - "MUST NOT assert on the TEXT of a Typst compiler error message — only that a real compile raises (test_package_only_config_gate D-06 convention)."
    - "MUST NOT assert a specific 'Table N' string equality between Sphinx numfig text and Typst's own numbering — assert the link RESOLVES (Pitfall 4, D-03)."
    - "MUST NOT wrap `typst.compile()` in try/except in the positive gate — a fatal must propagate and fail the test loudly."
    - "MUST NOT add a new runtime dependency or bump any @preview version (milestone invariant)."
---

<objective>
Ship the mandatory GATE-01 fail-pre-fix real `typst.compile()` regression fixture for the
captioned-table work: a self-contained Sphinx project with 2+ captioned tables (distinct
sentinels), a caption+`:width:` composition case, a `:numref:`/`:ref:`-resolves case
(`numfig = True`), and a captioned `csv-table` + `list-table` case (D-05), driven through
`sphinx-build -b typst` -> `typst.compile()` -> pypdf text-extraction, plus a durable
pre-fix-basis failure proof.

Purpose: The translator unit tests (Plan 25-01) never compile, so they cannot catch the
Critical Pitfall 3 duplicate-`<label>` compile fatal or the 2nd-table stale-buffer
caption-loss in the real pipeline. This plan is that real-compile bar (D-06). It runs in
Wave 2 because it proves the SHIPPED Plan 25-01 implementation compiles green.

Output: `tests/fixtures/captioned_table_render_gate/{conf.py,index.rst}` and
`TestCaptionedTableRenderGate` + `TestCaptionedTablePreFixBasisFailureProof` in
`tests/test_pdf_render_gate.py`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-captioned-table-figure-wrap-cross-references-reimplement-pr-/25-CONTEXT.md
@.planning/phases/25-captioned-table-figure-wrap-cross-references-reimplement-pr-/25-RESEARCH.md
@tests/test_pdf_render_gate.py
@.planning/phases/25-captioned-table-figure-wrap-cross-references-reimplement-pr-/25-01-SUMMARY.md

**Worktree execution (mandatory, per CLAUDE.md):** you run in an isolated worktree
(`.git` is a FILE). Provision first with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run every
command via `uv run`. The Wave-1 translator fix must already be merged into this worktree's
base — the positive gate depends on it compiling green.
</context>

<tasks>

<task type="auto">
  <name>Task 1: captioned_table_render_gate fixture + TestCaptionedTableRenderGate (real compile)</name>
  <files>tests/fixtures/captioned_table_render_gate/conf.py, tests/fixtures/captioned_table_render_gate/index.rst, tests/test_pdf_render_gate.py</files>
  <read_first>
    - `tests/test_pdf_render_gate.py` lines 262-395 (`TestFigureCaptionRenderGate` — the
      sentinel + `full_text.count(SENTINEL) == 1` + LEAK_SIGNATURES model), 598-685
      (`TestXrefRefidRenderGate` — the `link(<` present / `link("",` absent .typ-source
      assertions and the no-try/except real compile), 41 (`LEAK_SIGNATURES`), 135-172
      (`_run_sphinx_build_typst`), 974-1015 (the `@pytest.fixture(scope="class")`
      build-once-per-class pattern to model).
    - `tests/fixtures/table_width_render_gate/conf.py` and `index.rst` — the fixture layout
      template (project/author/release, `extensions = ["typsphinx"]`, `typst_documents`
      master tuple; all three table directive forms converging on nodes.table).
    - `tests/test_package_only_config_gate.py` lines 71-115 (class-scoped `build` fixture
      idiom + `TYPST_AVAILABLE` skip-guard style).
    - `.planning/phases/25-...-pr-/25-RESEARCH.md`: Validation Architecture (GATE-01 template
      correction), Open Questions #2 (csv/list "lighter caption-regression" wording), and
      Pitfall 4 (`numfig = True`; assert the link RESOLVES, not a specific "Table N" string).
  </read_first>
  <action>
    Create `tests/fixtures/captioned_table_render_gate/conf.py`: `project`/`author`/`release`,
    `extensions = ["typsphinx"]`, a `typst_documents` master tuple for `index`, and
    `numfig = True` (Pitfall 4 — cleanest warning-free `:numref:` text). Create
    `tests/fixtures/captioned_table_render_gate/index.rst` with distinct ALLCAPS-no-space
    sentinel captions:
    (a) two `.. table::` directives, each with a caption AND `:name:`, sentinels
    `TBLCAPFIRSTSENTINEL` (name `first-table`) and `TBLCAPSECONDSENTINEL` (name
    `second-table`) — the 2nd exposes the stale-buffer bug;
    (b) one `.. table::` with BOTH `:width:` and a caption+`:name:`, sentinel
    `TBLCAPWIDTHSENTINEL` (name `width-table`);
    (c) a paragraph using `` :numref:`first-table` `` and `` :ref:`first-table` `` (proves
    resolution);
    (d) one captioned `.. csv-table::`, sentinel `TBLCAPCSVSENTINEL`;
    (e) one captioned `.. list-table::`, sentinel `TBLCAPLISTSENTINEL`.

    In `tests/test_pdf_render_gate.py`: add a `captioned_table_render_gate_dir` fixture
    beside the existing `*_render_gate_dir` fixtures (~line 124), and a
    `@pytest.fixture(scope="class")` `captioned_table_render_gate_artifacts` that runs
    `_run_sphinx_build_typst` once, reads the emitted `index.typ` text, runs
    `typst.compile()` once (NO try/except — a fatal must propagate), and returns both the
    `.typ` source and the pypdf-extracted PDF text (model on the class-scoped
    `topic_line_block_render_gate_pdf_text` fixture at line 974). Add class
    `TestCaptionedTableRenderGate` (marked `@pytest.mark.slow`, skipif not
    `TYPST_AVAILABLE and PYPDF_AVAILABLE`) with thin methods asserting, against that one
    artifact: each of the five caption sentinels appears exactly once in the extracted text
    (`full_text.count(...) == 1`); the emitted `.typ` contains `figure(` and `kind: table`
    and shows no `heading(` in the captioned-table region; `link(<` is present and
    `link("",` is absent in the `.typ` source (numref/ref resolution, mirroring
    `TestXrefRefidRenderGate`); every `LEAK_SIGNATURES` token is absent from the extracted
    text; and the compiled file is a valid `%PDF`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pdf_render_gate.py -k CaptionedTableRenderGate -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pdf_render_gate.py -k CaptionedTableRenderGate -x` exits 0
      with the shipped Plan 25-01 implementation.
    - Each of the five caption sentinels (`TBLCAPFIRSTSENTINEL`, `TBLCAPSECONDSENTINEL`,
      `TBLCAPWIDTHSENTINEL`, `TBLCAPCSVSENTINEL`, `TBLCAPLISTSENTINEL`) has
      `full_text.count(...) == 1` — proving the 2nd-table caption is NOT lost.
    - The emitted `index.typ` contains `figure(` and `kind: table`; `link(<` is present and
      `link("",` is absent.
    - `typst.compile()` produces a valid `%PDF` with no duplicate/dangling-label fatal (the
      `:name:`-tagged tables prove the Critical Pitfall 3 fix in a real compile).
  </acceptance_criteria>
  <done>
    A real-compile GATE-01 fixture proves the full captioned-table pipeline (2+-table,
    caption+width, `:numref:`-resolves, csv/list) renders green with each caption present
    exactly once and every cross-reference resolving.
  </done>
</task>

<task type="auto">
  <name>Task 2: TestCaptionedTablePreFixBasisFailureProof (durable fail-pre-fix proof)</name>
  <files>tests/test_pdf_render_gate.py</files>
  <read_first>
    - `tests/test_package_only_config_gate.py` line 302 (`TestPreFixBasisFailureProof` — the
      reconstruction convention: rebuild the pre-fix basis from first principles and assert
      it fails, keeping the proof durable after the buggy code is deleted; never assert on
      the TEXT of a compiler error).
    - `tests/test_nested_master_render_gate.py` lines 72-81 and 376-413 (the two-half
      pre-fix-basis reproduction convention).
    - `.planning/phases/25-...-pr-/25-RESEARCH.md`: Verified Mechanism 2 (the pre-fix
      stale-buffer output where "Second Caption" is absent) and Critical Pitfall 3 (the
      double-anchor `label ... occurs multiple times` fatal).
  </read_first>
  <action>
    Add class `TestCaptionedTablePreFixBasisFailureProof` (marked `@pytest.mark.slow`, skipif
    not `TYPST_AVAILABLE`) reconstructing the two pre-fix defect bases from first principles
    (the buggy code no longer exists post Plan 25-01):
    (a) Double-anchor basis (Critical Pitfall 3): assemble a minimal Typst source string in
    which one label id is defined TWICE — once as a `[#metadata(none) <dup>]` anchor and once
    as a `figure(...) <dup>` postfix — write it to a temp `.typ`, and assert `typst.compile()`
    RAISES (the duplicate-label fatal the deferred `skip_ids` anchor prevents). Do not match
    on the error text — assert only that it raises (use `pytest.raises(Exception)` or the
    project's `TypstCompilationError`).
    (b) Stale-buffer basis (Verified Mechanism 2): assemble a reconstructed pre-fix two-table
    `.typ` (a plain `table(...)` for each, a stray `heading(...)` above the first, and the
    SECOND caption sentinel deliberately ABSENT — the observed pre-fix shape), and assert a
    `count(sentinel) == 1` check FAILS for the second sentinel (i.e. count is 0) — documenting
    that the gate's exact-once assertion is genuinely fail-pre-fix.
    Add a module comment tying both halves to Verified Mechanism 2 / Critical Pitfall 3 and to
    D-06's red->green requirement.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pdf_render_gate.py -k "PreFixBasisFailureProof and Captioned" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pdf_render_gate.py -k "PreFixBasisFailureProof and Captioned" -x`
      exits 0.
    - The reconstructed double-anchor Typst source makes `typst.compile()` raise (asserted via
      `pytest.raises`, with no assertion on the error message text).
    - The reconstructed stale-buffer basis has its second caption sentinel count == 0,
      demonstrating the positive gate's `== 1` assertion is fail-pre-fix.
    - `uv run pytest tests/test_pdf_render_gate.py -x` is fully green (positive gate + pre-fix
      proof), and the full suite `uv run pytest -q` is green.
  </acceptance_criteria>
  <done>
    The durable fail-pre-fix red->green proof is committed alongside the positive gate,
    matching the repo's `TestPreFixBasisFailureProof` convention; the whole suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture .rst -> Typst source -> compiled PDF | Build-time test artifact only; no runtime, no network, no untrusted input. Introduces no production code — this plan adds only test fixtures and assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-02 | Denial of Service (build-time) | duplicate/dangling `<label>` aborting the whole-document compile | medium | mitigate | This plan IS the mitigation's proof: the positive gate compiles `:name:`-tagged captioned tables green (no duplicate-label fatal), and the pre-fix-basis proof demonstrates the fatal existed pre-fix. |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | No installs: typst-py and pypdf are already pinned/installed project dependencies reused by the existing render-gate suite (RESEARCH Environment Availability). |

**Why no high-severity threats (block_on=high, ASVS L1):** this plan is test-only. It adds
no production code, no new input surface, no network/auth/secrets. Its sole security-relevant
role is to independently PROVE the T-25-02 build-time-DoS mitigation from Plan 25-01 holds in
a real compile.
</threat_model>

<verification>
- `uv run pytest tests/test_pdf_render_gate.py -k Captioned -x` green (positive gate +
  pre-fix proof).
- `uv run pytest -q` full suite green.
- `uv run ruff check tests/test_pdf_render_gate.py` and
  `uv run black --check tests/` clean.
- `templates/base.typ` untouched (grep-confirm no diff); no new runtime dep; no @preview
  version change.
</verification>

<success_criteria>
- D-06 satisfied: a real `typst.compile()` GATE-01 fixture exercises a 2+-table document, a
  caption+`:width:` case, a `:numref:`/`:ref:`-resolves case, and a csv-table + list-table
  case, with a durable fail-pre-fix red->green proof.
- Each caption sentinel appears exactly once; every cross-reference resolves; the compile
  produces a valid %PDF with no duplicate/dangling-label fatal.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/25-captioned-table-figure-wrap-cross-references-reimplement-pr-/25-02-SUMMARY.md` when done.
</output>
