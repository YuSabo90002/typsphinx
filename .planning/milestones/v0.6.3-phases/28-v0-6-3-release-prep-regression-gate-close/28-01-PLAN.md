---
phase: 28-v0-6-3-release-prep-regression-gate-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "pyproject.toml [project].version reads exactly 0.6.3 and remains the sole version literal in the file (SC#1)"
    - "uv.lock's typsphinx self-entry reads version = \"0.6.3\" and `uv sync --extra dev --locked` exits 0 (SC#1)"
    - "README.md's Status line reads `**Status**: Stable (v0.6.3) - Production ready`, and it is the ONLY changed line in README.md (SC#1)"
    - "The installed editable dist metadata was refreshed, so `typsphinx.__version__` reports 0.6.3 and tests/test_extension.py::test_version_matches_pyproject_toml stays green rather than reporting the stale 0.6.2 (SC#1)"
    - "tests/test_readme_version_sync.py passes — README's Status version and pyproject.toml's version were moved in lockstep, never leaving the suite RED between two separate commits (SC#1)"
    - "The uv.lock diff contains no change to any direct dependency specifier (sphinx / docutils / typst); a change there is a stop condition, not an accepted drift (SC#4)"
    - "tests/test_preview_version_sync.py stays green — this plan does not touch the @preview 3-way version-sync surface (SC#4)"
    - statement: "Any incidental transitive resolution change in uv.lock is recorded verbatim in the SUMMARY rather than silently accepted, so a reviewer can tell an intended one-line self-entry bump from an upstream drift that rode along"
      verification: backstop
  artifacts:
    - "pyproject.toml — [project].version literal at line 7 bumped to 0.6.3"
    - "uv.lock — regenerated; typsphinx self-entry at ~line 1379 bumped to 0.6.3"
    - "README.md — Status line at :315 bumped to Stable (v0.6.3)"
    - "28-01-SUMMARY.md — carries the verbatim `git diff --stat -- uv.lock` and a classification of every non-self-entry lockfile change"
  key_links:
    - "README.md:315 Status line ↔ pyproject.toml [project].version — bound by tests/test_readme_version_sync.py (added Phase 23 D-13); the two MUST move in the same edit or the suite is RED in between"
    - "pyproject.toml [project].version ↔ uv.lock typsphinx self-entry — NOT auto-derived; requires an explicit `uv lock` run or `uv sync --locked` reports drift"
    - "pyproject.toml [project].version ↔ typsphinx.__version__ — typsphinx/__init__.py:20 reads importlib.metadata.version(\"typsphinx\"), i.e. the INSTALLED editable dist metadata; it keeps reporting the old version until the editable install is rebuilt by `uv sync`"
  prohibitions:
    - statement: "No git tag named v0.6.3 (or any other tag) is created or pushed by this plan"
      status: required
      verification: "`git tag --list 'v0.6.3'` returns empty"
    - statement: "No PyPI upload, no GitHub Release creation, no manual or scripted trigger of .github/workflows/release.yml"
      status: required
      verification: "`git status --porcelain .github/workflows/release.yml` is empty; no `gh release`, `twine`, `uv publish`, or `git push --tags` command is run"
    - statement: "No behavior change under typsphinx/ — this plan touches no package source file and no test file"
      status: required
      verification: "`git diff --name-only` for this plan's commits lists no path under typsphinx/ and no path under tests/"
    - statement: "No modernization of typing imports and no change to the [tool.ruff] UP006/UP035 ignores while pyproject.toml is open"
      status: required
      verification: "`git diff -- pyproject.toml` shows no change to the [tool.ruff] ignore list"
    - statement: "No change to tox.ini's deliberate `tox-uv~=1.35` pin"
      status: required
      verification: "`git status --porcelain tox.ini` is empty"
    - statement: "No change to the README dependency-floor footer line or to the README github.io documentation links"
      status: required
      verification: "`git diff --numstat -- README.md` reads exactly `1\t1\tREADME.md`"
---

<objective>
typsphinx を 0.6.2 → 0.6.3 にバンプする。版リテラルを持つ 3 つの宣言面（`pyproject.toml` / `uv.lock` /
`README.md`）を **1 つのタスクで同時に**動かし、editable インストールのメタデータも作り直して、
版同期を守る既存テスト 3 本が緑のまま通り抜けることを証明する。

Purpose: ROADMAP SC#1 を満たす。`pyproject.toml:7` は実測上このリポジトリで唯一の版リテラルであり
（`typsphinx/__init__.py` は `importlib.metadata` 由来の動的取得）、`README.md:315` は Phase 23 D-13 で
新設した `tests/test_readme_version_sync.py` によって pyproject と機械的に結ばれている。片方だけ
動かすとスイートが赤くなる — だからこの 2 ファイルは分割不可の 1 単位である。

Output: `pyproject.toml` が 0.6.3、`uv.lock` が再生成済み、`README.md:315` が `Stable (v0.6.3)`、
`uv sync --extra dev --locked` が exit 0。

`requirements: []` は正しく意図的である。Phase 28 は要件 ID を持たない release/close フェーズであり
（`.planning/REQUIREMENTS.md:65` が「Phase 28 is a prep-only release/close phase and carries no
requirement」と明記）、ROADMAP は `**Requirements**: none (release/close phase — carries no requirement)`
と記録している。本フェーズの契約は ROADMAP SC#1〜SC#5 と `28-CONTEXT.md` の D-01〜D-12 であり、
`must_haves.truths` はそちらを引用している。
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-CONTEXT.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-RESEARCH.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-PATTERNS.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 28-01 share)

このフェーズは **Python シンボル・CLI フラグ・config キーを一切作らない**。`typsphinx/` 配下は一行も
変更しない。生成物はすべてリリース成果物（メタデータ・散文・証跡）である。

| Artifact | Kind | 定義される識別子・変更点 |
|---|---|---|
| `pyproject.toml` | modified | `[project].version` → `0.6.3`（ファイル内唯一の `^version = ` 行） |
| `uv.lock` | regenerated | `typsphinx` セルフエントリの `version` → `0.6.3`（~1379 行目） |
| `README.md` | modified | `:315` の Status 行 → `**Status**: Stable (v0.6.3) - Production ready` |

姉妹プランが作る成果物（網羅性のため列挙）:

- `28-VERIFICATION.md`（新規ファイル、28-02）— 回帰ゲート・フルスイート・docs ビルド・SC#4 diff の証跡記録
- `CHANGELOG.md` の `## [0.6.3]` 見出しとその節見出し `### Added` / `### Changed` / `### Removed` /
  `### Fixed` / `### Verified`、新規リンク参照行 `[0.6.3]:`、繰り上げ後の `[Unreleased]:` compare
  ターゲット（28-03）

<specless_probe_note>
spec-less probe fallback: **visible skip（記録済みの選択）**。`workflow.specless_probe_fallback` は既定 ON
で `28-SPEC.md` も存在しないため `EDGE_ABSENT` / `PROHIB_ABSENT` の両方が立つが、プローブは**要件テキスト**
から述語を導出する仕組みであり、本フェーズは要件 ID をゼロ個しか持たない（`.planning/REQUIREMENTS.md:65`）。
これは `references/specless-probe-fallback.md` §0 の nothing-to-probe ガードそのものであり、プローブを
回すために要件テキストを捏造することは同リファレンスの fail-loud placeholder ガードで禁止されている。
したがって**本実行ではプローブ由来の edge / prohibition 述語を一切生成していない**。上の
`must_haves.prohibitions` はプローブ由来ではなく、ROADMAP SC#5 のスコープ柵と `28-CONTEXT.md` の
Out of scope 節から直接書き起こしたものである。`must_haves.truths` は phase goal + SC#1〜SC#5 +
D-01〜D-12 から導出している。
</specless_probe_note>

<tasks>

<task type="auto">
  <name>Task 1: 版リテラルを pyproject.toml / README.md / uv.lock で同時にバンプし、editable メタデータを作り直す</name>
  <files>pyproject.toml, README.md, uv.lock</files>
  <read_first>
    - `pyproject.toml`（`[project]` テーブル全体 — `version = "0.6.2"` が 7 行目にあり、ファイル内の
      `^version = ` 行がこれ 1 本だけであることを確認する。`[tool.ruff]` の `target-version` と
      `[tool.mypy]` の `python_version` は版リテラルではない別物）
    - `README.md` の 305〜318 行（Version History 節と末尾 2 行のフッタブロック。`:315` が Status 行、
      `:316` が `**Python**: 3.12+ | **Sphinx**: 9.1+ | **Typst**: 0.15+` の依存下限フッタで、こちらは
      変更禁止）
    - `uv.lock` の 1375〜1440 行（`[[package]] name = "typsphinx"` セルフエントリの `version` フィールド、
      `dependencies` リスト、`[package.metadata]` の `requires-dist` ブロック）
    - `typsphinx/__init__.py` の 14〜22 行（`__version__ = importlib.metadata.version("typsphinx")` —
      インストール済み dist メタデータ由来であることの一次証拠。読むだけ、変更しない）
    - `tests/test_readme_version_sync.py`（全文 — README の Status 行と pyproject の version を
      互いに突き合わせる Phase 23 D-13 のガード）
    - `tests/test_extension.py` の 78〜95 行（`test_version_matches_pyproject_toml` — `typsphinx.__version__`
      と pyproject を突き合わせる。editable メタデータが古いままだとここが赤くなる）
    - `CLAUDE.md` § "Worktree-isolated execution"（このプロジェクトの必須プロビジョニング規約）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-RESEARCH.md` の
      § "`uv.lock` Regeneration"（セルフエントリが pyproject 編集に自動追随しない理由）と
      § "Common Pitfalls" の Pitfall 2 / Pitfall 5
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-PATTERNS.md` の
      §§ `pyproject.toml` / `uv.lock` / `README.md`
  </read_first>
  <action>
    まずワークツリーをプロビジョニングする。他のどのコマンドより先に
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` を実行し、以後このプランの
    すべてのコマンドを `uv run` 経由で走らせる。CLAUDE.md 上これは並列度に依存しない必須手順である。

    そのうえで、次の 4 つを**同一タスク内で**行う。1 と 2 の間にコミットを挟まないこと —
    `tests/test_readme_version_sync.py` が両者の一致を assert するため、片方だけ動いた状態を
    コミットするとその時点でスイートが赤くなる。

    1. `pyproject.toml` の 7 行目、`version` の値を `0.6.2` から `0.6.3` に変える。ファイル内の他は
       一切変えない。特に `requires-python = ">=3.12"`、`dependencies` 配列（`sphinx>=9.1,<10` /
       `docutils>=0.21,<0.23` / `typst>=0.15.0,<0.16`）、`[project.optional-dependencies]` の `dev` /
       `docs` リスト、`classifiers`、`[tool.ruff]` の `ignore` リスト（`UP006` / `UP035` の項目は
       `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` に
       記録された意図的な先送りであり、CLAUDE.md もこの todo が着地するまで typing imports を
       modernize しないと明記している）、`[tool.pytest.ini_options]` はバイト同一で残す。

    2. `README.md` の 315 行目を `**Status**: Stable (v0.6.3) - Production ready` にする。1 語だけの
       変更である。直下 316 行目の依存下限フッタ（`**Python**: 3.12+ | **Sphinx**: 9.1+ |
       **Typst**: 0.15+`）は触らない — README の依存下限同期テストは Phase 23 D-14 で明示的に
       スコープ外と裁定済みで、`28-CONTEXT.md` の Deferred にもそのまま残っている。README の
       github.io ドキュメントリンクにも触らない（RTD 移行に畳み込み済みのオーナー裁定）。

    3. ロックファイルを再生成する。`uv lock` を使う（素の `uv sync` ではない）。`uv lock` は
       `uv.lock` だけを書き換える最小の単目的コマンドで、「ロックが再生成されたか」と「venv に
       入ったか」を分離できる。続けて `uv sync --extra dev --locked` を実行する — これが SC#1 の
       受入基準そのもので、ドリフトが残っていれば黙って再ロックせず失敗で知らせる。`uv.lock` を
       手編集してはいけない。実測済みの落とし穴: `pyproject.toml` をバンプしても `uv.lock` の
       `typsphinx` セルフエントリ（~1379 行目）は自動追随しない。`uv lock` の明示実行が要る。

    4. インストール済み editable dist のメタデータを作り直す。`typsphinx/__init__.py:20` は
       `importlib.metadata.version("typsphinx")` から `__version__` を得ており、これは
       **インストール済み dist メタデータ**を読むため、editable インストールを作り直すまで古い版を
       名乗り続ける（実測: 現状 `0.6.2` を返す）。`uv run python -c "import typsphinx;
       print(typsphinx.__version__)"` が `0.6.3` を印字することを確認し、まだ `0.6.2` のままなら
       `uv sync --extra dev --locked --reinstall-package typsphinx` を実行して再確認する。ここを
       飛ばすと `tests/test_extension.py::test_version_matches_pyproject_toml` が赤くなる。

    最後に版同期の 3 本を回す:
    `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py
    tests/test_extension.py::test_version_matches_pyproject_toml -v`。

    スコープ柵（ROADMAP SC#5、絶対）: git タグを作らない・push しない。`uv publish` / `twine` /
    `gh release create` / `git push --tags` を実行しない。`.github/workflows/release.yml` を編集も
    起動もしない。publish 系はすべて `/gsd-complete-milestone` の仕事である。
  </action>
  <acceptance_criteria>
    - `uv run python -c "import tomllib;print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])"` が正確に `0.6.3` を印字する
    - `grep -c '^version = ' pyproject.toml` が `1` を返す（版リテラルは依然として 1 本だけ）
    - `grep -c '^version = "0.6.3"$' pyproject.toml` が `1` を返す
    - `grep -c '^\*\*Status\*\*: Stable (v0.6.3) - Production ready$' README.md` が `1` を返す
    - `grep -c '^\*\*Python\*\*: 3.12+ | \*\*Sphinx\*\*: 9.1+ | \*\*Typst\*\*: 0.15+$' README.md` が `1` を返す（依存下限フッタは無傷）
    - `git diff --numstat -- README.md` が正確に `1<TAB>1<TAB>README.md` を返す（変更行は 1 行だけ）
    - `git diff --numstat -- pyproject.toml` が正確に `1<TAB>1<TAB>pyproject.toml` を返す
    - `uv.lock` 内で `name = "typsphinx"` の直後の行が `version = "0.6.3"` である
    - `uv sync --extra dev --locked` が exit 0
    - `uv run python -c "import typsphinx;print(typsphinx.__version__)"` が `0.6.3` を印字する
    - `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py tests/test_extension.py::test_version_matches_pyproject_toml -v` が exit 0（3 passed）
    - `git status --porcelain tox.ini` が空
    - `git status --porcelain .github/workflows/release.yml` が空
    - `git tag --list 'v0.6.3'` が空
    - `git status --porcelain typsphinx/ tests/` が空（ソースもテストも触っていない）
  </acceptance_criteria>
  <verify>
    <automated>uv sync --extra dev --locked &amp;&amp; test "$(git tag --list 'v0.6.3')" = "" &amp;&amp; test "$(git status --porcelain typsphinx/ tests/ tox.ini)" = "" &amp;&amp; uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py tests/test_extension.py::test_version_matches_pyproject_toml -q &amp;&amp; uv run python -c "import tomllib,pathlib,sys,typsphinx;v=tomllib.load(open('pyproject.toml','rb'))['project']['version'];p=pathlib.Path('pyproject.toml').read_text(encoding='utf-8');r=pathlib.Path('README.md').read_text(encoding='utf-8');lock=pathlib.Path('uv.lock').read_text(encoding='utf-8');e=[];e.append('pyproject='+v) if v!='0.6.3' else None;e.append('multiple-version-literals') if sum(1 for L in p.splitlines() if L.startswith('version = '))!=1 else None;e.append('readme-status') if '**Status**: Stable (v0.6.3) - Production ready' not in r else None;e.append('readme-footer-changed') if '**Python**: 3.12+ | **Sphinx**: 9.1+ | **Typst**: 0.15+' not in r else None;e.append('uvlock-self-entry') if 'name = \"typsphinx\"\nversion = \"0.6.3\"' not in lock else None;e.append('dist-metadata='+typsphinx.__version__) if typsphinx.__version__!='0.6.3' else None;print('ERRORS:',e);sys.exit(1 if e else 0)"</automated>
  </verify>
  <done>
    `pyproject.toml` が 0.6.3（唯一の版リテラル）、`README.md:315` が `Stable (v0.6.3) - Production ready`
    で 316 行目は無傷、`uv.lock` のセルフエントリが 0.6.3、`uv sync --extra dev --locked` が exit 0、
    `typsphinx.__version__` が 0.6.3 を返し、版同期テスト 3 本が緑。タグは存在せず、publish コマンドは
    一切実行されておらず、`typsphinx/` と `tests/` の下は無変更。
  </done>
</task>

<task type="auto">
  <name>Task 2: uv.lock の diff 形状を分類し、直接依存レンジの変化がないことを証明する</name>
  <files>uv.lock</files>
  <read_first>
    - `git diff -- uv.lock` の実出力（このタスクで自分で生成する。read_first の意味は
      「思い込みで判定せず必ず実物を読め」である）
    - `uv.lock` の `[package.metadata]` `requires-dist` ブロック（~1414 行目以降 —
      `{ name = "sphinx", specifier = ">=9.1,<10" }` のように**直接依存のレンジ指定**が
      ここに文字列として現れる。SC#4 違反の検出面はこの行たち）
    - `pyproject.toml` の `dependencies` 配列（比較対象の正）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-RESEARCH.md` の
      § "期待される diff の形"（許容される差分と停止条件の切り分け）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-PATTERNS.md` § `uv.lock`
  </read_first>
  <action>
    `git diff -- uv.lock` と `git diff --stat -- uv.lock` を実行し、出力を全文読む。期待される形は
    `typsphinx` セルフエントリの `version` が `0.6.2` から `0.6.3` へ動く 1 対の行と、あればロック
    ファイル自身の `revision` メタデータカウンタの化粧的な増分だけである（実測: 本フェーズ着手時点で
    `git diff main..HEAD -- uv.lock` は完全に空、つまりクリーンな状態からの版バンプになる）。

    差分に現れたものを次の 3 分類に振り分け、SUMMARY に書く:

    - **セルフエントリの版更新と `revision` カウンタ** — 期待どおり。追加の説明は不要。
    - **transitive（間接）依存の解決版の変化** — 既存レンジ内に上流のパッチリリースが現れた場合に
      起こりうる偶発的なドリフト。SC#4 は typsphinx 自身が宣言したランタイム依存と `@preview` の版だけを
      問題にするので、これはブロッキングではない。ただし黙って受け入れず、SUMMARY に
      `## uv.lock diff` という見出しを立てて `git diff --stat -- uv.lock` の出力をそのまま貼り、
      変化したパッケージ名と before/after の版を列挙する。
    - **直接依存のレンジ指定の変化** — `requires-dist` の `{ name = "sphinx" | "docutils" | "typst",
      specifier = ... }` 行が差分に現れた場合。**これは停止条件である。** SC#4 違反であり、意図しない
      編集が `pyproject.toml` に届いたことを意味する。作業を止めて `pyproject.toml` の `dependencies`
      配列を読み直し、原因を SUMMARY に記録したうえでオーケストレータに報告する。

    最後に `uv sync --extra dev --locked` をもう一度実行して冪等に exit 0 になることを確認し、
    サンプリング用のクイックランとして
    `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v`
    を回す（`28-VALIDATION.md` § Sampling Rate が定めるタスクコミットごとの計測点、サブ秒）。

    スコープ柵（ROADMAP SC#5）: 依然としてタグなし・publish なし・GitHub Release なし・`release.yml`
    への一切の関与なし。
  </action>
  <acceptance_criteria>
    - `git diff -- uv.lock | grep -E '^[+-]' | grep -Ec 'name = "(sphinx|docutils|typst)", specifier'` が `0` を返す（直接依存レンジは無変化）
    - `git diff -- uv.lock | grep -c '^+version = "0.6.3"'` が `1` を返す
    - `git diff -- uv.lock | grep -c '^-version = "0.6.2"'` が `1` を返す
    - `uv sync --extra dev --locked` が exit 0（冪等再確認）
    - `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v` が exit 0（2 passed）
    - `28-01-SUMMARY.md` に `## uv.lock diff` という見出しがあり、その下に `git diff --stat -- uv.lock` の出力が逐語で貼られている
    - `28-01-SUMMARY.md` が、セルフエントリ以外の差分について「なし」か、または変化したパッケージ名と before/after 版の列挙のいずれかを明示的に述べている（沈黙は不可）
    - `git status --porcelain pyproject.toml` が示す変更は Task 1 のものと同一で、このタスクによる追加変更がない
    - `git tag --list 'v0.6.3'` が空
  </acceptance_criteria>
  <verify>
    <automated>test "$(git diff -- uv.lock | grep -E '^[+-]' | grep -Ec 'name = "(sphinx|docutils|typst)", specifier')" = "0" &amp;&amp; test "$(git diff -- uv.lock | grep -c '^+version = "0.6.3"')" = "1" &amp;&amp; uv sync --extra dev --locked &amp;&amp; uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q &amp;&amp; test "$(git tag --list 'v0.6.3')" = "" &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <done>
    `uv.lock` の差分がセルフエントリの版更新（＋あれば `revision` カウンタ）に限定されていることが
    実出力で確認され、直接依存のレンジ指定に変化がないことが grep で証明され、`uv sync --extra dev
    --locked` が冪等に exit 0 し、`28-01-SUMMARY.md` に `## uv.lock diff` 見出しと逐語の
    `git diff --stat` 出力、および非セルフエントリ差分の明示的な分類が記録されている。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| リポジトリ作業ツリー → 公開リリース成果物 | ここで書く版メタデータは `/gsd-complete-milestone` → `release.yml` → PyPI へ無改変で流れる。誤った値・不整合な値はここで確定し、公開後は不変の事実になる |
| PyPI インデックス → `uv lock` リゾルバ | `uv lock` はリモートインデックスに対して再解決し、その結果が出荷されるロックファイルに書き込まれる |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | `pyproject.toml` / `uv.lock` / `README.md` / インストール済み dist メタデータ の版リテラル | medium | mitigate | Task 1 が 4 面すべてを 1 タスクで動かす。`tests/test_readme_version_sync.py` が README↔pyproject の乖離を、`tests/test_extension.py::test_version_matches_pyproject_toml` が dist メタデータ↔pyproject の乖離を、`uv sync --extra dev --locked` が lock↔pyproject の乖離を、それぞれ硬い失敗にする |
| T-28-02 | Elevation of Privilege | `.github/workflows/release.yml`（タグ起動の PyPI publish） | high | mitigate | ROADMAP SC#5 のスコープ柵を `must_haves.prohibitions` に符号化し、両タスクの action で再掲。`git tag --list 'v0.6.3'` が空であること、`git status --porcelain .github/workflows/release.yml` が空であることで assert する |
| T-28-03 | Tampering | `uv.lock` の transitive 解決 | low | mitigate | Task 2 が `git diff -- uv.lock` の全出力を 3 分類に振り分け、SUMMARY に逐語記録する。直接依存レンジの変化（`requires-dist` の `specifier` 行）は grep で 0 件であることを証明する停止条件。直接依存は `pyproject.toml` の `>=X,<Y` レンジピンで保護されており、本プランはそのレンジを変更しない。これが `28-RESEARCH.md` § Security Domain が唯一挙げた低関連度パターン（`uv lock` によるピン外バンプの混入）への対応であり、`high` のブロック閾値を下回る |
| T-28-SC | Tampering | npm/pip/cargo 相当のパッケージインストール | low | accept | 本フェーズはパッケージを一切新規インストールしない。`28-RESEARCH.md` § "Package Legitimacy Audit" が「該当なし — 新規ランタイム依存・開発依存の追加はゼロ」と記録済み（`git diff main..HEAD -- pyproject.toml` が空であることを実測確認）。`uv lock` / `uv sync` は既に宣言済み・レンジピン済みの依存集合を再解決するだけ |

**Security posture（`28-RESEARCH.md` § Security Domain の所見をそのまま記録）:** 本フェーズには
評価すべき攻撃面の変化がない。触るのは版リテラルの文字列・CHANGELOG の markdown・README の 1 行・
ロックファイルの再生成のみで、ユーザー入力なし、新規ネットワーク呼び出しなし、
認証/セッション/アクセス制御面なし、暗号なし、新規テストコードなし。

| ASVS Category | Applies | Standard Control |
|---------------|---------|-------------------|
| V2 Authentication | No | N/A — 認証面に触れない |
| V3 Session Management | No | N/A |
| V4 Access Control | No | N/A |
| V5 Input Validation | No（リポジトリローカルの固定パスファイルを読むだけで、ユーザー/ネットワーク入力ではない） | N/A |
| V6 Cryptography | No | N/A |
</threat_model>

<verification>
- `uv sync --extra dev --locked` が exit 0
- `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py tests/test_extension.py::test_version_matches_pyproject_toml -v` → 3 passed
- `uv run python -c "import typsphinx;print(typsphinx.__version__)"` → `0.6.3`
- `git diff --numstat -- README.md pyproject.toml` が各 1 行のみの変更を示す
- `git tag --list 'v0.6.3'` が空
- `git status --porcelain typsphinx/ tests/ docs/ examples/ tox.ini` が空
</verification>

<success_criteria>
- SC#1 の版バンプ半分が達成: `pyproject.toml` が 0.6.3（唯一の版リテラル）、`uv.lock` が lockstep で
  再生成、`uv sync --locked` が緑、`README.md` の Status 行が v0.6.3
- SC#4 の前提が保たれている: 直接依存レンジ無変化、`tests/test_preview_version_sync.py` 緑
- SC#5 が保持: タグなし・publish なし・Release なし・`release.yml` への関与なし
- D-04 のファイル柵が保持: 本プランが触るのは `pyproject.toml` / `uv.lock` / `README.md` の 3 つのみ
</success_criteria>

<output>
`.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-01-SUMMARY.md` を作成する。
`## uv.lock diff` 見出しの下に `git diff --stat -- uv.lock` の逐語出力と、セルフエントリ以外の
差分の明示的な分類（「なし」も明示すること）を含めること。
</output>
