---
phase: 28-v0-6-3-release-prep-regression-gate-close
plan: 03
type: execute
wave: 3
depends_on:
  - 28-01
  - 28-02
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "CHANGELOG.md carries a `## [0.6.3] - YYYY-MM-DD` heading dated with the actual execution date, inserted between the `## [Unreleased]` heading and the `## [0.6.2] - 2026-07-23` heading, with `## [Unreleased]` retained above it per Keep a Changelog (SC#2)"
    - "The entry contains exactly 5 top-level change bullets: one in `### Added`, one in `### Changed`, one in `### Removed`, two in `### Fixed` — the user-visible bundling D-09 specifies, not a 1:1 enumeration of the requirement ledger (SC#2, D-09)"
    - "Six of the seven v1 ledger IDs (CONF-04, CONF-05, CONF-07, TBL-01, TBL-02, DOC-07) each appear as a trailing parenthetical citation on some bullet, with zero silent drops (SC#2, D-09)"
    - "The seventh ledger ID, the orphan-doc deletion item, is deliberately absent because it was never reachable from any toctree and so was never visible to a user — a recorded choice, not an oversight, and a reviewer must not add it back for completeness (D-10)"
    - "TBL-01 and TBL-02 are bundled into a single `### Added` bullet about captioned tables becoming numbered, cross-referenceable figures (D-09)"
    - "The CONF-04 unknown-key fail-loud bullet lives in `### Changed` and carries a bullet-leading bold BREAKING label, plus a one-to-two line upgrade remediation naming the allowlist and the typst_template_function.params escape hatch (SC#2, D-02, D-04)"
    - "The CONF-05 config-removal bullet lives in `### Removed` and carries a bullet-leading bold BREAKING label, with the measured caveat that a conf.py still setting it is silently ignored by Sphinx and that removal changes no build's output (SC#2, D-01)"
    - "The CONF-07 lang bullet lives in `### Fixed` and carries NO BREAKING label, states the before/after concretely, and states the applicability scope (default-template path only) — the D-01/D-03 asymmetry is preserved deliberately and must not be harmonized (SC#2, D-03)"
    - "A `### Verified` section records the same four points as the precedent — fatal-free, valid %PDF magic, clean unknown_visit catalogue, and the SC#4 invariant triple — stating only facts 28-VERIFICATION.md actually shows (SC#2, D-11)"
    - "The entry claims no page count or any other figure that has no verification mechanism (D-11)"
    - "The lead paragraph is written on the three-track axis (documented config now reaches the output / captioned tables render as numbered figures / docs brought into line with the implementation) plus the one-line invariant statement, in the same register as the [0.6.2] lead — BREAKING is not front-loaded into the lead (D-12)"
    - "A `[0.6.3]:` releases/tag link-reference line is added immediately above the `[0.6.2]:` line and the `[Unreleased]:` compare target is advanced to v0.6.3 — release-prep's own job per ROADMAP SC#2, not deferred to a review-driven follow-up as in Phase 23 (SC#2)"
    - "No pre-existing CHANGELOG entry is modified: the diff contains exactly one removed line, the superseded `[Unreleased]:` compare line (SC#2, 28-CONTEXT.md Out of scope)"
    - statement: "Each bullet leads with the observable symptom for a reader who has never seen a CONF or TBL number, with the requirement IDs relegated to a trailing parenthetical citation (D-09)"
      verification: backstop
    - statement: "The `### Verified` section confines itself to the four precedent points; the full pytest suite and documentation-build evidence added by D-05 stay in 28-VERIFICATION.md and do not leak into the release notes (D-11)"
      verification: backstop
  artifacts:
    - "CHANGELOG.md — new `## [0.6.3] - YYYY-MM-DD` section containing `### Added`, `### Changed`, `### Removed`, `### Fixed`, `### Verified` subsections"
    - "CHANGELOG.md — new `[0.6.3]:` link-reference line in the bottom-of-file block and an advanced `[Unreleased]:` compare target"
    - "28-03-SUMMARY.md — a ledger-ID to CHANGELOG-bullet coverage table proving zero unintended drops and naming the one deliberate omission"
  key_links:
    - "CHANGELOG.md `## [0.6.3]` entry ↔ the eventual GitHub Release body — ROADMAP SC#2 makes this entry the single source, so anything omitted here is omitted from the release announcement"
    - "`### Verified` section ↔ 28-VERIFICATION.md gate evidence produced in wave 2 — the section may only state facts that record actually shows (D-11)"
    - ".planning/REQUIREMENTS.md 7-item v1 ledger ↔ the trailing ID citations in each bullet — the traceability chain D-09 requires"
    - ".planning/PROJECT.md § Requirements ▸ Validated (Phase 24–27.1 entries) ↔ the factual source material for every bullet's prose"
  prohibitions:
    - statement: "No git tag named v0.6.3 (or any other tag) is created or pushed by this plan"
      status: required
      verification: "`git tag --list 'v0.6.3'` returns empty"
    - statement: "No PyPI upload, no GitHub Release creation, no manual or scripted trigger of .github/workflows/release.yml"
      status: required
      verification: "`git status --porcelain .github/` is empty; no `gh release`, `twine`, `uv publish`, or `git push --tags` command is run"
    - statement: "No pre-existing CHANGELOG version entry is edited — the historical record is immutable"
      status: required
      verification: "`git diff -- CHANGELOG.md | grep -c '^-[^-]'` returns 1 (only the superseded [Unreleased] compare line)"
    - statement: "No docs/ edit — upgrade guidance lives in the CHANGELOG body only, because touching docs drags in the ja .po gettext follow-up (D-04)"
      status: required
      verification: "`git status --porcelain docs/` is empty"
    - statement: "No behavior change under typsphinx/, tests/, or examples/"
      status: required
      verification: "`git status --porcelain typsphinx/ tests/ examples/` is empty"
    - statement: "No commentary on the project's version-numbering policy and no alternative version number proposed — ROADMAP SC#1 fixes this release at 0.6.3"
      status: required
      verification: "the `## [0.6.3]` section body contains no `0.7.0` string"
---

<objective>
`CHANGELOG.md` に `## [0.6.3]` エントリを新設し、ファイル末尾のリンクブロックを更新する。
v1 要件台帳 7 件のうち 6 件を、要件 ID の 1 対 1 列挙ではなく**ユーザーに見えた変化の単位**で
5 つのブレットに束ね、BREAKING の配分を D-01 / D-02 / D-03 の裁定どおりに立て、`### Verified` は
先例と同じ 4 点に留める。

Purpose: ROADMAP SC#2 を満たす。このエントリは `/gsd-complete-milestone` が作る GitHub Release body の
**単一ソース**になるので、ここに書かなかったことはリリース告知からも落ちる。

Output: `CHANGELOG.md` の `## [0.6.3] - YYYY-MM-DD` セクション（`### Added` / `### Changed` /
`### Removed` / `### Fixed` / `### Verified`）と、末尾リンクブロックの `[0.6.3]:` 行追加 +
`[Unreleased]:` compare ターゲットの繰り上げ。

**実行順序について（記録）:** `28-CONTEXT.md` の Claude's Discretion は「版バンプ → CHANGELOG →
ゲート実走 の順が自然」と括弧書きしているが、本プランは Phase 23 と同じく **版バンプ → ゲート実走 →
CHANGELOG** の順を採る。理由は D-11 — `### Verified` 節は「検証機構を持てない数値は載せない」原則の下、
ゲートが実際に assert した事実だけを述べてよい。したがってゲートはこの節が書かれる**前に**回っている
必要がある。作業分割と順序は Claude's Discretion の範囲内であり、これは裁量の行使を明示的に記録した
ものである。

`requirements: []` は正しく意図的である。ROADMAP §Phase 28 の `**Requirements**` は
`none (release/close phase — carries no requirement)` であり、`.planning/REQUIREMENTS.md:65` も
「Phase 28 is a prep-only release/close phase and carries no requirement」と明記している。台帳 7 件は
Phases 24〜27.1 に既に割り当て済みで delivered 済みであり、本プランはそれらを**引用**するだけで
新たに**実装**するわけではない。トレーサビリティはフロントマターではなく、下の
「Requirement ledger → CHANGELOG bullet coverage」表と `must_haves.truths` が担う。
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-CONTEXT.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-RESEARCH.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 28-03 share)

このフェーズは **Python シンボル・CLI フラグ・config キーを一切作らない**。本プランが作るのは
markdown の見出しとリンク参照行だけである。

| Artifact | Kind | 定義される識別子・見出し |
|---|---|---|
| `CHANGELOG.md` | modified | 新規見出し `## [0.6.3] - YYYY-MM-DD` と、その配下の `### Added` / `### Changed` / `### Removed` / `### Fixed` / `### Verified` |
| `CHANGELOG.md` | modified | 新規リンク参照行 `[0.6.3]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.6.3`、および繰り上げ後の `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.6.3...HEAD` |

姉妹プランが作る成果物（網羅性のため列挙）: `pyproject.toml` / `uv.lock` / `README.md` の版バンプ
（28-01）、新規ファイル `28-VERIFICATION.md`（28-02）。

## Requirement ledger → CHANGELOG bullet coverage

`requirements: []`（フロントマター）が正しい一方で、台帳のカバレッジはこの表が担う。executor は
`28-03-SUMMARY.md` にこの表を実測値で埋め直して再掲すること。

| Ledger ID | ユーザーに見えた変化（一言） | CHANGELOG 上の扱い | BREAKING | 根拠 |
|---|---|---|---|---|
| TBL-01 | キャプション付きテーブルが番号付き "Table N" figure になる | `### Added`（TBL-02 と 1 ブレットに束ねる） | なし | D-09 |
| TBL-02 | `:numref:`/`:ref:` がキャプション付きテーブルに解決する | `### Added`（TBL-01 と同一ブレット） | なし | D-09 |
| CONF-04 | `typst_elements` の allowlist 外キーがビルドを止める（従来は無音で破棄） | `### Changed` | **あり** | D-02 |
| CONF-05 | 死んでいた `typst_toctree_defaults` の削除 | `### Removed` | **あり** | D-01 |
| CONF-07 | Typst 組版言語が Sphinx `language` に連動する | `### Fixed` | なし | D-03 |
| DOC-07 | 公開ドキュメントの phantom 設定名の訂正 | `### Fixed` | なし | D-10 |
| （7 件目・孤児ドキュメント削除） | **意図的に載せない** — どの toctree からも到達不能で、ユーザーの目には元々入っていない内部整理 | 掲載なし | — | D-10 |

<!-- planner-discipline-allow: DOC-06 -->

**D-01 と D-03 の非対称性は意図的である。** 実害のない設定名の削除（CONF-05）には BREAKING を立て、
実際に出力が変わる lang 連動（CONF-07）には立てない。これは Phase 23 の D-05／D-07 の配分と同じ構造で、
オーナーが両フェーズで一貫して選んでいる。「一貫性がない」と感じて揃えないこと（`28-RESEARCH.md`
Pitfall 4）。基準は「設定名の削除・破壊的な fail-loud 化 = 公開 API の変化としての BREAKING」対
「バグ修正としての振る舞い訂正 = BREAKING ではない」である。

<specless_probe_note>
spec-less probe fallback: **visible skip（記録済みの選択）**。理由は 28-01-PLAN.md / 28-02-PLAN.md の
同名ブロックと同じ — プローブは要件テキストから述語を導出するが、本フェーズは要件 ID をゼロ個しか
持たない（`.planning/REQUIREMENTS.md:65`）。したがって**プローブ由来の edge / prohibition 述語は
この実行では一切生成していない**。上の `must_haves.prohibitions` は ROADMAP SC#5 と `28-CONTEXT.md` の
Out of scope 節から直接書き起こしたものである。
</specless_probe_note>

<tasks>

<task type="auto">
  <name>Task 1: CHANGELOG.md に `## [0.6.3]` エントリを新設する（リード段落 + 5 ブレット + Verified 4 点）</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` の 1〜80 行（`## [Unreleased]` が 8 行目、空行を挟んで `## [0.6.2] - 2026-07-23` が
      10 行目。挿入位置はこの 9 行目と 10 行目の間。`[0.6.2]` のリード段落 4 行と `### Removed` の
      BREAKING ブレット、`### Fixed` の要件 ID 引用スタイル、`### Verified` の 2 ブレット構成が
      直接の型紙）
    - `CHANGELOG.md` の `## [0.6.1]` エントリ（節順序の analog。`### Added` → `### Changed` →
      `### Fixed` → `### Verified` という Keep a Changelog 語彙順に近い並びで、4 種すべてが揃う
      本エントリはこちらに寄せる）
    - `.planning/PROJECT.md` § Requirements ▸ Validated の Phase 24 / 25 / 26 / 27 / 27.1 の項目
      （**各ブレットの文面の素材はここから取る。**何が出荷されたかの一次記録であり、
      CHANGELOG の主張はここに書かれた事実を超えてはいけない）
    - `.planning/REQUIREMENTS.md` の v1 要件 7 件（CONF-04 / CONF-05 / CONF-07 / TBL-01 / TBL-02 と
      docs 系 2 件）とトレーサビリティ表
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-RESEARCH.md` の
      § "CHANGELOG Structural Template"（挿入位置・BREAKING の書式・ID 引用位置の実測）と
      § "Code Examples" の「推奨 `[0.6.3]` CHANGELOG エントリ」（**ほぼそのまま使える完成形ドラフト。
      D-01〜D-12 と 6 件の ID カバレッジをすべて満たしている**）、および § "Common Pitfalls" Pitfall 4
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-PATTERNS.md` § `CHANGELOG.md`
      （リード段落パターン、BREAKING バレットの書式パターン、要件 ID 引用位置パターン、節順序の
      2 段構え analog 選択）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VERIFICATION.md`
      （wave 2 が作った証跡。`### Verified` 節が主張してよい事実の唯一の根拠。ここに書かれていない
      ことを `### Verified` に書いてはいけない）
    - `.planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-CONTEXT.md`
      （CONF-07 の適用範囲と precedence の一次記録。`### Fixed` の CONF-07 ブレットの根拠）
  </read_first>
  <action>
    まずワークツリーをプロビジョニングする:
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`。以後すべて `uv run` 経由。

    `CHANGELOG.md` の 9 行目と 10 行目の間、つまり `## [Unreleased]` の直後・`## [0.6.2] - 2026-07-23` の
    直前に、新しいセクションを挿入する。`## [Unreleased]` 見出しはそのまま上に残す（Keep a Changelog
    標準、`28-CONTEXT.md` の Claude's Discretion で確認済み）。

    見出しは `## [0.6.3] - YYYY-MM-DD` の形で、日付は**実行日**を使う（Phase 23 D-15 で確立した
    ルール。`- Unreleased` のまま残して `/gsd-complete-milestone` で確定する案は不採用）。実行時に
    `date +%F` を叩いてその値を使うこと（計画時点の見込みは `2026-07-25`）。

    節の並びは `### Added` → `### Changed` → `### Removed` → `### Fixed` → `### Verified`。
    `[0.6.2]` 自体は Removed → Fixed → Verified の順だが、本エントリは Added/Changed/Removed/Fixed が
    4 種すべて揃う最初のケースなので、Keep a Changelog の語彙順に忠実な `[0.6.1]` 型に寄せる
    （Claude's Discretion の範囲内、`28-RESEARCH.md` § Open Questions 1 の推奨）。

    **リード段落（D-12）:** 3 トラック軸 +不変量の一行で書く。3 軸は「ドキュメントに書いた設定が
    実際に出力へ効くようになった」「キャプション付きテーブルを番号付き figure として出力するように
    した」「docs の記述を実装と一致させた」。末尾に「新規ランタイム依存ゼロ、同梱 `@preview` の
    版同期面は未変更」に相当する一行を添える（`[0.6.2]` のリード末尾と同じ体裁）。BREAKING を
    リード冒頭で先出しする案は不採用 — BREAKING の可視化は各ブレット本体が担う。

    **本文は次の 5 ブレットちょうど。**書式はすべて `[0.6.2]` が採用したバレット先頭ボールド型
    （`- **見出し文 (ID)** — 本文…`）で、要件 ID は太字見出しの直後・閉じ括弧の直前に置く。

    1. `### Added` に 1 ブレット、末尾の括弧は `(TBL-01, TBL-02)`。キャプション付きの
       `.. table::`（および `csv-table` / `list-table`）が `figure(table(...), caption: {...},
       kind: table)` として出力され Typst ネイティブの "Table N" 番号が付くこと、直前の余計な
       `heading()` が消えたこと、`:numref:` / `:ref:` がコンパイル済み PDF 上の生きた相互参照に
       解決すること、キャプションのないテーブルは従来どおり素の `table()` のままで投機的に
       figure 包装されないこと。TBL-01 と TBL-02 は D-09 に従い 1 ブレットに束ねる。

    2. `### Changed` に 1 ブレット、先頭に **BREAKING** ラベル、末尾の括弧は `(CONF-04)`。
       `typst_elements` の `papersize` / `fontsize` / `lang` 以外のキーが 1 つでもあると
       `ExtensionError: typst_elements: unknown key ...` でビルドが中断すること、Phase 26 以前は
       同じキーが無音で破棄されていたこと（つまり飾りのキーを入れていたユーザーは `conf.py` を
       一切触らずに緑から硬失敗に変わる）を書く。そのうえで D-04 の対処手順を 1〜2 行で入れる —
       allowlist 外のキーは削除する、値をカスタムテンプレートへ渡し続けたい場合は
       `typst_template_function.params` を使う。`papersize` / `fontsize` が実際に
       コンパイル済み `.typ` / PDF に届くようになったこと（従来は allowlist に関係なく無音で
       捨てられていた）も同じブレットに含める。docs は一切触らない — アップグレード手順は
       CHANGELOG 本文にのみ書く（D-04。docs を開くと ja `.po` の gettext 追従が付随するため）。

    3. `### Removed` に 1 ブレット、先頭に **BREAKING** ラベル、末尾の括弧は `(CONF-05)`。
       `typst_toctree_defaults` が登録されていたがどのコードパスからも読まれていなかったこと、
       削除後も `conf.py` に残っていれば Sphinx は警告すら出さず無音で無視すること（未登録の
       config 値は警告を出さない）、削除前から出力に一切効いていなかったので実際のビルド結果は
       変わらないこと、非推奨期間は設けないこと。書式は `[0.6.2]` の CONF-01 ブレットと同型にする。

    4. `### Fixed` に 1 つ目のブレット、**BREAKING ラベルなし**、末尾の括弧は `(CONF-07)`。
       `templates/base.typ` が組版言語を `lang: "en"` でハードコードしていたため、
       `language = "ja"` のプロジェクトは本文こそ Sphinx の i18n transform で翻訳されるのに
       Typst が生成するラベルだけ英語のままだった（キャプション付きテーブルが「表 1」ではなく
       "Table 1" と出る）ことを before/after で示す。既定テンプレートが `config.language` から
       `lang` を導出するようになったこと、明示的な `typst_elements` の `lang` 指定は全経路で
       優先されることを書く。**適用範囲も必ず書く** — 自動導出は既定テンプレート経路のみで、
       カスタムテンプレート / `typst_package` / srcdir シャドウの 3 経路には注入されず、
       そちらは従来どおり自前で `lang` を宣言する必要がある。BREAKING を立てないのは
       「本来そうすべきだったものの修正」という位置づけによる（D-03、Phase 23 D-07 と同じ基準）。

    5. `### Fixed` に 2 つ目のブレット、末尾の括弧は `(DOC-07)`。
       `docs/source/user_guide/configuration.rst` の `typst_author` を実在する `typst_authors` に
       直したこと、実在しない `typst_use_codly` / `typst_code_line_numbers` を削除したこと、
       `typst_papersize` / `typst_fontsize` を動く `typst_elements` の例に書き換えたこと、
       `docs/source/api/index.rst` の重複してドリフトしていた設定値表を削除して唯一の
       `:doc:` ポインタに寄せたこと。

    **7 件目の台帳項目（孤児ドキュメントの削除）はこのエントリに載せない。** どの toctree からも
    到達不能でユーザーの目には元々入っていなかった内部整理であり、D-10 が可視性の非対称を理由に
    明示的に除外している。これは意図的な選択であって漏れではない。網羅性を理由に足し戻さないこと。
    その ID を `[0.6.3]` セクションの本文に書いてはいけない（この判断の記録は
    `28-03-SUMMARY.md` のカバレッジ表側に書く）。

    **`### Verified` 節（D-11）は先例と同じ 4 点に留める:** フルコーパス回帰ゲートが fatal-free で
    あること、有効な `%PDF` マジックバイトを持つ出力が得られること、`unknown_visit` カタログが
    空のままであること、そして SC#4 の不変量（新規ランタイム依存ゼロ・`@preview` 未バンプ・
    `base.typ` の `main` からの差分が Phase 27.1 の `lang` 2 行のみ）。`[0.6.2]` と同じく
    2 ブレットにまとめてよい。**この 4 点だけを書く。** D-05 が証拠として加えたフル pytest スイートと
    ドキュメントビルドの結果は `28-VERIFICATION.md` 側の証跡に留め、この節には持ち込まない。
    ページ数のように検証機構を持てない数値も書かない。各主張は wave 2 が作った
    `28-VERIFICATION.md` に実出力が残っているものだけにすること。

    既存の `## [0.6.2]` 以前のエントリは 1 バイトも変更しない（履歴は不変）。末尾リンクブロックの
    更新は Task 2 で行うので、このタスクでは触らない。

    版番号そのものへの言及・代替案（0.7.0 など）の提案は書かない — ROADMAP SC#1 が 0.6.3 に固定し、
    Phase 23 D-08 で同型の提案が明示的に不採用済みである。

    スコープ柵（ROADMAP SC#5、絶対）: タグを作らない・push しない。`uv publish` / `twine` /
    `gh release create` / `git push --tags` を実行しない。`.github/workflows/release.yml` を編集も
    起動もしない。`docs/` / `typsphinx/` / `tests/` / `examples/` に 1 バイトも書かない。
  </action>
  <acceptance_criteria>
    - `grep -Ec '^## \[0\.6\.3\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md` が `1` を返す
    - `grep -n '^## \[' CHANGELOG.md` が返す**最初の 3 行**が、この順で `## [Unreleased]` / `## [0.6.3] - <実行日>` / `## [0.6.2] - 2026-07-23` である（挿入位置と `## [Unreleased]` 見出しの保持を同時に固定する。**単純な行数カウントを使ってはいけない** — 実測: `CHANGELOG.md` の 708 行目に v0.1.0b1 時代の遺物である 2 本目の `## [Unreleased]` 見出しが存在し、見出しの並びもファイル全体では単調降順ではない。この遺物ブロックは履歴なので触らない）
    - `[0.6.3]` セクション（`awk '/^## \[0\.6\.3\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md` で抽出）に `### Added` / `### Changed` / `### Removed` / `### Fixed` / `### Verified` の 5 見出しがそれぞれ 1 回ずつ現れる
    - 同セクション内で 5 見出しの出現順が Added → Changed → Removed → Fixed → Verified である
    - 同セクション内の行頭 `- **` で始まる行（トップレベルブレット）が `### Added` から `### Fixed` の範囲で正確に 4 本（Added 1 / Changed 1 / Removed 1 / Fixed 2 のうち Fixed の 2 本目を含め、合計 5 本 — うち `### Verified` 配下のブレットは別勘定）
    - 同セクションに `(TBL-01, TBL-02)` / `(CONF-04)` / `(CONF-05)` / `(CONF-07)` / `(DOC-07)` の 5 つの括弧付き引用がそれぞれ 1 回以上現れる
    - 同セクション内の `**BREAKING` の出現回数が正確に `2`
    - `### Changed` 配下の最初のブレットに `**BREAKING` が含まれる
    - `### Removed` 配下の最初のブレットに `**BREAKING` が含まれる
    - `### Fixed` 配下のどのブレットにも `**BREAKING` が含まれない（D-03 の非対称性が保たれている）
    - 同セクションに `typst_template_function.params` が含まれる（D-04 の対処手順）
    - 同セクションに `ExtensionError` が含まれる（CONF-04 の具体的な失敗形）
    - 同セクションに `typst_toctree_defaults` が含まれる（CONF-05 の対象名）
    - 同セクションに `%PDF` と `unknown_visit` の両方が含まれる（`### Verified` の 4 点のうち 2 点）
    - 同セクションに `@preview` が含まれる（SC#4 不変量の主張）
    - 同セクションに `0.7.0` が含まれない（版番号政策への言及なし）
    - 同セクションが、arabic 数字の直後に `pages` / `ページ` が続く語を含まない（検証機構を持てない数値なし、D-11）
    - `awk '/^## \[0\.6\.3\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -c 'DOC-06'` が `0` を返す（7 件目の台帳項目は D-10 により意図的に不掲載。この判断は SUMMARY のカバレッジ表側に記録する）
    - `git diff -- CHANGELOG.md | grep -c '^-[^-]'` が `0` を返す（このタスクでは既存行を 1 行も削除・変更していない）
    - `git status --porcelain docs/ typsphinx/ tests/ examples/ .github/` が空
    - `git tag --list 'v0.6.3'` が空
  </acceptance_criteria>
  <verify>
    <automated>test "$(git diff -- CHANGELOG.md | grep -c '^-[^-]')" = "0" &amp;&amp; test "$(git status --porcelain docs/ typsphinx/ tests/ examples/ .github/)" = "" &amp;&amp; test "$(git tag --list 'v0.6.3')" = "" &amp;&amp; uv run python -c "import pathlib,re,sys;t=pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8');heads=re.findall(r'^## \[.*\$',t,re.M);e=[];e.append('heads-too-few') if len(heads)&lt;3 else None;e.append('h1='+str(heads[:1])) if heads[:1]!=['## [Unreleased]'] else None;e.append('h2='+str(heads[1:2])) if not (len(heads)&gt;1 and re.match(r'^## \[0\.6\.3\] - \d{4}-\d{2}-\d{2}\$',heads[1])) else None;e.append('h3='+str(heads[2:3])) if not (len(heads)&gt;2 and heads[2]=='## [0.6.2] - 2026-07-23') else None;s=re.search(r'^## \[0\.6\.3\].*?(?=^## \[)',t,re.M|re.S);e.append('section') if not s else None;sec=s.group(0) if s else '';[e.append('sub:'+x) for x in ['### Added','### Changed','### Removed','### Fixed','### Verified'] if sec.count(x)!=1];order=[sec.find(x) for x in ['### Added','### Changed','### Removed','### Fixed','### Verified']];e.append('order') if sec and order!=sorted(order) else None;[e.append('cite:'+c) for c in ['(TBL-01, TBL-02)','(CONF-04)','(CONF-05)','(CONF-07)','(DOC-07)'] if c not in sec];e.append('breaking-count='+str(sec.count('**BREAKING'))) if sec.count('**BREAKING')!=2 else None;[e.append('kw:'+k) for k in ['typst_template_function.params','ExtensionError','typst_toctree_defaults','%PDF','unknown_visit','@preview'] if k not in sec];e.append('version-policy') if '0.7.0' in sec else None;print('ERRORS:',e);sys.exit(1 if e else 0)"</automated>
  </verify>
  <done>
    `CHANGELOG.md` に `## [0.6.3] - <実行日>` セクションが `## [Unreleased]` と `## [0.6.2]` の間に
    存在し、Added → Changed → Removed → Fixed → Verified の順で 5 ブレットと 4 点の Verified を持ち、
    BREAKING がちょうど 2 箇所（Changed と Removed）に立ち、Fixed には立っておらず、6 件の ID 引用が
    揃い、7 件目は不掲載。既存エントリは 1 行も変更されていない。
  </done>
</task>

<task type="auto">
  <name>Task 2: ファイル末尾のリンクブロックに `[0.6.3]:` 行を追加し、`[Unreleased]:` compare を繰り上げる</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` の末尾 20 行（`[0.6.2]:` から `[0.1.0b1]:` までの releases/tag リンク一覧が
      新しい順に並び、その下に `[Unreleased]:` の compare リンクが 1 行ある。**実物の URL 形式を
      ここで確認すること** — ホスト・オーナー名・リポジトリ名を推測で書かない）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-RESEARCH.md`
      § "リンクブロックの Before → After（ファイル末尾）"（正確な Before / After）と
      § "CHANGELOG Structural Template" の該当箇所（v0.6.3 タグはこのフェーズでは存在しないため
      リンクは一時的に 404 になるが、これは v0.6.1 / v0.6.2 の release-prep がいずれも経ている
      確立済みの project convention である旨の記録）
    - `.planning/ROADMAP.md` § Phase 28 の SC#2（リンクブロック更新が release-prep 自身の仕事だと
      明記されている一次記録。Phase 23 は当初これをスコープ外とし、レビュー指摘で後追い追加した）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-PATTERNS.md`
      § `CHANGELOG.md` のリンクブロック analog
  </read_first>
  <action>
    `CHANGELOG.md` 末尾のリンク参照ブロックに 2 つの変更を加える。

    1. **追加:** `[0.6.3]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.6.3` を、既存の
       `[0.6.2]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.6.2` 行の**直前**に挿入する。
       このブロックは新しい順に並んでいるので、最新版が先頭に来る。

    2. **変更:** `[Unreleased]:` の compare リンクの左辺タグを繰り上げる。現状は
       `https://github.com/YuSabo90002/typsphinx/compare/v0.6.2...HEAD` を指しているので、これを
       `https://github.com/YuSabo90002/typsphinx/compare/v0.6.3...HEAD` に変える。ファイル内の
       `[Unreleased]:` 行は 1 本だけであり、変更後も 1 本のままである。

    `v0.6.3` タグは本フェーズでは存在しない（SC#5 のスコープ柵）ので、追加した releases/tag リンクは
    `/gsd-complete-milestone` がタグを打つまで 404 になる。これは v0.6.1 / v0.6.2 いずれの
    release-prep でも同じ状態を経ており、v0.6.1 のリリース準備コミット `eba914c` から続く確立済みの
    project convention として許容されている。ROADMAP SC#2 がこのリンクブロック更新を release-prep
    自身の仕事として明記しているので、Phase 23 のような後追い override を経由する必要はない。

    ブロック内の他のバージョン行（`[0.6.2]:` 以下 `[0.1.0b1]:` まで）は 1 バイトも変更しない。
    このタスク完了後、`CHANGELOG.md` 全体の差分で削除された既存行は `[Unreleased]:` の旧 compare 行
    ちょうど 1 行だけになるはずである。

    最後にサンプリング用のクイックランを回す:
    `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v`
    （`28-VALIDATION.md` § Sampling Rate が定めるタスクコミットごとの計測点）。`CHANGELOG.md` を
    読むテストは存在しないが、ツリーが緑のままであることの確認になる。

    スコープ柵（ROADMAP SC#5、絶対）: タグを作らない・push しない。publish 系コマンドを実行しない。
    `.github/workflows/release.yml` に触らない。
  </action>
  <acceptance_criteria>
    - `grep -c '^\[0\.6\.3\]: https://github\.com/YuSabo90002/typsphinx/releases/tag/v0\.6\.3$' CHANGELOG.md` が `1` を返す
    - `grep -c '^\[Unreleased\]: https://github\.com/YuSabo90002/typsphinx/compare/v0\.6\.3\.\.\.HEAD$' CHANGELOG.md` が `1` を返す
    - `grep -c '^\[Unreleased\]: ' CHANGELOG.md` が `1` を返す（`[Unreleased]:` 行は依然として 1 本だけ）
    - `[0.6.3]:` 行の行番号が `[0.6.2]:` 行の行番号のちょうど 1 つ前である
    - `grep -c '^\[0\.6\.2\]: https://github\.com/YuSabo90002/typsphinx/releases/tag/v0\.6\.2$' CHANGELOG.md` が `1` を返す（既存行は無傷）
    - `grep -c '^\[0\.1\.0b1\]: ' CHANGELOG.md` が `1` を返す（ブロック末尾の最古行も無傷）
    - `git diff -- CHANGELOG.md | grep -c '^-[^-]'` が `1` を返す（削除された既存行は旧 compare 行のちょうど 1 行）
    - `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v` が exit 0（2 passed）
    - `git status --porcelain docs/ typsphinx/ tests/ examples/ .github/` が空
    - `git tag --list 'v0.6.3'` が空
    - `28-03-SUMMARY.md` に Ledger ID → CHANGELOG bullet のカバレッジ表が実測値で再掲されており、7 件目の項目が「意図的に不掲載（D-10）」として明示されている
  </acceptance_criteria>
  <verify>
    <automated>test "$(git diff -- CHANGELOG.md | grep -c '^-[^-]')" = "1" &amp;&amp; test "$(git status --porcelain docs/ typsphinx/ tests/ examples/ .github/)" = "" &amp;&amp; test "$(git tag --list 'v0.6.3')" = "" &amp;&amp; uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q &amp;&amp; uv run python -c "import pathlib,sys;L=pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8').splitlines();B='https://github.com/YuSabo90002/typsphinx';e=[];i3=[i for i,l in enumerate(L) if l=='[0.6.3]: '+B+'/releases/tag/v0.6.3'];i2=[i for i,l in enumerate(L) if l=='[0.6.2]: '+B+'/releases/tag/v0.6.2'];u=[i for i,l in enumerate(L) if l.startswith('[Unreleased]: ')];e.append('missing-063-link') if len(i3)!=1 else None;e.append('missing-062-link') if len(i2)!=1 else None;e.append('unreleased-count='+str(len(u))) if len(u)!=1 else None;e.append('unreleased-target') if u and L[u[0]]!='[Unreleased]: '+B+'/compare/v0.6.3...HEAD' else None;e.append('ordering') if (i3 and i2 and i3[0]+1!=i2[0]) else None;e.append('oldest-link') if not any(l.startswith('[0.1.0b1]: ') for l in L) else None;print('ERRORS:',e);sys.exit(1 if e else 0)"</automated>
  </verify>
  <done>
    `CHANGELOG.md` 末尾に `[0.6.3]:` の releases/tag リンク行が `[0.6.2]:` 行の直前に入り、
    `[Unreleased]:` の compare ターゲットが v0.6.3 に繰り上がっている。他のバージョン行は無傷で、
    ファイル全体の差分で削除された既存行は旧 compare 行 1 行のみ。タグは存在せず、
    `docs/` / `typsphinx/` / `tests/` / `examples/` / `.github/` は無変更。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` の `## [0.6.3]` エントリ → GitHub Release body → 公開告知 | ROADMAP SC#2 によりこのエントリが Release body の単一ソースになる。ここに書いた事実（および書かなかった事実）はそのまま公開される |
| `28-VERIFICATION.md` のゲート証跡 → `### Verified` 節 | 証跡が示していない主張をこの節に書くと、検証不能な事実が公開告知に載る |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-07 | Spoofing | `### Verified` 節が検証されていない事実を主張する | medium | mitigate | 本プランを wave 3 に置き、wave 2 の `28-VERIFICATION.md` を `read_first` に含めることで、書ける事実の集合を証跡に制約する。受入基準が `%PDF` / `unknown_visit` / `@preview` の 3 語の存在と `0.7.0` の不在、ページ数系の数値の不在を assert する（D-11） |
| T-28-08 | Repudiation | BREAKING の告知漏れ、または D-01/D-03 の非対称性の「harmonize」 | high | mitigate | 受入基準が `**BREAKING` の出現回数を正確に 2 と固定し、`### Changed` と `### Removed` の各先頭ブレットに存在すること、`### Fixed` 配下には存在しないことを個別に assert する。`28-RESEARCH.md` Pitfall 4 と本プランの非対称性ノートで意図を明文化 |
| T-28-09 | Tampering | 過去バージョンエントリの改変（履歴の書き換え） | medium | mitigate | `git diff -- CHANGELOG.md | grep -c '^-[^-]'` を Task 1 で 0、Task 2 で 1（旧 compare 行のみ）に固定する。それ以外の既存行の削除・変更は機械的に検出される |
| T-28-02 | Elevation of Privilege | `.github/workflows/release.yml`（タグ起動の PyPI publish） | high | mitigate | ROADMAP SC#5 のスコープ柵を `must_haves.prohibitions` に符号化し、両タスクの action で再掲。`git tag --list 'v0.6.3'` が空、`git status --porcelain .github/` が空であることで assert する |
| T-28-SC | Tampering | パッケージインストール | low | accept | 本プランはパッケージを新規インストールしない。`28-RESEARCH.md` § "Package Legitimacy Audit" が「該当なし」を記録済み |

**Security posture（`28-RESEARCH.md` § Security Domain の所見をそのまま記録）:** 本プランが変更するのは
markdown の散文とリンク参照行だけで、評価すべき攻撃面の変化はない。ユーザー入力なし、新規ネットワーク
呼び出しなし、認証/セッション/アクセス制御面なし、暗号なし、新規テストコードなし。

| ASVS Category | Applies | Standard Control |
|---------------|---------|-------------------|
| V2 Authentication | No | N/A — 認証面に触れない |
| V3 Session Management | No | N/A |
| V4 Access Control | No | N/A |
| V5 Input Validation | No（リポジトリローカルの固定パスファイルを読むだけ） | N/A |
| V6 Cryptography | No | N/A |
</threat_model>

<verification>
- `grep -Ec '^## \[0\.6\.3\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md` → 1
- `[0.6.3]` セクション内の `### Added` / `### Changed` / `### Removed` / `### Fixed` / `### Verified` が各 1 回、この順で出現
- 同セクション内の `**BREAKING` が正確に 2 回（`### Changed` と `### Removed`）
- 6 件の ID 引用 `(TBL-01, TBL-02)` / `(CONF-04)` / `(CONF-05)` / `(CONF-07)` / `(DOC-07)` がすべて存在
- `grep -c '^\[0\.6\.3\]: ' CHANGELOG.md` → 1、`grep -c '^\[Unreleased\]: ' CHANGELOG.md` → 1 で compare 先が v0.6.3
- `git diff -- CHANGELOG.md | grep -c '^-[^-]'` → 1（旧 compare 行のみ）
- `git tag --list 'v0.6.3'` が空、`git status --porcelain docs/ typsphinx/ tests/ examples/ .github/` が空
</verification>

<success_criteria>
- SC#2 が満たされる: `## [0.6.3]` エントリが 5 ブレットで台帳 6 件を引用し、`[Unreleased]` compare が
  繰り上がり、`[0.6.3]` の releases/tag リンク行が追加されている
- D-01 / D-02 が満たされる: BREAKING がちょうど 2 箇所（`### Removed` の CONF-05、`### Changed` の CONF-04）
- D-03 が満たされる: CONF-07 は `### Fixed` で BREAKING なし。D-01 との非対称性が保たれている
- D-04 が満たされる: 対処手順は CHANGELOG 本文のみ。`docs/` は無変更
- D-09 / D-10 が満たされる: 束ね方はユーザー可視単位、7 件目は意図的に不掲載でその判断が SUMMARY に記録
- D-11 / D-12 が満たされる: `### Verified` は 4 点のみ、リード段落は 3 トラック軸 + 不変量の一行
- SC#5 が保持: タグなし・publish なし・Release なし・`release.yml` への関与なし
</success_criteria>

<output>
`.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-03-SUMMARY.md` を作成する。
本プラン本文の「Requirement ledger → CHANGELOG bullet coverage」表を、実際に書いたブレットの
引用位置で埋め直して再掲すること。7 件目の項目は「意図的に不掲載（D-10）」として明示し、
将来のレビュアーが網羅性を理由に足し戻さないようにする。
</output>
