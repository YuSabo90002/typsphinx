---
phase: 28-v0-6-3-release-prep-regression-gate-close
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - .planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The full Sphinx doc/ v9.1.0 corpus, rebuilt through `-b typstpdf`, compiles with zero fatal errors and produces output with valid `%PDF` magic bytes (SC#3)"
    - "The rebuilt corpus's `unknown_visit` catalogue is empty — no node type is silently dropped (SC#3)"
    - "The corpus gate demonstrably PASSED rather than skipped: the captured `-rs` run's summary line reads `1 passed in N.NNs` with an elapsed time in the real-build range, not a sub-second skip (SC#3, D-05)"
    - "The full pytest suite (slow markers included) is green against the post-version-bump tree, and the single reported skip is identified in the evidence as tests/test_corpus_gate.py::test_empty_url_before_after (env-gated behind TYPSPHINX_CORPUS_REPORT=1), explicitly NOT the SC#3 gate (D-05)"
    - "`tox -e docs-pdf` warning output has not grown beyond its own English-only baseline of 2 lines, and `tox -e docs-multilang` has not grown beyond its own 2-language baseline of 4 lines — two separate per-environment criteria, never one shared number (D-05, D-06)"
    - "Zero new runtime dependencies across the milestone: `git diff main..HEAD -- pyproject.toml` shows only plan 28-01's single version-literal line and no change inside the `dependencies = [` array (SC#4, D-07)"
    - "No `@preview` package version was bumped: `git diff main..HEAD` over writer.py / template_engine.py / templates/base.typ contains no added or removed line mentioning `@preview` (SC#4, D-07)"
    - "`templates/base.typ`'s diff from `main` is confined to the Phase 27.1 `lang` parameter and its wiring — exactly one added line and one changed line (SC#4, D-07, ROADMAP 2026-07-25 invariant amendment)"
    - "No sha256 baseline for base.typ is recorded — D-07 rejected it by name because every future deliberate base.typ change would require updating it (D-07)"
    - "No manual visual inspection of a Japanese PDF is performed or claimed: `docs-pdf` produces an English PDF and `docs-multilang` produces HTML only, so the claim is not observable in any tox env; Phase 27.1's GATE-01 fixtures cover it mechanically inside the full suite (D-08)"
    - "All evidence lands in 28-VERIFICATION.md — no separate 28-GATE.md or other bespoke report file is created (established Phase 18/23 pattern)"
    - statement: "The captured gate output was produced by this phase's own run against the post-version-bump tree, not copied from 28-RESEARCH.md's earlier session or from a prior phase's log"
      verification: backstop
  artifacts:
    - ".planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VERIFICATION.md — new evidence record with frontmatter (phase/verified/status/score) and sections for the corpus gate, full suite, both docs builds, the SC#4 invariant diffs, and the SC#5 scope-fence assertions"
    - "28-02-SUMMARY.md — carries a duplicate verbatim copy of the corpus-gate log as the durable, executor-owned record"
  key_links:
    - "tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error ↔ the cached corpus under ~/.cache/typsphinx-corpus-gate/sphinx-v9.1.0/doc (user-global, so it is reachable from inside a worktree) — if the cache is absent the fixture calls pytest.skip and the gate silently yields no evidence; `-rs` plus the elapsed-time sanity check is what exposes that"
    - "tests/test_corpus_gate.py::test_empty_url_before_after ↔ the TYPSPHINX_CORPUS_REPORT=1 env gate — this is the source of the full suite's single skip and is NOT the SC#3 gate; conflating the two is the phase's headline misread risk"
    - "28-VERIFICATION.md is also the filename /gsd-verify-work writes its own report to — the SUMMARY copy of the gate log is the non-clobberable record"
    - "SC#4 base ref is `main` (9f8e075), not the v0.6.2 tag: merge-base(main, HEAD) == main, so `main..HEAD` spans exactly Phases 24–27.1 plus this phase's own commits"
  prohibitions:
    - statement: "No git tag named v0.6.3 (or any other tag) is created or pushed by this plan"
      status: required
      verification: "`git tag --list 'v0.6.3'` returns empty"
    - statement: "No PyPI upload, no GitHub Release creation, no manual or scripted trigger of .github/workflows/release.yml"
      status: required
      verification: "`git status --porcelain .github/workflows/release.yml` is empty; no `gh release`, `twine`, `uv publish`, or `git push --tags` command is run"
    - statement: "No code is added to tests/test_corpus_gate.py to make a release-time skip fail — D-06 explicitly rules test-infrastructure expansion out of scope for release prep"
      status: required
      verification: "`git status --porcelain tests/` is empty"
    - statement: "No test asserting a docs-build warning line count is created — D-06 declines it by name"
      status: required
      verification: "`git status --porcelain tests/` is empty and no new file matching tests/test_docs*warn* exists"
    - statement: "No behavior change under typsphinx/, docs/, or examples/ — this plan runs existing gates and writes evidence only"
      status: required
      verification: "`git status --porcelain typsphinx/ docs/ examples/` is empty"
    - statement: "No separate 28-GATE.md report file is created — evidence aggregates into 28-VERIFICATION.md"
      status: required
      verification: "`ls .planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-GATE.md` fails"
---

<objective>
マイルストーン v0.6.3 の回帰ゲートを締める。既存資産（`tests/test_corpus_gate.py`、フル pytest スイート、
`tox -e docs-pdf` / `docs-multilang`）を**版バンプ後のツリーに対して**実走し、コーパスゲートが本当に
PASS したこと（skip ではないこと）を証明し、SC#4 のマイルストーン不変量を `main` を base ref にした
`git diff` の実出力で確認し、SC#5 のスコープ柵を否定的に assert し、そのすべてを逐語の証跡として
`28-VERIFICATION.md` に記録する。

Purpose: ROADMAP SC#3 / SC#4 と SC#5 の否定的 assert を満たし、プラン 28-03 の CHANGELOG `### Verified`
節が主張してよい事実の根拠を作る。D-11 は「検証機構を持てない数値は載せない」を要求するので、
ゲートはその節が書かれる**前に**回っていなければならない。これが本プランが wave 2、CHANGELOG が
wave 3 である理由である。

Output: 新規ファイル `28-VERIFICATION.md`（証跡記録）と、`28-02-SUMMARY.md` に置く同じコーパスゲート
ログの二重コピー。

**新規テストコードはゼロ（D-05〜D-08）。** `tests/test_corpus_gate.py` は既に tag 解決 → キャッシュ済み
浅いクローン → `-b typstpdf` ビルド → `%PDF` マジック確認 → `unknown_visit` カタログ抽出まで全部やる。
`tests/test_preview_version_sync.py` は既に `@preview` の 3-way 同期面を守っている。本プランは既存資産の
**実走と証跡採取のみ**を行う。

`requirements: []` は正しく意図的である（`.planning/REQUIREMENTS.md:65` — Phase 28 は要件 ID を持たない
release/close フェーズ）。本フェーズの契約は SC#1〜SC#5 と D-01〜D-12 であり、`must_haves.truths` は
そちらを引用している。
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-CONTEXT.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-RESEARCH.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-PATTERNS.md
@.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 28-02 share)

このフェーズは **Python シンボル・CLI フラグ・config キーを一切作らない**。本プランに至っては
リポジトリのソースツリーを 1 バイトも変更せず、`.planning/` 配下に証跡ファイルを 1 つ作るだけである。

| Artifact | Kind | 定義される識別子・見出し |
|---|---|---|
| `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VERIFICATION.md` | new file | frontmatter キー `phase` / `verified` / `status` / `score` / `behavior_unverified` / `overrides_applied`、および本文見出し `## Observable Truths`、`## SC#3 — Full-Corpus Regression Gate`、`## SC#3 Evidence — Full pytest Suite`、`## SC#3 Evidence — Docs Builds`、`## SC#4 — Milestone Invariants`、`## SC#5 — Scope Fence` |

姉妹プランが作る成果物（網羅性のため列挙）: `pyproject.toml` / `uv.lock` / `README.md` の版バンプ
（28-01）、`CHANGELOG.md` の `## [0.6.3]` 見出しとその節見出し・`[0.6.3]:` リンク参照行・繰り上げ後の
`[Unreleased]:` compare ターゲット（28-03）。

<specless_probe_note>
spec-less probe fallback: **visible skip（記録済みの選択）**。理由は 28-01-PLAN.md の同名ブロックと同じ
— プローブは要件テキストから述語を導出するが、本フェーズは要件 ID をゼロ個しか持たない
（`.planning/REQUIREMENTS.md:65`）。したがって**プローブ由来の edge / prohibition 述語はこの実行では
一切生成していない**。上の `must_haves.prohibitions` は ROADMAP SC#5 と `28-CONTEXT.md` の Out of scope
節から直接書き起こしたものである。
</specless_probe_note>

<tasks>

<task type="auto">
  <name>Task 1: 回帰ゲート 3 点（コーパスゲート / フルスイート / docs ビルド 2 環境）を実走し、生ログを 28-VERIFICATION.md に貼る</name>
  <files>.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VERIFICATION.md</files>
  <read_first>
    - `tests/test_corpus_gate.py`（構造把握のため。特に 275 行目付近の `class TestCorpusRenderGate` と
      284 行目の `test_corpus_compiles_with_no_fatal_error`、110 行目付近の
      `cache_root = Path.home() / ".cache" / "typsphinx-corpus-gate"`、そして 508〜531 行目の
      独立関数 `test_empty_url_before_after` とその `TYPSPHINX_CORPUS_REPORT` env ゲート。
      **読むだけ。1 バイトも変更しない**）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-RESEARCH.md` の
      § "Corpus Gate Execution Mechanics"（実行コマンドと前セッションの実測ログ）、
      § "Full-Suite Baseline"、§ "Docs Build Baseline (D-06)"（環境別ベースラインの実測値と根拠）、
      § "Common Pitfalls" の Pitfall 1 / Pitfall 3 / Pitfall 5 / Pitfall 6
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VALIDATION.md`
      § "Manual-Only Verifications"（2 つの caveat が本タスクの判定ミスを直接狙って書かれている）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-PATTERNS.md`
      § `28-VERIFICATION.md`（frontmatter 構造と Observable Truths 表の形式、skip 誤読を防ぐ証跡の書き方）
    - `.planning/milestones/v0.6.2-phases/23-v0-6-2-release-prep-regression-gate-close/23-VERIFICATION.md`
      （同型フェーズの完成形。frontmatter の形と Observable Truths 表の列構成をここから借りる）
    - `tox.ini` の `[testenv:docs-pdf]` と `[testenv:docs-multilang]`（どちらも
      `runner = uv-venv-lock-runner` / `extras = docs` / `changedir = docs` — tox 経由なら docs extras が
      自動解決されることの一次証拠）
    - `CLAUDE.md` § "Worktree-isolated execution"
  </read_first>
  <action>
    まずワークツリーをプロビジョニングする:
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`。以後すべて `uv run` 経由。
    プロジェクト venv に `--extra docs` を足す必要は**ない** — `tox.ini` の `docs-pdf` /
    `docs-multilang` はどちらも `extras = docs` を宣言した `uv-venv-lock-runner` を使うので、
    tox 経由なら docs extras は自動で解決される。手で `sphinx-build` を叩く回避に落ちた場合のみ
    `uv sync --extra dev --extra docs` が要る。

    次に `28-VERIFICATION.md` を新規作成する。frontmatter は `23-VERIFICATION.md` の形を踏襲し、
    `phase: 28-v0-6-3-release-prep-regression-gate-close`、`verified:`（実行日時の ISO8601）、
    `status:`、`score:`、`behavior_unverified: 0`、`overrides_applied: 0` を置く。本文は
    `## Observable Truths`（表）に加えて、以下の 3 つの証跡節をこの順で作り、**各コマンドの
    コマンドライン全文と生出力を逐語で**貼る。要約に置き換えてはいけない。

    1. `## SC#3 — Full-Corpus Regression Gate` に、
       `uv run python -m pytest tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error -m slow -rs -v -s`
       の全出力を貼る。`-rs` は skip 理由を summary に強制表示させる指定で、これが「本当に走った」と
       「静かに飛ばした」を区別する唯一の手段である（コーパス入手不能時にフィクスチャが
       `pytest.skip` する仕様のため、両者とも exit 0 になる）。証跡には次の 4 点が読めること:
       summary 行が `1 passed in N.NNs` であること、`Corpus tag:` と `Corpus commit SHA:` の印字、
       `Unknown Visit Catalogue: []`、そして所要時間が実ビルド相当のオーダー（前セッション実測
       13.08s、Phase 23 実測 13.67s/13.99s）であること。サブ秒で終わっていたらそれは skip の兆候
       なので、原因を突き止めるまで PASS と書かない。

    2. `## SC#3 Evidence — Full pytest Suite` に、`uv run python -m pytest -q -rs` の summary 行と
       `-rs` が出す skip 理由行を貼る。期待値は `656 passed, 1 skipped`（Phase 27.1 完了時点および
       本フェーズ着手時点の実測。テスト数が増えていればそれ以上でもよい。`failed` が 1 件でも
       あれば停止条件）。**この `1 skipped` の正体を証跡の中で明示的に名指しすること** —
       `tests/test_corpus_gate.py` の `test_empty_url_before_after`（`TYPSPHINX_CORPUS_REPORT` が
       `"1"` でない限り常に skip する計測用テスト）であり、`TestCorpusRenderGate` とは無関係で
       SC#3 の合否には一切影響しない。これを書かないと後続の読者が「コーパスゲートがまた skip した」
       と誤読する。`-m "not slow"` を付けてはいけない（slow マーカーのテストも含めた合計が
       D-05 の言うフルスイート）。

    3. `## SC#3 Evidence — Docs Builds` に、`uv run tox -e docs-pdf` と `uv run tox -e docs-multilang`
       の出力を**環境ごとに別の小節**で貼る。両者のベースラインは別物であり、共通の 1 つの数字で
       判定してはいけない:
       - `docs-pdf` は英語のみの単一言語ビルドで、構造上 2 行までしか出ない。基準は
         `build succeeded, 2 warnings.` 以下。既知の 2 行は `typsphinx/translator.py` の
         `visit_toctree` docstring 由来（`:7` の `ERROR: Unexpected indentation.` と
         `:8` の `WARNING: Block quote ends without a blank line; unexpected unindent.`）で、
         本フェーズのスコープ外の既存欠陥である。ゼロではない。
       - `docs-multilang` は en + ja の 2 言語 HTML ビルドで、同じ 2 行が言語ごとに出るため
         合計 4 行が基準。`28-CONTEXT.md` D-06 の「4 行」はこちらの値である。
       それぞれ、ビルド出力をファイルに落として警告/エラー行を数えるコマンドとその数値出力を
       逐語で記録する。証跡には機械可読な形の集計行を 1 行ずつ含めること —
       `docs-pdf warning lines: 2` と `docs-multilang warning lines: 4` という書式で、実測値を
       書く（増えていたらその実測値を正直に書き、増分の出所を特定して報告する）。加えて生の
       警告行そのものも貼る。D-06 のとおり行数を assert するテストは作らない。

    D-08 の記録: ja の PDF での目視確認は行わない。実測上 `docs-pdf` が作る PDF は英語
    （`docs/source` の conf に `language` 設定なし）、`docs-multilang` は en/ja とも HTML のみで、
    「ja の PDF で『表 N』が出る」はどの tox 環境でも観測できない。Phase 27.1 の GATE-01 フィクスチャ
    （ja の `.typ` ソース証明 + de の PDF 抽出、計 21 テスト）がこれを機械的に固めており、それは
    上記フルスイートの一部として回っている。この判断を証跡に 1 行で記録すること。

    NixOS サンドボックス注意（`28-RESEARCH.md` Pitfall 6）: `docs/build_multilang.py` は
    `sys.executable -m sphinx` ではなく PATH 上の `sphinx-build` バイナリを `subprocess.run` で
    直接呼ぶ。`uv run tox -e docs-multilang` 経由なら前セッションで完走を実測済みだが、ELF 実行
    エラーに当たった場合は `nix-shell` 経由の代替を検討し、その事実を証跡に記録する
    （`build_multilang.py` 自体の書き換えは本フェーズのスコープ外）。

    スコープ柵（ROADMAP SC#5、絶対）: タグを作らない・push しない。`uv publish` / `twine` /
    `gh release create` / `git push --tags` を実行しない。`.github/workflows/release.yml` を編集も
    起動もしない。`tests/` 配下に 1 バイトも書かない（D-06 がテスト基盤の拡張をスコープ外と
    裁定している）。`28-GATE.md` のような専用レポートファイルを別途作らない — 証跡は
    `28-VERIFICATION.md` に集約する（Phase 18 / 23 の確立済みパターン）。
  </action>
  <acceptance_criteria>
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VERIFICATION.md` が存在する
    - 同ファイルに見出し `## SC#3 — Full-Corpus Regression Gate` が含まれる
    - 同ファイルに文字列 `TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error` が含まれる（実行コマンドが逐語で貼られている証拠）
    - 同ファイルに `-m slow -rs` が含まれる
    - 同ファイルが `grep -Ec '1 passed in [0-9]+\.[0-9]+s'` で 1 以上を返す（コーパスゲートの summary 行が PASS を示している。skip なら pytest は `1 skipped in …` と印字するのでこの正規表現には一致しない）
    - 同ファイルに `Unknown Visit Catalogue: []` が含まれる
    - 同ファイルに `Corpus tag:` が含まれる
    - 同ファイルに見出し `## SC#3 Evidence — Full pytest Suite` が含まれる
    - 同ファイルが `grep -Ec '^[0-9]+ passed, [0-9]+ skipped in [0-9.]+s$'` で 1 以上を返す（フルスイートの summary 行。失敗があれば pytest は行頭を `N failed,` にするのでこの錨付き正規表現には一致しない）
    - 同ファイルに `test_empty_url_before_after` と `TYPSPHINX_CORPUS_REPORT` の両方が含まれる（フルスイートの単一 skip の正体が名指しで特定されている）
    - 同ファイルに見出し `## SC#3 Evidence — Docs Builds` が含まれる
    - 同ファイルに `docs-pdf warning lines:` で始まる行が正確に 1 行含まれる
    - 同ファイルに `docs-multilang warning lines:` で始まる行が正確に 1 行含まれる
    - 同ファイルに `build succeeded` が含まれる
    - 同ファイルに `visit_toctree` が含まれる（既知の既存警告の出所が記録されている）
    - `ls .planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-GATE.md` が失敗する
    - `git status --porcelain tests/ typsphinx/ docs/ examples/` が空
    - `git tag --list 'v0.6.3'` が空
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error -m slow -rs -v -s &amp;&amp; test "$(git status --porcelain tests/ typsphinx/ docs/ examples/)" = "" &amp;&amp; test "$(git tag --list 'v0.6.3')" = "" &amp;&amp; ! ls .planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-GATE.md &amp;&amp; uv run python -c "import pathlib,re,sys;p=pathlib.Path('.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VERIFICATION.md');t=p.read_text(encoding='utf-8');e=[];[e.append('missing:'+s) for s in ['## SC#3 — Full-Corpus Regression Gate','TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error','-m slow -rs','Unknown Visit Catalogue: []','Corpus tag:','## SC#3 Evidence — Full pytest Suite','test_empty_url_before_after','TYPSPHINX_CORPUS_REPORT','## SC#3 Evidence — Docs Builds','build succeeded','visit_toctree'] if s not in t];e.append('no-gate-passed-line') if not re.search(r'1 passed in [0-9]+\.[0-9]+s',t) else None;e.append('no-suite-summary-line') if not re.search(r'^[0-9]+ passed, [0-9]+ skipped in [0-9.]+s$',t,re.M) else None;e.append('docs-pdf-count-line') if len(re.findall(r'^docs-pdf warning lines:',t,re.M))!=1 else None;e.append('docs-multilang-count-line') if len(re.findall(r'^docs-multilang warning lines:',t,re.M))!=1 else None;print('ERRORS:',e);sys.exit(1 if e else 0)"</automated>
  </verify>
  <done>
    `28-VERIFICATION.md` が存在し、コーパスゲート（`1 passed in N.NNs`、`Unknown Visit Catalogue: []`、
    実ビルド相当の所要時間）・フルスイート（`N passed, M skipped` の summary と単一 skip の名指し特定）・
    docs ビルド 2 環境（環境別の集計行と生の警告行）の 3 点が逐語で記録されている。`tests/` /
    `typsphinx/` / `docs/` / `examples/` は無変更、`28-GATE.md` は作られておらず、タグも存在しない。
  </done>
</task>

<task type="auto">
  <name>Task 2: SC#4 不変量の git diff と SC#5 スコープ柵の否定 assert を 28-VERIFICATION.md に追記し、Observable Truths 表を完成させる</name>
  <files>.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VERIFICATION.md</files>
  <read_first>
    - Task 1 が作った `28-VERIFICATION.md`（現在の中身。追記先の構造を実物で確認する）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-RESEARCH.md`
      § "SC#4 Invariant Verification Commands"（4 本のコマンドと前セッションの実出力。base ref が
      `main` であること、`git merge-base main HEAD` == `main` == `9f8e075` であることの一次記録）
    - `.planning/ROADMAP.md` § Phase 28 の SC#4 と、v0.6.3 冒頭の
      "Invariant amendment (2026-07-25, owner decision — Phase 27.1 only)"
      （`base.typ` byte-unchanged が Phase 27.1 に限り解除された経緯）
    - `.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-CONTEXT.md` の D-07 / D-08
      （SC#4 の確認手段と、sha256 基準値を採らない理由）
    - `typsphinx/templates/base.typ` の 40〜70 行（`project()` シグネチャと `set text(...)` 行 —
      diff が何に限定されているべきかを実物で確認する。**読むだけ**）
    - `.planning/milestones/v0.6.2-phases/23-v0-6-2-release-prep-regression-gate-close/23-VERIFICATION.md`
      の SC#4 節（base-ref 限定 diff をそのまま貼り、自然文で結論を添える書き方）
  </read_first>
  <action>
    `28-VERIFICATION.md` に `## SC#4 — Milestone Invariants` と `## SC#5 — Scope Fence` の 2 節を
    追記し、最後に `## Observable Truths` 表を埋めて完成させる。base ref は **`main` ブランチ**であり、
    `v0.6.2` タグではない（実測: `git merge-base main HEAD` == `main` == `9f8e075`。`main` は
    `v0.6.2` タグより 3 コミット先だが、その 3 件は依存更新と STATE.md 整理のみで `typsphinx/` とは無関係）。

    `## SC#4 — Milestone Invariants` に、次の 4 本のコマンドラインと実出力を逐語で貼る:

    1. `git diff main..HEAD -- pyproject.toml` — **前セッションの実測（完全に空）とは結果が変わる。**
       プラン 28-01 が版リテラルを 0.6.2 → 0.6.3 に動かしているので、この diff は**その 1 行の対のみ**を
       含むのが期待値である。SC#4 が問うているのは「新規ランタイム依存ゼロ」なので、判定基準は
       `dependencies = [` 配列の内側に追加行も削除行も無いこと、および `requires-python` が
       無変更であること。版リテラル 1 行の変化は SC#1 が要求した変更そのものであって SC#4 違反ではない
       — 証跡でこの区別を明示的に書くこと。
    2. `git diff main..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ | grep -E '^[+-].*@preview'` —
       期待値は出力なし（exit 1 = マッチなし）。`@preview` 4 パッケージ（`codly:1.3.0`,
       `codly-languages:0.1.10`, `mitex:0.2.7`, `gentle-clues:1.3.1`）の版文字列が 3 ファイルすべてで
       未変更であることの直接証明。grep が何も出さなかったという事実そのものを証跡に書く。
    3. `git diff main..HEAD -- typsphinx/templates/base.typ` — 期待値は正確に 2 行の変化
       （`project()` への `lang: "en",` の追加 1 行と、`set text(size: fontsize, lang: "en")` から
       `set text(size: fontsize, lang: lang)` への変更 1 行）。diff 全文をそのまま貼る。
       `git diff --numstat main..HEAD -- typsphinx/templates/base.typ` の数値出力も併記して、
       追加 2 / 削除 1 という形（追加行 1 本 + 変更行の +/- 対）を機械可読に残す。
       ROADMAP の 2026-07-25 不変量改訂が許した範囲そのものであることを自然文で結論づける。
    4. `uv run python -m pytest tests/test_preview_version_sync.py -v` — `@preview` の 3-way 同期面の
       担保。Task 1 のフルスイートに含まれてはいるが、SC#4 の証跡としては単独実行の出力を貼る。

    **D-07 の否定的記録:** `base.typ` の sha256 を新しい基準値として記録する案は採らない。以降の
    フェーズが `base.typ` を意図的に変えるたびに更新が必要になるためであり、D-07 が名指しで却下している。
    証跡にこの判断を 1 行で書き、将来の読者が「基準値が抜けている」と誤解しないようにする。

    `## SC#5 — Scope Fence` に、次の否定 assert のコマンドと出力を貼る:
    `git tag --list 'v0.6.3'`（空であること）、`git status --porcelain .github/workflows/release.yml`
    （空であること）、`git status --porcelain typsphinx/ tests/ docs/ examples/`（空であること）、
    そして本フェーズが触ったファイルの全量を示す `git diff --name-only main..HEAD` のうち
    Phase 28 分のコミットが列挙するパスが `pyproject.toml` / `uv.lock` / `README.md` /
    `.planning/` 配下に限られていることを自然文で結論づける。`git tag` / `release.yml` 起動 / PyPI /
    GitHub Release / マージはいずれも `/gsd-complete-milestone` の仕事であり本フェーズでは行わない、
    と明記する。

    最後に `## Observable Truths` 表を埋める。列は `| # | Truth | Status | Evidence |` とし
    （`23-VERIFICATION.md` と同じ構成）、SC#1〜SC#5 と、証跡に関わる D-05 / D-06 / D-07 / D-08 を
    行として並べる。`Evidence` 欄には実際のコマンドと観測値を書く — 「確認した」ではなく
    「このコマンドがこの出力を出した」の形で書く。CHANGELOG（SC#2）はまだ wave 3 で未着手なので、
    その行の Status は未確定である旨を正直に書き、`/gsd-verify-work` が埋める前提にする。
    frontmatter の `score` と `status` は、この時点で確認できた項目数を正直に反映させる。

    スコープ柵（ROADMAP SC#5）: 依然としてタグなし・publish なし・Release なし・`release.yml` への
    一切の関与なし。`tests/` にも 1 バイトも書かない。
  </action>
  <acceptance_criteria>
    - `28-VERIFICATION.md` に見出し `## SC#4 — Milestone Invariants` が含まれる
    - 同ファイルに見出し `## SC#5 — Scope Fence` が含まれる
    - 同ファイルに見出し `## Observable Truths` が含まれ、その下に `| # | Truth | Status | Evidence |` という表ヘッダ行が存在する
    - 同ファイルに `git diff main..HEAD -- typsphinx/templates/base.typ` が逐語で含まれる
    - 同ファイルに `set text(size: fontsize, lang: lang)` が含まれる（`base.typ` diff が実際に貼られている証拠）
    - 同ファイルに `git diff main..HEAD -- pyproject.toml` が含まれる
    - 同ファイルに `@preview` が含まれる（不変量 2 のコマンドと結論が記録されている）
    - 同ファイルに `test_preview_version_sync.py` が含まれる
    - 同ファイルに `sha256` が含まれる（D-07 が sha256 基準値を採らないという判断の明示的記録）
    - 同ファイルに `git tag --list 'v0.6.3'` が含まれる
    - 同ファイルに `merge-base` または `9f8e075` が含まれる（base ref が `main` であることの根拠が記録されている）
    - `git diff --numstat main..HEAD -- typsphinx/templates/base.typ` の出力が `2<TAB>1<TAB>typsphinx/templates/base.typ` である
    - `git diff main..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ | grep -c '^[+-].*@preview'` が `0` を返す
    - `git diff main..HEAD -- pyproject.toml | grep -Ec '^[+-] *"(sphinx|docutils|typst)>='` が `0` を返す（`dependencies` 配列は無変化）
    - `uv run python -m pytest tests/test_preview_version_sync.py -v` が exit 0
    - `git tag --list 'v0.6.3'` が空
    - `git status --porcelain typsphinx/ tests/ docs/ examples/ .github/` が空
  </acceptance_criteria>
  <verify>
    <automated>test "$(git diff --numstat main..HEAD -- typsphinx/templates/base.typ)" = "$(printf '2\t1\ttypsphinx/templates/base.typ')" &amp;&amp; test "$(git diff main..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ | grep -c '^[+-].*@preview')" = "0" &amp;&amp; test "$(git diff main..HEAD -- pyproject.toml | grep -Ec '^[+-] *"(sphinx|docutils|typst)>=')" = "0" &amp;&amp; uv run python -m pytest tests/test_preview_version_sync.py -q &amp;&amp; test "$(git tag --list 'v0.6.3')" = "" &amp;&amp; test "$(git status --porcelain typsphinx/ tests/ docs/ examples/ .github/)" = "" &amp;&amp; uv run python -c "import pathlib,sys;t=pathlib.Path('.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-VERIFICATION.md').read_text(encoding='utf-8');e=[s for s in ['## SC#4 — Milestone Invariants','## SC#5 — Scope Fence','## Observable Truths','| # | Truth | Status | Evidence |','git diff main..HEAD -- typsphinx/templates/base.typ','set text(size: fontsize, lang: lang)','git diff main..HEAD -- pyproject.toml','@preview','test_preview_version_sync.py','sha256',\"git tag --list 'v0.6.3'\"] if s not in t];e.append('missing-base-ref-note') if ('merge-base' not in t and '9f8e075' not in t) else None;print('MISSING:',e);sys.exit(1 if e else 0)"</automated>
  </verify>
  <done>
    `28-VERIFICATION.md` に SC#4 の 4 本のコマンドと実出力（`pyproject.toml` の差分が版リテラル 1 行に
    限られること、`@preview` 行の grep が無出力であること、`base.typ` の差分が正確に 2 行であること、
    `test_preview_version_sync.py` の緑）、SC#5 の否定 assert 群、D-07 の sha256 不採用の明示的記録、
    そして `| # | Truth | Status | Evidence |` 形式の Observable Truths 表が揃っている。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| キャッシュ済み Sphinx `doc/` コーパス（`~/.cache/typsphinx-corpus-gate/`）→ 回帰ゲートの判定 | ゲートの結論はこのローカルキャッシュの内容に依存する。キャッシュが欠けるとフィクスチャは `pytest.skip` を呼び、exit 0 のまま「証拠ゼロ」を返す |
| ゲートの生ログ → `28-VERIFICATION.md` → CHANGELOG `### Verified` 節（wave 3）→ GitHub Release body | ここに記録した事実は無改変で公開リリースノートの主張になる。誤読や取り違えはそのまま公開される |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-04 | Spoofing | コーパスゲートの `SKIPPED` が `passed` に見える | medium | mitigate | `-rs` 付き実行を必須にし、受入基準を「summary 行が `1 passed in N.NNs` にマッチする」という正の assert にする（skip なら pytest は `1 skipped in …` と印字するのでマッチしない）。加えて所要時間が実ビルド相当（前セッション実測 13.08s）であることを人が読める形で残す |
| T-28-05 | Spoofing | フルスイートの `1 skipped` を「コーパスゲートがまた skip した」と誤読する | medium | mitigate | Task 1 の受入基準が、証跡に `test_empty_url_before_after` と `TYPSPHINX_CORPUS_REPORT` の両方の文字列を要求する。単一 skip の正体が名指しで特定されていない証跡は不合格になる |
| T-28-06 | Repudiation | 証跡が前セッション（`28-RESEARCH.md`）のログのコピーで、今回のツリーに対する実走ではない | medium | mitigate | `must_haves.truths` に backstop 項目として明記。verify コマンドがコーパスゲートを executor の環境で実際に再実行する。SUMMARY にログの二重コピーを置き、`/gsd-verify-work` が `28-VERIFICATION.md` を上書きしても改竄不能な記録が残る |
| T-28-02 | Elevation of Privilege | `.github/workflows/release.yml`（タグ起動の PyPI publish） | high | mitigate | ROADMAP SC#5 のスコープ柵を `must_haves.prohibitions` に符号化し、両タスクの action で再掲。`git tag --list 'v0.6.3'` が空、`git status --porcelain .github/` が空であることで assert する |
| T-28-SC | Tampering | パッケージインストール | low | accept | 本プランはパッケージを新規インストールしない。`28-RESEARCH.md` § "Package Legitimacy Audit" が「該当なし」を記録済み。`uv sync --extra dev` と tox の `uv-venv-lock-runner` は既に宣言済み・レンジピン済みの依存集合を再現するだけ |

**Security posture（`28-RESEARCH.md` § Security Domain の所見をそのまま記録）:** 本プランはリポジトリの
ソースツリーを 1 バイトも変更せず、既存ゲートを実走して `.planning/` 配下に証跡を書くだけである。
ユーザー入力なし、新規ネットワーク呼び出しなし（コーパスはキャッシュヒットでオフライン実行を実測確認済み）、
認証/セッション/アクセス制御面なし、暗号なし、新規テストコードなし。

| ASVS Category | Applies | Standard Control |
|---------------|---------|-------------------|
| V2 Authentication | No | N/A — 認証面に触れない |
| V3 Session Management | No | N/A |
| V4 Access Control | No | N/A |
| V5 Input Validation | No（リポジトリローカルの固定パスファイルとローカルキャッシュを読むだけ） | N/A |
| V6 Cryptography | No | N/A |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error -m slow -rs -v -s` → `1 passed in N.NNs`、`SKIPPED` 行なし、所要時間が実ビルド相当（実測 ~13s）
- `uv run python -m pytest -q -rs` → `N passed, 1 skipped`（`failed` ゼロ。基準値 656 passed / 1 skipped、~57s）
- `uv run tox -e docs-pdf` → `build succeeded, 2 warnings.` 以下
- `uv run tox -e docs-multilang` → 警告行が en/ja 合わせて 4 行以下
- `git diff --numstat main..HEAD -- typsphinx/templates/base.typ` → `2	1	typsphinx/templates/base.typ`
- `git diff main..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ | grep '@preview'` → 出力なし
- `git tag --list 'v0.6.3'` が空、`git status --porcelain typsphinx/ tests/ docs/ examples/ .github/` が空
</verification>

<success_criteria>
- SC#3 が満たされ、その根拠がゲートの生ログとして `28-VERIFICATION.md` に逐語で残っている
- D-05 の 3 点（コーパスゲート / フルスイート / docs ビルド）がすべて記録されている
- D-06 の合否基準が**環境ごとに別々**（`docs-pdf` は 2 行、`docs-multilang` は 4 行）に記録され、
  行数を assert するテストは作られていない
- SC#4 が満たされ、4 本の `git diff` / pytest の実出力が貼られている。D-07 の sha256 不採用も明記
- D-08 の ja PDF 目視確認不採用が理由つきで記録されている
- SC#5 が保持: タグなし・publish なし・Release なし・`release.yml` への関与なし・`tests/` 無変更
</success_criteria>

<output>
`.planning/phases/28-v0-6-3-release-prep-regression-gate-close/28-02-SUMMARY.md` を作成する。
コーパスゲートの生ログを逐語で二重コピーすること（`/gsd-verify-work` が `28-VERIFICATION.md` を
上書きしても失われない、executor 所有の記録として）。docs ビルド 2 環境の集計行も併記する。
</output>
