---
phase: 27-docs-orphan-delete-phantom-config-names
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/configuration.rst
  - tests/test_documentation_configuration.py
  - docs/source/user_guide/configuration.rst
  - docs/source/api/index.rst
  - docs/locale/ja/LC_MESSAGES/api/index.po
  - docs/locale/ja/LC_MESSAGES/user_guide/configuration.po
autonomous: true
requirements: [DOC-06, DOC-07]
user_setup:
  - service: git (manual merge — deletion guard)
    why: "This branch DELETES tracked files (docs/configuration.rst + tests/test_documentation_configuration.py). The worktree.cleanup-wave gate blocks any deletion-bearing branch with NO bypass (CONTEXT.md D-13). The branch is merged MANUALLY after scope is confirmed — do NOT expect cleanup-wave to auto-pass."
    dashboard_config:
      - task: "After execution: confirm the deletion scope (2 files removed, listed in the SUMMARY), then merge the plan branch by hand at ship time. Worktree-isolated execution stays ON."
        location: "Local git / /gsd-ship step"

must_haves:
  truths:
    - "The orphan docs/configuration.rst no longer exists (git rm — D-03), and its collateral test tests/test_documentation_configuration.py is deleted in the SAME wave (RESEARCH Pitfall 1: the test's 11 functions read_text() the orphan — deleting one without the other turns uv run pytest red)."
    - "No live inbound :doc:/:ref: reference to the deleted orphan remains: the 3 relative :doc:`configuration` refs under docs/source/user_guide/ (builders.rst, index.rst, templates.rst) resolve to the SURVIVING canonical docs/source/user_guide/configuration.rst, not the orphan (the orphan lives in the dead docs/ root tree that has no conf.py and is never built)."
    - "In docs/source/user_guide/configuration.rst the phantom set is gone: typst_use_codly, typst_code_line_numbers (codly knobs, D-06), the typst_author tuple form (D-07), and top-level typst_papersize/typst_fontsize (D-08) grep to zero."
    - "papersize/fontsize are documented as the working typst_elements = {\"papersize\": \"us-letter\", \"fontsize\": \"20pt\"} (D-02 allowlist keys ONLY — papersize string, fontsize raw length); typst_use_mitex = True is kept in the Complete Example (D-09)."
    - "The Available Configuration Values list-table is removed from docs/source/api/index.rst (D-10); the Configuration heading + intro and the See :doc:`/user_guide/configuration` pointer survive so config lives in ONE canonical place."
    - "docs/locale/ja/LC_MESSAGES/api/index.po follows the api-table deletion via the SCOPED sphinx-intl regen; no LIVE (non-#~) msgid references a deleted phantom name (removed msgids become inert #~-commented obsolete entries — normal)."
    - "SC#4 (D-12 proof bar, part b): every surviving typst_* token under docs/source/ is one of the 11 registered names (typst_documents, typst_template, typst_template_mapping, typst_use_mitex, typst_elements, typst_package, typst_package_imports, typst_template_function, typst_authors, typst_debug, typst_template_assets)."
    - "SC#5 (D-12 proof bar, parts c+d): uv run pytest is green post-deletion (test_documentation_configuration.py no longer collected); uv run tox -e docs-multilang + uv run tox -e docs-pdf build with no NEW sphinx-build WARNING beyond the 1 pre-existing translator.py docstring-spacing baseline."
  artifacts:
    - "docs/source/user_guide/configuration.rst (modified — phantom-free, working typst_elements example)"
    - "docs/source/api/index.rst (modified — list-table removed, pointer retained)"
    - "docs/locale/ja/LC_MESSAGES/api/index.po (regenerated to follow)"
    - "docs/locale/ja/LC_MESSAGES/user_guide/configuration.po (regenerated to follow)"
  key_links:
    - "api/index.rst -> user_guide/configuration via the surviving See :doc:`/user_guide/configuration` pointer stays valid after the list-table deletion (SC#5)."
    - "user_guide/configuration.rst papersize/fontsize example -> the CONF-04 allowlist (template_engine.py:44-56) — the documented typst_elements keys are self-verifying against the code that validates them."
  prohibitions:
    - "Do NOT bulk-migrate the orphan's 489 lines back into the canonical doc — re-drift (D-05); the orphan even documents phantom mainfont/monofont typst_elements keys that the CONF-04 allowlist rejects, so its content is unreliable. Default = salvage nothing."
    - "Do NOT rewrite the tuple author form into typst_authors = (tuple) — a tuple is type-invalid for typst_authors, that mints a NEW phantom (D-07). Delete the Simple Format subsection; keep the Detailed Format dict."
    - "Do NOT introduce top-level typst_papersize / typst_fontsize anywhere — they are unregistered (D-08); typst_elements is the only faithful vehicle."
    - "Do NOT touch the sibling tests/test_documentation_usage.py or tests/test_documentation_installation.py — they assert on still-present dead files (docs/usage.rst / docs/installation.rst), out of DOC-06 scope."
    - "Do NOT run the bare unscoped sphinx-build -b gettext source _build/gettext — it sweeps pre-existing unrelated drift (user_guide/builders.po, examples/advanced.po) into this diff (RESEARCH Pitfall 3). Use the two-filename scoped invocation."
    - "Do NOT hand-delete or hand-strip #~-commented obsolete msgids, and do NOT plan around GNU msgattrib/msgmerge/msgfmt — those binaries are ABSENT in this sandbox (RESEARCH Anti-Patterns). Any grep-zero assertion against the .po MUST exclude #~ lines."
    - "Milestone invariant: zero new runtime deps, no @preview version bump, the 3-way version-sync surface (writer.py / template_engine.py / templates/base.typ) untouched (base.typ byte-unchanged). GATE-01 does NOT apply — docs-only, no config->output change."
---

<!-- planner-discipline-allow: typst_use_codly -->
<!-- planner-discipline-allow: typst_code_line_numbers -->
<!-- planner-discipline-allow: typst_papersize -->
<!-- planner-discipline-allow: typst_fontsize -->
<!-- planner-discipline-allow: typst_author -->
<!-- planner-discipline-allow: list-table -->

<objective>
Truth-align the user-facing docs so every documented `typst_*` name maps to a value actually
registered in `typsphinx/__init__.py` (DOC-07), delete the unreachable orphan config doc and its
collateral test (DOC-06), and collapse config documentation to ONE canonical place
(`docs/source/user_guide/configuration.rst`) so it cannot re-drift.

This is a docs-only, content-fidelity phase. No `typsphinx/*.py` source changes → **GATE-01 does
not apply** (per ROADMAP). The honest bar is grep-zero / grep-cross-check proof + green docs build
+ green test suite — not a `typst.compile()` regression fixture.

Purpose: A user who copies a documented `conf.py` example gets what the docs promise. Today the
docs advertise 5 phantom config names (`typst_use_codly`, `typst_code_line_numbers`, a
`typst_author` tuple, `typst_papersize`, `typst_fontsize`) that do not exist, plus an orphan
config doc using the wrong package name (`sphinxcontrib.typst`).

Output: 2 tracked-file deletions + 2 `.rst` content edits + 2 regenerated `.po` catalogs.
Zero new files, zero new symbols.

**⚠ Deletion-guard (CONTEXT.md D-13 — STANDING project constraint):** this plan `git rm`s two
tracked files. The `worktree.cleanup-wave` gate **blocks any deletion-bearing branch with no
bypass** — recurrence is certain for Phase 27. The branch is **merged manually after scope
confirmation** at ship time; do not assume the cleanup-wave gate auto-passes. Worktree-isolated
execution stays ON (standing mode): provision the worktree with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and run everything via `uv run`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-docs-orphan-delete-phantom-config-names/27-CONTEXT.md
@.planning/phases/27-docs-orphan-delete-phantom-config-names/27-RESEARCH.md
@.planning/phases/27-docs-orphan-delete-phantom-config-names/27-VALIDATION.md

# Config source-of-truth (the anti-phantom oracle)
@typsphinx/__init__.py
@typsphinx/template_engine.py
</context>

## Artifacts this phase produces

This phase produces **NO new symbols and NO new files**. It is deletions + modifications only:

| File | Action |
|------|--------|
| `docs/configuration.rst` | **DELETE** (orphan, 489 lines, wrong package name `sphinxcontrib.typst`, no `conf.py` in its tree — never built) |
| `tests/test_documentation_configuration.py` | **DELETE** (collateral — 11 functions hard-assert the orphan exists; RESEARCH Pitfall 1) |
| `docs/source/user_guide/configuration.rst` | **MODIFY** (remove 5 phantom names; rewrite papersize/fontsize as working `typst_elements`) |
| `docs/source/api/index.rst` | **MODIFY** (delete the `Available Configuration Values` list-table; keep the `See :doc:` pointer) |
| `docs/locale/ja/LC_MESSAGES/api/index.po` | **REGEN** (follows the api/index.rst edit) |
| `docs/locale/ja/LC_MESSAGES/user_guide/configuration.po` | **REGEN** (follows the user_guide/configuration.rst edit) |

<tasks>

<task type="auto">
  <name>Task 1: Delete the orphan docs/configuration.rst + its collateral test file (same wave)</name>
  <files>docs/configuration.rst, tests/test_documentation_configuration.py</files>
  <read_first>
    - docs/configuration.rst (the 489-line orphan being deleted — skim headings to confirm salvage-nothing; note it uses the wrong package name `sphinxcontrib.typst` at lines 20/299/449)
    - tests/test_documentation_configuration.py (11 functions that read_text() the orphan; they go red the instant the orphan is deleted — RESEARCH Pitfall 1)
    - docs/source/user_guide/configuration.rst (the canonical doc — the salvage-comparison target)
    - typsphinx/template_engine.py (CONF-04 allowlist at lines 44-56 — proves the orphan's mainfont/monofont typst_elements example is itself phantom, reinforcing salvage-nothing)
  </read_first>
  <action>
    Perform the D-05 salvage-CHECK FIRST, then delete BOTH files in this one task (they MUST be
    same-wave per RESEARCH Pitfall 1). Salvage-CHECK: diff the orphan's section headings against
    docs/source/user_guide/configuration.rst — the orphan's only "extra" registered names are
    typst_template_mapping / typst_package_imports / typst_debug (undocumented in the canonical
    doc but out of DOC-06/DOC-07 scope — filed as a follow-up, NOT added here per D-05/Open
    Questions), and its typst_elements example uses phantom mainfont/monofont keys the CONF-04
    allowlist rejects. Record a one-line "salvage nothing" decision in the SUMMARY citing:
    superseded content + wrong package name (sphinxcontrib.typst) + phantom mainfont/monofont keys.
    Do NOT bulk-copy the 489 lines back (re-drift — D-05). Then run `git rm docs/configuration.rst`
    and `git rm tests/test_documentation_configuration.py`. Do NOT touch the sibling
    tests/test_documentation_usage.py or tests/test_documentation_installation.py — they assert on
    docs/usage.rst / docs/installation.rst (still-present dead files, out of scope).
  </action>
  <verify>
    <automated>test ! -f docs/configuration.rst && test ! -f tests/test_documentation_configuration.py && uv run pytest tests/test_documentation_usage.py tests/test_documentation_installation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/configuration.rst` no longer exists on disk (git rm staged).
    - `tests/test_documentation_configuration.py` no longer exists on disk (git rm staged).
    - `uv run pytest --collect-only -q 2>&1 | grep -c test_documentation_configuration` returns 0 (no longer collected).
    - `uv run pytest tests/test_documentation_usage.py tests/test_documentation_installation.py -q` passes (siblings untouched and green).
    - The SUMMARY records a "salvage nothing" decision citing superseded content + wrong package name + phantom mainfont/monofont keys.
  </acceptance_criteria>
  <done>Orphan and its collateral test are removed together; the two sibling doc-existence tests stay green; the salvage decision is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Remove phantom config names from user_guide/configuration.rst + delete the api/index.rst list-table</name>
  <files>docs/source/user_guide/configuration.rst, docs/source/api/index.rst</files>
  <read_first>
    - docs/source/user_guide/configuration.rst (the canonical config doc being edited — read the whole file; the four edit sites are the Code Highlighting section ~144-160, the Simple Format author subsection ~165-171, the Paper Size and Format section ~192-200, and the Complete Example ~243-246)
    - docs/source/api/index.rst (the api page — the Configuration section is lines 40-86; the list-table to delete is lines 45-85, the See pointer to keep is line 86)
    - typsphinx/__init__.py (the 11 registered typst_* names at lines 44-60 — D-01 legal set)
    - typsphinx/template_engine.py (typst_elements allowlist at 44-56: papersize=string, fontsize=raw length — D-02)
  </read_first>
  <action>
    Edit docs/source/user_guide/configuration.rst with four changes (D-06..D-09):
    (a) D-06: delete the entire "Code Highlighting" section — from the `Code Highlighting` heading
        (~line 144) through the second code-block whose body is the `typst_code_line_numbers`
        assignment (~line 160), so neither codly knob name survives. Do NOT invent a "codly is
        applied automatically" note (that documents unverified internal behavior — RESEARCH State
        of the Art / D-06).
    (b) D-07: delete the "Simple Format" author subsection — from the `Simple Format` heading
        (~line 165) through its code-block whose body is the two-name tuple author assignment
        (~line 171). Keep the existing "Detailed Format" typst_authors dict block (~172-190) as the
        SOLE author example. Do NOT convert the tuple into `typst_authors = (tuple)` — a tuple is
        type-invalid for typst_authors (new phantom).
    (c) D-08: under the "Paper Size and Format" heading (~line 192), replace the phantom code-block
        body (the two top-level papersize/fontsize assignment lines, ~197-200) with the single
        working block `typst_elements = {"papersize": "us-letter", "fontsize": "20pt"}` — ONLY the
        two D-02 allowlist keys (papersize a quoted string, fontsize a quoted raw length). Do NOT
        introduce top-level typst_papersize / typst_fontsize. You may add one sentence of prose
        noting these are the only two supported typst_elements keys (discretion, within D-01/D-02).
    (d) D-09: in the "Complete Example" code-block (~205-241), drop the two phantom trailing lines
        (the codly-enable and code-line-numbers assignments, ~245-246) and KEEP
        `typst_use_mitex = True` (~244). Optionally adjust the `# Math and code` comment to `# Math`
        and/or add a typst_elements demo line using only papersize/fontsize (discretion).
    Edit docs/source/api/index.rst per D-10: delete the "Available Configuration Values" subsection
    — the sub-heading + its `~` underline (~45-46) and the whole `.. list-table::` directive through
    its last data row (~48-84) plus the trailing blank line (~85). KEEP the "Configuration" heading
    + intro sentence (~40-43) and the `See :doc:` pointer to `/user_guide/configuration` (~86).
    Result: the Configuration section becomes heading + one intro sentence + the See pointer.
  </action>
  <verify>
    <automated>! grep -nE 'typst_use_codly|typst_code_line_numbers|typst_papersize|typst_fontsize' docs/source/user_guide/configuration.rst docs/source/api/index.rst && ! grep -nE 'typst_author\b' docs/source/user_guide/configuration.rst && grep -qF 'typst_elements = {"papersize": "us-letter", "fontsize": "20pt"}' docs/source/user_guide/configuration.rst && grep -q 'typst_use_mitex = True' docs/source/user_guide/configuration.rst && ! grep -q 'list-table' docs/source/api/index.rst && grep -q ':doc:' docs/source/api/index.rst</automated>
  </verify>
  <acceptance_criteria>
    - Over BOTH files, grep for the four code/papersize phantom names returns zero matches.
    - Over user_guide/configuration.rst, `grep -nE 'typst_author\b'` returns zero (the tuple form is gone); `typst_authors` (dict) is still present.
    - The literal `typst_elements = {"papersize": "us-letter", "fontsize": "20pt"}` is present in user_guide/configuration.rst.
    - `typst_use_mitex = True` is still present in the Complete Example.
    - api/index.rst contains no `list-table`; it still contains a `:doc:` (the surviving `See :doc:`/user_guide/configuration`` pointer).
  </acceptance_criteria>
  <done>Both `.rst` surfaces are phantom-free; papersize/fontsize are documented as a working typst_elements example; the api config table is replaced by the single canonical-doc pointer.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the scoped .po catalogs + prove the phase green (SC#4 cross-check, pytest, docs builds)</name>
  <files>docs/locale/ja/LC_MESSAGES/api/index.po, docs/locale/ja/LC_MESSAGES/user_guide/configuration.po</files>
  <read_first>
    - docs/locale/ja/LC_MESSAGES/api/index.po (the ja catalog that must follow the api-table deletion; every relevant msgstr is empty today — deletion loses zero translation)
    - docs/source/conf.py (locale_dirs = ../locale, gettext_compact / gettext_auto_build settings)
    - .github/workflows/docs.yml (the CI gate this mirrors: tox -e docs-multilang + tox -e docs-pdf)
    - typsphinx/__init__.py (registered set lines 44-60 — the SC#4 cross-check oracle)
  </read_first>
  <action>
    Runs AFTER Task 2's `.rst` edits (the scoped gettext extraction reads their current content).
    Regenerate the ja catalogs SCOPED to the two edited docs (RESEARCH Pitfall 3 — the bare
    unscoped `sphinx-build -b gettext source _build/gettext` sweeps pre-existing unrelated drift in
    user_guide/builders.po and examples/advanced.po into this diff). From the `docs/` directory run,
    in order: `uv run sphinx-build -b gettext source _build/gettext source/api/index.rst
    source/user_guide/configuration.rst`, then `uv run --extra docs sphinx-intl update -p
    _build/gettext -d locale -l ja`. Stage ONLY locale/ja/LC_MESSAGES/api/index.po and
    locale/ja/LC_MESSAGES/user_guide/configuration.po. If `git status --short` shows
    user_guide/builders.po or examples/advanced.po changed, the unscoped command was used by
    mistake — `git checkout` those and re-run scoped. Removed msgids become inert `#~`-commented
    obsolete entries (normal Babel merge semantics — do NOT hand-delete them; msgattrib/msgmerge are
    absent in this sandbox). Then run the phase gate: (1) SC#4 grep cross-check that every `typst_*`
    token under docs/source/ is one of the 11 registered names; (2) full `uv run pytest`
    (test_documentation_configuration.py must no longer be collected — pre-existing NixOS-sandbox
    integration failures documented in project memory are NOT regressions, but there must be zero
    NEW failure attributable to this phase); (3) `uv run tox -e docs-multilang` and `uv run tox -e
    docs-pdf`, capturing WARNING output. Baseline = exactly ONE pre-existing warning (translator.py
    docstring block-quote spacing, unrelated); any NEW "nonexisting document" / "undefined label" /
    "unknown document" warning is a real regression from the api-table deletion. (If the sandbox
    blocks the typst-py compiled path for `docs-pdf`, fall back to proving `docs-multilang` green —
    HTML is the xref/build proof — and record the docs-pdf environmental limitation for CI to
    confirm; RESEARCH lists docs-pdf as the CI-mirror gate.)
  </action>
  <verify>
    <automated>test -z "$(grep -ohE '\btypst_[a-z_]+\b' docs/source/user_guide/configuration.rst docs/source/api/index.rst | sort -u | grep -vxE 'typst_(documents|template|template_mapping|use_mitex|elements|package|package_imports|template_function|authors|debug|template_assets)')" && ! (grep -E 'typst_papersize|typst_fontsize|typst_use_codly|typst_code_line_numbers' docs/locale/ja/LC_MESSAGES/api/index.po | grep -v '#~' | grep -q .) && test -z "$(git status --short docs/locale/ja/LC_MESSAGES/user_guide/builders.po docs/locale/ja/LC_MESSAGES/examples/advanced.po)"</automated>
  </verify>
  <acceptance_criteria>
    - SC#4: `grep -ohE '\btypst_[a-z_]+\b'` over the two surfaces, uniqued, with the 11 registered names removed, yields an EMPTY remainder (no unregistered survivor).
    - The `.po` phantom check — `grep -E 'typst_papersize|typst_fontsize|typst_use_codly|typst_code_line_numbers' docs/locale/ja/LC_MESSAGES/api/index.po | grep -v '#~'` — returns zero LIVE lines (`#~` obsolete entries excluded per Pitfall 2).
    - `git status --short docs/locale/` shows changes ONLY in `api/index.po` and `user_guide/configuration.po` — builders.po / examples/advanced.po unchanged (Pitfall 3 scope proof).
    - Full `uv run pytest` shows no NEW failure vs. the known NixOS-sandbox baseline; `test_documentation_configuration` is absent from collection.
    - `uv run tox -e docs-multilang` (and `docs-pdf` where the sandbox permits) builds with no NEW `sphinx-build` WARNING beyond the single pre-existing translator.py docstring-spacing warning; specifically no `nonexisting document` / `undefined label` / `unknown document`.
  </acceptance_criteria>
  <done>The ja catalogs follow the edits (scoped, no unrelated drift); every documented `typst_*` is registered; the suite and the docs build are green with no new xref warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan edits static repo-authored `.rst` prose, deletes a dead `.rst` file and a dead
test file, and regenerates a translation catalog via a first-party build tool. No untrusted input
crosses any boundary — `.rst` content is authored in-repo, not supplied at runtime.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | (none) | Docs content + dead-test deletion | low | accept | No attack surface at ASVS L1 — no user-input processing, no auth/session/crypto, no network, no new dependencies (RESEARCH §Security Domain). Nothing to mitigate. |

**Package legitimacy:** N/A — this phase installs zero new packages (pure content edit + one
test-file deletion). No `T-27-SC` supply-chain checkpoint applies; no `pip`/`npm`/`cargo` install
task exists.
</threat_model>

<verification>
## Phase-level checks (all must pass before /gsd-verify-work)

- **DOC-06 (SC#1):** `test ! -f docs/configuration.rst` and `test ! -f tests/test_documentation_configuration.py`; `grep -rn ':doc:\`configuration\`' docs/source/` — the only hits are the 3 relative refs in `docs/source/user_guide/` that resolve to the SURVIVING canonical doc (expected, not a regression); `uv run pytest` green post-deletion.
- **DOC-07 A (SC#2):** phantom grep-zero (`typst_use_codly`, `typst_code_line_numbers`, `typst_author` tuple, `typst_papersize`, `typst_fontsize`) over `user_guide/configuration.rst`; the working `typst_elements = {"papersize": "us-letter", "fontsize": "20pt"}` present.
- **DOC-07 B (SC#3):** no `list-table` in `api/index.rst`; `See :doc:` pointer retained; `api/index.po` regenerated scoped, no live phantom msgid.
- **SC#4:** every surviving `typst_*` under `docs/source/` ∈ the 11 registered names.
- **SC#5:** `uv run tox -e docs-multilang` (+ `docs-pdf` where sandbox permits) green; no NEW `sphinx-build` WARNING beyond the 1 pre-existing translator.py baseline.

## ⚠ Deletion-guard / manual-merge (D-13) — surfaced for the executor + ship step

This plan `git rm`s two tracked files. The `worktree.cleanup-wave` gate blocks the deletion-bearing
branch with **no bypass** (recurrence certain for Phase 27). Expect the automatic cleanup-wave merge
to **halt**; the branch is merged **manually** after the SUMMARY's deletion scope (2 files) is
confirmed. This is expected, not a failure. Worktree isolation stays ON — provision per-worktree
with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and run all commands via
`uv run`.
</verification>

<success_criteria>
- `docs/configuration.rst` and `tests/test_documentation_configuration.py` deleted; `uv run pytest` green; salvage-nothing decision recorded (DOC-06 / SC#1).
- `user_guide/configuration.rst` phantom-free; papersize/fontsize documented as working `typst_elements`; `typst_use_mitex` kept (DOC-07 A / SC#2).
- `api/index.rst` list-table deleted, `See :doc:` pointer retained; `api/index.po` follows via scoped regen, no live phantom msgid (DOC-07 B / SC#3).
- Every documented `typst_*` under `docs/source/` is registered (SC#4).
- `tox -e docs-multilang` (+ `docs-pdf`) green; no new broken `:doc:`/`:ref:` warning (SC#5).
- Milestone invariant held: zero new runtime deps, no `@preview` bump, `base.typ` byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/27-docs-orphan-delete-phantom-config-names/27-01-SUMMARY.md` when done.
Record: the 2-file deletion scope (for the manual-merge/ship step), the salvage-nothing decision,
the final grep-zero / grep-cross-check results, the scoped `.po` diff (only api/index.po +
user_guide/configuration.po), and the docs-build WARNING comparison against the 1-warning baseline.
</output>
