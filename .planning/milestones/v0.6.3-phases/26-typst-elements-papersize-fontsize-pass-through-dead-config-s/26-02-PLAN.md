---
phase: 26-typst-elements-papersize-fontsize-pass-through-dead-config-s
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - tests/fixtures/typst_elements_pass_through_gate/
  - tests/test_typst_elements_pass_through_gate.py
autonomous: true
requirements: [CONF-04]
must_haves:
  truths:
    - "typst_elements={'papersize':'us-letter'} builds and real-compiles a valid %PDF; the emitted #show: project.with(...) region contains papersize as a quoted string (SC#1)"
    - "typst_elements={'fontsize':'20pt'} builds and real-compiles a valid %PDF from a fixture SEPARATE from papersize; the emitted region contains the fontsize length UNQUOTED and never its quoted-string form (SC#2)"
    - "an unknown typst_elements key makes sphinx-build exit non-zero via ExtensionError; no master is emitted carrying an undeclared kwarg (SC#3)"
    - "copyright: never appears anywhere in the emitted #show: project.with(...) region (SC#4)"
    - "a durable pre-fix-basis reconstruction (an undeclared kwarg / a leaked copyright argument spliced into project.with()) makes a real typst.compile() raise — the standing red proof that fail-loud + non-leak matter"
  artifacts:
    - "tests/fixtures/typst_elements_pass_through_gate/ — minimal fixture project(s) for the 4 required cases (papersize positive, fontsize positive, unknown-key, copyright non-leak)"
    - "tests/test_typst_elements_pass_through_gate.py — GATE-01 real-compile module (mirrors tests/test_package_only_config_gate.py) + TestPreFixBasisFailureProof durable reconstruction"
  key_links:
    - "sys.executable -m sphinx subprocess build (never uv run sphinx-build): the NixOS-sandbox-safe invocation the whole repo's GATE-01 modules use"
    - "class-scoped build fixture -> many independent assertions on one emitted master + one real compile per case"
    - "papersize and fontsize are proven by SEPARATE builds (SC#1/SC#2 require separate proofs — a combined case would hide a per-key emission-type bug)"
---

<objective>
Add the mandatory GATE-01 real-`typst.compile()` regression fixtures proving the Plan 01 wiring actually reaches the compiled output: (1) a positive `papersize` build, (2) a SEPARATE positive `fontsize` build, (3) a negative unknown-key build that aborts, and (4) a copyright-non-leak build — plus a durable `TestPreFixBasisFailureProof` that reconstructs the pre-fix defect shapes from the post-fix emitted master and proves a real compile raises against them.

Purpose: string-agreement asserts alone never suffice (ROADMAP standing bar). Registration-only tests stay green whether or not the feature works — this defect class (the 5th/6th dead-config instance) is exactly why every config→output change ships a real-compile gate. This plan is the SC#1–SC#4 proof that a documented `typst_elements = {"papersize": "us-letter", "fontsize": "20pt"}` takes effect end-to-end, with the unknown-key and copyright-leak failure modes proven closed.

Output: new fixture project(s) under `tests/fixtures/typst_elements_pass_through_gate/` and a new `tests/test_typst_elements_pass_through_gate.py` GATE-01 module, with a recorded red→green confirmation in the SUMMARY.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-typst-elements-papersize-fontsize-pass-through-dead-config-s/26-CONTEXT.md
@.planning/phases/26-typst-elements-papersize-fontsize-pass-through-dead-config-s/26-RESEARCH.md
@.planning/phases/26-typst-elements-papersize-fontsize-pass-through-dead-config-s/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture project(s) + the 4 standing GATE-01 real-compile cases</name>
  <files>tests/fixtures/typst_elements_pass_through_gate/, tests/test_typst_elements_pass_through_gate.py</files>
  <read_first>
    - tests/test_package_only_config_gate.py (the GATE-01 template named by ROADMAP/STATE — reuse its `_run_sphinx_build()` sys.executable-m-sphinx helper L71-96, its `_show_rule_call_region()` slicing helper L99-114, and the class-scoped @staticmethod `build` fixture pattern L184-219 with the `@pytest.mark.skipif(not TYPST_AVAILABLE, ...)` guard and the `%PDF` magic check L279-295)
    - tests/fixtures/confval_field_body_render_gate/conf.py (the simpler single-fixture conf.py/index.rst layout — a minimal master document with `typst_documents`)
    - tests/fixtures/package_only_config_gate/ (conf.py + index.rst layout for a per-case fixture dir)
    - typsphinx/templates/base.typ (project() L39-48 — confirms papersize/fontsize are real params, so a valid value compiles and a bogus kwarg would abort)
    - .planning/phases/26-.../26-01-SUMMARY.md (the marker/allowlist identifier names chosen in Plan 01, so assertions reference the real emission)
  </read_first>
  <action>
    Create the fixture(s) under tests/fixtures/typst_elements_pass_through_gate/ following whichever existing GATE-01 layout the executor judges cleanest for 4 small INDEPENDENT cases (per RESEARCH Open Question 2: four standalone fixture dirs, or one dir with clearly-named conf.py variants, or a `_write_variant_project`-style builder — executor discretion). papersize and fontsize MUST be proven by SEPARATE builds (SC#1/SC#2). Each fixture is a minimal master document (`typst_documents = [("index", ...)]`) with the relevant `typst_elements` value. Create tests/test_typst_elements_pass_through_gate.py mirroring tests/test_package_only_config_gate.py: reuse the `_run_sphinx_build()` subprocess-via-`sys.executable -m sphinx` helper (NEVER `uv run sphinx-build` — Pitfall 5, the NixOS-sandbox PATH hazard) and the `_show_rule_call_region()` slicer; guard the module with `@pytest.mark.skipif(not TYPST_AVAILABLE, ...)`; use class-scoped @staticmethod build fixtures that build once and share the emitted master text + PDF across assertions. Author these four standing cases: (1) POSITIVE papersize — build via `-b typstpdf`, assert returncode 0 + a `%PDF`-magic file exists, and the show-rule region contains papersize emitted as a quoted string with the configured value; (2) POSITIVE fontsize — a SEPARATE build, assert returncode 0 + valid `%PDF`, the show-rule region contains the fontsize length UNQUOTED with the configured value, and assert the quoted-string form of that fontsize does NOT appear in the region (the double-formatting-trap guard — Pitfall 1 — expressed as a negative string assertion in the TEST file); (3) NEGATIVE unknown-key — build a fixture whose `typst_elements` carries a key not in the allowlist, assert `sphinx-build` returns non-zero (the ExtensionError aborts the build) and that no emitted master carries that key as a project.with kwarg; per D-06 do NOT assert on the Typst/Sphinx error MESSAGE text, only that the build fails; (4) copyright-non-leak — build the positive fixture and assert `copyright:` never appears anywhere in the show-rule region (SC#4). These four cases are CONF-04's SC#1–SC#4 real-compile proof.
  </action>
  <verify>
    <automated>uv run pytest tests/test_typst_elements_pass_through_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_typst_elements_pass_through_gate.py -q` exits 0 (all four standing cases green post-fix).
    - papersize case: a real `-b typstpdf` build exits 0, a `%PDF`-magic file exists, and `grep 'papersize: "us-letter"'` over the emitted index.typ show-rule region is PRESENT.
    - fontsize case (separate build): a real compile exits 0 with valid `%PDF`; `grep 'fontsize: 20pt'` (unquoted) over the region is PRESENT and the quoted-string form is ABSENT.
    - unknown-key case: `sphinx-build` exits non-zero; no emitted master contains the bogus key as a `project.with(...)` kwarg; no assertion matches on compiler error TEXT (D-06).
    - copyright case: `grep -c 'copyright:' <show-rule region>` is 0.
    - The module invokes builds via `sys.executable -m sphinx` (grep confirms no `uv run` / bare `sphinx-build` in the test file) and is guarded by `skipif(not TYPST_AVAILABLE, ...)`.
  </acceptance_criteria>
  <done>
    Four standing GATE-01 cases (positive papersize, separate positive fontsize, negative unknown-key abort, copyright non-leak) each drive a real `typst.compile()`/`sphinx-build` and pass; papersize and fontsize are proven by separate builds; the fontsize case guards against the double-formatting trap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Durable pre-fix-basis failure proof + red→green confirmation + full-suite gate</name>
  <files>tests/test_typst_elements_pass_through_gate.py</files>
  <read_first>
    - tests/test_package_only_config_gate.py (TestPreFixBasisFailureProof L302-406 — the durable-reconstruction convention: build once via the faster `-b typst`, splice the defect back into the post-fix emitted master, assert a real `typst.compile()` raises with bare `pytest.raises(Exception)`, NEVER matching error text; note the docstring convention of recording the manual weaken-and-observe check in the plan SUMMARY rather than as a self-referential test)
    - tests/test_nested_master_render_gate.py (the same durable pre-fix-basis reconstruction convention referenced by the package-only gate)
    - .planning/phases/26-.../26-01-SUMMARY.md (identifier names + the emitted show-rule shape to splice into)
  </read_first>
  <action>
    Add a `TestPreFixBasisFailureProof` class to tests/test_typst_elements_pass_through_gate.py mirroring the convention in tests/test_package_only_config_gate.py: a class-scoped @staticmethod fixture builds the positive fixture ONCE via the faster `-b typst` builder and returns the emitted master text; each reconstruction mutates that post-fix text to recreate a pre-fix defect shape and asserts a real `typst.compile()` raises (bare `pytest.raises(Exception)`, NEVER matching on the compiler's message text — D-06). Author two reconstructions: (a) splice an UNDECLARED kwarg (a key `base.typ`'s project() does not declare) into the `project.with(...)` region and prove the compile raises — this is the durable red proof of WHY SC#3's fail-loud allowlist is required (an unguarded unknown key would reach this exact fatal); (b) splice a leaked `copyright` argument into the `project.with(...)` region and prove the compile raises — the durable red proof of WHY SC#4's structural non-leak matters (a leaked baseline metadatum is an undeclared kwarg to project()). Because these gates are authored AFTER the Plan 01 fix (repo convention — impl Wave 1, gate Wave 2), the executor MUST additionally perform and RECORD the manual red→green confirmation in the SUMMARY: temporarily revert the Plan 01 change (or stash it), run this module + Task 1's cases, observe the positive greps fail and the unknown-key build succeed (RED); restore the fix, observe GREEN. This is the repo's established way of keeping the red→green transition real for a durable gate (see the package-only gate docstring). Finally, run the full suite and confirm no NEW failures versus the pre-change baseline (the honest green bar per the NixOS-sandbox note — the ~45 documented environmental integration failures are pre-existing and excluded).
  </action>
  <verify>
    <automated>uv run pytest tests/test_typst_elements_pass_through_gate.py -q && uv run pytest -q -m "not slow"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_typst_elements_pass_through_gate.py -q` exits 0 including the two TestPreFixBasisFailureProof reconstructions.
    - Reconstruction (a): an undeclared-kwarg splice into project.with(...) makes `typst.compile()` raise (bare `pytest.raises(Exception)`; no error-text assertion).
    - Reconstruction (b): a copyright-argument splice into project.with(...) makes `typst.compile()` raise.
    - The SUMMARY records the manual red→green confirmation (fix reverted → positive greps fail + unknown-key build succeeds = RED; fix restored → GREEN), mirroring tests/test_package_only_config_gate.py's convention.
    - `uv run pytest -q -m "not slow"` shows no NEW failures versus the recorded pre-change baseline (documented ~45 environmental integration failures excluded).
    - `git diff --exit-code typsphinx/templates/base.typ` still exits 0 (SC#5 holds after the gate work — this plan touches only tests/).
  </acceptance_criteria>
  <done>
    A durable `TestPreFixBasisFailureProof` proves a real compile raises against both an undeclared-kwarg and a leaked-copyright reconstruction; the manual red→green transition is confirmed and recorded in the SUMMARY; the full suite has no new failures; base.typ remains byte-unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test tier only | This plan adds test fixtures and a test module (`tests/`). It introduces no new runtime trust boundary; the fixtures' `conf.py` values are test-authored, trusted inputs. The production trust boundary (`conf.py` → build-time Python) is unchanged from Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-02 | Denial of Service | The negative/reconstruction fixtures invoke real `typst.compile()` / `sphinx-build` subprocesses | low | accept | Bounded, offline, minimal fixture documents; guarded by `skipif(not TYPST_AVAILABLE)`; same pattern as every existing GATE-01 module. No untrusted input. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase — zero new runtime deps (milestone invariant). |

Test-tier only: no new production trust boundary, no network/data ingress. The threat model is trivially empty by design.
</threat_model>

<verification>
- `uv run pytest tests/test_typst_elements_pass_through_gate.py -q` green (the 4 standing cases + 2 durable reconstructions).
- Full suite (`uv run pytest -q -m "not slow"`) shows no NEW failures versus the pre-change baseline (honest green bar; ~45 documented environmental integration failures excluded per the NixOS-sandbox memory).
- `git diff --exit-code typsphinx/templates/base.typ` exits 0 (SC#5).
- Builds invoked as `sys.executable -m sphinx`; per-worktree provisioning (`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`) and `uv run` used throughout, per CLAUDE.md's Worktree-isolated execution section.
</verification>

<success_criteria>
- SC#1: a real `typstpdf` build emits `papersize` as a quoted string and compiles a valid PDF.
- SC#2: a SEPARATE real build emits `fontsize` as an unquoted length (never quoted) and compiles a valid PDF.
- SC#3: an unknown `typst_elements` key aborts the build (ExtensionError), proven both at build level (non-zero exit) and durably (undeclared-kwarg reconstruction raises a real compile).
- SC#4: `copyright:` never appears in the emitted show-rule region, and a leaked-copyright reconstruction raises a real compile.
- The red→green transition is confirmed and recorded; base.typ byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/26-typst-elements-papersize-fontsize-pass-through-dead-config-s/26-02-SUMMARY.md` when done. MUST record the manual red→green confirmation (what was reverted, what went red, what went green on restore) per the repo's durable-gate convention.
</output>
