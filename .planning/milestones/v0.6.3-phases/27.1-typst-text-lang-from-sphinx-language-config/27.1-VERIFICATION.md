---
phase: 27.1-typst-text-lang-from-sphinx-language-config
verified: 2026-07-25T00:00:00Z
status: passed
score: 12/12 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 27.1: Typst 組版 lang の Sphinx `language` 連動 Verification Report

**Phase Goal:** Typst の組版言語 (`set text(lang:)`) が Sphinx の `language` conf に連動する。`base.typ` の `project()` に `lang` パラメータを追加し、既定テンプレート使用時は `language` から自動導出、`typst_elements["lang"]` の明示指定が常に優先する。カスタムテンプレート／パッケージ利用者は `language` を設定しているだけでは壊れない。
**Verified:** 2026-07-25
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria, read via the D-07 split proof per CONTEXT.md and per this task's explicit instruction)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC#1 — `language = "ja"` on the default template makes compiled PDF figure/table supplements read 「図 N」/「表 N」, proven by real `typst.compile()` | ✓ VERIFIED | D-07 split proof, both halves independently re-run and passing: `TestJapaneseSourceProof` (real `-b typstpdf` compile succeeds; emitted `.typ` show-rule region carries `lang: "ja"`, font-independent) + `TestGermanLinkageProof` (real `-b typstpdf` compile of `language="de"`; pypdf-extracted text contains `"Tabelle\xa01"` and `"Abbildung\xa01"`, and explicitly does NOT contain `"Table"`/`"Figure"`). This is the actual behavioral proof that the setting drives Typst's generated labels — CJK glyph extraction was deliberately not attempted because CI's CJK font availability is unconfirmed (documented, deliberate, not an oversight). `uv run pytest tests/test_typst_lang_gate.py -v` — all 21 cases pass, independently re-executed in this verification session. |
| 2 | SC#2 — explicit `typst_elements = {"lang": ...}` beats the auto-derived value on every template path, riding the existing `ELEMENTS_ALLOWLIST` merge | ✓ VERIFIED | `TestPrecedence::test_explicit_lang_wins_over_auto_value` (real build, `language="de"` + explicit `lang="ja"` → emitted region carries exactly one `lang:` entry holding `"ja"`). Structural proof at the code level: `writer.py`'s `effective_elements = ({"lang": auto_lang} if auto_lang else {}) | (typst_elements or {})` puts the auto-derived value as the LEFT (losing) operand and the user's config as the RIGHT (winning) operand — no new precedence branch inside `map_parameters()`. Unit-level adjacency/ordering tests in `TestEffectiveElementsMerge` (tests/test_template_engine.py) additionally pin this without a compile. |
| 3 | SC#3 — auto-derivation never reaches a custom `typst_template`, a `typst_package`, or a `<srcdir>/base.typ` shadow; such users are not broken by an `unexpected argument: lang` fatal merely for setting `language` | ✓ VERIFIED | Three real/near-real regression fixtures, all passing: `TestCustomTemplateNoLang` (explicit lang-less custom template, real compile succeeds, no injected `lang`), `TestSrcdirShadowNoLang` (a `base.typ` beside `conf.py` with both `typst_template`/`typst_package` unset — the direct proof of the D-06 judgment boundary — real compile succeeds, no injected `lang`), `TestPackageNoLang` (package path, source-only build, package entry-function call carries no `lang`). The judgment predicate `uses_bundled_default_template()` is implemented as a single provenance-based check (`resolve_template().source == "default"` AND no `typst_package`), not a declaration check — confirmed by reading `typsphinx/template_engine.py:347-385` and its docstring, which explicitly reasons through why a declaration check would miss the shadow case. |
| 4 | SC#4 — the Sphinx-language→Typst-`lang` conversion rule is fixed and tested across locale-suffixed forms (`ja`, `zh_CN`, `pt_BR`, `sr@latin`, etc.), and unknown/non-standard values never abort the build | ✓ VERIFIED | `derive_typst_lang()` (`typsphinx/template_engine.py:84-149`) implements the D-02 generic split-and-lowercase rule with a D-03 ASCII-scoped `re.fullmatch(r"[a-z]{2,3}", head)` accept test (deliberately not Unicode-aware `str.isalpha()` — the documented reasoning is exactly the CJK/Cyrillic-false-accept hazard). Pinned by `TestDeriveTypstLang` (parametrized unit tests over the full measured table plus empty/None/single-char/CJK cases) and by `TestMalformedLanguage` (real `-b typstpdf` compile of a CJK `language` value: build exits 0, valid PDF produced, no `lang:` entry emitted). ROADMAP's own SC#4 wording mentions "（必要なら region）" — region support was explicitly rejected in CONTEXT.md D-01 (deliberate scope decision, not an oversight); the `zh_TW`→simplified-Chinese limitation this creates is documented in `docs/source/user_guide/configuration.rst`. |
| 5 | SC#5 — `base.typ`'s diff is confined to the `lang` parameter + its `set text()` wiring; `@preview` version strings untouched; version-sync gate green; zero new runtime deps | ✓ VERIFIED | `git diff -U0 605dd69 -- typsphinx/templates/base.typ` shows exactly two changed lines (the added `lang: "en"` parameter, and the rewritten `set text(size: fontsize, lang: lang)` line) — no other line, no `@preview` import touched. `uv run pytest tests/test_preview_version_sync.py -v` — 2/2 pass. `git diff 605dd69 -- pyproject.toml uv.lock` is empty — no new runtime dependency. |

**Score:** 5/5 ROADMAP Success Criteria verified (0 present-but-behavior-unverified).

### PLAN.md must_haves (representative sample beyond the SC table above)

| # | Must-have | Status | Evidence |
|---|-----------|--------|----------|
| 6 | Same-commit contract: `ELEMENTS_ALLOWLIST["lang"]` and `base.typ`'s new parameter land together | ✓ VERIFIED | `git show --stat 4a41c27` lists both `typsphinx/template_engine.py` and `typsphinx/templates/base.typ`. |
| 7 | `builder.py` / `__init__.py` untouched — judgment lives in exactly one place, no new top-level config value | ✓ VERIFIED | `git diff 605dd69 -- typsphinx/builder.py typsphinx/__init__.py` is empty. |
| 8 | D-04 prohibition: explicit `typst_elements["lang"]` never validated/rewritten | ✓ VERIFIED | `test_map_parameters_explicit_lang_not_validated` passes an obviously-invalid string through unchanged and unraised. |
| 9 | Docs: `lang` documented as 3rd `typst_elements` key, scope/precedence/`zh_TW` limitation, Complete Example (package path) untouched | ✓ VERIFIED | `docs/source/user_guide/configuration.rst` contains the "Document Language" section; `git diff` confirms the "Paper Size and Format" heading and "Complete Example" block are byte-unchanged. |
| 10 | ja catalog scope-limited regeneration, obsolete blocks preserved (≥12) | ✓ VERIFIED | `docs/locale/ja/LC_MESSAGES/user_guide/configuration.po` currently carries 17 `#~` lines (≥12); `git status --porcelain docs/locale/` shows no other catalog touched. |
| 11 | GATE-01 durable pre-fix-basis proof (both directions) | ✓ VERIFIED | `TestPreFixBasisFailureProof::test_omission_basis_falls_back_to_english_supplements` and `::test_undeclared_argument_basis_raises` both pass; SUMMARY 03 records the actual manual red→green transition output (5 failed + 1 error pre-fix → 20 passed post-fix, later 21 after the CR-01 fixture addition). |
| 12 | Post-merge orchestrator fixes (CR-01 TypeError, WR-01 docs example, docs-build docstring warnings) actually landed in the merged tree | ✓ VERIFIED | `typsphinx/writer.py:250-252` shows `(typst_elements or {})` normalization; a new `TestNullElementsDoesNotAbort` (3 tests) and `tests/fixtures/typst_lang_gate/null_elements/` fixture pin it — all pass. Docs precedence example now contrasts `language = "ja"` vs `typst_elements = {"lang": "de"}`. HTML docs build re-run in this session: 4 warning/error lines, all attributable to the pre-existing `translator.py::visit_toctree` docstring baseline — no new warnings from Plan 01's docstrings. |

**Score:** 12/12 must-haves verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `typsphinx/templates/base.typ` | `lang` param + `set text(lang: lang)` wiring, 2-line diff only | ✓ VERIFIED | Confirmed via `git diff -U0 605dd69` |
| `typsphinx/template_engine.py` | `derive_typst_lang()`, `TemplateResolution`, `resolve_template()`, `uses_bundled_default_template()`, `ELEMENTS_ALLOWLIST["lang"]` | ✓ VERIFIED | All present, substantive (full docstrings reasoning through the design decisions), wired (writer.py imports and calls them) |
| `typsphinx/writer.py` | `effective_elements` pre-merge gated on `uses_bundled_default_template()` | ✓ VERIFIED | Lines 219-257, confirmed CR-01-safe (`typst_elements or {}`) |
| `tests/test_template_engine.py` | Conversion-table + malformed + provenance + merge unit tests | ✓ VERIFIED | `TestDeriveTypstLang`, `TestTemplateResolutionProvenance`, `TestEffectiveElementsMerge` classes present and passing |
| `tests/test_typst_lang_gate.py` + `tests/fixtures/typst_lang_gate/` | GATE-01 real-compile module, 8 fixture projects | ✓ VERIFIED | 21 tests across 8 classes, all pass under a real re-run this session |
| `docs/source/user_guide/configuration.rst` + ja `.po` | Document Language section, scope-limited catalog | ✓ VERIFIED | Grep + diff confirmed |

### Data-Flow Trace (Level 4)

The `lang` value's full path was traced end-to-end and independently re-executed rather than merely read:
`config.language` → `writer.py::translate()` (gated by `uses_bundled_default_template()`) → `derive_typst_lang()` → `effective_elements` (right-biased union with user's `typst_elements`) → `TemplateEngine.map_parameters()` → `_format_typst_value()` (STRING kind, quoted) → emitted `#show: project.with(..., lang: "de", ...)` → real `typst.compile()` → PDF → pypdf-extracted `"Tabelle\xa01"` / `"Abbildung\xa01"`. Every link in this chain has a passing, independently-reproduced test. Status: ✓ FLOWING (real data, not a static/hardcoded stub at any link).

### Behavioral Spot-Checks / Probe Execution

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full lang gate module | `uv run pytest tests/test_typst_lang_gate.py -v` | 21 passed | ✓ PASS |
| Preview version-sync gate | `uv run pytest tests/test_preview_version_sync.py -v` | 2 passed | ✓ PASS |
| Full suite | `uv run python -m pytest -q` | 656 passed, 1 skipped, 0 failed | ✓ PASS |
| Lint/format/types | `black --check .`, `ruff check .`, `mypy typsphinx/` | all clean | ✓ PASS |
| Docs HTML build | `python -m sphinx -b html docs/source ... -W --keep-going` | 4 warning/error lines, all pre-existing `visit_toctree` baseline | ✓ PASS (no new warnings) |
| `base.typ` diff scope | `git diff -U0 605dd69 -- typsphinx/templates/base.typ` | exactly 2 lines | ✓ PASS |
| `builder.py`/`__init__.py` untouched | `git diff 605dd69 -- typsphinx/builder.py typsphinx/__init__.py` | empty | ✓ PASS |
| Zero new runtime deps | `git diff 605dd69 -- pyproject.toml uv.lock` | empty | ✓ PASS |

All commands above were re-run live in this verification session (not taken from SUMMARY claims) and produced the results shown.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|--------------|--------|----------|
| CONF-07 | 27.1-01, 27.1-02, 27.1-03 | Typst `lang` follows Sphinx `language`, precedence via `ELEMENTS_ALLOWLIST`, non-default paths never broken | ✓ SATISFIED | All 5 ROADMAP SCs verified above; no orphaned requirement IDs found for this phase (only CONF-07 mapped, claimed by all three plans). |

Note: `.planning/REQUIREMENTS.md` still shows CONF-07's checkbox unchecked and its coverage-table row as "Not started" — this is an administrative-bookkeeping lag (the file has not been updated to reflect phase completion), not a gap in the implementation itself. Recommend updating that file as a housekeeping follow-up.

### Anti-Patterns Found

None found. Searched all files modified/created by this phase (`typsphinx/template_engine.py`, `typsphinx/writer.py`, `typsphinx/templates/base.typ`, `tests/test_template_engine.py`, `tests/test_typst_lang_gate.py`, docs) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/stub patterns — zero hits in production code. One pre-existing Info-level finding from the code review (IN-01: `derive_typst_lang()`'s warning block is duplicated verbatim across its two rejection branches) was explicitly triaged and deliberately deferred by the orchestrator as a low-risk follow-up, not silently dropped — recorded in `27.1-REVIEW.md`'s Orchestrator Resolution section. This is judged acceptable: it is genuinely cosmetic (both copies are byte-identical today), does not affect correctness, and is documented rather than hidden.

### Human Verification Required

None. This phase's success criteria are all mechanically verifiable (real `typst.compile()` + PDF text extraction, diff scoping, requirement-ID cross-reference), and every check was independently re-executed in this session rather than trusted from SUMMARY narration.

### Gaps Summary

No gaps found. All 5 ROADMAP Success Criteria are verified against actual, independently re-run evidence (not SUMMARY claims): the `lang` parameter reaches `base.typ` with a minimal 2-line diff (SC#5), an explicit `typst_elements["lang"]` structurally wins over auto-derivation (SC#2), all three non-default template paths (explicit custom template, `<srcdir>/base.typ` shadow, Typst Universe package) are proven immune to the injected-argument hazard via real compiles (SC#3), the Sphinx→Typst language conversion rule is fixed and its malformed-input path is proven non-fatal (SC#4), and the D-07 split-proof design legitimately establishes SC#1 (a font-independent `ja` source-level assertion for value-reaches-emitted-output, plus a real `de` PDF-text-extraction assertion for the behavioral supplement-language linkage) — this split is a deliberate, well-reasoned, and explicitly-recorded engineering decision given CJK font availability is unconfirmed on the CI runner, not a gap in coverage.

One post-merge critical defect (CR-01: `typst_elements = None` aborting the build with an unhandled `TypeError`) was found by the code reviewer, fixed by the orchestrator, and is now itself pinned by a new regression fixture (`null_elements`) and 3 new passing tests — re-verified in this session. One warning (WR-01: a non-demonstrative docs example) was likewise fixed and re-verified. The one deferred finding (IN-01, cosmetic code duplication) is Info-severity, explicitly triaged, and does not block the phase goal.

Two follow-up items were deliberately NOT fixed in this phase and are correctly recorded as out-of-scope in `27.1-02-SUMMARY.md` rather than silently ignored: the `docs/usage.rst`/`docs/installation.rst` orphan-docs class, and non-allowlisted `typst_elements` keys in `examples/advanced/`. Neither is a CONF-07 concern.

The recommendation from `27.1-03-SUMMARY.md` to update ROADMAP SC#1's wording to describe the D-07 split proof has not yet been applied verbatim to SC#1 itself (only the adjacent "Note" explains the split) — this is a documentation-polish follow-up, not a functional gap, since the Note already gives the correct reading and this verification task explicitly instructed judging the split on its merits rather than the literal SC#1 wording.

---

_Verified: 2026-07-25_
_Verifier: Claude (gsd-verifier)_
