---
phase: 27.1-typst-text-lang-from-sphinx-language-config
plan: 03
type: execute
wave: 2
depends_on: ["27.1-01"]
files_modified:
  - tests/fixtures/typst_lang_gate/
  - tests/test_typst_lang_gate.py
autonomous: true
requirements: [CONF-07]
must_haves:
  truths:
    - "SC#1 source half (D-07): a project with Sphinx language 'ja' on the bundled default template real-compiles through -b typstpdf and its emitted master's show-rule region carries a quoted ja text-language entry — a font-independent proof, deliberately not a CJK glyph extraction"
    - "SC#1 linkage half (D-07): a project with Sphinx language 'de' real-compiles and the pypdf-extracted PDF text matches an NBSP-tolerant pattern for the German table supplement, and for the German figure supplement — this is the proof that the text-language setting actually drives Typst's generated labels"
    - "encoding edge (NBSP half): the supplement/number separator Typst emits is U+00A0, so every extraction assertion uses a regex whitespace class rather than an ASCII space literal; an ASCII-space substring check silently fails against correct output and nothing in this repo did NBSP-aware matching before this module"
    - "SC#2 (D-05): with Sphinx language 'de' and an explicit typst_elements lang of 'ja', the emitted show-rule region carries exactly one text-language entry and it holds the explicit value"
    - "SC#3 case ① (D-08): an explicitly configured custom template whose entry function declares no such parameter, combined with Sphinx language 'ja', real-compiles successfully and its show-rule region carries no auto-injected entry"
    - "SC#3 case ② (D-08, the direct proof of the D-06 judgment boundary): a base.typ placed beside conf.py — with typst_template and typst_package both unset — real-compiles successfully with Sphinx language 'ja' and its show-rule region carries no auto-injected entry"
    - "SC#3 case ③ (package path): a project configuring typst_package with Sphinx language 'ja' emits a show rule calling the package entry function with no auto-injected entry — the regression proof for the package guard inside uses_bundled_default_template()"
    - "SC#4: a project whose Sphinx language value cannot be reduced to a two- or three-letter code builds to completion rather than aborting, and emits no text-language entry"
    - "GATE-01 discipline: a durable pre-fix-basis reconstruction proves both directions — removing the entry from a post-fix German master makes the recompiled PDF fall back to the English supplements, and splicing the entry into a master whose template never declared it makes a real typst.compile() raise"
    - "The red-to-green transition is performed and recorded: with Plan 01 reverted, the German extraction case fails and the source-level cases fail; with it restored, all cases pass"
  prohibitions:
    - "Must NOT vendor a CJK font binary into the repository to convert the Japanese case into a PDF-text extraction assertion. D-07 rejected it deliberately: it adds multiple megabytes of binary plus a redistribution-licence obligation to a pure-Python extension, to buy an assertion the German case already provides. If CI font availability is ever confirmed, that is a separate, explicit decision."
  artifacts:
    - "tests/test_typst_lang_gate.py — GATE-01 real-compile module mirroring tests/test_typst_elements_pass_through_gate.py, with a new NBSP-tolerant supplement matcher and a TestPreFixBasisFailureProof class"
    - "tests/fixtures/typst_lang_gate/ja_default/ — conf.py + index.rst, default template, Sphinx language ja, ASCII-only content"
    - "tests/fixtures/typst_lang_gate/de_default/ — conf.py + index.rst + image.png, default template, Sphinx language de, one captioned table and one figure"
    - "tests/fixtures/typst_lang_gate/precedence/ — conf.py + index.rst, Sphinx language de with an explicit typst_elements lang of ja"
    - "tests/fixtures/typst_lang_gate/custom_template_no_lang/ — conf.py + index.rst + _templates/custom.typ declaring no such parameter"
    - "tests/fixtures/typst_lang_gate/srcdir_shadow_no_lang/ — conf.py + index.rst + base.typ at the srcdir root declaring no such parameter"
    - "tests/fixtures/typst_lang_gate/package_no_lang/ — conf.py + index.rst configuring typst_package"
    - "tests/fixtures/typst_lang_gate/malformed_language/ — conf.py + index.rst with a Sphinx language value that cannot be reduced to a valid code"
  key_links:
    - "sys.executable -m sphinx subprocess build -> the NixOS-sandbox-safe invocation every GATE-01 module in this repo uses; a console-script or uv-run invocation is the documented exec hazard"
    - "regex whitespace class in the extraction assertion -> U+00A0: the one detail that decides whether the German proof is real or vacuously red"
    - "srcdir_shadow_no_lang fixture -> uses_bundled_default_template()'s search-priority branch: the fixture IS the D-06 boundary proof"
    - "package_no_lang fixture -> uses_bundled_default_template()'s package guard: without that guard the engine's own walk resolves to the bundled default and injects an argument the package entry function never declared"
---

<objective>
Ship the mandatory GATE-01 real-`typst.compile()` regression module for CONF-07: prove the text language actually reaches the compiled PDF and changes Typst's generated labels, prove an explicit value wins, prove that none of the three non-default template paths get an injected argument, prove a malformed Sphinx `language` value cannot abort a build, and record the red-to-green transition durably.

Purpose: this project's standing bar is that every config-to-output change ships a fail-pre-fix real-compile fixture — string-agreement asserts stay green whether or not the feature works, which is precisely the defect class this milestone exists to close. Plan 01's unit tests prove the plumbing; only a real compile plus PDF text extraction proves 「表 1」 instead of "Table 1".

Output: `tests/test_typst_lang_gate.py` and a seven-case fixture tree under `tests/fixtures/typst_lang_gate/`, with the red-to-green confirmation recorded in the SUMMARY.

**Note for the verifier and for post-implementation roadmap maintenance:** ROADMAP SC#1 is worded as a single Japanese-supplement PDF-extraction proof. D-07 deliberately splits it — the Japanese half is proven at the emitted-source tier (font-independent) and the supplement-language linkage is proven with German plus PDF extraction, because CJK text extraction depends on system font availability and the CI ubuntu runner's CJK fonts are unconfirmed. This split is the accepted design, **not** an SC#1 miss. After this phase goes green, ROADMAP SC#1's wording should be updated to describe the split proof.
</objective>

## Artifacts this phase produces

See the full phase-level inventory in `27.1-01-PLAN.md` under "Artifacts this phase produces". This plan contributes the new test module, its helpers, and the seven fixture directories listed in this plan's `must_haves.artifacts`. All are new files; none is a modification of an existing symbol.

## Edge-probe coverage (spec-less fallback accounting)

The four edge-probe items for CONF-07 are authored in full in `27.1-01-PLAN.md` (4 items = 4 authored truths, 0 flagged assumptions). This plan carries the build-tier half of the `encoding` item — the U+00A0 separator in extracted PDF text — authored as its own truth above because it is a distinct, measurable failure mode from the ASCII-scoping half and has no other producer.

Prohibition recall (§B) kept one values-class item for this plan, authored descriptor-less in `must_haves.prohibitions`.

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-CONTEXT.md
@.planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-PATTERNS.md
@.planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-VALIDATION.md
@.planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-01-SUMMARY.md
</context>

<execution_environment>
Worktree-isolated execution is this project's standing mode (CLAUDE.md):

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

then run everything via `uv run`. A bare `pytest` imports the unchanged main-tree package and will not see Plan 01's changes.

Expect ~45-48 pre-existing environmental failures inside a worktree across `tests/test_integration_advanced.py`, `tests/test_integration_basic.py`, `tests/test_integration_multi_doc.py`, `tests/test_integration_nested_toctree.py`, and `tests/test_examples_basic.py` — they shell out to a console-script sphinx build and hit the NixOS `stub-ld` linker error. **These are not regressions; do not spend turns diagnosing them.** Clean-signal invocation:

`uv run python -m pytest -q --ignore=tests/test_integration_advanced.py --ignore=tests/test_integration_basic.py --ignore=tests/test_integration_multi_doc.py --ignore=tests/test_integration_nested_toctree.py --ignore=tests/test_examples_basic.py`

Every new fixture build in this module must be invoked as a `sys.executable -m sphinx` subprocess. Never build via a console script and never through `uv run` from inside test code — that is the documented exec hazard the whole GATE-01 suite routes around.
</execution_environment>

<tasks>

<task type="auto">
  <name>Task 1: Fixture tree, module helpers, and the four positive cases (ja source proof, de extraction proof, precedence, malformed)</name>
  <files>tests/fixtures/typst_lang_gate/, tests/test_typst_lang_gate.py</files>
  <read_first>
    - tests/test_typst_elements_pass_through_gate.py — the full file (423 lines). It is the direct structural analog: reuse its `_run_sphinx_build()` helper (a `subprocess.run` on `[sys.executable, "-m", "sphinx", "-b", builder, src, out]`, capture_output, text) and its `_show_rule_call_region()` slicer (regex-locates the `#show: <func>.with(` opening line and the matching closing paren line, returning that slice), and copy its class-scoped `@staticmethod` build-fixture pattern that builds once and shares the emitted text across independent assertions.
    - tests/test_pdf_render_gate.py lines 1-60 and any one of its extraction tests — the `TYPST_AVAILABLE` / `PYPDF_AVAILABLE` try-import guards, the `@pytest.mark.skipif(not (TYPST_AVAILABLE and PYPDF_AVAILABLE), ...)` decorator form, and the extraction idiom: construct a `pypdf.PdfReader` over the produced PDF path and join every page's extracted text with newlines.
    - tests/fixtures/typst_elements_pass_through_gate/papersize_positive/conf.py and index.rst — the minimal fixture shape: a project/author/release triple, `extensions = ["typsphinx"]`, a `typst_documents` entry making index a master, and a deliberately short index.rst so the real compile stays fast. Note the conf.py's header comment convention explaining what the fixture pins and why.
    - tests/fixtures/captioned_table_render_gate/index.rst — the `.. table:: Caption` directive form that Phase 25 renders as a numbered table figure. This is the construct whose supplement this phase's German proof reads out of the PDF.
    - tests/fixtures/figure_length_render_gate/ — a fixture that ships an `image.png` next to its index.rst; copy that image file for the German fixture's figure directive.
    - typsphinx/templates/base.typ — the post-Plan-01 project() signature, to confirm the parameter name the show rule will carry.
    - .planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-01-SUMMARY.md — the identifier names Plan 01 actually landed.
    - .planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-CONTEXT.md — the measured supplement table (which value yields which label in which language) and D-07's split-proof rationale.
  </read_first>
  <action>
    Create four fixture projects under tests/fixtures/typst_lang_gate/, each a directory holding conf.py and index.rst, each with a header comment in conf.py stating which success criterion it pins and why it is shaped the way it is (matching the convention in the Phase 26 fixture conf.py files).

    ja_default: sets Sphinx `language = "ja"`, leaves `typst_template` and `typst_package` unset, and places no base.typ in its own directory, so the bundled default template is what resolves. Keep index.rst content ASCII-only — this case asserts on emitted source, not on glyphs, and ASCII content keeps the compile independent of whatever fonts the runner has.

    de_default: sets Sphinx `language = "de"` with the same default-template shape. Its index.rst MUST contain a captioned table written with the table directive (the Phase 25 captioned table is this phase's motivating case, so the table proof is mandatory) and SHOULD also contain a figure directive referencing a small image copied from an existing fixture, so both supplements can be read out of the PDF. Content stays ASCII — the German words this case asserts on are generated by Typst, not written in the fixture.

    precedence: sets Sphinx `language = "de"` AND an explicit `typst_elements` entry for the text language with the value `ja`, default-template shape. This is the SC#2 case.

    malformed_language: sets Sphinx `language` to a value that cannot be reduced to a two- or three-letter code — use a CJK string, which is the load-bearing case, since it is exactly the input a Unicode-aware alphabetic check would wrongly accept. Default-template shape.

    Then create tests/test_typst_lang_gate.py with a module docstring in the house style: state what defect class the module pins (a hardcoded English text language in the bundled template, so a translated project still got English figure and table labels), state that per the established convention no assertion anywhere in the module matches on the TEXT of a Typst compiler error, and state D-07's split-proof rationale so a future reader does not mistake the absence of a Japanese extraction assertion for an oversight.

    Copy `_run_sphinx_build()` and `_show_rule_call_region()` near-verbatim from the Phase 26 gate module. Add the try-import guards and skipif decorators from the PDF render gate.

    Add ONE genuinely new helper: an NBSP-tolerant supplement matcher. Typst separates a supplement word from its number with U+00A0, not an ASCII space, so an extracted string is of the form word + U+00A0 + digit. The helper takes the extracted text and a supplement word and returns whether a regex built from that word followed by a whitespace class and the digit matches. Python's regex whitespace class matches U+00A0; a plain ASCII-space substring check does not, and would fail silently against perfectly correct output. Nothing in this repository does NBSP-aware matching today, so there is no existing helper to copy — write it with a docstring recording exactly this reasoning.

    Author the four positive cases, each driving a build through the module's helper:
    - ja_default through the pdf builder: assert the build exited 0, assert a produced PDF file exists and begins with the PDF magic bytes, and assert the emitted index master's show-rule region contains the text-language entry carrying a quoted `ja`. Do not extract PDF text in this case.
    - de_default through the pdf builder: assert the build exited 0 and a valid PDF exists, then read the PDF with pypdf, join every page's extracted text, and assert via the NBSP-tolerant helper that the German table supplement appears with number 1, and likewise the German figure supplement if the fixture includes a figure. Also assert the English forms do not appear, so a build that silently ignored the setting cannot pass.
    - precedence through the source-only builder (no compile needed): assert the show-rule region carries exactly one text-language entry and that it holds the explicit `ja` value rather than the `de` derived from the Sphinx setting.
    - malformed_language through the pdf builder: assert the build exited 0 and a valid PDF exists, and assert the show-rule region carries no text-language entry at all — the parameter is omitted so the template default applies. Do NOT assert on the warning text in the subprocess's captured stderr: the warning is emitted through a stdlib module logger rather than Sphinx's own logging hierarchy, so its presence in a subprocess's output is not a contract worth pinning. The warning is proven at the unit tier by Plan 01's caplog test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_typst_lang_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_typst_lang_gate.py -q` exits 0 with the four positive cases collected and passing (or cleanly skipped when the typst/pypdf guards are unmet — record which in the SUMMARY).
    - `uv run pytest tests/test_typst_lang_gate.py -k ja -q` exits 0 and its case asserts on the emitted master's show-rule region, not on extracted PDF text.
    - `uv run pytest tests/test_typst_lang_gate.py -k de -q` exits 0 and its case reads the produced PDF through pypdf and matches the German table supplement via the NBSP-tolerant helper.
    - `uv run pytest tests/test_typst_lang_gate.py -k precedence -q` exits 0 and its case asserts exactly one text-language entry in the region carrying the explicit value.
    - `uv run pytest tests/test_typst_lang_gate.py -k malformed -q` exits 0 and its case asserts a successful build with no text-language entry emitted.
    - Source assertion: the module's build helper constructs its argv beginning with `sys.executable, "-m", "sphinx"` (`grep -n 'sys.executable' tests/test_typst_lang_gate.py` shows it inside the helper).
    - Source assertion: `grep -n 'PYPDF_AVAILABLE\|TYPST_AVAILABLE' tests/test_typst_lang_gate.py` shows the skip guards.
    - Source assertion: the NBSP-tolerant helper builds its pattern with a regex whitespace class (`grep -n 'r"' tests/test_typst_lang_gate.py` shows the pattern construction) and its docstring names U+00A0.
    - `uv run black --check tests/test_typst_lang_gate.py` and `uv run ruff check tests/` exit 0.
  </acceptance_criteria>
  <done>
    Four fixture projects and a new GATE-01 module exist; the Japanese case proves the value reaches the emitted source under a real compile, the German case proves the setting drives Typst's generated table (and figure) supplements through PDF text extraction with NBSP-tolerant matching, the precedence case proves an explicit value wins, and the malformed case proves a build cannot be aborted by an unusable Sphinx language value.
  </done>
</task>

<task type="auto">
  <name>Task 2: The three non-regression cases — custom template, srcdir shadow, and package path</name>
  <files>tests/fixtures/typst_lang_gate/, tests/test_typst_lang_gate.py</files>
  <read_first>
    - examples/advanced/_templates/custom.typ lines 100-120 and examples/charged-ieee/approach2/source/_template.typ lines 1-20 — two real, repository-tracked templates whose project() declares title, authors, date and the toctree parameters and NOT a text-language parameter. These are the exact shape the non-regression fixtures must reproduce, because they are what a real user of this extension already has on disk.
    - typsphinx/templates/base.typ — the post-Plan-01 file, which the shadow and custom fixtures derive from by deleting the parameter Plan 01 added.
    - typsphinx/template_engine.py — the resolve_template() priority walk and uses_bundled_default_template() as landed by Plan 01, especially the package guard, so the third fixture targets the right branch.
    - typsphinx/builder.py lines 555-595 — `_write_template_file()`, which for the shadow case resolves the srcdir file and writes it as the shared template, and which returns early for a package configured alone.
    - tests/fixtures/package_only_config_gate/conf.py — a working package-path fixture configuration to model the third fixture on, including its typst_template_function block and its explicit warning never to also set typst_template.
    - .planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-CONTEXT.md — D-06 and D-08 in full, including the measured fact that a base.typ beside conf.py silently shadows the bundled template while both template settings are unset.
  </read_first>
  <action>
    Create three more fixture projects under tests/fixtures/typst_lang_gate/.

    custom_template_no_lang: sets Sphinx `language = "ja"` and points `typst_template` at a template file inside the fixture (a `_templates/` subdirectory keeps it conventional). Produce that template by copying typsphinx/templates/base.typ and removing the text-language parameter Plan 01 added from its project() signature, restoring the hardcoded literal in the text-setup line. That reproduces a genuine pre-existing user template — one that declares no such parameter — rather than a synthetic stub. This is D-08 case ①.

    srcdir_shadow_no_lang: sets Sphinx `language = "ja"` and leaves BOTH `typst_template` and `typst_package` unset, but places a file named base.typ directly at the fixture project's root, in the same directory as conf.py and index.rst — not under a templates subdirectory, because the engine's search priority looks for that exact filename in the source directory itself. Give it the same lang-less content as the custom template above. This is D-08 case ② and the direct proof of the D-06 judgment boundary: a declaration-based check would see two unset settings, conclude "default template", and inject an argument this template never declared.

    package_no_lang: sets Sphinx `language = "ja"` and configures `typst_package` with the same Typst Universe package and template-function shape the existing package-only gate fixture uses, with no `typst_template` set. This is the SC#3 package half and the regression proof for the package guard inside the judgment predicate — without that guard, an engine on this path has no explicit template path, so its own priority walk resolves to the bundled default and the auto-derived value would be forwarded to the package's entry function as an argument it never declared, which is a fatal.

    Then author the three corresponding cases in tests/test_typst_lang_gate.py.

    For custom_template_no_lang and srcdir_shadow_no_lang, drive real compiles through the pdf builder: assert the build exited 0, assert a produced PDF exists and begins with the PDF magic bytes, and assert the emitted master's show-rule region carries no text-language entry. The successful compile is the substance of these cases — the failure mode they guard against is a hard Typst abort on an undeclared argument, which shows up as a non-zero build exit.

    For package_no_lang, drive the source-only builder rather than a compile: assert the build exited 0 and that the show-rule region — which calls the package's own entry function, not the bundled project function, so the region slicer must tolerate the different function name — carries no text-language entry. A real compile here would add a second package download to the suite for no additional signal; the existing package-only gate already proves that path compiles.

    Do not weaken any of these three into a source-only string comparison against a hand-written expected block. The assertion is on the emitted region produced by a real build, so that a future change to the judgment predicate is caught rather than accommodated.
  </action>
  <verify>
    <automated>uv run pytest tests/test_typst_lang_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_typst_lang_gate.py -q` exits 0 with all seven cases from Tasks 1 and 2 collected.
    - `uv run pytest tests/test_typst_lang_gate.py -k custom_template_no_lang -q` exits 0: a real pdf-builder build exits 0, a valid PDF exists, and the region carries no text-language entry.
    - `uv run pytest tests/test_typst_lang_gate.py -k srcdir_shadow_no_lang -q` exits 0 under the same conditions, and the fixture's base.typ sits at the fixture root beside conf.py (`ls tests/fixtures/typst_lang_gate/srcdir_shadow_no_lang/` lists conf.py, index.rst, and base.typ at the same level).
    - `uv run pytest tests/test_typst_lang_gate.py -k package_no_lang -q` exits 0: the source-only build exits 0 and the package entry function's call region carries no text-language entry.
    - Both lang-less template files are derived from the real bundled template rather than hand-written stubs, and each carries a header comment saying so and naming which D-08 case it pins.
    - `uv run black --check tests/` and `uv run ruff check tests/` exit 0.
  </acceptance_criteria>
  <done>
    Three non-regression fixtures exist and pass: an explicitly configured lang-less custom template and a srcdir-shadowing lang-less base.typ both real-compile successfully with a Japanese Sphinx language and receive no injected argument, and a package-path project emits a package-function call with no injected argument.
  </done>
</task>

<task type="auto">
  <name>Task 3: Durable pre-fix-basis failure proof, recorded red-to-green transition, and the full-suite gate</name>
  <files>tests/test_typst_lang_gate.py</files>
  <read_first>
    - tests/test_typst_elements_pass_through_gate.py lines 340-423 — `TestPreFixBasisFailureProof`: a class-scoped staticmethod fixture that builds the positive fixture ONCE through the faster source-only builder and returns the emitted master text, then per-reconstruction tests that split that text into lines, locate the single show-rule opening line, splice or remove a line, write the result to a tmp path, and assert a real compile raises using a bare exception match with no error-text matching. Copy this structure.
    - tests/test_nested_master_render_gate.py — the same durable-reconstruction convention, referenced by the module above as the origin of the pattern.
    - .planning/phases/26-typst-elements-papersize-fontsize-pass-through-dead-config-s/26-02-SUMMARY.md — how the previous phase recorded its manual red-to-green confirmation, so this SUMMARY matches that standard.
    - .planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-CONTEXT.md — the GATE-01 discipline note stating the German case is automatically red before the fix.
  </read_first>
  <action>
    Add a `TestPreFixBasisFailureProof` class to tests/test_typst_lang_gate.py following the established convention: a class-scoped staticmethod fixture that builds a fixture ONCE through the source-only builder and hands the emitted master text to each reconstruction, which mutates that post-fix text to recreate a pre-fix shape and then compiles it for real.

    Author two reconstructions.

    First, the omission basis. Take the German fixture's post-fix emitted master, remove the text-language line from its show-rule region, write the result out, compile it for real, extract the text with pypdf, and assert that the English supplements now appear and the German ones do not. This is the durable red proof of the whole feature: it demonstrates that the one emitted argument is exactly what changes the compiled output, and it stays meaningful after the pre-fix code is long gone. This reconstruction needs the shared template file alongside the spliced master, so write the mutated master into the same build directory the source-only build produced rather than an unrelated tmp directory, or copy the shared template next to it — mirror whichever approach the analog module uses for its compile root.

    Second, the undeclared-argument basis. Take the custom-template fixture's post-fix emitted master — whose template declares no text-language parameter — splice a text-language argument into its show-rule region, and assert a real compile raises. Use a bare exception assertion and never match on the compiler's message text: the compiler's wording is not a contracted upstream API, and this repository has deliberately removed that class of coupling elsewhere. This is the durable red proof of WHY the judgment boundary exists, and it is the standing regression guard for anyone who later tries to simplify the predicate into a declaration check.

    Then perform and RECORD the manual red-to-green confirmation, because these gates are authored after the fix (implementation in Wave 1, gate in Wave 2 — the established order in this repository). Stash or revert Plan 01's changes, run this whole module, and observe what actually goes red: the German extraction case, the Japanese source case, and the precedence case should all fail, while the three non-regression cases should still pass because nothing was being injected before. Restore the fix and observe everything green. Write both observations into the SUMMARY with the actual failure output, not a paraphrase.

    Finally, run the clean-signal full suite and compare against the baseline Plan 01 recorded. Confirm no NEW failure versus that baseline, and confirm the version-sync gate is still green.

    Do NOT add a Japanese PDF-text extraction assertion, and do NOT add a font file to the repository to make one possible. D-07 rejected that deliberately; the German case already proves the linkage, and CJK extraction depends on runner font availability that has never been confirmed.
  </action>
  <verify>
    <automated>uv run pytest tests/test_typst_lang_gate.py tests/test_preview_version_sync.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_typst_lang_gate.py tests/test_preview_version_sync.py -q` exits 0 including both reconstructions.
    - The omission reconstruction compiles a mutated master for real and asserts the English supplements appear in the extracted text while the German ones do not.
    - The undeclared-argument reconstruction asserts a real compile raises using a bare exception assertion, with no assertion anywhere in the module matching on compiler error text.
    - The SUMMARY records the manual red-to-green confirmation with actual observed output: which cases failed with Plan 01 reverted, and that all pass once restored.
    - `uv run python -m pytest -q --ignore=tests/test_integration_advanced.py --ignore=tests/test_integration_basic.py --ignore=tests/test_integration_multi_doc.py --ignore=tests/test_integration_nested_toctree.py --ignore=tests/test_examples_basic.py` shows no NEW failure versus the baseline recorded in 27.1-01-SUMMARY.md.
    - `git diff -U0 typsphinx/templates/base.typ` still shows exactly the two lines Plan 01 changed, and `uv run pytest tests/test_preview_version_sync.py -q` exits 0.
    - No font binary was added to the repository (`git status --porcelain` lists no font file).
  </acceptance_criteria>
  <done>
    A durable TestPreFixBasisFailureProof proves both directions — removing the argument makes a real recompile fall back to English supplements, and splicing it into a template that never declared it makes a real compile raise; the manual red-to-green transition is performed and recorded with real output; the full suite shows no new failures and the version-sync gate is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test tier only | This plan adds fixtures and a test module under `tests/`. It introduces no new runtime trust boundary; every fixture `conf.py` is test-authored and trusted. The production boundary (`conf.py` → build-time Python → emitted argument → `typst.compile()`) is exactly as established in `27.1-01-PLAN.md` and unchanged here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27.1-06 | Denial of Service | The fixtures invoke real sphinx-build subprocesses and real Typst compiles | low | accept | Bounded, offline, deliberately minimal fixture documents; guarded by the typst/pypdf availability skips; the package-path case deliberately avoids a second package download by asserting on emitted source only. Same pattern every existing GATE-01 module in this repository uses. |
| T-27.1-07 | Tampering (supply chain) | A vendored font binary, had the Japanese case been converted to a PDF-text assertion | low | mitigate | Explicitly prohibited by this plan (see `must_haves.prohibitions`): no font binary enters the repository, so no redistribution-licence obligation and no multi-megabyte opaque blob is introduced to a pure-Python extension. |
| T-27.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase — zero new runtime deps (milestone invariant). The Typst Universe package used by the package-path fixture is already pinned by an existing fixture in this suite and is fetched by the Typst toolchain, not by a Python package manager. |

ASVS L1, block-on-high: no high or critical threat. Test tier only, with no network or data ingress beyond what the existing suite already performs.
</threat_model>

<verification>
- `uv run pytest tests/test_typst_lang_gate.py -q` green: four positive cases, three non-regression cases, two durable reconstructions.
- `uv run pytest tests/test_preview_version_sync.py -q` green (the 3-way version-sync surface is untouched by this phase).
- Clean-signal full suite shows no NEW failure versus the baseline recorded in `27.1-01-SUMMARY.md`; the ~45-48 documented worktree-environmental failures are excluded and are not regressions.
- `git diff -U0 typsphinx/templates/base.typ` still shows exactly two changed lines (SC#5 holds after the gate work — this plan touches only `tests/`).
- All builds are invoked as `sys.executable -m sphinx` subprocesses; worktree provisioning per CLAUDE.md is done first and every command runs via `uv run`.
</verification>

<success_criteria>
- SC#1 is proven by the D-07 split: the Japanese value reaching the emitted source under a real compile, plus the German supplements extracted from a real PDF with NBSP-tolerant matching. (ROADMAP SC#1's wording should be updated post-implementation to describe this split — see the objective's note.)
- SC#2: an explicit element value beats the derived one, proven on emitted output.
- SC#3: all three non-default paths — explicit custom template, srcdir shadow, and package — receive no injected argument and continue to build, two of them under a real compile.
- SC#4: a Sphinx language value that cannot be reduced to a valid code does not abort the build and emits nothing.
- The red-to-green transition is real and recorded; the durable reconstructions keep it meaningful after the pre-fix code is gone.
</success_criteria>

<output>
Create `.planning/phases/27.1-typst-text-lang-from-sphinx-language-config/27.1-03-SUMMARY.md` when done. It MUST record:
- The manual red-to-green confirmation with actual observed output: exactly which cases failed with Plan 01 reverted, and that all pass once restored.
- Whether any case was skipped rather than run (typst/pypdf availability) and on which guard.
- The clean-signal suite counts, compared against the baseline in `27.1-01-SUMMARY.md`.
- A recommendation to update ROADMAP SC#1's wording to describe the D-07 split proof, so the verifier and future readers do not treat the split as a miss.
</output>
