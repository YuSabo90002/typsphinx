---
phase: 74-the-doctest-block-handler-its-real-compile-gate-and-the-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md
autonomous: true
requirements: [TRN-01, QUA-14]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'PHASE_BASE_SHA is this worktree HEAD recorded before this plan makes any commit. Outside .planning/ its tree equals the milestone base 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b, so every base measurement is pre-edit. Every later plan reads PHASE_BASE_SHA from 74-BASE-EVIDENCE.md with sed and never re-derives it.'
    - 'Base build first (ROADMAP Phase 74 binding ordering): the clean LANG=C LC_ALL=C -b typst build of docs/source at PHASE_BASE_SHA is taken and recorded before any typsphinx/ edit of this phase exists anywhere.'
    - 'SC1 base: the evidence records the English `build succeeded` line and its N as BASE_WARNING_COUNT, the count of doctest_block unknown-node warnings as BASE_DOCTEST_UNKNOWN_COUNT (at least 1, the positive control for the tip zero), and the verbatim api/index.typ lines carrying the collapsed compute_content_include_path and compute_template_import_path example runs.'
    - 'QUA-14 census (constraint 6, D-07): the census comes from the whole base console log, not from the docstrings the requirement names. BASE_RAW_TOTAL is the raw substring count of both message classes over the whole log, BASE_ATTRIBUTED_COUNT the count of path-attributed lines, and every raw line is transcribed verbatim.'
    - 'D-07: every unattributed raw line is attributed to a named typsphinx docstring by its (docstring-relative line, message class) pair, using a scratch napoleon-plus-docutils probe over typsphinx docstrings. FIX_LIST names every docstring the census reaches, including typsphinx.pathfmt.quote_path when the probe attributes the unattributed lines to it.'
    - 'Edge (QUA-14 empty): a base census of zero, or a zero doctest unknown-node count, halts the plan, because a zero base would mean a broken filter or locale rather than a clean tree.'
    - 'Edge (QUA-14 encoding): every Sphinx build and every grep over its console text runs under LANG=C LC_ALL=C, and every count is void unless the English `build succeeded` line is present in the same log.'
    - 'Edge (QUA-14 precision): BASE_ATTRIBUTED_COUNT plus BASE_UNATTRIBUTED_COUNT equals BASE_RAW_TOTAL, and BASE_UNATTRIBUTED_UNMATCHED is 0.'
    - 'The D-06 amendment context is re-measured: BASE_API_TERMS_ITEM_COUNT records how many terms.item calls the base api/index.typ carries.'
    - 'Phase-head remote reads are recorded for 74-07: main''s required status checks (strict value and sorted contexts), the branch census, origin''s main head, and the job-name set of the reference branch CI run from 73-CI-EVIDENCE.md.'
  artifacts:
    - path: .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md
      provides: 'PHASE_BASE_SHA, the base -b typst build with its positive controls, the api/index.typ example region, the QUA-14 raw and attributed census with FIX_LIST, and the phase-head remote reads'
      contains: 'PHASE_BASE_SHA = '
  key_links:
    - from: '74-BASE-EVIDENCE.md'
      to: '74-03, 74-04, 74-05, 74-06 and 74-07'
      via: 'PHASE_BASE_SHA, FIX_LIST, BASE_WARNING_COUNT, BASE_DOCTEST_UNKNOWN_COUNT, BASE_RAW_TOTAL, BASE_ATTRIBUTED_COUNT, REQUIRED_CONTEXTS_HEAD and REFERENCE_CI_RUN_ID are read back with sed'
      pattern: 'FIX_LIST = '
    - from: 'the base console log'
      to: 'FIX_LIST'
      via: 'the attributed regex plus the (line, message) probe attribution of every unattributed line'
      pattern: 'BASE_UNATTRIBUTED_UNMATCHED = 0'
  prohibitions:
    - statement: 'MUST NOT clear, narrow or hide the docutils Unexpected indentation / Block quote ends message class by anything other than editing the offending docstring reST: no suppress_warnings, -q, -W exemption, keep_warnings or report_level change, no conf.py or api/index.rst edit, no noqa-style marker, no census narrowed to the docstrings the requirement names, and no unattributed line left out of the census.'
      requirement_id: QUA-14
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Take the phase's pre-edit baseline before anything else lands (ROADMAP Phase 74, "Base build
first"), and produce the QUA-14 census the fix list is derived from (constraint 6, D-07).

- Record `PHASE_BASE_SHA` and prove its product tree is the milestone base `6cc44f22`.
- Build `docs/source` clean with `-b typst` under the C locale and record SC1's three base facts:
  the `build succeeded, N warnings.` line, the doctest_block unknown-node count, and the verbatim
  `api/index.typ` example region.
- Census the two docutils message classes over the whole console log, both as a raw substring count
  and as an attributed count (D-07), attribute every unattributed line to a named docstring, and
  derive `FIX_LIST`, which 74-04 fixes. D-07 puts `quote_path`'s docstring in that list when the
  census reaches it.
- Read the phase-head remote facts that 74-07 compares at phase close.

Purpose: SC1 and SC3 compare the tip against this base, and QUA-14's fix list must come from a
fresh full-build measurement rather than from the two docstrings the requirement names.

Output: `74-BASE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CONTEXT.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-RESEARCH.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run` (`CLAUDE.md` § "Worktree-isolated execution"). The line is
CLAUDE.md's mandated line plus two additions decided here. `--extra docs` is needed because
`docs/source/conf.py` imports `myst_parser` and `sphinx_autodoc_typehints`, which only the `docs`
extra provides. `--python 3.13.13` puts every worktree of this phase on the same interpreter version
as the main checkout and 74-RESEARCH.md's measurements, the v0.9.4 and v0.9.5 precedent. It also
avoids the known `libz.so.1` Pillow failure of uv-managed CPython inside the FHS sandbox. Do not
change the interpreter on your own judgement; record whatever `pyvenv.cfg` reports.

**Build shape.** Every docs build runs from this worktree's root as
`LANG=C LC_ALL=C uv run python -m sphinx -b typst docs/source <out>`, into an absolute output
directory under your scratch directory that you remove with `rm -rf` immediately before the build.
Never build into `docs/_build/`, and never pass `-q`.

**Locale.** The host runs `LANG=ja_JP.UTF-8`. Prefix every Sphinx run and every grep, sort or diff of
its output with `LANG=C LC_ALL=C`. If the English `build succeeded` line is missing from a log, that
log is localised and every count taken from it is void.

**Timeouts.** Run every docs build, every full pytest run and every verify block that contains one with the Bash tool's `timeout` set to 600000, in the foreground. The default two-minute limit can cut a build or the full suite short and leave a truncated log.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put any explanation on
the next line. The verify fails on any key line containing an opening parenthesis.

**Scratch.** Parallel executors share the session scratchpad. Put every log and helper under your
own `mktemp -d` directory, recorded as `SCRATCH_74_01`, and prefix helper files `p7401_`. Helpers are
never committed.

**Commits.** Plain `git commit`. Never a GSD commit helper, which has created a
`gsd/vX.Y-milestone` decoy branch in every milestone since v0.9.1. Never write `74-VERIFICATION.md`.
Never touch the untracked `probe_*.typ` files in the main checkout's root.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->

## Planning-time measurements (2026-09-20, HEAD `1deb7e4b`, product tree = `6cc44f22`; context only, re-measure, never transcribe)

| Fact | Value at planning |
|------|-------------------|
| Clean base `-b typst`, C locale, main `.venv` (nix 3.13.13) | `build succeeded, 5 warnings.` |
| doctest_block unknown-node warnings | 2, both in `api/index`, each on a `writing output... [api/index]` progress line or on its own line |
| `api/index.typ` collapsed runs (`grep -c 'text(">>> '`) | 2, the first at `:811` (`compute_content_include_path`) |
| `grep -c 'terms\.item' api/index.typ` | 0 (the D-06 amendment's falsified claim 1) |
| Raw census | 6 `Unexpected indentation` + 4 `Block quote ends` = 10 lines |
| Attributed lines | 3, all `...translator.py:docstring of typsphinx.translator.TypstTranslator.visit_toctree:{5,6,21}` |
| Unattributed lines | 7 terse lines: `:19: (ERROR/3)` and `:21: (WARNING/2)` twice (quote_path, probe-parsed via builder and writer), and `:5: (ERROR/3)`, `:6: (WARNING/2)`, `:21: (ERROR/3)` once (visit_toctree, probe-parsed) |
| Scratch docstring probe | only `typsphinx.translator.TypstTranslator.visit_toctree` (5 ERROR, 6 WARNING, 21 ERROR) and `typsphinx.pathfmt.quote_path` (19 ERROR, 21 WARNING) among typsphinx-defined functions, classes and methods |
| Expected FIX_LIST | `typsphinx.pathfmt.quote_path|typsphinx.translator.TypstTranslator.visit_toctree` |
| Reference branch CI run | `RUN_ID = 35083828156` in `.planning/milestones/v0.9.5-phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md`, 12 jobs |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: head check, PHASE_BASE_SHA, and the clean base -b typst build with SC1's base records</name>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md</files>
  <read_first>
    - .planning/ROADMAP.md § "v0.9.6" constraints 4, 5, 6 and 11, and § "Phase 74" (binding ordering, SC1 and SC3)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-RESEARCH.md § "Base Build" and § "Worktree Docs-Extra Gap"
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CONTEXT.md D-06 and D-07 (the AMENDED post-research decisions)
    - docs/source/conf.py lines 1-40 (the `sys.path` insert of the checkout root and the environment-driven `language`)
    - typsphinx/writer.py lines 32-113 (the two `Examples:` docstrings whose doctest blocks the base collapses)
    - CLAUDE.md § "Worktree-isolated execution" and § "NixOS development shell"
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`; `pwd -P`, which must be under `/home/yuta/Documents/typsphinx/.claude/worktrees/`; `test -f .git; echo "exit:$?"`; `grep -c typsphinx-fhs-run "$(command -v uv)"`.
   - The provisioning line from `<worktree_provisioning>` and its exit status.
   - `sed -n 's/^home = //p;s/^version_info = //p' .venv/pyvenv.cfg`, as keys `PYVENV_HOME` and `PYVENV_VERSION_INFO`.
   - One `uv run python -c` call printing `importlib.metadata.version` of `sphinx`, `docutils`, `typst`, `sphinx-autodoc-typehints` and `myst-parser`, as keys `SPHINX_VERSION`, `DOCUTILS_VERSION`, `TYPST_PY_VERSION`, `AUTODOC_TYPEHINTS_VERSION` and `MYST_PARSER_VERSION`.
   - `printenv SPHINX_LANGUAGE READTHEDOCS_LANGUAGE; echo "exit:$?"`, which must print nothing but `exit:1`. Either variable would change the docs language (`conf.py` `_resolve_language()`). If one is set, write `## HALT: docs language overridden` and stop.
   - Before any commit: `PHASE_BASE_SHA = ` followed by `git rev-parse HEAD`, and `SCRATCH_74_01 = ` followed by the path `mktemp -d` printed.

2. **`## Base identity`.**
   - Record `MILESTONE_BASE = 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b`.
   - Record `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b "$PHASE_BASE_SHA" -- . ':(exclude).planning'`, which must print nothing. Otherwise write `## HALT: base is not the milestone base` and stop.
   - Record `grep -c doctest typsphinx/translator.py || true` as `BASE_TRANSLATOR_DOCTEST_MENTIONS`. The requirement says it is 0 on the base: no handler exists.

3. **`## Base Typst build`.**
   - Run `rm -rf "$S/base-typst"`, then `LANG=C LC_ALL=C uv run python -m sphinx -b typst docs/source "$S/base-typst"` with stdout and stderr both redirected into `$S/p7401_base-typst.log`, then `echo "exit:$?"`, which must print `exit:0`. Record the full command under the sub-heading `### Build command`, so 74-04 and 74-05 reuse it with only the output path changed.
   - The English summary line from `LANG=C LC_ALL=C grep -E '^build succeeded' "$S/p7401_base-typst.log"`, verbatim. Key `BASE_WARNING_COUNT` is the integer in `build succeeded, N warnings.` or `build succeeded, 1 warning.`, and `0` when the line reads `build succeeded.`.
   - Transcribe every `WARNING:` and `ERROR:` line of the log verbatim under `### Counted warnings`.

4. **`### Doctest block warnings`.**
   - Key `BASE_DOCTEST_UNKNOWN_COUNT`: `LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$S/p7401_base-typst.log" | wc -l`. Count occurrences, not lines: a warning can share a line with the `writing output...` progress text.
   - Transcribe each such warning's first line, with `grep -n`, verbatim.
   - **Positive control (constraints 4 and 5):** `BASE_DOCTEST_UNKNOWN_COUNT` must be at least 1. If it is 0, or the English `build succeeded` line is missing, write `## HALT: positive control failed` with the log's tail and stop.

5. **`### api/index.typ example region`.** Using `$S/base-typst/api/index.typ`:
   - Key `BASE_API_COLLAPSED_RUNS`: `grep -c 'text(">>> ' "$S/base-typst/api/index.typ" || true`, at least 1.
   - Key `BASE_API_EXAMPLE_LINE`: the line number of the line containing `text(">>> compute_content_include_path(`.
   - Transcribe `grep -n -B3 'text(">>> ' "$S/base-typst/api/index.typ"` verbatim. This is the verbatim region SC1 names: the three prompts, their continuations and their outputs on one line.
   - Key `BASE_API_PYTHON_FENCES`: the count of lines that are exactly three backticks followed by `python`, from `grep -cx` or 0.
   - Key `BASE_API_TERMS_ITEM_COUNT`: `grep -c 'terms\.item' "$S/base-typst/api/index.typ" || true`. This is context for the D-06 amendment, which was made because this count was 0 at research time. Record the measured value either way; it gates nothing.

6. Write `74-BASE-EVIDENCE.md` under `# Phase 74 — Base Evidence (PHASE_BASE_SHA, base build, QUA-14 census)`, with the key block at the top and the section headings above, and commit it.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-BASE-EVIDENCE.md" &amp;&amp; MB=6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; P="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$P" HEAD &amp;&amp; [ "$(k MILESTONE_BASE)" = "$MB" ] &amp;&amp; [ -z "$(git diff --name-only "$MB" "$P" -- . ':(exclude).planning')" ] &amp;&amp; S="$(k SCRATCH_74_01)" &amp;&amp; L="$S/p7401_base-typst.log" &amp;&amp; test -s "$L" &amp;&amp; T="$S/base-typst/api/index.typ" &amp;&amp; test -s "$T" &amp;&amp; BL="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$L" || true)" &amp;&amp; [ -n "$BL" ] &amp;&amp; N="$(printf '%s\n' "$BL" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')" &amp;&amp; { [ -n "$N" ] || { [ "$BL" = 'build succeeded.' ] &amp;&amp; N=0; }; } &amp;&amp; [ "$N" = "$(k BASE_WARNING_COUNT)" ] &amp;&amp; U="$(LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$L" | wc -l)" &amp;&amp; [ "$U" -ge 1 ] &amp;&amp; [ "$U" = "$(k BASE_DOCTEST_UNKNOWN_COUNT)" ] &amp;&amp; C="$(grep -c 'text(">>> ' "$T" || true)" &amp;&amp; [ "$C" -ge 1 ] &amp;&amp; [ "$C" = "$(k BASE_API_COLLAPSED_RUNS)" ] &amp;&amp; grep -qF 'text(">>> compute_content_include_path(' "$F" &amp;&amp; [ "$(k BASE_API_TERMS_ITEM_COUNT)" = "$(grep -c 'terms\.item' "$T" || true)" ] &amp;&amp; [ -n "$(k BASE_API_EXAMPLE_LINE)" ] &amp;&amp; [ -n "$(k BASE_API_PYTHON_FENCES)" ] &amp;&amp; [ -n "$(k PYVENV_VERSION_INFO)" ] &amp;&amp; [ -n "$(k PYVENV_HOME)" ] &amp;&amp; [ -n "$(k SPHINX_VERSION)" ] &amp;&amp; [ -n "$(k AUTODOC_TYPEHINTS_VERSION)" ] &amp;&amp; for s in '## Head check and provisioning' '## Base identity' '## Base Typst build' '### Build command' '### Counted warnings' '### Doctest block warnings' '### api/index.typ example region'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- not in a worktree under .claude/worktrees, or the FHS shim absent from PATH
- `PHASE_BASE_SHA` not 40 characters, not an ancestor of HEAD, or its product tree differing from 6cc44f22
- the base log or base `api/index.typ` missing, or no English `build succeeded` line in the log
- the log's N differing from `BASE_WARNING_COUNT`
- a doctest_block unknown-node count of 0, or one differing from `BASE_DOCTEST_UNKNOWN_COUNT`
- a collapsed-run count of 0 or differing from its key, or the compute_content_include_path run not transcribed
- the terms.item count differing from its key, or an interpreter or version key empty
- any product-tree file changed by this plan, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `PHASE_BASE_SHA`, `MILESTONE_BASE`, `SCRATCH_74_01`, `PYVENV_HOME`, `PYVENV_VERSION_INFO` and the five version keys are recorded as bare values, and the product tree at `PHASE_BASE_SHA` equals `6cc44f22` outside `.planning/`.
    - `BASE_WARNING_COUNT` equals the N of the English `build succeeded` line of `$S/p7401_base-typst.log`.
    - `BASE_DOCTEST_UNKNOWN_COUNT` is at least 1 and equals a fresh C-locale occurrence count of the log.
    - `BASE_API_COLLAPSED_RUNS` is at least 1, and the collapsed `compute_content_include_path` run is transcribed verbatim with its line number.
    - The build command is recorded verbatim under `### Build command`, without `-q`.
  </acceptance_criteria>
  <done>The pre-edit `-b typst` baseline exists at a recorded `PHASE_BASE_SHA`, with a non-zero C-locale doctest unknown-node count as SC1's positive control and the collapsed example region transcribed.</done>
</task>

<task type="auto">
  <name>Task 2: QUA-14 census over the whole base log (raw and attributed, D-07), attribution of every unattributed line, and FIX_LIST</name>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md (Task 1: `SCRATCH_74_01`, the base log path)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-RESEARCH.md § "QUA-14 Census Nuance" and Pitfall 1
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CONTEXT.md D-07 and § "Claude's Discretion" (QUA-14 fix granularity)
    - typsphinx/translator.py lines 5415-5461 (`visit_toctree`'s docstring) and typsphinx/pathfmt.py lines 46-80 (`quote_path`'s docstring)
  </read_first>
  <action>
All greps below run as `LANG=C LC_ALL=C` over `$S/p7401_base-typst.log`. Use `|| true` on every grep
whose count may be 0 so the shell does not abort.

1. **`## QUA-14 census`, raw counts (D-07).**
   - Key `BASE_RAW_UNEXPECTED_INDENTATION`: `grep -o 'Unexpected indentation' "$L" | wc -l`.
   - Key `BASE_RAW_BLOCK_QUOTE_ENDS`: `grep -o 'Block quote ends without a blank line' "$L" | wc -l`.
   - Key `BASE_RAW_TOTAL`: their sum.
   - Transcribe every raw line, `grep -nE 'Unexpected indentation|Block quote ends without a blank line' "$L"`, verbatim under `### Raw lines`.

2. **Attributed lines.**
   - The attributed filter is `grep -nE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)'`.
   - Transcribe its output verbatim under `### Attributed lines`, with each hit's originating file, fully qualified name and docstring-relative line.
   - Key `BASE_ATTRIBUTED_COUNT`: its line count.

3. **Unattributed lines.**
   - These are the raw lines that do not contain `.py:docstring of `. Transcribe them verbatim under `### Unattributed lines`.
   - Key `BASE_UNATTRIBUTED_COUNT`. `BASE_ATTRIBUTED_COUNT` plus `BASE_UNATTRIBUTED_COUNT` must equal `BASE_RAW_TOTAL`.

4. **`### Docstring probe` (attribution, D-07).**
   - Write the scratch helper `$S/p7401_docstring_probe.py` and run it with `uv run python`. It is never committed.
   - It imports every module under `typsphinx` with `pkgutil.walk_packages`. It visits every function, class, and function defined directly in a class body whose defining source file is under this worktree's `typsphinx/` directory (`inspect.getsourcefile`). That rule skips builtins and generated `__annotations__`, `__new__` and similar docstrings.
   - For each object whose own `__doc__` is a string, it converts `inspect.cleandoc(__doc__)` with `sphinx.ext.napoleon.docstring.GoogleDocstring(..., sphinx.ext.napoleon.Config()).lines()`. It parses the joined result with `docutils.core.publish_doctree(..., settings_overrides={"warning_stream": buffer, "report_level": 2})`.
   - It prints one tab-separated row per system message matching either class: fully qualified name, docstring-relative line, level word, message.
   - Transcribe the helper's full output verbatim.

5. **`### Attribution table`.**
   - One row per unattributed line. Name the probe row with the same docstring-relative line and the same message class: `Unexpected indentation` pairs with ERROR, `Block quote ends` with WARNING. A pair that matches several docstrings lists all of them.
   - One row per attributed line, confirming the probe reproduces its line and class for the same fully qualified name.
   - Key `BASE_UNATTRIBUTED_UNMATCHED`: the number of unattributed lines no probe row matches. It must be 0. Otherwise write `## HALT: unattributed census line` naming the lines, and stop. Do not guess an attribution.

6. **`### Fix list`.**
   - Key `FIX_LIST`: the `|`-joined, `LC_ALL=C`-sorted, de-duplicated fully qualified names of every docstring named in steps 2 and 5.
   - Key `FIX_LIST_COUNT`.
   - This is the whole census over the whole log (constraint 6), never narrowed to `visit_toctree`. Per D-07, `typsphinx.pathfmt.quote_path` belongs here when the probe attributes unattributed lines to it.
   - Probe rows for docstrings the build log never reported go under `### Probe-only rows (not in the build census)`. They are not added to `FIX_LIST`; name them in the SUMMARY for the owner.

7. **Positive control (constraint 5).** `BASE_RAW_TOTAL` and `BASE_ATTRIBUTED_COUNT` must both be at least 1. Otherwise write `## HALT: census positive control failed` and stop.

8. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-BASE-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; S="$(k SCRATCH_74_01)" &amp;&amp; L="$S/p7401_base-typst.log" &amp;&amp; test -s "$L" &amp;&amp; RU="$(LANG=C LC_ALL=C grep -o 'Unexpected indentation' "$L" | wc -l)" &amp;&amp; RB="$(LANG=C LC_ALL=C grep -o 'Block quote ends without a blank line' "$L" | wc -l)" &amp;&amp; [ "$RU" = "$(k BASE_RAW_UNEXPECTED_INDENTATION)" ] &amp;&amp; [ "$RB" = "$(k BASE_RAW_BLOCK_QUOTE_ENDS)" ] &amp;&amp; RT=$((RU + RB)) &amp;&amp; [ "$RT" -ge 1 ] &amp;&amp; [ "$RT" = "$(k BASE_RAW_TOTAL)" ] &amp;&amp; A="$(LANG=C LC_ALL=C grep -cE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' "$L" || true)" &amp;&amp; [ "$A" -ge 1 ] &amp;&amp; [ "$A" = "$(k BASE_ATTRIBUTED_COUNT)" ] &amp;&amp; UA="$(LANG=C LC_ALL=C grep -E 'Unexpected indentation|Block quote ends without a blank line' "$L" | LANG=C LC_ALL=C grep -cv '\.py:docstring of ' || true)" &amp;&amp; [ "$UA" = "$(k BASE_UNATTRIBUTED_COUNT)" ] &amp;&amp; [ "$((A + UA))" = "$RT" ] &amp;&amp; [ "$(k BASE_UNATTRIBUTED_UNMATCHED)" = 0 ] &amp;&amp; FL="$(k FIX_LIST)" &amp;&amp; [ -n "$FL" ] &amp;&amp; [ "$(printf '%s\n' "$FL" | tr '|' '\n' | grep -c .)" = "$(k FIX_LIST_COUNT)" ] &amp;&amp; for n in $(LANG=C LC_ALL=C grep -oE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected|Block)' "$L" | sed -E 's/^\.py:docstring of ([A-Za-z0-9_.]+):.*/\1/' | LC_ALL=C sort -u); do printf '%s\n' "$FL" | tr '|' '\n' | grep -qxF "$n" || { echo "fix list missing: $n"; exit 1; }; done &amp;&amp; test -s "$S/p7401_docstring_probe.py" &amp;&amp; for s in '## QUA-14 census' '### Raw lines' '### Attributed lines' '### Unattributed lines' '### Docstring probe' '### Attribution table' '### Fix list'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; P="$(k PHASE_BASE_SHA)" &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `fix list missing: NAME` or `missing section: NAME` printed. Each of these makes it fail:
- the base log missing
- a raw count of 0, or any raw count differing from its key
- an attributed count of 0 or differing from its key
- the unattributed count differing from its key, or attributed plus unattributed differing from the raw total
- `BASE_UNATTRIBUTED_UNMATCHED` not 0
- `FIX_LIST` empty, its entry count differing from `FIX_LIST_COUNT`, or an attributed docstring missing from it
- the probe helper absent, or a product-tree change
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `BASE_RAW_TOTAL` equals `BASE_RAW_UNEXPECTED_INDENTATION` plus `BASE_RAW_BLOCK_QUOTE_ENDS`, is at least 1, and every raw line is transcribed verbatim.
    - `BASE_ATTRIBUTED_COUNT` is at least 1, and each attributed line is transcribed with its file, fully qualified name and docstring-relative line.
    - Every unattributed line is matched to a probe row by line and message class, and `BASE_UNATTRIBUTED_UNMATCHED = 0`.
    - `FIX_LIST` contains every attributed docstring and every probe-attributed docstring, including `typsphinx.pathfmt.quote_path` when the probe attributes lines to it (D-07).
  </acceptance_criteria>
  <done>QUA-14's fix list is derived from the whole base build log, raw and attributed (D-07), with every line of both message classes accounted for.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-head remote reads: required status checks, branch census, origin main, and the reference CI job set</name>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md</files>
  <read_first>
    - .planning/ROADMAP.md § "v0.9.6" constraint 10 and § "Phase 74" SC4
    - .planning/milestones/v0.9.5-phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md (its `RUN_ID` and job census, the reference job set)
    - .planning/milestones/v0.9.5-phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md § "Phase-head remote reads" (the shape to mirror)
    - .github/workflows/ci.yml lines 1-30 (triggers: push and pull_request are scoped to main and develop)
  </read_first>
  <action>
1. **`## Phase-head remote reads`.**
   - `gh auth status; echo "exit:$?"`, which must print `exit:0`. Otherwise write `## HALT: gh not authenticated` and stop.
   - Required checks: `gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict`, key `REQUIRED_STRICT_HEAD`. The same call with `--jq '[.contexts[]] | sort | join("|")'` gives key `REQUIRED_CONTEXTS_HEAD`, and with `--jq '.contexts | length'` key `REQUIRED_CONTEXT_COUNT_HEAD`.
   - `git ls-remote origin refs/heads/main`: key `ORIGIN_MAIN_AT_HEAD`, the SHA. Key `MAIN_MOVED` is `no` when it equals `6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b`, otherwise `yes`. This is a record for `/gsd-complete-milestone`, not a gate.

2. **`## Branch census at phase head`.**
   - Record `git branch --list 'gsd/*' -v` and `git ls-remote --heads origin 'refs/heads/gsd/v0.9.6*'`, verbatim.
   - Key `HEAD_LOCAL_DECOY`: `present` if a local `gsd/v0.9.6-milestone` exists, else `absent`.
   - Key `ORIGIN_BRANCH_AT_HEAD`: the SHA of `refs/heads/gsd/v0.9.6-doctest-block-rendering-and-release` on origin, or `none`. `none` is expected, because the branch has never been pushed.
   - Change nothing here. Constraint 10's decoy handling runs in 74-07, immediately before the push.

3. **`## Reference CI job set`.**
   - Key `REFERENCE_CI_RUN_ID`: `sed -n 's/^RUN_ID = //p'` over `73-CI-EVIDENCE.md`.
   - Key `REFERENCE_JOB_NAMES`: `gh run view "$REFERENCE_CI_RUN_ID" --json jobs --jq '[.jobs[].name] | sort | join("|")'`.
   - Key `REFERENCE_JOB_COUNT`: `--jq '.jobs | length'`.
   - Key `CI_YML_CHANGED_SINCE_MILESTONE_BASE`: `no` when `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b HEAD -- .github/workflows/ci.yml` prints nothing, otherwise `yes`.

4. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-BASE-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; gh auth status &amp;&amp; RS="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict)" &amp;&amp; [ -n "$RS" ] &amp;&amp; [ "$RS" = "$(k REQUIRED_STRICT_HEAD)" ] &amp;&amp; RC="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '[.contexts[]] | sort | join("|")')" &amp;&amp; [ -n "$RC" ] &amp;&amp; [ "$RC" = "$(k REQUIRED_CONTEXTS_HEAD)" ] &amp;&amp; [ "$(k REQUIRED_CONTEXT_COUNT_HEAD)" -ge 1 ] &amp;&amp; OM="$(k ORIGIN_MAIN_AT_HEAD)" &amp;&amp; [ "${#OM}" = 40 ] &amp;&amp; case "$(k MAIN_MOVED)" in yes|no) true ;; *) false ;; esac &amp;&amp; case "$(k HEAD_LOCAL_DECOY)" in present|absent) true ;; *) false ;; esac &amp;&amp; [ -n "$(k ORIGIN_BRANCH_AT_HEAD)" ] &amp;&amp; RID="$(k REFERENCE_CI_RUN_ID)" &amp;&amp; [ "$RID" = "$(sed -n 's/^RUN_ID = //p' .planning/milestones/v0.9.5-phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md | head -n 1)" ] &amp;&amp; JN="$(gh run view "$RID" --json jobs --jq '[.jobs[].name] | sort | join("|")')" &amp;&amp; [ -n "$JN" ] &amp;&amp; [ "$JN" = "$(k REFERENCE_JOB_NAMES)" ] &amp;&amp; [ "$(k REFERENCE_JOB_COUNT)" -ge 1 ] &amp;&amp; [ "$(k CI_YML_CHANGED_SINCE_MILESTONE_BASE)" = no ] &amp;&amp; for s in '## Phase-head remote reads' '## Branch census at phase head' '## Reference CI job set'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; P="$(k PHASE_BASE_SHA)" &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- gh unauthenticated
- the live required strict value or sorted contexts differing from the recorded head keys
- `ORIGIN_MAIN_AT_HEAD` malformed, or `MAIN_MOVED` or `HEAD_LOCAL_DECOY` outside its allowed values
- the reference run id differing from 73-CI-EVIDENCE.md, or its live job-name set differing from `REFERENCE_JOB_NAMES`
- ci.yml changed since the milestone base, or a product-tree change
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `REQUIRED_STRICT_HEAD`, `REQUIRED_CONTEXTS_HEAD` and `REQUIRED_CONTEXT_COUNT_HEAD` equal a fresh `gh api` read.
    - The branch census is recorded verbatim, with `HEAD_LOCAL_DECOY` and `ORIGIN_BRANCH_AT_HEAD`.
    - `REFERENCE_CI_RUN_ID` equals 73-CI-EVIDENCE.md's `RUN_ID`, and `REFERENCE_JOB_NAMES` equals its live sorted job-name set.
  </acceptance_criteria>
  <done>74-07 has the phase-head required checks, branch census and reference job set it compares against at phase close.</done>
</task>

</tasks>

<flagged_assumptions>
**Phase-wide probe accounting** (spec-less fallback; no phase SPEC exists; `EDGE_ABSENT=1`, `PROHIB_ABSENT=1`).

Edge coverage: the orchestrator's report surfaced 14 items, and each is authored below or flagged.
- TRN-01 (5): adjacency, empty, encoding and ordering are explicit truths in 74-03. Concurrency is **unresolved**, flagged (a) below.
- TRN-02 (5): adjacency, encoding and ordering are explicit truths in 74-03. Empty is a `verification: backstop` marker in 74-03. Concurrency is **unresolved**, flagged (a) below.
- QUA-14 (4): empty, encoding and precision are explicit truths in this plan. Boundary is an explicit truth in 74-05.

Total: 14 = 11 explicit + 1 backstop + 2 unresolved + 0 dropped.

Prohibitions: five were kept, and each is authored descriptor-less and judgment-tier in the
`must_haves.prohibitions` of every plan it binds.
- P1, QUA-14 transparency: no suppression or narrowed census. This plan, 74-04, 74-05.
- P2, TRN-01 transparency: doctest text verbatim, no doctree mutation. 74-03.
- P3, TRN-02 safety: no pre-existing test edit, no reconstructed RED. 74-02, 74-03, 74-06.
- P4, TRN-01 values: no absorption of NUM-01, MSG-06, WR-02, WR-03 or another unknown_visit node; no config, workflow, flake.nix, pyproject.toml or uv.lock change. 74-04, 74-06.
- P5, TRN-01 safety: no tag, force-push, decoy push, PR, release trigger or second dispatch. 74-07.

Total: 5 kept = 5 authored + 0 dismissed. One canon candidate was referred and not minted: Typst
code injection through doctest content is canon injection, covered by `/gsd-secure-phase`. The
content reaches the output only inside a raw fence, the same trust model as `literal_block`.

Residual planner assumptions:
- (a) **Concurrency (TRN-01, TRN-02), unresolved.** A Sphinx `-j N` parallel write is not
  exercised by any plan. The handler adds no shared state: it delegates to `visit_literal_block`,
  whose flags live on the per-document translator instance. This is recorded as an assumption, not
  a proof.
- (b) **Empty (TRN-01).** docutils cannot produce an empty `doctest_block`: the parser needs a `>>>`
  line. The explicit truth covers a prompt line with no output line; the empty-block sub-case is
  inapplicable by grammar.
- (c) `uv sync --python 3.13.13` resolves in a fresh worktree, as it did throughout v0.9.4 and v0.9.5.
- (d) `gh` is authenticated in executor sessions (74-01 Task 3 and 74-07 check it).
- (e) **D-05 reading.** Planning-time measurement shows the tip's `api/index.typ` also differs where
  autodoc documents this phase's own changed symbols: the two new delegating methods, and the widened
  `node` parameter type of `visit_literal_block` / `depart_literal_block`, per D-04. These are
  neither a doctest-example region nor a QUA-14 docstring. D-05 calls them findings, not noise, so
  74-05 records them as findings and never folds them into either class. A hunk outside that
  enumerated set halts 74-05. The owner should confirm this reading at verification.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sphinx console text → evidence counts | a localised, quieted or incremental build could fake a zero or a base count |
| third-party autodoc extension → console | `sphinx-autodoc-typehints` prints unattributed docutils lines that a naive grep would miscount |
| worktree → GitHub API (read-only) | `gh api` and `gh run view` reads; no write crosses this boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-01 | Repudiation | base counts from a localised or incremental build | high | mitigate | `rm -rf` of the output directory before each build, `LANG=C LC_ALL=C`, the English `build succeeded` line required in the same log, and non-zero positive controls |
| T-74-02 | Tampering | a QUA-14 census narrowed to the named docstrings | high | mitigate | raw count over the whole log plus the attributed filter; every unattributed line matched by the probe; `BASE_UNATTRIBUTED_UNMATCHED = 0` enforced; prohibition P1 |
| T-74-03 | Information Disclosure | absolute worktree paths in transcribed console lines | low | accept | local paths only, no secrets; the repository is public and the paths carry no credentials |
| T-74-04 | Tampering | `uv sync` supply chain | low | accept | no new dependency is added; `uv sync` installs only what `uv.lock` pins |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide (every plan carries this list):
- `typsphinx/translator.py`: new methods `TypstTranslator.visit_doctest_block(self, node: nodes.doctest_block) -> None` and `TypstTranslator.depart_doctest_block(self, node: nodes.doctest_block) -> None` (74-03).
- `tests/test_doctest_block_render_gate.py` (74-02): class `TestDoctestBlockRenderGate`; module constants `FIXTURE_DIR`, `MASTER_DOCNAMES`, `SHAPE_SENTINELS`, `UNKNOWN_DOCTEST_BLOCK`, `FENCE_OPEN`; helpers `_run_sphinx_build_typstpdf`, `_wrapper_pdf_path`, `_assert_pdf_magic`, `_source_doctest_block`, `_unknown_doctest_chunks`, `_read_typ`; module fixture `doctest_block_build`.
- `tests/fixtures/doctest_block_render_gate/` (74-02): `conf.py`, `index.rst`, `context_a_paragraph.rst`, `context_b_nonfirst_positions.rst`.
- `tests/test_translator.py` new functions (74-03): `test_doctest_block_defaults_to_python_fence`, `test_doctest_block_honours_existing_language`, `test_literal_block_without_language_keeps_bare_fence`, `test_doctest_block_shares_list_item_wrapper`.
- Evidence files: `74-BASE-EVIDENCE.md` (this plan), `74-RED-EVIDENCE.md` (74-02, GREEN section by 74-03), `74-QUA14-EVIDENCE.md` (74-04), `74-TIP-EVIDENCE.md` (74-05), `74-GATES-EVIDENCE.md` (74-06), `74-CI-EVIDENCE.md` (74-07), and `COVERAGE.md` (74-06).

This plan creates `74-BASE-EVIDENCE.md` with keys `PHASE_BASE_SHA`, `MILESTONE_BASE`,
`SCRATCH_74_01`, `PYVENV_HOME`, `PYVENV_VERSION_INFO`, `SPHINX_VERSION`, `DOCUTILS_VERSION`,
`TYPST_PY_VERSION`, `AUTODOC_TYPEHINTS_VERSION`, `MYST_PARSER_VERSION`,
`BASE_TRANSLATOR_DOCTEST_MENTIONS`, `BASE_WARNING_COUNT`, `BASE_DOCTEST_UNKNOWN_COUNT`,
`BASE_API_COLLAPSED_RUNS`, `BASE_API_EXAMPLE_LINE`, `BASE_API_PYTHON_FENCES`,
`BASE_API_TERMS_ITEM_COUNT`, `BASE_RAW_UNEXPECTED_INDENTATION`, `BASE_RAW_BLOCK_QUOTE_ENDS`,
`BASE_RAW_TOTAL`, `BASE_ATTRIBUTED_COUNT`, `BASE_UNATTRIBUTED_COUNT`,
`BASE_UNATTRIBUTED_UNMATCHED`, `FIX_LIST`, `FIX_LIST_COUNT`, `REQUIRED_STRICT_HEAD`,
`REQUIRED_CONTEXTS_HEAD`, `REQUIRED_CONTEXT_COUNT_HEAD`, `ORIGIN_MAIN_AT_HEAD`, `MAIN_MOVED`,
`HEAD_LOCAL_DECOY`, `ORIGIN_BRANCH_AT_HEAD`, `REFERENCE_CI_RUN_ID`, `REFERENCE_JOB_NAMES`,
`REFERENCE_JOB_COUNT` and `CI_YML_CHANGED_SINCE_MILESTONE_BASE`. Scratch-only (never committed):
`p7401_docstring_probe.py`.
</artifacts_this_phase_produces>

<verification>
- `PHASE_BASE_SHA` is recorded before any commit and its product tree equals `6cc44f22`.
- The clean C-locale base build has non-zero doctest unknown-node and QUA-14 census counts as
  positive controls, and the collapsed `api/index.typ` example region is transcribed.
- `FIX_LIST` accounts for every raw census line, attributed or not (D-07).
- The phase-head remote reads are recorded.
</verification>

<success_criteria>
SC1's and SC3's base halves exist: the measured base that the tip is compared against, the positive
controls that make a tip zero believable, and the census-derived fix list for 74-04.
</success_criteria>

<output>
Create `.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-01-SUMMARY.md` when done.
Its first section states `PHASE_BASE_SHA`, `BASE_WARNING_COUNT`, `BASE_DOCTEST_UNKNOWN_COUNT`,
`BASE_RAW_TOTAL`, `BASE_ATTRIBUTED_COUNT` and `FIX_LIST`, and names any probe-only rows. Do not edit
REQUIREMENTS.md, ROADMAP.md or STATE.md.
</output>
