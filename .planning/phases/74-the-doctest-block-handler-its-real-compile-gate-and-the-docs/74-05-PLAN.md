---
phase: 74-the-doctest-block-handler-its-real-compile-gate-and-the-docs
plan: 05
type: execute
wave: 4
depends_on: ["74-01", "74-03", "74-04"]
files_modified:
  - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-TIP-EVIDENCE.md
autonomous: true
requirements: [TRN-01, QUA-14]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Evidence last (ROADMAP Phase 74 binding ordering): the tip measurements run in wave 4, after the handler (74-03, wave 2) and the docstring repairs (74-04, wave 3) have landed, and never in the same wave as the work they audit.'
    - 'Same environment (SC3): the tip clean build and a PHASE_BASE_SHA rebuild run back to back in this plan''s single venv, with its interpreter recorded. The rebuild checks typsphinx/ out from PHASE_BASE_SHA and restores it afterwards; git status --porcelain -- typsphinx is empty after the restore. The rebuild''s counts must equal 74-01''s base keys.'
    - 'SC1 proof: the tip -b typst build of docs/source reports 0 doctest_block unknown-node warnings, with the same-venv base rebuild''s non-zero count as positive control. The tip api/index.typ carries the compute_content_include_path example inside codly(number-format: none) plus a ```python fence, its three >>> prompts on separate lines, and no collapsed `text(">>> ` run.'
    - 'SC1 tag: TIP_FENCE_TAG is python, non-empty and supplied by the handler (D-01), and the evidence cites D-01''s recorded reason.'
    - 'SC3 proof: the tip build reports 0 raw and 0 attributed lines of both docutils message classes (D-07), with the same-venv base rebuild''s non-zero counts as positive control.'
    - 'Edge (QUA-14 boundary): the zero thresholds are tested on both sides. The base rebuild is at least 1 on each counted quantity and the tip exactly 0. The tip `build succeeded` N is not above the rebuilt base N, and the evidence records the difference.'
    - 'D-05: the whole output trees are compared with LC_ALL=C diff -rq --exclude=.doctrees. Every differing hunk is tabled as a TRN-01 doctest-example region, a QUA-14 fixed-docstring rendering, or a D-05 finding. The only permitted findings are the enumerated D-04 self-documentation: the widened node parameter type of visit_literal_block / depart_literal_block, and the new visit_doctest_block / depart_doctest_block entries. Anything else halts the plan.'
    - 'D-05, literal-block path unchanged: no removed diff line is a fence line, every collapsed run of the base is removed, and exactly as many ```python fences are added as collapsed runs removed.'
    - 'SC3 meaning: the rendered visit_toctree entry of base and tip, HTML and .typ, has identical word-token multisets once bullet markers are ignored. quote_path has no rendered API entry in either output.'
    - 'Edge (TRN-01 ordering): in the tip api/index.typ, the compute_content_include_path fence precedes the compute_template_import_path fence, in source order.'
  artifacts:
    - path: .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-TIP-EVIDENCE.md
      provides: 'the tip build, the same-venv base rebuild, the D-05 classification, the HTML and .typ meaning comparison, and SC1_VERDICT / SC3_VERDICT / D05_VERDICT'
      contains: 'SC3_VERDICT = '
  key_links:
    - from: '74-BASE-EVIDENCE.md PHASE_BASE_SHA'
      to: 'the base rebuild'
      via: 'git checkout PHASE_BASE_SHA -- typsphinx/, build, git checkout HEAD -- typsphinx/'
      pattern: 'RESTORE_CLEAN = yes'
    - from: '74-TIP-EVIDENCE.md verdict keys'
      to: '74-07 wave gate'
      via: 'SC1_VERDICT, SC3_VERDICT, D05_VERDICT and D05_UNEXPLAINED_HUNKS read with sed'
      pattern: 'D05_VERDICT = '
  prohibitions:
    - statement: 'MUST NOT clear, narrow or hide the docutils Unexpected indentation / Block quote ends message class by anything other than editing the offending docstring reST: no suppress_warnings, -q, -W exemption, keep_warnings or report_level change, no conf.py or api/index.rst edit, no noqa-style marker, no census narrowed to the docstrings the requirement names, and no unattributed line left out of the census.'
      requirement_id: QUA-14
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Prove SC1 and SC3 on the phase tip against a base measured in the same environment, and discharge
D-05 by diffing the whole output trees:
- the tip clean `-b typst` build: zero doctest_block warnings, the example fenced line by line under
  a `python` tag, and zero docutils lines, raw and attributed (D-01, D-07)
- a `PHASE_BASE_SHA` rebuild in the same venv, as positive control and as the not-risen comparator
- the whole-tree base/tip diff, every hunk classified (D-05)
- the rendered meaning of the repaired docstring, compared in HTML and `.typ`

Purpose: the ROADMAP's "evidence last". This plan audits waves 1-3 and changes no product file.

Output: `74-TIP-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CONTEXT.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-QUA14-EVIDENCE.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-RED-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. The `docs` extra is needed for every build here; the
interpreter rationale is in 74-01.

**Build shape and locale.** Every build: `rm -rf` its output directory, then
`LANG=C LC_ALL=C uv run python -m sphinx -b <typst|html> docs/source <out>` from this worktree's root,
into your scratch directory, never `-q`. Every grep, sort and diff runs under `LANG=C LC_ALL=C`. A log
without the English `build succeeded` line is void. `diff` exits 1 when the inputs differ, so capture
its output to a file and its status separately. Never put `VAR="$(diff ...)"` inside an `&&` chain.

**The base-rebuild window (Task 2).**
- `git checkout "$PHASE_BASE_SHA" -- typsphinx/` puts the base translator and pathfmt into this worktree.
- Build, then run `git checkout HEAD -- typsphinx/` and prove `git status --porcelain -- typsphinx` is empty.
- Never `git stash`, never `git commit -a`, and never commit while the window is open. Stage the evidence file by name only.

**Timeouts.** Run every docs build, every full pytest run and every verify block that contains one with the Bash tool's `timeout` set to 600000, in the foreground. The default two-minute limit can cut a build or the full suite short and leave a truncated log.

**Evidence key lines.** Bare values only; explanation on the next line.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_74_05`; helper prefix `p7405_`.

**Commits.** Plain `git commit`. Never a GSD commit helper or `74-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: text(">>> -->
<!-- planner-discipline-allow: ^>>> compute_content_include_path( -->
<!-- planner-discipline-allow: quote_path> -->
<!-- planner-discipline-allow: id="[^"]*quote_path" -->

## Planning-time prediction (2026-09-20 scratch: base built from `6cc44f22`, tip built from a patched snapshot; context only)

| Fact | Predicted |
|------|-----------|
| Tip `-b typst` | `build succeeded.`, 0 doctest warnings, 0 raw, 0 attributed; base 5 warnings, 2 doctest, 10 raw, 3 attributed |
| `diff -rq --exclude=.doctrees` | exactly one differing file, `api/index.typ` |
| Hunks of `diff -u` on it | 6. H1 `compute_content_include_path` and H2 `compute_template_import_path` are TRN-01. H3, the `visit_literal_block` parameter type, is a finding. H4, the `depart_literal_block` parameter type plus the two new method entries, is a finding. H5 and H6, the `visit_toctree` rendering, are QUA-14 |
| visit_toctree word tokens without `-` | `.typ` 329 = 329, HTML 332 = 332, identical multisets |
| `quote_path` entries | none in `api/index.typ` or `api/index.html` |
| HTML build | base `build succeeded, 3 warnings.`, tip `build succeeded.` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: the tip clean -b typst build and SC1's and SC3's tip readings</name>
  <precondition>74-RED-EVIDENCE.md reads `GREEN_VERDICT = MET`, 74-QUA14-EVIDENCE.md reads `QUA14_FIX_VERDICT = MET`, and 74-BASE-EVIDENCE.md carries `PHASE_BASE_SHA`.</precondition>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-TIP-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md (keys, `### Build command`, the census filters)
    - .planning/ROADMAP.md § "Phase 74" SC1 and SC3
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CONTEXT.md D-01, D-05, D-07
    - typsphinx/writer.py lines 55-66 and 105-112 (the two `Examples:` blocks the tip must fence)
  </read_first>
  <action>
1. **`## Head check and provisioning`** in a new `74-TIP-EVIDENCE.md`, headed `# Phase 74 — Tip Evidence (SC1, SC3, D-05)`.
   - Record the head check, the provisioning line with its exit, and `PYVENV_HOME` and `PYVENV_VERSION_INFO`.
   - `BASE_74_05 = ` followed by `git rev-parse HEAD`, before any commit; this is the tip being measured. `SCRATCH_74_05 = ` followed by the `mktemp -d` path.
   - Quote `GREEN_VERDICT`, `QUA14_FIX_VERDICT` and `PHASE_BASE_SHA`. If a verdict is not `MET`, write `## HALT: wave 2-3 not green` and stop.
   - Record `git diff --name-only "$PHASE_BASE_SHA" HEAD -- docs`, which must be empty: the docs sources are those of the base.

2. **`## Tip Typst build`.**
   - Run `rm -rf "$S/tip-typst"`, then `LANG=C LC_ALL=C uv run python -m sphinx -b typst docs/source "$S/tip-typst"` with stdout and stderr both redirected into `$S/p7405_tip-typst.log`, then `echo "exit:$?"` (exit:0).
   - Keys, each from 74-01's command applied to this log:
     - `TIP_WARNING_COUNT`, from the English `build succeeded` line
     - `TIP_DOCTEST_UNKNOWN_COUNT`
     - `TIP_RAW_UNEXPECTED_INDENTATION`, `TIP_RAW_BLOCK_QUOTE_ENDS` and `TIP_RAW_TOTAL`
     - `TIP_ATTRIBUTED_COUNT`
   - Transcribe every remaining `WARNING:` / `ERROR:` line verbatim.

3. **`## Tip api/index.typ example region` (SC1).** On `$S/tip-typst/api/index.typ`:
   - `TIP_API_COLLAPSED_RUNS`: the collapsed-run count, which must be 0.
   - `TIP_API_EXAMPLE_PROMPTS`: `grep -c '^>>> compute_content_include_path('`, which must be 3.
   - `TIP_FENCE_TAG`: the language after the three backticks on the line directly above the first `>>> compute_content_include_path(` line, which must be `python`.
   - `TIP_CODLY_ABOVE_FENCE`: `yes` when the line above that is `codly(number-format: none)`.
   - `TIP_API_PYTHON_FENCES`: the count of lines that are exactly three backticks plus `python`.
   - Transcribe both examples' regions with `grep -n -B3 -A8`, verbatim.
   - `TIP_EXAMPLES_IN_ORDER`: `yes` when the first `>>> compute_content_include_path(` line number is below the first `>>> compute_template_import_path(` line number.
   - Cite D-01's recorded reason for `python` from 74-CONTEXT.md, and 74-RED-EVIDENCE.md's `FENCE_TAG`.

4. Commit the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-TIP-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; [ "$(k GREEN_VERDICT "$D/74-RED-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k QUA14_FIX_VERDICT "$D/74-QUA14-EVIDENCE.md")" = MET ] &amp;&amp; PB="$(k PHASE_BASE_SHA "$D/74-BASE-EVIDENCE.md")" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; [ -z "$(git diff --name-only "$PB" HEAD -- docs)" ] &amp;&amp; S="$(k SCRATCH_74_05)" &amp;&amp; L="$S/p7405_tip-typst.log" &amp;&amp; T="$S/tip-typst/api/index.typ" &amp;&amp; test -s "$L" &amp;&amp; test -s "$T" &amp;&amp; BL="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$L" || true)" &amp;&amp; [ -n "$BL" ] &amp;&amp; N="$(printf '%s\n' "$BL" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')" &amp;&amp; { [ -n "$N" ] || { [ "$BL" = 'build succeeded.' ] &amp;&amp; N=0; }; } &amp;&amp; [ "$N" = "$(k TIP_WARNING_COUNT)" ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$L" | wc -l)" = 0 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$D/74-BASE-EVIDENCE.md" | wc -l)" -ge 1 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -oE 'Unexpected indentation|Block quote ends without a blank line' "$L" | wc -l)" = 0 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -cE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' "$D/74-BASE-EVIDENCE.md")" -ge 1 ] &amp;&amp; for key in TIP_DOCTEST_UNKNOWN_COUNT TIP_RAW_UNEXPECTED_INDENTATION TIP_RAW_BLOCK_QUOTE_ENDS TIP_RAW_TOTAL TIP_ATTRIBUTED_COUNT TIP_API_COLLAPSED_RUNS; do [ "$(k "$key")" = 0 ] || { echo "nonzero: $key"; exit 1; }; done &amp;&amp; [ "$(grep -cE 'text[(].>>> ' "$T" || true)" = 0 ] &amp;&amp; [ "$(grep -n '^>>> compute_content_include_path(' "$T" | wc -l)" -eq 3 ] &amp;&amp; [ "$(k TIP_API_EXAMPLE_PROMPTS)" = 3 ] &amp;&amp; FL="$(grep -n -m1 '^>>> compute_content_include_path(' "$T" | cut -d: -f1)" &amp;&amp; [ "$(sed -n "$((FL - 1))p" "$T")" = '```python' ] &amp;&amp; [ "$(sed -n "$((FL - 2))p" "$T")" = 'codly(number-format: none)' ] &amp;&amp; [ "$(k TIP_FENCE_TAG)" = python ] &amp;&amp; [ "$(k TIP_CODLY_ABOVE_FENCE)" = yes ] &amp;&amp; TL="$(grep -n -m1 '^>>> compute_template_import_path(' "$T" | cut -d: -f1)" &amp;&amp; [ "$FL" -lt "$TL" ] &amp;&amp; [ "$(k TIP_EXAMPLES_IN_ORDER)" = yes ] &amp;&amp; for s in '## Head check and provisioning' '## Tip Typst build' '## Tip api/index.typ example region'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$(k BASE_74_05)" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `nonzero: KEY` or `missing section: NAME` printed. Each of these makes it fail:
- not in a worktree, or a wave 2-3 verdict not MET
- docs/ changed since the base
- the tip log or api/index.typ missing, or no English summary line
- N differing from `TIP_WARNING_COUNT`
- a doctest_block warning or a raw census phrase in the tip log
- the base evidence no longer matching either pattern as positive control
- any tip zero-key not 0, or a collapsed run left in api/index.typ
- the example not having 3 prompt lines under a codly-configured ```python fence, or the two examples out of source order
- a product file changed, a section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `$S/p7405_tip-typst.log` has an English `build succeeded` line, 0 doctest_block warnings and 0 raw census phrases, and the base evidence still matches both patterns.
    - `$S/tip-typst/api/index.typ` has 0 collapsed runs and 3 `>>> compute_content_include_path(` lines, directly under a ```` ```python ```` line that sits directly under `codly(number-format: none)`.
    - `TIP_FENCE_TAG = python`, cited to D-01, and `TIP_EXAMPLES_IN_ORDER = yes`.
  </acceptance_criteria>
  <done>SC1's tip half and SC3's tip zero are measured on a clean C-locale build of the phase tip.</done>
</task>

<task type="auto">
  <name>Task 2: The PHASE_BASE_SHA rebuild in the same venv, the not-risen comparison, and the D-05 whole-tree diff with every hunk classified</name>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-TIP-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-TIP-EVIDENCE.md (Task 1)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CONTEXT.md D-04 and D-05
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-RESEARCH.md § "Determinism of the whole-tree diff (D-05)"
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-01-PLAN.md `<flagged_assumptions>` item (e) (the D-05 findings reading)
  </read_first>
  <action>
1. **`## Base rebuild in the same venv`.**
   - Record `git status --porcelain -- typsphinx`, which must be empty. Run `git checkout "$PHASE_BASE_SHA" -- typsphinx/`, then record `git diff "$PHASE_BASE_SHA" -- typsphinx`, which must be empty: the working tree now equals the base.
   - Build twice from the base tree:
     - `rm -rf "$S/base-typst"`, then `LANG=C LC_ALL=C uv run python -m sphinx -b typst docs/source "$S/base-typst"`, logged to `$S/p7405_base-typst.log`.
     - `rm -rf "$S/base-html"`, then the same with `-b html` into `$S/base-html`, logged to `$S/p7405_base-html.log`.
   - Run `git checkout HEAD -- typsphinx/`, then record `git status --porcelain -- typsphinx`, which must be empty. Key `RESTORE_CLEAN = yes`. If the restore is not clean, write `## HALT: worktree not restored` and stop without committing.
   - Keys from the base log, by 74-01's commands:
     - `REBUILT_BASE_WARNING_COUNT`
     - `REBUILT_BASE_DOCTEST_UNKNOWN_COUNT`
     - `REBUILT_BASE_RAW_TOTAL`
     - `REBUILT_BASE_ATTRIBUTED_COUNT`
   - `REBUILT_BASE_MATCHES_74_01 = yes` only when all four equal 74-01's `BASE_WARNING_COUNT`, `BASE_DOCTEST_UNKNOWN_COUNT`, `BASE_RAW_TOTAL` and `BASE_ATTRIBUTED_COUNT`. Otherwise record both sets and write `## HALT: base not reproducible`.
   - Record the base HTML build's English summary line too.

2. **`## Not risen (SC3)`.**
   - `NOT_RISEN = yes` when `TIP_WARNING_COUNT` is at most `REBUILT_BASE_WARNING_COUNT`.
   - `WARNING_DELTA`: the tip value minus the base value, a signed integer.
   - List which warning lines disappeared, from a `LC_ALL=C diff` of the two logs' `WARNING:` / `ERROR:` lines with paths stripped to the part after the worktree root.

3. **`## D-05 whole-tree diff`.**
   - Run `LANG=C LC_ALL=C diff -rq --exclude=.doctrees "$S/base-typst" "$S/tip-typst"` into `$S/p7405_d05-rq.txt`, and record its exit and content verbatim.
   - Keys `D05_DIFFERING_FILE_COUNT`, the line count, and `D05_DIFFERING_FILES`, the `|`-joined paths relative to the output root.
   - For each differing file, run `LANG=C LC_ALL=C diff -u` into `$S/p7405_d05-<n>.diff` and transcribe every hunk verbatim under `### Hunks`. Key `D05_HUNK_COUNT`: the total count of lines beginning `@@` across the files.

4. **`### Classification`.** One table row per hunk, format `| H<n> | <file> | <@@ header> | <class> | <reason> |`, where class is exactly one of `TRN-01`, `QUA-14` or `FINDING-D04`:
   - `TRN-01`: the hunk replaces a collapsed run with `codly(number-format: none)`, a ```` ```python ```` fence and the example's lines. The removed lines must include one collapsed run and the added lines the matching example lines from `typsphinx/writer.py`.
   - `QUA-14`: the hunk lies wholly inside the rendered entry of a docstring in `FIX_LIST`. That entry runs from the metadata line ending `TypstTranslator.visit_toctree>]` to the next line starting `block(sticky: true`.
   - `FINDING-D04`: D-05 names any other hunk a finding, not noise. Two findings are predicted and enumerated. First, the `node` parameter line of `visit_literal_block` or `depart_literal_block` gaining `text(" | ") + raw("doctest_block")`, D-04's widened annotation. Second, the added API entries whose metadata labels end `TypstTranslator.visit_doctest_block>]` and `TypstTranslator.depart_doctest_block>]`, the D-04 methods and their docstrings. Record each with the reason.
   - Any hunk fitting none of these three is unexplained: write `## HALT: D-05 unexplained hunk` naming it, and stop.
   - Keys `D05_TRN01_HUNKS`, `D05_QUA14_HUNKS`, `D05_FINDING_HUNKS` and `D05_UNEXPLAINED_HUNKS`, the last 0.

5. **`### Literal-block path unchanged`.** Over the concatenated diffs:
   - `D05_REMOVED_FENCE_LINES`: the removed lines that are a fence line, i.e. start with three backticks after the `-`. It must be 0.
   - `D05_REMOVED_COLLAPSED_RUNS`: the removed lines containing the collapsed-run marker. It must equal 74-01's `BASE_API_COLLAPSED_RUNS`.
   - `D05_ADDED_PYTHON_FENCES`: the added lines exactly three backticks plus `python`. It must equal `D05_REMOVED_COLLAPSED_RUNS`.

6. Stage `74-TIP-EVIDENCE.md` by name and commit.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-TIP-EVIDENCE.md" &amp;&amp; BE="$D/74-BASE-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; [ -z "$(git status --porcelain -- typsphinx)" ] &amp;&amp; [ -z "$(git diff --name-only "$(k BASE_74_05)" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ "$(k RESTORE_CLEAN)" = yes ] &amp;&amp; S="$(k SCRATCH_74_05)" &amp;&amp; BL="$S/p7405_base-typst.log" &amp;&amp; test -s "$BL" &amp;&amp; test -s "$S/base-html/api/index.html" &amp;&amp; BS="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$BL" || true)" &amp;&amp; [ -n "$BS" ] &amp;&amp; BN="$(printf '%s\n' "$BS" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')" &amp;&amp; { [ -n "$BN" ] || { [ "$BS" = 'build succeeded.' ] &amp;&amp; BN=0; }; } &amp;&amp; [ "$BN" = "$(k REBUILT_BASE_WARNING_COUNT)" ] &amp;&amp; [ "$BN" = "$(k BASE_WARNING_COUNT "$BE")" ] &amp;&amp; BU="$(LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$BL" | wc -l)" &amp;&amp; [ "$BU" -ge 1 ] &amp;&amp; [ "$BU" = "$(k BASE_DOCTEST_UNKNOWN_COUNT "$BE")" ] &amp;&amp; BR="$(LANG=C LC_ALL=C grep -oE 'Unexpected indentation|Block quote ends without a blank line' "$BL" | wc -l)" &amp;&amp; [ "$BR" -ge 1 ] &amp;&amp; [ "$BR" = "$(k BASE_RAW_TOTAL "$BE")" ] &amp;&amp; [ "$(k REBUILT_BASE_MATCHES_74_01)" = yes ] &amp;&amp; [ "$(k TIP_WARNING_COUNT)" -le "$BN" ] &amp;&amp; [ "$(k NOT_RISEN)" = yes ] &amp;&amp; { LANG=C LC_ALL=C diff -rq --exclude=.doctrees "$S/base-typst" "$S/tip-typst" &gt; "$S/p7405_verify-rq.txt" || [ $? = 1 ]; } &amp;&amp; [ "$(grep -c . "$S/p7405_verify-rq.txt")" = 1 ] &amp;&amp; [ "$(k D05_DIFFERING_FILE_COUNT)" = 1 ] &amp;&amp; grep -q 'api/index\.typ' "$S/p7405_verify-rq.txt" &amp;&amp; [ "$(k D05_DIFFERING_FILES)" = api/index.typ ] &amp;&amp; { LANG=C LC_ALL=C diff -u "$S/base-typst/api/index.typ" "$S/tip-typst/api/index.typ" &gt; "$S/p7405_verify-api.diff" || [ $? = 1 ]; } &amp;&amp; [ "$(grep -c '^@@' "$S/p7405_verify-api.diff")" = "$(k D05_HUNK_COUNT)" ] &amp;&amp; [ "$(grep -cE '^\| H[0-9]+ \|' "$F")" = "$(k D05_HUNK_COUNT)" ] &amp;&amp; [ "$(grep -cE '^\| H[0-9]+ \|.*\| TRN-01 \|' "$F")" = "$(k D05_TRN01_HUNKS)" ] &amp;&amp; [ "$(grep -cE '^\| H[0-9]+ \|.*\| QUA-14 \|' "$F")" = "$(k D05_QUA14_HUNKS)" ] &amp;&amp; [ "$(grep -cE '^\| H[0-9]+ \|.*\| FINDING-D04 \|' "$F")" = "$(k D05_FINDING_HUNKS)" ] &amp;&amp; [ "$(( $(k D05_TRN01_HUNKS) + $(k D05_QUA14_HUNKS) + $(k D05_FINDING_HUNKS) ))" = "$(k D05_HUNK_COUNT)" ] &amp;&amp; [ "$(k D05_UNEXPLAINED_HUNKS)" = 0 ] &amp;&amp; [ "$(k D05_TRN01_HUNKS)" -ge 1 ] &amp;&amp; [ "$(k D05_QUA14_HUNKS)" -ge 1 ] &amp;&amp; [ "$(grep -cE '^-```' "$S/p7405_verify-api.diff" || true)" = 0 ] &amp;&amp; RC="$(grep -cE '^-.*text\(">>> ' "$S/p7405_verify-api.diff" || true)" &amp;&amp; [ "$RC" = "$(k BASE_API_COLLAPSED_RUNS "$BE")" ] &amp;&amp; [ "$(grep -cx '+```python' "$S/p7405_verify-api.diff" || true)" = "$RC" ] &amp;&amp; for s in '## Base rebuild in the same venv' '## Not risen (SC3)' '## D-05 whole-tree diff' '### Hunks' '### Classification' '### Literal-block path unchanged'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- typsphinx/ left dirty or a product file committed, or `RESTORE_CLEAN` not yes
- the base logs or base HTML missing, or no English base summary line
- the rebuilt base warning, doctest or raw counts differing from their keys or from 74-01's, or a base positive control of 0
- the tip N above the base N, or `NOT_RISEN` not yes
- the tree diff not reporting a difference, its file count or list differing from the keys, or any file other than api/index.typ differing
- the hunk count, table-row count or per-class counts mismatched, or the three classes not summing to the total
- an unexplained hunk, or no TRN-01 or QUA-14 hunk
- a removed fence line, or the collapsed-run removals and python-fence additions not matching the base count
- a section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - The base rebuild, taken in this plan's venv with typsphinx/ from `PHASE_BASE_SHA` and restored afterwards (`RESTORE_CLEAN = yes`), reproduces 74-01's four base counts.
    - `TIP_WARNING_COUNT` is at most `REBUILT_BASE_WARNING_COUNT`, so `NOT_RISEN = yes`.
    - `diff -rq --exclude=.doctrees` reports exactly `api/index.typ`, and every one of its `D05_HUNK_COUNT` hunks is tabled as `TRN-01`, `QUA-14` or `FINDING-D04`, with `D05_UNEXPLAINED_HUNKS = 0`.
    - No removed diff line is a fence line, and the number of python fences added equals the number of collapsed runs removed, which equals `BASE_API_COLLAPSED_RUNS`.
  </acceptance_criteria>
  <done>D-05 is discharged: every output difference between base and tip is a doctest region, a repaired-docstring rendering, or an enumerated D-04 finding, and the warning total has not risen against a same-venv base.</done>
</task>

<task type="auto">
  <name>Task 3: The rendered meaning of the repaired docstring (HTML and .typ), quote_path's absence, and the SC1 / SC3 / D-05 verdicts</name>
  <files>.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-TIP-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-TIP-EVIDENCE.md (Tasks 1-2)
    - .planning/ROADMAP.md § "Phase 74" SC3, the sentence on reading the affected docstrings' rendered output
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-QUA14-EVIDENCE.md § "Diff shape"
  </read_first>
  <action>
1. **`## Tip HTML build`.** Run `rm -rf "$S/tip-html"`, then `LANG=C LC_ALL=C uv run python -m sphinx -b html docs/source "$S/tip-html"`, logged to `$S/p7405_tip-html.log`. Record its English summary line.

2. **`## Rendered meaning (SC3)`.** Write the scratch helper `$S/p7405_region.py`; it is never committed. It takes a mode (`html` or `typ`) and a path, and writes the sorted word tokens of `visit_toctree`'s rendered entry, one per line, with every token equal to a lone `-` dropped. A lone `-` is how the base renders the bullet markers the repair turned into list items.
   - `html` mode parses `api/index.html` with `html.parser`. It finds the `dt` whose `id` is `typsphinx.translator.TypstTranslator.visit_toctree` and collects the data of the following `dd` until that `dd` closes, counting nested `dd`s.
   - `typ` mode takes the lines after the metadata line ending `TypstTranslator.visit_toctree>]`, up to the next line starting `block(sticky: true`, and collects the contents of every `text("...")` string literal.
   - Run it on the base and tip of both formats into `$S/p7405_{html,typ}-{base,tip}.tokens`.
   - `HTML_VISIT_TOCTREE_WORDS_EQUAL = yes` and `TYP_VISIT_TOCTREE_WORDS_EQUAL = yes` when `cmp -s` finds each pair identical. Record each token count.
   - Transcribe both formats' base and tip regions verbatim, so a reader sees that only the list structure changed.
   - If either pair differs, record the token difference and write `## HALT: rendered meaning differs`.

3. **`## quote_path rendering`.**
   - `grep -c 'quote_path>'` over both `.typ` outputs' `api/index.typ`, and `grep -c 'id="[^"]*quote_path"'` over both `api/index.html` files.
   - `QUOTE_PATH_RENDERED = no` when all four are 0. That shows D-07's repair of this docstring is proven by the raw census alone.

4. **`## Verdicts`.**
   - `SC1_VERDICT = MET` when:
     - `TIP_DOCTEST_UNKNOWN_COUNT = 0`, with `REBUILT_BASE_DOCTEST_UNKNOWN_COUNT` at least 1
     - `TIP_API_COLLAPSED_RUNS = 0`
     - `TIP_API_EXAMPLE_PROMPTS = 3`
     - `TIP_FENCE_TAG = python` and `TIP_CODLY_ABOVE_FENCE = yes`
   - `SC3_VERDICT = MET` when:
     - `TIP_RAW_TOTAL = 0` and `TIP_ATTRIBUTED_COUNT = 0`, with the rebuilt base at least 1 on both
     - `NOT_RISEN = yes`
     - both words-equal keys are `yes`
   - `D05_VERDICT = MET` when `D05_UNEXPLAINED_HUNKS = 0` and `D05_REMOVED_FENCE_LINES = 0`.
   - Name every `FINDING-D04` hunk under `### D-05 findings for owner acknowledgement`, each with its reason. D-05 says a finding is not noise, so it is surfaced rather than folded into a class.

5. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-TIP-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; S="$(k SCRATCH_74_05)" &amp;&amp; test -s "$S/tip-html/api/index.html" &amp;&amp; [ -n "$(LANG=C LC_ALL=C grep -E '^build succeeded' "$S/p7405_tip-html.log" || true)" ] &amp;&amp; for f in html-base html-tip typ-base typ-tip; do test -s "$S/p7405_$f.tokens" || { echo "missing tokens: $f"; exit 1; }; done &amp;&amp; cmp -s "$S/p7405_html-base.tokens" "$S/p7405_html-tip.tokens" &amp;&amp; cmp -s "$S/p7405_typ-base.tokens" "$S/p7405_typ-tip.tokens" &amp;&amp; [ "$(k HTML_VISIT_TOCTREE_WORDS_EQUAL)" = yes ] &amp;&amp; [ "$(k TYP_VISIT_TOCTREE_WORDS_EQUAL)" = yes ] &amp;&amp; [ "$(grep -c 'quote_path>' "$S/tip-typst/api/index.typ" || true)" = 0 ] &amp;&amp; [ "$(grep -c 'quote_path>' "$S/base-typst/api/index.typ" || true)" = 0 ] &amp;&amp; [ "$(grep -c 'id="[^"]*quote_path"' "$S/tip-html/api/index.html" || true)" = 0 ] &amp;&amp; [ "$(grep -c 'id="[^"]*quote_path"' "$S/base-html/api/index.html" || true)" = 0 ] &amp;&amp; [ "$(k QUOTE_PATH_RENDERED)" = no ] &amp;&amp; [ "$(k SC1_VERDICT)" = MET ] &amp;&amp; [ "$(k SC3_VERDICT)" = MET ] &amp;&amp; [ "$(k D05_VERDICT)" = MET ] &amp;&amp; [ "$(k D05_UNEXPLAINED_HUNKS)" = 0 ] &amp;&amp; [ "$(k D05_REMOVED_FENCE_LINES)" = 0 ] &amp;&amp; [ "$(k REBUILT_BASE_DOCTEST_UNKNOWN_COUNT)" -ge 1 ] &amp;&amp; [ "$(k REBUILT_BASE_RAW_TOTAL)" -ge 1 ] &amp;&amp; [ "$(k REBUILT_BASE_ATTRIBUTED_COUNT)" -ge 1 ] &amp;&amp; for s in '## Tip HTML build' '## Rendered meaning (SC3)' '## quote_path rendering' '## Verdicts' '### D-05 findings for owner acknowledgement'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git status --porcelain -- typsphinx docs tests)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing tokens: NAME` or `missing section: NAME` printed. Each of these makes it fail:
- the tip HTML missing or its log without an English summary line
- a token file missing, or a base/tip token pair differing in HTML or .typ
- a words-equal key not yes
- a quote_path API entry present in any of the four outputs, or `QUOTE_PATH_RENDERED` not no
- an SC1, SC3 or D-05 verdict not MET, unexplained hunks or removed fence lines non-zero, or a rebuilt-base positive control of 0
- a section missing, the worktree dirty in typsphinx/docs/tests, a key line carrying a parenthesis, or a HALT heading</fails_when>
    <human-check>Read the verbatim base and tip `visit_toctree` regions transcribed under `## Rendered meaning (SC3)`, in HTML and in `.typ`. Confirm the repair changed only list structure (a run-on paragraph and block quote became a paragraph plus a bulleted list with two nested sub-lists) and not what the docstring says. Then read `### D-05 findings for owner acknowledgement` and confirm the D-04 self-documentation findings are acceptable under D-05.</human-check>
  </verify>
  <acceptance_criteria>
    - The HTML and `.typ` token files of `visit_toctree`'s rendered entry are byte-identical between base and tip, so both words-equal keys are `yes`.
    - No `quote_path` API entry exists in any of the four outputs (`QUOTE_PATH_RENDERED = no`).
    - `SC1_VERDICT = MET`, `SC3_VERDICT = MET` and `D05_VERDICT = MET`, with each `FINDING-D04` hunk listed for owner acknowledgement.
  </acceptance_criteria>
  <done>SC1 and SC3 are proven on the tip against a same-environment base, D-05's whole-tree comparison is classified hunk by hunk, and the repaired docstring's documented meaning is shown unchanged in both rendered formats.</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide accounting lives in `74-01-PLAN.md`. This plan authors the QUA-14 boundary edge and the
TRN-01 ordering edge's docs-level check, and carries prohibition P1. The `FINDING-D04` class is
planner assumption (e) in 74-01: D-05's "finding, not noise" is honoured by listing each such hunk
for owner acknowledgement, never by counting it as a TRN-01 or QUA-14 hunk. A hunk outside the
enumerated set still halts.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree working tree ↔ base checkout window | the base rebuild temporarily replaces typsphinx/ with base files |
| build output trees → evidence | an unclassified difference could hide a literal-block regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-15 | Tampering | base files left in, or committed from, the worktree after the rebuild | high | mitigate | `git checkout HEAD -- typsphinx/` right after the builds; the verify requires `git status --porcelain -- typsphinx` empty and no product-file change since `BASE_74_05`; the evidence is staged by name, never with `commit -a` |
| T-74-16 | Repudiation | an unexplained output difference waved through as noise | high | mitigate | per-hunk table counted against `diff -u`; the three classes must sum to the total; the finding set is enumerated in advance; any other hunk halts |
| T-74-17 | Repudiation | a tip zero believed without a same-environment positive control | medium | mitigate | the base rebuild in the same venv must reproduce 74-01's non-zero counts |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list (identical in every plan):
- `typsphinx/translator.py`: `TypstTranslator.visit_doctest_block(self, node: nodes.doctest_block) -> None` and `TypstTranslator.depart_doctest_block(self, node: nodes.doctest_block) -> None` (74-03); `visit_toctree` docstring blank lines (74-04).
- `typsphinx/pathfmt.py`: `quote_path` docstring blank line (74-04).
- `tests/test_doctest_block_render_gate.py` (74-02): class `TestDoctestBlockRenderGate`; constants `FIXTURE_DIR`, `MASTER_DOCNAMES`, `SHAPE_SENTINELS`, `UNKNOWN_DOCTEST_BLOCK`, `FENCE_OPEN`; helpers `_run_sphinx_build_typstpdf`, `_wrapper_pdf_path`, `_assert_pdf_magic`, `_source_doctest_block`, `_unknown_doctest_chunks`, `_read_typ`; module fixture `doctest_block_build`.
- `tests/fixtures/doctest_block_render_gate/{conf.py,index.rst,context_a_paragraph.rst,context_b_nonfirst_positions.rst}` (74-02).
- `tests/test_translator.py` (74-03): `test_doctest_block_defaults_to_python_fence`, `test_doctest_block_honours_existing_language`, `test_literal_block_without_language_keeps_bare_fence`, `test_doctest_block_shares_list_item_wrapper`.
- Evidence: `74-BASE-EVIDENCE.md`, `74-RED-EVIDENCE.md`, `74-QUA14-EVIDENCE.md`, `74-TIP-EVIDENCE.md`, `74-GATES-EVIDENCE.md`, `74-CI-EVIDENCE.md`, `COVERAGE.md`.

This plan creates `74-TIP-EVIDENCE.md` with keys `BASE_74_05`, `SCRATCH_74_05`, `PYVENV_HOME`,
`PYVENV_VERSION_INFO`, `TIP_WARNING_COUNT`, `TIP_DOCTEST_UNKNOWN_COUNT`,
`TIP_RAW_UNEXPECTED_INDENTATION`, `TIP_RAW_BLOCK_QUOTE_ENDS`, `TIP_RAW_TOTAL`,
`TIP_ATTRIBUTED_COUNT`, `TIP_API_COLLAPSED_RUNS`, `TIP_API_EXAMPLE_PROMPTS`, `TIP_FENCE_TAG`,
`TIP_CODLY_ABOVE_FENCE`, `TIP_API_PYTHON_FENCES`, `TIP_EXAMPLES_IN_ORDER`, `RESTORE_CLEAN`,
`REBUILT_BASE_WARNING_COUNT`, `REBUILT_BASE_DOCTEST_UNKNOWN_COUNT`, `REBUILT_BASE_RAW_TOTAL`,
`REBUILT_BASE_ATTRIBUTED_COUNT`, `REBUILT_BASE_MATCHES_74_01`, `NOT_RISEN`, `WARNING_DELTA`,
`D05_DIFFERING_FILE_COUNT`, `D05_DIFFERING_FILES`, `D05_HUNK_COUNT`, `D05_TRN01_HUNKS`,
`D05_QUA14_HUNKS`, `D05_FINDING_HUNKS`, `D05_UNEXPLAINED_HUNKS`, `D05_REMOVED_FENCE_LINES`,
`D05_REMOVED_COLLAPSED_RUNS`, `D05_ADDED_PYTHON_FENCES`, `HTML_VISIT_TOCTREE_WORDS_EQUAL`,
`TYP_VISIT_TOCTREE_WORDS_EQUAL`, `QUOTE_PATH_RENDERED`, `SC1_VERDICT`, `SC3_VERDICT` and
`D05_VERDICT`. Scratch-only: `p7405_region.py`.
</artifacts_this_phase_produces>

<verification>
- The tip build has zero doctest_block warnings and zero QUA-14 lines, raw and attributed, against a
  same-venv base rebuild that reproduces 74-01's non-zero counts.
- The example is a codly-styled python fence with separate lines, and the warning total has not
  risen.
- Every base/tip output difference is classified (D-05), and the repaired docstring reads the same
  in HTML and `.typ`.
</verification>

<success_criteria>
SC1 and SC3 are met on the phase tip, and D-05's literal-block-unchanged proof is recorded.
</success_criteria>

<output>
Create `.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-05-SUMMARY.md` when done.
Its first section states `SC1_VERDICT`, `SC3_VERDICT`, `D05_VERDICT`, `TIP_WARNING_COUNT`,
`REBUILT_BASE_WARNING_COUNT` and `D05_HUNK_COUNT`, and lists every `FINDING-D04` hunk for owner
acknowledgement. Do not edit REQUIREMENTS.md, ROADMAP.md or STATE.md.
</output>
