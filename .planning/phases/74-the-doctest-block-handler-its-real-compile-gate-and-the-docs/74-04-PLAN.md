---
phase: 74-the-doctest-block-handler-its-real-compile-gate-and-the-docs
plan: 04
type: execute
wave: 3
depends_on: ["74-01", "74-03"]
files_modified:
  - typsphinx/translator.py
  - typsphinx/pathfmt.py
  - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-QUA14-EVIDENCE.md
autonomous: true
requirements: [QUA-14]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Census before fix (ROADMAP Phase 74 binding ordering, constraint 6): the docstrings this plan edits are exactly FIX_LIST as recorded in 74-BASE-EVIDENCE.md from the base build''s own output, never the two the requirement names. A FIX_LIST that differs from the planned two docstrings halts the plan for a re-plan rather than being narrowed.'
    - 'Same file, sequenced not parallel: this plan edits typsphinx/translator.py in wave 3, after 74-03''s handler edit landed in wave 2.'
    - 'Minimal repair (CONTEXT Claude''s Discretion): each fix is in the docstring''s own reST, as blank lines before a list and before each nested sub-list. Every line this plan adds under typsphinx/ is empty, it removes none, and prose and documented meaning are untouched.'
    - 'D-07: typsphinx.pathfmt.quote_path''s docstring is fixed alongside TypstTranslator.visit_toctree''s, because the census attributes the unattributed probe-parse lines to it.'
    - 'After both fixes, a clean LANG=C LC_ALL=C -b typst build of docs/source in this worktree reports 0 raw and 0 attributed lines of both message classes. Its `build succeeded` N is not above BASE_WARNING_COUNT. The positive control is the attributed lines transcribed in 74-BASE-EVIDENCE.md, which the same filter matches.'
    - 'No suppression: docs/source/conf.py, docs/source/api/index.rst and every non-docstring line under typsphinx/ are unchanged by this plan.'
  artifacts:
    - path: typsphinx/translator.py
      provides: 'TypstTranslator.visit_toctree docstring with blank lines before its bullet list and its two nested sub-lists'
      contains: 'def visit_toctree'
    - path: typsphinx/pathfmt.py
      provides: 'quote_path docstring with a blank line before its Delimiter rule bullet list'
      contains: 'Delimiter rule (D-01), applied to the normalized string:'
    - path: .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-QUA14-EVIDENCE.md
      provides: 'the fix list, each repair with the census lines it clears, the post-fix build census, the diff shape, and QUA14_FIX_VERDICT'
      contains: 'QUA14_FIX_VERDICT = '
  key_links:
    - from: '74-BASE-EVIDENCE.md FIX_LIST'
      to: 'the docstrings edited here'
      via: 'read with sed; one repair row per FIX_LIST entry'
      pattern: 'FIX_LIST = '
  prohibitions:
    - statement: 'MUST NOT clear, narrow or hide the docutils Unexpected indentation / Block quote ends message class by anything other than editing the offending docstring reST: no suppress_warnings, -q, -W exemption, keep_warnings or report_level change, no conf.py or api/index.rst edit, no noqa-style marker, no census narrowed to the docstrings the requirement names, and no unattributed line left out of the census.'
      requirement_id: QUA-14
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT absorb NUM-01, MSG-06, WR-02, WR-03 or any other node still reaching unknown_visit, and MUST NOT change any typsphinx/ line other than the doctest_block handler and docstring text, nor any workflow file, flake.nix, pyproject.toml, uv.lock or typst_* config value.'
      requirement_id: TRN-01
      category: values
      status: resolved
      verification: judgment
---

<objective>
Clear QUA-14's docutils message class at its source: the reST of every docstring the base census
named (constraint 6). Per D-07, that includes `quote_path`'s docstring, whose probe-parse the census
attributes the unattributed lines to. The repair is minimal: blank lines, not rewording.

Purpose: SC3. The fix list comes from 74-01's census, never from the requirement's wording, and the
repair is proven by a clean rebuild here. The SC3 tip proof against a same-environment base
is 74-05's.

Output: the two docstring repairs and `74-QUA14-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CONTEXT.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-RESEARCH.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. `--extra docs` is required because this plan builds
`docs/source`; the interpreter rationale is in 74-01.

**Build shape and locale.** As in 74-01: `rm -rf` the output directory, then
`LANG=C LC_ALL=C uv run python -m sphinx -b typst docs/source <out>` from this worktree's root. The
output goes under your scratch directory, and every grep runs under `LANG=C LC_ALL=C`. A log without
the English `build succeeded` line is void.

**Timeouts.** Run every docs build, every full pytest run and every verify block that contains one with the Bash tool's `timeout` set to 600000, in the foreground. The default two-minute limit can cut a build or the full suite short and leave a truncated log.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put any explanation on
the next line.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_74_04`; helper prefix `p7404_`.

**Commits.** Plain `git commit`. Never a GSD commit helper, `git stash` or `74-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->

## Planning-time probe (2026-09-20; context only, the census and the rebuild are authoritative)

The napoleon-plus-docutils probe, 74-01's helper shape, was run over each docstring with candidate
blank lines inserted:

| Docstring | Messages at base (docstring-relative) | Blank lines inserted | Messages after |
|-----------|--------------------------------------|----------------------|----------------|
| `TypstTranslator.visit_toctree` | 5 ERROR, 6 WARNING, 21 ERROR | three: after `Requirement 13: Multi-document integration and toctree processing`; between `- Issue #5: Fix relative paths for nested toctrees` and its nested `- Calculate relative paths from current document`; between `- Issue #7: Simplify toctree output with single content block` and its nested `- Generate single #[...] block containing all guards` | 0 |
| `quote_path` | 19 ERROR, 21 WARNING | one: after `Delimiter rule (D-01), applied to the normalized string:` | 0 |

The first insertion alone left `visit_toctree` at 1 ERROR; all three were needed. A scratch
full-tree rebuild with the handler and these four blank lines produced `build succeeded.`, with 0 raw
and 0 attributed lines.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: read FIX_LIST, repair visit_toctree's docstring with blank lines, and prove the attributed census reaches 0 on a clean rebuild</name>
  <precondition>74-BASE-EVIDENCE.md carries `FIX_LIST`, `BASE_ATTRIBUTED_COUNT` at least 1 and `BASE_WARNING_COUNT`, and 74-RED-EVIDENCE.md reads `GREEN_VERDICT = MET`.</precondition>
  <files>typsphinx/translator.py, .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-QUA14-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md § "QUA-14 census" through § "Fix list" (`FIX_LIST`, the attributed and unattributed lines, the probe table, and the build command)
    - typsphinx/translator.py, the `visit_toctree` method and its whole docstring (about lines 5430-5480 after 74-03's insertion; locate it with `grep -n 'def visit_toctree'`)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CONTEXT.md D-07 and § "Claude's Discretion" (QUA-14 fix granularity)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-RESEARCH.md § "QUA-14 Census Nuance"
  </read_first>
  <action>
1. **`## Head check and fix list`** in a new `74-QUA14-EVIDENCE.md`, headed `# Phase 74 — QUA-14 Evidence (census-derived docstring repairs)`.
   - Record the head check: date, `pwd -P` under `.claude/worktrees/`, `test -f .git`, the shim.
   - Record the provisioning line with its exit, and `PYVENV_HOME` and `PYVENV_VERSION_INFO`.
   - `BASE_74_04 = ` followed by `git rev-parse HEAD`, before any commit; `SCRATCH_74_04 = ` followed by the `mktemp -d` path.
   - Quote `FIX_LIST` and `PHASE_BASE_SHA` from 74-BASE-EVIDENCE.md.
   - The plan is written for `FIX_LIST = typsphinx.pathfmt.quote_path|typsphinx.translator.TypstTranslator.visit_toctree`, the value measured at planning time on the same product tree. If the recorded value differs, write `## HALT: fix list differs from the planned two docstrings`, naming the extra or missing entries, and stop. The orchestrator re-plans so each extra docstring is fixed the same way. Never narrow the list.

2. **Probe before.** Copy 74-01's probe approach into `$S/p7404_docprobe.py`: napoleon `GoogleDocstring`, then `docutils.core.publish_doctree` at `report_level` 2. Make it accept a fully qualified name and print that docstring's messages of the two classes. Record its output for `typsphinx.translator.TypstTranslator.visit_toctree` under `## visit_toctree repair`. It must reproduce the census lines 74-01 attributed to this docstring.

3. **Repair (minimal, per the discretion rule).** Insert exactly one empty line in `visit_toctree`'s docstring at each of the three positions in § Planning-time probe. The lines must be truly empty, with no trailing whitespace. Change no other character: no rewording, no re-indentation, no re-wrapping.

4. **Probe after.** Re-run the probe: zero messages. If it is not zero, find the remaining position with the probe and add only a blank line or an indentation fix, recording each additional change with the probe line it clears. Every change must be recorded.

5. **Clean rebuild (fix verification).**
   - Run `rm -rf "$S/after-toctree"`, then `LANG=C LC_ALL=C uv run python -m sphinx -b typst docs/source "$S/after-toctree"` with stdout and stderr both redirected into `$S/p7404_after-toctree.log`.
   - Key `AFTER_TOCTREE_ATTRIBUTED_COUNT`, from 74-01's attributed filter, which must be 0.
   - Key `AFTER_TOCTREE_RAW_TOTAL`, measured. It still includes the `quote_path` probe-parse lines until Task 2.
   - Record the English `build succeeded` line.

6. Run `uv run ruff check typsphinx/translator.py`, `uv run black --check typsphinx/translator.py` and `uv run pytest tests/test_translator.py tests/test_doctest_block_render_gate.py -q -p no:cacheprovider`.

7. Commit the docstring as `fix(74-04): repair visit_toctree docstring reST (QUA-14)`, then the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-QUA14-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; [ "$(k FIX_LIST "$D/74-BASE-EVIDENCE.md")" = 'typsphinx.pathfmt.quote_path|typsphinx.translator.TypstTranslator.visit_toctree' ] &amp;&amp; [ "$(k GREEN_VERDICT "$D/74-RED-EVIDENCE.md")" = MET ] &amp;&amp; B="$(k BASE_74_04)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; S="$(k SCRATCH_74_04)" &amp;&amp; L="$S/p7404_after-toctree.log" &amp;&amp; test -s "$L" &amp;&amp; [ -n "$(LANG=C LC_ALL=C grep -E '^build succeeded' "$L" || true)" ] &amp;&amp; RE='\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' &amp;&amp; [ "$(LANG=C LC_ALL=C grep -cE "$RE" "$D/74-BASE-EVIDENCE.md")" -ge 1 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -cE "$RE" "$L" || true)" = 0 ] &amp;&amp; [ "$(k AFTER_TOCTREE_ATTRIBUTED_COUNT)" = 0 ] &amp;&amp; AD="$(git diff -U0 "$B" HEAD -- typsphinx/translator.py | grep -E '^\+' | grep -vE '^\+\+\+ ' || true)" &amp;&amp; [ -n "$AD" ] &amp;&amp; [ -z "$(printf '%s\n' "$AD" | grep -vx '+')" ] &amp;&amp; [ -z "$(git diff -U0 "$B" HEAD -- typsphinx/translator.py | grep -E '^-' | grep -vE '^--- ')" ] &amp;&amp; VT="$(grep -n '^    def visit_toctree' typsphinx/translator.py | cut -d: -f1)" &amp;&amp; for c in $(git diff -U0 "$B" HEAD -- typsphinx/translator.py | sed -n 's/^@@ -[0-9,]* +\([0-9]*\).*/\1/p'); do [ "$c" -gt "$VT" ] &amp;&amp; [ "$c" -lt "$((VT + 60))" ] || { echo "hunk outside visit_toctree docstring: $c"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- docs typsphinx/pathfmt.py tests)" ] &amp;&amp; uv run ruff check typsphinx/translator.py &amp;&amp; uv run black --check typsphinx/translator.py &amp;&amp; grep -qxF '## visit_toctree repair' "$F" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `hunk outside visit_toctree docstring: N` printed. Each of these makes it fail:
- not in a worktree
- `FIX_LIST` differing from the planned two docstrings, or GREEN not MET
- `BASE_74_04` malformed, or the rebuild log missing or localised
- the attributed filter matching nothing in 74-BASE-EVIDENCE.md (the positive control)
- any attributed line left in the rebuild log, or its key not 0
- translator.py gaining no line, gaining a non-empty line, or losing a line in this task
- a hunk outside visit_toctree's docstring region
- docs/, pathfmt.py or tests/ changed in this task
- ruff or black non-zero, the section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `FIX_LIST` in 74-BASE-EVIDENCE.md equals the planned two docstrings, quoted in 74-QUA14-EVIDENCE.md.
    - `git diff -U0 BASE_74_04..HEAD -- typsphinx/translator.py` adds only empty lines, removes none, and every hunk lies within `visit_toctree`'s docstring.
    - The clean rebuild log `$S/p7404_after-toctree.log` carries the English `build succeeded` line and 0 attributed lines (`AFTER_TOCTREE_ATTRIBUTED_COUNT = 0`), and the same filter matches at least one line of 74-BASE-EVIDENCE.md.
  </acceptance_criteria>
  <done>The attributed half of QUA-14's census is cleared by blank lines in `visit_toctree`'s own reST, proven by a clean C-locale rebuild.</done>
</task>

<task type="auto">
  <name>Task 2: Repair quote_path's docstring (D-07), prove raw and attributed both 0 with the total not risen, and record the diff shape and verdict</name>
  <files>typsphinx/pathfmt.py, .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-QUA14-EVIDENCE.md</files>
  <read_first>
    - typsphinx/pathfmt.py lines 46-80 (`quote_path`'s docstring and its `Delimiter rule (D-01)` bullet list)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md § "Unattributed lines" and § "Attribution table" (the `:19:` / `:21:` lines attributed to `quote_path`)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-QUA14-EVIDENCE.md (Task 1)
  </read_first>
  <action>
1. **`## quote_path repair`.** Record the probe output for `typsphinx.pathfmt.quote_path` before the edit; it must reproduce the lines 74-01 attributed to it. Insert exactly one empty line between `Delimiter rule (D-01), applied to the normalized string:` and the first bullet, `- no apostrophe present -> wrap in apostrophes (``'...'``)`. Re-run the probe: zero messages. Handle any residue as in Task 1 step 4, recorded. Per D-07, this docstring is repaired even though autodoc never renders it: the census reaches it through `sphinx-autodoc-typehints`' probe-parse of the name imported into `builder.py` and `writer.py`.

2. Run `uv run ruff check typsphinx/pathfmt.py` and `uv run black --check typsphinx/pathfmt.py`, then commit as `fix(74-04): repair quote_path docstring reST (QUA-14, D-07)`.

3. **`## Post-fix build`.**
   - Run `rm -rf "$S/after-fix"`, then `LANG=C LC_ALL=C uv run python -m sphinx -b typst docs/source "$S/after-fix"` with stdout and stderr both redirected into `$S/p7404_after-fix.log`.
   - Keys `FIX_RAW_UNEXPECTED_INDENTATION`, `FIX_RAW_BLOCK_QUOTE_ENDS` and `FIX_RAW_TOTAL`, from 74-01's raw commands. `FIX_ATTRIBUTED_COUNT`, from 74-01's attributed filter. All four must be 0.
   - `FIX_WARNING_COUNT`, from the English `build succeeded` line, must be at most `BASE_WARNING_COUNT`.
   - `FIX_DOCTEST_UNKNOWN_COUNT` is the doctest_block unknown-node count, which is 0 since 74-03.
   - Transcribe every remaining `WARNING:` / `ERROR:` line verbatim.
   - If a raw line remains, attribute it with the probe and write `## HALT: raw census not cleared`; never filter it away.

4. **`## Diff shape`.**
   - Record `git diff "$BASE_74_04" HEAD -- typsphinx/` verbatim.
   - `ADDED_BLANK_LINES`: the number of added lines, all empty.
   - `REMOVED_LINES = 0`.
   - One table row per inserted blank line: file, docstring, the census line it clears.

5. **`## Lint and tests`.** `uv run ruff check .`, `uv run black --check .`, `uv run mypy typsphinx/`, and `uv run pytest tests/test_translator.py tests/test_doctest_block_render_gate.py -q -p no:cacheprovider`, each with its exit, all 0.

6. **`## Verdict`.** `QUA14_FIX_VERDICT = MET` only when:
   - all four raw and attributed keys are 0
   - `FIX_WARNING_COUNT` is at most `BASE_WARNING_COUNT`
   - `REMOVED_LINES = 0`, and every added line is empty
   - lint and tests are green

   Otherwise write `NOT-MET` and a HALT heading. Commit the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; F="$D/74-QUA14-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_74_04)" &amp;&amp; S="$(k SCRATCH_74_04)" &amp;&amp; L="$S/p7404_after-fix.log" &amp;&amp; test -s "$L" &amp;&amp; BL="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$L" || true)" &amp;&amp; [ -n "$BL" ] &amp;&amp; N="$(printf '%s\n' "$BL" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')" &amp;&amp; { [ -n "$N" ] || { [ "$BL" = 'build succeeded.' ] &amp;&amp; N=0; }; } &amp;&amp; [ "$N" = "$(k FIX_WARNING_COUNT)" ] &amp;&amp; [ "$N" -le "$(k BASE_WARNING_COUNT "$D/74-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -o 'Unexpected indentation' "$L" | wc -l)" = 0 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -o 'Block quote ends without a blank line' "$L" | wc -l)" = 0 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -o 'Unexpected indentation' "$D/74-BASE-EVIDENCE.md" | wc -l)" -ge 1 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$L" | wc -l)" = 0 ] &amp;&amp; for key in FIX_RAW_UNEXPECTED_INDENTATION FIX_RAW_BLOCK_QUOTE_ENDS FIX_RAW_TOTAL FIX_ATTRIBUTED_COUNT FIX_DOCTEST_UNKNOWN_COUNT REMOVED_LINES; do [ "$(k "$key")" = 0 ] || { echo "nonzero: $key"; exit 1; }; done &amp;&amp; AD="$(git diff -U0 "$B" HEAD -- typsphinx | grep -E '^\+' | grep -vE '^\+\+\+ ' || true)" &amp;&amp; [ -z "$(printf '%s\n' "$AD" | grep -vx '+')" ] &amp;&amp; [ "$(printf '%s\n' "$AD" | grep -cx '+')" = "$(k ADDED_BLANK_LINES)" ] &amp;&amp; [ -z "$(git diff -U0 "$B" HEAD -- typsphinx | grep -E '^-' | grep -vE '^--- ')" ] &amp;&amp; [ "$(git diff --name-only "$B" HEAD -- typsphinx | LC_ALL=C sort | paste -sd'|')" = 'typsphinx/pathfmt.py|typsphinx/translator.py' ] &amp;&amp; QP="$(grep -n '^def quote_path' typsphinx/pathfmt.py | cut -d: -f1)" &amp;&amp; for c in $(git diff -U0 "$B" HEAD -- typsphinx/pathfmt.py | sed -n 's/^@@ -[0-9,]* +\([0-9]*\).*/\1/p'); do [ "$c" -gt "$QP" ] &amp;&amp; [ "$c" -lt "$((QP + 40))" ] || { echo "hunk outside quote_path docstring: $c"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- docs tests pyproject.toml uv.lock .github flake.nix)" ] &amp;&amp; uv run ruff check . &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; [ "$(k QUA14_FIX_VERDICT)" = MET ] &amp;&amp; for s in '## Head check and fix list' '## visit_toctree repair' '## quote_path repair' '## Post-fix build' '## Diff shape' '## Lint and tests' '## Verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `nonzero: KEY`, `hunk outside quote_path docstring: N` or `missing section: NAME` printed. Each of these makes it fail:
- the post-fix log missing or localised, or its N differing from `FIX_WARNING_COUNT` or above `BASE_WARNING_COUNT`
- any raw census phrase or doctest_block warning in the post-fix log
- the base evidence no longer carrying a raw phrase as positive control
- any zero-required key not 0
- a non-empty or removed line under typsphinx/ since `BASE_74_04`, or the blank-line count differing from `ADDED_BLANK_LINES`
- a changed typsphinx/ file other than pathfmt.py and translator.py, or a pathfmt hunk outside quote_path's docstring
- docs, tests, pyproject.toml, uv.lock, .github or flake.nix changed
- ruff, black or mypy non-zero, the verdict not MET, a section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff -U0 BASE_74_04..HEAD -- typsphinx/` lists only `typsphinx/translator.py` and `typsphinx/pathfmt.py`, and adds only empty lines inside the two docstrings, `ADDED_BLANK_LINES` of them, removing none.
    - The clean C-locale rebuild log shows 0 `Unexpected indentation`, 0 `Block quote ends without a blank line` and 0 doctest_block warnings, and its N is at most `BASE_WARNING_COUNT`.
    - `QUA14_FIX_VERDICT = MET`.
  </acceptance_criteria>
  <done>Every docstring the census named is repaired in its own reST, and a clean rebuild reports neither message class, raw or attributed (D-07).</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide accounting lives in `74-01-PLAN.md`. This plan carries prohibitions P1 and P4. It adds
no regression test for the message class, per CONTEXT's discretion note: a docs warnings gate is
Future, and Phase 75 SC4 re-proves the zero by measurement. The FIX_LIST halt in Task 1 is a
re-plan trigger, not a scope reduction.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docstring reST → autodoc → rendered API reference | an edit to fix a message could change what the reference documents |
| build console → evidence | a suppressed or localised build could fake the zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-12 | Repudiation | the message class suppressed instead of fixed | high | mitigate | the verify requires docs/ unchanged and only empty lines added under typsphinx/, inside the two docstrings; the English summary line and the base evidence serve as positive controls; prohibition P1 |
| T-74-13 | Tampering | documented meaning changed by the repair | medium | mitigate | blank-line-only diff enforced; 74-05 compares the rendered visit_toctree HTML and `.typ` token multisets |
| T-74-14 | Tampering | scope creep into other typsphinx/ code while the file is open | medium | mitigate | region-scoped hunk checks for both files; prohibition P4 |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list (identical in every plan):
- `typsphinx/translator.py`: `TypstTranslator.visit_doctest_block(self, node: nodes.doctest_block) -> None` and `TypstTranslator.depart_doctest_block(self, node: nodes.doctest_block) -> None` (74-03); `visit_toctree` docstring blank lines (this plan).
- `typsphinx/pathfmt.py`: `quote_path` docstring blank line (this plan).
- `tests/test_doctest_block_render_gate.py` (74-02): class `TestDoctestBlockRenderGate`; constants `FIXTURE_DIR`, `MASTER_DOCNAMES`, `SHAPE_SENTINELS`, `UNKNOWN_DOCTEST_BLOCK`, `FENCE_OPEN`; helpers `_run_sphinx_build_typstpdf`, `_wrapper_pdf_path`, `_assert_pdf_magic`, `_source_doctest_block`, `_unknown_doctest_chunks`, `_read_typ`; module fixture `doctest_block_build`.
- `tests/fixtures/doctest_block_render_gate/{conf.py,index.rst,context_a_paragraph.rst,context_b_nonfirst_positions.rst}` (74-02).
- `tests/test_translator.py` (74-03): `test_doctest_block_defaults_to_python_fence`, `test_doctest_block_honours_existing_language`, `test_literal_block_without_language_keeps_bare_fence`, `test_doctest_block_shares_list_item_wrapper`.
- Evidence: `74-BASE-EVIDENCE.md`, `74-RED-EVIDENCE.md`, `74-QUA14-EVIDENCE.md`, `74-TIP-EVIDENCE.md`, `74-GATES-EVIDENCE.md`, `74-CI-EVIDENCE.md`, `COVERAGE.md`.

This plan's evidence keys: `BASE_74_04`, `SCRATCH_74_04`, `PYVENV_HOME`, `PYVENV_VERSION_INFO`,
`AFTER_TOCTREE_ATTRIBUTED_COUNT`, `AFTER_TOCTREE_RAW_TOTAL`, `FIX_RAW_UNEXPECTED_INDENTATION`,
`FIX_RAW_BLOCK_QUOTE_ENDS`, `FIX_RAW_TOTAL`, `FIX_ATTRIBUTED_COUNT`, `FIX_WARNING_COUNT`,
`FIX_DOCTEST_UNKNOWN_COUNT`, `ADDED_BLANK_LINES`, `REMOVED_LINES` and `QUA14_FIX_VERDICT`.
Scratch-only: `p7404_docprobe.py`.
</artifacts_this_phase_produces>

<verification>
- The repaired docstrings are exactly `FIX_LIST`, derived from the base census (constraint 6, D-07).
- The only product-code change is empty lines inside two docstrings.
- A clean C-locale rebuild reports zero raw and zero attributed lines, with the total not risen.
</verification>

<success_criteria>
SC3's fix: the message class is cleared at its source everywhere the census found it, without a
filter, an exemption or a meaning change.
</success_criteria>

<output>
Create `.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-04-SUMMARY.md` when done.
Its first section states `FIX_LIST`, `ADDED_BLANK_LINES`, `FIX_RAW_TOTAL`, `FIX_ATTRIBUTED_COUNT`,
`FIX_WARNING_COUNT` and `QUA14_FIX_VERDICT`. Do not edit REQUIREMENTS.md, ROADMAP.md or STATE.md.
</output>
