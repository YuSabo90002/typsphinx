---
phase: 54.1-bundle-directory-safety-templates-path-collision-refusal-and
verified: 2026-08-16T05:30:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 54.1: Bundle Directory Safety Verification Report

**Phase Goal:** Close the two findings Phase 54's `/gsd-code-review` raised — WR-01 (a bundle copy
that can republish a project's Sphinx `templates_path` Jinja directory into public build output,
while the published docs actively recommended that layout) and CR-01 (the built-in `"typst"` key's
CONF-17 validation, and a reserved-key case collision, discovered only at `finish()` instead of
before any file is written).

**Verified:** 2026-08-16
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A used key's resolved bundle directory that IS/CONTAINS/IS CONTAINED BY a `templates_path` entry refuses the build with `ExtensionError`, named by the offending key, its resolved bundle directory, the colliding entry, and the `_typst/` remedy. `templates_path` is read for the first time in `typsphinx/`. | ✓ VERIFIED | `typsphinx/builder.py:164-223` (`_paths_related()`), `:942-1230` (`_validate_used_template_paths()`), wired in `write()` at `:1364`. Independently re-ran `sphinx-build -b typst tests/fixtures/templates_path_collision_gate` — non-zero exit, message contains `collides with the Sphinx templates_path entry` and `_typst`, zero `.typ` files written. `grep -rn templates_path typsphinx/` shows the only reads are inside `_validate_used_template_paths()`. |
| 2 | No page under `docs/source/`, no line of `README.md`, no file under `examples/` recommends putting a Typst template inside Sphinx's `templates_path` directory; replacement guidance matches `docs/source/conf.py`'s own `_typst/` pairing. | ✓ VERIFIED | `examples/advanced/_typst/custom.typ` and `examples/charged-ieee/approach2/source/_typst/_template.typ` exist; `examples/advanced/_templates/` and the charged-ieee `_templates/` no longer exist (`test -f`/`test ! -e` checks passed). Independent repo-wide greps for `typst_template.*_templates/` and bare `_templates` mentions (excluding lines naming `templates_path`) across `docs/source`, `README.md`, `examples/` both returned zero hits. `tests/test_docs_template_layout_gate.py` (3 tests, run-time `rglob()` discovery + fail-first control) passes. |
| 3 | A global `typst_template` naming a bare filename at the source root stops the build with **zero** `.typ` files — the A-01/CONF-17 guard runs pre-write, in the same slot as the other two pre-write validators. | ✓ VERIFIED | `typsphinx/builder.py:1189-1201` — hoisted `_violates_conf17()` check inside the per-used-key loop, `continue`s before the `templates_path` branch. Independently re-ran `sphinx-build -b typst tests/fixtures/conf17_prewrite_gate` — non-zero exit, message contains `has a parent directory that is srcdir itself, or an ancestor of srcdir`, zero `.typ` files. `finish()`-side guard confirmed still present at `builder.py:2035` (D-06 duplication intact), both sites sharing `_conf17_violation_message()`. |
| 4 | A declared registry key differing from the reserved `"typst"` key only by case is refused pre-write, not only at `finish()`. | ✓ VERIFIED | `typsphinx/builder.py:1118-1148` — case-collision branch routed through `TypstBuilder._collision_key()` (`grep -c 'def _collision_key'` → 1, no second case-folding primitive). Independently re-ran `sphinx-build -b typst tests/fixtures/reserved_key_case_prewrite_gate` — non-zero exit, message contains `differs from the built-in 'typst' registry key only by case`, zero `.typ` files. |
| 5 | Each fix pinned by a test recorded RED against the pre-fix tree before implementation; zero new runtime deps; no `@preview` bump; 3-way version-sync surface untouched; user-visible behaviour change carried into the v0.9.0 CHANGELOG. | ✓ VERIFIED | Five RED-EVIDENCE files exist (`54.1-01` through `54.1-04`), all citing pre-fix commit `9cc529b794563e57f2f070f93f0be9f141f8740e` (the commit immediately preceding this phase, matching the orchestrator's diff base). `git diff <base>..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ` → 0 lines changed; `@preview` count unchanged (verified present, unmodified, in all three lockstep files). No `pyproject.toml` diff. `CHANGELOG.md`'s `## [Unreleased]` carries a new `### Changed` entry naming WR-01, CR-01, `templates_path`, and `_typst/` as the migration target, phrased as an instruction (independently read, confirms diagnosis + remedy). |

**Score:** 5/5 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `typsphinx/builder.py::_paths_related()` | Symmetric is/contains/is-contained-by predicate, case-folded, no naive prefix test | ✓ VERIFIED | Present at `:164-223`; uses `posixpath.commonpath()` after `_collision_key()` folding; `grep -c startswith` inside the function body returns 0. |
| `typsphinx/builder.py::TypstBuilder._validate_used_template_paths()` | Pre-write validator carrying all three failure kinds into one `failures` list | ✓ VERIFIED | Present at `:942-1230`; wired into `write()` at `:1364`, between `_validate_registry_key_references()` and `prepare_writing()`. |
| `typsphinx/builder.py::_conf17_violation_message()` | Shared byte-identical message builder for both guard sites | ✓ VERIFIED | Present; both `_validate_used_template_paths()` (`:1193`) and `_copy_used_template_bundles()` (`:2037`) call it. |
| `tests/test_templates_path_collision_gate.py` | Real `sphinx-build` subprocess gate, WR-01 collision + edge/control cases | ✓ VERIFIED | 12 tests, all pass independently re-run. |
| `tests/test_conf17_prewrite_gate.py` | CONF-17 hoist gate | ✓ VERIFIED | 3 tests pass. |
| `tests/test_reserved_key_case_prewrite_gate.py` | Reserved-key case gate | ✓ VERIFIED | 2 tests pass. |
| `tests/test_docs_template_layout_gate.py` | Documentation repo-wide grep gate | ✓ VERIFIED | 3 tests pass (narrow assignment check, broad line-scoped check, fail-first control). |
| `tests/test_prewrite_failure_aggregation_gate.py` | Cross-kind aggregation, declaration-order-independence | ✓ VERIFIED | 4 tests pass. |
| `examples/advanced/_typst/custom.typ`, `examples/charged-ieee/approach2/source/_typst/_template.typ` | Renamed template bundles | ✓ VERIFIED | Both exist; both old `_templates/` paths gone; `git status --porcelain` clean, renames recorded via `git mv`. |
| `CHANGELOG.md` Unreleased entry | Migration announcement | ✓ VERIFIED | Present, names WR-01/CR-01/`templates_path`/`_typst`, does not mention Phase 57's WR-02 scope. |
| Five `*-RED-EVIDENCE.md` / `54.1-05-EVIDENCE.md` files | RED transcripts + phase-boundary green evidence | ✓ VERIFIED | All five present and non-trivial (6-17KB each); SHAs match the phase's pre-fix commit. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `TypstBuilder.write()` | `TypstBuilder._validate_used_template_paths()` | called after `_validate_registry_key_references()`, before `prepare_writing()` | ✓ WIRED | Confirmed by direct read of `builder.py:1300-1366`. |
| `_validate_used_template_paths()` | `template_registry._violates_conf17()` | imported and called unchanged, not re-derived | ✓ WIRED | `grep -c 'def _violates_conf17' typsphinx/*.py` → 1; hoisted check calls it directly. |
| `_validate_used_template_paths()` | `TypstBuilder._collision_key()` | reserved-key case comparison and `_paths_related()`'s folding both route through it | ✓ WIRED | Confirmed at `builder.py:1125,1129` and inside `_paths_related()`. |
| `_copy_used_template_bundles()` (finish()) | `_conf17_violation_message()` | shared message builder, not a re-derived inline f-string | ✓ WIRED | Confirmed at `builder.py:2037`. |
| `tests/test_docs_template_layout_gate.py` | `docs/source/`, `README.md`, `examples/` | run-time `rglob()` over three policed roots | ✓ WIRED | `grep -c rglob` present; independently confirmed zero offending hits on the current tree. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `templates_path` collision refuses build, zero `.typ` files | `sphinx-build -b typst tests/fixtures/templates_path_collision_gate` | non-zero exit, `collides with the Sphinx templates_path entry` + `_typst` present, 0 `.typ` files | ✓ PASS |
| CONF-17 hoist refuses build, zero `.typ` files | `sphinx-build -b typst tests/fixtures/conf17_prewrite_gate` | non-zero exit, marker present, 0 `.typ` files | ✓ PASS |
| Reserved-key case collision refuses build, zero `.typ` files | `sphinx-build -b typst tests/fixtures/reserved_key_case_prewrite_gate` | non-zero exit, marker present, 0 `.typ` files | ✓ PASS |
| All five new gate modules (24 tests) pass | `pytest tests/test_templates_path_collision_gate.py tests/test_conf17_prewrite_gate.py tests/test_reserved_key_case_prewrite_gate.py tests/test_docs_template_layout_gate.py tests/test_prewrite_failure_aggregation_gate.py -q` | 24 passed | ✓ PASS |
| Full suite green, unconditional | `pytest -q` | 1318 passed, 5 skipped, 0 failed | ✓ PASS |
| `test_state_guard_shapes_gate.py` (the STALE-note carve-out) | `pytest tests/test_state_guard_shapes_gate.py -q` | 18 passed, 0 failed | ✓ PASS (confirms STALE note is accurate — the "modulo 7" bar does not apply) |
| `black --check .` / `mypy typsphinx/` / `ruff check .` | (as above) | all exit 0 | ✓ PASS |
| Legitimate Sphinx config lines survive | `grep -c templates_path docs/source/conf.py examples/advanced/conf.py examples/basic/conf.py` | `1` for each | ✓ PASS |
| Version-sync surface untouched across whole phase | `git diff <base>..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ` | 0 lines | ✓ PASS |
| Milestone branch pushed (invariant #9) | `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` | non-empty (SHA `35ee8a0e...`) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|-------------|--------|----------|
| WR-01 | 54.1-01, 54.1-02, 54.1-04, 54.1-05 | Runtime collision refusal + documentation retirement + edge/control coverage + CHANGELOG announcement | ✓ SATISFIED (evidence supports closing; still marked `Pending` in REQUIREMENTS.md — see Gaps/Note below, not a code gap) | Truths 1, 2, 5 above; 21 of the 24 new gate tests trace to WR-01. |
| CR-01 | 54.1-01 (shared infra), 54.1-03, 54.1-05 | Pre-write A-01/CONF-17 hoist + reserved-key case collision + cross-kind aggregation | ✓ SATISFIED (evidence supports closing; still marked `Pending` in REQUIREMENTS.md — see note below) | Truths 3, 4, 5 above. |

**REQUIREMENTS.md traceability note (explicit, per verification instructions):** `.planning/REQUIREMENTS.md` lines 95-114 and the traceability table (lines 202-203) still list both `WR-01` and `CR-01` as `[ ]` / `Pending`. This verification's independent codebase evidence (all 5 ROADMAP success criteria VERIFIED, 24/24 new gate tests passing, both refusals independently re-run against real `sphinx-build` subprocesses, zero regressions in the 1318/5-skipped full suite) supports flipping both checkboxes to closed. No plan in this phase touched `REQUIREMENTS.md` — each SUMMARY.md explicitly and deliberately left it untouched, citing the worktree-mode convention that the orchestrator performs `requirements.mark-complete` after all of a requirement's contributing plans have merged. Since this phase's final plan (`54.1-05`) has merged and its own `requirements-completed` frontmatter names both `WR-01` and `CR-01`, the requirement-closure step is the one remaining administrative action — not a code or evidence gap. Recommend the orchestrator run `requirements.mark-complete WR-01 CR-01` as part of closing this phase.

No orphaned requirements found: `REQUIREMENTS.md`'s "Phase 54.1" mapping names exactly WR-01 and CR-01, and both appear in plan frontmatter (`54.1-01`, `54.1-02`, `54.1-04` declare `WR-01`; `54.1-03` declares `CR-01`; `54.1-05` declares both).

### Anti-Patterns Found

None blocking. Scanned `typsphinx/builder.py`'s new code (`_paths_related()`, `_validate_used_template_paths()`, `_conf17_violation_message()`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers and empty-return stubs — zero hits. No debt markers in any file this phase modified.

### Code-Review Findings Factored In (54.1-REVIEW.md: 0 critical, 2 warning, 2 info)

- **WR-01 (review, log-noise):** the new pre-write pass makes an existing "Custom template not found" warning fire a third time for one narrow misconfiguration shape (an explicit-but-missing global template on the synthesized built-in key). Reproduced by the reviewer against the post-fix tree, not a blocker to any ROADMAP success criterion, and not addressed in this phase (not mentioned in `54.1-05-EVIDENCE.md` or the CHANGELOG entry). **Not a gap against this phase's SCs** — it is log-noise, not a written-file or refusal-correctness defect — but it is real and undocumented. Recommend filing a todo for a future phase to add a `warn=False` parameter to the validator's `resolve_template()` call or to note the noise in the CHANGELOG.
- **WR-02 (review, confdir vs. srcdir):** `templates_path` is resolved against `self.srcdir`, not `self.confdir` — Sphinx's own documented semantics for that config value use `confdir`. This is **self-documented as a known limitation** in `_validate_used_template_paths()`'s own docstring (confirmed present: `awk` scoped read shows "confdir" and "declared" both stated inline), consistent with CONTEXT.md D-02's explicit `srcdir`-relative scoping decision. The reviewer's suggestion to also mention this carve-out in the CHANGELOG was not taken up — the CHANGELOG entry does not mention `-c`/confdir. **Not a gap against this phase's SCs** (D-02's `srcdir` scoping is a deliberate, recorded decision, not an oversight; SC#1/SC#2 do not require confdir coverage), but worth flagging as a documentation nicety not adopted.
- **IN-01 (review, symlinks not resolved in `_paths_related()`):** narrow, unlikely edge case, consistent with the rest of the module's existing non-resolution-of-symlinks convention. Not a gap.
- **IN-02 (review, `examples/advanced/README.md`/`index.rst` diagrams show a `_templates/` directory that doesn't exist on disk):** independently confirmed — `examples/advanced/` contains no `_templates/` directory, but the tree diagram shows one annotated "(empty here)". Cosmetic only; Sphinx tolerates a missing `templates_path` entry (the example still builds, confirmed via the `tox -e docs-html`-equivalent gate coverage in `test_examples_charged_ieee_gate.py` and the doc-layout gate). **Not a gap against this phase's SCs.**

None of the four review findings are BLOCKER-class, and none contradicts any of the five ROADMAP success criteria. They are recorded here per the verification instructions ("factor its WR-01/WR-02 findings into your assessment") as informational context, not as gaps.

### Human Verification Required

None. All must-haves are either directly observable via grep/read or independently re-executed as real `sphinx-build` subprocess commands during this verification pass — no behavior-dependent truth required a state-transition test beyond what was already run.

### Gaps Summary

No gaps found. All 5 ROADMAP success criteria are independently verified against the actual codebase (not SUMMARY.md claims): the runtime refusal mechanisms exist, are wired at the correct pre-write slot, were independently re-executed against real `sphinx-build` subprocesses and produced the expected refusal + zero-file behavior; the documentation retirement is complete and repo-wide-grep-verified; RED evidence exists for every fix, citing the correct pre-fix commit; the CHANGELOG carries the migration announcement; the full test suite is unconditionally green (1318 passed, 5 skipped, 0 failed — the "modulo 7" STALE carve-out correctly does not apply); lint/format/type gates all pass; the version-sync surface is untouched; and the milestone branch is pushed to origin.

The one administrative item — flipping `WR-01`/`CR-01` from `Pending` to closed in `.planning/REQUIREMENTS.md` — is not a code gap; it was deliberately left to the orchestrator by every contributing plan's SUMMARY.md, consistent with this project's worktree-mode convention, and the codebase evidence fully supports making that flip now.

---

_Verified: 2026-08-16_
_Verifier: Claude (gsd-verifier)_
