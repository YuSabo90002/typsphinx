---
phase: 54.1-bundle-directory-safety-templates-path-collision-refusal-and
plan: 04
type: execute
wave: 2
depends_on: ["54.1-01"]
files_modified:
  - tests/test_templates_path_collision_gate.py
  - tests/fixtures/templates_path_collision_multi_gate/conf.py
  - tests/fixtures/templates_path_collision_multi_gate/index.rst
  - tests/fixtures/templates_path_collision_multi_gate/_templates/alpha.typ
  - tests/fixtures/templates_path_collision_multi_gate/_templates/nested/beta.typ
  - tests/fixtures/templates_path_collision_multi_gate/_typst/gamma.typ
  - tests/fixtures/templates_path_adjacent_control_gate/conf.py
  - tests/fixtures/templates_path_adjacent_control_gate/index.rst
  - tests/fixtures/templates_path_adjacent_control_gate/_templatesx/custom.typ
  - tests/fixtures/templates_path_absent_control_gate/conf.py
  - tests/fixtures/templates_path_absent_control_gate/index.rst
  - tests/fixtures/templates_path_absent_control_gate/_templates/custom.typ
  - tests/fixtures/templates_path_no_typst_template_control_gate/conf.py
  - tests/fixtures/templates_path_no_typst_template_control_gate/index.rst
  - tests/fixtures/templates_path_default_documents_gate/conf.py
  - tests/fixtures/templates_path_default_documents_gate/index.rst
  - tests/fixtures/templates_path_default_documents_gate/_templates/custom.typ
  - .planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-04-RED-EVIDENCE.md
autonomous: true
requirements: [WR-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-01 adjacency edge — all THREE of D-02's relations refuse: the resolved bundle directory that IS a `templates_path` entry, one that IS CONTAINED BY an entry, and one that CONTAINS an entry; while a sibling directory whose name merely shares a prefix with an entry (a directory beside it, not under it) does NOT refuse — the false positive a naive string-prefix test would produce"
    - "WR-01 empty edge — a project with `templates_path` unset or empty never refuses on WR-01 grounds even when its Typst template sits in a directory named like Sphinx's default (the measured `examples/charged-ieee/approach2` shape, D-02 bullet 1); and a project that sets `templates_path` but configures no Typst template never refuses either (the measured `examples/basic` shape, D-02 bullet 2)"
    - "WR-01 empty edge, second half — a project that does NOT set `typst_documents` at all is still checked: the entry `_default_typst_documents()` synthesizes counts toward the checked key set (D-05), so an unset-`typst_documents` project with a colliding global Typst template refuses like any other"
    - "WR-01 ordering edge — multiple colliding keys are accumulated and raised ONCE (D-03), with every offending key named, iterated in `sorted()` key order so the aggregated message is byte-identical across runs"
    - "The aggregated message's failure count matches the number of offending keys, and each key's own sentence names its own resolved bundle directory and its own colliding `templates_path` entry — not a shared or first-offender summary"
  artifacts:
    - path: "tests/fixtures/templates_path_collision_multi_gate/conf.py"
      provides: "one build exercising all three D-02 relations across three registry keys, for the D-03 aggregation and ordering assertions"
    - path: "tests/fixtures/templates_path_adjacent_control_gate/conf.py"
      provides: "the prefix-sharing sibling directory that must NOT be reported as a collision"
    - path: "tests/fixtures/templates_path_absent_control_gate/conf.py"
      provides: "D-02 bullet 1's measured shape — a Typst template inside a directory named like Sphinx's default, with no `templates_path` set"
    - path: "tests/fixtures/templates_path_no_typst_template_control_gate/conf.py"
      provides: "D-02 bullet 2's measured shape — `templates_path` set, no Typst template configured"
    - path: "tests/fixtures/templates_path_default_documents_gate/conf.py"
      provides: "an unset `typst_documents`, proving the synthesized entry's key is checked (D-05)"
    - path: ".planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-04-RED-EVIDENCE.md"
      provides: "verbatim RED transcripts for the two refusal-side additions, measured against the phase's pre-fix production file"
  key_links:
    - from: "tests/test_templates_path_collision_gate.py"
      to: "typsphinx/builder.py::TypstBuilder._validate_used_template_paths"
      via: "real `sphinx-build` subprocess builds of five fixture trees, asserting refusal or non-refusal per D-02"
      pattern: "_run_sphinx_build"
  prohibitions:
    - requirement_id: WR-01
      category: integrity
      status: resolved
      verification: judgment
      statement: "MUST NOT record a RED transcript for a control fixture — a control case passes both before and after the fix by design, and manufacturing a failure for it to satisfy a checklist would falsify the evidence the phase rests on"
---

<objective>
Expand plan `54.1-01`'s proven refusal slice across the rest of WR-01's decision surface: all three
of D-02's path relations, the two measured non-colliding shapes, the adjacency false positive a
naive prefix test would produce, the unset-`typst_documents` case D-05 covers, and D-03's
accumulate-then-raise-once ordering guarantee.

Purpose: CONTEXT.md D-02 lists three measured consequences of choosing a path-relationship test over
a name match and says the plans should **assert** them rather than assume them. This plan is that
assertion. Without it, the phase ships a refusal whose false-positive and false-negative boundaries
are untested — and the most likely regression (someone "simplifying" the containment test into a
string-prefix check) would go unnoticed.

Output: five new fixture trees and the test methods that exercise them, appended to the gate module
plan `54.1-01` created; plus verbatim RED transcripts for the two refusal-side additions, measured
against this phase's pre-fix production file.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-CONTEXT.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-PATTERNS.md
@.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-01-SUMMARY.md
@CLAUDE.md
</context>

## Execution environment (MANDATORY — read before any command)

Worktree-isolated execution is this project's STANDING mode. Before running anything in your
worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run EVERY
subsequent command through `uv run …`. Without this, pytest imports the UNCHANGED main-tree package
and gates stay RED after a correct fix.

## Baseline: what "green" means in this phase

The full suite currently has **exactly 7 pre-existing failures** in
`tests/test_state_guard_shapes_gate.py`, recorded in
`.planning/phases/53-template-registry-foundation/deferred-items.md`. They predate Phase 53 and are
**NOT** a regression this phase causes. Do NOT fix them and do NOT report them as new RED.

## Cross-plan constraint that MUST hold (post-merge hazard)

Plan `54.1-03` runs in this same wave and hoists the A-01/CONF-17 check into the same pre-write
pass. Its worktree and yours are disjoint by file, but a fixture authored here that trips CONF-17
would pass in YOUR worktree and fail after the merge — the two plans each behaving correctly and the
tree still going red. To prevent that:

- **Every template file in every fixture created by this plan MUST live in a subdirectory of the
  source tree, never at the source-tree root.** A template whose parent directory IS `srcdir` is a
  CONF-17/A-01 violation and will be refused for a reason that has nothing to do with WR-01.
- **No fixture may declare a registry key that case-folds onto the reserved built-in key `"typst"`.**
  Use lowercase, obviously-distinct key names.

Assert both mechanically before finishing (see Task 3's acceptance criteria).

## Scope fence (do NOT touch)

- Do NOT modify `typsphinx/` in this plan except transiently, and only to produce the RED transcript
  described in Task 1 — restore it in the same task.
- **WR-02 / WR-03 / WR-04 / IN-01** are out of this phase entirely.
- Typing-import modernization is FORBIDDEN. The `@preview` count stays four with no fourth
  version-lockstep site — no fixture template declares a `@preview` import.

<tasks>

<task type="auto">
  <name>Task 1: Assert all three D-02 relations and D-03's single aggregated, sorted raise</name>

  <read_first>
    - `tests/test_templates_path_collision_gate.py` in full, as plan `54.1-01` left it — its
      `_run_sphinx_build()` and `_typ_files()` helpers, its `TEMPLATES_PATH_COLLISION_MARKER`
      constant, and the two existing tests you are extending rather than replacing.
    - `tests/fixtures/templates_path_collision_gate/conf.py` — the fixture shape and the
      "load-bearing properties" header-comment convention to imitate.
    - `typsphinx/builder.py` — `_paths_related()` and `_validate_used_template_paths()` as plan
      `54.1-01` left them: the three-way relation the fixture must exercise and the exact
      aggregate-message prefix the assertions read.
    - `.planning/phases/54.1-.../54.1-01-RED-EVIDENCE.md` — for the pre-fix commit SHA this task's
      RED transcript must be measured against, and for the evidence-file shape to follow.
    - `tests/test_registry_prewrite_validation_gate.py` — its cross-build message-identity helper
      (`_extract_registry_key_message`) as the model for asserting a message is stable rather than
      merely present.
  </read_first>

  <files>
    tests/fixtures/templates_path_collision_multi_gate/conf.py,
    tests/fixtures/templates_path_collision_multi_gate/index.rst,
    tests/fixtures/templates_path_collision_multi_gate/_templates/alpha.typ,
    tests/fixtures/templates_path_collision_multi_gate/_templates/nested/beta.typ,
    tests/fixtures/templates_path_collision_multi_gate/_typst/gamma.typ,
    tests/test_templates_path_collision_gate.py,
    .planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-04-RED-EVIDENCE.md
  </files>

  <behavior>
    - One build of the multi fixture refuses with a single `ExtensionError` whose text names all
      three offending registry keys.
    - The three keys appear in the message in `sorted()` order (`alpha`, then `beta`, then `gamma`).
    - The aggregate's failure count equals three.
    - Each key's own sentence names its own resolved bundle directory and its own colliding
      `templates_path` entry.
    - Zero `.typ` files exist under the build directory afterwards.
  </behavior>

  <action>
    Create `tests/fixtures/templates_path_collision_multi_gate/` as a real minimal Sphinx source
    tree whose ONE build exercises all three of D-02's relations at once:

    - `conf.py` sets `templates_path` to a two-element list: the directory named `_templates`, and
      the nested directory `_typst/inner`. It declares a `typst_document_templates` mapping with
      three keys, deliberately named so their sorted order is unambiguous:
      - `alpha` — template `_templates/alpha.typ`. Its bundle directory IS a `templates_path` entry.
      - `beta` — template `_templates/nested/beta.typ`. Its bundle directory IS CONTAINED BY a
        `templates_path` entry.
      - `gamma` — template `_typst/gamma.typ`. Its bundle directory CONTAINS a `templates_path`
        entry (`_typst/inner`).
      It declares three `typst_documents` entries, all naming the same docname is not possible —
      instead give the fixture three `.rst` docs, one per key, OR give one docname three entries with
      distinct target filenames; choose whichever satisfies the existing
      `_validate_output_path_collisions()` guard without tripping it, and say which you chose and why
      in the fixture's header comment.
      Head the file with the "load-bearing properties — do NOT touch any of these" block naming: the
      three relations each key exercises, that the key names' alphabetical order is what the ordering
      assertion reads, and that every template lives in a subdirectory (never at the source-tree
      root) so this fixture trips WR-01 and not CONF-17.
    - The three `.typ` templates — real, compilable, exposing a `project` function with the parameter
      surface `writer.py` passes, and declaring NO `@preview` imports.
    - The `.rst` document(s) — a title and one paragraph each.

    Append to `tests/test_templates_path_collision_gate.py` a test class covering the five behaviors
    above. For the ordering assertion, locate each key's own sentence in the combined output by index
    and assert the three indices are strictly increasing in sorted-key order — do not merely assert
    all three substrings are present, which would pass for any ordering. For the count assertion,
    read the number out of the aggregate prefix rather than counting substrings. Add a helper in the
    module (modelled on the message-identity helper in the registry-prewrite gate) that extracts the
    aggregate sentence from combined output so the assertions read one canonical string.

    **RED transcript.** These two assertions (the aggregation and the ordering) pin refusal-side
    behaviour, so binding constraint #6 applies. Measure it honestly against this phase's pre-fix
    production file rather than fabricating one: take the pre-fix commit SHA recorded in
    `54.1-01-RED-EVIDENCE.md`, write that commit's version of the production builder over the working
    copy (`git show <SHA>:typsphinx/builder.py > typsphinx/builder.py`), run the new test class,
    capture the output verbatim, then restore the working copy (`git checkout -- typsphinx/builder.py`)
    and confirm with `git status --porcelain typsphinx/` that nothing remains modified. Record the
    SHA, the swap-and-restore procedure, the exact command, the pass/fail counts and the verbatim
    failure tail into `54.1-04-RED-EVIDENCE.md`, following `54-01-RED-EVIDENCE.md`'s shape.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest tests/test_templates_path_collision_gate.py -x && git status --porcelain typsphinx/ | tee /dev/stderr | wc -l | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_templates_path_collision_gate.py` passes with every test green, and the new class contributes at least 4 test methods.
    - `uv run python -c "import subprocess,sys,tempfile,pathlib; d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','sphinx','-b','typst','tests/fixtures/templates_path_collision_multi_gate',d],capture_output=True,text=True); out=r.stdout+r.stderr; assert r.returncode!=0, out; ia,ib,ig=out.find(\"'alpha'\"),out.find(\"'beta'\"),out.find(\"'gamma'\"); assert -1 not in (ia,ib,ig), out; assert ia<ib<ig, (ia,ib,ig,out); assert not list(pathlib.Path(d).rglob('*.typ')); print('OK')"` prints `OK` — all three keys named, in sorted order, zero files written.
    - The aggregate message contains the failure count `3`; assert this in the test rather than by substring counting.
    - `uv run python -c "import pathlib,sys; root=pathlib.Path('tests/fixtures/templates_path_collision_multi_gate'); bad=[str(p) for p in root.glob('*.typ')]; print(bad); sys.exit(1 if bad else 0)"` exits 0 — no template sits at the fixture's source-tree root.
    - `grep -c '@preview' tests/fixtures/templates_path_collision_multi_gate/_templates/alpha.typ tests/fixtures/templates_path_collision_multi_gate/_templates/nested/beta.typ tests/fixtures/templates_path_collision_multi_gate/_typst/gamma.typ` reports `0` for all three.
    - `.planning/phases/54.1-.../54.1-04-RED-EVIDENCE.md` exists, is non-empty, names the pre-fix SHA carried over from `54.1-01-RED-EVIDENCE.md`, describes the swap-and-restore procedure, and carries a verbatim failure tail from the run against the pre-fix builder.
    - `git status --porcelain typsphinx/` produces no output at the end of the task — the production file was restored.
  </acceptance_criteria>

  <done>
    One fixture exercises all three of D-02's path relations; one build refuses with one aggregated
    message naming all three keys in sorted order with a correct failure count and zero written
    files; and the assertions are recorded RED against the pre-fix production file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assert the three shapes that must NOT refuse</name>

  <read_first>
    - `.planning/phases/54.1-.../54.1-CONTEXT.md` D-02's four bullets — the three measured shapes and
      the `srcdir`-relative resolution rule. These fixtures are the executable form of those bullets.
    - `examples/charged-ieee/approach2/conf.py` and `examples/basic/conf.py` — the two real projects
      whose shapes bullets 1 and 2 describe. Model the fixtures on what those files actually do.
    - `tests/test_registry_prewrite_validation_gate.py`'s `test_control_config_builds_unchanged_typ_set`
      — the established control-case shape: assert the build succeeds AND the produced `.typ` set is
      what it should be, not merely that the process exited 0.
    - `typsphinx/builder.py` — `_paths_related()` as plan `54.1-01` left it, so the adjacency fixture
      targets the exact false positive a prefix test would produce.
  </read_first>

  <files>
    tests/fixtures/templates_path_adjacent_control_gate/conf.py,
    tests/fixtures/templates_path_adjacent_control_gate/index.rst,
    tests/fixtures/templates_path_adjacent_control_gate/_templatesx/custom.typ,
    tests/fixtures/templates_path_absent_control_gate/conf.py,
    tests/fixtures/templates_path_absent_control_gate/index.rst,
    tests/fixtures/templates_path_absent_control_gate/_templates/custom.typ,
    tests/fixtures/templates_path_no_typst_template_control_gate/conf.py,
    tests/fixtures/templates_path_no_typst_template_control_gate/index.rst,
    tests/test_templates_path_collision_gate.py
  </files>

  <behavior>
    - Adjacent control: `templates_path` names one directory; the Typst template lives in a SIBLING
      directory whose name shares that directory's name as a prefix but is a different directory.
      The build succeeds and writes its expected `.typ` set.
    - Absent control: no `templates_path` is set at all; the Typst template lives inside a directory
      named exactly like Sphinx's default. The build succeeds (D-02 bullet 1 — the measured
      `examples/charged-ieee/approach2` shape).
    - No-template control: `templates_path` is set; no Typst template is configured, so the built-in
      key resolves to the packaged default. The build succeeds (D-02 bullet 2 — the measured
      `examples/basic` shape), and no containment test is applied to the packaged default's own
      directory.
  </behavior>

  <action>
    Create the three control fixtures as real minimal Sphinx source trees, each with a `conf.py`, an
    `index.rst`, and (for the first two) a real compilable template in a subdirectory exposing the
    `project` parameter surface `writer.py` passes, with NO `@preview` imports:

    - `templates_path_adjacent_control_gate/` — `templates_path` names `_templates`; the Typst
      template lives at `_templatesx/custom.typ`, a sibling directory whose name begins with the
      entry's name but which is NOT the entry and is NOT under it. Header comment: this fixture
      exists to catch a regression to a string-prefix containment test; if it ever starts refusing,
      the containment predicate has been "simplified" into a prefix comparison.
    - `templates_path_absent_control_gate/` — sets NO `templates_path` at all; the Typst template
      lives at `_templates/custom.typ`. Header comment: this is D-02 bullet 1's measured shape, the
      one `examples/charged-ieee/approach2` has; detection is a relationship test against live
      config values, never a name match on a directory name, and this fixture is what proves it.
    - `templates_path_no_typst_template_control_gate/` — sets `templates_path` and configures NO
      Typst template, so the built-in key falls through to the packaged default. Header comment: this
      is D-02 bullet 2's measured shape, the one `examples/basic` has; the packaged default's bundle
      lives inside the installed Python package and is never subjected to the containment test.

    Append a control test class to `tests/test_templates_path_collision_gate.py` covering the three
    behaviors. Follow the established control shape: assert exit code 0 AND assert the produced `.typ`
    file set matches what the fixture's `typst_documents` implies — an exit-0-only assertion would
    pass for a build that silently produced nothing. Also assert the collision marker substring is
    ABSENT from each control build's combined output.

    Do NOT record a RED transcript for these three fixtures. They pass both before and after the fix
    by design — that is the entire point of a control — and manufacturing a failure for them would
    falsify the phase's evidence.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates_path_collision_gate.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_templates_path_collision_gate.py` passes, and the control class contributes at least 3 test methods.
    - `uv run python -c "import subprocess,sys,tempfile,pathlib;\nfor f in ['templates_path_adjacent_control_gate','templates_path_absent_control_gate','templates_path_no_typst_template_control_gate']:\n    d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','sphinx','-b','typst','tests/fixtures/'+f,d],capture_output=True,text=True); out=r.stdout+r.stderr\n    assert r.returncode==0, (f,out)\n    assert 'collides with the Sphinx templates_path entry' not in out, (f,out)\n    assert list(pathlib.Path(d).rglob('*.typ')), (f,'no .typ written')\nprint('OK')"` prints `OK`.
    - `grep -c 'templates_path' tests/fixtures/templates_path_absent_control_gate/conf.py` returns `0` — the absent control really sets nothing.
    - `uv run python -c "import pathlib,sys; import glob; bad=[p for f in ['templates_path_adjacent_control_gate','templates_path_absent_control_gate','templates_path_no_typst_template_control_gate'] for p in pathlib.Path('tests/fixtures/'+f).glob('*.typ')]; print(bad); sys.exit(1 if bad else 0)"` exits 0 — no control fixture has a template at its source-tree root, so none of them trips the CONF-17 hoist plan `54.1-03` lands in the same wave.
    - `grep -rc '@preview' tests/fixtures/templates_path_adjacent_control_gate tests/fixtures/templates_path_absent_control_gate 2>/dev/null | grep -v ':0$' | wc -l` returns `0`.
    - `.planning/phases/54.1-.../54.1-04-RED-EVIDENCE.md` contains no section claiming a RED transcript for any of the three control fixtures.
  </acceptance_criteria>

  <done>
    All three of D-02's non-refusing shapes are asserted by real builds that succeed and write their
    expected `.typ` sets, and the adjacency false positive a prefix test would produce is pinned.
  </done>
</task>

<task type="auto">
  <name>Task 3: Assert the synthesized `typst_documents` entry is checked</name>

  <read_first>
    - `typsphinx/builder.py:218-240` — `_default_typst_documents()`, the callable default Sphinx
      falls back to when `typst_documents` is not set in `conf.py`, and the fifth element it
      synthesizes.
    - `typsphinx/builder.py` — `_validate_used_template_paths()`'s key-set derivation as plan
      `54.1-01` left it, so the fixture exercises the fallback rather than the explicit path.
    - `.planning/phases/54.1-.../54.1-CONTEXT.md` D-05 — why the checked key set is derived from
      `typst_documents` entries rather than the write-time accumulator.
    - `.planning/phases/54.1-.../54.1-04-RED-EVIDENCE.md` as Task 1 left it — you are appending a
      section, following the same swap-and-restore procedure.
  </read_first>

  <files>
    tests/fixtures/templates_path_default_documents_gate/conf.py,
    tests/fixtures/templates_path_default_documents_gate/index.rst,
    tests/fixtures/templates_path_default_documents_gate/_templates/custom.typ,
    tests/test_templates_path_collision_gate.py,
    .planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-04-RED-EVIDENCE.md
  </files>

  <behavior>
    - A project that does NOT set `typst_documents`, sets `templates_path`, and configures a global
      Typst template inside that directory, refuses the build with the collision message naming the
      built-in registry key.
    - Zero `.typ` files exist under the build directory afterwards.
  </behavior>

  <action>
    Create `tests/fixtures/templates_path_default_documents_gate/` as a real minimal Sphinx source
    tree: `conf.py` sets `templates_path` naming `_templates`, configures the GLOBAL Typst template
    at `_templates/custom.typ`, and deliberately does NOT set `typst_documents` at all — so Sphinx's
    config machinery falls back to the callable default and the single synthesized entry, whose fifth
    element is the built-in registry key, is what supplies the checked key. Head the file with the
    "load-bearing properties — do NOT touch any of these" block stating that the ABSENCE of
    `typst_documents` is the point of this fixture: adding it silently converts this into a duplicate
    of the plan-01 collision fixture. Add `index.rst` and the template (in its subdirectory, no
    `@preview` imports).

    Append a test to `tests/test_templates_path_collision_gate.py` covering both behaviors, asserting
    that the built-in registry key name appears in the message — that is the observable proof the
    synthesized entry reached the checked key set.

    Append a section to `54.1-04-RED-EVIDENCE.md` recording this assertion RED against the pre-fix
    production file, using the same swap-and-restore procedure Task 1 used (write the pre-fix
    commit's version of the production builder over the working copy, run, capture verbatim, restore,
    confirm clean). Then run the CROSS-PLAN fixture-safety checks over every fixture this plan
    created and record their results in the summary: no template at any fixture's source-tree root,
    and no fixture declaring a registry key that folds onto the reserved built-in key.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates_path_collision_gate.py -q && uv run pytest -q 2>&1 | tail -20 && git status --porcelain typsphinx/ | wc -l | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_templates_path_collision_gate.py` passes with every test green.
    - `grep -c 'typst_documents' tests/fixtures/templates_path_default_documents_gate/conf.py` returns `0` — the fixture really leaves it unset.
    - `uv run python -c "import subprocess,sys,tempfile,pathlib; d=tempfile.mkdtemp(); r=subprocess.run([sys.executable,'-m','sphinx','-b','typst','tests/fixtures/templates_path_default_documents_gate',d],capture_output=True,text=True); out=r.stdout+r.stderr; assert r.returncode!=0, out; assert 'collides with the Sphinx templates_path entry' in out, out; assert 'typst' in out; assert not list(pathlib.Path(d).rglob('*.typ')); print('OK')"` prints `OK`.
    - Fixture-safety sweep — `uv run python -c "import pathlib,sys; roots=[p for p in pathlib.Path('tests/fixtures').iterdir() if p.is_dir() and p.name.startswith('templates_path_')]; bad=[str(t) for r in roots for t in r.glob('*.typ')]; print(bad); sys.exit(1 if bad else 0)"` exits 0.
    - Fixture-safety sweep — `uv run python -c "import pathlib,re,sys; roots=[p for p in pathlib.Path('tests/fixtures').iterdir() if p.is_dir() and p.name.startswith('templates_path_')]; bad=[]; [bad.append(f'{c}:{k}') for r in roots for c in [r/'conf.py'] if c.is_file() for k in re.findall(r'[\x27\x22]([A-Za-z0-9_-]+)[\x27\x22]\s*:\s*\{', c.read_text()) if k.casefold()=='typst' and k!='typst']; print(bad); sys.exit(1 if bad else 0)"` exits 0 — no fixture declares a key that folds onto the reserved built-in key.
    - `uv run pytest -q` is green apart from exactly the 7 known `tests/test_state_guard_shapes_gate.py` failures.
    - `git status --porcelain typsphinx/` produces no output — the production file was restored after the RED measurement.
    - `.planning/phases/54.1-.../54.1-04-RED-EVIDENCE.md` carries a second section for this assertion with its own verbatim failure tail.
  </acceptance_criteria>

  <done>
    An unset `typst_documents` still refuses on a colliding global Typst template, proving the
    synthesized entry's key reaches the checked set; every fixture this plan created is verified safe
    against the CONF-17 hoist and the reserved-key case check landing in the same wave; and the
    assertion is recorded RED against the pre-fix production file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer-authored `conf.py` → `TypstBuilder.write()` | `templates_path` and the Typst template configuration cross into the containment predicate. This plan's fixtures are the adversarial inputs to that boundary. |
| the containment predicate → future maintainers | A regression that "simplifies" the predicate into a string comparison silently reopens the defect. The control fixtures are the tripwire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54.1-11 | Information Disclosure | a containment predicate regressed into a string-prefix or name-match test, silently letting a real collision through | high | mitigate | The multi-relation fixture pins all three of D-02's relations (is / contains / is-contained-by) in one build, so a predicate that only handles equality goes red immediately. |
| T-54.1-12 | Denial of Service | a containment predicate over-matching and refusing a legitimate configuration (a prefix-sharing sibling directory) | medium | mitigate | The adjacency control fixture builds a sibling directory whose name shares the entry's prefix and asserts the build SUCCEEDS; the two measured non-colliding shapes from D-02 are asserted the same way. |
| T-54.1-13 | Repudiation | a control fixture presented as RED evidence | medium | mitigate | An explicit prohibition and an acceptance criterion forbid a RED section for any control fixture; controls pass before and after the fix by design. |
| T-54.1-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed in this phase (binding constraint #11); `54.1-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. |

**ASVS level 1, block on `high`.** T-54.1-11 is the one `high` item and is dispositioned `mitigate`
by Task 1. Canon breadcrumb: generic path-traversal and information-disclosure hardening is canon —
covered by `/gsd-secure-phase` and this register; not minted as a bespoke prohibition.
</threat_model>

## Artifacts this phase produces

Created by THIS plan (excluded from drift verification — none exists on the pre-fix tree):

- `tests/fixtures/templates_path_collision_multi_gate/` — new fixture directory
- `tests/fixtures/templates_path_adjacent_control_gate/` — new fixture directory
- `tests/fixtures/templates_path_absent_control_gate/` — new fixture directory
- `tests/fixtures/templates_path_no_typst_template_control_gate/` — new fixture directory
- `tests/fixtures/templates_path_default_documents_gate/` — new fixture directory
- new test classes and a message-extraction helper inside `tests/test_templates_path_collision_gate.py`
- `.planning/phases/54.1-.../54.1-04-RED-EVIDENCE.md` — new evidence artifact

Created by sibling plans in this phase (also new, also excluded): `_paths_related()`,
`TypstBuilder._validate_used_template_paths()`, `_conf17_violation_message()`,
`tests/test_templates_path_collision_gate.py`, `tests/test_docs_template_layout_gate.py`,
`tests/test_conf17_prewrite_gate.py`, `tests/test_reserved_key_case_prewrite_gate.py`,
`tests/fixtures/templates_path_collision_gate/`, `tests/fixtures/conf17_prewrite_gate/`,
`tests/fixtures/reserved_key_case_prewrite_gate/`, `tests/fixtures/mixed_prewrite_failures_gate/`,
`examples/advanced/_typst/`, `examples/charged-ieee/approach2/source/_typst/`,
`54.1-01-RED-EVIDENCE.md`, `54.1-02-RED-EVIDENCE.md`, `54.1-03-RED-EVIDENCE.md`,
`54.1-05-EVIDENCE.md`.

<verification>
- `uv run pytest tests/test_templates_path_collision_gate.py` — all green, at least 10 test methods.
- `uv run pytest -q` — green modulo exactly the 7 pre-existing `tests/test_state_guard_shapes_gate.py` failures.
- `git status --porcelain typsphinx/` — empty at plan end.
- `54.1-04-RED-EVIDENCE.md` — two RED sections (aggregation/ordering, synthesized-entry), each
  measured against the pre-fix production file via the recorded swap-and-restore procedure; zero
  sections for control fixtures.
</verification>

<success_criteria>
- All three of D-02's path relations refuse, and all three of D-02's measured non-colliding shapes
  build — asserted by real `sphinx-build` subprocesses, not assumed.
- The adjacency false positive a naive prefix test would produce is pinned by a control fixture.
- Multiple colliding keys are named in ONE message, in `sorted()` order, with a correct failure
  count (D-03).
- An unset `typst_documents` is still checked through the synthesized entry (D-05).
- Every fixture created here is verified not to trip the CONF-17 hoist or the reserved-key case
  check landing in the same wave.
</success_criteria>

<output>
Create `.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-04-SUMMARY.md` when done.
</output>
