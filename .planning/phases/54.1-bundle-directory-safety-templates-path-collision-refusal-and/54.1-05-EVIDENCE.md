# Phase 54.1 Plan 05 — Evidence

Recorded against commit `9cc529b794563e57f2f070f93f0be9f141f8740e` -- the SAME pre-fix SHA plans
`54.1-01` and `54.1-04`'s own RED-evidence files recorded against (the tip of the worktree branch
before any `typsphinx/` file was touched; nothing in `typsphinx/` read `self.config.templates_path`
on that commit, and neither the `_validate_used_template_paths()` pre-write validator nor its
hoisted A-01/CONF-17 check nor its reserved-key case-collision check existed).

**Swap-and-restore procedure** (the same procedure `54.1-04-RED-EVIDENCE.md` used):

1. Confirm `typsphinx/` is clean: `git status --porcelain typsphinx/` produces no output.
2. Swap in the pre-fix production file: `git show 9cc529b794563e57f2f070f93f0be9f141f8740e:typsphinx/builder.py > typsphinx/builder.py`.
3. Confirm the swap actually changed the working tree: `git status --porcelain typsphinx/` reports `M typsphinx/builder.py`.
4. Run the target test(s) and capture output verbatim.
5. Restore the working copy: `git checkout -- typsphinx/builder.py`.
6. Confirm restoration: `git status --porcelain typsphinx/` produces no output again.

## Section 1 (Task 1) — WR-01/CR-01 cross-kind aggregation gate

**Fixtures:** `tests/fixtures/mixed_prewrite_failures_a_gate/` and
`tests/fixtures/mixed_prewrite_failures_b_gate/` -- two real Sphinx source trees, each carrying the
SAME two pre-write failure kinds (a reserved-key case collision on the declared `"Typst"` key, and a
CONF-17/A-01 violation on the synthesized built-in `"typst"` key via `typst_template =
"aardvark.typ"`) in INVERTED declaration order, with no `templates_path` set.

**Command:**

```
uv run pytest tests/test_prewrite_failure_aggregation_gate.py -v
```

**Result:** `4 failed in 1.31s` -- every test in the new module fails against the pre-fix tree,
because on that commit `_validate_used_template_paths()` does not exist at all: neither the hoisted
A-01/CONF-17 check nor the reserved-key case-collision check runs pre-write. The reserved-key case
collision is not caught by ANY check on the pre-fix tree (neither CONF-16, which rejects only the
literal spelling `"typst"`, nor CONF-18, which compares declared keys only against each other) --
the build proceeds all the way to `finish()`, where the ONLY failure that eventually surfaces is the
CONF-17 violation, raised from `_copy_used_template_bundles()`'s existing `finish()`-side guard
(`builder.py:1633` on the pre-fix tree) -- AFTER both content and wrapper `.typ` files for both
docnames are already on disk. Fixture A's registry key is folded into the reserved built-in `"typst"`
key by that pre-fix guard's collision-destination logic, so the single first-offender error it raises
names `'typst'` rather than a cross-kind aggregate.

**Verbatim tail (failure summary + the load-bearing single-failure-kind evidence: pre-fix, only the
CONF-17 half of the fixture is ever reported, and only at `finish()`, after both docnames' `.typ`
files already exist):**

```
tests/test_prewrite_failure_aggregation_gate.py::test_fixture_a_refuses_with_two_failures_naming_both_markers FAILED [ 25%]
tests/test_prewrite_failure_aggregation_gate.py::test_fixture_b_refuses_with_two_failures_naming_both_markers FAILED [ 50%]
tests/test_prewrite_failure_aggregation_gate.py::test_message_identical_across_inverted_declaration_order FAILED [ 75%]
tests/test_prewrite_failure_aggregation_gate.py::test_no_typ_file_written_after_either_refusal FAILED [100%]

=================================== FAILURES ===================================
_________ test_fixture_a_refuses_with_two_failures_naming_both_markers _________

    def test_fixture_a_refuses_with_two_failures_naming_both_markers(tmp_path):
        build_dir = tmp_path / "build"
        result = _run_sphinx_build(FIXTURE_DIR_A, build_dir, "typst")
        combined_output = result.stdout + result.stderr

        assert result.returncode != 0, (...)

        match = AGGREGATE_PREFIX_RE.search(combined_output)
>       assert match, (
            f"Expected an aggregate 'typst: N pre-write template path "
            f"failure(s):' sentence in fixture A's build output:\n"
            f"{combined_output}"
        )
E       AssertionError: Expected an aggregate 'typst: N pre-write template path failure(s):' sentence in fixture A's build output:
E         ...
E         typst: wrote 2 wrapper file(s) -- compile these: index_out.typ, other_out.typ
E         .../tests/fixtures/mixed_prewrite_failures_a_gate/other.rst: WARNING: ドキュメントはどの toctree にも含まれていません [toc.not_included]
E
E         Extension error!
E         ...
E         Traceback
E         =========
E
E               File ".../typsphinx/builder.py", line 1633, in _copy_used_template_bundles
E                 raise ExtensionError(
E                 ...<5 lines>...
E                 )
E             sphinx.errors.ExtensionError: typst_document_templates: registry key 'typst''s resolved template '.../tests/fixtures/mixed_prewrite_failures_a_gate/aardvark.typ' has a parent directory that is srcdir itself, or an ancestor of srcdir ('.../tests/fixtures/mixed_prewrite_failures_a_gate') -- put the template in its own subdirectory (CONF-17, A-01)

tests/test_prewrite_failure_aggregation_gate.py:164: AssertionError
_________ test_fixture_b_refuses_with_two_failures_naming_both_markers _________
    [same shape as fixture A above -- AssertionError: Expected an aggregate
    'typst: N pre-write template path failure(s):' sentence; the raised
    ExtensionError is the SAME single first-offender finish()-side CONF-17
    guard, naming fixture B's own absolute paths]

___________ test_message_identical_across_inverted_declaration_order ___________
    [AssertionError: No aggregate message found in fixture A's output --
    same underlying cause: no aggregate prefix exists pre-fix]

________________ test_no_typ_file_written_after_either_refusal _________________
    build_dir_a = tmp_path / "build_a"
    build_dir_b = tmp_path / "build_b"
    _run_sphinx_build(FIXTURE_DIR_A, build_dir_a, "typst")
    _run_sphinx_build(FIXTURE_DIR_B, build_dir_b, "typst")

    survivors_a = _typ_files(build_dir_a)
    survivors_b = _typ_files(build_dir_b)
>   assert not survivors_a, (...)
E   AssertionError: Expected NO .typ file written when fixture A's cross-kind aggregation refuses, found: [PosixPath('.../build_a/index.typ'), PosixPath('.../build_a/index_out.typ'), PosixPath('.../build_a/other.typ'), PosixPath('.../build_a/other_out.typ')]

tests/test_prewrite_failure_aggregation_gate.py:277: AssertionError
=========================== short test summary info ============================
FAILED tests/test_prewrite_failure_aggregation_gate.py::test_fixture_a_refuses_with_two_failures_naming_both_markers
FAILED tests/test_prewrite_failure_aggregation_gate.py::test_fixture_b_refuses_with_two_failures_naming_both_markers
FAILED tests/test_prewrite_failure_aggregation_gate.py::test_message_identical_across_inverted_declaration_order
FAILED tests/test_prewrite_failure_aggregation_gate.py::test_no_typ_file_written_after_either_refusal
============================== 4 failed in 1.31s ===============================
```

**Why it is RED on this tree:** `_validate_used_template_paths()` does not exist on the pre-fix
commit -- `write()` calls only `_validate_output_path_collisions()` and
`_validate_registry_key_references()` at its pre-write slot, neither of which reads
`self.config.templates_path`, checks the synthesized built-in `"typst"` key for CONF-17, or compares
a declared key against the reserved key by case-folded identity. The reserved-key case collision
(fixture's declared `"Typst"` key) is caught by NOTHING on this tree -- both docnames build past
`write()` and reach `finish()`, where `_copy_used_template_bundles()` folds `"Typst"`'s output
destination onto the built-in `"typst"` key's own destination and raises the SAME pre-existing
CONF-17 guard that fires anyway (because the built-in key's own template, `aardvark.typ`, violates
CONF-17). Both content and wrapper `.typ` files for both docnames are already on disk by the time
that single first-offender error surfaces -- exactly the CR-01 defect this phase closes, and the
absence of any aggregate `"typst: N pre-write template path failure(s):"` prefix at all is what every
one of these four tests fails on. `typsphinx/` is untouched by this task
(`git status --porcelain typsphinx/` produces no output after restoration).

---

## Task 3 — Phase-boundary green evidence

All commands below were run from this worktree's own provisioned venv (`.venv`, created via
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`), invoked as `uv run` /
`.venv/bin/python -m` equivalently. All measurements below are from a tree that already includes
every prior plan in this phase (`54.1-01` through `54.1-04`, merged into this worktree's base
commit `53e61c91f52538d851da86d64f27f825b11c48d7`) plus this plan's own two commits (the cross-kind
aggregation gate, the CHANGELOG entry).

### 1. Full suite

```
uv run pytest -q
```

**Result (verbatim totals line):**

```
================= 1318 passed, 5 skipped in 116.52s (0:01:56) ==================
```

**Failure count: 0 -- unconditional, not "modulo 7".** `54.1-VALIDATION.md` and this phase's own
`STATE.md` carve-out both name a previously-recorded class of 7 pre-existing failures in
`tests/test_state_guard_shapes_gate.py` (they read an archived `.planning/` path;
`.planning/phases/53-template-registry-foundation/deferred-items.md`). That carve-out is STALE on
this tree: `54.1-01-SUMMARY.md`, `54.1-03-SUMMARY.md` and `54.1-04-SUMMARY.md` each independently
measured **0** failures (not 7) on this same worktree lineage, and this plan's own measurement
above reproduces that -- **1318 passed, 5 skipped, 0 failed**, with no node ids to list because
none failed. This phase closes green on an unconditional zero-failure bar, not "zero modulo 7";
any future failure on this suite is a real regression, not a pre-existing exclusion.

### 2. Locale pre-check

```
LC_ALL=C uv run pytest tests/test_templates_path_collision_gate.py tests/test_conf17_prewrite_gate.py tests/test_reserved_key_case_prewrite_gate.py tests/test_docs_template_layout_gate.py tests/test_prewrite_failure_aggregation_gate.py -q
```

**Result (verbatim totals line):**

```
============================== 24 passed in 4.80s ==============================
```

### 3. Lint / format / types

```
uv run black --check .
```
**Result:** `All done! ✨ 🍰 ✨` / `334 files would be left unchanged.` -- **exit 0**.

```
uv run ruff check .
```
**Result (via the documented NixOS-sandbox workaround, `nix run nixpkgs#ruff -- check .`, since
the `.venv`-bundled `ruff` binary is a generic-linux ELF that cannot execute directly in this
sandbox -- previously documented in `54.1-01-SUMMARY.md`, `54.1-03-SUMMARY.md`,
`54.1-04-SUMMARY.md`):** `All checks passed!` -- **exit 0**.

```
uv run mypy typsphinx/
```
**Result:** `Success: no issues found in 8 source files` -- **exit 0**.

### 4. Version-sync invariant (#11)

```
uv run pytest tests/test_preview_version_sync.py -q
```
**Result:** `3 passed in 0.02s`.

```
git diff --stat 9cc529b794563e57f2f070f93f0be9f141f8740e..HEAD -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ
```
**Result:** empty output -- **zero changed lines** across the whole phase (the pre-fix SHA to this
plan's own `HEAD`, spanning all of `54.1-01` through `54.1-05`). The 3-way `@preview` version-sync
surface is untouched; the package count stays four with no fourth lockstep site.

### 5. Dogfooding builds

```
tox -e docs-html
```
**Result (final lines):**
```
build succeeded, 3 warnings.
  docs-html: OK (3.72=setup[0.11]+cmd[3.61] seconds)
  congratulations :) (3.77 seconds)
```

```
tox -e docs-pdf
```
**Result (final lines):**
```
typst: wrote 1 wrapper file(s) -- compile these: typsphinx.typ
Compiling 1 master document(s) to PDF...
Generated PDF: /home/yuta/Documents/typsphinx/.claude/worktrees/agent-aa0ba7ee14712dc44/docs/_build/pdf/typsphinx.pdf
build succeeded, 5 warnings.
  docs-pdf: OK (4.07=setup[0.12]+cmd[3.95] seconds)
  congratulations :) (4.11 seconds)
```

Both builders succeed on this repository's own dogfooding project, which sets `templates_path`
(`docs/source/conf.py:45`, `templates_path = ["_templates"]`) AND points its Typst template at a
SEPARATE directory (`docs/source/conf.py:96`, `typst_template = "_typst/custom_template.typ"`) --
the NON-colliding shape. This is the proof the new refusal does not fire on a correct
configuration (T-54.1-16's mitigation).

### 6. Milestone invariant #9

```
git ls-remote --heads origin gsd/v0.9.0-per-document-templates
```
**Result:**
```
35ee8a0ee8a4f8701c99a6596be8e37d975de307	refs/heads/gsd/v0.9.0-per-document-templates
```
Non-empty -- the milestone branch is present on `origin`. Milestone invariant #9 holds.

### 7. Requirement closure

| Requirement | Gate command that proves it closed |
|---|---|
| WR-01 (templates_path collision refusal, D-01/D-02/D-03/D-04) | `uv run pytest tests/test_templates_path_collision_gate.py -x` (12 passed, per `54.1-04-SUMMARY.md`) |
| WR-01 (documentation stops recommending the colliding layout, D-08/D-09/D-10) | `uv run pytest tests/test_docs_template_layout_gate.py -x` |
| WR-01 (breaking-change migration announced, transparency, T-54.1-14) | `uv run python -c "import pathlib; u=pathlib.Path('CHANGELOG.md').read_text().split('## [Unreleased]')[1].split('## [0.8.0]')[0]; assert 'templates_path' in u and 'WR-01' in u and '_typst' in u; print('OK')"` |
| CR-01 (hoisted A-01/CONF-17 check, D-04/D-05/D-06) | `uv run pytest tests/test_conf17_prewrite_gate.py -x` |
| CR-01 (reserved-key case collision, D-07) | `uv run pytest tests/test_reserved_key_case_prewrite_gate.py -x` |
| CR-01 (cross-kind aggregation, D-03's declaration-order-independence guarantee) | `uv run pytest tests/test_prewrite_failure_aggregation_gate.py -x` (this plan, Task 1 above -- 4 passed) |

WR-01 and CR-01 are both closed: every gate above is green on this tree (see §1, full suite, for
the aggregate confirmation that none of them is newly failing).

### Message quality

**Behavior manually verified:** `54.1-VALIDATION.md`'s one Manual-Only Verification -- that the
new `ExtensionError` text reads as an INSTRUCTION (tells the user where to move the template), not
only a diagnosis.

**Command run:**

```
.venv/bin/python -m sphinx -b typst tests/fixtures/templates_path_collision_gate <tmp-build-dir>
```

**Verbatim message, captured from the real build's stderr (`Extension error!` traceback section):**

```
sphinx.errors.ExtensionError: typst: 1 pre-write template path failure(s): 'paper': registry key 'paper''s resolved template bundle directory '/home/yuta/Documents/typsphinx/.claude/worktrees/agent-aa0ba7ee14712dc44/tests/fixtures/templates_path_collision_gate/_templates' collides with the Sphinx templates_path entry '_templates' (resolved to '/home/yuta/Documents/typsphinx/.claude/worktrees/agent-aa0ba7ee14712dc44/tests/fixtures/templates_path_collision_gate/_templates') -- the whole bundle directory is copied to the build output, so this would republish the project's Sphinx template directory; move the Typst template into a directory that is not on templates_path (this repository uses _typst/) and update typst_template / typst_document_templates to match
```

**Judgement: reads as an instruction, not only a diagnosis.** The message names the offending
registry key (`'paper'`), its resolved bundle directory (the absolute path ending in
`templates_path_collision_gate/_templates`), and the colliding `templates_path` entry (both the
raw config string `'_templates'` and its resolved absolute path) -- that is the diagnosis half.
It then tells the reader WHERE to move the template (`move the Typst template into a directory
that is not on templates_path (this repository uses _typst/)`) and WHAT config to update
(`update typst_template / typst_document_templates to match`) -- that is the remedy half a v0.8.x
user hitting this refusal needs. A reader who has never seen this codebase before can act on this
message without consulting any other document.

### Summary

The phase closes green: the full suite is unconditionally 0-failed (§1), the locale re-run of
every new gate module in this phase passes (§2), lint/format/types all exit 0 (§3), the
`@preview` version-sync surface is untouched across the whole phase (§4), both dogfooding builders
succeed on the non-colliding configuration (§5), the milestone branch is pushed (§6), and WR-01 /
CR-01 are both mapped to the gate commands that prove them closed (§7). The one manual-only
verification (message quality) confirms the new refusal reads as an instruction.
