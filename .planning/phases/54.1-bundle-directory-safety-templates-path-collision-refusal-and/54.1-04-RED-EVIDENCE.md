# Phase 54.1 Plan 04 — RED Evidence

Recorded against commit `9cc529b794563e57f2f070f93f0be9f141f8740e` -- the SAME pre-fix SHA plan
`54.1-01`'s own `54.1-01-RED-EVIDENCE.md` recorded against (the tip of the worktree branch before
any `typsphinx/` file was touched; nothing in `typsphinx/` read `self.config.templates_path` on
that commit).

**Swap-and-restore procedure** (used for every section below):

1. Confirm `typsphinx/` is clean: `git status --porcelain typsphinx/` produces no output.
2. Swap in the pre-fix production file: `git show 9cc529b794563e57f2f070f93f0be9f141f8740e:typsphinx/builder.py > typsphinx/builder.py`.
3. Confirm the swap actually changed the working tree: `git status --porcelain typsphinx/` reports `M typsphinx/builder.py`.
4. Run the target test(s) and capture output verbatim.
5. Restore the working copy: `git checkout -- typsphinx/builder.py`.
6. Confirm restoration: `git status --porcelain typsphinx/` produces no output again.

## Section 1 (Task 1) — WR-01 multi-relation aggregation and sorted-order gate

**Fixture:** `tests/fixtures/templates_path_collision_multi_gate/` — a real Sphinx source tree
whose ONE build exercises all three of D-02's path relations at once, across three registry
keys (`alpha`/`beta`/`gamma`), one docname (`index`) carrying three `typst_documents` entries
with three distinct target filenames.

**Command:**

```
uv run pytest tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate -v
```

**Result:** `5 failed in 1.09s` — every test in the new class fails against the pre-fix tree,
because nothing in `typsphinx/` reads `templates_path` on that commit: the build succeeds and
writes a full `.typ` tree (three wrapper files plus three copied template bundles) instead of
refusing.

**Verbatim tail (failure summary + the two load-bearing assertion failures binding constraint #6
requires -- the aggregation/count assertion and the ordering assertion):**

```
tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_multi_relation_build_refuses FAILED [ 20%]
tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_multi_relation_aggregate_names_all_three_keys_in_sorted_order FAILED [ 40%]
tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_multi_relation_aggregate_failure_count_is_three FAILED [ 60%]
tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_multi_relation_each_key_names_own_bundle_dir_and_own_entry FAILED [ 80%]
tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_no_typ_file_written_after_multi_relation_refusal FAILED [100%]

=================================== FAILURES ===================================
______ TestMultiRelationAggregationGate.test_multi_relation_build_refuses ______

self = <test_templates_path_collision_gate.TestMultiRelationAggregationGate object at 0x7a439b90efd0>
tmp_path = PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_multi_relation_build_refu0')

    def test_multi_relation_build_refuses(self, tmp_path):
        """All three of D-02's relations (is / is-contained-by /
        contains) refuse the build, not just the equality case plan
        54.1-01's own fixture already pins."""
        build_dir = tmp_path / "build"
        result = _run_sphinx_build(MULTI_FIXTURE_DIR, build_dir, "typst")
>       assert result.returncode != 0, (
            f"Expected the multi-relation fixture to refuse the build:\n"
            f"stdout: {result.stdout}\nstderr: {result.stderr}"
        )
E       AssertionError: Expected the multi-relation fixture to refuse the build:
E         stdout: Sphinx v9.1.0 を実行中
E         翻訳カタログをロードしています [en]... 完了
E         出力先ディレクトリを作成しています... 完了
E         ビルド中 [mo]: 更新された 0 件のpoファイル
E         出力中...
E         ビルド中 [typst]: 更新された 1 件のソースファイル
E         環境データを更新中[新しい設定] 1 件追加, 0 件更新, 0 件削除
E         ソースを読み込み中...[100%] index
E
E         更新されたファイルを探しています... 見つかりませんでした
E         環境データを保存中... 完了
E         整合性をチェック中... 完了
E         preparing documents... done
E         writing output... [index] done
E         typst: wrote 3 wrapper file(s) -- compile these: alpha_out.typ, beta_out.typ, gamma_out.typ
E         build succeeded.
E
E         stderr:
E       assert 0 != 0

______ TestMultiRelationAggregationGate.test_multi_relation_aggregate_names_all_three_keys_in_sorted_order ______

    def test_multi_relation_aggregate_names_all_three_keys_in_sorted_order(
        self, tmp_path
    ):
        build_dir = tmp_path / "build"
        result = _run_sphinx_build(MULTI_FIXTURE_DIR, build_dir, "typst")
        combined_output = result.stdout + result.stderr
        message = _extract_aggregate_message(combined_output)
>       assert message, (
            f"Expected an aggregate 'typst: N pre-write template path "
            f"failure(s):' sentence in the build output:\n{combined_output}"
        )
E       AssertionError: Expected an aggregate 'typst: N pre-write template path failure(s):' sentence in the build output:
E         Sphinx v9.1.0 を実行中
E         ...
E         typst: wrote 3 wrapper file(s) -- compile these: alpha_out.typ, beta_out.typ, gamma_out.typ
E         build succeeded.
E
E       assert ''

______ TestMultiRelationAggregationGate.test_multi_relation_aggregate_failure_count_is_three ______

    def test_multi_relation_aggregate_failure_count_is_three(self, tmp_path):
        build_dir = tmp_path / "build"
        result = _run_sphinx_build(MULTI_FIXTURE_DIR, build_dir, "typst")
        combined_output = result.stdout + result.stderr
        match = AGGREGATE_PREFIX_RE.search(combined_output)
>       assert match, (
            f"Expected an aggregate 'typst: N pre-write template path "
            f"failure(s):' sentence in the build output:\n{combined_output}"
        )
E       AssertionError: Expected an aggregate 'typst: N pre-write template path failure(s):' sentence in the build output:
E         Sphinx v9.1.0 を実行中
E         ...
E         typst: wrote 3 wrapper file(s) -- compile these: alpha_out.typ, beta_out.typ, gamma_out.typ
E         build succeeded.
E
E       assert None

______ TestMultiRelationAggregationGate.test_no_typ_file_written_after_multi_relation_refusal ______

    def test_no_typ_file_written_after_multi_relation_refusal(self, tmp_path):
        build_dir = tmp_path / "build"
        _run_sphinx_build(MULTI_FIXTURE_DIR, build_dir, "typst")
        survivors = _typ_files(build_dir)
>       assert not survivors, (
            f"Expected NO .typ file written when the multi-relation "
            f"collision refuses the build, found: {survivors}"
        )
E       AssertionError: Expected NO .typ file written when the multi-relation collision refuses the build, found: [PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/_template/alpha/alpha.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/_template/alpha/nested/beta.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/_template/beta/beta.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/_template/gamma/gamma.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/alpha_out.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/beta_out.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/gamma_out.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/index.typ')]
E       assert not [PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/_template/alpha/alpha.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/_template/alpha/nested/beta.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/_template/beta/beta.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/_template/gamma/gamma.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/alpha_out.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/beta_out.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/gamma_out.typ'), PosixPath('/tmp/pytest-of-yuta/pytest-1419/test_no_typ_file_written_after0/build/index.typ')]

=========================== short test summary info ============================
FAILED tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_multi_relation_build_refuses
FAILED tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_multi_relation_aggregate_names_all_three_keys_in_sorted_order
FAILED tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_multi_relation_aggregate_failure_count_is_three
FAILED tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_multi_relation_each_key_names_own_bundle_dir_and_own_entry
FAILED tests/test_templates_path_collision_gate.py::TestMultiRelationAggregationGate::test_no_typ_file_written_after_multi_relation_refusal
============================== 5 failed in 1.09s ===============================
```

**Why it is RED on this tree:** identical root cause to `54.1-01-RED-EVIDENCE.md` — the pre-fix
`write()` never calls `_validate_used_template_paths()` (it does not exist on this commit), so
none of `templates_path`'s three collision relations are ever checked; `finish()` runs
`_copy_used_template_bundles()`, which copies all three registry keys' resolved bundle
directories wholesale into `<outdir>/_template/<key>/`. `git status --porcelain typsphinx/`
produced no output after restoration — `typsphinx/` was left byte-identical to its pre-swap
state.

## Control fixtures (Task 2) — explicitly NOT recorded RED here

`templates_path_adjacent_control_gate/`, `templates_path_absent_control_gate/`, and
`templates_path_no_typst_template_control_gate/` are controls: they pass both before and after
the fix by design (this plan's binding prohibition). No RED transcript exists for any of them,
and none should ever be added.
