---
phase: 54-one-bundle-rule-template-key-per-document-selection-four-del
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/source/user_guide/templates.rst
  - docs/source/user_guide/configuration.rst
  - CHANGELOG.md
autonomous: true
requirements: [BLD-06, OUT-04]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md`'s BLD-06 line no longer asserts a symlink prohibition, and `.planning/ROADMAP.md`'s Phase 54 SC#3 no longer promises a named error for a symlink whose resolved path leaves the bundle — the owner retracted both (D-03) and no code, error, or test in this phase implements them."
    - "The metadata-exclusion half of BLD-06 and of SC#3 survives the amendment word for word; the retraction removes a clause, it does not weaken the requirement."
    - "Every reader-facing description of the shadow-template route names `<srcdir>/_typst/base.typ`, not `<srcdir>/base.typ` — a user following the published documentation after v0.9.0 places the file where the extension will actually look (D-14)."
    - "`CHANGELOG.md`'s `## [Unreleased]` section carries a breaking-change entry for the moved shadow route, stating the old location, the new location, and the observable consequence of doing nothing (the build typesets with the bundled default instead of that file)."
    - "No requirement checkbox state in `.planning/REQUIREMENTS.md` changes in this plan — only BLD-06's descriptive text."
  prohibitions:
    - "A guard nobody asked for is scope, not safety: no symlink refusal, no named symlink error, and no test asserting one may be introduced anywhere in this phase."
    - "A behaviour change that silently produces a different PDF is not acceptable — the shadow-route relocation must be announced in the changelog and in the published documentation, not discovered by a user whose output quietly changed."
  artifacts:
    - CHANGELOG.md
  key_links:
    - "`.planning/REQUIREMENTS.md` BLD-06 and `.planning/ROADMAP.md` SC#3 are the two places the retracted symlink clause is still written down; a downstream verifier reads them literally, so leaving either intact re-creates the retracted obligation."
    - "`docs/source/user_guide/templates.rst` and `configuration.rst` are the two published pages that name the shadow route; `54-04` moves the code and the test fixture, and these two lines are the user-facing half of the same move."
---

<objective>
Pay the two document debts this phase owes: retract the symlink clause the owner rejected, and
publish the relocated shadow-template route.

Purpose: the retraction (D-03) is not a note — `.planning/REQUIREMENTS.md`'s BLD-06 line and
`.planning/ROADMAP.md`'s Phase 54 SC#3 still carry the withdrawn wording, and every downstream
verifier, gap-analyser and reviewer reads those files literally. Left as-is they would reopen an
obligation the owner explicitly cancelled, and a verification pass would report a false gap. The
publication half (D-14) is the same shape in the other direction: the shadow-template route's
source-side location moves from `<srcdir>/base.typ` to `<srcdir>/_typst/base.typ`, which is a
published, documented contract; moving it without saying so gives an upgrading project a silently
different PDF and no diagnostic.

Output: two amended planning documents, two amended documentation pages, and one changelog entry.
No source code, no tests.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-CONTEXT.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Created by THIS PLAN (new — not drift):
- a new `### Changed` breaking-change bullet under `## [Unreleased]` in `CHANGELOG.md`

Created elsewhere in Phase 54 and therefore also NOT drift when referenced here:
`self._used_template_keys`, `_copy_used_template_bundles()`, `_copy_bundle_directory()`,
`_is_excluded_bundle_entry()`, `compute_template_import_path()`, `TEMPLATE_SEARCH_SUBDIR`,
`TEMPLATE_OUTPUT_DIR`, `typsphinx/templates/README.md`, `typsphinx/removed_config.py`,
`check_config_at_init()`, `_reserves_template_prefix()`,
`tests/test_user_template_relative_asset_gate.py`, `tests/test_two_key_selection_gate.py`,
`tests/test_bundle_copy_exclusion_manifest_gate.py`,
`tests/fixtures/template_prefix_reservation_gate/`, `tests/fixtures/nested_dir_multi_master/`.
</artifacts_this_phase_produces>

<what_this_phase_deletes>
THIS plan deletes text, not code: the symlink clause from `.planning/REQUIREMENTS.md`'s BLD-06 and
from `.planning/ROADMAP.md`'s Phase 54 SC#3.

Deleted by other plans in this phase: `_write_template_file()` and its `prepare_writing()` call
site; the `.typ` exclusion inside `_copy_template_directory()`; `copy_template_assets()`'s three
early returns and the method itself; `_copy_explicit_assets()` / `_copy_single_asset()`; the
`typst_template_assets` config registration; `compute_template_import_path_for_dir()`;
`tests/test_template_assets.py`; `srcdir` itself as a template `search_paths` member; and the
exact-name reservation of the root infrastructure file inside `_validate_output_path_collisions()`.
</what_this_phase_deletes>

<edge_probe_disposition>
The full 10-row spec-less edge-probe disposition table lives in `54-04-PLAN.md` §
`<edge_probe_disposition>`. This plan owns no probe row of its own; BLD-06's row is `unclassified`
and is surfaced there as a flagged planner assumption.
</edge_probe_disposition>

<tasks>

<task type="auto">
  <name>Task 1: Retract the symlink clause from BLD-06 and from Phase 54 SC#3</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the whole "Builder mechanics" block. BLD-06 currently reads
      "The bundle copy excludes VCS and OS metadata and does not follow a symlink out of the
      bundle". Read the surrounding lines so the amendment matches the file's bullet style exactly.
    - `.planning/ROADMAP.md` § "Phase 54: ..." Success Criteria item 3. It currently promises the
      copy "excludes VCS and OS metadata (`.git`, `.DS_Store`, `Thumbs.db`, editor backups) and
      refuses, with a named error, a symlink whose resolved path is not a descendant of the bundle;
      both are asserted by a manifest-diff test".
    - `.planning/phases/54-.../54-CONTEXT.md` D-02 and D-03 — D-03 is the retraction and its
      recorded reason; D-02 is the whole of the remaining answer to "what happens at a symlink"
      (`os.walk(followlinks=False)` copies a file symlink's content and does not descend into a
      directory symlink).
  </read_first>
  <action>
    Amend both documents with scoped edits — never rewrite either file wholesale.

    In `.planning/REQUIREMENTS.md`, rewrite BLD-06's descriptive text so it states only the
    metadata-exclusion obligation, and append a short parenthetical recording that the symlink
    clause was retracted by the owner at Phase 54 planning (D-03) and that
    `os.walk(followlinks=False)` plus per-file `shutil.copy2` is the recorded behaviour at a
    symlink. Do NOT change the checkbox state of BLD-06 or of any other requirement — this plan
    changes descriptive text only, and a flipped checkbox here would be indistinguishable from the
    auto-flip defect this project has recorded four times.

    In `.planning/ROADMAP.md`, edit only Phase 54's Success Criteria item 3. Remove the clause
    beginning "and refuses, with a named error, a symlink whose resolved path is not a descendant
    of the bundle", and repair the sentence that follows so it reads as a single obligation rather
    than a pair ("both are asserted by" becomes a singular construction). Keep, verbatim and
    unweakened: the four excluded kinds, the manifest-diff requirement ("no file I didn't expect is
    present", not presence-only), and the recorded re-run/staleness policy sentence. Append a short
    inline note that the symlink half was retracted by owner decision D-03 at Phase 54 planning, so
    a later reader does not mistake the shorter text for drift.

    Leave every other phase entry in `.planning/ROADMAP.md` untouched.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; test "$(grep -c 'symlink' .planning/REQUIREMENTS.md)" = "0" &amp;&amp; test "$(git diff --numstat .planning/ROADMAP.md | awk '{print ($1+$2 &lt; 30)}')" = "1" &amp;&amp; grep -q 'D-03' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'D-03' .planning/ROADMAP.md &amp;&amp; uv run pytest tests/ -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'symlink' .planning/REQUIREMENTS.md` returns 0.
    - `.planning/REQUIREMENTS.md` still contains a BLD-06 bullet, and that bullet still contains the
      literals `VCS` and `metadata`.
    - `.planning/REQUIREMENTS.md` contains the literal `D-03`.
    - `grep -c 'not a descendant of the bundle' .planning/ROADMAP.md` returns 0.
    - `.planning/ROADMAP.md`'s Phase 54 SC#3 still contains all four literals `.git`, `.DS_Store`,
      `Thumbs.db`, and `manifest-diff`.
    - `.planning/ROADMAP.md` contains the literal `D-03`.
    - `git diff --numstat .planning/ROADMAP.md` shows fewer than 30 changed lines total (the edit is
      scoped to one success criterion, not a rewrite).
    - `git diff .planning/REQUIREMENTS.md | grep -E '^[-+].*\- \[[ x]\]' | grep -v 'BLD-06'` returns
      nothing (no other requirement's line — and therefore no checkbox — was touched).
    - `uv run pytest tests/ -q` reports 0 failed and 0 errors.
  </acceptance_criteria>
  <done>Neither planning document asserts a symlink obligation any longer, the metadata-exclusion obligation is intact and unweakened, and both records name the decision that retracted it.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the relocated shadow-template route in the docs and the changelog</name>
  <files>docs/source/user_guide/templates.rst, docs/source/user_guide/configuration.rst, CHANGELOG.md</files>
  <read_first>
    - `docs/source/user_guide/templates.rst` around line 213 — the `title` bullet in the
      nine-parameter contract, which names the shadow route as one of the three non-package routes.
    - `docs/source/user_guide/configuration.rst` around line 325 — the "Scope: every non-package
      route" subsection, which names it again.
    - `CHANGELOG.md` lines 1-30 — the `## [Unreleased]` block as it stands today, and the
      `## [0.8.0]` block below it for the established section ordering and bullet voice
      (`### Added` / `### Changed`, bold lede, requirement IDs in parentheses).
    - `.planning/phases/54-.../54-CONTEXT.md` D-14 and D-15 — the new location, why `_typst/` and
      not `_templates/` (Sphinx's own `templates_path` default), and that the relocation announces
      itself rather than changing behaviour silently.
    - `tests/test_docs_contract_claims_gate.py` lines 120-160 — which documentation pages that gate
      reads, so the edit is made knowing what asserts on these files.
  </read_first>
  <action>
    Update the two published pages that name the shadow-template route, changing
    `<srcdir>/base.typ` to `<srcdir>/_typst/base.typ` in both. Keep each sentence's surrounding
    claim intact — both are statements about which routes receive the auto-derived parameters, and
    only the path changes. Search the whole of `docs/source/` for any further occurrence of the old
    spelling before finishing, rather than trusting the two cited line numbers: this project's own
    milestone invariant treats an "anywhere under X" obligation as a repo-wide grep, and a third
    occurrence added since the citation would otherwise ship stale. `docs/source/changelog.rst` is
    a historical record of past releases and its existing occurrences must NOT be rewritten — a
    changelog entry describes the release it belongs to.

    Add a new entry under `## [Unreleased]` in `CHANGELOG.md`. Follow the file's established
    structure: a `### Changed` subsection (create it under `## [Unreleased]` if absent, above the
    existing "Planned for Future Releases" list) with a bold-lede bullet naming this as a breaking
    change. It must state:
    - the old location (`<srcdir>/base.typ`) and the new one (`<srcdir>/_typst/base.typ`);
    - the observable consequence of doing nothing: the build now typesets with the bundled default
      template instead of that file;
    - that the build warns by name when the old location is still present and the new one is not,
      so the change announces itself;
    - the reason, in one clause: the resolved template's parent directory is now copied wholesale to
      the output as that key's bundle, so it must be a real bundle directory and not the whole
      source tree.
    Reference the requirement IDs the way the 0.8.0 entries do.

    Do NOT touch the link-reference block at the bottom of `CHANGELOG.md` and do NOT create a
    `## [0.9.0]` heading or move the `[Unreleased]` compare link — the version bump, the release
    heading and the link-block maintenance are Phase 57's work, and doing them here would collide
    with it.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; test "$(grep -rn 'srcdir>/base\.typ' docs/source/user_guide/ | wc -l)" = "0" &amp;&amp; grep -q '_typst/base\.typ' docs/source/user_guide/templates.rst &amp;&amp; grep -q '_typst/base\.typ' docs/source/user_guide/configuration.rst &amp;&amp; grep -q '_typst/base\.typ' CHANGELOG.md &amp;&amp; uv run pytest tests/test_docs_contract_claims_gate.py tests/test_output_layout_docs_gate.py -q 2>&amp;1 | tail -5 &amp;&amp; uv run pytest tests/ -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'srcdir>/base\.typ' docs/source/user_guide/` returns nothing.
    - `docs/source/user_guide/templates.rst` and `docs/source/user_guide/configuration.rst` each
      contain the literal `_typst/base.typ` at least once.
    - `docs/source/changelog.rst` is unmodified (`git diff --name-only` does not list it).
    - `CHANGELOG.md` contains a `### Changed` heading between the `## [Unreleased]` heading and the
      `## [0.8.0]` heading.
    - The new `CHANGELOG.md` bullet contains all of the literals `_typst/base.typ`, `base.typ`, and
      the word `warn` (or `warning`).
    - `grep -c '^## \[0\.9\.0\]' CHANGELOG.md` returns 0 (no release heading was created).
    - `git diff CHANGELOG.md | grep -c '^[-+]\[Unreleased\]:'` returns 0 (the link block is
      untouched).
    - `uv run pytest tests/test_docs_contract_claims_gate.py tests/test_output_layout_docs_gate.py -q`
      reports 0 failed and 0 errors.
    - `uv run pytest tests/ -q` reports 0 failed and 0 errors.
  </acceptance_criteria>
  <done>A reader of the published documentation is told the shadow template lives at `<srcdir>/_typst/base.typ`, and an upgrading project reading the changelog learns what moved, what happens if they do nothing, and that the build will tell them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published documentation and changelog → a user's `conf.py` and source layout | Readers act on these files; a stale instruction causes a wrong-by-construction project layout |
| planning documents → downstream verification agents | Verifiers read `REQUIREMENTS.md` / `ROADMAP.md` literally; a stale clause manufactures a false obligation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-15 | Repudiation | a retracted requirement clause left written down | medium | mitigate | Both surviving copies of the symlink clause are amended in this plan, each carrying an inline `D-03` breadcrumb so the shorter text is attributable rather than reading as drift |
| T-54-16 | Repudiation | a published contract relocated without announcement | high | mitigate | Docs updated and a changelog breaking-change entry added naming old location, new location, and the observable consequence; the runtime warning that pairs with it ships in `54-06` (D-15) |
| T-54-17 | Tampering | a whole-file rewrite of `ROADMAP.md` destroying unrelated phase entries | high | mitigate | Scoped `Edit` only, plus an acceptance criterion capping the diff at fewer than 30 changed lines |
| T-54-18 | Tampering | an unrelated requirement checkbox flipped while editing `REQUIREMENTS.md` | medium | mitigate | An acceptance criterion greps the diff for any changed checkbox line other than BLD-06's and requires it to be empty |
| T-54-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs zero packages; `54-RESEARCH.md` § Package Legitimacy Audit records that the gate has nothing to check |
</threat_model>

<verification>
- `grep -c 'symlink' .planning/REQUIREMENTS.md` → 0; `grep -c 'not a descendant of the bundle' .planning/ROADMAP.md` → 0.
- `grep -rn 'srcdir>/base\.typ' docs/source/user_guide/` → no hits.
- `CHANGELOG.md` has a `### Changed` bullet under `## [Unreleased]` naming both locations.
- `uv run pytest tests/ -q && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/`
  all green.
- `git diff --stat` lists exactly the five files in `files_modified` and nothing else.
</verification>

<success_criteria>
- The retracted symlink clause exists nowhere in `.planning/REQUIREMENTS.md` or
  `.planning/ROADMAP.md`, and the metadata-exclusion obligation survives word for word.
- Both published pages name `<srcdir>/_typst/base.typ`.
- The changelog announces the relocation as a breaking change with its observable consequence.
- No source file and no test file was modified.
</success_criteria>

<output>
Create `.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-03-SUMMARY.md` when done
</output>
