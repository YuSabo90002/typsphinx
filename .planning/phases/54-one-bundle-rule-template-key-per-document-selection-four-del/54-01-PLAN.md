---
phase: 54-one-bundle-rule-template-key-per-document-selection-four-del
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/user_template_relative_asset_gate/conf.py
  - tests/fixtures/user_template_relative_asset_gate/index.rst
  - tests/fixtures/user_template_relative_asset_gate/_typst/branded.typ
  - tests/fixtures/user_template_relative_asset_gate/_typst/logo.png
  - tests/fixtures/two_key_selection_gate/conf.py
  - tests/fixtures/two_key_selection_gate/index.rst
  - tests/fixtures/two_key_selection_gate/guide/index.rst
  - tests/fixtures/two_key_selection_gate/memo/index.rst
  - tests/fixtures/two_key_selection_gate/_typst/report/base.typ
  - tests/fixtures/two_key_selection_gate/_typst/memo/base.typ
  - tests/fixtures/bundle_exclusion_manifest_gate/conf.py
  - tests/fixtures/bundle_exclusion_manifest_gate/index.rst
  - tests/fixtures/bundle_exclusion_manifest_gate/_typst/styled/base.typ
  - tests/fixtures/bundle_exclusion_manifest_gate/_typst/styled/assets/note.txt
  - tests/test_user_template_relative_asset_gate.py
  - tests/test_two_key_selection_gate.py
  - tests/test_bundle_copy_exclusion_manifest_gate.py
  - .planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-01-RED-EVIDENCE.md
autonomous: true
requirements: [TPL-02, OUT-04, OUT-05, OUT-06, BLD-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real `sphinx-build -b typstpdf` of a fixture whose USER-supplied template calls `#image(\"logo.png\")` on a same-directory asset exits non-zero on the pre-relocation tree, and the failing output is recorded in 54-01-RED-EVIDENCE.md (SC#3's RED-first bar; the built-in template is not accepted as evidence)."
    - "A real `sphinx-build -b typstpdf` of a fixture whose `typst_documents` names two different registry keys shows, on the pre-relocation tree, that neither `<outdir>/_template/report/base.typ` nor `<outdir>/_template/memo/base.typ` exists — recorded RED."
    - "A real `sphinx-build -b typst` of a fixture whose bundle directory carries `.git/`, `.DS_Store`, `Thumbs.db` and an editor backup shows, on the pre-relocation tree, that `<outdir>/_template/styled/` does not exist — recorded RED."
    - "Each of the three new test modules runs (collects, executes, reports) under `uv run pytest`; none errors during collection or during a class-scoped fixture, so `xfail` can cover its failure."
    - "After the three modules are marked `xfail(strict=False)`, `uv run pytest tests/ -q` reports zero failures and zero errors."
    - "No new fixture asserts an exact file manifest over the bundled `\"typst\"` template directory shipped inside the installed package — `54-02` adds a file there in this same wave."
  prohibitions:
    - "A user's Sphinx source tree must never be republished as build output: no fixture in this phase may be shaped so that a passing assertion would require copying `conf.py` or `.rst` sources into the output tree."
    - "A behaviour change that silently produces a different PDF is not acceptable — every relocation and removal in this phase announces itself to the user."
    - "This extension never deletes a file under the output directory; no test may be written that would only pass if it did."
  artifacts:
    - tests/fixtures/user_template_relative_asset_gate/_typst/branded.typ
    - tests/fixtures/user_template_relative_asset_gate/_typst/logo.png
    - tests/fixtures/two_key_selection_gate/_typst/report/base.typ
    - tests/fixtures/two_key_selection_gate/_typst/memo/base.typ
    - tests/fixtures/bundle_exclusion_manifest_gate/_typst/styled/base.typ
    - tests/test_user_template_relative_asset_gate.py
    - tests/test_two_key_selection_gate.py
    - tests/test_bundle_copy_exclusion_manifest_gate.py
    - .planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-01-RED-EVIDENCE.md
  key_links:
    - "Every fixture's template file lives one directory below srcdir (never at srcdir root) so its parent is a genuine bundle directory — the invariant D-14 exists to establish."
    - "The three modules' `xfail(strict=False)` markers are the handshake with 54-04: 54-04 removes them and proves green."
---

<objective>
Create the three real-compile regression gates this phase's success criteria demand, and record
each one RED against the pre-relocation tree before any production code changes.

Purpose: ROADMAP binding constraint #6 (the standing GATE-01 bar since v0.6.0) and Phase 54 SC#3
both require the RED assertion to be written down BEFORE implementation starts. SC#3 goes further
for OUT-05: the fixture must be a genuine `sphinx-build → typst.compile()` fixture recorded RED
against the pre-relocation tree, and the built-in template — measured to have zero path-relative
references — is explicitly not accepted as evidence. This plan pays that bar for OUT-05, TPL-02/
OUT-06, and BLD-06/OUT-04 in one pass, on a tree where the production code is still untouched, so
the RED is real rather than reconstructed.

Output: three fixture directories, three test modules, and `54-01-RED-EVIDENCE.md` carrying the
verbatim failing output of each. The three modules end the plan marked
`@pytest.mark.xfail(strict=False)` so the wave stays green; `54-04` removes the markers and proves
green for real.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-CONTEXT.md
@.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Symbols, files and mechanisms created by THIS PLAN (new — not drift):

- `tests/fixtures/user_template_relative_asset_gate/` (new fixture dir: `conf.py`, `index.rst`,
  `_typst/branded.typ`, `_typst/logo.png`)
- `tests/fixtures/two_key_selection_gate/` (new fixture dir: `conf.py`, `index.rst`,
  `guide/index.rst`, `memo/index.rst`, `_typst/report/base.typ`, `_typst/memo/base.typ`)
- `tests/fixtures/bundle_exclusion_manifest_gate/` (new fixture dir: `conf.py`, `index.rst`,
  `_typst/styled/base.typ`, `_typst/styled/assets/note.txt`)
- `tests/test_user_template_relative_asset_gate.py` (new module)
- `tests/test_two_key_selection_gate.py` (new module)
- `tests/test_bundle_copy_exclusion_manifest_gate.py` (new module)
- `54-01-RED-EVIDENCE.md` (new evidence artifact)

Created elsewhere in Phase 54 and therefore also NOT drift when referenced here:
`self._used_template_keys`, `_copy_used_template_bundles()`, `_copy_bundle_directory()`,
`_is_excluded_bundle_entry()`, `compute_template_import_path()`, `TEMPLATE_SEARCH_SUBDIR`,
`TEMPLATE_OUTPUT_DIR`, `typsphinx/templates/README.md`, `typsphinx/removed_config.py`,
`check_config_at_init()`, `_reserves_template_prefix()`,
`tests/fixtures/template_prefix_reservation_gate/`, `tests/fixtures/nested_dir_multi_master/`.
</artifacts_this_phase_produces>

<what_this_phase_deletes>
Deleted by other plans in this phase (listed here so the whole identity of Phase 54 is visible from
any plan): `_write_template_file()` and its `prepare_writing()` call site; the `.typ` exclusion
inside `_copy_template_directory()`; `copy_template_assets()`'s three early returns and the method
itself; `_copy_explicit_assets()` / `_copy_single_asset()`; the `typst_template_assets` config
registration; `compute_template_import_path_for_dir()`; `tests/test_template_assets.py`; `srcdir`
itself as a template `search_paths` member; and the exact-name reservation of the root
infrastructure file inside `_validate_output_path_collisions()`.

THIS plan deletes nothing.
</what_this_phase_deletes>

<edge_probe_disposition>
The full 10-row spec-less edge-probe disposition table (and the six `unclassified` rows surfaced as
flagged planner assumptions) lives in `54-04-PLAN.md` § `<edge_probe_disposition>`, the phase spine.
This plan authors the predicates it owns into `must_haves.truths` above.
</edge_probe_disposition>

<tasks>

<task type="tracer">
  <name>Task 1: OUT-05 — a user template's own `#image("logo.png")`, end to end, recorded RED</name>
  <files>tests/fixtures/user_template_relative_asset_gate/conf.py, tests/fixtures/user_template_relative_asset_gate/index.rst, tests/fixtures/user_template_relative_asset_gate/_typst/branded.typ, tests/fixtures/user_template_relative_asset_gate/_typst/logo.png, tests/test_user_template_relative_asset_gate.py, .planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-01-RED-EVIDENCE.md</files>
  <read_first>
    - `tests/test_typst_lang_gate.py` lines 70-130 and 590-650 — the canonical GATE-01 scaffolding
      in this repository: module-level `FIXTURES_DIR` constants, `_run_sphinx_build()` invoking
      `sys.executable -m sphinx` (NEVER a resolved `sphinx-build` binary — NixOS PATH-shadowing),
      the `TYPST_AVAILABLE` guard, and the class-scoped `build` fixture shape.
    - `tests/fixtures/typst_lang_gate/srcdir_shadow_lang/base.typ` — a complete, currently-green
      template that declares the full parameter contract the writer passes on a non-package route.
      Copy its `project()` signature; do not invent one.
    - `typsphinx/templates/base.typ` — the shipped default, for the same signature contract.
    - `docs/source/user_guide/templates.rst` lines 205-235 — the nine-parameter contract a custom
      template must accept (`title`, `authors`, `date`, `lang` unconditionally on every
      non-package route; `papersize`/`fontsize` conditionally).
    - `.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-CONTEXT.md`
      § Claude's Discretion (fixture composition) and `54-VALIDATION.md` § Wave 0 Requirements.
    - `CLAUDE.md` § "Worktree-isolated execution".
  </read_first>
  <action>
    Build the OUT-05 gate the phase does not currently have, per ROADMAP constraint #7 (all three
    real templates in this repository were measured to carry font-family references only, so none
    of them proves the relocation preserves a path-relative reference) and SC#3 (the built-in
    template is not accepted as evidence).

    Fixture `tests/fixtures/user_template_relative_asset_gate/`:
    - `conf.py` — `extensions = ["typsphinx"]`; `project`, `author`, `release` set;
      `typst_template = "_typst/branded.typ"`; `typst_documents = [("index", "master", project,
      author)]`. The target is the bare string `master`, never the identity string `index` (an
      identity target makes the wrapper resolve onto its own content file and produces a cyclic
      import — the fixture de-collision rule this repository already applies in
      `tests/fixtures/typst_lang_gate/srcdir_shadow_lang/conf.py`). Carry a comment block, in this
      repository's established fixture-comment style, stating that the template deliberately lives
      one directory below srcdir so its parent is a genuine bundle directory (per D-14), and that
      the `#image()` call is the single load-bearing line — nobody may "clean it up".
    - `_typst/branded.typ` — a `project()` function whose signature matches
      `typsphinx/templates/base.typ`'s (`title`, `authors`, `date`, `lang`, plus whatever that file
      declares with defaults), whose body calls `#image("logo.png", width: 24pt)` with that exact
      relative, same-directory path, and which then emits `body`. The `@preview` package imports
      inside it must match the four versions already declared in `typsphinx/templates/base.typ` —
      or, preferably, declare no `@preview` import at all, so this fixture never becomes a fourth
      version-lockstep site (`CLAUDE.md` § "The `@preview` version-sync hazard";
      `tests/test_preview_version_sync.py` asserts the existing three agree).
    - `_typst/logo.png` — a real, minimal, valid PNG (a 1x1 or 2x2 image is enough); Typst must be
      able to decode it, so a text file renamed `.png` will not do. Generate it with Python's
      `zlib`/`struct` or copy an existing tiny PNG already in the repository if one exists.
    - `index.rst` — a title and one paragraph.

    Test module `tests/test_user_template_relative_asset_gate.py`, modelled on
    `tests/test_typst_lang_gate.py`'s `TestJapaneseSourceProof`/`TestGermanLinkageProof` shape:
    - module-level `FIXTURE_DIR` constant, the `_run_sphinx_build()` helper using
      `sys.executable -m sphinx`, and the `TYPST_AVAILABLE` skipif guard.
    - CRITICAL shape difference from the analog: the class-scoped `build` fixture must contain NO
      `assert` statements. It returns a dict carrying `result` (the `CompletedProcess`), `build_dir`
      and lazily-read text guarded by `.exists()`. Reason: this module is marked `xfail` in Task 3,
      and `xfail` covers only the call phase — an assertion inside a fixture reports as a setup
      ERROR, which `xfail` does not swallow.
    - `test_build_succeeds` — asserts `result.returncode == 0`, quoting stdout and stderr.
    - `test_pdf_is_valid` — asserts the PDF exists, is non-empty, and begins with `b"%PDF"`.
    - `test_asset_reached_the_bundle_destination` — asserts
      `(build_dir / "_template" / "typst" / "logo.png").exists()` and
      `(build_dir / "_template" / "typst" / "branded.typ").exists()`.
    - `test_wrapper_imports_the_bundled_template` — asserts the wrapper `master.typ` contains the
      literal `/_template/typst/branded.typ`.

    Run the module and capture its RED output. Create `54-01-RED-EVIDENCE.md` with a heading per
    gate; under "OUT-05" paste the verbatim `uv run pytest` tail (the failing assertion and the
    captured `sphinx-build` stderr), the exact command, the `git rev-parse HEAD` of the tree it was
    measured against, and one sentence naming WHY it is red on this tree (the template body is
    written to a file at the output root and the asset is copied beside the source-relative
    directory, so the relative reference has nothing to resolve against). Do NOT add the `xfail`
    marker yet — that is Task 3.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest tests/test_user_template_relative_asset_gate.py -x 2>&amp;1 | tail -40; test -s .planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-01-RED-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_user_template_relative_asset_gate.py` reports at least one FAILED
      test and reports zero ERRORS (an error would mean an assertion leaked into the class-scoped
      fixture).
    - `grep -c 'assert' tests/test_user_template_relative_asset_gate.py` is greater than 0, and no
      `assert` appears between the `def build(` line and the fixture's `return` statement.
    - `tests/fixtures/user_template_relative_asset_gate/_typst/branded.typ` contains the literal
      `#image("logo.png"`.
    - `python3 -c "import sys;d=open('tests/fixtures/user_template_relative_asset_gate/_typst/logo.png','rb').read();sys.exit(0 if d[:8]==b'\x89PNG\r\n\x1a\n' else 1)"` exits 0.
    - `tests/fixtures/user_template_relative_asset_gate/conf.py` contains `typst_template = "_typst/branded.typ"` and does not contain a `typst_documents` entry whose target element is the string `index`.
    - `54-01-RED-EVIDENCE.md` contains a `## OUT-05` heading, the literal string `returncode`, and a 40-hex-character commit SHA.
    - `grep -c '@preview' tests/fixtures/user_template_relative_asset_gate/_typst/branded.typ` is 0, or every `@preview` version string in it also appears in `typsphinx/templates/base.typ`.
  </acceptance_criteria>
  <done>The OUT-05 gate exists as a real `sphinx-build → typst.compile()` fixture, fails on the pre-relocation tree for the right reason, and that failure is recorded verbatim with the commit it was measured against.</done>
</task>

<task type="auto">
  <name>Task 2: TPL-02/OUT-06 two-key selection gate and BLD-06/OUT-04 manifest-diff gate, both recorded RED</name>
  <files>tests/fixtures/two_key_selection_gate/conf.py, tests/fixtures/two_key_selection_gate/index.rst, tests/fixtures/two_key_selection_gate/guide/index.rst, tests/fixtures/two_key_selection_gate/memo/index.rst, tests/fixtures/two_key_selection_gate/_typst/report/base.typ, tests/fixtures/two_key_selection_gate/_typst/memo/base.typ, tests/fixtures/bundle_exclusion_manifest_gate/conf.py, tests/fixtures/bundle_exclusion_manifest_gate/index.rst, tests/fixtures/bundle_exclusion_manifest_gate/_typst/styled/base.typ, tests/fixtures/bundle_exclusion_manifest_gate/_typst/styled/assets/note.txt, tests/test_two_key_selection_gate.py, tests/test_bundle_copy_exclusion_manifest_gate.py, .planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-01-RED-EVIDENCE.md</files>
  <read_first>
    - `tests/fixtures/template_named_dir_master/conf.py` — the established fixture-comment style
      for a multi-entry fixture, and the reasons two entries must carry two DISTINCT bare targets.
    - `typsphinx/template_registry.py` lines 179-210 (`TemplateRegistryEntry`) and 472-529
      (`resolve_registry_key()`) — how element `[4]` resolves, and that lookup is exact `str`
      equality, never case-folded.
    - `typsphinx/template_registry.py` lines 137-178 (`_violates_conf17`) — why a declared
      `template` may not sit directly under srcdir; every fixture bundle here must satisfy it.
    - `tests/test_typst_lang_gate.py` lines 70-130 — the `_run_sphinx_build()` / `TYPST_AVAILABLE`
      scaffolding to copy.
    - `.planning/phases/54-.../54-CONTEXT.md` D-01 (overwrite in place, never delete first; the
      manifest claim is about a CLEAN outdir) and D-04 (the exclusion set is exactly four kinds).
    - `tests/test_multi_master_metadata_no_leak.py` lines 1-60 — how this repository proves two
      masters keep their own metadata.
  </read_first>
  <action>
    Build the two remaining RED-first gates.

    **Fixture `tests/fixtures/two_key_selection_gate/`** (TPL-02, OUT-06):
    - `conf.py` declares two registry keys:
      `typst_document_templates = {"report": {"template": "_typst/report/base.typ"}, "memo": {"template": "_typst/memo/base.typ"}}`
      and three `typst_documents` entries:
      `("index", "master", "Report Root", "Report Author", "report")`,
      `("guide/index", "manuals/guide", "Report Nested", "Report Author", "report")`,
      `("memo/index", "memos/memo", "Memo Master", "Memo Author", "memo")`.
      The first two deliberately share the key `report` at two different wrapper depths (root and
      `manuals/`) — that pair is OUT-06's entire proof. Record that in the conf.py comment block,
      alongside the reason both bundles live under `_typst/<key>/` rather than at srcdir root.
    - `_typst/report/base.typ` and `_typst/memo/base.typ` — two `project()` functions with the same
      signature contract as Task 1's template, made VISIBLY different from each other in a way a
      PDF byte-comparison can see: give them different `set page(...)` paper sizes and different
      `set text(size: ...)` values, and put a distinct marker comment in each
      (`// TWO-KEY-GATE-REPORT-TEMPLATE`, `// TWO-KEY-GATE-MEMO-TEMPLATE`). Same `@preview`
      abstinence rule as Task 1.
    - `index.rst`, `guide/index.rst`, `memo/index.rst` — a title and a paragraph each; the root
      `index.rst` carries a toctree naming `guide/index` and `memo/index`.

    **Test module `tests/test_two_key_selection_gate.py`** — same non-asserting class-scoped `build`
    fixture rule as Task 1. Tests:
    - `test_build_succeeds` and `test_three_pdfs_produced` (all three PDFs exist, non-empty, `%PDF`).
    - `test_both_report_wrappers_emit_an_identical_import_string` — extract the `#import` line whose
      argument contains `_template` from `master.typ` and from `manuals/guide.typ`, and assert the
      two extracted strings are EQUAL to each other and equal to the literal
      `/_template/report/base.typ`. Equality between the two is OUT-06's contract; the literal is
      the layout.
    - `test_memo_wrapper_imports_its_own_key` — `memos/memo.typ` contains
      `/_template/memo/base.typ`.
    - `test_both_bundles_are_published` — `_template/report/base.typ` and `_template/memo/base.typ`
      both exist under the build directory.
    - `test_the_two_templates_produce_different_pdfs` — the `master.pdf` and `memos/memo.pdf` byte
      contents differ. Add a docstring naming the manual companion check recorded in
      `54-VALIDATION.md` § Manual-Only Verifications ("visibly different" is a rendering judgement
      a byte comparison cannot make).

    **Fixture `tests/fixtures/bundle_exclusion_manifest_gate/`** (BLD-06, OUT-04, D-01, D-04):
    - `conf.py` — `typst_document_templates = {"styled": {"template": "_typst/styled/base.typ"}}`,
      one entry `("index", "master", project, author, "styled")`.
    - `_typst/styled/base.typ` — same signature contract, marker comment
      `// BUNDLE-EXCLUSION-GATE-TEMPLATE`.
    - `_typst/styled/assets/note.txt` — a nested non-`.typ` file that MUST reach the output; it is
      what proves the copy is recursive and not top-level-only.
    - Do NOT commit `.git/`, `.DS_Store`, `Thumbs.db` or a backup file into the fixture: a nested
      `.git` directory cannot be tracked by this repository at all, and the other three are the
      kinds a working tree routinely gitignores. The test materialises them instead (below).

    **Test module `tests/test_bundle_copy_exclusion_manifest_gate.py`** — same non-asserting
    `build`-fixture rule. Its class-scoped fixture must:
    - `shutil.copytree` the fixture directory into a FRESH `tmp_path_factory.mktemp(...)` source
      directory (D-01: the manifest claim is a claim about a clean output directory, so the build
      target must be fresh too),
    - inject into the copied `_typst/styled/` the four excluded kinds: `.git/config` (a directory
      named `.git` containing a file), `.DS_Store`, `Thumbs.db`, and `notes.txt~`,
    - run `sphinx-build -b typst` into a fresh build directory and return the paths.
    Tests:
    - `test_bundle_manifest_is_exactly_the_expected_set` — build the actual manifest as
      `{p.relative_to(bundle_root).as_posix() for p in bundle_root.rglob("*") if p.is_file()}`
      against `bundle_root = build_dir / "_template" / "styled"`, and assert it EQUALS
      `{"base.typ", "assets/note.txt"}`. Equality, not membership — SC#3 requires manifest-diff
      ("no file I didn't expect is present"), not presence-only.
    - `test_each_excluded_kind_is_named_in_the_expected_set_comment` — a source-level guard: assert
      the module's own text enumerates all four excluded kinds as literals, so D-04's exclusion set
      is enumerated in the test rather than implied.
    - `test_rerun_leaves_a_removed_source_file_in_place` — build once, delete
      `_typst/styled/assets/note.txt` from the copied source, build AGAIN into the SAME build
      directory, and assert `assets/note.txt` is STILL present at the destination. This encodes
      D-01's recorded policy (overwrite in place, never delete first; a stale destination file on
      an incremental rebuild is accepted behaviour, not a defect) as a test rather than as prose.

    Run both modules, capture their RED output, and append `## TPL-02 / OUT-06` and
    `## BLD-06 / OUT-04` sections to `54-01-RED-EVIDENCE.md` in the same shape as Task 1's: verbatim
    tail, exact command, commit SHA, and one sentence naming why it is red on this tree (no
    per-key bundle directory is produced at all today). Do not add `xfail` markers yet.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; uv run pytest tests/test_two_key_selection_gate.py tests/test_bundle_copy_exclusion_manifest_gate.py 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_two_key_selection_gate.py tests/test_bundle_copy_exclusion_manifest_gate.py` reports at least one FAILED test in each module and zero ERRORS.
    - `tests/fixtures/two_key_selection_gate/conf.py` contains all three of the literals `"report"`, `"memo"`, and `typst_document_templates`.
    - `grep -c '"report"' tests/fixtures/two_key_selection_gate/conf.py` is at least 3 (registry key plus the two entries that share it).
    - `tests/test_two_key_selection_gate.py` contains an assertion comparing the root wrapper's import string to the nested wrapper's import string for equality (not two independent literal comparisons).
    - `tests/test_bundle_copy_exclusion_manifest_gate.py` contains all four literals `.git`, `.DS_Store`, `Thumbs.db`, and `~`.
    - `tests/test_bundle_copy_exclusion_manifest_gate.py` contains an `==` comparison against a set literal containing exactly `"base.typ"` and `"assets/note.txt"`.
    - `find tests/fixtures/bundle_exclusion_manifest_gate -name '.git' -o -name '.DS_Store' -o -name 'Thumbs.db'` returns nothing (the excluded kinds are materialised at runtime, not committed).
    - `54-01-RED-EVIDENCE.md` contains the headings `## OUT-05`, `## TPL-02 / OUT-06`, and `## BLD-06 / OUT-04`.
  </acceptance_criteria>
  <done>Both remaining gates exist as real builds, fail on the pre-relocation tree, and their failures are recorded verbatim alongside OUT-05's.</done>
</task>

<task type="auto">
  <name>Task 3: Mark the three gates xfail so wave 1 closes green, and hand the removal to 54-04</name>
  <files>tests/test_user_template_relative_asset_gate.py, tests/test_two_key_selection_gate.py, tests/test_bundle_copy_exclusion_manifest_gate.py, .planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-01-RED-EVIDENCE.md</files>
  <read_first>
    - The three test modules created in Tasks 1 and 2.
    - `.planning/ROADMAP.md` § v0.9.0 binding constraint #2 ("the tree must be green at every phase
      boundary") and #6 (the RED-first bar) — this task is what lets both hold at once.
    - `pyproject.toml` `[tool.pytest.ini_options]` — confirm no `xfail_strict = true` is configured
      globally; if it is, the marker must carry `strict=False` explicitly (it does below regardless).
  </read_first>
  <action>
    Add a class-level (or module-level, whichever matches each module's structure)
    `@pytest.mark.xfail(strict=False, reason="...")` to every test class in the three new modules.
    The reason string must, in each case, name the plan that removes it, in this exact form:
    `"Phase 54: green only once the per-key bundle relocation lands in 54-04; RED recorded in 54-01-RED-EVIDENCE.md"`.

    `strict=False` is deliberate and load-bearing: once `54-04` lands the bundle relocation but
    before its own task removes these markers, the tests XPASS, and a strict marker would turn an
    XPASS into a failure — inverting the gate exactly when it starts working.

    Append a final `## Handover` section to `54-01-RED-EVIDENCE.md` listing the three module paths
    and stating that `54-04` Task 3 removes the markers and proves green, so the marker cannot be
    forgotten in the tree.

    Then run the full suite and the three CI-matching gates to prove the wave closes green.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; uv run pytest tests/ -q 2>&amp;1 | tail -15 &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` reports 0 failed and 0 errors, and reports a non-zero xfailed count.
    - Each of the three new modules contains the literal `xfail(strict=False` at least once.
    - Each of the three new modules contains the literal `54-04` inside its xfail reason string.
    - `uv run black --check .` exits 0.
    - `uv run ruff check .` exits 0.
    - `uv run mypy typsphinx/` exits 0.
    - `54-01-RED-EVIDENCE.md` contains a `## Handover` heading naming all three module paths.
  </acceptance_criteria>
  <done>The full suite is green with three recorded xfails; the RED evidence names the plan obliged to turn them into real passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user `conf.py` / template bundle directory → published build output | Untrusted, author-controlled file content and directory shape crosses into a tree the project publishes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-02 | Information Disclosure | bundle copy — VCS/OS metadata reaching published output | medium | mitigate | This plan builds the manifest-diff gate that makes the exclusion set falsifiable (equality against `{base.typ, assets/note.txt}`), rather than a presence-only test that cannot see an extra file |
| T-54-03 | Information Disclosure | bundle copy — other sensitive files (`.env`, key material) in a user's bundle | medium | accept | Owner-recorded residual (D-04: exactly the four kinds the roadmap names, nothing more). Named in `54-RESEARCH.md` § Security Domain and carried to the shipped documentation in Phase 56 as "the bundle is copied wholesale minus four specific exclusions; keep it clean" |
| T-54-11 | Tampering | test fixtures materialising a `.git` directory | low | mitigate | The excluded kinds are created at runtime under `tmp_path`, never committed — a tracked nested `.git` directory would be unreadable to this repository and could shadow tooling that walks the tree |
| T-54-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs zero packages; `54-RESEARCH.md` § Package Legitimacy Audit records that the gate has nothing to check and `pyproject.toml`'s `dependencies` is unchanged |
</threat_model>

<verification>
- `uv run pytest tests/ -q` → 0 failed, 0 errors, 3 modules' worth of xfails.
- `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0.
- `54-01-RED-EVIDENCE.md` exists and carries four headings: `## OUT-05`, `## TPL-02 / OUT-06`,
  `## BLD-06 / OUT-04`, `## Handover`.
- `git status --porcelain` shows only the files named in `files_modified`.
</verification>

<success_criteria>
- Three real `sphinx-build` fixtures exist, none of which relies on the built-in template as
  evidence for OUT-05.
- Each of the three is recorded RED against a named commit of the pre-relocation tree.
- The full suite, black, ruff and mypy are all green at the end of the plan.
- No production source file under `typsphinx/` was modified.
</success_criteria>

<output>
Create `.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-01-SUMMARY.md` when done
</output>
