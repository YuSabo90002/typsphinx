---
phase: 37-signature-typography-the-desc-family
plan: 07
type: execute
wave: 4
depends_on: ["37-02", "37-04", "37-06"]
files_modified:
  - typsphinx/translator.py
autonomous: true
requirements: [SIG-05, SIG-06]

must_haves:
  truths:
    - "Every parameter-list delimiter -- the two parentheses, the parameter separator, and the optional-group brackets -- emits through the monospace primitive, so the whole signature reads as one typeface (SIG-05)."
    - "The concatenation and has-content bookkeeping around every delimiter is byte-unchanged: only the call name changes, so a delimiter swap cannot alter where separators land."
    - "The optional-group separator lands INSIDE the closing bracket, matching Sphinx's own HTML writer as measured this session -- and it is guarded on the optional GROUP's own following sibling, not on its last child (D-11)."
    - "The nested-optional rendering is unchanged by the D-11 fix, because both of its optional groups are last children -- this is the labelled control, not a case to be 'fixed'."
    - "The closing bracket and the following parameter stay explicitly concatenation-joined; measured, the current tree already does this, so the assertion is a non-regression guard rather than a fix (contract section 6.2 corrects CONTEXT.md D-11's second half)."
    - "desc_returns emits a real arrow glyph that survives into the compiled PDF's extracted text, with no ASCII two-character arrow remaining anywhere in signature output (SIG-06)."
    - "D-11 is proven by its own criterion and its own fixture constructs, never inside a SIG-05 assertion."
    - "golden.typ's byte-identity gate turns green against the expectations hand-derived in Wave 1 -- and if it does not, the divergence is investigated and reconciled by re-derivation, never by regenerating the golden from the new output."
  artifacts:
    - typsphinx/translator.py
  key_links:
    - "the five delimiter sites -> 37-EMISSION-CONTRACT.md section 6 table (call name changes only; + and has-content bookkeeping unchanged)"
    - "depart_desc_optional's new guarded emission -> the existing following-sibling test in depart_desc_parameter, applied to the optional group node itself"
    - "visit_desc_returns -> 37-EMISSION-CONTRACT.md section 7 (the exact three-expression form that was compiled and pypdf-verified)"
    - "golden.typ byte-identity -> 37-EMISSION-CONTRACT.md section 9 and the Wave 1 hand-derivation recorded in 37-GATE-EVIDENCE-04.md"
---

<objective>
Finish the signature's interior: swap every parameter-list delimiter to the monospace primitive, place
the optional-group separator inside its bracket where Sphinx's own HTML writer puts it, and replace the
ASCII return arrow with a real glyph.

Purpose: these are the last three emission changes the phase needs. After this plan every Wave 1 gate
is green, including Phase 36's `golden.typ` byte-identity gate against the expectations hand-derived
before any of this code existed — which is the phase's proof that the assertions were derived, not
fitted.

Output: one `typsphinx/translator.py` commit series. No test changes.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md
@.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md
@.planning/phases/37-signature-typography-the-desc-family/37-RESEARCH.md
@.planning/phases/37-signature-typography-the-desc-family/37-PATTERNS.md
@.planning/phases/37-signature-typography-the-desc-family/37-06-SUMMARY.md
@.planning/phases/37-signature-typography-the-desc-family/37-04-SUMMARY.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run`.
</environment_setup>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates no
new symbol — it changes emitted literals inside five existing handlers and adds one guarded emission
to `depart_desc_optional`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Swap every parameter-list delimiter to the monospace primitive</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §6 (the five-site table)
    - `.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md` D-01's treatment table (delimiters row) and D-03 (per-sub-part assertions)
    - `typsphinx/translator.py:4918-4984` (`visit_desc_parameterlist`, `depart_desc_parameterlist`, `visit_desc_parameter`, `depart_desc_parameter`, `visit_desc_optional`, `depart_desc_optional` — the whole concatenation machinery)
    - `typsphinx/translator.py:949-976` (`_emit_inline_concat_separator` / `_mark_inline_concat_content`, which drive where the separators land)
    - `typsphinx/translator.py:1282-1360` (`visit_literal` — the monospace primitive's proven form)
  </read_first>
  <files>typsphinx/translator.py</files>
  <action>
Apply contract §6's table: at each of the five delimiter emission sites, change ONLY the call name from
the proportional primitive to the monospace primitive. The delimiter characters themselves, the
surrounding concatenation operators, the trailing-space formatting in the opening-parenthesis site, and
every `_desc_parameter_has_content` assignment stay exactly as they are.

These delimiters are hardcoded ASCII carrying no user-supplied text, so no escaping call is added at
these sites — that is deliberate and must be stated in a comment, so a later reader does not read the
absence of an escaping call as an oversight. Every site that DOES carry user text (names, types,
defaults) already routes through the shared escaping helper via the monospace branch added in plan
37-06.

Add one docstring line to `visit_desc_parameterlist` recording that SIG-05's requirement is satisfied
by these five sites plus the operator and punctuation nodes, which reach monospace through the
signature flag rather than through a dedicated handler.

Touch nothing else in this family in this task — the optional-group separator is Task 2, deliberately
kept in a separate commit so the delimiter swap and the separator placement are independently
attributable in the history.
  </action>
  <acceptance_criteria>
    - SIG-05's assertions in `tests/test_signature_typography_gate.py` flip FAILED to PASSED: each of
      the five delimiter sites emits through the monospace primitive, and the empty-parameter-list
      signature emits its two delimiters with no separator between them.
    - The nested-optional reverse-close-order assertion PASSES.
    - `git diff typsphinx/translator.py` for this task shows only call-name changes inside the five
      sites — no change to any concatenation operator, to any `_desc_parameter_has_content` assignment,
      or to the trailing-space formatting of the opening-parenthesis emission.
    - `uv run pytest tests/test_desc_signature_concat_render_gate.py -v` PASSES — the Wave 1 migrated
      ordering assertions now match.
    - `uv run python -m sphinx -b typst tests/fixtures/signature_typography_gate <tmp>` exits 0 and the
      emitted `index.typ` compiles through `typst.compile()` without error.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_signature_typography_gate.py tests/test_desc_signature_concat_render_gate.py -v</automated>
  </verify>
  <done>All five delimiter sites emit monospace with their concatenation bookkeeping byte-unchanged, SIG-05 is green, and the deliberate absence of an escaping call at these hardcoded-ASCII sites is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Place the optional-group separator inside its closing bracket (D-11)</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §6.1 (the target/control table, both rows measured against Sphinx's own HTML writer) and §6.2 (the correction to CONTEXT.md's second half)
    - `.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md` D-11 in full
    - `.planning/phases/37-signature-typography-the-desc-family/37-RESEARCH.md` § "Code Examples" (the illustrative `depart_desc_optional` shape)
    - `typsphinx/translator.py:4953-4984` (`depart_desc_parameter`'s existing following-sibling test — the idiom to mirror — and `visit_desc_optional`/`depart_desc_optional`)
    - `.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-02.md` (the D-11 RED capture and the measured target versus current renderings)
  </read_first>
  <files>typsphinx/translator.py</files>
  <action>
Add one guarded emission to `depart_desc_optional`, immediately BEFORE the closing-bracket emission:
when the optional-group node itself has a following sibling, emit the same parameter separator
`depart_desc_parameter` emits, through the monospace primitive. The closing bracket and the
has-content assignment follow unchanged.

Two things about this guard are load-bearing and must be commented:

1. **The sibling test is against the `desc_optional` node itself**, mirroring what
   `depart_desc_parameter` already does for a parameter's own following sibling — NOT against
   `desc_optional`'s last child. The last parameter inside the group has no following sibling, which is
   exactly why the separator that Sphinx emits (because the *group* has one) is currently lost.
2. **The separator goes inside the bracket, not after it.** Measured this session against Sphinx's own
   HTML writer: the comma belongs before the closing bracket. Contract §6.1 records both the measured
   HTML target and the measured pre-phase typsphinx rendering side by side.

Record in the docstring that the nested-optional case is UNCHANGED by this guard because both of its
optional groups are last children, and that this is the fix's non-regression control — so nobody later
"extends" the fix to cover it.

Also record contract §6.2's correction in the docstring or the SUMMARY: CONTEXT.md D-11 describes a
second, cosmetic half — a missing concatenation operator between the closing bracket and the following
parameter — that does NOT reproduce on the current tree. That obligation is discharged as a
non-regression assertion (already written in Wave 1), and no code change is required for it. Do not go
looking for a defect that is not there, and do not remove the assertion that pins the behaviour.
  </action>
  <acceptance_criteria>
    - The D-11 placement assertion in `tests/test_signature_break_and_arrow_gate.py` flips FAILED to
      PASSED: the separator's index falls between the last optional parameter's index and the closing
      bracket's index.
    - The D-11 compiled-PDF assertion flips FAILED to PASSED: the extracted text (U+200B stripped)
      contains the target rendering from contract §6.1 and not the pre-phase one.
    - The D-11 nested-optional CONTROL assertion still PASSES, unchanged.
    - The D-11 explicit-concatenation non-regression assertion still PASSES.
    - `uv run pytest tests/test_pdf_render_gate.py -k desc_signature -v` PASSES — the existing
      nested-bracket rendering is unchanged, per contract §6.1's control row.
    - `git diff typsphinx/translator.py` for this task touches only `depart_desc_optional` and its
      docstring.
    - No D-11 assertion was added to, or moved into, a SIG-05 test function.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_signature_break_and_arrow_gate.py tests/test_pdf_render_gate.py -k "desc_signature or D11 or optional" -v</automated>
  </verify>
  <done>The optional-group separator lands inside its closing bracket when the group has a following sibling, the nested-optional rendering is provably unchanged, and the discharged cosmetic half is recorded rather than chased.</done>
</task>

<task type="auto">
  <name>Task 3: Emit a real return-arrow glyph and confirm the whole Wave 1 gate set is green</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md` §7 (the exact three-expression form that was compiled and extraction-verified), §4.2 (the extraction hazard), §9 (the golden derivation)
    - `.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md` D-13
    - `typsphinx/translator.py:4810-4827` (`visit_desc_returns` / `depart_desc_returns` — one literal changes, the separator bookkeeping does not)
    - `.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-04.md` (the Wave 1 hand-derivation of `golden.typ`, which this task's build must now match)
    - `tests/test_desc_rubric_decoupling_render_gate.py` (the byte-identity gate)
  </read_first>
  <files>typsphinx/translator.py</files>
  <action>
Replace `visit_desc_returns`'s single emitted literal with contract §7's three-expression monospace
form carrying the Unicode escape for the rightwards arrow. The surrounding list-item separator
bookkeeping is unchanged. Use the three-expression form specifically: that is the shape that was
actually compiled and extraction-verified this session, and the surrounding spaces are part of what was
proven.

Update the docstring: it currently says the handler emits a literal ASCII arrow; replace that sentence
with the glyph rationale (SIG-06 / D-13), keeping the existing note that resolved return-type
cross-references already stream through the unmodified reference handler.

Then confirm the whole Wave 1 gate set is green and reconcile `golden.typ`:

- run every Wave 1 gate module plus the migrated pre-existing modules;
- run `uv run pytest tests/test_desc_rubric_decoupling_render_gate.py -v`. Its byte-identity assertion
  compares a fresh build against the golden hand-derived in Wave 1, before any of this code existed. If
  it passes, that agreement IS the phase's evidence that the expectations were derived rather than
  fitted — record the fact prominently in the SUMMARY.
- **If it does NOT pass:** do not regenerate the golden. That is forbidden by ROADMAP SC#5 and
  milestone invariant #4 and would void the phase's evidence. Instead: capture the diff, decide for
  each differing byte whether the emission contract or the Wave 1 derivation was wrong, fix the side
  that is wrong, and record in the SUMMARY every byte that differed with the reason and which side was
  corrected. A contract error is a legitimate finding; a silently regenerated golden is not.
- run `uv run pytest -m "not slow" -q` and compare to the Wave 3 baseline by node-id set difference;
  every remaining intentional RED from Wave 1 must now be green, and nothing must have flipped in the
  unexpected direction;
- run the lint/type trio.
  </action>
  <acceptance_criteria>
    - The SIG-06 compiled-PDF assertion in `tests/test_signature_break_and_arrow_gate.py` flips FAILED
      to PASSED: the arrow glyph is present in the extracted text (U+200B stripped) and the ASCII
      two-character arrow is absent from the whole extracted text.
    - The migrated arrow assertion in
      `tests/test_pdf_render_gate.py::test_desc_signature_pdf_has_arrow_linebreak_brackets_and_inline`
      PASSES, along with that function's DESC-02, DESC-03 and DESC-04 legs.
    - `uv run pytest tests/test_desc_rubric_decoupling_render_gate.py -v` PASSES both the byte-identity
      and the delegation assertions; if any byte required reconciliation, the SUMMARY lists each one
      with the reason and states which side was corrected.
    - `uv run pytest -m "not slow" -q` reports zero failures, and the SUMMARY shows the node-id set
      difference against the Wave 3 baseline with nothing flipped in the unexpected direction.
    - `git diff typsphinx/translator.py` for this task touches only `visit_desc_returns` and its
      docstring.
    - `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
    - `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` is unmodified by this plan
      (`git diff --numstat` reports it absent).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -m "not slow" -q &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <done>The return arrow is a real glyph in the compiled PDF, every Wave 1 gate is green including the byte-identity gate against the pre-derived golden, and the whole suite passes with the delta verified by node-id set difference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.rst` / docstring source → translator | Author-controlled build-time text |
| translator → emitted `.typ` string literal | Where unescaped text would become Typst syntax |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | Typst string-literal injection via unescaped signature text | medium | mitigate | Every literal this plan emits — the five delimiters, the optional-group separator, the arrow escape — is hardcoded ASCII or a fixed Unicode escape carrying no user-supplied text. Sites carrying user text were already routed through the unmodified shared escaping helper in plan 37-06. No new escaping code. |
| T-37-09 | Tampering | An unbalanced optional-group bracket aborting the entire Typst compile rather than failing one assertion | medium | mitigate | The new emission is added before the existing closing bracket and changes no bracket count; pinned by the existing nested-optional compiled-PDF gate, which compiles without `try`/`except` so a mismatch aborts loudly. |
| T-37-04 | Repudiation | Reconciling a `golden.typ` mismatch by regenerating the file rather than re-deriving the expectation | high | mitigate | Task 3 makes regeneration an explicit prohibition, requires a per-byte reconciliation record naming which side was corrected, and asserts the golden file is unmodified by this plan. |
| T-37-02 | Information disclosure | none — no new network, file, subprocess, or dependency surface | low | accept | No new package, no font configuration, no new `@preview` import. |

ASVS L1: V5 Input Validation only, already mitigated project-wide by `escape_typst_string`. No
high-severity threat in the code itself; T-37-04 is the milestone's evidence-integrity threat and is
mitigated procedurally plus by a mechanical unmodified-file assertion. No package installs, so the
package-legitimacy gate does not apply.
</threat_model>

<verification>
- SIG-05, SIG-06 and D-11 assertions all flip red-to-green; the nested-optional control and the
  explicit-concatenation non-regression stay green.
- `tests/test_desc_rubric_decoupling_render_gate.py` passes byte-identity against the Wave 1
  hand-derived golden, with the golden file itself unmodified by this plan.
- `uv run pytest -m "not slow" -q` is fully green; the delta is verified by node-id set difference.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
</verification>

<success_criteria>
- All five delimiters monospace, with concatenation bookkeeping byte-unchanged.
- The optional-group separator inside its bracket, guarded on the group's own following sibling, with
  the nested-optional case provably unchanged.
- A real arrow glyph surviving into the compiled PDF, with no ASCII arrow anywhere in signature output.
- D-11 proven by its own criterion, never inside a SIG-05 assertion.
- The byte-identity gate green against a golden derived before this code existed — and any
  reconciliation recorded byte by byte rather than regenerated.
</success_criteria>

<output>
Create `.planning/phases/37-signature-typography-the-desc-family/37-07-SUMMARY.md` when done.
</output>
