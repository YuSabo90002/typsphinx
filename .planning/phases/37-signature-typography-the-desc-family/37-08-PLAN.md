---
phase: 37-signature-typography-the-desc-family
plan: 08
type: execute
wave: 5
depends_on: ["37-05", "37-06", "37-07"]
files_modified:
  - .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE.md
  - .planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md
  - .planning/phases/37-signature-typography-the-desc-family/37-VALIDATION.md
autonomous: false
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05, SIG-06, SIG-07, SIG-08, SIG-09]

must_haves:
  truths:
    - "Every SIG-01..SIG-09 requirement plus D-11 has a named, currently-green assertion, and every one of them was recorded RED (or, for the controls, deliberately green) against the pre-phase translator before the code existed."
    - "The full-corpus -b typstpdf gate passes on the real Sphinx v9.1.0 doc/ corpus, which is where 1,445 real signatures actually exercise the new emission -- the default -m 'not slow' run never reaches it."
    - "The milestone's standing invariants hold: zero new runtime dependencies, the @preview package count still four with no new version-lockstep site, and no new font selection anywhere in the tree."
    - "The 17 probe-surfaced edges are accounted for: 12 authored into must_haves as truths (one of them a backstop marker) and 5 surfaced as flagged assumptions -- no silent drops."
    - "The four recalled prohibitions are recorded descriptor-less and are dispositioned as flagged-unverified, not silently dismissed."
    - "The owner has looked at a compiled before/after rendering and confirmed the block wrapper introduces no visible artifact beyond the intended spacing -- the one manual-only verification 37-VALIDATION.md names."
  artifacts:
    - .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE.md
    - .planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md
  key_links:
    - "37-GATE-EVIDENCE.md -> the four per-plan evidence files 37-GATE-EVIDENCE-01..04.md, consolidated (the sibling files stay as the primary record)"
    - "the full-corpus gate -> tests/test_corpus_gate.py, @pytest.mark.slow, excluded from the default run and therefore invoked explicitly"
    - "the invariant checks -> tests/test_preview_version_sync.py and the three declaration sites CLAUDE.md names"
---

<objective>
Close the phase: prove every requirement green with its RED provenance, run the gates the default
suite does not reach, verify the milestone's standing invariants, consolidate the four wave-1
evidence files into one record, and take the single manual visual confirmation `37-VALIDATION.md`
identifies as unautomatable.

Purpose: ROADMAP SC#5 asks for the phase's own blast radius to be migrated within the phase and a
census recorded; the milestone's standing practice adds an explicit full-corpus run before verify.
Neither is reachable from inside an implementation plan, because both depend on every wave being done.

Output: `37-GATE-EVIDENCE.md`, a finalised `37-TEST-CENSUS.md`, an updated `37-VALIDATION.md`, and an
owner sign-off.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-signature-typography-the-desc-family/37-EMISSION-CONTRACT.md
@.planning/phases/37-signature-typography-the-desc-family/37-CONTEXT.md
@.planning/phases/37-signature-typography-the-desc-family/37-VALIDATION.md
@.planning/phases/37-signature-typography-the-desc-family/37-07-SUMMARY.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run`.

`tests/test_corpus_gate.py` is `@pytest.mark.slow` and is excluded from the default `-m "not slow"`
run — it must be invoked explicitly. The corpus is cached at
`~/.cache/typsphinx-corpus-gate/sphinx-v9.1.0`.
</environment_setup>

<artifacts_this_phase_produces>
See `37-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan contributes
`37-GATE-EVIDENCE.md` (the consolidated record). The four `37-GATE-EVIDENCE-0N.md` files remain in
place as the primary per-wave record and are not deleted.
</artifacts_this_phase_produces>

<edge_coverage_accounting>
The spec-less probe fallback surfaced 17 applicable edges across SIG-01..SIG-09. No-silent-drop
equality for this phase:

| Disposition | Count | Where authored |
|---|---|---|
| `covered` → plain string in `must_haves.truths` | 11 | `37-01-PLAN.md` (SIG-01 ×2, SIG-02 ×4, SIG-04 ×2, SIG-05 ×3) |
| `backstop` → `{ statement, verification: backstop }` marker in `must_haves.truths` | 1 | `37-01-PLAN.md` (SIG-05 encoding) |
| `unresolved` / `unclassified` → explicit flagged assumption | 5 | `37-01-PLAN.md` (SIG-03), `37-02-PLAN.md` (SIG-06, SIG-08), `37-03-PLAN.md` (SIG-07, SIG-09) |
| **Total** | **17** | matches the probe's `applicable: 17` |

None was auto-dismissed. Task 3 re-verifies this table against the four plans as written and records
the check.

Prohibitions: four recalled, all authored descriptor-less (no `check_*` scalar) in `37-01-PLAN.md`'s
`must_haves.prohibitions` block, so each disposes flagged-unverified. One canon candidate was dropped
with a breadcrumb: Typst string-literal injection via unescaped signature text is canon input
validation — covered by `/gsd-secure-phase` and by the existing `escape_typst_string` reuse; not
minted here.
</edge_coverage_accounting>

<tasks>

<task type="auto">
  <name>Task 1: Run the gates the default suite does not reach, and verify the milestone invariants</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-VALIDATION.md` § "Sampling Rate" (the phase-gate definition)
    - `.planning/ROADMAP.md` § "v0.7.0" binding constraints 1, 3, 4, 5
    - `CLAUDE.md` § "The `@preview` version-sync hazard" (the three lockstep declaration sites)
    - `tests/test_corpus_gate.py` (the slow-marked full-corpus gate and how it is invoked)
    - `tests/test_preview_version_sync.py` (the invariant test over the lockstep sites)
  </read_first>
  <files>.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE.md</files>
  <action>
Run and capture, verbatim:

- `uv run pytest -m "not slow" -q` — must be fully green;
- `uv run pytest tests/test_corpus_gate.py -m slow` — the full-corpus `-b typstpdf` run against the
  cached Sphinx v9.1.0 `doc/` corpus. This is where 1,445 real signatures exercise the new emission,
  and it is the only run that would surface a domain the fixtures missed. Record its duration and
  any warning-count change versus the phase's starting state;
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/`;
- `tox -e docs-pdf` (the project dogfooding its own builders) if it is runnable in this environment;
  if it is not, say so explicitly rather than omitting it.

Then verify the milestone's standing invariants and record each with the command that proved it:

- **Zero new runtime dependencies:** `git diff <phase-start>..HEAD -- pyproject.toml` is empty of
  dependency changes.
- **`@preview` package count still four, no new lockstep site:**
  `uv run pytest tests/test_preview_version_sync.py -v` passes, and a diff of the three declaration
  sites `CLAUDE.md` names shows no change.
- **No new font selection anywhere:** a repo-wide search finds no new font-family selection introduced
  by this phase — D-04 makes the monospace primitive the only mechanism, and STATE.md records that a
  font selection would silently shadow the Japanese build's CJK fallback with neither a warning nor an
  error.
- **No bundled Typst style module:** no new `.typ` file was added under `typsphinx/`.

If the full-corpus gate surfaces new warnings from the `desc_sig_*` family (the
literal-string/literal-number/keyword-type node types that have no dedicated handler and fall through
to the unknown-node path), file a todo rather than adding handlers — RESEARCH scoped that out and no
SIG requirement names it. Record the warning count either way.
  </action>
  <acceptance_criteria>
    - `uv run pytest -m "not slow" -q` reports zero failures and zero errors.
    - `uv run pytest tests/test_corpus_gate.py -m slow` passes, and its result plus duration is quoted
      verbatim in `37-GATE-EVIDENCE.md`.
    - `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` all pass and are
      quoted.
    - `uv run pytest tests/test_preview_version_sync.py -v` passes.
    - `git diff <phase-start>..HEAD -- pyproject.toml` shows no dependency change, and the diff is
      quoted.
    - `git diff --name-only <phase-start>..HEAD` lists no new file under `typsphinx/` with a `.typ`
      extension.
    - The evidence file records the repo-wide font-selection check and its result.
    - If the corpus gate emitted new unknown-node warnings, a todo was filed and its path is recorded;
      if it emitted none, that is stated explicitly.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -m "not slow" -q &amp;&amp; uv run pytest tests/test_corpus_gate.py -m slow &amp;&amp; uv run pytest tests/test_preview_version_sync.py -q &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <done>The full suite, the full-corpus gate, the lint/type trio and the version-sync invariant all pass, and each milestone invariant is recorded with the command that proved it.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate the evidence, finalise the census, and build the requirement verdict table</name>
  <read_first>
    - `.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE-01.md` through `-04.md`
    - `.planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md` (written in Wave 1, now to be finalised against reality)
    - `.planning/ROADMAP.md` § "Phase 37" success criteria 1-5
    - `.planning/phases/36-shared-emission-seam-cleanup/36-04-SUMMARY.md` (the phase-verdict-table precedent)
    - every `37-0N-SUMMARY.md` produced so far
  </read_first>
  <files>.planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE.md, .planning/phases/37-signature-typography-the-desc-family/37-TEST-CENSUS.md</files>
  <action>
Build `37-GATE-EVIDENCE.md` as the phase's consolidated record. It must contain:

- **A requirement verdict table** with one row per SIG-01..SIG-09 plus one for D-11, each naming: the
  test node id that proves it, the commit at which it was recorded RED, the commit at which it turned
  GREEN, and the emission-contract section that specified its expected shape. A requirement with no
  named RED commit is not verified — say so rather than marking it green.
- **A ROADMAP SC#1..SC#5 mapping**, showing which assertions discharge each of the phase's five success
  criteria, and calling out SC#3's wording explicitly: the criterion says the overflow strategy must be
  derived from measurements of actual corpus signatures, and it was — the measurement found the corpus
  does NOT overflow at production width, which is why the RED fixture is synthetic and the corpus case
  is a control. Record that reasoning, because a reader checking SC#3 against a synthetic fixture will
  otherwise read it as a shortcut.
- **The control roster:** every assertion that was green before AND after by design, with the reason
  each exists — the sibling body-less break control, the depth-counter trap, the nested-optional
  rendering, the explicit-concatenation non-regression, the real-corpus width control, the page-count
  non-inflation check, the two rubric assertions, and the anchor gates.
- **Pointers to the four per-wave evidence files** rather than copies of them; those stay as the
  primary record.
- **The reconciliation record** for `golden.typ`, if plan 37-07 needed one.

Finalise `37-TEST-CENSUS.md` against reality: for every row, record whether the predicted outcome
actually happened, and add any file that turned out to be affected but was not predicted. A census
that was right is evidence the blast radius was understood; a census that was wrong in a recorded way
is more useful than one silently corrected.

Update `37-VALIDATION.md`'s "Per-Task Verification Map" table — currently a single TBD placeholder —
with one row per task across the eight plans, and set `nyquist_compliant: true` and
`wave_0_complete: true` in its frontmatter only if the sign-off checklist at the bottom of that file
genuinely passes. If any box does not pass, leave the flag false and record why.
  </action>
  <acceptance_criteria>
    - `37-GATE-EVIDENCE.md` contains a verdict table with 10 rows (SIG-01..SIG-09 plus D-11), each
      carrying a test node id, a RED commit, a GREEN commit, and a contract section.
    - It contains an SC#1..SC#5 mapping, with SC#3's synthetic-fixture reasoning written out.
    - It contains a control roster naming at least the eight controls listed above.
    - `37-TEST-CENSUS.md` records predicted-versus-actual for every row and lists any unpredicted file.
    - `37-VALIDATION.md`'s per-task table has one row per task and no remaining TBD placeholder.
    - `37-VALIDATION.md`'s `nyquist_compliant` and `wave_0_complete` flags match the actual state of its
      own sign-off checklist, with a recorded reason for any flag left false.
    - No `37-VERIFICATION.md` is created — that filename is reserved by the verify stage and is
      overwritten wholesale.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE.md &amp;&amp; test ! -f .planning/phases/37-signature-typography-the-desc-family/37-VERIFICATION.md &amp;&amp; grep -c "SIG-0" .planning/phases/37-signature-typography-the-desc-family/37-GATE-EVIDENCE.md</automated>
  </verify>
  <done>One consolidated evidence file carries the per-requirement RED-to-GREEN provenance, the SC mapping including SC#3's reasoning, and the control roster; the census records predicted-versus-actual; the validation map is populated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner visual confirmation of the signature block's cosmetic defaults</name>
  <files>(no files modified — owner sign-off recorded in 37-08-SUMMARY.md)</files>
  <action>
Halt and present the before/after comparison below to the owner. This is the single manual-only
verification `37-VALIDATION.md` names: RESEARCH.md Assumption A2 records that the block wrapper's
non-spacing cosmetic defaults (inset, fill, stroke) were never re-measured, so no automated assertion
covers "does it look wrong". Do not self-approve and do not proceed on `auto_advance` — record the
owner's verbatim response in the SUMMARY, and if the owner reports a defect, stop and report rather
than fixing it inline.
  </action>
  <what-built>
The full signature restyle: monospace name and qualifier, an italic parameter name, monospace
delimiters, a real return arrow, a hanging indent for long signatures, page keep-together, and exactly
one break between blocks. Every mechanical assertion is green, including the full-corpus gate.

One thing no assertion covers, named in `37-VALIDATION.md` as manual-only and in RESEARCH.md as
Assumption A2: the block wrapper's NON-spacing cosmetic defaults (inset, fill, stroke) were never
re-measured. The automated checks prove the spacing is back to the plain-flow baseline and that the
page count did not inflate, but not that the block looks right.
  </what-built>
  <how-to-verify>
1. Build the phase's own fixture to PDF and open it:
   `uv run python -m sphinx -b typstpdf tests/fixtures/signature_typography_gate /tmp/sig37 && xdg-open /tmp/sig37/index.pdf`
2. Build the same fixture from the phase's starting commit into a second directory and open that too,
   so you are comparing two renderings rather than judging one in isolation.
3. Look for, specifically:
   - any box, rule, fill or unexpected inset around a signature that was not there before;
   - the vertical gap above and below each signature — it should look like the pre-phase gap, not
     larger;
   - the class signature with the nested method: exactly one blank line's worth of separation, not two;
   - the parameter names in italic monospace, distinguishable from the regular-monospace types and
     defaults at reading size;
   - the return arrow as a real arrow glyph, not two ASCII characters;
   - the long synthetic signature in `tests/fixtures/signature_overflow_render_gate/`: it should wrap
     with the continuation line stepped in, and nothing should cross the right margin.
4. Also build `tox -e docs-pdf` (the project's own documentation, which contains real API pages) and
   skim two or three signature-heavy pages — that is the closest thing to a real reader's view.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what looks wrong and on which page.</resume-signal>
  <verify>
    <automated>uv run python -m sphinx -b typstpdf tests/fixtures/signature_typography_gate /tmp/sig37 &amp;&amp; test -s /tmp/sig37/index.pdf</automated>
  </verify>
  <done>Both PDFs are built and opened, the owner has compared them against the six named checks, and their verbatim response is recorded in `37-08-SUMMARY.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached Sphinx `doc/` corpus → builders | Third-party source rendered through the full pipeline in the slow-marked corpus gate |
| translator → emitted `.typ` string literal | Where unescaped text would become Typst syntax |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | Typst string-literal injection via unescaped signature text | medium | mitigate | Discharged by construction in earlier plans (every new emission site reuses `escape_typst_string` unmodified); Task 1's full-corpus run over 1,445 real signatures is the broadest available exercise of that path, and a compile abort there would surface an escaping failure loudly. |
| T-37-10 | Tampering | Supply chain — a new runtime dependency or a new `@preview` package slipping in | high | mitigate | Task 1 verifies zero dependency change against `pyproject.toml`, runs the version-sync invariant test over the three lockstep sites, and asserts no new `.typ` file under `typsphinx/`. No package-manager install occurs anywhere in this phase, so the package-legitimacy gate has nothing to audit. |
| T-37-11 | Denial of service | A font selection silently shadowing the Japanese build's CJK fallback — Typst emits neither a warning nor an error | medium | mitigate | Task 1's repo-wide font-selection check; D-04 makes the monospace primitive the only mechanism and no handler in this phase names a font. |
| T-37-02 | Information disclosure | none — no new network, file, subprocess, or dependency surface | low | accept | Documentation-and-verification plan; all subprocesses are local builds. |

ASVS L1: V5 Input Validation only, already mitigated project-wide. No high-severity threat remains
open — T-37-10 is closed by mechanical checks in Task 1.
</threat_model>

<verification>
- `uv run pytest -m "not slow" -q` green; `uv run pytest tests/test_corpus_gate.py -m slow` green.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` green.
- `uv run pytest tests/test_preview_version_sync.py` green; no dependency change; no new `.typ` under
  `typsphinx/`; no new font selection.
- `37-GATE-EVIDENCE.md` carries a 10-row verdict table with RED and GREEN commits per requirement.
- The owner has approved the visual comparison.
</verification>

<success_criteria>
- Every SIG-01..SIG-09 and D-11 requirement green with recorded RED provenance and a contract section.
- The full-corpus gate green, run explicitly because the default suite excludes it.
- All four milestone invariants verified by command, not by assertion.
- The 17 probe edges and the 4 prohibitions accounted for with no silent drops.
- The one manual-only visual verification taken and signed off by the owner.
</success_criteria>

<output>
Create `.planning/phases/37-signature-typography-the-desc-family/37-08-SUMMARY.md` when done.
</output>
