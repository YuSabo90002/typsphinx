---
phase: 43-table-state-correctness-nested-tables-empty-title-anchors
plan: 05
type: execute
wave: 4
depends_on: ["43-01", "43-02", "43-03", "43-04"]
files_modified:
  - .planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-05.md
  - .planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-06.md
autonomous: true
requirements: [TBL-04, TBL-05, FIG-01, QUA-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every document in the compared corpus that contains no nested table, no nested figure and no empty-titled caption emits byte-identical `.typ` across the phase's whole change, proven by a two-build diff at a named pre-fix SHA and a named post-fix SHA (roadmap SC#4)."
    - "The two builds are proven to have run genuinely different code: each side's `typsphinx.__file__` is recorded and the two paths differ, and a positive control build produces a deliberately NON-empty diff. An empty diff proves nothing without both."
    - "The compared corpus covers all of `docs/source` and every root under `tests/roots`, and — because D-04's stated intent is that figure-bearing existing documents are compared, not only table-bearing ones — it also covers whatever figure-bearing existing fixtures the corpus inventory shows are needed to satisfy that intent."
    - "The production diff between the two named SHAs is isolated to `typsphinx/translator.py` and nothing else under `typsphinx/`."
    - "A COMPLETED GitHub Actions run exists against `gsd/v0.7.1-bug-fix-round` at a tip that contains all four requirements' changes, and every Windows lane's conclusion is recorded by name (roadmap SC#5, second half)."
    - "`pyproject.toml` and `uv.lock` are unmodified across the whole phase — zero new runtime dependencies (milestone invariant #1), and the `@preview` package count is still four with no new version-lockstep site (milestone invariant #5)."
  prohibitions:
    - statement: "MUST NOT report SC#4 byte-invariance from a diff whose two builds are not proven to have run different code. Both `typsphinx.__file__` paths and a deliberately non-empty positive-control diff must be recorded; an empty diff without them is indistinguishable from having built the same code twice, which is the failure mode this method exists to exclude."
      category: transparency
    - statement: "MUST NOT report SC#5 as met from a workflow run that is still in progress, or from a lane list narrower than the one recorded in 43-GATE-EVIDENCE-02.md. Reporting a criterion met on the strength of a plausible artifact rather than a completed measurement is the precise error v0.7.0 made with REL-04."
      category: transparency
  artifacts:
    - path: ".planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-05.md"
      provides: "SC#4 two-build byte-invariance proof with isolation proof and positive control"
    - path: ".planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-06.md"
      provides: "SC#5 completed-CI evidence and the Phase 44 handoff record"
  key_links:
    - from: "pre-fix tree exported by git archive at plan 43-01's RED commit"
      to: "post-fix phase tip after plan 43-04's fix"
      via: "two independent uv-provisioned builds of the same corpus, diffed pairwise"
      pattern: "typsphinx.__file__"
    - from: "origin/gsd/v0.7.1-bug-fix-round"
      to: ".github/workflows/ci.yml lanes"
      via: "gh run view of a completed run, lane conclusions read by name"
      pattern: "windows"
---

<objective>
Discharge the two phase-level roadmap criteria that are not code changes: SC#4 (no collateral change
to existing output, proven by the two-build byte-invariance method) and the second half of SC#5 (a
COMPLETED CI run against the milestone branch, including the Windows lanes).

Purpose: this phase changes the two most-used container handlers in the translator. SC#4 is what
makes "we fixed nesting" distinguishable from "we changed how every table and figure renders". And
v0.7.0 shipped after eight phases with the Windows lanes never having run against the milestone
branch at all — SC#5 exists so that cannot recur.

Output: `43-GATE-EVIDENCE-05.md` (byte-invariance) and `43-GATE-EVIDENCE-06.md` (completed CI plus
the Phase 44 handoff record). No code and no fixture is changed by this plan.
</objective>

<planner_decisions>
Carried forward from plan 43-02, enforced here:

**D-P2 — a RED Windows lane BLOCKS handoff to Phase 44.** The purpose of milestone invariant #5 is
to surface platform breakage while there is still milestone left in which to fix it. Recording a
known-red Windows lane and proceeding anyway would reproduce the v0.7.0 failure one phase later.
Task 2 therefore records every Windows lane's conclusion by name, and its `<done>` is met only when
either all Windows lanes concluded green, or a blocker is explicitly filed and surfaced to the
developer at the phase's human-check.
</planner_decisions>

<evidence_naming_hazard>
Every evidence artifact in this phase is named `43-GATE-EVIDENCE-NN.md`. Do NOT create a file named
`43-VERIFICATION.md` under this phase directory for any reason — that name is reserved by
`/gsd-verify-work`, which overwrites it wholesale, destroying any evidence written there.
</evidence_naming_hazard>

<artifacts_this_phase_produces>
Every symbol Phase 43 creates. Items marked **[this plan]** are created here.

**New `TypstTranslator` attributes**
- `_table_state_stack` (plan 43-01), `_figure_state_stack` (plan 43-03),
  `_figure_has_legend` (plan 43-03), `_table_is_captioned` (plan 43-04)

**New `TypstTranslator` methods**
- `_push_table_state()` / `_pop_table_state()` (plan 43-01)
- `_push_figure_state()` / `_pop_figure_state()` (plan 43-03)
- `visit_legend(node)` / `depart_legend(node)` (plan 43-03)

**New test files**
- `tests/test_nested_table_render_gate.py` (43-01),
  `tests/test_nested_figure_render_gate.py` (43-03),
  `tests/test_table_empty_caption_anchor_render_gate.py` (43-04)

**New fixture directories**
- `tests/fixtures/nested_table_render_gate/` (43-01),
  `tests/fixtures/nested_figure_render_gate/` (43-03),
  `tests/fixtures/table_empty_caption_anchor_render_gate/` (43-04)

**New evidence files**
- `43-GATE-EVIDENCE-01.md` (43-01), `43-GATE-EVIDENCE-02.md` (43-02),
  `43-GATE-EVIDENCE-03.md` (43-03), `43-GATE-EVIDENCE-04.md` (43-04),
  **`43-GATE-EVIDENCE-05.md` [this plan]**, **`43-GATE-EVIDENCE-06.md` [this plan]**

This plan creates no code symbol and modifies no file outside `.planning/`.
</artifacts_this_phase_produces>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-CONTEXT.md
@.planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md
@.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-01-SUMMARY.md
@.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-02-SUMMARY.md
@.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-03-SUMMARY.md
@.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove SC#4 byte-invariance with the two-build method and a positive control</name>
  <files>.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-05.md</files>
  <precondition>plan 43-01's RED commit SHA and plan 43-04's fix commit SHA are recorded in their respective SUMMARY files and both resolve via `git cat-file -e`</precondition>
  <read_first>
    - `.planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md`
      — the method to reproduce verbatim: two named commits, per-side worktree/export provisioning,
      the `typsphinx.__file__` isolation proof, the positive control, and the pathspec-scoped
      production diff
    - `.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-CONTEXT.md`
      D-04 (the corpus widening and why the figure path is now in scope)
    - `43-01-SUMMARY.md`, `43-03-SUMMARY.md`, `43-04-SUMMARY.md` (the RED and fix commit SHAs)
    - `CLAUDE.md` § "Worktree-isolated execution" (each side needs its OWN `uv sync`, or both sides
      resolve `import typsphinx` to the main checkout through the PEP-660 editable finder and the
      diff is empty for the wrong reason)
  </read_first>
  <action>
    **(a) Inventory the corpus first, and record the inventory.** D-04 names "all of `docs/source`
    AND every root under `tests/roots`" and states the intent: figure-bearing existing documents must
    be in the compared corpus, not just table-bearing ones. Measure whether the named corpus actually
    satisfies that intent before building anything. Run, and paste verbatim into
    `43-GATE-EVIDENCE-05.md`: `ls tests/roots/`, a recursive grep of `docs/source` for `figure::`,
    `image::`, `table::`, `list-table::` and `csv-table::`, and the same grep over every root under
    `tests/roots`. For each `figure::` hit, state whether it is a REAL directive or a literal inside
    a `code-block`/`literalinclude` (a literal one produces no figure node and covers nothing).

    If the inventory shows the named corpus contains no real figure-bearing document, WIDEN the
    compared corpus with the existing figure-bearing render-gate fixtures —
    `tests/fixtures/figure_propagated_target_render_gate`,
    `tests/fixtures/figure_target_caption_render_gate` and
    `tests/fixtures/figure_length_render_gate` — and record the widening and its justification
    explicitly in the evidence file as honouring D-04's stated intent rather than its literal path
    list. Do not silently proceed with a corpus that cannot exercise the figure path.

    **(b) Name the two commits.** PRE-FIX: plan 43-01's RED commit (the last commit on this phase's
    history that is an ancestor of every fix and touches nothing under `typsphinx/` — confirm with
    `git show --stat <sha> -- typsphinx/` returning nothing, and with
    `git merge-base --is-ancestor <pre> <post>`). POST-FIX: plan 43-04's fix commit, i.e. the phase
    tip after all three fixes landed. Record both as full 40-hex SHAs with their
    `git log -1 --oneline` lines.

    **(c) Build both sides in isolation.** Export the pre-fix tree with
    `git archive <pre-sha> | tar -x -C <scratch>/pre-fix-tree` (never a plain checkout of the main
    tree). Provision EACH side independently with
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` inside that side's own
    directory. Then, for each side, run
    `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"`
    and paste the output. The pre-fix path must lie INSIDE the exported tree and the two paths must
    DIFFER. If they do not, stop — every diff below would be vacuous.

    Note the pre-fix tree may lack fixtures created later in this phase. For each corpus item,
    confirm availability at BOTH SHAs with `git ls-tree -r --name-only <sha> | grep <path>` and
    record the result, exactly as `42-GATE-EVIDENCE-05.md` § 3 does; drop nothing silently.

    **(d) Build and diff.** Build every corpus item from both sides with
    `uv run python -m sphinx -b typst -q -E <source> <out>` and diff each emitted `.typ` pairwise.
    Paste every build command with its exit status and every `diff` command with its verbatim output.
    Every diff over a document containing no nested table, no nested figure and no empty-titled
    caption must be EMPTY.

    **(e) The positive control is mandatory.** Build `tests/fixtures/nested_table_render_gate` (which
    exists at both SHAs, since plan 43-01's RED commit created it) from both sides and diff. That
    diff MUST be non-empty, and its content must be the outer-table content appearing only on the
    post-fix side. Paste it verbatim and state in the evidence text that this non-empty diff is what
    makes the empty diffs above meaningful rather than vacuous. If it comes back empty, the two sides
    ran the same code — stop and fix the isolation, do not proceed.

    **(f) Isolate the production surface.** Run `git diff --stat <pre>..<post> -- typsphinx/` and
    paste it: only `typsphinx/translator.py` may appear. Then run the unscoped
    `git diff --stat <pre>..<post>` and paste it too, with one sentence explaining why it is larger
    (the phase's own fixtures, tests, evidence and SUMMARY artifacts landed between the two commits) —
    the measurement trap `42-GATE-EVIDENCE-05.md` § 5 names.

    **(g) Milestone invariants.** Record `git diff --stat <pre>..<post> -- pyproject.toml uv.lock`
    (must be empty) and `uv run python -m pytest tests/test_preview_version_sync.py -q` (must exit 0,
    proving the four `@preview` packages still agree across all three lockstep sites).

    Clean up every scratch worktree/export at the end and record `git worktree list` afterwards.
  </action>
  <verify>
    <automated>test -f .planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-05.md &amp;&amp; uv run python -m pytest tests/test_preview_version_sync.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `43-GATE-EVIDENCE-05.md` exists and contains the corpus inventory: the output of `ls tests/roots/`
      and the directive greps over `docs/source` and every root under `tests/roots`, with each
      `figure::` hit classified as a real directive or a literal inside a code block
    - It records TWO different `typsphinx.__file__` paths, and states that the pre-fix one lies
      inside the exported pre-fix tree
    - It records two full 40-hex SHAs, each with its `git log -1 --oneline` line, and an
      ancestry check showing the pre-fix SHA is an ancestor of the post-fix SHA
    - It contains at least one EMPTY `diff` output over a corpus document with no nested table, no
      nested figure and no empty-titled caption
    - It contains a NON-EMPTY positive-control `diff` over `tests/fixtures/nested_table_render_gate`,
      with an explicit sentence stating why that non-emptiness is what makes the empty diffs
      meaningful
    - It contains `git diff --stat <pre>..<post> -- typsphinx/` output naming
      `typsphinx/translator.py` and no other file under `typsphinx/`
    - It records an empty `git diff --stat <pre>..<post> -- pyproject.toml uv.lock`
    - `uv run python -m pytest tests/test_preview_version_sync.py -q` exits 0
    - `git status --porcelain typsphinx/ tests/` is empty (this plan changes no code and no fixture)
  </acceptance_criteria>
  <done>
    SC#4 is discharged: every non-nesting, non-empty-caption document in the widened corpus emits
    byte-identical `.typ` across the phase, the two builds are proven to have run different code by
    two distinct `typsphinx.__file__` paths AND a non-empty positive-control diff, and the production
    surface is isolated to `typsphinx/translator.py`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm a completed CI run on the milestone branch and record the Phase 44 handoff</name>
  <files>.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-06.md</files>
  <precondition>`gh auth status` reports an authenticated account and `gh run list --branch gsd/v0.7.1-bug-fix-round` returns at least the run recorded in 43-GATE-EVIDENCE-02.md</precondition>
  <read_first>
    - `.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-02.md`
      — the lane list recorded at the wave-1 push; this task must confirm completion against exactly
      that lane set, never a narrower one
    - `.github/workflows/ci.yml` — the `strategy.matrix` OS/Python combinations
    - `.planning/ROADMAP.md` § "Phase 43" Success Criterion 5 and the milestone invariants list
  </read_first>
  <action>
    Paste every command and its verbatim output into
    `.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-06.md`.

    1. Push the finished phase tip: `git push origin gsd/v0.7.1-bug-fix-round`. Record the output and
       then `git ls-remote --heads origin | grep gsd/v0.7.1-bug-fix-round`, asserting in the evidence
       text that the remote SHA now equals the local phase tip. This is the tip that carries all four
       requirements' changes; the wave-1 push in plan 43-02 carried only the phase's starting point.
    2. Wait for the run to finish and record it: `gh run list --branch gsd/v0.7.1-bug-fix-round --limit 10`,
       then `gh run view <run-id>` for the newest run against that tip. Record every polling attempt,
       including the in-progress ones, rather than only the final poll. The run's status must be
       `completed` — an in-progress run does not satisfy SC#5.
    3. Record EVERY lane's conclusion by name, cross-checked against the lane list in
       `43-GATE-EVIDENCE-02.md`. Name each Windows lane explicitly with its conclusion. If the two
       lane sets differ, record the difference and explain it rather than reporting against the
       narrower set.
    4. For any lane that did not conclude green, fetch its log with `gh run view <run-id> --log-failed`
       and paste the failing excerpt.
    5. Write the Phase 44 handoff section. Per planner decision D-P2, a red Windows lane blocks the
       handoff: if any Windows lane is not green, state that Phase 44 is BLOCKED, file the failure as
       a todo under `.planning/todos/pending/` with the captured log excerpt, and name the todo file
       in the evidence. If every Windows lane is green, state that the handoff to Phase 44 is clear
       and record the run id and lane conclusions that support it.
    6. Close the phase-level record: list, in one table, each of the six roadmap Success Criteria
       with the evidence file and section that discharges it (SC#1 and SC#4-partial →
       `43-GATE-EVIDENCE-01.md`; SC#5-first-half → `43-GATE-EVIDENCE-02.md`; SC#6 →
       `43-GATE-EVIDENCE-03.md`; SC#2 and SC#3 → `43-GATE-EVIDENCE-04.md`; SC#4 →
       `43-GATE-EVIDENCE-05.md`; SC#5 → this file). Any criterion with no evidence pointer is a gap,
       not an omission — say so explicitly rather than leaving the row blank.

    Do not modify any file outside `.planning/`. Do not merge, rebase or force-push.
  </action>
  <verify>
    <automated>gh run list --branch gsd/v0.7.1-bug-fix-round --limit 5 | grep -q completed &amp;&amp; grep -qi 'windows' .planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-GATE-EVIDENCE-06.md</automated>
    <human-check>Review `43-GATE-EVIDENCE-06.md` § Phase 44 handoff. Confirm the recorded Windows-lane conclusions and approve (or reject) the handoff to Phase 44. Per planner decision D-P2 a red Windows lane blocks the handoff — if you want to proceed anyway, say so explicitly so the override is recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - `43-GATE-EVIDENCE-06.md` exists and records a `git push origin gsd/v0.7.1-bug-fix-round` and a
      post-push `git ls-remote` hit whose SHA is asserted equal to the local phase tip
    - It records a `gh run view` output for a run whose status is `completed`
    - It names every Windows lane explicitly with that lane's conclusion, cross-checked against the
      lane list in `43-GATE-EVIDENCE-02.md`
    - It contains a Phase 44 handoff section stating either CLEAR (all Windows lanes green, run id
      cited) or BLOCKED (with the failing lane named and a todo file under
      `.planning/todos/pending/` named in the evidence)
    - It contains a six-row table mapping each roadmap Success Criterion to the evidence file and
      section that discharges it, with no blank rows
    - `git status --porcelain typsphinx/ tests/` is empty
  </acceptance_criteria>
  <done>
    A completed CI run exists against the milestone branch at a tip containing all four
    requirements' changes, every Windows lane's conclusion is recorded by name, the Phase 44 handoff
    is stated as CLEAR or BLOCKED with supporting evidence, and all six roadmap Success Criteria are
    mapped to their evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exported pre-fix tree → its own `uv` venv | The two-build method depends on each side importing its OWN copy of `typsphinx`; the main venv's PEP-660 editable finder is the boundary that silently breaks it |
| local repository → `origin` (GitHub) | The finished phase tip and its CI history become publicly visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-13 | Repudiation | A vacuous byte-invariance proof — two builds that ran the same code | medium | mitigate | Both sides' `typsphinx.__file__` paths are recorded and must differ, each side is provisioned with its own `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, and a mandatory positive-control build must produce a NON-empty diff. All three are acceptance criteria of Task 1; if the control diff is empty the task stops rather than reporting SC#4 met. |
| T-43-14 | Repudiation | Reporting SC#5 met from an in-progress run or a narrowed lane set | medium | mitigate | Task 2 requires the run's status to read `completed` and cross-checks the lane set against the one independently recorded at the wave-1 push in `43-GATE-EVIDENCE-02.md`; every polling attempt is recorded, not only the final one. |
| T-43-05 | Information Disclosure | The pushed phase tip and its `.planning/` artifacts | low | accept | `.planning/` is already tracked and already public on `main`; this phase adds no secret and no credential — asserted by the acceptance criterion that this plan changes no file outside `.planning/` and by the empty `pyproject.toml`/`uv.lock` diff in Task 1. |
| T-43-SC | Tampering | pip/npm/cargo installs | low | accept | This phase installs no packages. Task 1 records an empty `git diff --stat <pre>..<post> -- pyproject.toml uv.lock` across the whole phase, which is the direct proof. |
</threat_model>

<verification>
- `43-GATE-EVIDENCE-05.md` records: the corpus inventory with each `figure::` hit classified; two
  differing `typsphinx.__file__` paths; two 40-hex SHAs with an ancestry check; at least one empty
  pairwise `.typ` diff; a non-empty positive-control diff with its stated purpose; a pathspec-scoped
  production diff naming only `typsphinx/translator.py`; an empty `pyproject.toml`/`uv.lock` diff.
- `uv run python -m pytest tests/test_preview_version_sync.py -q` exits 0.
- `43-GATE-EVIDENCE-06.md` records: the post-phase push, the `ls-remote` hit with the matching SHA,
  a `completed` run, every Windows lane's conclusion by name, a CLEAR-or-BLOCKED handoff statement,
  and a six-row Success-Criteria-to-evidence table with no blank rows.
- `git status --porcelain typsphinx/ tests/` is empty for both tasks.
</verification>

<success_criteria>
- Roadmap SC#4 is discharged with a non-vacuous two-build proof over a corpus that actually
  exercises both the table and the figure paths.
- Roadmap SC#5 is fully discharged: the branch is on `origin` and a COMPLETED CI run including the
  Windows lanes has run against a tip carrying all four requirements' changes.
- Milestone invariant #1 (zero new runtime dependencies) and the four-`@preview`-package lockstep
  invariant are both proven, not assumed.
- Every one of the six roadmap Success Criteria has a named evidence file and section.
</success_criteria>

<output>
Create `.planning/phases/43-table-state-correctness-nested-tables-empty-title-anchors/43-05-SUMMARY.md` when done.
Carry the Phase 44 handoff verdict (CLEAR or BLOCKED) into the SUMMARY verbatim.
</output>
