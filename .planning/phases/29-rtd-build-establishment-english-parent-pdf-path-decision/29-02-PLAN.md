---
phase: 29-rtd-build-establishment-english-parent-pdf-path-decision
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - .planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md
autonomous: false
requirements: [RTD-01, RTD-04]
user_setup:
  - service: readthedocs
    why: "Hosting the English documentation. RTD project creation, the GitHub connection, Admin Language and Default Version have no .readthedocs.yaml representation — they exist only in RTD's own database via its web UI (REQUIREMENTS.md § Owner-Manual Steps items 1 and 5)"
    dashboard_config:
      - task: "Confirm the project slug `typsphinx` is unclaimed on RTD's import screen BEFORE creating anything. If it is taken, STOP and consult the owner — do not fall back to a second candidate (D-01/D-02)"
        location: "Read the Docs → Add project / Import a repository"
      - task: "Create the English project from the GitHub repository YuSabo90002/typsphinx with slug `typsphinx`, connecting GitHub so pushes to the default branch trigger builds"
        location: "Read the Docs → Add project"
      - task: "Set Language = English in that project's Admin settings (this is what makes RTD emit READTHEDOCS_LANGUAGE=en at build time; nothing in conf.py can set it)"
        location: "RTD project → Admin → Settings → Language"
      - task: "Leave Default Version at `latest`. Do NOT set it to `stable` — `stable` cannot exist until the v0.6.4 tag builds green, and RTD's root redirect targets Default Version even when that version has no build (RTD-04)"
        location: "RTD project → Admin → Advanced settings → Default version"
must_haves:
  truths:
    - statement: "A real HTTP GET of https://typsphinx.readthedocs.io/en/latest/ returns 200 and the response body contains typsphinx's own documentation content (a distinctive phrase from docs/source/index.rst), not an RTD placeholder or a 404 page — the command and its output are recorded verbatim in 29-VERIFICATION.md (SC#1)"
      verification: backstop
    - statement: "A real HTTP GET of the documentation root https://typsphinx.readthedocs.io/ follows redirects to a version that exists and serves real content, and the recorded effective URL names the `latest` version — proving Default Version resolves rather than reading the dashboard setting (SC#4, RTD-04)"
      verification: backstop
    - statement: "The raw build log for that same build has been read, the line that shows HOW typsphinx was installed has been identified in the actual log text, and it is recorded verbatim in 29-VERIFICATION.md alongside the reasoning for why it evidences a checked-out-commit install rather than a PyPI index resolve (SC#1). No grep pattern was pre-committed — the log dictated the wording (RESEARCH Open Question #1 / assumption A3)"
      verification: backstop
    - statement: "The build that served /en/latest/ completed successfully, and the log excerpt recorded includes the build's identity (build URL or id and the commit SHA it built) so a later reader can tell WHICH build the evidence came from"
      verification: backstop
    - "29-VERIFICATION.md records the RTD Default Version as deliberately left at `latest`, and records the `latest` → `stable` flip as an explicit precondition handed to Phase 33 — to be performed by the owner only AFTER the v0.6.4 tag has been pushed and built green"
    - "29-VERIFICATION.md distinguishes, per item, what was machine-verified (a recorded command plus its output) from what is `human_needed` (the RTD web-UI actions themselves) — no web-UI action is asserted as machine-verified"
    - "No repository source file is modified by this plan — its only output is the evidence record under .planning/"
  prohibitions:
    - statement: "The RTD project MUST NOT be created under any slug other than `typsphinx` without stopping to consult the owner. RTD slugs are not self-service changeable and Phase 31 burns this URL into README.md, pyproject.toml's Documentation metadata, and the repository's About → Website field"
      status: required
      verification: "The checkpoint's resume report states the created slug verbatim; if it is not `typsphinx`, execution stops and the owner is consulted before any further task runs"
    - statement: "Default Version MUST NOT be set to `stable` (or to any specific version) in this phase — it stays at `latest` for the entire milestone"
      status: required
      verification: "The recorded root-URL fetch's effective URL names `latest`; the checkpoint's resume report confirms Default Version was left untouched"
    - statement: "No owner-manual web-UI action may be recorded as machine-verified, and no criterion may be marked satisfied without a recorded command-plus-output or a recorded verbatim excerpt. An honest `human_needed` is required where only a human can confirm"
      status: required
      verification: "Human review of 29-VERIFICATION.md: every item carries either a verbatim command + output block or an explicit `human_needed` marker with its reason"
    - statement: "No live-network check introduced here may be committed as a pytest fixture or a repository script — these are one-off commands whose output is recorded (D-15's rationale)"
      status: required
      verification: "`git status --porcelain tests/` is empty for this plan, and no new script file exists under the repository root"
  artifacts:
    - "`.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` — NEW; sections `## SC#1 — /en/latest/ serves real content`, `## SC#1 — install provenance from the raw build log`, `## SC#4 — documentation root resolves`, `## Phase 33 Handoff Precondition`, `## Owner-Manual Steps (human_needed)`"
  key_links:
    - "RTD Admin → Default Version (`latest`) → the root URL's redirect target. Nothing in the repository can influence this; only a real HTTP fetch of the root can prove it resolves (RESEARCH Pitfall 4 — this failure mode has no locally observable symptom)"
    - "RTD Admin → Language (English) → the READTHEDOCS_LANGUAGE value passed to `_resolve_language()` in docs/source/conf.py. The seam landed in Plan 01; this setting is its producer"
    - "`.readthedocs.yaml` python.install → the raw build log's install line → SC#1. The manifest is the cause; the log line is the only evidence. A green build alone proves nothing (REQUIREMENTS.md invariant #7)"
---

<objective>
Stand up the English RTD project (owner-manual), then prove its **outcome** with real HTTP fetches and
a verbatim raw-build-log excerpt — discharging RTD-01's live half and RTD-04 in full, before the PDF
override exists.

Purpose: D-06's whole point. `/en/latest/` and the documentation root must be established and serving
real content BEFORE the unproven PDF step is added, so the phase never passes through a state where
the English site is red and RTD-04 is violated. SC#1's install-provenance check exists because a stale
published wheel shadowing the working tree would produce a perfectly green build serving documentation
for code that is not in this commit.

Output: `29-VERIFICATION.md` carrying, verbatim, the commands and outputs for the two live fetches and
the decisive raw-log excerpt — plus the Phase 33 Default-Version handoff recorded as an explicit
precondition.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VALIDATION.md
@.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See `29-01-PLAN.md` § `<artifacts_this_phase_produces>` for the full phase-level list. This plan's own
new artifact:

- `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md`
  (NEW) — the live-evidence record. Created here; **appended to** by Plans 03/04/05/06. Any later
  writer must append a new `##` section and must never rewrite or reorder existing ones.

No new repository source file, no new test, no new script. Nothing under `typsphinx/`.
</artifacts_this_phase_produces>

<flagged_probe_assumptions>
The 6 unresolved spec-less edge-probe rows are enumerated in full in `29-01-PLAN.md` §
`<flagged_probe_assumptions>` (6 rows == 0 authored into `must_haves` + 6 flagged assumptions, no
silent drops). Two of them bear directly on this plan and are restated here so this plan is
self-contained:

- **RTD-01 / `unclassified`** — UNRESOLVED. The probe could not classify RTD-01. Manual read: its real
  edge is install provenance, addressed by Task 3's raw-log excerpt, not by an edge predicate.
- **RTD-04 / `adjacency`, `empty`, `ordering`** — all three UNRESOLVED. `adjacency` and `ordering` do
  not bite (RTD-04 compares no quantities and produces no sorted output). `empty` is substantively
  adjacent — the single-active-version case (`latest` exists, `stable` does not) is exactly this
  phase's state and exactly why Default Version stays `latest` — and is addressed by this plan's
  root-URL truth. Recorded as unresolved rather than laundered into a probe-derived truth.
</flagged_probe_assumptions>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Owner creates the English RTD project and reports the first build</name>
  <what-built>
    Plan 01 landed `.readthedocs.yaml` (HTML-only) and the `conf.py` language seam on the branch.
    Everything a build needs is in the repository. RTD project creation, the GitHub connection,
    Admin Language and Default Version have **no** `.readthedocs.yaml` representation and no API this
    phase is authorized to script — they exist only in RTD's own database via its web UI. No test in
    this repository can assert that any of these clicks happened; Tasks 2 and 3 verify their
    *outcome* instead.
  </what-built>
  <files>none — owner-manual actions in the Read the Docs dashboard; no repository file is touched by this task</files>
  <action>
    Present the numbered dashboard steps in `<how-to-verify>` to the owner verbatim and BLOCK until they
    reply. Do not attempt to script, automate, or API-call any of them — no `.readthedocs.yaml` key and
    no command represents project creation, the GitHub connection, Admin Language, or Default Version.

    When the owner replies, carry their reported values forward into Tasks 2 and 3 **verbatim**: do not
    normalize the build URL, do not summarize the raw-log excerpt, and do not fill in a value the owner
    left blank — an absent value is an absent value and must be recorded as `human_needed`.

    If the reply is `SLUG TAKEN`, stop the plan immediately: do not run Tasks 2 or 3, and surface the
    stop to the orchestrator for owner consultation. Do not select a second slug candidate (D-02).
  </action>
  <verify>
    <human-check>Confirm the owner's reply contains every field named in `<resume-signal>`, and that the raw-log excerpt is verbatim log text rather than a paraphrase. If any field is missing, ask for it rather than proceeding with a gap.</human-check>
  </verify>
  <done>The owner has reported the slug, Admin Language, Default Version, build URL, build status, built commit and a verbatim raw-log install excerpt — or has reported `SLUG TAKEN` and the plan has stopped.</done>
  <read_first>
    - `.planning/REQUIREMENTS.md` — § "Owner-Manual Steps" items 1 and 5; § "Milestone Invariants" #7 (a green build proves nothing about content)
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md` — D-01, D-02, D-06; § "Specifics" for the 2026-07-25 measurement that `https://typsphinx.readthedocs.io/` returned 404 (slug appeared unclaimed at that time)
    - `.planning/ROADMAP.md` — § "Phase 29" → "Owner-manual dependencies" and the "RTD-04 ownership" paragraph
  </read_first>
  <how-to-verify>
    Perform these in the Read the Docs web UI, in this order:

    1. **Confirm the slug FIRST.** On RTD's "Add project" / import screen, check that the slug
       `typsphinx` is available. It was measured unclaimed on 2026-07-25
       (`https://typsphinx.readthedocs.io/` returned 404), but RTD's import screen is the
       authoritative check.
       **If `typsphinx` is already taken: STOP HERE.** Do not pick a second candidate, do not
       create the project. Report "SLUG TAKEN" and consult the owner. RTD slugs are not
       self-service changeable, and Phase 31 is about to write this URL into `README.md`,
       `pyproject.toml`'s `Documentation` metadata, and the repository's About → Website field.
    2. Create the project from the GitHub repository `YuSabo90002/typsphinx` with slug `typsphinx`,
       connecting GitHub so pushes trigger builds.
    3. Set **Admin → Settings → Language = English**.
    4. Confirm **Admin → Advanced settings → Default version** is `latest` and leave it there. Do
       not set `stable`.
    5. Let the first build run against the branch carrying Plan 01's commits, and wait for it to
       finish.
    6. Open that build's detail page and note its URL (shape:
       `https://app.readthedocs.org/projects/typsphinx/builds/<build-id>/`, or the
       `readthedocs.org` host). Click **View raw** to open the plain-text log.
    7. In the raw log, find the line(s) showing **how `typsphinx` itself was installed** during the
       `python.install` step. Do not search for a pre-decided string — read what the log actually
       says. Copy those lines verbatim.
  </how-to-verify>
  <acceptance_criteria>
    - The resume report states, verbatim: the created project slug; the Admin Language value; the Default Version value; the build-detail URL; the build's final status; and the commit SHA the build built
    - The resume report contains the copied raw-log line(s) covering the `typsphinx` install, pasted verbatim (not paraphrased, not summarized)
    - If the slug `typsphinx` was unavailable, the resume report says `SLUG TAKEN` and **no project was created** — execution stops for owner consultation and Tasks 2/3 do not run
    - Nothing in this task modifies any file in the repository
  </acceptance_criteria>
  <resume-signal>
    Reply with a block containing: `slug=`, `admin_language=`, `default_version=`,
    `build_url=`, `build_status=`, `built_commit=`, and a fenced `raw log excerpt` section holding the
    install line(s) verbatim. If the slug was taken, reply `SLUG TAKEN` plus what the import screen
    showed, and nothing else.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fetch /en/latest/ and the documentation root over real HTTP and record both verbatim</name>
  <files>.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md</files>
  <read_first>
    - `docs/source/index.rst` — read the "Key Features" and "Quick Links" sections to pick a distinctive literal phrase that only typsphinx's own rendered index page can contain (e.g. the `Sphinx to Typst Conversion` feature bullet). The content assertion must key on real project prose, not on the word "typsphinx" appearing in a page title
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VALIDATION.md` — § "Per-Task Verification Map" rows for RTD-01 (live half) and RTD-04; § "Manual-Only Verifications"; § "Scope note" (these fetches are one-off commands, NOT pytest members)
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-CONTEXT.md` — D-06 (why the HTML site must be established first), and the Deferred Ideas entry recording the `latest` → `stable` flip as Phase 33's
    - `.planning/ROADMAP.md` — § "Phase 29" success criteria 1 and 4, for the exact bars being discharged
  </read_first>
  <action>
    Run two one-off live fetches with `curl` and record each command together with its full output,
    verbatim, in `29-VERIFICATION.md`. Create that file if it does not exist, with the heading
    `# Phase 29 — Live Evidence Record` and a one-line note that later plans append sections and must
    never rewrite existing ones.

    Fetch 1 — the English latest docs. GET `https://typsphinx.readthedocs.io/en/latest/` following
    redirects, capturing the body to a temp file and writing the status code, effective URL and
    downloaded size to stdout. Then assert on the captured body that it contains the distinctive
    index-page phrase chosen from `docs/source/index.rst`. Record the command, the status/URL/size
    line, and the matched phrase with a couple of lines of surrounding context.

    Fetch 2 — the documentation root. GET `https://typsphinx.readthedocs.io/` following redirects,
    recording the full redirect chain (status code of each hop and the final effective URL) plus the
    final status code and downloaded size. The bar is that the root lands on a version that exists
    and serves real content: assert the final effective URL contains `latest`, and assert the final
    body contains the same distinctive index-page phrase. Record the whole chain verbatim — the
    redirect target is the entire point of RTD-04 and must be readable from the record, not inferred.

    Write both into `29-VERIFICATION.md` under `## SC#1 — /en/latest/ serves real content` and
    `## SC#4 — documentation root resolves`. Each section carries the exact command, the exact
    output, the UTC timestamp of the fetch, and one sentence stating what the output proves.

    Also add a `## Owner-Manual Steps (human_needed)` section recording, honestly as `human_needed`,
    the four web-UI actions from Task 1 (slug confirmation, project creation + GitHub connection,
    Admin Language = English, Default Version left at `latest`) with the owner's reported values —
    marked explicitly as owner-reported, NOT machine-verified. Do not phrase any of these as though a
    command confirmed them.

    If the network is unavailable in the execution environment, do NOT assert anything: record the
    attempted command, the exact error, and mark both criteria `human_needed` with that reason, then
    stop. An honest abstention is required here — never a passing claim without a recorded fetch.
  </action>
  <verify>
    <automated>curl -sS -o /tmp/p29-en-latest.html -w 'code=%{http_code} url=%{url_effective} size=%{size_download}\n' -L https://typsphinx.readthedocs.io/en/latest/ && curl -sS -o /tmp/p29-root.html -w 'code=%{http_code} url=%{url_effective} size=%{size_download}\n' -L https://typsphinx.readthedocs.io/ && grep -q 'Sphinx to Typst Conversion' /tmp/p29-en-latest.html && grep -q 'Sphinx to Typst Conversion' /tmp/p29-root.html && echo LIVE_FETCH_OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `code=200` for both fetches and prints `LIVE_FETCH_OK`
    - The root fetch's `url=` value contains `latest` — the root resolved to an existing version, not to a `stable` that has no build
    - `29-VERIFICATION.md` exists and contains the headings `## SC#1 — /en/latest/ serves real content`, `## SC#4 — documentation root resolves`, and `## Owner-Manual Steps (human_needed)`
    - `29-VERIFICATION.md` contains the literal string `code=200` at least twice and the literal string `https://typsphinx.readthedocs.io/` — the recorded evidence is the actual command output, not a prose summary
    - `29-VERIFICATION.md`'s owner-manual section contains the literal `human_needed` for each of the four web-UI actions
    - `git status --porcelain typsphinx/ tests/ docs/ pyproject.toml` is empty — this task touched no source file
    - If and only if the network was unavailable: `29-VERIFICATION.md` records the attempted commands and their exact errors, both criteria are marked `human_needed`, and no `code=200` claim appears anywhere
  </acceptance_criteria>
  <done>Both live fetches are recorded verbatim with their commands and outputs, the root's redirect chain shows it landing on `latest` with real content, and the owner-manual actions are recorded as `human_needed`.</done>
</task>

<task type="auto">
  <name>Task 3: Record the install-provenance log excerpt and the Phase 33 Default-Version handoff</name>
  <files>.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-RESEARCH.md` — § "Open Questions" #1 and § "Assumptions Log" A3: the exact `uv sync` wording that distinguishes a local-checkout install from a PyPI resolve is UNCONFIRMED; § "Common Pitfalls" #2 for why `python.install` is the control being evidenced
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VALIDATION.md` — § "Manual-Only Verifications", the SC#1 row: "Do not pre-commit the plan to a grep pattern. Read the first real build's log, identify the actual distinguishing line, record it verbatim"
    - `.planning/REQUIREMENTS.md` — § "Owner-Manual Steps" item 5 (the `stable` flip is Phase 33's, only after the tag); § "Milestone Invariants" #7
    - `.planning/ROADMAP.md` — § "🚧 v0.6.4" → the "RTD-04 ownership" paragraph, which is the authoritative statement of the handoff being recorded
    - `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` — the file being appended to; read it so the append lands after Task 2's sections without disturbing them
  </read_first>
  <action>
    Append two sections to `29-VERIFICATION.md`, without altering anything Task 2 wrote.

    Section `## SC#1 — install provenance from the raw build log`. Record the build's identity
    (build-detail URL, build status, and the commit SHA the build built) exactly as the owner reported
    it in Task 1. Then record the owner-supplied raw-log excerpt verbatim in a fenced block. Then
    state, in one or two sentences, the reading: which specific token in that excerpt evidences that
    `typsphinx` was installed from the checked-out working tree rather than resolved from a PyPI
    index, and what the contrasting PyPI-resolve output would have looked like instead. **Name the
    actual token found in the log** — do not assert a token that is not present in the recorded
    excerpt, and do not describe this criterion as satisfied by a grep pattern decided before the
    build existed.

    Attempt one strengthening step, and record whichever way it goes: try to fetch the build's public
    raw log over HTTP from the build-detail URL the owner reported. If the log text is retrievable,
    record the fetch command and then record a full-log scan for the strings `latexmk`, `pdflatex`
    and `.tex` — with its count output — as an early, recorded observation for Plan 04's SC#2 read
    (Plan 04 owns the verdict; this is a pre-observation, and this build has no PDF step at all yet,
    so the expected count is zero). If the raw log is not retrievable over HTTP, record that plainly
    and note that the excerpt rests on the owner's verbatim paste — which is exactly what D-07
    requires — rather than inventing a machine path.

    Section `## Phase 33 Handoff Precondition`. Record: RTD Default Version is deliberately at
    `latest` for the whole of v0.6.4; `stable` cannot exist until the `v0.6.4` tag builds green,
    because RTD has refused builds without a `.readthedocs.yaml` since 2023-09-25 and no earlier tag
    contains one; therefore the `latest` → `stable` flip is an owner-manual step handed to **Phase
    33**, to be performed only AFTER the tag is pushed and built green; and that Phases 30, 31 and 32
    each re-fetch the documentation root as part of their own verification, so RTD-04 stands as a
    standing invariant rather than a one-time check. Mark the flip itself as not-yet-done and
    `human_needed`.

    If the owner's Task 1 report did not include a usable raw-log excerpt, do NOT write a provenance
    claim: record the gap as `human_needed` with the reason, and say plainly that SC#1's
    install-provenance half is unmet pending that excerpt.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import pathlib,sys;p=pathlib.Path('.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md');t=p.read_text(encoding='utf-8');need=['## SC#1 — /en/latest/ serves real content','## SC#4 — documentation root resolves','## Owner-Manual Steps (human_needed)','## SC#1 — install provenance from the raw build log','## Phase 33 Handoff Precondition','build_url','built_commit','latest','stable'];miss=[s for s in need if s not in t];print('MISSING:',miss);sys.exit(1 if miss else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `MISSING: []` and exits 0
    - `29-VERIFICATION.md` § "SC#1 — install provenance from the raw build log" contains a fenced block holding the owner's raw-log excerpt, and the prose immediately after it names a token that actually appears inside that fenced block
    - The same section records the build-detail URL, the build status, and the built commit SHA
    - Either the raw-log HTTP fetch command and its output are recorded, or the section states plainly that the log was not retrievable over HTTP and the excerpt rests on the owner's verbatim paste — one of the two, never neither
    - `29-VERIFICATION.md` § "Phase 33 Handoff Precondition" contains the literal strings `latest`, `stable`, `Phase 33`, and `human_needed`, and states the flip has NOT been performed
    - Task 2's sections are byte-unchanged: `git diff -- .planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-VERIFICATION.md` shows only added lines for this task (no `-` lines other than at the file's former end-of-file boundary)
    - `git status --porcelain typsphinx/ tests/ docs/ pyproject.toml .readthedocs.yaml` is empty
  </acceptance_criteria>
  <done>The install-provenance excerpt is recorded verbatim with its build identity and an honest reading naming a token actually present in it, and the Phase 33 Default-Version handoff is recorded as an explicit un-done precondition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RTD web UI (owner session) → RTD project database | Owner-manual configuration crossing into a third-party system this repository cannot assert against |
| PyPI index → RTD build environment | Where a stale published wheel can shadow the working tree. This plan's Task 3 is the evidence step for that boundary |
| RTD build log (public) → this repository's evidence record | Untrusted-in-the-sense-of-unvalidated text is pasted into `.planning/`. It is recorded verbatim as evidence, never executed |

## STRIDE Threat Register

ASVS L1; reused from `29-RESEARCH.md` § "Security Domain". V14 Configuration applies; V2 does not (RTD's
GitHub OAuth connection is owner-manual dashboard work outside this repository's diff).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Spoofing | A stale published PyPI wheel shadowing the working tree, so `/en/latest/` documents code that is not in this commit | high | mitigate | `.readthedocs.yaml` `python.install` (`method: uv`, `command: sync`) installs the workspace-root project from the checkout; Task 3 records the raw-log line that evidences it. The mitigation is verified from the log, not inferred from a green build (REQUIREMENTS.md invariant #7) |
| T-29-08 | Tampering | An irreversible wrong RTD slug baked into README.md, pyproject.toml metadata and About → Website by Phase 31 | high | mitigate | Task 1 confirms slug availability BEFORE creation and stops for owner consultation if `typsphinx` is taken (D-01/D-02). No silent fallback candidate is permitted |
| T-29-09 | Denial of Service | Default Version pointing at a version with no build, breaking the documentation root for the rest of the migration window | medium | mitigate | Default Version stays `latest`; the mitigation is verified by a real HTTP fetch of the root whose redirect chain is recorded, not by reading the dashboard setting (RESEARCH Pitfall 4 — this failure mode has no locally observable symptom) |
| T-29-07 | Information Disclosure | RTD build logs are public for a public project; excerpts are pasted into `.planning/` | low | accept | The commit-1 manifest references no secret and no token, so there is nothing in this build's log to disclose. Standing rule carried into Plan 03: no secret may ever be echoed into a `build.jobs` command |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. Zero new Python/npm/crates packages phase-wide (milestone invariant #1); `29-RESEARCH.md` § "Package Legitimacy Audit" records "no packages to check" |
</threat_model>

<verification>
1. `curl` fetch of `/en/latest/` → `code=200`, body contains the distinctive index-page phrase; command + output recorded verbatim
2. `curl` fetch of the documentation root → redirect chain recorded, final `code=200`, effective URL names `latest`, body contains the same phrase
3. `29-VERIFICATION.md` holds all five required section headings and the owner-reported build identity
4. The install-provenance reading names a token that is actually present in the recorded excerpt
5. `git status --porcelain typsphinx/ tests/ docs/ pyproject.toml .readthedocs.yaml` is empty — this plan changed no source file
</verification>

<success_criteria>
- The English RTD project exists under the slug `typsphinx` with Admin Language = English and Default Version = `latest`, as reported by the owner and recorded as `human_needed`
- `https://typsphinx.readthedocs.io/en/latest/` returns 200 with typsphinx's own documentation content, recorded verbatim (SC#1, serving half)
- The documentation root resolves over real HTTP to an existing version serving real content, with the redirect chain recorded (SC#4 / RTD-04)
- The raw build log's install line is recorded verbatim with an honest reading that names a token present in it (SC#1, provenance half) — with no pre-committed grep pattern
- The `latest` → `stable` flip is recorded as an explicit, not-yet-done precondition handed to Phase 33
- No repository source file changed
</success_criteria>

<output>
Create `.planning/phases/29-rtd-build-establishment-english-parent-pdf-path-decision/29-02-SUMMARY.md` when done.

The SUMMARY must state plainly which criteria are machine-verified (with the command) and which are
`human_needed`, and must not restate an owner-reported web-UI value as though a command confirmed it.
</output>
