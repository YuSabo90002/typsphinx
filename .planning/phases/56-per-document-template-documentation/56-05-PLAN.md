---
phase: 56-per-document-template-documentation
plan: 05
type: execute
wave: 3
depends_on: [56-02, 56-03, 56-04]
files_modified:
  - examples/basic/README.md
  - examples/advanced/README.md
  - examples/charged-ieee/approach2/conf.py
  - CLAUDE.md
  - tests/test_bundle_layout_sweep_gate.py
  - .planning/phases/56-per-document-template-documentation/56-SWEEP-DISPOSITION.md
autonomous: true
requirements: [DOC-15, DOC-16, DOC-17]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC#4 / D-10: the sweep's hit set is the set the grep returns when it is RE-RUN at execution time, not the floor written during discussion — every hit is dispositioned in writing in `56-SWEEP-DISPOSITION.md` with its reason, including the ones deliberately left alone"
    - "SC#4: after this plan, no page in the policed documentation set — `docs/source/`, `README.md`, `examples/` — claims a root-level shared template file as an output artifact; the two runnable-example READMEs describe the per-key bundle directory instead"
    - "D-10: `examples/charged-ieee/approach2/conf.py`'s comment no longer claims that setting the package value skips emitting an output artifact that no longer exists; the file's own `typst_template` value, which legitimately NAMES a user template file whose basename coincides with the former reserved output basename, is left unchanged and its correctness stays proven by the existing charged-ieee gate"
    - "D-09: `CLAUDE.md`'s architecture description no longer names a builder method that was deleted in Phase 54 — measured: a repo-wide grep for that method name over `typsphinx/` and `tests/` returns zero hits — and the surrounding bullet describes the shipped bundle write path instead"
    - "D-09: the policed documentation set is NOT widened by this correction — no test greps `CLAUDE.md`, because it is agent-facing instruction rather than published user documentation, and the 54.1 D-12 scope stays exactly `docs/source/` plus `README.md` plus `examples/`"
    - "The sweep is machine-enforced from here on: a run-time presence gate over the policed set, discovered by `rglob` rather than a written file list, keeps the stale claim from returning — with an explicit exclusion dict naming each excluded path and its measured reason, and a staleness test that fails when an exclusion stops matching"
    - "The sweep pattern is anchored so it cannot be satisfied vacuously by a different filename that merely ends in the same characters — a template named with that suffix as part of a longer basename is not a hit, and the gate carries a teeth self-test proving both the fire and the no-fire cases"
    - "Historical release notes are excluded from the sweep with the reason recorded rather than silently skipped: `docs/source/changelog.rst` describes what was true at the version it documents, and rewriting it would falsify the record"
    - "Phase-boundary green is unconditional: the full pytest suite reports 0 failed, `black --check .`, `ruff check .` and `mypy typsphinx/` are all clean, and `tox -e docs-html` and `tox -e docs-pdf` both succeed — measured after every wave is merged, with no carve-out for any pre-existing failure, because the measured baseline at the phase's start was already 0 failed"
  prohibitions:
    - "No file under `typsphinx/` is modified"
    - "No file under `tests/` is edited as a stale-prose sweep target — `tests/` is deliberately outside the policed set on measured grounds, and its occurrences are either regression assertions that the artifact is gone or fixtures deliberately exercising the reserved-basename edge case"
    - "`docs/source/changelog.rst` and `CHANGELOG.md` are not edited — CHANGELOG curation belongs to the next phase and historical entries stay as shipped"
    - "No test is added that greps `CLAUDE.md`"
    - "`examples/charged-ieee/approach2/conf.py`'s `typst_template` value is not changed"
    - "No new runnable example project is added; `examples/` is in this phase as sweep surface only"
    - "A pre-existing failure is not asserted as an accepted baseline — the measured baseline is zero failures"
  artifacts:
    - path: ".planning/phases/56-per-document-template-documentation/56-SWEEP-DISPOSITION.md"
      provides: "the execution-time re-run of every discovery grep, the full hit set it returned, and a written disposition with a reason for every hit — fixed, excluded, or out of scope"
    - path: "tests/test_bundle_layout_sweep_gate.py"
      provides: "the run-time repo-wide presence gate over the policed set, its anchored pattern, its exclusion dict with per-entry reasons, its staleness test and its teeth self-tests"
      contains: "EXCLUDED_SWEEP_PATHS"
    - path: "examples/basic/README.md"
      provides: "the corrected description of what a build writes for that example"
    - path: "examples/advanced/README.md"
      provides: "the corrected output listing for that example"
    - path: "examples/charged-ieee/approach2/conf.py"
      provides: "a comment that describes the package route's actual consequence without naming a deleted output artifact"
    - path: "CLAUDE.md"
      provides: "an architecture bullet naming the shipped bundle write path instead of a deleted method"
  key_links:
    - from: "the execution-time discovery grep"
      to: "56-SWEEP-DISPOSITION.md"
      via: "a written disposition per hit — the mechanism that stops a written floor from being mistaken for the search set"
    - from: "tests/test_bundle_layout_sweep_gate.py"
      to: "docs/source/, README.md and examples/"
      via: "run-time rglob discovery over the three policed roots, so a page added later is swept without editing the gate"
---

<objective>
Close SC#4: re-run the discovery greps against the merged tree, dispose of every hit in writing,
fix the remainder, and leave a machine-enforced gate behind so the claims cannot come back.

Purpose: this milestone's standing invariant is that an "anywhere under X" criterion is checked by a
repo-wide grep at discovery time, never against the files a requirement happens to name — and the
previous phase's own experience is that a re-grep found a hit the written floor missed. This plan is
that re-grep, run in a tree where every other plan has landed, plus the gate that makes the result
durable.

Output: a disposition record, four corrected files, a new sweep gate, and the phase-boundary
evidence.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-per-document-template-documentation/56-CONTEXT.md
@.planning/phases/56-per-document-template-documentation/56-RESEARCH.md
@.planning/phases/56-per-document-template-documentation/56-PATTERNS.md
@.planning/phases/56-per-document-template-documentation/56-01-SUMMARY.md
@.planning/phases/56-per-document-template-documentation/56-02-SUMMARY.md
@.planning/phases/56-per-document-template-documentation/56-03-SUMMARY.md
@.planning/phases/56-per-document-template-documentation/56-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the discovery greps on the merged tree, dispose of every hit, and fix the example READMEs</name>
  <files>.planning/phases/56-per-document-template-documentation/56-SWEEP-DISPOSITION.md, examples/basic/README.md, examples/advanced/README.md, examples/charged-ieee/approach2/conf.py</files>
  <read_first>
    - `examples/basic/README.md` lines 30-45 — the three-file description of what the build writes
    - `examples/advanced/README.md` lines 55-75 — the bulleted output listing and the paragraph after it
    - `examples/charged-ieee/approach2/conf.py` lines 15-30 — the `typst_documents` entry, the four-line comment block, and the `typst_template` value whose basename coincides with the former reserved output basename
    - `tests/test_examples_charged_ieee_gate.py` — the gate that already proves that approach's template compiles correctly under the bundle layout, so the `typst_template` value must not be touched
    - `.planning/phases/56-per-document-template-documentation/56-RESEARCH.md` § "Repo-wide discovery grep" — the hit set measured at research time, which is a FLOOR to compare against, never the search set
    - `.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-CONTEXT.md` § D-12 and § D-13 — the measured basis for the policed set and the precedent of a re-grep finding a missed hit
  </read_first>
  <action>
<!-- planner-discipline-allow: _template.typ -->
<!-- planner-discipline-allow: accepted and ignored -->
<!-- planner-discipline-allow: _write_template_file -->
Run the discovery greps yourself, on the merged tree, before touching anything. Do not treat the
research document's hit list as the search set — it is a floor to compare your own results against.

Run at minimum these searches across the whole repository excluding `.git/` and `.planning/`, and
record each command and its complete output in the disposition record:
  - the former reserved output basename `_template.typ`;
  - the deleted builder method name `_write_template_file`;
  - the retracted element-[4] phrase `accepted and ignored`;
  - the three removed configuration value names;
  - the source-tree-shaped asset reference form `_typst/refs.bib`.

Write `.planning/phases/56-per-document-template-documentation/56-SWEEP-DISPOSITION.md` containing:
the exact commands run, the date and the commit they were run against, then a table with one row per
hit — file, line, matched text, disposition (`fixed in this phase` / `fixed in plan NN` / `excluded`
/ `not a hit`), and the reason. Compare your hit set against the research floor explicitly and state
whether it found anything the floor missed; if it did, that hit gets a disposition like any other,
never a silent drop. Record the exclusions with their reasons rather than omitting them:
  - historical release notes describe what was true at the version they document;
  - occurrences under `tests/` are outside the policed set on the measured 54.1 D-12 basis, and are
    either regression assertions that the artifact is gone or fixtures deliberately exercising the
    reserved-basename edge case;
  - a longer basename that merely ends in the same characters is a different filename and not a hit;
  - a user's own template file legitimately NAMED with that basename is an input path, not an output
    artifact claim.

Then fix the three example files:
  1. `examples/basic/README.md` — the sentence describing what the build writes currently names three
     files at the build root including a shared template. Rewrite it to name the two root-level files
     that example actually produces plus the template bundle one directory down, matching the
     vocabulary `output_layout.rst` now uses.
  2. `examples/advanced/README.md` — its bulleted output listing has a bullet for a root-level shared
     template. Replace that bullet with one naming the per-key bundle directory and the template file
     inside it, keeping the parenthetical that tells the reader which of their own files it came from.
     Check the rest of the listing against a real build of that example before committing.
  3. `examples/charged-ieee/approach2/conf.py` — the comment block claims that setting the package
     value would skip emitting an output artifact into the output directory. That artifact no longer
     exists, so the claim is about a thing that is gone. Rewrite the comment to state the actual
     consequence: this approach's own template imports the Typst Universe package itself, so setting
     the package value as well would switch typsphinx to the package-only route, no local bundle would
     be copied, and the template this project relies on would never reach the output. Do not change
     the `typst_template` value on the following line.

Verify the two READMEs against reality rather than against each other: build each example with the
`typst` builder into a scratch directory and confirm the file set you just published is the file set
that appears.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest tests/test_examples_charged_ieee_gate.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/56-per-document-template-documentation/56-SWEEP-DISPOSITION.md` exists and contains a row for every hit each recorded command returned, with a non-empty reason column
    - `grep -rnE '(^|[^A-Za-z0-9])_template\.typ' docs/source README.md examples/ | grep -v 'changelog.rst' | grep -v 'approach2/conf.py:.*typst_template = '` returns zero hits
    - `grep -c '_write_template_file' CLAUDE.md` is 0 after Task 2, and is recorded as a hit in the disposition record produced by this task
    - `grep -c 'typst_template = "_typst/_template.typ"' examples/charged-ieee/approach2/conf.py` is 1 — the input filename is unchanged
    - `uv run pytest tests/test_examples_charged_ieee_gate.py -q` exits 0
    - `git diff --stat docs/source/changelog.rst CHANGELOG.md typsphinx/ tests/` is empty for this task's commit
    - `uv run pytest -q` exits 0 with 0 failed
  </acceptance_criteria>
  <reversibility rating="reversible">Prose and comments in four files plus a planning record; every change is a text edit with no runtime effect.</reversibility>
  <done>Every discovery grep has been re-run against the merged tree, its complete output and a per-hit disposition are recorded, and the three example files describe the shipped output layout.</done>
</task>

<task type="auto">
  <name>Task 2: Correct CLAUDE.md's architecture description and land the machine-enforced sweep gate</name>
  <files>CLAUDE.md, tests/test_bundle_layout_sweep_gate.py</files>
  <read_first>
    - `CLAUDE.md` § Architecture — the `builder.py` bullet (line 49) naming a method that no longer exists, and the neighbouring `writer.py` and `template_engine.py` bullets for the voice and level of detail to match
    - `typsphinx/builder.py` lines 1985-2010 and 2145-2180 — the bundle copy that replaced the deleted method, so the corrected bullet names something that exists
    - `tests/test_docs_template_layout_gate.py` — `_discover_policed_files()` and `test_patterns_have_teeth`, the run-time discovery and self-test shapes this gate copies
    - `tests/test_docs_contract_claims_gate.py` lines 140-160 — `EXCLUDED_CLAIM_PAGES`, the exclusion-dict-with-inline-reason shape, and its stale-exclusion test
    - `.planning/phases/56-per-document-template-documentation/56-SWEEP-DISPOSITION.md` — the hit set Task 1 produced, which determines the gate's exclusion entries
  </read_first>
  <action>
<!-- planner-discipline-allow: _write_template_file -->
<!-- planner-discipline-allow: _template.typ -->
Two independent pieces of work that share a wave because neither is large enough to stand alone and
both close the same criterion.

**`CLAUDE.md` (D-09).** The `builder.py` bullet in the Architecture section states that the builder
writes a shared template file once per build, naming the method `_write_template_file`. That method
was deleted in Phase 54 — measured: `grep -rn "_write_template_file" typsphinx/ tests/` returns zero
hits. Rewrite the bullet's second sentence to describe what the builder actually does now: it
accumulates the registry keys used at write time and, in `finish()`, copies each used key's whole
bundle directory to that key's own directory under the reserved output directory. Correct anything
else in that bullet the deletion invalidated, and leave the rest of the Architecture section alone.
Do NOT add a test that greps `CLAUDE.md` — it is agent-facing instruction, not published user
documentation, and the policed set stays exactly what 54.1 D-12 measured it to be. Record that scope
decision in one sentence in the SUMMARY.

**`tests/test_bundle_layout_sweep_gate.py`.** A new never-skipping presence gate — text reads only,
no `typst-py` import and no `sphinx-build` subprocess. Structure:
  - a module docstring citing Phase 56, D-10 and the 54.1 D-12 policed scope, stating in its own words
    why `tests/` is not swept and why historical release notes are excluded;
  - `_discover_policed_files()`: run-time `rglob` over the three policed roots — every `*.rst` and
    `*.md` under `docs/source/`, the top-level `README.md`, and every `*.md`, `*.rst` and `*.py`
    under `examples/` — with roots derived from `Path(__file__).resolve().parent.parent`, never a
    written file list and never an absolute path literal;
  - an ANCHORED pattern for the former reserved output basename, requiring the character before the
    match to be a start-of-line or a non-alphanumeric — so a longer basename ending in the same
    characters is not a hit. State the anchoring requirement in a comment naming why it exists;
  - a second pattern for the deleted method name;
  - `EXCLUDED_SWEEP_PATHS`: a dict mapping a policed path to its written reason, populated from
    Task 1's disposition record. At minimum: the historical release-note page, and
    `examples/charged-ieee/approach2/conf.py` for the line that legitimately names a user template
    file whose basename coincides with the former reserved output basename. Prefer a line-scoped
    exemption over a whole-file one where the file has other lines worth policing, in the spirit of
    the existing same-line exemption rule in `tests/test_docs_template_layout_gate.py`;
  - `TestNoStaleBundleLayoutClaimSurvives`: discovery is non-empty (at least twenty files, so a broken
    root cannot make the gate pass vacuously); every non-excluded policed file is clean under both
    patterns; every exclusion still matches something (the staleness check);
  - `TestSweepPatternsHaveTeeth`: feed each pattern a synthetic string that must fire and a synthetic
    string that must NOT fire — for the anchored pattern, the no-fire case is a longer basename ending
    in the same characters, which is exactly the false positive the anchor exists to prevent.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bundle_layout_sweep_gate.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_bundle_layout_sweep_gate.py -q` exits 0 and collects at least 6 tests, none skipped
    - `grep -c '_write_template_file' CLAUDE.md` is 0
    - `grep -c 'skipif' tests/test_bundle_layout_sweep_gate.py` is 0 and `grep -c 'subprocess' tests/test_bundle_layout_sweep_gate.py` is 0
    - `grep -c 'EXCLUDED_SWEEP_PATHS' tests/test_bundle_layout_sweep_gate.py` is at least 1
    - `grep -rc 'CLAUDE.md' tests/` is 0 — the policed set was not widened
    - `uv run pytest -q` exits 0 with 0 failed
    - `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0
  </acceptance_criteria>
  <done>`CLAUDE.md` describes the shipped bundle write path, and a never-skipping anchored sweep gate with reasoned exclusions, a staleness check and both-direction teeth tests keeps the policed set clean.</done>
</task>

<task type="auto">
  <name>Task 3: Record the phase-boundary evidence</name>
  <files>.planning/phases/56-per-document-template-documentation/56-SWEEP-DISPOSITION.md</files>
  <read_first>
    - `.planning/phases/56-per-document-template-documentation/56-VALIDATION.md` — the sampling contract, the measured baseline (1366 passed, 5 skipped, 0 failed at the phase's start), and the single manual-only verification row
    - `.planning/STATE.md` § "The green bar is UNCONDITIONAL ZERO FAILURES" — the standing instruction that a pre-existing-failure carve-out is a stale note and must not be reintroduced
    - `.planning/ROADMAP.md` lines 466-472 — milestone invariant #11's phase-boundary requirement
    - `CLAUDE.md` § "Worktree-isolated execution" — every command runs after the per-worktree sync and through the project runner
  </read_first>
  <action>
Append a `## Phase-boundary evidence` section to `56-SWEEP-DISPOSITION.md` recording verbatim
transcripts, with the commit each was run against, of:
  - the full pytest suite — record the passed / skipped / failed counts and the wall time, and compare
    them against the phase's measured starting baseline. The failed count must be zero. If any test
    fails, do not attribute it to a pre-existing condition: the baseline at this phase's start was
    measured at zero failures, so re-measure and fix rather than carve out;
  - `black --check .`, `ruff check .` and `mypy typsphinx/`;
  - `tox -e docs-html` and `tox -e docs-pdf`, including their reported warning counts, noting that the
    unknown-node-type autodoc warnings are pre-existing and unrelated to any page this phase edited;
  - the `git diff --stat` of `typsphinx/` against the phase's base commit, which must be empty, since
    this phase changes no production code;
  - a re-run of the sweep gate and the registry documentation gate on their own, so their pass is
    recorded independently of the full suite.

Also record the requirement-to-evidence mapping: for each of DOC-15, DOC-16 and DOC-17, name the
specific test module and test that proves it, so the verification step does not have to rediscover it.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/ && uv run tox -e docs-html && uv run tox -e docs-pdf && git diff --stat typsphinx/</automated>
    <human-check>Open the built `docs/build/html/user_guide/configuration.html` and the `docs-pdf` output, and confirm the registry error table, the key-naming table and the removed-values table each render with every row visible and no text overflowing the page margin in the PDF. This is the one behaviour in this phase that no assertion can express; the build-green half of it is already automated above.</human-check>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exits 0 and its summary line reports `0 failed`
    - `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` each exit 0
    - `uv run tox -e docs-html` and `uv run tox -e docs-pdf` both report `build succeeded`
    - `git diff --stat typsphinx/` against the phase base commit is empty
    - `56-SWEEP-DISPOSITION.md` contains a `## Phase-boundary evidence` section carrying each command's verbatim output and the commit it ran against
    - `56-SWEEP-DISPOSITION.md` names, for each of DOC-15, DOC-16 and DOC-17, at least one test module and test function proving it
  </acceptance_criteria>
  <done>The phase boundary is green on the full suite, the lint and type trio, and both documentation builds, with the transcripts and the requirement-to-evidence mapping recorded against a named commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository file contents → sweep gate process | every file under the three policed roots is read as text by the gate |
| repository layout → gate assertion messages | discovered paths appear in pytest failure output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-13 | Tampering | `tests/test_bundle_layout_sweep_gate.py` reading `examples/**/*.py` | medium | mitigate | policed `.py` files are read as TEXT and matched with `re` only; the module must not `exec`, `eval`, `compile(..., "exec")`, `import_module` or `__import__` any swept file, so an `examples/` `conf.py` can never execute inside the test process |
| T-56-14 | Information Disclosure | path constants and failure messages in the sweep gate | low | mitigate | all three policed roots are derived from `Path(__file__).resolve().parent.parent`; no absolute path literal appears in the module, and failure output prints paths relative to that repository root, so nothing outside the repository is named |
| T-56-15 | Denial of Service | repo-wide `rglob` over three roots | low | accept | the roots are bounded and already walked by two existing gate modules of the same shape at negligible cost; the gate reads text and runs compiled regexes with no backtracking-prone construct |
| T-56-16 | Repudiation | a sweep result asserted rather than measured | medium | mitigate | Task 1 records every command and its complete output against a named commit in `56-SWEEP-DISPOSITION.md`, so the claim "the sweep is clean" is falsifiable by re-running the recorded commands rather than trusted |
| T-56-SC | Tampering | npm/pip/cargo installs | low | accept | this plan installs no package; `56-RESEARCH.md` § "Package Legitimacy Audit" measured that the phase adds no production or dev dependency |
</threat_model>

<verification>
- `uv run pytest -q` — full suite green, 0 failed
- `uv run pytest tests/test_bundle_layout_sweep_gate.py tests/test_registry_documentation_gate.py -q` — green
- `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` — all clean
- `uv run tox -e docs-html && uv run tox -e docs-pdf` — both `build succeeded`
- `git diff --stat typsphinx/` against the phase base — empty
- `56-SWEEP-DISPOSITION.md` carries every grep's complete output and a reasoned disposition per hit
</verification>

<success_criteria>
- Every discovery grep was re-run at execution time and every hit it returned is dispositioned in writing.
- No page in the policed set claims a root-level shared template file, and a run-time anchored gate with reasoned exclusions and teeth tests keeps it that way.
- `CLAUDE.md` describes the shipped architecture without widening the policed set.
- The phase boundary is unconditionally green on the suite, the lint and type trio, and both documentation builds, with the evidence recorded.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan:

- **New test module:** `tests/test_bundle_layout_sweep_gate.py`
- **New test classes:** `TestNoStaleBundleLayoutClaimSurvives`, `TestSweepPatternsHaveTeeth`
- **New module-level constants:** `EXCLUDED_SWEEP_PATHS`, `POLICED_ROOTS`, and the two anchored compiled patterns
- **New helper function:** `_discover_policed_files()`
- **New planning artifact:** `.planning/phases/56-per-document-template-documentation/56-SWEEP-DISPOSITION.md`, with its `## Phase-boundary evidence` section

<output>
Create `.planning/phases/56-per-document-template-documentation/56-05-SUMMARY.md` when done.
</output>
