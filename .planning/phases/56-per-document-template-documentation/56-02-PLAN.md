---
phase: 56-per-document-template-documentation
plan: 02
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - docs/source/user_guide/configuration.rst
  - tests/test_registry_documentation_gate.py
  - tests/test_removed_config_deprecation_gate.py
autonomous: true
requirements: [DOC-15, DOC-17]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "DOC-15 / D-01: `typst_document_templates` is documented in exactly one place — the `Per-Document Templates` subsection of `configuration.rst`'s `Template Configuration` section. No registry walkthrough is added to `templates.rst` and no new `user_guide/` page is created; `user_guide/index.rst` still lists four pages"
    - "DOC-15 / D-04: the central worked example is the `template` route with two masters and no network dependency — one `typst_documents` entry on the reserved `typst` key and one on a declared key resolving to a local `_typst/` file — and the `package` route appears only as a short schema-level example"
    - "DOC-15 / D-07: a `Registry Key Naming Rules` sub-subsection is published BEFORE the error table, states that a key becomes a directory name under `_template/`, and enumerates exactly the seven CONF-18 rejection cases in the fixed order `_validate_registry_key_shape()` checks them — bound to `typsphinx.template_registry._KEY_SHAPE_REJECTION_CASES` by import, never transcribed"
    - "DOC-15 adjacency edge (edge probe, explicit): the naming rules state that a declared key differing from another declared key only by case is refused (CONF-18 case 7), AND that a declared key folding onto the reserved `typst` key is refused before anything is written — so the ordinary-user-key status the registry originally gave `Typst` no longer holds, and the page says the current behaviour rather than the Phase 53 one"
    - "DOC-15 encoding edge (edge probe, explicit): the page states the case comparison is Python `str.casefold()` applied after path-shape normalization and applies NO Unicode normalization, so the NFC and NFD spellings of one character are two different keys; and separately that the `typst_documents` element [4] lookup is exact `str` equality, never case-folded"
    - "DOC-15 empty edge (edge probe, explicit): the page states that an empty or whitespace-only key is refused (case 1), that an absent element [4] resolves to the reserved `typst` key, and that a declared key no `typst_documents` entry names is still validated but its bundle is not copied to the output"
    - "DOC-15 / D-08: the `Custom Template File` subsection carries a preventive statement that a Typst template must not live inside a directory named in Sphinx's `templates_path`, naming `templates_path` on the SAME LINE as any bare `_templates` token so `tests/test_docs_template_layout_gate.py::test_every_surviving_jinja_dir_mention_names_templates_path` stays green"
    - "DOC-17: a `Removed Configuration Values` section publishes guidance for `typst_template_assets`, `typst_authors` and `typst_toctree_defaults`, each naming the replacement (or its absence) and the observable consequence for a `conf.py` that still sets it, bound by a test to `typsphinx.removed_config.REMOVED_CONFIG_VALUES` by import rather than transcription"
    - "DOC-17 ordering edge (edge probe, explicit): a build whose `conf.py` sets all three removed values emits three separate warnings, one per name, in `REMOVED_CONFIG_VALUES` declaration order — `typst_template_assets`, then `typst_authors`, then `typst_toctree_defaults` — proven by a real `sphinx-build` rather than asserted"
    - "DOC-17 adjacency edge (edge probe, explicit): setting two or three removed values produces one warning per name and never a single aggregated warning; the published guidance says so"
    - "DOC-17 encoding edge (edge probe, explicit): detection is exact `str` membership in the raw `conf.py` namespace, so a differently-cased or differently-spelled name is not detected; and presence of the name triggers the warning regardless of the value assigned, including `None` and `[]`"
    - "DOC-17 empty edge (edge probe, explicit): a `conf.py` that sets none of the three emits none of the three warnings — already pinned by `tests/test_removed_config_deprecation_gate.py::TestCleanConfigEmitsNoWarnings` and left in place, not duplicated"
    - "The published guidance does not offer a `suppress_warnings` route: `typsphinx/removed_config.py:92` is a bare `logger.warning(message)` with no `type`/`subtype`, so no such route exists; the page states that `sphinx-build -W` turns the warning into a failure, which is the only lever that does exist"
    - "The page states that the removed-value warning fires for EVERY builder including `-b html`, because `config-inited` runs before `app.builder` exists — it is not typst-builder-specific"
  prohibitions:
    - "No file under `typsphinx/` is modified"
    - "No registry walkthrough is added to `docs/source/user_guide/templates.rst`"
    - "No new page is created under `docs/source/user_guide/` and `user_guide/index.rst` is not edited"
    - "The worked example introduces no Typst Universe package fetch — the `template` route resolves to a local path"
    - "No `@preview` package version is quoted into any documentation page"
    - "No existing five-element `typst_documents` example anywhere in the repository is edited"
    - "The seven CONF-18 case names are not transcribed as string literals in the test; they are imported"
    - "The three removed-value warning strings are not transcribed as string literals in the test; they are imported"
    - "The `:confval:` role is not introduced"
  artifacts:
    - path: "docs/source/user_guide/configuration.rst"
      provides: "the completed `Per-Document Templates` subsection (schema, template-xor-package rule, reserved key, two-master worked example, package-route schema example, link to output_layout), the nested `Registry Key Naming Rules` sub-subsection, the D-08 preventive note in `Custom Template File`, and the top-level `Removed Configuration Values` section"
      contains: "Registry Key Naming Rules"
    - path: "tests/test_registry_documentation_gate.py"
      provides: "the key-naming binding (published rules ↔ imported `_KEY_SHAPE_REJECTION_CASES`), the removed-values binding (published guidance ↔ imported `REMOVED_CONFIG_VALUES`), and their teeth self-tests"
      contains: "_KEY_SHAPE_REJECTION_CASES"
    - path: "tests/test_removed_config_deprecation_gate.py"
      provides: "the multi-value real-build assertions: three names set together produce three separate warnings in declaration order"
  key_links:
    - from: "typsphinx/template_registry.py::_KEY_SHAPE_REJECTION_CASES"
      to: "configuration.rst's `Registry Key Naming Rules` sub-subsection"
      via: "an import-based enumeration assertion in tests/test_registry_documentation_gate.py — adding an eighth case in code fails the gate until the page names it"
    - from: "typsphinx/removed_config.py::REMOVED_CONFIG_VALUES"
      to: "configuration.rst's `Removed Configuration Values` section"
      via: "an import-based per-name assertion, so a future removal added to the dict fails the gate until it is published"
---

<objective>
Complete DOC-15's registry documentation and publish DOC-17's migration guidance, on the page and
inside the catalogue structure the tracer proved.

Purpose: the tracer established that a fragment published in `configuration.rst` is held to real
code text. This plan puts the rest of the registry's contract on that same page — how to declare
one, what a legal key looks like, what happens when a key is unused, and what to do about the three
config values this milestone removed — with every enumerable fact bound by import rather than
transcription, so the page cannot drift from the code silently.

Output: `configuration.rst` reaches roughly its planned final size; two new binding test classes;
two new real-build assertions in the existing removed-config gate.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-per-document-template-documentation/56-CONTEXT.md
@.planning/phases/56-per-document-template-documentation/56-RESEARCH.md
@.planning/phases/56-per-document-template-documentation/56-PATTERNS.md
@.planning/phases/56-per-document-template-documentation/56-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the registry itself — schema, the two routes, the reserved key, and the two-master worked example</name>
  <files>docs/source/user_guide/configuration.rst</files>
  <read_first>
    - `docs/source/user_guide/configuration.rst` — the `Per-Document Templates` subsection created by plan 56-01, and the four sibling subsections above it (`Template Function` at 91, `Custom Template File` at 110, `Typst Package` at 122, `Template Assets` at 131) for tone, `.. code-block:: python` usage and length
    - `typsphinx/template_registry.py` lines 1-60 (`RESERVED_REGISTRY_KEY`, module contract) and lines 330-440 (what a definition may contain: `template` xor `package`, optional `template_function`, and the CONF-15/CONF-16/CONF-17 refusals)
    - `typsphinx/builder.py` lines 1985-2010 and 2145-2180 — the bundle copy, so the subsection's statement about which bundles reach the output is measured rather than inferred
    - `docs/source/user_guide/output_layout.rst` — the page this subsection links to instead of restating the layout (note: plan 56-03 rewrites its layout story in the same wave; link by `:doc:` and section name, never by quoting its prose)
    - `.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-CONTEXT.md` § D-01 and § D-14 — no deletion happens under `outdir`, and the shadow route is `<srcdir>/_typst/base.typ`
  </read_first>
  <action>
Fill in the `Per-Document Templates` subsection above the `When the Build Stops` sub-subsection
plan 56-01 created. Content, in this order:

**Lead paragraph.** `typst_document_templates` is a dict mapping a registry key to a definition. A
`typst_documents` entry's element [4] names which key it uses; an entry with no element [4] uses the
reserved key `typst`, which typsphinx synthesizes on every build from the global `typst_template` /
`typst_package` / `typst_template_function` settings. State plainly that this is additive: a
`conf.py` that never sets `typst_document_templates` behaves exactly as before.

**Definition schema.** A definition is a dict carrying `template` **exclusively or** `package` —
setting both is refused (CONF-15) — plus an optional `template_function` taking the same string form
or dict-with-`params` form the `Template Function` subsection above already documents. Name the
reserved key `typst` as the one key that may not be declared in `typst_document_templates` (CONF-16),
because typsphinx owns it.

**Worked example (D-04).** One `.. code-block:: python` block showing two masters on the `template`
route with no network dependency: `typst_document_templates` declaring one key — use a concrete
descriptive name such as `report` — whose `template` is a local path under `_typst/`, and a
`typst_documents` list with two entries, one four-element entry (which resolves to the reserved
`typst` key) and one five-element entry naming `report`. Follow the block with two or three sentences
naming what each entry produces. This is the ONLY place in the published documentation that shows a
non-default registry key, per D-02.

**Package route.** A second, deliberately short `.. code-block:: python` showing a definition using
`package` instead of `template`, with one sentence saying the wrapper imports the package directly
and no bundle is copied for that key.

**Which bundles reach the output.** Two or three sentences, then a `:doc:` link rather than a
restatement: every key some `typst_documents` entry actually names has its bundle — the resolved
template file's own parent directory — copied wholesale to the output tree; a key that is declared
but that no entry names is still validated but its bundle is not copied. Link with
`See :doc:\`output_layout\`` for where the copies land, matching the corpus's pervasive `:doc:` usage.
Add one sentence recording Phase 54's D-01: nothing under the output directory is deleted, so a file
removed from a source bundle can linger at the destination across an incremental rebuild.

**Empty-registry statement (edge probe).** One sentence: an empty `typst_document_templates` dict is
accepted and leaves only the synthesized reserved key, which is the same state as not setting the
value at all.

Also add the D-08 preventive statement to the existing `Custom Template File` subsection (currently
lines 110-120): a short paragraph after the `typst_template = "_typst/custom.typ"` block saying the
template must live in its own directory that is NOT named in Sphinx's `templates_path`, because the
whole bundle directory is copied to the output and doing so would republish the project's Sphinx
template directory; state that this repository uses `_typst/` for exactly that reason. Hard
constraint from `tests/test_docs_template_layout_gate.py::test_every_surviving_jinja_dir_mention_names_templates_path`:
if the paragraph mentions the bare Jinja-directory token at all, the word `templates_path` must appear
on the SAME physical line.

Keep every config name in double-backtick inline literals. Do not add a `:confval:` role. Do not
quote any `@preview` version.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest tests/test_docs_template_layout_gate.py tests/test_registry_documentation_gate.py tests/test_docs_contract_claims_gate.py -q && uv run tox -e docs-html && uv run tox -e docs-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_docs_template_layout_gate.py tests/test_registry_documentation_gate.py tests/test_docs_contract_claims_gate.py -q` exits 0
    - `grep -c 'typst_document_templates' docs/source/user_guide/configuration.rst` is at least 6
    - `grep -c 'templates_path' docs/source/user_guide/configuration.rst` is at least 1
    - `grep -c 'output_layout' docs/source/user_guide/configuration.rst` is at least 1
    - `grep -rc 'typst_document_templates' docs/source/user_guide/templates.rst` is 0 — no registry walkthrough leaked onto the templates page
    - `git diff --stat docs/source/user_guide/index.rst` is empty
    - `grep -c '@preview' docs/source/user_guide/configuration.rst` is 0
    - `uv run tox -e docs-html` and `uv run tox -e docs-pdf` both report `build succeeded`
    - `git diff --stat typsphinx/` is empty
  </acceptance_criteria>
  <reversibility rating="reversible">Prose on one page with one cross-reference; superseding it is a rewrite of the same subsection.</reversibility>
  <done>The registry's schema, both routes, the reserved key, the two-master local worked example, the unused-key and empty-dict rules and the `output_layout` link are published in one subsection; the `Custom Template File` subsection carries the `templates_path` warning without breaking the existing same-line gate.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the key-naming rules and bind the seven cases to the code by import</name>
  <files>docs/source/user_guide/configuration.rst, tests/test_registry_documentation_gate.py</files>
  <read_first>
    - `typsphinx/template_registry.py` lines 34-140 — `_KEY_SHAPE_REJECTION_CASES` (the seven names, in order), `_WINDOWS_RESERVED_NAMES`, `_is_windows_reserved_name()` (compares case-insensitively against the stem before the first dot), `_has_case_collision()` (folds through `TypstBuilder._collision_key`), and `_validate_registry_key_shape()`'s fixed check order
    - `typsphinx/builder.py` lines 667-748 — `_collision_key()`: separator folding, `posixpath.normpath()`, then `casefold()`, and its explicit statement that NO Unicode NFC/NFD normalization is applied
    - `typsphinx/builder.py` lines 1040-1060 and 1150-1225 — the pre-write reserved-key case-collision check, which refuses a declared key that folds onto the reserved key
    - `typsphinx/template_registry.py` lines 480-530 — the `typst_documents` element [4] lookup, exact `str` equality, never case-folded
    - `docs/source/user_guide/configuration.rst` — the `Per-Document Templates` subsection and the `When the Build Stops` sub-subsection, so the new sub-subsection lands between them
    - `tests/test_registry_documentation_gate.py` — the module's existing helpers and teeth-test conventions
  </read_first>
  <action>
Insert a nested `^^^^` sub-subsection titled `Registry Key Naming Rules` inside `Per-Document
Templates`, positioned immediately BEFORE `When the Build Stops` (D-07). If plan 56-01's SUMMARY
records that the nested heading level had to be flattened to a sibling `~~~~`, follow that same
structure here and keep the ordering rule — naming rules before the error table — intact either way.

Content:
  - one lead sentence stating why the rules exist: the key becomes a directory name under the output
    tree's reserved `_template/` directory, so it must be a single, portable path segment;
  - the seven rejection cases, in the fixed order the code checks them, as a `.. list-table::` with
    `:header-rows: 1` and two columns headed `Rejected key shape` and `Why`. In order: empty or
    whitespace-only; exactly `.` or `..`; containing `/` or `\`; a Windows reserved device name
    (matched case-insensitively against everything before the first dot, so `CON.txt` is reserved and
    `ICONIC` is not); a trailing dot; a trailing space; differing from another declared key only by
    case;
  - a short paragraph after the table stating the comparison rule precisely: case comparison folds
    separators, normalizes path shape, then applies Python's `casefold()`, and applies no Unicode
    normalization at all — so the composed and decomposed spellings of one accented character are two
    DIFFERENT keys, on every platform, with no `sys.platform` branch;
  - a second short paragraph stating that a declared key that folds onto the reserved `typst` key is
    also refused, before any file is written, and that this refusal is reported by the pre-write
    failure shape the error table names — so a key that differs from the reserved key only by case is
    not usable;
  - one sentence stating that the `typst_documents` element [4] lookup itself is exact string
    equality and is never case-folded, so a key must be spelled in an entry exactly as it was
    declared;
  - a cross-reference from the error table's invalid-definition row to this sub-subsection using the
    corpus's implicit section-reference form `` `Registry Key Naming Rules`_ `` (add it to that row's
    third column in the same edit).

Then add a `TestKeyNamingRulesMatchTheCode` class to `tests/test_registry_documentation_gate.py`:
  - import `_KEY_SHAPE_REJECTION_CASES` from `typsphinx.template_registry` — do not transcribe the
    seven names;
  - assert the tuple still has exactly seven entries, so an eighth case added in code fails here;
  - for each case name, assert the naming-rules region (extracted at run time by the same
    heading-title-delimited helper the catalogue uses) contains a distinguishing phrase for it. Derive
    the mapping from case name to expected phrase in one module-level dict whose KEYS are checked
    for exact set-equality against `set(_KEY_SHAPE_REJECTION_CASES)`, so a renamed or added case fails
    loudly rather than silently going unchecked;
  - assert the region names `casefold` and states that no Unicode normalization is applied;
  - add a teeth test feeding the region-phrase checker a synthetic region text missing one phrase and
    asserting it reports that case uncovered.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry_documentation_gate.py -q && uv run tox -e docs-html && uv run tox -e docs-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry_documentation_gate.py -q` exits 0
    - `grep -c 'Registry Key Naming Rules' docs/source/user_guide/configuration.rst` is at least 2 — the heading and the error-table cross-reference
    - `grep -c 'casefold' docs/source/user_guide/configuration.rst` is at least 1
    - `grep -c '_KEY_SHAPE_REJECTION_CASES' tests/test_registry_documentation_gate.py` is at least 1
    - `awk '/Registry Key Naming Rules/,/When the Build Stops/' docs/source/user_guide/configuration.rst | grep -c 'list-table'` is at least 1 — the naming-rules table precedes the error table
    - `uv run pytest tests/test_registry_prewrite_validation_gate.py tests/test_template_prefix_reservation_gate.py -q` exits 0 — the runtime reproductions D-06 deliberately does not duplicate are untouched and still green
    - `uv run tox -e docs-html` and `uv run tox -e docs-pdf` both report `build succeeded`
  </acceptance_criteria>
  <done>The seven CONF-18 cases, the casefold-without-Unicode-normalization rule, the reserved-key fold refusal and the exact-equality lookup rule are published before the error table, and the seven cases are held to the code by an imported enumeration rather than a transcription.</done>
</task>

<task type="auto">
  <name>Task 3: Publish migration guidance for the three removed values, bound to REMOVED_CONFIG_VALUES</name>
  <files>docs/source/user_guide/configuration.rst, tests/test_registry_documentation_gate.py, tests/test_removed_config_deprecation_gate.py</files>
  <read_first>
    - `typsphinx/removed_config.py` in full — `REMOVED_CONFIG_VALUES` (the three names and their exact warning bodies, lines 36-57), and `check_config_at_init()` (lines 58-92): iteration order is the dict's declaration order, membership is exact `str` containment in `config._raw_config`, the trigger is presence not value, and the call is a bare `logger.warning(message)` with no keyword arguments
    - `tests/test_removed_config_deprecation_gate.py` in full — its `REQUIRED_PHRASES` per-name dict, its `_run_sphinx_build` helper copy, and its existing classes `TestEachRemovedValueWarnsByName`, `TestA03NoneValueStillWarns`, `TestCleanConfigEmitsNoWarnings`, `TestNoWarningSubtype`
    - `docs/source/user_guide/configuration.rst` — the section map: `Complete Example` is a `----` section beginning at line 333 and `See Also` at line 384; the new section goes between them
    - `docs/source/changelog.rst` — read only to confirm the historical entries this task must NOT edit
  </read_first>
  <action>
Add a new top-level `----` section titled `Removed Configuration Values`, placed after the
`Complete Example` section and before `See Also`. A top-level section rather than a subsection of
`Template Configuration` because two of the three names are not template configuration, and because
`=`/`-`/`~` are the only heading levels with precedent on this page.

Content:
  - a lead paragraph stating the mechanism: typsphinx detects a removed name still present in
    `conf.py` and emits one build warning per name; the warning fires for EVERY builder including
    `-b html`, because the check runs on Sphinx's `config-inited` event before a builder exists;
    setting a removed name to `None` or an empty list still warns, because writing the line at all
    means holding a wrong belief about what it does; the detection is exact name matching, so a
    differently-spelled or differently-cased name is not detected at all; and there is no
    per-warning suppression route — the warning carries no warning type — so the only lever is
    `sphinx-build -W`, which turns it into a failure;
  - a `.. list-table::` with `:header-rows: 1` and three columns headed `Removed value`,
    `Removed in`, `What to do instead`, one row per name in `REMOVED_CONFIG_VALUES` declaration
    order:
      * `typst_template_assets` — removed in v0.9.0 — delete the setting; every used template's
        bundle directory is now copied wholesale, so MORE files reach the output than the explicit
        list used to select, and no asset list is needed;
      * `typst_authors` — removed in v0.7.1 — express rich author structure through
        `typst_template_function`'s `params` route, cross-referencing the `Author Information`
        section already on this page; note the observable consequence that author department,
        organization and email do not reach the output unless supplied that way;
      * `typst_toctree_defaults` — removed in v0.6.3 — no replacement; it was registered but never
        read even when it existed, so deleting it changes no build output;
  - one closing sentence stating that a `conf.py` setting more than one of them emits one separate
    warning per name, in the order listed in the table, never a single combined warning.

Then add a `TestRemovedValuesGuidanceMatchesTheWarnings` class to
`tests/test_registry_documentation_gate.py` (keeping that module's never-skips property — this
assertion needs only a dict import and a text read):
  - import `REMOVED_CONFIG_VALUES` from `typsphinx.removed_config`; do not transcribe the strings;
  - extract the `Removed Configuration Values` region at run time with the same heading-title-delimited
    helper;
  - assert every key of `REMOVED_CONFIG_VALUES` appears in that region, so a fourth removal added to
    the dict fails until it is published;
  - for each name, assert the region carries a per-name required-phrase set derived from that name's
    own warning body — reuse the same bespoke-phrase-set idea `test_removed_config_deprecation_gate.py`'s
    `REQUIRED_PHRASES` already uses, and assert this module's phrase-set dict has exactly the same key
    set as `REMOVED_CONFIG_VALUES`;
  - assert the region states the no-suppression fact and the every-builder fact;
  - add a teeth test: feed the region checker a synthetic region missing one name and assert it fires.

Finally, extend `tests/test_removed_config_deprecation_gate.py` with one new class,
`TestMultipleRemovedValuesEachWarnSeparately`, using that module's existing `_run_sphinx_build` copy
and a `tmp_path`-built minimal source tree whose `conf.py` sets all three removed names. Assert the
combined build output contains each of the three warning bodies exactly once, and that their first
occurrence positions are strictly increasing in `REMOVED_CONFIG_VALUES` declaration order — the
ordering edge, measured rather than asserted. Import the dict; do not transcribe.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry_documentation_gate.py tests/test_removed_config_deprecation_gate.py -q && uv run tox -e docs-html && uv run tox -e docs-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry_documentation_gate.py tests/test_removed_config_deprecation_gate.py -q` exits 0
    - `grep -c 'Removed Configuration Values' docs/source/user_guide/configuration.rst` is at least 1
    - `grep -c 'typst_template_assets' docs/source/user_guide/configuration.rst` is at least 1, `grep -c 'typst_authors' docs/source/user_guide/configuration.rst` is at least 1, `grep -c 'typst_toctree_defaults' docs/source/user_guide/configuration.rst` is at least 1
    - `grep -c 'suppress_warnings' docs/source/user_guide/configuration.rst` is 0 — no route that does not exist is offered
    - `grep -c 'REMOVED_CONFIG_VALUES' tests/test_registry_documentation_gate.py` is at least 1
    - `git diff --stat docs/source/changelog.rst CHANGELOG.md` is empty — historical release notes untouched
    - `uv run pytest -q` exits 0 with 0 failed
    - `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0
  </acceptance_criteria>
  <done>All three removed values have published guidance whose per-name content, count, ordering, suppression and builder-scope claims are each held to `typsphinx/removed_config.py` by import or by a real build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository file contents → test process | the gate imports `typsphinx.template_registry` and `typsphinx.removed_config` and reads `configuration.rst` text |
| fixture `conf.py` → `sphinx-build` subprocess | the multi-value ordering test writes a `conf.py` under `tmp_path` and executes Sphinx over it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-04 | Tampering | the `tmp_path` `conf.py` executed by `sphinx-build` in `TestMultipleRemovedValuesEachWarnSeparately` | medium | mitigate | the `conf.py` body is a literal written by the test itself, never derived from a repository file, an environment variable or any external input; the subprocess argument list is fixed (`sys.executable -m sphinx -b <builder> <src> <build>`), matching the six existing gate modules, so no user-controlled string reaches the command line |
| T-56-05 | Information Disclosure | assertion messages quoting build output and file paths | low | mitigate | `tmp_path` roots are pytest-managed and their contents are test-authored; repository paths printed in failures are derived from `Path(__file__)` relatives, with no absolute path literal in either module |
| T-56-06 | Tampering | importing `typsphinx.template_registry` / `typsphinx.removed_config` in a static gate | low | accept | these are this repository's own already-imported production modules, imported by the whole existing suite; importing them is not new attack surface, and it is what makes the enumeration binding real instead of transcribed |
| T-56-SC | Tampering | npm/pip/cargo installs | low | accept | this plan installs no package; `56-RESEARCH.md` § "Package Legitimacy Audit" measured that the phase adds no dependency |
</threat_model>

<verification>
- `uv run pytest -q` — full suite green, 0 failed
- `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` — all clean
- `uv run tox -e docs-html && uv run tox -e docs-pdf` — both `build succeeded`
- `git diff --stat typsphinx/ docs/source/changelog.rst CHANGELOG.md docs/source/user_guide/index.rst` — empty
- Human check (end-of-phase, `human_verify_mode: end-of-phase`): open the rendered `configuration.html` and the `docs-pdf` output and confirm both new `list-table`s render with every row visible and no margin overflow.

</verification>

<success_criteria>
- `typst_document_templates` is documented once, on one page, with a network-free two-master worked example.
- The seven CONF-18 cases and the three removed values are each held to code by an imported enumeration, so adding an eighth case or a fourth removal fails the gate.
- The four DOC-17 edge behaviours — count, order, exact-name matching, and the clean-config case — are each either measured by a real build or published and pinned.
- Both documentation builds stay green and no historical release note is edited.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan:

- **New test classes:** `TestKeyNamingRulesMatchTheCode`, `TestRemovedValuesGuidanceMatchesTheWarnings` (in `tests/test_registry_documentation_gate.py`); `TestMultipleRemovedValuesEachWarnSeparately` (in `tests/test_removed_config_deprecation_gate.py`)
- **New module-level constants:** a case-name-to-phrase mapping and a removed-name-to-phrase mapping in `tests/test_registry_documentation_gate.py`
- **New RST section titles in `docs/source/user_guide/configuration.rst`:** `Registry Key Naming Rules` (nested under `Per-Document Templates`), `Removed Configuration Values` (top-level `----`)
- **New implicit RST link target:** `` `Registry Key Naming Rules`_ ``

<output>
Create `.planning/phases/56-per-document-template-documentation/56-02-SUMMARY.md` when done.
</output>
