---
phase: 33-v0-6-4-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - uv.lock
autonomous: true
requirements: [REL-02]

must_haves:
  truths:
    - "pyproject.toml's `version` key reads 0.6.4 and remains the repository's sole hard-coded package-version literal; typsphinx/__init__.py derives its version from importlib.metadata and is therefore not edited."
    - "pyproject.toml's `version` and README.md's Status-line version are exactly equal at 0.6.4 — the adjacency case where two independently-maintained values must coincide rather than drift — asserted by tests/test_readme_version_sync.py."
    - "`uv run python -c \"import typsphinx; print(typsphinx.__version__)\"` prints 0.6.4, proving the editable-install metadata was regenerated and not left reporting the previous release."
    - "uv.lock's typsphinx self-entry version reads 0.6.4 and no transitive dependency line in uv.lock changed (single-package diff)."
    - "Re-running the lock-and-sync sequence on the already-prepared tree is a no-op: `git diff --exit-code uv.lock` exits 0 on a second invocation (idempotency case — the version bump converges rather than churning the lockfile)."
    - "tests/test_readme_version_sync.py and tests/test_preview_version_sync.py both exit 0 on the bumped tree."
    - statement: "An interrupted or partially-applied run never leaves a committed tree in which pyproject.toml's version and README.md's Status version disagree, because both literals are bumped inside a single task and land in a single commit."
      verification: backstop
  artifacts:
    - pyproject.toml
    - README.md
    - uv.lock
  key_links:
    - "pyproject.toml `version` -> README.md Status line (bound by tests/test_readme_version_sync.py's _STATUS_LINE_RE / _extract_pyproject_version equality assertion)"
    - "pyproject.toml `version` -> uv.lock typsphinx self-entry (bound by `uv lock`; never hand-edited)"
    - "pyproject.toml `version` -> typsphinx.__version__ (bound by editable-install metadata that only `uv sync` regenerates)"
---

<objective>
Bump the package version literal from 0.6.3 to 0.6.4 across the three surfaces that must move
in lockstep, and prove the bump reached the runtime import path.

Purpose: Satisfies ROADMAP Phase 33 SC#1 — the half of REL-02 that this prep-only phase can
actually discharge. Every downstream artifact of this phase (CHANGELOG heading, handoff
checklist, tag) names 0.6.4; this plan is what makes that name true in the tree.
Output: pyproject.toml, README.md, and uv.lock all naming 0.6.4, with both version-sync guard
tests green and `typsphinx.__version__` reporting 0.6.4.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-v0-6-4-release-prep/33-RESEARCH.md
@.planning/phases/33-v0-6-4-release-prep/33-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan creates **no new symbols** — no functions, classes, decorators, CLI flags, or struct
fields. Milestone invariant #3 forbids any change under `typsphinx/`. The plan mutates three
existing values in place:

| Artifact | Kind | Change |
|----------|------|--------|
| `pyproject.toml` `[project] version` | existing TOML key | value `0.6.3` -> `0.6.4` |
| `README.md` Status line version | existing prose literal | `(v0.6.3)` -> `(v0.6.4)` |
| `uv.lock` typsphinx self-entry `version` | existing lock field | regenerated to `0.6.4` by `uv lock` |

No new file paths. No new test files (both guard tests already exist and are used as-is).

<tasks>

<task type="auto">
  <name>Task 1: Bump the version literal in pyproject.toml and README.md in one commit</name>

  <files>pyproject.toml, README.md</files>

  <read_first>
    - `pyproject.toml` — read the `[project]` table. The `version` key is at line 7 as measured
      at plan time; re-locate it by key name rather than trusting the offset.
    - `README.md` — read the region around line 317 (measured at plan time) containing the
      Status line. The line number shifts as the file grows; locate it by the `**Status**:`
      prefix.
    - `tests/test_readme_version_sync.py` — read in full (~75 lines). This is the assertion that
      binds the two edits together; you must understand `_STATUS_LINE_RE` and
      `_extract_pyproject_version()` before editing either file, because a formatting deviation
      (extra spaces, a different parenthetical shape) silently fails the regex match rather than
      producing a clear mismatch.
    - `.planning/phases/33-v0-6-4-release-prep/33-PATTERNS.md` — section `### README.md` and
      section `### pyproject.toml`, which reproduce the exact Phase 28 diff shape for both edits.
  </read_first>

  <action>
    Change `pyproject.toml`'s `[project]` table `version` key from `0.6.3` to `0.6.4`. This is
    the only key in the file that changes. Do NOT touch `[project.urls]` — its `Documentation`
    value is already `https://typsphinx.readthedocs.io/` (set in Phase 31); SC#3 is a
    re-verification handled in plan 33-04, never an edit here. Do NOT touch `dependencies` or
    `optional-dependencies`: milestone invariant #1 is zero new runtime dependencies, and
    `git diff main..HEAD -- pyproject.toml` must continue to show no change to those arrays.

    In the same task, change `README.md`'s Status line so its parenthetical version reads
    `v0.6.4` instead of `v0.6.3`. Preserve the surrounding text of that line verbatim, including
    the `**Status**:` prefix, the word `Stable`, the parentheses, and the trailing
    `- Production ready`. The guarding regex requires the shape
    `**Status**: Stable (vN.N.N)` — any reflow of that line breaks the match.

    Both edits MUST land in a single commit. `tests/test_readme_version_sync.py` asserts the two
    values are equal, so a tree with only one of them bumped is a red suite. Do not split this
    task across commits.
  </action>

  <verify>
    <automated>grep -c '^version = "0.6.4"$' pyproject.toml | grep -qx 1 &amp;&amp; grep -c '^version = "0.6.3"$' pyproject.toml | grep -qx 0 &amp;&amp; grep -c 'Stable (v0.6.4)' README.md | grep -qx 1 &amp;&amp; grep -c 'Stable (v0.6.3)' README.md | grep -qx 0 &amp;&amp; echo BUMP_OK</automated>
  </verify>

  <acceptance_criteria>
    - `pyproject.toml` contains the line `version = "0.6.4"` and contains zero occurrences of
      `version = "0.6.3"`.
    - `README.md` contains a line matching `**Status**: Stable (v0.6.4) - Production ready` and
      zero occurrences of `Stable (v0.6.3)`.
    - `git diff --stat pyproject.toml README.md` shows exactly 2 files changed, 2 insertions,
      2 deletions — one line per file, no collateral reformatting.
    - `git diff pyproject.toml` shows no change to any line inside the `dependencies`,
      `optional-dependencies`, or `[project.urls]` blocks.
  </acceptance_criteria>

  <done>
    Both version literals read 0.6.4, the diff is exactly two lines, and no dependency or URL
    metadata moved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate uv.lock and the editable-install metadata, then prove the runtime version</name>

  <files>uv.lock</files>

  <read_first>
    - `uv.lock` — read the region around line 1379 (measured at plan time) containing the
      typsphinx self-entry with `version = "0.6.3"`. You are confirming what the regeneration
      must change, not editing it by hand.
    - `.planning/phases/33-v0-6-4-release-prep/33-RESEARCH.md` — sections `### Pattern 1`,
      `### Pitfall 1`, and `### Pitfall 3`. Pitfall 1 documents the measured fact that editing
      `pyproject.toml` alone leaves `typsphinx.__version__` reporting the old release, because
      the editable-install metadata (`typsphinx.egg-info/`, the `__editable__.typsphinx-*.pth`
      file, and the `typsphinx-*.dist-info/` directory) embeds the version in filenames and
      contents that only a sync regenerates.
    - `CLAUDE.md` — section `### Worktree-isolated execution`. Worktree isolation is this
      project's standing execution mode; the provisioning form of the sync command below is
      mandatory, not conditional.
  </read_first>

  <action>
    Run `uv lock` to update `uv.lock`'s typsphinx self-entry to 0.6.4. Do NOT hand-edit
    `uv.lock` — the self-pin is regenerated, and a manual edit is non-reproducible and can
    desynchronize the resolution hash.

    Then run the sync that regenerates the editable-install metadata, so `importlib.metadata`
    stops returning the previous release. Because this project runs executors in isolated git
    worktrees by default, both commands must be invoked with `VIRTUAL_ENV` and
    `UV_PROJECT_ENVIRONMENT` unset (`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT`), so uv
    provisions a worktree-local `.venv` instead of re-pointing an already-activated main-tree
    venv at the worktree. Use the `--extra dev --locked` form so the sync installs the dev
    extras this phase's tests need and fails loudly if the lock drifted from `pyproject.toml`.

    After the sync, print `typsphinx.__version__` through `uv run` and confirm it reports
    `0.6.4`. If it still reports the prior version, the sync did not regenerate the metadata —
    diagnose before proceeding; do not paper over it by reading the version from
    `pyproject.toml` instead.

    Then inspect `git diff uv.lock`. The expected diff is the single `version` line in the
    typsphinx self-entry. If any transitive dependency line moved, stop: milestone invariant #1
    (zero new runtime dependencies) is at stake and an unexpected resolution change must be
    understood, not committed.

    Finally, run both version-sync guard tests. `tests/test_readme_version_sync.py` proves the
    Task 1 pair agree; `tests/test_preview_version_sync.py` proves the four `@preview`
    declaration surfaces (`typsphinx/writer.py`, `typsphinx/template_engine.py`,
    `typsphinx/templates/base.typ`, `examples/**/*.typ`) remain in lockstep and unbumped, which
    is SC#1's "now-four-surface" clause. Do not modify any of those four files to make the test
    pass — if it fails, that is a real invariant breach.

    Idempotency check: after committing, run `uv lock` and the sync a second time and confirm
    `git diff --exit-code uv.lock` exits 0. The bump must converge, not churn the lockfile on
    every invocation.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import typsphinx; print(typsphinx.__version__)" | grep -qx '0.6.4' &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints exactly `0.6.4`.
    - `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q`
      exits 0 with zero failures.
    - `git diff --numstat uv.lock` reports exactly `1` insertion and `1` deletion (the typsphinx
      self-entry version line only).
    - `git diff main..HEAD -- typsphinx/` produces empty output (this task changed nothing under
      `typsphinx/`, including the three in-package `@preview` declaration surfaces).
    - A second `uv lock` + sync invocation followed by `git diff --exit-code uv.lock` exits 0.
  </acceptance_criteria>

  <done>
    `uv.lock` names 0.6.4 in a one-line diff, the imported package reports 0.6.4, both guard
    tests are green, and re-running the lock-and-sync sequence produces no further diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `uv lock` -> package index resolution | The only step in this plan that can reach the network and alter what code gets installed. |
| repository -> published wheel metadata | The `version` string edited here becomes the identity of an artifact later published to PyPI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Tampering | `uv lock` regeneration of `uv.lock` | low | mitigate | Task 2 asserts `git diff --numstat uv.lock` is exactly 1 insertion / 1 deletion and halts if any transitive dependency line moved, so an out-of-band resolution change cannot ride along with the self-pin bump. |
| T-33-02 | Spoofing | package version identity in `pyproject.toml` | low | mitigate | The bump is constrained to `0.6.3` -> `0.6.4` (ROADMAP SC#1 fixes the number); Task 1's acceptance criteria assert zero remaining `0.6.3` literals so no surface can claim a different identity than the wheel. |
| T-33-SC | Tampering | package-manager installs | low | accept | This plan installs no new package. `33-RESEARCH.md`'s `## Package Legitimacy Audit` records the gate as not-applicable, corroborated at plan time: `git diff main..HEAD -- pyproject.toml` shows a single `Documentation` URL line and zero changes to `dependencies`/`optional-dependencies`, and `git diff main..HEAD -- uv.lock` is empty. The sync reinstalls from an existing pinned lock. |
</threat_model>

<verification>
- `grep -c 'version = "0.6.3"' pyproject.toml` returns 0; `grep -c 'Stable (v0.6.3)' README.md` returns 0.
- `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints `0.6.4`.
- `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q` exits 0.
- `git diff main..HEAD --stat -- typsphinx/` is empty.
</verification>

<success_criteria>
SC#1 of ROADMAP Phase 33 is discharged: `pyproject.toml` is the sole 0.6.4 version literal,
`uv.lock` is in lockstep, `README.md`'s Status line is updated, `typsphinx.__version__` reports
0.6.4, and both version-sync guard tests are green.
</success_criteria>

<output>
Create `.planning/phases/33-v0-6-4-release-prep/33-01-SUMMARY.md` when done.
Record the verbatim output of the `typsphinx.__version__` probe and the `git diff --numstat
uv.lock` result — plan 33-04 cites these as SC#1 evidence.
</output>
