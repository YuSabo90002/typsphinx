---
phase: 33-v0-6-4-release-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [REL-02]

must_haves:
  truths:
    - "CHANGELOG.md contains exactly one `## [0.6.4]` heading — running the entry insertion twice cannot produce a duplicate section (idempotency case)."
    - "The `## [0.6.4]` heading sits strictly between `## [Unreleased]` and `## [0.6.3]`, and its five subsections appear in the order Added, Changed, Removed, Fixed, Verified — the ordering is specified, not incidental (ordering case)."
    - "The `[0.6.4]` entry contains zero `BREAKING` labels, per D-01."
    - "The Removed section's hosting-teardown bullet states in its own body that the retired github.io URLs now return 404 with no redirect, and that browser-language auto-redirection is gone (D-01, D-04)."
    - "The Japanese documentation site is announced with no translation-coverage figure and no partial-translation qualifier (D-02)."
    - "The `### Verified` section asserts only the three invariants provable by git diff — zero new runtime dependencies, no `@preview` version bump, zero changes under `typsphinx/` — and no build-status or live-hosting observation (D-03)."
    - "The tail link block contains `[0.6.4]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.6.4` immediately above the `[0.6.3]:` line, and the file's final line is `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.6.4...HEAD`."
    - "Every heading and body line for release 0.6.3 and earlier is byte-identical to its pre-phase state — the empty-delta case: the correct diff against historical entries is no diff at all."
    - statement: "The newly added `[0.6.4]` release link resolves once the tag is cut; it is expected to 404 for the duration of this phase because no tag exists yet, and that transient state is the accepted project convention rather than a defect introduced here."
      verification: backstop
  artifacts:
    - CHANGELOG.md
  key_links:
    - "`## [0.6.4]` heading -> `[0.6.4]:` tail link reference (Markdown reference-link resolution; a heading without its tail entry renders as literal bracketed text)"
    - "`[Unreleased]:` compare range -> the 0.6.4 tag (carried forward so Unreleased describes work after 0.6.4, not after 0.6.3)"
    - "`## [0.6.4]` entry body -> GitHub Release body at `/gsd-complete-milestone` (this entry is that body's single source)"
---

<objective>
Author the curated `## [0.6.4]` CHANGELOG entry summarizing this milestone's user-visible
changes, and carry the tail release/compare link block forward in the same phase.

Purpose: Satisfies ROADMAP Phase 33 SC#2. This entry is also the single source for the GitHub
Release body that `/gsd-complete-milestone` will publish — it is the milestone's public
narrative, not an internal note.
Output: CHANGELOG.md with a five-section `[0.6.4]` entry and an updated tail link block.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-v0-6-4-release-prep/33-RESEARCH.md
@.planning/phases/33-v0-6-4-release-prep/33-PATTERNS.md
</context>

## Artifacts this phase produces

This plan creates **no new code symbols**. It adds two new Markdown anchors to an existing file:

| Artifact | Kind | Change |
|----------|------|--------|
| `## [0.6.4]` heading in `CHANGELOG.md` | new Markdown section | inserted between `## [Unreleased]` and `## [0.6.3]` |
| `### Verified` subsection under `[0.6.4]` | new subsection kind for this file | first use of a fifth Keep-a-Changelog-adjacent section name in the 0.6.x series |
| `[0.6.4]:` reference-link definition | new link reference | inserted above `[0.6.3]:` in the tail block |

No new file paths, functions, classes, CLI flags, or config keys.

<tasks>

<task type="auto">
  <name>Task 1: Insert the curated [0.6.4] entry between Unreleased and 0.6.3</name>

  <files>CHANGELOG.md</files>

  <read_first>
    - `CHANGELOG.md` lines 1–60 — read the header, the `## [Unreleased]` marker (line 8 as
      measured at plan time), and the entire `## [0.6.3]` entry. The 0.6.3 entry is the direct
      structural precedent: lead paragraph, then `### Added` / `### Changed` / `### Removed` /
      `### Fixed`, with requirement IDs in parentheses immediately before the em-dash of each
      bold lead-in. Note that 0.6.3 uses `- **BREAKING: ...**` bullets — that label is exactly
      what this entry must NOT reproduce.
    - `.planning/phases/33-v0-6-4-release-prep/33-RESEARCH.md` — the `## Code Examples` section
      contains a complete drafted `[0.6.4]` entry built from this template and fact-checked
      against PROJECT.md's Phase 29–32 Key Decisions. Use it as the starting text. Do not
      re-derive the structure from scratch.
    - `.planning/phases/33-v0-6-4-release-prep/33-PATTERNS.md` — section `### CHANGELOG.md`, for
      the requirement-ID citation style and the exact insertion position.
    - `.planning/REQUIREMENTS.md` — the v1 requirement list, to confirm the requirement IDs cited
      in each bullet (RTD-01..RTD-04, I18N-01..I18N-03, DOC-08..DOC-10, CI-04, CI-05) match
      their actual definitions.
  </read_first>

  <action>
    Insert a new `## [0.6.4] - <DATE>` section directly beneath the `## [Unreleased]` marker and
    directly above `## [0.6.3]`. `<DATE>` is the actual execution date in ISO form — obtain it
    from `date -I` at execution time rather than copying a date from any planning document.
    Leave the `## [Unreleased]` heading in place above the new entry; Keep a Changelog keeps it
    as a standing empty marker.

    Use the drafted entry in 33-RESEARCH.md's `## Code Examples` section as the base text. Its
    structure is a lead paragraph followed by five subsections in this exact order: `### Added`,
    `### Changed`, `### Removed`, `### Fixed`, `### Verified`.

    Content constraints that are locked decisions, not stylistic preferences:

<!-- planner-discipline-allow: BREAKING -->

    - Per D-01, the entry contains no `BREAKING` label anywhere. This milestone changed no
      package behavior — nothing under `typsphinx/` moved — so what broke is the retired
      github.io URLs and repository-internal tooling, neither of which is in the wheel. Instead
      of a label, the Removed section's hosting-teardown bullet carries a prominent in-body
      statement that the old github.io URLs now return 404 with no redirect, and that the
      documentation root's browser-language auto-redirection is gone (Read the Docs redirects to
      a version, never to a visitor's detected language).
    - Per D-02, announce the Japanese documentation site plainly. State no translation-coverage
      figure and add no hedging clause about how much of the site is rendered in Japanese. The
      owner selected brevity over the qualified phrasing.
    - Per D-03, `### Verified` lists only invariants a git diff can prove: zero new runtime
      dependencies, no `@preview` package version bump with the four version-sync surfaces
      untouched, and zero changes under `typsphinx/` across the milestone diff. Do not carry
      over the corpus-gate triple used in the 0.6.2 and 0.6.3 entries (fatal-free compile, PDF
      magic bytes, unknown-visit count) — those were justified by translator changes that this
      milestone does not have. Do not record any live Read the Docs serving observation: it is
      point-in-time and this project does not put facts it cannot re-verify into the CHANGELOG.
    - Per D-04, cite requirement IDs in parentheses at the end of each bullet's bold lead-in,
      matching the 0.6.3 entry's style. Added covers the Japanese site (I18N-01, I18N-03) and
      the repository-wide link-check CI (CI-05); Changed covers the hosting move to Read the
      Docs including the typstpdf-produced downloadable PDF (RTD-01..RTD-04); Removed covers the
      hand-rolled multi-language machinery plus orphan docs (I18N-02, DOC-08) and the GitHub
      Pages site and gh-pages branch (CI-04); Fixed covers the seven dead README/PyPI links
      (DOC-09, DOC-10, and GitHub issue 119).
    - The lead paragraph follows the established shape: the milestone's axis plus a one-line
      invariant statement. This milestone's axis is the hosting move from GitHub Pages to Read
      the Docs with zero runtime change.

    Do not modify any existing entry. In particular, leave the historical github.io mention in
    the 0.6.1-era body untouched — REQUIREMENTS.md's Out of Scope table records it as a
    deliberate historical record, on the same precedent as v0.6.3 Phase 24 D-02.
  </action>

  <verify>
    <automated>grep -c '^## \[0.6.4\] - 20' CHANGELOG.md | grep -qx 1 &amp;&amp; grep -n '^## \[' CHANGELOG.md | head -3 | awk -F'[][]' 'NR==1&amp;&amp;$2!="Unreleased"{exit 1} NR==2&amp;&amp;$2!="0.6.4"{exit 1} NR==3&amp;&amp;$2!="0.6.3"{exit 1} END{exit 0}' &amp;&amp; awk '/^## \[0.6.4\]/,/^## \[0.6.3\]/' CHANGELOG.md | grep -c 'BREAKING' | grep -qx 0 &amp;&amp; awk '/^## \[0.6.4\]/,/^## \[0.6.3\]/' CHANGELOG.md | grep -n '^### ' | tr '\n' ' ' | grep -q 'Added.*Changed.*Removed.*Fixed.*Verified' &amp;&amp; echo ENTRY_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## \[0.6.4\]' CHANGELOG.md` returns exactly 1.
    - The first three `^## \[` headings in the file are, in order: `[Unreleased]`, `[0.6.4]`,
      `[0.6.3]`.
    - The `[0.6.4]` heading line matches `^## \[0.6.4\] - \d{4}-\d{2}-\d{2}$` and the date equals
      the output of `date -I` on the execution day.
    - Within the `[0.6.4]` section, the `^### ` subsection headings appear in the order Added,
      Changed, Removed, Fixed, Verified.
    - Within the `[0.6.4]` section, `grep -c 'BREAKING'` returns 0.
    - Within the `[0.6.4]` section, `grep -cE '[0-9]+(\.[0-9]+)?%|24\.3'` returns 0 (D-02: no
      translation-coverage figure).
    - The `### Verified` subsection mentions runtime dependencies, the `@preview` version-sync
      surfaces, and `typsphinx/`, and mentions no build status or live-site observation.
    - `git diff CHANGELOG.md` shows only insertions in the region between the `[Unreleased]` and
      `[0.6.3]` headings — zero deletions and zero modifications to any line belonging to release
      0.6.3 or earlier.
  </acceptance_criteria>

  <done>
    A single, correctly positioned `[0.6.4]` entry exists with five subsections in the mandated
    order, no BREAKING label, no translation-coverage qualifier, a git-provable-only Verified
    section, and zero edits to historical entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the tail release/compare link block</name>

  <files>CHANGELOG.md</files>

  <read_first>
    - `CHANGELOG.md` last 20 lines — the reference-link block. As measured at plan time it runs
      from the `[0.6.3]:` release-tag line through a final `[Unreleased]:` compare line, with
      release links in descending version order. Re-locate by content; line numbers shift once
      Task 1 inserts the new entry.
    - `.planning/phases/33-v0-6-4-release-prep/33-PATTERNS.md` — section `### CHANGELOG.md`,
      subsection `**Tail link block**`, which states both edits explicitly.
  </read_first>

  <action>
    Make two edits to the reference-link block at the end of the file.

    First, insert a new release-link definition for 0.6.4 pointing at
    `https://github.com/YuSabo90002/typsphinx/releases/tag/v0.6.4`, positioned immediately above
    the existing 0.6.3 release-link line so the block stays in descending version order.

    Second, rewrite the final `[Unreleased]:` compare line so its compare range starts from the
    0.6.4 tag instead of the 0.6.3 tag — the base moves forward to `v0.6.4...HEAD`. This is the
    carry-forward that SC#2 names explicitly as this phase's own work, not the milestone-close
    command's. Keep this line last in the file.

    Leave every other release-link line byte-identical.

    The newly added 0.6.4 release link will return 404 until `/gsd-complete-milestone` pushes the
    tag. That is expected and correct: v0.6.1, v0.6.2, and v0.6.3 each passed through the same
    transient state during their own prep phases. Do not "fix" it by pointing the link elsewhere,
    and do not create a tag to make it resolve — no tag is created in this phase under any
    circumstances.
  </action>

  <verify>
    <automated>grep -q '^\[0.6.4\]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.6.4$' CHANGELOG.md &amp;&amp; grep -n '^\[0.6' CHANGELOG.md | head -2 | awk -F'[][]' 'NR==1&amp;&amp;$2!="0.6.4"{exit 1} NR==2&amp;&amp;$2!="0.6.3"{exit 1} END{exit 0}' &amp;&amp; tail -1 CHANGELOG.md | grep -qx '\[Unreleased\]: https://github.com/YuSabo90002/typsphinx/compare/v0.6.4...HEAD' &amp;&amp; test -z "$(git tag -l v0.6.4)" &amp;&amp; echo TAIL_OK</automated>
  </verify>

  <acceptance_criteria>
    - `CHANGELOG.md` contains the line
      `[0.6.4]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.6.4`.
    - The first two `^\[0.6` reference lines in the file are `[0.6.4]:` then `[0.6.3]:`.
    - The file's final line is
      `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.6.4...HEAD`.
    - `grep -c 'compare/v0.6.3...HEAD' CHANGELOG.md` returns 0.
    - `git diff CHANGELOG.md` for the tail block shows exactly 1 inserted line and 1 modified
      line; the release-link lines for 0.6.2 and earlier are unchanged.
    - `git tag -l v0.6.4` produces empty output — this task created no tag.
  </acceptance_criteria>

  <done>
    The tail block lists 0.6.4 above 0.6.3, the Unreleased compare base has moved forward to the
    0.6.4 tag, older links are untouched, and no tag exists in the repository.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG entry -> published GitHub Release body | This text is copied verbatim into a public release announcement by `/gsd-complete-milestone`; a false claim here becomes a published false claim. |
| CHANGELOG entry -> downstream user upgrade decisions | Readers decide whether an upgrade is safe based on the presence or absence of breaking-change language. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-03 | Spoofing | `### Verified` section claims | medium | mitigate | D-03 restricts the section to three invariants that `git diff` mechanically proves, and plan 33-04 records the live diff output backing each one. No claim is admitted that lacks a re-runnable verification mechanism. |
| T-33-04 | Repudiation | omission of breaking-change disclosure | medium | mitigate | D-01 removes the BREAKING label only because no packaged behavior changed; the user-visible loss (github.io URLs returning 404 with no redirect, and the removal of browser-language auto-redirection) is disclosed in the Removed section body, and Task 1's acceptance criteria assert that disclosure is present. |
| T-33-05 | Tampering | historical CHANGELOG entries | low | mitigate | Task 1's acceptance criteria require zero deletions or modifications to lines belonging to release 0.6.3 and earlier; the historical github.io mention is explicitly preserved per REQUIREMENTS.md's Out of Scope table. |
| T-33-SC | Tampering | package-manager installs | low | accept | This plan installs no package and touches only Markdown. `33-RESEARCH.md`'s `## Package Legitimacy Audit` records the gate as not-applicable for this phase. |
</threat_model>

<verification>
- `grep -c '^## \[0.6.4\]' CHANGELOG.md` returns 1.
- The first three `^## \[` headings are `[Unreleased]`, `[0.6.4]`, `[0.6.3]` in that order.
- Within the `[0.6.4]` section, `grep -c 'BREAKING'` returns 0 and the five `### ` subsections
  appear in the order Added, Changed, Removed, Fixed, Verified.
- `tail -1 CHANGELOG.md` is the Unreleased compare line based at the 0.6.4 tag.
- `git tag -l v0.6.4` is empty.
</verification>

<success_criteria>
SC#2 of ROADMAP Phase 33 is discharged: a curated `## [0.6.4]` entry covers the milestone's
user-visible changes (hosting moved to Read the Docs, the Japanese site, the machinery and
orphan removals, the URL rewrites, and the accepted loss of browser-language auto-redirect),
and the tail release/compare link block is updated in the same phase with the Unreleased
compare carried forward.
</success_criteria>

<output>
Create `.planning/phases/33-v0-6-4-release-prep/33-02-SUMMARY.md` when done.
Record the resolved `<DATE>` used in the heading and the verbatim tail-block diff — plan 33-04
cites both as SC#2 evidence.
</output>
