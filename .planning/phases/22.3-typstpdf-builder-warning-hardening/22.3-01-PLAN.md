---
phase: 22.3-typstpdf-builder-warning-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typsphinx/builder.py
  - tests/test_pdf_generation.py
  - .planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md
autonomous: true
requirements: [WR-01]
user_setup: []

must_haves:
  truths:
    - "D-01/D-05: a `sphinx-build -b typstpdf` run in which every configured master either has no generated `.typ` or is a malformed `typst_documents` entry raises `sphinx.errors.ExtensionError` instead of returning normally — no configuration of `typst_documents` can produce zero PDFs while `finish()` returns without raising."
    - "D-02: every failure kind (compile exception, missing `.typ`, malformed entry) is accumulated into the single existing `failures: List[Tuple[str, str]]` and reported by one aggregate `ExtensionError` raised after the loop; no entry short-circuits the loop, and a master that compiles still gets its `.pdf` written."
    - "D-04 adjacency edge (probe WR-01/adjacency): a missing-`.typ` master whose docname IS in `self.env.found_docs` produces the byte-identical fallback text `Master document not found: {typ_file}`, while a docname ABSENT from `found_docs` produces a distinct text that does not contain the substring `Master document not found` — the two branches can never emit the same message, and `tests/test_target_name_render_gate.py` stays green and unedited."
    - "WR-01/empty edge: an empty `typst_documents` list still takes the unchanged whole-project early return (warning + `return`, exit 0) and is NOT a per-master failure; a falsy per-entry value such as `()` IS a failure; a 1-element entry such as `('valid',)` is NOT malformed and resolves its stem from the docname."
    - "WR-01/ordering edge (probe WR-01/ordering): `failures` is accumulated strictly in `typst_documents` iteration order and the aggregate message renders that same order, so with entries [unknown-docname, malformed, known-docname-missing-typ] the three summary fragments appear in exactly that sequence."
    - "WR-01/encoding edge (probe WR-01/encoding): a non-ASCII docname absent from `found_docs` is reported in the aggregate `ExtensionError` as a Python `str` with its code points intact via `!r`, raising no UnicodeEncodeError and producing no mojibake."
    - "D-02 wording: the aggregate `ExtensionError` no longer restricts itself to compile failures — its f-string terminates in `master document(s) failed: {summary}`."
    - "D-06 scope fence: the malformed guard's detection condition remains exactly `if not doc_tuple:`; the non-str-docname raw `TypeError` is recorded as a filed follow-up todo, not fixed."
  prohibitions:
    - "The `finish()` docstring's no-silent-success claim must never be weakened to match the old tolerant behavior instead of the behavior being fixed — D-01 rejected the documentary resolution outright, and a weakened docstring would make the defect permanent while looking resolved."
    - "The new build-failing behavior must never be placed behind a new opt-in config value or a deprecation shim that lets existing silently-broken builds keep reporting success — D-01/D-03 lock a clean break, and an opt-out would preserve the exact harm the phase exists to remove."
  artifacts:
    - "typsphinx/builder.py — `TypstPDFBuilder.finish()` with three `failures.append(...)` call sites and a broadened aggregate message"
    - "tests/test_pdf_generation.py — four new unit tests pinning the two new failure kinds, the D-04 message split, and the aggregate ordering/wording"
    - ".planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md — the D-06 deferral, filed rather than dropped"
  key_links:
    - "`self.env.found_docs` (read-only, populated by Sphinx core before `finish()` runs) is the sole discriminator for the D-04 message split — the builder must never write to it or re-derive it from the source tree"
    - "the existing `failures: List[Tuple[str, str]]` declared at builder.py:902 is the single accumulation point; the aggregate raise at builder.py:943-948 is its only consumer"
    - "the fallback string `Master document not found: {typ_file}` is load-bearing for the pre-existing negative assertion at tests/test_target_name_render_gate.py:122"
---

<objective>
Close WR-01 in `typsphinx/builder.py`: make `TypstPDFBuilder.finish()` stop reporting a successful
build while silently emitting no PDF for a configured master. The two `logger.warning(...); continue`
branches inside the `typst_documents` loop — the malformed-entry guard (D-05/D-07) and the missing-`.typ`
guard (D-01/D-04) — start feeding the existing `failures` list, the aggregate `ExtensionError` message
widens past "compile" (D-02), and the `finish()` docstring's "no silent success" claim becomes true.
The known non-str-docname defect is deferred per D-06 and filed as a todo rather than dropped.

Tests come first (Task 1) so their RED run against the unmodified builder is itself one of the two
independent pre-fix must-fail proofs ROADMAP SC#5 requires (22.1 D-08 precedent); the D-11 GATE-01
subprocess gate in plan 22.3-03 is the second.

Purpose: a build that produces no PDF for a configured master must exit non-zero, because the
downstream consumer of that PDF otherwise receives nothing with no signal at all.
Output: a fail-loud `finish()` loop, four new unit tests, a filed deferral todo, and the exact
Phase 23 `[0.6.2]` CHANGELOG sentence staged in `<handoffs>` (D-03).
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-CONTEXT.md
@.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-RESEARCH.md
@.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-PATTERNS.md
@CLAUDE.md
</context>

<line_number_warning>
**`22.3-CONTEXT.md` and `.planning/REQUIREMENTS.md` §WR-01 cite `builder.py` line numbers that have
drifted by roughly 20-27 lines. Do not use them.** RESEARCH.md § "Pitfall 1" re-verified the real
coordinates twice:

| What | STALE (CONTEXT/REQUIREMENTS — ignore) | ACTUAL (use) |
|------|------|------|
| `finish()` def | — | **867** |
| `finish()` docstring | 850-856 | **868-886** |
| whole-project-empty early return | — | **894-898** (do NOT touch) |
| `failures` declaration | 900 | **902** |
| malformed-`doc_tuple` guard | 885-890 | **912-916** |
| missing-`.typ` guard | 895-897 | **922-924** |
| existing `except` + `failures.append` | — | **939-941** |
| aggregate raise | 943-947 | **939-948** |

Lines 895-897 in the current file are the **unrelated whole-project-empty early return**. Editing
them would be a wrong-target change that still looks plausible. Always re-read
`typsphinx/builder.py` before editing and locate the guards by their code, not by line number.
</line_number_warning>

<worktree_setup>
Worktree isolation is the standing execution mode for this project (`workflow.use_worktrees: true`,
`worktree.baseRef: "head"`). Before running ANY command in this plan:

```
env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev
```

Then run every command through `uv run`. Without this, pytest imports the UNCHANGED main-tree
`typsphinx` package and every gate stays red after a correct fix. Never invoke a compiled binary
directly under `uv run` (NixOS sandbox); `uv run pytest ...` is correct.

Pre-change baseline recorded during research: `uv run pytest -q -m "not slow"` → **560 passed,
23 deselected**. Any deviation not explained by a test this plan adds is a regression.
</worktree_setup>

<artifacts_produced>
## Artifacts this phase produces (new symbols — plan 22.3-01)

Newly created in this plan; they do not exist anywhere in the tree before it and must not be
treated as drifted references to existing code:

- `tests/test_pdf_generation.py::TestPDFErrorHandling::test_builder_appends_failure_for_missing_typ_file`
- `tests/test_pdf_generation.py::TestPDFErrorHandling::test_builder_appends_failure_for_unknown_docname`
- `tests/test_pdf_generation.py::TestPDFErrorHandling::test_builder_appends_failure_for_malformed_entry_but_not_short_entry`
- `tests/test_pdf_generation.py::TestPDFErrorHandling::test_aggregate_message_lists_failures_in_typst_documents_order`
- `.planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md`

Sibling plans in this phase additionally produce: `tests/fixtures/missing_and_malformed_master_gate/`
(`conf.py`, `index.rst`), `tests/test_missing_and_malformed_master_gate.py` (including
`_run_sphinx_build`, `GATE_FIXTURE_DIR`, `TestMissingAndMalformedMasterGate`), and the new local
names `fully_neutralized_text` / `fully_neutralized_copy` / `red_half_error` / `root_usage_copy`
inside `tests/test_nested_master_render_gate.py`.
</artifacts_produced>

<handoffs>
Hand-off obligations. These are NOT tasks in this plan — record them verbatim in
`22.3-01-SUMMARY.md` so Phase 23 and `/gsd-verify-work` do not have to rediscover them.

- **D-03 — Phase 23 `[0.6.2]` CHANGELOG sentence (drafted here, authored there).** Add to the
  existing 22.1 D-05a bullet (the one that already records "a PDF compile failure now fails the
  build"), extending it to read:

  > `sphinx-build -b typstpdf` now fails the build for **any** configured master that produces no
  > PDF — not only a Typst compile error, but also a master whose `.typ` file was never generated
  > (typically a `typst_documents` docname that is not a real Sphinx document) and a malformed
  > `typst_documents` entry. All failures are reported together in one error after every master has
  > been attempted; masters that do compile still get their `.pdf`. Builds that previously
  > "succeeded" while silently producing no PDF for such a master now exit non-zero. Phrase as a
  > fix, not merely a behavior change — the previous outcome was a missing deliverable reported as
  > success.

  No compatibility shim, no deprecation period, no new opt-in config value (D-01/D-03).

- **Source-todo closure (CONTEXT.md § Folded Todos).**
  `.planning/todos/pending/2026-07-22-wr01-silent-missing-pdf-wr02-typst-error-substring-coupling.md`
  is this phase's source of record and must be closed at phase completion (at `/gsd-verify-work`,
  after both WR-01 and WR-02 verify green). Do not close it inside this plan — WR-02 is not done
  here.

- **D-06 deferral.** The non-str-docname raw `TypeError` is filed as a new todo by Task 3 and stays
  unfixed in this phase.
</handoffs>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the four RED unit tests pinning WR-01's new failure contract (D-01/D-04/D-05/D-07/D-02)</name>
  <files>tests/test_pdf_generation.py</files>

  <read_first>
    - `tests/test_pdf_generation.py` — especially the `TestPDFErrorHandling` class and the two
      existing sibling tests at lines 262-313 (`test_builder_attempts_every_master_then_raises`,
      `test_builder_still_writes_pdf_for_masters_that_compile`). Their construction shape
      (`TypstPDFBuilder(temp_sphinx_app, temp_sphinx_app.env)`, `builder.outdir = str(tmp_path)`,
      assign `builder.config.typst_documents`, write `.typ` files into `tmp_path`, call
      `builder.finish()`) is the exact shape to reuse — do not invent a new harness.
    - `tests/conftest.py` — the `temp_sphinx_app` fixture (lines 45-80). Its `srcdir` contains
      `conf.py` and `index.rst`, so `temp_sphinx_app.env.found_docs` is expected to contain
      `"index"` and not `"ghost"`. Assert that as an explicit precondition rather than assuming it.
    - `typsphinx/builder.py` — the whole `finish()` body (currently 867-948) so the tests target the
      real control flow, not the drifted coordinates in CONTEXT.md.
    - `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-RESEARCH.md` § "Code Examples"
      for the exact post-fix message shapes these tests pin.
  </read_first>

  <behavior>
    Four new tests in `TestPDFErrorHandling`, all failing against the unmodified `finish()`:

    - `test_builder_appends_failure_for_missing_typ_file` (D-01, D-04 fallback branch): precondition
      `"index" in temp_sphinx_app.env.found_docs`; `typst_documents = [("index", "index", "T", "A")]`;
      no `.typ` written into `tmp_path`. Expect `ExtensionError`; its text contains
      `Master document not found:` and does NOT contain `is not a known Sphinx document`.
    - `test_builder_appends_failure_for_unknown_docname` (D-04 new branch, adjacency + encoding
      edges): precondition `"ghost" not in temp_sphinx_app.env.found_docs`; two entries — a plain
      `("ghost", "ghost", "T", "A")` and a non-ASCII `("日本語ドキュメント", "日本語ドキュメント",
      "T", "A")`. Expect `ExtensionError`; its text contains `'ghost'`, contains
      `is not a known Sphinx document`, contains `日本語ドキュメント` verbatim, and does NOT contain
      `Master document not found` (the two D-04 branches must never collide).
    - `test_builder_appends_failure_for_malformed_entry_but_not_short_entry` (D-05, D-07, empty and
      single-element edges): `typst_documents = [(), ("valid",)]` with `tmp_path/"valid.typ"`
      written as `= Valid Document\n\nContent here.\n`. Expect `ExtensionError` whose text contains
      `1 master document(s) failed` and `(): malformed typst_documents entry`; and
      `(tmp_path / "valid.pdf").exists()` is True — a 1-element entry is not malformed and its stem
      falls back to the docname.
    - `test_aggregate_message_lists_failures_in_typst_documents_order` (D-02, ordering edge):
      `typst_documents = [("ghost", "ghost", "T", "A"), (), ("index", "index", "T", "A")]`, no
      `.typ` files written. Expect `ExtensionError` whose text contains `3 master document(s) failed`,
      does NOT contain `failed to compile to PDF`, and whose `.index()` positions satisfy
      `pos("ghost") < pos("(): malformed typst_documents entry") < pos("Master document not found:")`
      — proving `failures` renders in `typst_documents` iteration order.
  </behavior>

  <action>
Append the four tests described in `<behavior>` to the `TestPDFErrorHandling` class in
`tests/test_pdf_generation.py`, immediately after `test_builder_still_writes_pdf_for_masters_that_compile`.
Reuse the existing `temp_sphinx_app` + `tmp_path` fixtures and the existing import of `ExtensionError`
at the top of the module; import `TypstPDFBuilder` inside each test body exactly as the sibling tests
do. Each test's docstring must name the decision it pins (D-01, D-04, D-05, D-07, D-02) and, where
applicable, the probe edge (adjacency, empty, single-element, ordering, encoding).

Every assertion must carry an explanatory message string, matching the module's house style. Assert
only on typsphinx's OWN authored message text — never on a Typst compiler diagnostic.

Then run the four tests and confirm they FAIL against the unmodified `typsphinx/builder.py`. Capture
the failure output (which assertion fired, and the fact that `finish()` returned without raising for
the three no-compile cases) into a scratch note; it becomes half of the ROADMAP SC#5 pre-fix
must-fail evidence and must be transcribed into `22.3-01-SUMMARY.md`.

Commit this task as a `test(22.3-01): ...` commit with the tests RED — that is the intended TDD
state, not a mistake.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest tests/test_pdf_generation.py -k "missing_typ_file or unknown_docname or malformed_entry or typst_documents_order" ; [ $? -ne 0 ]</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'def test_builder_appends_failure_for_missing_typ_file' tests/test_pdf_generation.py` returns 1
    - `grep -c 'def test_builder_appends_failure_for_unknown_docname' tests/test_pdf_generation.py` returns 1
    - `grep -c 'def test_builder_appends_failure_for_malformed_entry_but_not_short_entry' tests/test_pdf_generation.py` returns 1
    - `grep -c 'def test_aggregate_message_lists_failures_in_typst_documents_order' tests/test_pdf_generation.py` returns 1
    - `uv run pytest tests/test_pdf_generation.py -k "missing_typ_file or unknown_docname or malformed_entry or typst_documents_order"` exits NON-ZERO, and the captured output shows all four tests failing (this is the required RED state)
    - `uv run pytest tests/test_pdf_generation.py -k "attempts_every_master or still_writes_pdf"` exits 0 — the two pre-existing sibling tests are untouched and still green
    - `uv run black --check tests/test_pdf_generation.py` exits 0 and `uv run ruff check tests/test_pdf_generation.py` exits 0
    - `git diff --name-only` lists only `tests/test_pdf_generation.py`
  </acceptance_criteria>

  <done>
Four new tests exist in `TestPDFErrorHandling`, all four fail against the unmodified builder for the
right reason (no `ExtensionError` raised / wrong message), the two pre-existing sibling tests still
pass, lint and format are clean, and the RED output is captured for the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make finish() fail loudly for missing-.typ and malformed entries (D-01/D-02/D-04/D-05/D-06/D-07)</name>
  <files>typsphinx/builder.py</files>

  <read_first>
    - `typsphinx/builder.py` — read `finish()` in full (currently 867-948) plus
      `_directory_preserving_relpath` (239-267), `_resolve_output_stem` (132-237) and
      `get_outdated_docs` (269-279). Locate the two guards by their code
      (`if not doc_tuple:` and `if not path.exists(typ_file):`), never by the drifted line numbers.
    - `tests/test_target_name_render_gate.py` line 122 — the pre-existing negative assertion
      `assert "Master document not found" not in result.stderr`. This file must stay green and
      MUST NOT be edited; it is why the fallback text is byte-frozen (RESEARCH § "Pitfall 5").
    - `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-PATTERNS.md` § "`typsphinx/builder.py`"
      — the verbatim current excerpt and the `failures.append` shape to mirror.
    - `tests/test_pdf_generation.py` — the four RED tests from Task 1, which define the exact message
      substrings this task must produce.
  </read_first>

  <action>
Make four edits inside `TypstPDFBuilder.finish()`. Keep the diff minimal — no new helper, no new
data structure, no change to the `attempt-all-then-raise` shape (D-02 preserves 22.1 D-05).

1. **Malformed-entry guard (currently 912-916) — D-05, D-07.** Leave the detection condition exactly
   `if not doc_tuple:` (D-06 forbids widening it). Reword the existing `logger.warning` f-string from
   its current "Skipping malformed" phrasing to `Malformed typst_documents entry: {doc_tuple!r}`,
   since the entry is no longer merely skipped. Immediately before the existing `continue`, add
   `failures.append((repr(doc_tuple), "malformed typst_documents entry"))` — the identifier is the
   entry's own `repr` because a falsy entry has no docname (D-07).

2. **Missing-`.typ` guard (currently 922-924) — D-01, D-04.** Replace the single unconditional
   `logger.warning` with a two-branch message computation, then one `logger.warning(message)`, then
   `failures.append((docname, message))`, then the existing `continue`:
   - when `docname not in self.env.found_docs`: `message` is an f-string reading
     `Master document {docname!r} is not a known Sphinx document -- check typst_documents for a
     typo, a stray '.rst' suffix, or an exclude_patterns exclusion`. It must NOT contain the
     substring `Master document not found`.
   - otherwise: `message` is `f"Master document not found: {typ_file}"` — byte-identical to today's
     string. Do not reflow, reword, or re-punctuate it; `tests/test_target_name_render_gate.py:122`
     depends on it.
   `self.env.found_docs` is read-only here — never assign to it and never re-derive document
   discovery from `srcdir`.

3. **Aggregate raise (currently 943-948) — D-02.** Widen the message past compilation: the f-string
   must end with `master document(s) failed: {summary}`. Keep the `typstpdf:` prefix exactly
   (no underscore), keep `len(failures)`, and keep the `"; ".join(f"{docname}: {err}" ...)` summary
   construction unchanged — `failures` remains a `List[Tuple[str, str]]` regardless of which branch
   appended.

4. **Docstring (currently 868-886) — D-01.** Extend the existing "no silent success" paragraph so it
   names all three failure kinds explicitly: a Typst compile error, a configured master whose `.typ`
   file was never generated, and a malformed `typst_documents` entry. State that all three are
   collected and reported together by one `ExtensionError` raised after every entry has been
   attempted, and that masters which do compile still get their `.pdf`. Do not weaken the claim.

Do NOT touch the whole-project `if not typst_documents:` early return (currently 894-898) — an empty
`typst_documents` list stays a warning plus `return`, which is a whole-project no-op and not a
per-master failure. Do NOT add non-str-docname handling: that raw `TypeError` at
`_directory_preserving_relpath` is deferred by D-06 and is filed as a todo in Task 3.

Then run the four Task 1 tests and confirm they are now GREEN, and run the fast regression set.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest tests/test_pdf_generation.py tests/test_target_name_render_gate.py tests/test_builder_output_stem.py tests/test_nested_master_render_gate.py -x</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'failures.append' typsphinx/builder.py` returns 3
    - `grep -c 'if not doc_tuple:' typsphinx/builder.py` returns 1 — the guard's detection surface was not widened (D-06)
    - `grep -c 'Master document not found: {typ_file}' typsphinx/builder.py` returns 1 — the fallback text survives byte-identical (Pitfall 5)
    - `grep -c 'is not a known Sphinx document' typsphinx/builder.py` returns 1 — the D-04 discriminating branch exists
    - `grep -c 'self.env.found_docs' typsphinx/builder.py` returns 1 and the line is a membership test, not an assignment
    - `grep -c 'master document(s) failed: {summary}' typsphinx/builder.py` returns 1
    - `grep -c 'to PDF: {summary}' typsphinx/builder.py` returns 0 — the aggregate message no longer restricts itself to compilation (D-02)
    - `grep -c 'No documents defined in typst_documents' typsphinx/builder.py` returns 1 — the whole-project early return is untouched
    - `uv run pytest tests/test_pdf_generation.py -x` exits 0 (all four new tests GREEN, both pre-existing sibling tests still GREEN)
    - `uv run pytest tests/test_target_name_render_gate.py -x` exits 0 AND `git diff --name-only` does not list `tests/test_target_name_render_gate.py`
    - `uv run pytest tests/test_nested_master_render_gate.py tests/test_builder_output_stem.py tests/test_preview_version_sync.py -x` exits 0
    - `uv run black --check typsphinx/builder.py` exits 0, `uv run ruff check typsphinx/builder.py` exits 0, `uv run mypy typsphinx/` exits 0
    - `git diff --name-only` lists only `typsphinx/builder.py`
  </acceptance_criteria>

  <done>
`finish()` appends to `failures` at all three sites, the D-04 message split discriminates on
`found_docs` with a byte-frozen fallback, the aggregate message covers non-compile failures, the
docstring names all three failure kinds, and the four Task 1 tests plus the whole fast regression set
are green with `test_target_name_render_gate.py` unedited.
  </done>
</task>

<task type="auto">
  <name>Task 3: File the D-06 deferral todo and run the full-suite regression check</name>
  <files>.planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md</files>

  <read_first>
    - `.planning/todos/pending/2026-07-22-wr01-silent-missing-pdf-wr02-typst-error-substring-coupling.md`
      — this phase's source of record; copy its front-matter/heading shape for the new todo.
    - `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-RESEARCH.md` § "Pitfall 4" —
      the reproduced traceback and the exact crash site to transcribe.
    - `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-CONTEXT.md` § `<deferred>` —
      the three deferred items whose scope this todo must respect.
  </read_first>

  <action>
Create `.planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md`, matching
the structure of the existing pending todos in that directory (title heading, a captured/source line,
a repro, and an explicit statement of what is and is not in scope).

Content requirements — record, do not fix:
- A non-str docname in `typst_documents` (e.g. `[(123, "manual.typ", "T", "A")]`) crashes
  `TypstPDFBuilder.finish()` with an uncaught `TypeError`, bypassing the aggregate `ExtensionError`
  entirely and producing a raw traceback instead of a typsphinx-authored diagnostic.
- The verified call chain: `finish()` calls `self._directory_preserving_relpath(docname, stem)`,
  which reaches `path.dirname(docname)` and raises
  `TypeError: expected str, bytes or os.PathLike object, not int`. This happens BEFORE the `try:`
  block that would have routed it into `failures` — that is why it escapes the aggregation.
- Why it is out of Phase 22.3's scope: D-06 fences the malformed guard's detection surface to the
  existing `if not doc_tuple:`; WR-01's complaint was that detected skips never reached `failures`,
  not that some inputs go undetected. This is a distinct input-type-validation defect.
- Note the sibling deferrals it belongs with (comprehensive `typst_documents` shape validation, and
  `difflib`-based "did you mean" suggestions for an unknown docname), both recorded in
  `22.3-CONTEXT.md` § `<deferred>`.
- State that the failure is loud (non-zero exit, no silent success), so it does not reopen WR-01.

Then run the full test suite and compare against the recorded baseline.
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest -q -m "not slow"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md` succeeds
    - `grep -c '_directory_preserving_relpath' .planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md` returns at least 1
    - `grep -c 'D-06' .planning/todos/pending/2026-07-22-non-str-docname-typeerror-in-typstpdf-finish.md` returns at least 1
    - `uv run pytest -q -m "not slow"` exits 0 and reports **564 passed, 23 deselected** (the 560-passed research baseline plus the four tests added by Task 1). Any other passed count, or any failure, is a regression and must be explained before the plan is considered done.
    - `git status --porcelain tests/` is empty — the suite left no stray artifacts in the repo tree
    - `git diff --name-only` across the whole plan lists exactly: `typsphinx/builder.py`, `tests/test_pdf_generation.py`, and the new todo file
    - The `<handoffs>` block above (the D-03 CHANGELOG sentence and the source-todo closure obligation) is transcribed verbatim into `22.3-01-SUMMARY.md`
  </acceptance_criteria>

  <done>
The D-06 deferral is filed as a pending todo with its verified call chain and scope rationale, the
full non-slow suite is green at 564 passed / 23 deselected, and both hand-off obligations are
recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` → typsphinx builder | Developer-authored `typst_documents` entries cross into build orchestration. `conf.py` is fully-trusted arbitrary Python that Sphinx executes before typsphinx runs; this boundary is a correctness boundary, not a security boundary, and this phase does not change its trust level. |
| typsphinx builder → typst-py / Rust compiler | The `try` block around `compile_typst_file_to_pdf` is the only place typsphinx code meets the external compiler's error surface. Unchanged by this plan. |
| build process → CI / downstream PDF consumer | The process exit code and stderr are the only signals a downstream consumer of the PDF artifact receives. This is the boundary WR-01 is about. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22.3-01 | Denial of Service (availability by omission) | `TypstPDFBuilder.finish()` — the two `logger.warning(...); continue` branches | high | mitigate | Task 2 appends both branches to `failures`, so a build that emits no PDF for a configured master exits non-zero instead of reporting success. Pinned by the four unit tests in Task 1 (RED-before-GREEN) and by the D-11 subprocess gate in plan 22.3-03. |
| T-22.3-02 | Information Disclosure | aggregate `ExtensionError` embedding `repr(doc_tuple)` and the docname into stderr | low | accept | The values are developer-authored `conf.py` literals already visible to anyone who can read the build log or the repository. No secret material is reachable from `typst_documents`. Surfacing them is the diagnostic value D-07 asks for. |
| T-22.3-03 | Denial of Service | non-str docname → raw `TypeError` outside the `try` (deferred by D-06) | low | accept | Deferred deliberately, not silently: Task 3 files it as a pending todo with its verified call chain. The failure mode is loud (non-zero exit, full traceback), so it cannot reintroduce the silent-success class WR-01 closes. |
| T-22.3-04 | Tampering (input validation, ASVS V5) | `typst_documents` malformed shapes | medium | mitigate | The `if not doc_tuple:` guard now reports rather than swallows (D-05/D-07). Detection surface is deliberately unchanged (D-06) so this phase's diagnostic delta stays minimal and reviewable. |
| T-22.3-SC | Tampering (supply chain) | npm/pip/cargo installs | high | accept | Not applicable — this plan installs zero packages and adds zero runtime dependencies (milestone invariant, ROADMAP SC#5). `22.3-RESEARCH.md` § "Package Legitimacy Audit" records the gate as skipped by its own trigger condition. `uv sync --extra dev` installs only already-locked project dev dependencies. |

ASVS level 1; block-on threshold `high`. T-22.3-01 is the only `high` threat and it is mitigated in
this plan. T-22.3-SC is `high` by template but has no applicable surface (zero installs).
</threat_model>

<verification>
1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` — worktree provisioning (mandatory first step)
2. `uv run pytest tests/test_pdf_generation.py -x` → 0
3. `uv run pytest tests/test_target_name_render_gate.py tests/test_builder_output_stem.py tests/test_nested_master_render_gate.py tests/test_preview_version_sync.py -x` → 0
4. `uv run pytest -q -m "not slow"` → **564 passed, 23 deselected**
5. `uv run black --check .` → 0; `uv run ruff check .` → 0; `uv run mypy typsphinx/` → 0
6. `git diff --name-only` lists exactly `typsphinx/builder.py`, `tests/test_pdf_generation.py`, and the new todo file — in particular NOT `typsphinx/translator.py`, NOT `typsphinx/writer.py`, NOT `typsphinx/template_engine.py`, NOT `typsphinx/templates/base.typ`, NOT `tests/test_target_name_render_gate.py`, NOT `tests/test_corpus_gate.py`
</verification>

<success_criteria>
- ROADMAP SC#1: a configured master whose `.typ` was never generated makes `finish()` raise instead of returning; pinned by `test_builder_appends_failure_for_missing_typ_file` and `test_builder_appends_failure_for_unknown_docname`
- ROADMAP SC#2: the malformed-entry branch is aligned with the missing-`.typ` branch, both appending to the same `failures` list; pinned by `test_builder_appends_failure_for_malformed_entry_but_not_short_entry`
- ROADMAP SC#3: the behavioral resolution is staged for the Phase 23 `[0.6.2]` CHANGELOG entry via `<handoffs>`
- ROADMAP SC#5 (half): the four unit tests demonstrably fail against pre-fix `builder.py` and the RED output is transcribed into the SUMMARY; zero new runtime deps, no `@preview` bump, `tests/test_preview_version_sync.py` and `tests/test_corpus_gate.py` untouched and green
- `typsphinx/translator.py` has zero changes
</success_criteria>

<output>
Create `.planning/phases/22.3-typstpdf-builder-warning-hardening/22.3-01-SUMMARY.md` when done,
including the captured RED output from Task 1 under an explicit "SC#5 pre-fix must-fail proof
(unit half)" heading and the two `<handoffs>` obligations verbatim.
</output>
