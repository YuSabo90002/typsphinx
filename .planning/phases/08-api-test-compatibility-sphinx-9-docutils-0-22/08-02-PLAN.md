---
phase: 08-api-test-compatibility-sphinx-9-docutils-0-22
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_builder.py
  - tests/test_pdf_generation.py
  - tests/test_documentation_configuration.py
  - tests/test_documentation_usage.py
autonomous: true
requirements: [API-02]
must_haves:
  truths:
    - "tests/test_builder.py and tests/test_pdf_generation.py assert builder._app (not builder.app); no builder.app property access remains (SC2)"
    - "tests/test_documentation_configuration.py and tests/test_documentation_usage.py call publish_string with writer=get_writer_class('html')(); no writer_name= string argument remains (SC2)"
    - "pytest on these 4 files under -W error::PendingDeprecationWarning passes — the Sphinx RemovedInSphinx11Warning (builder.app) and the docutils writer_name PendingDeprecationWarning no longer fire (SC2)"
  artifacts:
    - "tests/test_builder.py, tests/test_pdf_generation.py (modified: builder._app)"
    - "tests/test_documentation_configuration.py, tests/test_documentation_usage.py (modified: writer= + get_writer_class import)"
  key_links:
    - "each file's existing right-hand-side variable (app / temp_sphinx_app) is preserved; only the .app accessor changes to ._app"
    - "RemovedInSphinx11Warning subclasses PendingDeprecationWarning (NOT DeprecationWarning) — these fixes are only caught when PendingDeprecationWarning is escalated, which is why the Plan 03 guard escalates both classes"
---

<objective>
Land the Sphinx-9 `builder.app` and docutils-0.22 `writer_name` test-file modernizations — the second half of the 6-edit deprecation sweep that satisfies D-01 (thorough audit — modernize ALL soft-deprecated docutils/Sphinx API). All 4 sites are exact mirror-pairs (2 files × `builder.app`, 2 files × `writer_name`), each a 1-line drop-in.

Purpose: SC2 (Sphinx 9.1 + docutils 0.22 compatibility, no deprecation-removal breakage). These edits land in Wave 1 alongside Plan 01, BEFORE the `filterwarnings` guard (Plan 03, Wave 2), so enabling the guard leaves the suite green (D-02 ordering constraint).

Output: 4 test files modernized off the `builder.app` property (`RemovedInSphinx11Warning`, sphinx#13627) and the `publish_string(writer_name=...)` string argument (deprecated ahead of docutils 2.0).
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-RESEARCH.md
@.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-PATTERNS.md
</context>

## Artifacts this phase produces

All changes in this plan are modifications to existing test assertions — no new public symbols. (The only NEW artifact across the whole phase is the `filterwarnings` ini key added to `pyproject.toml` in Plan 03.)

This plan's changes:
- `tests/test_builder.py` / `tests/test_pdf_generation.py` — the `builder.app` property read in a fixture assertion switches to the non-deprecated `builder._app` (the private attribute the deprecated `.app` property itself returns). `typsphinx/builder.py` runtime source never accesses `.app`, so this is test-assertion-only.
- `tests/test_documentation_configuration.py` / `tests/test_documentation_usage.py` — the `publish_string(..., writer_name="html", ...)` call switches to `publish_string(..., writer=get_writer_class("html")(), ...)`, adding a `from docutils.writers import get_writer_class` import.

<tasks>

<task type="auto">
  <name>Task 1: builder.app → builder._app in test_builder.py + test_pdf_generation.py (API-02)</name>
  <files>tests/test_builder.py, tests/test_pdf_generation.py</files>
  <read_first>
    - tests/test_builder.py line 64 (`assert builder.app == app`) — the file being modified
    - tests/test_pdf_generation.py line 80 (`assert builder.app == temp_sphinx_app`) — the mirror site being modified
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-PATTERNS.md §"tests/test_builder.py / tests/test_pdf_generation.py — Sphinx 9 builder.app deprecation"
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-RESEARCH.md §"Pattern 3: builder.app → avoid the deprecated property" (note: RemovedInSphinx11Warning subclasses PendingDeprecationWarning)
  </read_first>
  <action>
    Change the accessor `builder.app` → `builder._app` at exactly two sites, preserving each file's existing right-hand-side variable:
    (1) `tests/test_builder.py:64` — `assert builder.app == app` becomes `assert builder._app == app` (RHS `app` unchanged).
    (2) `tests/test_pdf_generation.py:80` — `assert builder.app == temp_sphinx_app` becomes `assert builder._app == temp_sphinx_app` (RHS `temp_sphinx_app` unchanged).
    `_app` is the underlying non-deprecated attribute the deprecated `.app` property returns (Sphinx 9 deprecated all public `.app` attributes, sphinx-doc/sphinx#13627, `RemovedInSphinx11Warning`). Do NOT change the RHS operands, do NOT add compatibility branching, and do NOT touch `typsphinx/builder.py` (its runtime source never accesses `.app` — grep-confirmed; this is test-assertion-only).
  </action>
  <verify>
    <automated>uv run pytest tests/test_builder.py tests/test_pdf_generation.py -q -W "error::PendingDeprecationWarning" -W "error::DeprecationWarning" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'builder\.app' tests/test_builder.py` returns `0` and `grep -c 'builder\.app' tests/test_pdf_generation.py` returns `0`.
    - `grep -c 'builder\._app' tests/test_builder.py` returns `1` and `grep -c 'builder\._app' tests/test_pdf_generation.py` returns `1`.
    - `uv run pytest tests/test_builder.py tests/test_pdf_generation.py -q -W "error::PendingDeprecationWarning" -W "error::DeprecationWarning" -x` exits 0.
    - NixOS-caveat note: if `tests/test_pdf_generation.py` reds with the `cannot start dynamically linked executable: uv` signature (a subprocess-launch failure — RESEARCH.md Pitfall 3), that is a pre-existing environment artifact, NOT an API-02 regression; confirm the `_app` assertion itself passes (the assertion is in-process). Never treat a Sphinx-9-shaped stack trace as environmental — only the exact NixOS launch signature qualifies.
  </acceptance_criteria>
  <done>Both files assert `builder._app`; zero `builder.app` reads remain; the two files pass under both escalated warning classes (or, if a subprocess test reds, the failure is confirmed to be the documented NixOS launch signature and the `_app` assertion itself is green).</done>
</task>

<task type="auto">
  <name>Task 2: publish_string writer_name → writer=get_writer_class in doc-example tests (API-02)</name>
  <files>tests/test_documentation_configuration.py, tests/test_documentation_usage.py</files>
  <read_first>
    - tests/test_documentation_configuration.py lines 97-108 (the `from docutils.core import publish_string` import at line 97 and the `publish_string(...)` call with `writer_name="html",` at line 106) — the file being modified
    - tests/test_documentation_usage.py lines 94-104 (the mirror site: import at line 94, `writer_name="html",` at line 103) — the mirror being modified
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-PATTERNS.md §"tests/test_documentation_configuration.py / tests/test_documentation_usage.py — docutils 0.22 writer_name deprecation"
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-RESEARCH.md §"Pattern 4: publish_string(writer_name=...) → publish_string(writer=...)"
  </read_first>
  <action>
    Apply the identical fix at both sites:
    (1) Add the import `from docutils.writers import get_writer_class` next to the existing `from docutils.core import publish_string` (test_documentation_configuration.py line 97; test_documentation_usage.py line 94).
    (2) In each `publish_string(...)` call, replace the keyword argument `writer_name="html",` with `writer=get_writer_class("html")(),` — note the double call: `get_writer_class("html")` returns the writer CLASS, and the trailing `()` instantiates it (docutils 0.22's `writer` argument accepts a component instance). Leave `source=content` and `settings_overrides={"report_level": 2}` and the `# Only report errors` comment unchanged. docutils 0.22 deprecated the `writer_name`/`reader_name`/`parser_name` STRING arguments in favor of the `writer`/`reader`/`parser` instance arguments (removal targeted for docutils 2.0). Output is identical; only the argument form changes. Do NOT add compatibility branching.
  </action>
  <verify>
    <automated>uv run pytest tests/test_documentation_configuration.py tests/test_documentation_usage.py -q -W "error::PendingDeprecationWarning" -W "error::DeprecationWarning" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'writer_name' tests/test_documentation_configuration.py` returns `0` and `grep -c 'writer_name' tests/test_documentation_usage.py` returns `0`.
    - `grep -c 'get_writer_class("html")()' tests/test_documentation_configuration.py` returns `1` and the same for `tests/test_documentation_usage.py` returns `1`.
    - `grep -c 'from docutils.writers import get_writer_class' tests/test_documentation_configuration.py` returns `1` and the same for `tests/test_documentation_usage.py` returns `1`.
    - `uv run pytest tests/test_documentation_configuration.py tests/test_documentation_usage.py -q -W "error::PendingDeprecationWarning" -W "error::DeprecationWarning" -x` exits 0 (these use `publish_string` in-process, so the NixOS subprocess caveat does not apply here).
  </acceptance_criteria>
  <done>Both files call `publish_string(..., writer=get_writer_class("html")(), ...)` with the new import; zero `writer_name` arguments remain; both files pass under both escalated warning classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → docutils publisher | Test-only `publish_string` invocations feed static fixture content through docutils' trusted publisher. No untrusted remote input; the operator is the test author. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Tampering | test-only publish_string / builder assertions | low | accept | Test-assertion-only edits; `get_writer_class` and the `_app` attribute are docutils/Sphinx's own trusted APIs. No runtime source, no input surface, no file I/O changes. RESEARCH.md Security Domain: phase is security-neutral. |
| T-08-SC | Tampering | package installs (pip) | high | accept | No new packages installed this phase (RESEARCH.md Standard Stack; Package Legitimacy Audit N/A). Supply-chain slopcheck gate does not apply. |
</threat_model>

<verification>
- All 4 files green under `uv run pytest tests/test_builder.py tests/test_pdf_generation.py tests/test_documentation_configuration.py tests/test_documentation_usage.py -q -W "error::PendingDeprecationWarning" -W "error::DeprecationWarning"`.
- `grep` gates: zero `builder.app`, zero `writer_name` across the 4 files.
- Do NOT enable the `pyproject.toml` guard here — Plan 03 owns that (Wave 2).
</verification>

<success_criteria>
- SC2 (partial): the Sphinx-9 `builder.app` (`RemovedInSphinx11Warning`) and docutils-0.22 `writer_name` deprecations are resolved in all 4 test files; no `AttributeError`/`TypeError`/deprecation-removal breakage against the resolved stack.
</success_criteria>

<output>
Create `.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-02-SUMMARY.md` when done.
</output>
