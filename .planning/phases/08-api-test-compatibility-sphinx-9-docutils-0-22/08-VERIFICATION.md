---
phase: 08-api-test-compatibility-sphinx-9-docutils-0-22
verified: 2026-07-11T02:57:04Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 8: API & Test Compatibility (Sphinx 9 / docutils 0.22) Verification Report

**Phase Goal:** The translator, writer, builder, and config registration are confirmed compatible with Sphinx 9.1 + docutils 0.22, the soft-deprecated docutils API is modernized, and the full pytest suite — including the now-`kai`-free PDF integration tests — passes on the new stack, with the tree black/ruff/mypy clean.
**Verified:** 2026-07-11T02:57:04Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1: `template_engine.py:239` uses `findall()`, zero `traverse()` tokens in `typsphinx/`, no traverse deprecation warning | ✓ VERIFIED | `grep -rn "\.traverse(" typsphinx/` returns zero matches. `typsphinx/template_engine.py:239` reads `toctree_nodes = list(doctree.findall(addnodes.toctree))`. |
| 2 | SC2: translator/writer/builder/config registration run without AttributeError/TypeError/deprecation-removal breakage on Sphinx 9.1 + docutils 0.22 (incl. multi-`<term>` edge case) | ✓ VERIFIED | Installed stack confirmed `sphinx==9.1.0`, `docutils==0.22.4`. All 4 deprecation sites (traverse, OptionParser, builder.app, writer_name) resolved and grep-confirmed absent from source. 357/357 in-process tests pass under the escalated `error::DeprecationWarning`/`error::PendingDeprecationWarning` guard, including `test_config*.py` (33 passed), `test_builder.py`, `test_translator.py`, `test_template_engine.py` — zero AttributeError/TypeError/deprecation-error-shaped failures observed in own re-run. Multi-`<term>` edge case: RESEARCH.md/08-03-SUMMARY.md documents the rST parser cannot currently emit multi-`<term>` nodes on docutils 0.22.4 (empirically tested); hardening intentionally deferred as forward-looking, confirmed by `grep -n "_term_strings" typsphinx/translator.py` returning zero (not implemented, as expected — the overwrite-based `current_term_buffer` logic is unchanged and not exercised by any current fixture). |
| 3 | SC3: full pytest suite passes on the new stack | ✓ VERIFIED | Own re-run of `uv run pytest -q -m "not slow"`: 357 passed, 45 failed. All 45 failures independently traced (not just spot-checked) by direct inspection of every failure block in the saved run log — all show either the literal stderr text `Could not start dynamically linked executable: uv` / `NixOS cannot run dynamically linked executables...` or are the identical downstream file-existence assertion in the same fixture/subprocess call that produces that stderr in its sibling test (e.g. `test_integration_nested_toctree.py`'s `test_sphinx_build_succeeds` shows the signature explicitly; `test_chapter1_index_has_relative_includes`/`test_root_index_has_correct_include` use the identical `uv run sphinx-build` subprocess call against the same fixture and fail only because the file was never generated by that same failed subprocess). Zero AttributeError/TypeError/DeprecationWarning-promoted failures found. Matches SUMMARY's independently-documented "357 passed, 45 failed" count exactly. |
| 4 | SC4: tree is black/ruff/mypy clean | ✓ VERIFIED | Own re-run: `uv run black --check .` → "All done! 50 files would be left unchanged." `nix-shell -p ruff --run "ruff check ."` (venv ruff hit the same NixOS launch issue, confirmed the identical signature, then fell back per RESEARCH.md) → "All checks passed!". `uv run mypy typsphinx/` → "Success: no issues found in 6 source files". |
| 5 | D-02: permanent `filterwarnings` guard in `pyproject.toml` escalating both `error::DeprecationWarning` and `error::PendingDeprecationWarning` | ✓ VERIFIED | `pyproject.toml` `[tool.pytest.ini_options]` (lines 82-94) contains `filterwarnings = ["error::DeprecationWarning", "error::PendingDeprecationWarning", ...]` with explanatory comments justifying the `PendingDeprecationWarning` escalation (catches Sphinx's `RemovedInSphinxNNWarning` family) and the third-party escape-hatch policy. |

**Score:** 5/5 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `typsphinx/template_engine.py` | line 239 `traverse()`→`findall()` | ✓ VERIFIED | Confirmed via direct read; `list(doctree.findall(addnodes.toctree))` present, `list()` wrapper preserved. |
| `tests/test_translator.py` | 3× `OptionParser`→`frontend.get_default_settings` | ✓ VERIFIED | `grep -n "frontend.get_default_settings"` shows exactly 3 hits (lines 1647, 1725, 1845); zero `OptionParser`/`get_default_values` tokens. |
| `tests/test_builder.py`, `tests/test_pdf_generation.py` | `builder.app`→`builder._app` | ✓ VERIFIED | Both files assert `builder._app`; zero `builder.app` reads remain. |
| `tests/test_documentation_configuration.py`, `tests/test_documentation_usage.py` | `writer_name=`→`writer=get_writer_class(...)()` | ✓ VERIFIED | Both files import `get_writer_class` and call `writer=get_writer_class("html")()`; zero `writer_name` tokens remain. |
| `pyproject.toml` | `filterwarnings` guard added | ✓ VERIFIED | Present inside `[tool.pytest.ini_options]`, TOML-valid, both error classes present. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `template_engine.py` `findall()` result | downstream `toctree_nodes[0]` indexing | `list(...)` eager-materialization wrapper preserved | WIRED | Line 239 retains the `list(...)` wrapper around `findall()`; the generator is materialized before indexing, matching the plan's key_link requirement. |
| `pyproject.toml` `filterwarnings` guard | every pytest invocation (this run + future CI) | ini-level `[tool.pytest.ini_options]` scope | WIRED | Own re-run of `uv run pytest -q -m "not slow" tests/test_template_engine.py tests/test_translator.py tests/test_builder.py tests/test_documentation_configuration.py tests/test_documentation_usage.py` implicitly exercises the guard (no explicit `-W` flags needed); all pass. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| No traverse() calls remain in typsphinx/ | `grep -rn "\.traverse(" typsphinx/` | zero matches | ✓ PASS |
| Installed stack matches Sphinx 9.1 / docutils 0.22 | `python -c "import sphinx, docutils; print(sphinx.__version__, docutils.__version__)"` | `sphinx 9.1.0`, `docutils 0.22.4` | ✓ PASS |
| Full pytest suite (excl. slow marker) | `uv run pytest -q -m "not slow"` | 357 passed, 45 failed (all 45 confirmed NixOS `uv`-launch signature) | ✓ PASS (with documented environmental caveat) |
| Config registration tests | `uv run pytest -q tests/test_config*.py` | 33 passed | ✓ PASS |
| black clean | `uv run black --check .` | 50 files unchanged | ✓ PASS |
| ruff clean | `nix-shell -p ruff --run "ruff check ."` | All checks passed! | ✓ PASS |
| mypy clean | `uv run mypy typsphinx/` | Success: no issues found in 6 source files | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| API-01 | 08-01 | `doctree.traverse()` at `template_engine.py:239` replaced with `doctree.findall()` | ✓ SATISFIED | Confirmed by direct file read + zero-`traverse()` grep sweep of `typsphinx/`. |
| API-02 | 08-01, 08-02, 08-03 | translator/writer/builder/config registration confirmed compatible with Sphinx 9.1 + docutils 0.22; full pytest suite passes | ✓ SATISFIED | All 4 deprecation-fix sites confirmed in source; installed stack confirmed at target versions; 357/357 in-process tests pass under the escalated guard; black/ruff/mypy clean. |

REQUIREMENTS.md maps both API-01 and API-02 exclusively to Phase 8 (no other phase claims them); no orphaned requirements found.

### Anti-Patterns Found

None. Scanned all phase-modified files (`typsphinx/template_engine.py`, `tests/test_translator.py`, `tests/test_builder.py`, `tests/test_pdf_generation.py`, `tests/test_documentation_configuration.py`, `tests/test_documentation_usage.py`, `pyproject.toml`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` — zero matches in any file.

### Human Verification Required

None. All must-haves resolved programmatically with direct codebase evidence.

### Gaps Summary

No gaps. All 5 must-haves (4 ROADMAP success criteria + D-02 permanent guard) verified directly against the codebase, independent of SUMMARY.md claims:

- Zero `traverse()` tokens confirmed via fresh grep sweep.
- Installed dependency versions independently confirmed at Sphinx 9.1.0 / docutils 0.22.4 (matching the phase's target stack).
- Full pytest suite independently re-run (not just SUMMARY-trusted): 357 passed / 45 failed, with every one of the 45 failures individually inspected in the saved run log and confirmed to carry the exact documented NixOS `uv`-launch signature (`Could not start dynamically linked executable: uv` / `NixOS cannot run dynamically linked executables...`) or to be a same-fixture downstream consequence of that identical subprocess failure — matching 08-RESEARCH.md's Pitfall 3 and 08-03-SUMMARY.md's independently-reported count exactly.
- black/ruff (nix-shell fallback)/mypy independently re-run and confirmed clean.
- The optional multi-`<term>` hardening (Task 2 of 08-03-PLAN.md) was confirmed genuinely skipped (no `_term_strings` symbol exists), consistent with the plan's explicit "OPTIONAL AND SKIPPABLE" framing and RESEARCH.md's empirical finding that no current rST syntax on the installed docutils parser can produce the multi-`<term>` shape — this is a documented, non-blocking deferral, not a gap.
- No debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) found in any phase-modified file.
- Both requirement IDs (API-01, API-02) are mapped only to Phase 8 in REQUIREMENTS.md and are satisfied with direct evidence.

---

_Verified: 2026-07-11T02:57:04Z_
_Verifier: Claude (gsd-verifier)_
