---
phase: 08-api-test-compatibility-sphinx-9-docutils-0-22
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - pyproject.toml
  - typsphinx/translator.py
  - tests/test_translator.py
autonomous: true
requirements: [API-01, API-02]
must_haves:
  truths:
    - "pyproject.toml [tool.pytest.ini_options] contains a filterwarnings array with both error::DeprecationWarning and error::PendingDeprecationWarning (D-02 permanent guard) (SC1/SC2)"
    - "the full pytest suite (~402 tests incl. PDF integration) passes with the guard active — any subprocess reds are confirmed to be the documented NixOS uv-launch signature, not Sphinx-9/docutils-0.22 regressions (SC3)"
    - "black --check ., ruff check ., mypy typsphinx/ all report clean (SC4)"
  artifacts:
    - "pyproject.toml (modified: filterwarnings guard added to [tool.pytest.ini_options])"
    - "OPTIONAL — typsphinx/translator.py + tests/test_translator.py: multi-<term> append-not-overwrite hardening + hand-built-doctree regression test (skippable; forward-looking only)"
  key_links:
    - "the guard only leaves the suite green because all 6 deprecation fixes (Plans 08-01, 08-02) landed in Wave 1 first — enabling the guard before the fixes would turn ~7 test-level warnings into hard failures (RESEARCH.md Pitfall 1)"
    - "escalating PendingDeprecationWarning (not just DeprecationWarning) is required to catch Sphinx's RemovedInSphinxNNWarning family — a deliberate deviation-with-rationale from CONTEXT.md's literal wording (RESEARCH.md Pitfall 2 / Open Question 1)"
---

<objective>
Enable the permanent deprecation guard (D-02), optionally harden the definition-list term buffering (forward-looking, skippable), and run the phase gate: full pytest suite green under the guard + black/ruff/mypy clean.

Purpose: SC1 (guard proves no `traverse()` warning remains suite-wide), SC2 (no deprecation-removal breakage), SC3 (full ~402-test suite passes), SC4 (tree lint/type clean). This plan runs in Wave 2, AFTER Plans 08-01 + 08-02 have landed all 6 deprecation fixes in Wave 1 — so enabling the guard finds the suite green, not red (D-02 ordering constraint, RESEARCH.md Pitfall 1).

Output: `pyproject.toml` with the `filterwarnings` guard; the phase's done-ness gate (SC3 + SC4) confirmed green.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-RESEARCH.md
@.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-PATTERNS.md
@.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-VALIDATION.md
</context>

## Artifacts this phase produces

The ONLY new artifact of the entire phase is created here:
- `pyproject.toml` `[tool.pytest.ini_options]` gains a new `filterwarnings` array key with exactly two escalation entries — `error::DeprecationWarning` and `error::PendingDeprecationWarning` — plus explanatory comments. This is a config addition, not a new test file or public symbol.

Optional (Task 2, skippable): `typsphinx/translator.py` `visit_definition_list_item`/`depart_term`/`depart_definition` gain an append-then-join term accumulation (`self._term_strings`), and `tests/test_translator.py` gains one forward-looking regression test that hand-builds a multi-`<term>` doctree. These are modifications/additions, not new public symbols. NOT required for SC1–SC4.

<tasks>

<task type="auto">
  <name>Task 1: Add the permanent filterwarnings deprecation guard to pyproject.toml (D-02)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml lines 72-81 (the existing `[tool.pytest.ini_options]` table: `testpaths`, `python_files`, `python_classes`, `python_functions`, `addopts`, `markers`) — the block being extended
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-PATTERNS.md §"pyproject.toml [tool.pytest.ini_options] — D-02 permanent deprecation guard" (shows the exact target block + the comment text to add)
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-RESEARCH.md §"Open Questions" #1 + §"Common Pitfalls" #2 (why both warning classes must be escalated)
  </read_first>
  <action>
    Add a `filterwarnings` array key to the existing `[tool.pytest.ini_options]` table in `pyproject.toml`, placed after the `markers` key (the table ends at line 81). The array has exactly two active entries, each a TOML string: first `error::DeprecationWarning`, then `error::PendingDeprecationWarning`. Escalating BOTH classes is mandatory (not just the literal `error::DeprecationWarning` from CONTEXT.md D-02): Sphinx's `RemovedInSphinxNNWarning` family — e.g. the `builder.app` warning fixed in Plan 08-02 — subclasses `PendingDeprecationWarning`, NOT `DeprecationWarning`, so a `DeprecationWarning`-only guard would silently let future Sphinx-specific deprecations through (RESEARCH.md Pitfall 2 / Open Question 1; this is a documented deviation-with-rationale from the literal locked text, honoring D-02's stated intent to "prevent silent re-accumulation of deprecated API"). Add a TOML comment (`#` lines) directly above the `error::PendingDeprecationWarning` entry stating it catches Sphinx's `RemovedInSphinxNNWarning` family per 08-RESEARCH.md, and a second comment noting that no third-party `ignore::` entries are required on this stack (RESEARCH.md verified zero third-party deprecation warnings on Sphinx 9.1.0 / docutils 0.22.4 / typst-py 0.15.0) but that any future dependency-originated warning goes here as a targeted `ignore::DeprecationWarning:<module>` entry rather than weakening the two `error` lines. Keep the existing `addopts = "-v --strict-markers"` and `markers` entries unchanged.
  </action>
  <verify>
    <automated>uv run pytest -q -m "not slow" tests/test_template_engine.py tests/test_translator.py tests/test_builder.py tests/test_documentation_configuration.py tests/test_documentation_usage.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'filterwarnings' pyproject.toml` returns `1`.
    - `grep -c 'error::DeprecationWarning' pyproject.toml` returns `1` and `grep -c 'error::PendingDeprecationWarning' pyproject.toml` returns `1`.
    - The guard is INSIDE the `[tool.pytest.ini_options]` table (it appears in the file after line 72 `[tool.pytest.ini_options]` and before the next `[tool.` table header).
    - Running the fixed test files (Plans 08-01/08-02) WITHOUT any explicit `-W` flags now picks up the guard from the ini file and exits 0 — proving the guard reads the fixed suite as green (the `-W` flags are no longer needed because the ini `filterwarnings` supplies the escalation).
    - `python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); fw=d['tool']['pytest']['ini_options']['filterwarnings']; assert fw==['error::DeprecationWarning','error::PendingDeprecationWarning'], fw; print('OK')"` prints `OK` (the TOML parses and the two entries are exactly present in order).
  </acceptance_criteria>
  <done>pyproject.toml `[tool.pytest.ini_options]` has a `filterwarnings` array with both `error::DeprecationWarning` and `error::PendingDeprecationWarning` and the escape-hatch comment; the previously-fixed test files pass with the guard active and no explicit `-W` flags.</done>
</task>

<task type="auto">
  <name>Task 2 (OPTIONAL — skip by default): multi-&lt;term&gt; append-not-overwrite hardening + regression test</name>
  <files>typsphinx/translator.py, tests/test_translator.py</files>
  <read_first>
    - typsphinx/translator.py lines 990-1122 (`visit_definition_list`/`visit_definition_list_item` ~1027, `visit_term` ~1047, `depart_term` ~1061-1082 with the overwriting `self.current_term_buffer = term_content`, `depart_definition` ~1098-1120 which reads `self.current_term_buffer` as a str to pair with the definition) — the methods being hardened
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-PATTERNS.md §"typsphinx/translator.py — optional defensive hardening, NOT required for SC2/SC3" (the self-analog buffer-swap idiom + the recommended append-then-join shape)
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-RESEARCH.md §"Common Pitfalls" #4 + §"Open Questions" #2 (the DTD-allows-but-parser-cannot-yet-emit finding; forward-looking only)
  </read_first>
  <action>
    THIS TASK IS OPTIONAL AND SKIPPABLE. It is forward-looking hardening only — RESEARCH.md empirically confirmed NO current reStructuredText syntax on docutils 0.22.4 produces a multi-`<term>` `definition_list_item`, so this is NOT required for SC2/SC3 and MUST NOT block the SC3 gate. Skip it unless there is spare wave capacity. If skipped, record in the SUMMARY that the multi-`<term>` hardening was deferred as forward-looking (see Assumption below) — do not silently drop it.

    IF executed: change the single-value overwrite to an append-then-join, following the file's own existing buffer-swap idiom (do NOT introduce a new mechanism). Specifically: (1) in `visit_definition_list_item` (~line 1027) initialize a fresh accumulator `self._term_strings = []`; (2) in `depart_term` (~line 1082) replace the overwriting assignment `self.current_term_buffer = term_content` with `self._term_strings.append(term_content)`; (3) in `depart_definition` (~lines 1116-1120) build the paired term by joining the accumulator, `joined_term = ", ".join(self._term_strings)`, use `joined_term` where the code currently uses the single `self.current_term_buffer` string when appending the `(term, definition)` tuple to `self.definition_list_items`, then reset `self._term_strings = []`. Preserve the existing single-term output byte-for-byte for the common one-term case (a 1-element join equals the element itself). Keep the `visit_term`/`depart_term` body-buffering (`self.saved_body`) idiom unchanged. Add a type annotation `self._term_strings: List[str]` consistent with the existing `List[str]` usage at line 86 (do NOT switch to lowercase `list[str]` — `UP006`/`UP035` stay ruff-ignored per CLAUDE.md). Then add ONE regression test to `tests/test_translator.py` that hand-constructs a `definition_list_item` doctree with TWO `nodes.term` children (bypassing the rST parser, which cannot emit this shape) and asserts the translator joins both terms rather than dropping the first; the test docstring must state it is forward-looking (the docutils 0.22.4 parser does not yet emit multi-`<term>` nodes; DTD allows it per docutils patches#95).
  </action>
  <verify>
    <automated>uv run pytest tests/test_translator.py -q -m "not slow"</automated>
  </verify>
  <acceptance_criteria>
    - IF SKIPPED: the SUMMARY explicitly notes the multi-`<term>` hardening was deferred as forward-looking (not required for SC2/SC3); no acceptance assertions below apply.
    - IF EXECUTED: `grep -c '_term_strings' typsphinx/translator.py` returns `>= 3` (init + append + join/reset).
    - IF EXECUTED: the new regression test exists and `uv run pytest tests/test_translator.py -k "multi_term or multiple_term" -q` exits 0 (adjust the `-k` filter to the test's actual name).
    - IF EXECUTED: the pre-existing definition-list tests still pass unchanged: `uv run pytest tests/test_translator.py -k definition_list -q` exits 0 (single-term output byte-identical — no fixture change).
    - IF EXECUTED: `uv run pytest tests/test_translator.py -q` exits 0 under the now-active guard.
  </acceptance_criteria>
  <done>Either (a) the task is skipped and the deferral is documented in the SUMMARY, OR (b) `translator.py` accumulates terms via `self._term_strings` (append-then-join), a forward-looking hand-built-doctree regression test passes, and all pre-existing definition-list tests stay green with byte-identical single-term output.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite green under guard + black/ruff/mypy clean (SC3/SC4)</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-VALIDATION.md §"Sampling Rate" (phase-gate command set) + §"Manual-Only Verifications" (the NixOS signature is the only human judgment)
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-RESEARCH.md §"Common Pitfalls" #3 (the exact NixOS `uv`/`ruff` launch-failure signature) + §"Environment Availability" (the `ruff` fallback via nix-shell)
    - CLAUDE.md §"Commands" (`pytest`, `black --check .`, `ruff check .`, `mypy typsphinx/`)
  </read_first>
  <action>
    Run the phase done-ness gate against the resolved Sphinx 9.1.0 / docutils 0.22.4 / typst 0.15.0 stack with the `filterwarnings` guard now active:
    (1) Full suite: `uv run pytest` — target is all ~402 tests green (includes the now-kai-free PDF integration tests, unblocked by Phase 7). The guard is read from `pyproject.toml`, so no explicit `-W` flags are needed.
    (2) Format: `black --check .` — must report all files unchanged (the 6 small edits should introduce no reformatting; if black wants a change, apply `black .` and re-verify, then note it).
    (3) Lint: `ruff check .` — must report "All checks passed!". If the sandboxed `.venv/bin/ruff` fails to launch with the NixOS ELF error, fall back to `nix-shell -p ruff --run "ruff check ."` per RESEARCH.md Environment Availability.
    (4) Types: `mypy typsphinx/` — must report "Success: no issues found".
    NixOS caveat handling (MANDATORY, do NOT silently ignore reds): if the full-suite run shows failures, inspect each failure's message. ONLY failures whose message contains the exact signature `cannot start dynamically linked executable: uv` (or `Could not start dynamically linked executable` / `NixOS cannot run dynamically linked executables`) — from the ~45 subprocess-based integration/example tests that call `subprocess.run(["uv","run","sphinx-build",...])` — may be treated as a pre-existing environment artifact (RESEARCH.md Pitfall 3; STATE.md records 402 green as of Phase 7). For those, re-verify the remaining ~357 non-subprocess tests + the source-level fixes are green independently, and surface the environment issue to the user as a note. Any failure with a Sphinx-9/docutils-0.22-shaped stack trace (AttributeError/TypeError/deprecation error) is a REAL regression and blocks the gate — fix source to preserve existing `.typ` output (test-fix bias, CONTEXT.md D locked default) unless the red is a genuine intended docutils/Sphinx output-shape change (then adapt the translator + update the fixture with a one-line why).
  </action>
  <verify>
    <automated>uv run pytest -q -m "not slow" && black --check . && mypy typsphinx/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` exits 0 with ~402 tests passing; OR, if some subprocess-based tests red, EACH red is confirmed to carry the exact NixOS `cannot start dynamically linked executable: uv` signature and the remaining ~357 tests + all source/test fixes are green (documented in the SUMMARY with the failing test count and the confirmed signature).
    - No test fails with a Sphinx-9/docutils-0.22-shaped `AttributeError`/`TypeError`/deprecation error (SC2 — zero real API-removal breakage).
    - `black --check .` reports no files would be reformatted (exit 0).
    - `ruff check .` reports "All checks passed!" (via `nix-shell -p ruff --run "ruff check ."` if the venv ruff cannot launch under NixOS).
    - `mypy typsphinx/` reports "Success: no issues found" (exit 0).
    - `grep -c 'traverse' typsphinx/template_engine.py` returns `0` (SC1 confirmed suite-wide: no traverse token, and the active guard would have failed any test that still triggered a traverse warning).
  </acceptance_criteria>
  <done>Full pytest suite green under the active guard (or subprocess reds confirmed as the documented NixOS signature with the rest green); black/ruff/mypy all clean; zero real Sphinx-9/docutils-0.22 API breakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/local test runner → pytest config | The `filterwarnings` guard governs how every `pytest` invocation (local + the Phase-9 CI matrix) treats warnings. Misconfiguring it (e.g. a blanket `ignore`) would silently re-admit deprecated API — the guard itself is the control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Tampering | pytest filterwarnings guard | low | mitigate | The guard is scoped to `error::DeprecationWarning` + `error::PendingDeprecationWarning` only; the escape-hatch comment forbids weakening the two `error` lines and requires any future exclusion to be a targeted, commented `ignore::DeprecationWarning:<module>` — preventing a blanket-ignore that would defeat D-02. Verified: no `ignore::` entry is needed on this stack (zero third-party warnings). |
| T-08-01 | Tampering | translator.py term buffering (optional) | low | accept | Optional hardening changes only in-process string accumulation for already-trusted doctree content; no new input/eval/file-I/O surface. RESEARCH.md Security Domain: security-neutral. |
| T-08-SC | Tampering | package installs (pip) | high | accept | No new packages installed this phase (RESEARCH.md Standard Stack; Package Legitimacy Audit N/A). Supply-chain slopcheck gate does not apply — nothing is fetched/installed. |
</threat_model>

<verification>
- Guard present and TOML-valid in `pyproject.toml` `[tool.pytest.ini_options]` with both `error::` entries.
- Full `uv run pytest` green under the guard (NixOS subprocess reds handled per the documented signature, never silently ignored).
- `black --check .`, `ruff check .` (nix-shell fallback if needed), `mypy typsphinx/` all clean.
- The optional multi-`<term>` hardening is either executed-with-test or explicitly deferred-as-forward-looking in the SUMMARY.

## Assumption (multi-&lt;term&gt; hardening)

Per RESEARCH.md Open Question #2, the multi-`<term>` definition-list hardening (Task 2) is treated as OPTIONAL/forward-looking: the docutils 0.22 DTD permits multiple `<term>` children (patches#95) but the installed 0.22.4 rST parser cannot emit that shape, so no current fixture or reST syntax exercises the single-term overwrite. Deferring it does not affect SC1–SC4. It is surfaced here (not silently dropped) so a future docutils parser version that emits multi-`<term>` nodes has a documented follow-up.
</verification>

<success_criteria>
- SC1: no `traverse()` deprecation warning remains — enforced suite-wide by the active guard.
- SC2: no `AttributeError`/`TypeError`/deprecation-removal breakage on the resolved Sphinx 9.1 + docutils 0.22 stack.
- SC3: full ~402-test pytest suite (incl. PDF integration) passes under the guard (NixOS subprocess reds confirmed environmental, if any).
- SC4: `black`/`ruff`/`mypy` clean.
</success_criteria>

<output>
Create `.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-03-SUMMARY.md` when done.
</output>
