---
phase: 08-api-test-compatibility-sphinx-9-docutils-0-22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typsphinx/template_engine.py
  - tests/test_translator.py
autonomous: true
requirements: [API-01, API-02]
must_haves:
  truths:
    - "typsphinx/template_engine.py line ~239 calls doctree.findall(addnodes.toctree); no traverse() call remains anywhere in typsphinx/ (SC1)"
    - "pytest tests/test_template_engine.py under -W error::DeprecationWarning passes — the toctree traversal no longer emits a traverse() deprecation warning through TemplateEngine.render() (SC1)"
    - "tests/test_translator.py builds settings via frontend.get_default_settings(RstParser) at all 3 sites; no OptionParser import or .get_default_values() call remains (SC2)"
  artifacts:
    - "typsphinx/template_engine.py (modified: traverse()→findall() at line 239)"
    - "tests/test_translator.py (modified: 3× OptionParser→frontend.get_default_settings)"
  key_links:
    - "findall() result feeds the existing list(...) wrapper + downstream toctree_nodes[0] indexing unchanged — the list() eager-materialization is preserved"
---

<objective>
Land the locked API-01 source fix and the docutils-0.22 `frontend` modernization in `tests/test_translator.py`. This is the first half of the empirically-enumerated 6-edit deprecation sweep (RESEARCH.md "Common Pitfalls" §Pitfall 1 — the complete work-list).

Purpose: SC1 (no `traverse()` deprecation warning) and part of SC2 (docutils 0.22 API compatibility). These edits must land in Wave 1, BEFORE the permanent `filterwarnings` guard is enabled in Plan 03 (Wave 2), so that turning the guard on leaves the suite green rather than red (D-02 ordering constraint).

Output: `typsphinx/template_engine.py` (findall swap) and `tests/test_translator.py` (3× `get_default_settings`) modernized, verified warning-free under the escalated warning filters.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-RESEARCH.md
@.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-PATTERNS.md
</context>

## Artifacts this phase produces

This is a pure API-modernization phase — it changes existing symbols, it does NOT create new public symbols. The only NEW artifact across the whole phase is the `filterwarnings` ini-option key added to `pyproject.toml` (landed in Plan 03).

This plan's changes (all modifications to existing code, no new symbols):
- `typsphinx/template_engine.py` — the toctree-collection call at line 239 switches from the deprecated `Node.traverse()` iterator to `Node.findall()` (same generator semantics; mirrors the existing `builder.py:151` usage).
- `tests/test_translator.py` — the 3 test-local `docutils.frontend.OptionParser(...).get_default_values()` settings constructions switch to `docutils.frontend.get_default_settings(RstParser)` (docutils' own blessed replacement ahead of the argparse rewrite in docutils 2.0).

<tasks>

<!-- planner-discipline-allow: traverse -->
<!-- planner-discipline-allow: OptionParser -->
<!-- These two deprecated identifiers must be named verbatim in the <action> bodies below
     because each task is a find-and-replace whose whole purpose is to remove that exact
     token; the acceptance criteria negative-grep the SOURCE FILES for them (they must be
     gone after the edit), and the actions explicitly forbid writing either token into the
     files as a comment. -->

<task type="auto">
  <name>Task 1: Swap template_engine.py toctree traverse() → findall() (API-01, locked)</name>
  <files>typsphinx/template_engine.py</files>
  <read_first>
    - typsphinx/template_engine.py (the file being modified — read the `extract_toctree_options` method around line 239 for the `toctree_nodes` collection and the downstream `toctree_nodes[0]` indexing that depends on the eager list)
    - typsphinx/builder.py lines 145-153 (the ANALOG: the existing `for node in doctree.findall(image):` call at line 151 — this is the exact call shape to mirror; PATTERNS.md §"typsphinx/template_engine.py")
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-PATTERNS.md §"typsphinx/template_engine.py (service, transform) — API-01, locked"
  </read_first>
  <action>
    At `typsphinx/template_engine.py:239`, replace the single deprecated call `list(doctree.traverse(addnodes.toctree))` with `list(doctree.findall(addnodes.toctree))`. Change ONLY the method name `traverse` → `findall`; keep the `list(...)` wrapper and the `addnodes.toctree` argument exactly as-is — downstream code indexes `toctree_nodes[0]`, so the eager list materialization is still required here (unlike `builder.py`'s direct-iterate `for` loop, which does not need `list()`). `findall()` is a drop-in generator replacement with identical document-order, node-matching semantics; no other line in the file changes. This is the ONLY `traverse()` call in `typsphinx/` (grep-confirmed), so no other source file needs this edit. Do NOT add any code comment that names the old method — keep the diff to the single identifier swap (avoids re-introducing the deprecated token into the file body). Do NOT modernize any `typing.Dict`/`typing.List` imports in this file — `UP006`/`UP035` stay ruff-ignored per CLAUDE.md; this sweep is docutils/Sphinx-deprecation-only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_template_engine.py -q -W "error::DeprecationWarning" -W "error::PendingDeprecationWarning" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'doctree.findall(addnodes.toctree)' typsphinx/template_engine.py` returns `1`.
    - `grep -c 'traverse' typsphinx/template_engine.py` returns `0` (no `traverse` token remains in the file).
    - `uv run pytest tests/test_template_engine.py -q -W "error::DeprecationWarning" -W "error::PendingDeprecationWarning" -x` exits 0 (the toctree-outline integration tests pass with the traverse deprecation escalated to an error — proving the call no longer fires it).
    - The `list(...)` wrapper is still present on line 239 (downstream `toctree_nodes[0]` indexing unbroken).
  </acceptance_criteria>
  <done>template_engine.py line 239 uses `list(doctree.findall(addnodes.toctree))`; zero `traverse` tokens remain in the file; test_template_engine.py passes under both escalated warning classes.</done>
</task>

<task type="auto">
  <name>Task 2: Modernize test_translator.py 3× OptionParser → frontend.get_default_settings (API-02)</name>
  <files>tests/test_translator.py</files>
  <read_first>
    - tests/test_translator.py (the file being modified — read the three identical settings-construction sites: import at lines 1606/1698/1819, call at lines 1647/1725/1845; note `RstParser` is already imported as `from docutils.parsers.rst import Parser as RstParser` at lines 1607/1699/1820)
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-PATTERNS.md §"tests/test_translator.py — 3 sites, docutils 0.22 OptionParser deprecation"
    - .planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-RESEARCH.md §"Pattern 2: frontend.OptionParser → frontend.get_default_settings()"
  </read_first>
  <action>
    Apply the identical 2-line substitution at all 3 sites in `tests/test_translator.py`:
    (1) Replace each `from docutils.frontend import OptionParser` import (lines ~1606, ~1698, ~1819) with `from docutils import frontend`.
    (2) Replace each settings line `settings = OptionParser(components=(RstParser,)).get_default_values()` (lines ~1647, ~1725, ~1845) with `settings = frontend.get_default_settings(RstParser)`.
    Leave the `from docutils.parsers.rst import Parser as RstParser` import and the `parser = RstParser()` line at each site UNCHANGED — only the `OptionParser` import and the settings-construction line change. `get_default_settings` returns the same `Values`-shaped settings object `get_default_values()` produced for this use case, so no downstream assertion in these tests changes. Do NOT add a compatibility try/except or version shim — this is a latest-only forward-port (no docutils-0.21 branching). Do NOT reformat unrelated lines; keep the diff minimal so black/ruff stay clean.
  </action>
  <verify>
    <automated>uv run pytest tests/test_translator.py -q -W "error::DeprecationWarning" -W "error::PendingDeprecationWarning" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'OptionParser' tests/test_translator.py` returns `0` (no import or usage of the deprecated class remains).
    - `grep -c 'get_default_values' tests/test_translator.py` returns `0`.
    - `grep -c 'frontend.get_default_settings(RstParser)' tests/test_translator.py` returns `3`.
    - `uv run pytest tests/test_translator.py -q -W "error::DeprecationWarning" -W "error::PendingDeprecationWarning" -x` exits 0 (all translator tests pass with `DeprecationWarning` escalated — proving the `OptionParser` deprecation no longer fires).
    - The definition-list tests specifically still pass: `uv run pytest tests/test_translator.py -k definition_list -q` exits 0 (confirms SC2 term/classifier handling unchanged for supported rST syntax).
  </acceptance_criteria>
  <done>All 3 sites use `frontend.get_default_settings(RstParser)`; zero `OptionParser`/`get_default_values` tokens remain; test_translator.py passes under both escalated warning classes including the `-k definition_list` subset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs author → build pipeline | `.rst` source + `conf.py` config cross into the translator/template-engine at build time. In this extension's context the document author is the trusted operator; no untrusted remote input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | template_engine.py toctree traversal | low | accept | This plan changes only the iterator method (`traverse`→`findall`); it introduces no new input surface, no eval/exec, and no new file I/O. `findall()` is docutils' own trusted traversal API. RESEARCH.md Security Domain confirms the phase is a pure API-modernization change with no new attack surface. |
| T-08-SC | Tampering | package installs (pip) | high | accept | No new packages are installed this phase (RESEARCH.md "Standard Stack": all deps already pinned; Package Legitimacy Audit N/A). The supply-chain slopcheck gate does not apply — nothing is fetched/installed. |
</threat_model>

<verification>
- Both tasks green under `uv run pytest tests/test_template_engine.py tests/test_translator.py -q -W "error::DeprecationWarning" -W "error::PendingDeprecationWarning"`.
- `grep -c 'traverse' typsphinx/template_engine.py` == 0; `grep -c 'OptionParser' tests/test_translator.py` == 0.
- Do NOT enable the `pyproject.toml` guard in this plan — that is Plan 03 (Wave 2). This plan only lands the fixes so the guard, once enabled, finds the suite green.
</verification>

<success_criteria>
- SC1 (partial): `template_engine.py:239` uses `findall()`; zero `traverse` tokens remain in `typsphinx/`; no traverse deprecation warning surfaces.
- SC2 (partial): the docutils-0.22 `frontend` deprecation is resolved in `test_translator.py`; definition-list term/classifier handling unchanged for supported syntax.
</success_criteria>

<output>
Create `.planning/phases/08-api-test-compatibility-sphinx-9-docutils-0-22/08-01-SUMMARY.md` when done.
</output>
