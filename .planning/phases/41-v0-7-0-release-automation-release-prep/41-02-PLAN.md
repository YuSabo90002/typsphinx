---
phase: 41-v0-7-0-release-automation-release-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - pyproject.toml
  - README.md
  - uv.lock
autonomous: true
requirements: [REL-05]

must_haves:
  truths:
    - "`pyproject.toml` holds version 0.7.0 as the sole version literal in the file, `README.md`'s Status line reads `Stable (v0.7.0)`, `uv.lock`'s typsphinx package entry reads 0.7.0, and `typsphinx.__version__` reports `0.7.0` (edge: boundary, REL-05)."
    - "`## [0.7.0]` is inserted between the top `## [Unreleased]` heading and `## [0.6.5]`, separated by exactly one blank line on each side, and `## [0.6.5]`'s body is byte-unchanged (edge: adjacency, REL-05)."
    - "The tail link block lists the `[0.7.0]:` release-tag line immediately above the `[0.6.5]:` line, and the final `[Unreleased]:` line compares against v0.7.0 rather than v0.6.5 (edge: ordering, REL-05)."
    - "`uv sync --extra dev --locked` exits 0 on the bumped tree — the lockfile moved with `pyproject.toml` rather than being left stale."
    - "The `## [0.7.0]` entry carries 5 to 6 bullets cut at user-visible-change granularity with requirement IDs in trailing parentheses (D-01), split `### Added` for CIT / `### Changed` for SIG+IND+FLD+ADM / `### Fixed` for MATH-02 (D-02), no BREAKING label (D-03), a lead paragraph whose axis is that API reference pages became readable (D-04), and a `### Verified` section carrying exactly the same three items as `## [0.6.5]` (D-05)."
  artifacts:
    - CHANGELOG.md
    - pyproject.toml
    - README.md
    - uv.lock
  key_links:
    - "pyproject.toml [project].version -> README.md Status line (pinned by tests/test_readme_version_sync.py)"
    - "pyproject.toml [project].version -> typsphinx.__version__ via importlib.metadata"
    - "pyproject.toml [project].version -> uv.lock typsphinx entry"
    - "CHANGELOG.md ## [0.7.0] -> the future GitHub Release body via plan 41-01's extractor"
  prohibitions:
    - statement: "MUST NOT take any irreversible release action — no `git tag`, no tag push, no `release.yml` trigger, no PyPI upload, no GitHub Release, no PR open or merge."
      category: safety
      requirement_id: REL-05
    - statement: "MUST NOT edit, reword, or reformat any historical version entry in CHANGELOG.md while inserting the new one, and MUST NOT touch the tail Planned-for-Future-Releases block."
      category: transparency
      requirement_id: REL-05
    - statement: "MUST NOT hand-edit `uv.lock` — it is regenerated by `uv lock`, never patched."
      category: transparency
      requirement_id: REL-05
---

<objective>
Deliver ROADMAP SC#2 — the REL-05 prep half. Write the curated `## [0.7.0]` CHANGELOG entry per
D-01..D-05, roll the tail link block over, and bump the version to 0.7.0 across its three lockstep
sites (`pyproject.toml` as the sole literal, `README.md`'s Status line, and `uv.lock` by
regeneration), so `typsphinx.__version__` reports `0.7.0`.

Purpose: this is the tree state everything downstream is measured against. Plan 41-05's green-tree
evidence and plan 41-04's `ja` glyph bar are both defined as evidence about the POST-bump tree, and
plan 41-01's extractor has nothing to extract for `0.7.0` until this entry exists.

Output: a curated CHANGELOG entry with its link-block rollover, and a version bump in lockstep.

This plan takes NO irreversible action. It writes files; it never tags, pushes, publishes, or opens a
PR. It also does NOT flip REL-05's checkbox or Traceability row in `.planning/REQUIREMENTS.md` —
that is close-side work at `/gsd-complete-milestone`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<worktree_provisioning>
Worktree isolation is this project's STANDING execution mode (CLAUDE.md § "Worktree-isolated
execution"). Before ANY other command, run once from the worktree root:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do ln -sf "$(command -v $t)" ".venv/bin/$t"; done`

Then invoke EVERY command through `uv run`. A bare `pytest` tests the MAIN checkout's package.

Note for Task 2 specifically: after `pyproject.toml`'s version changes, the worktree venv still holds
the OLD package metadata. You MUST re-run
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` after the bump before
`typsphinx.__version__` will report `0.7.0`.

**Do not reference `scripts/extract_changelog_section.py` in any verify command in this plan.** It is
created by plan 41-01 in the SAME wave, in a different worktree, and is not present here.
</worktree_provisioning>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the curated 0.7.0 CHANGELOG entry and roll the tail link block over</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` — read lines 1-90 (the top `## [Unreleased]` at line 8, the whole `## [0.6.5]`
      entry at lines 10-32 as the structural model, and the start of `## [0.6.4]` at line 33) and the
      final 30 lines (the tail link-reference block, whose last line currently compares v0.6.5 to
      HEAD). Also read around line 854, where a SECOND `## [Unreleased]` heading opens the "Planned
      for Future Releases" scratch area — that block is NOT a release entry and must not be touched.
    - `.planning/REQUIREMENTS.md` § "v1 Requirements" — read SIG-01..SIG-09, IND-01..IND-05,
      FLD-01..FLD-03, ADM-01..ADM-06, CIT-01..CIT-06, and MATH-02 verbatim. The bullets' claims must
      come from what these requirements actually say was delivered, not from recollection.
    - `.planning/ROADMAP.md` § "Phase 41" — the goal sentence, for the lead paragraph's axis.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` § D-01..D-05.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-PATTERNS.md` § the
      `CHANGELOG.md` entry section.
  </read_first>
  <action>
Insert a new `## [0.7.0] - YYYY-MM-DD` entry into `CHANGELOG.md` directly below the top
`## [Unreleased]` heading (currently line 8) and directly above the `## [0.6.5] - 2026-07-29` heading
(currently line 10), with exactly one blank line separating it from each neighbour. Use the actual
date on which you write it. Every existing entry, including the 0.6.5 body, stays byte-identical.

**Lead paragraph (D-04, D-03).** One paragraph, no heading, in the same position the 0.6.5 entry's
lead paragraph occupies. Its axis is that **API reference pages became readable**: autodoc/API output
moved from a flat wall of proportional bold text to something that reads as a reference document.
State the rendering change EXPLICITLY in this paragraph — the appearance of the emitted `.typ` and
the compiled PDF changes for anyone building API docs. Do NOT add a BREAKING label and do NOT add an
`Upgrade notes` subheading (D-03): measured, not one `typst_*` config value, builder name, or
template parameter changed this milestone, so no user's working setup breaks. Citations are secondary
in the lead — mentioned, not leading.

**Bullets (D-01, D-02).** Five bullets, cut at user-visible-change granularity, each in the
`- **Title (REQ-IDs)** — description` shape the 0.6.5 entry already uses, with requirement IDs in
trailing parentheses inside the bold title. Do NOT produce one bullet per requirement ID (that would
be 32 bullets) and do NOT roll up to three family-level bullets. The five, in these sections:

- `### Added` — citations as a full round trip (CIT-01..CIT-06). This is `Added`, not `Changed`,
  because zero citation handlers existed before: a document containing a citation aborted the Typst
  compile outright.
- `### Changed` — signatures becoming real typography (SIG-01..SIG-09).
- `### Changed` — description bodies and field lists indenting by nesting depth
  (IND-01..IND-05, FLD-01..FLD-03).
- `### Changed` — the admonition bucket re-taxonomy and rubric indentation (ADM-01..ADM-06).
- `### Fixed` — the redundant blank line after block math in a list item (MATH-02), a defect carried
  over from v0.6.5's Phase 34 review.

A sixth bullet is permitted ONLY if splitting the IND and FLD bullet genuinely reads better; do not
exceed six. Section order is `### Added`, then `### Changed`, then `### Fixed` — Keep-a-Changelog
order, and the order the 0.6.4 entry already uses.

**`### Verified` (D-05).** Exactly three items, the same three the 0.6.5 entry carries, reworded only
where the milestone name changes:
1. Zero new runtime dependencies across the full milestone diff.
2. The four bundled `@preview` package version strings unchanged across all four sync surfaces
   (`writer.py` / `template_engine.py` / `templates/base.typ` / `examples/**/*.typ`) — copy the
   0.6.5 entry's four-surface phrasing verbatim.
3. The full-corpus (Sphinx v9.1.0 `doc/`) `-b typstpdf` re-run remains fatal-free.

Do NOT add the two docs dogfooding builds and do NOT add ADM-04's owner visual sign-off to
`### Verified` — both are run and recorded as SC#3/SC#4 evidence, but the 0.6.5 rule that
test/verification machinery is not user-visible holds. These three claims are independently
re-proven by plans 41-05 and 41-06, which cross-check them against their own measured output.

**Tail link-block rollover (ROADMAP SC#2).** Add a `[0.7.0]:` line pointing at
`https://github.com/YuSabo90002/typsphinx/releases/tag/v0.7.0`, positioned immediately ABOVE the
existing `[0.6.5]:` line, and change the final `[Unreleased]:` line's compare range so it starts from
v0.7.0 instead of v0.6.5. Leave every other link line untouched. Note that this new link will 404
until `/gsd-complete-milestone` creates the release — that is expected and is exactly what the
v0.6.5 entry's own link did during Phase 35; `links.yml`'s lychee check is advisory.

Commit with `docs(41-02): add the curated 0.7.0 CHANGELOG entry and roll the link block`.
  </action>
  <verify>
    <automated>uv run python -c "
import re
t=open('CHANGELOG.md',encoding='utf-8').read()
heads=[m.group(1) for m in re.finditer(r'^## \[([^\]]+)\]', t, re.M)]
assert heads[0]=='Unreleased' and heads[1]=='0.7.0' and heads[2]=='0.6.5', heads[:4]
assert 'releases/tag/v0.7.0' in t
assert 'compare/v0.7.0...HEAD' in t and 'compare/v0.6.5...HEAD' not in t
sec=t.split('## [0.7.0]')[1].split('## [0.6.5]')[0]
assert sec.count('### Verified')==1
bullets=[l for l in sec.splitlines() if l.startswith('- **')]
assert 5 &lt;= len(bullets) &lt;= 6, len(bullets)
print('changelog ok:', len(bullets), 'bullets')
"</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md`'s ordered list of `^## \[...\]` headings begins `Unreleased`, `0.7.0`, `0.6.5` —
      asserted by parsing, not by eye.
    - The 0.7.0 section contains exactly one `### Verified` heading and 5 or 6 lines beginning
      `- **`.
    - The 0.7.0 section contains `### Added`, `### Changed`, and `### Fixed`, in that order, and the
      `### Added` subsection's single bullet names CIT requirement IDs.
    - The 0.7.0 section contains no occurrence of `BREAKING` and no `Upgrade notes` heading.
    - The 0.7.0 section's `### Verified` list has exactly three items and mentions all four sync
      surfaces by name.
    - `git diff -- CHANGELOG.md` shows insertions only in the region between the top `## [Unreleased]`
      heading and `## [0.6.5]`, plus the two tail link-block lines. Concretely:
      `git diff -U0 -- CHANGELOG.md | grep -c '^-'` (excluding the `---` file header) equals 1 — the
      single rewritten `[Unreleased]:` compare line — proving no historical entry was modified.
    - The tail `## [Unreleased]` "Planned for Future Releases" block is byte-unchanged.
  </acceptance_criteria>
  <done>A five-or-six-bullet 0.7.0 entry exists in the right position with the D-02 section split, the D-05 three-item Verified list, and a rolled-over tail link block, with every historical entry byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Bump the version to 0.7.0 across all three lockstep sites</name>
  <files>pyproject.toml, README.md, uv.lock</files>
  <read_first>
    - `pyproject.toml` — line 7 currently reads the 0.6.5 version literal. Confirm by reading the
      whole `[project]` table that this is the SOLE version literal in the file before editing.
    - `README.md` around line 317 — the Status line, currently
      `**Status**: Stable (v0.6.5) - Production ready`.
    - `tests/test_readme_version_sync.py` — read it in full. It parses README's Status line with
      `_STATUS_LINE_RE` and `pyproject.toml`'s `[project].version` with `tomllib`, then compares the
      two parsed values AGAINST EACH OTHER, never against a hardcoded string. This is why the two
      edits must land together: either alone turns the suite red.
    - `tests/test_extension.py::test_version_matches_pyproject_toml` — the second guard on the same
      pair, via `importlib.metadata`.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` § "Runtime State
      Inventory" — the four-site census (`pyproject.toml:7`, `uv.lock:1450`, `README.md:317`, plus
      the derived `typsphinx.__version__`).
  </read_first>
  <action>
Three coordinated edits, landed in ONE commit so neither version-sync guard is ever red on a
committed tree.

1. `pyproject.toml`: change the `[project]` table's `version` field from the 0.6.5 literal to
   `0.7.0`. This is the sole version literal in the file — do not add a second one anywhere.
2. `README.md`: change the Status line so it reads `**Status**: Stable (v0.7.0) - Production ready`.
   Keep the surrounding wording exactly as-is; `_STATUS_LINE_RE` in
   `tests/test_readme_version_sync.py` matches on `**Status**: Stable (vX.Y.Z)` and a reworded line
   would make that test raise its "has the Status line's wording changed?" assertion.
3. `uv.lock`: regenerate, never hand-edit. Run
   `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv lock`, then
   `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` to reinstall the package with
   the new metadata so `importlib.metadata` reports 0.7.0. Then re-create the NixOS shims:
   `for t in uv ruff; do ln -sf "$(command -v $t)" ".venv/bin/$t"; done` — `uv sync` may have
   replaced `.venv/bin/uv` with a generic-linux ELF that fails under the NixOS stub loader.

Then confirm, before committing:
- `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints `0.7.0`.
- `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked` exits 0 (this is the
  acceptance bar for the lockfile regeneration; a stale lock fails it).
- `uv run pytest tests/test_readme_version_sync.py tests/test_extension.py -x` is green.

Inspect `git diff -- uv.lock` before committing. The expected shape is the `typsphinx` package's own
`version` field moving, and nothing else. If `uv lock` also moved any THIRD-PARTY package's version,
STOP and report it: milestone invariant #1 is zero new runtime dependencies, and a silent
transitive-version drift landing inside the release-prep commit would invalidate the `### Verified`
claim plan 41-06 has to prove. Record what you found either way.

Commit with `chore(41-02): bump version to 0.7.0 across pyproject, README, and uv.lock`.
  </action>
  <verify>
    <automated>uv run python -c "
import tomllib, re
v=tomllib.load(open('pyproject.toml','rb'))['project']['version']
assert v=='0.7.0', v
r=open('README.md',encoding='utf-8').read()
m=re.search(r'\*\*Status\*\*:\s*Stable \(v(?P&lt;version&gt;\d+\.\d+\.\d+)\)', r)
assert m and m.group('version')=='0.7.0', m and m.group(0)
import typsphinx
assert typsphinx.__version__=='0.7.0', typsphinx.__version__
print('version lockstep ok')
" &amp;&amp; uv run pytest tests/test_readme_version_sync.py tests/test_extension.py -x &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml`'s `[project].version`, parsed with `tomllib`, equals `0.7.0`, and the file
      contains exactly one line matching `^version = ` (the sole-literal property).
    - `README.md` contains exactly one line matching `**Status**: Stable (v0.7.0)`.
    - `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints `0.7.0`.
    - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked` exits 0.
    - `uv run pytest tests/test_readme_version_sync.py tests/test_extension.py -x` is green.
    - `git diff -- uv.lock` over this commit changes only the `typsphinx` package's own `version`
      field. If any third-party package version also moved, that fact is reported in the SUMMARY
      rather than committed silently.
    - `git tag -l v0.7.0` returns empty and `git ls-remote --tags origin v0.7.0` returns empty after
      this plan's commits.
    - `git diff --stat -- .planning/REQUIREMENTS.md` over this plan's commits is empty (no checkbox
      or Traceability-row flip).
  </acceptance_criteria>
  <done>All three lockstep sites read 0.7.0, `typsphinx.__version__` reports it, the lockfile validates under `--locked`, and both version-sync guards are green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Newly created symbols, files, and CI step names across ALL of Phase 41 (listed so the
plan-review-convergence source-grounding pass excludes them from drift verification):

**Files created**
- `scripts/extract_changelog_section.py`
- `tests/test_changelog_extraction.py`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPH-BAR.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPHBAR-SIGNOFF.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-GREEN-TREE-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/translations-repo/` (working clone,
  D-17 — NEVER committed, removed at the end of plan 41-04)

**Functions / constants created**
- `scripts/extract_changelog_section.py`: `extract_section(changelog_text: str, version: str) -> str`,
  `REPO_ROOT`, `DEFAULT_CHANGELOG_PATH`, `_SECTION_HEADER_RE`; CLI positional `version`, optional
  `--changelog-path`

**Test functions created**
- `tests/test_changelog_extraction.py`: `test_extracts_real_version`,
  `test_section_terminates_at_next_version_heading`, `test_absent_version_fails`,
  `test_empty_section_fails`, `test_unreleased_headings_do_not_leak`, `test_changelog_path_override`,
  plus module-private `_run_extractor` and constant `SCRIPT_PATH`

**CI step names created**
- `.github/workflows/release.yml`, `validate` job: `Verify CHANGELOG has a section for this version`

**Markdown anchors created (this plan)**
- `CHANGELOG.md`: the `## [0.7.0]` heading and the `[0.7.0]:` link-reference line
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pyproject.toml` version literal -> published package identity | The bumped version becomes the PyPI artifact name at `/gsd-complete-milestone` |
| `CHANGELOG.md` file content -> GitHub Release body | Repo prose becomes published prose via plan 41-01's extractor |
| `uv lock` resolver -> `uv.lock` | An automated resolver rewrites a committed lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-05 | Tampering | `uv lock` silently advancing a third-party pin inside the release-prep commit | medium | mitigate | `git diff -- uv.lock` is inspected before the commit and the expected shape (only the `typsphinx` entry's own `version` field moving) is stated as an acceptance criterion. Any third-party movement is reported, not committed silently — it would invalidate the `### Verified` zero-new-dependency claim plan 41-06 must prove. |
| T-41-06 | Spoofing | The version claimed in `README.md` / `CHANGELOG.md` diverging from the packaged version | low | mitigate | Two pre-existing guards pin the pair (`tests/test_readme_version_sync.py` comparing parsed values against each other, and `tests/test_extension.py::test_version_matches_pyproject_toml` via `importlib.metadata`); both are run as this task's verify. |
| T-41-07 | Repudiation | A historical CHANGELOG entry being silently reworded while the new one is inserted | low | mitigate | The acceptance criteria bound the diff to a single removed line (the rewritten `[Unreleased]:` compare link), so any historical edit shows up as an extra removal and fails the check. Reinforced by this plan's second `must_haves.prohibitions` entry. |
| T-41-08 | Elevation of Privilege | An irreversible publish action taken during prep | medium | mitigate | The plan writes files only. Both tag-emptiness probes (`git tag -l v0.7.0`, `git ls-remote --tags origin v0.7.0`) are acceptance criteria on Task 2, and the first `must_haves.prohibitions` entry names every forbidden action explicitly. |
| T-41-SC | Tampering | npm/pip/cargo installs — supply chain | low | mitigate | This plan installs no new package. `41-RESEARCH.md` § "Package Legitimacy Audit" records the audit as not applicable because the phase adds none. `pyproject.toml`'s `dependencies` array and `[dependency-groups]` are asserted byte-unchanged; only the `version` field moves. |

**No `high`-severity threat was identified for this plan** (`security_asvs_level: 1`,
`security_block_on: high`). The register is complete, not empty: this plan adds no authentication,
session, access-control, cryptographic, or external-input surface — it edits four repo files, and the
only automated agent touching them is `uv lock`, whose output is diff-inspected.
</threat_model>

<verification>
- `uv run pytest tests/test_readme_version_sync.py tests/test_extension.py -x` — green.
- `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked` — exits 0.
- `uv run python -c "import typsphinx; print(typsphinx.__version__)"` — prints `0.7.0`.
- `uv run pytest tests/test_preview_version_sync.py -x` — green (the `@preview` surface is untouched
  by this plan; this is the control proving so).
- `git tag -l v0.7.0` and `git ls-remote --tags origin v0.7.0` — both empty.
- `git diff --stat -- .planning/REQUIREMENTS.md` over this plan's commits — empty.
</verification>

<success_criteria>
1. `## [0.7.0]` exists between the top `## [Unreleased]` and `## [0.6.5]`, with 5-6 bullets carrying
   requirement IDs (D-01), the D-02 section split, no BREAKING label (D-03), a readability-axis lead
   paragraph (D-04), and a three-item `### Verified` list identical in scope to 0.6.5's (D-05).
2. The tail link block carries the new release-tag line and the advanced `[Unreleased]` compare
   range.
3. No historical CHANGELOG entry changed, proven by the bounded diff.
4. `pyproject.toml` / `README.md` / `uv.lock` all read 0.7.0, `typsphinx.__version__` reports it, and
   `uv sync --extra dev --locked` is green.
5. Nothing irreversible happened, and REQUIREMENTS.md is untouched.
</success_criteria>

<output>
Create `.planning/phases/41-v0-7-0-release-automation-release-prep/41-02-SUMMARY.md` when done.
</output>