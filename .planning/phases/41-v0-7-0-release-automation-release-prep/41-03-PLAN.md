---
phase: 41-v0-7-0-release-automation-release-prep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - typsphinx/translator.py
  - .planning/PROJECT.md
  - .planning/todos/pending/2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md
  - .planning/todos/pending/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md
  - .planning/todos/completed/2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md
  - .planning/todos/completed/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md
autonomous: true
requirements: [REL-05]

must_haves:
  truths:
    - "Parsing `TypstTranslator.visit_desc_sig_name.__doc__` as reStructuredText produces an EMPTY docutils warning stream, where the same parse against the pre-fix docstring produced an inline-emphasis diagnostic — recorded RED before the fix and GREEN after (D-12)."
    - "The `visit_desc_sig_name` fix is docstring-only: `git diff` over the fix commit touches no executable line in `typsphinx/translator.py`, so it creates NO GATE-01 fixture obligation and plan 41-06's SC#4 sweep must not flag it as an unfixtured handler change."
    - "The two already-fixed todos named in D-13 are present under `.planning/todos/completed/` and absent from `.planning/todos/pending/`, and the code facts each one describes are re-verified in this worktree before the move rather than assumed from CONTEXT.md."
    - "`.planning/PROJECT.md` contains an equal number of HTML comment open and close markers — the two unterminated Prior-footer comments are terminated (D-13)."
  artifacts:
    - typsphinx/translator.py
    - .planning/PROJECT.md
    - .planning/todos/completed/2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md
    - .planning/todos/completed/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md
  key_links:
    - "typsphinx/translator.py visit_desc_sig_name docstring -> autodoc -> the published API reference page built by tox -e docs-pdf in plan 41-05"
    - "todos/pending -> todos/completed (record state follows code state)"
  prohibitions:
    - statement: "MUST NOT take any irreversible release action — no `git tag`, no tag push, no `release.yml` trigger, no PyPI upload, no GitHub Release, no PR open or merge."
      category: safety
      requirement_id: REL-05
    - statement: "MUST NOT change any executable line of `typsphinx/translator.py` in this plan — the D-12 change is a docstring edit, and any behavioural change would create a GATE-01 fixture obligation this phase has explicitly scoped out."
      category: safety
      requirement_id: REL-05
    - statement: "MUST NOT file a todo to `completed/` on the strength of CONTEXT.md's recorded measurement alone — each one's code fact is re-verified in this worktree first."
      category: transparency
      requirement_id: REL-05
---

<objective>
Take the phase's two record-and-docstring items: D-12's `visit_desc_sig_name` docstring escape (the
one `typsphinx/` change this phase makes) and D-13's planning-record hygiene (file two
already-fixed todos to `completed/`, and terminate `PROJECT.md`'s two unterminated HTML comments).

Purpose: D-12's unbalanced `*` makes autodoc emit an inline-emphasis warning and inject a stray
`problematic` node into the published API reference — the exact page family this whole milestone
exists to make readable. Plan 41-05 runs `tox -e docs-pdf` for SC#3 evidence, so the warning has to
be gone before that evidence is collected. D-13's two todos describe defects that are already fixed
in code, so leaving them under `pending/` is record drift.

Output: a docstring fix with a recorded RED, two todo files relocated, and a comment-balanced
`PROJECT.md`.

**Known worktree-cleanup interaction — read before starting.** This plan MOVES two files, which is a
deletion plus an addition in git terms. `worktree.cleanup-wave` blocks any branch containing
deletions, with no bypass. That is expected here and is not a defect: the deletions are planned,
their scope is exactly the two todo paths listed in `files_modified`, and the wave merge is expected
to require a manual step. Record the two deleted paths verbatim in the SUMMARY so the manual merge
can be scoped against a measured list rather than a guess.

This plan takes NO irreversible release action.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<worktree_provisioning>
Worktree isolation is this project's STANDING execution mode (CLAUDE.md § "Worktree-isolated
execution"). Before ANY other command, run once from the worktree root:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do ln -sf "$(command -v $t)" ".venv/bin/$t"; done`

Then invoke EVERY command through `uv run`. A bare `pytest` imports the MAIN checkout's package,
which for Task 1 would read the MAIN tree's docstring and make the fix look like it never landed.
</worktree_provisioning>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the visit_desc_sig_name docstring, RED first</name>
  <files>typsphinx/translator.py</files>
  <read_first>
    - `typsphinx/translator.py` — read the whole `visit_desc_sig_name` method including its docstring
      (the `def` is at roughly line 6564 and the docstring runs to roughly line 6610; re-measure with
      `grep -n "def visit_desc_sig_name" typsphinx/translator.py` rather than trusting these
      numbers). The defect is in the docstring's final paragraph, in the phrase describing
      37-04-SUMMARY.md's unresolved-C-domain-type measurement: the C type expression there contains
      a single, unpaired asterisk, which reStructuredText reads as an inline-emphasis start-string
      with no end-string.
    - `.planning/todos/pending/2026-08-01-visit-desc-sig-name-docstring-unbalanced-asterisk-warning.md`
      — the recorded problem statement and the observed autodoc diagnostic.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-PATTERNS.md` § the
      `typsphinx/translator.py` docstring entry — the two acceptable fix shapes (wrap the expression
      in a double-backtick inline literal, or move the asterisk so the token pairs).
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` § D-12.
  </read_first>
  <action>
**Step 1 — establish the RED.** Before editing anything, run a docutils parse of the live docstring
and capture the diagnostic. Use `inspect.cleandoc` to strip the uniform indentation, then
`docutils.core.publish_doctree` with `settings_overrides` of `report_level=2`, `halt_level=5`, and a
`warning_stream` bound to an in-memory `io.StringIO`, and print the stream's contents. Confirm the
captured stream is NON-EMPTY and names an inline-emphasis start-string problem. Save that output —
it is the RED.

If the stream comes back empty at this point, the probe is not measuring the defect. Do not proceed
on the assumption that the defect is already gone: instead find a probe that does reproduce the
autodoc diagnostic recorded in the todo file (for example, building the API reference page for this
module and reading the build's warning output) and record which probe you used and why the first one
did not fire. An unreproduced RED is not a pass.

**Step 2 — fix.** Edit ONLY the docstring. Make the C type expression a reStructuredText inline
literal by wrapping it in double backticks, so the asterisk is no longer parsed as emphasis markup.
Do not reword the surrounding sentence, do not restate the docutils diagnostic text inside the
docstring, and do not touch a single executable line of the method or of the file.

**Step 3 — GREEN.** Re-run the identical probe from Step 1 and confirm the warning stream is now
empty. Record both outputs.

**Step 4 — record the non-obligation.** In the commit body, state explicitly that this change alters
no emitted `.typ` shape and therefore creates NO GATE-01 fixture obligation under milestone
invariant #4. Plan 41-06's SC#4 sweep will see `visit_desc_sig_name` in its changed-handler census
for the range and needs this stated in the record so it can classify the change correctly rather
than flagging an unfixtured handler change.

Commit with `fix(41-03): escape the unbalanced asterisk in visit_desc_sig_name's docstring (D-12)`.
  </action>
  <verify>
    <automated>uv run python -c "
import io, inspect, docutils.core
from typsphinx.translator import TypstTranslator
doc = inspect.cleandoc(TypstTranslator.visit_desc_sig_name.__doc__)
w = io.StringIO()
docutils.core.publish_doctree(doc, settings_overrides={'report_level':2,'halt_level':5,'warning_stream':w})
assert w.getvalue()=='', 'docutils still reports:\n'+w.getvalue()
print('docstring parses clean')
" &amp;&amp; uv run pytest tests/test_translator.py -x -q &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <acceptance_criteria>
    - The pre-fix docutils probe output is captured and NON-EMPTY, and it is quoted in the SUMMARY as
      the recorded RED. If a substitute probe was needed, the substitution and its reason are
      recorded.
    - The post-fix docutils probe output is EMPTY.
    - `git diff <pre-fix-commit>..HEAD -- typsphinx/translator.py` changes lines that lie inside the
      `visit_desc_sig_name` docstring only. Assert mechanically that the diff adds and removes no
      line containing `self.`, `return`, `if `, `raise`, or `def ` — i.e. no executable line moved.
    - `uv run pytest tests/test_translator.py -x -q` is green, and the full non-slow suite
      (`uv run pytest -m "not slow" -q`) shows no new failures relative to the pre-fix run.
    - `uv run black --check .`, `uv run ruff check .`, and `uv run mypy typsphinx/` all exit 0.
    - The commit body states that the change creates no GATE-01 fixture obligation, with the reason.
  </acceptance_criteria>
  <done>The docstring parses without a docutils diagnostic, the RED was reproduced first, the diff is docstring-only, and the no-fixture-obligation reason is on the record for plan 41-06.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify and file the two already-fixed todos to completed/</name>
  <files>.planning/todos/pending/2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md, .planning/todos/pending/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md` — the
      defect it describes, in full.
    - `.planning/todos/pending/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md`
      — the defect it describes, in full.
    - `typsphinx/translator.py` around line 189 (`_desc_break_marker`'s assignment) and around lines
      5352 and 5695 (its two comparison sites) — re-measure the line numbers with grep; CONTEXT.md's
      numbers were taken at commit `279aea5` and the file has moved since.
    - `tests/test_signature_page_boundary_render_gate.py` around line 147 — the page-count ceiling
      constant.
    - `.planning/todos/completed/` — list its contents to confirm the naming convention files keep
      when they move (same filename, same directory-relative shape).
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` § D-13.
  </read_first>
  <action>
Re-verify each todo against THIS worktree's code before moving it. CONTEXT.md records both as already
fixed, but a recorded measurement is not a current measurement — this project's standing rule is that
a claim gets re-measured rather than transcribed.

- **`desc-break-marker-stale-across-body-buffer-swaps`**: confirm that `_desc_break_marker` is
  assigned a tuple of the body buffer's identity and its length — i.e. it carries both `id(self.body)`
  and `len(self.body)` — and that BOTH of its comparison sites compare against that same tuple shape.
  Locate all three sites by grep, quote the current line numbers and the lines themselves.
- **`expected-page-count-pre-phase-misnamed-post-phase-value`**: confirm the constant is now named
  `EXPECTED_PAGE_COUNT_CEILING` and that the old `EXPECTED_PAGE_COUNT_PRE_PHASE` name no longer
  appears anywhere under `tests/`. Quote the current definition line.

If either fact does NOT hold in this worktree, STOP and report it in the SUMMARY instead of moving
the file — the todo is then still genuinely pending and D-13's premise for that item has changed.

When both hold, move each file with `git mv` from `.planning/todos/pending/` to
`.planning/todos/completed/`, keeping the filename unchanged. Append to each moved file a short
`## Resolved` section recording: the phase that filed it (41), the date, the commit or file/line
evidence you just quoted proving the fix is in place, and the fact that the fix landed in an earlier
phase rather than in Phase 41 (Phase 41 files the record, it does not make the fix).

Do NOT move any other todo. In particular, leave the four todos D-14 defers to v0.7.1+ under
`pending/` (`add-sphinx-linkcheck-ci-job`, `non-str-docname-typeerror-in-typstpdf-finish`,
`derive-typst-lang-duplicated-warning-block`, `modernize-typing-imports-drop-up006-up035-ignore`),
and leave `release-notes-body-from-changelog-section` and
`visit-desc-sig-name-docstring-unbalanced-asterisk-warning` under `pending/` as well — those two are
resolved by this phase's own code work, and plan 41-07 records their filing as a close-side handoff
item alongside the REL-04/REL-05 checkbox flips.

Commit with `docs(41-03): file two already-fixed todos to completed (D-13)`.
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md &amp;&amp; test -f .planning/todos/completed/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md &amp;&amp; test ! -e .planning/todos/pending/2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md &amp;&amp; test ! -e .planning/todos/pending/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md &amp;&amp; test $(ls .planning/todos/pending/ | wc -l) -eq 7 &amp;&amp; uv run python -c "
import pathlib
for n in ['2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md','2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md']:
    t=pathlib.Path('.planning/todos/completed')/n
    assert '## Resolved' in t.read_text(encoding='utf-8'), n
print('todos filed with resolution evidence')
"</automated>
  </verify>
  <acceptance_criteria>
    - Both named files exist under `.planning/todos/completed/` and neither exists under
      `.planning/todos/pending/`.
    - `.planning/todos/pending/` holds exactly 7 files afterwards (9 before, minus these 2).
    - Each moved file contains a `## Resolved` section quoting the file/line evidence measured in
      THIS worktree, not copied from CONTEXT.md.
    - The four D-14-deferred todos are still present under `pending/`, by name.
    - `git log --diff-filter=D --name-only` over this plan's commits lists exactly the two
      `pending/` paths and no others — the deletion scope is measured, which is what the wave's
      manual merge will be checked against.
  </acceptance_criteria>
  <done>Both todos are filed with re-measured resolution evidence, nothing else moved, and the deletion scope is recorded for the cleanup guard.</done>
</task>

<task type="auto">
  <name>Task 3: Terminate PROJECT.md's two unterminated HTML comments</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` — read the whole trailing "Prior footer" region (from the first
      `<!-- Prior:` line to end of file). Do not trust CONTEXT.md's line numbers 492 and 506: they
      were measured at commit `279aea5` and the file has grown by roughly 250 lines since. As of the
      most recent measurement the file holds 31 comment-open markers and 29 comment-close markers,
      with the two unterminated openers at lines 761 and 775 — but re-derive this mechanically rather
      than trusting these numbers either.
    - `.planning/todos/pending/2026-07-29-project-md-unterminated-html-comments.md` — the recorded
      problem statement.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` § D-13.
  </read_first>
  <action>
Find the unterminated comments MECHANICALLY, not by eye. For each comment-open marker in
`.planning/PROJECT.md`, check whether a comment-close marker occurs before the NEXT comment-open
marker; an opener with no close before the next opener is unterminated. Print the line numbers and
the first 120 characters of each offending line so the SUMMARY can quote them.

There should be exactly two. If the count is not two, report the actual count and the list rather
than fixing an assumed number — the file is edited on every phase close and the defect set can drift.

Terminate each one by appending a closing marker at the end of that comment's intended content. Each
of these is a single-line "Prior footer" comment whose content ends with a closing italic marker, so
the fix is to append the closing marker to the end of that same line. Do not reflow the line, do not
edit the comment's prose, and do not touch any of the correctly-terminated Prior comments.

Verify by re-running the mechanical scan: zero unterminated openers, and the total count of open
markers equals the total count of close markers.

Do NOT make any other edit to `PROJECT.md` in this plan. In particular do not update its "Last
updated" footer or add a new Prior entry — the phase-close and milestone-close workflows own that,
and an extra edit here would collide with them.

Commit with `docs(41-03): terminate PROJECT.md's two unterminated HTML comments (D-13)`.
  </action>
  <verify>
    <automated>uv run python -c "
import re
s=open('.planning/PROJECT.md',encoding='utf-8').read()
opens=[m.start() for m in re.finditer(r'&lt;!--', s)]
closes=[m.start() for m in re.finditer(r'--&gt;', s)]
unterm=[]
for i,o in enumerate(opens):
    nxt = opens[i+1] if i+1 &lt; len(opens) else len(s)
    if not any(o &lt; c &lt; nxt for c in closes):
        unterm.append(s[:o].count(chr(10))+1)
assert len(opens)==len(closes), (len(opens), len(closes))
assert unterm==[], unterm
print('PROJECT.md comment markers balanced:', len(opens), 'pairs')
"</automated>
  </verify>
  <acceptance_criteria>
    - The mechanical scan reports zero unterminated comment openers in `.planning/PROJECT.md`.
    - The count of comment-open markers equals the count of comment-close markers in
      `.planning/PROJECT.md`.
    - The SUMMARY quotes the two line numbers found BEFORE the fix and the first 120 characters of
      each, measured in this worktree.
    - `git diff -- .planning/PROJECT.md` over this plan's commits shows exactly two changed lines,
      and each change is an append at end-of-line (no prose reflow, no other section touched).
    - The visible rendered content of `PROJECT.md` is unchanged — the fix only closes comments that
      were already meant to hide their content.
  </acceptance_criteria>
  <done>Comment markers are balanced, the pre-fix offender list is recorded, and the diff is two end-of-line appends.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Newly created symbols, files, and CI step names across ALL of Phase 41 (listed so the
plan-review-convergence source-grounding pass excludes them from drift verification):

**Files created**
- `scripts/extract_changelog_section.py`
- `tests/test_changelog_extraction.py`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPH-BAR.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPHBAR-SIGNOFF.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-GREEN-TREE-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/translations-repo/` (working clone,
  D-17 — NEVER committed, removed at the end of plan 41-04)

**Files relocated (this plan)**
- `.planning/todos/completed/2026-08-01-desc-break-marker-stale-across-body-buffer-swaps.md`
- `.planning/todos/completed/2026-08-01-expected-page-count-pre-phase-misnamed-post-phase-value.md`
- Each gains a new `## Resolved` section.

**Functions / constants created**
- `scripts/extract_changelog_section.py`: `extract_section(changelog_text: str, version: str) -> str`,
  `REPO_ROOT`, `DEFAULT_CHANGELOG_PATH`, `_SECTION_HEADER_RE`; CLI positional `version`, optional
  `--changelog-path`

**Test functions created**
- `tests/test_changelog_extraction.py`: `test_extracts_real_version`,
  `test_section_terminates_at_next_version_heading`, `test_absent_version_fails`,
  `test_empty_section_fails`, `test_unreleased_headings_do_not_leak`, `test_changelog_path_override`,
  plus module-private `_run_extractor` and constant `SCRIPT_PATH`

**CI step names created**
- `.github/workflows/release.yml`, `validate` job: `Verify CHANGELOG has a section for this version`

**Markdown anchors created**
- `CHANGELOG.md`: the `## [0.7.0]` heading and the `[0.7.0]:` link-reference line

**Explicitly NOT created**
- No new function, class, constant, or test in `typsphinx/` — this plan's only `typsphinx/` change is
  a docstring edit inside the existing `visit_desc_sig_name` method.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator docstring -> autodoc -> published API reference page | Docstring prose becomes rendered documentation |
| `todos/pending` -> `todos/completed` | A record assertion that a defect is fixed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-09 | Tampering | An "docstring-only" edit silently changing translator behaviour | medium | mitigate | The diff is asserted to add and remove no line containing `self.`, `return`, `if `, `raise`, or `def `; `tests/test_translator.py` plus the non-slow suite are run as the control. If any executable line moved, milestone invariant #4 would demand a GATE-01 fixture this phase has scoped out — so the assertion is the gate, not a formality. |
| T-41-10 | Repudiation | Filing a todo to `completed/` on a stale claim rather than a current measurement | medium | mitigate | Each todo's code fact is re-measured in THIS worktree, the measured file/line evidence is quoted into the moved file's `## Resolved` section, and the task stops rather than moving the file if the fact does not hold. Reinforced by this plan's third `must_haves.prohibitions` entry. |
| T-41-11 | Information Disclosure | Terminating a comment in a way that reveals content intended to stay hidden, or hides content intended to be visible | low | mitigate | The fix appends a closing marker at end-of-line on the two offending single-line comments only; the acceptance criteria bound the diff to two changed lines and require the rendered content to be unchanged. |
| T-41-12 | Elevation of Privilege | An irreversible publish action taken during prep | medium | mitigate | This plan edits a docstring and moves two files. `git tag -l v0.7.0` / `git ls-remote --tags origin v0.7.0` emptiness is carried as a phase-level check in plan 41-07, and the first `must_haves.prohibitions` entry names every forbidden action. |
| T-41-SC | Tampering | npm/pip/cargo installs — supply chain | low | mitigate | This plan installs nothing. `41-RESEARCH.md` § "Package Legitimacy Audit" records the audit as not applicable because the phase adds no package. Asserted mechanically: `git diff --stat -- pyproject.toml uv.lock` over this plan's commits is empty. |

**No `high`-severity threat was identified for this plan** (`security_asvs_level: 1`,
`security_block_on: high`). The register is complete, not empty: this plan adds no external-input,
authentication, session, access-control, or cryptographic surface — its two risks are both integrity
risks about a change being larger or less-evidenced than it claims, and both are bounded by measured
diff assertions.
</threat_model>

<verification>
- The docutils probe on `TypstTranslator.visit_desc_sig_name.__doc__` returns an empty warning
  stream, with the pre-fix non-empty output recorded alongside it.
- `uv run pytest -m "not slow" -q` — no new failures relative to the pre-fix baseline.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` — all exit 0.
- `.planning/PROJECT.md` comment-open and comment-close marker counts are equal, with zero
  unterminated openers.
- `.planning/todos/pending/` holds exactly 7 files; the two D-13 todos are under `completed/` with
  `## Resolved` sections.
- `git diff --stat -- pyproject.toml uv.lock .planning/REQUIREMENTS.md` over this plan's commits is
  empty.
- `git tag -l v0.7.0` and `git ls-remote --tags origin v0.7.0` — both empty.
</verification>

<success_criteria>
1. The `visit_desc_sig_name` docstring parses as reStructuredText with no docutils diagnostic, with
   the pre-fix RED recorded first (D-12).
2. The change is provably docstring-only, and the commit body records why it creates no GATE-01
   fixture obligation — the input plan 41-06's SC#4 sweep needs.
3. The two D-13 todos are filed to `completed/` with re-measured evidence, and nothing else moved.
4. `.planning/PROJECT.md`'s HTML comment markers are balanced with the pre-fix offender list on the
   record (D-13).
5. The deletion scope for the wave's `worktree.cleanup-wave` block is measured and recorded.
</success_criteria>

<output>
Create `.planning/phases/41-v0-7-0-release-automation-release-prep/41-03-SUMMARY.md` when done.
</output>