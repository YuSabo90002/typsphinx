---
phase: 41-v0-7-0-release-automation-release-prep
plan: 06
type: execute
wave: 2
depends_on: [41-01, 41-02, 41-03]
files_modified:
  - .planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md
autonomous: true
requirements: [REL-05]

must_haves:
  truths:
    - "The SHA range for the sweep is RE-MEASURED at execution time via `git merge-base main HEAD`, not transcribed from a planning-time constant, and both the base SHA and the commit count are recorded with the commands that produced them."
    - "Invariant 1 is proven mechanically: `pyproject.toml`'s runtime `dependencies` array and its dependency groups are byte-identical between the milestone base and HEAD, shown by the actual diff output rather than asserted."
    - "Invariant 2 is proven mechanically: the four bundled `@preview` package version strings are unchanged across every sync surface, the surface count is still the same set, and no NEW version-lockstep site was introduced anywhere in the milestone diff."
    - "Invariant 3's handler census enumerates every `visit_*`/`depart_*` handler changed in `typsphinx/translator.py` across the re-measured range, produced by hunk-line attribution rather than by trusting diffstat line counts, and the enumeration includes Phase 40.1's handler changes (D-11)."
    - "Each changed handler is mapped to its covering gate module by NODE name — the `visit_`/`depart_` prefix stripped before searching — because searching for the Python method name produces false negatives for roughly a quarter of the handlers."
    - "Every handler whose only covering module is a SINGLE hit is spot-checked against that module's actual `assert` statements, not accepted on the search hit alone, and the spot-check result is recorded per handler."
    - "`visit_desc_sig_name` appears in the census because of this phase's own D-12 docstring edit, and is classified explicitly as a docstring-only change carrying NO GATE-01 fixture obligation, with the reason recorded rather than left implicit."
    - "The first two `### Verified` claims in the new CHANGELOG `## [0.7.0]` entry are cross-checked against this sweep's measured output, and any divergence is flagged rather than passed over."
  artifacts:
    - .planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md
  key_links:
    - "git merge-base main HEAD -> the milestone base SHA -> every measurement in this sweep"
    - "changed handler -> node name -> covering gate module -> that module's assert statements"
    - "40.1-NONREGRESSION.md section 4 -> Phase 40.1's change-site-to-RED manifest -> invariant 3's 40.1 coverage"
    - "this sweep's output -> CHANGELOG ### Verified claims 1 and 2"
  prohibitions:
    - statement: "MUST NOT take any irreversible release action — no `git tag`, no tag push, no `release.yml` trigger, no PyPI upload, no GitHub Release, no PR open or merge."
      category: safety
      requirement_id: REL-05
    - statement: "MUST NOT accept a search hit as proof that a handler's change is covered by a gate — a module that merely mentions a node name in prose or a docstring is not a fixture, and the single-hit handlers are spot-checked against real assertions."
      category: transparency
      requirement_id: REL-05
    - statement: "MUST NOT record a skipped or non-executed check as evidence of a pass, and MUST NOT transcribe a planning-time SHA or commit count in place of a value measured at execution time."
      category: transparency
      requirement_id: REL-05
    - statement: "MUST NOT commit the census script or any sweep tooling — the artifacts under comparison are one-off by construction, and a script that looks like a gate but never runs in CI manufactures confidence."
      category: transparency
      requirement_id: REL-05
---

<objective>
Prove ROADMAP SC#4 mechanically over the SHA-anchored full milestone diff: zero new runtime
dependencies, the `@preview` package count still four with no new version-lockstep site, and every
node-handler change carrying its recorded-RED GATE-01 fixture — with the sweep range covering Phase
40.1's handler changes as well as Phases 36-40's (D-11).

Purpose: these three are the milestone's standing invariants, and the CHANGELOG's `### Verified`
section asserts the first two to users. This plan is where the assertions become measurements. The
third invariant is the substantive one: `typsphinx/translator.py` moved by roughly 1,700 lines across
the milestone, and SC#4 requires that every changed node handler be traceable to a fixture that was
recorded RED before its fix landed.

Output: `41-SC4-INVARIANTS.md`, with each invariant's command, verbatim output, and verdict.

**The census tooling is deliberately NOT committed.** Phase 29 D-15 and Phase 30.1 D-03 both rejected
committing a one-off comparison script, and `41-RESEARCH.md` carries the reasoning forward: the
artifact under comparison is a SHA-anchored diff range that exists once. Write the census as a
scratch script under `/tmp`, run it, transcribe its output, and do not add it to the repository.

This plan takes NO irreversible action and changes NO source or test file. It measures.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<worktree_provisioning>
Worktree isolation is this project's STANDING execution mode (CLAUDE.md § "Worktree-isolated
execution"). Before ANY other command, run once from the worktree root:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do ln -sf "$(command -v $t)" ".venv/bin/$t"; done`

Then invoke EVERY command through `uv run`.

Confirm the post-bump state reached this worktree before measuring:
`uv run python -c "import typsphinx; print(typsphinx.__version__)"` must print `0.7.0`, and
`git log --oneline -1` must show a commit from this phase's wave 1. If not, the wave-1 merges have
not landed here and the sweep would describe the wrong tree.
</worktree_provisioning>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md
@.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md
@.planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the range, then prove the dependency and @preview invariants</name>
  <files>.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md</files>
  <read_first>
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` §
      "Zero-new-dependency check" and § "`@preview` version-sync check" — the exact commands, and the
      measured outputs from the research session (base `51e02b6`, the three-line `dependencies`
      array, the four package pins, and the note that test-fixture `.typ` files legitimately mirror
      the same import block without being a new production sync site).
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` § "Pitfall 3" — the
      commit count is a moving target. CONTEXT.md recorded 328, the research session re-measured 369,
      and a later measurement gave 371. Do not transcribe any of them; measure now.
    - `CLAUDE.md` § "The `@preview` version-sync hazard" — the three declaration sites that must stay
      in lockstep, and that `tests/test_preview_version_sync.py` asserts they agree.
    - `tests/test_preview_version_sync.py` — read it in full to learn exactly which surfaces it
      watches, so the sweep can say precisely what is and is not guarded.
    - `.planning/STATE.md` § "Blockers/Concerns" — the standing note that
      `docs/source/_typst/custom_template.typ` is a pre-existing FOURTH lockstep site outside that
      test's watched surface, carried as a Warning since Phase 30.1. This is an already-acknowledged
      cost, not a new finding, and `docs/` is out of this phase's scope.
    - `.planning/milestones/v0.6.5-phases/35-v0-6-5-release-prep/35-RELEASE-EVIDENCE.md` § "SC#4" —
      the section shape, including its "Diff-range re-measurement (source of truth for this section)"
      subsection, which is exactly the discipline this task repeats.
    - `CHANGELOG.md` § the new `## [0.7.0]` entry's `### Verified` list — claims 1 and 2 are the ones
      this task measures.
  </read_first>
  <action>
Create `41-SC4-INVARIANTS.md` opening with a `## Diff-range re-measurement (source of truth)`
section. Run and transcribe verbatim:
- `git merge-base main HEAD` — the milestone base SHA. Record it and confirm from
  `git describe --tags <base>` how it relates to the `v0.6.5` tag.
- `git rev-parse HEAD` — the tip being measured.
- `git log --oneline <base>..HEAD | wc -l` — the commit count, measured NOW. Do not carry forward
  328, 369, or 371 from any earlier document; those are stale by construction on an actively
  developed branch, and this phase's own commits keep adding to it.
- A confirmation that Phase 40.1's commits fall inside the range: list the 40.1 commits found by
  `git log --oneline <base>..HEAD -- typsphinx/translator.py` filtered to `40.1` in the subject, or
  cross-check the GREEN commit SHAs recorded in `40.1-NONREGRESSION.md` § 4 against `git merge-base
  --is-ancestor`. D-11 makes this coverage a requirement, not a nicety.

Use the re-measured base SHA — call it BASE — for every command below.

**`## Invariant 1 of 3 — zero new runtime dependencies`.** Run `git diff BASE..HEAD -- pyproject.toml`
and transcribe the FULL diff, not a filtered slice, so the version-field change plan 41-02 made is
visible alongside everything else. Then print the `dependencies` array from both sides
(`git show BASE:pyproject.toml` and `git show HEAD:pyproject.toml`) and transcribe both. Also compare
the dependency-group / dev-extra declarations across the two sides. State the verdict: identical or
not. The expected result is that only the `version` field moved. If a dependency was added, changed,
or removed, that is a milestone-invariant breach and must be reported as a phase finding, not
absorbed into a verdict paragraph.

Also transcribe `git diff BASE..HEAD -- uv.lock | head -60` and state whether any THIRD-PARTY package
version moved in the lockfile, cross-referencing what plan 41-02's SUMMARY recorded about its own
`uv lock` run.

**`## Invariant 2 of 3 — the @preview surface`.** Run a grep for `@preview` across
`typsphinx/writer.py`, `typsphinx/template_engine.py`, and `typsphinx/templates/base.typ` and
transcribe every hit with its line number. Confirm the four package pins are the same four names and
the same four version strings on both sides of the range by running the same grep against
`git show BASE:<path>` output for each file. Then run
`git diff --diff-filter=A --name-only BASE..HEAD` piped through a search for `@preview` to find any
NEWLY ADDED file that declares a `@preview` import, and classify each hit: a test-fixture `.typ` or
fixture `conf.py` mirroring the same import block is expected and is NOT a new production sync site;
anything under `typsphinx/` or `examples/` would be. Record the classification per hit rather than a
bare count.

Note explicitly, as a carried-forward Warning and not a new finding, that
`docs/source/_typst/custom_template.typ` is a pre-existing fourth lockstep site outside
`tests/test_preview_version_sync.py`'s watched surface, and that `docs/` had zero line changes this
milestone so it is unchanged and out of this phase's scope.

**`### Cross-check — CHANGELOG `### Verified` claims 1 and 2`.** Quote the first two `### Verified`
bullets from the new `## [0.7.0]` CHANGELOG entry verbatim, place this task's measured results beside
them, and state per claim whether it holds. If either does not, flag it plainly — plan 41-02 wrote
those claims before they could be measured, and this is the designed place where that loop closes.
Do not edit `CHANGELOG.md` from this plan; flag and report.

Commit with `docs(41-06): prove the dependency and @preview milestone invariants (SC#4)`.
  </action>
  <verify>
    <automated>uv run python -c "
import pathlib
t=pathlib.Path('.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md').read_text(encoding='utf-8')
for needle in ['Diff-range re-measurement','merge-base','Invariant 1 of 3','Invariant 2 of 3','@preview','Cross-check','40.1']:
    assert needle in t, needle
print('invariants 1-2 recorded')
" &amp;&amp; test -z "$(git status --porcelain -- typsphinx tests scripts)"</automated>
  </verify>
  <acceptance_criteria>
    - The range section carries `git merge-base main HEAD`'s output, `git rev-parse HEAD`'s output,
      and a commit count measured during this task — none of `328`, `369`, or `371` appears as a
      claimed current value.
    - Phase 40.1's presence inside the range is demonstrated by SHA, not asserted.
    - Invariant 1 shows the full `pyproject.toml` diff plus both sides' `dependencies` arrays
      verbatim, with a stated verdict; any non-`version` change is reported as a finding.
    - The `uv.lock` diff is inspected and any third-party version movement is stated explicitly.
    - Invariant 2 lists every `@preview` hit with file and line for all three sync sites, on both
      sides of the range, and shows the four package names and version strings agree.
    - Every newly-added file containing a `@preview` import is listed and classified as
      fixture-mirror or new-production-site; no bare count.
    - The pre-existing fourth lockstep site under `docs/` is named as a carried Warning, not as a new
      finding.
    - The cross-check subsection quotes CHANGELOG `### Verified` claims 1 and 2 verbatim with a
      hold/does-not-hold verdict each.
    - `git status --porcelain -- typsphinx tests scripts` is empty; no census tooling is added to the
      repository.
  </acceptance_criteria>
  <done>The range is re-measured from git, both mechanical invariants are shown with verbatim output and stated verdicts, and the CHANGELOG's first two Verified claims are measured against them.</done>
</task>

<task type="auto">
  <name>Task 2: Census every changed node handler and map each to its covering gate module</name>
  <files>.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md</files>
  <read_first>
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` § "Code Examples" >
      "The handler census script (verified working this session)" — the hunk-boundary plus
      function-start-line attribution method, and its measured output of 51 touched handlers with the
      full list. Copy the method; re-derive the list.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` § "Pitfall 4" — why
      searching test files for the Python method name (with its `visit_`/`depart_` prefix) produces
      FALSE NEGATIVES for roughly a quarter of the handlers, and that the fix is to strip the prefix
      and search for the underlying docutils/Sphinx NODE name.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` § "Mapping each
      handler to its covering gate module" — the representative node-family-to-gate-module table, as
      a cross-check on your own derivation, not as a substitute for it.
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` § "Don't
      Hand-Roll" — why an AST-based diff was rejected in favour of hunk-line attribution.
    - `typsphinx/translator.py` — skim the class structure enough to confirm the census's
      function-start detection assumption holds (one large class, `def` lines at a consistent
      indentation).
  </read_first>
  <action>
Write the census as a scratch Python script under `/tmp` — NOT under `scripts/`, NOT under `tests/`,
and never `git add`ed. Implement the method `41-RESEARCH.md` records:

- Read the file at both ends of the range with `git show BASE:typsphinx/translator.py` and
  `git show HEAD:typsphinx/translator.py`.
- Build, for each side, an ordered list of `(line_number, function_name)` from lines matching a
  `def <name>(` pattern.
- Walk `git diff -U0 BASE..HEAD -- typsphinx/translator.py`, tracking the old and new line cursors
  from each hunk header, and attribute every removed line to the enclosing function on the BASE side
  and every added line to the enclosing function on the HEAD side.
- Collect the union, then filter to names beginning `visit_` or `depart_`.

Transcribe into a `## Invariant 3 of 3 — every node-handler change carries a recorded-RED GATE-01
fixture` section: the count, the full sorted list, and separately the handlers that are BRAND NEW on
the HEAD side (present in HEAD's function list, absent from BASE's). Also record the non-handler
methods the same census touched, as context for the scope statement.

Cross-check your count and list against `41-RESEARCH.md`'s recorded 51. A difference is expected and
is not an error — this phase's own D-12 commit adds `visit_desc_sig_name` activity, and the range tip
has moved. State the delta and explain each difference by name rather than reporting only a number.

Then build the coverage map. For each handler in the census, strip the leading `visit_` or `depart_`
to get the NODE name, and search `tests/*.py` for that node name to find covering gate modules.
Record, as a table, one row per handler: the handler, its node name, and every gate module that
matched. Record the count of matches per handler, because Task 3 acts on the single-match rows.

Explicitly record what this map does and does not establish: it shows the node type appears in that
module, which is necessary but NOT sufficient — it does not by itself prove that the module's
assertions exercise that handler's specific diff hunks. Say this in the file; Task 3 is the response
to it.

Commit with `docs(41-06): census the milestone's changed node handlers and map coverage (SC#4)`.
  </action>
  <verify>
    <automated>uv run python -c "
import pathlib, re
t=pathlib.Path('.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md').read_text(encoding='utf-8')
assert 'Invariant 3 of 3' in t
assert 'visit_citation' in t and 'depart_citation' in t and 'visit_desc_sig_name' in t
assert 'necessary but' in t.lower() or 'not sufficient' in t.lower(), 'the map must state its own limit'
rows=[l for l in t.splitlines() if l.strip().startswith('|') and ('visit_' in l or 'depart_' in l)]
assert len(rows) &gt;= 40, len(rows)
print('census recorded:', len(rows), 'handler rows')
" &amp;&amp; test -z "$(git status --porcelain -- typsphinx tests scripts)"</automated>
  </verify>
  <acceptance_criteria>
    - The census section records the handler count, the full sorted handler list, and separately the
      handlers brand-new on the HEAD side.
    - The census was produced by hunk-line attribution over `git diff -U0`, and the scratch script
      lives under `/tmp` — `git status --porcelain` shows no new script anywhere in the repository.
    - The delta against `41-RESEARCH.md`'s recorded count is stated and each differing handler is
      explained by name.
    - A coverage table exists with one row per censused handler carrying the handler name, the
      stripped node name, and every matching gate module, plus the match count.
    - The node-name stripping is used for the search; the file states why searching the method name
      would produce false negatives.
    - The file states in words that a search hit is necessary but not sufficient proof of coverage.
    - `git status --porcelain -- typsphinx tests scripts` is empty.
  </acceptance_criteria>
  <done>Every changed handler across the re-measured range is enumerated by hunk attribution and mapped to gate modules by node name, with the map's own limits stated and no tooling committed.</done>
</task>

<task type="auto">
  <name>Task 3: Spot-check the single-hit mappings, fold in 40.1's RED manifest, and classify D-12</name>
  <files>.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md</files>
  <read_first>
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RESEARCH.md` § "Open Questions"
      item 2 — the recommendation to spot-check the mapping against each module's actual `assert`
      statements for at least the handlers whose only covering module is a single hit, and the named
      examples among the signature pass-through handlers.
    - `.planning/phases/40.1-citation-degradation-hardening/40.1-NONREGRESSION.md` § 4 — the
      change-site-to-RED manifest, written expressly for this sweep: per row, the change site, the
      warning, the evidence file, the RED form, the RED's provenance, the pytest selector, and the
      commit the RED was recorded against.
    - `.planning/STATE.md` § "Active Milestone" > "GATE-01 methodology change" — what "RED" means in
      this milestone: every design defect compiles successfully today, so RED is a structural, regex,
      or `pypdf` assertion defined before any code is written, with the citation work as the sole
      classic-fatal exception.
    - Each gate module named as a SINGLE hit by Task 2's table — read its `assert` statements
      directly. (`41-RESEARCH.md` flags the signature pass-through handlers as the likeliest
      false-positive family.)
    - `.planning/phases/41-v0-7-0-release-automation-release-prep/41-CONTEXT.md` § D-11 and § D-12.
  </read_first>
  <action>
**Step 1 — spot-check the single-hit rows.** From Task 2's coverage table, take every handler whose
match count is 1. For each, open that gate module and read its actual `assert` statements. Record, per
handler: the module, the specific assertion or assertions that exercise that node's behaviour (quote
them), and a verdict of COVERED or NOT COVERED. A module that mentions the node name only in a
docstring, a comment, or explanatory prose is NOT coverage — that is precisely the false-positive
this step exists to catch, and recording it as covered would be the silent failure SC#4 is meant to
prevent.

If any single-hit handler turns out NOT to be covered by a real assertion, record it as an open SC#4
gap with the handler name, the module inspected, and what was missing. Do not write a fixture to
close it here — that would be new translator-adjacent test work in a release-prep phase, which is
exactly the scope enlargement D-11 kept out of this phase. Report it as a finding for the owner.

**Step 2 — fold in Phase 40.1's RED manifest (D-11).** Read `40.1-NONREGRESSION.md` § 4 and reproduce
its change-site-to-RED mapping into this file as a `### Phase 40.1 coverage` subsection, citing the
source rather than re-deriving it — that table was written for this sweep. For each of its rows,
confirm the named evidence file exists at the recorded path and the recorded RED commit SHA resolves
in this repository (`git cat-file -e <sha>`). Record both confirmations. Then verify each of that
table's handler change sites appears in your Task 2 census; if one does not, state why (for example,
a change site that is a helper method rather than a `visit_`/`depart_` handler is legitimately absent
from a handler census — say so explicitly rather than leaving a silent gap).

**Step 3 — classify this phase's own translator change.** `visit_desc_sig_name` will appear in the
census because plan 41-03 edited its docstring under D-12. Record a `### This phase's own translator
change` subsection stating: the change is DOCSTRING-ONLY; it alters no emitted `.typ` shape; it
therefore creates NO GATE-01 fixture obligation under milestone invariant #4; and the proof is plan
41-03's diff assertion that no executable line moved. Reproduce that proof here by re-running the
check against the range (`git diff BASE..HEAD -- typsphinx/translator.py` scoped to the
`visit_desc_sig_name` docstring region) so this file stands on its own measurement rather than on a
cross-reference. Note that `visit_desc_sig_name` ALSO has real Phase 37-era behavioural changes
inside this same range, which are covered by the signature-typography gate — so the handler is
covered on its behaviour and unobligated on its docstring, and both facts belong in the record.

**Step 4 — the SC#4 verdict.** Close the file with a per-invariant verdict table: invariant, what was
measured, by which command, and PROVEN / NOT PROVEN / PARTIAL. Every NOT PROVEN or PARTIAL row states
what is missing. Then add a short "executed versus skipped" summary line for every command in this
file, following `40.1-NONREGRESSION.md` § 9's shape — a skip is not a pass.

Commit with `docs(41-06): spot-check handler coverage and record the SC#4 verdict`.
  </action>
  <verify>
    <automated>uv run python -c "
import pathlib
t=pathlib.Path('.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md').read_text(encoding='utf-8')
for needle in ['Phase 40.1 coverage','This phase','docstring-only','GATE-01','verdict','executed','skipped','spot-check']:
    assert needle.lower() in t.lower(), needle
assert 'PROVEN' in t or 'NOT PROVEN' in t or 'PARTIAL' in t
print('spot-check, 40.1 fold-in, D-12 classification, and verdict recorded')
" &amp;&amp; test -z "$(git status --porcelain -- typsphinx tests scripts)"</automated>
  </verify>
  <acceptance_criteria>
    - Every single-hit handler from Task 2's table has its own spot-check entry quoting the actual
      `assert` statement(s) inspected and carrying a COVERED or NOT COVERED verdict. No single-hit
      row is left resolved on the search hit alone.
    - Any NOT COVERED handler is recorded as an open SC#4 gap with the module inspected and what was
      missing, and is reported in the SUMMARY rather than closed by writing a new fixture here.
    - The `### Phase 40.1 coverage` subsection cites `40.1-NONREGRESSION.md` § 4, and for each of its
      rows records that the named evidence file exists and that the recorded RED commit SHA resolves
      (`git cat-file -e`).
    - Any 40.1 change site absent from the handler census is explained by name (for example, a helper
      method rather than a node handler), never left silent.
    - The `### This phase's own translator change` subsection states that D-12's edit is
      docstring-only, alters no emitted `.typ` shape, and carries no GATE-01 fixture obligation, and
      reproduces the no-executable-line-moved proof from its own measurement.
    - That subsection also records that `visit_desc_sig_name` carries real Phase 37-era behavioural
      changes in the same range which ARE gate-covered — both facts on the record.
    - The closing verdict table has one row per invariant with PROVEN / NOT PROVEN / PARTIAL and, for
      anything other than PROVEN, what is missing.
    - An executed-versus-skipped summary line exists for every command in the file.
    - `git status --porcelain -- typsphinx tests scripts` is empty; no census script was committed.
  </acceptance_criteria>
  <done>Every single-hit coverage claim is verified against real assertions, 40.1's RED manifest is folded in with existence and SHA confirmations, D-12's change is classified as unobligated with its own proof, and the SC#4 verdict states per invariant what was proven.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Newly created symbols, files, and CI step names across ALL of Phase 41 (listed so the
plan-review-convergence source-grounding pass excludes them from drift verification):

**Files created**
- `scripts/extract_changelog_section.py`
- `tests/test_changelog_extraction.py`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPH-BAR.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-JA-GLYPHBAR-SIGNOFF.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-GREEN-TREE-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md` (this plan)
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/41-HANDOFF.md`
- `.planning/phases/41-v0-7-0-release-automation-release-prep/translations-repo/` (working clone,
  D-17 — NEVER committed, created and removed inside plan 41-04)

**Functions / constants created elsewhere in the phase**
- `scripts/extract_changelog_section.py`: `extract_section(changelog_text: str, version: str) -> str`,
  `REPO_ROOT`, `DEFAULT_CHANGELOG_PATH`, `_SECTION_HEADER_RE`; CLI positional `version`, optional
  `--changelog-path`
- `tests/test_changelog_extraction.py`: `test_extracts_real_version`,
  `test_section_terminates_at_next_version_heading`, `test_absent_version_fails`,
  `test_empty_section_fails`, `test_unreleased_headings_do_not_leak`, `test_changelog_path_override`,
  plus `_run_extractor` and `SCRIPT_PATH`
- `.github/workflows/release.yml`, `validate` job step: `Verify CHANGELOG has a section for this
  version`
- `CHANGELOG.md`: the `## [0.7.0]` heading and the `[0.7.0]:` link-reference line

**Transient working artifacts created by this plan (never committed)**
- A handler-census script under `/tmp`, deliberately not added to the repository (Phase 29 D-15 /
  Phase 30.1 D-03 precedent)

**Explicitly NOT created by this plan**
- No committed sweep tooling, no new test, and no change to any source, test, workflow, or CHANGELOG
  file. This plan writes exactly one markdown evidence file.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> invariant claims | Repository history becomes a milestone-invariant assertion |
| test-file text search -> a coverage claim | A textual match becomes "this handler is fixtured" |
| `40.1-NONREGRESSION.md` § 4 -> this sweep's 40.1 coverage | A prior phase's self-report is consumed as evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-23 | Repudiation | A node-name text search producing a FALSE-POSITIVE coverage claim — a module that mentions the node only in prose | medium | mitigate | Every single-hit row is spot-checked against that module's actual `assert` statements and quoted, with a COVERED / NOT COVERED verdict per handler; the coverage table itself states in words that a hit is necessary but not sufficient. This is `41-RESEARCH.md` Open Question 2 answered by measurement rather than left on paper. Reinforced by this plan's second `must_haves.prohibitions` entry. |
| T-41-24 | Spoofing | A stale SHA range or commit count making the sweep describe a tree that no longer exists | medium | mitigate | The base is re-derived from `git merge-base main HEAD` at execution time and the commit count is measured in-task; the plan names the three superseded values (328 / 369 / 371) precisely so none of them can be transcribed as current. |
| T-41-25 | Tampering | Consuming `40.1-NONREGRESSION.md` § 4 as evidence without confirming its referents exist | medium | mitigate | Each row's named evidence file is confirmed present at its recorded path and each recorded RED commit SHA is confirmed resolvable via `git cat-file -e`, so the fold-in rests on verified referents rather than on a prior phase's word. |
| T-41-26 | Repudiation | Silently treating this phase's own `visit_desc_sig_name` edit as either an unfixtured handler change or as covered without saying why | low | mitigate | A dedicated subsection classifies it as docstring-only with no GATE-01 obligation and reproduces the no-executable-line-moved proof from its own measurement, while also recording that the same handler's Phase 37-era behavioural changes ARE gate-covered. |
| T-41-27 | Elevation of Privilege | An irreversible publish action taken during a sweep | medium | mitigate | This plan runs read-only git and file commands and writes one markdown file. Tag emptiness is asserted phase-wide in plan 41-07. |
| T-41-SC | Tampering | npm/pip/cargo installs — supply chain | low | mitigate | This plan installs no new package, and its whole first invariant IS the supply-chain check: the runtime `dependencies` array and dependency groups are shown byte-identical across the milestone, and any third-party movement in `uv.lock` is stated explicitly. `41-RESEARCH.md` § "Package Legitimacy Audit" records the audit as not applicable because the phase adds no package. |

**No `high`-severity threat was identified for this plan** (`security_asvs_level: 1`,
`security_block_on: high`). The register is complete, not empty: the plan adds no authentication,
session, access-control, cryptographic, or external-input surface. Its risks are integrity risks
about the sweep asserting more than it measured, and each is bound to a quoted assertion, a
re-derived SHA, or a resolvable commit.
</threat_model>

<verification>
- The range section carries `git merge-base main HEAD`, `git rev-parse HEAD`, and a freshly measured
  commit count, with Phase 40.1's presence in the range shown by SHA.
- Invariant 1 shows both sides' `dependencies` arrays verbatim with a stated verdict.
- Invariant 2 shows every `@preview` hit on both sides across all three sync sites, plus a
  classification of every newly added file carrying such an import.
- Invariant 3 carries the hunk-attributed handler census, the node-name coverage table, and a
  spot-check entry with quoted assertions for every single-hit handler.
- `40.1-NONREGRESSION.md` § 4's rows are folded in with file-existence and commit-resolution
  confirmations.
- The closing verdict table gives PROVEN / NOT PROVEN / PARTIAL per invariant, and an
  executed-versus-skipped line exists for every command.
- `git status --porcelain -- typsphinx tests scripts` is empty; no census tooling committed.
- `git diff --stat -- typsphinx/ tests/ scripts/ .github/ CHANGELOG.md pyproject.toml uv.lock` over
  this plan's commits is empty.
- `git tag -l v0.7.0` and `git ls-remote --tags origin v0.7.0` — both empty.

**Honest-verifier note.** If a single-hit handler cannot be confirmed covered by a real assertion,
record it as an open SC#4 gap. Abstaining and reporting the gap is correct; writing a fixture to
close it inside a release-prep phase is the scope enlargement D-11 kept out of Phase 41.
</verification>

<success_criteria>
1. The SHA range is re-measured from git, covers Phase 40.1 provably, and no planning-time count is
   presented as current.
2. Zero new runtime dependencies is shown by verbatim diff, with any lockfile movement stated.
3. The `@preview` surface is shown unchanged with every newly added importing file classified, and
   the pre-existing fourth site named as a carried Warning.
4. Every changed node handler is censused by hunk attribution and mapped by node name, with every
   single-hit mapping verified against real assertions.
5. Phase 40.1's RED manifest is folded in with verified referents, D-12's own change is classified as
   unobligated with its own proof, and the verdict states per invariant what was proven.
</success_criteria>

<output>
Create `.planning/phases/41-v0-7-0-release-automation-release-prep/41-06-SUMMARY.md` when done.
</output>