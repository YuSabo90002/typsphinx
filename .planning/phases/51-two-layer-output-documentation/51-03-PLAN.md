---
phase: 51-two-layer-output-documentation
plan: 03
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - docs/source/user_guide/output_layout.rst
  - tests/test_output_layout_docs_gate.py
  - tests/fixtures/output_layout_refused_parent_gate/conf.py
  - tests/fixtures/output_layout_refused_parent_gate/index.rst
  - tests/fixtures/output_layout_refused_absolute_gate/conf.py
  - tests/fixtures/output_layout_refused_absolute_gate/index.rst
  - tests/fixtures/output_layout_refused_drive_gate/conf.py
  - tests/fixtures/output_layout_refused_drive_gate/index.rst
autonomous: true
requirements: [DOC-14]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader learns that a target containing a `..` segment, an absolute target, and a drive-qualified target are each refused with a build warning and a safe fallback to the target's basename written at the output root, and that the build still succeeds (SC#2)."
    - "The page quotes the builder's own refusal warning verbatim inside a literal block — `a path is not supported in a typst_documents target name:` — rather than paraphrasing it, so a reader can match the text they actually see in their terminal (51-RESEARCH.md Part B)."
    - "A reader learns which target values stop the build outright: a target resolving onto a path already claimed by the reserved `_template.typ`, by a docname's own content file, or by another entry's wrapper, raises an ExtensionError before anything is written, so no .typ file is left on disk (D-05)."
    - "Each of the three refusal shapes is a real fixture built with `sphinx-build -b typst`, and the gate asserts BOTH the emitted .typ file set AND the verbatim warning text on stderr — the file sets are literal copies of 51-RESEARCH.md Part C builds 3a, 3b and 3c (SC#3)."
    - "The refused parent-traversal target writes nothing outside the build directory — `escape.typ` exists at the build root and does NOT exist in the build directory's parent (OUT-02's escape guard, observed rather than asserted from code)."
    - "The collision abort is proved by a real build of the existing `tests/fixtures/bld03_self_collision_gate/` project, asserting a non-zero exit and zero .typ files on disk; that fixture is reused, never modified or duplicated."
  artifacts:
    - path: docs/source/user_guide/output_layout.rst
      provides: "Refused-target subsection with the verbatim warning, and the collision section"
      contains: "a path is not supported in a typst_documents target name"
    - path: tests/test_output_layout_docs_gate.py
      provides: "Three refusal build assertions, one collision build assertion, and their published-text counterparts"
      contains: "test_refused_parent_target_falls_back_to_basename"
    - path: tests/fixtures/output_layout_refused_parent_gate/conf.py
      provides: "51-RESEARCH.md Part C build 3a's configuration, verbatim"
      contains: "typst_documents"
    - path: tests/fixtures/output_layout_refused_absolute_gate/conf.py
      provides: "51-RESEARCH.md Part C build 3b's configuration, verbatim"
      contains: "typst_documents"
    - path: tests/fixtures/output_layout_refused_drive_gate/conf.py
      provides: "51-RESEARCH.md Part C build 3c's configuration, verbatim"
      contains: "typst_documents"
  key_links:
    - from: "tests/test_output_layout_docs_gate.py"
      to: "tests/fixtures/bld03_self_collision_gate/"
      via: "_run_sphinx_build against the existing Phase 47 collision fixture, asserting returncode != 0 and an empty *.typ glob — reuse, never a fourth copy of the same configuration"
      pattern: "bld03_self_collision_gate"
    - from: "docs/source/user_guide/output_layout.rst"
      to: "typsphinx/builder.py"
      via: "the refusal warning and the collision ExtensionError are quoted verbatim from builder.py's own f-strings, so a paraphrase drift is visible as a gate failure"
      pattern: "a path is not supported"
---

<objective>
Publish the two ways a `typst_documents` target can fail to be honoured — refused-and-fallen-back, and
refused-outright — and prove both against real builds.

Purpose: SC#2's refusal half ("a target containing `..`, absolute, or drive-qualified is refused with
a warning and a safe fallback") and D-05 (the Phase 47 collision hard-error documented as a contract on
the page). Each is carried by SC#3's machine-checked half: three new fixtures for the refusal shapes,
and the existing Phase 47 collision fixture reused for the abort.
Output: three fixture projects, two new page sections, and six new gate methods.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/51-two-layer-output-documentation/51-CONTEXT.md
@.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md
@.planning/phases/51-two-layer-output-documentation/51-PATTERNS.md
@.planning/phases/51-two-layer-output-documentation/51-01-SUMMARY.md
@CLAUDE.md
</context>

<worktree_protocol>
This project runs executors in isolated git worktrees as its STANDING execution mode (`CLAUDE.md`,
owner decision 2026-07-20). Before ANY command in this plan:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then run EVERY subsequent command through `uv run`. Sphinx subprocess builds inside test modules use
`sys.executable -m sphinx`, never a console-script shim.

`typst.compile()` does NOT work in this sandbox. Every assertion in this plan is at the `.typ` level.
</worktree_protocol>

<phase_scope_fence>
This phase changes **ZERO** lines under `typsphinx/`. `tests/fixtures/bld03_self_collision_gate/`,
`tests/test_typst_documents_collision_gate.py` and `tests/test_builder_output_stem.py` are read and
reused, never modified — they are the code-side proof the new prose must agree with, and they are
already green.
</phase_scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: The three refusal shapes — fixture, build, warning text, page</name>
  <precondition>`tests/test_output_layout_docs_gate.py` exists from plan 51-01 and `uv run python -m pytest tests/test_output_layout_docs_gate.py -q` reports 4 passed, 0 skipped before any edit. Halt and report if it does not.</precondition>
  <files>tests/fixtures/output_layout_refused_parent_gate/conf.py, tests/fixtures/output_layout_refused_parent_gate/index.rst, tests/fixtures/output_layout_refused_absolute_gate/conf.py, tests/fixtures/output_layout_refused_absolute_gate/index.rst, tests/fixtures/output_layout_refused_drive_gate/conf.py, tests/fixtures/output_layout_refused_drive_gate/index.rst, tests/test_output_layout_docs_gate.py, docs/source/user_guide/output_layout.rst</files>
  <read_first>
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part C builds 3a / 3b / 3c (the three measured configurations, their measured stderr warnings, and their measured `.typ` file sets) and Part B §`_resolve_target_stem` / §`_is_drive_qualified` / §`_escapes_outdir` (the decision tree and the two verbatim warning templates)
    - `typsphinx/builder.py` lines 36-114 (`_is_drive_qualified`, `_escapes_outdir`) and lines 370-392 (the two warning f-strings, verbatim)
    - `tests/test_output_layout_docs_gate.py` (as written by plan 51-01 — the constants, the two class names, and `_run_sphinx_build()`)
    - `tests/fixtures/output_layout_bare_target_gate/conf.py` (the fixture header-comment convention to mirror)
    - `docs/source/user_guide/output_layout.rst` (as written by plan 51-01 — the `Where the Wrapper Is Written` section this task extends)
    - `.planning/phases/51-two-layer-output-documentation/51-PATTERNS.md` §"Shared Patterns" (heading adornments; the plain-prose register D-08 requires; the no-admonition rule)
  </read_first>
  <action>
Create three fixture directories, each a literal copy of one measured configuration, each with the
fixture header-comment convention (a comment stating its ONE job — it is the built proof for one
refusal shape on `docs/source/user_guide/output_layout.rst` — plus a do-not-merge note naming the other
two `output_layout_refused_*` fixtures, since all three look nearly identical and merging them would
collide their fallback wrappers).

- `tests/fixtures/output_layout_refused_parent_gate/` — target `"../escape"`.
- `tests/fixtures/output_layout_refused_absolute_gate/` — target `"/abs/manual"`.
- `tests/fixtures/output_layout_refused_drive_gate/` — target `"C:manual"`.

Each `conf.py` carries `project`, `author`, `release`, `copyright`, `extensions = ["typsphinx"]` and a
single-entry `typst_documents` whose docname is `index`. Each `index.rst` is minimal and toctree-free.
State in each header comment WHY the three cannot live in one fixture: the absolute and
drive-qualified shapes both fall back to the same basename, so a single project holding both would trip
the collision abort instead of exercising the fallback.

**Gate.** Add `REFUSED_PARENT_FIXTURE_DIR`, `REFUSED_ABSOLUTE_FIXTURE_DIR` and
`REFUSED_DRIVE_FIXTURE_DIR` to the module constants, and three methods to
`TestOutputLayoutBuildFileSets`: `test_refused_parent_target_falls_back_to_basename`,
`test_refused_absolute_target_falls_back_to_basename` and
`test_refused_drive_qualified_target_falls_back_to_basename`. Each asserts, against the real build:
the process returns 0 (a refused target is a warning, not an error); the fallback wrapper exists at the
build root under the target's basename (`escape.typ`, `manual.typ`, `manual.typ` respectively);
`index.typ` and `_template.typ` also exist; and the process stderr contains the verbatim warning line
for that target, quoted exactly as `51-RESEARCH.md` Part C measured it. The parent-traversal method
additionally asserts that no `escape.typ` exists in the build directory's PARENT — the observable form
of the OUT-02 escape guard, checked against the filesystem rather than re-derived from the builder's
logic. Build each fixture into its own subdirectory under pytest's `tmp_path` so the parent-directory
assertion has a meaningful parent.

Add `test_page_quotes_the_verbatim_refusal_warning` to
`TestPublishedOutputLayoutTextMatchesBuild`, asserting the published page contains the invariant
fragment of the warning template exactly as the builder emits it, and contains each of the three target
values as written in the fixtures.

**Page.** Extend the `Where the Wrapper Is Written` section with a third `~`-level subsection,
`Targets that are refused`. State that three target shapes are refused: a target with a `..` path
segment, an absolute target, and a drive-qualified target such as a Windows drive prefix — and that the
drive-qualified check is a pure string-shape test, so a Windows-shaped target is refused identically on
Linux and macOS. State the outcome: the build emits a warning, falls back to the target's basename, and
writes the wrapper at the output root exactly as a bare target would; the build still succeeds. Show
the warning inside a literal block (a `.. code-block:: text`) so its double-hyphen renders as the
builder actually prints it, using one of the three measured lines verbatim, and name the other two
target values in the surrounding prose. Keep the config-example-then-consequence shape; add no
admonition anywhere.
  </action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_output_layout_docs_gate.py -q` exits 0 and reports 8 passed, 0 skipped.
    - `uv run python -m pytest tests/test_output_layout_docs_gate.py -q -rs 2>&1 | grep -c SKIPPED` returns 0.
    - `grep -c 'a path is not supported in a typst_documents target name' docs/source/user_guide/output_layout.rst` returns at least 1.
    - `grep -c 'code-block:: text' docs/source/user_guide/output_layout.rst` returns at least 1 — the warning is in a literal block, so its double-hyphen is preserved.
    - `grep -c 'Targets that are refused' docs/source/user_guide/output_layout.rst` returns 1.
    - `grep -c 'escape' docs/source/user_guide/output_layout.rst` returns at least 1, and the page also contains `C:manual` and `/abs/manual`.
    - `grep -c '.. note::' docs/source/user_guide/output_layout.rst` returns 0 and `grep -c '.. warning::' docs/source/user_guide/output_layout.rst` returns 0.
    - Each of the three new `conf.py` files contains exactly one `typst_documents` assignment: `grep -c 'typst_documents' tests/fixtures/output_layout_refused_parent_gate/conf.py` returns 1 (and the same for the other two fixtures).
    - `grep -c '_resolve_target_stem\|_escapes_outdir\|_is_drive_qualified' tests/test_output_layout_docs_gate.py` returns 0 — the gate observes the real build, never a reimplementation.
    - `git diff --name-only HEAD -- typsphinx/ | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_output_layout_docs_gate.py -q</automated>
  </verify>
  <done>All three refusal shapes are built for real, their emitted file sets and verbatim warnings are asserted, the parent-traversal case is proved to write nothing outside the build directory, and the page publishes exactly that behaviour with the warning quoted rather than paraphrased.</done>
</task>

<task type="auto">
  <name>Task 2: The collision abort — reuse the Phase 47 fixture, publish the contract</name>
  <files>tests/test_output_layout_docs_gate.py, docs/source/user_guide/output_layout.rst</files>
  <read_first>
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part B §`_validate_output_path_collisions` (the three claimant kinds, the run-before-anything-is-written ordering, and the verbatim `ExtensionError` message shape)
    - `typsphinx/builder.py` lines 502-613 (the collision map and the aggregated `ExtensionError` — read only, never edit)
    - `tests/fixtures/bld03_self_collision_gate/conf.py` (the existing canonical self-collision configuration and its load-bearing-properties header — reuse, do not modify)
    - `tests/test_typst_documents_collision_gate.py` (the existing code-side proof the new prose must agree with — read only, do not extend)
    - `.planning/phases/51-two-layer-output-documentation/51-CONTEXT.md` D-05 (why the collision rules belong on this page even though SC#1/SC#2 do not name them)
    - `docs/source/user_guide/output_layout.rst` (as extended by Task 1)
  </read_first>
  <action>
**Gate.** Add `SELF_COLLISION_FIXTURE_DIR` pointing at the EXISTING
`tests/fixtures/bld03_self_collision_gate/` — do not create a fourth copy of the same configuration and
do not edit that fixture's `conf.py` or its load-bearing-properties header. Add
`test_self_collision_target_aborts_the_build_with_no_typ_files` to `TestOutputLayoutBuildFileSets`: the
build returns a non-zero exit code; the combined stdout+stderr contains the literal
`output path collision(s)`; and a recursive glob for `*.typ` under the build directory yields zero
files, proving the check runs before anything is written. Add
`test_page_states_the_collision_abort` to `TestPublishedOutputLayoutTextMatchesBuild`, asserting the
page contains the same `output path collision(s)` fragment.

**Page.** Add a new `-`-level section `Targets That Stop the Build`, placed after
`Where the Wrapper Is Written`. State that a target is refused outright — the build raises an error
rather than falling back — when the path it resolves to is already claimed. Name the three claimant
kinds in the reader's own terms: the reserved `_template.typ` file typsphinx writes for its own use,
every document's content file (named after its docname, and present for every document whether or not
it appears in `typst_documents`), and every other entry's wrapper. Show the canonical case with a
`.. code-block:: python` carrying the self-collision fixture's own configuration, and state in prose
that this exact configuration built successfully in v0.7.x and now stops the build, because the
entry's target resolves onto the `index` document's own content file. Quote the error's opening in a
`.. code-block:: text` literal block. State that the check runs before any file is written, so a failed
build leaves no `.typ` files behind, and give the fix in one sentence: choose a target that is not any
document's own name. No admonition.
  </action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_output_layout_docs_gate.py -q` exits 0 and reports 10 passed, 0 skipped.
    - `grep -c 'output path collision(s)' docs/source/user_guide/output_layout.rst` returns at least 1.
    - `grep -c 'Targets That Stop the Build' docs/source/user_guide/output_layout.rst` returns 1.
    - `grep -c '_template.typ' docs/source/user_guide/output_layout.rst` returns at least 1 — the reserved claimant is named.
    - `grep -c 'bld03_self_collision_gate' tests/test_output_layout_docs_gate.py` returns at least 1.
    - `git diff --name-only HEAD -- tests/fixtures/bld03_self_collision_gate/ tests/test_typst_documents_collision_gate.py tests/test_builder_output_stem.py | wc -l` returns 0 — the reused fixture and the existing code-side gates are byte-unchanged.
    - `grep -c '.. note::' docs/source/user_guide/output_layout.rst` returns 0 and `grep -c '.. warning::' docs/source/user_guide/output_layout.rst` returns 0.
    - `git diff --name-only HEAD -- typsphinx/ | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_output_layout_docs_gate.py tests/test_typst_documents_collision_gate.py -q</automated>
  </verify>
  <done>A real build of the existing self-collision fixture aborts with a non-zero exit and leaves zero `.typ` files, the page publishes exactly that contract with the error quoted verbatim and the three claimant kinds named, and no existing fixture or gate was modified.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol below is NET-NEW in Phase 51. Nothing here exists at HEAD, so drift verification must not
expect a prior definition.

**Documentation**
- `docs/source/user_guide/output_layout.rst` (new page, title `Output Layout`) — created by plan 51-01,
  extended here with the sections `Targets that are refused` and `Targets That Stop the Build`
- New subsection `Migrating from 0.7.x to 0.8.0` in `docs/source/changelog.rst` — plan 51-02

**Tests**
- `tests/test_output_layout_docs_gate.py` — classes `TestOutputLayoutBuildFileSets` and
  `TestPublishedOutputLayoutTextMatchesBuild`; helper `_run_sphinx_build`; methods added here:
  `test_refused_parent_target_falls_back_to_basename`,
  `test_refused_absolute_target_falls_back_to_basename`,
  `test_refused_drive_qualified_target_falls_back_to_basename`,
  `test_self_collision_target_aborts_the_build_with_no_typ_files`,
  `test_page_quotes_the_verbatim_refusal_warning`, `test_page_states_the_collision_abort`;
  constants added here: `REFUSED_PARENT_FIXTURE_DIR`, `REFUSED_ABSOLUTE_FIXTURE_DIR`,
  `REFUSED_DRIVE_FIXTURE_DIR`, `SELF_COLLISION_FIXTURE_DIR`

**Test fixtures (new directories, each `conf.py` + `index.rst`)**
- `tests/fixtures/output_layout_bare_target_gate/` (plan 51-01)
- `tests/fixtures/output_layout_explicit_path_gate/` (plan 51-01)
- `tests/fixtures/output_layout_refused_parent_gate/` (this plan)
- `tests/fixtures/output_layout_refused_absolute_gate/` (this plan)
- `tests/fixtures/output_layout_refused_drive_gate/` (this plan)

**Reused, NOT created** — `tests/fixtures/bld03_self_collision_gate/` and
`tests/fixtures/state_guard_three_master_gate/` already exist and must not be modified.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; blocking threshold `high`. This phase changes zero lines under `typsphinx/`. The register
is short because the honest surface is small — it is not padded.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest process → `sphinx-build` subprocess | The gate spawns `sys.executable -m sphinx` on fixed, repo-controlled fixture paths and pytest's own `tmp_path`. No user-controlled input crosses it. |
| `typst_documents` target value → output filesystem path | This is the boundary OUT-02's escape guard defends in shipped code. This plan does not change the guard; it publishes and observes it. |
| repository → published documentation | Prose read by users; a claim that understates the guard would mislead. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Tampering | `_run_sphinx_build()` in `tests/test_output_layout_docs_gate.py` | low | accept | `subprocess.run` with a list argument, never `shell=True`; every path is a repo constant or pytest's `tmp_path`. Same pattern as every existing gate module. |
| T-51-03 | Tampering | the refused-target documentation and its fixture builds | medium | mitigate | The page must not read as "path components are now unrestricted". Task 1 publishes all three still-refused shapes with the verbatim warning, and the parent-traversal gate method asserts against the filesystem that nothing was written outside the build directory — the observable form of `_escapes_outdir()`. A drift toward understating the guard fails the gate, not merely review. |
| T-51-05 | Denial of Service | the collision fixture build | low | accept | The collision build is expected to exit non-zero; the gate asserts that outcome rather than treating it as an infrastructure failure. Build runs are second-scale (`51-RESEARCH.md` §Validation Architecture) and confined to `tmp_path`. |
| T-51-SC | Tampering | package-manager installs | — | accept | Not applicable: this plan installs no npm/pip/cargo package and adds no dependency. No Package Legitimacy Audit is owed. |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_output_layout_docs_gate.py -q` — 10 passed, 0 skipped.
- `uv run python -m pytest tests/test_typst_documents_collision_gate.py tests/test_builder_output_stem.py -q` — still green, unmodified.
- `git diff --name-only HEAD -- typsphinx/ tests/fixtures/bld03_self_collision_gate/` — empty.
</verification>

<success_criteria>
- Three refusal shapes are published with the verbatim warning and each is proved by a real build.
- The parent-traversal case is proved to write nothing outside the build directory.
- The collision abort is published with the verbatim error and proved by a real build of the existing
  Phase 47 fixture, with zero `.typ` files left on disk.
- No existing fixture or gate module was modified; no line under `typsphinx/` changed.
</success_criteria>

<output>
Create `.planning/phases/51-two-layer-output-documentation/51-03-SUMMARY.md` when done.
</output>
