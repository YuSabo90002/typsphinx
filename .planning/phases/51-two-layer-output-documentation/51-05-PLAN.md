---
phase: 51-two-layer-output-documentation
plan: 05
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - README.md
  - examples/basic/README.md
  - examples/advanced/README.md
autonomous: true
requirements: [DOC-14]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README.md's typst_documents description no longer claims each entry produces one emitted .typ file; it states that an entry produces a wrapper at its target plus a content file for its source document, and that every document in the project gets a content file (51-RESEARCH.md Part A row 9)."
    - "README.md's Configuration Options bullet for typst_documents says the target names the wrapper, keeping it consistent with the corrected Quick Start prose (Part A row 10)."
    - "README.md links to the new page and adds no two-layer explanation and no worked examples of its own (D-03)."
    - "examples/basic/README.md lists every .typ file its own build actually emits — basic-example.typ AND index.typ — and says which one to compile (Part A row 11)."
    - "examples/advanced/README.md's generated-file list includes index.typ, and its prose attributes the chapter includes to the content file rather than to the master target (Part A row 12)."
    - "examples/advanced/README.md's Typst code block shows the real state-guarded emission — a per-child compile-time guard reading the include-edge state, wrapped in a relative heading-offset context — not an unconditional include under an absolute offset (Part A row 13)."
    - "Every emitted file list published by this plan is transcribed from a real `sphinx-build -b typst` run of that example performed during execution, never predicted."
  artifacts:
    - path: README.md
      provides: "Corrected typst_documents claims and a link to the new page"
      contains: "output_layout"
    - path: examples/basic/README.md
      provides: "Corrected emitted-file list for the basic example"
      contains: "index.typ"
    - path: examples/advanced/README.md
      provides: "Corrected emitted-file list and the real state-guarded include emission"
      contains: "typsphinx:include-edges"
  key_links:
    - from: "README.md"
      to: "docs/source/user_guide/output_layout.rst"
      via: "a repo-relative markdown link next to the corrected Quick Start claim (the README.md:226 precedent) plus an RTD-absolute entry in the documentation links list (the README.md:298-304 precedent)"
      pattern: "output_layout"
    - from: "examples/advanced/README.md"
      to: "typsphinx/translator.py"
      via: "the published Typst code block must match render_include_guard()'s real output shape, so a drift in the emission is visible as a wrong example"
      pattern: "typsphinx:include-edges"
---

<objective>
Finish D-04's repo-wide sweep outside `docs/source/`: the PyPI front page and the two bundled example
walkthroughs.

Purpose: `51-RESEARCH.md` Part A rows 9, 10 (README.md) and 11, 12, 13 (`examples/**/README.md`), plus
D-03's scope fence for the README — false-claim correction and a link, nothing more. This is the share
of the sweep that this project's own recorded failure mode most often loses: an "anywhere under
`docs/source`" reading that never reaches `examples/`.
Output: three corrected markdown files, with every published file list transcribed from a real build.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/51-two-layer-output-documentation/51-CONTEXT.md
@.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md
@.planning/phases/51-two-layer-output-documentation/51-PATTERNS.md
@CLAUDE.md
</context>

<worktree_protocol>
This project runs executors in isolated git worktrees as its STANDING execution mode (`CLAUDE.md`,
owner decision 2026-07-20). Before ANY command in this plan:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then run EVERY subsequent command through `uv run`. The example builds this plan performs use
`uv run python -m sphinx -b typst <example-dir> <tmp-dir>` — never a resolved `sphinx-build` binary.
Build into a directory outside the repository working tree (or clean it up) so no `_build/` artifact
is committed.

`typst.compile()` does NOT work in this sandbox; every build in this plan is `-b typst` only.
</worktree_protocol>

<phase_scope_fence>
This phase changes **ZERO** lines under `typsphinx/`.

D-04's Claude's-Discretion clause allows this plan to touch the examples' own `conf.py` files if a
documented claim would otherwise stay false. **Decision: do not touch them.** Both
`examples/basic/conf.py` and `examples/advanced/conf.py` already use targets
(`basic-example.typ`, `advanced-example.typ`) that neither self-collide nor need changing under the
two-layer split; only their READMEs are wrong. Record this decision in the SUMMARY.
</phase_scope_fence>

<sweep_rows_owned_by_this_plan>
`51-RESEARCH.md` Part A is the closed, re-derived falsified-claim list for this phase. This plan owns
rows 9-13. Every row is FIXED; none is deferred.

| Row | Site | Verdict | Task |
|-----|------|---------|------|
| 9 | `README.md:82-85` — one-emitted-file-per-entry claim | FALSE | 1 |
| 10 | `README.md:228` — Configuration Options `typst_documents` bullet | STALE (companion to row 9) | 1 |
| 11 | `examples/basic/README.md:36` — emitted-file claim | FALSE by omission | 2 |
| 12 | `examples/advanced/README.md:59-65` — generated-file list and master-include claim | FALSE | 2 |
| 13 | `examples/advanced/README.md:113-125` — the Typst include code block | FALSE, twice over | 2 |

Sites the researcher checked and found STILL TRUE, deliberately NOT changed by this plan (record this
in the SUMMARY so the sweep is auditable rather than silently narrowed):

- `README.md:100-103` — the toctree-child paragraph. True in outcome; D-03 scopes the README to
  false-claim correction only, so no rewrite is mandated.
- `examples/basic/README.md:57` — `typst compile _build/typst/basic-example.typ output.pdf`.
  `basic-example.typ` IS the wrapper, so this is the correct file to compile.
- `examples/charged-ieee/README.md:107,116` — `typst compile paper.typ output.pdf`. Both
  `charged-ieee` configs target `paper`, so `paper.typ` is the wrapper. Correct as written.
</sweep_rows_owned_by_this_plan>

<tasks>

<task type="auto">
  <name>Task 1: README.md — correct the two typst_documents claims and link out (D-03)</name>
  <files>README.md</files>
  <read_first>
    - `README.md` lines 78-115 (the `#### typst_documents` block inside Quick Start, and the Build Typst Output section) and lines 222-234 (the Configuration Options list) and lines 294-310 (the documentation links list — the RTD-absolute link convention to copy)
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part A rows 9 and 10 (verbatim current text and the falsifying citation) and Part B §`_content_output_path` (why "one emitted file per entry" is false)
    - `.planning/phases/51-two-layer-output-documentation/51-CONTEXT.md` D-03 (the scope fence: corrections and a link, no explanation, no worked examples)
    - `.planning/phases/51-two-layer-output-documentation/51-PATTERNS.md` §"README.md (D-03 correction + link)" (both precedented link forms and where each belongs)
  </read_first>
  <action>
Two corrections and two links in `README.md`. Add no worked example and no two-layer explanation — D-03
fences this file to correction plus a pointer, because the README is the PyPI front page and a second
copy of the contract would be a second surface to keep in sync.

**Row 9 — the Quick Start `typst_documents` paragraph.** The sentence asserting a one-file-per-entry
outcome is false. Rewrite it so it states what a build actually writes: each entry produces a wrapper
`.typ` file at the entry's target and, under the `typstpdf` builder, one compiled `.pdf` from that
wrapper; the entry's source document is additionally emitted as its own `.typ` file holding the
document body, as is every other document in the project. Keep the sentence count close to what is
there now. Immediately after it, add one short sentence pointing at the new page for the full contract
and for which file to compile, using the repo-relative markdown link form this file already uses in
prose at line 226, targeting `docs/source/user_guide/output_layout.rst`.

Leave the rest of that block alone — the unset-derivation paragraph, the explicit-value-wins paragraph
and the toctree-child paragraph are all still true and are outside this correction.

**Row 10 — the Configuration Options bullet.** Extend the `typst_documents` bullet so it stays
consistent with the corrected Quick Start prose: say that the target names the entry's wrapper file.
Do not restate the derivation rules already in the bullet.

**Link.** Add one entry to the documentation links list, following that list's existing
RTD-absolute-URL convention and placed adjacent to the `Configuration Reference` entry:
an `Output Layout` label pointing at `https://typsphinx.readthedocs.io/en/latest/user_guide/output_layout.html`.
Note in the SUMMARY that this URL resolves only after the docs rebuild that follows a merge to `main`;
the repository's lychee link check is advisory, so this is a recorded consequence rather than a defect.
  </action>
  <acceptance_criteria>
    - `grep -c 'produces one emitted' README.md` returns 0.
    - `grep -c 'output_layout' README.md` returns 2 — the inline repo-relative link and the RTD-absolute list entry.
    - `grep -c 'docs/source/user_guide/output_layout.rst' README.md` returns at least 1.
    - `grep -c 'https://typsphinx.readthedocs.io/en/latest/user_guide/output_layout.html' README.md` returns 1.
    - `grep -c 'wrapper' README.md` returns at least 2 — the corrected Quick Start sentence and the Configuration Options bullet.
    - `grep -c 'A document reached only through a toctree is not a separate' README.md` returns 1 — the still-true toctree paragraph is untouched.
    - `awk '/^#### .typst_documents.$/,/^### Build Typst Output$/' README.md | grep -c 'code-block\|```'` returns 0 — D-03's no-worked-examples fence held.
    - `uv run python -m pytest tests/test_quickstart_docs_gate.py -q` exits 0 — the existing README assertions still hold.
    - `git diff --name-only HEAD -- typsphinx/ | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_quickstart_docs_gate.py -q && ! grep -q 'produces one emitted' README.md && grep -q 'output_layout' README.md</automated>
  </verify>
  <done>README.md's two `typst_documents` claims are true, it links to the new page twice in the file's own two precedented link forms, and it gained no explanation or example of its own.</done>
</task>

<task type="auto">
  <name>Task 2: The bundled example walkthroughs — build them, then publish what they actually emit</name>
  <files>examples/basic/README.md, examples/advanced/README.md</files>
  <read_first>
    - `examples/basic/README.md` (full file) and `examples/basic/conf.py` lines 28-36 (its `typst_documents` target)
    - `examples/advanced/README.md` (full file) and `examples/advanced/conf.py` lines 34-44 (its `typst_documents` target)
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part A rows 11, 12, 13 (verbatim current text, the measured actual file sets, and the two independent defects in row 13) and §"Code Examples" (the verbatim shape of a content file's state-guarded toctree emission and of the wrapper's own body)
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Pitfall 1 (why "the docs mention an include" is not enough — the wrapper does still contain exactly one include, of its own content file)
    - `typsphinx/translator.py` lines 190-195 and 330-378 (read only — `INCLUDE_STATE_KEY` and `render_include_guard()`, the source of the guard's exact shape) and lines 5295-5345 (the toctree emission site and its relative heading-offset context)
  </read_first>
  <action>
Before editing either file, BUILD both examples and transcribe the real output. Run
`uv run python -m sphinx -b typst examples/basic <tmpdir>/basic` and
`uv run python -m sphinx -b typst examples/advanced <tmpdir>/advanced`, then list the emitted `.typ`
files with `find <tmpdir>/... -name '*.typ' | sort`. Paste both listings verbatim into the SUMMARY.
Every file list published below must be transcribed from those listings, never predicted. Build outside
the repository working tree so no `_build/` directory is committed.

**Row 11 — `examples/basic/README.md`.** The sentence claiming the build creates one file omits the
content file and implies the target file is the whole document. Replace it with a short list of every
`.typ` file the build actually writes, per your own transcript, saying which is the wrapper (the
target) and which is the content file (named after the source document), and stating that the wrapper
is the file to compile. Leave the `typst compile` block further down unchanged — it already names the
wrapper and is correct.

**Row 12 — `examples/advanced/README.md`, the generated-file list.** The bullet list is missing the
content file for the master's own document, and the sentence beneath it attributes the chapter includes
to the target file. Rewrite the list from your transcript, labelling each file (wrapper, master
content, chapter content). Rewrite the sentence to say what is actually true: the target file is a thin
wrapper that includes the master's content file, and the chapter includes live inside that content
file, emitted at the toctree's own position. Do not use the target filename as the subject of any
sentence about chapter inclusion — that attribution is exactly the defect being fixed.

**Row 13 — `examples/advanced/README.md`, the Typst code block.** The block shows an unconditional
include under an absolute heading-offset assignment. Both halves are wrong. Replace it with the real
emitted shape, taken from `51-RESEARCH.md` §"Code Examples": a single `context` block that sets the
heading offset RELATIVE to the enclosing offset, containing one guarded line per child that includes
the child only when that child's edge key is present in the include-edge state the wrapper published.
Use the real state key and the real edge-key spelling for this example's own two chapters. Keep the
surrounding prose's framing ("This generates Typst ... directives") but adjust it to say the emission
is a compile-time guard, and add one sentence explaining in the reader's language why the guard exists:
each master publishes its own set of include edges, so a document reachable from several masters
renders once in each master's output rather than being claimed by only one of them.

Verify the block you publish against your own build: `grep` the emitted `index.typ` in your transcript
directory for the guard lines and confirm the published block matches the real text.
  </action>
  <acceptance_criteria>
    - `grep -c 'with the Typst markup' examples/basic/README.md` returns 0.
    - `grep -c 'index.typ' examples/basic/README.md` returns at least 1, and `grep -c 'basic-example.typ' examples/basic/README.md` returns at least 2.
    - `grep -c 'typst compile _build/typst/basic-example.typ output.pdf' examples/basic/README.md` returns 1 — the still-correct command is untouched.
    - `grep -c 'directives to' examples/advanced/README.md` returns 0.
    - `grep -c 'set heading(offset: 1)' examples/advanced/README.md` returns 0.
    - `grep -c 'heading.offset + 1' examples/advanced/README.md` returns 1.
    - `grep -c 'typsphinx:include-edges' examples/advanced/README.md` returns at least 2 — one guarded line per chapter.
    - `awk '/^This will generate/,/^### Generate PDF Output/' examples/advanced/README.md | grep -c 'index.typ'` returns at least 1 — the missing content file is now listed.
    - The SUMMARY contains both verbatim `find ... -name '*.typ' | sort` listings, and every filename published in either README appears in the corresponding listing.
    - `git diff --name-only HEAD -- typsphinx/ examples/basic/conf.py examples/advanced/conf.py | wc -l` returns 0.
    - `git status --porcelain examples/ | grep -c '_build'` returns 0 — no build artifact was committed.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_examples_basic.py tests/test_integration_advanced.py -q && ! grep -qE 'with the Typst markup|directives to|set heading\(offset: 1\)' examples/basic/README.md examples/advanced/README.md</automated>
  </verify>
  <done>Both example walkthroughs list every `.typ` file their own build emits, name which one to compile, and the advanced example's include block shows the real state-guarded compile-time emission — each transcribed from a build run during this task.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol below is NET-NEW in Phase 51. Nothing here exists at HEAD, so drift verification must not
expect a prior definition. **This plan creates none of them** — it only links to
`docs/source/user_guide/output_layout.rst`.

**Documentation**
- `docs/source/user_guide/output_layout.rst` (new page, title `Output Layout`) — plans 51-01 / 51-03 / 51-06
- New subsection `Migrating from 0.7.x to 0.8.0` in `docs/source/changelog.rst` — plan 51-02

**Tests**
- `tests/test_output_layout_docs_gate.py` — classes `TestOutputLayoutBuildFileSets` and
  `TestPublishedOutputLayoutTextMatchesBuild`; helper `_run_sphinx_build`

**Test fixtures (new directories, each `conf.py` + `index.rst`)**
- `tests/fixtures/output_layout_bare_target_gate/`
- `tests/fixtures/output_layout_explicit_path_gate/`
- `tests/fixtures/output_layout_refused_parent_gate/`
- `tests/fixtures/output_layout_refused_absolute_gate/`
- `tests/fixtures/output_layout_refused_drive_gate/`

**Reused, NOT created** — `tests/fixtures/bld03_self_collision_gate/` and
`tests/fixtures/state_guard_three_master_gate/` already exist and must not be modified.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; blocking threshold `high`. This plan edits three markdown files and changes zero lines
under `typsphinx/`. The register is short because the honest surface is small — it is not padded.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → PyPI project page and GitHub landing page | `README.md` is rendered on PyPI. A false claim or a broken link crosses this boundary to every prospective user. |
| executor shell → `sphinx-build` subprocess over bundled example projects | The example builds run repo-controlled source trees through `uv run python -m sphinx` with a list of literal arguments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Information Disclosure | published example build listings | low | mitigate | Only build-relative filenames are published (`basic-example.typ`, `index.typ`, `chapter1.typ`, `chapter2.typ`). The transcript directory is outside the repository and its absolute path is not published; the SUMMARY records the file names, not the host path prefix. |
| T-51-06 | Spoofing | the new RTD link in `README.md` | low | accept | The URL follows the same `typsphinx.readthedocs.io/en/latest/...` origin as every existing documentation link in the file. It resolves after the docs rebuild that follows a merge to `main`; the repository's lychee check is advisory, and the consequence is recorded in the SUMMARY rather than hidden. |
| T-51-SC | Tampering | package-manager installs | — | accept | Not applicable: this plan installs no npm/pip/cargo package and adds no dependency. No Package Legitimacy Audit is owed. |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_quickstart_docs_gate.py tests/test_examples_basic.py tests/test_integration_advanced.py -q` — green.
- All five negative greps in the acceptance criteria return 0.
- The SUMMARY carries both verbatim `find` listings from the builds run during Task 2.
- `git diff --name-only HEAD -- typsphinx/ examples/basic/conf.py examples/advanced/conf.py` is empty.
</verification>

<success_criteria>
- `51-RESEARCH.md` Part A rows 9-13 are all fixed; none is deferred.
- Every published file list came from a build run during execution, transcribed into the SUMMARY.
- `README.md` gained corrections and links only — no explanation, no worked example.
- The three STILL-TRUE sites listed above are recorded as deliberately unchanged.
- No line under `typsphinx/` or in either example's `conf.py` changed.
</success_criteria>

<output>
Create `.planning/phases/51-two-layer-output-documentation/51-05-SUMMARY.md` when done.
</output>
