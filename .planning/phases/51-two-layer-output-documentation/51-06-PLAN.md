---
phase: 51-two-layer-output-documentation
plan: 06
type: execute
wave: 3
depends_on: ["51-02", "51-03", "51-04", "51-05"]
files_modified:
  - docs/source/user_guide/output_layout.rst
  - tests/test_output_layout_docs_gate.py
  - .planning/phases/51-two-layer-output-documentation/51-SWEEP-AUDIT.md
autonomous: true
requirements: [DOC-14]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader learns that a document reached from more than one master renders in EACH of those masters' outputs, exactly once, at that master's own traversal position, and that its heading level therefore differs between masters — stated in the reader's own language, not in include-graph vocabulary (D-09, SC#3)."
    - "The page states the file-count rule for a multi-master project: one wrapper per typst_documents entry, one content file per document, plus the reserved _template.typ — and a real -b typst build of the existing three-master fixture emits exactly that set (10 .typ files), asserted by the gate (SC#3)."
    - "The two claims that require a real typst.compile() — the standalone-content compile and the per-master marker/heading-level counts — are CITED to Phase 49's recorded real-compile transcripts and are never re-derived, because typst.compile() does not run in this sandbox."
    - "docs/source/user_guide/output_layout.rst carries a See Also section linking back to configuration, builders and the changelog migration guide, matching the sibling pages' list style."
    - "The one published filename in this phase that is DERIVED rather than typed by the reader — the `<project>`-derived wrapper stem in builders.rst's and templates.rst's walkthroughs — is asserted from `sphinx.util.osutil.make_filename_from_project`, the same helper typsphinx/builder.py calls, never from a hard-coded literal (D-11)."
    - "The full test suite is green and a real sphinx-build -b html of docs/source succeeds with every :doc: cross-reference added by this phase resolving."
    - "51-SWEEP-AUDIT.md records the disposition of all 13 51-RESEARCH.md Part A rows plus the four STILL-TRUE sites, with the plan that closed each, so no row was dropped without a recorded reason."
    - { statement: "The D-07 exclusion is measured with three scoped checks recorded in 51-SWEEP-AUDIT.md, not with the single repo-wide grep 51-VALIDATION.md proposed — that one-liner is unsatisfiable because CHANGELOG.md already carries two pre-existing historical occurrences unrelated to the excluded divergence.", verification: backstop }
  artifacts:
    - path: docs/source/user_guide/output_layout.rst
      provides: "The shared-child composition section and the page's See Also list — the page is complete after this plan"
      contains: "See Also"
    - path: tests/test_output_layout_docs_gate.py
      provides: "The three-master file-set assertion and its published-text counterpart"
      contains: "state_guard_three_master_gate"
    - path: .planning/phases/51-two-layer-output-documentation/51-SWEEP-AUDIT.md
      provides: "Closed-list disposition of every Part A sweep row, the D-07 absence measurement, and the cited-not-re-derived evidence register"
      contains: "Part A"
  key_links:
    - from: "tests/test_output_layout_docs_gate.py"
      to: "tests/fixtures/state_guard_three_master_gate/"
      via: "_run_sphinx_build over the existing Phase 49 fixture, asserting the exact 10-file .typ set measured in 51-RESEARCH.md Part C build 4 — reuse, never a new fixture"
      pattern: "state_guard_three_master_gate"
    - from: ".planning/phases/51-two-layer-output-documentation/51-SWEEP-AUDIT.md"
      to: ".planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md"
      via: "citation of the two real-compile transcripts this phase may not re-derive (standalone compile; per-master marker and heading-level counts)"
      pattern: "49-EVIDENCE"
  prohibitions:
    - statement: "MUST NOT narrow the repo-wide falsified-claim sweep by dropping a measured Part A row, or a measured STILL-TRUE site, without recording an explicit reason in 51-SWEEP-AUDIT.md. Silently closing an 'anywhere under docs/source' criterion against a convenient subset is this project's own recorded failure mode."
      category: transparency
---

<objective>
Complete the page with the milestone's headline behaviour change, then close the phase by measuring
what it claimed: the whole suite, a real documentation build, the D-07 exclusion, and the sweep's own
completeness.

Purpose: D-09 (shared-child composition documented as part of the two-layer specification), SC#3's
"any accepted composition consequence Phase 49 measured appears here in the user's language", and the
audit that keeps D-04's repo-wide sweep from being closed against a convenient subset.
Output: the finished page, two more gate methods, and `51-SWEEP-AUDIT.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-two-layer-output-documentation/51-CONTEXT.md
@.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md
@.planning/phases/51-two-layer-output-documentation/51-PATTERNS.md
@.planning/phases/51-two-layer-output-documentation/51-VALIDATION.md
@CLAUDE.md
</context>

<worktree_protocol>
This project runs executors in isolated git worktrees as its STANDING execution mode (`CLAUDE.md`,
owner decision 2026-07-20). Before ANY command in this plan:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`

Both extras are needed here: Task 2 runs a real `sphinx-build -b html` of `docs/source`, whose
`changelog.rst` uses `myst_parser` — a `docs`-extra dependency the base `dev` sync does not install.
Then run EVERY subsequent command through `uv run`.

`typst.compile()` does NOT work in this sandbox. Do NOT run `tox -e docs-pdf`, do NOT run any
`-b typstpdf` build, and do NOT attempt to re-measure either PDF-level claim. Both are cited from
Phase 49's own recorded transcripts (`51-RESEARCH.md` Pitfall 3).
</worktree_protocol>

<phase_scope_fence>
This phase changes **ZERO** lines under `typsphinx/`. `tests/fixtures/state_guard_three_master_gate/`
is read and reused, never modified — its `conf.py` header enumerates load-bearing properties that make
it the three-master coverage proof for Phase 49.
</phase_scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Documents shared by several masters — the composition contract and the page's See Also</name>
  <precondition>`uv run python -m pytest tests/test_output_layout_docs_gate.py -q` reports 10 passed, 0 skipped before any edit — plans 51-01 and 51-03 have both landed. Halt and report if it does not.</precondition>
  <files>docs/source/user_guide/output_layout.rst, tests/test_output_layout_docs_gate.py</files>
  <read_first>
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part C build 4 (the measured 10-file `.typ` set for the three-master fixture and its wrapper-report line) and §"State of the Art" row 3 (the old vs. current composition model)
    - `.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md` §"Degenerate-shape closure" (the recorded per-master marker count and resolved heading levels — read for the FACTS; do not re-derive them and do not attempt a compile)
    - `tests/fixtures/state_guard_three_master_gate/conf.py` (the three entries, the shared children, and the load-bearing-properties header — reuse, do not modify)
    - `tests/test_output_layout_docs_gate.py` (as left by plan 51-03 — constants and class names to extend)
    - `docs/source/user_guide/output_layout.rst` (as left by plan 51-03 — the sections this task follows)
    - `docs/source/user_guide/configuration.rst` around line 405 and `docs/source/user_guide/builders.rst` around line 184 (the two `See Also` list styles to match)
  </read_first>
  <action>
**Page.** Add a new `-`-level section `Documents Shared by Several Masters`, placed after
`Targets That Stop the Build`. Write it in the reader's own language, not in include-graph vocabulary.
It must state: a document reached from more than one master appears in EVERY one of those masters'
outputs; it appears exactly once in each; it appears at that master's own position in its own table of
contents; and because its position differs per master, its heading level differs per master too — the
same shared chapter can be a second-level heading in one master's output and a third-level heading in
another's. State that this is the intended behaviour and that no configuration is needed to get it.
Add the file-count rule this implies, since it is what a reader will actually observe: a build writes
one wrapper per `typst_documents` entry, one content file for every document in the project, and the
reserved `_template.typ` — so a three-master project over six documents writes ten `.typ` files.

Do not add a new `typst_documents` code block for this section; the rule above is stated in prose and
its built proof is the gate's three-master build. Adding an unbuilt configuration block here would
reintroduce exactly the failure SC#3 exists to close. No admonition.

Then add the page's `See Also` section as its last section, matching the sibling pages' dash-and-
description list style: `:doc:`configuration``, `:doc:`builders``, and `:doc:`/changelog`` (the
leading-slash cross-directory form) for the migration guide describing what changed from v0.7.x.

**Gate.** Add `THREE_MASTER_FIXTURE_DIR` pointing at the existing
`tests/fixtures/state_guard_three_master_gate/`, and add
`test_three_master_project_emits_ten_typ_files` to `TestOutputLayoutBuildFileSets`: build it with
`-b typst`, assert a zero exit code, and assert the sorted set of `.typ` files under the build
directory is exactly the ten names `51-RESEARCH.md` Part C build 4 measured — three wrappers, six
content files (one per docname), and `_template.typ`. Assert the exact SET, not merely that each file
exists, so an extra or missing file fails. Add `test_page_states_the_shared_child_composition` to
`TestPublishedOutputLayoutTextMatchesBuild`, asserting the page's text contains the section heading and
the literal `ten` file-count claim the section publishes.

**Close D-11's one hard-coded-value gap.** Plans 51-04 and 51-05 publish a wrapper filename that is
not read off an explicit target but DERIVED — the walkthroughs in `builders.rst` and `templates.rst`
assume `typst_documents` is unset and `project = "My Project"`, so their filename comes from
`sphinx.util.osutil.make_filename_from_project`, the same helper `typsphinx/builder.py` calls. Import
that helper at module level (the precedent does the same, `tests/test_quickstart_docs_gate.py`), add
`BUILDERS_RST_PATH` and `TEMPLATES_RST_PATH` constants, and add
`test_helper_derived_wrapper_stem_matches_the_published_walkthroughs` to
`TestPublishedOutputLayoutTextMatchesBuild`: compute the stem from the literal project string once, and
assert `builders.rst` contains the `build/typst/<stem>.typ` and `build/pdf/<stem>.pdf` paths and that
`templates.rst` contains the `build/typst/<stem>.typ` path — all built from the computed stem, never
from a hard-coded filename literal. This is the one place in the phase where a published filename comes
from a helper rather than from a target the reader typed, so it is the one place D-11's
derive-from-the-helper rule has anything to bind to.
  </action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_output_layout_docs_gate.py -q` exits 0 and reports 13 passed, 0 skipped.
    - `uv run python -m pytest tests/test_output_layout_docs_gate.py -q -rs 2>&1 | grep -c SKIPPED` returns 0.
    - `grep -c 'make_filename_from_project' tests/test_output_layout_docs_gate.py` returns at least 1, and `grep -c 'myproject' tests/test_output_layout_docs_gate.py` returns 0 — the derived stem is computed, never hard-coded.
    - `grep -c 'Documents Shared by Several Masters' docs/source/user_guide/output_layout.rst` returns 1.
    - `grep -c 'heading level' docs/source/user_guide/output_layout.rst` returns at least 1.
    - `grep -c 'See Also' docs/source/user_guide/output_layout.rst` returns 1, and the section contains `:doc:`configuration``, `:doc:`builders`` and `:doc:`/changelog``.
    - `grep -c 'state_guard_three_master_gate' tests/test_output_layout_docs_gate.py` returns at least 1.
    - `git diff --name-only HEAD -- tests/fixtures/state_guard_three_master_gate/ | wc -l` returns 0 — the reused fixture is byte-unchanged.
    - `grep -c '.. note::' docs/source/user_guide/output_layout.rst` returns 0 and `grep -c '.. warning::' docs/source/user_guide/output_layout.rst` returns 0.
    - `git diff --name-only HEAD -- typsphinx/ | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_output_layout_docs_gate.py -q</automated>
  </verify>
  <done>The page states the shared-child composition behaviour in user language with the file-count rule it implies, links back to its three sibling pages, and a real build of the existing three-master fixture proves the exact ten-file set.</done>
</task>

<task type="auto">
  <name>Task 2: Close the phase — full suite, real docs build, D-07 measurement, sweep audit</name>
  <reversibility rating="costly">D-07 excludes a measured divergence from every v0.8.0 published surface, reversing a plan recorded across three prior artifacts; reversing it again means re-deriving prose from the Phase 49 measurement, so the exclusion is recorded here as a measurement rather than left implicit.</reversibility>
  <files>.planning/phases/51-two-layer-output-documentation/51-SWEEP-AUDIT.md</files>
  <read_first>
    - `.planning/phases/51-two-layer-output-documentation/51-RESEARCH.md` Part A in full (all 13 rows plus the STILL-TRUE table — the closed list this audit reconciles against) and §"Environment Availability"
    - `.planning/phases/51-two-layer-output-documentation/51-VALIDATION.md` §"Manual-Only Verifications" (the four rows this audit discharges, one of which needs correcting — see the action)
    - `.planning/phases/51-two-layer-output-documentation/51-CONTEXT.md` D-04 and D-07
    - the four SUMMARY files produced by plans 51-02, 51-03, 51-04 and 51-05 in this phase directory
    - `.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md` §"Handoff to Phase 51 and Phase 52" and §"Degenerate-shape closure" (the two cited transcripts)
  </read_first>
  <action>
Run the closing measurements, then write `51-SWEEP-AUDIT.md`. Do not create or overwrite any file
named `51-VERIFICATION.md` — that name is reserved for the verify workflow and would be clobbered.

**Measurements to run and transcribe verbatim:**

1. `uv run python -m pytest -q -m "not slow"` — the full suite, including the new gate.
2. `uv run python -m sphinx -b html docs/source <tmpdir>/html` — a real documentation build. Record the
   exit status and the full warning list. Every cross-reference this phase added must resolve; a
   `undefined label` or `unknown document` warning naming the new page is a failure, not a note.
   Build outside the repository working tree.
3. The three scoped exclusion checks for D-07 (see below).
4. `git diff --name-only <phase-base>..HEAD -- typsphinx/` — must be empty.

**Correct the exclusion check before running it.** `51-VALIDATION.md` proposes a single repo-wide grep
over `docs/source/`, `README.md` and `CHANGELOG.md` returning empty. Measured at planning time, that
one-liner is unsatisfiable and would be wrong to satisfy: `CHANGELOG.md` already carries two
pre-existing historical occurrences of the role, at lines 68 and 246, in v0.7.x entries about a table
anchor — entirely unrelated to the Phase 49 divergence D-07 excludes. Satisfying the naive grep would
mean rewriting shipped release history. Replace it with three scoped checks, and record in the audit
that the one-liner was corrected and why:

  a. absence across every surface this phase writes into — `docs/source/`, `README.md` and
     `examples/` (all three were measured empty of the role at planning time and must stay empty);
  b. the repo-root `CHANGELOG.md` is untouched by this phase (`git diff` over that path is empty), so
     this phase adds no occurrence there and Phase 52's own SC#2 amendment governs the new `## [0.8.0]`
     entry;
  c. `CHANGELOG.md`'s own occurrence count is still exactly the pre-existing two, so the historical
     record was neither rewritten nor added to.

The exact three commands are given in this task's acceptance criteria; run them as written.

**Write `51-SWEEP-AUDIT.md`** with these sections:

- *Part A row disposition* — a table with one row per `51-RESEARCH.md` Part A row 1 through 13:
  path:line, verdict, disposition (all FIXED), the plan that closed it, and the commit or file evidence.
  Both rows the researcher flagged as borderline (row 4, the pre-existing CONF-08-era staleness in
  `builders.rst`; row 8, the historical migration-guide command in `changelog.rst`) carry their FIX
  disposition and its recorded reason. No row may be left blank.
- *STILL-TRUE sites deliberately unchanged* — the four sites the researcher verified survive
  (`docs/source/quickstart.rst`, `examples/basic/README.md`'s compile command,
  `examples/charged-ieee/README.md`, and `README.md`'s toctree-child paragraph), each with the reason.
- *Cited, not re-derived* — the two claims that need a real `typst.compile()`: the standalone-content
  compile and the per-master marker count with resolved heading levels. Name Phase 49's
  `49-EVIDENCE.md` sections as the evidence and state that this sandbox cannot re-run them.
- *D-07 exclusion measurement* — the corrected three checks, their commands and their output.
- *Closing measurements* — the pytest and `-b html` transcripts.
- *Deferred* — any code defect noticed during this phase, recorded and NOT fixed here per the phase
  scope fence, with enough detail to file a todo later.

Do not add a standing pytest module for the D-07 exclusion. D-07 forbids publishing, not testing; a
permanent test would need the concatenated-fragment idiom to avoid matching itself, and the decision it
guards is rated `costly` precisely because a later milestone is expected to reverse it. Record this
decision in the audit.
  </action>
  <acceptance_criteria>
    - `uv run python -m pytest -q -m "not slow"` exits 0.
    - `uv run python -m sphinx -b html docs/source "$(mktemp -d)/html"` exits 0, and its output contains no line matching `WARNING.*output_layout` and no line matching `undefined label|unknown document`.
    - `grep -rn ':numref:' docs/source/ README.md examples/ | wc -l` returns 0 — every surface this phase writes into is clean. <!-- planner-discipline-allow: :numref: -->
    - `git diff --name-only HEAD -- CHANGELOG.md | wc -l` returns 0 — this phase does not touch the repo-root changelog, so it adds no occurrence there; the two pre-existing v0.7.x occurrences at lines 68 and 246 are historical release history and are left alone.
    - `grep -c ':numref:' CHANGELOG.md` returns exactly 2 — the pre-existing historical count, unchanged by this phase. <!-- planner-discipline-allow: :numref: -->
    - `.planning/phases/51-two-layer-output-documentation/51-SWEEP-AUDIT.md` exists and its Part A table has exactly 13 data rows, every one with a non-empty disposition and a named plan.
    - `grep -c '49-EVIDENCE' .planning/phases/51-two-layer-output-documentation/51-SWEEP-AUDIT.md` returns at least 1.
    - `ls .planning/phases/51-two-layer-output-documentation/51-VERIFICATION.md 2>/dev/null | wc -l` returns 0 — the reserved name was not used.
    - `git diff --name-only HEAD -- typsphinx/ | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest -q -m "not slow" && test -f .planning/phases/51-two-layer-output-documentation/51-SWEEP-AUDIT.md</automated>
    <human-check>Skim `51-SWEEP-AUDIT.md`'s Part A table: every one of the 13 rows should name the plan that closed it, and rows 4 and 8 should carry their FIX reason rather than a bare "fixed".</human-check>
  </verify>
  <done>The full suite is green, a real `-b html` build of `docs/source` succeeds with every new cross-reference resolving, the D-07 exclusion is measured with three scoped checks rather than the unsatisfiable one-liner, and `51-SWEEP-AUDIT.md` disposes all 13 Part A rows plus the four still-true sites with no silent drops.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol below is NET-NEW in Phase 51. Nothing here exists at HEAD, so drift verification must not
expect a prior definition.

**Documentation**
- `docs/source/user_guide/output_layout.rst` (new page, title `Output Layout`) — created by plan 51-01,
  extended by 51-03, completed here with `Documents Shared by Several Masters` and `See Also`
- New subsection `Migrating from 0.7.x to 0.8.0` in `docs/source/changelog.rst` — plan 51-02

**Tests**
- `tests/test_output_layout_docs_gate.py` — classes `TestOutputLayoutBuildFileSets` and
  `TestPublishedOutputLayoutTextMatchesBuild`; helper `_run_sphinx_build`; the thirteen test methods
  listed across plans 51-01, 51-03 and this plan; constants `THREE_MASTER_FIXTURE_DIR`,
  `BUILDERS_RST_PATH` and `TEMPLATES_RST_PATH` added here; methods added here:
  `test_three_master_project_emits_ten_typ_files`, `test_page_states_the_shared_child_composition`,
  `test_helper_derived_wrapper_stem_matches_the_published_walkthroughs`

**Test fixtures (new directories, each `conf.py` + `index.rst`)**
- `tests/fixtures/output_layout_bare_target_gate/`
- `tests/fixtures/output_layout_explicit_path_gate/`
- `tests/fixtures/output_layout_refused_parent_gate/`
- `tests/fixtures/output_layout_refused_absolute_gate/`
- `tests/fixtures/output_layout_refused_drive_gate/`

**Planning artifact**
- `.planning/phases/51-two-layer-output-documentation/51-SWEEP-AUDIT.md` (new, created here)

**Reused, NOT created** — `tests/fixtures/bld03_self_collision_gate/` and
`tests/fixtures/state_guard_three_master_gate/` already exist and must not be modified.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; blocking threshold `high`. This phase changes zero lines under `typsphinx/`. The register
is short because the honest surface is small — it is not padded.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest process → `sphinx-build` subprocess | The gate spawns `sys.executable -m sphinx` on repo-controlled fixture paths and pytest's `tmp_path`. No user-controlled input crosses it. |
| executor shell → `sphinx-build -b html` over `docs/source` | A repo-controlled source tree, built into a temporary directory outside the working tree. |
| repository → published documentation | The composition section is the last user-facing claim this phase adds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Tampering | `_run_sphinx_build()` and the Task 2 docs build | low | accept | Both invoke Sphinx with a list argument, never `shell=True`; every path is a repo constant, pytest's `tmp_path`, or a `mktemp -d` directory outside the working tree. |
| T-51-02 | Information Disclosure | `51-SWEEP-AUDIT.md` transcripts | low | mitigate | The audit records command output. Strip or elide any absolute host path prefix from the `mktemp -d` build directory before transcribing; record filenames and warning text, not the sandbox's directory layout. |
| T-51-07 | Repudiation | the sweep audit itself | medium | mitigate | The audit is the phase's own accountability record: 13 Part A rows plus 4 STILL-TRUE sites, each with a named plan and a reason. An acceptance criterion counts the table rows, so a silently shortened audit fails mechanically rather than passing review. This is the mechanical form of this plan's P2 prohibition. |
| T-51-SC | Tampering | package-manager installs | — | accept | Not applicable: this plan installs no npm/pip/cargo package and adds no dependency; `--extra docs` installs already-declared project extras only (`pyproject.toml`). No Package Legitimacy Audit is owed. |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_output_layout_docs_gate.py -q` — 13 passed, 0 skipped.
- `uv run python -m pytest -q -m "not slow"` — full suite green.
- `uv run python -m sphinx -b html docs/source <tmpdir>` — exit 0, no warning naming the new page and
  no undefined-label / unknown-document warning.
- `51-SWEEP-AUDIT.md` exists with 13 disposed Part A rows and the four STILL-TRUE sites.
- `git diff --name-only HEAD -- typsphinx/ CHANGELOG.md tests/fixtures/state_guard_three_master_gate/`
  is empty.
</verification>

<success_criteria>
- The page is complete: both layers named, which file to compile, the standalone-content behaviour as
  plain prose, target-as-path with worked examples, the refusal cases with verbatim warnings, the
  collision contract, the shared-child composition, and a See Also list.
- Every configuration example published on the page was built by the gate, which never skips.
- All 13 Part A sweep rows and all four STILL-TRUE sites are disposed in writing.
- The D-07 exclusion is measured with three scoped checks and the correction to the proposed one-liner
  is recorded.
- No line under `typsphinx/` changed.
</success_criteria>

<output>
Create `.planning/phases/51-two-layer-output-documentation/51-06-SUMMARY.md` when done.
</output>
