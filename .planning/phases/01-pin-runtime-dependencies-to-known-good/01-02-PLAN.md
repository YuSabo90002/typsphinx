---
phase: 01-pin-runtime-dependencies-to-known-good
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/build_multilang.py
  - tests/test_config_other_options.py
  - tests/test_config_toctree_defaults.py
autonomous: true
requirements: [LINT-01, LINT-02]
must_haves:
  truths:
    - "black --check . exits 0 on the full tree"
    - "ruff check . exits 0 on the full tree"
  artifacts:
    - docs/build_multilang.py
    - tests/test_config_other_options.py
    - tests/test_config_toctree_defaults.py
  key_links:
    - "black config ([tool.black] line-length 88, target-version py39 — unchanged this phase) -> reformatted files are consistent with the repo's black settings, so black --check stays 0"
---

<objective>
Make the tree lint-clean: run `black` to apply the three-file reformat (`docs/build_multilang.py`, `tests/test_config_other_options.py`, `tests/test_config_toctree_defaults.py`) until `black --check .` exits 0, then run `ruff check .` and fix any findings until it exits 0.

This plan lands the `black` reformat as its OWN commit, deliberately separate from the pin change in plan 01-01, so a red/green CI result is unambiguous and `git blame` on the reformatted lines stays clean (D-04).

Purpose: The unmaintained tree drifted out of `black` compliance (current `black` reformats exactly 3 files) and `ruff` cleanliness must be confirmed (it could not be executed in the planning sandbox — a native-binary limitation, not a codebase finding). LINT-01/02 close both.
Output: Three reformatted Python files (plus any files `ruff` fixes), landed as reviewable lint commit(s) separate from the pin change.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pin-runtime-dependencies-to-known-good/01-CONTEXT.md
@.planning/phases/01-pin-runtime-dependencies-to-known-good/01-RESEARCH.md
@.planning/phases/01-pin-runtime-dependencies-to-known-good/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the black reformat (separate commit from the pin change)</name>
  <files>docs/build_multilang.py, tests/test_config_other_options.py, tests/test_config_toctree_defaults.py</files>
  <read_first>
    - docs/build_multilang.py (module docstring / import block, lines 1-10 — black inserts a blank line between the docstring and the first import)
    - tests/test_config_other_options.py (repeated `conf_py.write_text("""..."""` calls — black collapses the multi-line `write_text(\n    """..."""\n)` shape)
    - tests/test_config_toctree_defaults.py (mostly already black-clean; only the multi-line `write_text(` calls change — run black's own diff first, do not assume every call changes)
    - .planning/phases/01-pin-runtime-dependencies-to-known-good/01-PATTERNS.md (exact before/after excerpts for all three files; the black reformat is the D-04 separate commit)
    - .planning/phases/01-pin-runtime-dependencies-to-known-good/01-RESEARCH.md (black 26.5.1 confirmed exactly these 3 files reformat, 46 unchanged; the collapse is black's stable style, not target-version related)
    - .planning/phases/01-pin-runtime-dependencies-to-known-good/01-CONTEXT.md (D-04 separate-commit constraint)
  </read_first>
  <action>
    Preview the exact diff first with `black --diff .` (or per-file) to confirm scope matches the expected 3 files (`docs/build_multilang.py`, `tests/test_config_other_options.py`, `tests/test_config_toctree_defaults.py`) with 46 files unchanged. Then apply with `black .` — let black reformat consistently; do NOT hand-edit individual lines. Do NOT change `[tool.black].target-version` in `pyproject.toml` (stays `py39` this phase — Phase 3 owns the floor bump). Do NOT touch `pyproject.toml`, `tox.ini`, or `uv.lock` in this commit.

    Commit ONLY the reformatted Python files as a standalone style commit. This commit MUST NOT contain any pin/config change from plan 01-01 (D-04) — the pin change and the black reformat are separate landing units.

    If `black` unexpectedly reformats files beyond the three named, inspect the extra changes before committing (they may indicate a black-version mismatch); the research-confirmed scope is exactly 3 files.
  </action>
  <verify>
    <automated>black --check .</automated>
  </verify>
  <acceptance_criteria>
    - `black --check .` exits 0 on the full tree — LINT-01
    - `docs/build_multilang.py`, `tests/test_config_other_options.py`, and `tests/test_config_toctree_defaults.py` are reformatted; the reformat is confined to these files (research-confirmed scope: 3 changed, 46 unchanged)
    - `[tool.black].target-version` in `pyproject.toml` is unchanged (still `py39`/`py310..py312`)
    - The style commit contains no `pyproject.toml`/`tox.ini`/`uv.lock` changes (D-04 — separate from the pin commit)
  </acceptance_criteria>
  <done>`black --check .` exits 0; the 3 files are reformatted and landed as a standalone style commit with no pin/config changes mixed in.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm ruff is clean on the full tree, fixing any findings</name>
  <files>(ruff-flagged files — none expected per research; scope determined by ruff output)</files>
  <read_first>
    - pyproject.toml (`[tool.ruff]` line-length 88, target-version py39; `[tool.ruff.lint]` select/ignore lists at lines 101-117 — these define what ruff enforces; do NOT change them this phase)
    - .planning/phases/01-pin-runtime-dependencies-to-known-good/01-RESEARCH.md (LINT-02 could not be run in the sandbox — native binary; Open Questions #1 and Assumptions Log A2: run ruff early, treat any findings as in-scope fixups, not replanning)
    - .planning/phases/01-pin-runtime-dependencies-to-known-good/01-VALIDATION.md (LINT-02 manual-only note — executor must run ruff in an environment where the native binary executes)
  </read_first>
  <action>
    Run `ruff check .` early (research could not execute the native `ruff` binary in the planning sandbox, so cleanliness is unconfirmed — treat this as an acceptance check, not a pre-confirmed pass). If it reports zero findings, LINT-02 is already satisfied and no commit is needed. If it reports findings, fix them: prefer `ruff check --fix .` for the auto-fixable ones (import ordering, simple lint fixes), then hand-fix the remainder, guided by the enabled rule set in `[tool.ruff.lint]` (do NOT loosen the `select`/`ignore` config to make findings disappear). Do NOT change `[tool.ruff].target-version` (stays `py39` this phase).

    If ruff's fixes modify files, re-run `black .` afterward so the tree stays black-clean (ruff fixups can shift formatting), then confirm both `ruff check .` and `black --check .` exit 0. Commit any ruff fixups as their own commit (may ride alongside the black style commit, but MUST remain separate from the pin/config commit in plan 01-01 — D-04).
  </action>
  <verify>
    <automated>ruff check . && black --check .</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check .` exits 0 on the full tree — LINT-02
    - `black --check .` still exits 0 (any ruff fixups did not reintroduce a formatting drift) — LINT-01 preserved
    - `[tool.ruff]` config (`target-version`, `select`, `ignore`) is unchanged — findings were fixed, not suppressed
    - Any ruff-fixup commit contains no `pyproject.toml`/`tox.ini`/`uv.lock` pin changes (D-04)
  </acceptance_criteria>
  <done>`ruff check .` exits 0 and `black --check .` still exits 0; any fixups landed as lint commits separate from the pin change, with the ruff config unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer/tool edit → committed source | `black`/`ruff` auto-fixes rewrite Python source; an auto-fix could in principle alter runtime behavior (e.g. `ruff` removing a symbol it deems unused). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-L01 | Tampering | `ruff --fix` auto-fixes on Python source | low | mitigate | `black` is a pure formatter (no semantic changes). `ruff --fix` fixups are kept in a reviewable, standalone commit separate from the pin change (D-04); the existing pytest suite that catches any behavior regression is exercised by the full CI matrix in Phase 2. The ruff `select`/`ignore` config is not loosened to hide findings. |
| T-01-SC | Tampering | package installs | low | accept | This plan installs no new packages — `black` and `ruff` are pre-existing `dev` dependencies already relied upon by the project. No `[ASSUMED]`/`[SUS]` install gate applies (see RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
Tooling checks (all < 60s, per 01-VALIDATION.md):
- `black --check .` exits 0 — LINT-01.
- `ruff check .` exits 0 — LINT-02.
- Confirm no `pyproject.toml`/`tox.ini`/`uv.lock` changes are present in the lint commit(s) — D-04 separation.
</verification>

<success_criteria>
- `black --check .` exits 0 on the full tree (the 3 research-named files reformatted).
- `ruff check .` exits 0 on the full tree (findings fixed, not suppressed).
- The black reformat landed as its own commit, separate from the pin change (D-04).
- `[tool.black]`/`[tool.ruff]` config unchanged (target-version stays `py39` — Phase 3 owns the floor bump).
</success_criteria>

## Artifacts this phase produces

Symbols this plan creates/changes (source-grounding exclusion list for downstream drift verification):

- **Reformatted files** (black, no logic change): `docs/build_multilang.py` (blank line inserted between module docstring and first import); `tests/test_config_other_options.py` (multi-line `conf_py.write_text(...)` calls collapsed to black's hugged-paren style); `tests/test_config_toctree_defaults.py` (multi-line `write_text(...)` calls only).
- **Any ruff fixups**: scope determined by `ruff check .` output (none expected per research); confined to whatever files ruff flags, using the existing `[tool.ruff.lint]` rule set (config unchanged).

Note: no config, dependency, or `@preview`-version files are touched by this plan. `[tool.black]`/`[tool.ruff]` `target-version` stays `py39` (Phase 3 owns the Python-floor bump).

<output>
Create `.planning/phases/01-pin-runtime-dependencies-to-known-good/01-02-SUMMARY.md` when done.
</output>
