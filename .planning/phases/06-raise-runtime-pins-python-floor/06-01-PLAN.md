---
phase: 06-raise-runtime-pins-python-floor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tox.ini
  - .github/workflows/ci.yml
  - .github/workflows/docs.yml
  - .github/workflows/release.yml
  - .github/workflows/drift.yml
autonomous: true
requirements: [FWD-01, PIN-01, PIN-02, PIN-03]

must_haves:
  truths:
    - "uv sync --locked exits 0 against the regenerated lockfile (no stale-lock / resolution-conflict error) — PIN-03"
    - "The lockfile resolves sphinx to 9.1.x and docutils to a 0.21-0.22 version (0.22.4), with typst unchanged in the 0.14.1-0.15 range — FWD-01 / PIN-01"
    - "sphinx-build -b typst tests/roots/test-basic succeeds (build succeeded, exit 0, index.typ + _template.typ produced) under Sphinx 9.1 — FWD-01"
    - "Both the typst and typstpdf builders register when a Sphinx application is constructed for each — FWD-01"
    - "import typsphinx succeeds cleanly under the raised stack — FWD-01"
    - "The repo-wide grep for old pins/floors (py310, py311, sphinx>=5.0,<9, docutils>=0.18,<0.22, bare 3.10/3.11) returns zero hits across toml/ini/yml/yaml — PIN-02"
    - "The typst pin remains >=0.14.1,<0.15 (Phase 7 scope fence honored)"
  artifacts:
    - "pyproject.toml — requires-python >=3.12, classifiers 3.12/3.13 only, sphinx>=9.1,<10, docutils>=0.21,<0.23, types-docutils>=0.21, black/ruff/mypy target-versions at 3.12"
    - "uv.lock — regenerated (sphinx 9.1.x, docutils 0.22.x, requires-python >=3.12 header)"
    - "tox.ini — env_list py312/py313, [testenv] + [testenv:type] sphinx>=9.1,<10 + docutils>=0.21,<0.23 + types-docutils>=0.21"
    - ".github/workflows/ci.yml — matrix ['3.12','3.13'], single-runner jobs on Python 3.12"
    - ".github/workflows/docs.yml, release.yml, drift.yml — single-runner Python 3.12"
  key_links:
    - "pyproject.toml requires-python (>=3.12) <-> uv.lock header <-> ci.yml matrix python-version — all must agree at the 3.12 floor or CI installs Sphinx 9.1 on an unsupported interpreter"
    - "pyproject.toml docutils ceiling <-> tox.ini [testenv:type] docutils ceiling <-> types-docutils floor — split-brain hazard, no automated sync test; must move in lockstep"
    - "sphinx pin in pyproject.toml [project] <-> tox.ini [testenv] <-> tox.ini [testenv:type] — three duplicated declarations of the same constraint"
    - "pyproject.toml pin edits <-> uv.lock regeneration — must land together (atomicity); raising sphinx/docutils without the floor + lock makes uv lock unsatisfiable"
---

<objective>
Atomically raise the runtime dependency pins and Python floor to the target v0.5.0 ecosystem
so the extension installs, imports, and registers both builders against Sphinx 9.1 + docutils
0.22 on Python 3.12-3.13 with a regenerated, minimal-diff lockfile. Re-pin `sphinx>=9.1,<10`
(FWD-01), `docutils>=0.21,<0.23` (PIN-01), raise the Python floor to 3.12-3.13 across every
declaration site (PIN-02), and regenerate `uv.lock` (PIN-03) — all as one internally-consistent
state so no intermediate pushed commit tries to install Sphinx 9.1 on Python 3.10/3.11
(success criterion #4).

All work happens on the `release/v0.5.0` integration branch per D-01 — `main` stays green and
is NOT touched. This phase does NOT merge to `main`; that merge is a milestone-completion action
(D-02, Phase 9). The PDF / `docs-pdf` CI lanes are EXPECTED to stay red on the branch after this
phase (the `kai` fix is Phase 7) — per D-03 do NOT add any CI-hiding config (no
`continue-on-error`, no skip, no allow-failure) and do NOT treat a `typstpdf` / `docs-pdf`
failure as a Phase 6 regression.

Purpose: Stand up the real target ecosystem so every downstream phase (7-10) can diagnose
against it. This is a pure declaration-surface change — no translator/writer/builder source
code moves.
Output: 21 edited declaration sites across 6 config files + 1 regenerated lockfile artifact,
landing as one atomic, verifiable state.

Scope fences (do NOT cross — Phase 7/8 territory):
- typst stays `>=0.14.1,<0.15` — do NOT edit that dependency line.
- Do NOT touch the 3-way `@preview` version-sync sites (`writer.py` / `template_engine.py` /
  `templates/base.typ`) or `tests/test_preview_version_sync.py` (Phase 7).
- Do NOT do `doctree.traverse()`->`findall()` or full pytest-suite compatibility work (Phase 8).
- Do NOT use `-b typstpdf` / `tox -e docs-pdf` as a gate — expected red until Phase 7.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/06-raise-runtime-pins-python-floor/06-CONTEXT.md
@.planning/phases/06-raise-runtime-pins-python-floor/06-RESEARCH.md
@.planning/phases/06-raise-runtime-pins-python-floor/06-PATTERNS.md
@.planning/phases/06-raise-runtime-pins-python-floor/06-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This phase creates NO new source symbols and NO new script files — verification runs inline.
It changes the declaration surface + regenerates the lockfile. Target values:

| File | Sites | Target values |
|------|-------|---------------|
| `pyproject.toml` | 7 (+1 drop) | `requires-python = ">=3.12"`; classifiers drop `3.10`/`3.11` (keep `3`, `3.12`, `3.13`); `"sphinx>=9.1,<10"`, `"docutils>=0.21,<0.23"` (leave `"typst>=0.14.1,<0.15"`); dev-extra `"types-docutils>=0.21"`; drop dead `"tomli>=2.0; python_version < '3.11'"` line; `[tool.black] target-version = ["py312", "py313"]`; `[tool.ruff] target-version = "py312"`; `[tool.mypy] python_version = "3.12"` |
| `uv.lock` | 1 (regenerated) | Regenerated via targeted upgrade; resolves `sphinx==9.1.x`, `docutils==0.22.x`; header `requires-python` auto-updates to `>=3.12`. Never hand-edited. |
| `tox.ini` | 3 | `env_list = py312, py313, lint, type, cov, docs`; `[testenv]` `sphinx>=9.1,<10`; `[testenv:type]` `sphinx>=9.1,<10`, `types-docutils>=0.21`, `docutils>=0.21,<0.23` |
| `.github/workflows/ci.yml` | 7 | matrix `python-version: ['3.12', '3.13']`; `include:` drops `3.10`/`py310` + `3.11`/`py311`; five single-runner `uv python install` lines -> `3.12` (matrix-driven `${{ matrix.python-version }}` line untouched) |
| `.github/workflows/docs.yml` | 1 | `python-version: "3.12"` |
| `.github/workflows/release.yml` | 2 | both single-runner `uv python install` lines -> `3.12` |
| `.github/workflows/drift.yml` | 1 | single-runner `uv python install` line -> `3.12` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Raise pyproject.toml pins + Python floor and regenerate uv.lock (atomic core — mirrors precedent fce2ffb)</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (the file being edited — current values confirmed: line 10 requires-python, lines 20-24 classifiers, lines 29-33 dependencies, line 45 dev types-docutils, line 53 tomli marker, line 88 black, line 103 ruff, line 120 mypy; line 136 dependency-groups types-docutils already current at >=0.22.2.20251006, do NOT touch)
    - .planning/phases/06-raise-runtime-pins-python-floor/06-PATTERNS.md § "pyproject.toml (7 sites)" — exact current-value excerpts and target values
    - .planning/phases/06-raise-runtime-pins-python-floor/06-RESEARCH.md § "Standard Stack" + § "Exhaustive Declaration-Site Checklist" + Pitfall 1 + Pitfall 4
    - `git show fce2ffb` — precedent floor-raise commit that bundled the pyproject edits + uv.lock regeneration together (the atomicity pattern to copy)
  </read_first>
  <action>
    FIRST, ensure work is on the integration branch per D-01: if `release/v0.5.0` does not exist,
    create it from the current `main` (`git checkout -b release/v0.5.0`); otherwise check it out.
    Do NOT commit any Phase 6 work to `main` (D-01 — `main` must stay green all milestone).

    Then edit pyproject.toml to the target values (per D-02 scope: this phase does not merge to main):
    - Set `requires-python` to `>=3.12` (from `>=3.10`).
    - In classifiers, remove the `Programming Language :: Python :: 3.10` and `:: 3.11` entries;
      keep the bare `:: 3`, `:: 3.12`, and `:: 3.13` entries.
    - In `[project] dependencies`, set sphinx to `sphinx>=9.1,<10` and docutils to
      `docutils>=0.21,<0.23`. LEAVE the `typst>=0.14.1,<0.15` line EXACTLY as-is (Phase 7 fence —
      do NOT edit it). Do NOT widen docutils past `<0.23` (Sphinx 9.1 itself caps at `<0.23`).
    - In the `dev` optional-dependencies extra, set `types-docutils` floor to `>=0.21` (matches the
      new runtime docutils floor per RESEARCH A1). Do NOT touch the second `types-docutils` in
      `[dependency-groups] dev` (already current).
    - Remove the now-dead `tomli` conditional-dependency line in the `docs` extra
      (`tomli>=2.0; python_version < '3.11'`): under the 3.12 floor its marker is permanently false
      and stdlib `tomllib` (used with a fallback in `docs/source/conf.py`) always resolves on 3.11+.
      Removing it eliminates dead weight AND keeps the PIN-02 grep audit (Task 2) clean of a
      spurious `3.11` hit.
    - Set `[tool.black] target-version` to `["py312", "py313"]`.
    - Set `[tool.ruff] target-version` to `"py312"`. Do NOT enable/remove the `UP035`/`UP006`
      ignores or their comment text — that is a code-modernization change out of scope (CLAUDE.md /
      Phase 8); only the target-version number changes here, keeping the Dict/List typing style.
    - Set `[tool.mypy] python_version` to `"3.12"`.

    Then regenerate the lockfile with the minimal-diff targeted upgrade (Claude's-Discretion default,
    RESEARCH): run `uv lock --upgrade-package sphinx --upgrade-package docutils`. If the follow-up
    `uv sync --locked` fails with a transitive resolution conflict (Pitfall 4), fall back to a full
    `uv lock` re-resolve for this one case only. Never hand-edit `uv.lock`; its header
    `requires-python` auto-updates to `>=3.12`.

    Commit LOCALLY only (message e.g. `feat(06-01): raise sphinx/docutils/python pins in pyproject + regenerate uv.lock`).
    Do NOT push yet — the pushed state must also include Task 2's tox/CI edits (atomicity, success
    criterion #4): a push after this task alone would leave the CI matrix still listing 3.10/3.11
    against Sphinx 9.1.
  </action>
  <verify>
    <automated>uv sync --locked && grep -A2 'name = "docutils"' uv.lock && grep -A2 'name = "sphinx"' uv.lock && uv run python -c "import typsphinx; print(typsphinx.__version__)" && uv run python -c "import tempfile, os
from sphinx.application import Sphinx
srcdir = 'tests/roots/test-basic'
with tempfile.TemporaryDirectory() as tmp:
    for name in ('typst', 'typstpdf'):
        app = Sphinx(srcdir, srcdir, os.path.join(tmp, name), os.path.join(tmp, name+'-dt'), name)
        assert app.builder.name == name, name + ' failed to register'
        print(name + ': OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync --locked` exits 0 (no "lockfile is not up to date" / resolution-conflict message).
    - `uv.lock` shows `version = "9.1.x"` for sphinx and a `0.21.x`/`0.22.x` (target `0.22.4`) for
      docutils — NOT `0.23.x`, NOT `<0.21`.
    - `uv.lock` header `requires-python` reads `>=3.12`.
    - pyproject.toml contains `requires-python = ">=3.12"`, `"sphinx>=9.1,<10"`,
      `"docutils>=0.21,<0.23"`, `"types-docutils>=0.21"` (dev extra), black `["py312", "py313"]`,
      ruff `"py312"`, mypy `"3.12"`; classifiers `3.10`/`3.11` absent; the `tomli` conditional line
      absent; `"typst>=0.14.1,<0.15"` still present and unchanged.
    - `import typsphinx` succeeds (prints a version string, no traceback).
    - Registration smoke prints `typst: OK` and `typstpdf: OK` (both builders register under the
      installed Sphinx 9.1).
    - Current git branch is `release/v0.5.0` (not `main`); the commit is local (not pushed).
  </acceptance_criteria>
  <done>
    pyproject.toml pins + floor + tool target-versions raised to the 3.12/Sphinx-9.1/docutils-0.22
    targets, `uv.lock` regenerated and `uv sync --locked` green, both builders register and
    `import typsphinx` is clean — committed locally on `release/v0.5.0`, not pushed. FWD-01 / PIN-01
    / PIN-03 core satisfied at the packaging tier.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mirror the 3.12 floor into tox.ini + all four CI workflows, audit, and confirm the atomic pushed state (mirrors precedent 2e285d4)</name>
  <files>tox.ini, .github/workflows/ci.yml, .github/workflows/docs.yml, .github/workflows/release.yml, .github/workflows/drift.yml</files>
  <read_first>
    - tox.ini (line 2 env_list, line 21 [testenv] sphinx, lines 40-42 [testenv:type] sphinx/types-docutils/docutils)
    - .github/workflows/ci.yml (line 18 matrix, lines 20-27 include mapping, line 38 matrix-driven install — do NOT edit, lines 68/89/110/144/176 single-runner installs)
    - .github/workflows/docs.yml (line 24), .github/workflows/release.yml (lines 33, 86), .github/workflows/drift.yml (line 26)
    - .planning/phases/06-raise-runtime-pins-python-floor/06-PATTERNS.md § "tox.ini" + § "ci.yml" + § "docs.yml" + § "release.yml" + § "drift.yml" + § "Split-brain ceiling sync" + § "Post-edit verification grep" + § "Done-ness smoke gate"
    - .planning/phases/06-raise-runtime-pins-python-floor/06-RESEARCH.md § "Common Pitfalls" (Pitfall 2 grep + Pitfall 3 expected-red PDF lane) + § "Validation Architecture"
    - `git show 2e285d4` — precedent commit that reconciled CI/docs/release workflows to the prior floor raise (the file-set + edit-shape pattern to copy)
  </read_first>
  <action>
    Edit tox.ini:
    - Set `env_list` to `py312, py313, lint, type, cov, docs` (drop `py310`, `py311`; keep the rest).
    - In `[testenv]` deps, set the sphinx line to `sphinx>=9.1,<10` (leave pytest / pytest-cov lines).
    - In `[testenv:type]` deps, set sphinx to `sphinx>=9.1,<10`, `types-docutils` to `>=0.21` (MUST
      match the value chosen in Task 1 site 4 exactly — this is the split-brain hazard: docutils /
      types-docutils are duplicated across pyproject + tox with no automated sync test), and docutils
      to `docutils>=0.21,<0.23`.

    Edit .github/workflows/ci.yml:
    - Set the matrix `python-version` list to `['3.12', '3.13']`.
    - In the `include:` mapping, remove the two `3.10`->`py310` and `3.11`->`py311` entries; keep the
      `3.12`->`py312` and `3.13`->`py313` mappings intact.
    - Change all five single-runner `uv python install` lines (lint / type-check / coverage / build /
      integration jobs) from installing 3.10 to installing `3.12` (per CONTEXT.md discretion default:
      non-matrix jobs move to the new floor 3.12, not 3.13). Do NOT edit the matrix-driven
      `uv python install ${{ matrix.python-version }}` line — it inherits from the narrowed matrix.

    Edit .github/workflows/docs.yml: set the `actions/setup-python` `python-version` to `"3.12"`.
    Edit .github/workflows/release.yml: change both single-runner `uv python install` lines (validate
    + build jobs) to install `3.12`.
    Edit .github/workflows/drift.yml: change the single-runner `uv python install` line to install `3.12`.

    Do NOT add any CI-hiding config anywhere (no `continue-on-error`, no `if:` skips, no
    `allow-failure`) — per D-03 the PDF / `docs-pdf` lanes are expected red until Phase 7 and must be
    left visibly red, not papered over.

    Then run the post-edit grep audit and the done-ness smoke gate (commands in <verify>). Commit
    LOCALLY (message e.g. `feat(06-01): reconcile tox + CI/docs/release/drift workflows to the 3.12 floor`).
    Confirm atomicity before any push: run `git show --stat` / `git log --oneline` and verify the two
    local commits together cover all 21 declaration sites + the `uv.lock` regeneration with no partial
    state — the pushed HEAD must never be a commit that has Sphinx 9.1 pins while CI still targets
    3.10/3.11. Only after that consistency check is the branch safe to push to `origin/release/v0.5.0`.
  </action>
  <verify>
    <automated>! grep -rn "sphinx>=5.0,<9\|sphinx<9\|docutils>=0.18,<0.22\|docutils<0.22\|py310\|py311" --include="*.toml" --include="*.ini" --include="*.yml" --include="*.yaml" . && ! grep -rn "python.*3\.10\|python.*3\.11\|'3.10'\|'3.11'\|\"3.10\"\|\"3.11\"" --include="*.toml" --include="*.ini" --include="*.yml" --include="*.yaml" . && rm -rf /tmp/typst-smoke-out && uv run sphinx-build -b typst tests/roots/test-basic /tmp/typst-smoke-out && test -f /tmp/typst-smoke-out/index.typ && test -f /tmp/typst-smoke-out/_template.typ && echo SMOKE_OK</automated>
  </verify>
  <acceptance_criteria>
    - tox.ini `env_list` reads `py312, py313, lint, type, cov, docs`; `[testenv]` and `[testenv:type]`
      both pin `sphinx>=9.1,<10`; `[testenv:type]` has `types-docutils>=0.21` and `docutils>=0.21,<0.23`.
    - ci.yml matrix is `['3.12', '3.13']`; the `include:` mapping has only `3.12`/`3.13` entries; all
      five single-runner jobs install Python `3.12`; the matrix-driven install line is unchanged.
    - docs.yml `python-version: "3.12"`; release.yml both jobs install `3.12`; drift.yml installs `3.12`.
    - The two grep audits (old sphinx/docutils ceilings + `py310`/`py311`, and bare `3.10`/`3.11`
      python-version literals) return ZERO hits across `*.toml` / `*.ini` / `*.yml` / `*.yaml`
      (planning/history `.md` docs are excluded by the file-type filter).
    - `sphinx-build -b typst tests/roots/test-basic /tmp/typst-smoke-out` prints `build succeeded.`,
      exits 0, and produces `index.typ` + `_template.typ` (prints `SMOKE_OK`).
    - No `continue-on-error` / skip / `allow-failure` was added to any workflow (D-03).
    - `git log --oneline` / `git show --stat` confirms all 21 declaration-site edits + the `uv.lock`
      regeneration are covered by the local commits as one consistent state before any push (success
      criterion #4).
  </acceptance_criteria>
  <done>
    tox.ini + all four workflow files mirror the 3.12 floor and Sphinx-9.1/docutils-0.22 ceilings,
    the repo-wide grep audit is clean, the `-b typst` non-PDF pipeline builds green under the new
    stack, and the full 21-site + lockfile change is confirmed atomic (no partial pushed state).
    PIN-02 satisfied; FWD-01 non-PDF pipeline proven end-to-end; atomicity (criterion #4) confirmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry -> build/CI resolution | `uv lock` / `uv sync` pull dependency versions and their transitive graph from PyPI across this boundary; the version ranges declared in `pyproject.toml` are the only control point. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | Dependency version ranges (`pyproject.toml` -> `uv.lock`) | medium | mitigate | Keep tight upper ceilings mirroring Sphinx's own constraint (`sphinx<10`, `docutils<0.23`) rather than unbounded ranges; typst ceiling (`<0.15`) left unchanged. No range is widened past what the resolver requires. |
| T-06-02 | Tampering | Version bump on existing deps (typosquat / dependency-confusion) | low | accept | No NEW package names are introduced — only version *ranges* on already-present, already-vetted deps (`sphinx`, `docutils`, `typst`). RESEARCH § Package Legitimacy Audit verified all three as OK/Approved via the PyPI JSON API (18/16/3-year histories, canonical source repos). |
| T-06-SC | Tampering | `uv lock` regeneration pulling transitive deps | medium | mitigate | Targeted `uv lock --upgrade-package sphinx --upgrade-package docutils` keeps the diff minimal; `uv sync --locked` is the currency gate that asserts internal consistency (Pitfall 4 fallback to full re-resolve if it fails). No `[ASSUMED]`/`[SUS]` packages remain after RESEARCH's manual audit override, and no new packages are installed — so NO blocking-human checkpoint is warranted (this differs from the new-package case the legitimacy gate targets). |
</threat_model>

<verification>
Phase-level checks (all CLI, < 30s total latency per VALIDATION.md — the full pytest suite is
explicitly Phase 8's gate, NOT this phase's):

1. Resolution / currency (PIN-01 / PIN-03): `uv sync --locked` exits 0; `uv.lock` resolves
   sphinx 9.1.x + docutils 0.22.x; header `requires-python >= 3.12`.
2. Registration + non-PDF pipeline (FWD-01): the `Sphinx()` registration smoke prints
   `typst: OK` + `typstpdf: OK`; `sphinx-build -b typst tests/roots/test-basic <out>` prints
   `build succeeded.` and produces `index.typ` + `_template.typ`; `import typsphinx` is clean.
3. Floor audit (PIN-02): the repo-wide grep for `py310` / `py311` / `sphinx>=5.0,<9` /
   `docutils>=0.18,<0.22` / bare `3.10`/`3.11` returns zero hits across toml/ini/yml/yaml.
4. Atomicity (success criterion #4): `git show --stat` / `git log` confirms the 21 sites + lock
   regen are one consistent state; no partial-edit commit is pushed with Sphinx 9.1 pins while
   CI still targets 3.10/3.11.
5. Scope-fence checks: `typst>=0.14.1,<0.15` unchanged; no `@preview` version-sync file touched;
   no `traverse()`->`findall()` edit; no CI-hiding config added (D-03); PDF / `docs-pdf` lanes
   left visibly red (NOT a Phase 6 failure — Pitfall 3).
</verification>

<success_criteria>
- `uv sync --locked` green against a regenerated, minimal-diff `uv.lock` resolving
  `sphinx==9.1.x` + `docutils==0.22.x` (typst unchanged).
- A `sphinx-build -b typst` build and the two-builder registration smoke both pass under
  Sphinx 9.1; `import typsphinx` clean.
- Python range reads 3.12-3.13 at every declaration site (pyproject requires-python +
  classifiers + black/ruff/mypy target-versions, tox `env_list`, and the CI/docs/release/drift
  workflow Python versions); 3.10 and 3.11 fully removed (grep audit zero hits).
- All 21 sites + the lockfile regeneration land as one atomic, internally-consistent state on
  `release/v0.5.0` before any push (no intermediate state installs Sphinx 9.1 on 3.10/3.11).
- No scope fence crossed: typst pin untouched, no `@preview` / `traverse()` / CI-hiding work.
</success_criteria>

<output>
Create `.planning/phases/06-raise-runtime-pins-python-floor/06-01-SUMMARY.md` when done.
</output>
