---
phase: 22.1-typstpdf-compile-root-alignment-for-nested-masters
plan: 03
subsystem: testing
tags: [typst, typst-py, sphinx-builder, pdf-compile, render-gate, gap-closure]

requires:
  - phase: 22.1-02
    provides: "tests/fixtures/nested_master_render_gate/ fixture and tests/test_nested_master_render_gate.py::TestNestedMasterRenderGate (3 real-compile tests) -- extended in place by this plan"
provides:
  - "tests/test_nested_master_render_gate.py::TestNestedMasterRenderGate::test_sibling_include_fails_at_outdir_root_and_resolves_in_place -- standing red/green pair proving the sibling include('usage.typ')/image('../logo.png') failure mode in isolation from the template-import root-escape, closing gap G-22.1-2"
  - "test_outdir_root_compile_basis_still_fails tightened to a single failure class (template root-escape only), partitioning the failure space with the new test instead of overlapping it via a two-branch disjunction"
  - "Module docstring's 'Confirmed both directions' record extended with the second verbatim Typst error (the include/image class) and prose explaining why two pre-fix-basis tests are required"
affects: ["Phase 23 (v0.6.2 release prep) -- no additional CHANGELOG entry needed, this is test-only gap closure, not a behavior change"]

tech-stack:
  added: []
  patterns:
    - "One -b typst build driving both the RED and GREEN halves of a proof in a single test function, so the two directions are provably talking about byte-identical emitted output rather than two separately-built trees that could drift"
    - "Narrow in-place string substitution (replace exactly one occurrence, assert the count, assert the result differs, assert unrelated references survive unchanged) to isolate one failure class from another that would otherwise mask it"

key-files:
  created: []
  modified:
    - tests/test_nested_master_render_gate.py

key-decisions:
  - "Kept test_outdir_root_compile_basis_still_fails's name (cited by 22.1-VERIFICATION.md, 22.1-UAT.md, 22.1-02-SUMMARY.md) but tightened its assertion from a two-branch disjunction ('not found' or 'escape') to a single pinned root-escape assertion, and relabelled its docstring as the TEMPLATE-reference half of the SC#2 proof."
  - "The new test's RED half neutralizes ONLY the template import (rewrite count asserted == 1) so the include/image class can be reached and pinned in isolation; the GREEN half, at the same build, compiles the unmodified master at its real location and asserts a valid PDF."
  - "Deferred to plan text verbatim: transcribed the REAL captured Typst error ('file not found (searched at <outdir>/usage.typ)') into the module docstring rather than the plan's anticipated form -- they matched exactly, so no adaptation was needed (unlike Plan 02's D-08(a) adaptation)."

requirements-completed: [PDF-02]

coverage:
  - id: D1
    description: "test_outdir_root_compile_basis_still_fails tightened to assert exactly one failure class (root-escape of the ../_template.typ import) instead of a tolerant two-branch disjunction, and relabelled as the template-reference half of the SC#2 proof"
    requirement: "PDF-02"
    verification:
      - kind: integration
        ref: "tests/test_nested_master_render_gate.py::TestNestedMasterRenderGate::test_outdir_root_compile_basis_still_fails"
        status: pass
    human_judgment: false
  - id: D2
    description: "New test_sibling_include_fails_at_outdir_root_and_resolves_in_place proves the sibling include/image failure mode as an explicit red/green pair, closing gap G-22.1-2 and discharging ROADMAP SC#2 in its literal file-not-found shape"
    requirement: "PDF-02"
    verification:
      - kind: integration
        ref: "tests/test_nested_master_render_gate.py::TestNestedMasterRenderGate::test_sibling_include_fails_at_outdir_root_and_resolves_in_place"
        status: pass
    human_judgment: false
  - id: D3
    description: "Module docstring's 'Confirmed both directions' record carries both real, verbatim Typst errors (template root-escape and include/image file-not-found) and explains why two pre-fix-basis tests are required"
    requirement: "PDF-02"
    verification:
      - kind: other
        ref: "Direct read of tests/test_nested_master_render_gate.py module docstring lines 64-140"
        status: pass
    human_judgment: false

duration: ~20min
completed: 2026-07-22
status: complete
---

# Phase 22.1 Plan 03: G-22.1-2 Gap Closure -- Sibling Include/Image Isolation Test Summary

**Added `test_sibling_include_fails_at_outdir_root_and_resolves_in_place`, an explicit red/green pair from one `-b typst` build that neutralizes only the masking `../_template.typ` template import to isolate and prove the sibling `include()`/`image()` failure mode ROADMAP SC#2 literally names, and tightened `test_outdir_root_compile_basis_still_fails` to pin exactly the template-reference class it actually exercises.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-07-22T03:30:13+09:00 (session start, base commit `3547784`)
- **Completed:** 2026-07-22T03:35:06+09:00
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Tightened `test_outdir_root_compile_basis_still_fails`'s assertion from a tolerant `"not found" in lowered or "escape" in lowered` two-branch disjunction to a single pinned `"escape" in lowered` assertion -- the exact failure class this test provably produces (a verbatim copy fails on the earlier-positioned `../_template.typ` import before the compiler ever reaches the sibling include/image references). Rewrote its docstring to identify itself as the TEMPLATE-reference half of the SC#2 proof.
- Added `test_sibling_include_fails_at_outdir_root_and_resolves_in_place`: from the same `-b typst` build, neutralizes ONLY the template import (asserting the substitution occurred exactly once and that the sibling `include("usage.typ")`/`image("../logo.png")` references survive byte-identical), then compiles the neutralized copy from the outdir root inside `pytest.raises`. The captured error -- `file not found (searched at <outdir>/usage.typ)` -- is asserted to name `usage.typ`, carry a missing-file signature, NOT name `_template.typ`, and NOT carry the `api/` path segment (proving the compiler searched at the outdir root). The GREEN half, at the same build, asserts the unmodified master at `outdir/api/index.typ` compiles to a `%PDF`-prefixed byte string.
- Rewrote the module docstring's "Confirmed both directions" record to present the FAILS direction as two complementary proofs (template-reference class + include/image class), transcribing the real captured error for each and explaining why two tests are required: the emitted master carries two independent outdir-root-relative references, and the earlier one (template import, line 7) masks the later one (sibling include, line 33) unless neutralized first.
- Ran the diagnosis-verified remediation for real and captured the exact error text (`file not found (searched at <outdir>/usage.typ)`) -- it matched the plan's anticipated form exactly, so no error-text adaptation was needed.

## Task Commits

Each task was committed atomically:

1. **Task 1: Tighten test_outdir_root_compile_basis_still_fails to the single failure class it actually exercises** - `309b321` (test)
2. **Task 2: Add the include/image isolation test as an explicit red-then-green pair, and rewrite the module docstring record** - `e3d0373` (test)

_Metadata commit for SUMMARY.md is created separately in worktree mode; STATE.md/ROADMAP.md updates are owned by the orchestrator after wave merge._

## Files Created/Modified
- `tests/test_nested_master_render_gate.py` - Tightened `test_outdir_root_compile_basis_still_fails` to a single failure class; added `test_sibling_include_fails_at_outdir_root_and_resolves_in_place` (module now has 4 tests); rewrote the module docstring's "Confirmed both directions" record; reformatted via `black` (line-wrapping only, no semantic change).

## Decisions Made
- Kept `test_outdir_root_compile_basis_still_fails`'s name and structure (build/copy/`pytest.raises` unchanged) per the plan's explicit decision -- only its post-`raises` assertions and docstring changed.
- The new test's neutralization step is scoped to a single `str.replace()` call on the one relative template-import occurrence, verified by an exact-count assertion (`occurrences == 1`) before rewriting and a differs-from-original assertion after, so an accidental over-broad rewrite would fail the test rather than silently invalidate the isolation.
- Wrote the neutralized copy to `_prefix_basis_neutralized.typ` (distinct from the sibling test's `_prefix_basis_copy.typ`) to avoid any collision within the shared `temp_build_dir_typst` build tree.

## Deviations from Plan

None - plan executed exactly as written. The diagnosis-verified remediation (neutralize the template import, compile from the outdir root) produced the exact anticipated error text (`file not found (searched at <outdir>/usage.typ)`) on the first real run, so no adaptation of the assertions or docstring record was required (unlike Plan 02's D-08(a), which did need adaptation).

One grep-based acceptance criterion in the plan (`grep -c '\[sys.executable' tests/test_nested_master_render_gate.py` outputs `1`) did not literally match after `black` reformatting: the `subprocess.run([sys.executable, ...])` call's list argument wraps across multiple lines (list-opening `[` and `sys.executable` land on separate lines), so the substring `[sys.executable` never appears in the file -- this was already true of the UNMODIFIED `_run_sphinx_build` helper before this plan touched anything (confirmed via `git show <pre-existing-commit>:tests/test_nested_master_render_gate.py | grep -c '\[sys.executable'` == `0`), so it is a pre-existing plan-authoring assumption about black's line-wrapping behavior, not a regression introduced here. The SUBSTANTIVE intent of that criterion -- "no second build helper, no resolved-binary subprocess argv" -- is verified directly: `grep -c 'def _run_sphinx_build'` is `1` (no second helper was added) and the sole subprocess call still invokes `sys.executable -m sphinx` (confirmed by reading the function). No code change was made to force this cosmetic grep to match, since `black` owns wrapping (CLAUDE.md) and reformatting solely to satisfy a literal-substring criterion would fight the formatter.

## Issues Encountered

None beyond the grep-formatting note documented above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Gap `G-22.1-2` is closed: `tests/test_nested_master_render_gate.py` now carries 4 passing tests (0 skipped, `typst-py` 0.15.0 present), with the two pre-fix-basis tests partitioning the failure space instead of overlapping it via a shared tolerant disjunction.
- `tests/test_preview_version_sync.py` stays 2/2; the 3-way `@preview` version-sync surface (`writer.py`/`template_engine.py`/`templates/base.typ`) is untouched (this plan is test-only, `typsphinx/` was never touched -- `git status --porcelain typsphinx/` empty for the whole plan).
- Full fast suite: 484 passed, 45 failed -- the 45 failures are the pre-existing NixOS-sandbox `sys.executable -m sphinx` exec-127 environmental hazard, confirmed unchanged in count and file set against the project's standing memory note (`nixos-sandbox-test-env`) and 22.1-02-SUMMARY.md's own baseline ("466/466 passing, 5 pre-existing NixOS-sandbox-environmental files excluded, confirmed unchanged at exactly 45 failures") -- not a regression introduced by this plan.
- `black --check .` (151 files unchanged after this plan's own reformat) and `ruff check .` (via `nix-shell -p ruff`) both clean across the whole repo.
- No blockers. This closes the one outstanding gap (`G-22.1-2`) flagged by `22.1-VERIFICATION.md`'s human_verification item #2; the phase's remaining two human_verification items (the `writer.py` scope-expansion acceptance and the judgment-tier prohibitions confirmation) are human policy decisions unaffected by this test-only plan and remain for the human reviewer.

## Known Stubs

None.

## Threat Flags

None -- this plan introduces no new network, auth, or file-access surface beyond what `22.1-03-PLAN.md`'s own `<threat_model>` (T-22.1-11 through T-22.1-14, T-22.1-SC) already registers and mitigates: the new test writes only into a pytest `tmp_path`-scoped build directory under a leading-underscore name distinct from the sibling test's copy, and asserts the substitution isolation (exact-count + differs + unrelated-references-survive) so an over-broad rewrite fails loudly rather than silently.

---
*Phase: 22.1-typstpdf-compile-root-alignment-for-nested-masters*
*Completed: 2026-07-22*
