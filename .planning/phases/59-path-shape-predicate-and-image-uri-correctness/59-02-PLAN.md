---
phase: 59-path-shape-predicate-and-image-uri-correctness
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - typsphinx/builder.py
  - tests/test_track_image_key_construction.py
  - tests/test_copy_image_files_name_too_long.py
  - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md
autonomous: true
requirements: [IMG-04, IMG-06]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'For a Windows-shaped absolute resolved_uri, the relocation key _track_image() writes into node["uri"] contains no backslash character.'
    - "The relocation key's final path component is at most 255 UTF-8 bytes, measured as len(component.encode(\"utf-8\")), with the {sha1[:8]}- digest anchor kept whole."
    - 'A relocation-key final component of exactly 255 UTF-8 bytes is returned untruncated; 256 bytes is truncated to at most 255; 254 bytes is returned unchanged.'
    - 'When the normalized basename is empty, the relocation key final component is exactly the 9-byte {digest}- prefix and no exception is raised.'
    - 'Truncation lands on a UTF-8 character boundary: for a basename of 3-byte CJK characters, component.encode("utf-8").decode("utf-8") round-trips without error.'
    - 'Truncation only removes bytes and never pads or rounds up: the basename budget is exactly 255 minus the byte length of the {digest}- prefix (246), and when the extension alone would consume the whole budget the extension is truncated so at least one stem byte survives.'
    - 'Two distinct long resolved_uri values sharing a basename still produce two distinct relocation keys (the collision property IMG-03 closed is preserved under truncation).'
    - 'copy_image_files() no longer logs a "Failed to copy image" warning for a long-basename absolute URI, and the destination file exists in outdir after the copy.'
    - 'The pre-fix "Failed to copy image ...: [Errno 36] File name too long" warning line and the pre-fix backslash-bearing key are both quoted verbatim in 59-WINDOWS-URI-EVIDENCE.md, recorded before the product edit.'
  artifacts:
    - tests/test_track_image_key_construction.py
    - tests/test_copy_image_files_name_too_long.py
    - 'typsphinx/builder.py :: MAX_PATH_COMPONENT_BYTES module constant'
    - 'typsphinx/builder.py :: _bound_relocation_component() module-level helper'
    - 'typsphinx/builder.py :: _build_relocation_key() module-level helper'
  key_links:
    - '_track_image() escape branch -> _build_relocation_key(resolved_uri): the single construction site for the escape-branch key.'
    - '_build_relocation_key() -> _bound_relocation_component(): normalization happens first, bounding second.'
    - '_build_relocation_key() -> hashlib.sha1(resolved_uri.encode("utf-8")): the digest input stays the RAW resolved_uri, never the normalized string.'
    - 'relocation key -> copy_image_files() dest = path.join(self.outdir, imguri): the bound is what keeps this write under the filesystem component limit.'
  prohibitions:
    - 'The SHA-1 digest input stays the raw, un-normalized resolved_uri; no shared normalized variable feeds both the digest and the basename extraction.'
    - 'The 255-byte bound applies to the whole {digest}-{basename} final path component, never to the basename alone.'
    - 'The two non-escape key branches in _track_image() are not modified.'
    - 'No drive-prefix stripping is added to the key -- D-12 is deferred with its measurement.'
    - 'The logger.warning f-string in the escape branch is not edited; its !r quoting is MSG-03 in Phase 60.'
    - 'The logged warning from copy_image_files() is captured with caplog, not with a warnings-module interception helper.'
    - 'No existing test assertion anywhere under tests/ is modified; only new test files are added.'
    - 'No new runtime dependency and no new typst_ config value is introduced.'
---

<objective>
Close IMG-04 and IMG-06: `_track_image()`'s escape branch builds its relocation key from a
forward-slash-normalized basename, and that key's final path component is bounded to 255 UTF-8 bytes
with the `{sha1[:8]}-` collision anchor kept whole.

Purpose: these two land in the same three lines of `builder.py`, thirteen lines below the
`_escapes_outdir()` call plan 01 hardened. ROADMAP constraint 3 forbids running them as parallel
worktrees against `builder.py`, so this plan runs strictly after plan 01 rather than beside it.

Output: two new module-level helpers in `typsphinx/builder.py` (`_bound_relocation_component`,
`_build_relocation_key`) plus the `MAX_PATH_COMPONENT_BYTES` constant; the two new gate modules
`tests/test_track_image_key_construction.py` and `tests/test_copy_image_files_name_too_long.py`; and
the IMG-04 / IMG-06 sections of `59-WINDOWS-URI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-PATTERNS.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-VALIDATION.md
@.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-01-SUMMARY.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 11). Before running ANY command in this plan, provision the worktree's own venv:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Do not degrade to main-tree execution.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Record IMG-04 and IMG-06 verbatim RED through the real product path</name>
  <files>tests/test_track_image_key_construction.py, tests/test_copy_image_files_name_too_long.py, .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</files>
  <read_first>
    - typsphinx/builder.py lines 1637-1790 (`_track_image`: the `_is_absolute_image_uri` gate, the `relpath` + `_escapes_outdir` call, the `digest`/`key` construction and its `logger.warning`, and the two non-escape branches)
    - typsphinx/builder.py lines 1957-1992 (`copy_image_files`, including the `except Exception as e: logger.warning(f"Failed to copy image {imguri}: {e}")` that swallows the OSError)
    - tests/test_builder.py lines 505-600 (`test_post_process_images_rehome_escape_relocates_with_warning` — the house pattern: `temp_sphinx_app` → `TypstBuilder(app, app.env)` → `builder.init()` → hand-built `nodes.image` + `Reporter`/`states.Struct` document → `post_process_images(doc)` inside `caplog.at_level("WARNING")`, with the expected key COMPUTED from the same construction, never hardcoded)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md § D-06, D-07, D-08
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-RESEARCH.md § "Common Pitfalls" Pitfalls 2, 3 and 4
  </read_first>
  <action>
    Create `tests/test_track_image_key_construction.py` with a class `TestRelocationKeyNoBackslash`
    holding IMG-04's behavioural RED gate, named
    `test_relocation_key_no_backslash_for_windows_shaped_uri`. It follows `tests/test_builder.py`'s
    house pattern exactly: `temp_sphinx_app` fixture → `TypstBuilder(app, app.env)` → `builder.init()`
    → a hand-built `nodes.image` node whose `uri` and `candidates["*"]` are the Windows-shaped
    absolute string literal `C:\\Users\\runner\\assets\\sub\\we"ird.png` (written as a Python raw
    string so it carries exactly one backslash per separator and one literal double quote) →
    `builder.post_process_images(doc)` inside `caplog.at_level("WARNING")`. Assert that
    `node["uri"]` starts with `f"{RESERVED_IMAGE_NAMESPACE}/"` (the escape branch fired) and that
    `"\\" not in node["uri"]` (IMG-04's actual claim), with an assertion message that echoes the full
    offending key so the RED transcript names it. Do not assert on the warning message text — its
    `!r` quoting is MSG-03's site in Phase 60 (ROADMAP constraint 4). Add a docstring recording
    Pitfall 3: a green `copy_image_files()` run is NOT evidence the key is backslash-free, because a
    POSIX filesystem happily creates a filename containing a literal backslash byte; this assertion
    is on the key string itself, independent of any filesystem operation.

    Add to the same module a class `TestRelocationKeyLengthBound` holding IMG-06's behavioural RED
    gate, named `test_relocation_key_length_bound_through_track_image`. Build a real 250-character
    ASCII basename plus `.png`, form an absolute POSIX-shaped URI from `tmp_path` and that basename,
    drive it through `post_process_images()` the same way, and assert
    `len(node["uri"].rsplit("/", 1)[-1].encode("utf-8")) <= 255`, with an assertion message that
    prints the actual byte length. Pre-fix this is 263 (9 bytes of `{digest}-` plus 254) — D-06's
    measured "bounding the basename alone still fails" case.

    Create `tests/test_copy_image_files_name_too_long.py` with a class
    `TestCopyImageFilesNameTooLong` holding IMG-06(b)'s integration gate, named
    `test_copy_image_files_length_bound_no_name_too_long_warning`. FIRST lines of the test body:
    probe the filesystem by attempting to create a file with a 250-character basename under
    `tmp_path`, wrapped in `except OSError as e: pytest.skip(...)` with the errno in the skip reason —
    an in-body probe, never a `pytest.mark.skipif` decorator, because decorators are evaluated at
    collection time before `tmp_path` exists (RESEARCH Pitfall 1). Then create that long-named file
    for real with valid PNG bytes copied from
    `tests/fixtures/absolute_image_render_gate/_static/converted_stand_in.png`, in a directory under
    `tmp_path` that is outside the app's `doctreedir` so the escape branch fires. Drive
    `builder.post_process_images(doc)` then `builder.copy_image_files()` inside
    `caplog.at_level("WARNING")` — use `caplog`, never a warnings-module interception helper, because
    this is a `sphinx.util.logging` call and not `warnings.warn()` (RESEARCH Pitfall 4). Assert (a)
    no captured WARNING record's message starts with `Failed to copy image`, with an assertion
    message that echoes every captured warning message verbatim, and (b) the destination
    `Path(builder.outdir) / node["uri"]` exists. Keep the assertion platform-independent: assert on
    the absence of the `Failed to copy image` prefix, never on a hardcoded errno number, because
    ENAMETOOLONG is 36 on Linux and 63 on macOS.

    Run both new modules, capture the full verbatim pytest output, and append it to
    `59-WINDOWS-URI-EVIDENCE.md` under `## IMG-04 / IMG-06` → `### RED (pre-fix)`, together with the
    commands and the `PHASE_BASE_SHA` the run was taken at. The IMG-06(b) transcript must contain the
    platform's verbatim `Failed to copy image ...: [Errno 36] File name too long` line (or that
    platform's equivalent errno text) — if it does not, the fixture did not reach `shutil.copy2` and
    the gate is measuring the wrong thing; HALT rather than proceeding. Commit the three RED files
    together; the fix arrives in task 2.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; ! uv run pytest tests/test_track_image_key_construction.py tests/test_copy_image_files_name_too_long.py -q &amp;&amp; grep -q 'File name too long' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_track_image_key_construction.py -k no_backslash -x` exits NON-ZERO with 1 failed, 0 skipped
    - `uv run pytest tests/test_track_image_key_construction.py -k length_bound -x` exits NON-ZERO with at least 1 failed, 0 skipped
    - `uv run pytest tests/test_copy_image_files_name_too_long.py -x` exits NON-ZERO with 1 failed and 0 skipped on this dev machine
    - `grep -c 'caplog.at_level' tests/test_copy_image_files_name_too_long.py` is at least 1
    - `grep -c 'pytest.skip' tests/test_copy_image_files_name_too_long.py` is at least 1 and `grep -c 'skipif' tests/test_copy_image_files_name_too_long.py` is 0
    - `59-WINDOWS-URI-EVIDENCE.md` § "IMG-04 / IMG-06" contains a verbatim transcript naming the pre-fix backslash-bearing key and a verbatim `Failed to copy image ...: [Errno 36] File name too long` line
    - `typsphinx/builder.py` is unchanged by this task (`git diff --stat -- typsphinx/builder.py` is empty)
  </acceptance_criteria>
  <done>Both RED gates are committed failing with 0 skipped, and their verbatim transcripts — including the ENAMETOOLONG warning line — are recorded in the evidence file with no product file touched.</done>
  <reversibility rating="reversible">Two new test modules plus an evidence append.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the bounded relocation-key helpers and wire the escape branch to them</name>
  <files>typsphinx/builder.py</files>
  <read_first>
    - typsphinx/builder.py lines 1-45 (module docstring, imports — `hashlib`, `posixpath`, `from os import path` — and `RESERVED_IMAGE_NAMESPACE`)
    - typsphinx/builder.py lines 86-240 (the existing module-level predicate cluster: the house style new helpers must match — docstring with `Args:`/`Returns:`/`Examples:`, `posixpath` over OS-native `path`)
    - typsphinx/builder.py lines 1750-1790 (the escape branch's `digest`/`key` construction, its `logger.warning`, and the two non-escape branches that must stay untouched)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-RESEARCH.md § "Code Examples" → "IMG-04/IMG-06 combined key construction (normalize, then bound)"
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md § D-06, D-07
  </read_first>
  <behavior>
    - `_bound_relocation_component("a1b2c3d4", "x"*250 + ".png")` returns a component of exactly 255 UTF-8 bytes, starting `a1b2c3d4-` and ending `.png`
    - `_bound_relocation_component("deadbeef", "図"*100 + ".png")` returns 253 UTF-8 bytes that round-trip through encode/decode and end `.png`
    - `_bound_relocation_component("a1b2c3d4", "x"*246)` returns exactly 255 bytes, untruncated
    - `_bound_relocation_component("a1b2c3d4", "x"*245)` returns exactly 254 bytes, untruncated
    - `_bound_relocation_component("a1b2c3d4", "")` returns exactly `a1b2c3d4-` (9 bytes)
    - `_bound_relocation_component("a1b2c3d4", "s" + "." + "e"*300)` truncates the extension and keeps at least one stem byte
    - `_build_relocation_key(r"C:\Users\runner\assets\sub\we\"ird.png")` returns `_typst_converted/{digest}-we"ird.png` with no backslash
    - `_build_relocation_key(a)` != `_build_relocation_key(b)` for two distinct long URIs sharing a basename
  </behavior>
  <action>
    In `typsphinx/builder.py`, add a module-level constant `MAX_PATH_COMPONENT_BYTES = 255` beside
    `RESERVED_IMAGE_NAMESPACE`, with a comment recording it as a hardcoded owner decision (ROADMAP
    constraint 7): `os.pathconf()`/`os.statvfs()` are documented Unix-only and unusable on the
    `windows-latest` lane, 255 matches ext4/APFS byte limits and is safely under NTFS's
    255-UTF-16-unit limit, and no CI probe is dispatched to measure it.

    Add a module-level helper
    `_bound_relocation_component(digest: str, raw_basename: str, limit: int = MAX_PATH_COMPONENT_BYTES) -> str`
    implementing D-06 and D-07, following `59-RESEARCH.md`'s verified sketch. It computes
    `prefix = f"{digest}-"`, subtracts the prefix's UTF-8 byte length from `limit` to get the
    basename budget (246 for an 8-hex digest), splits the basename with `posixpath.splitext`, and
    applies the precedence order: the digest and its hyphen survive whole first (truncating them
    would reintroduce the collision IMG-03 closed); then at least one byte of stem; then the
    extension. If the extension alone would consume the whole remaining budget, truncate the
    extension to at most `budget - 1` bytes rather than squeezing the stem to nothing. Size-check in
    bytes but land the cut on a UTF-8 character boundary by encoding, slicing, then walking the byte
    slice back one byte at a time until `.decode("utf-8")` succeeds — never slice the `str` and hope,
    and never return a byte slice that splits a multi-byte character. Give it the house docstring
    shape with an `Examples:` block recording the two measured cases (250 ASCII characters → 255
    bytes; 100 CJK characters → 253 bytes).

    Add a module-level helper `_build_relocation_key(resolved_uri: str) -> str` that returns the full
    escape-branch key. It computes `digest = hashlib.sha1(resolved_uri.encode("utf-8")).hexdigest()[:8]`
    over the RAW, un-normalized `resolved_uri` — this line's argument expression must not change,
    per RESEARCH Pitfall 2, because normalizing the digest input silently changes the collision-anchor
    formula for exactly the Windows shape this phase newly exercises. Separately, and only for the
    basename half (IMG-04), bind a distinctly-named local such as `basename_source =
    resolved_uri.replace("\\", "/")` and take `posixpath.basename(basename_source)` — `posixpath`,
    never `path.basename`, because on a POSIX build host `path` is `posixpath` and does not split on
    a backslash, which is the exact defect IMG-04 fixes. Return
    `f"{RESERVED_IMAGE_NAMESPACE}/{_bound_relocation_component(digest, normalized_basename)}"`. Carry
    the existing IMG-03 rationale comment (digest over the whole URI, `.encode()` first, no
    dependence on `self.images` or write order, non-cryptographic collision anchor) onto the new
    helper's docstring so it is not lost from the file.

    In `_track_image()`'s escape branch, replace the inline `digest = ...` / `key = (...)` pair with a
    single `key = _build_relocation_key(resolved_uri)` call, keeping the surrounding D-05/D-06
    explanatory comment. Leave the `logger.warning` f-string byte-identical — its `!r` quoting is
    MSG-03's site in Phase 60 and changing it here is the collision ROADMAP constraint 4 forbids.
    Leave both non-escape branches (`key = f"{RESERVED_IMAGE_NAMESPACE}/{rel_uri}"` and
    `key = rel_uri`) untouched. Add no drive-prefix stripping — D-12 is deferred with its measurement.
  </action>
  <verify>
    <automated>uv run pytest tests/test_track_image_key_construction.py tests/test_copy_image_files_name_too_long.py -q &amp;&amp; uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_track_image_key_construction.py -q` exits 0 with 0 skipped
    - `uv run pytest tests/test_copy_image_files_name_too_long.py -q` exits 0 with 0 skipped on this dev machine
    - `uv run pytest -q` exits 0 (full suite green, including the pre-existing IMG-03 collision tests in `tests/test_builder.py`)
    - `grep -c 'MAX_PATH_COMPONENT_BYTES' typsphinx/builder.py` is at least 2 (definition plus default argument)
    - `grep -c 'def _bound_relocation_component' typsphinx/builder.py` is 1 and `grep -c 'def _build_relocation_key' typsphinx/builder.py` is 1
    - `grep -c 'hashlib.sha1(resolved_uri.encode("utf-8")).hexdigest()\[:8\]' typsphinx/builder.py` is 1 (the digest input expression is unchanged and appears exactly once)
    - `uv run python -c "from typsphinx.builder import _build_relocation_key as k; r=k(chr(92).join(['C:','Users','sub','we'])+'.png'); print(chr(92) in r, r)"` prints `False` first
    - `uv run black --check .` exits 0 and `uv run mypy typsphinx/` exits 0
    - `git diff -- tests/` shows no modification to any file that existed before this phase
  </acceptance_criteria>
  <done>Both helpers exist, `_track_image()`'s escape branch calls `_build_relocation_key()`, the three RED gates from task 1 are green, and the full suite, black and mypy are clean.</done>
  <reversibility rating="reversible">Two new module-level helpers plus a two-line call-site substitution; revertible by restoring the inline construction.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Pure-string property gates for the bound, the collision re-proof, and the GREEN evidence</name>
  <files>tests/test_track_image_key_construction.py, .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</files>
  <read_first>
    - typsphinx/builder.py (the `_bound_relocation_component` and `_build_relocation_key` helpers written in task 2)
    - tests/test_track_image_key_construction.py (the two behavioural gates written in task 1)
    - .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-CONTEXT.md § D-07, D-08(a)
    - tests/test_path_naming_predicate.py (house `@pytest.mark.parametrize` convention)
  </read_first>
  <action>
    Extend `TestRelocationKeyLengthBound` in `tests/test_track_image_key_construction.py` with D-08(a)'s
    pure-string gates — direct calls to `_bound_relocation_component` and `_build_relocation_key`, no
    `TypstBuilder`, no `temp_sphinx_app`, no filesystem, so they run on every CI lane including
    `windows-latest`. Every test name contains `length_bound` so the
    `-k length_bound` selector in `59-VALIDATION.md` picks them up. Cover exactly these properties,
    each as its own named test:

    boundary — a 246-byte basename yields a component of exactly 255 bytes returned untruncated; a
    247-byte basename yields at most 255 bytes; a 245-byte basename yields exactly 254 bytes
    unchanged. encoding — a basename of 100 three-byte CJK characters plus `.png` yields a component
    whose `encode("utf-8").decode("utf-8")` round-trips without raising, is at most 255 bytes, and
    still ends `.png`; state in the docstring that the contract is byte-validity, not grapheme-cluster
    integrity, so a grapheme cluster may be split. empty — an empty basename yields exactly the
    9-byte `{digest}-` prefix and raises nothing. precision — the basename budget equals
    `255 - len(f"{digest}-".encode("utf-8"))` computed in the test from the same expression, and an
    extension longer than the whole budget is itself truncated while at least one stem byte survives.
    anchor — every truncated component still starts with `f"{digest}-"` and, when an extension
    survives at all, still ends with it.

    Add the SC#3 collision re-proof as `test_length_bound_two_long_uris_sharing_a_basename_stay_distinct`:
    two distinct absolute URIs that differ only in a directory component and share a 250-character
    basename must produce two DISTINCT `_build_relocation_key()` results — computing the expected
    digests in the test from the same `hashlib.sha1(...)` construction the product uses, never a
    hardcoded hex literal (the house rule from `tests/test_builder.py`).

    Then append the GREEN half of the evidence: re-run
    `uv run pytest tests/test_track_image_key_construction.py tests/test_copy_image_files_name_too_long.py -q`
    and paste the verbatim passing summary into `59-WINDOWS-URI-EVIDENCE.md` under
    `## IMG-04 / IMG-06` → `### GREEN (post-fix)`, immediately below the RED transcript, and record
    the measured post-fix key for the Windows-shaped URI and the post-fix component byte length for
    the 250-character basename so the before/after pair is readable side by side.
  </action>
  <verify>
    <automated>uv run pytest tests/test_track_image_key_construction.py -k length_bound -q &amp;&amp; uv run pytest -q &amp;&amp; grep -q 'GREEN (post-fix)' .planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-WINDOWS-URI-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_track_image_key_construction.py -k length_bound -q` exits 0 with at least 7 passed and 0 skipped
    - `grep -c 'TypstBuilder' tests/test_track_image_key_construction.py` counts only the occurrences inside `TestRelocationKeyNoBackslash` and `test_relocation_key_length_bound_through_track_image`; the pure-string tests added by this task construct no builder
    - `uv run pytest -q` exits 0
    - `59-WINDOWS-URI-EVIDENCE.md` § "IMG-04 / IMG-06" contains both a `### RED (pre-fix)` and a `### GREEN (post-fix)` transcript, and records the pre-fix and post-fix component byte lengths for the 250-character basename
    - `uv run black --check .` exits 0
  </acceptance_criteria>
  <done>The pure-string property gates and the collision re-proof pass on all lanes, and the evidence file carries the RED and GREEN transcripts for both requirements.</done>
  <reversibility rating="reversible">Test-only additions plus an evidence append.</reversibility>
</task>

</tasks>

<flagged_assumptions>
**IMG-04 / `unclassified` — UNRESOLVED, surfaced not dropped.** The deterministic edge probe returned
`{"requirement_id":"IMG-04","category":"unclassified","status":"unresolved","probe":"unclassified —
review manually"}`. Reviewed manually: the nearest un-covered edge in IMG-04's neighbourhood is the
**drive-relative colon shape** — `posixpath.basename("C:logo.png".replace("\\", "/"))` returns
`C:logo.png`, so the colon survives normalization and a `_typst_converted/{digest}-C:logo.png`
destination is an illegal NTFS filename. **No criterion is minted for it in this plan**: D-12
deferred it explicitly, with the measurement that the shape arises only for the drive-*relative* form
(a drive letter with no following separator), which no Sphinx image post-transform produces. Minting
a criterion here would widen IMG-04 past its literal "backslash" scope, which `59-CONTEXT.md`
forbids. It is recorded in `59-CONTEXT.md` § Deferred Ideas as a candidate requirement of its own.
This row stays `unresolved` and is not auto-resolved with a backstop.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party post-transform `node["uri"]` → `_track_image()` key construction | an extension-authored, arbitrarily-shaped and arbitrarily-long absolute path string crosses into builder-owned state |
| relocation key → `copy_image_files()` `dest = path.join(self.outdir, imguri)` | the key becomes a real filesystem write path inside `outdir` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-02 | Denial of Service | `typsphinx/builder.py::copy_image_files` swallowed `ENAMETOOLONG` (ASVS V12 File and Resources) | medium | mitigate | Bound the relocation key's final path component to `MAX_PATH_COMPONENT_BYTES` (255) in `_bound_relocation_component()` so the write can never exceed the limit, instead of relying on the `except Exception` catch-and-log that today silently drops the image. The pre-fix behaviour — a swallowed exception plus a missing file — is the more dangerous shape. |
| T-59-03 | Tampering | `typsphinx/builder.py::_build_relocation_key` (ASVS V5 Input Validation) | medium | mitigate | Normalize backslashes before taking the basename with `posixpath.basename()`, so no separator from a Windows-shaped URI survives into a value that is later interpolated into a Typst string literal. |
| T-59-04 | Spoofing | relocation-key collision between two distinct escaping URIs sharing a basename (ASVS V5) | low | mitigate | The `{sha1[:8]}-` anchor is kept whole under truncation by D-07's precedence order, and the property is re-proven by `test_length_bound_two_long_uris_sharing_a_basename_stay_distinct`. |
| T-59-SC | Tampering | package-manager installs (npm/pip/cargo) | low | accept | No package-manager install task exists in this plan; `59-RESEARCH.md` § "Package Legitimacy Audit" records the phase as stdlib-only with zero new packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `59-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. Newly created by
THIS plan: `typsphinx/builder.py`'s `MAX_PATH_COMPONENT_BYTES`, `_bound_relocation_component()` and
`_build_relocation_key()`; `tests/test_track_image_key_construction.py` with
`TestRelocationKeyNoBackslash` and `TestRelocationKeyLengthBound`; and
`tests/test_copy_image_files_name_too_long.py` with `TestCopyImageFilesNameTooLong`.
</artifacts_this_phase_produces>

<verification>
- `uv run pytest tests/test_track_image_key_construction.py tests/test_copy_image_files_name_too_long.py -q` — green, 0 skipped on this dev machine
- `uv run pytest -q` — full suite green, including the pre-existing IMG-03 collision tests
- `uv run black --check .` and `uv run mypy typsphinx/` — clean
- `git diff --stat <PHASE_BASE_SHA>..HEAD -- tests/` shows added files only
- `59-WINDOWS-URI-EVIDENCE.md` § "IMG-04 / IMG-06" carries a RED transcript quoting the verbatim `Failed to copy image ...: [Errno 36] File name too long` line and the pre-fix backslash-bearing key, plus the matching GREEN transcript
</verification>

<success_criteria>
1. For a Windows-shaped absolute URI, `node["uri"]` after `post_process_images()` contains no
   backslash and starts with the reserved namespace.
2. The relocation key's final path component is at most 255 UTF-8 bytes with the `{sha1[:8]}-`
   anchor whole, the extension preserved, the stem never empty, and the cut on a UTF-8 boundary.
3. Two long URIs sharing a basename still produce distinct keys.
4. `copy_image_files()` logs no `Failed to copy image` warning for the long-basename fixture and the
   destination file exists, with the pre-fix ENAMETOOLONG warning quoted verbatim in the evidence.
5. Full suite, `black --check` and `mypy typsphinx/` green, zero existing test assertions modified.
</success_criteria>

<output>
Create `.planning/phases/59-path-shape-predicate-and-image-uri-correctness/59-02-SUMMARY.md` when done.
</output>
