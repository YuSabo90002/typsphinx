# Phase 08.1: admonition rendering fix — translator markup/code-mode mismatch - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-11
**Phase:** 08.1-admonition-rendering-fix-translator-markup-code-mode-mismatch
**Areas discussed:** Title inline markup, Regression-proofing tests, Coverage/scope

---

## Gray-area selection

| Option | Description | Selected |
|--------|-------------|----------|
| 修正アプローチ (Fix approach) | content-block `info({...})` vs bracket+`#` `info[#...]` | |
| タイトルのマークアップ (Title markup) | flatten plain-string vs preserve markup | ✓ |
| リグレッション防止テスト (Regression tests) | strengthen asserts + real compile gate vs loose | ✓ |
| 対応する admonition 範囲 (Coverage) | nested content + missing types | ✓ |

**User's choice:** Discussed the latter 3; delegated the fix approach to Claude.

---

## Title inline markup

| Option | Description | Selected |
|--------|-------------|----------|
| プレーン文字列維持 (recommended) | Keep `title: "{node.astext()}"`, flatten; defer rich titles | |
| マークアップを保持 | Build title in content-mode (`title: [...]`), preserve emphasis/code | ✓ |

**User's choice:** Preserve markup.
**Notes:** Requires walking the title node's children through inline visitors instead of
`.astext()`. Composition with the code-mode body invocation to be verified against the
gentle-clues API (D-02).

---

## Regression-proofing tests

| Option | Description | Selected |
|--------|-------------|----------|
| ユニット強化＋実コンパイル (recommended) | Strengthen asserts to verify code-mode structure + `tox -e docs-pdf` exit-0 + PDF text extraction shows no literal source | ✓ |
| ユニット強化のみ | Structure asserts only; real compile left to CI/docs.yml | |

**User's choice:** Unit strengthening + real compile gate.
**Notes:** The existing loose asserts (`"info[" in output`) are how the bug slipped through
— the new gate must catch literal-source regressions in a real render.

---

## Coverage / scope

| Option | Description | Selected |
|--------|-------------|----------|
| ネストcontent全保証＋未実装型defer (recommended) | Guarantee nested content; defer missing types to another phase | |
| 段落のみ保証＋未実装型defer | Minimal: paragraphs only | |
| ネスト全保証＋未実装型も追加 | Guarantee nested content AND add missing admonition types | ✓ |

**User's choice:** Nested-content guarantee + add missing types (scope expansion).
**Notes:** Consciously chose to widen scope beyond a pure bug fix. Adds generic
`.. admonition::` plus `hint`/`error`/`danger`/`attention`. Acceptable because this is an
inserted urgent phase whose ROADMAP goal was TBD — the discussion defines its scope.

---

## Claude's Discretion

- **Fix approach (D-01):** unselected by the user → recommended unified code-mode content-block
  form; exact gentle-clues invocation syntax left to research/planning.
- **New-type clue mapping (D-06):** gentle-clues analogs for the added types left to planning.

## Deferred Ideas

- Configurable `@preview` package versions (FWD-03) — milestone-level out-of-scope tech debt.
- Broader translator markup/code-mode audit of non-admonition nodes — note candidates only,
  do not expand this phase.
