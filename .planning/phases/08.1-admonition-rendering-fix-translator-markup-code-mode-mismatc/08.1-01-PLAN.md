---
phase: 08.1-admonition-rendering-fix-translator-markup-code-mode-mismatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "pypdf is importable in the dev environment (enables the D-04 PDF-text-extraction gate)"
    - "The pypdf [SUS] legitimacy flag was human-confirmed as a false positive before install (supply-chain gate)"
  artifacts:
    - "pyproject.toml [project.optional-dependencies].dev contains a pinned pypdf entry"
    - "uv.lock regenerated with pypdf resolved"
  key_links:
    - "pypdf install must clear the blocking-human legitimacy checkpoint before the dependency is added — Plan 04's PDF gate imports pypdf and cannot proceed until this lands"
---

<objective>
Add `pypdf` as a dev-only dependency so the D-04 real-compile acceptance gate (Plan 04)
can text-extract the compiled PDF and assert no literal Typst source leaks into rendered
admonition prose. `pypdf` is flagged `[SUS]` ("too-new" / "unknown-downloads") by the
package-legitimacy seam — a documented false positive (long-established py-pdf project,
formerly PyPDF2) — so its install is gated behind a blocking-human legitimacy checkpoint per
the Package Legitimacy Gate protocol.

This plan is front-loaded to Wave 1 so the supply-chain checkpoint is cleared early rather
than blocking the phase at its final gate.

Purpose: Provide the only new (dev-only) dependency the phase needs; clear its SUS gate up
front. Supports D-04.
Output: `pypdf` pinned in the `dev` extras, `uv.lock` regenerated, `import pypdf` succeeds.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-RESEARCH.md
@.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-CONTEXT.md
</context>

## Artifacts This Phase Produces (phase-wide inventory)

- `typsphinx/translator.py`: rewritten `_visit_admonition` / `_depart_admonition` (code-mode
  content-block form); admonition-aware branch in `visit_title` / `depart_title`; NEW
  `visit_hint`/`depart_hint`, `visit_error`/`depart_error`, `visit_danger`/`depart_danger`,
  `visit_attention`/`depart_attention`, `visit_admonition`/`depart_admonition` (generic).
- `tests/test_admonitions.py`: strengthened structural asserts + new nested-content, inline-
  markup-title, and per-type tests.
- `tests/test_pdf_render_gate.py`: NEW real-compile + pypdf text-extraction gate.
- `tests/fixtures/admonition_render_gate/`: NEW minimal Sphinx fixture project for the gate.
- `pyproject.toml`: NEW `pypdf` dev dependency (this plan).

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify pypdf legitimacy (SUS false-positive gate)</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-RESEARCH.md — "## Package Legitimacy Audit" (pypdf verdict SUS: too-new / unknown-downloads) and "Standard Stack → Supporting" (pypdf 6.14.2 verified working)
  </read_first>
  <action>
    Pause for a blocking-human legitimacy confirmation before Task 2 installs pypdf. Present
    the package-legitimacy seam verdict (SUS: too-new / unknown-downloads) and the researcher's
    false-positive assessment, then wait for explicit human approval. Do not install anything
    in this task; this gate is not auto-approvable.
  </action>
  <what-built>
    Nothing installed yet. This gate confirms `pypdf` is legitimate before the next task adds
    it to `pyproject.toml` and runs `uv sync`. The package-legitimacy seam returned
    `{"verdict":"SUS","reasons":["too-new","unknown-downloads"]}` — the researcher assessed
    this as a false positive (the "too-new" signal reflects the latest release date, not the
    package's origin).
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/pypdf/ — confirm it is the long-established pure-Python
       PDF library maintained by the `py-pdf` GitHub organization (formerly `PyPDF2`, merged
       2022), with a regular release history.
    2. Confirm the source repo is https://github.com/py-pdf/pypdf (active, many contributors).
    3. Confirm this matches the intended library (PDF text extraction for the D-04 gate), not
       a typosquat.
  </how-to-verify>
  <acceptance_criteria>
    - Human explicitly approves the `pypdf` install, OR requests the documented fallback
      (`pdfplumber`, see RESEARCH "Alternatives Considered").
    - This checkpoint is NOT auto-approvable — `workflow.auto_advance` is ignored for
      legitimacy gates.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install pypdf, or "use pdfplumber" / describe concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add pypdf to dev extras and regenerate the lockfile</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml:33-46 — the existing `[project.optional-dependencies].dev` list (the exact
      insertion point and pin-style analog: every entry uses a bounded range like
      `"ruff>=0.15,<0.16"`)
    - CLAUDE.md — dev workflow uses `uv` for env/dependency management
  </read_first>
  <action>
    Append a bounded pypdf pin `"pypdf>=6.14,<7"` to the `dev` list in
    `[project.optional-dependencies]` (matching the sibling entries' bounded-range style;
    RESEARCH verified 6.14.2). Then regenerate the environment/lockfile with
    `uv add --dev pypdf` (which updates both `pyproject.toml` and `uv.lock`) OR, if editing
    `pyproject.toml` directly, run `uv sync --extra dev` to resolve and write `uv.lock`.
    Do NOT touch any runtime `dependencies` entry — pypdf is dev-only (per D-04, used solely
    by the new acceptance test). Do NOT change any `@preview` package version anywhere (the
    3-way version-sync hazard is out of scope for this phase).
  </action>
  <verify>
    <automated>uv run python -c "import pypdf; print(pypdf.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[project.optional-dependencies].dev` contains a `pypdf` entry with a
      bounded pin (`pypdf>=6.14,<7`).
    - `uv run python -c "import pypdf; print(pypdf.__version__)"` prints a 6.14+ version and
      exits 0.
    - `uv.lock` contains a resolved `pypdf` entry (`grep -c '^name = "pypdf"' uv.lock` >= 1).
    - No runtime `[project].dependencies` entry changed; no `@preview` version changed.
  </acceptance_criteria>
  <done>pypdf is a resolved dev dependency and imports successfully in the dev environment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → dev environment | A third-party package is fetched and installed into the build/test toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-SC | Tampering | `uv add --dev pypdf` / pip install | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) confirms pypdf's py-pdf provenance before install; SUS verdict kept and human-gated, not auto-overridden |
</threat_model>

<verification>
- `uv run python -c "import pypdf"` exits 0.
- `git diff pyproject.toml` shows only the added `pypdf` dev entry (no runtime dep or
  `@preview` version changes).
</verification>

<success_criteria>
pypdf is pinned in the dev extras, resolved in uv.lock, importable, and its SUS legitimacy
flag was human-confirmed before install.
</success_criteria>

<output>
Create `.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-01-SUMMARY.md` when done
</output>
