---
phase: 08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc
verified: 2026-07-11T04:54:41Z
status: passed
score: 6/6 must-haves verified (D-01..D-06)
behavior_unverified: 0
overrides_applied: 0
---

# Phase 08.1: Admonition Rendering Fix Verification Report

**Phase Goal:** Fix the admonition markup/code-mode mismatch in `typsphinx/translator.py` so
admonitions render typeset prose in the compiled PDF instead of literal, unevaluated Typst
source (`par({text("...")})`). Authoritative scope: CONTEXT.md decisions D-01..D-06.

**Verified:** 2026-07-11T04:54:41Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (CONTEXT.md D-01..D-06)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 (D-01) | Admonition body emits code-mode content-block form (`clue_type({ ... })`), NOT markup-mode (`clue_type[`) | ✓ VERIFIED | `typsphinx/translator.py:2292-2345` (`_visit_admonition`/`_depart_admonition`) emits `f"{clue_type}({{"` ... `"}"`. Ran `pytest tests/test_admonitions.py -v` myself: all 17 tests pass, asserting `"info({" in output` / `"info[" not in output` etc. for every type. |
| 2 (D-02) | Admonition titles preserve inline markup via buffer-swap in `visit_title`/`depart_title` (no `.astext()` flattening, no double-emission) | ✓ VERIFIED | `translator.py:190-238` — `visit_title` branches on `isinstance(node.parent, nodes.Admonition)`, buffer-swaps `self.body`, restores it in `depart_title`, captures `self._pending_admonition_title`. No `.astext()` call in this path. Ran `test_admonition_title_preserves_inline_markup` myself — passes: asserts `emph({` present in title, `heading(` absent (double-emission fixed), title text count == 1. |
| 3 (D-03) | Existing 9 admonition unit tests strengthened to structural asserts (code-mode structure, not loose substrings) | ✓ VERIFIED | Read `tests/test_admonitions.py` — every original test (`test_note_converts_to_info`, `test_warning_converts_to_warning`, `test_tip_converts_to_tip`, `test_important_...`, `test_caution_...`, `test_seealso_...`, `test_admonition_with_multiple_paragraphs`, `test_nested_admonitions`, `test_admonition_with_title_in_content`) now asserts `"X({" in output` AND `"X[" not in output` AND exact `par({text("..."))})`  body forms — not loose substrings. Ran the suite myself: 17/17 pass. |
| 4 (D-04) | Real `tox -e docs-pdf` compile exits 0 AND pypdf text-extraction from the compiled PDF confirms no literal Typst source leak | ✓ VERIFIED | Ran `uv run tox -e docs-pdf` myself: `docs-pdf: OK`, all 13 real docs compiled, PDF generated at `docs/_build/pdf/index.pdf`. Ran `tests/test_pdf_render_gate.py` myself: 1 passed, genuinely executed (not skipped — confirmed `typst` 0.15.0 and `pypdf` 6.14.2 both import successfully). Additionally, I independently extracted text from the real compiled `docs/_build/pdf/index.pdf` and confirmed the golden `typst_template_assets` note now renders as typeset prose ("Info / This setting only applies to local custom templates (typst_template)...") — not literal source. One `par({` substring was found elsewhere in the real-docs PDF, traced to the auto-generated API reference rendering `depart_paragraph`'s own docstring text ("Closes par({}) function...") — a legitimate prose description of Typst syntax, not a rendering defect (matches the documented caveat in 08.1-04-SUMMARY.md). |
| 5 (D-05) | Nested content (multiple paragraphs, bullet lists, code/literal blocks, nested admonitions) inside an admonition body all evaluate correctly | ✓ VERIFIED | `test_nested_list_in_note`, `test_nested_code_block_in_note`, `test_nested_admonitions`, `test_admonition_with_multiple_paragraphs` all present in `tests/test_admonitions.py` with structural asserts; all pass when I ran them. The render-gate fixture (`tests/fixtures/admonition_render_gate/index.rst`) also exercises a note-with-bullet-list, warning-with-code-block, and nested note/warning — compiled successfully via the real `tox -e docs-pdf` run. |
| 6 (D-06) | Five new admonition types implemented and routed through the fixed choke point: hint→tip, error→error, danger→danger, attention→warning, generic `.. admonition::`→base `clue()` | ✓ VERIFIED | `translator.py:2399-2454` — `visit_hint`→`_visit_admonition(node, "tip")`, `visit_error`→`"error"`, `visit_danger`→`"danger"`, `visit_attention`→`"warning"`, `visit_admonition`(generic)→`"clue"` (no `custom_title`, title flows through the D-02 buffer-swap). All 5 corresponding tests (`test_hint_converts_to_tip`, `test_error_converts_to_error`, `test_danger_converts_to_danger`, `test_attention_converts_to_warning`, `test_generic_admonition_converts_to_clue`) pass when I ran the suite; the generic test correctly uses a real `nodes.admonition()` + `nodes.title()` shape. |

**Score:** 6/6 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `typsphinx/translator.py` | Rewritten `_visit_admonition`/`_depart_admonition` (code-mode), admonition-aware `visit_title`/`depart_title`, 5 new type handlers | ✓ VERIFIED | Read in full; matches plan exactly. State fields (`_pending_admonition_title`, `_in_admonition_title`, `_saved_body_for_admonition_title`, `_custom_admonition_title`) initialized in `__init__` (lines 94-104). |
| `tests/test_admonitions.py` | Strengthened structural asserts + nested-content + inline-markup-title + per-type tests | ✓ VERIFIED | 17 tests total, all read and all pass (ran `pytest tests/test_admonitions.py -v`: 17 passed). |
| `tests/test_pdf_render_gate.py` | D-04 real-compile + pypdf text-extraction gate, not weakened/skipped | ✓ VERIFIED | Read in full. `LEAK_SIGNATURES = ("par({", 'text("', 'raw("')`. Skip guard is genuinely conditional on `TYPST_AVAILABLE and PYPDF_AVAILABLE`, both true in this environment — confirmed the test actually ran (not skipped) via `pytest -v` output. |
| `tests/fixtures/admonition_render_gate/` | Minimal Sphinx fixture with golden note + nested/multi-type admonitions | ✓ VERIFIED | `conf.py` sets `typst_documents` (master doc, gets full template + gentle-clues import); `index.rst` contains the golden `typst_template_assets` note, note+list, warning+code-block, hint, danger, and nested note/warning. |
| `pyproject.toml` | `pypdf` in dev extras only, no runtime dep or `@preview` version changed | ✓ VERIFIED | `pypdf>=6.14,<7` present only in `[project.optional-dependencies].dev`. `@preview` versions (`codly:1.3.0`, `codly-languages:0.1.10`, `mitex:0.2.7`, `gentle-clues:1.3.1`) unchanged and in sync across `writer.py`/`template_engine.py`/`templates/base.typ` — ran `tests/test_preview_version_sync.py` myself: 2/2 pass. |
| `uv.lock` | Regenerated with pypdf resolved | ✓ VERIFIED | `grep -c '^name = "pypdf"' uv.lock` → 1. `uv run python -c "import pypdf; print(pypdf.__version__)"` → `6.14.2`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `visit_title` admonition branch | `_depart_admonition` | `self._pending_admonition_title` buffer-swap | ✓ WIRED | Confirmed by reading both methods and by the passing `test_admonition_title_preserves_inline_markup` / `test_generic_admonition_converts_to_clue` tests (title emitted exactly once, wrapped in `{ ... }` code block via `"{" + self._pending_admonition_title + "}"`). |
| 6 existing `depart_*` admonition methods | `_depart_admonition()` | no-arg call signature preserved | ✓ WIRED | All 11 `depart_*` methods (note/warning/tip/important/caution/seealso/hint/error/danger/attention/admonition) call `self._depart_admonition()` with no arguments — zero call-site changes needed, confirmed by reading. |
| 5 new `visit_*` handlers (Plan 03) | fixed `_visit_admonition` (Plan 02) | direct method call | ✓ WIRED | `visit_hint`/`visit_error`/`visit_danger`/`visit_attention`/`visit_admonition` all call `self._visit_admonition(node, "<clue_type>")` — same choke point as Plan 02's fix. |
| `tests/test_pdf_render_gate.py` | `pypdf` (Plan 01 dependency) | `import pypdf` | ✓ WIRED | Import succeeds; `PYPDF_AVAILABLE=True` confirmed at test-run time (test not skipped). |
| render-gate fixture `index.rst` | master-document template + gentle-clues import | `conf.py`'s `typst_documents` config | ✓ WIRED | Confirmed via real `sphinx-build`/`tox -e docs-pdf` run: emitted `.typ` and compiled PDF contain the `info({`/`tip({`/`danger({` code-mode forms, proving the gentle-clues import was present. |

### Behavioral Spot-Checks (executed directly by this verifier, not sourced from SUMMARY)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Admonition unit suite structural asserts | `uv run pytest tests/test_admonitions.py -v` | 17 passed | ✓ PASS |
| D-04 PDF render gate (not skipped) | `uv run pytest tests/test_pdf_render_gate.py -v` | 1 passed (typst 0.15.0, pypdf 6.14.2 both available) | ✓ PASS |
| `@preview` version sync unaffected | `uv run pytest tests/test_preview_version_sync.py -v` | 2 passed | ✓ PASS |
| Full test suite | `uv run pytest -q` | 411 passed | ✓ PASS |
| Real docs compile to PDF | `uv run tox -e docs-pdf` | `docs-pdf: OK`, `build succeeded, 2 warnings` (pre-existing unrelated ref warnings), PDF generated | ✓ PASS |
| Real-docs golden note renders as prose (independent check beyond the gate) | `pypdf.PdfReader('docs/_build/pdf/index.pdf').extract_text()` search for the golden note text | Found rendered as: "Info / This setting only applies to local custom templates (typst_template)..." — no literal source | ✓ PASS |
| Format/lint/types | `uv run black --check .`; `nix-shell -p ruff --run "ruff check ."`; `uv run mypy typsphinx/` | All clean (black: 52 files unchanged; ruff: all checks passed; mypy: no issues in 6 source files) | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared or found for this phase — N/A.

### Requirements Coverage

No REQ-IDs are mapped to this phase (confirmed: `grep "08.1" .planning/REQUIREMENTS.md` returns nothing). Scope is fully defined by CONTEXT.md decisions D-01..D-06, all independently verified above. No orphaned requirements found.

### Anti-Patterns Found

None. Scanned `typsphinx/translator.py`, `tests/test_admonitions.py`, `tests/test_pdf_render_gate.py`, `tests/fixtures/admonition_render_gate/conf.py`, `tests/fixtures/admonition_render_gate/index.rst` for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` — zero matches. No stub patterns (`return null`, empty handlers, hardcoded empty data flowing to output) found in the admonition code path.

### Human Verification Required

None. D-04's acceptance criterion (pypdf text-extraction confirming no literal-source leak) is itself the automated proxy for "renders as typeset prose" and was independently re-verified by this agent against both the fixture and the real project docs — no additional human visual PDF review is required to close this phase's must-haves.

### Gaps Summary

No gaps. All six CONTEXT.md decisions (D-01 code-mode body, D-02 inline-markup titles + double-emission fix, D-03 strengthened structural asserts, D-04 real-compile + PDF-text-extraction gate, D-05 nested-content coverage, D-06 five new admonition types) are implemented in `typsphinx/translator.py` and independently confirmed by this verifier running the actual test suite, the real `tox -e docs-pdf` compile, and an additional manual pypdf extraction against the real compiled project docs — not by trusting SUMMARY.md claims. The one `par({` substring found during independent spot-checking of the real docs PDF was traced to a docstring describing Typst syntax in prose (in the auto-generated API reference), not a code-mode leak, matching the caveat already documented in 08.1-04-SUMMARY.md.

**Note (informational, not a gap):** Plan 01's blocking-human legitimacy checkpoint for `pypdf` was recorded in 08.1-01-SUMMARY.md as "pre-approved... per explicit instruction from the orchestrator/user" prior to this execution session, rather than re-prompted during the session itself. This is a self-attested claim about a prior human interaction that this verifier cannot independently confirm from the codebase alone. It does not affect the D-01..D-06 phase-goal verification (pypdf's legitimacy is orthogonal to whether the admonition rendering fix works), so it is not treated as a gap, but is noted here for completeness.

---

_Verified: 2026-07-11T04:54:41Z_
_Verifier: Claude (gsd-verifier)_
