---
phase: 08.1-admonition-rendering-fix-translator-markup-code-mode-mismatch
plan: 03
type: execute
wave: 2
depends_on:
  - 08.1-02
files_modified:
  - typsphinx/translator.py
  - tests/test_admonitions.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "D-06: The five currently-missing admonition types are implemented and route through the fixed code-mode _visit_admonition — hint→tip, error→error, danger→danger, attention→warning, and generic .. admonition:: → the base clue() function"
    - "D-06: The generic .. admonition:: uses its directive-supplied title (a real title child) via the same buffer-swap title path, so its inline-markup title is preserved"
  artifacts:
    - "typsphinx/translator.py: new visit_hint/depart_hint, visit_error/depart_error, visit_danger/depart_danger, visit_attention/depart_attention, visit_admonition/depart_admonition"
    - "tests/test_admonitions.py: new per-type conversion tests for hint/error/danger/attention and a nodes.admonition-based generic test"
  key_links:
    - "The five new visit_* handlers MUST call the fixed _visit_admonition from Plan 02 (code-mode form) — this plan is Wave 2 precisely because it depends on that fix"
    - "hint and attention have no dedicated gentle-clues clue; they map to existing predefined clues (tip, warning) — calling a non-existent clue would KeyError at compile"
---

<objective>
Implement the five currently-unimplemented admonition types (D-06), each as a two-method
`visit_*`/`depart_*` pair cloned from the existing `visit_note`/`depart_note` shape and
routed through the fixed `_visit_admonition` from Plan 02. Apply the research-verified
gentle-clues 1.3.1 clue mapping: `hint`→`tip`, `error`→`error`, `danger`→`danger`,
`attention`→`warning`, and the generic `.. admonition::`→the base `clue` function (which
carries the directive's own title). Add per-type unit tests, using `nodes.admonition` (the
real docutils shape with a title child) for the generic case.

Purpose: Deliver the user's chosen scope-expansion (D-06) so all standard Sphinx admonition
types render, reusing the single fixed choke point.
Output: All ten admonition types convert to the correct gentle-clues call; new per-type tests
green.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-RESEARCH.md
@.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-PATTERNS.md
@.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-02-SUMMARY.md
</context>

## Artifacts This Phase Produces (this plan)

- `typsphinx/translator.py`: `visit_hint`/`depart_hint`, `visit_error`/`depart_error`,
  `visit_danger`/`depart_danger`, `visit_attention`/`depart_attention`,
  `visit_admonition`/`depart_admonition` (generic).
- `tests/test_admonitions.py`: `test_hint_converts_to_tip`, `test_error_converts_to_error`,
  `test_danger_converts_to_danger`, `test_attention_converts_to_warning`, and a
  `nodes.admonition`-based generic test (e.g. `test_generic_admonition_converts_to_clue`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hint/error/danger/attention handlers + tests (D-06)</name>
  <files>typsphinx/translator.py, tests/test_admonitions.py</files>
  <read_first>
    - typsphinx/translator.py:2294-2340 — the existing `visit_note`/`depart_note` …
      `visit_seealso`/`depart_seealso` block (the exact two-line pattern to clone per type)
    - typsphinx/translator.py:2253-2292 — the fixed `_visit_admonition`/`_depart_admonition`
      from Plan 02 (confirm the signature the new handlers call)
    - 08.1-RESEARCH.md — "Code Examples → D-06 predefined-clue-type mapping" table (verified
      against the cached gentle-clues 1.3.1 `predefined.typ`/`theme.typ`: `error` and `danger`
      exist as direct clues; `hint` and `attention` do NOT exist and must map to `tip` /
      `warning`)
    - tests/test_admonitions.py — the existing per-type tests (e.g. `test_tip_converts_to_tip`)
      to clone for structural asserts
  </read_first>
  <behavior>
    - `nodes.hint` → `_visit_admonition(node, "tip")` → output contains `tip({`.
    - `nodes.error` → `_visit_admonition(node, "error")` → output contains `error({`.
    - `nodes.danger` → `_visit_admonition(node, "danger")` → output contains `danger({`.
    - `nodes.attention` → `_visit_admonition(node, "warning")` → output contains `warning({`.
    - Each body paragraph evaluates inside the code-mode block; no markup-bracket form remains.
  </behavior>
  <action>
    Add four new two-method handler pairs in the admonition section of
    `typsphinx/translator.py`, each cloning the `visit_note`/`depart_note` shape: `visit_hint`
    → `self._visit_admonition(node, "tip")`; `visit_error` → `self._visit_admonition(node,
    "error")`; `visit_danger` → `self._visit_admonition(node, "danger")`; `visit_attention` →
    `self._visit_admonition(node, "warning")`. Each corresponding `depart_*` calls
    `self._depart_admonition()`. Use the research-verified mapping exactly — `hint`→`tip` and
    `attention`→`warning` because gentle-clues 1.3.1 has no `hint`/`attention` clue (a
    non-existent clue KeyErrors at compile). Follow docutils visitor casing conventions (N802
    is intentionally ignored). Add matching per-type structural tests to
    `TestAdmonitionConversion` (clone `test_tip_converts_to_tip`), asserting `tip({` /
    `error({` / `danger({` / `warning({` respectively and body text present. Keep black
    line-length 88; do not modernize typing imports.
  </action>
  <verify>
    <automated>pytest tests/test_admonitions.py -k "hint or error or danger or attention" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_admonitions.py -k "hint or error or danger or attention" -x` exits 0
      and collects at least 4 tests.
    - Translating a `nodes.hint` yields output containing `tip({`; `nodes.attention` yields
      `warning({`; `nodes.error` yields `error({`; `nodes.danger` yields `danger({`.
    - `black --check` and `ruff check` on both files exit 0.
  </acceptance_criteria>
  <done>hint/error/danger/attention convert to their verified gentle-clues clues and are covered by structural tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add generic .. admonition:: handler (base clue) + nodes.admonition test (D-06)</name>
  <files>typsphinx/translator.py, tests/test_admonitions.py</files>
  <read_first>
    - typsphinx/translator.py:2253-2340 — the fixed helper + existing handlers (confirm the
      generic handler routes the title child through the Plan 02 buffer-swap title path)
    - 08.1-RESEARCH.md — D-06 table row for `nodes.admonition` (maps to the base `clue`
      function; this is the ONE type where a `title` child is normal/expected) and Open
      Question 2 (use a real `nodes.admonition` fixture, not `nodes.note` with an injected
      title)
    - 08.1-PATTERNS.md — "new admonition type handlers (D-06)" mapping table
  </read_first>
  <behavior>
    - A generic `nodes.admonition` with a title "My Note" and a body paragraph emits `clue({`
      (base function, no predefined wrapper), its body evaluated inside, and the title
      attached via the buffer-swap title path (emitted once, inline markup preserved).
    - No stray `heading(` appears in the generic admonition's body.
  </behavior>
  <action>
    Add `visit_admonition`/`depart_admonition` (generic) to `typsphinx/translator.py`:
    `visit_admonition` calls `self._visit_admonition(node, "clue")` (the base gentle-clues
    function, imported via the writer's `@preview/gentle-clues:1.3.1: *` glob), and
    `depart_admonition` calls `self._depart_admonition()`. The generic directive always
    supplies a real title child, which flows through the Plan 02 admonition-aware
    `visit_title`/`depart_title` buffer-swap — do NOT pass a `custom_title`; the dynamic title
    is captured automatically. Add a test built on `nodes.admonition` (docutils' real shape,
    with a `nodes.title` child) asserting `clue({` present, the title text present exactly
    once, and `heading(` absent. Name it to match `-k "generic or admonition"`.
  </action>
  <verify>
    <automated>pytest tests/test_admonitions.py -k "generic or admonition" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_admonitions.py -k "generic or admonition" -x` exits 0.
    - Translating a `nodes.admonition` with a title yields output containing `clue({` and the
      title text exactly once, with no `heading(` substring.
    - Full `pytest tests/test_admonitions.py -x` exits 0 (no regression to the existing types).
  </acceptance_criteria>
  <done>The generic .. admonition:: converts to the base clue() with its inline-markup title preserved and is covered by a real-shape test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rST content → generated Typst source | New admonition-type bodies/titles interpolate document text into the emitted `.typ` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-03 | Tampering | New `visit_*` handlers routing through `_visit_admonition` | low | mitigate | New handlers add no new interpolation path — they reuse the Plan 02 code-mode helper + buffer-swap title escaping; body/title text stays routed through the existing escaping visitors |
| T-08.1-04 | Denial of Service | `hint`/`attention` mapping to a non-existent clue | medium | mitigate | Use only verified-existing predefined clues (`tip`, `warning`); a wrong mapping would KeyError at compile — caught by Plan 04's real-compile gate |
</threat_model>

<verification>
- `pytest tests/test_admonitions.py -x` exits 0 (all types, existing + new).
- Per-wave real-compile gate: `tox -e docs-pdf` exits 0.
- `black --check .`, `ruff check .`, `mypy typsphinx/` exit 0.
</verification>

<success_criteria>
All ten admonition types (six existing + hint/error/danger/attention/generic) convert to the
correct gentle-clues call and are covered by structural tests.
</success_criteria>

<output>
Create `.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-03-SUMMARY.md` when done
</output>
