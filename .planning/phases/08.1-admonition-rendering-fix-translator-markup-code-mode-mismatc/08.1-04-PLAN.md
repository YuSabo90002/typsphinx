---
phase: 08.1-admonition-rendering-fix-translator-markup-code-mode-mismatch
plan: 04
type: execute
wave: 3
depends_on:
  - 08.1-01
  - 08.1-02
  - 08.1-03
files_modified:
  - tests/test_pdf_render_gate.py
  - tests/fixtures/admonition_render_gate/conf.py
  - tests/fixtures/admonition_render_gate/index.rst
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "D-04: tox -e docs-pdf exits 0 (the real docs, including the typst_template_assets note, compile to PDF via the typstpdf builder)"
    - "D-04: A pypdf text-extraction of the compiled admonition PDF confirms no literal Typst source (paragraph-call / text-call / raw-call open-paren signatures) leaks into rendered admonition prose"
  artifacts:
    - "tests/test_pdf_render_gate.py: NEW real-compile + pypdf text-extraction gate"
    - "tests/fixtures/admonition_render_gate/: NEW minimal Sphinx fixture project (conf.py + index.rst) carrying the golden typst_template_assets note plus nested + multi-type admonitions"
  key_links:
    - "The gate imports pypdf (Plan 01) and depends on the landed translator fix (Plan 02) + new types (Plan 03) — it is Wave 3 because it can only pass after all translator changes ship"
    - "The fixture's index.rst must be a master document so the writer emits the full template with the gentle-clues import (included docs get only minimal imports)"
---

<objective>
Add the D-04 real-render acceptance gate — the only place this bug was ever visible. Create a
minimal self-contained Sphinx fixture project carrying the canonical `typst_template_assets`
golden note plus nested and multi-type admonitions, and a new `tests/test_pdf_render_gate.py`
that: (1) runs `sphinx-build -b typst` on the fixture, (2) compiles the emitted `.typ` to PDF
with `typst.compile()` (already a project dep), and (3) extracts the PDF text with `pypdf` and
asserts no literal Typst source signature leaks into rendered admonition prose. Wire
`tox -e docs-pdf` (real docs) as the phase acceptance gate.

Purpose: Prove the fix in a real compile + extraction, closing the exact gap that let this bug
ship (loose in-process asserts passed while the PDF rendered literal source). Satisfies D-04.
Output: `tests/test_pdf_render_gate.py` green; `tox -e docs-pdf` exits 0; full suite + lint +
types clean.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-RESEARCH.md
@.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-PATTERNS.md
@.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-VALIDATION.md
</context>

## Artifacts This Phase Produces (this plan)

- `tests/fixtures/admonition_render_gate/conf.py` + `index.rst`: NEW minimal fixture project.
- `tests/test_pdf_render_gate.py`: NEW gate test (`TYPST_AVAILABLE`/`PYPDF_AVAILABLE` skip
  guards; a sphinx-build → typst.compile → pypdf-extract → no-leak assertion).

<!-- planner-discipline-allow: par( -->
<!-- planner-discipline-allow: text( -->
<!-- planner-discipline-allow: raw( -->

<tasks>

<task type="auto">
  <name>Task 1: Create the minimal admonition render-gate fixture project</name>
  <files>tests/fixtures/admonition_render_gate/conf.py, tests/fixtures/admonition_render_gate/index.rst</files>
  <read_first>
    - tests/roots/test-basic — a minimal Sphinx test project (conf.py + master doc) to model
      the fixture structure
    - tests/test_integration_nested_toctree.py:24-60 — how integration fixtures are located
      and built (`fixtures_dir` / project-dir fixtures)
    - docs/source/user_guide/configuration.rst:110-121 — the canonical golden
      `typst_template_assets` note text (the exact known-bad occurrence, with the inline
      literal `typst_template`)
    - typsphinx/writer.py:90-100 — the writer only emits the gentle-clues `@preview` import for
      MASTER documents, so the fixture's index must be a configured master document
    - 08.1-CONTEXT.md — `<specifics>` (the golden note is the canonical render/extraction
      fixture)
  </read_first>
  <action>
    Create `tests/fixtures/admonition_render_gate/` with a minimal `conf.py` (set
    `extensions = ["typsphinx"]`, a `project`/`author`, and `typst_documents = [("index",
    "index", "Admonition Render Gate", "Test")]` so `index` is a master document and gets the
    full template + gentle-clues import). Do NOT set an HTML theme (the typst builder does not
    need furo, so the fixture runs in the normal pytest suite without the docs extra). Create
    `index.rst` containing: (a) the canonical `typst_template_assets` `.. note::` reproduced
    from configuration.rst (with the inline `` ``typst_template`` `` literal that reproduced
    the `raw(...)` leak); (b) a `.. note::` containing a bullet list; (c) a `.. warning::`
    containing a literal/code block; (d) a `.. hint::` and a `.. danger::` (exercise the new
    D-06 types); (e) a nested admonition (a note containing a warning). Keep the document a
    valid single master doc (no toctree needed).
  </action>
  <verify>
    <automated>uv run sphinx-build -b typst tests/fixtures/admonition_render_gate /tmp/nix-shell.8pVDws/claude-1000/-home-yuta-Documents-typsphinx/5216ed3d-26d0-4019-a484-dd565003c3d4/scratchpad/argate_build && test -f /tmp/nix-shell.8pVDws/claude-1000/-home-yuta-Documents-typsphinx/5216ed3d-26d0-4019-a484-dd565003c3d4/scratchpad/argate_build/index.typ</automated>
  </verify>
  <acceptance_criteria>
    - `sphinx-build -b typst` on the fixture exits 0 and produces `index.typ`.
    - The emitted `index.typ` contains `info({` (the golden note rendered in code-mode form),
      confirming the master-doc template + gentle-clues import are present.
    - The fixture uses no HTML theme / docs extra.
  </acceptance_criteria>
  <done>A minimal master-doc fixture carrying the golden note + nested + multi-type admonitions builds to index.typ.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_pdf_render_gate.py (compile + pypdf no-leak assertion, D-04)</name>
  <files>tests/test_pdf_render_gate.py</files>
  <read_first>
    - tests/test_integration_nested_toctree.py:297-343 — `TestE2ETypstCompilation` /
      `test_nested_toctree_compiles_to_pdf` — the exact sphinx-build subprocess + `import typst`
      guard + `typst.compile()` + PDF-existence scaffolding to clone
    - 08.1-RESEARCH.md — "D-04: PDF text-extraction acceptance gate (verified working
      end-to-end)" and 08.1-PATTERNS.md "tests/test_pdf_render_gate.py (NEW)" — the exact
      `pypdf.PdfReader(...).extract_text()` + leaked-token loop to add
    - the fixture created in Task 1
  </read_first>
  <action>
    Create `tests/test_pdf_render_gate.py` cloning the nested-toctree e2e scaffolding: a
    module-level `try: import typst; TYPST_AVAILABLE=True` guard and an analogous
    `try: import pypdf; PYPDF_AVAILABLE=True` guard, with the test class marked
    `@pytest.mark.skipif(not (TYPST_AVAILABLE and PYPDF_AVAILABLE), reason=...)`. The test:
    (1) `subprocess.run(["uv","run","sphinx-build","-b","typst", <fixture>, <tmp>])` and assert
    returncode 0; (2) assert `index.typ` exists; (3) `typst.compile(str(index_typ),
    output=str(pdf_output))` and assert the PDF exists, is non-empty, and starts with the
    `%PDF` magic number; (4) `pypdf.PdfReader(str(pdf_output))`, join `page.extract_text()`
    across pages, and assert that none of the three literal-source leak signatures — the
    paragraph-call, text-call, and raw-call open-paren forms listed verbatim in
    08.1-RESEARCH.md's D-04 example tuple — appear in the extracted text (each with a message
    naming the admonition markup/code-mode regression). These three token strings appear only
    as the test's own negative-search literals; they are NOT expected in any source file's
    body prose. Use a `tmp_path`-based build dir (see the nested-toctree `temp_build_dir`
    fixture). Keep black line-length 88.
  </action>
  <verify>
    <automated>pytest tests/test_pdf_render_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_pdf_render_gate.py -x` exits 0 (test runs, not skipped, in the dev env
      where typst + pypdf are installed).
    - The test compiles the fixture to a valid `%PDF` and extracts text with pypdf.
    - The extracted PDF text contains NO literal Typst source open-paren signature
      (paragraph-call / text-call / raw-call) per the RESEARCH D-04 tuple — a deliberately
      failing pre-fix compile would trip these.
    - `black --check` and `ruff check` on the new file exit 0.
  </acceptance_criteria>
  <done>The compiled admonition PDF renders typeset prose with zero literal-source leak, proven by pypdf text extraction.</done>
</task>

<task type="auto">
  <name>Task 3: Phase acceptance gate — real docs-pdf compile + full suite + lint/types (D-04)</name>
  <files>tests/test_pdf_render_gate.py</files>
  <read_first>
    - tox.ini:61-67 — the `docs-pdf` env (`sphinx-build -b typstpdf source _build/pdf`) — the
      real-compile gate against the actual project docs (the 4 known bug occurrences)
    - CLAUDE.md — the CI-matching command set (`black --check .`, `ruff check .`,
      `mypy typsphinx/`, `pytest`)
  </read_first>
  <action>
    Run the phase acceptance gate end-to-end and record results in the SUMMARY: `tox -e
    docs-pdf` must exit 0 (compiles the real docs — including the golden `typst_template_assets`
    note and the auto-generated `docs/source/api/` reference — via the typstpdf builder); the
    full `pytest` suite must pass (admonition unit tests + the new PDF render gate + no
    regressions); and `black --check .`, `ruff check .`, `mypy typsphinx/` must be clean. This
    task adds no new code — it is the phase gate that proves D-04's real-compile requirement
    against the actual docs, complementing the self-contained fixture gate in Task 2. If
    `tox -e docs-pdf` fails, the translator fix is incomplete — do not paper over it by
    weakening the fixture.
  </action>
  <verify>
    <automated>tox -e docs-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `tox -e docs-pdf` exits 0 (real docs compile to PDF).
    - `pytest` (full suite) exits 0.
    - `black --check .`, `ruff check .`, `mypy typsphinx/` all exit 0.
    - `tests/test_preview_version_sync.py` still passes (no `@preview` version was changed).
  </acceptance_criteria>
  <done>The real docs compile to PDF, the full suite is green, and the tree is black/ruff/mypy clean — the D-04 phase gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture rST → compiled PDF | The gate compiles document text through the full translator → typst → PDF pipeline |
| PyPI → dev environment | pypdf (already gated + installed in Plan 01) is imported by the new test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-05 | Tampering | Literal-source leak into rendered PDF (the phase's core defect) | high | mitigate | The pypdf text-extraction gate (Task 2) + real `tox -e docs-pdf` compile (Task 3) fail loudly if any code-mode leak signature reaches rendered prose — this is the real-render regression gate D-04 mandates |
| T-08.1-SC | Tampering | pypdf import in the test | high | mitigate | Already mitigated by Plan 01's blocking-human legitimacy checkpoint; no new install here |
</threat_model>

<verification>
- `pytest tests/test_pdf_render_gate.py -x` exits 0.
- `tox -e docs-pdf` exits 0 (phase real-compile gate).
- `pytest` full suite, `black --check .`, `ruff check .`, `mypy typsphinx/` all clean.
</verification>

<success_criteria>
The compiled admonition PDF renders typeset prose with no literal-source leak (proven by pypdf
extraction), the real docs compile via `tox -e docs-pdf`, and the full suite + lint + types are
green — the D-04 acceptance gate passes.
</success_criteria>

<output>
Create `.planning/phases/08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc/08.1-04-SUMMARY.md` when done
</output>
