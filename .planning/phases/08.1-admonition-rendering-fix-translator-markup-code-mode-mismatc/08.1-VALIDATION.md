---
phase: 08.1
slug: admonition-rendering-fix-translator-markup-code-mode-mismatch
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-11
---

# Phase 08.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `08.1-RESEARCH.md` → `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (config in `pyproject.toml` `[tool.pytest.ini_options]`), already used by `tests/test_admonitions.py` |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `pytest tests/test_admonitions.py -v` |
| **Full suite command** | `pytest` |
| **Real-compile gate** | `tox -e docs-pdf` (invokes the real typst compiler via the typstpdf builder — expensive, run per-wave not per-task) |
| **Estimated runtime** | ~a few seconds (unit) · ~tens of seconds (`tox -e docs-pdf` real compile) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_admonitions.py -v` (fast, in-process, no real typst compile)
- **After every plan wave:** Run `pytest` (full suite) **+** `tox -e docs-pdf` (real compile gate, D-04) — the compile+extraction check runs once per wave, not per task
- **Before `/gsd-verify-work`:** `tox -e docs-pdf` must exit 0 AND the PDF-text-extraction assertion (no `par(`/`text(`/`raw(` leaked into rendered admonition prose) must pass
- **Max feedback latency:** ~seconds for unit; the real-compile gate is the deliberate per-wave expensive step

---

## Per-Decision Verification Map

> Phase 08.1 has **no mapped REQ-IDs** — scope is defined by CONTEXT.md decisions D-01..D-06.
> Concrete task IDs (`08.1-NN-MM`) are assigned by the planner; this map is the contract the
> plans must satisfy.

| Decision | Behavior to verify | Test Type | Automated Command | File Exists |
|----------|--------------------|-----------|-------------------|-------------|
| D-01 | Admonition body content EVALUATES — no literal `par(`/`text(`/`raw(` leaks inside the emitted `.typ` admonition body (code-mode `info({...})` form) | unit | `pytest tests/test_admonitions.py -k "structure" -x` | ❌ W0 (new structural asserts) |
| D-02 | Title inline markup (emphasis/`literal`/etc.) preserved, not flattened via `.astext()`; no title double-emission | unit | `pytest tests/test_admonitions.py -k "title" -x` | ❌ W0 (extend title fixture + emphasis-in-title case) |
| D-03 | Existing 9 admonition tests' loose substring asserts strengthened to structural checks | unit | `pytest tests/test_admonitions.py -x` | ✅ file exists, ❌ strengthened asserts |
| D-04 | Real `tox -e docs-pdf` compile + PDF-text-extraction confirms no literal-source leak in the 4 known occurrences | integration/e2e | `tox -e docs-pdf && pytest tests/test_pdf_render_gate.py -x` (new file) | ❌ W0 |
| D-05 | Nested paragraphs / lists / code-blocks / nested-admonitions inside an admonition body all evaluate | unit | `pytest tests/test_admonitions.py -k "nested" -x` | ❌ W0 (extend `test_nested_admonitions` + list/code-block cases) |
| D-06 | `hint`, `error`, `danger`, `attention`, generic `.. admonition::` convert to the correct gentle-clues call (`hint→tip`, `attention→warning`, `error→error`, `danger→danger`, generic→base `clue()`) | unit | `pytest tests/test_admonitions.py -k "hint or error or danger or attention or generic"` | ❌ W0 (new tests + new `visit_*`/`depart_*`) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky — all rows ⬜ pending until execution.*

---

## Wave 0 Requirements

- [ ] `tests/test_admonitions.py` — strengthen existing 9 tests to assert structural absence of literal source (D-03); add nested-content tests (D-05); add 5 new type-mapping tests (D-06); add inline-markup-title test (D-02)
- [ ] `tests/test_pdf_render_gate.py` (new) — real `docs-pdf` compile + `pypdf` text-extraction assertion against the 4 known occurrences in CONTEXT.md `<specifics>` (the `typst_template_assets` note at `docs/source/user_guide/configuration.rst:116-119` is the canonical golden fixture)
- [ ] `pyproject.toml` — add `pypdf` to `dev` optional-dependencies **behind `checkpoint:human-verify`** (flagged `[SUS]` by the package-legitimacy seam — false-positive "too-new" heuristic; requires human confirmation before install)
- [ ] Framework install: `uv sync --extra dev` after the gated `pypdf` addition

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| `pypdf` dependency legitimacy | D-04 | Package-legitimacy seam flagged `pypdf` `[SUS]` (too-new false positive) | Human confirms `pypdf` is the well-known PDF library (`checkpoint:human-verify`) before `uv add`/`uv sync` |

*All other phase behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have an `<automated>` verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`tests/test_pdf_render_gate.py`, strengthened `tests/test_admonitions.py`, `pypdf` dev dep)
- [ ] No watch-mode flags
- [ ] Real-compile gate (`tox -e docs-pdf`) wired as the per-wave / phase gate
- [ ] `nyquist_compliant: true` set in frontmatter after planner alignment

**Approval:** pending
