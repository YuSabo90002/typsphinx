---
phase: 08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc
plan: 01
subsystem: infra
tags: [uv, pypdf, dev-dependencies, supply-chain]

# Dependency graph
requires: []
provides:
  - "pypdf pinned in [project.optional-dependencies].dev, resolved in uv.lock, importable"
  - "Cleared blocking-human legitimacy checkpoint for pypdf (SUS false-positive confirmed)"
affects: ["08.1-04"]

# Tech tracking
tech-stack:
  added: ["pypdf>=6.14,<7 (dev-only, py-pdf/pypdf on PyPI)"]
  patterns: []

key-files:
  created: []
  modified: ["pyproject.toml", "uv.lock"]

key-decisions:
  - "pypdf's package-legitimacy seam SUS verdict (too-new/unknown-downloads) was human-confirmed as a false positive before install, per the blocking-human legitimacy checkpoint protocol"

patterns-established: []

requirements-completed: []

coverage:
  - id: D1
    description: "pypdf added as a bounded-pin dev dependency and resolved in uv.lock"
    verification:
      - kind: other
        ref: "uv run python -c 'import pypdf; print(pypdf.__version__)'"
        status: pass
    human_judgment: false
  - id: D2
    description: "pypdf legitimacy (SUS false-positive) confirmed by human before install"
    verification: []
    human_judgment: true
    rationale: "Supply-chain legitimacy judgment is inherently a human call, not automatable; user explicitly approved pypdf as the genuine py-pdf/pypdf project prior to this execution session"

duration: 1min
completed: 2026-07-11
status: complete
---

# Phase 08.1 Plan 01: pypdf dev dependency Summary

**Added `pypdf>=6.14,<7` (resolved 6.14.2) as a dev-only dependency, clearing its SUS supply-chain legitimacy gate via prior human approval, to unblock Plan 04's PDF-text-extraction acceptance gate.**

## Performance

- **Duration:** 1 min
- **Started:** 2026-07-11T04:27:02Z
- **Completed:** 2026-07-11T04:27:34Z
- **Tasks:** 2 (1 checkpoint, 1 auto)
- **Files modified:** 2 (`pyproject.toml`, `uv.lock`)

## Accomplishments
- Confirmed and recorded the pre-approved blocking-human legitimacy checkpoint for `pypdf` (Task 1) — no re-prompt needed since the user had already explicitly approved it as the genuine `py-pdf`/`pypdf` project (formerly `PyPDF2`) prior to this execution session.
- Added `"pypdf>=6.14,<7"` to `[project.optional-dependencies].dev` in `pyproject.toml`, matching the sibling entries' bounded-range pin style.
- Regenerated `uv.lock` via `uv sync --extra dev`; resolved `pypdf==6.14.2`.
- Verified `uv run python -c "import pypdf; print(pypdf.__version__)"` prints `6.14.2` and exits 0.
- Verified `uv.lock` contains exactly one resolved `pypdf` entry (`grep -c '^name = "pypdf"' uv.lock` → 1).
- Verified `git diff pyproject.toml` shows only the added `pypdf` dev entry — no runtime `[project].dependencies` change and no `@preview` package version touched.

## Task Commits

Each task was committed atomically:

1. **Task 1: Human-verify pypdf legitimacy (SUS false-positive gate)** - checkpoint recorded, no commit (nothing installed; pre-approved by human user before this session per the orchestrator's `<checkpoint_preapproved>` directive)
2. **Task 2: Add pypdf to dev extras and regenerate the lockfile** - `e08aab6` (chore)

**Plan metadata:** (this commit, made after this SUMMARY)

## Files Created/Modified
- `pyproject.toml` - Added `"pypdf>=6.14,<7"` to `[project.optional-dependencies].dev`
- `uv.lock` - Regenerated; resolved `pypdf==6.14.2` and its transitive deps

## Decisions Made
- The Task 1 blocking-human legitimacy checkpoint was treated as pre-approved per explicit instruction from the orchestrator/user: the human had already confirmed `pypdf` is the long-established `py-pdf` project (not a typosquat) and the `[SUS]` "too-new"/"unknown-downloads" flags are false positives reflecting release cadence, not provenance. This checkpoint was not re-prompted; it is recorded here as satisfied.
- Used `uv sync --extra dev` (direct `pyproject.toml` edit + sync) rather than `uv add --dev pypdf`, since the plan's insertion point and exact pin string (`"pypdf>=6.14,<7"`) were already specified in the plan text — editing directly and syncing achieves an identical resolved lockfile with less risk of `uv add` choosing a different pin style.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Dev dependency only; `uv sync --extra dev` picks it up automatically for anyone running the standard dev setup command from CLAUDE.md.

## Next Phase Readiness

Plan 04 (D-04 real-compile acceptance gate) can now `import pypdf` in its new `tests/test_pdf_render_gate.py` to text-extract the compiled PDF and assert no literal Typst source leaks into rendered admonition prose. No blockers remain from this plan.

---
*Phase: 08.1-admonition-rendering-fix-translator-markup-code-mode-mismatc*
*Completed: 2026-07-11*

## Self-Check: PASSED

- FOUND: pyproject.toml
- FOUND: uv.lock
- FOUND: 08.1-01-SUMMARY.md
- FOUND: e08aab6 (commit)
