---
phase: 44.2-typst-documents-title-and-author-consumption
plan: 03
subsystem: docs-build
tags: [sphinx, typst, ast-walk, documentation, requirements-tracking]

# Dependency graph
requires:
  - phase: 44.2-01
    provides: "phase_base_sha / red_anchor_sha (44.2-GATE-EVIDENCE-01.md), _resolve_entry_element() and the sphinx_metadata substitution, the D-05 typst_authors reorder"
  - phase: 44.2-02
    provides: "the full precedence matrix, multi-master no-leak proof, D-03's four-route uniformity table, and the corpus-delta handoff note (one new divergent-author entry in tests/fixtures/template_named_dir_master/conf.py)"
provides:
  - "SC#4: a re-derived, re-runnable AST-walk script (inlined in 44.2-GATE-EVIDENCE-03.md) that reproduces the corrected baseline corpus figures (108/111/107/3/6/0/72/2) against phase_base_sha and attributes the post-change delta file-by-file"
  - "SC#4: a measured refinement beyond D-08's own prediction -- examples/charged-ieee/approach1/conf.py, which CONTEXT.md named as a site that 'still changes', is measured byte-identical before/after (same package-alone+unset-mapping special case plan 02 discovered)"
  - "SC#5/D-10/D-11: configuration.rst's five-element contract publishes the resolution order, the accepted-and-ignored statement for element 5 (naming the real five-element configs), and the typst_authors forward-removal notice"
  - "D-06: CONF-10 filed under REQUIREMENTS.md Future Requirements, carrying the lossless-replacement measurement and full affected surface -- typst_authors itself stays registered and undeprecated"
  - "SC#6 (phase-wide): full suite/lint/type/preview-sync/corpus-gate all green, all four scope fences proven from phase_base_sha to HEAD"
affects: [45-docs, 45.1-templates-rst, 46-changelog-and-release-prep]

# Actuals (#2632)
actuals:
  tokens: 12143
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Reproducible AST-walk evidence inlined directly in a GATE-EVIDENCE-NN.md fenced block (no standalone scripts/ file), driven by git ls-tree + git show against a ref so the identical script runs against a historical SHA or HEAD unmodified"
    - "Temporary production-file revert (git checkout <phase_base_sha> -- <files>) for a real before/after build comparison, with a hard git-status-empty assertion both before starting and after restoring"

key-files:
  created:
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md
  modified:
    - docs/source/user_guide/configuration.rst
    - .planning/REQUIREMENTS.md

key-decisions:
  - "Reported a genuine discrepancy against CONTEXT.md's own D-08 citation rather than silently absorbing it: examples/charged-ieee/approach1/conf.py was predicted to 'still change' but measured byte-identical before/after, because typst_package is set with typst_template_mapping left completely unset -- the same package-alone+unset-mapping special case (empty parameter_mapping, not the default) that plan 02 independently discovered for its own A4 test fixture."
  - "CONF-10 filed under a new 'Filed during this milestone' sibling grouping in REQUIREMENTS.md's Future Requirements, rather than under 'Carried from earlier milestones' -- CONF-10 was discovered THIS milestone, so filing it under the carried-forward list would misstate its provenance."
  - "The refined D-08 partition (105 satisfying / 5 live-changing / 1 confirmed-inert, out of 111 baseline entries) is what SC#4's claim rests on, not the strict 3-condition partition (103/8) -- the strict partition is reported for completeness but two of its 8 'failing' entries are measured unchanged by real builds."

patterns-established:
  - "A GATE-EVIDENCE walk script parameterized by git ref (not by working-tree state) lets the identical script serve as both the baseline (historical SHA) and post-change (HEAD) measurement -- no working-tree mutation, no git archive, no checkout needed for the walk itself."

requirements-completed: [CONF-09]

coverage:
  - id: D1
    description: "SC#4: the corpus partition is proved by a machine-checkable AST walk re-derived and run in this plan's own session, reproducing the corrected baseline figures (108/111/107/3/6/0/72/2) on the first run against phase_base_sha"
    requirement: "CONF-09"
    verification:
      - kind: other
        ref: "44.2-GATE-EVIDENCE-03.md §2 (baseline walk output, verbatim reproduction)"
        status: pass
    human_judgment: false
  - id: D2
    description: "SC#4: the post-change delta against HEAD is attributed file-by-file to plans 01 and 02, with no unexplained discrepancy; the failing set carries verbatim before/after builds for all 7 non-inert entries, and the 8th (non_str_docname_gate[1]) is confirmed inert by source comparison plus a real build"
    requirement: "CONF-09"
    verification:
      - kind: other
        ref: "44.2-GATE-EVIDENCE-03.md §3-6 (delta table, partition, before/after table, inert-site record)"
        status: pass
    human_judgment: false
  - id: D3
    description: "SC#5/D-10/D-11: configuration.rst publishes the title/author resolution order, the literal 'accepted and ignored' statement for element 5 naming the real five-element configs, and the typst_authors 'slated for removal' forward notice -- every published rule traced to a passing test node id"
    requirement: "CONF-09"
    verification:
      - kind: unit
        ref: "tests/test_entry_metadata_precedence.py (17 tests) + tests/test_document_metadata_render_gate.py (3 tests)"
        status: pass
      - kind: other
        ref: "docs HTML build succeeds, 3 pre-existing warnings none naming configuration.rst (44.2-GATE-EVIDENCE-03.md §7)"
        status: pass
    human_judgment: true
    rationale: "The plan's own must_haves designates the prose-accuracy truth as a `backstop` -- whether the published precedence text genuinely reads as user-actionable guidance rather than an internal changelog is a human judgement the automated phrase/grep checks cannot settle on their own."
  - id: D4
    description: "D-06: typst_authors removal filed as CONF-10 under REQUIREMENTS.md Future Requirements with its measured lossless-replacement basis, full affected surface, and the charged-ieee/approach1 migration -- typst_authors itself remains registered and undeprecated in code"
    requirement: "CONF-09"
    verification:
      - kind: other
        ref: "grep -c 'CONF-10' .planning/REQUIREMENTS.md >= 1; CONF-09 traceability row unmodified; CONF-10 absent from the Traceability table"
        status: pass
      - kind: other
        ref: "uv run python -c \"import typsphinx, inspect; print('typst_authors' in inspect.getsource(typsphinx.setup))\" -> True"
        status: pass
    human_judgment: false
  - id: D5
    description: "SC#6 (phase-wide): full suite, black, ruff, mypy, preview-version-sync, and corpus-gate all green, with all four scope fences (templates.rst, todos/pending, conf.py census, @preview literals/base.typ) proven from phase_base_sha to HEAD"
    verification:
      - kind: unit
        ref: "uv run python -m pytest -q (900 passed, 1 skipped, 0 failed)"
        status: pass
      - kind: other
        ref: "uv run black --check . && uv run ruff check . && uv run mypy typsphinx/ && uv run python -m pytest tests/test_preview_version_sync.py tests/test_corpus_gate.py -q"
        status: pass
    human_judgment: false

duration: 10min
completed: 2026-08-05
status: complete
---

# Phase 44.2 Plan 03: Corpus Partition Evidence and Published-Contract Repair Summary

**A re-derived AST walk reproduces the corrected 108/111/107/3/6/0/72/2 corpus figures on the first run and, while measuring the failing entries' real before/after builds, catches CONTEXT.md's own D-08 mispredicting `examples/charged-ieee/approach1/conf.py` as a changing site when it is measured byte-identical -- `configuration.rst` now publishes the true precedence contract and `typst_authors`'s removal is filed as CONF-10, still registered and undeprecated in code.**

## Performance

- **Duration:** ~10 min (commit range 2026-08-05T07:48:25+09:00 -> 07:57:47+09:00, plus environment provisioning and reading before the first commit)
- **Started:** 2026-08-05T07:48:25+09:00 (first commit)
- **Completed:** 2026-08-05T07:57:47+09:00 (last commit)
- **Tasks:** 3
- **Files modified:** 3 (0 production, 0 test, 3 docs/evidence/requirements)

## Accomplishments

- Wrote and ran a fresh AST-walk script (inlined verbatim in `44.2-GATE-EVIDENCE-03.md` §1, no standalone `scripts/` file) that enumerates every `conf.py` in the repo via `git ls-tree`/`git show` against a ref -- reproduced all eight of RESEARCH.md's corrected baseline figures (108 files / 111 entries / 107 four-tuples / 3 five-tuples / 6 divergent-title / 0 divergent-author / 72 inline occurrences / 2 `typst_authors`-combining files) against `phase_base_sha` on the **first run**, with no reconciliation needed.
- Ran the same script against `HEAD` and attributed every one of the eight metric deltas to a named plan and file: plan 01's two new fixture `conf.py` files (+2 files/entries/four-tuples/divergent-title/2 of the 3 divergent-author) and plan 02's deliberate divergent `entry[3]` addition to `tests/fixtures/template_named_dir_master/conf.py[1]` (the third divergent-author entry, exactly as `44.2-GATE-EVIDENCE-02.md` §2 handed forward).
- Partitioned the baseline corpus by D-08's three conditions both strictly (103 satisfying / 8 failing) and by real-build measurement (105 satisfying / 5 live-changing / 1 confirmed-inert) -- and in doing so **found a discrepancy CONTEXT.md's own D-08 did not predict**: `examples/charged-ieee/approach1/conf.py`, cited by D-08 as a site that "still changes," is measured **byte-identical** before and after the D-05 reorder, because it hits the same package-alone-with-unset-mapping special case (`TemplateEngine.__init__` resolving to an *empty* `parameter_mapping`, not the default one) that plan 02 independently found for its own A4 test fixture. Reported as a disagreement, not smoothed over.
- Built all 7 non-inert failing-condition entries before (`phase_base_sha`'s code) and after (`HEAD`'s code), capturing verbatim `title:`/`authors:` lines for each; confirmed `tests/fixtures/non_str_docname_gate/conf.py[1]` is inert via both a source-code comparison (`int 123 == str docname` is always `False`) and a real build showing its target file is never written.
- Amended `docs/source/user_guide/configuration.rst` in three places: the five-element contract's items 3/4 (resolution order, empty-string-is-a-value, `typst_authors`/`typst_template_function` precedence) and item 5 (literal "accepted and ignored," naming `docs/source/conf.py` and both `charged-ieee` configs as real five-element producers instead of repeating D-10's measured-false "zero `conf.py` entries" claim); the `typst_authors` Detailed Format section (new precedence text plus a `.. note::` carrying the literal "slated for removal"); and the worked example's `# Author details` comment (a short forward-pointer). `templates.rst` untouched throughout. Docs HTML build succeeds with 3 pre-existing warnings, none naming `configuration.rst`.
- Filed `typst_authors`' removal as `CONF-10` under `.planning/REQUIREMENTS.md`'s Future Requirements (a new "Filed during this milestone" sibling grouping, since CONF-10 was discovered this milestone rather than carried from an earlier one), carrying the measured lossless-replacement basis, the full affected surface, and the `examples/charged-ieee/approach1` migration. `typst_authors` remains registered in code with no deprecation warning.
- Ran and recorded the full phase gate: `pytest -q` (900 passed, 1 skipped), `black --check .`, `ruff check .`, `mypy typsphinx/`, `test_preview_version_sync.py`, `test_corpus_gate.py` -- all green -- and proved all four scope fences (`templates.rst`, `.planning/todos/pending/`, the `conf.py` census, the `@preview` literals/`base.typ`) from `phase_base_sha` to `HEAD`, not merely across this plan.

## Task Commits

Each task was committed atomically:

1. **Task 1: The corpus partition, re-derived and measured on both sides of the change (SC#4)** - `f398cea` (docs)
2. **Task 2: The published contract made true (SC#5, D-10, D-11)** - `82d950d` (docs)
3. **Task 3: File CONF-10, close the phase gate (D-06, SC#6)** - `54c3584` (docs)

**Plan metadata:** this SUMMARY.md's own commit (worktree mode -- STATE.md/ROADMAP.md excluded, orchestrator applies them after merge)

## Files Created/Modified

- `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md` - new evidence file, §0 through §11: worktree isolation proof, the inlined AST-walk script, baseline/post-change/delta/partition/before-after-builds/inert-site (SC#4), docs-build result + rule-to-test table (SC#5), phase gate + fence proofs + SC#1-SC#6 roll-up (SC#6)
- `docs/source/user_guide/configuration.rst` - five-element contract items 3/4/5 amended (resolution order, empty-string-is-a-value, accepted-and-ignored); `typst_authors` Detailed Format section gains precedence text + forward-removal `.. note::`; worked example gains a forward-pointer comment
- `.planning/REQUIREMENTS.md` - new `CONF-10` Future Requirement (typst_authors removal, filed not performed)

## Decisions Made

- Reported the `examples/charged-ieee/approach1/conf.py` discrepancy against CONTEXT.md's own D-08 citation explicitly, rather than silently classifying it under the predicted refinement -- the plan's own prohibition forbids transcribing a planning document's figure over a measured one, and this is exactly the case that prohibition exists for.
- Placed CONF-10 in a new "Filed during this milestone" heading rather than folding it into "Carried from earlier milestones," since the latter would misstate when the requirement was discovered.
- Used the measured-refined partition (105/5/1), not the strict 3-condition partition (103/8), as the basis for SC#4's claim -- the strict partition is reported for completeness and to show the refinement's exact size, but two of its "failing" entries are proven unchanged by real builds.

## Deviations from Plan

None - plan executed exactly as written. All figures and discrepancies reported above were *predicted as possible* by the plan's own `<flagged_assumptions>` (specifically #3, "the baseline reproduction may not reproduce," and #4, "D-08's third condition is measurably conservative") and by the plan's explicit instruction to report any disagreement between a planning document and a fresh measurement -- finding and reporting the `charged-ieee/approach1` discrepancy is exactly what the plan asked for, not a deviation from it.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- CONF-09 is now fully discharged across all three plans of Phase 44.2: SC#1 (plan 01), SC#2/SC#3 (plan 02), SC#4/SC#5/SC#6 (this plan). Phase completion owns flipping the `.planning/REQUIREMENTS.md` CONF-09 traceability row to Complete -- this plan deliberately did not touch it.
- `CONF-10` is ready for a future milestone to pick up as a standalone removal phase; its entry names the full affected surface and the one migration it owes (`examples/charged-ieee/approach1/conf.py`).
- `docs/source/user_guide/templates.rst`'s `title`-comes-from-`typst_documents` claim is now TRUE (this phase made it so) but remains unedited, as required -- Phase 45.1 SC#2 verifies it.
- Phase 46/REL-06's CHANGELOG source text for CONF-09 is already recorded in `44.2-01-SUMMARY.md`'s "User-Visible Changes" section; this plan added no further user-visible behavior change (docs/evidence/requirements only).

## Self-Check: PASSED

All created/modified files confirmed present on disk (`44.2-GATE-EVIDENCE-03.md`,
`docs/source/user_guide/configuration.rst`, `.planning/REQUIREMENTS.md`, this SUMMARY).
All three task commit hashes (`f398cea`, `82d950d`, `54c3584`) confirmed present via
`git log --oneline`.

---
*Phase: 44.2-typst-documents-title-and-author-consumption*
*Completed: 2026-08-05*
