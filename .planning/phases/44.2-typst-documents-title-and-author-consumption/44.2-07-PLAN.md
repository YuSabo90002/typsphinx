---
phase: 44.2-typst-documents-title-and-author-consumption
plan: 07
type: execute
wave: 7
depends_on: ["44.2-01", "44.2-02", "44.2-03", "44.2-04", "44.2-05", "44.2-06"]
files_modified:
  - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md
  - docs/source/user_guide/configuration.rst
  - tests/test_authors_pipeline_stage_gate.py
autonomous: true
gap_closure: true
requirements: [CONF-09]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The exploration set is computed FROM THE CODE, not from the correction: `44.2-GATE-EVIDENCE-06.md` §1 re-derives the pipeline's `authors`/`title`-determining stage sites by an AST walk over the committed `typsphinx/` tree, run in this plan's own session and pasted verbatim -- not copied from plan 06's frozen `EXPECTED_STAGE_SITES`, not copied from any plan's prose."
    - "The falsification row space ranges over the STAGE axis: §2's generated table is the cross product of mapping source key x mapping target key x source-presence x `typst_template_function` `params[\"authors\"]` present/absent, so every row that a stage-1-only row space could not distinguish now exists as its own row. The table is produced by a loop, not typed by hand."
    - "The sentence set is derived from §1's stage list outward: §3 lists every published sentence in `docs/source/user_guide/configuration.rst` that constrains when a stage writes `authors`, or denies that another stage writes it. A sentence qualifies by what it claims about a stage -- never by whether any plan edited it -- and each carries an `S-NN` id and a SINGLE line number, never a line range."
    - "Every sentence is graded against NAMED falsification rows on both stages: §4 has one row per `S-NN` carrying a verdict token and a non-empty falsification-row cell for the mapping stage AND a non-empty falsification-row cell for the render stage, each naming a §2 row id and its observed value. A verdict with no named falsification row is an unfinished row, never a pass."
    - "No silent drops, mechanically: §5 contains a runnable assertion, with its verbatim output pasted, comparing the `S-NN` id set of §3 against the `S-NN` id set of §4 and failing on any difference in either direction. Equality is an executed assertion, not a prose claim."
    - "Any sentence graded NARROWER-THAN-CODE or FALSE-AT-PIPELINE-SCOPE is FIXED and §1-§5 re-run against the fixed file, never recorded as an accepted defect. §4 closes with zero open non-ACCURATE verdicts."
    - "`44.2-VERIFICATION.md`'s `missing:` bullet 2 is closed by construction: the line the verification names, or the sentence that replaced it, appears as its own graded `S-NN` row with a falsification row combining `typst_authors` + an authors-omitting mapping + `typst_template_function`'s `params[\"authors\"]`."
    - "SC#6 re-taken in this plan's own session: full `uv run python -m pytest`, `black --check .`, `ruff check .`, `mypy typsphinx/` green, a docs HTML build with no WARNING/ERROR line naming `configuration.rst`, and the passed count recorded as a delta against both the 932 measured at the round-3 base and this session's own pre-change baseline."
    - "The record is forward-only: `44.2-01..05-PLAN.md`, every `-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md` and `44.2-GATE-EVIDENCE-01..05.md` are byte-identical to the round-4 gap base, and `.planning/REQUIREMENTS.md` is untouched."
    - statement: "The grader is not the author. This plan runs in a LATER WAVE than the correction, in its own worktree, with no memory of what plan 06 intended to write; it reads the committed tree. Round 3's enumeration failed because the same agent that wrote the correction graded it and inherited its frame -- the false clause sat inside a graded site's own cited line range while a neighbouring sentence was the one quoted. Whether a paragraph is accurate at pipeline scope is a judgement no single assertion settles; this separation is the control, and it is structural rather than assertable."
      verification: backstop
  artifacts:
    - .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md
  key_links:
    - "the AST stage walk over the committed `typsphinx/` tree (§1) -> the widened generated row table (§2) -> the sentence inventory derived from the stage list (§3) -> the per-stage falsification grading (§4) -> the runnable id-set equality assertion (§5)"
    - "`44.2-VERIFICATION.md`'s `missing:` bullet 2 -> the `S-NN` row for `configuration.rst` line 194's replacement -> its three-knob falsification row in §2"
    - "plan 06's `test_render_template_function_authors_replaces_map_parameters_surviving_seed` and the two real-build gates -> §6's clause-to-test traceability table -> SC#2's 'each rule has a test'"
  prohibitions:
    - statement: "MUST NOT derive the sentence set from the passages plans 05 or 06 edited, from `44.2-VERIFICATION.md`'s `missing:` list, or from `44.2-REVIEW.md`'s CR-01 narrative. That frame is the measured root cause of round 3: its enumeration graded nine sites of which only three were in `configuration.rst`, and the false clause lay inside graded site #2's own cited line range. The set is computed from §1's stage list outward, and a sentence qualifies by what it claims about a stage."
      status: unverified
      verification: test
    - statement: "MUST NOT cite a line RANGE for any graded sentence. A cited range that contains an ungraded claim-bearing sentence is itself the defect this round closes -- `44.2-GATE-EVIDENCE-05.md` §3 row 2 cited 189-217 and graded the sentence at 201 while the false one sat at 194. Grade sentences, not ranges: every §4 row's line cell is a single integer."
      status: unverified
      verification: test
    - statement: "MUST NOT record a verdict without a named falsification row on EVERY later stage. A row whose render-stage falsification cell is empty is unfinished, not a pass. Round 3's rows each carried exactly one falsification row and all of them were stage-1 rows, which is why an unscoped claim about stage 2 could be graded ACCURATE."
      status: unverified
      verification: test
    - statement: "MUST NOT record a NARROWER-THAN-CODE or FALSE-AT-PIPELINE-SCOPE verdict as an accepted defect, a known limitation, or a follow-up. Fix the sentence, re-run §1-§5 against the fixed file, and record both passes. Shipping a graded-false sentence is what produced rounds 2, 3 and 4."
      status: unverified
      verification: test
    - statement: "MUST NOT publish an unscoped negative existential about the whole pipeline, and MUST NOT reintroduce one while fixing a residual. Any negative existential must name the stage it quantifies over and must carry a falsification row at every later stage. MUST NOT publish an exhaustive quantifier over config SHAPES, and MUST NOT hedge one with 'usually', 'typically', 'most often' or 'in practice' -- a hedge is worse than a wrong quantifier because no test can falsify it."
      status: unverified
      verification: test
    - statement: "MUST NOT change any executable line under `typsphinx/`. Verified by `ast.dump(ast.parse(...))` structural equality against the round-4 gap base for both `typsphinx/template_engine.py` and `typsphinx/writer.py`."
      status: unverified
      verification: test
    - statement: "MUST NOT weaken, delete, rename, `xfail`, `skip` or loosen any existing assertion anywhere in the suite -- including plan 06's new gates and `tests/test_params_authors_writers.py::test_configuration_rst_documents_both_target_key_shapes` -- to accommodate a residual fix. If a fix would break a drift guard, the fix is rewritten; the guard is not. Collected counts go up, never down."
      status: unverified
      verification: test
    - statement: "MUST NOT edit, amend or re-word `44.2-GATE-EVIDENCE-01..05.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, any `44.2-0[123456]-PLAN.md`, or any `-SUMMARY.md`. They are the record of four rounds of one failure mode. The correction is recorded FORWARD in `44.2-GATE-EVIDENCE-06.md`."
      status: unverified
      verification: test
    - statement: "MUST NOT name this plan's artifact `44.2-VERIFICATION.md`. That filename is owned and overwritten wholesale by `/gsd-verify-phase`, so plan-authored evidence there is lost silently on the next verify run. Use `44.2-GATE-EVIDENCE-06.md`."
      status: unverified
      verification: test
    - statement: "MUST NOT edit `.planning/REQUIREMENTS.md`. In particular MUST NOT flip `CONF-09`'s `- [ ]` checkbox and MUST NOT touch the Traceability row `CONF-09 | Phase 44.2 | Pending`. `phase.complete` has a recorded habit of flipping such rows early on this project; a plan doing it by hand would make the phase look sealed. Sealing is verify-phase's call, and this plan produces evidence, not a seal."
      status: unverified
      verification: test
    - statement: "MUST NOT edit `docs/source/user_guide/templates.rst`. Phase 45.1 SC#2 owns it and its job is to CHECK that file, which it cannot do if this phase rewrites it. If §3's stage-derived sweep surfaces a claim-bearing sentence there, record it in the deferred section as a Phase 45.1 input; do not fix it."
      status: unverified
      verification: test
    - statement: "MUST NOT transcribe any figure, count or observed value from `44.2-VERIFICATION.md`, `44.2-REVIEW.md`, `44.2-GATE-EVIDENCE-01..05.md`, or from plans 05 and 06 into this evidence file. Every number and every command output is produced by a command run in THIS plan's session and pasted verbatim. A transcribed figure is how a stale measurement survives a round."
      status: unverified
      verification: test
    - statement: "MUST NOT plan, fix or touch `44.2-REVIEW.md`'s WR-01 (`typsphinx/writer.py:319-321`'s unhandled `TypeError` on a non-`dict` `typst_elements`). It was measured to predate this phase (commit `cf22574`, Phase 27.1, 2026-07-25) and is a confirmed carry-forward, not a 44.2 gap."
      status: unverified
      verification: test
---

<objective>
Close `44.2-VERIFICATION.md`'s `missing:` bullet 2 -- the half of the gap plan 06 deliberately does
not touch -- by producing `44.2-GATE-EVIDENCE-06.md`: a per-sentence enumeration whose exploration
set is computed by reverse-computation from the CODE's writers at every pipeline stage, graded
against named falsification rows on the stage axis, with a mechanically enforced equality check
between the sentences surfaced and the sentences graded.

**Why this is its own plan, in its own wave.** Round 3 built exactly such an enumeration
(`44.2-GATE-EVIDENCE-05.md` §3) specifically to break this cycle, and it failed the same way for a
structural reason that is now measured: its site set was derived from the passages its own plan had
edited (only 3 of 9 graded sites were in `configuration.rst`), and its row space was
`SOURCE_KEYS x TARGET_KEYS x presence` -- entirely inside `map_parameters()`. `render()`'s
`all_params.update(self.typst_template_params)` had no axis in it. The false clause at line 194 sat
INSIDE graded site #2's own cited line range 189-217, but row 2 quoted and graded the *following*
sentence. `grep -in "nothing else" 44.2-GATE-EVIDENCE-05.md` returns zero hits.

The grader inherited the author's frame because the grader WAS the author. This plan runs in wave 7,
in its own worktree, after plan 06's correction is committed. It has no memory of what plan 06
intended to write; it reads the committed tree, re-derives the stage set from the AST itself, and
derives the sentence set from that stage set outward. Whether the correction actually landed is
something it measures, not something it inherits.

Purpose: make the enumeration mechanism itself falsifiable -- widened row space, single-line
citations, per-stage falsification cells, and a runnable no-silent-drop equality assertion -- so a
fifth round cannot be produced by the same defect one level deeper.

Output: `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md`
with sections §0-§7, plus any residual prose fix its own grading forces (and the re-run that proves
the fix).

Scope fence: no runtime behaviour change; no `.planning/REQUIREMENTS.md` edit; no `templates.rst`
edit; no edit to any prior plan, SUMMARY, REVIEW, VERIFICATION or GATE-EVIDENCE file. This plan does
not seal the phase -- it produces the evidence `/gsd-verify-phase` grades.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-REVIEW.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-CONTEXT.md
@typsphinx/template_engine.py
@typsphinx/writer.py
@docs/source/user_guide/configuration.rst
</context>

<measured_grounding>
Measured in the planning session, at `GAP_BASE_R4 = e9771569bc075673de2bddf67da2f72f9389299b`.
Re-derive everything; §2 is why this plan exists and must not be shortened.

## 1. The stage sites the enumeration must range over

`grep -rn 'params\[' typsphinx/*.py` and `grep -rn '\.update(' typsphinx/*.py`, then read each hit:
`template_engine.py:485` (the `typst_authors` seed), `:499` (the mapping loop), `:508/510/512` (the
non-package back-fill), `:531` (the `typst_elements` loop, allowlisted to
`fontsize`/`lang`/`papersize`), `writer.py:330` (`params.update(toctree_options)`),
`template_engine.py:696` (`all_params.update(params)`) and `:697`
(`all_params.update(self.typst_template_params)`). Nine sites, four functions, two modules, three
stages. Round 3's frame contained the first five and nothing else.

## 2. Why round 3's mechanism could not have caught the defect it shipped

`44.2-GATE-EVIDENCE-05.md` §3's table has nine rows, each with a `FALSIFY-ROW` cell. Every one of
those nine cells names a row from §2's `source x target x presence` table -- rows A through I, all
of them `map_parameters()`-only. Not one row in that space can distinguish "the seed survives
`map_parameters()`" from "the seed reaches the emitted `.typ`", because the table never called
`render()`. A sentence claiming the second while being true of the first is therefore unfalsifiable
inside that row space, and that is exactly the sentence that shipped. Widening the row space along
the stage axis is not a refinement of round 3's method; it is the repair of the thing that made it
inert.

## 3. What plan 06 changed, and what this plan must NOT assume about it

Plan 06 (wave 6) rewrites `configuration.rst`'s Precedence block as an ordered two-stage rule and
rescopes item 4's universal, adds `tests/test_authors_pipeline_stage_gate.py` (stage enumeration,
partition assertion, two real `-b typst` gates and their control, a knob-presence prose gate) and one
Group 3 cell in `tests/test_entry_metadata_precedence.py`. **This plan must verify all of that by
measurement rather than assume it.** If plan 06's correction is absent, partial, or itself carries a
sentence this plan's grading finds NARROWER-THAN-CODE, this plan fixes it and re-runs. Read the
committed file, not plan 06's action text.
</measured_grounding>

<flagged_assumptions>
Surfaced, not silently dropped, per the spec-less probe fallback protocol (`EDGE_ABSENT=1`,
`PROHIB_ABSENT=1` -- this phase has no `-SPEC.md`).

**FA-01 -- CONF-09's edge classification is `unclassified` / `unresolved`.** The deterministic edge
probe returned `{applicable: 1, resolved: 0, unresolved: 1}` for CONF-09 with
`probe: "unclassified -- review manually"`. Per §A/§B of the fallback protocol an `unclassified` row
stays `unresolved`: never auto-`backstop`ped, never auto-dismissed, never silently dropped, so **no
probe-derived edge predicate was authored into `must_haves` from it**. Every truth above was derived
goal-backward from `44.2-VERIFICATION.md`'s single FAILED truth (SC#2) and its FAILED backstop
must-have. Consequence accepted knowingly: **what an unclassified CONF-09 edge would have covered
that this plan does not** is the data-shape family -- author-value encoding, multi-author ordering
through the emitted tuple, and the empty/`None` shapes of `typst_template_function["params"]
["authors"]`. §2's generated row table varies the PRESENCE of that key across the stage axis, not the
SHAPE of its value. Judged acceptable: this plan grades published text against reachable
configurations, and shape concerns are pinned elsewhere in the suite. No-silent-drop equality for
edges: **1 surfaced == 0 authored + 1 flagged**.

**FA-02 -- prohibition probe: 18 raw candidates, 13 authored, 5 dropped, 0 canon referrals.**
Stage-2 precision dropped 5 as routine engineering or hygiene already owned by the acceptance
criteria and SC#6's gate, with breadcrumbs: docs-build health, markdown table well-formedness in the
evidence file, wrap-width conformance, commit granularity, and dependency/version hygiene (nothing
is installed). **No OWASP / injection / path-traversal / prototype-pollution / GDPR / fairness
candidate survived Stage 2** for this scope -- an evidence document, AST probes over this project's
own source, and a contingent prose fix, with no new input handling and no parsing of untrusted data
-- so there is no canon-referral breadcrumb to `/gsd-secure-phase`. The 13 kept prohibitions are
authored **descriptor-less** (no `check_kind` / `check_target` / `check_rule` /
`check_violation_fixture` / `check_clean_fixture`), so each disposes flagged-unverified.
No-silent-drop equality for prohibitions: **18 surfaced == 13 authored + 5 dropped with
breadcrumbs**.

**FA-03 -- the sentence-splitting step is heuristic and the enumeration's weakest link.** Deciding
where one published sentence ends and the next begins is not mechanically settled for reStructuredText
(bullet continuations, inline literals containing periods, run-in bold). The equality assertion in §5
proves that every sentence SURFACED in §3 was GRADED in §4; it cannot prove that §3 surfaced every
sentence in the file. That residual is mitigated three ways and not eliminated: the sweep is derived
from the stage list rather than from an edit set; the acceptance criteria require the split to be
produced by a pasted, runnable splitter rather than by hand; and every claim-bearing line inside any
paragraph a graded sentence belongs to must itself appear as its own row (the specific hole that
sank round 3). Stated rather than hidden.

**FA-04 -- a residual fix inside this plan is graded by its own author.** If §4 finds a
NARROWER-THAN-CODE sentence, this plan fixes it and re-runs §1-§5 -- and at that moment the
wave-separation control that this plan exists to provide no longer applies to that particular fix.
The compensating controls are that the re-run is mechanical (the same pasted probes, the same
equality assertion, the widened row space) and that both passes are recorded, so a reviewer can see
what changed between them. If the residual is large enough that a hand-graded re-run is not credible,
say so in §7's discharge and recommend a further wave rather than declaring it closed.
</flagged_assumptions>

<source_audit>
| SOURCE | ID | Item | Task | Status |
|---|---|---|---|---|
| GOAL | -- | "a defined and tested precedence against `typst_authors`" -- the residual is whether the DEFINITION is accurate at pipeline scope | 1, 2 | COVERED |
| REQ | CONF-09 | same clause; the only one `44.2-VERIFICATION.md` leaves unmet | 1, 2, 3 | COVERED |
| ROADMAP SC | SC#2 | "the chosen order is stated in the phase's own artifacts ... and each rule has a test" | 2 (grading), 3 (clause-to-test traceability) | COVERED |
| ROADMAP SC | SC#6 | full suite + `black`/`ruff`/`mypy` + docs build green | 3 | COVERED |
| VERIFY | missing 1 | correct or remove the unscoped clause at `configuration.rst:194` | -- | COVERED (plan 06 task 3); verified by measurement here, not assumed |
| VERIFY | missing 2 | that line (or its replacement) as its OWN graded row, with a three-knob falsification row | 1 (row space), 2 (the graded row) | COVERED |
| VERIFY | missing 3 | a named test exercising the three-knob scenario through `render()` | 3 (traceability table names it and re-runs it) | COVERED (plan 06 task 1) |
| VERIFY | backstop | plan 05's failed backstop must-have: every published sentence enumerated with a verdict AND a named falsification row | 1, 2 | COVERED |
| RESEARCH | GATE-EVIDENCE artifact shape | worktree-isolation proof first, then verbatim command/output pairs, no transcribed figures | 1, 3 | COVERED |
| RESEARCH | Package Legitimacy Audit | no package-manager install task in this plan; nothing to gate | -- | N/A |
| RESEARCH | Architectural Responsibility Map | this plan writes an evidence document and at most a prose fix; no tier assignment changes | -- | N/A |
| CONTEXT | D-04 | `typst_template_function`'s dict-form params win -- the stage-2 rule every §4 row is falsified against | 1, 2 | COVERED |
| CONTEXT | D-05 | the mapping-stage reorder -- the stage-1 rule | 1, 2 | COVERED |
| CONTEXT | D-11 | `configuration.rst` publishes the precedence decided here -- this plan grades whether it does | 2 | COVERED |
| CONTEXT | D-06 | `CONF-10` Future Requirement entry, corrected by plan 05; NOT re-edited here | -- | COVERED (plan 05) |
| CONTEXT | Deferred Ideas | none re-entered | -- | EXCLUDED |

No `⚠ MISSING` rows. `api-coverage`: no external API integration -- the reasoned declaration is
"No external API integration: the phase grades published precedence text against this project's own
AST and its own test suite." The schema-push gate finds no ORM file patterns in scope.
</source_audit>

<artifacts_this_phase_produces>
**New file:**
- `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md`

**New identifiers introduced INSIDE that evidence file** (documentary, not code): the sentence ids
`S-01`, `S-02`, ... assigned in §3 and reused in §4; the row ids of §2's generated table, formed as
`<source>-to-<target>-<present|absent>-<tf|notf>` where the final component records whether
`typst_template_function`'s `params["authors"]` was set; and the verdict tokens `ACCURATE`,
`NARROWER-THAN-CODE`, `FALSE-AT-PIPELINE-SCOPE`.

**Contingent, only if this plan's own grading forces it:** an edit to
`docs/source/user_guide/configuration.rst`, and — if the fix needs a new drift guard — one added
test function in `tests/test_authors_pipeline_stage_gate.py`. Both are declared in `files_modified`
so the executor is not blocked mid-run; if §4 grades every sentence ACCURATE on the first pass,
neither file is touched and that fact is recorded in §7.

**No new production symbols.** No new module under `typsphinx/`, no new config value, no new
fixture, no new dependency.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Re-derive the stage set from the committed code, and generate the widened falsification row space</name>

  <precondition>The worktree has its own environment: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` has been run, and `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"` prints a path inside THIS worktree, not `/home/yuta/Documents/typsphinx/typsphinx`. If it prints the main checkout, STOP — the PEP-660 editable finder is resolving to the main tree and every probe below would measure the wrong files. Plans 03, 04 and 05 all hit the NixOS generic-linux-ELF hazard on `.venv/bin/uv` and `.venv/bin/ruff`; the recorded fix is in `44.2-GATE-EVIDENCE-04.md` §0. Apply it and record what you applied. Additionally: plan 06's commits MUST be present in this worktree — assert that `docs/source/user_guide/configuration.rst` contains the string `Two stages decide what reaches the template` and that `tests/test_authors_pipeline_stage_gate.py` exists. If either is absent, plan 06 has not merged and this plan must not run: halt and report, rather than grading a file that was never corrected.</precondition>

  <reversibility rating="reversible">An evidence document plus AST probes; a single `git revert` removes it with no data, schema, API or published-behaviour consequence.</reversibility>

  <files>.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md</files>

  <read_first>
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md` §1, §2 and §3 in full. §1 enumerated three writers inside one function; §2 generated rows over `source x target x presence`; §3 graded nine sentences and every one of its `FALSIFY-ROW` cells names a §2 row. Understand precisely why that combination cannot falsify a claim about a later stage BEFORE designing §1 and §2 here — this file is the specification of what not to reproduce.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md` sections 0-2 — the established evidence-file shape for this phase: worktree-isolation proof first, then command/output pairs with verbatim output and no transcribed figures.
    - `typsphinx/template_engine.py` lines 484-535 and 690-704 — every `params[...]` assignment in `map_parameters()`, and `render()`'s `all_params` merge with its two `.update(` calls in source order. The AST probe in §1 must find these by walking, not by line number.
    - `typsphinx/writer.py` lines 255-335 — `sphinx_metadata` construction, the `map_parameters()` call and `params.update(toctree_options)` at line 330. `writer.py` is in the walk's scope; a probe that only parses `template_engine.py` reproduces round 3's frame error at one module's granularity.
    - `typsphinx/template_engine.py` lines 205-260 — `__init__`'s resolution of `typst_template_function` into `self.typst_template_params`, so §2's `tf`/`notf` axis is constructed from what the code actually reads (dict form with a `params` key vs. anything else).
    - `tests/test_authors_pipeline_stage_gate.py` as plan 06 left it — read `EXPECTED_STAGE_SITES`, `REACHABLE_STAGE_KNOBS` and `UNREACHABLE_STAGE_PROOFS`. Read them to COMPARE against your own independently-run walk, never to copy: §1's authority is the walk you run in this session, and a disagreement between the two is a finding to record, not an error to smooth over.
  </read_first>

  <action>
Set `GAP_BASE_R4=e9771569bc075673de2bddf67da2f72f9389299b`. Assert `git merge-base --is-ancestor $GAP_BASE_R4 HEAD`; if it fails, recompute the round-4 base as the commit plan 06 built on, use that value everywhere below and in every hardcoded SHA in the `<verify>` blocks, and record the substitution and its reason in §0 — never silently proceed with a base that does not match.

Create `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md`. Do NOT name it `44.2-VERIFICATION.md` — that name is owned and overwritten wholesale by `/gsd-verify-phase`. Every command output in this file is pasted verbatim from a command run in THIS task's session; transcribe no figure from this plan, from plan 06, from `44.2-RESEARCH.md`, from `44.2-VERIFICATION.md`, or from `44.2-GATE-EVIDENCE-01..05.md`.

Write §0, §1 and §2 in this task. §3 onwards is task 2's.

**§0 Provisioning, isolation and baseline.** The resolved `typsphinx.__file__` path proving worktree isolation; the sync command used; any NixOS ELF workaround applied; the pre-change full-suite counts measured in this session with `uv run python -m pytest -q`; `GAP_BASE_R4` with the verbatim output of `git merge-base --is-ancestor` (or the substituted value and why); and the verbatim output of the two plan-06-presence assertions from this task's precondition. State explicitly that every count later in this file is a delta against the baseline recorded here, never against an assumption that a failure belongs to this plan.

**§1 Stage enumeration, from the code, across the whole pipeline.** Paste a runnable `uv run python` AST probe and its verbatim output. The probe parses BOTH `typsphinx/template_engine.py` and `typsphinx/writer.py`, tracks the enclosing `FunctionDef` for every node, and reports every site that writes into a template-parameter carrier: each `Assign` whose target is a `Subscript` on a `Name` whose id is in the carrier set (`params`, `all_params`), and each `Call` to `.update(...)` whose receiver is such a `Name`. For each finding print the module path, the enclosing function, the line number, the kind, and the slice or the unparsed first argument. Also print `sorted(ELEMENTS_ALLOWLIST)` and the literal key set returned by `extract_toctree_options()`, both derived by probe rather than by reading.

Then state, in prose keyed to that output, which sites can determine the emitted `authors` and under exactly what guard, and which cannot and by what proof. **This section, not any config-shape list and not any prose paragraph, is the authority for §2, §3 and §4.** Close §1 by comparing your walk's result against plan 06's `EXPECTED_STAGE_SITES`; if they disagree in either direction, record the difference verbatim and treat YOUR walk as authoritative for the rest of this file.

**§2 The generated falsification row space, widened along the STAGE axis.** Paste a runnable `uv run python` probe and its verbatim output. Its rows are the cross product of: mapping source key (drawn from the keys of a realistic `sphinx_metadata`), mapping target key (`authors`, `doc_authors`, `writer`), source-present vs source-absent, and — the axis round 3 did not have — `typst_template_function` carrying `params["authors"]` vs not carrying it. `typst_authors` is set on every row. For each row construct the engine once, call `map_parameters()`, then call `render()` on its result, and print: the row id (`<source>-to-<target>-<present|absent>-<tf|notf>`), the mapping, the resolved `parameter_mapping`, the `params["authors"]` value after stage 1, whether the seed survived stage 1, and whether the seed's author name appears in the rendered output. **The row set must be produced by the loop, not typed by hand** — typing rows by hand is how the falsifying row was omitted three times.

Then assert in §2, from the printed table itself and not from recall, that it contains: a row whose source key is not `"author"` and whose target is `"authors"`; a row whose source is `"author"` and whose target is not `"authors"`; and — the row this whole round exists for — a row where the seed SURVIVES stage 1 and does NOT appear in the rendered output because the `tf` axis is set. Name that row's id explicitly; §4 will cite it.

Do not write §3 or later in this task. Do not edit `docs/source/user_guide/configuration.rst`, `tests/`, `typsphinx/` or `.planning/REQUIREMENTS.md` in this task.
  </action>

  <verify>
    <automated>
EV=.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md &&
test -f "$EV" &&
grep -qE '^## §0' "$EV" && grep -qE '^## §1' "$EV" && grep -qE '^## §2' "$EV" &&
grep -qF 'self.typst_template_params' "$EV" &&
grep -qF 'toctree_options' "$EV" &&
grep -qF 'writer.py' "$EV" &&
uv run python -c "import re,sys;t=open('.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md',encoding='utf-8').read();s2=t.split('## §2')[1];ids=set(re.findall(r'\b[a-z_]+-to-[a-z_]+-(?:present|absent)-(?:tf|notf)\b',s2));assert len(ids)>=8,('too few generated row ids in §2 -- the table must be produced by a loop over the widened axes',sorted(ids));assert any(i.endswith('-tf') for i in ids) and any(i.endswith('-notf') for i in ids),'§2 lacks the template_function stage axis';print('row-ids',len(ids))" &&
grep -qF 'Two stages decide what reaches the template' docs/source/user_guide/configuration.rst &&
test -f tests/test_authors_pipeline_stage_gate.py &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- typsphinx/ .planning/REQUIREMENTS.md docs/source/user_guide/templates.rst)"
    </automated>
  </verify>

  <acceptance_criteria>
    - `44.2-GATE-EVIDENCE-06.md` exists at the phase-directory path and contains headings `## §0`, `## §1` and `## §2`.
    - §0 records the resolved worktree `typsphinx.__file__` path, the verbatim `git merge-base --is-ancestor` result for `<GAP_BASE_R4>`, the verbatim pre-change full-suite counts measured in this session, and the verbatim output of both plan-06-presence assertions.
    - §1's probe output names sites in BOTH `typsphinx/template_engine.py` AND `typsphinx/writer.py` — a probe that parses only one module fails this criterion.
    - §1's probe output includes an entry whose unparsed update argument is `self.typst_template_params` and an entry whose unparsed update argument is `toctree_options` — the two stages entirely outside round 3's frame.
    - §1 prints `sorted(ELEMENTS_ALLOWLIST)` and `extract_toctree_options()`'s literal key set as probe output, not as prose claims.
    - §1 closes with an explicit comparison against plan 06's `EXPECTED_STAGE_SITES` and states which is authoritative here (this session's walk), recording any difference verbatim.
    - §2's table is generated by a pasted loop; at least 8 distinct row ids matching `<source>-to-<target>-<present|absent>-<tf|notf>` appear in §2, including at least one `-tf` id and at least one `-notf` id.
    - §2 names, by row id, a row where the seed survives stage 1 AND the seed's author name is absent from the rendered output. That row id is quoted in §2's prose so §4 can cite it.
    - §2 also names, by row id, a row whose source key is not `"author"` with target `"authors"`, and a row whose source is `"author"` with a target other than `"authors"`.
    - No figure in §0-§2 is transcribed from `44.2-VERIFICATION.md`, `44.2-REVIEW.md`, `44.2-GATE-EVIDENCE-01..05.md` or any plan; every number is accompanied by the command that produced it.
    - `git diff --name-only <GAP_BASE_R4> --` is empty for `typsphinx/`, `.planning/REQUIREMENTS.md` and `docs/source/user_guide/templates.rst`.
    - The file is NOT named `44.2-VERIFICATION.md` and no file of that name was created or modified.
  </acceptance_criteria>

  <done>
The exploration set for this round is computed from the committed code, in this session, across both
pipeline modules — and the falsification row space now has an axis for the stage whose absence made
round 3's nine ACCURATE verdicts unfalsifiable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Derive the sentence set from the stage set, grade every sentence against per-stage falsification rows, and enforce equality mechanically</name>

  <files>.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md, docs/source/user_guide/configuration.rst, tests/test_authors_pipeline_stage_gate.py</files>

  <read_first>
    - `docs/source/user_guide/configuration.rst` in full as it now stands — not the region plan 06 edited, the WHOLE file. The sweep's scope is "every sentence that constrains when a stage writes `authors`, or denies that another stage writes it", and a sentence qualifies by what it claims, not by where a plan happened to work. Read the five-element contract (34-68), the Template Configuration section (70-149), the Author Information section, and anything downstream that mentions `typst_authors`, `typst_template_function` or `typst_template_mapping`.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md` §3 — again, and specifically its Line column. Rows cite ranges like `189-217` and `49-60`. That is the defect: a range can contain an ungraded claim-bearing sentence and still show a green verdict. Every row you write cites one integer.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md` — the `gaps:` block's `missing:` list and the "Plan 05's Own Backstop Must-Have" table. `missing:` bullet 2 is the specific obligation this task discharges; the backstop table states exactly how the previous mechanism failed.
    - `44.2-GATE-EVIDENCE-06.md` §1 and §2 as task 1 left them — the stage list and the row ids. §3's sweep is derived from §1's stage list; §4's falsification cells cite §2's row ids.
    - `tests/test_params_authors_writers.py` lines 248-280 — the two drift guards over `configuration.rst` and over the `map_parameters()` comment region. Any residual fix must keep them green by satisfying them, never by relaxing them.
    - `typsphinx/template_engine.py` lines 428-466 — the D-05 comment block. It states the rule at `map_parameters()` scope. It is a published claim about a stage and therefore a candidate for §3's inventory; grade it, and if it is graded short, note that a fix there is a COMMENT-only edit and must preserve `ast.parse` equality.
  </read_first>

  <action>
Continue `44.2-GATE-EVIDENCE-06.md` with §3, §4 and §5.

**§3 Sentence inventory, derived from §1's stage list outward.** For each stage in §1, ask: which published sentences constrain when THIS stage writes `authors`, and which sentences deny that some OTHER stage writes it? Sweep `docs/source/user_guide/configuration.rst` in full, plus `typsphinx/template_engine.py`'s D-05 comment region, for sentences answering either question. Paste the runnable splitter you used (a `uv run python` snippet that reads the file, tracks physical line numbers, and emits candidate sentences with their starting line) and its verbatim output; do not split by hand.

Record the inventory as a table with columns `| S-NN | File | Line | Sentence (quoted verbatim) | Stage(s) it claims about |`. Rules, all enforced:

- `Line` is a SINGLE integer — the physical line on which the quoted sentence begins. Never a range.
- If a paragraph contains more than one claim-bearing sentence, EACH gets its own `S-NN` row. A paragraph is never a row.
- A sentence qualifies by what it claims about a stage. Explicitly note in §3's preamble that qualification does NOT depend on whether plan 05, plan 06 or any other plan edited the sentence — and state that this is the inverted derivation, with the round-3 counter-example named.
- Record NOT-instances in a short separate list with the reason (template-file routing, a pointer to the rule rather than a restatement, a bare config example), so the exclusions are visible rather than silent.

**§4 Grading, with a named falsification row per STAGE.** One row per `S-NN` from §3, in the same id order, with columns `| S-NN | Line | Verdict | FALSIFY-ROW (mapping stage) | FALSIFY-ROW (render stage) |`. Rules:

- `Verdict` is exactly one of `ACCURATE`, `NARROWER-THAN-CODE`, `FALSE-AT-PIPELINE-SCOPE`.
- BOTH falsification cells are non-empty for every row. Each names a §2 row id and the observed value from §2's output that shows the sentence holds (or fails) at that stage. A row with an empty render-stage cell is unfinished, not a pass — that emptiness is exactly what round 3 shipped, since all nine of its cells were stage-1 rows.
- For any sentence making a universal, exhaustive or negative-existential claim, the falsification row chosen must be one whose shape the sentence's own wording would predict WRONGLY if the sentence were one predicate or one stage short.
- The sentence at `docs/source/user_guide/configuration.rst` line 194 as it stood at `<GAP_BASE_R4>`, or the sentence that now occupies that position, MUST appear as its own `S-NN` row (`44.2-VERIFICATION.md` `missing:` bullet 2). Obtain the old text mechanically for the record — `git show <GAP_BASE_R4>:docs/source/user_guide/configuration.rst | sed -n '190,200p'` — and quote it from that output rather than from memory. Its render-stage falsification cell must cite the §2 row combining `typst_authors` + an authors-omitting mapping + `typst_template_function`'s `params["authors"]`.

**If any row comes out `NARROWER-THAN-CODE` or `FALSE-AT-PIPELINE-SCOPE`:** fix the sentence, then re-run §3's splitter and §4's grading against the fixed file and record BOTH passes in §4 (a "first pass" table and a "after fix" table), naming what changed between them. Do not record such a verdict as an accepted defect, a known limitation, or a follow-up. Constraints on the fix: it names the stage it quantifies over; it introduces no exhaustive quantifier over config shapes and no hedge; it keeps `tests/test_params_authors_writers.py::test_configuration_rst_documents_both_target_key_shapes`'s three literals and plan 06's positive literals intact; and if the fix lands in `typsphinx/template_engine.py`'s D-05 comment it is comment-only and `ast.parse`-verified. If the fix needs a new drift guard to keep it from reverting, add ONE test function to `tests/test_authors_pipeline_stage_gate.py` and name it in §6's traceability table.

**§5 No-silent-drop equality, mechanically enforced.** Paste a runnable `uv run python` assertion, and its verbatim output, that reads `44.2-GATE-EVIDENCE-06.md` itself, extracts the set of `S-NN` ids appearing in §3 and the set appearing in §4, and asserts the two sets are non-empty and equal, printing the symmetric difference in both directions on failure. Also assert in the same probe that every §4 row's `Line` cell parses as a single integer and that no §4 row has an empty falsification cell in either stage column. This is the equality check as an executed assertion; a prose claim that "all sentences were graded" does not satisfy this section.

Close §5 with the two counts as printed by the probe: sentences surfaced in §3, rows graded in §4, and the statement that they are equal because the probe asserted it.

Do not edit `.planning/REQUIREMENTS.md`, `docs/source/user_guide/templates.rst`, `typsphinx/writer.py`, any `44.2-0[123456]-PLAN.md`, any `-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, or `44.2-GATE-EVIDENCE-01..05.md`.
  </action>

  <verify>
    <automated>
EV=.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md &&
grep -qE '^## §3' "$EV" && grep -qE '^## §4' "$EV" && grep -qE '^## §5' "$EV" &&
uv run python - "$EV" <<'PY' &&
import re, sys
t = open(sys.argv[1], encoding="utf-8").read()
s3 = t.split("## §3")[1].split("## §4")[0]
s4 = t.split("## §4")[1].split("## §5")[0]
a, b = set(re.findall(r"S-\d\d", s3)), set(re.findall(r"S-\d\d", s4))
assert a and a == b, ("sentence-inventory/grading id mismatch", sorted(a - b), sorted(b - a))
rows = [r for r in s4.splitlines() if re.match(r"^\|\s*S-\d\d\s*\|", r)]
assert rows, "no graded rows found in §4"
for r in rows:
    c = [x.strip() for x in r.strip().strip("|").split("|")]
    assert re.fullmatch(r"\d+", c[1]), ("line cell is not a single integer", r)
    assert c[3] and c[4], ("empty falsification cell", r)
    assert c[2] in ("ACCURATE", "NARROWER-THAN-CODE", "FALSE-AT-PIPELINE-SCOPE"), ("bad verdict token", r)
print("S-ids", len(a), "graded rows", len(rows))
PY
! grep -qE '^\|\s*S-\d\d\s*\|[^|]*\|[^|]*(NARROWER-THAN-CODE|FALSE-AT-PIPELINE-SCOPE)' "$EV" &&
! grep -qF 'nothing else in typsphinx' docs/source/user_guide/configuration.rst &&
! grep -qF 'decided by one thing only' docs/source/user_guide/configuration.rst &&
! grep -qEi 'nothing (else )?(in|about) typsphinx|no other part of typsphinx|typsphinx never (replaces|overrides)' docs/source/user_guide/configuration.rst &&
! grep -qEi '(usually|typically|most often|in practice)[^.]*typst_authors' docs/source/user_guide/configuration.rst &&
uv run python -m pytest tests/test_params_authors_writers.py tests/test_authors_pipeline_stage_gate.py tests/test_entry_metadata_precedence.py -q &&
uv run python -c "import ast,subprocess;
base='e9771569bc075673de2bddf67da2f72f9389299b'
for p in ('typsphinx/template_engine.py','typsphinx/writer.py'):
    o=subprocess.check_output(['git','show',base+':'+p]).decode()
    n=open(p,encoding='utf-8').read()
    assert ast.dump(ast.parse(o))==ast.dump(ast.parse(n)), 'executable code changed under typsphinx/: '+p
print('ast-equal')" &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- .planning/REQUIREMENTS.md docs/source/user_guide/templates.rst)"
    </automated>
  </verify>

  <acceptance_criteria>
    - §3's table has one row per claim-bearing sentence, each with an `S-NN` id, a file, a SINGLE-integer line number, the sentence quoted verbatim, and the stage(s) it claims about. No row cites a line range.
    - §3's preamble states that qualification is by what a sentence claims about a stage and NOT by whether any plan edited it, and names the round-3 counter-example (a false clause inside a graded site's own cited line range).
    - §3 includes the runnable splitter and its verbatim output; the sentence list was not produced by hand.
    - §3 lists the NOT-instances with a stated reason each, so exclusions are visible rather than silent.
    - §4 has exactly one row per `S-NN` in §3, in the same id order. The runnable probe in §5 asserts this and its output is pasted.
    - Every §4 row carries a verdict token from `{ACCURATE, NARROWER-THAN-CODE, FALSE-AT-PIPELINE-SCOPE}` and a NON-EMPTY falsification cell for the mapping stage AND for the render stage, each naming a §2 row id and its observed value.
    - A row exists for `configuration.rst` line 194's sentence (as it stood at `<GAP_BASE_R4>`, quoted from `git show` output) or for the sentence that now occupies that position, and its render-stage falsification cell cites the three-knob `-tf` row from §2.
    - `44.2-GATE-EVIDENCE-06.md` contains no `NARROWER-THAN-CODE` or `FALSE-AT-PIPELINE-SCOPE` verdict left open: either every first-pass verdict was `ACCURATE`, or the file carries both a first-pass and an after-fix §4 table and the after-fix table is all `ACCURATE`, with the change between them named.
    - §5's probe is runnable, is pasted with its verbatim output, and asserts (a) `S-NN` set equality between §3 and §4 in both directions, (b) every §4 line cell is a single integer, (c) no §4 falsification cell is empty.
    - NEGATIVE, on the published file: `grep -c 'nothing else in typsphinx'` and `grep -c 'decided by one thing only'` are both `0` in `docs/source/user_guide/configuration.rst`, and the general unscoped-negative-existential regex and the hedge regex both return nothing.
    - `uv run python -m pytest tests/test_params_authors_writers.py tests/test_authors_pipeline_stage_gate.py tests/test_entry_metadata_precedence.py -q` passes with zero failures; `tests/test_params_authors_writers.py` still reports 24 passed and is byte-identical to `<GAP_BASE_R4>` unless a residual fix required a NEW test elsewhere — no existing assertion was weakened, deleted, renamed, `xfail`-ed or `skip`-ped anywhere.
    - `ast.dump(ast.parse(...))` equality holds for `typsphinx/template_engine.py` and `typsphinx/writer.py` against `<GAP_BASE_R4>` — any comment-level residual fix inside the D-05 block changed comments only.
    - `git diff --name-only <GAP_BASE_R4> --` is empty for `.planning/REQUIREMENTS.md` and `docs/source/user_guide/templates.rst`.
    - If any residual fix was made, §4 names the sentence, the verdict that forced it, the fix, and the re-run; if none was made, §4 states that explicitly rather than leaving it implied.
  </acceptance_criteria>

  <done>
Every published sentence that constrains — or denies — when a pipeline stage determines `authors` is
in the inventory with its own single-line citation, is graded against a named falsification row on
BOTH stages, and the inventory-to-grading correspondence is an executed assertion rather than a
claim. Nothing is left graded short.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clause-to-test traceability, scope-fence proofs, and the SC#6 re-take</name>

  <files>.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md</files>

  <read_first>
    - `44.2-GATE-EVIDENCE-06.md` §1-§5 as tasks 1-2 left them — §6's traceability table maps each clause of the published two-stage rule to a pinning test, and each stage in §1 to the test that proves it reachable or unreachable. Read what was actually written, not what was intended.
    - `tests/test_authors_pipeline_stage_gate.py` and `tests/test_entry_metadata_precedence.py` as they now stand — the exact node ids to cite in §6. Obtain them from `--collect-only -q`, never from a plan's prose.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md` — the `gaps:` block this file discharges (SC#2 plus plan 05's backstop must-have, one root cause) and the SC#6 command set the phase is measured with. §7's discharge names both.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-REVIEW.md` — CR-01 (the same finding as the gap, closed here) and WR-01 (the confirmed carry-forward that must be recorded as out of scope, not fixed).
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md` sections on scope-fence proofs — the established form for `git diff` fence evidence in this phase.
  </read_first>

  <action>
Complete `44.2-GATE-EVIDENCE-06.md` with §6 and §7 and the discharge statement.

**§6 Clause-to-stage-to-test traceability.** One table with a row per clause of the published rule and a row per enumerated stage. Clauses: the two-stage ordering itself; the mapping's target must be `"authors"`; the source key must be present; a source routed away from `"authors"` keeps the seed and passes its own key; a non-author source routed into `"authors"` destroys the seed; `typst_package` affects only which mapping is in force; the back-fill cannot replace a seed; `typst_elements` cannot reach the key; `toctree_options` cannot reach the key; `typst_template_function`'s dict-form `params["authors"]` replaces whatever stage 1 produced. Each row names the EXACT test node id that pins it, obtained from a `--collect-only -q` run pasted in this section, plus the verbatim `pytest -q` line proving it passes. Run the whole named set once with `-v` and paste that output. This table is what SC#2's "each rule has a test" now rests on.

Add one short paragraph under the table: **why round 3's mechanism could not have caught this defect, and what changed.** State that all nine of `44.2-GATE-EVIDENCE-05.md` §3's falsification cells named rows from a `map_parameters()`-only table, so no row in that space could distinguish "the seed survives stage 1" from "the seed reaches the emitted output" — and that §2's `tf`/`notf` axis is the specific repair. Name the three failures in sequence (config shapes, then the mapping's source key, then the mapping's target key inside one function) and the frame each shared: the exploration set was derived from the prose being corrected. Do not editorialize beyond what §1-§5 measured.

**§7 Scope fences and the SC#6 re-take.** Paste, verbatim:

- `git diff <GAP_BASE_R4> -- typsphinx/` in full, plus the `ast.parse` equality result for both modules, with the assertion that no executable line changed.
- `git diff --name-only <GAP_BASE_R4>` showing that none of `44.2-01/02/03/04/05/06-PLAN.md`, any `-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, `44.2-GATE-EVIDENCE-01..05.md`, `.planning/REQUIREMENTS.md`, or `docs/source/user_guide/templates.rst` appears.
- Confirmation that `.planning/REQUIREMENTS.md`'s `CONF-09` checkbox is still `- [ ]` and its Traceability row still reads `Pending`, quoted from the file — this plan produces evidence, not a seal.
- The full `uv run python -m pytest -q` output, `black --check .`, `ruff check .`, `mypy typsphinx/` (invoked as `uv run python -m <tool>` if a compiled launcher fails to exec), and a docs HTML build with the grep proving no WARNING or ERROR line names `configuration.rst`.
- The new full-suite passed count as measured, with the delta stated against BOTH the 932 recorded at the round-3 base in `44.2-VERIFICATION.md` and the §0 pre-change baseline measured in this session. State the delta as a count of added tests, and name them.

Close with a one-paragraph discharge naming: the FAILED truth from `44.2-VERIFICATION.md` (SC#2) and the FAILED backstop must-have, both discharged; the three `missing:` bullets and where each is closed (bullets 1 and 3 in plan 06, bullet 2 here); the sentences graded and the falsification rows that graded them; and, if FA-04 applied — if this plan made a residual fix and then graded it itself — say so explicitly and state whether a further wave is recommended.

Finally, record the two carry-forwards in a clearly-labelled out-of-scope block, NOT as gaps: `44.2-REVIEW.md` WR-01 (`typsphinx/writer.py:319-321`, introduced in commit `cf22574`, Phase 27.1, 2026-07-25 — a Future Requirement candidate for a builder-robustness phase), and any claim-bearing sentence the §3 sweep found in `docs/source/user_guide/templates.rst` (Phase 45.1 SC#2's to verify, recorded as an input to that phase and deliberately not fixed here).

Do not edit `.planning/REQUIREMENTS.md`, `docs/source/user_guide/templates.rst`, `typsphinx/`, any prior plan, any `-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, or `44.2-GATE-EVIDENCE-01..05.md` in this task.
  </action>

  <verify>
    <automated>
EV=.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-06.md &&
grep -qE '^## §6' "$EV" && grep -qE '^## §7' "$EV" &&
grep -qF 'test_render_template_function_authors_replaces_map_parameters_surviving_seed' "$EV" &&
grep -qF 'test_three_knob_control_build_without_template_function_emits_the_seed' "$EV" &&
grep -qF 'test_pipeline_stage_sites_that_can_determine_authors_are_exactly_these' "$EV" &&
grep -qF 'WR-01' "$EV" &&
uv run python -m pytest -q &&
uv run python -m black --check . &&
uv run python -m ruff check . &&
uv run python -m mypy typsphinx/ &&
uv run python -m pytest "tests/test_entry_metadata_precedence.py::test_render_template_function_authors_replaces_map_parameters_surviving_seed" -q &&
uv run python -c "import ast,subprocess;
base='e9771569bc075673de2bddf67da2f72f9389299b'
for p in ('typsphinx/template_engine.py','typsphinx/writer.py'):
    o=subprocess.check_output(['git','show',base+':'+p]).decode()
    n=open(p,encoding='utf-8').read()
    assert ast.dump(ast.parse(o))==ast.dump(ast.parse(n)), 'executable code changed under typsphinx/: '+p
print('ast-equal')" &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- .planning/REQUIREMENTS.md docs/source/user_guide/templates.rst typsphinx/)" &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-01-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-02-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-03-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-04-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-05-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-REVIEW.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md)" &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-01.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-02.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-04.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md)" &&
grep -qE '^\- \[ \] \*\*CONF-09\*\*' .planning/REQUIREMENTS.md &&
grep -qE '\| CONF-09 \| Phase 44\.2 \| Pending \|' .planning/REQUIREMENTS.md &&
DOCS_OUT="$(uv run --extra dev --extra docs python -m sphinx -b html docs/source "$(mktemp -d)" 2>&1)" &&
printf '%s' "$DOCS_OUT" | grep -q 'build succeeded' &&
! printf '%s' "$DOCS_OUT" | grep -E 'WARNING|ERROR' | grep -q 'configuration.rst'
    </automated>
  </verify>

  <acceptance_criteria>
    - `44.2-GATE-EVIDENCE-06.md` contains headings `## §0` through `## §7` and a closing discharge paragraph.
    - §6's table has one row per named clause AND one row per enumerated stage from §1; every row names a test node id that appears in the `--collect-only -q` output pasted in the same section, and the `-v` run of the whole named set is pasted verbatim showing all PASSED.
    - §6 names at least `test_render_template_function_authors_replaces_map_parameters_surviving_seed`, `test_three_knob_build_emits_template_function_authors_not_the_seed`, `test_three_knob_control_build_without_template_function_emits_the_seed`, `test_pipeline_stage_sites_that_can_determine_authors_are_exactly_these`, `test_reachable_and_unreachable_stages_partition_the_enumeration`, `test_toctree_options_cannot_reach_authors_or_title` and `test_render_applies_template_function_params_after_map_parameters_output`.
    - §6's explanatory paragraph states that all of `44.2-GATE-EVIDENCE-05.md` §3's falsification cells named stage-1-only rows, and names the `tf`/`notf` axis as the repair.
    - `uv run python -m pytest -q` passes with zero failures; the passed count is pasted verbatim in §7 with the delta stated against BOTH 932 and the §0 baseline, and the added tests are named individually so the delta reconciles.
    - `uv run python -m black --check .`, `uv run python -m ruff check .` and `uv run python -m mypy typsphinx/` all exit `0`, with output pasted.
    - The docs HTML build prints `build succeeded` and no line matching `WARNING` or `ERROR` contains `configuration.rst`.
    - `git diff <GAP_BASE_R4> -- typsphinx/` is empty and the `ast.parse` equality result for both modules is pasted.
    - `git diff --name-only <GAP_BASE_R4>` includes NONE of `44.2-0[12345]-PLAN.md`, any `-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, `44.2-GATE-EVIDENCE-01..05.md`, `.planning/REQUIREMENTS.md`, or `docs/source/user_guide/templates.rst`.
    - `.planning/REQUIREMENTS.md` still carries `- [ ] **CONF-09**` and the Traceability row `| CONF-09 | Phase 44.2 | Pending |`, quoted in §7 — no checkbox flip, no row edit.
    - §7's discharge names SC#2 and plan 05's backstop must-have as the two discharged failures, maps each of the three `missing:` bullets to where it is closed, and — if this plan made a residual fix — states FA-04 applied and gives a recommendation.
    - The out-of-scope block records WR-01 with its provenance (commit `cf22574`, Phase 27.1) as a carry-forward, and records any `templates.rst` finding as a Phase 45.1 input, neither of them as a gap of this phase.
    - No file named `44.2-VERIFICATION.md` was created or modified by this plan.
  </acceptance_criteria>

  <done>
Every clause of the published two-stage rule and every enumerated stage has a named passing test
cited by node id from a live collection; the scope fences are proved by pasted `git diff` output; the
phase gate is green against a baseline measured in this plan's own session; and the discharge names
what was closed, by which mechanism, and what deliberately remains a carry-forward.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the enumeration's frame -> the sentences it can grade | the boundary that failed in round 3. An enumeration scoped to one function cannot falsify a claim about a later stage; nine ACCURATE verdicts were produced against a row space in which the false sentence was unfalsifiable |
| the author of a correction -> the grader of that correction | when they are the same agent in the same session, the grader inherits the author's frame. This plan's wave separation is that boundary made structural |
| a cited line RANGE -> the sentences inside it | a range implies coverage it does not provide. Round 3's row 2 cited 189-217 and graded the sentence at 201 while the defect sat at 194 |
| the evidence file -> `/gsd-verify-phase`'s reserved output name | `44.2-VERIFICATION.md` is written by the verifier and clobbers same-named plan output, silently, on the next verify run |
| a transcribed figure -> the measurement it claims to be | a count copied from a previous round's document survives as evidence long after it stopped being true |
| a residual fix -> its own grading | FA-04: if this plan fixes a sentence and then grades it, the wave-separation control does not apply to that fix |

## STRIDE Threat Register (ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44.2.7-01 | Information Disclosure | the published precedence guidance a user configures against | high | mitigate | Every claim-bearing sentence is graded against a named falsification row on BOTH stages, and any non-`ACCURATE` verdict must be fixed and re-graded rather than recorded. Rated `high` — this boundary has failed four times and each failure reached users as published text |
| T-44.2.7-02 | Spoofing | the enumeration itself (an audit that looks complete and is not) | high | mitigate | §5's runnable id-set equality assertion, the single-integer line-cell assertion and the non-empty-falsification-cell assertion are executed and pasted, so "all sentences were graded" is a measured fact rather than a claim. §1's stage walk is re-run in this session rather than copied from plan 06's frozen constant |
| T-44.2.7-03 | Elevation of Privilege | a claim about a stage the row space cannot reach | high | mitigate | §2's row space gains the `tf`/`notf` axis, so a sentence that is true of stage 1 and false of the pipeline now has a row that distinguishes them; §4 requires a render-stage falsification cell on every row |
| T-44.2.7-04 | Repudiation | the phase evidence trail (`GATE-EVIDENCE-01..05`, `REVIEW`, `VERIFICATION`, six PLANs, every SUMMARY) | medium | mitigate | Task 3 asserts `git diff --name-only <GAP_BASE_R4>` lists none of them; the correction is recorded FORWARD in this new file, never by amending the record of what each round published |
| T-44.2.7-05 | Tampering | `.planning/REQUIREMENTS.md` (`CONF-09`'s checkbox and Traceability row) | medium | mitigate | This plan declares zero edits to that file, asserts its diff is empty, and additionally asserts the checkbox is still `- [ ]` and the row still reads `Pending` — the recorded early-flip hazard cannot fire, and the plan does not seal the phase |
| T-44.2.7-06 | Tampering | `typsphinx/` (production files in a contingent-fix plan's blast radius) | medium | mitigate | `ast.dump(ast.parse(...))` equality against `<GAP_BASE_R4>` for both modules in every task's `<verify>`; a residual fix inside the D-05 comment is comment-only by construction and proved so |
| T-44.2.7-07 | Tampering | existing assertions, including plan 06's new gates | medium | mitigate | Task 2 asserts the three precedence-related modules pass and that `tests/test_params_authors_writers.py` still reports 24 passed and is byte-identical unless a NEW test was added elsewhere; a residual fix that would break a drift guard must be rewritten, never the guard relaxed |
| T-44.2.7-08 | Spoofing | a stale figure transcribed into the evidence file | medium | mitigate | A dedicated prohibition forbids transcribing any figure from prior documents, and every acceptance criterion requires the producing command to be pasted alongside its number; §7 states the full-suite delta against two independently measured baselines |
| T-44.2.7-09 | Denial of Service | the published docs page (a malformed list from a residual fix) | low | mitigate | Task 3 runs a real `sphinx -b html` build and requires `build succeeded` with no WARNING/ERROR line naming `configuration.rst` |
| T-44.2.7-SC | Tampering | supply chain / package installs | low | accept | This plan installs nothing. `uv sync --extra dev --extra docs` resolves only already-pinned `pyproject.toml` extras; no dependency is added, removed or version-bumped, and neither `pyproject.toml` nor `uv.lock` appears in `files_modified`. No `## Package Legitimacy Audit` gate is required because no package-manager install task exists |

Three threats are rated `high` (T-44.2.7-01/02/03) and all three are dispositioned `mitigate`.
Nothing is left `accept`-ed at or above `security_block_on: high`; the single `accept`-ed item is
`low`.
</threat_model>

<deferred>
- **WR-01 (carry-forward, NOT a 44.2 gap, NOT planned here).** `typsphinx/writer.py:319-321`'s
  `effective_elements` merge raises an unhandled `TypeError` rather than a graceful `ExtensionError`
  on a non-`dict` `typst_elements`. Introduced in commit `cf22574` (Phase 27.1, 2026-07-25) per
  `44.2-VERIFICATION.md`'s own `git log -S` check. Recorded as a **Future Requirement candidate** for
  a builder-robustness phase; §7 of the evidence file repeats it as an out-of-scope item.
- **`docs/source/user_guide/templates.rst`.** Phase 45.1 SC#2 owns it. If §3's stage-derived sweep
  surfaces a claim-bearing sentence there, it is recorded in §7 as an input to that phase and
  deliberately not fixed — fixing it here would destroy the thing Phase 45.1 exists to check.
- **The `title` analogue.** §1 enumerates `title`'s writers for symmetry, but this plan's prose
  grading is scoped to `authors` claims in `configuration.rst`. A `title` claim outside item 4 that
  §3's sweep surfaces gets a row; anything in `templates.rst` is Phase 45.1's.
</deferred>

<verification>
Phase-level checks after all three tasks:

1. `uv run python -m pytest -q` — full suite green, passed count strictly greater than the §0
   pre-change baseline measured in this plan's own session, with the added tests named.
2. `uv run python -m black --check .`, `uv run python -m ruff check .`,
   `uv run python -m mypy typsphinx/` — all clean.
3. Docs HTML build succeeds with no WARNING/ERROR line naming `configuration.rst`.
4. `44.2-GATE-EVIDENCE-06.md` exists with §0-§7; §5's equality probe passes and its output is
   pasted; no `NARROWER-THAN-CODE` or `FALSE-AT-PIPELINE-SCOPE` verdict is left open; every §4 row
   cites a single-integer line and carries two non-empty falsification cells.
5. `grep -c 'nothing else in typsphinx'` and `grep -c 'decided by one thing only'` are both `0` in
   `docs/source/user_guide/configuration.rst`; the general unscoped-negative-existential regex and
   the hedge regex both return nothing.
6. `ast.dump(ast.parse(...))` equality holds against `<GAP_BASE_R4>` for `typsphinx/template_engine.py`
   and `typsphinx/writer.py`; `git diff --name-only <GAP_BASE_R4> -- typsphinx/` is empty.
7. `git diff --name-only <GAP_BASE_R4>` lists none of `44.2-0[12345]-PLAN.md`, any `-SUMMARY.md`,
   `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, `44.2-GATE-EVIDENCE-01..05.md`,
   `.planning/REQUIREMENTS.md`, or `docs/source/user_guide/templates.rst`.
8. `.planning/REQUIREMENTS.md` still carries `- [ ] **CONF-09**` and `| CONF-09 | Phase 44.2 |
   Pending |`.
9. No file named `44.2-VERIFICATION.md` was created or modified.
</verification>

<success_criteria>
- `44.2-GATE-EVIDENCE-06.md` records an enumeration whose exploration set was computed by reverse
  computation from the code's writers at every pipeline stage, in this plan's own session, across
  both modules — not from the prose being corrected and not from any plan's frozen constant.
- The falsification row space has an axis for the render stage, and every graded sentence carries a
  named falsification row on the mapping stage AND on the render stage. A verdict without both is
  mechanically rejected.
- Every graded row cites a single line, never a range, so a claim-bearing sentence cannot hide inside
  a green row's citation the way it did in round 3.
- The correspondence between sentences surfaced and sentences graded is an executed assertion whose
  output is pasted — `44.2-VERIFICATION.md`'s `missing:` bullet 2, closed, and plan 05's failed
  backstop must-have, discharged.
- Every clause of the published two-stage rule and every enumerated stage has a named passing test,
  cited by node id from a live collection.
- The production tree is byte-unchanged, the historical record is byte-identical,
  `.planning/REQUIREMENTS.md` is untouched and `CONF-09` remains unsealed, and the full SC#6 gate is
  green against a baseline measured in this plan's own session.
</success_criteria>

<output>
Create `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-07-SUMMARY.md` when
done. Record in it: the resolved worktree `typsphinx.__file__` path, the NixOS workaround applied (if
any), the verbatim pre-change and post-change full-suite counts, the number of sentences surfaced in
§3 and rows graded in §4 as printed by §5's probe, whether any residual fix was required (and if so
what it was and whether FA-04's caveat applies), and any disagreement between this session's stage
walk and plan 06's `EXPECTED_STAGE_SITES`.
</output>
