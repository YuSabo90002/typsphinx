---
phase: 44.2-typst-documents-title-and-author-consumption
plan: 06
type: execute
wave: 6
depends_on: ["44.2-01", "44.2-02", "44.2-03", "44.2-04", "44.2-05"]
files_modified:
  - tests/test_authors_pipeline_stage_gate.py
  - tests/test_entry_metadata_precedence.py
  - docs/source/user_guide/configuration.rst
autonomous: true
gap_closure: true
requirements: [CONF-09]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CONF-09 / SC#2 at PIPELINE scope: `docs/source/user_guide/configuration.rst` publishes the `authors` precedence as an ordered TWO-STAGE rule -- the parameter mapping applied inside `map_parameters()`, then `typst_template_function`'s dict-form `params[\"authors\"]` applied later in `render()` -- and every universal or negative-existential claim in the Author Information section and in item 4 of the five-element contract names the stage it quantifies over."
    - "The three-knob configuration `44.2-VERIFICATION.md` reproduced is pinned end to end by named tests: a real `sphinx-build -b typst` on `typst_authors` set + a mapping targeting only `title` + `typst_template_function`'s `params[\"authors\"]` emits the template-function value and not the seed; and a CONTROL build identical except for the absent `typst_template_function` emits the seed -- so the first result is attributable to the render stage alone and not to the mapping."
    - "D-04's render-stage override is also pinned at API level in one body that spans both stages: a named cell asserts the seed SURVIVES `map_parameters()` (`params[\"authors\"]` is the `typst_authors` list of dicts) and is then REPLACED by `render()`'s merge in the same test -- the combination `44.2-VERIFICATION.md` measured as having no single-assertion coverage."
    - "The writer frame is the whole pipeline, not `map_parameters()`: an AST enumeration over `typsphinx/template_engine.py` AND `typsphinx/writer.py` pins every site that can determine the emitted `authors`/`title`, including `writer.py`'s `params.update(toctree_options)` and BOTH of `render()`'s `all_params.update(...)` calls, and fails with a message naming the published prose as the thing to re-derive if a site appears, disappears or moves."
    - "No stage is silently dropped from the frame: every enumerated site is classified either REACHABLE (it can determine `authors`) or UNREACHABLE-WITH-PROOF (a named test in the same module shows it cannot), and an assertion requires the two classes to partition the enumerated set exactly."
    - "The prose is linked to the enumeration mechanically: every REACHABLE stage's user-facing config knob is asserted present in `configuration.rst`'s Author Information section, so a newly-enumerated reachable stage forces a prose update instead of shipping undocumented."
    - "D-05/D-04 behaviour is untouched: `ast.dump(ast.parse(...))` of `typsphinx/template_engine.py` and of `typsphinx/writer.py` is byte-equal to the round-4 gap base, and `render()`'s `all_params.update(params)`-then-`all_params.update(self.typst_template_params)` order is asserted from the source rather than assumed."
    - "SC#6 still holds: full `uv run python -m pytest`, `black --check .`, `ruff check .`, `mypy typsphinx/` are green against a baseline measured in the SAME session, a docs HTML build emits no WARNING/ERROR line naming `configuration.rst`, and collected test counts go up, never down."
    - "The historical record is intact: `44.2-01/02/03/04/05-PLAN.md`, every `-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md` and `44.2-GATE-EVIDENCE-01..05.md` are byte-identical to the round-4 gap base. This round's correction is recorded FORWARD, in plan 07's `44.2-GATE-EVIDENCE-06.md`."
    - statement: "Every published sentence that constrains -- or DENIES -- when any pipeline stage determines `authors` is stage-scoped. Whole-paragraph accuracy at pipeline scope is a judgement no single assertion settles: round 3 shipped a false clause that sat inside a graded line range, because the grader was the author and the frame was the correction set. That is why plan 07, in a LATER wave and with no memory of what this plan intended to write, re-derives the sentence set from the committed code and grades it."
      verification: backstop
  artifacts:
    - tests/test_authors_pipeline_stage_gate.py
    - tests/test_entry_metadata_precedence.py
    - docs/source/user_guide/configuration.rst
  key_links:
    - "the AST stage enumeration over `typsphinx/template_engine.py` + `typsphinx/writer.py` -> the REACHABLE/UNREACHABLE partition -> the stage-knob presence gate over `configuration.rst`'s Author Information section"
    - "`render()`'s `all_params.update(self.typst_template_params)` -- the fourth writer of `params[\"authors\"]`, outside round 3's frame entirely -> the three-knob real `sphinx-build -b typst` gate -> the emitted `index.typ`"
    - "the mapping-targets-nothing-but-title case (the seed's surviving case) -> the CONTROL build with `typst_template_function` removed -> attribution of the first gate's result to the render stage and not to the mapping"
    - "`writer.py`'s `params.update(toctree_options)` -- a fifth writer the enumeration surfaces rather than assumes away -> `extract_toctree_options()`'s returned literal key set -> the UNREACHABLE-WITH-PROOF classification"
  prohibitions:
    - statement: "MUST NOT publish an unscoped negative existential about the whole pipeline -- any sentence of the form 'nothing/no other part of typsphinx replaces/overrides/touches it'. That is the exact grammatical shape of the clause this round closes: it was published at `configuration.rst:194` by round 3's own correction, graded ACCURATE by round 3's own enumeration, and falsified by a real `sphinx-build`. A negative existential is permitted ONLY when it names the stage it quantifies over (e.g. 'no other assignment in that mapping step replaces the seed') and only when a later-stage falsification row exists for it."
      status: unverified
      verification: test
    - statement: "MUST NOT derive the set of sentences to correct from the set of passages this phase has edited. That frame is the measured root cause of round 3: only 3 of its 9 graded sites were in `configuration.rst`, and the false clause lay INSIDE graded site #2's own cited line range while a neighbouring sentence was the one quoted and graded. The set is derived from the CODE's stage enumeration outward: a sentence qualifies by what it claims about a stage, never by whether any plan touched it."
      status: unverified
      verification: test
    - statement: "MUST NOT publish any exhaustive quantifier over config SHAPES for the survives-case (of the form 'exactly N configurations/shapes/routes'), and MUST NOT hedge one with 'usually', 'typically', 'most often' or 'in practice'. The only exhaustive statements permitted are the iff over the mapping predicate (scoped to the mapping stage) and the ordered two-stage list itself; individual config shapes are ILLUSTRATIONS of the rule, never an enumeration of it. A hedge is worse than a wrong quantifier because no test can falsify it."
      status: unverified
      verification: test
    - statement: "MUST NOT change any executable line under `typsphinx/`. `map_parameters()` and `render()` are both CORRECT and have been throughout this phase -- four rounds of failure are all text failures. This is a patch release already carrying two user-visible behaviour changes; a runtime change smuggled in under a docs/test banner would ship untested behaviour. Verified by `ast.dump(ast.parse(...))` structural equality against the gap base for BOTH `template_engine.py` and `writer.py`."
      status: unverified
      verification: test
    - statement: "MUST NOT weaken, delete, rename, `xfail`, `skip` or loosen any existing assertion anywhere in the suite to accommodate the corrected prose. In particular `tests/test_params_authors_writers.py::test_configuration_rst_documents_both_target_key_shapes` asserts three literals are present in `configuration.rst`; the rewrite keeps all three rather than the gate being relaxed. Collected counts go up, never down."
      status: unverified
      verification: test
    - statement: "MUST NOT edit, amend or re-word `44.2-GATE-EVIDENCE-01..05.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, any `44.2-0[12345]-PLAN.md`, or any `-SUMMARY.md`. They are the record of four rounds of one failure mode; erasing or softening them destroys the only evidence of what was published, reviewed and measured each round. The correction is recorded FORWARD in `44.2-GATE-EVIDENCE-06.md` (plan 07)."
      status: unverified
      verification: test
    - statement: "MUST NOT name any plan-authored artifact `44.2-VERIFICATION.md`. That filename is owned and overwritten wholesale by `/gsd-verify-phase`, so plan-authored evidence there is lost silently on the next verify run."
      status: unverified
      verification: test
    - statement: "MUST NOT edit `.planning/REQUIREMENTS.md` at all in this plan. In particular MUST NOT flip `CONF-09`'s `- [ ]` checkbox and MUST NOT touch the Traceability table row `CONF-09 | Phase 44.2 | Pending`. `phase.complete` has a recorded habit of flipping such rows early on this project; a plan doing it by hand would make the phase look sealed while a gap is open. Sealing is verify-phase's call."
      status: unverified
      verification: test
    - statement: "MUST NOT edit `docs/source/user_guide/templates.rst`. Phase 45.1 SC#2 owns it: its job is to CHECK whether that file's `title` claim became true, which it cannot do if this phase rewrites it first. It was measured this session to carry no precedence prose at all -- its `authors` mentions are template-parameter listings -- so nothing in this round's scope reaches it."
      status: unverified
      verification: test
    - statement: "MUST NOT 'correct' the accurate template-file-ROUTING sentences elsewhere in the repo as collateral: `typsphinx/template_engine.py`'s `uses_bundled_default_template()` docstring, `tests/test_entry_metadata_route_uniformity.py` (5 occurrences), `tests/test_package_only_config_gate.py`, `tests/test_package_template_routing.py`, `tests/test_examples_charged_ieee_gate.py`, both fixture `conf.py` files, `typsphinx/writer.py`, `typsphinx/builder.py`. They describe a different subject and are correct. Negative gates over `typsphinx/template_engine.py` must therefore be region-scoped, never file-wide."
      status: unverified
      verification: test
    - statement: "MUST NOT write any expected value in the new module by calling the code under test. The stage enumeration's expectation is a frozen, hand-declared tuple that the AST walk is compared AGAINST; the prose gate's knob list is hand-declared with a stated reason per entry. Computing either from the code's own output would make the gate a tautology that passes for any pipeline, including one with an unenumerated fifth stage."
      status: unverified
      verification: test
    - statement: "MUST NOT plan, fix or touch `44.2-REVIEW.md`'s WR-01 (`typsphinx/writer.py:319-321`'s `effective_elements` merge raising an unhandled `TypeError` on a non-`dict` `typst_elements`). It was measured to predate this phase -- introduced in commit `cf22574`, Phase 27.1, 2026-07-25 -- and is a confirmed carry-forward, not a 44.2 gap. It is recorded in this plan's deferred section as a Future Requirement candidate only."
      status: unverified
      verification: test
---

<objective>
Close the single remaining gap in `44.2-VERIFICATION.md` (status `gaps_found`, 5/6): ROADMAP SC#2
fails, together with plan 05's own backstop must-have, on one root cause. `configuration.rst:194`
publishes an unscoped negative existential about the `typst_authors` seed -- a claim that no other
part of typsphinx replaces it -- which is false, because `typst_template_function`'s dict-form
`params["authors"]` is applied later, in `render()`, and does replace it. The verifier reproduced
this with a real `sphinx-build -b typst`.

**The structural cause, measured, is the FRAME -- not diligence.** Round 3 built a per-sentence
enumeration specifically to break this cycle and it failed the same way: its site set was derived
from the passages plan 05 had edited (only 3 of 9 graded sites were in `configuration.rst`), and its
row matrix was scoped to `map_parameters()`'s three writers. `render()`'s
`all_params.update(self.typst_template_params)` -- a fourth writer of the same key, at a later
stage -- was outside the frame entirely, so a clause making an unscoped claim about that stage could
not be caught by anything round 3 built. `grep -in "nothing else" 44.2-GATE-EVIDENCE-05.md` returns
zero hits: the sentence that instantiated the defect was never quoted, never graded, never
falsified.

This plan inverts the derivation. It enumerates the WRITERS first, from the code, across the whole
pipeline (`writer.py`'s `sphinx_metadata` construction and `params.update(toctree_options)`;
`map_parameters()`'s four assignment sites; `render()`'s two merges), classifies each as REACHABLE
or UNREACHABLE-WITH-PROOF with a partition assertion so none is dropped, and only then derives the
prose that must be true. The correction is published as an ordered TWO-STAGE rule, and the exact
three-knob configuration that falsified round 3 becomes a named passing gate at two levels: a real
`sphinx-build -b typst` with a control, and an API-level cell spanning `map_parameters()` and
`render()` in one body.

Purpose: make SC#2 true at pipeline scope, and make a fifth-stage regression fail in CI rather than
at a fifth verify round.

Output: `tests/test_authors_pipeline_stage_gate.py` (new); one new Group 3 cell in
`tests/test_entry_metadata_precedence.py`; a rewritten Precedence block and a stage-scoped item-4
bullet in `docs/source/user_guide/configuration.rst`.

Scope fence: no runtime behaviour change (`map_parameters()` and `render()` are both correct and
stay untouched); no `.planning/REQUIREMENTS.md` edit at all; no `templates.rst` edit (Phase 45.1
SC#2's); no edit to any prior plan, SUMMARY, REVIEW, VERIFICATION or GATE-EVIDENCE file. The
independent grading of the corrected prose is plan 07's, in wave 7, deliberately not this plan's.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-REVIEW.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-CONTEXT.md
@.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-PATTERNS.md
@typsphinx/template_engine.py
@typsphinx/writer.py
@docs/source/user_guide/configuration.rst
@tests/test_params_authors_writers.py
@tests/test_entry_metadata_precedence.py
</context>

<measured_grounding>
Every figure below was measured in this planning session by direct execution against the shipped
tree at `GAP_BASE = e9771569bc075673de2bddf67da2f72f9389299b` (round-4 gap base = the `HEAD` this
plan was written against). Re-derive rather than trust -- but §1 is the whole point of this round
and must not be shortened into a hand-list.

## 1. The pipeline's `authors` / `title` writers, enumerated across ALL stages

Round 3 enumerated three writers inside `map_parameters()`. The pipeline has more. Mechanically
(`grep -rn 'params\[' typsphinx/*.py` plus `grep -rn '\.update(' typsphinx/*.py`, then read):

| Stage | Site | File:line at `GAP_BASE` | Can it determine emitted `authors`? |
|---|---|---|---|
| S0 | `sphinx_metadata` construction via `_resolve_entry_element` | `writer.py:272-280` | Indirectly -- it supplies the VALUE S2 can write. Not a `params` writer. |
| W1 | `params["authors"] = [...]` (the `typst_authors` seed) | `template_engine.py:485` | YES -- knob `typst_authors` |
| W2 | `params[template_key] = value` (mapping loop) | `template_engine.py:499` | YES -- knob `typst_template_mapping` |
| W3 | `params["title"]="" / params["authors"]=() / params["date"]=None` (non-package back-fill) | `template_engine.py:508/510/512` | YES but fill-only -- guarded by `"authors" not in params`; knob `typst_package` |
| W4 | `params[key] = ...` (`typst_elements` loop) | `template_engine.py:531` | NO -- `ELEMENTS_ALLOWLIST` is `fontsize`/`lang`/`papersize`; an unknown key raises `ExtensionError` |
| W5 | `params.update(toctree_options)` | `writer.py:330` | NO -- `extract_toctree_options()` returns only `toctree_maxdepth`/`toctree_numbered`/`toctree_caption`, or `{}` |
| W6 | `all_params.update(params)` | `template_engine.py:696` | Carries W1-W5's result forward; no knob of its own |
| W7 | `all_params.update(self.typst_template_params)` | `template_engine.py:697` | **YES -- knob `typst_template_function` (dict form). THE WRITER ROUND 3'S FRAME EXCLUDED.** |

W5 is the concrete instance of the orchestrator's "if a fifth writer exists, the enumeration must
surface it rather than the planner assuming there are four". It exists, it is a real `params`
writer, and it is unreachable for a reason that must be PROVEN (its key set), not asserted.

## 2. Why the prose set is bigger than the one sentence the verification names

The verification's `missing:` bullet 1 names `configuration.rst:194`. Deriving the set from the
stage list instead surfaces neighbours in the SAME paragraph and in item 4 of the five-element
contract that make universal or negative-universal claims whose quantification domain is the whole
pipeline rather than the mapping stage: the block's opening "one question decides this" (189-191),
the iff sentence's "survives as the sole source of ``authors``" (201-203), "governed by the one
rule above ... never whether the seed is replaced" (222-225), and item 4's "decided by one thing
only" (51-52). Each is TRUE at mapping-stage scope and each is FALSE or misleading at pipeline
scope. Fixing only line 194 and leaving its neighbours is precisely how rounds 1, 2 and 3 each
produced the next round's defect. Task 3 therefore rewrites the block as an ordered two-stage rule
rather than patching one clause -- and plan 07 grades the result independently.

## 3. Existing coverage, and the hole

`tests/test_entry_metadata_precedence.py` Group 3 already has
`test_render_explicit_template_function_authors_wins_over_entry_derived`, and
`tests/test_template_engine.py` has `test_explicit_template_function_params_win_on_colliding_key`.
Both HAND-BUILD their `params` dict and call `render()` directly -- neither runs `map_parameters()`,
so neither exercises a `typst_authors` seed that SURVIVED the mapping and is then replaced at render
time. That is the exact combination `44.2-VERIFICATION.md` records as having no single-assertion
coverage, and the combination its own `sphinx-build` reproduction used.

## 4. Existing gates the rewrite must not break

`tests/test_params_authors_writers.py::test_configuration_rst_documents_both_target_key_shapes`
whitespace-normalizes `configuration.rst` and requires all three of
``no entry of the active mapping targets ``authors```, `{"author": "doc_authors"}` and
`{"project": "authors"}` to be present. The rewrite in task 3 preserves all three verbatim.
No other test in the suite constrains `configuration.rst`'s content (measured:
`grep -rn "configuration.rst" tests/*.py`).
</measured_grounding>

<assumption_delta_decision>
The `assumption-delta` detector's `pluralization` family fires on this round's own subject matter:
this phase has modelled "the thing that decides `authors`" as a singular for four rounds, and this
round makes it plural.

- **The noun that is now primary:** *the pipeline STAGE that writes `authors`*. Round 1 modelled the
  decision as "the config shape / template route"; round 2 as "the mapping's SOURCE key"; round 3 as
  "the mapping's TARGET key". All three are now demoted to derived details of ONE stage -- the
  mapping applied inside `map_parameters()`. `render()`'s `typst_template_params` merge is a second,
  co-equal stage that no previous round's identity model had a slot for.
- **Decision: `promote`.** The stage list becomes the primary statement in the published prose and
  in the test frame; the target-key iff survives as the rule *of stage 1*, correctly scoped, not as
  the rule of the whole pipeline. Adding the render stage alongside a still-unscoped stage-1 rule --
  which is what round 3 did when it appended "`typst_template_function`'s dict-form `params` take
  precedence over both" three paragraphs below an unscoped denial -- is exactly what let the
  contradiction ship inside one document section.
- **Companion invariant test, accepted:**
  `test_pipeline_stage_sites_that_can_determine_authors_are_exactly_these` plus the
  REACHABLE/UNREACHABLE partition assertion encode the generalized intent. They go red the instant a
  future change adds a stage, so a sixth writer cannot land silently the way the fourth did.
</assumption_delta_decision>

<flagged_assumptions>
Surfaced, not silently dropped, per the spec-less probe fallback protocol (this phase has no
`-SPEC.md`, so no `## Edge Coverage` / `## Prohibitions` section exists to lift from;
`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`).

**FA-01 -- CONF-09's edge classification is `unclassified` / `unresolved`.** The deterministic edge
probe returned `{applicable: 1, resolved: 0, unresolved: 1}` for CONF-09 with
`probe: "unclassified -- review manually"`. Per §A/§B of the fallback protocol an `unclassified` row
stays `unresolved`: it is never auto-`backstop`ped, never auto-dismissed and never silently dropped,
so **no probe-derived edge predicate was authored into `must_haves` from it**. Every truth above was
derived goal-backward from the single FAILED truth in `44.2-VERIFICATION.md` (SC#2 plus plan 05's
backstop, one root cause) and from the measured stage enumeration in `<measured_grounding>` §1.
Consequence accepted knowingly, stated rather than hidden: **what an unclassified CONF-09 edge would
have covered that this plan does not** is the data-shape family -- author-value encoding (non-ASCII
names, embedded quotes), ordering (multi-author dict iteration order reaching the emitted tuple),
adjacency/boundary (a zero-author `typst_authors = {}`, a one-element vs. many-element seed), and
empty/`None` shapes of `typst_template_function["params"]["authors"]`. This plan asserts only
PRESENCE/ABSENCE of the right value at each stage, never its shape under those edges. Judged
acceptable because the phase's shape concerns are already pinned elsewhere
(`test_typst_authors_through_pipeline_produces_native_array_of_dicts`'s double-formatting guard,
and `_convert_to_authors_tuple`'s own tests), and because this round's scope is stage-scope accuracy
of published text over an already-shipped, already-verified mechanism (SC#1/SC#3/SC#4/SC#5/SC#6 all
VERIFIED). No-silent-drop equality for edges: **1 surfaced == 0 authored + 1 flagged**.

**FA-02 -- prohibition probe: 19 raw candidates, 12 authored, 7 dropped, 0 canon referrals.**
Stage-1 adversarial recall produced 19; Stage-2 precision dropped 7 as routine engineering or
hygiene already owned by the acceptance criteria and SC#6's gate, each with a breadcrumb: docs-build
health, test-collection correctness (folded into the no-weakening prohibition), `@preview` version
lockstep (nothing in scope touches those three files), rst list well-formedness and wrap-width
conformance, dependency/version hygiene (nothing is installed), commit granularity, and
evidence-file section completeness (plan 07's, not this plan's). **No OWASP / injection /
path-traversal / prototype-pollution / GDPR / fairness candidate survived Stage 2** for this scope
-- prose, tests and a tmp-path Sphinx build, with no new input handling and no parsing of untrusted
data -- so there is no canon-referral breadcrumb to `/gsd-secure-phase`. The 12 kept prohibitions
are bespoke project-hazard items, authored **descriptor-less** (no `check_kind` / `check_target` /
`check_rule` / `check_violation_fixture` / `check_clean_fixture`), so each disposes
flagged-unverified. No-silent-drop equality for prohibitions: **19 surfaced == 12 authored + 7
dropped with breadcrumbs**.

**FA-03 -- the stage-knob prose gate is a presence assertion, not a semantic one.** No test can
prove an English paragraph is accurate.
`test_authors_section_names_every_stage_knob_that_can_write_authors` pins that each reachable
stage's config knob is NAMED in the Author Information section, and the negative gates pin that the
two known-false phrasings are gone -- but a NEW wrong sentence added beside them would not go red.
That residual is exactly what plan 07's independently-derived, falsification-graded, equality-checked
enumeration exists to catch, and it is a human-reviewable artifact rather than an automated one.
This is the same residual plan 05 recorded as its FA-04; it is not discharged by this plan, it is
handed to a later wave with a different agent.

**FA-04 -- the real-build gates run a Sphinx subprocess and are environment-sensitive.** This
project's NixOS sandbox is recorded as failing ~45 integration tests environmentally unless the ELF
workaround from `44.2-GATE-EVIDENCE-04.md` §0 is applied. The two new `-b typst` gates are
subprocess builds and could join that set. The precondition in task 1 requires a pre-change
full-suite baseline measured in the SAME session; every count in this plan is a DELTA against that
baseline, never against an assumption that a failure is this plan's.

**FA-05 -- `title`'s prose is enumerated but not prose-gated.** The stage enumeration covers the
`title` writers for symmetry (W2's `template_key`, W3's `params["title"]`, W7's dict-form params),
but `test_authors_section_names_every_stage_knob_that_can_write_authors` is scoped to `authors` and
to the Author Information section. `title`'s published precedence lives in items 3-4 of the
five-element contract and in `templates.rst` (Phase 45.1 SC#2's, prohibited here). If `title` carries
an analogous unscoped claim outside item 4, this plan does not gate it -- plan 07's enumeration,
which derives its sentence set from the same stage list, is where it would surface.
</flagged_assumptions>

<source_audit>
| SOURCE | ID | Item | Task | Status |
|---|---|---|---|---|
| GOAL | -- | "entry `[2]`/`[3]` reach the compiled PDF ... with a defined and tested precedence against `typst_authors`" -- the residual: the *definition* is unscoped at pipeline scope | 1, 2, 3 | COVERED |
| REQ | CONF-09 | "a defined and tested precedence against `typst_authors`" -- the only clause `44.2-VERIFICATION.md` leaves unmet | 1, 2, 3 | COVERED |
| ROADMAP SC | SC#2 | precedence defined + asserted for every combination that can occur; each rule has a test | 1 (three-knob gate + control + API cell), 2 (stage enumeration), 3 (published two-stage rule) | COVERED |
| ROADMAP SC | SC#6 | full suite + `black`/`ruff`/`mypy` green | 3 (`<verify>`), `<verification>` | COVERED |
| ROADMAP SC | SC#1/SC#3/SC#4/SC#5 | VERIFIED in `44.2-VERIFICATION.md`; this plan asserts they stay green and their files untouched | 3 (control gates) | COVERED (prior plans) |
| VERIFY | missing 1 | correct or remove the unscoped clause at `configuration.rst:194` | 3 | COVERED |
| VERIFY | missing 2 | that line as its own graded row in a per-sentence enumeration with a named three-knob falsification row | -- | DEFERRED TO PLAN 07 (D-R4-C wave split -- the grader must not be the author) |
| VERIFY | missing 3 | one named test exercising the three-knob scenario end-to-end through `render()` | 1 | COVERED |
| RESEARCH | Pattern 2 | the D-05 reorder as a minimal block move -- landed by plan 01; this plan asserts it is untouched | 2 (`ast.parse` equality) | COVERED |
| RESEARCH | Pitfall 3 | do NOT re-implement D-05 as a per-entry override -- the corrected prose keeps describing the reorder, not a branch | 3 | COVERED |
| RESEARCH | Package Legitimacy Audit | no package-manager install task exists in this plan; nothing to gate | -- | N/A |
| RESEARCH | Architectural Responsibility Map | this plan writes only prose and tests; no tier assignment changes | -- | N/A |
| RESEARCH | §Validation Architecture | `sys.executable -m sphinx` subprocess idiom, discrete named functions | 1, 2 | COVERED |
| CONTEXT | D-04 | `typst_template_function`'s dict-form `params` still win over the entry values -- "needs no new code", it is `render()`'s existing merge | 1, 2 (order asserted from source), 3 (published as stage 2) | COVERED |
| CONTEXT | D-05 | `entry[3]` beats `typst_authors`; the mechanism is an assignment reorder, not a branch | 2, 3 | COVERED |
| CONTEXT | D-06 | `typst_authors` removal filed as Future Requirement `CONF-10` -- corrected by plan 05; NOT re-edited here | -- | COVERED (plan 05) |
| CONTEXT | D-11 | `configuration.rst` publishes the precedence rules decided here | 3 | COVERED |
| CONTEXT | D-01/D-02/D-03/D-07/D-08/D-09/D-10 | landed and verified by plans 01-03 | -- | COVERED (prior plans) |
| CONTEXT | Deferred Ideas | none re-entered by this plan | -- | EXCLUDED |

No `⚠ MISSING` rows. `api-coverage` detector: this phase integrates no external API or SDK -- the
scope is this project's own prose, its own AST and a tmp-path Sphinx build -- so no `COVERAGE.md`
matrix is produced; the reasoned declaration is "No external API integration: the phase corrects
published precedence text and adds tests over typsphinx's own pipeline." The schema-push gate finds
no ORM file patterns in scope (no Prisma/Drizzle/Payload/Supabase/TypeORM paths anywhere in this
repository).
</source_audit>

<artifacts_this_phase_produces>
Newly-created symbols and paths introduced by this plan. Drift verification must EXCLUDE these from
"does it already exist in the source" checks -- none of them exists before this plan runs.

**New file:**
- `tests/test_authors_pipeline_stage_gate.py`

**New module-level names in `tests/test_authors_pipeline_stage_gate.py`:**
- Constants: `REPO_ROOT`, `TEMPLATE_ENGINE_PATH`, `WRITER_PATH`, `CONFIGURATION_RST_PATH`,
  `PARAMS_CARRIERS`, `EXPECTED_STAGE_SITES`, `REACHABLE_STAGE_KNOBS`, `UNREACHABLE_STAGE_PROOFS`,
  `SEED_NAME`, `TEMPLATE_FUNCTION_AUTHOR`
- Helpers: `_module_ast`, `_stage_sites`, `_function_def`, `_author_information_section`,
  `_write_three_knob_project`, `_run_sphinx_build_typst`
- Tests: `test_three_knob_build_emits_template_function_authors_not_the_seed`,
  `test_three_knob_control_build_without_template_function_emits_the_seed`,
  `test_pipeline_stage_sites_that_can_determine_authors_are_exactly_these`,
  `test_reachable_and_unreachable_stages_partition_the_enumeration`,
  `test_toctree_options_cannot_reach_authors_or_title`,
  `test_render_applies_template_function_params_after_map_parameters_output`,
  `test_authors_section_names_every_stage_knob_that_can_write_authors`

**New test function in `tests/test_entry_metadata_precedence.py` (Group 3):**
- `test_render_template_function_authors_replaces_map_parameters_surviving_seed`

**No new production symbols.** No new fixture directory, no new committed `conf.py` (both real-build
gates write their project into `tmp_path`), no new helper under `typsphinx/`, no new config value,
no new dependency. `docs/source/user_guide/configuration.rst` and
`tests/test_entry_metadata_precedence.py` are edited in place.

**Not produced by this plan (plan 07's):** `44.2-GATE-EVIDENCE-06.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The three-knob path end to end — a real `-b typst` build, its control, and one API-level cell spanning both stages</name>

  <precondition>The worktree has its own environment: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` has been run, and `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"` prints a path inside THIS worktree, not `/home/yuta/Documents/typsphinx/typsphinx`. If it prints the main checkout, STOP — the PEP-660 editable finder is resolving to the main tree and every gate below would measure the wrong files. Plans 03, 04 and 05 all hit the NixOS generic-linux-ELF hazard on `.venv/bin/uv` and `.venv/bin/ruff`; the recorded fix is in `44.2-GATE-EVIDENCE-04.md` §0 (symlink `.venv/bin/uv` to the nix-store copy; `patchelf --set-interpreter` on `.venv/bin/ruff`). Apply it and record what you applied. Then record a pre-change full-suite baseline (`uv run python -m pytest -q`) in this same session: every count in this plan is a delta against THAT number, never against an assumption.</precondition>

  <reversibility rating="reversible">Two new test files' worth of assertions and one new test cell; a single `git revert` removes them with no data, schema, API or published-behaviour consequence.</reversibility>

  <files>tests/test_authors_pipeline_stage_gate.py, tests/test_entry_metadata_precedence.py</files>

  <read_first>
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md` — the whole file, and the `gaps:` block in the YAML frontmatter in particular. Its `reason:` records the exact `conf.py` the verifier built and the exact emitted value it observed. That configuration is what this task turns into a permanent gate.
    - `typsphinx/template_engine.py` lines 623-711 — `render()` in full, especially `all_params = {}`, `all_params.update(params)`, `all_params.update(self.typst_template_params)` and the `#show: {func}.with(` emission loop. This is the STAGE ROUND 3'S FRAME EXCLUDED; read it before writing any assertion about it.
    - `typsphinx/template_engine.py` lines 205-260 — `__init__`'s resolution of `typst_template_function` into `self.typst_template_params` (dict form vs string form vs unset), so the fixture's `typst_template_function` value is constructed from what the code actually reads.
    - `typsphinx/template_engine.py` lines 484-512 — the seed (W1), the mapping loop (W2) and the non-package back-fill (W3), so the "seed survives stage 1" half of the API-level cell is derived from the code and not transcribed from this plan.
    - `tests/test_package_template_routing.py` lines 1-70 — the project's `-b typst` subprocess idiom: `_run_sphinx_build_typst(srcdir, outdir)` invoking `[sys.executable, "-m", "sphinx", "-b", "typst", ...]` with `capture_output=True`, and `_make_index_rst`. Mirror it exactly; never `["uv", "run", "sphinx-build", ...]` (documented PATH-shadowing hazard).
    - `tests/test_entry_metadata_precedence.py` lines 470-509 — Group 3's banner and its two existing `render()` cells. Note that BOTH hand-build their `params` dict and never call `map_parameters()`; that is precisely the hole this task fills. The new cell goes after them, in the same discrete-named-function convention.
    - `tests/test_params_authors_writers.py` lines 1-100 — the sibling module's docstring, its path constants and `_seed_survives()`. The new module mirrors its idiom (module docstring naming the hazard, `REPO_ROOT = Path(__file__).resolve().parents[1]`, path constants) but widens the frame from `map_parameters()` to the pipeline.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md` §3 — read the whole table. It is the enumeration that FAILED. Note that row 2 cites line range 189-217 and grades a sentence at 201 while the defect sat at 194 inside the same range. Understand that failure before writing anything.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-PATTERNS.md` lines 549-580 — the real-compile render-gate pattern, the discrete-one-function-per-case convention, and the `sys.executable -m sphinx` rule.
  </read_first>

  <behavior>
    All three assertions below pass against UNMODIFIED production code — `render()`'s merge has been
    correct throughout this phase and D-04 explicitly requires "no new code". Do not expect a
    RED→GREEN transition from the Python. These are characterization gates: they pin, permanently and
    by name, the behaviour that four rounds of published prose kept mis-stating.

    - `test_three_knob_build_emits_template_function_authors_not_the_seed`: a tmp-path Sphinx
      project with `typst_authors` set, `typst_template_mapping = {"project": "title"}` (nothing
      targets `authors`, so per the published stage-1 rule the seed survives that stage), and
      `typst_template_function` in dict form carrying `params["authors"]`. `sphinx-build -b typst`
      exits 0; the emitted `index.typ` contains the template-function author string and does NOT
      contain the seed's author name.
    - `test_three_knob_control_build_without_template_function_emits_the_seed`: byte-identical
      project except the `typst_template_function` line is absent. The emitted `index.typ` DOES
      contain the seed's author name and does NOT contain the template-function author string.
      Without this control the first test could pass because the mapping ate the seed; with it, the
      difference between the two builds is attributable to the render stage alone.
    - `test_render_template_function_authors_replaces_map_parameters_surviving_seed`: one body
      spanning both stages at API level. Construct the engine once with all three knobs; call
      `map_parameters()` and assert the seed SURVIVED stage 1 (`params["authors"]` is the
      `typst_authors` list of dicts, `params["authors"][0]["name"]` is the seed name); then call
      `render(params, ...)` and assert the emitted string carries the template-function value and
      not the seed name. Asserting the survival first is what makes the second assertion mean
      "replaced at render time" rather than "never seeded".
  </behavior>

  <action>
Set `GAP_BASE=e9771569bc075673de2bddf67da2f72f9389299b` (the tree this plan was written against). Before anything else assert it is an ancestor of `HEAD` with `git merge-base --is-ancestor $GAP_BASE HEAD`. If it is not, recompute it as the commit this task builds on, use that value everywhere below and in every hardcoded SHA in the `<verify>` blocks, and record the substitution and its reason in this task's SUMMARY — never silently proceed with a base that does not match.

**(1) Create `tests/test_authors_pipeline_stage_gate.py` with its docstring and the two real-build gates.** Mirror `tests/test_params_authors_writers.py`'s idiom: a module docstring naming the hazard, then `REPO_ROOT = Path(__file__).resolve().parents[1]`, then path constants `TEMPLATE_ENGINE_PATH`, `WRITER_PATH`, `CONFIGURATION_RST_PATH`.

The module docstring must state, in these terms: (i) the enumeration axis is the PIPELINE STAGE that determines the emitted `authors`, widened deliberately from the sibling module's `map_parameters()`-only frame; (ii) rounds 1, 2 and 3 each derived their correction set from the prose being corrected, and round 3's per-sentence enumeration graded nine sites while the defect sat inside one graded site's own cited line range — because its row space was `SOURCE_KEYS x TARGET_KEYS x presence` and `render()`'s later merge had no axis in it; (iii) this module therefore enumerates stages from the code and requires every enumerated stage to be classified REACHABLE or UNREACHABLE-WITH-PROOF, with a partition assertion; (iv) the deliberate convention note — `44.2-PATTERNS.md` prescribes discrete named functions over `@pytest.mark.parametrize`, and this module keeps that convention throughout.

Constants for the two build gates, exactly these names: `SEED_NAME = "Jane Doe"`; `TEMPLATE_FUNCTION_AUTHOR = "FROM TEMPLATE FUNCTION"`.

Helper `_write_three_knob_project(srcdir, with_template_function)` writes `index.rst` (a two-line title plus one body line, mirroring `_make_index_rst` in `tests/test_package_template_routing.py`) and `conf.py`. The `conf.py` sets, in this order: `project = "P"`, `author = "A"`, `release = "1.0"`, `extensions = ["typsphinx"]`, `typst_documents = [("index", "index", "T", "Entry Author")]`, `typst_authors = {SEED_NAME: {"organization": "MIT"}}`, `typst_template_mapping = {"project": "title"}`, and — only when `with_template_function` is true — `typst_template_function = {"name": "project", "params": {"authors": (TEMPLATE_FUNCTION_AUTHOR,)}}`. Its docstring must state that the mapping deliberately targets `title` and nothing else, so the seed survives stage 1 by the published stage-1 rule, and that `with_template_function` is the ONLY difference between the gate and its control.

Helper `_run_sphinx_build_typst(srcdir, outdir)` runs `[sys.executable, "-m", "sphinx", "-b", "typst", str(srcdir), str(outdir)]` via `subprocess.run` with `capture_output=True, text=True` and returns the completed process. Its docstring must record why `sys.executable -m sphinx` and never a `uv run sphinx-build` form: the documented PATH-shadowing hazard, and the worktree-isolation requirement that the interpreter running the test is the one that must import the worktree's editable `typsphinx`.

`test_three_knob_build_emits_template_function_authors_not_the_seed` builds with `with_template_function=True` into `tmp_path`, asserts `result.returncode == 0` with `result.stderr` included in the assertion message, reads the emitted `index.typ`, and asserts `TEMPLATE_FUNCTION_AUTHOR in emitted` and `SEED_NAME not in emitted`. Its docstring must name this as the configuration `44.2-VERIFICATION.md` reproduced with a real `sphinx-build`, and state that this is the row no published sentence had a falsification attempt against for three rounds.

`test_three_knob_control_build_without_template_function_emits_the_seed` builds the same project with `with_template_function=False` and asserts `SEED_NAME in emitted` and `TEMPLATE_FUNCTION_AUTHOR not in emitted`. Its docstring must state that it is the attribution control: the two builds differ in exactly one config line, so the difference in the emitted `authors` value is attributable to the render stage's `typst_template_params` merge and to nothing in the mapping.

**(2) Add one Group 3 cell to `tests/test_entry_metadata_precedence.py`.** Append `test_render_template_function_authors_replaces_map_parameters_surviving_seed` after `test_render_explicit_template_function_authors_wins_over_entry_derived`, at module level, in the module's existing discrete-function convention (no `@pytest.mark.parametrize`). Construct one `TemplateEngine` with `parameter_mapping={"project": "title"}`, `typst_authors={"Jane Doe": {"organization": "MIT"}}` and `typst_template_function={"name": "project", "params": {"authors": ("FROM TEMPLATE FUNCTION",)}}`, no package. Assert in this order: `params = engine.map_parameters({"project": "P", "author": "Explicit Entry Author", "release": "1.0"})`; `isinstance(params["authors"], list)`; `params["authors"][0]["name"] == "Jane Doe"`; then `result = engine.render(params, "Content")`; `'"FROM TEMPLATE FUNCTION"' in result`; `"Jane Doe" not in result`.

Its docstring must state: that this is the combination `44.2-VERIFICATION.md` measured as having no single-assertion coverage anywhere in the suite; that the two existing Group 3 cells above it hand-build their `params` and never call `map_parameters()`, so neither can express "a seed that SURVIVED the mapping is replaced at render time"; that asserting the survival first is what makes the second half mean replacement rather than absence; and that D-04 requires no code for this — it is `render()`'s existing `all_params.update(params)` then `all_params.update(self.typst_template_params)` merge.

Change nothing else in `tests/test_entry_metadata_precedence.py`: every existing docstring, banner and assertion in Groups 1-3 stays byte-identical.

Do not touch `docs/source/user_guide/configuration.rst` in this task — task 3 owns it. Do not add the stage enumeration here — task 2 owns it.
  </action>

  <verify>
    <automated>
uv run python -m pytest tests/test_authors_pipeline_stage_gate.py -q &&
uv run python -m pytest "tests/test_entry_metadata_precedence.py::test_render_template_function_authors_replaces_map_parameters_surviving_seed" -q &&
uv run python -m pytest tests/test_entry_metadata_precedence.py -q &&
uv run python -m pytest tests/test_authors_pipeline_stage_gate.py --collect-only -q | grep -qF 'test_three_knob_control_build_without_template_function_emits_the_seed' &&
uv run python -c "import ast,subprocess;
base='e9771569bc075673de2bddf67da2f72f9389299b'
for p in ('typsphinx/template_engine.py','typsphinx/writer.py'):
    o=subprocess.check_output(['git','show',base+':'+p]).decode()
    n=open(p,encoding='utf-8').read()
    assert ast.dump(ast.parse(o))==ast.dump(ast.parse(n)), 'executable code changed under typsphinx/: '+p
print('ast-equal')" &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- docs/source/user_guide/configuration.rst docs/source/user_guide/templates.rst .planning/REQUIREMENTS.md)"
    </automated>
  </verify>

  <acceptance_criteria>
    - `uv run python -m pytest tests/test_authors_pipeline_stage_gate.py -q` passes with zero failures and collects at least 2 node ids at the end of this task.
    - The node id `tests/test_entry_metadata_precedence.py::test_render_template_function_authors_replaces_map_parameters_surviving_seed` exists and PASSES (named test required by `44.2-VERIFICATION.md`'s `missing:` bullet 3).
    - That cell's body contains BOTH a call to `engine.map_parameters(...)` and a call to `engine.render(...)`, and asserts `params["authors"][0]["name"] == "Jane Doe"` BEFORE the render assertions — verified by reading the function. A cell that only calls `render()` on a hand-built dict does not satisfy this criterion.
    - `uv run python -m pytest tests/test_entry_metadata_precedence.py -q` reports 26 passed (25 pre-existing plus the one added here) and zero failures. The count went UP; no existing cell was deleted, renamed, `xfail`-ed or `skip`-ped.
    - Both real-build gates invoke `[sys.executable, "-m", "sphinx", "-b", "typst", ...]` — `grep -c 'uv", *"run' tests/test_authors_pipeline_stage_gate.py` is `0`.
    - The gate and its control differ in exactly one `conf.py` line: `_write_three_knob_project` takes a single boolean and the only branch in it is the `typst_template_function` assignment — verified by reading the helper.
    - `ast.dump(ast.parse(...))` of the working-tree `typsphinx/template_engine.py` AND `typsphinx/writer.py` each equal that of `git show <GAP_BASE>:<path>` — this task changed no production code at all.
    - `git diff --name-only <GAP_BASE> --` for `docs/source/user_guide/configuration.rst`, `docs/source/user_guide/templates.rst` and `.planning/REQUIREMENTS.md` is empty — this task touched none of them.
    - No new file was created under `tests/fixtures/`; both build gates write their project into `tmp_path` (`git status --porcelain tests/fixtures/` is empty).
  </acceptance_criteria>

  <done>
The three-knob configuration that falsified round 3's published clause is a permanently named,
passing gate at two levels — a real `sphinx-build -b typst` with an attribution control, and one
API-level cell that asserts the seed survives the mapping stage and is then replaced by the render
stage in the same body — with the production tree proven byte-unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enumerate the pipeline's `authors`/`title` writers from the code, and partition them REACHABLE / UNREACHABLE-WITH-PROOF</name>

  <files>tests/test_authors_pipeline_stage_gate.py</files>

  <read_first>
    - `typsphinx/template_engine.py` lines 484-535 — every `params[...]` assignment site in `map_parameters()`: the seed, the mapping loop's `params[template_key]`, the three back-fill literals, and the `typst_elements` loop's `params[key]` with its `ELEMENTS_ALLOWLIST` raise. Re-derive the site list from this source; do not transcribe `<measured_grounding>` §1's table.
    - `typsphinx/template_engine.py` lines 563-608 — `extract_toctree_options()` in full, including the early `return {}` and the single returned dict literal. Its literal key set is the PROOF that `writer.py`'s `params.update(toctree_options)` cannot reach `authors`/`title`; read it before writing that proof.
    - `typsphinx/template_engine.py` lines 690-704 — `render()`'s merge: `all_params = {}`, then the two `.update(` calls in source order, then the emission loop. The ORDER of those two calls is what D-04 and the published stage-2 rule both rest on.
    - `typsphinx/writer.py` lines 255-335 — `translate()`'s `sphinx_metadata` construction, the `effective_elements` union, the `map_parameters()` call, and `params.update(toctree_options)` at line 330. That `.update(` is a real `params` writer in the pipeline and must appear in the enumeration, classified, not omitted.
    - `tests/test_params_authors_writers.py` lines 90-145 — `_map_parameters_ast()`, `_params_assignments()` and `_map_parameters_region()`. The new module's walkers are the same technique widened to two modules and to `.update(` calls; keep the sibling module's helpers untouched and do not import from it.
    - `typsphinx/template_engine.py` lines 358-386 — `uses_bundled_default_template()`'s docstring, which uses the package-routing qualifier ACCURATELY about a different subject. It is why any negative gate over this file must be region-scoped and never file-wide.
  </read_first>

  <behavior>
    All assertions pass against unmodified production code; they are structural drift guards, not
    behaviour changes.

    - `test_pipeline_stage_sites_that_can_determine_authors_are_exactly_these`: AST-walks
      `typsphinx/template_engine.py` and `typsphinx/writer.py`, collecting (a) every `Assign` whose
      target is a `Subscript` on a `Name` whose id is in `PARAMS_CARRIERS`, and (b) every `Call` to
      `.update(...)` whose receiver is such a `Name`. Each finding is rendered as a stable id string
      of the form `<relpath>::<enclosing function>::<assign|update>::<literal slice, dynamic Name
      id, or unparsed first argument>`. The sorted multiset of ids equals the frozen
      `EXPECTED_STAGE_SITES` tuple, which contains the two `render()` merges and `writer.py`'s
      `toctree_options` update alongside `map_parameters()`'s four assignment sites.
    - `test_reachable_and_unreachable_stages_partition_the_enumeration`: the key set of
      `REACHABLE_STAGE_KNOBS` and the key set of `UNREACHABLE_STAGE_PROOFS` are disjoint and their
      union equals `set(EXPECTED_STAGE_SITES)` — no enumerated site may be silently dropped from
      either class. Every value in `UNREACHABLE_STAGE_PROOFS` names a test function that exists at
      module level in this module.
    - `test_toctree_options_cannot_reach_authors_or_title`: AST over `extract_toctree_options()`
      collects the literal string keys of every returned `Dict`; the set is exactly
      `{"toctree_maxdepth", "toctree_numbered", "toctree_caption"}` and contains neither `"authors"`
      nor `"title"`. Plus a behavioural check that calling it with a doctree carrying no toctree
      node returns `{}`.
    - `test_render_applies_template_function_params_after_map_parameters_output`: within `render()`,
      the unparsed first arguments of the `.update(` calls on `all_params`, in source order, are
      exactly `["params", "self.typst_template_params"]`.
    - `test_authors_section_names_every_stage_knob_that_can_write_authors`: every value of
      `REACHABLE_STAGE_KNOBS` appears in the Author Information section of `configuration.rst`,
      whitespace-normalized. This test is expected to FAIL until task 3 lands the corrected prose;
      that failure is this plan's structural RED, recorded before the edit.
  </behavior>

  <action>
Extend `tests/test_authors_pipeline_stage_gate.py` (created in task 1). Add nothing to `tests/test_params_authors_writers.py` and change nothing in it — its `map_parameters()`-scoped frame is part of the record of round 3 and stays as it is.

**(1) Constants.** `PARAMS_CARRIERS = ("params", "all_params")` — the local names that carry template parameters anywhere in `typsphinx/`; its comment must say that a new carrier name is itself a frame change and must be added here deliberately, not discovered later. `EXPECTED_STAGE_SITES` is a frozen tuple of id strings, sorted, hand-declared from a reading of the source (NOT computed by running the walker and pasting its output — that would make the gate a tautology). At `GAP_BASE` the expected members are: `map_parameters`'s `assign::authors` (twice — the seed and the back-fill), `assign::template_key`, `assign::title`, `assign::date`, `assign::key`; `render`'s `update::params` and `update::self.typst_template_params`; and `translate`'s `update::toctree_options`. Re-derive each from the source before freezing it; if the source disagrees with that list, the SOURCE wins and the discrepancy is recorded in the SUMMARY.

`REACHABLE_STAGE_KNOBS` is a dict from stage-site id to the user-facing config name that controls it, one inline comment per entry stating why: the seed → `typst_authors`; the mapping-loop assignment → `typst_template_mapping`; the back-fill's `authors` literal → `typst_package` (it is the guard that decides whether the back-fill runs at all); the `render()` merge of `self.typst_template_params` → `typst_template_function`. `UNREACHABLE_STAGE_PROOFS` is a dict from stage-site id to the NAME of the test in this module that proves the site cannot determine `authors`: the `typst_elements` loop and the `toctree_options` update each map to their proof test; the back-fill's `title`/`date` literals and `render()`'s `update::params` map to the tests that pin them as non-deciding. Both dicts are hand-declared with reasons, never derived from the code's output.

**(2) Helpers.** `_module_ast(path)` parses a path and returns the `Module`. `_function_def(path, name)` returns the `FunctionDef` of that name (raising a named `AssertionError` if absent). `_stage_sites()` walks BOTH `TEMPLATE_ENGINE_PATH` and `WRITER_PATH`, tracking the enclosing `FunctionDef` for each node, and returns a sorted list of id strings built as described in `<behavior>`; use `ast.unparse` for the `.update(` argument so `self.typst_template_params` renders exactly. `_author_information_section()` reads `CONFIGURATION_RST_PATH`, slices from the `Author Information` section title to the next same-level section title, and returns the slice with whitespace collapsed to single spaces so a re-wrap cannot fool the gate.

**(3) The five tests** named in `<artifacts_this_phase_produces>`, with the behaviour given in `<behavior>`. Specific requirements:

- `test_pipeline_stage_sites_that_can_determine_authors_are_exactly_these`'s assertion message must name the CONSEQUENCE, not just the mismatch: a stage that can determine the emitted `authors` was added, removed or moved, so the published two-stage rule in `docs/source/user_guide/configuration.rst`, the D-05 comment in `typsphinx/template_engine.py`, and `REACHABLE_STAGE_KNOBS` below must all be re-derived from the code before this expectation is changed — and it must state explicitly that round 3's enumeration missed a writer precisely because its frame was one function rather than the pipeline.
- `test_reachable_and_unreachable_stages_partition_the_enumeration`'s assertion messages must print the symmetric difference in both directions, so an unclassified new site names itself.
- `test_toctree_options_cannot_reach_authors_or_title`'s docstring must state that this site is a REAL `params` writer in the pipeline — `writer.py` calls `params.update(toctree_options)` between `map_parameters()` and `render()` — and that it is excluded by PROOF of its key set, not by assumption.
- `test_render_applies_template_function_params_after_map_parameters_output`'s docstring must state that this order is the mechanism D-04 rests on ("needs no new code") and the mechanism the published stage-2 sentence describes; if the two `.update(` calls were ever transposed, the published rule would invert silently and no behavioural test in the suite names the order itself.
- `test_authors_section_names_every_stage_knob_that_can_write_authors` reads `_author_information_section()` and asserts each `REACHABLE_STAGE_KNOBS` value is present, with an assertion message naming the missing knob and stating that a stage the code can reach must be named in the section a user configures against.

**(4) Record the structural RED before task 3 edits the prose.** Run `uv run python -m pytest "tests/test_authors_pipeline_stage_gate.py::test_authors_section_names_every_stage_knob_that_can_write_authors" -q` now, against the un-edited `configuration.rst`, and keep the verbatim failure output for the SUMMARY. `typst_template_function` is currently named in the Author Information section only in the trailing sentence and the `.. note::`, so confirm from the actual output whether this is RED or GREEN at this point and record what you measured either way — do not assert a colour you did not observe. If it is GREEN, say so and record that the section already names all four knobs, so this gate's value is forward-looking drift protection rather than a pre-fix failure.

Do not touch `docs/source/user_guide/configuration.rst` in this task. Do not modify `tests/test_params_authors_writers.py`, `tests/test_entry_metadata_route_uniformity.py`, `tests/test_package_only_config_gate.py`, `tests/test_package_template_routing.py`, `tests/test_examples_charged_ieee_gate.py`, `tests/test_config_template_mapping.py`, either fixture `conf.py`, `typsphinx/writer.py`, `typsphinx/builder.py`, or `typsphinx/template_engine.py`.
  </action>

  <verify>
    <automated>
uv run python -m pytest tests/test_authors_pipeline_stage_gate.py -q -k "not authors_section_names_every_stage_knob" &&
uv run python -m pytest tests/test_authors_pipeline_stage_gate.py --collect-only -q | grep -qF 'test_pipeline_stage_sites_that_can_determine_authors_are_exactly_these' &&
uv run python -m pytest tests/test_authors_pipeline_stage_gate.py --collect-only -q | grep -qF 'test_reachable_and_unreachable_stages_partition_the_enumeration' &&
uv run python -c "import re,sys;t=open('tests/test_authors_pipeline_stage_gate.py',encoding='utf-8').read();[sys.exit('MISSING: '+n) for n in ('self.typst_template_params','toctree_options','all_params','PARAMS_CARRIERS','EXPECTED_STAGE_SITES','REACHABLE_STAGE_KNOBS','UNREACHABLE_STAGE_PROOFS') if n not in t]" &&
uv run python -c "import ast,subprocess;
base='e9771569bc075673de2bddf67da2f72f9389299b'
for p in ('typsphinx/template_engine.py','typsphinx/writer.py'):
    o=subprocess.check_output(['git','show',base+':'+p]).decode()
    n=open(p,encoding='utf-8').read()
    assert ast.dump(ast.parse(o))==ast.dump(ast.parse(n)), 'executable code changed under typsphinx/: '+p
print('ast-equal')" &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- tests/test_params_authors_writers.py tests/test_entry_metadata_route_uniformity.py tests/test_config_template_mapping.py tests/test_package_only_config_gate.py tests/test_package_template_routing.py typsphinx/)"
    </automated>
  </verify>

  <acceptance_criteria>
    - `uv run python -m pytest tests/test_authors_pipeline_stage_gate.py -q -k "not authors_section_names_every_stage_knob"` passes with zero failures.
    - `EXPECTED_STAGE_SITES` contains an entry whose id names `render` and `self.typst_template_params`, and an entry whose id names `translate` and `toctree_options` — the two stages entirely outside round 3's frame. Verified by reading the constant.
    - `test_reachable_and_unreachable_stages_partition_the_enumeration` asserts set equality between `set(EXPECTED_STAGE_SITES)` and `set(REACHABLE_STAGE_KNOBS) | set(UNREACHABLE_STAGE_PROOFS)`, AND asserts the two key sets are disjoint. Both directions of the symmetric difference appear in the assertion messages.
    - Every value of `UNREACHABLE_STAGE_PROOFS` is the name of a function defined at module level in `tests/test_authors_pipeline_stage_gate.py` — asserted inside `test_reachable_and_unreachable_stages_partition_the_enumeration`, not merely by convention.
    - `REACHABLE_STAGE_KNOBS`'s values include all four of `typst_authors`, `typst_template_mapping`, `typst_package`, `typst_template_function`.
    - Neither `EXPECTED_STAGE_SITES` nor `REACHABLE_STAGE_KNOBS` nor `UNREACHABLE_STAGE_PROOFS` is built by calling `_stage_sites()`, `map_parameters`, `render` or `TemplateEngine` — verified by reading the module: all three are literal declarations.
    - `test_render_applies_template_function_params_after_map_parameters_output` asserts the unparsed update arguments in source order equal `["params", "self.typst_template_params"]`.
    - `test_toctree_options_cannot_reach_authors_or_title` asserts the returned literal key set equals `{"toctree_maxdepth", "toctree_numbered", "toctree_caption"}` and that neither `"authors"` nor `"title"` is in it.
    - The verbatim output of running `test_authors_section_names_every_stage_knob_that_can_write_authors` against the un-edited `configuration.rst` is captured in the SUMMARY, with the observed colour stated as measured (not assumed).
    - `ast.dump(ast.parse(...))` equality holds for both `typsphinx/template_engine.py` and `typsphinx/writer.py` against `<GAP_BASE>`; `git diff --name-only <GAP_BASE> -- typsphinx/` is empty.
    - `git diff --name-only <GAP_BASE> --` is empty for `tests/test_params_authors_writers.py`, `tests/test_entry_metadata_route_uniformity.py`, `tests/test_config_template_mapping.py`, `tests/test_package_only_config_gate.py` and `tests/test_package_template_routing.py` — the round-3 module and the accurate routing modules are untouched.
  </acceptance_criteria>

  <done>
Every site in `typsphinx/` that can determine the emitted `authors` or `title` is enumerated from
the code across the whole pipeline — including the two `render()` merges and `writer.py`'s
`toctree_options` update that round 3's frame excluded — each is classified REACHABLE or
UNREACHABLE-WITH-PROOF under a partition assertion that admits no silent drop, and the classification
is wired to the published prose by a knob-presence gate.
  </done>
</task>

<task type="auto">
  <name>Task 3: Publish the precedence as an ordered two-stage rule, with every universal and negative-existential claim stage-scoped</name>

  <files>docs/source/user_guide/configuration.rst</files>

  <read_first>
    - `docs/source/user_guide/configuration.rst` lines 34-68 — the numbered five-element contract. Only item 4 changes in this task; items 1, 2, 3 and 5 must not move. Item 5's accepted-and-ignored statement is plan 03's VERIFIED SC#5 deliverable.
    - `docs/source/user_guide/configuration.rst` lines 163-240 — the whole Author Information section: the `typst_authors` code-block example (172-187), the Precedence block being replaced (189-228), and the `.. note::` forward-removal block (230-238) which must stay byte-identical.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md` `gaps:` → `artifacts:` → the entry for `docs/source/user_guide/configuration.rst`. It quotes the exact clause at line 194 that this task removes. Read the quote there rather than relying on memory of it.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-REVIEW.md` CR-01 in full — the same finding, with the falsifying `conf.py` and the contradicting sentence three paragraphs later both quoted. Its "Fix:" suggestion is a scoping suggestion only; the two-stage rewrite below supersedes it.
    - `tests/test_params_authors_writers.py` lines 248-266 — `test_configuration_rst_documents_both_target_key_shapes`. It requires three literals to survive whitespace-normalized in this file. Read them before editing so the rewrite preserves all three verbatim rather than the gate being relaxed.
    - `tests/test_authors_pipeline_stage_gate.py` as task 2 left it — specifically `REACHABLE_STAGE_KNOBS` and `_author_information_section()`, so the rewritten section is written to satisfy the knob-presence gate by naming the knobs, not by widening the gate.
    - `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-CONTEXT.md` D-04 (lines 91-98), D-05 (100-109) and D-11 (209-224) — the locked decisions this text publishes. D-04 is the render-stage rule; D-05 is the mapping-stage reorder; D-11 is the obligation that this file publish them.
  </read_first>

  <action>
Replace `docs/source/user_guide/configuration.rst` lines 189-228 in full — the Precedence block, everything between the `typst_authors` code-block example ending at line 187 and the `.. note::` directive at line 230. Per D-11 (this file publishes the precedence the code implements), D-05 (the mapping-stage reorder) and D-04 (`typst_template_function`'s dict-form params still win). Write exactly this in its place, keeping the file's existing wrap width and leaving a blank line before and after each list so docutils parses the enumerated list and the bulleted list as lists rather than folding them into a paragraph:

    **Precedence.** Two stages decide what reaches the template's ``authors``
    parameter, in this order:

    1. **The parameter mapping**, applied while Sphinx metadata is turned into
       template parameters. typsphinx seeds ``authors`` from ``typst_authors``
       first and then applies the mapping, so whatever the mapping writes into
       ``authors`` replaces that seed. No other assignment in that mapping step
       replaces the seed: the defaults typsphinx back-fills for a missing
       parameter only fill a key that is not already set, and ``typst_elements``
       accepts no ``authors`` key at all.
    2. **``typst_template_function``'s dict-form** ``params``, applied later,
       when the document is rendered. A ``params["authors"]`` entry there
       replaces whatever stage 1 produced -- including a ``typst_authors`` seed
       that came through stage 1 untouched.

    Within stage 1, one question decides the outcome, and it is not about the
    template route: **does the active parameter mapping send some Sphinx
    metadata key to the template parameter named** ``authors``?

    The default mapping sends ``author`` to ``authors``, so on an ordinary
    build the entry's own author element (the fourth position in
    `Typst Documents`_ above) -- or the ``author`` fallback it resolves to
    -- wins.

    ``typst_authors`` therefore comes through stage 1 as the sole source of
    ``authors`` if and only if no entry of the active mapping targets
    ``authors`` with a source key the build actually supplies. It is the
    *target* key that decides, not whether ``"author"`` appears in the mapping
    at all:

    * A mapping that sends ``"author"`` somewhere other than ``authors``
      -- for example ``typst_template_mapping = {"author": "doc_authors"}``,
      for a custom template whose function names its author parameter
      differently -- leaves ``authors`` alone. Both values are then passed:
      ``typst_authors`` as ``authors``, and the entry's author value as
      ``doc_authors``.
    * A mapping that sends some other key to ``authors`` -- for example
      ``typst_template_mapping = {"project": "authors"}`` -- replaces the
      seed even though it never mentions ``"author"``. What lands in
      ``authors`` is then the project name, in the same shape an author
      value would take.

    A ``typst_package`` build with ``typst_template_mapping`` not set at
    all reaches stage 1's surviving case by a side door rather than by a rule
    of its own: typsphinx then passes only what was explicitly mapped, and
    nothing was, so the mapping is empty and can target nothing.
    Package-based and template-based builds -- including the bundled
    default template -- are governed by the one stage-1 rule above;
    ``typst_package`` affects only which mapping is in force, never whether
    the mapping replaces the seed.

    Coming through stage 1 is not the end of the story. Setting
    ``typst_template_function`` to its dict form with a ``params["authors"]``
    entry replaces the ``typst_authors`` seed at stage 2, so a project that
    sets ``typst_authors`` alongside a mapping targeting nothing but ``title``
    still renders the template function's own ``authors`` value.

Three constraints on that text, all of them load-bearing:

- The phrase ``no entry of the active mapping targets ``authors``` and both mapping literals ``{"author": "doc_authors"}`` and ``{"project": "authors"}`` appear above verbatim and must survive the edit. `tests/test_params_authors_writers.py::test_configuration_rst_documents_both_target_key_shapes` asserts all three whitespace-normalized; the gate is satisfied by keeping them, never by relaxing it.
- Every negative claim in the block names the stage it quantifies over ("No other assignment in **that mapping step**", "never whether **the mapping** replaces the seed"). None quantifies over typsphinx as a whole. This is the corollary prohibition this round adds, and it is the exact grammatical shape that shipped at line 194 and was graded ACCURATE by round 3's own enumeration.
- The block states no exhaustive count of config shapes and contains no "usually"/"typically"/"in practice" hedge. The only exhaustive statements are the ordered two-stage list and the iff, and the iff is explicitly scoped to stage 1.

**(2) `docs/source/user_guide/configuration.rst` — item 4 of the five-element contract (lines 49-61).** One sentence in that bullet makes a universal claim about what decides the outcome, without naming a stage: the sentence beginning ``Whether this value also beats ``typst_authors`` is``. Rewrite that sentence, and only that sentence, so the universal is scoped to the mapping stage and the replacement text does not contain the un-scoped run as a substring. Write exactly this in its place:

       Whether this value also beats ``typst_authors`` is settled at the
       mapping stage by exactly one thing -- whether the active parameter
       mapping sends ``"author"`` to the template parameter named ``authors``,
       which is what the default mapping does.

Leave the rest of item 4 byte-identical, including the run beginning ```Author Information`_ below states the full rule`` with its "the two cases this summary does not cover" clause, and the closing ``typst_template_function``-precedence sentence. Leave items 1, 2, 3 and 5 completely untouched. Leave the `.. note::` block at 230-238 untouched.

**(3) Confirm the docs still build and the section still parses as intended.** Run a real `sphinx -b html docs/source <tmpdir>` build and grep its output for any WARNING or ERROR line naming `configuration.rst`; an enumerated list or bulleted list that lost its surrounding blank line shows up there. Then re-run `test_authors_section_names_every_stage_knob_that_can_write_authors` from task 2 and record its now-GREEN output alongside the pre-edit output captured in task 2.

Do not edit `docs/source/user_guide/templates.rst`, `.planning/REQUIREMENTS.md`, `typsphinx/template_engine.py`'s D-05 comment (it is already stated over the target key and is correct at its own stage scope; widening it is plan 07's call if its enumeration grades it short), or any file under `.planning/phases/44.2-.../` other than this plan's own SUMMARY.
  </action>

  <verify>
    <automated>
! grep -qF 'nothing else in typsphinx' docs/source/user_guide/configuration.rst &&
! grep -qF 'decided by one thing only' docs/source/user_guide/configuration.rst &&
! grep -qEi '(usually|typically|most often|in practice)[^.]*typst_authors' docs/source/user_guide/configuration.rst &&
uv run python -c 'import re,sys;t=re.sub(r"\s+"," ",open(sys.argv[1],encoding="utf-8").read());[sys.exit("MISSING: "+n) for n in sys.argv[2:] if n not in t]' docs/source/user_guide/configuration.rst 'Two stages decide what reaches the template' 'replaces whatever stage 1 produced' 'No other assignment in that mapping step replaces the seed' 'settled at the mapping stage by exactly one thing' 'no entry of the active mapping targets ``authors``' '{"author": "doc_authors"}' '{"project": "authors"}' 'the two cases this summary does not cover' 'accepted and ignored' &&
uv run python -m pytest tests/test_authors_pipeline_stage_gate.py tests/test_params_authors_writers.py tests/test_entry_metadata_precedence.py tests/test_template_engine.py -q &&
uv run python -m pytest -q &&
uv run python -m black --check . &&
uv run python -m ruff check . &&
uv run python -m mypy typsphinx/ &&
uv run python -c "import ast,subprocess;
base='e9771569bc075673de2bddf67da2f72f9389299b'
for p in ('typsphinx/template_engine.py','typsphinx/writer.py'):
    o=subprocess.check_output(['git','show',base+':'+p]).decode()
    n=open(p,encoding='utf-8').read()
    assert ast.dump(ast.parse(o))==ast.dump(ast.parse(n)), 'executable code changed under typsphinx/: '+p
print('ast-equal')" &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- typsphinx/ docs/source/user_guide/templates.rst .planning/REQUIREMENTS.md tests/test_params_authors_writers.py tests/test_entry_metadata_route_uniformity.py)" &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-01-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-02-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-03-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-04-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-05-PLAN.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-REVIEW.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-VERIFICATION.md)" &&
test -z "$(git diff --name-only e9771569bc075673de2bddf67da2f72f9389299b -- .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-01.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-02.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-03.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-04.md .planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-GATE-EVIDENCE-05.md)" &&
DOCS_OUT="$(uv run --extra dev --extra docs python -m sphinx -b html docs/source "$(mktemp -d)" 2>&1)" &&
printf '%s' "$DOCS_OUT" | grep -q 'build succeeded' &&
! printf '%s' "$DOCS_OUT" | grep -E 'WARNING|ERROR' | grep -q 'configuration.rst'
    </automated>
  </verify>

  <acceptance_criteria>
    - NEGATIVE: `grep -c 'nothing else in typsphinx' docs/source/user_guide/configuration.rst` is `0` — the unscoped negative existential `44.2-VERIFICATION.md` reproduced is gone.
    - NEGATIVE: `grep -c 'decided by one thing only' docs/source/user_guide/configuration.rst` is `0` — item 4's unscoped universal is gone, and its replacement does not contain that run as a substring.
    - NEGATIVE: no hedge qualifies a `typst_authors` claim — `grep -Ei '(usually|typically|most often|in practice)[^.]*typst_authors' docs/source/user_guide/configuration.rst` returns nothing.
    - POSITIVE (whitespace-normalized, so a re-wrap cannot fool it): `docs/source/user_guide/configuration.rst` contains all of `Two stages decide what reaches the template`, `replaces whatever stage 1 produced`, `No other assignment in that mapping step replaces the seed`, and `settled at the mapping stage by exactly one thing`.
    - PRESERVATION (whitespace-normalized): the same file still contains ``no entry of the active mapping targets ``authors```, `{"author": "doc_authors"}`, `{"project": "authors"}`, `the two cases this summary does not cover` and `accepted and ignored` — the round-3 drift guard and plan 03's VERIFIED SC#5 deliverable both survive the rewrite untouched.
    - Every negative claim in the rewritten Precedence block names its stage: reading the block, each occurrence of a negation is followed or preceded by `that mapping step`, `the mapping`, or `stage 1`. No sentence in the block denies an action of typsphinx as a whole.
    - `uv run python -m pytest tests/test_authors_pipeline_stage_gate.py -q` passes with zero failures INCLUDING `test_authors_section_names_every_stage_knob_that_can_write_authors`, which was measured before this edit in task 2 and is recorded GREEN after it.
    - `uv run python -m pytest tests/test_params_authors_writers.py -q` still reports 24 passed — the round-3 module was not modified and its drift guard was not relaxed.
    - `uv run python -m pytest -q` passes with zero failures and a passed count strictly GREATER than the pre-change baseline recorded in task 1's precondition (932 at `<GAP_BASE>` per `44.2-VERIFICATION.md`, plus the tests added by tasks 1-2). Counts go up, never down.
    - `uv run python -m black --check .`, `uv run python -m ruff check .` and `uv run python -m mypy typsphinx/` all exit `0`.
    - The docs HTML build prints `build succeeded` and no line matching `WARNING` or `ERROR` contains `configuration.rst`.
    - `ast.dump(ast.parse(...))` equality holds for both `typsphinx/template_engine.py` and `typsphinx/writer.py` against `<GAP_BASE>`, and `git diff --name-only <GAP_BASE> -- typsphinx/` is empty.
    - `git diff --name-only <GAP_BASE> --` is empty for `docs/source/user_guide/templates.rst`, `.planning/REQUIREMENTS.md`, `tests/test_params_authors_writers.py`, `tests/test_entry_metadata_route_uniformity.py`, all five `44.2-0[12345]-PLAN.md`, `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, all of `44.2-GATE-EVIDENCE-01..05.md`, and every `-SUMMARY.md`.
    - No file named `44.2-VERIFICATION.md` was created or modified by this plan.
    - `git diff <GAP_BASE> -- docs/source/user_guide/configuration.rst` touches only the item-4 sentence and the Precedence block; the `.. note::` at 230-238 and items 1, 2, 3, 5 are byte-identical.
  </acceptance_criteria>

  <done>
`configuration.rst` publishes the `authors` precedence as an ordered two-stage rule in which the
render stage is a first-class stage rather than a trailing afterthought, every universal and
negative claim names the stage it quantifies over, the round-3 drift guard's three literals and
plan 03's SC#5 deliverable survive verbatim, the production tree is proven byte-unchanged, and the
full phase gate is green against a baseline measured in the same session.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs -> user's `conf.py` | `configuration.rst` is the authority a user configures against. A wrong precedence rule here becomes a wrong configuration in every downstream project, with no error to signal it. A user who reads the current text and sets `typst_authors` alongside a mapping targeting only `title` is told the seed is final; the render stage then silently replaces it |
| one document section -> itself | the defect this round closes is an INTERNAL contradiction: two sentences in one section describing the same code path and disagreeing. The boundary is between what a reader takes from paragraph 1 and what paragraph 4 says |
| enumeration frame -> the sentences it can grade | an enumeration scoped to one function cannot grade a claim about a later stage. Round 3's frame was the boundary that let the defect through, not its diligence |
| test expectation -> the code it tests | a stage enumeration whose expected value is computed by running the walker is a tautology that would pass for a pipeline with an unenumerated sixth stage |
| plan-authored artifact name -> `/gsd-verify-phase`'s reserved output | `44.2-VERIFICATION.md` is written by the verifier and clobbers same-named plan output |
| production tree -> a text-accuracy plan | a docs/test plan that edits an executable line ships untested behaviour under a documentation banner, inside a patch release already carrying two user-visible changes |

## STRIDE Threat Register (ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44.2.6-01 | Information Disclosure | `docs/source/user_guide/configuration.rst` precedence guidance | high | mitigate | The published rule is derived from task 2's code-side stage enumeration rather than from the prose being corrected; every reachable stage's knob is asserted present in the section by `test_authors_section_names_every_stage_knob_that_can_write_authors`; the two known-false phrasings are negative-gated; and the exact falsifying configuration is a named passing gate at two levels (real `-b typst` build plus an API cell spanning both stages). Rated `high` — this boundary has now failed FOUR times and the current published text actively mispredicts a documented, tested, reachable configuration |
| T-44.2.6-02 | Spoofing | the stage enumeration's expected values | high | mitigate | `EXPECTED_STAGE_SITES`, `REACHABLE_STAGE_KNOBS` and `UNREACHABLE_STAGE_PROOFS` are hand-declared literals compared AGAINST the AST walk, never computed from it; the acceptance criteria require reading the module to confirm no production entry point is called in their construction. A tautological enumeration would have passed for round 3's frame too, which is exactly how the fourth writer stayed invisible |
| T-44.2.6-03 | Elevation of Privilege | an unclassified new pipeline stage | high | mitigate | `test_reachable_and_unreachable_stages_partition_the_enumeration` requires REACHABLE and UNREACHABLE to partition the enumerated set exactly and to be disjoint, and requires every UNREACHABLE entry to name an existing proof test. A new stage cannot be silently absorbed into "we already knew about that one" — the way `render()`'s merge was for three rounds |
| T-44.2.6-04 | Tampering | `typsphinx/template_engine.py` and `typsphinx/writer.py` (production files in a text-accuracy plan's blast radius) | medium | mitigate | Every task asserts `ast.dump(ast.parse(...))` equality against `<GAP_BASE>` for BOTH files and `git diff --name-only <GAP_BASE> -- typsphinx/` empty; a behaviour change cannot ride along under a docs banner |
| T-44.2.6-05 | Tampering | existing assertions in `tests/test_entry_metadata_precedence.py` and `tests/test_params_authors_writers.py` | medium | mitigate | Task 1 asserts the precedence module's collected count rises to 26 (never falls); task 3 asserts `tests/test_params_authors_writers.py` still reports 24 passed and is byte-identical to `<GAP_BASE>`; the rewrite satisfies the round-3 drift guard by preserving its three literals rather than by relaxing it |
| T-44.2.6-06 | Repudiation | the phase evidence trail (`GATE-EVIDENCE-01..05`, `REVIEW`, `VERIFICATION`, all five prior PLANs and every SUMMARY) | medium | mitigate | Task 3 asserts `git diff --name-only <GAP_BASE>` lists none of them; the correction is recorded FORWARD in plan 07's `44.2-GATE-EVIDENCE-06.md`, never by amending the record of what was published each round |
| T-44.2.6-07 | Tampering | `.planning/REQUIREMENTS.md` (`CONF-09`'s checkbox and the Traceability row) | medium | mitigate | This plan declares zero edits to that file and asserts `git diff --name-only <GAP_BASE> -- .planning/REQUIREMENTS.md` is empty; the recorded early-flip hazard cannot fire from here |
| T-44.2.6-08 | Denial of Service | the published docs page (a malformed enumerated or bulleted list breaks rendering) | low | mitigate | Task 3 runs a real `sphinx -b html` build and requires `build succeeded` with no WARNING/ERROR line naming `configuration.rst`; the blank-line requirement around both lists is stated explicitly in the action |
| T-44.2.6-09 | Denial of Service | the two new subprocess `-b typst` gates in a constrained sandbox | low | accept | The NixOS environmental-failure set is recorded and the precondition requires a same-session baseline; a build gate that fails environmentally is attributable by delta rather than mistaken for a regression. Accepted rather than mitigated because removing the real-build gate would remove the only end-to-end reproduction of the falsifying configuration |
| T-44.2.6-SC | Tampering | supply chain / package installs | low | accept | This plan installs nothing. `uv sync --extra dev --extra docs` resolves only already-pinned `pyproject.toml` extras; no dependency is added, removed or version-bumped, and neither `pyproject.toml` nor `uv.lock` appears in `files_modified`. No `## Package Legitimacy Audit` gate is required because no package-manager install task exists |

Three threats are rated `high` (T-44.2.6-01/02/03) and all three are dispositioned `mitigate` with
the mitigations named above. Nothing is left `accept`-ed at or above `security_block_on: high`; the
two `accept`-ed items are `low`.
</threat_model>

<deferred>
- **WR-01 (carry-forward, NOT a 44.2 gap, NOT planned here).** `typsphinx/writer.py:319-321`'s
  `effective_elements` merge raises an unhandled `TypeError` rather than a graceful `ExtensionError`
  when `typst_elements` is a non-`dict`. `44.2-VERIFICATION.md` confirms by `git log -S` that it was
  introduced in commit `cf22574` (Phase 27.1, 2026-07-25) and never modified by any 44.2 plan. It is
  a **Future Requirement candidate** for a builder-robustness phase, filed here as a note only. This
  plan does not touch it and asserts `writer.py` is byte-unchanged.
- **The `title` half of FA-05.** `title`'s published precedence outside item 4 (notably
  `templates.rst:189`, which Phase 45.1 SC#2 owns) is enumerated by task 2 but not prose-gated by
  this plan. If plan 07's stage-derived sentence set surfaces an analogous unscoped `title` claim
  inside `configuration.rst`, plan 07 fixes it; anything in `templates.rst` stays Phase 45.1's.
</deferred>

<verification>
Phase-level checks after all three tasks:

1. `uv run python -m pytest -q` — full suite green, passed count strictly greater than the
   same-session pre-change baseline recorded in task 1's precondition.
2. `uv run python -m black --check .`, `uv run python -m ruff check .`,
   `uv run python -m mypy typsphinx/` — all clean.
3. Docs HTML build succeeds with no WARNING/ERROR line naming `configuration.rst`.
4. `uv run python -m pytest tests/test_entry_metadata_precedence.py -q` reports 26 passed;
   `tests/test_params_authors_writers.py` still reports 24 passed and is byte-identical to
   `<GAP_BASE>`; `tests/test_authors_pipeline_stage_gate.py` collects at least 7 node ids, all
   passing.
5. `grep -c 'nothing else in typsphinx'` and `grep -c 'decided by one thing only'` are both `0` in
   `docs/source/user_guide/configuration.rst`; the nine preserved/added positive literals are all
   present whitespace-normalized.
6. `ast.dump(ast.parse(...))` equality holds against `<GAP_BASE>` for `typsphinx/template_engine.py`
   and `typsphinx/writer.py`; `git diff --name-only <GAP_BASE> -- typsphinx/` is empty.
7. `git diff --name-only <GAP_BASE>` lists none of `44.2-0[12345]-PLAN.md`, any `-SUMMARY.md`,
   `44.2-REVIEW.md`, `44.2-VERIFICATION.md`, `44.2-GATE-EVIDENCE-01..05.md`,
   `.planning/REQUIREMENTS.md`, or `docs/source/user_guide/templates.rst`.
8. No file named `44.2-VERIFICATION.md` was created or modified by this plan.
</verification>

<success_criteria>
- `docs/source/user_guide/configuration.rst` publishes the `authors` precedence as an ordered
  two-stage rule; the render stage is stated as a stage rather than as a trailing sentence; and
  every universal or negative-existential claim in the Author Information section and in item 4
  names the stage it quantifies over. The two known-false phrasings are negative-gated to zero
  occurrences.
- The three-knob configuration that falsified round 3 is pinned by name at two levels: a real
  `sphinx-build -b typst` gate with an attribution control, and one API-level cell that asserts the
  seed survives `map_parameters()` and is replaced by `render()` in the same body —
  `44.2-VERIFICATION.md`'s `missing:` bullet 3, closed.
- The writer frame is the pipeline: every `authors`/`title`-determining site in `typsphinx/` is
  enumerated from the code, including the two `render()` merges and `writer.py`'s `toctree_options`
  update, and a partition assertion forbids any enumerated site from being dropped from both the
  REACHABLE and the UNREACHABLE-WITH-PROOF class.
- A future stage regression fails in CI rather than at a fifth verify round: the enumeration's
  assertion message names the published prose, the D-05 comment and `REACHABLE_STAGE_KNOBS` as the
  things to re-derive before the expectation may be changed.
- The production diff is empty, the historical record is byte-identical, `.planning/REQUIREMENTS.md`
  is untouched, and the full SC#6 gate is green against a same-session baseline.
</success_criteria>

<output>
Create `.planning/phases/44.2-typst-documents-title-and-author-consumption/44.2-06-SUMMARY.md` when
done. Record in it: the verbatim pre-change full-suite baseline, the NixOS workaround applied (if
any), the resolved worktree `typsphinx.__file__` path, the measured colour of
`test_authors_section_names_every_stage_knob_that_can_write_authors` before and after task 3, and
any discrepancy between `<measured_grounding>` §1's stage table and what the AST walk actually found
(the source wins).
</output>
