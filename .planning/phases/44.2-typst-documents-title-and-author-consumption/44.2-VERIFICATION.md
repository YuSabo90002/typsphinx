---
phase: 44.2-typst-documents-title-and-author-consumption
verified: 2026-08-06T22:27:45Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/6
  gaps_closed:
    - "SC#2 / plan 05's backstop must-have (round-3 gap): configuration.rst:194's unscoped 'and
      nothing else in typsphinx replaces it' claim is gone (grep -c 'nothing else in typsphinx'
      configuration.rst -> 0, independently re-run this session). The Precedence block is now an
      explicit, ordered TWO-STAGE rule (stage 1: the parameter mapping inside map_parameters();
      stage 2: typst_template_function's dict-form params, applied later in render()), with every
      universal/negative-existential claim scoped to the stage it quantifies over."
    - "The exact three-knob configuration this verification chain falsified across rounds 3 and 4
      (typst_authors set, a mapping targeting only 'title', typst_template_function's
      params['authors'] set) was independently reproduced THIS session with a real
      `sphinx-build -b typst` (not the project's own test, not SUMMARY narrative): the emitted
      .typ carries `authors: (\"FROM TEMPLATE FUNCTION\",)`, matching what the corrected prose
      now claims (stage 2 overrides stage 1's typst_authors seed), not what the old, now-deleted
      sentence claimed. A control build with the same config minus typst_template_function was
      also independently reproduced and confirmed the typst_authors seed survives, as the
      corrected prose's stage-1 half claims."
    - "A residual, narrower instance of the same defect class (two Precedence-block bullets,
      S-14/S-16, using unscoped 'is then passed' / 'lands...is then' language that read as claims
      about the final rendered output rather than merely stage 1's own return value) was found and
      fixed by plan 07, an INDEPENDENT grader running in its own worktree/session (not the plan
      that wrote the correction). Both bullets now read '...subject to stage 2's override' /
      '...again subject to stage 2's override', confirmed present in the current file at lines
      223-226 and 229-231 by direct Read this session, and pinned by a new drift-guard test
      (`test_configuration_rst_stage1_bullets_name_stage2_override_explicitly`, independently
      re-run and PASSED this session)."
    - "The per-sentence falsification enumeration was widened from map_parameters()-only (round
      3's blind spot) to the whole pipeline: an AST walk over BOTH typsphinx/template_engine.py
      AND typsphinx/writer.py enumerates all 9 write sites and partitions them
      REACHABLE/UNREACHABLE-WITH-PROOF with a mechanically-enforced no-silent-drop assertion. The
      falsification row space gained a typst_template_function tf/notf axis (36 rows), the
      specific axis absent from round 3's row space that let the false claim through undetected.
      23 claim-bearing sentences were swept from the whole configuration.rst file plus
      template_engine.py's D-05 comment block (derived from the stage list outward, not from the
      passages any plan happened to edit) and graded against a named falsification row on BOTH
      stages; a mechanical equality assertion (23 surfaced == 23 graded) was independently re-run
      this session against the current file and reproduced exactly (23/23, all ACCURATE, zero
      residual NARROWER-THAN-CODE or FALSE-AT-PIPELINE-SCOPE verdicts)."
    - "17 named tests, one per clause/enumerated stage, independently re-run this session with
      `-v`: 17/17 PASSED. Full targeted module set (test_authors_pipeline_stage_gate.py +
      test_params_authors_writers.py + test_entry_metadata_precedence.py): 58/58 PASSED,
      independently re-run this session."
  gaps_remaining: []
  regressions: []
gaps: []
---

# Phase 44.2: `typst_documents` Title and Author Consumption Verification Report

**Phase Goal:** The `typst_documents` entry elements the documentation publishes actually take
effect — `entry[2]` (title) and `entry[3]` (author) must reach the compiled PDF, overriding
`config.project`/`config.author`, with a defined and tested precedence against `typst_authors`,
exactly as Sphinx's LaTeX builder already does for its own five-element entries.

**Verified:** 2026-08-06T22:27:45Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plans 06 and 07, round 4)

## Independence note

This is the fourth verification round. Rounds 1-3 each let a false unfalsified/unscoped claim
through because the enumeration frame that graded the prose was derived from the passages the
correcting plan itself had edited, not from the code's own writers. Per this round's brief, every
check below was re-derived from the code's own assignment sites (an independent `grep`/AST sweep
of `typsphinx/template_engine.py` and `typsphinx/writer.py`, not from the sentences plans 06/07
chose to touch) and every behavioral claim was independently reproduced with a real
`sphinx-build -b typst`, not trusted from `44.2-06-SUMMARY.md`, `44.2-07-SUMMARY.md`, or
`44.2-GATE-EVIDENCE-06.md`'s own narrative. Where this report cites `44.2-GATE-EVIDENCE-06.md`,
it does so only after the cited figure was reproduced independently in this session (see command
outputs throughout).

## Goal Achievement

### Observable Truths (ROADMAP §Phase 44.2 SC#1–SC#6)

| # | Truth (SC) | Status | Evidence |
|---|---|---|---|
| 1 | SC#1: an entry's `[2]`/`[3]` reach the compiled PDF's own metadata, overriding `config.project`/`config.author` | ✓ VERIFIED | `uv run python -m pytest tests/test_document_metadata_render_gate.py -q` → 3 passed, reproduced this session. `typsphinx/` is `ast`-equal to the round-3 gap base (`e977156...`); this mechanism is untouched by plans 06/07 (docs+tests only). |
| 2 | SC#2: precedence defined + asserted for every combination that can occur, not just the happy path; each rule has a test | ✓ VERIFIED | The three-knob configuration that falsified rounds 3-4 (`typst_authors` set + mapping targeting only `title` + `typst_template_function`'s `params["authors"]` set) was independently rebuilt and run with a real `sphinx-build -b typst` this session: emitted `authors: ("FROM TEMPLATE FUNCTION",)`, matching the corrected prose's stage-2-override claim. A control build (same config minus `typst_template_function`) independently confirmed the `typst_authors` seed survives, matching the stage-1 claim. `configuration.rst`'s Precedence block, independently re-read this session (lines 190-256), is now an ordered two-stage rule with every claim scoped to the stage it quantifies over — `grep -c "nothing else in typsphinx"` and `grep -c "decided by one thing only"` both return 0. The §3-§5 per-sentence falsification enumeration in `44.2-GATE-EVIDENCE-06.md` (23 sentences swept from the whole file + the D-05 comment block, stage-derived rather than edit-derived) was independently re-executed this session — its own equality-assertion probe reproduced `S-ids 23, graded rows 23`, all 23 verdicts `ACCURATE`, zero residual `NARROWER-THAN-CODE`/`FALSE-AT-PIPELINE-SCOPE`. 17 named clause/stage-pinning tests independently re-run with `-v`: 17/17 PASSED. Own independent sweep of `configuration.rst` for residual unscoped quantifiers (`always\|never\|only\|guarantee\|no other\|nothing else\|regardless\|every case\|exactly one\|sole\|solely`) found no remaining hit outside the ones already graded ACCURATE in §4 (each explicitly scoped to "that mapping step" / "stage 1" / etc.). |
| 3 | SC#3: multi-master builds keep per-document title/author, no leak | ✓ VERIFIED | `uv run python -m pytest tests/test_multi_master_metadata_no_leak.py -q` → 5 passed, reproduced this session. Untouched by plans 06/07. |
| 4 | SC#4: the divergent in-repo entries are handled deliberately; the rest of the corpus is proven unchanged | ✓ VERIFIED | `uv run python -m pytest tests/test_entry_metadata_route_uniformity.py -q` → 5 passed, reproduced this session. `44.2-GATE-EVIDENCE-03.md` and the corpus partition are untouched by plans 06/07 (`git diff <round-3 base> --name-only` does not list them). |
| 5 | SC#5: the fifth element is given meaning or documented accepted-and-ignored; no contract element left silently inert | ✓ VERIFIED | `grep -n "accepted and ignored" docs/source/user_guide/configuration.rst` → line 63, present, independently re-checked this session; untouched by plans 06/07's diff. |
| 6 | SC#6: full suite, `black`/`ruff`/`mypy` green, GATE-01 corpus gate builds fatal-free | ✓ VERIFIED | Independently re-run this session (main tree, not worktree, not trusted from any SUMMARY): `uv run python -m pytest -q` → **941 passed, 1 skipped, 0 failed**. `uv run python -m black --check .` → clean, 239 files. `uv run python -m ruff check .` → all checks passed. `uv run python -m mypy typsphinx/` → no issues, 6 files. `uv run python -m sphinx -b html docs/source <tmp>` → build succeeded, 3 warnings; `grep -iE "warning|error" <log> | grep -i configuration.rst` → 0 hits (the 3 warnings are pre-existing `translator.py` docstring issues, unrelated). |

**Score:** 6/6 truths verified. `behavior_unverified: 0` — SC#2's state-transition/precedence claim
was independently exercised with two real `sphinx-build` runs (three-knob build and its
attribution control) in this session, not left on symbol presence alone.

### Round-4 Gap Closure — What Was Actually Checked, Not Just Claimed

The round-3 gap (preserved at
`/tmp/claude-1000/.../44.2-VERIFICATION.round3.bak.md`) was: `configuration.rst:194`'s "and
nothing else in typsphinx replaces it" is an unscoped, pipeline-wide claim that is false, because
`typst_template_function`'s `params["authors"]` (applied later, in `render()`) also replaces the
`typst_authors` seed — and plan 05's own backstop (a per-sentence falsification enumeration)
missed grading that exact clause.

Independent checks performed this session (not transcribed from any SUMMARY or GATE-EVIDENCE
narrative):

1. **The false sentence is gone.** `grep -c "nothing else in typsphinx" docs/source/user_guide/configuration.rst` → `0`. `grep -c "decided by one thing only"` → `0` (plan 06's own SUMMARY claimed this; independently reproduced here).
2. **The replacement is not merely present but behaviorally correct.** Built a project reproducing the exact three-knob configuration and ran a real `sphinx-build -b typst`: `authors: ("FROM TEMPLATE FUNCTION",)` was emitted — matching the corrected prose's stage-2-override claim, not the deleted claim. A control build (same config, `typst_template_function` removed) emitted `authors: ((name: "Jane Doe", organization: "MIT"),)` — the seed survives, matching the corrected prose's stage-1 claim.
3. **The per-sentence enumeration mechanism itself was re-run, not trusted.** Extracted and executed `44.2-GATE-EVIDENCE-06.md`'s own §5 equality-assertion Python probe against the CURRENT file this session: `S-ids 23, graded rows 23`; every verdict token in §4 is `ACCURATE` (no `NARROWER-THAN-CODE`/`FALSE-AT-PIPELINE-SCOPE` left).
4. **The residual plan 07 found and fixed (S-14/S-16) is present in the current file, not just claimed fixed.** Direct `Read` of `docs/source/user_guide/configuration.rst` lines 220-231 this session confirms both bullets now read "...subject to stage 2's override above..." / "...again subject to stage 2's override." and the drift-guard test (`test_configuration_rst_stage1_bullets_name_stage2_override_explicitly`) independently re-run and PASSED.
5. **No production code changed.** `git diff <round-3 base> -- typsphinx/` is empty; `ast.dump(ast.parse(...))` equality independently re-confirmed for both `template_engine.py` and `writer.py` against the round-3 base this session.
6. **Independent residual sweep, not scoped to the passages plans 06/07 edited.** Grepped the WHOLE `configuration.rst` for `always|never|only|guarantee|no other|nothing else|regardless|in all cases|every case|exactly one|sole|solely` — every hit outside the Document Language section (a different feature, out of this defect class's scope) is one of the 23 sentences already graded `ACCURATE` in §4, each explicitly scoped to a named stage ("that mapping step", "stage 1", "in this method").

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| CONF-09 | 44.2-01..07-PLAN.md (all seven declare `requirements: [CONF-09]`) | An explicit `typst_documents` entry's `[2]` title / `[3]` author take effect, overriding `config.project`/`config.author`, with a defined and tested precedence against `typst_authors`, no element of the five-element contract left silently inert | ✓ SATISFIED | Title/author-reaches-PDF (SC#1), multi-master isolation (SC#3), the corpus partition (SC#4), and the five-element contract's fifth-element documentation (SC#5) all independently re-verified this session. The "defined and tested precedence" clause — the exact clause SC#2 failed on for three rounds — is now independently reproduced behaviorally (two real builds) and its own falsification-enumeration mechanism re-run and found complete (23/23 ACCURATE). `.planning/REQUIREMENTS.md`'s CONF-09 checkbox is still `- [ ]` / "Pending" in the Traceability table — correctly left unflipped by every plan through round 4 (verified `git diff` empty against that file across plans 06/07); flipping it is outside this verifier's scope and belongs to a later phase-completion step, not a gap of this phase. |

No orphaned requirements: `.planning/REQUIREMENTS.md` maps only CONF-09 to Phase 44.2, and all
seven plans declare exactly `requirements: [CONF-09]`.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `typsphinx/template_engine.py`, `typsphinx/writer.py` | No production-code change; comment-only | ✓ VERIFIED | `ast.dump(ast.parse(...))` independently confirmed structurally identical to the round-3 base this session, for both files. `git diff <round-3 base> -- typsphinx/` empty. |
| `docs/source/user_guide/configuration.rst` | Precedence block republished as an ordered, stage-scoped two-stage rule; both known-false phrasings gone; S-14/S-16 residual fixed | ✓ VERIFIED | Directly `Read` this session, lines 30-256. Two-stage structure present (lines 190-203); every claim in the Author Information section names its stage; `grep -c` for both prior false phrasings → 0 each; S-14/S-16 carry the "subject to stage 2's override" cross-reference. |
| `tests/test_authors_pipeline_stage_gate.py` | New module: pipeline-wide AST stage enumeration, REACHABLE/UNREACHABLE partition, two real-build gates, drift guards | ✓ VERIFIED, WIRED | 8 tests collected and independently re-run with `-v`: 8/8 PASSED, including both real `sphinx-build` gates and the new S-14/S-16 drift guard. |
| `tests/test_entry_metadata_precedence.py` | One new Group-3 cell spanning `map_parameters()` + `render()` in one test body | ✓ VERIFIED, WIRED | `test_render_template_function_authors_replaces_map_parameters_surviving_seed` present and PASSED in this session's `-v` run; 26 tests collected total (up from round 3's 25). |
| `44.2-GATE-EVIDENCE-06.md` | Independently-derived, per-sentence, per-stage falsification enumeration produced by a SEPARATE grader (plan 07, distinct worktree/session from plan 06's correction) | ✓ VERIFIED | File exists (§0-§7 + Self-Check). §5's own equality-assertion probe extracted and re-executed against the CURRENT file this session, reproducing `23 == 23`, all `ACCURATE`. §0 confirms a distinct worktree (`agent-a6307571c95a824d4`) from plan 06's (`agent-a9e1de63a0ead7dd8`). |
| `.planning/REQUIREMENTS.md`, `44.2-01..05-PLAN.md`/`-SUMMARY.md`, `44.2-REVIEW.md`, `44.2-GATE-EVIDENCE-01..05.md` | Historical record, untouched by plans 06/07 | ✓ VERIFIED | `git diff <round-3 base> --name-only` for each of these confirmed empty this session; the round-4 diff touches only `docs/source/user_guide/configuration.rst`, `tests/test_authors_pipeline_stage_gate.py`, `tests/test_entry_metadata_precedence.py`, `44.2-GATE-EVIDENCE-06.md`, and phase-tracking files (`ROADMAP.md`, `STATE.md`, plan/summary files for 06/07). |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `typst_documents` entry | compiled PDF metadata | `_resolve_entry_element` → `sphinx_metadata` → `map_parameters()` → `#show:` call → PDF info dict | ✓ WIRED | Untouched by plans 06/07 (docs/tests-only); SC#1's real-compile gate still passes. |
| `map_parameters()`'s seed/mapping-loop/back-fill (stage 1) → `render()`'s `typst_template_function` merge (stage 2) | the published two-stage precedence rule | AST enumeration over BOTH `typsphinx/template_engine.py` and `typsphinx/writer.py`, partitioning 9 sites (2 REACHABLE knobs beyond stage 1's own two, 5 UNREACHABLE-WITH-PROOF) | ✓ WIRED, BEHAVIORALLY CONFIRMED | Independently re-run: `test_pipeline_stage_sites_that_can_determine_authors_are_exactly_these` and `test_reachable_and_unreachable_stages_partition_the_enumeration` PASSED this session; the two real `sphinx-build` reproductions above independently confirm the wiring end to end, not just through a test's internal call graph. |
| `render()`'s `typst_template_function` params merge | overrides whatever `map_parameters()` produced, including the `typst_authors` seed | `all_params.update(params)` then `all_params.update(self.typst_template_params)` | ✓ WIRED, ACCURATELY DOCUMENTED | The link is real (confirmed via a real build, not just a test), and — unlike round 3 — the prose describing it is now correctly scoped: no remaining sentence claims this override doesn't exist or claims something contrary to it at pipeline scope. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Three-knob scenario (the exact configuration that falsified rounds 3-4): `typst_authors` set, mapping targeting only `title`, `typst_template_function.params["authors"]` set | `sphinx-build -b typst` on an independently-constructed project (not reusing any test fixture) | emitted `authors: ("FROM TEMPLATE FUNCTION",)` | ✓ PASS — matches the corrected prose, falsifies nothing |
| Attribution control: same config minus `typst_template_function` | `sphinx-build -b typst` | emitted `authors: ((name: "Jane Doe", organization: "MIT"),)` — the `typst_authors` seed | ✓ PASS — confirms stage 1's claim independently |
| §5's no-silent-drop equality probe, re-extracted and re-run against the current `44.2-GATE-EVIDENCE-06.md` | inline Python (pasted from the evidence file's own §5) | `S-ids 23 graded rows 23`; all verdicts `{'ACCURATE'}` | ✓ PASS |
| Named clause/stage-pinning tests (17 rows from §6, plus the 8-test module and 26-test module they draw from) | `uv run python -m pytest tests/test_authors_pipeline_stage_gate.py tests/test_params_authors_writers.py tests/test_entry_metadata_precedence.py -v` | 58 passed | ✓ PASS |
| Full pytest suite (run once) | `uv run python -m pytest -q` | 941 passed, 1 skipped, 0 failed | ✓ PASS |
| Lint/format/types | `uv run black --check .` / `uv run ruff check .` / `uv run mypy typsphinx/` | clean / clean / clean | ✓ PASS |
| Docs HTML build, no `configuration.rst` WARNING/ERROR | `uv run python -m sphinx -b html docs/source <tmp>` | build succeeded, 3 pre-existing unrelated warnings, 0 naming `configuration.rst` | ✓ PASS |
| Production code unchanged | `ast.dump(ast.parse(...))` equality for both `template_engine.py` and `writer.py` against round-3 base | equal | ✓ PASS |

### Anti-Patterns Found

None. `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` over
`docs/source/user_guide/configuration.rst`, `tests/test_authors_pipeline_stage_gate.py`,
`tests/test_entry_metadata_precedence.py`, and `typsphinx/template_engine.py` returns zero
matches.

### Carry-Forward Items (Out of Scope for This Phase — Not Gaps)

- **`44.2-REVIEW.md` WR-01** (`typsphinx/writer.py:319-321`'s `effective_elements` merge raises an
  unhandled `TypeError` rather than a graceful `ExtensionError` on a non-`dict` `typst_elements`).
  Confirmed pre-existing (introduced in phase 27.1's commit `cf22574`, 2026-07-25), unrelated to
  CONF-09, and untouched by any of the seven 44.2 plans. Not a blocker for this phase.
- **`44.2-REVIEW.md` IN-01** (`test_invalid_mapping_type` never calls `app.build()` and does not
  actually exercise the misconfiguration it claims to test). Predates phase 44.2, untouched by its
  diff. A test-quality note, not a CONF-09 defect.
- **`docs/source/user_guide/templates.rst:189`** ("`title`: Document title (from
  `typst_documents`)") is now TRUE (CONF-09 has landed the behavior it claims), but verifying/
  rewriting that file is explicitly Phase 45.1 SC#2's job per the ROADMAP's own phase-ordering
  note, not this phase's.

### Human Verification Required

None. Every truth in this report was independently verified this session by direct code/doc
measurement, direct test execution, or a directly-constructed `sphinx-build` reproduction (two
real builds, not reused from any existing test fixture) — no visual, real-time, or subjective
judgment was required. This is the fourth round; the specific failure mode of the first three
rounds (accepting a plan's own narrative or a narrowly-scoped enumeration as sufficient) was
avoided by independently re-deriving the check set from the code and re-executing every
probe/test/build cited above in this session rather than trusting any SUMMARY or GATE-EVIDENCE
narrative at face value.

### Gaps Summary

No gaps. The round-3/round-4 defect chain — an unfalsified universal or negative-existential claim
about when `params["authors"]` is written, recurring at progressively narrower scope across three
prior rounds because each correction's own grading mechanism was framed from the passages it
edited rather than from the code's writers — is closed. This round's closure (plans 06 and 07)
structurally differs from rounds 1-3: the enumeration frame is computed from an AST walk over BOTH
pipeline modules before any sentence is read, the falsification row space includes a
`typst_template_function` tf/notf axis that distinguishes "survives the mapping stage" from
"reaches the emitted output" (the specific axis absent from round 3's row space), and the grading
was performed by an independent second plan (07) in a separate worktree/session from the one that
wrote the correction (06) — which is precisely how it caught a residual (S-14/S-16) that the
correcting plan's own re-read had missed. This verification independently re-executed the
mechanical probes underlying both plans' claims (the equality assertion, the two real builds, the
58-test named run, the full suite, lint/type/docs gates) rather than accepting their SUMMARYs, and
found no discrepancy between what was claimed and what is actually in the tree.

---

_Verified: 2026-08-06T22:27:45Z_
_Verifier: Claude (gsd-verifier)_
