---
phase: 61-v0-9-1-release-prep-prep-only
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md
  - .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md
  - .planning/phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md
autonomous: true
requirements: [REL-09]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'D-10 / SC#4 (RETAINED in full): a sha256 checksum of .planning/REQUIREMENTS.md, its line count, a wall-clock UTC timestamp, and the verbatim byte content of all three REL-09-bearing lines are recorded at PHASE HEAD, before any other plan in this phase has run — this is the detector for the phase.complete auto-flip that has fired at FIVE consecutive release-prep closes.'
    - 'D-08: REL-09 is measurably still unmet at phase head — its checkbox line is still an unchecked box and its Traceability row still reads Phase 61 / Pending — and this plan reads .planning/REQUIREMENTS.md without ever editing it.'
    - 'D-10 / SC#4, observation 1 of 2: the no-irreversible-action fence is probed and recorded with a wall-clock UTC timestamp — no local tag for the skipped version, no such tag on the remote, the latest published GitHub Release is still the PRIOR milestone''s v0.9.0, and release.yml has no run for a skipped-version tag. Each negative probe is paired with a POSITIVE control proving the probe actually reached its source, so an unreachable remote fails loudly instead of passing vacuously.'
    - 'The phase base SHA is recorded here, at phase head, so plan 61-04 can scope its typsphinx/ diff and its whole-tree positive control against a real anchor rather than an assumed one.'
    - 'D-10 open-question resolution, recorded rather than assumed: D-10''s literal text names exactly four fence items (tag probe, no-publish probe, typsphinx/ diff, REQUIREMENTS.md checksum) and does NOT name the milestone-invariant sweep that Phases 52 and 57 ran to back a `### Verified` section. Because this phase authors no `### Verified` section (Claude''s Discretion, taken as the cheaper default in 61-01), the invariant sweep is deliberately not run, and 61-SC4-INVARIANTS.md says so in its own words.'
    - 'The plan-time external-API detector result is recorded verbatim in COVERAGE.md as a reasoned declaration, so a seal-time false positive over this phase''s gh- and workflow-dense plan prose is met with an explanation rather than a fabricated capability matrix.'
    - 'REL-09 is cited by this plan as the phase''s only requirement ID for coverage purposes only. This plan does NOT close it and does not touch its checkbox; per D-08 it stays ``[ ]`` and carries forward unmet to the v0.9.2 milestone with its literal wording unchanged.'
  artifacts:
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md
    - .planning/phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md
  key_links:
    - '.planning/REQUIREMENTS.md at phase head -> 61-CLOSEOUT-GUARD.md baseline -> 61-04''s close-time re-verification -> a hand reversion of any flip: the only defence this project has against the phase.complete auto-flip, which has now fired five consecutive times and is MORE likely to slip through here than at prior closes, because D-08 leaves REL-09''s wording no longer matching what the phase did.'
    - 'fence probe observation 1 (this plan, wave 1) -> fence probe observation 2 (61-04, wave 3): the two observations are placed two waves apart precisely so the timestamps are genuinely separated rather than collapsed into one command run.'
    - 'phase base SHA recorded here -> 61-04''s scoped git diff over typsphinx/: without a recorded anchor the phase-scoped diff has nothing real to measure against.'
  prohibitions:
    - 'MUST NOT perform any irreversible action in this phase — no local or remote tag, no PyPI publish, no GitHub Release, no pull request, no trigger of release.yml. The prep-only fence is absolute and has no amended exception in this phase.'
    - 'MUST NOT record REL-09 as satisfied. No checkbox flip, no Traceability-row status change, no prose claiming a release occurred. .planning/REQUIREMENTS.md is READ and QUOTED by this plan and never edited; a flip found later is reverted, never shipped.'
    - 'MUST NOT record inherited, paraphrased or fabricated evidence as this phase''s own. Every probe in this plan is executed here and transcribed verbatim; a negative result with no positive control proving the probe reached its source is not evidence.'
    - 'MUST NOT fabricate an external-API capability matrix for an integration this phase does not have. COVERAGE.md is a reasoned declaration recording the real detector result, following the Phase 57 and Phase 60 precedent.'
---

<objective>
Record, at phase head and before any other plan has changed anything, the three things this phase's
fence proof needs as its anchors: a checksum guard over `.planning/REQUIREMENTS.md` that will catch
the `phase.complete` auto-flip of REL-09, the first of two separated fence observations proving no
irreversible action has occurred, and the phase base SHA every later scoped diff measures against.
Also record the external-API coverage declaration this project has twice needed at seal time.

Purpose: two of these must happen *first* or they are worthless. A checksum recorded after another
plan has already run cannot prove the file was untouched at phase head, and a fence observation taken
at the same moment as its sibling is one observation, not two separated ones. This plan is
deliberately thin and deliberately early.

Output: `61-CLOSEOUT-GUARD.md`, `61-SC4-INVARIANTS.md` (observation 1 of 2), and `COVERAGE.md`.

**Phase reframe (read before judging this plan against ROADMAP.md).** `61-CONTEXT.md` D-11 maps
ROADMAP Phase 61's success criteria onto what this phase actually does: SC#1 is **DROPPED**, SC#2 is
**REWORDED**, SC#3 is **RETAINED and re-anchored**, SC#4 is **RETAINED in full**, SC#5 is **RETAINED
and re-aimed** at what v0.9.2 inherits. This plan implements the first half of the D-11-mapped SC#4.
It does not bump a version, does not create a versioned release section, and does not close REL-09.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md
@.planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution";
`.planning/REQUIREMENTS.md` § "Standing constraints" #5). Before running ANY command in this plan,
provision this worktree's own virtual environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command that invokes project tooling with `uv run`. This plan runs no
test suite, but the provisioning rule is unconditional and a later task in this phase depends on the
worktree being correctly provisioned.
</worktree_provisioning>

<!-- planner-discipline-allow: Latest -->
<!-- The word above is the marker the GitHub release listing prints against the current release, and
     it is the POSITIVE control (count == 1, proving the command reached GitHub) that makes the
     paired negative probe non-vacuous. It is part of the verification command's own grep regex. -->

<tasks>

<task type="auto">
  <name>Task 1: Record the phase-head REQUIREMENTS.md closeout guard and the phase base SHA</name>
  <files>.planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md § "REL — release" (REL-09's checkbox line, around line 127), § "Traceability" (the REL-09 row, around line 206) and the phase-totals line naming Phase 61 (around line 220) — the three lines this guard protects. Read them so the recorded quotes are byte-accurate.
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/57-CLOSEOUT-GUARD.md — the full structure to mirror almost 1:1, substituting REL-08 for REL-09 and the new checksum and line numbers.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md § "`61-CLOSEOUT-GUARD.md`" — the section-by-section skeleton, the live baseline values measured at research time, and the explicit instruction to re-run the commands rather than transcribe those values as final.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md § `<decisions>` D-08 and D-10, and § `<specifics>` item 4.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md § "Pitfall 1" — why the flip happens (generic close tooling pattern-matching a completed Traceability row) and the exact avoidance protocol.
  </read_first>
  <action>
    Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md`, mirroring
    `57-CLOSEOUT-GUARD.md`'s structure.

    Open with a `# Phase 61 — REL-09 Checkbox-Flip Closeout Guard` heading and a one-line statement
    that this task changes NO requirement state: REL-09 stays an unchecked box and its Traceability
    row stays Pending; `.planning/REQUIREMENTS.md` is read and quoted here and never edited.

    Under a `## Baseline` section record, each as the verbatim command followed by its verbatim
    output: `sha256sum .planning/REQUIREMENTS.md`, `wc -l .planning/REQUIREMENTS.md`,
    `date -u +"%Y-%m-%dT%H:%M:%SZ"`, and `git rev-parse HEAD`. Label that last value explicitly as
    **PHASE_BASE_SHA** — plan 61-04 reads it back from this file by name to scope its `typsphinx/`
    diff, so it must be a bare 40-character hex string on its own line, easy to extract.

    The values measured at research time were `4682f8cde6b068c2ebbe42201fdff4b0b4cf17558d68c889baaf2f4506d531e1`
    for the checksum and 258 for the line count. Re-run both commands yourself rather than
    transcribing those; they are supplied so you can cross-check. If either differs, do NOT silently
    record the new value as the baseline: record BOTH the research-time value and your measured value,
    run `git diff -- .planning/REQUIREMENTS.md` and `git log --oneline -3 -- .planning/REQUIREMENTS.md`
    to find what moved, and HALT with that finding. A requirements file that changed between planning
    and execution is exactly the class of event this guard exists to notice.

    Under a `## The lines under guard` section paste the verbatim output of
    `grep -n 'REL-09' .planning/REQUIREMENTS.md`. There should be three hits: the unchecked-checkbox
    requirement line, the Traceability row reading Phase 61 and Pending, and the phase-totals line.
    Quote all three with their line numbers exactly as printed.

    Under a `## Why this file exists` section state the count plainly: `phase.complete`-family tooling
    has auto-flipped the release requirement's checkbox and Traceability-row state against an explicit
    CONTEXT decision at **five consecutive** release-prep closes. Phase 61 increments the running count
    that `57-CLOSEOUT-GUARD.md` recorded as four. Add the reason this close is MORE exposed than prior
    ones rather than less: per D-08 REL-09's wording still names a PyPI release of the skipped version,
    which is no longer what this phase did, so a flip here would be both wrong and plausible-looking.

    Under a `## Re-verification protocol (phase close)` section give the exact commands plan 61-04 will
    run — the checksum re-run compared byte-for-byte against the Baseline, a `git diff --name-only`
    restricted to `.planning/REQUIREMENTS.md` expected to print nothing, and the `grep -n 'REL-09'`
    re-run expected byte-identical to the quotes above.

    Under a `## Post-close detection and reversion (after phase.complete runs)` section give the same
    three commands again, framed for the moment AFTER the phase-completion tooling has run — which is
    outside any plan's reach and is precisely when the flip has historically landed — together with the
    reversion instruction `git checkout -- .planning/REQUIREMENTS.md` and the explicit rule that the
    flip is reverted and reported, never accepted and never committed. State that this section is
    reproduced in `61-HANDOFF.md` so an operator following the handoff reaches it without opening this
    file.
  </action>
  <verify>
    <automated>test -f .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(grep -oE '[0-9a-f]{64}' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md | head -1)" ] &amp;&amp; [ "$(git rev-parse HEAD)" = "$(grep -oE '^[0-9a-f]{40}$' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md | head -1)" ] &amp;&amp; grep -qE '^- \[ \] \*\*REL-09\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-09 \| Phase 61 \| Pending \|' .planning/REQUIREMENTS.md &amp;&amp; [ "$(grep -c 'REL-09' .planning/REQUIREMENTS.md)" = 3 ] &amp;&amp; grep -q 'five consecutive' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md &amp;&amp; grep -q 'Post-close detection and reversion' .planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `61-CLOSEOUT-GUARD.md` § "Baseline" holds four command-plus-output blocks: the sha256 of `.planning/REQUIREMENTS.md`, its line count, a UTC timestamp in `YYYY-MM-DDTHH:MM:SSZ` form, and `git rev-parse HEAD` labelled **PHASE_BASE_SHA** as a bare 40-character hex string on its own line
    - the recorded sha256 equals the live `sha256sum .planning/REQUIREMENTS.md` at verification time, and the recorded PHASE_BASE_SHA equals the live `git rev-parse HEAD`
    - `61-CLOSEOUT-GUARD.md` § "The lines under guard" holds the verbatim three-line output of `grep -n 'REL-09' .planning/REQUIREMENTS.md`, with line numbers
    - `.planning/REQUIREMENTS.md` still matches `^- \[ \] \*\*REL-09\*\*` and `^\| REL-09 \| Phase 61 \| Pending \|`, and `grep -c 'REL-09'` over it returns exactly 3
    - `61-CLOSEOUT-GUARD.md` § "Why this file exists" states the count as **five consecutive** release-prep closes and explains why D-08's unchanged wording makes this close more exposed, not less
    - `61-CLOSEOUT-GUARD.md` contains a `## Re-verification protocol (phase close)` section and a `## Post-close detection and reversion (after phase.complete runs)` section, the second carrying the `git checkout -- .planning/REQUIREMENTS.md` reversion instruction and the revert-never-accept rule
    - `git status --porcelain .planning/REQUIREMENTS.md` is empty — this task reads and quotes that file and never edits it
  </acceptance_criteria>
  <done>The phase-head checksum, line count, timestamp, phase base SHA and the three verbatim REL-09-bearing lines are recorded, together with both the close-time and the post-`phase.complete` detection-and-reversion protocols, and `.planning/REQUIREMENTS.md` is provably unmodified.</done>
  <reversibility rating="reversible">One new planning document; no product-tree or requirement-state change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Record fence observation 1 of 2 with positive controls, and resolve the milestone-invariant-sweep question in writing</name>
  <files>.planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md</files>
  <read_first>
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/57-SC4-INVARIANTS.md — the most recent fence-proof precedent, including its anchor-and-coincidence command block and its "hunk-level argument" escalation pattern for when an empty-diff assertion no longer suffices.
    - .planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md — the two-observation shape (minutes apart, same phase) that is the accepted minimum bar, and its explicit warning against copying a prior milestone's "no new config value" assertion forward unexamined.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-CONTEXT.md § `<decisions>` D-02 and D-10, and § "Claude's Discretion" (the milestone-diff-sweep discretion item and its real-positive-control requirement).
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md § "Pattern 3" and § "Open Questions" item 1 — the open question this task closes in writing.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-PATTERNS.md § "`61-SC4-INVARIANTS.md`" — the anchor block shape and the instruction that the anchor for this phase is the v0.9.0 tag, measured fresh rather than copied.
  </read_first>
  <action>
    Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md` with a
    `# Phase 61 — SC#4 Fence Invariants (RETAINED in full per D-11)` heading.

    Under `## Observation 1 of 2` record every probe below as its verbatim command followed by its
    verbatim output, opening the section with the output of `date -u +"%Y-%m-%dT%H:%M:%SZ"` so the
    separation from observation 2 is measurable rather than asserted:

    - the local tag probe, `git tag -l 'v0.9.1'`, expected to print nothing;
    - the remote tag probe. Do NOT run a bare `git ls-remote --tags origin 'v0.9.1'` and record its
      silence — a network failure produces the same silence. Run `git ls-remote --tags origin`
      unfiltered and record two counts from the single fetched listing: the count of lines matching
      the v0.9.0 tag reference at end-of-line, which must be 1 and is the **positive control** proving
      the remote was actually reached, and the count of lines mentioning the skipped version's tag
      reference, which must be 0;
    - the publish probe, `gh release list --limit 20`. Record the verbatim first five lines. The
      positive control is that the listing is non-empty and its first row carries the `Latest` marker
      against `v0.9.0` — the PRIOR milestone's release — proving the command reached GitHub; the
      negative assertion is that no row names the skipped version;
    - the release-workflow probe, `gh run list --workflow=release.yml --limit 5`, recorded verbatim,
      with an explicit statement that no run corresponds to a tag for the skipped version.

    State plainly under the observation: the fence holds at phase head. `v0.9.0`, a prior milestone,
    is still the latest published release; no tag for the skipped version exists locally or on the
    remote; nothing has been published.

    Under `## Milestone anchor (recorded, not swept)` record the fresh measurement of this milestone's
    own anchor — `git rev-parse v0.9.0^{commit}`, `git merge-base --is-ancestor v0.9.0 HEAD`,
    `git rev-list --count v0.9.0..HEAD`, and a `--stat` summary of `git diff v0.9.0..HEAD` restricted
    to exclude `.planning`. The anchor for this phase is the v0.9.0 tag, not the v0.8.0 tag Phase 57
    used; measure it here rather than copying either prior document's numbers.

    Then close RESEARCH's open question in writing, under the same heading. D-10's literal text names
    exactly four fence items — the tag probe, the no-publish probe, the `git diff` showing no
    unintended change under `typsphinx/`, and the `REQUIREMENTS.md` checksum — and does **not** name
    the three milestone invariants (no new runtime dependency, the four Typst Universe package versions
    in lockstep, no new configuration value) that Phases 52 and 57 swept to back a `### Verified`
    CHANGELOG section. Because this phase authors no `### Verified` section — the cheaper default taken
    under Claude's Discretion in plan 61-01 — the invariant sweep is deliberately NOT run here. Write
    that decision and its reasoning into the file so a later reader does not mistake the absence for an
    omission, and add the warning it replaces: whoever runs the v0.9.2 sweep must re-measure those
    invariants against their own anchor rather than copying this milestone's, because Phase 57's own
    milestone falsified the "no new configuration value" assertion by adding one.

    Finally, under `## Handoff to observation 2`, state that observation 2 and the phase-scoped
    `typsphinx/` diff are owned by plan 61-04, which runs two waves later so the two timestamps are
    genuinely separated, and name the file section 61-04 will append to.
  </action>
  <verify>
    <automated>test -f .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md &amp;&amp; [ -z "$(git tag -l 'v0.9.1')" ] &amp;&amp; [ "$(git ls-remote --tags origin | grep -c 'refs/tags/v0\.9\.0$')" = 1 ] &amp;&amp; [ "$(git ls-remote --tags origin | grep -c 'refs/tags/v0\.9\.1')" = 0 ] &amp;&amp; [ "$(gh release list --limit 20 | grep -c 'Latest')" = 1 ] &amp;&amp; [ "$(gh release list --limit 20 | grep -c 'v0\.9\.1')" = 0 ] &amp;&amp; grep -q 'Observation 1 of 2' .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md &amp;&amp; grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md &amp;&amp; grep -q 'Milestone anchor' .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md &amp;&amp; grep -q 'Handoff to observation 2' .planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `61-SC4-INVARIANTS.md` § "Observation 1 of 2" opens with a UTC timestamp in `YYYY-MM-DDTHH:MM:SSZ` form and records four probes, each as a verbatim command plus its verbatim output
    - the remote tag probe is recorded as an UNFILTERED `git ls-remote --tags origin` with two counts derived from it: 1 for the v0.9.0 tag reference (the positive control) and 0 for the skipped version's tag reference — a bare filtered probe whose silence could equally mean an unreachable network is not accepted
    - `gh release list --limit 20` output is recorded verbatim, its first row carries the `Latest` marker against `v0.9.0` (the positive control), and no row names the skipped version
    - `gh run list --workflow=release.yml --limit 5` output is recorded verbatim with an explicit statement that no run corresponds to a tag for the skipped version
    - `61-SC4-INVARIANTS.md` § "Milestone anchor (recorded, not swept)" holds a fresh measurement anchored at the **v0.9.0** tag — the commit SHA, the ancestry check, the revision count, and a `--stat` summary excluding `.planning`
    - that section states, in the file's own words, that the three milestone invariants are deliberately NOT swept, gives D-10's four-item literal reading as the reason, and carries forward the warning that a future sweep must re-measure rather than copy
    - `61-SC4-INVARIANTS.md` § "Handoff to observation 2" names plan 61-04 as the owner of observation 2 and of the phase-scoped `typsphinx/` diff, and names the section 61-04 appends to
    - `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md` is empty — this plan touches no product-tree file
  </acceptance_criteria>
  <done>The first of two separated fence observations is recorded with a positive control on every network-dependent probe, the milestone anchor is measured fresh against the v0.9.0 tag, and the decision not to run the milestone-invariant sweep is written down with its reasoning rather than left as a silent absence.</done>
  <reversibility rating="reversible">One new planning document recording read-only probes; no write action is taken against any remote.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Write the external-API coverage declaration</name>
  <files>.planning/phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md</files>
  <read_first>
    - .planning/phases/60-one-delimiter-aware-path-quoting-helper-routed-everywhere/COVERAGE.md — the precedent declaration, its reasoning structure, and the verbatim-detector-result convention.
    - .planning/milestones/v0.9.0-phases/57-v0-9-0-release-prep-prep-only/COVERAGE.md — the older sibling, written for a release-prep phase whose own plan prose was dense with `gh` and workflow language and which hit the seal-time false positive directly.
    - .planning/phases/61-v0-9-1-release-prep-prep-only/61-RESEARCH.md § "Standard Stack" — the tool census this declaration describes (`tox`, `uv`, `gh`, `scripts/extract_changelog_section.py`), none of which is an external API integration.
  </read_first>
  <action>
    Create `.planning/phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md` following the Phase 57 and
    Phase 60 precedent: a reasoned prose declaration, not a capability matrix.

    Record the plan-time detector result verbatim as the JSON object with `detected` false and an empty
    `signals` array, and state that it was produced by running the detector over this phase's scope
    before any plan existed.

    Explain what this phase actually does and why none of it is an external API integration: it edits
    one markdown file in the product tree, runs the project's own test, lint, type and documentation
    tooling, and writes planning documents under the phase directory. The `gh` CLI is used for
    read-only probes and one workflow dispatch against this project's own GitHub repository — the
    project's own CI, not a third-party service integration — and `scripts/extract_changelog_section.py`
    is named in the handoff as an inheritance record but is not invoked at all this phase.

    State plainly why this file exists: the seal-time coverage gate re-runs the same detector over the
    concatenated plan bodies, and this phase's plans are unavoidably dense with `gh`, workflow and
    release-pipeline prose — which is exactly the false-positive surface. Phase 57 hit this precise
    case, its detector going positive at seal time on a self-referential match inside its own
    declaration. This file exists so a false-positive detector run is met with a reasoned declaration
    rather than a fabricated capability matrix; a matrix for an API this phase does not touch would be
    worse than no matrix.
  </action>
  <verify>
    <automated>test -f .planning/phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md &amp;&amp; grep -q 'detected' .planning/phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md &amp;&amp; grep -q 'signals' .planning/phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md &amp;&amp; [ "$(wc -l &lt; .planning/phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md)" -ge 12 ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ CHANGELOG.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `COVERAGE.md` exists under the phase directory and is at least 12 lines long
    - it records the plan-time detector result verbatim as a JSON object whose `detected` field is false and whose `signals` array is empty
    - it names what this phase actually touches (one markdown file in the product tree, the project's own tooling, planning documents) and states why the `gh` usage is read-only probing plus one dispatch against this project's own CI rather than a third-party integration
    - it states that `scripts/extract_changelog_section.py` is named in the handoff but not invoked this phase
    - it explains the seal-time re-run false-positive risk and names the Phase 57 recurrence, and contains no capability matrix table
    - `git status --porcelain typsphinx/ tests/ CHANGELOG.md` is empty
  </acceptance_criteria>
  <done>`COVERAGE.md` records the real plan-time detector result and a reasoned declaration of why no capability matrix is supplied, in the shape Phases 57 and 60 established.</done>
  <reversibility rating="reversible">One new planning document.</reversibility>
</task>

</tasks>

<flagged_assumptions>
## Spec-less edge-coverage accounting (pointer)

The phase's single probe-surfaced edge item — REL-09, category `unclassified`, status **`unresolved`**
— is surfaced in full in `61-01-PLAN.md` § "Spec-less edge-coverage accounting", together with the
reason it is honest to leave unresolved: the probe ran against REL-09's literal PyPI-release text,
which D-01, D-02 and D-08 place out of scope for this phase and this milestone. It is restated here so
no plan in this phase drops it silently. Accounting for the phase: 1 surfaced = 0 authored into
`truths` + 1 flagged assumption + 0 dismissed + 0 dropped.

## Prohibition recall accounting (pointer)

The full two-stage recall/precision accounting and the five kept, descriptor-less prohibitions are in
`61-01-PLAN.md` § "Prohibition recall accounting". This plan carries P1 (no irreversible action),
P3 (REL-09 must not be recorded as satisfied) and P5 (no inherited, paraphrased or fabricated
evidence) in its own `must_haves.prohibitions`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local worktree → `origin` / GitHub API (read-only probes) | `git ls-remote`, `gh release list` and `gh run list` cross this boundary; all three are reads, and no write action is taken by this plan |
| recorded probe output → the phase's fence verdict | these transcripts are what SC#4 is judged on, so a vacuous, unreachable-source or fabricated probe is the failure mode this plan guards against |
| `.planning/REQUIREMENTS.md` → downstream close tooling | the requirement state this phase must NOT change, and which generic close tooling has changed anyway at five consecutive prior closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-05 | Repudiation | a fence probe passing vacuously because the network was unreachable, producing the same silence as a genuinely absent tag (ASVS V7 Logging, L1) | high | mitigate | Every network-dependent probe is paired with a positive control derived from the SAME fetched listing: the v0.9.0 tag reference must appear exactly once in the unfiltered `git ls-remote` output, and the `gh release list` output must carry a `Latest` marker. An unreachable source now fails the gate instead of passing it. |
| T-61-06 | Tampering | the `phase.complete` auto-flip writing REL-09 to a satisfied state against this phase's own recorded decision — observed at five consecutive prior release-prep closes (ASVS V4 Access Control, L1, by analogy: an automated actor changing state it was not authorised to change) | high | mitigate | A phase-head sha256, line count, timestamp and verbatim three-line quote recorded before any other plan runs; a close-time re-verification protocol; and a post-`phase.complete` detection-and-reversion protocol carrying the explicit `git checkout` reversion and the revert-never-accept rule, reproduced into `61-HANDOFF.md` so the operator reaches it. |
| T-61-07 | Repudiation | a fence observation recorded at effectively the same instant as its sibling, so "probed twice at separated times" is satisfied only nominally | medium | mitigate | Observation 1 is owned by this wave-1 plan and observation 2 by wave-3 plan 61-04, two waves and a full CI dispatch apart; both record a UTC wall-clock timestamp so the separation is measurable. |
| T-61-08 | Elevation of Privilege | an irreversible action — a real tag push or a `release.yml` trigger — slipping in under cover of the fence probes themselves, since the probe commands live one flag away from their writing counterparts | high | mitigate | Every command in this plan is read-only by construction (`git tag -l`, `git ls-remote`, `gh release list`, `gh run list`); the plan's `must_haves.prohibitions` names the fence explicitly; and an emptiness check over `git status --porcelain` across the product tree runs in both evidence tasks. |
| T-61-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. `61-RESEARCH.md` § "Package Legitimacy Audit" records the phase as introducing no external package. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `61-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide list. This plan creates
three of them: `.planning/phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md`,
`.planning/phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md` (observation 1 of 2 only —
plan 61-04 appends observation 2 and the scoped diff), and
`.planning/phases/61-v0-9-1-release-prep-prep-only/COVERAGE.md`. It modifies no product-tree file.
**No file named `61-VERIFICATION.md` is produced or referenced as an output.**
</artifacts_this_phase_produces>

<verification>
- `61-CLOSEOUT-GUARD.md` holds a phase-head sha256, line count, UTC timestamp and PHASE_BASE_SHA, the
  three verbatim REL-09-bearing lines, the five-consecutive-closes count, and both the close-time and
  post-`phase.complete` reversion protocols
- the recorded checksum and PHASE_BASE_SHA match the live measurements at verification time
- `.planning/REQUIREMENTS.md` is unmodified and REL-09 is still an unchecked box with a Pending
  Traceability row
- `61-SC4-INVARIANTS.md` holds observation 1 of 2 with a UTC timestamp and four probes, each with a
  positive control where the source is remote, plus a freshly measured v0.9.0 milestone anchor and the
  written decision not to run the milestone-invariant sweep
- `COVERAGE.md` records the verbatim plan-time detector result and a reasoned declaration with no
  fabricated capability matrix
- `git status --porcelain typsphinx/ tests/ CHANGELOG.md pyproject.toml README.md` is empty
- no file named `61-VERIFICATION.md` exists under the phase directory
</verification>

<success_criteria>
1. The `REQUIREMENTS.md` checksum guard is recorded at phase head, before any other plan has run, with
   the three REL-09-bearing lines quoted byte-for-byte and both a close-time and a post-`phase.complete`
   detection-and-reversion protocol written out — the checksum half of ROADMAP SC#4, RETAINED in full
   per D-11.
2. The first of two separated fence observations is recorded, every remote probe carrying a positive
   control so a negative result cannot come from an unreachable source.
3. The phase base SHA is recorded in an extractable form so plan 61-04's scoped `typsphinx/` diff has a
   real anchor.
4. The decision not to run the milestone-invariant sweep is written down with D-10's four-item literal
   reading as its reason, rather than left as a silent absence.
5. `COVERAGE.md` records the real plan-time detector result and explains why no capability matrix is
   supplied.
</success_criteria>

<output>
Create `.planning/phases/61-v0-9-1-release-prep-prep-only/61-02-SUMMARY.md` when done
</output>
