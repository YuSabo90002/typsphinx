---
phase: 55-v0-8-0-derived-defects
plan: 04
type: execute
wave: 3
depends_on: ["55-01", "55-02", "55-03"]
files_modified:
  - CHANGELOG.md
  - .planning/ROADMAP.md
  - .planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md
autonomous: true
requirements: [XREF-05, BLD-07, BLD-08, BLD-09, IMG-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-03: all five fixes are announced in the `Unreleased` section of `CHANGELOG.md` under a `Fixed` heading, authored in THIS phase rather than deferred to the release-prep phase, following the precedent Phase 54's plan `54-03` and Phase 54.1 both set — the release-prep phase curates rather than authors"
    - "D-03: none of the five is a breaking change — v0.9.0's two declared breaking axes stay exactly two (the template output location and the removed configuration values), and the `Changed` section gains no third entry"
    - "D-03 / D-01 / D-02: the XREF-05 entry notes in passing that a label name changes for an identifier that literally spells the encoder's own escape token, that the only such name in this repository is one test fixture's docname, and that PDF appearance is unchanged — only the label name in the emitted output and the link destination name in the PDF"
    - "IMG-03's user-visible consequence is announced too: the emitted filename for a relocated escaping image now carries a short digest prefix ahead of the basename, so two images that used to overwrite each other keep separate files"
    - "The phase closes on the UNCONDITIONAL zero-failure bar with all three CI-matching gates green, and that measurement is recorded in `55-04-EVIDENCE.md` from real runs rather than asserted — the stale seven-failure carve-out is neither cited nor re-introduced"
    - "D-05 is discharged across the phase: `55-04-EVIDENCE.md` carries a requirement-to-gate map naming, for each of the five, its evidence level, its RED-EVIDENCE file and its gate command, so a reviewer can see at a glance that the per-defect tiering was honoured and not flattened"
    - "Milestone binding constraint #11 holds at the phase boundary: zero new runtime dependencies, the `@preview` package count is still four with no fourth version-lockstep site, and no typing-import modernization was performed"
    - "Milestone binding constraint #9 holds: the milestone branch is confirmed present on `origin` by a real remote query recorded in the evidence file"
    - "If the plan `55-03` decision checkpoint resolved in favour of a predicate spelling that differs from the one ROADMAP SC#4 names literally, SC#4's wording is amended here so the roadmap and the shipped code agree — and if the owner declined the amendment, that declination is recorded instead"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "a `Fixed` subsection under `Unreleased` carrying one entry per defect, each naming the observable consequence and what a user does about it"
      contains: "### Fixed"
    - path: ".planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md"
      provides: "the phase-boundary green measurement, the three CI-matching gate results, the requirement-to-gate map, the milestone-branch remote check and the record of the plan `55-03` checkpoint resolution"
  key_links:
    - from: "CHANGELOG.md § Unreleased § Fixed"
      to: ".planning/REQUIREMENTS.md § v0.8.0-derived defects"
      via: "one changelog entry per requirement ID this phase closes, so the announcement set and the requirement set are the same size"
      pattern: "### Fixed"
    - from: ".planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md"
      to: "the four per-plan RED-EVIDENCE files"
      via: "the requirement-to-gate map cites each by path, so binding constraint #6's discharge is traceable from one place"
      pattern: "RED-EVIDENCE"
  prohibitions:
    - requirement_id: XREF-05
      category: integrity
      status: resolved
      verification: judgment
      statement: "MUST NOT transcribe a suite count, a lint result or a remote-branch result from memory or from a sibling plan's summary — every number in the evidence file must come from a command run in this task, in this worktree, after the merge"
    - requirement_id: XREF-05
      category: correctness
      status: resolved
      verification: judgment
      statement: "MUST NOT edit `.planning/REQUIREMENTS.md` checkbox state in this plan — the phase-completion step owns that transition, and it has flipped a requirement against a recorded decision four consecutive times, so leaving it untouched here keeps the later diff readable"
    - requirement_id: BLD-07
      category: correctness
      status: resolved
      verification: judgment
      statement: "MUST NOT add a third entry to the `Changed` section — none of this phase's five fixes is a breaking change, and v0.9.0's declared breaking axes stay at two"
    - requirement_id: BLD-09
      category: correctness
      status: resolved
      verification: judgment
      statement: "MUST NOT report the suite as green while citing the stale `tests/test_state_guard_shapes_gate.py` seven-failure carve-out — that carve-out was measured stale on 2026-08-16 and the bar for this phase is unconditional zero failures"
---

<objective>
Announce this phase's five fixes in the changelog and close the phase on a measured, unconditional
green bar.

Purpose: D-03 puts the announcement in THIS phase rather than in the release-prep phase, following the
precedent Phase 54's plan `54-03` and Phase 54.1 both set — release prep curates, it does not author.
Two of the five change something a user can observe in emitted output (a label name and a relocated
image's filename), so leaving them unannounced would make them silent.

Output: a `Fixed` subsection under `Unreleased`; a phase-boundary evidence file carrying the real
suite, lint, type, `@preview` and remote-branch measurements plus a requirement-to-gate map that shows
D-05's per-defect evidence tiering was honoured; and, conditionally, an amendment to ROADMAP SC#4's
wording if the plan `55-03` checkpoint resolved that way.

**This plan expands plans `55-01` through `55-03`** and adds no product code. It is the phase's
aggregation slice.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-v0-8-0-derived-defects/55-CONTEXT.md
@.planning/phases/55-v0-8-0-derived-defects/55-VALIDATION.md
@.planning/phases/55-v0-8-0-derived-defects/55-01-SUMMARY.md
@.planning/phases/55-v0-8-0-derived-defects/55-02-SUMMARY.md
@.planning/phases/55-v0-8-0-derived-defects/55-03-SUMMARY.md
@CLAUDE.md
</context>

## Execution environment (MANDATORY — read before any command)

Worktree-isolated execution is this project's STANDING mode. Before running anything in your
worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run EVERY
subsequent command through `uv run …`.

## Baseline: what "green" means in this phase

**UNCONDITIONAL ZERO FAILURES.** The "7 pre-existing `tests/test_state_guard_shapes_gate.py`
failures" carve-out recorded under `phases/53-template-registry-foundation/deferred-items.md` was
**measured stale on 2026-08-16** — that module now passes 18/18 and the Phase 54.1 tree closed at
1318 passed / 5 skipped / 0 failed. Do not re-introduce or cite it. If the suite is not at zero
failures after the merge, that is a real regression to report, not a baseline to absorb.

## Scope fence (do NOT touch)

- No product code. `typsphinx/` must be byte-identical to the merged wave-2 tree at the end of this
  plan.
- No `.planning/REQUIREMENTS.md` checkbox edits — the phase-completion step owns that transition and
  has mis-flipped a requirement four consecutive times; leaving the file untouched here keeps the
  later diff readable.
- No `### Changed` entry. None of the five is breaking.
- Editing `.planning/ROADMAP.md` is CONDITIONAL on the plan `55-03` checkpoint resolution and is
  scoped to SC#4's wording only. Use a scoped replacement; never rewrite the file.

<tasks>

<task type="auto">
  <name>Task 1: Author the Unreleased Fixed entries for all five defects</name>

  <read_first>
    - `CHANGELOG.md` — the whole `Unreleased` section, especially the two existing `Changed` entries,
      so the new entries match their voice: a bold one-sentence lead naming the requirement's
      observable consequence, then the reason in one clause, then what the user does about it and what
      happens if they do nothing.
    - `.planning/REQUIREMENTS.md` § "v0.8.0-derived defects" — the five requirement texts, verbatim, so
      each entry announces what the requirement actually promises.
    - `.planning/phases/55-v0-8-0-derived-defects/55-01-SUMMARY.md`,
      `55-02-SUMMARY.md` and `55-03-SUMMARY.md` — what actually shipped, including the plan `55-03`
      checkpoint resolution and the exact new label spelling and relocation-key shape.
    - `.planning/phases/54-one-bundle-rule-template-key-per-document-selection-four-del/54-03-PLAN.md`
      — the precedent for a mid-milestone phase authoring its own changelog entry.
  </read_first>

  <files>CHANGELOG.md, .planning/ROADMAP.md</files>

  <behavior>
    - `CHANGELOG.md` gains a `### Fixed` subsection inside `## [Unreleased]`, carrying exactly five
      entries, one per requirement ID this phase closes.
    - The `### Changed` section is unmodified and still carries exactly its two existing entries.
    - Each entry names the requirement ID in parentheses, matching the two existing entries' style.
    - The two entries with a user-observable output change say so explicitly.
  </behavior>

  <action>
    Insert a `### Fixed` subsection into `CHANGELOG.md`'s `## [Unreleased]` section, positioned after
    the existing `### Changed` block and before `### Planned for Future Releases`, following
    Keep-a-Changelog ordering. Write five entries, in requirement order, each in the voice the two
    existing `Changed` entries use — a bold lead sentence, the reason in one clause, then the
    consequence for a user who does nothing:

    - **XREF-05.** A cross-reference whose target document is absent from the master being compiled no
      longer resolves to a different document that happened to sanitize to the same label; it degrades
      to plain text, as an absent target always should. State in passing that the label name changes
      for an identifier that literally spells the sanitizer's own escape token, that the only such name
      in this repository is one test fixture's docname, and that PDF appearance is unchanged — what
      moves is the label name in the emitted output and the corresponding link destination name in the
      PDF. Cite D-03's own framing: this is not a breaking change.
    - **BLD-07.** A document name containing a number sign or a greater-than sign can no longer make
      two different include edges share one key, which previously let a guard that should have stayed
      dark fire and duplicate or substitute a document's content in the compiled output. Note that
      document names without either character produce byte-identical keys, so nothing changes for an
      ordinary project.
    - **BLD-08.** An include chain deeper than the module's own bound now stops the build with a named
      extension error identifying the depth and the offending chain, instead of escaping as a raw
      interpreter recursion failure with a Python traceback. Name the bound's value and say it is two
      orders of magnitude beyond any real documentation tree.
    - **BLD-09.** An absolute image URI written by a third-party extension in the driveless Windows
      shape now reaches the relocate-and-warn path on Python 3.13, where it was previously left
      untouched — which mattered because an untouched rooted URI reached the image copy step, whose
      platform-native destination join discards the output directory for a rooted path.
    - **IMG-03.** Two absolute image URIs in different directories that share a filename and both fall
      outside the doctree directory no longer collapse onto one relocated file, so neither image
      silently replaces the other. State the user-visible consequence explicitly: the relocated file's
      emitted name now carries a short prefix ahead of the original filename.

    Do not touch the `### Changed` block and do not add a third entry to it.

    **Conditional ROADMAP amendment.** Read the plan `55-03` checkpoint resolution recorded in
    `55-03-SUMMARY.md`. If the owner selected the predicate spelling that differs from the one
    ROADMAP SC#4 names literally AND confirmed the wording should be amended, apply a SCOPED
    replacement to `.planning/ROADMAP.md` § "Phase 55" SC#4 — never a whole-file rewrite — restating
    the predicate as what shipped, and appending one sentence recording that the original wording was
    measured not to produce the behaviour the same criterion requires, with a pointer to
    `55-03-RED-EVIDENCE.md` § "Predicate measurement". Also correct SC#4's stale
    `builder.py:910` citation to a textual reference to the gate inside the image-tracking method,
    since a line number in a success criterion is the exact defect that pitfall names. If the owner
    declined the amendment, or selected the literal spelling, leave `.planning/ROADMAP.md` untouched
    and say so in the summary.
  </action>

  <verify>
    <automated>uv run python -c "import re,sys; t=open('CHANGELOG.md').read(); u=t.split('## [Unreleased]',1)[1].split('## [',1)[0]; assert '### Fixed' in u, 'no Fixed section'; f=u.split('### Fixed',1)[1].split('###',1)[0]; missing=[r for r in ('XREF-05','BLD-07','BLD-08','BLD-09','IMG-03') if r not in f]; sys.exit('missing: '+str(missing) if missing else 0)" && echo ok</automated>
  </verify>

  <acceptance_criteria>
    - `uv run python -c "import sys; t=open('CHANGELOG.md').read(); u=t.split('## [Unreleased]',1)[1].split('## [',1)[0]; f=u.split('### Fixed',1)[1].split('###',1)[0]; missing=[r for r in ('XREF-05','BLD-07','BLD-08','BLD-09','IMG-03') if r not in f]; sys.exit('missing: '+str(missing) if missing else 0)" && echo ok` prints `ok` — all five requirement IDs appear inside the `Fixed` subsection specifically, not merely somewhere in the file.
    - `uv run python -c "import sys; t=open('CHANGELOG.md').read(); u=t.split('## [Unreleased]',1)[1].split('## [',1)[0]; c=u.split('### Changed',1)[1].split('###',1)[0]; n=c.count(chr(10)+'- '); sys.exit('Changed entry count is '+str(n) if n!=2 else 0)" && echo ok` prints `ok` — the `Changed` section still carries exactly its two breaking entries.
    - `git diff -- CHANGELOG.md | grep -cE '^-[^-]' ` returns `0` — the change is purely additive; no existing changelog line was removed or reworded.
    - `git diff --stat -- typsphinx/ tests/` produces no output — this plan adds no product or test code.
    - If the plan `55-03` checkpoint selected the non-literal predicate and the owner confirmed the amendment: `git diff -- .planning/ROADMAP.md | grep -c '910'` returns at least `1` (the stale citation was removed) and `git diff --stat -- .planning/ROADMAP.md` shows fewer than 40 changed lines (a scoped replacement, not a rewrite). Otherwise: `git diff --name-only -- .planning/ROADMAP.md` produces no output.
    - `git diff --name-only -- .planning/REQUIREMENTS.md` produces no output.
  </acceptance_criteria>

  <done>
    All five fixes are announced under `Unreleased` as `Fixed`, the two declared breaking axes are
    still two, and the roadmap either matches what shipped or records why it was left alone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Measure and record the phase-boundary green bar and the D-05 evidence map</name>

  <read_first>
    - `.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-05-EVIDENCE.md`
      — the phase-boundary evidence artifact shape to reproduce.
    - `.planning/phases/55-v0-8-0-derived-defects/55-VALIDATION.md` § "Per-Task Verification Map" —
      the requirement-to-gate contract this task reports against, and the § "Sampling Rate" list of
      gates a phase boundary owes.
    - `.planning/ROADMAP.md` lines 392-472 — the eleven binding constraints, specifically #2 (green at
      every phase boundary), #6 (RED recorded before implementation), #9 (milestone branch pushed) and
      #11 (zero new runtime deps, `@preview` count four, no typing modernization).
    - The three RED-EVIDENCE files this phase produced, so the map can cite each by path and level.
  </read_first>

  <files>.planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md</files>

  <behavior>
    - Every number in the evidence file comes from a command run in this task, after the wave-2 merge.
    - The requirement-to-gate map shows five rows, each naming its evidence level, its RED-EVIDENCE
      file and its gate command.
    - All three CI-matching gates are green and recorded.
    - The milestone branch is confirmed on the remote by a real query.
  </behavior>

  <action>
    Create `.planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md`, modelled on
    `54.1-05-EVIDENCE.md`. Open it with the 40-character SHA from `git rev-parse HEAD` in this
    worktree and the date. Then run each command below and paste its verbatim output under its own
    heading — no number in this file may be transcribed from a sibling summary or from memory:

    - **Full suite.** `uv run pytest -q` — record the final counts line in full. It must show zero
      failures and zero errors. If it does not, stop and report it as a regression; do not absorb it
      into a baseline and do not cite the stale carve-out.
    - **The three CI-matching gates.** `uv run black --check .`, `uv run ruff check .` and
      `uv run mypy typsphinx/`, each recorded with its exit status. `55-VALIDATION.md` § "Sampling
      Rate" carries this as a standing lesson: a pytest-only post-merge gate misses a CI-only defect
      class.
    - **`@preview` invariant.** `uv run pytest tests/test_preview_version_sync.py -q`, plus
      `grep -c '@preview' typsphinx/templates/base.typ` and the equivalent counts in
      `typsphinx/writer.py` and `typsphinx/template_engine.py`. Record that the package count is still
      four and that no fourth version-lockstep site was introduced.
    - **Zero new runtime dependencies.** `git diff --stat <merge-base> -- pyproject.toml` for this
      phase's range, recording that the dependency arrays are unchanged. Note in prose that the two
      symbols this phase reached for are Python standard library and an already-declared dependency's
      own module.
    - **No typing-import modernization.** `git diff <merge-base> -- typsphinx/ | grep -cE '^[-+].*from typing import'`
      recording the count, which must be zero.
    - **Milestone branch on the remote (binding constraint #9).**
      `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` — record the raw output. If it
      is empty, push the branch and re-run, recording both.

    Then write two tables.

    **Requirement-to-gate map**, one row per requirement, with columns: requirement ID; the D-05
    evidence level for that defect (real two-master compile for XREF-05, real compile for BLD-07, unit
    for BLD-08, unit for BLD-09, unit for IMG-03); the RED-EVIDENCE file that records its pre-fix
    transcript, by path; and the gate command that now passes. This table is how binding constraint #6
    is shown discharged for all five at once, and how a reviewer sees that the per-defect tiering was
    honoured rather than flattened to one uniform level.

    **Checkpoint resolution**, recording the plan `55-03` Task 2 decision: which option the owner
    selected, the predicate that shipped, and whether ROADMAP SC#4's wording was amended in Task 1 of
    this plan or deliberately left alone.

    Close the file with a short "Open after this phase" list naming exactly two items: the
    `_escapes_outdir()` normalization gap filed as a todo by plan `55-03`, and the residual
    truncated-digest collision probability recorded as a flagged assumption in that plan's
    `must_haves`. Both are recorded, neither is a blocker.
  </action>

  <verify>
    <automated>uv run pytest -q 2>&1 | tail -5 && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/ && uv run pytest tests/test_preview_version_sync.py -q && test -s .planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q 2>&1 | tail -3` shows `0 failed` and contains no `error` line.
    - `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all exit 0.
    - `uv run pytest tests/test_preview_version_sync.py -q` passes.
    - `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` produces a non-empty line.
    - `.planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md` exists, contains the 40-character SHA from `git rev-parse HEAD`, and contains all five requirement IDs.
    - `grep -c 'RED-EVIDENCE' .planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md` returns at least `3` — every per-plan evidence file is cited by path in the map.
    - `grep -c 'Checkpoint resolution' .planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md` returns `1`.
    - `git diff --stat -- typsphinx/ tests/` produces no output — this plan added no product or test code.
  </acceptance_criteria>

  <done>
    The phase closes on a measured, unconditional zero-failure bar with all three CI-matching gates
    green, the five requirements' evidence levels and RED transcripts are traceable from one file, the
    milestone branch is confirmed on the remote, and the two deliberately-open items are named.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped behaviour → published `CHANGELOG.md` | The only place two of this phase's output changes (a label name, a relocated image filename) are announced; an unannounced output change is a silent one. |
| measured gate results → `55-04-EVIDENCE.md` → phase completion | The evidence file is what a later reviewer, and the milestone audit, treat as the record that this phase closed green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-12 | Repudiation | an evidence file whose counts were transcribed from a sibling summary rather than measured after the merge, so the phase records a green bar it never observed | medium | mitigate | Every number is produced by a command run in Task 2 in this worktree after the wave-2 merge, pasted verbatim, and the prohibition list forbids transcription. |
| T-55-13 | Information Disclosure | a user-visible output change shipping unannounced, so a project whose build output changed has no way to learn why | medium | mitigate | Task 1 announces all five, and the two entries with an observable output change say so explicitly. |
| T-55-14 | Tampering | the phase-completion step flipping a requirement's checkbox against a recorded decision, as it has done four consecutive times | low | accept | Not this plan's transition to make; leaving `REQUIREMENTS.md` untouched here keeps the later diff readable so the flip is visible if it happens. |
| T-55-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed in this phase (binding constraint #11); `55-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. Task 2 records the dependency-array diff as evidence. |

**ASVS level 1, block on `high`.** No `high` item in this plan; it ships no product code. Canon
breadcrumb: generic supply-chain and audit-trail guidance is canon — covered by this register and
`/gsd-secure-phase`; not minted as a bespoke prohibition.
</threat_model>

## Artifacts this phase produces

Created by THIS plan (excluded from drift verification — none exists on the pre-fix tree):

- `CHANGELOG.md` — a new `### Fixed` subsection under `## [Unreleased]` with five entries
- `.planning/phases/55-v0-8-0-derived-defects/55-04-EVIDENCE.md` — new evidence artifact
- `.planning/ROADMAP.md` § Phase 55 SC#4 — conditionally amended wording

Created by sibling plans in this phase (also new, also excluded): `_LABEL_TOKEN_INTRODUCER_RE`,
`tests/test_sanitize_label_injectivity_unit.py`, `_decode_label`,
`test_label_collision_no_longer_links_to_decoy`, `_escape_include_edge_separators()`,
`_MAX_INCLUDE_CHAIN_DEPTH`, `tests/test_include_edge_separator_collision_gate.py`,
`TestMakeIncludeEdgeKeySeparatorInjectivity`, `TestDeriveMasterEdgeKeysDepthBound`,
`_is_absolute_image_uri()`,
`test_post_process_images_driveless_absolute_uri_reaches_rehome_branch`,
`test_post_process_images_unc_absolute_uri_reaches_rehome_branch`,
`test_post_process_images_relative_uri_is_not_treated_as_absolute`,
`test_post_process_images_escape_same_basename_keys_stay_distinct`,
`test_post_process_images_escape_key_is_pure_function_of_uri`,
`.planning/todos/pending/2026-08-16-escapes-outdir-isabs-not-backslash-normalized.md`,
`55-01-RED-EVIDENCE.md`, `55-02-RED-EVIDENCE.md`, `55-03-RED-EVIDENCE.md`.

<verification>
- `uv run pytest -q` — zero failures, zero errors, no carve-out cited.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` — exit 0.
- `uv run pytest tests/test_preview_version_sync.py -q` — passes; `@preview` count still four.
- `git ls-remote --heads origin gsd/v0.9.0-per-document-templates` — non-empty.
- `CHANGELOG.md`'s `Unreleased` section carries a `Fixed` subsection naming all five requirement IDs,
  and its `Changed` section still carries exactly two entries.
- `55-04-EVIDENCE.md` carries the requirement-to-gate map, the checkpoint resolution, and the
  two-item open list.
</verification>

<success_criteria>
- D-03 is satisfied: all five fixes are announced in `Unreleased` as `Fixed`, authored in this phase,
  with the XREF-05 label-name note and the IMG-03 filename note stated explicitly and no third
  breaking axis introduced.
- The phase closes on an unconditional zero-failure suite with all three CI-matching gates green, and
  every number in the evidence file was measured after the wave-2 merge.
- Binding constraints #2, #6, #9 and #11 are each shown discharged by a real command result recorded
  in `55-04-EVIDENCE.md`.
- D-05's per-defect evidence tiering is visible in one place, so no reviewer has to reconstruct it.
- The roadmap either matches what shipped or records why it was left alone; `REQUIREMENTS.md` is
  untouched.
</success_criteria>

<output>
Create `.planning/phases/55-v0-8-0-derived-defects/55-04-SUMMARY.md` when done.
</output>
