---
phase: 64-fhs-wrapper-and-command-shims-in-flake-nix
plan: 04
type: execute
wave: 3
depends_on: ["64-02", "64-03"]
files_modified:
  - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md
autonomous: true
requirements: [NIX-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Constraint 9: the gsd/v0.9.3-* branch census is re-measured immediately before the push. A decoy with zero unique commits is deleted with git branch -d; a decoy carrying commits halts the plan; the decoy is never pushed.'
    - 'SC#5: the canonical branch gsd/v0.9.3-toolchain-and-dependency-update-repair, and no other ref, is on origin with upstream tracking, at the SHA the local canonical carried once waves 1 and 2 had merged.'
    - 'SC#5: exactly one CI run is dispatched, with gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair; its headSha equals the pushed SHA, it was created after the push, and it reaches status completed.'
    - 'SC#5: every job conclusion is transcribed literally; Test Python 3.12 on windows-latest, Test Python 3.13 on windows-latest, Test Python 3.12 on macos-latest and Test Python 3.13 on macos-latest are named individually and conclude success.'
    - 'NIX-01 cross-check: the Lint and Format Check job ruff verdict, quoted from its Run lint with tox step, is set beside 64-02 local ruff check . output; both use the uv.lock ruff 0.15.20, so local and CI lint now agree.'
    - 'The run is recorded as the pre-revert baseline that Phase 65 TOX-04 compares against: tox-uv-bare is still pinned at this tip.'
    - 'The pushed tip is the phase final code-bearing tip: flake.nix is its only code change, and CI reads no file under .planning/, where this plan own evidence commit lands after the push.'
  artifacts:
    - path: .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md
      provides: 'branch census, push, dispatch, the completed run and its full job census'
      contains: 'windows-latest'
  key_links:
    - from: 'local canonical ref gsd/v0.9.3-toolchain-and-dependency-update-repair'
      to: 'origin/gsd/v0.9.3-toolchain-and-dependency-update-repair'
      via: 'git push -u origin gsd/v0.9.3-toolchain-and-dependency-update-repair (shared ref and shared config, pushed from the worktree)'
      pattern: 'origin/gsd/v0.9.3-toolchain-and-dependency-update-repair'
    - from: 'gh workflow run CI --ref <canonical>'
      to: 'ci.yml twelve jobs on ubuntu, windows and macos'
      via: 'workflow_dispatch, since the push and pull_request triggers are scoped to main and develop'
      pattern: 'workflow_dispatch'
  prohibitions:
    - statement: 'MUST NOT create a tag, open a pull request, trigger release.yml by any route, force-push, or push any ref other than the canonical branch.'
      status: resolved
      verification: test
    - statement: 'MUST NOT dispatch a second CI run, and MUST NOT edit any file to turn a red job green; a non-success job is transcribed and handed to the owner.'
      status: resolved
      verification: test
    - statement: 'MUST NOT move a ref of the main checkout (HEAD, a symbolic-ref, a branch pointer other than deleting a zero-unique-commit decoy) from this worktree.'
      status: resolved
      verification: test
    - statement: 'MUST NOT cite an earlier CI run, such as Phase 63 run 33309565005, as this phase baseline.'
      status: resolved
      verification: judgment
---

<objective>
Put the milestone branch on `origin` with upstream tracking, then observe one completed 3-OS CI run on
the phase's pushed tip, transcribing every job conclusion and naming the `windows-latest` and
`macos-latest` lanes individually.

Purpose: ROADMAP constraint 10 and SC#5. Phase 64's own deliverable, `flake.nix`, is invisible to CI.
But Phase 65's TOX-04 and Phase 69's PR both depend on the branch already being on `origin` with a
known-green baseline taken before the `tox-uv` revert. Constraint 9's decoy correction was done at
roadmap approval, but it must be re-measured here: the decoy re-appears after `commit` helper calls.

Requirement mapping: SC#5 carries no REQ-ID of its own. This plan lists NIX-01 because its
`Lint and Format Check` quote is the CI side of NIX-01's rationale ("local and CI lint would
diverge"): the local shim's `ruff check .` (plan 64-02) and CI's `ruff check .` now run the same
`uv.lock` version. This plan does not complete NIX-01; plan 64-02 does.

Output: `64-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-02-SUMMARY.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-03-SUMMARY.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<!-- planner-discipline-allow: gsd/v0.9.3-milestone -->

## Planning-time census (2026-09-11 — re-measure, never trust)

- The local branches matching `gsd/v0.9.3*`: only `gsd/v0.9.3-toolchain-and-dependency-update-repair`,
  at `96484915`, carrying HEAD. No `gsd/v0.9.3-milestone` exists.
- `git ls-remote --heads origin`: 9 heads, 0 matching `0.9.3`.
- `gh` is authenticated (RESEARCH § Environment Availability).
- A Phase 61/63 CI dispatch took about 7 minutes of wall clock.

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `64-CI-EVIDENCE.md` | evidence file (SC#5) | phase directory |
| upstream tracking `branch.gsd/v0.9.3-toolchain-and-dependency-update-repair.remote = origin` | shared git config written by `git push -u` | repository config |
| `origin/gsd/v0.9.3-toolchain-and-dependency-update-repair` | remote branch | GitHub |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — branch census, tip identity, lock check, push the canonical branch with tracking, dispatch CI exactly once</name>
  <precondition>`gh auth status` succeeds and `git ls-remote --heads origin` returns at least one line. This worktree's HEAD also carries `64-NIX05-WORKTREE-EVIDENCE.md`, `64-NIX07-RENAME-EVIDENCE.md` and `64-NIX08-ENV-EVIDENCE.md`, meaning waves 1 and 2 have merged. If not, halt and report rather than pushing a tip that lacks the phase's evidence.</precondition>
  <reversibility rating="reversible">A pushed branch can be deleted from origin, and a CI run publishes nothing; no tag, release or PR is created.</reversibility>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md</files>
  <read_first>
    - .github/workflows/ci.yml (the `name: CI`; the `push`/`pull_request` triggers scoped to `main`/`develop`; the twelve job names)
    - .planning/ROADMAP.md "Binding constraints" 9 and 10, and Phase 64 SC#5 with its RESOLVED constraint-9 note
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-CI-EVIDENCE.md (structure to mirror)
    - CLAUDE.md § "Worktree-isolated execution"
  </read_first>
  <action>
1. Run CLAUDE.md's standing provisioning line in this worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

2. Branch census. Constraint 9 requires re-measuring here, not trusting the RESOLVED note. Record:
   - `git branch --list 'gsd/v0.9.3*' -v`
   - `git rev-parse gsd/v0.9.3-toolchain-and-dependency-update-repair`
   - `git ls-remote --heads origin | grep -c '0\.9\.3'`, which should be 0 before the first push

   If `gsd/v0.9.3-milestone` exists, run `git merge-base --is-ancestor gsd/v0.9.3-milestone gsd/v0.9.3-toolchain-and-dependency-update-repair`:
   - Exit 0 means the decoy has zero unique commits. Delete it with `git branch -d gsd/v0.9.3-milestone`, which refuses to delete anything unmerged, and record that.
   - Non-zero means the decoy carries commits the canonical lacks. Record `git log --oneline gsd/v0.9.3-toolchain-and-dependency-update-repair..gsd/v0.9.3-milestone` and halt at a blocking checkpoint for the owner. Move no ref, and never re-point the main checkout's HEAD from a worktree.

   The decoy is never pushed.

3. Tip identity.
   - `git rev-parse HEAD` must equal `git rev-parse gsd/v0.9.3-toolchain-and-dependency-update-repair`: this worktree was cut from the canonical tip after waves 1 and 2 merged, and this plan has not committed yet. Record the value as `PUSHED_SHA`.
   - Record `git log --oneline -1`.
   - Record `grep -c typsphinx-fhs-run flake.nix`, which must be at least 1, showing the flake change is on this tip.

   If the SHAs differ, record both and halt.

4. Pre-dispatch lock check: `uv sync --extra dev --locked` must exit 0. Every `ci.yml` job begins with this step, so a stale lock would fail all twelve lanes at install, before any test, lint or type signal exists.

5. Push the canonical branch, and only it, with tracking: `git push -u origin gsd/v0.9.3-toolchain-and-dependency-update-repair`.
   - This pushes the shared ref from this worktree, and `-u` writes the branch's tracking into the repository's shared config.
   - No force, no tags, no other ref.
   - Record the push output, including GitHub's "Create a pull request" hint. Do not act on that hint.
   - Verify that `git rev-parse --abbrev-ref gsd/v0.9.3-toolchain-and-dependency-update-repair@{upstream}` prints `origin/gsd/v0.9.3-toolchain-and-dependency-update-repair`.
   - Verify that the first field of `git ls-remote --heads origin refs/heads/gsd/v0.9.3-toolchain-and-dependency-update-repair` equals `PUSHED_SHA`.

6. Dispatch exactly once: `gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair`. `ci.yml`'s push and pull_request triggers are scoped to main and develop, so the push alone runs nothing.
   - Find the run with `gh run list --workflow=ci.yml --branch gsd/v0.9.3-toolchain-and-dependency-update-repair --event workflow_dispatch --limit 1 --json databaseId,headSha,status,createdAt,url`.
   - Its `headSha` must equal `PUSHED_SHA`, and its `createdAt` must be later than the push.
   - Record the run id and URL.

7. Write `64-CI-EVIDENCE.md` with the sections `## Branch census`, `## Tip identity and lock check`, `## Push` and `## Dispatch`.
   - State that `flake.nix` is invisible to CI (constraint 10), so this run's value is the branch being on origin with a known pre-revert baseline.
   - State that this plan's own evidence commit lands on the worktree branch after the push. It is `.planning/`-only, which no CI job reads.

   Commit.
  </action>
  <verify>
    <automated>[ "$(git rev-parse --abbrev-ref 'gsd/v0.9.3-toolchain-and-dependency-update-repair@{upstream}')" = origin/gsd/v0.9.3-toolchain-and-dependency-update-repair ] &amp;&amp; L="$(git ls-remote --heads origin refs/heads/gsd/v0.9.3-toolchain-and-dependency-update-repair)" &amp;&amp; P="$(printf '%s\n' "$L" | cut -f1)" &amp;&amp; [ -n "$P" ] &amp;&amp; H="$(git ls-remote --heads origin)" &amp;&amp; [ "$(printf '%s\n' "$H" | grep -c 'gsd/v0.9.3-milestone')" = 0 ] &amp;&amp; RUNID="$(grep -oE '\b[0-9]{9,}\b' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md | head -1)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json workflowName -q .workflowName)" = CI ] &amp;&amp; [ "$(gh run view "$RUNID" --json event -q .event)" = workflow_dispatch ] &amp;&amp; grep -q 'Branch census' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md &amp;&amp; [ -z "$(git tag --points-at "$P")" ]</automated>
    <fails_when>non-zero exit; no upstream configured, or an upstream other than the canonical remote branch; the canonical branch absent on origin; a decoy branch present on origin; no run id in the evidence; the run's headSha differs from the pushed SHA; the run's workflow is not `CI` or its event is not `workflow_dispatch`; a tag pointing at the pushed tip</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records the branch census (local `gsd/v0.9.3*` refs with SHAs, and the pre-push origin count), and any decoy handling with its `merge-base --is-ancestor` result.
    - The evidence records `PUSHED_SHA`, equal on HEAD and on the canonical ref before the push, and the `uv sync --extra dev --locked` exit 0.
    - `git rev-parse --abbrev-ref gsd/v0.9.3-toolchain-and-dependency-update-repair@{upstream}` prints `origin/gsd/v0.9.3-toolchain-and-dependency-update-repair`, and origin's head SHA equals `PUSHED_SHA`.
    - Exactly one `gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair` command is recorded, with the resulting run id and URL, `headSha` equal to `PUSHED_SHA`, and `event` equal to `workflow_dispatch`.
    - No tag, PR or `release.yml` run is created.
  </acceptance_criteria>
  <done>The canonical milestone branch is on origin with tracking, and one CI run is dispatched against exactly the pushed tip.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the run to completion and transcribe the full job census — both windows-latest and both macos-latest lanes named individually, ruff's CI verdict quoted</name>
  <precondition>The run id recorded by Task 1 resolves with `gh run view <id> --json databaseId`. If the executor was resumed after returning mid-wait, it reuses that id, and no second dispatch exists: `gh run list --workflow=ci.yml --branch gsd/v0.9.3-toolchain-and-dependency-update-repair --event workflow_dispatch --json databaseId` lists exactly one run created during this phase.</precondition>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md (Task 1's sections and the run id)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md § NIX-01 — ruff (the local `ruff check .` output to set beside CI's)
    - .github/workflows/ci.yml lines 10-70 (matrix and job names; `Run lint with tox` at line 69)
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-CI-EVIDENCE.md § 12-job census, § ruff's verdict
  </read_first>
  <action>
1. Bounded wait. Run `gh run watch "$RUNID" --exit-status --interval 30` with `run_in_background` and wait for its completion notification. If a background wait is unavailable, run it in the foreground with the Bash timeout at 600000 ms, and repeat until `gh run view "$RUNID" --json status -q .status` prints `completed`.
   - The overall cap is 90 minutes. If it is reached, or control returns to the orchestrator before completion, record the run id and its current status in the evidence and return.
   - The orchestrator resumes this same agent once the run completes. Never dispatch a second run.

2. On completion, record:
   - `gh run view "$RUNID" --json status,conclusion,workflowName,headSha,url`
   - the job census, transcribed as a numbered table covering every row: `gh run view "$RUNID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'`

   The expected census, derived from `ci.yml`, is twelve jobs:
   - `test`: 3 OS × 2 Python = 6
   - `Lint and Format Check`
   - `Type Check`
   - `Code Coverage`
   - `Build Package`
   - `Integration Test - basic`
   - `Integration Test - advanced`

   Add two sub-tables naming individually `Test Python 3.12 on windows-latest` and `Test Python 3.13 on windows-latest`, then `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each with its conclusion.

3. ruff's CI verdict.
   - Read the `Lint and Format Check` job's conclusion.
   - Quote the `Run lint with tox` step's lines from `gh run view --job <lint job id> --log`: the `commands[0]> black --check .` line, the `commands[1]> ruff check .` line, and the verdict line that follows.
   - Set them beside 64-02's local `ruff check .` output. Both run the `uv.lock` ruff (0.15.20), so local and CI lint now agree (NIX-01's rationale). The lint step in `ci.yml` is named `Run lint with tox`. The differently named lint step belongs to `release.yml`, which this plan never searches for and never triggers.

4. Record `gh run list --workflow=release.yml --limit 5 --json headSha`: none of the entries equals `PUSHED_SHA`.

5. Label the run as the pre-revert baseline Phase 65's TOX-04 compares against: `tox-uv-bare` is still pinned at this tip.

6. If any job concludes other than `success`, transcribe it literally. Do not re-dispatch and do not edit anything. Return a blocking checkpoint naming the failing jobs: SC#5 is unmet until the owner decides.

7. Append `## Run`, `## Job census`, `## windows-latest lanes`, `## macos-latest lanes`, `## ruff's verdict`, `## No release-workflow run against this tip` and `## Dispatch count` (exactly one). Commit.
  </action>
  <verify>
    <automated>F=.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md &amp;&amp; RUNID="$(grep -oE '\b[0-9]{9,}\b' "$F" | head -1)" &amp;&amp; [ "$(gh run view "$RUNID" --json status -q .status)" = completed ] &amp;&amp; [ "$(gh run view "$RUNID" --json conclusion -q .conclusion)" = success ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '.jobs | length')" -ge 12 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.conclusion != "success")] | length')" = 0 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("windows-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("macos-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name == "Lint and Format Check" and .conclusion == "success")] | length')" = 1 ] &amp;&amp; for n in 'Test Python 3.12 on windows-latest' 'Test Python 3.13 on windows-latest' 'Test Python 3.12 on macos-latest' 'Test Python 3.13 on macos-latest' 'Lint and Format Check' 'Run lint with tox'; do grep -qF "$n" "$F" || { echo "missing: $n"; exit 1; }; done &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch gsd/v0.9.3-toolchain-and-dependency-update-repair --event workflow_dispatch --json databaseId -q 'length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 5 --json headSha -q '[.[] | select(.headSha == "'"$(gh run view "$RUNID" --json headSha -q .headSha)"'")] | length')" = 0 ] &amp;&amp; [ "$(grep -c 'Run linters' "$F")" = 0 ]</automated>
    <fails_when>non-zero exit; the run not `completed`, or its conclusion not `success`; fewer than 12 jobs, or any job not `success`; a windows-latest or macos-latest count other than 2; `Lint and Format Check` not success; `missing: <name>` printed for any lane, the lint job or its step; more than one dispatched CI run on the branch; a release-workflow run at this tip; the release-only lint step name in the evidence</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records the run's `status`, `conclusion`, `workflowName`, `headSha` and `url`, plus a numbered table of every job name and conclusion. The count equals the `ci.yml`-derived twelve.
    - The four lanes `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest` appear by name in their own sub-tables, each `success`.
    - The `Run lint with tox` step's `ruff check .` invocation and verdict are quoted verbatim and set beside 64-02's local `ruff check .` output.
    - Exactly one `workflow_dispatch` CI run exists on the branch, and no `release.yml` run carries the pushed SHA.
    - The run is labelled the pre-revert baseline for Phase 65's TOX-04.
  </acceptance_criteria>
  <done>A completed, all-green 3-OS CI run on the pushed canonical tip is transcribed job by job, with both Windows and both macOS lanes named. It stands as the baseline Phase 65 compares against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local refs → origin | a push publishes refs; a wrong ref (the decoy), a force or a tag would publish something this phase must not |
| gh CLI → GitHub Actions | a dispatch starts remote work; the wrong workflow (`release.yml`) would begin a publish pipeline |
| CI log → committed evidence | quoted job logs cross into the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-14 | Tampering | pushing the decoy branch, a force-push, or a tag | high | mitigate | census and `merge-base --is-ancestor` before the push; the only push command names the canonical ref; no force or tags; the gate checks origin carries no decoy and no tag points at the pushed tip |
| T-64-15 | Elevation of Privilege | triggering `release.yml` (publish pipeline) | high | mitigate | only `gh workflow run CI` is ever issued; the gate asserts no `release.yml` run carries the pushed SHA; prohibition forbids every route |
| T-64-16 | Repudiation | a green inherited from an earlier run, or a second dispatch masking a red first one | medium | mitigate | the run's `headSha` must equal the pushed SHA and `createdAt` must follow the push; exactly one dispatch on the branch is gated |
| T-64-17 | Information Disclosure | CI log excerpts in committed evidence | low | accept | the repository and its Actions logs are public; only the lint step's tool-output lines are quoted |
</threat_model>

<verification>
- The canonical branch is on origin with tracking at the pushed SHA; there is no decoy on origin and no tag.
- One `workflow_dispatch` CI run at that SHA completed with all twelve jobs green, and both Windows and both macOS lanes are named.
- ruff's CI verdict is quoted and set against the local shim's verdict.
</verification>

<success_criteria>
SC#5 is met: the milestone branch is on `origin`, and a CI run dispatched with `gh workflow run CI --ref <branch>` has completed with the `windows-latest` and `macos-latest` lanes named individually and green. It is recorded as Phase 65's pre-revert baseline.
</success_criteria>

<output>
Create `.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-04-SUMMARY.md` when done. Frontmatter declares `requirements-completed: []`: this plan cross-checks NIX-01, which plan 64-02 closes.
</output>
