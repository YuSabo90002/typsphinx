---
phase: 64-fhs-wrapper-and-command-shims-in-flake-nix
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md
autonomous: true
requirements: [NIX-01, NIX-02, NIX-03, NIX-04, NIX-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'D-09 / NIX-05: before provisioning, command -v ruff and command -v uv in the fresh worktree resolve to the shim store paths purely by inheritance from a session launched after 64-01 merged, and the evidence answers in one sentence, from direnv status taken inside the worktree, whether the shim PATH was reachable before any direnv allow for that worktree path.'
    - 'D-04: NIX-01 through NIX-04 are measured in a freshly created worktree nested under /home/yuta/Documents/typsphinx/.claude/worktrees/ (.venv and .tox absent at the head check), after the documented provisioning line, never in the main checkout.'
    - 'NIX-01: ruff --version prints exactly ruff 0.15.20, its xtrace exec line names this worktree own .venv/bin/ruff, and ruff check . runs to completion; black --check ., mypy typsphinx/ and the pytest shim each run to completion out of this worktree .venv.'
    - 'NIX-02: tox -e lint, tox -e type, tox -e py312 and tox -e py313 each end with an <env>: OK line in this worktree.'
    - 'NIX-03: tox -e cov, tox -e docs-html and tox -e docs-pdf each end with an <env>: OK line, and docs/_build/pdf/typsphinx.pdf is non-empty and begins with %PDF.'
    - 'NIX-04: one full-suite run through the pytest shim, under the maintainer own locale, yields 1543 passed and 5 skipped (the carried-in dev-extra baseline; 1548 collected at planning), with every skip itemised and zero environment-caused failures.'
    - 'NIX-05: the only provisioning command anywhere in the procedure is env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev, and no manual symlink step and no ELF-patching step appears anywhere.'
    - 'D-02 / D-06: the provisioning uv is the shim second leg (the nixpkgs uv inside the sandbox), and its -u unsets reach uv, proven by uv writing this worktree .venv and typsphinx.__file__ resolving inside this worktree.'
    - 'NIX-05 (adjacency edge): the worktree is nested inside the main checkout, and every recorded xtrace target and typsphinx.__file__ lies under this worktree pwd -P, never under /home/yuta/Documents/typsphinx/.venv or /home/yuta/Documents/typsphinx/typsphinx.'
    - 'NIX-05 (empty edge): if the shims are absent from the inherited PATH the precondition halts the plan before provisioning, so the NIX-05 run never provisions with a bare nix-store uv.'
    - 'NIX-05 (ordering edge): the evidence records the D-09 check, then provisioning, then the gates, in that order and with ascending UTC timestamps.'
    - 'NIX-02 (empty edge): .tox is absent before the first tox run, so every environment is provisioned cold inside the sandbox, py312 included, whose pytest header names Python 3.12.'
    - 'NIX-02 (adjacency edge): each environment runs in its own invocation, in the fixed order lint, type, py312, py313, and each has its own recorded OK line; no result is inferred from another environment shared .tox state.'
    - statement: 'NIX-02 (ordering edge): the outcome of NIX-02 does not depend on the order the four environments run in, because each is self-contained (extras = dev, uv-venv-lock-runner).'
      verification: backstop
    - 'NIX-03 (adjacency edge): docs-html and docs-pdf share docs/_build, so each build command begins with rm -rf docs/_build and neither build output or warning count can be the other.'
    - 'NIX-03 (empty edge): the PDF check is test -s plus the %PDF magic bytes, taken from a build started with docs/_build absent; exit 0 alone never counts.'
    - statement: 'NIX-03 (ordering edge): the docs-pdf PDF and its warning count are independent of whether docs-html ran before it.'
      verification: backstop
    - 'Every transcript in this plan evidence file is the genuine D-09 shape (the inherited session PATH), never nix develop and never direnv exec, and the file says so at its top.'
  artifacts:
    - path: .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md
      provides: 'NIX-05 procedure in one fresh nested worktree, with NIX-01, NIX-02, NIX-03 and NIX-04 transcripts'
      contains: 'genuine D-09 shape'
  key_links:
    - from: 'the Claude Code session PATH (frozen at launch, direnv-derived in the main checkout)'
      to: 'this worktree Bash calls'
      via: 'environment inheritance: no direnv hook fires in non-interactive Bash, so the launch PATH survives cd into the worktree'
      pattern: 'typsphinx-fhs-run'
    - from: 'the provisioning line'
      to: 'this worktree .venv'
      via: 'the uv shim second leg exec inside typsphinx-fhs-run, with the -u unsets preserved'
      pattern: 'uv sync --extra dev'
    - from: 'tox (inside the sandbox)'
      to: '.tox/<env>/bin/* and the uv-managed CPython 3.12'
      via: 'fork/exec namespace inheritance: no shim below the tox entrypoint'
      pattern: 'py312: OK'
  prohibitions:
    - statement: 'MUST NOT modify any tracked file other than this plan evidence file. flake.nix is included in that ban: a flake change needs another session relaunch, so it belongs to a Phase 64 gap-closure plan (/gsd-plan-phase 64 --gaps) that edits flake.nix inside this phase before NIX-03 can close (D-07: fixed inside Phase 64, not deferred).'
      status: resolved
      verification: test
    - statement: 'MUST NOT use a manual symlink, an ELF-patching tool, a system-wide loader shim or nixpkgs ruff package as a workaround anywhere in the NIX-05 procedure.'
      status: resolved
      verification: test
    - statement: 'MUST NOT run the full suite a second time under a different locale, and MUST NOT set any locale variable for the NIX-04 run (D-08).'
      status: resolved
      verification: test
    - statement: 'MUST NOT attribute a test failure away without its traceback, and MUST NOT adjust the 1543 / 5 baseline or the 3 / 5 docs-warning baselines to match a measurement.'
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record a main-checkout measurement, a nix develop run or a direnv exec run as this plan evidence.'
      status: resolved
      verification: judgment
---

<objective>
Run the documented worktree procedure end to end, in the genuine D-09 shape. In one freshly created
worktree nested inside the main checkout, first confirm the shims are on the inherited session PATH,
then run CLAUDE.md's single provisioning line, then every gate:
- the bare commands (NIX-01)
- the full suite (NIX-04)
- `tox -e lint/type/py312/py313` (NIX-02)
- `tox -e cov/docs-html/docs-pdf` with a real PDF (NIX-03)

No manual symlink and no ELF patch appears anywhere (NIX-05).

Purpose: SC#1, SC#2 and the second half of SC#4. D-04 fixes the locus: a main-tree run passes before
this phase changes anything, so only a fresh `uv sync` in a worktree can show the hazard is gone. D-09
fixes the reachability mechanism: session PATH inheritance, checked at the head of the procedure. This
plan's evidence is the observation NIX-05 demands, not a diagnostic.

**Shape measured, stated plainly (orchestrator note 3):** this plan runs only in a Claude Code session
launched *after* plan 64-01's `flake.nix` merged into the main checkout, from a shell where direnv had
loaded the new devShell. Its Bash calls inherit that PATH. The task precondition enforces this: in an
older session the precondition fails and the executor halts at a blocking-human gate, without
provisioning anything. Nothing here uses `nix develop --command` or `direnv exec`.

Output: `64-NIX05-WORKTREE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-01-SUMMARY.md
@CLAUDE.md
@tox.ini
</context>

## Carried-in baselines (re-measure; never inherit)

| Baseline | Value | Source |
|----------|-------|--------|
| Tests collected | **1548** | re-measured at planning, 2026-09-11 (`--collect-only`, main venv); `git diff --stat v0.9.2..HEAD -- tests/ typsphinx/` empty |
| Full suite, dev extra | **1543 passed, 5 skipped** | `63-GREEN-TREE-EVIDENCE.md` § SC#4; skips = 4 myst-parser docs-extra gaps + 1 env-gated corpus report |
| `docs-html` warnings | **3** | same file, clean build |
| `docs-pdf` warnings | **5** | same file, clean build |
| `ruff` pin | **0.15.20** | `uv.lock:1209-1210` |

## Flagged edge-probe assumptions (unclassified rows — surfaced, not silently dropped)

- **NIX-01 (unclassified).** Exact-version equality is checked as a string match against `uv.lock`'s
  `version =` line. The main checkout's older `.venv/bin/ruff` also prints `ruff 0.15.20`, so the
  version alone cannot tell the worktree's binary from the main tree's. Only the recorded xtrace
  `+ exec` path distinguishes them. This plan therefore gates on that path, not on the version.
- **NIX-04 (unclassified).** "Environment-caused" is attributed by comparison with the carried-in
  baseline and its skip reasons. A failure absent from that baseline counts as environment-caused
  unless its traceback names product behaviour. That is a judgment, and the evidence must show the
  traceback it rests on.

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `64-NIX05-WORKTREE-EVIDENCE.md` | evidence file (NIX-01..NIX-05) | phase directory |

No code symbol is created by this plan.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — D-09 head check, the one provisioning line, then ruff check . through the inherited-PATH shim in a fresh nested worktree</name>
  <precondition>`command -v ruff` and `command -v uv` print `/nix/store/<hash>-ruff/bin/ruff` and `/nix/store/<hash>-uv/bin/uv`, and both files contain the string `typsphinx-fhs-run`. That means this session was launched after 64-01's `flake.nix` merged. If not, halt at a blocking-human gate: the maintainer opens a terminal in /home/yuta/Documents/typsphinx so direnv re-evaluates the flake, confirms `command -v ruff`, relaunches Claude Code from that shell, and re-runs /gsd-execute-phase 64.</precondition>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md</files>
  <read_first>
    - CLAUDE.md § "Worktree-isolated execution" (the provisioning line verbatim; the editable-install hazard)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-02, D-04, D-06, D-09)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-01-SUMMARY.md (the relaunch note; the shim names and message contract)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md (the diagnostic transcripts this plan re-takes in the genuine shape)
    - flake.nix (read-only: `venvWalk`, `uvShim`, the D-03 message)
    - uv.lock lines 1209-1210
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-GREEN-TREE-EVIDENCE.md (evidence format; tree-identity proof)
  </read_first>
  <action>
1. D-09 head check. Run it before anything else, and before provisioning (NIX-05 ordering edge). Record:
   - `date -u +%FT%TZ`
   - `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/` (the nested location)
   - `test -f .git`
   - `test ! -e .venv` and `test ! -e .tox` (freshly created; nothing provisioned yet)
   - `command -v ruff` and `command -v uv`
   - `grep -c typsphinx-fhs-run "$(command -v ruff)"`
   - `echo "$DIRENV_DIR"` and `echo "$IN_NIX_SHELL"`
   - `direnv status 2>&1 | grep -E 'RC'`, taken from inside the worktree

   The `Found RC` lines show whether direnv has ever allowed THIS worktree's `.envrc`. Together with the `command -v` results, they answer NIX-05's open question by observation. Write the answer as one sentence, whichever way it falls: the shim PATH is, or is not, reachable here before any direnv allow for this path (D-09, ROADMAP constraint 5b).

   Do not run a per-worktree direnv allow, and do not wrap anything in `nix develop`: D-09 rejects both.

2. Provision exactly as CLAUDE.md documents: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.
   - The `uv` here is the shim. Because `.venv/bin/uv` does not exist before Phase 65, D-02's second leg runs, and the nixpkgs `uv` executes inside the sandbox. Record `bash -x "$(command -v uv)" --version 2>&1 | grep '^+ exec'`.
   - Record tree identity: `uv run python -c 'import typsphinx,os;print(os.path.realpath(typsphinx.__file__))'` must print `$(pwd -P)/typsphinx/__init__.py`. That proves the editable install binds this worktree. It also proves D-06: the `-u` unsets reached `uv` through the sandbox; otherwise an inherited `VIRTUAL_ENV` could have redirected the sync.
   - No manual symlink and no ELF-patching step happens anywhere (NIX-05).

3. NIX-01, `ruff`, with no wrapper.
   - `ruff --version` must print exactly `ruff 0.15.20`, compared against `uv.lock` read at run time.
   - `bash -x "$(command -v ruff)" --version 2>&1 | grep '^+ exec'` must name `$(pwd)/.venv/bin/ruff` inside this worktree (orchestrator note 1). The main checkout's `.venv/bin/ruff` also reports 0.15.20, so the version alone cannot distinguish the two.
   - `ruff check .` must run to completion. Record its full final output and exit code. A lint finding is a real ruff verdict and is recorded as one, not as an environment failure.
   - Outside-sandbox control: `.venv/bin/ruff --version` still fails with the stub-ld rejection at rc 127, in this same worktree. The file is unchanged; the shim is what makes it run.

4. Write `64-NIX05-WORKTREE-EVIDENCE.md`.
   - It opens with the line: `Measured shape: genuine D-09 shape — inherited session PATH, no nix develop wrapper, no direnv exec (orchestrator note 3).`
   - Then the sections `## D-09 head check (session launched after 64-01 merged)`, `## Provisioning and tree identity` and `## NIX-01 — ruff`.
   - Use the `$ <command>` + verbatim output + interpretive prose convention, and keep all paths verbatim.

   Commit.
  </action>
  <verify>
    <automated>test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v ruff)" &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; ruff --version | grep -Fx 'ruff 0.15.20' &amp;&amp; bash -x "$(command -v ruff)" --version 2&gt;&amp;1 | grep -F "typsphinx-fhs-run $(pwd)/.venv/bin/ruff --version" &amp;&amp; ruff check . &amp;&amp; [ "$(uv run python -c 'import typsphinx,os;print(os.path.realpath(typsphinx.__file__))')" = "$(pwd -P)/typsphinx/__init__.py" ] &amp;&amp; grep -q 'genuine D-09 shape' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md &amp;&amp; grep -q 'RC' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md &amp;&amp; [ -z "$(git status --porcelain -- flake.nix flake.lock typsphinx tests scripts .github CLAUDE.md tox.ini pyproject.toml uv.lock)" ]</automated>
    <fails_when>non-zero exit; `pwd -P` outside `/home/yuta/Documents/typsphinx/.claude/worktrees/`; either shim lacks `typsphinx-fhs-run` (stale session PATH); a version line other than `ruff 0.15.20`; the xtrace line absent or naming a path outside this worktree; `ruff check .` exiting non-zero; `typsphinx.__file__` resolving outside this worktree</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records the head-check block with ascending UTC timestamps, BEFORE the provisioning transcript. It shows `.venv` and `.tox` absent at the head check, `command -v ruff` and `command -v uv` as `/nix/store/…` shim paths, and the `direnv status` RC lines.
    - One sentence in the evidence answers whether the shim PATH was reachable in this worktree before any direnv allow for its path.
    - The provisioning command transcribed is exactly `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, and the uv xtrace `+ exec` line names a `/nix/store/…-uv-…/bin/uv` path.
    - `typsphinx.__file__` resolves to `<pwd -P>/typsphinx/__init__.py`.
    - `ruff --version` prints `ruff 0.15.20`; its xtrace line names `<this worktree>/.venv/bin/ruff`; `ruff check .` exits 0 with its output recorded; the rc-127 outside-sandbox control is recorded.
  </acceptance_criteria>
  <done>A fresh nested worktree reaches the shims through the inherited session PATH, provisions with the one documented line, and runs `ruff check .` on the `uv.lock` ruff, all recorded in the genuine D-09 shape.</done>
</task>

<task type="auto">
  <name>Task 2: NIX-01's other bare commands and NIX-04's single full-suite run through the pytest shim</name>
  <precondition>The same session check as Task 1: `command -v pytest` prints a `/nix/store/<hash>-pytest/bin/pytest` file containing `typsphinx-fhs-run`, and this worktree's `.venv` exists from Task 1's provisioning.</precondition>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md (Task 1's sections)
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-GREEN-TREE-EVIDENCE.md § SC#4 — full pytest suite (the 1543 / 5 baseline and its five itemised skips)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-08: one full-suite run, no second locale)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md § Assumptions Log A4 (subprocess bare-name resolution inside the sandbox)
  </read_first>
  <action>
1. NIX-01's remaining bare commands, each through its shim with no wrapper:
   - `black --check .`, `mypy typsphinx/` and `sphinx-build --version`: record each exit code and its tail.
   - `bash -x "$(command -v <t>)" --version 2>&1 | grep '^+ exec'` for `black`, `mypy`, `pytest` and `sphinx-build`: each line must name `$(pwd)/.venv/bin/<t>` inside this worktree.

2. NIX-04: run the full suite ONCE, through the bare `pytest` shim, under the maintainer's own locale.
   - Set no locale variable. D-08 forbids a second full run under another locale, and the locale class is plan 64-03's targeted probe.
   - Command: `pytest -q -rs` with the Bash timeout at 600000 ms. Its summary line closes both SC#1's "pytest runs to completion" and NIX-04.
   - Compare against the carried-in baseline: 1548 collected, 1543 passed, 5 skipped under the dev extra.
   - Itemise every skip with its reason. Four are expected to be the myst-parser docs-extra gap and one the env-gated corpus report; a sixth skip is a finding, not noise.
   - For any failure, record the node id and the first traceback frame, then attribute it: environment-caused (loader, locale, path, a binary missing inside the sandbox) or not (product behaviour). The gate is zero environment-caused failures.
   - Never adjust the baseline to match.

3. Record `uv run pytest tests/test_extension.py -q`. This is CLAUDE.md's worktree idiom through the uv shim's second leg, and it must exit 0.

4. Append `## NIX-01 — black, mypy, pytest, sphinx-build` and `## NIX-04 — full suite (one run, maintainer locale)`, then commit.
  </action>
  <verify>
    <automated>black --check . &amp;&amp; mypy typsphinx/ &amp;&amp; for t in black mypy pytest sphinx-build; do bash -x "$(command -v $t)" --version 2&gt;&amp;1 | grep -qF "typsphinx-fhs-run $(pwd)/.venv/bin/$t --version" || exit 1; done &amp;&amp; OUT="$(mktemp)" &amp;&amp; pytest -q &gt; "$OUT" 2&gt;&amp;1 &amp;&amp; grep -Eq '1543 passed, 5 skipped' "$OUT" &amp;&amp; [ "$(grep -cE '[0-9]+ failed' "$OUT")" = 0 ] &amp;&amp; uv run pytest tests/test_extension.py -q &amp;&amp; grep -q '1543 passed, 5 skipped' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md &amp;&amp; grep -q 'NIX-04' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md</automated>
    <fails_when>non-zero exit from `black --check .`, `mypy typsphinx/`, the pytest run or the `uv run` idiom; an xtrace line absent or naming a path outside this worktree; the pytest summary not reading `1543 passed, 5 skipped`, or containing any `failed` count</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records `black --check .` and `mypy typsphinx/` exiting 0, `sphinx-build --version` printing the `uv.lock` Sphinx version, and four xtrace `+ exec` lines, each naming `<this worktree>/.venv/bin/<tool>`.
    - The evidence records exactly one full-suite run (`pytest -q -rs`) and its summary line, 1543 passed and 5 skipped. Each of the five skips appears with its node id or file:line and its reason verbatim.
    - No locale variable is set in any command of this task.
    - `uv run pytest tests/test_extension.py -q` exits 0.
  </acceptance_criteria>
  <done>Every documented bare command runs to completion out of this worktree's `.venv`, and one full-suite run through the shim matches the carried-in baseline with no environment-caused failure.</done>
</task>

<task type="auto">
  <name>Task 3: NIX-02 and NIX-03 — seven tox environments provisioned cold inside the sandbox, docs-pdf producing a real PDF, then the NIX-05 procedure summary</name>
  <precondition>The same session check as Task 1: `command -v tox` prints a `/nix/store/<hash>-tox/bin/tox` file containing `typsphinx-fhs-run`, and `test ! -e .tox` still holds in this worktree before the first tox run.</precondition>
  <files>.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md</files>
  <read_first>
    - tox.ini (every environment; `changedir = docs` at lines 65, 73, 81; `requires = tox-uv-bare~=1.35` — read-only)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-07, § Folded Todos)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md § Assumptions Log A3, § Open Question 1 (cold provisioning timing is unmeasured)
    - .planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md (folded source todo; its py312 section)
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-GREEN-TREE-EVIDENCE.md § docs builds (the clean-build discipline and the 3 / 5 counts)
  </read_first>
  <action>
1. NIX-02. Record `test ! -e .tox` first: every environment is provisioned cold inside the sandbox (the NIX-02 empty edge).
   - Run each environment in its own Bash call with the timeout at 600000 ms. If a call would exceed ten minutes, use `run_in_background` and wait for its completion notification. Cold `py312` includes downloading a uv-managed CPython 3.12 (RESEARCH A3).
   - Use this fixed order and record each environment separately: `tox -e lint`, `tox -e type`, `tox -e py312`, `tox -e py313`. Each must end with its `<env>: OK` line and `congratulations :)`; record the tail and the exit code.
   - For `py312`, also record the pytest session header line naming `Python 3.12`, and an outside-sandbox control: `.tox/py312/bin/python --version` run bare, with its exit code, which is expected to be the stub-ld rejection because this is a generic-linux uv-managed interpreter.
   - State that the folded todo's acceptance wording ("not an auto-downloaded generic-linux build") is superseded by CONTEXT § Folded Todos: the todo is closed by this phase's mechanism, and the downloaded build runs inside the sandbox. Do not move the todo file; execute-phase moves `resolves_phase` todos itself.

2. NIX-03, in this fixed order:
   - `tox -e cov`: record its `TOTAL` line and pytest summary.
   - `rm -rf docs/_build` followed by `tox -e docs-html`.
   - `rm -rf docs/_build` followed by `tox -e docs-pdf`.

   The `rm -rf` belongs to each build command, not to a preamble: the two builds share `docs/_build` (the NIX-03 adjacency edge).

   For docs-html, record the `build succeeded` line and its warning count against the baseline of 3. For docs-pdf, record the `Generated PDF:` line and the `build succeeded` line with its warning count against the baseline of 5, then `test -s docs/_build/pdf/typsphinx.pdf`, `head -c 4 docs/_build/pdf/typsphinx.pdf` (must be `%PDF`) and `stat -c %s` in bytes.

   A warning count that differs from 3 or 5 is not silently accepted: record the differing warning lines verbatim and attribute them.

   D-07:
   - Record `ls "$HOME/.cache/typst/packages/preview" | wc -l` before and after.
   - If docs-pdf cannot reach its `@preview` packages, do not edit `flake.nix` here. 64-01 owns it, and a flake change would force another session relaunch. Record the failing transcript and halt with a blocking checkpoint. The fix still lands inside Phase 64, not later (D-07): the checkpoint hands it to a Phase 64 gap-closure plan (`/gsd-plan-phase 64 --gaps`) that edits `flake.nix` in this phase, and NIX-03 stays open until `tox -e docs-pdf` is re-run green in a session relaunched after that plan merges.

3. Append the sections below, then commit:
   - `## NIX-02 — tox lint / type / py312 / py313`
   - `## NIX-03 — tox cov / docs-html / docs-pdf`
   - `## NIX-05 — procedure summary`: one paragraph, naming the fresh nested worktree, the D-09 check first, the one provisioning line, then every gate, with no manual symlink or ELF-patch step anywhere and every tool target resolved inside this worktree.
  </action>
  <verify>
    <automated>tox -e lint &amp;&amp; rm -rf docs/_build &amp;&amp; tox -e docs-pdf &amp;&amp; test -s docs/_build/pdf/typsphinx.pdf &amp;&amp; [ "$(head -c 4 docs/_build/pdf/typsphinx.pdf)" = '%PDF' ] &amp;&amp; test -d .tox/py312 &amp;&amp; test -d .tox/py313 &amp;&amp; test -d .tox/cov &amp;&amp; for e in lint type py312 py313 cov docs-html docs-pdf; do grep -q "$e: OK" .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md || { echo "missing: $e"; exit 1; }; done &amp;&amp; grep -q 'Python 3.12' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md &amp;&amp; [ "$(grep -c 'rm -rf docs/_build' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md)" -ge 2 ] &amp;&amp; grep -q '%PDF' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md &amp;&amp; grep -q 'NIX-05 — procedure summary' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md &amp;&amp; [ -z "$(git status --porcelain -- flake.nix flake.lock typsphinx tests scripts .github CLAUDE.md tox.ini pyproject.toml uv.lock)" ]</automated>
    <fails_when>non-zero exit from the re-run of `tox -e lint` or `tox -e docs-pdf`; the PDF missing, empty, or not beginning with `%PDF`; `missing: <env>` printed for any of the seven environments; no `Python 3.12` header recorded; fewer than two clean-build commands recorded; a scope-fenced path modified</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records `test ! -e .tox` before the first tox run, then seven separately recorded environments in the stated order, each ending with its `<env>: OK` line.
    - `py312`'s pytest header names Python 3.12, and its bare-interpreter control is recorded with its exit code.
    - Each docs build command in the evidence begins with `rm -rf docs/_build`, and both warning counts are compared against 3 and 5 respectively.
    - `docs/_build/pdf/typsphinx.pdf` is non-empty, its first four bytes are `%PDF`, and its size in bytes is recorded.
    - `## NIX-05 — procedure summary` names the fresh nested worktree, the head check, the single provisioning line and the gate list, and states that no manual symlink or ELF-patch step occurred.
  </acceptance_criteria>
  <done>All seven tox environments run to completion, provisioned cold inside the sandbox, with `docs-pdf` producing a real PDF. The NIX-05 procedure is recorded end to end in one fresh worktree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session PATH → worktree | the shims reach the worktree only by inheritance from the launching session; a stale session silently provides no shim |
| worktree → main checkout | the worktree is nested inside the main checkout, whose `.venv` holds a runnable 0.15.20 `ruff` and a PEP-660 finder bound to the main tree |
| evidence → verifier | this file is the NIX-01..05 authority; a transcript from the wrong tree or the wrong shape would certify nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-06 | Repudiation | a `nix develop` or `direnv exec` run passed off as the D-09 observation | medium | mitigate | every task carries a precondition that fails in a session predating the merge; the evidence opens with the measured-shape line; no task command uses either wrapper |
| T-64-07 | Tampering | measuring the main checkout instead of this worktree (editable-install hazard, nested location) | medium | mitigate | `typsphinx.__file__` identity gate plus an xtrace `+ exec` path gate for every shimmed tool, each required to lie under `pwd -P` |
| T-64-08 | Repudiation | environment-caused failures attributed away, or a baseline moved to match | medium | mitigate | the 1543 / 5 and 3 / 5 baselines are gated literally; every skip and failure is itemised with its reason or traceback frame |
| T-64-09 | Denial of Service | a cold tox provisioning exceeding one Bash call | low | mitigate | one environment per Bash call at a 600000 ms timeout, with `run_in_background` for any call that would exceed it |
</threat_model>

<verification>
- The head check, then provisioning, then the gates, in that order, in one fresh nested worktree, in the genuine D-09 shape.
- `ruff 0.15.20` through the shim; every tool target resolves inside this worktree.
- The full suite reads 1543 passed and 5 skipped; seven tox environments are OK; the PDF begins with `%PDF`.
- `git status --porcelain` over the scope-fenced paths is empty.
</verification>

<success_criteria>
SC#1 and SC#2 are observed, and the second half of SC#4: the maintainer's documented commands and every tox environment run to completion on this machine through the shims. An executor in a freshly created worktree needs only the one documented provisioning line. Whether the shim PATH reaches that worktree before any direnv allow is answered by observation.
</success_criteria>

<output>
Create `.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-02-SUMMARY.md` when done. Frontmatter declares `requirements-completed: [NIX-01, NIX-02, NIX-03, NIX-04, NIX-05]`.
</output>
