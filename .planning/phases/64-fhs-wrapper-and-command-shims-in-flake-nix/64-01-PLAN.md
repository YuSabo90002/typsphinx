---
phase: 64-fhs-wrapper-and-command-shims-in-flake-nix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flake.nix
  - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md
autonomous: true
requirements: [NIX-01, NIX-06, NIX-07]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'D-01: the Linux devShell carries exactly seven shim command names (uv, tox, ruff, black, mypy, pytest, sphinx-build) and the x86_64-linux package census reads nodejs-24.16.0, pnpm-11.9.0, git-2.54.0, python3-3.13.13, uv, tox, ruff, black, mypy, pytest, sphinx-build.'
    - 'D-02: the uv shim resolves in two legs, the bounded upward walk for .venv/bin/uv and then the fixed store path of nixpkgs uv, and both legs exec through typsphinx-fhs-run; with no .venv/bin/uv present its xtrace exec line names a /nix/store uv path.'
    - 'D-03: the six other shims have no fallback. A walk that stops without finding .venv/bin/<tool> prints one stderr line beginning typsphinx-shim: <tool>: that names the start directory, and exits 127.'
    - 'D-06: the FHS passthrough runScript is exec "$@" and nothing else; neither it nor any shim clears, normalizes or sets an environment variable, and flake.nix sets no locale variable anywhere (D-08).'
    - 'Orchestrator note 1: the upward walk stops at the first ancestor holding a .git entry (a file in a worktree, a directory in the main checkout), so a shim run inside a nested executor worktree can never resolve the main checkout .venv, while a shim run from docs/ still climbs to the checkout root (tox changedir = docs).'
    - 'D-04: RED is observed before the edit, in this plan freshly provisioned worktree: .venv/bin/ruff --version fails with the stub-ld rejection at rc 127, or its non-reproduction is recorded verbatim and flagged, never invented.'
    - 'Tracer: through the edited devShell (DIAGNOSTIC nix develop, from the worktree root and from docs/) ruff --version prints exactly ruff 0.15.20 and the xtrace exec line names this worktree absolute .venv/bin/ruff.'
    - 'tox subprocess tree runs inside the sandbox: tox -e lint completes through the tox shim, including tox-uv-bare resolving uv from PATH and re-entering the sandbox through the uv shim; tox -e docs-pdf produces a non-empty file beginning with %PDF, so D-07 is settled inside this plan.'
    - 'NIX-06: nix flake show --all-systems and nix flake check --all-systems --no-build both exit 0 on the Linux evaluator after the edit.'
    - 'NIX-06 (empty edge): both darwin devShell drvPaths are byte-identical before and after the edit (x86_64-darwin fclfls55m9lp06679x7zrw0qzjya834j-nix-shell.drv, aarch64-darwin 2m6y6pshri0vyw3jb5agczjnz9a92sxc-nix-shell.drv at the unchanged flake.lock), so on darwin the Linux-only additions are the empty list and the package set is unchanged.'
    - 'NIX-06 (adjacency edge): the Linux devShell has exactly one uv provider, the shim, and no nixpkgs uv-<version> entry; both darwin devShells keep uv-0.11.25.'
    - 'NIX-06 (ordering edge): inside the Linux devShell the first uv on PATH (type -P uv) is the shim /nix/store/<hash>-uv/bin/uv, and the darwin package order stays nodejs, pnpm, git, python3, uv.'
    - 'Every shim run in this plan is labelled DIAGNOSTIC: nix develop --command is not the D-09 session-inheritance shape, which only a session launched after this plan merges can observe (orchestrator note 3).'
    - 'flake.nix is the only tracked source file modified and flake.lock is unchanged, so no flake input is added.'
  artifacts:
    - path: flake.nix
      provides: 'Linux-guarded buildFHSEnv passthrough plus seven writeShellScriptBin command shims on the mkShell devShell'
      contains: 'typsphinx-fhs-run'
    - path: .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md
      provides: 'RED, tracer GREEN, roster diagnostics, tox shakeout, NIX-06 evaluation and the session-relaunch instruction'
      contains: 'NIX-06'
  key_links:
    - from: 'flake.nix shim bodies (writeShellScriptBin)'
      to: 'flake.nix fhsRun (buildFHSEnv named typsphinx-fhs-run)'
      via: 'exec of the absolute ${fhsRun}/bin/typsphinx-fhs-run path with the resolved absolute target and "$@"'
      pattern: 'typsphinx-fhs-run'
    - from: 'flake.nix venvWalk'
      to: '<checkout root>/.venv/bin/<tool>'
      via: 'upward walk from $PWD, stopping at the first ancestor holding .git'
      pattern: '\.git'
    - from: 'tox-uv-bare inside the sandbox'
      to: 'the uv shim'
      via: 'PATH lookup of uv from inside tox, which re-enters typsphinx-fhs-run (nested entry)'
      pattern: 'lint: OK'
    - from: 'flake.nix packages list'
      to: 'devShells.SYSTEM.default on all four systems'
      via: 'pkgs.lib.optionals pkgs.stdenv.hostPlatform.isLinux guard at the point of use'
      pattern: 'hostPlatform.isLinux'
  prohibitions:
    - statement: 'MUST NOT modify any tracked file other than flake.nix and this plan evidence file: nothing under typsphinx/, tests/, scripts/ or .github/, and not CLAUDE.md, tox.ini, pyproject.toml, uv.lock or flake.lock.'
      status: resolved
      verification: test
    - statement: 'MUST NOT let any of the six venv shims degrade to a binary other than the checkout own .venv/bin/<tool>; the fixed store-path second leg of the uv shim (D-02) is the single documented exception. A verdict from a version uv.lock does not pin is a result that disagrees with CI.'
      status: resolved
      verification: test
    - statement: 'MUST NOT resolve a shim target by a bare-name PATH lookup or by climbing out of the checkout the command runs in.'
      status: resolved
      verification: test
    - statement: 'MUST NOT describe the bubblewrap FHS sandbox as a security or isolation boundary in any artifact this plan writes; it binds the host filesystem read-write and shares the network by design.'
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record a nix develop diagnostic as the D-09 session-inheritance observation, and MUST NOT write a RED that did not reproduce.'
      status: resolved
      verification: judgment
---

<objective>
Add one Linux-guarded `pkgs.buildFHSEnv` passthrough and the seven D-01 command shims to `flake.nix`.
Prove a single `ruff` path end to end first: RED in a freshly provisioned worktree, then GREEN through
the shim. Then expand to the full roster (D-01, D-02, D-03), shake the design out through tox's own
subprocess tree (nested sandbox entry via tox-uv-bare's PATH lookup of `uv`, and `docs-pdf` for D-07),
and prove the flake still evaluates on all four declared systems with both darwin devShells
byte-identical (NIX-06).

Purpose: nothing else in Track A is safe until the mechanism exists and is shaken out (ROADMAP
constraint 1). This is the only plan that may edit `flake.nix`. Any design defect a tox run exposes
(nested entry, D-07's `@preview` reach) must surface here, while the file is still this plan's to
change. A later-wave fix would need a second session relaunch.

**Session boundary — read this before executing.** The Claude Code session PATH is frozen at launch
(orchestrator note 3). The session that runs this plan predates the edit, so it can never carry the
shims. Every shim invocation in this plan therefore goes through `nix develop . --command …` from the
worktree root and is labelled **DIAGNOSTIC** in the evidence. That is not the D-09 session-inheritance
shape. The genuine shape is observed by plans 64-02 and 64-03, whose precondition halts unless the
maintainer relaunched Claude Code from a direnv-loaded shell in the main checkout after this plan
merged. Task 3 writes that instruction into the evidence file, and the SUMMARY repeats it at the top.

Output: the edited `flake.nix` and `64-FLAKE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-PATTERNS.md
@flake.nix
@tox.ini
@CLAUDE.md
</context>

<!-- planner-region-allow: pkgs.ruff -->
The file-wide ban on nixpkgs' own ruff package in `flake.nix` is intentional: no task in this plan, or
in any plan of this phase, adds it (REQUIREMENTS.md § Out of Scope).

<assumption_delta_decision>
Detector (`assumption-delta scan 64`): `detected: true`, with two pluralization signals (`another`,
`second`) and one chosen signal (`chosen`). All three snippets are ROADMAP prose: the fall-through
wording in SC#3, the decoy-branch history, and the `symbolic-ref`-over-`checkout` choice. None of them
is an identity-model change. The one real plurality in this phase is D-02's two resolution sources for
`uv`, recorded here explicitly:
- **Primary noun:** the checkout's own `.venv/bin/<tool>`, pinned by `uv.lock`. It is the single
  identity every shim resolves.
- **Decision for the six venv shims:** `no-change`. One source, no fallback (D-03).
- **Decision for `uv`:** `add-alongside`, accepted as documented debt. The fixed `${pkgs.uv}` store
  path sits alongside `.venv/bin/uv` because `.venv/bin/uv` does not exist until Phase 65's `tox-uv`
  revert installs it.
- **What forces a later change:** Phase 65 making `.venv/bin/uv` always present. At that point the
  store-path leg can be dropped and `uv` joins the six strict shims (D-02's reversibility note).
- **Invariant check:** plan 64-03's rename proof already encodes "a shim resolves only its checkout's
  `.venv`", plus leg-1-before-leg-2 for `uv`. No test task is added, because D-05 forbids a new test
  file.
</assumption_delta_decision>

## Planning-time measurements this plan builds on (2026-09-11, re-measure; never cite as your own)

- A scratch `buildFHSEnv` at this flake's nixpkgs pin built in 3.3 s from cache. Inside it,
  `/lib64/ld-linux-x86-64.so.2` resolved to glibc-2.42's loader; outside, it resolved to `stub-ld`. A
  **nested** entry (the wrapper exec'ing the wrapper) returned rc 0 with the glibc loader visible.
- The caller's environment passed through: a variable containing a space and a non-ASCII word
  arrived intact, and `HOME`, `LANG=ja_JP.UTF-8` and
  `LOCALE_ARCHIVE=/run/current-system/sw/lib/locale/locale-archive` were all unchanged. `TMPDIR` is
  unset in this harness. `/tmp` has the same device and inode inside and out. `/etc/ssl/certs`,
  `/etc/fonts` and `/etc/resolv.conf` are symlinks to `/.host-etc/…`. `exit 7` propagated as rc 7.
- A generic-linux `ruff` ELF from the uv cache printed the stub-ld rejection outside (rc 127) and
  `ruff 0.15.20` inside the sandbox.
- `nix flake metadata .` run in a git worktree whose `.git` is a file resolved to
  `git+file://<worktree>`. A git ref copies tracked files only. A `path:` ref copies the whole
  directory, including an untracked `.venv` (~312 MB) and `.tox` (~599 MB) (orchestrator note 4).
- Pre-edit drvPaths at `96484915` (flake.lock unchanged since):
  - x86_64-darwin `/nix/store/fclfls55m9lp06679x7zrw0qzjya834j-nix-shell.drv`
  - aarch64-darwin `/nix/store/2m6y6pshri0vyw3jb5agczjnz9a92sxc-nix-shell.drv`
  - x86_64-linux `/nix/store/xbhqkzmnak21qk9j8ph0f00b8w65jr9l-nix-shell.drv`
  - aarch64-linux `/nix/store/z9yf853g0b6yfk5zf0x0jzvankdn9l34-nix-shell.drv`
- Pre-edit package census on both Linux and darwin:
  `["nodejs-24.16.0","pnpm-11.9.0","git-2.54.0","python3-3.13.13","uv-0.11.25"]`.
- `uv.lock` pins:
  - `ruff` 0.15.20 (line 1210)
  - `black` 26.5.1 (line 95)
  - `mypy` 2.1.0 (line 727)
  - `pytest` 9.1.1 (line 1011)
  - `tox` 4.56.1 (line 1395)
  - `sphinx` 9.1.0 (line 1266)

  The session `uv` is `/nix/store/cgvijxnmydknslkl368k4j4j43akvl8b-uv-0.11.25/bin/uv`.

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `fhsRun` | Nix let-binding: `pkgs.buildFHSEnv { name = "typsphinx-fhs-run"; … }` | `flake.nix`, per-system `let` |
| `typsphinx-fhs-run` | executable produced by `fhsRun` (`${fhsRun}/bin/typsphinx-fhs-run`) | nix store |
| `typsphinx-fhs-passthrough` | `pkgs.writeShellScript` used as `fhsRun`'s `runScript` (body: `exec "$@"`) | nix store |
| `venvWalk` | Nix function: tool name + on-stop fragment → bash fragment implementing the bounded upward walk | `flake.nix` |
| `venvShimNames` | Nix list `[ "tox" "ruff" "black" "mypy" "pytest" "sphinx-build" ]` | `flake.nix` |
| `venvShims` | Nix list of six `pkgs.writeShellScriptBin` derivations | `flake.nix` |
| `uvShim` | `pkgs.writeShellScriptBin "uv"` (D-02 two legs) | `flake.nix` |
| shim executables `uv`, `tox`, `ruff`, `black`, `mypy`, `pytest`, `sphinx-build` | `/nix/store/<hash>-<name>/bin/<name>` on the Linux devShell PATH | nix store |
| stderr prefix `typsphinx-shim:` and exit status `127` | D-03 not-found contract of the six venv shims | shim bodies |
| `64-FLAKE-EVIDENCE.md` | evidence file | phase directory |
| `COVERAGE.md` | external-API coverage declaration — **written by the planner at plan time**, not by this plan | phase directory |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — ruff RED in a fresh worktree, then one ruff shim through one FHS passthrough, GREEN from the root and from docs/</name>
  <files>flake.nix, .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md</files>
  <read_first>
    - flake.nix (all 40 lines — the edit lands inside the per-system `let` block at lines 21-38; `systems`, `forAllSystems`, `pkgsFor` and `inputs` stay untouched)
    - CLAUDE.md § "Worktree-isolated execution" (the provisioning line and the editable-install hazard)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-03, D-04, D-06, D-08, § Claude's Discretion)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md § Pattern 1, § Anti-Patterns, § Pitfall 1, § Pitfall 5, § Code Examples §3-§5
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-PATTERNS.md § flake.nix, § Evidence markdown
    - tox.ini lines 61-84 (`changedir = docs` for docs-html, docs-pdf, docs)
    - uv.lock lines 1209-1210 (the ruff pin)
    - .planning/milestones/v0.9.2-phases/63-v0-9-2-release-prep-prep-only/63-GREEN-TREE-EVIDENCE.md (the `$ <command>` + verbatim output + interpretive prose convention)
  </read_first>
  <action>
Work only inside this plan's isolated worktree. Guard every `cd` with an explicit failure path (RESEARCH Pitfall 5: a failed `cd` once let a research edit land on the real `flake.nix`).

1. Provisioning (CLAUDE.md, standing mode). Confirm `test -f .git` (isolated worktree) and that `git status --short` is empty. Run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`; in this session `uv` is still the bare nix-store `uv` because no shim exists yet, and that is expected. Record `pwd -P`, `git rev-parse HEAD` and `git rev-parse --abbrev-ref HEAD`.

2. Pre-edit session observation, the first half of D-09's question. Record `command -v ruff` (expected: no output, exit 1), `command -v uv` (expected: a `/nix/store/…-uv-0.11.25/bin/uv` path) and `echo "$DIRENV_DIR"`. This PATH was frozen when the session launched and can carry no shim.

3. RED, at the D-04 locus: a freshly provisioned worktree, never the main tree. Record `file .venv/bin/ruff`, and `.venv/bin/ruff --version` with its exit code. Expected: the stub-ld rejection text and rc 127.
   - If it instead prints a version at rc 0, record that verbatim under the heading `## RED did not reproduce (D-04 premise contradicted)` and continue.
   - Never write a RED that did not happen.

4. Pre-edit baselines, before touching `flake.nix`. Use the git flake ref `.` from the worktree root and never a `path:` ref (orchestrator note 4, re-verified at planning).
   - For each of `x86_64-linux aarch64-linux x86_64-darwin aarch64-darwin`, record `nix eval --raw .#devShells.<sys>.default.drvPath`, keeping stdout apart from stderr. The x86_64-darwin nixpkgs deprecation warning arrives on stderr and is recorded as a warning, not an error.
   - For x86_64-linux and aarch64-darwin, record the package census `nix eval --json .#devShells.<sys>.default.nativeBuildInputs --apply 'map (p: p.name)'`.

   Cross-check each value against the planning-time values listed above this task list.

5. Edit `flake.nix` inside the per-system `let` block, after `pkgs = pkgsFor system;`.

   a. Add `fhsRun = pkgs.buildFHSEnv { name = "typsphinx-fhs-run"; runScript = "${pkgs.writeShellScript "typsphinx-fhs-passthrough" …}"; }`, where the passthrough's whole body is `exec "$@"`.
      - Write the attribute name `buildFHSEnv` exactly: it is the bubblewrap alias. The removed chroot-based builder hard-fails evaluation on this pin.
      - Give it no `targetPkgs` and no `multiPkgs`. Per the discretion item, add a package only when a failing measurement demands it; D-07's `cacert` is the one anticipated case and belongs to Task 3, and only if docs-pdf fails on TLS.
      - Per D-06, the passthrough neither clears nor normalizes the caller's environment and sets no variable of its own. Do not use an env-clearing wrapper or a bubblewrap flag that drops the environment.
      - Per D-08, set no locale variable anywhere.

   b. Add `venvWalk`: a Nix function taking a tool name and an on-stop bash fragment, returning a bash fragment for the bounded upward walk (the planner's choice for the discretion item "shape of the upward .venv walk", required by orchestrator note 1).
      - Set `start="$PWD"` and `dir="$PWD"`, then loop:
        - If `[ -x "$dir/.venv/bin/<tool>" ]`, then `exec "${fhsRun}/bin/typsphinx-fhs-run" "$dir/.venv/bin/<tool>" "$@"`.
        - Else, if `[ -e "$dir/.git" ]` (a FILE in a worktree, a DIRECTORY in the main checkout), stop: this directory is the checkout root.
        - Else, if `[ "$dir" = / ]`, stop.
        - Else, set `dir="$(dirname "$dir")"`.
      - After the loop, splice in the on-stop fragment.
      - Effect: the walk climbs from `docs/` to the checkout root (tox `changedir = docs`). It never leaves the checkout it started in, so a worktree nested at `/home/yuta/Documents/typsphinx/.claude/worktrees/agent-<id>` stops at its own `.git` file and never reaches the main checkout's runnable `.venv/bin/<tool>`.
      - Double-quote every expansion so paths containing spaces or non-ASCII survive.
      - Never resolve the tool by bare name: no PATH lookup of the tool name and no exec of the tool by bare name (NIX-07, RESEARCH Pitfall 1).

   c. The D-03 on-stop fragment for venv shims. Print one line to stderr:
      `typsphinx-shim: <tool>: no executable .venv/bin/<tool> found walking up from $start (stopped at $dir); provision with: env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
      then `exit 127`. The discretion choice is 127 because it is the shell's command-not-found status, so callers read it as not-found. There is no fallback of any kind.

   d. One shim for this tracer: `pkgs.writeShellScriptBin "ruff"`, whose body is `set -eu` followed by `venvWalk "ruff"` with the D-03 on-stop fragment.

   e. Set `packages` to:
      - the four common entries `pkgs.nodejs`, `pkgs.pnpm`, `pkgs.git`, `pkgs.python3` (keep the existing Python-toolchain comment),
      - `++ pkgs.lib.optionals pkgs.stdenv.hostPlatform.isLinux [ <the ruff shim> ]`,
      - `++ [ pkgs.uv ]`.

      Darwin's list therefore stays nodejs, pnpm, git, python3, uv in today's order; Task 2 replaces the trailing `[ pkgs.uv ]`. Keep `fhsRun`, `venvWalk` and the shim inside the per-system `let` so Nix laziness never forces `buildFHSEnv` when `isLinux` is false.

6. GREEN, as a DIAGNOSTIC: label every transcript produced through `nix develop --command` with the word DIAGNOSTIC.
   - From the worktree root, run `nix develop . --command bash -c 'command -v ruff; ruff --version'`. Expected: a `/nix/store/…-ruff/bin/ruff` path, then exactly `ruff 0.15.20`. Compare against the `version =` line under `name = "ruff"` in `uv.lock`.
   - Record the resolved target, as orchestrator note 1 requires. Take the `+ exec …/typsphinx-fhs-run <path> --version` line from `nix develop . --command bash -c 'bash -x "$(command -v ruff)" --version 2>&1'`; it must name `$(pwd)/.venv/bin/ruff` inside this worktree.
   - Set `WT="$(git rev-parse --show-toplevel)"`, then run `cd docs` (guarded) followed by `nix develop "$WT" --command ruff --version`. Expected: `ruff 0.15.20`, which shows the walk survives tox's changedir.
   - Record the outside-sandbox control: `.venv/bin/ruff --version` still fails at rc 127, because the shim, not a repair of the file, is what makes it run.

7. Write `64-FLAKE-EVIDENCE.md` with the sections `## Worktree and provisioning`, `## Pre-edit session PATH`, `## RED`, `## Pre-edit baselines` and `## Tracer GREEN (DIAGNOSTIC)`.
   - Use the `$ <command>` + verbatim output + interpretive prose convention.
   - Keep machine paths verbatim and never substitute them: here they are the evidence of worktree containment.
   - Commit `flake.nix` and the evidence file together.
  </action>
  <verify>
    <automated>test -f .git &amp;&amp; grep -A1 '^name = "ruff"$' uv.lock | grep -Fqx 'version = "0.15.20"' &amp;&amp; nix develop . --command ruff --version | grep -Fx 'ruff 0.15.20' &amp;&amp; nix develop . --command bash -c 'bash -x "$(command -v ruff)" --version 2&gt;&amp;1' | grep -F "typsphinx-fhs-run $(pwd)/.venv/bin/ruff --version" &amp;&amp; WT="$(pwd)" &amp;&amp; ( cd docs &amp;&amp; nix develop "$WT" --command ruff --version ) | grep -Fx 'ruff 0.15.20' &amp;&amp; grep -q 'pkgs.buildFHSEnv' flake.nix &amp;&amp; grep -q 'typsphinx-fhs-run' flake.nix &amp;&amp; grep -q 'hostPlatform.isLinux' flake.nix &amp;&amp; [ "$(grep -c 'buildFHSEnvChroot' flake.nix)" = 0 ] &amp;&amp; git diff --quiet HEAD -- flake.lock &amp;&amp; grep -Eq 'Could not start dynamically linked executable|RED did not reproduce' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md &amp;&amp; [ "$(grep -c 'DIAGNOSTIC' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md)" -ge 1 ] &amp;&amp; [ -z "$(git status --porcelain -- typsphinx tests scripts .github CLAUDE.md tox.ini pyproject.toml uv.lock flake.lock)" ]</automated>
    <fails_when>non-zero exit from any link; a version line other than `ruff 0.15.20`; the xtrace `+ exec` line is absent or names a path outside this worktree (for example the main checkout's `.venv`); the evidence carries neither the stub-ld rejection text nor the labelled non-reproduction heading; a scope-fenced path shows in `git status --porcelain`</fails_when>
  </verify>
  <acceptance_criteria>
    - `flake.nix` contains `pkgs.buildFHSEnv`, `name = "typsphinx-fhs-run"`, `typsphinx-fhs-passthrough`, `pkgs.lib.optionals pkgs.stdenv.hostPlatform.isLinux`, `typsphinx-shim:`, `exit 127` and a `"$dir/.git"` test.
    - `grep -c 'buildFHSEnvChroot' flake.nix` prints `0`, and `git diff --quiet HEAD -- flake.lock` exits 0.
    - `nix develop . --command ruff --version` prints exactly `ruff 0.15.20` from both the worktree root and `docs/`.
    - The recorded xtrace `+ exec` line names `<this worktree>/.venv/bin/ruff`, a path under `pwd -P`.
    - `64-FLAKE-EVIDENCE.md` records `.venv/bin/ruff --version` failing at rc 127 with the stub-ld text, or the heading `## RED did not reproduce (D-04 premise contradicted)` with the verbatim output.
    - The evidence records the four pre-edit drvPaths, the x86_64-darwin and aarch64-darwin values labelled by system.
    - The only tracked paths changed by the commit are `flake.nix` and the evidence file.
  </acceptance_criteria>
  <done>A single `ruff` command name travels shim → FHS passthrough → this worktree's `.venv/bin/ruff` and prints the `uv.lock` version from the root and from `docs/`, with the RED it replaces recorded beside it and every shim run labelled DIAGNOSTIC.</done>
</task>

<task type="auto">
  <name>Task 2: Expand to the full seven-name roster — uv's two legs, five more strict venv shims, nixpkgs uv dropped from the Linux list</name>
  <files>flake.nix, .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md</files>
  <read_first>
    - flake.nix (as Task 1 left it)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-01, D-02, D-03, § Specific Ideas on the uv asymmetry)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md § Summary point 2 (packages-list order decides same-name collisions), § Pattern 1 (uvShim sketch)
    - CLAUDE.md § "Commands" (the documented bare commands D-01 derives from, including `sphinx-build` at lines 39-40)
    - uv.lock lines 95, 727, 1011, 1210, 1266, 1395 (the pins the diagnostics compare against)
  </read_first>
  <action>
1. Replace the single tracer shim with `venvShimNames = [ "tox" "ruff" "black" "mypy" "pytest" "sphinx-build" ]` and `venvShims = map (cmd: pkgs.writeShellScriptBin cmd …) venvShimNames`.
   - Each body is `set -eu` followed by `venvWalk cmd` with the D-03 on-stop fragment (the named stderr line, `exit 127`).
   - Per D-03, none of the six has a fallback: degrading to another binary replaces "doesn't run" with "runs but disagrees with CI".
   - Per D-01, the roster is exactly these six plus `uv`, derived from CLAUDE.md's documented bare commands. Nothing below them needs a shim, because the sandbox is inherited across fork/exec.

2. Add `uvShim = pkgs.writeShellScriptBin "uv" …`, D-02's one documented exception.
   - Leg 1 is the same bounded upward walk for `.venv/bin/uv`. Per orchestrator note 2, the planner chose the walk rather than a bare `$PWD/.venv/bin/uv` check, so that a `uv` call made from `docs/` under tox's changedir finds `../.venv/bin/uv` once Phase 65 makes it exist.
   - The on-stop fragment is leg 2: `exec "${fhsRun}/bin/typsphinx-fhs-run" "${pkgs.uv}/bin/uv" "$@"`. The store path is fixed at flake-evaluation time and cannot be shadowed.
   - Both legs enter the sandbox, which is what makes `uv run <tool>`, the mandatory worktree idiom, carry the sandbox into every child process.
   - There is no third leg, no PATH lookup and no error branch.
   - The walk logic exists once, in `venvWalk`. Only the on-stop fragment differs between `uvShim` and the six.

3. Set `packages` to the four common entries `++ pkgs.lib.optionals pkgs.stdenv.hostPlatform.isLinux ([ uvShim ] ++ venvShims) ++ pkgs.lib.optionals (!pkgs.stdenv.hostPlatform.isLinux) [ pkgs.uv ]`.
   - Discretion item taken: nixpkgs' `uv` package leaves the Linux list, so exactly one `uv` exists on the Linux devShell PATH. Research measured that earlier `packages` entries win same-name collisions; dropping the package removes the race outright.
   - Darwin's list stays nodejs, pnpm, git, python3, uv in today's order. Task 3 proves this by drvPath identity.

4. Diagnostics, labelled DIAGNOSTIC and run with `nix develop .` from the worktree root.
   - For each of the seven names, record `command -v <name>` (a `/nix/store/<hash>-<name>/bin/<name>` path), `<name> --version 2>&1` and the xtrace `+ exec` line from `bash -x "$(command -v <name>)" --version 2>&1`.
   - Read the expected versions from `uv.lock` at run time. The planning-time reads were ruff 0.15.20, black 26.5.1, mypy 2.1.0, pytest 9.1.1, tox 4.56.1, sphinx 9.1.0; `uv` is the pinned store build, 0.11.25 at planning.
   - Six xtrace lines must name `$(pwd)/.venv/bin/<name>`.
   - The `uv` line must name `/nix/store/<hash>-uv-<version>/bin/uv`, because `.venv/bin/uv` does not exist before Phase 65 (D-02).
   - Record `nix develop . --command bash -c 'type -P uv'`: it must print the shim's `/nix/store/<hash>-uv/bin/uv`, with no version in the store name. This is the NIX-06 ordering edge.

5. Append `## Roster expansion (DIAGNOSTIC)` to the evidence and commit `flake.nix` with it.
  </action>
  <verify>
    <automated>nix develop . --command bash -c 'for t in tox ruff black mypy pytest sphinx-build; do bash -x "$(command -v $t)" --version 2&gt;&amp;1 | grep -qF "typsphinx-fhs-run $PWD/.venv/bin/$t --version" || { echo "unresolved: $t"; exit 1; }; done; bash -x "$(command -v uv)" --version 2&gt;&amp;1 | grep -Eq "typsphinx-fhs-run /nix/store/[a-z0-9]+-uv-[0-9.]+/bin/uv --version"; type -P uv | grep -Eq "^/nix/store/[a-z0-9]+-uv/bin/uv$"; ruff --version | grep -Fxq "ruff 0.15.20"; black --version 2&gt;&amp;1 | grep -Fq "26.5.1"; mypy --version 2&gt;&amp;1 | grep -Fq "2.1.0"; pytest --version 2&gt;&amp;1 | grep -Fq "9.1.1"; tox --version 2&gt;&amp;1 | grep -Fq "4.56.1"; sphinx-build --version 2&gt;&amp;1 | grep -Fq "9.1.0"' &amp;&amp; grep -q 'venvShimNames' flake.nix &amp;&amp; grep -q 'uvShim' flake.nix &amp;&amp; grep -q '(!pkgs.stdenv.hostPlatform.isLinux)' flake.nix &amp;&amp; grep -q 'Roster expansion (DIAGNOSTIC)' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md</automated>
    <fails_when>non-zero exit; `unresolved: <tool>` printed for any venv shim; the uv xtrace line does not name a `/nix/store/…-uv-<version>/bin/uv` path; `type -P uv` prints anything other than the unversioned shim store path; any expected version substring is missing from its tool's output</fails_when>
  </verify>
  <acceptance_criteria>
    - `flake.nix` defines `venvShimNames` with exactly the six strings `tox`, `ruff`, `black`, `mypy`, `pytest`, `sphinx-build` in that order, plus `uvShim`.
    - The Linux branch of `packages` is `[ uvShim ] ++ venvShims`; the non-Linux branch is `[ pkgs.uv ]`.
    - The evidence records seven `command -v` paths, seven version outputs matching the `uv.lock` pins (uv matching its store build), and seven xtrace `+ exec` lines. Six name `<worktree>/.venv/bin/<tool>`; the seventh names a `/nix/store/…-uv-…/bin/uv` path.
    - `type -P uv` inside `nix develop .` prints `/nix/store/<hash>-uv/bin/uv`.
  </acceptance_criteria>
  <done>All seven D-01 names resolve by absolute path through the one shared sandbox. The six venv shims are strict, and `uv` alone has the D-02 store-path second leg.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the final flake — tox's subprocess tree through the sandbox (nested entry, D-07), all four systems evaluate, darwin byte-identical</name>
  <files>flake.nix, .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md</files>
  <read_first>
    - flake.nix (as Task 2 left it)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-06, D-07, D-08, § Deferred Ideas on the cold `@preview` cache)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-RESEARCH.md § Pitfall 3 (the all-systems flag), § Code Examples §3, § Summary point 3 (host `/etc` binds)
    - .planning/ROADMAP.md "Binding constraints" 8 (zero CI coverage; darwin unverified by construction)
    - tox.ini (the `lint` and `docs-pdf` environments; `requires = tox-uv-bare~=1.35` — read-only)
  </read_first>
  <action>
1. Nested-entry and namespace-inheritance shakeout, labelled DIAGNOSTIC.
   - Record `test ! -e .tox`, then run `nix develop . --command tox -e lint` from the worktree root.
   - What this exercises: `tox` runs inside the sandbox; tox-uv-bare resolves `uv` from PATH, which is now the uv shim; that shim enters the sandbox again, a nested entry. Planning measured a nested entry of a scratch `buildFHSEnv` returning rc 0; this run re-measures it on the real flake.
   - Expected: `black --check .` and `ruff check .` both execute, then `lint: OK` and `congratulations :)`.
   - If it fails because of the nested entry:
     - record the failure verbatim,
     - fix it in `flake.nix` in this task, keeping D-06's pure passthrough and D-03's strictness intact,
     - record the rationale, and re-run.

     Never route around the failure outside `flake.nix`.

2. D-07 shakeout.
   - Record `ls "$HOME/.cache/typst/packages/preview"`.
   - Run `rm -rf docs/_build` followed by `nix develop . --command tox -e docs-pdf`, then check `test -s docs/_build/pdf/typsphinx.pdf` and that `head -c 4 docs/_build/pdf/typsphinx.pdf` prints `%PDF`.
   - Record the same `ls` afterwards.
   - If the build cannot reach its `@preview` packages:
     - First confirm `$HOME` reaches the sandbox by recording the output of `nix develop . --command uv run python -c 'import os; print(os.environ["HOME"])'`.
     - Only if a cold fetch then fails on TLS, add `pkgs.cacert` to `fhsRun`'s `targetPkgs` (D-07: fixed in this phase, not deferred), recording the failing transcript before the fix.
   - If the warm cache did all the work, write one sentence saying the TLS path stayed unexercised. Do not manufacture a cold-cache scenario (CONTEXT § Deferred Ideas).

3. NIX-06, on the FINAL `flake.nix` content (after any step-1 or step-2 fix).
   - `nix flake show --all-systems` must exit 0 and list `devShells` for all four systems.
   - `nix flake check --all-systems --no-build` must exit 0. The flag is mandatory: without it the Linux evaluator silently omits three systems (RESEARCH Pitfall 3).
   - Record per-system `nix eval --raw .#devShells.<sys>.default.drvPath` for all four. Both darwin values must be byte-identical to Task 1's pre-edit values and to the planning-time values; this is the NIX-06 empty edge (the Linux-only additions are the empty list on darwin). Both Linux values must differ from their pre-edit values.
   - Record the package census `nix eval --json .#devShells.<sys>.default.nativeBuildInputs --apply 'map (p: p.name)'`:
     - x86_64-linux must read exactly `["nodejs-24.16.0","pnpm-11.9.0","git-2.54.0","python3-3.13.13","uv","tox","ruff","black","mypy","pytest","sphinx-build"]`: one `uv`, no `uv-<version>`. This is the NIX-06 adjacency edge.
     - x86_64-darwin and aarch64-darwin must read exactly the pre-edit `["nodejs-24.16.0","pnpm-11.9.0","git-2.54.0","python3-3.13.13","uv-0.11.25"]`.
   - State in prose that darwin *evaluation* is proven here but darwin *execution* is unverified by construction (ROADMAP constraint 8). Phase 68's DOC-21 writes that up; this plan adds no documentation comments to `flake.nix` beyond what the code needs.

4. Append the evidence sections `## tox subprocess tree through the sandbox (DIAGNOSTIC)`, `## D-07` and `## NIX-06 — all four systems`, then a final section `## Session relaunch required before wave 2` that states:
   - The session that ran this plan predates the edit, and its PATH carries no shim (orchestrator note 3).
   - After this plan merges into the main checkout, the maintainer opens a terminal in `/home/yuta/Documents/typsphinx` so direnv re-evaluates the flake.
   - The maintainer confirms that `command -v ruff` prints a `/nix/store/…-ruff/bin/ruff` path, launches Claude Code from that same shell, and re-runs `/gsd-execute-phase 64`.
   - Plans 64-02 and 64-03 carry a precondition that halts otherwise. No per-worktree direnv allow and no `nix develop --command` wrapper around the provisioning line is involved; D-09 rejects both.

   Commit.
  </action>
  <verify>
    <automated>nix flake check --all-systems --no-build &amp;&amp; nix flake show --all-systems &gt;/dev/null &amp;&amp; [ "$(nix eval --raw .#devShells.x86_64-darwin.default.drvPath 2&gt;/dev/null)" = /nix/store/fclfls55m9lp06679x7zrw0qzjya834j-nix-shell.drv ] &amp;&amp; [ "$(nix eval --raw .#devShells.aarch64-darwin.default.drvPath 2&gt;/dev/null)" = /nix/store/2m6y6pshri0vyw3jb5agczjnz9a92sxc-nix-shell.drv ] &amp;&amp; [ "$(nix eval --raw .#devShells.x86_64-linux.default.drvPath 2&gt;/dev/null)" != /nix/store/xbhqkzmnak21qk9j8ph0f00b8w65jr9l-nix-shell.drv ] &amp;&amp; [ "$(nix eval --json .#devShells.x86_64-linux.default.nativeBuildInputs --apply 'map (p: p.name)' 2&gt;/dev/null)" = '["nodejs-24.16.0","pnpm-11.9.0","git-2.54.0","python3-3.13.13","uv","tox","ruff","black","mypy","pytest","sphinx-build"]' ] &amp;&amp; [ "$(nix eval --json .#devShells.aarch64-darwin.default.nativeBuildInputs --apply 'map (p: p.name)' 2&gt;/dev/null)" = '["nodejs-24.16.0","pnpm-11.9.0","git-2.54.0","python3-3.13.13","uv-0.11.25"]' ] &amp;&amp; nix develop . --command tox -e lint &amp;&amp; test -s docs/_build/pdf/typsphinx.pdf &amp;&amp; [ "$(head -c 4 docs/_build/pdf/typsphinx.pdf)" = '%PDF' ] &amp;&amp; [ "$(grep -c 'LC_ALL' flake.nix)" = 0 ] &amp;&amp; [ "$(grep -c 'clearenv' flake.nix)" = 0 ] &amp;&amp; [ "$(grep -c 'patchelf' flake.nix)" = 0 ] &amp;&amp; [ "$(grep -c 'pkgs.ruff' flake.nix)" = 0 ] &amp;&amp; grep -q 'Session relaunch required before wave 2' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md &amp;&amp; grep -q 'lint: OK' .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md &amp;&amp; git diff --quiet HEAD -- flake.lock &amp;&amp; [ -z "$(git status --porcelain -- typsphinx tests scripts .github CLAUDE.md tox.ini pyproject.toml uv.lock flake.lock)" ]</automated>
    <fails_when>non-zero exit from either all-systems command; a darwin drvPath that differs from the recorded pre-edit value; the x86_64-linux drvPath unchanged (the shims did not land); a census JSON not matching the stated arrays; `tox -e lint` not exiting 0; the PDF missing, empty, or not beginning with `%PDF`; a locale variable, an env-clearing flag, an ELF-patch tool or nixpkgs' ruff package appearing in `flake.nix`; the relaunch section absent</fails_when>
  </verify>
  <acceptance_criteria>
    - Both all-systems commands exit 0 on the final content, and their transcripts are in the evidence.
    - The two darwin drvPaths equal the pre-edit values, `/nix/store/fclfls55m9lp06679x7zrw0qzjya834j-nix-shell.drv` and `/nix/store/2m6y6pshri0vyw3jb5agczjnz9a92sxc-nix-shell.drv`.
    - The x86_64-linux census is exactly the eleven-name array stated in the action; the darwin census is exactly the five-name pre-edit array.
    - The evidence shows `tox -e lint` ending in `lint: OK` with both `black --check .` and `ruff check .` executed, and `tox -e docs-pdf` producing `docs/_build/pdf/typsphinx.pdf` whose first four bytes are `%PDF`.
    - The evidence states whether `pkgs.cacert` was needed (D-07). It was added only if a recorded TLS failure demanded it.
    - `64-FLAKE-EVIDENCE.md` ends with `## Session relaunch required before wave 2`, naming `/home/yuta/Documents/typsphinx`, `command -v ruff` and `/gsd-execute-phase 64`.
  </acceptance_criteria>
  <done>The final flake runs tox's whole subprocess tree through the sandbox (nested entry included), produces a real PDF, evaluates on all four systems with darwin unchanged byte for byte, and hands wave 2 an explicit relaunch instruction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| devShell PATH → shim | a bare command name typed by the maintainer or spawned by tox is resolved to a shim; the shim decides which binary actually runs |
| shim → FHS sandbox | the resolved absolute target is exec'd through `typsphinx-fhs-run`; the sandbox binds the host filesystem read-write and shares the network (compatibility shim, not isolation) |
| flake.nix → four declared systems | an unguarded Linux-only reference breaks evaluation on darwin, where no CI or local check exists except this plan's evaluation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01 | Tampering | shim target resolution (bare-name PATH lookup / self-recursion) | medium | mitigate | `venvWalk` resolves an absolute `.venv/bin/<tool>` path or, for `uv`, the fixed `${pkgs.uv}` store path; no shim body performs a PATH lookup of a tool name; Task 2 records every xtrace `+ exec` line; plan 64-03 runs the rename proof |
| T-64-02 | Tampering | walk escaping a nested worktree into the main checkout's runnable `.venv` | medium | mitigate | the walk stops at the first ancestor holding `.git` (orchestrator note 1); plan 64-03 proves it from a nested worktree with a positive control |
| T-64-03 | Information Disclosure | FHS sandbox binds `/home`, `/tmp` and host `/etc` entries read-write | low | accept | same trusted local tooling the maintainer already runs unsandboxed; the sandbox adds no attack surface relative to the pre-phase baseline; no artifact may call it an isolation boundary (prohibition) |
| T-64-04 | Denial of Service | darwin devShell broken by an unguarded `buildFHSEnv` reference | medium | mitigate | `buildFHSEnv` lives in the per-system `let` and is only reached through `pkgs.lib.optionals pkgs.stdenv.hostPlatform.isLinux`; Task 3 gates on `--all-systems` evaluation and byte-identical darwin drvPaths |
| T-64-05 | Tampering | supply chain of the flake inputs | low | accept | no new input; `flake.lock` is asserted unchanged (`git diff --quiet HEAD -- flake.lock`); the pinned nixpkgs narHash is the existing integrity mechanism |
| T-64-SC | Tampering | npm/pip/cargo installs | low | accept | none — RESEARCH § Package Legitimacy Audit: zero registry packages installed; every tool used is a nixpkgs builtin of the already-pinned input |
</threat_model>

<verification>
- `nix flake check --all-systems --no-build` exits 0 and both darwin drvPaths equal their pre-edit values.
- All seven shims resolve by absolute path (six into this worktree's `.venv`, `uv` into the nix store), with versions matching `uv.lock`.
- `tox -e lint` and `tox -e docs-pdf` complete through the sandbox (DIAGNOSTIC), and the PDF begins with `%PDF`.
- `git status --porcelain` over the scope-fenced paths is empty; `flake.lock` is unchanged.
</verification>

<success_criteria>
`flake.nix` carries exactly one Linux-guarded `buildFHSEnv` passthrough and the seven D-01 shims. Six are strict (D-03) and `uv` has the D-02 store-path second leg. The walk is bounded at the checkout root. The flake evaluates on all four systems with darwin byte-identical. RED and every diagnostic GREEN are recorded verbatim, and the relaunch instruction for wave 2 is written down.
</success_criteria>

<output>
Create `.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-01-SUMMARY.md` when done.
- Its first section must be `## Session relaunch required before wave 2`, repeating the evidence file's instruction verbatim.
- Frontmatter declares `requirements-completed: [NIX-06]`. NIX-01 and NIX-07 are proven by plans 64-02 and 64-03 in the genuine session shape, not by this plan's diagnostics.
</output>
