---
phase: 58-repr-format-decoupling-test-side-only
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - tests/test_builder.py
  - .planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md
autonomous: true
requirements: [MSG-01]
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "MSG-01/SC#1: `tests/test_builder.py`'s image-rehome pass criterion no longer applies `repr()` to a path value — `test_post_process_images_rehome_escape_relocates_with_warning` asserts that `abs_uri` is NAMED in the captured `caplog` message via `path_named_in`, with no line narrowing needed because the message is already a single record's `getMessage()`."
    - "The rewrite is a pass-criterion substitution only: the `os.name` branch building `abs_root`/`abs_uri` at lines 555-561, the SHA-1 `expected_key` computation, the `img[\"uri\"] == expected_key` assertion, the `\"..\" not in img[\"uri\"].split(\"/\")` assertion, the `builder.images.get(expected_key) == abs_uri` assertion, the single-WARNING-record assertion and the `\"could not rehome image URI\" in message` assertion all survive byte-identical."
    - "SC#2(b): the rewritten test is recorded green on the post-rewrite tree with the verbatim pytest summary line and no `skipped` term; its pre-rewrite green was already recorded by plan 58-01 against the untouched tree and is not re-derived here."
    - "SC#2(c)/D-05(b): with `{resolved_uri!r}` dropped from the image-rehome warning at `typsphinx/builder.py:1766-1769` while `{key!r}` is still interpolated, the rewritten test goes RED — recorded verbatim — and green again on the restored file."
    - "SC#4: `typsphinx/builder.py` is restored inside the same task that perturbs it; `git status --porcelain typsphinx/` and `git diff --name-only -- typsphinx/` are both recorded empty before this plan's commit."
    - "After this plan the whole-tree AST pass-criterion sweep over `tests/**/*.py` returns exactly 7 hits — the seven non-path sites — down from 9 at the phase base and 8 after plan 58-01. Zero path-valued pass-criterion sites remain, which is SC#3's measurable half."
    - "`typsphinx/builder.py:1767` is left byte-identical to the phase base, so Phase 60's MSG-03 rewiring of that exact site remains an honest, uncircular test of the zero-test-edits discipline."
  prohibitions:
    - statement: "The rewritten image-rehome assertion must not become tautological — it must not pass against a warning message that names no path at all."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed by this plan; the D-05(b) temporary edit is reverted inside the same task and must not survive into any commit."
      status: unverified
      verification: flagged
    - statement: "Evidence appended to 58-DECOUPLING-EVIDENCE.md is never asserted, paraphrased or reconstructed — only verbatim captured command output, including the pytest summary line with its collected/passed/skipped counts."
      status: unverified
      verification: flagged
    - statement: "No section this plan did not write in 58-DECOUPLING-EVIDENCE.md is edited, reordered or removed — plan 58-01's recorded runs are append-only history."
      status: unverified
      verification: flagged
    - statement: "No assertion already present in test_post_process_images_rehome_escape_relocates_with_warning is deleted or loosened, and the os.name branch that builds the absolute URI is not touched."
      status: unverified
      verification: flagged
  artifacts:
    - path: "tests/test_builder.py"
      provides: "the image-rehome warning test, rewritten onto the shared predicate"
      contains: "path_named_in(abs_uri, message)"
    - path: ".planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md"
      provides: "the second call site's post-rewrite green and the recorded RED under the builder.py:1767 falsification with its revert proof, appended below plan 58-01's sections"
      contains: "builder.py:1767"
  key_links:
    - from: "tests/test_builder.py"
      to: "tests/_path_naming.py"
      via: "a bare top-level import of the leaf module plan 58-01 created; this is the second and final consumer, and the reason the predicate is shared rather than duplicated"
      pattern: "from _path_naming import path_named_in"
    - from: "tests/test_builder.py"
      to: "typsphinx/builder.py:1766-1769"
      via: "a real in-process builder.post_process_images() call whose logged WARNING is captured by caplog; the test asserts on the product's own emitted message, never on a re-pasted copy of its format string"
      pattern: "could not rehome image URI"
  flagged_assumptions: []
---

<objective>
Expand the proven tracer slice to the second and final call site: `tests/test_builder.py`'s
image-rehome warning test. Its pass criterion moves onto the shared predicate created in plan 58-01,
its post-rewrite green is recorded, and a second real falsification — the path field dropped from
`typsphinx/builder.py:1766-1769` while the relocation key is still interpolated — is recorded RED and
reverted inside one task.

Purpose: completes ROADMAP Phase 58 SC#1 and SC#2 across both sites MSG-01 names, and brings the
whole-tree AST pass-criterion count to exactly 7 — the seven non-path sites — which is the enumeration
plan 58-03's guard then locks. This plan is deliberately in its own wave: it consumes
`tests/_path_naming.py` from plan 58-01, and it changes what the census sweep finds, so it must not
run beside either the plan that creates the module or the plan that asserts on the sweep's result.

Output: a rewritten `tests/test_builder.py` and two new sections appended to
`58-DECOUPLING-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-repr-format-decoupling-test-side-only/58-CONTEXT.md
@.planning/phases/58-repr-format-decoupling-test-side-only/58-RESEARCH.md
@.planning/phases/58-repr-format-decoupling-test-side-only/58-PATTERNS.md
@.planning/phases/58-repr-format-decoupling-test-side-only/58-VALIDATION.md
@.planning/phases/58-repr-format-decoupling-test-side-only/58-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-level list, repeated in every plan of this phase so each is readable standalone. Every symbol
below is NEW in Phase 58 — none of it exists at the phase base, so none of it is drift.

**Source tree (new files and new symbols, all under `tests/`):**

| Artifact | Plan | Shape |
|---|---|---|
| `tests/_path_naming.py` | 58-01 | new leaf test-support module; zero `typsphinx` imports |
| `path_named_in(value, text) -> bool` | 58-01 | new function in `tests/_path_naming.py`; the shared naming predicate |
| `tests/test_path_naming_predicate.py` | 58-01 | new test module; D-05(a)'s durable meta-tests |
| `tests/test_out02_escape_target_gate.py` | 58-01 | MODIFIED — the pass criterion at line 134 is replaced by D-02's line-narrowed predicate call; a `from _path_naming import path_named_in` import is added |
| `tests/test_builder.py` | 58-02 | MODIFIED — the pass criterion at line 598 is replaced by a direct predicate call; a `from _path_naming import path_named_in` import is added |
| `tests/test_repr_census_guard.py` | 58-03 | new test module; the AST-backed census guard |
| `PASS_CRITERION_REPR_ALLOWLIST` | 58-03 | new module-level constant in `tests/test_repr_census_guard.py`; the recorded seven-site allowlist |
| `_collect_pass_criterion_repr_sites()` | 58-03 | new module-level helper in `tests/test_repr_census_guard.py`; the `ast`-based sweep |

**Planning artifacts (evidence family):**

| Path (under `.planning/phases/58-repr-format-decoupling-test-side-only/`) | Plan |
|---|---|
| `58-DECOUPLING-EVIDENCE.md` | 58-01 (created) → 58-02 (appended) → 58-03 (appended) |
| `58-REPR-CENSUS.md` | 58-03 |
| `COVERAGE.md` | 58-01 |

**Reserved name — never produced:** no artifact in this phase may be named `58-VERIFICATION.md`.
`gsd-verifier` owns and clobbers that exact filename wholesale at verify time (D-07).

**Nothing under `typsphinx/` is an artifact of this phase.** The temporary edits D-05(b) requires are
perturbations reverted inside the task that makes them; `typsphinx/builder.py` is NOT listed in any
plan's `files_modified` and must be byte-identical to the phase base at every commit.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the image-rehome warning test's pass criterion onto the shared predicate and record its post-rewrite green</name>
  <precondition>`tests/_path_naming.py` exists in this worktree and `PYTHONPATH=tests uv run python -c "from _path_naming import path_named_in"` exits 0. Plan 58-01 created it in the previous wave; if the import fails, the wave merge did not land and nothing below is meaningful — STOP and report.</precondition>
  <reversibility rating="reversible">One replaced assertion expression and one added import inside one test function; undo is a single `git revert`.</reversibility>
  <files>tests/test_builder.py, .planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md</files>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — provisioning is MANDATORY on this project and is not conditional on parallelism. Without `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` followed by `uv run` for every command, pytest imports the UNCHANGED main-tree package and every gate below reports against the wrong tree.
    - `tests/test_builder.py` lines 1-12 — the module docstring and the existing import block (`import os`, `from pathlib import Path`, then `from docutils import nodes` and `from sphinx.builders import Builder`), which is where the new import goes.
    - `tests/test_builder.py` lines 520-605 — the whole of `test_post_process_images_rehome_escape_relocates_with_warning`: the `os.name` branch building `abs_root`/`abs_uri` at 555-561 (with its 52-09 comment about CPython 3.13's `ntpath.isabs()` change), the doctree construction, the `caplog.at_level("WARNING")` block, the SHA-1 `expected_key` computation, and the final assertion block at 588-598 that is the only thing changing.
    - `tests/_path_naming.py` — the predicate created by plan 58-01, so the call is made against the function as written.
    - `typsphinx/builder.py` lines 1755-1775 — the image-rehome warning this test observes, including the `RESERVED_IMAGE_NAMESPACE`/digest key construction. READ ONLY in this task.
    - `.planning/phases/58-repr-format-decoupling-test-side-only/58-PATTERNS.md` § "`tests/test_builder.py` (MODIFIED…)" — the exact block being replaced and the "no `os.name` branch needed in the assertion itself" note.
  </read_first>
  <action>
Provision the worktree FIRST, before any other command:
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Then run every subsequent command
through `uv run`. Use the bare `uv` on PATH, never `.venv/bin/uv`, which does not exist under
`tox-uv-bare`.

**Step 1 — add the import.** Add `from _path_naming import path_named_in` to
`tests/test_builder.py`'s import block. Measured at plan time against this repository's `ruff`
(`select` includes `I`, and `_path_naming` is classified third-party because no matching module
exists under the project root): the correct placement is the FIRST line of the third-party block,
above `from docutils import nodes`, giving

`import os` / `from pathlib import Path` / blank / `from _path_naming import path_named_in` /
`from docutils import nodes` / `from sphinx.builders import Builder`

That exact arrangement was probed at plan time and is both `ruff`-clean and `black`-clean. Never
write `from tests._path_naming import ...` — `tests/` has no `__init__.py` and pytest's default
`prepend` import mode puts `tests/` itself on `sys.path`.

**Step 2 — replace the pass criterion.** In
`test_post_process_images_rehome_escape_relocates_with_warning`, replace the six-line comment block
plus the single final `assert` at lines 591-598 — the assertion whose test expression applies
Python's builtin representation function to the `abs_uri` variable and searches `message` for the
result — with `assert path_named_in(abs_uri, message)` carrying a failure message that names the URI
and quotes the whole captured message.

No line narrowing is needed here and none must be added: `message` is already a single `caplog`
record's `getMessage()` (`warning_records[0]`, after the test has asserted there is exactly one
WARNING record), so D-02's whole-capture unsoundness does not apply.

Replace the stale explanatory comment rather than keeping it — it currently explains why the test
asserts on the representation form, which is precisely the coupling this phase removes. Write a
short replacement comment stating that the assertion is now format-agnostic: it holds whether the
message site quotes with `!r` (today), with a hardcoded `'{value}'`, or with the delimiter-aware
helper MSG-02 introduces, which is what lets Phase 60 rewire this exact message site with zero test
edits.

Pass the `abs_uri` variable the test already computed — never a re-typed literal, never a
re-derivation of the `os.name` branch. Everything else in the function stays byte-identical: the
`os.name` branch, the `digest`/`expected_key` computation, `img["uri"] == expected_key`, the `".."`
segment assertion, `builder.images.get(expected_key) == abs_uri`, `len(warning_records) == 1` and
`"could not rehome image URI" in message`.

**Step 3 — record the post-rewrite green.** Append to
`.planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md` (append only —
do not edit, reorder or remove any section plan 58-01 wrote) a heading
`## SC#1/SC#2 (b) — post-rewrite green: image-rehome warning` and record, each with its command line
shown above its output:

- `uv run pytest tests/test_builder.py::test_post_process_images_rehome_escape_relocates_with_warning -q`
  — whole output verbatim, expecting `1 passed` with no `skipped` term.
- `uv run pytest tests/test_builder.py -q` — the whole module, verbatim, proving the added import
  broke nothing else in a 600-line file.
- The AST pass-criterion count for this file, run as
  `uv run python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_builder.py').read_text(encoding='utf-8'));print(sum(1 for a in ast.walk(t) if isinstance(a,ast.Assert) for s in ast.walk(a.test) if (isinstance(s,ast.Call) and isinstance(s.func,ast.Name) and s.func.id=='repr') or (isinstance(s,ast.FormattedValue) and s.conversion==114)))"`
  — expected output `0`. It measured `1` at the phase base, so this number moving from 1 to 0 IS
  SC#1 for this file.
- The whole-tree count
  `uv run python -c "import ast,pathlib;print(sum(1 for f in pathlib.Path('tests').rglob('*.py') if '__pycache__' not in f.parts for a in ast.walk(ast.parse(f.read_text(encoding='utf-8'))) if isinstance(a,ast.Assert) for s in ast.walk(a.test) if (isinstance(s,ast.Call) and isinstance(s.func,ast.Name) and s.func.id=='repr') or (isinstance(s,ast.FormattedValue) and s.conversion==114)))`
  — expected output `7`. It was `9` at the phase base and `8` after plan 58-01. Label this line
  `SC#3 — path-valued pass-criterion count is now zero`.
- `uv run black --check tests/test_builder.py` and `uv run ruff check tests/test_builder.py`. Apply
  the same `ruff`-may-not-exec rule plan 58-01 used: on this NixOS host a freshly-synced worktree
  venv's `ruff` may be a generic-linux ELF that cannot exec; retry with
  `nix-shell -p ruff --run "ruff check tests/test_builder.py"`, and if BOTH routes fail to execute,
  record both failures verbatim under a `### ruff could not execute in this worktree` sub-heading and
  state that lint authority for this file falls to CI. An environment defect is not a code RED and
  must not be reported as one — but it must not be silently omitted either.
- `git status --porcelain typsphinx/` — expected empty.
  </action>
  <verify>
    <automated>uv run pytest tests/test_builder.py -q && uv run python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_builder.py').read_text(encoding='utf-8'));assert 0==sum(1 for a in ast.walk(t) if isinstance(a,ast.Assert) for s in ast.walk(a.test) if (isinstance(s,ast.Call) and isinstance(s.func,ast.Name) and s.func.id=='repr') or (isinstance(s,ast.FormattedValue) and s.conversion==114))" && uv run python -c "import ast,pathlib;assert 7==sum(1 for f in pathlib.Path('tests').rglob('*.py') if '__pycache__' not in f.parts for a in ast.walk(ast.parse(f.read_text(encoding='utf-8'))) if isinstance(a,ast.Assert) for s in ast.walk(a.test) if (isinstance(s,ast.Call) and isinstance(s.func,ast.Name) and s.func.id=='repr') or (isinstance(s,ast.FormattedValue) and s.conversion==114))" && uv run black --check tests/test_builder.py && test -z "$(git status --porcelain typsphinx/)" && grep -q '^## SC#1/SC#2 (b) — post-rewrite green: image-rehome warning' .planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_builder.py` contains the literal `from _path_naming import path_named_in` exactly once and the literal `path_named_in(abs_uri, message)` exactly once.
    - The AST pass-criterion count for `tests/test_builder.py` prints `0`. It printed `1` at the phase base.
    - The whole-tree AST pass-criterion count prints `7`. It printed `9` at the phase base and `8` after plan 58-01.
    - `tests/test_builder.py` still contains `expected_key = f"{RESERVED_IMAGE_NAMESPACE}/{digest}-chart.png"`, `assert img["uri"] == expected_key`, `assert builder.images.get(expected_key) == abs_uri`, `assert len(warning_records) == 1` and `assert "could not rehome image URI" in message` — no pre-existing assertion was deleted or loosened.
    - `git diff -- tests/test_builder.py` shows changes confined to the import block and to the assertion block inside `test_post_process_images_rehome_escape_relocates_with_warning`; the `if os.name == "nt":` branch and the lines building `abs_root`/`abs_uri` are unchanged.
    - `uv run pytest tests/test_builder.py -q` reports every test `passed` with no `skipped` term in the summary line.
    - `uv run black --check tests/test_builder.py` exits 0.
    - `58-DECOUPLING-EVIDENCE.md` contains the heading `## SC#1/SC#2 (b) — post-rewrite green: image-rehome warning`, and still contains all four headings plan 58-01 wrote — `## SC#2 (a)`, `## SC#1/SC#2 (b) — post-rewrite green: escape-target gate`, `## SC#2 (c) — recorded falsification: builder.py:697 (docname target warning)` and `## D-05(a) — durable meta-tests and the running census count`.
    - Either a `ruff` transcript exiting 0 is present in the evidence file for this file, or a `### ruff could not execute in this worktree` sub-heading is present carrying both failure transcripts verbatim.
    - `git status --porcelain typsphinx/` produces no output.
  </acceptance_criteria>
  <done>The image-rehome test asserts that the URI is named in the warning rather than that the warning contains a particular representation of it, every other assertion in that test is intact, the whole-tree pass-criterion count has reached 7 with zero path-valued sites left, and the run is recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: D-05(b) — the recorded real falsification of the image-rehome test against a temporarily edited `typsphinx/builder.py`, reverted inside this task</name>
  <precondition>`git status --porcelain typsphinx/` is empty before this task starts. If it is not, STOP — a prior task left a product edit behind and SC#4 is already violated; report rather than revert blindly.</precondition>
  <reversibility rating="reversible">The product edit is transient by construction and is restored by `git checkout --` within this same task; nothing about it reaches a commit.</reversibility>
  <files>.planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md</files>
  <read_first>
    - `typsphinx/builder.py` lines 1755-1775 — the exact `logger.warning(...)` call whose message begins with the literal `could not rehome image URI` and interpolates two fields, `resolved_uri` and `key`, both with the `!r` conversion. Note the surrounding comment recording that the SHA-1 digest is a collision-avoidance key, not a security boundary.
    - `tests/test_builder.py` lines 520-600 — the rewritten test from Task 1, so the falsification runs against the real post-rewrite assertion.
    - `.planning/phases/58-repr-format-decoupling-test-side-only/58-CONTEXT.md` § D-05, D-06 — both routes are required and the edit/measure/revert cycle stays inside one plan.
    - `.planning/phases/58-repr-format-decoupling-test-side-only/58-RESEARCH.md` § "Common Pitfalls" #6 — the worktree/editable-install hazard that makes an unprovisioned RED meaningless.
    - `.planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md` § `## SC#2 (c) — recorded falsification: builder.py:697 (docname target warning)` — plan 58-01's section, whose shape this one mirrors.
  </read_first>
  <action>
This is the second and last point in Phase 58 that touches `typsphinx/`, and the edit must not
survive the task. Work in this exact order and do not reorder it.

**Step 1 — make the falsifying edit.** In `typsphinx/builder.py`, at the `logger.warning(...)` call
whose message begins with the literal `could not rehome image URI`, delete ONLY the interpolation of
the `resolved_uri` field from the message, leaving the `key` interpolation and its `!r` conversion
exactly as they are. Two properties are load-bearing and must both hold:

- The literal `could not rehome image URI` must remain intact, because the test asserts it is present
  in the message BEFORE it applies the predicate. If it breaks, the test goes RED on that earlier
  assertion instead of on the naming assertion and the falsification proves nothing.
- The `key` field must still be interpolated. That is the analogue of the D-03 trap on this site: the
  relocation key ends in the same basename (`chart.png`) as the URI it replaced, so a message still
  carrying `key` is exactly the shape a basename-based predicate would wrongly accept.

Capture `git diff -- typsphinx/builder.py` while the edit is in place — you will need it in Step 5,
after Step 3 has discarded it. Do not run `black`, `ruff` or any formatter against the edited file;
lint cleanliness of a file about to be discarded is not a signal.

**Step 2 — measure the RED.** Run
`uv run pytest tests/test_builder.py::test_post_process_images_rehome_escape_relocates_with_warning -q`
and capture the WHOLE output verbatim. Expected: `1 failed`. Verified analytically at plan time that
this goes RED under this edit — the absolute URI the test builds is not a substring of the surviving
message in either the raw or the doubled-backslash rendering, because the surviving `key` is the
namespaced digest form, not the original path.

Then confirm the RED is attributable to the right assertion: the failure output must show the
`path_named_in` assertion failing, NOT `len(warning_records) == 1` and NOT the
`"could not rehome image URI" in message` assertion. If a different assertion failed, the edit was
wrong — restore the file, fix the edit, and re-measure. Do not record a RED you cannot attribute.

**Step 3 — revert and prove it.** Run `git checkout -- typsphinx/builder.py`, then
`git status --porcelain typsphinx/` and `git diff --stat -- typsphinx/`. Both must produce no output.
A dirty `typsphinx/` at commit time is a HALT, not a deviation (D-06).

**Step 4 — re-prove green on the restored tree.** Run the same pytest command again; expected `1
passed`.

**Step 5 — record everything.** Append (append only — never edit a section written by a prior plan) a
heading `## SC#2 (c) — recorded falsification: builder.py:1767 (image-rehome warning)` containing, in
order and each with its command line shown:

- The unified diff of the falsifying edit captured in Step 1.
- The verbatim RED output from Step 2, including the `1 failed` summary line and the `AssertionError`
  block showing the `path_named_in` assertion's own failure message.
- A one-sentence attribution statement naming which assertion failed and why the other two did not.
- The verbatim `git checkout` / `git status --porcelain typsphinx/` / `git diff --stat -- typsphinx/`
  transcript from Step 3, with the empty outputs shown as empty.
- The verbatim `1 passed` output from Step 4.

Then append a short closing sub-heading `### SC#2 — the three recorded runs, both sites` listing, as
a plain table with one row per site, where in this file each of the three runs SC#2 names
(pre-rewrite green, post-rewrite green, recorded RED) is recorded. Write the section in English. Do
not paraphrase any captured output, do not reconstruct it from memory, and do not elide a line
because it looks like noise.

**Step 6 — one final scope observation.** Record, under the same closing sub-heading, the verbatim
output of `git diff --stat -- typsphinx/` and of
`git diff --name-only <phase-base-SHA>..HEAD -- typsphinx/`, using the phase-base SHA plan 58-01
recorded in the `## SC#2 (a)` section of this same file. Both must be empty. This is SC#4's own
evidence, taken from inside the phase rather than only at close.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain typsphinx/)" && test -z "$(git diff --name-only -- typsphinx/)" && uv run pytest tests/test_builder.py::test_post_process_images_rehome_escape_relocates_with_warning -q && grep -q '^## SC#2 (c) — recorded falsification: builder.py:1767' .planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md && grep -q '^### SC#2 — the three recorded runs, both sites' .planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md && grep -q '1 failed' .planning/phases/58-repr-format-decoupling-test-side-only/58-DECOUPLING-EVIDENCE.md</automated>
    <human-check>Read the `## SC#2 (c) — recorded falsification: builder.py:1767` section and the `### SC#2 — the three recorded runs, both sites` table end to end, and confirm three things a command cannot: (1) the pasted RED output is verbatim pytest output and not a reconstruction — it carries real file paths, line numbers and a traceback shape; (2) the failing assertion shown is the naming assertion, not the record-count or the warning-substring assertion; (3) the summary table's six cells each point at a section that actually exists in this file, with no cell claiming a run that was never recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain typsphinx/` produces no output and `git diff --name-only -- typsphinx/` produces no output.
    - `58-DECOUPLING-EVIDENCE.md` contains the heading `## SC#2 (c) — recorded falsification: builder.py:1767 (image-rehome warning)`.
    - That section contains a `diff --git a/typsphinx/builder.py` hunk, the literal `1 failed`, the literal `AssertionError`, the literal `path_named_in`, and the literal `1 passed`.
    - That section contains a `git checkout -- typsphinx/builder.py` line followed by a `git status --porcelain typsphinx/` line whose output is shown as empty.
    - The file contains the sub-heading `### SC#2 — the three recorded runs, both sites` and, under it, a table with exactly two data rows — one per rewritten site — each naming the section holding its pre-rewrite green, its post-rewrite green and its recorded RED.
    - That sub-heading's section contains the output of `git diff --name-only <phase-base-SHA>..HEAD -- typsphinx/` shown as empty, with the SHA it used matching the one recorded in the `## SC#2 (a)` section.
    - `uv run pytest tests/test_builder.py::test_post_process_images_rehome_escape_relocates_with_warning -q` reports `1 passed` on the restored tree with no `skipped` term.
    - All five headings written by plans 58-01 and 58-02 are still present and in their original order — no prior section was edited, reordered or removed.
  </acceptance_criteria>
  <done>The image-rehome test is recorded RED against a real product message with the URI removed and the same-basename relocation key still present, the RED is attributed to the naming assertion specifically, the product file is restored with a recorded empty status, the same test is recorded green again, and SC#2's six required runs are indexed in one table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree working tree → milestone branch | A transient product edit made inside a worktree can cross into a merged commit if the revert is skipped or partially applied. This is the only boundary this phase actively opens. |
| product log record → test assertion | The only "input" the predicate sees on this site: a `caplog` record's `getMessage()` from the product's own logger. No external or user-controlled data reaches it. |
| plan 58-01's evidence sections → plan 58-02's appends | Two plans write one file in different waves; an append that rewrites a prior section would destroy already-recorded, non-re-derivable evidence (the pre-rewrite baseline in particular). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-01 | Tampering | `typsphinx/builder.py` — the D-05(b) temporary edit | high | mitigate | The edit/measure/revert cycle is confined to Task 2. `git checkout -- typsphinx/builder.py` is followed by `git status --porcelain typsphinx/` and `git diff --stat -- typsphinx/`, both recorded empty in `58-DECOUPLING-EVIDENCE.md` before the commit, plus a `git diff --name-only <phase-base>..HEAD -- typsphinx/` taken against the SHA plan 58-01 recorded. Re-asserted in both tasks' `<verify>` and acceptance criteria. A dirty `typsphinx/` at commit time is a HALT. |
| T-58-02 | Repudiation | the rewritten pass criterion | medium | mitigate | D-05's two independently-required routes, now closed on both sites: the durable meta-tests from plan 58-01 (whose four-shape parametrization and fallback-trap `False` case survive after this plan's transient edit is gone) plus this plan's one-time recorded real RED. |
| T-58-05 | Tampering | `58-DECOUPLING-EVIDENCE.md` — prior plans' recorded runs | medium | mitigate | This plan appends only. The SC#2(a) pre-rewrite baseline cannot be re-derived once the tree is rewritten, so an overwrite is unrecoverable. Acceptance asserts all five headings are present in their original order. |
| T-58-SC | Tampering | package-manager installs | low | accept | This phase installs no new package. `uv sync --extra dev` resolves the existing committed `uv.lock` only, and this plan adds no import beyond the leaf module plan 58-01 created. `58-RESEARCH.md` § "Package Legitimacy Audit" records the gate as not applicable: zero packages proposed, zero `[SUS]`, zero `[SLOP]`. |
</threat_model>

<verification>
Run from the worktree root, after `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`:

1. `uv run pytest tests/test_builder.py tests/test_out02_escape_target_gate.py tests/test_path_naming_predicate.py -q` — all pass, zero skipped.
2. The whole-tree AST pass-criterion count prints `7`, and the per-file count prints `0` for both `tests/test_builder.py` and `tests/test_out02_escape_target_gate.py`.
3. `test -z "$(git status --porcelain typsphinx/)"` and `test -z "$(git diff --name-only -- typsphinx/)"`.
4. `uv run black --check .` exits 0. `uv run ruff check .` exits 0, or the documented NixOS exec failure is recorded verbatim with the `nix-shell -p ruff` retry.
5. `58-DECOUPLING-EVIDENCE.md` carries all six headings written so far, in order, and no file named `58-VERIFICATION.md` exists in the phase directory.
</verification>

<success_criteria>
- ROADMAP Phase 58 SC#1 is discharged for both sites MSG-01 names: the per-file AST pass-criterion count is `0` for `tests/test_builder.py` and for `tests/test_out02_escape_target_gate.py`.
- ROADMAP Phase 58 SC#2 is discharged in full: six recorded real runs (pre-rewrite green, post-rewrite green and a recorded RED, for each of the two sites), indexed in one table.
- The whole-tree AST pass-criterion count is exactly `7` and zero of the seven are path-valued — SC#3's measurable half, which plan 58-03 then locks with a guard.
- ROADMAP Phase 58 SC#4 holds across this plan: `typsphinx/` is byte-identical to the phase base at commit time, proven both against the working tree and against the phase-base SHA.
</success_criteria>

<output>
Create `.planning/phases/58-repr-format-decoupling-test-side-only/58-02-SUMMARY.md` when done.
</output>
