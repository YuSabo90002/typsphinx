---
phase: 47-two-layer-output-content-wrapper-split-target-as-path-collis
plan: 08
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - tests/test_template_import_path.py
  - tests/test_package_template_routing.py
  - tests/test_config_template_mapping.py
  - tests/test_params_exclusivity_gate.py
  - tests/test_typst_elements_pass_through_gate.py
  - tests/test_typst_lang_gate.py
  - tests/test_authors_pipeline_stage_gate.py
  - tests/test_entry_metadata_route_uniformity.py
  - tests/test_entry_metadata_precedence.py
  - tests/test_default_typst_documents_gate.py
  - tests/test_empty_typst_documents_optout_gate.py
  - tests/test_nested_master_render_gate.py
  - tests/test_target_name_render_gate.py
  - tests/test_cross_doc_label_namespace_render_gate.py
  - tests/test_multi_master_metadata_no_leak.py
  - tests/test_admonition_locale_title_precedence_gate.py
  - tests/test_pdf_generation.py
  - tests/test_examples_basic.py
  - tests/test_examples_charged_ieee_gate.py
  - tests/test_quickstart_docs_gate.py
  - tests/test_corpus_gate.py
  - tests/test_docs_contract_claims_gate.py
  - tests/test_builder.py
  - tests/test_builder_requirement13.py
  - tests/fixtures/nested_master_render_gate/
  - tests/fixtures/template_named_dir_master/
  - tests/fixtures/cross_doc_label_namespace_render_gate/
  - tests/roots/test-basic/
autonomous: true
requirements: [COMP-01, COMP-02, OUT-03, BLD-02, BLD-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`tests/fixtures/template_named_dir_master/` no longer configures two entries resolving to one target path — that shape is the BLD-02 duplicate-target collision and would fail the build once 47-09 lands."
    - "`tests/fixtures/nested_master_render_gate/` no longer configures a target that resolves onto another docname's content path — that shape is the BLD-03 collision."
    - "The template import path asserted by `tests/test_template_import_path.py` is derived from the WRAPPER's resolved output directory, not from the master docname, so a wrapper written outside its docname's directory imports a `_template.typ` that exists."
    - "The project's own documentation build (`tox -e docs-pdf`) still produces its PDF at the same path it produced before the split, because `docs/source/conf.py` configures a non-colliding target."
    - "The bundled `examples/basic` and `examples/advanced` projects still build and compile, because both configure non-colliding targets."
    - "The full-corpus gate still compiles its master and its assertion still names the same PDF path."
  artifacts:
    - tests/test_template_import_path.py
    - tests/test_nested_master_render_gate.py
    - tests/test_corpus_gate.py
  key_links:
    - "wrapper resolved directory -> `_template.typ` import depth: this is the second load-bearing path (after the include path) that must not be computed from the docname."
    - "`docs/source/conf.py` target `typsphinx` -> `tox -e docs-pdf` output path: dogfooding must not silently change the published PDF filename."
  prohibitions:
    - "A collision is never resolved by inventing a filename the user did not write."
    - "A written output filename is never case-folded, Unicode-normalized or transliterated; folding is comparison-only."
    - "The fifth typst_documents tuple element is never read or repurposed in this phase."
---

<objective>
Migrate the residual surface — the modules and fixtures that read the pre-split output shape without
going through a self-colliding render-gate fixture — and re-prove the project's own dogfooding
builds.

Purpose: groups A through D cover the 87 self-colliding fixtures and the 68 modules that consume
them. A further 18 modules read `index.typ` or `index.pdf` through other routes: the template-routing
and config-mapping unit suites, the `typst_documents`-shape gates, the two dogfooding example
projects, the quickstart-docs gate, the full-corpus gate, and the shared `tests/roots/test-basic`
project. Two residual fixtures also carry collisions of their own kind that groups A-D never see:
`template_named_dir_master` configures TWO entries resolving to one target (the BLD-02 shape), and
`nested_master_render_gate` targets `index` from docname `api/index` while a real `index` docname
exists (the BLD-03 shape).

Output: the residual surface green against the post-split emitter, both dogfooding builds re-proved,
and the two residual collisions removed.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-CONTEXT.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-EXPECTED-STRUCTURE.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `47-01-PLAN.md` section "Artifacts this phase produces". This plan creates no new symbols.

## Migration rules (authority: `47-EXPECTED-STRUCTURE.md` `## Corpus migration rules`)

| Rule | Assertion class | Reads today | Reads after the split | Why |
|------|-----------------|-------------|------------------------|-----|
| R1 | translator body markup, marker strings, code-mode leakage | `index.typ` | `index.typ` (content) — **UNCHANGED** | the translated body is emitted into the content file |
| R2 | template application: a `_template.typ` import, a `#show:` application, title/author parameters, the outline call | `index.typ` | `<target-stem>.typ` (wrapper) | the template application moved to the wrapper |
| R3 | `typst.compile()` of a complete document | `index.typ` | `<target-stem>.typ` (wrapper) | only the wrapper is a complete document |
| R4 | a `-b typstpdf` produced PDF | `index.pdf` | `<target-stem>.pdf` | `typstpdf` compiles wrappers only |
| R5 | toctree `include()` emission | `index.typ` | `index.typ` (content) — **UNCHANGED** | toctrees are emitted where the doctree is translated |

**Do not touch** the four CR-01 collision fixtures (`derived_docname_collision_gate`,
`derived_template_collision_gate`, `explicit_docname_collision_gate`,
`explicit_template_collision_gate`), `tests/test_typst_documents_collision_gate.py` or
`tests/test_builder_output_stem.py` — those belong to plans 47-03 and 47-09.

<tasks>

<task type="auto">
  <name>Task 1: Migrate the template-routing, config-mapping and metadata-route suites (9 modules)</name>
  <files>
    tests/test_template_import_path.py, tests/test_package_template_routing.py,
    tests/test_config_template_mapping.py, tests/test_params_exclusivity_gate.py,
    tests/test_typst_elements_pass_through_gate.py, tests/test_typst_lang_gate.py,
    tests/test_authors_pipeline_stage_gate.py, tests/test_entry_metadata_route_uniformity.py,
    tests/test_entry_metadata_precedence.py, tests/fixtures/template_named_dir_master/conf.py
  </files>
  <read_first>
    - tests/test_template_import_path.py in full — it unit-tests the docname-based depth counter directly
    - typsphinx/writer.py as written by 47-02: `compute_template_import_path_for_dir`, the surviving `_compute_template_import_path`, and `render_wrapper`
    - tests/test_entry_metadata_precedence.py in full — it unit-tests the element-resolution helper whose docname first-match behaviour D-08 bypasses for wrappers
    - tests/fixtures/template_named_dir_master/conf.py — two entries, both targeting `index`, in a project whose docnames include `index`
    - .planning/phases/47-.../47-CONTEXT.md decisions D-04, D-08, D-09
  </read_first>
  <action>
Provision the worktree first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`; run
everything through `uv run`.

`tests/test_template_import_path.py` is the module most directly invalidated by the split. Its
parametrized matrix asserts the depth counter over DOCNAMES; after the split the same computation
must run over the WRAPPER's resolved output directory, because that is the file that carries the
import. Rewrite the matrix over wrapper directories: an empty directory yields `_template.typ`, a
one-segment directory yields `../_template.typ`, a two-segment directory yields `../../_template.typ`,
and a directory literally named `_template` yields `../_template.typ` — the original CR-01 case,
which must keep working because the computation still has no string-equality dependence on the
reserved basename. Keep the module's existing docstring explanation of the CR-01 defect and extend it
to state why the input changed from a docname to a wrapper directory. If 47-02 left the docname-based
function in place for another caller, keep a reduced set of its own tests; if it has no caller left,
delete the function and its tests together.

`tests/fixtures/template_named_dir_master/conf.py` configures TWO entries both targeting `index`,
which is simultaneously the BLD-02 duplicate-target shape and a collision with the real `index`
docname's content file. Give each entry a distinct, non-colliding target that preserves the fixture's
purpose — its purpose is a master whose DIRECTORY is literally named `_template`, so keep the
docnames and choose targets such as `template-dir-master.typ` and `template-dir-sub.typ`. Record in
that `conf.py` comment that the two distinct targets are load-bearing because two entries resolving
to one path is a build error under D-02/D-03.

For the remaining seven modules: `test_package_template_routing.py`, `test_config_template_mapping.py`,
`test_params_exclusivity_gate.py`, `test_typst_elements_pass_through_gate.py`, `test_typst_lang_gate.py`
and `test_authors_pipeline_stage_gate.py` all assert what reaches the template. The template is now
applied only in the wrapper, so every such assertion is R2 and moves to the wrapper; the asserted
parameter VALUES do not change. `test_entry_metadata_route_uniformity.py` and
`test_entry_metadata_precedence.py` unit-test the element-resolution helper: D-08 forbids USING its
docname first-match behaviour for wrappers but does not forbid the helper's existence, so keep the
helper's own unit tests if 47-02 kept the helper, and delete them together with the helper if it left
no caller. Add a unit test asserting the positional read `render_wrapper` performs: for two entries
naming the same docname, each resolves its own `[2]` and `[3]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_template_import_path.py tests/test_package_template_routing.py tests/test_config_template_mapping.py tests/test_params_exclusivity_gate.py tests/test_typst_elements_pass_through_gate.py tests/test_typst_lang_gate.py tests/test_authors_pipeline_stage_gate.py tests/test_entry_metadata_route_uniformity.py tests/test_entry_metadata_precedence.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0.
    - `tests/test_template_import_path.py` contains a case for a wrapper directory literally named `_template` asserting `../_template.typ`.
    - `tests/fixtures/template_named_dir_master/conf.py` has two entries with two DIFFERENT targets, neither equal to any docname's content path nor to `_template`.
    - A unit test exists asserting that two entries with the same docname and different `[2]` values resolve to two different wrapper titles.
    - No template parameter value expectation changed: `git diff tests/test_params_exclusivity_gate.py tests/test_typst_elements_pass_through_gate.py` shows path changes only.
    - `git diff --stat typsphinx/` is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">Test and fixture edits only.</reversibility>
  <done>The template-routing and metadata suites assert against the wrapper, the template import depth is computed from the wrapper directory, and the duplicate-target fixture is de-collided.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the typst_documents-shape gates and the nested-master fixture (8 modules)</name>
  <files>
    tests/test_default_typst_documents_gate.py, tests/test_empty_typst_documents_optout_gate.py,
    tests/test_nested_master_render_gate.py, tests/test_target_name_render_gate.py,
    tests/test_cross_doc_label_namespace_render_gate.py, tests/test_multi_master_metadata_no_leak.py,
    tests/test_admonition_locale_title_precedence_gate.py, tests/test_pdf_generation.py,
    tests/fixtures/nested_master_render_gate/conf.py, tests/fixtures/cross_doc_label_namespace_render_gate/conf.py
  </files>
  <read_first>
    - tests/fixtures/nested_master_render_gate/conf.py and its `.rst` files — the entry `("api/index", "index", ...)` targets `index` while a real `index` docname exists, which is the BLD-03 collision shape
    - tests/test_default_typst_documents_gate.py and typsphinx/builder.py `_default_typst_documents` — the derived default's target is `make_filename_from_project(project) + ".typ"`, which must be checked against every docname in each derived-default fixture
    - tests/test_empty_typst_documents_optout_gate.py — an explicit `typst_documents = []` now means content files for every docname and zero wrappers
    - tests/test_multi_master_metadata_no_leak.py — the multi-master metadata isolation this phase's per-entry wrappers make structural
    - .planning/phases/47-.../47-CONTEXT.md decision D-09 (the fifth element stays accepted and ignored)
  </read_first>
  <action>
Fix the two residual collisions, then migrate the eight modules against R1-R5.

`tests/fixtures/nested_master_render_gate/conf.py` targets `index` from docname `api/index` while a
real `index` docname exists in the same project — after the split that wrapper lands exactly on the
`index` docname's content file, which is the BLD-03 error. Change the target to a distinct name such
as `nested-master.typ` and record in the `conf.py` comment that the nested docname with a
directory-bearing target is the load-bearing part, not the target's basename. Then re-derive the
module's expected paths from the new target: the wrapper at the output root and the content at
`api/index.typ`, with the wrapper's include being `api/index.typ` and its template import being
`_template.typ`.

`tests/test_default_typst_documents_gate.py` exercises the CONF-08 derived default. For each fixture
that relies on the derived default, compute `make_filename_from_project(project)` and check it
against every docname in that fixture; if it collides, change the fixture's `project` value rather
than adding an explicit `typst_documents`, because the point of the fixture is that no explicit
setting exists. Record the reason in the fixture comment.

`tests/test_empty_typst_documents_optout_gate.py` must now assert the post-split meaning of an
explicit empty list: a content file for every docname, zero wrapper files, no `.pdf` under
`-b typstpdf`, and the existing "explicitly set to an empty list" warning unchanged.

`tests/test_multi_master_metadata_no_leak.py` asserts that one master's metadata does not leak into
another's. After the split each wrapper reads its own entry positionally (D-08), which makes the
isolation structural rather than incidental; keep every expected value and add one assertion that two
wrappers built from two entries carry two different titles.

`tests/test_target_name_render_gate.py` is the module whose whole subject is the target name. Extend
it with an OUT-01 case: a target with a directory component writes the wrapper at that path under
`outdir`, with the content still at its docname. Derive the expected paths from the config, not from
a build.

For `tests/test_cross_doc_label_namespace_render_gate.py`, the fixture already uses a non-colliding
target (`namespace-gate`); leave it and migrate only per R1-R5. Its whole purpose is that labels are
namespaced by SOURCE docname and not by output filename, so no label expectation may change.
  </action>
  <verify>
    <automated>uv run pytest tests/test_default_typst_documents_gate.py tests/test_empty_typst_documents_optout_gate.py tests/test_nested_master_render_gate.py tests/test_target_name_render_gate.py tests/test_cross_doc_label_namespace_render_gate.py tests/test_multi_master_metadata_no_leak.py tests/test_admonition_locale_title_precedence_gate.py tests/test_pdf_generation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0.
    - `tests/fixtures/nested_master_render_gate/conf.py` no longer configures a target whose resolved stem equals any docname in that fixture.
    - `tests/test_empty_typst_documents_optout_gate.py` asserts zero wrapper files and one content file per docname for an explicit empty list.
    - `tests/test_target_name_render_gate.py` contains an OUT-01 case with a directory-bearing target and its expected paths carry a docstring stating they were derived from `conf.py`.
    - `git diff tests/test_cross_doc_label_namespace_render_gate.py` shows no label expectation change.
    - `git diff --stat typsphinx/` is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">Test and fixture edits only.</reversibility>
  <done>The `typst_documents`-shape gates assert the post-split meaning of every entry shape and both residual collisions are removed.</done>
</task>

<task type="auto">
  <name>Task 3: Re-prove the dogfooding builds, the corpus gate and the shared test root, then sweep for anything unenumerated</name>
  <files>
    tests/test_examples_basic.py, tests/test_examples_charged_ieee_gate.py, tests/test_quickstart_docs_gate.py,
    tests/test_corpus_gate.py, tests/test_docs_contract_claims_gate.py, tests/test_builder.py,
    tests/test_builder_requirement13.py, tests/roots/test-basic/conf.py
  </files>
  <read_first>
    - tests/roots/test-basic/conf.py and the `rootdir`/`app` fixtures in tests/conftest.py — this project is consumed through `sphinx.testing.fixtures`, not through a subprocess, so its failure mode differs from every render gate
    - tests/test_corpus_gate.py around line 287-345 (it compiles a master and asserts `sphinx-corpus.pdf`)
    - examples/basic/conf.py and examples/advanced/conf.py (targets `basic-example.typ` and `advanced-example.typ` — both already non-colliding)
    - docs/source/conf.py line 72-74 (target `typsphinx`, already non-colliding) and tox.ini `[testenv:docs-pdf]`
    - tests/test_docs_contract_claims_gate.py — it asserts claims the published documentation makes; OUT-01 changes one of those claims, but the documentation itself is Phase 51's scope
  </read_first>
  <action>
Migrate the remaining modules and then sweep.

`tests/roots/test-basic/conf.py` is the only project under `tests/roots/`. Apply the de-collision
rule to it, and update `tests/test_builder.py` and `tests/test_builder_requirement13.py` against
R1-R5. These two go through `sphinx.testing.fixtures` rather than a subprocess, so any hand-built
doctree or mock builder they use must still work with the shared write path 47-02 introduced.

`tests/test_corpus_gate.py` compiles the full Sphinx `doc/` corpus master and asserts
`sphinx-corpus.pdf`. Its `typst_documents` target already differs from any docname, so the compiled
path is unchanged; verify that and repoint anything that reads the master `.typ` per R1/R2.

`tests/test_examples_basic.py` and `tests/test_examples_charged_ieee_gate.py` build the bundled
`examples/` projects. Both example `conf.py` files configure non-colliding targets
(`basic-example.typ`, `advanced-example.typ`), so no example configuration changes; migrate only the
assertions per R1-R5. Do NOT modify `examples/*/conf.py` unless a collision is actually computed.

`tests/test_docs_contract_claims_gate.py` asserts that published documentation claims match the code.
OUT-01 falsifies one published claim — `docs/source/user_guide/configuration.rst` states a path
component is not supported in a target. Rewriting the documentation is Phase 51's requirement DOC-14
and is OUT of scope here. If this gate asserts that specific claim, mark the assertion
`pytest.mark.xfail(strict=True, reason="OUT-01 falsified this published claim; DOC-14 in Phase 51
rewrites the documentation")` and file a note in the plan summary so Phase 51 inherits it. Do not
edit `docs/` in this phase.

Then run the FULL suite and fix whatever the four group plans and the two preceding tasks did not
reach. Bucket every remaining failure by rule class before editing, and record the bucket counts in
the plan summary. Finally run both dogfooding builds — `tox -e docs-html` and `tox -e docs-pdf` — and
confirm the PDF is produced at the same path as before the split, since `docs/source/conf.py`'s
target is `typsphinx` and that name is unchanged.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exits 0 except for the tests plans 47-03 and 47-09 own (the two CR-01 stem tests, the four CR-01 collision-gate fixtures' module, and the strict-xfail collision-validator module).
    - `uv run tox -e docs-pdf` exits 0 and `docs/_build/pdf/typsphinx.pdf` exists.
    - `uv run tox -e docs-html` exits 0.
    - `git diff --stat examples/` is empty unless a real collision was computed, in which case the plan summary names the fixture and the computed collision.
    - `git diff --stat docs/` is empty — documentation changes belong to Phase 51.
    - `git diff --stat typsphinx/` is empty.
    - The plan summary records the residual-failure bucket counts by rule class.
  </acceptance_criteria>
  <reversibility rating="reversible">Test, fixture and test-root edits only.</reversibility>
  <done>The full suite is green apart from the tests 47-03 and 47-09 own, both dogfooding builds pass, and the published PDF path is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture / example / docs `conf.py` -> builder config | the residual surface includes the project's OWN documentation config, which is the closest thing in-repo to a real user's `conf.py` |
| wrapper resolved directory -> `_template.typ` import | a second path that must not be derived from the docname |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-03 | Denial of Service (silent content loss) | `template_named_dir_master`'s two entries resolving to one target | medium | mitigate | Task 1 gives each entry a distinct target and records why in the fixture comment |
| T-47-02 | Tampering | `nested_master_render_gate`'s target landing on another docname's content path | high | mitigate | Task 2 changes the target to a distinct name and re-derives the module's expected paths |
| T-47-06 | Tampering | `_template.typ` import path computed from the docname instead of the wrapper directory | medium | mitigate | Task 1 rewrites the parametrized matrix over wrapper directories, including the `_template`-named-directory case |
| T-47-13 | Repudiation | a published documentation claim silently falsified by OUT-01 | medium | mitigate | Task 3 marks the affected contract assertion strict-xfail with a reason naming DOC-14 and files a note so Phase 51 inherits it, rather than deleting the assertion |
| T-47-05 | Information Disclosure | build-time file generation | low | accept | No network or data-egress surface |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 apart from the tests plans 47-03 and 47-09 own.
- `uv run tox -e docs-html` and `uv run tox -e docs-pdf` both exit 0.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` exit 0.
- `git diff --stat typsphinx/ docs/` is empty.
</verification>

<success_criteria>
- The residual 18 modules are green against the post-split emitter.
- Both residual collisions (`template_named_dir_master`, `nested_master_render_gate`) are removed.
- The template import depth is proven computed from the wrapper directory.
- Both dogfooding builds pass and the published PDF path is unchanged.
</success_criteria>

<output>
Create `.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-08-SUMMARY.md` when done
</output>
