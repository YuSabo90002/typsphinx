---
phase: 47-two-layer-output-content-wrapper-split-target-as-path-collis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-EXPECTED-STRUCTURE.md
  - .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-RED-EVIDENCE.md
  - tests/fixtures/two_layer_root_master_gate/
  - tests/fixtures/two_layer_nested_master_gate/
  - tests/fixtures/bld02_duplicate_target_gate/
  - tests/fixtures/bld03_self_collision_gate/
  - tests/fixtures/bld04_case_collision_gate/
  - tests/test_two_layer_output_gate.py
  - tests/test_collision_validator_gate.py
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, OUT-03, BLD-02, BLD-03, BLD-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "47-EXPECTED-STRUCTURE.md exists and states, for every new fixture, the exact wrapper path, the exact content path, and the exact wrapper-to-content #include() string, each derived from the fixture's conf.py and .rst sources read literally — written and committed BEFORE any emitter code exists (binding constraint #6)."
    - "47-RED-EVIDENCE.md records, per requirement, the verbatim pre-fix failure captured against the unfixed tree: COMP-03's TypstError text, COMP-04's pypdf page-text sequence, and the BLD-02/BLD-03/BLD-04 structural observations (binding constraint #4)."
    - "COMP-03's recorded RED is a classic TypstError containing 'file not found', and COMP-04's recorded RED is a structural pypdf-text assertion and NOT a TypstError — the shape research measured, not the shape assumed."
    - "Every new gate test that cannot pass until a later plan lands is marked pytest.mark.xfail(strict=True) with a reason naming the plan that flips it, so `pytest` exits 0 on the unfixed tree while still proving RED (an accidental early pass becomes XPASS, a failure)."
    - "The wrapper-to-content include path is a pure function of (wrapper resolved directory, content path) and is asserted as such at the unit level, independent of the order documents were written (COMP-03, edge/concurrency)."
  artifacts:
    - .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-EXPECTED-STRUCTURE.md
    - .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-RED-EVIDENCE.md
    - tests/test_two_layer_output_gate.py
    - tests/test_collision_validator_gate.py
    - tests/fixtures/two_layer_root_master_gate/conf.py
    - tests/fixtures/two_layer_nested_master_gate/conf.py
    - tests/fixtures/bld02_duplicate_target_gate/conf.py
    - tests/fixtures/bld03_self_collision_gate/conf.py
    - tests/fixtures/bld04_case_collision_gate/conf.py
  key_links:
    - "47-EXPECTED-STRUCTURE.md -> tests/test_two_layer_output_gate.py: every asserted path in the gate module is traceable to a row in the expected-structure table, never to emitter output."
    - "tests/fixtures/two_layer_nested_master_gate -> COMP-03: the nested master's target basename differs from its docname, which is the only shape that proves include paths come from the resolved wrapper location."
    - "tests/fixtures/bld04_case_collision_gate -> BLD-04: the case-varied target Manual.typ against docname manual is the fixture SC#4 names."
  prohibitions:
    - "A collision is never resolved by inventing a filename the user did not write."
    - "A written output filename is never case-folded, Unicode-normalized or transliterated; folding is comparison-only."
    - "The fifth typst_documents tuple element is never read or repurposed in this phase."
---

<objective>
Write down, from first principles and BEFORE any emitter code exists, exactly what the two-layer
output must look like — then capture the pre-fix RED for all five defects this phase closes, against
the unfixed tree.

Purpose: Binding constraint #6 forbids laundered gates — the expected wrapper/content structure must
be an INPUT to the tests, derived from each fixture's `conf.py` plus its `.rst` toctree read
literally, not copy-pasted from the new emitter's output. Binding constraint #4 requires each closed
defect to ship a pre-fix RED recorded against the unfixed tree, and research has already MEASURED the
correct RED shape for each: a classic `TypstError` for COMP-03 (B-1), and a structural `pypdf`/
emitted-`.typ` assertion for COMP-04 (B-2), BLD-02, BLD-03 and BLD-04, all of which compile fine
today and merely produce wrong output.

Output: five new fixture projects, two new gate modules, `47-EXPECTED-STRUCTURE.md` (the
first-principles derivation) and `47-RED-EVIDENCE.md` (the captured pre-fix failures). No production
code is touched by this plan.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-CONTEXT.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-RESEARCH.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-PATTERNS.md
@.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files and identifiers created by Phase 47 (new — not drift):

**New production symbols (created in 47-02 / 47-03 / 47-09):**
- `TypstWriter.render_content()` — emits a content file (D-06 preamble + body, no template)
- `TypstWriter.render_wrapper()` — emits a wrapper file (template application + `#include()`)
- `compute_content_include_path()` — module-level function in `typsphinx/writer.py`
- `compute_template_import_path_for_dir()` — module-level function in `typsphinx/writer.py`
- `TypstBuilder._content_output_path()` — docname to content path
- `TypstBuilder._wrapper_output_relpath()` — entry to outdir-relative wrapper path
- `TypstBuilder._write_typst_files()` — the ONE shared write path both builders call
- `TypstBuilder._validate_output_path_collisions()` — the D-01..D-05 unified pre-write validator
- `TypstBuilder._collision_key()` — the `casefold()`-normalizing comparison helper
- `_escapes_outdir()` — module-level OUT-02 guard predicate in `typsphinx/builder.py`

**New error/warning message identifiers:**
- `typst: N output path collision(s)` — the single `ExtensionError` summary prefix (D-02)
- `wrapper target ... resolves onto the content file of docname ...` — BLD-03 / D-01 kind
- `two typst_documents entries resolve to the same output path` — BLD-02 kind
- `wrapper target ... resolves onto the reserved _template.typ` — reserved-file kind

**New test modules:** `tests/test_two_layer_output_gate.py`, `tests/test_collision_validator_gate.py`

**New fixture projects:** `tests/fixtures/two_layer_root_master_gate/`,
`tests/fixtures/two_layer_nested_master_gate/`, `tests/fixtures/bld02_duplicate_target_gate/`,
`tests/fixtures/bld03_self_collision_gate/`, `tests/fixtures/bld04_case_collision_gate/`,
`tests/fixtures/out02_escape_target_gate/`

**New planning artifacts:** `47-EXPECTED-STRUCTURE.md` (including its `## Corpus migration rules`
section), `47-RED-EVIDENCE.md`, `47-CI-EVIDENCE.md`, `COVERAGE.md`

**Removed symbols:** `TypstWriter._is_master_document` (success criterion 1 — verified by repo-wide grep)

## Edge Coverage Ledger (spec-less probe fallback, §C)

All 18 probe rows are accounted for; none is silently dropped.

| # | Req | Category | Disposition | Where |
|---|-----|----------|-------------|-------|
| 1 | COMP-01 | adjacency | resolved (explicit) | 47-09 `must_haves.truths` |
| 2 | COMP-01 | empty | resolved (explicit) | 47-02 `must_haves.truths` |
| 3 | COMP-01 | ordering | resolved (explicit) | 47-02 `must_haves.truths` |
| 4 | COMP-02 | adjacency | resolved (explicit) | 47-09 `must_haves.truths` |
| 5 | COMP-02 | empty | resolved (explicit) | 47-02 `must_haves.truths` |
| 6 | COMP-02 | ordering | resolved (explicit) | 47-02 `must_haves.truths` |
| 7 | COMP-02 | concurrency | resolved (backstop) | 47-02 `must_haves.truths` (structured marker) |
| 8 | COMP-03 | concurrency | resolved (explicit) | 47-01 `must_haves.truths` (this plan) |
| 9 | COMP-04 | unclassified | **unresolved — FLAGGED** | see Flagged Planner Assumptions below |
| 10 | OUT-01 | empty | resolved (explicit) | 47-03 `must_haves.truths` |
| 11 | OUT-01 | encoding | resolved (explicit) | 47-03 `must_haves.truths` (D-05) |
| 12 | OUT-02 | unclassified | **unresolved — FLAGGED** | see Flagged Planner Assumptions below |
| 13 | OUT-03 | empty | resolved (explicit) | 47-02 `must_haves.truths` |
| 14 | OUT-03 | encoding | resolved (explicit) | 47-02 `must_haves.truths` |
| 15 | OUT-03 | concurrency | resolved (explicit) | 47-02 `must_haves.truths` |
| 16 | BLD-02 | unclassified | **unresolved — FLAGGED** | see Flagged Planner Assumptions below |
| 17 | BLD-03 | concurrency | resolved (explicit) | 47-09 `must_haves.truths` |
| 18 | BLD-04 | unclassified | **unresolved — FLAGGED** | see Flagged Planner Assumptions below |

### Flagged Planner Assumptions (4 unclassified rows, never auto-resolved)

- **COMP-04 (unclassified).** The probe could not classify what edge B-2's closure has. The planner's
  assumption: the only edge is "an included document contributes zero template applications", and it
  is verified by the `pypdf` structural assertion in `tests/test_two_layer_output_gate.py`. Not
  auto-resolved — a reviewer should confirm no second edge (e.g. an included document that is itself
  a wrapper) exists.
- **OUT-02 (unclassified).** The planner's assumption: OUT-02's edge space is exactly the three
  escape shapes already enumerated (`..`, absolute, drive-qualified) and no fourth shape exists.
  RESEARCH.md's Assumptions-Log A3 records that a repo-wide grep for a second path-rejection site was
  NOT exhaustively run — 47-03 task 1 runs that grep and will surface a fourth site if one exists.
- **BLD-02 (unclassified).** The planner's assumption: the duplicate-target edge is symmetric (order
  of the two entries does not change the outcome) and both entries are named in the single error.
- **BLD-04 (unclassified).** The planner's assumption: `casefold()` alone is the correct equality,
  and Unicode NFC/NFD divergence is deliberately OUT of scope per D-05. Linux CI structurally cannot
  observe the physical consequence; the Windows/macOS lanes in 47-10 are the only observation point.

<tasks>

<task type="auto">
  <name>Task 1: Derive the expected two-layer structure from first principles and build the five fixture projects</name>
  <files>
    .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-EXPECTED-STRUCTURE.md,
    tests/fixtures/two_layer_root_master_gate/conf.py,
    tests/fixtures/two_layer_root_master_gate/index.rst,
    tests/fixtures/two_layer_nested_master_gate/conf.py,
    tests/fixtures/two_layer_nested_master_gate/index.rst,
    tests/fixtures/two_layer_nested_master_gate/guide/index.rst,
    tests/fixtures/bld02_duplicate_target_gate/conf.py,
    tests/fixtures/bld02_duplicate_target_gate/index.rst,
    tests/fixtures/bld02_duplicate_target_gate/other.rst,
    tests/fixtures/bld03_self_collision_gate/conf.py,
    tests/fixtures/bld03_self_collision_gate/index.rst,
    tests/fixtures/bld04_case_collision_gate/conf.py,
    tests/fixtures/bld04_case_collision_gate/index.rst,
    tests/fixtures/bld04_case_collision_gate/manual.rst
  </files>
  <read_first>
    - tests/fixtures/derived_docname_collision_gate/conf.py (the load-bearing-facts conf.py comment convention to copy verbatim)
    - tests/fixtures/integration_nested_toctree/conf.py and its .rst files (the nested-toctree fixture shape)
    - .planning/phases/47-.../47-PATTERNS.md section "Fixture Convention (tests/fixtures/ vs. tests/roots/)"
    - .planning/phases/47-.../47-RESEARCH.md sections "Architecture Patterns" (Pattern 1, Pattern 2) and "Common Pitfalls" 1-3
    - .planning/phases/47-.../47-CONTEXT.md sections "Specific Ideas" and "Implementation Decisions"
    - typsphinx/builder.py lines 28-47 (_default_typst_documents, the five-element entry shape)
  </read_first>
  <action>
Provision the worktree first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then
run every command through `uv run`.

Create five minimal Sphinx fixture projects under `tests/fixtures/`, each with a `conf.py` carrying a
load-bearing-facts comment block in the exact style of
`tests/fixtures/derived_docname_collision_gate/conf.py` (enumerate which config values are
load-bearing and why renaming them breaks the fixture). Every `.rst` body carries a unique
all-caps marker string so a missing body is provable by a `grep -c` on the emitted file.

1. `two_layer_root_master_gate` — one docname `index`; `typst_documents = [("index", "manual.typ",
   "Root Master Gate", "Probe Author")]`; body marker `ROOT-BODY-MARKER-AAA`. This is the plan-02
   tracer fixture: a bare target at the output root whose stem differs from the docname.
2. `two_layer_nested_master_gate` — docnames `index` and `guide/index`; `index.rst` carries a
   `toctree` listing `guide/index` and body marker `OUTER-PROSE-MARKER`; `guide/index.rst` carries
   body marker `GUIDE-BODY-MARKER`. `typst_documents = [("index", "outer.typ", "Outer Master",
   "Probe Author"), ("guide/index", "manuals/guide.typ", "Nested Master", "Probe Author")]`. Per
   47-CONTEXT.md "Specific Ideas", the nested master's target basename (`guide`) MUST differ from
   its docname's basename (`index`) and MUST sit in a directory (`manuals/`) unrelated to the
   docname's own directory (`guide/`) — this is the only shape that proves include paths are
   computed from the wrapper's RESOLVED location. This fixture carries both COMP-03 (B-1) and
   COMP-04 (B-2).
3. `bld02_duplicate_target_gate` — docnames `index` and `other`; two entries both targeting
   `manual.typ`; markers `INDEX-MASTER-MARKER-AAA` and `OTHER-MASTER-MARKER-BBB`.
4. `bld03_self_collision_gate` — docname `index`; `typst_documents = [("index", "index.typ", ...)]`
   — the exact configuration D-01 names as the one that builds in v0.7.x and stops building.
5. `bld04_case_collision_gate` — docnames `index` and `manual`; entries `("index", "index-wrapper.typ",
   ...)` and `("manual", "Manual.typ", ...)`. The capital `M` against docname `manual` is the fixture
   success criterion 4 names.

Then write `47-EXPECTED-STRUCTURE.md`. For each of the five fixtures, derive from the `conf.py` list
and the `.rst` toctree read literally — NOT by running any builder — a table with one row per emitted
file, giving: logical role (content or wrapper), the outdir-relative path, whether the file carries a
template application, and (for wrappers) the exact `#include()` argument string. Show the derivation
arithmetic for the nested case explicitly: wrapper `manuals/guide.typ` sits in directory `manuals`,
content sits at `guide/index.typ`, so the include argument is
`posixpath.relpath("guide/index.typ", start="manuals")` = `../guide/index.typ`; and the template
import from that same wrapper directory is `../_template.typ`. Add a second table stating what each
fixture's CURRENT (unfixed) output is, so the RED and the GREEN are both written down before either
is observed. Add a "Reversal notice" paragraph stating in plain terms that OUT-01 deliberately
reverses Phase 44's D-05/D-06/D-07 and that the existing `_resolve_output_stem` guard code is
therefore not sacred (per ROADMAP Phase 47's own instruction).

Finally add a `## Corpus migration rules` section to the same document — the authority plans 47-04
through 47-08 migrate the existing 87 fixture projects and 68 test modules against. It must contain
exactly two things, both derived from the decisions and not from any emitter output. First, a
five-row rule table mapping each assertion class to where it reads after the split: R1 translator
body markup stays on the docname-named content file; R2 template application (a template import, a
`#show:` application, title or author parameters, the outline call) moves to the wrapper; R3 a
`typst.compile()` of a complete document targets the wrapper; R4 a `-b typstpdf` produced PDF is the
wrapper's `.pdf`; R5 a toctree `include()` emission stays on the content file. Second, the fixture
de-collision rule: any entry whose resolved target path `casefold()`-equals a docname's own content
path, and any two entries resolving to one target, must have element `[1]` changed to a distinct
name — the canonical replacement is `"master.typ"` unless the fixture's own purpose names its target,
in which case a purpose-specific distinct name is chosen and the reason is written into that
fixture's `conf.py` comment. Elements `[0]`, `[2]`, `[3]` and `[4]` are never changed.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; d=pathlib.Path('tests/fixtures'); missing=[n for n in ['two_layer_root_master_gate','two_layer_nested_master_gate','bld02_duplicate_target_gate','bld03_self_collision_gate','bld04_case_collision_gate'] if not (d/n/'conf.py').exists()]; sys.exit(1 if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/fixtures/two_layer_root_master_gate/conf.py tests/fixtures/two_layer_nested_master_gate/guide/index.rst tests/fixtures/bld02_duplicate_target_gate/conf.py tests/fixtures/bld03_self_collision_gate/conf.py tests/fixtures/bld04_case_collision_gate/manual.rst` exits 0.
    - `grep -c 'manuals/guide.typ' tests/fixtures/two_layer_nested_master_gate/conf.py` returns at least 1.
    - `grep -c 'Manual.typ' tests/fixtures/bld04_case_collision_gate/conf.py` returns at least 1.
    - `47-EXPECTED-STRUCTURE.md` contains the literal string `../guide/index.typ` and the literal string `../_template.typ` in the nested-master derivation table.
    - `47-EXPECTED-STRUCTURE.md` contains a "Reversal notice" heading naming Phase 44's D-05, D-06 and D-07.
    - `47-EXPECTED-STRUCTURE.md` contains a `## Corpus migration rules` section with the five-row rule table R1-R5 and the fixture de-collision rule, both stated before any corpus file is touched.
    - No file under `typsphinx/` is modified by this task: `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>Five fixture projects exist and 47-EXPECTED-STRUCTURE.md states every expected path and include string, derived from config and source only, with no builder run.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_two_layer_output_gate.py and record its RED against the unfixed tree</name>
  <files>
    tests/test_two_layer_output_gate.py,
    .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-RED-EVIDENCE.md
  </files>
  <read_first>
    - .planning/phases/47-.../47-EXPECTED-STRUCTURE.md (written by task 1 — the only source of expected paths)
    - tests/test_pdf_render_gate.py (the class-scoped sphinx-build to typst.compile to pypdf.extract_text fixture pattern, and the availability-guard block)
    - tests/test_typst_documents_collision_gate.py lines 18-72 (the `_run_sphinx_build` helper this repo copies per-module rather than importing)
    - .planning/phases/47-.../47-RESEARCH.md "Common Pitfalls" 1 and 2 (the measured RED shapes)
    - .planning/phases/47-.../47-VALIDATION.md "Requirement to evidence contract"
  </read_first>
  <action>
Create `tests/test_two_layer_output_gate.py` covering COMP-01, COMP-02, COMP-03, COMP-04 and OUT-03.
Copy the `_run_sphinx_build` helper verbatim into this module (this repo's convention is per-module
duplication, never a cross-module import) and copy the `TYPST_AVAILABLE` / `PYPDF_AVAILABLE`
availability-guard blocks, applying `@pytest.mark.skipif` at class level.

Every asserted path string must be copied from `47-EXPECTED-STRUCTURE.md`, and each test's docstring
must name the row of that table it came from. Do not run the builder to discover a path.

Tests to write:
- COMP-01: build `two_layer_root_master_gate` with `-b typst`; assert `index.typ` exists and its text
  contains neither `#show: project.with(` nor `_template.typ`, and DOES contain the four `@preview`
  import lines and `ROOT-BODY-MARKER-AAA`.
- COMP-02: same build; assert `manual.typ` exists, contains `_template.typ`, and contains an
  `#include(` naming `index.typ`.
- OUT-03: build `two_layer_nested_master_gate`; assert the content files are at `index.typ` and
  `guide/index.typ` — docname-derived — while the wrappers are at `outer.typ` and
  `manuals/guide.typ`.
- COMP-03 (B-1): build `two_layer_nested_master_gate` with `-b typst`, then `typst.compile()` the
  `outer.typ` wrapper with `root=` the build dir. Assert the compile SUCCEEDS. RESEARCH.md measured
  the pre-fix failure as `TypstError` whose text contains `file not found` and
  `guide/index.typ`; assert that substring is absent from any raised error.
- COMP-04 (B-2): class-scoped fixture compiling `outer.typ` to PDF and extracting per-page text with
  `pypdf`. Assert the joined page text contains `OUTER-PROSE-MARKER` and `GUIDE-BODY-MARKER`, and
  that the substring `Nested Master` (the nested entry's title, which can only appear via a second
  title page) does NOT appear, and that the count of `Contents` occurrences is exactly 1. This is a
  structural pypdf assertion, NOT a `TypstError` assertion — B-2 was measured as
  compiles-fine-but-wrong-output.
- Unit test for the include-path purity edge: import the new `compute_content_include_path` from
  `typsphinx.writer` and assert `compute_content_include_path("manuals", "guide/index.typ") ==
  "../guide/index.typ"`, `compute_content_include_path("", "guide/index.typ") == "guide/index.typ"`,
  and `compute_content_include_path("guide", "guide/index.typ") == "index.typ"`. Mark this test
  `xfail(strict=True, raises=ImportError)` until 47-02 lands.

Mark every test in the module `@pytest.mark.xfail(strict=True, reason="RED until 47-02 lands the
two-layer emitter")` EXCEPT any that already passes on the unfixed tree. Run the module against the
unfixed tree and capture the raw pytest output plus, for COMP-03 and COMP-04, a direct
`typst.compile()` / `pypdf` transcript of the pre-fix behaviour. Record all of it verbatim in
`47-RED-EVIDENCE.md` under one heading per requirement, each heading stating the RED shape
(`TypstError` for COMP-03, structural/pypdf for COMP-04) and the command that produced it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_two_layer_output_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_two_layer_output_gate.py -q` exits 0 with every test reported `xfailed` (no `xpassed`, no `failed`).
    - `grep -c 'xfail(strict=True' tests/test_two_layer_output_gate.py` returns at least 5.
    - `47-RED-EVIDENCE.md` contains a heading for each of COMP-01, COMP-02, COMP-03, COMP-04 and OUT-03.
    - The COMP-03 section of `47-RED-EVIDENCE.md` contains the captured error text including `file not found`.
    - The COMP-04 section of `47-RED-EVIDENCE.md` contains a per-page pypdf transcript and states in prose that the compile SUCCEEDED, so the RED is structural and not a `TypstError`.
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>The two-layer gate module exists, proves RED against the unfixed tree via strict xfail, and the raw pre-fix evidence is recorded per requirement.</done>
</task>

<task type="auto">
  <name>Task 3: Write tests/test_collision_validator_gate.py and record the three non-fatal collision REDs</name>
  <files>
    tests/test_collision_validator_gate.py,
    .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-RED-EVIDENCE.md
  </files>
  <read_first>
    - tests/test_typst_documents_collision_gate.py (the whole module — the subprocess gate shape whose assertions 47-09 inverts)
    - .planning/phases/47-.../47-EXPECTED-STRUCTURE.md (the bld02/bld03/bld04 rows)
    - .planning/phases/47-.../47-RESEARCH.md "Common Pitfalls" 5 and the Validation Architecture table rows for BLD-02, BLD-03, BLD-04
    - .planning/phases/47-.../47-CONTEXT.md decisions D-01 through D-05
  </read_first>
  <action>
Create `tests/test_collision_validator_gate.py` covering BLD-02, BLD-03 and BLD-04, structured like
`tests/test_typst_documents_collision_gate.py` (one fixture-directory constant per scenario, one test
method per scenario, its own copy of `_run_sphinx_build`) but with the OPPOSITE outcome asserted.

Define a module constant `COLLISION_ERROR_SUBSTRING = "output path collision"` — the substring the
47-09 `ExtensionError` message must contain. Assert per scenario:
- BLD-02: `-b typst` of `bld02_duplicate_target_gate` returns a non-zero exit; combined stdout+stderr
  contains `ExtensionError` and `COLLISION_ERROR_SUBSTRING`; the message names BOTH entries (assert
  both `index` and `other` appear in the combined output); and D-02's no-partial-write rule holds —
  neither `manual.typ` nor any `.typ` file exists in the build directory.
- BLD-03: `-b typst` of `bld03_self_collision_gate` returns a non-zero exit with `ExtensionError`
  and `COLLISION_ERROR_SUBSTRING`, and no `index.typ` is written (D-01 + D-02).
- BLD-04 subprocess half: `-b typst` of `bld04_case_collision_gate` returns a non-zero exit with the
  same substrings, on every platform including Linux (D-05 makes this platform-independent).
- BLD-04 unit half (the load-bearing one, since Linux cannot observe the physical overwrite): import
  the new `TypstBuilder._collision_key` and assert `_collision_key("Manual.typ") ==
  _collision_key("manual.typ")` and `_collision_key("MANUAL") == _collision_key("manual")`, and that
  the function does NOT normalize Unicode: assert `_collision_key("Å")` differs from
  `_collision_key("Å")` (NFC vs NFD), which is D-05's explicit exclusion.
- Run each of the three fixtures with `-b typstpdf` as well, asserting the same non-zero exit, so
  both builders are proven to share the one validator.

Mark every test `@pytest.mark.xfail(strict=True, reason="RED until 47-09 lands the unified collision
validator")`. Run the module against the unfixed tree, then additionally capture the raw pre-fix
observations directly: for BLD-02, the exit code, the absence of any collision warning in combined
output, and `grep -c` counts of `INDEX-MASTER-MARKER-AAA` (expected 0) and `OTHER-MASTER-MARKER-BBB`
(expected 1) in the surviving `manual.typ`; for BLD-03, the exit code and the emitted file list; for
BLD-04, the exit code and the two distinct filenames Linux produces. Append all of it verbatim to
`47-RED-EVIDENCE.md` under one heading per requirement, each stating that the RED is structural
because none of the three defects fails the compile today.
  </action>
  <verify>
    <automated>uv run pytest tests/test_collision_validator_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_collision_validator_gate.py -q` exits 0 with every test reported `xfailed` (no `xpassed`, no `failed`).
    - `grep -c 'COLLISION_ERROR_SUBSTRING' tests/test_collision_validator_gate.py` returns at least 4.
    - `47-RED-EVIDENCE.md` contains a heading for each of BLD-02, BLD-03 and BLD-04.
    - The BLD-02 section records a `grep -c INDEX-MASTER-MARKER-AAA` result of 0 against the pre-fix `manual.typ`, proving the first master's body actually goes missing.
    - The BLD-04 section states in prose that Linux produces two distinct files and that the physical collision is observable only on the Windows/macOS lanes.
    - `uv run pytest tests/test_two_layer_output_gate.py tests/test_collision_validator_gate.py -q` exits 0.
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>The collision gate module exists, proves RED via strict xfail, and the pre-fix structural evidence for all three non-fatal defects is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` -> builder config | `typst_documents` target strings are project-owner-supplied Python; a well-meaning but mistaken value crosses here |
| builder -> filesystem (`outdir`) | every emitted path is joined under `outdir`; a target string decides part of that path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering / Elevation of Privilege | `_resolve_output_stem` wrapper-path resolution | high | mitigate | Fixtures in this plan pin the expected behaviour; the guard split itself lands in 47-03 preserving the three OUT-02 terms |
| T-47-02 | Tampering | wrapper vs. content physical path | high | mitigate | `bld03_self_collision_gate` fixture + strict-xfail RED here; the validator lands in 47-09 |
| T-47-03 | Denial of Service (silent content loss) | two entries, one target | medium | mitigate | `bld02_duplicate_target_gate` fixture + recorded `grep -c` proof the body goes missing |
| T-47-04 | Tampering | case-insensitive filesystem overwrite | high | mitigate | `bld04_case_collision_gate` fixture + a unit assertion on the comparison itself, since Linux cannot observe the overwrite |
| T-47-05 | Information Disclosure | build-time file generation | low | accept | No network or data-egress surface; output is written only under `outdir` |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero new packages (binding constraint #7); `47-RESEARCH.md` Package Legitimacy Audit records "Not applicable". No plan in this phase contains a package-manager install task |
</threat_model>

<verification>
- `uv run pytest tests/test_two_layer_output_gate.py tests/test_collision_validator_gate.py -q` exits 0 (all xfailed).
- `uv run pytest -q` exits 0 — this plan adds only strict-xfail tests and new fixtures, so the rest of the suite is untouched.
- `uv run black --check .` and `uv run ruff check .` exit 0.
- `git status --porcelain typsphinx/` prints nothing — no production code was touched.
</verification>

<success_criteria>
- `47-EXPECTED-STRUCTURE.md` and `47-RED-EVIDENCE.md` exist and are committed before any emitter code.
- All five new fixtures build (or fail) exactly as `47-RED-EVIDENCE.md` records.
- COMP-03's recorded RED is a `TypstError`; COMP-04's, BLD-02's, BLD-03's and BLD-04's are structural.
- The full test suite still exits 0.
</success_criteria>

<output>
Create `.planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-01-SUMMARY.md` when done
</output>
