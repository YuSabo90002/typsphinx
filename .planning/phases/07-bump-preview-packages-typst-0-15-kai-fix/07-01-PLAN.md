---
phase: 07-bump-preview-packages-typst-0-15-kai-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - typsphinx/writer.py
  - typsphinx/template_engine.py
  - typsphinx/templates/base.typ
autonomous: true
requirements: [FWD-02, PKG-01, PKG-02, PKG-03]

must_haves:
  truths:
    - "`tox -e docs-pdf` compiles the project docs to PDF with zero `TypstError` (no `unknown variable: kai`) — FWD-02, PKG-01, PKG-02"
    - "`pytest tests/test_preview_version_sync.py` passes — the three sync sites agree in lockstep — PKG-03"
    - "`uv sync --locked` resolves cleanly with typst 0.15.x installed from the regenerated lockfile — FWD-02"
    - "The generated PDF opens with no missing-glyph tofu, no broken/blank boxes, no error glyphs (coarse visual glance per D-01/D-04) — FWD-02"
  artifacts:
    - "pyproject.toml pinning `typst>=0.15.0,<0.16` (FWD-02)"
    - "uv.lock regenerated with typst 0.15.x (FWD-02)"
    - "typsphinx/writer.py, typsphinx/template_engine.py, typsphinx/templates/base.typ carrying mitex 0.2.7 / gentle-clues 1.3.1 / codly-languages 0.1.10 (codly 1.3.0 unchanged) — PKG-01, PKG-02, PKG-03"
  key_links:
    - "The four `@preview` version strings are identical across writer.py / template_engine.py / base.typ, or `test_preview_version_sync.py` fails — PKG-03"
    - "mitex 0.2.7 still exports `mi` and `mitex`; the named import in writer.py/template_engine.py must resolve — the docs-pdf compile of included (non-master) documents catches a silent export rename — PKG-01"
    - "codly 1.3.0 compiles under typst 0.15 — the core empirical unknown; the docs-pdf compile is the only proof — PKG-02"
    - "uv.lock installs typst 0.15.x so the tox docs-pdf env compiles against the new compiler, not the stale 0.14.9 — FWD-02"
---

<objective>
Atomic forward-port: raise the `typst` PyPI pin to `>=0.15.0,<0.16`, bump the four bundled `@preview` packages in lockstep across the three sync sites, regenerate the lockfile, then prove the whole graph compiles under typst 0.15 with zero `TypstError` via the real `docs-pdf` gate.

Purpose: This is the empirical root-cause fix and the highest-risk gate of the v0.5.0 milestone. Phase 6 intentionally left the PDF/`docs-pdf` CI lanes red on the `unknown variable: kai` break; raising typst without the mitex bump (or vice-versa) leaves an intermediate state broken — both must land atomically before the compile gate can go green.

Output: `pyproject.toml` typst pin raised, `uv.lock` regenerated, the four `@preview` version strings bumped in `writer.py` / `template_engine.py` / `templates/base.typ`, and a green `tox -e docs-pdf` compile producing a PDF with no error glyphs. No new files or public symbols.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-CONTEXT.md
@.planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-RESEARCH.md
@.planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-PATTERNS.md
@.planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-VALIDATION.md

# Source-of-truth for the version-sync hazard (three files must change in lockstep):
@CLAUDE.md
@typsphinx/writer.py
@typsphinx/template_engine.py
@typsphinx/templates/base.typ
@tests/test_preview_version_sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise the typst PyPI pin and regenerate the lockfile (FWD-02)</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (the `dependencies` array — line ~30 holds the current typst pin)
    - .planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-RESEARCH.md (Standard Stack table + the typst dependency-pin Code Example — confirms 0.15.0 resolves cleanly via dry-run this session)
    - .planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-PATTERNS.md (the "pyproject.toml" section — the guard-ceiling `>=floor,<ceiling` convention)
  </read_first>
  <action>
    In pyproject.toml, replace the single dependency-array entry `"typst>=0.14.1,<0.15",` with `"typst>=0.15.0,<0.16",` (FWD-02). This is a one-line version-range edit following the project's existing guard-ceiling convention — do NOT switch to a bare `>=` floor or an exact pin, and do NOT touch any other dependency line. Then regenerate the lockfile: run `uv lock` to re-resolve, then `uv sync --locked` to install against it. This must move the installed typst from 0.14.9 to 0.15.x so the downstream `docs-pdf` compile (Task 3) runs against the new compiler. Commit the regenerated uv.lock alongside pyproject.toml — a reproducible resolve is the supply-chain control for the typst bump (threats T-07-01 / T-07-SC).
  </action>
  <verify>
    <automated>grep -q 'typst>=0.15.0,<0.16' pyproject.toml && ! grep -q 'typst>=0.14.1,<0.15' pyproject.toml && uv sync --locked && grep -A2 '^name = "typst"' uv.lock | grep -q 'version = "0.15'</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains the literal `"typst>=0.15.0,<0.16",` and no longer contains `typst>=0.14.1,<0.15`.
    - `uv sync --locked` exits 0 (lockfile is current — no stale-lock / resolution-conflict error).
    - The `[[package]]` block named `typst` in `uv.lock` shows `version = "0.15..."` (replacing the previous `0.14.9`).
    - No other `dependencies` entry in pyproject.toml is changed.
  </acceptance_criteria>
  <done>pyproject.toml pins `typst>=0.15.0,<0.16`, uv.lock is regenerated with typst 0.15.x, and `uv sync --locked` is green.</done>
</task>

<task type="auto">
  <name>Task 2: Bump the four @preview versions in lockstep across the three sync sites (PKG-01, PKG-02, PKG-03)</name>
  <files>typsphinx/writer.py, typsphinx/template_engine.py, typsphinx/templates/base.typ</files>
  <read_first>
    - typsphinx/writer.py (lines ~94-97 — the included-document `imports.append(...)` block)
    - typsphinx/template_engine.py (lines ~313-316 — the master-document `output_parts.append(...)` block)
    - typsphinx/templates/base.typ (lines ~8-19 — the default-template `#import "@preview/..."` lines; note these are NOT contiguous)
    - CLAUDE.md (the "@preview version-sync hazard" section — the three-places-must-stay-in-lockstep rule)
    - .planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-PATTERNS.md (the "3-way lockstep version-string replacement" section — the exact substitution table + import-style-preservation warning)
    - tests/test_preview_version_sync.py (the guard that fails CI on desync — verify-only, do NOT edit it)
  </read_first>
  <action>
    Apply the same three version-substring replacements to all three sync sites, changing ONLY the version number inside each `@preview/<pkg>:<version>` string literal — no reordering, no restructuring, no touching the surrounding `.append(...)` / import context. In writer.py bump `@preview/codly-languages:0.1.1` to `@preview/codly-languages:0.1.10`, `@preview/mitex:0.2.4` to `@preview/mitex:0.2.7`, and `@preview/gentle-clues:1.2.0` to `@preview/gentle-clues:1.3.1`; leave `@preview/codly:1.3.0` unchanged (registry ceiling — no newer version exists). Apply the byte-identical three substitutions in template_engine.py (same strings, the collection variable is `output_parts` instead of `imports`) and in base.typ. PRESERVE the mitex import style asymmetry exactly: writer.py and template_engine.py keep the named form `: mi, mitex`; base.typ keeps the glob form `: *`. Do NOT convert one style to the other — that is an out-of-scope refactor per the CONTEXT.md planner note. Do NOT edit tests/test_preview_version_sync.py — it is a version-agnostic guard that passes automatically once the three sites agree. Run `black --check .` and `ruff check .` after editing to confirm the string edits did not disturb formatting (all changed lines stay under the 88-char limit).
  </action>
  <verify>
    <automated>pytest tests/test_preview_version_sync.py -x && grep -rq '@preview/mitex:0.2.7' typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ && grep -rq '@preview/gentle-clues:1.3.1' typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ && grep -rq '@preview/codly-languages:0.1.10' typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ && ! grep -rEq '@preview/(mitex:0.2.4|gentle-clues:1.2.0|codly-languages:0.1.1")' typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ && black --check . && ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - All three files (`writer.py`, `template_engine.py`, `base.typ`) contain `@preview/mitex:0.2.7`, `@preview/gentle-clues:1.3.1`, and `@preview/codly-languages:0.1.10`.
    - No stray old-version strings remain: none of the three files contains `@preview/mitex:0.2.4`, `@preview/gentle-clues:1.2.0`, or `@preview/codly-languages:0.1.1"` (the trailing `"` guards against matching the new `0.1.10` prefix).
    - `@preview/codly:1.3.0` is present and unchanged in all three files.
    - The mitex import style is preserved: `writer.py` and `template_engine.py` keep `: mi, mitex`; `base.typ` keeps `: *`.
    - `pytest tests/test_preview_version_sync.py -x` exits 0 (both sync tests green).
    - `black --check .` and `ruff check .` exit 0.
  </acceptance_criteria>
  <done>The four `@preview` version strings agree in lockstep across all three sync sites (mitex 0.2.7 / gentle-clues 1.3.1 / codly-languages 0.1.10 / codly 1.3.0), the sync test passes, and the tree is black/ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: Empirical compile gate — docs-pdf compiles kai-free, plus coarse PDF glance (FWD-02, PKG-01, PKG-02)</name>
  <files>docs/_build/pdf/ (compile output — no source edit unless a fallback fix is required)</files>
  <read_first>
    - tox.ini ([testenv:docs-pdf] — `sphinx-build -b typstpdf source _build/pdf`, `changedir = docs`)
    - .planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-CONTEXT.md (D-01/D-04 — the compile is the HARD gate; the visual glance is a coarse "no tofu/broken boxes" backstop, NOT an element-by-element audit; D-02/D-03 — accept the new appearance, no before/after diff)
    - .planning/ROADMAP.md (Phase 7 "Contingency" — the locked bisect fallback: revert one package at a time; codly 1.3.0 is the fallback suspect → source-level workaround escalation)
    - .planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-RESEARCH.md (Common Pitfalls #3 — the named mitex import is validated by compiling included/non-master docs; Open Question 1 — codly 0.15 compatibility is the core empirical unknown; Open Question 2 — any non-kai TypstError under 0.15 is equally in-scope for the clean-compile gate)
  </read_first>
  <action>
    Run the phase's hard gate: `tox -e docs-pdf` (tox handles `changedir = docs` and runs `sphinx-build -b typstpdf source _build/pdf`). The full docs corpus exercises all four packages — admonitions (gentle-clues), inline/block math (mitex, with `typst_use_mitex = True` in docs/source/conf.py), and code-blocks (codly/codly-languages) — across both master and included (`#include`d) documents, so this single compile validates every bumped package end-to-end AND validates that mitex 0.2.7 still resolves the named `mi, mitex` import on the included-document path. Capture the full command output and confirm it contains no `TypstError` and no `unknown variable: kai` line. Treat ANY `TypstError` as a failure of the FWD-02 clean-compile gate — including non-kai errors surfacing from typst 0.15's stricter validation or path handling (per RESEARCH Open Question 2, those are in-scope for this phase, not deferred). After a green compile, open the produced PDF under `docs/_build/pdf/` for the coarse D-01/D-04 visual glance. FALLBACK PATH (only if the compile fails on a `TypstError`, per the locked ROADMAP contingency — do NOT do this proactively): read the error message to identify which `@preview` symbol/package is implicated (the error names it directly, as the original `kai` break named mitex); then bisect by temporarily reverting mitex/gentle-clues/codly-languages one at a time to their pre-bump versions (keeping typst at 0.15) to isolate the culprit. codly 1.3.0 is the named fallback suspect and has no newer version to bump to — if it is the culprit, escalate per the locked ladder: source-level patch/vendor → alternate code-highlighting path → pin typst to the highest 0.15.x codly tolerates. Record whichever resolution was applied in the SUMMARY.
  </action>
  <verify>
    <automated>tox -e docs-pdf 2>&1 | tee /tmp/07-docs-pdf.log; test ${PIPESTATUS[0]} -eq 0 && ! grep -Eq 'TypstError|unknown variable: kai' /tmp/07-docs-pdf.log && ls docs/_build/pdf/*.pdf</automated>
    <human-check>Open a PDF under docs/_build/pdf/ and do a coarse D-01/D-04 glance: pass if there are no blank/broken boxes, no missing-glyph tofu, and no error glyphs. Do NOT do element-by-element auditing or before/after diffing (explicitly rejected by D-02/D-03/D-04); accept the new post-bump appearance as the baseline.</human-check>
  </verify>
  <acceptance_criteria>
    - `tox -e docs-pdf` exits 0.
    - The captured compile output contains no `TypstError` and no `unknown variable: kai` line.
    - At least one `.pdf` file exists under `docs/_build/pdf/`.
    - Coarse visual glance (deferred to end-of-phase human verification): the PDF shows no tofu / broken boxes / error glyphs.
  </acceptance_criteria>
  <done>`tox -e docs-pdf` is green with zero `TypstError`, a PDF is produced, and the coarse visual glance finds no obviously-broken output. If a fallback was needed, the applied resolution is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typsphinx build → PyPI | `uv` installs the `typst` (typst-py) binding; this phase makes a version-range change to an already-adopted direct dependency, not a new install |
| typst compiler → Typst Universe registry | `typst-py` resolves `@preview/<pkg>:<version>` imports against the `typst/packages` registry at compile time — the version strings this phase edits are the only inputs to that resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `typst` PyPI pin (`pyproject.toml` / `uv.lock`) | medium | mitigate | Keep the guard-ceiling range `>=0.15.0,<0.16` (no bare `>=` floor); regenerate and commit `uv.lock` so the resolved dependency graph is reproducible and pinned (Task 1) |
| T-07-02 | Spoofing | `@preview` package names in the three sync sites | low | mitigate | Only version substrings change — no new or renamed package names are introduced; `test_preview_version_sync.py::test_all_four_packages_declared` guards that all four names remain present in each of the three sites (Task 2) |
| T-07-SC | Tampering | `uv`/pip install of `typst-py` 0.15.0 + compile-time `@preview` resolution | medium | mitigate | `typst-py` is a pre-existing direct dependency (since Phase 1); RESEARCH.md Package Legitimacy Audit flags it `SUS` on release-recency grounds only (not identity/slopsquat) and approves it with context — so no blocking-human identity checkpoint is inserted. The supply-chain verification for the "too-new" risk is the empirical `docs-pdf` compile gate (Task 3, never trusting changelog claims alone), plus the committed `uv.lock` pinning the resolved artifact. All four `@preview` target versions were confirmed to exist in the authoritative `typst/packages` registry during research |

All threats are medium/low; the configured block threshold is `high`, so none block execution. No high/critical threats are introduced by a version-string forward-port that adds no new package names and no new user-facing input surface (RESEARCH.md Security Domain: V14 Configuration is the only applicable ASVS category).
</threat_model>

<verification>
Phase-level checks (mapped to ROADMAP Phase 7 success criteria):

1. **FWD-02 / clean compile (HARD gate):** `tox -e docs-pdf` exits 0 and the captured output contains no `TypstError` and no `unknown variable: kai` line; a PDF is produced under `docs/_build/pdf/`. Verified by a real compile, not changelog inference.
2. **PKG-01 / PKG-02 exact bumps:** `writer.py`, `template_engine.py`, and `base.typ` all declare mitex 0.2.7, gentle-clues 1.3.1, codly-languages 0.1.10; codly stays 1.3.0 and is empirically confirmed to compile by the same `docs-pdf` gate.
3. **PKG-03 / 3-way sync:** `pytest tests/test_preview_version_sync.py -x` exits 0.
4. **Toolchain clean:** `black --check .` and `ruff check .` exit 0 after the edits.
5. **Coarse visual glance (D-01/D-04, human, deferred to end-of-phase):** the produced PDF has no tofu / broken boxes / error glyphs; the new post-bump appearance is accepted as the baseline (no restoration of old styling, no before/after diff).
</verification>

<success_criteria>
- `pyproject.toml` pins `typst>=0.15.0,<0.16`; `uv.lock` is regenerated to typst 0.15.x; `uv sync --locked` is green.
- The four `@preview` version strings are bumped in lockstep (mitex 0.2.7 / gentle-clues 1.3.1 / codly-languages 0.1.10 / codly 1.3.0 unchanged) across all three sync sites; `test_preview_version_sync.py` passes.
- `tox -e docs-pdf` compiles the docs to PDF with zero `TypstError` (no `unknown variable: kai`) — the milestone's central `kai`-free gate is empirically satisfied.
- The coarse PDF visual glance finds no obviously-broken output.
- Tree is black/ruff clean. No new files or public symbols were created.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

This phase creates **NO new public symbols and NO new files**. It is a pure in-place version forward-port. Every change is an edit to an existing file:

| Artifact | Change |
|----------|--------|
| `pyproject.toml` (`dependencies`, line ~30) | typst pin `"typst>=0.14.1,<0.15"` → `"typst>=0.15.0,<0.16"` (FWD-02) |
| `uv.lock` | Regenerated via `uv lock`; `typst` resolves 0.14.9 → 0.15.x (FWD-02) |
| `typsphinx/writer.py` (lines ~95-97) | `@preview/codly-languages:0.1.1`→`0.1.10`, `@preview/mitex:0.2.4`→`0.2.7`, `@preview/gentle-clues:1.2.0`→`1.3.1` (named mitex import `: mi, mitex` preserved) (PKG-01/02/03) |
| `typsphinx/template_engine.py` (lines ~314-316) | Same three version-string bumps (named mitex import `: mi, mitex` preserved) (PKG-01/02/03) |
| `typsphinx/templates/base.typ` (lines ~9, 14, 19) | Same three version-string bumps (glob mitex import `: *` preserved) (PKG-01/02/03) |

Unchanged on purpose: `@preview/codly:1.3.0` (registry ceiling), the mitex import-style asymmetry, and `tests/test_preview_version_sync.py` (version-agnostic guard — passes once the three sites agree).
</artifacts_produced>

<output>
Create `.planning/phases/07-bump-preview-packages-typst-0-15-kai-fix/07-01-SUMMARY.md` when done.
</output>
