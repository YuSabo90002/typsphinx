---
phase: 10-version-string-fix-v0-5-0-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typsphinx/__init__.py
  - pyproject.toml
  - uv.lock
  - tests/test_extension.py
autonomous: true
requirements: [REL-01]
tags: [release, versioning, packaging, single-source, drift-guard]

must_haves:
  truths:
    - "Importing typsphinx reports 0.5.0: `uv run python -c \"import typsphinx; assert typsphinx.__version__ == '0.5.0'\"` exits 0 (implements ROADMAP Phase 10 SC #1, D-04/D-05)"
    - "typsphinx/__init__.py derives __version__ from importlib.metadata.version(\"typsphinx\") inside a try/except catching importlib.metadata.PackageNotFoundError, falling back to the sentinel \"unknown\" — no hardcoded release-looking literal remains (D-04)"
    - "pyproject.toml [project].version is the sole version literal and reads 0.5.0 (bumped from 0.4.4) (D-05)"
    - "uv.lock is regenerated in lockstep so `uv sync --locked` exits 0 (research Pitfall 2 — ci.yml examples job gate)"
    - "tests/test_extension.py::test_version_matches_pyproject_toml independently parses pyproject.toml [project].version via tomllib and asserts it equals typsphinx.__version__, and passes (research Pattern 2 — the genuine drift guard)"
    - "The existing tautological test_setup_version_matches is kept unchanged (validates setup() wiring) and the full suite + black/ruff/mypy stay green (ROADMAP Phase 10 SC #3)"
  artifacts:
    - "typsphinx/__init__.py (reworked __version__ assignment via importlib.metadata + PackageNotFoundError fallback)"
    - "pyproject.toml (version 0.4.4 -> 0.5.0)"
    - "uv.lock (regenerated typsphinx self-version entry)"
    - "tests/test_extension.py (new test_version_matches_pyproject_toml drift-guard function)"
  key_links:
    - "pyproject.toml [project].version -> importlib.metadata.version(\"typsphinx\") -> typsphinx.__version__ -> setup() returned metadata[\"version\"] key"
    - "pyproject.toml version <-> uv.lock typsphinx package entry (enforced by `uv sync --locked` in ci.yml examples job)"
---

<objective>
Make `typsphinx.__version__` derive from installed-distribution metadata instead of a hardcoded string, with `pyproject.toml` as the sole literal version (bumped `0.4.4` -> `0.5.0`), regenerate `uv.lock` in the same commit, and add an independent `tomllib`-based drift-guard test. This is the root-cause fix for the stale `0.4.3` string (D-04/D-05): version drift between the two files becomes structurally impossible, not merely corrected once.

Purpose: Satisfy ROADMAP Phase 10 Success Criteria #1 and #3 (in-scope) — single-sourced version reporting `0.5.0`, a genuine (non-tautological) drift guard, and a green suite/lint/type on `release/v0.5.0`.
Output: Reworked `typsphinx/__init__.py`, bumped `pyproject.toml`, regenerated `uv.lock`, new `test_version_matches_pyproject_toml`.

## Scope fence (CRITICAL — do NOT cross)
This plan is release *preparation only*, on branch `release/v0.5.0`. Do NOT tag (`git tag v*`), push tags, run `release.yml`, publish to PyPI, create a GitHub Release, or merge `release/v0.5.0 -> main` (PR #112 stays OPEN). Those are DEFERRED to `/gsd-complete-milestone` per CONTEXT.md D-01/D-02.

## Scope reconciliation note (bookkeeping — no action required)
ROADMAP.md Phase 10 Success Criteria and REQUIREMENTS.md REL-01 have ALREADY been reconciled to the prepare-vs-publish split (re-scope commit `3ef680d`): current ROADMAP SC #1 = version single-source, SC #2 = CHANGELOG entry, SC #3 = green suite / no tag / no publish / PR #112 open. The pre-rescope "release.yml green / published to PyPI" clause now lives only in the explicit "Deferred to /gsd-complete-milestone" note and MUST NOT be treated as a Phase 10 acceptance criterion. No ROADMAP/REQUIREMENTS edit is needed here — surfaced so the reconciliation is not silently dropped (must_incorporate finding #5, research Open Question 2).

## Artifacts this phase produces (this plan's share)
- `typsphinx/__init__.py` — `__version__` reworked from `= "0.4.3"` literal to `importlib.metadata.version("typsphinx")` with a `PackageNotFoundError` -> `"unknown"` fallback.
- `pyproject.toml` — `[project].version` bumped `0.4.4` -> `0.5.0` (sole version literal).
- `uv.lock` — regenerated so the embedded `typsphinx` workspace-member version matches `0.5.0`.
- `tests/test_extension.py::test_version_matches_pyproject_toml` — NEW independent drift-guard test.
(The `## [0.5.0]` CHANGELOG section is produced by plan 10-02.)
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-version-string-fix-v0-5-0-release/10-CONTEXT.md
@.planning/phases/10-version-string-fix-v0-5-0-release/10-RESEARCH.md
@.planning/phases/10-version-string-fix-v0-5-0-release/10-PATTERNS.md

# In-repo analogs the tasks must replicate:
@typsphinx/pdf.py
@docs/source/conf.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-source __version__ via importlib.metadata, bump pyproject 0.4.4 -> 0.5.0, regenerate uv.lock</name>
  <files>typsphinx/__init__.py, pyproject.toml, uv.lock</files>
  <read_first>
    - `typsphinx/__init__.py` (lines 1-22) — the current hardcoded `__version__ = "0.4.3"` at line 14, the `from typing import Any, Dict` / `from sphinx.application import Sphinx` imports below it, and `setup()` returning `"version": __version__` at line 62 (which needs NO edit).
    - `typsphinx/pdf.py` (lines 89-107, `get_typst_version`) — the in-repo precedent: `from importlib.metadata import version` + try/except + `"unknown"` string-sentinel fallback. Copy this shape, adapted to a module-level assignment catching the specific `importlib.metadata.PackageNotFoundError`.
    - `pyproject.toml` (lines 5-10) — `[project]` table, `version = "0.4.4"` at line 7, `requires-python = ">=3.12"` at line 10 (confirms stdlib `importlib.metadata`/`tomllib` are always available; no backport).
  </read_first>
  <action>
    Add `import importlib.metadata` at the top of the import block in `typsphinx/__init__.py` (module import form, so `importlib.metadata.PackageNotFoundError` is reachable without a second import line). Replace the single line `__version__ = "0.4.3"` with a try/except: assign `__version__ = importlib.metadata.version("typsphinx")` in the `try`, and on `except importlib.metadata.PackageNotFoundError:` assign the sentinel string `"unknown"` (chosen deliberately to match the existing `typsphinx/pdf.py` convention and to be obviously-non-release, per research Anti-Patterns — do NOT use a real-looking version as the fallback). The distribution name argument is `"typsphinx"`, matching `pyproject.toml [project].name`. Keep `__author__ = "YuSabo"` unchanged. Do NOT edit `setup()` — it already returns `"version": __version__` and is fixed transitively.

    In `pyproject.toml`, change `[project].version` from `0.4.4` to `0.5.0` (this single literal is now the sole source of truth per D-05).

    Regenerate the lockfile in the SAME task/commit: run `uv lock` to rewrite `uv.lock`'s embedded `typsphinx` workspace-member version (currently `0.4.4`), then run `uv sync` to re-install the editable package into the venv so `importlib.metadata.version("typsphinx")` reports `0.5.0` at runtime (research Runtime State Inventory: the local editable install still reports the old version until re-synced). This is mandatory because `ci.yml`'s `examples` job runs `uv sync --locked`, which fails on a stale lockfile (research Pitfall 2).
  </action>
  <verify>
    <automated>uv sync --locked && uv run python -c "import typsphinx; assert typsphinx.__version__ == '0.5.0', typsphinx.__version__"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import typsphinx; assert typsphinx.__version__ == '0.5.0'"` exits 0.
    - `uv run python -c "import tomllib; assert tomllib.load(open('pyproject.toml','rb'))['project']['version'] == '0.5.0'"` exits 0.
    - `grep -q 'importlib.metadata.version("typsphinx")' typsphinx/__init__.py` matches (single-sourced, not a literal).
    - `grep -q 'importlib.metadata.PackageNotFoundError' typsphinx/__init__.py` matches (the fallback is present).
    - `uv sync --locked` exits 0 (uv.lock regenerated in lockstep with the pyproject bump).
    - `uv run python -c "import sys; sys.modules.pop('typsphinx',None); import importlib.metadata; assert importlib.metadata.version('typsphinx')=='0.5.0'"` exits 0 (installed distribution metadata reports 0.5.0 after uv sync).
  </acceptance_criteria>
  <done>`typsphinx.__version__` reports `0.5.0` at runtime, derived from `importlib.metadata` with a `PackageNotFoundError` -> `"unknown"` fallback; `pyproject.toml [project].version` is `0.5.0`; `uv.lock` is regenerated and `uv sync --locked` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add independent tomllib drift-guard test; keep the tautological one; prove full suite + lint/type green</name>
  <files>tests/test_extension.py</files>
  <read_first>
    - `tests/test_extension.py` (whole file, 77 lines) — flat `def test_*()` functions, no class, no shared fixtures, each doing local imports inside the function body. Note the existing `test_setup_version_matches` (lines 54-68) — KEEP it unchanged; it validates `setup()` echoes `__version__` (wiring), and is no longer sufficient alone as a drift guard.
    - `docs/source/conf.py` (lines 10-25) — the in-repo `tomllib`-parse-of-`pyproject.toml` precedent to copy: `import tomllib` + `Path(__file__).parent... / "pyproject.toml"` + `tomllib.load(f)` + `pyproject_data["project"]["version"]`. NOTE the path depth: `conf.py` is 3 levels below repo root (`.parent.parent.parent`); `tests/test_extension.py` is 2 levels below root, so use `Path(__file__).parent.parent / "pyproject.toml"`.
  </read_first>
  <action>
    Append a new flat function `test_version_matches_pyproject_toml` to `tests/test_extension.py`, matching this file's existing style (local imports inside the function body, one assertion-focused docstring, no class wrapper). Inside it: `import tomllib` and `from pathlib import Path` (plain `import tomllib` — do NOT copy `conf.py`'s dead `tomli` <3.11 fallback branch, which is out of scope given the confirmed 3.12 floor); `import typsphinx`; build `pyproject_path = Path(__file__).parent.parent / "pyproject.toml"` (two `.parent` levels — see read_first); `open(pyproject_path, "rb")` and `tomllib.load(f)`; then `assert typsphinx.__version__ == pyproject_data["project"]["version"]`. This is an INDEPENDENT code path from the `importlib.metadata` call `__version__` itself uses — that independence is what makes it a genuine regression guard rather than a tautology (research Pattern 2, Anti-Patterns). Do NOT modify or delete `test_setup_version_matches` (research keep-both recommendation).
  </action>
  <verify>
    <automated>uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml tests/test_extension.py::test_setup_version_matches -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml -v` passes.
    - `uv run pytest tests/test_extension.py::test_setup_version_matches -v` still passes (existing test kept, unchanged).
    - `grep -q 'def test_version_matches_pyproject_toml' tests/test_extension.py` matches; `grep -q 'def test_setup_version_matches' tests/test_extension.py` matches (both present).
    - `grep -q 'tomllib' tests/test_extension.py` matches (independent parse path present).
    - Full phase gate: `uv run pytest && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0.
  </acceptance_criteria>
  <done>`test_version_matches_pyproject_toml` exists, uses an independent `tomllib` parse of `pyproject.toml`, and passes; `test_setup_version_matches` is retained; the full pytest suite plus `black --check`/`ruff check`/`mypy typsphinx/` are all green on `release/v0.5.0`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pyproject.toml` (developer-authored config) -> runtime `__version__` reported to importers/Sphinx | The version literal a package advertises about itself crosses from build metadata into runtime code |
| `pyproject.toml` file on disk -> `tomllib.load()` in the drift-guard test | A repo-local, trusted config file is parsed by the test suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | `typsphinx.__version__` / `setup()` metadata `version` key | low | mitigate | Single-source `__version__` via `importlib.metadata.version("typsphinx")` (D-04) so the reported version is always the actually-installed distribution's real version, never an independently-editable literal that can drift out of truthfulness; the new independent `tomllib` drift-guard test catches re-introduction of a hardcoded literal |
| T-10-02 | Tampering | `tomllib.load(pyproject.toml)` in the drift-guard test | low | accept | Input is a trusted, repo-local, developer-authored config file, not untrusted external input (research Security Domain V5); no validation control warranted |
| T-10-SC | Tampering | pip/uv package installs | low | accept | No new third-party package is installed this phase — `importlib.metadata` and `tomllib` are Python stdlib at the confirmed 3.12 floor (research Package Legitimacy Audit: not applicable); no `[ASSUMED]`/`[SUS]` package, no legitimacy checkpoint needed |
</threat_model>

<verification>
- `uv sync --locked` exits 0 (uv.lock regenerated in lockstep — the ci.yml `examples` gate).
- `uv run python -c "import typsphinx; assert typsphinx.__version__ == '0.5.0'"` exits 0.
- `uv run pytest tests/test_extension.py -v` passes (incl. the new drift guard and the retained wiring test).
- Full gate: `uv run pytest && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0.
- NOT verified here (out of scope, deferred to milestone close): `release.yml` green / published to PyPI / tag pushed / PR #112 merged.
</verification>

<success_criteria>
- `typsphinx/__init__.py` no longer hardcodes a version; `__version__` derives from `importlib.metadata.version("typsphinx")` with a `PackageNotFoundError` -> `"unknown"` fallback (ROADMAP Phase 10 SC #1).
- `pyproject.toml [project].version` is `0.5.0` (sole literal); `uv.lock` is in sync (`uv sync --locked` exits 0).
- The independent `test_version_matches_pyproject_toml` drift guard passes and `test_setup_version_matches` is retained (ROADMAP Phase 10 SC #3).
- Full suite + `black`/`ruff`/`mypy` green on `release/v0.5.0`; no tag, no publish, PR #112 untouched.
</success_criteria>

<output>
Create `.planning/phases/10-version-string-fix-v0-5-0-release/10-01-SUMMARY.md` when done.
</output>
