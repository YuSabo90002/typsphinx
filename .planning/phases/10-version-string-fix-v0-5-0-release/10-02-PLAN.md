---
phase: 10-version-string-fix-v0-5-0-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [REL-01]
tags: [release, changelog, docs, release-notes]

must_haves:
  truths:
    - "CHANGELOG.md has a `## [0.5.0]` entry inserted directly under the TOP `## [Unreleased]` header (line 8) and above `## [0.4.3]` (line 10) — NOT near the stray second `## [Unreleased]` at line 517 (research Pitfall 3, implements ROADMAP Phase 10 SC #2 / D-06)"
    - "The [0.5.0] entry reads as a user-facing milestone summary (not a commit dump) covering: Sphinx 9.1 + docutils 0.22 support, typst 0.15 + @preview bumps (the kai fix), the admonition code-mode rendering fix (Phase 8.1), Python floor raised to 3.12-3.13, and the CI smoke-gate + drift guardrails (D-06 / research Specific Ideas)"
    - "The entry uses this file's existing `### Changed`/`### Fixed`/`### Added` subsection shape (matching the `[0.4.3]` entry) and a dated header `## [0.5.0] - 2026-07-11`"
    - "A `[0.5.0]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.5.0` link reference is added to the bottom link list and the `[Unreleased]:` compare link is updated from `v0.4.3...HEAD` to `v0.5.0...HEAD` (research Pitfall 3)"
    - "No `[0.4.4]` section is backfilled (v0.4.4 was internal tooling only — research Pitfall 4)"
  artifacts:
    - "CHANGELOG.md (new `## [0.5.0]` section + `[0.5.0]` link reference + updated `[Unreleased]` compare link)"
  key_links:
    - "CHANGELOG.md `## [0.5.0]` section -> (deferred to milestone close) `release.yml` `body_path` GitHub Release body — this curated section is the single source of the eventual Release notes (D-06)"
---

<objective>
Add a curated `## [0.5.0]` entry to `CHANGELOG.md` under the top `## [Unreleased]` header. This section is the single source of truth for the eventual v0.5.0 GitHub Release body (D-06) — at publish time (milestone close, out of scope here) it is passed manually to `release.yml`'s `body_path`, avoiding the noisy 92-commit auto-generated `git log`.

Purpose: Satisfy ROADMAP Phase 10 Success Criterion #2 (in-scope) — a curated, user-facing v0.5.0 changelog entry.
Output: One new dated `## [0.5.0]` section + its link references in `CHANGELOG.md`.

## Scope fence (CRITICAL — do NOT cross)
Release preparation only, on branch `release/v0.5.0`. Do NOT tag, push tags, run `release.yml`, publish to PyPI, create a GitHub Release, or merge PR #112. Those are DEFERRED to `/gsd-complete-milestone` (CONTEXT.md D-01/D-02). This plan only writes the CHANGELOG text.

## Artifacts this phase produces (this plan's share)
- `CHANGELOG.md` — new `## [0.5.0] - 2026-07-11` section (with `### Changed`/`### Fixed`/`### Added` subsections), a new `[0.5.0]` link reference, and an updated `[Unreleased]` compare link.
(The version single-sourcing, `pyproject.toml` bump, `uv.lock`, and the drift-guard test are produced by plan 10-01.)
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-version-string-fix-v0-5-0-release/10-CONTEXT.md
@.planning/phases/10-version-string-fix-v0-5-0-release/10-RESEARCH.md
@.planning/phases/10-version-string-fix-v0-5-0-release/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert curated ## [0.5.0] CHANGELOG entry under the top Unreleased header + fix link references</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1-16 — the Keep-a-Changelog preamble, the canonical `## [Unreleased]` at line 8, and the existing `## [0.4.3] - 2025-11-01` entry (lines 10-16) whose `### Changed` subsection shape and dated-header format are the structural template to copy.
    - `CHANGELOG.md` lines 515-537 — the bottom link-reference list (`[0.4.3]: .../releases/tag/v0.4.3` at line 528 down to `[Unreleased]: .../compare/v0.4.3...HEAD` at line 537), AND the stray second `## [Unreleased]` at line 517 under `### Planned for Future Releases` — this second header is a leftover to IGNORE (do not insert near it — research Pitfall 3).
    - `.planning/phases/10-version-string-fix-v0-5-0-release/10-PATTERNS.md` (CHANGELOG section, lines 152-194) — the content skeleton and exact insertion instructions.
    - `.planning/phases/10-version-string-fix-v0-5-0-release/10-RESEARCH.md` (Code Examples, lines 465-491) — the v0.5.0 entry shape example (themes, not literal required text).
  </read_first>
  <action>
    Insert a new `## [0.5.0] - 2026-07-11` section into `CHANGELOG.md` positioned directly between the top `## [Unreleased]` header (line 8) and the `## [0.4.3] - 2025-11-01` header (line 10) — i.e. it becomes the newest dated entry. Do NOT touch or insert near the stray `## [Unreleased]` at line 517.

    Write it as a curated, user-facing milestone summary (NOT a commit dump), following this file's existing `### Changed` / `### Fixed` / `### Added` subsection convention (as seen in the `[0.4.3]` entry). Cover these themes (exact wording/granularity is the implementer's call per D-06 discretion — follow existing CHANGELOG style):
      - Changed: Forward-ecosystem port — Sphinx re-pinned to the 9.1 line, `typst` to the 0.15 line, `docutils` to the 0.22 line; Python floor raised to 3.12-3.13 (3.10/3.11 dropped). Bundled `@preview` packages bumped (mitex bump fixes the `unknown variable: kai` break under typst 0.15; gentle-clues and codly-languages bumped in lockstep).
      - Fixed: Admonition rendering — fixed a translator markup/code-mode mismatch that caused `.. note::`-style admonitions to render literal, unevaluated Typst source instead of typeset prose (Phase 8.1).
      - Added: CI — a `typst compile` smoke test that catches `kai`-class `@preview` breaks before release; `drift.yml` / Dependabot guardrails updated to the new major-version ceilings.

    Then update the bottom link-reference list: add `[0.5.0]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.5.0` immediately above the existing `[0.4.3]:` line, and change the `[Unreleased]:` compare link from `.../compare/v0.4.3...HEAD` to `.../compare/v0.5.0...HEAD`.

    Do NOT backfill a `[0.4.4]` section — v0.4.4 was internal CI/packaging tooling only, with no user-facing change (research Pitfall 4). Do NOT tag, publish, or merge anything (scope fence).
  </action>
  <verify>
    <automated>grep -n "## \[0.5.0\] - 2026-07-11" CHANGELOG.md && grep -q "^\[0.5.0\]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.5.0" CHANGELOG.md && grep -q "compare/v0.5.0...HEAD" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## \[0.5.0\] - 2026-07-11" CHANGELOG.md` returns a match near the top of the file (line number well below 500 — under the canonical `## [Unreleased]` at line 8, above `## [0.4.3]`).
    - The `## [0.5.0]` header appears BEFORE the `## [0.4.3]` header in file order: `awk '/## \[0.5.0\]/{a=NR} /## \[0.4.3\]/{b=NR} END{exit !(a>0 && b>0 && a<b)}' CHANGELOG.md` exits 0.
    - The entry contains `### Changed`, `### Fixed`, and `### Added` subsections under the `## [0.5.0]` header (matching the file's convention).
    - `grep -q "^\[0.5.0\]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.5.0" CHANGELOG.md` matches (link reference added).
    - `grep -q "compare/v0.5.0...HEAD" CHANGELOG.md` matches AND `grep -c "compare/v0.4.3...HEAD" CHANGELOG.md` returns 0 (the `[Unreleased]` compare link was updated, not duplicated).
    - `grep -c "## \[0.4.4\]" CHANGELOG.md` returns 0 (no backfilled 0.4.4 section — research Pitfall 4).
    - Content covers the five milestone themes (Sphinx 9.1 + docutils 0.22, typst 0.15 + `@preview` kai fix, admonition render fix, Python 3.12-3.13 floor, CI smoke-gate + guardrails) as user-facing prose.
  </acceptance_criteria>
  <done>`CHANGELOG.md` has a curated, correctly-positioned `## [0.5.0] - 2026-07-11` section under the top `## [Unreleased]` header, with `### Changed`/`### Fixed`/`### Added` subsections, a `[0.5.0]` link reference, and the `[Unreleased]` compare link updated to `v0.5.0...HEAD`; no `[0.4.4]` backfill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` `## [0.5.0]` section -> (deferred) GitHub Release body via `release.yml` `body_path` | Curated release-notes prose crosses from the repo into a public, published release artifact at milestone close |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering / Repudiation | `CHANGELOG.md` v0.5.0 release-notes content | low | accept | Content is human-authored, developer-reviewed release notes in a version-controlled file; the curated section is the single source consumed by the (deferred, out-of-scope) `release.yml` `body_path`. No untrusted input, no automated generation to poison. Correctness is enforced by review, not a runtime control |
| T-10-SC | Tampering | pip/uv package installs | low | accept | No package is installed in this docs-only plan (no npm/pip/cargo action); Package Legitimacy Audit not applicable |
</threat_model>

<verification>
- `grep -n "## \[0.5.0\] - 2026-07-11" CHANGELOG.md` matches near the top of the file.
- `## [0.5.0]` precedes `## [0.4.3]` in file order; `### Changed`/`### Fixed`/`### Added` subsections present.
- `[0.5.0]` link reference added; `[Unreleased]` compare link updated to `v0.5.0...HEAD`; no `[0.4.4]` backfill.
- NOT verified here (out of scope, deferred to milestone close): `release.yml` `body_path` consumption / published GitHub Release / tag pushed / PR #112 merged.
</verification>

<success_criteria>
- `CHANGELOG.md` has a curated `## [0.5.0] - 2026-07-11` entry under the top `## [Unreleased]` header, above `## [0.4.3]`, covering the five milestone themes (ROADMAP Phase 10 SC #2 / D-06).
- Link references corrected (`[0.5.0]` added, `[Unreleased]` compare link -> `v0.5.0...HEAD`); no `[0.4.4]` backfill.
- No tag, no publish, no merge (scope fence honored).
</success_criteria>

<output>
Create `.planning/phases/10-version-string-fix-v0-5-0-release/10-02-SUMMARY.md` when done.
</output>