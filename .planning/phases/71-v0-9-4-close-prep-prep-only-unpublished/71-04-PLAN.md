---
phase: 71-v0-9-4-close-prep-prep-only-unpublished
plan: 04
type: execute
wave: 2
depends_on: ["71-01", "71-02"]
files_modified:
  - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md
autonomous: true
requirements: [REL-13]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-10 census: immediately before the push, `git branch --list gsd/v0.9.4*` and `git ls-remote --heads origin gsd/v0.9.4*` are recorded. A decoy is never pushed. A decoy carrying commits the canonical branch lacks halts the plan, because D-10 orders the canonical pointer advanced before the decoy is deleted, and that is a main-checkout action.'
    - 'The pushed tip carries every product-tree change of this phase. PUSHED_SHA is the canonical ref gsd/v0.9.4-typing-modernization. Its CHANGELOG.md region holds four bold bullets with the five-ID span, and its pyproject.toml:7 is `version = "0.9.2"`. Nothing under typsphinx/ or tests/ differs from CODE_FREEZE_ANCHOR (D-11), and nothing under .github/ differs from MILESTONE_BASE (constraint 12). Its merge-base with origin/main is still MILESTONE_BASE (D-05), and outside .planning/ it is identical to this worktree HEAD.'
    - 'D-10 push: the canonical ref is pushed with `git push --no-follow-tags origin gsd/v0.9.4-typing-modernization` as a fast-forward of the origin head ORIGIN_BEFORE, re-measured just before, with no force, no tag and no other ref. Afterwards the origin head equals PUSHED_SHA and no tag points at it.'
    - 'D-10 dispatch: exactly one `gh workflow run CI --ref gsd/v0.9.4-typing-modernization` is issued after the push. RUN_ID has headSha equal to PUSHED_SHA, event workflow_dispatch, workflowName CI, and createdAt later than PUSH_AT.'
    - 'D-10 / SC#3 CI half: the run is observed in the foreground to status completed, and all 12 job conclusions are transcribed in a numbered table. Test Python 3.12 on windows-latest, Test Python 3.13 on windows-latest, Test Python 3.12 on macos-latest and Test Python 3.13 on macos-latest are named individually. Every job is success; a skipped, cancelled or missing job never counts as success.'
    - 'Constraint 8: the ruff verdict is quoted from the `Lint and Format Check` job, step `Run lint with tox`, and CI_RUFF_VERSION read from that job install output equals the ruff version in uv.lock at PUSHED_SHA.'
    - 'Edge (ordering): the run''s job names, compared as a sorted set, equal those of Phase 70''s run 34742047126, so ci.yml has not changed since Phase 70 (constraint 12).'
    - 'Edge (idempotency): the CI workflow list holds exactly one workflow_dispatch run at PUSHED_SHA, an executor that resumes mid-wait reuses RUN_ID and never dispatches again, and the release.yml list holds no run at PUSHED_SHA.'
    - statement: 'Edge (concurrency): only this plan writes to origin, as one fast-forward of the ORIGIN_BEFORE it re-measured just before the push, and it halts rather than forcing if origin moved. Its wave-2 siblings 71-03 and 71-05 share no file with it and read none of its evidence.'
      verification: backstop
    - 'Failure path: a non-success run is transcribed in full and kept. The executor stops at a blocking checkpoint, with no rerun, no second dispatch and no edit.'
    - 'REL-13 is cited for coverage only; SUMMARY requirements-completed: [] (D-09).'
  artifacts:
    - path: .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md
      provides: 'decoy census, tip identity and fence, fast-forward push, one dispatch, the completed run with all 12 jobs, ruff verdict, job-name comparison with Phase 70, dispatch-count and no-release guards, SC#3 CI verdict'
      contains: 'PUSHED_SHA = '
  key_links:
    - from: 'local ref gsd/v0.9.4-typing-modernization'
      to: 'origin/gsd/v0.9.4-typing-modernization'
      via: 'git push --no-follow-tags origin gsd/v0.9.4-typing-modernization, fast-forward only; refs are shared between the worktree and the main checkout'
      pattern: 'ORIGIN_BEFORE = '
    - from: 'gh workflow run CI --ref gsd/v0.9.4-typing-modernization'
      to: 'ci.yml twelve jobs on ubuntu, windows and macos'
      via: 'workflow_dispatch, since the push and pull_request triggers are scoped to main and develop'
      pattern: 'RUN_ID = '
    - from: 'uv.lock at PUSHED_SHA'
      to: 'CI_RUFF_VERSION in the Lint and Format Check job'
      via: 'uv sync --locked on the runner'
      pattern: 'CI_RUFF_VERSION = '
  prohibitions:
    - statement: 'MUST NOT create a tag, open or comment on a pull request, trigger release.yml or update-pin.yml, force-push, push any ref other than the canonical branch, or push a decoy.'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT dispatch a second CI run, rerun jobs, or edit any file to turn a red job green; MUST NOT cite any run whose headSha differs from PUSHED_SHA as this phase''s green.'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT move any ref of the main checkout from this worktree, other than deleting a decoy that is already an ancestor of the canonical ref, and MUST NOT merge origin/main into the branch (D-05).'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Put this phase's tip on `origin` and obtain CI's verdict on it (D-10, ROADMAP SC#3 CI half):
- re-check for a decoy branch immediately before the push
- fast-forward the canonical milestone branch, and push nothing else
- dispatch `ci.yml` exactly once on it and observe the run in the foreground until it completes
- transcribe every one of the 12 job conclusions, naming both `windows-latest` and both
  `macos-latest` lanes
- quote `ruff`'s verdict from the `Lint and Format Check` job

Purpose: CI holds lint authority and is the only route to Windows and macOS (ROADMAP constraint 8).
The dispatched tip must carry the CHANGELOG edit, so this plan runs in wave 2, after 71-01 has
merged. Every later commit of the phase touches only `.planning/`, which no CI job reads, so this
run covers the phase's final product tree; plan 71-06 proves that. It runs in parallel with 71-03
and 71-05, and it is the only plan in the phase that writes to a remote.

Output: `71-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-RESEARCH.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CI-EVIDENCE.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run project tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Refs are shared.** This worktree's HEAD is on its own worktree branch, but
`refs/heads/gsd/v0.9.4-typing-modernization` is the same ref the main checkout has checked out. Push
that ref by name; never `git branch -f`, `git update-ref` or `git symbolic-ref` it from here.

**Waiting on CI.** Wait in the foreground: set the Bash tool's `timeout` to 600000 and never use
`run_in_background`. Wrap each watch in `timeout 590` so it returns inside the tool limit, then
read `gh run view "$RUNID" --json status,conclusion`. Repeat these bounded foreground calls until
`status` is `completed`, and do not end the turn while the run is in progress. Commit `RUN_ID` to
the evidence file before the first wait, so a resumed executor can reuse it rather than dispatch
again.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line. The verify fails on any key line containing an opening parenthesis. Phase 69's executor
appended explanations to five values although its plan said not to.

**Scratch files.** Parallel executors share the session scratchpad. Put every captured log under your
own `mktemp -d` directory, recorded as `SCRATCH_71_04`, and give helper files a `p7104_` prefix.

**Locale.** The host runs `LANG=ja_JP.UTF-8`; prefix `LC_ALL=C` to any `git` command whose message you
quote for comparison.
</worktree_provisioning>

<!-- planner-discipline-allow: gsd/v0.9.4-milestone -->
<!-- planner-discipline-allow: gsd/v0\.9\.4-milestone -->
<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: v0.9.4 -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[34] -->
<!-- planner-discipline-allow: Run linters -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
The decoy name is named because the census looks for it and the gate requires it absent from
origin. The release workflow's lint step name is named because the gate requires it absent from the
evidence. `## HALT` is written only on a failure path.

## Planning-time census (2026-09-13, main checkout at `d8679b01`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| Local `gsd/v0.9.4*` refs | only the canonical `gsd/v0.9.4-typing-modernization` (HEAD of the main checkout, 17 commits ahead of its origin counterpart, all `.planning/`); no decoy | `git branch -a --list '*v0.9.4*' -v` |
| Origin head of the canonical branch | `e70e31fba9039133a153a5bba16577d7b2f889c1`, Phase 70's pushed tip | `git branch -vv`, `70-CI-EVIDENCE.md` `PUSHED_SHA` |
| Last CI run on the branch | `34742047126` at `e70e31fb` (Phase 70, 12 jobs, all success, `CI_RUFF_VERSION = 0.16.6`) | `70-CI-EVIDENCE.md` |
| `ci.yml` jobs | 12: `Test Python {3.12,3.13} on {ubuntu,windows,macos}-latest` (6), `Lint and Format Check` (step `Run lint with tox`, `ci.yml:52`, `:69`), `Type Check`, `Code Coverage`, `Build Package`, `Integration Test - basic`, `Integration Test - advanced`; `push` and `pull_request` scoped to `main`/`develop`, plus `workflow_dispatch` | `.github/workflows/ci.yml` |
| `release.yml` lint step | named differently (`release.yml:84`); never searched and never triggered here | `release.yml` |
| Branch-lock ruff | 0.16.6 | `uv.lock` |
| Anchors | `CODE_FREEZE_ANCHOR` and `MILESTONE_BASE` come from `71-SC1-INVARIANTS.md` (wave 1) | 71-02 |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: gate on wave 1, tip identity and fence, decoy census, fast-forward push of the canonical branch, dispatch CI exactly once</name>
  <precondition>`gh auth status` succeeds and `git ls-remote --heads origin` returns at least one line. The canonical ref `gsd/v0.9.4-typing-modernization` carries 71-01's edit and 71-02's anchors: `git show gsd/v0.9.4-typing-modernization:CHANGELOG.md` has four bold-lead bullets between `## [Unreleased]` and `## [0.9.2]`, and `git cat-file -e gsd/v0.9.4-typing-modernization:.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md` succeeds. Otherwise write `## HALT: canonical tip lacks wave 1` and stop without pushing.</precondition>
  <reversibility rating="reversible">The branch already exists on origin and advances only by fast-forward, and a CI run publishes nothing. No tag, release or pull request is created. CONTEXT's Phase Boundary pre-authorises this push and dispatch as standing prep-phase precedent (Phases 57, 61, 63, 69).</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-04-PLAN.md Task 1 and 69-CI-EVIDENCE.md (the census, push and dispatch procedure and section structure this task mirrors)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CI-EVIDENCE.md (the previous run on this branch, `PUSHED_SHA`, `RUN_ID`)
    - .github/workflows/ci.yml (`name: CI`, the triggers and the twelve job names)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md (`MILESTONE_BASE`, `CODE_FREEZE_ANCHOR`)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-05, D-10 and D-11
    - .planning/ROADMAP.md binding constraints 8, 9 and 12 of the v0.9.4 section
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line
   - the same provisioning line with `--locked` appended, then `echo "exit:$?"`, which must print `exit:0`. Every `ci.yml` job begins with a locked sync, so a stale lock would fail all twelve lanes before any signal. On a non-zero exit write `## HALT: lock not in sync`, and never regenerate the lock.
   - before any commit: `BASE_71_04 = ` followed by the output of `git rev-parse HEAD`
   - `SCRATCH_71_04 = ` followed by the path `mktemp -d` printed

2. **`## Wave-1 gate`.** Quote the precondition checks:
   - the bold-bullet count over the canonical ref's `CHANGELOG.md` region, and the presence of the `(QUA-09, QUA-11, QUA-12, DOC-22, DOC-23).**` span
   - `git cat-file -e` of `71-SC1-INVARIANTS.md` at the canonical ref

3. **`## Tip identity and fence`.** Read `MILESTONE_BASE` and `CODE_FREEZE_ANCHOR` from `71-SC1-INVARIANTS.md`. Record each of these; all must hold:
   - `PUSHED_SHA = ` followed by the output of `git rev-parse gsd/v0.9.4-typing-modernization`, plus `git log --oneline -1 "$PUSHED_SHA"`
   - `git diff --name-only HEAD "$PUSHED_SHA" -- . ':(exclude).planning'`, empty: the pushed product tree equals this worktree's
   - `git show "$PUSHED_SHA:pyproject.toml" | sed -n 7p`, which must be `version = "0.9.2"`
   - `git diff --name-only "$CODE_FREEZE_ANCHOR" "$PUSHED_SHA" -- typsphinx/ tests/`, empty (D-11: no code change since Phase 70)
   - `git diff --name-only "$MILESTONE_BASE" "$PUSHED_SHA" -- .github/`, empty (constraint 12: CI is unchanged)
   - after `git fetch origin main`: `git merge-base "$PUSHED_SHA" origin/main`, which must equal `MILESTONE_BASE`, so no `main` commit was absorbed (D-05)
   - `git tag --points-at "$PUSHED_SHA"`, empty
   - the ruff version in `git show "$PUSHED_SHA:uv.lock"`, key `LOCK_RUFF_VERSION`

4. **`## Decoy census (D-10)`.** Run this immediately before the push, with nothing but the step-5 measurements in between. Record `git branch --list 'gsd/v0.9.4*' -v` and `git ls-remote --heads origin 'gsd/v0.9.4*'`.
   - If `gsd/v0.9.4-milestone` exists on origin, write `## HALT: decoy on origin` and stop. It is never pushed, and its removal belongs to the owner.
   - If it exists locally, run `git merge-base --is-ancestor gsd/v0.9.4-milestone gsd/v0.9.4-typing-modernization; echo "exit:$?"`:
     - `exit:0`: the canonical pointer already contains it, so D-10's advance-first step is satisfied. Delete it with `git branch -d gsd/v0.9.4-milestone` and record that.
     - Non-zero: the decoy carries commits the canonical branch lacks. Record `git log --oneline gsd/v0.9.4-typing-modernization..gsd/v0.9.4-milestone`, write `## HALT: decoy carries unique commits`, and stop. D-10 requires the canonical pointer to be advanced first, and that happens in the main checkout, not from this worktree.
   - If it exists nowhere, record that as the census result.

5. **`## Push`.**
   - Run `git fetch origin gsd/v0.9.4-typing-modernization`, then record `ORIGIN_BEFORE = ` with the first field of `git ls-remote --heads origin refs/heads/gsd/v0.9.4-typing-modernization`.
   - Record `git merge-base --is-ancestor "$ORIGIN_BEFORE" "$PUSHED_SHA"; echo "exit:$?"`, which must print `exit:0`. Otherwise write `## HALT: origin moved` and stop; a force-push is forbidden.
   - Record `git config --get push.followTags`, then `PUSH_AT = ` with the output of `date -u +%FT%TZ`.
   - Run `git push --no-follow-tags origin gsd/v0.9.4-typing-modernization`, D-10's single push with the guard against tags riding along. Record the output, including GitHub's pull-request hint, and do not act on that hint.
   - Record that `git ls-remote --heads origin refs/heads/gsd/v0.9.4-typing-modernization` now prints `PUSHED_SHA`, and that `git ls-remote --tags origin` shows `refs/tags/v0.9.2` and no v0.9.3 or v0.9.4 tag.

6. **`## Dispatch`.**
   - Run `gh workflow run CI --ref gsd/v0.9.4-typing-modernization` exactly once.
   - Locate the run with `gh run list --workflow=ci.yml --branch gsd/v0.9.4-typing-modernization --event workflow_dispatch --limit 5 --json databaseId,headSha,status,createdAt,url`. Select the row whose `headSha` equals `PUSHED_SHA` and whose `createdAt` is later than `PUSH_AT`.
   - If no such row is listed yet, that is registration lag. Repeat the list query a few seconds later; never dispatch again.
   - Record `RUN_ID = ` and `RUN_URL = `.

7. Write `71-CI-EVIDENCE.md` under the heading `# Phase 71 — CI Evidence (D-10, SC#3 CI half)`, using the section names above, and commit it. This plan's own commits land after the push and touch only `.planning/`, which no CI job reads.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; F="$D/71-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.4-typing-modernization &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_71_04)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; A="$(k CODE_FREEZE_ANCHOR "$D/71-SC1-INVARIANTS.md")" &amp;&amp; [ "${#A}" = 40 ] &amp;&amp; MB="$(k MILESTONE_BASE "$D/71-SC1-INVARIANTS.md")" &amp;&amp; [ "${#MB}" = 40 ] &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; HR="$(git ls-remote --heads origin "refs/heads/$BR")" &amp;&amp; [ "$(printf '%s\n' "$HR" | cut -f1)" = "$P" ] &amp;&amp; OB="$(k ORIGIN_BEFORE)" &amp;&amp; [ "${#OB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$OB" "$P" &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; [ "$(printf '%s\n' "$HL" | grep -c 'gsd/v0\.9\.4-milestone')" = 0 ] &amp;&amp; [ -z "$(git tag --points-at "$P")" ] &amp;&amp; TL="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.[34]')" = 0 ] &amp;&amp; PC="$(git show "$P:CHANGELOG.md")" &amp;&amp; R="$(printf '%s\n' "$PC" | awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}')" &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^- \*\*' | wc -l)" = 4 ] &amp;&amp; printf '%s\n' "$R" | tr '\n' ' ' | tr -s ' ' | grep -qF '(QUA-09, QUA-11, QUA-12, DOC-22, DOC-23).**' &amp;&amp; PP="$(git show "$P:pyproject.toml")" &amp;&amp; [ "$(printf '%s\n' "$PP" | sed -n 7p)" = 'version = "0.9.2"' ] &amp;&amp; [ -z "$(git diff --name-only "$A" "$P" -- typsphinx tests)" ] &amp;&amp; [ -z "$(git diff --name-only "$MB" "$P" -- .github)" ] &amp;&amp; git fetch -q origin main &amp;&amp; [ "$(git merge-base "$P" origin/main)" = "$MB" ] &amp;&amp; [ -z "$(git diff --name-only HEAD "$P" -- . ':(exclude).planning')" ] &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json workflowName -q .workflowName)" = CI ] &amp;&amp; [ "$(gh run view "$RUNID" --json event -q .event)" = workflow_dispatch ] &amp;&amp; PA="$(k PUSH_AT)" &amp;&amp; [ -n "$PA" ] &amp;&amp; [[ "$(gh run view "$RUNID" --json createdAt -q .createdAt)" &gt; "$PA" ]] &amp;&amp; for s in '## Head check and provisioning' '## Wave-1 gate' '## Tip identity and fence' '## Decoy census (D-10)' '## Push' '## Dispatch'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; DN="$(git diff --name-only "$B")" &amp;&amp; ! printf '%s\n' "$DN" | grep -vxF -e "$F" -e "$D/71-04-SUMMARY.md" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_71_04`, `CODE_FREEZE_ANCHOR` or `MILESTONE_BASE` missing
- `PUSHED_SHA` not 40 characters, or the origin head not equal to it
- `ORIGIN_BEFORE` missing or not an ancestor of the pushed tip (a non-fast-forward)
- the decoy present on origin
- a tag pointing at the pushed tip, the v0.9.2 tag missing from origin, or a v0.9.3 or v0.9.4 tag on origin
- the pushed CHANGELOG.md region lacking four bullets or the five-ID span
- the pushed `pyproject.toml:7` not 0.9.2
- a typsphinx/ or tests/ change since the anchor, or a `.github/` change since the milestone base
- the pushed tip's merge-base with origin/main differing from `MILESTONE_BASE`
- the pushed product tree differing from HEAD
- no `RUN_ID`, or the run's headSha, workflow, event or creation time not matching
- another tracked file changed
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Decoy census (D-10)` records both listings immediately before the push, plus any deletion of a decoy that is already an ancestor, with its ancestry result.
    - `## Tip identity and fence` records `PUSHED_SHA`, the empty product diff against HEAD, the 0.9.2 version line, the empty code diff from `CODE_FREEZE_ANCHOR`, the empty `.github/` diff, the merge-base equal to `MILESTONE_BASE`, no tag, and `LOCK_RUFF_VERSION`.
    - `## Push` records `ORIGIN_BEFORE` as an ancestor (`exit:0`), `PUSH_AT`, the verbatim `git push --no-follow-tags origin gsd/v0.9.4-typing-modernization` output, and the origin head equal to `PUSHED_SHA`.
    - `## Dispatch` records exactly one `gh workflow run CI --ref gsd/v0.9.4-typing-modernization`, plus `RUN_ID` and `RUN_URL`, with `headSha` equal to `PUSHED_SHA`.
  </acceptance_criteria>
  <done>The phase tip carrying the CHANGELOG edit is on origin as a fast-forward, with no decoy and no tag, and one CI run is dispatched against exactly that tip.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the run to completion in the foreground, transcribe all 12 jobs, name both windows-latest and both macos-latest lanes, compare job names with Phase 70's run, and quote ruff's CI verdict</name>
  <precondition>The `RUN_ID` recorded by Task 1 resolves with `gh run view <id> --json databaseId`. If this executor resumes after returning mid-wait, it reuses that id; exactly one `workflow_dispatch` CI run on the branch carries `headSha` equal to `PUSHED_SHA`.</precondition>
  <reversibility rating="reversible">Read-only observation of a run already dispatched.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md (Task 1: `RUN_ID`, `PUSHED_SHA`, `LOCK_RUFF_VERSION`)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CI-EVIDENCE.md § "Job census" and the ruff-verdict section (the shape to mirror)
    - .github/workflows/ci.yml lines 10-72 (the matrix, the names, `Run lint with tox` at line 69)
  </read_first>
  <action>
1. **Wait in the foreground.**
   - Run `timeout 590 gh run watch "$RUNID" --interval 30` in a Bash call with `timeout` 600000 and no `run_in_background`.
   - After each return, read `gh run view "$RUNID" --json status,conclusion`.
   - If `status` is not `completed`, issue the same pair again. Do not end the turn and do not return control while it is in progress.
   - Overall cap: 90 minutes of wall clock. If it is reached, record `RUN_ID` and the current status in the evidence and return a blocking checkpoint. Never dispatch a second run.

2. **`## Run`.** Record `gh run view "$RUNID" --json status,conclusion,workflowName,headSha,url,createdAt,updatedAt`, and the keys `RUN_HEAD_SHA` and `RUN_CONCLUSION`.

3. **`## Job census`.** Record `gh run view "$RUNID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'` as a numbered table covering every row. Keys: `JOB_COUNT = 12` and `NON_SUCCESS_JOBS = 0`.

   The census derived from `ci.yml`:
   - six `Test Python …` jobs (3 OS × 2 Python)
   - `Lint and Format Check`, `Type Check`, `Code Coverage` and `Build Package`
   - `Integration Test - basic` and `Integration Test - advanced`

   Then compare the job names as a sorted set with Phase 70's run: record `gh run view 34742047126 --json jobs --jq '[.jobs[].name] | sort | join("|")'` beside the same query for `RUN_ID`. Key `JOB_NAMES_MATCH_PHASE70 = yes` when they are equal. A difference means `ci.yml` changed since Phase 70, which constraint 12 forbids; write `## HALT: ci.yml job set changed` with both lists.

4. **`## windows-latest lanes`** and **`## macos-latest lanes`.** Two sub-tables naming `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each with its conclusion.

5. **`## ruff's verdict`.** Take the `Lint and Format Check` job id from the run's jobs. From `gh run view --job <id> --log`, quote:
   - the `+ ruff==` line of the `Install dependencies` step, key `CI_RUFF_VERSION`, which must equal `LOCK_RUFF_VERSION`
   - the `Run lint with tox` step's `commands[0]> black --check .` line and its verdict
   - its `commands[1]> ruff check .` line and its verdict
   - `lint: OK`

   CI is the lint authority (constraint 8). The lint step of `ci.yml` is `Run lint with tox`; the release workflow's lint step has a different name, and this plan never searches that workflow and never triggers it. 71-03's local ruff run is set beside this one in 71-07's handoff, not here.

6. **`## Dispatch count and no release run`.** Record:
   - `gh run list --workflow=ci.yml --branch gsd/v0.9.4-typing-modernization --event workflow_dispatch --limit 50 --json headSha`, showing exactly one run at `PUSHED_SHA`, key `DISPATCH_COUNT = 1`
   - `gh run list --workflow=release.yml --limit 20 --json headSha`, showing none at `PUSHED_SHA`, key `RELEASE_RUNS_AT_PUSHED = 0`

7. **`## D-10 final tip`.** State:
   - this run is D-10's single dispatch
   - `PUSHED_SHA` carries every product-tree change of the phase, the CHANGELOG edit
   - every later commit of this phase touches only `.planning/`, which no CI job reads, and plan 71-06 proves it with an empty product-tree diff from `PUSHED_SHA`
   - D-10 permits a second dispatch only for a code-affecting change, so none is warranted

8. **`## SC#3 CI verdict`.** Write `SC3_CI_VERDICT = MET` only when the run is completed and success, `JOB_COUNT = 12`, `NON_SUCCESS_JOBS = 0`, `JOB_NAMES_MATCH_PHASE70 = yes`, and all four named lanes are success.
   - If any job is not success: transcribe every job literally and keep the run. Do not rerun, dispatch again or edit anything. Write `SC3_CI_VERDICT = NOT-MET`, and return a blocking checkpoint naming each failing job and step.

9. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; F="$D/71-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.4-typing-modernization &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; [ "$(gh run view "$RUNID" --json status -q .status)" = completed ] &amp;&amp; [ "$(gh run view "$RUNID" --json conclusion -q .conclusion)" = success ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '.jobs | length')" = 12 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.conclusion != "success")] | length')" = 0 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("windows-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("macos-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name == "Lint and Format Check" and .conclusion == "success")] | length')" = 1 ] &amp;&amp; JN="$(gh run view "$RUNID" --json jobs -q '[.jobs[].name] | sort | join("|")')" &amp;&amp; [ -n "$JN" ] &amp;&amp; [ "$JN" = "$(gh run view 34742047126 --json jobs -q '[.jobs[].name] | sort | join("|")')" ] &amp;&amp; for n in 'Test Python 3.12 on windows-latest' 'Test Python 3.13 on windows-latest' 'Test Python 3.12 on macos-latest' 'Test Python 3.13 on macos-latest' 'Lint and Format Check' 'Run lint with tox' 'Integration Test - basic' 'Integration Test - advanced'; do grep -qF "$n" "$F" || { echo "missing: $n"; exit 1; }; done &amp;&amp; CR="$(k CI_RUFF_VERSION)" &amp;&amp; [ -n "$CR" ] &amp;&amp; [ "$CR" = "$(k LOCK_RUFF_VERSION)" ] &amp;&amp; UL="$(git show "$P:uv.lock")" &amp;&amp; [ "$(printf '%s\n' "$UL" | grep -A1 -xF 'name = "ruff"' | sed -n 's/^version = "\(.*\)"$/\1/p')" = "$CR" ] &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch "$BR" --event workflow_dispatch --limit 50 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; [ "$(grep -c 'Run linters' "$F")" = 0 ] &amp;&amp; [ "$(k JOB_COUNT)" = 12 ] &amp;&amp; [ "$(k NON_SUCCESS_JOBS)" = 0 ] &amp;&amp; [ "$(k DISPATCH_COUNT)" = 1 ] &amp;&amp; [ "$(k RELEASE_RUNS_AT_PUSHED)" = 0 ] &amp;&amp; [ "$(k JOB_NAMES_MATCH_PHASE70)" = yes ] &amp;&amp; [ "$(k SC3_CI_VERDICT)" = MET ] &amp;&amp; for s in '## Run' '## Job census' '## windows-latest lanes' '## macos-latest lanes' "## ruff's verdict" '## Dispatch count and no release run' '## D-10 final tip' '## SC#3 CI verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing: <name>` / `missing section: <name>` printed. Each of these makes it fail:
- the run not completed, or its conclusion not success
- its headSha differing from `PUSHED_SHA`
- a job count other than 12, or any job not success
- a windows-latest or macos-latest count other than 2
- `Lint and Format Check` not success
- the sorted job-name set differing from Phase 70's run 34742047126
- a lane, job or step name absent from the evidence
- `CI_RUFF_VERSION` empty or differing from the ruff version of uv.lock at the pushed tip
- more than one dispatched run at `PUSHED_SHA`, or a release.yml run at it
- the release workflow's lint step name present in the evidence
- a count, match or verdict key wrong, or a section missing
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Run` records `status`, `conclusion`, `workflowName`, `headSha` and `url`, and `## Job census` holds a numbered table of all twelve jobs, with `JOB_COUNT = 12`, `NON_SUCCESS_JOBS = 0` and `JOB_NAMES_MATCH_PHASE70 = yes`.
    - The two lane sub-tables name `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each success.
    - `## ruff's verdict` quotes the `Run lint with tox` step's black and ruff lines and `lint: OK`, with `CI_RUFF_VERSION` equal to `LOCK_RUFF_VERSION`.
    - `DISPATCH_COUNT = 1` and `RELEASE_RUNS_AT_PUSHED = 0`.
    - `## D-10 final tip` states why no second dispatch is warranted, and `SC3_CI_VERDICT = MET`.
  </acceptance_criteria>
  <done>One completed, all-green, twelve-job CI run on this phase's tip is transcribed job by job. Both Windows and both macOS lanes are named, the job set matches Phase 70's run, and ruff's verdict comes from CI at the lock's own version.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`71-01-PLAN.md`. This plan carries P1, P5 and P6, and it authors the `ordering`, `idempotency` and
`concurrency` edges. The last is a backstop marker, because the parallel siblings' independence
holds by construction rather than by a single observable check.

It also carries residual assumption (e): the local canonical ref is assumed not to move between the
census and the push. If it does move, `PUSHED_SHA` records what was pushed, the run is bound to it by
`headSha`, and the empty product diff against HEAD shows whether the dispatched tree is this
phase's. Phase accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local refs → origin | a push publishes refs; a decoy, a force, another ref or a tag would publish something this phase must not |
| gh CLI → GitHub Actions | a dispatch starts remote work; the wrong workflow would begin a publish pipeline |
| CI log → committed evidence | quoted job-log lines enter the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-16 | Tampering | pushing the decoy, a force-push, another ref, or a tag riding along | high | mitigate | Decoy census immediately before the push, then `git push --no-follow-tags origin gsd/v0.9.4-typing-modernization` with `ORIGIN_BEFORE` required as an ancestor. The verify requires no decoy on origin, no tag at the pushed tip, and no v0.9.3 or v0.9.4 tag on origin. |
| T-71-17 | Elevation of Privilege | triggering `release.yml` or `update-pin.yml` (publish pipelines) | high | mitigate | Only `gh workflow run CI` is ever issued; the verify asserts no `release.yml` run carries `PUSHED_SHA`; a prohibition forbids every route. |
| T-71-18 | Repudiation | citing Phase 70's run `34742047126` or any other stale run as this phase's green, or a second dispatch masking a red first one | high | mitigate | `RUN_ID`'s `headSha` must equal `PUSHED_SHA` and its `createdAt` must follow `PUSH_AT`; exactly one dispatched run at that SHA; a red run is kept and escalated. Phase 70's run is read only for its job-name set. |
| T-71-19 | Denial of Service | a stale `uv.lock` failing all twelve lanes before any signal (ASVS V14, L1) | medium | mitigate | The locked sync must exit 0 before the push; drift halts, and the lock is never regenerated. |
| T-71-20 | Tampering | a code or workflow change riding on the pushed tip after Phase 70's verification (D-11, constraint 12) | high | mitigate | Empty diff from `CODE_FREEZE_ANCHOR` over `typsphinx/` and `tests/`, empty `.github/` diff from `MILESTONE_BASE`, and a job-name set equal to Phase 70's run. |
| T-71-21 | Information Disclosure | CI log excerpts in committed evidence | low | accept | The repository and its Actions logs are public, and only tool-output lines (versions, lint verdicts) are quoted. |
| T-71-SC | Tampering | package installs | low | accept | Provisioning and the locked check install only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `71-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`71-CI-EVIDENCE.md`, with the keys `BASE_71_04`, `SCRATCH_71_04`, `PUSHED_SHA`, `LOCK_RUFF_VERSION`,
`ORIGIN_BEFORE`, `PUSH_AT`, `RUN_ID`, `RUN_URL`, `RUN_HEAD_SHA`, `RUN_CONCLUSION`, `JOB_COUNT`,
`NON_SUCCESS_JOBS`, `JOB_NAMES_MATCH_PHASE70`, `CI_RUFF_VERSION`, `DISPATCH_COUNT`,
`RELEASE_RUNS_AT_PUSHED` and `SC3_CI_VERDICT`. Outside the repository it fast-forwards
`origin/gsd/v0.9.4-typing-modernization` to `PUSHED_SHA` and creates one `workflow_dispatch` CI run.
It modifies no product-tree file. No file named `71-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- The canonical branch is on origin at `PUSHED_SHA` as a fast-forward, with no decoy and no tag. The
  pushed tree carries the four CHANGELOG bullets, and nothing under `typsphinx/`, `tests/` or the
  workflows changed.
- One `workflow_dispatch` CI run at `PUSHED_SHA` completed with all twelve jobs green, both
  `windows-latest` and both `macos-latest` lanes named, the job set equal to Phase 70's, and ruff's
  verdict quoted from `Run lint with tox` at the lock's version.
- No `release.yml` run at `PUSHED_SHA`, and exactly one CI dispatch.
</verification>

<success_criteria>
ROADMAP SC#3's CI half: one fresh three-OS run dispatched on this phase's own tip, every job
conclusion transcribed, both `windows-latest` and both `macos-latest` lanes named, and `ruff` green
in `Lint and Format Check` (D-10).
</success_criteria>

<output>
Create `.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-04-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-09).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run any GSD state-writing verb.
- The first section states `PUSHED_SHA`, `ORIGIN_BEFORE`, `RUN_ID`, `RUN_URL`, `JOB_COUNT`,
  `NON_SUCCESS_JOBS`, `JOB_NAMES_MATCH_PHASE70`, `CI_RUFF_VERSION` and `SC3_CI_VERDICT`. If the run
  was not all green, that section names the failing jobs first and states that SC#3 awaits the owner.
</output>
