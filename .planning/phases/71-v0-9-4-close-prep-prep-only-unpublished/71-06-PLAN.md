---
phase: 71-v0-9-4-close-prep-prep-only-unpublished
plan: 06
type: execute
wave: 3
depends_on: ["71-02", "71-03", "71-04", "71-05"]
files_modified:
  - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md
autonomous: true
requirements: [REL-13]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'SC#1, observation 2 of 2: every probe of observation 1 is re-run at OBS2_AT, later than OBS1_AT, with the same positive controls against v0.9.2, and the fence still holds. No v0.9.3 or v0.9.4 tag, PyPI release or GitHub Release exists, no release.yml run exists since 2026-08-31, and no pull request has the milestone branch as its head (D-08, D-11).'
    - 'SC#1 phase-scoped fence: `git diff PHASE_BASE_SHA HEAD -- typsphinx/ tests/` is empty. The same diff widened to every non-.planning path lists exactly CHANGELOG.md with zero deletions, which is the positive control on the anchor.'
    - 'D-10 final tip: PUSHED_SHA from 71-CI-EVIDENCE.md is an ancestor of HEAD, and `git diff --name-only PUSHED_SHA HEAD` outside .planning/ is empty, so every commit after the single CI dispatch is planning-only. Exactly one CI dispatch and no release.yml run carry PUSHED_SHA.'
    - 'D-11 part 1 on the close tip: the last commit touching typsphinx/ or tests/ is still CODE_FREEZE_ANCHOR, and the diff from it to HEAD over those paths is empty. The same pathspec still lists ten files across Phase 70''s own range, the positive control. D-05: the merge-base of HEAD and origin/main still equals MILESTONE_BASE.'
    - 'D-11 part 2: Phase 70''s masked-AST harness is extracted from 70-MASK-PILOT-EVIDENCE.md and hashes to its recorded MASK_HARNESS_SHA256. It re-runs on the close tip under CPython 3.13.13, and for each of the ten converted files the masked hash at Phase 70''s PHASE_BASE_SHA 697a1132 equals the masked hash on the close tip (D11_MASK_EQUAL_COUNT = 10).'
    - 'D-11 part 2 is non-vacuous. The raw bytes of all ten files differ between 697a1132 and HEAD (D11_RAW_DIFFER_COUNT = 10). A copy of translator.py with its first class renamed hashes differently (CONTROL_RENAME_71 = DIFFER), and so does a copy of tests/test_include_ledger_removal_gate.py without its `import textwrap` line (CONTROL_IMPORT_71 = DIFFER). Each close-tip hash also appears in 70-AFTER-STATIC-EVIDENCE.md''s leg (a) table, computed on the same interpreter version (PHASE70_TABLE_MATCH = 10).'
    - 'Edge (encoding): the masked equality compares SHA-256 digests of `ast.dump` text produced by one interpreter on both sides, not raw bytes, which differ by design; the product-tree fences compare paths and bytes with git.'
    - 'Edge (empty): every empty result in observation 2 and in the code-freeze diff is paired with a positive control from the same source.'
    - 'REL-13 is cited for coverage only; SUMMARY requirements-completed: [] (D-09).'
  artifacts:
    - path: .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md
      provides: 'observation 2 of 2, the phase-scoped product diff, the post-dispatch planning-only proof, D-11 part 1 on the close tip, and the D-11 part 2 masked-AST re-run with its non-vacuity controls'
      contains: 'D11_VERDICT = '
  key_links:
    - from: '70-MASK-PILOT-EVIDENCE.md mask-harness block'
      to: 'the close-tip masked hashes'
      via: 'sed extraction, sha256 equal to MASK_HARNESS_SHA256, run with uv run python -c'
      pattern: 'MASK_HARNESS_SHA256'
    - from: '71-CI-EVIDENCE.md PUSHED_SHA'
      to: 'the post-dispatch planning-only proof'
      via: 'git diff --name-only PUSHED_SHA HEAD outside .planning must be empty'
      pattern: 'PUSHED_SHA = '
    - from: 'CODE_FREEZE_ANCHOR (observation 1)'
      to: 'the close-tip code-freeze diff and the ten-file list the harness runs over'
      via: 'KEY = value line read with sed'
      pattern: 'CODE_FREEZE_ANCHOR = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action: every command is a read or a local hash, and nothing is pushed, tagged, released or dispatched.'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record a masked hash, digest or count that was not computed here: Phase 70''s recorded hashes are only compared against, never transcribed as this phase''s measurement, and the harness is extracted and hash-checked, never retyped.'
      requirement_id: REL-13
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT change any file under typsphinx/, tests/, pyproject.toml or uv.lock; the non-vacuity controls operate on copies in a scratch directory only (D-11).'
      requirement_id: REL-13
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Close SC#1 on the phase's close tip:
- re-probe the unpublished shape at a genuinely later timestamp than observation 1, with the same
  positive controls
- prove, with a positive control, that nothing outside `CHANGELOG.md` and `.planning/` changed across
  the phase
- prove that every commit after the single CI dispatch is planning-only (D-10)
- run D-11 part 1 on the close tip: the code-freeze diff since Phase 70's final code commit, against
  a positively controlled anchor
- run D-11 part 2: Phase 70's masked-AST harness against Phase 70's `PHASE_BASE_SHA` for the ten
  converted files, with non-vacuity controls

Purpose: this plan runs in wave 3, one wave after the CI dispatch (71-04), the green tree (71-03)
and the pre-flight (71-05). It is therefore the first plan that can read all their evidence, and its
observation is separated from observation 1 by two waves and a full CI run. D-11 is new to this
phase, since Phase 69 had no preceding code phase in its milestone. It shows that no code change
slipped in after Phase 70's verification.

Output: `71-SC1-INVARIANTS.md` § observation 2, § the phase-scoped product diff, § commits after the
CI dispatch, § the close-tip code freeze, and § the masked-AST re-run.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-RESEARCH.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md
@.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-STATIC-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run project tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution"). `--python 3.13.13`
matters for this plan in particular: `ast.dump` output can differ between CPython versions, and
3.13.13 is the version Phase 70's leg (a) table was computed on (`VENV_VERSION_INFO = 3.13.13`), so
the close-tip hashes can be cross-checked against that table as well as against the base.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line. The verify fails on any key line containing an opening parenthesis. Phase 69's executor
appended explanations to five values although its plan said not to.

**Scratch files.** Parallel executors share the session scratchpad. Put the non-vacuity copies and
every captured output under your own `mktemp -d` directory, recorded as `SCRATCH_71_06`, with a
`p7106_` prefix. The tracked tree is never edited for a control.

**Locale.** The host runs `LANG=ja_JP.UTF-8`; prefix `LC_ALL=C` to any `git` command whose message you
quote for comparison.

**Reserved name.** Never create `71-VERIFICATION.md`; `gsd-verifier` owns it and overwrites it
wholesale.
</worktree_provisioning>

<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: v0.9.4 -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[34] -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
The version tags are named because the probes assert their absence. `## HALT` is written only on a
failure path.

## Planning-time census (2026-09-13; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| Mask harness | the `~~~python mask-harness` block at `70-MASK-PILOT-EVIDENCE.md:206`, `MASK_HARNESS_SHA256 = 11cdbeb68cae48a890dfc98736ae2ff7fc15c4557cddad6c5f3a06f425db8a65` (`:252`); Phase 70 extracted it with two `sed` passes (the fenced range, then the first and last lines removed) and ran it as a shell function passing the text to `uv run python -c` with source on stdin | `70-MASK-PILOT-EVIDENCE.md` § "Mask harness" |
| Phase 70 base | `PHASE_BASE_SHA = 697a113221a8a267d7e8c6dd1f2b95672f9454d2` | `70-BASELINE-EVIDENCE.md:4` |
| Converted files | ten: `tests/conftest.py`, `tests/test_bundle_layout_sweep_gate.py`, `tests/test_include_edge_derivation_unit.py`, `tests/test_include_ledger_removal_gate.py`, `typsphinx/__init__.py`, `typsphinx/builder.py`, `typsphinx/template_engine.py`, `typsphinx/template_registry.py`, `typsphinx/translator.py`, `typsphinx/writer.py` | `git diff --name-only 697a1132 e721ff89 -- typsphinx/ tests/` |
| Phase 70 leg (a) table | ten rows, all EQUAL, on CPython 3.13.13 | `70-AFTER-STATIC-EVIDENCE.md` § "Leg (a) — every converted file" |
| Phase 70 non-vacuity controls | a translator.py copy with `class TypstTranslator(` renamed to `class TypstTranslatorZZZ(` (string replace, count 1) → DIFFER; a ledger-gate copy without `import textwrap` (line 45) → DIFFER | `70-MASK-PILOT-EVIDENCE.md` § "Non-vacuity controls" |
| Code-freeze anchor | `e721ff899a981eafdad696ef1a9c93aaab41ece5`, recorded as `CODE_FREEZE_ANCHOR` by 71-02 | `71-SC1-INVARIANTS.md` (wave 1) |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: SC#1 observation 2 of 2, the phase-scoped product diff, the post-dispatch planning-only proof, and D-11 part 1 on the close tip</name>
  <precondition>Plans 71-02 to 71-05 are merged into this worktree's HEAD: their evidence files exist without a HALT heading, `71-CI-EVIDENCE.md` reads `SC3_CI_VERDICT = MET`, `71-GREEN-TREE-EVIDENCE.md` reads `SC3_LOCAL_VERDICT = MET`, and `71-PREFLIGHT-EVIDENCE.md` reads `TRIAL_MERGE_VERDICT = MET`. Otherwise write `## HALT: wave 2 incomplete` naming the missing item, and stop.</precondition>
  <reversibility rating="reversible">Evidence appends recording read-only probes.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md</files>
  <read_first>
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md (observation 1: `OBS1_AT`, the probes, the anchors, § "Handoff to observation 2")
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md (`PHASE_BASE_SHA`)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md (`PUSHED_SHA`, `RUN_ID`, § "D-10 final tip")
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md § "Observation 2 of 2" onward (the shape to mirror)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-05, D-10 and D-11
  </read_first>
  <action>
Append to `71-SC1-INVARIANTS.md`.

1. **`## Observation 2 of 2`.**
   - Record the head check and provisioning, then `BASE_71_06 = ` from `git rev-parse HEAD` before any commit, `SCRATCH_71_06 = ` from `mktemp -d`, and `OBS2_AT = ` from `date -u +"%Y-%m-%dT%H:%M:%SZ"`.
   - Re-run every probe of observation 1 in the same order, with the same positive controls, each as a command with its verbatim output. Use the same key names with an `OBS2_` prefix: `OBS2_VERSION_LINE`, `OBS2_LOCAL_V093_TAGS`, `OBS2_LOCAL_V094_TAGS`, `OBS2_REMOTE_V092_TAG`, `OBS2_REMOTE_V093_TAG`, `OBS2_REMOTE_V094_TAG`, `OBS2_PYPI_092_HTTP`, `OBS2_PYPI_093_HTTP`, `OBS2_PYPI_094_HTTP`, `OBS2_GH_LATEST`, `OBS2_GH_V093_RELEASES`, `OBS2_GH_V094_RELEASES`, `OBS2_RELEASE_RUNS_SINCE_V092` and `OBS2_BRANCH_PRS`.
   - Quote `OBS1_AT` beside `OBS2_AT` and state the elapsed interval.
   - State that the fence held across the whole phase, including after 71-04's push and dispatch, and that both unpublished numbers remain unclaimed (D-08).

2. **`## The phase-scoped product diff`.** Read `PHASE_BASE_SHA` from `71-CLOSEOUT-GUARD.md` and record:
   - `git diff "$PHASE_BASE_SHA" HEAD -- typsphinx/ tests/`, empty, key `PHASE_CODE_DIFF = empty`
   - `git diff --numstat "$PHASE_BASE_SHA" HEAD -- . ':(exclude).planning'`: exactly one row, `CHANGELOG.md`, with a non-zero addition count and zero deletions, key `PHASE_PRODUCT_FILES = CHANGELOG.md`

   State why both are needed: the empty scoped diff is the claim, and the non-empty widened diff from the same anchor proves the anchor is real.

3. **`## Commits after the CI dispatch`.** Read `PUSHED_SHA` from `71-CI-EVIDENCE.md` and record:
   - `git merge-base --is-ancestor "$PUSHED_SHA" HEAD; echo "exit:$?"`, which must print `exit:0`
   - `git log --oneline "$PUSHED_SHA"..HEAD`
   - `git diff --name-only "$PUSHED_SHA" HEAD -- . ':(exclude).planning'`, empty, key `POST_DISPATCH_PRODUCT_FILES = 0`
   - `gh run list --workflow=release.yml --limit 20 --json headSha`, none at `PUSHED_SHA`
   - `gh run list --workflow=ci.yml --branch gsd/v0.9.4-typing-modernization --event workflow_dispatch --limit 50 --json headSha,createdAt`, exactly one run at `PUSHED_SHA`

   State the D-10 reading. The single dispatch ran on the tip carrying every product-tree change of the phase, and every later commit is `.planning/`-only, which no CI job reads. D-10 permits a second dispatch only for a code-affecting change, so the post-dispatch commits are not a stale-tip violation.

4. **`## Code freeze on the close tip (D-11 part 1)`.** Read `CODE_FREEZE_ANCHOR` and `MILESTONE_BASE` from observation 1's sections and record:
   - `git log --format='%H %s' -1 -- typsphinx/ tests/`, which must print `CODE_FREEZE_ANCHOR`. If it prints another SHA, write `## HALT: code changed after Phase 70` with the output quoted, and stop.
   - `git diff --stat "$CODE_FREEZE_ANCHOR" HEAD -- typsphinx/ tests/`, empty, key `CLOSE_CODE_FREEZE_DIFF_FILES = 0`
   - the positive control `git diff --name-only 697a113221a8a267d7e8c6dd1f2b95672f9454d2 "$CODE_FREEZE_ANCHOR" -- typsphinx/ tests/`, ten files, key `CLOSE_ANCHOR_CONTROL_FILES = 10`
   - after `git fetch origin main`, `git merge-base HEAD origin/main`, equal to `MILESTONE_BASE`, key `CLOSE_MAIN_ABSORBED = no` (D-05)

5. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; S="$D/71-SC1-INVARIANTS.md" &amp;&amp; B70=697a113221a8a267d7e8c6dd1f2b95672f9454d2 &amp;&amp; BR=gsd/v0.9.4-typing-modernization &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; ks() { sed -n "s/^$1 = //p" "${2:-$S}" | head -n 1; } &amp;&amp; B="$(ks BASE_71_06)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; O1="$(ks OBS1_AT)" &amp;&amp; O2="$(ks OBS2_AT)" &amp;&amp; [ -n "$O1" ] &amp;&amp; [ -n "$O2" ] &amp;&amp; [[ "$O2" &gt; "$O1" ]] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; [ -n "$(git tag -l 'v0.9.2')" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.3')" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.4')" ] &amp;&amp; T="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.[34]')" = 0 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json)" = 200 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.3/json)" = 404 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.4/json)" = 404 ] &amp;&amp; [ "$(gh release list --limit 20 --json tagName,isLatest --jq '.[] | select(.isLatest) | .tagName')" = v0.9.2 ] &amp;&amp; [ "$(gh release list --limit 50 --json tagName --jq '[.[] | select(.tagName == "v0.9.3" or .tagName == "v0.9.4")] | length')" = 0 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json databaseId --jq '[.[] | select(.databaseId == 33318905691)] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headBranch,createdAt --jq '[.[] | select(.headBranch == "v0.9.3" or .headBranch == "v0.9.4" or .createdAt &gt; "2026-08-31")] | length')" = 0 ] &amp;&amp; [ "$(gh pr list --state all --limit 5 --json number --jq 'length')" -ge 1 ] &amp;&amp; [ "$(gh pr list --head "$BR" --state all --json number --jq 'length')" = 0 ] &amp;&amp; PB="$(ks PHASE_BASE_SHA "$D/71-CLOSEOUT-GUARD.md")" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; [ -z "$(git diff --name-only "$PB" HEAD -- typsphinx tests)" ] &amp;&amp; [ "$(git diff --name-only "$PB" HEAD -- . ':(exclude).planning')" = CHANGELOG.md ] &amp;&amp; NS="$(git diff --numstat "$PB" HEAD -- CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$NS" | cut -f2)" = 0 ] &amp;&amp; P="$(ks PUSHED_SHA "$D/71-CI-EVIDENCE.md")" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$P" HEAD &amp;&amp; [ -z "$(git diff --name-only "$P" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha --jq '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch "$BR" --event workflow_dispatch --limit 50 --json headSha --jq '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; A="$(ks CODE_FREEZE_ANCHOR)" &amp;&amp; [ "${#A}" = 40 ] &amp;&amp; [ "$(git log --format=%H -1 -- typsphinx tests)" = "$A" ] &amp;&amp; [ -z "$(git diff --name-only "$A" HEAD -- typsphinx tests)" ] &amp;&amp; CL="$(git diff --name-only "$B70" "$A" -- typsphinx tests)" &amp;&amp; [ "$(printf '%s\n' "$CL" | wc -l)" = 10 ] &amp;&amp; git fetch -q origin main &amp;&amp; [ "$(git merge-base HEAD origin/main)" = "$(ks MILESTONE_BASE)" ] &amp;&amp; for x in OBS2_LOCAL_V093_TAGS OBS2_LOCAL_V094_TAGS OBS2_REMOTE_V093_TAG OBS2_REMOTE_V094_TAG OBS2_GH_V093_RELEASES OBS2_GH_V094_RELEASES OBS2_RELEASE_RUNS_SINCE_V092 OBS2_BRANCH_PRS POST_DISPATCH_PRODUCT_FILES CLOSE_CODE_FREEZE_DIFF_FILES; do [ "$(ks "$x")" = 0 ] || { echo "key not 0: $x"; exit 1; }; done &amp;&amp; [ "$(ks OBS2_REMOTE_V092_TAG)" = 1 ] &amp;&amp; [ "$(ks OBS2_PYPI_092_HTTP)" = 200 ] &amp;&amp; [ "$(ks OBS2_PYPI_093_HTTP)" = 404 ] &amp;&amp; [ "$(ks OBS2_PYPI_094_HTTP)" = 404 ] &amp;&amp; [ "$(ks OBS2_GH_LATEST)" = v0.9.2 ] &amp;&amp; [ "$(ks CLOSE_ANCHOR_CONTROL_FILES)" = 10 ] &amp;&amp; [ "$(ks PHASE_PRODUCT_FILES)" = CHANGELOG.md ] &amp;&amp; [ "$(ks PHASE_CODE_DIFF)" = empty ] &amp;&amp; [ "$(ks CLOSE_MAIN_ABSORBED)" = no ] &amp;&amp; for s in '## Observation 2 of 2' '## The phase-scoped product diff' '## Commits after the CI dispatch' '## Code freeze on the close tip (D-11 part 1)'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; DN="$(git diff --name-only "$B")" &amp;&amp; ! printf '%s\n' "$DN" | grep -vxF -e "$S" -e "$D/71-06-SUMMARY.md" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$S" &amp;&amp; ! grep -q '^## HALT' "$S"</automated>
    <fails_when>Non-zero exit, or `key not 0: <key>` / `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_71_06` missing, or `OBS2_AT` empty or not later than `OBS1_AT`
- `pyproject.toml:7` not 0.9.2
- the v0.9.2 positive controls missing, or any v0.9.3 or v0.9.4 tag, PyPI release, GitHub Release, recent release.yml run or pull request from the branch present
- the latest release not v0.9.2
- `PHASE_BASE_SHA` unreadable, a typsphinx/ or tests/ change since it, or the widened diff not exactly CHANGELOG.md with zero deletions
- `PUSHED_SHA` unreadable, not an ancestor of HEAD, or followed by a product-tree change
- a release.yml run at `PUSHED_SHA`, or other than one CI dispatch at it
- the last code commit not the anchor, a code change after it, or the anchor control not ten files
- the merge-base with origin/main differing from `MILESTONE_BASE`
- a recorded key contradicting its probe, a section missing, or another tracked file changed
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Observation 2 of 2` records `OBS2_AT`, later than `OBS1_AT`, with the elapsed interval, and repeats every probe of observation 1 with its positive control under an `OBS2_` key.
    - `## The phase-scoped product diff` shows the empty scoped diff and the widened `--numstat` diff listing exactly `CHANGELOG.md` with zero deletions.
    - `## Commits after the CI dispatch` shows `PUSHED_SHA` as an ancestor, the post-dispatch commit list, `POST_DISPATCH_PRODUCT_FILES = 0`, no release.yml run at `PUSHED_SHA`, one CI dispatch at it, and the D-10 reading.
    - `## Code freeze on the close tip (D-11 part 1)` records the last code commit equal to `CODE_FREEZE_ANCHOR`, `CLOSE_CODE_FREEZE_DIFF_FILES = 0`, `CLOSE_ANCHOR_CONTROL_FILES = 10` and `CLOSE_MAIN_ABSORBED = no`.
  </acceptance_criteria>
  <done>The fence held across the whole phase at two separated timestamps, the phase changed nothing outside `CHANGELOG.md` and `.planning/`, the single CI run covers every product-tree change, and no code changed after Phase 70.</done>
</task>

<task type="auto">
  <name>Task 2: D-11 part 2: Phase 70's masked-AST harness re-run on the close tip against Phase 70's PHASE_BASE_SHA for the ten converted files, with non-vacuity controls and a cross-check against Phase 70's table</name>
  <precondition>Task 1 is committed with no HALT heading, and `.venv/pyvenv.cfg` reads `version_info = 3.13.13`.</precondition>
  <reversibility rating="reversible">Evidence appends; the controls operate on copies in a scratch directory.</reversibility>
  <files>.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md § "Mask harness", § "Pilot (SC#3)" and § "Non-vacuity controls" (the harness, its extraction and the two controls)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-STATIC-EVIDENCE.md § "Leg (a) — every converted file" (the invocation pattern and the ten-row table)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PHASE_BASE_SHA`, `VENV_VERSION_INFO`)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-SC1-INVARIANTS.md (Task 1 and `CODE_FREEZE_ANCHOR`)
    - .planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CONTEXT.md D-11 and § "Claude's Discretion" third bullet
  </read_first>
  <action>
Append to `71-SC1-INVARIANTS.md`.

1. Re-run the head check and the provisioning line. Record `VENV_VERSION_INFO_71_06 = ` with the `version_info` value of `.venv/pyvenv.cfg`, which must be `3.13.13`.

2. **`## Masked-AST re-run on the close tip (D-11 part 2)`.** Per D-11 and Claude's discretion, this reuses Phase 70's mask: annotation subtrees plus `typing` and `collections.abc` `ImportFrom` nodes.
   - Harness: extract, from `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md`, the block between the `~~~python mask-harness` fence line and the closing `~~~` line, dropping the two fence lines. Use the same two `sed` passes Phase 70's verifies use. Hash the extracted text with `sha256sum`: it must equal the `MASK_HARNESS_SHA256` recorded in that file. Key `MASK_HARNESS_SHA256_71`. Never retype the harness.
   - Run the harness as a shell function that passes the extracted text to `uv run python -c` and reads the source on stdin.
   - The file list: `git diff --name-only 697a113221a8a267d7e8c6dd1f2b95672f9454d2 "$CODE_FREEZE_ANCHOR" -- typsphinx/ tests/`, ten paths, in a fenced block.
   - A table with one row per path: the base masked hash (the output of `git show 697a1132…:<path>` fed to the harness), the close-tip masked hash (the working-tree file fed to the harness), and the verdict. Every hash is 64 hex characters, and every verdict must be `EQUAL`. Key `D11_MASK_EQUAL_COUNT = 10`.
   - If any row differs, write `## HALT: masked-AST equality broken on the close tip` with the row quoted, and stop.

3. **`## Non-vacuity controls (D-11 part 2)`.** Work only on copies under `SCRATCH_71_06`:
   - For each of the ten paths, record `git diff --quiet 697a1132… HEAD -- <path>; echo "exit:$?"`, which must print `exit:1`: the raw bytes differ, and the mask is what makes the hashes equal. Key `D11_RAW_DIFFER_COUNT = 10`.
   - Copy `typsphinx/translator.py`, replacing the first `class TypstTranslator(` with `class TypstTranslatorZZZ(` through a `uv run python -c` string replace with a count of 1, not `sed`. Its masked hash must differ from the original's. Key `CONTROL_RENAME_71 = DIFFER`.
   - Copy `tests/test_include_ledger_removal_gate.py` with its `import textwrap` line removed the same way. Its masked hash must differ from the original's. Key `CONTROL_IMPORT_71 = DIFFER`.

   Both controls show that the mask is not vacuous: a rename and a removed non-`typing` import both change the hash.

4. **`## Cross-check with the Phase 70 leg (a) table`.** The interpreter here is 3.13.13, the version Phase 70's leg (a) table was computed on. Each close-tip hash must therefore also appear in `70-AFTER-STATIC-EVIDENCE.md` § "Leg (a) — every converted file". Record the per-file result. Key `PHASE70_TABLE_MATCH = 10`. This compares live hashes with a recorded table; it never copies a recorded hash into this file as a measurement.

5. **`## D-11 verdict`.** State what the check proves: the non-import, non-annotation structure of every converted file is unchanged since Phase 70's base. State what it does not prove, the correctness of the import lists, which mypy and the full suite in 71-03 cover. Write `D11_VERDICT = MET` only when `D11_MASK_EQUAL_COUNT`, `D11_RAW_DIFFER_COUNT` and `PHASE70_TABLE_MATCH` are all 10 and both controls DIFFER, and Task 1's `CLOSE_CODE_FREEZE_DIFF_FILES = 0` holds.

6. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished &amp;&amp; S="$D/71-SC1-INVARIANTS.md" &amp;&amp; P70=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; PE="$P70/70-MASK-PILOT-EVIDENCE.md" &amp;&amp; B0=697a113221a8a267d7e8c6dd1f2b95672f9454d2 &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = 3.13.13 ] &amp;&amp; ks() { sed -n "s/^$1 = //p" "${2:-$S}" | head -n 1; } &amp;&amp; B="$(ks BASE_71_06)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; A="$(ks CODE_FREEZE_ANCHOR)" &amp;&amp; [ "${#A}" = 40 ] &amp;&amp; H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')" &amp;&amp; [ -n "$H" ] &amp;&amp; HS="$(printf '%s\n' "$H" | sha256sum | cut -d' ' -f1)" &amp;&amp; [ "$HS" = "$(ks MASK_HARNESS_SHA256 "$PE")" ] &amp;&amp; [ "$(ks MASK_HARNESS_SHA256_71)" = "$HS" ] &amp;&amp; sk() { uv run python -c "$H"; } &amp;&amp; FL="$(git diff --name-only "$B0" "$A" -- typsphinx tests)" &amp;&amp; [ "$(printf '%s\n' "$FL" | wc -l)" = 10 ] &amp;&amp; for f in $FL; do a="$(sk &lt; "$f")" &amp;&amp; bs="$(git show "$B0:$f")" &amp;&amp; b="$(printf '%s\n' "$bs" | sk)" &amp;&amp; [ "${#a}" = 64 ] &amp;&amp; [ "$a" = "$b" ] &amp;&amp; grep -qF "$a" "$S" &amp;&amp; grep -qF "$a" "$P70/70-AFTER-STATIC-EVIDENCE.md" || { echo "mask check failed: $f"; exit 1; }; ! git diff --quiet "$B0" HEAD -- "$f" || { echo "raw bytes unchanged: $f"; exit 1; }; done &amp;&amp; [ "$(grep -cF 'class TypstTranslator(' typsphinx/translator.py)" -ge 1 ] &amp;&amp; X="$(mktemp -d)" &amp;&amp; uv run python -c "import sys; s=open(sys.argv[1]).read(); open(sys.argv[2],'w').write(s.replace('class TypstTranslator(','class TypstTranslatorZZZ(',1))" typsphinx/translator.py "$X/rename.py" &amp;&amp; [ "$(sk &lt; "$X/rename.py")" != "$(sk &lt; typsphinx/translator.py)" ] &amp;&amp; rm -rf "$X" &amp;&amp; for x in D11_MASK_EQUAL_COUNT D11_RAW_DIFFER_COUNT PHASE70_TABLE_MATCH; do [ "$(ks "$x")" = 10 ] || { echo "key not 10: $x"; exit 1; }; done &amp;&amp; [ "$(ks CONTROL_RENAME_71)" = DIFFER ] &amp;&amp; [ "$(ks CONTROL_IMPORT_71)" = DIFFER ] &amp;&amp; [ "$(ks VENV_VERSION_INFO_71_06)" = 3.13.13 ] &amp;&amp; [ "$(ks D11_VERDICT)" = MET ] &amp;&amp; for s in '## Masked-AST re-run on the close tip (D-11 part 2)' '## Non-vacuity controls (D-11 part 2)' '## Cross-check with the Phase 70 leg (a) table' '## D-11 verdict'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ pyproject.toml uv.lock)" ] &amp;&amp; DN="$(git diff --name-only "$B")" &amp;&amp; ! printf '%s\n' "$DN" | grep -vxF -e "$S" -e "$D/71-06-SUMMARY.md" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$S" &amp;&amp; ! grep -q '^## HALT' "$S"</automated>
    <fails_when>Non-zero exit, or `mask check failed: <path>`, `raw bytes unchanged: <path>`, `key not 10: <key>` or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, the shim absent, or `.venv` not on 3.13.13
- `BASE_71_06` or `CODE_FREEZE_ANCHOR` unreadable
- the extracted harness empty, or not hashing to Phase 70's `MASK_HARNESS_SHA256`, or its recorded key wrong
- the converted-file list not ten paths
- for any file: a masked hash not 64 characters, the base and close-tip hashes differing, the hash absent from this evidence file, or absent from Phase 70's leg (a) table
- a file whose raw bytes did not change since Phase 70's base (a vacuous comparison)
- the rename control not changing the hash
- a count, control, interpreter or verdict key wrong, or a section missing
- a tracked product file modified, or another tracked file changed
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Masked-AST re-run on the close tip (D-11 part 2)` records `MASK_HARNESS_SHA256_71` equal to Phase 70's recorded digest, the ten-path list, and a ten-row table of base and close-tip masked hashes, every row EQUAL, with `D11_MASK_EQUAL_COUNT = 10`.
    - `## Non-vacuity controls (D-11 part 2)` records `D11_RAW_DIFFER_COUNT = 10`, `CONTROL_RENAME_71 = DIFFER` and `CONTROL_IMPORT_71 = DIFFER`, with each command and hash.
    - `## Cross-check with the Phase 70 leg (a) table` records `PHASE70_TABLE_MATCH = 10` with the per-file result.
    - `## D-11 verdict` states what is and is not proven, and `D11_VERDICT = MET`.
  </acceptance_criteria>
  <done>Phase 70's masked-AST equality holds on the close tip for all ten converted files, measured live with a checked harness and shown non-vacuous by two mutation controls and ten raw-byte differences.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`71-01-PLAN.md`. This plan carries P1, P5 and P6, and it authors the `encoding` edge (masked-AST
equality versus raw bytes) and the `empty` edge on the close tip. It also carries residual assumption
(e): if the canonical ref moved between 71-04's census and its push, the empty product diff from
`PUSHED_SHA` to HEAD recorded here shows whether the dispatched tree is this phase's. Phase
accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → origin, GitHub API, PyPI (read-only) | observation 2's probes; nothing is written |
| recorded Phase 70 evidence → this phase's D-11 verdict | a copied hash or a retyped harness would claim equality without a measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-26 | Repudiation | a masked-AST verdict built from Phase 70's recorded hashes or a retyped harness (ASVS V7, L1) | high | mitigate | The harness is extracted and hash-checked against `MASK_HARNESS_SHA256`; the verify recomputes every base and close-tip hash live and only then looks them up in Phase 70's table. |
| T-71-27 | Repudiation | a vacuous masked equality, from identical inputs or a mask that hides everything | high | mitigate | All ten files must differ in raw bytes, and the rename control, re-run live in the verify, must change the hash. |
| T-71-28 | Repudiation | an empty code-freeze diff accepted from a wrong anchor | high | mitigate | The last code commit must be the anchor, and the same pathspec must list ten files across Phase 70's own range. |
| T-71-29 | Repudiation | post-dispatch commits read as a stale-tip violation, or a product change slipping in after the dispatch | medium | mitigate | `git diff --name-only PUSHED_SHA HEAD` outside `.planning/` must be empty; the D-10 reading is written into this file. |
| T-71-30 | Tampering | a control mutating the tracked tree | low | mitigate | Controls operate on copies under `SCRATCH_71_06`; the verify requires an empty `git status --porcelain` over the product paths. |
| T-71-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `71-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan appends to
`71-SC1-INVARIANTS.md` the sections `## Observation 2 of 2`, `## The phase-scoped product diff`,
`## Commits after the CI dispatch`, `## Code freeze on the close tip (D-11 part 1)`,
`## Masked-AST re-run on the close tip (D-11 part 2)`, `## Non-vacuity controls (D-11 part 2)`,
`## Cross-check with the Phase 70 leg (a) table` and `## D-11 verdict`, with the keys
`BASE_71_06`, `SCRATCH_71_06`, `OBS2_AT`, the fourteen `OBS2_*` probe keys, `PHASE_CODE_DIFF`,
`PHASE_PRODUCT_FILES`, `POST_DISPATCH_PRODUCT_FILES`, `CLOSE_CODE_FREEZE_DIFF_FILES`,
`CLOSE_ANCHOR_CONTROL_FILES`, `CLOSE_MAIN_ABSORBED`, `VENV_VERSION_INFO_71_06`,
`MASK_HARNESS_SHA256_71`, `D11_MASK_EQUAL_COUNT`, `D11_RAW_DIFFER_COUNT`, `CONTROL_RENAME_71`,
`CONTROL_IMPORT_71`, `PHASE70_TABLE_MATCH` and `D11_VERDICT`. It modifies no product-tree file. No
file named `71-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- Two SC#1 observations at separated timestamps, each remote probe positively controlled.
- The phase-scoped product diff is exactly `CHANGELOG.md` against a proven anchor, and every
  post-dispatch commit is planning-only.
- No code changed after Phase 70 (D-11 part 1), and Phase 70's masked-AST equality holds on the
  close tip for all ten converted files, non-vacuously (D-11 part 2).
</verification>

<success_criteria>
ROADMAP SC#1 in full: the tree is proven unpublished-shaped twice with positive controls, Phase 71's
own diff touches nothing under `typsphinx/` or `tests/`, and Phase 70's masked-AST equality is
re-verified on the close tip, so no code change slipped in after Phase 70's verification (D-11).
</success_criteria>

<output>
Create `.planning/phases/71-v0-9-4-close-prep-prep-only-unpublished/71-06-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-09).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run any GSD state-writing verb.
- The first section states `OBS1_AT`, `OBS2_AT`, `PHASE_PRODUCT_FILES`,
  `POST_DISPATCH_PRODUCT_FILES`, `CLOSE_CODE_FREEZE_DIFF_FILES`, `D11_MASK_EQUAL_COUNT`,
  `PHASE70_TABLE_MATCH` and `D11_VERDICT`. If a HALT heading was written, that section says so
  first.
</output>
