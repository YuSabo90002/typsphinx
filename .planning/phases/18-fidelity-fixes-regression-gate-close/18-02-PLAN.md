---
phase: 18-fidelity-fixes-regression-gate-close
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: []
autonomous: true
requirements: [GATE-03]
must_haves:
  truths:
    - "After the FID-01a fix (18-01) touches every table in the corpus via the uniform fr-column change (D-02), the full Sphinx-`doc/` corpus still compiles fatal-free through `typstpdf`: a non-empty `index.pdf` with valid `%PDF` magic is produced and 0 fatal errors are reported — GATE-02 non-regression (SC#2, GATE-03, D-04)."
    - "The corpus build produces a non-empty `index.pdf` (an empty or absent PDF is a red gate) — GATE-03/empty edge: the gate rejects a zero-output build."
    - "The re-run `unknown_visit` catalogue is asserted EMPTY of `todo_node` and `manpage`, confirming Phase 16's handlers still eliminate both drops on the real corpus (SC#3, GATE-03) — the check is a deterministic set/count membership independent of the order nodes are encountered (GATE-03/ordering edge)."
    - "Zero new runtime dependencies were added and no `@preview` package version was bumped: the 3-way version-sync surface (`writer.py` / `template_engine.py` / `templates/base.typ`) is unchanged and `tests/test_preview_version_sync.py` is green (SC#4)."
  artifacts:
    - path: ".planning/phases/18-fidelity-fixes-regression-gate-close/18-02-SUMMARY.md"
      provides: "Recorded GATE-03 outcome (fatal-free corpus + empty unknown_visit catalogue) and SC#4 invariant confirmation; notes if run environmentally deferred"
      contains: "GATE-03"
  key_links:
    - from: "18-01 fr-column + ZWSP translator fix"
      to: "test_corpus_gate.py::test_corpus_compiles_with_no_fatal_error"
      via: "full-corpus real `sphinx-build -b typstpdf` re-run (D-04)"
      pattern: "no_fatal_error"
    - statement: "GATE-03's fatal-only corpus re-run does NOT machine-detect a NEW *silent* (non-fatal) collision on a corpus table other than the extdev/deprecated repro; per research assumption A2 that residual is out of FID-01a's locked scope and is a future-audit candidate, not a Phase 18 regression — the confirmatory human visual check on extdev/deprecated is the residual guard."
      verification: backstop
  prohibitions:
    - "MUST NOT modify `writer.py` / `template_engine.py` / `templates/base.typ` `@preview` version declarations, nor add any dependency to `pyproject.toml`, to make the gate pass (SC#4 milestone invariant)."
    - "MUST NOT weaken or delete the empty-`unknown_visit`-catalogue assertion in `test_corpus_gate.py` to force GATE-03 green — the gate is reused as-is (D-04)."
    - "MUST NOT report GATE-03 as passed from the NixOS sandbox — the real ~684-page corpus build requires the full local env; a sandbox run that cannot execute it is an explicit environmental deferral, never a silent pass."
  prohibitions_note: "Descriptor-less (no wired check_* scalars); each disposes flagged-unverified per the spec-less fallback."
---

<objective>
Close milestone v0.6.1 by re-running the existing full-corpus regression gate after the FID-01a fix (18-01) has landed, and confirming the SC#4 milestone invariants. GATE-03 is mechanical (D-04): reuse `tests/test_corpus_gate.py::TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error` — it already asserts BOTH that a fatal-free `index.pdf` is produced (SC#2) AND that the re-run `unknown_visit` catalogue is empty of `todo_node`/`manpage` (SC#3). No source changes; this plan re-runs the gate and records the outcome, plus guards SC#4 (no new deps, no `@preview` bump).

Purpose: Prove the uniform fr-column change (which touches every table across the ~684-page corpus, per D-02) did not regress fatal-free compilation, and formally close the milestone's regression gate.
Output: A recorded GATE-03 outcome (green or environmentally-deferred) and an SC#4 invariant confirmation in the plan summary.

Decisions implemented: **D-04: GATE-03 is a mechanical re-run of the existing corpus-gate machinery, not a new design.**
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-fidelity-fixes-regression-gate-close/18-CONTEXT.md
@.planning/phases/18-fidelity-fixes-regression-gate-close/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard the SC#4 milestone invariants (no new deps, no @preview bump)</name>
  <files>tests/test_preview_version_sync.py (verification target — not modified), pyproject.toml (inspected — not modified)</files>
  <read_first>
    - tests/test_preview_version_sync.py (the guard that asserts writer.py / template_engine.py / templates/base.typ agree on the four @preview versions)
    - pyproject.toml (dependency lists — confirm pypdf is already a dev/test dep and no new runtime dep was introduced by 18-01)
    - CLAUDE.md section "The @preview version-sync hazard" (the three declaration sites that must stay in lockstep)
  </read_first>
  <action>
    Confirm the FID-01a fix introduced no new runtime dependency and no `@preview` version change. Run `git diff --stat` against the phase-start baseline and verify `pyproject.toml` is unchanged (or only test-scoped, non-dependency lines changed) and that `writer.py` / `template_engine.py` / `templates/base.typ` carry no `@preview` version edits. Run `tests/test_preview_version_sync.py` to assert the 3-way version agreement still holds. pypdf is already a dev/test dependency (used by existing render-gate tests) — confirm it is present, do NOT add it as a runtime dep.
  </action>
  <verify>
    <automated>pytest tests/test_preview_version_sync.py -q && git diff --name-only -- pyproject.toml writer.py 2>/dev/null; git -C . diff --name-only -- typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_preview_version_sync.py -q` passes (3-way `@preview` version agreement intact).
    - `git diff` shows no `@preview` version change in `typsphinx/writer.py`, `typsphinx/template_engine.py`, or `typsphinx/templates/base.typ`.
    - `pyproject.toml` has no new runtime dependency added by this phase; `pypdf` remains a dev/test dependency only.
  </acceptance_criteria>
  <done>SC#4 invariants confirmed: zero new runtime deps, no `@preview` bump, version-sync surface untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full-corpus GATE-03 (fatal-free + unknown_visit clear)</name>
  <files>tests/test_corpus_gate.py (re-run target — not modified)</files>
  <read_first>
    - tests/test_corpus_gate.py lines 284-360 (`TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error` — the exact GATE-03 mechanism: `-b typstpdf` full-corpus build, `index.pdf` produced assertion, and the empty-`unknown_visit`-catalogue assertion at ~352-357)
    - tests/test_corpus_gate.py lines 190-266 (`catalogue_unknown_visit` — how todo_node/manpage would be counted if they reappeared)
    - 18-RESEARCH.md lines 532-540 (Environment Availability: this needs the FULL LOCAL env; the NixOS sandbox cannot run the real corpus build — ~45 integration tests fail environmentally there)
  </read_first>
  <action>
    Run the existing corpus gate in the FULL LOCAL environment (not the NixOS sandbox): `pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -q`. This performs a real `sphinx-build -b typstpdf` over the cached Sphinx v9.1.0 corpus (`~/.cache/typsphinx-corpus-gate/sphinx-v9.1.0/doc`; re-clone via the test's own `get_or_clone_corpus` if the cache was cleared — needs network to github.com/sphinx-doc/sphinx.git) and asserts `index.pdf` is produced fatal-free (SC#2) AND the `unknown_visit` catalogue is empty of `todo_node`/`manpage` (SC#3). Do NOT modify the test to force it green (D-04). If the executor is confined to the NixOS sandbox and cannot run the real corpus build, record an explicit environmental deferral in the summary (per memory `nixos-sandbox-test-env`) with the exact command for the operator to run in the full local env — never report GATE-03 passed without the real run.
  </action>
  <verify>
    <automated>pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -q  # full local env only; NixOS sandbox cannot run the real ~684-page corpus build (environmental deferral, not a silent pass)</automated>
    <human-check>Confirmatory (VALIDATION.md manual-only row): build `doc/extdev/deprecated.rst` via `typstpdf` and visually confirm the "Deprecated APIs" grid table now wraps its cells within the text block with no inter-column collision and no right-margin clip. The automated collision-absence extraction covers the regression; this visual check is confirmatory only.</human-check>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -q` passes in the full local env: `index.pdf` produced, 0 fatal errors, `unknown_visit` catalogue empty of `todo_node`/`manpage`.
    - If run in the NixOS sandbox where the corpus build cannot execute, the summary records an explicit environmental deferral with the operator command — not a claimed pass.
    - No modification to `tests/test_corpus_gate.py` (`git diff` clean for that file).
  </acceptance_criteria>
  <done>GATE-03 is green in the full local env (corpus fatal-free + `unknown_visit` clear of `todo_node`/`manpage`), or an explicit environmental deferral is recorded for the operator; the milestone regression gate is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → CI/operator report | GATE-03 outcome (corpus fatal-free + empty `unknown_visit`) is read from the real build; a falsely-green report is the only risk surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03 | Repudiation | GATE-03 result reporting | low | mitigate | The gate is a real `-b typstpdf` corpus build with an assertion on produced `index.pdf` + empty `unknown_visit` catalogue; a sandbox that cannot run it records an explicit environmental deferral (never a silent pass), so the reported state always corresponds to an actual run. |
| T-18-SC | Tampering | package installs | low | accept | No packages installed — GATE-03 re-runs pre-existing machinery only; the "zero new runtime deps" invariant is itself part of what this plan verifies (SC#4). |

All threats are low severity; `security_block_on: high` → no blocking threat in this plan.
</threat_model>

<verification>
- SC#4 guard: `pytest tests/test_preview_version_sync.py -q` green + `git diff` clean of `@preview`/dependency changes.
- GATE-03: `pytest tests/test_corpus_gate.py::TestCorpusRenderGate -m slow -q` green in the full local env (or explicit environmental deferral recorded).
- Confirmatory (human-check): visual inspection of the `extdev/deprecated` grid table wrapping in the real corpus PDF.
</verification>

<success_criteria>
- Full Sphinx-`doc/` corpus compiles fatal-free through `typstpdf`: `index.pdf` produced, 0 errors (SC#2, GATE-03).
- Re-run `unknown_visit` catalogue contains no `todo_node`/`manpage` (SC#3, GATE-03).
- Zero new runtime deps, no `@preview` bump, version-sync surface unchanged (SC#4).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 18-02)

- No source symbols created or changed — this plan re-runs existing test machinery and records outcomes.
- `.planning/phases/18-fidelity-fixes-regression-gate-close/18-02-SUMMARY.md` → records the GATE-03 result (fatal-free corpus + empty `unknown_visit` catalogue) and the SC#4 invariant confirmation, plus any environmental deferral note.

Reused unchanged: `tests/test_corpus_gate.py` (GATE-03 mechanism, D-04), `tests/test_preview_version_sync.py` (SC#4 guard).
</artifacts_produced>

<output>
Create `.planning/phases/18-fidelity-fixes-regression-gate-close/18-02-SUMMARY.md` when done.
</output>
