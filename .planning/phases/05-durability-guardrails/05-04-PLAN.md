---
phase: 05-durability-guardrails
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified: []
autonomous: false
requirements: [DUR-01, DUR-02, DUR-03, DUR-04]

must_haves:
  truths:
    - "A PR targeting `main` carries all Phase-5 edits, and `ci.yml` (full matrix + lint/type/coverage/build/integration) and `docs.yml` are GREEN on it — empirically confirming the DUR-01 `--locked` gate does not break the currently-in-sync tree, and that DUR-03/DUR-04/D-11-syntactic changes are valid (D-10)."
    - "The CI status badge renders as a live SVG in README on GitHub and links to the ci.yml Actions page (DUR-04 visual confirmation)."
    - "`drift.yml` runs to completion via `gh workflow run drift.yml` — validated POST-MERGE, because `workflow_dispatch` requires the workflow to exist on the default branch (DUR-02 / D-10)."
  artifacts:
    - "A PR against main (no repo source files created by this plan)"
  key_links:
    - "The push→observe gate is the reused Phase-2/3/4 verification pattern (D-10) — it is the empirical proof that the lockfile-currency gate keeps CI green rather than silently breaking it."
  prohibitions:
    - "`drift.yml`'s `drift-check` job is NOT present in `main`'s required status checks — `gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '.contexts'` contains no drift entry (D-07: it reports, never blocks)."
    - "A green PR is NOT claimed as proof that the `softprops/action-gh-release@v3` bump works at runtime — docs.yml's release step is tag-gated (`refs/tags/v*`) and release.yml's is tag/dispatch-gated, so neither runs on a normal `pull_request` (RESEARCH Pitfall 3). Full D-11 runtime confirmation is deferred to a real release tag or a manual release.yml dispatch, with explicit human sign-off — the PR asserts only syntactic validity of the `@v3` edits."
---

<objective>
Terminal verification gate for Phase 5. Push the phase branch, open a PR targeting `main`,
and observe the CI-visible guardrails land green (DUR-01 `--locked`, DUR-03 Dependabot
group, DUR-04 badge render, D-11 syntactic validity). Separately validate DUR-02's
scheduled `drift.yml` via manual `workflow_dispatch` (post-merge, per the trigger's
default-branch requirement), confirm it never becomes a required check (D-07), and record
the D-11 tag-gated runtime confirmation as an explicit deferred/human-signed item
(RESEARCH Pitfall 3) rather than falsely claiming the PR proved it.

Purpose: reuse the proven push→observe gate (D-10) so the milestone closes on a confirmed,
empirically-green, guarded pipeline — while being honest about the two verifications a
normal PR run cannot perform (the scheduled drift job and the tag-gated release steps).

Output: an opened/observed PR against main; a validated drift.yml dispatch run; a recorded
D-11 deferral sign-off. No repo source files modified by this plan.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-durability-guardrails/05-CONTEXT.md
@.planning/phases/05-durability-guardrails/05-RESEARCH.md
@.planning/phases/05-durability-guardrails/05-VALIDATION.md
@.planning/phases/05-durability-guardrails/05-01-PLAN.md
@.planning/phases/05-durability-guardrails/05-02-PLAN.md
@.planning/phases/05-durability-guardrails/05-03-PLAN.md
</context>

<artifacts_produced>
This plan produces no repo source symbols. It creates a PR and (post-merge) triggers the
`drift.yml` workflow. The `drift` issue label may be auto-created if a forced-failure smoke
test is run — noted so drift/graph verification does not treat it as unexpected.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Local static pre-checks, then push the phase branch and open a PR targeting main (D-10)</name>
  <files>(no repo files modified — git branch/push + gh pr create)</files>
  <read_first>
    - .planning/phases/05-durability-guardrails/05-VALIDATION.md (the full per-requirement static-check inventory to run before pushing)
    - .planning/phases/05-durability-guardrails/05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md (confirm the three Wave-1 plans completed and what they changed)
    - .planning/phases/04-refresh-dev-tooling/04-CONTEXT.md (the reused push→observe gate pattern; main's curated required-status-check set)
  </read_first>
  <action>
    First run the aggregate local static checks that gate the push (all must pass): (a)
    `grep -n "uv sync" .github/workflows/*.yml | grep -v -- "--locked"` returns empty (DUR-01);
    (b) `grep -rn "softprops/action-gh-release@v2" .github/workflows/` returns empty (D-11);
    (c) `.github/dependabot.yml` parses as YAML and contains the single `sphinx-typst-stack`
    group (DUR-03); (d) exactly one `actions/workflows/ci.yml/badge.svg` line in README (DUR-04);
    (e) `.github/workflows/drift.yml` parses as YAML with both triggers and minimal permissions
    (DUR-02). Then push the Phase-5 working branch (the branch carrying the 05-01/02/03 commits)
    to origin and open a PR targeting `main` via `gh pr create`, titled to reflect the durability
    guardrails (e.g. "Phase 5: durability guardrails — lockfile-currency gate, drift detector,
    Dependabot grouping, CI badge"). Do NOT merge — merge is a separate owner action (consistent
    with the Phase-4 gate). Capture the PR URL and the triggered `ci.yml`/`docs.yml` run IDs for
    the human-verify task.
  </action>
  <acceptance_criteria>
    - All five local static checks pass (empty/valid as specified above) BEFORE the push.
    - A PR against `main` exists (`gh pr view --json url,state` returns an open PR) and has triggered `ci.yml` and `docs.yml` runs (`gh pr checks` lists them).
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -n 'uv sync' .github/workflows/*.yml | grep -v -- '--locked')" && test -z "$(grep -rn 'softprops/action-gh-release@v2' .github/workflows/)" && python -c "import yaml; yaml.safe_load(open('.github/workflows/drift.yml'))" && test "$(grep -c 'actions/workflows/ci.yml/badge.svg' README.md)" -eq 1 && gh pr view --json state --jq '.state' | grep -q OPEN && echo PASS || echo FAIL</automated>
  </verify>
  <done>All local static checks green; a PR targeting main is open with ci.yml/docs.yml runs triggered; PR URL + run IDs captured. Not merged (owner action).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm ci.yml + docs.yml green on the PR and the CI badge renders</name>
  <action>Present the PR URL and the triggered ci.yml/docs.yml run results (from Task 1) to the developer for the CI-visible phase-gate confirmation (DUR-01/03/04 + D-11 syntactic); block until the developer approves. The automated verify below asserts drift.yml is not a required check (D-07). Do not merge or mark Phase 5 done until approved.</action>
  <what-built>
    All CI-visible Phase-5 guardrails on a PR targeting main: DUR-01 `--locked` at all 9 `uv sync`
    sites, DUR-03 `sphinx-typst-stack` Dependabot group, DUR-04 CI badge, and the D-11
    `softprops@v3` syntactic edits. The push→observe gate empirically confirms the `--locked`
    gate keeps the (currently in-sync) tree green rather than breaking it.
  </what-built>
  <how-to-verify>
    1. Open the PR URL from Task 1 on GitHub.
    2. Confirm `ci.yml` is GREEN — the full matrix (ubuntu/macos/windows × Python 3.10-3.13) plus
       Lint, Type Check, Coverage, Build Package, and the integration job (the bare `uv sync`
       site at ci.yml:179 now `--locked`) all pass. `gh pr checks <PR>` should show all required
       checks passing.
    3. Confirm `docs.yml` is GREEN end-to-end (including the PDF build) — note its tag-gated
       "Upload PDF to Release" step (softprops@v3) does NOT run on a PR; that is expected
       (Pitfall 3), not a failure.
    4. On the PR's "Files changed" or the branch's README, confirm the CI badge renders as a live
       SVG image and its link points to `.../actions/workflows/ci.yml` (DUR-04). It may show
       "no status"/"passing" depending on default-branch history until merge — the point is it
       renders and links correctly.
    5. (Automated, included in verify) Confirm `drift.yml`'s `drift-check` job is NOT in main's
       required status checks (D-07).
  </how-to-verify>
  <verify>
    <automated>gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '.contexts' 2>/dev/null | grep -qi drift && echo "FAIL: drift is a required check" || echo "PASS: drift not required"</automated>
  </verify>
  <resume-signal>Type "approved" once ci.yml and docs.yml are green on the PR and the badge renders correctly; or describe the failing check(s).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Validate drift.yml via post-merge workflow_dispatch and record the D-11 tag-gated sign-off</name>
  <action>Walk the developer through the two verifications a normal PR run cannot perform: (1) POST-MERGE, run `gh workflow run drift.yml && gh run watch` and confirm the drift detector runs to completion (workflow_dispatch requires the file on the default branch); (2) record the D-11 tag-gated softprops runtime decision — either sign off deferring full confirmation to the next real release tag, or smoke-test via a manual release.yml dispatch (details in how-to-verify below). Block until the developer confirms and the decision is recorded in the SUMMARY.</action>
  <what-built>
    DUR-02's `drift.yml` (weekly + `workflow_dispatch` drift detector) and the D-11
    `softprops/action-gh-release@v3` bump — the two verifications a normal PR run cannot perform.
  </what-built>
  <how-to-verify>
    DUR-02 (scheduled job — validate via manual dispatch, POST-MERGE):
    1. `workflow_dispatch` requires the workflow to exist on the DEFAULT branch, so this runs
       AFTER the PR merges to main (owner action). Once merged: `gh workflow run drift.yml`
       then `gh run watch` (RESEARCH Code Examples / D-10).
    2. Confirm the run reaches completion — steps execute in order (`uv lock --upgrade` →
       `uv sync ... --locked` → `uv run tox -e cov,docs-pdf`). A PASS means latest deps still
       resolve/build; a FAIL means real forward drift was detected AND a single `drift`-labelled
       GitHub Issue was filed (check the repo Issues tab). Either outcome validates the workflow.
    3. (Optional, to prove dedup) On a throwaway branch, force the exercise step to fail and
       dispatch twice — confirm exactly ONE open `drift`-labelled issue exists (second run
       comments rather than creating a duplicate — D-06).

    D-11 (tag-gated softprops steps — NOT exercised by this PR, Pitfall 3):
    4. Acknowledge that docs.yml:67 and release.yml:177 (`softprops/action-gh-release@v3`) only
       run on `refs/tags/v*` (docs.yml) or tag push / `workflow_dispatch` with a `tag` input
       (release.yml) — so the green PR did NOT prove they work at runtime. Choose one:
       (a) defer full confirmation to the next real release tag with this human sign-off, or
       (b) manually `gh workflow run release.yml -f tag=<test-tag>` to smoke-test (aware it may
       create a real release). Record the choice.
  </how-to-verify>
  <verify>
    <human-check>Post-merge `gh workflow run drift.yml && gh run watch` reaches completion (and, if it fails, files exactly one deduplicated drift issue); D-11 runtime confirmation is either signed-off-as-deferred to next release tag or smoke-tested via release.yml dispatch — the choice is recorded in the SUMMARY.</human-check>
  </verify>
  <resume-signal>Type "approved" once drift.yml has run to completion via workflow_dispatch (post-merge) and the D-11 deferral/confirmation choice is recorded; or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → main branch protection | The gate confirms required checks pass and that drift.yml is not (mis)promoted into the required set |
| workflow_dispatch → drift job | Manual trigger exercises the scheduled drift detector's token-scoped issue-writing path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Denial of Service (self-inflicted) | main's required-status-check set | high | mitigate | Automated `gh api .../required_status_checks` assertion that `drift-check` is absent — the intentionally-advisory, sometimes-failing drift job must never block merges (D-07) |
| T-05-08 | Spoofing (false assurance) | D-11 softprops runtime verification | medium | mitigate | Pitfall 3 — the tag-gated release steps are NOT run by the PR; full confirmation is explicitly deferred to a real release tag or a manual release.yml dispatch with recorded human sign-off, and the PR is documented as asserting only syntactic validity |
</threat_model>

<verification>
- CI-visible: ci.yml (full matrix + lint/type/coverage/build/integration) and docs.yml GREEN on the PR (human-verify + `gh pr checks`); badge renders (visual).
- D-07: `gh api .../required_status_checks --jq '.contexts'` contains no drift entry (automated).
- DUR-02: `gh workflow run drift.yml && gh run watch` reaches completion POST-MERGE (workflow_dispatch needs the file on the default branch); forced-failure dedup optional.
- D-11: tag-gated — runtime confirmation deferred to next release tag or manual release.yml dispatch with human sign-off (NOT proven by the PR — Pitfall 3).
</verification>

<success_criteria>
The PR against main is green on ci.yml + docs.yml; the CI badge renders; drift.yml is
confirmed to run to completion via workflow_dispatch (post-merge) and is absent from main's
required checks; the D-11 tag-gated runtime confirmation is recorded as deferred/human-signed
rather than falsely claimed green.
</success_criteria>

<output>
Create `.planning/phases/05-durability-guardrails/05-04-SUMMARY.md` when done — record the
PR URL, the ci.yml/docs.yml run results, the drift.yml dispatch outcome, the drift-not-required
assertion, and the D-11 deferral/confirmation decision.
</output>
