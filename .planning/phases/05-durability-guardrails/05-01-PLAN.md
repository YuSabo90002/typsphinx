---
phase: 05-durability-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/docs.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [DUR-01]

must_haves:
  truths:
    - "Every `uv sync` invocation across ci.yml, docs.yml, and release.yml carries the `--locked` flag (9 sites total: ci.yml 41/71/92/113/151/179, docs.yml 31, release.yml 36/93) — per D-01/D-02."
    - "Each site's pre-existing `--extra` flags are preserved unchanged (`--extra dev`, and docs.yml's `--extra dev --extra docs`); `--locked` is appended after them."
    - "`softprops/action-gh-release` is referenced at `@v3` at docs.yml:67 and release.yml:177 (D-11 carried-over node24 hardening)."
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/docs.yml
    - .github/workflows/release.yml
  key_links:
    - "`uv sync --locked` fails the build when uv.lock and pyproject.toml disagree — the load-bearing anti-drift gate that closes DUR-01 (D-01). Every tox env resolves from uv.lock via uv-venv-lock-runner, so gating at the uv sync layer pins what CI actually runs."
  prohibitions:
    - "No `uv sync` line in any of the three workflow files lacks `--locked` — `grep -n 'uv sync' .github/workflows/*.yml | grep -v -- '--locked'` returns empty."
    - "The bare `uv sync` at ci.yml:179 (integration job, no `--extra`) is NOT missed (RESEARCH Pitfall 1)."
    - "No `softprops/action-gh-release@v2` reference remains in any workflow file."
---

<objective>
Close DUR-01 by appending `--locked` to all 9 `uv sync` call sites across the three
workflow files, and land the carried-over D-11 hardening by bumping
`softprops/action-gh-release@v2 → @v3` at its 2 call sites. Both are mechanical,
uniform, in-place text edits — no structural changes to any workflow.

Purpose: `uv.lock` is the single source of truth every workflow resolves from. Forcing
every `uv sync` to fail on a stale/silently-rewritten lock is the milestone-consistent
anti-drift gate (D-01) that makes this cycle's silent-rot failure mode impossible to
repeat unnoticed. D-11 keeps release automation on a supported Node runtime ahead of the
2026-09-16 hosted-runner Node-20 removal.

Output: 3 modified workflow files (ci.yml, docs.yml, release.yml).
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-durability-guardrails/05-CONTEXT.md
@.planning/phases/05-durability-guardrails/05-RESEARCH.md
@.planning/phases/05-durability-guardrails/05-PATTERNS.md
</context>

<artifacts_produced>
This plan MODIFIES existing files only; it creates no new files or symbols. The
downstream drift/graph verification should treat ci.yml/docs.yml/release.yml as
pre-existing (edited, not created).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Append --locked to all 9 uv sync call sites (DUR-01 / D-01, D-02)</name>
  <files>.github/workflows/ci.yml, .github/workflows/docs.yml, .github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current state — the 6 `uv sync` sites at lines 41/71/92/113/151/179; note 179 is the BARE one with no `--extra`)
    - .github/workflows/docs.yml (current state — `uv sync` at line 31 with `--extra dev --extra docs`)
    - .github/workflows/release.yml (current state — `uv sync` at lines 36 and 93 with `--extra dev`)
    - .planning/phases/05-durability-guardrails/05-CONTEXT.md §D-01/D-02 (locked call-site inventory)
    - .planning/phases/05-durability-guardrails/05-RESEARCH.md §Pattern 1 + §Pitfall 1 (append-preserving-flags; do not miss the bare site)
    - .planning/phases/05-durability-guardrails/05-PATTERNS.md §ci.yml/docs.yml/release.yml (exact before/after per site)
  </read_first>
  <action>
    Append the literal flag `--locked` to the end of every `uv sync` invocation in all three
    workflow files, preserving each line's existing `--extra` flags exactly. Per D-01/D-02, the
    9 sites are: ci.yml lines 41, 71, 92, 113, 151 (each currently `uv sync --extra dev` → becomes
    `uv sync --extra dev --locked`) and line 179 (the BARE integration-job `uv sync` with NO
    `--extra` → becomes `uv sync --locked` — RESEARCH Pitfall 1: do not miss this one, it will not
    be caught by searching for `--extra dev`); docs.yml line 31 (`uv sync --extra dev --extra docs`
    → append `--locked` after `docs`); release.yml lines 36 and 93 (each `uv sync --extra dev` →
    append `--locked`). Do NOT reorder existing flags, do NOT touch the `uv pip install -e .`
    line that follows docs.yml:31, and do NOT touch the `uv run twine check dist/*` lines that
    follow the ci.yml:151 / release.yml:93 build-job sync calls. Line numbers were re-verified
    against the working tree 2026-07-05 (RESEARCH Call-Site Drift Check: zero drift); run a fresh
    `grep -n "uv sync"` immediately before editing in case contents shifted.
  </action>
  <acceptance_criteria>
    - `grep -n "uv sync" .github/workflows/ci.yml .github/workflows/docs.yml .github/workflows/release.yml | grep -v -- "--locked"` returns EMPTY output (no `uv sync` line lacks `--locked`).
    - `grep -c "uv sync" .github/workflows/ci.yml` == 6; `grep -c "uv sync" .github/workflows/docs.yml` == 1; `grep -c "uv sync" .github/workflows/release.yml` == 2 (total still 9 — no lines added or removed, only edited).
    - docs.yml:31 still contains both `--extra dev` and `--extra docs` in addition to `--locked` (existing flags preserved).
    - The bare integration-job site reads exactly `uv sync --locked` (no stray `--extra`).
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -n 'uv sync' .github/workflows/ci.yml .github/workflows/docs.yml .github/workflows/release.yml | grep -v -- '--locked')" && echo PASS || echo FAIL</automated>
  </verify>
  <done>All 9 `uv sync` sites carry `--locked`; existing `--extra` flags preserved; the negated grep returns empty; per-file `uv sync` counts remain 6/1/2.</done>
</task>

<task type="auto">
  <name>Task 2: Bump softprops/action-gh-release @v2 → @v3 at both sites (D-11)</name>
  <files>.github/workflows/docs.yml, .github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/docs.yml (current state — line 67 `uses: softprops/action-gh-release@v2` inside the tag-gated "Upload PDF to Release" step)
    - .github/workflows/release.yml (current state — line 177 `uses: softprops/action-gh-release@v2` inside the tag/dispatch-gated "Create GitHub Release" step)
    - .planning/phases/05-durability-guardrails/05-CONTEXT.md §D-11 (softprops bump rationale + exact locations)
    - .planning/phases/05-durability-guardrails/05-RESEARCH.md §Standard Stack + §Pitfall 3 (v3.0.0/v3.0.1 exist, Node24 drop-in; tag-gated verification limitation)
  </read_first>
  <action>
    Change the action reference string `softprops/action-gh-release@v2` to
    `softprops/action-gh-release@v3` at exactly two sites: docs.yml line 67 and release.yml line
    177. This is a pure version-tag swap (per D-11) — do NOT modify the `with:` inputs
    (`files:`, `draft:`, `prerelease:`, `body_path:`, `tag_name:`, `generate_release_notes:`),
    the `if:` conditions, or step names; RESEARCH confirms `@v3` is a Node-24-runtime-only change
    with no API differences for this repo's usage. Keep the moving-major-tag convention (`@v3`,
    matching `actions/checkout@v6` / `astral-sh/setup-uv@v7`); SHA-pinning is out of scope (D-12
    deferral). CRITICAL (RESEARCH Pitfall 3): both edited steps are tag-gated
    (docs.yml fires only on `refs/tags/v*`; release.yml on tag push or workflow_dispatch), so a
    normal PR push→observe run does NOT exercise these lines — runtime confirmation is handled
    separately in plan 05-04, not here.
  </action>
  <acceptance_criteria>
    - `grep -rn "softprops/action-gh-release@v3" .github/workflows/` returns exactly 2 matches (docs.yml, release.yml).
    - `grep -rn "softprops/action-gh-release@v2" .github/workflows/` returns EMPTY (no @v2 reference remains).
    - The `with:` blocks under both softprops steps are byte-for-byte unchanged from before the edit (only the `@v2`→`@v3` on the `uses:` line differs).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -rc 'softprops/action-gh-release@v3' .github/workflows/ | grep -c ':1')" -ge 1 && test -z "$(grep -rn 'softprops/action-gh-release@v2' .github/workflows/)" && echo PASS || echo FAIL</automated>
  </verify>
  <done>Both softprops references read `@v3`; zero `@v2` references remain; `with:` inputs unchanged. Full runtime confirmation is explicitly deferred to 05-04 (tag-gated, Pitfall 3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → package index | `uv sync` resolves and installs dependencies; untrusted resolution can pull unexpected versions if the lock is not enforced |
| Actions runtime → release publishing | `softprops/action-gh-release` runs with a token that can create GitHub Releases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `uv sync` across ci.yml/docs.yml/release.yml | high | mitigate | DUR-01 `--locked` at all 9 sites fails the build on lock↔pyproject desync (D-01) — the core anti-drift control this milestone exists to install |
| T-05-02 | Tampering (supply chain) | `softprops/action-gh-release@v2→v3` | medium | accept | Same-maintainer moving-major bump already inside the repo's trust boundary; v3.0.0/v3.0.1 confirmed to exist and be Node24 via the action's GitHub Releases page (RESEARCH); continues the repo's moving-major-tag convention, SHA-pinning consciously deferred (D-12) |
</threat_model>

<verification>
- Static: `grep -n "uv sync" .github/workflows/*.yml | grep -v -- "--locked"` → empty.
- Static: `grep -rn "softprops/action-gh-release@v2" .github/workflows/` → empty; `@v3` → 2 matches.
- CI-visible confirmation of the `--locked` gate (green on a PR targeting main) is handled by plan 05-04; D-11's tag-gated runtime confirmation is deferred there per Pitfall 3.
</verification>

<success_criteria>
All 9 `uv sync` sites carry `--locked` with existing `--extra` flags intact; both
`softprops/action-gh-release` references are `@v3`; no `@v2` and no unlocked `uv sync`
remain. Static greps pass.
</success_criteria>

<output>
Create `.planning/phases/05-durability-guardrails/05-01-SUMMARY.md` when done.
</output>
