---
phase: 05-durability-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
  - README.md
autonomous: true
requirements: [DUR-03, DUR-04]

must_haves:
  truths:
    - "`.github/dependabot.yml`'s existing `pip` ecosystem entry gains a `groups:` block named `sphinx-typst-stack` that bundles the sphinx/docutils/typst runtime cluster into a single PR (DUR-03 / D-08)."
    - "The `github-actions` ecosystem entry in dependabot.yml is left byte-for-byte unchanged (D-08)."
    - "README.md's badge row contains a CI-workflow status badge whose image URL is `https://github.com/YuSabo90002/typsphinx/actions/workflows/ci.yml/badge.svg` and whose link target is the workflow's Actions page (DUR-04 / D-09)."
  artifacts:
    - .github/dependabot.yml
    - README.md
  key_links:
    - "Grouped sphinx/docutils/typst bumps land as one combined-review PR, so a lone `typst` bump can no longer slip through and reintroduce the `kai`-class break — the Dependabot-layer complement to the milestone's upper-bound pinning (DUR-03)."
  prohibitions:
    - "The `sphinx-typst-stack` group does NOT over-match `sphinx-autodoc-typehints` or `sphinx-intl` (dev/docs tools confirmed present in pyproject.toml) — the group is scoped to exactly the runtime risk cluster, honoring D-08 'group exactly sphinx/docutils/typst, do not broaden' (RESEARCH Pitfall 4)."
    - "No `groups:` block is added to the `github-actions` ecosystem entry (D-08)."
    - "No `*sphinx*`-style substring/double-wildcard patterns are used (RESEARCH Pitfall 4)."
---

<objective>
Close DUR-03 by appending a precisely-scoped `groups:` block to the existing `pip`
ecosystem entry in `.github/dependabot.yml`, and close DUR-04 by adding one CI-workflow
status badge to README.md's existing badge row.

Purpose: DUR-03 forces the tightly-coupled sphinx/docutils/typst cluster to move as one
reviewable unit so a lone `typst` bump cannot reintroduce the `kai`-class break unnoticed
(D-08). DUR-04 makes the now-green, guarded pipeline's health visible at a glance on the
repo landing page (D-09).

Output: 2 modified files (.github/dependabot.yml, README.md).
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-durability-guardrails/05-CONTEXT.md
@.planning/phases/05-durability-guardrails/05-RESEARCH.md
@.planning/phases/05-durability-guardrails/05-PATTERNS.md
</context>

<artifacts_produced>
This plan produces two new symbols the downstream drift/graph verification must treat as
newly-created (not drift):
- The `sphinx-typst-stack` Dependabot group under the `pip` ecosystem in `.github/dependabot.yml`.
- The CI status badge line in `README.md` (`[![CI](.../ci.yml/badge.svg)](.../ci.yml)`).
No new files are created; both are in-place appends/inserts to existing files.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the sphinx-typst-stack Dependabot group (DUR-03 / D-08)</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .github/dependabot.yml (current state — existing `pip` ecosystem entry at lines 4-13; `github-actions` entry at lines 16-23, which must stay untouched)
    - .planning/phases/05-durability-guardrails/05-CONTEXT.md §D-08 (group exactly sphinx/docutils/typst; do not broaden; suggested group name `sphinx-typst-stack`)
    - .planning/phases/05-durability-guardrails/05-RESEARCH.md §Pattern 3 + §Pitfall 4 (groups/patterns syntax; prefix-anchored patterns, no substring wildcards)
    - .planning/phases/05-durability-guardrails/05-PATTERNS.md §.github/dependabot.yml (exact before/after append)
  </read_first>
  <action>
    Append a `groups:` block to the EXISTING `pip` ecosystem entry (do not create a new
    ecosystem entry, do not duplicate `schedule:`/`labels:`/`open-pull-requests-limit:`),
    keeping all current keys of that entry unchanged. Name the group `sphinx-typst-stack`
    (D-08 suggested name). Set its `patterns:` to the three prefix-anchored globs `sphinx*`,
    `docutils*`, `typst*`. CRITICAL SCOPING (D-08 "group EXACTLY sphinx/docutils/typst, do not
    broaden" + RESEARCH Pitfall 4): a plain `sphinx*` also matches `sphinx-autodoc-typehints`
    and `sphinx-intl`, which a `grep -in "sphinx" pyproject.toml` confirms are present as
    dev/docs tools — those are NOT part of the runtime risk cluster. Therefore also add an
    `exclude-patterns:` key under the same group listing exactly `sphinx-autodoc-typehints`
    and `sphinx-intl`, so the group resolves to precisely the sphinx/docutils/typst runtime
    trio. (`docutils*` does not match `types-docutils` — that starts with `types-`; `typst*`
    matches only `typst`. Only `sphinx*` over-matches, hence the two excludes.) Do NOT use
    `*sphinx*`-style substring patterns. Do NOT add any `groups:` block to the `github-actions`
    ecosystem entry — leave it byte-for-byte unchanged (D-08). Preserve the file's existing
    2-space indentation style.
  </action>
  <acceptance_criteria>
    - `grep -c "sphinx-typst-stack" .github/dependabot.yml` == 1 (group declared exactly once).
    - The `groups:` block appears under the `pip` ecosystem entry and lists `patterns:` `sphinx*`, `docutils*`, `typst*` and `exclude-patterns:` `sphinx-autodoc-typehints`, `sphinx-intl`.
    - The `github-actions` ecosystem entry is unchanged: `grep -c "groups:" .github/dependabot.yml` == 1 (only one group block in the whole file).
    - `python -c "import yaml,sys; yaml.safe_load(open('.github/dependabot.yml'))"` exits 0 (valid YAML).
    - No `*sphinx*` / `*docutils*` / `*typst*` substring patterns present: `grep -E '\*(sphinx|docutils|typst)\*' .github/dependabot.yml` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/dependabot.yml')); g=[e for e in d['updates'] if e['package-ecosystem']=='pip'][0]['groups']['sphinx-typst-stack']; assert sorted(g['patterns'])==['docutils*','sphinx*','typst*'], g; assert sorted(g['exclude-patterns'])==['sphinx-autodoc-typehints','sphinx-intl'], g; assert all('groups' not in e for e in d['updates'] if e['package-ecosystem']=='github-actions'); print('PASS')"</automated>
  </verify>
  <done>The `pip` entry has a single `sphinx-typst-stack` group scoped to exactly the sphinx/docutils/typst runtime trio (dev/docs sphinx-prefixed tools excluded); the `github-actions` entry is untouched; YAML is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CI status badge to README.md's badge row (DUR-04 / D-09)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — existing badge row at lines 3-7: PyPI / Python Support / License / Code style: black / Documentation; each badge is one line `[![alt](image-url)](link-url)` with no blank lines between them)
    - .planning/phases/05-durability-guardrails/05-CONTEXT.md §D-09 (single CI badge; exact URL form; repo slug YuSabo90002/typsphinx)
    - .planning/phases/05-durability-guardrails/05-RESEARCH.md §Pattern 4 (badge URL form + placement discretion)
    - .planning/phases/05-durability-guardrails/05-PATTERNS.md §README.md (exact before/after; leading position recommended)
  </read_first>
  <action>
    Insert ONE new markdown badge line into the existing badge row (lines 3-7), following the
    row's exact convention `[![alt](image-url)](link-url)`, one line per badge, no blank lines
    between badges. Use alt text `CI`, image URL
    `https://github.com/YuSabo90002/typsphinx/actions/workflows/ci.yml/badge.svg`, and link target
    `https://github.com/YuSabo90002/typsphinx/actions/workflows/ci.yml` (per D-09 — a single CI
    badge satisfies DUR-04; do NOT add a docs-workflow badge). Placement within the row is
    Claude's discretion (D-09); place it FIRST (immediately before the PyPI badge on line 3) to
    foreground the build-health signal this whole milestone exists to restore. Do NOT alter the
    other five badge lines and do NOT introduce a blank line inside the row. Note the repo slug
    casing is `YuSabo90002/typsphinx` (mixed case) in the actions URLs, matching the existing
    Repository/Issues links elsewhere in README.
  </action>
  <acceptance_criteria>
    - `grep -c "actions/workflows/ci.yml/badge.svg" README.md` == 1 (exactly one CI badge added).
    - The badge line matches the row convention `[![CI](<image-url>)](<link-url>)` and the link target is `.../actions/workflows/ci.yml` (the Actions page, not the badge svg).
    - The five pre-existing badge lines (PyPI, Python Support, License, black, Documentation) are all still present: `grep -c "img.shields.io\|badge.fury.io" README.md` >= 5.
    - No blank line was introduced inside the badge row (the CI badge line is adjacent to the PyPI badge line).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'actions/workflows/ci.yml/badge.svg' README.md)" -eq 1 && grep -q '\[!\[CI\](https://github.com/YuSabo90002/typsphinx/actions/workflows/ci.yml/badge.svg)\](https://github.com/YuSabo90002/typsphinx/actions/workflows/ci.yml)' README.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>README's badge row carries exactly one CI status badge with the correct badge.svg image URL and Actions-page link target; the five existing badges are intact; no blank line inside the row. Live SVG render is visually confirmed on GitHub in plan 05-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot service → dependency PRs | GitHub-hosted Dependabot reads `dependabot.yml` and opens PRs that bump the project's dependencies |
| README badge → GitHub-hosted SVG endpoint | Read-only image fetch of a public workflow-status badge; no data crosses into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | Dependabot `pip` update flow (`dependabot.yml`) | high | mitigate | DUR-03's `sphinx-typst-stack` group forces sphinx/docutils/typst into one combined-review PR (D-08), so a lone version-incompatible bump can't land unreviewed and reintroduce the `kai`-class break |
| T-05-04 | Information disclosure | README CI badge SVG endpoint | low | accept | Public repository; workflow status is already publicly visible via the Actions tab — the badge exposes nothing not already public |
</threat_model>

<verification>
- Static: `python -c "import yaml; yaml.safe_load(open('.github/dependabot.yml'))"` valid; group present and correctly scoped; `github-actions` entry untouched.
- Static: exactly one CI badge in README with the correct image URL + Actions-page link; five existing badges intact.
- Live badge render on GitHub (post-push visual confirmation) is handled by plan 05-04.
</verification>

<success_criteria>
`dependabot.yml` has a single `sphinx-typst-stack` group scoped to exactly the
sphinx/docutils/typst runtime trio under the `pip` ecosystem, `github-actions` untouched;
README carries exactly one correctly-formed CI status badge. Static checks pass.
</success_criteria>

<output>
Create `.planning/phases/05-durability-guardrails/05-02-SUMMARY.md` when done.
</output>
