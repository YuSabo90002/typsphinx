---
phase: 62-the-visit-image-separator-fix-and-its-real-compile-gate
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - tests/fixtures/inline_image_separator_render_gate/conf.py
  - tests/fixtures/inline_image_separator_render_gate/index.rst
  - tests/fixtures/inline_image_separator_render_gate/pass_parent.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_02_two_subs_adjacent.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_03_sub_in_list_item.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_04_block_image_second_in_list_item.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_05_image_in_table_cell.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_06_image_in_definition_list_body.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_07_image_in_admonition.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_08_image_in_footnote_body.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_09_image_in_legend_mid_text.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_10_two_images_in_legend.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_11_image_after_inline_literal.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_12_image_after_emphasis.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_13_image_after_reference.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_14_image_in_field_list_body.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_15_image_in_section_title.rst
  - tests/fixtures/inline_image_separator_render_gate/fail_16_image_with_width_mid_sentence.rst
  - tests/fixtures/inline_image_separator_render_gate/pass_b_figure_with_caption.rst
  - tests/fixtures/inline_image_separator_render_gate/pass_c_image_first_in_paragraph.rst
  - tests/fixtures/inline_image_separator_render_gate/pass_d_image_with_dimensions_and_scale_align.rst
  - tests/fixtures/inline_image_separator_render_gate/pass_e_image_with_propagated_target_id.rst
  - tests/fixtures/inline_image_separator_render_gate/pass_f_figure_with_plain_legend.rst
  - tests/fixtures/inline_image_separator_render_gate/pass_g_figure_in_list_item_after_paragraph.rst
  - tests/fixtures/inline_image_separator_render_gate/pass_h_figure_first_in_list_item.rst
  - tests/fixtures/inline_image_separator_render_gate/pass_i_bare_image_first_in_list_item.rst
  - tests/test_inline_image_separator_render_gate.py
autonomous: true
requirements: [IMG-08, IMG-09, TEST-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'All 16 measured failing shapes from `.planning/research/FEATURES.md` § Q1 exist as one fixture document each, each declared as its own master, and every one of them compiles to a non-empty `%PDF`-prefixed file in a single `sphinx-build -b typstpdf` invocation.'
    - 'The image inside a figure legend (Q1 rows 9 and 10) compiles - the shape the non-`in_figure`-only mechanism measurably left broken, closed by the AMENDED leading-half placement.'
    - 'The image inside a field-list body (Q1 row 14, a concat context) compiles and does not produce the `cannot apply unary ''+'' to content` refusal that a trailing-newline-unaware fix produces.'
    - 'All 9 measured must-keep-passing shapes from § Q2 exist as one fixture document each under a single `pass_parent` master, and `pass_parent` compiles to a valid PDF.'
    - '`typst_documents` holds exactly 18 entries - `index` plus the 16 FAIL docnames plus `pass_parent` - and the fixture holds exactly 27 `.rst` documents.'
    - '`pass_parent` shares no content file with any FAIL master, so its green verdict is independent of every failing document and is a real positive control rather than an artefact of build ordering.'
    - '`TypstPDFBuilder.finish()` attempts every configured master regardless of earlier failures, so post-fix all 18 wrapper PDFs exist and pre-fix all 17 failing docnames appear in one aggregate error.'
    - statement: 'The `:width:` value conversion is untouched by this phase: `fail_16`''s `:width: 50px` still emits `width: 37.5pt` in the post-fix content `.typ`, so no rounding, truncation or unit-conversion behaviour changed.'
      verification: backstop
    - statement: 'All 18 masters are compiled sequentially inside one synchronous `finish()` loop in one `sphinx-build` subprocess, and the gate asserts only on that process after it exits - there is no parallel or mid-run observation to guarantee.'
      verification: backstop
  artifacts:
    - tests/fixtures/inline_image_separator_render_gate/conf.py
    - 'tests/fixtures/inline_image_separator_render_gate/ :: 16 fail_*.rst documents'
    - 'tests/fixtures/inline_image_separator_render_gate/ :: 9 pass_*.rst documents under pass_parent'
    - 'tests/test_inline_image_separator_render_gate.py :: FAIL_DOCNAMES, PASS_DOCNAMES, ALL_MASTER_DOCNAMES widened to the full matrix'
    - 'tests/test_inline_image_separator_render_gate.py :: TestInlineImageSeparatorFullMatrix, TestInlineImageSeparatorFailShapes'
  key_links:
    - 'index.rst toctree -> the 16 FAIL content files: this is the `#include()` edge that makes an image-free master fail, so removing any FAIL doc from index''s toctree silently removes SC#1''s blast-radius property.'
    - 'pass_parent.rst toctree -> the 9 PASS content files: the only include edges pass_parent has, which is what keeps it green while 17 masters are red.'
    - 'conf.py typst_documents target stems -> _wrapper_output_relpath(): every target is `<docname>-out.typ`, so the gate derives every wrapper PDF path as `<docname>-out.pdf` from the docname alone.'
    - 'visit_legend() -> in_list_item/list_item_needs_separator -> visit_image() figure path: the link rows 9 and 10 depend on; a fix that reverts the leading half to the non-`in_figure` branch re-breaks exactly these two documents.'
  prohibitions:
    - 'MUST NOT reduce, merge or omit any of the 16 measured failing shapes or the 9 must-keep-passing shapes; the matrix is the measured trigger surface, not a sample of it.'
    - 'MUST NOT introduce `numref` usage anywhere in this fixture - it collides with the known-open NUM-01 defect and would make a gate failure ambiguous.'
    - 'MUST NOT make the gate or the suite green by editing, weakening, deleting or skipping any pre-existing test, assertion or fixture.'
    - 'MUST NOT take any irreversible release action - no version literal, no CHANGELOG.md edit, no tag, no publish, no PR.'
---

<objective>
Expand the tracer's 3-master fixture to the full measured trigger surface: 16 FAIL documents (one per
`.planning/research/FEATURES.md` § Q1 row, each its own master), 9 PASS documents (one per § Q2 row,
all under one `pass_parent` master), and the image-free `index` root - 18 masters, 27 documents - and
widen the gate module to assert across all of them from a single `sphinx-build -b typstpdf`.

Purpose: the tracer proved the mechanism on one shape. This is the horizontal expansion that turns
"the fix works" into "the fix is general", and it is the only place rows 9, 10 and 14 - the three
shapes the originally-recommended mechanism measurably did not close - are bound.

Output: the completed fixture project and a gate module whose `-k full_matrix` selector covers all 18
masters and whose `-k fail` selector covers all 16 failing shapes.
</objective>

<amendments_reference>
The mechanism this plan's fixture exercises is the AMENDED one recorded in `62-01-PLAN.md`'s
`<amendments>` block (D-08 / IMG-10 amendment 1, and D-06 amendment 2). Read that block before
diagnosing any failure in this plan: if `fail_09`, `fail_10` or `fail_14` are red, the leading half of
the triad has been scoped back to the non-`in_figure` branch or the trailing half has lost its
concat-awareness, and the fix - not the fixture - is what regressed.
</amendments_reference>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-CONTEXT.md
@.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-PATTERNS.md
@.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-VALIDATION.md
@.planning/research/FEATURES.md
@.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-01-PLAN.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 12). Before running ANY command in this plan:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Without this, pytest imports the UNCHANGED
main-tree package, plan 01's fix is invisible, and every FAIL document appears still-broken.
</worktree_provisioning>

<wave_rationale>
Wave 2. It cannot join wave 1: every task here asserts the expanded matrix compiles GREEN, which is
only true once plan 01's fix exists, and it rewrites `conf.py`, `index.rst`, `pass_parent.rst` and
the gate module that plan 01 creates - a hard `files_modified` overlap that forbids the same wave. It
must precede wave 3: plan 03's RED choreography and golden capture both require the full 27-document
fixture in the tree, and its transcript must name all 17 failing masters, not a subset.
</wave_rationale>

<fixture_document_map>
Every image reference in this fixture is `/_static/pic.png`. Every substitution is named `|sub|` and
its `.. |sub| image:: /_static/pic.png` definition is repeated inside each document that uses it
(substitutions are per-document). Every document opens with a title and its own underline.

**FAIL documents** - docname to `.planning/research/FEATURES.md` § Q1 row. Row 1 already exists from
plan 01.

| # | docname | Q1 row | Shape |
|---|---------|--------|-------|
| 1 | `fail_01_sub_mid_sentence` | 1 | (exists) substitution image mid-sentence |
| 2 | `fail_02_two_subs_adjacent` | 2 | two substitution images adjacent in one paragraph |
| 3 | `fail_03_sub_in_list_item` | 3 | substitution image mid-text inside a bullet-list item |
| 4 | `fail_04_block_image_second_in_list_item` | 4 | block `.. image::` as the second element of a list item, after a paragraph |
| 5 | `fail_05_image_in_table_cell` | 5 | substitution image mid-text in a `list-table` cell |
| 6 | `fail_06_image_in_definition_list_body` | 6 | substitution image mid-text in a definition-list body |
| 7 | `fail_07_image_in_admonition` | 7 | substitution image mid-text inside a `.. note::` |
| 8 | `fail_08_image_in_footnote_body` | 8 | substitution image mid-text in a footnote body, with a body-text reference to it |
| 9 | `fail_09_image_in_legend_mid_text` | 9 | substitution image mid-text in a figure's legend paragraph |
| 10 | `fail_10_two_images_in_legend` | 10 | two substitution images adjacent in a figure's legend |
| 11 | `fail_11_image_after_inline_literal` | 11 | substitution image after an inline literal |
| 12 | `fail_12_image_after_emphasis` | 12 | substitution image after emphasis |
| 13 | `fail_13_image_after_reference` | 13 | substitution image after an external link |
| 14 | `fail_14_image_in_field_list_body` | 14 | substitution image mid-text in a `:Returns:` field-list body (a concat context) |
| 15 | `fail_15_image_in_section_title` | 15 | substitution image inside the section title itself |
| 16 | `fail_16_image_with_width_mid_sentence` | 16 | substitution image carrying `:width: 50px` on its definition, used mid-sentence |

**PASS documents** - docname to § Q2 row. Row A already exists from plan 01. § Q2's rows D and D2 are
merged into one document carrying both directives, per ROADMAP SC#3's own single-item phrasing
("the image with `:width:`/`:height:`/`:scale:`/`:align:`").

| # | docname | Q2 row | Shape |
|---|---------|--------|-------|
| A | `pass_a_standalone_block_image` | A | (exists) standalone block image with text before and after |
| B | `pass_b_figure_with_caption` | B | `.. figure::` with a caption |
| C | `pass_c_image_first_in_paragraph` | C | substitution image FIRST in its paragraph, followed by text |
| D | `pass_d_image_with_dimensions_and_scale_align` | D + D2 | one block image with `:width:`/`:height:`, then one with `:scale:`/`:align:` |
| E | `pass_e_image_with_propagated_target_id` | E | an explicit target immediately followed by a standalone block image |
| F | `pass_f_figure_with_plain_legend` | F | figure with a caption and a plain image-free legend paragraph |
| G | `pass_g_figure_in_list_item_after_paragraph` | G | `.. figure::` as the second element of a list item |
| H | `pass_h_figure_first_in_list_item` | H | `.. figure::` as the first element of a list item |
| I | `pass_i_bare_image_first_in_list_item` | I | bare block `.. image::` as the only element of a list item |

**Counting note, surfaced not silently corrected.** `62-CONTEXT.md` D-01 says "26 documents"; the
enumeration D-01 itself specifies sums to **27** (`index` + 16 FAIL + `pass_parent` + 9 PASS). The
binding half of D-01 - 18 masters, 16 FAIL each its own master, 9 PASS under one `pass_parent`, one
image-free `index` - is preserved exactly; the "26" is an arithmetic slip in the CONTEXT prose and
27 is the count this plan builds and asserts.
</fixture_document_map>

<tasks>

<task type="auto">
  <name>Task 1: Add the 15 remaining FAIL documents and widen typst_documents to 17 masters</name>
  <files>tests/fixtures/inline_image_separator_render_gate/conf.py, tests/fixtures/inline_image_separator_render_gate/index.rst, tests/fixtures/inline_image_separator_render_gate/fail_02_two_subs_adjacent.rst, tests/fixtures/inline_image_separator_render_gate/fail_03_sub_in_list_item.rst, tests/fixtures/inline_image_separator_render_gate/fail_04_block_image_second_in_list_item.rst, tests/fixtures/inline_image_separator_render_gate/fail_05_image_in_table_cell.rst, tests/fixtures/inline_image_separator_render_gate/fail_06_image_in_definition_list_body.rst, tests/fixtures/inline_image_separator_render_gate/fail_07_image_in_admonition.rst, tests/fixtures/inline_image_separator_render_gate/fail_08_image_in_footnote_body.rst, tests/fixtures/inline_image_separator_render_gate/fail_09_image_in_legend_mid_text.rst, tests/fixtures/inline_image_separator_render_gate/fail_10_two_images_in_legend.rst, tests/fixtures/inline_image_separator_render_gate/fail_11_image_after_inline_literal.rst, tests/fixtures/inline_image_separator_render_gate/fail_12_image_after_emphasis.rst, tests/fixtures/inline_image_separator_render_gate/fail_13_image_after_reference.rst, tests/fixtures/inline_image_separator_render_gate/fail_14_image_in_field_list_body.rst, tests/fixtures/inline_image_separator_render_gate/fail_15_image_in_section_title.rst, tests/fixtures/inline_image_separator_render_gate/fail_16_image_with_width_mid_sentence.rst</files>
  <read_first>
    - .planning/research/FEATURES.md § Q1 - the rST source column for rows 2 through 16 verbatim; this is the authoritative source for each document's body
    - tests/fixtures/inline_image_separator_render_gate/fail_01_sub_mid_sentence.rst and conf.py (plan 01's output - the conventions to extend)
    - tests/fixtures/glob_image_render_gate/index.rst (the `/_static/...` image reference convention)
    - the `<fixture_document_map>` section of this plan
  </read_first>
  <action>
    Create the 15 remaining FAIL documents named in `<fixture_document_map>`, one per
    `.planning/research/FEATURES.md` § Q1 row, taking each body from that row's rST source column and
    substituting `/_static/pic.png` for its `img.png` placeholder. Per D-01 (IMG-08's full container
    set), all 16 must exist - the matrix is the measured trigger surface, not a sample of it.

    Shape-specific requirements that a literal transcription of the Q1 column will otherwise miss:
    row 8's footnote is only retained by docutils when the body text carries a reference to it, so
    that document needs a body paragraph containing a `[#f1]_` reference in addition to the
    `.. [#f1]` footnote definition; rows 9 and 10 need a real `.. figure::` with a caption paragraph
    AND a following legend paragraph (a figure's second-and-later body paragraph is what docutils
    turns into a `legend` node); row 15 carries the substitution inside the section title itself, so
    the title underline must be at least as long as the rendered title line; and row 16's `:width:
    50px` option belongs on the `.. |sub| image::` substitution definition, not on a separate
    directive.

    Extend `conf.py`'s `typst_documents` to 17 entries - `index` plus all 16 FAIL docnames - each with
    a `<docname>-out.typ` target stem, preserving the Phase 47 de-collision rule. Extend `index.rst`'s
    toctree to list all 16 FAIL docnames plus `pass_parent`; this toctree is the `#include()` edge set
    that makes the image-free `index` master fail on the unfixed tree, so an omission here silently
    removes SC#1's blast-radius property (IMG-09).

    Add no `numref` usage anywhere. Add no `:scale:`/`:align:` option to any FAIL document - those
    options are silently dropped today and belong only to the PASS-side D2 shape.

    Then build the fixture and confirm all 17 masters now compile with the fix in place.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run python -m sphinx -b typstpdf tests/fixtures/inline_image_separator_render_gate /tmp/gsd62_p02_t1 &amp;&amp; test "$(ls /tmp/gsd62_p02_t1/*-out.pdf | wc -l)" = "17" &amp;&amp; grep -c 'width: 37.5pt' /tmp/gsd62_p02_t1/fail_16_image_with_width_mid_sentence.typ &amp;&amp; ! grep -rl 'numref' tests/fixtures/inline_image_separator_render_gate/</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/fixtures/inline_image_separator_render_gate/fail_*.rst | wc -l` returns 16
    - `uv run python -m sphinx -b typstpdf tests/fixtures/inline_image_separator_render_gate /tmp/gsd62_p02_t1` exits 0 and its stderr contains no `master document(s) failed`
    - Exactly 17 `*-out.pdf` files exist in the build directory, and each of `index-out.pdf`, `fail_09_image_in_legend_mid_text-out.pdf`, `fail_10_two_images_in_legend-out.pdf` and `fail_14_image_in_field_list_body-out.pdf` is non-empty and begins with the `%PDF` magic bytes
    - The build stderr contains neither `expected semicolon or line break` nor `cannot apply unary`
    - `grep -c 'width: 37.5pt' /tmp/gsd62_p02_t1/fail_16_image_with_width_mid_sentence.typ` returns 1 (the `:width:` conversion is untouched by this phase)
    - `grep -rl 'numref' tests/fixtures/inline_image_separator_render_gate/` finds no file
    - `python - <<'P'` style count check: `conf.py`'s `typst_documents` has exactly 17 entries at the end of this task
    - `git diff --name-status -- tests/` shows only `A` and `M` entries against files this phase itself created; no pre-existing test file appears
  </acceptance_criteria>
  <done>All 16 measured failing shapes exist as fixture documents, each is its own master, `index` toctrees every one of them, and a single `sphinx-build -b typstpdf` writes a valid PDF for all 17 masters with no Typst refusal of any kind in stderr.</done>
  <reversibility rating="reversible">Test-fixture data only; deleting the directory restores the prior state with no product effect.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add the 8 remaining PASS documents under pass_parent and reach 18 masters</name>
  <files>tests/fixtures/inline_image_separator_render_gate/conf.py, tests/fixtures/inline_image_separator_render_gate/pass_parent.rst, tests/fixtures/inline_image_separator_render_gate/pass_b_figure_with_caption.rst, tests/fixtures/inline_image_separator_render_gate/pass_c_image_first_in_paragraph.rst, tests/fixtures/inline_image_separator_render_gate/pass_d_image_with_dimensions_and_scale_align.rst, tests/fixtures/inline_image_separator_render_gate/pass_e_image_with_propagated_target_id.rst, tests/fixtures/inline_image_separator_render_gate/pass_f_figure_with_plain_legend.rst, tests/fixtures/inline_image_separator_render_gate/pass_g_figure_in_list_item_after_paragraph.rst, tests/fixtures/inline_image_separator_render_gate/pass_h_figure_first_in_list_item.rst, tests/fixtures/inline_image_separator_render_gate/pass_i_bare_image_first_in_list_item.rst</files>
  <read_first>
    - .planning/research/FEATURES.md § Q2 - the rST source column for rows B through I verbatim, and the closing paragraph explaining WHICH existing mechanism each row depends on staying undisturbed
    - tests/fixtures/inline_image_separator_render_gate/pass_a_standalone_block_image.rst and pass_parent.rst (plan 01's output)
    - .planning/ROADMAP.md § "Phase Details → Phase 62" SC#3 (the nine shapes named in the criterion's own words)
    - the `<fixture_document_map>` section of this plan
  </read_first>
  <action>
    Create the 8 remaining PASS documents named in `<fixture_document_map>`, one per
    `.planning/research/FEATURES.md` § Q2 row, taking each body from that row's rST source column and
    substituting `/_static/pic.png` for its `img.png` placeholder. Merge § Q2's rows D and D2 into the
    single `pass_d_image_with_dimensions_and_scale_align` document - two separate standalone block
    image directives, the first carrying `:width: 200px` and `:height: 100px`, the second carrying
    `:scale: 50%` and `:align: center` - matching ROADMAP SC#3's own single-item phrasing. Both are
    first in their own containers, so both stay on the no-separator path.

    Extend `pass_parent.rst`'s toctree to list all 9 PASS docnames, and `conf.py`'s `typst_documents`
    to the final 18 entries: `index`, the 16 FAIL docnames, and `pass_parent`. The 9 PASS documents
    are NOT masters - they are toctree'd children of `pass_parent` only, and are reachable from no
    other master. That disjointness is what makes `pass_parent`'s green verdict a real positive
    control (D-03) rather than an artefact: if a PASS document were also reachable from `index` or a
    FAIL master, its own compile status would stop being independently attributable.

    Update `conf.py`'s provenance comment block to state the final counts (18 masters, 27 documents),
    to name `pass_parent` as the D-03 positive control that must stay green in the RED run, and to
    record the load-bearing properties an editor must not disturb: `index` carries no image;
    `pass_parent`'s children are shared with no other master; every target stem is `<docname>-out`;
    no `numref` usage.

    Then build and confirm all 18 masters compile and every one of the 9 PASS content `.typ` files is
    emitted.
  </action>
  <verify>
    <automated>uv run python -m sphinx -b typstpdf tests/fixtures/inline_image_separator_render_gate /tmp/gsd62_p02_t2 &amp;&amp; test "$(ls /tmp/gsd62_p02_t2/*-out.pdf | wc -l)" = "18" &amp;&amp; test "$(ls tests/fixtures/inline_image_separator_render_gate/*.rst | wc -l)" = "27" &amp;&amp; test "$(ls tests/fixtures/inline_image_separator_render_gate/pass_[a-i]*.rst | wc -l)" = "9" &amp;&amp; test -s /tmp/gsd62_p02_t2/pass_parent-out.pdf</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/fixtures/inline_image_separator_render_gate/*.rst | wc -l` returns 27
    - `ls tests/fixtures/inline_image_separator_render_gate/pass_[a-i]*.rst | wc -l` returns 9
    - `conf.py`'s `typst_documents` holds exactly 18 entries and every target stem ends `-out.typ`
    - `uv run python -m sphinx -b typstpdf tests/fixtures/inline_image_separator_render_gate /tmp/gsd62_p02_t2` exits 0 and writes exactly 18 `*-out.pdf` files, each non-empty and `%PDF`-prefixed
    - All 9 `pass_*` content `.typ` files exist in the build directory and each begins with the shared `// Essential imports for included document` `@preview` preamble that every included content file carries
    - `grep -c 'pass_' tests/fixtures/inline_image_separator_render_gate/index.rst` returns 1 (only `pass_parent`; no PASS child is reachable from `index`)
    - `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/ | grep -cv '^A' ` returns 0 for every path outside this phase's own new files
  </acceptance_criteria>
  <done>The fixture holds 27 documents and 18 masters; all 18 compile to valid PDFs in one build; the 9 PASS documents are reachable only from `pass_parent`, making it an independently attributable positive control.</done>
  <reversibility rating="reversible">Test-fixture data only.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Widen the gate module to the full 18-master matrix</name>
  <files>tests/test_inline_image_separator_render_gate.py</files>
  <read_first>
    - tests/test_inline_image_separator_render_gate.py (plan 01's output - the skeleton, constants and helpers to widen)
    - tests/test_abbr_pep_separator_render_gate.py (the multi-shape FAIL+PASS pairing variant, for the class-organisation convention)
    - .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RESEARCH.md § "Gate skeleton to extend" and its "Structural delta this phase's gate must add"
    - .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-VALIDATION.md § "Per-Task Verification Map" (the `-k fail` and `-k full_matrix` selectors this module must keep resolvable)
    - typsphinx/builder.py lines 2600-2645 (`TypstPDFBuilder.finish()`'s failure-accumulation loop and the aggregate error it raises)
  </read_first>
  <action>
    Widen the gate module created in plan 01 so it asserts across the full matrix from ONE
    `_run_sphinx_build_typstpdf()` invocation - this is the structural delta neither precedent gate
    has, since both compile exactly one master per fixture.

    Widen `FAIL_DOCNAMES` to all 16 FAIL docnames in numeric order, `PASS_DOCNAMES` to all 9 PASS
    docnames in `a` through `i` order, and keep `ALL_MASTER_DOCNAMES` derived as
    `["index"] + FAIL_DOCNAMES + ["pass_parent"]` so the 18-master list has exactly one definition.

    In `TestInlineImageSeparatorFullMatrix`, keep `test_full_matrix_every_master_writes_a_pdf` running
    ONE build and extend it to iterate `ALL_MASTER_DOCNAMES`, asserting for each that the wrapper PDF
    exists, is non-empty and is `%PDF`-prefixed; include the docname in every assertion message so a
    single failing master is attributable without re-running. Add
    `test_full_matrix_stderr_carries_no_typst_refusal`, asserting that neither `expected semicolon or
    line break` nor `cannot apply unary` nor `master document(s) failed` appears in the build stderr -
    the second of those is the refusal a concat-unaware trailing half produces and would otherwise be
    invisible behind a generic returncode check. Add
    `test_full_matrix_pass_parent_positive_control`, asserting `pass_parent`'s own wrapper PDF exists
    and is valid AND that the build stdout carries its `Generated PDF:` line - per D-03 and
    62-RESEARCH.md Pitfall 2 a successful master is NEVER named in the aggregate error text, so its
    green verdict must be read from the filesystem and the log, never from an exception.

    In `TestInlineImageSeparatorFailShapes`, parametrize
    `test_fail_shape_emits_a_separator_before_image` over all 16 `FAIL_DOCNAMES`, reading each
    document's emitted content `.typ` with an explicit utf-8 text read and asserting the body contains
    no occurrence of a closing parenthesis immediately followed by `image(`. Add
    `test_fail_16_width_conversion_is_unchanged` asserting `fail_16`'s content `.typ` still carries the
    converted `width: 37.5pt` value, pinning that this phase changed nothing about dimension handling.

    Share the single build across the methods of a class using a class-scoped or module-scoped pytest
    fixture rather than rebuilding 18 masters per test - measured cost is 1 to 2 seconds per build, so
    a per-test rebuild would multiply it by the method count for no additional signal. Keep the
    `-k full_matrix` and `-k fail` selectors resolvable: every method name must contain one of those
    two tokens.

    Do not name any forbidden binary-read API in any comment or docstring in this module.
  </action>
  <verify>
    <automated>uv run pytest tests/test_inline_image_separator_render_gate.py -q &amp;&amp; uv run pytest tests/test_inline_image_separator_render_gate.py -k full_matrix -q &amp;&amp; uv run pytest tests/test_inline_image_separator_render_gate.py -k fail -q &amp;&amp; uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; test "$(grep -c 'read_bytes' tests/test_inline_image_separator_render_gate.py)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_inline_image_separator_render_gate.py -q` exits 0 with 0 failed and 0 skipped
    - `uv run pytest tests/test_inline_image_separator_render_gate.py -k fail -q` selects at least 17 tests (16 parametrized shapes plus the width-conversion pin) and all pass
    - `uv run pytest tests/test_inline_image_separator_render_gate.py -k full_matrix -q` selects at least 3 tests and all pass
    - `grep -c 'typst.compile\|TYPST_AVAILABLE' tests/test_inline_image_separator_render_gate.py` returns a value greater than 0
    - `grep -c 'read_bytes' tests/test_inline_image_separator_render_gate.py` returns 0
    - `uv run pytest -q` exits 0 with 0 failed; `uv run black --check .` and `uv run mypy typsphinx/` both exit 0
    - `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/` shows no `M` entry against any file that existed at `PHASE_BASE_SHA`
  </acceptance_criteria>
  <done>One `sphinx-build -b typstpdf` drives assertions over all 18 masters and all 16 failing shapes; `pass_parent`'s success is read from disk and stdout rather than from the exception; the `-k fail` and `-k full_matrix` selectors both resolve; and the full suite, `black` and `mypy` are green.</done>
  <reversibility rating="reversible">Test-only additions to a module this phase created.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rST source document → `TypstTranslator` | Untrusted document text (image URIs, substitution names, titles, link targets) crosses into emitted Typst code-mode source |
| emitted `.typ` → `typst.compile()` | Emitted source is parsed and executed by the Typst compiler, 18 times in one process |
| fixture `_static/pic.png` → build output tree | A binary asset is copied into the build directory by `copy_image_files()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-01 | Tampering | `visit_image()`'s emitted `image("…")` string literal | high | mitigate | `escape_typst_string()` (existing, IMG-05) is reused unmodified and this plan edits no product file at all - `files_modified` contains only fixture data and the new gate module. Task 3's full-suite run re-proves `tests/test_typst_string_escape_gate.py` and `tests/test_image_literal_escaping_gate.py` unedited. |
| T-62-02 | Tampering | `fail_13_image_after_reference.rst`'s external link target | medium | mitigate | The link target is a literal `https://example.com` in committed fixture data - it is never fetched, resolved or followed by any builder in this phase; the reference exists only to place an image after a `reference` node. No network access is introduced. |
| T-62-03 | Information Disclosure | build output written to `/tmp` during the fixture builds | low | accept | Build directories are per-run temporary paths holding only the fixture's own synthetic content and a 68-byte stand-in PNG; nothing sensitive is written and nothing is committed from them in this plan. |
| T-62-04 | Denial of Service | 18-master compile cost per gate run | low | mitigate | The build is shared across the class's test methods via one pytest fixture rather than rebuilt per test; measured cost is 1 to 2 seconds per build, which is why no `@pytest.mark.slow` marker is needed (research assumption A1). |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager install tasks in this plan. `62-RESEARCH.md` § "Package Legitimacy Audit" records "Not applicable"; no `[ASSUMED]`, `[SUS]` or `[SLOP]` package exists to gate. |

ASVS L1, blocking on `high`. The one `high` threat is dispositioned `mitigate` with a runnable check.
This plan touches no product code, so it opens no new security surface; the register is carried
forward rather than re-derived, per `62-RESEARCH.md` § "Security Domain".
</threat_model>

<verification>
- One `sphinx-build -b typstpdf` over the fixture exits 0 and writes 18 valid PDFs.
- Build stderr contains no `expected semicolon or line break`, no `cannot apply unary`, and no
  `master document(s) failed`.
- 27 `.rst` documents; 16 `fail_*`; 9 `pass_*`; 18 `typst_documents` entries.
- `-k fail` selects at least 17 tests, `-k full_matrix` at least 3; all pass.
- `uv run pytest -q`, `uv run black --check .`, `uv run mypy typsphinx/` all green.
- No `M` entry under `tests/` against any file that existed at `PHASE_BASE_SHA`.
</verification>

<success_criteria>
- Every one of the 16 measured failing shapes, including the three the originally-recommended
  mechanism left broken (legend mid-text, two-images-in-legend, field-list body), compiles (IMG-08).
- Every one of the 18 masters, including the image-free `index`, writes a valid PDF (IMG-09).
- The gate binds the full FAIL and PASS matrix on a real `typst.compile()` from one build, with the
  positive control read from the filesystem and stdout rather than from the aggregate exception
  (TEST-05).
</success_criteria>

<output>
Create `.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-02-SUMMARY.md` when done.
Declare `requirements-completed: []` - the phase's requirements close only after plan 04's
phase-close measurements.
</output>
