---
phase: 62-the-visit-image-separator-fix-and-its-real-compile-gate
plan: 04
type: execute
wave: 4
depends_on: ["62-03"]
files_modified:
  - .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md
autonomous: true
requirements: [IMG-09, IMG-10]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'A CI run dispatched with `gh workflow run CI --ref gsd/v0.9.2-inline-image-blocker-fix-and-release` has reached the `completed` status, and its `windows-latest` and `macos-latest` lanes are each named individually with their own recorded conclusion.'
    - '`ruff`''s verdict for this phase is taken from that run''s `Run linters` step and from nowhere else - never from this machine, where `ruff` is an unrunnable generic-linux ELF in any freshly provisioned worktree venv.'
    - 'Exactly one `0.9.2` branch exists locally and it is the canonical config-derived name; if the decoy reappeared, the canonical pointer was advanced before the decoy was deleted.'
    - 'No tag matching `v0.9.2` exists locally or on `origin`, and no PyPI upload, GitHub Release or PR was created - the release half belongs to Phase 63 and `/gsd-complete-milestone`.'
    - '`git diff --name-status` over the phase range scoped to `tests/` shows only `A` entries; any `M` entry is reported as an over-reach signal with its justification rather than absorbed as routine test maintenance.'
    - 'The phase diff over `typsphinx/translator.py` is a pure insertion in which both `in_figure` branch bodies are textually unchanged, and none of the three line-boundary-predicate spellings ROADMAP SC#3 enumerates appears in that file.'
    - 'The two exact-byte figure assertions in `tests/test_nested_figure_render_gate.py` and `tests/test_pdf_render_gate.py` pass and neither file was edited in this phase.'
    - '`CHANGELOG.md`, `pyproject.toml`, `uv.lock` and `README.md` are untouched by the entire phase range.'
    - statement: 'The final measurement is taken on the merged phase tip rather than on any individual worktree, so the wave-merge order cannot hide a `tests/` modification introduced by one plan and reverted in another.'
      verification: backstop
  artifacts:
    - '.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md :: § "SC#5 - authority CI run" and § "Phase-close measurements"'
  key_links:
    - '`gh workflow run CI --ref <branch>` -> the run''s `Run linters` step: the only link by which this phase obtains a `ruff` verdict, because `ci.yml`''s push/pull_request triggers are scoped to `main`/`develop` and a push alone starts nothing.'
    - 'PHASE_BASE_SHA (recorded in plan 01) -> `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/`: the only link by which D-13 is measured rather than asserted.'
    - 'the canonical branch pointer -> the decoy `gsd/v0.9.2-milestone`: the pointer must be advanced before any deletion, because the ahead/behind direction has been observed in both directions across milestones.'
  prohibitions:
    - 'MUST NOT report a lint verdict obtained from this machine as the phase lint verdict; ruff authority belongs to the dispatched CI run only.'
    - 'MUST NOT dispatch more than one authority CI run for this phase.'
    - 'MUST NOT take any irreversible release action - no version literal, no CHANGELOG.md edit, no tag local or remote, no PyPI upload, no GitHub Release, no PR.'
    - 'MUST NOT absorb a modification to a pre-existing test as routine maintenance; if one exists it is reported as an over-reach signal with its justification.'
---

<objective>
Discharge SC#5's second half - one dispatched CI run, waited to completion, with the `windows-latest`
and `macos-latest` lanes named individually and `ruff`'s verdict taken from its `Run linters` step -
and take the phase-close measurements that turn IMG-10's and D-13's claims into observations.

Purpose: this plan audits the three that precede it. Keeping the measurement and its audit in
separate waves is deliberate: this project has already recorded one case where an audit placed in the
same wave as the thing it audits abstained on its own criterion. CI is also the only instrument that
can see the three defect classes local runs structurally cannot - a locale-dependent failure, a
`ruff` violation, and a Windows-only path or newline behaviour.

Output: `62-RED-EVIDENCE.md` gains an SC#5 authority-run section and a phase-close measurement
section. No source file changes.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-CONTEXT.md
@.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-VALIDATION.md
@.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-01-PLAN.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution",
ROADMAP constraint 12). Before running ANY command in this plan:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Do not attempt a local `ruff` invocation even
after provisioning - it is the exact configuration in which `ruff` is known unrunnable on this host.
</worktree_provisioning>

<wave_rationale>
Wave 4, last. The authority run must be dispatched on the phase's final code tip, so every plan that
touches `typsphinx/` or `tests/` must have merged first. The phase-close measurements must likewise
run on the merged tip: a `tests/` modification introduced in one worktree and reverted in another is
invisible from inside either worktree and visible only from the merged range. Only planning-document
commits follow the dispatch, and `.planning/` changes cannot affect a CI result - this plan states
that explicitly in the evidence rather than leaving it implied.
</wave_rationale>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch the single authority CI run and record all three OS lanes</name>
  <files>.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md</files>
  <read_first>
    - .github/workflows/ci.yml (the `on:` block, the OS x Python matrix, and the `Run linters` step name this task quotes a verdict from)
    - .planning/ROADMAP.md § "🚧 v0.9.2" constraints 10 and 11, and § "Phase Details → Phase 62" SC#5
    - .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-CONTEXT.md § D-11, D-12
    - .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md § "SC#5 - branch on origin" (plan 01's push record, which this section continues)
    - .planning/STATE.md § "Accumulated Context" → the Phase 53 entry (the precedent for recording an honest multi-run CI history when the first run fails on something unrelated)
  </read_first>
  <precondition>`gh auth status` exits 0 and `git ls-remote --heads origin` succeeds from this worktree - both the final push and the workflow dispatch require an authenticated session no plan task can provision.</precondition>
  <action>
    FIRST re-run `git branch -vv` and apply D-12: if the decoy `gsd/v0.9.2-milestone` has been
    re-created by a commit helper since plan 01, advance the canonical
    `gsd/v0.9.2-inline-image-blocker-fix-and-release` pointer to whichever tip is ahead BEFORE deleting
    the decoy - never delete first, because the ahead/behind direction has been observed in both
    directions across consecutive milestones and a delete-first orphans commits.

    Then push the phase's final code tip to `origin` on the canonical branch so the run is dispatched
    against the tree that actually contains the fix, the fixture, the goldens and the gate.

    Dispatch exactly ONE run (D-11):
    `gh workflow run CI --ref gsd/v0.9.2-inline-image-blocker-fix-and-release`. Resolve the run id it
    created, then wait for it to reach the `completed` status - poll or watch, but do not report on a
    run still `in_progress`. If the run fails on something demonstrably unrelated to this phase, follow
    the Phase 53 precedent: fix the unrelated cause, dispatch a second run, and record the honest
    two-run history in the evidence rather than deleting the first. Do not dispatch a second run for
    any other reason.

    Append a `## SC#5 - authority CI run` section to `62-RED-EVIDENCE.md` recording: the dispatch
    command and the run id and URL; the run's final `status` and `conclusion`; a per-job table naming
    `ubuntu-latest`, `windows-latest` and `macos-latest` individually with each lane's own conclusion
    (SC#5 requires the two non-Linux lanes named individually, not summarised as "all green"); and the
    `Run linters` step's conclusion quoted as the phase's `ruff` verdict, with an explicit sentence
    stating that this is the only source of that verdict and why - `ruff` fails to start on this host
    in a freshly provisioned worktree venv, so a green local `black --check .` plus `mypy` is not
    "lint clean" (ROADMAP constraint 11).

    Close the section by stating that only `.planning/` documentation commits follow this dispatch and
    that they cannot affect the result, so the run is authoritative for the phase's code tip.
  </action>
  <verify>
    <automated>gh run list --workflow CI --branch gsd/v0.9.2-inline-image-blocker-fix-and-release --limit 1 --json status,conclusion --jq '.[0] | select(.status=="completed" and .conclusion=="success") | "ok"' | grep -q ok &amp;&amp; grep -q 'windows-latest' .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md &amp;&amp; grep -q 'macos-latest' .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md &amp;&amp; grep -q 'Run linters' .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md &amp;&amp; test -z "$(git tag -l 'v0.9.2*')" &amp;&amp; test -z "$(git ls-remote --tags origin 'v0.9.2*')"</automated>
    <human-check>Open the recorded run URL and confirm by eye that the `windows-latest` and `macos-latest` lanes both show a green conclusion and that the `Run linters` step succeeded - `human_verify_mode` is `end-of-phase`, so this is an end-of-phase visual confirmation rather than a blocking checkpoint.</human-check>
  </verify>
  <acceptance_criteria>
    - `gh run list --workflow CI --branch gsd/v0.9.2-inline-image-blocker-fix-and-release --limit 5 --json status,conclusion` shows the recorded run with `status` `completed`
    - `62-RED-EVIDENCE.md` § "SC#5 - authority CI run" names `ubuntu-latest`, `windows-latest` and `macos-latest` individually, each with its own recorded conclusion
    - That section quotes the `Run linters` step's conclusion and states in prose that it is the sole source of the phase's `ruff` verdict, with the reason
    - `git branch --list 'gsd/v0.9.2*' | wc -l` returns 1
    - `git tag -l 'v0.9.2*'` is empty and `git ls-remote --tags origin 'v0.9.2*'` is empty
    - `gh pr list --head gsd/v0.9.2-inline-image-blocker-fix-and-release` returns no pull request
    - `gh run list --workflow CI --branch gsd/v0.9.2-inline-image-blocker-fix-and-release` shows no more runs than the evidence file accounts for and explains
  </acceptance_criteria>
  <done>One CI run dispatched against the phase's final code tip has completed with the `windows-latest` and `macos-latest` lanes recorded individually and `ruff`'s verdict taken from its `Run linters` step; exactly one `0.9.2` branch exists; no tag, PR, upload or Release was created.</done>
  <reversibility rating="costly">A dispatched CI run consumes runner minutes and cannot be un-run; D-11 permits exactly one, and a second is justified only by the Phase 53 precedent (a failure demonstrably unrelated to this phase), recorded honestly rather than hidden.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Phase-close measurements - zero test edits, branch bodies untouched, release fence held</name>
  <files>.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md</files>
  <read_first>
    - .planning/ROADMAP.md § "Phase Details → Phase 62" SC#3 and SC#4 (the exact wording of the two structural criteria this task measures)
    - .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md § "Phase base SHA" (the range endpoint every measurement here uses)
    - .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-CONTEXT.md § D-09, D-13
    - typsphinx/translator.py lines 4718-4800 (the final post-fix state, to read the two `in_figure` branch bodies directly)
    - tests/test_nested_figure_render_gate.py around line 256 and tests/test_pdf_render_gate.py around line 2303
  </read_first>
  <action>
    Append a `## Phase-close measurements` section to `62-RED-EVIDENCE.md` recording each of the
    following as a transcript, not as a claim. Run every command against the merged phase tip using the
    `PHASE_BASE_SHA` recorded in plan 01 as the range start.

    D-13 / SC#4 - zero pre-existing test edits. Record `git diff --name-status $PHASE_BASE_SHA..HEAD --
    tests/` in full. Every line must begin with `A`. If ANY line begins with `M`, do not absorb it:
    record it, name the file, and report it in the plan SUMMARY as an over-reach signal with its
    justification, per SC#4's own wording. Record separately that none of the 20 files carrying the
    144 `image(` matches - and in particular none of the nine string-level image tests in
    `tests/test_translator.py` - appears in the diff at all.

    IMG-10 / SC#3 - the fix stayed inside its shape. Record `git diff --numstat $PHASE_BASE_SHA..HEAD
    -- typsphinx/translator.py` showing a pure insertion with zero deletions, and record the full
    `git diff` for that file so a reader can see directly that both `in_figure` branch bodies are
    textually unchanged and that the leading half sits above the branch split. Record the result of
    the absence check over `typsphinx/translator.py` for the three line-boundary-predicate spellings
    ROADMAP SC#3 enumerates - it must find nothing. Record that
    `tests/test_nested_figure_render_gate.py` and `tests/test_pdf_render_gate.py` both pass and that
    neither appears in the phase's `tests/` diff, discharging SC#3's exact-byte figure-assertion clause.

    D-09 / prep-only fence. Record `git diff --name-only $PHASE_BASE_SHA..HEAD -- CHANGELOG.md
    pyproject.toml uv.lock README.md` as empty, and `git tag -l` and `git ls-remote --tags origin` as
    carrying no `v0.9.2` tag. Phase 63 owns every CHANGELOG bullet and the version bump; this phase's
    diff is `typsphinx/translator.py` plus new test files plus planning documents, and that is
    recorded rather than assumed.

    Finally record one full-suite run on the merged tip with `black --check` and `mypy`, and state
    explicitly that no local `ruff` verdict is claimed anywhere in this phase - task 1's CI run holds
    that authority.

    Also cross-check the amendments: confirm that the shipped `visit_image()` still applies the leading
    half above the figure/non-figure split (the AMENDED D-08 placement, without which the two legend
    shapes regress) and that `depart_image()`'s trailing half is still concat-aware (without which the
    field-list shape regresses). Record both as a direct read of the final source, not as an inference
    from the green suite.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; test -z "$(git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/ | grep -v '^A')" &amp;&amp; test "$(git diff --numstat $PHASE_BASE_SHA..HEAD -- typsphinx/translator.py | awk '{print $2}')" = "0" &amp;&amp; ! grep -F -e 'endswith("\n")' -e 'rstrip().endswith' -e '[-1:]' typsphinx/translator.py &amp;&amp; test -z "$(git diff --name-only $PHASE_BASE_SHA..HEAD -- CHANGELOG.md pyproject.toml uv.lock README.md)" &amp;&amp; uv run pytest -q &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; grep -q 'Phase-close measurements' .planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-RED-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-status $PHASE_BASE_SHA..HEAD -- tests/ | grep -cv '^A'` returns 0
    - `git diff --name-only $PHASE_BASE_SHA..HEAD -- tests/test_translator.py tests/test_nested_figure_render_gate.py tests/test_pdf_render_gate.py` is empty
    - `git diff --numstat $PHASE_BASE_SHA..HEAD -- typsphinx/translator.py` reports 0 deleted lines
    - `! grep -F -e 'endswith("\n")' -e 'rstrip().endswith' -e '[-1:]' typsphinx/translator.py` succeeds
    - `git diff --name-only $PHASE_BASE_SHA..HEAD -- CHANGELOG.md pyproject.toml uv.lock README.md` is empty
    - `git diff --name-only $PHASE_BASE_SHA..HEAD -- typsphinx/` lists exactly one file, `typsphinx/translator.py`
    - `uv run pytest -q` exits 0 with 0 failed; `uv run black --check .` and `uv run mypy typsphinx/` both exit 0
    - `62-RED-EVIDENCE.md` contains a `## Phase-close measurements` section carrying the verbatim transcripts of every command above
    - The SUMMARY states that no local `ruff` verdict was claimed for this phase
  </acceptance_criteria>
  <done>Every SC#3, SC#4 and D-09 obligation is recorded as a transcript taken on the merged phase tip: only added files under `tests/`, a pure-insertion translator diff with both `in_figure` branch bodies untouched, no line-boundary predicate, no version or CHANGELOG literal moved, and a green suite with lint authority left to CI.</done>
  <reversibility rating="reversible">Measurement and evidence recording only; no source file is altered.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local worktree → `origin` | The phase's final code tip crosses the repository boundary |
| local repository → GitHub Actions | A workflow dispatch causes the project's own committed `ci.yml` to execute on hosted runners |
| CI run output → phase evidence | A remote run's conclusions cross into the phase's recorded verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-02 | Spoofing | branch identity at push and dispatch time | medium | mitigate | The canonical branch name is derived from `config.json`'s `git.milestone_branch_template`; `git branch -vv` is re-read before the push and the decoy, if present, is resolved pointer-first per D-12. Acceptance asserts exactly one local `0.9.2` branch. |
| T-62-06 | Elevation of Privilege | `gh workflow run` dispatch | low | accept | The dispatch runs the repository's own committed `ci.yml` under the developer's existing authenticated `gh` session; no secret, token, permission or workflow file is added or modified by this phase. |
| T-62-08 | Repudiation | a CI verdict claimed from the wrong run or the wrong tip | medium | mitigate | The run id and URL are recorded, its `status` must read `completed`, each OS lane's conclusion is recorded individually, and the evidence states that only `.planning/` commits follow the dispatch so the run is authoritative for the code tip. A second run, if any, is recorded rather than hidden (Phase 53 precedent). |
| T-62-09 | Tampering | an irreversible release action taken early | high | mitigate | Acceptance asserts `git tag -l 'v0.9.2*'` empty, `git ls-remote --tags origin 'v0.9.2*'` empty, no PR on the branch, and an empty diff over `CHANGELOG.md`, `pyproject.toml`, `uv.lock` and `README.md` across the whole phase range. The prep-only fence is measured, not asserted. |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager install tasks in this plan. `62-RESEARCH.md` § "Package Legitimacy Audit" records "Not applicable"; no `[ASSUMED]`, `[SUS]` or `[SLOP]` package exists to gate. |

ASVS L1, blocking on `high`. The one `high` threat is dispositioned `mitigate` with runnable checks.
This plan changes no product code and therefore opens no new input-validation surface; V5's control
(`escape_typst_string()`) remains as reused and unmodified by the phase.
</threat_model>

<verification>
- A `completed` CI run exists on the canonical branch with `ubuntu-latest`, `windows-latest` and
  `macos-latest` recorded individually and `Run linters` quoted as the phase's `ruff` verdict.
- Exactly one local `0.9.2` branch; no `v0.9.2` tag locally or on `origin`; no PR.
- `git diff --name-status` over the phase range scoped to `tests/` shows only `A` entries.
- `git diff --numstat` over the phase range for `typsphinx/translator.py` shows 0 deletions, and
  `typsphinx/` shows exactly one changed file.
- None of the three ROADMAP SC#3 line-boundary-predicate spellings appears in
  `typsphinx/translator.py`.
- `CHANGELOG.md`, `pyproject.toml`, `uv.lock` and `README.md` are untouched across the phase range.
- `uv run pytest -q`, `uv run black --check .`, `uv run mypy typsphinx/` all green on the merged tip.
</verification>

<success_criteria>
- SC#5 is discharged in full: the milestone branch is on `origin` and a dispatched run has completed
  with both non-Linux lanes named individually and `ruff`'s verdict taken from CI (D-10, D-11, D-12).
- SC#4 is discharged by measurement: zero pre-existing test edits, or an explicit over-reach report
  (D-13).
- SC#3's structural half is discharged by measurement: the `in_figure` branch bodies are unmodified,
  no line-boundary predicate exists, and the two exact-byte figure assertions pass unedited (IMG-10).
- The prep-only fence held: no version literal, no CHANGELOG bullet, no tag, no publish (D-09).
</success_criteria>

<output>
Create `.planning/phases/62-the-visit-image-separator-fix-and-its-real-compile-gate/62-04-SUMMARY.md` when done.
This is the phase's closing plan: declare `requirements-completed: [IMG-08, IMG-09, IMG-10, TEST-05]`
only if every acceptance criterion in this plan and in plans 01-03 was met and recorded; otherwise
declare the subset actually evidenced and name the gap.
</output>
