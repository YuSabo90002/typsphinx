---
phase: 68-documentation-follow-through-claude-md-tox-ini-flake-nix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tox.ini
  - tests/test_toolchain_config_gate.py
  - tests/test_pdf_render_gate.py
  - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-TOX-EVIDENCE.md
autonomous: true
requirements: [DOC-20, DOC-19]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'D-16: the tox.ini comment block between `isolated_build = True` and `requires = tox-uv~=1.35` consists only of `#` lines. It states the current `tox-uv~=1.35` pin, restates the ini-parser constraint in full (tox splits a single-line `requires` on commas, so `>=1.35,<2` is parsed as two bogus requirements and tox fails to start; `~=1.35` is the comma-free equivalent), and says why `tox-uv` is safe now (on NixOS the FHS shims run tox, so its bundled uv executes; CI runners were never affected). It also says the pin was briefly `tox-uv-bare` because the bundled uv could not exec on NixOS outside FHS.'
    - 'D-16 with the D-09 citation rule: the new tox.ini comment carries no decision ID, no SUMMARY-file citation and no `.planning/` path. Any evidence file it names sits on a line that also carries its milestone and phase ("v0.9.3 Phase NN").'
    - 'D-16: the `requires = tox-uv~=1.35` line and everything below it, and tox.ini lines 1-3, are byte-identical to base. The only removed lines are old lines 4-10, and every added line is a `#` comment. `uv run tox config -e py312 --core -k requires` still prints `tox-uv~=1.35` and no `tox-uv-bare`.'
    - 'D-16: the equivalence of `~=1.35` and `>=1.35,<2`, now stated without the old SUMMARY citation, is re-measured in this phase with `packaging.specifiers.SpecifierSet` over a version sample, recorded as `SPECIFIER_EQUIV = True`.'
    - 'D-13: in test_dev_extra_pins_tox_uv_not_tox_uv_bare, the docstring at base lines 302-304 and the assertion message tail at base lines 366-368 describe the post-Phase-68 state in past tense. CLAUDE.md "Conventions & gotchas" was rewritten in Phase 68. The strings no longer claim CLAUDE.md keeps naming tox-uv-bare as deliberate, and they do not quote any new CLAUDE.md heading.'
    - 'D-14: the _run_sphinx_build_typst docstring keeps its QUA-04 historical sentence and its closing "`sys.executable -m sphinx` is kept regardless" sentence verbatim (whitespace-normalised). Between them it gains one sentence saying the tox-uv revert put `.venv/bin/uv` back and on NixOS it now runs through the FHS shims.'
    - 'D-13 and D-14 are text only. With docstrings and assert messages masked, each edited test file AST hashes identically to base. Removed lines lie only inside the named ranges. The two files pass/skip summary and the suite collected count are identical before and after in this worktree. black --check and ruff check pass on both files.'
  artifacts:
    - path: tox.ini
      provides: 'D-16 requires-rationale comment'
      contains: 'requires = tox-uv~=1.35'
    - path: tests/test_toolchain_config_gate.py
      provides: 'D-13 docstring and assert-message tail in post-Phase-68 state'
      contains: 'Phase 68'
    - path: tests/test_pdf_render_gate.py
      provides: 'D-14 added sentence in _run_sphinx_build_typst docstring'
      contains: 'is kept regardless'
    - path: .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-TOX-EVIDENCE.md
      provides: 'head check, baseline, SpecifierSet equivalence, tox.ini rewrite, two test-file string edits, gates after the edits'
      contains: 'BASE_68_02 = '
  key_links:
    - from: 'tox.ini comment block'
      to: 'requires = tox-uv~=1.35'
      via: 'tox config -e py312 --core -k requires prints tox-uv~=1.35 (comments never parsed)'
      pattern: 'tox-uv~=1.35'
    - from: 'tests/test_toolchain_config_gate.py assertion logic'
      to: 'canonicalize_name("tox-uv-bare") not in dev_names'
      via: 'AST hash with docstrings and assert messages masked, identical to base'
      pattern: 'canonicalize_name'
  prohibitions:
    - statement: 'MUST NOT change the `requires` value or put a comma-separated range on the `requires` line (D-16; tox would fail to start).'
      requirement_id: DOC-20
      category: safety
      status: resolved
      verification: test
    - statement: 'MUST NOT leave tox-uv-bare rationale presented as current in tox.ini or either test file; every surviving mention must read as history or as the value a gate forbids (D-15 classes).'
      requirement_id: DOC-20
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT change any assertion logic, compared distribution name, test function or test count in the two test files (D-13, D-14 are text only).'
      requirement_id: DOC-20
      category: safety
      status: resolved
      verification: test
---

<objective>
Replace every piece of `tox-uv-bare` rationale presented as current outside CLAUDE.md with text describing the landed pin, without changing anything tox or pytest executes (DOC-20, and DOC-19's test-file echoes).

- **D-16:** `tox.ini`'s `requires` comment (lines 4-10) is rewritten. It keeps the ini-parser constraint in full, states why `tox-uv` is safe now, and keeps a one-to-two-sentence history. The old decision-ID and SUMMARY citations are restated in self-contained form under the D-09 citation rule. The SUMMARY file the old citation named no longer exists in the repository, so the equivalence it cited is re-measured here instead.
- **D-13:** `tests/test_toolchain_config_gate.py`'s docstring (302-304) and assertion-message tail (366-368) move to the post-Phase-68 state.
- **D-14:** `tests/test_pdf_render_gate.py`'s docstring gains one sentence after the QUA-04 sentence.

Nothing executable changes. An AST hash with docstrings and assert messages masked, a tox `requires` read-back, and same-worktree pytest counts prove it.

Output: the three edited files and `68-TOX-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-PATTERNS.md
@tox.ini
@tests/test_toolchain_config_gate.py
@tests/test_pdf_render_gate.py
</context>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
`## HALT` is a heading the executor writes only on a failure path; the gates require its absence.

## Planning-time census (2026-09-13: re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `tox.ini:4-10` | the stale rationale block; `:4` opens with the old pin name, and `:7` spells the bogus requirement with the old name | `Read`; RESEARCH § D-15 rows 26-27 |
| `tox.ini:11` | `requires = tox-uv~=1.35` | `Read` |
| `tox.ini:19-32` | the unrelated `package = editable` comment, out of scope | `Read` |
| `tox config -e py312 --core -k requires` | prints `requires =`, then `  tox-uv~=1.35`, then `  tox` (tox adds itself) | main checkout, 2026-09-13 |
| Old equivalence citation | the Phase-04 SUMMARY the comment names exists nowhere in the repository (`find .planning -name` returns nothing), so it cannot be cited archive-stably | `find` |
| `tests/test_toolchain_config_gate.py:302-304`, `:366-368` | the D-13 targets; `:164` cites `CLAUDE.md 'Conventions & gotchas'` and is untouched | `Read`, `grep -n` |
| `tests/test_pdf_render_gate.py:166-171` | the QUA-04 sentence (`:166-168`) and the closing reasoning (`:168-171`) | `Read` |
| Two-file pytest result | `35 passed` (main checkout, nixpkgs python). A worktree may differ, so compare within this worktree only (D-06). | `pytest tests/test_toolchain_config_gate.py tests/test_pdf_render_gate.py -q` |
| AST hash, docstrings and assert messages masked | changing a compared name changes the hash; changing a message does not (dry-run 2026-09-13) | session probe |

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| New `requires` rationale comment | edited comment block | `tox.ini`, between `isolated_build = True` and `requires = tox-uv~=1.35` |
| Post-Phase-68 docstring and assert-message tail | edited strings | `tests/test_toolchain_config_gate.py`, `test_dev_extra_pins_tox_uv_not_tox_uv_bare` |
| Added revert/FHS sentence | edited docstring | `tests/test_pdf_render_gate.py`, `_run_sphinx_build_typst` |
| `68-TOX-EVIDENCE.md` | new evidence file. Sections: `## Head check and provisioning`, `## Baseline`, `## SpecifierSet equivalence`, `## tox.ini rewrite`, `## test_toolchain_config_gate.py strings`, `## test_pdf_render_gate.py sentence`, `## Gates after the edits`. Keys: `BASE_68_02`, `PYVENV_HOME_68_02`, `PYVENV_VERSION_68_02`, `COLLECT_BEFORE_68_02`, `COLLECT_AFTER_68_02`, `TWO_FILE_RESULT_BEFORE`, `TWO_FILE_RESULT_AFTER`, `SPECIFIER_EQUIV` | phase directory |

## Flagged assumptions (spec-less fallback, surfaced and not auto-resolved)

- **Edge-probe row `DOC-20`:** category `unclassified`, status `unresolved`. No edge was auto-resolved or dismissed. Planner's reading, for the verifier to confirm or reject:
  - DOC-20's mechanically checkable edges are the ones Tasks 1-3 gate:
    - the `requires` value read back by tox
    - the byte fences around the comment block
    - the AST skeleton identity of the two test files
    - the same-worktree pass and collect counts
  - Whether each surviving `tox-uv-bare` mention reads as history is a judgment classified on the merged tree in 68-04 (D-15).
- **Edge-probe row `DOC-19`:** this plan touches DOC-19 only through the test-file prose that describes CLAUDE.md. Same status, surfaced, not resolved.
- **Prohibitions:** the three prohibitions carry no wired-check descriptor (spec-less fallback), so each disposes as flagged-unverified downstream.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: head check and baseline, SpecifierSet re-measurement, then the D-16 tox.ini comment rewrite gated by tox's own requires read-back</name>
  <precondition>The session was launched from a shell in which direnv loaded the main checkout's devShell, so `grep -q typsphinx-fhs-run "$(command -v uv)"` succeeds in this worktree. Otherwise write `## HALT: uv shim not on PATH` and stop: an environment prerequisite, not a code regression.</precondition>
  <reversibility rating="reversible">A comment block in a git-tracked config file. tox never reads comments, and one revert restores it.</reversibility>
  <files>tox.ini, .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-TOX-EVIDENCE.md</files>
  <read_first>
    - tox.ini (lines 1-35; only 4-10 are in scope)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md (D-16, D-09 citation rule, D-15)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md (§ Current Text Inventory → tox.ini; § Common Pitfalls 1)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-PATTERNS.md (§ tox.ini: the `package = editable` idiom to copy)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md (§ TOX-03 D-02 observation inside FHS: `using bundled uv from: <tree>/.venv/bin/uv`)
    - .planning/ROADMAP.md (§ Phase 68 SC#2)
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `command -v uv` and `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, the tail of its output
   - `BASE_68_02 = <git merge-base HEAD gsd/v0.9.3-toolchain-and-dependency-update-repair>`, measured here and equal to `git rev-parse HEAD` before any commit

2. **`## Baseline`**, each command quoted:
   - `PYVENV_HOME_68_02` and `PYVENV_VERSION_68_02`, from `sed -n 's/^home = //p;s/^version_info = //p' .venv/pyvenv.cfg`
   - `COLLECT_BEFORE_68_02`, the N from `uv run pytest --collect-only -q -p no:cacheprovider`'s last line "N tests collected"
   - `TWO_FILE_RESULT_BEFORE`, from `uv run pytest tests/test_toolchain_config_gate.py tests/test_pdf_render_gate.py -q -p no:cacheprovider`: its last line, reduced by `grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' '`. It must contain no `failed` and no `error`; otherwise write `## HALT: baseline not green` and stop.
   - `uv run tox config -e py312 --core -k requires` verbatim
   - `sed -n '1,11p' tox.ini` verbatim

3. **`## SpecifierSet equivalence`** (D-16: the old comment cited an equivalence proof in a SUMMARY file that no longer exists, so re-measure it). Run and quote `uv run python -c` with a one-line program that:
   - builds `SpecifierSet('~=1.35')` and `SpecifierSet('>=1.35,<2')` from `packaging.specifiers`
   - tests each of the versions `1.34.9`, `1.35`, `1.35.0`, `1.35.1`, `1.36.0`, `1.99.99`, `2.0`, `2.0.0`, `2.1`, `1.35.0rc1` and `2.0.0a1` against both, printing one `version in_a in_b` row each
   - prints `True` last if every row agrees

   Write `SPECIFIER_EQUIV = <that last value>`. It must be `True`; otherwise write `## HALT: specifier equivalence falsified` and stop.

4. **D-16 rewrite.** Replace lines 4-10 of `tox.ini` with a new block of `#` comment lines only. No blank line and no non-comment line goes inside it. It sits directly between `isolated_build = True` and `requires = tox-uv~=1.35`, and follows the `package = editable` block's idiom: current setting, constraint, why it is safe, history. The block states:
   - (a) `requires` pins `tox-uv~=1.35`.
   - (b) The parser constraint, in full. tox's own ini-list loader splits a single-line `requires` value on "," (only multi-entry lists keep newline splitting), so a literal `>=1.35,<2` is parsed as two bogus requirements, `tox-uv>=1.35` and `<2`, and tox fails to even start. `~=1.35` admits the same versions as `>=1.35,<2` with no comma.
   - (c) Why `tox-uv` is safe now. On NixOS the FHS shims from `flake.nix` run `tox` inside the sandbox, so the `uv` that `tox-uv` bundles (resolved as the tree's `.venv/bin/uv`) executes. CI runners were never affected. Use the words `bundled`, `NixOS`, `FHS` and `CI`.
   - (d) One-to-two sentences of history: the pin was briefly `tox-uv-bare`, because outside FHS that bundled `uv` could not exec on NixOS.
   - (e) Optional citations, per the D-09 rule. Write each as milestone, phase and file name on one comment line, e.g. v0.9.3 Phase 65 with `65-REVERT-EVIDENCE.md`, or v0.9.3 Phase 68 with `68-TOX-EVIDENCE.md` for the re-measured equivalence. No decision ID, no SUMMARY file name, no `.planning/` path.

   Do not touch lines 1-3, the `requires =` line, or anything below it, including the `package = editable` block.

5. **`## tox.ini rewrite`.** Quote each of these:
   - `sed -n '1,/^requires = /p' tox.ini`
   - `git diff -U0 "$BASE_68_02" -- tox.ini`: removed lines are only old 4-10, and every added line starts with `#`
   - `uv run tox config -e py312 --core -k requires`: it still prints `tox-uv~=1.35` and no `tox-uv-bare`
   - the `sha256sum` of `sed -n '/^requires = /,$p' tox.ini`, next to the same over `git show "$BASE_68_02:tox.ini"`: equal

6. Commit `tox.ini` and the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-TOX-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_68_02)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; for v in "$(k PYVENV_HOME_68_02)" "$(k PYVENV_VERSION_68_02)" "$(k COLLECT_BEFORE_68_02)" "$(k TWO_FILE_RESULT_BEFORE)"; do [ -n "$v" ] || { echo "missing evidence key"; exit 1; }; done &amp;&amp; case "$(k TWO_FILE_RESULT_BEFORE)" in *failed*|*error*) false ;; esac &amp;&amp; [ "$(k SPECIFIER_EQUIV)" = True ] &amp;&amp; RQ="$(uv run tox config -e py312 --core -k requires 2&gt;/dev/null)" &amp;&amp; printf '%s\n' "$RQ" | grep -qx '[[:space:]]*tox-uv~=1.35' &amp;&amp; ! printf '%s\n' "$RQ" | grep -q 'tox-uv-bare' &amp;&amp; [ "$(grep -c '^requires = tox-uv~=1.35$' tox.ini)" = 1 ] &amp;&amp; [ "$(sed -n '/^requires = /,$p' tox.ini | sha256sum)" = "$(git show "$B:tox.ini" | sed -n '/^requires = /,$p' | sha256sum)" ] &amp;&amp; [ "$(sed -n '1,3p' tox.ini | sha256sum)" = "$(git show "$B:tox.ini" | sed -n '1,3p' | sha256sum)" ] &amp;&amp; [ "$(git diff -U0 "$B" -- tox.ini | grep '^+' | grep -v '^+++ b/' | grep -vc '^+#')" = 0 ] &amp;&amp; ! grep -vxF -f &lt;(git show "$B:tox.ini" | sed -n '4,10p') &lt;(git diff -U0 "$B" -- tox.ini | grep '^-' | grep -v '^--- a/' | sed 's/^-//') &amp;&amp; X="$(awk '/^isolated_build = True$/{f=1;next} /^requires = /{f=0} f' tox.ini)" &amp;&amp; [ -n "$X" ] &amp;&amp; ! printf '%s\n' "$X" | grep -qv '^#' &amp;&amp; for t in 'tox-uv~=1.35' '&gt;=1.35,&lt;2' '~=1.35' 'bogus' 'tox-uv-bare' 'NixOS' 'FHS' 'CI' 'bundled'; do printf '%s\n' "$X" | grep -qF -- "$t" || { echo "tox.ini comment lacks: $t"; exit 1; }; done &amp;&amp; printf '%s\n' "$X" | grep -qE 'comma|","' &amp;&amp; ! printf '%s\n' "$X" | grep -qE 'D-[0-9]|SUMMARY\.md|\.planning/' &amp;&amp; ! grep -qF 'tox-uv-bare pinned via' tox.ini &amp;&amp; ! grep -qF 'tox-uv-bare&gt;=1.35' tox.ini &amp;&amp; ! printf '%s\n' "$X" | grep -E '[0-9][0-9.]*-[A-Z0-9-]+\.md' | grep -v 'Phase [0-9]' | grep -q . &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e tox.ini -e tests/test_toolchain_config_gate.py -e tests/test_pdf_render_gate.py -e "$F" -e "$D/68-02-SUMMARY.md" &amp;&amp; for s in '## Head check and provisioning' '## Baseline' '## SpecifierSet equivalence' '## tox.ini rewrite'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing evidence key`, `tox.ini comment lacks: <token>` or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or no shim
- the baseline two-file result containing `failed` or `error`
- `SPECIFIER_EQUIV` not `True`
- `tox config` not printing a `tox-uv~=1.35` line, or printing `tox-uv-bare`
- the `requires` line or anything below it, or lines 1-3, differing from base
- an added non-comment line, or a removed line outside old 4-10
- a non-comment or blank line inside the block
- a required token missing from the block
- a decision ID, SUMMARY citation or `.planning/` path in the block
- either stale opening or stale example requirement still present
- an evidence citation without its milestone and phase
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run tox config -e py312 --core -k requires` prints a line `  tox-uv~=1.35` and no `tox-uv-bare`.
    - The block between `isolated_build = True` and `requires =` is all `#` lines. It contains `tox-uv~=1.35`, `>=1.35,<2`, `~=1.35`, `bogus`, a comma mention, `tox-uv-bare`, `NixOS`, `FHS`, `CI` and `bundled`, and no `D-` followed by a digit, no `SUMMARY.md` and no `.planning/`.
    - `sed -n '/^requires = /,$p' tox.ini` and `sed -n '1,3p' tox.ini` hash identically to `BASE_68_02`.
    - `SPECIFIER_EQUIV = True` sits above its quoted one-liner output.
  </acceptance_criteria>
  <done>tox.ini's comment describes the pin that is actually there. The parser constraint is intact, the history is kept short, and tox provably reads the same `requires` value.</done>
</task>

<task type="auto">
  <name>Task 2: D-13: rewrite the self-referential docstring and assertion-message tail in tests/test_toolchain_config_gate.py to the post-Phase-68 state, text only</name>
  <precondition>Task 1 is committed, and `68-TOX-EVIDENCE.md` holds `BASE_68_02` and `TWO_FILE_RESULT_BEFORE` with no HALT heading.</precondition>
  <reversibility rating="reversible">String literals only, in a git-tracked test file.</reversibility>
  <files>tests/test_toolchain_config_gate.py, .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-TOX-EVIDENCE.md</files>
  <read_first>
    - tests/test_toolchain_config_gate.py (lines 155-170 for the `:164` pointer, and lines 268-370 for the target function)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md (D-13)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md (§ Current Text Inventory → tests/test_toolchain_config_gate.py; § D-15 Grep Classification rows 14 and 21; § Wave Plan "Coordination note")
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-PATTERNS.md (§ tests/test_toolchain_config_gate.py)
  </read_first>
  <action>
1. Re-run the head check and `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

2. **Docstring, base lines 302-304 (D-13).** Replace the paragraph that describes CLAUDE.md's Conventions sentence in the present tense and points forward to this phase. The replacement is past-tense and says:
   - CLAUDE.md's "Conventions & gotchas" sentence named `tox-uv-bare` as deliberate when this revert landed.
   - Phase 68 (DOC-19/DOC-20) rewrote it to the `tox-uv~=1.35` pin, keeping `tox-uv-bare` only as history.
   - Phase 65's D-06 had left that rewrite to Phase 68. Name the decision as Phase 65's, never bare.

   The literal `Conventions & gotchas` and the words `Phase 68` must appear. Do not quote or name any heading CLAUDE.md gained in this phase, and do not use Markdown heading marks.

3. **Assertion message tail, base lines 366-368 (D-13).** Keep the adjacent-literal message shape. Keep the `pyproject.toml. See 65-REVERT-EVIDENCE.md for the lock regeneration proof (TOX-01, D-04)` wording, where D-04 is Phase 65's. Replace the trailing clause, the one that says CLAUDE.md keeps naming `tox-uv-bare` as deliberate pending this phase, with a past-tense clause: CLAUDE.md 'Conventions & gotchas' was rewritten in Phase 68 (DOC-19/DOC-20) to describe the `tox-uv` pin.

   Do not touch the `assert` expression on base line 359, message lines 360-365, or any other string or line in the file. Base lines 1-301 must stay byte-identical, including the `:164` pointer.

4. Keep every edited line at or under 88 characters and let `uv run black --check` confirm the layout; do not run black in write mode. Then run `uv run ruff check` on the file.

5. **`## test_toolchain_config_gate.py strings`.** Quote each of these:
   - `git diff -U0 "$BASE_68_02" -- tests/test_toolchain_config_gate.py`
   - the masked AST hash for base and for now, computed by the `sk` helper in the verify command. They must be equal.
   - the whitespace-normalised string constants of `test_dev_extra_pins_tox_uv_not_tox_uv_bare`, from the `uv run python -c` extractor in the verify command
   - the two-file pytest result, which must equal `TWO_FILE_RESULT_BEFORE`
   - `uv run black --check tests/test_toolchain_config_gate.py` and `uv run ruff check tests/test_toolchain_config_gate.py`

6. Commit the test file and the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-TOX-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_68_02)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; sk() { uv run python -c 'import ast,sys,hashlib;t=ast.parse(sys.stdin.read());[setattr(n.body[0].value,"value","&lt;D&gt;") for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef,ast.ClassDef,ast.Module)) and n.body and isinstance(n.body[0],ast.Expr) and isinstance(n.body[0].value,ast.Constant) and isinstance(n.body[0].value.value,str)];[setattr(c,"value","&lt;M&gt;") for a in ast.walk(t) if isinstance(a,ast.Assert) and a.msg is not None for c in ast.walk(a.msg) if isinstance(c,ast.Constant) and isinstance(c.value,str)];print(hashlib.sha256(ast.dump(t).encode()).hexdigest())'; } &amp;&amp; G=tests/test_toolchain_config_gate.py &amp;&amp; [ "$(sk &lt; "$G")" = "$(git show "$B:$G" | sk)" ] &amp;&amp; T="$(uv run python -c 'import ast,sys;t=ast.parse(open(sys.argv[1]).read());f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name==sys.argv[2]][0];print(" ".join(" ".join(c.value for c in ast.walk(f) if isinstance(c,ast.Constant) and isinstance(c.value,str)).split()))' "$G" test_dev_extra_pins_tox_uv_not_tox_uv_bare)" &amp;&amp; [ -n "$T" ] &amp;&amp; case "$T" in *'still names'*|*'until Phase 68'*|*'goes stale'*|*'###'*|*'development shell'*) false ;; esac &amp;&amp; case "$T" in *'Phase 68'*) true ;; *) false ;; esac &amp;&amp; case "$T" in *'Conventions &amp; gotchas'*) true ;; *) false ;; esac &amp;&amp; ! grep -vxF -f &lt;(git show "$B:$G" | sed -n '302,304p;366,368p') &lt;(git diff -U0 "$B" -- "$G" | grep '^-' | grep -v '^--- a/' | sed 's/^-//') &amp;&amp; [ "$(sed -n '1,301p' "$G" | sha256sum)" = "$(git show "$B:$G" | sed -n '1,301p' | sha256sum)" ] &amp;&amp; [ "$(uv run pytest tests/test_toolchain_config_gate.py tests/test_pdf_render_gate.py -q -p no:cacheprovider 2&gt;&amp;1 | tail -n 1 | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" = "$(k TWO_FILE_RESULT_BEFORE)" ] &amp;&amp; uv run black --check "$G" &amp;&amp; uv run ruff check "$G" &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e tox.ini -e tests/test_toolchain_config_gate.py -e tests/test_pdf_render_gate.py -e "$F" -e "$D/68-02-SUMMARY.md" &amp;&amp; grep -qxF '## test_toolchain_config_gate.py strings' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- the masked AST hash differing from base (a compared name, an expression or a statement changed)
- the function's normalised strings still holding the present-tense or forward-pointing CLAUDE.md claim, quoting a heading, or lacking `Phase 68` or `Conventions & gotchas`
- a removed line outside base 302-304 and 366-368, or base lines 1-301 not byte-identical
- the two-file pytest result differing from `TWO_FILE_RESULT_BEFORE`
- black or ruff reporting the file
- a file outside this plan's set changed
- the section missing, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - The masked AST hash of `tests/test_toolchain_config_gate.py` equals the hash of `git show BASE_68_02:tests/test_toolchain_config_gate.py`.
    - The normalised strings of `test_dev_extra_pins_tox_uv_not_tox_uv_bare` contain `Phase 68` and `Conventions & gotchas`, and contain none of `still names`, `until Phase 68`, `goes stale`, `###` or `development shell`.
    - `sed -n '1,301p'` of the file hashes identically to base, so line 164's `CLAUDE.md 'Conventions & gotchas'` pointer is intact.
    - The two-file pytest result equals `TWO_FILE_RESULT_BEFORE`, and black and ruff pass on the file.
  </acceptance_criteria>
  <done>The toolchain gate's prose describes CLAUDE.md as it stands after this phase. Its assertion logic is provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: D-14: add the revert/FHS sentence to _run_sphinx_build_typst's docstring in tests/test_pdf_render_gate.py, then close the plan's same-worktree gates</name>
  <precondition>Task 2 is committed, with no HALT heading in `68-TOX-EVIDENCE.md`.</precondition>
  <reversibility rating="reversible">One docstring sentence in a git-tracked test file.</reversibility>
  <files>tests/test_pdf_render_gate.py, .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-TOX-EVIDENCE.md</files>
  <read_first>
    - tests/test_pdf_render_gate.py (lines 150-185: `_run_sphinx_build_typst` and its docstring)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md (D-14)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md (§ Current Text Inventory → tests/test_pdf_render_gate.py)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-PATTERNS.md (§ tests/test_pdf_render_gate.py; the "historical fact kept, current reasoning added" idiom)
  </read_first>
  <action>
1. Re-run the head check and `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

2. **D-14.** In the `_run_sphinx_build_typst` docstring, keep the sentence beginning "That cause was removed by QUA-04 (2026-08-10;" verbatim, whitespace aside. Directly after it, insert one sentence: the `tox-uv` revert (v0.9.3 Phase 65) put `.venv/bin/uv` back, and on NixOS it now runs through the Phase 64 FHS shims rather than bare, so that hazard does not recur. The words `revert`, `.venv/bin/uv` and `FHS` must appear in it.

   The closing sentence, beginning "`sys.executable -m sphinx` is kept regardless", stays verbatim after it, whitespace aside. So do the `extra_args` paragraph and everything outside base lines 166-171. Re-wrap only as needed to stay at or under 88 characters, keeping the docstring's existing four-space indentation.

3. **`## test_pdf_render_gate.py sentence`.** Quote each of these:
   - `git diff -U0 "$BASE_68_02" -- tests/test_pdf_render_gate.py`
   - the masked AST hash for base and for now, which must be equal
   - the docstring order check from the verify command, which must exit 0
   - `uv run black --check` and `uv run ruff check` on both edited test files

4. **`## Gates after the edits`.** Quote each of these, then record the keys:
   - the two-file pytest result, recorded as `TWO_FILE_RESULT_AFTER`, which must equal `TWO_FILE_RESULT_BEFORE`
   - the suite collect count, recorded as `COLLECT_AFTER_68_02`, which must equal `COLLECT_BEFORE_68_02`
   - `git diff --name-only "$BASE_68_02"`, which must list only `tox.ini`, the two test files and the evidence file
   - `uv run tox config -e py312 --core -k requires` once more

5. Commit the test file and the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-TOX-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_68_02)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; sk() { uv run python -c 'import ast,sys,hashlib;t=ast.parse(sys.stdin.read());[setattr(n.body[0].value,"value","&lt;D&gt;") for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef,ast.ClassDef,ast.Module)) and n.body and isinstance(n.body[0],ast.Expr) and isinstance(n.body[0].value,ast.Constant) and isinstance(n.body[0].value.value,str)];[setattr(c,"value","&lt;M&gt;") for a in ast.walk(t) if isinstance(a,ast.Assert) and a.msg is not None for c in ast.walk(a.msg) if isinstance(c,ast.Constant) and isinstance(c.value,str)];print(hashlib.sha256(ast.dump(t).encode()).hexdigest())'; } &amp;&amp; P=tests/test_pdf_render_gate.py &amp;&amp; G=tests/test_toolchain_config_gate.py &amp;&amp; [ "$(sk &lt; "$P")" = "$(git show "$B:$P" | sk)" ] &amp;&amp; [ "$(sk &lt; "$G")" = "$(git show "$B:$G" | sk)" ] &amp;&amp; uv run python -c 'import ast,sys;t=ast.parse(open(sys.argv[1]).read());f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=="_run_sphinx_build_typst"][0];d=" ".join(ast.get_docstring(f).split());h="That cause was removed by QUA-04 (2026-08-10; `tox-uv` -&gt; `tox-uv-bare` drops the bundled generic-linux `uv` wheel binary).";c="`sys.executable -m sphinx` is kept regardless, because it depends on no PATH resolution at all -- a better reason than the hazard ever was, and one that holds no matter what is installed in `.venv/bin`.";a=d.find(h);z=d.find(c);m=d[a+len(h):z] if a in range(0,z) else "";sys.exit(0 if ("FHS" in m and ".venv/bin/uv" in m and "revert" in m) else 1)' "$P" &amp;&amp; ! grep -vxF -f &lt;(git show "$B:$P" | sed -n '166,171p') &lt;(git diff -U0 "$B" -- "$P" | grep '^-' | grep -v '^--- a/' | sed 's/^-//') &amp;&amp; [ "$(sed -n '1,165p' "$P" | sha256sum)" = "$(git show "$B:$P" | sed -n '1,165p' | sha256sum)" ] &amp;&amp; [ "$(uv run pytest tests/test_toolchain_config_gate.py tests/test_pdf_render_gate.py -q -p no:cacheprovider 2&gt;&amp;1 | tail -n 1 | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" = "$(k TWO_FILE_RESULT_BEFORE)" ] &amp;&amp; [ "$(k TWO_FILE_RESULT_AFTER)" = "$(k TWO_FILE_RESULT_BEFORE)" ] &amp;&amp; [ "$(uv run pytest --collect-only -q -p no:cacheprovider 2&gt;/dev/null | sed -n 's/^\([0-9][0-9]*\) tests\{0,1\} collected.*/\1/p' | tail -n 1)" = "$(k COLLECT_BEFORE_68_02)" ] &amp;&amp; [ "$(k COLLECT_AFTER_68_02)" = "$(k COLLECT_BEFORE_68_02)" ] &amp;&amp; uv run black --check "$G" "$P" &amp;&amp; uv run ruff check "$G" "$P" &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e tox.ini -e "$G" -e "$P" -e "$F" -e "$D/68-02-SUMMARY.md" &amp;&amp; for s in '## test_pdf_render_gate.py sentence' '## Gates after the edits'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- either test file's masked AST hash differing from base
- the docstring losing the QUA-04 sentence or the closing sentence, reordering them, or lacking `FHS`, `.venv/bin/uv` and `revert` between them
- a removed line outside base 166-171, or base lines 1-165 not byte-identical
- the two-file result or the collect count differing from before
- black or ruff reporting either file
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - Whitespace-normalised, the `_run_sphinx_build_typst` docstring contains the QUA-04 sentence, then a sentence with `revert`, `.venv/bin/uv` and `FHS`, then the unchanged `is kept regardless` sentence, in that order.
    - Both edited test files' masked AST hashes equal base.
    - `TWO_FILE_RESULT_AFTER` equals `TWO_FILE_RESULT_BEFORE`, and `COLLECT_AFTER_68_02` equals `COLLECT_BEFORE_68_02`.
    - `uv run black --check` and `uv run ruff check` pass on both edited test files.
  </acceptance_criteria>
  <done>Every stale tox-uv-bare rationale outside CLAUDE.md is rewritten, and the tests execute exactly what they executed before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tox.ini → tox startup | tox parses `requires` before anything else runs; a malformed value stops every environment |
| test files → CI lint and test lanes | CI runs these files on three OSes; an accidental logic change would silently weaken a gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-04 | Tampering | `tox.ini` `requires` line | medium | mitigate | Everything from `requires =` to end of file must hash identically to base. Every added line must be a comment. `tox config -e py312 --core -k requires` must read back `tox-uv~=1.35`. |
| T-68-05 | Tampering | assertion logic in the two test files | medium | mitigate | The AST hash with docstrings and assert messages masked must equal base, so any change to a compared name, expression or statement fails. The two-file pass/skip summary and the collect count must equal before. |
| T-68-06 | Repudiation | the self-contained equivalence claim in tox.ini | low | mitigate | It is re-measured in this plan with `SpecifierSet` over an eleven-version sample and transcribed verbatim. |
| T-68-SC | Tampering | `uv sync --extra dev` during provisioning | low | accept | It installs only the pins already in `uv.lock`, and no new package is added. |
</threat_model>

<verification>
- `tox.ini`'s comment states the current pin, the parser constraint in full, why `tox-uv` is safe, and the short history. The `requires` value reads back unchanged.
- Both test files' stale self-references describe the post-Phase-68 state. Their executable content hashes identically to base.
- The pass/skip summary and the collect count are unchanged in this worktree.
</verification>

<success_criteria>
DOC-20's comment half is delivered, and every location RESEARCH classed as "rationale presented as current" outside CLAUDE.md is rewritten. 68-04 re-runs the repository-wide D-15 grep on the merged tree to prove that class is empty.
</success_criteria>

<output>
Create `.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-02-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`: DOC-20 closes in 68-04.
- The first section states `BASE_68_02`, `SPECIFIER_EQUIV`, `TWO_FILE_RESULT_BEFORE` / `TWO_FILE_RESULT_AFTER`, `COLLECT_BEFORE_68_02` / `COLLECT_AFTER_68_02`, and `PYVENV_HOME_68_02` / `PYVENV_VERSION_68_02`.
- If a HALT heading was written, that section says so first.
</output>
