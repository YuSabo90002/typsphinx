---
phase: 68-documentation-follow-through-claude-md-tox-ini-flake-nix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - flake.nix
  - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-FLAKE-EVIDENCE.md
autonomous: true
requirements: [DOC-21]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Derivation identity (CONTEXT § Specific Ideas): `nix eval --raw .#devShells.<sys>.default.drvPath` for x86_64-linux, aarch64-linux, x86_64-darwin and aarch64-darwin is byte-identical before and after the edit in this worktree. With comment and blank lines removed, flake.nix hashes identically to base. Every added or removed line is a comment or blank line. `nix flake check --all-systems --no-build` exits 0 after the edit.'
    - 'D-09: flake.nix contains no decision ID (no `D-` followed by a digit), no phrasing true only relative to a phase landing, and no phase-directory path. Every evidence file it names sits on a line carrying "v0.9.3 Phase NN". The header block ends with a list of measurement sources.'
    - 'D-10 (a): a header comment block directly above `outputs =` explains why the devShell stays `mkShell` and names both falsified alternatives. The first is `buildFHSEnv` `.env` as the devShell, which leaves `/lib64/ld-linux-x86-64.so.2` resolving to stub-ld under both `nix develop` and direnv. The second is a `shellHook` that execs into the wrapper, which `nix develop` never runs and which under direnv replaces only nix-direnv capture subshell.'
    - 'D-10 (b): the header says which commands are shimmed and why. They are the seven bare commands CLAUDE.md documents, `uv` plus `venvShimNames`, and the sandbox is inherited across fork/exec, so only top-level entrypoints need shims.'
    - 'D-11 and D-10 (c): the header darwin note says darwin is unverified by construction. `buildFHSEnv` is Linux-only, no maintainer machine can exercise the darwin branch, and no CI lane evaluates flake.nix at all, so a darwin contributor should report breakage directly as a GitHub issue. It adds that the only check ever performed is `nix eval` of all four systems on a Linux evaluator, that the darwin shell has never been built or entered, and that on darwin the shell carries nixpkgs `uv` with no shims.'
    - 'D-10 per-element notes: the `fhsRun` note explains `zlib` (Pillow `_imaging` bare `NEEDED libz.so.1`; uv-managed CPython links zlib statically) and cites v0.9.3 Phase 64 `64-LIBZ-FIX-EVIDENCE.md`. The `venvWalk` note keeps the bounded walk to the first `.git` ancestor, `changedir`, and that a nested worktree never resolves the main checkout `.venv`. The strict-shim note says no fallback and exit 127.'
    - 'D-12: the `uvShim` note describes both legs by role, without version numbers. Leg 1 is the tree own `.venv/bin/uv`, the `uv.lock` pin. Leg 2 is nixpkgs `uv` at a store path fixed at evaluation time, the bootstrap for a fresh clone or worktree before its first `uv sync`. It says this is the only fallback in the file, and the stale wording tying `.venv/bin/uv` to a future revert is gone.'
    - 'The pytest collected count in this worktree is identical before and after the edit.'
  artifacts:
    - path: flake.nix
      provides: 'header notes above outputs (D-10 a/b/c, D-11, D-09 sources) and per-element notes (fhsRun, venvWalk, strict shims, roster, uvShim per D-12)'
      contains: 'only fallback'
    - path: .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-FLAKE-EVIDENCE.md
      provides: 'head check, baseline drvPaths x4 and flake check, uvShim note rewrite, header and per-element notes, gates after the edit'
      contains: 'DRV_BEFORE_x86_64_linux = '
  key_links:
    - from: 'flake.nix comment edits'
      to: 'the devShell derivation on all four systems'
      via: 'nix eval --raw .#devShells.<sys>.default.drvPath before/after, byte-identical'
      pattern: 'drvPath'
    - from: 'flake.nix header (b)'
      to: 'CLAUDE.md § Commands bare commands'
      via: 'the seven shimmed names = uv + venvShimNames (checked on the merged tree in 68-04)'
      pattern: 'venvShimNames'
  prohibitions:
    - statement: 'MUST NOT change any Nix expression or shell-script body in flake.nix while claiming a comment-only edit; the four devShell drvPaths must stay byte-identical (constraint 8: flake.nix has zero CI coverage).'
      requirement_id: DOC-21
      category: safety
      status: resolved
      verification: test
    - statement: 'MUST NOT claim or imply that darwin is verified, built, entered, or covered by CI (D-11, constraint 8).'
      requirement_id: DOC-21
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT describe the uv fallback so that it reads as a pattern the six strict shims could adopt; a lenient shim runs but lies (D-12, constraint 6).'
      requirement_id: DOC-21
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Give `flake.nix` notes that explain its own structure, including what cannot be verified (DOC-21). The edit is Nix `#` comments only.

- **D-10:** a header overview sits directly above `outputs`:
  - (a) the FHS wrapper, why the devShell stays `mkShell`, and the two falsified alternatives
  - (b) which commands are shimmed, and why only top-level entrypoints
  - (c) the darwin guard. **D-11** states that darwin is unverified by construction, plus what was actually checked.
  - The header ends with archive-stable measurement sources (**D-09**).
- The per-element notes beside `fhsRun`, `venvWalk`, the strict shims, the roster and `uvShim` are restated with no decision IDs and no phase-relative phrasing (**D-09**).
- **D-12:** `uvShim`'s two legs are described by role, marked as the file's only fallback, and the stale wording is removed.

A derivation-identity gate proves no script body moved: four `drvPath`s byte-identical before and after, a comment-stripped hash identical to base, and a comment-only diff.

Output: the edited `flake.nix` and `68-FLAKE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-PATTERNS.md
@flake.nix
@.planning/PROJECT.md
</context>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
`## HALT` is a heading the executor writes only on a failure path; the gates require its absence.

## Planning-time census (2026-09-13: re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `flake.nix` | 120 lines. `outputs =` at `:8` (two-space indent, nothing after `=`). `inputs` attrset at `:4-6`. No header comment exists. | `Read` |
| Decision-ID comments | `:63` (strict shims), `:74-75` (roster), `:92` (uvShim) | `grep -n 'D-[0-9]' flake.nix` |
| Stale phrasing | `:93` ties `.venv/bin/uv`'s existence to the Phase 65 revert, which has landed | `grep -n` |
| Existing citation | `:30` names `64-LIBZ-FIX-EVIDENCE.md` bare, with no milestone or phase | `Read` |
| `venvShimNames` | `tox`, `ruff`, `black`, `mypy`, `pytest`, `sphinx-build` (`:76-83`); `uv` is `uvShim` (`:98`) | `Read` |
| Darwin guard | `:113-114`: shims only when `hostPlatform.isLinux`, else `pkgs.uv` | `Read` |
| drvPaths (RESEARCH, main checkout) | x86_64-linux `…jnbia281…-nix-shell.drv`, aarch64-linux `…8qqw29d5…`, x86_64-darwin `…fclfls55…`, aarch64-darwin `…2m6y6psh…`; all four evaluate in 1.6 s; `nix (Nix) 2.34.8` | RESEARCH § NIX-06 |
| Comment-stripped hash | `grep -vE '^[[:space:]]*(#|$)' flake.nix \| sha256sum` is stable across comment edits (dry-run 2026-09-13) | session probe |
| Falsified designs, verbatim source | `.planning/PROJECT.md:66-76` "Binding measurements taken during scoping (2026-09-02)" | `grep -n` |
| Namespace inheritance source | `.planning/research/ARCHITECTURE.md:155-190` (Pattern 2), `:192` (Pattern 3) | `grep -n` |
| CI coverage of flake.nix | `grep -rliE 'nix\|flake\|CLAUDE\.md' .github/workflows/` returns 0 files | RESEARCH § Hazard Census |

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| flake.nix header notes | new comment block (named element: the header above `outputs`) | `flake.nix`, directly above `  outputs =` |
| Restated per-element notes | edited comments | `flake.nix`: above `targetPkgs`, `venvWalk`, `venvShimOnStop`, `venvShimNames`, `uvShim` |
| `68-FLAKE-EVIDENCE.md` | new evidence file. Sections: `## Head check and provisioning`, `## Baseline`, `## uvShim note rewrite`, `## Header and per-element notes`, `## Gates after the edit`. Keys: `BASE_68_03`, `NIX_VERSION`, `PYVENV_HOME_68_03`, `PYVENV_VERSION_68_03`, `COLLECT_BEFORE_68_03`, `COLLECT_AFTER_68_03`, `DRV_BEFORE_x86_64_linux`, `DRV_BEFORE_aarch64_linux`, `DRV_BEFORE_x86_64_darwin`, `DRV_BEFORE_aarch64_darwin`, the four matching `DRV_AFTER_*`, `FLAKE_CHECK_BEFORE_EXIT`, `FLAKE_CHECK_AFTER_EXIT` | phase directory |

## Flagged assumptions (spec-less fallback, surfaced and not auto-resolved)

- **Edge-probe row `DOC-21`:** category `unclassified`, status `unresolved`. No edge was auto-resolved or dismissed. Planner's reading, for the verifier to confirm or reject:
  - DOC-21's mechanically checkable edges are the ones Tasks 1-2 gate:
    - the four-system drvPath identity
    - the comment-stripped hash
    - the comment-only diff
    - the decision-ID, phase-relative and path absence
    - the header and per-element token presence
  - The darwin branch itself is unverifiable by construction (constraint 8). The note's job is to say so, so no edge can close it.
- **Prohibitions:** the three prohibitions carry no wired-check descriptor (spec-less fallback), so each disposes as flagged-unverified downstream.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: head check, four-system drvPath baseline, then the D-12 uvShim note rewrite gated end to end by derivation identity</name>
  <precondition>The session was launched from a shell in which direnv loaded the main checkout's devShell, so `grep -q typsphinx-fhs-run "$(command -v uv)"` succeeds in this worktree, and `nix --version` runs. Otherwise write `## HALT: uv shim or nix not available` and stop: an environment prerequisite, not a code regression.</precondition>
  <reversibility rating="reversible">Comments in a git-tracked flake. The drvPath gate proves the derivation is unchanged, and one revert restores the text.</reversibility>
  <files>flake.nix, .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-FLAKE-EVIDENCE.md</files>
  <read_first>
    - flake.nix (whole file, 120 lines; lines 91-101 are this task's target)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md (D-09, D-12; § Specific Ideas "Derivation-identity gate" and "The uv leg-2 bootstrap")
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md (§ Current Text Inventory → flake.nix; § Derivation-Identity Gate; § Common Pitfalls 2-3)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CONTEXT.md (D-02 two-leg uv, D-03 strict shims, § Specific Ideas: the uv exception must be stated)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CONTEXT.md (D-05: the nixpkgs leg stays as bootstrap)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md (§ Addendum: main-checkout re-sync; leg 1 resolving `.venv/bin/uv`)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md (the drvPath and flake-check transcript shape)
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `command -v uv` and `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - `nix --version`, as `NIX_VERSION`
   - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, the tail of its output
   - `BASE_68_03 = <git merge-base HEAD gsd/v0.9.3-toolchain-and-dependency-update-repair>`, measured here and equal to `git rev-parse HEAD` before any commit

2. **`## Baseline`**, before any edit, each command quoted:
   - For each system in x86_64-linux, aarch64-linux, x86_64-darwin and aarch64-darwin, run `nix eval --raw ".#devShells.<sys>.default.drvPath"` and write `DRV_BEFORE_<system with - replaced by _> = <path>`. Record the dirty-tree stderr warning as seen; it is expected.
   - Run `nix flake check --all-systems --no-build; echo "exit:$?"` and write `FLAKE_CHECK_BEFORE_EXIT`.
   - Record the `sha256sum` of `grep -vE '^[[:space:]]*(#|$)' flake.nix`, the comment-stripped projection.
   - Write `PYVENV_HOME_68_03` / `PYVENV_VERSION_68_03` from `.venv/pyvenv.cfg`.
   - Write `COLLECT_BEFORE_68_03` from `uv run pytest --collect-only -q -p no:cacheprovider`.
   - If any system fails to evaluate, write `## HALT: flake does not evaluate at base` and stop.

3. **D-12 rewrite.** Replace only the full-line comment block directly above `uvShim = pkgs.writeShellScriptBin "uv"` (currently six lines). Every line stays a full-line `#` comment at the same indentation, with no trailing comment on a code line. Nothing may go inside a two-single-quote string. Remove the decision-ID prefix and the sentence that ties `.venv/bin/uv`'s existence to the revert. In your own words, state:
   - (a) `uv` resolves in two legs, described by role. Name no version number of any kind: no digit-dot-digit anywhere in this block.
   - (b) Leg 1 is the same bounded upward walk, to the tree's own `.venv/bin/uv`. That is the `uv` version `uv.lock` pins, present once `uv sync` has provisioned the tree.
   - (c) Leg 2, the on-stop fragment, is nixpkgs' `uv` at a store path fixed at evaluation time, so it is un-shadowable. It is the bootstrap for a fresh clone or worktree that has no `.venv` before its first `uv sync`. Without it such a tree could not provision itself, so do not remove it as cleanup.
   - (d) Both legs enter the sandbox, which is what makes `uv run <tool>` carry FHS into every downstream process.
   - (e) Plainly: this is the only fallback in this file, it is deliberate, and the six strict shims above must not gain one. Use the exact words `only fallback`.

4. **`## uvShim note rewrite`.** Quote each of these:
   - the new block, via the `cb` helper in the verify command applied to the `uvShim` line
   - for each of the four systems, the drvPath after the edit, written as `DRV_AFTER_<sys>`, which must equal its `DRV_BEFORE_` value
   - the comment-stripped `sha256sum`, which must equal base
   - `git diff -U0 "$BASE_68_03" -- flake.nix`, whose every changed line is a comment or blank line

   If any drvPath differs, revert the edit, write `## HALT: comment edit changed the derivation`, and stop.

5. Commit `flake.nix` and the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-FLAKE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_68_03)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; for v in "$(k DRV_BEFORE_x86_64_linux)" "$(k DRV_BEFORE_aarch64_linux)" "$(k DRV_BEFORE_x86_64_darwin)" "$(k DRV_BEFORE_aarch64_darwin)" "$(k COLLECT_BEFORE_68_03)" "$(k PYVENV_HOME_68_03)" "$(k NIX_VERSION)"; do [ -n "$v" ] || { echo "missing evidence key"; exit 1; }; done &amp;&amp; [ "$(k FLAKE_CHECK_BEFORE_EXIT)" = 0 ] &amp;&amp; for s in x86_64-linux aarch64-linux x86_64-darwin aarch64-darwin; do V="$(nix eval --raw ".#devShells.$s.default.drvPath" 2&gt;/dev/null)"; [ -n "$V" ] &amp;&amp; [ "$V" = "$(k "DRV_BEFORE_$(printf '%s' "$s" | tr - _)")" ] || { echo "drvPath changed or unevaluable: $s"; exit 1; }; done &amp;&amp; [ "$(grep -vE '^[[:space:]]*(#|$)' flake.nix | sha256sum)" = "$(git show "$B:flake.nix" | grep -vE '^[[:space:]]*(#|$)' | sha256sum)" ] &amp;&amp; [ "$(git diff -U0 "$B" -- flake.nix | grep -E '^[+-]' | grep -vE '^(\+\+\+ b/|--- a/)' | grep -vcE '^[+-][[:space:]]*(#.*)?$')" = 0 ] &amp;&amp; cb() { awk -v s="$1" 'index($0, s) == 1 {printf "%s", b; exit} /^[[:space:]]*#/{b=b $0 "\n"; next} {b=""}' flake.nix; } &amp;&amp; U="$(cb '          uvShim = pkgs')" &amp;&amp; [ -n "$U" ] &amp;&amp; for t in 'only fallback' 'bootstrap' '.venv/bin/uv' 'uv.lock' 'nixpkgs' 'evaluation' 'uv sync'; do printf '%s\n' "$U" | grep -qF -- "$t" || { echo "uvShim note lacks: $t"; exit 1; }; done &amp;&amp; ! printf '%s\n' "$U" | grep -qE '[0-9]\.[0-9]|D-[0-9]|until Phase|exist until' &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e flake.nix -e "$F" -e "$D/68-03-SUMMARY.md" &amp;&amp; for s in '## Head check and provisioning' '## Baseline' '## uvShim note rewrite'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing evidence key`, `drvPath changed or unevaluable: <sys>`, `uvShim note lacks: <token>` or `missing section: <name>` printed. Each of these makes it fail:
- the baseline flake check exit not 0
- any live drvPath differing from its `DRV_BEFORE_` value, or failing to evaluate
- the comment-stripped hash differing from base
- any changed line that is not a comment or blank line
- the uvShim note missing a required token, or holding a dotted version, a decision ID, or phase-relative wording
- a file outside flake.nix and the evidence changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `nix eval --raw .#devShells.<sys>.default.drvPath` for all four systems equals the recorded `DRV_BEFORE_<sys>`.
    - `grep -vE '^[[:space:]]*(#|$)' flake.nix | sha256sum` equals the same over `git show BASE_68_03:flake.nix`.
    - The comment block directly above `uvShim = pkgs.writeShellScriptBin` contains `only fallback`, `bootstrap`, `.venv/bin/uv`, `uv.lock`, `nixpkgs`, `evaluation` and `uv sync`. It contains no `D-` followed by a digit and no digit-dot-digit sequence.
  </acceptance_criteria>
  <done>The one stale note in flake.nix is corrected, and the four-system derivation-identity gate is proven on a real edit.</done>
</task>

<task type="auto">
  <name>Task 2: Header notes above outputs (D-10 a/b/c, D-11, D-09 sources) and the remaining per-element notes restated under D-09, with the derivation still byte-identical</name>
  <precondition>Task 1 is committed, and `68-FLAKE-EVIDENCE.md` holds the four `DRV_BEFORE_` keys and `## uvShim note rewrite` with no HALT heading.</precondition>
  <reversibility rating="reversible">Comments only, proven by the same derivation-identity gate.</reversibility>
  <files>flake.nix, .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-FLAKE-EVIDENCE.md</files>
  <read_first>
    - flake.nix (as left by Task 1)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md (D-07, D-09, D-10, D-11)
    - .planning/PROJECT.md (lines 66-76: the two falsified devShell designs and the Linux-only guard, verbatim source)
    - .planning/research/ARCHITECTURE.md (lines 107-200: Pattern 1 one FHS derivation plus thin shims, Pattern 2 namespace inheritance across fork/exec, Pattern 3 mkShell stays)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md (shims reach a worktree by PATH inheritance)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-FLAKE-EVIDENCE.md (NIX-06: four-system `nix eval`)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-LIBZ-FIX-EVIDENCE.md (why `zlib`)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX07-RENAME-EVIDENCE.md (strict shims' not-found behaviour)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md (tox-uv's bundled uv resolving `.venv/bin/uv` inside FHS)
    - CLAUDE.md (§ Commands: the bare commands the header refers to)
    - .planning/REQUIREMENTS.md (§ Future Requirements "Arising from this milestone": the zero-CI-coverage risk)
  </read_first>
  <action>
1. Re-run the head check and `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

2. **Header (D-10, D-11, D-09).** Insert one contiguous block of full-line `#` comments at two-space indentation directly above the `  outputs =` line, after the `inputs` attrset and a blank line. No blank line may separate the block from `  outputs =`; a `#`-only line is fine as a paragraph break. Self-contained prose, no decision IDs, no phase-relative phrasing. Cover, in order:
   - **(a) The FHS wrapper.**
     - The devShell is a plain `mkShell`. On Linux it adds one `buildFHSEnv` passthrough (`typsphinx-fhs-run`, a pure exec) and PATH command shims that enter it.
     - Two obvious alternatives were measured and falsified. Using `buildFHSEnv`'s `.env` as the devShell does not put the shell inside FHS under either `nix develop` or direnv, because `/lib64/ld-linux-x86-64.so.2` still resolves to NixOS's stub-ld. A `shellHook` that execs into the wrapper is never run by `nix develop`, and under direnv its exec replaces only nix-direnv's environment-capture subshell.
     - So the shell stays `mkShell`, and `.envrc`'s `use flake` keeps working.
   - **(b) Which commands are shimmed, and why.**
     - Exactly the seven bare commands CLAUDE.md documents: `uv` plus the six in `venvShimNames`.
     - The sandbox (its mount namespace) is inherited across fork/exec by every descendant, so only top-level entrypoints need a shim. Everything tox or uv spawns, including the tree's `.venv/bin/uv`, downloaded interpreters and `.tox/<env>/bin` tools, already runs inside.
     - The shims keep every documented command string unchanged.
   - **(c) The darwin guard (D-11).**
     - `buildFHSEnv` is Linux-only, so the wrapper and shims are added only when the host platform is Linux. On darwin the shell carries nixpkgs' `uv` and no shims.
     - Darwin is unverified by construction: no maintainer machine can exercise that branch, and no CI lane evaluates this file at all.
     - The only check ever performed is `nix eval` of all four systems' devShells on a Linux evaluator; the darwin shell has never been built or entered.
     - A darwin contributor who hits breakage should report it directly as a GitHub issue rather than assume CI would have caught it.
     - Use the words `darwin`, `unverified`, `Linux-only`, `CI`, `nix eval`, `issue` and `built`.
   - **Measurement sources (D-09).** These are the final lines of the block. Each entry is on one line, and each evidence file is named as milestone, phase and file name. Include at least:
     - `.planning/PROJECT.md`, "Binding measurements taken during scoping", for the two falsified designs
     - v0.9.3 Phase 64 `64-NIX05-WORKTREE-EVIDENCE.md`, for PATH inheritance into worktrees
     - v0.9.3 Phase 64 `64-FLAKE-EVIDENCE.md`, for the four-system `nix eval`
     - v0.9.3 Phase 64 `64-LIBZ-FIX-EVIDENCE.md`, for `zlib`
     - v0.9.3 Phase 65 `65-REVERT-EVIDENCE.md`, for tox-uv's bundled uv resolving `.venv/bin/uv` inside FHS
     - optionally v0.9.3 Phase 64 `64-NIX07-RENAME-EVIDENCE.md`, for strict-shim behaviour

     The last line of the block must name a `.md` file. Never cite a phase-directory path, because those directories move when the milestone is archived; cite by milestone, phase and file name, or by `.planning/PROJECT.md`.

3. **Per-element notes (D-10, D-09).** Full-line comments only, at the existing indentation, never inside a two-single-quote string:
   - **`fhsRun`**, in the block above `targetPkgs`. Keep the reason: Pillow's `_imaging` extension carries a bare `NEEDED libz.so.1`, and uv-managed CPython links zlib statically, so nothing in the sandboxed process maps it without this entry. Restate the citation in one line as v0.9.3 Phase 64 `64-LIBZ-FIX-EVIDENCE.md`.
   - **`venvWalk`**, in the block above `venvWalk =`. Keep the content: a bounded upward walk from `$PWD` to `.venv/bin/<tool>`, stopping at the first `.git` ancestor or `/`. It survives tox's `changedir = docs`, and a nested worktree never resolves the main checkout's `.venv`. Edit only if needed for D-09.
   - **Strict shims**, in the block above `venvShimOnStop =`. Drop the decision-ID prefix. State that the six venv shims have no fallback: a missing `.venv/bin/<tool>` is a loud failure that names the tool and the walk's start directory, prints the provisioning line, and exits 127, the shell's command-not-found status. Add that a shim quietly running some other binary would turn "does not run" into "runs but lies". Use the word `fallback`.
   - **Roster**, in the block above `venvShimNames = [`. Drop both decision IDs. State: the seven shimmed names are these six strict shims plus `uv` (`uvShim` below, the one exception).
   - **Darwin guard, optional.** At most one short line above `default = pkgs.mkShell {` pointing at the header's darwin note.
   - Leave the comment above `pkgs.python3` as is.

4. **`## Header and per-element notes`.** Quote each new or restated block, printed with the `cb` helper from the verify command for each anchor line.

5. **`## Gates after the edit`.** Quote each of these:
   - the four drvPaths, updating the `DRV_AFTER_<sys>` keys, each equal to `DRV_BEFORE_`
   - `nix flake check --all-systems --no-build; echo "exit:$?"`, as `FLAKE_CHECK_AFTER_EXIT` (0)
   - the comment-stripped `sha256sum` pair, which must be equal
   - `git diff -U0 "$BASE_68_03" -- flake.nix`, with comment and blank lines only
   - `grep -nE 'D-[0-9]' flake.nix`, which must be empty
   - a grep for phase-relative wording, which must be empty
   - `COLLECT_AFTER_68_03`, which must equal `COLLECT_BEFORE_68_03`

   On any drvPath change: revert, write `## HALT: comment edit changed the derivation`, and stop.

6. Commit `flake.nix` and the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-FLAKE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_68_03)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; for s in x86_64-linux aarch64-linux x86_64-darwin aarch64-darwin; do K="$(printf '%s' "$s" | tr - _)"; V="$(nix eval --raw ".#devShells.$s.default.drvPath" 2&gt;/dev/null)"; [ -n "$V" ] &amp;&amp; [ "$V" = "$(k "DRV_BEFORE_$K")" ] &amp;&amp; [ "$(k "DRV_AFTER_$K")" = "$(k "DRV_BEFORE_$K")" ] || { echo "drvPath changed or unevaluable: $s"; exit 1; }; done &amp;&amp; nix flake check --all-systems --no-build &gt;/dev/null 2&gt;&amp;1 &amp;&amp; [ "$(k FLAKE_CHECK_AFTER_EXIT)" = 0 ] &amp;&amp; [ "$(grep -vE '^[[:space:]]*(#|$)' flake.nix | sha256sum)" = "$(git show "$B:flake.nix" | grep -vE '^[[:space:]]*(#|$)' | sha256sum)" ] &amp;&amp; [ "$(git diff -U0 "$B" -- flake.nix | grep -E '^[+-]' | grep -vE '^(\+\+\+ b/|--- a/)' | grep -vcE '^[+-][[:space:]]*(#.*)?$')" = 0 ] &amp;&amp; ! grep -nE 'D-[0-9]' flake.nix &amp;&amp; ! grep -nE 'until Phase|exist until' flake.nix &amp;&amp; ! grep -nF '.planning/phases' flake.nix &amp;&amp; ! grep -E '[0-9][0-9.]*-[A-Z0-9-]+\.md' flake.nix | grep -v 'v0\.9\.3 Phase [0-9]' | grep -q . &amp;&amp; cb() { awk -v s="$1" 'index($0, s) == 1 {printf "%s", b; exit} /^[[:space:]]*#/{b=b $0 "\n"; next} {b=""}' flake.nix; } &amp;&amp; H="$(cb '  outputs =')" &amp;&amp; [ -n "$H" ] &amp;&amp; for t in 'mkShell' 'buildFHSEnv' '/lib64/ld-linux-x86-64.so.2' 'stub-ld' 'nix develop' 'direnv' 'shellHook' 'subshell' 'fork' 'exec' 'inherit' 'CLAUDE.md' 'darwin' 'unverified' 'Linux-only' 'CI' 'nix eval' 'issue' 'built' 'nixpkgs' '.planning/PROJECT.md' '64-NIX05-WORKTREE-EVIDENCE.md' '64-FLAKE-EVIDENCE.md' '64-LIBZ-FIX-EVIDENCE.md' '65-REVERT-EVIDENCE.md'; do printf '%s\n' "$H" | grep -qF -- "$t" || { echo "header lacks: $t"; exit 1; }; done &amp;&amp; printf '%s\n' "$H" | grep -qE 'venvShimNames|sphinx-build' &amp;&amp; printf '%s' "$H" | tail -n 1 | grep -q '\.md' &amp;&amp; Z="$(cb '            targetPkgs = ')" &amp;&amp; for t in 'zlib' 'libz.so.1' 'v0.9.3 Phase 64' '64-LIBZ-FIX-EVIDENCE.md'; do printf '%s\n' "$Z" | grep -qF -- "$t" || { echo "fhsRun note lacks: $t"; exit 1; }; done &amp;&amp; W="$(cb '          venvWalk =')" &amp;&amp; for t in '.git' 'changedir' 'main checkout'; do printf '%s\n' "$W" | grep -qF -- "$t" || { echo "venvWalk note lacks: $t"; exit 1; }; done &amp;&amp; O="$(cb '          venvShimOnStop =')" &amp;&amp; printf '%s\n' "$O" | grep -qF '127' &amp;&amp; printf '%s\n' "$O" | grep -qF 'fallback' &amp;&amp; R="$(cb '          venvShimNames = [')" &amp;&amp; printf '%s\n' "$R" | grep -qF 'uvShim' &amp;&amp; U="$(cb '          uvShim = pkgs')" &amp;&amp; printf '%s\n' "$U" | grep -qF 'only fallback' &amp;&amp; ! printf '%s\n' "$U" | grep -q '[0-9]\.[0-9]' &amp;&amp; [ "$(uv run pytest --collect-only -q -p no:cacheprovider 2&gt;/dev/null | sed -n 's/^\([0-9][0-9]*\) tests\{0,1\} collected.*/\1/p' | tail -n 1)" = "$(k COLLECT_BEFORE_68_03)" ] &amp;&amp; [ "$(k COLLECT_AFTER_68_03)" = "$(k COLLECT_BEFORE_68_03)" ] &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e flake.nix -e "$F" -e "$D/68-03-SUMMARY.md" &amp;&amp; for s in '## Header and per-element notes' '## Gates after the edit'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `drvPath changed or unevaluable: <sys>`, `header lacks: <token>`, `fhsRun note lacks: <token>`, `venvWalk note lacks: <token>` or `missing section: <name>` printed, or `grep -n` printing a line. Each of these makes it fail:
- any drvPath or `DRV_AFTER_` value differing from `DRV_BEFORE_`
- `nix flake check` failing
- the comment-stripped hash differing from base
- a non-comment line changed
- any decision ID, phase-relative wording or phase-directory path in flake.nix
- an evidence citation without "v0.9.3 Phase N"
- the header missing, separated from `outputs =` by a blank line, lacking a required token, or not ending on a `.md` source line
- a per-element note lacking its token
- a dotted version in the uvShim note
- the collect count differing
- a file outside flake.nix and the evidence changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'D-[0-9]' flake.nix` and `grep -nE 'until Phase|exist until' flake.nix` print nothing, and `grep -nF '.planning/phases' flake.nix` prints nothing.
    - The comment block directly above `  outputs =` contains:
      - `mkShell`, `buildFHSEnv`, `/lib64/ld-linux-x86-64.so.2`, `stub-ld`, `nix develop`, `direnv`, `shellHook` and `subshell` (a)
      - `fork`, `exec`, `inherit`, `CLAUDE.md`, and `venvShimNames` or `sphinx-build` (b)
      - `darwin`, `unverified`, `Linux-only`, `CI`, `nix eval`, `issue`, `built` and `nixpkgs` (c)
      - the five named sources, with its last line naming a `.md` file
    - Every line naming an `NN-*.md` evidence file also contains `v0.9.3 Phase N`.
    - All four drvPaths equal their `DRV_BEFORE_` values. `FLAKE_CHECK_AFTER_EXIT = 0`. The comment-stripped hash equals base. `COLLECT_AFTER_68_03` equals `COLLECT_BEFORE_68_03`.
  </acceptance_criteria>
  <done>flake.nix explains the FHS wrapper, the shim roster and namespace inheritance, and the darwin guard, including that darwin is unverified by construction. The notes are archive-stable, and the derivation is provably unchanged on all four systems.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flake.nix → maintainer's development shell | the file is load-bearing on NixOS, with zero CI coverage (constraint 8); a breaking edit is found only when the shell is next entered |
| flake.nix notes → darwin contributors | the notes are the only place a darwin contributor learns the branch is unverified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-07 | Tampering | a comment edit drifting into a `writeShellScript` body or a Nix expression | medium | mitigate | The four devShell drvPaths must be byte-identical before and after in the same worktree, and `nix flake check --all-systems --no-build` must still pass. The comment-stripped hash must equal base, and every changed diff line must be a comment or blank line. On mismatch the executor reverts and halts. |
| T-68-08 | Spoofing | a note implying darwin is tested or CI-covered | low | mitigate | The header must carry `unverified`, `Linux-only`, `CI`, `nix eval`, `issue` and `built` (D-11). 68-04 re-reads the note on the merged tree. |
| T-68-09 | Tampering | a future reader "fixing" the strict shims into lenient ones | low | mitigate | The uvShim note states `only fallback` and that the six strict shims must not gain one. The strict-shim note keeps exit 127 and the reason (D-12). |
| T-68-SC | Tampering | `uv sync --extra dev` during provisioning | low | accept | It installs only the pins already in `uv.lock`, and no new package is added. `nix eval` fetches nothing beyond the locked `flake.lock` inputs. |
</threat_model>

<verification>
- The header above `outputs` covers D-10 (a)(b)(c) and D-11, and ends with archive-stable sources (D-09).
- The per-element notes carry no decision IDs and no phase-relative phrasing. The uvShim note follows D-12.
- The four drvPaths are byte-identical before and after, the flake check passes, and the diff is comment-only.
</verification>

<success_criteria>
DOC-21 is delivered in `flake.nix`, and the edit is comments only, as proven by derivation identity on all four declared systems. 68-04 re-reads the header against CLAUDE.md's Commands block and re-evaluates the drvPaths on the merged tree.
</success_criteria>

<output>
Create `.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-03-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`: DOC-21 closes in 68-04.
- The first section states `BASE_68_03`, the four `DRV_BEFORE_` / `DRV_AFTER_` pairs, `FLAKE_CHECK_AFTER_EXIT`, and `COLLECT_BEFORE_68_03` / `COLLECT_AFTER_68_03`.
- If a HALT heading was written, that section says so first.
</output>
