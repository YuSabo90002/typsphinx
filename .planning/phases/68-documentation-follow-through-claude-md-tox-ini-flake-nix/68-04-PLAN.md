---
phase: 68-documentation-follow-through-claude-md-tox-ini-flake-nix
plan: 04
type: execute
wave: 2
depends_on: ["68-01", "68-02", "68-03"]
files_modified:
  - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLOSURE-EVIDENCE.md
autonomous: true
requirements: [DOC-19, DOC-20, DOC-21]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Wave-1 gate: 68-CLAUDEMD-EVIDENCE.md, 68-TOX-EVIDENCE.md, 68-FLAKE-EVIDENCE.md and the three wave-1 SUMMARY files exist at HEAD with no HALT heading. PHASE_BASE is the common ancestor of BASE_68_01, BASE_68_02 and BASE_68_03, and an ancestor of HEAD.'
    - 'D-15 (SC#2): `git grep -n tox-uv-bare -- :!.planning :!uv.lock` is re-run on the merged tree and transcribed verbatim. Every hit is classified in a table with exactly one row per hit, as C1 historical and accurate, C2 named by a test as the forbidden value, or C3 rationale presented as current. C3 has zero rows, and SC2_VERDICT = MET.'
    - 'SC#2 cross-checks on the merged tree: the six sentences RESEARCH measured as C3 are gone from CLAUDE.md, tox.ini and tests/test_toolchain_config_gate.py, and `uv run tox config -e py312 --core -k requires` prints `tox-uv~=1.35`.'
    - 'D-01 (SC#1 readings, reported separately): the literal reading is tabulated clause by clause, and the unaffected-and-unassisted clause is recorded as contradicted by NIX-05 by design. The amended reading is MET: the recipe tail is byte-identical to PHASE_BASE, the boundary paragraph states the recipe is mandatory and that the shims arrive by inheritance, not direnv, and the ROADMAP AMENDED 2026-09-12 block is present.'
    - 'D-02 on the merged tree: `git grep -lE "ln -sf|patchelf" -- :!.planning` lists only CLAUDE.md, every such line lies inside the NixOS subsection, and the ROADMAP AMENDED block records the vacuity ("satisfied vacuously").'
    - 'SC#3 on the merged tree: the four devShell drvPaths equal 68-FLAKE-EVIDENCE.md DRV_BEFORE values. flake.nix carries no decision ID. The header states darwin is unverified by construction, that no CI lane covers flake.nix, and that darwin contributors should report breakage directly (D-11).'
    - 'Cross-file consistency: the seven shimmed names (`uv` plus the six in `venvShimNames`) each begin a command line in CLAUDE.md § Commands, CLAUDE.md names the `tox-uv~=1.35` pin the tox.ini comment and the two test files describe, and CROSS_FILE_VERDICT = consistent.'
    - 'Green tree on the merged tree: the full pytest suite reports 0 failed and 0 errors, with interpreter recorded; `uv run black --check .` and `uv run ruff check .` pass; nothing under typsphinx/, .github/, pyproject.toml, uv.lock or CHANGELOG.md changed since PHASE_BASE; and the only non-.planning files changed are the five named edit targets, each actually changed.'
    - 'Requirement closure: DOC19_VERDICT, DOC20_VERDICT and DOC21_VERDICT are each MET, derived from the sections above.'
  artifacts:
    - path: .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLOSURE-EVIDENCE.md
      provides: 'wave-1 inputs, D-15 grep and classification, SC#2, SC#1 literal and amended readings, SC#3, cross-file consistency, green tree, phase scope fence, requirement closure'
      contains: 'SC2_VERDICT = '
  key_links:
    - from: 'the merged tree after wave 1'
      to: 'SC#2 zero C3 rows'
      via: 'git grep -n tox-uv-bare -- :!.planning :!uv.lock, every hit classified'
      pattern: 'C3_ROWS = '
    - from: '68-FLAKE-EVIDENCE.md DRV_BEFORE_*'
      to: 'merged-tree nix eval drvPaths'
      via: 'byte equality on all four systems'
      pattern: 'DRV_BEFORE_'
    - from: 'flake.nix venvShimNames + uv'
      to: 'CLAUDE.md § Commands'
      via: 'each shimmed name begins a documented command line'
      pattern: 'venvShimNames'
  prohibitions:
    - statement: 'MUST NOT record a count, SHA, drvPath, class or verdict copied from RESEARCH, CONTEXT, a wave-1 evidence file or this plan as a measurement; every figure comes from a command quoted in the same section.'
      requirement_id: DOC-20
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT fold SC#1 literal reading into its amended reading; both are reported separately with their own verdicts (D-01, Phase 65 D-01 precedent).'
      requirement_id: DOC-19
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT edit CLAUDE.md, tox.ini, flake.nix, either test file, or any file outside this plan evidence; a defect found here is recorded under a HALT heading for the owner, never fixed in the closure plan.'
      requirement_id: DOC-21
      category: safety
      status: resolved
      verification: test
---

<objective>
Measure the merged wave-1 tree and close DOC-19, DOC-20 and DOC-21 on evidence. This plan runs in a later wave than every edit it measures, so it sees all three.

- **D-15:** re-run the repository-wide `tox-uv-bare` grep on the merged tree, transcribe every hit, and classify each into exactly one of three classes. SC#2 is MET only when "rationale presented as current" has zero rows.
- **D-01:** report SC#1's literal reading and its amended reading separately. Confirm the recipe is byte-identical to the pre-phase tree.
- **D-02:** confirm the vacuity on the merged tree.
- **SC#3:** re-read against `flake.nix`, with the drvPaths re-evaluated on the merged tree.
- Check that the three files and the two test files agree with each other.
- Record the full suite, `black` and `ruff` on the merged tree, and the phase-wide scope fence.

Output: `68-CLOSURE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLAUDEMD-EVIDENCE.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-TOX-EVIDENCE.md
@.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-FLAKE-EVIDENCE.md
@CLAUDE.md
@tox.ini
@flake.nix
</context>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
`## HALT` is a heading the executor writes only on a failure path; the gates require its absence.

## Planning-time census (2026-09-13: pre-edit tree; this plan measures the merged tree)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `git grep -n tox-uv-bare -- ':!.planning' ':!uv.lock'` | 27 hits; 6 in the "rationale presented as current" class, at `CLAUDE.md:11`, `CLAUDE.md:77`, `tox.ini:4`, `tox.ini:7`, `tests/test_toolchain_config_gate.py:302-304` and `:366-368` | RESEARCH § D-15 Grep Classification (a prediction, never a substitute for this plan's measurement) |
| Hits after wave 1 | expected to change: D-08 and D-16 keep `tox-uv-bare` as history in CLAUDE.md and tox.ini, and D-13/D-14 may add or remove mentions in test prose. Count them; do not predict them. | CONTEXT D-08, D-13, D-14, D-16 |
| Stale-literal locations at planning | the C3 sentences sit only at the six locations above; the literal `"simplify"` also occurs in `tests/fixtures/mixed_prewrite_failures_a_gate/conf.py:11`, and `still names` in four unrelated test files, so those two negatives are file-scoped | `git grep -nF` |
| ROADMAP SC#1 | `AMENDED 2026-09-12 (Phase 68 discuss, owner-approved)` block present at `:646-658`, including "satisfied vacuously" | `Read` |
| CLAUDE.md § Commands | bare `uv`, `pytest`, `black`, `ruff`, `mypy`, `tox` and `sphinx-build` each begin a command line (`:14-40`) | `Read` |
| Full suite | 1548 collected in the main checkout (nixpkgs python). A fresh worktree may use a different interpreter (D-06), so record `pyvenv.cfg` alongside. | RESEARCH § Green-Tree Gate Method |

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `68-CLOSURE-EVIDENCE.md` | new evidence file. Sections: `## Head check and provisioning`, `## Wave-1 inputs`, `## D-15 grep (merged tree)`, `## D-15 classification`, `## SC#2 reading`, `## SC#1 literal reading`, `## SC#1 amended reading`, `## SC#3 reading`, `## Cross-file consistency`, `## Green tree (merged)`, `## Phase scope fence`, `## Requirement closure`. Keys: `BASE_68_04`, `PHASE_BASE`, `TOXUVBARE_HITS`, `C1_ROWS`, `C2_ROWS`, `C3_ROWS`, `SC2_VERDICT`, `SC1_LITERAL_VERDICT`, `SC1_AMENDED_VERDICT`, `SC3_VERDICT`, `CROSS_FILE_VERDICT`, `PYVENV_HOME_68_04`, `PYVENV_VERSION_68_04`, `FULL_SUMMARY`, `FULL_FAILED`, `DOC19_VERDICT`, `DOC20_VERDICT`, `DOC21_VERDICT` | phase directory |

## Flagged assumptions (spec-less fallback, surfaced and not auto-resolved)

- **Edge-probe rows `DOC-19`, `DOC-20` and `DOC-21`:** each has category `unclassified` and status `unresolved`. No edge was auto-resolved or dismissed. This plan does not resolve them either. It records measurements the verifier can use to judge each requirement:
  - the D-15 table
  - the two SC#1 readings
  - the SC#3 reading
  - cross-file consistency
- Prose quality against the Phase 64/65 evidence remains a verify-time judgment.
- **Prohibitions:** the three prohibitions carry no wired-check descriptor (spec-less fallback), so each disposes as flagged-unverified downstream.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: wave-1 gate, then the D-15 repository-wide tox-uv-bare grep on the merged tree with every hit classified, closing SC#2</name>
  <precondition>68-01, 68-02 and 68-03 are merged into the milestone branch: their evidence and SUMMARY files exist at this worktree's HEAD with no HALT heading. `grep -q typsphinx-fhs-run "$(command -v uv)"` succeeds. Otherwise write `## HALT: wave 1 not merged` or `## HALT: uv shim not on PATH` and stop.</precondition>
  <reversibility rating="reversible">Read-only toward every tracked file except this plan's own evidence.</reversibility>
  <files>.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLOSURE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md (D-15; D-08, D-13, D-14, D-16 for what legitimately survives as history)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md (§ D-15 Grep Classification: the class definitions and the planning-time table, used as a guide only)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLAUDEMD-EVIDENCE.md, 68-TOX-EVIDENCE.md, 68-FLAKE-EVIDENCE.md (their `BASE_68_0N` keys and gates)
    - CLAUDE.md, tox.ini, tests/test_toolchain_config_gate.py, tests/test_pdf_render_gate.py (each hit's context, read to classify it)
    - .planning/ROADMAP.md (§ Phase 68 SC#2)
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `command -v uv` and `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, the tail of its output
   - `BASE_68_04 = <git merge-base HEAD gsd/v0.9.3-toolchain-and-dependency-update-repair>`
   - `git log --oneline -15`

2. **`## Wave-1 inputs`.**
   - For each of `68-CLAUDEMD-EVIDENCE.md`, `68-TOX-EVIDENCE.md`, `68-FLAKE-EVIDENCE.md`, `68-01-SUMMARY.md`, `68-02-SUMMARY.md` and `68-03-SUMMARY.md`, record `git cat-file -e HEAD:<phase dir>/<file>; echo "exit:$?"` (0 required) and `grep -c '^## HALT'` (0 required on the evidence files).
   - Read `BASE_68_01`, `BASE_68_02` and `BASE_68_03` from the three evidence files with the `KEY = value` reader.
   - Write `PHASE_BASE = <git merge-base --octopus BASE_68_01 BASE_68_02 BASE_68_03>` and record `git merge-base --is-ancestor "$PHASE_BASE" HEAD; echo "exit:$?"` (0).

3. **`## D-15 grep (merged tree)`** (D-15). Run `git grep -n tox-uv-bare -- ':!.planning' ':!uv.lock'` and transcribe its output verbatim in a fenced block. Write `TOXUVBARE_HITS = <line count of that output>`.

4. **`## D-15 classification`.** A Markdown table with columns `#`, `Location`, `Class`, `Why`, and exactly one row per hit from step 3. Rules:
   - `Location` is the hit's `path:line` in backticks, exactly as `git grep -n` printed it.
   - `Class` is exactly one of `C1`, `C2` or `C3`:
     - `C1`: historical and accurate. It describes a past state as past.
     - `C2`: named by a test as the forbidden value, or as a compared distribution name.
     - `C3`: rationale presented as current. It states or implies that `tox-uv-bare` is the pin now, is deliberate now, or should be returned to.
   - Classify each hit by reading its surrounding sentence in the file, not by line number or by copying the planning-time table.
   - After the table, write `C1_ROWS = <n>`, `C2_ROWS = <n>` and `C3_ROWS = <n>`. The three must sum to `TOXUVBARE_HITS`.
   - If any row is C3: write `## HALT: rationale still presented as current`, name the rows, and stop. The closure plan fixes nothing.

5. **`## SC#2 reading`.** Record, each with its command:
   - Negative checks: CLAUDE.md no longer carries the old line 11 parenthetical, the old `-bare` rationale sentence or the old do-not-revert instruction. tox.ini no longer carries the old comment's opening or its old bogus-requirement example. tests/test_toolchain_config_gate.py no longer carries the present-tense or forward-pointing CLAUDE.md claims. Use the file-scoped greps from the verify command.
   - `uv run tox config -e py312 --core -k requires`, verbatim
   - `SC2_VERDICT = MET` only when `C3_ROWS = 0` and every check above holds; otherwise `NOT-MET`, with `## HALT: SC#2 not met`

6. Commit the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-CLOSURE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; kf() { sed -n "s/^$2 = //p" "$D/$1" | head -n 1; } &amp;&amp; B="$(k BASE_68_04)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; for E in 68-CLAUDEMD-EVIDENCE.md 68-TOX-EVIDENCE.md 68-FLAKE-EVIDENCE.md 68-01-SUMMARY.md 68-02-SUMMARY.md 68-03-SUMMARY.md; do git cat-file -e "HEAD:$D/$E" || { echo "wave-1 artifact missing at HEAD: $E"; exit 1; }; done &amp;&amp; for E in 68-CLAUDEMD-EVIDENCE.md 68-TOX-EVIDENCE.md 68-FLAKE-EVIDENCE.md; do ! grep -q '^## HALT' "$D/$E" || { echo "wave-1 evidence halted: $E"; exit 1; }; done &amp;&amp; PB="$(k PHASE_BASE)" &amp;&amp; [ -n "$PB" ] &amp;&amp; [ "$PB" = "$(git merge-base --octopus "$(kf 68-CLAUDEMD-EVIDENCE.md BASE_68_01)" "$(kf 68-TOX-EVIDENCE.md BASE_68_02)" "$(kf 68-FLAKE-EVIDENCE.md BASE_68_03)")" ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; N="$(git grep -n tox-uv-bare -- ':!.planning' ':!uv.lock' | wc -l)" &amp;&amp; [ "$(k TOXUVBARE_HITS)" = "$N" ] &amp;&amp; T="$(awk '/^## D-15 classification$/{f=1;next} /^## /{f=0} f' "$F")" &amp;&amp; [ "$(printf '%s\n' "$T" | awk '/^\| [0-9]+ \|/{n++} END{print n+0}')" = "$N" ] &amp;&amp; git grep -n tox-uv-bare -- ':!.planning' ':!uv.lock' | cut -d: -f1,2 | while IFS= read -r L; do printf '%s\n' "$T" | grep -qF "\`$L\`" || { echo "unclassified hit: $L"; exit 1; }; done &amp;&amp; ! printf '%s\n' "$T" | grep -qE '^\| [0-9]+ \|.*\| C3 \|' &amp;&amp; [ "$(k C3_ROWS)" = 0 ] &amp;&amp; [ "$(( $(k C1_ROWS) + $(k C2_ROWS) ))" = "$N" ] &amp;&amp; ! grep -qF '(with `tox-uv-bare`)' CLAUDE.md &amp;&amp; ! grep -qF 'package is also deliberate' CLAUDE.md &amp;&amp; ! grep -qF '"simplify"' CLAUDE.md &amp;&amp; ! grep -qF 'tox-uv-bare pinned via' tox.ini &amp;&amp; ! grep -qF 'tox-uv-bare&gt;=1.35' tox.ini &amp;&amp; ! grep -qE 'still names|until Phase 68|goes stale as of this revert' tests/test_toolchain_config_gate.py &amp;&amp; RQ="$(uv run tox config -e py312 --core -k requires 2&gt;/dev/null)" &amp;&amp; printf '%s\n' "$RQ" | grep -qx '[[:space:]]*tox-uv~=1.35' &amp;&amp; [ "$(k SC2_VERDICT)" = MET ] &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e "$F" -e "$D/68-04-SUMMARY.md" &amp;&amp; for s in '## Head check and provisioning' '## Wave-1 inputs' '## D-15 grep (merged tree)' '## D-15 classification' '## SC#2 reading'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `wave-1 artifact missing at HEAD: <file>`, `wave-1 evidence halted: <file>`, `unclassified hit: <path:line>` or `missing section: <name>` printed. Each of these makes it fail:
- `PHASE_BASE` not the octopus merge-base of the three wave-1 bases, or not an ancestor of HEAD
- `TOXUVBARE_HITS` not equal to the live hit count
- the table row count differing from the hit count, or a live hit missing from the table
- any row classed C3, or `C3_ROWS` not 0, or `C1_ROWS + C2_ROWS` not equal to the hit count
- any of the six stale C3 sentences surviving in its file
- `tox config` not printing `tox-uv~=1.35`
- `SC2_VERDICT` not MET
- a tracked file other than this evidence changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## D-15 grep (merged tree)` holds the verbatim `git grep -n tox-uv-bare -- ':!.planning' ':!uv.lock'` output, and `TOXUVBARE_HITS` equals its line count.
    - `## D-15 classification` has one row per hit, each `Location` in backticks, each `Class` one of C1/C2/C3. `C3_ROWS = 0`, and `C1_ROWS + C2_ROWS = TOXUVBARE_HITS`.
    - `SC2_VERDICT = MET`, with the file-scoped negative checks and the `tox config` read-back quoted.
  </acceptance_criteria>
  <done>SC#2's repository-wide grep is measured on the merged tree, with every hit classified and none presented as current.</done>
</task>

<task type="auto">
  <name>Task 2: SC#1 literal and amended readings (D-01, D-02), SC#3 reading with merged-tree drvPaths (D-11), and cross-file consistency</name>
  <precondition>Task 1 is committed with `SC2_VERDICT = MET`, no HALT heading, and `PHASE_BASE` recorded.</precondition>
  <reversibility rating="reversible">Read-only toward every tracked file except this plan's evidence.</reversibility>
  <files>.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLOSURE-EVIDENCE.md</files>
  <read_first>
    - .planning/ROADMAP.md (§ Phase 68 SC#1 with its AMENDED 2026-09-12 block, SC#3; binding constraints 8 and 11)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CONTEXT.md (D-01, D-02, D-03, D-10, D-11)
    - CLAUDE.md (the `### NixOS development shell` subsection and `### Worktree-isolated execution`)
    - flake.nix (header and per-element notes)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-FLAKE-EVIDENCE.md (`DRV_BEFORE_*`)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-NIX05-WORKTREE-EVIDENCE.md (the measurement the literal clause is read against)
    - tests/test_toolchain_config_gate.py (`test_dev_extra_pins_tox_uv_not_tox_uv_bare` strings), tests/test_pdf_render_gate.py (`_run_sphinx_build_typst` docstring)
  </read_first>
  <action>
1. Re-run the head check and `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

2. **`## SC#1 literal reading`** (D-01: literal and amended are reported separately). Build a table with one row per clause of SC#1's original text. Each row carries the clause, the measurement quoted from a command run here, and a verdict. The clauses:
   - (a) The manual `ln -sf` / `patchelf` guidance is gone, retired and not made conditional. Quote `git grep -nE 'ln -sf|patchelf' -- ':!.planning'` on the merged tree, whose only hits must be CLAUDE.md lines inside the NixOS subsection, and cite 68-CLAUDEMD-EVIDENCE.md's zero-count keys at base. This is D-02's vacuity.
   - (b) The section describes what Phase 64 actually landed. Quote the NixOS subsection.
   - (c) Executors keep using the recipe. Quote the `sha256sum` of `awk '/^When operating inside a worktree, provision/{f=1} f' CLAUDE.md`, next to the same pipeline over `git show "$PHASE_BASE:CLAUDE.md"`.
   - (d) Executors are "unaffected and unassisted by `flake.nix`". The verdict is contradicted by measurement: NIX-05 shows worktree `uv run` works only through the inherited `uv` shim, and CLAUDE.md states that dependence by design.
   - (e) A worktree is a directory direnv has never seen. Quote the boundary paragraph.
   - (f) If NIX-05 found the shims reachable, the section says so and keeps the recipe mandatory. Quote the boundary paragraph.

   Write `SC1_LITERAL_VERDICT` as `PARTIAL` when every clause except (d) is met, which is the expected outcome; as `MET` only if (d) is somehow met; otherwise `NOT-MET`.

3. **`## SC#1 amended reading`.** Record each of these:
   - `grep -n 'AMENDED 2026-09-12 (Phase 68 discuss' .planning/ROADMAP.md` and `grep -n 'satisfied vacuously' .planning/ROADMAP.md`
   - the recipe-tail hash equality from step 2(c)
   - the boundary paragraph, quoted, showing that:
     - the recipe is unchanged and mandatory
     - `flake.nix` does not substitute for it
     - on NixOS the recipe runs through the shims
     - the shims arrive by inheritance, not by direnv
   - the D-03 check `grep -q typsphinx-fhs-run "$(command -v uv)"` present in the subsection, with its NixOS-only scoping

   Write `SC1_AMENDED_VERDICT = MET` only if all of these hold.

4. **`## SC#3 reading`.** Record each of these:
   - for each of the four systems, `nix eval --raw ".#devShells.<sys>.default.drvPath"` on the merged tree, beside 68-FLAKE-EVIDENCE.md's `DRV_BEFORE_` value; they must be equal
   - `grep -nE 'D-[0-9]' flake.nix`, which must be empty
   - the header block, quoted, and mapped onto SC#3's parts:
     - the FHS wrapper, `mkShell`, and both falsified alternatives
     - which commands are shimmed, and why only top-level entrypoints
     - the darwin note: unverified by construction, Linux-only `buildFHSEnv`, no maintainer machine, no CI lane, report directly (D-11)

   Write `SC3_VERDICT = MET` only if every part is present.

5. **`## Cross-file consistency`.** Record each of these:
   - (a) the seven shimmed names, from `awk '/venvShimNames = \[/{f=1;next} f && /\];/{exit} f' flake.nix` plus `uv`, each shown to begin a command line in CLAUDE.md's `## Commands` section
   - (b) CLAUDE.md's `tox.ini` bullet naming `tox-uv~=1.35`, beside tox.ini's `requires` line and comment
   - (c) the normalised strings of `test_dev_extra_pins_tox_uv_not_tox_uv_bare`, read against CLAUDE.md's bullet: the test's past-tense claim about CLAUDE.md is true
   - (d) the `_run_sphinx_build_typst` docstring's `.venv/bin/uv` / FHS sentence, read against `ls -l .venv/bin/uv` in this worktree and the uv shim check
   - (e) CLAUDE.md's `uv` bootstrap sentence, read against flake.nix's uvShim note

   Write `CROSS_FILE_VERDICT = consistent` only if all five agree; otherwise name the disagreement, write `## HALT: cross-file inconsistency`, and stop.

6. Commit the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-CLOSURE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; kf() { sed -n "s/^$2 = //p" "$D/$1" | head -n 1; } &amp;&amp; B="$(k BASE_68_04)" &amp;&amp; PB="$(k PHASE_BASE)" &amp;&amp; [ -n "$B" ] &amp;&amp; [ -n "$PB" ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; [ "$(awk '/^When operating inside a worktree, provision/{f=1} f' CLAUDE.md | sha256sum)" = "$(git show "$PB:CLAUDE.md" | awk '/^When operating inside a worktree, provision/{f=1} f' | sha256sum)" ] &amp;&amp; [ "$(git grep -lE 'ln -sf|patchelf' -- ':!.planning')" = CLAUDE.md ] &amp;&amp; S="$(awk '/^### NixOS development shell$/{f=1} /^### Worktree-isolated execution$/{f=0} f' CLAUDE.md)" &amp;&amp; [ -n "$S" ] &amp;&amp; [ "$(grep -cE 'ln -sf|patchelf' CLAUDE.md)" = "$(printf '%s\n' "$S" | grep -cE 'ln -sf|patchelf')" ] &amp;&amp; printf '%s\n' "$S" | grep -qF 'grep -q typsphinx-fhs-run "$(command -v uv)"' &amp;&amp; [ "$(grep -c 'AMENDED 2026-09-12 (Phase 68 discuss' .planning/ROADMAP.md)" = 1 ] &amp;&amp; grep -qF 'satisfied vacuously' .planning/ROADMAP.md &amp;&amp; case "$(k SC1_LITERAL_VERDICT)" in MET|PARTIAL|NOT-MET) true ;; *) false ;; esac &amp;&amp; [ "$(k SC1_AMENDED_VERDICT)" = MET ] &amp;&amp; for s in x86_64-linux aarch64-linux x86_64-darwin aarch64-darwin; do V="$(nix eval --raw ".#devShells.$s.default.drvPath" 2&gt;/dev/null)"; [ -n "$V" ] &amp;&amp; [ "$V" = "$(kf 68-FLAKE-EVIDENCE.md "DRV_BEFORE_$(printf '%s' "$s" | tr - _)")" ] || { echo "merged-tree drvPath differs: $s"; exit 1; }; done &amp;&amp; ! grep -nE 'D-[0-9]' flake.nix &amp;&amp; [ "$(k SC3_VERDICT)" = MET ] &amp;&amp; C="$(awk '/^## Commands$/{f=1;next} /^## Architecture$/{f=0} f' CLAUDE.md)" &amp;&amp; NM="$(awk '/venvShimNames = \[/{f=1;next} f &amp;&amp; /\];/{exit} f' flake.nix | tr -d ' "')" &amp;&amp; [ "$(printf '%s\n' "$NM" | sort | tr '\n' ' ')" = 'black mypy pytest ruff sphinx-build tox ' ] &amp;&amp; for n in uv $NM; do printf '%s\n' "$C" | grep -qE "^$n( |$)" || { echo "shimmed command not in CLAUDE.md Commands: $n"; exit 1; }; done &amp;&amp; grep -qF -- '- `tox.ini` pins `tox-uv~=1.35`' CLAUDE.md &amp;&amp; [ "$(k CROSS_FILE_VERDICT)" = consistent ] &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e "$F" -e "$D/68-04-SUMMARY.md" &amp;&amp; for s in '## SC#1 literal reading' '## SC#1 amended reading' '## SC#3 reading' '## Cross-file consistency'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `merged-tree drvPath differs: <sys>`, `shimmed command not in CLAUDE.md Commands: <name>` or `missing section: <name>` printed, or `grep -n` printing a decision ID from flake.nix. Each of these makes it fail:
- the recipe tail differing from `PHASE_BASE`
- `ln -sf` / `patchelf` found outside CLAUDE.md, or outside the NixOS subsection
- the shim check absent from the subsection
- the ROADMAP AMENDED block or its vacuity line missing
- `SC1_LITERAL_VERDICT` not one of MET/PARTIAL/NOT-MET, or `SC1_AMENDED_VERDICT` not MET
- any merged-tree drvPath differing from `DRV_BEFORE_`
- `SC3_VERDICT` not MET
- `venvShimNames` not six names
- CLAUDE.md's `tox-uv~=1.35` bullet absent
- `CROSS_FILE_VERDICT` not `consistent`
- another tracked file changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## SC#1 literal reading` tabulates clauses (a)-(f), each with a quoted measurement, and gives clause (d) the verdict "contradicted by measurement, by design". `SC1_LITERAL_VERDICT` is recorded.
    - `## SC#1 amended reading` quotes the ROADMAP AMENDED lines, the recipe-tail hash pair and the boundary paragraph, and records `SC1_AMENDED_VERDICT = MET`.
    - `## SC#3 reading` shows the four merged-tree drvPaths equal to `DRV_BEFORE_`, an empty decision-ID grep, and each SC#3 part in the header, with `SC3_VERDICT = MET`.
    - `## Cross-file consistency` covers (a)-(e), with `CROSS_FILE_VERDICT = consistent`.
  </acceptance_criteria>
  <done>SC#1 is reported in both readings, SC#3 is re-proven on the merged tree, and the files the phase edited agree with each other.</done>
</task>

<task type="auto">
  <name>Task 3: Green tree on the merged tree (full suite, black, ruff), the phase-wide scope fence, and the DOC-19/DOC-20/DOC-21 closure table</name>
  <precondition>Task 2 is committed with `SC1_AMENDED_VERDICT = MET`, `SC3_VERDICT = MET`, `CROSS_FILE_VERDICT = consistent` and no HALT heading.</precondition>
  <reversibility rating="reversible">Read-only toward every tracked file except this plan's evidence.</reversibility>
  <files>.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLOSURE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-RESEARCH.md (§ Green-Tree Gate Method; § Common Pitfalls 4)
    - .planning/ROADMAP.md (binding constraint 13: every phase closes green on the full pytest suite; constraint 3: CI unchanged, so no dispatch)
    - .planning/REQUIREMENTS.md (DOC-19, DOC-20, DOC-21 text)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLOSURE-EVIDENCE.md (Tasks 1-2)
  </read_first>
  <action>
1. Re-run the head check and `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

2. **`## Green tree (merged)`.**
   - Write `PYVENV_HOME_68_04` and `PYVENV_VERSION_68_04` from `.venv/pyvenv.cfg`. Per D-06, this interpreter may differ from the main checkout's and from the wave-1 worktrees'. Compare no count across trees; the gate here is 0 failed.
   - Run `uv run pytest -q -p no:cacheprovider` in the foreground, in a Bash call with `timeout` set to 600000 ms. Never set `run_in_background`.
     - If one invocation cannot finish inside that limit, split `tests/test_*.py` (sorted) into two disjoint halves, run each half the same way, and record both transcripts and both summaries.
   - Quote the final summary line or lines verbatim.
   - Write `FULL_SUMMARY` as the counts joined with `, `, e.g. `N passed, M skipped`; when split, sum each count across both halves.
   - Write `FULL_FAILED` as failed plus errors, which must be 0. On a failure, quote the failing test IDs, write `## HALT: suite not green on the merged tree`, and stop. Fix nothing.
   - Record `uv run black --check .; echo "exit:$?"` and `uv run ruff check .; echo "exit:$?"`, both exit 0.
   - Record `uv run tox config -e py312 --core -k requires`.
   - No CI dispatch: no success criterion of this phase requires one, and CI is unchanged (constraint 3).

3. **`## Phase scope fence`.** Record each of these:
   - `git diff --name-only "$PHASE_BASE" HEAD -- . ':!.planning'`: exactly `CLAUDE.md`, `tox.ini`, `flake.nix`, `tests/test_toolchain_config_gate.py` and `tests/test_pdf_render_gate.py`, each present
   - `git diff --stat "$PHASE_BASE" HEAD -- typsphinx .github pyproject.toml uv.lock CHANGELOG.md`, which must be empty (constraint 13; CHANGELOG is Phase 69's)
   - `git diff --name-only "$BASE_68_04" -- .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md`, which must be empty: executors do not edit tracking files

4. **`## Requirement closure`.** A table with columns `Requirement`, `Criterion`, `Evidence (section)` and `Verdict`:
   - DOC-19: SC#1 amended reading, plus 68-01's gates
   - DOC-20: SC#2, the D-15 classification, plus 68-02's gates
   - DOC-21: SC#3, plus 68-03's gates

   Then write `DOC19_VERDICT`, `DOC20_VERDICT` and `DOC21_VERDICT`, each `MET` only when its sections hold and the green tree holds. Beneath the table, state that the SC#1 literal reading is reported separately above, with its own verdict.

5. Commit the evidence.
  </action>
  <verify>
    <automated>D=.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix &amp;&amp; F="$D/68-CLOSURE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_68_04)" &amp;&amp; PB="$(k PHASE_BASE)" &amp;&amp; [ -n "$B" ] &amp;&amp; [ -n "$PB" ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; [ "$(k FULL_FAILED)" = 0 ] &amp;&amp; FS="$(k FULL_SUMMARY)" &amp;&amp; [ -n "$FS" ] &amp;&amp; case "$FS" in *failed*|*error*) false ;; esac &amp;&amp; case "$FS" in *passed*) true ;; *) false ;; esac &amp;&amp; [ -n "$(k PYVENV_HOME_68_04)" ] &amp;&amp; [ -n "$(k PYVENV_VERSION_68_04)" ] &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; git diff --quiet "$PB" HEAD -- typsphinx .github pyproject.toml uv.lock CHANGELOG.md &amp;&amp; ! git diff --name-only "$PB" HEAD -- . ':!.planning' | grep -vxF -e CLAUDE.md -e tox.ini -e flake.nix -e tests/test_toolchain_config_gate.py -e tests/test_pdf_render_gate.py &amp;&amp; for f in CLAUDE.md tox.ini flake.nix tests/test_toolchain_config_gate.py tests/test_pdf_render_gate.py; do ! git diff --quiet "$PB" HEAD -- "$f" || { echo "expected edit missing: $f"; exit 1; }; done &amp;&amp; git diff --quiet "$B" -- .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md &amp;&amp; for r in DOC19_VERDICT DOC20_VERDICT DOC21_VERDICT; do [ "$(k "$r")" = MET ] || { echo "requirement not MET: $r"; exit 1; }; done &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e "$F" -e "$D/68-04-SUMMARY.md" &amp;&amp; for s in '## Green tree (merged)' '## Phase scope fence' '## Requirement closure'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `expected edit missing: <file>`, `requirement not MET: <key>` or `missing section: <name>` printed. Each of these makes it fail:
- `FULL_FAILED` not 0, or `FULL_SUMMARY` empty, containing `failed` or `error`, or lacking `passed`
- the interpreter keys missing
- black or ruff reporting any file
- any change under typsphinx/, .github/, pyproject.toml, uv.lock or CHANGELOG.md since `PHASE_BASE`
- a non-.planning file outside the five targets changed, or one of the five not changed
- STATE, ROADMAP or REQUIREMENTS edited in this worktree
- a DOC verdict not MET
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Green tree (merged)` quotes the full-suite summary verbatim, with `FULL_FAILED = 0`, `FULL_SUMMARY` and the interpreter keys, plus `black --check .` and `ruff check .` at exit 0.
    - `## Phase scope fence` shows exactly the five edit targets outside `.planning/`, each changed, and an empty diff for `typsphinx/`, `.github/`, `pyproject.toml`, `uv.lock` and `CHANGELOG.md` since `PHASE_BASE`.
    - `## Requirement closure` records `DOC19_VERDICT = MET`, `DOC20_VERDICT = MET` and `DOC21_VERDICT = MET`, each tied to its evidence sections.
  </acceptance_criteria>
  <done>The merged tree is green, the phase touched only its five named files, and DOC-19..DOC-21 close on evidence measured in this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closure evidence → verifier and requirement tracking | the verifier and the orchestrator's requirement flip read this file; a copied value would close a requirement on a claim instead of a measurement |
| merged tree → next phase (69) | Phase 69 packages whatever this phase leaves; a silent product-file change would ride into the milestone PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-10 | Repudiation | classification and verdicts copied from RESEARCH or the wave-1 evidence | medium | mitigate | The verify re-runs the D-15 grep live and requires one table row per live hit, each with its `path:line`. It re-evaluates the drvPaths on the merged tree, and re-runs black, ruff and the scope diffs. Verdict keys are checked against those live results, not against prose. |
| T-68-11 | Tampering | the closure plan "fixing" a defect it finds | medium | mitigate | The scope fence allows only this plan's evidence and SUMMARY to change in its worktree. Defects halt under a `## HALT` heading for the owner. |
| T-68-12 | Tampering | an out-of-scope change riding in from wave 1 (typsphinx/, .github/, pyproject.toml, uv.lock, CHANGELOG.md) | medium | mitigate | `git diff --quiet PHASE_BASE HEAD` over those paths, and an exact allow-list of the five edit targets outside `.planning/`. |
| T-68-SC | Tampering | `uv sync --extra dev` during provisioning | low | accept | It installs only the pins already in `uv.lock`, and no new package is added. |
</threat_model>

<verification>
- SC#2: the D-15 grep on the merged tree, with every hit classified and zero C3 rows.
- SC#1: literal and amended readings reported separately, the amended one MET, and the recipe byte-identical to `PHASE_BASE`.
- SC#3: the header content present, the drvPaths unchanged on the merged tree, and no decision IDs.
- The files agree with each other; the full suite, black and ruff are green; the scope is exactly the five targets.
</verification>

<success_criteria>
DOC-19, DOC-20 and DOC-21 are closed on measurements of the merged tree, taken in a wave after every edit they measure. SC#1's literal reading is kept distinct from its amended reading.
</success_criteria>

<output>
Create `.planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-04-SUMMARY.md` when done.
- Frontmatter `requirements-completed: [DOC-19, DOC-20, DOC-21]`.
- The first section states `PHASE_BASE`, `TOXUVBARE_HITS` with the C1/C2/C3 counts, `SC2_VERDICT`, `SC1_LITERAL_VERDICT`, `SC1_AMENDED_VERDICT`, `SC3_VERDICT`, `CROSS_FILE_VERDICT`, `FULL_SUMMARY` with the interpreter, and the three DOC verdicts.
- If a HALT heading was written, that section says so first.
</output>
