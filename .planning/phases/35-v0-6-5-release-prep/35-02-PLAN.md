---
phase: 35-v0-6-5-release-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/
autonomous: true
requirements: [REL-03]

must_haves:
  truths:
    - "A pending todo file records WR-01 — `visit_math_block`'s pre-existing unconditional double-newline stacking with the new `list_item_needs_separator` flag to emit one redundant blank line — including both fix options the Phase 34 review offers and the measured reason it was not taken in v0.6.5 (D-05, D-10)."
    - "A second pending todo file records the `release.yml` release-notes rework: drop the hand-rolled commit-dump block, extract the `## [X.Y.Z]` section from CHANGELOG.md as the body, keep the Installation block and GitHub's own auto-generated notes (D-11)."
    - "The `release.yml` todo records the measured fact that the workflow never reads CHANGELOG.md today, so the Phase 33 CONTEXT statement that the CHANGELOG entry is the single source for the GitHub Release body is false until this todo is resolved (D-11)."
    - "Both files carry the frontmatter shape every existing pending todo uses — `created`, `title`, `area`, and a `files` list whose entries pair a path with a rationale — and a `## Problem` / `## Solution` body."
    - "Neither file changes `.github/workflows/release.yml` and neither changes anything under `typsphinx/` — both deferrals stay deferred (D-05, D-11)."
    - "Filing these todos now is what makes the decision not to pick WR-01 up a recorded decision rather than a lost one (D-10)."
  artifacts:
    - path: ".planning/todos/pending/<date>-visit-math-block-redundant-blank-line-in-list-items.md"
      provides: "WR-01 deferral record with both candidate fixes and the measured cost of taking it pre-release"
      contains: "visit_math_block"
    - path: ".planning/todos/pending/<date>-release-notes-body-from-changelog-section.md"
      provides: "release.yml release-notes rework design direction plus the measured 308-line body breakdown"
      contains: "release.yml"
  key_links:
    - from: ".planning/todos/pending/<date>-visit-math-block-redundant-blank-line-in-list-items.md"
      to: ".planning/phases/34-inline-math-after-text-separator-fix/34-REVIEW.md"
      via: "the todo cites WR-01's file/line anchor and reproduces both Fix options, so the deferred work is reconstructible without re-deriving it"
      pattern: "WR-01"
    - from: ".planning/todos/pending/<date>-release-notes-body-from-changelog-section.md"
      to: ".github/workflows/release.yml"
      via: "the todo names the create-release job's Generate release notes step as the edit site, without editing it"
      pattern: "release\\.yml"
---

<objective>
File the two deferred-work items this phase deliberately does not pick up, so each decision is
recorded rather than lost: WR-01 (the redundant blank line `visit_math_block` emits inside a list
item) and the `release.yml` release-notes-body rework.

Purpose: D-05 excludes WR-01 from Phase 35 because fixing it requires a translator change, which
would violate milestone invariant #3, force the GATE-01 fixture's expected strings to be re-derived,
and force a full-corpus re-run immediately before a release. D-11 excludes the `release.yml` rework
from v0.6.5 for the same reason of release proximity. D-10 requires that Phase 35 nonetheless write
the WR-01 todo now — an unrecorded deferral is indistinguishable from an oversight at the next
backlog review. Both files are inputs to `/gsd-complete-milestone`'s handoff checklist (item 6) and
candidates for the v0.6.6 scope.

Output: two new files under `.planning/todos/pending/`. Nothing else in the repository changes.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-v0-6-5-release-prep/35-CONTEXT.md
@.planning/phases/35-v0-6-5-release-prep/35-RESEARCH.md
@.planning/phases/35-v0-6-5-release-prep/35-PATTERNS.md
@CLAUDE.md
</context>

## Environment prerequisite (read before Task 1)

If `.git` is a FILE you are in an isolated worktree. Provision it before running anything, then run
every command through `uv run`:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`

This plan writes only Markdown, but the provisioning step is unconditional in this repository: a
worktree without it resolves `import typsphinx` to the unchanged main-tree package, which silently
poisons any verification you might run.

## Artifacts this phase produces

| Artifact | Kind | Change |
|----------|------|--------|
| `.planning/todos/pending/<date>-visit-math-block-redundant-blank-line-in-list-items.md` | new file | created — WR-01 deferral record |
| `.planning/todos/pending/<date>-release-notes-body-from-changelog-section.md` | new file | created — `release.yml` rework deferral record |

`<date>` is the `date +%F` output on the day the file is written (the existing pending todos use the
same `YYYY-MM-DD-kebab-case-english-slug.md` form). No new symbols, no code, no workflow edits.

## Language note (applies to both tasks)

Write both files in **English**. The five existing files under `.planning/todos/pending/` are written
in Japanese; that predates the standing convention that everything under `.planning/` is written in
English, which every Phase 34 and Phase 35 artifact already follows. Replicate the existing files'
frontmatter *shape* and section *structure* exactly, and write their *content* in English. Do not
retranslate or otherwise touch the five existing files.

<tasks>

<task type="auto">
  <name>Task 1: File the WR-01 deferral todo</name>

  <files>.planning/todos/pending/&lt;date&gt;-visit-math-block-redundant-blank-line-in-list-items.md</files>

  <read_first>
    - `.planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md` — read in full. This is the
      frontmatter and body shape to replicate: `created` as an ISO-8601 timestamp with the `+09:00`
      offset, `title`, `area` as a short comma-separated tag list, `files` as a YAML list whose
      entries each pair a path with a parenthetical rationale, then `## Problem` and `## Solution`.
    - `.planning/phases/34-inline-math-after-text-separator-fix/34-REVIEW.md` — section `### WR-01`
      in full. It carries the file anchor (`typsphinx/translator.py:4079-4088`), the empirical
      reproduction (Construct E emitting two blank lines between the `mitex(...)` call and the
      following `parbreak()` where every other block-level construct emits one), and two candidate
      fixes in the Fix field.
    - `typsphinx/translator.py` — read only the region around lines 4040-4095 (`_emit_id_anchors`
      call site, the unconditional double-newline emission, and the new `list_item_needs_separator`
      block). You are reading to describe it accurately in prose. Do not edit this file.
    - `.planning/phases/35-v0-6-5-release-prep/35-CONTEXT.md` — decisions D-05 and D-10, and the
      `<deferred>` block's WR-01 paragraph.
    - `.planning/phases/35-v0-6-5-release-prep/35-PATTERNS.md` — section
      `### Todo file frontmatter and body shape`.
  </read_first>

  <action>
    Create a new file under `.planning/todos/pending/` named with the current `date +%F` output as
    the prefix and the slug `visit-math-block-redundant-blank-line-in-list-items`.

    Frontmatter: set `created` to the current local timestamp in ISO-8601 with the `+09:00` offset.
    Set `title` to a one-line English statement that block math inside a list item emits one
    redundant blank line. Set `area` to `translator, tests`. Set `files` to a list naming, each with
    an English parenthetical rationale: `typsphinx/translator.py` (the `visit_math_block` site
    carrying both separation mechanisms, around lines 4079-4088 as measured in the Phase 34 review),
    `tests/test_inline_math_after_text_render_gate.py` (the exact-string assertions for Constructs E
    and G that would have to be re-derived once the emitted shape changes), and
    `tests/test_corpus_gate.py` (the full-corpus gate that has to be re-run because the emitted
    `.typ` shape changes across the whole corpus).

    In `## Problem`, state the measured facts, not a summary of them: the pre-existing unconditional
    double-newline emission in `visit_math_block` already guarantees separation from any following
    sibling in every context; the Phase 34 fix additionally sets `list_item_needs_separator` when
    inside a list item; the next sibling's own visitor independently consults that flag and emits its
    own leading newline; so the two mechanisms stack and block math is followed by one more blank
    line than every other block-level handler produces. Record that this is inert in Typst code mode
    (the compiled and visual output is unaffected, because the real paragraph break is the
    `parbreak()` call, not the whitespace) and that the cost is therefore diff noise and divergence
    from every other block-level visitor rather than a rendering defect. Record the measured
    reproduction from the review: Construct E's emission carries two blank lines where one is the
    norm elsewhere in the same document.

    Also record why it was deferred, in the review's and D-05's own terms: the fix requires a change
    under `typsphinx/`, which milestone invariant #3 forbids for v0.6.5; it changes the emitted output
    shape, which forces the GATE-01 fixture's Construct E and Construct G exact-string assertions to
    be re-derived and the full-corpus `-b typstpdf` gate to be re-run; and v0.6.5 is a hotfix release
    where an output-shape change immediately before the tag is not worth taking.

    In `## Solution`, give both candidate fixes the review names as concrete alternatives, each with
    its consequence: (a) drop the newly added block that sets `list_item_needs_separator` inside
    `visit_math_block`, on the grounds that the pre-existing unconditional separation already
    prevents any juxtaposition fatal; or (b) keep the flag and instead gate the pre-existing
    unconditional separation so it fires only outside a list item, so exactly one mechanism owns
    separation in each context — the shape `depart_paragraph` already follows. Add the follow-up work
    either option implies: re-derive the Construct E and Construct G exact-string assertions from a
    fresh build on both emission paths, and re-run the full-corpus gate.

    Do not modify `typsphinx/translator.py`. Do not modify any test file. This task creates exactly
    one new file.
  </action>

  <verify>
    <automated>ls .planning/todos/pending/*-visit-math-block-redundant-blank-line-in-list-items.md | wc -l | grep -qx 1 &amp;&amp; F=$(ls .planning/todos/pending/*-visit-math-block-redundant-blank-line-in-list-items.md) &amp;&amp; grep -qE '^created: ' "$F" &amp;&amp; grep -qE '^title: ' "$F" &amp;&amp; grep -qE '^area: ' "$F" &amp;&amp; grep -qE '^files:' "$F" &amp;&amp; grep -qx '## Problem' "$F" &amp;&amp; grep -qx '## Solution' "$F" &amp;&amp; git diff --name-only -- typsphinx/ tests/ .github/ | wc -l | grep -qx 0 &amp;&amp; echo WR01_TODO_OK</automated>
  </verify>

  <acceptance_criteria>
    - Exactly one file matches
      `.planning/todos/pending/*-visit-math-block-redundant-blank-line-in-list-items.md`, and its
      basename prefix matches the current `date +%F` output.
    - That file's frontmatter contains all four keys `created`, `title`, `area`, `files`, with
      `created` matching an ISO-8601 timestamp ending in `+09:00`.
    - The file contains a line exactly equal to `## Problem` and a line exactly equal to
      `## Solution`.
    - The file names `visit_math_block`, cites `typsphinx/translator.py`, and mentions both candidate
      fixes — `grep -c 'typsphinx/translator.py' <file>` returns at least `1`.
    - `git diff --name-only -- typsphinx/ tests/ .github/` prints nothing.
    - `git status --porcelain` lists exactly one new path, under `.planning/todos/pending/`.
  </acceptance_criteria>

  <done>
    WR-01 is recorded as a deliberate deferral with its measurement, its cost, and both candidate
    fixes, and no source, test, or workflow file moved.
  </done>
</task>

<task type="auto">
  <name>Task 2: File the release.yml release-notes-body rework todo</name>

  <files>.planning/todos/pending/&lt;date&gt;-release-notes-body-from-changelog-section.md</files>

  <read_first>
    - `.planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md` — the frontmatter and body
      shape, if not already read in Task 1.
    - `.github/workflows/release.yml` — read the `create-release` job (starting line 127): the
      `Extract version from tag` step (line 142), the `Generate release notes` step (line 152)
      including the commit-dump command on line 164 and the Installation block written from line 170,
      and the `Create GitHub Release` step (line 176) whose options include `generate_release_notes`
      on line 185. You are reading to describe the edit site accurately. Do not edit this file.
    - `.planning/phases/35-v0-6-5-release-prep/35-CONTEXT.md` — decision D-11 in full, including the
      measured 308-line breakdown of the v0.6.4 release body and the measured fact about what the
      workflow does and does not read.
    - `CHANGELOG.md` — read lines 1-50 only, to see the `## [X.Y.Z]` section boundary shape a future
      extraction step would have to key on.
  </read_first>

  <action>
    Create a new file under `.planning/todos/pending/` named with the current `date +%F` output as
    the prefix and the slug `release-notes-body-from-changelog-section`.

    Frontmatter: `created` as an ISO-8601 timestamp with the `+09:00` offset; a one-line English
    `title` stating that the GitHub Release body should be the CHANGELOG section rather than a commit
    dump; `area` set to `ci, release`; and a `files` list naming `.github/workflows/release.yml` (the
    `create-release` job's `Generate release notes` step at line 152, whose commit-dump command sits
    at line 164 and whose Installation block starts at line 170) and `CHANGELOG.md` (the source the
    reworked step would extract the `## [X.Y.Z]` section from), each with an English parenthetical
    rationale.

    In `## Problem`, record the measurement from D-11 rather than a characterization of it: the
    v0.6.4 GitHub Release body is 308 lines; lines 1 through 296 are the commit dump the workflow
    itself produces from a `git log` range with a one-line-per-commit format, including planning
    commits that mean nothing to a reader of the release; lines 297 through 303 are the Installation
    block; lines 304 through 308 are GitHub's own output from the auto-generated-notes option, which
    is a single pull-request line plus a full-changelog link. State the conclusion the measurement
    supports: the auto-generated portion is already compact and the bloat is entirely the hand-rolled
    commit-dump block.

    Also record the second measured fact D-11 requires: `release.yml` never reads `CHANGELOG.md` at
    all today. Note explicitly that the Phase 33 CONTEXT statement describing the CHANGELOG entry as
    the single source for the GitHub Release body contradicts the workflow as written, and only
    becomes true once this todo is resolved. This correction is the reason the todo exists in this
    form rather than as a cosmetic cleanup.

    In `## Solution`, give the design direction as concrete steps: remove the commit-dump block from
    the `Generate release notes` step; extract the single `## [X.Y.Z]` section matching the tag's
    version from `CHANGELOG.md` and use it as the release body; keep the Installation block; keep the
    auto-generated-notes option enabled so the pull-request line and full-changelog link survive. Add
    the failure mode a future implementer must handle: the tag's version may have no matching
    CHANGELOG section, and the step must fail loudly rather than publish an empty body.

    Do not modify `.github/workflows/release.yml`. This task creates exactly one new file.
  </action>

  <verify>
    <automated>ls .planning/todos/pending/*-release-notes-body-from-changelog-section.md | wc -l | grep -qx 1 &amp;&amp; F=$(ls .planning/todos/pending/*-release-notes-body-from-changelog-section.md) &amp;&amp; grep -qE '^created: ' "$F" &amp;&amp; grep -qE '^title: ' "$F" &amp;&amp; grep -qE '^area: ' "$F" &amp;&amp; grep -qE '^files:' "$F" &amp;&amp; grep -qx '## Problem' "$F" &amp;&amp; grep -qx '## Solution' "$F" &amp;&amp; grep -q 'release.yml' "$F" &amp;&amp; git diff --name-only -- .github/ typsphinx/ tests/ | wc -l | grep -qx 0 &amp;&amp; echo RELEASE_TODO_OK</automated>
  </verify>

  <acceptance_criteria>
    - Exactly one file matches
      `.planning/todos/pending/*-release-notes-body-from-changelog-section.md`, and its basename
      prefix matches the current `date +%F` output.
    - That file's frontmatter contains `created`, `title`, `area`, `files`, with `created` matching
      an ISO-8601 timestamp ending in `+09:00`.
    - The file contains a line exactly equal to `## Problem` and a line exactly equal to
      `## Solution`.
    - The file cites `.github/workflows/release.yml` and `CHANGELOG.md`, and states the 308 / 296 /
      7 / 5 line breakdown of the v0.6.4 release body — `grep -c '308' <file>` returns at least `1`.
    - The file states that the workflow does not read `CHANGELOG.md` today, and names the Phase 33
      CONTEXT statement it corrects.
    - `git diff --name-only -- .github/ typsphinx/ tests/` prints nothing.
    - `git status --porcelain` lists exactly one new path for this task, under
      `.planning/todos/pending/`.
  </acceptance_criteria>

  <done>
    The `release.yml` release-notes rework is recorded with its measurement, its design direction,
    and the correction to the Phase 33 CONTEXT claim, with the workflow file itself untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deferred defect -> backlog record | The only thing standing between a deliberate deferral and a silently lost defect is the written record this plan produces. |
| planning prose -> future implementer | A todo that summarizes rather than measures leads a future implementer to re-derive facts already established, or to act on a stale characterization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-04 | Repudiation | the WR-01 deferral record | medium | mitigate | Task 1's acceptance criteria require the file to cite the source anchor and carry both candidate fixes, so the deferral is reconstructible rather than a bare "deferred" note. Handoff item 6 in plan 35-05 re-confirms both files exist before the milestone closes. |
| T-35-05 | Information disclosure | the corrected Phase 33 CONTEXT claim | low | mitigate | Task 2 requires the todo to state plainly that the workflow does not read `CHANGELOG.md`, so the incorrect prior claim is corrected in writing rather than propagating into the next release's planning. |
| T-35-06 | Tampering | `.github/workflows/release.yml` | high | mitigate | Both tasks gate on `git diff --name-only -- .github/` printing nothing. The release workflow is the mechanism that performs the irreversible publish; editing it during release prep is out of scope by D-11 and would change publish behavior under a plan that is not reviewing publish behavior. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and writes only Markdown. `35-RESEARCH.md` § Package Legitimacy Audit records the gate as not-applicable. |
</threat_model>

<verification>
- `ls .planning/todos/pending/ | wc -l` returns 7 (the five pre-existing files plus the two created here).
- `git diff --name-only -- typsphinx/ tests/ .github/ docs/` prints nothing.
- `git status --porcelain` lists exactly two new paths, both under `.planning/todos/pending/`.
</verification>

<success_criteria>
Both deliberately-deferred items are recorded as pending todos with their measurements, their
rationale for deferral, and concrete design direction. D-05's WR-01 exclusion and D-11's `release.yml`
exclusion are now documented decisions. `/gsd-complete-milestone` can confirm both files exist as
handoff item 6, and the v0.6.6 backlog review has both items in hand.
</success_criteria>

<output>
Create `.planning/phases/35-v0-6-5-release-prep/35-02-SUMMARY.md` when done.
Record the two resulting filenames verbatim — plan 35-05 cites them in `35-HANDOFF.md` item 6.
</output>
