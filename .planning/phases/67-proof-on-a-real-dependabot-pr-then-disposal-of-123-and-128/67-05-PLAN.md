---
phase: 67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128
plan: 05
type: execute
wave: 4
depends_on: ["67-03", "67-04"]
files_modified:
  - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CLOSURE-EVIDENCE.md
autonomous: true
requirements: [DEP-02, DEP-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Wave gate: 67-PROOF-EVIDENCE.md (DEP02_VERDICT = MET), 67-RUFF-EVIDENCE.md (OWNER_DECISION_138 = merge, OWNER_DECISION_123 = close) and 67-DOCUTILS-EVIDENCE.md (OWNER_DECISION_128 = close, D05_CAP_RELAXED_PRECLOSE = no) are complete, and none of the four phase evidence files carries a HALT heading. Live, #138 is MERGED at MERGE_SHA_138, and #123 and #128 are CLOSED with exactly one owner comment each, equal to APPROVED_COMMENT_123 and APPROVED_COMMENT_128.'
    - 'Constraint 4 and D-02 ordering, from recorded timestamps: DEP02_PROOF_AT is earlier than DECIDED_AT_138 and DECIDED_AT_128, and MERGED_AT_138 is earlier than CLOSED_AT_123. The proof preceded every merit decision, and #138 merged before #123 closed.'
    - 'D-06 / SC#4 literal reading, stated in writing: this milestone proof does not cover a grouped sphinx-typst-stack uv update, and a future grouped PR failing at resolution is an uncovered case, not a regression of this fix.'
    - 'D-06 / SC#4 amended reading, stated separately. #128 is a sphinx-typst-stack group PR (title and branch quoted live). UV_GROUP_PR_COUNT, the live count of dependabot/uv/sphinx-typst-stack PRs, is recorded (0 at planning). The group docutils member failed with dependency_file_not_resolvable, quoted live from run 34688990228 log and citing 66-DEPENDABOT-EVIDENCE.md § uv update job conclusion. #138 is not grouped. The ROADMAP.md and PROJECT.md AMENDED 2026-09-12 (Phase 67 discuss) blocks are cited by grep -n, never re-edited.'
    - 'Closure: ## Requirement closure lists DEP-02 and DEP-05, each MET with its deciding sections named. SC#1 to SC#4 are each mapped to their sections, with SC#4 reported as literal and amended readings separately. REQUIREMENTS.md is not edited by any Phase 67 plan; its checkboxes flip at phase completion.'
    - 'Flagged assumption A-67-01 is re-checked at closure: PROOF_RUN_ID is still readable through gh (attempt 1, conclusion recorded), or the evidence states that DEP-02 now rests on the 67-01 transcription alone.'
    - 'Todo: .planning/todos/pending/2026-08-16-dependabot-prs-die-on-uv-lock-locked-mismatch.md stays in pending with resolves_phase: 67, and this plan neither moves nor edits it. execute-phase step close_phase_todos moves it to completed/ at phase completion, and TODO_DISPOSITION records that.'
    - 'Deferred PRs #139-#142: the owner account made no comment and no timeline event on any of them during this phase; they are outside DEP-05 text.'
  artifacts:
    - path: .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CLOSURE-EVIDENCE.md
      provides: 'wave gate and live re-assertion, SC#4 grouped-update coverage gap (D-06, literal and amended), ordering, requirement closure, deferred-PR check, todo disposition'
      contains: '## Requirement closure'
  key_links:
    - from: 'ROADMAP.md and PROJECT.md AMENDED 2026-09-12 (Phase 67 discuss) blocks'
      to: 'the SC#4 section of 67-CLOSURE-EVIDENCE.md'
      via: 'grep -n citation, with no edit to either file'
      pattern: 'UV_GROUP_PR_COUNT = '
    - from: 'todo resolves_phase: 67'
      to: 'execute-phase close_phase_todos at phase completion'
      via: 'the todo left in pending with its frontmatter untouched'
      pattern: 'TODO_DISPOSITION = '
  prohibitions:
    - statement: 'MUST NOT edit .planning/REQUIREMENTS.md (DEP-02/DEP-05 checkboxes or text), .planning/ROADMAP.md or .planning/PROJECT.md; the D-06 AMENDED blocks are cited, not rewritten.'
      requirement_id: DEP-05
      category: transparency
      status: resolved
      verification: test
    - statement: 'MUST NOT move, edit or delete the folded todo; execute-phase closes it at phase completion.'
      requirement_id: DEP-05
      category: values
      status: resolved
      verification: test
    - statement: 'MUST NOT report the grouped-update gap as covered, or describe a future grouped uv resolution failure as a regression of this milestone fix (SC#4).'
      requirement_id: DEP-05
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT merge, close, comment on or @dependabot-command any pull request in this plan; it is read-only toward GitHub.'
      requirement_id: DEP-05
      category: safety
      status: resolved
      verification: test
---

<objective>
Close the phase in writing. Two things are recorded here:
- **SC#4, the grouped-update coverage gap (D-06),** reported in both its literal and its amended reading.
- **The requirement closure** for DEP-02 and DEP-05, from the three dispositions already recorded, each re-asserted live.

**D-06:** SC#4's premise ("neither #123 nor #128 is a grouped bump") was falsified at discuss time. #128 *is* a `sphinx-typst-stack` group PR, and the owner-approved `AMENDED 2026-09-12` blocks are already committed in `ROADMAP.md` and `PROJECT.md`. `REQUIREMENTS.md` DEP-05 stays literal. The conclusion stands unchanged:
- Under `uv`, no group PR opened, because the group's `docutils` member hit `dependency_file_not_resolvable`. That is itself a live instance of the "genuinely unresolvable dependency graph" SC#4 names.
- The PR that carries the proof (#138) is not grouped.

So the milestone's proof does not cover a grouped `uv` update, and a future grouped PR failing at resolution is an uncovered case, not a regression. This plan cites the AMENDED blocks, never edits them, and the verifier reports the literal and amended readings separately.

This plan edits no requirement file. The checkboxes flip at phase completion, as Phase 66's did. It also leaves the folded todo to execute-phase's `close_phase_todos` step, which moves it at completion because it carries `resolves_phase: 67`.

Output: `67-CLOSURE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CONTEXT.md
@.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-PROOF-EVIDENCE.md
@.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-RUFF-EVIDENCE.md
@.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-DOCUTILS-EVIDENCE.md
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md
@.planning/todos/pending/2026-08-16-dependabot-prs-die-on-uv-lock-locked-mismatch.md
@CLAUDE.md
</context>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: NOT MET -->
`## HALT` and `NOT MET` are words the executor must be able to write on a failure path; the gates require their absence.

## Planning-time census (2026-09-12T12:22Z: re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| AMENDED blocks | `ROADMAP.md` Phase 67 SC#4 block "AMENDED 2026-09-12 (Phase 67 discuss, owner-approved)"; `PROJECT.md:126` block with the same prefix | `grep -n` |
| `dependabot/uv/sphinx-typst-stack*` PRs | none, ever | `gh pr list --state all --author app/dependabot` |
| Group PRs under `pip` | #108, #113, #122, #128 (`dependabot/pip/sphinx-typst-stack-*`) | `66-DEPENDABOT-EVIDENCE.md` § Grouping |
| uv attempt | run `34688990228`, one error-table row: `docutils` `dependency_file_not_resolvable` | `66-DEPENDABOT-EVIDENCE.md` § uv update job conclusion |
| Folded todo | `.planning/todos/pending/2026-08-16-dependabot-prs-die-on-uv-lock-locked-mismatch.md`, frontmatter `resolves_phase: 67` | `head` of the file |
| Todo auto-close | `execute-phase.md` step `close_phase_todos` (line 1482): "After `update_roadmap`, moves todos whose `resolves_phase` matches to `completed/`" | `.claude/gsd-core/workflows/execute-phase.md:1482-1519` |
| Deferred PRs | #139 (`tox`), #140 (`sphinx-intl`), #141 (`pre-commit`), #142 (`mypy`), lockfile-only, not merged, closed or commented on in this phase | CONTEXT `<deferred>` |

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `67-CLOSURE-EVIDENCE.md` sections `## Head check and provisioning`, `## Wave gate and live re-assertion`, `## SC#4 grouped-update coverage gap (D-06)`, `## Ordering (constraint 4)`, `## Deferred PRs untouched`, `## Folded todo`, `## Requirement closure`; keys `BASE_67_05`, `UV_GROUP_PR_COUNT`, `A6701_STATUS`, `TODO_DISPOSITION` | evidence | phase directory |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: wave gate over the three evidence files, live re-assertion of all three dispositions, and the SC#4 grouped-update coverage gap in its literal and amended readings (D-06)</name>
  <precondition>`67-PROOF-EVIDENCE.md`, `67-RUFF-EVIDENCE.md` and `67-DOCUTILS-EVIDENCE.md` exist with `DEP02_VERDICT = MET`, `OWNER_DECISION_138 = merge`, `OWNER_DECISION_123 = close` and `OWNER_DECISION_128 = close`, and none has a HALT heading. Otherwise halt at a blocking-human gate: a disposition is incomplete, so DEP-05 cannot close.</precondition>
  <reversibility rating="reversible">Read-only toward GitHub; only the evidence file is written.</reversibility>
  <files>.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CLOSURE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-PROOF-EVIDENCE.md (all keys)
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-RUFF-EVIDENCE.md (all keys; § SC#3 merits for #138; § #123 disposition (D-03))
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-DOCUTILS-EVIDENCE.md (all keys; § D-05 merits for #128; § #128 disposition (D-05))
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CONTEXT.md (D-06, and the Specifics note on the neighbouring PROJECT.md claim)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md (§ uv update job conclusion; § D-06 observations)
    - .planning/ROADMAP.md (Phase 67 SC#4 and its AMENDED block)
    - .planning/PROJECT.md (lines 116-135, the grouping bullet and its AMENDED block)
    - CLAUDE.md, section "Worktree-isolated execution"
  </read_first>
  <action>
1. **Head check and provisioning** (CLAUDE.md, mandatory).
   - Record `test -f .git; echo "exit:$?"` and `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`.
   - Run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, and record `gh auth status`.
   - Record `BASE_67_05 = <git rev-parse HEAD>`.

2. **`## Wave gate and live re-assertion`.**
   - Quote every key named in the precondition, plus `MERGE_SHA_138`, `MERGED_AT_138`, `CLOSED_AT_123`, `CLOSED_AT_128`, `APPROVED_COMMENT_123`, `APPROVED_COMMENT_128`, `DEP02_PROOF_AT`, `DECIDED_AT_138` and `DECIDED_AT_128`, each read with `sed -n 's/^KEY = //p' <file> | head -n 1`.
   - Record `grep -c '^## HALT'` for each of the three files, each 0.
   - Re-assert live, and record verbatim:
     - `gh pr view 138 --json state,mergeCommit,mergedAt`, which must read MERGED at `MERGE_SHA_138`
     - `gh pr view 123 --json state,closedAt,comments` and `gh pr view 128 --json state,closedAt,comments`: each CLOSED, with exactly one comment by `gh api user --jq .login` whose body equals its approved text
   - **A-67-01 re-check.** Record `gh run view "$PROOF_RUN_ID" --json attempt,conclusion,headSha`. When it resolves at `PROOF_SHA` with attempt 1, write `A6701_STATUS = held`. Otherwise quote the error, write `A6701_STATUS = run-unreadable`, and state that DEP-02 now rests on the 67-01 transcription alone.

3. **`## SC#4 grouped-update coverage gap (D-06)`.** Write the section in this order.
   - **Measurements**, each quoted:
     - `grep -n 'AMENDED 2026-09-12 (Phase 67 discuss' .planning/ROADMAP.md .planning/PROJECT.md`, at least one line per file; the blocks are cited and not edited
     - `gh pr view 128 --json title,headRefName` (the group PR)
     - `gh pr view 138 --json title,headRefName`, where the title names no group
     - `gh pr list --state all --author app/dependabot --limit 200 --json number,headRefName,state --jq '[.[] | select(.headRefName | startswith("dependabot/uv/sphinx-typst-stack"))]'`, written as `UV_GROUP_PR_COUNT = <length>`
     - `gh run view 34688990228 --log | grep -m3 'dependency_file_not_resolvable'`, citing `66-DEPENDABOT-EVIDENCE.md` § uv update job conclusion
   - **Literal reading** (ROADMAP SC#4 as written): quote SC#4's text. State in writing that this milestone's proof does not cover the `sphinx-typst-stack` grouped path, and that a future grouped PR failing at resolution is an uncovered case, not a regression of this fix.
   - **Amended reading** (D-06, owner-approved):
     - SC#4's premise "neither #123 nor #128 is a grouped bump" is falsified by the #128 measurement above.
     - Under `uv` no group PR opened (`UV_GROUP_PR_COUNT`), because the group's `docutils` member hit `dependency_file_not_resolvable`. That is itself a live instance of the unresolvable-graph failure SC#4 names.
     - The proof PR #138 is not grouped.
     - The conclusion is unchanged: the gap is recorded, and it is not a regression.
   - State that `REQUIREMENTS.md` DEP-05 stays literal, because its text is correct.
   - Record, without amending it, that `PROJECT.md`'s neighbouring claim that #123/#128 "will need closing so the `uv` ecosystem opens fresh ones" is superseded by Phase 66's measurement. It is left for the milestone-close `PROJECT.md` update (CONTEXT Specifics).

4. Append the sections, with keys at column 0. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128 &amp;&amp; F="$D/67-CLOSURE-EVIDENCE.md" &amp;&amp; FP="$D/67-PROOF-EVIDENCE.md" &amp;&amp; FR="$D/67-RUFF-EVIDENCE.md" &amp;&amp; FD="$D/67-DOCUTILS-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; kx() { sed -n "s/^$2 = //p" "$1" | head -n 1; } &amp;&amp; [ "$(kx "$FP" DEP02_VERDICT)" = MET ] &amp;&amp; [ "$(kx "$FR" OWNER_DECISION_138)" = merge ] &amp;&amp; [ "$(kx "$FR" OWNER_DECISION_123)" = close ] &amp;&amp; [ "$(kx "$FD" OWNER_DECISION_128)" = close ] &amp;&amp; [ "$(kx "$FD" D05_CAP_RELAXED_PRECLOSE)" = no ] &amp;&amp; for f in "$FP" "$FR" "$FD" "$F"; do ! grep -q '^## HALT' "$f" || { echo "HALT heading in $f"; exit 1; }; done &amp;&amp; [ -n "$(kx "$F" BASE_67_05)" ] &amp;&amp; [ -n "$(kx "$F" A6701_STATUS)" ] &amp;&amp; [ "$(gh pr view 138 --json state,mergeCommit -q '[.state,.mergeCommit.oid] | join(" ")')" = "MERGED $(kx "$FR" MERGE_SHA_138)" ] &amp;&amp; [ "$(gh pr view 123 --json state -q .state)" = CLOSED ] &amp;&amp; [ "$(gh pr view 128 --json state -q .state)" = CLOSED ] &amp;&amp; [ "$(grep -c 'AMENDED 2026-09-12 (Phase 67 discuss' .planning/ROADMAP.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'AMENDED 2026-09-12 (Phase 67 discuss' .planning/PROJECT.md)" -ge 1 ] &amp;&amp; G="$(kx "$F" UV_GROUP_PR_COUNT)" &amp;&amp; [ -n "$G" ] &amp;&amp; [ "$(gh pr list --state all --author app/dependabot --limit 200 --json headRefName -q '[.[] | select(.headRefName | startswith("dependabot/uv/sphinx-typst-stack"))] | length')" = "$G" ] &amp;&amp; [ "$(gh pr view 128 --json title -q '.title | test("sphinx-typst-stack group")')" = true ] &amp;&amp; [ "$(gh pr view 138 --json title -q '.title | test("group")')" = false ] &amp;&amp; gh run view 34688990228 --log | grep -q 'dependency_file_not_resolvable' &amp;&amp; S="$(awk '/^## SC#4 grouped-update coverage gap \(D-06\)/{f=1;next} /^## /{f=0} f' "$F")" &amp;&amp; printf '%s\n' "$S" | grep -qi 'literal reading' &amp;&amp; printf '%s\n' "$S" | grep -qi 'amended reading' &amp;&amp; printf '%s\n' "$S" | grep -qi 'not a regression' &amp;&amp; printf '%s\n' "$S" | grep -q 'dependency_file_not_resolvable' &amp;&amp; grep -q '^## Wave gate and live re-assertion' "$F" &amp;&amp; git diff --quiet "$(kx "$F" BASE_67_05)" HEAD -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md .planning/todos typsphinx .github pyproject.toml uv.lock</automated>
    <fails_when>Non-zero exit, or `HALT heading in <file>` printed. Each of these makes it fail:
- a disposition key not as required, in any of the three evidence files
- `BASE_67_05` or `A6701_STATUS` missing
- #138 not MERGED at `MERGE_SHA_138`, or #123 or #128 not CLOSED
- either AMENDED citation absent from `ROADMAP.md` or `PROJECT.md`
- the live `dependabot/uv/sphinx-typst-stack` count differing from `UV_GROUP_PR_COUNT`
- #128's title not naming the group, or #138's title naming one
- the run log no longer containing `dependency_file_not_resolvable`
- the SC#4 section lacking the literal reading, the amended reading, the not-a-regression statement or the error name
- the wave-gate section missing
- any change to `REQUIREMENTS.md`, `ROADMAP.md`, `PROJECT.md`, `.planning/todos`, `typsphinx`, `.github`, `pyproject.toml` or `uv.lock` since `BASE_67_05`</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Wave gate and live re-assertion` quotes the disposition keys from all three files and the live states of #138 (MERGED), #123 and #128 (CLOSED, one approved owner comment each), with `A6701_STATUS`.
    - `## SC#4 grouped-update coverage gap (D-06)` carries the quoted measurements, a **literal reading** paragraph and an **amended reading** paragraph kept apart, and the statement that a future grouped resolution failure is an uncovered case, not a regression.
    - The AMENDED blocks are cited by `grep -n`, and `ROADMAP.md`, `PROJECT.md` and `REQUIREMENTS.md` are unchanged.
  </acceptance_criteria>
  <done>SC#4's coverage gap is written down in both readings, grounded in live measurements, and all three dispositions are re-asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Ordering, deferred-PR and todo checks, then the requirement closure table for DEP-02 and DEP-05</name>
  <precondition>Task 1's sections are recorded in `67-CLOSURE-EVIDENCE.md` with no HALT heading.</precondition>
  <reversibility rating="reversible">Evidence-only.</reversibility>
  <files>.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CLOSURE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CLOSURE-EVIDENCE.md (Task 1)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md (§ Requirement closure: the table shape to mirror)
    - .planning/todos/pending/2026-08-16-dependabot-prs-die-on-uv-lock-locked-mismatch.md (frontmatter `resolves_phase`)
    - .planning/REQUIREMENTS.md (DEP-02 and DEP-05 wording, read only)
  </read_first>
  <action>
1. **`## Ordering (constraint 4)`.** Quote the timestamps, and state each comparison as a string comparison of UTC `YYYY-MM-DDTHH:MM:SSZ` values:
   - `DEP02_PROOF_AT` is earlier than `DECIDED_AT_138` and earlier than `DECIDED_AT_128`: the proof preceded every merit decision (D-02).
   - `MERGED_AT_138` is earlier than `CLOSED_AT_123`: the merge preceded the close (D-03).
   - SC#2's branch is `can` (`SC2_BRANCH`). Both stale PRs stayed open until the proof was recorded, and no close in this phase was mechanical (`MECHANICAL_CLOSE = none`).
   - If any comparison fails, write `## HALT: ordering violated` and stop.

2. **`## Deferred PRs untouched`.** For `n` in 139, 140, 141 and 142:
   - record `gh pr view "$n" --json number,state,headRefName,headRefOid,updatedAt` verbatim
   - record the count of comments authored by `gh api user --jq .login`, and the count of timeline events from `gh api --paginate "repos/YuSabo90002/typsphinx/issues/$n/events"` whose actor is that login; both must be 0
   - state that any change of state on them (a dependabot rebase or supersession after #138's merge) is recorded, not acted on (CONTEXT `<deferred>`)

   Also record `gh pr list --state open --author app/dependabot --json number,title,headRefName,createdAt` verbatim as the dependabot state at close.

3. **`## Folded todo`.**
   - Record `test -f .planning/todos/pending/2026-08-16-dependabot-prs-die-on-uv-lock-locked-mismatch.md; echo "exit:$?"`, and the frontmatter `resolves_phase` value from `awk '/^---/{c++;next} c==1 && /^resolves_phase:/{print $2;exit} c==2{exit}'` over that file, which must print `67`.
   - Quote the todo's two asks: "prove it on a real dependabot PR", and "decide what to do with the two open PRs". Map them to 67-01 and to 67-02, 67-03 and 67-04.
   - Write `TODO_DISPOSITION = moved-at-phase-completion-by-execute-phase`, explaining that execute-phase's `close_phase_todos` step moves every pending todo whose `resolves_phase` matches the completed phase, so this plan neither moves nor edits it.

4. **`## Requirement closure`.** A table with columns Requirement | Status | Deciding sections, where each status reads MET or NOT MET:
   - **DEP-02 is MET when all of these hold:**
     - `DEP02_VERDICT = MET` (`67-PROOF-EVIDENCE.md` § D-01 proof run, § Job census, § Check runs on PROOF_SHA, § Per-step reads, § DEP-02 verdict)
     - `A6701_STATUS` is recorded
   - **DEP-05 is MET when all of these hold:**
     - #138 merged on its merits and #123 closed as superseded (`67-RUFF-EVIDENCE.md` § SC#3 merits for #138, § Main after the merge, § #123 disposition (D-03))
     - #128 closed with its reason (`67-DOCUTILS-EVIDENCE.md` § D-05 PyPI re-measurement, § Pre-close PyPI re-measurement, § #128 disposition (D-05))
     - the ordering holds (§ Ordering (constraint 4))
     - the grouped-update gap is recorded (§ SC#4 grouped-update coverage gap (D-06))

   Beneath the table:
   - map ROADMAP Phase 67 SC#1 to SC#4 to their sections, reporting SC#4's literal and amended readings as two separate lines
   - state that `REQUIREMENTS.md` is unedited by every Phase 67 plan, and that its DEP-02/DEP-05 checkboxes flip at phase completion
   - if any row reads NOT MET, stop at a blocking-human checkpoint naming it

5. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128 &amp;&amp; F="$D/67-CLOSURE-EVIDENCE.md" &amp;&amp; FP="$D/67-PROOF-EVIDENCE.md" &amp;&amp; FR="$D/67-RUFF-EVIDENCE.md" &amp;&amp; FD="$D/67-DOCUTILS-EVIDENCE.md" &amp;&amp; T=.planning/todos/pending/2026-08-16-dependabot-prs-die-on-uv-lock-locked-mismatch.md &amp;&amp; kx() { sed -n "s/^$2 = //p" "$1" | head -n 1; } &amp;&amp; PA="$(kx "$FP" DEP02_PROOF_AT)" &amp;&amp; [ -n "$PA" ] &amp;&amp; [[ "$PA" &lt; "$(kx "$FR" DECIDED_AT_138)" ]] &amp;&amp; [[ "$PA" &lt; "$(kx "$FD" DECIDED_AT_128)" ]] &amp;&amp; [[ "$(kx "$FR" MERGED_AT_138)" &lt; "$(kx "$FR" CLOSED_AT_123)" ]] &amp;&amp; for r in DEP-02 DEP-05; do grep -qE "^\| *$r *\|.*MET" "$F" || { echo "no MET row: $r"; exit 1; }; ! grep -qE "^\| *$r *\|.*NOT MET" "$F" || { echo "NOT MET row: $r"; exit 1; }; done &amp;&amp; test -f "$T" &amp;&amp; [ "$(awk '/^---/{c++;next} c==1 &amp;&amp; /^resolves_phase:/{print $2;exit} c==2{exit}' "$T")" = 67 ] &amp;&amp; [ "$(kx "$F" TODO_DISPOSITION)" = moved-at-phase-completion-by-execute-phase ] &amp;&amp; ME="$(gh api user --jq .login)" &amp;&amp; [ -n "$ME" ] &amp;&amp; [ "$(gh pr view 123 --json comments -q ".comments[] | select(.author.login == \"$ME\") | .body")" = "$(kx "$FR" APPROVED_COMMENT_123)" ] &amp;&amp; [ "$(gh pr view 128 --json comments -q ".comments[] | select(.author.login == \"$ME\") | .body")" = "$(kx "$FD" APPROVED_COMMENT_128)" ] &amp;&amp; for n in 139 140 141 142; do [ "$(gh pr view "$n" --json comments -q "[.comments[] | select(.author.login == \"$ME\")] | length")" = 0 ] &amp;&amp; [ "$(gh api --paginate "repos/YuSabo90002/typsphinx/issues/$n/events" --jq ".[] | select(.actor.login == \"$ME\") | .event" | wc -l)" = 0 ] || { echo "owner activity on deferred #$n"; exit 1; }; done &amp;&amp; for s in '## Ordering (constraint 4)' '## Deferred PRs untouched' '## Folded todo' '## Requirement closure'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; git diff --quiet "$(kx "$F" BASE_67_05)" HEAD -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md .planning/todos typsphinx .github pyproject.toml uv.lock &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `no MET row: <req>`, `NOT MET row: <req>`, `owner activity on deferred #<n>` or `missing section: <name>` printed. Each of these makes it fail:
- `DEP02_PROOF_AT` not earlier than both decisions, or `MERGED_AT_138` not earlier than `CLOSED_AT_123`
- a closure row for DEP-02 or DEP-05 absent or reading NOT MET
- the todo missing from `pending/` or its `resolves_phase` not `67`
- `TODO_DISPOSITION` not the stated value
- the owner comment on #123 or #128 differing from its approved text
- any owner comment or event on #139–#142
- a section missing
- a protected or planning-tracking path changed since `BASE_67_05`
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Ordering (constraint 4)` shows the proof before both decisions and the merge before the #123 close, with `SC2_BRANCH = can` and `MECHANICAL_CLOSE = none` restated.
    - `## Deferred PRs untouched` shows zero owner comments and zero owner events on #139–#142, and records the dependabot PR list at close.
    - `## Folded todo` shows the todo still in `pending/` with `resolves_phase: 67`, and `TODO_DISPOSITION = moved-at-phase-completion-by-execute-phase`.
    - `## Requirement closure` reads MET for DEP-02 and DEP-05 with named deciding sections, and maps SC#1–SC#4 with SC#4's two readings separate.
    - No requirement, roadmap, project or todo file was edited.
  </acceptance_criteria>
  <done>DEP-02 and DEP-05 are closed on recorded, re-asserted evidence; the grouped-update gap is written down; and the todo is left for the phase-completion step that owns it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence files → closure verdict | three executors' records are summarized into one verdict |
| GitHub state → evidence | live PR, event and log reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-18 | Repudiation | a MET verdict resting on an unrechecked or HALTed record | medium | mitigate | Every disposition is re-asserted live: PR state, owner comment body and merge commit. The verify requires no HALT heading in all four files, and the ordering is checked from recorded timestamps. |
| T-67-19 | Tampering | a closure plan quietly editing requirement, roadmap, project or todo files | medium | mitigate | Prohibitions forbid it, and the verify runs `git diff --quiet BASE_67_05 HEAD` over exactly those paths. |
| T-67-20 | Repudiation | the grouped-update gap reported as covered | low | mitigate | The SC#4 section must carry the literal reading, the amended reading and the not-a-regression statement, and the verify greps for all three. |
| T-67-SC | Tampering | `uv sync --extra dev` during provisioning | low | accept | installs only the pins already in `uv.lock`; no new package |
</threat_model>

<verification>
- All three dispositions are complete and re-asserted live, with the ordering proven from timestamps.
- SC#4's coverage gap is written in both readings, and the AMENDED blocks are cited unedited.
- DEP-02 and DEP-05 read MET, the todo is left in place for `close_phase_todos`, #139–#142 are untouched, and no requirement file was edited.
</verification>

<success_criteria>
The phase goal holds end to end:
- The fix is proven on a real dependabot PR (DEP-02).
- Only then were both stale bumps judged on their merits and disposed of, with the judgements written down (DEP-05, D-03–D-05).
- The grouped-update coverage gap is recorded rather than passed over, in its literal and amended readings (D-06).
</success_criteria>

<output>
Create `.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-05-SUMMARY.md` when done.
- Its frontmatter `requirements-completed:` lists only the requirements whose closure row reads MET: `[DEP-02, DEP-05]` when both do. DEP-02 is also listed by 67-01; the repeat is the closure record.
- Its first section states `UV_GROUP_PR_COUNT`, `A6701_STATUS`, `TODO_DISPOSITION` and both closure verdicts.
- If a HALT heading was written, that section says so first.
</output>
