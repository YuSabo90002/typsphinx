---
phase: 11-issue-114-fatal-fixes-graceful-degrade-net
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - tests/test_pdf_render_gate.py
  - tests/fixtures/figure_length_render_gate/conf.py
  - tests/fixtures/figure_length_render_gate/index.rst
  - tests/fixtures/figure_target_caption_render_gate/conf.py
  - tests/fixtures/figure_target_caption_render_gate/index.rst
  - tests/fixtures/graphviz_degrade_render_gate/conf.py
  - tests/fixtures/graphviz_degrade_render_gate/index.rst
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "sphinx-build then typst.compile() then pypdf on the figure-length fixture produces a valid PDF with no TypstCompilationError; the generated .typ carries a converted pt width and no raw px unit (D-04)"
    - "The figure-target-caption fixture (external and internal :target:, caption with underscore/asterisk/backtick/brackets) compiles and its caption sentinel appears exactly once in extracted PDF text with no LEAK_SIGNATURES present (D-04)"
    - "The graphviz fixture (graphviz + inheritance_diagram) compiles; the placeholder wording is present in the PDF text; raw diagram-source tokens are absent; build stderr shows one degrade warning per node (D-04)"
    - "The extended gate fails loudly on any TypstCompilationError and runs (does not skip) when typst-py and pypdf are present, as they are in CI's cov job (D-04)"
  artifacts:
    - "tests/fixtures/figure_length_render_gate/{conf.py,index.rst}"
    - "tests/fixtures/figure_target_caption_render_gate/{conf.py,index.rst}"
    - "tests/fixtures/graphviz_degrade_render_gate/{conf.py,index.rst}"
    - "tests/test_pdf_render_gate.py::TestFigureLengthRenderGate"
    - "tests/test_pdf_render_gate.py::TestFigureCaptionRenderGate"
    - "tests/test_pdf_render_gate.py::TestGraphvizDegradeRenderGate"
  key_links:
    - "each new test class reuses LEAK_SIGNATURES and the TYPST_AVAILABLE/PYPDF_AVAILABLE skipif guard and the sys.executable -m sphinx subprocess convention from the existing TestAdmonitionPdfRenderGate"
    - "graphviz_degrade_render_gate/conf.py adds sphinx.ext.graphviz and sphinx.ext.inheritance_diagram to extensions"
---

<objective>
Stand up GATE-01 (per D-04): extend `tests/test_pdf_render_gate.py` with three new fixture projects
and three new real-compile test classes that drive `sphinx-build -> typst.compile() -> pypdf`
text-extraction against the FIG-01, FIG-02, and DEG-01/02 fixes landed in Plans 11-01 and 11-02. This
is the standing real-compile acceptance gate the rest of milestone v0.6.0 (Phases 12-14) extends —
string-agreement unit tests alone are insufficient (pitfall #9): a translator can emit plausible Typst
that still fails a real compile.

Purpose: prove, through an actual `typst.compile()`, that px converts and no raw unit leaks, that a
targeted+captioned figure's caption appears exactly once with no juxtaposition leak, and that graphviz
/ inheritance_diagram degrade to a visible placeholder without aborting or leaking diagram source.

Output: three fixture directories (each a minimal Sphinx project cloned from
`admonition_render_gate`) and three `slow`-marked test classes (`TestFigureLengthRenderGate`,
`TestFigureCaptionRenderGate`, `TestGraphvizDegradeRenderGate`).

**Requirements coverage:** GATE-01 (per D-04).

**Dependency note:** `depends_on: [11-01, 11-02]` is a genuine logical dependency — the fixtures only
compile once the FIG-01/FIG-02/DEG fixes exist; wave 3 follows both production waves.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-issue-114-fatal-fixes-graceful-degrade-net/11-CONTEXT.md
@.planning/phases/11-issue-114-fatal-fixes-graceful-degrade-net/11-RESEARCH.md
@.planning/phases/11-issue-114-fatal-fixes-graceful-degrade-net/11-PATTERNS.md
@.planning/phases/11-issue-114-fatal-fixes-graceful-degrade-net/11-VALIDATION.md
@tests/test_pdf_render_gate.py
@tests/fixtures/admonition_render_gate/conf.py
@tests/fixtures/admonition_render_gate/index.rst
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three render-gate fixture projects (D-04)</name>
  <files>tests/fixtures/figure_length_render_gate/conf.py, tests/fixtures/figure_length_render_gate/index.rst, tests/fixtures/figure_target_caption_render_gate/conf.py, tests/fixtures/figure_target_caption_render_gate/index.rst, tests/fixtures/graphviz_degrade_render_gate/conf.py, tests/fixtures/graphviz_degrade_render_gate/index.rst</files>
  <read_first>
    - `tests/fixtures/admonition_render_gate/conf.py` and `tests/fixtures/admonition_render_gate/index.rst` — the minimal fixture shape to clone (project/author/release, `extensions = ["typsphinx"]`, a `typst_documents` master-doc tuple, and a flat "one section per case" index.rst).
    - `11-PATTERNS.md` section "New fixture directories — clone the admonition_render_gate shape" (per-fixture case lists and the graphviz conf extension additions).
    - `11-RESEARCH.md` "Recommended Project Structure" + Wave 0 Gaps (fixture case coverage; note `:height:` rejects `%` in docutils, so use `%` on width only; the graphviz `inheritance-diagram::` directive needs an importable target class).
    - `11-CONTEXT.md` <specifics> (the enumerated length and caption-stress cases) and D-04.
  </read_first>
  <action>
    <!-- planner-discipline-allow: 200px -->
    <!-- planner-discipline-allow: digraph -->
    Clone the `admonition_render_gate` `conf.py`/`index.rst` shape into three new directories.
    (1) `figure_length_render_gate/`: conf.py with `extensions = ["typsphinx"]` and a `typst_documents`
    master tuple for "index"; index.rst with one figure/image block per length case enumerated in
    11-CONTEXT.md <specifics> — 200px, 50% (on width only, since docutils rejects percentage height),
    3em, a bare unitless number, 2in, and at least one unconvertible unit such as 1ex (the warn+drop
    case). Provide a real image file the fixtures can point at (reuse or copy a tiny PNG already present
    under tests/, or add a minimal one) so `image(...)` resolves. (2) `figure_target_caption_render_gate/`:
    conf.py same as above; index.rst with two figures — one with an external-URL `:target:` and one with
    an internal same-document `:target:` label reference (add a labeled section for the internal target
    to point at) — each carrying a caption that embeds the markup-special characters underscore,
    asterisk, backtick, open-bracket and close-bracket, plus a distinctive single-word sentinel token
    (choose an unlikely word) so the test can count its occurrences exactly. (3)
    `graphviz_degrade_render_gate/`: conf.py that ADDS "sphinx.ext.graphviz" and
    "sphinx.ext.inheritance_diagram" to the extensions list (both bundled with Sphinx core, no new
    dependency); index.rst with one graphviz directive whose DOT body is a minimal two-node directed
    graph and one inheritance-diagram directive targeting a small importable class defined inside the
    fixture (define the class in conf.py or a sibling module on the fixture's path, and ensure the
    fixture dir is importable, so the directive resolves without importing anything from typsphinx
    itself). Keep every fixture self-contained and minimal.
  </action>
  <verify>
    <automated>for f in figure_length_render_gate figure_target_caption_render_gate graphviz_degrade_render_gate; do python -m sphinx -b typst tests/fixtures/$f /tmp/gate_$f || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All three fixture directories exist with a `conf.py` and `index.rst`; `graphviz_degrade_render_gate/conf.py` lists `sphinx.ext.graphviz` and `sphinx.ext.inheritance_diagram` in `extensions`.
    - `python -m sphinx -b typst tests/fixtures/<each> <tmpout>` exits 0 for all three and emits an `index.typ`.
    - The figure-length fixture covers all six enumerated length cases; the caption fixture has an external and an internal `:target:` figure each with the markup-special-character caption + sentinel token; the graphviz fixture has a graphviz directive and an inheritance-diagram directive that resolves.
  </acceptance_criteria>
  <done>Three minimal, self-contained Sphinx fixture projects exist and each builds to a .typ under the (already-landed) FIG/DEG fixes without a sphinx-build error.</done>
</task>

<task type="auto">
  <name>Task 2: Add the three real-compile render-gate test classes (GATE-01, D-04)</name>
  <files>tests/test_pdf_render_gate.py</files>
  <read_first>
    - `tests/test_pdf_render_gate.py` in full — the imports + availability guards (lines ~1-40), `LEAK_SIGNATURES` (line ~36), the `fixtures_dir`/`admonition_render_gate_dir`/`temp_build_dir` fixtures (lines ~43-58), and the whole `TestAdmonitionPdfRenderGate` class (lines ~61-134) including the in-code comment (lines ~80-92) explaining the `sys.executable -m sphinx` subprocess convention and the PATH-shadowing hazard it avoids.
    - `11-PATTERNS.md` section "tests/test_pdf_render_gate.py — extend with three new test classes" (the exact skeleton to clone per class and what each varies).
    - `11-RESEARCH.md` "Validation Architecture" (Phase Requirements -> Test Map, the loud-failure-on-TypstCompilationError requirement) and the note that `@pytest.mark.slow` is newly applied in this repo (registered but unused; adding it is sanctioned, and does not exclude the test from any CI job).
    - `11-CONTEXT.md` D-04.
  </read_first>
  <action>
    <!-- planner-discipline-allow: 200px -->
    <!-- planner-discipline-allow: digraph -->
    Add three new fixture-path fixtures (`figure_length_render_gate_dir`,
    `figure_target_caption_render_gate_dir`, `graphviz_degrade_render_gate_dir`), each returning
    `fixtures_dir / "<name>"`, mirroring the existing `admonition_render_gate_dir` fixture. Add three
    new test classes, each decorated with the existing `@pytest.mark.skipif(not (TYPST_AVAILABLE and
    PYPDF_AVAILABLE), ...)` guard AND `@pytest.mark.slow`, each cloning the `TestAdmonitionPdfRenderGate`
    method skeleton: run `subprocess.run([sys.executable, "-m", "sphinx", "-b", "typst",
    <fixture_dir>, <build_dir>], capture_output=True, text=True)`, assert returncode 0, assert
    `index.typ` exists, call `typst.compile(str(index_typ), output=str(pdf))` (any TypstCompilationError
    must propagate and fail the test loudly — do not wrap it in try/except), assert the PDF exists / is
    non-empty / starts with the `%PDF` magic bytes, then extract text with `pypdf.PdfReader`. Vary the
    assertions per class: `TestFigureLengthRenderGate` reads the generated `index.typ` and asserts the
    converted pt value is present for the px case and that the raw pre-conversion px token is absent
    (a numeric-value-plus-px string must not appear), and that the compile succeeded (covering the
    warn+drop unknown-unit case implicitly — a leaked raw unit would have aborted the compile).
    `TestFigureCaptionRenderGate` asserts `full_text.count(<sentinel>) == 1` (caption appears exactly
    once — guards the double-emission), asserts none of the existing `LEAK_SIGNATURES` tokens appear in
    the extracted text, and covers both the external and internal `:target:` figures via the one
    fixture project. `TestGraphvizDegradeRenderGate` asserts the placeholder wording (e.g. the word
    "omitted") is present in the extracted text, asserts the raw diagram-source tokens are ABSENT from
    the extracted text (the DOT keyword and the edge arrow), and asserts the build's captured stderr
    contains the degrade warning for the graphviz node and for the inheritance-diagram node exactly
    once each (subprocess stderr is captured, so count the warning-message substring). Reuse the shared
    fixtures, `LEAK_SIGNATURES`, and the subprocess convention verbatim; do not shell out to a bare
    `sphinx-build`.
  </action>
  <verify>
    <automated>pytest tests/test_pdf_render_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_pdf_render_gate.py -q` exits 0 with the existing admonition class plus the three new classes all passing (not skipped, since typst-py and pypdf are installed).
    - `TestFigureLengthRenderGate` proves the px case compiles with a converted pt value in the .typ and no raw px unit; `TestFigureCaptionRenderGate` proves the caption sentinel appears exactly once and no LEAK_SIGNATURES leak; `TestGraphvizDegradeRenderGate` proves the placeholder text is present, raw diagram source is absent, and exactly one degrade warning fires per node.
    - Any `TypstCompilationError` propagates and fails the test (no try/except swallow).
    - `pytest -q` (full suite including the new slow gate) exits 0; `ruff check .` and `black --check tests/` pass.
  </acceptance_criteria>
  <done>The standing GATE-01 real-compile acceptance gate exercises all three Phase 11 fixes through sphinx-build -> typst.compile() -> pypdf and fails loudly on any compile error — the pattern Phases 12-14 will extend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated Typst -> typst.compile() -> PDF -> pypdf text | the render gate is the verification instrument that samples this whole boundary for source/unit/diagram leaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Tampering / Denial of Service (regression detection) | render-gate test harness | high | mitigate | Real `typst.compile()` fails loudly on any TypstCompilationError; negative-control assertions (LEAK_SIGNATURES, raw px, raw diagram source) prove no invalid source reaches the compiled PDF (D-04) |
| T-11-06 | Information Disclosure | graphviz fixture / gate | medium | mitigate | Gate asserts raw diagram-source tokens are absent from extracted PDF text, proving DEG-01/02's SkipNode prevents source leakage |
| T-11-SC | Tampering | pip installs | low | accept | Zero new packages introduced this phase; `typst`/`pypdf`/`sphinx.ext.*` are already project dependencies. Supply-chain surface unchanged |
</threat_model>

<verification>
- `pytest tests/test_pdf_render_gate.py -q` green with four classes (existing admonition + three new) all running (not skipped).
- `pytest -q` full suite green including the new slow render gate.
- Each fixture builds via `python -m sphinx -b typst`.
- `ruff check .`, `black --check tests/` clean.
</verification>

<success_criteria>
- Three fixture projects and three slow-marked real-compile test classes exist and pass.
- The gate proves FIG-01 (px->pt, no raw unit), FIG-02 (caption exactly once, no leak, internal+external :target:), and DEG-01/02 (placeholder present, no diagram-source leak, one warning per node) through an actual compile.
- The gate fails loudly on any TypstCompilationError and runs unconditionally where typst-py/pypdf are present.
</success_criteria>

<output>
Create `.planning/phases/11-issue-114-fatal-fixes-graceful-degrade-net/11-03-SUMMARY.md` when done.
</output>
