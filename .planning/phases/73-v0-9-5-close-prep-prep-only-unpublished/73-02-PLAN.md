---
phase: 73-v0-9-5-close-prep-prep-only-unpublished
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/COVERAGE.md
autonomous: true
requirements: [REL-14]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'D-08 / SC#4 baseline at phase head: 73-CLOSEOUT-GUARD.md records PHASE_BASE_SHA, REQ_SHA256_BASE, REQ_LINES_BASE, REL14_HITS_BASE, REQ_LAST_COMMIT and GUARD_AT as bare key lines before this plan commits anything, plus the verbatim `grep -n REL-14 .planning/REQUIREMENTS.md` output with each hit classified as state-bearing or informational. The recorded digest equals the live `sha256sum`, and PHASE_BASE_SHA carries no plan-scoped commit subject.'
    - 'D-08: PHASE_BASE_SHA''s product tree equals Phase 72''s pushed tip (PUSHED_SHA in 72-CI-EVIDENCE.md) outside .planning/, and the last commit that touched REQUIREMENTS.md is an ancestor of PHASE_BASE_SHA.'
    - 'D-08: the guard names both entry points at which a release requirement has flipped, `phase.complete` reached from `/gsd-execute-phase` and the inline transition of `/gsd-verify-work`. It instructs a scratch backup of REQUIREMENTS.md, ROADMAP.md and STATE.md before either runs, prescribes `git checkout -- .planning/REQUIREMENTS.md` as the only response to a divergence (reverted and reported, never committed), and warns that the same tooling has also rewritten STATE.md''s `current_phase_name` field and ROADMAP.md''s Status cell.'
    - 'REL-14 is measurably unmet at phase head: its checkbox reads `- [ ] **REL-14**` and its Traceability row reads `| REL-14 | Phase 73 | Pending`. This plan reads REQUIREMENTS.md and never edits it (D-08).'
    - 'D-13 / SC#1, observation 1 of 2, every remote negative paired with a positive control against v0.9.2: pyproject.toml:7 reads `version = "0.9.2"`. No v0.9.3, v0.9.4 or v0.9.5 tag exists locally or on origin, while `refs/tags/v0.9.2` appears once in the same unfiltered ls-remote listing. PyPI answers 200 for 0.9.2 and 404 for 0.9.3, 0.9.4 and 0.9.5. The latest GitHub release is v0.9.2 and none is tagged v0.9.3, v0.9.4 or v0.9.5; the release.yml run RELEASE_POSITIVE_CONTROL on v0.9.2 is listed and no run since 2026-08-31 or on an unpublished ref exists; no pull request has the milestone branch as its head, while the all-state pull-request listing is non-empty.'
    - 'D-13 milestone fences at phase head: MILESTONE_BASE (the merge-base of HEAD and origin/main) equals 098a8ff64cf008822eef9dc69f75102ded3f7bc1; since it no commit changes a `version = ` line in pyproject.toml; `git diff --stat 098a8ff6..HEAD -- typsphinx/ .github/workflows/` is empty. The positive controls: the same pathspec lists tracked files at HEAD (PATHSPEC_TRACKED_FILES at least 1), and the widened diff from the same base lists exactly Phase 72''s five product files, equal to PRODUCT_DIFF_FILES in 72-GATES-EVIDENCE.md.'
    - 'Edge (empty): no negative probe in 73-SC1-INVARIANTS.md is recorded without a positive control from the same source, so an empty result is distinguishable from a broken probe or an unreachable remote.'
    - 'COVERAGE.md opens with `No external API integration: ` and a reason, records the detector result verbatim, and `check api-coverage.verify-pre` reports `"passed": true` for this phase directory.'
    - 'REL-14 is cited for coverage only; this plan SUMMARY declares requirements-completed: [] (D-08).'
  artifacts:
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md
      provides: 'phase-head REQUIREMENTS.md digest, line count, REL-14 hits, PHASE_BASE_SHA, and the close-time and post-transition procedures'
      contains: 'PHASE_BASE_SHA = '
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md
      provides: 'SC#1 observation 1 of 2 with positive controls, and the D-13 milestone fences at phase head'
      contains: 'OBS1_AT = '
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/COVERAGE.md
      provides: 'reasoned no-external-API declaration for the seal-time gate'
      contains: 'No external API integration: '
  key_links:
    - from: '.planning/REQUIREMENTS.md at phase head'
      to: '73-07 close-time re-verification and the operator third observation reproduced in 73-HANDOFF.md'
      via: 'REQ_SHA256_BASE compared byte for byte; a flip leaves wc -l unchanged, so the digest is the primary probe'
      pattern: 'REQ_SHA256_BASE = '
    - from: 'PHASE_BASE_SHA'
      to: '73-03 product-tree delta and 73-06 phase-scoped diff'
      via: 'KEY = value line read with sed'
      pattern: 'PHASE_BASE_SHA = '
    - from: 'MILESTONE_BASE and RELEASE_POSITIVE_CONTROL'
      to: '73-03, 73-04, 73-05 and 73-06 checks'
      via: 'KEY = value lines read with sed'
      pattern: 'MILESTONE_BASE = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action. Every command in this plan is a read; it creates no tag, release, pull request, push or workflow run, and touches no Dependabot pull request.'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record REL-14 as satisfied: no checkbox flip, no Traceability status change, no prose claiming the merge happened. REQUIREMENTS.md is read and quoted, never edited (D-08).'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT record a negative probe without its positive control, or copy a digest, SHA or count from RESEARCH, CONTEXT or this plan as a measurement.'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Record, at phase head and before any other plan's commit reaches the branch, the anchors this phase's
fence proof needs:
- the `.planning/REQUIREMENTS.md` checksum guard for REL-14 (D-08), with both operator procedures
- the phase base SHA
- the first of two separated SC#1 observations that nothing is published, each remote negative
  paired with a positive control against `v0.9.2` (D-13), for all three unclaimed version numbers
- the D-13 milestone fences at phase head: `git diff --stat 098a8ff6..HEAD -- typsphinx/ .github/workflows/`
  empty, with a pathspec control and a widened-diff control
- the reasoned external-API declaration the seal-time gate reads

Purpose: a checksum recorded after another plan has run cannot prove the file was untouched at phase
head, and a fence observation taken beside its sibling is one observation, not two. This plan is
deliberately thin and early. It runs in parallel with 73-01, which touches only `CHANGELOG.md` and its
own evidence file.

Output: `73-CLOSEOUT-GUARD.md`, `73-SC1-INVARIANTS.md` (observation 1 of 2) and `COVERAGE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-RESEARCH.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Before running any command, provision this worktree with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`, then
prefix project tooling with `uv run` (`CLAUDE.md` § "Worktree-isolated execution"). This plan runs no
test suite, but the rule is unconditional, and the interpreter matches every other plan of the phase.

**Evidence key lines.** Every `KEY = value` line carries the bare value only, with any explanation on
the next line. The verify commands read values with `sed -n "s/^KEY = //p"` and compare them
exactly, and they fail on any key line that contains an opening parenthesis.

**Scratch files.** Parallel executors share the session scratchpad. Put every helper file and
captured output under your own `mktemp -d` directory, recorded as `SCRATCH_73_02`, with a `p7302_`
prefix.

**Locale.** The host runs `LANG=ja_JP.UTF-8`. Prefix `LANG=C LC_ALL=C` to any command whose English
message you quote for comparison.

**GSD tooling is not tracked.** `.claude/gsd-core/` is absent from a worktree. Call it by absolute
path: `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs` and
`node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/lib/api-coverage.cjs`. Do not call any GSD
state-writing verb or the GSD commit helper; commit with plain `git commit`.
</worktree_provisioning>

<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: v0.9.4 -->
<!-- planner-discipline-allow: v0.9.5 -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[345] -->
<!-- planner-discipline-allow: capability -->
<!-- planner-discipline-allow: ^\| *capability *\| -->
<!-- planner-discipline-allow: \(73-0[0-9]\) -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- plan-criteria-allow: R4 - every git call in these verifies is captured into a variable before any pipe; the match is the scanner losing quote state at the escaped double quotes inside a --jq filter string -->
The version tags are named because every probe asserts their absence. The word `capability` is named
because the declaration must not be a capability matrix. `## HALT` is written only on a failure path.

## Planning-time census (2026-09-14, main checkout at `9d276727`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `.planning/REQUIREMENTS.md` | 70 lines; digest measured at execution | `wc -l` |
| `grep -n 'REL-14'` hits | 4. State-bearing: `:22` checkbox (`- [ ] **REL-14**: Close prep only, unpublished. …`) and `:57` Traceability row (`\| REL-14 \| Phase 73 \| Pending \|`). Informational: `:62` (the "Mapped to phases" coverage line) and `:65` (the "REL-14 is mapped to Phase 73 for coverage only" line). REL-14 has no AMENDED block. | `grep -n` |
| Milestone base | `git merge-base HEAD origin/main` = `098a8ff64cf008822eef9dc69f75102ded3f7bc1` = `origin/main`; the branch lacks no `main` commit | `git merge-base`, `git rev-parse` |
| Milestone diff | `git diff --stat 098a8ff6..HEAD -- typsphinx/ .github/workflows/` empty; outside `.planning/`, exactly `CLAUDE.md`, `README.md`, `docs/source/contributing.rst`, `docs/source/index.rst`, `tox.ini` (= `PRODUCT_DIFF_FILES` in `72-GATES-EVIDENCE.md`) | `git diff` |
| Phase 72 pushed tip | `PUSHED_SHA = 0b2595df21399363f50e5e1a55935f470e0df00d` in `72-CI-EVIDENCE.md` = `origin/gsd/v0.9.5-docs-link-check-and-navigation`; the local branch is ahead by `.planning/` commits only | `git rev-parse` |
| Local tags | `v0.9.0`, `v0.9.2`; no `v0.9.3`, `v0.9.4` or `v0.9.5` | `git tag -l 'v0.9*'` |
| Remote tags | `refs/tags/v0.9.0` and `refs/tags/v0.9.2` (each with a `^{}` peel line); none for `v0.9.3`, `v0.9.4` or `v0.9.5` | `git ls-remote --tags origin` |
| PyPI | 200 for `0.9.2`; 404 for `0.9.3`, `0.9.4`, `0.9.5` | `curl -w '%{http_code}'` |
| GitHub releases | `v0.9.2` is Latest (published 2026-08-30) | `gh release list` |
| `release.yml` runs | newest is `33318905691` (2026-08-30, `headBranch` `v0.9.2`, `push`, success); none since | `gh run list --workflow=release.yml` |
| PRs from the milestone branch | 0 | `gh pr list --head … --state all` |
| Flip history | the release-requirement flip landed at 8 of the 9 prior release-prep closes and did not fire at Phase 71; Phase 71's third observation also recorded `phase.complete` deleting STATE.md's `current_phase_name` and ROADMAP.md Status-cell padding changes | ROADMAP constraint 10; `71-CLOSEOUT-GUARD.md` § "Third observation" |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: record the phase-head REQUIREMENTS.md closeout guard for REL-14, PHASE_BASE_SHA tied to Phase 72's pushed tip, and both operator procedures</name>
  <reversibility rating="reversible">One new planning document; no product-tree or requirement-state change.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the REL-14 checkbox line, the Traceability row and the two coverage lines, read so the quotes are byte-accurate)
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CLOSEOUT-GUARD.md (the structure to mirror, including § "For the operator running phase.complete" and § "Third observation")
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md (the `PUSHED_SHA` key line)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-08
    - .planning/ROADMAP.md binding constraint 10 of the v0.9.5 section
  </read_first>
  <action>
Create `73-CLOSEOUT-GUARD.md` with the heading `# Phase 73 — REL-14 Checkbox-Flip Closeout Guard` and a one-line statement: this file changes no requirement state, and `.planning/REQUIREMENTS.md` is read and quoted, never edited (D-08: no Phase 73 plan writes it).

1. **`## Baseline`** (D-08). Record these before this plan's first commit, each command with its output, then its key line:
   - `git rev-parse HEAD`, key `PHASE_BASE_SHA`
   - `git log -1 --format='%H %s'`, where the subject must carry no plan scope of the form `(73-0N)`: no plan commit has landed yet
   - the Phase 72 tie: read `PUSHED_SHA` from `.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md`, then record `git merge-base --is-ancestor <that SHA> "$PHASE_BASE_SHA"; echo "exit:$?"` (must print `exit:0`) and `git diff --name-only <that SHA> "$PHASE_BASE_SHA" -- . ':(exclude).planning'` (must print nothing). Key `PHASE_BASE_PRODUCT_EQUALS_P72_TIP = yes`: the product tree this phase starts from is the one Phase 72's CI run tested.
   - `git log -1 --format=%H -- .planning/REQUIREMENTS.md`, key `REQ_LAST_COMMIT`, then `git merge-base --is-ancestor "$REQ_LAST_COMMIT" "$PHASE_BASE_SHA"; echo "exit:$?"`, which must print `exit:0`
   - `sha256sum .planning/REQUIREMENTS.md`, key `REQ_SHA256_BASE`, the digest only
   - `wc -l < .planning/REQUIREMENTS.md`, key `REQ_LINES_BASE`
   - `grep -c 'REL-14' .planning/REQUIREMENTS.md`, key `REL14_HITS_BASE`
   - `date -u +"%Y-%m-%dT%H:%M:%SZ"`, key `GUARD_AT`

   Then cross-check against the planning-time values in this plan's census: 70 lines, 4 hits.
   - If they agree, say so.
   - If they differ, record both values, run `git log --oneline -3 -- .planning/REQUIREMENTS.md`, and quote the change from the previous commit that touched the file.
   - If REL-14's checkbox is not `- [ ]` or its Traceability row is not `Pending`, write `## HALT: REL-14 already moved before phase head` and stop.
   - Otherwise keep your fresh values as the baseline, with the explanation beneath.

2. **`## The lines under guard`.** Paste the verbatim `grep -n 'REL-14' .planning/REQUIREMENTS.md` in a fenced block. Classify each hit:
   - state-bearing: the `- [ ] **REL-14**:` checkbox line and the `| REL-14 | Phase 73 | Pending |` row
   - informational: the "Mapped to phases" coverage line and the "REL-14 is mapped to Phase 73 for coverage only" line

   Explain in two sentences why the whole-file SHA-256 is the primary probe:
   - a flip is line-count-neutral
   - Phase 63's flip moved three requirements at once, which a grep scoped to one ID would have missed

3. **`## Why this file exists`.** State the history from ROADMAP constraint 10 and `71-CLOSEOUT-GUARD.md`, without inventing a count:
   - `phase.complete`-family tooling has flipped the release requirement to `[x]` against an explicit CONTEXT decision at 8 of the 9 prior release-prep closes
   - it did not fire at Phase 71, and one non-firing is not treated as a fix
   - it has landed through both entry points, `phase.complete` from `/gsd-execute-phase` and `/gsd-verify-work`'s inline transition
   - REL-14 is again a requirement meant to close only at `/gsd-complete-milestone`, on the observed merge (D-08)

4. **`## Re-verification protocol (phase close)`.** Give the exact commands plan 73-07 runs, and what each must show:
   - `sha256sum`, compared with `REQ_SHA256_BASE`
   - `wc -l`
   - `git diff --name-only -- .planning/REQUIREMENTS.md`, expected empty
   - `git log --oneline "$PHASE_BASE_SHA"..HEAD -- .planning/REQUIREMENTS.md`, expected empty
   - `grep -n 'REL-14'`, expected byte-identical to § "The lines under guard", compared line by line in file order

5. **`## For the operator running phase.complete`.** This section applies after this phase's plans have finished, at whichever of the two entry points runs: `/gsd-execute-phase`'s `phase.complete` step, or `/gsd-verify-work`'s inline transition.
   - Before either runs, back up `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` to a scratch directory outside the repository.
   - After it runs, run the same probes as step 4, with the baseline digest and the four grep lines written out inline so the reader needs no other file.
   - On any divergence, run `git checkout -- .planning/REQUIREMENTS.md`, re-run the probes to show a MATCH, and then diff ROADMAP.md and STATE.md against the backup.
   - Warn that the same tooling has also rewritten other tracking fields at Phase 71: STATE.md's `current_phase_name` deleted and `Plan: Not started` written, and ROADMAP.md's Status-cell padding. Those are hand-corrected against the backup; they are not the REL-14 guard's own failure.
   - The rule: reverted and reported, never committed, and no `/gsd-complete-milestone` step starts until the probes MATCH.

   State that `73-HANDOFF.md` reproduces this section, and that the orchestrator appends a `## Third observation (after phase.complete, orchestrator)` section here after the tooling runs.

6. **`## This task's own effect on .planning/REQUIREMENTS.md`.** Record `git diff --name-only -- .planning/REQUIREMENTS.md` and `git status --porcelain .planning/REQUIREMENTS.md`, both empty.

7. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; G="$D/73-CLOSEOUT-GUARD.md" &amp;&amp; P72=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$G}" | head -n 1; } &amp;&amp; PB="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; SJ="$(git log -1 --format=%s "$PB")" &amp;&amp; [ -n "$SJ" ] &amp;&amp; ! printf '%s\n' "$SJ" | grep -qE '\(73-0[0-9]\)' &amp;&amp; T72="$(k PUSHED_SHA "$P72/72-CI-EVIDENCE.md")" &amp;&amp; [ "${#T72}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$T72" "$PB" &amp;&amp; [ -z "$(git diff --name-only "$T72" "$PB" -- . ':(exclude).planning')" ] &amp;&amp; [ "$(k PHASE_BASE_PRODUCT_EQUALS_P72_TIP)" = yes ] &amp;&amp; RL="$(git log -1 --format=%H -- .planning/REQUIREMENTS.md)" &amp;&amp; [ "${#RL}" = 40 ] &amp;&amp; [ "$(k REQ_LAST_COMMIT)" = "$RL" ] &amp;&amp; git merge-base --is-ancestor "$RL" "$PB" &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(wc -l &lt; .planning/REQUIREMENTS.md)" = "$(k REQ_LINES_BASE)" ] &amp;&amp; [ "$(grep -c 'REL-14' .planning/REQUIREMENTS.md)" = "$(k REL14_HITS_BASE)" ] &amp;&amp; grep -qE '^- \[ \] \*\*REL-14\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-14 \| Phase 73 \| Pending' .planning/REQUIREMENTS.md &amp;&amp; [ -n "$(k GUARD_AT)" ] &amp;&amp; grep -qF '/gsd-verify-work' "$G" &amp;&amp; grep -qF '/gsd-execute-phase' "$G" &amp;&amp; grep -qF 'git checkout -- .planning/REQUIREMENTS.md' "$G" &amp;&amp; grep -qF 'current_phase_name' "$G" &amp;&amp; [ "$(grep -cF "$(k REQ_SHA256_BASE)" "$G")" -ge 2 ] &amp;&amp; for s in '## Baseline' '## The lines under guard' '## Why this file exists' '## Re-verification protocol (phase close)' '## For the operator running phase.complete'; do grep -qxF "$s" "$G" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$PB" -- .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$G" &amp;&amp; ! grep -q '^## HALT' "$G"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `PHASE_BASE_SHA` not 40 characters, not an ancestor of HEAD, or carrying a plan-scoped `(73-0N)` subject
- Phase 72's `PUSHED_SHA` unreadable, not an ancestor of `PHASE_BASE_SHA`, or its product tree differing from the phase base, or the tie key not yes
- `REQ_LAST_COMMIT` differing from the live last commit on REQUIREMENTS.md, or not an ancestor of the phase base
- the live digest, line count or REL-14 hit count differing from the recorded baseline keys
- the REL-14 checkbox not unchecked, or its row not Pending
- `GUARD_AT` empty
- either entry point, the reversion command, the STATE.md side-effect warning, or the baseline digest written out a second time missing from the file
- REQUIREMENTS.md changed since `PHASE_BASE_SHA`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Baseline` holds `PHASE_BASE_SHA` (40 hex), `PHASE_BASE_PRODUCT_EQUALS_P72_TIP = yes`, `REQ_LAST_COMMIT` (40 hex, an ancestor of `PHASE_BASE_SHA`), `REQ_SHA256_BASE` (64 hex), `REQ_LINES_BASE`, `REL14_HITS_BASE` and `GUARD_AT` as bare key lines, each beside its command, plus a written cross-check against the planning-time values.
    - `## The lines under guard` quotes every `grep -n 'REL-14'` hit verbatim and classifies each as state-bearing or informational.
    - `## For the operator running phase.complete` names `/gsd-execute-phase` and `/gsd-verify-work`, instructs the scratch backup of REQUIREMENTS.md, ROADMAP.md and STATE.md, writes out the baseline digest and grep lines inline, warns about the `current_phase_name` and Status-cell side effects, and carries `git checkout -- .planning/REQUIREMENTS.md` with the rule "reverted and reported, never committed".
    - REQUIREMENTS.md is unchanged in this worktree.
  </acceptance_criteria>
  <done>The phase-head digest, line count, REL-14 hits and PHASE_BASE_SHA are recorded, the base is tied to the product tree Phase 72's CI tested, and both operator procedures are written with the two entry points named.</done>
</task>

<task type="auto">
  <name>Task 2: SC#1 fence observation 1 of 2 with a positive control on every remote negative, and the D-13 milestone fences at phase head</name>
  <reversibility rating="reversible">One new planning document recording read-only probes; nothing is written to any remote.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md</files>
  <read_first>
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-SC1-INVARIANTS.md § "Observation 1 of 2" and § "The typsphinx/ fence (constraint 13)" (the two-observation shape and the scoped-plus-widened diff this plan mirrors; not 71's masked-AST variant)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PATTERNS.md § "`73-SC1-INVARIANTS.md`"
    - .planning/ROADMAP.md § "Phase 73" SC#1, and binding constraints 1, 2 and 3 of the v0.9.5 section
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-11 and D-13, and the measured-state list in `<domain>`
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-GATES-EVIDENCE.md (the `PRODUCT_DIFF_FILES` key line)
  </read_first>
  <action>
Create `73-SC1-INVARIANTS.md` with the heading `# Phase 73 — SC#1 Fence Invariants (unpublished-shaped tree)`.

1. **`## Observation 1 of 2`.** Open with `OBS1_AT = ` followed by the output of `date -u +"%Y-%m-%dT%H:%M:%SZ"`. Then record each probe as a command with its verbatim output, then its key line (D-13: each remote probe carries a positive control against the existing `v0.9.2`):
   - `sed -n 7p pyproject.toml`, key `VERSION_LINE`, which must be `version = "0.9.2"`
   - `git tag -l 'v0.9*'`, the local positive control, with v0.9.0 and v0.9.2 present; then `git tag -l 'v0.9.3'`, `git tag -l 'v0.9.4'` and `git tag -l 'v0.9.5'`, keys `LOCAL_V093_TAGS = 0`, `LOCAL_V094_TAGS = 0` and `LOCAL_V095_TAGS = 0`
   - the unfiltered `git ls-remote --tags origin`. Derive four counts from that single listing: `REMOTE_V092_TAG = 1`, the lines ending `refs/tags/v0.9.2`, which is the positive control; `REMOTE_V093_TAG = 0`; `REMOTE_V094_TAG = 0`; and `REMOTE_V095_TAG = 0`. A filtered probe whose silence could mean an unreachable remote is not accepted.
   - `curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json`, key `PYPI_092_HTTP = 200` (positive control); the same for `0.9.3`, `0.9.4` and `0.9.5`, keys `PYPI_093_HTTP = 404`, `PYPI_094_HTTP = 404` and `PYPI_095_HTTP = 404`
   - `gh release list --limit 20 --json tagName,isLatest,publishedAt`, verbatim. Key `GH_LATEST = v0.9.2` (positive control), and keys `GH_V093_RELEASES = 0`, `GH_V094_RELEASES = 0` and `GH_V095_RELEASES = 0`
   - `gh run list --workflow=release.yml --limit 20 --json databaseId,createdAt,headBranch,event,conclusion`, verbatim. Record the newest run's id as `RELEASE_POSITIVE_CONTROL`; it must be on `headBranch` `v0.9.2`. Record `RELEASE_RUNS_SINCE_V092 = 0`, counting runs created after `2026-08-31` or on a `v0.9.3`, `v0.9.4` or `v0.9.5` ref.
   - `gh pr list --state all --limit 5 --json number`, non-empty (positive control); then `gh pr list --head gsd/v0.9.5-docs-link-check-and-navigation --state all --json number,state`, key `BRANCH_PRS = 0`

   State plainly that the fence holds at phase head: v0.9.2 is still the latest published release, no v0.9.3, v0.9.4 or v0.9.5 tag exists locally or on origin, nothing is on PyPI or GitHub Releases for any of the three numbers, and no pull request exists from the milestone branch. D-11 records all three numbers as unclaimed, not decided, and this phase creates no tag.

2. **`## Milestone fences (D-13)`.**
   - Run `git fetch origin main`, then record `MILESTONE_BASE = ` with `git merge-base HEAD origin/main`, which must be `098a8ff64cf008822eef9dc69f75102ded3f7bc1`, and `ORIGIN_MAIN_SHA_OBS1 = ` with `git rev-parse origin/main`. Record `MAIN_MOVED_OBS1 = no` when `origin/main` equals `MILESTONE_BASE`, `yes` otherwise; a moved `main` is not a failure here, it is D-07's case, which plan 73-05's trial merge measures.
   - Record `git log --format=%h -G '^version = ' "$MILESTONE_BASE"..HEAD -- pyproject.toml`, which must print nothing, key `VERSION_BUMP_COMMITS = 0`.
   - **The claim.** Record `git diff --stat "$MILESTONE_BASE" HEAD -- typsphinx/ .github/workflows/`, which must print nothing, key `MILESTONE_CODE_WORKFLOW_DIFF = empty` (D-13; constraints 2 and 3: no builder change and no workflow edit in this milestone).
   - **Pathspec control.** Record `git ls-tree -r --name-only HEAD -- typsphinx/ .github/workflows/`, and the number of files it lists as `PATHSPEC_TRACKED_FILES`, which must be at least 1: the pathspec names real tracked files, so its empty diff is not a typo.
   - **Widened-diff control.** Record `git diff --name-only "$MILESTONE_BASE" HEAD -- . ':(exclude).planning'`, then its C-sorted, space-joined form (pipe through `LC_ALL=C sort`, `tr '\n' ' '` and strip the trailing space) as `MILESTONE_PRODUCT_FILES_OBS1`. It must equal `PRODUCT_DIFF_FILES` in `72-GATES-EVIDENCE.md`, read with `grep -n`: Phase 72's five product files. The same base with a wider pathspec is non-empty, so the empty scoped diff is not a wrong base.

   Explain in two sentences why both controls are needed, and that this is Phase 69's scoped-plus-widened shape rather than Phase 71's masked-AST re-run, because nothing under `typsphinx/` changed in this milestone (D-13).

3. **`## Handoff to observation 2`.** State that plan 73-06 owns observation 2, the close-tip repeat of the D-13 fences, the phase-scoped diff from `PHASE_BASE_SHA`, and the post-dispatch proof. It runs two waves and one CI dispatch later, so the two `OBS*_AT` stamps are genuinely separated. Name the sections 73-06 appends.

4. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; S="$D/73-SC1-INVARIANTS.md" &amp;&amp; P72=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; MB0=098a8ff64cf008822eef9dc69f75102ded3f7bc1 &amp;&amp; BR=gsd/v0.9.5-docs-link-check-and-navigation &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; ks() { sed -n "s/^$1 = //p" "${2:-$S}" | head -n 1; } &amp;&amp; [ -n "$(ks OBS1_AT)" ] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; [ -n "$(git tag -l 'v0.9.2')" ] &amp;&amp; for v in 3 4 5; do [ -z "$(git tag -l "v0.9.$v")" ] || { echo "local tag: v0.9.$v"; exit 1; }; done &amp;&amp; T="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$T" | grep -c 'refs/tags/v0\.9\.[345]')" = 0 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json)" = 200 ] &amp;&amp; for v in 3 4 5; do [ "$(curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/typsphinx/0.9.$v/json")" = 404 ] || { echo "pypi: 0.9.$v"; exit 1; }; done &amp;&amp; [ "$(gh release list --limit 20 --json tagName,isLatest --jq '.[] | select(.isLatest) | .tagName')" = v0.9.2 ] &amp;&amp; [ "$(gh release list --limit 50 --json tagName --jq '[.[] | select(.tagName == "v0.9.3" or .tagName == "v0.9.4" or .tagName == "v0.9.5")] | length')" = 0 ] &amp;&amp; RPC="$(ks RELEASE_POSITIVE_CONTROL)" &amp;&amp; [ -n "$RPC" ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json databaseId,headBranch --jq "[.[] | select(.databaseId == $RPC and .headBranch == \"v0.9.2\")] | length")" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headBranch,createdAt --jq '[.[] | select(.headBranch == "v0.9.3" or .headBranch == "v0.9.4" or .headBranch == "v0.9.5" or .createdAt &gt; "2026-08-31")] | length')" = 0 ] &amp;&amp; [ "$(gh pr list --state all --limit 5 --json number --jq 'length')" -ge 1 ] &amp;&amp; [ "$(gh pr list --head "$BR" --state all --json number --jq 'length')" = 0 ] &amp;&amp; git fetch -q origin main &amp;&amp; [ "$(git merge-base HEAD origin/main)" = "$MB0" ] &amp;&amp; [ "$(ks MILESTONE_BASE)" = "$MB0" ] &amp;&amp; [ -n "$(ks ORIGIN_MAIN_SHA_OBS1)" ] &amp;&amp; case "$(ks MAIN_MOVED_OBS1)" in yes|no) true ;; *) false ;; esac &amp;&amp; VB="$(git log --format=%h -G '^version = ' "$MB0"..HEAD -- pyproject.toml)" &amp;&amp; [ -z "$VB" ] &amp;&amp; CW="$(git diff --stat "$MB0" HEAD -- typsphinx/ .github/workflows/)" &amp;&amp; [ -z "$CW" ] &amp;&amp; TF="$(git ls-tree -r --name-only HEAD -- typsphinx/ .github/workflows/)" &amp;&amp; [ -n "$TF" ] &amp;&amp; [ "$(printf '%s\n' "$TF" | wc -l)" = "$(ks PATHSPEC_TRACKED_FILES)" ] &amp;&amp; WD="$(git diff --name-only "$MB0" HEAD -- . ':(exclude).planning')" &amp;&amp; for f in CLAUDE.md README.md docs/source/contributing.rst docs/source/index.rst tox.ini; do printf '%s\n' "$WD" | grep -qxF "$f" || { echo "widened diff lacks: $f"; exit 1; }; done &amp;&amp; [ -z "$(printf '%s\n' "$WD" | grep -vxF -e CLAUDE.md -e README.md -e docs/source/contributing.rst -e docs/source/index.rst -e tox.ini -e CHANGELOG.md)" ] &amp;&amp; [ "$(ks MILESTONE_PRODUCT_FILES_OBS1)" = "$(ks PRODUCT_DIFF_FILES "$P72/72-GATES-EVIDENCE.md")" ] &amp;&amp; for x in LOCAL_V093_TAGS LOCAL_V094_TAGS LOCAL_V095_TAGS REMOTE_V093_TAG REMOTE_V094_TAG REMOTE_V095_TAG GH_V093_RELEASES GH_V094_RELEASES GH_V095_RELEASES RELEASE_RUNS_SINCE_V092 BRANCH_PRS VERSION_BUMP_COMMITS; do [ "$(ks "$x")" = 0 ] || { echo "key not 0: $x"; exit 1; }; done &amp;&amp; [ "$(ks REMOTE_V092_TAG)" = 1 ] &amp;&amp; [ "$(ks PYPI_092_HTTP)" = 200 ] &amp;&amp; for v in 093 094 095; do [ "$(ks "PYPI_${v}_HTTP")" = 404 ] || { echo "key: PYPI_${v}_HTTP"; exit 1; }; done &amp;&amp; [ "$(ks GH_LATEST)" = v0.9.2 ] &amp;&amp; [ "$(ks MILESTONE_CODE_WORKFLOW_DIFF)" = empty ] &amp;&amp; for s in '## Observation 1 of 2' '## Milestone fences (D-13)' '## Handoff to observation 2'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$S" &amp;&amp; ! grep -q '^## HALT' "$S" &amp;&amp; PB="$(ks PHASE_BASE_SHA "$D/73-CLOSEOUT-GUARD.md")" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; [ -z "$(git diff --name-only "$PB" -- .planning/REQUIREMENTS.md)" ]</automated>
    <fails_when>Non-zero exit, or `local tag: TAG`, `pypi: VERSION`, `widened diff lacks: FILE`, `key not 0: KEY`, `key: KEY` or `missing section: NAME` printed. Each of these makes it fail:
- `OBS1_AT` empty, or `pyproject.toml:7` not the 0.9.2 assignment
- the v0.9.2 local or remote tag missing (positive control), or any v0.9.3, v0.9.4 or v0.9.5 tag present
- PyPI not 200 for 0.9.2, or not 404 for any of 0.9.3, 0.9.4, 0.9.5
- the latest release not v0.9.2, or a v0.9.3, v0.9.4 or v0.9.5 release present
- the recorded release.yml positive-control run absent or not on v0.9.2, or a run since 2026-08-31 or on an unpublished ref present
- the pull-request listing empty (positive control), or a pull request from the milestone branch present
- the live merge-base with origin/main or `MILESTONE_BASE` not 098a8ff6, or `MAIN_MOVED_OBS1` not yes or no
- a version-line commit since the base, or a non-empty diff over typsphinx/ and .github/workflows/
- the pathspec listing no tracked file or differing from its key
- the widened diff lacking one of Phase 72's five files, carrying another file, or its recorded key differing from Phase 72's `PRODUCT_DIFF_FILES`
- a recorded key contradicting its probe, a section missing, a key line carrying a parenthesis, or a HALT heading
- REQUIREMENTS.md changed since the phase base</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Observation 1 of 2` opens with `OBS1_AT` and records every probe from action step 1 verbatim, each with its key, for all three version numbers.
    - Every negative has a positive control from the same source: the v0.9.2 tag line from the same `ls-remote`, PyPI 200 for 0.9.2, v0.9.2 as the latest release, the `RELEASE_POSITIVE_CONTROL` run on v0.9.2 in the release.yml listing, and a non-empty pull-request listing.
    - `## Milestone fences (D-13)` records `MILESTONE_BASE = 098a8ff64cf008822eef9dc69f75102ded3f7bc1`, `ORIGIN_MAIN_SHA_OBS1`, `MAIN_MOVED_OBS1`, `VERSION_BUMP_COMMITS = 0`, `MILESTONE_CODE_WORKFLOW_DIFF = empty`, `PATHSPEC_TRACKED_FILES` and `MILESTONE_PRODUCT_FILES_OBS1` equal to Phase 72's `PRODUCT_DIFF_FILES`, each with its command.
    - `## Handoff to observation 2` names plan 73-06 and the sections it appends.
  </acceptance_criteria>
  <done>The first SC#1 observation is recorded with a positive control on every remote probe, and the milestone-wide `typsphinx/` and `.github/workflows/` diff is measured empty against a proven pathspec and a proven base.</done>
</task>

<task type="auto">
  <name>Task 3: Write the reasoned no-external-API COVERAGE.md and prove the seal-time gate accepts it</name>
  <reversibility rating="reversible">One new planning document.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/COVERAGE.md</files>
  <read_first>
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/COVERAGE.md (the declaration shape whose opening line the gate accepts)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-RESEARCH.md § "Standard Stack" and § "Environment Availability" (the tools the phase uses)
  </read_first>
  <action>
Create `COVERAGE.md` in the phase directory, following Phase 71's shape: a reasoned declaration with no capability matrix.

1. The heading is `# Phase 73: External API Coverage Declaration`. The first body line starts `No external API integration: ` and continues with one sentence. That sentence says the phase:
   - edits `CHANGELOG.md` only
   - runs the project's own tooling (`uv`, `tox`, `pytest`, `black`, `mypy`, `ruff`, `sphinx-build`, including Sphinx's own `linkcheck` builder)
   - pushes the milestone branch once and dispatches the project's own `ci.yml` once (D-12)
   - measures a trial merge with `git merge-tree` (D-09)
   - writes planning evidence

2. Run the detector over this phase's scope and paste its JSON output verbatim in a fenced block. Build the scope from:
   - the concatenated bodies of every `73-0*-PLAN.md` in the phase directory
   - the output of `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs query roadmap.get-phase 73`

   Pipe that scope to `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/lib/api-coverage.cjs --json`. Whatever it returns is recorded as-is. If `detected` is true, name each signal and explain why it is not an integration. For example, the `gh api` protection read and the PyPI JSON endpoint are read-only probes of this project's own repository and package page, and `tox -e linkcheck` checks the project's own documentation links with a builder Sphinx ships.

3. List what the phase's network use actually is:
   - `git fetch`/`ls-remote` and one fast-forward `git push` against this repository
   - `gh workflow run CI` once
   - read-only `gh run`, `gh release`, `gh pr` and `gh api .../branches/main/protection` calls
   - `curl` status-code probes of `pypi.org/pypi/typsphinx/<version>/json` for four versions
   - Sphinx's `linkcheck` builder requesting the documentation's own outbound links
   - `uv` fetching only what `uv.lock` pins

   `release.yml` and `update-pin.yml` are named in the plans only to forbid them.

4. State why the file exists: the seal-time `api-coverage.verify-pre` gate re-runs the detector over plan prose dense with `gh`, workflow and API wording, and this declaration meets a false positive with a reason rather than a fabricated matrix.

5. Run `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished` and paste its JSON. It must report `"passed": true`.

6. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; C="$D/COVERAGE.md" &amp;&amp; test -f "$C" &amp;&amp; grep -qE '^No external API integration: .{20,}' "$C" &amp;&amp; grep -qF '"detected"' "$C" &amp;&amp; ! grep -qiE '^\| *capability *\|' "$C" &amp;&amp; V="$(node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre "$D")" &amp;&amp; printf '%s\n' "$V" | grep -q '"passed": true' &amp;&amp; PB="$(sed -n 's/^PHASE_BASE_SHA = //p' "$D/73-CLOSEOUT-GUARD.md" | head -n 1)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; PD="$(git diff --name-only "$PB" -- . ':(exclude).planning')" &amp;&amp; [ -z "$(printf '%s\n' "$PD" | grep -vxF CHANGELOG.md)" ] &amp;&amp; [ -z "$(git diff --name-only "$PB" -- .planning/REQUIREMENTS.md)" ]</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- COVERAGE.md absent
- its declaration line missing or shorter than a sentence
- no verbatim detector JSON
- a capability-matrix table present
- the gate output lacking `"passed": true`
- `PHASE_BASE_SHA` unreadable
- a product-tree file other than 73-01's CHANGELOG.md changed since the phase base, or REQUIREMENTS.md changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `COVERAGE.md` opens with `No external API integration: ` followed by a one-sentence reason.
    - It holds the detector's JSON verbatim, run over the concatenated plan bodies and the ROADMAP Phase 73 section, and explains any signal the detector reports.
    - It lists the phase's actual network use and holds no capability table.
    - The pasted `check api-coverage.verify-pre` output reports `"passed": true`.
  </acceptance_criteria>
  <done>The seal-time API-coverage gate has a reasoned declaration it accepts, recorded with the real detector result.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`73-01-PLAN.md` § "Spec-less edge-coverage accounting" and § "Prohibition recall accounting". This
plan carries P1, P2 and P5, and authors the `empty` edge: every negative probe here carries a
positive control. It also realises the concurrency guarantee that no plan writes
`.planning/REQUIREMENTS.md`: the phase-head digest it records detects any writer, including tooling
that runs after every plan. Phase accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed +
0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → origin, GitHub API, PyPI (read-only) | `git ls-remote`, `gh release/run/pr list`, `curl` status probes; no write crosses |
| `.planning/REQUIREMENTS.md` → `phase.complete`-family tooling | the requirement state this phase must not change, and that tooling has changed at 8 of 9 prior closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-06 | Tampering | `phase.complete` (or `/gsd-verify-work`'s transition) flipping REL-14 against D-08 (ASVS V4, L1, by analogy) | high | mitigate | A phase-head digest, line count and quoted lines, recorded before any other plan's commit lands. Both entry points are named, with a scratch backup and a `git checkout` reversion procedure, and 73-07 reproduces all of it in the handoff. |
| T-73-07 | Repudiation | a vacuous remote negative from an unreachable source (ASVS V7, L1) | high | mitigate | Every negative is derived from the same listing as a positive control: v0.9.2 tag, PyPI 200, latest release v0.9.2, the release.yml run on v0.9.2, and a non-empty pull-request listing. The verify re-runs every probe live. |
| T-73-08 | Repudiation | an empty milestone code-and-workflow diff accepted from a mistyped pathspec or a wrong base | high | mitigate | The pathspec must list tracked files at HEAD, and the widened diff from the same base must list exactly Phase 72's five recorded product files. |
| T-73-09 | Elevation of Privilege | a probe becoming a write (tag, release, dispatch, pull request, PR comment) | high | mitigate | Only read commands appear (`git tag -l`, `ls-remote`, `gh … list/view`, `gh api` GET, `curl -o /dev/null`); the plan pushes nothing and dispatches nothing. |
| T-73-10 | Repudiation | a fabricated capability matrix in COVERAGE.md | low | mitigate | A reasoned declaration with the verbatim detector output, and the seal gate's own verdict recorded. |
| T-73-SC | Tampering | package installs | low | accept | `uv sync` installs only what `uv.lock` pins; no package is added. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `73-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates:
- `73-CLOSEOUT-GUARD.md`: baseline, lines under guard, and both operator procedures. Keys:
  `PHASE_BASE_SHA`, `PHASE_BASE_PRODUCT_EQUALS_P72_TIP`, `REQ_LAST_COMMIT`, `REQ_SHA256_BASE`,
  `REQ_LINES_BASE`, `REL14_HITS_BASE`, `GUARD_AT`. 73-07 appends the close-time re-verification.
- `73-SC1-INVARIANTS.md`: observation 1 of 2 and the D-13 milestone fences at phase head. Keys:
  `OBS1_AT`, `VERSION_LINE`, `LOCAL_V093_TAGS`, `LOCAL_V094_TAGS`, `LOCAL_V095_TAGS`,
  `REMOTE_V092_TAG`, `REMOTE_V093_TAG`, `REMOTE_V094_TAG`, `REMOTE_V095_TAG`, `PYPI_092_HTTP`,
  `PYPI_093_HTTP`, `PYPI_094_HTTP`, `PYPI_095_HTTP`, `GH_LATEST`, `GH_V093_RELEASES`,
  `GH_V094_RELEASES`, `GH_V095_RELEASES`, `RELEASE_POSITIVE_CONTROL`, `RELEASE_RUNS_SINCE_V092`,
  `BRANCH_PRS`, `MILESTONE_BASE`, `ORIGIN_MAIN_SHA_OBS1`, `MAIN_MOVED_OBS1`, `VERSION_BUMP_COMMITS`,
  `MILESTONE_CODE_WORKFLOW_DIFF`, `PATHSPEC_TRACKED_FILES`, `MILESTONE_PRODUCT_FILES_OBS1`. 73-06
  appends observation 2 and the close-tip checks.
- `COVERAGE.md`.

It modifies no product-tree file and never edits `.planning/REQUIREMENTS.md`. No file named
`73-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- The recorded digest, line count and REL-14 hit count match the live file, the base is tied to
  Phase 72's CI-tested product tree, and REL-14 is `[ ]` and Pending.
- Observation 1 holds on every probe, for all three unclaimed version numbers, with a positive
  control on each remote one.
- The milestone-wide `typsphinx/` and `.github/workflows/` diff is empty against a proven pathspec
  and a proven base.
- `check api-coverage.verify-pre` passes on `COVERAGE.md`.
</verification>

<success_criteria>
1. SC#4's phase-head half: the REL-14 fence baseline is recorded before any other plan's commit
   lands, with both operator procedures written (D-08).
2. SC#1's first observation: the tree is unpublished-shaped, each remote probe is positively
   controlled, and the milestone changed nothing under `typsphinx/` or `.github/workflows/` (D-13).
3. The seal-time API-coverage gate is satisfied by a reasoned declaration.
</success_criteria>

<output>
Create `.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-02-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-08).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run any GSD state-writing verb.
- The first section states `PHASE_BASE_SHA`, `REQ_SHA256_BASE`, `REQ_LINES_BASE`,
  `REL14_HITS_BASE`, `OBS1_AT`, `MILESTONE_BASE`, `MAIN_MOVED_OBS1` and `MILESTONE_CODE_WORKFLOW_DIFF`.
</output>
