---
phase: 73-v0-9-5-close-prep-prep-only-unpublished
plan: 05
type: execute
wave: 2
depends_on: ["73-01", "73-02"]
files_modified:
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PREFLIGHT-EVIDENCE.md
autonomous: true
requirements: [REL-14]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-09: after `git fetch origin`, `git merge-tree --write-tree TRIAL_HEAD ORIGIN_MAIN_SHA` exits 0, and the tree SHA it prints is recorded as MERGE_TREE. TRIAL_HEAD is this worktree HEAD before any commit, and it carries both new CHANGELOG subsections.'
    - 'Edge (idempotency): re-running the same merge-tree command from the recorded inputs reproduces MERGE_TREE (MERGE_TREE_REPRODUCED = yes), and the verify recomputes it live.'
    - 'Edge (adjacency and empty): when origin/main equals MILESTONE_BASE (MAIN_MOVED = no, nothing to absorb), the trial merge is a no-op: MERGE_TREE equals TRIAL_HEAD''s own tree and TRIAL_IS_NOOP = yes, and the handoff''s branch-update step is a recorded no-op. When main has moved, MAIN_ONLY_COMMITS names the commits it carries, and any uv.lock or ruff change among them is flagged.'
    - 'D-07: if main moved, for example because a Dependabot pull request landed, the merged-tree `ruff check .` below is authoritative for the conditional branch update, and no docs build of the merged tree is added.'
    - 'D-09: pyproject.toml and uv.lock are extracted from MERGE_TREE into a scratch directory outside the repository with `git archive`, and `uv lock --check` exits 0 there. The merged pyproject.toml:7 still reads `version = "0.9.2"`, and the merged uv.lock ruff version is recorded as MERGED_RUFF_LOCK_VERSION.'
    - 'D-09 / SC#3: a scratch copy of MERGE_TREE is synced with `--locked` from its own uv.lock, and `ruff check .` exits 0 there, the ruff that runs being MERGED_RUFF_LOCK_VERSION; `black --check .` is recorded beside it. A non-zero exit is a FINDING for the owner, never fixed here, and TRIAL_MERGE_VERDICT = MET only when the merge, the lock check, ruff and black all pass.'
    - 'D-09 non-committing: no merge commit is created and no ref of the branch moves. `git rev-list --merges BASE_73_05..HEAD` is empty, the merge-base of HEAD and origin/main still equals MILESTONE_BASE, and the scratch directories are removed.'
    - 'D-09: main protection is read live: `strict: true`, and the sorted contexts equal REQUIRED_CONTEXTS_HEAD in 72-BASE-EVIDENCE.md. The first-parent history of origin/main shows #143 and #145, the two previous milestone pull requests, as `Merge pull request` commits.'
    - 'D-06: the open pull requests are read at a recorded PR_CENSUS_AT, with a non-empty `--state all` listing as positive control. Each of #146, #147, #148, #149 and #150 is recorded with its package, version range, state, changed files and author (Dependabot), together with the number of comments and reviews by the authenticated gh account (GH_ACTOR); the live count equals the recorded one. No merge, rebase, comment or close is performed.'
    - 'REL-14 is cited for coverage only; SUMMARY requirements-completed: [] (D-08).'
  artifacts:
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PREFLIGHT-EVIDENCE.md
      provides: 'trial merge, merged-lock check, merged-tree lint, main protection, merge-method precedent, Dependabot and open-PR census, trial-merge verdict'
      contains: 'MERGE_TREE = '
  key_links:
    - from: 'TRIAL_HEAD and ORIGIN_MAIN_SHA'
      to: 'MERGE_TREE'
      via: 'git merge-tree --write-tree, which writes objects only and moves no ref'
      pattern: 'MERGE_RC = '
    - from: 'MERGE_TREE uv.lock'
      to: 'the conditional branch-update step in 73-HANDOFF.md (D-10)'
      via: 'uv lock --check in scratch and a locked scratch sync, proving the post-update lock valid before the operator reaches it'
      pattern: 'LOCK_CHECK_EXIT = '
    - from: '73-SC1-INVARIANTS.md MILESTONE_BASE and 72-BASE-EVIDENCE.md REQUIRED_CONTEXTS_HEAD'
      to: 'the no-op reading, the non-absorption check and the protection comparison'
      via: 'KEY = value lines read with sed'
      pattern: 'MILESTONE_BASE = '
  prohibitions:
    - statement: 'MUST NOT run `git merge`, `git rebase`, `git commit --amend` or any ref-moving command for the trial. It uses `git merge-tree --write-tree` only, and nothing from it is committed (D-09).'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT act on any open pull request, Dependabot or otherwise: no merge, close, comment, review, label or rebase request (D-06).'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT copy the tree SHA, lock result, protection list or pull-request states from CONTEXT or RESEARCH; every value comes from a command run here. MUST NOT install any package outside a committed uv.lock, and MUST NOT build the merged tree''s documentation (D-07).'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Measure, without committing anything to the branch, that the merge `/gsd-complete-milestone` will
run is safe (D-09): a merge of `origin/main` into the milestone branch is conflict-free, the merged
`uv.lock` is valid, and the merged tree lints cleanly at the merged lock's `ruff` version. At
planning `origin/main` equals the merge-base, so the trial merge is expected to be a no-op, and it
still runs: it catches D-07's case, a `main` that moved (for instance with Dependabot #147's `ruff`
bump) between planning and execution. The plan also records the live facts the handoff's
branch-update and merge steps depend on:
- `main`'s strict protection and its required checks, against the set read at Phase 72's base (D-09)
- the merge-commit method the previous milestone pull requests used, #143 and #145 (D-10)
- the five open Dependabot pull requests #146 to #150, recorded by number, package, version range
  and state, and left untouched (D-06)

Purpose: `main`'s protection is `strict: true`, so the REL-14 pull request cannot merge until the
branch is up to date. This plan makes the conditional update step known-good before the operator
reaches it. Because the trial merge must include the CHANGELOG edit, it runs in wave 2, after 73-01,
in parallel with 73-03 and 73-04 and sharing no file with either.

Output: `73-PREFLIGHT-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-RESEARCH.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md
@.planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PREFLIGHT-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run project tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Scratch directories.** Parallel executors share the session scratchpad. Create one parent with
`mktemp -d`, record it as `SCRATCH_73_05`, and put every scratch tree, archive and captured output
under it with a `p7305_` prefix. Remove each scratch tree with `rm -rf` when its step finishes.

**Invoking uv on a scratch directory.** Run every `uv` command from the worktree root and point it at
the scratch tree with `uv --directory <scratch> …`. The `uv` shim resolves this worktree's own
`.venv/bin/uv` from the current directory, inside the FHS sandbox. Run from inside the scratch
directory, the shim would fall back to nixpkgs' `uv`.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line. The verify fails on any key line containing an opening parenthesis. Version ranges are
written with an ASCII arrow (`1.5.0 -> 1.6.1`), never in parentheses.

**Locale.** The host runs `LANG=ja_JP.UTF-8`; prefix `LANG=C LC_ALL=C` to any `git` command whose
message you quote for comparison.

**No tracking edits.** Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or
`.planning/STATE.md`, and do not call any GSD state-writing verb or the GSD commit helper; commit with
plain `git commit`.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
`## HALT` is written only on a failure path; the gates require its absence.

## Planning-time census (2026-09-14, main checkout at `9d276727`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `origin/main` against the milestone base | `origin/main` = `098a8ff64cf008822eef9dc69f75102ded3f7bc1` = `git merge-base HEAD origin/main`; the branch lacks no `main` commit | `git rev-parse`, `git merge-base` |
| Trial merge expectation | with `origin/main` equal to the merge-base, `git merge-tree --write-tree` returns HEAD's own tree with exit 0 | `git merge-tree` semantics, `71-PREFLIGHT-EVIDENCE.md` |
| Branch lock | ruff 0.16.6 | `uv.lock` |
| `main` protection | `strict: true`; six contexts equal to `REQUIRED_CONTEXTS_HEAD` in `72-BASE-EVIDENCE.md` | live `gh api` |
| Merge-method precedent | `58d578f2` (#143, v0.9.3) and `383a07e9` (#145, v0.9.4), both `Merge pull request` commits on `origin/main`'s first-parent history; at the v0.9.3 close the Dependabot PRs #142 and #141 merged after #143 | `git log --first-parent` |
| Open PRs | five, all `app/dependabot`, all base `main`, created 2026-09-14T00:15Z: #146 `build` 1.5.0 → 1.6.1; #147 `ruff` 0.16.6 → 0.16.7; #148 `pypdf` 6.14.2 → 6.18.0; #149 `twine` 6.2.0 → 7.0.0; #150 `sphinx-autodoc-typehints` 3.0.1 → 3.13.6 (a `docs`-extra dependency). #146 changes `uv.lock` only, and its only comment author is `dependabot`, with no reviews | `gh pr list`, `gh pr view 146` |
| gh account | `YuSabo90002` | `gh api user -q .login` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: fetch, non-committing trial merge of origin/main reproduced from its inputs, and the merged lock checked in scratch</name>
  <precondition>73-01 and 73-02 are merged into this worktree's HEAD: the `CHANGELOG.md` region between `## [Unreleased]` and `## [0.9.2]` holds `### Added`, `### Changed` and `### Fixed` with six bold-lead bullets, and `73-SC1-INVARIANTS.md` carries `MILESTONE_BASE`. Otherwise write `## HALT: wave 1 not merged` and stop.</precondition>
  <reversibility rating="reversible">`git merge-tree --write-tree` writes unreferenced objects only; no ref moves and the scratch directory is deleted.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PREFLIGHT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-RESEARCH.md § "Pattern 2: Non-committing trial merge against `origin/main`" and § "Don't Hand-Roll"
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-06, D-07 and D-09
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PREFLIGHT-EVIDENCE.md (the transcript shape to mirror)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md (`MILESTONE_BASE`)
    - pyproject.toml lines 1-60 and uv.lock (the branch's own dev-extra ranges and ruff pin, for comparison)
  </read_first>
  <action>
Create `73-PREFLIGHT-EVIDENCE.md` with the heading `# Phase 73 — REL-14 Update-Step Pre-Flight (D-09, non-committing)`. Open it with one paragraph stating that nothing in this file is committed to the branch except the file itself, that no merge is performed, and that no value is copied from CONTEXT.md or RESEARCH.md.

1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line
   - `uv --version`
   - before any commit: `BASE_73_05 = ` and `TRIAL_HEAD = `, both from `git rev-parse HEAD`
   - `START_CANONICAL = ` from `git rev-parse gsd/v0.9.5-docs-link-check-and-navigation`
   - `SCRATCH_73_05 = ` followed by the path `mktemp -d` printed

2. **`## Fetch and divergence`.** Run `git fetch origin` (D-09's own command), and record:
   - `ORIGIN_MAIN_SHA = ` from `git rev-parse origin/main`
   - `git log --oneline "$TRIAL_HEAD".."$ORIGIN_MAIN_SHA"`, and the count of those commits, key `MAIN_ONLY_COMMITS`
   - `git rev-list --left-right --count "$ORIGIN_MAIN_SHA"...HEAD`
   - `git merge-base HEAD "$ORIGIN_MAIN_SHA"`, which must equal `MILESTONE_BASE` from `73-SC1-INVARIANTS.md`
   - `MAIN_MOVED = no` when `ORIGIN_MAIN_SHA` equals `MILESTONE_BASE`, `yes` otherwise

   If `main` moved, name each commit it carries, map any Dependabot merge to its pull-request number, and state whether any commit changes `uv.lock` or the `ruff` pin in `pyproject.toml`. That is D-07's case: Task 2's lint count on the merged tree is then authoritative, and the handoff's branch-update step is live rather than a no-op.

3. **`## Trial merge`.** Record `git merge-tree --write-tree "$TRIAL_HEAD" "$ORIGIN_MAIN_SHA"; echo "exit:$?"` verbatim.
   - Key `MERGE_RC` is the exit code. Key `MERGE_TREE` is the first output line, the tree SHA.
   - On a non-zero exit, quote the conflict listing, write `## HALT: trial merge is not clean`, and stop. The handoff's update step would not be conflict-free, and that decision belongs to the owner.
   - Run the same command a second time and record `MERGE_TREE_REPRODUCED = yes` when it prints the same tree (edge: idempotency).
   - Record `git rev-parse "$TRIAL_HEAD^{tree}"`, and `TRIAL_IS_NOOP = yes` when it equals `MERGE_TREE`, `no` otherwise. With `MAIN_MOVED = no` it must be `yes`: `origin/main` only touches the branch at the merge-base, so there is nothing to absorb (edges: adjacency, empty).
   - State that this tree SHA is valid only for these two inputs and must be re-measured immediately before any real merge.

4. **`## Merged lock`** (D-09).
   - Create a scratch tree under `SCRATCH_73_05` and record its path.
   - Write `pyproject.toml` and `uv.lock` from `MERGE_TREE` into an archive file under `SCRATCH_73_05` with `git archive --format=tar -o <archive> "$MERGE_TREE" pyproject.toml uv.lock`, then extract it into the scratch tree with `tar -xf <archive> -C <scratch tree>`.
   - From the worktree root, record `uv --directory <scratch tree> lock --check; echo "exit:$?"` verbatim. Keys: `LOCK_CHECK_EXIT`, which must be 0, and `LOCK_CHECK_UV_VERSION`, the version number only from `uv --version`.
   - Record the merged facts, each as a command with its output:
     - `sed -n 7p <scratch tree>/pyproject.toml`, key `MERGED_VERSION_LINE`, which must be `version = "0.9.2"`
     - `grep -A1 -xF 'name = "ruff"' <scratch tree>/uv.lock`, key `MERGED_RUFF_LOCK_VERSION`, the version only
     - the same against this branch's own `uv.lock`, key `BRANCH_RUFF_LOCK_VERSION`
     - the `###` heading list and bold-bullet count of the `CHANGELOG.md` region in `git show "$MERGE_TREE:CHANGELOG.md"`, key `MERGED_CHANGELOG_BOLD = 6`
   - Remove the scratch tree.

5. **`## Nothing moved`.** Record each of these:
   - `git rev-parse HEAD`, still equal to `TRIAL_HEAD` before this plan's commit
   - `git rev-parse gsd/v0.9.5-docs-link-check-and-navigation`, still equal to `START_CANONICAL` unless the orchestrator or 73-04 advanced it, in which case record both
   - `git status --porcelain`, which lists only this evidence file
   - `git merge-base HEAD "$ORIGIN_MAIN_SHA"`, still equal to `MILESTONE_BASE`

   State that `merge-tree` added only unreferenced objects to the object store.

6. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; F="$D/73-PREFLIGHT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_73_05)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; TH="$(k TRIAL_HEAD)" &amp;&amp; [ "$TH" = "$B" ] &amp;&amp; OM="$(k ORIGIN_MAIN_SHA)" &amp;&amp; [ "${#OM}" = 40 ] &amp;&amp; git cat-file -e "$OM^{commit}" &amp;&amp; MB="$(k MILESTONE_BASE "$D/73-SC1-INVARIANTS.md")" &amp;&amp; [ "$MB" = 098a8ff64cf008822eef9dc69f75102ded3f7bc1 ] &amp;&amp; [ "$(git merge-base "$TH" "$OM")" = "$MB" ] &amp;&amp; [ "$(k MERGE_RC)" = 0 ] &amp;&amp; MT="$(git merge-tree --write-tree "$TH" "$OM")" &amp;&amp; [ "$MT" = "$(k MERGE_TREE)" ] &amp;&amp; [ "$(k MERGE_TREE_REPRODUCED)" = yes ] &amp;&amp; THT="$(git rev-parse "$TH^{tree}")" &amp;&amp; if [ "$OM" = "$MB" ]; then [ "$MT" = "$THT" ] &amp;&amp; [ "$(k TRIAL_IS_NOOP)" = yes ] &amp;&amp; [ "$(k MAIN_MOVED)" = no ]; else [ "$(k MAIN_MOVED)" = yes ]; fi &amp;&amp; PP="$(git show "$MT:pyproject.toml")" &amp;&amp; [ "$(printf '%s\n' "$PP" | sed -n 7p)" = 'version = "0.9.2"' ] &amp;&amp; UL="$(git show "$MT:uv.lock")" &amp;&amp; RV="$(printf '%s\n' "$UL" | grep -A1 -xF 'name = "ruff"' | sed -n 's/^version = "\(.*\)"$/\1/p')" &amp;&amp; [ -n "$RV" ] &amp;&amp; [ "$(k MERGED_RUFF_LOCK_VERSION)" = "$RV" ] &amp;&amp; MC="$(git show "$MT:CHANGELOG.md")" &amp;&amp; MR="$(printf '%s\n' "$MC" | awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}')" &amp;&amp; [ "$(printf '%s\n' "$MR" | grep -E '^### ' | tr '\n' '|')" = '### Added|### Changed|### Fixed|### Planned for Future Releases|' ] &amp;&amp; [ "$(printf '%s\n' "$MR" | grep -E '^- \*\*' | wc -l)" = 6 ] &amp;&amp; [ "$(k MERGED_CHANGELOG_BOLD)" = 6 ] &amp;&amp; S="$(mktemp -d)" &amp;&amp; X="$(mktemp)" &amp;&amp; git archive --format=tar -o "$X" "$MT" pyproject.toml uv.lock &amp;&amp; tar -xf "$X" -C "$S" &amp;&amp; uv --directory "$S" lock --check &amp;&amp; rm -rf "$S" "$X" &amp;&amp; [ "$(k LOCK_CHECK_EXIT)" = 0 ] &amp;&amp; MG="$(git rev-list --merges "$B"..HEAD)" &amp;&amp; [ -z "$MG" ] &amp;&amp; [ "$(git merge-base HEAD "$OM")" = "$MB" ] &amp;&amp; for s in '## Head check and provisioning' '## Fetch and divergence' '## Trial merge' '## Merged lock' '## Nothing moved'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_73_05` missing, or `TRIAL_HEAD` differing from it
- `ORIGIN_MAIN_SHA` not a commit, or `MILESTONE_BASE` not 098a8ff6
- the merge-base of the two inputs differing from `MILESTONE_BASE`
- `MERGE_RC` not 0, the live `merge-tree` exiting non-zero, or printing a tree other than `MERGE_TREE`
- `MERGE_TREE_REPRODUCED` not yes
- with `origin/main` still at the milestone base, the merge tree differing from HEAD's own tree, or `TRIAL_IS_NOOP` or `MAIN_MOVED` keys contradicting that; with `main` moved, `MAIN_MOVED` not yes
- the merged `pyproject.toml:7` not 0.9.2
- the merged ruff version empty or differing from its key
- the merged CHANGELOG lacking the Added, Changed, Fixed order or six bullets
- the live `uv lock --check` failing, or `LOCK_CHECK_EXIT` not 0
- a merge commit on the branch since the base, or origin/main absorbed
- a product-tree file changed since `BASE_73_05`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Fetch and divergence` records `ORIGIN_MAIN_SHA`, `MAIN_ONLY_COMMITS`, the merge-base equal to `MILESTONE_BASE`, and `MAIN_MOVED`, with any moved-main commit named.
    - `## Trial merge` records the verbatim `git merge-tree --write-tree` output with `MERGE_RC = 0`, `MERGE_TREE`, `MERGE_TREE_REPRODUCED = yes` and `TRIAL_IS_NOOP`, and states that the SHA must be re-measured before any real merge.
    - `## Merged lock` records the scratch path, `LOCK_CHECK_EXIT = 0` with `LOCK_CHECK_UV_VERSION`, `MERGED_VERSION_LINE`, `MERGED_RUFF_LOCK_VERSION`, `BRANCH_RUFF_LOCK_VERSION` and `MERGED_CHANGELOG_BOLD = 6`.
    - `## Nothing moved` shows HEAD and the canonical ref unmoved by the trial, only the evidence file in `git status`, and the merge-base with `origin/main` still `MILESTONE_BASE`.
  </acceptance_criteria>
  <done>The merge `/gsd-complete-milestone` will run is measured conflict-free and reproducible, with a valid merged lock, and the branch is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Merged-tree lint at the merged lock's versions, main's protection and merge-method precedent, and the Dependabot census recorded without acting</name>
  <precondition>Task 1 is committed with `MERGE_RC = 0`, `LOCK_CHECK_EXIT = 0` and no HALT heading.</precondition>
  <reversibility rating="reversible">A scratch directory outside the repository, and read-only GitHub calls.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PREFLIGHT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-PREFLIGHT-EVIDENCE.md (Task 1: `MERGE_TREE`, `MERGED_RUFF_LOCK_VERSION`, `MAIN_MOVED`)
    - tox.ini (the `lint` environment's two commands, which this task reproduces on the merged tree)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md (`REQUIRED_STRICT_HEAD`, `REQUIRED_CONTEXTS_HEAD`)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-06, D-07, D-09 and D-10
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-PREFLIGHT-EVIDENCE.md § "Merged-tree lint", § "main protection (D-06)" and § "Open pull requests (D-12)" (the shape to mirror; those headings name Phase 71's own decisions)
  </read_first>
  <action>
1. Re-run the head check, then `git fetch origin`.

2. **`## Merged-tree lint`** (D-09). The trial merge must pass `ruff check .` on the merged tree, at the merged lock's `ruff`; under D-07 that count is authoritative if `main` moved.
   - Create a second scratch tree under `SCRATCH_73_05`. Archive the whole `MERGE_TREE` into a file under `SCRATCH_73_05` with `git archive --format=tar -o <archive> "$MERGE_TREE"`, and extract it into the scratch tree.
   - Run `uv --directory <scratch tree> sync --locked --extra dev --no-install-project` from the worktree root. This is a hash-verified install from the merged `uv.lock` only.
   - Record `uv --directory <scratch tree> run --no-sync ruff --version`, key `MERGED_RUFF_RUN_VERSION`, which must equal `MERGED_RUFF_LOCK_VERSION`.
   - Record `uv --directory <scratch tree> run --no-sync ruff check .; echo "exit:$?"` verbatim, key `MERGED_RUFF_EXIT`.
   - Record `uv --directory <scratch tree> run --no-sync black --check .; echo "exit:$?"` verbatim, key `MERGED_BLACK_EXIT`.
   - Remove the scratch tree. Do not build the merged tree's documentation (D-07).
   - If either exit is non-zero, write a `## FINDING: merged-tree lint` section quoting each finding, write `TRIAL_MERGE_VERDICT = NOT-MET`, and return a blocking checkpoint naming the finding. Do not fix anything: a fix is a product change this phase does not make, and it would need a second CI dispatch.

3. **`## main protection (D-09)`.** Record `gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict` and `--jq '[.contexts[]] | sort | join("|")'` verbatim, with the keys:
   - `PROTECTION_STRICT`, which must be `true`
   - `PROTECTION_CONTEXTS`, the sorted, pipe-joined list, which must equal `REQUIRED_CONTEXTS_HEAD` in `72-BASE-EVIDENCE.md`
   - `PROTECTION_CONTEXTS_COUNT`

   List the context names exactly as printed. State why the handoff's branch-update step is conditional (D-10): under `strict: true` the pull request cannot merge until the branch is up to date with `main`, so if `main` has moved by then it is merged in, and otherwise the step is a recorded no-op.

4. **`## Merge-method precedent (D-10)`.** Record `git log --first-parent --format='%h %s' origin/main`, filtered to the lines naming `Merge pull request #143` and `Merge pull request #145`, key `MERGE_PRECEDENT_HITS = 2`, and `git log --first-parent --format='%h %s' -12 origin/main`. State that the two previous milestone pull requests merged with merge commits, never squash or rebase, and that at the v0.9.3 close the Dependabot pull requests merged after the milestone pull request (#143, then #142 and #141).

5. **`## Dependabot and open pull requests (D-06)`.**
   - Record `gh pr list --state all --limit 5 --json number`, which must be non-empty: the positive control.
   - Record `gh pr list --state open --json number,title,author,headRefName,baseRefName,updatedAt` verbatim.
   - Record `gh api user -q .login`, key `GH_ACTOR`.
   - For each of #146, #147, #148, #149 and #150, record `gh pr view <n> --json number,title,state,author,headRefName,baseRefName,files,mergeable,updatedAt` and the count `gh pr view <n> --json comments,reviews -q '[.comments[], .reviews[] | select(.author.login == "<GH_ACTOR>")] | length'`. Keys per pull request: `PR_<n>_STATE`, `PR_<n>_PACKAGE`, `PR_<n>_RANGE` (written `old -> new`), `PR_<n>_FILES` (space-joined paths) and `PR_<n>_ACTOR_TOUCHES`. Tabulate each in a row starting `| #<n> |`.
   - If any other pull request is open, tabulate it the same way and name it.
   - Keys: `OPEN_PRS`, the open count; `DEPENDABOT_OPEN_PRS`, those whose author login is `app/dependabot`; and `PR_CENSUS_AT`, the `date -u +%FT%TZ` of the census.
   - State that no merge, close, comment, review, label or rebase request was made (D-06). If any of the five is already `MERGED`, it merged outside this phase: say so, and confirm `MAIN_MOVED = yes` in Task 1 (D-07). The order the handoff records is: milestone pull request first, Dependabot second, with Dependabot rebasing the remaining `uv.lock`-only pull requests on its own after each merge.

6. **`## What the handoff can rely on`.** Summarise in one table, citing each key above:
   - conflict-free merge, and whether it is a no-op
   - valid merged lock
   - the merged ruff version and the merged-tree lint result
   - strict protection with its contexts equal to Phase 72's base reading
   - the merge-commit method
   - the Dependabot census time and each pull request's state

   Then write `TRIAL_MERGE_VERDICT = MET` only when `MERGE_RC`, `LOCK_CHECK_EXIT`, `MERGED_RUFF_EXIT` and `MERGED_BLACK_EXIT` are all 0.

7. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; F="$D/73-PREFLIGHT-EVIDENCE.md" &amp;&amp; P72=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_73_05)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; MT="$(k MERGE_TREE)" &amp;&amp; [ -n "$MT" ] &amp;&amp; S2="$(mktemp -d)" &amp;&amp; X="$(mktemp)" &amp;&amp; git archive --format=tar -o "$X" "$MT" &amp;&amp; tar -xf "$X" -C "$S2" &amp;&amp; uv --directory "$S2" sync --locked --extra dev --no-install-project -q &amp;&amp; [ "$(uv --directory "$S2" run --no-sync ruff --version | cut -d' ' -f2)" = "$(k MERGED_RUFF_LOCK_VERSION)" ] &amp;&amp; [ "$(k MERGED_RUFF_RUN_VERSION)" = "$(k MERGED_RUFF_LOCK_VERSION)" ] &amp;&amp; RX="$(uv --directory "$S2" run --no-sync ruff check . &gt;/dev/null 2&gt;&amp;1; echo $?)" &amp;&amp; BX="$(uv --directory "$S2" run --no-sync black --check . &gt;/dev/null 2&gt;&amp;1; echo $?)" &amp;&amp; rm -rf "$S2" "$X" &amp;&amp; [ "$RX" = 0 ] &amp;&amp; [ "$BX" = 0 ] &amp;&amp; [ "$(k MERGED_RUFF_EXIT)" = 0 ] &amp;&amp; [ "$(k MERGED_BLACK_EXIT)" = 0 ] &amp;&amp; RC="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '[.contexts[]] | sort | join("|")')" &amp;&amp; [ -n "$RC" ] &amp;&amp; [ "$RC" = "$(k REQUIRED_CONTEXTS_HEAD "$P72/72-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(k PROTECTION_CONTEXTS)" = "$RC" ] &amp;&amp; [ "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict)" = true ] &amp;&amp; [ "$(k PROTECTION_STRICT)" = true ] &amp;&amp; git fetch -q origin main &amp;&amp; FP="$(git log --first-parent --format='%h %s' origin/main)" &amp;&amp; [ -n "$FP" ] &amp;&amp; [ "$(printf '%s\n' "$FP" | grep -E ' Merge pull request #(143|145) ' | wc -l)" = 2 ] &amp;&amp; [ "$(k MERGE_PRECEDENT_HITS)" = 2 ] &amp;&amp; [ "$(gh pr list --state all --limit 5 --json number --jq 'length')" -ge 1 ] &amp;&amp; [ -n "$(k PR_CENSUS_AT)" ] &amp;&amp; A="$(k GH_ACTOR)" &amp;&amp; [ -n "$A" ] &amp;&amp; [ "$A" = "$(gh api user -q .login)" ] &amp;&amp; for p in 146 147 148 149 150; do grep -qE "^\| #$p \|" "$F" || { echo "census row missing: #$p"; exit 1; }; [ "$(gh pr view "$p" --json author -q .author.login)" = app/dependabot ] || { echo "not dependabot: #$p"; exit 1; }; [ -n "$(k "PR_${p}_STATE")" ] &amp;&amp; [ -n "$(k "PR_${p}_PACKAGE")" ] &amp;&amp; [ -n "$(k "PR_${p}_RANGE")" ] &amp;&amp; [ -n "$(k "PR_${p}_FILES")" ] || { echo "census keys missing: #$p"; exit 1; }; [ "$(gh pr view "$p" --json comments,reviews -q "[.comments[], .reviews[] | select(.author.login == \"$A\")] | length")" = "$(k "PR_${p}_ACTOR_TOUCHES")" ] || { echo "actor touches changed: #$p"; exit 1; }; if [ "$(k "PR_${p}_STATE")" = MERGED ]; then [ "$(k MAIN_MOVED)" = yes ] || { echo "merged but main not moved: #$p"; exit 1; }; fi; done &amp;&amp; case "$(k OPEN_PRS)" in ''|*[!0-9]*) false ;; *) true ;; esac &amp;&amp; case "$(k DEPENDABOT_OPEN_PRS)" in ''|*[!0-9]*) false ;; *) true ;; esac &amp;&amp; [ "$(k TRIAL_MERGE_VERDICT)" = MET ] &amp;&amp; for s in '## Merged-tree lint' '## main protection (D-09)' '## Merge-method precedent (D-10)' '## Dependabot and open pull requests (D-06)' '## What the handoff can rely on'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `census row missing: #N`, `not dependabot: #N`, `census keys missing: #N`, `actor touches changed: #N`, `merged but main not moved: #N` or `missing section: NAME` printed. Each of these makes it fail:
- `MERGE_TREE` missing, or the locked scratch sync failing
- the running ruff version differing from the merged lock's, or its key wrong
- the live ruff or black run on the merged tree exiting non-zero, or its recorded exit not 0
- `main` protection not strict, or its sorted contexts differing from Phase 72's base reading or from the recorded key
- the #143 and #145 merge-commit precedent not both found, or its key wrong
- the pull-request listing empty (positive control), or the census time or gh account missing or wrong
- any of #146 to #150 missing from the census table or keys, not authored by Dependabot, or with a live count of comments and reviews by the gh account differing from the recorded one
- a pull request recorded MERGED while `MAIN_MOVED` is not yes
- a PR count not a number
- `TRIAL_MERGE_VERDICT` not MET
- a section missing, or a product-tree file changed since `BASE_73_05`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Merged-tree lint` records a locked scratch sync from the merged `uv.lock`, `MERGED_RUFF_RUN_VERSION` equal to `MERGED_RUFF_LOCK_VERSION`, and the verbatim `ruff check .` and `black --check .` outputs with `MERGED_RUFF_EXIT = 0` and `MERGED_BLACK_EXIT = 0`. A non-zero exit would carry a `## FINDING: merged-tree lint` section, and nothing is fixed.
    - `## main protection (D-09)` records `PROTECTION_STRICT = true`, `PROTECTION_CONTEXTS` equal to Phase 72's `REQUIRED_CONTEXTS_HEAD`, `PROTECTION_CONTEXTS_COUNT`, and the names verbatim.
    - `## Merge-method precedent (D-10)` records `MERGE_PRECEDENT_HITS = 2`.
    - `## Dependabot and open pull requests (D-06)` records the positive control, the verbatim open-PR listing, `GH_ACTOR`, a `| #<n> |` row and the five `PR_<n>_*` keys for each of #146 to #150, `OPEN_PRS`, `DEPENDABOT_OPEN_PRS` and `PR_CENSUS_AT`, and states that no action was taken and the merge order.
    - `## What the handoff can rely on` summarises every key, and `TRIAL_MERGE_VERDICT = MET`.
  </acceptance_criteria>
  <done>Every fact the handoff's branch-update and merge steps depend on is measured live: the merged-tree lint at the merged versions, strict protection with the contexts read at Phase 72's base, the merge-commit method, and the five Dependabot pull requests recorded and left untouched.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`73-01-PLAN.md`. This plan carries P1, P5 and P6, and it authors the `adjacency` and `empty` edges
(the no-op merge when `origin/main` sits at the merge-base) and the `idempotency` edge (the
reproduced tree). It records the inputs for residual assumptions (b) and (c):
- `ORIGIN_MAIN_SHA` and `MERGE_TREE` hold only for this moment, so the handoff re-runs `merge-tree`
  immediately before the pull request.
- The Dependabot pull requests' states hold only at `PR_CENSUS_AT`, so the handoff re-reads them.

Phase accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trial merge → branch refs | the pre-flight must measure the merge without creating it |
| merged `uv.lock` → scratch environment | packages are installed from a lock that may not be on this branch yet (a Dependabot bump that landed on `main`) |
| worktree → GitHub API (read-only) | protection, pull-request and history reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-22 | Tampering | the pre-flight creating a merge commit or moving HEAD or the canonical ref, absorbing `main` into the branch | high | mitigate | Only `git merge-tree --write-tree` is used; `## Nothing moved` records HEAD and the canonical ref; the verify requires no merge commit since the base and a merge-base with `origin/main` still equal to `MILESTONE_BASE`. |
| T-73-23 | Tampering | installing packages outside a committed lock in the scratch lint (ASVS V14, L1) | medium | mitigate | The scratch environment is created with `uv sync --locked` from the merged `uv.lock`, which is hash-pinned; nothing is fetched with an ad-hoc spec. |
| T-73-24 | Repudiation | a stale tree SHA, lock result or protection list copied from CONTEXT or RESEARCH | medium | mitigate | A fresh `git fetch origin`, `ORIGIN_MAIN_SHA` recorded, and the verify recomputes `merge-tree` from the recorded inputs, re-runs `uv lock --check`, ruff and black live, and re-reads the protection against Phase 72's base reading. |
| T-73-25 | Elevation of Privilege | a census call becoming an action on a Dependabot pull request (D-06) | medium | mitigate | Only `gh pr list` and `gh pr view` are used; the verify re-counts comments and reviews by the gh account on each of #146 to #150 against the census. |
| T-73-26 | Repudiation | a Dependabot merge on `main` mid-phase going unnoticed (D-07) | medium | mitigate | `MAIN_MOVED` and `MAIN_ONLY_COMMITS` are measured after a fresh fetch; a pull request recorded as merged requires `MAIN_MOVED = yes`, and the merged-tree lint is then authoritative. |
| T-73-SC | Tampering | package installs | low | accept | The worktree sync and the scratch sync both install from committed locks only; no package is added to this branch. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `73-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`73-PREFLIGHT-EVIDENCE.md`, with the keys `BASE_73_05`, `TRIAL_HEAD`, `START_CANONICAL`,
`SCRATCH_73_05`, `ORIGIN_MAIN_SHA`, `MAIN_ONLY_COMMITS`, `MAIN_MOVED`, `MERGE_RC`, `MERGE_TREE`,
`MERGE_TREE_REPRODUCED`, `TRIAL_IS_NOOP`, `LOCK_CHECK_EXIT`, `LOCK_CHECK_UV_VERSION`,
`MERGED_VERSION_LINE`, `MERGED_RUFF_LOCK_VERSION`, `BRANCH_RUFF_LOCK_VERSION`,
`MERGED_CHANGELOG_BOLD`, `MERGED_RUFF_RUN_VERSION`, `MERGED_RUFF_EXIT`, `MERGED_BLACK_EXIT`,
`PROTECTION_STRICT`, `PROTECTION_CONTEXTS`, `PROTECTION_CONTEXTS_COUNT`, `MERGE_PRECEDENT_HITS`,
`GH_ACTOR`, `PR_<n>_STATE`, `PR_<n>_PACKAGE`, `PR_<n>_RANGE`, `PR_<n>_FILES` and
`PR_<n>_ACTOR_TOUCHES` for n in 146 to 150, `OPEN_PRS`, `DEPENDABOT_OPEN_PRS`, `PR_CENSUS_AT` and
`TRIAL_MERGE_VERDICT`. It also creates scratch directories outside the repository, all removed. It
modifies no product-tree file, creates no commit other than its evidence, and moves no ref. No file
named `73-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- The trial merge of `origin/main` is clean and reproducible from the recorded inputs, a no-op while
  `main` sits at the merge-base, and the merged lock passes `uv lock --check`.
- The merged tree passes `ruff check .` and `black --check .` at the merged versions.
- `main`'s strict protection equals Phase 72's base reading, the #143 and #145 merge-commit
  precedents are recorded, and the five Dependabot pull requests are recorded live and untouched.
- Nothing was merged into, or moved on, the branch.
</verification>

<success_criteria>
The trial-merge half of ROADMAP SC#3 is met without committing anything to the branch (D-09), the
moved-main case is measured rather than assumed (D-07), and the facts D-06 and D-10 need are
recorded, so the handoff's conditional update step is known conflict-free with a valid lock and a
clean lint.
</success_criteria>

<output>
Create `.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-05-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-08).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run any GSD state-writing verb.
- The first section states `ORIGIN_MAIN_SHA`, `MAIN_MOVED`, `MERGE_RC`, `MERGE_TREE`,
  `TRIAL_IS_NOOP`, `LOCK_CHECK_EXIT`, `MERGED_RUFF_LOCK_VERSION`, `MERGED_RUFF_EXIT`,
  `MERGED_BLACK_EXIT`, `PROTECTION_STRICT`, the Dependabot census with each state, and
  `TRIAL_MERGE_VERDICT`. If a FINDING or HALT section was written, it says so first.
</output>
