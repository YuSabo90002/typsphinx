---
phase: 73-v0-9-5-close-prep-prep-only-unpublished
plan: 04
type: execute
wave: 2
depends_on: ["73-01", "73-02"]
files_modified:
  - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md
autonomous: true
requirements: [REL-14]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-12(1) census: immediately before the push, `git branch --list gsd/v0.9.5*` and `git ls-remote --heads origin gsd/v0.9.5*` are recorded. A decoy is never pushed. A local decoy that is already an ancestor of the canonical branch is deleted; a decoy carrying commits the canonical branch lacks halts the plan, because the canonical pointer must be advanced to it first, and that is a main-checkout action.'
    - 'The pushed tip carries every product-tree change of this phase. PUSHED_SHA is the canonical ref gsd/v0.9.5-docs-link-check-and-navigation. Its CHANGELOG.md region reads `### Added`, `### Changed`, `### Fixed` with six bold bullets, and its pyproject.toml:7 is `version = "0.9.2"`. Nothing under typsphinx/ or .github/workflows/ differs from MILESTONE_BASE (D-13), its merge-base with origin/main is still MILESTONE_BASE, and outside .planning/ it is identical to this worktree HEAD.'
    - 'D-12(2) push: only the canonical ref is pushed, with `git push --no-follow-tags origin gsd/v0.9.5-docs-link-check-and-navigation`, as a fast-forward of the origin head ORIGIN_BEFORE (which descends from Phase 72''s PUSHED_SHA), re-measured just before, with no force, no tag and no other ref. Afterwards the origin head equals PUSHED_SHA and no tag points at it.'
    - 'D-12(3) dispatch: exactly one `gh workflow run CI --ref gsd/v0.9.5-docs-link-check-and-navigation` takes effect after the push. RUN_ID has headSha equal to PUSHED_SHA, event workflow_dispatch, workflowName CI, and createdAt later than PUSH_AT. A dispatch call that errors is followed by a run listing before any retry.'
    - 'D-12(4) / SC#3 CI half: the run is observed in the foreground to status completed, and every job conclusion is transcribed in a numbered table (JOB_COUNT, measured). Test Python 3.12 on windows-latest, Test Python 3.13 on windows-latest, Test Python 3.12 on macos-latest and Test Python 3.13 on macos-latest are named individually. Every job is success; a skipped, cancelled or missing job never counts as success. `ruff`''s verdict is quoted from the `Lint and Format Check` job''s `Run lint with tox` step, and CI_RUFF_VERSION equals the ruff version in uv.lock at PUSHED_SHA.'
    - 'D-12: no release.yml run carries PUSHED_SHA, and exactly one workflow_dispatch CI run on the branch carries it (DISPATCH_COUNT = 1).'
    - 'D-09 at phase close: main''s required status checks read `strict: true` with the same six contexts as REQUIRED_CONTEXTS_HEAD in 72-BASE-EVIDENCE.md (REQUIRED_CHECKS_UNCHANGED = yes).'
    - 'Edge (ordering): the run''s job names, compared as a sorted set, equal those of Phase 72''s run (RUN_ID in 72-CI-EVIDENCE.md), so ci.yml has not changed since Phase 72.'
    - 'Edge (idempotency): an executor that resumes mid-wait reuses RUN_ID, which is committed before the first wait, and never dispatches again.'
    - statement: 'Edge (concurrency): only this plan writes to origin, as one fast-forward of the ORIGIN_BEFORE it re-measured just before the push, halting rather than forcing if origin moved; a Dependabot pull request landing on main mid-phase does not touch the branch and is caught by 73-05''s trial merge (D-07, D-09); its wave-2 siblings 73-03 and 73-05 share no file with it and read none of its evidence.'
      verification: backstop
    - 'Failure path: a non-success run is transcribed in full and kept. The executor stops at a blocking checkpoint, with no rerun, no second dispatch and no edit.'
    - 'REL-14 is cited for coverage only; SUMMARY requirements-completed: [] (D-08).'
  artifacts:
    - path: .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md
      provides: 'wave-1 gate, tip identity and fence, decoy census, fast-forward push, one dispatch, the completed run with every job, both windows and macos lanes, lint verdict, job-name comparison with Phase 72, dispatch-count and no-release guards, required checks at close, SC#3 CI verdict'
      contains: 'PUSHED_SHA = '
  key_links:
    - from: 'local ref gsd/v0.9.5-docs-link-check-and-navigation'
      to: 'origin/gsd/v0.9.5-docs-link-check-and-navigation'
      via: 'git push --no-follow-tags origin gsd/v0.9.5-docs-link-check-and-navigation, fast-forward only; refs are shared between the worktree and the main checkout'
      pattern: 'ORIGIN_BEFORE = '
    - from: 'gh workflow run CI --ref gsd/v0.9.5-docs-link-check-and-navigation'
      to: 'ci.yml jobs on ubuntu, windows and macos'
      via: 'workflow_dispatch, since the push and pull_request triggers are scoped to main and develop'
      pattern: 'RUN_ID = '
    - from: 'uv.lock at PUSHED_SHA'
      to: 'CI_RUFF_VERSION in the Lint and Format Check job'
      via: 'uv sync --locked on the runner'
      pattern: 'CI_RUFF_VERSION = '
  prohibitions:
    - statement: 'MUST NOT create a tag, open or comment on a pull request, act on a Dependabot pull request, trigger release.yml or update-pin.yml, force-push, push any ref other than the canonical branch, or push a decoy (D-06, D-12).'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT dispatch a second CI run, rerun jobs, or edit any file to turn a red job green; MUST NOT cite any run whose headSha differs from PUSHED_SHA as this phase''s green.'
      requirement_id: REL-14
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT move any ref of the main checkout from this worktree, other than deleting a decoy that is already an ancestor of the canonical ref, and MUST NOT merge origin/main into the branch.'
      requirement_id: REL-14
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Put this phase's tip on `origin` and obtain CI's verdict on it (D-12, ROADMAP SC#3 CI half):
- re-run the decoy census immediately before the push (D-12(1))
- fast-forward only the canonical milestone branch, and push nothing else (D-12(2))
- dispatch `ci.yml` exactly once on it (D-12(3)) and observe the run in the foreground until it
  completes
- transcribe every job conclusion, naming both `windows-latest` and both `macos-latest` lanes, and
  quote `ruff`'s verdict from the `Lint and Format Check` job (D-12(4))
- re-read `main`'s required checks at phase close against the set read at Phase 72's base (D-09)

Purpose: CI holds lint authority and is the only route to Windows and macOS. The dispatched tip must
carry the CHANGELOG edit, so this plan runs in wave 2, after 73-01 has merged. Every later commit of
the phase touches only `.planning/`, which no CI job reads, so this run covers the phase's final
product tree; plan 73-06 proves that. It runs in parallel with 73-03 and 73-05, and it is the only
plan in the phase that writes to a remote. The push and the dispatch are the two outward actions
73-CONTEXT's Phase Boundary pre-authorises as standing prep-phase precedent (Phases 57, 61, 63, 69,
71); no plan triggers `release.yml`.

Output: `73-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-RESEARCH.md
@.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run project tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Refs are shared.** This worktree's HEAD is on its own worktree branch, but
`refs/heads/gsd/v0.9.5-docs-link-check-and-navigation` is the same ref the main checkout has checked
out. Push that ref by name; never `git branch -f`, `git update-ref` or `git symbolic-ref` it from here.

**Waiting on CI.** Wait in the foreground: set the Bash tool's `timeout` parameter to 600000 and never
use `run_in_background`. Wrap each watch in `timeout 590` so it returns inside the tool limit, then
read `gh run view "$RUNID" --json status,conclusion`. Repeat these bounded foreground calls until
`status` is `completed`, and do not end the turn while the run is in progress. Commit `RUN_ID` to
the evidence file before the first wait, so a resumed executor can reuse it rather than dispatch
again. An executor that backgrounds the wait returns early with no SUMMARY.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line. The verify fails on any key line containing an opening parenthesis.

**Scratch files.** Parallel executors share the session scratchpad. Put every captured log under your
own `mktemp -d` directory, recorded as `SCRATCH_73_04`, with a `p7304_` prefix.

**Locale.** The host runs `LANG=ja_JP.UTF-8`; prefix `LANG=C LC_ALL=C` to any `git` command whose
message you quote for comparison.

**No tracking edits.** Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or
`.planning/STATE.md`, and do not call any GSD state-writing verb or the GSD commit helper (it has
created decoy milestone branches in this repository); commit with plain `git commit`.
</worktree_provisioning>

<!-- planner-discipline-allow: gsd/v0.9.5-milestone -->
<!-- planner-discipline-allow: gsd/v0\.9\.5-milestone -->
<!-- planner-discipline-allow: v0.9.3 -->
<!-- planner-discipline-allow: v0.9.4 -->
<!-- planner-discipline-allow: v0.9.5 -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[345] -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- plan-criteria-allow: R4 - the only git pipelines are git show and git ls-remote outputs already captured into variables before any pipe; the gh pipes read JSON from the API -->
The decoy name is named because the census looks for it and the gate requires it absent from
origin. The version tags are named because the gate requires them absent from origin. `## HALT` is
written only on a failure path.

## Planning-time census (2026-09-14, main checkout at `9d276727`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| Local `gsd/v0.9.5*` refs | only the canonical `gsd/v0.9.5-docs-link-check-and-navigation` (HEAD of the main checkout, ahead of its origin counterpart by `.planning/` commits only); no decoy | `git branch -a` |
| Origin head of the canonical branch | `0b2595df21399363f50e5e1a55935f470e0df00d`, Phase 72's `PUSHED_SHA`; `branch.<name>.remote` is `origin` | `git rev-parse`, `72-CI-EVIDENCE.md` |
| Last CI run on the branch | `34761445288` at `0b2595df` (Phase 72, 12 jobs, all success, `JOB_NAMES_MATCH_PHASE71 = yes`) | `72-CI-EVIDENCE.md` |
| `ci.yml` | `name: CI`; `workflow_dispatch` at `ci.yml:8`; lint step `Run lint with tox` at `ci.yml:69`; 12 jobs: `Test Python {3.12,3.13} on {ubuntu,windows,macos}-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`, `Integration Test - basic`, `Integration Test - advanced` | `.github/workflows/ci.yml`, `gh run view 34761445288` |
| Required checks on `main` | `strict: true`; `REQUIRED_CONTEXTS_HEAD = Build Package\|Code Coverage\|Lint and Format Check\|Test Python 3.12 on ubuntu-latest\|Test Python 3.13 on ubuntu-latest\|Type Check` | `72-BASE-EVIDENCE.md`, live `gh api` |
| Branch-lock ruff | 0.16.6 | `uv.lock` |
| Dispatch hazard | Phase 71's `gh workflow run` returned HTTP 5xx three times while one of those calls had created a run; a retry made a duplicate | `71-04-SUMMARY.md` § "Deviations" |
| Anchors | `MILESTONE_BASE` comes from `73-SC1-INVARIANTS.md` (wave 1) | 73-02 |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: gate on wave 1, tip identity and fence, decoy census, fast-forward push of the canonical branch, one CI dispatch</name>
  <precondition>`gh auth status` succeeds and `git ls-remote --heads origin` returns at least one line. The canonical ref `gsd/v0.9.5-docs-link-check-and-navigation` carries 73-01's edit and 73-02's anchors: `git show gsd/v0.9.5-docs-link-check-and-navigation:CHANGELOG.md` has `### Added`, `### Changed` and `### Fixed` between `## [Unreleased]` and `## [0.9.2]`, and `git cat-file -e gsd/v0.9.5-docs-link-check-and-navigation:.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md` succeeds. Otherwise write `## HALT: canonical tip lacks wave 1` and stop without pushing.</precondition>
  <reversibility rating="costly">The fast-forward publishes this phase's commits on an already-public branch and the dispatch leaves a run in Actions history; undoing either would need a force-push or a run deletion, both forbidden here. Pre-authorised by the owner in 73-CONTEXT's Phase Boundary as standing prep-phase precedent (Phases 57, 61, 63, 69, 71); no tag, release or pull request is created, so nothing is published.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-06-PLAN.md Task 1 and 72-CI-EVIDENCE.md (the census, push and dispatch procedure and section structure this task mirrors; `PUSHED_SHA`, `RUN_ID`)
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-04-SUMMARY.md § "Deviations from Plan" (the HTTP 5xx duplicate-dispatch hazard)
    - .github/workflows/ci.yml (`name: CI`, the triggers and the job names)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md (`MILESTONE_BASE`)
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CONTEXT.md D-12 and D-13, and the Phase Boundary
    - .planning/ROADMAP.md binding constraints 3 and 6 of the v0.9.5 section
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line
   - the same provisioning line with `--locked` appended, then `echo "exit:$?"`, which must print `exit:0`. Every `ci.yml` job begins with a locked sync, so a stale lock would fail every lane before any signal. On a non-zero exit write `## HALT: lock not in sync`, and never regenerate the lock.
   - before any commit: `BASE_73_04 = ` followed by the output of `git rev-parse HEAD`
   - `SCRATCH_73_04 = ` followed by the path `mktemp -d` printed

2. **`## Wave-1 gate`.** Quote the precondition checks:
   - the `###` heading list and the bold-bullet count over the canonical ref's `CHANGELOG.md` region, and the presence of the `(QUA-13, DOC-24).**` and `(DOC-18).**` spans
   - `git cat-file -e` of `73-SC1-INVARIANTS.md` at the canonical ref
   - the absence of a `## HALT` heading in `73-CHANGELOG-EVIDENCE.md` and `73-CLOSEOUT-GUARD.md` at the canonical ref

3. **`## Tip identity and fence`.** Read `MILESTONE_BASE` from `73-SC1-INVARIANTS.md`. Record each of these; all must hold:
   - `PUSHED_SHA = ` followed by the output of `git rev-parse gsd/v0.9.5-docs-link-check-and-navigation`, plus `git log --oneline -1 "$PUSHED_SHA"`
   - `git diff --name-only HEAD "$PUSHED_SHA" -- . ':(exclude).planning'`, empty: the pushed product tree equals this worktree's
   - the version line of `git show "$PUSHED_SHA:pyproject.toml"` (line 7), which must be `version = "0.9.2"`
   - `git diff --name-only "$MILESTONE_BASE" "$PUSHED_SHA" -- typsphinx/ .github/workflows/`, empty (D-13; constraints 2 and 3)
   - after `git fetch origin main`: `git merge-base "$PUSHED_SHA" origin/main`, which must equal `MILESTONE_BASE`, so no `main` commit was absorbed
   - `git tag --points-at "$PUSHED_SHA"`, empty
   - the ruff version in `git show "$PUSHED_SHA:uv.lock"`, key `LOCK_RUFF_VERSION`

4. **`## Decoy census (D-12)`.** Run this immediately before the push, with nothing but the step-5 measurements in between. Record `git branch --list 'gsd/v0.9.5*' -v` and `git ls-remote --heads origin 'gsd/v0.9.5*'`.
   - If `gsd/v0.9.5-milestone` exists on origin, write `## HALT: decoy on origin` and stop. It is never pushed, and its removal belongs to the owner.
   - If it exists locally, run `git merge-base --is-ancestor gsd/v0.9.5-milestone gsd/v0.9.5-docs-link-check-and-navigation; echo "exit:$?"`:
     - `exit:0`: the canonical pointer already contains it, so D-12(1)'s advance-first step is already satisfied. Delete it with `git branch -d gsd/v0.9.5-milestone` and record that.
     - Non-zero: the decoy carries commits the canonical branch lacks. Record `git log --oneline gsd/v0.9.5-docs-link-check-and-navigation..gsd/v0.9.5-milestone`, write `## HALT: decoy carries unique commits`, and stop. D-12(1) requires the canonical pointer to be advanced to the decoy before the decoy is deleted, and that happens in the main checkout (fast-forward the canonical branch there, re-point HEAD, then delete the decoy), not from this worktree.
   - If it exists nowhere, record that as the census result.

5. **`## Push`** (D-12(2)).
   - Run `git fetch origin gsd/v0.9.5-docs-link-check-and-navigation`, then record `ORIGIN_BEFORE = ` with the first field of `git ls-remote --heads origin refs/heads/gsd/v0.9.5-docs-link-check-and-navigation`.
   - Record `git merge-base --is-ancestor "$ORIGIN_BEFORE" "$PUSHED_SHA"; echo "exit:$?"`, which must print `exit:0`, and the same check with Phase 72's `PUSHED_SHA` from `72-CI-EVIDENCE.md` in place of `ORIGIN_BEFORE`. Otherwise write `## HALT: origin moved` and stop; a force-push is forbidden.
   - Record `git config --get push.followTags` and `git config --get branch.gsd/v0.9.5-docs-link-check-and-navigation.remote` (which must print `origin`), then `PUSH_AT = ` with the output of `date -u +%FT%TZ`.
   - Run `git push --no-follow-tags origin gsd/v0.9.5-docs-link-check-and-navigation`, the single push with the guard against tags riding along. Record the output, including GitHub's pull-request hint, and do not act on that hint.
   - Record that `git ls-remote --heads origin refs/heads/gsd/v0.9.5-docs-link-check-and-navigation` now prints `PUSHED_SHA`, and that `git ls-remote --tags origin` shows `refs/tags/v0.9.2` and no v0.9.3, v0.9.4 or v0.9.5 tag.

6. **`## Dispatch`** (D-12(3)).
   - Run `gh workflow run CI --ref gsd/v0.9.5-docs-link-check-and-navigation` once, and record its exit code and output.
   - Whether it succeeded or errored, locate the run with `gh run list --workflow=ci.yml --branch gsd/v0.9.5-docs-link-check-and-navigation --event workflow_dispatch --limit 5 --json databaseId,headSha,status,createdAt,url`. Select the row whose `headSha` equals `PUSHED_SHA` and whose `createdAt` is later than `PUSH_AT`.
   - If no such row is listed yet, that is registration lag or a lost response. Repeat the list query for up to two minutes. Only if the call itself had errored (for example HTTP 5xx) and still no row appears after that, issue the dispatch once more, and list again before anything else. Never dispatch while a matching row exists: in Phase 71 an errored call had created a run, and a retry made a duplicate.
   - Record `RUN_ID = ` and `RUN_URL = `, and record how many dispatch calls were issued and their exit codes.

7. Write `73-CI-EVIDENCE.md` under the heading `# Phase 73 — CI Evidence (D-12, SC#3 CI half)`, using the section names above, and commit it with plain `git commit`. This plan's own commits land after the push and touch only `.planning/`, which no CI job reads.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; F="$D/73-CI-EVIDENCE.md" &amp;&amp; P72=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; BR=gsd/v0.9.5-docs-link-check-and-navigation &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_73_04)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; MB="$(k MILESTONE_BASE "$D/73-SC1-INVARIANTS.md")" &amp;&amp; [ "$MB" = 098a8ff64cf008822eef9dc69f75102ded3f7bc1 ] &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; HR="$(git ls-remote --heads origin "refs/heads/$BR")" &amp;&amp; [ "$(printf '%s\n' "$HR" | cut -f1)" = "$P" ] &amp;&amp; OB="$(k ORIGIN_BEFORE)" &amp;&amp; [ "${#OB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$OB" "$P" &amp;&amp; T72="$(k PUSHED_SHA "$P72/72-CI-EVIDENCE.md")" &amp;&amp; [ "${#T72}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$T72" "$P" &amp;&amp; [ "$(git config --get "branch.$BR.remote")" = origin ] &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; [ "$(printf '%s\n' "$HL" | grep -c 'gsd/v0\.9\.5-milestone')" = 0 ] &amp;&amp; [ -z "$(git tag --points-at "$P")" ] &amp;&amp; TL="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.[345]')" = 0 ] &amp;&amp; PC="$(git show "$P:CHANGELOG.md")" &amp;&amp; RR="$(printf '%s\n' "$PC" | awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}')" &amp;&amp; [ "$(printf '%s\n' "$RR" | grep -E '^### ' | tr '\n' '|')" = '### Added|### Changed|### Fixed|### Planned for Future Releases|' ] &amp;&amp; [ "$(printf '%s\n' "$RR" | grep -E '^- \*\*' | wc -l)" = 6 ] &amp;&amp; git cat-file -e "$P:$D/73-SC1-INVARIANTS.md" &amp;&amp; PP="$(git show "$P:pyproject.toml")" &amp;&amp; [ "$(printf '%s\n' "$PP" | sed -n 7p)" = 'version = "0.9.2"' ] &amp;&amp; [ -z "$(git diff --name-only "$MB" "$P" -- typsphinx .github/workflows)" ] &amp;&amp; git fetch -q origin main &amp;&amp; [ "$(git merge-base "$P" origin/main)" = "$MB" ] &amp;&amp; [ -z "$(git diff --name-only HEAD "$P" -- . ':(exclude).planning')" ] &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json workflowName -q .workflowName)" = CI ] &amp;&amp; [ "$(gh run view "$RUNID" --json event -q .event)" = workflow_dispatch ] &amp;&amp; PA="$(k PUSH_AT)" &amp;&amp; [ -n "$PA" ] &amp;&amp; [[ "$(gh run view "$RUNID" --json createdAt -q .createdAt)" &gt; "$PA" ]] &amp;&amp; for s in '## Head check and provisioning' '## Wave-1 gate' '## Tip identity and fence' '## Decoy census (D-12)' '## Push' '## Dispatch'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_73_04` missing, or `MILESTONE_BASE` not 098a8ff6
- `PUSHED_SHA` not 40 characters, or the origin head not equal to it
- `ORIGIN_BEFORE` or Phase 72's pushed tip missing or not an ancestor of the pushed tip (a non-fast-forward)
- the branch's configured remote not origin
- the decoy present on origin
- a tag pointing at the pushed tip, the v0.9.2 tag missing from origin, or a v0.9.3, v0.9.4 or v0.9.5 tag on origin
- the pushed CHANGELOG.md region lacking the Added, Changed, Fixed order or six bullets, or 73-SC1-INVARIANTS.md absent at the pushed tip
- the pushed `pyproject.toml:7` not 0.9.2
- a typsphinx/ or .github/workflows/ change since the milestone base at the pushed tip
- the pushed tip's merge-base with origin/main differing from `MILESTONE_BASE`
- the pushed product tree differing from HEAD
- no `RUN_ID`, or the run's headSha, workflow, event or creation time not matching
- a product-tree file changed since `BASE_73_04`
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Decoy census (D-12)` records both listings immediately before the push, plus any deletion of a decoy that is already an ancestor, with its ancestry result.
    - `## Tip identity and fence` records `PUSHED_SHA`, the empty product diff against HEAD, the 0.9.2 version line, the empty `typsphinx/` and `.github/workflows/` diff from `MILESTONE_BASE`, the merge-base equal to `MILESTONE_BASE`, no tag, and `LOCK_RUFF_VERSION`.
    - `## Push` records `ORIGIN_BEFORE` as an ancestor (`exit:0`), Phase 72's pushed tip as an ancestor, `PUSH_AT`, the verbatim `git push --no-follow-tags origin gsd/v0.9.5-docs-link-check-and-navigation` output, and the origin head equal to `PUSHED_SHA`.
    - `## Dispatch` records the dispatch call or calls with their exit codes, the listing used to locate the run, `RUN_ID` and `RUN_URL`, with `headSha` equal to `PUSHED_SHA`.
  </acceptance_criteria>
  <done>The phase tip carrying the CHANGELOG edit is on origin as a fast-forward, with no decoy and no tag, and one CI run is dispatched against exactly that tip.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the run to completion in the foreground, transcribe every job, name both windows-latest and both macos-latest lanes, compare job names with Phase 72's run, quote ruff's CI verdict, and re-read the required checks</name>
  <precondition>The `RUN_ID` recorded by Task 1 resolves with `gh run view RUN_ID --json databaseId`. If this executor resumes after returning mid-wait, it reuses that id.</precondition>
  <reversibility rating="reversible">Read-only observation of a run already dispatched.</reversibility>
  <files>.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CI-EVIDENCE.md (Task 1: `RUN_ID`, `PUSHED_SHA`, `LOCK_RUFF_VERSION`)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md § "Job census", § "windows-latest lanes", § "macos-latest lanes", § "Lint through tox" and § "Required checks at phase close" (the shape to mirror)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md (`REQUIRED_CONTEXTS_HEAD`, `REQUIRED_STRICT_HEAD`)
    - .github/workflows/ci.yml lines 10-72 (the matrix, the names, `Run lint with tox` at line 69)
  </read_first>
  <action>
1. **Wait in the foreground.**
   - Run `timeout 590 gh run watch "$RUNID" --interval 30` in a Bash call with `timeout` 600000 and no `run_in_background`.
   - After each return, read `gh run view "$RUNID" --json status,conclusion`.
   - If `status` is not `completed`, issue the same pair again. Do not end the turn and do not return control while it is in progress.
   - Overall cap: 90 minutes of wall clock. If it is reached, record `RUN_ID` and the current status in the evidence and return a blocking checkpoint. Never dispatch a second run.

2. **`## Run`.** Record `gh run view "$RUNID" --json status,conclusion,workflowName,headSha,url,createdAt,updatedAt`, and the keys `RUN_HEAD_SHA` and `RUN_CONCLUSION`.

3. **`## Job census`.** Record `gh run view "$RUNID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'` as a numbered table covering every row. Keys: `JOB_COUNT` (the number of jobs, measured) and `NON_SUCCESS_JOBS = 0`.

   Then compare the job names as a sorted set with Phase 72's run: read `RUN_ID` from `72-CI-EVIDENCE.md`, and record `gh run view <that id> --json jobs --jq '[.jobs[].name] | sort | join("|")'` beside the same query for this `RUN_ID`. Key `JOB_NAMES_MATCH_PHASE72 = yes` when they are equal. A difference means `ci.yml` changed since Phase 72, which constraint 3 forbids; write `## HALT: ci.yml job set changed` with both lists.

4. **`## windows-latest lanes`** and **`## macos-latest lanes`** (D-12(4)). Two sub-tables naming `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each with its conclusion.

5. **`## Lint through tox`** (D-12(4)). Take the `Lint and Format Check` job id from the run's jobs. From `gh run view --job <id> --log`, quote:
   - the `+ ruff==` line of the dependency-install step, key `CI_RUFF_VERSION`, which must equal `LOCK_RUFF_VERSION`
   - the `Run lint with tox` step's black command line and its verdict
   - its ruff command line and its verdict
   - `lint: OK`

   CI is the lint authority. 73-03's local ruff run is set beside this one in 73-07's handoff, not here.

6. **`## Dispatch count and no release run`** (D-12). Record:
   - `gh run list --workflow=ci.yml --branch gsd/v0.9.5-docs-link-check-and-navigation --event workflow_dispatch --limit 50 --json databaseId,headSha,status,conclusion`, showing exactly one run at `PUSHED_SHA`, key `DISPATCH_COUNT = 1`
   - `gh run list --workflow=release.yml --limit 20 --json headSha`, showing none at `PUSHED_SHA`, key `RELEASE_RUNS_AT_PUSHED = 0`

   If more than one run sits at `PUSHED_SHA` (a lost 5xx response created one), record the honest count, do not cancel, delete or dispatch anything, and return a blocking checkpoint naming both run ids, so the owner can rule as Phase 71's owner did.

7. **`## Required checks at phase close`** (D-09). Record `gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict` and `--jq '[.contexts[]] | sort | join("|")'`, keys `REQUIRED_STRICT_CLOSE` and `REQUIRED_CONTEXTS_CLOSE`. They must equal `REQUIRED_STRICT_HEAD` and `REQUIRED_CONTEXTS_HEAD` from `72-BASE-EVIDENCE.md`. Key `REQUIRED_CHECKS_UNCHANGED = yes`.

8. **`## D-12 final tip`.** State:
   - this run is the phase's single dispatch
   - `PUSHED_SHA` carries every product-tree change of the phase, the CHANGELOG edit
   - every later commit of this phase touches only `.planning/`, which no CI job reads, and plan 73-06 proves it with an empty product-tree diff from `PUSHED_SHA`
   - no second dispatch is warranted

9. **`## SC#3 CI verdict`.** Write `SC3_CI_VERDICT = MET` only when the run is completed and success, `NON_SUCCESS_JOBS = 0`, `JOB_NAMES_MATCH_PHASE72 = yes`, all four named lanes are success, `DISPATCH_COUNT = 1`, and `REQUIRED_CHECKS_UNCHANGED = yes`.
   - If any job is not success: transcribe every job literally and keep the run. Do not rerun, dispatch again or edit anything. Write `SC3_CI_VERDICT = NOT-MET`, and return a blocking checkpoint naming each failing job and step.

10. Commit with plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished &amp;&amp; F="$D/73-CI-EVIDENCE.md" &amp;&amp; P72=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; BR=gsd/v0.9.5-docs-link-check-and-navigation &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; [ "$(gh run view "$RUNID" --json status -q .status)" = completed ] &amp;&amp; [ "$(gh run view "$RUNID" --json conclusion -q .conclusion)" = success ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; JC="$(gh run view "$RUNID" --json jobs -q '.jobs | length')" &amp;&amp; [ "$JC" -ge 1 ] &amp;&amp; [ "$JC" = "$(k JOB_COUNT)" ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.conclusion != "success")] | length')" = 0 ] &amp;&amp; R72="$(k RUN_ID "$P72/72-CI-EVIDENCE.md")" &amp;&amp; [ -n "$R72" ] &amp;&amp; JN="$(gh run view "$RUNID" --json jobs -q '[.jobs[].name] | sort | join("|")')" &amp;&amp; [ -n "$JN" ] &amp;&amp; [ "$JN" = "$(gh run view "$R72" --json jobs -q '[.jobs[].name] | sort | join("|")')" ] &amp;&amp; for n in 'Test Python 3.12 on windows-latest' 'Test Python 3.13 on windows-latest' 'Test Python 3.12 on macos-latest' 'Test Python 3.13 on macos-latest'; do [ "$(gh run view "$RUNID" --json jobs -q "[.jobs[] | select(.name == \"$n\" and .conclusion == \"success\")] | length")" = 1 ] || { echo "lane: $n"; exit 1; }; grep -qF "$n" "$F" || { echo "missing: $n"; exit 1; }; done &amp;&amp; grep -qF 'Lint and Format Check' "$F" &amp;&amp; grep -qF 'Run lint with tox' "$F" &amp;&amp; grep -qF 'lint: OK' "$F" &amp;&amp; CR="$(k CI_RUFF_VERSION)" &amp;&amp; [ -n "$CR" ] &amp;&amp; [ "$CR" = "$(k LOCK_RUFF_VERSION)" ] &amp;&amp; UL="$(git show "$P:uv.lock")" &amp;&amp; [ "$(printf '%s\n' "$UL" | grep -A1 -xF 'name = "ruff"' | sed -n 's/^version = "\(.*\)"$/\1/p')" = "$CR" ] &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch "$BR" --event workflow_dispatch --limit 50 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; RC="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '[.contexts[]] | sort | join("|")')" &amp;&amp; [ -n "$RC" ] &amp;&amp; [ "$RC" = "$(k REQUIRED_CONTEXTS_HEAD "$P72/72-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict)" = "$(k REQUIRED_STRICT_HEAD "$P72/72-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(k NON_SUCCESS_JOBS)" = 0 ] &amp;&amp; [ "$(k JOB_NAMES_MATCH_PHASE72)" = yes ] &amp;&amp; [ "$(k DISPATCH_COUNT)" = 1 ] &amp;&amp; [ "$(k RELEASE_RUNS_AT_PUSHED)" = 0 ] &amp;&amp; [ "$(k REQUIRED_CHECKS_UNCHANGED)" = yes ] &amp;&amp; [ "$(k SC3_CI_VERDICT)" = MET ] &amp;&amp; for s in '## Run' '## Job census' '## windows-latest lanes' '## macos-latest lanes' '## Lint through tox' '## Dispatch count and no release run' '## Required checks at phase close' '## D-12 final tip' '## SC#3 CI verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `lane: NAME`, `missing: NAME` or `missing section: NAME` printed. Each of these makes it fail:
- the run not completed, or its conclusion not success
- its headSha differing from `PUSHED_SHA`
- the live job count differing from `JOB_COUNT`, or any job not success
- the sorted job-name set differing from Phase 72's recorded run
- a windows-latest or macos-latest lane missing, not success, or not named in the evidence
- the lint job name, the `Run lint with tox` step or `lint: OK` absent from the evidence
- `CI_RUFF_VERSION` empty or differing from the ruff version of uv.lock at the pushed tip
- other than one dispatched run at `PUSHED_SHA`, or a release.yml run at it
- the live required contexts or strict flag differing from Phase 72's base reading
- a count, match or verdict key wrong, or a section missing
- a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Run` records `status`, `conclusion`, `workflowName`, `headSha` and `url`, and `## Job census` holds a numbered table of every job, with `JOB_COUNT`, `NON_SUCCESS_JOBS = 0` and `JOB_NAMES_MATCH_PHASE72 = yes`.
    - The two lane sub-tables name `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each success.
    - `## Lint through tox` quotes the `Run lint with tox` step's black and ruff lines and `lint: OK`, with `CI_RUFF_VERSION` equal to `LOCK_RUFF_VERSION`.
    - `DISPATCH_COUNT = 1`, `RELEASE_RUNS_AT_PUSHED = 0` and `REQUIRED_CHECKS_UNCHANGED = yes`.
    - `## D-12 final tip` states why no second dispatch is warranted, and `SC3_CI_VERDICT = MET`.
  </acceptance_criteria>
  <done>One completed, all-green CI run on this phase's tip is transcribed job by job. Both Windows and both macOS lanes are named, the job set matches Phase 72's run, ruff's verdict comes from CI at the lock's own version, and main's required checks are unchanged.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's six spec-less edge items and the prohibition recall (P1..P6) are accounted in
`73-01-PLAN.md`. This plan carries P1, P5 and P6, and it authors the `ordering` and `idempotency`
edges and the `concurrency` edge. The last is a backstop marker, because the parallel siblings'
independence holds by construction rather than by a single observable check.

It also carries residual assumption (a): the local canonical ref is assumed not to move between the
census and the push. If it does move, `PUSHED_SHA` records what was pushed, the run is bound to it by
`headSha`, and the empty product diff against HEAD shows whether the dispatched tree is this phase's.
Phase accounting: 6 surfaced = 5 explicit + 1 backstop + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local refs → origin | a push publishes refs; a decoy, a force, another ref or a tag would publish something this phase must not |
| gh CLI → GitHub Actions | a dispatch starts remote work; the wrong workflow would begin a publish pipeline, and a retried dispatch can create a duplicate run |
| CI log → committed evidence | quoted job-log lines enter the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-16 | Tampering | pushing the decoy, a force-push, another ref, or a tag riding along | high | mitigate | Decoy census immediately before the push, then `git push --no-follow-tags origin gsd/v0.9.5-docs-link-check-and-navigation` with `ORIGIN_BEFORE` and Phase 72's pushed tip required as ancestors. The verify requires no decoy on origin, no tag at the pushed tip, and no v0.9.3, v0.9.4 or v0.9.5 tag on origin. |
| T-73-17 | Elevation of Privilege | triggering `release.yml` or `update-pin.yml` (publish pipelines) | high | mitigate | Only `gh workflow run CI` is ever issued; the verify asserts no `release.yml` run carries `PUSHED_SHA`; a prohibition forbids every route. |
| T-73-18 | Repudiation | citing Phase 72's run or any other stale run as this phase's green, or a second dispatch masking a red first one | high | mitigate | `RUN_ID`'s `headSha` must equal `PUSHED_SHA` and its `createdAt` must follow `PUSH_AT`; exactly one dispatched run at that SHA; a red run is kept and escalated. Phase 72's run is read only for its job-name set. |
| T-73-19 | Repudiation | an HTTP 5xx from the dispatch API hiding a created run, followed by a duplicate dispatch | medium | mitigate | Every dispatch call is followed by a run listing before any retry; a retry is allowed only when the call errored and no matching row appears; a duplicate is recorded honestly and escalated, never cancelled or deleted. |
| T-73-20 | Denial of Service | a stale `uv.lock` failing every lane before any signal (ASVS V14, L1) | medium | mitigate | The locked sync must exit 0 before the push; drift halts, and the lock is never regenerated. |
| T-73-21 | Tampering | a builder or workflow change riding on the pushed tip (constraints 2, 3) | high | mitigate | Empty diff from `MILESTONE_BASE` over `typsphinx/` and `.github/workflows/` at `PUSHED_SHA`, a job-name set equal to Phase 72's run, and required checks equal to Phase 72's base reading. |
| T-73-SC | Tampering | package installs | low | accept | Provisioning and the locked check install only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See `73-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`73-CI-EVIDENCE.md`, with the keys `BASE_73_04`, `SCRATCH_73_04`, `PUSHED_SHA`, `LOCK_RUFF_VERSION`,
`ORIGIN_BEFORE`, `PUSH_AT`, `RUN_ID`, `RUN_URL`, `RUN_HEAD_SHA`, `RUN_CONCLUSION`, `JOB_COUNT`,
`NON_SUCCESS_JOBS`, `JOB_NAMES_MATCH_PHASE72`, `CI_RUFF_VERSION`, `DISPATCH_COUNT`,
`RELEASE_RUNS_AT_PUSHED`, `REQUIRED_STRICT_CLOSE`, `REQUIRED_CONTEXTS_CLOSE`,
`REQUIRED_CHECKS_UNCHANGED` and `SC3_CI_VERDICT`. Outside the repository it fast-forwards
`origin/gsd/v0.9.5-docs-link-check-and-navigation` to `PUSHED_SHA` and creates one
`workflow_dispatch` CI run. It modifies no product-tree file. No file named `73-VERIFICATION.md` is
produced.
</artifacts_this_phase_produces>

<verification>
- The canonical branch is on origin at `PUSHED_SHA` as a fast-forward, with no decoy and no tag. The
  pushed tree carries both new CHANGELOG subsections, and nothing under `typsphinx/` or the
  workflows changed.
- One `workflow_dispatch` CI run at `PUSHED_SHA` completed with every job green, both
  `windows-latest` and both `macos-latest` lanes named, the job set equal to Phase 72's, and ruff's
  verdict quoted from `Run lint with tox` at the lock's version.
- No `release.yml` run at `PUSHED_SHA`, exactly one CI dispatch, and `main`'s required checks
  unchanged since Phase 72's base.
</verification>

<success_criteria>
ROADMAP SC#3's CI half: one fresh three-OS run dispatched on this phase's own tip, every job
conclusion transcribed, both `windows-latest` and both `macos-latest` lanes named, and `ruff` green
in `Lint and Format Check` (D-12), with the required contexts equal to the six read at Phase 72's
base (D-09).
</success_criteria>

<output>
Create `.planning/phases/73-v0-9-5-close-prep-prep-only-unpublished/73-04-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []` (D-08).
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`, and do not
  run any GSD state-writing verb.
- The first section states `PUSHED_SHA`, `ORIGIN_BEFORE`, `RUN_ID`, `RUN_URL`, `JOB_COUNT`,
  `NON_SUCCESS_JOBS`, `JOB_NAMES_MATCH_PHASE72`, `CI_RUFF_VERSION`, `DISPATCH_COUNT` and
  `SC3_CI_VERDICT`. If the run was not all green, or more than one run sits at `PUSHED_SHA`, that
  section says so first and states that SC#3 awaits the owner.
</output>
