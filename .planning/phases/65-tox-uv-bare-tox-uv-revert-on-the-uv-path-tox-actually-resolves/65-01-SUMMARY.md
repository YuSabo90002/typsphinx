---
phase: 65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves
plan: 01
subsystem: toolchain
tags: [tox, tox-uv, tox-uv-bare, uv, nixos, fhs, pytest-gate]

# Dependency graph
requires:
  - phase: 64-fhs-wrapper-and-command-shims-in-flake-nix
    provides: typsphinx-fhs-run sandbox and the uv/tox command shims that make the tox-uv bundled uv wheel executable on NixOS
provides:
  - pyproject.toml dev extra declaring tox-uv (in place of tox-uv-bare)
  - tox.ini comma-free requires = tox-uv~=1.35
  - uv.lock regenerated by one plain uv lock, carrying tox-uv, tox-uv-bare (transitive) and uv
  - tests/test_toolchain_config_gate.py G5 gate inverted (test_dev_extra_pins_tox_uv_not_tox_uv_bare)
  - 65-REVERT-EVIDENCE.md — head check, RED/GREEN gate, lock regen, TOX-02 parser proof, D-02 inside-FHS observation, D-03 outside-FHS control, SC#3 readings, requirement closure
affects: [65-02 (pushes this tip and dispatches CI for TOX-04), 68 (tox.ini/CLAUDE.md/flake.nix documentation follow-through)]

# Actuals (#2632) — pairs with the plan's estimate to calibrate future estimates.
actuals:
  tokens: 12513
  tasks: 3
  commits: 4

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Evidence recorded verbatim in *-EVIDENCE.md (Phase 64 D-05 convention) — no new script, no new test file for D-02/D-03 observation"
    - "tox -vv (verbosity >= 4) makes tox-uv append -v to its own uv venv/uv sync subprocess calls, surfacing uv's own version banner with zero committed tox.ini edits"
    - "Isolated outside-FHS control built on the nix python3 store-path interpreter (never the worktree's own uv-managed .venv), to isolate the uv-exec failure from an earlier interpreter-exec failure"

key-files:
  created:
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md
  modified:
    - pyproject.toml
    - tox.ini
    - uv.lock
    - tests/test_toolchain_config_gate.py

key-decisions:
  - "D-01: SC#3's literal 'different function on a different code path' / 'bundled wheel vs shimmed binary' framing is falsified by measurement — both mechanisms name the same file (find_uv_bin() returns the calling interpreter's own bin/uv). The amended reading (observe branch/path/version from inside a real tox run, plus an isolated outside-FHS control) is what closes SC#3, and both readings are reported separately in the evidence."
  - "D-02: The bundled-branch resolution to this worktree's own .venv/bin/uv, with the tox environment passing, closes SC#3 with no TOX_UV_PATH set anywhere. Observed via tox -vv -e py312 -r; DIVERGENT protocol never triggered."
  - "D-03: The isolated control must run from a venv built on the nix python3 store-path interpreter (never the worktree's uv-managed .venv, which itself fails on the interpreter and isolates nothing). Reproduced exactly: control fails on the uv venv exec with exit 127, nothing earlier."
  - "D-04: uv.lock regenerated by one plain uv lock with no --upgrade/--upgrade-package/hand edit; ride-along bumps (tox-uv-bare 1.35.2 -> 1.36.0, new uv 0.12.13 block) recorded as-is."
  - "D-06 AMENDED: the Phase 45.2 static gate test_dev_extra_pins_tox_uv_bare_not_tox_uv, which asserted the literal opposite of this revert, is inverted (renamed, assertions swapped, docstrings rewritten) in the same commit as the three pin/lock files, keeping the tree green at every commit boundary."

patterns-established:
  - "Gate inversion follows RED-then-GREEN even outside TDD plans: the test was renamed and its assertions swapped BEFORE the pin moved, recorded RED (1 failed, 3 passed), then GREEN (4 passed) after the swap+lock — giving positive proof the new assertion direction actually fires."

requirements-completed: [TOX-01, TOX-02, TOX-03]

coverage:
  - id: D1
    description: "TOX-01 revert commit — pyproject.toml/tox.ini pin swapped to tox-uv, uv.lock regenerated by plain uv lock in the same commit, uv sync --extra dev --locked and uv lock --check both exit 0, and the inverted G5 gate goes RED then GREEN"
    requirement: "TOX-01"
    verification:
      - kind: integration
        ref: "65-01-PLAN.md Task 1 <verify> automated block (re-run live against HEAD, all conditions PASS)"
        status: pass
      - kind: unit
        ref: "tests/test_toolchain_config_gate.py::test_dev_extra_pins_tox_uv_not_tox_uv_bare (and the other 3 tests in the file) — 4 passed"
        status: pass
    human_judgment: false
  - id: D2
    description: "TOX-02 tox starts on the comma-free tox-uv~=1.35 requires form; the comma-bearing form is rejected with InvalidRequirement"
    requirement: "TOX-02"
    verification:
      - kind: integration
        ref: "tox --showconfig -e py312 (exit 0) / tox config -e py312 --core -k requires (lists '  tox-uv~=1.35') / scratch comma-form negative control (exit 1, InvalidRequirement)"
        status: pass
    human_judgment: false
  - id: D3
    description: "TOX-03 — the uv tox actually resolves from inside a real cold tox -vv -e py312 -r run is the worktree's own .venv/bin/uv at exactly the uv.lock version (bundled branch, no TOX_UV_PATH, no PATH-fallback), and the isolated outside-FHS control (nix-interpreter venv) fails on the uv venv exec with exit 127 and nothing earlier"
    requirement: "TOX-03"
    verification:
      - kind: integration
        ref: "65-01-PLAN.md Task 2 <verify> automated block (live cold tox -vv -e py312 -r --notest re-run, all conditions PASS)"
        status: pass
      - kind: integration
        ref: "65-01-PLAN.md Task 3 <verify> automated block (live isolated control re-run, all conditions PASS)"
        status: pass
    human_judgment: false

duration: 18min
completed: 2026-09-12
status: complete
---

# Phase 65 Plan 01: tox-uv-bare -> tox-uv Revert Summary

**One four-file commit swaps the dev-extra/tox.ini pin from `tox-uv-bare` back to upstream `tox-uv`, with `uv.lock` regenerated by a plain `uv lock` and the Phase 45.2 static gate inverted in lockstep; a live cold `tox -vv -e py312 -r` proves the bundled branch resolves this worktree's own `.venv/bin/uv` inside the Phase 64 FHS sandbox, and an isolated nix-interpreter control proves the same branch fails outside it on the `uv` exec alone.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-09-12T07:29:47Z
- **Completed:** 2026-09-12T07:47:04Z
- **Tasks:** 3 completed
- **Files modified:** 5 (`pyproject.toml`, `tox.ini`, `uv.lock`, `tests/test_toolchain_config_gate.py`, `65-REVERT-EVIDENCE.md`)

## Accomplishments

- Reverted `pyproject.toml`'s dev extra and `tox.ini`'s `requires` line from `tox-uv-bare` back to `tox-uv`, one token each, with `uv.lock` regenerated by a single plain `uv lock` (`Added tox-uv v1.36.0`, `Updated tox-uv-bare v1.35.2 -> v1.36.0`, `Added uv v0.12.13`) in the same commit — TOX-01.
- Inverted the Phase 45.2 static gate `test_dev_extra_pins_tox_uv_bare_not_tox_uv` -> `test_dev_extra_pins_tox_uv_not_tox_uv_bare` (both assertions swapped, docstrings rewritten), recorded RED against the pre-revert tree (`1 failed, 3 passed`) then GREEN after the swap (`4 passed`) — D-06 AMENDED.
- Confirmed `tox` starts on the comma-free `requires = tox-uv~=1.35` form via `tox --showconfig -e py312` and `tox config ... -k requires`, with a scratch negative control on the comma-bearing form failing with `InvalidRequirement` — TOX-02.
- From inside a real, cold `tox -vv -e py312 -r`, observed the bundled discovery branch resolving to this worktree's own `.venv/bin/uv`, at exactly the `uv.lock` `uv` version (`0.12.13`), with the full suite passing at baseline (1548 collected, 1543 passed, 5 skipped) — TOX-03 / D-02.
- Built an isolated control venv on the nix `python3` store-path interpreter (never the worktree's own uv-managed `.venv`), installed the four lock-pinned packages, and ran it outside FHS by absolute path: tox started, resolved the same bundled branch, and failed exactly on the `uv venv` exec (`Could not start dynamically linked executable`, exit 127) with nothing earlier failing — TOX-03 / D-03.
- Reported SC#3's literal reading (falsified — the "bundled wheel vs shimmed binary" dichotomy does not exist) and its amended reading (MET) separately, per D-01, and closed the requirement table with TOX-01/TOX-02/TOX-03 all MET.

## Task Commits

Each task was committed atomically (production commit + its own evidence commit):

1. **Task 1: Tracer — head check, gate RED/GREEN, pin swap, lock regen, locked sync, tox-uv end to end, four-file commit** - `d32eb5db` (fix) + `65930d72` (docs, evidence)
2. **Task 2: TOX-03 D-02 — cold tox -vv -e py312 -r inside FHS** - `0c471d78` (docs, evidence)
3. **Task 3: TOX-03 D-03 — isolated outside-FHS control, SC#3 readings, requirement closure** - `236b68a1` (docs, evidence)

**Plan metadata:** committed separately, see below.

_Note: Tasks 2 and 3 only touch the evidence markdown — no production code changes for those tasks._

## Files Created/Modified

- `pyproject.toml` - dev extra now names `tox-uv>=1.35,<2` (was `tox-uv-bare>=1.35,<2`), one line
- `tox.ini` - `requires = tox-uv~=1.35` (was `tox-uv-bare~=1.35`), one line; lines 4-10's rationale comment intentionally left stale (Phase 68 DOC-20)
- `uv.lock` - regenerated by one plain `uv lock`; `tox-uv` and `uv` package blocks added, `tox-uv-bare` bumped to match `tox-uv`'s version; header unchanged (`version = 1`, `revision = 3`)
- `tests/test_toolchain_config_gate.py` - `test_dev_extra_pins_tox_uv_bare_not_tox_uv` renamed to `test_dev_extra_pins_tox_uv_not_tox_uv_bare`, both G5 assertions swapped, function docstring and module docstring G5 paragraph rewritten for the Phase 65 revert
- `.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md` - head check, provisioning, gate RED/GREEN, lock regen, D-05 before/after, TOX-02 proof, D-02 observation, D-03 control, SC#3 readings, requirement closure

## Decisions Made

- Kept CLAUDE.md's now-stale "-bare package is deliberate" sentence untouched (Phase 68, DOC-19/DOC-20) — D-06.
- Used `tox -vv` (verbosity >= 4) to surface uv's own `-v` version banner inside the tox transcript, rather than adding any committed `tox.ini` observation edit — per D-02's explicit constraint.
- Built the D-03 control on the nix `python3` store-path interpreter rather than the worktree's own `.venv`, since the latter is itself a uv-managed generic-linux ELF and would fail on the interpreter before ever reaching the `uv` exec (exactly the failure the prior control suffered).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug in the plan's own automated `<verify>` scripts] `pytest -q`'s decorated summary line does not match the anchored `^N passed in ` / `^N tests collected` regex under this project's `addopts = "-v --strict-markers"`**
- **Found during:** Task 1's tracer-feedback-gate re-run of its own `<verify>` block, and again in Task 2's `<verify>` block
- **Issue:** `pyproject.toml`'s `[tool.pytest.ini_options]` sets `addopts = "-v --strict-markers"`. A single CLI `-q` therefore nets to verbosity 0 (one `-v` from addopts, one `-q` from the CLI), which produces pytest's normal decorated summary line (`============================== 4 passed in 0.02s ===============================`), never the bare `4 passed in 0.02s` / `1548 tests collected in 1.22s` line the plan's `grep -Eq '^4 passed in '` / `grep -Eq '^1548 tests collected'` anchors require. A bare `pytest -qq` (double-quiet) does produce the unanchored form, but the plan's literal `<verify>` invokes a single `-q`. This is an environmental mismatch in the plan's own verify scripts against this project's already-configured `addopts`, not a defect in the revert itself — the substantive proof (exactly 4 tests pass; exactly 1548 tests collected) holds and was independently confirmed via an unanchored grep and by the full task acceptance criteria.
- **Fix:** Ran the substance of both checks with the `^` anchor dropped (`grep -Eq '4 passed in '` / `grep -Eq '1548 tests collected'`), confirming the underlying data is correct; did not modify `pyproject.toml`'s `addopts` (out of scope, unrelated to this phase, and forbidden by D-06/constraint 13's file-footprint fence) or the plan text itself.
- **Files modified:** None — this is a verification-methodology adjustment, not a code change.
- **Verification:** Both the anchored substance check (unanchored grep) and the full recorded transcripts in `65-REVERT-EVIDENCE.md` (`## Gate GREEN and lint`, `## TOX-03 D-02 observation inside FHS`) show the exact `4 passed` / `1548 items` counts the plan's acceptance criteria require.
- **Committed in:** No separate commit — discovered and resolved during verification, no file changed.

**2. [Rule 1 - Bug in my own evidence draft] `<CTRL>`/`<CW>`/`<LOG>` placeholder tokens left in evidence transcripts instead of the real absolute paths**
- **Found during:** Task 3's own `<verify>` automated block, which requires the literal control-venv path to appear verbatim in the `## TOX-03 D-03 isolated control outside FHS` section
- **Issue:** While drafting the D-02/D-03 evidence sections I used symbolic placeholders (`<CTRL>`, `<CW>`, `<LOG>`) for readability, which broke the plan's own `grep -qF "using bundled uv from: $C/bin/uv"`-style literal-path assertions.
- **Fix:** `sed -i` replaced every `<CTRL>`/`<CW>`/`<LOG>` occurrence with the real absolute scratchpad paths throughout `65-REVERT-EVIDENCE.md`.
- **Files modified:** `.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md`
- **Verification:** Re-ran Task 3's full `<verify>` automated block after the fix — `TASK3_VERIFY: ALL PASS`.
- **Committed in:** `236b68a1` (part of the Task 3 evidence commit).

---

**Total deviations:** 2 auto-fixed (1 verify-script methodology note, 1 self-caught documentation bug). **Impact:** No scope creep, no product/config change beyond the plan's own four-file footprint; both were caught and resolved entirely inside the verification loop before any task was marked done.

## Issues Encountered

None beyond the two deviations above (both resolved inline).

## Authentication Gates

None — no external service or CLI login was needed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `REVERT_SHA = d32eb5db6219bf4917ab820a44562ba479e5fe71` and the full evidence trail are ready for plan 65-02, which pushes this worktree's tip (once merged) and dispatches one CI run for TOX-04.
- No DIVERGENT result occurred at any point — plan 65-02 is clear to proceed once this wave merges.
- The main checkout's own `.venv` still carries `tox-uv-bare` until an explicit `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` runs there post-merge (per `65-CONTEXT.md` D-05); this does not block anything since `tox`'s own `[tox] requires` provisioning self-heals via a side-car environment in the interval.

---
*Phase: 65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves*
*Completed: 2026-09-12*

## Self-Check: PASSED

- `65-01-SUMMARY.md` exists on disk — FOUND
- `65-REVERT-EVIDENCE.md` exists on disk — FOUND
- Commit `d32eb5db` (revert) — FOUND in `git log --oneline --all`
- Commit `65930d72` (Task 1 evidence) — FOUND
- Commit `0c471d78` (Task 2 evidence) — FOUND
- Commit `236b68a1` (Task 3 evidence) — FOUND
- All three tasks' `<acceptance_criteria>` re-verified against HEAD (Tasks 1-3 `<verify>` automated blocks re-run live): PASS
- All plan-level `<verification>` bullets re-confirmed: PASS
- `plan_head_before: cf605d8563e6d4d0017041b9196dc764936925d6`, `commits: 4` (measured via `git rev-list --count`)
