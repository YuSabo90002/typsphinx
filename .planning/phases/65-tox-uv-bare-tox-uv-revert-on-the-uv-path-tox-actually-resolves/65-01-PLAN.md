---
phase: 65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tox.ini
  - uv.lock
  - tests/test_toolchain_config_gate.py
  - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md
autonomous: true
requirements: [TOX-01, TOX-02, TOX-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Head check (Phase 64 D-09, CLAUDE.md): every task runs in a git worktree whose pwd -P lies under /home/yuta/Documents/typsphinx/.claude/worktrees/ with .git a file. command -v uv, tox, pytest, ruff and black each name a Phase 64 shim whose body contains typsphinx-fhs-run and whose rootfs holds /usr/lib/libz.so.1. The only provisioning command is env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev, run once on the pre-revert tree, and .venv/pyvenv.cfg home and version_info are recorded before any count is compared with a baseline.'
    - 'TOX-01 / SC#1: pyproject.toml line 38 reads "tox-uv>=1.35,<2", keeping the existing range, and the dev extra no longer names tox-uv-bare.'
    - 'TOX-01 / SC#1 / D-04: uv.lock is exactly what one plain uv lock produced after the edit, with no --upgrade, no --upgrade-package, no pin to 1.35.2 and no hand edit. Its header stays version = 1 / revision = 3. It carries tox-uv, tox-uv-bare at the same version as tox-uv, and uv. The Added/Updated lines are recorded verbatim, with the uv version that ran the regeneration (the shim second leg, nixpkgs uv, because .venv/bin/uv does not exist yet).'
    - 'TOX-01 / SC#1 / D-06 AMENDED: one commit carries exactly pyproject.toml, tox.ini, uv.lock and tests/test_toolchain_config_gate.py; git show --name-only on it lists pyproject.toml and uv.lock together. env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked exits 0 on that tree, and uv lock --check leaves the lock byte-identical under the lock-pinned uv.'
    - 'D-06 AMENDED: the Phase 45.2 gate is inverted, not deleted. It is renamed test_dev_extra_pins_tox_uv_not_tox_uv_bare and asserts that the dev extra names tox-uv and does not name tox-uv-bare directly, both on canonicalize_name-normalized names. The Requirement parse loop and its raise-from chaining are unchanged. It is recorded RED against the pre-revert pyproject.toml (1 failed, 3 passed) before the pin moves and GREEN after (4 passed); the file still defines exactly four tests.'
    - 'D-06: across the revert commit tox.ini changes by exactly one line (the requires value), so its lines 4-10 rationale comment still names tox-uv-bare for Phase 68 (DOC-20). CLAUDE.md, flake.nix, flake.lock, .github/ and typsphinx/ are byte-identical across it.'
    - 'TOX-02 / SC#2: tox.ini line 11 reads requires = tox-uv~=1.35. tox --showconfig -e py312 exits 0, and tox config -e py312 --core -k requires lists the line "  tox-uv~=1.35". tox --version is never offered as this evidence (RESEARCH Pitfall 1).'
    - 'TOX-02 (empty edge): the requires value is a single entry with no comma, and tox parses it into exactly one tox-uv requirement beside its implicit tox entry. A scratch negative control of the comma form, never committed, exits 1 with InvalidRequirement.'
    - 'TOX-02 (encoding edge): equality follows packaging definitions, never raw strings. Distribution names compare via canonicalize_name (Tox_UV normalizes to tox-uv), and the specifier ~=1.35 admits exactly what >=1.35,<2 admits over the probe versions 1.34.9, 1.35.0, 1.36.0, 1.99.0 and 2.0.0.'
    - 'D-05: through the shim, uv --version prints the nixpkgs uv before the locked sync, while .venv/bin/uv is absent, and exactly the uv.lock uv version after uv sync --extra dev --locked installs .venv/bin/uv. The shim second leg stays, and flake.nix is not edited.'
    - 'TOX-03 / SC#3 / D-02: one cold tox -vv -e py312 -r through the shim in this worktree logs "using bundled uv from:" followed by this worktree .venv/bin/uv. Every uv -v banner in that log reads DEBUG uv followed by the uv.lock uv version. The log carries no TOX_UV_PATH-branch line, no PATH-branch line and no tox self-provisioning line, and it ends py312: OK with collected 1548 items and 1543 passed, 5 skipped.'
    - 'D-02 DIVERGENT: if the log instead shows the TOX_UV_PATH branch or the PATH branch, a resolved path other than this worktree .venv/bin/uv, or a banner version other than the uv.lock one, the executor records it verbatim under a DIVERGENT heading and stops at a blocking checkpoint. It never sets TOX_UV_PATH, never returns to tox-uv-bare, and never edits flake.nix or tox.ini to make the observation match.'
    - 'D-02: TOX_UV_PATH is unset in every command of this plan and appears in no tracked file outside .planning/.'
    - 'TOX-03 / D-03: the isolated control runs outside FHS, where test -e /usr/lib/libz.so.1 fails in the same shell. It runs from a venv built on the nix python3 (readlink -f and pyvenv.cfg home both under /nix/store/), kept outside the tracked tree, with tox, tox-uv, tox-uv-bare and uv at exactly the uv.lock versions. Invoked by absolute path against this worktree tox.ini, tox starts and logs "using bundled uv from:" followed by the control venv bin/uv. Its first failing subprocess is that uv venv call, with Could not start dynamically linked executable and exit 127; nothing fails earlier.'
    - 'D-01: the evidence reports the literal reading and the AMENDED reading of SC#3 separately, and .planning/research/PITFALLS.md is not amended.'
  artifacts:
    - path: pyproject.toml
      provides: 'dev extra declaring tox-uv in place of tox-uv-bare (TOX-01)'
      contains: '"tox-uv>=1.35,<2",'
    - path: tox.ini
      provides: 'comma-free requires on tox-uv (TOX-02)'
      contains: 'requires = tox-uv~=1.35'
    - path: uv.lock
      provides: 'lock regenerated by one plain uv lock, carrying tox-uv, tox-uv-bare and uv (TOX-01, D-04)'
      contains: 'name = "tox-uv"'
    - path: tests/test_toolchain_config_gate.py
      provides: 'the inverted G5 gate (D-06 AMENDED)'
      contains: 'def test_dev_extra_pins_tox_uv_not_tox_uv_bare'
    - path: .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md
      provides: 'head check, lock regeneration, gate RED/GREEN, TOX-02 start proof, the D-02 observation, the D-03 control, SC#3 literal and amended readings, requirement closure'
      contains: 'using bundled uv from:'
  key_links:
    - from: 'pyproject.toml [project.optional-dependencies].dev'
      to: 'uv.lock'
      via: 'one plain uv lock, staged in the same commit (constraint 13)'
      pattern: 'name = "tox-uv"'
    - from: 'tox.ini [tox] requires'
      to: 'tox startup'
      via: 'the tox ini-list loader, which splits a single-line value on commas'
      pattern: 'tox-uv~=1.35'
    - from: 'tox_uv/_venv.py uv property, bundled branch'
      to: 'this worktree .venv/bin/uv (the PyPI uv package script)'
      via: 'from uv import find_uv_bin, called from the tox interpreter itself, executed inside typsphinx-fhs-run'
      pattern: 'using bundled uv from:'
    - from: 'tests/test_toolchain_config_gate.py inverted gate'
      to: 'pyproject.toml dev extra'
      via: '_load_pyproject, Requirement and canonicalize_name'
      pattern: 'canonicalize_name\("tox-uv"\) in dev_names'
  prohibitions:
    - statement: 'MUST NOT set TOX_UV_PATH in flake.nix, in a tox.ini setenv, in the shell, or in any command of this plan (D-02).'
      status: resolved
      verification: test
    - statement: 'MUST NOT self-recover a DIVERGENT D-02 or D-03 result: no TOX_UV_PATH, no return to tox-uv-bare, and no flake.nix or tox.ini edit made to force the expected observation. Record and halt.'
      status: resolved
      verification: judgment
    - statement: 'MUST NOT hand-edit uv.lock, pass --upgrade or --upgrade-package, pin tox-uv or tox-uv-bare to 1.35.2, or commit pyproject.toml in a commit that lacks uv.lock (D-04, constraint 13).'
      status: resolved
      verification: test
    - statement: 'MUST NOT edit tox.ini lines 4-10, CLAUDE.md, flake.nix, flake.lock, any file under .github/ or typsphinx/, and MUST NOT delete or weaken the Phase 45.2 gate (D-06 and its AMENDED block, constraints 3 and 13).'
      status: resolved
      verification: test
    - statement: 'MUST NOT commit the control venv, the scratch comma-form tox.ini or any tox log, and MUST NOT add a script or a test to record the D-02 or D-03 observation (Phase 64 D-05 convention).'
      status: resolved
      verification: test
    - statement: 'MUST NOT push, dispatch a workflow, open a pull request or create a tag; TOX-04 belongs to plan 65-02.'
      status: resolved
      verification: test
    - statement: 'MUST NOT absorb SEED-003 or change any other dependency declaration while pyproject.toml is open.'
      status: resolved
      verification: judgment
---

<objective>
Perform the `tox-uv-bare` → `tox-uv` revert and prove it on this machine.

1. One commit swaps the pin in `pyproject.toml:38` and in `tox.ini`'s `requires` line. The same commit carries `uv.lock`, regenerated by a plain `uv lock`, and the Phase 45.2 gate, inverted per D-06's AMENDED block (TOX-01, TOX-02).
2. From inside a real, cold `tox -vv -e py312 -r`, observe which `uv` tox resolved: the branch that fired, the path and the version (D-02).
3. Run the isolated control from a nix-interpreter venv outside FHS. It must fail on the uv exec and on nothing earlier (D-03, TOX-03).

Purpose: SC#1–SC#3 of Phase 65. The revert itself is a package-name swap; the proof is what this phase is for (ROADMAP constraint 12 and its AMENDED block). Plan 65-02 pushes this tip and dispatches CI (TOX-04). It reads this plan's evidence and must not push a revert whose SC#3 proof is DIVERGENT, which is why it sits in a later wave.

Decisions implemented here:
- D-01: the literal and the amended SC#3 readings are reported separately.
- D-02: the observation, the version-from-inside requirement, no `TOX_UV_PATH`, and DIVERGENT → HALT.
- D-03: the nix-interpreter control.
- D-04: plain `uv lock`, ride-along bumps recorded.
- D-05: the `uv` before/after, with the shim's second leg kept.
- D-06 and its AMENDED block: the four-file footprint and the gate inversion.

Output: the revert commit (`REVERT_SHA`) and `65-REVERT-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CONTEXT.md
@.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-RESEARCH.md
@.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-PATTERNS.md
@CLAUDE.md
@pyproject.toml
@tox.ini
@tests/test_toolchain_config_gate.py
</context>

<assumption_delta_decision>
Noun now primary: the tox plugin distribution declared in the dev extra (`tox-uv`). Decision: `no-change`. The detector's only signal is the word "chosen" in "the defect `-bare` was chosen to avoid". That describes a historical package choice. No configuration decision becomes a modeling decision, and this phase has no identity model to re-anchor.
</assumption_delta_decision>

<!-- planner-discipline-allow: using system uv from PATH -->
<!-- planner-discipline-allow: using uv from TOX_UV_PATH -->
<!-- planner-discipline-allow: automatically provisioned -->
<!-- planner-discipline-allow: TOX_UV_PATH -->
<!-- planner-discipline-allow: test_dev_extra_pins_tox_uv_bare_not_tox_uv -->
<!-- planner-discipline-allow: def test_dev_extra_pins_tox_uv_bare_not_tox_uv -->
<!-- planner-discipline-allow: ## DIVERGENT -->
<!-- planner-discipline-allow: ^## DIVERGENT -->
<!-- planner-discipline-allow: NOT MET -->
The three tox-uv log texts are named in actions because the executor must grep for them; the gates negative-grep them only inside the recorded D-02/D-03 transcripts. The Phase 45.2 test name is named because it is renamed. `DIVERGENT` and `NOT MET` are headings/words the executor must be able to write when the result diverges, and the gates require their absence.

## Carried-in facts (planner measurements, 2026-09-12 — re-measure, never inherit)

| Fact | Value | Source |
|------|-------|--------|
| Current pin | `pyproject.toml:38` = `"tox-uv-bare>=1.35,<2",`; `tox.ini:11` = `requires = tox-uv-bare~=1.35` | planner `grep -n` |
| Current lock | `uv.lock:1415-1416` `tox-uv-bare` `1.35.2`; zero `name = "uv"` blocks; header `version = 1` / `revision = 3`; `tox` `4.56.1` at `uv.lock:1394-1395` | planner `grep -n` |
| `tox config` output shape | `tox config -e py312 --core -k requires` prints `requires =` then `  tox-uv-bare~=1.35` then `  tox`, exit 0; `tox --showconfig -e py312` exit 0 in 0.36 s | planner probe, main checkout |
| Comma form | a scratch copy with `requires = tox-uv>=1.35,<2`, run as `tox -c <scratch>/tox.ini --showconfig -e py312`, exits 1 with `packaging.requirements.InvalidRequirement: Expected package name at the start of dependency specifier` | planner probe, scratchpad |
| tox-uv log texts | `_venv.py:229` warning `using uv from TOX_UV_PATH: %s (%s)`; `:237` debug `using bundled uv from: %s`; `:251` debug `using system uv from PATH: %s (%s)`; `_run_lock.py:130-131` appends `-v` to `uv` when verbosity > 3 (installed 1.35.2; line numbers may move in 1.36.x) | installed source |
| Pre-revert worktree tox run | `py312: venv> /nix/store/3vpz…-uv/bin/uv venv …`: the PATH branch resolved the nixpkgs uv | `64-GAP-REMEASURE-EVIDENCE.md:255` |
| Suite baseline | 1548 collected; `1543 passed, 5 skipped` under bare pytest (cp3.14) and under `tox -e py312` (3.12.13) | `64-GAP-REMEASURE-EVIDENCE.md:190,261` |
| Shim second leg | nixpkgs uv `0.11.25` | `65-CONTEXT.md` D-05 |
| Lock forecast (not evidence) | `Added tox-uv v1.36.0` / `Updated tox-uv-bare v1.35.2 -> v1.36.0` / `Added uv v0.12.13` | `65-RESEARCH.md` |
| Session environment | `VIRTUAL_ENV`, `UV_PROJECT_ENVIRONMENT`, `TOX_UV_PATH` all unset; `git grep TOX_UV_PATH` outside `.planning/`: no match | planner probe |
| nix interpreter on PATH | `/nix/store/l9k0anq0z7zz81zcwy035jfwap9ga6rl-python3-3.13.13/bin` | planner PATH listing |
| `uv pip list` shape | `tox                           4.56.1` (name, spaces, version, optional trailing columns) | planner probe |

## Edge coverage lift (spec-less fallback — all 7 probe items accounted for)

| # | Requirement | Category | Disposition | Where |
|---|-------------|----------|-------------|-------|
| 1 | TOX-01 | unclassified | flagged assumption | § Flagged edge-probe assumptions |
| 2 | TOX-02 | empty | resolved, explicit | truth "TOX-02 (empty edge)" |
| 3 | TOX-02 | encoding | resolved, explicit | truth "TOX-02 (encoding edge)" |
| 4 | TOX-03 | unclassified | flagged assumption | § Flagged edge-probe assumptions |
| 5 | TOX-04 | adjacency | resolved, explicit | `65-02-PLAN.md` truth "TOX-04 (adjacency edge)" |
| 6 | TOX-04 | empty | resolved, explicit | `65-02-PLAN.md` truth "TOX-04 (empty edge)" |
| 7 | TOX-04 | ordering | resolved, backstop | `65-02-PLAN.md` structured backstop truth |

Seven surfaced: five authored as truths across the two plans, plus two flagged here.

## Flagged edge-probe assumptions (unclassified — surfaced, never silently dropped)

- **TOX-01 (unclassified).** "Regenerated in lockstep" is read as three things: the same commit, a lock produced by the tool rather than by hand, and a lock that `uv sync --extra dev --locked` accepts. It is **not** read as "no other package moves", because D-04 accepts ride-along bumps and records them. The verifier may contest that reading.
- **TOX-03 (unclassified).** The D-02 observation and the D-03 control are assumed independent of the order they run in. Each resolves `uv` from its own interpreter's `bin/` via `find_uv_bin()`, and the control uses its own `--workdir` outside the tree. This plan runs them in one order only.
- **Interpreter provenance.** The worktree `.venv` is uv-managed cp3.14 and the `py312` tox environment is uv-managed 3.12.13, while one 1543 / 5 baseline was taken under nix 3.13.13. Any divergence attributable to that is recorded as such and never normalized by pinning an interpreter.

## Prohibition recall (spec-less fallback, stage 2 survivors)

The survivors are the seven `must_haves.prohibitions` above, authored descriptor-less. The integrity of the newly locked `uv` wheel is canon dependency security, covered by `/gsd-secure-phase` and T-65-SC below, and is not minted as a prohibition.

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| the revert commit, recorded as `REVERT_SHA = <sha>` | one commit touching exactly `pyproject.toml`, `tox.ini`, `uv.lock`, `tests/test_toolchain_config_gate.py` | canonical branch after merge |
| `test_dev_extra_pins_tox_uv_not_tox_uv_bare` | renamed and inverted test function; it replaces the Phase 45.2 function name | `tests/test_toolchain_config_gate.py` |
| `tox-uv` and `uv` package blocks | new `uv.lock` entries; `uv sync --extra dev` now installs `.venv/bin/uv` | `uv.lock` |
| `65-REVERT-EVIDENCE.md` | evidence for SC#1–SC#3, with the `REVERT_SHA = `, `LOCK_REGENERATED_BY = ` and `CTRL_VENV = ` lines and a `## Requirement closure` table | phase directory |

No script, no new test file and no committed venv are created.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — head check, provision the pre-revert tree, invert the gate RED, swap the pin, one plain uv lock, locked sync, gate GREEN, tox starts on tox-uv end to end, one four-file commit</name>
  <precondition>This worktree's HEAD still carries the pre-revert pin: `grep -c 'tox-uv-bare>=1.35,<2' pyproject.toml` prints 1 and `grep -c '^name = "uv"$' uv.lock` prints 0. `command -v uv` names a file whose body contains `typsphinx-fhs-run`. If either check fails, halt at a blocking-human gate: the revert has already landed, or the session lacks the Phase 64 shims.</precondition>
  <reversibility rating="reversible">One commit over four files, undone with one git revert. uv.lock regenerates deterministically from pyproject.toml, and nothing is published.</reversibility>
  <files>pyproject.toml, tox.ini, uv.lock, tests/test_toolchain_config_gate.py, .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md</files>
  <read_first>
    - CLAUDE.md § "Worktree-isolated execution" (the provisioning line verbatim; the editable-install hazard) and § "Conventions & gotchas" (the `~=` rationale)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CONTEXT.md (D-01 to D-06, including D-06's AMENDED block)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-RESEARCH.md § "Code Examples" (lock regeneration), § "Common Pitfalls" 1 and 3, § "Decision Conflicts"
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-PATTERNS.md § pyproject/tox.ini and § the gate inversion
    - pyproject.toml (line 38), tox.ini (lines 1-11), uv.lock (lines 1-3 and 1394-1416)
    - tests/test_toolchain_config_gate.py in full (module docstring lines 1-65, G5 paragraph lines 35-49, the gate at lines 269-362, the helpers at 79-115)
    - `git show e603f36e` (the Phase 45.2 inverse commit; mirror its commit-message structure, inverted)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md lines 1-140 (head-check and provisioning transcript shape)
  </read_first>
  <action>
Record every command below with its verbatim output in `65-REVERT-EVIDENCE.md`, in the `$ <command>` + output + one interpretive sentence convention (Phase 64 D-05). Do not add a script or a test to do the recording.

1. **Head check, before anything is provisioned.** Record each of:
   - `date -u +%FT%TZ`
   - `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git`, `test ! -e .venv` and `test ! -e .tox`
   - for each of `uv tox pytest ruff black`: `command -v <tool>` plus `grep -c typsphinx-fhs-run` over that file, which must be at least 1
   - the FHS rootfs check the 64-06 verify used: the `typsphinx-fhs-run` path grepped from the body of `command -v ruff`, run as `"$FHS" /bin/sh -c 'test -e /usr/lib/libz.so.1'; echo "exit:$?"`, which must print `exit:0`
   - `printenv TOX_UV_PATH; echo "exit:$?"`, which must print `exit:1` (unset, per D-02)

2. **Provision the pre-revert tree, exactly as CLAUDE.md documents:** `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, with no interpreter flag. Record:
   - `cat .venv/pyvenv.cfg`, naming its `home` and `version_info`
   - the tree identity `uv run python -c 'import typsphinx,os;print(os.path.realpath(typsphinx.__file__))'`, which must equal `$(pwd -P)/typsphinx/__init__.py`
   - `test ! -e .venv/bin/uv; echo "exit:$?"`, which must print `exit:0`, because tox-uv-bare pulls in no `uv` package
   - `uv --version`: this is the D-05 **before** value, taken through the shim's second leg (nixpkgs uv, 0.11.25 at planning)

3. **Invert the gate first, so that it is recorded RED against the unchanged `pyproject.toml`.** This implements D-06's AMENDED block. In `tests/test_toolchain_config_gate.py`:
   - Rename the function `test_dev_extra_pins_tox_uv_bare_not_tox_uv` to `test_dev_extra_pins_tox_uv_not_tox_uv_bare`.
   - In the two G5 assertions, swap the string literals passed to `canonicalize_name(...)`. The first assertion then requires `tox-uv` to be in `dev_names`, and the second requires `tox-uv-bare` to be absent from it.
   - Leave untouched the vacuous-pass guards, the `Requirement` parse loop, its `raise AssertionError(...) from e`, and the `canonicalize_name` mechanism.
   - Rewrite the two assertion messages to describe the Phase 65 revert. For the first: the Phase 64 FHS shims run the bundled `.venv/bin/uv` inside the `typsphinx-fhs-run` sandbox, so the QUA-04 stub-loader defect no longer bites. Without `tox-uv`, tox-uv's bundled branch is absent and tox falls back to whichever `uv` is on PATH, so CI's tox would run setup-uv's uv instead of the lock-pinned one. For the second: naming `tox-uv-bare` directly is the stale Phase 45.2 workaround pin. `tox-uv-bare` stays present only as `tox-uv`'s own exact-version dependency in `uv.lock`, which is expected. Cite `65-REVERT-EVIDENCE.md`.
   - Rewrite the function docstring in the same direction. Explain that `tox-uv`'s bundled step is `uv.find_uv_bin()`, which returns the tox interpreter's own `bin/uv` (65-CONTEXT D-01). Keep the "naming trap" and "Scope" paragraphs as they are. Replace the CLAUDE.md quotation with a sentence stating that CLAUDE.md's `-bare` sentence stays stale until Phase 68 (DOC-19/DOC-20) rewrites it.
   - Rewrite the module docstring's **G5 paragraph only** (lines 35-49) to match. Leave the module intro, the G3, G4 and G6 paragraphs, the Implementation paragraph and the other three test functions byte-identical.
   - Run `uv run pytest tests/test_toolchain_config_gate.py -q` and record it. It must read exactly `1 failed, 3 passed`, with the failure being the renamed test and its new first message. This is the RED. The pin has not moved yet, so this `uv run`'s implicit lock is a no-op.

4. **Swap the pin.**
   - `pyproject.toml` line 38 becomes `    "tox-uv>=1.35,<2",`, keeping the four-space indent and the trailing comma.
   - `tox.ini` line 11 becomes `requires = tox-uv~=1.35`.
   - Change no other byte of either file. `tox.ini` lines 4-10 keep naming `tox-uv-bare`; they are DOC-20's in Phase 68 (D-06).

5. **Regenerate the lock, with the uv that runs it observable (D-04, D-05).** Run no `uv run`, no `uv sync` and no `tox` between step 4 and this step. Record:
   - `test ! -e .venv/bin/uv; echo "exit:$?"` and `uv --version`
   - `sha256sum uv.lock`
   - then plain `uv lock`, with no flags, and its complete output

   Add the line `LOCK_REGENERATED_BY = <uv --version output>`. After it, record:
   - `sed -n 1,3p uv.lock`
   - `grep -A1` over `^name = "tox-uv"$`, `^name = "tox-uv-bare"$` and `^name = "uv"$` in `uv.lock`
   - `git diff --stat uv.lock`

   Record the `Added`/`Updated` lines literally, whatever versions they name at run time. Pass no `--upgrade`, no `--upgrade-package` and no pin, and make no hand edit.

6. **Locked sync and the D-05 after.** Run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked`; it must exit 0. Then record:
   - `test -x .venv/bin/uv; echo "exit:$?"`
   - `uv --version`, which must equal the `uv.lock` `uv` version: the shim's first leg now resolves `.venv/bin/uv`
   - `uv lock --check`, which must exit 0
   - `sha256sum uv.lock`, which must equal the post-`uv lock` digest: the lock-pinned uv leaves the lock byte-identical. That is the adjacency of two uv versions over one lock.

7. **GREEN, lint, and TOX-02.** Record each of these:
   - `uv run pytest tests/test_toolchain_config_gate.py -q`, which must read `4 passed`
   - `ruff check .` and `black --check .` through the shims, which must be clean. If black reformats the rewritten messages, run `black tests/test_toolchain_config_gate.py`, then re-run both checks.
   - `tox --showconfig -e py312 > /dev/null; echo "exit:$?"`, which must print `exit:0`
   - `tox config -e py312 --core -k requires`, which must list `  tox-uv~=1.35`
   - the negative control, run in the session scratchpad and never in the tree: copy `tox.ini` there with line 11 replaced by the comma form `requires = tox-uv>=1.35,<2`, then run `tox -c <scratch>/tox.ini --showconfig -e py312` from this worktree. It must exit 1 with `InvalidRequirement`; record its last four lines.
   - the encoding edge, as one `uv run python -c` command. It asserts that `canonicalize_name('Tox_UV') == 'tox-uv'`, and that `SpecifierSet('~=1.35')` and `SpecifierSet('>=1.35,<2')` agree on membership for `1.34.9`, `1.35.0`, `1.36.0`, `1.99.0` and `2.0.0`. It prints the five membership pairs.

8. **Tracer end to end: tox-uv drives one real environment through the shim.** Run `tox -e py312 -- tests/test_toolchain_config_gate.py`. It must end `py312: OK`. Record its `venv>` and `uv-sync>` lines: they must name `$(pwd -P)/.venv/bin/uv`, the bundled branch. The pre-revert run named the nixpkgs uv (`64-GAP-REMEASURE-EVIDENCE.md:255`).

9. **One commit carrying exactly the four files.**
   - Run `git add pyproject.toml tox.ini uv.lock tests/test_toolchain_config_gate.py` and commit with the subject `fix(65-01): revert tox-uv-bare to tox-uv now that the Phase 64 FHS shims dissolve QUA-04`.
   - Give it a bullet body mirroring `e603f36e`, inverted: the pyproject dev extra; the tox.ini `requires` line, with the comment left for DOC-20 per D-06; the gate inverted per D-06 AMENDED; and `uv.lock` regenerated by one plain `uv lock`, quoting its `Added`/`Updated` lines.
   - Record `git show --name-only --format='%H %s' HEAD`, and add the line `REVERT_SHA = <full sha>`.

10. Write `65-REVERT-EVIDENCE.md`.
   - Open it with the line `Measured shape: executor worktree, Phase 64 shims inherited from the session PATH; no nix develop, no direnv exec, never the main checkout.`
   - Then add the sections `## Head check`, `## Provisioning on the pre-revert tree`, `## Gate inversion RED`, `## TOX-01 lock regeneration`, `## D-05 uv before and after`, `## Gate GREEN and lint`, `## TOX-02 tox starts`, `## Tracer tox-uv end to end` and `## Revert commit`.
   - Use ASCII section headings and `###` for any subsection.

   Commit the evidence in its own `docs(65-01): ...` commit, so the revert commit stays at four files.
  </action>
  <verify>
    <automated>E=.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; [ -z "${TOX_UV_PATH+x}" ] &amp;&amp; { git grep -q TOX_UV_PATH HEAD -- . ':(exclude).planning'; [ $? -eq 1 ]; } &amp;&amp; R="$(sed -n 's/^REVERT_SHA = //p' "$E" | head -n 1)" &amp;&amp; [ -n "$R" ] &amp;&amp; N="$(git show --name-only --format= "$R")" &amp;&amp; [ "$(printf '%s\n' "$N" | sort | tr '\n' ' ')" = 'pyproject.toml tests/test_toolchain_config_gate.py tox.ini uv.lock ' ] &amp;&amp; git diff --quiet "$R^" "$R" -- CLAUDE.md flake.nix flake.lock .github typsphinx &amp;&amp; NT="$(git diff --numstat "$R^" "$R" -- tox.ini)" &amp;&amp; [ "$(printf '%s\n' "$NT" | cut -f1,2)" = "$(printf '1\t1')" ] &amp;&amp; NP="$(git diff --numstat "$R^" "$R" -- pyproject.toml)" &amp;&amp; [ "$(printf '%s\n' "$NP" | cut -f1,2)" = "$(printf '1\t1')" ] &amp;&amp; sed -n 38p pyproject.toml | grep -Fqx '    "tox-uv&gt;=1.35,&lt;2",' &amp;&amp; sed -n 11p tox.ini | grep -Fqx 'requires = tox-uv~=1.35' &amp;&amp; [ "$(sed -n 1,2p uv.lock | tr '\n' ' ')" = 'version = 1 revision = 3 ' ] &amp;&amp; TUV="$(grep -A1 '^name = "tox-uv"$' uv.lock | sed -n 's/^version = "\(.*\)"$/\1/p')" &amp;&amp; [ -n "$TUV" ] &amp;&amp; [ "$(grep -A1 '^name = "tox-uv-bare"$' uv.lock | sed -n 's/^version = "\(.*\)"$/\1/p')" = "$TUV" ] &amp;&amp; UVV="$(grep -A1 '^name = "uv"$' uv.lock | sed -n 's/^version = "\(.*\)"$/\1/p')" &amp;&amp; [ -n "$UVV" ] &amp;&amp; env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked &amp;&amp; uv lock --check &amp;&amp; test -x .venv/bin/uv &amp;&amp; uv --version | grep -Fq "uv $UVV" &amp;&amp; GT="$(uv run pytest tests/test_toolchain_config_gate.py -q 2&gt;&amp;1)" &amp;&amp; printf '%s\n' "$GT" | grep -Eq '^4 passed in ' &amp;&amp; for t in test_uv_venv_lock_runner_requires_extras_forbids_deps test_testenv_package_is_editable_not_wheel test_dev_extra_pins_tox_uv_not_tox_uv_bare test_runtime_dependencies_carry_no_toolchain_package; do grep -q "^def $t()" tests/test_toolchain_config_gate.py || { echo "missing test: $t"; exit 1; }; done &amp;&amp; ! grep -q 'def test_dev_extra_pins_tox_uv_bare_not_tox_uv' tests/test_toolchain_config_gate.py &amp;&amp; tox config -e py312 --core -k requires | grep -Fqx '  tox-uv~=1.35' &amp;&amp; grep -q '^## Gate inversion RED' "$E" &amp;&amp; grep -q '1 failed, 3 passed' "$E" &amp;&amp; grep -q 'InvalidRequirement' "$E" &amp;&amp; grep -q '^LOCK_REGENERATED_BY = uv [0-9]' "$E" &amp;&amp; grep -q 'Added tox-uv v' "$E" &amp;&amp; grep -q 'Added uv v' "$E" &amp;&amp; GS="$(git status --porcelain -- pyproject.toml tox.ini uv.lock tests typsphinx flake.nix flake.lock CLAUDE.md .github)" &amp;&amp; [ -z "$GS" ]</automated>
    <fails_when>non-zero exit, or `missing test: <name>` printed. That covers each of these:
- `pwd -P` outside the worktrees directory, or `TOX_UV_PATH` set or present in a tracked file outside `.planning/`
- no `REVERT_SHA` line, or the revert commit listing anything other than the four files
- any byte changed in `CLAUDE.md`, `flake.nix`, `flake.lock`, `.github/` or `typsphinx/` across that commit, or a `tox.ini` or `pyproject.toml` diff other than one line in, one line out
- line 38 or line 11 not the exact tox-uv form
- a lock header other than version 1 / revision 3, no `tox-uv` block, a `tox-uv-bare` version differing from `tox-uv`'s, or no `uv` block
- the locked sync or `uv lock --check` refusing
- `.venv/bin/uv` absent, or `uv --version` not naming the lock version
- the gate file not reading `4 passed`, any of its four test functions missing, or the old name still defined
- `tox config` not listing `  tox-uv~=1.35`
- the evidence missing the RED section, the `1 failed, 3 passed` summary, the InvalidRequirement negative control, the `LOCK_REGENERATED_BY` line, or the `Added tox-uv` and `Added uv` lines
- uncommitted changes under the scope-fenced paths</fails_when>
  </verify>
  <acceptance_criteria>
    - The head check records the worktree path, `.git` as a file, `.venv` and `.tox` absent, five shim bodies naming `typsphinx-fhs-run`, the rootfs libz test at `exit:0`, and `TOX_UV_PATH` unset (`exit:1`).
    - The provisioning transcript is exactly `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. `.venv/pyvenv.cfg`'s `home` and `version_info` are recorded, `typsphinx.__file__` resolves inside this worktree, and `.venv/bin/uv` is absent before the lock.
    - `## Gate inversion RED` shows `1 failed, 3 passed` against the pre-revert `pyproject.toml`, and the failing node is `test_dev_extra_pins_tox_uv_not_tox_uv_bare`.
    - The `uv lock` output is recorded verbatim, with `LOCK_REGENERATED_BY` naming the nixpkgs uv, and it contains `Added tox-uv v` and `Added uv v`. The header is `version = 1` / `revision = 3`. `tox-uv` and `tox-uv-bare` carry the same version.
    - D-05: `uv --version` before the sync names the nixpkgs uv. After it names the `uv.lock` uv version, and `.venv/bin/uv` exists. The lock's sha256 is identical before and after `uv lock --check` and the locked sync.
    - `## Gate GREEN and lint` shows `4 passed`, `ruff check .` clean and `black --check .` clean.
    - `## TOX-02 tox starts` shows `tox --showconfig -e py312` at `exit:0`, `tox config … -k requires` listing `  tox-uv~=1.35`, the scratch comma-form control at `exit:1` with `InvalidRequirement`, and the five SpecifierSet membership pairs agreeing.
    - `## Tracer tox-uv end to end` shows `py312: OK`, with `venv>` and `uv-sync>` naming `<this worktree>/.venv/bin/uv`.
    - `git show --name-only` on `REVERT_SHA` lists exactly `pyproject.toml`, `tests/test_toolchain_config_gate.py`, `tox.ini` and `uv.lock`.
  </acceptance_criteria>
  <done>The revert is one four-file commit. uv.lock was produced by a plain uv lock and is accepted by the locked sync. The inverted gate went RED then GREEN, tox starts on tox-uv, and one real tox environment ran through the bundled uv inside FHS.</done>
</task>

<task type="auto">
  <name>Task 2: TOX-03 D-02 — one cold tox -vv -e py312 -r inside FHS: branch, path and version of the uv tox resolved, from tox's own log; the full suite green</name>
  <files>.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md (Task 1's sections; `REVERT_SHA`)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CONTEXT.md D-02 (pass criterion, version-from-inside requirement, DIVERGENT → HALT)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-RESEARCH.md § "Architecture Patterns" (discovery order; the D-02 observation pattern and its forecast transcript)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-GAP-REMEASURE-EVIDENCE.md lines 180-275 (the pre-revert py312 run and the 1543 / 5 baseline)
    - uv.lock (the `name = "uv"` block)
  </read_first>
  <action>
1. Record these values:
   - `printenv TOX_UV_PATH; echo "exit:$?"`, which must print `exit:1`
   - `P="$(pwd -P)"`
   - `V`, the `uv.lock` `uv` version, read with `grep -A1 '^name = "uv"$' uv.lock`
   - `ls .tox 2>&1`, the state before this run

2. **Run the observation once, cold, through the shim, in the FOREGROUND.** Use one Bash call with the timeout at 600000 ms and never `run_in_background`, and do not end the turn while it runs. The command is `tox -vv -e py312 -r > "$LOG" 2>&1; echo "exit:$?"`. Put `$LOG` in the session scratchpad, never inside the tree.
   - `-vv` makes tox's verbosity 4. tox-uv then appends `-v` to its own `uv venv` and `uv sync` commands, and uv's own `DEBUG uv <version>` banner lands in the log. That is the version observed from inside the tox run, with no `tox.ini` change (D-02).
   - The run is cold (`-r`), so provisioning actually exercises the resolved uv.

3. Extract and record the decisive lines verbatim with `grep -nE 'using bundled uv from|using system uv from PATH|using uv from TOX_UV_PATH|automatically provisioned|venv> |uv-sync> |DEBUG uv [0-9]|platform linux|collected [0-9]+ items|[0-9]+ passed|py312: (OK|FAIL)' "$LOG"`. Also record:
   - `grep -oE 'DEBUG uv [0-9]+\.[0-9]+\.[0-9]+' "$LOG" | sort -u`
   - `wc -l "$LOG"` and `sha256sum "$LOG"`
   - `cat .tox/py312/pyvenv.cfg`

4. **Judge against D-02.** The result is CONFIRMED only if all of the following hold:
   - (a) At least one `using bundled uv from:` line exists, and every such line names `$P/.venv/bin/uv`. If a logged path differs from `$P/...` only by symlink resolution, record `readlink -f` of both.
   - (b) Zero lines for the TOX_UV_PATH branch, the PATH branch or tox self-provisioning. The last would mean tox ran from `.tox/.tox`, not this `.venv`.
   - (c) The unique banner set is exactly `DEBUG uv $V`.
   - (d) `py312: OK`, `collected 1548 items` and `1543 passed, 5 skipped`, with the pytest header naming Python 3.12.

   If (a), (b) or (c) fails, the result is DIVERGENT. Write a `## DIVERGENT` section quoting the offending lines and commit the evidence. Stop at a blocking checkpoint for the owner. Do not set `TOX_UV_PATH`, do not return to `tox-uv-bare`, and do not edit `flake.nix` or `tox.ini`.

   If (d) alone fails, add `## Baseline divergence`. List every differing node with its first traceback frame or its skip reason, attributed as environment-caused or not. Never adjust the baseline. Then stop at a blocking checkpoint.

5. Append `## TOX-03 D-02 observation inside FHS`, using `###` for subsections. It carries the recorded transcript, the four-part judgment, and one sentence contrasting it with the pre-revert run. That run was `64-GAP-REMEASURE-EVIDENCE.md:255`, whose `venv>` named the nixpkgs uv through the PATH branch. Commit.
  </action>
  <verify>
    <automated>E=.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md &amp;&amp; P="$(pwd -P)" &amp;&amp; V="$(grep -A1 '^name = "uv"$' uv.lock | sed -n 's/^version = "\(.*\)"$/\1/p')" &amp;&amp; [ -n "$V" ] &amp;&amp; [ -z "${TOX_UV_PATH+x}" ] &amp;&amp; O="$(tox -vv -e py312 -r --notest 2&gt;&amp;1)" &amp;&amp; printf '%s\n' "$O" | grep -qF "using bundled uv from: $P/.venv/bin/uv" &amp;&amp; ! printf '%s\n' "$O" | grep -qE 'using system uv from PATH|using uv from TOX_UV_PATH|automatically provisioned' &amp;&amp; [ "$(printf '%s\n' "$O" | grep -oE 'DEBUG uv [0-9]+\.[0-9]+\.[0-9]+' | sort -u)" = "DEBUG uv $V" ] &amp;&amp; S="$(awk '/^## TOX-03 D-02 observation inside FHS/{f=1;next} /^## /{f=0} f' "$E")" &amp;&amp; printf '%s\n' "$S" | grep -qF "using bundled uv from: $P/.venv/bin/uv" &amp;&amp; printf '%s\n' "$S" | grep -qF "DEBUG uv $V" &amp;&amp; printf '%s\n' "$S" | grep -q 'py312: OK' &amp;&amp; printf '%s\n' "$S" | grep -q 'collected 1548 items' &amp;&amp; printf '%s\n' "$S" | grep -q '1543 passed, 5 skipped' &amp;&amp; ! printf '%s\n' "$S" | grep -qE 'using system uv from PATH|using uv from TOX_UV_PATH|automatically provisioned' &amp;&amp; pytest --collect-only -q 2&gt;&amp;1 | grep -Eq '^1548 tests collected' &amp;&amp; ! grep -q '^## DIVERGENT' "$E"</automated>
    <fails_when>non-zero exit. That covers each of these:
- `TOX_UV_PATH` set
- the live cold `tox -vv -e py312 -r --notest` failing, not logging `using bundled uv from: <this worktree>/.venv/bin/uv`, logging the PATH branch, the TOX_UV_PATH branch or tox self-provisioning, or printing any uv banner version other than the `uv.lock` one
- the evidence's D-02 section missing the bundled line, the lock-version banner, `py312: OK`, `collected 1548 items` or `1543 passed, 5 skipped`, or containing a branch line other than the bundled one
- the live collection not reading `1548 tests collected`
- a `## DIVERGENT` heading present</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records `TOX_UV_PATH` unset, `P`, `V`, and one cold `tox -vv -e py312 -r` run through the shim, with `$LOG`'s line count and sha256.
    - The recorded lines show `using bundled uv from: <this worktree>/.venv/bin/uv` and the `venv>`/`uv-sync>` commands naming that same file, each followed by `DEBUG uv <V>`. The unique banner set is exactly one line.
    - The recorded lines contain no TOX_UV_PATH-branch line, no PATH-branch line and no self-provisioning line.
    - The run ends `py312: OK`, with a Python 3.12 header, `collected 1548 items` and `1543 passed, 5 skipped`, and `.tox/py312/pyvenv.cfg` is recorded.
    - One sentence contrasts this with `64-GAP-REMEASURE-EVIDENCE.md:255`'s pre-revert PATH-branch resolution.
  </acceptance_criteria>
  <done>From inside a real, cold tox run, the uv tox resolved is observed: the bundled branch, this worktree's .venv/bin/uv, at exactly the uv.lock version. The environment passes with the full suite at its baseline.</done>
</task>

<task type="auto">
  <name>Task 3: TOX-03 D-03 — the isolated outside-FHS control from a nix-interpreter venv, failing on the uv exec and nothing earlier; SC#3 literal and amended readings; requirement closure</name>
  <files>.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md (Tasks 1 and 2)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CONTEXT.md D-01 and D-03, and § Specific Ideas (the scratch pre-measurement table)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-RESEARCH.md § "Pattern: The Isolated Outside-FHS Control (D-03)" (forecast transcript)
    - .planning/ROADMAP.md Phase 65 SC#3 and its AMENDED block; constraint 12 and its AMENDED block; constraint 5(e)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-LIBZ-DIAGNOSIS.md (why the worktree `.venv` interpreter cannot run outside FHS)
  </read_first>
  <action>
1. **Place the control outside the tree.** Take the session scratchpad directory, or `mktemp -d` if none is provided. Resolve it to its physical path (`cd <dir> && pwd -P`). Set:
   - `CTRL=<that>/ctrl-venv`
   - `CW=<that>/ctrl-tox-workdir`

   Record `case "$CTRL" in "$(pwd -P)"/*) echo INSIDE ;; *) echo OUTSIDE ;; esac`; it must print `OUTSIDE`. Add the line `CTRL_VENV = <absolute CTRL path>`.

2. **Interpreter provenance (D-03).**
   - Take `PY="$(command -v python3)"` and record `readlink -f "$PY"`. It must be a `/nix/store/…-python3-…` path: not this worktree's `.venv`, not a uv-managed CPython. Otherwise the control is invalid, and you halt at a blocking checkpoint.
   - Create the venv: `"$PY" -m venv "$CTRL"`.
   - Record `cat "$CTRL/pyvenv.cfg"`. Its `home` must be under `/nix/store/`.

3. **Lock-pinned installs.** Read the four versions from this worktree's `uv.lock` with `grep -A1` over `^name = "tox"$`, `^name = "tox-uv"$`, `^name = "tox-uv-bare"$` and `^name = "uv"$`.
   - Run `uv pip install --python "$CTRL/bin/python" "tox==<v>" "tox-uv==<v>" "tox-uv-bare==<v>" "uv==<v>"` and record its output.
   - Record `uv pip list --python "$CTRL/bin/python"` filtered to those four names, each equal to its lock version.
   - Record `file "$CTRL/bin/uv"`, a generic-linux ELF whose interpreter is `/lib64/ld-linux-x86-64.so.2`.

4. **Prove the shell is outside FHS and the interpreter itself runs there.** In the same Bash call as step 5, first record:
   - `test -e /usr/lib/libz.so.1; echo "fhs-probe exit:$?"`. It must print `exit:1`, because the host root lacks the file the FHS root carries.
   - `"$CTRL/bin/python" -c 'import sys;print(sys.executable, sys.version.split()[0])'`. It must succeed. This is exactly what the earlier control failed on (`.venv/bin/python3`), so it is proven not to fail here.

5. **Run the control by absolute path, from this worktree's root, with no shim.**
   - The command is `env -u TOX_UV_PATH PATH=/usr/bin:/bin "$CTRL/bin/python" -m tox -vv -e py312 --workdir "$CW" -r > "$S/ctrl.log" 2>&1; echo "exit:$?"`. It reads this worktree's edited `tox.ini`.
   - Record `grep -n '^requires' tox.ini`, the exit code, and `head -n 5` of the log.
   - Record `grep -nE 'using bundled uv from|using system uv from PATH|using uv from TOX_UV_PATH|automatically provisioned|venv> |Could not start dynamically linked executable|exit [0-9]+ \(|py312: (OK|FAIL)' "$S/ctrl.log"`.
   - Record `ls "$CW"`, and `git status --porcelain` showing the tree untouched.

6. **Judge against D-03.** The control closes only if all of these hold:
   - tox started, shown by the bundled line appearing at all
   - the bundled line names `$CTRL/bin/uv`
   - the first line matching `exit [0-9]+ (` is the `$CTRL/bin/uv venv …` call with `exit 127`
   - `Could not start dynamically linked executable: $CTRL/bin/uv` is present, and `py312: FAIL code 127`
   - no PATH-branch, TOX_UV_PATH-branch or self-provisioning line appears

   Any failure before that uv exec means the control isolated nothing. Write `## DIVERGENT` with the lines and halt: SC#3 stays open.

7. **Closing sections.**
   - `## TOX-03 D-03 isolated control outside FHS`, holding steps 1-6.
   - `## SC#3 literal and amended readings` (D-01). The literal reading holds that `uv.find_uv_bin()` is "a different function on a different code path" and that the "bundled wheel" and the "shimmed/`.venv` binary" compete. Report both clauses as falsified by this file's own observations: the bundled branch fired, and both it and the shim's first leg name one file, `.venv/bin/uv`. Under the amended reading, record which branch fired, the resolved path, the version, the environment's outcome and the control's isolation, then state the SC#3 verdict.
   - `## Requirement closure`: a table with one row each for TOX-01, TOX-02 and TOX-03, reading `MET` or `NOT MET`, each naming the section that decides it. TOX-04 is plan 65-02's and gets no row here.

   Commit.
  </action>
  <verify>
    <automated>E=.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md &amp;&amp; C="$(sed -n 's/^CTRL_VENV = //p' "$E" | head -n 1)" &amp;&amp; [ -n "$C" ] &amp;&amp; case "$C" in "$(pwd -P)"/*) false ;; *) true ;; esac &amp;&amp; case "$(readlink -f "$C/bin/python")" in /nix/store/*) true ;; *) false ;; esac &amp;&amp; grep -Eq '^home = /nix/store/[a-z0-9]{32}-python3-[0-9.]+/bin$' "$C/pyvenv.cfg" &amp;&amp; ! test -e /usr/lib/libz.so.1 &amp;&amp; for p in tox tox-uv tox-uv-bare uv; do L="$(grep -A1 "^name = \"$p\"\$" uv.lock | sed -n 's/^version = "\(.*\)"$/\1/p')"; [ -n "$L" ] &amp;&amp; uv pip list --python "$C/bin/python" | grep -Eq "^$p +$L( |\$)" || { echo "pin mismatch: $p"; exit 1; }; done &amp;&amp; W="$(dirname "$C")/ctrl-tox-verify" &amp;&amp; { O="$(env -u TOX_UV_PATH PATH=/usr/bin:/bin "$C/bin/python" -m tox -vv -e py312 --workdir "$W" -r 2&gt;&amp;1)"; true; } &amp;&amp; printf '%s\n' "$O" | grep -qF "using bundled uv from: $C/bin/uv" &amp;&amp; printf '%s\n' "$O" | grep -qF "Could not start dynamically linked executable: $C/bin/uv" &amp;&amp; printf '%s\n' "$O" | grep -E 'exit [0-9]+ \(' | head -n 1 | grep -q 'exit 127' &amp;&amp; printf '%s\n' "$O" | grep -q 'py312: FAIL code 127' &amp;&amp; ! printf '%s\n' "$O" | grep -qE 'using system uv from PATH|using uv from TOX_UV_PATH|automatically provisioned' &amp;&amp; S="$(awk '/^## TOX-03 D-03 isolated control outside FHS/{f=1;next} /^## /{f=0} f' "$E")" &amp;&amp; printf '%s\n' "$S" | grep -qF "using bundled uv from: $C/bin/uv" &amp;&amp; printf '%s\n' "$S" | grep -q 'exit 127' &amp;&amp; grep -q '^## SC#3 literal and amended readings' "$E" &amp;&amp; grep -q '^## Requirement closure' "$E" &amp;&amp; ! grep -qE '^\| *TOX-0[123] *\|.*NOT MET' "$E" &amp;&amp; for r in TOX-01 TOX-02 TOX-03; do grep -qE "^\| *$r *\|.*MET" "$E" || { echo "missing closure row: $r"; exit 1; }; done &amp;&amp; ! grep -q '^## DIVERGENT' "$E" &amp;&amp; GS="$(git status --porcelain -- pyproject.toml tox.ini uv.lock tests typsphinx flake.nix flake.lock CLAUDE.md .github)" &amp;&amp; [ -z "$GS" ]</automated>
    <fails_when>non-zero exit, or `pin mismatch: <name>` printed. That covers each of these:
- no `CTRL_VENV` line, or the control venv inside this worktree
- its python not resolving under `/nix/store/`, or `pyvenv.cfg` `home` not a nix python3 store path
- the verify shell seeing `/usr/lib/libz.so.1`, meaning it ran inside FHS
- any of the four packages not at its `uv.lock` version
- the live control not logging `using bundled uv from: <CTRL>/bin/uv` or the dynamically-linked refusal for that file
- the first `exit N (` line not being `exit 127`, or no `py312: FAIL code 127`
- a PATH-branch, TOX_UV_PATH-branch or self-provisioning line
- the evidence missing the control's bundled line or `exit 127`, the SC#3 readings section or the closure table
- a TOX-01..TOX-03 row reading NOT MET, or `missing closure row: <id>` printed
- a `## DIVERGENT` heading
- uncommitted changes under the scope-fenced paths</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records `CTRL_VENV` outside this worktree, `readlink -f` of the interpreter and `pyvenv.cfg` `home`, both under `/nix/store/`, the four installs at their `uv.lock` versions, and `file "$CTRL/bin/uv"`.
    - In the same shell as the run, `fhs-probe exit:1` shows it is outside FHS, and the control interpreter prints its version, so it executes there.
    - The control transcript shows tox starting, `using bundled uv from: <CTRL>/bin/uv`, then that `uv venv` call as the first failing subprocess, with `Could not start dynamically linked executable` and `exit 127`, and `py312: FAIL code 127`. No earlier failure appears.
    - `## SC#3 literal and amended readings` reports the two readings separately (D-01) and states the amended verdict.
    - `## Requirement closure` holds three rows, TOX-01, TOX-02 and TOX-03, each MET and each naming its deciding section.
  </acceptance_criteria>
  <done>The isolated control fails exactly on the uv exec. The same bundled branch, outside FHS, resolves a generic-linux uv the host cannot start, and nothing earlier fails. SC#3 is closed under both readings, reported separately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → `uv.lock` / `.venv` | `uv lock` adds the `tox-uv` and `uv` wheels; `uv` is a native binary executed inside FHS by every tox run |
| PyPI → control venv (scratch) | `uv pip install` of four exact versions into a throwaway venv that runs `tox` on the host |
| scratch → tracked tree | the control venv, the comma-form `tox.ini` and the tox logs must never enter a commit |
| evidence → verifier and plan 65-02 | this file decides SC#1-SC#3 and gates 65-02's push; a transcript from the wrong process, tree or shell would certify nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-SC | Tampering | pip/uv installs: `uv lock` + `uv sync --extra dev --locked` adding `tox-uv` and `uv`; the control venv's `uv pip install` | high | mitigate | Package legitimacy was verified at plan time. `tox-uv` 1.35.2 and `uv` 0.11.26 are both in this repository's pre-45.2 lock (`git show e603f36e^:uv.lock`). PyPI metadata (queried 2026-09-12) points `tox-uv`/`tox-uv-bare` at github.com/tox-dev/tox-uv and `uv` at github.com/astral-sh/uv, with `tox-uv 1.36.0` requiring `tox-uv-bare==1.36.0` and `uv<1,>=0.9.27`. RESEARCH's audit table rates every package Approved; none is `[ASSUMED]` or `[SUS]`, so no legitimacy checkpoint is owed. `uv.lock` records sha256 for every artifact, and `--locked` refuses on mismatch. The control venv installs only the four exact `==` versions and is never committed. |
| T-65-01 | Elevation of Privilege | a `TOX_UV_PATH` redirect silently choosing the uv every tox run executes | medium | mitigate | Every verify asserts `TOX_UV_PATH` unset; `git grep` proves no tracked file outside `.planning/` carries it; D-02 prohibits setting it anywhere |
| T-65-02 | Repudiation | a shell-level `uv --version` or `find_uv_bin()` presented as the observation from inside tox | medium | mitigate | The evidence quotes tox-uv's own DEBUG line and uv's own `-v` banner from a `tox -vv` log. Task 2's verify re-derives both live from a cold run. |
| T-65-03 | Tampering | scratch artifacts (control venv, comma-form `tox.ini`, logs) committed | low | mitigate | All of them live in the session scratchpad, whose physical path is checked to be outside the tree. `git status --porcelain` over the scoped paths is gated empty. |
| T-65-04 | Repudiation | a worktree measurement that actually binds the main checkout (editable-install hazard) | medium | mitigate | The `typsphinx.__file__` identity check; the bundled path must be `$(pwd -P)/.venv/bin/uv`; the `pwd -P` worktree check sits in every verify |
</threat_model>

<verification>
- One commit, `REVERT_SHA`, carries exactly the four files. `uv sync --extra dev --locked` exits 0, the header is version 1 / revision 3, and `tox-uv`, `tox-uv-bare` and `uv` are locked, with `tox-uv` equal to `tox-uv-bare`.
- The inverted gate went RED (`1 failed, 3 passed`) and then GREEN (`4 passed`). `tox` starts on the comma-free `tox-uv~=1.35`, and the comma form is refused in scratch.
- Inside FHS, a cold `tox -vv -e py312 -r` resolves `<worktree>/.venv/bin/uv` through the bundled branch at the `uv.lock` version, and passes with 1548 collected / 1543 passed / 5 skipped.
- Outside FHS, the nix-interpreter control fails on its `uv venv` exec with exit 127 and nothing earlier. The closure table reads MET for TOX-01 to TOX-03.
</verification>

<success_criteria>
SC#1: `tox-uv` is declared, and `uv.lock` was regenerated in the same commit and is accepted by `--locked`. SC#2: `tox` starts on the comma-free `~=` form, proven by a command that evaluates `requires`. SC#3: the uv a real tox run resolved is observed from inside it, and the isolated control fails on the uv path. Both readings of SC#3 are reported (D-01). No `TOX_UV_PATH`, no DIVERGENT, and no edit outside the four files.
</success_criteria>

<output>
Create `.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-01-SUMMARY.md` when done. Its frontmatter `requirements-completed:` lists exactly the TOX-01..TOX-03 rows that read MET in `## Requirement closure`. If the plan halted DIVERGENT, the SUMMARY's first section says so, names the branch/path/version observed, and states that plan 65-02 must not run.
</output>
