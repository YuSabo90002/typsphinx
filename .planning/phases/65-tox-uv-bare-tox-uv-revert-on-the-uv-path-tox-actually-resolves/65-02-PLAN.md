---
phase: 65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md
autonomous: true
requirements: [TOX-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Gate from wave 1: this worktree HEAD contains the revert commit, meaning the REVERT_SHA line of 65-REVERT-EVIDENCE.md is an ancestor of HEAD. That file closure table reads MET for TOX-01, TOX-02 and TOX-03, and it carries no DIVERGENT heading. Otherwise nothing is pushed and nothing is dispatched (D-02 DIVERGENT → HALT carries into this plan).'
    - 'Head check and provisioning (CLAUDE.md, Phase 64 D-09): pwd -P under /home/yuta/Documents/typsphinx/.claude/worktrees/, .git a file, the uv and tox shims naming typsphinx-fhs-run, TOX_UV_PATH unset; the one provisioning line, then env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked exits 0, the same step every ci.yml job runs first.'
    - 'Constraint 9: the gsd/v0.9.3-* branch census is re-measured before the push. A decoy with zero unique commits is deleted with git branch -d; a decoy carrying commits halts the plan; the decoy is never pushed.'
    - 'Constraint 13 and D-06 fence on the pushed tip: REVERT_SHA itself changes exactly pyproject.toml, tests/test_toolchain_config_gate.py, tox.ini and uv.lock. No later commit on the tip touches those four files, typsphinx/, .github/workflows/, flake.nix, flake.lock or CLAUDE.md. Nothing under typsphinx/ or .github/workflows/ differs from the merge-base with origin/main, so the four @preview declarations are unchanged. No tracked file outside .planning/ names TOX_UV_PATH (D-02).'
    - 'SC#4: the canonical branch gsd/v0.9.3-toolchain-and-dependency-update-repair is pushed as a fast-forward of the origin head re-measured before the push, with no force, no tag and no other ref; afterwards the origin head equals PUSHED_SHA.'
    - 'SC#4 / TOX-04: exactly one CI run carries headSha equal to PUSHED_SHA. It was dispatched with gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair after the push, its event is workflow_dispatch, and it is observed in the foreground to status completed.'
    - 'SC#4: every job conclusion is transcribed literally in a numbered table. Test Python 3.12 on windows-latest, Test Python 3.13 on windows-latest, Test Python 3.12 on macos-latest and Test Python 3.13 on macos-latest are named individually, each success.'
    - 'SC#4: the ruff verdict is taken from the Lint and Format Check job, step Run lint with tox, quoted verbatim from that job log, and set beside the local ruff check . of 65-REVERT-EVIDENCE.md; the local run is not the authority.'
    - 'TOX-04 (empty edge): the census holds exactly the twelve jobs ci.yml defines, and a skipped, cancelled or missing job is never counted as success; zero non-success conclusions are required.'
    - 'TOX-04 (adjacency edge): the run headSha equals PUSHED_SHA exactly, and REVERT_SHA is an ancestor of PUSHED_SHA, so the green belongs to the post-revert tree. Phase 64 run 34618719267 is the pre-revert baseline, cited only for comparison and never as this green.'
    - statement: 'TOX-04 (ordering edge): the verdict does not depend on the order gh lists jobs in. Every assertion selects jobs by name, and the numbered census follows the API order only for transcription.'
      verification: backstop
    - 'Bundled branch on CI (D-02 continuity, observation): for each of the six test lanes and Lint and Format Check, the first tox venv> line of the new run is quoted beside the same lane venv> line from baseline run 34618719267. The baseline names the setup-uv hostedtoolcache uv (the PATH branch); the new run is expected to name the runner checkout .venv uv (the bundled branch). A lane that still names hostedtoolcache is recorded as a finding for the owner, and nothing is edited to change it.'
    - 'Failure path: if the run concludes other than success, every job is still transcribed literally. The failing run stays in the evidence and is never overwritten, and the executor stops at a blocking checkpoint for the owner, with no second dispatch, no rerun and no edit.'
  artifacts:
    - path: .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md
      provides: 'gate from wave 1, branch census, constraint-13 fence, fast-forward push, one dispatch, the completed run and its full job census, ruff verdict, per-lane uv resolution against the pre-revert baseline, TOX-04 closure'
      contains: 'windows-latest'
  key_links:
    - from: 'local canonical ref gsd/v0.9.3-toolchain-and-dependency-update-repair'
      to: 'origin/gsd/v0.9.3-toolchain-and-dependency-update-repair'
      via: 'git push -u origin gsd/v0.9.3-toolchain-and-dependency-update-repair, fast-forward only, from the worktree (shared refs)'
      pattern: 'origin/gsd/v0.9.3-toolchain-and-dependency-update-repair'
    - from: 'gh workflow run CI --ref <canonical>'
      to: 'ci.yml twelve jobs on ubuntu, windows and macos'
      via: 'workflow_dispatch, since the push and pull_request triggers are scoped to main and develop'
      pattern: 'workflow_dispatch'
    - from: 'ci.yml uv sync --extra dev --locked, then uv run tox -e <env>'
      to: 'tox-uv bundled branch on each runner (the checkout .venv uv)'
      via: 'find_uv_bin() from the runner .venv interpreter, replacing the setup-uv uv the PATH branch resolved at the baseline'
      pattern: 'venv> '
  prohibitions:
    - statement: 'MUST NOT create a tag, open a pull request, trigger release.yml by any route, force-push, or push any ref other than the canonical branch.'
      status: resolved
      verification: test
    - statement: 'MUST NOT dispatch a second CI run, rerun failed jobs, or edit any file to turn a red job green; a non-success job is transcribed and handed to the owner.'
      status: resolved
      verification: test
    - statement: 'MUST NOT edit ci.yml or any workflow, and MUST NOT set TOX_UV_PATH anywhere (constraint 3, D-02).'
      status: resolved
      verification: test
    - statement: 'MUST NOT cite Phase 64 run 34618719267, or any run whose headSha differs from PUSHED_SHA, as the TOX-04 green.'
      status: resolved
      verification: judgment
    - statement: 'MUST NOT move a ref of the main checkout (HEAD, a symbolic-ref, or a branch pointer other than deleting a zero-unique-commit decoy) from this worktree.'
      status: resolved
      verification: test
    - statement: 'MUST NOT push when the wave-1 gate is unmet: a DIVERGENT or missing 65-REVERT-EVIDENCE.md closure means there is no proven revert to put on origin.'
      status: resolved
      verification: test
---

<objective>
Put the post-revert tip on `origin` and obtain CI's verdict on it. The branch is pushed as a fast-forward, then exactly one CI run is dispatched on that tip and observed in the foreground until it completes. Every job conclusion is transcribed, both `windows-latest` and both `macos-latest` lanes are named, and `ruff`'s verdict is quoted from the `Lint and Format Check` job's `Run lint with tox` step (TOX-04, SC#4).

Purpose: CI is the authority (constraint 7), and only CI reaches Windows and macOS. Those lanes have caught real defects at three consecutive closes. This run is also where tox-uv's bundled branch is first observed on a runner that is not this machine. Each lane's `venv>` line, set beside the same lane's line from the Phase 64 pre-revert baseline (run `34618719267`), shows which uv tox resolved on Windows, macOS and Linux (65-CONTEXT D-02; the "Integration Points" note).

The plan sits in wave 2 because it must read wave 1's evidence and push wave 1's commit. Worktrees cannot see a sibling plan's output (the same-wave evidence blind spot).

Output: `65-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CONTEXT.md
@.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md
@.planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<!-- planner-discipline-allow: gsd/v0.9.3-milestone -->
<!-- planner-discipline-allow: NOT MET -->
<!-- planner-discipline-allow: ^## DIVERGENT -->
<!-- planner-discipline-allow: ## DIVERGENT -->
<!-- planner-discipline-allow: TOX_UV_PATH -->
The decoy branch name is named because the census must look for it, and the gate asserts it is absent from origin. `NOT MET` and `DIVERGENT` are words the executor must be able to write, and the gates require their absence.

## Planning-time census (2026-09-12 — re-measure, never trust)

| Fact | Value | Source |
|------|-------|--------|
| Local `gsd/v0.9.3*` refs | only `gsd/v0.9.3-toolchain-and-dependency-update-repair` (HEAD); no `gsd/v0.9.3-milestone` | `git branch --list` |
| Origin head of the canonical branch | `7afbf5b2c1c35d07fa775d194d4aaba6d575e1f5` (64-04's pushed SHA); local was 32 commits ahead at planning | `git ls-remote`, `git rev-list --count` |
| Pre-revert baseline | run `34618719267`, 12 jobs all `success` | `64-CI-EVIDENCE.md` § Job census |
| Baseline `venv>` lines (PATH branch → setup-uv's uv) | windows py3.12: `py312: venv> C:\hostedtoolcache\windows\uv\0.12.13\x86_64\uv.EXE venv …`; macos py3.12: `py312: venv> /Users/runner/hostedtoolcache/uv/0.12.13/aarch64/uv venv …`; lint: `lint: venv> /opt/hostedtoolcache/uv/0.12.13/x86_64/uv venv …` | planner `gh run view --job <id> --log`, jobs 103327017672 / 103327017740 / 103327017656 |
| Expected new `venv>` paths (bundled branch) | `D:\a\typsphinx\typsphinx\.venv\Scripts\uv.exe` (Windows), `/Users/runner/work/typsphinx/typsphinx/.venv/bin/uv` (macOS), `/home/runner/work/typsphinx/typsphinx/.venv/bin/uv` (Linux) | inferred from the baseline checkout paths — a forecast, not evidence |
| `ci.yml` invocation | every test lane: `uv sync --extra dev --locked` then `uv run tox -e ${{ matrix.tox-env }}`; lint: `Run lint with tox` = `uv run tox -e lint` | `ci.yml:36-40, 66-70` |
| Track B | Phase 66 may merge `.github/dependabot.yml` (not a workflow) into this branch in parallel; that file is legitimate on the tip | ROADMAP constraint 1 |

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `65-CI-EVIDENCE.md` | evidence file (TOX-04), with `PUSHED_SHA = ` and `RUN_ID = ` lines, a `## uv resolved on CI` table and a `## Requirement closure` row for TOX-04 | phase directory |
| `origin/gsd/v0.9.3-toolchain-and-dependency-update-repair` advanced to `PUSHED_SHA` | remote branch (fast-forward) | GitHub |
| one `workflow_dispatch` CI run at `PUSHED_SHA` | CI run | GitHub Actions |

No code symbol and no tracked file other than the evidence is created by this plan.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — wave-1 gate, head check and provisioning, branch census, constraint-13 fence, fast-forward push of the canonical branch, dispatch CI exactly once</name>
  <precondition>`65-REVERT-EVIDENCE.md` exists in this worktree. Its `REVERT_SHA = ` commit is an ancestor of HEAD (`git merge-base --is-ancestor`). Its `## Requirement closure` rows for TOX-01, TOX-02 and TOX-03 read MET, and it has no DIVERGENT heading. `gh auth status` succeeds, and `git ls-remote --heads origin` returns at least one line. If any check fails, halt at a blocking-human gate: there is no proven revert to push.</precondition>
  <reversibility rating="reversible">A fast-forwarded branch can be reset on origin, and a CI run publishes nothing; no tag, release or pull request is created.</reversibility>
  <files>.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md (`REVERT_SHA`, `## Requirement closure`, `## Gate GREEN and lint`)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-04-PLAN.md Task 1 (the census, push and dispatch procedure this task mirrors)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md (the section structure to mirror)
    - .github/workflows/ci.yml (the `name: CI`; triggers scoped to `main`/`develop`; the twelve job names)
    - .planning/ROADMAP.md binding constraints 3, 7, 9 and 13, and Phase 65 SC#4
    - CLAUDE.md § "Worktree-isolated execution"
  </read_first>
  <action>
1. **Wave-1 gate, recorded.** Record each of:
   - `R="$(sed -n 's/^REVERT_SHA = //p' .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md | head -n 1)"`
   - `git merge-base --is-ancestor "$R" HEAD; echo "exit:$?"`
   - the three TOX-01..TOX-03 closure rows quoted from that file
   - `grep -c '^## DIVERGENT' <that file>`, which must be 0

2. **Head check and provisioning.**
   - Record `test -f .git` and `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`.
   - Record `command -v uv` and `command -v tox`; each file's body must contain `typsphinx-fhs-run`.
   - Record `printenv TOX_UV_PATH; echo "exit:$?"`, which must print `exit:1`.
   - Run CLAUDE.md's provisioning line: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.
   - Record the pre-dispatch lock check `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --locked` with exit 0. Every `ci.yml` job begins with this step, so a stale lock would fail all twelve lanes before any signal.

3. **Branch census (constraint 9 — re-measure, never trust).** Record:
   - `git branch --list 'gsd/v0.9.3*' -v`
   - `git rev-parse gsd/v0.9.3-toolchain-and-dependency-update-repair`
   - `git ls-remote --heads origin | grep '0\.9\.3'`

   If `gsd/v0.9.3-milestone` exists locally, run `git merge-base --is-ancestor gsd/v0.9.3-milestone gsd/v0.9.3-toolchain-and-dependency-update-repair`:
   - Exit 0 means zero unique commits. Delete it with `git branch -d gsd/v0.9.3-milestone` and record that.
   - Non-zero means it carries commits. Record `git log --oneline gsd/v0.9.3-toolchain-and-dependency-update-repair..gsd/v0.9.3-milestone` and halt at a blocking checkpoint.

   Never move the main checkout's HEAD, and never push the decoy.

4. **Tip identity.**
   - `git rev-parse HEAD` must equal `git rev-parse gsd/v0.9.3-toolchain-and-dependency-update-repair`: this worktree was cut from the canonical tip after wave 1 merged, and this plan has not committed yet. If they differ, record both and halt.
   - Record the line `PUSHED_SHA = <HEAD sha>` and `git log --oneline -1`.

5. **Constraint-13 and D-06 fence over the tip.** Record each of these; all must hold:
   - (a) `git show --name-only --format= "$R" | sort` lists exactly `pyproject.toml`, `tests/test_toolchain_config_gate.py`, `tox.ini` and `uv.lock`.
   - (b) `git diff --name-only "$R" HEAD -- pyproject.toml tox.ini uv.lock tests/test_toolchain_config_gate.py typsphinx .github/workflows flake.nix flake.lock CLAUDE.md` is empty. Nothing after the revert touches them. `.github/dependabot.yml` from Phase 66 may legitimately appear in a wider diff, and is not a workflow.
   - (c) `git fetch origin main` first. Then `git diff --name-only "$(git merge-base HEAD origin/main)" HEAD -- typsphinx .github/workflows` is empty. So nothing under `typsphinx/` changed this milestone, and the four `@preview` declarations (`writer.py`, `template_engine.py`, `templates/base.typ`) are untouched.
   - (d) `git grep -n TOX_UV_PATH HEAD -- . ':(exclude).planning'` prints nothing.
   - (e) `sed -n 38p pyproject.toml` and `sed -n 11p tox.ini` show the tox-uv forms.

6. **Fast-forward push, and only it.**
   - Run `git fetch origin gsd/v0.9.3-toolchain-and-dependency-update-repair`, then record `ORIGIN_BEFORE`, the first field of `git ls-remote --heads origin refs/heads/gsd/v0.9.3-toolchain-and-dependency-update-repair` (`7afbf5b2…` at planning).
   - Record `git merge-base --is-ancestor "$ORIGIN_BEFORE" HEAD; echo "exit:$?"`. It must print `exit:0`. Otherwise halt: origin moved, and a force-push is forbidden.
   - Record `PUSH_AT`, the output of `date -u +%FT%TZ`.
   - Run `git push -u origin gsd/v0.9.3-toolchain-and-dependency-update-repair`, with no force, no tags and no other ref. Record the output, including GitHub's pull-request hint, and do not act on that hint.
   - Verify that `git rev-parse --abbrev-ref gsd/v0.9.3-toolchain-and-dependency-update-repair@{upstream}` prints `origin/gsd/v0.9.3-toolchain-and-dependency-update-repair`, and that the remote head now equals `PUSHED_SHA`.

7. **Dispatch exactly once.**
   - Run `gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair`.
   - Locate the run with `gh run list --workflow=ci.yml --branch gsd/v0.9.3-toolchain-and-dependency-update-repair --event workflow_dispatch --limit 5 --json databaseId,headSha,status,createdAt,url`, selecting the row whose `headSha` equals `PUSHED_SHA` and whose `createdAt` is later than `PUSH_AT`.
   - If no such row is listed yet, that is registration lag. Repeat the same `gh run list` query a few seconds later and never dispatch again.
   - Record the line `RUN_ID = <databaseId>` and the URL.

8. Write `65-CI-EVIDENCE.md` with these sections:
   - `## Wave-1 gate`
   - `## Head check and provisioning`
   - `## Branch census`
   - `## Tip identity and constraint-13 fence`
   - `## Push`
   - `## Dispatch`

   State in it that this is TOX-04's run on the post-revert tip, and that Phase 64 run `34618719267` is the pre-revert baseline, cited only for comparison. Also state that this plan's evidence commits land after the push and touch only `.planning/`, which no CI job reads. Commit.
  </action>
  <verify>
    <automated>F=.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md &amp;&amp; RE=.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md &amp;&amp; B=gsd/v0.9.3-toolchain-and-dependency-update-repair &amp;&amp; [ "$(git rev-parse --abbrev-ref "$B@{upstream}")" = "origin/$B" ] &amp;&amp; L="$(git ls-remote --heads origin "refs/heads/$B")" &amp;&amp; P="$(printf '%s\n' "$L" | cut -f1)" &amp;&amp; [ -n "$P" ] &amp;&amp; [ "$(sed -n 's/^PUSHED_SHA = //p' "$F" | head -n 1)" = "$P" ] &amp;&amp; H="$(git ls-remote --heads origin)" &amp;&amp; [ "$(printf '%s\n' "$H" | grep -c 'gsd/v0.9.3-milestone')" = 0 ] &amp;&amp; R="$(sed -n 's/^REVERT_SHA = //p' "$RE" | head -n 1)" &amp;&amp; [ -n "$R" ] &amp;&amp; git merge-base --is-ancestor "$R" "$P" &amp;&amp; N="$(git show --name-only --format= "$R")" &amp;&amp; [ "$(printf '%s\n' "$N" | sort | tr '\n' ' ')" = 'pyproject.toml tests/test_toolchain_config_gate.py tox.ini uv.lock ' ] &amp;&amp; D1="$(git diff --name-only "$R" "$P" -- pyproject.toml tox.ini uv.lock tests/test_toolchain_config_gate.py typsphinx .github/workflows flake.nix flake.lock CLAUDE.md)" &amp;&amp; [ -z "$D1" ] &amp;&amp; MB="$(git merge-base "$P" origin/main)" &amp;&amp; D2="$(git diff --name-only "$MB" "$P" -- typsphinx .github/workflows)" &amp;&amp; [ -z "$D2" ] &amp;&amp; { git grep -q TOX_UV_PATH "$P" -- . ':(exclude).planning'; [ $? -eq 1 ]; } &amp;&amp; RUNID="$(sed -n 's/^RUN_ID = //p' "$F" | head -n 1)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$RUNID" != 34618719267 ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json workflowName -q .workflowName)" = CI ] &amp;&amp; [ "$(gh run view "$RUNID" --json event -q .event)" = workflow_dispatch ] &amp;&amp; TG="$(git tag --points-at "$P")" &amp;&amp; [ -z "$TG" ] &amp;&amp; grep -q '^## Tip identity and constraint-13 fence' "$F" &amp;&amp; ! grep -q '^## DIVERGENT' "$RE"</automated>
    <fails_when>non-zero exit. That covers each of these:
- no upstream, or an upstream other than the canonical remote branch
- the canonical branch absent on origin, or its origin head not the recorded `PUSHED_SHA`
- the decoy present on origin
- no `REVERT_SHA`, or `REVERT_SHA` not an ancestor of the pushed tip
- the revert commit listing anything other than the four files
- a later commit touching a fenced path
- any change under `typsphinx/` or `.github/workflows/` since the merge-base with `origin/main`
- a tracked file outside `.planning/` naming TOX_UV_PATH
- no `RUN_ID`, or `RUN_ID` equal to the baseline run
- the run's headSha differing from the pushed SHA, its workflow not `CI`, or its event not `workflow_dispatch`
- a tag pointing at the pushed tip
- the fence section missing
- a DIVERGENT heading in the wave-1 evidence</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Wave-1 gate` quotes `REVERT_SHA`, the ancestor check at `exit:0`, three MET closure rows and a zero DIVERGENT count.
    - The head check shows the worktree path, the shim bodies and `TOX_UV_PATH` unset. The provisioning line runs verbatim, and the locked sync exits 0.
    - The branch census records the local `gsd/v0.9.3*` refs with SHAs, plus the origin state, and any decoy handling with its ancestor result.
    - The fence records (a) to (e), each holding.
    - `ORIGIN_BEFORE` is an ancestor of HEAD, and the push output is recorded without force. The upstream is the canonical remote branch, and the origin head equals `PUSHED_SHA`.
    - Exactly one `gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair` is recorded, with `RUN_ID`, its URL, `headSha` equal to `PUSHED_SHA`, and `event` equal to `workflow_dispatch`.
  </acceptance_criteria>
  <done>The proven post-revert tip is on origin as a fast-forward, fenced to the four-file revert with nothing under typsphinx/ or the workflows changed, and one CI run is dispatched against exactly that tip.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the run to completion in the foreground, transcribe the twelve-job census, name both windows-latest and both macos-latest lanes, quote ruff's CI verdict, and record per-lane uv resolution against the pre-revert baseline</name>
  <precondition>The `RUN_ID` recorded by Task 1 resolves with `gh run view <id> --json databaseId`. If this executor was resumed after returning mid-wait, it reuses that id; exactly one `workflow_dispatch` CI run on the branch carries `headSha` equal to `PUSHED_SHA`.</precondition>
  <files>.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md (Task 1's sections, `RUN_ID`, `PUSHED_SHA`)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-CI-EVIDENCE.md § Job census, § windows-latest lanes, § macos-latest lanes, § ruff's verdict (the baseline and the shape to mirror)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md § Gate GREEN and lint (the local `ruff check .` to set beside CI's)
    - .github/workflows/ci.yml lines 10-72 (the matrix, the job names, `Run lint with tox` at line 69)
  </read_first>
  <action>
1. **Wait in the FOREGROUND, never in the background.**
   - Run `gh run watch "$RUNID" --interval 30` in a Bash call with the timeout at 600000 ms. Do not use `run_in_background`.
   - If the call returns before the run completes, for example on the tool timeout, issue the same call again.
   - After each return, read `gh run view "$RUNID" --json status,conclusion`.
   - Do not end the turn and do not return control to the orchestrator while `status` is not `completed`.
   - The overall cap is 90 minutes of wall clock. If it is reached, record the run id and its current status in the evidence and return a blocking checkpoint. Never dispatch a second run.

2. **On completion, record:**
   - `gh run view "$RUNID" --json status,conclusion,workflowName,headSha,url`
   - the job census `gh run view "$RUNID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'`, as a numbered table covering every row. It must hold twelve jobs: `test` 3 OS × 2 Python = 6, plus `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`, `Integration Test - basic` and `Integration Test - advanced`.
   - two sub-tables. One names `Test Python 3.12 on windows-latest` and `Test Python 3.13 on windows-latest`; the other names `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`. Each row gives the job's conclusion.

3. **ruff's verdict from CI.**
   - Take the `Lint and Format Check` job id from `gh run view "$RUNID" --json jobs`.
   - From `gh run view --job <id> --log`, quote the `Run lint with tox` step's `commands[0]> black --check .` line, its `commands[1]> ruff check .` line, the verdict line after it, and `lint: OK`.
   - Set them beside the local `ruff check .` output in `65-REVERT-EVIDENCE.md`. CI is the authority. The lint step in `ci.yml` is `Run lint with tox`; the differently named lint step belongs to `release.yml`, which this plan never searches for and never triggers.

4. **`## uv resolved on CI`.** For each of the six `Test Python …` lanes and `Lint and Format Check`, quote the first line matching `venv>` from the new run's job log. Also quote the same lane's first `venv>` line from baseline run `34618719267`, taking its job ids from `gh run view 34618719267 --json jobs`.
   - Tabulate lane | baseline uv path | new uv path.
   - Expected: the baseline names setup-uv's `hostedtoolcache` uv (the PATH branch under `tox-uv-bare`). The new run names the runner checkout's `.venv` uv: `.venv\Scripts\uv.exe` on Windows, `.venv/bin/uv` elsewhere. That is tox-uv's bundled branch.
   - A lane that still names `hostedtoolcache` is recorded as a finding for the owner. The job conclusion stays the TOX-04 authority, and nothing is edited.
   - If a log has no `venv>` line, record the empty grep verbatim.

5. Record `gh run list --workflow=release.yml --limit 5 --json headSha`: no entry equals `PUSHED_SHA`. Record the dispatch count: `gh run list --workflow=ci.yml --branch gsd/v0.9.3-toolchain-and-dependency-update-repair --event workflow_dispatch --limit 50 --json headSha` shows exactly one run at `PUSHED_SHA`.

6. **If any job concludes other than `success`:**
   - transcribe every job literally, and keep the failing run in the evidence without overwriting it
   - do not dispatch again, do not rerun, and do not edit anything
   - return a blocking checkpoint naming the failing jobs and their failing step, because TOX-04 is unmet until the owner decides

7. Append `## Run`, `## Job census`, `## windows-latest lanes`, `## macos-latest lanes`, `## ruff's verdict`, `## uv resolved on CI`, `## Comparison with the pre-revert baseline` (twelve rows, baseline conclusion beside the new one), `## No release-workflow run against this tip`, `## Dispatch count` and `## Requirement closure`.
   - The closure table carries one row, TOX-04, reading `MET` or `NOT MET` and naming its deciding section.
   - Under the table, restate the TOX-01..TOX-03 verdicts by pointing to `65-REVERT-EVIDENCE.md` § Requirement closure.

   Commit.
  </action>
  <verify>
    <automated>F=.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md &amp;&amp; RUNID="$(sed -n 's/^RUN_ID = //p' "$F" | head -n 1)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$(gh run view "$RUNID" --json status -q .status)" = completed ] &amp;&amp; [ "$(gh run view "$RUNID" --json conclusion -q .conclusion)" = success ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '.jobs | length')" = 12 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.conclusion != "success")] | length')" = 0 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("windows-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("macos-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name == "Lint and Format Check" and .conclusion == "success")] | length')" = 1 ] &amp;&amp; for n in 'Test Python 3.12 on windows-latest' 'Test Python 3.13 on windows-latest' 'Test Python 3.12 on macos-latest' 'Test Python 3.13 on macos-latest' 'Lint and Format Check' 'Run lint with tox' '34618719267'; do grep -qF "$n" "$F" || { echo "missing: $n"; exit 1; }; done &amp;&amp; P="$(gh run view "$RUNID" --json headSha -q .headSha)" &amp;&amp; [ "$(sed -n 's/^PUSHED_SHA = //p' "$F" | head -n 1)" = "$P" ] &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch gsd/v0.9.3-toolchain-and-dependency-update-repair --event workflow_dispatch --limit 50 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 5 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; [ "$(grep -c 'Run linters' "$F")" = 0 ] &amp;&amp; grep -q '^## uv resolved on CI' "$F" &amp;&amp; [ "$(awk '/^## uv resolved on CI/{f=1;next} /^## /{f=0} f' "$F" | grep -c 'venv&gt;')" -ge 7 ] &amp;&amp; grep -q '^## Requirement closure' "$F" &amp;&amp; grep -qE '^\| *TOX-04 *\|.*MET' "$F" &amp;&amp; ! grep -qE '^\| *TOX-04 *\|.*NOT MET' "$F"</automated>
    <fails_when>non-zero exit, or `missing: <name>` printed. That covers each of these:
- the run not `completed`, or its conclusion not `success`
- a job count other than 12, or any job not `success`
- a windows-latest or macos-latest count other than 2
- `Lint and Format Check` not success
- a lane name, the lint job, its step name or the baseline run id absent from the evidence
- the run's headSha differing from the recorded `PUSHED_SHA`
- more than one dispatched CI run at that SHA
- a release-workflow run at that SHA
- the release-only lint step name present in the evidence
- the uv-resolution section missing, or holding fewer than seven quoted `venv>` lines
- the closure table missing, or its TOX-04 row absent or reading NOT MET</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence records the run's `status`, `conclusion`, `workflowName`, `headSha` and `url`, and a numbered table of all twelve job names and conclusions.
    - The four lanes `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest` appear by name in their own sub-tables, each `success`.
    - The `Run lint with tox` step's `black --check .` and `ruff check .` invocations and verdicts are quoted verbatim from CI, and set beside 65-01's local `ruff check .`.
    - `## uv resolved on CI` tabulates, for all six test lanes and the lint job, the baseline and new `venv>` uv paths, and names every lane that still resolves `hostedtoolcache`, if any.
    - Exactly one `workflow_dispatch` CI run exists at `PUSHED_SHA`, and no `release.yml` run carries it.
    - `## Requirement closure` holds a TOX-04 row reading MET and naming its deciding section.
  </acceptance_criteria>
  <done>A completed, all-green, twelve-job CI run on the pushed post-revert tip is transcribed job by job, with both Windows and both macOS lanes named and ruff's verdict quoted from CI. The uv each runner's tox resolved is recorded against the pre-revert baseline.</done>
</task>

</tasks>

## Orchestrator step after this plan merges (not an executor task)

**Decision (Claude's discretion, 65-CONTEXT):** the main checkout's `.venv` is re-synced **once, orchestrator-side**, after this plan's worktree has merged and before phase verification. Until then it keeps working: without `.venv/bin/uv`, the `uv` shim's second leg serves nixpkgs uv, and `tox` self-heals through `.tox/.tox` (RESEARCH Pitfall 2). The re-sync is what actually moves the maintainer's own machine onto the lock-pinned `uv`, which is D-05's "accepted and recorded" switch. It is not an executor task, because it runs outside any worktree, on the main checkout's `.venv`. That `.venv` is deliberately built on a host-runnable nix CPython.

A bare `uv run` does **not** perform this re-sync: it syncs only the base project, leaving the dev extra and `.venv/bin/uv` untouched (RESEARCH Pitfall 2). Only the documented line does.

In `/home/yuta/Documents/typsphinx`, record in order:
1. `uv --version`, expected to name the nixpkgs uv
2. `test ! -e .venv/bin/uv; echo "exit:$?"`
3. `sed -n 's/^home = //p;s/^version_info = //p' .venv/pyvenv.cfg`
4. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
5. `test -x .venv/bin/uv; echo "exit:$?"`
6. `uv --version`, which must equal `uv.lock`'s `uv` version
7. the same `pyvenv.cfg` `home` / `version_info`, which must be unchanged: the nix `python3-3.13.13` store path. If it changed, stop and tell the owner rather than proceeding.
8. `tox --version`, whose plugin line now names the tox-uv distribution with `uv==<version>`

Append these as `## Addendum: main-checkout re-sync (orchestrator, D-05)` to `65-REVERT-EVIDENCE.md` and commit as `docs(65): record main-checkout uv switch (D-05)`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local refs → origin | a push publishes refs; a wrong ref (the decoy), a force or a tag would publish something this phase must not |
| gh CLI → GitHub Actions | a dispatch starts remote work; the wrong workflow (`release.yml`) would begin a publish pipeline |
| wave-1 evidence → this plan | the push is gated on another plan's closure table; a stale or DIVERGENT file must stop the push |
| CI log → committed evidence | quoted job-log lines cross into the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-05 | Tampering | pushing the decoy branch, a force-push, or a tag | high | mitigate | The census and `merge-base --is-ancestor` run before the push. The only push command names the canonical ref, with no force and no tags. `ORIGIN_BEFORE` must be an ancestor of HEAD. The gate checks that origin carries no decoy and that no tag points at the pushed tip. |
| T-65-06 | Elevation of Privilege | triggering `release.yml` (publish pipeline) | high | mitigate | Only `gh workflow run CI` is ever issued. The gate asserts that no `release.yml` run carries the pushed SHA, and a prohibition forbids every route. |
| T-65-07 | Repudiation | a green inherited from an earlier run (the Phase 64 baseline), or a second dispatch masking a red first one | medium | mitigate | `RUN_ID` must differ from `34618719267`, its `headSha` must equal `PUSHED_SHA`, and exactly one dispatched run at that SHA is gated. A red run is kept and escalated, never re-dispatched. |
| T-65-08 | Tampering | pushing a revert whose SC#3 proof diverged | medium | mitigate | The precondition and verify both read `65-REVERT-EVIDENCE.md`: `REVERT_SHA` is an ancestor, three MET rows exist, and there is no DIVERGENT heading. The four-file fence is checked on the tip. |
| T-65-09 | Information Disclosure | CI log excerpts in committed evidence | low | accept | The repository and its Actions logs are public, and only tool-output lines (`venv>`, lint verdicts) are quoted |
</threat_model>

<verification>
- The wave-1 gate holds. The canonical branch is on origin at `PUSHED_SHA` as a fast-forward, with no decoy and no tag, and the tip is fenced to the four-file revert with nothing under `typsphinx/` or `.github/workflows/` changed.
- One `workflow_dispatch` CI run at `PUSHED_SHA` completed, with all twelve jobs green. Both Windows and both macOS lanes are named, and ruff's verdict is quoted from CI.
- The `venv>` uv path of every test lane and of the lint job is tabulated against the pre-revert baseline.
</verification>

<success_criteria>
SC#4 is met. One run dispatched with `gh workflow run CI --ref gsd/v0.9.3-toolchain-and-dependency-update-repair` on the post-revert tip has completed. Every job's conclusion is transcribed literally, both `windows-latest` and both `macos-latest` lanes are named individually, and `ruff`'s verdict is taken from `Lint and Format Check` / `Run lint with tox`. CI is the authority.
</success_criteria>

<output>
Create `.planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-02-SUMMARY.md` when done. Its frontmatter `requirements-completed:` is `[TOX-04]` only if the TOX-04 row reads MET. If the run was not all green, the SUMMARY's first section names the failing jobs and states that TOX-04 awaits the owner. Remind the orchestrator of the main-checkout re-sync step above.
</output>
