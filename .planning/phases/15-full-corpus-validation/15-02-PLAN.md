---
phase: 15-full-corpus-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - tests/test_corpus_gate.py
autonomous: true
requirements:
  - GATE-02
must_haves:
  truths:
    - "SC#3: the empty-URL cross-reference warning count is measured before vs. after the XREF-01 fix against the SAME corpus, quantifying the reduction rather than assuming it"
    - "**D-07: the 'before' translator is obtained by reverse-applying commit `79c9d45` to `depart_term`'s `node.get(\"ids\")` branch ONLY (NOT the `visit_reference` refid branch, which predates XREF-01), via an isolated `git worktree` at `79c9d45~1`**"
    - "both before/after builds run with `-b typst` (translate-phase only, NO typst.compile) so the reverted translator's dangling `:term:` glossary label cannot fatally abort the measurement (RESEARCH Pitfall 2)"
    - "empty-URL warnings are counted by the literal `Reference node has empty URL` emitted in `visit_reference` (translator.py:2362-2369), held constant across both builds"
  artifacts:
    - tests/test_corpus_gate.py
  key_links:
    - "the SC#3 measurement is env-var-gated so it does NOT add two extra corpus builds to every routine `pytest -m slow` run (RESEARCH Open Question 1) while staying git-tracked + reproducible"
    - "the pre-fix translator shadows the installed typsphinx only for the 'before' subprocess via a prepended PYTHONPATH — the main working tree is never mutated (worktree, not `git stash`)"
---

<objective>
Add the SC#3 empty-URL before/after measurement machinery to `tests/test_corpus_gate.py`
(created in Plan 01). This measures the empty-URL cross-reference warning-count reduction
delivered by the Phase-12 XREF-01 fix by rebuilding the SAME corpus twice: once against an
isolated `git worktree` checkout of the translator immediately BEFORE the XREF-01 `depart_term`
fix (`79c9d45~1`, D-07), and once against the as-shipped translator — both with `-b typst`
(translate-phase only, never `typst.compile()`, per RESEARCH Pitfall 2), then diffing the
`Reference node has empty URL` warning counts.

Purpose: turns the previously-assumed empty-URL reduction into a real measured number
(SC#3), which Plan 03 records into the committed report.
Output: `checkout_pre_fix_translator`, `count_empty_url_warnings`, the `FIX_COMMIT` constant,
and an env-gated `test_empty_url_before_after` function, all appended to `tests/test_corpus_gate.py`,
plus a fast unit test for the counter.
</objective>

## Artifacts this phase produces (created by THIS plan)

New symbols appended to `tests/test_corpus_gate.py` — exclude from drift verification:

- `FIX_COMMIT` — `"79c9d45"` constant (the XREF-01 `depart_term` fix commit)
- `checkout_pre_fix_translator(repo_root, worktree_dir)` — `git worktree add --detach <dir> 79c9d45~1` (D-07)
- `EMPTY_URL_SIGNATURE` — the literal `"Reference node has empty URL"` counted in stderr
- `count_empty_url_warnings(stderr_text)` — returns an int count (SC#3)
- `test_count_empty_url_warnings` — fast (non-slow) unit test for the counter
- `test_empty_url_before_after` — env-gated (`TYPSPHINX_CORPUS_REPORT=1`) `@pytest.mark.slow` measurement function

(Reuses Plan 01's `get_or_clone_corpus`/`corpus_doc_dir`/`wire_typsphinx_into_corpus_conf`/
`_run_corpus_sphinx_build`. Plan 03 consumes these to populate `15-CORPUS-REPORT.md`.)

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-full-corpus-validation/15-CONTEXT.md
@.planning/phases/15-full-corpus-validation/15-RESEARCH.md
@.planning/phases/15-full-corpus-validation/15-PATTERNS.md
@.planning/phases/15-full-corpus-validation/15-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Empty-URL counter + git-worktree pre-fix-translator helper (D-07)</name>
  <files>tests/test_corpus_gate.py</files>
  <read_first>
    - tests/test_corpus_gate.py (the Plan 01 module — reuse `_run_corpus_sphinx_build`, `corpus_doc_dir`, `wire_typsphinx_into_corpus_conf`; do NOT duplicate them)
    - typsphinx/translator.py lines 2337-2369 (`visit_reference` — the `if not refuri and refid:` branch at 2343-2356 that PREDATES XREF-01 and must NOT be reverted; the empty-URL warning `Reference node has empty URL` at 2362-2369 that SC#3 counts)
    - typsphinx/translator.py lines 1234-1267 (`depart_term` — the `if node.get("ids"):` label-anchor branch at 1262-1264 that IS the sole D-07 revert target)
    - .planning/phases/15-full-corpus-validation/15-RESEARCH.md §"Pattern 3", §"Pitfall 2", §"D-07 correction note" (revert `79c9d45` only; `depart_term` only; `-b typst` only)
    - .planning/phases/15-full-corpus-validation/15-PATTERNS.md §"D-07 git worktree before/after helper", §"Empty-URL warning source"
  </read_first>
  <behavior>
    Counter (`count_empty_url_warnings`) over synthetic stderr:
    - stderr containing three lines each with `Reference node has empty URL` → returns 3.
    - stderr with zero occurrences → returns 0.
    - The count is by the literal signature `Reference node has empty URL` (the exact warning
      prefix emitted in `visit_reference`), robust to the trailing `Check for broken references
      in source: <astext>` suffix that varies per node.
  </behavior>
  <action>
    Append to `tests/test_corpus_gate.py`:
    (1) `FIX_COMMIT = "79c9d45"` with an inline comment naming it `fix(12-02): emit bracket-wrap
    <label> anchor in depart_term` — the D-07 revert anchor. The precise revert target is ONLY
    `depart_term`'s `node.get("ids")` label-anchor branch; the `visit_reference` refid branch predates
    XREF-01 (Phase 11) and MUST NOT be reverted (RESEARCH correction note).
    (2) `checkout_pre_fix_translator(repo_root, worktree_dir)` per RESEARCH Pattern 3:
    `subprocess.run(["git", "worktree", "add", "--detach", str(worktree_dir), f"{FIX_COMMIT}~1"], cwd=repo_root, check=True)`
    — an isolated read-only checkout, NEVER `git stash` (which mutates the live working tree).
    (3) `EMPTY_URL_SIGNATURE = "Reference node has empty URL"` and `count_empty_url_warnings(stderr_text)`
    returning `stderr_text.count(EMPTY_URL_SIGNATURE)` — a plain substring count is safe here because this
    warning (unlike `unknown_visit`) does NOT embed a multi-line node dump; the counted signature is a
    fixed prefix on a single logical warning.
    (4) `test_count_empty_url_warnings` — a fast NON-slow unit test over a synthetic 3-occurrence stderr
    string and a zero-occurrence string (Nyquist automated verify for SC#3 counting logic, no network).
    All subprocess calls use list-arg form, never `shell=True`.
  </action>
  <acceptance_criteria>
    - `FIX_COMMIT == "79c9d45"` and `checkout_pre_fix_translator` runs `git worktree add --detach ... 79c9d45~1` (never `git stash`).
    - `count_empty_url_warnings` counts the literal `Reference node has empty URL` signature and returns an int.
    - `pytest tests/test_corpus_gate.py::test_count_empty_url_warnings -v` passes (fast, no network).
    - No change to `typsphinx/translator.py` ships in this plan — the revert is a git-worktree operation only.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_corpus_gate.py::test_count_empty_url_warnings -v && pytest tests/test_corpus_gate.py --collect-only -q</automated>
  </verify>
  <done>The empty-URL counter and the git-worktree pre-fix-translator helper exist; the fast counter unit test passes; no translator.py edit ships.</done>
</task>

<task type="auto">
  <name>Task 2: Env-gated before/after measurement function (SC#3, -b typst both sides)</name>
  <files>tests/test_corpus_gate.py</files>
  <read_first>
    - tests/test_corpus_gate.py (Task 1 helpers + Plan 01's `_run_corpus_sphinx_build` / `corpus_doc_dir`)
    - .planning/phases/15-full-corpus-validation/15-RESEARCH.md §"Pattern 3" (`run_translate_only` with PYTHONPATH override), §"Open Question 1" (keep SC#3 out of the routine slow run), §"Anti-Patterns to Avoid"
    - pyproject.toml lines 78-81 (`addopts = "-v --strict-markers"` + registered `slow` marker — a NEW marker would fail strict-markers, so gate SC#3 by env var, not a new marker)
  </read_first>
  <action>
    Append `test_empty_url_before_after(corpus_doc_dir, tmp_path)`, `@pytest.mark.slow`, that
    `pytest.skip`s at the top unless `os.environ.get("TYPSPHINX_CORPUS_REPORT") == "1"` — this keeps
    SC#3's two extra corpus builds OUT of the routine `pytest -m slow` run while staying git-tracked and
    reproducibly re-runnable (RESEARCH Open Question 1; avoids a new pytest marker under `--strict-markers`).
    When enabled, the function:
    (1) resolves `repo_root` (the typsphinx checkout, e.g. `Path(__file__).resolve().parents[1]`), creates a
    `git worktree` at `79c9d45~1` via `checkout_pre_fix_translator` into `tmp_path/"pre-fix"`, and registers
    cleanup to `git worktree remove --force` it afterward.
    (2) `wire_typsphinx_into_corpus_conf(corpus_doc_dir)` (idempotent) so both builds see identical config.
    (3) BEFORE build: `_run_corpus_sphinx_build("typst", corpus_doc_dir, tmp_path/"before", env={"PYTHONPATH": str(<worktree>), **os.environ})`
    — the prepended PYTHONPATH shadows the installed typsphinx with the pre-fix translator for THIS subprocess only.
    MUST be `-b typst` (translate-only) — `-b typstpdf` would invoke `typst.compile()` and the reverted
    `depart_term`'s dangling `:term:`→glossary label would fatally abort before any count (RESEARCH Pitfall 2).
    (4) AFTER build: `_run_corpus_sphinx_build("typst", corpus_doc_dir, tmp_path/"after")` (as-installed typsphinx,
    no PYTHONPATH override), also `-b typst`.
    (5) `before = count_empty_url_warnings(before_result.stderr)`, `after = count_empty_url_warnings(after_result.stderr)`;
    assert `after <= before` (the fix cannot increase empty-URL warnings) and print both counts + the delta so
    Plan 03 can transcribe the numbers. Do NOT assert an exact magnitude (the corpus is external and may drift).
  </action>
  <acceptance_criteria>
    - `test_empty_url_before_after` carries `@pytest.mark.slow` and `pytest.skip`s unless `TYPSPHINX_CORPUS_REPORT=1` (no new pytest marker introduced).
    - Both the before and after builds use `-b typst` (NOT `-b typstpdf`) — verifiable in the `_run_corpus_sphinx_build` call sites.
    - The before build passes `env` with a prepended `PYTHONPATH` pointing at the worktree; the after build passes no PYTHONPATH override.
    - The function asserts `after <= before` and emits both counts + delta to stdout.
    - The worktree is cleaned up (`git worktree remove --force`) regardless of outcome.
    - `pytest -m "not slow" -q` stays green; a plain `pytest -m slow` run does NOT trigger this function's two extra builds (env-gated).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_corpus_gate.py --collect-only -q && pytest -m "not slow" -q</automated>
    <human-check>Outside the sandbox with network: `TYPSPHINX_CORPUS_REPORT=1 pytest tests/test_corpus_gate.py::test_empty_url_before_after -m slow -v -s` runs both `-b typst` builds and prints before/after empty-URL counts with `after <= before`. These numbers feed Plan 03's report.</human-check>
  </verify>
  <done>The env-gated SC#3 before/after function exists, runs both sides with `-b typst`, shadows the pre-fix translator via worktree+PYTHONPATH, asserts `after <= before`, and prints the measured counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git worktree checkout `79c9d45~1` → build subprocess | a historical translator revision is exec'd for the 'before' build |
| PYTHONPATH override → sphinx subprocess | a prepended path shadows the installed package for one subprocess |
| worktree path / commit ref → git & sphinx subprocess args | test-constructed strings become subprocess arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering | live working tree during the D-07 revert | low | mitigate | Use an isolated `git worktree add --detach` (read-only relative to the main tree) + `git worktree remove --force` cleanup — never `git stash`, which mutates the live tree |
| T-15-05 | Tampering | PYTHONPATH shadowing scope | low | mitigate | The override is passed only in the single 'before' subprocess `env`; the 'after' build and the rest of the process are unaffected |
| T-15-03 | Tampering | subprocess arg construction (worktree dir, commit ref) | low | mitigate | All git/sphinx invocations use `subprocess.run([...])` list-arg form, never `shell=True` |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new external packages introduced this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `pytest tests/test_corpus_gate.py::test_count_empty_url_warnings -v` passes (SC#3 counter logic, no network).
- `pytest tests/test_corpus_gate.py --collect-only -q` collects the new function + unit test without import errors.
- `pytest -m "not slow" -q` remains green.
- `git status` shows no modification to `typsphinx/translator.py` (the D-07 revert is worktree-only).
- Manual (outside sandbox): `TYPSPHINX_CORPUS_REPORT=1 pytest tests/test_corpus_gate.py::test_empty_url_before_after -m slow -v -s` prints before/after counts with `after <= before`.
</verification>

<success_criteria>
- The SC#3 before/after machinery is appended to `tests/test_corpus_gate.py`, reusing Plan 01's helpers.
- D-07 is encoded exactly: revert `79c9d45`'s `depart_term` branch only, via `git worktree` at `79c9d45~1`, `-b typst` both sides.
- The measurement is env-gated (reproducible but out of the routine slow suite) and needs no new pytest marker.
- No `typsphinx/translator.py` edit ships in the phase's final state.
</success_criteria>

<output>
Create `.planning/phases/15-full-corpus-validation/15-02-SUMMARY.md` when done.
</output>
