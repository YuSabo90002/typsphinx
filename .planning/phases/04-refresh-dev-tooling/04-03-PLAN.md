---
phase: 04-refresh-dev-tooling
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - README.md
  - pyproject.toml
autonomous: true
requirements: [TOOL-01]
tags: [docs, cleanup, python-version, ruff]

must_haves:
  truths:
    - "README.md line 36 reads 'Python 3.10 or higher' and line 323's version footer reads 'Python: 3.10+' — no surviving Python 3.9 reference in README"
    - "pyproject.toml ruff UP035/UP006 ignore-list comments read 'Python 3.10+ support' (was 'Python 3.9+ support')"
    - "uv run tox -e lint stays clean — the comment change has no functional effect on ruff/black"
  artifacts:
    - "README.md (lines 36 and 323)"
    - "pyproject.toml (ruff UP035/UP006 comment text, lines 111-112)"
  key_links:
    - "The ruff comment edit shares pyproject.toml with plan 04-01's [dev] edit — sequenced after 04-01 (wave 2) so the two edits to the same file do not collide; different line regions, no functional overlap"
---

<objective>
Fold in the Phase-3 leftover (D-04): remove the last two `Python 3.9` references in `README.md` (lines 36 and 323 -> `3.10`) and update the stale ruff `UP035`/`UP006` ignore-list comment text in `pyproject.toml` from "Python 3.9+ support" to "Python 3.10+ support". Phase 3 already dropped 3.9 from the CI matrix, PyPI classifiers, tox env_list, and black/ruff/mypy target-versions — these three text strings are the only survivors. This is a small doc/comment cleanup folded into the tooling PR (D-06 commit 3), not new scope.

Purpose: Eliminate the remaining stale 3.9 references so the documented floor matches the real 3.10 floor established in Phase 3.
Output: README and the ruff comment aligned to the 3.10 floor; lint still green.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-refresh-dev-tooling/04-CONTEXT.md
@.planning/phases/04-refresh-dev-tooling/04-PATTERNS.md
@README.md
@pyproject.toml
</context>

<artifacts_produced>
Concrete changed strings this plan lands:

- `README.md` line 36: `- Python 3.10 or higher` (was `- Python 3.9 or higher`)
- `README.md` line 323: `**Python**: 3.10+ | **Sphinx**: 5.0+ | **Typst**: 0.11.1+` (was `**Python**: 3.9+ | ...`)
- `pyproject.toml` lines 111-112: `UP035` / `UP006` ignore comments now say `Python 3.10+ support`

Not touched: README line 322 `(v0.4.2)` status string (pre-existing, unrelated to D-04); the ruff rule codes themselves (UP035/UP006 stay ignored — only the comment text changes).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Update the two README Python-version references to 3.10</name>
  <files>README.md</files>
  <read_first>
    - README.md line 36 (the `## Requirements` bullet) and line 323 (the version footer line)
    - 04-PATTERNS.md "README.md Python 3.9 references" section (current-state/target-state excerpt + Phase 3 classifier-cleanup precedent)
    - 04-CONTEXT.md D-04 (exact scope: lines 36 and 323 only)
  </read_first>
  <action>
    In `README.md`, change the requirements bullet on line 36 to read `- Python 3.10 or higher`, and change the version footer on line 323 so its Python segment reads `**Python**: 3.10+` while leaving the rest of that line (`| **Sphinx**: 5.0+ | **Typst**: 0.11.1+`) unchanged. Do not alter any other line (in particular, leave the `Status: Stable (v0.4.2)` line 322 alone — that is out of D-04 scope).
  </action>
  <acceptance_criteria>
    - `grep -q 'Python 3.10 or higher' README.md` (exit 0)
    - `grep -q '\*\*Python\*\*: 3.10+' README.md` (exit 0)
    - `grep -q '\*\*Sphinx\*\*: 5.0+' README.md` still present (footer otherwise unchanged)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Python 3.10 or higher' README.md && grep -q '\*\*Python\*\*: 3.10+' README.md</automated>
  </verify>
  <done>Both README Python-version references read 3.10; the rest of the footer line and line 322 are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update the ruff UP035/UP006 comment text in pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml lines 111-112 (the `UP035` and `UP006` entries in `[tool.ruff.lint] ignore` with the trailing "(Python 3.9+ support)" comments)
    - 04-PATTERNS.md "pyproject.toml ruff UP035/UP006 comment text" section (target-state: "Python 3.10+ support")
    - 04-RESEARCH.md Pitfall 4 (comment-only change — verify ruff/black stay clean)
  </read_first>
  <action>
    In `pyproject.toml`'s `[tool.ruff.lint] ignore` list, update the trailing comment on the `UP035` entry and the `UP006` entry so each reads `(Python 3.10+ support)` instead of the current `(Python 3.9+ support)`. Change only the comment text — leave the `"UP035"` and `"UP006"` rule codes and every other ignore entry exactly as-is. This is a comment-only edit with no functional effect on linting. It shares the file with plan 04-01's `[dev]` edit but touches a different region (lines ~111-112 vs 36-48); 04-01 lands first (wave ordering) so there is no collision.
  </action>
  <acceptance_criteria>
    - `grep -q 'UP035.*Python 3.10+ support' pyproject.toml` (exit 0)
    - `grep -q 'UP006.*Python 3.10+ support' pyproject.toml` (exit 0)
    - `grep -q '"UP035"' pyproject.toml` and `grep -q '"UP006"' pyproject.toml` — rule codes still present/ignored
    - `uv run tox -e lint` exits 0 (comment change did not perturb ruff/black)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'UP035.*Python 3.10+ support' pyproject.toml && grep -q 'UP006.*Python 3.10+ support' pyproject.toml && uv run tox -e lint</automated>
  </verify>
  <done>Both ruff comment strings read "Python 3.10+ support"; rule codes unchanged; `uv run tox -e lint` still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation and comment text only — no code path, input surface, dependency, or CI-runner behavior changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Information Disclosure | README/comment version accuracy | low | accept | Pure doc/comment text alignment to the already-shipped 3.10 floor; no attack surface. `uv run tox -e lint` confirms no functional side effect on the linter config. |
</threat_model>

<verification>
- README carries `Python 3.10 or higher` and `**Python**: 3.10+`; no 3.9 reference survives.
- Both ruff ignore comments read "Python 3.10+ support"; rule codes unchanged.
- `uv run tox -e lint` still green (comment change is inert).
</verification>

<success_criteria>
- The three stale "3.9" text strings (README x2 + ruff comment x2 lines) are updated to 3.10.
- Lint stays clean.
</success_criteria>

<output>
Create `.planning/phases/04-refresh-dev-tooling/04-03-SUMMARY.md` when done.
</output>
