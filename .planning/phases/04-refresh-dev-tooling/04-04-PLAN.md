---
phase: 04-refresh-dev-tooling
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified: []
autonomous: false
requirements: [TOOL-01, TOOL-02]
tags: [ci, verification, pr, push-observe]

must_haves:
  truths:
    - "A PR targeting main carries the four per-surface commits (pyproject.toml dev deps + uv.lock; tox.ini env deps + [tox] requires; README + ruff comment; workflow artifact-action bumps)"
    - "ci.yml is observed green on the PR head across the full 3.10-3.13 matrix plus the lint / type-check / coverage / build / integration jobs"
    - "docs.yml is observed green on the PR head (end-to-end, including the PDF-copy step)"
  artifacts:
    - "An open PR to main with recorded gh run URLs and success conclusions for ci.yml and docs.yml"
  key_links:
    - "The observed CI run — not local tox — is the phase gate (D-05): regenerating uv.lock after the floor/ceiling changes can shift transitive deps unpredictably, so only an observed green matrix proves no regression"
---

<objective>
Close Phase 4 with the milestone's `push -> observe = done` gate (D-05, carried forward from Phase 2/3): ensure the four per-surface commits from plans 04-01/02/03 are on a branch, open a PR targeting `main`, and observe an actual CI run go green — `ci.yml` (Python 3.10-3.13 matrix x 3 OS + lint + type-check + coverage + build + integration) and `docs.yml` (end-to-end incl. the multi-language PDF-copy step). A blocking human-verify checkpoint confirms the observed green (and reviews the plan-04-02 Node-20 straggler findings) before the phase is marked done. Satisfies TOOL-01 and TOOL-02's success-criterion 3 ("CI remains green after the tooling refresh, no regression").

Purpose: local `uv run tox` is a cheap pre-check only; `uv.lock` regeneration can shift transitive deps, so an observed green CI run is the milestone-consistent proof of no regression.
Output: An open PR to `main` with recorded green `ci.yml` + `docs.yml` runs and a human-confirmed gate.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-refresh-dev-tooling/04-CONTEXT.md
@.planning/phases/04-refresh-dev-tooling/04-PATTERNS.md
@.planning/phases/04-refresh-dev-tooling/04-VALIDATION.md
</context>

<artifacts_produced>
- An open PR targeting `main` containing the four Phase-4 per-surface commits.
- Recorded `gh run view <id>` URLs + `success` conclusions for both `ci.yml` and `docs.yml` on the PR head.
- No file edits in this plan — it is the verification/observation gate only.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Push the Phase-4 branch and open a PR targeting main</name>
  <files>(git/PR operations — no working-tree file edits)</files>
  <read_first>
    - 04-PATTERNS.md "Push -> observe = done (D-05)" and "Per-surface commit granularity (D-06)" sections (the Phase 2/3 push->observe + per-surface commit pattern, with gh run URL examples)
    - 04-CONTEXT.md D-05 and D-06 (PR targeting main; four per-surface commits within a single PR)
    - STATE.md (Phase 3 pushed a work branch + PR #104 targeting main and observed ci.yml + docs.yml green — reuse verbatim)
  </read_first>
  <action>
    Confirm the working tree carries the four per-surface commits from plans 04-01/02/03 (pyproject.toml dev deps + regenerated uv.lock; tox.ini env deps + [tox] requires; README + ruff comment cleanup; the ci.yml/docs.yml/release.yml artifact-action bumps) — if any surface is uncommitted, commit it at the D-06 granularity. Push the phase branch to the remote and open a PR targeting `main` using `gh pr create` (reuse the Phase 3 push->observe pattern). Capture the PR URL and head SHA for the observation step. Do NOT merge in this task.
  </action>
  <acceptance_criteria>
    - `gh pr view --json url,baseRefName -q '.baseRefName'` returns `main` (PR exists and targets main)
    - `git log --oneline main..HEAD` shows the four per-surface commits (pyproject+lock, tox.ini, README+comment, workflows)
    - the PR head SHA is recorded for Task 2
  </acceptance_criteria>
  <verify>
    <automated>gh pr view --json baseRefName -q '.baseRefName' | grep -qx 'main'</automated>
  </verify>
  <done>A PR targeting `main` is open with the four Phase-4 commits pushed to its head; PR URL + head SHA recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Observe ci.yml and docs.yml go green on the PR head</name>
  <files>(CI observation — no working-tree file edits)</files>
  <read_first>
    - 04-VALIDATION.md "Sampling Rate" -> "Before /gsd-verify-work: observed green ci.yml + docs.yml" and the full-suite command (push->observe full matrix)
    - 04-PATTERNS.md "Push -> observe = done (D-05)" (the exact `gh run view <id> --json jobs -q '[.jobs[].conclusion] | unique'` -> ["success"] check for both workflows)
    - ROADMAP.md Phase 4 success criterion 3 (CI remains green, no regression)
  </read_first>
  <action>
    Watch the CI runs triggered on the PR head. For `ci.yml`, wait for completion and assert every job concluded `success` across the full 3.10-3.13 x 3-OS matrix plus lint, type-check, coverage, build, and integration jobs. For `docs.yml`, assert the run concluded `success` end-to-end (including the multi-language PDF-copy step). Use `gh run list --branch <branch>` to find the run IDs and `gh run view <id> --json jobs -q '[.jobs[].conclusion] | unique'` to confirm `["success"]` for each. If any job is red, capture the failing job log, treat it as a regression to diagnose (do NOT close the phase), and surface it — a red run means the tooling refresh introduced a regression that must be fixed before the gate passes. Record both run URLs and their success conclusions in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `gh run view <ci-run-id> --json jobs -q '[.jobs[].conclusion] | unique'` returns `["success"]`
    - `gh run view <docs-run-id> --json jobs -q '[.jobs[].conclusion] | unique'` returns `["success"]`
    - the ci.yml run includes matrix jobs for Python 3.10, 3.11, 3.12, 3.13 plus lint/type/coverage/build/integration (spot-check job names via `gh run view <ci-run-id> --json jobs -q '.jobs[].name'`)
    - both run URLs recorded in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>gh run list --branch "$(git rev-parse --abbrev-ref HEAD)" --workflow ci.yml --limit 1 --json databaseId -q '.[0].databaseId' | xargs -I{} gh run view {} --json jobs -q '[.jobs[].conclusion] | unique' | grep -qx '["success"]'</automated>
  </verify>
  <done>Both `ci.yml` (full 3.10-3.13 matrix + lint/type/coverage/build/integration) and `docs.yml` observed green on the PR head; run URLs recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm observed-green CI and review Node-20 straggler findings</name>
  <action>Present the observed ci.yml + docs.yml run results, the PR (targeting main with the four per-surface commits), and the plan-04-02 Node-20 straggler findings to the developer for the final phase-gate confirmation (details in what-built / how-to-verify below); block until the developer approves. Do not merge or mark Phase 4 done until approved.</action>
  <what-built>The Phase-4 tooling refresh (dev-tool floor+ceiling bounds across pyproject.toml + tox.ini, regenerated uv.lock, upload-artifact@v7 / download-artifact@v8, README + ruff-comment 3.9->3.10 cleanup) is on an open PR targeting `main`, and CI has been observed green.</what-built>
  <how-to-verify>
    1. Open the PR URL recorded in Task 1 and confirm it targets `main` with the four per-surface commits.
    2. Open the `ci.yml` run URL from Task 2 — confirm all jobs are green across the 3.10-3.13 x 3-OS matrix plus lint, type-check, coverage, build, and integration.
    3. Open the `docs.yml` run URL — confirm it completed green end-to-end (including the PDF-copy step).
    4. Review the plan-04-02 SUMMARY straggler-check findings: confirm the three release/docs-only actions (`pypa/gh-action-pypi-publish`, `peaceiris/actions-gh-pages`, `softprops/action-gh-release`) had their Node runtimes recorded, and that any node20 straggler is listed as an explicit tracked/deferred item (not silently closed).
    5. Note that the `release.yml` download-artifact@v8 bump is not exercised by this PR (release.yml only fires on a tag push) — this is expected (Assumption A1, low risk).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 4 verification complete, or describe any red job / missing straggler finding to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR branch -> GitHub-hosted CI runners | The observed CI run executes the bumped dev tools + bumped actions on hosted runners; this is where a transitive-dep or action-runtime regression would surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | transitive-dep shift from uv.lock regen surfacing only in the full matrix | medium | mitigate | The observed CI matrix (D-05), not local single-Python tox, is the gate — it catches cross-version resolution regressions the NixOS-limited local pre-check cannot |
| T-04-06 | Repudiation | phase closed on unverified/assumed-green CI | medium | mitigate | Blocking human-verify checkpoint requires opening the actual ci.yml + docs.yml run URLs and confirming `["success"]`; no auto-advance — the checkpoint is not auto-approvable |
</threat_model>

<verification>
- PR exists, targets `main`, carries the four per-surface commits.
- `gh run view` reports `["success"]` for both ci.yml and docs.yml on the PR head; ci.yml covers the full 3.10-3.13 matrix + lint/type/coverage/build/integration.
- Human-verify checkpoint confirms the observed green and reviews the 04-02 straggler findings before the phase is marked done.
</verification>

<success_criteria>
- An open PR to `main` with observed-green `ci.yml` + `docs.yml` on its head.
- No regression from any dev-tool or action bump (all jobs `success`).
- Human sign-off recorded; any Node-20 straggler carried as a tracked/deferred item.
</success_criteria>

<output>
Create `.planning/phases/04-refresh-dev-tooling/04-04-SUMMARY.md` when done.
</output>
