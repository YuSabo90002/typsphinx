---
phase: 52-v0-8-0-release-prep-prep-only
plan: 05
type: execute
wave: 3
depends_on: ["52-02", "52-03"]
files_modified:
  - .planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md
autonomous: true
requirements: [REL-07]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`tox -e docs-html` and `tox -e docs-pdf` both build clean against the post-bump tree, and `docs-pdf` produces its PDF through typsphinx's own `typstpdf` builder — the project dogfooding itself (ROADMAP SC#3, toolchain half)."
    - "The full-corpus Sphinx `doc/` `-b typstpdf` GATE-02 gate is re-run against the post-bump tree and its result is recorded as PASSED or as SKIPPED-with-reason, never conflated — `tests/test_corpus_gate.py` is `@pytest.mark.slow` and calls `pytest.skip` rather than failing when the corpus is unavailable, so a summary line reading `0 failed` is NOT evidence the gate ran."
    - "A full local `pytest` spot-check over the whole suite is run and recorded as a SPOT-CHECK, explicitly not as authority — pytest / lint / type / matrix authority belongs to the dispatched CI run collected by plan 52-04 (D-08)."
    - "The two environments that cannot provision on this machine are named and their non-execution explained: a bare `tox` (its `env_list` includes `lint`, and `ruff` is a generic-linux ELF the NixOS stub loader rejects at exit 127) and `tox -e py312` (`uv venv -p cpython3.12` downloads a standalone CPython with the same ELF problem)."
    - "Every `tox` environment is invoked individually with `-e`; no bare `tox` is run anywhere in this plan."
    - "Nothing under `typsphinx/` changed in this plan, and no test gate was weakened to obtain a green result (D-01, prep-only fence)."
  artifacts:
    - path: ".planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md"
      provides: "the local half of SC#3 — both docs builds, the produced PDF, the full-corpus gate, and an honest executed-versus-skipped register"
      contains: "docs-pdf"
  key_links:
    - from: "the post-bump tree"
      to: "docs/_build/pdf/typsphinx.pdf"
      via: "`tox -e docs-pdf` runs `sphinx-build -b typstpdf` over this project's own docs, so the PDF is produced by the very builder this release ships — the dogfooding check CI does not perform"
      pattern: "typstpdf"
    - from: "Sphinx's own 154-document `doc/` corpus"
      to: "a fatal-free `-b typstpdf` compile"
      via: "tests/test_corpus_gate.py clones and builds the corpus at real scale, which is the only place COMP-12's state/context multi-pass layout convergence is exercised outside a fixture"
      pattern: "test_corpus_gate"
  prohibitions:
    - statement: "No git tag named v0.8.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged; a PR would also fire CI a second, unintended way and is the publish half's first step."
      status: unverified
      verification: flagged
    - statement: "No second tag is created on the typsphinx-doc-translations repository and its pin is not advanced."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed — the four minor defects this milestone's own reviews filed ship unfixed per D-01."
      status: unverified
      verification: flagged
    - statement: "No test gate is weakened — no skip guard loosened, no marker removed, no assertion deleted — to make a run report green."
      status: unverified
      verification: flagged
    - statement: "A SKIPPED result is never recorded, summarised or rolled up as a PASSED result; the two are kept distinct at the per-test level."
      status: unverified
      verification: flagged
    - statement: "No local run is presented as the authority for pytest, lint, type or the OS matrix; that authority belongs to the dispatched CI run (D-08)."
      status: unverified
      verification: flagged
    - statement: "REL-07's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped; the requirement stays Pending until the publish."
      status: unverified
      verification: flagged
    - statement: "No figure is transcribed from the phase's CONTEXT or RESEARCH document into an acceptance criterion or an evidence file; every measured value is re-run live at execution time."
      status: unverified
      verification: flagged
---

<objective>
Collect the half of SC#3 that CI structurally does not produce: both docs builds against the post-bump
tree, and the full-corpus `-b typstpdf` GATE-02 gate — with the corpus gate's PASSED-versus-SKIPPED
distinction preserved, because that gate skips rather than fails when the corpus is unavailable.

Purpose: discharges the local half of ROADMAP Phase 52 SC#3. D-08 splits the evidence: the dispatched CI
run (plan 52-04) is the authority for pytest, lint, type and the OS matrix; the docs builds and the
corpus gate run here, because CI runs neither. This plan runs alongside 52-04 and neither depends on
the other's result.

Output: `52-GREEN-TREE-EVIDENCE.md` with verbatim transcripts and an honest executed-versus-skipped
register.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-CONTEXT.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

The phase-level list lives in `52-01-PLAN.md` § "Artifacts this phase produces". This plan produces one
planning artifact, `.planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md`, and
changes no source file.

<tasks>

<task type="auto">
  <name>Task 1: Run both docs builds against the post-bump tree and record the produced PDF</name>
  <precondition>HEAD carries `version = "0.8.0"` in `pyproject.toml` and a `## [0.8.0]` heading in `CHANGELOG.md` — plans 52-01 and 52-02 have merged back. `tox` must be provisioned through `tox-uv-bare`, which Phase 45.2 landed; if `tox` itself cannot start, stop and report rather than substituting a different runner.</precondition>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md</files>
  <reversibility rating="reversible">Read-only build runs plus one planning artifact.</reversibility>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — MANDATORY: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` first, then every command through `uv run`. Without it the docs build imports the UNCHANGED main-tree package and the build proves nothing about this tree.
    - `CLAUDE.md` § the `tox.ini` note — why `tox-uv-bare~=1.35` is pinned and must not be "simplified" back to `tox-uv`.
    - `tox.ini` — the `docs-html` and `docs-pdf` environments (their `extras`, `changedir` and commands), and the `env_list` whose `lint` member is why a bare `tox` cannot be used here.
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-GREEN-TREE-EVIDENCE.md` — the section structure to adapt. Drop Phase 46's "ja build (D-12)" section entirely: this milestone carries no CONF-12-shaped requirement.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md` § "Pitfall 3".
  </read_first>
  <action>
Provision first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

Confirm the tree is the post-bump one: `grep '^version = ' pyproject.toml` must read `0.8.0` and
`grep -c '^## \[0\.8\.0\]' CHANGELOG.md` must return `1`. If either fails, stop — waves 1 and 2 have not
merged back.

Run each environment INDIVIDUALLY, capturing each transcript verbatim:
`uv run --extra dev tox -e docs-html`, then `uv run --extra dev tox -e docs-pdf`.

**Never run a bare `tox`.** Its `env_list` includes `lint`, which dies at exit 127 on this machine
because `ruff` ships as a generic-linux ELF the NixOS stub loader rejects — a filed, still-open
toolchain defect (`.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md`),
not a validation gap: lint authority belongs to the CI run plan 52-04 collected (D-08). **Never run
`tox -e py312`** either: `uv venv -p cpython3.12` downloads a standalone CPython with the same ELF
problem and the environment cannot provision at all.

After `docs-pdf`, record the produced PDF as an artifact fact, not an assumption: its path, its size in
bytes from `stat`, and its first-page text extracted through `pypdf` (or, if that is impractical, its
page count) — enough to show the file is a real, non-empty PDF produced by this project's own
`typstpdf` builder rather than an empty stub.

Begin writing `.planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md` in English
with the headings `# Phase 52 — Green-Tree Evidence (Local Half of SC#3)`, a provisioning note,
`## Local evidence — docs builds`, `### tox -e docs-html`, `### tox -e docs-pdf`, and `### Produced PDF`.
Task 2 appends the remaining sections.

Take no irreversible action.
  </action>
  <verify>
    <automated>test "$(grep -c '^version = \"0.8.0\"$' pyproject.toml)" = "1" && uv run --extra dev tox -e docs-html && uv run --extra dev tox -e docs-pdf && test -s docs/_build/pdf/typsphinx.pdf && grep -q '^### tox -e docs-pdf' .planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra dev tox -e docs-html` exits 0 and its transcript ends with a `docs-html: OK` / congratulations line, recorded verbatim.
    - `uv run --extra dev tox -e docs-pdf` exits 0 and its transcript is recorded verbatim.
    - `docs/_build/pdf/typsphinx.pdf` exists and `stat -c %s docs/_build/pdf/typsphinx.pdf` reports a size greater than 100000 bytes; that figure appears in the evidence file.
    - `52-GREEN-TREE-EVIDENCE.md` contains the headings `## Local evidence — docs builds`, `### tox -e docs-html`, `### tox -e docs-pdf`, and `### Produced PDF`.
    - The evidence file contains no transcript of a bare `tox` invocation and no transcript of `tox -e py312`.
    - `git diff --name-only -- typsphinx/` and `git diff --name-only -- tox.ini pyproject.toml` all produce no output.
  </acceptance_criteria>
  <done>Both docs environments build clean against the post-bump tree and the `typstpdf`-produced PDF is recorded as a measured artifact, not an assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full-corpus GATE-02 gate and record executed-versus-skipped honestly</name>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md</files>
  <reversibility rating="reversible">Read-only test runs plus appended evidence sections.</reversibility>
  <read_first>
    - `tests/test_corpus_gate.py` — in particular its network-availability branch, which calls `pytest.skip` and NEVER fails when the corpus cannot be cloned or the tag cannot be resolved. This is the whole reason the pass/skip distinction must be recorded per test rather than read off a summary line.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md` § "Pitfall 4".
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md` § "Skip-vs-pass discipline".
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-VALIDATION.md` § "Sampling Rate" — the "PASSED or honestly SKIPPED, never conflated" bar this task is measured against.
  </read_first>
  <action>
Run the full-corpus gate with per-test visibility and a machine-readable result:
`uv run pytest tests/test_corpus_gate.py -v -m "" --junit-xml="${TMPDIR:-/tmp}/52-05-corpus.xml"`.
Capture the transcript verbatim, INCLUDING each test's own PASSED or SKIPPED status line and, if
anything skipped, that test's skip reason verbatim.

Then run a whole-suite local spot-check: `uv run pytest tests/ -q --junit-xml="${TMPDIR:-/tmp}/52-05-suite.xml"`.
Record its `testsuite` attribute line (tests / failures / errors / skipped counts) and label it, in
words, a SPOT-CHECK — pytest authority for this phase is the dispatched CI run plan 52-04 collected, not
this run. If any test fails locally, do not paper over it: report the failure with its output and stop.

Append these sections to `52-GREEN-TREE-EVIDENCE.md`:

- `## Local evidence — full-corpus gate`, holding the corpus-gate transcript and the JUnit `testsuite`
  attributes, plus one sentence stating in plain words whether the gate RAN (a real
  `sphinx-build -b typstpdf` over Sphinx's own `doc/` corpus completed fatal-free) or SKIPPED, and if it
  skipped, the reason. If it skipped, also write `human_needed` explicitly against SC#3's corpus item —
  a skip is not a pass, and this phase must not claim otherwise.
- `## Local suite spot-check`, holding the whole-suite JUnit attributes and the explicit statement that
  this is a spot-check, not authority.
- `## Executed versus skipped`, a short table with one row per environment or gate — `docs-html`,
  `docs-pdf`, the corpus gate, the local suite, a bare `tox`, `tox -e py312` — each row recording
  RAN / SKIPPED / NOT RUN and the reason. The two NOT-RUN rows must name the ELF/stub-loader cause and
  cite the filed todo by filename, and must state that lint / type / matrix authority sits with CI per
  D-08.

Never assert a command's result from memory — run it and paste the transcript. Where a result is
genuinely unavailable, record `human_needed` honestly rather than inferring it. Take no irreversible
action.
  </action>
  <verify>
    <automated>uv run pytest tests/test_corpus_gate.py -v -m "" --junit-xml="${TMPDIR:-/tmp}/52-05-corpus.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/52-05-corpus.xml" && grep -q 'errors="0"' "${TMPDIR:-/tmp}/52-05-corpus.xml" && grep -q '^## Executed versus skipped' .planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md && grep -qE '^## Local evidence — full-corpus gate' .planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The corpus-gate JUnit XML reports `failures="0"` and `errors="0"`, and its `skipped` count is transcribed into the evidence file as a number, not summarised away.
    - The evidence file's `## Local evidence — full-corpus gate` section states in one unambiguous sentence whether `test_corpus_compiles_with_no_fatal_error` PASSED or SKIPPED; if SKIPPED, that section also contains the literal `human_needed` and the skip reason verbatim.
    - The whole-suite JUnit `testsuite` attribute line appears in the evidence file, and the section containing it contains the literal `spot-check` and an explicit statement that it is not authority.
    - `52-GREEN-TREE-EVIDENCE.md` contains the headings `## Local evidence — full-corpus gate`, `## Local suite spot-check`, and `## Executed versus skipped`.
    - The `## Executed versus skipped` table has a row for each of `docs-html`, `docs-pdf`, the corpus gate, the local suite, a bare `tox`, and `tox -e py312`, each with a RAN / SKIPPED / NOT RUN verdict and a reason.
    - The two NOT-RUN rows name `ruff` / the standalone CPython ELF cause and cite `2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` by filename.
    - `git diff --name-only -- tests/ typsphinx/` produces no output — no gate was edited to obtain a green result.
    - `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` both produce no output.
  </acceptance_criteria>
  <done>The full-corpus gate's result is recorded with its PASSED-or-SKIPPED status explicit, a whole-suite spot-check is recorded as a spot-check, and the executed-versus-skipped register names every environment that did not run and why.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a skipped test → a green claim | `tests/test_corpus_gate.py` skips rather than fails; a summary line read carelessly converts an unrun gate into evidence. This is the boundary this plan exists to police. |
| the network (a corpus clone) → the local build | The corpus gate clones an external repository; its unavailability must degrade to a recorded skip, never to a silent pass. |
| the working tree → `origin` / PyPI (the prep/publish fence) | Everything this plan does must stay on the reversible side of that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-19 | Repudiation | a SKIPPED corpus gate recorded as a PASSED corpus gate | high | mitigate | The gate is run with `-v` and a JUnit XML; acceptance requires the per-test PASSED/SKIPPED status in words, the skip count as a number, and a literal `human_needed` marker plus the verbatim reason if it skipped. |
| T-52-20 | Repudiation | a local run presented as authority for pytest / lint / type / matrix | medium | mitigate | The suite run is labelled `spot-check` in the evidence file by acceptance criterion, and the executed-versus-skipped table states that authority sits with CI per D-08. |
| T-52-21 | Tampering | a test gate or marker edited so a local run turns green | high | mitigate | `git diff --name-only -- tests/ typsphinx/` asserted empty; this plan writes only a planning artifact. |
| T-52-22 | Denial of service | a bare `tox` or `tox -e py312` consuming the plan in an unprovisionable environment | low | accept | Both are forbidden by the action and their non-execution is recorded with cause; the filed toolchain todo is the tracked repair path (Future requirement QUA-06). |
| T-52-03 | Tampering | the prep/publish fence | high | mitigate | Tag absence re-asserted in Task 2's acceptance; no push, dispatch, tag, upload, release or PR occurs in this plan. |
| T-52-SC | Tampering | npm/pip/cargo installs | medium | accept | No package is installed or added; `--extra dev` and the `docs` extra provision only what `uv.lock` already pins. `52-RESEARCH.md` records the Package Legitimacy Gate as **not applicable** — zero candidate package names, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. |
</threat_model>

<verification>
- `tox -e docs-html` and `tox -e docs-pdf` both exit 0 against the post-bump tree.
- `docs/_build/pdf/typsphinx.pdf` exists, is larger than 100000 bytes, and its size is recorded.
- The corpus gate's per-test PASSED or SKIPPED status is recorded in words, with the skip reason if it skipped.
- The whole-suite run is recorded and labelled a spot-check, not authority.
- The executed-versus-skipped table names every environment that did not run and why.
- `git diff --name-only -- tests/ typsphinx/ tox.ini pyproject.toml` produces no output.
</verification>

<success_criteria>
- Both docs builds are green on the post-bump tree and the `typstpdf`-produced PDF is measured (SC#3, local half).
- The full-corpus GATE-02 gate's result is recorded as PASSED or as SKIPPED-with-reason, never conflated (SC#3).
- No local run is presented as authority for pytest, lint, type or the OS matrix (D-08).
- No gate was weakened and nothing under `typsphinx/` changed.
</success_criteria>

<output>
Create `.planning/phases/52-v0-8-0-release-prep-prep-only/52-05-SUMMARY.md` when done.
</output>
