---
phase: 52-v0-8-0-release-prep-prep-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - uv.lock
  - .planning/phases/52-v0-8-0-release-prep-prep-only/52-BUMP-EVIDENCE.md
  - .planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md
autonomous: true
requirements: [REL-07]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`pyproject.toml` is the sole `0.8.0` version literal; `README.md`'s Status line and `uv.lock`'s own `typsphinx` entry moved with it in one commit, and no second hardcoded version string was introduced (ROADMAP SC#1)."
    - "`uv run python -c \"import typsphinx; print(typsphinx.__version__)\"` prints `0.8.0` — the editable-install `.dist-info`/`.pth` metadata was regenerated, not merely the literal edited."
    - "`uv sync --extra dev --locked` and `uv lock --check` both exit 0, proving `uv.lock` and `pyproject.toml` agree after the bump."
    - "The three version-sync guard modules (`tests/test_extension.py::test_version_matches_pyproject_toml`, `tests/test_readme_version_sync.py`, `tests/test_preview_version_sync.py`) are green on the post-bump tree with zero skips."
    - "The release machinery's own reader — `scripts/extract_changelog_section.py` — still runs against this tree and exits 0 for an already-published version, proving the end-to-end consumer path is live before plan 52-02 adds the `## [0.8.0]` section it will read next."
    - "The prep/publish fence is observed at phase head and recorded: `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` are both empty (ROADMAP SC#5, first of three observations; the roll-up and the handoff carry the standing two)."
    - "Every anchor figure this phase depends on — the `v0.7.1` commit SHA, the `origin/main` merge-base, the commits-ahead count, and the `.planning`-excluded shortstat — is RE-MEASURED live at execution time and recorded with its command, never transcribed from `52-CONTEXT.md` or `52-RESEARCH.md`."
    - "Nothing under `typsphinx/` changed in this plan — the four minor defects this milestone's own reviews filed ship unfixed per D-01, and the prep-only fence holds."
    - "`COVERAGE.md` exists in the phase directory as a reasoned no-external-API declaration, so the seal-time `api-coverage.verify-pre` re-run over the PLAN bodies cannot block on this project's standing false-positive shape."
  artifacts:
    - path: "pyproject.toml"
      provides: "the sole version literal for the v0.8.0 release"
      contains: "version = \"0.8.0\""
    - path: "README.md"
      provides: "the human-readable release status line, moved in lockstep"
      contains: "Stable (v0.8.0)"
    - path: "uv.lock"
      provides: "the regenerated lock whose own first-party typsphinx entry carries the new version"
      contains: "0.8.0"
    - path: ".planning/phases/52-v0-8-0-release-prep-prep-only/52-BUMP-EVIDENCE.md"
      provides: "SC#1's verbatim transcripts plus the phase-head anchor re-measurement and fence observation"
      contains: "SC#1"
    - path: ".planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md"
      provides: "the reasoned external-API coverage declaration the seal-time gate accepts in place of a matrix"
      contains: "No external API integration"
  key_links:
    - from: "pyproject.toml `[project] version`"
      to: "typsphinx.__version__"
      via: "importlib.metadata.version(\"typsphinx\") reads the installed .dist-info, which only `uv sync` regenerates — editing the literal alone does not move it"
      pattern: "importlib\\.metadata"
    - from: "pyproject.toml `[project] version`"
      to: "README.md Status line"
      via: "tests/test_readme_version_sync.py regex-parses the Status line and compares it against tomllib's parse of pyproject.toml, so the two can never be edited independently"
      pattern: "Stable \\(v0\\.8\\.0\\)"
    - from: "CHANGELOG.md `## [X.Y.Z]` headings"
      to: "release.yml's `validate` job"
      via: "scripts/extract_changelog_section.py is the ONE committed, pytest-covered extractor both `validate` and `create-release` call; this plan proves it runs against this tree before plan 52-02 authors the section it will read"
      pattern: "extract_changelog_section"
  prohibitions:
    - statement: "No git tag named v0.8.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged; a PR would also fire CI a second, unintended way and is the publish half's first step."
      status: unverified
      verification: flagged
    - statement: "No second tag is created on the typsphinx-doc-translations repository and its pin is not advanced."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed — the four minor defects this milestone's own reviews filed ship unfixed per D-01."
      status: unverified
      verification: flagged
    - statement: "REL-07's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped; the requirement stays Pending until the publish."
      status: unverified
      verification: flagged
    - statement: "No test gate is weakened — no skip guard loosened, no marker removed, no assertion deleted — to make a run report green."
      status: unverified
      verification: flagged
    - statement: "No figure is transcribed from the phase's CONTEXT or RESEARCH document into an acceptance criterion or an evidence file; every measured value is re-run live at execution time."
      status: unverified
      verification: flagged
    - statement: "No prohibition above is read as forbidding the two reversible actions D-08 depends on — a plain fast-forward push of the milestone branch to origin, and a workflow_dispatch of ci.yml on that branch."
      status: unverified
      verification: flagged
  flagged_assumptions:
    - statement: "Edge probe row (category: boundary, REL-07) is UNRESOLVED. Planner reading: the phase's only boundary is the version-literal boundary 0.7.1 -> 0.8.0 and the one step either side of it. This plan resolves the boundary as far as it can be resolved mechanically — the post-bump tree must read exactly 0.8.0 on all three surfaces and the pre-bump value 0.7.1 must survive nowhere as a release-surface literal — but no acceptance criterion can prove what happens 'one step past' the boundary (a 0.8.1 or 0.9.0 tree) because that tree does not exist. Recorded as an explicit flagged assumption, not auto-backstopped and not dismissed."
      status: unresolved
      verification: none
      source: "spec-less probe fallback section A — no 52-SPEC.md supplied a ## Edge Coverage section"
    - statement: "Edge probe row (category: precision, REL-07) is UNRESOLVED and is OWNED BY PLAN 52-02, where it bites: the semver ordering the CHANGELOG tail link block and the RELEASE_VERSIONS tuple both depend on. It is authored in full in 52-02's own flagged_assumptions block and is named here only so the pair is visible from the phase's entry plan. It is not dropped."
      status: unresolved
      verification: none
      source: "spec-less probe fallback section A — no 52-SPEC.md supplied a ## Edge Coverage section"
---

<objective>
The phase's tracer: prove the whole release-surface path end to end on one thin slice — the version
literal moves from `0.7.1` to `0.8.0` across all three surfaces, the editable-install metadata is
regenerated so `typsphinx.__version__` actually reports the new value, the three version-sync guard
modules stay green, and the release machinery's own reader still runs against this tree — with the
prep/publish fence and every anchor figure re-measured live before anything is edited.

Purpose: discharges ROADMAP Phase 52 SC#1 and lays down SC#5's first fence observation. This is the
phase's tracer because it is the only slice that touches every layer the phase will modify — the
release-surface literal, the lockfile, the installed package metadata, the pytest guard tier, and the
release-machinery consumer — end to end, on one path, for keeps. Nothing here is a stub: the bump is
the real bump, the guards are the real guards, and the extractor call is the real call
`release.yml`'s `validate` job makes.

Output: a post-bump tree whose literal, lockfile, README status line and installed metadata all
agree; `52-BUMP-EVIDENCE.md` with the transcripts and the live anchor re-measurement; and
`COVERAGE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-CONTEXT.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This is the phase-level list; sibling plans reference it rather than repeating it.

**Source tree (modified files and new symbols):**

| Artifact | Plan | Shape |
|---|---|---|
| `pyproject.toml:7` | 52-01 | literal moves to `version = "0.8.0"` |
| `README.md:347` | 52-01 | literal moves to `**Status**: Stable (v0.8.0) - Production ready` |
| `uv.lock` | 52-01 | regenerated; its first-party `typsphinx` block carries `version = "0.8.0"` |
| `CHANGELOG.md` — new `## [0.8.0] - <date>` heading and its `### Added` / `### Changed` / `### Fixed` / `### Verified` sections | 52-02 | new section, no `### Removed` |
| `CHANGELOG.md` tail link block — new line `[0.8.0]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.8.0`, and the `[Unreleased]` compare base advanced to `v0.8.0...HEAD` | 52-02 | one inserted line, one edited line |
| `tests/test_changelog_page_gate.py` — `"0.8.0"` appended to `RELEASE_VERSIONS` (13 → 14 entries) and the comment's count/range moved | 52-02 | tuple + comment edit only |
| `docs/source/changelog.rst` | 52-02 | read-and-confirm; measured 2026-08-15 to already carry all three `**Breaking:**` migration items at lines 15, 36, 57 — no edit expected |
| `tests/test_state_guard_shapes_gate.py` — new `_Build.pdf_page_texts()` helper method | 52-03 | new symbol |
| `tests/test_state_guard_shapes_gate.py` — new `TestThreeMasterGate.test_three_masters_each_carry_their_full_include_set_in_pdf` test method | 52-03 | new symbol |

**Planning artifacts (evidence family, adapted from Phase 46's):**

| Path | Plan |
|---|---|
| `.planning/phases/52-v0-8-0-release-prep-prep-only/52-BUMP-EVIDENCE.md` | 52-01 |
| `.planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md` | 52-01 |
| `.planning/phases/52-v0-8-0-release-prep-prep-only/52-GOAL-CLAIM-EVIDENCE.md` | 52-03 |
| `.planning/phases/52-v0-8-0-release-prep-prep-only/52-CI-EVIDENCE.md` | 52-04 |
| `.planning/phases/52-v0-8-0-release-prep-prep-only/52-GREEN-TREE-EVIDENCE.md` | 52-05 |
| `.planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md` | 52-06 |
| `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RELEASE-EVIDENCE.md` | 52-07 |
| `.planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md` | 52-07 |

**Reserved name — never produced:** no artifact in this phase may be named `52-VERIFICATION.md`.
`gsd-verifier` owns and clobbers that exact filename (46-CONTEXT D-15).

<assumption_delta_decision>
The `assumption-delta scan` detector returned `detected: true` with two `pluralization` signals, on
the terms `second` (from the ROADMAP prose "the standing **second** tag on
`typsphinx-doc-translations`") and `alongside` ("**Alongside** them, the goal claim is discharged").

- **Primary noun:** the **master** — a `typst_documents` entry and its per-master include edge set.
- **Decision:** `no-change` **for this phase**. Neither signal is a live identity-model change here.
  The second-repository tag has been a standing, already-adopted release cost since v0.6.4 D-07, and
  `alongside` is ordinary prose. The genuine singular→plural transition this milestone carries —
  "one `.typ` shared by every master" → "per-master wrapper files publishing their own include edge
  set" — was **promoted**, not added alongside, in Phases 47 and 49: the per-master edge set became
  the primary representation and the build-scoped `_included_docnames` ledger was removed outright
  (COMP-11). This prep-only phase records that promotion in the CHANGELOG; it makes no new modelling
  decision and changes nothing under `typsphinx/`.
- **Companion invariant test:** already accepted, and it is this phase's D-10 work — plan 52-03's
  extension of `TestThreeMasterGate` goes red the instant a future phase reintroduces the singular
  assumption (a shared child reaching only one master's PDF, or one master's content leaking into
  another's).
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end release-surface slice — re-measure the fence and anchors, then bump the version across all three surfaces and regenerate the editable install</name>
  <files>pyproject.toml, README.md, uv.lock</files>
  <precondition>The working tree is on the milestone branch with no uncommitted source changes, and `git tag -l v0.8.0` is empty before any edit is made. If a `v0.8.0` tag already exists locally or on origin, stop — the publish half has already run and this phase must not proceed.</precondition>
  <reversibility rating="reversible">A version literal, a lockfile entry and a README status line are all plain tracked-file edits; reverting is a single `git revert` plus one `uv sync`.</reversibility>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — provisioning is MANDATORY on this project, not conditional. Without `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` followed by `uv run` for every command, pytest imports the UNCHANGED main-tree package and the guard tests will report against the wrong tree.
    - `pyproject.toml` lines 1-60 — the `[project]` table (`version` at line 7), the `dependencies` array, and the `dev` / `docs` optional-dependency extras.
    - `README.md` lines 340-352 — the `**Status**` line at line 347 and the `**Python**` line below it.
    - `typsphinx/__init__.py` lines 1-30 — the `importlib.metadata.version("typsphinx")` derivation and its `PackageNotFoundError` fallback. READ ONLY; this file must not be edited.
    - `tests/test_readme_version_sync.py` — the assertion that couples the two literals.
    - `scripts/extract_changelog_section.py` — the extractor `release.yml`'s `validate` and `create-release` jobs both call, and its documented no-side-effects property.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md` § "1. `pyproject.toml:7`, `README.md:347`, `uv.lock`".
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md` § "Pattern 1" and § "Pitfall 3".
  </read_first>
  <action>
Provision the worktree FIRST, before any other command:
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Then run every subsequent command
through `uv run`. Use the bare `uv` on PATH, never `.venv/bin/uv`, which does not exist post-Phase-45.2.

**Step 1 — observe the fence and re-measure every anchor, before editing anything.** Run and capture
verbatim, each with its own command line: `git tag -l v0.8.0` (expect no output);
`git ls-remote --tags origin v0.8.0` (expect no output); `git rev-parse v0.7.1^{commit}` (the sweep
anchor — note that on this annotated tag a bare `git rev-parse v0.7.1` returns the TAG OBJECT sha,
not the commit, so the `^{commit}` peel is required); `git merge-base origin/main HEAD`;
`git merge-base --is-ancestor origin/main HEAD && echo ancestor`;
`git rev-list --count origin/gsd/v0.8.0-multi-master-composition..HEAD`; and
`git diff v0.7.1..HEAD --stat -- . ':(exclude).planning' | tail -1`. Record whatever these commands
print. Do NOT copy the figures in `52-CONTEXT.md` (155 commits ahead) or `52-RESEARCH.md` (157) into
your output — both were measured on earlier trees and the count has moved again since. If
`git merge-base --is-ancestor origin/main HEAD` fails, stop and report: `origin/main` is no longer an
ancestor and the "nothing to merge in" premise this phase rests on has changed.

**Step 2 — move the three version surfaces in one edit set.** Edit `pyproject.toml` line 7 from
`version = "0.7.1"` to `version = "0.8.0"`. This is the repository's sole version literal —
`typsphinx.__version__` derives from it indirectly through `importlib.metadata.version("typsphinx")`,
never from a second hardcoded string, and none may be added. Edit `README.md` line 347 from
`**Status**: Stable (v0.7.1) - Production ready` to
`**Status**: Stable (v0.8.0) - Production ready`; change only the version token and leave the
`**Python**: 3.12+ | **Sphinx**: 9.1+ | **Typst**: 0.15+` line below it untouched.

**Step 3 — regenerate the lock and the installed metadata.** Run `uv lock`, which rewrites `uv.lock`'s
own first-party `typsphinx` block (the one under `name = "typsphinx"` with `source = { editable = "." }`,
currently carrying `version = "0.7.1"`). No other line in `uv.lock` should move: this phase adds and
removes no dependency. Then run `uv sync --extra dev --locked`. This does two jobs at once — `--locked`
fails loudly on any lock/manifest disagreement, which is the acceptance bar CONTEXT names for the
regeneration procedure, and the sync regenerates the editable-install metadata (the
`typsphinx-0.8.0.dist-info/` directory and the `__editable__.typsphinx-0.8.0.pth` /
`__editable___typsphinx_0_8_0_finder.py` pair, whose filenames literally embed the version). Editing
`pyproject.toml` alone does NOT change what `importlib.metadata.version()` returns; this step is what
does.

**Step 4 — prove the slice end to end.** Read the version back with
`uv run python -c "import typsphinx; print(typsphinx.__version__)"`, expecting `0.8.0`. Run
`uv lock --check`, expecting exit 0. Then prove the release machinery's own consumer path is live on
this tree by running `uv run python scripts/extract_changelog_section.py 0.7.1` — the already-published
section — and confirming exit 0 with a non-empty body, and `uv run python scripts/extract_changelog_section.py 9.9.9`
and confirming exit 1 with a message on stderr. That pair is the extractor's own liveness proof: it
shows the reader `release.yml`'s `validate` job calls both finds a real section and rejects a missing
one, BEFORE plan 52-02 authors the `## [0.8.0]` section it will read next.

Do not edit any other file. `CHANGELOG.md`, `docs/source/changelog.rst` and
`tests/test_changelog_page_gate.py` belong to plan 52-02; `tests/test_state_guard_shapes_gate.py`
belongs to 52-03. Nothing under `typsphinx/` may change (D-01, prep-only fence). Take no irreversible
action of any kind: no tag, no tag push, no upload, no release, no pull request.
  </action>
  <verify>
    <automated>uv lock --check && uv sync --extra dev --locked && uv run python -c "import typsphinx, sys; sys.exit(0 if typsphinx.__version__ == '0.8.0' else 1)" && uv run python scripts/extract_changelog_section.py 0.7.1 > /dev/null && test -z "$(git tag -l v0.8.0)" && test -z "$(git ls-remote --tags origin v0.8.0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "0.8.0"$' pyproject.toml` returns `1`.
    - `grep -c '^version = "0.7.1"$' pyproject.toml` returns `0`.
    - `grep -c 'Stable (v0.8.0) - Production ready' README.md` returns `1`.
    - `grep -A2 'name = "typsphinx"' uv.lock` shows `version = "0.8.0"` and `source = { editable = "." }`.
    - `uv lock --check` exits 0 and `uv sync --extra dev --locked` exits 0.
    - `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints exactly `0.8.0`.
    - `uv run python scripts/extract_changelog_section.py 0.7.1` exits 0 and writes a non-empty body to stdout; `uv run python scripts/extract_changelog_section.py 9.9.9` exits non-zero with a message on stderr.
    - `diff <(git show HEAD:pyproject.toml | sed -n '/^dependencies = \[/,/^\]/p') <(sed -n '/^dependencies = \[/,/^\]/p' pyproject.toml)` produces no output — the runtime dependency array is byte-identical to HEAD's.
    - `git diff --name-only` lists exactly `README.md`, `pyproject.toml`, `uv.lock` — no fourth path, and in particular nothing under `typsphinx/`.
    - `git diff --name-only -- typsphinx/` produces no output.
    - `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` both produce no output.
  </acceptance_criteria>
  <done>All three version surfaces read `0.8.0`, the lock and manifest agree under `--locked`, the installed package metadata reports `0.8.0` on import, the release machinery's extractor runs against this tree in both its success and its failure direction, and the fence is observed empty.</done>
</task>

<task type="auto">
  <name>Task 2: Run the version-sync guard battery and record SC#1's evidence with the live anchor re-measurement</name>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-BUMP-EVIDENCE.md</files>
  <read_first>
    - `tests/test_extension.py` around `def test_version_matches_pyproject_toml` — what the drift guard actually compares (a `tomllib` parse against `importlib.metadata`, not a hardcoded literal).
    - `tests/test_preview_version_sync.py` — the four-surface lockstep guard and its three test functions.
    - `CLAUDE.md` § "The `@preview` version-sync hazard" — the surfaces plan 52-06's sweep will count.
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-BUMP-EVIDENCE.md` — the exact heading structure and evidence register to match.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md` § "6. `52-*-EVIDENCE.md` family" and § "Evidence culture".
  </read_first>
  <action>
Run the three guard modules and capture their output verbatim, each with its own summary line:
`uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml -v`,
`uv run pytest tests/test_readme_version_sync.py -v`, and
`uv run pytest tests/test_preview_version_sync.py -v`. All must pass with zero skips.

Do NOT run a bare `tox`: `tox.ini`'s `env_list` includes `lint`, which dies at exit 127 on this
machine because `ruff` ships as a generic-linux ELF the NixOS stub loader rejects
(`.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md`). Do NOT run
`tox -e py312` either: `uv venv -p cpython3.12` downloads a standalone CPython with the same ELF
problem. Lint, type and full-matrix authority belongs to the dispatched CI run collected by plan
52-04 (D-08), not to this plan.

Then write `.planning/phases/52-v0-8-0-release-prep-prep-only/52-BUMP-EVIDENCE.md` in English, with
these headings, following `46-BUMP-EVIDENCE.md`'s structure:

- a provisioning note naming `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and
  the `uv run` convention, per `CLAUDE.md`;
- `## SC#1 — version-literal lockstep`, holding the before/after values of the three surfaces
  (`pyproject.toml` line 7, `README.md` line 347, `uv.lock`'s `typsphinx` entry), plus the `uv lock`,
  `uv sync --extra dev --locked`, `uv lock --check` and `import typsphinx` read-back transcripts;
- `## Release-machinery consumer path`, holding both `scripts/extract_changelog_section.py`
  transcripts from Task 1 — the exit-0 run against `0.7.1` and the exit-1 run against `9.9.9` — and
  one sentence stating that this proves the reader is live BEFORE plan 52-02 authors the section it
  will read;
- `## Guard tests`, holding all three pytest transcripts verbatim, plus the combined JUnit-XML run's
  `testsuite` attributes;
- `## Phase-head anchor re-measurement`, holding every command from Task 1 step 1 with its own live
  output — the peeled `v0.7.1` commit sha, the `origin/main` merge-base, the ancestry check, the
  commits-ahead count, and the `.planning`-excluded shortstat — and one explicit sentence stating
  that these were RE-RUN at execution time and that the figures in `52-CONTEXT.md` and
  `52-RESEARCH.md` were deliberately not transcribed;
- `## SC#5 — fence observation at phase head`, holding the `git tag -l v0.8.0` and
  `git ls-remote --tags origin v0.8.0` transcripts with a `date -u` timestamp, and one sentence
  stating that this is an EXTRA observation and that the standing two independent observations are
  carried by `52-RELEASE-EVIDENCE.md` and `52-HANDOFF.md` (plan 52-07);
- `## Invariant spot-check`, recording that `tests/test_preview_version_sync.py` passed and that this
  plan changed no template, writer or import code, with the full mechanical sweep CITED as plan
  52-06's work rather than duplicated here;
- `## Executed versus skipped`, stating plainly that a bare `tox` and `tox -e py312` were not run and
  why, and that lint / type / full-matrix authority belongs to CI per D-08.

Never assert a command's result from memory — run it and paste the transcript. Where a result is
genuinely unavailable, record `human_needed` honestly rather than inferring it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q --junit-xml="${TMPDIR:-/tmp}/52-01-guards.xml" && grep -q 'skipped="0"' "${TMPDIR:-/tmp}/52-01-guards.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/52-01-guards.xml" && grep -q 'errors="0"' "${TMPDIR:-/tmp}/52-01-guards.xml" && grep -q '^## SC#1' .planning/phases/52-v0-8-0-release-prep-prep-only/52-BUMP-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The combined guard run produces a JUnit `testsuite` element carrying `skipped="0"`, `failures="0"` and `errors="0"`, with at least 5 tests (1 version-match + 1 readme-sync + 3 preview-sync).
    - `52-BUMP-EVIDENCE.md` exists, is written entirely in English, and contains the headings `## SC#1 — version-literal lockstep`, `## Release-machinery consumer path`, `## Guard tests`, `## Phase-head anchor re-measurement`, `## SC#5 — fence observation at phase head`, `## Invariant spot-check`, and `## Executed versus skipped`.
    - The evidence file contains the literals `0.7.1`, `0.8.0`, `uv sync --extra dev --locked`, `uv lock --check`, and `extract_changelog_section.py`.
    - The `## Phase-head anchor re-measurement` section contains the string `git merge-base` and a commits-ahead figure produced by a command shown in the same section; the figures `155` and `157` do NOT appear as this phase's own measurement.
    - The `## SC#5 — fence observation at phase head` section contains a `date -u` timestamp and names `52-RELEASE-EVIDENCE.md` and `52-HANDOFF.md` as the owners of the standing two observations.
    - The `## Invariant spot-check` section names plan 52-06 as the owner of the full D-09 sweep rather than restating its figures.
    - The `## Executed versus skipped` section names `tox -e py312` and a bare `tox`, and states for each why it was not run.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>All five version-sync guard assertions pass with zero skips on the post-bump tree, and SC#1's transcripts — including the live anchor re-measurement and an honest statement of what was not run locally — are on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Write the reasoned external-API coverage declaration</name>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md</files>
  <read_first>
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/COVERAGE.md` — the exact declaration form and register to match; it is a reasoned prose declaration with NO capability matrix, which the seal-time gate accepts.
    - `.planning/STATE.md` § "Blockers/Concerns" — the standing note that `api-coverage.verify-pre` false-positives on prose describing compile / render / API-read evidence, with three recorded overrides in v0.6.4.
  </read_first>
  <action>
Write `.planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md` as a reasoned declaration, in
English, following `46-COVERAGE.md`'s shape. Its first content line must be a single sentence
beginning with the literal `No external API integration:` followed by a one-line reason naming what
this phase touches instead — release-surface files (`pyproject.toml`, `uv.lock`, `README.md`,
`CHANGELOG.md`), two data constants and one test class in existing test modules, and planning
artifacts — plus the fact that the only remote surface read is this repository's own GitHub Actions
run status through the already-authenticated `gh` CLI.

Then add a short `**Why this declaration exists.**` paragraph recording that the detector was run over
the phase scope at plan time and returned `{"detected":false,"signals":[]}`, and that this file exists
for the seal-time re-run over the PLAN bodies, which are dense with `gh run view`, `git ls-remote` and
`release.yml` prose — the exact false-positive shape STATE.md names.

Finally add a `**No capability matrix is supplied**` paragraph stating that a reasoned declaration
with no rows is the correct form here because there is no external API being integrated, and that
inventing a table would fabricate rows for capabilities that do not exist.

Do not invent a capability matrix. Do not add rows.
  </action>
  <verify>
    <automated>test -f .planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md && grep -q 'No external API integration:' .planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md && test "$(grep -c '^| ' .planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md` exists and is written in English.
    - It contains a line beginning with the literal `No external API integration:` followed by a reason on the same line.
    - It contains the literal `{"detected":false,"signals":[]}`.
    - `grep -c '^| ' .planning/phases/52-v0-8-0-release-prep-prep-only/COVERAGE.md` returns `0` — no capability-matrix table rows were fabricated.
    - The file is NOT named `52-VERIFICATION.md` and no file with that name exists in the phase directory.
  </acceptance_criteria>
  <done>A reasoned, matrix-free external-API declaration is on disk in the phase directory, in the form the seal-time gate accepts.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pyproject.toml` version literal → the published artifact's identity | The literal edited here becomes the PyPI package version and the git tag name at `/gsd-complete-milestone`. |
| `uv.lock` → the CI and release-build environments | Every CI job and the release build provision from this lockfile. |
| the working tree → `origin` and PyPI (the prep/publish fence) | Everything this phase does must stay on the reversible side of that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Spoofing | version identity across `pyproject.toml` / `uv.lock` / `README.md` / installed `.dist-info` | high | mitigate | All four are asserted to agree: `uv lock --check`, `uv sync --extra dev --locked`, the `importlib.metadata` read-back, and `tests/test_readme_version_sync.py`. A tree whose declared version differs from its installed metadata would publish an artifact that does not match the reviewed tree. |
| T-52-02 | Tampering | `uv.lock` dependency set silently moving during regeneration | high | mitigate | Acceptance asserts `git diff --name-only` lists exactly three paths and that the `[project] dependencies` array is byte-identical to HEAD's; plan 52-06 re-asserts it mechanically against the `v0.7.1` anchor. |
| T-52-03 | Tampering | the prep/publish fence | high | mitigate | No tag, tag push, PyPI upload, GitHub Release or PR is created; `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` are asserted empty in `<verify>` and in acceptance, and recorded with a `date -u` timestamp in the evidence file. |
| T-52-04 | Repudiation | evidence transcribed from a prior document instead of re-measured | high | mitigate | Acceptance asserts the anchor section shows its own commands and that the stale figures `155`/`157` do not appear as this phase's own measurement. |
| T-52-05 | Elevation of privilege | a `typsphinx/` source change riding in under a release-prep phase | medium | mitigate | `git diff --name-only -- typsphinx/` asserted empty in two separate tasks. |
| T-52-SC | Tampering | npm/pip/cargo installs | medium | accept | `uv sync --extra dev --locked` installs only what the existing lock already pins; no package is added. `52-RESEARCH.md` records the Package Legitimacy Gate as **not applicable** — zero candidate package names, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. If `uv lock` produces an unexpected new transitive dependency, stop and escalate rather than proceeding. |
</threat_model>

<verification>
- `uv lock --check` exits 0 and `uv sync --extra dev --locked` exits 0.
- `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints `0.8.0`.
- The guard battery reports `skipped="0"`, `failures="0"` and `errors="0"`.
- Both `scripts/extract_changelog_section.py` runs behave as specified (exit 0 / exit 1).
- `git diff --name-only` lists exactly `README.md`, `pyproject.toml`, `uv.lock` plus the two planning artifacts.
- `git diff --name-only -- typsphinx/` produces no output.
- `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` are both empty.
</verification>

<success_criteria>
- `pyproject.toml` is the sole `0.8.0` literal, with `README.md` and `uv.lock` moved in lockstep (SC#1).
- The editable-install metadata was regenerated, so `typsphinx.__version__` reports `0.8.0` (SC#1).
- All three version-sync guard modules pass with zero skips (SC#1).
- Every anchor figure was re-measured live and recorded with its command (SC#4 precondition, SC#5).
- The fence is observed empty at phase head (SC#5, first of three observations).
- `52-BUMP-EVIDENCE.md` and `COVERAGE.md` are on disk; no file named `52-VERIFICATION.md` exists.
</success_criteria>

<output>
Create `.planning/phases/52-v0-8-0-release-prep-prep-only/52-01-SUMMARY.md` when done.
</output>
