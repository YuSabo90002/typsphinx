---
phase: 52-v0-8-0-release-prep-prep-only
plan: 07
type: execute
wave: 4
depends_on: ["52-04", "52-05", "52-06"]
files_modified:
  - .planning/phases/52-v0-8-0-release-prep-prep-only/52-RELEASE-EVIDENCE.md
  - .planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md
autonomous: true
requirements: [REL-07]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single roll-up artifact, `52-RELEASE-EVIDENCE.md`, gives one verdict per ROADMAP success criterion (SC#1 through SC#5) by CITING the four evidence artifacts the earlier plans produced, never by restating their figures as if re-measured."
    - "A standalone publish checklist, `52-HANDOFF.md`, exists for `/gsd-complete-milestone`, covering merge -> tag -> `release.yml` (with an explicit item to OBSERVE `create-release` succeed, the job that failed at the v0.7.0 close) -> PyPI + GitHub Release -> the second tag on `typsphinx-doc-translations` -> the Read the Docs `stable` measurement on both projects (ROADMAP SC#5)."
    - "The handoff states EXPLICITLY that REL-07 remains open until the publish and is not reported complete on the strength of the prep being correct — this is the exact failure mode (lesson 12b) that lost REL-04 at the v0.7.0 close, and REL-07's checkbox and Traceability row in `REQUIREMENTS.md` stay `Pending` through this phase."
    - "The four minor defects this milestone's own reviews filed are named INDIVIDUALLY in the handoff, each with its `.planning/todos/pending/*.md` filename, its measured reachability condition, and the reason it ships unfixed — because D-01 and D-03 make the todo ledger and this handoff the ONLY records of them: no `### Known Limitations` CHANGELOG section, no GitHub issue, and no ROADMAP backlog promotion, so an omission here has no second surface."
    - "The `:numref:` record is carried forward as a todo with the same treatment (D-03), excluded from every published surface by 51-CONTEXT D-07 and classified as a bug for a later milestone rather than a published limitation."
    - "The prep/publish fence is observed a second and third time, in two separate artifacts at two separate moments, each with its own `date -u` timestamp — the standing convention since `35-HANDOFF.md` (ROADMAP SC#5)."
    - "Every commit on the branch AFTER the CI authority run's recorded SHA touches only paths under `.planning/`, so the dispatched authority run genuinely covers every source change this phase made."
    - "`.planning/REQUIREMENTS.md` is diffed after any closeout automation runs and REL-07's row is reverted if it was auto-flipped — the `phase.complete` auto-flip hazard is recorded, repeated, and was caught in Phase 41 and pre-empted in Phase 42 (RESEARCH Pitfall 5)."
    - "Nothing under `typsphinx/` changed in this plan (D-01, prep-only fence)."
  artifacts:
    - path: ".planning/phases/52-v0-8-0-release-prep-prep-only/52-RELEASE-EVIDENCE.md"
      provides: "the SC#1-SC#5 roll-up with one verdict per criterion and the first of the two standing fence observations"
      contains: "Phase verdict"
    - path: ".planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md"
      provides: "the standalone publish checklist for /gsd-complete-milestone, the deferred-defect register, and the second of the two standing fence observations"
      contains: "REL-07 remains open"
  key_links:
    - from: "52-HANDOFF.md"
      to: "/gsd-complete-milestone"
      via: "the handoff is the only document the publish command reads for this milestone's owner-manual steps; it must stand alone, without requiring the reader to reconstruct context from PLAN or SUMMARY files"
      pattern: "complete-milestone"
    - from: ".planning/todos/pending/ (the four minor defect records plus the :numref: record)"
      to: "52-HANDOFF.md's deferred register"
      via: "D-01 and D-03 make these two the only surfaces recording the deferrals — no CHANGELOG section, no GitHub issue, no ROADMAP backlog item — so an omission in the handoff loses the record entirely"
      pattern: "todos/pending"
    - from: "the CI authority run's recorded SHA (plan 52-04)"
      to: "the branch tip at phase close"
      via: "every commit in between must touch only .planning/, which is what makes the authority run's green result cover the phase's entire source delta"
      pattern: "\\.planning/"
  prohibitions:
    - statement: "No git tag named v0.8.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged; a PR would also fire CI a second, unintended way and is the publish half's first step."
      status: unverified
      verification: flagged
    - statement: "No second tag is created on the typsphinx-doc-translations repository and its pin is not advanced; the handoff DESCRIBES that step, it does not perform it."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed — the four minor defects this milestone's own reviews filed ship unfixed per D-01."
      status: unverified
      verification: flagged
    - statement: "REL-07's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped by this phase; if closeout automation flips either, the change is reverted before commit and the revert is recorded."
      status: unverified
      verification: flagged
    - statement: "No artifact produced by this phase is named 52-VERIFICATION.md; that filename is reserved by gsd-verifier and would be clobbered."
      status: unverified
      verification: flagged
    - statement: "No `### Known Limitations` section is added to the CHANGELOG and no GitHub issue is filed for the four deferred defects; they are disclosed internally only, per D-01."
      status: unverified
      verification: flagged
    - statement: "The deferred defects are not promoted to ROADMAP backlog items; they stay in the todo ledger, per D-03."
      status: unverified
      verification: flagged
    - statement: "No figure from an earlier plan's evidence artifact is restated in the roll-up as though independently re-measured; the roll-up cites, it does not re-assert."
      status: unverified
      verification: flagged
---

<objective>
Close the phase: roll the four evidence artifacts up into one verdict per success criterion, write the
standalone publish checklist `/gsd-complete-milestone` will read, and record — plainly and in the one
place that has no second surface — the four defects that ship unfixed and the fact that REL-07 is still
open.

Purpose: discharges ROADMAP Phase 52 SC#5 and gives SC#1-SC#4 their single roll-up verdict. Two of this
project's own recorded lessons bear directly on this plan. **12a:** the release-prep phase is the one
phase whose `phase_complete=true` has, four milestones running, never been independently
machine-verified before close. **12b:** a requirement reported complete on the strength of the code
being correct is exactly how v0.7.0 lost REL-04 — so this plan's most important single sentence is the
one saying REL-07 is NOT closed here.

Output: `52-RELEASE-EVIDENCE.md` and `52-HANDOFF.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-CONTEXT.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

The phase-level list lives in `52-01-PLAN.md` § "Artifacts this phase produces". This plan produces two
planning artifacts — `52-RELEASE-EVIDENCE.md` and `52-HANDOFF.md` — and changes no source file. Neither
may be named `52-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: Roll the evidence up into one verdict per success criterion</name>
  <precondition>All four evidence artifacts exist and are committed: `52-BUMP-EVIDENCE.md` (plan 52-01), `52-GOAL-CLAIM-EVIDENCE.md` (52-03), `52-CI-EVIDENCE.md` (52-04), `52-GREEN-TREE-EVIDENCE.md` (52-05) and `52-SC4-INVARIANTS.md` (52-06). If any is missing, stop — the roll-up would be citing a document that does not exist.</precondition>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-RELEASE-EVIDENCE.md</files>
  <reversibility rating="reversible">A planning artifact.</reversibility>
  <read_first>
    - All five sibling evidence artifacts in the phase directory, in full — `52-BUMP-EVIDENCE.md`, `52-GOAL-CLAIM-EVIDENCE.md`, `52-CI-EVIDENCE.md`, `52-GREEN-TREE-EVIDENCE.md`, `52-SC4-INVARIANTS.md`. The roll-up cites them; it must therefore know exactly what each one actually recorded, including any `human_needed` or SKIPPED result.
    - `.planning/ROADMAP.md` § "Phase 52" — the five success criteria verbatim, which are the rows this roll-up must give verdicts for.
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-RELEASE-EVIDENCE.md` — the roll-up structure and the register to match.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md` § "6. `52-*-EVIDENCE.md` family" — the `46-RELEASE-EVIDENCE.md` heading skeleton.
  </read_first>
  <action>
Write `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RELEASE-EVIDENCE.md` in English with the
headings `# Phase 52: v0.8.0 Release Prep (prep-only) — Release Evidence`, `## SC#1`, `## SC#2`,
`## SC#3` (with `### CI authority`, `### Local half`, `### Goal-claim half`, `### SC#3 roll-up verdict`),
`## SC#4` (with `### SC#4 roll-up verdict`),
`## SC#5: no irreversible action taken — the fence, observation 1 of 2` (with
`### SC#5 (observation 1) verdict`), `## Phase verdict`, and `## Executed versus skipped`.

**Cite, do not restate.** Each success-criterion section names the sibling artifact that holds the
primary evidence and quotes at most the one line that carries the verdict — a run id, a job-conclusion
summary, a PASSED/SKIPPED status, a diff-empty result. Do NOT copy figures out of a sibling artifact and
present them as though this plan re-measured them; that is the transcription failure mode the phase's
own prohibitions forbid. Where a figure genuinely needs restating, attribute it: name the artifact and
the section it came from.

**SC#3 has three parts, not two.** The CI authority run (52-04), the local half — docs builds and the
full-corpus gate (52-05), and the goal-claim half — the multi-master PDF round trip (52-03). Give each
its own subsection and then a single SC#3 verdict. If the corpus gate SKIPPED rather than ran, SC#3's
verdict must say so and carry the `human_needed` marker forward rather than rounding it to MET.

**SC#5 observation 1 of 2.** Run and record, with a `date -u` timestamp: `git tag -l v0.8.0` (expect
empty), `git ls-remote --tags origin v0.8.0` (expect empty),
`gh pr list --head gsd/v0.8.0-multi-master-composition --json number,state` (expect an empty list), and
`gh run list --workflow=release.yml --limit 5 --json databaseId,createdAt,event` showing no run started
by this phase. State that this is observation 1 of 2 and that `52-HANDOFF.md` carries observation 2 at a
later moment.

**Prove the authority run covers the whole source delta.** Read the authority SHA out of
`52-CI-EVIDENCE.md`, then run
`git diff --name-only <authority-sha>..HEAD -- . ':(exclude).planning'` and show it is EMPTY — every
commit since the CI run touched only planning artifacts. Record the command and its output in the
`### CI authority` subsection. If it is not empty, SC#3's CI half does not cover the current tree: say
so, name the files, and do not round the verdict to MET.

**`## Phase verdict`** gives one line per criterion — MET / NOT MET / MET WITH CAVEAT (naming the
caveat) — and then one overall sentence. It must ALSO state, in its own sentence, that REL-07 is NOT
closed by this phase and remains `Pending` until `/gsd-complete-milestone`.

**`## Executed versus skipped`** consolidates what did not run across the whole phase — a bare `tox`,
`tox -e py312`, local `ruff` — each with its cause, citing the sibling artifact that recorded it.

Take no irreversible action.
  </action>
  <verify>
    <automated>test -f .planning/phases/52-v0-8-0-release-prep-prep-only/52-RELEASE-EVIDENCE.md && grep -q '^## Phase verdict' .planning/phases/52-v0-8-0-release-prep-prep-only/52-RELEASE-EVIDENCE.md && grep -q '^## SC#5' .planning/phases/52-v0-8-0-release-prep-prep-only/52-RELEASE-EVIDENCE.md && test -z "$(git tag -l v0.8.0)" && test -z "$(git ls-remote --tags origin v0.8.0)" && test ! -f .planning/phases/52-v0-8-0-release-prep-prep-only/52-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `52-RELEASE-EVIDENCE.md` exists, is written entirely in English, and contains the headings `## SC#1`, `## SC#2`, `## SC#3`, `## SC#4`, `## SC#5: no irreversible action taken — the fence, observation 1 of 2`, `## Phase verdict`, and `## Executed versus skipped`.
    - The `## SC#3` section contains all three subsections `### CI authority`, `### Local half` and `### Goal-claim half`, and each names its source artifact filename verbatim.
    - Each of `52-BUMP-EVIDENCE.md`, `52-GOAL-CLAIM-EVIDENCE.md`, `52-CI-EVIDENCE.md`, `52-GREEN-TREE-EVIDENCE.md` and `52-SC4-INVARIANTS.md` is named at least once in the roll-up.
    - The `### CI authority` subsection contains the authority run's SHA and the verbatim output of `git diff --name-only <authority-sha>..HEAD -- . ':(exclude).planning'`; if that output is non-empty, the SC#3 verdict is not MET and the files are named.
    - The `## SC#5` section contains a `date -u` timestamp and the verbatim output of all four fence commands, plus the literal `observation 1 of 2`.
    - The `## Phase verdict` section contains one verdict line for each of SC#1 through SC#5, and a sentence containing the literal `REL-07` stating it is not closed by this phase.
    - If `tests/test_corpus_gate.py` SKIPPED in plan 52-05, the SC#3 verdict carries the literal `human_needed`; if it PASSED, it does not.
    - No file named `52-VERIFICATION.md` exists in the phase directory.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>One roll-up artifact gives an unambiguous verdict for each of the five success criteria, cites rather than restates its sources, proves the CI authority run covers the whole source delta, and states plainly that REL-07 is not closed here.</done>
</task>

<task type="auto">
  <name>Task 2: Write the standalone publish checklist and the deferred-defect register</name>
  <precondition>`52-RELEASE-EVIDENCE.md` exists and carries a phase verdict, so the handoff can state what the prep did and did not satisfy without re-deriving it.</precondition>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md</files>
  <reversibility rating="reversible">A planning artifact.</reversibility>
  <read_first>
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md` — IN FULL. It is the shape SC#5 asks for: the "What this phase satisfied, and what it did not" opening, the numbered checklist, the "Not done in this phase, by design", "Deferred by decision, not oversight" and "Proof the fence held" sections, and the prose register to match.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-CONTEXT.md` § `<deferred>` — the SOURCE LIST to transcribe: the four minor defects with their measured reachability conditions, the `:numref:` record, and the per-record disposition reasoning for all nine reviewed-but-not-folded todos.
    - `.planning/todos/pending/` — list the directory and read each of the five records the handoff must name, so each is cited by its real filename rather than by a remembered one.
    - `.planning/STATE.md` § "Accumulated Context" and § "Shipped Milestone (v0.7.1 — archived)" — the standing two-repository tagging cost (v0.6.4 D-07), and the Read the Docs `stable` measurement procedure both projects were verified against at the last two closes.
    - `.github/workflows/release.yml` — the four jobs the checklist must name in order (`validate` -> `build` -> `publish-pypi` -> `create-release`), and in particular `create-release`, the job that failed at the v0.7.0 close with `uv: command not found` and whose success is the item SC#5 requires an explicit observation of.
  </read_first>
  <action>
Write `.planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md` in English, following
`46-HANDOFF.md`'s structure.

**Opening — `## What this phase satisfied, and what it did not`.** One short paragraph per success
criterion, citing `52-RELEASE-EVIDENCE.md` for the verdicts. Then, in its own paragraph and in
unambiguous words: **REL-07 remains open.** It closes at `/gsd-complete-milestone`, on the publish, not
here. Its checkbox and its Traceability row in `.planning/REQUIREMENTS.md` are still `Pending` and this
phase did not change them. Name lesson 12b explicitly — a requirement reported complete on the strength
of the code being correct is how v0.7.0 lost REL-04 — so a later reader understands the sentence is
deliberate rather than an omission.

**`## Checklist`** — numbered `###` items, each stating the action, the exact command or UI path, and
what to observe:

1. Open the pull request against `main` and merge it.
2. Push the `v0.8.0` tag on the merge commit.
3. Let `release.yml` run to completion, naming its four jobs in order — `validate`, `build`,
   `publish-pypi` (which waits on owner approval of the `pypi` environment), and `create-release`. Say
   explicitly that `create-release` must be OBSERVED to succeed, and why: it failed at the v0.7.0 close
   at `uv: command not found`, the release body was repaired by hand, and it was only proven end to end
   at the v0.7.1 close.
4. Advance the `typsphinx-doc-translations` pin and tag `v0.8.0` there. This item STAYS — it is a
   standing cost adopted at v0.6.4 (D-07) and ROADMAP SC#5 names it explicitly; do not drop it.
5. Confirm Read the Docs `stable` is green and reports `0.8.0` on BOTH projects (`en` and the `ja`
   project slug), measuring through RTD's unauthenticated public API and a real fetch of each PDF,
   as the last two closes did — not by asking.
6. Flip REL-07's checkbox and its Traceability row in `.planning/REQUIREMENTS.md` — at the publish, and
   only then.
7. Re-date the `## [0.8.0]` CHANGELOG heading if the merge landed on a different UTC day than the one
   plan 52-02 wrote, and re-confirm `scripts/extract_changelog_section.py 0.8.0` still exits 0
   afterwards.

**`## Not done in this phase, by design`** — the prep/publish fence, enumerated: no tag, no
`release.yml` run, no PyPI upload, no GitHub Release, no pull request, no second-repository tag, and no
`typsphinx/` code change.

**`## Deferred by decision, not oversight`** — this is the section with no second surface, and it is
the one to get exactly right. Name each of the four minor defects INDIVIDUALLY, each with: its
`.planning/todos/pending/` filename verbatim, its severity, its measured reachability condition, and one
sentence on why it ships unfixed. Transcribe the reachability conditions from `52-CONTEXT.md`
`<specifics>` item 6 — the label-collision false negative in the compile-time xref guard, the unescaped
include-edge-key separators, the unbounded recursion in the master-edge-key derivation, and the escape
branch keying on basename while the collision branch keys on the full relative URI. Then record, in its
own paragraph, that D-01 keeps all four to internal disclosure only — no limitations section in the
CHANGELOG, no GitHub issue — and that this is the v0.7.1 D-27 shape applied a second time, with the one
distinction the owner had on the table when deciding: all four here are NEW failure classes created by
features this milestone shipped, which was not true of the v0.7.1 pair. Then record the `:numref:`
record the same way, noting it is excluded from every published surface by 51-CONTEXT D-07 and is
classified as a bug for a later milestone rather than a limitation to publish. Finally state that all
five stay in the todo ledger and are NOT promoted to ROADMAP backlog items (D-03), and list the
remaining reviewed-but-not-folded todos with their one-line reasons from `52-CONTEXT.md` `<deferred>` —
including the flagged question of whether the already-closed REL-04 record belongs in `todos/completed/`
rather than `pending/`.

**`## Proof the fence held`** — observation 2 of 2. Run `git tag -l v0.8.0` and
`git ls-remote --tags origin v0.8.0` again, at this later moment, and record both with a fresh `date -u`
timestamp. State that this is the second of two independent observations and name
`52-RELEASE-EVIDENCE.md` as the holder of the first.

The document must stand alone: a reader with only this file and the repository must be able to execute
the publish without opening a PLAN or SUMMARY. Take no irreversible action — this file DESCRIBES the
publish, it does not perform any part of it.
  </action>
  <verify>
    <automated>test -f .planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md && grep -q 'REL-07 remains open' .planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md && grep -q '^## Proof the fence held' .planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md && grep -q 'create-release' .planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md && grep -q 'typsphinx-doc-translations' .planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md && test "$(grep -c '2026-08-1[24]-' .planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md)" -ge 4 && test -z "$(git tag -l v0.8.0)"</automated>
  </verify>
  <acceptance_criteria>
    - `52-HANDOFF.md` exists, is written entirely in English, and contains the headings `## What this phase satisfied, and what it did not`, `## Checklist`, `## Not done in this phase, by design`, `## Deferred by decision, not oversight`, and `## Proof the fence held`.
    - It contains the literal string `REL-07 remains open`, and a sentence stating REL-07's checkbox and Traceability row were not changed by this phase.
    - The `## Checklist` section contains seven numbered `###` items, and names `validate`, `build`, `publish-pypi` and `create-release` in that order, with an explicit instruction to observe `create-release` succeed and the v0.7.0 reason why.
    - The checklist names `typsphinx-doc-translations` and the Read the Docs `stable` measurement on both projects.
    - The `## Deferred by decision, not oversight` section names at least four distinct `.planning/todos/pending/` filenames verbatim, and `ls .planning/todos/pending/` confirms each named filename actually exists.
    - The same section contains a reachability sentence for each of the four defects and one paragraph naming D-01 and the v0.7.1 D-27 precedent, plus a paragraph naming D-03 and stating the records are not promoted to ROADMAP backlog items.
    - The `## Proof the fence held` section contains a `date -u` timestamp DIFFERENT from the one in `52-RELEASE-EVIDENCE.md`'s SC#5 section, the verbatim output of both tag commands, and the literal `observation 2`.
    - No `### Known Limitations` heading exists anywhere in `CHANGELOG.md`'s `## [0.8.0]` section, and `gh issue list --state open --limit 20` shows no issue filed by this phase.
    - No file named `52-VERIFICATION.md` exists in the phase directory.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>A standalone publish checklist exists covering all six owner-manual steps plus the re-date item, the four deferred defects and the `:numref:` record are each named with their real todo filenames and reachability conditions, and the fence's second observation is recorded at its own timestamp.</done>
</task>

<task type="auto">
  <name>Task 3: Guard REQUIREMENTS.md against the closeout auto-flip and confirm the phase's own fences</name>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md</files>
  <reversibility rating="reversible">A read-only diff check plus an appended handoff section; any revert it triggers restores a file to its committed state.</reversibility>
  <read_first>
    - `.planning/REQUIREMENTS.md` — REL-07's checkbox line and its Traceability row, recorded verbatim BEFORE anything else in this task runs, so a later diff has something to compare against.
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md` § "Pitfall 5" — the recorded, repeated `phase.complete` auto-flip hazard: caught in Phase 41, pre-empted in Phase 42 by `42-CLOSEOUT-GUARD.md`, and named in this project's own memory index as a release-prep-specific hazard.
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-HANDOFF.md` — its item 6, which documents the same revert-if-flipped procedure.
  </read_first>
  <action>
Record REL-07's two lines in `.planning/REQUIREMENTS.md` verbatim, with their line numbers, plus a
checksum of the whole file (`sha256sum .planning/REQUIREMENTS.md`), as an appended
`## Closeout guard — REL-07 must stay Pending` section of `52-HANDOFF.md`. Note that REL-07's checkbox
must read `- [ ]` and its Traceability row must read `Pending` at phase close, and that
`.planning/REQUIREMENTS.md` is expected to appear in NO diff produced by this phase.

Then run the phase's own closing fence checks and record them in the same section:
`git diff --name-only -- .planning/REQUIREMENTS.md` (expect empty),
`grep -n 'REL-07' .planning/REQUIREMENTS.md` (expect the checkbox still `- [ ]` and the row still
`Pending`), `git diff --name-only -- typsphinx/` (expect empty),
`ls .planning/phases/52-v0-8-0-release-prep-prep-only/` (expect no `52-VERIFICATION.md`), and
`git status --porcelain` at rest.

Write, in the same section, the instruction for whoever runs the phase close: **diff
`.planning/REQUIREMENTS.md` after any closeout automation runs and before committing the close.** If
REL-07's checkbox or Traceability row changed, revert that change by hand and re-apply the correct
still-`Pending` state, then record the revert. This has happened before on this exact class of phase and
must not be assumed not to recur.

Never assert a command's result from memory — run it and paste the transcript. Take no irreversible
action.
  </action>
  <verify>
    <automated>grep -q '^## Closeout guard' .planning/phases/52-v0-8-0-release-prep-prep-only/52-HANDOFF.md && grep -qE '^- \[ \] \*\*REL-07\*\*' .planning/REQUIREMENTS.md && grep -qE '^\| REL-07 \| Phase 52 \| Pending \|' .planning/REQUIREMENTS.md && test -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" && test -z "$(git diff --name-only -- typsphinx/)" && test ! -f .planning/phases/52-v0-8-0-release-prep-prep-only/52-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `52-HANDOFF.md` contains the heading `## Closeout guard — REL-07 must stay Pending`.
    - That section contains REL-07's checkbox line and its Traceability row quoted verbatim with their line numbers, and the `sha256sum` of `.planning/REQUIREMENTS.md`.
    - `grep -E '^- \[ \] \*\*REL-07\*\*' .planning/REQUIREMENTS.md` matches — the checkbox is unchecked.
    - `grep -E '^\| REL-07 \| Phase 52 \| Pending \|' .planning/REQUIREMENTS.md` matches — the Traceability row still reads `Pending`.
    - `git diff --name-only -- .planning/REQUIREMENTS.md` produces no output.
    - The section contains an explicit instruction to diff `.planning/REQUIREMENTS.md` after closeout automation runs and to revert any REL-07 flip, citing the Phase 41 catch and the Phase 42 pre-emption.
    - `ls .planning/phases/52-v0-8-0-release-prep-prep-only/` shows no file named `52-VERIFICATION.md`.
    - `git diff --name-only -- typsphinx/` produces no output.
    - `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` both produce no output.
  </acceptance_criteria>
  <done>REL-07's still-open state is pinned in the handoff with a file checksum and a revert instruction, and the phase's own fences — no `typsphinx/` change, no reserved filename, no requirements diff — are recorded as observed rather than assumed.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's artifacts → `/gsd-complete-milestone` | The handoff is the only document the publish command reads for this milestone's owner-manual steps; anything omitted here is lost. |
| `.planning/todos/pending/` → the four deferred defects' only record | D-01 and D-03 remove every other surface — no CHANGELOG section, no GitHub issue, no backlog item — so the handoff and the ledger are the complete record. |
| closeout automation → `.planning/REQUIREMENTS.md` | `phase.complete` has a recorded, repeated habit of auto-flipping REL rows against a CONTEXT decision. |
| the working tree → `origin` / PyPI (the prep/publish fence) | Everything this plan does must stay on the reversible side of that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-27 | Repudiation | REL-07 reported complete on the strength of the prep being correct | high | mitigate | The handoff must contain the literal `REL-07 remains open`; Task 3 pins the checkbox and Traceability row with a file checksum, asserts both still read unchecked/`Pending`, and records a revert instruction for the closeout auto-flip. This is the failure that lost REL-04 at the v0.7.0 close. |
| T-52-28 | Repudiation | a deferred defect silently losing its only record | high | mitigate | Acceptance asserts at least four distinct `todos/pending/` filenames are named verbatim AND that each named filename actually exists on disk, so a mis-remembered filename cannot pass as a citation. |
| T-52-29 | Repudiation | a roll-up that restates sibling figures as though re-measured | medium | mitigate | The action forbids restating without attribution; acceptance requires each SC#3 subsection to name its source artifact filename, and the roll-up to name all five evidence artifacts. |
| T-52-30 | Tampering | source changes landing after the CI authority run, leaving them unproven | high | mitigate | Task 1 asserts `git diff --name-only <authority-sha>..HEAD -- . ':(exclude).planning'` is empty and requires the SC#3 verdict to fall short if it is not. |
| T-52-31 | Tampering | an evidence artifact clobbered by the verifier's reserved filename | medium | mitigate | Every task asserts no `52-VERIFICATION.md` exists in the phase directory; the whole evidence family is named after Phase 46's convention instead. |
| T-52-03 | Tampering | the prep/publish fence | high | mitigate | Two independent timestamped observations (roll-up and handoff) plus a third in Task 3; PR absence and `release.yml` run absence recorded alongside. |
| T-52-SC | Tampering | npm/pip/cargo installs | medium | accept | No package is installed or added by this plan. `52-RESEARCH.md` records the Package Legitimacy Gate as **not applicable** — zero candidate package names, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. |
</threat_model>

<verification>
- `52-RELEASE-EVIDENCE.md` carries one verdict per success criterion and names all five sibling evidence artifacts.
- `git diff --name-only <authority-sha>..HEAD -- . ':(exclude).planning'` is empty.
- `52-HANDOFF.md` contains the literal `REL-07 remains open`, seven checklist items, and at least four verbatim `todos/pending/` filenames that all exist on disk.
- The fence is observed twice more, at two distinct `date -u` timestamps in two separate files.
- REL-07's checkbox is `- [ ]` and its Traceability row reads `Pending`; `git diff --name-only -- .planning/REQUIREMENTS.md` is empty.
- No `52-VERIFICATION.md` exists; `git diff --name-only -- typsphinx/` is empty.
</verification>

<success_criteria>
- A standalone publish checklist exists covering merge → tag → `release.yml` (with an explicit `create-release` observation) → PyPI + GitHub Release → the second tag on `typsphinx-doc-translations` → the Read the Docs `stable` measurement on both projects (SC#5).
- The phase's artifacts state that REL-07 remains open until the publish and do not report it complete (SC#5, lesson 12b).
- `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` are both empty at phase end, recorded on two independent timestamped observations (SC#5).
- The four deferred defects and the `:numref:` record are each named with their real todo filenames, per D-01 and D-03.
- SC#1 through SC#4 each carry one roll-up verdict citing their source evidence.
</success_criteria>

<output>
Create `.planning/phases/52-v0-8-0-release-prep-prep-only/52-07-SUMMARY.md` when done.
</output>
