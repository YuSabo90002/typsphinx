---
phase: 52-v0-8-0-release-prep-prep-only
plan: 06
type: execute
wave: 3
depends_on: ["52-02", "52-03"]
files_modified:
  - .planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md
autonomous: true
requirements: [REL-07]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The three standing milestone invariants are asserted MECHANICALLY over the SHA-anchored full milestone diff, excluding `.planning/`: zero new runtime dependencies, the `@preview` package count still four with no new version-lockstep site, and no new `typst_*` config value (ROADMAP SC#4)."
    - "The sweep is anchored at the `v0.7.1` tag, peeled to its commit — `git rev-parse v0.7.1^{commit}`, because a bare `git rev-parse v0.7.1` returns the annotated tag OBJECT sha and not the commit. ROADMAP SC#4 says merge-base-to-HEAD and D-09 says the release tag; the two coincide this milestone because `origin/main` is both an ancestor of HEAD and the merge-base, and the four commits between the tag and `origin/main` are planning and docs only — so no choice is being made, and the coincidence is re-verified live rather than assumed (D-09)."
    - "Each of the three invariants carries its OWN positive control — a live command demonstrating that the detector fires on a real violation — not a restatement of the invariant. This is new work for this phase: Phase 46's `46-SC4-INVARIANTS.md` recorded figures and reasoning but never attempted an independent liveness proof of its detectors (RESEARCH A3), so the precedent is deliberately not copied forward uncritically."
    - "The dependency invariant is asserted as a byte-identity of the `[project]` `dependencies` array between the two anchors, not merely as an empty file diff, so an empty result cannot be produced by a mis-typed pathspec."
    - "The `typst_*` invariant is asserted as a SET COMPARISON of the config names registered at each anchor — the sorted set extracted from `typsphinx/__init__.py` at `v0.7.1` diffed against the same set at HEAD — rather than inferred from an empty `grep` over a diff, so a pattern that matches nothing on any input cannot masquerade as a clean result."
    - "The `@preview` invariant is asserted twice over: the version strings are identical across all four sync surfaces at HEAD, and the SET of files declaring a `@preview` import has not grown between the two anchors — the second half is what 'no new lockstep site' actually means."
    - "No tracked file is mutated to produce a control; every mutation control operates on a scratch copy under a temporary directory and the tracked tree is asserted clean afterwards."
    - "Nothing under `typsphinx/` changed in this plan (D-01, prep-only fence)."
  artifacts:
    - path: ".planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md"
      provides: "SC#4's mechanical sweep with a per-invariant positive control and a roll-up verdict"
      contains: "Positive control"
  key_links:
    - from: "the `v0.7.1` tag, peeled to its commit"
      to: "HEAD"
      via: "`git diff v0.7.1..HEAD -- . ':(exclude).planning'` — the same range ROADMAP SC#4 names as merge-base-to-HEAD, which coincides with the tag this milestone because origin/main is both an ancestor and the merge-base"
      pattern: "exclude\\)\\.planning"
    - from: "the four `@preview` declaration sites (`writer.py`, `template_engine.py`, `templates/base.typ`, `examples/**/*.typ`)"
      to: "tests/test_preview_version_sync.py"
      via: "the lockstep guard that fails when the four surfaces disagree — the mechanical detector whose liveness this plan proves rather than assumes"
      pattern: "@preview"
  prohibitions:
    - statement: "No git tag named v0.8.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged; a PR would also fire CI a second, unintended way and is the publish half's first step."
      status: unverified
      verification: flagged
    - statement: "No second tag is created on the typsphinx-doc-translations repository and its pin is not advanced."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed — the four minor defects this milestone's own reviews filed ship unfixed per D-01."
      status: unverified
      verification: flagged
    - statement: "No tracked file is mutated to demonstrate a positive control; every mutation control runs against a scratch copy and the tracked tree is asserted clean afterwards."
      status: unverified
      verification: flagged
    - statement: "No positive control is a restatement of the invariant it is supposed to control; each must be a live command whose output is NON-EMPTY or whose exit status is a failure, demonstrating the detector fires."
      status: unverified
      verification: flagged
    - statement: "REL-07's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped; the requirement stays Pending until the publish."
      status: unverified
      verification: flagged
    - statement: "No figure is transcribed from the phase's CONTEXT or RESEARCH document into an acceptance criterion or an evidence file; every measured value is re-run live at execution time."
      status: unverified
      verification: flagged
---

<objective>
Assert the three standing milestone invariants mechanically over the SHA-anchored milestone diff, and —
the part with no precedent to copy — give each one a real positive control that would fail if the sweep
were vacuous.

Purpose: discharges ROADMAP Phase 52 SC#4. The invariants themselves are cheap: two of the three are
one-command proofs. The genuinely new work is the control. ROADMAP SC#4 demands a control, and
`46-SC4-INVARIANTS.md` — the only precedent — never attempted one: it recorded figures and reasoning but
no independent demonstration that its detectors could fire at all. A detector that returns "clean" is
indistinguishable from a detector pointed at the wrong ref, the wrong path, or matching a pattern that
occurs nowhere. This plan closes that gap by proving each detector's sensitivity on a real input.

Output: `52-SC4-INVARIANTS.md` with the anchor, the swept scale, three invariant verdicts, three named
positive controls, and a roll-up verdict.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-CONTEXT.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md
@.planning/phases/52-v0-8-0-release-prep-prep-only/52-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

The phase-level list lives in `52-01-PLAN.md` § "Artifacts this phase produces". This plan produces one
planning artifact, `.planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md`, and changes
no source file.

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the anchor and assert the three invariants mechanically</name>
  <precondition>Plans 52-01, 52-02 and 52-03 have merged back, so the swept range covers this phase's own source changes as well as the milestone's. The `v0.7.1` tag must resolve locally; if it does not, fetch tags before starting rather than substituting a different anchor.</precondition>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md</files>
  <reversibility rating="reversible">Read-only git and grep commands plus one planning artifact.</reversibility>
  <read_first>
    - `CLAUDE.md` § "The `@preview` version-sync hazard" — the exact surfaces (`writer.py`, `template_engine.py`, `templates/base.typ`) the invariant counts, and the fact that `tests/test_preview_version_sync.py` asserts all three agree.
    - `tests/test_preview_version_sync.py` — its three test functions and the fourth surface (`examples/**/*.typ`) it added at the v0.6.3 close.
    - `typsphinx/__init__.py` — every `add_config_value(...)` registration, so the extracted name set can be checked against what the file actually contains.
    - `pyproject.toml` — the `[project] dependencies` array and the `[project.optional-dependencies]` extras, so "runtime dependency" is measured against the right array and not against the `dev`/`docs` extras.
    - `.planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-SC4-INVARIANTS.md` — the section-heading shape to adapt. Note its Invariant 3 was a phase-scope fence specific to Phase 46 and must NOT be copied; this milestone's Invariant 3 is the `typst_*` config-value invariant (D-09).
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md` § "Pattern 5" and its Assumptions Log row A3.
  </read_first>
  <action>
Provision the worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

**Step 1 — re-verify the anchor, do not assume it.** Run and record verbatim:
`git rev-parse v0.7.1^{commit}` (the `^{commit}` peel is required — a bare `git rev-parse v0.7.1` on
this annotated tag returns the TAG OBJECT sha, which is not a commit and will silently mis-anchor the
sweep); `git rev-parse origin/main`; `git merge-base origin/main HEAD`;
`git merge-base --is-ancestor v0.7.1 HEAD && echo tag-is-ancestor`; and
`git merge-base --is-ancestor origin/main HEAD && echo main-is-ancestor`. Then show the anchor
coincidence D-09 rests on by running the shortstat at BOTH anchors —
`git diff v0.7.1..HEAD --stat -- . ':(exclude).planning' | tail -1` and
`git diff origin/main..HEAD --stat -- . ':(exclude).planning' | tail -1` — and comparing them. If they
differ, the four intervening commits are no longer planning/docs-only; record that difference plainly,
anchor at the merge-base as ROADMAP SC#4 literally says, and say why. Do NOT transcribe the figures in
`52-CONTEXT.md` or `52-RESEARCH.md`; both were measured on earlier trees.

**Step 2 — record the swept scale, which doubles as the sweep's own liveness proof.** Record
`git rev-list --count v0.7.1..HEAD` and the `.planning`-excluded shortstat. A non-zero commit count and
a non-zero file/insertion/deletion triple is what distinguishes "this range is genuinely clean for these
three invariants" from "this range is empty and every detector returned nothing for that reason". State
that role explicitly.

**Step 3 — Invariant 1, zero new runtime dependencies.** Assert it as a byte identity of the array
itself, not as an empty file diff: extract the `[project] dependencies` block from both anchors with
`git show v0.7.1:pyproject.toml` and `git show HEAD:pyproject.toml`, isolate each with the same
`sed -n '/^dependencies = \[/,/^\]/p'` range, and `diff` the two — expect no output. Then ALSO record
the whole-file diff `git diff v0.7.1..HEAD -- pyproject.toml` and note what it shows: at plan time it
was empty except for this phase's own version-literal bump, which is expected and is not a dependency
change. Read the extras separately and state plainly that `dev` / `docs` are not runtime dependencies,
so a change there would not violate this invariant.

**Step 4 — Invariant 2, the `@preview` count and the lockstep-site set.** Two assertions, not one.
(a) Count: `grep -c "@preview" typsphinx/templates/base.typ` and the equivalent counts on
`typsphinx/writer.py` and `typsphinx/template_engine.py`; record each. (b) Version identity across all
four surfaces: extract the four package/version pairs from each surface and show the four sets agree,
then run `uv run pytest tests/test_preview_version_sync.py -v` and paste the transcript. (c) The
lockstep-SITE set — this is what "no new version-lockstep site" actually means, and a count alone does
not prove it: enumerate every tracked file containing a `@preview/` import at BOTH anchors
(`git grep -l '@preview/' v0.7.1 -- . ':(exclude).planning'` and the same against `HEAD`), sort both
lists, and `diff` them. Expect no output. If the set grew, name the new file and stop — a new lockstep
site is a real SC#4 violation and must not be waved through.

**Step 5 — Invariant 3, no new `typst_*` config value.** Assert it as a SET COMPARISON, not as an empty
grep over a diff. Extract the sorted set of registered config names from `typsphinx/__init__.py` at each
anchor — `git show v0.7.1:typsphinx/__init__.py` and `git show HEAD:typsphinx/__init__.py`, each piped
through a `grep -o` that captures the quoted first argument of every `add_config_value(` call, then
`sort` — and `diff` the two sets. Expect no output. Record both sets in full in the evidence file, so a
later reader can see WHAT the invariant is over rather than only that it held. Then also record the
diff-grep form `git diff v0.7.1..HEAD -- typsphinx/__init__.py | grep add_config_value` for continuity
with prior phases, and state that the set comparison, not the grep, is the authoritative form here.

Begin writing `.planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md` in English with
`# Phase 52 — SC#4 Milestone-Invariant Sweep`, `## Anchor`, `## Scale of the swept diff`,
`## Invariant 1 — zero new runtime dependencies` (with a `### Verdict — Invariant 1`),
`## Invariant 2 — the @preview count is still four, no new lockstep site` (with
`### The version identity check`, `### The lockstep-site set`, `### Verdict — Invariant 2`), and
`## Invariant 3 — no new typst_* config value` (with `### The registered-name sets`,
`### Verdict — Invariant 3`). Task 2 appends the controls and the roll-up.

Every command and its output is transcribed verbatim. Take no irreversible action.
  </action>
  <verify>
    <automated>diff <(git show v0.7.1:pyproject.toml | sed -n '/^dependencies = \[/,/^\]/p') <(git show HEAD:pyproject.toml | sed -n '/^dependencies = \[/,/^\]/p') && diff <(git grep -l '@preview/' v0.7.1 -- . ':(exclude).planning' | sed 's/^[^:]*://' | sort) <(git grep -l '@preview/' HEAD -- . ':(exclude).planning' | sed 's/^[^:]*://' | sort) && diff <(git show v0.7.1:typsphinx/__init__.py | grep -o 'add_config_value(\s*"[A-Za-z0-9_]*"' | grep -o '"[A-Za-z0-9_]*"' | sort) <(git show HEAD:typsphinx/__init__.py | grep -o 'add_config_value(\s*"[A-Za-z0-9_]*"' | grep -o '"[A-Za-z0-9_]*"' | sort) && test "$(git rev-list --count v0.7.1..HEAD)" -gt 0</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse v0.7.1^{commit}` output appears in the evidence file, and it is a 40-character commit sha; the evidence file states explicitly why the `^{commit}` peel is required.
    - Both `git merge-base --is-ancestor` checks are recorded as passing, and both anchors' `.planning`-excluded shortstats appear side by side with an explicit statement of whether they agree.
    - `git rev-list --count v0.7.1..HEAD` is recorded and is greater than 0, and the evidence file states in words that this non-empty range is what makes the three clean verdicts meaningful rather than vacuous.
    - The `[project] dependencies` array extracted from both anchors diffs empty, and both extractions appear in the evidence file.
    - The evidence file states that `dev` / `docs` extras are not runtime dependencies and therefore out of Invariant 1's scope.
    - `grep -c "@preview" typsphinx/templates/base.typ` returns `4`, and that figure appears in the evidence file alongside the counts for `writer.py` and `template_engine.py`.
    - `uv run pytest tests/test_preview_version_sync.py -v` reports all three test functions PASSED with zero skips, transcript pasted verbatim.
    - The sorted `@preview/`-declaring file lists at both anchors diff empty, and BOTH lists appear in full in the evidence file.
    - The sorted `add_config_value` name sets at both anchors diff empty, and BOTH sets appear in full in the evidence file.
    - `52-SC4-INVARIANTS.md` contains the headings `## Anchor`, `## Scale of the swept diff`, `## Invariant 1 — zero new runtime dependencies`, `## Invariant 2 — the @preview count is still four, no new lockstep site`, and `## Invariant 3 — no new typst_* config value`, each with its own `### Verdict — Invariant N` subsection.
    - The figures `155` and `157` do NOT appear as this phase's own measurement anywhere in the file.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>All three milestone invariants are asserted over a re-verified, non-empty, SHA-anchored range, each in its strongest available form — array byte-identity, file-set identity, and registered-name-set identity — with every command and output transcribed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove each detector fires — three real positive controls — and write the roll-up verdict</name>
  <files>.planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md</files>
  <reversibility rating="reversible">Read-only history queries plus scratch-copy mutations under a temporary directory; the tracked tree is asserted clean afterwards.</reversibility>
  <read_first>
    - `.planning/phases/52-v0-8-0-release-prep-prep-only/52-RESEARCH.md` § "Pattern 5" — the three control candidates and the explicit note that Phase 46 attempted none of them, so this is new work rather than a repeat.
    - `.planning/STATE.md` § "Roadmap Evolution" (the v0.6.3 close entry) — the historical `@preview` drift incident: the bundled `examples/advanced` sample's `custom.typ` was three milestones behind on its pins and failed with `unknown variable: kai`, which is when `test_preview_version_sync.py` was extended over `examples/**/*.typ`. This is corroborating history, not the control itself.
    - `.planning/ROADMAP.md` § "Phase 52" success criterion 4 — the exact phrase "with a positive control", which is what this task must satisfy.
  </read_first>
  <action>
A positive control is NOT a restatement of the invariant. It is a live command demonstrating that the
detector used in Task 1 would report a violation if one existed. Each of the three below must produce a
NON-EMPTY output or a FAILING exit status; a control that comes back clean has proved nothing and must
be rebuilt until it fires.

**Control 1 — the dependency detector fires on a real dependency change.** The risk this closes: an
empty `dependencies`-array diff is indistinguishable from a mis-typed pathspec or a `sed` range that
matched nothing. Find a historical commit where `pyproject.toml`'s `dependencies` array genuinely moved
— `git log --oneline -S'dependencies = [' -- pyproject.toml` and `git log --oneline -- pyproject.toml`
will both surface candidates; a commit that added a runtime dependency is ideal. Run the IDENTICAL
extraction-and-diff command shape from Task 1 step 3 across that commit and its parent, and show the
diff is NON-EMPTY. Paste it. Also record `git show HEAD:pyproject.toml | sed -n '/^dependencies = \[/,/^\]/p' | wc -l`
as greater than 2, proving the `sed` range matches a real, non-empty block at HEAD rather than nothing.

**Control 2 — the `@preview` detector fires on a real version drift.** The risk this closes: a
cross-surface identity check that compares two empty sets always agrees. Copy the four sync surfaces to
a scratch directory under `"${TMPDIR:-/tmp}"` — never mutate the tracked files — change ONE `@preview`
version string in ONE copy, and run the same cross-surface identity comparison Task 1 step 4(b) used
against the scratch copies. Show that it reports a mismatch and names the surface that drifted. Then
confirm the tracked tree is untouched with `git status --porcelain` (empty) and re-run
`uv run pytest tests/test_preview_version_sync.py -q` green. Alongside the live control, cite — as
corroborating history, explicitly labelled as such rather than as the control — the v0.6.3 close's real
catch recorded in `STATE.md`, when the bundled example's pins were three milestones behind and the
sample failed with `unknown variable: kai`.

**Control 3 — the `add_config_value` detector fires on a real config addition.** The risk this closes:
a pattern that matches nothing on ANY input returns empty on this range too, and looks identical to a
clean result. Two parts. (a) Prove the token exists in the file at all right now:
`grep -c 'add_config_value' typsphinx/__init__.py` must be greater than 0 — if it were 0, the whole
invariant would be vacuous. (b) Find a historical range where a `typst_*` config value was genuinely
added — `git log --oneline -S'add_config_value' -- typsphinx/__init__.py` surfaces them; the CONF-04
`typst_elements` era is one known candidate — and run the IDENTICAL set-extraction-and-diff shape from
Task 1 step 5 across that commit and its parent. Show the two sets differ and name the added config
value. Paste the output.

Then append to `52-SC4-INVARIANTS.md`:

- `## Positive control`, with one subsection per control — `### Control 1 — the dependency detector`,
  `### Control 2 — the @preview detector`, `### Control 3 — the config-value detector` — each holding
  its commands, its NON-EMPTY or FAILING output verbatim, and one sentence naming exactly which vacuity
  mode it closes;
- a short paragraph inside `## Positive control` recording, honestly, that this is NEW work for this
  phase: `46-SC4-INVARIANTS.md` recorded figures and reasoning but no detector-liveness proof, so the
  precedent was deliberately not copied forward;
- `## Roll-up verdict`, stating each invariant's verdict, each control's verdict, and a single
  overall SC#4 verdict — MET or NOT MET. If any control failed to fire, SC#4 is NOT MET and this must
  say so; do not soften a failed control into a caveat.

Never assert a command's result from memory — run it and paste the transcript. Take no irreversible
action.
  </action>
  <verify>
    <automated>test "$(grep -c 'add_config_value' typsphinx/__init__.py)" -gt 0 && test "$(git show HEAD:pyproject.toml | sed -n '/^dependencies = \[/,/^\]/p' | wc -l)" -gt 2 && grep -q '^## Positive control' .planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md && grep -q '^### Control 1' .planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md && grep -q '^### Control 2' .planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md && grep -q '^### Control 3' .planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md && grep -q '^## Roll-up verdict' .planning/phases/52-v0-8-0-release-prep-prep-only/52-SC4-INVARIANTS.md && test -z "$(git status --porcelain -- typsphinx/ pyproject.toml)"</automated>
  </verify>
  <acceptance_criteria>
    - `52-SC4-INVARIANTS.md` contains the headings `## Positive control`, `### Control 1 — the dependency detector`, `### Control 2 — the @preview detector`, `### Control 3 — the config-value detector`, and `## Roll-up verdict`.
    - Each of the three control subsections contains at least one command whose recorded output is NON-EMPTY (Controls 1 and 3) or whose recorded result is an explicit mismatch/failure (Control 2) — no control subsection records only a clean result.
    - Control 1 names a specific historical commit sha and shows a non-empty `dependencies`-array diff across it, and records the HEAD `dependencies` block line count as greater than 2.
    - Control 2 records that the mutation was performed on a scratch copy under a temporary directory, shows the identity comparison reporting a mismatch and naming the drifted surface, and follows it with an empty `git status --porcelain` and a green re-run of `tests/test_preview_version_sync.py`.
    - Control 3 records `grep -c 'add_config_value' typsphinx/__init__.py` as greater than 0, names a specific historical commit sha, and shows the two extracted name sets differing with the added config value named.
    - Each control subsection contains one sentence naming the specific vacuity mode it closes (wrong pathspec / empty-set comparison / pattern that matches nothing).
    - The `## Positive control` section states explicitly that no prior phase's evidence attempted a detector-liveness proof and that this is new work rather than a repeated pattern.
    - The `## Roll-up verdict` section gives a verdict for each of the three invariants, a verdict for each of the three controls, and a single overall SC#4 verdict.
    - `git status --porcelain -- typsphinx/ pyproject.toml tests/` produces no output — no tracked file was mutated to produce a control.
    - `git tag -l v0.8.0` and `git ls-remote --tags origin v0.8.0` both produce no output.
  </acceptance_criteria>
  <done>Each of SC#4's three detectors has been shown to fire on a real violation, on live evidence rather than by assertion, with the tracked tree provably unmutated — and the roll-up states one unambiguous SC#4 verdict.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a detector's "clean" result → the milestone's invariant claim | A detector pointed at the wrong ref, the wrong path, or matching a pattern that occurs nowhere returns exactly what a genuinely clean tree returns. This is the boundary the positive controls police. |
| the milestone diff → the published `### Verified` section | Two of the three `### Verified` items in the `## [0.8.0]` CHANGELOG entry (plan 52-02) are the claims this sweep proves; a vacuous sweep publishes an unfounded claim. |
| the working tree → `origin` / PyPI (the prep/publish fence) | Everything this plan does must stay on the reversible side of that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-23 | Repudiation | a vacuous invariant sweep cited as proof | high | mitigate | Each invariant carries its own positive control whose output must be NON-EMPTY or FAILING; acceptance asserts no control subsection records only a clean result, and the roll-up must declare SC#4 NOT MET if any control fails to fire. |
| T-52-24 | Repudiation | a mis-anchored sweep (annotated-tag object sha instead of the commit) | high | mitigate | The action requires the `^{commit}` peel and explains why; acceptance asserts the recorded anchor is a 40-character commit sha and that both ancestry checks passed, with both anchors' shortstats compared side by side. |
| T-52-25 | Tampering | a tracked file mutated to demonstrate a control and left behind | high | mitigate | Control 2's mutation is confined to a scratch copy under a temporary directory; acceptance asserts `git status --porcelain -- typsphinx/ pyproject.toml tests/` is empty and that the sync guard re-runs green afterwards. |
| T-52-26 | Tampering | a new `@preview` lockstep site added without the guard noticing | high | mitigate | Invariant 2 asserts the SET of `@preview/`-declaring files, not merely a count on one file; both sets are recorded in full and the action requires stopping if the set grew. |
| T-52-03 | Tampering | the prep/publish fence | high | mitigate | Tag absence re-asserted in Task 2's acceptance; no push, dispatch, tag, upload, release or PR occurs in this plan. |
| T-52-SC | Tampering | npm/pip/cargo installs | medium | accept | No package is installed or added by this plan. `52-RESEARCH.md` records the Package Legitimacy Gate as **not applicable** — zero candidate package names, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. Invariant 1 is itself the check that would catch an unexpected new dependency. |
</threat_model>

<verification>
- The anchor is re-verified with the `^{commit}` peel and both ancestry checks pass.
- `git rev-list --count v0.7.1..HEAD` is greater than 0 and the `.planning`-excluded shortstat is non-zero.
- The `dependencies` array, the `@preview/`-declaring file set, and the `add_config_value` name set all diff empty between the two anchors.
- `grep -c "@preview" typsphinx/templates/base.typ` returns `4` and `tests/test_preview_version_sync.py` passes with zero skips.
- All three positive controls produce non-empty or failing output, each with its vacuity mode named.
- `git status --porcelain -- typsphinx/ pyproject.toml tests/` produces no output.
</verification>

<success_criteria>
- The three standing invariants are asserted mechanically over the SHA-anchored milestone diff, excluding `.planning/` (SC#4, D-09).
- Each invariant carries a real positive control that fires, not a restatement (SC#4).
- The evidence records honestly that the control is new work with no precedent to copy (RESEARCH A3).
- One unambiguous overall SC#4 verdict is stated.
</success_criteria>

<output>
Create `.planning/phases/52-v0-8-0-release-prep-prep-only/52-06-SUMMARY.md` when done.
</output>
