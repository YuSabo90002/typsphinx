---
phase: 23-v0-6-2-release-prep-regression-gate-close
plan: 03
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - FID-02
  - FID-03
  - FID-04
  - FID-05
  - FID-06
  - FID-07
  - FID-08
  - FID-09
  - FID-10
  - FID-11
  - FID-12
  - FID-13
  - FID-14
  - PDF-01
  - PDF-02
  - CONF-01
  - CONF-02
  - CONF-03
  - WR-01
  - WR-02
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
must_haves:
  truths:
    - "CHANGELOG.md carries a `## [0.6.2] - YYYY-MM-DD` heading dated with the actual execution date, inserted between the `## [Unreleased]` heading and the `## [0.6.1]` heading (SC#2, D-15)"
    - "Every one of the 25 v0.6.2 ledger IDs (FID-02..FID-14, PDF-01, PDF-02, CONF-01..CONF-03, WR-01, WR-02, DOC-01..DOC-05) is covered by some bullet's trailing requirement-ID citation, with zero silent drops (SC#2, D-01)"
    - "Fidelity fixes are bundled by root-cause cluster rather than enumerated one-per-requirement: 10-12 change bullets total across the entry (SC#2, D-01)"
    - "A `### Removed` section exists carrying a single bullet-level bold BREAKING item for the `typst_output_dir` and `typst_author_params` deletions (SC#2, D-05)"
    - "Issue #117 appears as a bold-led bullet in `### Fixed` showing the concrete before/after filename change (`index.pdf` becomes `mydoc.pdf` for a target named `mydoc`), and is NOT labelled BREAKING (SC#2, D-06, D-07)"
    - "The Phase 22.4 README/CLAUDE.md accuracy corrections appear as one bullet inside `### Fixed`, with no separate documentation subsection created (SC#2, D-03, D-04)"
    - "A `### Verified` section records the corpus-gate outcome and the SC#4 milestone invariant, stating only facts the gate actually asserts (SC#2, D-02, D-11)"
    - "The entry contains no commentary about the project's version-numbering policy and proposes no alternative version number (D-08)"
    - "The entry claims no document-length figure, because no test asserts one (D-11)"
    - "No `[0.6.2]:` link-reference line is added to the bottom-of-file block and the `[Unreleased]:` compare link is left pointing at v0.6.1 — the tag does not exist yet (SC#5)"
    - statement: "The entry reads as a description of what visibly changed for a user who has never seen a FID number: each bullet leads with the observable symptom, with the requirement IDs relegated to a trailing citation (D-01)"
      verification: backstop
    - statement: "The BREAKING bullet states the breaking fact plainly without diluting the signal, while still being honest that a conf.py retaining either removed value is silently ignored rather than erroring (D-05)"
      verification: backstop
  artifacts:
    - "CHANGELOG.md — new `## [0.6.2] - YYYY-MM-DD` section containing `### Removed`, `### Fixed`, and `### Verified` subsections"
    - "23-03-SUMMARY.md — a 25-row requirement-ID to CHANGELOG-bullet coverage table proving zero drops"
  key_links:
    - "CHANGELOG.md `## [0.6.2]` entry ↔ the eventual GitHub Release body — ROADMAP SC#2 makes this entry the single source, so anything omitted here is omitted from the release announcement"
    - "`### Verified` section ↔ 23-VERIFICATION.md gate evidence (produced in wave 2) — the section may only state facts that record actually shows"
    - ".planning/REQUIREMENTS.md 25-item ledger ↔ the trailing ID citations in each bullet — the traceability chain D-01 requires"
  prohibitions:
    - statement: "No git tag named v0.6.2 (or any other tag) is created or pushed by this plan"
      status: required
      verification: "`git tag --list 'v0.6.2'` returns empty"
    - statement: "No PyPI upload, no GitHub Release creation, no manual or scripted trigger of .github/workflows/release.yml"
      status: required
      verification: "`git diff v0.6.1..HEAD -- .github/workflows/release.yml` is empty; no `gh release`, `twine`, `uv publish`, or `git push --tags` command is run"
    - statement: "No `[0.6.2]:` link-reference line is added to the bottom-of-file link block, and the `[Unreleased]:` compare link is not advanced — both are tag-dependent and belong to /gsd-complete-milestone"
      status: required
      verification: "`grep -c '^\\[0.6.2\\]:' CHANGELOG.md` returns 0 and `grep -c 'compare/v0.6.1...HEAD' CHANGELOG.md` returns 1"
    - statement: "No historical CHANGELOG entry ([0.6.1] and older) is edited"
      status: required
      verification: "`git diff -- CHANGELOG.md` shows only added lines above the `## [0.6.1]` heading and no removed lines"
    - statement: "No behavior change under typsphinx/ and no test file is modified — this plan edits prose only"
      status: required
      verification: "`git diff --name-only` for this plan's commits lists CHANGELOG.md only"
---

<objective>
Author the curated `## [0.6.2]` CHANGELOG entry — the single source for the eventual GitHub Release body —
covering the complete 25-item v0.6.2 requirement ledger with zero silent drops, bundled by root-cause
cluster so a reader who has never seen a FID number can tell what visibly changed.

Purpose: satisfies ROADMAP SC#2 and the D-01 coverage obligation. This is wave 3 because its `### Verified`
section may only state facts the wave-2 corpus gate actually produced (D-11's "no unverifiable claims"
principle, inherited from Phase 22.4).

Output: a new `## [0.6.2] - <execution date>` section in `CHANGELOG.md` with `### Removed`, `### Fixed`, and
`### Verified` subsections, plus a 25-row coverage audit table in the SUMMARY.

`requirements:` lists all 25 v0.6.2 ledger IDs. Phase 23 has no requirement IDs mapped to it in the ROADMAP;
these are listed here because D-01 makes the entire ledger this entry's de-facto coverage surface, and the
audit in Task 2 mechanically proves the coverage.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-CONTEXT.md
@.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-RESEARCH.md
@.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-PATTERNS.md
@.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-VERIFICATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 23-03 share)

| Artifact | Kind | Contents |
|---|---|---|
| `CHANGELOG.md` | modified | new `## [0.6.2] - YYYY-MM-DD` section with `### Removed`, `### Fixed`, `### Verified` |
| `23-03-SUMMARY.md` | new file | 25-row requirement-ID to bullet coverage table |

No new source symbols are defined by this plan — it edits prose only.

Produced by sibling plans: `tests/test_readme_version_sync.py` and the version-literal bumps (23-01),
`23-VERIFICATION.md` gate evidence (23-02).

<assumption_delta_decision>
noun: n/a · decision: no-change · rationale: detector fired on the prose word "also" in a CHANGELOG
section list; no core assumption changes in a release-prep phase.
</assumption_delta_decision>

<specless_probe_note>
spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). `must_haves.truths`
are derived directly from ROADMAP SC#2/SC#5 and CONTEXT.md decisions D-01 through D-08, D-11, and D-15.
</specless_probe_note>

<tasks>

<task type="auto">
  <name>Task 1: Author the `## [0.6.2]` CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1–120 (the file header declaring Keep a Changelog, the `## [Unreleased]` heading
      at line 8, the blank line 9, and the whole `## [0.6.1] - 2026-07-20` entry at line 10 onward — the
      direct template for lead-paragraph length and tone, `###` subsection style, and the
      `- **Short label (ID range)** — description` bullet shape)
    - `CHANGELOG.md` lines 640–665 (the bottom-of-file link-reference block — read it to know exactly what
      NOT to touch)
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-RESEARCH.md` § "Code Examples" §
      "Recommended `[0.6.2]` CHANGELOG entry outline" — this is a near-final draft and is the starting text
      for this task, not a re-derivation target. Also read § "Requirement-Ledger → Cluster Mapping" (the
      authoritative 23-ID to 11-bullet mapping), § "CHANGELOG Structural Template", and § "Common Pitfalls"
      Pitfalls 3 and 4
    - `.planning/REQUIREMENTS.md` (the 25-item ledger and its cluster headings)
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-VERIFICATION.md` (wave 2's gate
      evidence — the factual basis for the `### Verified` section)
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-CONTEXT.md` §§ decisions D-01
      through D-08, D-11, D-15, and the `<specifics>` section on the deliberate D-05/D-07 asymmetry
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-PATTERNS.md` § `CHANGELOG.md`
  </read_first>
  <action>
    Provision the worktree first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then
    run everything via `uv run`.

    Determine the execution date with `date +%Y-%m-%d` and use that literal value in the heading. Per D-15
    the entry is dated with the real prep date, not left as an unreleased placeholder — the v0.6.1 precedent
    already tolerates a one-day gap between the entry date and the eventual tag date.

    Insert the new entry into `CHANGELOG.md` between line 9 (the blank line following `## [Unreleased]` at
    line 8) and line 10 (`## [0.6.1] - 2026-07-20`). Keep the empty `## [Unreleased]` heading above it —
    standard Keep a Changelog practice, and Claude's Discretion in CONTEXT.md already assumes it stays.

    Structure, using RESEARCH.md's § "Code Examples" draft as the starting text:

    - `## [0.6.2] - <execution date>` heading at level 2; subsections at level 3.
    - A 4-5 line present-tense lead paragraph in the shape `[0.6.1]`'s lead uses: state the release's overall
      theme (closing the remaining 13 fidelity findings across six root-cause clusters, the typstpdf
      output-filename fix, the nested-master compile-root fix, the `typst_package` template-path repair,
      builder hardening against silent partial success, two long-dead config removals, and the
      README/CLAUDE.md accuracy corrections), and end with the standing sentence that this release adds zero
      new runtime dependencies and leaves the bundled `@preview` version-sync surface untouched.
    - Then, in this order: `### Removed`, `### Fixed`, `### Verified`.

    The subsection order deliberately differs from RESEARCH.md's draft, which put `### Fixed` first. Two
    reasons, both within the formatting latitude CONTEXT.md grants: Keep a Changelog — which this file's own
    header declares it follows — orders Removed before Fixed; and D-05's BREAKING item must not be buried
    beneath eleven bullets of fixes. Create neither an Added nor a Changed subsection: every item in this
    release is a fix or a removal, and per D-04 the README/CLAUDE.md corrections do not get a separate
    documentation subsection of their own.

    `### Removed` — exactly one bullet, using the bullet-level bold-prefix form
    `- **BREAKING: ...** — ...`. This is a first use of both a Removed subsection and the bullet-level
    BREAKING prefix in this file; RESEARCH.md § "CHANGELOG Structural Template" establishes that the
    bullet-level form (not the section-header-suffix form used in `[0.4.0]`/`[0.3.0]`) is the right
    precedent here, because only one bullet is breaking, not the whole subsection. The bullet must: name
    both `typst_output_dir` and `typst_author_params`; state that both were registered but never read
    (`outdir` is controlled by the `sphinx-build` CLI argument, and `typst_author_params` was silently
    ignored by the author-formatting path); state honestly that because neither ever affected compiled
    output, removal changes no build's result, and a `conf.py` still setting either is silently ignored by
    Sphinx rather than erroring; and state that there is no deprecation period. Carry the trailing `(CONF-01)`
    citation. Per D-05 the BREAKING label is mandatory and is not to be softened away on the grounds that
    the practical impact is nil — that option was put to the owner and explicitly rejected.

    `### Fixed` — exactly 11 bullets, each in the form `- **Short user-facing label (ID citation)** — what
    the user saw before, and what they see now`. Lead with the observable symptom, not the internal cause;
    the reader is a user who does not know FID numbers, and the IDs exist only for traceability. The 11
    bullets and their required trailing citations, per RESEARCH.md's cluster mapping:

    1. Lost block separation across five constructs — `(FID-02–FID-06)`
    2. Lost intra-signature token spacing — `(FID-07–FID-09)`
    3. Long inline-literal runs clipping at the right margin — `(FID-10)`
    4. Soft/semantic paragraph line breaks now reflow — `(FID-11)`
    5. Codly config wrapper leaking as visible text — `(FID-12)`
    6. Meaning-bearing inline styling restored (external link styling, PEP 3102/570 separators) — `(FID-13–FID-14)`
    7. Issue #117 output filename — `(PDF-01)`
    8. Nested master documents compile with includes and images intact — `(PDF-02)`
    9. `typst_package` template path repaired end-to-end, plus the new config-to-output regression fixture — `(CONF-02, CONF-03)`
    10. `typstpdf` no longer reports success while silently skipping a configured master, plus the render-gate test decoupled from typst-py's internal error wording — `(WR-01, WR-02)`
    11. README.md and CLAUDE.md corrected to match measured behavior — `(DOC-01–DOC-05)`

    RESEARCH.md's draft omits the trailing citation on bullets 7 through 11; add them. Every one of the 25
    ledger IDs must be reachable from some bullet's citation, either named outright or as an endpoint of a
    stated range — Task 2 checks this mechanically.

    Bullet 7 (Issue #117) has extra presentation requirements from D-06 and D-07. It must lead with a bold
    heading so it does not get buried, must show the concrete before/after with a real config line — for
    `typst_documents = [("index", "mydoc", "My Manual", "Author")]` the compiled PDF is now `mydoc.pdf`
    where it was previously `index.pdf` — and must tell readers whose CI or release pipeline references the
    old docname-derived filename to update it. Per D-07 this bullet is NOT labelled BREAKING: it is the
    correction of a bug where the builder used the docname instead of the `typst_documents` target name it
    should always have used. The asymmetry with the `### Removed` bullet is intentional and owner-adjudicated
    — do not harmonize the two.

    Bullet 11 (D-03, D-04) covers Phase 22.4 as a single Fixed entry because those were corrections of
    incorrect information, not typo cleanup: unverifiable test-count and coverage numbers with no enforcing
    gate were removed, "Configuration Options" was reworded as an explicitly partial list linking to the
    real built documentation, a false citation-support claim was dropped (with Citations added to Known
    Limitations instead), a stale Glossary limitation was removed, and CLAUDE.md's Python-version floor was
    corrected to 3.12+.

    `### Verified` — two bullets, sourced strictly from `23-VERIFICATION.md`'s wave-2 evidence (D-02):
    (a) the closing full-corpus regression gate — the Sphinx `doc/` v9.1.0 corpus re-run through
    `-b typstpdf` remains fatal-free, produces valid `%PDF` output, and leaves the `unknown_visit` catalogue
    empty; (b) the milestone invariant held — zero new runtime dependencies, no `@preview` package version
    bump, and the 3-way version-sync surface (`writer.py` / `template_engine.py` / `templates/base.typ`)
    untouched.

    Two content prohibitions apply to the whole entry:

    - Per D-11, state only the three facts the gate actually asserts (fatal-free compilation, valid `%PDF`
      magic bytes, empty `unknown_visit` catalogue). RESEARCH.md's draft `### Verified` bullet opens with an
      approximate document-length figure lifted from the ROADMAP's SC#3 wording — delete that figure. No
      test asserts it, so per the principle established in Phase 22.4 it must not be claimed. The `[0.6.1]`
      entry stated such a figure; this entry deliberately does not.
    - Per D-08, include no commentary about the project's version-numbering policy: no sentence explaining
      why a breaking change ships under a patch-level number, no appeal to the project being early-stage,
      and no mention of an alternative version number for this release.

    Do not touch anything else in the file. Historical entries (`[0.6.1]` and older) are immutable history.
    Add no link-reference line for this version to the bottom-of-file link block, and do not advance the
    unreleased compare link away from `v0.6.1...HEAD` — both are tag-dependent and the tag does not exist
    yet. `/gsd-complete-milestone` owns both when it cuts the tag.

    Scope fence (ROADMAP SC#5, absolute): no tag, no `uv publish`/`twine`/`gh release`, no `git push --tags`,
    no edit to or invocation of `.github/workflows/release.yml`.
  </action>
  <acceptance_criteria>
    - `CHANGELOG.md` contains exactly one heading matching `^## \[0.6.2\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$`, and its date equals the output of `date +%Y-%m-%d`
    - That heading appears after the `## [Unreleased]` heading and before the `## [0.6.1] - 2026-07-20` heading
    - The `[0.6.2]` section contains the subsections `### Removed`, `### Fixed`, and `### Verified`, in that order
    - The `[0.6.2]` section contains no `### Added` and no `### Changed` subsection, and no separate documentation subsection
    - The `[0.6.2]` section contains 12 top-level bullets before `### Verified` (11 in `### Fixed`, 1 in `### Removed`) — inside D-01's 10–12 target
    - The literal `BREAKING` appears exactly once in the `[0.6.2]` section, and that occurrence is inside `### Removed`
    - The `[0.6.2]` section contains `typst_output_dir` and `typst_author_params`
    - The `[0.6.2]` section contains both `index.pdf` and `mydoc.pdf`
    - The `[0.6.2]` section's `### Verified` subsection contains `unknown_visit` and `%PDF`
    - `grep -c '^\[0.6.2\]:' CHANGELOG.md` returns 0
    - `grep -c 'compare/v0.6.1...HEAD' CHANGELOG.md` returns 1
    - `git diff -- CHANGELOG.md` contains no removed (`-`) line other than the diff header
    - `git tag --list 'v0.6.2'` returns empty
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "
import re,sys,subprocess
t=open('CHANGELOG.md',encoding='utf-8').read()
i=t.index('## [0.6.2]')
head=t[i:].splitlines()[0]
s=t[i:].split('## [0.6.1]')[0]
today=subprocess.run(['date','+%Y-%m-%d'],capture_output=True,text=True).stdout.strip()
want=['### Removed','### Fixed','### Verified']
errs=[]
if not re.fullmatch(r'## \[0\.6\.2\] - \d{4}-\d{2}-\d{2}',head): errs.append('heading:'+head)
if today not in head: errs.append('date-not-today:'+head)
if '## [0.6.2]' not in t.split('## [Unreleased]')[1].split('## [0.6.1]')[0]: errs.append('placement')
order=sorted([k for k in want if k in s],key=s.index)
if order!=want: errs.append('sections:'+str(order))
if '### Added' in s or '### Changed' in s: errs.append('extra-section')
if '### Documentation' in s: errs.append('doc-section')
if 'typst_output_dir' not in s or 'typst_author_params' not in s: errs.append('conf01-names')
if 'index.pdf' not in s or 'mydoc.pdf' not in s: errs.append('issue117-example')
if s.count('BREAKING')!=1: errs.append('breaking-count:%d'%s.count('BREAKING'))
if re.search(r'[0-9][0-9,~ -]*-?page',s): errs.append('document-length-figure')
if t.count('[0.6.2]: ')!=0: errs.append('premature-link-ref')
if 'compare/v0.6.1...HEAD' not in t: errs.append('unreleased-link-moved')
if not errs:
    bullets=[l for l in s.split('### Verified')[0].splitlines() if l.startswith('- ')]
    if len(bullets) not in (10,11,12): errs.append('bullets:%d'%len(bullets))
    if 'BREAKING' not in s.split('### Removed')[1].split('### Fixed')[0]: errs.append('breaking-misplaced')
    v=s.split('### Verified')[1]
    if 'unknown_visit' not in v or '%PDF' not in v: errs.append('verified-facts')
print('ERRORS:',errs)
sys.exit(1 if errs else 0)
" &amp;&amp; test "$(git tag --list 'v0.6.2')" = "" &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <done>
    `CHANGELOG.md` carries a correctly dated, correctly placed `## [0.6.2]` entry with a lead paragraph and
    `### Removed` / `### Fixed` / `### Verified` subsections in that order, 12 change bullets, a single
    bullet-level BREAKING item inside `### Removed`, the Issue #117 before/after example, and no
    document-length figure, version-numbering commentary, premature link-reference line, or edit to
    historical entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit the entry against the full 25-item requirement ledger and remediate any drop</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (the authoritative 25-item v0.6.2 ledger: FID-02..FID-14, PDF-01, PDF-02,
      CONF-01..CONF-03, WR-01, WR-02, DOC-01..DOC-05, and each ID's one-line description)
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-RESEARCH.md` §
      "Requirement-Ledger → Cluster Mapping" (the ID-to-bullet mapping table, including the two judgment
      calls: CONF-03 and WR-02 are test-infrastructure-only and are folded as parentheticals into CONF-02's
      and WR-01's bullets rather than getting bullets of their own)
    - `CHANGELOG.md` — the `## [0.6.2]` section written in Task 1
    - `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-VALIDATION.md` § "Manual-Only
      Verifications" (rows 1–3 describe exactly what this audit must confirm)
  </read_first>
  <action>
    Run the mechanical ledger-coverage audit over the `## [0.6.2]` section. The check extracts the section
    between the `## [0.6.2]` and `## [0.6.1]` headings, collects every literal requirement ID, expands every
    stated range (an en-dash or hyphen between two IDs of the same prefix, e.g. a FID-02 through FID-06
    citation) into its full member set, and diffs the union against the expected 25-ID set. Any ID in the
    expected set that is not reachable is a silent drop and must be fixed by amending the relevant bullet's
    trailing citation — never by deleting the ID from the expectation.

    Ledger size — read this before trusting any prose count. The v0.6.2 ledger holds **25** IDs, not 23.
    `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-CONTEXT.md` and `23-RESEARCH.md` both
    say "23" in their prose, but that is an arithmetic slip: FID-02 through FID-14 is 13 IDs, and
    13 + 2 (PDF) + 3 (CONF) + 2 (WR) + 5 (DOC) = 25. Both documents' own enumerated tables list all 25 rows
    correctly, and `grep -oE '\b(FID|PDF|CONF|WR|DOC)-[0-9]+\b' .planning/REQUIREMENTS.md | sort -u | wc -l`
    returns 25. The enumeration is authoritative; the prose count is not. Do not "correct" this plan's
    expectation set down to 23 on the strength of that prose, and do not treat two IDs as out of scope to
    make an arithmetic count line up.

    The same check enforces D-08 by confirming the section contains none of the version-numbering-policy
    terms it screens for. Both checks are in the automated verify command below; run it and act on its
    output.

    Then produce the human half of the audit, which no machine can do: cross-read each of the 25 IDs'
    one-line descriptions in `.planning/REQUIREMENTS.md` against the bullet that cites it, and confirm the
    bullet actually *describes* that item rather than merely carrying its number. Record the result in
    `23-03-SUMMARY.md` as a 25-row table with columns: Requirement ID, ledger one-liner, CHANGELOG
    subsection, the bullet's bold label, and Covered/Gap. If any row reads Gap, amend the bullet in
    `CHANGELOG.md` so it genuinely covers the item, then re-run the audit. Do not close this task with a Gap
    row outstanding.

    While cross-reading, confirm the three presentation requirements 23-VALIDATION.md flags as manual:
    (a) the Issue #117 bullet leads with a bold label and shows the concrete `index.pdf` to `mydoc.pdf`
    change for a `typst_documents` target named `mydoc`; (b) the `### Removed` subsection's single item
    carries the bullet-level bold BREAKING prefix; (c) each bullet's opening clause names the observable
    symptom a user would have seen, so the entry is readable by someone who has never encountered a FID
    number. Record each as confirmed in the SUMMARY.

    Finally re-run the version-sync and 3-way-preview tests to confirm nothing in wave 3 disturbed wave 1's
    state, and re-assert the SC#5 scope fence.

    Do not modify any file other than `CHANGELOG.md`. Do not edit `.planning/REQUIREMENTS.md` to make the
    audit pass.

    Scope fence (ROADMAP SC#5, absolute): no tag, no `uv publish`/`twine`/`gh release`, no `git push --tags`,
    no edit to or invocation of `.github/workflows/release.yml`.
  </action>
  <acceptance_criteria>
    - The ledger-coverage check reports `MISSING_IDS: []` — all 25 of FID-02..FID-14, PDF-01, PDF-02, CONF-01..CONF-03, WR-01, WR-02, DOC-01..DOC-05 are reachable from the entry's citations
    - The policy-term check reports `BANNED_TERMS: []`
    - `23-03-SUMMARY.md` contains a 25-row coverage table and every row reads Covered
    - `23-03-SUMMARY.md` records the three manual presentation confirmations (Issue #117 bold label plus before/after, BREAKING bullet prefix, symptom-first bullet openings)
    - `git diff --name-only` for this plan shows `CHANGELOG.md` as the only modified repository file
    - `.planning/REQUIREMENTS.md` is unmodified
    - `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v` exits 0 (3 passed)
    - `uv sync --extra dev --locked` exits 0
    - `git tag --list 'v0.6.2'` returns empty and `git diff v0.6.1..HEAD -- .github/workflows/release.yml` is empty
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "
import re,sys
t=open('CHANGELOG.md',encoding='utf-8').read()
s=t.split('## [0.6.2]')[1].split('## [0.6.1]')[0]
found=set(re.findall(r'(?:FID|PDF|CONF|WR|DOC)-[0-9]+',s))
for pre,lo,hi in re.findall(r'(FID|PDF|CONF|WR|DOC)-([0-9]+)[–—-](?:FID|PDF|CONF|WR|DOC)-([0-9]+)',s):
    found |= set('%s-%02d'%(pre,n) for n in range(int(lo),int(hi)+1))
exp=set('FID-%02d'%n for n in range(2,15))|{'PDF-01','PDF-02','CONF-01','CONF-02','CONF-03','WR-01','WR-02'}|set('DOC-%02d'%n for n in range(1,6))
miss=sorted(exp-found)
ban=[w for w in ('semver','semantic version','pre-1.0','0.7.0') if w in s.lower()]
print('MISSING_IDS:',miss)
print('BANNED_TERMS:',ban)
sys.exit(1 if miss or ban else 0)
" &amp;&amp; uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v &amp;&amp; uv sync --extra dev --locked &amp;&amp; test "$(git status --porcelain .planning/REQUIREMENTS.md | wc -l)" = "0" &amp;&amp; test "$(git tag --list 'v0.6.2')" = "" &amp;&amp; test "$(git diff v0.6.1..HEAD -- .github/workflows/release.yml | wc -l)" = "0" &amp;&amp; echo LEDGER_COVERAGE_OK</automated>
  </verify>
  <done>
    All 25 v0.6.2 ledger IDs are demonstrably reachable from the `## [0.6.2]` entry's citations with zero
    drops, the entry carries no version-numbering-policy commentary, the SUMMARY holds a 25-row
    Covered-only coverage table plus the three manual presentation confirmations, `.planning/REQUIREMENTS.md`
    is unmodified, and the SC#5 scope fence still holds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` `[0.6.2]` entry → GitHub Release body → users' upgrade decisions | ROADMAP SC#2 makes this entry the single source for the release announcement; an omitted or mislabelled item becomes a silent behavior change for downstream users |
| Repository working tree → tag-triggered publish pipeline | Adding a `[0.6.2]:` link reference or a tag here would point at, or create, a release that does not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-10 | Repudiation | `## [0.6.2]` entry vs the 25-item requirement ledger | high | mitigate | Task 2's mechanical range-expanding coverage check fails the task on any unreachable ID; a 25-row human cross-read table in the SUMMARY catches bullets that carry an ID without describing it. The expectation set is fixed in the check and may not be narrowed to make it pass |
| T-23-11 | Information Disclosure | Removal of publicly documented config values (`typst_output_dir`, `typst_author_params`) | medium | mitigate | D-05's mandatory bullet-level BREAKING label in a `### Removed` subsection, asserted by an exact-count check; the bullet must also disclose that a stale `conf.py` is silently ignored rather than erroring, so users are not left debugging a no-op |
| T-23-12 | Information Disclosure | Output-filename change (Issue #117) breaking downstream CI | medium | mitigate | D-06's bold-led bullet with the concrete `index.pdf` to `mydoc.pdf` before/after and an explicit instruction to update pipelines referencing the old name; presence of both filenames is asserted mechanically |
| T-23-13 | Spoofing | Unverifiable claims in the `### Verified` subsection | medium | mitigate | Wave ordering forces the corpus gate (wave 2) to run before this section is written; D-11 restricts the section to the three facts the gate asserts, with a mechanical screen rejecting a document-length figure |
| T-23-14 | Elevation of Privilege | `.github/workflows/release.yml` (tag-triggered PyPI publish) | high | mitigate | SC#5 scope fence in `must_haves.prohibitions` and both task actions; asserted by `git tag --list 'v0.6.2'` empty, a zero-line workflow diff, no `[0.6.2]:` link-reference line, and an unmoved `[Unreleased]:` compare link |
| T-23-SC | Tampering | pip/uv package installation | low | accept | No package-manager install task exists in this phase; `23-RESEARCH.md` § "Package Legitimacy Audit" records the gate as not applicable (zero new runtime or dev dependencies) |
</threat_model>

<verification>
- Ledger coverage check reports `MISSING_IDS: []` and `BANNED_TERMS: []`
- Structure check reports `ERRORS: []` (heading, date, placement, section order and set, bullet count, single BREAKING inside `### Removed`, #117 before/after, `### Verified` facts, no premature link reference)
- `uv run python -m pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -v` → 3 passed
- `uv sync --extra dev --locked` exits 0
- `git tag --list 'v0.6.2'` empty; `.github/workflows/release.yml` unchanged since `v0.6.1`
- `git diff -- CHANGELOG.md` shows additions only, all above the `## [0.6.1]` heading
</verification>

<success_criteria>
- SC#2 satisfied: a curated `## [0.6.2]` entry exists, fit to serve as the eventual Release body, with the Issue #117 filename change presented as user-visible, a `### Removed` subsection for the CONF-01 deletions, and a fix note for the CONF-02 `typst_package` repair
- D-01 satisfied and proven: all 25 ledger IDs covered by cluster-bundled bullets, 10–12 change bullets, zero silent drops
- D-02/D-11 satisfied: a `### Verified` subsection stating only gate-asserted facts, with no document-length figure
- D-05/D-07 asymmetry preserved exactly as adjudicated; D-08 respected
- SC#5 held: no tag, no publish, no Release, no premature link reference
</success_criteria>

<output>
Create `.planning/phases/23-v0-6-2-release-prep-regression-gate-close/23-03-SUMMARY.md` when done.
It MUST include the 25-row requirement-ID to CHANGELOG-bullet coverage table.
</output>
