---
phase: 09-green-ci-matrix-smoke-test-guardrails
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: []
autonomous: false
requirements: [CI-01, CI-03]
tags: [ci, github-actions, branch-protection, guardrails, release-pr]
user_setup:
  - service: github
    why: "Observing the release PR's Actions run and reconciling main branch-protection required-checks require an authenticated gh CLI against YuSabo90002/typsphinx"
    dashboard_config:
      - task: "Reconcile main branch protection required_status_checks to current ci.yml job names (or approve the gh api PUT)"
        location: "GitHub -> Settings -> Branches -> main -> Require status checks (or via gh api repos/YuSabo90002/typsphinx/branches/main/protection)"

must_haves:
  truths:
    - "pyproject ceilings sphinx>=9.1,<10 / docutils>=0.21,<0.23 / typst>=0.15.0,<0.16 are present and drift.yml/dependabot.yml carry no conflicting hardcoded ceilings (CI-03 verified as a no-op per D-06)"
    - "main branch protection required_status_checks.contexts match the current ci.yml job names — the stale, un-producible 'Test Python 3.10/3.11 on ubuntu-latest' contexts are removed (or the reconciliation is explicitly deferred to Phase 10 with a documented rationale)"
    - "A PR release/v0.5.0 -> main is open and every job is observed green: lint, the 3-OS x Python 3.12-3.13 test matrix, type-check, coverage, build, integration, and docs.yml build-docs"
    - "The observation PR is NOT merged — merge is deferred to Phase 10 per D-03"
  artifacts:
    - "PR release/v0.5.0 -> main (process artifact: open + observed all-green)"
    - "09-02-SUMMARY.md with the observed job conclusions, the PR number, and the branch-protection decision recorded"
  key_links:
    - "branch protection required contexts <-> current ci.yml job names (access-control durability — Security Domain V4)"
    - "pull_request:[main] trigger fires BOTH ci.yml and docs.yml against release-branch content — the only path that exercises docs.yml build-docs (D-01); ci.yml's existing workflow_dispatch enables a D-02-safe pre-flight"
---

<objective>
Close CI-01 and CI-03 for the v0.5.0 milestone. Verify that the durability guardrails already reflect the new
majors (CI-03 — a documented no-op per D-06), reconcile the stale `main` branch-protection required-checks list
(the load-bearing access-control finding), then open a single PR `release/v0.5.0 -> main` and OBSERVE every CI
job green (CI-01). Phase 9 opens and observes only — it does NOT merge (D-03; Phase 10 adds the version-string
fix to the same PR and merges).

Purpose: All Phase 6-8 work lives on `release/v0.5.0` and has NEVER run through GitHub Actions — the PR is the
first (and only) time the new Sphinx 9.1 / docutils 0.22 / typst 0.15 stack is exercised on Windows/macOS runners
and the only path that fires `docs.yml`. Observing green pre-merge is the safety gate the whole milestone exists
to restore.

Output: An open, all-green observation PR (CI-01 closed), a documented CI-03 verification, and a resolved
branch-protection decision — all recorded in the SUMMARY for Phase 10 to pick up.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-green-ci-matrix-smoke-test-guardrails/09-CONTEXT.md
@.planning/phases/09-green-ci-matrix-smoke-test-guardrails/09-RESEARCH.md
@.github/workflows/ci.yml
@.github/workflows/docs.yml
@.github/workflows/drift.yml
@.github/dependabot.yml
@pyproject.toml
</context>

<artifacts_this_phase_produces>
This plan produces NO new git-tracked source symbols. Its artifacts are process/observation:

- The open PR `release/v0.5.0 -> main` (a GitHub process artifact, not a file)
- An updated `main` branch-protection `required_status_checks.contexts` list (a GitHub repo SETTING, not a git-tracked file — D-02 forbids only workflow-trigger edits, which this is not)
- No new files, no new test symbols, no `.github/` file edits (CI-03 is verification-only per D-06)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify CI-03 guardrails already reflect the new majors (no-op, D-06)</name>
  <files>(verification-only — no file edits expected; evidence recorded in SUMMARY)</files>
  <read_first>
    - pyproject.toml (dependency ceilings, lines ~28-30)
    - .github/workflows/drift.yml (confirm no hardcoded version ceilings — its `uv lock --upgrade` respects pyproject)
    - .github/dependabot.yml (confirm `sphinx-typst-stack` group is pattern-only, no version fields)
    - .planning/phases/09-green-ci-matrix-smoke-test-guardrails/09-RESEARCH.md (CI-03: Guardrail Verification; Pitfall 4 "Manufacturing a CI-03 file change that isn't needed")
  </read_first>
  <action>
    Per D-06, treat CI-03 as verification, NOT a file edit. Confirm via grep/read that `pyproject.toml` declares
    `sphinx>=9.1,<10`, `docutils>=0.21,<0.23`, and `typst>=0.15.0,<0.16`; that `.github/workflows/drift.yml`
    contains no hardcoded `sphinx<`/`typst<`/`docutils<` ceiling (it uses `uv lock --upgrade`, which derives its
    effective ceiling from pyproject); and that the `.github/dependabot.yml` `sphinx-typst-stack` group carries
    only name patterns (`sphinx*`/`docutils*`/`typst*`) with no version fields. Record this evidence in the SUMMARY.
    Do NOT edit `drift.yml` or `dependabot.yml` "just to be safe" — RESEARCH.md Pitfall 4 flags that as manufactured
    busywork. Only if verification surfaces a REAL discrepancy (a stale ceiling that conflicts with the majors) make
    the minimal follow-through edit and note it explicitly; otherwise close CI-03 as a documented no-op.
  </action>
  <verify>
    <automated>grep -E 'sphinx>=9.1,<10' pyproject.toml && grep -E 'docutils>=0.21,<0.23' pyproject.toml && grep -E 'typst>=0.15.0,<0.16' pyproject.toml && ! grep -Eq 'sphinx<|typst<|docutils<' .github/workflows/drift.yml && echo CI-03-VERIFIED-NOOP</automated>
  </verify>
  <acceptance_criteria>
    - `grep` confirms all three ceilings present in `pyproject.toml`: `sphinx>=9.1,<10`, `docutils>=0.21,<0.23`, `typst>=0.15.0,<0.16`.
    - `.github/workflows/drift.yml` contains no hardcoded `sphinx<`/`typst<`/`docutils<` ceiling (grep returns nothing).
    - `.github/dependabot.yml` `sphinx-typst-stack` group has only `patterns`/`exclude-patterns` (no `versions`/version-constraint fields).
    - Zero git-tracked file changes are committed for CI-03 (unless a real discrepancy was found and explicitly noted); the no-op verification is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>CI-03 is closed as a verified no-op: the pyproject ceilings are the single source of truth for `sphinx<10`/`typst<0.16`/`docutils<0.23`, and neither `drift.yml` nor `dependabot.yml` declares a conflicting ceiling. Evidence documented; no busywork edit made.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Reconcile stale main branch-protection required_status_checks (open-question 1, decision (a))</name>
  <files>(GitHub repo setting via gh api — not a git-tracked file)</files>
  <read_first>
    - .github/workflows/ci.yml (current job names: the `test` matrix produces "Test Python {3.12,3.13} on {ubuntu,windows,macos}-latest"; plus "Lint and Format Check", "Type Check", "Code Coverage", "Build Package", "Integration Test - basic/advanced")
    - .github/workflows/docs.yml (the `build-docs` job)
    - .planning/phases/09-green-ci-matrix-smoke-test-guardrails/09-RESEARCH.md (Finding 2; Common Pitfalls #1; Security Domain V4; Open Questions #1; Assumptions Log A1)
  </read_first>
  <action>
    DECISION (resolving RESEARCH open-question 1 in favor of variant (a) — fix in Phase 9, framed as an
    access-control durability guardrail, Security Domain V4): reconcile `main`'s branch-protection
    `required_status_checks.contexts` to the CURRENT `ci.yml` job names. First run
    `gh api repos/YuSabo90002/typsphinx/branches/main/protection` to capture the live stale list (it still requires
    "Test Python 3.10 on ubuntu-latest" / "Test Python 3.11 on ubuntu-latest" — job names no longer produced since
    Phase 6 dropped 3.10/3.11 — and omits the OS-matrix legs and `build-docs`). Propose the reconciled context set
    and, on human approval, apply it via `gh api --method PUT
    repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks` (or Settings -> Branches UI). This
    is a repo SETTING, not a workflow-trigger YAML edit, so it does not violate D-02. Then confirm via a follow-up
    `gh api ... GET` that no context names a non-existent job. Record the before/after context lists and the
    decision in the SUMMARY so Phase 10 (which owns the merge) inherits a clean gate. If the human instead prefers
    to DEFER this to Phase 10, that is acceptable — but capture the deferral rationale explicitly in the SUMMARY and
    STATE.md (do NOT leave it silent; Phase 10 must not discover a merge-blocked PR with no prior context).
  </action>
  <what-built>
    Captured the live stale `required_status_checks.contexts` for `main` and prepared a reconciled context list
    that matches the current `ci.yml`/`docs.yml` job names (replacing the removed 3.10/3.11 ubuntu contexts with the
    current 3.12/3.13 legs; optionally adding the windows/macos matrix legs and `build-docs`).
  </what-built>
  <how-to-verify>
    1. Review the printed BEFORE list from `gh api repos/YuSabo90002/typsphinx/branches/main/protection` — confirm it names "Test Python 3.10 on ubuntu-latest" / "Test Python 3.11 on ubuntu-latest" (jobs that no longer exist).
    2. Review the proposed AFTER list — confirm every context maps to a real job in `ci.yml`/`docs.yml` (at minimum: "Lint and Format Check", "Type Check", "Code Coverage", "Build Package", and the ubuntu 3.12/3.13 test legs; the exact required subset — whether to also require the windows/macos legs and `build-docs` — is your policy call).
    3. Approve applying the PUT (or apply it yourself in Settings -> Branches), OR direct that this be deferred to Phase 10.
    4. Confirm the follow-up GET shows no context referencing a non-existent job.
  </how-to-verify>
  <acceptance_criteria>
    - The live stale context list was captured and shown (BEFORE state documented in SUMMARY).
    - EITHER: `required_status_checks.contexts` now contains only names that correspond to real `ci.yml`/`docs.yml` jobs (no "Test Python 3.10/3.11 on ubuntu-latest") — verified by a GET after the PUT; OR: an explicit deferral-to-Phase-10 decision with rationale is recorded in SUMMARY + STATE.md.
    - No `.github/workflows/*.yml` trigger was edited (D-02 preserved — this is a repo setting only).
  </acceptance_criteria>
  <resume-signal>Type "approved" (contexts reconciled) or "defer" (documented deferral to Phase 10), or describe an alternative required-checks policy.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pre-flight the matrix, open the release PR, and observe every job green (CI-01) — do NOT merge (D-03)</name>
  <files>(GitHub Actions observation + PR — no git-tracked file changes)</files>
  <read_first>
    - .github/workflows/ci.yml (triggers `pull_request: [main]` + existing `workflow_dispatch`; job names)
    - .github/workflows/docs.yml (triggers `pull_request: [main]`; `build-docs` — no workflow_dispatch, observable only via the PR)
    - .planning/phases/09-green-ci-matrix-smoke-test-guardrails/09-RESEARCH.md (D-01/D-02/D-03; Finding 1 "never run in CI"; Pitfall 2; the "Pre-flighting the matrix" and observation commands; Pitfall 5 re: pre-existing red Dependabot PR #108 — do not conflate)
  </read_first>
  <action>
    Execute D-01's PR-based observation as the FINAL, highest-confidence step. (1) D-02-safe pre-flight: trigger the
    existing `workflow_dispatch` on `ci.yml` against the release branch — `gh workflow run ci.yml --repo
    YuSabo90002/typsphinx --ref release/v0.5.0` — and watch it (`gh run watch`) to surface any Windows/macOS-specific
    breakage on the never-before-CI-run stack (Finding 1) in a disposable run BEFORE it counts as "the" observation;
    fix any cross-OS break and re-run before proceeding. Note `docs.yml` has NO workflow_dispatch, so its build-docs
    lane is pre-flightable only via `tox -e docs-pdf` locally — run that as the local proxy. (2) Open the single
    observation PR `release/v0.5.0 -> main` (`gh pr create --base main --head release/v0.5.0`), which fires BOTH
    `ci.yml` and `docs.yml` on `pull_request:[main]` — the one path that exercises docs.yml against release-branch
    content (D-01). (3) Observe every job green: `gh pr checks <PR#> --watch` and/or `gh run list --workflow=ci.yml
    --workflow=docs.yml --branch release/v0.5.0 --json name,conclusion`. Also spot-check the PR's "Merge" panel
    state (per Assumptions Log A1) to confirm the Task-2 branch-protection reconciliation took effect. (4) STOP —
    do NOT merge (D-03: Phase 10 adds the `__version__` 0.4.3->0.5.0 REL-01 commit to this SAME PR, which re-triggers
    the checks, then merges -> tags v* -> release.yml publishes). Do NOT edit ci.yml/docs.yml triggers (D-02). Do
    NOT touch the pre-existing red Dependabot PR #108 (Pitfall 5 — unrelated, superseded once v0.5.0 merges in
    Phase 10). Record the PR number and per-job conclusions in the SUMMARY.
  </action>
  <what-built>
    Pre-flighted the 3-OS x 3.12/3.13 matrix on `release/v0.5.0` via ci.yml's existing `workflow_dispatch` (D-02
    safe) plus a local `tox -e docs-pdf` proxy for the docs lane, then opened the single observation PR
    `release/v0.5.0 -> main` that fires both ci.yml and docs.yml on `pull_request:[main]`.
  </what-built>
  <how-to-verify>
    1. `gh pr checks <PR#> --watch` — confirm EVERY job reports success: "Lint and Format Check", "Type Check", "Code Coverage", "Build Package", all six "Test Python {3.12,3.13} on {ubuntu,windows,macos}-latest" legs, "Integration Test - basic/advanced", and docs.yml "build-docs".
    2. Confirm the new `tests/test_preview_smoke_gate.py` ran (and passed) inside the matrix legs (it rides `pytest tests/` — CI-02 now also proven in real CI, not just locally).
    3. Inspect the PR "Merge" panel — confirm it reflects the reconciled required checks from Task 2 (no context stuck "Expected — waiting for status").
    4. Confirm the PR is OPEN and NOT merged (D-03 — Phase 10 owns the merge).
  </how-to-verify>
  <acceptance_criteria>
    - CI-01: an observed Actions run on the `release/v0.5.0 -> main` PR shows every job green — lint, the 3-OS × Python 3.12–3.13 test matrix, type-check, coverage, build, integration, and docs.yml build-docs (per-job conclusions recorded in SUMMARY with the PR number).
    - The pre-flight `gh workflow run ci.yml --ref release/v0.5.0` run completed (green, or breakage fixed and re-run green) before the observation PR was opened.
    - The PR is open and UNMERGED (D-03); no `ci.yml`/`docs.yml` trigger was edited (D-02).
    - The new smoke test executed within the matrix legs (CI-02 confirmed in live CI).
  </acceptance_criteria>
  <resume-signal>Type "approved" (all jobs observed green, PR left open/unmerged) or describe which job is red so it can be fixed and re-observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR -> main merge | Branch protection `required_status_checks` is the access-control gate over `main` — it decides whether GitHub considers a run's checks satisfied for merge |
| `@preview` registry -> build (Dependabot) | Dependabot may propose `@preview`/runtime bumps; ceilings in pyproject bound them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering / Elevation of Privilege | Stale `main` branch-protection `required_status_checks.contexts` (names removed 3.10/3.11 jobs, omits current legs) | high | mitigate | Task 2 reconciles the context list to current `ci.yml`/`docs.yml` job names so the gate actually verifies the checks it claims to; `enforce_admins.enabled=false` means an admin could otherwise bypass a perpetually-"pending" gate without the intended verification (Security Domain V4) |
| T-09-03 | Tampering (supply chain) | Dependabot proposing an out-of-ceiling bump (e.g. pre-existing red PR #108 targeting docutils 0.23) | medium | accept | Existing pyproject upper-bound ceilings + human review of Dependabot PRs already bound this; PR #108 is superseded once v0.5.0 merges in Phase 10 (informational, not a Phase 9 action — Pitfall 5) |
| T-09-06 | Information Disclosure / Spoofing | Relying on the post-merge push run instead of pre-merge PR checks (merging blind) | high | mitigate | D-01's PR-based observation runs the checks on a simulated merge pre-merge; Phase 9 observes green BEFORE any merge, and defers the merge itself to Phase 10 (D-03) |
| T-09-SC | Tampering (supply chain) | pip/npm/cargo installs | n/a | accept | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: Not applicable) — no install checkpoint required |
</threat_model>

<verification>
- CI-03: `pyproject.toml` shows all three ceilings; `drift.yml`/`dependabot.yml` carry no conflicting hardcoded ceilings (verified no-op, documented).
- Branch protection: `gh api .../branches/main/protection` after Task 2 shows `required_status_checks.contexts` free of non-existent job names (or a documented deferral to Phase 10).
- CI-01: `gh pr checks <PR#>` on the `release/v0.5.0 -> main` PR shows every ci.yml + docs.yml job green; PR is open and unmerged.
- No `.github/workflows/*.yml` trigger edits (D-02); no merge performed (D-03).
</verification>

<success_criteria>
CI-01 and CI-03 satisfied: the durability guardrails are confirmed to already reflect the new majors (documented
no-op), the `main` branch-protection required-checks list is reconciled to real job names (or explicitly deferred
with rationale), and a single `release/v0.5.0 -> main` PR is open with every CI job observed green — left unmerged
for Phase 10.
</success_criteria>

<output>
Create `.planning/phases/09-green-ci-matrix-smoke-test-guardrails/09-02-SUMMARY.md` when done — include the PR
number, the per-job green conclusions, the CI-03 no-op evidence, and the branch-protection before/after (or
deferral) decision.
</output>
