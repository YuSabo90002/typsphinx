---
phase: 53-template-registry-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md
autonomous: true
requirements: [TPL-03, TPL-04]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "53-RED-EVIDENCE.md exists and records the pre-change commit SHA measured by `git rev-parse HEAD` at capture time, not copied from any planning document."
    - "For each of the four existing configuration shapes (typst_template set / typst_package set alone / typst_template_function set alone / nothing set) the artifact records the full list of emitted .typ files with a SHA-256 per file."
    - "The artifact records which PDF path was taken — real typst.compile() page counts, or the -b typst hash-only fallback — as a live probe result, not as an assumption (RESEARCH Assumption A1)."
    - "The TPL-04 equivalence baseline records the four-element tuple build and the explicit fifth-element \"typst\" build as two separately hashed trees compared against each other."
    - "Nothing set (bundled base.typ, no typst_template / typst_package / typst_template_function) is one of the four captured shapes, so the empty-configuration case has a recorded pre-change baseline (TPL-03)."
    - "The evidence artifact is named 53-RED-EVIDENCE.md; no file named 53-VERIFICATION.md is created or written by this plan (D-12)."
  artifacts:
    - .planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md
  key_links:
    - "53-RED-EVIDENCE.md's pre-change section is the only artifact that can prove pre-change identity; plan 53-05 reads its recorded SHAs and diffs post-change measurements against them."
  prohibitions:
    - "Evidence in 53-RED-EVIDENCE.md must be produced by running the commands and transcribing their real output; hashes, page counts, file lists and commit SHAs must never be reconstructed, estimated, or carried over from a planning document."
---

<objective>
Capture the SC#2 pre-change byte-identity baseline BEFORE any Phase 53 code lands, into
`53-RED-EVIDENCE.md`. Per D-12 this is a one-off evidence artifact, not a new golden-file pytest
gate: a golden generated from post-change code cannot prove pre-change identity, and Phase 54
deliberately invalidates the `_template.typ` layout one phase later.

Per the standing GATE-01 RED-first bar (ROADMAP binding constraint #6, as amended in v0.7.0), the
RED for TPL-03/TPL-04 is a *recorded pre-change baseline*, not a compile failure — the whole point of
Phase 53 is that it changes no output, so the only way to falsify that claim later is to have
measured the "before" first.

Purpose: make TPL-03's zero-edit-equivalence claim and TPL-04's four-element-tuple-equivalence claim
falsifiable by identity rather than by inspection.
Output: `.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` with a complete
pre-change section (commit SHA, per-file SHA-256 across four shapes, PDF page counts or a recorded
fallback, and the TPL-04 comparison).
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/53-template-registry-foundation/53-CONTEXT.md
@.planning/phases/53-template-registry-foundation/53-RESEARCH.md
</context>

## Artifacts this phase produces

Symbols and files created across the whole of Phase 53 (this plan produces only the first item;
the rest are listed so drift verification does not flag them as pre-existing symbols):

| Artifact | Kind | Plan |
|---|---|---|
| `.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` | evidence artifact | 53-01 (pre), 53-05 (post) |
| `.planning/phases/53-template-registry-foundation/53-CI-EVIDENCE.md` | evidence artifact | 53-05 |
| `typsphinx/template_registry.py` | new module | 53-02 |
| `TemplateRegistryEntry` (frozen dataclass: `key`, `template`, `package`, `template_function`) | dataclass | 53-02 |
| `resolve_template_registry(config, srcdir)` | function | 53-02 |
| `resolve_registry_key(registry, entry)` | function | 53-02 |
| `RESERVED_REGISTRY_KEY` | module constant | 53-02 |
| `typst_document_templates` | Sphinx config value | 53-02 |
| `TypstBuilder._document_template_registry` | builder attribute | 53-02 |
| `TypstWriter.render_wrapper(..., template_entry=...)` | new keyword parameter | 53-02 |
| `tests/test_template_registry.py` | new test module | 53-02 (created), 53-03 (expanded) |
| `_WINDOWS_RESERVED_NAMES`, `_is_windows_reserved_name`, `_has_case_collision`, `_violates_conf17`, `_validate_registry_key_shape` | private predicates | 53-03 |
| `TemplateResolution.path` | new dataclass field | 53-04 |

<tasks>

<task type="tracer">
  <name>Task 1: Probe the environment and capture the pre-change baseline for all four shapes</name>
  <files>.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md</files>
  <read_first>
    - `./CLAUDE.md` — the "Worktree-isolated execution" section. This is a hard prerequisite: run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` FIRST, then invoke every subsequent command through `uv run`. Without it, `sphinx-build` imports the unchanged main-tree package and the measurement is of the wrong code.
    - `.planning/phases/53-template-registry-foundation/53-RESEARCH.md` § "Q6 — SC#2's byte-identity evidence procedure (D-12)" — the six-step procedure, the fixture table, and the environment note on `typst.compile()`.
    - `.planning/phases/53-template-registry-foundation/53-CONTEXT.md` § D-12 — why this is an evidence artifact rather than a pytest gate, and the reserved-filename warning.
    - `tests/fixtures/documented_params_contract_gate/conf.py`, `tests/fixtures/typst_lang_gate/package_no_lang/conf.py`, `tests/fixtures/params_exclusivity_gate/zero_params_default/conf.py`, `tests/roots/test-basic/conf.py` — the four shapes' real `typst_documents` tuples and `typst_*` settings, to be transcribed into the artifact.
  </read_first>
  <precondition>The worktree has been provisioned with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and `uv run python -c "import typsphinx, sphinx, pypdf"` succeeds.</precondition>
  <action>
    Provision the worktree per CLAUDE.md, then record `uv run python -c "import typst; print('probe')"` plus one real `typst.compile()` call against a trivial source string as a LIVE probe, and write the probe's verbatim outcome into the artifact. RESEARCH Assumption A1 warns that `typst.compile()` succeeded on 2026-08-15 but contradicted an earlier 2026-08-14 measurement, so the path taken must be recorded, never assumed. If `typst.compile()` raises, fall back to the `-b typst`-only, hash-only procedure (the shape `51-RESEARCH.md` used) and state in the artifact that the PDF page-count half of SC#2's evidence was unobtainable in this environment, naming the exception.

    Record `git rev-parse HEAD` as the pre-change commit SHA. Do not copy any SHA out of a planning document — measure it.

    For each of the four existing configuration shapes, build into a scratch directory under the session scratchpad and hash every emitted `.typ` file:
    shape A `typst_template` set — `tests/fixtures/documented_params_contract_gate`;
    shape B `typst_package` set alone (no `typst_template`) — `tests/fixtures/typst_lang_gate/package_no_lang`;
    shape C `typst_template_function` set alone (no `typst_template`, no `typst_package`) — `tests/fixtures/params_exclusivity_gate/zero_params_default`;
    shape D nothing set, bundled `base.typ` — `tests/roots/test-basic`.
    Use `uv run python -m sphinx -b typstpdf <source_dir> <scratch_pre>` on the PDF path, or `-b typst` on the fallback path. Then `find <scratch_pre> -name '*.typ' | sort` and `sha256sum` each, transcribing the full sorted list verbatim.

    On the PDF path, read each compiled PDF's page count with `pypdf.PdfReader(str(pdf_path))` then `len(reader.pages)` — the exact call shape already precedented at `tests/test_admonition_locale_title_precedence_gate.py:301` — and record it per shape.

    Write `53-RED-EVIDENCE.md` with one section per shape: the fixture path, the fixture's `typst_*` settings quoted from its `conf.py`, the builder used, the sorted `.typ` file list with SHA-256 per file, and the PDF page count or the recorded fallback reason. Name the file `53-RED-EVIDENCE.md` exactly — `53-VERIFICATION.md` is reserved by `gsd-verifier` and would be clobbered (D-12).
  </action>
  <verify>
    <automated>test -f .planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md &amp;&amp; grep -c '^[0-9a-f]\{64\}' .planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` exists.
    - The file contains a line matching `git rev-parse HEAD` output: a 40-hex-character pre-change commit SHA, and the artifact states it was measured with `git rev-parse HEAD` in this worktree.
    - `grep -c '^[0-9a-f]\{64\}'` over the artifact returns at least 4 (one SHA-256 per emitted `.typ` file, across at least four shapes).
    - The artifact names all four fixture paths verbatim: `tests/fixtures/documented_params_contract_gate`, `tests/fixtures/typst_lang_gate/package_no_lang`, `tests/fixtures/params_exclusivity_gate/zero_params_default`, `tests/roots/test-basic`.
    - The artifact contains a section recording the live `typst.compile()` probe outcome and which of the two evidence paths (PDF page counts, or `-b typst` hash-only fallback) was taken.
    - `test -e .planning/phases/53-template-registry-foundation/53-VERIFICATION.md` returns non-zero (this plan creates no file at that reserved path).
  </acceptance_criteria>
  <done>The pre-change baseline for all four shapes is measured and transcribed into `53-RED-EVIDENCE.md`, with the PDF-path decision recorded from a live probe.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the TPL-04 four-element-vs-fifth-element equivalence baseline</name>
  <files>.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md` — the sections Task 1 just wrote; this task appends to the same artifact.
    - `.planning/phases/53-template-registry-foundation/53-RESEARCH.md` § Q6 step 6 — the fifth-shape comparison, and its note that `params_exclusivity_gate/zero_params_default/conf.py:30` already authors a four-element tuple, making it directly usable without modification.
    - `tests/fixtures/params_exclusivity_gate/zero_params_default/conf.py` — confirm the exact element count of each `typst_documents` tuple before copying the tree.
  </read_first>
  <action>
    Copy `tests/fixtures/params_exclusivity_gate/zero_params_default` twice into the session scratchpad. Leave the first copy's `typst_documents` tuples exactly as authored (four elements). In the second copy, append the literal string `"typst"` as a fifth element to every `typst_documents` tuple, changing nothing else. Build both copies with the same builder Task 1 settled on, into two separate scratch output directories.

    Hash every emitted `.typ` file in both trees and diff the two hash sets against EACH OTHER — this is TPL-04's own equivalence claim, independent of the four-shapes pre/post baseline, so it does not compare against a stored value. On the PDF path also compare page counts.

    Append a "TPL-04 equivalence (pre-change)" section to `53-RED-EVIDENCE.md` recording: the two source trees, the exact tuple shapes before and after, both sorted `.typ` file lists with SHA-256 per file, and the comparison result. If the two trees differ pre-change, record that verbatim as a measured fact rather than adjusting the procedure — a pre-existing divergence is exactly what this baseline exists to expose (TPL-04).
  </action>
  <verify>
    <automated>grep -c 'TPL-04 equivalence' .planning/phases/53-template-registry-foundation/53-RED-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `53-RED-EVIDENCE.md` contains a heading whose text includes `TPL-04 equivalence`.
    - That section lists two sorted `.typ` file inventories, each with one SHA-256 per file, and states explicitly whether the two sets are identical.
    - That section quotes the four-element `typst_documents` tuple as authored and the five-element form used for the second build.
    - `uv run pytest tests/ -q` still exits 0 — this plan touches no source file, so the tree is unchanged and green.
  </acceptance_criteria>
  <done>TPL-04's pre-change equivalence baseline is measured and appended to `53-RED-EVIDENCE.md`, with the comparison result stated as measured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence artifact → later phases | `53-RED-EVIDENCE.md`'s recorded hashes are trusted by plan 53-05 and by `/gsd-verify-work` as ground truth for pre-change state; a fabricated or reconstructed value would silently launder SC#2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-04 | Tampering | `53-RED-EVIDENCE.md` | medium | mitigate | Every recorded value is transcribed from a command executed in this plan; the acceptance criteria require the commit SHA to be produced by `git rev-parse HEAD` and the SHA-256 lines to be countable by regex, so an artifact written from prose fails the gate. |
| T-53-05 | Repudiation | `typst.compile()` availability | low | mitigate | The evidence path is decided by a live probe recorded in the artifact, not by a note in RESEARCH; a later reader can tell which path was taken and why (Assumption A1). |
| T-53-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. STACK.md's verdict for this milestone is "Add nothing" — zero new runtime dependencies, zero new stdlib imports. No package-legitimacy audit is required because no install task exists. |
</threat_model>

<verification>
- `uv run pytest tests/ -q` exits 0 (this plan modifies no source; the 32-file `_template.typ` net is untouched).
- `53-RED-EVIDENCE.md` exists with a measured pre-change commit SHA, four shape sections, and a TPL-04 equivalence section.
- No file exists at `.planning/phases/53-template-registry-foundation/53-VERIFICATION.md`.
</verification>

<success_criteria>
SC#2's pre-change half is complete and measured. Plan 53-05 can diff post-change measurements against
recorded values without re-deriving anything.
</success_criteria>

<output>
Create `.planning/phases/53-template-registry-foundation/53-01-SUMMARY.md` when done.
</output>
