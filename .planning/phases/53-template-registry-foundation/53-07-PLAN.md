---
phase: 53-template-registry-foundation
plan: 07
type: execute
wave: 6
depends_on: ["53-06"]
gap_closure: true
files_modified:
  - typsphinx/template_registry.py
  - tests/test_template_registry.py
autonomous: true
requirements: [CONF-15, CONF-17, CONF-18]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A legal absolute `template` path on a different drive from `srcdir` does not crash the build: `_violates_conf17()` treats the `ValueError` `os.path.commonpath()` raises for cross-drive absolute paths as 'not a CONF-17 violation' and returns `False`, mirroring `_track_image()`'s own `except ValueError` cross-drive handling at builder.py:950-955 (53-REVIEW.md CR-02; 53-VERIFICATION.md Anti-Patterns row 2)."
    - "A non-`str` registry key stops the build with this module's own accumulated `ExtensionError` naming the offending key, never a raw `AttributeError: 'int' object has no attribute 'strip'` (53-REVIEW.md WR-02; 53-VERIFICATION.md Anti-Patterns row 3)."
    - "A truthy non-`dict` registry definition stops the build with this module's own accumulated `ExtensionError` naming the offending value, never a raw `AttributeError: 'str' object has no attribute 'get'`; a FALSY definition (`None`, empty string, `0`) still normalizes to an empty definition and resolves, exactly as it does today (53-REVIEW.md WR-03)."
    - "A heterogeneous key set such as an `int` key beside a `str` key stops the build with the module's `ExtensionError`, never the `TypeError` `sorted()` raises when it compares an `int` to a `str` -- the iteration key is total across mixed types."
    - "D-03's determinism survives the new ordering: the same malformed registry, declared in two different `dict` insertion orders, yields a byte-identical `ExtensionError` message -- including a mixed-type key set -- and the existing all-`str` ordering is unchanged, so `test_three_independently_broken_keys_raise_once_order_independently` passes UNMODIFIED (TPL-01 ordering edge; probe fallback §A)."
    - "A non-`str` key never reaches `TypstBuilder._collision_key()`: `all_keys` stays `isinstance(key, str)`-filtered, so CONF-18's case-collision comparison is still defined only over `str` keys and a registry mixing a non-`str` key with two case-colliding `str` keys reports BOTH failures in the one accumulated raise (CONF-18 encoding edge; probe fallback §A)."
    - "Both new failure classes join the SAME `failures` list and the SAME `typst_document_templates: N invalid definition(s): ...` summary -- no second message shape is minted, and `_validate_output_path_collisions()`'s `typst: N output path collision(s):` text is still untouched (D-03)."
    - "`_KEY_SHAPE_REJECTION_CASES` still holds exactly seven distinct entries and `_validate_registry_key_shape()` still judges exactly those seven cases: the non-`str` key guard is a TYPE GUARD in `resolve_template_registry()`'s accumulate loop, placed BEFORE the dispatch into `_validate_registry_key_shape()`, and is not a denylist case (53-03-PLAN.md prohibition; D-02)."
  artifacts:
    - typsphinx/template_registry.py
    - tests/test_template_registry.py
  key_links:
    - "The `except ValueError` in `_violates_conf17()` must return `False`, not `True`. Two paths on different drives can never share an ancestor, so a cross-drive parent is definitionally NOT `srcdir`-or-an-ancestor-of-`srcdir` -- returning `True` would newly reject the absolute-path-outside-srcdir case D-07 explicitly keeps legal."
    - "The iteration key must preserve the existing all-`str` ordering exactly. `53-REVIEW.md` suggests `repr(k)` for every key; `repr()` switches to double quotes for a string containing an apostrophe, which would silently reorder such keys against today's plain `sorted()` and break D-03's published byte-identity for a config that has one."
    - "The second loop at template_registry.py:309-317 (`for key, definition in declared.items()`) needs NO new guard: it runs only after the accumulate pass found zero failures, by which point every key is a `str` and every definition is a `dict` or falsy. Record that reachability argument in a comment instead of adding a redundant second check that could drift from the first."
  prohibitions:
    - "MUST NOT weaken, skip, xfail, delete, or rewrite any existing test in `tests/test_template_registry.py` to make this plan pass. All 57 pass at HEAD; a red one means the production change is wrong."
    - "MUST NOT downgrade either new failure class to a `logger.warning` or a tolerate-and-skip. A malformed registry stops the build -- that is the framing sentence of ROADMAP SC#3 these two defects currently undercut."
    - "MUST NOT extend `_KEY_SHAPE_REJECTION_CASES` beyond its seven locked entries, and MUST NOT implement `53-REVIEW.md` CR-01's drive-qualified key case in this phase. `53-CONTEXT.md`'s Deferred Ideas locks that shape as accepted in Phase 53 by design, and tightening it after v0.9.0 ships would turn a previously-building `conf.py` into a hard `ExtensionError` without the CHANGELOG breaking-change entry D-02's reversibility note requires."
---

<objective>
Close the two ⚠ WARNING robustness defects `53-VERIFICATION.md`'s Anti-Patterns table records, both
live-reproduced by the verifier, both also `53-REVIEW.md` findings, and both sitting on the same
criterion 53-06 closes: SC#3's framing sentence, "every malformed registry stops the build with a
message naming the specific reason".

1. `_violates_conf17()` (template_registry.py:137-158) calls `os.path.commonpath()` with no handler
   for the `ValueError: Paths don't have the same drive` it raises for two absolute Windows paths on
   different drives — so a case D-07 documents as LEGAL crashes with a raw traceback instead of this
   module's clean `ExtensionError` contract. The milestone runs a `windows-latest` CI lane, so this
   is reachable, not theoretical.
2. A non-`str` registry key and a truthy non-`dict` definition each crash with a raw `AttributeError`
   instead of joining the module's accumulate-then-raise-once pass.

Purpose: make the module's own error contract total over its input surface, so "malformed registry"
means "clean, specific, accumulated `ExtensionError`" for every malformed shape rather than for the
six the resolver happened to anticipate.
Output: three guards in `typsphinx/template_registry.py` and their coverage in
`tests/test_template_registry.py`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/53-template-registry-foundation/53-CONTEXT.md
@.planning/phases/53-template-registry-foundation/53-REVIEW.md
@.planning/phases/53-template-registry-foundation/53-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-drive absolute template path stops crashing CONF-17's predicate</name>
  <precondition>The worktree has been provisioned with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"` prints a path inside THIS worktree, not the main checkout. Per CLAUDE.md § "Worktree-isolated execution" this is mandatory.</precondition>
  <files>typsphinx/template_registry.py, tests/test_template_registry.py</files>
  <read_first>
    - `typsphinx/template_registry.py` lines 137-158 — `_violates_conf17()` in full, including the docstring paragraph explaining why one `commonpath` comparison covers both of D-07's rejected shapes and why absolute paths evaluate against their own parent. The new handler must extend that reasoning, not contradict it.
    - `typsphinx/builder.py` lines 905-956 — `_track_image()`'s established in-repo pattern for exactly this hazard: the `try:` around `path.relpath()` and its `except ValueError:` body commented `D-07: Windows cross-drive relpath() crash`. This is the precedent to follow and to cite by name.
    - `.planning/phases/53-template-registry-foundation/53-REVIEW.md` § CR-02 — the `ntpath` reproduction transcript and the suggested fix, including its comment text.
    - `.planning/phases/53-template-registry-foundation/53-CONTEXT.md` D-07 — "srcdir's siblings and absolute paths outside srcdir stay legal", and the measured `os.path.join(srcdir, "/abs/x.typ")` behaviour.
    - `tests/test_template_registry.py` lines 472-545 — the five existing CONF-17 tests, especially `test_conf17_absolute_template_path_outside_srcdir_resolves`, which is the same-drive control this task must leave green and unmodified.
  </read_first>
  <behavior>
    - Test A: with `os.path.commonpath` monkeypatched to raise `ValueError`, `_violates_conf17("/elsewhere/tpl.typ", "/some/srcdir")` returns `False` and propagates nothing.
    - Test B: with the same monkeypatch in place, `resolve_template_registry()` over a registry whose user key declares an absolute `template` path raises this module's `ExtensionError` (D-08's not-found failure), never a `ValueError` — i.e. the cross-drive crash path can no longer escape as an internal exception.
    - Test C (control, existing, unmodified): `test_conf17_absolute_template_path_outside_srcdir_resolves` still passes — the same-drive absolute path stays legal and takes no new branch.
  </behavior>
  <action>
Wrap `_violates_conf17()`'s single `os.path.commonpath([norm_srcdir, parent]) == parent` comparison
in a `try` / `except ValueError` that returns `False`. Compute `parent` and `norm_srcdir` before the
`try` so only the `commonpath` call is guarded — the narrowest possible scope, matching
`_track_image()`, which guards the `relpath()` call itself rather than the whole branch.

The `except` body carries a comment recording, in this order: that `os.path.commonpath()` raises
`ValueError` for two absolute paths on different Windows drives; that two paths on different drives
can never share an ancestor, so a cross-drive parent is definitionally not `srcdir` itself and not an
ancestor of `srcdir`, which is why `False` is the correct answer and not a silent permissive default;
and that this mirrors `builder.py`'s `_track_image()` cross-drive `ValueError` handling. Cite
`53-REVIEW.md` CR-02 and D-07 by name. Extend the function's docstring with one sentence stating that
a cross-drive absolute `template` stays LEGAL, so the reader does not later "tighten" it into a
rejection and silently withdraw D-07's guarantee.

Add tests A and B to `tests/test_template_registry.py` in the existing CONF-17 section, immediately
after `test_conf17_absolute_template_path_outside_srcdir_resolves`. Simulate the Windows failure
deterministically on POSIX rather than importing `ntpath`: `import os` in the test body and
`monkeypatch.setattr(os.path, "commonpath", <a function raising ValueError("Paths don't have the
same drive")>)`. `template_registry` binds `os` at import and resolves `os.path.commonpath` at call
time, so patching that attribute reaches the call under test, and `monkeypatch` reverts it. Each
test's docstring states that this is a deliberate platform-independent simulation of the
`windows-latest` CI lane's real behaviour, following this repo's own D-05 precedent of validating
Windows-shaped input identically on POSIX, and names `53-REVIEW.md` CR-02 as the source. Test A
asserts the return value is `False`. Test B declares one user key whose `template` is an absolute
path under a directory that does not exist, and asserts `pytest.raises(ExtensionError)` with a
message containing `does not exist` — proving the failure that surfaces is the module's own, not a
`ValueError`.

Change nothing else in `_violates_conf17()`: the `parent`/`norm_srcdir` derivation, the single
comparison, and the pure-path-arithmetic property D-09 depends on all stay exactly as they are.
  </action>
  <verify>
    <automated>uv run pytest tests/test_template_registry.py -v -k conf17</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_template_registry.py -q -k conf17` exits 0 with 7 passed (the 5 existing CONF-17 tests plus tests A and B).
    - `uv run python -c "import os, typsphinx.template_registry as m; orig=os.path.commonpath; os.path.commonpath=lambda p:(_ for _ in ()).throw(ValueError('Paths don\'t have the same drive')); r=m._violates_conf17('/elsewhere/tpl.typ','/some/srcdir'); os.path.commonpath=orig; assert r is False, r"` exits 0.
    - `uv run python -c "import inspect, typsphinx.template_registry as m; s=inspect.getsource(m._violates_conf17); assert 'except ValueError' in s and '_track_image' in s"` exits 0 — the guard exists and cites the in-repo precedent.
    - `uv run pytest tests/test_template_registry.py -q` exits 0 with 59 passed.
    - `git diff tests/test_template_registry.py` shows only additions in the CONF-17 section; no existing test body is modified.
  </acceptance_criteria>
  <done>A legal cross-drive absolute template path produces this module's clean `ExtensionError` contract or resolves normally, never a raw Python traceback, and the same-drive control still passes unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Non-str keys and non-dict definitions join the accumulated ExtensionError</name>
  <reversibility rating="costly">These two failure strings and the new total iteration key become the registry's published malformed-input contract from v0.9.0 onward; narrowing or reordering them later changes what every malformed `conf.py` emits, which D-03 publishes as byte-identical.</reversibility>
  <files>typsphinx/template_registry.py, tests/test_template_registry.py</files>
  <read_first>
    - `typsphinx/template_registry.py` lines 194-330 — `resolve_template_registry()` in full: the `all_keys` comprehension at 240 (already `isinstance(key, str)`-filtered), the `for key in sorted(declared.keys())` accumulate loop at 243, the `_validate_registry_key_shape()` dispatch at 244, the CONF-16 branch, the `definition = declared[key] or {}` line at 266, the single raise at 302-307, and the second build loop at 309-317.
    - `typsphinx/template_registry.py` lines 53-134 — `_KEY_SHAPE_REJECTION_CASES` and `_validate_registry_key_shape()`. Read the comment on `_WINDOWS_RESERVED_NAMES` explaining that silently adding a case is forbidden; the same constraint governs this task.
    - `.planning/phases/53-template-registry-foundation/53-REVIEW.md` § WR-02 and § WR-03 — both reproduction transcripts, both suggested fixes, and WR-02's note that `sorted()` itself raises `TypeError` on a heterogeneous key set before any validation runs.
    - `.planning/phases/53-template-registry-foundation/53-03-PLAN.md` frontmatter `must_haves.prohibitions` — the two locked prohibitions this task operates under. Read them verbatim before writing any code.
    - `tests/test_template_registry.py` lines 288-425 and 594-635 — the key-shape section's parametrized style, `test_key_shape_validator_exposes_exactly_seven_distinct_rejection_reasons`, and `test_three_independently_broken_keys_raise_once_order_independently`, which is the determinism test that must stay green unmodified.
    - `typsphinx/builder.py` lines 460-515 — `_collision_key()`'s signature and casefold body, to confirm it is only ever handed a `str`.
  </read_first>
  <behavior>
    - Test D: a registry declaring an `int` key raises `ExtensionError` whose message contains `is not a string` and the `repr` of the key; no `AttributeError` escapes.
    - Test E: a registry declaring a truthy non-`dict` definition (a bare string) raises `ExtensionError` whose message contains `must be a dict` and the `repr` of the value; no `AttributeError` escapes.
    - Test F (parametrized): a falsy definition — `None`, empty string, `0` — still normalizes and resolves without raising, and the resolved entry's `template`, `package` and `template_function` are all `None`.
    - Test G: a registry mixing an `int` key with a `str` key raises `ExtensionError`, not `TypeError`, and names both the type failure and any failure the `str` key independently has.
    - Test H: the same mixed-type malformed registry declared in two different insertion orders produces two byte-identical message strings.
    - Test I: a registry holding an `int` key plus two `str` keys differing only by case reports BOTH the type failure and the case-collision failure in one raise — proving `all_keys` still excludes the non-`str` key from the `_collision_key()` comparison rather than crashing it.
    - Test J (existing, unmodified): `test_key_shape_validator_exposes_exactly_seven_distinct_rejection_reasons` and `test_three_independently_broken_keys_raise_once_order_independently` both still pass.
  </behavior>
  <action>
Three edits inside `resolve_template_registry()`, all in the accumulate loop, all feeding the
existing `failures` list so the single raise at the end keeps its one message shape (D-03).

**Edit 1 — a total iteration key.** Replace the loop header's plain `sorted(declared.keys())` with a
`sorted()` call whose `key` argument is `lambda k: (not isinstance(k, str), k if isinstance(k, str)
else repr(k))`. Slot 0 partitions `str` keys before non-`str` keys; slot 1 then compares `str` to
`str` inside each partition, so the ordering is total across any mix of key types and can never
reach `sorted()`'s `TypeError`. Deliberately do NOT use `53-REVIEW.md`'s suggested `repr(k)` for both
partitions: `repr()` switches to double quotes for a string containing an apostrophe, which would
silently reorder such a key relative to today's plain `sorted()` and break D-03's published
byte-identity for any config that has one. Record that divergence and its reason in a comment on the
loop header, and state that the all-`str` ordering is byte-for-byte unchanged.

**Edit 2 — the non-`str` key type guard.** As the FIRST statement inside the loop body, before the
`_validate_registry_key_shape()` dispatch, append `f"registry key {key!r} is not a string"` to
`failures` and `continue` when `isinstance(key, str)` is False. This placement is load-bearing and
must be stated in a comment: `53-03-PLAN.md`'s prohibition freezes the key-shape denylist at exactly
the seven locked cases, so this is a TYPE GUARD on the loop, never an eighth entry in
`_KEY_SHAPE_REJECTION_CASES` and never a new branch inside `_validate_registry_key_shape()` — that
function keeps its `str`-only contract and is simply never reached with a non-`str`. Leave
`_KEY_SHAPE_REJECTION_CASES` and `_validate_registry_key_shape()` completely untouched.

**Edit 3 — the non-`dict` definition guard.** Replace the `definition = declared[key] or {}` line
with: read `declared[key]` raw; when it is truthy and `isinstance(..., dict)` is False, append
`f"registry key {key!r}'s definition must be a dict, got {definition!r}"` to `failures` and
`continue`; otherwise normalize with `or {}` exactly as today. The falsy path is unchanged, which is
what keeps `None` / empty string / `0` resolving as they do now.

**Do not touch the second loop** at lines 309-317. Add a one-line comment there recording that it is
reachable only when `failures` is empty, by which point every key is a `str` and every definition is
a `dict` or falsy, so a second guard would be redundant state that can drift from the first. Leave
`all_keys`'s `isinstance(key, str)` filter at line 240 exactly as it is — it is what keeps
`TypstBuilder._collision_key()` from ever being handed a non-`str`, and test I pins that.

Extend `resolve_template_registry()`'s docstring `Raises:` section with the two new failure classes,
naming `53-REVIEW.md` WR-02 and WR-03, and add one sentence to the body prose stating that the
iteration key is total across mixed key types so a heterogeneous registry produces this module's own
error rather than `sorted()`'s `TypeError`.

Add tests D through I to `tests/test_template_registry.py` in a new section headed for plan 53-07,
placed after the existing Task 2 (CONF-15/CONF-17/D-08) section and before the Task 3 (CONF-14)
section, following the file's existing comment-banner convention. Parametrize test F over the three
falsy values. Test H must assert equality of the two full message strings, not a substring. Give
each test a docstring naming the `53-REVIEW.md` finding it closes and the anti-pattern row in
`53-VERIFICATION.md` it discharges. Modify no existing test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_template_registry.py -v && uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_template_registry.py -q` exits 0 with at least 67 passed (57 at HEAD, plus 2 from Task 1, plus tests D, E, G, H, I and F's three parametrized cases).
    - `uv run python -c "from typsphinx.template_registry import _KEY_SHAPE_REJECTION_CASES as C; assert len(C) == 7 and len(set(C)) == 7, C"` exits 0 — the denylist is still exactly seven distinct cases.
    - `uv run python -c "import inspect, typsphinx.template_registry as m; s=inspect.getsource(m._validate_registry_key_shape); assert 'isinstance' not in s, 'the type guard belongs in resolve_template_registry(), not the shape validator'"` exits 0.
    - Reproduce `53-REVIEW.md` WR-02 with `uv run pytest tests/test_template_registry.py -q -k non_str_registry_key` — exits 0, and the test asserts the raised type is `ExtensionError` with `is not a string` in the message. (The reviewer's own one-liner, `resolve_template_registry(config, "/tmp")` over `{42: {}}`, must no longer raise `AttributeError`.)
    - Reproduce `53-REVIEW.md` WR-03 with `uv run pytest tests/test_template_registry.py -q -k non_dict_definition` — exits 0, and the test asserts the raised type is `ExtensionError` with `must be a dict` in the message.
    - Reproduce the heterogeneous-key hazard with `uv run pytest tests/test_template_registry.py -q -k mixed_type_key` — exits 0, and the tests assert the raised type is `ExtensionError`, never `TypeError`, and that the two insertion orders produce equal message strings.
    - `uv run pytest tests/test_template_registry.py -q -k "exactly_seven or order_independently"` exits 0 and `git diff tests/test_template_registry.py` shows no modification inside either of those two test bodies.
    - `uv run pytest tests/ -q` reports `0 failed`.
  </acceptance_criteria>
  <done>Every malformed `typst_document_templates` shape the verifier and reviewer reproduced — a non-`str` key, a truthy non-`dict` definition, and a heterogeneous key set — produces one clean accumulated `ExtensionError` naming the specific reason, with the seven-case denylist and D-03's message determinism both intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` `typst_document_templates` → `resolve_template_registry()` | Sphinx registers this value as `[dict]`, so it validates only the outer container. Keys and definition values arrive with arbitrary Python types. |
| declared `template` string → `os.path` arithmetic | A user-authored path string reaches `abspath`/`dirname`/`normpath`/`commonpath`. CONF-17 decides, from that arithmetic alone, whether the value would make Phase 54 copy the source tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-07-01 | Denial of Service | `_violates_conf17()` | medium | mitigate | An unhandled `ValueError` from `os.path.commonpath()` aborts the whole build with an internal traceback on a config D-07 documents as legal, on the `windows-latest` lane only. Mitigated by the narrow `except ValueError: return False` guard plus a POSIX-deterministic monkeypatch test, so the crash path is closed and covered without depending on which OS runs the suite. |
| T-53-07-02 | Denial of Service | `resolve_template_registry()` accumulate loop | medium | mitigate | A non-`str` key, a truthy non-`dict` definition, or a heterogeneous key set aborts the build with `AttributeError`/`TypeError` instead of the module's clean contract. Mitigated by two type guards and a total iteration key, all feeding the existing single accumulated raise. |
| T-53-07-03 | Information disclosure | new `ExtensionError` message text | low | accept | Both new messages embed the offending key/value via `repr()`. Those are values the user authored in their own `conf.py`; naming them is required by SC#3's "a message naming the specific reason". No filesystem paths or environment values beyond what the user declared are added. |
| T-53-07-04 | Elevation of privilege | registry key reaching a directory path | medium | transfer | A registry key becomes a directory name in Phase 54, where `mkdir`/`copytree` first exist. This phase's mitigation is the frozen seven-case single-path-segment denylist; the drive-qualified shape (`53-REVIEW.md` CR-01) is a KNOWN accepted gap, locked as a Deferred Idea in `53-CONTEXT.md` and transferred to the phase that creates the write site, where the destination-collision check belongs alongside it. |
| T-53-07-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan. Zero new runtime dependencies is a standing milestone invariant; only stdlib `os` is used, already imported. |

Canon referral: path traversal and directory-escape via a user-supplied key are canon security
ground owned by `/gsd-secure-phase` and by Phase 54's bundle-write threat model (T-53-07-04's
transfer target); not minted as a bespoke prohibition here.
</threat_model>

<verification>
1. `uv run pytest tests/test_template_registry.py -v` — all pass, including the 57 pre-existing ones
   unmodified.
2. `uv run pytest tests/ -q` — `0 failed`.
3. The three inline `uv run python -c` reproductions from `53-REVIEW.md` CR-02 / WR-02 / WR-03 each
   now produce this module's `ExtensionError` (or `False`), never `AttributeError`, `TypeError`, or
   `ValueError`.
4. `_KEY_SHAPE_REJECTION_CASES` still has exactly seven distinct entries.
</verification>

<success_criteria>
- Both ⚠ WARNING rows in `53-VERIFICATION.md`'s Anti-Patterns table (`_violates_conf17()`'s
  cross-drive crash; the raw `AttributeError` pair) are closed, and `53-REVIEW.md`'s CR-02, WR-02 and
  WR-03 are each discharged with a named test.
- SC#3's framing sentence holds over the module's whole input surface: every malformed registry stops
  the build with a message naming the specific reason.
- The two locked prohibitions hold: the denylist is still exactly seven cases, CR-01 is unimplemented,
  and no existing test was weakened.
</success_criteria>

## Artifacts this phase produces

**Changed production behaviour (no signature changes)**

- `typsphinx/template_registry.py::_violates_conf17(template_abs_path, srcdir) -> bool` — gains
  `except ValueError: return False` around the `os.path.commonpath()` call. Same signature; a
  cross-drive absolute path now returns `False` instead of propagating `ValueError`.
- `typsphinx/template_registry.py::resolve_template_registry(config, srcdir) -> Dict[str, TemplateRegistryEntry]`
  — same signature. Gains a total `sorted()` key over mixed-type keys, a non-`str` key type guard, and
  a non-`dict` definition guard. Two new accumulated failure strings:
  `registry key {key!r} is not a string` and
  `registry key {key!r}'s definition must be a dict, got {definition!r}`. The summary prefix
  `typst_document_templates: N invalid definition(s):` is unchanged.

**Unchanged, deliberately**

- `_KEY_SHAPE_REJECTION_CASES` — still exactly seven entries.
- `_validate_registry_key_shape(key, other_keys)` — untouched; keeps its `str`-only contract.
- `resolve_registry_key(registry, entry)` — untouched.
- `TemplateRegistryEntry` — untouched.
- `TypstBuilder._validate_output_path_collisions()`'s `typst: N output path collision(s):` text.

**New test names** — `tests/test_template_registry.py`

- `test_conf17_cross_drive_commonpath_valueerror_is_not_a_violation`
- `test_conf17_cross_drive_valueerror_surfaces_as_extension_error_not_valueerror`
- `test_non_str_registry_key_raises_extension_error_not_attributeerror`
- `test_non_dict_definition_raises_extension_error_not_attributeerror`
- `test_falsy_definition_still_normalizes_and_resolves` (parametrized over `None`, `""`, `0`)
- `test_mixed_type_keys_raise_extension_error_not_sorted_typeerror`
- `test_mixed_type_key_message_is_byte_identical_across_insertion_orders`
- `test_non_str_key_is_excluded_from_case_collision_comparison`

## Spec-less probe fallback — edges authored here (§A/§C)

The full 15-row disposition table lives in `53-06-PLAN.md`. Two of those rows are authored into THIS
plan's `must_haves.truths` because this plan's changes re-open or extend them:

| # | Req / category | Authored truth |
|---|---|---|
| 3 | TPL-01 / ordering | "D-03's determinism survives the new ordering ... including a mixed-type key set, and the existing all-`str` ordering is unchanged, so `test_three_independently_broken_keys_raise_once_order_independently` passes UNMODIFIED." Verification: explicit (tests G, H, J). |
| 15 | CONF-18 / encoding | "A non-`str` key never reaches `TypstBuilder._collision_key()`: `all_keys` stays `isinstance(key, str)`-filtered ... and a registry mixing a non-`str` key with two case-colliding `str` keys reports BOTH failures in the one accumulated raise." Verification: explicit (test I). |

<output>
Create `.planning/phases/53-template-registry-foundation/53-07-SUMMARY.md` when done
</output>
