# 45.1 Plan 06 — Contract Enumeration and Closeout Evidence

This file is the phase's single evidence record for SC#2, SC#5 and SC#6, plus the SC#3 RED basis
for `tests/test_documented_params_contract_gate.py`. Every claim below carries a command line, an
exit code, and pasted real output dated to this closeout session — none of it is inherited from an
earlier plan's SUMMARY (T-45.1-13).

## Task 1 — SC#3 RED basis: the documented-contract gate, pre-45.1-01

**Pre-45.1-01 commit SHA:** `e6d19cfe4c078bf691877090cf6f1b0bd09ee5c0` (`docs(45.1): create phase
plan` — the parent of `75ed1d4`, plan 45.1-01's first code commit). Resolved via
`git rev-parse 75ed1d4^` against this repository's own history; confirmed reachable and checked
out cleanly into a detached `git worktree add --detach` at recording time.

**Method:** the fixture files this plan adds
(`tests/fixtures/documented_params_contract_gate/{conf.py,index.rst,chapter.rst,
_templates/documented.typ}`) do not exist at the pre-45.1-01 SHA — they are new in this plan. The
RED proof is therefore taken by pointing a **pre-fix `typsphinx` package** (checked out at the
recorded SHA, in its own detached worktree with its own `uv sync --extra dev` venv, per this
repository's worktree-isolated-execution convention) at the **same, real fixture files** the
post-fix gate module builds against. This is not a reconstruction from the post-fix emitted
output — it is a real `sphinx-build` run against the pre-fix code.

**Sandbox note:** the empty-string `sys.path[0]` entry (CWD, when invoking `python -c`/`-m`)
resolves ahead of the PEP-660 editable finder's `sys.meta_path` entry, so the build command must be
run with the detached worktree as the process's CWD -- otherwise `import typsphinx` silently
resolves back to whichever checkout happens to be the caller's own working directory, defeating the
whole point of the detached checkout. Confirmed empirically before trusting the result below.

### Command (source emission, no compile needed to see the RED axis)

```bash
cd <detached-worktree-at-e6d19cf>
.venv/bin/python -m sphinx -b typst \
  tests/fixtures/documented_params_contract_gate \
  <tmp-build-dir>
```

### Result

Exit code: **0** (build succeeded).

### Verbatim emitted `#show:` region (pre-fix)

```typst
#show: project.with(
  title: "Documented Params Contract Gate",
  authors: ("Gate Fixture Author",),
  date: "1.0",
  papersize: "a4",
  fontsize: 11pt,
  toctree_maxdepth: 3,
  toctree_numbered: true,
  toctree_caption: "Documented Contents",
)
```

**Eight named arguments**, not nine — `lang` is absent. `language = "ja"` is set in the fixture's
`conf.py`, and `typst_template = "_templates/documented.typ"` is an explicit custom template, so at
this SHA `TemplateEngine.uses_bundled_default_template()` still carries the pre-D-I
`resolve_template().source == "default"` check (CONF-07/D-06's original shape) and returns `False`
for an explicit-template route. `writer.py`'s `auto_lang` block therefore never fires, and `lang`
never reaches the emitted call — even though the fixture's own template declares a `lang`
parameter with a default, so its absence does not abort the compile.

### Also measured: the compile axis (confirms it is NOT the RED axis)

```bash
.venv/bin/python -m sphinx -b typstpdf \
  tests/fixtures/documented_params_contract_gate \
  <tmp-build-dir>
```

Exit code: **0**. `index.pdf` generated (28,932 bytes). The pre-fix emission is a **subset** of the
nine published parameters (eight of nine, missing only `lang`), and every one of the fixture's own
template parameters carries a default (`lang: "en"` on `documented.typ`'s own signature), so the
compile succeeds regardless of whether `lang` is passed. **This is why the compile-success assertion
in `tests/test_documented_params_contract_gate.py` is a non-regression control, not a RED-proved
gate** — a compile-failure RED is not available for this specific fixture, and the module's
docstring says so explicitly rather than overclaiming it.

### RED verdict

`tests/test_documented_params_contract_gate.py::TestDocumentedParamsContractGate::
test_emitted_key_set_equals_published_key_set_exactly` is the RED-proved assertion: it asserts the
emitted named-argument key set equals `{title, authors, date, papersize, fontsize, lang,
toctree_maxdepth, toctree_numbered, toctree_caption}` (nine names, set equality both directions).
Against the pre-45.1-01 tree the emitted set is only eight of those nine (missing `lang`), so this
specific assertion is genuinely **RED** at that SHA and turns **GREEN** only once CONF-12/D-I's
widened `uses_bundled_default_template()` lands (plan 45.1-03). The compile-success assertion
(`test_build_succeeds_and_produces_valid_pdf`) is GREEN at both SHAs and is documented in the module
as a non-regression control, per D-K's instruction to state the axis honestly rather than
transcribe a post-fix-only observation into a claimed RED proof.
