---
phase: 45.1-custom-template-parameter-contract-correction
plan: 04
type: execute
wave: 3
depends_on: ["45.1-01", "45.1-02", "45.1-03"]
files_modified:
  - typsphinx/__init__.py
  - typsphinx/writer.py
  - typsphinx/builder.py
  - typsphinx/template_engine.py
  - tests/fixtures/package_only_config_gate/conf.py
  - examples/charged-ieee/approach1/conf.py
  - examples/charged-ieee/README.md
  - tests/test_entry_metadata_precedence.py
  - tests/test_template_engine.py
  - tests/test_params_authors_writers.py
  - tests/test_authors_pipeline_stage_gate.py
  - tests/test_package_only_config_gate.py
  - tests/test_typst_elements_pass_through_gate.py
  - tests/test_entry_metadata_route_uniformity.py
  - tests/test_examples_charged_ieee_gate.py
autonomous: false
requirements: [CONF-10]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-F: the config value is removed outright — its registration in typsphinx/__init__.py is gone, and no file under typsphinx/, tests/, docs/source/ or examples/ references it except as history in CHANGELOG.md"
    - "D-F: the removal lands as ONE coherent unit — the registration, the TemplateEngine constructor parameter, the attribute, the seed block in map_parameters(), and both call-site kwargs move together, because dropping the registration while leaving a config read in place would silently pass None and mask a stale reference"
    - "D-F: examples/charged-ieee/approach1/conf.py, which presents the removed value as Recommended, is migrated onto that file's already-present typst_template_function dict and still builds"
    - "D-F: the fixture tests/fixtures/package_only_config_gate/conf.py keeps its author-shape coverage by moving its author data onto typst_template_function params rather than deleting it, so the BUG-C guarantee (a native Typst array of dictionaries, never a pre-rendered quoted string) survives"
    - "CONF-10 / edge: concurrency — a partial landing guarantees nothing, so completion is defined as a single state in which the repository-wide reference count is zero AND the full pytest suite is green in the same run; there is no intermediate state this plan may commit"
    - "SC#8: the published documentation no longer tells a reader to prefer a route that, under CONF-11, would silently drop the other parameters — the prose surfaces are plan 45.1-05's work and this plan removes only the config, its code, its tests and its shipped sample"
  prohibitions:
    - "Do NOT soften the removal into a deprecation warning, a shim that reads the value and warns, or a compatibility alias — the owner considered and overrode the published forward-removal notice promising a major release, and a shim would silently reinstate the behaviour the notice was about"
    - "Do NOT delete a test module wholesale to make the suite green — where a module's premise is genuinely removed, re-derive the coverage it carried onto the surviving route and say so in the module docstring"
  artifacts:
    - path: "typsphinx/__init__.py"
      provides: "The Sphinx config registration block, with the removed value's add_config_value call gone"
    - path: "typsphinx/template_engine.py"
      provides: "TemplateEngine without the author-details constructor parameter, attribute, or seed block"
    - path: "examples/charged-ieee/approach1/conf.py"
      provides: "The shipped sample migrated onto typst_template_function params"
      contains: "typst_template_function"
    - path: "tests/fixtures/package_only_config_gate/conf.py"
      provides: "The package-route gate fixture with its author data on the params route"
      contains: "params"
  key_links:
    - from: "typsphinx/__init__.py"
      to: "typsphinx/writer.py and typsphinx/builder.py TemplateEngine construction"
      via: "the config value read by getattr at both construction sites; removing the registration without removing both reads passes None silently"
      pattern: "TemplateEngine("
    - from: "examples/charged-ieee/approach1/conf.py"
      to: "tests/test_examples_charged_ieee_gate.py"
      via: "the gate that builds the shipped sample and would go red on an unmigrated author route"
      pattern: "charged-ieee"
---

<objective>
Remove the `typst_authors` config value (CONF-10, D-F) across every surface in this repository in a
single coherent landing, and migrate the two configurations that depend on it onto the
`typst_template_function` `params` route that CONF-11 has just made an explicit, self-contained
declaration.

Purpose: typsphinx bills itself as a LaTeX alternative and should follow `latex_documents`, which
has no facility for supplying an author as a dictionary. Rich author structure now belongs on the
`params` route. The owner reaffirmed this after the concern was stated that Phase 44.2 published a
forward-removal notice promising a future major release and that v0.7.1 is a bug-fix round.

Output: four production files, one fixture, one shipped sample and its README, and seven test
modules, all moved in lockstep, with the repository-wide reference count at zero.
</objective>

## Artifacts this phase produces

**Removed by this plan**
- The `app.add_config_value(...)` registration for the author-details config value, and its preceding comment, in `typsphinx/__init__.py`
- The `typst_authors` constructor parameter, its docstring line, and the `self.typst_authors` attribute in `typsphinx/template_engine.py`
- The author-details seed block in `TemplateEngine.map_parameters()`, and the W1 references in the surrounding rationale comment
- The `typst_authors=getattr(config, ...)` kwarg at the `TemplateEngine(...)` construction site in `typsphinx/writer.py` and in `typsphinx/builder.py`
- Every reference in the seven affected test modules and in `tests/fixtures/package_only_config_gate/conf.py`
- The "Recommended" framing of the removed route in `examples/charged-ieee/approach1/conf.py` and `examples/charged-ieee/README.md`

**Added by this plan**
- Nothing new. Author data moves onto the existing `typst_template_function["params"]` route in the fixture and the shipped sample; no new symbol, file or config key is created.

**Retained deliberately**
- Historical mentions in `CHANGELOG.md` and in `.planning/` records — those are history, not references

<!-- planner-discipline-allow: typst_authors -->

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-PATTERNS.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the CONF-10 one-way door before the public config value disappears</name>
  <decision>Remove the author-details config value outright in this phase, or halt.</decision>
  <context>
    CONTEXT.md rates D-F **one-way**: a public config value disappears. Restoring it means re-adding
    the registration, the seed, the precedence rule and its whole test matrix — most of which is
    Phase 44.2's own work from this same milestone.

    Two facts the owner already weighed and overrode, restated here so the door is walked through
    with them in view: Phase 44.2 published a forward-removal notice saying the value is "slated for
    removal in a future **major** release", and v0.7.1 is a bug-fix round whose next phase is
    release prep. The owner reaffirmed the decision after this was stated, and REQUIREMENTS.md's
    CONF-10 entry records the override in full.

    Measured blast radius at discussion time (2026-08-10): 14 implementation sites, 71 test sites
    across 7 files, 1 fixture, 13 documentation sites, and one shipped sample that presents the
    value as "Recommended". Before answering, the executor should have re-run the census — a
    repository-wide reference count taken now, excluding `.planning/`, `_build/` and `.venv/` — and
    stated it in this checkpoint, so the door is confirmed against today's tree rather than against
    the discussion record.
  </context>
  <options>
    <option id="option-a">
      <name>Proceed with the removal as locked</name>
      <pros>Delivers CONF-10 / SC#8; aligns the config surface with `latex_documents`; removes a route that, under CONF-11, would silently drop the other parameters for anyone who migrated to it for `authors` alone.</pros>
      <cons>Lands earlier than the published forward-removal notice promised; a fifth user-visible change inside a patch release; a large, one-shot test-suite edit.</cons>
    </option>
    <option id="option-b">
      <name>Halt — today's census materially differs from the discussion record</name>
      <pros>Avoids a lockstep removal sized against stale numbers.</pros>
      <cons>Blocks CONF-10 and SC#8, and leaves the harmful migration advice in `configuration.rst` pointing at a route CONF-11 has just changed the meaning of.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the config value from production, the gate fixture, and the shipped sample</name>
  <reversibility rating="one-way">A public config value disappears; restoring it means re-adding the registration, the constructor parameter, the seed, the precedence rule and its entire test matrix.</reversibility>
  <files>typsphinx/__init__.py, typsphinx/writer.py, typsphinx/builder.py, typsphinx/template_engine.py, tests/fixtures/package_only_config_gate/conf.py, examples/charged-ieee/approach1/conf.py, examples/charged-ieee/README.md</files>
  <read_first>
    - `typsphinx/__init__.py` lines 40-62 — the `app.add_config_value(...)` block; the sibling registrations are the template for what the block should look like afterwards
    - `typsphinx/template_engine.py` lines 202-256 (constructor signature, docstring, attribute assignment) and 384-532 (`map_parameters()`, especially the long W1/W2/W3 rationale comment at 416-485 and the seed block at 481-485)
    - `typsphinx/writer.py` lines 247-256 — the `TemplateEngine(...)` construction site
    - `typsphinx/builder.py` lines 620-635 — the structurally identical construction site
    - `tests/fixtures/package_only_config_gate/conf.py` in full — its BUG-C author dict, and its `typst_template_function["params"]` block, which already carries `title`, `abstract`, `index-terms` and `paper-size`
    - `examples/charged-ieee/approach1/conf.py` in full — the "Recommended" framing and the `typst_template_function` dict already present in the file
    - `examples/charged-ieee/README.md` lines around 32 and 152
    - `tests/fixtures/package_only_config_gate/conf.py`'s sibling gate module `tests/test_package_only_config_gate.py` for what BUG-C asserts about the emitted shape
  </read_first>
  <behavior>
    - Constructing `TemplateEngine` with the removed keyword raises `TypeError` — the parameter is gone from the signature, not merely ignored
    - `map_parameters()` returns the same dict it does today for every input that did not set the removed value
    - The `package_only_config_gate` fixture's emitted call still carries `authors` as a native Typst array of dictionaries with `name`/`department`/`organization`/`location`/`email` keys, sourced now from the params route
    - `examples/charged-ieee/approach1` still builds to a PDF and its emitted call still carries the same author structure
  </behavior>
  <action>
    Delete the four production sites in one edit pass, in this order so the tree is never left with
    a live read against a removed registration: the constructor parameter, its docstring line and
    the attribute assignment in `typsphinx/template_engine.py`; the seed block in
    `map_parameters()`; the kwarg at the `TemplateEngine(...)` call in `typsphinx/writer.py`; the
    kwarg at the structurally identical call in `typsphinx/builder.py`; and finally the
    `app.add_config_value(...)` registration and its preceding comment in `typsphinx/__init__.py`.

    `map_parameters()`'s W1/W2/W3 rationale comment at lines 416-485 documents three writers of the
    `authors` key. W1 is the seed being deleted. Do not leave the comment describing a writer that
    no longer exists — re-derive it over the two surviving writers (the mapping loop and the
    back-fill), keep the explicit `Dict[str, Any]` annotation on `params` and the note explaining
    why it is needed, and record that the third writer is now `render()`'s exclusive `params`
    branch from D-B rather than a seed here.

    Migrate `tests/fixtures/package_only_config_gate/conf.py` rather than stripping it. Its author
    dict is BUG-C's whole subject: it proves the value reaches the output as a native Typst array of
    dictionaries and never as a pre-rendered quoted string. Move that same structure into the
    fixture's existing `typst_template_function["params"]` block as an `authors` entry — a list
    containing one dict with `name` plus the department/organization/location/email keys the fixture
    already carries — and update the fixture's header comment so BUG-C's description names the
    params route. Note in that comment that under D-B the fixture's
    `typst_template_mapping = {"project": "title"}` no longer reaches the emitted call at all.

    Migrate `examples/charged-ieee/approach1/conf.py` the same way, into the
    `typst_template_function["params"]` dict already present in that file, and rewrite the two
    section comments that present the removed route as "Recommended" and "Approach 1" so they name
    the params route instead. Keep the file's existing Japanese comments and its note about why
    there is no citation-source parameter. Update the two matching spots in
    `examples/charged-ieee/README.md`.

    <!-- planner-discipline-allow: typst_authors -->
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && test "$(grep -rn 'typst_authors' typsphinx/ examples/ | wc -l)" -eq 0 && uv run pytest tests/test_examples_charged_ieee_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'typst_authors' typsphinx/ | wc -l` returns `0`
    - `grep -rn 'typst_authors' examples/ | wc -l` returns `0`
    - `grep -rn 'typst_authors' tests/fixtures/ | wc -l` returns `0`
    - `uv run python -c "import inspect; from typsphinx.template_engine import TemplateEngine; print('typst_authors' in inspect.signature(TemplateEngine.__init__).parameters)"` prints `False`
    - `uv run python -c "from sphinx.application import Sphinx" && uv run pytest tests/test_examples_charged_ieee_gate.py -q` exits 0
    - A fresh `-b typst` build of `examples/charged-ieee/approach1` emits an `authors:` named argument formatted as a Typst array of dictionaries containing the `department`, `organization`, `location` and `email` keys — not a quoted string
    - `examples/charged-ieee/approach1/conf.py` contains no section header describing the removed route as recommended (`! grep -qi 'Recommended' examples/charged-ieee/approach1/conf.py` or, if the word survives for another reason, the surviving occurrence names the params route)
  </acceptance_criteria>
  <done>The config value is gone from every production file, the gate fixture keeps its BUG-C coverage on the params route, and the shipped sample builds unchanged in output shape.</done>
</task>

<task type="auto">
  <name>Task 3: Move the seven test modules onto the surviving route</name>
  <files>tests/test_entry_metadata_precedence.py, tests/test_template_engine.py, tests/test_params_authors_writers.py, tests/test_authors_pipeline_stage_gate.py, tests/test_package_only_config_gate.py, tests/test_typst_elements_pass_through_gate.py, tests/test_entry_metadata_route_uniformity.py</files>
  <read_first>
    - Each of the seven modules, reading only the regions the census identifies: `tests/test_entry_metadata_precedence.py` (37 sites), `tests/test_template_engine.py` (13), `tests/test_params_authors_writers.py` (7), `tests/test_authors_pipeline_stage_gate.py` (7), `tests/test_package_only_config_gate.py` (6), `tests/test_typst_elements_pass_through_gate.py` (1, docstring prose), `tests/test_entry_metadata_route_uniformity.py` (1, docstring prose)
    - `tests/test_authors_pipeline_stage_gate.py`'s module docstring and its `ast` walk, as already re-derived by plan 45.1-01 — this task removes its W1 stage, not the module
    - `tests/test_params_authors_writers.py` — the generated writer matrix, whose row space includes the seed
    - `typsphinx/template_engine.py` `map_parameters()` as changed by Task 2
  </read_first>
  <action>
    Re-derive, do not amputate. Run the census first with
    `grep -rn 'typst_authors' tests/` and work the list top to bottom, so the removal is complete
    rather than sampled.

    `tests/test_entry_metadata_precedence.py` carries the largest share. Its Group 2 cases encode
    the seed-survival rule — "the seed survives iff no mapping entry targets `authors` with a source
    key the build supplies". With the seed gone that rule has no subject. Replace each such case
    with the assertion that now carries the same guarantee: the mapping's own target-key rule still
    decides what reaches `authors` when no `params` is declared, and a declared `params["authors"]`
    is authoritative when one is. Keep the module's existing convention of discrete,
    individually-named test functions rather than collapsing rows into a parametrize.

    `tests/test_params_authors_writers.py`'s generated matrix loses the seed dimension; regenerate
    the matrix over the surviving writers and keep its partition assertion.

    `tests/test_authors_pipeline_stage_gate.py` keeps its `ast` walk and its no-silent-drop
    partition. Remove the W1 stage from the enumerated set and update the module docstring, which
    currently describes the pipeline in terms of the seed.

    `tests/test_package_only_config_gate.py`'s BUG-C tests keep their names and their meaning but
    now read the author structure off the fixture's params route, matching Task 2's fixture
    migration. `test_removing_authors_config_changes_output` — a config-to-output difference test
    whose independent variable was the removed value — needs a new independent variable: remove the
    `authors` entry from the fixture's params in the comparison build instead, so the test still
    proves that a config value changes the emitted output.

    The two single-site modules carry the name only in docstring prose. Rewrite those sentences to
    describe the surviving route; do not leave a docstring naming a config value that no longer
    exists.

    Finish with the four project gates. In a worktree run everything under `uv run`; in the main
    tree a human runs pytest as `.venv/bin/python -m pytest`.

    <!-- planner-discipline-allow: typst_authors -->
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && test "$(grep -rn 'typst_authors' typsphinx/ tests/ docs/source/ examples/ | wc -l)" -eq 0 && uv run pytest -q && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'typst_authors' typsphinx/ tests/ docs/source/ examples/ | wc -l` returns `0`
    - `uv run pytest -q` exits 0 with zero failures and zero errors, in the same run as the count above
    - All seven modules still exist as files (`for f in tests/test_entry_metadata_precedence.py tests/test_template_engine.py tests/test_params_authors_writers.py tests/test_authors_pipeline_stage_gate.py tests/test_package_only_config_gate.py tests/test_typst_elements_pass_through_gate.py tests/test_entry_metadata_route_uniformity.py; do test -s "$f" || exit 1; done`)
    - `uv run pytest tests/test_authors_pipeline_stage_gate.py -q` exits 0 and the module's partition assertion still runs
    - `uv run pytest tests/test_package_only_config_gate.py -q` exits 0 and its BUG-C tests still assert a Typst array of dictionaries
    - `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` all exit 0
    - The plan SUMMARY states the test-function count before and after, and justifies any net decrease case by case
  </acceptance_criteria>
  <done>Every reference is gone from the source, test and example trees in one landing, and no coverage was dropped rather than moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sphinx config registry → `TemplateEngine` construction | A registration removed while a `getattr` read survives silently yields `None` and masks the stale reference instead of failing |
| shipped sample → user's first configuration | `examples/charged-ieee/approach1/conf.py` is copy-paste source material; a sample naming a removed config value teaches a broken configuration |
| published removal notice → this release | The forward-removal notice promised a major release; this lands in a patch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45.1-07 | Tampering | a partial landing that removes the registration but leaves a config read | high | mitigate | The read would return `None`, the seed would never fire, and nothing would fail loudly — a silent behaviour change disguised as a clean removal. Mitigated by landing all four production sites in Task 2 in one edit pass, and by a completion gate that requires a repository-wide reference count of zero AND a green full suite in the same command. |
| T-45.1-08 | Repudiation | the published forward-removal notice promising a major release | medium | transfer | Transferred to the release record: plan 45.1-06 records this as one of the five CHANGELOG callouts Phase 46 owes, and REQUIREMENTS.md's CONF-10 entry already states the override and the reasoning. Not silently absorbed. |
| T-45.1-09 | Tampering | author data moved into `params` and emitted into Typst source | low | accept | The same values already reached the same serializer through the seed; the route changes, the escaping does not. The fixture's BUG-C assertion continues to prove the value is emitted as a native array of dictionaries rather than a quoted string. |
| T-45.1-15 | Information disclosure | example author data (names, organizations, email addresses) in a shipped sample | low | accept | The values in `examples/charged-ieee/approach1/conf.py` are already-published fictional sample data; the migration moves them without introducing real contact information. |
| T-45.1-SC | Tampering | third-party package fetch during the example build | low | accept | No npm/pip/cargo install task exists in this plan. The example build fetches the already-pinned `@preview/charged-ieee:0.1.4`; the pin is unchanged. |
</threat_model>

<verification>
- `grep -rn 'typst_authors' typsphinx/ tests/ docs/source/ examples/ | wc -l` returns `0`
- `uv run pytest -q` exits 0 over the whole suite
- `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0
- `uv run pytest tests/test_examples_charged_ieee_gate.py -q` exits 0

<!-- planner-discipline-allow: typst_authors -->
</verification>

<success_criteria>
- SC#8's three clauses are each discharged: the registration is gone, no source file references the value, and the shipped sample is migrated and still builds
- No test module was deleted to reach green; every removed assertion has a named replacement
- The removal landed as one unit, with the zero count and the green suite proven by the same command
</success_criteria>

<output>
Create `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-04-SUMMARY.md` when done
</output>
