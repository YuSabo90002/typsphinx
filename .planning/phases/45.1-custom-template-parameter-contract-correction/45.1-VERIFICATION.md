---
phase: 45.1-custom-template-parameter-contract-correction
verified: 2026-08-10T17:55:00Z
status: passed
score: 10/10 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/10
  gaps_closed:
    - "Truth 2b: docs/source/user_guide/configuration.rst's 'Document Language' → 'Scope: default template only' subsection published the pre-CONF-12 rule, contradicting typsphinx/template_engine.py:353 and templates.rst:222-225 on the same behaviour."
  gaps_remaining: []
  regressions: []
---

# Phase 45.1: Custom-Template Parameter Contract Correction Verification Report

**Phase Goal:** A reader who writes a custom template from the published documentation gets a build
that works. Chosen shape: publish the measured default parameter set (DOC-13); make a declared
`typst_template_function["params"]` the complete parameter set (CONF-11); remove `typst_authors`
(CONF-10); extend the auto-derived `lang` to every non-package template route (CONF-12).

**Verified:** 2026-08-10T17:55:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plan 45.1-07)

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | SC#1 — a template declaring exactly the published params, built with a toctree + `typst_elements`, compiles via a real `-b typstpdf` build | ✓ VERIFIED | `tests/test_documented_params_contract_gate.py::TestDocumentedParamsContractGate::test_build_succeeds_and_produces_valid_pdf` — present in the 988-passed full-run this session |
| 2 | SC#2 — every parameter typsphinx can emit is in `templates.rst`, and every parameter `templates.rst` tells a reader to declare is one typsphinx actually passes (single-page enumeration) | ✓ VERIFIED | `45.1-CONTRACT-ENUMERATION.md` Task 2; `tests/test_documented_params_contract_gate.py::test_emitted_key_set_equals_published_key_set_exactly` |
| 2b | Phase-goal-level, cross-page reading — "the contract published in the docs matches what typsphinx actually passes" (DOC-13 requirement text, not page-scoped). **This is the gap plan 07 closed.** | ✓ VERIFIED | `docs/source/user_guide/configuration.rst:313-333` ("Scope: every non-package route") now states the D-I rule, independently re-read and confirmed byte-for-byte against `typsphinx/template_engine.py:353`'s `uses_bundled_default_template()` (`not self.typst_package`) and `templates.rst:222-225`. New permanent guard `tests/test_docs_contract_claims_gate.py` (8 tests) independently re-run this session: `8 passed in 0.05s`. Re-ran `.venv/bin/python -m pytest -q` over the whole suite: `988 passed, 1 skipped` (main-tree convention per `CLAUDE.md`, not an outer `uv run`) |
| 3 | SC#3 — a regression test locks the contract, RED-proved against the pre-fix commit before acceptance | ✓ VERIFIED | `45.1-CONTRACT-ENUMERATION.md` Task 1 (unchanged from prior verification, regression spot-check: file present, referenced test IDs collect and pass in this session's full run) |
| 4 | SC#4 — the breaking-change-on-parameter-addition consequence is recorded wherever the contract is documented | ✓ VERIFIED | `templates.rst:241` "Adding a template parameter is a **breaking change**..."; `configuration.rst`'s corrected section also states the parallel mandatory-`lang` consequence, naming the real `unexpected argument: lang` failure |
| 5 | SC#5 — repo's own docs build (`tox -e docs-pdf` equivalent) is clean; bundled-default route parameter set unchanged or justified | ✓ VERIFIED | Independently re-ran `sphinx -b typstpdf docs/source <tmp>` this session: exit succeeded, real `typsphinx.pdf` (2.37 MB) produced, only the three pre-existing `visit_toctree` docstring-indentation warnings — no new warning from the configuration.rst edit |
| 6 | SC#6 — full pytest + black/ruff/mypy green; every behaviour change maps to SC#7/8/9 | ✓ VERIFIED | Independently re-ran `.venv/bin/python -m pytest -q`: `988 passed, 1 skipped` (984 from plan 07's own closeout + 4 more collected between sessions — no regression; 0 failed). `45.1-07-SUMMARY.md` records black/mypy/ruff all clean this session |
| 7 | SC#7 (CONF-11) — a declared `params` (dict form) is the complete, exclusive parameter set on all three routes; `params: {}` passes nothing | ✓ VERIFIED | Regression spot-check: `grep -c 'all_params.update' typsphinx/template_engine.py` → `0`; exclusivity branch still present at `template_engine.py:693` |
| 8 | SC#8 (CONF-10) — `typst_authors` removed outright; `examples/charged-ieee/approach1/conf.py` migrated | ✓ VERIFIED | Regression spot-check: `grep -rn typst_authors typsphinx/ tests/ examples/ docs/source/` → 0 hits |
| 9 | SC#9 (CONF-12) — `lang` auto-derived on every non-package route; `typst_package` guard still holds; `docs/source/conf.py` workaround removed | ✓ VERIFIED | Regression spot-check: `template_engine.py:353` `uses_bundled_default_template()` still `return not self.typst_package`, docstring explicitly documents the D-I narrowing |
| 10 | SC#10 — CHANGELOG callout debt for Phase 46 recorded (5 total, per REQUIREMENTS.md's own updated count) | ✓ VERIFIED | `45.1-HANDOFF.md` Item 2; REQUIREMENTS.md's own CONF-10/11/12/DOC-13 entries each carry an "accepted CHANGELOG callout" annotation, unchanged by plan 07 |

**Score:** 10/10 truths verified — the one gap from the prior verification round (truth 2b) is closed
and independently re-confirmed, not merely re-read from the SUMMARY.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `typsphinx/template_engine.py` | `typst_template_params_specified` predicate + exclusive `render()` branch | ✓ VERIFIED | Regression-confirmed present and wired |
| `typsphinx/template_engine.py` `uses_bundled_default_template()` | Narrowed to `not self.typst_package` | ✓ VERIFIED | Regression-confirmed at `:353-376` |
| `typsphinx/__init__.py`, `writer.py`, `builder.py` | `typst_authors` registration/reads gone | ✓ VERIFIED | Regression-confirmed zero repo-wide references |
| `docs/source/user_guide/templates.rst` | Nine-parameter contract, per-parameter emission conditions, breaking-change consequence | ✓ VERIFIED | Unchanged since prior verification; still correct |
| `docs/source/user_guide/configuration.rst` | Precedence model on exclusivity (D-B/D-E); lang scope description accurate | ✓ VERIFIED (gap closed) | "Document Language" → "Scope: every non-package route" now states the D-I rule; `git show 46451a5` confirms the "Precedence and known limitation" sub-subsection is byte-unchanged (diff touches only the three named paragraphs) |
| `docs/source/examples/advanced.rst` | Corrected examples, build-verified | ✓ VERIFIED | Unchanged since prior verification |
| `tests/test_params_exclusivity_gate.py`, `tests/test_typst_lang_gate.py`, `tests/test_documented_params_contract_gate.py` | Permanent regression gates | ✓ VERIFIED | All present, collected and passing in this session's full run |
| `tests/test_docs_contract_claims_gate.py` | New permanent cross-page contract-claim guard: page-set closure, route-scope claim agreement, fail-first detector proof | ✓ VERIFIED | 477 lines, 8 tests across 3 classes, independently re-run this session: `8 passed in 0.05s`. Derives route truth from real `TemplateEngine(**kwargs).uses_bundled_default_template()` calls (`grep -c` confirms ≥1 reference), asserts by `==` set equality (no `issubset`/`issuperset`), RED-proves its own classifier against the verbatim pre-fix sentence |
| `.planning/phases/.../45.1-07-CROSS-PAGE-CONTRACT-SWEEP.md` | Per-page verdict table, directional conclusions, live RED/GREEN reintroduction proof | ✓ VERIFIED | Present; enumerates all 13 `docs/source/**/*.rst` pages, records live command output with exit codes for both the reintroduction test and the classifier fail-first proof |
| `.planning/REQUIREMENTS.md` | Traceability flipped to `Complete` for DOC-13/CONF-10/CONF-11/CONF-12 | ✓ VERIFIED | Independently re-confirmed: 4/4 `Complete` rows, 4/4 ticked checklist markers, coverage tally still 18 total/18 mapped/0 unmapped, REL-04/REL-06 rows byte-unchanged |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `TemplateEngine.__init__` | `TemplateEngine.render()` | `self.typst_template_params_specified` | ✓ WIRED | Regression-confirmed |
| `writer.py` `auto_lang` block | `template_engine.py` `uses_bundled_default_template()` | Single judgment call before deriving `lang` | ✓ WIRED | Regression-confirmed via passing `test_typst_lang_gate.py` |
| `templates.rst` "Standard Parameters" (`lang` bullet) | `configuration.rst` "Document Language" section | `:doc:`configuration`` cross-reference | ✓ WIRED (content now agrees) | Independently re-read both pages: `templates.rst:222-225` states "on every non-package route"; `configuration.rst:317-333` states the same D-I rule with the full route enumeration. No contradiction found |
| `tests/test_docs_contract_claims_gate.py` | `typsphinx/template_engine.py` `uses_bundled_default_template()` | Five real `TemplateEngine` constructions, including the `typst_package` + `typst_template` combination absent from every fixture | ✓ WIRED | `grep -c 'uses_bundled_default_template'` ≥ 1; tests pass |
| `tests/test_docs_contract_claims_gate.py` | `configuration.rst`, `templates.rst`, `examples/advanced.rst` | `REVIEWED_CLAIM_PAGES` closed-set assertion against a live `docs/source/**/*.rst` scan | ✓ WIRED | `TestContractClaimPageEnumerationIsClosed`'s 3 tests pass; `changelog.rst` correctly excluded with a reasoned, non-stale exclusion |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| CONF-11 | 45.1-01, 45.1-02, 45.1-07 (bookkeeping) | Declared `params` is the complete, exclusive set on all three routes | ✓ SATISFIED | Code regression-confirmed; `REQUIREMENTS.md:106` `[x]`, traceability row `Complete` |
| CONF-12 | 45.1-03, 45.1-07 (bookkeeping) | `lang` auto-derived on every non-package route | ✓ SATISFIED | Code regression-confirmed; documentation now agrees (gap closed); `REQUIREMENTS.md:122` `[x]`, traceability row `Complete` |
| CONF-10 | 45.1-04, 45.1-07 (bookkeeping) | `typst_authors` removed | ✓ SATISFIED | Regression-confirmed zero references; `REQUIREMENTS.md:137` `[x]`, traceability row `Complete` |
| DOC-13 | 45.1-05, 45.1-06, 45.1-07 | Published contract and shipped behaviour agree in both directions | ✓ SATISFIED | `templates.rst` accurate (12/12 build-verified examples, unchanged); `configuration.rst` now agrees (Task 1); permanent cross-page guard (Task 2) closes the recurrence hole across all claim-publishing pages, not `templates.rst` alone; `REQUIREMENTS.md:169` `[x]`, traceability row `Complete` |

**No orphaned requirements.** All four Phase 45.1 requirement IDs (DOC-13, CONF-10, CONF-11, CONF-12)
appear in at least one plan's `requirements:` frontmatter and in `.planning/REQUIREMENTS.md`'s
traceability table; the coverage tally (18 total / 18 mapped / 0 unmapped) is independently
re-confirmed unchanged.

### Anti-Patterns Found

None in `docs/source/user_guide/configuration.rst` or `tests/test_docs_contract_claims_gate.py`
(checked `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` and prose "coming soon"/"not yet implemented"
patterns — zero hits).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Four pre-fix literals fully retired from `configuration.rst` | 4x `grep -Fc` (each pre-fix sentence) | `0, 0, 0, 0` | ✓ PASS |
| D-I replacement text present | `grep -Fc 'Scope: every non-package route'` / `'unexpected argument: lang'` / '`Document Language`_' | `1, 1, 1` | ✓ PASS |
| New guard module collects and passes standalone | `.venv/bin/python -m pytest tests/test_docs_contract_claims_gate.py -q` | `8 passed in 0.05s` | ✓ PASS |
| Full suite, independently re-run this session | `.venv/bin/python -m pytest -q` (main-tree convention, no outer `uv run` per `CLAUDE.md`) | `988 passed, 1 skipped in 187.27s` | ✓ PASS |
| `docs-pdf` build, independently re-run this session | `sphinx -b typstpdf docs/source <tmp>` | Real `typsphinx.pdf` produced (2.37 MB), only 3 pre-existing `visit_toctree` warnings | ✓ PASS |
| `all_params.update` union fully removed | `grep -c 'all_params.update' typsphinx/template_engine.py` | `0` | ✓ PASS |
| `typst_authors` fully removed repo-wide | `grep -rn typst_authors typsphinx/ tests/ examples/ docs/source/` | no matches | ✓ PASS |
| `uses_bundled_default_template()` still narrowed correctly | `sed -n '353,376p' typsphinx/template_engine.py` | `return not self.typst_package` | ✓ PASS |
| REQUIREMENTS.md traceability flip | `grep -cE` (4x `Complete` rows, 4x ticked markers) | `4, 4` | ✓ PASS |

### Probe Execution

Not applicable — this phase has no `scripts/*/tests/probe-*.sh` convention; its gates are the
project's standing pytest/black/ruff/mypy/tox suite, independently re-run above.

### Human Verification Required

None. The prior gap (a documentation contradiction) and its closure are both fully verifiable by
direct text comparison, real builds, and a passing permanent test suite — none of it is a matter of
taste, visual judgment, or live runtime behaviour a human must observe.

### WR-01 Assessment (code review finding, not a phase gap)

`45.1-REVIEW.md` WR-01 reports that `examples/charged-ieee/approach1/conf.py` (the "Recommended"
charged-ieee sample) never includes `title` in its declared `params` dict, so the compiled example
PDF has no title. Assessed against this phase's must-haves: **this is a pre-existing
example-quality issue outside Phase 45.1's scope, not a phase-goal or requirement gap.**

- The build still **works** (compiles to a valid PDF) — the phase goal is "a reader ... gets a build
  that works," which this satisfies; WR-01 is a missing-content defect, not a compile abort, which is
  the failure category DOC-13/CONF-11/CONF-12 exist to eliminate.
- `git log -p` (per the review) confirms `title` was never in this file's `params` dict in any
  commit — it predates this phase entirely. Plan 04 touched this file only to migrate
  `typst_authors` onto the `params` route (CONF-10), which it did correctly; that migration is not
  what created the missing-title condition.
- `templates.rst`'s introductory "Configuration-Based Templates" sample (lines 38-51) is a partial
  illustrative snippet ("params": with three keys) — it does not claim completeness or assert
  `title` is included, so it makes no contract claim `test_docs_contract_claims_gate.py`'s
  route-scope axis or DOC-13's parameter-emission axis is about. DOC-13 is about whether the
  *emission rules* agree with the docs, not about whether every shipped example is fully
  featured.
- Recommended as follow-up work (not a Phase 45.1 gap): add `"title": project` to
  `examples/charged-ieee/approach1/conf.py` and `README.md`, and strengthen
  `test_examples_charged_ieee_gate.py` to assert a title reaches the show-rule region, per the
  review's own fix suggestion.

### Gaps Summary

None. The single gap from the prior verification round — `configuration.rst`'s stale pre-CONF-12
route-scope prose, and the single-page scoping of the SC#2 agreement proof that let it escape — is
closed by plan 45.1-07 and independently re-confirmed in this session:

1. **Prose corrected**, matched to the shipped predicate and to `templates.rst`, verified by direct
   re-reading, a real independent `docs-pdf`-equivalent build, and diff inspection confirming the
   `Precedence and known limitation` sub-subsection is untouched.
2. **Recurrence hole closed**: `tests/test_docs_contract_claims_gate.py` is a permanent, 8-test,
   never-skipping guard that (a) closes the claim-page set in both directions so a new
   claim-publishing page or a rotted exclusion cannot go unreviewed, (b) proves two-way set equality
   between every reviewed page's route-scope prose and `uses_bundled_default_template()`'s real
   output across five route configurations (including the CONF-12 adjacency edge no fixture covers),
   and (c) is itself RED-proved against the verbatim pre-fix sentence, so the detector is not a
   silent no-op. Independently re-run this session: `8 passed in 0.05s`.
3. **REQUIREMENTS.md bookkeeping flip** independently re-confirmed correct: DOC-13, CONF-10,
   CONF-11, CONF-12 all read `Complete` with matching ticked checklist markers; coverage tally
   unchanged at 18/18/0; `REL-04`/`REL-06` rows untouched.
4. **D-J honored**: `docs/source/_typst/custom_template.typ` remains unguarded by design; the new
   guard's only reference to it is inside the module docstring's fence statement, confirmed by
   `grep -n 'custom_template.typ' tests/test_docs_contract_claims_gate.py` returning exactly one hit
   (the docstring line), not an assertion.
5. **CONF-10's flagged `unclassified` edge-probe assumption accepted**: a pure config-value removal
   genuinely has no data-shape edge to resolve once the value does not exist; the removal itself is
   independently regression-confirmed (zero repo-wide references). This verifier does not challenge
   the planner's judgment on this row.
6. **WR-01 assessed and correctly scoped out** (see above) — a real, worthwhile follow-up, but not a
   Phase 45.1 must-have.

Full suite independently re-run this session: `988 passed, 1 skipped, 0 failed`. No regression
relative to plan 07's own closeout (984 passed at the time of that session; the difference is
consistent with normal test collection between sessions, not a failure).

---

_Verified: 2026-08-10T17:55:00Z_
_Verifier: Claude (gsd-verifier)_
