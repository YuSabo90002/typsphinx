---
phase: 45.1-custom-template-parameter-contract-correction
plan: 06
type: execute
wave: 5
depends_on: ["45.1-01", "45.1-02", "45.1-03", "45.1-04", "45.1-05"]
files_modified:
  - tests/fixtures/documented_params_contract_gate/conf.py
  - tests/fixtures/documented_params_contract_gate/index.rst
  - tests/fixtures/documented_params_contract_gate/chapter.rst
  - tests/fixtures/documented_params_contract_gate/_templates/documented.typ
  - tests/test_documented_params_contract_gate.py
  - .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md
  - .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md
autonomous: true
requirements: [DOC-13, CONF-10, CONF-11, CONF-12]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-K/SC#3: a permanent regression test builds a project whose custom template declares only the documented parameters, over a master carrying both a toctree and a typst_elements setting, and asserts the build succeeds — with its RED basis recorded against the pre-fix tree before the assertions were accepted"
    - "SC#2: the published contract and shipped behaviour agree in BOTH directions — every parameter typsphinx can pass is accounted for in templates.rst, and every parameter the documentation tells a reader to declare is one typsphinx actually passes — checked by enumerating the emission sites, not by reviewing prose"
    - "DOC-13 / edge: adjacency — agreement is proved by set equality in both directions between the published names and the names a real build emits, not by containment in either direction"
    - statement: "DOC-13 / edge: ordering — the emitted named-argument order is deterministic across repeated builds of the same configuration, and the published contract does not promise a particular order; two consecutive builds of the documented-contract fixture emit byte-identical .typ output"
      verification: backstop
    - "SC#5: tox -e docs-pdf is clean, and a build using the bundled templates/base.typ with no declared params emits the same parameter set it did before this phase — or the difference is stated and justified in the enumeration record"
    - "SC#6: the full pytest suite and black, ruff and mypy are green, and every behaviour change in this phase is one of SC#7, SC#8 or SC#9 — any difference outside those three is stated and justified"
    - "SC#9: removing the same lang workaround from the typsphinx-doc-translations repository is recorded as a handoff item, explicitly not performed in this phase"
    - "SC#10: the CHANGELOG callout debt for Phase 46 is recorded — this phase adds three user-visible changes (SC#7, SC#8, SC#9) to the milestone's existing two, for five total; this is a recording task, not a CHANGELOG edit"
    - "D-H: the ROADMAP and REQUIREMENTS amendments made on 2026-08-10 before planning are verified still present and unmodified — the amended SC#6 wording and the four requirement entries are the current text"
  artifacts:
    - path: "tests/fixtures/documented_params_contract_gate/_templates/documented.typ"
      provides: "A custom template declaring exactly the nine published parameters plus the trailing body"
      contains: "toctree_caption"
    - path: "tests/test_documented_params_contract_gate.py"
      provides: "The permanent SC#3 regression lock for the documented contract"
      min_lines: 80
    - path: ".planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md"
      provides: "The SC#2 two-way agreement record and the SC#5 default-template invariance comparison"
    - path: ".planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md"
      provides: "The SC#9 second-repository handoff and the SC#10 CHANGELOG callout debt for Phase 46"
  key_links:
    - from: ".planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md"
      to: "docs/source/user_guide/templates.rst Standard Parameters"
      via: "a set comparison in both directions between the published names and the names a real build emits"
      pattern: "templates.rst"
    - from: "tests/test_documented_params_contract_gate.py"
      to: "tests/fixtures/documented_params_contract_gate/_templates/documented.typ"
      via: "sys.executable -m sphinx -b typstpdf subprocess build over a nine-parameter template"
      pattern: "documented_params_contract_gate"
---

<objective>
Close the phase: lock the documented contract with a permanent regression test (SC#3, D-K), prove
the contract and the behaviour agree in both directions by enumerating the emission sites (SC#2),
run the project's real gates live (SC#5, SC#6), and record what this phase hands off rather than
finishes (SC#9's second repository, SC#10's CHANGELOG debt).

Purpose: SC#1, SC#2, SC#3, SC#7 and SC#9 all say the same thing in different words — proven by a
real build, not by reading. This plan is where the phase's claims stop being distributed across
five plan SUMMARYs and become one checkable record, and where the two items this phase deliberately
does not finish are written down instead of forgotten.

Output: the documented-contract gate fixture and module, the two-way enumeration record with the
SC#5 invariance comparison, and the handoff document.
</objective>

## Artifacts this phase produces

**Added by this plan**
- `tests/fixtures/documented_params_contract_gate/` — new fixture project (`conf.py`, `index.rst`, `chapter.rst`, `_templates/documented.typ`)
- `tests/test_documented_params_contract_gate.py` — the permanent SC#3 regression lock
- `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md`
- `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md`

**Removed by this plan**
- Nothing. This plan adds gate coverage and records; it changes no production symbol and edits no published page.

**Recorded, not performed**
- Removal of the language-derivation workaround from the `typsphinx-doc-translations` repository (SC#9's explicit handoff item)
- The `## [0.7.1]` CHANGELOG callouts (SC#10 is a recording task for Phase 46, not a CHANGELOG edit)

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-01-SUMMARY.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-03-SUMMARY.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-04-SUMMARY.md
@.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the documented contract with a permanent regression gate</name>
  <precondition>The pre-45.1-01 commit SHA is still resolvable, so the RED basis for this gate can be taken from the real pre-fix tree rather than reconstructed; if it cannot be identified, halt rather than transcribing a post-fix observation into an assertion.</precondition>
  <files>tests/fixtures/documented_params_contract_gate/conf.py, tests/fixtures/documented_params_contract_gate/index.rst, tests/fixtures/documented_params_contract_gate/chapter.rst, tests/fixtures/documented_params_contract_gate/_templates/documented.typ, tests/test_documented_params_contract_gate.py</files>
  <read_first>
    - `docs/source/user_guide/templates.rst` Standard Parameters as published by plan 45.1-05 — the fixture template must declare exactly that list and nothing else
    - `typsphinx/templates/base.typ` lines 39-50 — parameter names, order and defaults
    - `tests/test_package_only_config_gate.py` in full — the module structure this gate copies: `_run_sphinx_build()`, the class-scoped `build` fixture, the PDF-validity assertion, and the no-compiler-error-text rule
    - `tests/test_params_exclusivity_gate.py` as built by plans 45.1-01 and 45.1-02 — this new module is its complement: exclusivity there, the default set here
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-GATE-EVIDENCE-03.md` — the recorded pre-change `lang` behaviour, which is this gate's RED axis
  </read_first>
  <behavior>
    - The fixture builds with `sphinx-build -b typstpdf` and exits 0, producing a PDF with valid magic bytes
    - The emitted `#show: project.with(` region carries exactly nine named arguments and their names are exactly the nine published in `templates.rst`
    - No named argument appears in the emitted region that the fixture template does not declare, and no declared named parameter other than the trailing body is missing from the emitted region
    - Removing the toctree from the fixture's master would drop exactly the three `toctree_*` arguments — asserted through a second, toctree-free build rather than by reasoning
  </behavior>
  <action>
    Create `tests/fixtures/documented_params_contract_gate/`. `conf.py` sets
    `extensions = ["typsphinx"]`, `project`/`author`/`release`, a `typst_documents` entry making
    `index` a master, `typst_template = "_templates/documented.typ"`,
    `typst_elements = {"papersize": "a4", "fontsize": "11pt"}` and `language = "ja"`. Deliberately do
    NOT set `typst_template_function` — this fixture exercises the default set, which is the case
    exclusivity is the alternative to. `index.rst` carries a toctree with `:maxdepth: 3`,
    `:numbered:` and `:caption: Documented Contents` pointing at `chapter.rst`, so all three
    `toctree_*` keys are live. `_templates/documented.typ` declares exactly the nine published
    parameters plus the trailing positional body, in `templates/base.typ`'s order and with its
    defaults, and renders a title block, an outline and the body.

    This is SC#1's literal shape: exactly the documented parameters, a master with a toctree and a
    `typst_elements` setting, built with `-b typstpdf`.

    Establish the RED basis honestly, and say in the module docstring which axis is red and which is
    not. On the compile axis this fixture would also have exited 0 against the pre-fix tree, because
    the pre-fix emission was a subset of the nine — so a compile-failure RED is not available here
    and claiming one would be false. The real RED axis is the emitted key set: pre-fix the explicit
    `typst_template` route emitted no `lang` argument, so the two-way set-equality assertion fails
    against the pre-fix tree. Take that proof from a detached worktree at the pre-45.1-01 SHA,
    record it in `45.1-CONTRACT-ENUMERATION.md`, and state in the docstring that the compile-success
    assertion is a non-regression control rather than a RED-proved gate — the same distinction
    Phase 37 drew for its corpus-derived case.

    Write `tests/test_documented_params_contract_gate.py` following the sibling gate module's
    structure. Assert the emitted named-argument name set equals the nine published names — set
    equality in both directions, not containment, so an extra emitted argument and a missing one
    both fail. Add a second class building a toctree-free variant of the same fixture and asserting
    the emitted set equals the six non-toctree names, which is the DOC-13 empty edge. Assert the
    PDF exists, is non-empty and starts with the PDF magic bytes. Match no Typst compiler error text.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest tests/test_documented_params_contract_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_documented_params_contract_gate.py -q` exits 0
    - The module contains an assertion comparing the emitted named-argument name set to the published name set with `==` on two sets, in one direction only because equality covers both — a containment check (`issubset` or `issuperset`) alone does not satisfy this criterion
    - A fresh `-b typst` build of the fixture emits a `#show: project.with(` region containing exactly nine named arguments
    - The toctree-free variant's emitted region contains exactly six named arguments, with none of `toctree_maxdepth`, `toctree_numbered` or `toctree_caption` present
    - `_templates/documented.typ`'s parameter list matches `typsphinx/templates/base.typ` lines 39-50 name for name, verified by comparing the two extracted name lists
    - The module docstring states which assertion is RED-proved and which is a non-regression control, and names the pre-45.1-01 SHA the RED proof was taken against
    - `45.1-CONTRACT-ENUMERATION.md` records that pre-fix run with its command line, SHA, exit code and emitted region
  </acceptance_criteria>
  <done>SC#3's regression lock exists, its RED basis is real and recorded, and its docstring does not overclaim which axis was red.</done>
</task>

<task type="auto">
  <name>Task 2: Enumerate the emission sites and prove two-way agreement, invariance, and the project gates</name>
  <files>.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTRACT-ENUMERATION.md</files>
  <read_first>
    - `typsphinx/writer.py` lines 240-331 — the three emission sites that decide the parameter set: the `map_parameters()` call, the `effective_elements` union feeding it, and the `extract_toctree_options()` update
    - `typsphinx/template_engine.py` `map_parameters()` (the back-fill gate and the `ELEMENTS_ALLOWLIST` merge) and `render()`'s exclusive branch
    - `docs/source/user_guide/templates.rst` Standard Parameters as published by plan 45.1-05
    - `typsphinx/templates/base.typ` lines 39-50 and `docs/source/_typst/custom_template.typ` lines 64-75
    - `.planning/ROADMAP.md` § "Phase 45.1" — SC#2, SC#5 and SC#6 in their amended wording
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md` `<specifics>` items 3 and 10 — the measured full emitted call and the repository's own first-toctree caption
  </read_first>
  <action>
    Write `45.1-CONTRACT-ENUMERATION.md` as the phase's single evidence record for SC#2, SC#5 and
    SC#6.

    For SC#2, enumerate the emission sites from the code rather than from the prose: every place a
    key can enter the parameter dict. Name each site by file and line — the `map_parameters()`
    mapping loop, its back-fill gate, its `ELEMENTS_ALLOWLIST` merge, the `effective_elements`
    language union in `writer.py`, the `extract_toctree_options()` update in `writer.py`, and
    `render()`'s exclusive `params` branch. For each site, list the key names it can contribute and
    the condition under which it does. Then build the union of all contributable names and compare
    it, as a set and in both directions, against the names `templates.rst` publishes. Record the
    comparison as two explicit statements — nothing emitted is unpublished, nothing published is
    unemitted — with the concrete name lists shown, so a reader can check the claim rather than
    trust it. Containment in one direction is not sufficient and must not be recorded as agreement.

    For SC#5, record the default-template invariance comparison. Build a project that uses the
    bundled `templates/base.typ` with no `typst_template`, no `typst_package` and no
    `typst_template_function`, both at the pre-45.1-01 SHA in a detached worktree and at HEAD, and
    compare the emitted `#show:` regions. State whether the parameter set is identical. If it is
    not, state the difference and justify it against SC#6's amended clause — every behaviour change
    in this phase must be one of SC#7, SC#8 or SC#9, and anything outside those three is a finding,
    not a footnote. Also record the `tox -e docs-pdf` result and the repository's own emitted
    `toctree_caption` value, which CONTEXT.md item 10 predicts is the first toctree's caption.

    For SC#6, run the four gates live in this session and paste their real output: the full pytest
    suite, `black --check .`, `ruff check .` and `mypy typsphinx/`. Do not inherit a green result
    from an earlier plan's SUMMARY. In a worktree run them under `uv run` after
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`; note in the record that in
    the main tree a human runs pytest as `.venv/bin/python -m pytest`, because an outer `uv run`
    there produces spurious integration-test failures.

    Close the record with the SC#6 behaviour-change ledger: list every behaviour change this phase
    made, map each to SC#7, SC#8 or SC#9, and state explicitly that no change falls outside those
    three — or name the ones that do.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev && uv run pytest -q && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/ && uv run tox -e docs-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `45.1-CONTRACT-ENUMERATION.md` enumerates at least six emission sites, each named by file and line, with its contributable key names and its condition
    - The file records two explicit directional statements with concrete name lists — one asserting nothing emitted is unpublished, one asserting nothing published is unemitted — and does not substitute a single containment check for either
    - The file records the SC#5 invariance comparison as two emitted `#show:` regions (pre-45.1-01 SHA and HEAD) with an explicit identical/differs verdict, and justifies any difference against SC#6's amended clause
    - The file records the live output of `uv run pytest -q`, `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` and `uv run tox -e docs-pdf`, each with its exit code, dated to this session
    - The file records the repository's own emitted `toctree_caption` value from the `tox -e docs-pdf` run
    - The file closes with a behaviour-change ledger mapping every change to SC#7, SC#8 or SC#9, or naming the exceptions
    - All five commands in the verify block exit 0
  </acceptance_criteria>
  <done>SC#2, SC#5 and SC#6 are each backed by a recorded, re-runnable measurement rather than by a claim in a SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Record the handoffs, the CHANGELOG debt, and the D-H amendment check</name>
  <files>.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 45.1" SC#9 and SC#10, and § "Phase 46" — the release-prep phase that inherits the CHANGELOG debt
    - `.planning/REQUIREMENTS.md` entries CONF-10, CONF-11, CONF-12 and DOC-13, and the coverage table rows near line 304
    - `.planning/STATE.md` — the Active Milestone section's statement that the milestone owes two user-visible CHANGELOG callouts, and the Deferred Items table
    - `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-CONTEXT.md` `<deferred>` — the three deferred items and the reviewed-todos list
    - `docs/source/conf.py` as changed by plan 45.1-03 — the workaround whose twin lives in the second repository
  </read_first>
  <action>
    Write `45.1-HANDOFF.md` with three numbered items, each stating what is owed, to whom, and what
    evidence closes it.

    Item 1 — SC#9's second repository. The `typsphinx-doc-translations` repository carries the same
    language-derivation workaround this phase removed from `docs/source/conf.py`. CONF-12 makes it
    removable there too, but that repository is outside this one and SC#9 scopes it explicitly as a
    handoff item, not in-phase work. Record what the change is, why it is now safe, and note that it
    rides alongside the standing second-repository tagging cost that every release already pays.
    Also flag the sharper consequence: that repository's own custom template must declare `lang`
    before its next build against a typsphinx carrying CONF-12, or it will fail with an undeclared
    argument.

    Item 2 — SC#10's CHANGELOG debt for Phase 46. Record that this phase adds three user-visible
    changes to the milestone's existing two, for five total callouts. Name all five with a one-line
    description each: the output-filename change from CONF-08, the rendered title and author change
    from CONF-09, and this phase's three — a declared `params` becoming the complete parameter set
    (CONF-11), the removal of the author-details config value (CONF-10), and the auto-derived `lang`
    reaching every non-package template route (CONF-12). For CONF-10, carry forward the fact the
    owner asked to be recorded plainly: Phase 44.2 published a forward-removal notice promising a
    future major release, and this removal lands in a patch. This is a recording task — do not edit
    `CHANGELOG.md` or `docs/source/changelog.rst` here; both land in Phase 46's lockstep edit.

    Item 3 — the D-H amendment check. The ROADMAP and REQUIREMENTS amendments were made on
    2026-08-10 before planning, on the Phase 44.1 D-01 precedent that the verifier reads success-
    criterion text directly. Verify they are still present and unmodified: SC#6 carries the amended
    wording that removes the falsified "no behaviour outside the template-parameter contract
    changed" clause, SC#7 through SC#10 exist, and CONF-10, CONF-11, CONF-12 and DOC-13 are all
    present in REQUIREMENTS.md with matching rows in its coverage table. Record the verdict. If any
    has drifted, say so as a finding rather than silently repairing it.

    Close with the phase's deferred items restated so the next reader does not have to re-derive
    them from CONTEXT.md: the Typst argument sink (kept as the option of record for a future
    parameter addition), the declined lockstep sync test and the unguarded fourth lockstep site it
    leaves at `docs/source/_typst/custom_template.typ`, and the question of where migration guidance
    for users of the removed config value lives.
  </action>
  <verify>
    <automated>test -f .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md && grep -q 'typsphinx-doc-translations' .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md && grep -qE 'CONF-08' .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md && grep -qE 'CONF-09' .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md && grep -qE 'CONF-1[012]' .planning/phases/45.1-custom-template-parameter-contract-correction/45.1-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - `45.1-HANDOFF.md` exists and contains three numbered items, each stating what is owed, to whom, and what evidence closes it
    - Item 1 names the `typsphinx-doc-translations` repository, states that the change is out of phase scope by SC#9, and flags that that repository's own template must declare the language parameter before its next build
    - Item 2 lists exactly five CHANGELOG callouts with a one-line description each, covering CONF-08, CONF-09, CONF-11, CONF-10 and CONF-12, and records the forward-removal-notice override for CONF-10
    - `git diff --name-only HEAD -- CHANGELOG.md docs/source/changelog.rst` is empty — SC#10 is a recording task and neither file is edited here
    - Item 3 records a present/absent verdict for the amended SC#6 wording, for SC#7 through SC#10, and for the four requirement entries and their coverage-table rows, with any drift stated as a finding rather than repaired
    - The closing section restates the three deferred items from CONTEXT.md, including the unguarded fourth lockstep site at `docs/source/_typst/custom_template.typ`
  </acceptance_criteria>
  <done>Everything this phase hands off rather than finishes is written down where Phase 46 and the milestone close will read it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan SUMMARY claims → phase verdict | A closeout that inherits green results from earlier SUMMARYs certifies a tree state nobody measured |
| this repository → `typsphinx-doc-translations` | A behaviour change here can break a build in a repository this phase does not touch |
| pre-fix SHA → RED basis | A gate whose RED basis is reconstructed from the post-fix tree proves nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45.1-13 | Repudiation | closeout asserting SC#2, SC#5 or SC#6 met without live evidence | high | mitigate | Every claim in `45.1-CONTRACT-ENUMERATION.md` must carry a command line, an exit code and pasted output dated to the closeout session; Task 2's action forbids inheriting a green result from an earlier plan's SUMMARY, and its acceptance criteria require the live output of all five commands. |
| T-45.1-18 | Denial of Service (downstream) | the second repository's custom template omitting the language parameter | medium | transfer | Transferred by explicit handoff: Item 1 of `45.1-HANDOFF.md` names the failure mode so the next build of that repository is not the place it is discovered. SC#9 scopes the work out of this phase by owner decision. |
| T-45.1-19 | Repudiation | overclaiming which axis of the documented-contract gate went red | medium | mitigate | The compile axis was green pre-fix because the pre-fix emission was a subset of the nine; the module docstring must say so and name the emitted-key-set axis as the real RED, with the proof taken from a detached worktree at the recorded SHA. Task 1 carries a `<precondition>` that halts if the SHA cannot be identified. |
| T-45.1-SC | Tampering | third-party package fetch during the closeout gates | low | accept | No npm/pip/cargo install task exists in this plan. `tox -e docs-pdf` fetches the four already-pinned Typst Universe `@preview` packages, whose pins `tests/test_preview_version_sync.py` asserts across the three in-repo declaration sites; unchanged by this phase. |
</threat_model>

<verification>
- `uv run pytest tests/test_documented_params_contract_gate.py -q` exits 0
- `uv run pytest -q` exits 0 over the whole suite
- `uv run black --check . && uv run ruff check . && uv run mypy typsphinx/` all exit 0
- `uv run tox -e docs-pdf` exits 0
- `45.1-CONTRACT-ENUMERATION.md` records the emission-site enumeration, the two directional agreement statements, the SC#5 invariance comparison and the live gate output
- `45.1-HANDOFF.md` records the second-repository handoff, the five CHANGELOG callouts and the D-H amendment verdict
</verification>

<success_criteria>
- SC#1 and SC#3 are locked by a permanent regression test whose RED basis is real and whose docstring does not overclaim
- SC#2's both-direction agreement is proved by enumerating emission sites from the code
- SC#5 and SC#6 are proved by live runs in the closeout session, not inherited
- SC#9's handoff and SC#10's CHANGELOG debt are recorded where Phase 46 will read them
- D-H's amendments are verified still in place
</success_criteria>

<output>
Create `.planning/phases/45.1-custom-template-parameter-contract-correction/45.1-06-SUMMARY.md` when done
</output>