---
phase: 46-v0-7-1-release-prep-prep-only
plan: 04
type: execute
wave: 3
depends_on: ["46-02", "46-03"]
files_modified:
  - .planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md
  - .planning/phases/46-v0-7-1-release-prep-prep-only/46-GREEN-TREE-EVIDENCE.md
autonomous: true
requirements: [REL-06]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A second, live CI run — dispatched on the post-bump commit, not inherited from an earlier phase — reports `success` for every job: the six `{ubuntu, macos, windows} × {3.12, 3.13}` test lanes plus Lint and Format Check, Type Check, Code Coverage, Build Package, and both Integration Test jobs (SC#3, D-11, D-23 run 2)."
    - "The commit that CI proved green is the commit on `origin/gsd/v0.7.1-bug-fix-round` and carries both the `0.7.1` version literal and the `## [0.7.1]` CHANGELOG heading."
    - "`tox -e docs-html` and `tox -e docs-pdf` both build clean on the post-bump tree, and `docs/_build/pdf/typsphinx.pdf` is produced by typsphinx's own `typstpdf` builder."
    - "The full-corpus `-b typstpdf` gate over Sphinx's own `doc/` tree re-runs fatal-free on the post-bump tree, or skips honestly with its reason recorded."
    - "A single `SPHINX_LANGUAGE=ja` docs-pdf build succeeds and its emitted `.typ` carries the Japanese `lang` value, exercising CONF-12's route to a published artifact (D-12)."
    - "No local run is presented as the authority for pytest, `black`, `ruff` or `mypy` — D-11 assigns that to CI, and the evidence file states which local environments could not run here and why."
  artifacts:
    - path: ".planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md"
      provides: "the D-23 run 2 authority-run transcripts, replacing the placeholder plan 46-01 left"
      contains: "D-23 run 2"
    - path: ".planning/phases/46-v0-7-1-release-prep-prep-only/46-GREEN-TREE-EVIDENCE.md"
      provides: "the local half of SC#3 — docs builds, the full-corpus gate, and the ja build"
      contains: "docs-pdf"
  key_links:
    - from: "the post-bump milestone-branch commit"
      to: "the dispatched `ci.yml` authority run"
      via: "`gh workflow run ci.yml --ref gsd/v0.7.1-bug-fix-round` after a fast-forward push; CI triggers on push only for `main`/`develop`, so the run must be dispatched by hand and matched by headSha"
      pattern: "workflow_dispatch"
    - from: "docs/source/conf.py `_resolve_language()`"
      to: "the emitted `lang` template argument in the ja build"
      via: "`READTHEDOCS_LANGUAGE` then `SPHINX_LANGUAGE`, defaulting to `en`; Sphinx's `language` then reaches the custom template through CONF-12's auto-derivation"
      pattern: "SPHINX_LANGUAGE"
---

<objective>
Prove the post-bump tree green **live**, not inherited: dispatch and read the CI authority run on the
pushed commit, then collect the local evidence CI structurally does not produce.

Purpose: discharges ROADMAP Phase 46 SC#3. D-11 assigns pytest / `black` / `ruff` / `mypy` authority
to the branch CI run, because local never sees Windows or macOS and, on this machine, `tox -e lint`
cannot run at all — `.venv/bin/ruff` is a generic-linux ELF the NixOS stub loader rejects. D-23 makes
this the second of two runs: run 1 (plan 46-01) confirmed the Windows repair; this one carries the
bump and the `## [0.7.1]` entry and **is** SC#3's authority. The local half — both docs builds, the
full-corpus gate, and a single `ja` build — is what CI does not cover.

Output: a fully green authority CI run recorded verbatim, plus a local green-tree evidence file that
states plainly what was executed and what was not.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-CONTEXT.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `46-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan fills
the `## D-23 run 2` placeholder in `46-CI-EVIDENCE.md` and creates
`.planning/phases/46-…/46-GREEN-TREE-EVIDENCE.md`.

<tasks>

<task type="auto">
  <name>Task 1: Push the post-bump tip, dispatch the authority CI run, and read every job</name>
  <precondition>HEAD already carries `version = "0.7.1"` in `pyproject.toml` and a `## [0.7.1]` heading in `CHANGELOG.md` (plans 46-02 and 46-03 merged back), and `origin/gsd/v0.7.1-bug-fix-round` is an ancestor of HEAD so a plain fast-forward push is possible.</precondition>
  <files>.planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md</files>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — provisioning is mandatory.
    - `.github/workflows/ci.yml` — the trigger block (push only on `main`/`develop`, plus `workflow_dispatch`), the `os × python-version` matrix, and the job names: `Test Python ${{ matrix.python-version }} on ${{ matrix.os }}`, plus `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`, `Integration Test - basic`, `Integration Test - advanced`.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md` — plan 46-01's run-1 section and the `## D-23 run 2 — the authority run` placeholder this task replaces.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-VALIDATION.md` § "Manual-Only Verifications" row M2.
  </read_first>
  <action>
**Do this before writing any file in this worktree.** The push must publish exactly the milestone
branch's merged tip, so it has to happen while HEAD still equals that tip — if an evidence commit
lands first, the CI run covers a commit the phase never intended to prove.

Provision the worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.

Confirm the tree is the one to prove: `grep '^version = ' pyproject.toml` must read `0.7.1`, and
`CHANGELOG.md` must contain a `## [0.7.1]` heading. If either is absent, stop — waves 1 and 2 have not
merged back and this plan is running out of order.

Push with `git push origin HEAD:refs/heads/gsd/v0.7.1-bug-fix-round`. This is a plain fast-forward;
never force. Record the pushed SHA with `git rev-parse HEAD`, and confirm
`git rev-parse origin/gsd/v0.7.1-bug-fix-round` now equals it.

Dispatch CI by hand — `ci.yml` triggers on push only for `main` and `develop`, so pushing this branch
starts nothing but the Link Check workflow: run
`gh workflow run ci.yml --ref gsd/v0.7.1-bug-fix-round`. Poll
`gh run list --workflow=ci.yml --branch gsd/v0.7.1-bug-fix-round --limit 5 --json databaseId,headSha,event,status`
until a `workflow_dispatch` run whose `headSha` equals the pushed SHA appears; export its
`databaseId` as `RUN_ID`. Wait with `gh run watch "$RUN_ID"`, then read every job's conclusion with
`gh run view "$RUN_ID" --json jobs`.

This run is SC#3's authority (D-11, D-23 run 2). **Every** job must be `success` — not just the
Windows lanes. If any job fails, do not paper over it: report the failure with its log excerpt and
stop. A retry is legitimate only after the cause is fixed, and any fix commit must be pushed and a
fresh run dispatched, with both runs recorded.

Then replace the `## D-23 run 2 — the authority run` placeholder line in `46-CI-EVIDENCE.md` with the
real section: the pushed SHA, the push transcript, the dispatch command, the run id and its
`https://github.com/YuSabo90002/typsphinx/actions/runs/<id>` URL, a `### Job conclusions` subsection
with the complete name/conclusion table from `gh run view --json jobs`, and a `### Why this run is the
authority` subsection restating D-11's split in one paragraph — CI covers the matrix, the lint trio
and the type check; local covers the docs builds, the corpus gate, and the `ja` build.

Do not delete or edit plan 46-01's run-1 section. Take no irreversible action: no `git tag`, no tag
push, no PyPI upload, no GitHub Release, no PR.
  </action>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = "$(git rev-parse origin/gsd/v0.7.1-bug-fix-round)" && gh run view "$RUN_ID" --json jobs --jq '[.jobs[].conclusion]|unique|@csv' | grep -qx '"success"' && grep -q '^### Job conclusions' .planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse HEAD` equals `git rev-parse origin/gsd/v0.7.1-bug-fix-round` at push time, and that SHA's tree contains `version = "0.7.1"` in `pyproject.toml` and a `## [0.7.1]` heading in `CHANGELOG.md`.
    - `gh run view "$RUN_ID" --json jobs --jq '[.jobs[].conclusion]|unique'` returns exactly `["success"]`.
    - The recorded job table names all six matrix lanes (`Test Python 3.12 on ubuntu-latest`, `… on windows-latest`, `… on macos-latest`, and the three 3.13 equivalents) plus `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`, `Integration Test - basic`, `Integration Test - advanced`.
    - `46-CI-EVIDENCE.md` contains the headings `## D-23 run 2 — the authority run`, `### Job conclusions`, and `### Why this run is the authority`, and no longer contains the string `_Pending — filled by plan 46-04._`.
    - `46-CI-EVIDENCE.md` still contains plan 46-01's `## D-23 run 1 — the Windows check run` section unchanged.
    - `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` both produce no output.
  </acceptance_criteria>
  <done>A live CI run dispatched on the post-bump commit reports `success` for every job, and its full transcript is recorded as SC#3's authority alongside run 1.</done>
</task>

<task type="auto">
  <name>Task 2: Run the local docs builds and the full-corpus `-b typstpdf` gate</name>
  <files>.planning/phases/46-v0-7-1-release-prep-prep-only/46-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - `tox.ini` — the `docs-html` and `docs-pdf` environments (`extras = docs`, `changedir = docs`, `runner = uv-venv-lock-runner`), and the `[testenv]` block's `package = editable` note.
    - `tests/test_corpus_gate.py` — the gate's own network-availability skip design, so an honest skip is recognised as such rather than reported as a pass.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md` § "Pitfall 1" (why `tox -e py312` cannot provision here) and § "Pattern 4" (the exact commands, all verified green on 2026-08-11).
    - `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` — why a bare `tox` still dies in its `lint` environment, and why that does not weaken SC#3.
  </read_first>
  <action>
Local `tox` runs at all only because Phase 45.2 landed the `tox-uv` → `tox-uv-bare` dependency repair
that D-18 routed out of this phase; that dependency is already in place and this plan must not touch
`pyproject.toml` or `tox.ini`.

Provision, then run each environment **individually** — never a bare `tox`, whose `env_list` includes
`lint`, which dies at exit 127 on this machine because `.venv/bin/ruff` is a generic-linux ELF the
NixOS stub loader rejects. That is a filed, out-of-scope environmental defect, not a validation gap:
D-11 amendment (b) already assigns lint authority to CI, which task 1 collected.

Run, capturing each transcript verbatim:
`uv run --extra dev tox -e docs-html`; `uv run --extra dev tox -e docs-pdf`; and
`uv run pytest tests/test_corpus_gate.py -v`.

**Never invoke `tox -e py312` here.** On this machine `uv venv -p cpython3.12` downloads a standalone
CPython whose ELF the stub loader rejects (exit 127), so the environment cannot provision at all. If a
local pytest sanity spot-check is wanted beyond the gate above, use `uv run pytest` directly or
`tox -e py313`, which matches this machine's system interpreter — and label it a spot-check, never an
authority.

For the corpus gate, record the summary line and, if any test skipped, its skip reason verbatim. The
gate skips honestly when the network is unavailable; a skip recorded as a skip is correct evidence, a
skip reported as a pass is not. Note that `test_empty_url_before_after` is gated on
`TYPSPHINX_CORPUS_REPORT=1` and its skip is unrelated to the gate itself.

Then create `46-GREEN-TREE-EVIDENCE.md` in the phase directory, in English, with a provisioning note
(`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, everything through `uv run`), a
`## Local evidence — docs builds` section holding both tox transcripts and the produced PDF's path and
byte size, and a `## Local evidence — full-corpus gate` section holding the pytest transcript and its
summary line. Leave a `## Local evidence — ja build (D-12)` heading for task 3.
  </action>
  <verify>
    <automated>uv run --extra dev tox -e docs-html && uv run --extra dev tox -e docs-pdf && test -s docs/_build/pdf/typsphinx.pdf && uv run pytest tests/test_corpus_gate.py -q && grep -q '^## Local evidence — docs builds' .planning/phases/46-v0-7-1-release-prep-prep-only/46-GREEN-TREE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra dev tox -e docs-html` exits 0 and its transcript contains `build succeeded`.
    - `uv run --extra dev tox -e docs-pdf` exits 0, its transcript names the generated PDF, and `docs/_build/pdf/typsphinx.pdf` exists with a non-zero size.
    - `uv run pytest tests/test_corpus_gate.py -v` exits 0, and `TestCorpusRenderGate::test_corpus_compiles_with_no_fatal_error` is reported `PASSED` (or, if the network was unavailable, reported `SKIPPED` with its reason transcribed).
    - `46-GREEN-TREE-EVIDENCE.md` exists, is in English, and contains the headings `## Local evidence — docs builds`, `## Local evidence — full-corpus gate`, and `## Local evidence — ja build (D-12)`.
    - The evidence file records the byte size of `docs/_build/pdf/typsphinx.pdf`.
    - The evidence file contains no claim that a local run is the authority for pytest, `black`, `ruff` or `mypy`; it cites task 1's CI run for those.
  </acceptance_criteria>
  <done>Both docs builds and the full-corpus `-b typstpdf` gate ran on the post-bump tree with their transcripts recorded verbatim, and the local/CI authority split is stated rather than implied.</done>
</task>

<task type="auto">
  <name>Task 3: Run the single `ja` docs-pdf build and close out the local evidence</name>
  <files>.planning/phases/46-v0-7-1-release-prep-prep-only/46-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - `docs/source/conf.py` lines 48-104 — `_resolve_language()` reads `READTHEDOCS_LANGUAGE`, then `SPHINX_LANGUAGE`, defaulting to `en`; `language = _resolve_language()`; `typst_template = "_typst/custom_template.typ"`; and the CONF-12 comment stating typsphinx now auto-derives `lang` on every non-package template route and that the old `conf.py` workaround must not be re-added.
    - `docs/source/_typst/custom_template.typ` lines 60-80 — the nine-parameter declaration including `lang`, which is what makes the ja build safe on this route.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-CONTEXT.md` D-12 and D-13 — why this is a single build and not Phase 41's four-check glyph bar, and why the translations repository has nothing to remove.
  </read_first>
  <action>
D-12 asks for one thing: a single local `ja` docs-pdf build. The mechanism is now resolved by direct
read rather than assumed — `docs/source/conf.py`'s `_resolve_language()` reads the `SPHINX_LANGUAGE`
environment variable (after `READTHEDOCS_LANGUAGE`) and defaults to `en`.

Do **not** try to drive this through `tox -e docs-pdf`: that environment declares no `passenv`, so
`SPHINX_LANGUAGE` does not reach the child process and the build would silently produce the English
document while appearing to succeed. Invoke Sphinx directly in a docs-extras environment instead:
`SPHINX_LANGUAGE=ja uv run --extra dev --extra docs python -m sphinx -b typstpdf docs/source docs/_build/pdf-ja`.

Prove the language actually took effect rather than trusting the exit code: the emitted
`docs/_build/pdf-ja/typsphinx.typ` must carry a `lang: "ja"` argument in its template call — that
argument is CONF-12's whole subject, and its presence on this route is the reason D-12 exercises `ja`
at all. Also confirm `docs/_build/pdf-ja/typsphinx.pdf` exists with a non-zero size.

This is deliberately **not** Phase 41's four-check glyph bar. Both of that bar's triggers were
re-measured for this milestone and neither holds: `raw(` call sites in `typsphinx/translator.py` are
41 before and 41 after, and the milestone diff over `typsphinx/` names no font family. Do not re-run
the bar, and do not extend this task into a glyph audit.

Record the result under the `## Local evidence — ja build (D-12)` heading in
`46-GREEN-TREE-EVIDENCE.md`: the exact command, the transcript, the emitted `lang` line quoted from
the `.typ`, and the PDF's byte size. Add a short note recording D-13's correction of record — the
`typsphinx-doc-translations` repository contains no `conf.py` and no `.typ` template, so there is
nothing to remove there and this build exercises this repository's own files; the standing
two-repository tagging cost is unaffected and is carried in `46-HANDOFF.md`.

Finish the file with an `## Executed versus skipped` section stating plainly which environments were
not run and why: `tox -e py312` (interpreter cannot provision on this machine), a bare `tox` (its
`lint` environment dies at exit 127), and anything the corpus gate skipped. Never gloss a gap.
  </action>
  <verify>
    <automated>SPHINX_LANGUAGE=ja uv run --extra dev --extra docs python -m sphinx -b typstpdf docs/source docs/_build/pdf-ja && test -s docs/_build/pdf-ja/typsphinx.pdf && grep -q 'lang: "ja"' docs/_build/pdf-ja/typsphinx.typ && grep -q '^## Executed versus skipped' .planning/phases/46-v0-7-1-release-prep-prep-only/46-GREEN-TREE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The `ja` build command exits 0 and its transcript contains `build succeeded`.
    - `docs/_build/pdf-ja/typsphinx.pdf` exists with a non-zero size, and its byte size is recorded in the evidence file.
    - `docs/_build/pdf-ja/typsphinx.typ` contains `lang: "ja"`, and that line is quoted verbatim in the evidence file.
    - `46-GREEN-TREE-EVIDENCE.md`'s `## Local evidence — ja build (D-12)` section contains the exact command including the `SPHINX_LANGUAGE=ja` prefix, and a note recording D-13's finding about the translations repository.
    - `46-GREEN-TREE-EVIDENCE.md` contains an `## Executed versus skipped` section naming `tox -e py312` and the bare-`tox` `lint` failure as things not run, each with its reason.
    - `git diff --name-only -- docs/ typsphinx/ tests/` produces no output (the build wrote only into the gitignored `_build` tree).
    - `git tag -l v0.7.1` produces no output.
  </acceptance_criteria>
  <done>A single `ja` docs-pdf build succeeded on the post-bump tree with the Japanese `lang` value provably reaching the custom template, and the local evidence file closes with an honest executed-versus-skipped statement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → `origin` | The push publishes the release-candidate tree that CI, and later the release automation, reads. |
| GitHub Actions job conclusions → this phase's SC#3 record | The run's output becomes the phase's claim that the tree is green. |
| `SPHINX_LANGUAGE` → the emitted `.typ` template call | An environment variable decides which language the published artifact is built in; a silently-dropped variable would produce an English document reported as Japanese. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-11 | Spoofing | a CI run proving a different commit than the one the phase ships | high | mitigate | The dispatched run is matched by `headSha` against the pushed SHA, and acceptance asserts `git rev-parse HEAD` equals `git rev-parse origin/<branch>` at push time and that the tree carries both the `0.7.1` literal and the `## [0.7.1]` heading. |
| T-46-07 | Repudiation | a green claim asserted from memory or from an inherited badge | medium | mitigate | Evidence culture: run id, URL, and the complete job table are transcribed verbatim; an `## Executed versus skipped` section names every gap. |
| T-46-12 | Spoofing | the `ja` build silently producing the English document | medium | mitigate | The verify step greps the emitted `.typ` for `lang: "ja"` rather than trusting the exit code, and the action explains why `tox -e docs-pdf` would drop the variable. |
| T-46-03 | Tampering | prep/publish fence | high | mitigate | No tag, tag push, PyPI upload, GitHub Release or PR; the push is fast-forward-only with force explicitly forbidden; `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` asserted empty. |
| T-46-SC | Tampering | npm/pip/cargo installs | medium | accept | The tox environments and `uv run --extra` invocations install only what `uv.lock` already pins; no package is added. RESEARCH.md records the Package Legitimacy Gate as not applicable — zero packages evaluated, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. |
</threat_model>

<verification>
- `gh run view "$RUN_ID" --json jobs --jq '[.jobs[].conclusion]|unique'` returns `["success"]`.
- `uv run --extra dev tox -e docs-html` and `tox -e docs-pdf` both exit 0 with `build succeeded`.
- `uv run pytest tests/test_corpus_gate.py -v` exits 0 with its gate test PASSED or honestly SKIPPED.
- The `ja` build exits 0 and `docs/_build/pdf-ja/typsphinx.typ` contains `lang: "ja"`.
- `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` are both empty.
</verification>

<success_criteria>
- The authority CI run (D-23 run 2) is green on every job, on the exact post-bump commit, and recorded verbatim.
- Both docs builds and the full-corpus gate ran locally on that same tree.
- A single `ja` docs-pdf build succeeded with the Japanese `lang` provably reaching the template.
- The evidence states which environments could not run here and why, rather than glossing the gaps.
- No irreversible action was taken.
</success_criteria>

<output>
Create `.planning/phases/46-v0-7-1-release-prep-prep-only/46-04-SUMMARY.md` when done.
</output>
