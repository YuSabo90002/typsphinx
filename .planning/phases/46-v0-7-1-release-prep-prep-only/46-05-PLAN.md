---
phase: 46-v0-7-1-release-prep-prep-only
plan: 05
type: execute
wave: 3
depends_on: ["46-02", "46-03"]
files_modified:
  - .planning/phases/46-v0-7-1-release-prep-prep-only/46-SC4-INVARIANTS.md
  - .planning/phases/46-v0-7-1-release-prep-prep-only/46-REL04-EVIDENCE.md
autonomous: true
requirements: [REL-06, REL-04]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The milestone invariant sweep is anchored at the `v0.7.0` tag (commit `75fd8ed`) and re-measured on the post-merge HEAD, so the swept diff is exactly what a v0.7.0 user receives (D-21, superseding D-14's `87f242a` branch fork-point)."
    - "Zero new runtime dependencies across the full milestone diff: the `[project] dependencies` array is byte-identical to `v0.7.0`'s, and the only dependency-adjacent movement in `pyproject.toml` is inside the `dev` and `docs` extras (D-08's runtime scoping)."
    - "The `@preview` package count is still four with no new version-lockstep site: `tests/test_preview_version_sync.py` passes and the repo-wide `@preview/` enumeration contains no file beyond the set that guard already documents, plus the already-known, already-out-of-scope `docs/source/_typst/custom_template.typ`."
    - "`.github/workflows/release.yml`'s `create-release` job carries the `astral-sh/setup-uv` and `Set up Python` steps ahead of its `extract_changelog_section.py` call, and this phase changed nothing in that file — recorded as a precondition, never as acceptance."
    - "`uv run python scripts/extract_changelog_section.py 0.7.1` exits 0 and prints a non-empty body; the same call for a version with no section exits 1 with a message on stderr."
    - "Two consecutive `extract_changelog_section.py 0.7.1` invocations produce byte-identical stdout, and neither creates a git tag or any other side effect."
    - "The extracted `0.7.1` body is byte-identical to the `CHANGELOG.md` lines between the `## [0.7.1]` heading and the next `## [` heading with leading and trailing blank lines stripped — it carries no text from the adjacent `## [0.7.0]` section and none from `## [Unreleased]`."
    - statement: "REL-04 remains open. Neither this plan nor any artifact it writes reports the requirement complete: its acceptance evidence is a real tag push whose `create-release` job runs to completion, which only `/gsd-complete-milestone` can generate, and an interrupted or partially-executed phase leaves that unchanged."
      verification: backstop
  artifacts:
    - path: ".planning/phases/46-v0-7-1-release-prep-prep-only/46-SC4-INVARIANTS.md"
      provides: "the D-21 invariant sweep transcripts, anchored at the v0.7.0 tag"
      contains: "75fd8ed"
    - path: ".planning/phases/46-v0-7-1-release-prep-prep-only/46-REL04-EVIDENCE.md"
      provides: "REL-04's in-phase precondition checks and the written statement that the requirement stays open"
      contains: "REL-04 remains open"
  key_links:
    - from: "CHANGELOG.md `## [0.7.1]` section"
      to: ".github/workflows/release.yml `create-release` job"
      via: "`uv run python scripts/extract_changelog_section.py \"${TAG#v}\" > release_notes.md` — the same committed script the `validate` job also calls, never a second parser"
      pattern: "extract_changelog_section\\.py"
    - from: "pyproject.toml `[project] dependencies`"
      to: "the v0.7.0 tag's copy of the same array"
      via: "a direct `diff` of the two extracted blocks; an empty diff is the zero-new-runtime-dependencies invariant"
      pattern: "^dependencies = \\["
---

<objective>
Assert the milestone's standing invariants mechanically over the SHA-anchored full milestone diff, and
discharge REL-04's in-phase share — without reporting REL-04 closed.

Purpose: discharges ROADMAP Phase 46 SC#4, both halves. D-21 anchors the sweep at the `v0.7.0` tag
(commit `75fd8ed`) rather than the branch fork-point `87f242a`, so the swept diff equals "what a
v0.7.0 user receives" — the same diff the CHANGELOG's completeness is judged against. The REL-04 half
is deliberately scoped as *precondition verification plus an exercise*: v0.7.0 reported this
requirement's mechanism as done and the release then failed, and this phase must not repeat that.

Output: two evidence files — the invariant sweep, and REL-04's precondition record with its openness
stated in writing.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-CONTEXT.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `46-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan creates
`.planning/phases/46-…/46-SC4-INVARIANTS.md` and `.planning/phases/46-…/46-REL04-EVIDENCE.md`.

<tasks>

<task type="auto">
  <name>Task 1: Run the D-21 invariant sweep over the v0.7.0-anchored milestone diff</name>
  <precondition>The `v0.7.0` tag resolves locally (`git rev-parse v0.7.0^{commit}` returns `75fd8ed…`) and HEAD already contains `origin/main` (plan 46-01's merge merged back), so the sweep measures the post-merge tree rather than a pre-merge one.</precondition>
  <files>.planning/phases/46-v0-7-1-release-prep-prep-only/46-SC4-INVARIANTS.md</files>
  <read_first>
    - `CLAUDE.md` § "The `@preview` version-sync hazard" — the three declaration sites that must stay in lockstep, which is the surface this sweep counts.
    - `pyproject.toml` lines 1-60 — the `[project] dependencies` array and the `dev` / `docs` extras.
    - `tests/test_preview_version_sync.py` — its own docstring naming the four sync surfaces it guards.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md` § "Code Examples" → "SC#4's invariant sweep — concrete, verified command shapes" — both command shapes were executed against the live tree at research time.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-CONTEXT.md` D-21 (the anchor and why it supersedes D-14) and D-08 (why the dependency claim is scoped to runtime).
    - `.planning/milestones/v0.7.0-phases/41-v0-7-0-release-automation-release-prep/41-SC4-INVARIANTS.md` — the evidence-file shape the roll-up later cites.
  </read_first>
  <action>
Provision the worktree: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run
Python through `uv run`.

Anchor every measurement at the `v0.7.0` **tag** — `git rev-parse v0.7.0^{commit}` resolves to
`75fd8ed` (`git ls-remote` shows the annotated-tag object `7327d01` pointing at the same commit).
Do **not** use `87f242a`: that is one commit after the tag and is merely where this branch happened to
fork, so it measures the branch's contribution rather than the release. D-14's figures are superseded
and are kept only as a record.

**Scale of the swept diff.** Take `git diff v0.7.0..HEAD --stat -- . ':(exclude).planning'` and record
the file / insertion / deletion counts. The pre-merge figures measured on 2026-08-11 were 126 files,
+10,582 / −932; these are expected to move now that the merge and the bump have landed, so record what
you measure, not what was measured before.

**Invariant 1 — zero new runtime dependencies.** Diff only the `[project] dependencies` array between
the anchor and HEAD, by extracting the block from `^dependencies = \[` to the closing bracket on both
sides and comparing them; the diff must be empty. Then take the broader eyeball check —
`git diff v0.7.0..HEAD -- pyproject.toml` filtered to added/removed lines — and confirm that the only
dependency-adjacent movement is `tox-uv` → `tox-uv-bare` in the `dev` extra (Phase 45.2 / QUA-04) and
`+myst-parser>=5.0` in the `docs` extra (Phase 45 / DOC-12), alongside the version literal this phase
moved. This is exactly why D-08 scopes the CHANGELOG's `### Verified` claim to **runtime**
dependencies: the unscoped claim would be false to anyone who diffs `uv.lock`.

**Invariant 2 — the `@preview` count is still four with no new lockstep site.** Run
`uv run pytest tests/test_preview_version_sync.py -v`, which performs the identity check mechanically
across `typsphinx/writer.py`, `typsphinx/template_engine.py`, `typsphinx/templates/base.typ` and
`examples/**/*.typ`. Then enumerate every `@preview/` mention repo-wide with a recursive grep over
`*.typ` and `*.py`, excluding `.git`, `.tox` and `.venv`, and compare the result against the set that
guard's own docstring documents. Record any file in the enumeration that is not in that set. One is
already expected and is **not** new: `docs/source/_typst/custom_template.typ`, an unguarded fourth
lockstep site flagged by the v0.6.4-era 30.1 review, carried in `STATE.md`'s Deferred Items and out of
this phase's scope. Confirm specifically that PR #131's `typsphinx/builder.py` changes introduced no
fifth surface — `_track_image()` is not template or import code, so they should not.

**Invariant 3 — the prep-only fence over the whole milestone.** Record
`git diff origin/main..HEAD --name-only -- typsphinx/`, which must be empty: this phase changed no
`typsphinx/` file, including the two measured `_track_image()` defects, which ship unfixed by D-27.

Write `46-SC4-INVARIANTS.md` in the phase directory, in English, with a provisioning note, one section
per invariant holding the exact command and its verbatim output, an explicit `## Anchor` section
recording `v0.7.0` → `75fd8ed` and why `87f242a` was rejected, and a `## Roll-up verdict` section. Do
not restate figures from memory — every number in the file must come from a command in the file.
  </action>
  <verify>
    <automated>test "$(git rev-parse v0.7.0^{commit} | cut -c1-7)" = "75fd8ed" && diff <(git show v0.7.0:pyproject.toml | sed -n '/^dependencies = \[/,/^\]/p') <(sed -n '/^dependencies = \[/,/^\]/p' pyproject.toml) && uv run pytest tests/test_preview_version_sync.py -q --junit-xml="${TMPDIR:-/tmp}/46-05-preview.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/46-05-preview.xml" && test -z "$(git diff origin/main..HEAD --name-only -- typsphinx/)" && grep -q '75fd8ed' .planning/phases/46-v0-7-1-release-prep-prep-only/46-SC4-INVARIANTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse v0.7.0^{commit}` resolves to a commit whose short SHA is `75fd8ed`, and the evidence file records it under an `## Anchor` heading together with the reason `87f242a` was rejected.
    - The `[project] dependencies` block diff between `v0.7.0` and HEAD produces no output.
    - The filtered `git diff v0.7.0..HEAD -- pyproject.toml` output contains exactly the `tox-uv` → `tox-uv-bare` swap, the `myst-parser>=5.0` addition, and the version-literal move — and the evidence file names each with its owning phase.
    - `uv run pytest tests/test_preview_version_sync.py` reports `failures="0"` and `errors="0"` in its JUnit report.
    - The repo-wide `@preview/` enumeration is recorded verbatim, and every file in it is either in `tests/test_preview_version_sync.py`'s documented sync surface or is `docs/source/_typst/custom_template.typ`, which the evidence file identifies as the already-known, already-deferred fourth site.
    - `git diff origin/main..HEAD --name-only -- typsphinx/` produces no output, and this is recorded.
    - `46-SC4-INVARIANTS.md` contains the headings `## Anchor`, one section per invariant, and `## Roll-up verdict`, and every figure in it is accompanied by the command that produced it.
    - `git tag -l v0.7.1` produces no output.
  </acceptance_criteria>
  <done>All three milestone invariants are asserted mechanically over the `v0.7.0`-anchored, post-merge diff, with every figure traceable to a command in the evidence file.</done>
</task>

<task type="auto">
  <name>Task 2: Discharge REL-04's in-phase preconditions and record that the requirement stays open</name>
  <precondition>`CHANGELOG.md` contains a `## [0.7.1]` heading with a non-empty body (plan 46-03 merged back) — the extractor cannot be exercised before the section exists.</precondition>
  <files>.planning/phases/46-v0-7-1-release-prep-prep-only/46-REL04-EVIDENCE.md</files>
  <read_first>
    - `.github/workflows/release.yml` lines 60-90 and 145-200 — the `validate` job's existence-and-non-emptiness call, and the `create-release` job's `Install uv` (`astral-sh/setup-uv@v7`) and `Set up Python` (`uv python install 3.12`) steps at lines 162-168 with their inline comment naming the failed run `30848860064`, sitting ahead of the `Generate release notes` step's extractor call.
    - `scripts/extract_changelog_section.py` — the whole file: the purely positional algorithm, the two failure modes (no heading found; empty body after stripping), and the ASVS V5 note explaining that `version` is only ever a string-equality comparison.
    - `.planning/REQUIREMENTS.md` § "Release" — REL-04 verbatim, including the sentence that its acceptance evidence cannot be discharged before `/gsd-complete-milestone`.
    - `.planning/todos/pending/2026-08-04-release-create-job-missing-uv-verify-end-to-end.md` — REL-04's own record, which stays in `todos/pending/`.
    - `.planning/milestones/v0.7.0-phases/41-v0-7-0-release-automation-release-prep/41-REL04-EVIDENCE.md` — the evidence-file shape.
  </read_first>
  <action>
**Precondition check 1 — the workflow fix is present and untouched.** Read
`.github/workflows/release.yml` lines 156-200 directly and transcribe them verbatim, confirming that
the `Install uv` step using `astral-sh/setup-uv@v7` and the `Set up Python` step running
`uv python install 3.12` both sit inside the `create-release` job and ahead of the `Generate release
notes` step's `uv run python scripts/extract_changelog_section.py` call. Do not eyeball this in an
editor and assert it from memory — paste the lines.

Then confirm this phase changed nothing in that file: `git diff origin/main..HEAD -- .github/workflows/release.yml`
must produce no output. This also preserves the file's own security invariant, stated in its inline
comment, that GitHub Actions expressions are passed through `env:` rather than interpolated directly
into a `run:` block — the standard mitigation against a maliciously crafted tag name executing shell
in a job holding `contents: write`.

**Precondition check 2 — exercise the extractor against the real section.** Run
`uv run python scripts/extract_changelog_section.py 0.7.1`, capturing stdout to a file and recording
the exit status; expect 0 and a non-empty body. Then run these four checks and record each:

- *Idempotency.* Run the same command a second time to a second file and `diff` the two outputs; they
  must be byte-identical, and neither invocation may create a git tag or any other side effect
  (`git tag -l v0.7.1` empty before and after, `git status --porcelain` unchanged).
- *Empty input.* Run the same script for a version that has no section — use `9.9.9` — and confirm it
  exits 1 with a message on stderr. This is the failure the `validate` job relies on to stop a release
  before the PyPI upload rather than after it.
- *Adjacency.* Extract the same region independently with `awk`, taking the lines strictly between the
  `## [0.7.1]` heading and the next line matching `^## \[`, then strip leading and trailing blank
  lines and `diff` that against the script's stdout. They must be identical, which proves the body
  carries no text from the adjacent `## [0.7.0]` section and none from `## [Unreleased]`.
- *Ordering.* Confirm the extracted body's line order matches the source region's line order — the
  `diff` above already establishes this; record it explicitly as the ordering check rather than
  leaving it implicit.

**Record the boundary in writing.** Write `46-REL04-EVIDENCE.md` in the phase directory, in English,
with a provisioning note, a `## Precondition 1 — the create-release job carries the uv setup` section,
a `## Precondition 2 — the extractor runs against the real section` section holding all five
transcripts, and a closing section headed `## REL-04 remains open` that states plainly:

- REL-04's acceptance evidence is a real tag push whose `create-release` job runs to completion, and
  only `/gsd-complete-milestone` can generate it;
- everything above is recorded as a **precondition**, never as acceptance;
- v0.7.0 reported this requirement's mechanism as done and the release then failed at exactly this
  job, which is why the boundary is drawn here;
- `.planning/todos/pending/2026-08-04-release-create-job-missing-uv-verify-end-to-end.md` stays in
  `todos/pending/` and is not filed to `completed/` by this phase;
- `.planning/REQUIREMENTS.md`'s REL-04 row stays `Pending` and is not edited by this phase (D-26 also
  keeps the file untouched for PR #131).

Do not edit `REQUIREMENTS.md`, do not move the todo record, and take no irreversible action — no
`git tag`, no tag push, no PyPI upload, no GitHub Release, no PR.
  </action>
  <verify>
    <automated>test -z "$(git diff origin/main..HEAD -- .github/workflows/release.yml)" && uv run python scripts/extract_changelog_section.py 0.7.1 > "${TMPDIR:-/tmp}/46-05-a.txt" && uv run python scripts/extract_changelog_section.py 0.7.1 > "${TMPDIR:-/tmp}/46-05-b.txt" && diff "${TMPDIR:-/tmp}/46-05-a.txt" "${TMPDIR:-/tmp}/46-05-b.txt" && test -s "${TMPDIR:-/tmp}/46-05-a.txt" && ! uv run python scripts/extract_changelog_section.py 9.9.9 >/dev/null 2>&1 && test -f .planning/todos/pending/2026-08-04-release-create-job-missing-uv-verify-end-to-end.md && grep -q 'REL-04 remains open' .planning/phases/46-v0-7-1-release-prep-prep-only/46-REL04-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `46-REL04-EVIDENCE.md` transcribes `.github/workflows/release.yml` lines 156-200 verbatim, and those lines contain `astral-sh/setup-uv@v7` and `uv python install 3.12` inside the `create-release` job ahead of the `extract_changelog_section.py` call.
    - `git diff origin/main..HEAD -- .github/workflows/release.yml` produces no output.
    - `uv run python scripts/extract_changelog_section.py 0.7.1` exits 0 and writes a non-empty body to stdout.
    - Two consecutive invocations of that command produce byte-identical stdout (`diff` exits 0).
    - `uv run python scripts/extract_changelog_section.py 9.9.9` exits 1 and writes a message to stderr.
    - An independent `awk` extraction of the lines strictly between `## [0.7.1]` and the next `^## \[` heading, blank-stripped, is byte-identical to the script's stdout.
    - `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` are both empty before and after the extractor runs, and `git status --porcelain` shows no change caused by them.
    - `.planning/todos/pending/2026-08-04-release-create-job-missing-uv-verify-end-to-end.md` still exists in `todos/pending/`.
    - `git diff --name-only -- .planning/REQUIREMENTS.md` produces no output.
    - `46-REL04-EVIDENCE.md` contains a section headed `## REL-04 remains open` naming `/gsd-complete-milestone` as the only place its acceptance evidence can be generated.
  </acceptance_criteria>
  <done>REL-04's two in-phase preconditions are verified and transcribed, the extractor's empty, idempotency, adjacency and ordering behaviours are each measured, and the requirement's openness is stated in writing rather than implied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an attacker-controllable git tag name → `release.yml`'s `run:` blocks | Git permits backticks, `$()` and quotes in a tag name; the workflow runs with `contents: write` and `id-token: write`. |
| `CHANGELOG.md` text → the published GitHub Release body | The extractor's stdout becomes the release notes verbatim. |
| the milestone diff → the `### Verified` claims in the CHANGELOG | Three published claims rest on this sweep's measurements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-06 | Tampering / Elevation of privilege | `.github/workflows/release.yml` GitHub Actions expression handling | high | mitigate | The file's own invariant — every expression passed through `env:`, never interpolated into a `run:` block — is preserved by asserting `git diff origin/main..HEAD -- .github/workflows/release.yml` is empty, so the merge and this phase changed nothing in it. |
| T-46-13 | Repudiation | a published `### Verified` claim resting on a figure nobody re-measured | high | mitigate | Every figure in `46-SC4-INVARIANTS.md` must be accompanied by the command that produced it; D-14's superseded figures are explicitly rejected in favour of a fresh D-21 measurement on the post-merge tree. |
| T-46-14 | Information disclosure | reporting REL-04 closed on the strength of a correct workflow file | high | mitigate | The evidence file's closing section states the boundary in writing, the REL-04 todo record is asserted still present in `todos/pending/`, and `REQUIREMENTS.md` is asserted unedited. This is the precise error v0.7.0 made. |
| T-46-02 | Tampering | `typsphinx/` source across the whole milestone | high | mitigate | Invariant 3 asserts `git diff origin/main..HEAD --name-only -- typsphinx/` is empty. |
| T-46-SC | Tampering | npm/pip/cargo installs | medium | accept | No package is added; `uv sync --extra dev` installs only from the existing lock, and invariant 1 asserts `[project] dependencies` is byte-identical to `v0.7.0`'s. RESEARCH.md records the Package Legitimacy Gate as not applicable — zero packages evaluated, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. |
</threat_model>

<verification>
- `git rev-parse v0.7.0^{commit}` starts with `75fd8ed`.
- The `[project] dependencies` block diff between `v0.7.0` and HEAD is empty.
- `uv run pytest tests/test_preview_version_sync.py` passes with zero failures.
- `git diff origin/main..HEAD --name-only -- typsphinx/` and `git diff origin/main..HEAD -- .github/workflows/release.yml` are both empty.
- `extract_changelog_section.py 0.7.1` exits 0, is idempotent, and its output matches an independent `awk` extraction; `… 9.9.9` exits 1.
- The REL-04 todo record is still in `todos/pending/`; `REQUIREMENTS.md` is unedited.
- `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` are both empty.
</verification>

<success_criteria>
- All three milestone invariants are asserted mechanically over the `v0.7.0`-anchored, post-merge diff.
- REL-04's two in-phase preconditions are verified and transcribed verbatim.
- The extractor's empty, idempotency, adjacency and ordering behaviours are each measured against the real `## [0.7.1]` section.
- Both evidence files state REL-04 as **open**, with `/gsd-complete-milestone` named as the only place its acceptance evidence can be generated.
- No irreversible action was taken.
</success_criteria>

<output>
Create `.planning/phases/46-v0-7-1-release-prep-prep-only/46-05-SUMMARY.md` when done.
</output>
