---
phase: 46-v0-7-1-release-prep-prep-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - tests/test_docs_contract_claims_gate.py
  - .planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md
autonomous: true
requirements: [REL-06]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`git merge-base --is-ancestor origin/main HEAD` exits 0 — commit `9b2b76b` (the PR #131 merge) is contained in the milestone branch, so the tree proven green and the tree eventually tagged are the same tree (D-20)."
    - "`tests/test_docs_contract_claims_gate.py`'s `_discovered_claim_pages()` compares forward-slash keys on every platform, so the reviewed/excluded page sets can match on Windows as well as on POSIX (D-22)."
    - "A live CI run dispatched on the pushed commit reports `success` for both `Test Python 3.12 on windows-latest` and `Test Python 3.13 on windows-latest` — the two lanes that were the only failures in baseline run `31445582363` (D-23 run 1)."
    - "No file under `typsphinx/` differs between `origin/main` and the milestone branch HEAD — the prep-only fence holds through the merge (D-03, D-27)."
    - "`git tag -l v0.7.1` is empty after this plan — nothing irreversible was taken."
  artifacts:
    - path: "tests/test_docs_contract_claims_gate.py"
      provides: "platform-independent claim-page key normalisation"
      contains: "as_posix()"
    - path: "CHANGELOG.md"
      provides: "a merged `## [Unreleased]` block carrying both the local Planned-for-Future-Releases list and origin/main's Issue #130 entry"
      contains: "Issue #130"
    - path: ".planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md"
      provides: "verbatim transcripts for D-23 run 1, plus an empty placeholder section for run 2"
      contains: "D-23 run 1"
  key_links:
    - from: "tests/test_docs_contract_claims_gate.py `_discovered_claim_pages()`"
      to: "tests/test_docs_contract_claims_gate.py `EXCLUDED_CLAIM_PAGES` / `REVIEWED_CLAIM_PAGES`"
      via: "set comparison between discovered keys and the reviewed/excluded forward-slash literals; both sides must use the same separator"
      pattern: "as_posix\\(\\)"
    - from: "the pushed milestone-branch commit"
      to: "the dispatched `ci.yml` run"
      via: "`gh workflow run ci.yml --ref gsd/v0.7.1-bug-fix-round` — this repository's CI triggers on push only for `main`/`develop`, so the run must be dispatched by hand"
      pattern: "workflow_dispatch"
---

<objective>
Take `origin/main` (`9b2b76b`, the PR #131 merge) into the milestone branch, repair the Windows-only
path-separator defect in the contract-claims gate, and prove the whole phase's evidence loop live —
edit, commit, push, dispatch CI, read the run — on the thinnest slice that touches every layer this
phase will use.

Purpose: this is the phase's tracer. D-20 puts the merge at the head of the phase so that the tree
SC#3 proves green and the tree `/gsd-complete-milestone` eventually tags are the same tree. D-22
repairs the only red lanes on the branch, in a test module (not `typsphinx/`), so the prep-only fence
holds. D-23 run 1 exists precisely because the Windows repair **cannot be verified locally** — there
is no Windows on this machine — and a missed repair discovered at the end of the phase would land its
retry commits after the version bump.

Output: a milestone branch that contains `origin/main`, a platform-independent claim-page comparison,
a live CI run with both `windows-latest` lanes green, and `46-CI-EVIDENCE.md` holding the transcripts.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-CONTEXT.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md
@CLAUDE.md
</context>

<assumption_delta_decision>
**Verdict: `no-change`.**

The deterministic `assumption-delta` scan fired on the term **"second"**, in the ROADMAP Phase 46
prose "the publish half — merge → tag → `release.yml` → PyPI + GitHub Release, plus the standing
**second** tag on `typsphinx-doc-translations`".

That noun is the second git tag pushed on the separate `typsphinx-doc-translations` repository — a
two-repository tagging cost this project adopted at the v0.6.4 close (v0.6.4 D-07) and has paid at
every release since. It is a pre-existing, unchanged fact about the *publish* half, which this phase
does not execute. It is **not** a singular→plural identity-model transition inside this phase: no
entity in this phase gains a second instance, no data shape changes from one to many, and no
architectural promotion is warranted. Recording `no-change` rather than fabricating one.
</assumption_delta_decision>

## Artifacts this phase produces

This phase produces no code symbols. Every artifact below is a **file or a document section** created
by this phase and therefore excluded from drift verification against pre-existing sources:

| Artifact | Kind | Owning plan |
|---|---|---|
| `## [0.7.1]` heading in `CHANGELOG.md` | document section | 46-03 |
| `### Removed` section under `## [0.7.1]` (first in this file's history) | document section | 46-03 |
| `[0.7.1]: …/releases/tag/v0.7.1` tail link-block line | document line | 46-03 |
| `Migrating from 0.7.0 to 0.7.1` section in `docs/source/changelog.rst` | document section | 46-03 |
| `"0.7.1"` entry in `RELEASE_VERSIONS` (`tests/test_changelog_page_gate.py`) | test data constant | 46-03 |
| `.planning/phases/46-…/46-CI-EVIDENCE.md` | planning artifact | 46-01, 46-04 |
| `.planning/phases/46-…/46-BUMP-EVIDENCE.md` | planning artifact | 46-02 |
| `.planning/phases/46-…/46-GREEN-TREE-EVIDENCE.md` | planning artifact | 46-04 |
| `.planning/phases/46-…/46-SC4-INVARIANTS.md` | planning artifact | 46-05 |
| `.planning/phases/46-…/46-REL04-EVIDENCE.md` | planning artifact | 46-05 |
| `.planning/phases/46-…/46-RELEASE-EVIDENCE.md` (the roll-up; deliberately **not** `46-VERIFICATION.md`, D-15) | planning artifact | 46-06 |
| `.planning/phases/46-…/46-HANDOFF.md` | planning artifact | 46-06 |
| `.planning/phases/46-…/COVERAGE.md` | planning artifact | written at plan time |

<!-- planner-discipline-allow: ^## \[0.7.1\] -->
<!-- planner-discipline-allow: ## [0.7.1] -->

<tasks>

<task type="tracer">
  <name>Task 1: Merge `origin/main`, repair the Windows claim-page keys, and prove the loop on a live CI run</name>
  <precondition>`origin/gsd/v0.7.1-bug-fix-round` is an ancestor of HEAD (a plain fast-forward push is possible), and `gh auth status` reports an account authorised to dispatch workflows on `YuSabo90002/typsphinx` — runs `31445582363` and `31444883849` on this branch were `workflow_dispatch`, so the permission has been exercised before.</precondition>
  <reversibility rating="reversible">A merge commit on a local milestone branch and a one-line normalisation in a test module; the merge commit can be dropped and the line reverted at any point before anything is tagged (D-20's own rating).</reversibility>
  <files>CHANGELOG.md, tests/test_docs_contract_claims_gate.py</files>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — provisioning is mandatory, not conditional.
    - `CHANGELOG.md` lines 1-20 — the single `## [Unreleased]` heading (line 8) and its `### Planned for Future Releases` list, and the `## [0.7.0]` transition immediately below it.
    - `tests/test_docs_contract_claims_gate.py` lines 99-175 — `PUBLISHED_PARAM_NAMES`, `CLAIM_ROUTE_TOKENS`, `REVIEWED_CLAIM_PAGES`, `EXCLUDED_CLAIM_PAGES`, `_iter_rst_pages`, `_page_makes_contract_claim`, `_discovered_claim_pages`.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md` § "Pattern 3: The D-20 merge-conflict mechanics" and § "Pitfall 3".
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md` § `tests/test_docs_contract_claims_gate.py:170` and § `CHANGELOG.md`.
    - `.planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md` — the verbatim CI failure text from run `31445582363`, job `93638966551`.
    - `.github/workflows/ci.yml` lines 1-25 — the trigger block. **CI auto-triggers only on `main` and `develop`; a push to the milestone branch does not start it.** The job-name template is `Test Python ${{ matrix.python-version }} on ${{ matrix.os }}`.
  </read_first>
  <action>
Provision the worktree before anything else, per `CLAUDE.md` § "Worktree-isolated execution": run
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then invoke every subsequent
Python/pytest command through `uv run`. Use the bare `uv` on PATH; `.venv/bin/uv` no longer exists
after Phase 45.2 and must not be reintroduced.

**Merge (D-20).** Run `git fetch origin`, then `git merge --no-ff origin/main`. The read-only dry run
`git merge-tree --write-tree HEAD origin/main` was measured twice (CONTEXT D-20, RESEARCH Pattern 3)
and reports exactly one conflict, `CHANGELOG.md`; `typsphinx/builder.py` and `tests/test_builder.py`
auto-merge clean. If the actual merge reports any conflict beyond `CHANGELOG.md`, stop and report —
do not improvise a resolution.

**Resolve the conflict by hand.** Never use `-X ours` or `-X theirs`: both silently discard one
side's real content, and one side here is an external contributor's own account of the fix. The
conflict sits in the file's single top-of-file `## [Unreleased]` block, at line 8, immediately after
the Keep-a-Changelog preamble. Measured this session: the file contains exactly **one**
`## [Unreleased]` heading — the tail block's `[Unreleased]:` line is a markdown link definition, not a
heading, and `### Planned for Future Releases` is a subsection under the one heading, not a second
one. (`scripts/extract_changelog_section.py`'s module docstring still asserts there are two headings;
that assertion is stale. Do **not** edit the script — the phase's fence permits exactly two
non-planning code edits and this is not one of them. Record the observation in Task 2 instead.)

Resolve so the merged block keeps BOTH sides under the one `## [Unreleased]` heading, in this order:
first `origin/main`'s `### Fixed` bullet for "Absolute image URIs from Sphinx's image
converter/downloader break copy and path resolution (Issue #130)" **verbatim**, all of its body lines
intact; then the local side's `### Planned for Future Releases` list with its five items — BibTeX/
bibliography support, Glossary generation, Index generation, Pre-commit hooks, Additional Typst
Universe template integration. Compressing that bullet to house granularity and moving it into a new
`## [0.7.1]` section is plan 46-03's job (D-24, D-25); **do not create a `## [0.7.1]` heading here**,
and do not touch the tail link block.

**Repair the claim-page keys (D-22).** In `_discovered_claim_pages()`, the set comprehension's key
expression currently wraps `page.relative_to(REPO_ROOT)` in `str(...)`, which renders backslashes on
Windows and can therefore never equal the forward-slash literals in `REVIEWED_CLAIM_PAGES` or the keys
of `EXCLUDED_CLAIM_PAGES`. Replace that key expression with `.as_posix()` called on the
`relative_to(REPO_ROOT)` result, so the discovered keys are forward-slash on every platform. Leave
`REVIEWED_CLAIM_PAGES` and `EXCLUDED_CLAIM_PAGES` exactly as they are — both already hold
forward-slash literals, which is now the single normalisation both sides of the comparison share.
Change nothing else in the module: `_iter_rst_pages`, `_page_makes_contract_claim`,
`PUBLISHED_PARAM_NAMES`, `CLAIM_ROUTE_TOKENS`, and every test body stay byte-identical.

Note the premise correction that governs this repair (RESEARCH Pitfall 3, `46-VALIDATION.md`
§ "Correction carried forward from research"): `docs/source/changelog.rst` **already** satisfies
`_page_makes_contract_claim()` today via its existing "Migrating from 0.5.x to 0.6.x" section, and all
8 tests in this module pass on Linux right now. The Windows failure is a pure separator mismatch. It
follows that this repair is what fixes Windows, independent of plan 46-03's migration-section content,
and that neither edit has to land before the other.

**Touch nothing under `typsphinx/` (D-03, D-27).** In particular leave `TypstBuilder._track_image()`
and `post_process_images()` exactly as `origin/main` has them: both measured defects in that code ship
in v0.7.1 unfixed by owner decision, and are disclosed internally in `46-HANDOFF.md` only.

**Local sanity before the push.** Run `uv run pytest tests/test_docs_contract_claims_gate.py -v` and
`uv run pytest tests/test_changelog_extraction.py -q`. The first must report 8 passed with no skips.
This is a pre-push smoke check only — it proves the repair does not regress POSIX; it cannot prove
the Windows behaviour, which is what the CI run below is for.

Commit the merge resolution and the repair.

**Push and dispatch (D-23 run 1).** Push the milestone-branch ref with
`git push origin HEAD:refs/heads/gsd/v0.7.1-bug-fix-round`. This is a plain fast-forward; if it is
rejected, stop and report — never force-push, milestone invariant #5 puts this branch on `origin`
already. Record the pushed SHA with `git rev-parse HEAD`.

CI does not start on that push (`ci.yml` triggers on push only for `main`/`develop`), so dispatch it:
`gh workflow run ci.yml --ref gsd/v0.7.1-bug-fix-round`. Then poll
`gh run list --workflow=ci.yml --branch gsd/v0.7.1-bug-fix-round --limit 5 --json databaseId,headSha,event,status`
until a `workflow_dispatch` run whose `headSha` equals the pushed SHA appears, and export its
`databaseId` as `RUN_ID`. Wait with `gh run watch "$RUN_ID"`, then read every job's conclusion with
`gh run view "$RUN_ID" --json jobs`.

The two lanes this task exists to prove are `Test Python 3.12 on windows-latest` and
`Test Python 3.13 on windows-latest`. Baseline for the comparison is run `31445582363`
(`conclusion: failure`), whose only failing jobs were those same two. Capture the full job list, not
just the two — a regression anywhere else is equally disqualifying at this point.

Take **no** irreversible action: no `git tag`, no tag push, no PyPI upload, no GitHub Release, no PR.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor origin/main HEAD && test -z "$(git diff origin/main..HEAD --name-only -- typsphinx/)" && uv run pytest tests/test_docs_contract_claims_gate.py -q --junit-xml="${TMPDIR:-/tmp}/46-01-claims.xml" && grep -q 'skipped="0"' "${TMPDIR:-/tmp}/46-01-claims.xml" && gh run view "$RUN_ID" --json jobs --jq '[.jobs[]|select(.name|test("windows-latest"))|.conclusion]|unique|@csv' | grep -qx '"success"'</automated>
  </verify>
  <acceptance_criteria>
    - `git merge-base --is-ancestor origin/main HEAD` exits 0.
    - `git show HEAD:typsphinx/builder.py | grep -c _track_image` prints `4` (PR #131's builder code arrived and is unmodified).
    - `git diff origin/main..HEAD --name-only -- typsphinx/` produces no output.
    - `grep -c 'as_posix()' tests/test_docs_contract_claims_gate.py` returns at least 1, and that occurrence is inside `_discovered_claim_pages`.
    - `uv run pytest tests/test_docs_contract_claims_gate.py -q --junit-xml=<tmp>` produces a JUnit `testsuite` element carrying `skipped="0"` and `failures="0"`, with 8 tests.
    - `grep -c '^## \[Unreleased\]' CHANGELOG.md` returns `1`.
    - `grep -c '^## \[0.7.1\]' CHANGELOG.md` returns `0` (the curated entry is plan 46-03's).
    - `grep -c 'Issue #130' CHANGELOG.md` returns at least 1, and `grep -c 'Planned for Future Releases' CHANGELOG.md` returns 1.
    - `git rev-parse HEAD` equals `git rev-parse origin/gsd/v0.7.1-bug-fix-round`.
    - `gh run view "$RUN_ID" --json jobs` reports `success` for `Test Python 3.12 on windows-latest` and for `Test Python 3.13 on windows-latest`, and reports no job whose conclusion is `failure`.
    - `git tag -l v0.7.1` produces no output.
  </acceptance_criteria>
  <done>`origin/main` is an ancestor of the milestone branch, the contract-claims gate compares forward-slash keys on every platform, and a live CI run on the pushed commit reports both `windows-latest` lanes `success` against a baseline run in which those were the only failures.</done>
</task>

<task type="auto">
  <name>Task 2: Record D-23 run 1's evidence and open the run-2 placeholder</name>
  <files>.planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md</files>
  <read_first>
    - `.planning/milestones/v0.7.0-phases/41-v0-7-0-release-automation-release-prep/41-GREEN-TREE-EVIDENCE.md` — the sibling per-plan evidence-file shape the roll-up later cites.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md` § "Evidence culture" and § "Provisioning-note convention".
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-VALIDATION.md` § "Manual-Only Verifications" row M1 — what this run does and does not prove.
  </read_first>
  <action>
Create `46-CI-EVIDENCE.md` in the phase directory. Write it in English. Structure it as:

A title line naming the phase and "CI Evidence (D-23)". Then a **provisioning note** stating that
every command was run inside this plan's isolated git worktree after
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, per `CLAUDE.md`
§ "Worktree-isolated execution", and invoked through `uv run` where applicable.

Then a section `## D-23 run 1 — the Windows check run` containing, verbatim and unparaphrased:
the `git merge --no-ff origin/main` output (the conflict list); the resolved-conflict summary naming
which content came from which side; the `git rev-parse HEAD` pushed SHA; the
`git push origin HEAD:refs/heads/gsd/v0.7.1-bug-fix-round` output; the `gh workflow run` command; the
run id and its `https://github.com/YuSabo90002/typsphinx/actions/runs/<id>` URL; and a
`### Job conclusions` subsection holding the complete `gh run view --json jobs` name/conclusion table.
Immediately below it, a `### Baseline` subsection recording run `31445582363` (`failure`) and the fact
that its only failing jobs were the two `windows-latest` lanes, so the delta this run demonstrates is
exactly those two lanes flipping to `success`.

Add a `### What this run does not prove` subsection stating plainly, per `46-VALIDATION.md` M1, that
the local 8/8-green run of `tests/test_docs_contract_claims_gate.py` proves only non-regression on
POSIX — backslash path rendering is Windows `pathlib` behaviour and is not reproducible on this
machine — so this CI run, not the local run, is D-22's acceptance evidence.

Add a `### Observation carried forward` subsection recording that
`scripts/extract_changelog_section.py`'s module docstring claims `CHANGELOG.md` carries two
`## [Unreleased]` headings, and that a direct count on the merged file returns one. State that the
script was deliberately **not** edited, because this phase permits only two non-planning code edits
(D-22's repair and the `RELEASE_VERSIONS` append), and that the observation is carried into
`46-HANDOFF.md` § "Not done in this phase, by design". Note that the script's algorithm is purely
positional and is therefore correct either way — only its docstring is stale.

Finally add a section `## D-23 run 2 — the authority run` whose entire body is the single line
`_Pending — filled by plan 46-04._`. Do not speculate about run 2's outcome.

Do not write anything to a file named `46-VERIFICATION.md` (D-15): that name is reserved by
`/gsd-verify-work` and is overwritten wholesale when the verifier runs.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md && grep -q '^## D-23 run 1' .planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md && grep -q '^## D-23 run 2' .planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md && grep -q '31445582363' .planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md && grep -q 'Test Python 3.13 on windows-latest' .planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md && test ! -e .planning/phases/46-v0-7-1-release-prep-prep-only/46-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-CI-EVIDENCE.md` exists and is written entirely in English.
    - It contains the headings `## D-23 run 1 — the Windows check run`, `### Job conclusions`, `### Baseline`, `### What this run does not prove`, `### Observation carried forward`, and `## D-23 run 2 — the authority run`.
    - It contains the literal `31445582363`, the literal `Test Python 3.12 on windows-latest`, the literal `Test Python 3.13 on windows-latest`, and the pushed commit SHA produced by task 1.
    - The `## D-23 run 2 — the authority run` section's body is exactly `_Pending — filled by plan 46-04._`.
    - It contains a provisioning note naming `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.
    - No file named `46-VERIFICATION.md` exists in the phase directory.
  </acceptance_criteria>
  <done>D-23 run 1's transcripts, its baseline comparison, and an honest statement of what it does and does not prove are on disk, with a named placeholder awaiting plan 46-04's authority run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `origin/main` → the milestone branch | Content authored outside this milestone's phase chain (PR #131, an external contributor's code, merged over a `CHANGES_REQUESTED` review) crosses into the release tree here. |
| local working tree → `origin` | The push publishes the release-candidate tree to a remote that CI, and later the release automation, reads. |
| GitHub Actions run output → this phase's evidence | Job conclusions read from `gh` become the phase's SC#3 record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01 | Tampering | `CHANGELOG.md` merge-conflict resolution | medium | mitigate | Resolve by hand, never `-X ours`/`-X theirs`; acceptance asserts BOTH sides survive (`Issue #130` present AND `Planned for Future Releases` present). |
| T-46-02 | Tampering | `typsphinx/` source during the merge | high | mitigate | `test -z "$(git diff origin/main..HEAD --name-only -- typsphinx/)"` in `<verify>`, plus an acceptance criterion pinning `_track_image` occurrence count to `origin/main`'s. |
| T-46-03 | Tampering | prep/publish fence (an accidental tag or force-push) | high | mitigate | The task explicitly forbids `git tag`, tag push, PyPI, GitHub Release, PR; the push is fast-forward-only with force explicitly forbidden; `git tag -l v0.7.1` asserted empty. |
| T-46-07 | Repudiation | CI evidence asserted from memory rather than from a run | medium | mitigate | Evidence culture: task 2 records the run id, URL, and full job table verbatim, and states in writing what the local run does not prove. |
| T-46-SC | Tampering | npm/pip/cargo installs | medium | accept | No package is added by this phase. `uv sync --extra dev` installs only from the existing `uv.lock`, and `git diff v0.7.0..HEAD -- pyproject.toml` shows `[project] dependencies` byte-identical (re-asserted in plan 46-05). RESEARCH.md's Package Legitimacy Audit records the gate as not applicable; no `[ASSUMED]`/`[SUS]`/`[SLOP]` package exists to gate. |
</threat_model>

<verification>
- `git merge-base --is-ancestor origin/main HEAD` exits 0.
- `uv run pytest tests/test_docs_contract_claims_gate.py -v` reports 8 passed, 0 skipped, 0 failed.
- `gh run view "$RUN_ID" --json jobs` shows both `windows-latest` lanes `success` and no job `failure`.
- `git diff origin/main..HEAD --name-only -- typsphinx/` is empty.
- `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` are both empty.
</verification>

<success_criteria>
- The milestone branch contains `origin/main` (`9b2b76b`), with the single `CHANGELOG.md` conflict resolved by hand and both sides' content preserved.
- `_discovered_claim_pages()` normalises its comparison keys with `as_posix()`.
- A live, dispatched CI run on the pushed commit reports both `windows-latest` lanes `success`.
- `46-CI-EVIDENCE.md` holds run 1's verbatim transcripts and an unfilled, named placeholder for run 2.
- No irreversible action was taken.
</success_criteria>

<output>
Create `.planning/phases/46-v0-7-1-release-prep-prep-only/46-01-SUMMARY.md` when done.
</output>
