---
phase: 46-v0-7-1-release-prep-prep-only
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - pyproject.toml
  - uv.lock
  - README.md
  - .planning/phases/46-v0-7-1-release-prep-prep-only/46-BUMP-EVIDENCE.md
autonomous: true
requirements: [REL-06]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`pyproject.toml` is the sole `0.7.1` version literal; `README.md`'s Status line and `uv.lock`'s own `typsphinx` entry moved with it, and no second hardcoded version string was introduced (SC#1, D-01)."
    - "`uv run python -c \"import typsphinx; print(typsphinx.__version__)\"` prints `0.7.1` — the editable-install `.dist-info`/`.pth` pair was regenerated, not merely the literal edited."
    - "`uv sync --extra dev --locked` exits 0, proving `uv.lock` and `pyproject.toml` agree after the bump."
    - "The three version-sync guard tests (`test_version_matches_pyproject_toml`, `test_readme_version_sync.py`, `test_preview_version_sync.py`) are green on the post-bump tree."
    - "The `@preview` package count is still four and no new lockstep site joined the sync surface — the bump changed no template or import code."
  artifacts:
    - path: "pyproject.toml"
      provides: "the sole version literal for the release"
      contains: "version = \"0.7.1\""
    - path: "README.md"
      provides: "the human-readable release status line, moved in lockstep"
      contains: "Stable (v0.7.1)"
    - path: "uv.lock"
      provides: "the regenerated lock whose own typsphinx entry carries the new version"
      contains: "0.7.1"
    - path: ".planning/phases/46-v0-7-1-release-prep-prep-only/46-BUMP-EVIDENCE.md"
      provides: "SC#1's verbatim transcripts"
      contains: "SC#1"
  key_links:
    - from: "pyproject.toml `[project] version`"
      to: "typsphinx.__version__"
      via: "importlib.metadata.version(\"typsphinx\") reads the installed .dist-info, which only `uv sync` regenerates — editing the literal alone does not move it"
      pattern: "importlib\\.metadata"
    - from: "pyproject.toml `[project] version`"
      to: "README.md Status line"
      via: "tests/test_readme_version_sync.py asserts the two agree, so they can never be edited independently"
      pattern: "Stable \\(v0\\.7\\.1\\)"
---

<objective>
Move the release version from `0.7.0` to `0.7.1` across the three surfaces that carry it, and
regenerate the editable-install metadata so `typsphinx.__version__` actually reports the new value.

Purpose: discharges ROADMAP Phase 46 SC#1. `0.7.1` is locked by D-01 — the `0.8.0` alternative was
argued in full and declined, which is why the CHANGELOG (plan 46-03) is the only surface that can
carry the breakage warning. This plan is deliberately kept clear of `CHANGELOG.md`: D-20 rejected
crossing `uv.lock` regeneration with CHANGELOG work, so the two run as sibling plans in the same wave
with disjoint files.

Output: a post-bump tree whose version literal, lockfile, README status line, and installed package
metadata all agree, with the guard-test transcripts recorded.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-CONTEXT.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md
@.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `46-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan
produces no new file in the source tree — it moves an existing literal in three existing files and
adds one planning artifact, `.planning/phases/46-…/46-BUMP-EVIDENCE.md`.

<tasks>

<task type="auto">
  <name>Task 1: Move the version literal across all three surfaces and regenerate the editable install</name>
  <files>pyproject.toml, README.md, uv.lock</files>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — provisioning is mandatory.
    - `pyproject.toml` lines 1-60 — the `[project]` table (`version` at line 7), `[project] dependencies`, and the `dev` / `docs` optional-dependency extras.
    - `README.md` lines 335-345 — the `**Status**` line at line 342 and the `**Python**` line immediately below it.
    - `typsphinx/__init__.py` lines 1-30 — the `importlib.metadata.version("typsphinx")` derivation and its `PackageNotFoundError` fallback. Read only; do not edit.
    - `tests/test_readme_version_sync.py` — the assertion that couples the two literals.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-RESEARCH.md` § "Pattern 1: Version-literal bump and editable-install metadata regeneration".
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md` § `pyproject.toml:7`, § `uv.lock`, § `README.md:342`.
  </read_first>
  <action>
Provision the worktree first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then
run everything through `uv run` where applicable. Use the bare `uv` on PATH — never `.venv/bin/uv`,
which no longer exists post-Phase-45.2.

Edit `pyproject.toml` line 7 from `version = "0.7.0"` to `version = "0.7.1"`. This is the repository's
sole version literal (re-measured at discussion time); `typsphinx.__version__` derives from it
indirectly through `importlib.metadata.version("typsphinx")` in `typsphinx/__init__.py`, never from a
second hardcoded string. Do not add one.

Edit `README.md` line 342 from `**Status**: Stable (v0.7.0) - Production ready` to
`**Status**: Stable (v0.7.1) - Production ready`. Change only the version token; leave the rest of the
line and the `**Python**: 3.12+ | **Sphinx**: 9.1+ | **Typst**: 0.15+` line below it untouched.

Run `uv lock`. This regenerates the lockfile so its own first-party `typsphinx` entry — the block
under `name = "typsphinx"` with `source = { editable = "." }`, currently carrying `version = "0.7.0"`
around line 1467 — picks up the new value. Expect no other line in `uv.lock` to move: this phase adds
and removes no dependency.

Run `uv sync --extra dev --locked`. This does two jobs at once: `--locked` fails loudly if the lock
and `pyproject.toml` disagree (which is the acceptance criterion CONTEXT.md's Claude's Discretion
section names for the regeneration procedure), and the sync regenerates `.venv`'s installed-package
metadata — the `typsphinx-0.7.1.dist-info/` directory and the `__editable__.typsphinx-0.7.1.pth` /
`__editable___typsphinx_0_7_1_finder.py` pair, whose filenames literally embed the version. Editing
`pyproject.toml` alone does not change what `importlib.metadata.version()` returns; this step is what
does.

Confirm the read-back with `uv run python -c "import typsphinx; print(typsphinx.__version__)"`,
expecting `0.7.1`, and with `uv lock --check`, expecting exit 0.

Do not edit any other file. `CHANGELOG.md`, `docs/source/changelog.rst` and
`tests/test_changelog_page_gate.py` belong to plan 46-03, which runs in this same wave — touching them
here would create a merge-back collision. Nothing under `typsphinx/` may change (D-03, D-27), and no
tag, tag push, PyPI upload, GitHub Release or PR may be created.
  </action>
  <verify>
    <automated>uv lock --check && uv sync --extra dev --locked && uv run python -c "import typsphinx, sys; sys.exit(0 if typsphinx.__version__ == '0.7.1' else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "0.7.1"$' pyproject.toml` returns `1`.
    - `grep -c 'Stable (v0.7.1) - Production ready' README.md` returns `1`.
    - `grep -A2 'name = "typsphinx"' uv.lock` shows `version = "0.7.1"` and `source = { editable = "." }`.
    - `uv lock --check` exits 0.
    - `uv sync --extra dev --locked` exits 0.
    - `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints exactly `0.7.1`.
    - `git diff --name-only` lists exactly `README.md`, `pyproject.toml`, `uv.lock` — no fourth path.
    - `diff <(git show HEAD:pyproject.toml | sed -n '/^dependencies = \[/,/^\]/p') <(sed -n '/^dependencies = \[/,/^\]/p' pyproject.toml)` produces no output.
  </acceptance_criteria>
  <done>All three version surfaces read `0.7.1`, the lock and manifest agree under `--locked`, and the installed package metadata reports `0.7.1` on import.</done>
</task>

<task type="auto">
  <name>Task 2: Run the version-sync guard battery and record SC#1's evidence</name>
  <files>.planning/phases/46-v0-7-1-release-prep-prep-only/46-BUMP-EVIDENCE.md</files>
  <read_first>
    - `tests/test_extension.py` around `def test_version_matches_pyproject_toml` — what the drift guard actually compares.
    - `tests/test_preview_version_sync.py` — the four-surface `@preview` lockstep guard and its own docstring naming those surfaces.
    - `CLAUDE.md` § "The `@preview` version-sync hazard" — the three-place lockstep this guard defends.
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-PATTERNS.md` § "Evidence culture" and § "Provisioning-note convention".
    - `.planning/milestones/v0.7.0-phases/41-v0-7-0-release-automation-release-prep/41-RELEASE-EVIDENCE.md` § SC#1 — the evidence-section shape the roll-up later cites.
  </read_first>
  <action>
Run the three guard modules and capture their output verbatim:
`uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml -v`,
`uv run pytest tests/test_readme_version_sync.py -v`, and
`uv run pytest tests/test_preview_version_sync.py -v`. All must pass with zero skips.

Do **not** run `tox -e py312` anywhere: on this machine `uv venv -p cpython3.12` downloads a
standalone CPython whose ELF the NixOS stub loader rejects (exit 127), so that environment cannot even
provision. If a broader local pytest sanity spot-check is wanted, use `uv run pytest` directly or
`tox -e py313`, which matches this machine's system interpreter. The authority for the full matrix is
CI (D-11), collected by plan 46-04 — not this plan.

Then write `46-BUMP-EVIDENCE.md` in the phase directory, in English, with:

- a provisioning note naming `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and
  the `uv run` invocation convention, per `CLAUDE.md` § "Worktree-isolated execution";
- a section `## SC#1 — version-literal lockstep` holding the before/after values of the three
  surfaces (`pyproject.toml` line 7, `README.md` line 342, `uv.lock`'s `typsphinx` entry), the
  `uv lock` and `uv sync --extra dev --locked` transcripts, the `uv lock --check` transcript, and the
  `python -c "import typsphinx"` read-back;
- a section `## Guard tests` holding all three pytest transcripts verbatim, each with its own
  summary line;
- a section `## Invariant spot-check` recording that `tests/test_preview_version_sync.py` passed and
  that this plan changed no template, writer, or import code, so the four bundled `@preview` package
  versions and their sync surfaces are untouched — with the full mechanical sweep deferred to plan
  46-05 (D-21), which this section must cite rather than duplicate;
- a section `## Executed versus skipped` stating plainly that `tox -e py312` was not run and why
  (RESEARCH.md Pitfall 1), and that lint/type/full-matrix authority belongs to CI per D-11
  amendment (b).

Never assert a command's result from memory — run it and paste the transcript. Where a result is
genuinely unavailable, record `human_needed` honestly rather than inferring it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_extension.py::test_version_matches_pyproject_toml tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q --junit-xml="${TMPDIR:-/tmp}/46-02-guards.xml" && grep -q 'skipped="0"' "${TMPDIR:-/tmp}/46-02-guards.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/46-02-guards.xml" && grep -q '^## SC#1' .planning/phases/46-v0-7-1-release-prep-prep-only/46-BUMP-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The combined guard run produces a JUnit `testsuite` element carrying `skipped="0"`, `failures="0"` and `errors="0"`, with at least 5 tests (1 version-match + 1 readme-sync + 3 preview-sync).
    - `.planning/phases/46-v0-7-1-release-prep-prep-only/46-BUMP-EVIDENCE.md` exists, is written entirely in English, and contains the headings `## SC#1 — version-literal lockstep`, `## Guard tests`, `## Invariant spot-check`, and `## Executed versus skipped`.
    - The evidence file contains the literals `0.7.0`, `0.7.1`, `uv sync --extra dev --locked`, and `uv lock --check`.
    - The `## Executed versus skipped` section names `tox -e py312` and states it was not run because the interpreter cannot provision on this machine.
    - The `## Invariant spot-check` section cites plan 46-05 as the owner of the full D-21 sweep rather than restating its figures.
    - `git tag -l v0.7.1` produces no output.
  </acceptance_criteria>
  <done>All five version-sync guard assertions pass with zero skips on the post-bump tree, and SC#1's transcripts — including an honest statement of what was not run locally — are on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pyproject.toml` version literal → the published artifact's identity | The literal edited here becomes the PyPI package version and the git tag name at `/gsd-complete-milestone`. |
| `uv.lock` → the CI and release-build environments | Every CI job and the release build provision from this lockfile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-08 | Spoofing | version identity across `pyproject.toml` / `uv.lock` / `README.md` / installed `.dist-info` | high | mitigate | All four are asserted to agree: `uv lock --check`, `uv sync --extra dev --locked`, the `importlib.metadata` read-back, and `tests/test_readme_version_sync.py`. A tree whose declared version differs from its installed metadata would publish an artifact that does not match the reviewed tree. |
| T-46-09 | Tampering | `uv.lock` dependency set silently moving during regeneration | high | mitigate | Acceptance asserts `git diff --name-only` lists exactly three paths and that the `[project] dependencies` block is byte-identical to HEAD's; plan 46-05 re-asserts it against the `v0.7.0` anchor. |
| T-46-03 | Tampering | prep/publish fence | high | mitigate | No tag, tag push, PyPI upload, GitHub Release or PR is created; `git tag -l v0.7.1` asserted empty. |
| T-46-SC | Tampering | npm/pip/cargo installs | medium | accept | `uv sync --extra dev --locked` installs only what the existing lock already pins; no package is added. RESEARCH.md records the Package Legitimacy Gate as not applicable (zero packages evaluated, none `[ASSUMED]`/`[SUS]`/`[SLOP]`). |
</threat_model>

<verification>
- `uv lock --check` exits 0 and `uv sync --extra dev --locked` exits 0.
- `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints `0.7.1`.
- The guard battery reports `skipped="0"` and `failures="0"`.
- `git diff --name-only` lists exactly `README.md`, `pyproject.toml`, `uv.lock` plus the evidence file.
- `git tag -l v0.7.1` and `git ls-remote --tags origin v0.7.1` are both empty.
</verification>

<success_criteria>
- `pyproject.toml` is the sole `0.7.1` literal, with `README.md` and `uv.lock` moved in lockstep.
- The editable-install metadata was regenerated, so `typsphinx.__version__` reports `0.7.1`.
- All three version-sync guard modules pass with zero skips.
- `46-BUMP-EVIDENCE.md` records the transcripts and states plainly what was not run locally.
</success_criteria>

<output>
Create `.planning/phases/46-v0-7-1-release-prep-prep-only/46-02-SUMMARY.md` when done.
</output>