---
phase: 45-documentation-currency-carried-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - CHANGELOG.md
  - docs/source/changelog.rst
  - tests/test_changelog_page_gate.py
  - tests/fixtures/changelog_include_gate/conf.py
  - tests/fixtures/changelog_include_gate/index.rst
  - tests/fixtures/changelog_include_gate/changelog.rst
  - .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-02-docs-build-clean.md
autonomous: true
requirements: [DOC-12]
user_setup: []

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`CHANGELOG.md` carries a `## [0.4.4]` section describing the released v0.4.4 work, with a matching `[0.4.4]:` line in the tail link-reference block (D-03)."
    - "`CHANGELOG.md` contains exactly one `## [Unreleased]` heading, and `scripts/extract_changelog_section.py` extracts a numbered version's body identically before and after the merge — its regression suite stays green (D-04, DOC-12 ordering edge: two equal-named headings no longer compete)."
    - "`CHANGELOG.md` contains zero U+2705 check-mark characters, so `tox -e docs-pdf` cannot render tofu for them (D-05)."
    - "The published changelog page carries no hand-maintained per-release section and no stale current-release marker — adding a release section to `CHANGELOG.md` is the only edit a future release needs (ROADMAP SC#2's one-line-addition clause)."
    - "Both docs builds emit zero WARNING lines mentioning the changelog page, and neither build's total warning count exceeds the pre-phase baseline recorded in 45-GATE-EVIDENCE-01-docs-build-baseline.md."
    - "The rendered changelog page carries all 12 previously-missing releases, `0.4.4` included, in both the built HTML and the text extracted from the compiled PDF."
    - "`docs/source/changelog.rst`'s `Migration Guides` section carries entries for the 0.6.x and 0.7.0 emitted-`.typ` changes, and `Release Process` describes what `.github/workflows/release.yml` actually does today (D-06)."
  artifacts:
    - "CHANGELOG.md — the `## [0.4.4] - 2026-07-05` section and its `[0.4.4]:` link reference"
    - "docs/source/changelog.rst — corrected `Migration Guides` and `Release Process` sections"
    - "tests/test_changelog_page_gate.py — new regression module"
    - "tests/fixtures/changelog_include_gate/ — conf.py, index.rst, changelog.rst"
    - ".planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-02-docs-build-clean.md"
  key_links:
    - "CHANGELOG.md `## [X.Y.Z]` headings → `scripts/extract_changelog_section.py`'s positional slicing → release.yml's validate gate and release body (REL-04) — both edits in this plan must leave this link intact"
    - "CHANGELOG.md lines 1-7 → `docs/source/changelog.rst`'s `:start-line: 7` offset — pinned byte-invariant by this plan so the include keeps skipping exactly the preamble"
    - "tests/fixtures/changelog_include_gate/changelog.rst → repo-root CHANGELOG.md — the fixture reads the REAL changelog, so its content assertions are genuine, not synthetic"
  prohibitions:
    - "`scripts/extract_changelog_section.py` and `.github/workflows/release.yml` are not modified."
    - "The string `Unreleased` is not special-cased anywhere in the extractor — the module's own docstring forbids it and the positional algorithm already handles both headings."
    - "`CHANGELOG.md`'s physically out-of-chronological-order `## [0.2.0]` section is not reordered; file-order changes DO matter to the extractor's positional slicing."
    - "No `## [0.7.1]` section is added — Phase 46 owns it."
    - "`docs/source/user_guide/templates.rst` is not edited."
    - "CHANGELOG.md is not converted away from Markdown (D-02: GitHub renders Release bodies as Markdown and REL-04 has never run green end to end)."
---

<objective>
Make the delegated changelog page complete and clean: backfill the release `CHANGELOG.md` never
recorded, remove the two structural defects that would surface as page defects under D-01, correct
the page's remaining framing sections, and prove the result against both builders with a regression
gate plus a warning delta.

Purpose: ROADMAP SC#2's content half — every release from 0.4.4 through 0.7.0 present, no stale
current-release marker, both `tox -e docs-html` and `tox -e docs-pdf` building the page clean.
Output: a complete `CHANGELOG.md`, a corrected framing set, a new pytest gate, and clean-build
evidence measured as a delta.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-CONTEXT.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-RESEARCH.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-PATTERNS.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-01-SUMMARY.md
@CLAUDE.md
</context>

<worktree_protocol>
Provision first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`.
Run every command through `uv run`. Invoke Sphinx as `python -m sphinx`, never a resolved
`sphinx-build` binary.
</worktree_protocol>

<tasks>

<task type="auto">
  <name>Task 1: Backfill 0.4.4, merge the duplicate Unreleased, and strip the emoji from CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-12 (the preamble that must stay byte-identical, and the top-of-file `## [Unreleased]` at line 8)
    - CHANGELOG.md lines 377-436 (the `## [0.5.0]` and `## [0.4.3]` sections — the reconstructed `## [0.4.4]` belongs between them, and `## [0.4.3]` is the shape template for the new entry's `### Added`/`### Changed`/`### Fixed` convention)
    - CHANGELOG.md lines 795-820 and 885-905 (the two Requirements-Status blocks holding the check-mark characters)
    - CHANGELOG.md lines 905-939 (the second `## [Unreleased]` heading with its Planned-for-Future-Releases body, and the tail link-reference block where the new `[0.4.4]:` line goes)
    - scripts/extract_changelog_section.py (the `_SECTION_HEADER_RE` regex and `extract_section()` — confirm for yourself that the algorithm is positional and never inspects heading names)
    - tests/test_changelog_extraction.py (in particular `test_unreleased_headings_do_not_leak`, the existing regression proving the two-Unreleased structure is already handled)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-RESEARCH.md (§ Code Examples: `v0.4.4` reconstruction material; § Architecture Patterns Pattern 2; § Anti-Patterns to Avoid)
  </read_first>
  <precondition>`git tag -l v0.4.4` prints `v0.4.4` and `git rev-list --count v0.4.3..v0.4.4` prints 148 — the commit range the reconstructed entry is derived from must be reachable in this worktree.</precondition>
  <action>
    Three edits to `CHANGELOG.md`, all locked decisions. Make them in this order.

    **D-03 — backfill the missing `## [0.4.4]` section.** `v0.4.4` was tagged and released
    (2026-07-05) but `CHANGELOG.md` has no section for it; the link-reference block jumps from
    `[0.5.0]` straight to `[0.4.3]`. Because D-01 delegates the published page to this file, that
    hole is a hole on the page — this is why ROADMAP SC#2 counts 12 missing releases while the source
    todo listed 11. Insert a `## [0.4.4] - 2026-07-05` heading immediately above the existing
    `## [0.4.3] - 2025-11-01` heading (so file order stays newest-first) and write its body using the
    same `### Added` / `### Changed` / `### Fixed` convention the neighbouring `## [0.4.3]` entry uses.

    Derive the content from the release itself, not from memory: run
    `git log --oneline v0.4.3..v0.4.4` and, if `gh` is authenticated,
    `gh release view v0.4.4 --json body`. The range is a CI/release-durability milestone with no
    `typsphinx` behaviour change — the substantive items are: Python support floor raised to
    3.10-3.13 and the CI/tox matrices reconciled to it; `softprops/action-gh-release` bumped v2 to
    v3; `--locked` appended to every `uv sync` call site; a weekly dependency drift-detection
    workflow added; a `sphinx-typst-stack` Dependabot group plus a CI status badge added; GitHub
    Actions artifact actions bumped to the Node 24 majors; a `tomllib`/`tomli` fallback fix in the
    release workflow's version-verify step; and Sphinx i18n infrastructure with Japanese
    translations plus multi-language GitHub Pages support. Keep the entry curated and
    user-facing-first, matching the tone of the surrounding entries — it is release-notes prose, not
    a commit dump. Add the matching `[0.4.4]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.4.4`
    line to the tail link-reference block, positioned between the existing `[0.5.0]:` and `[0.4.3]:`
    lines so the block's ordering matches the section ordering.

    **D-04 — merge the duplicate `## [Unreleased]`.** The file carries `## [Unreleased]` twice: at
    line 8 (an empty top-of-file placeholder) and again near line 911 with a
    Planned-for-Future-Releases list, sitting inside the `## [0.2.0]` section's body span. Under
    D-01 both would surface on the published manual. Fold the second heading's content into the
    top-of-file `[Unreleased]` — or drop it if you judge it stale, recording which you did and why in
    the plan summary — and delete the second heading. Delete the now-orphaned `---` transition that
    preceded it only if it leaves a doubled rule; do not otherwise restructure the tail.

    **D-05 — remove the 25 check-mark characters (U+2705).** They sit in two near-identical
    Requirements-Status blocks (around lines 801-813 and 889-901) on `Requirement N` lines whose
    surrounding prose already states completion. Under D-01 they reach `tox -e docs-pdf`, and neither
    typst-py's embedded fonts nor RTD's `fonts-noto-cjk` package has emoji coverage; Typst's font
    fallback is silent, so the build would report success while rendering tofu. Delete every one of
    them and lightly reword each affected line so it still reads as a completion statement without
    the glyph. The `⏳` character on the one in-progress line in the first block is a different code
    point and is out of D-05's scope — leave it or reword it consistently, your choice, but say which
    in the summary.

    **Invariant this plan owes plan 45-01.** `docs/source/changelog.rst`'s include uses
    `:start-line: 7`, which skips exactly CHANGELOG.md's first seven lines. None of the three edits
    above may add, remove, or alter any of lines 1-7. This is how RESEARCH Pitfall 1's ordering
    hazard is discharged: the offset is pinned by an invariant rather than re-measured.

    Do not touch `scripts/extract_changelog_section.py` or `.github/workflows/release.yml`. Do not
    reorder the physically out-of-order `## [0.2.0]` section (line 840) — the extractor's slicing is
    positional, so file-order changes DO matter to it even though heading names do not.
  </action>
  <verify>
    <automated>uv run pytest tests/test_changelog_extraction.py -v && uv run python - <<'PY'
import pathlib, re, subprocess
text = pathlib.Path("CHANGELOG.md").read_text(encoding="utf-8")
lines = text.split("\n")
assert re.search(r"^## \[0\.4\.4\]", text, re.M), "missing ## [0.4.4] section"
assert re.search(r"^\[0\.4\.4\]: ", text, re.M), "missing [0.4.4]: link reference"
heads = [i for i, l in enumerate(lines) if re.match(r"^## \[Unreleased\]", l)]
assert len(heads) == 1, "expected exactly one ## [Unreleased], found %d" % len(heads)
assert "✅" not in text, "U+2705 still present"
base = subprocess.run(["git", "show", "HEAD:CHANGELOG.md"], capture_output=True, text=True).stdout.split("\n")
assert lines[:7] == base[:7], "CHANGELOG.md lines 1-7 changed; the include's :start-line: 7 offset is now wrong"
order = [m.group(1) for m in re.finditer(r"^## \[([^\]]+)\]", text, re.M)]
i = order.index("0.5.0")
assert order[i + 1] == "0.4.4" and order[i + 2] == "0.4.3", "0.4.4 is not between 0.5.0 and 0.4.3: %r" % order[i:i+3]
print("OK changelog structure")
PY</automated>
  </verify>
  <acceptance_criteria>
    - Source: `CHANGELOG.md` contains a line matching `^## \[0\.4\.4\]` and a line matching `^\[0\.4\.4\]: `.
    - Source: the section order around the insertion is exactly `0.5.0`, `0.4.4`, `0.4.3`.
    - Source: exactly one line in `CHANGELOG.md` matches `^## \[Unreleased\]`.
    - Source: `grep -c $'✅' CHANGELOG.md` returns 0.
    - Source: `git show HEAD:CHANGELOG.md | head -7` and `head -7 CHANGELOG.md` are byte-identical (the `:start-line: 7` invariant).
    - Test: `uv run pytest tests/test_changelog_extraction.py -v` exits 0 — including `test_unreleased_headings_do_not_leak`.
    - CLI: `uv run python scripts/extract_changelog_section.py 0.4.4` prints a non-empty body and exits 0.
    - CLI: `git show HEAD:CHANGELOG.md > <tmp>/pre.md` then `uv run python scripts/extract_changelog_section.py 0.7.0 --changelog-path <tmp>/pre.md` and `uv run python scripts/extract_changelog_section.py 0.7.0` produce byte-identical output — the edits changed no other section's extracted body.
    - CLI: `git diff --stat -- scripts/ .github/` prints nothing.
  </acceptance_criteria>
  <done>`CHANGELOG.md` is complete (0.4.4 present), structurally single-`[Unreleased]`, emoji-free, its preamble byte-invariant, and REL-04's extractor demonstrably undisturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the changelog page's remaining framing sections</name>
  <files>docs/source/changelog.rst</files>
  <read_first>
    - docs/source/changelog.rst (as plan 45-01 left it — the `Migration Guides`, `Deprecation Policy`, `Upcoming Features`, `Versioning`, `Release Process` and `See Also` sections, and the section-underline convention: `-` for level 2, `~` for level 3)
    - .github/workflows/release.yml (the real job graph the `Release Process` section must describe: the `validate` job's version/tag/CHANGELOG-section checks, `build`, `publish-pypi` behind the `pypi` environment approval, and `create-release`)
    - CHANGELOG.md `## [0.6.0]` through `## [0.7.0]` (the emitted-`.typ` and user-visible changes the new migration entries must describe)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-CONTEXT.md (D-06)
  </read_first>
  <action>
    D-06's second half. Plan 45-01 already deleted the stale per-version support-status section and
    the hand-maintained release history. This task corrects the two remaining stale sections in place.

    **`Migration Guides`.** It currently stops at "Migrating from 0.2.x to 0.3.x", so the 0.6.x and
    0.7.0 emitted-`.typ` changes have no migration note anywhere in the manual. Add two new level-3
    subsections above the existing ones so the list reads newest-first: one covering 0.5.x to 0.6.x
    and one covering 0.6.x to 0.7.0. Source their content from the corresponding `CHANGELOG.md`
    sections — describe what a user must change or expect, not what the internals did. Keep the
    existing 0.2.x-to-0.3.x and 0.1.x-to-0.2.x subsections; they are historical record, not staleness.

    **`Release Process`.** Its numbered list no longer matches `release.yml`. Restate it against
    what the workflow actually does today: the version literal lives in `pyproject.toml`; a tag push
    triggers `release.yml`; the `validate` job checks the tag against the version and aborts if the
    matching `## [X.Y.Z]` CHANGELOG section is missing or empty; `build` produces the wheel and
    sdist; `publish-pypi` runs behind the protected `pypi` environment and needs an approval; and
    `create-release` publishes a GitHub Release whose body is the extracted `## [X.Y.Z]` CHANGELOG
    section. Describe it as prose or a numbered list, whichever reads better — but every step must
    correspond to a job or step that exists in `release.yml`, and none may be invented.

    Do not add release-specific content to this page. The whole point of D-01 is that the release
    record lives in `CHANGELOG.md`; anything you write here must be evergreen framing.
  </action>
  <verify>
    <automated>uv run python - <<'PY'
import pathlib, re, subprocess, sys, tempfile
src = pathlib.Path("docs/source/changelog.rst").read_text(encoding="utf-8")
assert ".. include:: ../../CHANGELOG.md" in src, "the include directive was lost"
assert ":parser: myst_parser.sphinx_" in src, "the parser option was lost"
assert re.search(r"^Migration Guides$", src, re.M), "Migration Guides section missing"
assert re.search(r"0\.6\.x", src) and re.search(r"0\.7\.0", src), "no 0.6.x / 0.7.0 migration entries"
assert re.search(r"^Release Process$", src, re.M), "Release Process section missing"
for token in ("validate", "publish-pypi", "create-release"):
    assert token in src, "Release Process does not name the real job: " + token
assert not re.search(r"^Version [0-9]", src, re.M), "a hand-maintained per-release heading is back on the page"
out = tempfile.mkdtemp()
r = subprocess.run([sys.executable, "-m", "sphinx", "-b", "html", "docs/source", out], capture_output=True, text=True)
assert r.returncode == 0, r.stdout + r.stderr
noisy = [ln for ln in (r.stdout + r.stderr).splitlines() if "WARNING" in ln and "changelog" in ln]
assert not noisy, "changelog-attributable warnings: " + repr(noisy)
print("OK framing")
PY</automated>
  </verify>
  <acceptance_criteria>
    - Source: `docs/source/changelog.rst` still contains `.. include:: ../../CHANGELOG.md` and `:parser: myst_parser.sphinx_`.
    - Source: the `Migration Guides` section contains subsections naming both `0.6.x` and `0.7.0`.
    - Source: the `Release Process` section names `validate`, `publish-pypi` and `create-release`, and every step it lists corresponds to a job or step present in `.github/workflows/release.yml`.
    - Source: `grep -cE '^Version [0-9]' docs/source/changelog.rst` returns 0.
    - Source: `grep -c 'Development Status' docs/source/changelog.rst` returns 0.
    - Behavior: `uv run python -m sphinx -b html docs/source <tmp>` exits 0 with zero WARNING lines mentioning `changelog`.
  </acceptance_criteria>
  <done>The page's remaining framing sections describe the project as it is today: migration notes reach 0.7.0 and the release process matches `release.yml`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Changelog-page regression gate plus the clean-build delta evidence</name>
  <files>tests/test_changelog_page_gate.py, tests/fixtures/changelog_include_gate/conf.py, tests/fixtures/changelog_include_gate/index.rst, tests/fixtures/changelog_include_gate/changelog.rst, .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-02-docs-build-clean.md</files>
  <read_first>
    - tests/test_default_typst_documents_gate.py (the `_run_sphinx_build` helper each gate module carries its own copy of, the `TYPST_AVAILABLE` skipif shape, and the assertion-message style)
    - tests/test_no_stale_github_io_links.py (the established repo-hygiene shape: parses raw file text, no network, splits a literal to avoid self-matching)
    - tests/fixtures/default_typst_documents_gate/conf.py (fixture conf.py conventions and the self-documenting header comment)
    - docs/source/changelog.rst (the final directive shape this gate must mirror and assert on)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md (the three baseline counts this task's delta compares against)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-VALIDATION.md (§ Wave 0 Requirements — the two net-new build-checks this task closes)
  </read_first>
  <behavior>
    - `TestPublishedChangelogPageDelegates::test_page_delegates_to_changelog_md` — reads `docs/source/changelog.rst` as text (no build): the include directive and the `:parser:` option are present.
    - `TestPublishedChangelogPageDelegates::test_page_carries_no_hand_maintained_release_history` — the page has zero lines matching a per-release heading pattern and zero occurrences of a current-release marker, proving a future release needs no edit here.
    - `TestChangelogPageContentCoverage::test_rendered_page_carries_every_release` — a real `-b html` build of `docs/source`: `changelog.html` contains all 12 previously-missing version strings.
    - `TestChangelogPageContentCoverage::test_rendered_page_has_one_changelog_heading` — exactly one heading element in `changelog.html` has the text `Changelog` (the DOC-12 adjacency edge: the included Markdown's own H1 does not collide with the host page's title).
    - `TestChangelogPageContentCoverage::test_build_emits_no_changelog_warnings` — the build's combined output has zero `WARNING` lines mentioning `changelog`.
    - `TestChangelogIncludeCompilesToPdf::test_included_changelog_reaches_the_pdf` — a real `-b typstpdf` build of the small fixture: exit 0, `%PDF` magic, and pypdf-extracted text contains all 12 version strings.
  </behavior>
  <action>
    Close the two net-new Wave 0 gaps `45-VALIDATION.md` records, then record the clean-build delta.

    **Fixture `tests/fixtures/changelog_include_gate/`.** A three-file Sphinx project whose
    `changelog.rst` includes the REAL repository-root `CHANGELOG.md` — from this depth the relative
    path is `../../../CHANGELOG.md` — with the same `:parser:` and `:start-line:` options
    `docs/source/changelog.rst` uses. `conf.py` sets a distinct `project` value, lists
    `myst_parser` and `typsphinx` in `extensions`, and sets an explicit single-entry
    `typst_documents`; `index.rst` holds a title plus a toctree naming `changelog`, mirroring how
    the real docs tree reaches the page. Give `conf.py` a header comment stating why the include
    path points outside the fixture directory (it must read the real changelog, or the content
    assertions would be synthetic and prove nothing) and warning against copying the file into the
    fixture tree.

    **Module `tests/test_changelog_page_gate.py`.** Carry its own `_run_sphinx_build` helper
    invoking `sys.executable -m sphinx` rather than importing a sibling module's — that is this
    suite's standing convention and its rationale (the NixOS sandbox PATH-shadowing hazard) belongs
    in the helper's docstring. Define the 12 version strings as one module-level tuple
    (`0.4.1`, `0.4.2`, `0.4.3`, `0.4.4`, `0.5.0`, `0.6.0`, `0.6.1`, `0.6.2`, `0.6.3`, `0.6.4`,
    `0.6.5`, `0.7.0`) used by both the HTML and PDF assertions.

    Guard the two build classes so they degrade honestly rather than failing spuriously: skip when
    `myst_parser` is not importable (it lives in the `docs` extra only, per D-01 — CI's dev-only
    lane will skip these, and the module docstring must say so plainly rather than leaving a reader
    to discover it), and skip the PDF class additionally when `typst` is not importable, matching
    `tests/test_default_typst_documents_gate.py`'s `TYPST_AVAILABLE` pattern. Mark both build
    classes `@pytest.mark.slow` — the HTML class builds the whole `docs/source` tree and the marker
    already exists in `pyproject.toml`'s `markers` list. `TestPublishedChangelogPageDelegates`
    requires no import and no build, so it must NOT be skipped or marked slow: it is the one
    assertion in this module that always runs in CI.

    For the per-release-heading assertion, follow `tests/test_no_stale_github_io_links.py`'s
    precedent and build the searched literals from split fragments so the test module cannot match
    itself if it is ever scanned.

    For PDF text extraction use `pypdf` (already in the `dev` extra) and normalise the extracted
    text before searching — strip zero-width spaces, since U+200B is known to poison pypdf
    extraction in this project.

    **Evidence `45-GATE-EVIDENCE-02-docs-build-clean.md`.** Re-run both builds against the final
    tree, capture their output, and record: the post-change SHA; `html_warning_count`,
    `pdf_warning_count`, `changelog_attributable_warning_count` in the same key shape plan 45-01's
    baseline used; the delta against each baseline value; and every `WARNING` line that appears now
    but did not appear in the baseline, verbatim, with a one-line disposition for each. The phase's
    bar is `changelog_attributable_warning_count == 0` and neither total exceeding its baseline;
    pre-existing warnings elsewhere in the docs build are outside this phase's fence (RESEARCH
    Pitfall 2 — neither tox docs env passes `-W`, so exit code alone proves nothing).

    Also record in that file, under a `## Carried consequences` heading: (1) every line the include
    newly surfaces renders untranslated on the `ja` site until the separate
    `typsphinx-doc-translations` catalogs are regenerated — out of this repository's scope, flag at
    the milestone close; (2) the diff-inspection finding for ROADMAP SC#2's one-line-addition
    clause, namely that `docs/source/changelog.rst` now contains only framing plus the include, so
    Phase 46's `0.7.1` entry is a `CHANGELOG.md`-only edit.
  </action>
  <verify>
    <automated>uv run pytest tests/test_changelog_page_gate.py -v && uv run black --check tests/test_changelog_page_gate.py && uv run ruff check tests/test_changelog_page_gate.py && grep -qE '^- changelog_attributable_warning_count: 0$' .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-02-docs-build-clean.md</automated>
  </verify>
  <acceptance_criteria>
    - Test: `uv run pytest tests/test_changelog_page_gate.py -v` exits 0 with zero failures; `TestPublishedChangelogPageDelegates` reports as passed (not skipped).
    - Test: `uv run pytest tests/test_changelog_page_gate.py -m "not slow" -v` exits 0 and deselects the two build classes.
    - Behavior: with `myst_parser` installed, `TestChangelogPageContentCoverage::test_rendered_page_carries_every_release` passes against a real `-b html` build and asserts all 12 version strings.
    - Behavior: `TestChangelogIncludeCompilesToPdf` produces a file whose first four bytes are `%PDF` and whose pypdf-extracted text contains all 12 version strings.
    - Source: `tests/fixtures/changelog_include_gate/changelog.rst` contains `.. include:: ../../../CHANGELOG.md` and `:parser: myst_parser.sphinx_`.
    - Source: `45-GATE-EVIDENCE-02-docs-build-clean.md` contains a line matching `^- changelog_attributable_warning_count: 0$`, a `## Carried consequences` heading, and a delta line for each of the three baseline counts.
    - CLI: `uv run black --check .` and `uv run ruff check .` exit 0.
  </acceptance_criteria>
  <done>A committed regression gate proves the published page carries every release and builds without changelog-attributable warnings, and the evidence file records the clean-build delta plus the two carried consequences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG.md → release.yml (CI tier) | The same file this plan edits supplies the GitHub Release body through `extract_changelog_section.py`; a structural break here breaks a release, not just a page |
| CHANGELOG.md → published docs (docs tier) | The same file is rendered into HTML and a compiled PDF served from Read the Docs |
| test fixture → repository root | `tests/fixtures/changelog_include_gate/changelog.rst` reads `../../../CHANGELOG.md`, a path outside the fixture directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-05 | Tampering | `CHANGELOG.md` edits silently breaking `extract_changelog_section.py`'s section slicing, aborting or corrupting a release (REL-04, which has never run green end to end) | high | mitigate | The extractor's algorithm is positional and name-agnostic (verified in RESEARCH); this plan changes no code in it; `tests/test_changelog_extraction.py` including `test_unreleased_headings_do_not_leak` is an acceptance gate; the extractor is additionally exercised directly on both `0.4.4` and `0.7.0`, with `0.7.0`'s body compared byte-for-byte against its pre-edit output |
| T-45-06 | Denial of Service | An unclosed or malformed Markdown construct in the new `## [0.4.4]` section aborting the Typst compile of the published PDF | medium | mitigate | `TestChangelogIncludeCompilesToPdf` compiles the real `CHANGELOG.md` through `-b typstpdf` and asserts `%PDF` magic plus extracted content; the full `docs/source` HTML build is asserted warning-free for the changelog page |
| T-45-07 | Repudiation | "Build clean" claimed from a green exit code while warnings were emitted (neither tox docs env passes `-W`) | medium | mitigate | Clean is defined and measured as a delta against the plan-45-01 baseline capture, with every new WARNING line reproduced verbatim and dispositioned in the evidence file |
| T-45-08 | Information Disclosure | The fixture reaching outside its directory to read a repository-root file | low | accept | Fixed literal relative path to a first-party, already-public, repo-tracked file; no build input controls it |
</threat_model>

<flagged_assumptions>
- **RESEARCH assumption A2:** the exact `## [0.4.4]` prose is not pre-drafted; only the commit-range
  material is. Task 1 derives it live from `git log v0.4.3..v0.4.4` and the existing GitHub Release,
  which is the intended execution-time drafting step, not a gap.
- **RESEARCH assumption A3:** which section the first check-mark block (around lines 801-813) is
  attributed to was not individually re-verified. Immaterial — D-05 deletes all 25 occurrences
  regardless of containing section.
</flagged_assumptions>

<verification>
- `uv run pytest tests/test_changelog_extraction.py tests/test_changelog_page_gate.py -v` green.
- `uv run python scripts/extract_changelog_section.py 0.7.0` output byte-identical to its pre-edit output.
- Both docs builders exit 0; zero WARNING lines mentioning the changelog page; neither total exceeds its baseline.
- `docs/source/changelog.rst` contains only framing plus the include — verified by diff inspection and pinned by `TestPublishedChangelogPageDelegates`.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` clean.
</verification>

<success_criteria>
- ROADMAP SC#2 in full: 12 releases (0.4.4 through 0.7.0) present on the published page, no stale current-release marker, both docs builds clean against the baseline, and the Phase 46 `0.7.1` entry reduced to a one-line `CHANGELOG.md` addition.
</success_criteria>

## Artifacts this phase produces

New symbols created by phase 45 (all four plans) — exclude these from drift/source-grounding checks:

- `pyproject.toml` — new dependency entry `myst-parser>=5.0` in `[project.optional-dependencies].docs`
- `docs/source/conf.py` — new `extensions` member `"myst_parser"`
- `docs/source/changelog.rst` — new `Release History` section and the `.. include:: ../../CHANGELOG.md` directive with options `:parser: myst_parser.sphinx_` and `:start-line: 7`
- `CHANGELOG.md` — new `## [0.4.4] - 2026-07-05` section and new `[0.4.4]:` link-reference line
- `tests/test_changelog_page_gate.py` — new test module; classes `TestPublishedChangelogPageDelegates`, `TestChangelogPageContentCoverage`, `TestChangelogIncludeCompilesToPdf`
- `tests/fixtures/changelog_include_gate/conf.py`, `tests/fixtures/changelog_include_gate/index.rst`, `tests/fixtures/changelog_include_gate/changelog.rst` — new fixture project
- `tests/test_quickstart_docs_gate.py` — new test module; classes `TestQuickstartFirstPdfGate`, `TestPublishedQuickstartTextMatchesBuild`
- `tests/fixtures/quickstart_docs_gate/conf.py`, `tests/fixtures/quickstart_docs_gate/index.rst` — new fixture project mirroring the published Quick Start
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-include-shape.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-02-docs-build-clean.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-qua03-comment-scan.md`

<output>
Create `.planning/phases/45-documentation-currency-carried-hygiene/45-02-SUMMARY.md` when done.
</output>
