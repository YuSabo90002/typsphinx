---
phase: 45-documentation-currency-carried-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/source/quickstart.rst
  - docs/source/user_guide/configuration.rst
  - tests/test_quickstart_docs_gate.py
  - tests/fixtures/quickstart_docs_gate/conf.py
  - tests/fixtures/quickstart_docs_gate/index.rst
autonomous: true
requirements: [DOC-11]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reader who follows the Quick Start's own steps with `typst_documents` unset gets the output filename the documentation names: a real `-b typstpdf` build of a fixture mirroring the published Quick Start emits `myproject.typ` and `myproject.pdf` and no `index.pdf`, and `docs/source/quickstart.rst` names that same path."
    - "The README Quick Start section names `typst_documents`, states that it need not be set, states the derived default's `<project>.typ` name shape, states that an explicit setting overrides the derived default, and states which documents become PDFs (ROADMAP SC#1's five statements)."
    - "The README no longer claims `typst_documents` must be set before a PDF can be produced — CONF-08 made that false."
    - "An explicit `typst_documents` entry naming a target produces exactly that target and no additional derived-default target; the explicit setting and the derived default do not both fire (DOC-11 adjacency edge, pinned by the existing `test_explicit_typst_documents_wins`)."
    - "The README states that each entry in `typst_documents` becomes its own master `.typ`/`.pdf`, and that a document reached only through a toctree is `#include()`d into its master rather than becoming a separate PDF (DOC-11 ordering edge: which documents become PDFs, and in what relationship)."
    - statement: "With `typst_documents` unset and `project` left at Sphinx's own default sentinel, the typstpdf build still emits exactly one master target named from that default rather than crashing or silently reverting to index.pdf (DOC-11 empty-input edge)."
      verification: backstop
    - "`docs/source/user_guide/configuration.rst` states that `typst_documents` is optional and describes the derived default, without altering the title/author element text Phase 44.2 wrote there."
  artifacts:
    - "README.md — Quick Start `typst_documents` explanation and a corrected Configuration Options bullet"
    - "docs/source/quickstart.rst — a corrected `Your First PDF` output path and a Configuration Options note on the derived default"
    - "docs/source/user_guide/configuration.rst — a derived-default paragraph added ahead of the tuple element list"
    - "tests/test_quickstart_docs_gate.py"
    - "tests/fixtures/quickstart_docs_gate/conf.py, tests/fixtures/quickstart_docs_gate/index.rst"
  key_links:
    - "typsphinx/builder.py `_default_typst_documents()` → `make_filename_from_project(config.project) + '.typ'` → the output filename the docs must name; the gate computes the expected stem from that same helper rather than hardcoding it"
    - "typsphinx/__init__.py `app.add_config_value('typst_documents', _default_typst_documents, ...)` → Sphinx's `Config.__getattr__` checking `_raw_config` first → the explicit-setting-wins precedence the docs must state"
    - "docs/source/quickstart.rst's stated output path → tests/fixtures/quickstart_docs_gate's real build → the assertion that binds prose to measured behaviour"
  prohibitions:
    - "`docs/source/user_guide/templates.rst` is not edited — its four-parameter custom-template contract is Phase 45.1 / DOC-13, whose SC#2 must check the published contract rather than inherit an edit made here."
    - "The title (element 3) and author (element 4) descriptions Phase 44.2 wrote into `docs/source/user_guide/configuration.rst` are not rewritten or contradicted."
    - "No `typst_documents` line is added to `tests/fixtures/default_typst_documents_gate/conf.py` — that fixture's whole purpose is to exercise the unset path."
    - "No `typsphinx/` source file is modified by this plan; DOC-11 is documentation plus a gate."
---

<objective>
Make the published surface describe the behaviour Phase 44 actually shipped: `typst_documents` is
optional, an unset value derives one master entry whose target stem comes from
`make_filename_from_project(project)`, an explicit setting always wins, and the documents that become
PDFs are exactly the entries in that list.

Purpose: ROADMAP SC#1 — a reader following the README Quick Start exactly is not surprised by the
output filename or by which documents become PDFs, with each statement checked against a real build
of the Quick Start's own steps rather than against requirement text.
Output: three corrected documentation sites and a real-`sphinx-build` gate that binds the prose to
the measured behaviour.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-CONTEXT.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-RESEARCH.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-PATTERNS.md
@.planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-CONTEXT.md
@CLAUDE.md
</context>

<worktree_protocol>
Provision first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`.
Run every command through `uv run`. Invoke Sphinx as `python -m sphinx`, never a resolved
`sphinx-build` binary.
</worktree_protocol>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real-build gate for the published Quick Start's own steps</name>
  <files>tests/test_quickstart_docs_gate.py, tests/fixtures/quickstart_docs_gate/conf.py, tests/fixtures/quickstart_docs_gate/index.rst</files>
  <read_first>
    - tests/test_default_typst_documents_gate.py (the exact analog: `_run_sphinx_build`, the `TYPST_AVAILABLE` skipif, both must-SUCCEED assertions and their message style)
    - tests/fixtures/default_typst_documents_gate/conf.py (the self-documenting header comment explaining why the file deliberately has no `typst_documents` line)
    - tests/fixtures/default_typst_documents_gate/index.rst
    - typsphinx/builder.py lines 20 and 28-47 (`make_filename_from_project` import site and `_default_typst_documents()` — the derivation the gate must compute against rather than hardcode)
    - docs/source/quickstart.rst lines 40-91 (the `Your First PDF` steps and the `Configuration Options` block this fixture mirrors verbatim)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-VALIDATION.md (the DOC-11 row: a Quick-Start-mirroring fixture asserting the emitted PDF is `<project>.pdf`, not `index.pdf`)
  </read_first>
  <precondition>`uv run python -c "import typst"` succeeds — the `-b typstpdf` half of this gate needs typst-py, exactly as `tests/test_default_typst_documents_gate.py` does.</precondition>
  <behavior>
    - `TestQuickstartFirstPdfGate::test_quickstart_steps_produce_the_documented_pdf` — a real `-b typstpdf` build of the fixture exits 0 and writes `myproject.typ` and `myproject.pdf`, where `myproject` is computed as `make_filename_from_project("My Project")` rather than hardcoded.
    - `TestQuickstartFirstPdfGate::test_quickstart_steps_do_not_produce_index_pdf` — neither `index.typ` nor `index.pdf` exists in the build directory; the derived target renames the root document's output.
    - `TestPublishedQuickstartTextMatchesBuild::test_quickstart_names_the_measured_output_path` — `docs/source/quickstart.rst` contains the exact `build/pdf/<stem>.pdf` path the build just produced, with `<stem>` computed from the same helper.
    - `TestPublishedQuickstartTextMatchesBuild::test_readme_quickstart_explains_typst_documents` — the README's Quick Start section (the text between its own heading and the next same-level heading) mentions `typst_documents`.
    - `TestPublishedQuickstartTextMatchesBuild::test_readme_no_longer_calls_typst_documents_mandatory` — the README's Configuration Options bullet no longer carries the mandatory-for-PDF clause.
  </behavior>
  <action>
    Write the gate FIRST, before the prose edits, so the three source assertions start red and the
    prose edits in tasks 2 and 3 turn them green. This is the mechanism that satisfies ROADMAP SC#1's
    "checked against a real build of the Quick Start's own steps, not against the requirement text".

    **Fixture `tests/fixtures/quickstart_docs_gate/`.** Mirror the published Quick Start's own steps
    rather than inventing a project. `conf.py` sets `project = "My Project"`,
    `author = "Your Name"`, `release = "1.0.0"` — the exact values `docs/source/quickstart.rst`'s
    Configuration Options block publishes — plus `extensions = ["typsphinx"]`, and deliberately has
    NO `typst_documents` line, because the Quick Start's `Your First PDF` flow sets none. Give the
    file a header comment saying so and warning that adding a `typst_documents` line would silently
    stop the fixture exercising the documented path, following
    `tests/fixtures/default_typst_documents_gate/conf.py`'s precedent. `index.rst` reproduces the
    sample document the Quick Start tells the reader to create (the `Welcome to My Documentation`
    title, the sample paragraph, and the `Features` subsection with its three bullets).

    This fixture is deliberately NOT the same as `tests/fixtures/default_typst_documents_gate/`:
    that one exists to prove the CONF-08 derivation mechanism with a synthetic project name, while
    this one exists to prove the *published example* produces the *published filename*. Do not merge
    them and do not modify the existing fixture.

    **Module `tests/test_quickstart_docs_gate.py`.** Carry its own `_run_sphinx_build` helper
    invoking `sys.executable -m sphinx` with `capture_output=True, text=True`, with the same
    docstring rationale the sibling gate modules carry (each module owns its copy rather than
    importing a sibling's; `sys.executable -m sphinx` avoids the NixOS-sandbox PATH-shadowing
    hazard). Import `make_filename_from_project` from `sphinx.util.osutil` — the same import
    `typsphinx/builder.py` uses — and derive the expected stem from the fixture's own `project`
    string, so the gate cannot drift from the derivation it is asserting about.

    Guard the two build tests with the `TYPST_AVAILABLE` skipif pattern. The
    `TestPublishedQuickstartTextMatchesBuild` class does no build and must never be skipped — it is
    the assertion that binds prose to behaviour and it must run in every CI lane.

    For the README assertions, slice the Quick Start section out of `README.md` by locating its own
    heading and the next heading at the same level, and assert against that slice rather than the
    whole file — a mention of `typst_documents` elsewhere in the README must not satisfy SC#1's
    Quick Start requirement. For the mandatory-clause assertion, build the searched literal from
    split fragments the way `tests/test_no_stale_github_io_links.py` does, so the test module cannot
    match itself.

    Expect this task to end with the two build tests green and the three text tests red. Record that
    red state in the plan summary — it is the recorded-RED evidence this project's standing gate bar
    asks for.
  </action>
  <verify>
    <automated>uv run pytest tests/test_quickstart_docs_gate.py::TestQuickstartFirstPdfGate -v && uv run black --check tests/test_quickstart_docs_gate.py && uv run ruff check tests/test_quickstart_docs_gate.py</automated>
  </verify>
  <acceptance_criteria>
    - Test: `uv run pytest tests/test_quickstart_docs_gate.py::TestQuickstartFirstPdfGate -v` exits 0 — the real `-b typstpdf` build of the fixture succeeds and produces `myproject.typ` + `myproject.pdf`.
    - Test: `uv run pytest tests/test_quickstart_docs_gate.py::TestPublishedQuickstartTextMatchesBuild -v` exits non-zero at this point (recorded RED), and the failure messages name the specific missing prose.
    - Source: `tests/fixtures/quickstart_docs_gate/conf.py` sets `project = "My Project"` and contains no line matching `^typst_documents`.
    - Source: `tests/test_quickstart_docs_gate.py` imports `make_filename_from_project` from `sphinx.util.osutil` and derives the expected stem from it rather than hardcoding `myproject`.
    - Source: `git diff --stat -- typsphinx/` prints nothing.
    - CLI: `uv run black --check .` and `uv run ruff check .` exit 0.
  </acceptance_criteria>
  <done>A real-build gate exists that produces the documented filename and fails loudly on the three published statements that are still wrong.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the README's Quick Start and Configuration Options</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 63-126 (the Quick Start section: `Basic Configuration`, `Build Typst Output`, `Example Document`) and lines 199-208 (the Configuration Options bullet list)
    - typsphinx/builder.py lines 28-47 (`_default_typst_documents()` — the derivation, the precedence comment, and the five-element tuple shape)
    - typsphinx/__init__.py line 44 (the `app.add_config_value` registration that makes the default a callable)
    - docs/source/user_guide/configuration.rst lines 23-70 (the tuple element semantics already published — the README must not contradict them)
    - .planning/phases/44-typst-documents-default-derivation-builder-input-hardening/44-CONTEXT.md (D-01, D-04 and especially D-05 — the unset-config change is not merely a rename: the derived entry makes `root_doc` a master, so the emitted `.typ` gains the full template)
    - tests/test_quickstart_docs_gate.py (the two README assertions this task must turn green)
  </read_first>
  <action>
    ROADMAP SC#1 names five statements the README Quick Start must make. Today it makes none of them:
    the Quick Start section (lines 63-126) does not mention `typst_documents` at all, and the
    Configuration Options bullet at line 203 asserts the setting is mandatory before any PDF can be
    produced — which CONF-08 made false.

    **Quick Start — `Basic Configuration`.** Extend the `conf.py` example and the prose around it so
    all five statements are present and each is accurate:

    1. What `typst_documents` does: it is the list of master documents, each entry a tuple of
       `(source, target, title, author, documentclass)`, and each entry produces one emitted `.typ`
       and — under the `typstpdf` builder — one compiled `.pdf`.
    2. When it must be set: never, for a single-master project. Leaving it unset is supported and
       is what the Quick Start's own flow does.
    3. The derived default: an unset value resolves to a single entry derived from `root_doc`,
       `project` and `author`, whose target stem is `project` run through the same
       `make_filename_from_project` helper Sphinx's own LaTeX builder uses — so `project = "My Project"`
       yields `myproject.typ` and, under `typstpdf`, `myproject.pdf`. State the `<project>.typ` name
       shape explicitly, since that is the surprise SEED-001 recorded.
    4. That an explicit setting overrides the derived default, and that this includes an explicit
       empty list — Sphinx resolves the user's raw config value before falling back to the callable
       default, so an explicit `[]` means "no masters", not "use the default".
    5. Which documents become PDFs: exactly the entries in `typst_documents` (or the single derived
       entry). A document reached only through a toctree is not a separate PDF — it is emitted as its
       own `.typ` and pulled into its master through a Typst `#include()`.

    Also carry Phase 44's D-05 finding: leaving `typst_documents` unset is not merely an output-name
    change. The derived entry makes `root_doc` a master document, so the emitted `.typ` gains the
    full template wrapper it did not previously receive. Do not describe the change as a rename alone.

    **Configuration Options list (line 203).** Rewrite the `typst_documents` bullet. Its trailing
    clause today asserts the setting is a precondition for producing a PDF at all; replace that with
    a statement that the setting is optional, naming the derived default and the fact that an
    explicit value wins. Leave the other four bullets and the pointer to
    `docs/source/user_guide/configuration.rst` untouched.

    Keep the README's existing voice and formatting conventions — fenced `python` blocks for
    `conf.py` snippets, `-` bullets, `##`/`###` headings. Do not restructure sections that are not
    named above.
  </action>
  <verify>
    <automated>uv run pytest "tests/test_quickstart_docs_gate.py::TestPublishedQuickstartTextMatchesBuild::test_readme_quickstart_explains_typst_documents" "tests/test_quickstart_docs_gate.py::TestPublishedQuickstartTextMatchesBuild::test_readme_no_longer_calls_typst_documents_mandatory" -v</automated>
  </verify>
  <acceptance_criteria>
    - Test: both README tests in `TestPublishedQuickstartTextMatchesBuild` pass.
    - Source: the README's Quick Start section slice (between its own heading and the next same-level heading) contains `typst_documents`.
    - CLI: `grep -c 'required for PDF output' README.md` returns 0.
    - Source: the README states the derived target stem shape and names `make_filename_from_project` or its behaviour explicitly enough that `project = "My Project"` producing `myproject.pdf` is derivable by a reader.
    - Source: the README states that an explicit `typst_documents` value overrides the derived default.
    - Source: the README states that documents reached through a toctree are included into their master rather than becoming separate PDFs.
    - CLI: `git diff --stat -- typsphinx/ docs/source/user_guide/templates.rst` prints nothing.
  </acceptance_criteria>
  <done>The README Quick Start makes all five statements ROADMAP SC#1 names, and the false mandatory-setting claim is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Correct quickstart.rst's output path and add the derived default to configuration.rst</name>
  <files>docs/source/quickstart.rst, docs/source/user_guide/configuration.rst</files>
  <read_first>
    - docs/source/quickstart.rst lines 40-91 (the `Your First PDF` steps — step 3 names the output path — and the `Configuration Options` block that sets `typst_documents` explicitly)
    - docs/source/user_guide/configuration.rst lines 23-70 (the `Typst Documents` section: its opening sentence, its code block, and the five-item element list Phase 44.2 wrote items 3 and 4 of)
    - typsphinx/builder.py lines 28-47 (the derivation and its precedence docstring)
    - README.md (as task 2 left it — the two pages must not contradict each other)
    - tests/test_quickstart_docs_gate.py (the `test_quickstart_names_the_measured_output_path` assertion this task must turn green)
  </read_first>
  <action>
    Two documentation pages, both currently wrong or incomplete about the same fact.

    **`docs/source/quickstart.rst` — `Your First PDF`.** Step 3 names an output path under
    `build/pdf/` derived from the root document's name. With `typst_documents` unset — which is what
    this flow does — CONF-08's derived default names the file from `project` instead, so the stated
    path is measurably false. Correct step 3 to name the path the fixture build in task 1 actually
    produces, `build/pdf/myproject.pdf`, and add a short note explaining where that filename comes
    from: `project` run through Sphinx's own project-filename helper, the same rule `-b latex` uses.
    The note must make it obvious that a reader whose `project` differs gets a different filename;
    do not present `myproject` as a constant. If the steps do not already set `project`, add it to
    the flow so the reader can see where the name comes from — keeping the example's `project`
    value equal to the one the Configuration Options block below already publishes.

    **`docs/source/quickstart.rst` — `Configuration Options`.** The block sets `typst_documents`
    explicitly. Keep it (an explicit setting is a legitimate thing to show) but add one sentence
    stating the value is optional and that omitting it derives a single master entry, cross-linking
    to the configuration page's fuller treatment with a `:doc:` reference to
    `user_guide/configuration`.

    **`docs/source/user_guide/configuration.rst` — `Typst Documents`.** Its opening sentence
    ("Define which documents to build") gives no hint that the value is optional or derived. Add a
    paragraph immediately after that sentence and before the code block stating: the setting is
    optional; when it is absent, typsphinx derives a single entry from `root_doc`, `project` and
    `author`, with the target stem produced by the same `make_filename_from_project` helper Sphinx's
    LaTeX builder uses; an explicitly-set value always wins, including an explicit empty list,
    because Sphinx resolves a raw user-set config value before falling back to a callable default;
    and the derived entry makes the root document a master, so its emitted `.typ` receives the full
    template wrapper.

    Do NOT touch items 3 (document title) and 4 (author) of the element list, or the
    `Author Information` cross-reference they carry — that text is Phase 44.2's, and rewriting it
    here would put this phase's edit inside another phase's evidence range. Do NOT edit
    `docs/source/user_guide/templates.rst`; its custom-template parameter contract belongs to Phase
    45.1 / DOC-13, whose own success criterion must check the published contract rather than inherit
    an edit made here.

    Both pages are rendered by `tox -e docs-pdf` as well as `docs-html`, so keep to the rST
    constructs already used on these pages (`.. code-block::`, `:doc:`, `~`-underlined subsections)
    and add no new directive types.
  </action>
  <verify>
    <automated>uv run pytest tests/test_quickstart_docs_gate.py -v && uv run python - <<'PY'
import pathlib, subprocess, sys, tempfile
cfg = pathlib.Path("docs/source/user_guide/configuration.rst").read_text(encoding="utf-8")
assert "make_filename_from_project" in cfg or "project-filename" in cfg, "configuration.rst does not describe the derived stem rule"
assert "optional" in cfg.lower(), "configuration.rst does not state that typst_documents is optional"
base = subprocess.run(["git", "show", "HEAD:docs/source/user_guide/templates.rst"], capture_output=True, text=True).stdout
now = pathlib.Path("docs/source/user_guide/templates.rst").read_text(encoding="utf-8")
assert base == now, "templates.rst was modified; it belongs to Phase 45.1"
out = tempfile.mkdtemp()
r = subprocess.run([sys.executable, "-m", "sphinx", "-b", "html", "docs/source", out], capture_output=True, text=True)
assert r.returncode == 0, r.stdout + r.stderr
noisy = [l for l in (r.stdout + r.stderr).splitlines() if "WARNING" in l and ("quickstart" in l or "configuration" in l)]
assert not noisy, "warnings on the edited pages: " + repr(noisy)
print("OK docs pages")
PY</automated>
  </verify>
  <acceptance_criteria>
    - Test: `uv run pytest tests/test_quickstart_docs_gate.py -v` exits 0 with every test passing — the recorded-RED text assertions from task 1 are now green.
    - Source: `docs/source/quickstart.rst` contains the literal `build/pdf/myproject.pdf`, and that stem equals `make_filename_from_project("My Project")` as computed by the gate.
    - CLI: `grep -c 'build/pdf/index.pdf' docs/source/quickstart.rst` returns 0.
    - Source: `docs/source/user_guide/configuration.rst`'s `Typst Documents` section states the setting is optional, describes the derived entry, and states that an explicit value (including an empty list) wins.
    - CLI: `git diff -- docs/source/user_guide/templates.rst` prints nothing.
    - Behavior: the element-list items 3 and 4 in `configuration.rst` are byte-identical to `git show HEAD:docs/source/user_guide/configuration.rst`'s.
    - Behavior: `uv run python -m sphinx -b html docs/source <tmp>` exits 0 with no WARNING line mentioning `quickstart` or `configuration`.
  </acceptance_criteria>
  <done>Both documentation pages describe the derived default accurately, `quickstart.rst` names the filename a real build of its own steps produces, and Phase 44.2's and Phase 45.1's text is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published documentation → user's `conf.py` | Documentation this plan edits is copied verbatim by readers into their own build configuration; a wrong statement becomes a wrong configuration in every downstream project |
| test fixture → real `sphinx-build` subprocess | The gate spawns `sys.executable -m sphinx` and compiles Typst, executing code paths under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-09 | Repudiation | Documentation asserting a behaviour that was never measured — the exact failure mode DOC-11 exists to close (`README.md:203` and `quickstart.rst:67` are both currently false) | medium | mitigate | Every claim about the output filename is bound to a real `-b typstpdf` build of a fixture mirroring the published steps, and the expected stem is computed from the same `make_filename_from_project` helper the builder calls rather than hardcoded |
| T-45-10 | Tampering | A future edit silently re-introducing the false claim or renaming the derived target | low | mitigate | `TestPublishedQuickstartTextMatchesBuild` runs in every CI lane (no skip, no slow marker) and fails if the prose and the measured build diverge |
| T-45-11 | Information Disclosure | The gate fixture writing build output into the repository tree | low | mitigate | The gate builds into pytest's `tmp_path`, following `tests/test_default_typst_documents_gate.py`; nothing is written under `tests/fixtures/` at run time |
</threat_model>

<flagged_assumptions>
- **QUA-02 edge-probe row (`unclassified`) — UNRESOLVED, surfaced not dropped.** The deterministic
  edge probe returned an `unclassified` category for QUA-02 with the placeholder prompt "review
  manually". Per the spec-less fallback rules an `unclassified` row must not be auto-resolved with a
  backstop, so it is recorded here as a flagged assumption rather than authored into `must_haves`.
  Reason it cannot be classified: QUA-02 is a pure refactor whose entire acceptance bar is
  *identity* — the observable behaviour before and after must be indistinguishable — so no
  boundary/empty/ordering/concurrency edge exists for it to probe; the only meaningful edge is
  "does the warning text change", which plan 45-04 pins directly with a byte-identity assertion and
  green-both-sides regression runs. Disposition: flagged-unverified, no check path fabricated.
- **DOC-11 empty-input edge, authored as a backstop truth:** behaviour when `project` is left at
  Sphinx's own default sentinel is recorded (Phase 44 D-01 measured that the sentinel is preserved
  rather than special-cased) but this phase adds no dedicated fixture for it — the existing
  `tests/fixtures/default_typst_documents_gate/` covers the unset-`typst_documents` path with a real
  project name. At verify time an unconfirmable backstop truth abstains to `human_needed`.
</flagged_assumptions>

<verification>
- `uv run pytest tests/test_quickstart_docs_gate.py -v` fully green.
- `uv run pytest tests/test_default_typst_documents_gate.py -v` still green — the pre-existing CONF-08 gate is untouched.
- `uv run python -m sphinx -b html docs/source <tmp>` and `-b typstpdf` both exit 0.
- `git diff --stat -- typsphinx/ docs/source/user_guide/templates.rst` prints nothing.
- `uv run black --check .`, `uv run ruff check .` clean.
</verification>

<success_criteria>
- ROADMAP SC#1: all five statements present in the README Quick Start, each checked against a real build of the Quick Start's own steps.
- The two measured-false published claims (the README's mandatory-setting bullet and quickstart's output path) are corrected, and a standing test prevents their return.
</success_criteria>

## Artifacts this phase produces

New symbols created by phase 45 (all four plans) — exclude these from drift/source-grounding checks:

- `pyproject.toml` — new dependency entry `myst-parser>=5.0` in `[project.optional-dependencies].docs`
- `docs/source/conf.py` — new `extensions` member `"myst_parser"`
- `docs/source/changelog.rst` — new `Release History` section and the `.. include:: ../../CHANGELOG.md` directive with options `:parser: myst_parser.sphinx_` and `:start-line: 7`
- `CHANGELOG.md` — new `## [0.4.4] - 2026-07-05` section and new `[0.4.4]:` link-reference line
- `tests/test_changelog_page_gate.py` — new test module; classes `TestPublishedChangelogPageDelegates`, `TestChangelogPageContentCoverage`, `TestChangelogIncludeCompilesToPdf`
- `tests/fixtures/changelog_include_gate/conf.py`, `tests/fixtures/changelog_include_gate/index.rst`, `tests/fixtures/changelog_include_gate/changelog.rst` — new fixture project
- `tests/test_quickstart_docs_gate.py` — new test module; classes `TestQuickstartFirstPdfGate`, `TestPublishedQuickstartTextMatchesBuild`
- `tests/fixtures/quickstart_docs_gate/conf.py`, `tests/fixtures/quickstart_docs_gate/index.rst` — new fixture project mirroring the published Quick Start
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-include-shape.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-02-docs-build-clean.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-qua03-comment-scan.md`

<output>
Create `.planning/phases/45-documentation-currency-carried-hygiene/45-03-SUMMARY.md` when done.
</output>
