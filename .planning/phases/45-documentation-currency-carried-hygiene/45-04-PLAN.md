---
phase: 45-documentation-currency-carried-hygiene
plan: 04
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - typsphinx/template_engine.py
  - .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-qua03-comment-scan.md
  - .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-phase-terminal.md
autonomous: true
requirements: [QUA-02, QUA-03]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`derive_typst_lang()` contains exactly one `logger.warning(` call site, verified by a scan bounded to the function's own body with docstrings and comment lines removed (ROADMAP SC#3's grep-over-the-branches bar)."
    - "The rejection warning's rendered text is byte-identical to the pre-refactor text for every input the corpus exercises; `tests/test_template_engine.py::TestDeriveTypstLang` and `tests/test_typst_lang_gate.py` are green on both sides of the refactor, and both assert warning content rather than merely absence of a crash — so a green run on both sides IS the identity proof."
    - "`.planning/PROJECT.md` contains zero unterminated `<!--`, measured by a fence-aware and backtick-aware opener-stack scan of the whole file rather than a token-count comparison (D-09)."
    - "The scan pairs an opener with a closer that appears later on the same line, so a single-line comment contributes nothing to the residual stack (QUA-03 adjacency edge)."
    - "The scan returns a defined result for input containing zero openers — residual stack length 0 and opener count 0, not an error (QUA-03 empty-input edge)."
    - "The scan pairs each closer with the most recently unmatched opener (LIFO) and reports any residual opener line numbers in ascending order (QUA-03 ordering edge)."
    - "The QUA-03 scan modifies no file, and its evidence record names the exact commit SHA it was run against, so a later re-run is comparable rather than merely repeated (QUA-03 concurrency edge)."
    - "The phase's record states that the two openers the source todo recorded at `279aea5` were closed by a deliberate, attributed repair — commit `43a2a78`, Phase 41 plan 03, decision D-13 — not incidentally (D-08, correcting CONTEXT.md's own phrasing)."
    - "The full pytest suite plus `black --check .`, `ruff check .` and `mypy typsphinx/` are green over the whole phase's tree, and the only `typsphinx/` change since the phase's baseline SHA is the single-site warning refactor (ROADMAP SC#5)."
  artifacts:
    - "typsphinx/template_engine.py — `derive_typst_lang()` restructured to one warning call site"
    - ".planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-qua03-comment-scan.md"
    - ".planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-phase-terminal.md"
  key_links:
    - "typsphinx/template_engine.py `derive_typst_lang()` → docs/source/conf.py's own import of it (this repository dogfoods the helper) → both docs builds; a behaviour change here would surface in the phase's own docs evidence"
    - "the single tail `logger.warning(...)` → `tests/test_template_engine.py`'s caplog assertion that `repr(value)` appears in the message → the wording constraint that forbids distinguishing the two rejection reasons"
    - "45-GATE-EVIDENCE-01-docs-build-baseline.md `baseline_sha` → the SC#5 scope diff's left-hand side"
  prohibitions:
    - "No comment-balance recurrence guard is added to the pytest suite or to `scripts/` — D-07 declines it explicitly; the scan is a one-off diagnostic recorded as evidence."
    - "`.planning/PROJECT.md` is not edited — QUA-03 closes on verification alone."
    - "The two rejection reasons are not distinguished in the warning's wording; distinguishing them would change build output and fail SC#3."
    - "The four `@preview` package version declarations in `typsphinx/template_engine.py` (`codly`, `codly-languages`, `mitex`, `gentle-clues`) are not touched — `tests/test_preview_version_sync.py` asserts they agree with `writer.py` and `templates/base.typ`."
    - "Typing imports are not modernized — `CLAUDE.md` and the pending `modernize-typing-imports` todo both forbid it until that todo lands."
    - "No `typsphinx/` file other than `template_engine.py` is modified, and within it no function other than `derive_typst_lang` changes behaviour."
---

<objective>
Close the two carried hygiene items and seal the phase: `derive_typst_lang()` emits its rejection
warning from exactly one site with byte-identical wording, `.planning/PROJECT.md` is verified to hold
zero unterminated `<!--` with the closing commit named, and the whole phase's tree is proven green
with `typsphinx/` change confined to that single refactor.

Purpose: ROADMAP SC#3, SC#4 and SC#5.
Output: a one-site warning, two evidence records, and a terminal green gate over the full phase.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-CONTEXT.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-RESEARCH.md
@.planning/phases/45-documentation-currency-carried-hygiene/45-PATTERNS.md
@CLAUDE.md
</context>

<worktree_protocol>
Provision first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`.
Run every command through `uv run`. This plan is wave 3, so its worktree already carries plans
45-01, 45-02 and 45-03's merged changes — the terminal gate in task 3 measures the whole phase, not
just this plan.
</worktree_protocol>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consolidate derive_typst_lang()'s rejection warning to a single call site</name>
  <files>typsphinx/template_engine.py</files>
  <read_first>
    - typsphinx/template_engine.py lines 84-150 (the whole function: its ~45-line docstring recording CONF-07's D-02/D-03/D-04 rationale, the two rejection branches at 131-137 and 144-150, and the byte-identical four-line `logger.warning(...)` in each)
    - tests/test_template_engine.py lines 1159-1220 (`TestDeriveTypstLang` — the parametrized accept cases and `test_malformed_inputs_return_none_and_warn`, whose caplog assertion pins that `repr(value)` appears in the message and pins no call count)
    - tests/test_typst_lang_gate.py (`TestMalformedLanguage` and the module docstring explaining that it deliberately does not pin warning text; the other four real-build fixture classes are the rest of the lang corpus)
    - docs/source/conf.py lines 97-113 (this repository imports and calls `derive_typst_lang` itself — a behaviour change here changes this project's own docs build)
    - .planning/todos/pending/2026-07-25-derive-typst-lang-duplicated-warning-block.md (the source record; written in Japanese — it carries the Phase 27.1 review IN-01 framing and the constraint that the caplog test must survive)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-RESEARCH.md (§ Architecture Patterns Pattern 3 — the illustrative single-tail shape and why it is compatible with both pinning tests)
  </read_first>
  <behavior>
    - `derive_typst_lang("ja")` returns `"ja"`; `derive_typst_lang("zh_CN")` returns `"zh"`; `derive_typst_lang("pt-BR")` returns `"pt"`; `derive_typst_lang("sr@latin")` returns `"sr"`; `derive_typst_lang("JA")` returns `"ja"`; `derive_typst_lang("xyz")` returns `"xyz"` and does NOT warn.
    - `derive_typst_lang(None)`, `derive_typst_lang("")`, `derive_typst_lang(123)`, `derive_typst_lang("a")`, `derive_typst_lang("abcd")`, `derive_typst_lang("_")`, `derive_typst_lang("日本語")` each return `None` and emit exactly one warning whose message contains `repr(input)`.
    - The warning message text is byte-identical for every rejected input regardless of which rejection reason applies — the two reasons are NOT distinguished.
    - `derive_typst_lang` never raises for any input.
  </behavior>
  <action>
    Capture the pre-refactor evidence first, then restructure, then prove identity.

    **Step 1 — record the baseline.** Run
    `uv run pytest tests/test_template_engine.py::TestDeriveTypstLang tests/test_typst_lang_gate.py -v`
    and record the pass counts in the plan summary. Both modules assert on warning presence and
    content, not merely on absence of a crash, so a green run on both sides of the refactor IS the
    warning-for-warning identity proof ROADMAP SC#3 asks for — no new baseline-capture harness is
    needed. Additionally capture the exact rendered message for one rejected input (for example
    `derive_typst_lang("abcd")` under `caplog`) and paste it verbatim into the summary; that string
    is the byte-identity reference for step 3.

    **Step 2 — restructure to one call site.** The function currently has two early-return rejection
    branches, each ending in its own verbatim copy of the same four-line `logger.warning(...)` call
    (lines 131-137 and 144-150). Restructure so both rejection paths reach a single warning call.
    The shape is yours to choose — a nested helper, an early-return guard, or a restructured single
    tail — but two constraints are hard: exactly ONE `logger.warning(` call site inside the function,
    and the message's rendered text byte-identical to the baseline. In particular the two rejection
    reasons must NOT be distinguished in the wording; distinguishing them would change build output
    and fail SC#3.

    Preserve the docstring in full. It records CONF-07's D-02 (the generic split-and-lowercase rule
    rather than a per-language table), D-03 (the ASCII-scoped `re.fullmatch(r"[a-z]{2,3}", head)`
    accept test and why `str.isalpha()` is wrong, plus the unknown-but-well-formed code case that
    deliberately does not warn) and D-04 (never applied to a user's explicit
    `typst_elements["lang"]`). Update only what the restructure makes literally inaccurate; do not
    trim it.

    Touch nothing else in `typsphinx/template_engine.py`. In particular the four `@preview` package
    version declarations in this file are one of the three sites `tests/test_preview_version_sync.py`
    holds in lockstep with `writer.py` and `templates/base.typ` — leave them exactly as they are.

    **Step 3 — prove identity.** Re-run the same two test targets and confirm the same pass counts.
    Re-capture the rendered message for the same rejected input and confirm it equals the string
    recorded in step 1 character for character. Then run the structural single-site check.
  </action>
  <verify>
    <automated>uv run pytest tests/test_template_engine.py::TestDeriveTypstLang tests/test_typst_lang_gate.py tests/test_preview_version_sync.py -v && uv run python - <<'PY'
import pathlib, re
src = pathlib.Path("typsphinx/template_engine.py").read_text(encoding="utf-8")
m = re.search(r"^def derive_typst_lang\(.*?(?=^def |\Z)", src, re.S | re.M)
assert m, "derive_typst_lang not found"
body = re.sub(r'(?s)""".*?"""', "", m.group(0))
body = "\n".join(l for l in body.split("\n") if not l.strip().startswith("#"))
n = body.count("logger.warning(")
assert n == 1, "expected exactly one logger.warning( call site in derive_typst_lang, found %d" % n
from typsphinx.template_engine import derive_typst_lang
for good, want in (("ja", "ja"), ("zh_CN", "zh"), ("pt-BR", "pt"), ("sr@latin", "sr"), ("JA", "ja"), ("xyz", "xyz")):
    assert derive_typst_lang(good) == want, "%r -> %r, expected %r" % (good, derive_typst_lang(good), want)
for bad in ("日本語", "", None, "a", "abcd", "_", 123):
    assert derive_typst_lang(bad) is None, "%r should be rejected" % (bad,)
print("call_sites", n, "OK")
PY</automated>
  </verify>
  <acceptance_criteria>
    - CLI: the scan bounded to `derive_typst_lang`'s body, with docstrings and comment lines removed, counts exactly 1 occurrence of `logger.warning(`.
    - Test: `uv run pytest tests/test_template_engine.py::TestDeriveTypstLang -v` exits 0 with the same number of passing tests as the pre-refactor run recorded in step 1.
    - Test: `uv run pytest tests/test_typst_lang_gate.py -v` exits 0 with the same pass/skip counts as the pre-refactor run.
    - Behavior: the rendered warning message for a rejected input is character-for-character equal to the string captured in step 1 and recorded in the plan summary.
    - Test: `uv run pytest tests/test_preview_version_sync.py -v` exits 0 — the four `@preview` declarations in this file are undisturbed.
    - CLI: `git diff --stat -- typsphinx/` lists `typsphinx/template_engine.py` and nothing else.
    - CLI: `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` all exit 0.
  </acceptance_criteria>
  <done>The rejection warning is emitted from exactly one site, its wording is byte-identical, and both pinning test surfaces are green on both sides of the change.</done>
</task>

<task type="auto">
  <name>Task 2: Verify PROJECT.md's comment balance and record the D-08 closing-commit finding</name>
  <files>.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-qua03-comment-scan.md</files>
  <read_first>
    - .planning/todos/pending/2026-07-29-project-md-unterminated-html-comments.md (the source record: its measured line numbers 492/506 at commit `279aea5`, and its step 3 recurrence guard, which D-07 explicitly declines)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-CONTEXT.md (D-07, D-08, D-09 — including D-09's measured false-positive sites)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-RESEARCH.md (§ Code Examples: the QUA-03 verification script and the D-08 bisect finding; § Common Pitfalls, Pitfall 4)
    - tests/test_no_stale_github_io_links.py (the established repo-hygiene shape and its split-literal technique for avoiding self-match — the reference the scan's design follows)
    - .planning/REQUIREMENTS.md around line 141 and .planning/ROADMAP.md around line 731 (the two measured false-positive sites: each carries a backticked comment-opener literal inside prose describing this very requirement)
  </read_first>
  <precondition>`git cat-file -e 279aea5^{commit}` and `git cat-file -e 43a2a78^{commit}` both succeed — the D-08 bisect endpoints must be reachable in this worktree's object store.</precondition>
  <action>
    QUA-03 is verification only. RESEARCH Pitfall 4 warns that the requirement's own text still reads
    as if two comments need closing; they do not. Do not edit `.planning/PROJECT.md`, and do not go
    hunting for false-positive matches inside prose to "fix".

    **Run the scan.** Write a throwaway script (a heredoc, not a committed file — D-07 declines a
    standing guard, so nothing goes into `tests/` or `scripts/`) that walks `.planning/PROJECT.md`
    line by line, toggles a fenced-block flag on lines whose stripped form starts a fence, skips
    lines inside a fence, strips inline backtick spans from each remaining line, then scans left to
    right for comment-open and comment-close tokens, pushing each opener's line number onto a stack
    and popping on each closer. Report the opener count, the closer count, and the residual stack.
    D-09 is binding: a token-count comparison is NOT a valid check for this requirement, because a
    raw count structurally cannot distinguish real openers from backticked mentions — the two live
    planning documents that describe QUA-03 itself are measured false-positive sites for exactly
    that reason.

    **Self-check the scan on three crafted inputs** before trusting its verdict on the real file.
    These answer the edge questions the phase must dispose of, and they cost three extra assertions
    in the same throwaway script:
    (a) a single line carrying an opener and its closer together must contribute nothing to the
        residual stack;
    (b) input containing zero openers must yield opener count 0 and residual stack length 0, and
        must not error;
    (c) two openers followed by one closer must leave exactly one residual opener, and residual line
        numbers must be reported in ascending order — the closer pairs with the most recent
        unmatched opener.

    **Record the evidence.** Write `45-GATE-EVIDENCE-04-qua03-comment-scan.md` containing: the exact
    commit SHA the scan ran against (`git rev-parse HEAD`); the script verbatim so the measurement is
    reproducible without committing a standing guard; its output on `.planning/PROJECT.md` (opener
    count, closer count, residual stack); the three self-check results; and a `## D-08 finding`
    section.

    The D-08 section states the closing commit and its character. Research already located it and
    read it verbatim, exceeding what D-08 asked for: `43a2a78`, "docs(41-03): terminate PROJECT.md's
    two unterminated HTML comments (D-13)", Phase 41 plan 03, 2026-08-03 — it closed exactly the two
    openers the source todo named (moved by file growth to lines 761 and 775 by that point) and its
    message states the post-fix counts. Confirm this yourself with `git show 43a2a78` rather than
    copying the claim, then record the correction explicitly: CONTEXT.md's phrasing that the
    openers were closed "incidentally" is wrong — the repair was **deliberate, self-documented and
    attributed**; only its timing (inside a release-prep-adjacent plan rather than a dedicated
    hygiene phase) reads as incidental. D-08 exists to distinguish these two cases, so say which one
    it was plainly.

    Close with a `## Disposition` section: SC#4 is satisfied on verification alone; no file was
    edited; no recurrence guard was added, per D-07; and if the drift channel ever reopens, D-09
    records the design constraint any future guard must satisfy.
  </action>
  <verify>
    <automated>uv run python - <<'PY'
import pathlib, re, subprocess
def scan(text):
    stack, openers, closers, in_fence = [], 0, 0, False
    for idx, line in enumerate(text.split("\n"), start=1):
        s = line.strip()
        if s.startswith("```") or s.startswith("~~~"):
            in_fence = not in_fence
            continue
        if in_fence:
            continue
        stripped = re.sub(r"`{1,2}[^`\n]*?`{1,2}", "", line)
        for m in re.finditer(r"<!--|-->", stripped):
            if m.group(0) == "<!--":
                openers += 1
                stack.append(idx)
            else:
                closers += 1
                if stack:
                    stack.pop()
    return openers, closers, stack
o, c, stack = scan(pathlib.Path(".planning/PROJECT.md").read_text(encoding="utf-8"))
assert not stack, "unterminated comment openers at lines %r" % stack
assert scan("x <" + "!-- inline -" + "-> y")[2] == [], "same-line pair must not persist"
assert scan("no markers here")[:2] == (0, 0), "zero-opener input must report (0, 0)"
o2, c2, s2 = scan("<" + "!-- a\n<" + "!-- b\n--" + ">\n")
assert len(s2) == 1 and s2 == sorted(s2), "LIFO pairing / ascending residual order failed: %r" % s2
ev = pathlib.Path(".planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-qua03-comment-scan.md").read_text(encoding="utf-8")
assert re.search(r"[0-9a-f]{7,40}", ev), "evidence file names no commit SHA"
assert "43a2a78" in ev, "evidence file does not name the D-08 closing commit"
assert "## D-08 finding" in ev and "## Disposition" in ev, "evidence file is missing a required section"
print("QUA-03 OK: %d openers, %d closers, 0 unterminated" % (o, c))
PY</automated>
  </verify>
  <acceptance_criteria>
    - CLI: the scan reports a residual stack of length 0 for `.planning/PROJECT.md`, with equal opener and closer counts printed.
    - CLI: the three self-checks pass — same-line pair leaves nothing residual; zero-opener input reports `(0, 0)` without erroring; two-openers-one-closer leaves exactly one residual whose line numbers are ascending.
    - Source: `45-GATE-EVIDENCE-04-qua03-comment-scan.md` contains the scan script verbatim, a commit SHA matching `[0-9a-f]{7,40}`, a `## D-08 finding` section naming `43a2a78`, and a `## Disposition` section.
    - Source: the `## D-08 finding` section states in plain words that the repair was deliberate and attributed, and corrects CONTEXT.md's "incidentally" phrasing.
    - CLI: `git status --porcelain -- .planning/PROJECT.md` prints nothing — no edit was made.
    - CLI: `git status --porcelain -- tests/ scripts/` shows no new comment-balance guard file.
  </acceptance_criteria>
  <done>SC#4 is discharged by measurement, the scan's edge behaviour is self-checked, and the phase record names the deliberate commit that closed the defect.</done>
</task>

<task type="auto">
  <name>Task 3: Terminal phase gate — full suite green and typsphinx/ change scoped to QUA-02</name>
  <files>.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-phase-terminal.md</files>
  <read_first>
    - .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md (its `baseline_sha` line is the phase's start point and the left-hand side of the SC#5 scope diff)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-02-docs-build-clean.md (the clean-build delta this gate re-confirms against the final tree)
    - .planning/ROADMAP.md § "Phase 45" (SC#1 through SC#5, verbatim — this gate maps evidence to each)
    - .planning/phases/45-documentation-currency-carried-hygiene/45-VALIDATION.md (§ Sampling Rate — the phase-gate row)
    - tox.ini (the `lint`, `type`, `docs-html` and `docs-pdf` env definitions)
  </read_first>
  <action>
    This plan is wave 3, so this worktree carries every prior plan's changes. Run the phase's
    terminal gate over the whole tree and record it.

    **Run, capturing output for each:** `uv run pytest` (full suite, no `-m` filter, so the
    slow-marked changelog build classes run); `uv run black --check .`; `uv run ruff check .`;
    `uv run mypy typsphinx/`; and both docs builds,
    `uv run python -m sphinx -b html docs/source <scratch>/html` and
    `uv run python -m sphinx -b typstpdf docs/source <scratch>/pdf`.

    **Compute the SC#5 scope diff.** Read `baseline_sha` from
    `45-GATE-EVIDENCE-01-docs-build-baseline.md` and run
    `git diff --stat <baseline_sha> HEAD -- typsphinx/`. SC#5 requires that no `typsphinx/`
    behaviour changed beyond QUA-02's single-site warning refactor, so the only path that may appear
    is `typsphinx/template_engine.py`, and the only hunk in it must be inside `derive_typst_lang`.
    Record the full diff of that file verbatim in the evidence and state explicitly whether any hunk
    falls outside the function; if one does, that is an SC#5 failure to surface, not to explain away.

    **Re-confirm the docs-build delta.** Recompute the three counts against the final tree in the
    same key shape plan 45-01's baseline used, and confirm
    `changelog_attributable_warning_count` is 0 and neither total exceeds its baseline. RESEARCH
    Pitfall 2 stands: neither tox docs env passes `-W`, so exit code 0 alone proves nothing.

    **Write `45-GATE-EVIDENCE-04-phase-terminal.md`** with one section per ROADMAP success criterion,
    SC#1 through SC#5, each naming the specific artifact or command output that discharges it and
    the measured result. For SC#1 cite the `tests/test_quickstart_docs_gate.py` run; for SC#2 cite
    the `tests/test_changelog_page_gate.py` run plus the delta counts; for SC#3 cite the single-site
    scan output and the both-sides-green test runs; for SC#4 cite
    `45-GATE-EVIDENCE-04-qua03-comment-scan.md`; for SC#5 cite the full-suite result and the scope
    diff. Include the final HEAD SHA and the pytest summary line verbatim.

    Also carry forward, under a `## Carried to the milestone close` heading: the `ja` catalogue
    regeneration owed in the separate `typsphinx-doc-translations` repository for every line the
    changelog include newly surfaces, and the two RESEARCH open questions' measured answers from
    `45-GATE-EVIDENCE-01-include-shape.md`.
  </action>
  <verify>
    <automated>uv run pytest && uv run black --check . && uv run ruff check . && uv run mypy typsphinx/ && uv run python - <<'PY'
import pathlib, re, subprocess
ev_dir = pathlib.Path(".planning/phases/45-documentation-currency-carried-hygiene")
base = re.search(r"^- baseline_sha: ([0-9a-f]{7,40})$", (ev_dir / "45-GATE-EVIDENCE-01-docs-build-baseline.md").read_text(encoding="utf-8"), re.M)
assert base, "baseline_sha not found in the plan-45-01 evidence file"
names = subprocess.run(["git", "diff", "--name-only", base.group(1), "HEAD", "--", "typsphinx/"], capture_output=True, text=True).stdout.split()
assert names == ["typsphinx/template_engine.py"], "SC#5 scope violation: typsphinx/ files changed: %r" % names
term = (ev_dir / "45-GATE-EVIDENCE-04-phase-terminal.md").read_text(encoding="utf-8")
for sc in ("SC#1", "SC#2", "SC#3", "SC#4", "SC#5"):
    assert sc in term, "terminal evidence does not address " + sc
assert "## Carried to the milestone close" in term, "terminal evidence is missing the carry-forward section"
print("terminal gate OK")
PY</automated>
  </verify>
  <acceptance_criteria>
    - Test: `uv run pytest` exits 0 with zero failures and zero errors over the full suite (slow-marked classes included).
    - CLI: `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` each exit 0.
    - CLI: `git diff --name-only <baseline_sha> HEAD -- typsphinx/` outputs exactly `typsphinx/template_engine.py` and nothing else.
    - Behavior: every hunk in that file's diff falls inside `derive_typst_lang`'s span; the evidence file reproduces the diff and states this explicitly.
    - Behavior: both `uv run python -m sphinx -b html docs/source <tmp>` and `-b typstpdf` exit 0, with `changelog_attributable_warning_count` equal to 0 and neither total warning count above its baseline.
    - Source: `45-GATE-EVIDENCE-04-phase-terminal.md` contains a section for each of SC#1 through SC#5 naming the discharging artifact and result, the final HEAD SHA, the verbatim pytest summary line, and a `## Carried to the milestone close` section.
  </acceptance_criteria>
  <done>The whole phase is proven green, each ROADMAP success criterion has a named piece of evidence, and `typsphinx/` change is demonstrably confined to the QUA-02 refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sphinx build-time config → `derive_typst_lang()` | A user's `language` config value (arbitrary string, including non-ASCII) flows into this helper and its output reaches a Typst `lang` parameter |
| planning record → downstream readers | `.planning/PROJECT.md` is read by every subsequent agent; an unterminated comment silently swallows the remainder of the file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-12 | Tampering | `derive_typst_lang()` refactor weakening the ASCII-scoped accept test, letting a CJK/Cyrillic `language` value through to Typst and aborting the compile (ASVS V5, the only input-validation surface this phase touches) | medium | mitigate | The restructure preserves the `re.fullmatch(r"[a-z]{2,3}", head)` test unchanged; `tests/test_template_engine.py::TestDeriveTypstLang`'s six parametrized malformed inputs (including a CJK string) and `tests/test_typst_lang_gate.py`'s five real-build fixture classes are green acceptance gates on both sides of the change |
| T-45-13 | Repudiation | Claiming warning-for-warning identity without measuring it | medium | mitigate | The exact rendered message for a rejected input is captured verbatim before and after and compared character for character, on top of both pinning suites running green on both sides |
| T-45-14 | Information Disclosure | An unterminated `<!--` in a planning record hiding the remainder of the file from every downstream reader — the defect QUA-03 names | low | mitigate | Whole-file fence- and backtick-aware opener-stack scan, self-checked on three crafted inputs, result recorded against a named SHA |
| T-45-15 | Tampering | The scan being written naively (token counts) and reporting a false clean because prose false positives happen to balance | medium | mitigate | D-09 forbids the count comparison; the scan is an opener-stack walk with fence and inline-backtick exclusion, and its own self-check proves the pairing semantics rather than assuming them |
| T-45-16 | Elevation of Privilege | An out-of-scope `typsphinx/` change riding along inside a documentation phase | medium | mitigate | The terminal gate diffs `typsphinx/` against the phase's baseline SHA and fails on any path other than `template_engine.py`, with the hunk locations recorded verbatim |
</threat_model>

<flagged_assumptions>
- **QUA-02 edge-probe row (`unclassified`) — UNRESOLVED, surfaced not dropped.** The deterministic
  edge probe returned `unclassified` for QUA-02 ("review manually"). Per the spec-less fallback
  rules an `unclassified` row must stay unresolved and be surfaced to the planner rather than
  auto-resolved with a backstop. Reason it resists classification: QUA-02's entire acceptance bar is
  *identity* — before and after must be indistinguishable — so no boundary, empty-input, ordering or
  concurrency edge is distinguishable from the requirement itself. The one meaningful edge, "does
  the warning text change", is pinned directly by task 1's byte-identity capture and the
  both-sides-green regression runs. Disposition: flagged-unverified; no check path fabricated, no
  auto-dismissal.
</flagged_assumptions>

<verification>
- `uv run pytest` full suite green; `black --check .`, `ruff check .`, `mypy typsphinx/` clean.
- Exactly one `logger.warning(` call site inside `derive_typst_lang`.
- The rendered rejection message is byte-identical to the pre-refactor capture.
- `.planning/PROJECT.md` scan: zero residual openers; three self-checks pass; `.planning/PROJECT.md` unmodified.
- `git diff --name-only <baseline_sha> HEAD -- typsphinx/` outputs only `typsphinx/template_engine.py`.
- Both docs builds exit 0 with zero changelog-attributable warnings.
</verification>

<success_criteria>
- ROADMAP SC#3: one warning site, warning-for-warning identical output over the existing lang corpus.
- ROADMAP SC#4: zero unterminated comment openers in `.planning/PROJECT.md`, whole-file scan, with the closing commit named and characterised.
- ROADMAP SC#5: full suite plus the lint/type trio green, and no `typsphinx/` behaviour changed beyond the single-site refactor.
</success_criteria>

## Artifacts this phase produces

New symbols created by phase 45 (all four plans) — exclude these from drift/source-grounding checks:

- `pyproject.toml` — new dependency entry `myst-parser>=5.0` in `[project.optional-dependencies].docs`
- `docs/source/conf.py` — new `extensions` member `"myst_parser"`
- `docs/source/changelog.rst` — new `Release History` section and the `.. include:: ../../CHANGELOG.md` directive with options `:parser: myst_parser.sphinx_` and `:start-line: 7`
- `CHANGELOG.md` — new `## [0.4.4] - 2026-07-05` section and new `[0.4.4]:` link-reference line
- `tests/test_changelog_page_gate.py` — new test module; classes `TestPublishedChangelogPageDelegates`, `TestChangelogPageContentCoverage`, `TestChangelogIncludeCompilesToPdf`
- `tests/fixtures/changelog_include_gate/conf.py`, `tests/fixtures/changelog_include_gate/index.rst`, `tests/fixtures/changelog_include_gate/changelog.rst` — new fixture project
- `tests/test_quickstart_docs_gate.py` — new test module; classes `TestQuickstartFirstPdfGate`, `TestPublishedQuickstartTextMatchesBuild`
- `tests/fixtures/quickstart_docs_gate/conf.py`, `tests/fixtures/quickstart_docs_gate/index.rst` — new fixture project mirroring the published Quick Start
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-docs-build-baseline.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-01-include-shape.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-02-docs-build-clean.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-qua03-comment-scan.md`
- `.planning/phases/45-documentation-currency-carried-hygiene/45-GATE-EVIDENCE-04-phase-terminal.md`

<output>
Create `.planning/phases/45-documentation-currency-carried-hygiene/45-04-SUMMARY.md` when done.
</output>
