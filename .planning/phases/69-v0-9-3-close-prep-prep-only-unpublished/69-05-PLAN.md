---
phase: 69-v0-9-3-close-prep-prep-only-unpublished
plan: 05
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PREFLIGHT-EVIDENCE.md
autonomous: true
requirements: [REL-12]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-07: after `git fetch origin`, `git merge-tree --write-tree TRIAL_HEAD ORIGIN_MAIN_SHA` exits 0, and the tree SHA it prints is recorded as MERGE_TREE. TRIAL_HEAD is this worktree HEAD before any commit, and it carries the three CHANGELOG bullets. Re-running the same command from the recorded inputs reproduces MERGE_TREE.'
    - 'D-07: `pyproject.toml` and `uv.lock` are extracted from MERGE_TREE into a scratch directory outside the repository with `git archive`, and `uv --directory SCRATCH lock --check` exits 0. The merged pyproject.toml:7 still reads `version = "0.9.2"`, and the merged uv.lock ruff version is recorded as MERGED_RUFF_LOCK_VERSION.'
    - 'D-07 non-committing: no merge commit is created and no ref of the branch moves. `git rev-list --merges BASE_69_05..HEAD` is empty, origin/main is not an ancestor of HEAD (D-06), and scratch directories are removed.'
    - 'D-07 optional lint, exercised: a scratch copy of MERGE_TREE is synced with `--locked` from its own uv.lock, and `ruff check .` and `black --check .` are run there. The ruff version that runs equals MERGED_RUFF_LOCK_VERSION, and both exit codes are recorded. A non-zero exit is a finding for the owner, not fixed here.'
    - 'D-08 inputs: `main` protection is recorded live: `strict: true` and exactly six required contexts, namely Test Python 3.12 on ubuntu-latest, Test Python 3.13 on ubuntu-latest, Lint and Format Check, Type Check, Code Coverage and Build Package. The first-parent history of origin/main shows #135 and #136 as Merge pull request commits.'
    - 'D-10 input: dependabot pull requests #139, #140, #141 and #142 are each read with `gh pr view` (state, base branch, title, updatedAt) at a recorded time. No action is taken on any of them.'
    - 'REL-12 is cited for coverage only; SUMMARY requirements-completed: [].'
  artifacts:
    - path: .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PREFLIGHT-EVIDENCE.md
      provides: 'trial merge, merged-lock check, merged-tree lint, main protection census, merge-method precedent, dependabot PR census'
      contains: 'MERGE_TREE = '
  key_links:
    - from: 'TRIAL_HEAD and ORIGIN_MAIN_SHA'
      to: 'MERGE_TREE'
      via: 'git merge-tree --write-tree, which writes objects only and moves no ref'
      pattern: 'MERGE_RC = '
    - from: 'MERGE_TREE uv.lock'
      to: 'the D-08 update step in 69-HANDOFF.md'
      via: 'uv lock --check in scratch and a locked scratch sync, proving the post-update lock is valid before the operator reaches it'
      pattern: 'LOCK_CHECK_EXIT = '
  prohibitions:
    - statement: 'MUST NOT run `git merge`, `git rebase`, `git commit --amend` or any ref-moving command for the trial. It uses `git merge-tree --write-tree` only, and nothing from it is committed (D-07, D-06).'
      requirement_id: REL-12
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT act on dependabot pull requests #139..#142: no merge, close, comment or rebase request (D-10).'
      requirement_id: REL-12
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT copy the tree SHA, lock result or protection list from CONTEXT or RESEARCH; every value comes from a command run here. MUST NOT install any package outside a committed uv.lock.'
      requirement_id: REL-12
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Measure, without committing anything to the branch, that the REL-12 update step
`/gsd-complete-milestone` will run is safe (D-07). That step is a merge of `origin/main` into the
milestone branch. The plan measures that the merge is conflict-free, that the merged `uv.lock` is
valid, and that the merged tree lints cleanly at the merged lock's `ruff` version. It also records
the live facts the handoff's branch-update step depends on:
- `main`'s strict protection and its six required checks (D-08)
- the merge-commit method prior milestone PRs used (D-08)
- the state of dependabot PRs #139..#142, which are left untouched (D-10)

Purpose: `main`'s protection is `strict: true`, so the REL-12 PR cannot merge until the branch is up
to date. This plan makes the update step known-good before the operator reaches it. Because the trial
merge must include the CHANGELOG edit, it runs in wave 2, after 69-01, in parallel with 69-03 and
69-04 and sharing no file with either.

Output: `69-PREFLIGHT-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-RESEARCH.md
@.planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CONTEXT.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and run project
tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Scratch directories.** Create each with `mktemp -d`, outside the repository, record its path, and
remove it with `rm -rf` when the step finishes.

**Invoking uv on a scratch directory.** Run every `uv` command from the worktree root and point it
at the scratch directory with `uv --directory <scratch> …`. The `uv` shim resolves this worktree's
own `.venv/bin/uv` (the lock-pinned version) from the current directory, inside the FHS sandbox, and
every process it spawns inherits the sandbox. Run from inside the scratch directory, the shim would
fall back to nixpkgs' `uv`.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put explanations on the
next line.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
`## HALT` is written only on a failure path; the gates require its absence.

## Planning-time census (2026-09-13; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `origin/main` beyond the milestone base | 4 commits: `7cc85d28`, `293f0c26` (#137, dependabot `pip` → `uv`) and `88088071`, `cf3305ce` (#138, ruff 0.15.20 → 0.16.6) | `git log HEAD..origin/main` |
| Trial merge | rc 0. The tree SHA was `7c9e3002…` at discussion and `2a8a5907…` at research; it moves with every commit, so it is re-measured here. | 69-CONTEXT, 69-RESEARCH Pattern 3 |
| Merged lock | `uv lock --check` → `Resolved 91 packages`, rc 0; ruff 0.16.6; `tox-uv>=1.35,<2`, `ruff>=0.15,<0.17` | 69-CONTEXT `<domain>` |
| `main` protection | `strict: true`; contexts `Test Python 3.12 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`, `Test Python 3.13 on ubuntu-latest` | `gh api repos/YuSabo90002/typsphinx/branches/main/protection` |
| Open dependabot PRs | #139 (`tox`), #140 (`sphinx-intl`), #141 (`pre-commit`), #142 (`mypy`), lockfile-only, base `main` | 69-CONTEXT `<domain>` |
| ruff on the merged tree | 0.16.6 raised no violation on `main` (#138's Lint job) or on the milestone tip at 67's measurement | 67-CONTEXT D-03 |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: fetch, non-committing trial merge of origin/main, and the merged lock checked in scratch</name>
  <precondition>69-01 is merged into this worktree's HEAD: the `CHANGELOG.md` region between `## [Unreleased]` and `## [0.9.2]` holds three bold-lead bullets, so the trial merge includes the phase's product-tree edit. Otherwise write `## HALT: CHANGELOG edit not merged` and stop.</precondition>
  <reversibility rating="reversible">`git merge-tree --write-tree` writes unreferenced objects only; no ref moves and the scratch directory is deleted.</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PREFLIGHT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-RESEARCH.md § "Pattern 3: D-07's non-committing trial-merge pre-flight" and § "Don't Hand-Roll"
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md D-06, D-07 and D-08
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CONTEXT.md D-03 and D-04 (the ruff divergence between the branch and `main`)
    - pyproject.toml lines 33-55 and uv.lock (the branch's own dev-extra ranges and ruff pin, for comparison)
  </read_first>
  <action>
Create `69-PREFLIGHT-EVIDENCE.md` with the heading `# Phase 69 — REL-12 Update-Step Pre-Flight (D-07, non-committing)`. Open it with one paragraph stating that nothing in this file is committed to the branch except the file itself, and that no merge is performed.

1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line
   - `uv --version`
   - before any commit: `BASE_69_05 = ` and `TRIAL_HEAD = `, both from `git rev-parse HEAD`
   - `START_CANONICAL = ` from `git rev-parse gsd/v0.9.3-toolchain-and-dependency-update-repair`

2. **`## Fetch and divergence`.** Run `git fetch origin`, D-07's own command, and record:
   - `ORIGIN_MAIN_SHA = ` from `git rev-parse origin/main`
   - `git log --oneline "$TRIAL_HEAD".."$ORIGIN_MAIN_SHA"`, key `MAIN_ONLY_COMMITS`, the count
   - `git rev-list --left-right --count "$ORIGIN_MAIN_SHA"...HEAD`
   - `git merge-base HEAD "$ORIGIN_MAIN_SHA"`

3. **`## Trial merge`.** Record `git merge-tree --write-tree "$TRIAL_HEAD" "$ORIGIN_MAIN_SHA"; echo "exit:$?"` verbatim.
   - Key `MERGE_RC` is the exit code. Key `MERGE_TREE` is the first output line, the tree SHA.
   - On a non-zero exit, quote the conflict listing, write `## HALT: trial merge is not clean`, and stop. D-08's update step would not be conflict-free, and that decision belongs to the owner.
   - State that this tree SHA is valid only for these two inputs and must be re-measured immediately before the real merge.

4. **`## Merged lock`.**
   - Run `S="$(mktemp -d)"` and record the path.
   - Run `git archive "$MERGE_TREE" pyproject.toml uv.lock | tar -x -C "$S"`.
   - From the worktree root, record `uv --directory "$S" lock --check; echo "exit:$?"` verbatim. Keys: `LOCK_CHECK_EXIT` (must be 0) and `LOCK_CHECK_UV_VERSION`, from `uv --version`.
   - Record the merged facts, each as a command with its output:
     - `sed -n 7p "$S/pyproject.toml"`, key `MERGED_VERSION_LINE`, which must be `version = "0.9.2"`
     - `grep -nE 'tox-uv|"ruff' "$S/pyproject.toml"`
     - `grep -A1 -xF 'name = "ruff"' "$S/uv.lock"`, key `MERGED_RUFF_LOCK_VERSION`
     - the same pair against this branch's own `uv.lock`, key `BRANCH_RUFF_LOCK_VERSION`
     - the bold-bullet count of the `CHANGELOG.md` region in `git show "$MERGE_TREE:CHANGELOG.md"`, key `MERGED_CHANGELOG_BOLD = 3`
   - `rm -rf "$S"`.

5. **`## Nothing moved`.** Record each of these:
   - `git rev-parse HEAD` still equals `TRIAL_HEAD` (before this plan's commit)
   - `git rev-parse gsd/v0.9.3-toolchain-and-dependency-update-repair` still equals `START_CANONICAL`, unless the orchestrator advanced it, in which case record both
   - `git status --porcelain`, which lists only this evidence file
   - `git merge-base --is-ancestor "$ORIGIN_MAIN_SHA" HEAD; echo "exit:$?"`, which must print `exit:1` (D-06)

   State that `merge-tree` added only unreferenced objects to the object store.

6. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; F="$D/69-PREFLIGHT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_69_05)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; TH="$(k TRIAL_HEAD)" &amp;&amp; [ "$TH" = "$B" ] &amp;&amp; OM="$(k ORIGIN_MAIN_SHA)" &amp;&amp; [ "${#OM}" = 40 ] &amp;&amp; git cat-file -e "$OM^{commit}" &amp;&amp; [ "$(k MERGE_RC)" = 0 ] &amp;&amp; MT="$(git merge-tree --write-tree "$TH" "$OM")" &amp;&amp; [ "$MT" = "$(k MERGE_TREE)" ] &amp;&amp; [ "$(git show "$MT:pyproject.toml" | sed -n 7p)" = 'version = "0.9.2"' ] &amp;&amp; RV="$(git show "$MT:uv.lock" | grep -A1 -xF 'name = "ruff"' | sed -n 's/^version = "\(.*\)"$/\1/p')" &amp;&amp; [ -n "$RV" ] &amp;&amp; [ "$(k MERGED_RUFF_LOCK_VERSION)" = "$RV" ] &amp;&amp; MC="$(git show "$MT:CHANGELOG.md")" &amp;&amp; [ -n "$MC" ] &amp;&amp; [ "$(printf '%s\n' "$MC" | awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' | grep -E '^- \*\*' | wc -l)" = 3 ] &amp;&amp; S="$(mktemp -d)" &amp;&amp; git archive "$MT" pyproject.toml uv.lock | tar -x -C "$S" &amp;&amp; uv --directory "$S" lock --check &amp;&amp; rm -rf "$S" &amp;&amp; [ "$(k LOCK_CHECK_EXIT)" = 0 ] &amp;&amp; [ -z "$(git rev-list --merges "$B"..HEAD)" ] &amp;&amp; { git merge-base --is-ancestor "$OM" HEAD; [ $? = 1 ]; } &amp;&amp; for s in '## Head check and provisioning' '## Fetch and divergence' '## Trial merge' '## Merged lock' '## Nothing moved'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e "$F" -e "$D/69-05-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_69_05` missing, or `TRIAL_HEAD` differing from it
- `ORIGIN_MAIN_SHA` not a commit
- `MERGE_RC` not 0, or the live `merge-tree` exiting non-zero
- the live `merge-tree` printing a tree other than `MERGE_TREE`
- the merged `pyproject.toml:7` not 0.9.2
- the merged ruff version empty or differing from its key
- the merged CHANGELOG lacking the three bullets
- the live `uv lock --check` failing, or `LOCK_CHECK_EXIT` not 0
- a merge commit on the branch since the base
- origin/main absorbed
- another tracked file changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Trial merge` records the verbatim `git merge-tree --write-tree` output with `MERGE_RC = 0` and `MERGE_TREE`, and states that the SHA must be re-measured before the real merge.
    - `## Merged lock` records the scratch path, `LOCK_CHECK_EXIT = 0` with `LOCK_CHECK_UV_VERSION`, `MERGED_VERSION_LINE`, `MERGED_RUFF_LOCK_VERSION`, `BRANCH_RUFF_LOCK_VERSION` and `MERGED_CHANGELOG_BOLD = 3`.
    - `## Nothing moved` shows HEAD and the canonical ref unmoved by the trial, only the evidence file in `git status`, and the `exit:1` D-06 check.
  </acceptance_criteria>
  <done>The REL-12 update merge is measured conflict-free, with a valid merged lock, and the branch is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Merged-tree lint at the merged lock's versions, main's protection and merge-method precedent, and the dependabot PR census</name>
  <precondition>Task 1 is committed with `MERGE_RC = 0`, `LOCK_CHECK_EXIT = 0` and no HALT heading.</precondition>
  <reversibility rating="reversible">A scratch directory outside the repository, and read-only GitHub calls.</reversibility>
  <files>.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PREFLIGHT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PREFLIGHT-EVIDENCE.md (Task 1: `MERGE_TREE`, `MERGED_RUFF_LOCK_VERSION`)
    - tox.ini lines 45-52 (the `lint` environment's two commands, which this task reproduces on the merged tree)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md D-07 (optional lint), D-08 (the six checks, #135/#136 precedent) and D-10
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CONTEXT.md § "Deferred Ideas" (why #139..#142 are untouched)
  </read_first>
  <action>
1. Re-run the head check.

2. **`## Merged-tree lint`.** This is D-07's optional half, exercised under Claude's discretion, because the milestone changed test files that `main` has not yet linted under ruff 0.16.x.
   - Run `S2="$(mktemp -d)"` and `git archive "$MERGE_TREE" | tar -x -C "$S2"`.
   - Run `uv --directory "$S2" sync --locked --extra dev --no-install-project` from the worktree root. This is a hash-verified install from the merged `uv.lock` only, and every package in it entered a lock through a reviewed commit, #138 for ruff.
   - Record `uv --directory "$S2" run --no-sync ruff --version`, key `MERGED_RUFF_RUN_VERSION`, which must equal `MERGED_RUFF_LOCK_VERSION`.
   - Record `uv --directory "$S2" run --no-sync ruff check .; echo "exit:$?"` verbatim, key `MERGED_RUFF_EXIT`.
   - Record `uv --directory "$S2" run --no-sync black --check .; echo "exit:$?"` verbatim, key `MERGED_BLACK_EXIT`.
   - Run `rm -rf "$S2"`.
   - If either exit is non-zero, write a `## FINDING: merged-tree lint` section quoting each finding. Do not fix anything: a fix is a code change outside this phase's scope (D-06), and it would force a second CI dispatch (D-13). Plan 69-06 carries the finding into the handoff for the owner.

3. **`## main protection (D-08)`.** Record `gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '.required_status_checks'` verbatim, with the keys:
   - `PROTECTION_STRICT`, which must be `true`
   - `PROTECTION_CONTEXTS_COUNT`, which must be `6`

   List the six context names exactly as printed. State why the branch update is mandatory: under `strict: true`, the PR cannot merge until the branch is up to date with `main`.

4. **`## Merge-method precedent (D-08)`.** Record `git log --first-parent --format='%h %s' origin/main | grep -E ' Merge pull request #(135|136) '`, key `MERGE_PRECEDENT_HITS = 2`, and `git log --first-parent --format='%h %s' -8 origin/main`. State that prior milestone PRs merged with merge commits, never squash or rebase.

5. **`## Dependabot PRs (D-10)`.** For each of 139, 140, 141 and 142, record `gh pr view <n> --json number,state,baseRefName,headRefName,title,updatedAt`, read-only.
   - Tabulate them in rows starting `| #<n> |`, with state, base, head, title and updatedAt.
   - Key `PR_CENSUS_AT` is the `date -u +%FT%TZ` of the census.
   - State that no merge, close, comment or rebase request was made (D-10), and that dependabot will rebase them once REL-12 changes `uv.lock` on `main`.

6. **`## What the handoff can rely on`.** Summarise in one table, citing each key above:
   - conflict-free merge
   - valid merged lock
   - the merged ruff version, 0.16.x, which is consistent with NIX-01 per 67-CONTEXT D-03, because NIX-01's binding sense is the version `uv.lock` pins
   - the merged-tree lint result
   - strict protection with its six checks
   - the merge-commit method
   - the PR census time

7. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; F="$D/69-PREFLIGHT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_69_05)" &amp;&amp; [ -n "$B" ] &amp;&amp; MT="$(k MERGE_TREE)" &amp;&amp; [ -n "$MT" ] &amp;&amp; S2="$(mktemp -d)" &amp;&amp; git archive "$MT" | tar -x -C "$S2" &amp;&amp; uv --directory "$S2" sync --locked --extra dev --no-install-project -q &amp;&amp; [ "$(uv --directory "$S2" run --no-sync ruff --version | cut -d' ' -f2)" = "$(k MERGED_RUFF_LOCK_VERSION)" ] &amp;&amp; [ "$(k MERGED_RUFF_RUN_VERSION)" = "$(k MERGED_RUFF_LOCK_VERSION)" ] &amp;&amp; RX="$(uv --directory "$S2" run --no-sync ruff check . &gt;/dev/null 2&gt;&amp;1; echo $?)" &amp;&amp; [ "$RX" = "$(k MERGED_RUFF_EXIT)" ] &amp;&amp; BX="$(uv --directory "$S2" run --no-sync black --check . &gt;/dev/null 2&gt;&amp;1; echo $?)" &amp;&amp; [ "$BX" = "$(k MERGED_BLACK_EXIT)" ] &amp;&amp; rm -rf "$S2" &amp;&amp; { [ "$RX" = 0 ] &amp;&amp; [ "$BX" = 0 ] || grep -qxF '## FINDING: merged-tree lint' "$F"; } &amp;&amp; [ "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '.required_status_checks.strict')" = true ] &amp;&amp; [ "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '.required_status_checks.contexts | sort | join("|")')" = 'Build Package|Code Coverage|Lint and Format Check|Test Python 3.12 on ubuntu-latest|Test Python 3.13 on ubuntu-latest|Type Check' ] &amp;&amp; [ "$(k PROTECTION_STRICT)" = true ] &amp;&amp; [ "$(k PROTECTION_CONTEXTS_COUNT)" = 6 ] &amp;&amp; FP="$(git log --first-parent --format='%h %s' origin/main)" &amp;&amp; [ -n "$FP" ] &amp;&amp; [ "$(printf '%s\n' "$FP" | grep -E ' Merge pull request #(135|136) ' | wc -l)" = 2 ] &amp;&amp; [ "$(k MERGE_PRECEDENT_HITS)" = 2 ] &amp;&amp; for n in 139 140 141 142; do grep -qE "^\| *#$n *\|" "$F" || { echo "PR row missing: #$n"; exit 1; }; done &amp;&amp; [ -n "$(k PR_CENSUS_AT)" ] &amp;&amp; for s in '## Merged-tree lint' '## main protection (D-08)' '## Merge-method precedent (D-08)' '## Dependabot PRs (D-10)' '## What the handoff can rely on'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e "$F" -e "$D/69-05-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `PR row missing: #<n>` / `missing section: <name>` printed. Each of these makes it fail:
- `MERGE_TREE` missing
- the locked scratch sync failing
- the running ruff version differing from the merged lock's
- a recorded ruff or black exit differing from the live re-run
- a non-zero lint exit without a FINDING section
- `main` protection not strict, or its contexts not exactly the six names
- the protection keys wrong
- the #135/#136 merge-commit precedent not both found
- a dependabot PR row or the census time missing
- a section missing
- another tracked file changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Merged-tree lint` records a locked scratch sync from the merged `uv.lock`, `MERGED_RUFF_RUN_VERSION` equal to `MERGED_RUFF_LOCK_VERSION`, and the verbatim `ruff check .` and `black --check .` outputs with `MERGED_RUFF_EXIT` and `MERGED_BLACK_EXIT`. A non-zero exit carries a `## FINDING: merged-tree lint` section, and nothing is fixed.
    - `## main protection (D-08)` records `PROTECTION_STRICT = true`, `PROTECTION_CONTEXTS_COUNT = 6` and the six names verbatim.
    - `## Merge-method precedent (D-08)` records `MERGE_PRECEDENT_HITS = 2`.
    - `## Dependabot PRs (D-10)` holds one `| #n |` row each for 139, 140, 141 and 142, with `PR_CENSUS_AT`, and states that no action was taken.
    - `## What the handoff can rely on` summarises every key.
  </acceptance_criteria>
  <done>Every fact the handoff's branch-update step depends on is measured live: merged-tree lint at the merged versions, strict protection with its six checks, the merge-commit method, and the untouched dependabot PRs.</done>
</task>

</tasks>

<flagged_assumptions>
The phase's single spec-less edge item (REL-12, concurrency, **unresolved**) and the prohibition
recall (P1..P6) are accounted in `69-01-PLAN.md`. This plan carries P1, P5 and P6, and it records
the inputs for assumptions (f) and (g) of that edge:
- `ORIGIN_MAIN_SHA` and `MERGE_TREE` hold only for this moment, so the handoff re-runs `merge-tree`
  immediately before the real merge.
- The dependabot PR states hold only at `PR_CENSUS_AT`.

Phase accounting: 1 surfaced = 0 truths + 1 flagged + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trial merge → branch refs | the pre-flight must measure the merge without creating it |
| merged `uv.lock` → scratch environment | packages are installed from a lock that is not yet on this branch |
| worktree → GitHub API (read-only) | protection, PR and history reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-19 | Tampering | the pre-flight creating a merge commit or moving HEAD or the canonical ref, which would absorb `main` against D-06 | high | mitigate | Only `git merge-tree --write-tree` is used; `## Nothing moved` records HEAD and the canonical ref; the verify requires no merge commit since the base and `origin/main` not an ancestor of HEAD. |
| T-69-20 | Tampering | installing packages outside a committed lock in the scratch lint (ASVS V14, L1) | medium | mitigate | The scratch environment is created with `uv sync --locked` from the merged `uv.lock`, which is hash-pinned. Nothing is fetched with an ad-hoc `--from` spec, and ruff 0.16.6 entered that lock via reviewed PR #138 (67-PROOF-EVIDENCE.md). |
| T-69-21 | Repudiation | a stale tree SHA or lock result copied from CONTEXT or RESEARCH | medium | mitigate | A fresh `git fetch origin`, `ORIGIN_MAIN_SHA` recorded, and the verify recomputes `merge-tree` from the recorded inputs and re-runs `uv lock --check` live. |
| T-69-22 | Elevation of Privilege | a census call becoming an action on #139..#142 (D-10) | medium | mitigate | Only `gh pr view` is used; the prohibition names merge, close, comment and rebase request. |
| T-69-SC | Tampering | package installs | low | accept | The worktree sync and the scratch sync both install from committed locks only; no package is added to this branch. |
</threat_model>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` § "Artifacts this phase produces" for the phase-wide list. This plan creates
`69-PREFLIGHT-EVIDENCE.md` and two scratch directories outside the repository, both removed. It
modifies no product-tree file, creates no commit other than its evidence, and moves no ref. No file
named `69-VERIFICATION.md` is produced.
</artifacts_this_phase_produces>

<verification>
- The trial merge of `origin/main` is clean and reproducible from the recorded inputs, and the merged
  lock passes `uv lock --check`.
- The merged tree's lint result at the merged versions is recorded, with a FINDING section if it is
  not clean.
- `main`'s strict protection, its six checks, the merge-commit precedent and the four dependabot PRs
  are recorded live.
- Nothing was merged into, or moved on, the branch.
</verification>

<success_criteria>
D-07's pre-flight is recorded as evidence without committing anything to the branch, and the facts
D-08 and D-10 need are measured, so the handoff's update step is known conflict-free with a valid
lock.
</success_criteria>

<output>
Create `.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-05-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`.
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
- The first section states `ORIGIN_MAIN_SHA`, `MERGE_RC`, `MERGE_TREE`, `LOCK_CHECK_EXIT`,
  `MERGED_RUFF_LOCK_VERSION`, `MERGED_RUFF_EXIT`, `MERGED_BLACK_EXIT`, `PROTECTION_STRICT` and the
  PR census. If a FINDING or HALT section was written, it says so first.
</output>
