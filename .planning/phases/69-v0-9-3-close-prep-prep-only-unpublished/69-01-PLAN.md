---
phase: 69-v0-9-3-close-prep-prep-only-unpublished
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CHANGELOG-EVIDENCE.md
autonomous: true
requirements: [REL-12]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-01: the existing `## [Unreleased]` heading of CHANGELOG.md carries exactly three bold-lead bullets under a new `### Changed` subsection. One covers the return to `tox-uv` (TOX-01..TOX-04), one the Dependabot `pip` to `uv` ecosystem switch (DEP-01..DEP-05), and one the NixOS FHS development shell with its contributor notes (NIX-01..NIX-08, DOC-19..DOC-21). The region between `## [Unreleased]` and `## [0.9.2]` cites all 20 IDs, and each bullet bold span ends in its parenthesised ID list.'
    - 'D-02: each of the three bullets contains the phrase "no effect on installing or using typsphinx", and the string 0.9.3 occurs nowhere in CHANGELOG.md.'
    - 'D-03 / SC#2: the `### Changed` block sits between `## [Unreleased]` and `### Planned for Future Releases`, and the Planned block is byte-identical to the plan base (equal SHA-256 of the extracted block). The release-heading count and link-reference-line count equal the plan base, the final line is still the `[Unreleased]` compare link against `v0.9.2...HEAD`, no `## [0.9.3]` heading or `[0.9.3]:` link exists, and `scripts/extract_changelog_section.py` is not run.'
    - 'D-04: the only `###` headings in the `## [Unreleased]` region are `### Changed` followed by `### Planned for Future Releases` (no `### Verified`), and no non-blank line sits between `## [Unreleased]` and `### Changed`.'
    - 'D-05: the ruff 0.15.20 to 0.16.6 bump has no bullet of its own; the region holds exactly three bold-lead bullets.'
    - 'The edit is pure addition: `git diff BASE_69_01 -- CHANGELOG.md` contains no removed source line.'
    - 'SC#3 docs half, both sides from a clean build: DOCS_HTML_WARN_BASE and DOCS_PDF_WARN_BASE are measured on the pre-edit tree with `rm -rf docs/_build` immediately before each build, and the post-edit clean builds report the same two counts; docs/_build/pdf/typsphinx.pdf begins with %PDF-.'
    - 'Every claim in the three bullets is traced in an Accuracy basis section to a named Phase 64-68 evidence file. No bullet claims an effect on users, CI coverage of flake.nix, verified darwin support, or an observed grouped Dependabot update.'
    - 'REL-12 is cited for coverage only. This plan does not touch its checkbox, and its SUMMARY declares requirements-completed: [].'
  artifacts:
    - path: CHANGELOG.md
      provides: 'three ### Changed bullets under the existing ## [Unreleased] heading'
      contains: '### Changed'
    - path: .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CHANGELOG-EVIDENCE.md
      provides: 'plan base, pre-edit measurements, clean-build docs baselines, the bullets and their accuracy basis, pure-addition proof, fence assertions, post-edit docs counts'
      contains: 'DOCS_HTML_WARN_BASE = '
  key_links:
    - from: CHANGELOG.md
      to: docs/source/changelog.rst
      via: 'the include directive with :parser: myst_parser.sphinx_ makes every bullet Read the Docs latest text once REL-12 merges; a malformed bullet shows as a new docs warning'
      pattern: 'myst_parser.sphinx_'
    - from: '69-CHANGELOG-EVIDENCE.md DOCS_HTML_WARN_BASE and DOCS_PDF_WARN_BASE'
      to: '69-03 final-tree docs builds'
      via: 'KEY = value lines read with sed by plan 69-03'
      pattern: 'DOCS_PDF_WARN_BASE = '
  prohibitions:
    - statement: 'MUST NOT take any outward or irreversible action: no tag, PyPI upload, GitHub Release, pull request, merge, release.yml or update-pin.yml run, PR comment, or push. This plan pushes nothing; the phase single push belongs to 69-04.'
      requirement_id: REL-12
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT publish a reference to a version the project has not released: no ## [0.9.3] heading, no [0.9.3] link line, no 0.9.3 or v0.9.3 in the prose. CHANGELOG.md is included wholesale into the published docs.'
      requirement_id: REL-12
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT overstate what the milestone did: no claim of a user-facing effect, of CI coverage for flake.nix, of verified darwin support, of an observed grouped Dependabot update under uv, or of a ranking such as first that the evidence does not record.'
      requirement_id: REL-12
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT change any file under typsphinx/, tests/, docs/, pyproject.toml or uv.lock, and MUST NOT merge origin/main into the branch (D-06, constraint 13).'
      requirement_id: REL-12
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Author this milestone's toolchain work as three user-readable bullets under the existing
`## [Unreleased]` heading of `CHANGELOG.md` (D-01..D-05), and prove with clean documentation builds,
taken before and after the edit, that the new prose renders through MyST without adding a warning.

Purpose: this is the phase's only product-tree change, and the only thing a reader of the published
changelog will see from v0.9.3. Nothing is released (ROADMAP constraint 2), so the bullets stay under
`## [Unreleased]` until a later release-prep phase promotes them (D-11). The block holds zero real
change bullets today, so every bullet is authored from scratch.

Output: the edited `## [Unreleased]` block in `CHANGELOG.md`, and `69-CHANGELOG-EVIDENCE.md`
recording the plan base, the pre-edit measurements, both clean-build docs baselines, the accuracy
basis, the pure-addition proof, the fence assertions and the post-edit docs counts.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-RESEARCH.md
@.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution").
Before running any command in this plan, provision this worktree's own environment with both extras:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`

The `docs` extra carries `myst-parser`, `furo`, `sphinx-intl` and `sphinx-autodoc-typehints`; a
fresh worktree's `--extra dev` alone lacks them. Prefix every later command with `uv run`.

**Evidence key lines.** Every `KEY = value` line in the evidence file carries the bare value only.
Put any explanation on the next line, never after the value, because the verify commands read
values with `sed -n "s/^KEY = //p"` and compare them exactly.
</worktree_provisioning>

<!-- planner-discipline-allow: 0\.9\.3 -->
<!-- planner-discipline-allow: 0.9.3 -->
<!-- planner-discipline-allow: patchelf|ln -sf -->
<!-- planner-region-allow: 0\.9\.3 -->
<!-- planner-region-allow: patchelf|ln -sf -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
The version string is named in actions because D-02 forbids it and the executor must know what to
avoid. The two manual-step words are named because Phase 68 confined them to `CLAUDE.md`. `## HALT`
is a heading written only on a failure path. The gates require the absence of all three.

## Planning-time census (2026-09-13, main checkout at `b2fea682`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `## [Unreleased]` region | heading at `CHANGELOG.md:8`, `### Planned for Future Releases` at `:10` with five items, `## [0.9.2]` at `:17`; zero bold-lead bullets | `grep -n`, awk region |
| Structural counts | 23 `^## \[` headings, 23 link-reference lines, 1297 lines; the last line is `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.2...HEAD` | `grep -cE`, `tail -n 1` |
| Strings the bullets must not add | `0.9.3`: 0 hits; the two manual-step words: 0 hits in CHANGELOG.md, and `git grep -lE` over them outside `.planning/` lists `CLAUDE.md` only | `grep -c`, `git grep -l` |
| Only one `## [Unreleased]` heading today | the docstring of `tests/test_changelog_extraction.py` describes a second one in the tail, but the live file has one; the region extractor below exits at `## [0.9.2]` either way | `grep -n` |
| Docs environments | `tox.ini` `docs-html`: `changedir = docs`, `sphinx-build -b html source _build/html`; `docs-pdf`: `sphinx-build -b typstpdf source _build/pdf`, output `docs/_build/pdf/typsphinx.pdf` (`docs/source/conf.py:72-74`) | `tox.ini:67-81` |
| Prior warning counts | 3 (docs-html) and 5 (docs-pdf) at the Phase 61 and 63 closes; a cross-check only, never this plan's baseline | 61/63 evidence |
| House style | `## [0.9.2]`: bold lead sentence with IDs in trailing parentheses inside the bold span, continuation lines indented two spaces, a blank line between bullets | `CHANGELOG.md:17-60` |

The region extractor used throughout, printing the lines between `## [Unreleased]` and
`## [0.9.2]`: `awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md`.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: pre-edit measurements and clean-build docs baselines, then the TOX bullet carried from its evidence through MyST into a rendered page</name>
  <reversibility rating="reversible">A markdown addition under an existing heading; a later release-prep phase promotes it by editing a heading (D-11).</reversibility>
  <files>CHANGELOG.md, .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - CHANGELOG.md lines 1-60 (the file being modified: the `## [Unreleased]` block and the `## [0.9.2]` bullets whose house style this bullet matches)
    - docs/source/changelog.rst lines 1-3 (the include that publishes this edit)
    - tox.ini lines 67-81 (the two docs environments and their output directories)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md (`<decisions>` D-01 to D-05; `<specifics>` first bullet: the draft TOX bullet shape)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-PATTERNS.md § "`CHANGELOG.md` (documentation, transform)"
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-REVERT-EVIDENCE.md and 65-CI-EVIDENCE.md (the facts the TOX bullet may state)
    - .planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-CHANGELOG-EVIDENCE.md (the evidence-file structure to mirror)
    - CLAUDE.md § "Conventions and gotchas", toolchain-pin bullet (the sync points the bullet must not be mistaken for)
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record each command with its output:
   - `date -u +%FT%TZ`
   - `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`
   - `test -f .git; echo "exit:$?"`
   - `command -v uv`, then `grep -c typsphinx-fhs-run "$(command -v uv)"`
   - the provisioning line `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` (tail of its output)
   - before any commit: `BASE_69_01 = ` followed by the output of `git rev-parse HEAD`
   - `PYVENV_HOME_69_01 = ` and `PYVENV_VERSION_69_01 = `, taken from `.venv/pyvenv.cfg`

2. **`## Pre-edit measurements`.** Record each command with its output, then its key line:
   - `grep -cE '^## \[' CHANGELOG.md`, key `HEADINGS_BEFORE`
   - `grep -cE '^\[[^]]+\]: https' CHANGELOG.md`, key `LINKREFS_BEFORE`
   - the region extractor piped to `grep -cE '^- \*\*'`, key `UNRELEASED_BOLD_BEFORE`
   - `grep -c '0\.9\.3' CHANGELOG.md`, key `V093_BEFORE`
   - `awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' CHANGELOG.md | sha256sum`, key `PLANNED_SHA_BEFORE`, the digest field only
   - the verbatim `tail -n 1 CHANGELOG.md`, in a fenced block rather than a key line

   Add a short `## What this file is NOT` section. It states that `scripts/extract_changelog_section.py` is not run in this phase: no versioned section exists for it to extract (SC#2, D-03), and a later release-prep phase runs it when it promotes these bullets (D-11).

3. **`## Docs baseline — pre-edit, clean builds`.** Take both baselines before touching `CHANGELOG.md`.
   - Run `rm -rf docs/_build`, then `uv run tox -e docs-html`, in the foreground with the Bash `timeout` set to 600000 ms and no `run_in_background`.
   - Paste the log's final lines verbatim, including the literal `build succeeded` line, and every line containing `WARNING` in a fenced block.
   - Write `DOCS_HTML_WARN_BASE = ` with the number from `build succeeded, N warnings.`. If Sphinx prints `build succeeded.` with no count, the value is 0.
   - Repeat for `docs-pdf`, again with `rm -rf docs/_build` immediately before it, and write `DOCS_PDF_WARN_BASE = `. Record `head -c 5 docs/_build/pdf/typsphinx.pdf`, which must print `%PDF-`.

   An incremental rebuild under-reports warnings, which is why each counted build starts from an empty `docs/_build`.

4. **`## The TOX bullet (tracer slice)`.** Edit `CHANGELOG.md`:
   - Insert a `### Changed` heading immediately below `## [Unreleased]`, with one blank line on each side, and above the existing `### Planned for Future Releases`, which stays exactly as it is (D-03).
   - Write no lead paragraph and no `### Verified` subsection (D-04).
   - Under the heading, write the first of the three bullets (D-01 order: TOX, DEP, NIX), in the `## [0.9.2]` house style: `- **` + a plain-language lead sentence + ` (TOX-01, TOX-02, TOX-03, TOX-04).**`, then one to three sentences of prose. Continuation lines are indented two spaces and wrap near 100 columns.
   - Content, bounded by `65-REVERT-EVIDENCE.md` and `65-CI-EVIDENCE.md`: the `dev` extra and `tox.ini`'s `requires` name `tox-uv` again in place of `tox-uv-bare`, with `uv.lock` regenerated in the same change and CI green on Linux, Windows and macOS.
   - Write for a reader of the published changelog, not in internal shorthand (D-02). No run ids or phase numbers, and never the version string `0.9.3`.
   - Include the exact phrase `no effect on installing or using typsphinx` in a sentence of its own.
   - Name `tox-uv-bare` only as the previous state. This bullet is a dated record, not one of the toolchain-pin sync points `CLAUDE.md` lists, so no other file changes with it.
   - Add a `## Accuracy basis` section. Map each factual claim in the bullet to the evidence file and section that supports it.

5. **`## Docs render — tracer slice`.** Run `rm -rf docs/_build`, then `uv run tox -e docs-html` in the foreground, and paste the verbatim `build succeeded` line and all `WARNING` lines. Write `DOCS_HTML_WARN_TRACER = `.
   - It must equal `DOCS_HTML_WARN_BASE`, and the set of `WARNING` lines must match the baseline's.
   - If a new warning names the changelog page, the MyST in the bullet is malformed. Fix the bullet and re-run from the `rm -rf`.
   - If a new warning names any other page, write `## HALT: docs-html warning drift not caused by CHANGELOG.md` with the warning quoted, and stop. Never move the baseline.

6. Commit `CHANGELOG.md` and the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; F="$D/69-CHANGELOG-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_69_01)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; R="$(awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^### ' | tr '\n' '|')" = '### Changed|### Planned for Future Releases|' ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -cE '^- \*\*')" -ge 1 ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -oE 'TOX-0[1-4]' | sort -u | wc -l)" = 4 ] &amp;&amp; [ "$(git diff "$B" -- CHANGELOG.md | grep -cE '^-[^-]')" = 0 ] &amp;&amp; P1="$(awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' CHANGELOG.md | sha256sum)" &amp;&amp; P0="$(git show "$B:CHANGELOG.md" | awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' | sha256sum)" &amp;&amp; [ "$P1" = "$P0" ] &amp;&amp; [ "$(tail -n 1 CHANGELOG.md)" = "$(git show "$B:CHANGELOG.md" | tail -n 1)" ] &amp;&amp; [ "$(grep -c '0\.9\.3' CHANGELOG.md)" = 0 ] &amp;&amp; HB="$(k DOCS_HTML_WARN_BASE)" &amp;&amp; PW="$(k DOCS_PDF_WARN_BASE)" &amp;&amp; [ -n "$HB" ] &amp;&amp; [ -n "$PW" ] &amp;&amp; [ "$(k DOCS_HTML_WARN_TRACER)" = "$HB" ] &amp;&amp; dw() { rm -rf docs/_build &amp;&amp; O="$(mktemp)" &amp;&amp; uv run tox -e "$1" &gt;"$O" 2&gt;&amp;1 &amp;&amp; L="$(grep -oE 'build succeeded(, [0-9]+ warnings?)?\.' "$O" | tail -n 1)" &amp;&amp; [ -n "$L" ] &amp;&amp; N="$(printf '%s' "$L" | tr -cd '0-9')" &amp;&amp; printf '%s\n' "${N:-0}"; } &amp;&amp; [ "$(dw docs-html)" = "$HB" ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ docs/ pyproject.toml uv.lock)" ] &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e CHANGELOG.md -e "$F" -e "$D/69-01-SUMMARY.md" &amp;&amp; for s in '## Head check and provisioning' '## Pre-edit measurements' '## What this file is NOT' '## Docs baseline — pre-edit, clean builds' '## The TOX bullet (tracer slice)' '## Accuracy basis' '## Docs render — tracer slice'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the uv shim not on PATH
- `BASE_69_01` missing or not an ancestor of HEAD
- the region's `###` headings other than `### Changed` then `### Planned for Future Releases`
- no bold bullet in the region, or the four TOX IDs not all cited
- a removed line in the CHANGELOG.md diff from the base
- the Planned block digest or the final line differing from the base
- any `0.9.3` in CHANGELOG.md
- either baseline key empty, or the tracer count differing from `DOCS_HTML_WARN_BASE`
- the live clean docs-html build failing, printing no `build succeeded` line, or reporting a different count
- a change under typsphinx/, tests/, docs/, pyproject.toml or uv.lock
- another tracked file changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `69-CHANGELOG-EVIDENCE.md` holds `BASE_69_01` (40 hex), both `PYVENV_*` keys, and the five pre-edit keys: `HEADINGS_BEFORE`, `LINKREFS_BEFORE`, `UNRELEASED_BOLD_BEFORE = 0`, `V093_BEFORE = 0` and `PLANNED_SHA_BEFORE`, plus the verbatim final line in a fenced block.
    - `## What this file is NOT` states that `scripts/extract_changelog_section.py` is not run, and why.
    - `DOCS_HTML_WARN_BASE` and `DOCS_PDF_WARN_BASE` each come from a build preceded by `rm -rf docs/_build` on the pre-edit tree, with the verbatim `build succeeded` line and the `WARNING` lines quoted; `head -c 5 docs/_build/pdf/typsphinx.pdf` printed `%PDF-`.
    - The region's `###` headings are exactly `### Changed` then `### Planned for Future Releases`. The TOX bullet cites TOX-01..TOX-04 inside its bold span and contains `no effect on installing or using typsphinx`.
    - `## Accuracy basis` maps every claim in the TOX bullet to a named evidence file and section.
    - `DOCS_HTML_WARN_TRACER` equals `DOCS_HTML_WARN_BASE`, from a clean post-edit build.
    - `git diff BASE_69_01 -- CHANGELOG.md` has no removed line, and no file outside `CHANGELOG.md` and this evidence file changed.
  </acceptance_criteria>
  <done>Both docs baselines are taken from clean builds of the pre-edit tree, and the first bullet renders through MyST into the docs page with the warning count unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: The DEP and NIX bullets, the pure-addition proof, the fence assertions, and both docs environments rebuilt clean against the baseline</name>
  <reversibility rating="reversible">Markdown additions under an existing heading; no version literal, tag or published artifact moves.</reversibility>
  <files>CHANGELOG.md, .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - CHANGELOG.md (the state after Task 1; the TOX bullet stays as written)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CHANGELOG-EVIDENCE.md (Task 1's keys and sections)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md and 66-MAIN-PR-EVIDENCE.md (the ecosystem switch, grouping and lock-revision facts)
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-PROOF-EVIDENCE.md and 67-CLOSURE-EVIDENCE.md (the DEP-02 proof on #138, the grouped-update coverage gap `UV_GROUP_PR_COUNT = 0`, DEP-05)
    - .planning/phases/67-proof-on-a-real-dependabot-pr-then-disposal-of-123-and-128/67-CONTEXT.md D-03 and D-04 (the ruff 0.16.6 bump lives on `main`, not on this branch)
    - .planning/phases/64-fhs-wrapper-and-command-shims-in-flake-nix/64-VERIFICATION.md, 64-NIX05-WORKTREE-EVIDENCE.md and 64-NIX08-ENV-EVIDENCE.md (NIX-01..NIX-08)
    - .planning/phases/68-documentation-follow-through-claude-md-tox-ini-flake-nix/68-CLOSURE-EVIDENCE.md (DOC-19..DOC-21; § SC#1 literal reading, clause (a), where the two manual-step words are confined to `CLAUDE.md`)
    - CLAUDE.md § "NixOS development shell" and flake.nix header comments (the landed mechanism the NIX bullet describes)
    - .planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-CONTEXT.md D-01 to D-05 and § "Claude's Discretion"
  </read_first>
  <action>
1. Re-run the head check and `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`.

2. **The DEP bullet**, second in the `### Changed` block (D-01).
   - Lead: Dependabot's Python version updates now use the `uv` ecosystem instead of `pip`, with `(DEP-01, DEP-02, DEP-03, DEP-04, DEP-05)` inside the bold span.
   - Prose, bounded by the Phase 66/67 evidence:
     - each dependency pull request now updates `pyproject.toml` and `uv.lock` in the same commit
     - so CI's `uv sync --locked` step succeeds and the test, lint and type jobs actually run on it
     - before the switch, every such pull request stopped at that step before any test ran
   - D-05 discretion: you may name the `ruff` update pull request as the one on which this was observed. Do not call it the first, and do not give the ruff bump its own bullet.
   - Do not claim that the `sphinx-typst-stack` group has been seen opening a grouped pull request under `uv`: 67-05 recorded that coverage gap (`UV_GROUP_PR_COUNT = 0`). You may say the grouping, labels and pull-request limit carried over unchanged.
   - Include `no effect on installing or using typsphinx` (D-02).

3. **The NIX bullet**, third (D-01).
   - Lead: a NixOS development shell, with `(NIX-01, NIX-02, NIX-03, NIX-04, NIX-05, NIX-06, NIX-07, NIX-08, DOC-19, DOC-20, DOC-21)` inside the bold span.
   - Prose, bounded by the Phase 64/68 evidence:
     - `flake.nix` puts command shims for `uv`, `tox`, `ruff`, `black`, `mypy`, `pytest` and `sphinx-build` on PATH
     - they run the checkout's own `.venv` tools inside an FHS sandbox, so the versions `uv.lock` pins run on NixOS without a manual step
     - this applies only to contributors who enter that shell on NixOS; CI and every other platform are unchanged
     - the darwin systems evaluate but are unverified
     - the contributor notes in `CLAUDE.md`, the `tox.ini` comment and the `flake.nix` header now describe it
   - Do not claim that CI covers `flake.nix` (ROADMAP constraint 8).
   - Do not use either of the two manual-step words that Phase 68 confined to `CLAUDE.md`'s NixOS subsection (`patchelf`, `ln -sf`). A `git grep -lE` over them outside `.planning/` must still list `CLAUDE.md` alone.
   - Include `no effect on installing or using typsphinx` (D-02).

4. Extend `## Accuracy basis` with one row per claim in the DEP and NIX bullets, each naming its evidence file and section. Add `## Discretion exercised`, one line each:
   - whether the ruff update is named, and why (D-05)
   - that `### Verified` is deliberately absent, because a later release-prep phase writes it against its own whole diff (D-04)
   - the bullet order

5. **`## Pure-addition proof`.** Paste the verbatim `git diff "$BASE_69_01" -- CHANGELOG.md`, then record the count of removed source lines in it, meaning lines that start with a single minus sign rather than the `---` file header. The count must be 0.

6. **`## Fence assertions`.** Record each as a command, its output, and a key line:
   - `HEADINGS_AFTER` and `LINKREFS_AFTER`, each equal to its `_BEFORE`
   - `UNRELEASED_BOLD_AFTER = 3`
   - `UNRELEASED_IDS_AFTER = 20`, from the region piped to `grep -oE '(TOX-0[1-4]|DEP-0[1-5]|NIX-0[1-8]|DOC-(19|20|21))' | sort -u | wc -l`
   - `NO_EFFECT_PHRASES = 3`, counted over the region joined into one line
   - `V093_AFTER = 0`
   - `PLANNED_SHA_AFTER`, equal to `PLANNED_SHA_BEFORE`
   - `MANUAL_STEP_WORDS = 0`, from `grep -cE 'patchelf|ln -sf' CHANGELOG.md`

   Also record:
   - the verbatim final line, identical to the base's
   - `sed -n 7p pyproject.toml`, which must be `version = "0.9.2"`
   - the region's `###` heading list
   - the empty lead-paragraph check `awk '/^### Changed$/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md | grep -c .`, which must be 0
   - `git status --porcelain typsphinx/ tests/ docs/ pyproject.toml uv.lock`, which must be empty

7. **`## Docs render — post-edit, clean builds`.** Build each environment in the foreground (Bash `timeout` 600000 ms, no `run_in_background`), running `rm -rf docs/_build` immediately before each:
   - `uv run tox -e docs-html`, key `DOCS_HTML_WARN_POST`
   - `uv run tox -e docs-pdf`, key `DOCS_PDF_WARN_POST`, then `head -c 5 docs/_build/pdf/typsphinx.pdf`, which must print `%PDF-`

   Quote each `build succeeded` line and its `WARNING` lines. Both counts must equal their `_BASE`, with the same set of `WARNING` lines. The halt rule of Task 1 step 5 applies.

8. Commit `CHANGELOG.md` and the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished &amp;&amp; F="$D/69-CHANGELOG-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_69_01)" &amp;&amp; [ -n "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; R="$(awk '/^## \[0\.9\.2\]/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md)" &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^### ' | tr '\n' '|')" = '### Changed|### Planned for Future Releases|' ] &amp;&amp; [ "$(awk '/^### Changed$/{exit} f; /^## \[Unreleased\]/{f=1}' CHANGELOG.md | grep -c .)" = 0 ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -E '^- \*\*' | wc -l)" = 3 ] &amp;&amp; [ "$(printf '%s\n' "$R" | grep -oE '(TOX-0[1-4]|DEP-0[1-5]|NIX-0[1-8]|DOC-(19|20|21))' | sort -u | wc -l)" = 20 ] &amp;&amp; J="$(printf '%s\n' "$R" | tr '\n' ' ' | tr -s ' ')" &amp;&amp; [ "$(printf '%s\n' "$J" | grep -oi 'no effect on installing or using typsphinx' | wc -l)" = 3 ] &amp;&amp; [ "$(printf '%s\n' "$J" | grep -oE '\((TOX|DEP|NIX|DOC)-[0-9]{2}(, (TOX|DEP|NIX|DOC)-[0-9]{2})*\)\.?\*\*' | wc -l)" = 3 ] &amp;&amp; BC="$(git show "$B:CHANGELOG.md")" &amp;&amp; [ -n "$BC" ] &amp;&amp; [ "$(grep -E '^## \[' CHANGELOG.md | wc -l)" = "$(printf '%s\n' "$BC" | grep -E '^## \[' | wc -l)" ] &amp;&amp; [ "$(grep -E '^\[[^]]+\]: https' CHANGELOG.md | wc -l)" = "$(printf '%s\n' "$BC" | grep -E '^\[[^]]+\]: https' | wc -l)" ] &amp;&amp; [ "$(tail -n 1 CHANGELOG.md)" = "$(printf '%s\n' "$BC" | tail -n 1)" ] &amp;&amp; [ "$(grep -F 'compare/v0.9.2...HEAD' CHANGELOG.md | wc -l)" = 1 ] &amp;&amp; [ "$(grep -c '0\.9\.3' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(grep -cE 'patchelf|ln -sf' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(git diff "$B" -- CHANGELOG.md | grep -cE '^-[^-]')" = 0 ] &amp;&amp; P1="$(awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' CHANGELOG.md | sha256sum)" &amp;&amp; P0="$(git show "$B:CHANGELOG.md" | awk '/^### Planned for Future Releases$/{f=1} /^## \[0\.9\.2\]/{exit} f' | sha256sum)" &amp;&amp; [ "$P1" = "$P0" ] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; HB="$(k DOCS_HTML_WARN_BASE)" &amp;&amp; PW="$(k DOCS_PDF_WARN_BASE)" &amp;&amp; [ -n "$HB" ] &amp;&amp; [ -n "$PW" ] &amp;&amp; [ "$(k DOCS_HTML_WARN_POST)" = "$HB" ] &amp;&amp; [ "$(k DOCS_PDF_WARN_POST)" = "$PW" ] &amp;&amp; dw() { rm -rf docs/_build &amp;&amp; O="$(mktemp)" &amp;&amp; uv run tox -e "$1" &gt;"$O" 2&gt;&amp;1 &amp;&amp; L="$(grep -oE 'build succeeded(, [0-9]+ warnings?)?\.' "$O" | tail -n 1)" &amp;&amp; [ -n "$L" ] &amp;&amp; N="$(printf '%s' "$L" | tr -cd '0-9')" &amp;&amp; printf '%s\n' "${N:-0}"; } &amp;&amp; [ "$(dw docs-html)" = "$HB" ] &amp;&amp; [ "$(dw docs-pdf)" = "$PW" ] &amp;&amp; [ "$(head -c 5 docs/_build/pdf/typsphinx.pdf)" = '%PDF-' ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ tests/ docs/ pyproject.toml uv.lock)" ] &amp;&amp; ! git diff --name-only "$B" | grep -vxF -e CHANGELOG.md -e "$F" -e "$D/69-01-SUMMARY.md" &amp;&amp; for s in '## The DEP and NIX bullets' '## Accuracy basis' '## Discretion exercised' '## Pure-addition proof' '## Fence assertions' '## Docs render — post-edit, clean builds'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- the heading order in the region wrong, or a non-blank line between `## [Unreleased]` and `### Changed`
- a bold-bullet count other than 3
- fewer or more than 20 distinct IDs
- the no-effect phrase not present exactly three times
- fewer than three bold spans ending in a parenthesised ID list
- the heading count, link-reference count or final line differing from the base
- the `v0.9.2...HEAD` compare link not present exactly once
- any `0.9.3` or either manual-step word in CHANGELOG.md
- a removed line in the diff from the base
- the Planned block digest changed
- `pyproject.toml:7` not the 0.9.2 assignment
- a post-edit key differing from its baseline
- a live clean docs-html or docs-pdf build failing, printing no `build succeeded` line, or reporting a count different from the baseline
- the PDF not starting with `%PDF-`
- a change under typsphinx/, tests/, docs/, pyproject.toml or uv.lock
- another tracked file changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - The region between `## [Unreleased]` and `## [0.9.2]` holds exactly three bold-lead bullets, TOX, DEP and NIX in that order, citing all 20 IDs. Each bold span ends in its ID list, and each bullet contains `no effect on installing or using typsphinx` (D-01, D-02).
    - The DEP bullet does not claim an observed grouped update and does not call the ruff update first; the NIX bullet does not claim CI coverage of `flake.nix` or verified darwin support, and contains neither manual-step word.
    - `## Accuracy basis` covers every claim of all three bullets, and `## Discretion exercised` records the D-05 choice, the absent `### Verified` (D-04) and the order.
    - `## Pure-addition proof` holds the verbatim diff with a removed-line count of 0.
    - `## Fence assertions` records every key listed in action step 6 with its command. The heading and link-reference counts equal the base, `V093_AFTER = 0`, `PLANNED_SHA_AFTER` equals `PLANNED_SHA_BEFORE`, and `pyproject.toml:7` still reads `version = "0.9.2"`.
    - `DOCS_HTML_WARN_POST` and `DOCS_PDF_WARN_POST` equal their clean pre-edit baselines, with the PDF starting `%PDF-`.
    - `scripts/extract_changelog_section.py` was not run, and only `CHANGELOG.md` and this evidence file changed.
  </acceptance_criteria>
  <done>The milestone's three tracks are described under the existing `## [Unreleased]` heading as pure addition. The tail block, the Planned block and every version literal are unmoved, and both docs environments rebuilt clean match the clean pre-edit baseline.</done>
</task>

</tasks>

<flagged_assumptions>
## Spec-less edge-coverage accounting (1 probe-surfaced item, 0 silent drops)

This phase has no SPEC.md. The deterministic fallback probe ran over REL-12's literal text and
returned one item, which it left **unresolved**:

| Requirement | Category | Status | Verification | Probe | Disposition |
|---|---|---|---|---|---|
| REL-12 | concurrency | **unresolved** | null | "If interrupted or run in parallel, what is guaranteed?" | **flagged assumption, surfaced here; not authored into `truths`, not dismissed** |

**What the plan set guarantees by construction:**

- (a) Wave-1 plans 69-01 and 69-02, and wave-2 plans 69-03, 69-04 and 69-05, run in parallel
  isolated worktrees with disjoint `files_modified`. No plan reads a sibling's evidence from the same
  wave: 69-03 reads 69-01's baselines one wave later, and 69-06 reads everything one wave after
  that.
- (b) Only 69-04 writes to `origin`. It pushes one named ref as a fast-forward of the origin head it
  re-measured just before the push, and halts rather than forcing if origin moved.
- (c) An interrupted CI wait resumes on the recorded `RUN_ID`. 69-04 Task 2's precondition requires
  that id, and a second dispatch is forbidden.
- (d) No plan writes `.planning/REQUIREMENTS.md`. The SHA-256 fence therefore detects any writer,
  including `phase.complete`-family tooling that runs after every plan, and a detected flip is
  reverted rather than committed.

**What is assumed, not guaranteed:**

- (e) The local canonical ref does not move between 69-04's census and its push. The orchestrator
  merges wave-2 worktrees only after every wave-2 executor has returned. If it did move, the pushed
  SHA is still recorded as `PUSHED_SHA`, and the run is still bound to it by `headSha`. The
  product-tree equality check, `git diff --name-only PUSHED_SHA HEAD` outside `.planning/`, then
  shows whether the dispatched tree is this phase's.
- (f) `origin/main` does not move between 69-05's pre-flight and `/gsd-complete-milestone`. The
  handoff therefore re-runs `git merge-tree --write-tree` immediately before the operator merges.
- (g) Dependabot does not act on #139..#142 between 69-05's census and the milestone close. The
  handoff records each pull request's state with its measurement time, not as a standing fact.

**Accounting:** 1 surfaced = 0 authored into `must_haves.truths` + 1 flagged assumption + 0
dismissed + 0 dropped.

## Prohibition recall accounting (descriptor-less, never auto-dismissed)

Stage 1 over-produced against REL-12's text. The candidates were:

- a tag, a PyPI upload or a GitHub Release
- a version bump
- the pull request opened or merged inside this phase
- `release.yml` or `update-pin.yml` started
- the dependabot pull requests touched
- `main` absorbed into the branch
- REL-12 recorded as satisfied
- `0.9.3` named or linked
- the bullets overstating the work
- stale or inherited evidence
- a force-push or a decoy push
- a `typsphinx/` change
- malformed markdown
- flaky tests

Stage 2 dropped two as routine engineering: malformed markdown, which the docs gates cover, and
flaky tests. No candidate was canon security or compliance, so no canon-referral breadcrumb
applies. Six bespoke prohibitions survive. Each is authored with no `check_*` descriptor, so each
disposes flagged-unverified downstream.

- **P1 (safety):** no irreversible or outward action beyond D-13's push and one `ci.yml` dispatch.
  In plans 69-01, 69-02, 69-04, 69-05 and 69-06.
- **P2 (honesty):** REL-12 is never recorded as satisfied. In plans 69-02 and 69-06.
- **P3 (transparency):** no reference to a version the project has not released. In plans 69-01
  and 69-06.
- **P4 (accuracy):** the bullets do not overstate what the milestone did. In plan 69-01.
- **P5 (evidence integrity):** no inherited, paraphrased or fabricated evidence. In plans 69-02,
  69-03, 69-04, 69-05 and 69-06.
- **P6 (scope):** no `origin/main` absorption and no change under `typsphinx/`, `pyproject.toml`,
  `uv.lock` or `.github/` (D-06, constraint 13). In plans 69-01, 69-03, 69-04 and 69-05.

**Accounting:** 6 kept = 6 authored into `must_haves.prohibitions` + 0 dismissed + 0 dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` → `docs/source/changelog.rst` → Read the Docs `latest` | authored prose becomes a public page once REL-12 merges |
| authored prose → the project's factual record | the bullets are the account of what v0.9.3 changed; an unsupported claim misrepresents it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-01 | Tampering | malformed MyST in a bullet publishing broken markup (ASVS V5, L1, marginal: the input is prose this project authors) | medium | mitigate | Both docs environments are built from an empty `docs/_build` before and after the edit, and the counts and `WARNING` sets must be equal. The tracer proves the render chain on one bullet first, and a drift from another page halts instead of moving the baseline. |
| T-69-02 | Tampering | a `## [0.9.3]` heading, a `[0.9.3]:` link or version prose publishing a reference to a release that does not exist (CHANGELOG.md is excluded from the lychee scan) | high | mitigate | Whole-file `0.9.3` count 0; heading and link-reference counts equal to the base; the final line byte-identical; pure addition proven by a zero removed-line count. |
| T-69-03 | Repudiation | a recalled or incremental-build warning count recorded as the baseline | medium | mitigate | `rm -rf docs/_build` precedes every counted build. The verify re-runs clean builds live and compares them to the recorded baseline instead of trusting the recorded post-edit numbers. |
| T-69-04 | Elevation of Privilege | scope escape into `typsphinx/`, `tests/`, `docs/`, `pyproject.toml` or `uv.lock` under cover of release prep | high | mitigate | An emptiness check over `git status --porcelain` for those paths, and a scope fence allowing only `CHANGELOG.md`, the evidence file and the SUMMARY to differ from `BASE_69_01`. |
| T-69-05 | Tampering | a bullet claiming CI coverage of `flake.nix`, verified darwin, an observed grouped update or a user-facing effect | medium | mitigate | `## Accuracy basis` maps each claim to a named evidence file; the prohibitions name each overstatement; each bullet carries the D-02 no-effect phrase. |
| T-69-SC | Tampering | package installs | low | accept | No new package is added. `uv sync --extra dev --extra docs` installs only what `uv.lock` already pins, and `69-RESEARCH.md` § "Package Legitimacy Audit" records zero added packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This is the phase-wide list; plans 69-02 to 69-06 point here rather than repeating it.

**New files under `.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/`:**

| File | Created by | Extended by |
|------|------------|-------------|
| `69-CHANGELOG-EVIDENCE.md` | 69-01 | — |
| `69-CLOSEOUT-GUARD.md` | 69-02 (phase-head baseline and operator procedure) | 69-06 (close-time re-verification) |
| `69-SC1-INVARIANTS.md` | 69-02 (observation 1 of 2 and the milestone `typsphinx/` fence) | 69-06 (observation 2 of 2, the phase-scoped diff, commits after the CI dispatch) |
| `COVERAGE.md` | 69-02 (reasoned no-external-API declaration) | — |
| `69-GREEN-TREE-EVIDENCE.md` | 69-03 | — |
| `69-CI-EVIDENCE.md` | 69-04 | — |
| `69-PREFLIGHT-EVIDENCE.md` | 69-05 (D-07 non-committing trial merge) | — |
| `69-HANDOFF.md` | 69-06 | — |
| `69-01-SUMMARY.md` to `69-06-SUMMARY.md` | one per plan | — |

**Product tree:** exactly one file changes, `CHANGELOG.md`, and only by addition. The change adds a
`### Changed` subsection with three bullets inside the existing `## [Unreleased]` heading. No new
`## [...]` heading and no link-reference line is added.

**Outside the repository:** 69-04 fast-forwards `origin/gsd/v0.9.3-toolchain-and-dependency-update-repair`
to `PUSHED_SHA` and starts one `workflow_dispatch` run of `ci.yml`. 69-05 creates scratch directories
outside the repository and removes them.

**Reserved name, never produced:** no plan authors `69-VERIFICATION.md`; `gsd-verifier` owns that
name and overwrites it wholesale.
</artifacts_this_phase_produces>

<verification>
- The `## [Unreleased]` region holds `### Changed` with exactly three bold-lead bullets citing all 20
  IDs, above a byte-identical `### Planned for Future Releases`.
- No `0.9.3` anywhere in `CHANGELOG.md`; heading count, link-reference count and final line equal
  the base; the diff from the base is pure addition.
- Both docs environments, built clean before and after the edit, report equal warning counts, and
  the PDF is real.
- Only `CHANGELOG.md` and `69-CHANGELOG-EVIDENCE.md` changed; `.planning/REQUIREMENTS.md` untouched.
</verification>

<success_criteria>
1. ROADMAP SC#2: the milestone's work is described under the existing `## [Unreleased]` heading, with
   no release section, no `[0.9.3]` link and the compare base left at `v0.9.2` (D-01..D-05).
2. The docs half of SC#3: both docs environments build clean after the edit with the warning counts
   of the clean pre-edit baseline.
3. Every bullet is accurate to the recorded evidence and states it has no effect on installing or
   using typsphinx.
</success_criteria>

<output>
Create `.planning/phases/69-v0-9-3-close-prep-prep-only-unpublished/69-01-SUMMARY.md` when done.
- Frontmatter `requirements-completed: []`. REL-12 closes at `/gsd-complete-milestone`, never in a
  plan.
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
- The first section states `BASE_69_01`, the four docs keys (`_BASE` and `_POST` for both
  environments), the bullet count and the ID count. If a HALT heading was written, that section
  says so first.
</output>
