---
phase: 63-v0-9-2-release-prep-prep-only
plan: 04
type: execute
wave: 3
depends_on: ["63-02", "63-03"]
files_modified:
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md
autonomous: true
requirements: [REL-09, REL-11]
requirements-completed: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC#5 observation 2 of 2: the same four probes are re-run with the same positive controls at a LATER timestamp and in a LATER WAVE than observation 1. 63-SC5-INVARIANTS.md carries at least two distinct UTC timestamps and states the wave separation explicitly, because SC#5 requires the two observations to be separated by intervening waves rather than by wall-clock luck.'
    - 'SC#5: the scoped diff of typsphinx/ from PHASE_BASE_SHA to the phase tip is empty, and it is paired with a same-anchor WIDENED diff as its positive control. The widened diff must be non-empty and must be exactly this phase''s five touched files — pyproject.toml, uv.lock, README.md, CHANGELOG.md and tests/test_changelog_page_gate.py — not Phase 61''s single-file result. An empty scoped diff from a wrong or unreachable anchor is indistinguishable from a genuinely clean tree without that control.'
    - 'SC#3 / REL-11 / D-16: the closeout guard triad is re-run at phase close and compared line-for-line against the recorded Baseline — the SHA-256 digest, a name-only git diff over .planning/REQUIREMENTS.md expecting no output, and the REL-09 grep expecting byte-identical hits. A divergence is reverted with ``git checkout -- .planning/REQUIREMENTS.md`` and reported, never accepted and never committed.'
    - 'D-16: the third observation is not this plan''s to take. It runs after phase.complete-family tooling, outside any plan''s reach, and is the one that historically catches the flip. 63-CLOSEOUT-GUARD.md carries its protocol and 63-HANDOFF.md points an operator at it, so it is reachable without opening the guard file by name.'
    - 'D-13: 63-HANDOFF.md opens by stating the POSITIVE — this milestone DOES publish, and the checklist below IS the sequence /gsd-complete-milestone executes. Phase 61 opened with the negative because publishing nothing was the anomaly there; restoring the standing shape is the correction, not an oversight.'
    - 'SC#5: 63-HANDOFF.md is standalone and enumerates every step /gsd-complete-milestone must execute — the v0.9.2 tag push, the pypi GitHub Environment''s manual approval, the GitHub Release body''s byte-identity to the extractor''s stdout, the typsphinx-doc-translations update-pin.yml MANUAL dispatch and that repository''s own separate tag, and the Read the Docs en and ja stable endpoints reporting 0.9.2. An operator who reads only this file can execute the close.'
    - 'D-15: the pypi GitHub Environment''s manual approval is named as an EXPECTED gate, and it is positioned in the operator''s own reading order BEFORE the step that triggers it — a workflow paused on an environment approval is indistinguishable from a failed workflow to an operator scanning a run list.'
    - 'D-14: REL-04 is re-offered as a named pre-flight and post-flight observation inside 63-HANDOFF.md, carrying all four of its items: that the create-release job has explicit uv-install steps at HEAD and ran green at the v0.8.0 and v0.9.0 real tag pushes so a failure now is a regression rather than the known earlier defect; the exact observation to make; the response if it fails, which is fix-and-rerun inside the release work rather than defer; and that its todo stays in pending and closes only on an observed green create-release at a real tag push. REL-04 is not folded into any plan and is not promoted to a requirement.'
    - 'Every publish step in 63-HANDOFF.md is RECORDED, never performed. No command in this plan tags, pushes a tag, publishes, creates a release, opens a pull request, dispatches update-pin.yml, or triggers release.yml.'
    - 'D-16: REL-09 is cited by this plan as a coverage ID ONLY. Its checkbox is read directly out of .planning/REQUIREMENTS.md as unchecked at close and never inferred from frontmatter; 63-04-SUMMARY.md frontmatter declares ``requirements-completed: []`` for it; and the handoff quotes REL-09''s requirement text verbatim out of that file rather than paraphrasing it.'
    - 'D-19: no plan in this phase authors a file named 63-VERIFICATION.md. That name is gsd-verifier''s reserved output for the phase and a plan-authored file there is clobbered wholesale at verify time, so the phase''s evidence set is exactly 63-CLOSEOUT-GUARD.md, 63-CHANGELOG-EVIDENCE.md, 63-GREEN-TREE-EVIDENCE.md, 63-CI-EVIDENCE.md, 63-SC5-INVARIANTS.md and 63-HANDOFF.md — the naming set Phase 61 used, with no seventh file and no substitution. This is a phase-wide invariant and this plan is where the goal-backward check lands, because this plan closes the phase and its tip is the FINAL phase directory; all four plans additionally restate the absence in their own plan-level verification section, and plans 63-02 and 63-04 assert it runnably with a file-existence test, so the reserved name cannot appear transiently mid-phase either.'
  artifacts:
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md
  key_links:
    - '63-CLOSEOUT-GUARD.md PHASE_BASE_SHA -> this plan''s scoped and widened diffs: one recorded value decides whether SC#5''s central claim is a finding or an artifact. The widened diff is the control that distinguishes the two, and its expected result differs from Phase 61''s because this phase actually bumps.'
    - '63-HANDOFF.md -> /gsd-complete-milestone: this is the only artifact carrying the publish sequence forward. A step omitted here is a step that does not happen — the translations-repository pin dispatch in particular is a MANUAL dispatch on another repository and is not a side effect of this repository''s tag push.'
    - 'scripts/extract_changelog_section.py 0.9.2 -> release.yml create-release -> the published GitHub Release body: the handoff records the byte-identity check an operator runs after the release exists. Plan 63-01 already proved the extractor''s output matches the section on disk; this is the second half of the same chain, executed at milestone close rather than here.'
    - '.planning/REQUIREMENTS.md REL-09 checkbox -> phase.complete-family tooling: the flip has landed at five consecutive prior release-prep closes and lands AFTER the last plan runs, which is why the decisive observation is delegated to the operator through the guard file rather than attempted here.'
  prohibitions:
    - 'MUST NOT perform any irreversible action in this phase — no git tag (local or remote), no tag push, no PyPI upload, no GitHub Release creation, no pull request, no update-pin.yml dispatch on typsphinx-doc-translations, no Read the Docs re-flip, and no trigger of release.yml by any route (tag push or workflow_dispatch). Each of those executes at /gsd-complete-milestone and is RECORDED in 63-HANDOFF.md, never performed. Every command shape written into the handoff is a record for a future operator, not an instruction for this phase.'
    - 'MUST NOT touch anything under typsphinx/. SC#5 git diff over the phase proves that path empty, and this plan is the one that takes that measurement.'
    - 'MUST NOT modify .planning/REQUIREMENTS.md REL-09 checkbox, its Traceability row, or record REL-09 as satisfied anywhere — including in the handoff prose. If tooling flips it, the flip is reverted with ``git checkout -- .planning/REQUIREMENTS.md`` and reported — never accepted, never committed. No SUMMARY.md in this phase declares REL-09 in requirements-completed.'
    - 'MUST NOT publish a version reference the project cannot resolve, and MUST NOT let the handoff read as authorising a publish now. The handoff describes what /gsd-complete-milestone will do; it never instructs this phase to do it.'
    - 'MUST NOT frame this release as Windows-exclusive, and MUST NOT assert in the handoff any result this phase did not measure. Every claim about the tree''s state cites the evidence file and section it rests on.'
    - 'MUST NOT record inherited, paraphrased, recalled or fabricated evidence as this phase own. Observation 2''s probe outputs are captured fresh at a later timestamp, never copied from observation 1, and no probe is recorded without the positive control that proves it reached its source.'
---

<objective>
Close the phase honestly: take the second of SC#5's two waves-separated fence observations together
with the scoped `typsphinx/` diff and its positive control, re-verify the REL-09 checksum fence at
phase close and revert by hand rather than accept any flip, and author a standalone `63-HANDOFF.md`
that opens by stating this milestone **does** publish and enumerates every step
`/gsd-complete-milestone` will execute.

Purpose: this wave exists so the fence's two observations are separated by real intervening work
rather than by wall-clock luck, and so the audit sits later than the thing it audits — this project
has already recorded a case where an audit co-located with its subject abstained on its own
criterion. It is also the phase's only chance to write the publish sequence down: the tag push, the
PyPI upload, the GitHub Release, the second repository's pin dispatch and tag, and the Read the Docs
verification all happen after this phase ends, and none of them happens at all if it is not recorded
here.

Output: `63-SC5-INVARIANTS.md` extended with observation 2 and the scoped/widened diff pair,
`63-CLOSEOUT-GUARD.md` extended with the close-time re-verification, and `63-HANDOFF.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-VALIDATION.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution";
ROADMAP milestone constraint 12). It is mandatory and not conditional on parallelism. Before running
ANY command in this plan, provision this worktree's own virtual environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. This plan runs no test suite and no build, but
the provisioning is unconditional and establishes the worktree identity the diff measurements are
taken in.
</worktree_provisioning>

<!-- planner-discipline-allow: Latest -->
<!-- planner-discipline-allow: positive control -->
<!-- planner-discipline-allow: Observation 2 of 2 -->
<!-- planner-discipline-allow: widened -->
<!-- planner-discipline-allow: wave 3 -->
<!-- planner-discipline-allow: REL-09 -->
<!-- planner-discipline-allow: REL-04 -->
<!-- planner-discipline-allow: Re-verification at phase close -->
<!-- planner-discipline-allow: For the operator running phase.complete -->
<!-- planner-discipline-allow: git checkout -- .planning/REQUIREMENTS.md -->
<!-- planner-discipline-allow: update-pin.yml -->
<!-- planner-discipline-allow: typsphinx-ja -->
<!-- planner-discipline-allow: create-release -->
<!-- planner-discipline-allow: extract_changelog_section.py -->
<!-- planner-discipline-allow: expected gate -->
<!-- planner-discipline-allow: does publish -->
<!-- planner-discipline-allow: 2026-08-04-release-create-job-missing-uv-verify-end-to-end.md -->
<!-- Every literal above appears in a POSITIVE presence check (`-ge 1`, `-ge 4`, `-ge 6`, `= 1`) or
     a line-ordering comparison over an evidence or handoff file this plan writes — never in an
     absence check — so echoing it into an action body cannot invalidate anything. The only
     zero-count assertions in this plan are over `.planning/REQUIREMENTS.md` (a checked REL-09
     checkbox, which no action body writes) and over the remote tag list (the release tag, which no
     action body creates). -->

<tasks>

<task type="auto">
  <name>Task 1: Record SC#5 fence observation 2 of 2 at a later timestamp in a later wave, with the scoped `typsphinx/` diff and its same-anchor widened positive control</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md</files>
  <precondition>`gh auth status` succeeds and `git ls-remote --heads origin` returns at least one line, and `63-CLOSEOUT-GUARD.md` exists carrying a labelled PHASE_BASE_SHA that `git cat-file -e` resolves. An unreachable remote turns every probe into a silence indistinguishable from a clean result, and an unresolvable anchor turns the scoped diff into a vacuous empty. Halt and report on either.</precondition>
  <read_first>
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md` — the file being extended, including observation 1's four probe subsections with their recorded values and timestamp, the milestone-anchor section, and the handoff-to-observation-2 section that states what this task owes.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md` § "Baseline" — the labelled PHASE_BASE_SHA this task scopes both diffs against, and the anchor commit's quoted subject.
    - `.planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-SC4-INVARIANTS.md` §§ "Observation 2 of 2", "Observation 2 verdict — the separation, stated explicitly", "The typsphinx/ diff (SC#4)" and "Commits after the CI dispatch" — the analog structure and the explicit-separation wording to mirror.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-RESEARCH.md` § "Code Examples" §§ "SC#5's tag/publish probe pair, with positive controls" and "The scoped-vs-widened `typsphinx/` diff", plus § "Anti-Patterns to Avoid" bullets 1 and 2.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md` § "`63-SC5-INVARIANTS.md`" — including its warning that the widened diff's expected non-empty result for this phase is five files, not Phase 61's single file.
    - `.planning/ROADMAP.md` § "Phase Details → Phase 63" SC#5.
  </read_first>
  <action>
    Append to `63-SC5-INVARIANTS.md` — do not overwrite observation 1, which is this file's proof
    that the two observations are separated.

    **`## Observation 2 of 2`.** Record a fresh UTC timestamp, then re-run all four probes with their
    positive controls and record each as a command with its literal output. Capture the values fresh;
    copying observation 1's output would defeat the entire purpose of taking a second observation.

    The local tag probe: the release tag list, expected empty, paired with the 0.9.0 tag list as the
    positive control, expected non-empty. The remote tag probe: one unfiltered
    `git ls-remote --tags origin` fetch, from which both counts are derived — at least one reference
    matching the 0.9.0 tag as the control and exactly zero matching the release tag as the assertion.
    The publish probe: one `gh release list --limit 20` fetch, from which the `Latest` marker count
    is the control and the release-version row count is the assertion; plus `gh release view` on the
    release tag recorded with its literal not-found message and non-zero exit. The release-workflow
    probe: `gh run list --workflow=release.yml --limit 5`, a READ-ONLY listing, recorded literally,
    with the statement that no run corresponds to a tag push for this release and that the workflow
    was never dispatched.

    **`### Observation 2 verdict — the separation, stated explicitly`.** State the two timestamps
    side by side, state that observation 1 was taken in wave 1 and this one in wave 3, and name what
    happened in between: the bump commit and CHANGELOG promotion in wave 1, and the full green-tree
    proof and the CI dispatch in wave 2. SC#5 asks for separation by intervening waves rather than by
    wall-clock luck, and this paragraph is where that is made legible rather than left to be inferred
    from two nearby timestamps.

    **`## The typsphinx/ diff (SC#5)`.** Read PHASE_BASE_SHA back out of `63-CLOSEOUT-GUARD.md`
    rather than re-deriving it, and confirm with `git cat-file -e` that it resolves. Then record two
    diffs against that one anchor.

    `### The scoped diff (the SC#5 claim)`: the `typsphinx/`-scoped diff from PHASE_BASE_SHA to
    HEAD, expected to produce no output — this phase makes no product-tree behaviour change.

    `### The widened diff (the positive control)`: the same anchor with the scope widened to
    everything except `.planning/`, recorded as a `--stat`. It must be NON-empty and must list
    exactly the five files this phase touches: `CHANGELOG.md`, `README.md`, `pyproject.toml`,
    `tests/test_changelog_page_gate.py` and `uv.lock`. Do not copy Phase 61's expected single-file
    result — that phase performed no bump, and this one does. State in one line why this pairing
    exists: an empty scoped diff produced by a wrong or unreachable anchor looks exactly like a
    genuinely clean tree, and only a non-empty same-anchor widened result distinguishes them.

    **`## Commits after the CI dispatch`.** Record the commits that landed after the run recorded in
    `63-CI-EVIDENCE.md`, with their paths, and state that all of them are confined to `.planning/` —
    which is why one dispatch satisfies D-18 and no second run is owed. If any commit outside
    `.planning/` landed after the dispatch, that is a finding: record it and report, because it would
    mean the CI run no longer covers the tree.
  </action>
  <verify>
    <automated>BASE=$(grep -oE '\b[0-9a-f]{40}\b' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md | head -1) &amp;&amp; git cat-file -e "$BASE" &amp;&amp; [ -z "$(git diff "$BASE"..HEAD -- typsphinx/)" ] &amp;&amp; [ -n "$(git diff --stat "$BASE"..HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ "$(git diff --name-only "$BASE"..HEAD -- . ':(exclude).planning' | LC_ALL=C sort | tr '\n' ' ')" = 'CHANGELOG.md README.md pyproject.toml tests/test_changelog_page_gate.py uv.lock ' ] &amp;&amp; [ -z "$(git tag -l 'v0.9.2')" ] &amp;&amp; [ -n "$(git tag -l 'v0.9.0')" ] &amp;&amp; git ls-remote --tags origin &gt; /tmp/63-tags2.txt &amp;&amp; [ "$(grep -c 'refs/tags/v0\.9\.0' /tmp/63-tags2.txt)" -ge 1 ] &amp;&amp; [ "$(grep -c 'refs/tags/v0\.9\.2' /tmp/63-tags2.txt)" = 0 ] &amp;&amp; gh release list --limit 20 &gt; /tmp/63-releases2.txt &amp;&amp; [ "$(grep -c 'Latest' /tmp/63-releases2.txt)" -ge 1 ] &amp;&amp; [ "$(grep -c 'v0\.9\.2' /tmp/63-releases2.txt)" = 0 ] &amp;&amp; [ "$(grep -c 'Observation 2 of 2' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md)" -ge 1 ] &amp;&amp; [ "$(grep -oE '20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md | LC_ALL=C sort -u | wc -l)" -ge 2 ] &amp;&amp; [ "$(grep -ci 'positive control' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md)" -ge 6 ] &amp;&amp; [ "$(grep -c 'widened' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md)" -ge 1 ] &amp;&amp; [ "$(grep -ci 'wave 3' .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md)" -ge 1 ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ .planning/REQUIREMENTS.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `63-SC5-INVARIANTS.md` contains both an `## Observation 1 of 2` and an `## Observation 2 of 2` heading, and at least two DISTINCT ISO-8601 Zulu timestamps
    - observation 2 records all four probes fresh, each with its literal output, and the phrase `positive control` appears at least six times across the file (three per observation)
    - the local tag probe records the release tag list empty and the 0.9.0 tag list non-empty; the remote tag probe derives at least 1 control reference and exactly 0 release-tag references from ONE unfiltered fetch; the publish probe derives at least 1 `Latest` marker and exactly 0 release rows from ONE fetch, with `gh release view` on the release tag recorded as not found
    - `### Observation 2 verdict` names wave 1 and wave 3 explicitly and lists the intervening work — the bump and CHANGELOG promotion, then the green-tree proof and the CI dispatch
    - PHASE_BASE_SHA is read back out of `63-CLOSEOUT-GUARD.md`, `git cat-file -e` on it exits 0, and both diffs are recorded against that one anchor
    - the `typsphinx/`-scoped diff from PHASE_BASE_SHA to HEAD produces no output
    - the same-anchor widened diff is NON-empty and its file list under `LC_ALL=C sort` is exactly `CHANGELOG.md README.md pyproject.toml tests/test_changelog_page_gate.py uv.lock`, and the file states in one line why this pairing is what makes the scoped emptiness a finding
    - `## Commits after the CI dispatch` lists every commit landed since the recorded run and states that all are confined to `.planning/`; any commit outside `.planning/` is recorded as a finding and reported
    - `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md` is empty
  </acceptance_criteria>
  <done>The second waves-separated fence observation is recorded with fresh values and full positive controls, and the SC#5 claim that this phase changed nothing under the product tree rests on a scoped diff whose anchor is proven reachable by a non-empty widened diff over the same anchor.</done>
  <reversibility rating="reversible">Read-only probes and appended planning-document sections; nothing is written to any remote and no workflow is dispatched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Re-verify the REQUIREMENTS.md closeout guard at phase close and revert any flip by hand rather than accept it</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md</files>
  <read_first>
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md` in full — the file being extended: its recorded Baseline digest, line count, PHASE_BASE_SHA and timestamp; its classified lines under guard; its re-verification protocol; and its operator-facing post-`phase.complete` section.
    - `.planning/REQUIREMENTS.md` — the file being re-measured and quoted, never edited.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-16 in full.
    - `.planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-CLOSEOUT-GUARD.md` § "Re-verification at phase close" — the analog section shape, including how a MATCH is recorded rather than merely asserted.
    - `.planning/ROADMAP.md` § "🚧 v0.9.2" milestone constraint 9, and § "Phase Details → Phase 63" SC#3.
  </read_first>
  <action>
    Append a `## Re-verification at phase close` section to `63-CLOSEOUT-GUARD.md`, recording the
    protocol's own triad as commands with their literal output, at a fresh UTC timestamp.

    Run `sha256sum .planning/REQUIREMENTS.md` and compare the printed digest character-for-character
    against the Baseline digest recorded in this same file. Run `wc -l` and compare against the
    recorded line count. Run a name-only `git diff` scoped to `.planning/REQUIREMENTS.md` and expect
    no output. Run `grep -n 'REL-09' .planning/REQUIREMENTS.md` and compare its hits line-for-line
    against the quoted Baseline hits — both the line numbers and the byte content of the two
    state-bearing lines, and the presence of the informational-only third hit.

    Record the verdict as MATCH or DIVERGED, with the compared values shown side by side rather than
    a bare word. A verdict a reader cannot re-derive from the file is not evidence.

    **If any comparison diverges** — most likely REL-09's checkbox having been flipped to checked
    against this phase's explicit decision — do not accept it and do not commit it. Run
    `git checkout -- .planning/REQUIREMENTS.md`, re-run the full triad, record the before state, the
    reversion command, and the after state, and report the divergence explicitly in this section and
    in the plan's summary. The flip has landed at five consecutive prior release-prep closes and
    Phase 61 was the first to hold; the only thing that made the difference was recording, comparing
    and reverting rather than assuming.

    Then re-state, in a closing paragraph of this section, that the DECISIVE observation is still
    owed and is not this plan's to take: it runs after `phase.complete`-family tooling, outside any
    plan's reach, and its protocol is already written into this file's operator-facing section.
    Confirm that section is present and reachable, and that `63-HANDOFF.md` will point at it.

    Finally, read REL-09's state directly out of `.planning/REQUIREMENTS.md` and record its checkbox
    line and its Traceability row verbatim, showing the checkbox unchecked and the row reading
    Pending. REL-09's state is read out of that file at close and never inferred from any plan's
    frontmatter.
  </action>
  <verify>
    <automated>[ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(grep -oE '\b[0-9a-f]{64}\b' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md | head -1)" ] &amp;&amp; [ "$(grep -c "$(wc -l &lt; .planning/REQUIREMENTS.md) .planning/REQUIREMENTS.md" .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md)" -ge 1 ] &amp;&amp; [ -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" ] &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md)" ] &amp;&amp; [ "$(grep -c '^- \[ \] \*\*REL-09\*\*' .planning/REQUIREMENTS.md)" = 1 ] &amp;&amp; [ "$(grep -c '^- \[x\] \*\*REL-09\*\*' .planning/REQUIREMENTS.md)" = 0 ] &amp;&amp; [ "$(grep -c '| REL-09 | Phase 63 | Pending |' .planning/REQUIREMENTS.md)" = 1 ] &amp;&amp; [ "$(grep -c 'REL-09' .planning/REQUIREMENTS.md)" = 3 ] &amp;&amp; [ "$(grep -c 'Re-verification at phase close' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'For the operator running phase.complete' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md)" -ge 1 ] &amp;&amp; [ "$(grep -oE '20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md | LC_ALL=C sort -u | wc -l)" -ge 2 ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - the live `sha256sum .planning/REQUIREMENTS.md` digest equals the Baseline digest recorded in `63-CLOSEOUT-GUARD.md`, and the live `wc -l` equals the recorded line count — both shown side by side in the appended section, not asserted as a bare verdict word
    - `git diff --name-only -- .planning/REQUIREMENTS.md` and `git status --porcelain .planning/REQUIREMENTS.md` are both empty
    - `.planning/REQUIREMENTS.md` contains exactly one UNCHECKED REL-09 requirement bullet, zero checked ones, exactly one `| REL-09 | Phase 63 | Pending |` Traceability row, and exactly three total `REL-09` occurrences — the same three the Baseline recorded
    - the appended `## Re-verification at phase close` section records a MATCH or DIVERGED verdict with the compared values shown, at a fresh UTC timestamp distinct from the Baseline's
    - if any comparison diverged, the section records the before state, the literal `git checkout -- .planning/REQUIREMENTS.md` reversion, the re-run triad and the after state, and the divergence is reported in the plan summary — never accepted, never committed
    - the section closes by re-stating that the decisive third observation is still owed, runs after `phase.complete`-family tooling, and has its protocol in this file's operator-facing section, which is confirmed present
    - REL-09's checkbox line and Traceability row are quoted verbatim out of `.planning/REQUIREMENTS.md`, read from the file rather than inferred from any plan's frontmatter
    - `git status --porcelain typsphinx/` is empty
  </acceptance_criteria>
  <done>The checksum fence is re-verified at phase close against its own recorded Baseline with the compared values shown, REL-09's checkbox is confirmed still unchecked by reading the file directly, and the decisive post-tooling observation is confirmed reachable in the operator-facing section rather than assumed.</done>
  <reversibility rating="reversible">A read-only comparison appended to an existing planning document; the only mutation this task may perform is a `git checkout` that RESTORES a file to its committed state.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Author `63-HANDOFF.md` — a standalone publish checklist opening with the positive, carrying D-14's four REL-04 items and D-15's approval gate ahead of the step that triggers it</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md</files>
  <read_first>
    - `.planning/milestones/v0.9.1-phases/61-v0-9-1-release-prep-prep-only/61-HANDOFF.md` in full — the structure being followed and the polarity being INVERTED. Read especially § "What the v0.9.2 milestone inherits" (the three publish steps written with a version placeholder precisely so this phase can resolve them) and § "What v0.9.2 must also pick up" (the REL-04 re-offer instruction and the bullet-promotion instruction).
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-13, D-14 and D-15 in full, and § "Claude's Discretion" bullet 5 (the heading structure is delegated, provided D-13's positive opening, D-14's four items and D-15's note all survive).
    - `.planning/ROADMAP.md` § "Phase Details → Phase 63" SC#5's five enumerated steps, and its second cross-cutting constraint about the release workflow's create-release job.
    - `.planning/REQUIREMENTS.md` § "REL — release" REL-09's text — quoted VERBATIM in the handoff, not paraphrased — and § "v2 Requirements" REL-04's entry with its note about a failure being handled inside the release phase rather than deferred.
    - `.planning/todos/pending/2026-08-04-release-create-job-missing-uv-verify-end-to-end.md` — the todo D-14 item (d) says stays in `pending/`.
    - `.github/workflows/release.yml` — read to describe accurately: its `validate`, `build`, `publish-pypi` and `create-release` jobs, the `pypi` environment gate, and the `Install uv` steps in `create-release`. Read only; never triggered.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md` § "`63-HANDOFF.md`" — the section skeleton, the three inherited publish-step command shapes with their placeholder resolved, the REL-09 verbatim-quote convention, and the D-14 / D-15 requirements.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md` § "For the operator running phase.complete" — the section this handoff must point at by name.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md`, `63-GREEN-TREE-EVIDENCE.md`, `63-CI-EVIDENCE.md`, `63-SC5-INVARIANTS.md` — the evidence each of the handoff's status claims cites; every claim names its file and section rather than restating a verdict.
  </read_first>
  <action>
    Create `.planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md`. The heading structure is
    delegated to judgement (Claude's Discretion bullet 5), provided the three binding elements below
    all survive.

    **Open with the POSITIVE (D-13).** The first paragraph states plainly that this milestone **does**
    publish, that the checklist below **is** the sequence `/gsd-complete-milestone` executes, and that
    nothing in it was performed during this phase. Phase 61 opened with the negative because
    publishing nothing was the anomaly there; restoring the standing shape that seven handoffs before
    it used is the correction, not an oversight. Also state in the same opening that this phase itself
    took zero irreversible action, citing `63-SC5-INVARIANTS.md`'s two observations by name.

    **Report what this phase satisfied, citing evidence rather than restating verdicts.** Quote
    REL-09's requirement text VERBATIM out of `.planning/REQUIREMENTS.md`, as a blockquote, and state
    that it remains open and unmet because the release itself has not happened — it closes at
    `/gsd-complete-milestone`, not here. Then report each of the phase's five success criteria with
    the evidence file and section it rests on: the bump and its one-commit shape, the curated section
    and the extractor's read stdout, the checksum fence, the green-tree and CI proof, and the fence
    observations. Name files and sections; do not re-derive results.

    **The publish checklist — every step recorded, none performed.** Enumerate, in the operator's own
    execution order, all five steps SC#5 requires.

    Put D-15's note BEFORE the step that triggers it: the `pypi` GitHub Environment carries a manual
    approval, and a workflow paused on that approval looks exactly like a failed workflow to an
    operator scanning a run list. Name it as an EXPECTED gate in its own right, positioned ahead of
    the tag push, so the operator meets the warning before the surprise.

    Then the tag push itself, recorded as a command shape for the operator to execute later —
    creating the annotated tag on the merge commit and pushing it — with a plain statement that this
    is what triggers the release workflow and that nothing in this phase executed it.

    Then the GitHub Release body's byte-identity check: run the extraction script for 0.9.2 into a
    file, read the published release's body into another, and diff them, with a non-empty diff being
    a hard failure rather than a formatting difference. Note that plan 63-01 already proved the
    extractor's stdout byte-identical to the section on disk, so this check closes the second half of
    the same chain.

    Then the `typsphinx-doc-translations` step, stated as TWO separate actions: a MANUAL
    `update-pin.yml` dispatch on that repository — it is not a side effect of this repository's tag
    push — followed by tagging that repository's own tracked branch. Record the dispatch, run-list
    and watch command shapes.

    Then the Read the Docs verification: the `en` project and the `ja` project (slug `typsphinx-ja`),
    both `stable` endpoints, checked with unauthenticated public API calls — the root resolving to the
    English stable path, each project's stable version identifier, both pages reporting the new
    version, and both PDFs served as PDF content.

    **D-14's REL-04 re-offer, as its own named subsection with all four items.** State (a) that the
    release workflow's `create-release` job carries explicit uv-install steps at HEAD and ran green at
    the v0.8.0 and v0.9.0 real tag pushes, so a failure now would be a regression rather than the
    known earlier defect; (b) the exact observation to make — watch the release run, then read that
    job's conclusion literally; (c) the response if it fails — fix it inside this release work and
    re-run the job, do not defer; and (d) that
    `2026-08-04-release-create-job-missing-uv-verify-end-to-end.md` stays in `.planning/todos/pending/`
    and closes only on an observed green `create-release` at a real tag push, which does not happen in
    this phase. State that REL-04 is not folded into any plan and is not promoted to a requirement.

    **Before declaring the milestone closed.** Point the operator explicitly at
    `63-CLOSEOUT-GUARD.md` § "For the operator running phase.complete" and state that the third and
    decisive fence observation is owed there, after `phase.complete`-family tooling has run — the
    moment the REL-09 flip has landed at five consecutive prior closes. Reproduce the reversion rule
    in one line so it is actionable without opening the other file.

    **What this phase deliberately did not do.** Close with the fence restated: no tag local or
    remote, no PyPI upload, no GitHub Release, no pull request, no `update-pin.yml` dispatch, no Read
    the Docs re-flip, and no trigger of the release workflow by any route. Note the three ideas
    declined for this phase by D-10 and where they may be revisited: a security advisory, a yank of
    the published 0.9.0, and a README upgrade banner are all outward-facing and irreversible, and
    belong at or after `/gsd-complete-milestone` if the owner wants them — recorded here rather than
    silently dropped. Add one line for D-12 in the same place: no migration guide was written,
    because 0.9.2 breaks nothing and the one prior no-breaking-change patch release has none either,
    so nothing about migration is owed at the close.

    Write it so an operator who reads only this file can execute the close. Do not assume the reader
    has any other file open.
  </action>
  <verify>
    <automated>test -f .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md &amp;&amp; [ "$(wc -l &lt; .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 60 ] &amp;&amp; [ "$(head -12 .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md | grep -ci 'does publish')" -ge 1 ] &amp;&amp; [ "$(grep -c 'update-pin.yml' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'typsphinx-ja' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'extract_changelog_section.py' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'create-release' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 1 ] &amp;&amp; [ "$(grep -c '2026-08-04-release-create-job-missing-uv-verify-end-to-end.md' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'REL-04' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 4 ] &amp;&amp; [ "$(grep -c 'For the operator running phase.complete' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'git checkout -- .planning/REQUIREMENTS.md' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 1 ] &amp;&amp; [ "$(grep -c '^&gt; - \[ \] \*\*REL-09\*\*' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 1 ] &amp;&amp; [ "$(grep -ci 'expected gate' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md)" -ge 1 ] &amp;&amp; [ "$(grep -n 'git push origin v0\.9\.2' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md | head -1 | cut -d: -f1)" -gt "$(grep -ni 'expected gate' .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md | head -1 | cut -d: -f1)" ] &amp;&amp; [ -z "$(git tag -l 'v0.9.2')" ] &amp;&amp; [ "$(git ls-remote --tags origin | grep -c 'refs/tags/v0\.9\.2')" = 0 ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ .planning/REQUIREMENTS.md)" ] &amp;&amp; [ ! -f .planning/phases/63-v0-9-2-release-prep-prep-only/63-VERIFICATION.md ]</automated>
    <human-check>Read `63-HANDOFF.md` cold, as an operator who has no other file open, against SC#5's five enumerated steps. Confirm three things a grep cannot: that the opening reads as a publish checklist rather than as an inheritance record (D-13's polarity), that every one of the five steps is present in an order an operator could actually follow, and that nothing in the document reads as authorising a publish NOW rather than describing what `/gsd-complete-milestone` will do. Standalone-ness is a reading test, and polarity is a judgement.</human-check>
  </verify>
  <acceptance_criteria>
    - `63-HANDOFF.md` exists, is at least 60 lines, and its first 12 lines state that this milestone DOES publish and that the checklist below is the sequence `/gsd-complete-milestone` executes
    - the opening also states that this phase took zero irreversible action, citing `63-SC5-INVARIANTS.md`'s two observations by name
    - REL-09's requirement text is quoted VERBATIM as a blockquote beginning with the unchecked-checkbox form, read out of `.planning/REQUIREMENTS.md` rather than paraphrased, and reported as open and unmet
    - each of the phase's five success criteria is reported with the evidence file and section it rests on, named rather than restated
    - all five SC#5 steps are present: the tag push, the `pypi` Environment approval, the Release-body byte-identity check naming `extract_changelog_section.py`, the `update-pin.yml` MANUAL dispatch plus that repository's own separate tag, and the Read the Docs `en` and `ja` (`typsphinx-ja`) stable checks
    - the `pypi` Environment approval is named as an EXPECTED gate, and its first mention occurs at a LOWER line number than the first occurrence of the tag-push command — the operator meets the warning before the surprise
    - a named REL-04 subsection carries all four D-14 items, including the todo filename `2026-08-04-release-create-job-missing-uv-verify-end-to-end.md` and the statement that it stays in `pending/`
    - the handoff points at `63-CLOSEOUT-GUARD.md` § "For the operator running phase.complete" by name and reproduces the `git checkout -- .planning/REQUIREMENTS.md` reversion rule in one line
    - a closing section restates the fence and names D-10's three declined outward-facing ideas (a security advisory, a yank of the published 0.9.0, a README upgrade banner) with where they may be revisited, plus one line for D-12 stating that no migration guide was written and why
    - `git tag -l 'v0.9.2'` is empty and an unfiltered `git ls-remote --tags origin` shows zero references matching the release tag — the handoff records the publish sequence without performing any part of it
    - `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md` is empty and no file named `63-VERIFICATION.md` exists under the phase directory
  </acceptance_criteria>
  <done>A standalone handoff exists that opens by stating this milestone publishes, enumerates all five close-out steps in the operator's own execution order with the approval gate named ahead of the step that triggers it, carries REL-04's four re-offered items, and points at the decisive post-tooling fence observation — with every step recorded and none performed.</done>
  <reversibility rating="reversible">A new planning document describing future actions; nothing is executed and no remote is written.</reversibility>
</task>

</tasks>

<flagged_assumptions>
## Spec-less accounting

The phase-wide edge-coverage and prohibition-recall accounting is authored once, in
`63-01-PLAN.md` § `<flagged_assumptions>`. This plan does not repeat it.

Relevant here: REL-11's probe row is the phase's single **unresolved** item, classified
`unclassified` and surfaced as a flagged assumption rather than authored into `must_haves.truths` or
auto-resolved with a backstop marker. This plan carries the second and third of REL-11's three
verification observations — the close-time re-verification in Task 2, and the operator-facing
post-`phase.complete` protocol whose reachability Task 2 confirms and Task 3 points at.

## A carried assumption this plan cannot discharge

`63-RESEARCH.md` § "Assumptions Log" A3 records that the release workflow's `create-release` job is
expected to run green when actually exercised at `/gsd-complete-milestone`, because it did so at the
v0.8.0 and v0.9.0 real tag pushes. Nothing in a prep-only phase can verify that — a real tag push is
the only thing that exercises it, and this phase performs none. D-14's re-offer inside `63-HANDOFF.md`
is the honest handling: the observation is named, the response is pre-decided as fix-and-rerun rather
than defer, and the risk is stated as scoped entirely to the milestone close rather than to this
phase.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `63-HANDOFF.md` → a future `/gsd-complete-milestone` operator | this document is the only carrier of the publish sequence across the phase boundary; a step omitted here is a step that does not happen, and a step worded as an instruction rather than a record could be executed early |
| `.planning/REQUIREMENTS.md` → `phase.complete`-family tooling → the milestone-close reader | project-state metadata crosses into automated tooling that has mutated it against an explicit decision at five consecutive prior closes |
| this worktree → `origin` and the GitHub API (read-only probes) | observation 2's probes cross a network boundary whose failure mode is silence |
| `63-CLOSEOUT-GUARD.md` PHASE_BASE_SHA → this plan's scoped `typsphinx/` diff | one recorded value decides whether SC#5's central claim is a finding or an artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-18 | Tampering | REL-09's checkbox flipped by `phase.complete`-family tooling after the last plan runs, misrepresenting the release's true state to `/gsd-complete-milestone` | high | mitigate | Task 2 re-verifies the fence at close with the compared values shown and reverts by hand on divergence; the decisive third observation is delegated to the operator through `63-CLOSEOUT-GUARD.md`'s own section, and Task 3 points at that section by name so it is reachable without prior knowledge. |
| T-63-19 | Repudiation | a vacuous SC#5 proof — an empty `typsphinx/` diff produced by a wrong or unreachable anchor, or an empty remote probe produced by an unreachable endpoint | high | mitigate | The scoped diff is paired with a same-anchor widened diff whose expected NON-empty five-file result would expose a bad anchor; `git cat-file -e` confirms the anchor resolves; every remote probe derives a positive control from the same fetch; and a precondition halts the task if the GitHub CLI is unauthenticated or the remote is unreachable. |
| T-63-20 | Elevation of Privilege | a recorded command shape in the handoff being executed as an instruction — a tag push, a pin dispatch, or a release-workflow trigger performed inside the prep-only phase | high | mitigate | The handoff's opening states that nothing in the checklist was performed in this phase, every step is framed as what `/gsd-complete-milestone` will do, the prohibitions block forbids each action explicitly, and the automated gate re-asserts after authoring that no local or remote release tag exists. |
| T-63-21 | Information Disclosure | an operator misreading a workflow paused on the `pypi` Environment approval as a failed workflow and abandoning or re-running the release mid-flight | medium | mitigate | D-15's note is placed ahead of the step that triggers the gate, and the gate is asserted to occur at a lower line number than the tag-push command, so the operator meets the warning first in their own reading order. |
| T-63-22 | Repudiation | the handoff omitting a close-out step that is not a side effect of anything else — most dangerously the second repository's `update-pin.yml` MANUAL dispatch and its own separate tag | high | mitigate | All five SC#5 steps are gated by name in the acceptance criteria, the pin step is specified as TWO separate actions with the not-a-side-effect statement, and a `<human-check>` reads the document cold against SC#5's enumeration to catch an omission a grep would miss. |
| T-63-23 | Repudiation | observation 2 recorded by copying observation 1's values, collapsing the two-observation fence into one | medium | mitigate | The gate requires at least two DISTINCT ISO-8601 Zulu timestamps in the file and at least six `positive control` occurrences, and the verdict section must name wave 1 and wave 3 and list the intervening work explicitly. |
| T-63-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. `63-RESEARCH.md` § "Package Legitimacy Audit" records the phase as installing, adding or upgrading zero packages; this plan touches no dependency declaration and no product-tree file at all. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The phase-wide list is authored once, in `63-01-PLAN.md` § `<artifacts_this_phase_produces>`. This
plan contributes one new file and extends two existing ones:

- `.planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md` — created here
- `.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md` — created by plan 63-02,
  extended here with observation 2 of 2, the scoped/widened `typsphinx/` diff pair, and the
  post-dispatch commit census
- `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md` — created by plan 63-02,
  extended here with the close-time re-verification; its operator-facing third-observation section is
  confirmed present and pointed at from the handoff

No product-tree file at all, and nothing under `typsphinx/`, is created or modified by this plan.

**Reserved name — never produced:** no artifact in this phase may be named `63-VERIFICATION.md`.
That name is owned by `gsd-verifier` and is overwritten wholesale on phase close (D-19).
</artifacts_this_phase_produces>

<verification>
- `63-SC5-INVARIANTS.md` carries both observations with at least two distinct UTC timestamps, four
  probes each, and a positive control on every remote-reachable probe
- the `typsphinx/`-scoped diff from PHASE_BASE_SHA is empty and the same-anchor widened diff is
  exactly the five files this phase touches
- the observation-2 verdict names wave 1 and wave 3 and lists the intervening work
- `63-CLOSEOUT-GUARD.md` carries a close-time re-verification whose compared values are shown, the
  live SHA-256 matches the recorded Baseline, and `.planning/REQUIREMENTS.md` is unmodified with
  REL-09 unchecked and its Traceability row reading Pending
- the guard file's operator-facing post-`phase.complete` section is present and is pointed at from
  the handoff
- `63-HANDOFF.md` opens by stating the milestone publishes, quotes REL-09 verbatim, enumerates all
  five SC#5 steps with the approval gate ahead of the tag push, and carries D-14's four REL-04 items
- no local or remote release tag exists, no release exists, and no plan command performed any
  irreversible action
- `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md` is empty
- no file named `63-VERIFICATION.md` exists under the phase directory
</verification>

<success_criteria>
1. SC#5's second fence observation is recorded at a later timestamp in a later wave, with every
   remote probe carrying a positive control, and the separation is stated explicitly rather than left
   to be inferred from two nearby timestamps.
2. The `typsphinx/`-scoped diff is empty against an anchor proven reachable by a non-empty
   same-anchor widened diff whose file list is exactly this phase's five touched files.
3. The REL-09 checksum fence is re-verified at phase close with compared values shown, and any
   divergence is reverted by hand and reported rather than accepted — ROADMAP SC#3's close-time half.
4. `63-HANDOFF.md` is standalone, opens by stating this milestone publishes, enumerates all five
   close-out steps in the operator's execution order with the `pypi` approval named ahead of the step
   that triggers it, and carries D-14's four REL-04 items — ROADMAP SC#5's handoff half.
5. Every publish step is recorded and none is performed: no tag local or remote, no upload, no
   Release, no pull request, no pin dispatch, no release-workflow trigger.
</success_criteria>

<output>
Create `.planning/phases/63-v0-9-2-release-prep-prep-only/63-04-SUMMARY.md` when done.

Its frontmatter MUST declare `requirements-completed: []` for REL-09. REL-09 is cited by this plan
for coverage only; it closes at `/gsd-complete-milestone`, not here. The v0.9.1 audit found three of
Phase 61's four plans wrongly declaring `[REL-09]`, contradicting the correctly-unmet checkbox — that
is the specific defect this instruction exists to prevent. REL-09's state is reported by reading
`.planning/REQUIREMENTS.md` directly and quoting it, never by inferring it from frontmatter.
</output>