---
phase: 63-v0-9-2-release-prep-prep-only
plan: 05
type: execute
wave: 4
depends_on: ["63-04"]
gap_closure: true
files_modified:
  - CHANGELOG.md
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-GAP-CLOSURE-EVIDENCE.md
autonomous: true
requirements: [REL-09, REL-10]
requirements-completed: []

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC#2 / CR-01 / D-23 Edit 1: no sentence anywhere in the ``## [0.9.2]`` section claims that this release''s runtime changes as a whole are limited to one file. The blanket file-confinement sentence is deleted from the intro paragraph. The negative gate is proven non-vacuous by recording its own count BEFORE the edit (1) and after (0) — a zero-count gate whose pre-state was never measured is indistinguishable from a gate that matches nothing.'
    - 'SC#2 / CR-01 / D-23 Edit 2: the ``### Fixed`` bullet citing IMG-08, IMG-09 and IMG-10 carries a file-scope claim that is TRUE and narrower than the one deleted, and that claim is backed by a measurement RE-RUN in this gap closure and transcribed into the phase evidence: ``git diff --stat e3399825..dd385436 -- typsphinx/`` reporting ``typsphinx/translator.py`` alone, 1 file changed, 23 insertions, over exactly the two commits 8430ca62 and 1adad07f. An unproven file-scope claim is what shipped once already; this one ships with its proof attached.'
    - 'D-23 honesty scoping: the evidence states, side by side, that the milestone-wide ``typsphinx/translator.py`` delta over ``v0.9.0..HEAD`` is 33 insertions while the IMG-08/09/10 range''s is 23, and names the third commit (756b9fad, feat(59-03)) that accounts for the difference — so a reader can see the bullet''s narrower scope is deliberate rather than a second undermeasurement.'
    - 'SC#2 structural set, re-run against the CORRECTED text and never carried forward from the pre-correction run: the extractor exits 0; its stdout is non-empty; its stdout is byte-identical to the ``## [0.9.2]`` section as it sits on disk, proven by an empty ``diff`` with the pre-existing ``## [0.6.5]`` section as the positive control; the extracted body contains zero occurrences of the scratch-block heading text; ``CHANGELOG.md`` carries no versioned heading and no tail-link reference for the never-published version; and the tail block still advances the unreleased compare base to ``v0.9.2...HEAD``.'
    - 'D-20 / adjacency edge: after the mid-paragraph deletion the intro remains exactly ONE paragraph — the sentence before and the sentence after the deleted one are joined by a single space, no double space and no orphaned short line survive, exactly one blank line separates the paragraph from ``### Fixed``, and the ``^## [`` heading count over the whole file is unchanged at 23 so no heading merged, collided or split.'
    - 'REL-10 / empty edge: the extractor''s stdout on the corrected section is non-empty (byte count strictly greater than zero, recorded from ``wc -c`` on the captured file rather than predicted), and the section still carries at least one ``### Fixed`` bullet. ``release.yml``''s ``validate`` job aborts the whole release on an empty section, so emptiness here is a release-blocking state, not a cosmetic one.'
    - 'REL-09 / ordering edge: the edit changes no heading order. ``grep -n ''^## \['' CHANGELOG.md | head -3`` still yields the unreleased placeholder first, then ``## [0.9.2]``, then ``## [0.9.0]`` in that order, and the ``### Fixed`` bullets keep D-03''s specified order (the image blocker first, then PATH-01, then the IMG-04..IMG-07 family, then the MSG family). The extractor selects positionally, so heading order is what decides published content.'
    - 'REL-10 / encoding edge: the byte-identity claim is asserted over BYTES, not characters — ``diff`` plus ``wc -c``, with the recorded size labelled as bytes. The section carries 9 U+2014 em dashes (3 UTF-8 bytes each), so a character count and a byte count differ by 18; recording one as the other would make a future byte-identity comparison against the published GitHub Release body fail for a reason unrelated to content.'
    - 'Constraint 7 — the green tree is still green after the edit, on runs executed in THIS gap closure. The suite is run with the ``docs`` extra explicitly added so ``TestChangelogPageContentCoverage`` and ``TestChangelogIncludeCompilesToPdf`` actually execute rather than skip; a worktree provisioned with ``--extra dev`` alone silently skips exactly the classes that bind CHANGELOG content to the published page. Reported PASSED and SKIPPED counts are recorded as observed, never predicted from a prior run.'
    - 'Constraint 7 / D-21 — both documentation environments are rebuilt from a REMOVED ``docs/_build`` and their warning counts recorded, because an incremental rebuild under-reports warnings and manufactures a false baseline match. Both builders consume the edited prose through ``docs/source/changelog.rst``''s MyST include, so both are in the causal path of this diff.'
    - 'Constraint 8 — the decision NOT to dispatch a fresh CI run is RECORDED with its measurement, not assumed: every job in ``ci.yml`` installs the ``dev`` extra and no job installs the ``docs`` extra, every ``tox`` environment ``ci.yml`` invokes declares ``extras = dev``, and ``tests/test_changelog_page_gate.py`` guards its two content-coverage classes on an importable ``myst_parser`` — so no CI lane reads CHANGELOG content and a dispatch would exercise nothing in this diff. Lint authority stays with the phase''s existing green run.'
    - 'Constraint 5 — ``63-CHANGELOG-EVIDENCE.md`` is EXTENDED, never rewritten: the original transcription of the false claim and of the contradicting ``git diff --stat`` both survive, and a new clearly-headed section states explicitly that the file''s internal contradiction is now resolved and by which commit. The phase''s record of what was inspected when is itself evidence; deleting it would erase the proof that the defect shipped once.'
    - 'Constraint 4 — the correction lands as a NEW follow-up commit touching only ``CHANGELOG.md`` in the product tree. Commit 10d9d95d is not amended, rebased or rewritten; SC#1 is a claim about that commit''s four-file shape and stays true.'
    - 'D-24 — the declination of ``63-REVIEW.md`` IN-01 is recorded visibly in the phase evidence with its reason, so a later reader does not read the absence of a ``tests/test_changelog_page_gate.py`` edit as an oversight. No plan in this closure edits that file.'
  artifacts:
    - CHANGELOG.md
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-GAP-CLOSURE-EVIDENCE.md
  key_links:
    - '``CHANGELOG.md`` ``## [0.9.2]`` section -> ``scripts/extract_changelog_section.py`` stdout -> the future GitHub Release body AND the Read the Docs changelog page: this text publishes byte-identically down both paths. A false claim corrected in the file is only corrected in the published artifacts because the extractor is positional over the file — which is why the extractor is re-run and re-read rather than reasoned about.'
    - 'the phase-62 commit range e3399825..dd385436 -> the IMG-08/09/10 bullet''s scoped file-confinement claim: one recorded ``git diff --stat`` decides whether the replacement sentence is a measured fact or a second unproven assertion. The measurement is re-run in this closure, not copied.'
    - '``docs/source/changelog.rst`` MyST include -> both ``tox`` documentation environments and both content-coverage test classes: the edited prose is parsed by a real ``-b html`` build of the whole docs tree and compiled into a real PDF by ``-b typstpdf``, so a malformed construct surfaces as a build warning or a compile failure rather than as a silently broken published page.'
    - '``tests/test_changelog_page_gate.py`` ``myst_parser`` import guard -> what a green test run actually proves: without the ``docs`` extra the two asserting classes skip and the run reports green while proving nothing about CHANGELOG content. The extra is added explicitly and the SKIPPED count is read, not the exit code.'
  prohibitions:
    - 'MUST NOT trade one false claim for another. Every factual claim added to published release prose in this closure is backed by a command executed in this closure and transcribed into the phase evidence at the time of writing. No claim is carried forward from prior prose, recalled, or inferred. This is the exact defect class being closed and it must not recur one level deeper.'
    - 'MUST NOT weaken the 0.9.0-blocker disclosure or the non-Windows-exclusivity framing while correcting the paragraph. The sentence telling 0.9.0 users to upgrade (D-08) and the statement that the path work is not a Windows-exclusive fix (D-04, ``<specifics>`` item 3) both survive the edit intact. A correction must not become a quiet retraction of a user-protecting disclosure.'
    - 'MUST NOT make the release notes vaguer to dodge checkability. The blanket claim is replaced by a claim that is narrower AND more checkable, never by hedged or unfalsifiable prose. A downstream integrator scoping a security review of the path-handling fixes must be able to derive the true blast radius from the release notes plus the repository, not be left with nothing to check.'
    - 'MUST NOT take any irreversible action — no git tag local or remote, no tag push, no PyPI upload, no GitHub Release, no pull request, no ``update-pin.yml`` dispatch on the translations repository, no Read the Docs re-flip, and no trigger of ``release.yml`` by any route including ``workflow_dispatch``. The prep-only fence is unchanged by the fact that this is a gap closure.'
    - 'MUST NOT rewrite, renumber, supersede or delete plans 63-01 through 63-04 (D-22: this closure is ADDITIVE — new plans numbered from 63-05 onward, scoped by ``--gaps-only`` on the ``gap_closure: true`` flag) or any of their four SUMMARY files, and MUST NOT amend or rebase commit 10d9d95d. Those artifacts describe work already committed and independently re-verified; rewriting them would erase the record that this defect shipped once and was caught.'
    - 'MUST NOT record inherited, recalled or predicted values as measurements. Every number written into the evidence in this closure — the extracted byte count, the test PASSED and SKIPPED counts, the two documentation warning counts, the diff statistics — is captured from a command executed in this closure. The pre-correction byte count in particular must not be reused.'
    - 'MUST NOT touch anything under ``typsphinx/``. This gap is a documentation-content defect; ``CHANGELOG.md`` is the only product-tree file this closure edits.'
    - 'MUST NOT modify ``.planning/REQUIREMENTS.md``. Its REL-09 checkbox stays unchecked, its Traceability row stays Pending, and no SUMMARY in this closure declares REL-09 in ``requirements-completed``. If tooling flips it, the flip is reverted with a checkout and reported — never accepted, never committed.'
    - 'MUST NOT author a file named ``63-VERIFICATION.md``. That name is the verifier''s reserved output for this phase and a plan-authored file there is clobbered wholesale at verify time (D-19).'
    - 'MUST NOT edit ``tests/test_changelog_page_gate.py``. ``63-REVIEW.md`` IN-01 is Info-severity, pre-existing, absent from the verification report''s gaps block, and explicitly declined by the owner (D-24). The declination is recorded rather than acted on.'
---

<objective>
Close the single open gap in Phase 63: `63-VERIFICATION.md`'s SC#2 failure, which is
`63-REVIEW.md` CR-01. The `## [0.9.2]` intro paragraph asserts that this release's runtime
changes are confined to `typsphinx/translator.py`; the milestone's own recorded
`git diff --stat v0.9.0..HEAD -- typsphinx/` shows five files. That text publishes
byte-identically as the GitHub Release body and as the Read the Docs changelog page, where a
reader auditing the path-handling fixes would be misdirected to skip `builder.py`,
`pathfmt.py` and `template_registry.py`.

Apply D-23 in full: delete the blanket claim from the intro, and re-scope a measured, true
version of it into the IMG-08/IMG-09/IMG-10 bullet where it actually holds. Then re-run the
extractor, read its stdout end to end, and re-run SC#2's whole structural set against the
corrected text rather than against the run that missed the defect. Finally re-prove the tree
green on runs executed in this closure, and record the reasoned decision about a fresh CI
dispatch instead of taking it reflexively or skipping it silently.

Purpose: SC#2's bar is "the extracted release body read rather than assumed." The prior
inspection reached the body's shape and stopped there. This closure extends it to the body's
accuracy and leaves behind the measurement that proves it — so the one claim that replaces the
false one ships with its own evidence attached.

Output: a corrected `CHANGELOG.md`, an extended `63-CHANGELOG-EVIDENCE.md` whose internal
contradiction is annotated resolved, and a new `63-GAP-CLOSURE-EVIDENCE.md` carrying the
green-tree re-proof, the CI-dispatch decision and D-24's recorded declination.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-VERIFICATION.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-REVIEW.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution";
ROADMAP milestone constraint 12). It is mandatory and not conditional on parallelism. Before
running ANY command in this plan, provision this worktree's own virtual environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. Task 3 additionally needs the `docs` extra
for the changelog page gate's two content-coverage classes; add it at the invocation
(`uv run --extra dev --extra docs pytest ...`) exactly as plan 63-01 did, rather than
re-provisioning the worktree.

Without the per-worktree sync, `import typsphinx` resolves to the MAIN checkout via the editable
finder and this worktree's edits are invisible to pytest.
</worktree_provisioning>

<!-- planner-discipline-allow: The runtime changes are confined to -->
<!-- planner-discipline-allow: confined to -->
<!-- planner-discipline-allow: with no other file under -->
<!-- planner-discipline-allow: Planned for Future Releases -->
<!-- planner-discipline-allow: 0\.9\.1 -->
<!-- planner-discipline-allow: ^## \[0\.9\.1\] -->
<!-- planner-discipline-allow: ^\[0\.9\.1\]: -->
<!-- planner-discipline-allow: ^- \[x\] \*\*REL-09\*\* -->
<!-- planner-discipline-allow: ^## \[ -->
<!-- planner-discipline-allow: SKIPPED -->
<!-- planner-discipline-allow: confined to `typsphinx/translator.py` -->
<!-- planner-discipline-allow: Post-correction -->
<!-- planner-discipline-allow: resolved -->
<!-- planner-discipline-allow: Byte-for-byte identity -->
<!-- planner-discipline-allow: Milestone-invariant sweep -->
<!-- planner-discipline-allow: The extracted body, verbatim -->
<!-- planner-discipline-allow: e3399825 -->
<!-- planner-discipline-allow: dd385436 -->
<!-- planner-discipline-allow: 8430ca62 -->
<!-- planner-discipline-allow: 1adad07f -->
<!-- planner-discipline-allow: 756b9fad -->
<!-- planner-discipline-allow: bytes -->
<!-- planner-discipline-allow: rm -rf docs/_build -->
<!-- planner-discipline-allow: docs-html -->
<!-- planner-discipline-allow: docs-pdf -->
<!-- planner-discipline-allow: Lint and Format Check -->
<!-- planner-discipline-allow: IN-01 -->
<!-- planner-discipline-allow: COVERAGE.md -->
<!-- planner-discipline-allow: myst_parser -->
<!-- planner-discipline-allow: 0\.9\.0 users should upgrade to this release -->
<!-- planner-discipline-allow: Windows-exclusive fix -->
<!-- The first three literals in the list above are echoed into action bodies because this plan's
     entire purpose is to delete and re-scope that exact sentence — naming it by concept alone
     would leave the executor guessing which sentence. Their negative gates run against
     `CHANGELOG.md` or against a captured extractor output file, never against this PLAN file, so
     echoing them here cannot invalidate any gate. Every literal from `confined to
     `typsphinx/translator.py`` onward appears ONLY in POSITIVE presence or equality checks in this
     plan (`-ge 1`, `-ge 2`, `= 1`, `= 3`, `= 9`, `= 23`) over the evidence and changelog files this
     plan writes — the structure gate reads them as absence checks because they sit in the same
     `&&`-chain as unrelated zero-count assertions. `REL-09`'s one genuine zero-count gate
     (`^- \[x\] \*\*REL-09\*\*`) is asserted over `.planning/REQUIREMENTS.md`, a file no action body
     in this plan writes to. -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — delete the blanket file-confinement claim, re-scope a measured true one into the IMG-08 bullet, and run the extractor to read what will actually publish</name>
  <files>CHANGELOG.md</files>
  <precondition>`git rev-parse --verify e3399825`, `git rev-parse --verify dd385436` and `git rev-parse --verify v0.9.0` all resolve in this worktree. A shallow or partial clone makes every diff measurement in this plan vacuously empty, which would let an unproven replacement claim pass as measured — the exact failure being closed. Halt and report if any of the three does not resolve.</precondition>
  <read_first>
    - `CHANGELOG.md` lines 1 through 75 — the file being edited, in its current state. Read the `## [0.9.2]` heading, the seven-line intro paragraph, all four `### Fixed` bullets and the three `### Verified` bullets. Note the wrap discipline: measure the longest line in the section yourself rather than assuming a width.
    - `CHANGELOG.md` around line 410 — the `## [0.6.5]` section, D-01's named structural model and the source of the file-confinement sentence shape this task re-scopes. Read it to see what a TRUE version of that sentence looks like in a genuinely single-file hotfix.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-REVIEW.md` § "CR-01" in full, including both halves of its **Fix** block — the owner-approved shape this task implements.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-VERIFICATION.md` frontmatter `gaps:` block — the closure contract's two `missing:` items, verbatim.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-01, D-02, D-04, D-05 and D-08, and § `<specifics>` items 2, 3 and 4.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md` §§ "Milestone-invariant sweep (D-06 bullets 1 and 2)", "The extracted body, verbatim" and "Byte-for-byte identity, with its positive control" — the recorded contradiction, and the exact comparison pipeline to reuse.
    - `scripts/extract_changelog_section.py` — its module docstring documents the positional selection algorithm this task's structural assertions depend on.
  </read_first>
  <action>
    Two edits to `CHANGELOG.md`, both inside the `## [0.9.2]` section. Nothing else in the file
    changes and nothing under `typsphinx/` is touched.

    **Before editing, record the negative gate's own pre-state.** Run the whole-file
    newline-normalised count for the phrase "The runtime changes are confined to" and capture the
    result. It must be 1 right now. A zero-count gate whose pre-state was never measured proves
    nothing after the edit — this recorded 1 is what makes the post-edit 0 a finding.

    **Edit 1 — the intro paragraph (currently `CHANGELOG.md:19-25`).** Delete the sentence
    "The runtime changes are confined to `typsphinx/translator.py`, with no other file under
    `typsphinx/` touched." It currently begins on line 23 after "…should upgrade to this release."
    and ends on line 24 before "Zero new". Delete exactly that one sentence. The paragraph's other
    four sentences are unchanged, including the opening "This release curates the Windows-shaped
    path-handling hardening accumulated since 0.9.0 …", the 0.9.0-upgrade disclosure sentence, and
    the closing "Zero new runtime dependencies; the bundled `@preview` version-sync surface is
    untouched." Join the surviving neighbours with a single space, then re-wrap the paragraph so no
    line exceeds the section's existing maximum width, which you measured in `read_first`. Do not
    reflow the four `### Fixed` bullets, the `### Verified` bullets, or any neighbouring paragraph —
    the diff for this edit should touch only the lines of this one paragraph.

    **Measure the replacement claim before writing it.** Run `git diff --stat e3399825..dd385436 --
    typsphinx/` and `git log --oneline e3399825..dd385436 -- typsphinx/`, and capture both outputs.
    Expect one file, `typsphinx/translator.py`, 23 insertions, over exactly two commits: 8430ca62
    and 1adad07f. Also run `git diff --stat v0.9.0..HEAD -- typsphinx/translator.py` and note that
    the milestone-wide figure is larger, because commit 756b9fad also touched that file for a
    different fix family. If the phase-62 range's result is anything other than
    `typsphinx/translator.py` alone, STOP: do not write the replacement sentence, record the actual
    measurement, and report. The whole point of this gap is that a file-scope claim shipped without
    its proof.

    **Edit 2 — the first `### Fixed` bullet, the one citing IMG-08, IMG-09 and IMG-10.** Append one
    sentence immediately after the bullet's existing final sentence, which ends "…so every one of
    those containers compiles." The sentence to add, phrasing adjustable but claim not: "This fix
    is confined to `typsphinx/translator.py`; no other file under `typsphinx/` was touched for it."
    Keep it inside the same bullet, under the bullet's two-space continuation indent, wrapped to
    the same width as the rest of the section. It must NOT land in the intro paragraph — that is
    where the false version lived.

    **Then run the extractor and read its stdout end to end.** Execute
    `uv run python scripts/extract_changelog_section.py 0.9.2`, capture stdout to a file, confirm
    exit 0, and read the captured body from its first line to its last. Do not skim for the two
    edited sentences — the criterion this closure exists to satisfy is that the extracted body is
    read rather than assumed, and reading only the diff is assuming everything else. While reading,
    check every remaining factual claim in the body against something you can run: if any other
    claim is checkable and you cannot check it, record it in the summary as an open question rather
    than letting it pass.

    Then prove the corrected body byte-identical to the section as it sits on disk, reusing the
    comparison pipeline recorded in `63-CHANGELOG-EVIDENCE.md` § "Byte-for-byte identity", and run
    the same pipeline against the pre-existing `## [0.6.5]` section as the positive control — an
    empty `diff` from a broken pipeline is indistinguishable from an empty `diff` from agreement.

    Record every captured value for Task 2 to transcribe; do not predict any of them.

    Commit this edit as its own commit touching only `CHANGELOG.md`. Do not amend, rebase or
    otherwise rewrite commit 10d9d95d — SC#1 is a claim about that commit's four-file shape and it
    stays true.
  </action>
  <verify>
    <automated>[ "$(tr '\n' ' ' &lt; CHANGELOG.md | tr -s ' ' | grep -c 'The runtime changes are confined to')" = 0 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^### Fixed/{exit} f' CHANGELOG.md | grep -c 'confined to')" = 0 ] &amp;&amp; [ "$(awk '/^- \*\*An image not first in its container/{f=1} f&amp;&amp;/^$/{exit} f' CHANGELOG.md | tr '\n' ' ' | tr -s ' ' | grep -c 'confined to `typsphinx/translator.py`')" = 1 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | tr '\n' ' ' | tr -s ' ' | grep -c '0\.9\.0 users should upgrade to this release')" = 1 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | grep -c 'Windows-exclusive fix')" = 1 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | grep -o '—' | wc -l)" = 9 ] &amp;&amp; [ "$(awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | awk 'length&gt;99' | wc -l)" = 0 ] &amp;&amp; [ "$(grep -c '^## \[' CHANGELOG.md)" = 23 ] &amp;&amp; [ "$(grep -c '0\.9\.1' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(grep -c '^## \[0\.9\.1\]' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(grep -c '^\[0\.9\.1\]:' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(git diff --name-only e3399825..dd385436 -- typsphinx/ | tr '\n' ' ')" = 'typsphinx/translator.py ' ] &amp;&amp; uv run python scripts/extract_changelog_section.py 0.9.2 &gt; /tmp/63g-e.md &amp;&amp; [ -s /tmp/63g-e.md ] &amp;&amp; [ "$(grep -c 'Planned for Future Releases' /tmp/63g-e.md)" = 0 ] &amp;&amp; awk '/^## \[0\.9\.2\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | sed -e '/./,$!d' | tac | sed -e '/./,$!d' | tac &gt; /tmp/63g-s.md &amp;&amp; diff -q /tmp/63g-e.md /tmp/63g-s.md &amp;&amp; uv run python scripts/extract_changelog_section.py 0.6.5 &gt; /tmp/63g-e65.md &amp;&amp; [ -s /tmp/63g-e65.md ] &amp;&amp; awk '/^## \[0\.6\.5\]/{f=1;next} f&amp;&amp;/^## \[/{exit} f' CHANGELOG.md | sed -e '/./,$!d' | tac | sed -e '/./,$!d' | tac &gt; /tmp/63g-s65.md &amp;&amp; diff -q /tmp/63g-e65.md /tmp/63g-s65.md &amp;&amp; [ "$(tail -1 CHANGELOG.md)" = '[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.2...HEAD' ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ .planning/REQUIREMENTS.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - the pre-edit count for the phrase "The runtime changes are confined to" over the newline-normalised whole file was captured and equals 1, and the post-edit count equals 0 — both recorded, so the negative gate is proven non-vacuous rather than merely satisfied
    - the intro-paragraph region between the `## [0.9.2]` heading and `### Fixed` contains zero occurrences of the substring `confined to`
    - the `### Fixed` bullet region beginning `- **An image not first in its container` contains exactly one occurrence of `confined to \`typsphinx/translator.py\`` after newline-normalisation and whitespace squeezing
    - `git diff --name-only e3399825..dd385436 -- typsphinx/` yields exactly `typsphinx/translator.py`, and the captured `--stat` reports 1 file changed and 23 insertions over exactly commits 8430ca62 and 1adad07f
    - the `## [0.9.2]` section still contains exactly one occurrence of `0.9.0 users should upgrade to this release` and exactly one of `Windows-exclusive fix` — the disclosure and the non-Windows-exclusivity framing both survived
    - the `## [0.9.2]` section still carries exactly 9 U+2014 em dashes and no line in it exceeds 99 columns
    - `grep -c '^## \[' CHANGELOG.md` is 23, unchanged, and the first three `^## \[` hits are the unreleased placeholder, then `## [0.9.2]`, then `## [0.9.0]`, in that order
    - `grep -c '0\.9\.1' CHANGELOG.md`, `grep -c '^## \[0\.9\.1\]' CHANGELOG.md` and `grep -c '^\[0\.9\.1\]:' CHANGELOG.md` are all 0
    - `uv run python scripts/extract_changelog_section.py 0.9.2` exits 0, its captured stdout is non-empty, and `grep -c 'Planned for Future Releases'` over that captured file is 0
    - `diff` between the captured extractor stdout and the on-disk `## [0.9.2]` section is empty, and the same pipeline over `## [0.6.5]` is also empty — the positive control proving the comparison is not vacuous
    - `tail -1 CHANGELOG.md` is the unreleased compare link with base `v0.9.2...HEAD`
    - the captured extractor stdout was read from first line to last, and any remaining checkable claim that could not be checked is recorded as an open question in the summary
    - `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md` is empty, and the commit created by this task touches `CHANGELOG.md` and nothing else in the product tree
    - commit 10d9d95d is unchanged — `git show --name-only --format= 10d9d95d` still lists exactly `CHANGELOG.md`, `README.md`, `pyproject.toml`, `uv.lock`
  </acceptance_criteria>
  <done>The `## [0.9.2]` section carries no blanket file-confinement claim; the IMG-08/IMG-09/IMG-10 bullet carries a narrower one that a re-run `git diff --stat` proves true; and the extractor's stdout — the exact bytes that will publish as the GitHub Release body and the Read the Docs changelog page — has been produced, read end to end, and proven byte-identical to the section on disk with a positive control.</done>
  <reversibility rating="reversible">A prose edit to one Markdown file plus read-only git and extractor invocations. Nothing is written to any remote, no tag exists, no workflow is dispatched, and the commit is a plain follow-up that can be reverted with a single revert.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Extend `63-CHANGELOG-EVIDENCE.md` with the post-correction SC#2 re-run and annotate its own internal contradiction as resolved</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md` in full — the file being extended. Read especially § "The extractor, run and read (REL-10, D-20)", § "Milestone-invariant sweep (D-06 bullets 1 and 2)" (which transcribes the contradicting five-file `git diff --stat`), § "The extracted body, verbatim" (which transcribes the false sentence), and § "Byte-for-byte identity, with its positive control" (whose recorded 4087-byte figure is now stale).
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-VERIFICATION.md` § "Gaps Summary" and the frontmatter `gaps:` block — the contract this section closes, quoted rather than paraphrased.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-19 and D-20.
    - `CHANGELOG.md` in its post-Task-1 state, and the captured extractor output from Task 1.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-01-SUMMARY.md` — the record of how the original evidence section was produced, so the new section's shape matches rather than diverges.
  </read_first>
  <action>
    APPEND a new, clearly-headed top-level section to `63-CHANGELOG-EVIDENCE.md`. Do not delete,
    rewrite or edit any existing section. The original transcription of the false sentence and the
    original contradicting five-file diff both stay exactly where they are: the phase's record of
    what was inspected when is itself the evidence that the defect shipped once and was caught.

    Head the new section so a reader lands on it knowing it post-dates the correction — for example
    `## Post-correction re-run (gap closure, SC#2)` — and open it with a fresh UTC timestamp and the
    SHA of Task 1's commit.

    **State the contradiction and its resolution explicitly.** In the section's first paragraph,
    name the two places in this same file that contradicted each other: the sweep section's
    five-file `git diff --stat v0.9.0..HEAD -- typsphinx/`, and the verbatim body's blanket
    file-confinement sentence. State that the sentence has been deleted from the intro, that a
    narrower measured claim now sits in the IMG-08/IMG-09/IMG-10 bullet, and that the contradiction
    is therefore RESOLVED — naming the commit that resolved it. A later reader must not have to
    reconstruct this from two distant sections.

    **Transcribe the replacement claim's own proof.** Record, as commands with their literal
    output, the phase-62 range measurement Task 1 captured: the scoped `--stat`, the two-commit
    log, and each commit's own single-file stat. Then record the milestone-wide
    `typsphinx/translator.py` figure alongside it and name commit 756b9fad as the reason the two
    differ, so the bullet's narrower scope is visibly deliberate rather than a second
    undermeasurement.

    **Re-record SC#2's structural set against the corrected text.** Every value comes from Task 1's
    captured run; none is copied from the pre-correction section. Record: the extractor's exit
    status; its stdout size from `wc -c`, labelled explicitly as BYTES (the section carries 9
    U+2014 em dashes at three bytes each, so a character count would differ by 18 and would break a
    future byte-identity comparison against the published Release body); D-20's three named greps;
    the byte-identity `diff` with its `## [0.6.5]` positive control and both sizes; the whole-file
    heading count; the absence of any heading or tail link for the never-published version; and the
    tail block's unreleased compare base.

    **Transcribe the corrected body verbatim.** Reproduce the extractor's full stdout in a fenced
    block, exactly as the original section did for the pre-correction body. This is the artifact
    `/gsd-complete-milestone` will publish byte-identically; a reader must be able to compare the
    published Release body against this transcription without re-running anything.

    **Record the adjacency, ordering and encoding observations** the gap's own edge probe asks for:
    that the intro remains one paragraph separated from `### Fixed` by exactly one blank line with
    no double space at the join and no orphaned line; that the three leading `^## [` headings are
    unchanged in order; and that the em-dash count in the section is unchanged at 9.

    Close by quoting `63-VERIFICATION.md`'s two `missing:` items verbatim and stating, item by
    item, which recorded measurement above discharges each — the first by the two edits, the second
    by this re-run. Do not assert the gap closed; show the discharge and let a reader re-derive it.
  </action>
  <verify>
    <automated>E=.planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md &amp;&amp; [ "$(grep -c 'Post-correction' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -ci 'resolved' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -c 'Milestone-invariant sweep' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -c 'Byte-for-byte identity' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -c 'The extracted body, verbatim' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -c 'e3399825' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -c 'dd385436' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -c '8430ca62' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -c '1adad07f' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -c '756b9fad' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -ci 'bytes' "$E")" -ge 1 ] &amp;&amp; [ "$(grep -c '0\.6\.5' "$E")" -ge 2 ] &amp;&amp; [ "$(grep -oE '20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' "$E" | LC_ALL=C sort -u | wc -l)" -ge 1 ] &amp;&amp; NEW=$(uv run python scripts/extract_changelog_section.py 0.9.2 | wc -c) &amp;&amp; [ "$(grep -c "$NEW" "$E")" -ge 1 ] &amp;&amp; [ "$(git diff --name-only HEAD~1 -- .planning/phases/63-v0-9-2-release-prep-prep-only/63-01-PLAN.md .planning/phases/63-v0-9-2-release-prep-prep-only/63-02-PLAN.md .planning/phases/63-v0-9-2-release-prep-prep-only/63-03-PLAN.md .planning/phases/63-v0-9-2-release-prep-prep-only/63-04-PLAN.md | wc -l)" = 0 ] &amp;&amp; [ ! -e .planning/phases/63-v0-9-2-release-prep-prep-only/63-VERIFICATION-GAP.md ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ .planning/REQUIREMENTS.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `63-CHANGELOG-EVIDENCE.md` gains a new top-level section whose heading marks it as post-correction, carrying a fresh ISO-8601 Zulu timestamp and Task 1's commit SHA
    - all four pre-existing sections named in `read_first` still exist in the file byte-for-byte — nothing was deleted or rewritten, only appended
    - the new section names both halves of the file's internal contradiction explicitly and states it RESOLVED, naming the resolving commit
    - the phase-62 range proof is transcribed as commands with literal output: the scoped `--stat`, the two-commit log listing 8430ca62 and 1adad07f, and each commit's own single-file stat
    - the milestone-wide `typsphinx/translator.py` figure is recorded alongside the phase-62 figure and commit 756b9fad is named as the reason they differ
    - the recorded extractor size is the value produced by re-running the extractor against the corrected file at verification time — not the pre-correction figure — and is labelled in BYTES, with the 9 em dashes noted as why bytes and characters differ
    - D-20's three greps, the byte-identity `diff` with its `## [0.6.5]` positive control and both recorded sizes, the `^## \[` heading count, the two never-published-version absence greps, and the tail-block compare base are all recorded as commands with literal output
    - the corrected extractor stdout is transcribed verbatim in a fenced block
    - the adjacency, ordering and encoding observations are recorded: one paragraph with one blank line before `### Fixed`, no double space at the sentence join, the three leading `^## [` headings unchanged in order, and 9 em dashes
    - `63-VERIFICATION.md`'s two `missing:` items are quoted verbatim, each paired with the recorded measurement that discharges it
    - `63-01-PLAN.md` through `63-04-PLAN.md` are untouched by this commit, and no file named `63-VERIFICATION.md` was authored
    - `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md` is empty
  </acceptance_criteria>
  <done>The phase's changelog evidence file records the post-correction extractor run in full, its own internal contradiction is annotated resolved with the resolving commit named, the replacement claim's proof is transcribed beside the milestone-wide figure it is deliberately narrower than, and both of the verification report's `missing:` items are shown discharged by recorded measurements rather than asserted closed.</done>
  <reversibility rating="reversible">An append-only edit to a planning document. No product-tree file and no remote is affected.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Re-prove the tree green on runs executed in this closure, and record the reasoned CI-dispatch decision and D-24's declination in a new `63-GAP-CLOSURE-EVIDENCE.md`</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-GAP-CLOSURE-EVIDENCE.md</files>
  <precondition>The worktree was provisioned with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and `uv run python -c "import typsphinx, sys; print(typsphinx.__file__)"` prints a path inside THIS worktree, not the main checkout. The main checkout's editable finder resolves `import typsphinx` to the main tree, which would make every test result in this task a measurement of the wrong tree. Halt and report if the printed path is outside this worktree.</precondition>
  <read_first>
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-GREEN-TREE-EVIDENCE.md` in full — the baseline this task re-measures against, including the recorded full-suite counts, the `black`/`mypy` results, the two documentation warning counts taken from a removed build directory, and its § "Division of authority" statement about local `ruff`.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CI-EVIDENCE.md` — the dispatched run this task decides whether to supersede, including its run id, head SHA and the `Lint and Format Check` job's transcribed conclusion.
    - `tests/test_changelog_page_gate.py` in full — read the module docstring, the `myst_parser` and `typst` import guards, the `RELEASE_VERSIONS` tuple, and all three classes. Determine from the code itself, not from prose, exactly what each class asserts about CHANGELOG content and under which extras it runs.
    - `.github/workflows/ci.yml` in full and `tox.ini` — read which extra each job and each tox environment installs, so the dispatch decision rests on a measurement of the workflow rather than on recollection.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-REVIEW.md` § "IN-01" — the finding D-24 declines.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-11, D-18 and D-21, and § `<amendments>` item 1, and § `<code_context>` § "Measured hazard the planner must carry".
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/COVERAGE.md` — the existing external-API declaration, to confirm rather than rewrite.
  </read_first>
  <action>
    Create `.planning/phases/63-v0-9-2-release-prep-prep-only/63-GAP-CLOSURE-EVIDENCE.md`. Record
    every result as a command with its literal output at a fresh UTC timestamp. Do not predict any
    number; every figure below comes from a run executed in this closure.

    **The test suite, run with the documentation extra.** Run the full suite with the `docs` extra
    added explicitly at the invocation. Reasoning to record in the file: `tests/test_changelog_page_gate.py`
    guards `TestChangelogPageContentCoverage` and `TestChangelogIncludeCompilesToPdf` on an
    importable `myst_parser`, which lives in the `docs` extra only, so a worktree provisioned with
    the `dev` extra alone reports green while skipping exactly the two classes that bind CHANGELOG
    content to the published page and to a real compiled PDF. Record the reported passed and
    skipped counts as observed and state plainly that they differ from `63-GREEN-TREE-EVIDENCE.md`'s
    recorded counts because the extras differ — do not present the two as if they were the same
    measurement. If any test fails, stop and report; do not adjust a test to accommodate the edit.

    Record why the full suite was run rather than a targeted subset: it subsumes both the version-sync
    guard trio and every test file that reads `CHANGELOG.md`, so it removes the need to trust a
    grep-derived bound on what could observe the diff. Also record `grep -rln 'CHANGELOG' tests/`
    with its literal output, so a reader can see which files that bound would have contained.

    **Both documentation builds, from a removed build directory (D-21).** Run `rm -rf docs/_build`
    and then `tox -e docs-html`; run `rm -rf docs/_build` again and then `tox -e docs-pdf`. Record
    each build's exit status and its reported warning count, and compare each against
    `63-GREEN-TREE-EVIDENCE.md`'s recorded baseline. Record why both were run: `docs/source/changelog.rst`
    includes `CHANGELOG.md` through the MyST parser, so both builders consume the edited prose, and
    the two baseline warning counts are numbers this phase's SC#4 evidence asserts. An incremental
    rebuild under-reports warnings and manufactures a false baseline match — that is why the build
    directory is removed each time, and the removal is recorded as a command, not assumed.

    If either count differs from its baseline, that is a finding: record the differing warning lines
    verbatim and report it rather than restating the baseline.

    **The CI-dispatch decision, measured rather than assumed.** Record, as commands with literal
    output, which extra each `ci.yml` job installs and which extra each `tox` environment `ci.yml`
    invokes declares. Then state the decision and its consequence: because no CI lane installs the
    documentation extra, the two content-coverage classes skip in every lane, so no `ci.yml` job
    reads CHANGELOG content and a fresh dispatch would exercise nothing in this diff at a cost of
    twelve jobs. Record that lint authority therefore remains with the phase's existing green run —
    naming its id and its `Lint and Format Check` job conclusion as already transcribed in
    `63-CI-EVIDENCE.md` — and that `black --check .` and `ruff check .`, the two commands
    `tox -e lint` runs, do not read Markdown, so a prose-only diff cannot change their verdict.
    Never take `ruff`'s verdict from this machine, where it is an unrunnable generic-linux ELF, and
    never drill for a step name that exists only in the release workflow. `release.yml` is not
    triggered for any reason.

    Also record explicitly that this closure adds a commit outside `.planning/` after the recorded
    CI dispatch — `63-SC5-INVARIANTS.md` § "Commits after the CI dispatch" states that all
    post-dispatch commits were confined to `.planning/`, and that statement is superseded by this
    closure. State that the superseding is deliberate and reasoned rather than an omission, and note
    that plan 63-06 records the correction in that file itself.

    **D-24 — the declined finding, recorded visibly.** Write a short section naming `63-REVIEW.md`
    IN-01, quoting the finding, and recording that it is Info-severity, pre-existing, absent from
    `63-VERIFICATION.md`'s gaps block, and declined by the project owner for this closure. State
    that no plan in this closure edits `tests/test_changelog_page_gate.py`, so a later reader does
    not read the absence of that edit as an oversight.

    **Confirm the existing external-API declaration rather than rewriting it.** Record that
    `COVERAGE.md` already carries a reasoned no-external-API-integration declaration covering this
    phase's file set, that this closure's scope is a strict subset of that set — one Markdown file
    plus planning documents — and that the declaration therefore still holds. Do not author a
    capability matrix for an API this closure does not touch.
  </action>
  <verify>
    <automated>G=.planning/phases/63-v0-9-2-release-prep-prep-only/63-GAP-CLOSURE-EVIDENCE.md &amp;&amp; [ -f "$G" ] &amp;&amp; [ "$(grep -oE '20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' "$G" | LC_ALL=C sort -u | wc -l)" -ge 1 ] &amp;&amp; [ "$(grep -c 'extra docs' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'SKIPPED\|skipped' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'rm -rf docs/_build' "$G")" -ge 2 ] &amp;&amp; [ "$(grep -c 'docs-html' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'docs-pdf' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'Lint and Format Check' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'Run linters' "$G")" = 0 ] &amp;&amp; [ "$(grep -c '33309565005' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'IN-01' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'COVERAGE.md' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'myst_parser' "$G")" -ge 1 ] &amp;&amp; [ "$(git status --porcelain tests/test_changelog_page_gate.py | wc -l)" = 0 ] &amp;&amp; [ "$(git diff --name-only v0.9.0..HEAD -- tests/test_changelog_page_gate.py | wc -l)" = 1 ] &amp;&amp; uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py tests/test_changelog_extraction.py tests/test_extension.py::test_version_matches_pyproject_toml tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q &amp;&amp; [ -z "$(git status --porcelain typsphinx/ .planning/REQUIREMENTS.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `63-GAP-CLOSURE-EVIDENCE.md` exists, carries a fresh ISO-8601 Zulu timestamp, and records every result as a command with its literal output
    - the full suite was invoked with the documentation extra added explicitly, and its reported passed and skipped counts are transcribed as observed; the file states that these counts differ from `63-GREEN-TREE-EVIDENCE.md`'s because the extras differ, and does not present them as the same measurement
    - `grep -rln 'CHANGELOG' tests/` is recorded with its literal output alongside the stated reason the full suite was run rather than that subset
    - `rm -rf docs/_build` appears at least twice, each immediately preceding one of the two documentation builds; both builds' exit statuses and warning counts are recorded and compared against the recorded baseline, with any difference reported as a finding rather than restated as the baseline
    - the per-job and per-tox-environment extra measurements over `.github/workflows/ci.yml` and `tox.ini` are recorded with literal output, and the no-dispatch decision is stated as a consequence of them
    - the file names the existing CI run id and its `Lint and Format Check` job as the standing lint authority, contains zero occurrences of the release-workflow-only step name, and records that no workflow was dispatched in this closure
    - the file records explicitly that this closure lands a commit outside `.planning/` after the recorded dispatch, that this supersedes `63-SC5-INVARIANTS.md`'s post-dispatch statement, and that plan 63-06 corrects it in that file
    - a section records `63-REVIEW.md` IN-01 as Info-severity, pre-existing, absent from the verification gaps block, and declined by the owner, and states that no plan in this closure edits `tests/test_changelog_page_gate.py`
    - `git status --porcelain tests/test_changelog_page_gate.py` is empty and that file's milestone-wide change count is unchanged at one commit's worth — D-24 held
    - the existing `COVERAGE.md` declaration is confirmed as still covering this closure's strictly smaller scope, and no capability matrix was fabricated
    - the targeted re-run of the changelog gate, the extractor contract tests and the version-sync guard trio passes under the documentation extra
    - `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md` is empty
  </acceptance_criteria>
  <done>The corrected tree is proven green on runs executed in this closure — with the documentation extra present so the changelog content gates actually execute — both documentation builders are re-measured from a removed build directory against their recorded baselines, the decision not to dispatch a fresh CI run rests on a transcribed measurement of the workflow rather than on recollection, and the one declined review finding is recorded visibly rather than silently absent.</done>
  <reversibility rating="reversible">Test and build invocations plus one new planning document. No remote is written to and no workflow is dispatched.</reversibility>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

| Artifact | Created or modified | What changes |
|---|---|---|
| `CHANGELOG.md` | modified | The `## [0.9.2]` intro paragraph loses the blanket file-confinement sentence (D-23 Edit 1); the first `### Fixed` bullet, citing IMG-08/IMG-09/IMG-10, gains a scoped, measured file-confinement sentence (D-23 Edit 2). No other section of the file changes. |
| `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md` | modified (append-only) | A new post-correction section records the re-run extractor output, the replacement claim's proof, SC#2's full structural set against the corrected text, the verbatim corrected body, and an explicit annotation that the file's own internal contradiction is resolved. No existing section is edited. |
| `.planning/phases/63-v0-9-2-release-prep-prep-only/63-GAP-CLOSURE-EVIDENCE.md` | created | The green-tree re-proof under the documentation extra, both documentation builds from a removed build directory, the measured CI-dispatch decision, the record that a non-`.planning/` commit now post-dates the CI dispatch, D-24's declination of IN-01, and confirmation of the existing external-API declaration. |

No symbol, module, function or configuration value is created or modified anywhere under
`typsphinx/`. No test file is created or modified. No requirement checkbox is moved.
</artifacts_produced>

<flagged_assumptions>
## Spec-less edge-coverage accounting (6 probe-surfaced items, 0 silent drops)

This phase has no `63-SPEC.md`, so both `EDGE_ABSENT` and `PROHIB_ABSENT` hold and the
deterministic fallback probe ran over REL-09, REL-10 and REL-11's literal `REQUIREMENTS.md` text,
returning six items, all `unresolved` with `verification: null`. The probes were generated against
the whole requirement text as written for the full release, not against this narrow gap, so each
row is re-read against the gap's actual scope before being authored. This is the phase-wide
accounting for the gap closure; plan 63-06 references this section rather than repeating it.

| Requirement | Category | Resolution | Verification | Where authored |
|---|---|---|---|---|
| REL-09 | adjacency | resolved | explicit | `must_haves.truths` of this plan — the mid-paragraph deletion's join: one paragraph, one blank line before `### Fixed`, no double space, no orphaned line, heading count unchanged at 23 |
| REL-09 | empty | resolved | explicit | `must_haves.truths` of this plan — the extractor's stdout on the corrected section is non-empty, measured by `wc -c` rather than predicted, because the release workflow's validate job aborts on an empty section |
| REL-09 | ordering | resolved | explicit | `must_haves.truths` of this plan — the three leading `^## [` headings keep their order and the `### Fixed` bullets keep D-03's specified order, because the extractor selects positionally |
| REL-10 | empty | resolved | explicit | `must_haves.truths` of this plan — zero occurrences of the scratch-block heading text in the extracted body, re-measured against the corrected text |
| REL-10 | encoding | resolved | explicit | `must_haves.truths` of this plan — byte-identity asserted over bytes with `diff` and `wc -c`, with the section's 9 U+2014 em dashes named as the 18-byte reason a character count would diverge |
| REL-11 | unclassified | **unresolved** | null | **flagged assumption — surfaced below, not authored into `truths`, not dismissed, not backstopped** |

**Why REL-11's row stays `unresolved`.** The probe classified it `unclassified`, and the fallback
protocol is explicit that an `unclassified` row is never auto-resolved with a backstop marker and
never auto-dismissed. REL-11 is not a behavioural requirement with an input domain: it asserts that
a checkbox in a planning document does not move, verified by comparing a SHA-256 across timestamped
observations. There is no adjacency, emptiness, ordering or encoding edge to author for it that
would not be manufactured. It is surfaced here so no downstream reader mistakes the absence for an
oversight; its actual verification for this closure lives in plan 63-06's fence re-verification
task, and the decisive post-tooling observation remains delegated to the operator through
`63-CLOSEOUT-GUARD.md`.

**Accounting:** 6 surfaced = 5 authored into `must_haves.truths` + 1 surfaced as a flagged
assumption + 0 dismissed + 0 dropped.

## Prohibition recall accounting (descriptor-less, never auto-dismissed)

Stage 1 over-produced adversarially against REL-09, REL-10 and REL-11's text read through the lens
of this gap. Stage 2 dropped the routine-engineering candidates — must not leave temporary files
behind, must not break the Markdown, must not reflow unrelated paragraphs, must not hand-edit the
lockfile — as edge-probe or code-review territory, or as mechanical instructions already carried in
the action bodies.

**Canon-referral breadcrumb (dropped by rule, ADR-550 D6):** one surviving candidate — "the
corrected release notes must not become a vector for supply-chain misdirection" — reduces to canon
release-integrity practice already owned by the byte-identity check the handoff records and by the
package-legitimacy gate, which this closure measures as owing nothing (zero packages installed,
added or upgraded). Not minted.

Ten bespoke values, safety, honesty and scope-integrity prohibitions survived and are authored
**descriptor-less** (no `check_*` scalar, so each disposes flagged-unverified) across this plan and
plan 63-06:

- **P1 (honesty):** no false claim traded for another — every added factual claim is backed by a
  command run in this closure. Authored in both plans.
- **P2 (user safety):** the 0.9.0-blocker disclosure and the non-Windows-exclusivity framing survive
  the correction intact. Authored in 63-05.
- **P3 (transparency):** the replacement claim is narrower and MORE checkable, never hedged or
  unfalsifiable. Authored in 63-05.
- **P4 (safety):** no irreversible action of any kind. Authored in both plans.
- **P5 (record integrity):** plans 63-01 through 63-04, their SUMMARY files, and commit 10d9d95d are
  not rewritten, renumbered, superseded, deleted, amended or rebased. Authored in both plans.
- **P6 (evidence integrity):** no inherited, recalled or predicted value recorded as a measurement.
  Authored in both plans.
- **P7 (scope integrity):** nothing under `typsphinx/` is touched. Authored in both plans.
- **P8 (honesty):** `.planning/REQUIREMENTS.md` is not modified and REL-09 is not recorded as
  satisfied anywhere, including in prose. Authored in both plans.
- **P9 (record integrity):** no file named `63-VERIFICATION.md` is authored. Authored in both plans.
- **P10 (scope integrity):** `tests/test_changelog_page_gate.py` is not edited; the declined finding
  is recorded rather than acted on. Authored in 63-05.

**Accounting:** 10 kept = 10 authored into `must_haves.prohibitions` + 0 dismissed + 0 dropped
(+ 1 canon-referral breadcrumb, dropped by rule).

## Reversibility accounting

Every decision this closure implements is **reversible**: the correction is a prose edit to one
Markdown file in a repository with no `v0.9.2` tag, no PyPI upload and no GitHub Release, so the
text has not been published anywhere and reverting the commit fully undoes it. There is no `one-way`
decision here and therefore no `checkpoint:decision` — manufacturing a gate for a revertible
Markdown edit would be dishonest about the actual stakes. The genuinely one-way step is the tag push
at `/gsd-complete-milestone`, which is recorded in `63-HANDOFF.md` and performed by neither plan.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` `## [0.9.2]` → `scripts/extract_changelog_section.py` → the future GitHub Release body | curated prose crosses from a repository file into a body `release.yml`'s `create-release` job publishes verbatim at `/gsd-complete-milestone`; selection is positional, so physical layout decides published content |
| `CHANGELOG.md` → `docs/source/changelog.rst` MyST include → Read the Docs | the same prose crosses into a publicly reachable page on every documentation build, in both the `en` and `ja` projects |
| published release notes → a downstream integrator's own security review scope | a blast-radius claim in the release notes is what a security-conscious consumer uses to decide which files to diff; a false narrower claim removes files from that review |
| this worktree → `origin` and the GitHub API | read-only probes only; no write, no dispatch, no tag |
| `.planning/REQUIREMENTS.md` → automated phase-completion tooling | a file this closure must leave byte-identical, against tooling that has mutated it at five consecutive prior release-prep closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63G-01 | Tampering | the published release-note blast-radius claim (`CHANGELOG.md:19-25` → the Release body and the RTD changelog page) | high | mitigate | Task 1 deletes the blanket claim and replaces it with a claim scoped to the one fix it holds for, proven by a `git diff --stat` re-run in this closure over `e3399825..dd385436`; Task 2 transcribes that proof beside the milestone-wide figure so the narrower scope is visibly deliberate. This is the defect being closed. |
| T-63G-02 | Elevation of Privilege | irreversible release actions — tag, PyPI upload, GitHub Release, pull request, translations-repository pin dispatch, `release.yml` trigger | high | mitigate | The prohibitions block forbids each by name; no command in any task writes to a remote or dispatches a workflow; plan 63-06 takes a fresh probe pair with positive controls after this plan's commits land, so the fence is observed against the new tip rather than inherited. |
| T-63G-03 | Tampering | `.planning/REQUIREMENTS.md`'s REL-09 checkbox and Traceability row | high | mitigate | No task edits the file; every task gates on `git status --porcelain .planning/REQUIREMENTS.md` being empty; plan 63-06 re-verifies the recorded SHA-256 fence after these commits land and reverts any divergence with a checkout rather than accepting it. |
| T-63G-04 | Repudiation | the phase's own evidence trail — the record of what was inspected, when, and what it missed | medium | mitigate | `63-CHANGELOG-EVIDENCE.md` is appended to, never rewritten; the original transcription of the false claim and of the contradicting diff both survive; the new section names the contradiction and the commit that resolved it, so the history of the miss is preserved rather than erased. |
| T-63G-05 | Information Disclosure | the 0.9.0-blocker disclosure and the non-Windows-exclusivity framing, which a careless correction could delete along with the false sentence | medium | mitigate | Task 1's gates positively assert that the upgrade sentence and the non-Windows-exclusivity statement each still appear exactly once in the section; a correction that silently retracted a user-protecting disclosure would fail the task. |
| T-63G-06 | Spoofing | an extracted body that looks correct but is not the bytes that publish — a stale or partially-updated extractor output | medium | mitigate | The extractor is re-run against the corrected file and its stdout proven byte-identical to the on-disk section by `diff`, with the pre-existing `## [0.6.5]` section as the positive control so an empty `diff` from a broken pipeline cannot masquerade as agreement; the recorded size is labelled in bytes. |
| T-63G-07 | Repudiation | a green test run recorded as proof of something it never executed — the changelog content gates skip without the documentation extra | medium | mitigate | Task 3 adds the extra explicitly at the invocation and records the reported skipped count alongside the passed count, and states that the counts differ from the phase's earlier baseline because the extras differ rather than presenting them as one measurement. |
| T-63G-SC | Tampering | npm/pip/cargo installs | low | accept | This closure installs, adds and upgrades zero packages — it edits one Markdown file and writes planning documents. `63-RESEARCH.md` § "Package Legitimacy Audit" already measured the phase as owing no package-legitimacy checkpoint, and this closure's scope is a strict subset of that. No install task exists in this plan. |
</threat_model>

<verification>
The plan is complete when, on the corrected tree:

- the newline-normalised whole-file count for the deleted sentence's distinguishing phrase is 0,
  and the pre-edit count of 1 is recorded so the gate is provably non-vacuous
- the intro region carries zero occurrences of `confined to` and the IMG-08 bullet region carries
  exactly one occurrence scoped to `typsphinx/translator.py`
- the extractor exits 0, its stdout is non-empty, byte-identical to the section on disk, free of
  the scratch-block heading text, and its size is recorded in bytes from a run in this closure
- `## [0.6.5]`'s identical comparison is also empty, proving the pipeline is not vacuous
- the section still carries the 0.9.0 upgrade disclosure and the non-Windows-exclusivity statement,
  9 em dashes, no line over 99 columns, and no reference to the never-published version
- the full suite passes with the documentation extra present, with the skipped count recorded
- both documentation builds run from a removed build directory and their warning counts are
  compared against the recorded baseline
- the CI-dispatch decision is recorded with the workflow measurement it rests on, and no workflow
  was dispatched
- `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md` is empty and plans 63-01 through
  63-04 and their SUMMARY files are unmodified
- no file named `63-VERIFICATION.md` was authored anywhere in the phase directory
</verification>

<success_criteria>
`63-VERIFICATION.md`'s SC#2 gap is closed on both of its `missing:` items: the false
file-confinement sentence is gone from the `## [0.9.2]` intro and a measured, narrower version sits
in the bullet it is true for; and the extractor has been re-run with SC#2's byte-identity and
structural checks re-verified against the corrected text, with every figure captured in this
closure rather than carried forward. The tree is proven green on runs executed here, both
documentation builders are re-measured from a clean build, and the decisions not to dispatch CI and
not to act on IN-01 are recorded with their reasoning rather than taken silently.
</success_criteria>

<output>
Create `.planning/phases/63-v0-9-2-release-prep-prep-only/63-05-SUMMARY.md` when done, with
frontmatter declaring `requirements-completed: []`.
</output>
