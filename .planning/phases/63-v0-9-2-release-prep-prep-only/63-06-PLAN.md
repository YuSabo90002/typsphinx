---
phase: 63-v0-9-2-release-prep-prep-only
plan: 06
type: execute
wave: 2
depends_on: ["63-05"]
gap_closure: true
files_modified:
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md
  - .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md
autonomous: true
requirements: [REL-09, REL-11]
requirements-completed: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Constraint 6 / SC#5: a THIRD fence observation is taken after the gap-closure correction commit lands, at a fresh timestamp in a later wave than observations 1 and 2, with all four probes re-run and each remote probe carrying its own positive control. The two prior observations both pre-date the correction commit, so neither says anything about the tree that now exists; SC#5''s zero-irreversible-action claim is re-established against the current tip rather than inherited.'
    - 'SC#5 positive controls: the local tag probe pairs an empty release-tag list with a NON-empty 0.9.0 tag list; the remote tag probe derives both counts from ONE unfiltered fetch so the control and the assertion cannot disagree about reachability; the publish probe derives its control and its assertion from ONE release listing. A probe recorded without a control that would fail if the probe were vacuous is not evidence.'
    - 'SC#5 scoped/widened diff pair, re-taken from the recorded PHASE_BASE_SHA against the post-correction tip: the ``typsphinx/``-scoped diff is empty, and the same-anchor widened diff is non-empty and lists exactly the five files this phase touches. The correction adds no sixth file, because ``CHANGELOG.md`` was already in the set.'
    - 'Evidence-integrity correction: ``63-SC5-INVARIANTS.md`` § "Commits after the CI dispatch" currently states that every commit landed after the recorded CI run is confined to ``.planning/``. The gap closure lands a ``CHANGELOG.md`` commit outside ``.planning/``, so that statement is superseded. The supersession is recorded IN THAT FILE, with a cross-reference to the measured reasoning for why no re-dispatch is owed — a statement left standing while it is false is the same defect class this whole closure exists to fix.'
    - 'Constraint 3 / REL-11 / D-16: the closeout guard triad is re-run AFTER the gap-closure commits land and compared value-for-value against the recorded Baseline — the SHA-256 digest, the line count, a name-only git diff over ``.planning/REQUIREMENTS.md`` expecting no output, and the REL-09 grep expecting byte-identical hits. New commits change HEAD; they must not change any of those four. A divergence is reverted with a checkout and reported, never accepted and never committed.'
    - 'REL-09''s checkbox is read directly out of ``.planning/REQUIREMENTS.md`` as unchecked and its Traceability row as Pending, never inferred from any plan''s frontmatter. Both gap-closure plans declare ``requirements-completed: []`` for it. REL-09 closes at ``/gsd-complete-milestone``, not here, and this closure changes nothing about that.'
    - 'D-16: the decisive post-tooling observation is still owed and is still not a plan''s to take. It runs after ``phase.complete``-family tooling, outside any plan''s reach, and it is the one that has historically caught the flip at five consecutive prior release-prep closes. ``63-CLOSEOUT-GUARD.md`` § "For the operator running phase.complete" stays present and reachable, and ``63-HANDOFF.md`` keeps pointing at it by name.'
    - 'Constraint 9: ``63-HANDOFF.md`` stays accurate. It commits ``/gsd-complete-milestone`` to publishing a GitHub Release body byte-identical to the extractor''s stdout, and it transcribes that stdout''s size — a figure the correction changes. The recorded size is replaced with the value measured on the corrected text, the SC#2 report names the correction and the plan that made it, the fence-observation citation names all three observations, and the prohibitions citation names the gap-closure plans as well.'
    - 'SC#5 standalone property preserved: after the update ``63-HANDOFF.md`` still enumerates every step ``/gsd-complete-milestone`` executes — the tag push, the ``pypi`` Environment''s manual approval named BEFORE the step that triggers it, the Release body byte-identity check, the translations-repository ``update-pin.yml`` MANUAL dispatch and that repository''s own separate tag, and the Read the Docs ``en`` and ``ja`` ``stable`` verification — and D-14''s four REL-04 items all survive. An operator who reads only that file can still execute the close.'
    - 'Append-only extraction hazard: the automated gates read PHASE_BASE_SHA and the Baseline digest as the FIRST 40-hex and FIRST 64-hex match in ``63-CLOSEOUT-GUARD.md``. Every addition to that file goes at the END, below the existing Baseline section; inserting any new hexadecimal value above it would silently re-point every gate at the wrong anchor and turn a real comparison into a vacuous one.'
  artifacts:
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md
    - .planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md
  key_links:
    - '``63-CLOSEOUT-GUARD.md`` PHASE_BASE_SHA -> this plan''s scoped and widened diffs: one recorded value decides whether the SC#5 emptiness claim is a finding or an artifact of an unreachable anchor. The widened diff is the control that tells the two apart, and it must still list exactly five files after the correction.'
    - '``.planning/REQUIREMENTS.md`` -> ``63-CLOSEOUT-GUARD.md`` Baseline digest -> ``phase.complete``-family tooling: the gap-closure commits move HEAD, which is precisely the condition under which the historical checkbox flip has landed. Re-running the triad after those commits is what distinguishes "still held" from "assumed still held".'
    - '``scripts/extract_changelog_section.py 0.9.2`` corrected stdout -> ``63-HANDOFF.md`` item 3 -> the published GitHub Release body: the handoff commits an operator to a byte-identity diff against that stdout. A stale size recorded there would make a correct publish look like a failure, or hide a real mismatch.'
    - '``63-SC5-INVARIANTS.md`` § "Commits after the CI dispatch" -> ``63-GAP-CLOSURE-EVIDENCE.md``''s workflow measurement: the first file states a fact the closure invalidates; the second holds the measurement explaining why no re-dispatch follows. The cross-reference is what keeps the invalidated statement from being read as still true.'
  prohibitions:
    - 'MUST NOT take any irreversible action — no git tag local or remote, no tag push, no PyPI upload, no GitHub Release, no pull request, no ``update-pin.yml`` dispatch on the translations repository, no Read the Docs re-flip, and no trigger of ``release.yml`` by any route including ``workflow_dispatch``. Every command shape written into the handoff is a record for a future operator, never an instruction for this plan.'
    - 'MUST NOT trade one false claim for another. Every figure written into the handoff and into the two evidence files in this plan is captured from a command run in this plan, or is read back out of a file written in plan 63-05. No number is recalled, predicted, or carried forward from a pre-correction run.'
    - 'MUST NOT rewrite, renumber, supersede or delete plans 63-01 through 63-04 (D-22: this closure is ADDITIVE — new plans numbered from 63-05 onward, scoped by ``--gaps-only`` on the ``gap_closure: true`` flag) or any of their four SUMMARY files, and MUST NOT amend or rebase commit 10d9d95d.'
    - 'MUST NOT overwrite or delete any existing section of ``63-SC5-INVARIANTS.md`` or ``63-CLOSEOUT-GUARD.md``. Observations 1 and 2, the recorded Baseline and the close-time re-verification all stay byte-identical; corrections are appended and cross-referenced, never applied in place. The record of what was observed when is itself the evidence.'
    - 'MUST NOT insert any new 40-character or 64-character hexadecimal value above the existing Baseline section of ``63-CLOSEOUT-GUARD.md``. The gates extract the anchor and the digest by first match; a value inserted above would silently re-point them.'
    - 'MUST NOT modify ``.planning/REQUIREMENTS.md``, record REL-09 as satisfied anywhere including in handoff prose, or declare it in any SUMMARY''s ``requirements-completed``. If tooling flips the checkbox, revert with a checkout and report — never accept, never commit.'
    - 'MUST NOT weaken ``63-HANDOFF.md``''s standalone property while updating it. No step, no REL-04 item and no approval-gate warning may be dropped, and the file must stay readable without opening any other phase file.'
    - 'MUST NOT touch anything under ``typsphinx/``, and MUST NOT edit ``tests/test_changelog_page_gate.py`` — ``63-REVIEW.md`` IN-01 is declined for this closure (D-24).'
    - 'MUST NOT author a file named ``63-VERIFICATION.md``. That name is the verifier''s reserved output for this phase and a plan-authored file there is clobbered wholesale at verify time (D-19).'
---

<objective>
Re-close Phase 63 honestly after the gap-closure correction. Plan 63-05 changed the tree, which
means every phase-close claim taken before it is now a claim about a tree that no longer exists.

Take a third zero-irreversible-action fence observation against the post-correction tip with full
positive controls; correct, in the file that carries it, the now-false statement that every commit
after the CI dispatch was confined to the planning tree; re-verify the REL-09 checksum fence after
the gap-closure commits land, since new commits move HEAD and that is exactly the condition under
which the historical checkbox flip has occurred; and bring `63-HANDOFF.md` back into accuracy,
because it commits `/gsd-complete-milestone` to a byte-identity check against an extractor output
whose size the correction changed.

Purpose: the phase's original close was taken on a tree carrying a false published claim. Re-closing
by re-running the observations rather than by re-asserting the prior verdicts is the difference
between a phase that was verified and a phase that is verified.

Output: `63-SC5-INVARIANTS.md` extended with observation 3 and the superseded-statement correction,
`63-CLOSEOUT-GUARD.md` extended with the post-gap-closure fence re-verification, and an updated,
still-standalone `63-HANDOFF.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-VERIFICATION.md
@.planning/phases/63-v0-9-2-release-prep-prep-only/63-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
Worktree isolation is the standing execution mode (`CLAUDE.md` § "Worktree-isolated execution";
ROADMAP milestone constraint 12). It is mandatory and not conditional on parallelism. Before
running ANY command in this plan, provision this worktree's own virtual environment:

`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`

Then prefix every subsequent command with `uv run`. This plan runs no test suite and no build; the
provisioning is unconditional and establishes the worktree identity the diff and extractor
measurements are taken in.
</worktree_provisioning>

<!-- planner-discipline-allow: Run linters -->
<!-- planner-discipline-allow: 4087 -->
<!-- planner-discipline-allow: ^- \[x\] \*\*REL-09\*\* -->
<!-- planner-discipline-allow: positive control -->
<!-- planner-discipline-allow: Observation 1 of 2 -->
<!-- planner-discipline-allow: Observation 2 of 2 -->
<!-- planner-discipline-allow: Observation 3 -->
<!-- planner-discipline-allow: Commits after the CI dispatch -->
<!-- planner-discipline-allow: For the operator running phase.complete -->
<!-- planner-discipline-allow: Re-verification at phase close -->
<!-- planner-discipline-allow: git checkout -- .planning/REQUIREMENTS.md -->
<!-- planner-discipline-allow: update-pin.yml -->
<!-- planner-discipline-allow: create-release -->
<!-- planner-discipline-allow: extract_changelog_section.py -->
<!-- planner-discipline-allow: superseded -->
<!-- planner-discipline-allow: 63-GAP-CLOSURE-EVIDENCE.md -->
<!-- planner-discipline-allow: REL-09 -->
<!-- planner-discipline-allow: Re-verification after gap closure -->
<!-- planner-discipline-allow: REL-04 -->
<!-- planner-discipline-allow: manual approval -->
<!-- planner-discipline-allow: typsphinx-ja -->
<!-- The five literals in this second group appear ONLY in POSITIVE presence or equality checks in
     this plan (`-ge 1`, `-ge 3`, `-ge 4`, `= 1`, `= 3`) over the evidence, guard and handoff files
     this plan writes; the structure gate reads them as absence checks because they share an
     `&&`-chain with unrelated zero-count assertions. `REL-09`'s one genuine zero-count gate
     (`^- \[x\] \*\*REL-09\*\*`) is asserted over `.planning/REQUIREMENTS.md`, a file no action body
     in this plan writes to, so echoing the identifier cannot invalidate it.
     Two literals here are used in absence checks. "Run linters" is a step name that exists only in
     the release workflow, which this phase never triggers; the handoff and evidence must not drill
     for it, and the gate asserts zero occurrences in the updated handoff. "4087" is the
     pre-correction extractor size whose survival in the handoff would be the stale-figure defect
     this plan closes; its gate is guarded so it cannot fire spuriously if the corrected size
     happens to equal it. Both are echoed in action bodies only because naming them is the only way
     to say which figure must go. Every other literal above appears in positive presence checks or
     line-ordering comparisons over files this plan writes. -->

<tasks>

<task type="auto">
  <name>Task 1: Take fence observation 3 against the post-correction tip with full positive controls, and correct the now-superseded post-dispatch commit statement in the file that carries it</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md</files>
  <precondition>`gh auth status` succeeds, `git ls-remote --heads origin` returns at least one line, and `63-CLOSEOUT-GUARD.md` exists carrying a labelled PHASE_BASE_SHA that `git cat-file -e` resolves. An unreachable remote turns every probe into a silence indistinguishable from a clean result, and an unresolvable anchor turns the scoped diff into a vacuous empty. Halt and report on either.</precondition>
  <read_first>
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md` in full — the file being extended. Read observations 1 and 2 with their four probe subsections and recorded values, the explicit-separation verdict paragraph, § "The typsphinx/ diff (SC#5)" with both halves of its diff pair, and § "Commits after the CI dispatch" — the statement this task supersedes.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md` § "Baseline" — the labelled PHASE_BASE_SHA both diffs are scoped against, and the anchor commit's quoted subject.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-GAP-CLOSURE-EVIDENCE.md` — written by plan 63-05, carrying the measured reasoning for why the closure's non-planning commit does not oblige a fresh CI dispatch. This is the document the correction cross-references.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CI-EVIDENCE.md` — the dispatched run's id, head SHA and job conclusions, so the correction names the run it re-scopes rather than describing it.
    - `.planning/ROADMAP.md` § "Phase Details → Phase 63" SC#5, and `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<specifics>` item 6.
  </read_first>
  <action>
    APPEND to `63-SC5-INVARIANTS.md`. Do not overwrite, edit or renumber observations 1 or 2, or the
    existing diff-pair section — this file's value is that it shows what was observed when, and the
    two prior observations are what prove the separation SC#5 asks for.

    **Head the new section so its relationship to the existing pair is unambiguous** — for example
    `## Observation 3 — post-gap-closure re-probe`. Open it by stating plainly why it exists: the
    "of 2" in the two prior headings refers to SC#5's requirement of two waves-separated
    observations, both of which were taken before the gap-closure correction commit and therefore
    say nothing about the tree that now exists. This third observation is additional, not a
    renumbering. Record a fresh UTC timestamp and the current HEAD SHA.

    **Re-run all four probes with their positive controls, capturing values fresh.** Copying any
    value from observation 1 or 2 would defeat the purpose of taking a third.

    The local tag probe: the release-tag list, expected empty, paired with the 0.9.0 tag list as the
    control, expected non-empty. The remote tag probe: ONE unfiltered `git ls-remote --tags origin`
    fetch, with both the control count and the assertion count derived from that single fetch, and
    the fetch's total line count recorded and compared against the count observation 2 recorded — a
    difference is a finding to report, not a number to quietly overwrite. The publish probe: ONE
    `gh release list` fetch, with the latest-release marker count as the control and the release
    version's row count as the assertion, plus `gh release view` on the release tag recorded with
    its literal not-found message and non-zero exit. The release-workflow probe: a read-only
    `gh run list --workflow=release.yml` listing, recorded literally, with the explicit statement
    that no run corresponds to this branch and that the workflow was never dispatched. Add a
    pull-request probe recorded the same way: the open-PR listing, with the pre-existing dependabot
    entries named by number and date so a reader can see they predate this phase, and the assertion
    that no open PR has this milestone branch as its head.

    **Re-take the scoped and widened diff pair.** Read PHASE_BASE_SHA back out of
    `63-CLOSEOUT-GUARD.md` rather than re-deriving it, confirm it resolves with `git cat-file -e`,
    and record both diffs against that one anchor. The `typsphinx/`-scoped diff must produce no
    output. The same-anchor widened diff — everything outside the planning tree — must be non-empty
    and must still list exactly the five files this phase touches; the correction edits
    `CHANGELOG.md`, which was already in that set, so no sixth file may appear. State in one line
    why the pairing exists: an empty scoped diff from a wrong or unreachable anchor is
    indistinguishable from a genuinely clean tree, and only a non-empty same-anchor widened result
    tells them apart.

    **Correct the superseded statement, in place in this file, by appending — not by editing.** Add
    a clearly-headed subsection recording that § "Commits after the CI dispatch" stated every
    post-dispatch commit was confined to the planning tree, that the gap closure lands a
    `CHANGELOG.md` commit outside it, and that the earlier statement is therefore SUPERSEDED as of
    the named commit. Name the CI run being re-scoped, and cross-reference
    `63-GAP-CLOSURE-EVIDENCE.md` for the measurement showing that no CI lane reads changelog content
    and that no re-dispatch is therefore owed. Leaving the original sentence standing without this
    annotation would reproduce, one level down, the exact defect this closure exists to fix.
  </action>
  <verify>
    <automated>F=.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md &amp;&amp; BASE=$(grep -oE '\b[0-9a-f]{40}\b' .planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md | head -1) &amp;&amp; git cat-file -e "$BASE" &amp;&amp; [ -z "$(git diff "$BASE"..HEAD -- typsphinx/)" ] &amp;&amp; [ -n "$(git diff --stat "$BASE"..HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ "$(git diff --name-only "$BASE"..HEAD -- . ':(exclude).planning' | LC_ALL=C sort | tr '\n' ' ')" = 'CHANGELOG.md README.md pyproject.toml tests/test_changelog_page_gate.py uv.lock ' ] &amp;&amp; [ -z "$(git tag -l 'v0.9.2*')" ] &amp;&amp; [ -n "$(git tag -l 'v0.9.0*')" ] &amp;&amp; git ls-remote --tags origin &gt; /tmp/63g-tags3.txt &amp;&amp; [ "$(grep -c 'refs/tags/v0\.9\.0' /tmp/63g-tags3.txt)" -ge 1 ] &amp;&amp; [ "$(grep -c 'refs/tags/v0\.9\.2' /tmp/63g-tags3.txt)" = 0 ] &amp;&amp; gh release list --limit 20 &gt; /tmp/63g-rel3.txt &amp;&amp; [ "$(grep -c 'Latest' /tmp/63g-rel3.txt)" -ge 1 ] &amp;&amp; [ "$(grep -c 'v0\.9\.2' /tmp/63g-rel3.txt)" = 0 ] &amp;&amp; [ "$(gh pr list --state open --json headRefName -q '.[].headRefName' | grep -c 'v0.9.2-inline-image-blocker-fix-and-release')" = 0 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headBranch -q '.[].headBranch' | grep -c 'v0.9.2-inline-image-blocker-fix-and-release')" = 0 ] &amp;&amp; [ "$(grep -c 'Observation 1 of 2' "$F")" -ge 1 ] &amp;&amp; [ "$(grep -c 'Observation 2 of 2' "$F")" -ge 1 ] &amp;&amp; [ "$(grep -c 'Observation 3' "$F")" -ge 1 ] &amp;&amp; [ "$(grep -n 'Observation 2 of 2' "$F" | head -1 | cut -d: -f1)" -lt "$(grep -n 'Observation 3' "$F" | head -1 | cut -d: -f1)" ] &amp;&amp; [ "$(grep -ci 'positive control' "$F")" -ge 9 ] &amp;&amp; [ "$(grep -oE '20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' "$F" | LC_ALL=C sort -u | wc -l)" -ge 3 ] &amp;&amp; [ "$(grep -ci 'superseded' "$F")" -ge 1 ] &amp;&amp; [ "$(grep -c '63-GAP-CLOSURE-EVIDENCE.md' "$F")" -ge 1 ] &amp;&amp; [ "$(grep -c '33309565005' "$F")" -ge 1 ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ .planning/REQUIREMENTS.md tests/test_changelog_page_gate.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `63-SC5-INVARIANTS.md` still contains both `## Observation 1 of 2` and `## Observation 2 of 2` with their recorded values byte-identical, and gains a third observation section that appears strictly AFTER observation 2 in file order
    - the new section states explicitly that the two prior observations pre-date the correction commit and that this third is additional rather than a renumbering, and records a fresh ISO-8601 Zulu timestamp and the current HEAD SHA
    - at least three distinct ISO-8601 Zulu timestamps now appear in the file, and the phrase `positive control` appears at least nine times across it — three per observation
    - the local tag probe records the release-tag list empty and the 0.9.0 tag list non-empty; the remote tag probe derives at least one control reference and exactly zero release-tag references from ONE unfiltered fetch, whose total line count is recorded and compared against observation 2's, with any difference reported as a finding
    - the publish probe derives at least one latest-release marker and exactly zero release rows from ONE listing, and records `gh release view` on the release tag with its literal not-found message and non-zero exit
    - the release-workflow probe and the pull-request probe are both recorded literally; no open PR has this milestone branch as its head and no release-workflow run has it as its head branch; the pre-existing dependabot PRs are named by number and date
    - PHASE_BASE_SHA is read back out of `63-CLOSEOUT-GUARD.md`, `git cat-file -e` on it exits 0, and both diffs are recorded against that one anchor
    - the `typsphinx/`-scoped diff from that anchor to HEAD produces no output, and the same-anchor widened diff is non-empty with a file list under `LC_ALL=C sort` of exactly `CHANGELOG.md README.md pyproject.toml tests/test_changelog_page_gate.py uv.lock` — no sixth file
    - an appended subsection records § "Commits after the CI dispatch" as SUPERSEDED, names the correction commit and the CI run being re-scoped, and cross-references `63-GAP-CLOSURE-EVIDENCE.md` for why no re-dispatch is owed; the original sentence is left in place rather than edited
    - `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md tests/test_changelog_page_gate.py` is empty
  </acceptance_criteria>
  <done>The zero-irreversible-action guarantee is re-established against the post-correction tip with fresh values and full positive controls, the scoped emptiness claim rests on an anchor proven reachable by a non-empty same-anchor widened diff still listing exactly five files, and the one statement in this file that the closure invalidated is annotated superseded with a pointer to the measurement that explains it.</done>
  <reversibility rating="reversible">Read-only probes and an appended planning-document section. Nothing is written to any remote and no workflow is dispatched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Re-verify the REQUIREMENTS.md closeout fence after the gap-closure commits land, and revert any flip by hand rather than accept it</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md</files>
  <read_first>
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md` in full — the file being extended: its recorded Baseline digest, line count, PHASE_BASE_SHA and timestamp; its three classified lines under guard quoted verbatim with their line numbers; its re-verification protocol; its § "Re-verification at phase close" from plan 63-04; and its operator-facing § "For the operator running phase.complete".
    - `.planning/REQUIREMENTS.md` — the file being re-measured and quoted, never edited.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-16 in full.
    - `.planning/ROADMAP.md` § "Phase Details → Phase 63" SC#3 and § "🚧 v0.9.2" milestone constraint 9.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-04-SUMMARY.md` — how the close-time re-verification was recorded, so this one matches its shape rather than inventing a second.
  </read_first>
  <action>
    APPEND a `## Re-verification after gap closure` section to the END of `63-CLOSEOUT-GUARD.md`.
    Append only. Do not edit the Baseline section, the classified guarded lines, the protocol
    sections or plan 63-04's close-time re-verification, and — critically — do not insert any new
    40-character or 64-character hexadecimal value above the Baseline. Every automated gate in this
    phase reads the anchor and the digest as the first such match in the file; a value placed above
    would silently re-point them and turn a real comparison into a vacuous one.

    Open the section by stating why it exists: the gap-closure commits move HEAD, and moving HEAD is
    precisely the condition under which the historical checkbox flip has landed at five consecutive
    prior release-prep closes. A fence verified before those commits says nothing about the tree
    after them.

    Record the protocol's triad at a fresh UTC timestamp, as commands with their literal output.
    Run `sha256sum .planning/REQUIREMENTS.md` and compare the printed digest character-for-character
    against the Baseline digest recorded in this same file — show both values side by side, not a
    bare verdict word. Run `wc -l` and compare against the recorded line count. Run a name-only
    `git diff` scoped to `.planning/REQUIREMENTS.md` and expect no output, and run
    `git status --porcelain` on the same path and expect no output. Run
    `grep -n 'REL-09' .planning/REQUIREMENTS.md` and compare its hits line-for-line against the
    quoted Baseline hits — both the line numbers and the byte content of the two state-bearing
    lines, and the presence of the informational-only third hit.

    Record the verdict as MATCH or DIVERGED with the compared values shown. A verdict a reader
    cannot re-derive from the file is not evidence.

    **If any comparison diverges** — most likely REL-09's checkbox flipped to checked against this
    phase's explicit decision — do not accept it and do not commit it. Run
    `git checkout -- .planning/REQUIREMENTS.md`, re-run the full triad, and record the before state,
    the reversion command, the after state, and an explicit report of the divergence both in this
    section and in the plan's summary.

    Then read REL-09's state directly out of `.planning/REQUIREMENTS.md` and quote its checkbox line
    and its Traceability row verbatim, showing the checkbox unchecked and the row Pending. REL-09's
    state is read out of that file, never inferred from any plan's frontmatter, and both
    gap-closure plans declare an empty completed-requirements list for it.

    Close the section by re-stating that the DECISIVE observation is still owed and is still not a
    plan's to take: it runs after `phase.complete`-family tooling, outside any plan's reach, and its
    protocol already lives in this file's operator-facing section. Confirm that section is still
    present and reachable, and that `63-HANDOFF.md` still points at it by name.
  </action>
  <verify>
    <automated>G=.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(grep -oE '\b[0-9a-f]{64}\b' "$G" | head -1)" ] &amp;&amp; [ "$(grep -c "$(wc -l &lt; .planning/REQUIREMENTS.md) .planning/REQUIREMENTS.md" "$G")" -ge 1 ] &amp;&amp; [ -z "$(git diff --name-only -- .planning/REQUIREMENTS.md)" ] &amp;&amp; [ -z "$(git status --porcelain .planning/REQUIREMENTS.md)" ] &amp;&amp; [ "$(grep -c '^- \[ \] \*\*REL-09\*\*' .planning/REQUIREMENTS.md)" = 1 ] &amp;&amp; [ "$(grep -c '^- \[x\] \*\*REL-09\*\*' .planning/REQUIREMENTS.md)" = 0 ] &amp;&amp; [ "$(grep -c '| REL-09 | Phase 63 | Pending |' .planning/REQUIREMENTS.md)" = 1 ] &amp;&amp; [ "$(grep -c 'REL-09' .planning/REQUIREMENTS.md)" = 3 ] &amp;&amp; [ "$(grep -c 'Re-verification at phase close' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'Re-verification after gap closure' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -c 'For the operator running phase.complete' "$G")" -ge 1 ] &amp;&amp; [ "$(grep -n 'Re-verification at phase close' "$G" | head -1 | cut -d: -f1)" -lt "$(grep -n 'Re-verification after gap closure' "$G" | head -1 | cut -d: -f1)" ] &amp;&amp; [ "$(grep -oE '20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' "$G" | LC_ALL=C sort -u | wc -l)" -ge 3 ] &amp;&amp; [ "$(grep -n -oE '\b[0-9a-f]{64}\b' "$G" | head -1 | cut -d: -f1)" -lt "$(grep -n 'Re-verification after gap closure' "$G" | head -1 | cut -d: -f1)" ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - the live `sha256sum .planning/REQUIREMENTS.md` digest equals the Baseline digest recorded in `63-CLOSEOUT-GUARD.md`, and the live `wc -l` equals the recorded line count — both shown side by side in the appended section rather than asserted as a bare verdict
    - `git diff --name-only -- .planning/REQUIREMENTS.md` and `git status --porcelain .planning/REQUIREMENTS.md` are both empty
    - `.planning/REQUIREMENTS.md` contains exactly one unchecked REL-09 requirement bullet, zero checked ones, exactly one Pending Traceability row for it, and exactly three total occurrences — the same three the Baseline recorded
    - the appended `## Re-verification after gap closure` section records a MATCH or DIVERGED verdict with the compared values shown, at a fresh Zulu timestamp, and the file now carries at least three distinct such timestamps
    - the new section appears strictly AFTER plan 63-04's `Re-verification at phase close` section, and the file's FIRST 64-hex match still occurs above the new section — proving no hexadecimal value was inserted above the Baseline
    - if any comparison diverged, the section records the before state, the literal reversion command, the re-run triad and the after state, and the divergence is reported in the plan summary — never accepted, never committed
    - REL-09's checkbox line and Traceability row are quoted verbatim out of `.planning/REQUIREMENTS.md`, read from the file rather than inferred from frontmatter
    - the section closes by re-stating that the decisive post-tooling observation is still owed, and § "For the operator running phase.complete" is confirmed still present
    - `git status --porcelain typsphinx/` is empty
  </acceptance_criteria>
  <done>The checksum fence is re-verified against its own recorded Baseline on the tree that now includes the gap-closure commits, with the compared values shown so a reader can re-derive the verdict; REL-09's checkbox is confirmed still unchecked by reading the file directly; and the decisive post-tooling observation remains reachable in the operator-facing section rather than assumed.</done>
  <reversibility rating="reversible">A read-only comparison appended to an existing planning document. The only mutation this task may perform is a checkout that RESTORES a file to its committed state.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Bring `63-HANDOFF.md` back into accuracy — the corrected extractor size, the correction itself, the third fence observation, and the gap-closure plans' prohibitions — without weakening its standalone property</name>
  <files>.planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md</files>
  <read_first>
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md` in full — the file being updated. Note precisely where it transcribes figures and cross-references that the closure changed: its opening paragraph's citation of the fence observations, its SC#2 bullet with the recorded extractor size, its statement about which plan's prohibitions block forbids the publish actions, and item 3's byte-identity procedure with its attribution of the byte-identity proof to plan 63-01.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CHANGELOG-EVIDENCE.md` § the post-correction section written by plan 63-05 — the source of the corrected extractor size and of the correction's own description. Read the value out of that file; do not re-derive it from memory.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md` § the observation-3 section and the superseded-statement subsection, both written earlier in this plan.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md` § "Re-verification after gap closure" and § "For the operator running phase.complete".
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-VERIFICATION.md` frontmatter `gaps:` block and § "Gaps Summary" — the SC#2 finding whose status the handoff's own SC#2 report must now reflect honestly.
    - `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CONTEXT.md` § `<decisions>` D-13, D-14, D-15 and D-19, and § "Claude's Discretion" bullet 5.
    - `.planning/REQUIREMENTS.md` § "REL — release" REL-09's text — the handoff quotes it verbatim and that quotation must stay byte-accurate.
  </read_first>
  <action>
    Update `63-HANDOFF.md` in place with targeted edits. This is not a rewrite: every existing
    section, every publish step, D-14's four REL-04 items, D-15's approval-gate warning and the
    verbatim REL-09 quotation all survive. The file must remain readable and executable by an
    operator who opens no other file.

    **Replace the recorded extractor size.** The handoff's SC#2 report transcribes the extractor's
    stdout size, and the correction changed it. Read the corrected value out of
    `63-CHANGELOG-EVIDENCE.md`'s post-correction section — or re-run the extractor and measure it —
    and replace the stale figure everywhere it appears. Do not leave the pre-correction figure
    anywhere in the file: item 3 commits an operator to a byte-identity diff against the extractor's
    output, and a stale size there would make a correct publish look like a failure or hide a real
    mismatch. Keep the figure labelled in bytes.

    **Report SC#2 honestly rather than restating the original verdict.** Rewrite that bullet to say
    what actually happened: the extractor was run and its structural properties inspected; a false
    file-confinement claim in the extracted body survived that inspection and was found by the code
    review and the verification report; the claim was corrected in the gap closure; and the
    structural set plus the byte-identity proof were re-run against the corrected text. Cite the
    evidence sections by name. Do not present SC#2 as having been clean all along, and do not
    present the correction as a formatting change.

    **Update the fence-observation citation** in the opening paragraph to name all three
    observations rather than two, stating that the third was taken after the correction commit
    because the first two pre-date it. Keep the paragraph's positive opening intact: this milestone
    does publish, and the checklist below is the sequence `/gsd-complete-milestone` executes.

    **Update the prohibitions attribution.** The file currently attributes the publish-action
    prohibitions to a single plan's block; name the gap-closure plans as carrying the same
    prohibitions, so the claim that none of the publish actions occurred covers the whole phase
    including this closure.

    **Update item 3's attribution.** It credits plan 63-01 with proving the extractor's stdout
    byte-identical to the section on disk. That proof was re-taken against the corrected text in the
    gap closure; say so, and point at the post-correction evidence section, so the operator diffs
    against the right bytes.

    **Leave everything else alone.** Do not touch the `pypi` Environment approval warning or its
    position ahead of the step that triggers it, the tag-push step, the translations-repository
    manual-dispatch step and its separate tag, the Read the Docs verification step, D-14's four
    REL-04 items, the pointer to the closeout guard's operator-facing section, the declined-ideas
    record, or the migration-guide note. Do not drill for a step name that exists only in the
    release workflow; lint authority is the continuous-integration workflow's lint job, as already
    recorded.
  </action>
  <verify>
    <automated>H=.planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md &amp;&amp; NEW=$(uv run python scripts/extract_changelog_section.py 0.9.2 | wc -c) &amp;&amp; [ "$(grep -c "$NEW" "$H")" -ge 1 ] &amp;&amp; { [ "$NEW" = 4087 ] || [ "$(grep -c '4087' "$H")" = 0 ]; } &amp;&amp; [ "$(grep -c 'Run linters' "$H")" = 0 ] &amp;&amp; [ "$(grep -c '63-05' "$H")" -ge 1 ] &amp;&amp; [ "$(grep -ci 'observation 3' "$H")" -ge 1 ] &amp;&amp; [ "$(grep -c 'REL-04' "$H")" -ge 4 ] &amp;&amp; [ "$(grep -c 'update-pin.yml' "$H")" -ge 1 ] &amp;&amp; [ "$(grep -c 'create-release' "$H")" -ge 3 ] &amp;&amp; [ "$(grep -c 'For the operator running phase.complete' "$H")" -ge 1 ] &amp;&amp; [ "$(grep -c 'typsphinx-ja' "$H")" -ge 1 ] &amp;&amp; [ "$(grep -ci 'manual approval' "$H")" -ge 1 ] &amp;&amp; [ "$(grep -c 'extract_changelog_section.py' "$H")" -ge 2 ] &amp;&amp; [ "$(grep -c '^- \[ \] \*\*REL-09\*\*' .planning/REQUIREMENTS.md)" = 1 ] &amp;&amp; [ "$(grep -c '^- \[x\] \*\*REL-09\*\*' .planning/REQUIREMENTS.md)" = 0 ] &amp;&amp; [ "$(grep -n -i 'manual approval' "$H" | head -1 | cut -d: -f1)" -lt "$(grep -n 'git push origin v0.9.2' "$H" | head -1 | cut -d: -f1)" ] &amp;&amp; [ ! -e .planning/phases/63-v0-9-2-release-prep-prep-only/63-VERIFICATION-GAP.md ] &amp;&amp; [ -z "$(git tag -l 'v0.9.2*')" ] &amp;&amp; [ -z "$(git status --porcelain typsphinx/ .planning/REQUIREMENTS.md tests/test_changelog_page_gate.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - the extractor size recorded in `63-HANDOFF.md` equals the value produced by re-running the extractor against the corrected `CHANGELOG.md` at verification time, and is labelled in bytes
    - the pre-correction size no longer appears anywhere in the file, unless the corrected size happens to equal it
    - the SC#2 bullet states what actually happened — structural inspection passed, a false file-confinement claim survived it, the code review and verification report found it, the gap closure corrected it, and the structural set plus byte-identity proof were re-run against the corrected text — and cites the evidence sections by name
    - the opening paragraph still opens with the positive and now names all three fence observations, stating that the third post-dates the correction commit
    - the prohibitions attribution names the gap-closure plans alongside the original, so the no-publish claim covers the whole phase
    - item 3's byte-identity procedure points the operator at the post-correction evidence section rather than only at plan 63-01
    - every pre-existing element survives: the `pypi` Environment manual approval named BEFORE the tag-push step, the tag push itself, D-14's four REL-04 items, the translations-repository manual dispatch and its separate tag, the Read the Docs `en` and `ja` verification including the `typsphinx-ja` project, the pointer to the closeout guard's operator section, the declined-ideas record and the migration-guide note
    - the file contains zero occurrences of the release-workflow-only lint step name
    - `.planning/REQUIREMENTS.md` still shows REL-09 unchecked, no `v0.9.2` tag exists, and `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md tests/test_changelog_page_gate.py` is empty
  </acceptance_criteria>
  <done>`63-HANDOFF.md` is accurate against the corrected tree: it commits `/gsd-complete-milestone` to a byte-identity check against the extractor output that now exists, reports SC#2's real history rather than its original verdict, cites all three fence observations, and remains a standalone checklist an operator can execute without opening another file.</done>
  <reversibility rating="reversible">Targeted edits to one planning document. No product-tree file, no remote and no workflow is affected.</reversibility>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

| Artifact | Created or modified | What changes |
|---|---|---|
| `.planning/phases/63-v0-9-2-release-prep-prep-only/63-SC5-INVARIANTS.md` | modified (append-only) | A third zero-irreversible-action observation taken against the post-correction tip, with all four probes plus a pull-request probe and their positive controls, a re-taken scoped/widened `typsphinx/` diff pair from the recorded anchor, and an appended subsection recording the post-dispatch-commit statement as superseded with a cross-reference to the measurement that explains it. Observations 1 and 2 and the original diff-pair section are untouched. |
| `.planning/phases/63-v0-9-2-release-prep-prep-only/63-CLOSEOUT-GUARD.md` | modified (append-only) | A `## Re-verification after gap closure` section appended below the existing close-time re-verification, recording the SHA-256 / line-count / name-only-diff / REL-09-grep triad against the recorded Baseline on the tree that now includes the gap-closure commits, with the compared values shown and the reversion protocol restated. The Baseline and every value above it are untouched. |
| `.planning/phases/63-v0-9-2-release-prep-prep-only/63-HANDOFF.md` | modified | The recorded extractor size replaced with the corrected measurement, the SC#2 report rewritten to state what actually happened, the fence-observation citation extended to three, the prohibitions attribution extended to the gap-closure plans, and item 3's byte-identity attribution pointed at the post-correction evidence. Every publish step, REL-04 item and approval-gate warning survives. |

No symbol, module, function or configuration value is created or modified anywhere under
`typsphinx/`. No test file is created or modified. No requirement checkbox is moved. No file named
`63-VERIFICATION.md` is authored.
</artifacts_produced>

<flagged_assumptions>
The spec-less edge-coverage accounting and the prohibition-recall accounting for this gap closure
are recorded once, phase-wide, in `63-05-PLAN.md` § `<flagged_assumptions>`: six probe-surfaced edge
items resolved as five explicit `must_haves.truths` plus one flagged assumption (REL-11's
`unclassified` row, which stays `unresolved` and is neither backstopped nor dismissed), and ten kept
prohibitions authored descriptor-less across both plans plus one canon-referral breadcrumb dropped
by rule. This plan carries P1 and P4 through P9 of that set in its own `must_haves.prohibitions`,
and REL-11's flagged row is the one whose actual verification lands in this plan's Task 2 — the
fence re-verification — with the decisive post-tooling observation still delegated to the operator
through `63-CLOSEOUT-GUARD.md`. No probe-surfaced item is dropped and none is re-derived here.

**Reversibility:** every decision in this plan is reversible. It writes only planning documents,
takes only read-only probes against the remote, and creates no tag, release, pull request or
workflow run. There is no `one-way` decision and therefore no `checkpoint:decision`; the genuinely
one-way step is the tag push at `/gsd-complete-milestone`, which this plan records and does not
perform.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this worktree → `origin` and the GitHub API (`git ls-remote`, `gh release list`, `gh pr list`, `gh run list`) | read-only probes cross out to the remote; no write, no dispatch, no tag |
| `.planning/REQUIREMENTS.md` → `phase.complete`-family tooling | a file this closure must leave byte-identical, against tooling that has flipped its release checkbox at five consecutive prior release-prep closes |
| `63-HANDOFF.md` → the operator running `/gsd-complete-milestone` | the only artifact carrying the publish sequence forward; a step omitted or a figure left stale here becomes a step done wrong or not done at all |
| `63-CLOSEOUT-GUARD.md` Baseline values → every automated gate in this phase | the anchor and the digest are extracted by first match; the file's ordering is load-bearing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63H-01 | Elevation of Privilege | irreversible release actions — tag, tag push, PyPI upload, GitHub Release, pull request, translations-repository pin dispatch, `release.yml` trigger | high | mitigate | The prohibitions block forbids each by name; every command in this plan is read-only or writes a planning document; Task 1 re-probes for each of them against the post-correction tip with positive controls, so the absence is observed rather than assumed. |
| T-63H-02 | Tampering | `.planning/REQUIREMENTS.md` REL-09 checkbox and Traceability row | high | mitigate | Task 2 re-runs the recorded SHA-256, line-count, name-only-diff and grep triad after the gap-closure commits move HEAD — the exact condition under which the flip has historically landed — and reverts any divergence with a checkout, reporting rather than committing it. |
| T-63H-03 | Repudiation | a phase-close verdict re-asserted rather than re-observed after the tree changed | high | mitigate | Both fence observations pre-date the correction, so a third is taken with values captured fresh; copying any prior value is prohibited, and the gate requires three distinct timestamps and nine positive-control mentions. |
| T-63H-04 | Tampering | the anchor and digest extraction in `63-CLOSEOUT-GUARD.md` | medium | mitigate | Appending is mandated and inserting any new 40- or 64-hex value above the Baseline is prohibited; the gate asserts the first 64-hex match still occurs above the newly appended section, so a re-pointed anchor fails the task. |
| T-63H-05 | Spoofing | a stale extractor size in `63-HANDOFF.md` making a correct publish look like a failure, or masking a real body mismatch | medium | mitigate | Task 3's gate recomputes the size from the corrected file at verification time, requires it to appear in the handoff, and requires the pre-correction figure to be absent unless the two coincide. |
| T-63H-06 | Information Disclosure | a handoff quietly shortened during the update, dropping a publish step or the approval-gate warning an operator depends on | medium | mitigate | The gate asserts every element survives by name — the manual approval positioned before the tag-push step, all four REL-04 items, the translations-repository dispatch, the `ja` project, the closeout-guard pointer — so a rewrite that loses one fails. |
| T-63H-07 | Repudiation | a statement in the evidence left standing after the closure invalidated it | medium | mitigate | Task 1 appends an explicit supersession record for the post-dispatch-commit statement, naming the correction commit and cross-referencing the measurement that explains why no re-dispatch follows, rather than editing the original away or leaving it unqualified. |
| T-63H-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs, adds and upgrades zero packages — it writes three planning documents and runs read-only probes. `63-RESEARCH.md` § "Package Legitimacy Audit" already measured the phase as owing no package-legitimacy checkpoint. No install task exists in this plan. |
</threat_model>

<verification>
The plan is complete when, on the post-correction tree:

- `63-SC5-INVARIANTS.md` carries observations 1, 2 and 3 with at least three distinct Zulu
  timestamps and at least nine positive-control mentions, observation 3 appearing after
  observation 2, and the superseded post-dispatch statement annotated with its cross-reference
- the release tag is absent locally and remotely, no release, no open PR on this branch and no
  release-workflow run on this branch exist, each proven against a control that would fail if the
  probe were vacuous
- the `typsphinx/`-scoped diff from the recorded anchor is empty and the same-anchor widened diff
  lists exactly the five files this phase touches
- `63-CLOSEOUT-GUARD.md`'s Baseline digest and line count still match the live file, REL-09 is
  still unchecked with a Pending row and exactly three occurrences, and the new re-verification
  section sits below the existing one with no hexadecimal value inserted above the Baseline
- `63-HANDOFF.md` carries the corrected extractor size, no stale size, an honest SC#2 report, a
  three-observation citation, and every pre-existing publish step and REL-04 item
- `git status --porcelain typsphinx/ .planning/REQUIREMENTS.md tests/test_changelog_page_gate.py`
  is empty and no file named `63-VERIFICATION.md` was authored
</verification>

<success_criteria>
Phase 63 is re-closed against the tree that actually exists: the zero-irreversible-action guarantee
is re-observed after the correction rather than inherited from observations that pre-date it, the
REL-09 checksum fence is re-verified after the gap-closure commits moved HEAD, the one evidence
statement the closure invalidated is annotated superseded instead of left standing, and the standalone
publish handoff commits `/gsd-complete-milestone` to a byte-identity check against the corrected
extractor output rather than a stale one.
</success_criteria>

<output>
Create `.planning/phases/63-v0-9-2-release-prep-prep-only/63-06-SUMMARY.md` when done, with
frontmatter declaring `requirements-completed: []`.
</output>
