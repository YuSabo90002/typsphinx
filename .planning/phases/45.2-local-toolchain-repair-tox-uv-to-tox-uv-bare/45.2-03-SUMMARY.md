---
phase: 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare
plan: 03
subsystem: testing
tags: [pytest, docstrings, planning-docs, record-correction, grep-sweep]

requires:
  - phase: 45.2-01
    provides: pre-fix toolchain evidence baseline (Steps 0-3 of 45.2-TOOLCHAIN-EVIDENCE.md)
provides:
  - "Execution-time repo-wide grep inventory (5 patterns, 255 raw hits / 248 unique locations) with a full disposition per hit, appended as Step 4 of 45.2-TOOLCHAIN-EVIDENCE.md"
  - "6 test docstring/comment sites moved to past tense with a QUA-04 rationale (D-06)"
  - "ROADMAP.md and REQUIREMENTS.md corrected from a 13-test-module miscount to the measured 5, with SC#1/SC#5/SC#6 amended to match reality"
  - "STATE.md's wrong 'known NixOS false positives' framing corrected, with a new sentence distinguishing this milestone's two unrelated exit-127 causes (QUA-04 vs REL-04)"
affects: [45.2-04, 45.2-05, 46]

actuals:
  tokens: 13400
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Discovery-time repo-wide grep as the authority for a record-correction sweep, not a file list from CONTEXT.md — the sweep itself found 2 files the plan's own frontmatter named that scored zero hits under the mandated patterns (test_authors_pipeline_stage_gate.py, test_package_template_routing.py), and one file the plan's file list omitted that did have a hit (test_heading_depth_render_gate.py)"

key-files:
  created: []
  modified:
    - .planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - tests/test_pdf_render_gate.py
    - tests/test_preview_smoke_gate.py
    - tests/test_epigraph_render_gate.py
    - tests/test_heading_depth_render_gate.py
    - tests/test_admonition_bucket_render_gate.py
    - tests/test_admonition_locale_title_precedence_gate.py

key-decisions:
  - "Extended the plan's 3-bucket disposition taxonomy (CORRECT / OUT-OF-SCOPE REL-04 / OUT-OF-SCOPE archived) with two documented sub-buckets, OUT-OF-SCOPE (not-yet-archived record) and OUT-OF-SCOPE (unrelated topic), because the actual 255-hit sweep surfaced completed-phase SUMMARY/PLAN/EVIDENCE artifacts from this same not-yet-shipped milestone (43, 44, 44.1, 44.2, 45, 45.1) and genuinely unrelated 'false positive' hits about unrelated test-assertion logic, neither of which the plan's three named categories cover"
  - "Excluded tests/test_authors_pipeline_stage_gate.py and tests/test_package_template_routing.py from Task 2's edits despite being named in the plan's own frontmatter files_modified line, because the grep sweep found zero hits from the 5 mandated patterns in either — their hazard wording ('returncode 127' / 'a stray uv binary makes...fail') doesn't match any of the 5 literal patterns. Per the plan's own instruction ('the inventory wins'), this is intentional, not an omission"

requirements-completed: []

coverage: []

duration: ~25min
completed: 2026-08-11
status: complete
---

# Phase 45.2 Plan 03: SC#6 Discovery-Time Sweep + Record Correction Summary

**Corrected the "13 test modules" miscount to 5 and the "unfixable NixOS false positives" misdiagnosis to the real, fixable cause across ROADMAP.md, REQUIREMENTS.md, STATE.md, and 6 test docstrings — via an actual execution-time repo-wide grep (255 raw hits) rather than the file list CONTEXT.md started from.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 3
- **Files modified:** 10 (1 evidence file, 3 planning docs, 6 test modules)
- **Commits:** 3 task commits (`18ffa14`, `0efd3d8`, `228f288`)

## Accomplishments

- Ran the 5 SC#6-mandated grep patterns (`Could not start dynamically linked`, `command not found`,
  `not on PATH`, `non-Nix`, `false positive`) across the whole repository at execution time — 255 raw
  hits collapsing to 248 unique locations — and recorded a complete disposition table (every location
  classified) as Step 4 of `45.2-TOOLCHAIN-EVIDENCE.md`.
- The sweep confirmed all six of D-08's REL-04 sites and found two genuine discrepancies from the
  plan's own file list: `tests/test_heading_depth_render_gate.py` had a hit the D-05 file list
  omitted (no literal `uv` token, phrased as "non-Nix package-manager"), and
  `tests/test_authors_pipeline_stage_gate.py` / `tests/test_package_template_routing.py` — both named
  in the plan's frontmatter — scored zero hits (different wording that matches none of the 5
  patterns).
- Moved the 6 grep-confirmed test docstring/comment sites to past tense with an added QUA-04
  rationale, leaving the diagnosis mechanism itself untouched (D-06) and the 5 real `uv run`-invoking
  integration modules byte-unchanged (D-09).
- Amended `ROADMAP.md`'s Phase 45.2 section (top note, phase-list bullet, Goal paragraph, SC#1, SC#3,
  SC#5, SC#6) and `REQUIREMENTS.md`'s QUA-04 body to state 5 modules instead of 13, with SC#1 now
  correctly closing on "provisioning + command execution both reached, `ruff` routed out" instead of
  falsely claiming `tox -e lint` completes.
- Corrected `STATE.md`'s "known NixOS `uv` exit-127 false positives" framing to the real diagnosis and
  added the sentence distinguishing this milestone's two unrelated `exit 127` causes (QUA-04 vs
  REL-04), leaving both REL-04 record sites in that file byte-unchanged.

## Task Commits

Each task was committed atomically:

1. **Task 1: Run the discovery-time sweep and record the authoritative hit inventory** - `18ffa14` (docs)
2. **Task 2: Move the test-docstring diagnosis to past tense** - `0efd3d8` (docs)
3. **Task 3: Amend ROADMAP.md, REQUIREMENTS.md and STATE.md** - `228f288` (docs)

## Files Created/Modified

- `.planning/phases/45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare/45.2-TOOLCHAIN-EVIDENCE.md` - Appended Step 4: the full grep inventory and disposition table (304 new lines)
- `.planning/ROADMAP.md` - 13→5 module count (4 sites), SC#1/SC#5/SC#6 amended
- `.planning/REQUIREMENTS.md` - QUA-04 body: 13→5, added the `tox.ini` deps-inert diagnosis half, routed `ruff` out
- `.planning/STATE.md` - Corrected the "false positives" framing at the former line 349; added D-08's two-causes sentence
- `tests/test_pdf_render_gate.py` - Both the canonical docstring and its inline-comment clone moved to past tense + QUA-04 rationale
- `tests/test_preview_smoke_gate.py`, `tests/test_epigraph_render_gate.py`, `tests/test_heading_depth_render_gate.py`, `tests/test_admonition_bucket_render_gate.py`, `tests/test_admonition_locale_title_precedence_gate.py` - Same tense/rationale correction

## Decisions Made

- Extended the plan's 3-bucket disposition taxonomy with two documented sub-buckets
  (`not-yet-archived record`, `unrelated topic`) because the real 255-hit sweep surfaced completed
  Phase 43/44/44.1/44.2/45/45.1 artifacts and genuinely unrelated "false positive" hits about
  unrelated test-assertion logic, neither of which fit CORRECT / REL-04 / archived cleanly. Documented
  the rationale and full location list for both extensions directly in the evidence file rather than
  silently forcing them into a mismatched bucket.
- Did not edit `tests/test_authors_pipeline_stage_gate.py` or `tests/test_package_template_routing.py`
  despite both being named in the plan's own frontmatter `files_modified` line, because Task 1's
  sweep found zero hits from the 5 mandated patterns in either file. Per the plan's own rule ("the
  inventory wins... skip any listed here that it dispositioned out of scope"), this is the correct,
  intentional outcome — a second independent proof (alongside `test_heading_depth_render_gate.py`)
  that SC#6's grep, not a file list, is authoritative.
- Reclassified `.planning/milestones/v0.7.0-REQUIREMENTS.md:226` from the generic "archived record"
  bucket to the more specific "REL-04" bucket, since it is both — REL-04 is the more informative and
  correct reason it must stay byte-unchanged.

## Deviations from Plan

None — plan executed as written. The taxonomy extension documented above is a transparent handling
of real grep output the plan's three named categories did not anticipate, not a deviation from any
task's instructions; every location the sweep found is still recorded and dispositioned, and no
file outside this plan's stated scope was edited.

## Issues Encountered

The `false positive` pattern alone returned 111 raw hits, most unrelated to the uv/NixOS diagnosis
(test-assertion-logic comments in `test_citation_render_gate.py` and similar files). Handled by
reading each hit's actual text (not assuming from the file path) and recording the "unrelated topic"
disposition explicitly for the 6 files it covers, per the plan's own instruction to judge each site
on its wording rather than force-matching.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `45.2-04` (Wave 3, post-fix main-tree proof) can proceed: this plan's edits to `.planning/` and
  `tests/` do not touch `pyproject.toml`, `tox.ini`, or `uv.lock` — Plan 02's exclusive files —
  and `pytest --collect-only` still reports 989 collected tests (matching Plan 01's pre-fix baseline)
  with `black --check tests/` green.
- `45.2-05` (SC#4/SC#7/closing verdict) inherits a corrected `ROADMAP.md`/`REQUIREMENTS.md`/`STATE.md`
  it can quote without re-deriving the module count or the diagnosis framing.
- No blockers. The `ruff` generic-linux-ELF defect remains open and routed out per D-03 — Plan 05
  files its todo.

---
*Phase: 45.2-local-toolchain-repair-tox-uv-to-tox-uv-bare*
*Completed: 2026-08-11*
