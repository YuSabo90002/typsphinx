---
phase: 45.2
slug: local-toolchain-repair-tox-uv-to-tox-uv-bare
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: false
wave_0_complete: true
created: 2026-08-11
---

# Phase 45.2 — Validation Strategy

> Reconstructed retroactively from phase artifacts (State B — no VALIDATION.md existed;
> 5 PLAN/SUMMARY pairs, `45.2-TOOLCHAIN-EVIDENCE.md`, `45.2-VERIFICATION.md` read).

**Phase shape, and why it validates unusually.** Phase 45.2 repaired local build tooling
(`pyproject.toml`'s `dev` extra, `tox.ini`, `uv.lock`) and corrected the project's own recorded
diagnosis of the defect. It ships **no runtime surface** — SC#7 asserts `typsphinx/` and
`[project].dependencies` are byte-unchanged, and `COVERAGE.md` records the API-surface detector
as `{"detected": false, "signals": []}`. Consequently most of the phase's 14 coverage IDs are
`manual_procedural`: live command transcripts of `tox`/`uv sync`/`pytest` on the maintainer's
NixOS machine, plus a real CI dispatch. Those measurements are **not convertible into tests** —
several observe a pre-fix state that no longer exists, and the rest require that specific
machine. What *is* convertible is the set of **config invariants** the fix established, which
had zero automated guards before this audit.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.1.1 (Python 3.13.13) |
| **Config file** | `pyproject.toml` → `[tool.pytest.ini_options]` |
| **Quick run command** | `uv run pytest tests/test_toolchain_config_gate.py -q` |
| **Full suite command** | `uv run pytest -q` |
| **Estimated runtime** | ~0.04 s (gate module) · ~190 s (full suite, 986 passed / 5 skipped) |

**Execution note (QUA-04's own consequence).** `uv run pytest` works in the main tree as of this
phase — that is precisely what SC#3 proved. Before it, the outer `uv run` prepended a broken
generic-linux `.venv/bin/uv` to `PATH` and 45 tests failed. The project memory recording the
`.venv/bin/python -m pytest` workaround as mandatory was retired in Plan 05 (D-12). A prior
`uv sync --extra dev` is still required.

---

## Sampling Rate

- **After every task commit:** `uv run pytest tests/test_toolchain_config_gate.py -q`
- **After every plan wave:** `uv run pytest -q`
- **Before `/gsd-verify-work`:** full suite green
- **Max feedback latency:** ~190 s (full suite); < 1 s for the toolchain gates

---

## Per-Task Verification Map

Coverage IDs are as declared in each plan's SUMMARY frontmatter. Threat refs are from
`45.2-SECURITY.md`; this phase's threat register carries no code-level mitigations (the
change is a dependency rename plus tox config), so the column reads `—` throughout.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 45.2-01-D1 | 01 | 1 | QUA-04 | — | N/A | manual_procedural | — (pre-fix baseline, see Manual-Only M1) | n/a | ✅ green |
| 45.2-01-D2 | 01 | 1 | QUA-04 | — | N/A | manual_procedural | — (pre-fix baseline, see Manual-Only M1) | n/a | ✅ green |
| 45.2-01-D3 | 01 | 1 | QUA-04 | — | N/A | manual_procedural | — (pre-fix baseline, see Manual-Only M1) | n/a | ✅ green |
| 45.2-02-D1 | 02 | 2 | QUA-04 | — | N/A | manual (env state) | — (see Manual-Only M2; declined for automation, see Declined D1) | n/a | ✅ green |
| 45.2-02-D2 | 02 | 2 | QUA-04 | — | N/A | manual (grep) | — (see Manual-Only M2; declined for automation, see Declined D2) | n/a | ✅ green |
| **45.2-02-D3** | 02 | 2 | QUA-04 | — | N/A | **unit** | `uv run pytest tests/test_toolchain_config_gate.py::test_uv_venv_lock_runner_requires_extras_forbids_deps -q` | ✅ | ✅ green |
| 45.2-02-D4 | 02 | 2 | QUA-04 | — | N/A | integration | — (see Manual-Only M3) | n/a | ✅ green |
| 45.2-02-D5 | 02 | 2 | QUA-04 | — | N/A | integration | — (see Manual-Only M3) | n/a | ✅ green |
| 45.2-03-∅ | 03 | 2 | QUA-04 (SC#6) | — | N/A | manual_procedural | — (see Manual-Only M4) | n/a | ✅ green |
| 45.2-04-D1 | 04 | 3 | QUA-04 | — | N/A | manual_procedural | — (see Manual-Only M3) | n/a | ✅ green |
| 45.2-04-D2 | 04 | 3 | QUA-04 | — | N/A | manual_procedural | — (see Manual-Only M3) | n/a | ✅ green |
| 45.2-04-D3 | 04 | 3 | QUA-04 | — | N/A | manual_procedural | — (see Manual-Only M3) | n/a | ✅ green |
| 45.2-05-D1 | 05 | 4 | QUA-04 (SC#4) | — | N/A | manual_procedural | — (see Manual-Only M5) | n/a | ✅ green |
| 45.2-05-D2 | 05 | 4 | QUA-04 (SC#7) | — | N/A | unit (partial) | `uv run pytest tests/test_preview_version_sync.py -q` (covers the `@preview`-count clause only) | ✅ | ✅ green |
| **45.2-05-D3** | 05 | 4 | QUA-04 | — | N/A | **unit** | `uv run pytest tests/test_toolchain_config_gate.py::test_testenv_package_is_editable_not_wheel -q` | ✅ | ✅ green |
| 45.2-05-D4 | 05 | 4 | QUA-04 | — | N/A | unit (file-existence) | `test -f .planning/todos/pending/2026-08-11-*.md` | ✅ | ✅ green |
| 45.2-05-D5 | 05 | 4 | QUA-04 (D-12) | — | N/A | manual_procedural | — (see Manual-Only M6) | n/a | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Bold rows** are the two coverage IDs this audit converted from a one-shot manual command into a
committed regression gate.

---

## Wave 0 Requirements

- [x] `tests/test_toolchain_config_gate.py` — new module, guards 45.2-02-D3 and 45.2-05-D3
- [x] `tests/conftest.py` — no change needed; the gate module uses no fixtures
- [x] Framework install — none needed; stdlib `configparser` + `pathlib` only

Existing infrastructure covered everything else. The new module performs no subprocess, no
network, and no `typst-py`/`sphinx-build` call, so it never skips.

### Regression-detection proof (anti-vacuity)

Both gates were mutation-tested against **temporary copies** of `tox.ini` — the real file was
never modified. Five regression variants, all caught:

| Mutation (applied to a tmp copy) | `..._requires_extras_forbids_deps` | `..._package_is_editable_not_wheel` |
|---|---|---|
| `[testenv] package = editable` → `wheel` | pass | **CAUGHT** |
| `→ wheel` + `wheel_build_env = .pkg` restored (true pre-45.2 state) | pass | **CAUGHT** |
| `package` key deleted entirely | pass | **CAUGHT** |
| `[testenv:lint]` reverts `extras` → `deps` | **CAUGHT** | pass |
| A *new* `[testenv:audit]` added with `deps` | **CAUGHT** | pass |

The last row is the load-bearing one: the G3 gate discovers `runner = uv-venv-lock-runner`
sections dynamically (7 today), so environments added in future phases are covered without
editing the test. That is the invariant Plan 02's `patterns-established` block asked for.

Each gate also carries an explicit vacuous-pass guard: a missing or unparseable `tox.ini`, an
empty `uv-venv-lock-runner` section set, or a missing `[testenv]` section each fail loudly
rather than iterating zero items.

---

## Manual-Only Verifications

| ID | Behavior | Requirement | Why Manual | Test Instructions |
|----|----------|-------------|------------|-------------------|
| M1 | Pre-fix baseline: `.venv/bin/uv` is a generic-linux ELF exiting 127; every `tox` env fails identically; 45 pytest failures confined to the 5 `uv`-invoking modules (01-D1/D2/D3) | QUA-04 | **Permanently unrepeatable.** These observe a state this phase destroyed. Re-creating it means reinstalling the broken `tox-uv` — i.e. reintroducing the defect. | Read `45.2-TOOLCHAIN-EVIDENCE.md` §§ Step 0–3. Verbatim command + output for each. |
| M2 | A clean `uv sync --extra dev` leaves no `.venv/bin/uv`, no `.venv/bin/uvx`, and no installed `uv` distribution (02-D1) | QUA-04 (SC#2) | Asserts **filesystem/environment state after a real sync**, not repository content. A test asserting `.venv` shape would fail in any environment that syncs differently (CI installs `uv` deliberately via `astral-sh/setup-uv`), and would pass vacuously where `.venv` is absent. | `uv sync --extra dev`, then `test ! -e .venv/bin/uv && test ! -e .venv/bin/uvx`, then `python -c "import importlib.metadata as m; m.distribution('uv')"` → must raise `PackageNotFoundError`. Transcript: Evidence Step 5. |
| M3 | `tox -e type/lint/docs-html/docs-pdf` provision and reach their commands with no `TOX_UV_PATH`; `black` executes and passes; full-suite pytest goes 45 failed → 0 under an outer `uv run` (02-D4/D5, 04-D1/D2/D3) | QUA-04 (SC#1, SC#3) | Requires **the maintainer's NixOS machine**. The whole point of SC#1 is that this environment resolves `uv` differently from CI (`shutil.which` → nix-store vs. runner-installed). A test invoking `tox` from inside pytest would nest environments, take minutes, and assert nothing CI does not already assert by running `tox` directly. | On the maintainer's machine: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run tox -e type,lint,docs-html,docs-pdf`; then `uv run pytest -q`. Expect 986 passed / 5 skipped as of this audit (984 + the 2 gates added here). `ruff check .` will still fail — see Known Environmental Defect below. Transcripts: Evidence Steps 6–7. |
| M4 | The wrong "unfixable NixOS false positives" diagnosis is corrected wherever recorded, found by a **discovery-time** repo-wide grep (Plan 03, SC#6) | QUA-04 (SC#6) | SC#6 mandates a grep run *at execution time*, over 5 patterns, with each of 248 unique locations dispositioned by reading its actual wording. That is an **action**, not an invariant — a test could only re-assert today's disposition, and would break on every unrelated future use of the phrase "false positive" (111 raw hits already, most about unrelated test-assertion logic). | Read `45.2-TOOLCHAIN-EVIDENCE.md` § Step 4 for the full 248-location disposition table. |
| M5 | A real `workflow_dispatch` CI run at this phase's SHA is green, and its `uv-sync>` line proves CI reaches `uv` by a different route than local (05-D1) | QUA-04 (SC#4) | SC#4 explicitly forbids arguing this from the workflow file — "the precise error v0.7.0 made with REL-04". It must be **observed on a real run**. | `gh run view 31445582363`; compare the `uv-sync>`/`freeze>` lines against runs `31444258982`/`31444883849`. Transcript: Evidence Step 8. |
| M6 | The project memory `pytest-no-outer-uv-run-in-main-tree.md` is retired, recording the prohibition as lifted (05-D5, D-12) | QUA-04 | The file lives at `~/.claude/projects/.../memory/`, **outside the repository**. It is not version-controlled here and cannot be asserted by a test in this repo. | Read the memory file and `MEMORY.md`'s index line; both must describe the prohibition as lifted, citing the post-fix counts. |

### Declined for automation (owner decision, this audit, 2026-08-11)

Three further gaps were identified as statically automatable and **explicitly declined** by the
project owner in favour of the two above. Recording them so a future audit does not silently
re-litigate the same call:

| ID | Invariant | Would have guarded | Residual risk |
|----|-----------|--------------------|---------------|
| D1 | `pyproject.toml`'s `dev` extra names `tox-uv-bare`, never `tox-uv` | 02-D1 / SC#2 | A revert to `tox-uv` reintroduces the broken `.venv/bin/uv` and re-breaks all local `tox` + 45 pytest tests. **CI would stay green** — the defect is NixOS-specific, so only the maintainer's machine would show it. CLAUDE.md's "Conventions & gotchas" carries the prose warning ("Do not 'simplify' it back to `tox-uv`"). |
| D2 | `tox.ini`'s `requires = tox-uv-bare~=1.35` stays comma-free and `SpecifierSet`-equivalent to `>=1.35,<2`, with its explanatory comment intact | 02-D2 / SC#5 | A "consistency" edit to `>=1.35,<2` makes tox's ini list-loader parse two bogus requirements and **tox fails to start at all** — loud, immediate, and equally visible on CI. The in-file comment documents the hazard. |
| D3 | `[project].dependencies` carries no toolchain package (`uv`/`tox`/`tox-uv*`) | 05-D2 / SC#7 | Would ship dev tooling to every typsphinx user. Not currently guarded; the `@preview`-count clause of SC#7 *is* guarded, by `tests/test_preview_version_sync.py`. |

---

## Known Environmental Defect (not a validation gap)

`.venv/bin/ruff` cannot execute on this NixOS machine — a generic-linux ELF hitting the same
stub-ld defect class QUA-04 fixed for `uv`. It is **filed, unfixed, and deliberately out of
scope** (D-03): `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md`
(with the sibling `tox -e py312` interpreter-download defect folded in). `ruff check .` on
`tests/test_toolchain_config_gate.py` therefore could not be run locally during this audit; CI
runs it. `black --check` passed locally.

A second filed, unrelated defect: `tests/test_docs_contract_claims_gate.py` fails on Windows CI
via `Path.relative_to()`'s backslash separators —
`.planning/todos/pending/2026-08-11-windows-path-separator-breaks-contract-claims-gate.md`.
Neither defect belongs to QUA-04, and neither is reframed here as an environmental false
positive — that reframing is exactly the error SC#6 corrected.

---

## Validation Sign-Off

- [x] All tasks have an `<automated>` verify, a Wave 0 dependency, or a justified Manual-Only entry
- [x] Sampling continuity: the two new gates run in < 1 s and cover the phase's only two durable code-adjacent invariants
- [x] Wave 0 covers the MISSING references the owner elected to fill (G3, G4); the three declined are recorded above with residual risk stated
- [x] No watch-mode flags
- [x] Feedback latency < 1 s (gate module) / ~190 s (full suite)
- [ ] `nyquist_compliant: true` — **not set.** 11 of 17 map rows remain manual-only, and 3 automatable gaps were declined. This is PARTIAL by design, not by omission.

**Approval:** approved 2026-08-11

---

## Validation Audit 2026-08-11

| Metric | Count |
|--------|-------|
| Coverage IDs audited | 17 (14 declared + Plan 03's uncovered SC#6 work + 2 new gates) |
| Gaps found | 5 |
| Resolved (automated) | 2 (G3 → 02-D3, G4 → 05-D3) |
| Declined by owner | 3 (recorded above with residual risk) |
| Escalated | 0 |
| Manual-only (justified) | 6 groups (M1–M6), covering 11 map rows |
| Full suite after audit | 986 passed / 5 skipped / 0 failed (was 984/5/0) |
| Implementation files changed | 0 |
