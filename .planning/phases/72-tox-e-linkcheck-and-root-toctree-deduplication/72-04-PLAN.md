---
phase: 72-tox-e-linkcheck-and-root-toctree-deduplication
plan: 04
type: execute
wave: 3
depends_on: ["72-01", "72-03"]
files_modified:
  - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-TOCTREE-EVIDENCE.md
  - .planning/todos/pending/2026-09-13-html-and-typst-toctree-parents-diverge-after-dedup.md
autonomous: true
requirements: [DOC-18]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Evidence last (ROADMAP binding ordering): this plan runs in wave 3, after both docs edits have merged. The tip it builds carries 72-01''s index.rst edit and 72-03''s contributing.rst line, so one base/tip pair covers both.'
    - 'SC#3: PHASE_BASE_SHA and the phase tip are each materialised with git archive and built clean, back to back, with the identical HTML command, in this one worktree venv, whose interpreter is recorded. Under LANG=C LC_ALL=C the base count of `document is referenced in multiple toctrees` is at least 1, the tip count is 0, and the tip N in `build succeeded, N warnings.` equals the base N.'
    - 'SC#3 edit shape: docs/source/index.rst loses exactly five entry lines and gains none between PHASE_BASE_SHA and the tip; the two section-index files are absent from the diff; conf.py gains no theme or sidebar key.'
    - 'SC#4 HTML: in the tip index.html the sidebar-tree holds exactly one link to each of the five pages, each nested in its section index list item; the control user_guide/output_layout appears once. The counts come from the markup via the stdlib html.parser, not from prose.'
    - 'SC#4 Typst: in the clean tip -b typst build, root index.typ has no include line for any of the five pages; the base had five dead state-guarded ones. Each page is guarded in user_guide/index.typ or examples/index.typ, and the master wrapper include-edges state holds exactly one edge per page, from its section index.'
    - 'SC#4 divergence (72-RESEARCH.md PC-2): the parent of both examples/basic and examples/advanced is transcribed at base and at tip, as Sphinx HTML resolves it and as the Typst edge map records it. It is re-measured, not fixed; a todo is filed only if a divergence survives at the tip.'
    - 'Edge (DOC-18 ordering): when a page sat in two toctrees, the console `selecting:` token is `max(parents)` in Sphinx''s log-only _check_toc_parents (sphinx/environment/__init__.py:940-957). It is transcribed but never read as the parent HTML uses. The HTML parent is read from env.collect_relations(), the pre-order traversal the HTML builder consumes (builders/html/__init__.py:492), and from _get_toctree_ancestors().'
    - 'Edge (DOC-18 precision): the warning count is an exact integer parsed from the English summary line (`build succeeded.` parses as 0); the base and tip must be equal. A difference caused only by an intersphinx inventory-fetch warning re-takes the pair, at most three pairs, every pair kept; any other difference halts to the owner.'
  artifacts:
    - path: .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-TOCTREE-EVIDENCE.md
      provides: 'the SC#3 base/tip pair, SC#4 sidebar and Typst comparisons, the parent-divergence table and its disposition'
      contains: 'SC3_VERDICT = '
  key_links:
    - from: '72-BASE-EVIDENCE.md'
      to: '72-TOCTREE-EVIDENCE.md'
      via: 'PHASE_BASE_SHA and the wave-1 base counts, cross-checked against this plan''s same-environment rebuild'
      pattern: 'W1_BASE_MATCH = '
    - from: 'sphinx env.collect_relations()'
      to: 'the HTML parent transcribed for SC#4'
      via: 'unpickling <out>/.doctrees/environment.pickle in the same venv'
      pattern: 'TIP_HTML_PARENT_EXAMPLES_BASIC = '
  prohibitions:
    - statement: 'MUST NOT substitute theme or sidebar configuration, a :maxdepth: change, suppress_warnings or a -q-quieted build for the toctree fix or its proof, and MUST NOT act on the examples parent divergence beyond re-measuring it and, if it survives, filing one todo.'
      requirement_id: DOC-18
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Prove DOC-18 on the phase tip against the base: SC#3's clean-build pair and SC#4's three bullets.

Purpose: this is the audit of 72-01's and 72-03's edits, so it runs a wave after both. Both base and
tip are built here, back to back, in one venv, which makes "the same environment" literal. 72-01's
earlier base build stays the positive control taken before any edit. This plan also resolves the
parent-divergence question with the relation Sphinx's HTML builder actually uses. The owner's
sidebar look stays the UAT human check.

Output: `72-TOCTREE-EVIDENCE.md`, plus one todo only if a divergence survives.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run everything through `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Build shape.** Use 72-01's shape exactly. Build from this worktree's root, through this venv,
against an absolute `docs/source` inside a `git archive` snapshot, into an absolute output directory
under your scratch directory that is absent before each build. Prefix every build and every grep with
`LANG=C LC_ALL=C`, and never pass `-q`. The English `build succeeded` line must be present in every
log, or that log's counts are void.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put any explanation on
the next line. The verify fails on any key line containing an opening parenthesis.

**Scratch.** Use your own `mktemp -d` directory, recorded as `SCRATCH_72_04`, and prefix helper
files `p7204_`. Helpers are never committed.

**Commits.** Plain `git commit`. Never a GSD commit helper. Never write `72-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: document is referenced in multiple toctrees -->
<!-- planner-discipline-allow: \ -->
The Sphinx message is named in actions because the executor must grep for it. The gates require it
positive on the base and zero on the tip. The backslash is the shell escape of the quoted Typst
edge keys that the gates count.

## Planning-time measurements (2026-09-13, HEAD `704beaba`; context only, re-measure, never trust)

- **Base console lines:** `selecting: index <- examples/basic` and `… <- examples/advanced`, and
  `selecting: user_guide/index <- …` for the three User Guide pages. In Sphinx 9.1.0 that token is
  `max(parents)`, printed by `_check_toc_parents`, which only logs. `max(['examples/index', 'index'])`
  is `index`.
- **Base `collect_relations()`:** unpickled from `<out>/.doctrees/environment.pickle`, it gave parent
  `examples/index` for both Examples pages and `user_guide/index` for the four User Guide pages. The
  HTML builder takes its parent/prev/next links from exactly this relation
  (`sphinx/builders/html/__init__.py:492`, `:570`, `:599`).
- **Typst edge map, base and tip:** `examples/index#0>examples/basic` and
  `examples/index#0>examples/advanced`. The state line is byte-identical between base and tip
  (72-RESEARCH.md).
- **Tip sidebar:** each of the five pages counted 1 under its section index; the control counted 1.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: build base and tip back to back in one environment and compare SC#3's two numbers</name>
  <files>.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-TOCTREE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md (`PHASE_BASE_SHA`, `BASE_MULTI_TOCTREE_COUNT`, `BASE_WARNING_COUNT`, `### Build command`, `### Counted warnings`)
    - .planning/ROADMAP.md Phase 72 SC#3 and v0.9.5 constraints 7 and 8
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md § "Common Pitfalls" 1 and § "Anti-Patterns"
    - docs/source/index.rst and docs/source/contributing.rst at HEAD (both edits must be present)
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record:
   - `date -u +%FT%TZ`, `pwd -P` under `.claude/worktrees/`, `test -f .git; echo "exit:$?"` and the shim check
   - the provisioning line with its exit
   - `PYVENV_HOME` and `PYVENV_VERSION_INFO` from `.venv/pyvenv.cfg`
   - `SPHINX_VERSION`, `DOCUTILS_VERSION` and `FURO_VERSION` via `uv run python -c`
   - `printenv SPHINX_LANGUAGE READTHEDOCS_LANGUAGE; echo "exit:$?"`, which must print only `exit:1`
   - before any commit, `TIP_SHA = ` followed by `git rev-parse HEAD`, and `SCRATCH_72_04 = ` followed by the path `mktemp -d` printed

   Read `PHASE_BASE_SHA` from `72-BASE-EVIDENCE.md`.

2. **`## Tip carries both edits`.** Record:
   - `git diff --stat "$PHASE_BASE_SHA" "$TIP_SHA" -- docs/`, which lists `docs/source/index.rst` and `docs/source/contributing.rst`, plus `docs/source/conf.py` only if 72-02 recorded a D-02 key
   - `git grep -c 'tox -e linkcheck' "$TIP_SHA" -- docs/source/contributing.rst`, which must be 1

   If either edit is missing, write `## HALT: tip lacks an edit` and stop.

3. **`## Edit shape`.** Record:
   - `git diff --numstat "$PHASE_BASE_SHA" "$TIP_SHA" -- docs/source/index.rst`, which must be 0 added and 5 removed, with the five removed lines
   - `git diff --name-only "$PHASE_BASE_SHA" "$TIP_SHA" -- docs/source/user_guide/index.rst docs/source/examples/index.rst`, which must be empty
   - `git diff "$PHASE_BASE_SHA" "$TIP_SHA" -- docs/source/conf.py`, which must be empty or add only a `linkcheck_*` line and its comment

   Key `SC3_EDIT_SHAPE = MET`.

4. **`## HTML pair 1`.**
   - `git archive "$PHASE_BASE_SHA" | tar -x -C "$S/tree-base"` and `git archive "$TIP_SHA" | tar -x -C "$S/tree-tip"`.
   - Run 72-01's `### Build command` twice from the worktree root, base first, then tip, with only the tree and output paths changed: outputs `$S/html-base` and `$S/html-tip`, logs `$S/p7204_html-base.out`/`.err` and `$S/p7204_html-tip.out`/`.err`.
   - Record both exits and both commands verbatim.
   - Keys:
     - `MULTI_TOCTREE_BASE` and `MULTI_TOCTREE_TIP`: `LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees'` over each `.out`, with `|| true`
     - `WARNINGS_BASE` and `WARNINGS_TIP`: parsed from each English `build succeeded` line, with `build succeeded.` meaning 0
   - Transcribe both summary lines and the base's matching lines verbatim.
   - Positive control first: `MULTI_TOCTREE_BASE` must be at least 1 before the tip's count is read as meaningful.

5. **`## Warning lines`.** Take the `WARNING:` and `ERROR:` lines of each `.err`, replace each tree's absolute root with `<tree>`, and compare the two sets. Key `WARNING_LINES_IDENTICAL = yes` or `no`. It is recorded, not gating; SC#3 gates on the count.

6. **Re-take rule (the planner's rule, evidence-preserving).**
   - If `WARNINGS_BASE` differs from `WARNINGS_TIP`, and every differing warning line is an intersphinx inventory fetch failure (network), re-take both builds clean as `## HTML pair 2`, and if needed `## HTML pair 3`, keeping every earlier pair. Pair n's outputs are `$S/html-base-n` and `$S/html-tip-n`, and its logs are `$S/p7204_html-base-n.out`/`.err` and `$S/p7204_html-tip-n.out`/`.err`. Pair 1 uses the unsuffixed names. The keys in steps 4, 7 and 8 then describe the pair used. Key `SC3_PAIR`: the pair used, `1` when no re-take happened.
   - Any other difference, or no equal pair within three: write `## HALT: SC#3 warning count differs`, commit, and return a blocking checkpoint.

7. **`## Cross-check with wave 1`.** Key `W1_BASE_MATCH = yes` when `MULTI_TOCTREE_BASE` equals `BASE_MULTI_TOCTREE_COUNT` and `WARNINGS_BASE` equals `BASE_WARNING_COUNT`. Otherwise `no`, with both values on the next lines; it is recorded, and SC#3 is judged on this plan's same-environment pair.

8. **`## SC#3 verdict`.** `SC3_VERDICT = MET` when `SC3_EDIT_SHAPE = MET`, `MULTI_TOCTREE_BASE` is at least 1, `MULTI_TOCTREE_TIP = 0`, and `WARNINGS_TIP` equals `WARNINGS_BASE`, both builds from this one venv.

9. Write `72-TOCTREE-EVIDENCE.md` under `# Phase 72 — Toctree Evidence (DOC-18, SC#3 and SC#4)` and commit.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-TOCTREE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; P="$(k PHASE_BASE_SHA "$D/72-BASE-EVIDENCE.md")" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; T="$(k TIP_SHA)" &amp;&amp; [ "${#T}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$P" "$T" &amp;&amp; git merge-base --is-ancestor "$T" HEAD &amp;&amp; [ "$(git diff --numstat "$P" "$T" -- docs/source/index.rst | cut -f1,2)" = "$(printf '0\t5')" ] &amp;&amp; [ -z "$(git diff --name-only "$P" "$T" -- docs/source/user_guide/index.rst docs/source/examples/index.rst)" ] &amp;&amp; [ "$(git grep -c 'tox -e linkcheck' "$T" -- docs/source/contributing.rst | cut -d: -f3)" = 1 ] &amp;&amp; [ -z "$(git diff "$P" "$T" -- docs/source/conf.py | grep -E '^[-+][^-+]' | grep -vE '^\+(linkcheck_(timeout|retries|rate_limit_timeout) = |#)')" ] &amp;&amp; S="$(k SCRATCH_72_04)" &amp;&amp; PR="$(k SC3_PAIR)" &amp;&amp; case "$PR" in 1) SUF= ;; 2|3) SUF="-$PR" ;; *) false ;; esac &amp;&amp; BO="$S/p7204_html-base$SUF.out" &amp;&amp; TO="$S/p7204_html-tip$SUF.out" &amp;&amp; test -s "$BO" &amp;&amp; test -s "$TO" &amp;&amp; MB="$(LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees' "$BO" || true)" &amp;&amp; MT="$(LANG=C LC_ALL=C grep -c 'document is referenced in multiple toctrees' "$TO" || true)" &amp;&amp; [ "$MB" -ge 1 ] &amp;&amp; [ "$MT" = 0 ] &amp;&amp; [ "$MB" = "$(k MULTI_TOCTREE_BASE)" ] &amp;&amp; [ "$(k MULTI_TOCTREE_TIP)" = 0 ] &amp;&amp; n() { l="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$1" || true)"; [ -n "$l" ] || return 1; v="$(printf '%s\n' "$l" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')"; [ -n "$v" ] || { [ "$l" = 'build succeeded.' ] &amp;&amp; v=0; }; printf '%s' "$v"; } &amp;&amp; WB="$(n "$BO")" &amp;&amp; WT="$(n "$TO")" &amp;&amp; [ "$WB" = "$WT" ] &amp;&amp; [ "$WB" = "$(k WARNINGS_BASE)" ] &amp;&amp; [ "$WT" = "$(k WARNINGS_TIP)" ] &amp;&amp; [ -n "$(k PYVENV_VERSION_INFO)" ] &amp;&amp; [ -n "$(k W1_BASE_MATCH)" ] &amp;&amp; [ -n "$(k WARNING_LINES_IDENTICAL)" ] &amp;&amp; [ "$(k SC3_EDIT_SHAPE)" = MET ] &amp;&amp; [ "$(k SC3_VERDICT)" = MET ] &amp;&amp; for s in '## Head check and provisioning' '## Tip carries both edits' '## Edit shape' '## HTML pair 1' '## Warning lines' '## Cross-check with wave 1' '## SC#3 verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$T" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `PHASE_BASE_SHA` or `TIP_SHA` missing, or not an ancestor chain base, then tip, then HEAD
- index.rst numstat between base and tip not 0 added and 5 removed
- a section-index file in the diff
- the tip lacking the contributing.rst linkcheck line
- conf.py changed other than by a timing key and its comment
- `SC3_PAIR` outside 1 to 3, or its base or tip stdout log missing
- the base count 0, the tip count non-zero, or either differing from the recorded keys
- an English summary line missing, or the base and tip warning counts differing from each other or from the keys
- interpreter, cross-check or warning-line keys empty
- a verdict not MET, a section missing, a product-tree change by this plan, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `TIP_SHA` carries both docs edits, and the index.rst diff from `PHASE_BASE_SHA` is 0 added and 5 removed.
    - In one venv, with the recorded interpreter, `MULTI_TOCTREE_BASE` is at least 1, `MULTI_TOCTREE_TIP = 0`, and `WARNINGS_TIP` equals `WARNINGS_BASE`, each re-derived by the verify from the saved C-locale logs.
    - `W1_BASE_MATCH` and `WARNING_LINES_IDENTICAL` are recorded, and `SC3_VERDICT = MET`.
  </acceptance_criteria>
  <done>SC#3 is proven on a same-environment clean pair with its positive control.</done>
</task>

<task type="auto">
  <name>Task 2: SC#4 in the sidebar markup and in the Typst output</name>
  <files>.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-TOCTREE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md § "Code Examples" "Sidebar-scoped link count" (copy verbatim) and "Typst side: dead include() lines disappear, edges stay stable"
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md § "Base Typst build" and § "Base sidebar"
    - .planning/ROADMAP.md Phase 72 SC#4
  </read_first>
  <action>
1. **`## Sidebar`.** Copy the parser from 72-RESEARCH.md § "Sidebar-scoped link count" verbatim to `$S/p7204_sidebar.py`. Run `uv run python "$S/p7204_sidebar.py"` over `$S/html-base/index.html` and over `$S/html-tip/index.html`, using the pair `SC3_PAIR` names, and transcribe both outputs. Keys, in the page order configuration, builders, templates, basic, advanced:
   - `SIDEBAR_BASE_COUNTS` and `SIDEBAR_TIP_COUNTS`
   - `SIDEBAR_TIP_PARENTS`, the tip's single parent href per page, space-separated
   - `SIDEBAR_BASE_CONTROL` and `SIDEBAR_TIP_CONTROL`, the `user_guide/output_layout.html` counts

   The tip must show a count of 1 for every page, with parent `user_guide/index.html` for the first three and `examples/index.html` for the last two, and a control of 1.

2. **`## Typst pair`.** Build `-b typst` for base and tip back to back, clean, from the same trees, into `$S/typst-base` and `$S/typst-tip` with `LANG=C LC_ALL=C uv run sphinx-build -b typst <tree>/docs/source <out>`. Record both exits, which must be 0. Then:
   - Keys `ROOT_INCLUDE_LINES_BASE` and `ROOT_INCLUDE_LINES_TIP`: `grep -c 'include("'` over each `index.typ`.
   - For each page P, `grep -cF "\"index#0>P\""` over each `index.typ`. Key `ROOT_DEAD_INCLUDES_BASE`, the sum, which should equal 72-01's `BASE_ROOT_DEAD_INCLUDES`, and `ROOT_DEAD_INCLUDES_TIP`, which must be 0.
   - For each page, the guard line in its tip section index, `grep -F "\"S#0>P\""` over `user_guide/index.typ` or `examples/index.typ`, verbatim. Key `SECTION_GUARDS_TIP`, the count found, which must be 5.
   - The include-edges state line of each `typsphinx.typ`, verbatim. Parse the tip line's quoted edge strings with `uv run python`, and for each page count the edges ending in `#0>P`. Key `EDGES_PER_PAGE_TIP`, space-separated in page order, which must be `1 1 1 1 1`, each of them `S#0>P` with S the page's section index. Key `EDGE_STATE_IDENTICAL = yes` or `no`: the base and tip state lines compared byte for byte, recorded.
   - `LANG=C LC_ALL=C diff -rq "$S/typst-base" "$S/typst-tip" || true`, transcribed. Key `TYPST_DIFF_FILES`: the differing paths relative to the output root, space-separated. It is recorded; it is expected to hold `index.typ` and `contributing.typ`.

3. **`## SC#4 HTML and Typst verdicts`.**
   - `SC4_HTML_VERDICT = MET` when the tip counts are `1 1 1 1 1`, the parents are as stated, the control is 1, and the base counts are each at least 2.
   - `SC4_TYPST_VERDICT = MET` when `ROOT_DEAD_INCLUDES_TIP = 0`, `ROOT_DEAD_INCLUDES_BASE` is at least 1, `SECTION_GUARDS_TIP = 5` and `EDGES_PER_PAGE_TIP = 1 1 1 1 1`.

   Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-TOCTREE-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; S="$(k SCRATCH_72_04)" &amp;&amp; PR="$(k SC3_PAIR)" &amp;&amp; case "$PR" in 1) SUF= ;; 2|3) SUF="-$PR" ;; *) false ;; esac &amp;&amp; test -f "$S/p7204_sidebar.py" &amp;&amp; O="$(uv run python "$S/p7204_sidebar.py" "$S/html-tip$SUF/index.html")" &amp;&amp; for e in "user_guide/configuration.html: count=1 parents=['user_guide/index.html']" "user_guide/builders.html: count=1 parents=['user_guide/index.html']" "user_guide/templates.html: count=1 parents=['user_guide/index.html']" "examples/basic.html: count=1 parents=['examples/index.html']" "examples/advanced.html: count=1 parents=['examples/index.html']" "user_guide/output_layout.html: count=1 parents=['user_guide/index.html']"; do [ "$(printf '%s\n' "$O" | grep -cxF "$e")" = 1 ] || { echo "sidebar: $e"; exit 1; }; done &amp;&amp; [ "$(k SIDEBAR_TIP_COUNTS)" = '1 1 1 1 1' ] &amp;&amp; [ "$(k SIDEBAR_TIP_CONTROL)" = 1 ] &amp;&amp; for c in $(k SIDEBAR_BASE_COUNTS); do [ "$c" -ge 2 ] || { echo "base sidebar $c"; exit 1; }; done &amp;&amp; TB="$S/typst-base" &amp;&amp; TT="$S/typst-tip" &amp;&amp; test -s "$TT/index.typ" &amp;&amp; test -s "$TB/index.typ" &amp;&amp; db=0 &amp;&amp; for p in user_guide/configuration user_guide/builders user_guide/templates examples/basic examples/advanced; do [ "$(grep -cF "\"index#0&gt;$p\"" "$TT/index.typ" || true)" = 0 ] || { echo "tip root include: $p"; exit 1; }; db=$((db + $(grep -cF "\"index#0&gt;$p\"" "$TB/index.typ" || true))); done &amp;&amp; [ "$db" -ge 1 ] &amp;&amp; [ "$db" = "$(k ROOT_DEAD_INCLUDES_BASE)" ] &amp;&amp; [ "$(k ROOT_DEAD_INCLUDES_TIP)" = 0 ] &amp;&amp; sg=0 &amp;&amp; for p in configuration builders templates; do sg=$((sg + $(grep -cF "\"user_guide/index#0&gt;user_guide/$p\"" "$TT/user_guide/index.typ" || true))); done &amp;&amp; for p in basic advanced; do sg=$((sg + $(grep -cF "\"examples/index#0&gt;examples/$p\"" "$TT/examples/index.typ" || true))); done &amp;&amp; [ "$sg" = 5 ] &amp;&amp; [ "$(k SECTION_GUARDS_TIP)" = 5 ] &amp;&amp; EP="$(grep -F 'state("typsphinx:include-edges", ()).update(' "$TT/typsphinx.typ" | uv run python -c 'import re,sys; e=re.findall(r"\"([^\"]+)\"", sys.stdin.read()); sec={"user_guide/configuration":"user_guide/index","user_guide/builders":"user_guide/index","user_guide/templates":"user_guide/index","examples/basic":"examples/index","examples/advanced":"examples/index"}; out=[]
for p,s in sec.items():
    m=[x for x in e if x.endswith("#0>"+p)]; assert m==[s+"#0>"+p], (p,m); out.append(str(len(m)))
print(" ".join(out))')" &amp;&amp; [ "$EP" = '1 1 1 1 1' ] &amp;&amp; [ "$(k EDGES_PER_PAGE_TIP)" = '1 1 1 1 1' ] &amp;&amp; [ -n "$(k EDGE_STATE_IDENTICAL)" ] &amp;&amp; [ -n "$(k TYPST_DIFF_FILES)" ] &amp;&amp; [ "$(k SC4_HTML_VERDICT)" = MET ] &amp;&amp; [ "$(k SC4_TYPST_VERDICT)" = MET ] &amp;&amp; for s in '## Sidebar' '## Typst pair' '## SC#4 HTML and Typst verdicts'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, a Python AssertionError, or one of `sidebar: <line>`, `base sidebar <n>`, `tip root include: <page>` or `missing section: <name>` printed. Each of these makes it fail:
- the tip sidebar parse lacking any of the six exact count-and-parent lines
- the tip counts or control keys wrong, or a base count below 2
- a root include guard for any of the five pages left in the tip index.typ, or none in the base
- the dead-include keys mismatching the re-count
- fewer or more than five section-index guards at the tip
- a page with other than exactly one edge in the tip state, or that edge not from its section index
- an edge-state or diff-files key empty, a verdict not MET, a section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
    <human-check>At UAT the owner opens the tip HTML build's `index.html` and confirms the furo sidebar shows Configuration, Builders, Templates and Output Layout once each under User Guide, and Basic and Advanced once each under Examples. This is the human check the 2026-08-16 todo names.</human-check>
  </verify>
  <acceptance_criteria>
    - The tip sidebar holds exactly one link per page, nested under its section index, and the `output_layout` control holds one link. The base shows each page at least twice.
    - The tip root `index.typ` has no guard for the five pages, the tip section indexes carry five guards, and the tip include-edges state holds exactly one edge per page, from its section index.
    - `SC4_HTML_VERDICT = MET` and `SC4_TYPST_VERDICT = MET`.
  </acceptance_criteria>
  <done>Each page appears once, in the sidebar markup and in the Typst output, through its section index.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure the parent of both Examples pages as HTML and Typst record it, and dispose of the divergence</name>
  <files>.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-TOCTREE-EVIDENCE.md, .planning/todos/pending/2026-09-13-html-and-typst-toctree-parents-diverge-after-dedup.md</files>
  <read_first>
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md § "Premise Check" PC-2
    - .planning/todos/completed/2026-08-16-root-toctree-duplicates-section-children-in-html-sidebar.md § "The PDF is NOT affected", observation 2 (the divergence as first recorded)
    - The installed Sphinx sources, read only: `sphinx/environment/__init__.py` `_check_toc_parents` and `collect_relations`, `sphinx/environment/adapters/toctree.py` `_get_toctree_ancestors`, and `sphinx/builders/html/__init__.py` lines 490-600
    - .planning/REQUIREMENTS.md § Out of Scope (the divergence row)
  </read_first>
  <action>
1. **`## Parent notions`.** For each of the base and tip HTML builds of pair `SC3_PAIR`, run `uv run python -c` to unpickle `<out>/.doctrees/environment.pickle`. For each of the six pages (the five plus `user_guide/output_layout`), print:
   - (a) the relations parent `env.collect_relations()[d][0]`
   - (b) the immediate ancestor, the second element of `sphinx.environment.adapters.toctree._get_toctree_ancestors(env.toctree_includes, d)`

   Add two more columns:
   - (c) the base console `selecting:` token from `$S/p7204_html-base.out`, with `none` at the tip, where no such message exists
   - (d) the Typst edge parent, the section before `#0>` in the matching edge of each build's include-edges state

   Transcribe a table per build with columns page, (a), (b), (c), (d). Beneath it, quote with file and line numbers:
   - `_check_toc_parents` logs `max(parents)` and feeds nothing
   - `collect_relations` is a pre-order traversal from `root_doc`, and the HTML builder reads its parent/prev/next from it
   - `_get_toctree_ancestors` builds a last-assignment parent map

   Transcribe these, and do not argue beyond them.

2. **Keys.**
   - `BASE_CONSOLE_SELECTING_EXAMPLES_BASIC` and `BASE_CONSOLE_SELECTING_EXAMPLES_ADVANCED`
   - `BASE_HTML_PARENT_EXAMPLES_BASIC` and `BASE_HTML_PARENT_EXAMPLES_ADVANCED` from (a)
   - `TIP_HTML_PARENT_EXAMPLES_BASIC` and `TIP_HTML_PARENT_EXAMPLES_ADVANCED` from (a)
   - `TIP_ANCESTOR_PARENT_EXAMPLES_BASIC` and `TIP_ANCESTOR_PARENT_EXAMPLES_ADVANCED` from (b)
   - `TYPST_PARENT_EXAMPLES_BASIC` and `TYPST_PARENT_EXAMPLES_ADVANCED` from (d) at the tip

3. **`## Divergence disposition`.**
   - `DIVERGENCE_SURVIVES = no` when, at the tip, (a) and (b) equal (d) for all six pages. Then `DIVERGENCE_TODO = none`, and no todo is filed.
   - Otherwise `DIVERGENCE_SURVIVES = yes`. Create `.planning/todos/pending/2026-09-13-html-and-typst-toctree-parents-diverge-after-dedup.md` with frontmatter keys `created` (the `date -u` timestamp), `title`, `area: docs`, `severity: minor` and `files`, and a body transcribing both tables and the three Sphinx facts. Set `DIVERGENCE_TODO` to that path. Nothing more is done here (REQUIREMENTS Out of Scope).

   State in one sentence whether the 2026-08-16 observation, "Sphinx HTML selected `index`", was read from the console token (c).

4. **`## SC#4 verdict`.** `SC4_VERDICT = MET` when `SC4_HTML_VERDICT` and `SC4_TYPST_VERDICT` are both MET and the divergence disposition is recorded, either way. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-TOCTREE-EVIDENCE.md" &amp;&amp; TD=.planning/todos/pending/2026-09-13-html-and-typst-toctree-parents-diverge-after-dedup.md &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; S="$(k SCRATCH_72_04)" &amp;&amp; T="$(k TIP_SHA)" &amp;&amp; [ "${#T}" = 40 ] &amp;&amp; PR="$(k SC3_PAIR)" &amp;&amp; case "$PR" in 1) SUF= ;; 2|3) SUF="-$PR" ;; *) false ;; esac &amp;&amp; HT="$S/html-tip$SUF" &amp;&amp; test -f "$HT/.doctrees/environment.pickle" &amp;&amp; R="$(uv run python -c 'import pickle,sys; from sphinx.environment.adapters.toctree import _get_toctree_ancestors as a; env=pickle.load(open(sys.argv[1],"rb")); r=env.collect_relations(); print(" ".join(r[d][0]+","+list(a(env.toctree_includes,d))[1] for d in ("examples/basic","examples/advanced")))' "$HT/.doctrees/environment.pickle")" &amp;&amp; [ "$R" = "$(k TIP_HTML_PARENT_EXAMPLES_BASIC),$(k TIP_ANCESTOR_PARENT_EXAMPLES_BASIC) $(k TIP_HTML_PARENT_EXAMPLES_ADVANCED),$(k TIP_ANCESTOR_PARENT_EXAMPLES_ADVANCED)" ] &amp;&amp; [ -n "$(k TYPST_PARENT_EXAMPLES_BASIC)" ] &amp;&amp; [ -n "$(k TYPST_PARENT_EXAMPLES_ADVANCED)" ] &amp;&amp; [ -n "$(k BASE_HTML_PARENT_EXAMPLES_BASIC)" ] &amp;&amp; [ -n "$(k BASE_CONSOLE_SELECTING_EXAMPLES_BASIC)" ] &amp;&amp; DS="$(k DIVERGENCE_SURVIVES)" &amp;&amp; if [ "$DS" = no ]; then [ "$(k TIP_HTML_PARENT_EXAMPLES_BASIC)" = "$(k TYPST_PARENT_EXAMPLES_BASIC)" ] &amp;&amp; [ "$(k TIP_HTML_PARENT_EXAMPLES_ADVANCED)" = "$(k TYPST_PARENT_EXAMPLES_ADVANCED)" ] &amp;&amp; [ "$(k TIP_ANCESTOR_PARENT_EXAMPLES_BASIC)" = "$(k TYPST_PARENT_EXAMPLES_BASIC)" ] &amp;&amp; [ "$(k TIP_ANCESTOR_PARENT_EXAMPLES_ADVANCED)" = "$(k TYPST_PARENT_EXAMPLES_ADVANCED)" ] &amp;&amp; [ "$(k DIVERGENCE_TODO)" = none ] &amp;&amp; test ! -e "$TD"; elif [ "$DS" = yes ]; then [ "$(k DIVERGENCE_TODO)" = "$TD" ] &amp;&amp; test -s "$TD" &amp;&amp; grep -q '^area: docs' "$TD"; else false; fi &amp;&amp; [ -z "$(git diff --name-only --diff-filter=A "$T" HEAD -- .planning/todos | grep -vxF "$TD")" ] &amp;&amp; grep -qF '_check_toc_parents' "$F" &amp;&amp; grep -qF 'collect_relations' "$F" &amp;&amp; [ "$(k SC4_VERDICT)" = MET ] &amp;&amp; for s in '## Parent notions' '## Divergence disposition' '## SC#4 verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$T" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, a Python traceback, or `missing section: <name>` printed. Each of these makes it fail:
- the tip environment pickle missing
- the recorded tip relations and ancestor parents of both Examples pages differing from a live re-read
- a Typst, base or console parent key empty
- `DIVERGENCE_SURVIVES` neither no nor yes
- `no` recorded while a tip HTML parent differs from the Typst parent, or the divergence todo present anyway
- `yes` recorded without the todo at the fixed path carrying area docs
- any other new todo file
- the Sphinx function names absent from the evidence
- `SC4_VERDICT` not MET, a section missing, a product-tree change, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - For both `examples/basic` and `examples/advanced`, the evidence records at base and at tip the console token, the `collect_relations` parent, the `_get_toctree_ancestors` parent and the Typst edge parent. The tip values are re-read live by the verify.
    - `DIVERGENCE_SURVIVES` is `no` with no todo filed, or `yes` with exactly one todo at the fixed path. No fix is attempted.
    - `SC4_VERDICT = MET`.
  </acceptance_criteria>
  <done>The divergence question is re-measured with the relation Sphinx's HTML builder actually uses, and disposed of within scope.</done>
</task>

</tasks>

<flagged_assumptions>
Accounting lives in `72-01-PLAN.md`. This plan authors the DOC-18 ordering and precision edges and
carries prohibition P5.

Residual assumption (d): the intersphinx re-take rule in Task 1 step 6. It keeps every pair in the
evidence and halts on any other difference.

Planning-time premise note: the ROADMAP's SC#4 wording ("the parent Sphinx HTML selects") and the
2026-08-16 todo both take the console `selecting:` token as Sphinx's choice. Measured at planning,
that token is `max(parents)` in a log-only function, and `collect_relations()` gave `examples/index`
at the base. SC#4's disposition is unchanged; Task 3 records all the parent notions.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scratch builds → evidence | the SC#3 and SC#4 numbers are read from scratch logs and output trees |
| pickled Sphinx environment → the parent transcription | a pickle from the same venv's own build is loaded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-13 | Repudiation | a tip zero read from localised or quieted output | high | mitigate | `LANG=C LC_ALL=C`, no `-q`, the English summary line required in both logs, and the same-venv base count at least 1, all re-derived by the verify |
| T-72-14 | Repudiation | base and tip built in different environments | medium | mitigate | both built back to back here in one venv, with interpreter and package versions recorded, cross-checked with wave 1 |
| T-72-15 | Tampering | acting on the divergence beyond scope | low | mitigate | prohibition P5; the verify allows at most the one fixed-path todo and no product change |
| T-72-16 | Tampering | unpickling an untrusted file | low | accept | the pickle is this executor's own build output in its own `mktemp -d` directory |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The phase-wide list is in `72-01-PLAN.md`. This plan produces `72-TOCTREE-EVIDENCE.md`, and
`.planning/todos/pending/2026-09-13-html-and-typst-toctree-parents-diverge-after-dedup.md` only if a
divergence survives. Keys: `TIP_SHA`, `SCRATCH_72_04`, `PYVENV_HOME`, `PYVENV_VERSION_INFO`,
`SPHINX_VERSION`, `DOCUTILS_VERSION`, `FURO_VERSION`, `SC3_EDIT_SHAPE`, `MULTI_TOCTREE_BASE`,
`MULTI_TOCTREE_TIP`, `WARNINGS_BASE`, `WARNINGS_TIP`, `WARNING_LINES_IDENTICAL`, `SC3_PAIR`,
`W1_BASE_MATCH`, `SC3_VERDICT`, `SIDEBAR_BASE_COUNTS`, `SIDEBAR_TIP_COUNTS`, `SIDEBAR_TIP_PARENTS`,
`SIDEBAR_BASE_CONTROL`, `SIDEBAR_TIP_CONTROL`, `ROOT_INCLUDE_LINES_BASE`, `ROOT_INCLUDE_LINES_TIP`,
`ROOT_DEAD_INCLUDES_BASE`, `ROOT_DEAD_INCLUDES_TIP`, `SECTION_GUARDS_TIP`, `EDGES_PER_PAGE_TIP`,
`EDGE_STATE_IDENTICAL`, `TYPST_DIFF_FILES`, `SC4_HTML_VERDICT`, `SC4_TYPST_VERDICT`, the parent
keys of Task 3, `DIVERGENCE_SURVIVES`, `DIVERGENCE_TODO` and `SC4_VERDICT`.
</artifacts_this_phase_produces>

<verification>
- SC#3: same-venv clean pair, base count at least 1, tip 0, warning N equal.
- SC#4: one sidebar link per page under its section index; no root Typst include for the five pages;
  one edge per page from its section index; the parent notions of both Examples pages transcribed and
  disposed of.
</verification>

<success_criteria>
ROADMAP SC#3 and SC#4 are evidenced on the phase tip against `PHASE_BASE_SHA`, with the owner's
sidebar look left for UAT.
</success_criteria>

<output>
Create `.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-04-SUMMARY.md` when done.
Its first section states `SC3_VERDICT`, `MULTI_TOCTREE_BASE`, `MULTI_TOCTREE_TIP`, `WARNINGS_BASE`,
`WARNINGS_TIP`, `SC4_VERDICT` and `DIVERGENCE_SURVIVES`. Do not edit REQUIREMENTS.md, ROADMAP.md or
STATE.md.
</output>
