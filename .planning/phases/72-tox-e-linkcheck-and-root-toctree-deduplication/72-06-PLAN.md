---
phase: 72-tox-e-linkcheck-and-root-toctree-deduplication
plan: 06
type: execute
wave: 4
depends_on: ["72-04", "72-05"]
files_modified:
  - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md
autonomous: true
requirements: [QUA-13, DOC-24, DOC-18]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Evidence last (ROADMAP binding ordering): the first push and the CI dispatch run in wave 4, after every edit and after 72-04 (SC3_VERDICT and SC4_VERDICT MET) and 72-05 (SC5_LOCAL_VERDICT MET).'
    - 'Constraint 6 / SC#5 census: immediately before the push, `git branch --list ''gsd/v0.9.5*'' -v` and `git ls-remote --heads origin ''gsd/v0.9.5*''` are recorded. A decoy is never pushed. A decoy that is an ancestor of the canonical ref is deleted. A decoy carrying unique commits halts the plan, because constraint 6''s fast-forward and HEAD re-point are main-checkout actions.'
    - 'PUSHED_SHA is the canonical ref gsd/v0.9.5-docs-link-check-and-navigation. Outside .planning/ it equals this worktree HEAD. Nothing under typsphinx/ or .github/ differs from PHASE_BASE_SHA, pyproject.toml still reads version 0.9.2, and no tag points at it.'
    - 'SC#5 first push: `git push --no-follow-tags -u origin gsd/v0.9.5-docs-link-check-and-navigation` runs once, as a branch creation (or a fast-forward of an ORIGIN_BEFORE re-measured just before). Afterwards the origin head equals PUSHED_SHA and the local branch tracks origin.'
    - 'SC#5 CI: exactly one `gh workflow run CI --ref gsd/v0.9.5-docs-link-check-and-navigation` is issued. RUN_ID has headSha PUSHED_SHA, event workflow_dispatch and createdAt after PUSH_AT. It is observed in the foreground to completed, every job conclusion is transcribed, the two windows-latest and two macos-latest lanes are named individually, and every job is success.'
    - 'The Lint and Format Check job''s `Run lint with tox` step is quoted, which shows CI exercised the edited tox.ini on the runner.'
    - 'SC#5 required checks: re-read at phase close, main''s required status checks give the same strict value and the same sorted contexts as REQUIRED_STRICT_HEAD and REQUIRED_CONTEXTS_HEAD recorded at phase head.'
    - 'Edge (idempotency of the dispatch): after an HTTP 5xx from gh workflow run, the run list is checked for a run at PUSHED_SHA created after PUSH_AT before any retry. An executor resuming mid-wait reuses RUN_ID. The CI list holds exactly one workflow_dispatch run at PUSHED_SHA, and release.yml holds none.'
  artifacts:
    - path: .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md
      provides: 'gate on waves 3, tip identity and fence, decoy census, first push with tracking, one dispatch, the completed run with every job, the four named lanes, the tox lint step, required checks at close, SC#5 CI verdict'
      contains: 'PUSHED_SHA = '
  key_links:
    - from: 'local ref gsd/v0.9.5-docs-link-check-and-navigation'
      to: 'origin/gsd/v0.9.5-docs-link-check-and-navigation'
      via: 'git push --no-follow-tags -u, a branch creation; refs are shared between the worktree and the main checkout'
      pattern: 'ORIGIN_BEFORE = '
    - from: 'gh workflow run CI --ref gsd/v0.9.5-docs-link-check-and-navigation'
      to: 'ci.yml jobs on ubuntu, windows and macos'
      via: 'workflow_dispatch, since ci.yml push and pull_request triggers are scoped to main and develop'
      pattern: 'RUN_ID = '
  prohibitions:
    - statement: 'MUST NOT create or push a tag, force-push, push any ref other than the canonical branch, push a decoy, open or comment on a pull request, trigger release.yml or update-pin.yml, dispatch a second CI run, rerun jobs, or edit any file to turn a red job green.'
      requirement_id: QUA-13
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Put the phase tip on `origin` for the first time, and obtain CI's three-OS verdict on it (SC#5,
milestone invariant #5, constraint 6):
- the branch census and decoy check, immediately before the push
- one push with tracking
- exactly one dispatched CI run, observed in the foreground to completion, with every job
  transcribed
- `main`'s required checks re-read at phase close

Purpose: `ci.yml` runs on this branch only by `workflow_dispatch`, and only such a run reaches the
Windows and macOS lanes. It runs last because 72-04 and 72-05 must be green first. Every later
commit of the phase touches only `.planning/`, which no CI job reads.

Output: `72-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md
@.planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --python 3.13.13`
and run project tooling via `uv run` (`CLAUDE.md` § "Worktree-isolated execution").

**Refs are shared.** This worktree's HEAD is on its own worktree branch, but
`refs/heads/gsd/v0.9.5-docs-link-check-and-navigation` is the same ref the main checkout has checked
out. Push that ref by name. Never run `git branch -f`, `git update-ref` or `git symbolic-ref` on it
from here.

**Waiting on CI.** Wait in the foreground: set the Bash tool's `timeout` to 600000 and never use
`run_in_background`. Wrap each watch in `timeout 590` so it returns inside the tool limit, then read
`gh run view "$RUNID" --json status,conclusion`. Repeat these bounded foreground calls until `status`
is `completed`; do not end the turn or return control while the run is in progress. Commit `RUN_ID`
to the evidence before the first wait, so a resumed executor reuses it instead of dispatching again.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put any explanation on
the next line. The verify fails on any key line containing an opening parenthesis.

**Scratch.** Use your own `mktemp -d` directory, recorded as `SCRATCH_72_06`, and prefix helpers
`p7206_`.

**Commits.** Plain `git commit`. Never a GSD commit helper, which has created the
`gsd/vX.Y-milestone` decoy in every milestone since v0.9.1. Never write `72-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: gsd/v0\.9\.5-milestone -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[345] -->

## Planning-time census (2026-09-13, HEAD `704beaba`; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| Local `gsd/v0.9.5*` refs | only the canonical `gsd/v0.9.5-docs-link-check-and-navigation`; no decoy | orchestrator census |
| Origin | `refs/heads/main` at `098a8ff6`; nothing for `gsd/v0.9.5*`, so the push creates the branch | `git ls-remote origin` |
| `ci.yml` | `name: CI`; six `Test Python {3.12,3.13} on {ubuntu,windows,macos}-latest` jobs plus `Lint and Format Check` (step `Run lint with tox`), `Type Check`, `Code Coverage`, `Build Package` and two integration tests | `.github/workflows/ci.yml` |
| Last branch CI run with the same `ci.yml` | `34748483361` (Phase 71, 12 jobs, all success) | `71-CI-EVIDENCE.md` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: gate on wave 3, tip identity and fence, decoy census, first push with tracking, one CI dispatch</name>
  <precondition>`gh auth status` succeeds and `git ls-remote --heads origin` returns at least one line.</precondition>
  <reversibility rating="reversible">The push creates a new remote branch that can be deleted, and a CI run publishes nothing. No tag, release or pull request is created, and constraint 6 pre-authorises this first push in Phase 72.</reversibility>
  <files>.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md (the census, push and dispatch sections this mirrors)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md (`PHASE_BASE_SHA`, head branch census)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-TOCTREE-EVIDENCE.md and 72-GATES-EVIDENCE.md (the verdict keys gated on)
    - .github/workflows/ci.yml (triggers and job names)
    - .planning/ROADMAP.md v0.9.5 constraints 1, 3 and 6, and Phase 72 SC#5
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record:
   - `date -u +%FT%TZ`, `pwd -P` under `.claude/worktrees/`, `test -f .git; echo "exit:$?"` and the shim check
   - the provisioning line, then the same line with `--locked` appended and `echo "exit:$?"`, which must print `exit:0`. Every CI job starts with a locked sync, so a stale lock would fail every lane before any signal. On a non-zero exit write `## HALT: lock not in sync`; never regenerate the lock.
   - before any commit, `BASE_72_06 = ` followed by `git rev-parse HEAD`, and `SCRATCH_72_06 = ` followed by the path `mktemp -d` printed

2. **`## Wave-3 gate`.** Quote these keys, which must read as shown:
   - `SC3_VERDICT = MET`, `SC4_VERDICT = MET` from `72-TOCTREE-EVIDENCE.md`
   - `SC5_LOCAL_VERDICT = MET`, `TIP_LINKCHECK_VERDICT = PASS` from `72-GATES-EVIDENCE.md`
   - `LINKCHECK_VERDICT = PASS` from `72-LINKCHECK-EVIDENCE.md`
   - `DOC24_VERDICT = MET` from `72-DOC24-EVIDENCE.md`

   Otherwise write `## HALT: wave 3 not green` and stop without pushing.

3. **`## Tip identity and fence`.** Record each of these; all must hold:
   - `PUSHED_SHA = ` followed by `git rev-parse gsd/v0.9.5-docs-link-check-and-navigation`, with `git log --oneline -1`
   - `git diff --name-only HEAD "$PUSHED_SHA" -- . ':(exclude).planning'`, empty
   - `git diff --name-only "$PHASE_BASE_SHA" "$PUSHED_SHA" -- typsphinx/ .github/`, empty
   - `git show "$PUSHED_SHA:pyproject.toml" | grep -m1 '^version = '`, which must be `version = "0.9.2"`
   - `git tag --points-at "$PUSHED_SHA"`, empty

4. **`## Decoy census (constraint 6)`.** Run this immediately before the push, with only step 5's measurements in between. Record `git branch --list 'gsd/v0.9.5*' -v` and `git ls-remote --heads origin 'gsd/v0.9.5*'`.
   - If `gsd/v0.9.5-milestone` exists on origin, write `## HALT: decoy on origin` and stop.
   - If it exists locally, record `git merge-base --is-ancestor gsd/v0.9.5-milestone gsd/v0.9.5-docs-link-check-and-navigation; echo "exit:$?"`.
     - On `exit:0`, the canonical ref already contains it: delete it with `git branch -d gsd/v0.9.5-milestone` and record that.
     - Otherwise record `git log --oneline gsd/v0.9.5-docs-link-check-and-navigation..gsd/v0.9.5-milestone`, write `## HALT: decoy carries unique commits`, and stop. Constraint 6's fast-forward, HEAD re-point and delete belong to the main checkout.
   - If it exists nowhere, record that.

5. **`## Push`.**
   - `ORIGIN_BEFORE = ` followed by the first field of `git ls-remote --heads origin refs/heads/gsd/v0.9.5-docs-link-check-and-navigation`, or `none` when that prints nothing, which is expected for the first push.
   - If it is a SHA, record `git merge-base --is-ancestor "$ORIGIN_BEFORE" "$PUSHED_SHA"; echo "exit:$?"`, which must be `exit:0`. Otherwise write `## HALT: origin moved`; never force.
   - Record `git config --get push.followTags`, then `PUSH_AT = ` followed by `date -u +%FT%TZ`.
   - Run `git push --no-follow-tags -u origin gsd/v0.9.5-docs-link-check-and-navigation` once. Record the output, including GitHub's pull-request hint, and do not act on the hint.
   - Record the following:
     - the origin head now printing `PUSHED_SHA`
     - `git config --get branch.gsd/v0.9.5-docs-link-check-and-navigation.remote` printing `origin`, which is the tracking SC#5 names
     - `git ls-remote --tags origin` showing `refs/tags/v0.9.2` and no `v0.9.3`, `v0.9.4` or `v0.9.5` tag

6. **`## Dispatch`.**
   - Run `gh workflow run CI --ref gsd/v0.9.5-docs-link-check-and-navigation` exactly once.
   - Locate the run with `gh run list --workflow=ci.yml --branch gsd/v0.9.5-docs-link-check-and-navigation --event workflow_dispatch --limit 5 --json databaseId,headSha,status,createdAt,url`, choosing the row whose `headSha` is `PUSHED_SHA` and whose `createdAt` is after `PUSH_AT`.
   - If no row is listed yet, that is registration lag: repeat the list query, and never dispatch again.
   - If the dispatch returned an HTTP 5xx, GitHub may still have created the run. Repeat the list query several times, recording each. Dispatch a second time only if every query shows no run at `PUSHED_SHA` created after `PUSH_AT`, and record why.
   - Record `RUN_ID = ` and `RUN_URL = `.

7. Write `72-CI-EVIDENCE.md` under `# Phase 72 — CI Evidence (first push, SC#5 CI half)`, with the section names above, and commit it.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.5-docs-link-check-and-navigation &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; [ "$(k SC3_VERDICT "$D/72-TOCTREE-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k SC4_VERDICT "$D/72-TOCTREE-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k SC5_LOCAL_VERDICT "$D/72-GATES-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k DOC24_VERDICT "$D/72-DOC24-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k LINKCHECK_VERDICT "$D/72-LINKCHECK-EVIDENCE.md")" = PASS ] &amp;&amp; PB="$(k PHASE_BASE_SHA "$D/72-BASE-EVIDENCE.md")" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; B="$(k BASE_72_06)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; HR="$(git ls-remote --heads origin "refs/heads/$BR")" &amp;&amp; [ "$(printf '%s\n' "$HR" | cut -f1)" = "$P" ] &amp;&amp; OB="$(k ORIGIN_BEFORE)" &amp;&amp; { [ "$OB" = none ] || git merge-base --is-ancestor "$OB" "$P"; } &amp;&amp; [ "$(git config --get "branch.$BR.remote")" = origin ] &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; [ "$(printf '%s\n' "$HL" | grep -c 'gsd/v0\.9\.5-milestone')" = 0 ] &amp;&amp; [ -z "$(git tag --points-at "$P")" ] &amp;&amp; TL="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.[345]')" = 0 ] &amp;&amp; [ -z "$(git diff --name-only "$PB" "$P" -- typsphinx .github)" ] &amp;&amp; [ -z "$(git diff --name-only HEAD "$P" -- . ':(exclude).planning')" ] &amp;&amp; PP="$(git show "$P:pyproject.toml")" &amp;&amp; [ "$(printf '%s\n' "$PP" | grep -m1 '^version = ')" = 'version = "0.9.2"' ] &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json workflowName -q .workflowName)" = CI ] &amp;&amp; [ "$(gh run view "$RUNID" --json event -q .event)" = workflow_dispatch ] &amp;&amp; PA="$(k PUSH_AT)" &amp;&amp; [ -n "$PA" ] &amp;&amp; [[ "$(gh run view "$RUNID" --json createdAt -q .createdAt)" &gt; "$PA" ]] &amp;&amp; for s in '## Head check and provisioning' '## Wave-3 gate' '## Tip identity and fence' '## Decoy census (constraint 6)' '## Push' '## Dispatch'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD | grep -vxF -e "$F" -e "$D/72-06-SUMMARY.md")" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- any wave-3, 72-02 or 72-03 verdict not MET or PASS
- `BASE_72_06` or `PUSHED_SHA` malformed
- the origin head differing from `PUSHED_SHA`, or `ORIGIN_BEFORE` neither `none` nor an ancestor
- the branch not tracking origin
- the decoy on origin
- a tag at the pushed tip, the v0.9.2 tag missing on origin, or a v0.9.3, v0.9.4 or v0.9.5 tag on origin
- a typsphinx/ or .github/ change since the phase base, or the pushed product tree differing from HEAD
- the pushed version line not 0.9.2
- no `RUN_ID`, or the run's headSha, workflow, event or creation time not matching
- another tracked file changed, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Decoy census (constraint 6)` records both listings immediately before the push, and any ancestor-decoy deletion with its ancestry result.
    - `## Push` records `ORIGIN_BEFORE`, `PUSH_AT` and the verbatim `git push --no-follow-tags -u origin gsd/v0.9.5-docs-link-check-and-navigation` output. The origin head equals `PUSHED_SHA`, and the branch tracks `origin`.
    - `## Dispatch` records exactly one `gh workflow run CI --ref gsd/v0.9.5-docs-link-check-and-navigation`, with `RUN_ID` and `RUN_URL` whose `headSha` is `PUSHED_SHA`.
  </acceptance_criteria>
  <done>The milestone branch is on origin at the green phase tip, with no decoy and no tag, and one CI run is dispatched against exactly that tip.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the run to completion in the foreground, transcribe every job, name the four windows and macos lanes, quote the tox lint step, and re-read the required checks at phase close</name>
  <precondition>The `RUN_ID` recorded by Task 1 resolves with `gh run view <id> --json databaseId`. A resumed executor reuses it.</precondition>
  <reversibility rating="reversible">Read-only observation of a run already dispatched, plus a read-only branch-protection query.</reversibility>
  <files>.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CI-EVIDENCE.md (Task 1: `RUN_ID`, `PUSHED_SHA`)
    - .planning/milestones/v0.9.4-phases/71-v0-9-4-close-prep-prep-only-unpublished/71-CI-EVIDENCE.md § "Job census" and the ruff-verdict section (the shape to mirror; its `RUN_ID` 34748483361 is the job-name reference)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-BASE-EVIDENCE.md (`REQUIRED_STRICT_HEAD`, `REQUIRED_CONTEXTS_HEAD`)
    - .github/workflows/ci.yml lines 10-72
  </read_first>
  <action>
1. **Wait in the foreground.**
   - Run `timeout 590 gh run watch "$RUNID" --interval 30` in a Bash call with `timeout` 600000 and no `run_in_background`.
   - After each return, read `gh run view "$RUNID" --json status,conclusion`, and repeat until `status` is `completed`.
   - Overall cap: 90 minutes. If it is reached, record the status and return a blocking checkpoint. Never dispatch again.

2. **`## Run`.** Record `gh run view "$RUNID" --json status,conclusion,workflowName,headSha,url,createdAt,updatedAt`, with keys `RUN_HEAD_SHA` and `RUN_CONCLUSION`.

3. **`## Job census`.**
   - Record `gh run view "$RUNID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'` as a numbered table covering every row. Keys `JOB_COUNT` (measured) and `NON_SUCCESS_JOBS`, which must be 0.
   - Record the sorted job-name set, `gh run view <id> --json jobs --jq '[.jobs[].name] | sort | join("|")'`, for this run and for Phase 71's run 34748483361. Key `JOB_NAMES_MATCH_PHASE71 = yes` when they are equal. Constraint 3 forbids any workflow edit, so a difference means `ci.yml` changed: write `## HALT: ci.yml job set changed`.

4. **`## windows-latest lanes`** and **`## macos-latest lanes`.** Two sub-tables naming `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each with its conclusion.

5. **`## Lint through tox`.** Take the `Lint and Format Check` job id from the run's jobs. From `gh run view --job <id> --log`, quote the `Run lint with tox` step's `commands[0]> black --check .` line, its `commands[1]> ruff check .` line and `lint: OK`. That shows the runner parsed the edited `tox.ini`.

6. **`## Dispatch count and no release run`.** Record:
   - `gh run list --workflow=ci.yml --branch gsd/v0.9.5-docs-link-check-and-navigation --event workflow_dispatch --limit 50 --json headSha`, key `DISPATCH_COUNT`, the runs at `PUSHED_SHA`, which must be 1
   - `gh run list --workflow=release.yml --limit 20 --json headSha`, key `RELEASE_RUNS_AT_PUSHED`, which must be 0

7. **`## Required checks at phase close`.** Re-read `gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks` with `--jq .strict` and with `--jq '[.contexts[]] | sort | join("|")'`, keys `REQUIRED_STRICT_CLOSE` and `REQUIRED_CONTEXTS_CLOSE`. Key `REQUIRED_CHECKS_UNCHANGED = yes` when both equal the head values in `72-BASE-EVIDENCE.md`.

8. **`## SC#5 CI verdict`.**
   - `SC5_CI_VERDICT = MET` only when the run is completed and success, `NON_SUCCESS_JOBS = 0`, `JOB_NAMES_MATCH_PHASE71 = yes`, all four named lanes are success, `DISPATCH_COUNT = 1`, `RELEASE_RUNS_AT_PUSHED = 0` and `REQUIRED_CHECKS_UNCHANGED = yes`.
   - If any job is not success, transcribe every job literally and keep the run. Do not rerun, dispatch again or edit anything. Write `SC5_CI_VERDICT = NOT-MET` and return a blocking checkpoint naming each failing job and step.

9. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.5-docs-link-check-and-navigation &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; [ "$(gh run view "$RUNID" --json status -q .status)" = completed ] &amp;&amp; [ "$(gh run view "$RUNID" --json conclusion -q .conclusion)" = success ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; JC="$(gh run view "$RUNID" --json jobs -q '.jobs | length')" &amp;&amp; [ "$JC" -ge 1 ] &amp;&amp; [ "$JC" = "$(k JOB_COUNT)" ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.conclusion != "success")] | length')" = 0 ] &amp;&amp; JN="$(gh run view "$RUNID" --json jobs -q '[.jobs[].name] | sort | join("|")')" &amp;&amp; [ -n "$JN" ] &amp;&amp; [ "$JN" = "$(gh run view 34748483361 --json jobs -q '[.jobs[].name] | sort | join("|")')" ] &amp;&amp; for n in 'Test Python 3.12 on windows-latest' 'Test Python 3.13 on windows-latest' 'Test Python 3.12 on macos-latest' 'Test Python 3.13 on macos-latest'; do [ "$(gh run view "$RUNID" --json jobs -q "[.jobs[] | select(.name == \"$n\" and .conclusion == \"success\")] | length")" = 1 ] || { echo "lane: $n"; exit 1; }; grep -qF "$n" "$F" || { echo "missing: $n"; exit 1; }; done &amp;&amp; grep -qF 'Run lint with tox' "$F" &amp;&amp; grep -qF 'lint: OK' "$F" &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch "$BR" --event workflow_dispatch --limit 50 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; RC="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '[.contexts[]] | sort | join("|")')" &amp;&amp; [ -n "$RC" ] &amp;&amp; [ "$RC" = "$(k REQUIRED_CONTEXTS_HEAD "$D/72-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict)" = "$(k REQUIRED_STRICT_HEAD "$D/72-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(k NON_SUCCESS_JOBS)" = 0 ] &amp;&amp; [ "$(k JOB_NAMES_MATCH_PHASE71)" = yes ] &amp;&amp; [ "$(k DISPATCH_COUNT)" = 1 ] &amp;&amp; [ "$(k RELEASE_RUNS_AT_PUSHED)" = 0 ] &amp;&amp; [ "$(k REQUIRED_CHECKS_UNCHANGED)" = yes ] &amp;&amp; [ "$(k SC5_CI_VERDICT)" = MET ] &amp;&amp; for s in '## Run' '## Job census' '## windows-latest lanes' '## macos-latest lanes' '## Lint through tox' '## Dispatch count and no release run' '## Required checks at phase close' '## SC#5 CI verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or one of `lane: <name>`, `missing: <name>` or `missing section: <name>` printed. Each of these makes it fail:
- the run not completed, not success, or at a headSha other than `PUSHED_SHA`
- the job count zero or differing from `JOB_COUNT`, or any job not success
- the sorted job-name set differing from Phase 71's run 34748483361
- any of the four windows or macos lanes absent, not success, or not named in the evidence
- the tox lint step or `lint: OK` not quoted
- more than one dispatched run at `PUSHED_SHA`, or a release.yml run at it
- the live required contexts or strict value differing from the phase-head record
- a count, match or verdict key wrong, a section missing, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Job census` holds a numbered table of every job, with `NON_SUCCESS_JOBS = 0` and `JOB_NAMES_MATCH_PHASE71 = yes`.
    - The lane sub-tables name `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each success.
    - `## Lint through tox` quotes the `Run lint with tox` step's black and ruff lines and `lint: OK`.
    - `DISPATCH_COUNT = 1`, `RELEASE_RUNS_AT_PUSHED = 0`, `REQUIRED_CHECKS_UNCHANGED = yes` and `SC5_CI_VERDICT = MET`.
  </acceptance_criteria>
  <done>One completed, all-green CI run on the phase tip is transcribed job by job. Both Windows and both macOS lanes are named, and main's required checks are unchanged from phase head to phase close.</done>
</task>

</tasks>

<flagged_assumptions>
Accounting lives in `72-01-PLAN.md`. This plan carries prohibition P6's remote half. The
dispatch-idempotency truth encodes the project's HTTP 5xx hazard. It is an execution guard beyond
the fifteen probe items and is not counted among them.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local refs → origin | a push publishes refs; a decoy, a force, another ref or a tag would publish what this phase must not |
| gh CLI → GitHub Actions | a dispatch starts remote work; the wrong workflow could begin a publish pipeline |
| CI log → committed evidence | quoted job-log lines enter the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-20 | Tampering | pushing a decoy, force-pushing, pushing another ref, or a tag riding along | high | mitigate | census immediately before the push; `--no-follow-tags -u` of the canonical ref only; the verify requires no decoy on origin, no tag at the tip, and no v0.9.3, v0.9.4 or v0.9.5 tag on origin |
| T-72-21 | Elevation of Privilege | triggering `release.yml` or `update-pin.yml` | high | mitigate | only `gh workflow run CI` is issued; the verify asserts no release.yml run at `PUSHED_SHA`; prohibition P6 |
| T-72-22 | Repudiation | citing Phase 71's run or a stale run as this phase's green, or masking a red run with a second dispatch | high | mitigate | `RUN_ID` headSha must equal `PUSHED_SHA`, createdAt must follow `PUSH_AT`, and exactly one dispatched run exists at that SHA; a red run is kept and escalated |
| T-72-23 | Tampering | required status checks changing mid-phase | medium | mitigate | head and close reads compared on strict and on the sorted contexts |
| T-72-24 | Denial of Service | a stale `uv.lock` failing every lane | medium | mitigate | the locked sync must exit 0 before the push; the lock is never regenerated |
| T-72-25 | Information Disclosure | CI log excerpts in evidence | low | accept | public repository and logs; only tool-output lines are quoted |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The phase-wide list is in `72-01-PLAN.md`. This plan creates `72-CI-EVIDENCE.md`, with keys
`BASE_72_06`, `SCRATCH_72_06`, `PUSHED_SHA`, `ORIGIN_BEFORE`, `PUSH_AT`, `RUN_ID`, `RUN_URL`,
`RUN_HEAD_SHA`, `RUN_CONCLUSION`, `JOB_COUNT`, `NON_SUCCESS_JOBS`, `JOB_NAMES_MATCH_PHASE71`,
`DISPATCH_COUNT`, `RELEASE_RUNS_AT_PUSHED`, `REQUIRED_STRICT_CLOSE`, `REQUIRED_CONTEXTS_CLOSE`,
`REQUIRED_CHECKS_UNCHANGED` and `SC5_CI_VERDICT`. Outside the repository it creates
`origin/gsd/v0.9.5-docs-link-check-and-navigation` at `PUSHED_SHA`, sets its tracking, and creates
one `workflow_dispatch` CI run. It modifies no product-tree file.
</artifacts_this_phase_produces>

<verification>
- The canonical branch is on origin at `PUSHED_SHA`, with no decoy and no tag, and tracks origin.
- One dispatched CI run at `PUSHED_SHA` completed all green. The four Windows and macOS lanes are
  named, the job set equals Phase 71's, and lint ran through tox.
- The required checks are unchanged from phase head to close.
</verification>

<success_criteria>
ROADMAP SC#5's remote half: the milestone branch is on origin, and a completed, green three-OS CI
run on the phase tip is transcribed.
</success_criteria>

<output>
Create `.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-06-SUMMARY.md` when done.
Its first section states `PUSHED_SHA`, `RUN_ID`, `RUN_URL`, `JOB_COUNT`, `NON_SUCCESS_JOBS`,
`REQUIRED_CHECKS_UNCHANGED` and `SC5_CI_VERDICT`. If the run was not all green, that section names
the failing jobs first. Do not edit REQUIREMENTS.md, ROADMAP.md or STATE.md.
</output>
