---
phase: 72-tox-e-linkcheck-and-root-toctree-deduplication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tox.ini
  - docs/source/conf.py
  - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-LINKCHECK-EVIDENCE.md
autonomous: true
requirements: [QUA-13]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'QUA-13 / SC#1: tox.ini gains one [testenv:linkcheck] section with runner = uv-venv-lock-runner, extras = docs, changedir = docs and the single command sphinx-build -b linkcheck source _build/linkcheck; the base tox.ini is a byte-identical prefix of the new one, so env_list and the requires pin with its comment are untouched.'
    - '`uv run tox list` shows linkcheck under "additional environments:" and not under "default environments:", so a plain tox run does not run it.'
    - 'Run with docs/_build/linkcheck removed first, one run of `uv run tox -e linkcheck` exits 0 and every row of docs/_build/linkcheck/output.json has status working; LINKCHECK_TOTAL and LINKCHECK_WORKING are measured at execution and equal (constraint 8: never compared with 95).'
    - 'D-01: a transient status (timeout; ignored with info service unavailable; rate-limited; broken whose code is 0, a connection-level exception) is re-run from a clean output directory, at most three runs in total, every run transcribed and none deleted; only a run that meets SC#1 on its own counts as the pass.'
    - 'D-02: docs/source/conf.py gains at most one key among linkcheck_timeout, linkcheck_retries and linkcheck_rate_limit_timeout, only when the same URL failed transiently in two runs, with a comment naming the URL, the status and the run numbers; otherwise conf.py is unchanged.'
    - 'D-03: a non-transient row (broken with an HTTP status, any redirected row, a missing anchor, or anything that would need an ignore-style key) halts the plan to the owner with the URL and its source recorded; no link is edited and no key is added.'
    - 'D-04 is read literally: the pass condition stays "every output.json entry working, total equals the working count".'
    - 'Edge (QUA-13 boundary): total == working is the pass; a single non-working row among any number of working rows fails the run.'
    - 'Edge (QUA-13 empty): an absent, empty or zero-row output.json is a failed run, never a pass; LINKCHECK_TOTAL is at least 1.'
    - 'Edge (QUA-13 ordering): output.json row order is not relied on; the census counts statuses and the non-working rows are transcribed sorted by uri.'
    - 'Edge (QUA-13 precision): the total counts rows, not unique URIs; a URL with a rate-limited row and a later working row in the same output.json fails that run (72-RESEARCH.md Pitfall 4); LINKCHECK_UNIQUE_URIS is recorded beside the row total.'
    - 'Edge (QUA-13 adjacency): only docs/_build/linkcheck is removed before a run, never docs/_build or the html/pdf siblings; the new tox.ini section follows [testenv:docs] after one blank line and every existing line is unchanged.'
    - statement: 'Edge (QUA-13 concurrency): every run writes to this worktree''s own docs/_build/linkcheck, never shared with the wave-1 sibling 72-01, whose builds go to its own mktemp directory; an interrupted run is discarded and re-run clean, never read.'
      verification: backstop
  artifacts:
    - path: tox.ini
      provides: '[testenv:linkcheck], outside env_list'
      contains: '[testenv:linkcheck]'
    - path: .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-LINKCHECK-EVIDENCE.md
      provides: 'tox.ini diff proof, tox list, every linkcheck run census, D-01..D-03 dispositions, SC#1 verdict'
      contains: 'LINKCHECK_VERDICT = '
  key_links:
    - from: 'tox.ini [testenv:linkcheck]'
      to: 'docs/_build/linkcheck/output.json'
      via: 'changedir = docs, then sphinx-build -b linkcheck source _build/linkcheck'
      pattern: 'sphinx-build -b linkcheck source _build/linkcheck'
    - from: '72-LINKCHECK-EVIDENCE.md'
      to: '72-03 (DOC-24) and 72-05'
      via: 'the environment exists and passed before any surface names it'
      pattern: 'LINKCHECK_VERDICT = PASS'
  prohibitions:
    - statement: 'MUST NOT make linkcheck pass by adding linkcheck_ignore, linkcheck_anchors_ignore, linkcheck_anchors_ignore_for_url, linkcheck_allowed_redirects or any key that turns a row into something other than working, or by editing a link or docstring, to get past a non-transient result (D-03, D-04).'
      requirement_id: QUA-13
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT count a run as the pass unless that run alone meets SC#1, and MUST NOT delete or overwrite the transcript of an earlier failed run (D-01).'
      requirement_id: QUA-13
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT add linkcheck to env_list, to any workflow file or to any required check; it needs the network (constraint 3).'
      requirement_id: QUA-13
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Land QUA-13: a `tox -e linkcheck` environment shaped like `docs-html`, proven on a clean run at
execution time, with D-01, D-02 and D-03 governing any non-`working` row and D-04's literal pass
condition. The environment stays out of `env_list`.

Purpose: QUA-13 must land before DOC-24 names it (ROADMAP binding ordering), so this runs in wave 1,
beside 72-01's base capture and DOC-18 edit, with which it shares no file. 72-05 re-runs the
environment on the final tip.

Output: the `tox.ini` section, `72-LINKCHECK-EVIDENCE.md`, and a `docs/source/conf.py` key only
under D-02.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md
@.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md
@tox.ini
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run` (`CLAUDE.md` § "Worktree-isolated execution"). The linkcheck
tox environment installs `extras = docs` on its own through `uv-venv-lock-runner`.

**Status, not console text.** Linkcheck results are read from `docs/_build/linkcheck/output.json`,
one JSON object per line, whose `status` field is not localised. Record the exit status of every run
as well. Sphinx 9.1.0 sets a non-zero exit when any row is `broken` or `timeout`
(`sphinx/builders/linkcheck.py:109-110`), while `redirected` and `ignored` rows leave it at 0. That
is why the `output.json` census, not the exit code, is the binding half of SC#1.

**Evidence key lines.** Every `KEY = value` line carries the bare value only; put any explanation on
the next line. The verify fails on any key line containing an opening parenthesis.

**Scratch.** Put copies of each run's `output.json` and each log under your own `mktemp -d`
directory, recorded as `SCRATCH_72_02`, and prefix helper files `p7202_`.

**Commits.** Plain `git commit`. Never a GSD commit helper. Never write `72-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: linkcheck_(ignore|anchors_ignore|allowed_redirects) -->

## Planning-time measurements (2026-09-13; context only, re-measure, never trust)

| Fact | Value at planning |
|------|-------------------|
| `tox.ini` | 90 lines, ends with a newline; `[testenv:docs]` is last, its final line `    sphinx-build -b typstpdf source _build/pdf` |
| `tox list` today | `default environments:` py312, py313, lint, type, cov, docs; `additional environments:` docs-html, docs-pdf |
| Discussion-time linkcheck | exit 0 in about 10 s; 95 rows, all `working`; 41 URIs carry an anchor |
| `conf.py` | no `linkcheck_*` key |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: add [testenv:linkcheck] by pure insertion, prove it stays out of env_list, and run it once from a clean output directory</name>
  <files>tox.ini, .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-LINKCHECK-EVIDENCE.md</files>
  <read_first>
    - tox.ini (whole file; `[testenv:docs-html]` at lines 67-73 is the shape, `[testenv:docs]` ends the file)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md § "Pattern 1" and § "Code Examples" "output.json census"
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md D-01..D-04 and § "Claude's Discretion"
    - .planning/ROADMAP.md Phase 72 SC#1 and v0.9.5 constraints 3, 7 and 8
  </read_first>
  <action>
1. **`## Head check and provisioning`.** Record:
   - `date -u +%FT%TZ`, `pwd -P` under `.claude/worktrees/`, `test -f .git; echo "exit:$?"` and the shim check
   - the provisioning line and its exit, and `PYVENV_HOME` and `PYVENV_VERSION_INFO` from `.venv/pyvenv.cfg`
   - before any commit, `BASE_72_02 = ` followed by `git rev-parse HEAD`, and `SCRATCH_72_02 = ` followed by the path `mktemp -d` printed

2. **The section (QUA-13).** Append to the end of `tox.ini` one blank line and then these seven lines, in this order, nothing else:
   - `[testenv:linkcheck]`
   - `description = Check external links and anchors in the documentation (needs network)`
   - `runner = uv-venv-lock-runner`
   - `extras = docs`
   - `changedir = docs`
   - `commands =`
   - four spaces followed by `sphinx-build -b linkcheck source _build/linkcheck`

   The `description` text is Claude's discretion under CONTEXT; the other keys are fixed by SC#1. Do not edit `env_list` (line 2), the `requires` pin or its comment, or any other line. Commit with `git commit -m "build(72-02): add tox -e linkcheck environment (QUA-13)"`, and record `TOX_EDIT_COMMIT`.

3. **`## tox.ini diff`.** Record:
   - `git diff --numstat "$BASE_72_02" HEAD -- tox.ini`, which must show 8 added and 0 removed
   - `git diff "$BASE_72_02" HEAD -- tox.ini`, verbatim
   - `sed -n 2p tox.ini`, which must read `env_list = py312, py313, lint, type, cov, docs`
   - `uv run tox list`, verbatim: `linkcheck` must appear under `additional environments:` and not under `default environments:`

4. **`## Run 1`.**
   - `rm -rf docs/_build/linkcheck`; remove only that directory. Record `test ! -e docs/_build/linkcheck; echo "exit:$?"`.
   - Run `uv run tox -e linkcheck`, output to `$S/p7202_run1.log`, then `echo "exit:$?"`. Key `LINKCHECK_RUN_1_EXIT`.
   - Copy `docs/_build/linkcheck/output.json` to `$S/p7202_run1_output.json`.
   - Census, with `uv run python -c` over the JSON lines. Keys:
     - `LINKCHECK_RUN_1_TOTAL`: row count
     - `LINKCHECK_RUN_1_WORKING`: working count
     - `LINKCHECK_RUN_1_UNIQUE_URIS`: count of distinct `uri`
     - `LINKCHECK_RUN_1_STATUS_CENSUS`: status:count pairs, sorted and space-separated, for example `working:95`
     - `LINKCHECK_RUN_1_ANCHORED_URIS`: count of distinct `uri` containing `#`
   - Transcribe every non-`working` row as its full JSON object, sorted by `uri`, or the literal line `no non-working rows`.
   - Tail the run log's last 5 lines.

5. Write `72-LINKCHECK-EVIDENCE.md` under `# Phase 72 — Linkcheck Evidence (QUA-13, SC#1)`, with the section names above, and commit it. Task 2 decides the verdict.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-LINKCHECK-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_72_02)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; N="$(git show "$B:tox.ini" | wc -l)" &amp;&amp; [ "$(head -n "$N" tox.ini | git hash-object --stdin)" = "$(git rev-parse "$B:tox.ini")" ] &amp;&amp; [ "$(git diff --numstat "$B" HEAD -- tox.ini | cut -f1,2)" = "$(printf '8\t0')" ] &amp;&amp; [ "$(sed -n 2p tox.ini)" = 'env_list = py312, py313, lint, type, cov, docs' ] &amp;&amp; grep -qx 'requires = tox-uv~=1.35' tox.ini &amp;&amp; uv run python -c 'import configparser; c=configparser.ConfigParser(interpolation=None); c.read("tox.ini"); s=c["testenv:linkcheck"]; assert s["runner"]=="uv-venv-lock-runner"; assert s["extras"]=="docs"; assert s["changedir"]=="docs"; assert s["commands"].strip()=="sphinx-build -b linkcheck source _build/linkcheck"; assert "linkcheck" not in c["tox"]["env_list"]' &amp;&amp; TL="$(uv run tox list)" &amp;&amp; [ "$(printf '%s\n' "$TL" | sed -n '/^additional environments:/,$p' | grep -cE '^linkcheck +-&gt; ')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$TL" | sed -n '/^default environments:/,/^$/p' | grep -c linkcheck)" = 0 ] &amp;&amp; [ -n "$(k LINKCHECK_RUN_1_EXIT)" ] &amp;&amp; S="$(k SCRATCH_72_02)" &amp;&amp; test -f "$S/p7202_run1_output.json" &amp;&amp; C="$(uv run python -c 'import json,sys; r=[json.loads(l) for l in open(sys.argv[1]) if l.strip()]; print(len(r), sum(1 for x in r if x["status"]=="working"))' "$S/p7202_run1_output.json")" &amp;&amp; [ "${C% *}" = "$(k LINKCHECK_RUN_1_TOTAL)" ] &amp;&amp; [ "${C#* }" = "$(k LINKCHECK_RUN_1_WORKING)" ] &amp;&amp; [ -n "$(k LINKCHECK_RUN_1_UNIQUE_URIS)" ] &amp;&amp; [ -n "$(k LINKCHECK_RUN_1_STATUS_CENSUS)" ] &amp;&amp; for s in '## Head check and provisioning' '## tox.ini diff' '## Run 1'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning' | grep -vxF tox.ini)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F"</automated>
    <fails_when>Non-zero exit, a Python AssertionError, or `missing section: <name>` printed. Each of these makes it fail:
- not in a worktree, or the shim absent
- `BASE_72_02` missing or not an ancestor
- the base tox.ini not a byte-identical prefix of the new one, or the numstat not 8 added and 0 removed
- env_list line 2 changed, or the requires pin line missing
- the linkcheck section keys not exactly runner, extras, changedir and commands as specified, or linkcheck in env_list
- `tox list` not listing linkcheck once under additional environments, or listing it under default environments
- run 1's exit, total, working, unique-URI or status-census keys missing, or total and working not matching a recount of the saved output.json
- a product-tree file other than tox.ini changed, or a key line carrying a parenthesis</fails_when>
  </verify>
  <acceptance_criteria>
    - `tox.ini`'s diff from `BASE_72_02` is 8 added, 0 removed, and the base file is a byte-identical prefix of the new file.
    - `[testenv:linkcheck]` parses with `runner = uv-venv-lock-runner`, `extras = docs`, `changedir = docs` and the command `sphinx-build -b linkcheck source _build/linkcheck`.
    - `uv run tox list` shows `linkcheck` only under `additional environments:`.
    - Run 1 is transcribed: its exit status, the census keys and every non-`working` row, sorted by `uri`, re-countable from the saved `output.json`.
  </acceptance_criteria>
  <done>The environment exists outside `env_list`, and one clean run of it is transcribed with its census.</done>
</task>

<task type="auto">
  <name>Task 2: Apply D-01, D-02 and D-03 to the run census and record SC#1's verdict</name>
  <files>docs/source/conf.py, .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-LINKCHECK-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-LINKCHECK-EVIDENCE.md (Task 1: run 1)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-CONTEXT.md D-01, D-02, D-03 and D-04 (the transient list, the three-run ceiling, the permitted keys, the owner route)
    - .planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-RESEARCH.md § "Common Pitfalls" 3-4 and the 429/503 handling
    - docs/source/conf.py (read before any D-02 insertion; bare `key = value` assignments)
  </read_first>
  <action>
1. **Classify run 1.** Classify each non-`working` row:
   - *Transient* (D-01): `timeout`; `ignored` with info `service unavailable`; `rate-limited`; `broken` whose `code` is 0, meaning a connection-level exception (reset, DNS or TLS) rather than an HTTP status.
   - *Non-transient* (D-03): `broken` with an HTTP status in `code` (404, 410 and so on); any `redirected` row; `broken` whose info reports a missing anchor; any row whose pass would need `linkcheck_ignore`, `linkcheck_anchors_ignore`, `linkcheck_anchors_ignore_for_url`, `linkcheck_allowed_redirects` or any other key that rewrites a row's status.

2. **Pass.** If run 1 exited 0, its total is at least 1 and it equals the `working` count, run 1 is the pass. Go to step 6.

3. **D-03 halt.** If any row is non-transient, write `## HALT: non-transient linkcheck result`. For each such row, record:
   - the URL, status, code and info
   - its `filename` and `lineno` from `output.json`
   - its source class: an editable docs source (a `docs/source/**.rst` page, or `CHANGELOG.md` through `changelog.rst`), or an autodoc docstring under `typsphinx/`, which constraint 2 makes unfixable here. Rows whose `filename` is under `api/` come from autodoc.

   Do not edit any link or docstring and add no key. Commit the evidence and return a blocking checkpoint to the owner.

4. **D-01 re-runs.** If every non-`working` row is transient:
   - `rm -rf docs/_build/linkcheck` and run `uv run tox -e linkcheck` again as `## Run 2`, with the same keys under `LINKCHECK_RUN_2_*` and its own saved `output.json`.
   - If run 2 is transient-only again, take `## Run 3` the same way.
   - Stop at three runs in total. Never delete or edit an earlier run's section. A run is the pass only if it alone exits 0 with total equal to `working`. A non-transient row in a later run goes to step 3.

5. **D-02 key.** This is the planner's reading of D-01 and D-02 together. D-02's fresh run is one of D-01's three runs, not a fourth.
   - When the same URL has failed transiently in two runs and a run remains, add exactly one key to `docs/source/conf.py`, whichever addresses that URL's status: `linkcheck_timeout` for a `timeout`, `linkcheck_rate_limit_timeout` for a `rate-limited` row, or `linkcheck_retries` for a connection-level `broken` or a 503. Use the file's bare `key = value` style.
   - Directly above it, put a `#` comment naming the URL, the status and the run numbers that showed it.
   - Commit it with `git commit -m "docs(72-02): linkcheck timing key for a recurring transient failure (QUA-13, D-02)"`, then take the next run clean.
   - If the recurrence appears only after run 3, add no key and do not run again.
   - If no run within three meets SC#1, write `## HALT: no passing linkcheck run within three`, name the D-02 candidate URL if any, commit, and return a blocking checkpoint.

6. **`## SC#1 verdict`.** Record these keys:
   - `LINKCHECK_RUNS`: runs taken
   - `LINKCHECK_PASS_RUN`: the passing run's number
   - `LINKCHECK_TOTAL` and `LINKCHECK_WORKING`: the passing run's figures
   - `CONF_LINKCHECK_KEYS`: the count of lines in `docs/source/conf.py` starting with `linkcheck_`, which is 0 unless D-02 fired
   - `LINKCHECK_VERDICT = PASS`

   State that `docs/_build/linkcheck/output.json` in this worktree is the passing run's output. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication &amp;&amp; F="$D/72-LINKCHECK-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_72_02)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; [ "$(k LINKCHECK_VERDICT)" = PASS ] &amp;&amp; R="$(k LINKCHECK_RUNS)" &amp;&amp; PR="$(k LINKCHECK_PASS_RUN)" &amp;&amp; [ "$R" -ge 1 ] &amp;&amp; [ "$R" -le 3 ] &amp;&amp; [ "$PR" = "$R" ] &amp;&amp; for i in $(seq 1 "$R"); do grep -qxF "## Run $i" "$F" || { echo "missing section: ## Run $i"; exit 1; }; [ -n "$(k "LINKCHECK_RUN_${i}_TOTAL")" ] || { echo "missing run $i total"; exit 1; }; done &amp;&amp; [ "$(k "LINKCHECK_RUN_${PR}_EXIT")" = 0 ] &amp;&amp; O=docs/_build/linkcheck/output.json &amp;&amp; test -s "$O" &amp;&amp; C="$(uv run python -c 'import json,sys; r=[json.loads(l) for l in open(sys.argv[1]) if l.strip()]; print(len(r), sum(1 for x in r if x["status"]=="working"))' "$O")" &amp;&amp; T="${C% *}" &amp;&amp; W="${C#* }" &amp;&amp; [ "$T" -ge 1 ] &amp;&amp; [ "$T" = "$W" ] &amp;&amp; [ "$T" = "$(k LINKCHECK_TOTAL)" ] &amp;&amp; [ "$W" = "$(k LINKCHECK_WORKING)" ] &amp;&amp; [ "$T" = "$(k "LINKCHECK_RUN_${PR}_TOTAL")" ] &amp;&amp; CK="$(grep -c '^linkcheck_' docs/source/conf.py || true)" &amp;&amp; [ "$CK" = "$(k CONF_LINKCHECK_KEYS)" ] &amp;&amp; ! grep -qE 'linkcheck_(ignore|anchors_ignore|allowed_redirects)' docs/source/conf.py &amp;&amp; if [ "$CK" = 0 ]; then [ -z "$(git diff --name-only "$B" HEAD -- docs/source/conf.py)" ]; else [ "$CK" = 1 ] &amp;&amp; grep -qE '^linkcheck_(timeout|retries|rate_limit_timeout) = ' docs/source/conf.py &amp;&amp; [ "$(git diff --numstat "$B" HEAD -- docs/source/conf.py | cut -f2)" = 0 ] &amp;&amp; grep -B1 -E '^linkcheck_' docs/source/conf.py | head -n 1 | grep -qE '^#.*http.*run'; fi &amp;&amp; grep -qxF '## SC#1 verdict' "$F" &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning' | grep -vxF -e tox.ini -e docs/source/conf.py)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` or `missing run <n> total` printed. Each of these makes it fail:
- no PASS verdict
- a run count outside 1 to 3, or the pass run not the last run taken
- a run section or its keys missing, or the pass run's exit not 0
- the live output.json absent, holding zero rows, or with total differing from working or from the recorded figures
- the conf.py linkcheck key count differing from `CONF_LINKCHECK_KEYS`, or an ignore-style key present
- conf.py changed with no key recorded; or a recorded key that is not one of the three timing keys, not a pure addition, or lacks a comment naming a URL and a run
- the verdict section missing
- a product-tree file other than tox.ini and conf.py changed, a key line carrying a parenthesis, or a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `LINKCHECK_VERDICT = PASS`, `LINKCHECK_TOTAL` equals `LINKCHECK_WORKING` and is at least 1, and a live recount of `docs/_build/linkcheck/output.json` agrees.
    - Every run taken has its own `## Run n` section and keys, and no run's transcript was removed.
    - `CONF_LINKCHECK_KEYS = 0` with `conf.py` untouched, or `1` with exactly one timing key preceded by a comment naming the URL and the run numbers (D-02).
    - No ignore-style key exists in `conf.py` (D-03, D-04).
  </acceptance_criteria>
  <done>SC#1 is met on a clean run taken at execution time, or the phase has stopped at the owner with the non-transient row and its source recorded.</done>
</task>

</tasks>

<flagged_assumptions>
Accounting lives in `72-01-PLAN.md`. This plan authors the six QUA-13 edges, the last of them a
backstop marker because the two wave-1 plans' isolation holds by construction. It also carries
prohibitions P1, P2 and P3. Residual assumption (c): the reading of D-01 with D-02 in Task 2 step 5.
It keeps every run in the evidence and routes a fourth run's need to the owner instead of taking it.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| linkcheck builder → the internet | outbound HTTP to URLs already committed in docs sources, CHANGELOG.md and docstrings |
| output.json → committed evidence | row data enters the repository |
| tox.ini → every tox user and CI's tox-driven jobs | a new section is parsed by every tox invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-05 | Repudiation | a pass manufactured by an ignore-style key or a link edit | high | mitigate | D-03 halts to the owner; the verify rejects any ignore-style key and allows at most one timing key carrying a URL-and-run comment |
| T-72-06 | Repudiation | a pass read from a run that did not meet SC#1, or an earlier failure erased | high | mitigate | the pass run must be the last run, exit 0, and re-count to total == working from the live output.json; every run keeps its own section |
| T-72-07 | Denial of Service | linkcheck entering `env_list`, so a plain `tox` needs the network | medium | mitigate | the base `tox.ini` must be a byte-identical prefix; line 2 is exact; `tox list` shows linkcheck under additional environments only |
| T-72-08 | Information Disclosure | SSRF or internal-host probing through a committed doc URL | low | accept | local, manual tool over URLs reviewed at commit time; canon, referred to `/gsd-secure-phase` |
| T-72-09 | Tampering | a malformed `tox.ini` breaking CI's `Run lint with tox` and test lanes | medium | mitigate | configparser parse of the section in the verify; 72-06's CI run exercises `tox.ini` on the runner |
| T-72-SC | Tampering | package installs | low | accept | `uv-venv-lock-runner` installs only what `uv.lock` pins |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The phase-wide list is in `72-01-PLAN.md`. This plan produces:
- `[testenv:linkcheck]` in `tox.ini`
- a `docs/source/conf.py` timing key, only under D-02
- `72-LINKCHECK-EVIDENCE.md`, with keys `BASE_72_02`, `SCRATCH_72_02`, `PYVENV_HOME`,
  `PYVENV_VERSION_INFO`, `TOX_EDIT_COMMIT`, `LINKCHECK_RUN_<n>_EXIT`, `_TOTAL`, `_WORKING`,
  `_UNIQUE_URIS`, `_STATUS_CENSUS`, `_ANCHORED_URIS`, `LINKCHECK_RUNS`, `LINKCHECK_PASS_RUN`,
  `LINKCHECK_TOTAL`, `LINKCHECK_WORKING`, `CONF_LINKCHECK_KEYS` and `LINKCHECK_VERDICT`.
</artifacts_this_phase_produces>

<verification>
- `tox.ini` gains exactly the eight-line section. `env_list`, the pin and its comment are unchanged,
  and linkcheck sits outside the default environment list.
- A clean `uv run tox -e linkcheck` passes SC#1 by the live `output.json` census. Any non-`working`
  row is dispositioned under D-01, D-02 or D-03, and no ignore-style key exists.
</verification>

<success_criteria>
ROADMAP SC#1: `tox -e linkcheck` exists, is shaped like `docs-html`, and passes on a clean run
measured at execution time, with total equal to `working`.
</success_criteria>

<output>
Create `.planning/phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-02-SUMMARY.md` when done.
Its first section states `LINKCHECK_VERDICT`, `LINKCHECK_RUNS`, `LINKCHECK_TOTAL`,
`LINKCHECK_WORKING` and `CONF_LINKCHECK_KEYS`. Do not edit REQUIREMENTS.md, ROADMAP.md or STATE.md.
</output>
