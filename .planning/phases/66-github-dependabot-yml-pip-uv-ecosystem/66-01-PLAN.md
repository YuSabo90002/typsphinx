---
phase: 66-github-dependabot-yml-pip-uv-ecosystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
  - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md
autonomous: true
requirements: [DEP-01, DEP-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-01 / DEP-01: the milestone branch carries MILESTONE_COMMIT, whose only file is .github/dependabot.yml. The file equals the origin/main copy at BASE_SHA with line 4 changed from package-ecosystem "pip" to package-ecosystem "uv" and every other byte identical (cmp), so the github-actions entry is untouched.'
    - 'D-02: the pip entry is replaced outright. After the edit the file holds exactly two package-ecosystem lines, "uv" and "github-actions"; no transitional pip entry exists.'
    - 'D-06 / DEP-03: no key is added or removed. The weekly Monday 00:00 schedule, open-pull-requests-limit 5, the dependencies and automated labels, and the sphinx-typst-stack group with its sphinx-autodoc-typehints and sphinx-intl exclusions carry across unchanged, proven by the same byte comparison.'
    - 'D-01: branch chore/dependabot-uv-ecosystem exists on origin at PR_COMMIT. Its single parent is BASE_SHA, it is exactly one commit ahead of BASE_SHA, its only changed file is .github/dependabot.yml, and that blob equals CONFIG_BLOB, the milestone copy blob.'
    - 'D-01: pull request PR_NUMBER targets main from that branch with one commit and one file, carries the prescribed English title and body, and is OPEN and unmerged when this plan ends.'
    - 'D-01: CI run PR_RUN_ID (event pull_request, headSha PR_COMMIT) is observed in the foreground to completion. The six required checks each conclude success: Test Python 3.12 on ubuntu-latest, Test Python 3.13 on ubuntu-latest, Lint and Format Check, Type Check, Code Coverage and Build Package. Every other job is transcribed literally.'
    - 'DEP-03 (encoding edge): unchanged and byte-identical are decided on git blob SHAs and cmp over bytes, never on a YAML-semantic or whitespace-insensitive comparison.'
    - 'Failure path: a required check that concludes other than success is transcribed, and the executor stops at a blocking checkpoint for the owner, with no rerun, no second push and no edit.'
  artifacts:
    - path: .github/dependabot.yml
      provides: 'the uv package-ecosystem entry, milestone copy (D-01)'
      contains: 'package-ecosystem: "uv"'
    - path: .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md
      provides: 'baseline, milestone commit, main-bound commit, push, pull request, CI run and required-check census'
      contains: 'CONFIG_BLOB = '
  key_links:
    - from: 'MILESTONE_COMMIT:.github/dependabot.yml'
      to: 'PR_COMMIT:.github/dependabot.yml on origin chore/dependabot-uv-ecosystem'
      via: 'git commit-tree over a temporary index read from BASE_SHA with that one path replaced by CONFIG_BLOB'
      pattern: 'CONFIG_BLOB = '
    - from: 'pull request PR_NUMBER'
      to: 'ci.yml six required checks'
      via: 'pull_request trigger scoped to main, with no paths filter'
      pattern: 'PR_RUN_ID = '
  prohibitions:
    - statement: 'MUST NOT merge, approve, or enable auto-merge on the main-bound PR in this plan; the merge belongs to 66-02, behind the owner decision (D-01).'
      requirement_id: DEP-01
      category: safety
      status: resolved
      verification: test
    - statement: 'MUST NOT close, merge, comment on, label, cross-reference, or send an @dependabot command to #123 or #128 (D-02).'
      requirement_id: DEP-01
      category: values
      status: resolved
      verification: test
    - statement: 'MUST NOT add, remove or reorder any key in .github/dependabot.yml beyond the one package-ecosystem value (versioning-strategy included), and MUST NOT create repository labels (D-06).'
      requirement_id: DEP-03
      category: values
      status: resolved
      verification: test
    - statement: 'MUST NOT edit .github/workflows/ or typsphinx/, force-push, push the milestone branch, or push any ref other than the new main-bound branch (constraints 3 and 13).'
      requirement_id: DEP-01
      category: safety
      status: resolved
      verification: test
---

<objective>
Put the `uv` ecosystem into `.github/dependabot.yml` on both branches that matter, with byte-identical content, and get the change in front of `main`'s required checks.

- Per D-01, the milestone branch gets one commit that swaps line 4's value from `"pip"` to `"uv"` and nothing else. D-02 is honoured by replacing the entry outright, and D-06 by adding no key.
- A second commit, built from `origin/main`'s tree with the **same blob**, is pushed as `chore/dependabot-uv-ecosystem`, and a pull request to `main` is opened.
- Its CI run is then observed to completion, and the six checks branch protection requires are transcribed.

Purpose: GitHub reads `dependabot.yml` only from the default branch (`about-the-dependabot-yml-file.md:46`). No `uv`-ecosystem PR can exist until this file reaches `main`. Sharing one blob makes REL-12's later merge conflict-free by construction.

This plan does **not** merge. The merge is one-way (D-01) and sits in 66-02, behind the owner's decision.

Discretion choices (66-CONTEXT "Claude's Discretion"), recorded:
- Branch name: `chore/dependabot-uv-ecosystem`.
- Order: the milestone commit comes first. The main-bound commit reuses its blob through `git commit-tree`, so this worktree's HEAD never moves.
- Evidence file: `66-MAIN-PR-EVIDENCE.md`.
- Merge style, applied in 66-02: a two-parent merge commit, as PRs #132 to #136 were.

Output: the milestone commit, the pushed branch and its open PR, and `66-MAIN-PR-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-CONTEXT.md
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-RESEARCH.md
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-PATTERNS.md
@.github/dependabot.yml
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
`## HALT` is a heading the executor must be able to write on a failure path; the gates require its absence.

<assumption_delta_decision>
The detector fired on "fallback" (pluralization) and "custom" (chosen). Both come from the ROADMAP prose about the custom lockfile workflow.
- **Primary noun:** the single Python `updates` entry, whose `package-ecosystem` is now `"uv"`.
- **Decision:** `promote`. `uv` replaces `pip` as the only Python entry, and `pip` is removed rather than kept alongside it (D-02, owner-chosen over a transitional dual entry).
- **The custom workflow is not a second mechanism.** It is a contingency that comes into play only if D-05 leg 2 fails (66-04), and even then it is recorded and handed to the owner, never built in this phase.
- **Suggested invariant:** the byte comparison in Task 1 (`cmp` against `origin/main` with one value swapped) encodes "exactly one Python entry, and it is `uv`".
</assumption_delta_decision>

## Planning-time census (2026-09-12: re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| `origin/main` | `6181768f64b4cee62a77ac4e26c60c3c976cbb6e` | `git rev-parse origin/main` |
| Milestone branch | local `gsd/v0.9.3-toolchain-and-dependency-update-repair` at `27cf0a63`, 12 commits ahead of its origin copy `d9c75553` | `git rev-parse`, `git rev-list --count` |
| `.github/dependabot.yml` on the two branches | identical (`git diff origin/main HEAD -- .github/dependabot.yml` is empty); line 4 is `  - package-ecosystem: "pip"`; last touched by `0bf6b5a5` | `git diff`, `sed -n 4p`, `git log` |
| Branch protection on `main` | contexts `Test Python 3.12 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`, `Test Python 3.13 on ubuntu-latest`; `strict: true`; `enforce_admins: false`; no required reviews | `gh api repos/YuSabo90002/typsphinx/branches/main/protection` |
| Merge settings | merge, squash and rebase all allowed; `delete_branch_on_merge: false`; #132 to #136 merged as two-parent merge commits (`45962faa` has 2 parents) | `gh api repos/YuSabo90002/typsphinx`, `git rev-list --parents` |
| `ci.yml` triggers | `push` and `pull_request` on `[ main, develop ]`, plus `workflow_dispatch`; no `paths` or `paths-ignore` filter | `ci.yml:3-8` |
| PR template | none (`.github/` holds only `ISSUE_TEMPLATE`, `dependabot.yml`, `workflows`) | `ls .github` |
| Repository labels | no `dependencies`, `automated` or `ci` label exists | `gh label list` |
| Local uv / latest uv | `uv 0.12.13` / release `0.12.13` (2026-09-10). The local-uv figure in ROADMAP SC#2 and PROJECT.md's AMENDED block is stale. | `uv --version`, `gh api repos/astral-sh/uv/releases/latest` |

## Artifacts this phase produces

| Artifact | Kind | Plan |
|----------|------|------|
| `.github/dependabot.yml`, line 4 value `"uv"` (milestone copy) | tracked config, one value changed | 66-01 |
| `origin/chore/dependabot-uv-ecosystem` at `PR_COMMIT`, and its PR to `main` | remote branch and pull request | 66-01 |
| the merge of that PR into `main` (`MERGE_SHA`) | two-parent merge commit on `main` | 66-02 |
| `66-MAIN-PR-EVIDENCE.md` with keys `BASE_SHA`, `MILESTONE_COMMIT`, `CONFIG_BLOB`, `PR_BRANCH`, `PR_COMMIT`, `PUSH_AT`, `PR_NUMBER`, `PR_URL`, `PR_RUN_ID`, `OWNER_DECISION`, `DECIDED_AT`, `MERGE_SHA`, `MERGED_AT` | evidence | 66-01, 66-02 |
| `66-DEPENDABOT-EVIDENCE.md` with keys `POLL1_START`, `POLL1_END`, `D03_POLL1`, `UV_RUN_ID`, `UPDATER_UV_IMAGE`, `OWNER_TAB_ANNOTATION`, `CLICKED_AT`, `D03_BRANCH`, `UV_PR_CANDIDATES`, `SC1_PR`, `SC1_SHA`, `SC1_RUN_ID`, `CI_UV_VERSION`, `LOCAL_UV`, `UV_LATEST`, `DOCS_UV_ROW`, `DEPENDABOT_UV_MAIN`, `DEPENDABOT_UV_DEPLOYED`, `D05_LEG2`, and the `## Requirement closure` table | evidence | 66-03, 66-04 |

No code symbol and no test file is created (constraint 13; Phase 64 D-05 and Phase 65 D-03 evidence-only convention).

This plan's share: the `.github/dependabot.yml` edit, the pushed branch and its PR, and the first nine keys of `66-MAIN-PR-EVIDENCE.md`.

## Spec-less probe ledger (phase-wide; no SPEC exists for Phase 66)

7 edge-probe items = 5 authored truths + 2 flagged assumptions. None is silently dropped.

| # | Requirement | Category | Disposition | Carried by |
|---|-------------|----------|-------------|------------|
| 1 | DEP-01 | unclassified | flagged assumption A-DEP-01 | 66-03, 66-04 |
| 2 | DEP-03 | boundary | truth (explicit): open uv PR count against `open-pull-requests-limit: 5` | 66-04 |
| 3 | DEP-03 | empty | truth (explicit): empty labels, single-member group, zero PRs | 66-04 |
| 4 | DEP-03 | encoding | truth (explicit): byte identity decided on blob SHAs and `cmp` | 66-01, 66-02 |
| 5 | DEP-03 | precision | truth (explicit): versions compared as literal strings only | 66-04 |
| 6 | DEP-03 | concurrency | truth (backstop): selection by `createdAt` after `MERGED_AT`, never list position | 66-03 |
| 7 | DEP-04 | unclassified | flagged assumption A-DEP-04 | 66-04 |

Prohibition recall ran in-prompt. The kept items live in each plan's `must_haves.prohibitions`, descriptor-less.
- **Canon breadcrumb:** GITHUB_TOKEN scope and `pull_request_target` exposure are canon supply-chain security, covered by `/gsd-secure-phase`. They are not minted here, except as 66-04's fallback-workflow prohibition, which is a scope decision (D-05) rather than a canon item.

**Flagged assumptions** (spec-less probe, unresolved by construction):
- **A-DEP-01.** Assumed: dependabot will open at least one `dependabot/uv/` PR whose head commit changes both `pyproject.toml` and `uv.lock` within this phase's observation window. The expected candidates are ruff 0.16 and docutils 0.23, which lie outside the current ranges.
  - If only lockfile-only PRs open, or none, SC#1's same-commit clause cannot close in this phase, and 66-04 halts for the owner.
- **A-DEP-04.** Assumed: the uv updater image dependabot deploys is named in the `Dependabot Updates` run log (`Pulling image ghcr.io/dependabot/dependabot-updater-uv:<tag>`), and its tag resolves as a dependabot-core commit. This was measured at planning for the pip updater: `61ec2dff…` resolves, and `uv/Dockerfile` at that ref pins `0.12.7`.
  - If the tag does not resolve, D-05 leg 1 rests on dependabot-core `main` and says so. Leg 2 is unaffected.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: swap line 4 to "uv" on the milestone branch, build the byte-identical main-bound commit from origin/main, push the new branch, open the PR, locate its CI run</name>
  <reversibility rating="reversible">An unmerged PR can be closed and its branch deleted. The milestone commit is local until REL-12. Nothing reaches main here.</reversibility>
  <files>.github/dependabot.yml, .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md</files>
  <read_first>
    - .github/dependabot.yml (the whole file; line 4 is the only line that changes)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-CONTEXT.md (D-01, D-02, D-06, Claude's Discretion)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-RESEARCH.md ("The exact diff", Pitfall 4 on exact required-check names)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-PATTERNS.md (evidence-file structure: KEY = value lines, sections, verbatim output)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-CI-EVIDENCE.md (a recent evidence file to mirror)
    - CLAUDE.md, section "Worktree-isolated execution"
  </read_first>
  <action>
1. **Head check and provisioning.**
   - Record `test -f .git; echo "exit:$?"` (must be `exit:0`), `pwd -P` (must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`), `git rev-parse --abbrev-ref HEAD` and `git log --oneline -1`.
   - Run CLAUDE.md's provisioning line verbatim: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. CLAUDE.md makes this mandatory for every executor, even though this plan runs only git and gh afterwards.
   - Record `gh auth status` succeeding.

2. **Baseline.**
   - Run `git fetch origin main`, then record `BASE_SHA = <git rev-parse origin/main>` (`6181768f…` at planning).
   - Record `git diff --stat origin/main HEAD -- .github/dependabot.yml`, which must be empty: the two copies are identical before the edit.
   - Record `git show origin/main:.github/dependabot.yml | sed -n 4p`, which must print the `pip` line.
   - Record `git ls-tree origin/main .github/dependabot.yml`; the mode must be `100644`.
   - If the diff is non-empty, or line 4 differs, write `## HALT: baseline moved` with both outputs and stop at a blocking-human checkpoint. D-01's byte-identity premise no longer holds.

3. **Milestone-branch edit (D-01 milestone copy; D-02 outright replacement; D-06 no key added).**
   - With the Edit tool, change only the quoted value on line 4 of `.github/dependabot.yml` from `"pip"` to `"uv"`. Leave the indentation, the `# Python dependencies (pyproject.toml)` comment above it, and every other line untouched.
   - Assert `git show "$BASE_SHA:.github/dependabot.yml" | sed '4s/"pip"$/"uv"/' | cmp - .github/dependabot.yml` exits 0.
   - Assert `git diff --numstat -- .github/dependabot.yml` prints `1`, `1`, `.github/dependabot.yml`.
   - Stage only that file with `git add .github/dependabot.yml`, and commit with subject `chore(66-01): switch dependabot Python updates from pip to uv`, plus the attribution trailers your session prescribes. Nothing else goes into this commit.
   - Record `MILESTONE_COMMIT = <sha>` and `CONFIG_BLOB = <git rev-parse HEAD:.github/dependabot.yml>`.
   - Record `git show --name-only --format= "$MILESTONE_COMMIT"`, which must be exactly `.github/dependabot.yml`.

4. **Main-bound commit built from origin/main, without moving this worktree's HEAD** (D-01; the milestone copy is first, and the PR reuses its blob, so the bytes are identical by construction).
   - Make a scratch directory `S="$(mktemp -d)"`.
   - Build the tree:
     - `GIT_INDEX_FILE="$S/index" git read-tree "$BASE_SHA"`
     - `GIT_INDEX_FILE="$S/index" git update-index --cacheinfo "100644,$CONFIG_BLOB,.github/dependabot.yml"`
     - `TREE="$(GIT_INDEX_FILE="$S/index" git write-tree)"`
   - Write the commit message to `$S/msg`: subject `chore(deps): switch dependabot Python updates from pip to uv`, a blank line, then your session's attribution trailers.
   - Create the commit: `PR_COMMIT="$(git commit-tree "$TREE" -p "$BASE_SHA" -F "$S/msg")"`.
   - Before any push, assert all four:
     - `git rev-parse "$PR_COMMIT^"` equals `BASE_SHA`
     - `git diff --name-only "$BASE_SHA" "$PR_COMMIT"` prints exactly `.github/dependabot.yml`
     - `git diff --numstat "$BASE_SHA" "$PR_COMMIT"` prints `1`, `1` for that file
     - `git rev-parse "$PR_COMMIT:.github/dependabot.yml"` equals `CONFIG_BLOB`
   - Never `git checkout`, `git switch`, `git reset` or `git cherry-pick` this worktree onto origin/main. The worktree's own index and HEAD stay as step 3 left them.

5. **Push the new branch, and only it.**
   - Record `PR_BRANCH = chore/dependabot-uv-ecosystem`.
   - Record `git ls-remote --heads origin refs/heads/chore/dependabot-uv-ecosystem`, which must print nothing. If it prints a line, write `## HALT: branch exists` and stop: an existing branch is never overwritten.
   - Record `PUSH_AT = <date -u +%FT%TZ>`.
   - Run `git push origin "$PR_COMMIT:refs/heads/chore/dependabot-uv-ecosystem"`: no `--force`, no `--tags`, no `-u`, no other refspec. Record its output.
   - Assert the remote head (`git ls-remote --heads origin refs/heads/chore/dependabot-uv-ecosystem`, first field) equals `PR_COMMIT`.
   - The milestone branch itself is not pushed by this phase.

6. **Open the pull request (D-01; outward-facing text in English, terse).**
   - Write `$S/body` with exactly these two paragraphs, followed by the pull-request attribution lines your session prescribes:
     - Paragraph 1: "Dependabot's `pip` ecosystem updates `pyproject.toml` but never `uv.lock`, so its Python PRs fail `uv sync --locked` in CI. The `uv` ecosystem updates both in one commit."
     - Paragraph 2: "Only `package-ecosystem` changes. The schedule, PR limit, labels, `sphinx-typst-stack` group and `github-actions` entry are unchanged."
   - The body names no pull-request or issue number. A `#123` or `#128` mention would write a cross-reference event onto those PRs, which D-02 forbids touching.
   - Run `gh pr create --base main --head chore/dependabot-uv-ecosystem --title "chore(deps): switch dependabot Python updates from pip to uv" --body-file "$S/body"`.
   - Request no reviewer, add no label or assignee, and do not enable auto-merge.
   - Record `PR_NUMBER = <n>` and `PR_URL = <url>`.

7. **Locate the CI run.** The `pull_request` trigger starts it; never run `gh workflow run`.
   - Query `gh run list --workflow=ci.yml --branch chore/dependabot-uv-ecosystem --event pull_request --limit 5 --json databaseId,headSha,status,createdAt,url`.
   - Select the row whose `headSha` equals `PR_COMMIT` and whose `createdAt` is later than `PUSH_AT`.
   - If no such row is listed yet, that is registration lag: repeat the same query a few seconds later.
   - Record `PR_RUN_ID = <databaseId>` and its URL.

8. **Write the evidence.**
   - Create `66-MAIN-PR-EVIDENCE.md`. Its opening line states the measured shape: "executor worktree, provisioned with the CLAUDE.md line; git and gh only; the main checkout is never touched".
   - Sections: `## Head check and provisioning`, `## Baseline`, `## Milestone-branch commit`, `## Main-bound commit`, `## Push and pull request`, `## CI run located`.
   - Every command is quoted with its literal output.
   - Put the `KEY = value` lines at column 0: `BASE_SHA`, `MILESTONE_COMMIT`, `CONFIG_BLOB`, `PR_BRANCH`, `PR_COMMIT`, `PUSH_AT`, `PR_NUMBER`, `PR_URL`, `PR_RUN_ID`.
   - Commit the evidence as its own commit (`docs(66-01): record main-bound PR evidence`). Never amend `MILESTONE_COMMIT`.
  </action>
  <verify>
    <automated>F=.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_SHA)" &amp;&amp; BL="$(k CONFIG_BLOB)" &amp;&amp; MC="$(k MILESTONE_COMMIT)" &amp;&amp; PB="$(k PR_BRANCH)" &amp;&amp; PC="$(k PR_COMMIT)" &amp;&amp; N="$(k PR_NUMBER)" &amp;&amp; R="$(k PR_RUN_ID)" &amp;&amp; for v in "$B" "$BL" "$MC" "$PB" "$PC" "$N" "$R"; do [ -n "$v" ] || { echo "missing evidence key"; exit 1; }; done &amp;&amp; [ "$PB" = chore/dependabot-uv-ecosystem ] &amp;&amp; T="$(mktemp)" &amp;&amp; git show "$B:.github/dependabot.yml" &gt; "$T" &amp;&amp; [ "$(sed -n 4p "$T")" = '  - package-ecosystem: "pip"' ] &amp;&amp; [ "$(sed -n 4p .github/dependabot.yml)" = '  - package-ecosystem: "uv"' ] &amp;&amp; sed '4s/"pip"$/"uv"/' "$T" | cmp -s - .github/dependabot.yml &amp;&amp; [ "$(git rev-parse HEAD:.github/dependabot.yml)" = "$BL" ] &amp;&amp; git merge-base --is-ancestor "$MC" HEAD &amp;&amp; [ "$(git show --name-only --format= "$MC")" = .github/dependabot.yml ] &amp;&amp; LR="$(git ls-remote --heads origin "refs/heads/$PB")" &amp;&amp; [ "$(printf '%s\n' "$LR" | cut -f1)" = "$PC" ] &amp;&amp; [ "$(git rev-parse "$PC^")" = "$B" ] &amp;&amp; [ "$(git rev-list --count "$B..$PC")" = 1 ] &amp;&amp; [ "$(git diff --name-only "$B" "$PC")" = .github/dependabot.yml ] &amp;&amp; [ "$(git rev-parse "$PC:.github/dependabot.yml")" = "$BL" ] &amp;&amp; [ "$(gh pr view "$N" --json baseRefName,headRefName,headRefOid -q '[.baseRefName,.headRefName,.headRefOid] | join(" ")')" = "main $PB $PC" ] &amp;&amp; [ "$(gh pr view "$N" --json files -q '[.files[].path] | join(" ")')" = .github/dependabot.yml ] &amp;&amp; [ "$(gh pr view "$N" --json commits -q '.commits | length')" = 1 ] &amp;&amp; [ "$(gh pr view "$N" --json state -q .state)" = OPEN ] &amp;&amp; [ "$(gh run view "$R" --json headSha,event -q '[.headSha,.event] | join(" ")')" = "$PC pull_request" ] &amp;&amp; git diff --quiet "$B" HEAD -- .github/workflows typsphinx &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing evidence key` printed. Each of these makes it fail:
- a missing key
- a branch name other than `chore/dependabot-uv-ecosystem`
- line 4 of the base copy not being the pip line, or line 4 of the milestone copy not being the uv line
- the milestone copy differing from the base copy anywhere else (`cmp` non-zero), which also rules out a second Python entry
- the HEAD blob differing from `CONFIG_BLOB`
- `MILESTONE_COMMIT` not an ancestor of HEAD, or touching more than the one file
- the remote branch head not equal to `PR_COMMIT`
- `PR_COMMIT` not parented on `BASE_SHA`, more than one commit ahead, touching another file, or holding another blob
- the PR base, head or head SHA mismatching; more than one file or commit; or state not OPEN
- the run's headSha or event mismatching
- any change under `.github/workflows` or `typsphinx` between `BASE_SHA` and HEAD
- a HALT heading in the evidence</fails_when>
  </verify>
  <acceptance_criteria>
    - `.github/dependabot.yml` line 4 reads `  - package-ecosystem: "uv"`. The whole file equals `origin/main`'s copy at `BASE_SHA` with that one value swapped (`cmp` exit 0), and it still holds exactly two `package-ecosystem:` lines.
    - `MILESTONE_COMMIT` lists only `.github/dependabot.yml`, and `CONFIG_BLOB` equals `git rev-parse HEAD:.github/dependabot.yml`.
    - `PR_COMMIT` has parent `BASE_SHA`, and is one commit ahead touching one file, whose blob is `CONFIG_BLOB`. `origin/chore/dependabot-uv-ecosystem` points at it, pushed without force.
    - `PR_NUMBER` is OPEN, with base `main` and head `chore/dependabot-uv-ecosystem` at `PR_COMMIT`, one commit and one file. The prescribed title and body name no issue or PR number.
    - `PR_RUN_ID` has headSha `PR_COMMIT` and event `pull_request`.
    - The evidence carries the six sections and nine keys above, each command quoted with its output.
  </acceptance_criteria>
  <done>The uv switch exists on the milestone branch and on a pushed main-bound branch as the same blob, a one-commit, one-file PR to main is open, and its CI run is located.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the PR's CI run to completion in the foreground, transcribe the job census, and prove the six required checks green without merging</name>
  <precondition>`66-MAIN-PR-EVIDENCE.md` records `PR_RUN_ID`, and `gh run view <PR_RUN_ID> --json databaseId` resolves. If this executor was resumed after returning mid-wait, it reuses that id.</precondition>
  <files>.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md (Task 1's keys: PR_NUMBER, PR_COMMIT, PR_RUN_ID)
    - .github/workflows/ci.yml (job names; the `test` matrix)
    - .planning/phases/65-tox-uv-bare-tox-uv-revert-on-the-uv-path-tox-actually-resolves/65-02-PLAN.md, Task 2 (the foreground-wait and census procedure this task mirrors)
  </read_first>
  <action>
1. **Wait in the foreground, never in the background.**
   - Run `gh run watch "$PR_RUN_ID" --interval 30` in a Bash call with the timeout at 600000 ms. Do not use `run_in_background`.
   - If the call returns before the run completes, issue it again. After each return, read `gh run view "$PR_RUN_ID" --json status,conclusion`.
   - Do not end the turn while `status` is not `completed`. The overall cap is 90 minutes of wall clock.
   - At the cap: record the run id and its current status, write `## HALT: CI not complete`, and return a blocking checkpoint. Never rerun and never dispatch.

2. **On completion, record:**
   - `gh run view "$PR_RUN_ID" --json status,conclusion,headSha,event,url`
   - under `## Job census`, a numbered table of `gh run view "$PR_RUN_ID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'`, covering every row literally.

3. **`## Required checks`.**
   - Record `gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '{strict: .required_status_checks.strict, contexts: .required_status_checks.contexts, enforce_admins: .enforce_admins.enabled}'` verbatim.
   - Tabulate each required context by its exact name, with its conclusion from the census.
   - All six must read `success`: `Test Python 3.12 on ubuntu-latest`, `Test Python 3.13 on ubuntu-latest`, `Lint and Format Check`, `Type Check`, `Code Coverage`, `Build Package`.

4. **PR state.**
   - Record `gh pr view "$PR_NUMBER" --json state,mergeable,mergeStateStatus,headRefOid`.
   - `state` must be OPEN and `headRefOid` must equal `PR_COMMIT`. `mergeStateStatus` is recorded as-is (`CLEAN` is expected; `BEHIND` means main moved, which 66-02 handles).

5. **A non-required job concluding other than success:** list it under `## Non-required job findings` with its name, conclusion and failing step, quoted from `gh run view --job <id> --log-failed`. Continue: D-01 gates on the six required checks, and 66-02's owner decision surfaces these findings.

6. **A required job concluding other than success:**
   - write `## HALT: required check red`, naming the job and quoting its failing step
   - do not rerun, push or edit anything
   - return a blocking-human checkpoint

7. Append `## Handoff to 66-02`, stating that the PR is open and unmerged, awaiting the owner's merge decision (D-01), and listing any non-required findings. Commit.
  </action>
  <verify>
    <automated>F=.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; N="$(k PR_NUMBER)" &amp;&amp; R="$(k PR_RUN_ID)" &amp;&amp; PC="$(k PR_COMMIT)" &amp;&amp; [ -n "$N" ] &amp;&amp; [ -n "$R" ] &amp;&amp; [ -n "$PC" ] &amp;&amp; [ "$(gh run view "$R" --json status -q .status)" = completed ] &amp;&amp; for c in 'Test Python 3.12 on ubuntu-latest' 'Test Python 3.13 on ubuntu-latest' 'Lint and Format Check' 'Type Check' 'Code Coverage' 'Build Package'; do [ "$(gh run view "$R" --json jobs -q "[.jobs[] | select(.name == \"$c\" and .conclusion == \"success\")] | length")" = 1 ] || { echo "required check not green: $c"; exit 1; }; grep -qF "$c" "$F" || { echo "not in evidence: $c"; exit 1; }; done &amp;&amp; [ "$(gh api repos/YuSabo90002/typsphinx/branches/main/protection --jq '.required_status_checks.contexts | length')" = 6 ] &amp;&amp; [ "$(gh pr view "$N" --json state,headRefOid -q '[.state,.headRefOid] | join(" ")')" = "OPEN $PC" ] &amp;&amp; grep -q '^## Job census' "$F" &amp;&amp; grep -q '^## Required checks' "$F" &amp;&amp; grep -q '^## Handoff to 66-02' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `required check not green: <name>` or `not in evidence: <name>` printed. Each of these makes it fail:
- the run not `completed`
- any of the six required jobs not `success`, or not named in the evidence
- branch protection listing other than six contexts
- the PR not OPEN, or its head moved off `PR_COMMIT`
- the census, required-checks or handoff section missing
- a HALT heading in the evidence</fails_when>
  </verify>
  <acceptance_criteria>
    - The run is `completed`, and every job is transcribed in a numbered table.
    - The six required contexts appear by exact name, each `success`, next to the verbatim branch-protection read (`strict`, `contexts`, `enforce_admins`).
    - The PR is OPEN at `PR_COMMIT`, and `mergeStateStatus` is recorded.
    - Any non-required non-success job is listed with its failing step; no rerun, dispatch or edit happened.
    - `## Handoff to 66-02` exists, and the PR has not been merged.
  </acceptance_criteria>
  <done>The main-bound PR's CI run completed with all six required checks green, transcribed, and the PR waits unmerged for 66-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree objects → origin | A push publishes a ref. Pushing the milestone branch, a force-push, or a commit carrying milestone history would publish what this phase must not. |
| gh → the pull request | The PR body is outward-facing, and a PR number in it writes an event onto another PR. |
| CI log → committed evidence | Job-log excerpts cross into the repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-01 | Tampering | main-bound commit carrying `.planning/` or other milestone history into `main` | high | mitigate | The commit is built with `git commit-tree` from `BASE_SHA`'s tree with one path replaced. Before the push, `rev-list --count` equals 1, the name-only diff is the single file, and the blob equals `CONFIG_BLOB`. The verify repeats these checks and adds the PR's own `files` and `commits` counts. |
| T-66-02 | Tampering | wrong ref pushed, or a force-push | high | mitigate | The only refspec is `$PR_COMMIT:refs/heads/chore/dependabot-uv-ecosystem`, with no `--force`. The branch must be absent on origin first, and the remote head is checked to equal `PR_COMMIT`. A prohibition forbids pushing the milestone branch. |
| T-66-03 | Elevation of Privilege | merging before the owner decides | high | mitigate | No merge command exists in this plan, and a prohibition forbids merge and auto-merge. Task 2's verify requires state OPEN at plan end. |
| T-66-04 | Repudiation | PR text cross-referencing #123/#128 (D-02) | medium | mitigate | The body text is prescribed verbatim and names no PR or issue number. |
| T-66-05 | Information Disclosure | CI log excerpts in evidence | low | accept | The repository and its Actions logs are public, and only tool-output lines are quoted. |
| T-66-SC | Tampering | `uv sync --extra dev` during provisioning | low | accept | It installs only the pins already in `uv.lock`. No new package is added, and no package-manager add command runs (RESEARCH, "Package Legitimacy Audit: not applicable"). |
</threat_model>

<verification>
- The milestone copy of `.github/dependabot.yml` equals `origin/main`'s copy with line 4's value swapped to `"uv"`, and the main-bound commit holds the identical blob.
- `chore/dependabot-uv-ecosystem` is on origin at a one-commit, one-file `PR_COMMIT`, parented on `BASE_SHA`. Its PR to `main` is OPEN.
- The PR's CI run completed with the six required checks green, and nothing was merged.
</verification>

<success_criteria>
D-01's first half is complete: a separate PR to `main` changes only `.github/dependabot.yml`, the same bytes are committed on the milestone branch, and all six required checks are green. SC#1's "the Python updates entry reads `package-ecosystem: "uv"` and the `github-actions` entry is untouched" holds on the milestone branch and on the PR head. It reaches `main` only in 66-02.
</success_criteria>

<output>
Create `.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-01-SUMMARY.md` when done.
- Its frontmatter `requirements-completed:` is `[]`: DEP-01 and DEP-03 close in 66-04 from dependabot's own output.
- Name `PR_NUMBER`, `PR_URL` and any non-required findings in its first section.
- If a HALT heading was written, that section says so first.
</output>
