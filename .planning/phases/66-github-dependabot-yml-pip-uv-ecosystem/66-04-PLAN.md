---
phase: 66-github-dependabot-yml-pip-uv-ecosystem
plan: 04
type: execute
wave: 4
depends_on: ["66-02", "66-03"]
files_modified:
  - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md
autonomous: true
requirements: [DEP-01, DEP-03, DEP-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Wave-3 gate: 66-DEPENDABOT-EVIDENCE.md records UV_RUN_ID, UPDATER_UV_IMAGE, D03_BRANCH, OWNER_TAB_ANNOTATION and a non-empty UV_PR_CANDIDATES, and neither evidence file carries a HALT heading.'
    - 'D-04 / DEP-01 / SC#1: SC1_PR is a pull request on a dependabot/uv/ branch, authored by dependabot and based on main, whose head commit SC1_SHA lists both pyproject.toml and uv.lock in git show --name-only. Every candidate is classified BOTH, LOCK-ONLY or OTHER from its own head commit, and a lockfile-only PR never closes SC#1 on its own.'
    - 'D-05 leg 1 / DEP-04: the uv version dependabot uses is recorded literally from three sources. (1) GitHub docs: the dependabot-options-reference.md uv row and the supported-package-managers.md uv rows (v0.11 at planning), as DOCS_UV_ROW. (2) dependabot-core main: the uv/Dockerfile FROM line and the uv/helpers/requirements.txt uv== line, as DEPENDABOT_UV_MAIN. (3) The same two files at the commit named by the UPDATER_UV_IMAGE tag, as DEPENDABOT_UV_DEPLOYED. A disagreement between the docs and source is recorded as a stale-documentation finding, and REQUIREMENTS.md is not edited.'
    - 'D-05 leg 2 / DEP-04: three checks. (a) SC1_SHA:uv.lock lines 1 and 2 read version = 1 and revision = 3. (b) uv lock --check --directory on an exported copy of SC1_SHA exits 0, with LOCAL_UV recorded. (c) On the SC1_PR CI run SC1_RUN_ID, the Test Python 3.12 on ubuntu-latest job Install uv step logs Successfully installed uv version CI_UV_VERSION, and its Install dependencies step (uv sync --extra dev --locked) concludes success. D05_LEG2 = PASS only when all three hold.'
    - 'DEP-04: UV_LATEST (gh api repos/astral-sh/uv/releases/latest), LOCAL_UV and CI_UV_VERSION are recorded side by side with the dependabot versions. The local-uv figure carried by the ROADMAP SC#2 text is recorded as superseded by the measurement.'
    - 'D-05 failure branch: if leg 2 fails, a HALT section names the failing part verbatim and states that the fallback custom workflow is now in play; nothing is built, and the executor stops for the owner.'
    - 'D-06 / DEP-03: recorded from the real uv PRs, each item with a verdict of behaves-as-before, divergence-recorded or unobserved-documented-support: grouping (sphinx*, docutils*, typst* arriving as one sphinx-typst-stack PR, with sphinx-autodoc-typehints and sphinx-intl kept out), labels and any extra ecosystem label, the open uv PR count against open-pull-requests-limit 5, the lockfile-only PR count, and the ruff range change beside the pip-era #123 widening. Nothing is fixed.'
    - 'DEP-03 (boundary edge): the open dependabot/uv/ PR count is recorded against open-pull-requests-limit 5. A count above 5 is a divergence. At exactly 5, the UV_RUN_ID log is grepped for its limit message, and the lines are quoted.'
    - 'DEP-03 (empty edge): an empty labels array on every uv PR is recorded as the pre-existing state (the dependencies and automated labels do not exist, and #123 and #128 carry labels []), not as a regression. A group PR bumping a single package counts as the group behaving as before, as #128 did. Zero uv PRs halts in 66-03 rather than being recorded as behaving as before.'
    - 'DEP-04 (precision edge): versions are recorded as literal strings and compared only for string equality. No ordering is inferred by lexical or float comparison (0.12.13 against 0.12.7), and v0.11 from the docs is recorded verbatim, never normalized.'
    - 'Closure: the ## Requirement closure table carries DEP-01, DEP-03 and DEP-04, each reading MET with its deciding sections named. DEP-02 and DEP-05 are named as Phase 67 and left open.'
  artifacts:
    - path: .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md
      provides: 'same-commit classification (SC#1), D-05 legs 1 and 2, DEP-04 comparison, D-06 observations, requirement closure'
      contains: '## Requirement closure'
  key_links:
    - from: 'SC1_SHA (dependabot uv PR head)'
      to: 'pyproject.toml and uv.lock in one commit'
      via: 'git show --name-only after git fetch origin refs/pull/SC1_PR/head'
      pattern: 'SC1_SHA = '
    - from: 'UPDATER_UV_IMAGE tag'
      to: 'dependabot-core uv/Dockerfile and uv/helpers/requirements.txt at that commit'
      via: 'gh api repos/dependabot/dependabot-core/contents/<path>?ref=<tag>'
      pattern: 'DEPENDABOT_UV_DEPLOYED = '
    - from: 'SC1_PR CI run, Install dependencies step'
      to: 'CI uv (setup-uv version latest) running uv sync --locked on the dependabot-written lock'
      via: 'pull_request trigger scoped to main'
      pattern: 'CI_UV_VERSION = '
  prohibitions:
    - statement: 'MUST NOT build, commit or stage a custom lockfile-regeneration workflow, or edit any workflow, even when D-05 leg 2 fails; the finding is recorded and the executor halts (D-05, constraint 3).'
      requirement_id: DEP-04
      category: safety
      status: resolved
      verification: test
    - statement: 'MUST NOT merge, approve, close, comment on, rebase or @dependabot-command any dependabot PR, uv or pip; DEP-02 and DEP-05 belong to Phase 67 (D-02).'
      requirement_id: DEP-01
      category: values
      status: resolved
      verification: test
    - statement: 'MUST NOT fix a DEP-03 divergence in this phase: no versioning-strategy key, no label creation, no dependabot.yml edit (D-06).'
      requirement_id: DEP-03
      category: values
      status: resolved
      verification: test
    - statement: 'MUST NOT record a version, count or state copied from CONTEXT, RESEARCH, ROADMAP or this plan as a measurement; every figure in the evidence comes from a command run in this plan or a cited earlier evidence section.'
      requirement_id: DEP-04
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT check out a dependabot PR in this worktree (gh pr checkout, git switch, git checkout); its head is read through git show and an exported copy only.'
      requirement_id: DEP-04
      category: safety
      status: resolved
      verification: test
---

<objective>
Close DEP-01, DEP-03 and DEP-04 from the real `uv`-ecosystem PRs 66-03 found.

- **D-04 (SC#1):** a real `dependabot/uv/` PR whose head commit lists both `pyproject.toml` and `uv.lock`. It is read from the commit itself, not inferred from the YAML.
- **D-05:** the "v0.11 vs 0.12" question gets two legs of evidence, and REQUIREMENTS.md's wording stays literal.
  - Leg 1: the documented figure against dependabot-core's source, both on `main` and at the commit named by the updater image dependabot actually pulled (66-03's `UPDATER_UV_IMAGE`). The second read closes the gap D-05 itself names: source on `main` is not proof of the deployed image.
  - Leg 2: the PR's lock header, a local `uv lock --check`, and CI's own uv running `uv sync --locked` on that head.
  - If leg 2 fails, the fallback is recorded as in play and the executor halts. Nothing is built.
- **D-06:** grouping, labels, the PR limit, lockfile-only volume and range behaviour are recorded against the `pip`-era baseline. Divergences are written down, not fixed.

Reading the SC1 PR's `Install dependencies` step is DEP-04 evidence about lock compatibility only. DEP-02, which requires the test, lint and type jobs to run to a conclusion on a real dependabot PR, is Phase 67's and is not judged here.

Output: `66-DEPENDABOT-EVIDENCE.md` completed with the closure table.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-CONTEXT.md
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-RESEARCH.md
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md
@.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md
@.github/dependabot.yml
@pyproject.toml
@CLAUDE.md
</context>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: NOT MET -->
`## HALT` and `NOT MET` are words the executor must be able to write on a failure path; the gates require their absence.

## Planning-time census (2026-09-12: re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| GitHub docs uv rows | `dependabot-options-reference.md:618` `\| uv           \| \`uv\`             \| v0.11 \|`; `supported-package-managers.md:59` and `:61` `uv        \| \`uv\`            \| v0.11 …` | `gh api repos/github/docs/contents/…` |
| dependabot-core `main` | `uv/Dockerfile:15` `FROM ghcr.io/astral-sh/uv:0.12.7 AS uv`; `uv/helpers/requirements.txt:10` `uv==0.12.7` | `gh api repos/dependabot/dependabot-core/contents/…` |
| Deployed pip updater tag | `61ec2dff3408a9f16f4feffe7281fc69efccaaa5` resolves as a dependabot-core commit; the same two files at it read `0.12.7` | `gh api …/commits/61ec2dff…`, `?ref=` |
| `uv.lock` header on the milestone branch and `main` | `version = 1`, `revision = 3` | `sed -n 1,2p uv.lock` |
| Local uv / latest uv release / CI's uv | `0.12.13` / `0.12.13` (2026-09-10) / `Successfully installed uv version 0.12.13` in run 34681968010's `Install uv` step | `uv --version`; `gh api repos/astral-sh/uv/releases/latest`; `gh run view --job 103522124123 --log` |
| `ci.yml` ubuntu py3.12 job steps | `Install uv`, `Set up Python 3.12`, `Install dependencies` (`uv sync --extra dev --locked`), `Run tests with tox` | `gh api …/actions/jobs/103522124123` |
| Group members in `pyproject.toml` | `sphinx>=9.1,<10` (28), `docutils>=0.21,<0.23` (29), `typst>=0.15.0,<0.16` (30); exclusions `sphinx-autodoc-typehints>=1.0` (51), `sphinx-intl>=2.0` (52); `ruff>=0.15,<0.16` (40) | `grep -n pyproject.toml` |
| pip-era baseline | #123 title "update ruff requirement from <0.16,>=0.15 to >=0.15,<0.17" (range widened); #128 title "…docutils… in the sphinx-typst-stack group across 1 directory" (a one-package group PR); both `labels: []`; the labels `dependencies`, `automated` do not exist | `gh pr list`, `gh label list` |

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Where |
|----------|------|-------|
| `66-DEPENDABOT-EVIDENCE.md` sections `## Wave-3 gate`, `## Same-commit classification`, `## D-05 leg 1 documented and source uv versions`, `## D-05 leg 2 lock header`, `## D-05 leg 2 local lock check`, `## D-05 leg 2 CI uv on the PR head`, `## DEP-04 comparison`, `## D-06 observations`, `## Requirement closure`; keys `SC1_PR`, `SC1_SHA`, `SC1_RUN_ID`, `CI_UV_VERSION`, `LOCAL_UV`, `UV_LATEST`, `DOCS_UV_ROW`, `DEPENDABOT_UV_MAIN`, `DEPENDABOT_UV_DEPLOYED`, `D05_LEG2` | evidence | phase directory |

**Flagged assumptions carried here** (66-01 ledger):
- **A-DEP-01.** No BOTH-class PR means SC#1 halts.
- **A-DEP-04.** If the updater image tag does not resolve as a dependabot-core commit, leg 1 rests on `main` and says so.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: wave-3 gate, then classify every dependabot/uv/ PR by its own head commit and select the SC#1 PR whose head commit changes pyproject.toml and uv.lock together</name>
  <precondition>`66-DEPENDABOT-EVIDENCE.md` records a non-empty `UV_PR_CANDIDATES`, plus `UV_RUN_ID`, `UPDATER_UV_IMAGE`, `D03_BRANCH` and `OWNER_TAB_ANNOTATION`, and neither evidence file has a HALT heading. Otherwise halt at a blocking-human gate: there is no real uv PR to read.</precondition>
  <files>.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md (`UV_PR_CANDIDATES`, `## uv pull requests`)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-CONTEXT.md (D-04: head commit lists both files; lockfile-only goes to D-06)
    - CLAUDE.md, section "Worktree-isolated execution"
  </read_first>
  <action>
1. **Head check and provisioning.**
   - Record `test -f .git; echo "exit:$?"` and `pwd -P`, which must lie under `/home/yuta/Documents/typsphinx/.claude/worktrees/`.
   - Run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Task 2 runs uv from this worktree's `.venv`.

2. **Wave-3 gate.** Read the keys with `sed -n 's/^KEY = //p'`. Record `grep -c '^## HALT'` on both evidence files; both must be 0.

3. **Re-list, then classify.**
   - Re-list with 66-03's PR query (`gh pr list --state all --author app/dependabot --limit 50 --json number,title,headRefName,headRefOid,state,labels,createdAt,closedAt`). Record any `dependabot/uv/` PR that is new since 66-03 as new, and include it.
   - For each open `dependabot/uv/` PR `n`, ascending:
     - Record `gh pr view "$n" --json number,title,state,author,headRefName,headRefOid,baseRefName,createdAt,labels,commits,files` verbatim.
     - Run `git fetch origin "refs/pull/$n/head"`, and assert `git rev-parse FETCH_HEAD` equals `headRefOid`.
     - Record `git show --name-only --format='%H%n%an <%ae>%n%s' "$headRefOid"` verbatim.
     - Classify:
       - `BOTH` when the name list contains both `pyproject.toml` and `uv.lock`
       - `LOCK-ONLY` when it is exactly `uv.lock`
       - `OTHER` otherwise
   - Tabulate under `## Same-commit classification`: number, title, branch, head SHA, commit count, file list, class.

4. **Select the SC#1 PR (D-04).**
   - Record `SC1_PR = <the lowest-numbered BOTH PR>` and `SC1_SHA = <its headRefOid>`.
   - Also record its branch (it must start with `dependabot/uv/`), its author login (it must match `dependabot`), its base (`main`), and its commit count. When a PR has more than one commit, SC#1 reads the head commit, and the count is noted.
   - Quote `git show --stat "$SC1_SHA"` and `git show "$SC1_SHA" -- pyproject.toml` verbatim; D-06 reads the range change from them.
   - If no PR is BOTH: write `## HALT: no same-commit uv PR`, listing the LOCK-ONLY PRs as D-06 material, and stop at a blocking-human checkpoint (A-DEP-01). A lockfile-only PR never closes SC#1's same-commit clause on its own.

5. Never `gh pr checkout`, `git switch` or `git checkout` a dependabot head in this worktree; reading goes through `git show` only.

6. Append `## Wave-3 gate` and `## Same-commit classification`, with `SC1_PR` and `SC1_SHA` at column 0. Commit.
  </action>
  <verify>
    <automated>FM=.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md &amp;&amp; F=.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; [ -n "$(k UV_PR_CANDIDATES)" ] &amp;&amp; N="$(k SC1_PR)" &amp;&amp; SHA="$(k SC1_SHA)" &amp;&amp; [ -n "$N" ] &amp;&amp; [ -n "$SHA" ] &amp;&amp; git cat-file -e "$SHA^{commit}" &amp;&amp; L="$(git show --name-only --format= "$SHA")" &amp;&amp; printf '%s\n' "$L" | grep -qx pyproject.toml &amp;&amp; printf '%s\n' "$L" | grep -qx uv.lock &amp;&amp; [ "$(gh pr view "$N" --json headRefName -q '.headRefName | startswith("dependabot/uv/")')" = true ] &amp;&amp; [ "$(gh pr view "$N" --json author -q '.author.login | test("dependabot")')" = true ] &amp;&amp; [ "$(gh pr view "$N" --json baseRefName -q .baseRefName)" = main ] &amp;&amp; gh pr view "$N" --json commits -q '.commits[].oid' | grep -qx "$SHA" &amp;&amp; grep -q '^## Same-commit classification' "$F" &amp;&amp; ! grep -q '^## HALT' "$F" &amp;&amp; ! grep -q '^## HALT' "$FM"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- `UV_PR_CANDIDATES`, `SC1_PR` or `SC1_SHA` missing
- `SC1_SHA` not a commit in this repository
- its head commit not listing both `pyproject.toml` and `uv.lock`
- the PR not on a `dependabot/uv/` branch, not authored by dependabot, or not based on `main`
- `SC1_SHA` not among the PR's commits
- the classification section missing
- a HALT heading in either evidence file</fails_when>
  </verify>
  <acceptance_criteria>
    - Every open `dependabot/uv/` PR is classified BOTH, LOCK-ONLY or OTHER from `git show --name-only` of its own head commit, with the gh JSON quoted.
    - `SC1_PR` is on a `dependabot/uv/` branch, authored by dependabot and based on `main`. Its head `SC1_SHA` lists both `pyproject.toml` and `uv.lock`.
    - The `pyproject.toml` hunk of `SC1_SHA` is quoted for D-06.
    - No dependabot head was checked out in this worktree.
  </acceptance_criteria>
  <done>SC#1's same-commit clause is read from a real dependabot uv PR's own head commit.</done>
</task>

<task type="auto">
  <name>Task 2: D-05 legs 1 and 2: documented vs source vs deployed dependabot uv version, the lock header, a local uv lock --check, and CI's uv running uv sync --locked on the SC#1 head</name>
  <precondition>`SC1_PR` and `SC1_SHA` are recorded by Task 1, and `UPDATER_UV_IMAGE` by 66-03.</precondition>
  <files>.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md (`SC1_PR`, `SC1_SHA`, `UPDATER_UV_IMAGE`)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md (`MERGE_SHA`)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-CONTEXT.md (D-05, both legs and the failure branch)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-RESEARCH.md (Pitfall 3; Astral's revision vs version semantics)
    - .github/workflows/ci.yml lines 10-40 (the `test` job: `Install uv` with `version: "latest"`, then `Install dependencies`)
  </read_first>
  <action>
1. **Leg 1: documentation.**
   - Record `gh api repos/github/docs/commits/main --jq .sha`.
   - Record `gh api repos/github/docs/contents/content/code-security/reference/supply-chain-security/dependabot-options-reference.md --jq .content | base64 -d | grep -n '^| uv '` verbatim (line 618 at planning).
   - Record `gh api repos/github/docs/contents/data/reusables/dependabot/supported-package-managers.md --jq .content | base64 -d | grep -n '^uv '` verbatim (lines 59 and 61 at planning).
   - Record `DOCS_UV_ROW = <the version cell, verbatim>`.

2. **Leg 1: dependabot-core `main`.**
   - Record `gh api repos/dependabot/dependabot-core/commits/main --jq .sha`.
   - Record `gh api repos/dependabot/dependabot-core/contents/uv/Dockerfile --jq .content | base64 -d | grep -n 'astral-sh/uv'` verbatim.
   - Record `gh api repos/dependabot/dependabot-core/contents/uv/helpers/requirements.txt --jq .content | base64 -d | grep -n '^uv=='` verbatim.
   - Record `DEPENDABOT_UV_MAIN = <the FROM-line version>`.

3. **Leg 1: the deployed image.**
   - Set `TAG` to the text after the last `:` of `UPDATER_UV_IMAGE`.
   - Record `gh api "repos/dependabot/dependabot-core/commits/$TAG" --jq '[.sha, .commit.committer.date, (.commit.message | split("\n")[0])] | @tsv'`.
   - Record the same two files read `?ref=$TAG`, verbatim.
   - Record `DEPENDABOT_UV_DEPLOYED = <the FROM-line version at TAG>`.
   - If the tag does not resolve: record the API error verbatim, and record `DEPENDABOT_UV_DEPLOYED = unresolved (tag not a dependabot-core commit)`. Leg 1 then rests on `main` (A-DEP-04).
   - Write the finding in one sentence. Where `DOCS_UV_ROW` differs from the source versions, this is a stale-documentation finding (D-05), and REQUIREMENTS.md's DEP-04 wording stays literal and unedited.

4. **Leg 2 (a): lock header.** Record `git show "$SC1_SHA:uv.lock" | sed -n 1,2p` and `git show "$MERGE_SHA:uv.lock" | sed -n 1,2p` verbatim. Both must be `version = 1` and `revision = 3`.

5. **Leg 2 (b): local lock check on an exported copy.**
   - Make `S="$(mktemp -d)"`. Write the archive to a file with `git archive -o "$S/head.tar" "$SC1_SHA"`, so a git failure is not swallowed by a pipe, then extract it with `tar -x -f "$S/head.tar" -C "$S"`.
   - From this worktree's own directory (so the `uv` shim resolves this worktree's `.venv/bin/uv`), record `command -v uv` and `uv --version`, and set `LOCAL_UV = <version>`.
   - Record `uv lock --check --directory "$S"; echo "exit:$?"` verbatim. It must print `exit:0`.
   - Never run it with the working directory inside `$S`, where the shim would resolve a different uv.
   - Remove `$S` afterwards. The PR branch is never written to.

6. **Leg 2 (c): CI's uv on the PR head.**
   - Locate the run: `gh run list --workflow=ci.yml --event pull_request --limit 20 --json databaseId,headSha,headBranch,status,conclusion,createdAt`, selecting the row whose headSha is `SC1_SHA`. Record `SC1_RUN_ID = <id>`.
   - If no row exists, repeat the query at 60-second intervals for up to 15 minutes. If there is still none, record that and stop at a blocking-human checkpoint.
   - Wait with `gh run watch "$SC1_RUN_ID" --interval 30`, in the foreground with a timeout of 600000 ms, reissued as needed (cap 60 minutes).
   - For the `Test Python 3.12 on ubuntu-latest` job:
     - record its id and the steps from `gh api repos/YuSabo90002/typsphinx/actions/jobs/<id> --jq '.steps[] | [.number, .name, .conclusion] | @tsv'`, verbatim
     - quote `gh run view --job <id> --log | grep -m1 'Successfully installed uv version'` verbatim, and set `CI_UV_VERSION = <version>`
     - record the `Install dependencies` step's conclusion
   - Transcribe the other jobs' conclusions as a census only, stating that DEP-02 is judged in Phase 67 and not here.

7. **DEP-04 comparison.**
   - Record `gh api repos/astral-sh/uv/releases/latest --jq '[.tag_name, .published_at] | @tsv'`, and set `UV_LATEST = <tag>`.
   - Tabulate `DOCS_UV_ROW`, `DEPENDABOT_UV_MAIN`, `DEPENDABOT_UV_DEPLOYED`, `CI_UV_VERSION`, `UV_LATEST` and `LOCAL_UV`, plus the lock header, with string equality as the only comparison (the precision edge).
   - Note that the local-uv figure the ROADMAP SC#2 text carries is superseded by `LOCAL_UV`, without copying the stale number.
   - Quote Astral's lockfile-versioning statement from RESEARCH Pitfall 3 as context, not as evidence.

8. **Leg 2 verdict.** Record `D05_LEG2 = PASS` only when (a) holds, (b) printed `exit:0`, and (c)'s `Install dependencies` concluded `success`. Otherwise:
   - record `D05_LEG2 = FAIL`
   - write `## HALT: D-05 leg 2 failed`, quoting the failing part verbatim and stating that the fallback custom workflow is now in play per D-05
   - build and stage nothing, and edit no workflow
   - stop at a blocking-human checkpoint

9. Append `## D-05 leg 1 documented and source uv versions`, `## D-05 leg 2 lock header`, `## D-05 leg 2 local lock check`, `## D-05 leg 2 CI uv on the PR head` and `## DEP-04 comparison`, with the keys at column 0. Commit.
  </action>
  <verify>
    <automated>F=.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; SHA="$(k SC1_SHA)" &amp;&amp; [ -n "$SHA" ] &amp;&amp; UL="$(git show "$SHA:uv.lock")" &amp;&amp; [ "$(printf '%s\n' "$UL" | sed -n 1,2p | tr '\n' ' ')" = 'version = 1 revision = 3 ' ] &amp;&amp; [ "$(k D05_LEG2)" = PASS ] &amp;&amp; RID="$(k SC1_RUN_ID)" &amp;&amp; [ -n "$RID" ] &amp;&amp; [ "$(gh run view "$RID" --json headSha -q .headSha)" = "$SHA" ] &amp;&amp; J="$(gh run view "$RID" --json jobs -q '.jobs[] | select(.name == "Test Python 3.12 on ubuntu-latest") | .databaseId')" &amp;&amp; [ -n "$J" ] &amp;&amp; [ "$(gh api "repos/YuSabo90002/typsphinx/actions/jobs/$J" --jq '.steps[] | select(.name == "Install dependencies") | .conclusion')" = success ] &amp;&amp; CV="$(k CI_UV_VERSION)" &amp;&amp; [ -n "$CV" ] &amp;&amp; gh run view --job "$J" --log | grep -qF "Successfully installed uv version $CV" &amp;&amp; for key in UV_LATEST LOCAL_UV DOCS_UV_ROW DEPENDABOT_UV_MAIN DEPENDABOT_UV_DEPLOYED; do [ -n "$(k "$key")" ] || { echo "missing key: $key"; exit 1; }; done &amp;&amp; for s in '## D-05 leg 1 documented and source uv versions' '## D-05 leg 2 lock header' '## D-05 leg 2 local lock check' '## D-05 leg 2 CI uv on the PR head' '## DEP-04 comparison'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; awk '/^## D-05 leg 2 local lock check/{f=1;next} /^## /{f=0} f' "$F" | grep -q 'exit:0' &amp;&amp; awk '/^## D-05 leg 1 documented and source uv versions/{f=1;next} /^## /{f=0} f' "$F" | grep -q 'uv==' &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing key: <name>` or `missing section: <name>` printed. Each of these makes it fail:
- the SC#1 head's `uv.lock` not reading `version = 1` / `revision = 3`
- `D05_LEG2` not `PASS`
- `SC1_RUN_ID` missing, or not at `SC1_SHA`
- no `Test Python 3.12 on ubuntu-latest` job, or its `Install dependencies` step not `success`
- `CI_UV_VERSION` missing or absent from that job's `Install uv` log line
- a leg-1 or comparison key missing
- a section missing
- the local lock check lacking `exit:0`
- the leg-1 section quoting no `uv==` line
- a HALT heading (the leg-2 failure path)</fails_when>
  </verify>
  <acceptance_criteria>
    - Leg 1 quotes the docs rows, the dependabot-core `main` FROM and `uv==` lines, and the same lines at the deployed image tag (or its API error), with the three version keys and the github/docs and dependabot-core commit SHAs read.
    - Leg 2 records the lock header of `SC1_SHA` and `MERGE_SHA` (`1`/`3`), `uv lock --check --directory` on the exported copy with `exit:0` and `LOCAL_UV`, and the SC#1 CI run's `Install uv` log line and `Install dependencies` conclusion.
    - `D05_LEG2 = PASS` is recorded only when all three hold.
    - `## DEP-04 comparison` compares versions by string equality only, and states that DEP-02 remains Phase 67's.
    - No workflow is created or edited, and no dependabot head is checked out.
  </acceptance_criteria>
  <done>The uv version question is answered from documentation, source, the deployed image, the PR's own lock and CI's own uv: recorded either way, with the fallback reached only on a measured leg-2 failure.</done>
</task>

<task type="auto">
  <name>Task 3: D-06 observations against the pip-era baseline, then the requirement closure table for DEP-01, DEP-03 and DEP-04</name>
  <precondition>`D05_LEG2 = PASS` is recorded by Task 2, and there is no HALT heading.</precondition>
  <files>.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md (all sections; `## Same-commit classification`; `## uv pull requests`; `## D-02 post-merge snapshot`)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-MAIN-PR-EVIDENCE.md (`## D-02 pre-merge snapshot`: the pip-era titles and labels; `## Post-merge main`)
    - .planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-CONTEXT.md (D-06 item list; the #15693 expectation)
    - .github/dependabot.yml (the group, labels and limit as configured)
    - pyproject.toml lines 25-55 (group members and exclusions)
  </read_first>
  <action>
1. **Grouping (D-06).**
   - Re-list the dependabot PRs verbatim with 66-03's query.
   - The group PR is a `dependabot/uv/` PR whose branch starts with `dependabot/uv/sphinx-typst-stack` or whose title contains `sphinx-typst-stack group`.
   - For it:
     - quote the `+` and `-` lines of `gh pr diff <n>` that fall in `pyproject.toml`, and name the packages bumped
     - record `gh pr diff <n> | grep -cE '^[-+].*(sphinx-autodoc-typehints|sphinx-intl)'`; 0 means the exclusions held, and anything else is a divergence to record
   - Set it beside #128's pip-era title, from the pre-merge snapshot. A group PR bumping one package counts as behaving as before, as #128 was (the empty edge).
   - If no group PR opened: record "no sphinx*/docutils*/typst* bump observed in the window". Quote the `groups` section of `dependabot-options-reference.md`, where `patterns` and `exclude-patterns` carry no per-ecosystem restriction, and give the verdict `unobserved-documented-support` (SC#3 accepts documented ecosystem support).

2. **Labels.**
   - Record each uv PR's `labels` verbatim, and `gh label list --json name --jq '.[].name'` verbatim.
   - An empty array on every uv PR, with `dependencies` and `automated` still absent from the repository, is `behaves-as-before`: #123 and #128 carry `[]` (the empty edge; Pitfall 2).
   - Any label present is `divergence-recorded`, named, and set against `dependabot-options-reference.md:464` on ecosystem labels.
   - No label is created.

3. **PR limit** (the boundary edge).
   - Record the count of open `dependabot/uv/` PRs.
   - At or below 5 is recorded. At exactly 5, quote `gh run view "$UV_RUN_ID" --log | grep -i 'limit'`. Above 5 is `divergence-recorded`.
   - Record whether the `pip`-ecosystem PRs, still open or now closed, share that count.

4. **Lockfile-only volume.** Record the LOCK-ONLY count from Task 1's classification. It is new under uv, since pip had no lockfile, so it is recorded as `divergence-recorded` by construction. Setting `versioning-strategy` stays a deferred idea, and nothing is added.

5. **Range behaviour.**
   - For the uv PR that bumps `ruff`, when one exists, quote its `pyproject.toml` diff line.
   - Set it beside #123's pip-era widening (`<0.16` to `<0.17`, from #123's title).
   - The verdict is `behaves-as-before` when it widens the upper bound, and `divergence-recorded` otherwise.
   - The expectation, taken from dependabot-core #15693 (merged 2026-08-18), is stated as an expectation and confirmed or contradicted only by this observation.
   - If no ruff PR opened, record that and give the verdict `unobserved-documented-support`, quoting the `versioning-strategy` `auto` row.

6. **`## D-06 observations`.** A table with columns item | observed | pip-era baseline | verdict. It has one row each for: `sphinx-typst-stack` grouping, exclusions, labels, `open-pull-requests-limit`, lockfile-only PRs, and `ruff` range. Each verdict is one of `behaves-as-before`, `divergence-recorded` or `unobserved-documented-support`. Nothing is fixed (D-06).

7. **`## Requirement closure`.** A table of Requirement | Status | Deciding sections, where each status reads MET or NOT MET:
   - **DEP-01 is MET when all of these hold:**
     - `main`'s `.github/dependabot.yml` is `CONFIG_BLOB` with the uv line and an untouched `github-actions` entry (`66-MAIN-PR-EVIDENCE.md` § Post-merge main)
     - `UV_RUN_ID` succeeded (§ uv update run)
     - `OWNER_TAB_ANNOTATION = none` (§ Owner Dependabot-tab read)
     - `SC1_PR` is BOTH (§ Same-commit classification)
   - **DEP-03 is MET when** every D-06 row carries a verdict (§ D-06 observations).
   - **DEP-04 is MET when** leg 1 is recorded and `D05_LEG2 = PASS` (§ D-05 leg 1, § D-05 leg 2 …, § DEP-04 comparison).
   - Under the table, state:
     - DEP-02 and DEP-05 are Phase 67's
     - Phase 67 SC#2's inputs are § D-02 pre-merge snapshot, § D-02 post-merge snapshot and § uv pull requests
     - the deferred correction about #128 being a grouped PR is carried to Phase 67 discuss and not amended here
   - If any row reads NOT MET, stop at a blocking-human checkpoint naming it.

8. Commit.
  </action>
  <verify>
    <automated>F=.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-DEPENDABOT-EVIDENCE.md &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; for r in DEP-01 DEP-03 DEP-04; do grep -qE "^\| *$r *\|.*MET" "$F" || { echo "no MET row: $r"; exit 1; }; ! grep -qE "^\| *$r *\|.*NOT MET" "$F" || { echo "NOT MET row: $r"; exit 1; }; done &amp;&amp; [ "$(k OWNER_TAB_ANNOTATION)" = none ] &amp;&amp; [ "$(k D05_LEG2)" = PASS ] &amp;&amp; D6="$(awk '/^## D-06 observations/{f=1;next} /^## /{f=0} f' "$F")" &amp;&amp; for t in sphinx-typst-stack exclu labels open-pull-requests-limit lockfile-only ruff; do printf '%s\n' "$D6" | grep -qi -- "$t" || { echo "D-06 item missing: $t"; exit 1; }; done &amp;&amp; [ "$(printf '%s\n' "$D6" | grep -c '^|')" -ge 8 ] &amp;&amp; [ "$(printf '%s\n' "$D6" | grep -cE 'behaves-as-before|divergence-recorded|unobserved-documented-support')" -ge 6 ] &amp;&amp; grep -q 'Phase 67' "$F" &amp;&amp; grep -q '^## Requirement closure' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `no MET row: <req>`, `NOT MET row: <req>` or `D-06 item missing: <item>` printed. Each of these makes it fail:
- a closure row for DEP-01, DEP-03 or DEP-04 absent or reading NOT MET
- `OWNER_TAB_ANNOTATION` not `none`
- `D05_LEG2` not `PASS`
- the D-06 section lacking any of its six items
- the D-06 table having fewer than eight table lines, or fewer than six verdicts
- no mention of Phase 67
- the closure section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## D-06 observations` has six rows (grouping, exclusions, labels, `open-pull-requests-limit`, lockfile-only PRs, `ruff` range), each carrying observed, pip-era baseline and one of the three verdicts, with the underlying gh output quoted above the table.
    - `## Requirement closure` has DEP-01, DEP-03 and DEP-04 rows, each MET and naming its deciding sections.
    - DEP-02 and DEP-05 are named as Phase 67's.
    - `.github/dependabot.yml`, the workflows and the repository labels are unchanged by this plan.
  </acceptance_criteria>
  <done>DEP-01, DEP-03 and DEP-04 are closed on recorded evidence from dependabot's own PRs and jobs, with every divergence written down rather than fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependabot PR head → local tooling | a bot-authored `pyproject.toml`/`uv.lock` is resolved locally by `uv lock --check` |
| external repositories → evidence | github/docs and dependabot-core content is quoted as leg-1 evidence |
| a failed leg 2 → the fallback workflow | the one path by which this phase could grow a privileged workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-14 | Elevation of Privilege | a fallback lockfile workflow on `pull_request_target` or a PAT, built on a leg-2 failure | high | mitigate | A prohibition forbids building, staging or editing any workflow. Leg-2 failure writes a HALT heading and stops at a blocking-human checkpoint, and the verify requires `D05_LEG2 = PASS` with no HALT heading. |
| T-66-15 | Tampering | executing dependabot-authored content in this worktree | medium | mitigate | The head is never checked out. `uv lock --check` runs on a `git archive` export in a scratch directory; it resolves and writes nothing. The author must match dependabot and the branch must be `dependabot/uv/`. |
| T-66-16 | Tampering | new dependency versions arriving through the uv PRs | medium | transfer | Nothing is merged in this phase. The merit judgement is DEP-05's, in Phase 67. |
| T-66-17 | Repudiation | a version figure copied from planning text instead of measured | medium | mitigate | Every key is written from a command quoted in the same section. A prohibition (judgment tier) forbids copying figures, and the comparison is string equality on recorded values. |
| T-66-SC | Tampering | `uv sync --extra dev` during provisioning | low | accept | installs only the pins already in `uv.lock`; no new package |
</threat_model>

<verification>
- SC#1 is read from a real dependabot uv PR's head commit listing `pyproject.toml` and `uv.lock`.
- D-05 leg 1 is recorded from the docs, dependabot-core `main` and the deployed tag.
- D-05 leg 2 passed on the lock header, the local lock check and CI's own `uv sync --locked`.
- D-06 carries six verdicts, and the closure table reads MET for DEP-01, DEP-03 and DEP-04.
</verification>

<success_criteria>
SC#1, SC#2 and SC#3 of Phase 66 are recorded:
- **SC#1:** the uv entry took effect, per dependabot's own job, the owner's tab read and a same-commit PR.
- **SC#2:** the uv version question is measured against docs, source, the deployed image, CI's uv and the lock.
- **SC#3:** grouping, labels and the limit behave as before, or the divergence is recorded.
</success_criteria>

<output>
Create `.planning/phases/66-github-dependabot-yml-pip-uv-ecosystem/66-04-SUMMARY.md` when done.
- Its frontmatter `requirements-completed:` lists only the requirements whose closure row reads MET.
- Its first section states `SC1_PR`, `SC1_SHA`, `D05_LEG2`, the three dependabot-side uv versions beside `CI_UV_VERSION`, and every D-06 divergence.
</output>
