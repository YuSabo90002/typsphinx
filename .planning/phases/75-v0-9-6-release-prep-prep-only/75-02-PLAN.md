---
phase: 75-v0-9-6-release-prep-prep-only
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-LEFTOVERS-EVIDENCE.md
  - .planning/phases/75-v0-9-6-release-prep-prep-only/COVERAGE.md
  - .planning/todos/pending/2026-09-20-literal-block-docstring-args-still-name-only-the-literal-block-node.md
autonomous: true
requirements: [REL-15]
# REL-15 is a coverage ID only (ROADMAP constraint 1 and the Phase 75 cross-cutting
# constraints): no plan closes it, no plan touches its checkbox, and it is checked at
# /gsd-complete-milestone against observed publish evidence.
requirements-completed: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'D-13: the five untracked repository-root scratch files left by Phase 74''s language-tag probing are gone from the main checkout, so `git status` is clean of them at the milestone close. They are untracked, so the deletion produces no commit and does not enter SC5''s `git diff` reading.'
    - 'D-13 boundary: `.gitignore` is not edited. Adding an ignore rule would be a product-tree commit outside the file set REL-15 names, and deletion alone is what was decided.'
    - 'The deletion is falsifiable: the five paths, their byte sizes and the main checkout''s `git status --porcelain` rows are recorded before removal, and only paths that are untracked, sit at the main checkout root, and match the probe naming shape are removed.'
    - 'D-14: IN-01 is filed, not fixed. A new record in `.planning/todos/pending/` names `visit_literal_block` and `depart_literal_block`, quotes the stale `Args:` text, cites `74-REVIEW.md`, and states that the prep-only fence is why it is deferred. Nothing under `typsphinx/` is edited.'
    - 'The api-coverage seal-time gate cannot false-positive on this phase''s read-only `gh` / PyPI / Read the Docs probes and single `workflow_dispatch`: `COVERAGE.md` carries the one-line declaration naming what this phase touches instead, and the gate is run and its JSON recorded.'
  artifacts:
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-LEFTOVERS-EVIDENCE.md
      provides: 'the before and after census of the five untracked probe files, the filed-todo record and the api-coverage gate result'
      contains: 'PROBE_FILES_AFTER = '
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/COVERAGE.md
      provides: 'the one-line no-external-API declaration the verify-pre gate reads'
      contains: 'No external API integration: '
    - path: .planning/todos/pending/2026-09-20-literal-block-docstring-args-still-name-only-the-literal-block-node.md
      provides: 'the deferred IN-01 record (D-14), with the two function names, the stale text and the deferral reason'
      contains: 'depart_literal_block'
  key_links:
    - from: '74-REVIEW.md IN-01'
      to: '.planning/todos/pending/ IN-01 record'
      via: 'a new todo whose source field cites the review finding by ID'
      pattern: 'IN-01'
    - from: 'COVERAGE.md'
      to: 'api-coverage.verify-pre at phase seal'
      via: 'gsd-tools check api-coverage.verify-pre over the phase directory'
      pattern: 'No external API integration: '
  prohibitions:
    - statement: 'MUST NOT edit any file under `typsphinx/`, including the IN-01 `Args:` text this plan files as a todo, and MUST NOT edit `.gitignore`.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT delete, move or modify any tracked file, any file outside the main checkout root, or any root file whose name does not match the recorded probe shape; the deletion is limited to the five untracked paths named in the census.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT edit, stage or commit `.planning/REQUIREMENTS.md`, and MUST NOT move REL-15''s checkbox.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT fabricate a coverage matrix row for a capability that does not exist; the declaration states what this phase touches instead of an external API integration.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
---

<objective>
Clear the two Phase-74 leftovers the milestone audit handed to this phase, and pre-empt the
api-coverage gate:
- delete the five untracked `probe_*.typ` scratch files at the repository root (D-13)
- file IN-01 as a pending todo rather than fixing it (D-14, the prep-only fence)
- write the one-line `COVERAGE.md` declaration and run the gate

Purpose: the probe files pollute `git status` at the milestone close, IN-01 must be tracked rather
than silently dropped, and this phase's plans name `gh api`, PyPI, Read the Docs and a
`workflow_dispatch` often enough that the seal-time api-coverage detector has a live false-positive
risk in this repository's own history.

Output: `75-LEFTOVERS-EVIDENCE.md`, `COVERAGE.md`, one new pending todo.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md
@.planning/v0.9.6-MILESTONE-AUDIT.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` and
run project tooling through `uv run`.

**The five scratch files are in the MAIN CHECKOUT, not in this worktree.** They are untracked, so
`git worktree add` never materialised them here. Address them by absolute path under
`/home/yuta/Documents/typsphinx/`. This is the one place in this phase where a task reaches outside
its own worktree, and it is deliberate: an untracked file cannot be removed by a commit. Touch
nothing else in the main checkout — no tracked file, no ref, no `git` command that writes.

**Evidence key lines.** Bare values only, `KEY = value` on its own line, explanation on the next.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_75_02`; helper prefix `p7502_`.

**Commits.** Plain `git commit`. Never a GSD commit helper. Never write `75-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: probe_ -->
<!-- planner-discipline-allow: probe_\*\.typ -->

## Planning-time census (2026-09-20, main checkout; context only — re-measure, never trust)

| Fact | Value at planning |
|------|-------------------|
| `git status --porcelain` in the main checkout | exactly five `??` rows: `probe_bogusxyz.typ`, `probe_none.typ`, `probe_pycon.typ`, `probe_python.typ`, `probe_text.typ` |
| `.planning/todos/pending/` | three records: QUA-08 linkcheck CI, NUM-01, MSG-06 |
| IN-01 | `74-REVIEW.md` § "Info", `typsphinx/translator.py:2444-2445` and `:2593-2595` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: census, guard and delete the five untracked probe scratch files in the main checkout</name>
  <precondition>`/home/yuta/Documents/typsphinx/.git` is a directory (the main checkout), and this executor's own `pwd -P` is a different directory under `.claude/worktrees/`.</precondition>
  <reversibility rating="costly">Deletes five untracked files that exist in no commit, so `git` cannot restore them. They are regenerable at any time by re-running Phase 74's D-01 language-tag probe, and the milestone audit classifies them as leftover scratch, so the cost is a re-run rather than a lost artifact.</reversibility>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-LEFTOVERS-EVIDENCE.md</files>
  <read_first>
    - .planning/v0.9.6-MILESTONE-AUDIT.md § `tech_debt` (the item naming the five files and why they are outside every scope fence)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md D-13 (deletion only; `.gitignore` is not edited)
    - .gitignore (read it to confirm the deletion is not being substituted for an ignore rule, and leave it unchanged)
  </read_first>
  <action>
Create `75-LEFTOVERS-EVIDENCE.md`, headed `# Phase 75 — Phase 74 Leftovers (D-13, D-14)`.

1. **`## Head check and provisioning`.** Record `date -u +%FT%TZ`, `pwd -P`,
   `test -f .git; echo "exit:$?"`, `command -v uv`, the shim check, the provisioning line with its
   exit, `BASE_75_02 = ` from `git rev-parse HEAD` before any commit, and `SCRATCH_75_02 = `.

2. **`## Main checkout identity`.** Record `test -d /home/yuta/Documents/typsphinx/.git; echo "exit:$?"`,
   which must print `exit:0`, and `git -C /home/yuta/Documents/typsphinx rev-parse --show-toplevel`,
   which must print `/home/yuta/Documents/typsphinx`. Key `MAIN_CHECKOUT_CONFIRMED = yes`. State in
   one sentence that this task acts on the main checkout's untracked working-tree files and on
   nothing else there.

3. **`## Probe census before`.** Record:
   - `git -C /home/yuta/Documents/typsphinx status --porcelain`, verbatim, in a fenced block.
   - `find /home/yuta/Documents/typsphinx -maxdepth 1 -name 'probe_*.typ' -printf '%p %s\n'`,
     verbatim, as the file list with byte sizes. Key `PROBE_FILES_BEFORE = ` the line count.
   - For each path found, `git -C /home/yuta/Documents/typsphinx ls-files --error-unmatch <path>; echo "exit:$?"`,
     which must print a non-zero exit for every one: a tracked file would mean the census matched
     something it must not delete. Key `PROBE_TRACKED_HITS = 0`.
   - The five expected basenames as a table with each file's first line, so the evidence shows what
     was destroyed.
   If `PROBE_FILES_BEFORE` is already 0, that is a resumed run or an already-clean tree: record it,
   set `PROBE_FILES_AFTER = 0` and skip step 4 rather than searching for something else to delete.
   If any path found is tracked, write `## HALT: probe census matched a tracked file` and stop.

4. **`## Deletion`.** Remove exactly the paths enumerated in step 3, one `rm` per path, each with
   its own recorded command line. Do not use a recursive remove and do not widen the glob.

5. **`## Probe census after`.** Record the same `find` command, key `PROBE_FILES_AFTER = `, which
   must be 0, and `git -C /home/yuta/Documents/typsphinx status --porcelain` again, verbatim. Record
   `GITIGNORE_UNCHANGED = yes` after showing
   `git -C /home/yuta/Documents/typsphinx diff --name-only -- .gitignore` is empty, and state that
   D-13 rejected an ignore rule as a product-tree commit outside the file set REL-15 names.

6. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-LEFTOVERS-EVIDENCE.md" &amp;&amp; M=/home/yuta/Documents/typsphinx &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_75_02)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; test -d "$M/.git" &amp;&amp; [ "$(git -C "$M" rev-parse --show-toplevel)" = "$M" ] &amp;&amp; [ "$(k MAIN_CHECKOUT_CONFIRMED)" = yes ] &amp;&amp; [ "$(find "$M" -maxdepth 1 -name 'probe_*.typ' | wc -l)" = 0 ] &amp;&amp; [ "$(k PROBE_FILES_AFTER)" = 0 ] &amp;&amp; [ "$(k PROBE_TRACKED_HITS)" = 0 ] &amp;&amp; PBF="$(k PROBE_FILES_BEFORE)" &amp;&amp; case "$PBF" in ''|*[!0-9]*) false ;; *) true ;; esac &amp;&amp; for n in probe_bogusxyz.typ probe_none.typ probe_pycon.typ probe_python.typ probe_text.typ; do test ! -e "$M/$n" || { echo "still present: $n"; exit 1; }; grep -qF "$n" "$F" || { echo "not in census: $n"; exit 1; }; done &amp;&amp; [ "$(k GITIGNORE_UNCHANGED)" = yes ] &amp;&amp; [ -z "$(git -C "$M" diff --name-only -- .gitignore)" ] &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- .gitignore typsphinx)" ] &amp;&amp; for s in '## Head check and provisioning' '## Main checkout identity' '## Probe census before' '## Deletion' '## Probe census after'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `still present: NAME`, `not in census: NAME` or `missing section: NAME` printed. Each of these makes it fail: not in a worktree, or the shim absent; `BASE_75_02` malformed or not an ancestor of HEAD; `/home/yuta/Documents/typsphinx/.git` not a directory, or its toplevel resolving elsewhere; `MAIN_CHECKOUT_CONFIRMED` not `yes`; any file matching the probe shape still present at the main checkout root; `PROBE_FILES_AFTER` not 0, `PROBE_TRACKED_HITS` not 0, or `PROBE_FILES_BEFORE` not an integer; any of the five expected basenames still on disk or absent from the evidence census; `.gitignore` modified in either tree, or `GITIGNORE_UNCHANGED` not `yes`; anything under `typsphinx/` changed; any named section missing; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `find /home/yuta/Documents/typsphinx -maxdepth 1 -name 'probe_*.typ'` returns nothing, and none of the five expected basenames exists at that path.
    - `75-LEFTOVERS-EVIDENCE.md` records the before census with byte sizes, a non-zero `ls-files --error-unmatch` exit for every path removed, one `rm` line per path, and the after census.
    - `git -C /home/yuta/Documents/typsphinx diff --name-only -- .gitignore` is empty and `GITIGNORE_UNCHANGED = yes`.
    - No file under `typsphinx/` changed in this worktree.
  </acceptance_criteria>
  <done>The repository root is clean of Phase 74's scratch probe files, the deletion is auditable file by file, and `.gitignore` was not used as a substitute.</done>
</task>

<task type="auto">
  <name>Task 2: File IN-01 as a pending todo rather than fixing it (D-14)</name>
  <files>.planning/todos/pending/2026-09-20-literal-block-docstring-args-still-name-only-the-literal-block-node.md, .planning/phases/75-v0-9-6-release-prep-prep-only/75-LEFTOVERS-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-REVIEW.md § "Info" → IN-01 (the finding, its two file:line references and its suggested fix)
    - typsphinx/translator.py around the `visit_literal_block` and `depart_literal_block` definitions (read the current `Args:` text verbatim so the todo quotes the real state, and change nothing)
    - .planning/todos/pending/2026-08-29-hardcoded-delimiter-path-fragments-in-translator-relative-path-debug-logs.md (the front-matter schema and prose register every todo in this directory uses)
  </read_first>
  <action>
1. Write `.planning/todos/pending/2026-09-20-literal-block-docstring-args-still-name-only-the-literal-block-node.md`
   with front matter in the same schema the existing pending todos use: `created` as an ISO date,
   `title` naming both functions and the stale `Args:` text, `area: translator, docs`,
   `resolves_phase: null`, `source` citing `74-REVIEW.md` § Info IN-01 by ID, `severity: minor`, and
   a `files:` list carrying `typsphinx/translator.py` with the two line ranges the review named,
   each as a trailing comment. Re-measure those line numbers against the current file before writing
   them — the review's `2444-2445` / `2593-2595` are planning-time context, not facts to transcribe.

2. `## Problem`: state that both signatures were widened to
   `nodes.literal_block | nodes.doctest_block` in Phase 74 and that the surrounding prose was
   updated in several places, but the `Args:` sections of both docstrings still describe only the
   literal block node, so a reader consulting only the `Args:` line does not learn that
   `visit_literal_block` is also the doctest-block entry point. Quote the current `Args:` line
   verbatim for each of the two functions.

3. `## Solution`: name the one-line-per-docstring edit the review suggested — the `Args:` entry
   naming the literal block node or a doctest block delegated to it — and state explicitly that it
   is documentation text only, changes no behaviour, and needs no test.

4. `## Why it was deferred`: record D-14. Phase 75 is prep-only; touching `typsphinx/` here would
   force SC4's entire green proof — both full pytest runs, the clean `docs-html` and `docs-pdf`
   builds, the `linkcheck` run and the single CI dispatch — to be re-taken on a changed tip. Cite
   the precedent: v0.7.1's Phase 46 D-03 declined a `typst_authors` shim and D-27 declined a
   `builder.py` fix on the same reasoning. Note that MSG-06 stays pending for the same reason.

5. Append `## D-14: IN-01 filed, not fixed` to `75-LEFTOVERS-EVIDENCE.md`, recording
   `TODO_PATH = ` with the new file's repository-relative path, `TODO_PENDING_COUNT = ` from
   `ls .planning/todos/pending/ | wc -l` after the write, and the verbatim
   `git diff --name-only -- typsphinx`, which must be empty.

6. Commit both files.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-LEFTOVERS-EVIDENCE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; T="$(k TODO_PATH)" &amp;&amp; [ -n "$T" ] &amp;&amp; test -s "$T" &amp;&amp; case "$T" in .planning/todos/pending/*.md) true ;; *) false ;; esac &amp;&amp; git ls-files --error-unmatch "$T" &gt;/dev/null &amp;&amp; head -n 1 "$T" | grep -qx -- '---' &amp;&amp; grep -qE '^resolves_phase: null' "$T" &amp;&amp; grep -qE '^severity: ' "$T" &amp;&amp; grep -qE '^area: ' "$T" &amp;&amp; grep -qF 'visit_literal_block' "$T" &amp;&amp; grep -qF 'depart_literal_block' "$T" &amp;&amp; grep -qF 'nodes.doctest_block' "$T" &amp;&amp; grep -qF 'typsphinx/translator.py' "$T" &amp;&amp; grep -qF 'IN-01' "$T" &amp;&amp; grep -qF '74-REVIEW.md' "$T" &amp;&amp; grep -qxF '## Problem' "$T" &amp;&amp; grep -qxF '## Solution' "$T" &amp;&amp; grep -qxF '## Why it was deferred' "$T" &amp;&amp; grep -qF 'D-14' "$T" &amp;&amp; PC="$(ls .planning/todos/pending/ | wc -l)" &amp;&amp; [ "$PC" = "$(k TODO_PENDING_COUNT)" ] &amp;&amp; [ "$PC" -ge 4 ] &amp;&amp; grep -qxF '## D-14: IN-01 filed, not fixed' "$F" &amp;&amp; B="$(k BASE_75_02)" &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- typsphinx)" ] &amp;&amp; [ -z "$(git status --porcelain -- typsphinx)" ] &amp;&amp; grep -qF 'def visit_literal_block' typsphinx/translator.py &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail: `TODO_PATH` absent, empty, outside `.planning/todos/pending/`, or untracked by git; the file not opening with a `---` front-matter fence, or lacking `resolves_phase: null`, `area:` or `severity:`; the body not naming both `visit_literal_block` and `depart_literal_block`, `nodes.doctest_block`, `typsphinx/translator.py`, `IN-01`, `74-REVIEW.md` or `D-14`; any of the three required headings missing; the live pending-todo count differing from `TODO_PENDING_COUNT`, or fewer than four records present, which would mean the new todo replaced an existing one; the evidence file lacking the D-14 section; anything under `typsphinx/` changed or dirty; the control `def visit_literal_block` no longer found in `typsphinx/translator.py`, which would mean the file was rewritten; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/pending/` gains exactly one tracked record and still contains the three pre-existing ones, so the live count is at least 4 and matches `TODO_PENDING_COUNT`.
    - The new record's front matter carries `resolves_phase: null`, `area:`, `severity:` and a `files:` entry for `typsphinx/translator.py` with re-measured line references.
    - Its body has `## Problem`, `## Solution` and `## Why it was deferred`, names both functions and `nodes.doctest_block`, quotes the current `Args:` text, and cites `74-REVIEW.md` IN-01 and D-14.
    - `git diff --name-only -- typsphinx` is empty and `git status --porcelain -- typsphinx` is empty.
  </acceptance_criteria>
  <done>IN-01 is tracked as a pending todo with its deferral reason on the record, and no line of `typsphinx/` moved.</done>
</task>

<task type="auto">
  <name>Task 3: Write COVERAGE.md's one-line declaration and run the api-coverage gate</name>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/COVERAGE.md, .planning/phases/75-v0-9-6-release-prep-prep-only/75-LEFTOVERS-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/COVERAGE.md (the exact single-line shape the gate accepts)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-RESEARCH.md § "Security Domain" (why this phase's `gh` and PyPI calls are read-only probes rather than an integration)
  </read_first>
  <action>
1. Write `COVERAGE.md` in the phase directory containing exactly one line, with no heading and no
   trailing prose:
   `No external API integration: release-metadata literals, CHANGELOG prose and phase evidence files only; the gh, PyPI and Read the Docs calls are read-only probes and one workflow_dispatch, not an integration being built.`

2. Run
   `node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/75-v0-9-6-release-prep-prep-only`
   and record its JSON output verbatim in a new `## API coverage declaration` section of
   `75-LEFTOVERS-EVIDENCE.md`. Key `API_COVERAGE_PASSED = ` from the JSON's `passed` field.

3. In the same section, name each surface a detector could read as an external API and state why it
   is not one: `gh api` reads this repository's own branch protection; `gh workflow run` dispatches
   this repository's own CI; the PyPI JSON endpoint is an unauthenticated 404-or-200 probe of this
   project's own package page; the Read the Docs endpoints appear only inside `75-HANDOFF.md` as
   steps a different command executes later. No client, wrapper, credential path or retry policy is
   built anywhere in this phase.

4. Commit both files.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-LEFTOVERS-EVIDENCE.md" &amp;&amp; C="$D/COVERAGE.md" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; test -s "$C" &amp;&amp; [ "$(wc -l &lt; "$C")" -le 1 ] &amp;&amp; head -n 1 "$C" | grep -qF 'No external API integration: ' &amp;&amp; head -n 1 "$C" | grep -qF 'read-only probes' &amp;&amp; grep -qxF '## API coverage declaration' "$F" &amp;&amp; [ "$(k API_COVERAGE_PASSED)" = true ] &amp;&amp; J="$(node /home/yuta/Documents/typsphinx/.claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre "$D" 2&gt;&amp;1)" &amp;&amp; printf '%s\n' "$J" | grep -q '"passed": *true' &amp;&amp; git ls-files --error-unmatch "$C" &gt;/dev/null &amp;&amp; B="$(k BASE_75_02)" &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail: `COVERAGE.md` missing, empty, or carrying more than one line; its first line not opening with the declaration prefix or not naming the probes as read-only; the evidence file lacking the `## API coverage declaration` section; the recorded `API_COVERAGE_PASSED` not `true`; the live gate re-run not reporting `"passed": true`; `COVERAGE.md` untracked by git; any tracked product file changed by this plan; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `COVERAGE.md` is exactly one line beginning `No external API integration: ` and naming what this phase touches instead.
    - `node .claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre <phase dir>` reports `passed: true`, both as recorded and on a live re-run.
    - `75-LEFTOVERS-EVIDENCE.md` § "API coverage declaration" names `gh api`, `gh workflow run`, PyPI and Read the Docs and says why each is a probe rather than an integration.
  </acceptance_criteria>
  <done>The seal-time api-coverage gate has a reasoned declaration to read and passes on a live run.</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide spec-less probe accounting lives in `75-01-PLAN.md` § "Spec-less probe accounting". This
plan carries no edge row of its own.

**Assumption specific to this plan:** the five scratch files still exist only in the main checkout
and are still untracked when this plan runs. Both are re-measured in Task 1 rather than assumed —
an already-clean tree is recorded as `PROBE_FILES_BEFORE = 0` and short-circuits the deletion, and a
tracked match halts the task instead of deleting anything.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → main checkout working tree | this plan's first task is the only one in the phase that writes outside its own worktree |
| review finding → tracked backlog | an unfiled finding is a silently dropped defect |
| phase scope → seal-time api-coverage detector | a detector reading this phase's prose could classify read-only probes as an integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-06 | Tampering | a deletion in the main checkout reaching a tracked or unrelated file | high | mitigate | the toplevel is confirmed, every candidate path is proven untracked with `ls-files --error-unmatch` before removal, the glob is depth-1 and name-shaped, and one `rm` per path is recorded; a tracked match halts |
| T-75-07 | Repudiation | IN-01 dropped silently because the prep-only fence forbade fixing it | medium | mitigate | a tracked pending todo naming both functions, the stale text, the review ID and the deferral reason; the phase evidence records its path and the live pending count |
| T-75-08 | Spoofing | a fabricated coverage matrix row for an integration this phase does not build | low | accept | a reasoned one-line declaration is written instead of a matrix, which is the documented accepted form; the gate validates it |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list, identical in every plan of this phase. Every symbol below is created by Phase 75
and did not exist at the phase base.

**New files under `.planning/phases/75-v0-9-6-release-prep-prep-only/`:** `75-CLOSEOUT-GUARD.md`,
`75-BASE-EVIDENCE.md`, `75-SC5-INVARIANTS.md`, `75-LEFTOVERS-EVIDENCE.md`, `COVERAGE.md`,
`75-BUMP-EVIDENCE.md`, `75-CHANGELOG-EVIDENCE.md`, `75-GREEN-TREE-EVIDENCE.md`,
`75-PREFLIGHT-EVIDENCE.md`, `75-CI-EVIDENCE.md`, `75-HANDOFF.md`, plus one `75-0N-SUMMARY.md` per
plan.

**New file under `.planning/todos/pending/`:** a record for IN-01 (D-14), the
`visit_literal_block` / `depart_literal_block` `Args:` text that still names only the literal block
node.

**New headings and lines in `CHANGELOG.md`:** `## [0.9.6] - <prep date>` and, inside it,
`### Added`, `### Changed`, `### Fixed`, `### Known Limitations` and `### Verified`; a fresh
`## [Unreleased]` heading placed above it, retaining only `### Planned for Future Releases`; and a
new tail link line `[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6`.

**New value in `tests/test_changelog_page_gate.py`:** the `RELEASE_VERSIONS` tuple member `"0.9.6"`.

**Changed version literals (not new symbols):** `pyproject.toml`'s `[project].version`, the
`typsphinx` stanza's `version` in `uv.lock`, and `README.md`'s `**Status**: Stable (vX.Y.Z)` line,
all moving `0.9.2` to `0.9.6`.

**Deletions (no new symbol):** the five untracked repository-root files `probe_bogusxyz.typ`,
`probe_none.typ`, `probe_pycon.typ`, `probe_python.typ` and `probe_text.typ`.
</artifacts_this_phase_produces>

<verification>
- The main checkout root carries no file matching the probe scratch shape, and the deletion is
  auditable path by path.
- `.gitignore` is unchanged in both trees.
- A tracked pending todo for IN-01 exists with its three required sections, and `typsphinx/` is
  untouched and clean.
- `COVERAGE.md` is one declarative line and the api-coverage gate passes live.
</verification>

<success_criteria>
- Both Phase-74 leftovers the milestone audit handed to this phase are discharged: the scratch files
  deleted, IN-01 filed rather than fixed.
- The prep-only fence held: no file under `typsphinx/` and no `.gitignore` entry changed.
- The seal-time api-coverage gate has a reasoned declaration and passes.
</success_criteria>

<output>
Create `.planning/phases/75-v0-9-6-release-prep-prep-only/75-02-SUMMARY.md` when done, declaring
`requirements-completed: []`.
</output>
