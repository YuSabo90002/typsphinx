---
phase: 75-v0-9-6-release-prep-prep-only
plan: 06
type: execute
wave: 3
depends_on: ["75-02", "75-03", "75-04", "75-05"]
files_modified:
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CI-EVIDENCE.md
autonomous: true
requirements: [REL-15]
# REL-15 is a coverage ID only (ROADMAP constraint 1 and the Phase 75 cross-cutting
# constraints): no plan closes it, no plan touches its checkbox, and it is checked at
# /gsd-complete-milestone against observed publish evidence.
requirements-completed: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Evidence last: the push and the CI dispatch run in wave 3, after 75-04''s SC4_LOCAL_VERDICT and 75-05''s TRIAL_MERGE_VERDICT both read MET, and never alongside the work they audit.'
    - 'SC4 CI on the BUMPED tip (D-15): exactly one `gh workflow run CI --ref gsd/v0.9.6-doctest-block-rendering-and-release` is issued, after the bump and CHANGELOG work is pushed, so the run covers the whole tree — including `tests/test_docstring_rest_census_guard.py`, which Phase 74''s run on `e54d47d0` does not.'
    - 'The pushed SHA is the canonical milestone ref. Outside `.planning/` it equals this worktree''s HEAD; it contains the bump commit and the census guard test; its `pyproject.toml` reads 0.9.6; against the milestone base it changes only `typsphinx/pathfmt.py` and `typsphinx/translator.py` under `typsphinx/`, and nothing under `.github/` or `flake.nix`; and no tag points at it.'
    - 'Constraint 10 decoy handling: immediately before the push, the local and remote `gsd/v0.9.6*` censuses are recorded. A decoy is never pushed. A local decoy that is an ancestor of the canonical ref is deleted and recorded; a decoy carrying unique commits halts the plan before anything is deleted, because the fast-forward and HEAD re-point are main-checkout actions.'
    - 'The push is a fast-forward of an `ORIGIN_BEFORE` re-measured immediately before it, never a force. Afterwards the origin head equals the pushed SHA and the branch tracks origin.'
    - 'The run is waited on in the foreground to completion. Every job conclusion is transcribed, the two `windows-latest` and two `macos-latest` lanes are named individually, ruff''s verdict is read from the `Lint and Format Check` job''s own log, and every job is success.'
    - 'Edge (concurrency): if `gh workflow run` returns an HTTP 5xx, `gh run list` is read for a run at the pushed SHA created after the push timestamp **before** any retry, and a second dispatch is issued only when every such query shows none. A resumed executor reuses the recorded `RUN_ID` rather than dispatching again. At close, the CI list holds exactly one `workflow_dispatch` run at the pushed SHA and `release.yml` holds none.'
  artifacts:
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-CI-EVIDENCE.md
      provides: 'the wave-2 gate, tip identity and fence, decoy census, the push, one dispatch, the completed run with every job, the four named lanes, the tox lint step, the dispatch count, required checks at close, and SC4_CI_VERDICT'
      contains: 'PUSHED_SHA = '
  key_links:
    - from: 'local ref gsd/v0.9.6-doctest-block-rendering-and-release'
      to: 'origin/gsd/v0.9.6-doctest-block-rendering-and-release'
      via: 'git push --no-follow-tags; refs are shared between this worktree and the main checkout'
      pattern: 'ORIGIN_BEFORE = '
    - from: 'gh workflow run CI --ref gsd/v0.9.6-doctest-block-rendering-and-release'
      to: 'ci.yml jobs on ubuntu, windows and macos'
      via: 'workflow_dispatch, since ci.yml push and pull_request triggers are scoped to main and develop'
      pattern: 'RUN_ID = '
    - from: '75-CI-EVIDENCE.md SC4_CI_VERDICT'
      to: '75-07 closeout roll-up and 75-HANDOFF.md'
      via: 'read with sed in wave 4'
      pattern: 'SC4_CI_VERDICT = '
  prohibitions:
    - statement: 'MUST NOT create or push a tag, force-push, push any ref other than the canonical milestone branch, push or delete a decoy that carries unique commits, open or comment on a pull request, trigger `release.yml` or `update-pin.yml`, dispatch a second CI run, rerun jobs, or edit any file to turn a red job green.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT retry a dispatch that returned an error without first reading `gh run list` for a run at the pushed SHA created after the push timestamp; a silent second run breaks the exactly-one reading.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT touch the main checkout''s working tree or refs, and MUST NOT run `git branch -f`, `git update-ref` or `git symbolic-ref` on the shared canonical ref from this worktree.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT edit, stage or commit `.planning/REQUIREMENTS.md`, and MUST NOT move REL-15''s checkbox.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Put the bumped tip on `origin` and obtain CI's three-OS verdict on exactly that tip (SC4, D-15,
ROADMAP constraint 10):
- the branch and decoy census, taken immediately before the push
- one push, as a fast-forward, with tracking
- exactly one dispatched CI run, waited on in the foreground to completion, every job transcribed
- `main`'s required checks re-read at phase close

Purpose: `ci.yml` runs on this branch only through `workflow_dispatch`, and only such a run reaches
the Windows and macOS lanes. The milestone audit recorded that the last CI run, on `e54d47d0`,
predates `tests/test_docstring_rest_census_guard.py` — so this run must be on a tip that contains
it, which is why the dispatch happens after the bump is pushed.

Output: `75-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CI-EVIDENCE.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` and
run project tooling through `uv run`.

**Refs are shared.** This worktree's HEAD is on its own worktree branch, but
`refs/heads/gsd/v0.9.6-doctest-block-rendering-and-release` is the same ref the main checkout has
checked out. Push that ref by name. Never run `git branch -f`, `git update-ref` or
`git symbolic-ref` on it from here, and never touch the main checkout's working tree.

**Waiting on CI.** Wait in the foreground: set the Bash tool's `timeout` to 600000 and never use
`run_in_background`. Wrap each watch in `timeout 590` so it returns inside the tool limit, then read
`gh run view "$RUNID" --json status,conclusion`. Repeat these bounded foreground calls until
`status` is `completed`; do not end the turn or return control while the run is in progress. Write
`RUN_ID` into the evidence and commit it **before** the first wait, so a resumed executor reuses it
instead of dispatching again.

**Evidence key lines.** Bare values only, `KEY = value` on its own line, explanation on the next.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_75_06`; helper prefix `p7506_`.

**Commits.** Plain `git commit`. Never a GSD commit helper, which has created the
`gsd/vX.Y-milestone` decoy in every milestone since v0.9.1. Never write `75-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: gsd/v0\.9\.6-milestone -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.[3456] -->

## Planning-time census (2026-09-20; context only — re-measure, never trust)

| Fact | Value at planning |
|------|-------------------|
| Local `gsd/v0.9.6*` refs | only the canonical `gsd/v0.9.6-doctest-block-rendering-and-release`; no decoy |
| Origin | `refs/heads/gsd/v0.9.6-doctest-block-rendering-and-release` at `e54d47d0`, so this push is a fast-forward, not a branch creation |
| Last CI run | `35476044079` on `e54d47d0`, 12/12 green, predating `tests/test_docstring_rest_census_guard.py` |
| Reference job set | `REFERENCE_JOB_NAMES` in `74-BASE-EVIDENCE.md`, 12 jobs including two `windows-latest` and two `macos-latest` lanes |
| Required checks at Phase 74's close | `REQUIRED_STRICT_CLOSE` and `REQUIRED_CONTEXTS_CLOSE` in `74-CI-EVIDENCE.md`, six contexts, strict `true` |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: gate on wave 2, tip identity and fence, decoy census, the fast-forward push, and one CI dispatch</name>
  <precondition>`gh auth status` succeeds and `git ls-remote --heads origin` returns at least one line.</precondition>
  <reversibility rating="reversible">The push advances an existing remote branch that carries no protection and publishes nothing, and a CI run produces no artifact outside Actions. No tag, release or pull request is created, and ROADMAP constraint 10 and D-15 pre-authorise this push and this single dispatch.</reversibility>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CI-EVIDENCE.md (the census, push and dispatch section structure this mirrors, and `PUSHED_SHA` as the previous origin head)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-GREEN-TREE-EVIDENCE.md and 75-PREFLIGHT-EVIDENCE.md (the two verdict keys gated on)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md (`BUMP_COMMIT_SHA`)
    - .github/workflows/ci.yml (the trigger scope and the job names)
    - .planning/ROADMAP.md § "v0.9.6" constraints 7 and 10, and § "Phase 75" SC4
  </read_first>
  <action>
Create `75-CI-EVIDENCE.md`, headed `# Phase 75 — CI Evidence (bumped-tip push and dispatch, SC4 CI half)`.

1. **`## Head check and provisioning`.** `date -u +%FT%TZ`, `pwd -P`, `test -f .git; echo "exit:$?"`,
   the shim check, the provisioning line with its exit, then the same line with `--locked` appended
   and its exit, which must be 0 — every CI job starts with a locked sync, so a stale lock would
   fail every lane before any signal. On a non-zero exit write `## HALT: lock not in sync` and stop;
   never regenerate the lock. Record `BASE_75_06 = ` from `git rev-parse HEAD` before any commit and
   `SCRATCH_75_06 = `.

2. **`## Wave-2 gate`.** Quote `SC4_LOCAL_VERDICT` from `75-GREEN-TREE-EVIDENCE.md` and
   `TRIAL_MERGE_VERDICT` from `75-PREFLIGHT-EVIDENCE.md`. Both must read `MET`. Otherwise write
   `## HALT: wave 2 not green` and stop without pushing.

3. **`## Tip identity and fence`.** Record each of these; all must hold:
   - `PUSHED_SHA = ` from `git rev-parse gsd/v0.9.6-doctest-block-rendering-and-release`, with
     `git log --oneline -1` of it.
   - `git diff --name-only HEAD "$PUSHED_SHA" -- . ':(exclude).planning'`, empty: outside
     `.planning/` the ref equals this worktree's tree.
   - `git merge-base --is-ancestor <BUMP_COMMIT_SHA> "$PUSHED_SHA"; echo "exit:$?"`, printing
     `exit:0`: the ref being pushed carries the bump.
   - `git cat-file -e "$PUSHED_SHA:tests/test_docstring_rest_census_guard.py"`, exit 0: the run will
     cover the test file the milestone audit recorded as uncovered by the previous run.
   - `git cat-file -e` for `75-GREEN-TREE-EVIDENCE.md` and `75-PREFLIGHT-EVIDENCE.md` under the
     phase directory, both exit 0: wave 2 is merged into the ref being pushed.
   - `git show "$PUSHED_SHA:pyproject.toml" | grep -m1 '^version = '`, which must read
     `version = "0.9.6"`.
   - `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b "$PUSHED_SHA" -- typsphinx`,
     which must be exactly `typsphinx/pathfmt.py` and `typsphinx/translator.py` — Phase 74's own
     diff, unchanged by this phase.
   - `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b "$PUSHED_SHA" -- .github flake.nix`,
     empty.
   - `git tag --points-at "$PUSHED_SHA"`, empty.

4. **`## Decoy census (constraint 10)`.** Run this immediately before the push, with only step 5's
   measurements in between. Record `git branch --list 'gsd/v0.9.6*' -v` and
   `git ls-remote --heads origin 'refs/heads/gsd/v0.9.6*'`.
   - If `gsd/v0.9.6-milestone` exists on origin, write `## HALT: decoy on origin` and stop.
   - If it exists locally, record
     `git merge-base --is-ancestor gsd/v0.9.6-milestone gsd/v0.9.6-doctest-block-rendering-and-release; echo "exit:$?"`.
     - On `exit:0` it carries no unique commit: delete it with `git branch -d gsd/v0.9.6-milestone`,
       record that, and set `DECOY_ACTION = deleted-ancestor`.
     - Otherwise record
       `git log --oneline gsd/v0.9.6-doctest-block-rendering-and-release..gsd/v0.9.6-milestone`,
       write `## HALT: decoy carries unique commits`, and stop without deleting anything.
       Constraint 10's order is: fast-forward the canonical ref to the decoy, re-point the main
       checkout's HEAD with `git symbolic-ref`, and only then delete the decoy. That order acts on
       the main checkout's HEAD, so the orchestrator performs it there and re-dispatches this plan.
   - If it exists nowhere, set `DECOY_ACTION = none-present`.

5. **`## Push`.**
   - `ORIGIN_BEFORE = ` the first field of
     `git ls-remote --heads origin refs/heads/gsd/v0.9.6-doctest-block-rendering-and-release`, or
     `none` when that prints nothing.
   - If it is a SHA, record
     `git merge-base --is-ancestor "$ORIGIN_BEFORE" "$PUSHED_SHA"; echo "exit:$?"`, which must print
     `exit:0`. Otherwise write `## HALT: origin moved`; never force.
   - Record `git config --get push.followTags`, then `PUSH_AT = ` from `date -u +%FT%TZ`.
   - Run `git push --no-follow-tags origin gsd/v0.9.6-doctest-block-rendering-and-release` once.
     Record the output verbatim, including GitHub's pull-request hint, and do not act on the hint.
   - Record the origin head now printing `PUSHED_SHA`, and
     `git ls-remote --tags origin` showing `refs/tags/v0.9.2` present and no `v0.9.3`, `v0.9.4`,
     `v0.9.5` or `v0.9.6` tag.

6. **`## Dispatch`.**
   - Run `gh workflow run CI --ref gsd/v0.9.6-doctest-block-rendering-and-release` exactly once.
   - Locate the run with
     `gh run list --workflow=ci.yml --branch gsd/v0.9.6-doctest-block-rendering-and-release --event workflow_dispatch --limit 5 --json databaseId,headSha,status,createdAt,url`,
     choosing the row whose `headSha` is `PUSHED_SHA` and whose `createdAt` is after `PUSH_AT`.
   - If no row is listed yet, that is registration lag: repeat the list query, and never dispatch
     again.
   - If the dispatch returned an HTTP 5xx, GitHub may still have created the run. Repeat the list
     query several times, recording each. Dispatch a second time only if every query shows no run at
     `PUSHED_SHA` created after `PUSH_AT`, and record why.
   - Record `RUN_ID = ` and `RUN_URL = `, and commit the evidence file before the first wait.

7. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.6-doctest-block-rendering-and-release &amp;&amp; MB=6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; [ "$(k SC4_LOCAL_VERDICT "$D/75-GREEN-TREE-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k TRIAL_MERGE_VERDICT "$D/75-PREFLIGHT-EVIDENCE.md")" = MET ] &amp;&amp; B="$(k BASE_75_06)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; C="$(k BUMP_COMMIT_SHA "$D/75-BUMP-EVIDENCE.md")" &amp;&amp; [ "${#C}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$C" "$P" &amp;&amp; git cat-file -e "$P:tests/test_docstring_rest_census_guard.py" &amp;&amp; git cat-file -e "$P:$D/75-GREEN-TREE-EVIDENCE.md" &amp;&amp; git cat-file -e "$P:$D/75-PREFLIGHT-EVIDENCE.md" &amp;&amp; HR="$(git ls-remote --heads origin "refs/heads/$BR")" &amp;&amp; [ -n "$HR" ] &amp;&amp; [ "$(printf '%s\n' "$HR" | cut -f1)" = "$P" ] &amp;&amp; OB="$(k ORIGIN_BEFORE)" &amp;&amp; { [ "$OB" = none ] || git merge-base --is-ancestor "$OB" "$P"; } &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; [ "$(printf '%s\n' "$HL" | grep -c 'gsd/v0\.9\.6-milestone')" = 0 ] &amp;&amp; case "$(k DECOY_ACTION)" in none-present|deleted-ancestor) true ;; *) false ;; esac &amp;&amp; [ -z "$(git tag --points-at "$P")" ] &amp;&amp; TL="$(git ls-remote --tags origin)" &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.[3456]')" = 0 ] &amp;&amp; [ "$(git diff --name-only "$MB" "$P" -- typsphinx | LC_ALL=C sort | paste -sd'|')" = 'typsphinx/pathfmt.py|typsphinx/translator.py' ] &amp;&amp; [ -z "$(git diff --name-only "$MB" "$P" -- .github flake.nix)" ] &amp;&amp; [ -z "$(git diff --name-only HEAD "$P" -- . ':(exclude).planning')" ] &amp;&amp; PP="$(git show "$P:pyproject.toml")" &amp;&amp; [ "$(printf '%s\n' "$PP" | grep -m1 '^version = ')" = 'version = "0.9.6"' ] &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json workflowName -q .workflowName)" = CI ] &amp;&amp; [ "$(gh run view "$RUNID" --json event -q .event)" = workflow_dispatch ] &amp;&amp; PA="$(k PUSH_AT)" &amp;&amp; [ -n "$PA" ] &amp;&amp; [[ "$(gh run view "$RUNID" --json createdAt -q .createdAt)" &gt; "$PA" ]] &amp;&amp; [ -n "$(k RUN_URL)" ] &amp;&amp; for s in '## Head check and provisioning' '## Wave-2 gate' '## Tip identity and fence' '## Decoy census (constraint 10)' '## Push' '## Dispatch'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail: not in a worktree, or the shim absent; either wave-2 verdict not `MET`; `BASE_75_06`, `PUSHED_SHA` or `BUMP_COMMIT_SHA` malformed, or the bump not an ancestor of the pushed SHA; the pushed ref lacking `tests/test_docstring_rest_census_guard.py` or either wave-2 evidence file; `git ls-remote` returning nothing for the branch, or the origin head differing from `PUSHED_SHA`; `ORIGIN_BEFORE` neither `none` nor an ancestor of the pushed SHA, which would mean a force-push; a `gsd/v0.9.6-milestone` decoy on origin, or `DECOY_ACTION` outside its two allowed values; a tag pointing at the pushed tip; the `v0.9.2` tag missing on origin, or any `v0.9.3` to `v0.9.6` tag present; the `typsphinx/` diff against the milestone base not being exactly the two Phase 74 files; `.github/` or `flake.nix` changed since the milestone base; the pushed product tree differing from this worktree's HEAD; the pushed `pyproject.toml` not reading `version = "0.9.6"`; `RUN_ID` absent, or the run's headSha, workflow name, event or creation time not matching; `RUN_URL` empty; a named section missing; any tracked product file changed by this plan; `.planning/REQUIREMENTS.md` changed; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Decoy census (constraint 10)` records both listings immediately before the push, with any ancestor-decoy deletion and its ancestry result.
    - `## Push` records `ORIGIN_BEFORE`, `PUSH_AT` and the verbatim push output; the origin head equals `PUSHED_SHA`; the push was a fast-forward, never a force.
    - The pushed ref carries the bump commit, `tests/test_docstring_rest_census_guard.py` and both wave-2 evidence files, reads `version = "0.9.6"`, and carries no tag.
    - `## Dispatch` records exactly one `gh workflow run CI --ref gsd/v0.9.6-doctest-block-rendering-and-release`, with `RUN_ID` and `RUN_URL`, whose `headSha` is `PUSHED_SHA` and whose `createdAt` is after `PUSH_AT`, and the evidence file is committed before the first wait.
  </acceptance_criteria>
  <done>The bumped milestone tip is on origin, with no decoy and no tag, and exactly one CI run is dispatched against exactly that tip.</done>
</task>

<task type="auto">
  <name>Task 2: Wait for the run in the foreground, transcribe every job, name the four Windows and macOS lanes, quote the tox lint step, and re-read the required checks at phase close</name>
  <precondition>The `RUN_ID` recorded by Task 1 resolves with `gh run view <id> --json databaseId`. A resumed executor reuses it and never dispatches again.</precondition>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CI-EVIDENCE.md (Task 1: `RUN_ID`, `PUSHED_SHA`, `PUSH_AT`)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CI-EVIDENCE.md § "Job census", § "windows-latest lanes", § "macos-latest lanes", § "Lint through tox", § "Required checks at phase close" (the section shapes to mirror, and `REQUIRED_STRICT_CLOSE` / `REQUIRED_CONTEXTS_CLOSE`)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md (`REFERENCE_JOB_NAMES`, the sorted job-name set constraint 7 says must not have changed)
    - .github/workflows/ci.yml (job names and the lint step's own command lines)
  </read_first>
  <action>
1. **Wait in the foreground.**
   - Run `timeout 590 gh run watch "$RUNID" --interval 30` in a Bash call with `timeout` 600000 and
     no `run_in_background`.
   - After each return, read `gh run view "$RUNID" --json status,conclusion`, and repeat until
     `status` is `completed`.
   - Overall cap: 90 minutes. If it is reached, record the status and return a blocking checkpoint.
     Never dispatch again.

2. **`## Run`.** Record
   `gh run view "$RUNID" --json status,conclusion,workflowName,headSha,url,createdAt,updatedAt`,
   with keys `RUN_HEAD_SHA` and `RUN_CONCLUSION`.

3. **`## Job census`.**
   - Record `gh run view "$RUNID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'` as a
     numbered table covering every row. Keys `JOB_COUNT`, measured, and `NON_SUCCESS_JOBS`, which
     must be 0.
   - Record the sorted job-name set,
     `gh run view "$RUNID" --json jobs --jq '[.jobs[].name] | sort | join("|")'`. Key
     `JOB_NAMES_MATCH_REFERENCE = yes` when it equals `REFERENCE_JOB_NAMES` in
     `74-BASE-EVIDENCE.md`. Constraint 7 forbids any workflow edit, so a difference means `ci.yml`
     changed: write `## HALT: ci.yml job set changed`.

4. **`## windows-latest lanes`** and **`## macos-latest lanes`.** Two sub-tables naming
   `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`,
   `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each with its
   conclusion.

5. **`## Lint through tox`.** Take the `Lint and Format Check` job id from the run's jobs. From
   `gh run view --job <id> --log`, quote the lint step's `black --check .` and `ruff check .`
   command lines and its `lint: OK` line, and record `CI_RUFF_VERSION` if the log prints one. Record
   `LOCK_RUFF_VERSION` from the pushed tree's own lock,
   `git show "$PUSHED_SHA:uv.lock" | grep -A1 -xF 'name = "ruff"'`. This is the gate this project
   has seen fail in CI while every test lane passed.

6. **`## Dispatch count and no release run`.** Record:
   - `gh run list --workflow=ci.yml --branch gsd/v0.9.6-doctest-block-rendering-and-release --event workflow_dispatch --limit 50 --json headSha`,
     key `DISPATCH_COUNT`, the number of runs at `PUSHED_SHA`, which must be 1
   - `gh run list --workflow=release.yml --limit 20 --json headSha`, key
     `RELEASE_RUNS_AT_PUSHED`, which must be 0 — no release workflow has run on this tip

7. **`## Required checks at phase close`.** Re-read
   `gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks` with
   `--jq .strict` and with `--jq '[.contexts[]] | sort | join("|")'`, keys
   `REQUIRED_STRICT_CLOSE_75` and `REQUIRED_CONTEXTS_CLOSE_75`. Key
   `REQUIRED_CHECKS_UNCHANGED = yes` when both equal Phase 74's close values. Also record which of
   those required contexts appear in this run's job list, so the handoff can say the required set is
   covered by a run that has already passed on this exact tree.

8. **`## SC4 CI verdict`.**
   - `SC4_CI_VERDICT = MET` only when the run is completed and success, `NON_SUCCESS_JOBS = 0`,
     `JOB_NAMES_MATCH_REFERENCE = yes`, all four named lanes are success, `DISPATCH_COUNT = 1`,
     `RELEASE_RUNS_AT_PUSHED = 0` and `REQUIRED_CHECKS_UNCHANGED = yes`.
   - If any job is not success, transcribe every job literally and keep the run. Do not rerun,
     dispatch again or edit anything. Write `SC4_CI_VERDICT = NOT-MET` and return a blocking
     checkpoint naming each failing job and step.

9. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-CI-EVIDENCE.md" &amp;&amp; P74=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; BR=gsd/v0.9.6-doctest-block-rendering-and-release &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; [ "$(gh run view "$RUNID" --json status -q .status)" = completed ] &amp;&amp; [ "$(gh run view "$RUNID" --json conclusion -q .conclusion)" = success ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(k RUN_HEAD_SHA)" = "$P" ] &amp;&amp; [ "$(k RUN_CONCLUSION)" = success ] &amp;&amp; JC="$(gh run view "$RUNID" --json jobs -q '.jobs | length')" &amp;&amp; [ "$JC" -ge 1 ] &amp;&amp; [ "$JC" = "$(k JOB_COUNT)" ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.conclusion != "success")] | length')" = 0 ] &amp;&amp; JN="$(gh run view "$RUNID" --json jobs -q '[.jobs[].name] | sort | join("|")')" &amp;&amp; [ -n "$JN" ] &amp;&amp; RJ="$(k REFERENCE_JOB_NAMES "$P74/74-BASE-EVIDENCE.md")" &amp;&amp; [ -n "$RJ" ] &amp;&amp; [ "$JN" = "$RJ" ] &amp;&amp; [ "$(k JOB_NAMES_MATCH_REFERENCE)" = yes ] &amp;&amp; for n in 'Test Python 3.12 on windows-latest' 'Test Python 3.13 on windows-latest' 'Test Python 3.12 on macos-latest' 'Test Python 3.13 on macos-latest'; do [ "$(gh run view "$RUNID" --json jobs -q "[.jobs[] | select(.name == \"$n\" and .conclusion == \"success\")] | length")" = 1 ] || { echo "lane: $n"; exit 1; }; grep -qF "$n" "$F" || { echo "missing: $n"; exit 1; }; done &amp;&amp; grep -qF 'Lint and Format Check' "$F" &amp;&amp; grep -qF 'ruff check .' "$F" &amp;&amp; grep -qF 'black --check .' "$F" &amp;&amp; LR="$(k LOCK_RUFF_VERSION)" &amp;&amp; [ -n "$LR" ] &amp;&amp; printf '%s\n' "$(git show "$P:uv.lock" | grep -A1 -xF 'name = "ruff"')" | grep -qF "$LR" &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch "$BR" --event workflow_dispatch --limit 50 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; [ "$(k DISPATCH_COUNT)" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; [ "$(k RELEASE_RUNS_AT_PUSHED)" = 0 ] &amp;&amp; RC="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '[.contexts[]] | sort | join("|")')" &amp;&amp; [ -n "$RC" ] &amp;&amp; [ "$(k REQUIRED_CONTEXTS_CLOSE_75)" = "$RC" ] &amp;&amp; ST="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict)" &amp;&amp; [ "$(k REQUIRED_STRICT_CLOSE_75)" = "$ST" ] &amp;&amp; [ "$RC" = "$(k REQUIRED_CONTEXTS_CLOSE "$P74/74-CI-EVIDENCE.md")" ] &amp;&amp; [ "$ST" = "$(k REQUIRED_STRICT_CLOSE "$P74/74-CI-EVIDENCE.md")" ] &amp;&amp; [ "$(k REQUIRED_CHECKS_UNCHANGED)" = yes ] &amp;&amp; [ "$(k NON_SUCCESS_JOBS)" = 0 ] &amp;&amp; [ "$(k SC4_CI_VERDICT)" = MET ] &amp;&amp; for s in '## Run' '## Job census' '## windows-latest lanes' '## macos-latest lanes' '## Lint through tox' '## Dispatch count and no release run' '## Required checks at phase close' '## SC4 CI verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `lane: NAME`, `missing: NAME` or `missing section: NAME` printed. Each of these makes it fail: `RUN_ID` or `PUSHED_SHA` absent or malformed; the run not completed, not success, or on a different head SHA than the recorded push; the recorded run head or conclusion disagreeing with the live read; the live job count zero or differing from `JOB_COUNT`; any job with a conclusion other than success; the sorted job-name set empty, differing from Phase 74's reference set, or `JOB_NAMES_MATCH_REFERENCE` not `yes`; any of the four named lanes not a single successful job, or absent from the evidence text; the evidence lacking the lint job name or either lint command line; `LOCK_RUFF_VERSION` empty or not found in the pushed tree's own lock stanza; the dispatch count at the pushed SHA not exactly 1, live or recorded; any `release.yml` run at the pushed SHA, live or recorded; the live required contexts or strict flag differing from the recorded keys or from Phase 74's close values, or `REQUIRED_CHECKS_UNCHANGED` not `yes`; `NON_SUCCESS_JOBS` not 0; `SC4_CI_VERDICT` not `MET`; any named section missing; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - The run is `completed` / `success` on `PUSHED_SHA`, every job is transcribed by name and conclusion, and `NON_SUCCESS_JOBS = 0`.
    - The sorted job-name set equals Phase 74's `REFERENCE_JOB_NAMES`, and all four `windows-latest` / `macos-latest` lanes are named individually with a success conclusion.
    - The `Lint and Format Check` job's own `ruff check .` and `black --check .` lines are quoted, and the lock's ruff version is recorded from the pushed tree.
    - `DISPATCH_COUNT = 1` at the pushed SHA and `RELEASE_RUNS_AT_PUSHED = 0`.
    - `main`'s required contexts and strict flag are re-read live and equal Phase 74's close values, with `REQUIRED_CHECKS_UNCHANGED = yes`.
    - `SC4_CI_VERDICT = MET`.
  </acceptance_criteria>
  <done>The bumped tip has a completed, fully green three-OS CI run of its own, dispatched exactly once, with every job and both lint commands on the record.</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide spec-less probe accounting lives in `75-01-PLAN.md` § "Spec-less probe accounting". This
plan authors one of the six rows: `Edge (concurrency)`, above, covering the HTTP-5xx dispatch case
where GitHub creates the run anyway.

**Assumption specific to this plan:** that `ci.yml`'s job set is unchanged since Phase 74 recorded
`REFERENCE_JOB_NAMES`. Constraint 7 forbids any workflow edit this milestone, so a difference means
something changed outside this project's plans — the task halts and reports rather than adapting the
reference, because silently widening it would hide exactly the change worth knowing about.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch → `origin` | the first write this phase makes to a remote, and the only one besides the single dispatch |
| dispatch call → GitHub Actions | a transport error can leave a run created, making a blind retry produce two |
| CI verdict → merge readiness | the run's conclusion is the evidence `/gsd-complete-milestone` relies on before opening the PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-25 | Tampering | a force-push overwriting remote history, or a decoy branch pushed or deleted out of order | high | mitigate | `ORIGIN_BEFORE` is re-measured immediately before the push and must be an ancestor; force flags are prohibited; a decoy carrying unique commits halts the plan before any deletion |
| T-75-26 | Repudiation | two CI runs making the exactly-one-dispatch reading unverifiable | high | mitigate | `gh run list` is read before any retry, `RUN_ID` is committed before the first wait so a resumed executor reuses it, and the dispatch count at the pushed SHA is asserted to be 1 at close |
| T-75-27 | Tampering | an irreversible action slipping in beside the push — a tag, a release run, a pull request | critical | mitigate | `--no-follow-tags` on the push, `git tag --points-at` empty, the remote tag probe showing only `v0.9.2`, and `release.yml` runs at the pushed SHA asserted to be 0 |
| T-75-28 | Repudiation | a green verdict read from the run status while an individual job failed | high | mitigate | every job's conclusion is transcribed individually, the four cross-platform lanes are asserted one by one, and the job-name set is compared against Phase 74's reference |
| T-75-29 | Information Disclosure | a job log transcribed wholesale carrying a token or environment dump | medium | mitigate | only the named lint step's command lines and its result line are quoted, never a whole log |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list, identical in every plan of this phase. Every symbol below is created by Phase 75
and did not exist at the phase base.

**New files under `.planning/phases/75-v0-9-6-release-prep-prep-only/`:** `75-CLOSEOUT-GUARD.md`,
`75-BASE-EVIDENCE.md`, `75-SC5-INVARIANTS.md`, `75-LEFTOVERS-EVIDENCE.md`, `COVERAGE.md`,
`75-BUMP-EVIDENCE.md`, `75-CHANGELOG-EVIDENCE.md`, `75-GREEN-TREE-EVIDENCE.md`,
`75-PREFLIGHT-EVIDENCE.md`, `75-CI-EVIDENCE.md`, `75-HANDOFF.md`, plus one `75-0N-SUMMARY.md` per
plan.

**New file under `.planning/todos/pending/`:** a record for IN-01 (D-14), the
`visit_literal_block` / `depart_literal_block` `Args:` text that still names only the literal block
node.

**New headings and lines in `CHANGELOG.md`:** `## [0.9.6] - <prep date>` and, inside it,
`### Added`, `### Changed`, `### Fixed`, `### Known Limitations` and `### Verified`; a fresh
`## [Unreleased]` heading placed above it, retaining only `### Planned for Future Releases`; and a
new tail link line `[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6`.

**New value in `tests/test_changelog_page_gate.py`:** the `RELEASE_VERSIONS` tuple member `"0.9.6"`.

**Changed version literals (not new symbols):** `pyproject.toml`'s `[project].version`, the
`typsphinx` stanza's `version` in `uv.lock`, and `README.md`'s `**Status**: Stable (vX.Y.Z)` line,
all moving `0.9.2` to `0.9.6`.

**Deletions (no new symbol):** the five untracked repository-root files `probe_bogusxyz.typ`,
`probe_none.typ`, `probe_pycon.typ`, `probe_python.typ` and `probe_text.typ`.
</artifacts_this_phase_produces>

<verification>
- `origin`'s milestone branch head equals `PUSHED_SHA`, reached by fast-forward, carrying the bump
  commit and the census guard test, with no tag at that tip.
- Exactly one `workflow_dispatch` CI run exists at that SHA, completed and success, every job
  transcribed, all four cross-platform lanes named.
- No `release.yml` run exists at that SHA and no pull request was opened.
- `SC4_CI_VERDICT = MET`, and `main`'s required checks are unchanged since Phase 74's close.
</verification>

<success_criteria>
- SC4's CI clause is discharged on the bumped tip, by one dispatch, with every job's conclusion on
  the record.
- The push took no irreversible action alongside it.
</success_criteria>

<output>
Create `.planning/phases/75-v0-9-6-release-prep-prep-only/75-06-SUMMARY.md` when done, declaring
`requirements-completed: []`.
</output>
