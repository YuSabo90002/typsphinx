---
phase: 75-v0-9-6-release-prep-prep-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CLOSEOUT-GUARD.md
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-BASE-EVIDENCE.md
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-SC5-INVARIANTS.md
autonomous: true
requirements: [REL-15, REL-16]
# REL-15 is a coverage ID only (ROADMAP constraint 1 and the Phase 75 cross-cutting
# constraints): no plan closes it, no plan touches its checkbox, and it is checked at
# /gsd-complete-milestone against observed publish evidence. REL-16 closes inside this
# phase, but its checkbox is moved by phase-completion tooling at close, never by a plan.
requirements-completed: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Baseline first: this plan runs at the phase base, before any product edit of this phase exists, and records the fence and the clean docs ledger that 75-04 and 75-07 compare against.'
    - 'The REQUIREMENTS fence is line-scoped to REL-15 (ROADMAP constraint 9): `sha256sum`, `wc -l`, `PHASE_BASE_SHA` and the verbatim `grep -n ''REL-15''` transcript are recorded at phase head, each hit classified state-bearing or informational. REL-16''s `grep -n` transcript is recorded in a separate, explicitly expected-to-move callout.'
    - 'The guard carries its own re-verification protocol and an operator section naming the third observation, the one taken after `phase.complete`-family tooling (including `/gsd-verify-work`''s inline transition) has run.'
    - 'REQUIREMENTS.md, ROADMAP.md and STATE.md are copied to scratch outside the repository before any later tooling can rewrite them, and the backup paths are recorded as key lines.'
    - 'SC4 base ledger: a clean `tox -e docs-html` and a clean `tox -e docs-pdf` run at the phase base under `LANG=C LANGUAGE=C LC_ALL=C`, each from a removed output directory, and their `build succeeded` lines, warning integers, `doctest_block` unknown-node counts and attributed docutils indentation/block-quote counts are recorded as the numbers 75-04 re-measures on the bumped tip.'
    - 'Every zero this plan records is falsifiable: a synthetic scratch fixture carrying one literal of each message class is grepped with the same pattern and returns a non-zero count, and Phase 74''s own pre-fix measurement at the milestone base is quoted from `74-BASE-EVIDENCE.md` as the real-build control.'
    - 'SC5 observation 1 of 2: `git tag -l`, a remote tag probe, PyPI and `gh release list` all report no v0.9.6, each with a `v0.9.2` positive control that is present, and the version literal still reads 0.9.2 at this observation.'
  artifacts:
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-CLOSEOUT-GUARD.md
      provides: 'the phase-head fence baseline, the guarded REL-15 lines, REL-16''s expected-to-move lines, the scratch backup paths, the re-verification protocol and the operator section'
      contains: 'REQ_SHA256_BASE = '
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-BASE-EVIDENCE.md
      provides: 'the clean base docs-html and docs-pdf warning ledger with both message-class counts, the synthetic grep control and the quoted Phase 74 pre-fix control'
      contains: 'BASE_HTML_WARNINGS = '
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-SC5-INVARIANTS.md
      provides: 'SC5 probe observation 1 of 2 with its positive controls'
      contains: 'OBS1_AT = '
  key_links:
    - from: '75-BASE-EVIDENCE.md BASE_HTML_WARNINGS / BASE_PDF_WARNINGS'
      to: '75-04 tip docs comparison'
      via: 'read with sed from the committed evidence file in wave 2'
      pattern: 'BASE_HTML_WARNINGS = '
    - from: '75-CLOSEOUT-GUARD.md REQ_SHA256_BASE'
      to: '75-07 fence re-verification at phase close'
      via: 'recomputed sha256sum compared against the recorded key'
      pattern: 'REQ_SHA256_BASE = '
    - from: '75-SC5-INVARIANTS.md Observation 1 of 2'
      to: '75-SC5-INVARIANTS.md Observation 2 of 2 (75-07)'
      via: 'two observations separated by waves 2 and 3, not by wall-clock luck'
      pattern: 'OBS1_AT = '
  prohibitions:
    - statement: 'MUST NOT edit, stage or commit `.planning/REQUIREMENTS.md`, and MUST NOT move REL-15''s checkbox. On an unbidden change, run `git checkout -- .planning/REQUIREMENTS.md`, report it, and never commit it.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT create or push a tag, upload to PyPI, create a GitHub Release, open or comment on a pull request, dispatch `update-pin.yml`, or take any other irreversible action; every probe in this plan is read-only.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT edit any file under `typsphinx/`: the prep-only fence holds for the whole phase, IN-01 and MSG-06 included.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT believe a zero count that has no positive control, and MUST NOT take an incremental docs build or a build outside the C locale as a baseline.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Lay the phase's two baselines before any product edit of this phase exists:
- the line-scoped REQUIREMENTS fence on REL-15, with REL-16 recorded separately as expected-to-move
  (ROADMAP SC5, constraint 9)
- the clean `docs-html` / `docs-pdf` warning ledger at this phase's own base, which SC4 requires the
  bumped tip to be compared against (constraint 5)
- SC5 probe observation 1 of 2, with positive controls

Purpose: the auto-flip this fence guards has landed at 9 of the 10 prior release-prep closes, and a
baseline taken after the bump would make SC4's "not risen" comparison vacuous. Both must be measured
here, at the base, in wave 1.

Output: `75-CLOSEOUT-GUARD.md`, `75-BASE-EVIDENCE.md`, `75-SC5-INVARIANTS.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` and
run every tool through `uv run`. The `docs` extra is required for the documentation builds and keeps
`tests/test_changelog_page_gate.py` out of the skip path (ROADMAP constraint 11, 75-RESEARCH.md
Pitfall 1).

**Tooling.** Invoke bare `uv`, `tox`, `pytest`, `ruff`, `black`, `mypy`, `sphinx-build` through the
PATH shims; never `.venv/bin/<tool>` directly (75-RESEARCH.md Pitfall 2).

**Timeouts.** Run every documentation build, and every verify block containing one, with the Bash
tool's `timeout` set to 600000, in the foreground.

**Locale.** tox 4 forwards `LANG` and `LANGUAGE` but not `LC_ALL`, so every counted documentation
build is invoked as `LANG=C LANGUAGE=C LC_ALL=C uv run tox -e <env>`.

**Evidence key lines.** Bare values only, `KEY = value` on its own line, with the explanation on the
next line. A key line containing an opening parenthesis fails the verify.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_75_01`; helper prefix `p7501_`.

**Commits.** Plain `git commit`. Never a GSD commit helper, which has created the
`gsd/vX.Y-milestone` decoy in every milestone since v0.9.1. Never write `75-VERIFICATION.md`, which
is a reserved name the verifier overwrites.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: Unexpected indentation -->
<!-- planner-discipline-allow: Block quote ends without a blank line -->
<!-- planner-discipline-allow: unknown node type: .doctest_block -->
<!-- plan-criteria-allow: R1 - the only literals at or above 2 in these chains are HTTP status codes, 200 for the PyPI positive control and 404 for the absent version; every count-grep comparison in this plan is against 0, against 1, or against a key recorded from the same command -->

## Planning-time census (2026-09-20, HEAD `1048b73e`; context only — re-measure, never trust)

| Fact | Value at planning |
|------|-------------------|
| `.planning/REQUIREMENTS.md` | `sha256sum` `481e2091ced842747944e80c6e0bdaafc7b2a2385f5e179e77e6b1e62dca603f`, 81 lines |
| `grep -n 'REL-15'` | 4 hits: `22` checkbox, `59` traceability row, `73` and `76` prose |
| `grep -n 'REL-16'` | 5 hits: `23` checkbox, `60` traceability row, `30`, `47`, `75` prose |
| Phase 74 pre-fix control | `74-BASE-EVIDENCE.md` keys `BASE_WARNING_COUNT = 5` and `BASE_DOCTEST_UNKNOWN_COUNT = 2` at milestone base `6cc44f22`, with 3 attributed docutils messages |
| Milestone audit tip re-measurement | clean `-b typst` at `a3fe2102`: `build succeeded.`, 0 warnings, 0 of each message class |
| Version | `pyproject.toml` line 7 `version = "0.9.2"`; `v0.9.2` tagged locally, `v0.9.6` absent everywhere |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: head check, PHASE_BASE_SHA, the line-scoped REL-15 fence with REL-16 recorded separately, and the scratch backups</name>
  <precondition>`.planning/REQUIREMENTS.md` exists at the phase base and `git log -1 --format=%H -- .planning/REQUIREMENTS.md` resolves.</precondition>
  <reversibility rating="reversible">Writes only new phase-evidence files; reads `.planning/REQUIREMENTS.md` without modifying it.</reversibility>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-CLOSEOUT-GUARD.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file under guard — read it before recording anything about it)
    - .planning/milestones/v0.9.5-phases/73-v0-9-5-close-prep-prep-only-unpublished/73-CLOSEOUT-GUARD.md (the exact section structure and key-line shape this file mirrors, whole-file-scoped there and line-scoped here)
    - .planning/ROADMAP.md § "Phase 75" SC5 and § "v0.9.6" constraint 9
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md § "Claude's Discretion" → "The fence"
  </read_first>
  <action>
Create `75-CLOSEOUT-GUARD.md`, headed `# Phase 75 — Closeout Guard (REL-15 fence)`.

1. **`## Head check and provisioning`.** Record `date -u +%FT%TZ`, `pwd -P`,
   `test -f .git; echo "exit:$?"`, `command -v uv`, and `grep -c typsphinx-fhs-run "$(command -v uv)"`.
   Record the provisioning line `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs`
   with its exit, then `.venv/pyvenv.cfg`'s `home` and `version_info` as `PYVENV_HOME` and
   `PYVENV_VERSION_INFO`. Record `SCRATCH_75_01 = ` followed by the `mktemp -d` path.

2. **`## Baseline`.** Record, each as a bare key line:
   - `PHASE_BASE_SHA = ` from `git rev-parse HEAD`, taken before this plan's first commit, plus
     `git log -1 --format='%H %s'` of it. Its subject must not already name a `75-0N` plan.
   - `MILESTONE_BASE = 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b`, confirmed by
     `git merge-base HEAD origin/main` after `git fetch -q origin main`.
   - `REQ_SHA256_BASE = ` from `sha256sum .planning/REQUIREMENTS.md`, first field only.
   - `REQ_LINES_BASE = ` from `wc -l < .planning/REQUIREMENTS.md`.
   - `REL15_HITS_BASE = ` from `grep -c 'REL-15' .planning/REQUIREMENTS.md`.
   - `REL16_HITS_BASE = ` from `grep -c 'REL-16' .planning/REQUIREMENTS.md`.
   - `REQ_LAST_COMMIT = ` from `git log -1 --format=%H -- .planning/REQUIREMENTS.md`, which must be
     an ancestor of `PHASE_BASE_SHA`.
   - `GUARD_AT = ` from `date -u +%FT%TZ`.

3. **`## The lines under guard`.** Paste the verbatim `grep -n 'REL-15' .planning/REQUIREMENTS.md`
   transcript in a fenced block, then a table classifying every hit as **state-bearing** (the
   `- [ ] **REL-15**:` checkbox line and the `| REL-15 | Phase 75 | Pending |` traceability row) or
   **informational** (prose mentions). State that both state-bearing lines must be byte-identical at
   every later observation, and that the checkbox must still read `- [ ] **REL-15**` and the row
   `| REL-15 | Phase 75 | Pending |`.

4. **`## Expected to move: REL-16`.** A clearly separated callout. Paste the verbatim
   `grep -n 'REL-16' .planning/REQUIREMENTS.md` transcript and classify its hits the same way.
   State that REL-16 closes inside this phase, so its checkbox and traceability row **may**
   legitimately change to `[x]` / `Complete` when phase-completion tooling runs — that this is the
   reason constraint 9's fence is line-scoped to REL-15 this milestone rather than whole-file — and
   that no plan in this phase edits `.planning/REQUIREMENTS.md` itself, so `REQ_SHA256_BASE` is
   expected to MATCH at phase close and to change only after that tooling has run.

5. **`## Why this file exists`.** Record that `phase.complete`-family tooling, including
   `/gsd-verify-work`'s inline transition and `/gsd-execute-phase`'s tail, has auto-flipped the
   release requirement against an explicit CONTEXT decision at 9 of the 10 prior release-prep
   closes; that SHA-256 is the primary probe because a checkbox flip leaves `wc -l` unchanged; and
   that on any divergence the fix is `git checkout -- .planning/REQUIREMENTS.md`, reported and never
   committed. Mention `current_phase_name` as one of the STATE.md fields the same tooling rewrites.

6. **`## Scratch backups`.** Copy `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and
   `.planning/STATE.md` into `$SCRATCH_75_01` and record `BACKUP_REQUIREMENTS = `,
   `BACKUP_ROADMAP = ` and `BACKUP_STATE = ` with their absolute paths, plus each copy's
   `sha256sum`. These exist because the same tooling has orphaned wrapped lines in ROADMAP.md and
   flipped STATE.md status fields in prior phases, not only flipped a checkbox.

7. **`## Re-verification protocol (phase close)`** and **`## For the operator running phase.complete`.**
   Reproduce, inline, the four commands a later reader runs without opening a second file:
   `sha256sum .planning/REQUIREMENTS.md` compared against the recorded digest;
   `wc -l .planning/REQUIREMENTS.md`; `git diff --name-only -- .planning/REQUIREMENTS.md`, expected
   empty; and `grep -n 'REL-15' .planning/REQUIREMENTS.md`, expected byte-identical to § "The lines
   under guard". Name the **three** observations explicitly: phase head (this task), phase close
   (75-07), and once more after `phase.complete`-family tooling has run — stating that the third is
   the one that actually catches the flip, that it happens outside any plan, and that
   `/gsd-verify-work`'s inline transition counts as that tooling. Include the reversion recipe and
   the instruction to diff ROADMAP.md and STATE.md against the scratch backups too.

8. Commit the file with a plain `git commit`.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; G="$D/75-CLOSEOUT-GUARD.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$G}" | head -n 1; } &amp;&amp; PB="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#PB}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$PB" HEAD &amp;&amp; SJ="$(git log -1 --format=%s "$PB")" &amp;&amp; [ -n "$SJ" ] &amp;&amp; ! printf '%s\n' "$SJ" | grep -qE '75-0[0-9]' &amp;&amp; [ "$(k MILESTONE_BASE)" = 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b ] &amp;&amp; [ "$(sha256sum .planning/REQUIREMENTS.md | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; [ "$(wc -l &lt; .planning/REQUIREMENTS.md)" = "$(k REQ_LINES_BASE)" ] &amp;&amp; [ "$(grep -c 'REL-15' .planning/REQUIREMENTS.md)" = "$(k REL15_HITS_BASE)" ] &amp;&amp; [ "$(grep -c 'REL-16' .planning/REQUIREMENTS.md)" = "$(k REL16_HITS_BASE)" ] &amp;&amp; RL="$(git log -1 --format=%H -- .planning/REQUIREMENTS.md)" &amp;&amp; [ "${#RL}" = 40 ] &amp;&amp; [ "$(k REQ_LAST_COMMIT)" = "$RL" ] &amp;&amp; git merge-base --is-ancestor "$RL" "$PB" &amp;&amp; grep -qE '^- \[ \] \*\*REL-15\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| REL-15 \| Phase 75 \| Pending' .planning/REQUIREMENTS.md &amp;&amp; [ -n "$(k GUARD_AT)" ] &amp;&amp; [ "$(grep -cF "$(k REQ_SHA256_BASE)" "$G")" -ge 2 ] &amp;&amp; grep -qF 'git checkout -- .planning/REQUIREMENTS.md' "$G" &amp;&amp; grep -qF '/gsd-verify-work' "$G" &amp;&amp; grep -qF 'current_phase_name' "$G" &amp;&amp; BR1="$(k BACKUP_REQUIREMENTS)" &amp;&amp; BR2="$(k BACKUP_ROADMAP)" &amp;&amp; BR3="$(k BACKUP_STATE)" &amp;&amp; test -s "$BR1" &amp;&amp; test -s "$BR2" &amp;&amp; test -s "$BR3" &amp;&amp; [ "$(sha256sum "$BR1" | cut -d' ' -f1)" = "$(k REQ_SHA256_BASE)" ] &amp;&amp; for s in '## Head check and provisioning' '## Baseline' '## The lines under guard' '## Expected to move: REL-16' '## Why this file exists' '## Scratch backups' '## Re-verification protocol (phase close)' '## For the operator running phase.complete'; do grep -qxF "$s" "$G" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$PB" HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; [ -z "$(git diff --name-only "$PB" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$G" &amp;&amp; ! grep -q '^## HALT' "$G"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail: not in a worktree or the `uv` shim absent; `PHASE_BASE_SHA` not a 40-character ancestor of HEAD, or its commit subject already naming a `75-0N` plan; the recorded milestone base not `6cc44f22…`; the live `sha256sum`, `wc -l` or either `grep -c` disagreeing with its recorded key; `REQ_LAST_COMMIT` not matching or not an ancestor of the phase base; REL-15's checkbox not reading `- [ ] **REL-15**` or its traceability row not reading `Pending`; the digest appearing fewer than twice in the guard; the reversion recipe, the `/gsd-verify-work` mention or the `current_phase_name` mention absent; a backup path missing, empty, or the REQUIREMENTS backup's digest differing from the recorded base digest; any named section missing; `.planning/REQUIREMENTS.md` or any tracked product file changed since the phase base; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `75-CLOSEOUT-GUARD.md` contains all eight named sections and the key lines `PHASE_BASE_SHA`, `MILESTONE_BASE`, `REQ_SHA256_BASE`, `REQ_LINES_BASE`, `REL15_HITS_BASE`, `REL16_HITS_BASE`, `REQ_LAST_COMMIT`, `GUARD_AT`, `BACKUP_REQUIREMENTS`, `BACKUP_ROADMAP`, `BACKUP_STATE`.
    - The recorded `REQ_SHA256_BASE`, `REQ_LINES_BASE`, `REL15_HITS_BASE` and `REL16_HITS_BASE` equal the values recomputed live from `.planning/REQUIREMENTS.md`.
    - § "The lines under guard" carries the verbatim `grep -n 'REL-15'` transcript with every hit classified state-bearing or informational; § "Expected to move: REL-16" carries REL-16's transcript in a separate callout.
    - § "For the operator running phase.complete" names three observations, the third taken after `phase.complete`-family tooling including `/gsd-verify-work`, and reproduces the four re-verification commands plus `git checkout -- .planning/REQUIREMENTS.md`.
    - `git diff --name-only <PHASE_BASE_SHA> HEAD -- .planning/REQUIREMENTS.md` is empty.
  </acceptance_criteria>
  <done>The REL-15 fence exists with a recorded digest, its guarded lines transcribed verbatim, REL-16's lines separated as expected-to-move, and REQUIREMENTS/ROADMAP/STATE backed up outside the repository.</done>
</task>

<task type="auto">
  <name>Task 2: The clean base documentation ledger, in the C locale, with a synthetic grep control and Phase 74's pre-fix real-build control</name>
  <precondition>`tox.ini` carries `[testenv:docs-html]` and `[testenv:docs-pdf]`, and the provisioned venv carries the `docs` extra (`uv run python -c "import myst_parser"` exits 0).</precondition>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-BASE-EVIDENCE.md</files>
  <read_first>
    - tox.ini (the `docs-html`, `docs-pdf` and `linkcheck` environment definitions and their output directories)
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md (`BASE_WARNING_COUNT`, `BASE_DOCTEST_UNKNOWN_COUNT`, the attributed-docstring filter expression and its verbatim pre-fix hits at the milestone base)
    - .planning/milestones/v0.9.5-phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-05-PLAN.md (the clean-rebuild, `LANG=C LANGUAGE=C LC_ALL=C` tox invocation and the `build succeeded` parsing idiom this task reuses)
    - .planning/ROADMAP.md § "v0.9.6" constraints 4 and 5
  </read_first>
  <action>
Create `75-BASE-EVIDENCE.md`, headed `# Phase 75 — Base Evidence (clean documentation ledger at the phase base)`.

1. **`## Head check and provisioning`.** The same header shape as Task 1, plus
   `BASE_75_02 = ` from `git rev-parse HEAD` before this task's commit and
   `SCRATCH_75_01` reused or re-recorded.

2. **`## Base identity`.** Quote `PHASE_BASE_SHA` and `MILESTONE_BASE` from
   `75-CLOSEOUT-GUARD.md`, and record `git show HEAD:pyproject.toml | grep -m1 '^version = '`,
   which must read `version = "0.9.2"` — this ledger is taken on the unbumped tree.

3. **`## Base docs-html`.** Run `rm -rf docs/_build` — the whole output tree, because an incremental
   rebuild under-reports warnings and is how this project once manufactured a false baseline match —
   then
   `LANG=C LANGUAGE=C LC_ALL=C uv run tox -e docs-html > "$S/p7501_html.log" 2>&1; echo "exit:$?"`.
   Record the exit as `BASE_HTML_EXIT` and the verbatim `build succeeded` line. Derive
   `BASE_HTML_WARNINGS` by `sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p'` on that
   line, taking 0 when the line is exactly `build succeeded.`. Also record
   `BASE_HTML_DOCSTRING_REST`, the attributed-message count in this log, using the same filter as
   step 5.

4. **`## Base docs-pdf`.** Run `rm -rf docs/_build` again, then
   `LANG=C LANGUAGE=C LC_ALL=C uv run tox -e docs-pdf > "$S/p7501_pdf.log" 2>&1; echo "exit:$?"`.
   Record `BASE_PDF_EXIT` and, the same way, `BASE_PDF_WARNINGS`. `tox.ini`'s `docs-pdf` environment
   runs `sphinx-build -b typstpdf source _build/pdf` from `docs/`, so this is the build carrying the
   translator's own node-handler messages.

5. **`## Base message-class census`.** Against `$S/p7501_pdf.log` — the `-b typstpdf` run, the build
   the `doctest_block` message originates in — record, each with the verbatim command:
   - `BASE_DOCTEST_UNKNOWN = ` from `LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$L" | wc -l`
   - `BASE_DOCSTRING_REST = ` from the attributed filter
     `LANG=C LC_ALL=C grep -cE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' "$L"`
   - `BASE_RAW_REST = ` from the unattributed
     `LANG=C LC_ALL=C grep -cE 'Unexpected indentation|Block quote ends without a blank line' "$L"`
   Both are expected to be 0 on this tree, since Phase 74 closed them; record whatever they are.

6. **`## Positive controls`.** Two, both required because a zero is otherwise unfalsifiable:
   - **Synthetic:** write `$S/p7501_control.txt` containing one line carrying the literal
     `unknown node type: <doctest_block ...>`, one carrying a `.py:docstring of x.y:5: ERROR: Unexpected indentation.`
     shape and one carrying a `.py:docstring of x.y:6: WARNING: Block quote ends without a blank line`
     shape. Run each of the three census commands from step 5 against that file and record the
     counts as `CONTROL_DOCTEST_HITS`, `CONTROL_DOCSTRING_HITS` and `CONTROL_RAW_HITS`, each of
     which must be at least 1. This proves the patterns and the C locale are not the reason for the
     zeros.
   - **Real build, cited:** quote `BASE_WARNING_COUNT` and `BASE_DOCTEST_UNKNOWN_COUNT` from
     `74-BASE-EVIDENCE.md` as `P74_BASE_WARNINGS` and `P74_BASE_DOCTEST_UNKNOWN`, together with one
     verbatim attributed hit line from that file, and name `6cc44f22` as the pre-fix tree they were
     measured on. Record `P74_CONTROL_NONZERO = yes` only when `P74_BASE_DOCTEST_UNKNOWN` is at
     least 1.

7. **`## Base ledger`.** A table naming each key and its value, with a sentence stating that 75-04
   re-measures all of these on the bumped tip and that SC4 requires the tip's warning integers not
   to have risen above these and both message-class counts to be 0.

8. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-BASE-EVIDENCE.md" &amp;&amp; G="$D/75-CLOSEOUT-GUARD.md" &amp;&amp; P74=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_75_02)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; [ "$(k PHASE_BASE_SHA "$G")" != "" ] &amp;&amp; [ "$(git show HEAD:pyproject.toml | grep -m1 '^version = ')" = 'version = "0.9.2"' ] &amp;&amp; S="$(k SCRATCH_75_01 "$G")" &amp;&amp; [ -n "$S" ] &amp;&amp; HL="$S/p7501_html.log" &amp;&amp; PL="$S/p7501_pdf.log" &amp;&amp; CT="$S/p7501_control.txt" &amp;&amp; test -s "$HL" &amp;&amp; test -s "$PL" &amp;&amp; test -s "$CT" &amp;&amp; [ "$(k BASE_HTML_EXIT)" = 0 ] &amp;&amp; [ "$(k BASE_PDF_EXIT)" = 0 ] &amp;&amp; for L in "$HL" "$PL"; do BLN="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$L" || true)"; [ -n "$BLN" ] || { echo "no build succeeded line: $L"; exit 1; }; done &amp;&amp; HB="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$HL" | head -n1)" &amp;&amp; HN="$(printf '%s\n' "$HB" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')" &amp;&amp; { [ -n "$HN" ] || { [ "$HB" = 'build succeeded.' ] &amp;&amp; HN=0; }; } &amp;&amp; [ "$HN" = "$(k BASE_HTML_WARNINGS)" ] &amp;&amp; PB2="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$PL" | head -n1)" &amp;&amp; PN="$(printf '%s\n' "$PB2" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')" &amp;&amp; { [ -n "$PN" ] || { [ "$PB2" = 'build succeeded.' ] &amp;&amp; PN=0; }; } &amp;&amp; [ "$PN" = "$(k BASE_PDF_WARNINGS)" ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$PL" | wc -l)" = "$(k BASE_DOCTEST_UNKNOWN)" ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -cE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' "$PL" || true)" = "$(k BASE_DOCSTRING_REST)" ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -cE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' "$HL" || true)" = "$(k BASE_HTML_DOCSTRING_REST)" ] &amp;&amp; [ "$(k BASE_DOCTEST_UNKNOWN)" = 0 ] &amp;&amp; [ "$(k BASE_DOCSTRING_REST)" = 0 ] &amp;&amp; [ "$(k BASE_HTML_DOCSTRING_REST)" = 0 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$CT" | wc -l)" -ge 1 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -cE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' "$CT" || true)" -ge 1 ] &amp;&amp; [ "$(k CONTROL_DOCTEST_HITS)" -ge 1 ] &amp;&amp; [ "$(k CONTROL_DOCSTRING_HITS)" -ge 1 ] &amp;&amp; [ "$(k CONTROL_RAW_HITS)" -ge 1 ] &amp;&amp; [ "$(k P74_BASE_DOCTEST_UNKNOWN)" = "$(k BASE_DOCTEST_UNKNOWN_COUNT "$P74/74-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(k P74_BASE_WARNINGS)" = "$(k BASE_WARNING_COUNT "$P74/74-BASE-EVIDENCE.md")" ] &amp;&amp; [ "$(k P74_BASE_DOCTEST_UNKNOWN)" -ge 1 ] &amp;&amp; [ "$(k P74_CONTROL_NONZERO)" = yes ] &amp;&amp; for s in '## Head check and provisioning' '## Base identity' '## Base docs-html' '## Base docs-pdf' '## Base message-class census' '## Positive controls' '## Base ledger'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `no build succeeded line: PATH` or `missing section: NAME` printed. Each of these makes it fail: not in a worktree or the shim absent; the tree already bumped away from `version = "0.9.2"`; either saved build log or the control fixture missing or empty; either recorded tox exit non-zero; either log lacking an English `build succeeded` line; either recorded warning integer differing from the one parsed live out of its log; either recorded message-class count differing from the live recount, or either not 0; any of the three synthetic control counts zero, live or recorded; the quoted Phase 74 keys differing from `74-BASE-EVIDENCE.md`'s own values, or the pre-fix doctest count not at least 1; `P74_CONTROL_NONZERO` not `yes`; any named section missing; a tracked product file changed; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/_build`'s html and pdf output directories were removed before each build, and both tox invocations carry `LANG=C LANGUAGE=C LC_ALL=C`.
    - `BASE_HTML_WARNINGS` and `BASE_PDF_WARNINGS` equal the integers parsed out of the two saved logs' own `build succeeded` lines.
    - `BASE_DOCTEST_UNKNOWN = 0` and `BASE_DOCSTRING_REST = 0`, each recomputable from the saved `-b typst` log.
    - The synthetic control fixture yields at least one hit for each of the three census patterns, and `74-BASE-EVIDENCE.md`'s pre-fix counts are quoted with `P74_BASE_DOCTEST_UNKNOWN` at least 1.
  </acceptance_criteria>
  <done>The phase base has a clean, C-locale documentation warning ledger with both message classes at zero, and both zeros are falsifiable against a synthetic control and Phase 74's real pre-fix measurement.</done>
</task>

<task type="auto">
  <name>Task 3: SC5 probe observation 1 of 2 — no tag, no PyPI release, no GitHub Release, each with a v0.9.2 positive control</name>
  <precondition>`gh auth status` succeeds and the network reaches `pypi.org` (a `curl` to the `0.9.2` JSON endpoint returns an HTTP status code).</precondition>
  <reversibility rating="reversible">Every command is a read: `git tag -l`, `git ls-remote`, an unauthenticated `curl`, and `gh release list` / `gh run list`.</reversibility>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-SC5-INVARIANTS.md</files>
  <read_first>
    - .planning/milestones/v0.9.5-phases/73-v0-9-5-close-prep-prep-only-unpublished/73-SC1-INVARIANTS.md (the two-observation probe structure, the positive-control discipline and the key-line names this file mirrors)
    - .planning/ROADMAP.md § "Phase 75" SC5 first bullet
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md § "Claude's Discretion" → "The probes"
  </read_first>
  <action>
Create `75-SC5-INVARIANTS.md`, headed `# Phase 75 — SC5 Invariants (zero irreversible action)`.

1. **`## Observation 1 of 2`.** Record `OBS1_AT = ` from `date -u +%FT%TZ`, then every probe below
   with its verbatim command and output. Each expected-empty probe is paired with a control that
   must be non-empty, so an empty result cannot come from a broken command:
   - `git tag -l 'v0.9.6'`, expected empty; `git tag -l 'v0.9.2'`, expected non-empty. Keys
     `LOCAL_V096_TAGS = 0` and `LOCAL_V092_TAGS = 1`.
   - `git ls-remote --tags origin`, from which record `REMOTE_V092_TAG` — the count of lines ending
     `refs/tags/v0.9.2` — which must be 1, and `REMOTE_V096_TAG`, the count matching
     `refs/tags/v0.9.6`, which must be 0.
   - `curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.6/json`, key
     `PYPI_096_HTTP`, expected `404`; the same for `0.9.2`, key `PYPI_092_HTTP`, expected `200`.
   - `gh release list --limit 50 --json tagName,isLatest`. Key `GH_LATEST`, the `tagName` of the
     entry whose `isLatest` is true, expected `v0.9.2`; key `GH_V096_RELEASES`, the number of
     entries whose `tagName` is `v0.9.6`, expected 0.
   - `gh run list --workflow=release.yml --limit 20 --json headBranch,databaseId,createdAt`. Key
     `RELEASE_RUNS_SINCE_V092`, the number of runs created after `2026-08-31`, expected 0, and key
     `RELEASE_POSITIVE_CONTROL`, the `databaseId` of the run whose `headBranch` is `v0.9.2` — the
     v0.9.2 publish — recorded so a later reader can see the query is not returning an empty list
     because the query itself is wrong.
   - `git ls-remote --heads origin`, transcribed whole. Key `DECOY_ON_ORIGIN_OBS1`, the number of
     lines naming `gsd/v0.9.6-milestone`, expected 0.

2. **`## Version and branch state at observation 1`.** Record `sed -n 7p pyproject.toml`, which must
   read `version = "0.9.2"` at this observation, and
   `git log --format=%h -G '^version = ' 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b..HEAD -- pyproject.toml`,
   which must be empty — no version-bump commit exists yet at this point in the phase. Key
   `VERSION_BUMP_COMMITS_OBS1 = 0`.

3. **`## Handoff to observation 2`.** State that 75-07 repeats every probe above as observation 2 of
   2, after waves 2 and 3 — so the two observations are separated by the bump commit, the local
   green-tree runs, the trial merge, the push and the dispatched CI run, not by wall-clock luck —
   and that at observation 2 the version literal is expected to read `0.9.6` while every probe above
   is still expected to be empty, with the same controls still present.

4. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; S="$D/75-SC5-INVARIANTS.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; ks() { sed -n "s/^$1 = //p" "${2:-$S}" | head -n 1; } &amp;&amp; [ -n "$(ks OBS1_AT)" ] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.2"' ] &amp;&amp; [ -z "$(git tag -l 'v0.9.6')" ] &amp;&amp; [ -n "$(git tag -l 'v0.9.2')" ] &amp;&amp; TL="$(git ls-remote --tags origin)" &amp;&amp; [ -n "$TL" ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.2$')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$TL" | grep -c 'refs/tags/v0\.9\.6')" = 0 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.6/json)" = 404 ] &amp;&amp; [ "$(curl -sS -o /dev/null -w '%{http_code}' https://pypi.org/pypi/typsphinx/0.9.2/json)" = 200 ] &amp;&amp; [ "$(gh release list --limit 50 --json tagName,isLatest --jq '.[] | select(.isLatest) | .tagName')" = v0.9.2 ] &amp;&amp; [ "$(gh release list --limit 50 --json tagName --jq '[.[] | select(.tagName == "v0.9.6")] | length')" = 0 ] &amp;&amp; RPC="$(ks RELEASE_POSITIVE_CONTROL)" &amp;&amp; [ -n "$RPC" ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json databaseId,headBranch --jq "[.[] | select(.databaseId == $RPC and .headBranch == \"v0.9.2\")] | length")" = 1 ] &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; [ "$(printf '%s\n' "$HL" | grep -c 'gsd/v0\.9\.6-milestone')" = 0 ] &amp;&amp; [ -z "$(git log --format=%h -G '^version = ' 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b..HEAD -- pyproject.toml)" ] &amp;&amp; for key in LOCAL_V096_TAGS REMOTE_V096_TAG GH_V096_RELEASES RELEASE_RUNS_SINCE_V092 DECOY_ON_ORIGIN_OBS1 VERSION_BUMP_COMMITS_OBS1; do [ "$(ks "$key")" = 0 ] || { echo "key not 0: $key"; exit 1; }; done &amp;&amp; [ "$(ks LOCAL_V092_TAGS)" = 1 ] &amp;&amp; [ "$(ks REMOTE_V092_TAG)" = 1 ] &amp;&amp; [ "$(ks PYPI_096_HTTP)" = 404 ] &amp;&amp; [ "$(ks PYPI_092_HTTP)" = 200 ] &amp;&amp; [ "$(ks GH_LATEST)" = v0.9.2 ] &amp;&amp; for s in '## Observation 1 of 2' '## Version and branch state at observation 1' '## Handoff to observation 2'; do grep -qxF "$s" "$S" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$S" &amp;&amp; ! grep -q '^## HALT' "$S"</automated>
    <fails_when>Non-zero exit, or `key not 0: KEY` or `missing section: NAME` printed. Each of these makes it fail: not in a worktree; the working tree already bumped away from `version = "0.9.2"`; a local or remote `v0.9.6` tag existing; the `v0.9.2` control tag missing locally or on origin; `git ls-remote --tags origin` returning nothing at all, which would make every remote zero vacuous; PyPI answering anything but 404 for `0.9.6` or anything but 200 for the `0.9.2` control; the latest GitHub Release not being `v0.9.2`, or a `v0.9.6` Release existing; `RELEASE_POSITIVE_CONTROL` absent or not resolving to a real `release.yml` run on the `v0.9.2` ref; a `gsd/v0.9.6-milestone` decoy on origin; a version-bump commit already present in the milestone range; any recorded key not matching its required value; any named section missing; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `75-SC5-INVARIANTS.md` § "Observation 1 of 2" records every probe with its verbatim command and output, and each expected-empty probe sits beside a non-empty `v0.9.2` control.
    - `LOCAL_V096_TAGS`, `REMOTE_V096_TAG`, `GH_V096_RELEASES`, `RELEASE_RUNS_SINCE_V092`, `DECOY_ON_ORIGIN_OBS1` and `VERSION_BUMP_COMMITS_OBS1` all read 0; `LOCAL_V092_TAGS`, `REMOTE_V092_TAG` read 1; `PYPI_092_HTTP` reads 200 and `PYPI_096_HTTP` reads 404; `GH_LATEST` reads `v0.9.2`.
    - § "Handoff to observation 2" names the intervening waves that separate the two observations.
  </acceptance_criteria>
  <done>Observation 1 of SC5's two probe observations is on the record, every emptiness claim paired with a control that is not empty.</done>
</task>

</tasks>

<flagged_assumptions>
## Spec-less probe accounting (phase-wide; this section is the single accounting point)

This phase has no `*-SPEC.md`, so the deterministic edge probe ran over REL-15's and REL-16's
requirement text and returned six rows. All six are accounted for here; none is silently dropped and
none is auto-dismissed.

| # | Requirement | Category | Disposition | Where |
|---|-------------|----------|-------------|-------|
| 1 | REL-15 | adjacency | resolved, explicit | `75-03-PLAN.md` `must_haves.truths`, prefixed `Edge (adjacency):` |
| 2 | REL-15 | empty | resolved, explicit | `75-03-PLAN.md` `must_haves.truths`, prefixed `Edge (empty):` |
| 3 | REL-15 | ordering | resolved, explicit | `75-03-PLAN.md` `must_haves.truths`, prefixed `Edge (ordering):` |
| 4 | REL-15 | idempotency | resolved, explicit | `75-03-PLAN.md` `must_haves.truths`, prefixed `Edge (idempotency):` |
| 5 | REL-15 | concurrency | resolved, explicit | `75-06-PLAN.md` `must_haves.truths`, prefixed `Edge (concurrency):` |
| 6 | REL-16 | unclassified | **unresolved — flagged assumption** | below |

**Row 6, unresolved and surfaced rather than auto-resolved.** The probe classified REL-16's text as
`unclassified` and supplied no predicate. The assumption this phase runs on is that "settled on the
record" is discharged by a phase artifact pair — the `### Known Limitations` section in
`## [0.9.6]` and this phase's own decision record — rather than by any runtime behaviour, and that
the two agreeing with each other (ROADMAP SC3's own wording) is the whole of the requirement. Two
things follow that nothing in this phase can falsify by measurement: whether the owner would accept
the NUM-01-only candidate set the AMENDED block establishes as "the carried major defects" REL-16
names, and whether a disclosure-only entry that promises no fix satisfies "settled". Both were
decided in discussion (D-10, D-11) and are carried here as recorded decisions, not as measured
facts. If either is wrong, the corrective is an edit to `## [0.9.6]`'s `### Known Limitations`
before the tag is pushed, which `75-HANDOFF.md` notes is still possible at `/gsd-complete-milestone`.

## Other flagged assumptions

- **The Dependabot and open-PR census is not inherited.** `75-CONTEXT.md` recorded nine stale
  `dependabot/uv/*` remote branches; 75-RESEARCH.md measured zero the same calendar day; this plan's
  own `git ls-remote --heads origin` at observation 1 is a third reading. No plan asserts any of the
  three numbers — 75-05 measures and writes whatever it finds (ROADMAP constraint 4).
- **`<assumption_delta_decision>`** is carried by this plan; see the block below.
</flagged_assumptions>

<assumption_delta_decision>
**Noun now primary:** none — no identity model changes in this phase.
**Decision:** `no-change`.
**Rationale:** the detector's single `pluralization` signal fired on the word "second" in
"a silent second run breaks the exactly-one reading", where it modifies a duplicate CI workflow run,
not a second platform, tenant, auth method or source of truth. This phase introduces no second case
of anything and no new identity model, so the promote-versus-add-alongside question does not arise
and no invariant test is owed.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/REQUIREMENTS.md` → phase-completion tooling | automation writes to a file this phase must hold unchanged, with no human in the loop at write time |
| local measurement → published claim | a zero count measured here becomes the basis for a claim in the release notes and the handoff |
| repository state → remote publish surfaces | tag, PyPI and GitHub Release probes are the only evidence that nothing irreversible has happened |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-01 | Tampering | `.planning/REQUIREMENTS.md` REL-15 checkbox flipped by `phase.complete`-family tooling | high | mitigate | SHA-256 + `wc -l` + verbatim `grep -n 'REL-15'` fence recorded at phase head, re-verified at close and once more after the tooling runs; revert-and-report, never commit |
| T-75-02 | Repudiation | a zero warning or message-class count that cannot be falsified, manufacturing a false baseline | high | mitigate | every zero paired with a synthetic control fixture that yields a non-zero count under the same pattern, plus Phase 74's quoted pre-fix real-build measurement at `6cc44f22` |
| T-75-03 | Repudiation | an "empty" tag/PyPI/Release probe that is empty because the command is wrong | medium | mitigate | every emptiness probe carries a `v0.9.2` positive control that must be non-empty in the same command family |
| T-75-04 | Information Disclosure | command output transcribed into evidence carrying a token or credential | medium | mitigate | only `gh` JSON field selections and `git`/`curl` status codes are transcribed; no `gh auth token`, no environment dump, no `--body` built by command substitution |
| T-75-05 | Denial of Service | an incremental documentation build under-reporting warnings and passing a false baseline forward | medium | mitigate | every counted build removes its output directory first and runs under `LANG=C LANGUAGE=C LC_ALL=C` |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list, identical in every plan of this phase. Every symbol below is created by Phase 75
and did not exist at the phase base.

**New files under `.planning/phases/75-v0-9-6-release-prep-prep-only/`:** `75-CLOSEOUT-GUARD.md`,
`75-BASE-EVIDENCE.md`, `75-SC5-INVARIANTS.md`, `75-LEFTOVERS-EVIDENCE.md`, `COVERAGE.md`,
`75-BUMP-EVIDENCE.md`, `75-CHANGELOG-EVIDENCE.md`, `75-GREEN-TREE-EVIDENCE.md`,
`75-PREFLIGHT-EVIDENCE.md`, `75-CI-EVIDENCE.md`, `75-HANDOFF.md`, plus one `75-0N-SUMMARY.md` per
plan.

**New file under `.planning/todos/pending/`:** a record for IN-01 (D-14), the
`visit_literal_block` / `depart_literal_block` `Args:` text that still names only the literal block
node.

**New headings and lines in `CHANGELOG.md`:** `## [0.9.6] - <prep date>` and, inside it,
`### Added`, `### Changed`, `### Fixed`, `### Known Limitations` and `### Verified`; a fresh
`## [Unreleased]` heading placed above it, retaining only `### Planned for Future Releases`; and a
new tail link line `[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6`.

**New value in `tests/test_changelog_page_gate.py`:** the `RELEASE_VERSIONS` tuple member `"0.9.6"`.

**Changed version literals (not new symbols):** `pyproject.toml`'s `[project].version`, the
`typsphinx` stanza's `version` in `uv.lock`, and `README.md`'s `**Status**: Stable (vX.Y.Z)` line,
all moving `0.9.2` to `0.9.6`.

**Deletions (no new symbol):** the five untracked repository-root files `probe_bogusxyz.typ`,
`probe_none.typ`, `probe_pycon.typ`, `probe_python.typ` and `probe_text.typ`.
</artifacts_this_phase_produces>

<verification>
- `75-CLOSEOUT-GUARD.md` exists with all eight sections, and its recorded `REQ_SHA256_BASE`,
  `REQ_LINES_BASE`, `REL15_HITS_BASE` and `REL16_HITS_BASE` match the live values.
- `.planning/REQUIREMENTS.md` is byte-identical to its state at `PHASE_BASE_SHA`.
- `75-BASE-EVIDENCE.md` carries both clean C-locale builds with their warning integers and both
  message-class counts at 0, each falsifiable against the synthetic control and Phase 74's quoted
  pre-fix measurement.
- `75-SC5-INVARIANTS.md` § "Observation 1 of 2" is complete, every emptiness paired with a control.
</verification>

<success_criteria>
- The REL-15 fence is recorded, line-scoped, with REL-16 separated as expected-to-move, and the
  three-observation protocol is written down for the operator.
- The phase-base documentation ledger exists and is the number 75-04 compares the bumped tip to.
- SC5's first probe observation is on the record.
- Nothing under `typsphinx/` changed, no tag or release exists, and `.planning/REQUIREMENTS.md` is
  untouched.
</success_criteria>

<output>
Create `.planning/phases/75-v0-9-6-release-prep-prep-only/75-01-SUMMARY.md` when done, declaring
`requirements-completed: []`.
</output>
