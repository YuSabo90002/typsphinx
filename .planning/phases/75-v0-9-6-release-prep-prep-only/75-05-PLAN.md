---
phase: 75-v0-9-6-release-prep-prep-only
plan: 05
type: execute
wave: 2
depends_on: ["75-03"]
files_modified:
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-PREFLIGHT-EVIDENCE.md
autonomous: true
requirements: [REL-15]
# REL-15 is a coverage ID only (ROADMAP constraint 1 and the Phase 75 cross-cutting
# constraints): no plan closes it, no plan touches its checkbox, and it is checked at
# /gsd-complete-milestone against observed publish evidence.
requirements-completed: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'SC4 trial merge is non-committing (71 D-05, 73 D-09): `git merge-tree --write-tree HEAD origin/main` is used, never `git merge`. No ref is created, moved or deleted, and no merge commit exists anywhere after this plan.'
    - 'The merged tree is materialised into scratch from its own tree object and checked there: `uv lock --check`, `ruff check .` and `black --check .` all exit 0 against what the merge would actually produce, not against this worktree.'
    - 'Idempotency: `git merge-tree --write-tree HEAD origin/main` is run twice and prints the same tree SHA both times, so the recorded tree is a property of the two commits rather than of one invocation.'
    - '`main`''s branch protection is read rather than assumed (ROADMAP constraint 10): the required status-check contexts and the strict flag are fetched with `gh api` and compared against the values Phase 74 recorded at its close; a difference is recorded, not silently absorbed.'
    - 'The merge-method precedent is read from `origin/main`''s own first-parent history rather than from memory, and the repository''s allowed merge methods are fetched.'
    - 'The Dependabot and open-pull-request census is measured fresh at this plan''s own execution time and written as found. Neither the nine stale branches recorded at discussion time nor the zero measured during research is carried forward as an assumption; whatever is found becomes the handoff''s ordering input.'
    - 'Every command in this plan is a read or a scratch-directory write. The repository''s own refs, working tree and remote are unchanged by it.'
  artifacts:
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-PREFLIGHT-EVIDENCE.md
      provides: 'the non-committing trial merge with its idempotency reading, the merged-tree lock and lint checks, main''s protection and merge-method reads, the fresh branch and pull-request census, and TRIAL_MERGE_VERDICT'
      contains: 'MERGE_TREE = '
  key_links:
    - from: 'origin/main'
      to: 'the bumped milestone branch tip'
      via: 'git merge-tree --write-tree, which writes a tree object and no commit'
      pattern: 'MERGE_TREE = '
    - from: '75-PREFLIGHT-EVIDENCE.md open-PR census'
      to: '75-HANDOFF.md Dependabot ordering note'
      via: 'read with sed in wave 4 and written into the handoff as measured'
      pattern: 'OPEN_PRS = '
  prohibitions:
    - statement: 'MUST NOT run `git merge`, `git rebase`, `git cherry-pick`, `git push`, `git branch -f`, `git update-ref` or `git symbolic-ref`; the trial merge is non-committing and this plan mutates no ref.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT open, comment on, review, close or merge any pull request, and MUST NOT create a tag, a release or a workflow dispatch; the pull-request census is read-only.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT assume `main`''s required contexts, strict flag or merge method from a prior phase''s memory; each is fetched live and any difference from the recorded prior value is reported.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT transcribe the discussion-time or research-time Dependabot branch count into this plan''s evidence; the census is measured at execution time and written as found.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT build any `gh` command body by shell command substitution; a body that is needed comes from a file path.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Discharge SC4's remote-readiness half without taking any action:
- a non-committing trial merge of `origin/main` into the bumped tip, checked twice for the same tree
  SHA, with `uv lock --check`, `ruff check .` and `black --check .` run against the merged tree
  itself
- `main`'s protection, required contexts and merge method read live
- the branch, Dependabot and open-pull-request census measured fresh

Purpose: `/gsd-complete-milestone` opens a PR against a `strict`-protected `main`, so the handoff
needs to know now whether the merge is clean and what checks will be required — and it needs a
Dependabot ordering input measured today, not the number recorded at discussion time, which had
already drifted by the time research ran the same day.

Output: `75-PREFLIGHT-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md
@.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-BASE-EVIDENCE.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` and
run project tooling through `uv run`. The merged-tree checks run inside a **separate** scratch
directory with their own `uv --directory <scratch>` invocations, so this worktree's venv is never
re-pointed at the merged tree.

**Refs are shared.** `refs/heads/gsd/v0.9.6-doctest-block-rendering-and-release` is the same ref the
main checkout has checked out. This plan reads it and never writes it.

**Evidence key lines.** Bare values only, `KEY = value` on its own line, explanation on the next.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_75_05`; helper prefix `p7505_`. The merged tree is
extracted into a second `mktemp -d` recorded as `MERGED_TREE_DIR`.

**Commits.** Plain `git commit`, `.planning/` only. Never write `75-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->

## Planning-time census (2026-09-20; context only — every row is re-measured, none is asserted)

| Fact | Value at planning |
|------|-------------------|
| `origin/main` | `6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b`, still the milestone base, so the trial merge is expected to be a recorded no-op |
| `git ls-remote --heads origin` | four refs: three `gsd/*` milestone branches and `main`; no `dependabot/*` branch |
| Open pull requests | zero at research time; nine stale Dependabot branches had been recorded six hours earlier at discussion time |
| `main` required contexts (Phase 74 close) | `REQUIRED_CONTEXTS_CLOSE` in `74-CI-EVIDENCE.md`, six contexts, `REQUIRED_STRICT_CLOSE = true` |
| Merge precedent | PRs #132, #136, #143, #145 and #151 each landed on `main` as a merge commit |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: the non-committing trial merge, twice for the same tree, then lock and lint the merged tree in scratch</name>
  <precondition>`git fetch origin` succeeds and `git rev-parse origin/main` resolves to a 40-character SHA.</precondition>
  <reversibility rating="reversible">`git merge-tree --write-tree` writes a tree object into the object database and creates no ref and no commit; the scratch extraction is outside the repository.</reversibility>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-PREFLIGHT-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v0.9.5-phases/73-v0-9-5-close-prep-prep-only-unpublished/73-05-PLAN.md Task 1 (the archive-and-check idiom, including the `uv --directory <scratch> sync --locked --extra dev --no-install-project` line this task reuses)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md § "Claude's Discretion" → "The trial merge is non-committing"
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md (`BUMP_COMMIT_SHA`, so the tree being merged is the bumped one)
  </read_first>
  <action>
Create `75-PREFLIGHT-EVIDENCE.md`, headed `# Phase 75 — Preflight Evidence (SC4 trial merge and remote reads)`.

1. **`## Head check and provisioning`.** `date -u +%FT%TZ`, `pwd -P`, `test -f .git; echo "exit:$?"`,
   the shim check, the provisioning line with its exit, `BASE_75_05 = ` from `git rev-parse HEAD`
   before any commit, `SCRATCH_75_05 = `, and `MERGED_TREE_DIR = `.

2. **`## Tip identity`.** Quote `BUMP_COMMIT_SHA` from `75-BUMP-EVIDENCE.md`, record
   `git merge-base --is-ancestor <BUMP_COMMIT_SHA> HEAD; echo "exit:$?"`, which must print
   `exit:0`, and `sed -n 7p pyproject.toml`, which must read `version = "0.9.6"`. If either fails,
   write `## HALT: not the bumped tip` and stop.

3. **`## Trial merge`.**
   - `git fetch origin`, then `ORIGIN_MAIN_SHA = ` from `git rev-parse origin/main` and
     `git log -1 --format='%H %s' origin/main`.
   - `MILESTONE_BASE = ` from `git merge-base HEAD origin/main`. Record
     `MAIN_MOVED = ` `yes` or `no` by comparing `ORIGIN_MAIN_SHA` to `MILESTONE_BASE`: `no` means
     `origin/main` has not moved off the milestone base and the merge is a fast-forwardable no-op.
   - `git merge-tree --write-tree HEAD origin/main; echo "exit:$?"`. Keys `MERGE_TREE_EXIT`, which
     must be 0 — a non-zero exit means a conflict — and `MERGE_TREE = `, the printed tree SHA.
   - Run the identical command a second time and record `MERGE_TREE_SECOND = `. Key
     `MERGE_TREE_IDEMPOTENT = yes` only when the two tree SHAs are equal.
   - On a non-zero exit, transcribe the conflict output verbatim, write
     `## HALT: trial merge conflicts` and stop. Never resolve it with a real merge here.

4. **`## Merged-tree lock and lint`.**
   - `git archive --format=tar -o "$SCRATCH_75_05/p7505_merged.tar" "$MERGE_TREE"` and
     `tar -xf ... -C "$MERGED_TREE_DIR"`.
   - `uv --directory "$MERGED_TREE_DIR" lock --check; echo "exit:$?"`, key `MERGED_LOCK_CHECK_EXIT`,
     which must be 0: the merged tree's `pyproject.toml` and `uv.lock` still agree.
   - `uv --directory "$MERGED_TREE_DIR" sync --locked --extra dev --no-install-project -q; echo "exit:$?"`,
     key `MERGED_SYNC_EXIT`.
   - `uv --directory "$MERGED_TREE_DIR" run --no-sync ruff check .` and the same with
     `black --check .`, keys `MERGED_RUFF_EXIT` and `MERGED_BLACK_EXIT`, both of which must be 0.
     Record the ruff version the merged lock resolves, key `MERGED_RUFF_VERSION`, from
     `uv --directory "$MERGED_TREE_DIR" run --no-sync ruff --version` — this project has had a CI
     run fail on lint alone while every test lane passed, so the version that will run matters.
   - Record the merged tree's own version literal:
     `MERGED_VERSION = ` from `grep -m1 '^version = ' "$MERGED_TREE_DIR/pyproject.toml"`, which must
     read `0.9.6` — the merge must not have reverted the bump.

5. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-PREFLIGHT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_75_05)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; C="$(k BUMP_COMMIT_SHA "$D/75-BUMP-EVIDENCE.md")" &amp;&amp; [ "${#C}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$C" HEAD &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.6"' ] &amp;&amp; git fetch -q origin main &amp;&amp; OM="$(git rev-parse origin/main)" &amp;&amp; [ "${#OM}" = 40 ] &amp;&amp; [ "$(k ORIGIN_MAIN_SHA)" = "$OM" ] &amp;&amp; MB="$(git merge-base HEAD origin/main)" &amp;&amp; [ "$(k MILESTONE_BASE)" = "$MB" ] &amp;&amp; case "$(k MAIN_MOVED)" in yes|no) true ;; *) false ;; esac &amp;&amp; MT="$(k MERGE_TREE)" &amp;&amp; [ "${#MT}" = 40 ] &amp;&amp; [ "$(k MERGE_TREE_EXIT)" = 0 ] &amp;&amp; [ "$(k MERGE_TREE_SECOND)" = "$MT" ] &amp;&amp; [ "$(k MERGE_TREE_IDEMPOTENT)" = yes ] &amp;&amp; LIVE="$(git merge-tree --write-tree HEAD origin/main)" &amp;&amp; [ "$LIVE" = "$MT" ] &amp;&amp; git cat-file -e "$MT^{tree}" &amp;&amp; [ "$(git cat-file -t "$MT")" = tree ] &amp;&amp; MD="$(k MERGED_TREE_DIR)" &amp;&amp; test -d "$MD" &amp;&amp; test -s "$MD/pyproject.toml" &amp;&amp; [ "$(grep -m1 '^version = ' "$MD/pyproject.toml")" = 'version = "0.9.6"' ] &amp;&amp; [ "$(k MERGED_VERSION)" = 'version = "0.9.6"' ] &amp;&amp; S2="$(mktemp -d)" &amp;&amp; X="$(mktemp)" &amp;&amp; git archive --format=tar -o "$X" "$MT" &amp;&amp; tar -xf "$X" -C "$S2" &amp;&amp; uv --directory "$S2" lock --check &amp;&amp; uv --directory "$S2" sync --locked --extra dev --no-install-project -q &amp;&amp; RX="$(uv --directory "$S2" run --no-sync ruff check . &gt;/dev/null 2&gt;&amp;1; echo $?)" &amp;&amp; BX="$(uv --directory "$S2" run --no-sync black --check . &gt;/dev/null 2&gt;&amp;1; echo $?)" &amp;&amp; RV="$(uv --directory "$S2" run --no-sync ruff --version | cut -d' ' -f2)" &amp;&amp; rm -rf "$S2" "$X" &amp;&amp; [ "$RX" = 0 ] &amp;&amp; [ "$BX" = 0 ] &amp;&amp; [ -n "$RV" ] &amp;&amp; [ "$(k MERGED_RUFF_VERSION)" = "$RV" ] &amp;&amp; for key in MERGED_LOCK_CHECK_EXIT MERGED_SYNC_EXIT MERGED_RUFF_EXIT MERGED_BLACK_EXIT; do [ "$(k "$key")" = 0 ] || { echo "nonzero: $key"; exit 1; }; done &amp;&amp; [ -z "$(git for-each-ref --format='%(refname)' 'refs/heads/*merge*' 'refs/heads/*trial*')" ] &amp;&amp; for s in '## Head check and provisioning' '## Tip identity' '## Trial merge' '## Merged-tree lock and lint'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `nonzero: KEY` or `missing section: NAME` printed. Each of these makes it fail: not in a worktree, or the shim absent; `BASE_75_05` or `BUMP_COMMIT_SHA` malformed or not an ancestor of HEAD; `pyproject.toml` line 7 not reading `version = "0.9.6"`; the recorded `origin/main` SHA or milestone base differing from the live read; `MAIN_MOVED` outside `yes` and `no`; `MERGE_TREE` not a 40-character SHA, the recorded merge-tree exit non-zero, the two recorded tree SHAs differing, `MERGE_TREE_IDEMPOTENT` not `yes`, or a live third `merge-tree` printing a different SHA; the recorded SHA not naming a real tree object; the merged-tree scratch directory missing, or its `pyproject.toml` not carrying the 0.9.6 literal; a live `uv lock --check`, `uv sync --locked` , `ruff check .` or `black --check .` against a freshly re-extracted merged tree exiting non-zero; the recorded ruff version differing from the one the merged lock actually resolves; any recorded merged-tree exit key non-zero; a local branch whose name suggests a real merge or trial branch was created; any named section missing; any tracked product file changed; `.planning/REQUIREMENTS.md` changed; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `git merge-tree --write-tree HEAD origin/main` exits 0 and prints the same tree SHA on two recorded runs and a third live run.
    - The merged tree is extracted from its own tree object and passes `uv lock --check`, `uv sync --locked --extra dev --no-install-project`, `ruff check .` and `black --check .` there, with the resolved ruff version recorded.
    - The merged tree's `pyproject.toml` still reads `version = "0.9.6"`.
    - No merge commit, no new branch and no ref update exists after the task; `MAIN_MOVED` records whether `origin/main` has moved off the milestone base.
  </acceptance_criteria>
  <done>The merge `/gsd-complete-milestone` will perform is proven clean and lint-green against `origin/main` as it stands, without a single ref having been written.</done>
</task>

<task type="auto">
  <name>Task 2: Read main's protection, the merge-method precedent and the fresh branch and pull-request census, then record the trial-merge verdict</name>
  <precondition>`gh auth status` succeeds and `gh api repos/YuSabo90002/typsphinx` returns JSON.</precondition>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-PREFLIGHT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CI-EVIDENCE.md (`REQUIRED_STRICT_CLOSE`, `REQUIRED_CONTEXTS_CLOSE` — the values this task compares its live read against)
    - .planning/ROADMAP.md § "v0.9.6" constraint 10 (each phase reads the required checks rather than assuming them)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md § domain (the branch and Dependabot figures recorded at discussion time, present only so this task can show its own reading differs)
  </read_first>
  <action>
1. **`## main protection`.** Record verbatim:
   - `gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict`,
     key `PROTECTION_STRICT`.
   - the same endpoint with `--jq '[.contexts[]] | sort | join("|")'`, key `PROTECTION_CONTEXTS`,
     and `--jq '.contexts | length'`, key `PROTECTION_CONTEXT_COUNT`.
   - Quote `REQUIRED_STRICT_CLOSE` and `REQUIRED_CONTEXTS_CLOSE` from `74-CI-EVIDENCE.md` beside
     them. Key `REQUIRED_CHECKS_UNCHANGED_SINCE_74 = ` `yes` or `no`. A `no` is recorded, named
     context by context, and carried into the handoff — it is a finding, not a failure.

2. **`## Merge method`.** Record
   `gh api repos/YuSabo90002/typsphinx --jq '{allow_merge_commit, allow_squash_merge, allow_rebase_merge}'`
   verbatim, keys `ALLOW_MERGE_COMMIT`, `ALLOW_SQUASH_MERGE` and `ALLOW_REBASE_MERGE`. Then read the
   precedent from history rather than memory: `git log --first-parent --format='%h %s' origin/main`,
   and record `MERGE_PRECEDENT_HITS = `, the number of first-parent subjects matching
   `Merge pull request #`, with the most recent five quoted. State which method the handoff should
   therefore expect.

3. **`## Branch census`.** Record `git ls-remote --heads origin` verbatim and, from it:
   - `REMOTE_BRANCH_COUNT = `, the total line count
   - `DEPENDABOT_BRANCHES = `, the number of refs under `refs/heads/dependabot/`
   - `MILESTONE_BRANCH_ON_ORIGIN = `, the SHA of
     `refs/heads/gsd/v0.9.6-doctest-block-rendering-and-release`, or `none`
   - `DECOY_ON_ORIGIN = `, the number of refs naming a `gsd/v0.9.6-milestone` decoy
   Write one sentence stating that this count is measured here and is neither the figure recorded at
   discussion time nor the one measured during research — both are already stale by construction.

4. **`## Open pull requests`.** Record
   `gh pr list --state open --json number,title,author,headRefName,baseRefName,createdAt` verbatim.
   Keys `OPEN_PRS = `, the total, and `DEPENDABOT_OPEN_PRS = `, the number authored by
   `app/dependabot`. Also record `gh pr list --state all --limit 5 --json number,state` as a
   positive control proving the query reaches a non-empty result set, so a zero open count is not a
   broken query. Key `PR_QUERY_CONTROL_ROWS = `, which must be at least 1. Record
   `BRANCH_PRS = `, the number of pull requests whose head ref is the milestone branch, which must
   be 0 — this phase opens none. Record `PR_CENSUS_AT = ` from `date -u +%FT%TZ`.
   - If `DEPENDABOT_OPEN_PRS` is greater than 0, record each PR's number, package and version range,
     and note that the handoff must order them **after** the milestone PR, the order v0.9.3 used.
   - If it is 0, say so plainly: there is no merge-ordering question at this reading, and the handoff
     carries the rule conditionally in case one opens before `/gsd-complete-milestone` runs.

5. **`## What the handoff can rely on`** and **`## Trial merge verdict`.** Summarise, in the order
   `/gsd-complete-milestone` will need them: the merge is clean or not; the required contexts that
   must be green before merge; the merge method; the Dependabot ordering input. Then
   `TRIAL_MERGE_VERDICT = MET` only when `MERGE_TREE_EXIT`, `MERGED_LOCK_CHECK_EXIT`,
   `MERGED_RUFF_EXIT` and `MERGED_BLACK_EXIT` are all 0, `MERGE_TREE_IDEMPOTENT` is `yes`,
   `PROTECTION_CONTEXT_COUNT` is at least 1, and `BRANCH_PRS` is 0.

6. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-PREFLIGHT-EVIDENCE.md" &amp;&amp; P74=.planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs &amp;&amp; BR=gsd/v0.9.6-doctest-block-rendering-and-release &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; RC="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '[.contexts[]] | sort | join("|")')" &amp;&amp; [ -n "$RC" ] &amp;&amp; [ "$(k PROTECTION_CONTEXTS)" = "$RC" ] &amp;&amp; ST="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq .strict)" &amp;&amp; [ -n "$ST" ] &amp;&amp; [ "$(k PROTECTION_STRICT)" = "$ST" ] &amp;&amp; N="$(gh api repos/YuSabo90002/typsphinx/branches/main/protection/required_status_checks --jq '.contexts | length')" &amp;&amp; [ "$N" -ge 1 ] &amp;&amp; [ "$(k PROTECTION_CONTEXT_COUNT)" = "$N" ] &amp;&amp; P74C="$(k REQUIRED_CONTEXTS_CLOSE "$P74/74-CI-EVIDENCE.md")" &amp;&amp; [ -n "$P74C" ] &amp;&amp; case "$(k REQUIRED_CHECKS_UNCHANGED_SINCE_74)" in yes|no) true ;; *) false ;; esac &amp;&amp; if [ "$RC" = "$P74C" ]; then [ "$(k REQUIRED_CHECKS_UNCHANGED_SINCE_74)" = yes ]; else [ "$(k REQUIRED_CHECKS_UNCHANGED_SINCE_74)" = no ]; fi &amp;&amp; AM="$(gh api repos/YuSabo90002/typsphinx --jq .allow_merge_commit)" &amp;&amp; [ -n "$AM" ] &amp;&amp; [ "$(k ALLOW_MERGE_COMMIT)" = "$AM" ] &amp;&amp; [ -n "$(k ALLOW_SQUASH_MERGE)" ] &amp;&amp; [ -n "$(k ALLOW_REBASE_MERGE)" ] &amp;&amp; git fetch -q origin main &amp;&amp; FP="$(git log --first-parent --format=%s origin/main)" &amp;&amp; [ -n "$FP" ] &amp;&amp; MP="$(printf '%s\n' "$FP" | grep -c 'Merge pull request #')" &amp;&amp; [ "$MP" -ge 1 ] &amp;&amp; [ "$(k MERGE_PRECEDENT_HITS)" = "$MP" ] &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; RB="$(printf '%s\n' "$HL" | wc -l)" &amp;&amp; [ "$(k REMOTE_BRANCH_COUNT)" = "$RB" ] &amp;&amp; DB="$(printf '%s\n' "$HL" | grep -c 'refs/heads/dependabot/')" &amp;&amp; [ "$(k DEPENDABOT_BRANCHES)" = "$DB" ] &amp;&amp; DC="$(printf '%s\n' "$HL" | grep -c 'gsd/v0\.9\.6-milestone')" &amp;&amp; [ "$DC" = 0 ] &amp;&amp; [ "$(k DECOY_ON_ORIGIN)" = 0 ] &amp;&amp; [ -n "$(k MILESTONE_BRANCH_ON_ORIGIN)" ] &amp;&amp; OP="$(gh pr list --state open --json number --jq 'length')" &amp;&amp; [ "$(k OPEN_PRS)" = "$OP" ] &amp;&amp; DP="$(gh pr list --state open --json author --jq '[.[] | select(.author.login == "app/dependabot")] | length')" &amp;&amp; [ "$(k DEPENDABOT_OPEN_PRS)" = "$DP" ] &amp;&amp; CR="$(gh pr list --state all --limit 5 --json number --jq 'length')" &amp;&amp; [ "$CR" -ge 1 ] &amp;&amp; [ "$(k PR_QUERY_CONTROL_ROWS)" = "$CR" ] &amp;&amp; [ "$(gh pr list --head "$BR" --state all --json number --jq 'length')" = 0 ] &amp;&amp; [ "$(k BRANCH_PRS)" = 0 ] &amp;&amp; [ -n "$(k PR_CENSUS_AT)" ] &amp;&amp; [ "$(k TRIAL_MERGE_VERDICT)" = MET ] &amp;&amp; for s in '## main protection' '## Merge method' '## Branch census' '## Open pull requests' '## What the handoff can rely on' '## Trial merge verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; B="$(k BASE_75_05)" &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail: the protection endpoint returning nothing, or the recorded contexts, strict flag or context count differing from the live read; the context count below 1; `74-CI-EVIDENCE.md`'s recorded contexts unreadable; `REQUIRED_CHECKS_UNCHANGED_SINCE_74` outside `yes` and `no`, or disagreeing with the live comparison against Phase 74's value; any allowed-merge-method key empty or differing from the live read; `origin/main`'s first-parent history empty, or its merge-PR count zero or differing from the recorded key; `git ls-remote --heads origin` returning nothing, or the branch, Dependabot or decoy counts differing from their recorded keys; a `gsd/v0.9.6-milestone` decoy on origin; `MILESTONE_BRANCH_ON_ORIGIN` empty; the open-PR or Dependabot-PR count differing from its recorded key; the all-state control query returning zero rows, which would make a zero open count unfalsifiable, or differing from its recorded key; any pull request already open against the milestone branch, or `BRANCH_PRS` not 0; `PR_CENSUS_AT` empty; `TRIAL_MERGE_VERDICT` not `MET`; any named section missing; any tracked product file changed; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `PROTECTION_CONTEXTS`, `PROTECTION_STRICT` and `PROTECTION_CONTEXT_COUNT` equal live `gh api` reads, and `REQUIRED_CHECKS_UNCHANGED_SINCE_74` states whether they still match Phase 74's close reading.
    - The three allowed merge methods are recorded from a live read and `MERGE_PRECEDENT_HITS` equals the live first-parent merge-PR count on `origin/main`, with five subjects quoted.
    - The branch census records the total, the Dependabot count, the milestone branch's origin SHA and a zero decoy count, all matching live reads, with a sentence stating the figure is measured here.
    - `OPEN_PRS` and `DEPENDABOT_OPEN_PRS` match live reads, `PR_QUERY_CONTROL_ROWS` is at least 1, and `BRANCH_PRS = 0`.
    - `TRIAL_MERGE_VERDICT = MET` with its contributing keys enumerated.
  </acceptance_criteria>
  <done>The handoff has live, falsifiable answers to what the merge PR will face: a clean merge, the exact required contexts, the merge method, and today's Dependabot ordering input.</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide spec-less probe accounting lives in `75-01-PLAN.md` § "Spec-less probe accounting". This
plan authors no edge row of its own; the merge-tree idempotency check here is an additional,
non-edge invariant.

**Assumption specific to this plan:** that `origin/main` at read time is what the release PR will
merge against. It may move between this reading and `/gsd-complete-milestone` — five Dependabot PRs
merged into `main` between a milestone's phases once before. The plan therefore records
`ORIGIN_MAIN_SHA` and `MAIN_MOVED` rather than asserting a no-op, and `75-HANDOFF.md` carries the
conditional update-merge step keyed on re-reading `origin/main` at that time.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| milestone branch → `origin/main` | the merge is the irreversible step this phase must prove safe without performing |
| remote configuration → local assumption | required contexts, strict flag and merge method are repository settings that change outside this project's control |
| `gh` command construction → GitHub API | a command body assembled from unvetted text is the injection surface here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-20 | Tampering | a trial merge that creates a real merge commit or moves a ref | high | mitigate | `git merge-tree --write-tree` only; `git merge`, `git push`, `git branch -f` and `git update-ref` are prohibited, and the verify asserts no merge- or trial-named branch exists |
| T-75-21 | Spoofing | assuming `main`'s required checks from a prior phase's memory | medium | mitigate | every value is fetched live and compared against Phase 74's recorded reading, with a difference recorded context by context |
| T-75-22 | Repudiation | a zero open-PR count that is zero because the query is wrong | medium | mitigate | an all-state control query must return at least one row before any zero is believed |
| T-75-23 | Elevation of Privilege | a `gh` command body built by shell command substitution from file content | medium | mitigate | no `gh` write command is issued by this plan at all, and any body needed later comes from a file path rather than a substitution |
| T-75-24 | Denial of Service | a lint version skew between the merged tree and CI turning the release PR red after merge | medium | mitigate | the merged tree's own resolved ruff version is recorded and lint is run inside it, not in this worktree |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list, identical in every plan of this phase. Every symbol below is created by Phase 75
and did not exist at the phase base.

**New files under `.planning/phases/75-v0-9-6-release-prep-prep-only/`:** `75-CLOSEOUT-GUARD.md`,
`75-BASE-EVIDENCE.md`, `75-SC5-INVARIANTS.md`, `75-LEFTOVERS-EVIDENCE.md`, `COVERAGE.md`,
`75-BUMP-EVIDENCE.md`, `75-CHANGELOG-EVIDENCE.md`, `75-GREEN-TREE-EVIDENCE.md`,
`75-PREFLIGHT-EVIDENCE.md`, `75-CI-EVIDENCE.md`, `75-HANDOFF.md`, plus one `75-0N-SUMMARY.md` per
plan.

**New file under `.planning/todos/pending/`:** a record for IN-01 (D-14), the
`visit_literal_block` / `depart_literal_block` `Args:` text that still names only the literal block
node.

**New headings and lines in `CHANGELOG.md`:** `## [0.9.6] - <prep date>` and, inside it,
`### Added`, `### Changed`, `### Fixed`, `### Known Limitations` and `### Verified`; a fresh
`## [Unreleased]` heading placed above it, retaining only `### Planned for Future Releases`; and a
new tail link line `[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6`.

**New value in `tests/test_changelog_page_gate.py`:** the `RELEASE_VERSIONS` tuple member `"0.9.6"`.

**Changed version literals (not new symbols):** `pyproject.toml`'s `[project].version`, the
`typsphinx` stanza's `version` in `uv.lock`, and `README.md`'s `**Status**: Stable (vX.Y.Z)` line,
all moving `0.9.2` to `0.9.6`.

**Deletions (no new symbol):** the five untracked repository-root files `probe_bogusxyz.typ`,
`probe_none.typ`, `probe_pycon.typ`, `probe_python.typ` and `probe_text.typ`.
</artifacts_this_phase_produces>

<verification>
- The trial merge exits 0, prints the same tree SHA three times, and no ref was written.
- The merged tree passes `uv lock --check`, a locked sync, `ruff check .` and `black --check .` in
  scratch, and still carries the 0.9.6 literal.
- `main`'s protection, required contexts, strict flag and merge methods are recorded from live reads
  and compared against Phase 74's close reading.
- The branch, Dependabot and open-PR census matches live reads and carries a non-empty control.
- `TRIAL_MERGE_VERDICT = MET`.
</verification>

<success_criteria>
- SC4's trial-merge clause is discharged without a merge commit existing anywhere.
- The handoff has measured answers for the merge PR: cleanliness, required checks, merge method and
  Dependabot ordering.
- No pull request, tag, release or dispatch was created by this plan.
</success_criteria>

<output>
Create `.planning/phases/75-v0-9-6-release-prep-prep-only/75-05-SUMMARY.md` when done, declaring
`requirements-completed: []`.
</output>
