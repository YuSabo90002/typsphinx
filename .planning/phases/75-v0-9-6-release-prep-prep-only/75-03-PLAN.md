---
phase: 75-v0-9-6-release-prep-prep-only
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - README.md
  - CHANGELOG.md
  - tests/test_changelog_page_gate.py
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CHANGELOG-EVIDENCE.md
autonomous: true
requirements: [REL-15, REL-16]
# REL-15 is a coverage ID only (ROADMAP constraint 1 and the Phase 75 cross-cutting
# constraints): this plan does the bump and the CHANGELOG promotion REL-15 describes, but
# does not close it and never touches its checkbox — that happens at /gsd-complete-milestone
# against observed publish evidence. REL-16 is settled on the record by this plan's
# ### Known Limitations section, but its checkbox is moved by phase-completion tooling at
# close, never by a plan.
requirements-completed: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC1 one commit, five files (the AMENDED resolution): `git show --name-only` on the bump commit lists `pyproject.toml`, `uv.lock`, `README.md`, `CHANGELOG.md` and `tests/test_changelog_page_gate.py` together and nothing else. The bump and the CHANGELOG curation cannot be split across plans, waves or commits; Phase 46''s two-commit split is explicitly not the precedent.'
    - 'SC1 bump mechanics: `pyproject.toml`''s `[project].version` is the sole hand-edited version literal and reads `0.9.6`; `uv.lock`''s `typsphinx` stanza reads `0.9.6` because `uv lock` regenerated it, never because it was edited; `README.md`''s Status line reads `v0.9.6`; `uv sync --extra dev --extra docs --locked` exits 0; `tests/test_readme_version_sync.py` and `tests/test_preview_version_sync.py` are green.'
    - 'SC1 zero-skip: `tests/test_changelog_page_gate.py` runs with `-rs` in an environment carrying the `docs` extra and reports zero skipped, after `RELEASE_VERSIONS` gains `"0.9.6"` and the `## [0.9.6]` section exists for it to render.'
    - 'SC2 byte-identical carry (D-01): the `### Added` and `### Changed` bodies of the promoted section are byte-identical to the phase base''s `## [Unreleased]` bodies, and the base''s single `### Fixed` bullet is the byte-identical tail of the new `### Fixed`. The six carried bullets are counted at the phase base, not assumed to be six.'
    - 'SC2 new content (D-02, D-03, D-06, D-07, D-08, D-09): a lead paragraph framed on the doctest rendering fix with an explicit upgrade recommendation written against 0.9.2; a doctest bullet claiming both the rendering fix and the compile abort, disclosing the `python`-fence trade-off and citing the published API reference as its one evidence sentence; a QUA-14 bullet of its own, second in `### Fixed`.'
    - 'SC2 structure: a fresh `## [Unreleased]` above `## [0.9.6]` retaining only `### Planned for Future Releases`; the tail link block moved so `[Unreleased]` compares against `v0.9.6` and a `[0.9.6]` releases/tag link sits above `[0.9.2]`; no `## [0.9.3]`, `## [0.9.4]` or `## [0.9.5]` heading and no matching tail link anywhere in the file.'
    - 'SC3 REL-16 settled (D-10, D-11, and the AMENDED candidate-set correction): `## [0.9.6]` carries a `### Known Limitations` section holding NUM-01 and nothing else — with its precondition, both symptoms and an honest workaround, promising no fix and no version. The converted-image rehome collision and the `typst_documents` duplicate-target cluster were measured closed in v0.8.0 and are not written; WR-02 and WR-03 are not added.'
    - 'SC2 extractor (D-06 of the script''s own docstring): `scripts/extract_changelog_section.py 0.9.6` is executed, not reimplemented, and its stdout is transcribed into the phase evidence — non-empty and byte-faithful to the `## [0.9.6]` section body.'
    - 'D-04 `### Verified`: zero new runtime dependencies with the `dev` extra''s `tox-uv` return named as the one change, the four `@preview` packages unchanged across the three sync surfaces, the GATE-01 real-compile coverage, and the clean-build warning ledger. The blanket dependency sentence `[0.9.2]` used is measurably false for this milestone and is not copied.'
    - 'Edge (adjacency): after the curation `CHANGELOG.md` carries exactly two `^### Known Limitations` headings — the pre-existing one in `[0.1.0b1]` plus the new one (D-12) — exactly one `^## [Unreleased]` heading, and exactly one `^## [0.9.6]` heading whose line number is greater than the `## [Unreleased]` heading''s. The `[0.9.6]` tail link sits on the line immediately above the `[0.9.2]` tail link. The extractor''s positional algorithm terminates at the next `## [` heading, so its stdout contains no `## [` line at all.'
    - 'Edge (empty): the extractor''s stdout is non-empty measured in bytes, and carries zero `^### Planned for Future Releases` lines — the scratch block stays behind under the fresh `## [Unreleased]` and must not leak into the release body.'
    - 'Edge (ordering): inside `## [0.9.6]` the `###` headings read `Added`, `Changed`, `Fixed`, `Known Limitations`, `Verified` in that source order; within `### Fixed` the bullets read doctest, then QUA-14, then the carried sidebar bullet; the fresh `## [Unreleased]` is placed above `## [0.9.6]`, and the promoted `### Added` / `### Changed` / `### Fixed` keep the assignment the bullets already had.'
    - 'Edge (idempotency): `uv lock` is run twice and the second run leaves `uv.lock`''s digest unchanged; `uv lock --check` then exits 0 against the committed pair, so the lock is derivable rather than hand-written.'
  artifacts:
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md
      provides: 'the before and after version-surface table, the lock regeneration transcript with its idempotency reading, the locked sync, the three version-sync gates and the five-file commit shape'
      contains: 'BUMP_COMMIT_SHA = '
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-CHANGELOG-EVIDENCE.md
      provides: 'the base bullet census, the byte-identity digests of the carried subsections, the new-section structure census, the Known Limitations branch record and the extractor transcript'
      contains: 'CARRIED_BULLETS_BASE = '
    - path: CHANGELOG.md
      provides: 'the curated `## [0.9.6]` section, the fresh `## [Unreleased]` and the moved tail link block'
      contains: '## [0.9.6]'
  key_links:
    - from: 'CHANGELOG.md `## [0.9.6]` section'
      to: 'the GitHub Release body at /gsd-complete-milestone'
      via: 'scripts/extract_changelog_section.py, which release.yml calls from both its validate and create-release jobs'
      pattern: 'extract_changelog_section.py 0.9.6'
    - from: 'pyproject.toml [project].version'
      to: 'uv.lock typsphinx stanza and README.md Status line'
      via: 'uv lock regeneration and tests/test_readme_version_sync.py'
      pattern: 'version = "0.9.6"'
    - from: 'tests/test_changelog_page_gate.py RELEASE_VERSIONS'
      to: 'the rendered changelog.html content-coverage assertions'
      via: 'a real -b html build of docs/source, which needs myst-parser from the docs extra'
      pattern: '"0.9.6"'
  prohibitions:
    - statement: 'MUST NOT split the version bump and the CHANGELOG curation across two commits; the bump commit carries the five-file union and `git show --name-only` on it lists exactly those five.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT hand-edit `uv.lock`; it is regenerated by `uv lock` and proved by `uv sync --extra dev --extra docs --locked` and `uv lock --check`.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT re-word, compress, re-order or re-assign the carried `## [Unreleased]` bullets; they are promoted byte-identically in the `### Added` / `### Changed` / `### Fixed` assignment they already have.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT name the converted-image rehome collision or the `typst_documents` duplicate-target cluster in `### Known Limitations` — both were measured closed in v0.8.0 — and MUST NOT add WR-02 or WR-03, which REL-16 does not name.'
      requirement_id: REL-16
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT copy the blanket zero-dependency-change sentence from the `[0.9.2]` section into `### Verified`; it is measurably false for this milestone, whose `dev` extra changed.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT create a `## [0.9.3]`, `## [0.9.4]` or `## [0.9.5]` heading or tail link, and MUST NOT promote the `### Planned for Future Releases` scratch block into the released section.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT reimplement or hand-copy the changelog extraction; `scripts/extract_changelog_section.py` is executed and its stdout transcribed.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT edit any file under `typsphinx/`, `.github/`, `flake.nix` or `tox.ini`, and MUST NOT edit, stage or commit `.planning/REQUIREMENTS.md` or move REL-15''s checkbox.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Produce the release itself, as one commit (SC1, SC2, SC3):
- move the version to `0.9.6` across `pyproject.toml`, a regenerated `uv.lock`, `README.md` and
  `tests/test_changelog_page_gate.py`'s `RELEASE_VERSIONS`
- curate one `## [0.9.6]` section: the carried bullets promoted byte-identically, two new Phase-74
  bullets, a `### Known Limitations` section holding NUM-01 alone, a `### Verified` section whose
  invariance claims are true for this milestone's diff, a fresh `## [Unreleased]` above it and the
  tail link block moved
- land all five files in one commit, then execute the extractor and transcribe its stdout

Purpose: `git show --name-only` on this commit is SC1's literal acceptance check, and a commit
touching only `pyproject.toml` is the shape that once killed every dependabot PR here. The
`### Known Limitations` section is the branch REL-16 takes, and its candidate set is NUM-01 alone
because the other two defects REL-16's text names were measured closed in v0.8.0.

Output: the five-file commit, `75-BUMP-EVIDENCE.md`, `75-CHANGELOG-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/75-v0-9-6-release-prep-prep-only/75-CONTEXT.md
@.planning/phases/75-v0-9-6-release-prep-prep-only/75-RESEARCH.md
@.planning/phases/75-v0-9-6-release-prep-prep-only/75-PATTERNS.md
@.planning/todos/pending/2026-08-14-numref-number-diverges-per-master-and-vanishes-for-non-root-only-figures.md
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` and
run every tool through `uv run`. **The `docs` extra is not optional here:** without `myst-parser`,
`tests/test_changelog_page_gate.py`'s content-coverage classes skip instead of running, and SC1
demands a zero-skipped reading. `-rs` goes on every invocation of that file.

**Tooling.** Bare `uv`, `tox`, `pytest`, `ruff`, `black`, `mypy`, `sphinx-build` through the PATH
shims; never `.venv/bin/<tool>` directly.

**Commit discipline.** Tasks 1 and 2 leave the five product edits **uncommitted** and commit only
their own `.planning/` evidence path, staged explicitly by path so no product file is swept in.
Task 3 makes the single five-file product commit, then commits its evidence separately. Plain
`git commit` throughout; never a GSD commit helper.

**Evidence key lines.** Bare values only, `KEY = value` on its own line, explanation on the next.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_75_03`; helper prefix `p7503_`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: ### Planned for Future Releases -->
<!-- planner-discipline-allow: Planned for Future Releases -->
<!-- planner-discipline-allow: ## \[0\.9\.[345]\] -->
<!-- planner-discipline-allow: 0\.9\.[345] -->
<!-- planner-discipline-allow: compare/v0\.9\.2\.\.\.HEAD -->
<!-- planner-discipline-allow: Zero new runtime or dev dependencies -->
<!-- planner-discipline-allow: rehome -->
<!-- planner-discipline-allow: duplicate-target -->
<!-- planner-discipline-allow: WR-02 -->
<!-- planner-discipline-allow: WR-03 -->
<!-- planner-discipline-allow: rehome|duplicate-target|WR-02|WR-03 -->
<!-- planner-discipline-allow: ^- \*\* -->
<!-- planner-discipline-allow: ^### Planned for Future Releases -->
<!-- planner-discipline-allow: ^## \[ -->
<!-- plan-criteria-allow: R1 - the counts at or above 2 are structural assertions on a region extracted from the new section (four Changed bullets, three Fixed bullets, and two anchored Known Limitations headings after this phase adds the second). Each is required to be false before this task runs and true after, which is the intent rather than the trap: the region is re-extracted live in the same command, so the number cannot be satisfied by pre-existing text elsewhere in the file -->

## Planning-time census (2026-09-20, HEAD `1048b73e`; context only — re-measure, never trust)

| Fact | Value at planning |
|------|-------------------|
| `pyproject.toml` line 7 | `version = "0.9.2"`; it is the only `^version = ` line in the file |
| `README.md` line 348 | `**Status**: Stable (v0.9.2) - Production ready` |
| `uv.lock` `typsphinx` stanza | `version = "0.9.2"` |
| `CHANGELOG.md` | 1349 lines; `## [Unreleased]` at `:8` with `### Added` (1 bullet), `### Changed` (4), `### Fixed` (1) and `### Planned for Future Releases` at `:62`; next heading `## [0.9.2] - 2026-08-30` at `:69` |
| Bullet census | `awk` over the Unreleased section counts **6** `^- \*\*` bullets |
| Heading / link counts | twenty-three version headings and twenty-three tail link references; the link block runs `:1327` to `:1349` with `[Unreleased]` last |
| `### Known Limitations` | anchored `grep -c '^### Known Limitations'` = **1** (`:1205`, in `[0.1.0b1]`); unanchored `grep -c 'Known Limitations'` = **2**, because `:665` is a prose mention — anchor every check |
| `RELEASE_VERSIONS` | `tests/test_changelog_page_gate.py:50`, ending `"0.9.2"` |
| Extractor output | body only, heading excluded, leading and trailing blanks stripped (`extract_section`) |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: move the version literal end to end — pyproject, a regenerated uv.lock, README — and prove it with a locked sync and the two version-sync gates</name>
  <precondition>The working tree is clean of product edits at task start (`git status --porcelain -- . ':(exclude).planning'` is empty), so the diff this task produces is entirely its own.</precondition>
  <reversibility rating="reversible">All four surfaces are plain file edits on an unpushed branch; nothing is tagged, published or pushed by this task.</reversibility>
  <files>pyproject.toml, uv.lock, README.md, .planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md</files>
  <read_first>
    - pyproject.toml (the `[project]` table — confirm `version` is the only `^version = ` line before editing, and re-measure its line number)
    - README.md (the `**Status**: Stable (vX.Y.Z) - Production ready` line — re-measure its line number)
    - tests/test_readme_version_sync.py (the regex it parses the Status line with, so the edit keeps the wording it guards)
    - uv.lock (the `typsphinx` stanza — read it, and never edit it)
    - .planning/milestones/v0.7.1-phases/46-v0-7-1-release-prep-prep-only/46-BUMP-EVIDENCE.md (the before/after table and regeneration transcript shape this evidence file mirrors)
  </read_first>
  <action>
Create `75-BUMP-EVIDENCE.md`, headed `# Phase 75 — Bump Evidence (SC1)`.

1. **`## Head check and provisioning`.** `date -u +%FT%TZ`, `pwd -P`, `test -f .git; echo "exit:$?"`,
   the shim check, the provisioning line with its exit, `PYVENV_HOME`, `PYVENV_VERSION_INFO`,
   `BASE_75_03 = ` from `git rev-parse HEAD` before any commit, and `SCRATCH_75_03 = `.

2. **`## Before`.** Record the three surfaces verbatim with their re-measured line numbers:
   `grep -n '^version = ' pyproject.toml`; `grep -n '\*\*Status\*\*: Stable' README.md`; and the
   `typsphinx` stanza of `uv.lock` via `grep -A1 -xF 'name = "typsphinx"' uv.lock`. Key
   `PYPROJECT_VERSION_LINES = ` the count of `^version = ` lines in `pyproject.toml`, which must be
   1 — that is what makes it the sole hand-edited literal.

3. **Edit `pyproject.toml`'s `[project].version`** from `0.9.2` to `0.9.6`. This is the only
   hand-typed version literal in the whole task.

4. **Regenerate and prove the lock.** Record, each with its exit:
   - `sha256sum uv.lock` before, then `uv lock`, then `sha256sum uv.lock` after. Key `UV_LOCK_EXIT`.
   - `uv lock` a second time and `sha256sum uv.lock` again. Key `UV_LOCK_IDEMPOTENT = yes` only when
     the two post-regeneration digests are equal. This is the idempotency reading the plan owes.
   - `uv lock --check`, key `UV_LOCK_CHECK_EXIT`, which must be 0.
   - `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --locked`, key
     `UV_SYNC_LOCKED_EXIT`, which must be 0.
   - `git diff --numstat -- uv.lock` and, in a fenced block, every changed stanza name from
     `git diff -U0 -- uv.lock`. Key `UV_LOCK_TYPSPHINX_VERSION = ` read back from the regenerated
     file. If any stanza other than `typsphinx` moved, list each one by name and version delta — that
     is a measured outcome to record, not a failure, and `uv lock --check` plus CI are what validate
     it.

5. **Edit `README.md`'s Status line** so it reads `**Status**: Stable (v0.9.6) - Production ready`,
   keeping the wording `tests/test_readme_version_sync.py`'s regex requires.

6. **`## After`.** The same three reads, plus
   `uv run --no-sync python -c "import typsphinx; print(typsphinx.__version__)"`, key
   `IMPORTED_VERSION`, which must be `0.9.6`. Present § Before and § After as one
   Surface / Before / After table.

7. **`## Version-sync gates`.** Run
   `uv run pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q -rs -p no:cacheprovider`
   and record the summary line verbatim and every `SKIPPED` line. Keys `READMESYNC_PREVIEW_EXIT`,
   which must be 0, and `PREVIEW_PACKAGE_COUNT` from
   `grep -ho '@preview/[a-z-]*:' typsphinx/templates/base.typ | sort -u | wc -l`, which must be 4.

8. Stage and commit **only** `75-BUMP-EVIDENCE.md`, by explicit path. The three product edits stay
   uncommitted for Task 3's single five-file commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-BUMP-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_75_03)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; [ "$(grep -c '^version = ' pyproject.toml)" = 1 ] &amp;&amp; [ "$(k PYPROJECT_VERSION_LINES)" = 1 ] &amp;&amp; [ "$(grep -m1 '^version = ' pyproject.toml)" = 'version = "0.9.6"' ] &amp;&amp; [ "$(grep -c '^\*\*Status\*\*: Stable (v0\.9\.6) - Production ready$' README.md)" = 1 ] &amp;&amp; [ "$(grep -c '^\*\*Status\*\*: Stable (v0\.9\.2)' README.md)" = 0 ] &amp;&amp; UVV="$(grep -A1 -xF 'name = "typsphinx"' uv.lock | sed -n 's/^version = "\(.*\)"$/\1/p')" &amp;&amp; [ "$UVV" = 0.9.6 ] &amp;&amp; [ "$(k UV_LOCK_TYPSPHINX_VERSION)" = 0.9.6 ] &amp;&amp; [ "$(k UV_LOCK_EXIT)" = 0 ] &amp;&amp; [ "$(k UV_LOCK_CHECK_EXIT)" = 0 ] &amp;&amp; [ "$(k UV_SYNC_LOCKED_EXIT)" = 0 ] &amp;&amp; [ "$(k UV_LOCK_IDEMPOTENT)" = yes ] &amp;&amp; uv lock --check &amp;&amp; [ "$(uv run --no-sync python -c 'import typsphinx;print(typsphinx.__version__)')" = 0.9.6 ] &amp;&amp; [ "$(k IMPORTED_VERSION)" = 0.9.6 ] &amp;&amp; uv run pytest tests/test_readme_version_sync.py tests/test_preview_version_sync.py -q -rs -p no:cacheprovider &amp;&amp; [ "$(k READMESYNC_PREVIEW_EXIT)" = 0 ] &amp;&amp; [ "$(grep -ho '@preview/[a-z-]*:' typsphinx/templates/base.typ | sort -u | wc -l)" = 4 ] &amp;&amp; [ "$(k PREVIEW_PACKAGE_COUNT)" = 4 ] &amp;&amp; WD="$(git diff --name-only "$B" -- . ':(exclude).planning')" &amp;&amp; for f in pyproject.toml uv.lock README.md; do printf '%s\n' "$WD" | grep -qxF "$f" || { echo "not modified: $f"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" -- typsphinx .github flake.nix tox.ini)" ] &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; for s in '## Head check and provisioning' '## Before' '## After' '## Version-sync gates'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `not modified: FILE` or `missing section: NAME` printed. Each of these makes it fail: not in a worktree, or the shim absent; `pyproject.toml` carrying other than exactly one `^version = ` line, or that line not reading `version = "0.9.6"`; the README Status line not present in its 0.9.6 form, or a 0.9.2 Status line still present; the `uv.lock` `typsphinx` stanza not reading `0.9.6`, live or as recorded; any of the recorded lock, lock-check or locked-sync exits non-zero; `UV_LOCK_IDEMPOTENT` not `yes`; a live `uv lock --check` exiting non-zero, which means the lock is not derivable from the committed `pyproject.toml`; the installed package importing a version other than `0.9.6`; the live readme-sync or preview-sync run failing; the live `@preview` package count not 4; any of the three version surfaces absent from the working-tree diff; anything under `typsphinx/`, `.github/`, `flake.nix` or `tox.ini` changed; `.planning/REQUIREMENTS.md` changed; a named section missing; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` has exactly one `^version = ` line and it reads `version = "0.9.6"`.
    - `README.md` carries exactly one `**Status**: Stable (v0.9.6) - Production ready` line and no 0.9.2 Status line.
    - `uv.lock`'s `typsphinx` stanza reads `0.9.6`, `uv lock --check` exits 0, `uv sync --extra dev --extra docs --locked` exited 0, and `UV_LOCK_IDEMPOTENT = yes` from two consecutive regenerations.
    - `uv run --no-sync python -c "import typsphinx; print(typsphinx.__version__)"` prints `0.9.6`.
    - `tests/test_readme_version_sync.py` and `tests/test_preview_version_sync.py` pass, and `typsphinx/templates/base.typ` still declares exactly four `@preview` packages.
    - The three product edits are present in the working tree and uncommitted; only `75-BUMP-EVIDENCE.md` was committed.
  </acceptance_criteria>
  <done>The version literal has moved to 0.9.6 across every surface that carries it, the lock is regenerated and provably derivable, and the two gates that guard the bump are green.</done>
</task>

<task type="auto">
  <name>Task 2: Curate the `## [0.9.6]` section, the fresh `## [Unreleased]`, the `### Known Limitations` branch and the tail link block, then append `"0.9.6"` to RELEASE_VERSIONS and take the zero-skip reading</name>
  <precondition>Task 1's three product edits are present and uncommitted, and `75-BUMP-EVIDENCE.md` records `BASE_75_03`.</precondition>
  <files>CHANGELOG.md, tests/test_changelog_page_gate.py, .planning/phases/75-v0-9-6-release-prep-prep-only/75-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - CHANGELOG.md lines 1 to 130 (the current `## [Unreleased]` section with its six bullets, the `### Planned for Future Releases` scratch block, and the whole `## [0.9.2]` section as the lead-paragraph, bullet-register and `### Verified` model)
    - CHANGELOG.md around the existing `### Known Limitations` heading in `[0.1.0b1]` (the bold-lead / sub-bullet / `Workaround:` register D-11 follows) and the tail link block at the file's end
    - .planning/todos/pending/2026-08-14-numref-number-diverges-per-master-and-vanishes-for-non-root-only-figures.md (the authoritative source for NUM-01's precondition, both symptoms and the corrected one-warning finding)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-RESEARCH.md § "Pattern 4" (the draft `### Known Limitations` entry to adapt, not copy) and § "Anti-Patterns to Avoid"
    - .planning/phases/74-the-doctest-block-handler-its-real-compile-gate-and-the-docs/74-CONTEXT.md D-01, D-02 and D-07 (the measured basis for the doctest bullet's `python`-fence sentence and the QUA-14 bullet)
    - tests/test_changelog_page_gate.py (the `RELEASE_VERSIONS` tuple and the skip conditions)
  </read_first>
  <action>
Create `75-CHANGELOG-EVIDENCE.md`, headed `# Phase 75 — CHANGELOG Evidence (SC2, SC3)`, and edit
`CHANGELOG.md` and `tests/test_changelog_page_gate.py`.

1. **`## Base census`.** Before editing, from `git show "$B:CHANGELOG.md"` where `B` is
   `BASE_75_03`, record:
   - `CARRIED_BULLETS_BASE = `, the count of `^- \*\*` lines in the `## [Unreleased]` section body
     (the `awk` scan from `^## \[Unreleased\]` to the line before the next `^## \[`). SC2 says the
     six are counted at the base, not assumed to be six; record what you count.
   - `BASE_SUBSECTIONS = `, the `|`-joined list of that section's `^### ` headings.
   - `HEADINGS_BEFORE = ` from `grep -cE '^## \['` and `LINKREFS_BEFORE = ` from
     `grep -cE '^\[[^]]+\]: https'`.
   - `KNOWN_LIMITATIONS_BEFORE = ` from the **anchored** `grep -c '^### Known Limitations'`. Record
     in one sentence that the unanchored form already counts a prose mention elsewhere in the file,
     which is why every check here is anchored.
   - `CARRIED_ADDED_SHA`, `CARRIED_CHANGED_SHA` and `CARRIED_FIXED_SHA`: the SHA-256 of each
     subsection's body with blank lines dropped, so the digests survive the re-indentation-free move
     intact.

2. **Curate `CHANGELOG.md`.** Leave every carried bullet byte-identical — no re-wrap, no re-word, no
   re-order, no re-assignment. Produce, in this order:
   - A fresh `## [Unreleased]` heading holding **only** the existing `### Planned for Future Releases`
     block with its five items, unchanged and not promoted.
   - `## [0.9.6] - <today>`, where `<today>` is `date -u +%F` taken at execution. Per D-05 this is the
     prep authoring date, is fixed, and is not corrected if the tag lands later.
   - A **lead paragraph** (D-02, D-03) in the register `[0.9.2]`'s opening uses: the doctest
     rendering fix is the release's subject; the contributor tooling is acknowledged in one sentence
     as a side note; the upgrade recommendation is explicit and written against **0.9.2** as the
     reader's previous release, because `0.9.3`, `0.9.4` and `0.9.5` are permanently unclaimed on
     PyPI; and the justification given is that the pre-fix failure was not merely cosmetic — a
     doctest block followed by further content in the same container aborted the Typst compile with
     `expected semicolon or line break`.
   - `### Added`, `### Changed` and `### Fixed` carrying the promoted bullets in their existing
     assignment, with the two new bullets first in `### Fixed` (D-01):
     - the **doctest bullet** (D-06, D-07, D-08), trailing `(TRN-01, TRN-02)`: lead claim that a
       `>>>` example now renders as a Typst code block with its line structure intact; a second
       sentence recording that where further content followed the block in the same container the
       PDF compile itself failed; one sentence disclosing that the whole block is highlighted as
       Python, so output lines are coloured as Python source rather than as console output; and one
       evidence sentence pointing at typsphinx's own published API reference as the worked example.
       Do **not** mention the honour-an-existing-language path — standard Sphinx never sets a
       language on a doctest block, so no ordinary build reaches it.
     - the **QUA-14 bullet** (D-09), trailing `(QUA-14)`, second in `### Fixed`: typsphinx's own
       docstrings no longer raise docutils `Unexpected indentation` or
       `Block quote ends without a blank line` errors when the package is autodoc'd. Name no warning
       count and no build number.
     - Neither new bullet carries the "no effect on installing or using typsphinx" sentence the five
       carried tooling bullets use; these two are the release's user-visible content.
   - `### Known Limitations` (D-10, D-11, SC3), holding **one** entry and nothing else: NUM-01, in
     the bold-lead / prose / `Workaround:` register the `[0.1.0b1]` section established. It states
     the precondition (only a `typst_documents` configuration declaring more than one master; a
     single-master project is unaffected, and the entry says so); symptom (a), a figure reachable
     from two masters getting one baked-in Sphinx number in the reference text while Typst counts
     captions independently per compiled wrapper, so the same reference reads correctly in one
     master's PDF and points at the wrong number in the other's, with nothing reporting the
     divergence; symptom (b), a figure reachable only from a non-root master never entering the
     root-document figure-number scan, so the reference falls back to the raw label text, with
     exactly one Sphinx warning naming the label — a build-log reader gets a diagnostic even though
     the PDF reader does not; and the honest workaround, a single-master configuration or `:ref:` in
     place of `:numref:` for the affected figures. It promises no fix and names no version.
   - `### Verified` (D-04), four bullets: zero new runtime dependencies with the `dev` extra's
     return to `tox-uv` named as the one change across the measured `v0.9.2..HEAD` diff; the four
     `@preview` packages unchanged across the three sync surfaces `writer.py`,
     `template_engine.py` and `templates/base.typ`; the GATE-01 real-compile coverage behind the
     doctest handler; and the clean-build warning ledger. Do not write the blanket dependency
     sentence `[0.9.2]` used — the `dev` extra measurably did change.
   - **Tail link block:** insert `[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6`
     on the line immediately above the `[0.9.2]` link, and re-point `[Unreleased]`'s compare base
     from `v0.9.2` to `v0.9.6`. Create no `## [0.9.3]`, `## [0.9.4]` or `## [0.9.5]` heading and no
     matching tail link.

3. **Append `"0.9.6"`** to `tests/test_changelog_page_gate.py`'s `RELEASE_VERSIONS` tuple, in the
   same plain-string style the existing entries use, after `"0.9.2"`, reordering nothing.

4. **`## New section census`.** Record, each with its command: the `|`-joined `^### ` heading list
   inside `## [0.9.6]`; the `^- \*\*` bullet count per subsection; `HEADINGS_AFTER`,
   `LINKREFS_AFTER` and the anchored `KNOWN_LIMITATIONS_AFTER`; the `^## \[0\.9\.6\]` heading line
   number and the `^## \[Unreleased\]` heading line number, the latter smaller; `RELEASE_HEADING_DATE = `;
   the two tail-link lines with their line numbers; and the recomputed carried digests beside the
   base ones.

5. **`## REL-16: which branch was taken`.** State explicitly that the branch taken is the
   `### Known Limitations` section, not the declination; that its candidate set is NUM-01 alone; and
   why — quoting the measured evidence that the converted-image rehome collision closed in v0.8.0
   (`typsphinx/builder.py`'s reserved image namespace constant, with its todo in
   `.planning/todos/completed/`) and that the `typst_documents` duplicate-target cluster closed with
   the pre-write output-path collision validator in the same release. Record that WR-02 and WR-03
   are genuinely open but are not named by REL-16 and are therefore not added, and that D-12's
   anchored grep expects two `### Known Limitations` headings after this phase. Key
   `REL16_BRANCH = known-limitations-section`.

6. **`## Zero-skip changelog page gate`.** Run
   `uv run pytest tests/test_changelog_page_gate.py -q -rs -p no:cacheprovider` and record the
   summary line verbatim plus every `SKIPPED` line. Keys `CHANGELOG_GATE_EXIT`, which must be 0, and
   `CHANGELOG_GATE_SKIPS`, the count of `SKIPPED` lines naming that file, which must be 0. If it is
   not 0, the `docs` extra is missing from this environment — re-provision with
   `--extra dev --extra docs` and re-run; never accept the skipped reading.

7. Stage and commit **only** `75-CHANGELOG-EVIDENCE.md`, by explicit path. `CHANGELOG.md` and
   `tests/test_changelog_page_gate.py` stay uncommitted for Task 3.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-CHANGELOG-EVIDENCE.md" &amp;&amp; BF="$D/75-BUMP-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k BASE_75_03 "$BF")" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; blk() { awk -v h="$1" '$0==h{f=1;next} f&amp;&amp;/^### /{exit} f' | grep -v '^$'; } &amp;&amp; OLD="$(git show "$B:CHANGELOG.md" | awk '/^## \[/{if(f)exit} f; /^## \[Unreleased\]$/{f=1}')" &amp;&amp; NEW="$(awk '/^## \[/{if(f)exit} f; /^## \[0\.9\.6\]/{f=1}' CHANGELOG.md)" &amp;&amp; FRESH="$(awk '/^## \[/{if(f)exit} f; /^## \[Unreleased\]$/{f=1}' CHANGELOG.md)" &amp;&amp; [ -n "$OLD" ] &amp;&amp; [ -n "$NEW" ] &amp;&amp; [ -n "$FRESH" ] &amp;&amp; OB="$(printf '%s\n' "$OLD" | grep -cE '^- \*\*')" &amp;&amp; [ "$OB" -ge 1 ] &amp;&amp; [ "$OB" = "$(k CARRIED_BULLETS_BASE)" ] &amp;&amp; OA="$(printf '%s\n' "$OLD" | blk '### Added')" &amp;&amp; NA="$(printf '%s\n' "$NEW" | blk '### Added')" &amp;&amp; [ -n "$OA" ] &amp;&amp; [ "$(printf '%s\n' "$OA" | sha256sum)" = "$(printf '%s\n' "$NA" | sha256sum)" ] &amp;&amp; OC="$(printf '%s\n' "$OLD" | blk '### Changed')" &amp;&amp; NC="$(printf '%s\n' "$NEW" | blk '### Changed')" &amp;&amp; [ -n "$OC" ] &amp;&amp; [ "$(printf '%s\n' "$OC" | sha256sum)" = "$(printf '%s\n' "$NC" | sha256sum)" ] &amp;&amp; OFX="$(printf '%s\n' "$OLD" | blk '### Fixed')" &amp;&amp; NFX="$(printf '%s\n' "$NEW" | blk '### Fixed')" &amp;&amp; [ -n "$OFX" ] &amp;&amp; NL="$(printf '%s\n' "$OFX" | wc -l)" &amp;&amp; [ "$(printf '%s\n' "$NFX" | tail -n "$NL" | sha256sum)" = "$(printf '%s\n' "$OFX" | sha256sum)" ] &amp;&amp; [ "$(printf '%s\n' "$NEW" | grep -E '^### ' | tr '\n' '|')" = '### Added|### Changed|### Fixed|### Known Limitations|### Verified|' ] &amp;&amp; [ "$(printf '%s\n' "$NA" | grep -cE '^- \*\*')" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$NC" | grep -cE '^- \*\*')" = 4 ] &amp;&amp; [ "$(printf '%s\n' "$NFX" | grep -cE '^- \*\*')" = 3 ] &amp;&amp; NKL="$(printf '%s\n' "$NEW" | blk '### Known Limitations')" &amp;&amp; [ "$(printf '%s\n' "$NKL" | grep -cE '^- \*\*')" = 1 ] &amp;&amp; printf '%s\n' "$NKL" | grep -qF 'NUM-01' &amp;&amp; printf '%s\n' "$NKL" | grep -qF 'Workaround:' &amp;&amp; printf '%s\n' "$NKL" | grep -qF 'numref' &amp;&amp; [ "$(printf '%s\n' "$NKL" | grep -ciE 'rehome|duplicate-target|WR-02|WR-03')" = 0 ] &amp;&amp; NV="$(printf '%s\n' "$NEW" | blk '### Verified')" &amp;&amp; [ "$(printf '%s\n' "$NV" | grep -cE '^- ')" -ge 4 ] &amp;&amp; [ "$(printf '%s\n' "$NV" | grep -cF 'Zero new runtime or dev dependencies')" = 0 ] &amp;&amp; printf '%s\n' "$NFX" | grep -qF '(TRN-01, TRN-02)' &amp;&amp; printf '%s\n' "$NFX" | grep -qF '(QUA-14)' &amp;&amp; T1="$(printf '%s\n' "$NFX" | grep -nF 'TRN-01' | head -n1 | cut -d: -f1)" &amp;&amp; Q1="$(printf '%s\n' "$NFX" | grep -nF 'QUA-14' | head -n1 | cut -d: -f1)" &amp;&amp; P1="$(printf '%s\n' "$NFX" | grep -nE '^- \*\*' | sed -n 1p | cut -d: -f1)" &amp;&amp; P2="$(printf '%s\n' "$NFX" | grep -nE '^- \*\*' | sed -n 2p | cut -d: -f1)" &amp;&amp; P3="$(printf '%s\n' "$NFX" | grep -nE '^- \*\*' | sed -n 3p | cut -d: -f1)" &amp;&amp; [ "$P1" -le "$T1" ] &amp;&amp; [ "$T1" -lt "$P2" ] &amp;&amp; [ "$P2" -le "$Q1" ] &amp;&amp; [ "$Q1" -lt "$P3" ] &amp;&amp; [ "$(printf '%s\n' "$FRESH" | grep -E '^### ' | tr '\n' '|')" = '### Planned for Future Releases|' ] &amp;&amp; [ "$(printf '%s\n' "$FRESH" | grep -cE '^- \*\*')" = 0 ] &amp;&amp; UL="$(grep -n '^## \[Unreleased\]$' CHANGELOG.md | cut -d: -f1)" &amp;&amp; RL="$(grep -n '^## \[0\.9\.6\]' CHANGELOG.md | cut -d: -f1)" &amp;&amp; [ "$(printf '%s\n' "$UL" | wc -l)" = 1 ] &amp;&amp; [ "$(printf '%s\n' "$RL" | wc -l)" = 1 ] &amp;&amp; [ "$UL" -lt "$RL" ] &amp;&amp; [ "$(grep -cE '^## \[0\.9\.6\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md)" = 1 ] &amp;&amp; [ -n "$(k RELEASE_HEADING_DATE)" ] &amp;&amp; [ "$(grep -c '^### Known Limitations$' CHANGELOG.md)" = 2 ] &amp;&amp; [ "$(k KNOWN_LIMITATIONS_AFTER)" = 2 ] &amp;&amp; [ "$(k REL16_BRANCH)" = known-limitations-section ] &amp;&amp; grep -qxF '## REL-16: which branch was taken' "$F" &amp;&amp; [ "$(grep -cE '^## \[0\.9\.[345]\]' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(grep -cE '^\[0\.9\.[345]\]: ' CHANGELOG.md)" = 0 ] &amp;&amp; [ "$(grep -cxF '[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6' CHANGELOG.md)" = 1 ] &amp;&amp; L6="$(grep -nxF '[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6' CHANGELOG.md | cut -d: -f1)" &amp;&amp; L2="$(grep -nxF '[0.9.2]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.2' CHANGELOG.md | cut -d: -f1)" &amp;&amp; [ "$((L6 + 1))" = "$L2" ] &amp;&amp; [ "$(grep -cxF '[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.6...HEAD' CHANGELOG.md)" = 1 ] &amp;&amp; [ "$(grep -cE '^\[Unreleased\]: .*compare/v0\.9\.2\.\.\.HEAD$' CHANGELOG.md)" = 0 ] &amp;&amp; HA="$(grep -cE '^## \[' CHANGELOG.md)" &amp;&amp; LA="$(grep -cE '^\[[^]]+\]: https' CHANGELOG.md)" &amp;&amp; [ "$HA" = "$(( $(git show "$B:CHANGELOG.md" | grep -cE '^## \[') + 1 ))" ] &amp;&amp; [ "$LA" = "$(( $(git show "$B:CHANGELOG.md" | grep -cE '^\[[^]]+\]: https') + 1 ))" ] &amp;&amp; [ "$HA" = "$(k HEADINGS_AFTER)" ] &amp;&amp; [ "$LA" = "$(k LINKREFS_AFTER)" ] &amp;&amp; grep -qE '^\s*"0\.9\.6",' tests/test_changelog_page_gate.py &amp;&amp; [ "$(grep -cE '^\s*"0\.9\.2",' tests/test_changelog_page_gate.py)" = 1 ] &amp;&amp; O="$(uv run pytest tests/test_changelog_page_gate.py -q -rs -p no:cacheprovider; echo "rc:$?")" &amp;&amp; printf '%s\n' "$O" | grep -qx 'rc:0' &amp;&amp; printf '%s\n' "$O" | grep -qE '[0-9]+ passed' &amp;&amp; ! printf '%s\n' "$O" | grep -qE '^SKIPPED .*test_changelog_page_gate' &amp;&amp; [ "$(k CHANGELOG_GATE_EXIT)" = 0 ] &amp;&amp; [ "$(k CHANGELOG_GATE_SKIPS)" = 0 ] &amp;&amp; [ -z "$(git diff --name-only "$B" -- typsphinx .github flake.nix tox.ini docs)" ] &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; for s in '## Base census' '## New section census' '## REL-16: which branch was taken' '## Zero-skip changelog page gate'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail: not in a worktree, or the shim absent; `BASE_75_03` unreadable from the bump evidence; either the base `## [Unreleased]` body, the new `## [0.9.6]` body or the fresh `## [Unreleased]` body extracting empty; the recorded base bullet count disagreeing with the live recount, or the recount being zero; the promoted `### Added` or `### Changed` body digest differing from the base's; the base `### Fixed` body not being the byte-identical tail of the new `### Fixed`; the `###` heading order inside `## [0.9.6]` not reading Added, Changed, Fixed, Known Limitations, Verified; the per-subsection bullet counts not 1, 4 and 3; `### Known Limitations` not holding exactly one bold-lead bullet, or that entry not naming NUM-01, not carrying a `Workaround:` line, not mentioning `numref`, or mentioning the rehome collision, the duplicate-target cluster, WR-02 or WR-03; `### Verified` carrying fewer than four bullets or reproducing the blanket dependency sentence; the doctest bullet not first in `### Fixed` or the QUA-14 bullet not second, or either requirement-ID parenthesis absent; the fresh `## [Unreleased]` carrying anything but the scratch subsection, or carrying a bold-lead bullet; more or fewer than one `## [Unreleased]` or one `## [0.9.6]` heading, or the released heading not sitting below the unreleased one; the released heading not carrying an ISO date; the anchored `### Known Limitations` count not exactly 2, live or recorded; `REL16_BRANCH` not `known-limitations-section`, or its evidence section missing; any `## [0.9.3]`, `## [0.9.4]` or `## [0.9.5]` heading or tail link present; the `[0.9.6]` tail link absent, duplicated, or not immediately above the `[0.9.2]` link; the `[Unreleased]` compare base not moved to `v0.9.6`, or the old compare line still present; the heading or link-ref count not exactly one greater than the base's, or disagreeing with its recorded key; `"0.9.6"` absent from `RELEASE_VERSIONS` or `"0.9.2"` no longer present exactly once; the live changelog page gate exiting non-zero, reporting no passed tests, or reporting any skip in that file; either recorded gate key non-zero; anything under `typsphinx/`, `.github/`, `flake.nix`, `tox.ini` or `docs/` changed; `.planning/REQUIREMENTS.md` changed; a named section missing; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - The promoted `### Added` and `### Changed` bodies are byte-identical to the phase base's, and the base's `### Fixed` body is the byte-identical tail of the new one.
    - `## [0.9.6]`'s subsections read `### Added`, `### Changed`, `### Fixed`, `### Known Limitations`, `### Verified` in that order, with 1, 4 and 3 bold-lead bullets in the first three.
    - `### Known Limitations` holds exactly one entry, naming NUM-01 with a `Workaround:` line, and mentions neither the rehome collision, the duplicate-target cluster, WR-02 nor WR-03.
    - `grep -c '^### Known Limitations$' CHANGELOG.md` is 2, and `REL16_BRANCH = known-limitations-section` with its reasoning recorded.
    - Exactly one `## [Unreleased]` heading exists, above `## [0.9.6]`, holding only `### Planned for Future Releases` and no bold-lead bullet.
    - The tail block carries `[0.9.6]: …/releases/tag/v0.9.6` on the line immediately above `[0.9.2]`, `[Unreleased]` compares against `v0.9.6`, and no `## [0.9.3]`, `## [0.9.4]` or `## [0.9.5]` heading or link exists.
    - `RELEASE_VERSIONS` gains `"0.9.6"`, and `uv run pytest tests/test_changelog_page_gate.py -q -rs` exits 0 with zero skips.
  </acceptance_criteria>
  <done>`CHANGELOG.md` carries one curated `## [0.9.6]` section with the carried bullets provably unchanged, REL-16 settled by a NUM-01-only `### Known Limitations` section, the tail link block moved, and the page gate green with no skips.</done>
</task>

<task type="auto">
  <name>Task 3: Land all five files in one commit, then execute the extractor and transcribe its stdout</name>
  <precondition>Tasks 1 and 2 left exactly five product files modified and uncommitted, and both evidence files are committed.</precondition>
  <reversibility rating="reversible">A single local commit on an unpushed branch; no tag, no remote write.</reversibility>
  <files>pyproject.toml, uv.lock, README.md, CHANGELOG.md, tests/test_changelog_page_gate.py, .planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md, .planning/phases/75-v0-9-6-release-prep-prep-only/75-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md (`BASE_75_03`, the surfaces already edited)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CHANGELOG-EVIDENCE.md (the section census this task extends)
    - scripts/extract_changelog_section.py (the positional algorithm and the body-only, stripped output shape — execute it, never reimplement it)
    - .planning/ROADMAP.md § "Phase 75" SC1 and SC2 (the literal acceptance wording this commit is checked against)
  </read_first>
  <action>
1. **`## The one commit`** in `75-BUMP-EVIDENCE.md`.
   - Record `git status --porcelain -- . ':(exclude).planning'` first: it must list exactly the five
     files and nothing else. If it lists anything more, write `## HALT: unexpected product change`
     and stop rather than committing a superset.
   - Stage by explicit path:
     `git add pyproject.toml uv.lock README.md CHANGELOG.md tests/test_changelog_page_gate.py`.
     Never `git add -A` and never `git add .`.
   - Commit with a plain `git commit` whose subject names the release prep and the version.
   - Record `BUMP_COMMIT_SHA = ` from `git rev-parse HEAD` and the verbatim
     `git show --name-only --format='%H %s' HEAD` output. Key `BUMP_COMMIT_FILES = `, the
     `LC_ALL=C`-sorted, `|`-joined file list, which must be exactly
     `CHANGELOG.md|README.md|pyproject.toml|tests/test_changelog_page_gate.py|uv.lock`.
   - Record one sentence naming the AMENDED resolution this five-file union implements: the Phase
     Boundary and SC1 name one four-item set, the bump-mechanics bullet names a different four-item
     set, and the union is the only reading that satisfies both. Note that Phase 46's two-commit
     split is explicitly not the precedent here.

2. **`## Extractor transcript`** in `75-CHANGELOG-EVIDENCE.md`.
   - Run `uv run python scripts/extract_changelog_section.py 0.9.6 > "$S/p7503_release_notes.md" 2>"$S/p7503_extract.err"; echo "exit:$?"`.
     Keys `EXTRACT_EXIT`, which must be 0, and `EXTRACT_BYTES` from `wc -c`, which must be greater
     than 0.
   - Paste the stdout verbatim in a fenced block — this is the text that becomes the GitHub Release
     body, so the evidence must carry it, not describe it.
   - Key `EXTRACT_PLANNED_BLOCK = `, the count of `^### Planned for Future Releases` lines in the
     output, which must be 0, and `EXTRACT_VERSION_HEADINGS = `, the count of `^## \[` lines, which
     must be 0 — the extractor emits the body only and terminates at the next heading.
   - Key `EXTRACT_MATCHES_SECTION = yes` after showing that the output's SHA-256 equals the SHA-256
     of the `## [0.9.6]` section body read straight out of `CHANGELOG.md` with leading and trailing
     blank lines stripped. Record both digests.
   - Record `uv run pytest tests/test_changelog_extraction.py -q -p no:cacheprovider` and its exit as
     `EXTRACTION_TESTS_EXIT`, which must be 0: the committed extractor is still the one under test.

3. Commit both evidence files in a second, `.planning`-only commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; BF="$D/75-BUMP-EVIDENCE.md" &amp;&amp; CF="$D/75-CHANGELOG-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; kb() { sed -n "s/^$1 = //p" "$BF" | head -n 1; } &amp;&amp; kc() { sed -n "s/^$1 = //p" "$CF" | head -n 1; } &amp;&amp; B="$(kb BASE_75_03)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; C="$(kb BUMP_COMMIT_SHA)" &amp;&amp; [ "${#C}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$C" HEAD &amp;&amp; git merge-base --is-ancestor "$B" "$C" &amp;&amp; FL="$(git show --name-only --format= "$C" | grep -v '^$' | LC_ALL=C sort | paste -sd'|')" &amp;&amp; [ "$FL" = 'CHANGELOG.md|README.md|pyproject.toml|tests/test_changelog_page_gate.py|uv.lock' ] &amp;&amp; [ "$(kb BUMP_COMMIT_FILES)" = "$FL" ] &amp;&amp; [ -z "$(git status --porcelain -- . ':(exclude).planning')" ] &amp;&amp; [ "$(git show "$C:pyproject.toml" | grep -m1 '^version = ')" = 'version = "0.9.6"' ] &amp;&amp; [ "$(git show "$C:uv.lock" | grep -A1 -xF 'name = "typsphinx"' | sed -n 's/^version = "\(.*\)"$/\1/p')" = 0.9.6 ] &amp;&amp; git show "$C:README.md" | grep -qxF '**Status**: Stable (v0.9.6) - Production ready' &amp;&amp; git show "$C:CHANGELOG.md" | grep -qE '^## \[0\.9\.6\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' &amp;&amp; git show "$C:tests/test_changelog_page_gate.py" | grep -qE '^\s*"0\.9\.6",' &amp;&amp; S="$(kb SCRATCH_75_03)" &amp;&amp; [ -n "$S" ] &amp;&amp; RN="$S/p7503_release_notes.md" &amp;&amp; test -s "$RN" &amp;&amp; [ "$(kc EXTRACT_EXIT)" = 0 ] &amp;&amp; [ "$(kc EXTRACT_BYTES)" -gt 0 ] &amp;&amp; [ "$(kc EXTRACT_PLANNED_BLOCK)" = 0 ] &amp;&amp; [ "$(kc EXTRACT_VERSION_HEADINGS)" = 0 ] &amp;&amp; [ "$(kc EXTRACT_MATCHES_SECTION)" = yes ] &amp;&amp; [ "$(kc EXTRACTION_TESTS_EXIT)" = 0 ] &amp;&amp; X="$(uv run python scripts/extract_changelog_section.py 0.9.6)" &amp;&amp; [ -n "$X" ] &amp;&amp; [ "$(printf '%s\n' "$X" | wc -c)" -gt 1 ] &amp;&amp; [ "$(printf '%s\n' "$X" | grep -c '^### Planned for Future Releases')" = 0 ] &amp;&amp; [ "$(printf '%s\n' "$X" | grep -cE '^## \[')" = 0 ] &amp;&amp; printf '%s\n' "$X" | grep -qxF '### Known Limitations' &amp;&amp; printf '%s\n' "$X" | grep -qxF '### Verified' &amp;&amp; SEC="$(awk '/^## \[/{if(f)exit} f; /^## \[0\.9\.6\]/{f=1}' CHANGELOG.md | sed -e '/./,$!d' | tac | sed -e '/./,$!d' | tac)" &amp;&amp; [ "$(printf '%s\n' "$SEC" | sha256sum)" = "$(printf '%s\n' "$X" | sha256sum)" ] &amp;&amp; uv run pytest tests/test_changelog_extraction.py -q -p no:cacheprovider &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- typsphinx .github flake.nix tox.ini docs)" ] &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; grep -qxF '## The one commit' "$BF" &amp;&amp; grep -qxF '## Extractor transcript' "$CF" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$BF" &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$CF" &amp;&amp; ! grep -q '^## HALT' "$BF" &amp;&amp; ! grep -q '^## HALT' "$CF"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail: not in a worktree, or the shim absent; `BASE_75_03` or `BUMP_COMMIT_SHA` malformed, the commit not an ancestor of HEAD, or the base not an ancestor of the commit; the commit's file list not exactly the five sorted names, or disagreeing with `BUMP_COMMIT_FILES`; any product file still uncommitted; the committed `pyproject.toml`, `uv.lock` stanza, README Status line, `## [0.9.6]` dated heading or `RELEASE_VERSIONS` entry not carrying 0.9.6; the saved extractor stdout missing or empty; any recorded extractor key wrong; a live extractor run producing empty output, a `### Planned for Future Releases` line, any `## [` heading line, or missing the `### Known Limitations` or `### Verified` heading; the live output's digest differing from the `## [0.9.6]` section body read out of the file; `tests/test_changelog_extraction.py` failing; anything under `typsphinx/`, `.github/`, `flake.nix`, `tox.ini` or `docs/` changed since the plan base; `.planning/REQUIREMENTS.md` changed; either named section missing; a key line carrying an opening parenthesis; or a HALT heading in either evidence file.</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --name-only <BUMP_COMMIT_SHA>` lists exactly `CHANGELOG.md`, `README.md`, `pyproject.toml`, `tests/test_changelog_page_gate.py` and `uv.lock`, and the working tree has no remaining product change.
    - That commit's `pyproject.toml`, `uv.lock` `typsphinx` stanza, README Status line, `## [0.9.6]` dated heading and `RELEASE_VERSIONS` entry all read 0.9.6.
    - `uv run python scripts/extract_changelog_section.py 0.9.6` exits 0, prints a non-empty body carrying `### Known Limitations` and `### Verified`, carries no `## [` heading line and no `### Planned for Future Releases` line, and its stdout digest equals the `## [0.9.6]` section body's.
    - The stdout is transcribed verbatim into `75-CHANGELOG-EVIDENCE.md` § "Extractor transcript", and `tests/test_changelog_extraction.py` passes.
  </acceptance_criteria>
  <done>The release lands as one five-file commit, and the exact text that will become the GitHub Release body has been produced by the committed extractor and written into the phase evidence.</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide spec-less probe accounting lives in `75-01-PLAN.md` § "Spec-less probe accounting". This
plan authors four of the six rows: `Edge (adjacency)`, `Edge (empty)`, `Edge (ordering)` and
`Edge (idempotency)`, each a plain `must_haves.truths` string above.

**Row 6 restated here because this plan is where it bites.** The REL-16 edge row came back
`unclassified` with no predicate, and stays unresolved. The assumption this plan runs on is that the
AMENDED candidate-set correction is right: that "the carried major defects" REL-16 names reduces to
NUM-01 alone, because the converted-image rehome collision and the `typst_documents`
duplicate-target cluster were both measured closed in v0.8.0 and their todos are in
`.planning/todos/completed/`. Task 2 re-quotes that evidence rather than asserting it, but no command
in this plan can prove the owner would accept the reduced set — that was a discussion decision
(D-10), and it is carried as such. The corrective, if it is wrong, is an edit to
`### Known Limitations` before the tag is pushed.

**Assumption on `uv.lock`'s blast radius.** `uv lock` preserves existing pins unless the
requirements changed, so only the `typsphinx` stanza is expected to move. The plan does not assert
that: Task 1 enumerates every changed stanza in the evidence and lets `uv lock --check`, the locked
sync and CI validate whatever the resolver actually produced.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` → published documentation | `docs/source/changelog.rst` includes this file, so every bullet is published prose |
| `CHANGELOG.md` → GitHub Release body | `release.yml` reads the `## [0.9.6]` section through the extractor at tag time; whatever is in the tagged tree ships |
| `pyproject.toml` → `uv.lock` → installed distribution | a hand-edited lock would be underivable and could diverge from what CI resolves |
| hand-written release prose → reader's upgrade decision | an over-broad true-sounding claim has shipped from this repository before |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-09 | Repudiation | a release-notes claim that is not true of this milestone's measured diff | high | mitigate | D-04's corrected `### Verified` wording is required and the blanket dependency sentence is negative-checked out of the section; every invariance claim names the surface it was measured on |
| T-75-10 | Tampering | a carried bullet silently re-worded or re-ordered during promotion | high | mitigate | per-subsection SHA-256 digests taken at the phase base and re-compared after the move, with the carried `### Fixed` bullet checked as a byte-identical tail |
| T-75-11 | Repudiation | publishing an already-fixed defect as a current limitation | high | mitigate | `### Known Limitations` is negative-checked for the rehome collision, the duplicate-target cluster, WR-02 and WR-03, and holds exactly one bold-lead entry naming NUM-01 |
| T-75-12 | Tampering | a hand-edited `uv.lock` diverging from what the resolver produces | medium | mitigate | the lock is regenerated, run twice for idempotency, proved by `uv lock --check` and a `--locked` sync, and every changed stanza is enumerated |
| T-75-13 | Information Disclosure | the scratch `Planned for Future Releases` block leaking into the public release body | medium | mitigate | the extractor is executed and its stdout checked for zero occurrences of that heading and zero `## [` heading lines |
| T-75-14 | Denial of Service | a version bump landing alone and stalling every dependency PR, the shape that once broke them here | high | mitigate | the single commit is checked to carry the five-file union exactly, staged by explicit path, with `git add -A` forbidden |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list, identical in every plan of this phase. Every symbol below is created by Phase 75
and did not exist at the phase base.

**New files under `.planning/phases/75-v0-9-6-release-prep-prep-only/`:** `75-CLOSEOUT-GUARD.md`,
`75-BASE-EVIDENCE.md`, `75-SC5-INVARIANTS.md`, `75-LEFTOVERS-EVIDENCE.md`, `COVERAGE.md`,
`75-BUMP-EVIDENCE.md`, `75-CHANGELOG-EVIDENCE.md`, `75-GREEN-TREE-EVIDENCE.md`,
`75-PREFLIGHT-EVIDENCE.md`, `75-CI-EVIDENCE.md`, `75-HANDOFF.md`, plus one `75-0N-SUMMARY.md` per
plan.

**New file under `.planning/todos/pending/`:** a record for IN-01 (D-14), the
`visit_literal_block` / `depart_literal_block` `Args:` text that still names only the literal block
node.

**New headings and lines in `CHANGELOG.md`:** `## [0.9.6] - <prep date>` and, inside it,
`### Added`, `### Changed`, `### Fixed`, `### Known Limitations` and `### Verified`; a fresh
`## [Unreleased]` heading placed above it, retaining only `### Planned for Future Releases`; and a
new tail link line `[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6`.

**New value in `tests/test_changelog_page_gate.py`:** the `RELEASE_VERSIONS` tuple member `"0.9.6"`.

**Changed version literals (not new symbols):** `pyproject.toml`'s `[project].version`, the
`typsphinx` stanza's `version` in `uv.lock`, and `README.md`'s `**Status**: Stable (vX.Y.Z)` line,
all moving `0.9.2` to `0.9.6`.

**Deletions (no new symbol):** the five untracked repository-root files `probe_bogusxyz.typ`,
`probe_none.typ`, `probe_pycon.typ`, `probe_python.typ` and `probe_text.typ`.
</artifacts_this_phase_produces>

<verification>
- `git show --name-only` on the bump commit lists exactly the five files.
- Every version surface reads 0.9.6 in that commit, and `uv lock --check` exits 0.
- The carried subsections' digests survive the promotion unchanged, and the new section's heading
  order, bullet counts and requirement-ID parentheses are as specified.
- `grep -c '^### Known Limitations$' CHANGELOG.md` is 2; the new entry names NUM-01 only.
- The tail link block is moved and no `0.9.3`, `0.9.4` or `0.9.5` heading or link exists.
- The extractor runs, its stdout is non-empty, scratch-free and digest-equal to the section body,
  and it is transcribed verbatim into the evidence.
</verification>

<success_criteria>
- SC1 is met: one commit, five files, the sole hand-edited version literal in `pyproject.toml`, a
  regenerated lock, a green `--locked` sync and a zero-skip changelog page gate.
- SC2 is met: one curated `## [0.9.6]` section with byte-identical carried bullets, a fresh
  `## [Unreleased]` above it, the tail link block moved, and the extractor's stdout transcribed.
- SC3 is met: the `### Known Limitations` branch is taken, holds NUM-01 alone, and the anchored grep
  agrees with the decision record.
</success_criteria>

<output>
Create `.planning/phases/75-v0-9-6-release-prep-prep-only/75-03-SUMMARY.md` when done, declaring
`requirements-completed: []`.
</output>
