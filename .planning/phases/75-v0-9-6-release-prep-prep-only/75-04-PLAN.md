---
phase: 75-v0-9-6-release-prep-prep-only
plan: 04
type: execute
wave: 2
depends_on: ["75-01", "75-03"]
files_modified:
  - .planning/phases/75-v0-9-6-release-prep-prep-only/75-GREEN-TREE-EVIDENCE.md
autonomous: true
requirements: [REL-15]
# REL-15 is a coverage ID only (ROADMAP constraint 1 and the Phase 75 cross-cutting
# constraints): no plan closes it, no plan touches its checkbox, and it is checked at
# /gsd-complete-milestone against observed publish evidence.
requirements-completed: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'SC4 proven here, not inherited: every gate in this plan runs on the bumped, curated tip produced by 75-03, in this plan''s own environment, and Phase 74''s results are re-measured rather than quoted.'
    - 'SC4 local gates: `uv run ruff check .`, `uv run black --check .` and `uv run mypy typsphinx/` exit 0, and the full pytest suite passes twice — once plainly and once under `LC_ALL=C`, which is the locale CI runs in. Each run reports zero failed and zero errors, and every skip reason is transcribed.'
    - 'SC1 zero-skip re-proof: neither full pytest run skips any test in `tests/test_changelog_page_gate.py`, which is only possible in an environment carrying the `docs` extra.'
    - 'SC4 documentation: `tox -e docs-html` and `tox -e docs-pdf` each pass from a removed `docs/_build`, under `LANG=C LANGUAGE=C LC_ALL=C`, and their `build succeeded, N warnings.` integers have not risen above the clean baseline 75-01 recorded at this phase''s own base.'
    - 'SC4 re-proof of Phase 74 on the bumped tip: the tip `-b typstpdf` log carries zero `unknown node type` lines naming a doctest block and zero attributed docutils `Unexpected indentation` / `Block quote ends without a blank line` messages, each zero paired with the synthetic control fixture that yields a non-zero count under the same pattern.'
    - 'SC4 linkcheck: `tox -e linkcheck` passes on a clean run and its `output.json` reports a `working` count equal to its total, both recorded fresh. Network flakiness is bounded to at most three attempts, each recorded.'
    - 'No gate is made green by editing anything: this plan changes no product file, and a red gate is recorded verbatim with a NOT-MET verdict and escalated rather than worked around.'
  artifacts:
    - path: .planning/phases/75-v0-9-6-release-prep-prep-only/75-GREEN-TREE-EVIDENCE.md
      provides: 'the lint trio, both full pytest runs with their skip transcripts, the clean tip documentation builds with their warning ledger and message-class census, the linkcheck run, and SC4_LOCAL_VERDICT'
      contains: 'SC4_LOCAL_VERDICT = '
  key_links:
    - from: '75-BASE-EVIDENCE.md BASE_HTML_WARNINGS and BASE_PDF_WARNINGS'
      to: 'this plan''s tip warning comparison'
      via: 'read with sed from the committed wave-1 evidence file'
      pattern: 'BASE_HTML_WARNINGS = '
    - from: '75-GREEN-TREE-EVIDENCE.md SC4_LOCAL_VERDICT'
      to: '75-06 push gate'
      via: 'read with sed before the branch is pushed and CI dispatched'
      pattern: 'SC4_LOCAL_VERDICT = MET'
  prohibitions:
    - statement: 'MUST NOT edit, weaken, skip, xfail or delete any test, assertion, configuration key or source file to turn a red gate green; a red gate is recorded verbatim, the verdict set NOT-MET, and a blocking checkpoint returned.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT accept an incremental documentation build, a build outside the C locale, or a zero message-class count that has no positive control.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT edit any file under `typsphinx/`, `tests/`, `.github/`, `docs/`, `flake.nix`, `tox.ini`, `pyproject.toml` or `uv.lock`; this plan is measurement only.'
      requirement_id: REL-15
      category: values
      status: resolved
      verification: judgment
    - statement: 'MUST NOT edit, stage or commit `.planning/REQUIREMENTS.md`, and MUST NOT move REL-15''s checkbox.'
      requirement_id: REL-15
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Discharge SC4's local half on the bumped, curated tip, by runs executed here rather than on the
preceding phase's word:
- the lint trio and the full pytest suite twice, once under `LC_ALL=C`, with no changelog-page-gate
  skip
- clean `tox -e docs-html` and `tox -e docs-pdf` in the C locale, compared against the baseline
  75-01 recorded at this phase's own base, re-proving Phase 74's two zero counts on the changed tree
- a clean `tox -e linkcheck` with its `working` count equal to its total

Purpose: 75-06 pushes and dispatches CI only on a tip this plan has shown green locally, and a
version bump touches `pyproject.toml`, which the documentation build reads — so SC4's re-proof on
the changed tree is the point, not an optional extra.

Output: `75-GREEN-TREE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/75-v0-9-6-release-prep-prep-only/75-BASE-EVIDENCE.md
@.planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md
@tox.ini
@CLAUDE.md
</context>

<worktree_provisioning>
Provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` and
run every tool through `uv run`. The `docs` extra is mandatory: without it
`tests/test_changelog_page_gate.py` skips instead of running and SC1's zero-skip reading cannot be
taken here either.

**Tooling.** Bare `uv`, `tox`, `pytest`, `ruff`, `black`, `mypy`, `sphinx-build` through the PATH
shims. `.venv/bin/ruff` does not execute on this machine.

**Timeouts.** Run every documentation build, every full pytest run, the linkcheck run and every
verify block containing one with the Bash tool's `timeout` set to 600000, in the foreground. The
default two-minute limit truncates the suite and the typstpdf build.

**Locale.** tox 4 forwards `LANG` and `LANGUAGE` but not `LC_ALL`, so every counted build is invoked
as `LANG=C LANGUAGE=C LC_ALL=C uv run tox -e <env>`.

**Counts are recorded, not compared across environments.** A worktree `.venv` may be built on a
different interpreter than the main checkout's, so `pyvenv.cfg`'s `home` and `version_info` are
recorded before any count is written down, and this plan compares its numbers only against
`75-BASE-EVIDENCE.md`, which was measured in a sibling worktree of the same phase.

**Evidence key lines.** Bare values only, `KEY = value` on its own line, explanation on the next.

**Scratch.** `mktemp -d`, recorded as `SCRATCH_75_04`; helper prefix `p7504_`.

**Commits.** Plain `git commit`. This plan changes no product file. Never write
`75-VERIFICATION.md`.
</worktree_provisioning>

<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ^[A-Z][A-Z0-9_]* = .*[(] -->
<!-- planner-discipline-allow: Unexpected indentation -->
<!-- planner-discipline-allow: Block quote ends without a blank line -->
<!-- planner-discipline-allow: unknown node type -->
<!-- planner-discipline-allow: test_changelog_page_gate -->

## Planning-time census (2026-09-20; context only — re-measure, never trust)

| Fact | Value at planning |
|------|-------------------|
| Milestone audit tip re-measurement | `1569 passed, 1 skipped`; mypy clean on 9 source files; `358 files would be left unchanged`; `All checks passed!` |
| tox environments | `docs-html` builds `-b html source _build/html` from `docs/`; `docs-pdf` builds `-b typstpdf source _build/pdf`; `linkcheck` builds `-b linkcheck source _build/linkcheck`; all three use `extras = docs` |
| Phase 72 linkcheck reading | 95/95 links `working` on a clean run, no `conf.py` timing key |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: gate on the bump commit, then run the lint trio and the full pytest suite twice on the bumped tip</name>
  <precondition>`75-BUMP-EVIDENCE.md` records a `BUMP_COMMIT_SHA` that is an ancestor of this worktree's HEAD, and `75-CHANGELOG-EVIDENCE.md` records `EXTRACT_MATCHES_SECTION = yes`.</precondition>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-BUMP-EVIDENCE.md (`BUMP_COMMIT_SHA`, `BUMP_COMMIT_FILES`)
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-CHANGELOG-EVIDENCE.md (`EXTRACT_MATCHES_SECTION`, `CHANGELOG_GATE_SKIPS`)
    - .planning/ROADMAP.md § "Phase 75" SC4 and § "v0.9.6" constraint 11
    - CLAUDE.md § "Worktree-isolated execution" and § "NixOS development shell" (Locale; Interpreters may differ)
  </read_first>
  <action>
Create `75-GREEN-TREE-EVIDENCE.md`, headed `# Phase 75 — Green Tree Evidence (SC4 local half)`.

1. **`## Head check and provisioning`.** `date -u +%FT%TZ`, `pwd -P`, `test -f .git; echo "exit:$?"`,
   the shim check, the provisioning line with its exit, then the same line with `--locked` appended
   and its exit, which must be 0 — every CI job starts with a locked sync, so a stale lock would
   fail every lane before any signal. On a non-zero exit write `## HALT: lock not in sync` and stop;
   never regenerate the lock here. Record `PYVENV_HOME`, `PYVENV_VERSION_INFO`,
   `BASE_75_04 = ` from `git rev-parse HEAD` before any commit, and `SCRATCH_75_04 = `.

2. **`## Wave-1 gate`.** Quote `BUMP_COMMIT_SHA` and `BUMP_COMMIT_FILES` from `75-BUMP-EVIDENCE.md`
   and `EXTRACT_MATCHES_SECTION` from `75-CHANGELOG-EVIDENCE.md`. Record
   `git merge-base --is-ancestor <BUMP_COMMIT_SHA> HEAD; echo "exit:$?"`, which must print `exit:0`,
   and `sed -n 7p pyproject.toml`, which must read `version = "0.9.6"`. If any of these does not
   hold, write `## HALT: wave 1 not landed` and stop — every measurement below would otherwise be
   taken on the wrong tree.

3. **`## Lint trio`.** Run `uv run ruff check .`, `uv run black --check .` and
   `uv run mypy typsphinx/`, each with its output and exit. Keys `RUFF_EXIT`, `BLACK_EXIT`,
   `MYPY_EXIT`. Lint is the gate this project has seen fail only in CI, so it is run here first.

4. **`## Full pytest`.**
   - `uv run pytest -rs -p no:cacheprovider` with stdout and stderr redirected into
     `$S/p7504_pytest.txt`, then `echo "exit:$?"`.
   - `LANG=C LC_ALL=C uv run pytest -rs -p no:cacheprovider` into `$S/p7504_pytest-c.txt`. The
     C-locale run is the one matching CI's English locale.
   - For each run record the final summary line verbatim and every `SKIPPED` line of the `-rs`
     summary verbatim.
   - Keys `FULL_PYTEST_EXIT`, `FULL_PYTEST_C_EXIT`, `FULL_PYTEST_PASSED`, `FULL_PYTEST_C_PASSED`,
     `FULL_PYTEST_FAILED` and `FULL_PYTEST_C_FAILED` (0 when no failure line appears),
     `FULL_PYTEST_ERRORS` (0 when no error line appears in either run), and `CHANGELOG_GATE_SKIPS`,
     the number of `SKIPPED` lines naming `tests/test_changelog_page_gate.py` across both runs,
     which must be 0.
   - Record the interpreter beside the counts and state that these numbers are recorded, not
     compared with the main checkout's.

5. **`## @preview invariant`.** `uv run pytest tests/test_preview_version_sync.py -v -p no:cacheprovider`,
   key `PREVIEW_SYNC_EXIT`; `PREVIEW_PACKAGE_COUNT` from
   `grep -ho '@preview/[a-z-]*:' typsphinx/templates/base.typ | sort -u | wc -l`, which must be 4;
   and `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b HEAD -- typsphinx/templates typsphinx/writer.py typsphinx/template_engine.py`,
   which must be empty — the three sync surfaces are untouched across the whole milestone.

6. Commit. If any exit is non-zero, record it verbatim, carry it into Task 3's verdict as NOT-MET,
   and return a blocking checkpoint naming the failing gate. Never edit anything to make it green.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-GREEN-TREE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; C="$(k BUMP_COMMIT_SHA "$D/75-BUMP-EVIDENCE.md")" &amp;&amp; [ "${#C}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$C" HEAD &amp;&amp; [ "$(k EXTRACT_MATCHES_SECTION "$D/75-CHANGELOG-EVIDENCE.md")" = yes ] &amp;&amp; [ "$(sed -n 7p pyproject.toml)" = 'version = "0.9.6"' ] &amp;&amp; B="$(k BASE_75_04)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; uv lock --check &amp;&amp; uv run ruff check . &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &amp;&amp; O="$(LANG=C LC_ALL=C uv run pytest -q -rs -p no:cacheprovider; echo "rc:$?")" &amp;&amp; printf '%s\n' "$O" | grep -qx 'rc:0' &amp;&amp; printf '%s\n' "$O" | grep -qE '[0-9]+ passed' &amp;&amp; ! printf '%s\n' "$O" | grep -qE '[0-9]+ (failed|errors?)\b' &amp;&amp; ! printf '%s\n' "$O" | grep -qE '^SKIPPED .*test_changelog_page_gate' &amp;&amp; uv run pytest tests/test_preview_version_sync.py -q -p no:cacheprovider &amp;&amp; [ "$(grep -ho '@preview/[a-z-]*:' typsphinx/templates/base.typ | sort -u | wc -l)" = 4 ] &amp;&amp; [ -z "$(git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b HEAD -- typsphinx/templates typsphinx/writer.py typsphinx/template_engine.py)" ] &amp;&amp; S="$(k SCRATCH_75_04)" &amp;&amp; test -s "$S/p7504_pytest.txt" &amp;&amp; test -s "$S/p7504_pytest-c.txt" &amp;&amp; for key in RUFF_EXIT BLACK_EXIT MYPY_EXIT FULL_PYTEST_EXIT FULL_PYTEST_C_EXIT FULL_PYTEST_FAILED FULL_PYTEST_C_FAILED FULL_PYTEST_ERRORS CHANGELOG_GATE_SKIPS PREVIEW_SYNC_EXIT; do [ "$(k "$key")" = 0 ] || { echo "nonzero: $key"; exit 1; }; done &amp;&amp; [ "$(k PREVIEW_PACKAGE_COUNT)" = 4 ] &amp;&amp; [ "$(k FULL_PYTEST_PASSED)" -ge 1 ] &amp;&amp; [ "$(k FULL_PYTEST_C_PASSED)" -ge 1 ] &amp;&amp; [ -n "$(k PYVENV_VERSION_INFO)" ] &amp;&amp; [ -n "$(k PYVENV_HOME)" ] &amp;&amp; for s in '## Head check and provisioning' '## Wave-1 gate' '## Lint trio' '## Full pytest' '## @preview invariant'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `nonzero: KEY` or `missing section: NAME` printed. Each of these makes it fail: not in a worktree, or the shim absent; `BUMP_COMMIT_SHA` malformed or not an ancestor of HEAD; `EXTRACT_MATCHES_SECTION` not `yes`; `pyproject.toml` line 7 not reading `version = "0.9.6"`; `BASE_75_04` malformed; `uv lock --check` non-zero, meaning the lock drifted from `pyproject.toml`; ruff, black or mypy exiting non-zero; the live C-locale full suite exiting non-zero, reporting no passed tests, reporting a failure or error, or skipping a changelog page gate test; the preview-sync test failing, the live `@preview` count not 4, or any of the three sync surfaces changed since the milestone base; either saved pytest log missing or empty; any recorded exit, failure, error or skip key not 0; the recorded package count not 4; either recorded passed count below 1; either interpreter key empty; a named section missing; any tracked product file changed by this plan; `.planning/REQUIREMENTS.md` changed; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `BUMP_COMMIT_SHA` is an ancestor of HEAD and `pyproject.toml` line 7 reads `version = "0.9.6"`, so every measurement below is taken on the bumped tree.
    - `uv run ruff check .`, `uv run black --check .` and `uv run mypy typsphinx/` exit 0.
    - Both full pytest runs exit 0 with zero failed and zero errors, their summary and `SKIPPED` lines transcribed verbatim, and `CHANGELOG_GATE_SKIPS = 0`.
    - `tests/test_preview_version_sync.py` passes, `PREVIEW_PACKAGE_COUNT = 4`, and none of `typsphinx/templates`, `typsphinx/writer.py` or `typsphinx/template_engine.py` changed since `6cc44f22`.
    - `PYVENV_HOME` and `PYVENV_VERSION_INFO` are recorded beside the counts.
  </acceptance_criteria>
  <done>The bumped tip is locally green on every gate CI runs, including the C-locale suite, with no changelog-page-gate skip and the `@preview` invariant intact.</done>
</task>

<task type="auto">
  <name>Task 2: Clean C-locale docs-html and docs-pdf on the bumped tip, compared against 75-01's base ledger, re-proving both Phase 74 zero counts by measurement</name>
  <precondition>`75-BASE-EVIDENCE.md` records `BASE_HTML_WARNINGS`, `BASE_PDF_WARNINGS`, `BASE_DOCTEST_UNKNOWN` and `BASE_DOCSTRING_REST` from clean builds at this phase's own base.</precondition>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/75-v0-9-6-release-prep-prep-only/75-BASE-EVIDENCE.md (the four baseline keys, the three census commands verbatim, and the synthetic control fixture's construction)
    - tox.ini (the `docs-html` and `docs-pdf` environments, their `changedir` and their output directories)
    - .planning/ROADMAP.md § "v0.9.6" constraints 4 and 5, and § "Phase 74" for the two counts being re-proved
  </read_first>
  <action>
1. **`## Tip docs-html`.** `rm -rf docs/_build`, then
   `LANG=C LANGUAGE=C LC_ALL=C uv run tox -e docs-html > "$S/p7504_html.log" 2>&1; echo "exit:$?"`.
   Keys `TIP_HTML_EXIT`, which must be 0, and `TIP_HTML_WARNINGS`, parsed from the log's own verbatim
   `build succeeded` line exactly as 75-01 parsed the base's.

2. **`## Tip docs-pdf`.** `rm -rf docs/_build` again, then
   `LANG=C LANGUAGE=C LC_ALL=C uv run tox -e docs-pdf > "$S/p7504_pdf.log" 2>&1; echo "exit:$?"`.
   Keys `TIP_PDF_EXIT` and `TIP_PDF_WARNINGS`. Removing the whole output tree before each build is
   not optional: an incremental rebuild under-reports warnings, which is how this project once
   manufactured a false baseline match.

3. **`## Warning ledger`.** A four-row table — base html, tip html, base pdf, tip pdf — quoting
   `BASE_HTML_WARNINGS` and `BASE_PDF_WARNINGS` from `75-BASE-EVIDENCE.md` beside this plan's
   readings. Keys `HTML_WARNINGS_NOT_RISEN = yes` and `PDF_WARNINGS_NOT_RISEN = yes`, each set only
   when the tip integer is less than or equal to its base. SC4's requirement is "not risen", so an
   equal count passes and a lower count passes.

4. **`## Tip message-class census`.** Against `$S/p7504_pdf.log`, run the three census commands
   verbatim from `75-BASE-EVIDENCE.md` and record `TIP_DOCTEST_UNKNOWN`, `TIP_DOCSTRING_REST` and
   `TIP_RAW_REST`. The first two must be 0. Also record `TIP_HTML_DOCSTRING_REST` from
   `$S/p7504_html.log` with the attributed filter, which must be 0.

5. **`## Positive controls`.** Rebuild the same synthetic fixture at `$S/p7504_control.txt`, carrying
   one literal of each message class, and run the same three commands against it. Keys
   `CONTROL_DOCTEST_HITS`, `CONTROL_DOCSTRING_HITS` and `CONTROL_RAW_HITS`, each at least 1. Then
   quote `P74_BASE_DOCTEST_UNKNOWN` from `75-BASE-EVIDENCE.md` as the real pre-fix build control and
   state in one sentence that the zeros above are therefore a measured absence and not a broken
   pattern or a localised console.

6. **`## Docs invariants`.** Record `git diff --name-only <BASE_75_04> HEAD -- docs`, which must be
   empty, and `git diff --name-only 6cc44f22a01f1e9d2080a8220fca2dc2cdcb264b HEAD -- docs/source`,
   whose contents are recorded as measured — this plan changes no documentation source, and the
   milestone-range reading is context for the handoff.

7. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-GREEN-TREE-EVIDENCE.md" &amp;&amp; BE="$D/75-BASE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; S="$(k SCRATCH_75_04)" &amp;&amp; [ -n "$S" ] &amp;&amp; HL="$S/p7504_html.log" &amp;&amp; PL="$S/p7504_pdf.log" &amp;&amp; CT="$S/p7504_control.txt" &amp;&amp; test -s "$HL" &amp;&amp; test -s "$PL" &amp;&amp; test -s "$CT" &amp;&amp; [ "$(k TIP_HTML_EXIT)" = 0 ] &amp;&amp; [ "$(k TIP_PDF_EXIT)" = 0 ] &amp;&amp; HB="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$HL" | head -n1)" &amp;&amp; [ -n "$HB" ] &amp;&amp; HN="$(printf '%s\n' "$HB" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')" &amp;&amp; { [ -n "$HN" ] || { [ "$HB" = 'build succeeded.' ] &amp;&amp; HN=0; }; } &amp;&amp; [ "$HN" = "$(k TIP_HTML_WARNINGS)" ] &amp;&amp; PBL="$(LANG=C LC_ALL=C grep -E '^build succeeded' "$PL" | head -n1)" &amp;&amp; [ -n "$PBL" ] &amp;&amp; PN="$(printf '%s\n' "$PBL" | sed -n 's/^build succeeded, \([0-9]*\) warnings\{0,1\}\.$/\1/p')" &amp;&amp; { [ -n "$PN" ] || { [ "$PBL" = 'build succeeded.' ] &amp;&amp; PN=0; }; } &amp;&amp; [ "$PN" = "$(k TIP_PDF_WARNINGS)" ] &amp;&amp; BH="$(k BASE_HTML_WARNINGS "$BE")" &amp;&amp; BP="$(k BASE_PDF_WARNINGS "$BE")" &amp;&amp; case "$BH" in ''|*[!0-9]*) false ;; *) true ;; esac &amp;&amp; case "$BP" in ''|*[!0-9]*) false ;; *) true ;; esac &amp;&amp; [ "$HN" -le "$BH" ] &amp;&amp; [ "$PN" -le "$BP" ] &amp;&amp; [ "$(k HTML_WARNINGS_NOT_RISEN)" = yes ] &amp;&amp; [ "$(k PDF_WARNINGS_NOT_RISEN)" = yes ] &amp;&amp; TD="$(LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$PL" | wc -l)" &amp;&amp; [ "$TD" = 0 ] &amp;&amp; [ "$(k TIP_DOCTEST_UNKNOWN)" = 0 ] &amp;&amp; TR="$(LANG=C LC_ALL=C grep -cE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' "$PL" || true)" &amp;&amp; [ "$TR" = 0 ] &amp;&amp; [ "$(k TIP_DOCSTRING_REST)" = 0 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -cE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' "$HL" || true)" = 0 ] &amp;&amp; [ "$(k TIP_HTML_DOCSTRING_REST)" = 0 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -o 'unknown node type: .doctest_block' "$CT" | wc -l)" -ge 1 ] &amp;&amp; [ "$(LANG=C LC_ALL=C grep -cE '\.py:docstring of [A-Za-z0-9_.]+:[0-9]+: (ERROR|WARNING): (Unexpected indentation|Block quote ends without a blank line)' "$CT" || true)" -ge 1 ] &amp;&amp; [ "$(k CONTROL_DOCTEST_HITS)" -ge 1 ] &amp;&amp; [ "$(k CONTROL_DOCSTRING_HITS)" -ge 1 ] &amp;&amp; [ "$(k CONTROL_RAW_HITS)" -ge 1 ] &amp;&amp; [ "$(k P74_BASE_DOCTEST_UNKNOWN "$BE")" -ge 1 ] &amp;&amp; B="$(k BASE_75_04)" &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- docs)" ] &amp;&amp; for s in '## Tip docs-html' '## Tip docs-pdf' '## Warning ledger' '## Tip message-class census' '## Positive controls' '## Docs invariants'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: NAME` printed. Each of these makes it fail: not in a worktree, or the shim absent; either saved build log or the control fixture missing or empty; either recorded tox exit non-zero; either log lacking an English `build succeeded` line, which on this machine means the locale override did not reach Sphinx; either recorded warning integer differing from the one parsed live out of its own log; either baseline key in `75-BASE-EVIDENCE.md` absent or non-numeric; a tip warning integer greater than its base, or either not-risen key not `yes`; any tip message-class count non-zero, live or recorded, in either the pdf or the html log; any synthetic control count zero, live or recorded; the quoted Phase 74 pre-fix doctest count below 1, which would make the control vacuous; anything under `docs/` changed by this plan; any named section missing; any tracked product file changed; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both builds ran from a removed `docs/_build` under `LANG=C LANGUAGE=C LC_ALL=C` and exited 0.
    - `TIP_HTML_WARNINGS` is at most `BASE_HTML_WARNINGS` and `TIP_PDF_WARNINGS` at most `BASE_PDF_WARNINGS`, with both not-risen keys `yes`.
    - `TIP_DOCTEST_UNKNOWN`, `TIP_DOCSTRING_REST` and `TIP_HTML_DOCSTRING_REST` are all 0, recomputable from the saved logs.
    - The synthetic control returns at least one hit for each census pattern, and `75-BASE-EVIDENCE.md`'s quoted Phase 74 pre-fix count is at least 1.
    - `git diff --name-only <BASE_75_04> HEAD -- docs` is empty.
  </acceptance_criteria>
  <done>Phase 74's two zero counts are re-proved on the bumped tree by fresh clean builds, the warning ledger has not risen, and every zero is falsifiable.</done>
</task>

<task type="auto">
  <name>Task 3: The clean linkcheck run, and SC4's local verdict</name>
  <precondition>The network is reachable — a first `tox -e linkcheck` attempt produces a `docs/_build/linkcheck/output.json` with at least one record.</precondition>
  <files>.planning/phases/75-v0-9-6-release-prep-prep-only/75-GREEN-TREE-EVIDENCE.md</files>
  <read_first>
    - tox.ini `[testenv:linkcheck]` (the environment, its `changedir` and its output directory)
    - docs/source/conf.py (confirm there is still no `linkcheck_ignore`, `linkcheck_anchors_ignore` or `linkcheck_allowed_redirects` key, so the run is unsoftened)
    - .planning/milestones/v0.9.5-phases/72-tox-e-linkcheck-and-root-toctree-deduplication/72-05-PLAN.md Task 1 (the clean-run, bounded-retry and `output.json` parsing idiom this task reuses)
  </read_first>
  <action>
1. **`## Tip linkcheck`.** `rm -rf docs/_build`, then
   `uv run tox -e linkcheck > "$S/p7504_lc1.log" 2>&1; echo "exit:$?"`. Record the exit and the tail
   of the log.
   - `tox -e linkcheck` reaches the network, so a transient failure is possible. Allow at most
     **three** attempts total, each from a removed `docs/_build`, each with its own log
     (`p7504_lc1.log`, `p7504_lc2.log`, `p7504_lc3.log`) and its own recorded exit. Key
     `TIP_LINKCHECK_RUNS = ` the number of attempts made, and `TIP_LINKCHECK_RUN_<N>_EXIT` for each.
     The final attempt's exit must be 0. If three attempts all fail, record every failing URL
     verbatim, set `TIP_LINKCHECK_VERDICT = FAIL` and return a blocking checkpoint; never add an
     ignore key to soften it.
   - From `docs/_build/linkcheck/output.json`, record `TIP_LINKCHECK_TOTAL` — the number of records —
     and `TIP_LINKCHECK_WORKING` — the number whose `status` is `working`. They must be equal and at
     least 1. Key `TIP_LINKCHECK_VERDICT = PASS`.
   - Record that `docs/source/conf.py` carries no `linkcheck_ignore`, `linkcheck_anchors_ignore` or
     `linkcheck_allowed_redirects` key, so the count is not softened by an accept-list.

2. **`## SC4 local verdict`.** `SC4_LOCAL_VERDICT = MET` only when every one of these holds:
   - Task 1's `RUFF_EXIT`, `BLACK_EXIT`, `MYPY_EXIT`, both pytest exits, both failure counts, the
     error count, `CHANGELOG_GATE_SKIPS` and `PREVIEW_SYNC_EXIT` are all 0, and
     `PREVIEW_PACKAGE_COUNT` is 4
   - Task 2's `TIP_HTML_EXIT` and `TIP_PDF_EXIT` are 0, `HTML_WARNINGS_NOT_RISEN` and
     `PDF_WARNINGS_NOT_RISEN` are `yes`, and `TIP_DOCTEST_UNKNOWN`, `TIP_DOCSTRING_REST` and
     `TIP_HTML_DOCSTRING_REST` are 0
   - `TIP_LINKCHECK_VERDICT` is `PASS`

   Otherwise write `NOT-MET`, name every failing item, and return a blocking checkpoint. 75-06 reads
   this key before it pushes anything.

3. Commit.
  </action>
  <verify>
    <automated>D=.planning/phases/75-v0-9-6-release-prep-prep-only &amp;&amp; F="$D/75-GREEN-TREE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; [ "$(k TIP_LINKCHECK_VERDICT)" = PASS ] &amp;&amp; R="$(k TIP_LINKCHECK_RUNS)" &amp;&amp; [ "$R" -ge 1 ] &amp;&amp; [ "$R" -le 3 ] &amp;&amp; [ "$(k "TIP_LINKCHECK_RUN_${R}_EXIT")" = 0 ] &amp;&amp; S="$(k SCRATCH_75_04)" &amp;&amp; test -s "$S/p7504_lc${R}.log" &amp;&amp; O=docs/_build/linkcheck/output.json &amp;&amp; test -s "$O" &amp;&amp; C="$(uv run python -c 'import json,sys; r=[json.loads(l) for l in open(sys.argv[1]) if l.strip()]; print(len(r), sum(1 for x in r if x["status"]=="working"))' "$O")" &amp;&amp; [ "${C% *}" -ge 1 ] &amp;&amp; [ "${C% *}" = "${C#* }" ] &amp;&amp; [ "${C% *}" = "$(k TIP_LINKCHECK_TOTAL)" ] &amp;&amp; [ "${C#* }" = "$(k TIP_LINKCHECK_WORKING)" ] &amp;&amp; ! grep -qE '^linkcheck_(ignore|anchors_ignore|allowed_redirects)' docs/source/conf.py &amp;&amp; for key in RUFF_EXIT BLACK_EXIT MYPY_EXIT FULL_PYTEST_EXIT FULL_PYTEST_C_EXIT FULL_PYTEST_FAILED FULL_PYTEST_C_FAILED FULL_PYTEST_ERRORS CHANGELOG_GATE_SKIPS PREVIEW_SYNC_EXIT TIP_HTML_EXIT TIP_PDF_EXIT TIP_DOCTEST_UNKNOWN TIP_DOCSTRING_REST TIP_HTML_DOCSTRING_REST; do [ "$(k "$key")" = 0 ] || { echo "nonzero: $key"; exit 1; }; done &amp;&amp; [ "$(k PREVIEW_PACKAGE_COUNT)" = 4 ] &amp;&amp; [ "$(k HTML_WARNINGS_NOT_RISEN)" = yes ] &amp;&amp; [ "$(k PDF_WARNINGS_NOT_RISEN)" = yes ] &amp;&amp; [ "$(k SC4_LOCAL_VERDICT)" = MET ] &amp;&amp; for s in '## Tip linkcheck' '## SC4 local verdict'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; B="$(k BASE_75_04)" &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- . ':(exclude).planning')" ] &amp;&amp; [ -z "$(git diff --name-only "$B" HEAD -- .planning/REQUIREMENTS.md)" ] &amp;&amp; ! grep -qE '^[A-Z][A-Z0-9_]* = .*[(]' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `nonzero: KEY` or `missing section: NAME` printed. Each of these makes it fail: not in a worktree; `TIP_LINKCHECK_VERDICT` not `PASS`; the attempt count outside one to three, or the final attempt's recorded exit non-zero; the final attempt's log missing; `docs/_build/linkcheck/output.json` missing or empty; the live record count below 1, or the working count not equal to the total, or either differing from its recorded key; `docs/source/conf.py` carrying a linkcheck softening key; any Task 1 or Task 2 numeric key not 0; the `@preview` package count key not 4; either not-risen key not `yes`; `SC4_LOCAL_VERDICT` not `MET`; either named section missing; any tracked product file changed; `.planning/REQUIREMENTS.md` changed; a key line carrying an opening parenthesis; or a HALT heading present.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tox -e linkcheck` ran from a removed `docs/_build`, in at most three recorded attempts, with the final exit 0.
    - `docs/_build/linkcheck/output.json`'s `working` count equals its total and is at least 1, both matching their recorded keys.
    - `docs/source/conf.py` carries no `linkcheck_ignore`, `linkcheck_anchors_ignore` or `linkcheck_allowed_redirects` key.
    - `SC4_LOCAL_VERDICT = MET`, with every contributing key enumerated in the same section.
  </acceptance_criteria>
  <done>Every external documentation link resolves on a clean unsoftened run, and SC4's local half is recorded MET for 75-06 to gate its push on.</done>
</task>

</tasks>

<flagged_assumptions>
Phase-wide spec-less probe accounting lives in `75-01-PLAN.md` § "Spec-less probe accounting". This
plan authors no edge row of its own.

**Assumption specific to this plan:** that the base ledger in `75-BASE-EVIDENCE.md` and this plan's
tip readings are comparable. Both are taken in sibling worktrees of the same phase, provisioned with
the same command, and both record `pyvenv.cfg`'s `home` and `version_info` — but the two worktrees'
interpreters are not guaranteed identical (`CLAUDE.md` § "Interpreters may differ"). The comparison
this plan makes is "not risen", which tolerates an equal reading, and the two message-class counts
being compared are absolute zeros rather than deltas, so an interpreter difference cannot silently
turn a rise into a pass. If the two `version_info` values differ, record that fact beside the ledger
rather than suppressing the comparison.

**`tox -e linkcheck` is the one network-dependent gate in this phase.** Its bounded three-attempt
retry is a deliberate tolerance for transient DNS and rate-limit failures, not for a genuinely dead
link: a link that fails all three attempts is escalated, never ignored.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local gates → push and CI dispatch decision | a tip pushed without local proof defers every signal to a single dispatched run |
| measured warning count → SC4's "not risen" claim | an incremental or localised build makes the comparison meaningless |
| documentation → external network | `linkcheck` is the only gate whose verdict depends on hosts outside this project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-15 | Repudiation | a gate turned green by editing a test, a config key or a source file | high | mitigate | this plan changes no product file and the verify asserts an empty product diff against its own base; a red gate is recorded verbatim with a NOT-MET verdict and escalated |
| T-75-16 | Denial of Service | a locale-dependent test or message class that only fails in CI's English locale | medium | mitigate | the full suite runs a second time under `LC_ALL=C`, and every counted build is invoked with `LANG`, `LANGUAGE` and `LC_ALL` all set to C |
| T-75-17 | Repudiation | a false baseline match produced by an incremental documentation rebuild | high | mitigate | `rm -rf docs/_build` before every counted build, and each recorded integer is re-parsed from its own saved log at verify time |
| T-75-18 | Tampering | a linkcheck pass bought by adding an ignore or allowed-redirect key | medium | mitigate | `docs/source/conf.py` is negative-checked for the three softening keys, and a link failing all three attempts is escalated |
| T-75-19 | Repudiation | a zero-skip claim taken in an environment where the gate could only skip | high | mitigate | the `docs` extra is provisioned explicitly, `-rs` is mandatory, and both the recorded key and a live run assert no `SKIPPED` line names the changelog page gate |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide list, identical in every plan of this phase. Every symbol below is created by Phase 75
and did not exist at the phase base.

**New files under `.planning/phases/75-v0-9-6-release-prep-prep-only/`:** `75-CLOSEOUT-GUARD.md`,
`75-BASE-EVIDENCE.md`, `75-SC5-INVARIANTS.md`, `75-LEFTOVERS-EVIDENCE.md`, `COVERAGE.md`,
`75-BUMP-EVIDENCE.md`, `75-CHANGELOG-EVIDENCE.md`, `75-GREEN-TREE-EVIDENCE.md`,
`75-PREFLIGHT-EVIDENCE.md`, `75-CI-EVIDENCE.md`, `75-HANDOFF.md`, plus one `75-0N-SUMMARY.md` per
plan.

**New file under `.planning/todos/pending/`:** a record for IN-01 (D-14), the
`visit_literal_block` / `depart_literal_block` `Args:` text that still names only the literal block
node.

**New headings and lines in `CHANGELOG.md`:** `## [0.9.6] - <prep date>` and, inside it,
`### Added`, `### Changed`, `### Fixed`, `### Known Limitations` and `### Verified`; a fresh
`## [Unreleased]` heading placed above it, retaining only `### Planned for Future Releases`; and a
new tail link line `[0.9.6]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.6`.

**New value in `tests/test_changelog_page_gate.py`:** the `RELEASE_VERSIONS` tuple member `"0.9.6"`.

**Changed version literals (not new symbols):** `pyproject.toml`'s `[project].version`, the
`typsphinx` stanza's `version` in `uv.lock`, and `README.md`'s `**Status**: Stable (vX.Y.Z)` line,
all moving `0.9.2` to `0.9.6`.

**Deletions (no new symbol):** the five untracked repository-root files `probe_bogusxyz.typ`,
`probe_none.typ`, `probe_pycon.typ`, `probe_python.typ` and `probe_text.typ`.
</artifacts_this_phase_produces>

<verification>
- The lint trio and both full pytest runs are green on the bumped tip, with zero changelog-page-gate
  skips.
- Both documentation builds are clean, in the C locale, with their warning integers not risen above
  the phase-base ledger.
- Both Phase 74 message classes read zero on the tip, each falsifiable against a synthetic control
  and the quoted pre-fix measurement.
- `linkcheck` passes with `working` equal to its total on an unsoftened run.
- `SC4_LOCAL_VERDICT = MET`.
</verification>

<success_criteria>
- SC4's local half is discharged by runs executed in this phase on the bumped tree.
- Nothing was edited to make a gate pass, and the product tree is byte-identical to what 75-03
  committed.
</success_criteria>

<output>
Create `.planning/phases/75-v0-9-6-release-prep-prep-only/75-04-SUMMARY.md` when done, declaring
`requirements-completed: []`.
</output>
