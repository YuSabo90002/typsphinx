---
phase: 30.1-translations-repository-japanese-rtd-site
plan: 08
type: execute
wave: 7
depends_on: ["30.1-07"]
files_modified:
  - docs/source/contributing.rst
  - .planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml
  - .planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md
autonomous: true
gap_closure: true
requirements: [I18N-01]
must_haves:
  truths:
    - statement: "A contributor following `docs/source/contributing.rst`'s Translations section verbatim, command by command, reaches a successful `make locale-update` — proven by actually running the documented sequence in a fresh clone, with the pre-fix sequence run first and its failure recorded as a negative control (WR-01)"
      verification: backstop
    - statement: "The staged `update-pin.yml` acquires an explicit submodule branch fetch that runs BEFORE `git submodule update --remote typsphinx`, and that fetch writes `refs/remotes/origin/<branch>` — the exact ref whose absence produced `fatal: Unable to find refs/remotes/origin/gsd/v0.6.4-read-the-docs-migration revision in submodule path 'typsphinx'` on run 30188423465"
      verification: backstop
    - statement: "The branch the new fetch step uses is read at runtime from `.gitmodules`' `submodule.typsphinx.branch` field rather than hard-coded, so the owed post-merge flip of that field to `main` (Handoff item 5) cannot silently re-break the workflow"
      verification: backstop
    - statement: "The live `typsphinx-doc-translations` copy of `.github/workflows/update-pin.yml` is byte-identical to the staged copy after the patch, proven by `diff` against an independent fresh clone — the invariant Plan 03 established and this round preserves"
      verification: backstop
    - "The parent repository's milestone branch `gsd/v0.6.4-read-the-docs-migration` on `origin` is advanced to include this plan's commits by a plain fast-forward push, with the before and after SHAs both recorded, so the pin-bump run in Plan 09 has current source to advance to"
    - "The full test suite is run after the documentation edit and its result recorded, including the known NixOS-sandbox environmental failures named as such rather than presented as regressions"
    - "No file is deleted, `30.1-VERIFICATION.md` is not modified, and nothing under `typsphinx/` is touched (milestone invariant #3)"
  prohibitions:
    - statement: "MUST NOT accept WR-01 as fixed on the basis of reading the corrected text. The defect is that a command sequence fails when actually run; the only evidence that answers that is actually running it. A recorded negative control — the pre-fix sequence run in a fresh clone and its failure captured verbatim — plus a recorded successful post-fix run is the bar, because without the negative control a passing run proves only that the environment happened to be already warm"
      status: required
      verification: "The evidence section contains two runs of the documented sequence in two independent fresh clones: one with the unfixed clone command showing its failure verbatim, one with the fixed command showing `make locale-update` exiting 0"
    - statement: "MUST NOT push anything that was not deliberately authored. The parent-branch push carries no `--force`, no `--force-with-lease` and no leading-plus refspec, and the catalog churn that the WR-01 verification clone generates under its own `locale/` directory is never committed or pushed to `typsphinx-doc-translations` — a verification side effect that becomes a push turns a read-only check into an unreviewed change to a repository the pin-bump workflow is about to operate on"
      status: required
      verification: "The recorded push command lines contain none of those three forms, and a fresh clone of `typsphinx-doc-translations` after this plan shows its tip commit unchanged from `34647d25f6a4ab71db9df6635e13fc0c14a9ae2a` except for the deliberate workflow-patch commit this plan authors"
  artifacts:
    - "`docs/source/contributing.rst` — the Translations section's clone command corrected, plus one sentence covering the already-cloned case"
    - "`.planning/phases/30.1-.../translations-repo/.github/workflows/update-pin.yml` — one new step `Fetch the tracked branch into the submodule`, plus an added SHA echo on the existing advance step"
    - "`typsphinx-doc-translations`' live `.github/workflows/update-pin.yml` — updated to the byte-identical patched copy, in its own commit"
    - "30.1-EVIDENCE.md — sections `## Gap round — WR-01 documented clone command, negative control and fix`, `## Gap round — update-pin.yml submodule branch fetch (staged and live)`, and `## Gap round — parent milestone branch pushed` appended"
  key_links:
    - "`actions/checkout@v7` with `submodules: true` → `git submodule update --init --force --depth=1` → a submodule fetched BY PINNED SHA (`* branch dcc6a523... -> FETCH_HEAD`) → no `refs/remotes/origin/<branch>` ref exists → `git submodule update --remote` cannot resolve the tracked branch and exits 128. The new fetch step is inserted exactly between the third and fourth links of that chain"
    - "`.gitmodules`' `submodule.typsphinx.branch` → the fetch step's refspec → `git submodule update --remote`'s resolution target. Reading the field at runtime keeps all three in agreement through Handoff item 5's post-merge flip; hard-coding the branch name would make the flip a silent re-break"
    - "`origin/gsd/v0.6.4-read-the-docs-migration`'s tip → what the pin-bump run in Plan 09 can advance to → what `/ja/latest/` will serve. The pin currently sits at `dcc6a523...`, 51 commits behind `ff800068...`; this plan adds to that distance deliberately so the propagation probe has a visible delta"
---

<objective>
Close the two repairable defects this round inherited: the missing submodule branch fetch that makes the
D-08 pin-bump workflow fail deterministically at its fourth step, and the documented contributor clone
command that fails at the very first command after `cd` (code review WR-01).

Purpose: SC#3 has never been exercised past its fourth step. Run 30188423465 died at
`git submodule update --remote typsphinx` because `actions/checkout@v7`'s `submodules: true` fetches the
submodule by its pinned commit SHA at depth 1 and never creates the `refs/remotes/origin/<branch>` ref
that `--remote` resolution requires. Every step after that — including the `decide` step and both commit
paths — is unproven, not working. This plan makes the ref exist; Plan 09 finds out what the rest of the
workflow actually does. WR-01 rides along because both defects live in the same contributor-facing
surface and both are fixed by editing text this repository owns.

Output: the patched workflow in both the staged copy and the live repository, byte-identical; a corrected
clone command proven by an actual run with a recorded negative control; the parent's milestone branch
advanced so the pin bump has somewhere to advance to; all three recorded in `30.1-EVIDENCE.md`.

**Autonomy note:** `autonomous: true`. Every action here is machine-executable with the credentials the
executor already has — Plans 03 and 05 cloned, committed and pushed to `typsphinx-doc-translations` and
drove `gh` against it, all recorded verbatim in `30.1-EVIDENCE.md`. Nothing in this plan waits on the
owner.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-CONTEXT.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VERIFICATION.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-REVIEW.md
@.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-05-SUMMARY.md
</context>

<gap_round_scope>
Part of the Phase 30.1 gap-closure round (Plans 07-11, waves 6-10). Plans 01-06 are complete and are not
modified, renumbered or re-run. `30.1-VERIFICATION.md` is input only and is never written.
Full artifact list: `30.1-07-PLAN.md` § `<artifacts_this_phase_produces>`.
</gap_round_scope>

<flagged_probe_assumptions>
Both unresolved spec-less edge-probe rows are enumerated in full in `30.1-07-PLAN.md` §
`<flagged_probe_assumptions>` (edges: 2 surfaced == 0 authored + 2 flagged; prohibitions: 10 kept == 10
authored + 0 flagged; canon breadcrumbs recorded there). The row this plan carries, restated so the plan
is self-contained:

- **I18N-01 / `unclassified` — UNRESOLVED.** The probe could not classify it and per §A it is not
  auto-`backstop`ped. *What is assumed instead:* I18N-01's edge here is **a translation site that builds
  green while its source is frozen** — no data boundary to probe, and no error anywhere in the system
  when it happens. This plan addresses the mechanism (the workflow can now resolve the tracked branch);
  Plan 09 addresses the observation. **If the assumption is wrong,** the gap is that no probe-derived
  edge predicate exists for I18N-01 and the observed-run evidence in Plan 09 is its entire edge surface.
</flagged_probe_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Fix WR-01's clone command and prove it by running the documented sequence, with a negative control</name>
  <files>docs/source/contributing.rst, .planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-REVIEW.md` § "WR-01" in full — the exact defect, the exact line range (303-311), the two acceptable fix shapes, and the note that Read the Docs itself is unaffected because its own manifest declares `submodules: {include: all, recursive: false}`
    - `docs/source/contributing.rst` — the whole `Translations` section as it stands, so the edit is scoped to the clone line plus one added sentence and touches nothing else
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/Makefile` — `SUBMODULE = typsphinx`, `SOURCEDIR = $(SUBMODULE)/docs/source`, and the `locale-update` / `locale-stat` bodies, which are what actually fails on an un-recursed clone
    - `CLAUDE.md` § "Worktree-isolated execution" — provision with `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs` (docs extras needed for `sphinx-intl`), then the NixOS `uv` shim line `ln -sf "$(command -v uv)" .venv/bin/uv`, then run everything through `uv run`
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#3 — parent source change pushed" § 1 — the shape and placement of the Translations section this edit modifies
  </read_first>
  <action>
    **Negative control first, before editing anything.** In a temporary directory outside this
    repository, run the documented sequence exactly as `contributing.rst` currently states it: a plain
    `git clone` of `https://github.com/YuSabo90002/typsphinx-doc-translations.git` with no submodule
    flag, then `cd`, then `make locale-update`. Capture the failure verbatim — the exact `sphinx-build`
    error and the exit status — and additionally record that the `typsphinx` directory in that clone is
    empty (an `ls` of it, and the clone's `git submodule status` line showing the leading minus that
    marks an uninitialised submodule). This is the recorded proof that WR-01 is a real, reproducible
    defect and not a reviewer's inference.

    Run `make locale-update` with `SPHINXBUILD` overridden to the worktree venv's `python -m sphinx` form
    and the venv's `bin` directory prefixed onto `PATH` so `sphinx-intl` resolves — the console-script
    form is the one this NixOS sandbox is known to break on, and the Makefile exposes `SPHINXBUILD` as an
    override for exactly this reason. Record which invocation form was used. If `sphinx-intl`'s console
    script cannot exec, invoke the same entry point through the venv's interpreter and record that too.

    **Then fix.** Edit `docs/source/contributing.rst`'s Translations section so the clone command carries
    `--recurse-submodules`, per the review's preferred fix. Add one short sentence immediately after the
    code block for the contributor who already cloned without it, naming `git submodule update --init` as
    the recovery. Change nothing else in that section — not the URL, not the `make locale-update` /
    `make locale-stat` lines, not the coverage sentence, not the heading.

    **Then prove.** In a second, independent temporary directory, run the corrected sequence exactly as
    the edited file now states it, and record `make locale-update` completing with exit status 0 and its
    output. Confirm in the same record that the clone's `typsphinx/` directory is populated this time.

    **Do not push the verification clone's side effects.** `make locale-update` rewrites the clone's
    `locale/ja/LC_MESSAGES/*.po` headers. Those clones are scratch: nothing from them is committed or
    pushed anywhere. Record `git status --short` inside the clone to show what it produced, then leave it.

    Run the full test suite and a docs HTML build to confirm the edit did not break the documentation
    tree, naming the known NixOS-sandbox integration failures as environmental rather than presenting
    them as regressions. Append `## Gap round — WR-01 documented clone command, negative control and fix`
    to `30.1-EVIDENCE.md` with every command and its output verbatim, both runs, in order.

    Note the NixOS accommodation established earlier in this phase: a Bash command line containing this
    repository's Sphinx source path as a literal is false-flagged by the worktree safety checker. Build
    such paths at runtime inside a Python string (for example with `glob`) rather than writing them as
    literal command-line arguments.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "import glob,pathlib,sys;p=glob.glob('docs/*/contributing.rst')[0];t=pathlib.Path(p).read_text(encoding='utf-8');ev=pathlib.Path('.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md').read_text(encoding='utf-8');ok1='--recurse-submodules' in t;ok2='git submodule update --init' in t;ok3='## Gap round — WR-01 documented clone command, negative control and fix' in ev;ok4='make locale-update' in ev;print('clone_flag',ok1,'recovery_sentence',ok2,'evidence_section',ok3,'ran_make',ok4);sys.exit(0 if all([ok1,ok2,ok3,ok4]) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints all four flags `True` and exits 0
    - The evidence section records the pre-fix run in a fresh clone with its verbatim `sphinx-build` failure and its non-zero exit status, plus a listing showing the clone's `typsphinx` directory empty and its `git submodule status` line marking the submodule uninitialised
    - The evidence section records the post-fix run in a second independent fresh clone with `make locale-update` exiting 0 and the clone's `typsphinx` directory populated
    - The evidence section records which `SPHINXBUILD` / `sphinx-intl` invocation form was used, and states why the console-script form was or was not usable in this sandbox
    - `git diff -- docs/` shows changes confined to the Translations section of `contributing.rst`: the clone line gains `--recurse-submodules` and one recovery sentence is added; the URL, the `make` lines, the heading and the coverage sentence are unchanged
    - The full test suite result is recorded, with any NixOS-sandbox integration failures named as environmental and matched against the known list rather than reported as regressions
    - A docs HTML build of this repository completes and the record states that the Translations section still renders with a TOC entry and produced no new warning attributable to this file
    - `git status --porcelain typsphinx/ pyproject.toml .readthedocs.yaml .github/` is empty
    - No file is deleted: `git diff --diff-filter=D --name-only` against the pre-task tree is empty
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VERIFICATION.md` is unmodified
  </acceptance_criteria>
  <done>The documented contributor sequence is proven broken before the edit and proven working after it, both by actually running it in fresh clones, and the corrected text is confined to the two lines that carry the defect and its recovery.</done>
</task>

<task type="auto">
  <name>Task 2: Insert the submodule branch fetch into update-pin.yml, staged and live, byte-identical</name>
  <files>.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml, .planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml` — the whole file: its header comment, the `schedule` + `workflow_dispatch` triggers, the deliberate `permissions: contents: write` scope and the comment explaining it, and the exact step sequence the patch inserts into
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#3 — observed pin-bump run" §§ 3, 4 and 5 — the per-step outcome list, the failing step's verbatim log, and the checkout step's own log showing `* branch dcc6a523... -> FETCH_HEAD` and `git submodule update --init --force --depth=1`. The diagnosis is measured; do not re-derive it
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#3 precondition — translations repository populated" §§ 3, 6 and 9 — `.gitmodules`' exact contents, the byte-identity check shape (`diff` staged against clone, echoing an `IDENTICAL` marker), and the clone/commit/push shape used against the live repository
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "Handoffs" item 5 — the owed post-merge flip of `.gitmodules`' `branch` field to `main`, which is why the fetch step must read the branch rather than hard-code it
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-RESEARCH.md` § "Common Pitfalls" Pitfall 2 — the tracked-branch semantics, and the warning not to conflate `.gitmodules`' `branch` with RTD's own Admin Default-branch setting
  </read_first>
  <action>
    Patch the staged workflow, then make the live copy byte-identical to it.

    **The patch.** Insert one new step named `Fetch the tracked branch into the submodule`, positioned
    after `Set up Python` and immediately before `Advance submodule to latest tracked branch`. Its body
    resolves the tracked branch at runtime from `.gitmodules` — read `submodule.typsphinx.branch` with
    `git config -f .gitmodules` — echoes the resolved value so the run log evidences which branch was
    followed, then runs a fetch inside the submodule directory against `origin` with an explicit refspec
    that maps `refs/heads/<branch>` onto `refs/remotes/origin/<branch>`, then verifies with
    `git rev-parse --verify` that the remote-tracking ref now exists, then echoes the submodule's current
    `HEAD` SHA as the "before" value. Use `git -C typsphinx` rather than a directory change, matching the
    style the rest of the file uses. Keep `--depth=1` on the fetch: the submodule arrives shallow from
    `actions/checkout`, only the branch tip is needed, and a full-history fetch of the parent repository
    on every scheduled run is cost with no benefit.

    Write a comment above the step that states WHY it exists, in the same rationale-carrying register the
    rest of this file uses: `actions/checkout@v7`'s `submodules: true` runs `git submodule update --init
    --force --depth=1`, which fetches the submodule by its pinned commit SHA and therefore never creates
    the remote-tracking ref that the next step's `--remote` resolution reads; without this step
    `git submodule update --remote` exits 128 with `fatal: Unable to find refs/remotes/origin/<branch>
    revision in submodule path 'typsphinx'`, which is exactly what run 30188423465 did. Name the run.

    Then extend the existing `Advance submodule to latest tracked branch` step to echo the submodule's
    `HEAD` SHA after the update, so the run log itself carries a before/after pair rather than requiring
    a clone to reconstruct one. Add lines; do not restructure the step, do not change its name, and do
    not touch the `decide`, `Commit and push if changed` or `Discard header-only churn` steps — those are
    unexercised, and Plan 09 exists to find out what they actually do.

    **Sanity-check the staged file before it goes anywhere:** parse it as YAML and confirm it still loads,
    that the job's step list is one longer than before, that the new step precedes the `--remote` step in
    document order, and that `permissions` is still exactly `contents: write`.

    **The live copy.** Clone `typsphinx-doc-translations` into a temporary directory, copy the staged file
    over the live one, confirm with `diff` that the two are byte-identical, commit with a message naming
    the defect and the run it was diagnosed from, and push to `main`. Then clone a second, independent
    time and `diff` the staged file against that fresh clone's copy, echoing an explicit identity marker —
    the same shape Plan 03 used. A byte-identity claim made against the clone you just wrote into proves
    nothing; the second clone is the check.

    Append `## Gap round — update-pin.yml submodule branch fetch (staged and live)` to
    `30.1-EVIDENCE.md`: the full diff of the staged file, the YAML sanity-check output, the commit and
    push transcript, and the independent byte-identity check. Do not trigger the workflow here — that is
    Plan 09's task, and triggering it from this task would leave the observation unrecorded.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "
import pathlib,sys,yaml
p=pathlib.Path('.planning/phases/30.1-translations-repository-japanese-rtd-site/translations-repo/.github/workflows/update-pin.yml')
raw=p.read_text(encoding='utf-8')
d=yaml.safe_load(raw)
steps=d['jobs']['update-pin']['steps']
names=[s.get('name','') for s in steps]
fetch=[i for i,n in enumerate(names) if 'Fetch the tracked branch' in n]
adv=[i for i,n in enumerate(names) if 'Advance submodule' in n]
ok_order = bool(fetch) and bool(adv) and fetch[0] < adv[0]
body=' '.join(str(s.get('run','')) for s in steps)
ok_cfg='submodule.typsphinx.branch' in body
ok_ref='refs/remotes/origin/' in body
ok_perm=d.get('permissions')=={'contents':'write'}
ev=pathlib.Path('.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md').read_text(encoding='utf-8')
ok_ev='## Gap round — update-pin.yml submodule branch fetch (staged and live)' in ev
print('steps',len(steps),'order',ok_order,'reads_gitmodules',ok_cfg,'writes_tracking_ref',ok_ref,'perm',ok_perm,'evidence',ok_ev)
sys.exit(0 if all([ok_order,ok_cfg,ok_ref,ok_perm,ok_ev]) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `order True`, `reads_gitmodules True`, `writes_tracking_ref True`, `perm True`, `evidence True` and exits 0, with the step count one higher than the pre-patch count of 10
    - The new step's `run` body resolves the branch from `.gitmodules` at runtime and contains no hard-coded branch name
    - The new step's `run` body verifies the remote-tracking ref exists after the fetch, so a silently-ineffective fetch fails loudly at the step that created it rather than at the next one
    - The new step's comment names `actions/checkout@v7`, the `--depth=1` commit-scoped submodule fetch, and run `30188423465`
    - The `Advance submodule to latest tracked branch` step echoes the submodule `HEAD` SHA after the update, and the new step echoes it before, giving the run log its own before/after pair
    - The `decide`, `Commit and push if changed` and `Discard header-only churn` steps are byte-unchanged: `git diff` on the staged file shows no line inside them
    - The evidence section contains the full staged-file diff, the YAML parse result, the commit and push transcript against `typsphinx-doc-translations`, and a byte-identity `diff` run against a SECOND, independently created fresh clone
    - No workflow run is triggered by this task — the evidence section contains no `gh workflow run` invocation
    - `git status --porcelain typsphinx/ tests/ pyproject.toml .readthedocs.yaml .github/` is empty
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VERIFICATION.md` is unmodified and no file is deleted
  </acceptance_criteria>
  <done>The staged and live copies of `update-pin.yml` both carry an explicit, branch-agnostic submodule fetch that creates the remote-tracking ref `--remote` resolution requires, they are proven byte-identical from an independent clone, and nothing downstream of the previously failing step has been altered.</done>
</task>

<task type="auto">
  <name>Task 3: Advance the parent's milestone branch on origin so the pin bump has current source to reach</name>
  <files>.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#3 — parent source change pushed" § 6 — the exact push discipline to reuse: record `origin/<branch>` before, push `HEAD` to the branch ref with no force and no leading-plus refspec, re-fetch, record after, and state plainly that the range notation itself evidences the fast-forward
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` § "SC#3 — observed pin-bump run" § 6 — the gitlink's current value `dcc6a523fc2d16f3dae925e17d63a1a24318b6d7` and the distance to the parent's tip
    - `CLAUDE.md` § "Worktree-isolated execution" — this task runs inside an isolated worktree whose branch was forked from the current head; the push targets the milestone branch, not the worktree branch
  </read_first>
  <action>
    Record the parent's branch state, push, and record it again.

    Read `origin/gsd/v0.6.4-read-the-docs-migration` fresh (fetch first, do not trust a stale ref) and
    record the SHA. Record this worktree's `HEAD`. State whether `HEAD` is a descendant of the recorded
    origin SHA — if it is not, stop and record why rather than pushing; a non-fast-forward here would
    mean the worktree forked from something other than the current head and the situation needs a human,
    not a flag.

    Push `HEAD` to `refs/heads/gsd/v0.6.4-read-the-docs-migration` on `origin` with no force, no lease and
    no leading-plus refspec. Re-fetch and record the new `origin` SHA, and confirm it equals this
    worktree's `HEAD`.

    Then record two consequences plainly, neither of which is a defect:
    - This push re-triggers the PARENT project's own English Read the Docs build, because that project's
      Default branch is this same milestone branch. That is incidental, harmless, and is not what SC#3
      measures.
    - It does NOT trigger a rebuild of the ja project. That is RESEARCH assumption A3, already measured
      once in Plan 05; re-state it here rather than letting a reader infer that the ja site should have
      updated.

    State explicitly what this push is for: the pin-bump run in Plan 09 advances the submodule to the tip
    of the tracked branch, so the tip must contain the source whose arrival on `/ja/latest/` is the
    propagation half of SC#3. The gitlink currently sits at `dcc6a523...`; after this push the distance is
    larger, and both the Plan 05 `Translations` sentence and this plan's WR-01 correction are inside it —
    which gives Plan 09 two independent propagation probes instead of one.

    Append `## Gap round — parent milestone branch pushed` to `30.1-EVIDENCE.md` with every command and
    output verbatim, including the before and after SHAs and the exact push command line.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run python -c "
import pathlib,subprocess,sys
ev=pathlib.Path('.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md').read_text(encoding='utf-8')
sec='## Gap round — parent milestone branch pushed'
ok_sec=sec in ev
body=ev.split(sec)[-1]
ok_no_force=('--force' not in body)
subprocess.run(['git','fetch','origin','gsd/v0.6.4-read-the-docs-migration','--quiet'],check=False)
origin=subprocess.run(['git','rev-parse','origin/gsd/v0.6.4-read-the-docs-migration'],capture_output=True,text=True).stdout.strip()
head=subprocess.run(['git','rev-parse','HEAD'],capture_output=True,text=True).stdout.strip()
anc=subprocess.run(['git','merge-base','--is-ancestor','dcc6a523fc2d16f3dae925e17d63a1a24318b6d7',origin]).returncode==0
print('section',ok_sec,'no_force_in_section',ok_no_force,'origin',origin,'head',head,'pin_is_ancestor',anc,'origin_has_head', origin==head)
sys.exit(0 if all([ok_sec,ok_no_force,anc,origin==head]) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `section True`, `no_force_in_section True`, `pin_is_ancestor True` and `origin_has_head True`, and exits 0
    - The evidence section records the `origin/gsd/v0.6.4-read-the-docs-migration` SHA before the push, this worktree's `HEAD`, an explicit descendant check, the exact push command line, and the `origin` SHA after the push
    - The recorded push command line contains no `--force`, no `--force-with-lease` and no leading-plus refspec, and the push output shows two-dot range notation rather than forced-update notation
    - The evidence section states that the push re-triggers the parent English RTD project and does not trigger the ja project, citing RESEARCH assumption A3 as previously measured rather than newly inferred
    - The evidence section names both propagation probes Plan 09 will have available — the Plan 05 Translations sentence and this plan's `--recurse-submodules` correction
    - `git status --porcelain typsphinx/ tests/ pyproject.toml .readthedocs.yaml .github/` is empty
    - `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-VERIFICATION.md` is unmodified and no file is deleted
  </acceptance_criteria>
  <done>`origin/gsd/v0.6.4-read-the-docs-migration` carries this plan's commits by a recorded fast-forward push, and the record states exactly what that push does and does not trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This repository → `typsphinx-doc-translations` (external, owner-owned) | A workflow file that runs with `contents: write` is written into a repository this one does not control. The content crossing the boundary is authored here and proven byte-identical there |
| The executor's `gh`/git credentials → two remote repositories | Push access to both the parent and the translations repository is present in the executor's environment. Only two deliberate pushes are authored; every other remote interaction is read-only |
| `.gitmodules`' `branch` field → the workflow's fetch refspec | A value read from a file in the external repository is interpolated into a git refspec inside a privileged job |

## STRIDE Threat Register

ASVS L1, block on `high`. V2/V3/V4/V5/V6 remain not applicable — no authentication surface, session,
access control, user input processing or cryptography is added. Threat IDs continue the phase's
`T-30.1-NN` sequence.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30.1-13 | Elevation of Privilege | The patched `update-pin.yml`, which runs unattended on a schedule with `contents: write` and pushes to `typsphinx-doc-translations` | high | mitigate | The patch adds one read-only fetch step and two echoes. `permissions: contents: write` is asserted unchanged by the verify command; no new secret, no `pull_request_target`, no third-party action, and no change to any step that writes or pushes. The full staged diff is recorded and the live copy is proven byte-identical from an independent clone, so what runs in the external repository is exactly what was reviewed here |
| T-30.1-19 | Tampering | `.gitmodules`' `branch` value being interpolated into a git refspec inside a privileged job | medium | mitigate | The value is read with `git config -f .gitmodules`, is a git ref name by construction, and is used only as the two halves of an explicit `refs/heads/…:refs/remotes/origin/…` refspec — never as a shell word that could reach another command. The step verifies the resulting ref with `git rev-parse --verify` so a value that resolved to nothing fails at that step rather than producing a silently wrong pin. The file itself is only writable by someone who already has push access to the repository the workflow pushes to, so this is not a privilege boundary crossing |
| T-30.1-20 | Tampering | An unreviewed change reaching `typsphinx-doc-translations` as a side effect of verification | high | mitigate | The WR-01 verification clones are scratch directories; the plan's prohibition forbids committing or pushing their regenerated `locale/` churn, and the acceptance criteria require a fresh clone showing the live repository's tip carries only the deliberate workflow-patch commit |
| T-30.1-17 | Information Disclosure | `gh` and git credentials present in the executor's environment | medium | mitigate | Only two authored pushes and ordinary clones occur; no token is printed, echoed into the evidence record, or written into any file. Recorded transcripts are checked for credential material before being appended |
| T-30.1-06 | Information Disclosure | Public build logs and public repositories | low | accept | Both repositories and all logs are already public. The workflow references no secret; the standing rule from Phase 29 holds — no secret may ever be echoed into a `build.jobs` or workflow command |
| T-30.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan and no new package is introduced (milestone invariant #1). `30.1-RESEARCH.md` § "Package Legitimacy Audit" records zero `[ASSUMED]`/`[SUS]`/`[SLOP]` rows, so no legitimacy checkpoint is owed. The workflow continues to use only `actions/checkout` and `astral-sh/setup-uv`, both already present and unchanged |
</threat_model>

<verification>
1. The documented contributor sequence is run twice in two independent fresh clones — once pre-fix showing the failure verbatim, once post-fix showing `make locale-update` exit 0
2. `contributing.rst`'s diff is confined to the clone line and one added recovery sentence
3. The staged `update-pin.yml` parses as YAML, gains exactly one step, and that step precedes the `--remote` step
4. The new step reads the tracked branch from `.gitmodules` at runtime and verifies the remote-tracking ref after fetching
5. `permissions: contents: write` and every step downstream of the previously failing one are unchanged
6. The live workflow file is byte-identical to the staged one, proven from a second independent clone
7. `origin/gsd/v0.6.4-read-the-docs-migration` is advanced by a recorded fast-forward push with no force
8. The full test suite and a docs HTML build are recorded, with environmental failures named as such
9. `30.1-VERIFICATION.md` is unmodified, no file is deleted, and nothing under `typsphinx/` is touched
</verification>

<success_criteria>
- WR-01 is proven a real defect and proven fixed, both by running the documented commands rather than by reading them
- The pin-bump workflow can resolve its tracked branch, and the reason it previously could not is written into the file next to the fix
- The fix survives the owed post-merge branch flip because the branch name is read, not hard-coded
- Staged and live copies of the workflow are byte-identical, proven independently
- The parent's milestone branch carries this round's commits, giving the pin bump current source and Plan 09 two independent propagation probes
- No workflow run is triggered here; no file is deleted; `30.1-VERIFICATION.md` is untouched
</success_criteria>

<output>
Create `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-08-SUMMARY.md` when done.

The SUMMARY must state the pre-fix failure message verbatim in one line, the post-fix exit status, the
name and position of the new workflow step, the live commit SHA in `typsphinx-doc-translations`, and the
before/after SHAs of the parent push — and must not describe SC#3 as met, because nothing has been
triggered or observed yet.
</output>
