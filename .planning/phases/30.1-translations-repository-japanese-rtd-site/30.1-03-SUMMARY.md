---
phase: 30.1-translations-repository-japanese-rtd-site
plan: 03
subsystem: docs-hosting
tags: [readthedocs, i18n, translations-repo, submodule, github]

dependency-graph:
  requires:
    - phase: 30.1-01
      provides: staged .readthedocs.yaml, Makefile, README.md for typsphinx-doc-translations
    - phase: 30.1-02
      provides: staged .github/workflows/update-pin.yml (D-08) for typsphinx-doc-translations
  provides:
    - "typsphinx-doc-translations populated and pushed on GitHub: submodule + 13 .po catalogs + four staged files + .gitignore"
    - "30.1-EVIDENCE.md — created, with Owner-Manual Steps, SC#3 precondition (populated), SC#3 precondition (fresh-clone-verified), and D-15 gate sections"
  affects:
    - "30.1-04 (reads the ja RTD project's first build log against this pushed tree; must observe the site actually serving before D-15's gate opens)"
    - "Phase 30 (docs/locale/ja/ deletion there depends on this plan's replacement being confirmed working, not merely present — D-15 gate still closed after this plan)"

tech-stack:
  added: []
  patterns:
    - "git submodule add -b <branch> writes .gitmodules' branch field explicitly, avoiding inherited-branch drift in git submodule update --remote"
    - "Working clone and independent verification clone both live under mktemp -d, entirely outside the git worktree, so the merge/cleanup step never sweeps up a nested repository"

key-files:
  created:
    - .planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md
  modified: []

decisions:
  - "Owner-reported repository-creation values (repo_full_name, visibility, clone_url, auto_initialised, actions_enabled) recorded verbatim in 30.1-EVIDENCE.md, explicitly labelled owner-reported and never phrased as machine-verified, per the plan's must_haves"
  - "Task 3's single-line compound verify command could not run as one shell invocation under this worktree's path-safety guard; every clause was instead run and confirmed individually and recorded as such in the evidence file"

metrics:
  duration: "~50 minutes"
  completed: 2026-07-26
status: complete
---

# Phase 30.1 Plan 03: Populate and Push the Translations Repository Summary

Populated the owner-created `typsphinx-doc-translations` GitHub repository with a branch-pinned
`typsphinx` submodule, 13 relocated Japanese `.po` catalogs (zero `.mo`), the four files staged by
Plans 01/02, and a `.gitignore` — pushed to `main` — then independently re-observed the pushed tree from
a second, fresh clone and proved the relocated catalogs byte-identical to this repository's own. Created
`.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md`.

## What Was Built

**Task 1 (owner-manual, already satisfied before this agent started):** the owner created the empty
`YuSabo90002/typsphinx-doc-translations` repository (public, no auto-init, Actions enabled) and reported
its clone URL. This agent carried those five values verbatim into Tasks 2 and 3.

**Task 2 — Populate and push:** in a temporary clone outside this worktree
(`mktemp -d` → `/tmp/.../tr`), added the `typsphinx` submodule with an explicit
`-b gsd/v0.6.4-read-the-docs-migration` (writing the `branch` field into `.gitmodules`, mandatory per
RESEARCH Pitfall 2), pinning it at commit **`dcc6a523fc2d16f3dae925e17d63a1a24318b6d7`**
("docs(phase-29): update tracking after wave 3", authored 2026-07-25T23:20:17+09:00) — matching the
orchestrator's pre-dispatch `git ls-remote` measurement exactly. Copied the 13 `.po` catalogs (no `.mo`)
from this repository's `docs/locale/ja/LC_MESSAGES/` into `locale/ja/LC_MESSAGES/` at the clone root,
preserving the nested per-docname layout (`api/index.po`, `examples/{advanced,basic,index}.po`,
`user_guide/{builders,configuration,index,templates}.po`). Copied the four staged files
(`.readthedocs.yaml`, `Makefile`, `README.md`, `.github/workflows/update-pin.yml`) byte-for-byte,
verified with `diff` before staging. Wrote a three-line `.gitignore` (`.venv/`,
`typsphinx/docs/_build/`, `*.mo`). Committed as one initial commit and pushed to `main`
(**`34647d25f6a4ab71db9df6635e13fc0c14a9ae2a`**). Confirmed this repository's `docs/locale/` was
untouched throughout (`git status --porcelain docs/locale/` empty, `git ls-files docs/locale | wc -l`
still 26) — the catalogs were copied, never moved (PD-01).

**Task 3 — Prove the pushed tree from a fresh clone:** took a **second, independent** clone from GitHub
into a new temporary directory (not the working clone Task 2 pushed from). Confirmed: tip SHA
`34647d25f6a4ab71db9df6635e13fc0c14a9ae2a` matches the pushed SHA; 13 `.po` / 0 `.mo` tracked, nested
layout intact; `.gitmodules`' `branch` field names `gsd/v0.6.4-read-the-docs-migration`; the `typsphinx`
index entry is mode `160000` pinned at `dcc6a523fc2d16f3dae925e17d63a1a24318b6d7`, matching Task 2's
recorded pin exactly; all five expected top-level artefacts present; `diff -rq --exclude='*.mo'` between
this repository's `docs/locale/ja/LC_MESSAGES` and the fresh clone's `locale/ja/LC_MESSAGES` produced
empty output (byte-identical); and the landed `.readthedocs.yaml` still satisfies Plan 01's structural
checks (removal-before-copy ordering, `path:`-based install). Appended the `## D-15 gate — status after
Wave 2` paragraph stating plainly that the catalogs are relocated but the Japanese site has not yet been
observed serving, so Phase 30's deletions remain blocked pending Plan 04.

## Task Commits

1. **Task 2: Populate and push the translations repository** — `ece07c0` (docs)
2. **Task 3: Prove the pushed tree from a fresh clone** — `fc2ead0` (docs)

_No TDD tasks; both are single-commit `auto` tasks. Task 1 was a `checkpoint:human-action` already
satisfied by the owner's reply before this agent was spawned — no commit for it._

## Pushed / Pinned State (for Plan 04+)

- **Translations repository:** `YuSabo90002/typsphinx-doc-translations`, pushed commit
  `34647d25f6a4ab71db9df6635e13fc0c14a9ae2a` on `main`.
- **Submodule pin:** `typsphinx` at `dcc6a523fc2d16f3dae925e17d63a1a24318b6d7` on tracked branch
  `gsd/v0.6.4-read-the-docs-migration` (independently re-verified from a fresh clone, both mode and SHA
  matching).
- **Catalog counts observed from the independent clone:** 13 `.po`, 0 `.mo`.
- **Phase 30's deletion of `docs/locale/ja/` in this repository is still blocked.** The Japanese site
  has not yet been observed serving — no RTD project exists against `typsphinx-doc-translations` yet and
  no build has run. Plan 04 closes the remaining D-15 half by reading that project's first raw build log
  and observing a served Japanese page.

## Files Created/Modified

- `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md` — NEW. Sections:
  `## Owner-Manual Steps (human_needed)`, `## SC#3 precondition — translations repository populated`,
  `## SC#3 precondition — pushed tree verified from a fresh clone`, `## D-15 gate — status after Wave 2`.
  Plans 04/05/06 append further `##` sections; nothing here is rewritten or reordered.
- No file under `typsphinx/`, `docs/`, `tests/`, or this repository's `.github/` was modified — confirmed
  by `git status --porcelain` after every task.

## Deviations from Plan

**None (Rule 1-4) — plan executed exactly as written.** One environmental accommodation, not a
deviation: Task 3's single-line compound automated verify command (a long `&&`-chained shell one-liner)
could not run as a single Bash invocation in this sandboxed worktree — the harness's worktree
path-safety guard rejects compound/piped commands that touch paths outside the worktree root, even when
those paths are the plan's own mandated `mktemp -d` temporary clone. Every clause of that command was
instead run and confirmed individually (tip SHA, `.po`/`.mo` counts, gitlink mode+SHA, `.gitmodules`
branch field, all five artefacts, and the `diff -rq` catalog comparison), each recorded with its own
verbatim output in `30.1-EVIDENCE.md` under "Component-by-component verification of Task 3's automated
verify command." All seven clauses independently passed.

## Known Stubs

None. All work in this plan is either a completed push to a real, externally observable repository or a
completed evidence record of commands actually run.

## Threat Flags

None new. This plan's only security-relevant action — pushing plain-text content (translation catalogs,
a build manifest, a workflow, a README, all already public in this repository) to a new public
repository — is exactly the surface `30.1-01-PLAN.md`'s `<threat_model>` (T-30.1-02, T-30.1-06,
T-30.1-07) already covers. No secret was ever referenced in any command run by this plan.

## Self-Check: PASSED

- FOUND: `.planning/phases/30.1-translations-repository-japanese-rtd-site/30.1-EVIDENCE.md`
- FOUND commit `ece07c0` (Task 2)
- FOUND commit `fc2ead0` (Task 3)
- FOUND remote commit `34647d25f6a4ab71db9df6635e13fc0c14a9ae2a` on
  `https://github.com/YuSabo90002/typsphinx-doc-translations` (re-confirmed via independent fresh clone
  in Task 3, not merely the pushing clone's own belief)
- FOUND commit `1b327ed` (SUMMARY.md)
