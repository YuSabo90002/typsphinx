---
phase: 30.1-translations-repository-japanese-rtd-site
verified: 2026-07-26T12:00:00Z
status: human_needed
score: 4/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 3/5
  gaps_closed:
    - "SC#3 — the translations repository builds the current English source via an automated, observed submodule-pin advance (was FAILED: workflow died at the missing-fetch step; now MET, independently re-confirmed live)"
    - "SC#4 / I18N-03 D-03 checks 1-3 (page count, extracted text/NUL scan, embedded CJK font enumeration) — was FAILED (10 NUL bytes replacing 発/単/釈, 95-vs-94 page mismatch); now a clean machine pass (94=94, NUL 0/0), independently re-confirmed by this verifier's own pypdf extraction against a freshly re-downloaded artifact"
  gaps_remaining: []
  regressions: []
gaps: []
human_verification:
  - test: "D-03 check 4 (SC#4/I18N-03) — visually inspect the CORRECTED Japanese PDF (current SHA-256 23885dcdab16955b62a7110d62f234493f7b53fe96bb07b4452fc7267368355c, re-confirmed live by this verifier at https://typsphinx.readthedocs.io/_/downloads/ja/latest/pdf/) at the pages previously carrying the defect and confirm 発/単/釈 render as real Han ideographs, not tofu/blank boxes"
    expected: "発, 単, 釈 render as real Han ideographs on every page where they occur in the corrected artifact"
    why_human: "Text-extraction equality (this verifier independently re-confirmed NUL count 0/0, page count 94=94, text length 128,285 chars matching on both sides, and the three target codepoints present as real characters, not NUL) is structurally blind to glyph substitution — a wrong-but-present glyph extracts as correct Unicode text while potentially still looking wrong on the rendered page. Plan 07's owner tofu-check answer (発/釈 confirmed tofu, 単 indeterminate) was recorded against the SUPERSEDED pre-fix artifact (SHA 4eb8497e...) and does not carry forward to this different-bytes, different-font corrected artifact (SHA 23885dcd...) — this specific human check has never been performed on the artifact currently being served. Plan 11 attached a `<verify><human-check>` for exactly this re-check, deferred to end-of-phase per `workflow.human_verify_mode`."
---

# Phase 30.1: Translations Repository + Japanese RTD Site Verification Report

**Phase Goal:** Japanese readers get typsphinx's documentation from Read the Docs as actual Japanese
prose, switchable through RTD's own flyout, served from a separate translations repository
(`typsphinx-doc-translations`) registered as an RTD translation project of the `typsphinx` parent — with
the `docs/locale/ja/` catalogs relocated there, the locale tooling moved with them, the submodule pin
kept current automatically, and the Japanese PDF proven glyph-correct rather than merely built.

**Verified:** 2026-07-26T12:00:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap-closure round (plans 07-11), following a prior `gaps_found` (3/5)
verification.

## Note on evidence provenance and this pass's own re-measurement

`30.1-EVIDENCE.md` (append-only, 26 sections after this round, deliberately not named
`30.1-VERIFICATION.md` per PD-03) records the gap-closure round's own rigorous, self-consolidated
criterion status. This verifier did not merely re-read that record — for every claim that changed
status since the prior `gaps_found` verification, this pass independently re-executed the check against
live systems, in a fresh process, and compares below:

- Re-fetched `/ja/latest/`, `/en/latest/` and the documentation root over real HTTP (all `200`).
- Re-downloaded both the RTD-served Japanese and English PDFs fresh (not reusing any cached bytes) and
  confirmed their SHA-256 hashes match the values `30.1-EVIDENCE.md`'s final gap-closure section
  records for the post-fix artifacts (`23885dcd...` ja, `c4416251...` en).
- Ran `pypdf` independently (own script, not the evidence file's `measure_pdf.py`/`compare_texts.py`)
  against the freshly downloaded ja PDF: 94 pages, 0 NUL bytes, and 発/単/釈 present as real Unicode
  characters (counts 5/3/2) rather than as `\x00` — corroborating, from a second independent
  implementation, the evidence's own "0/0 NUL, 94=94 pages" verdict.
- Independently enumerated the ja PDF's embedded `/BaseFont` list and confirmed it matches the
  evidence's recorded post-fix set exactly (`NotoSerifCJKjp-{Regular,Bold,SemiBold}`, `UnDotum`, no
  `HanaMinA`/`UnPen`/`STIX`).
- Re-queried the RTD public v3 API for both projects' build lists and the ja project's `translations/`
  record; re-confirmed `count: 1` (`typsphinx-ja` under the parent) and that no undisclosed rebuild has
  since superseded the artifact this report evaluates.
- Read `docs/source/conf.py` and `docs/source/_typst/custom_template.typ` directly to confirm the
  option-b fix (custom `typst_template` + explicit `derive_typst_lang()` re-derivation) is actually
  wired in the current tree, not merely described in the evidence narrative.
- Byte-diffed the staged `translations-repo/.readthedocs.yaml` and `.github/workflows/update-pin.yml`
  against the live `typsphinx-doc-translations` repository's `main` branch via the GitHub Contents API —
  both byte-identical (SHA-256 match), confirming no staged/live drift.
- Ran the full non-slow test suite locally (`633 passed, 29 deselected`), matching the evidence's own
  recorded baseline.
- Re-read the gap-closure round's own owner-observation sections in full (H1 flyout, H2 tofu-check) to
  determine whether the two previously-open human items are actually closed by this round, rather than
  taking the evidence file's own summary table at face value (see SC#2 finding below, where this
  verifier's own reading reaches a different conclusion than the evidence file's closing table).

## Goal Achievement

### Observable Truths (ROADMAP.md § Phase 30.1 Success Criteria — the authoritative contract)

| # | Truth (Success Criterion) | Status | Evidence |
|---|---|---|---|
| SC#1 | `/ja/latest/` serves actual translated strings from the relocated catalogs, matched against specific msgstr values, on a full-coverage docname | ✓ VERIFIED | Independently re-fetched `user_guide/builders.html` (contains `typsphinxは、異なる用途に対応する2つのビルダーを提供しています。`, `200`) and `examples/basic.html` (`lang="ja"`, `200`). Unchanged since the prior verification pass; this round did not touch SC#1. |
| SC#2 | RTD's flyout offers en↔ja from both sites; ja project's version list independently activated | ✓ VERIFIED | The machine-verifiable half (parent's `translations/` API record, `/_/addons/` data source both directions, `latest` version `active: true, built: true`) was already verified previously and is re-confirmed live in this pass. The one previously-open human half — does the flyout **widget** itself show the switch — is now answered: `30.1-EVIDENCE.md` § "Gap round — SC#2 flyout, owner answer and fresh re-measurement" (Plan 07) records the owner's direct, fresh, in-session reply: `flyout_on_en=yes`, `flyout_on_ja=yes`, `linked_under_parent=yes` — explicitly reconciling the earlier contradictory `linked_under_parent=no`. This is a genuine owner observation of the actual widget (not a data-source proxy), and unlike D-03 check 4 it is not tied to an artifact that has since been superseded (the flyout widget's behavior does not depend on which PDF is currently served) — so it is not stale. Counted VERIFIED, not `human_needed`, on that basis. |
| SC#3 | The translations repository builds the *current* English source via an automated, observed submodule-pin advance | ✓ VERIFIED | Was FAILED in the prior pass (workflow died at `git submodule update --remote` — missing branch-tracking ref). Plan 08 added an explicit `git -C typsphinx fetch --depth=1 origin "refs/heads/$branch:refs/remotes/origin/$branch"` step (confirmed present in both the staged file and, via GitHub Contents API SHA-256 match, the live `typsphinx-doc-translations` repository). Plan 09 then triggered and observed the repaired workflow end to end: the pushed commit's gitlink hunk shows `Subproject commit` moving `dcc6a523...`→`5297d38...`; ja build `33762438` matched on `commit` field, `finished`/`success`, its raw log's resolved submodule SHA agreeing exactly; two independent page probes (`contributing.html`'s new `Translations` section text, the WR-01 `--recurse-submodules` correction) both present on the rebuilt `/ja/latest/`; the previously-Japanese `user_guide/builders.html` re-confirmed unregressed. This verifier independently re-fetched the ja project's build list (7 builds now on record, latest `33762884` matching the Plan 11 fix commit) and confirms no regression back to the pre-fetch-fix failure mode. |
| SC#4 | The Japanese PDF is glyph-correct, not merely built: page count/text/CJK-font checks recorded, owner confirms sampled pages render Japanese | ⚠️ human_needed | Was FAILED in the prior pass on machine evidence alone (10 NUL bytes replacing 発/単/釈, 95-vs-94 page mismatch). Plan 10 root-caused it to a Typst-internal font-selection/shaping failure (CID `0` emitted, tagged to the Latin body font `LibertinusSerif`, despite `HanaMinA` being present, Typst-enumerated, and already used successfully elsewhere in the same document). The owner selected option-b (custom `typst_template` with an explicit font fallback list, `accept_phase29_impact=yes`); Plan 11 applied it (confirmed live in `docs/source/conf.py` + `docs/source/_typst/custom_template.typ`), rebuilt both projects, and re-ran D-03 checks 1-3 against a freshly rebuilt same-source local baseline: page count 94=94, extracted text byte-for-byte equal (125,950 chars, `text_equal=True`), NUL count 0 on both sides. This verifier independently re-downloaded the currently-served PDF (SHA-256 `23885dcd...`, matching the evidence's recorded post-fix artifact exactly — no undisclosed rebuild since) and independently re-extracted its text with a separate `pypdf` invocation: 94 pages, 0 NUL bytes, 発/単/釈 present as real characters. **Checks 1-3 are a clean, independently-corroborated MET.** Check 4 — the owner's visual confirmation that the corrected pages actually render Japanese rather than a wrong-but-present glyph substitution that text extraction cannot see — has **not yet been performed against this corrected artifact**. Plan 07's owner tofu-check answer applies only to the superseded pre-fix PDF (SHA `4eb8497e...`) and `30.1-EVIDENCE.md` itself states this explicitly, attaching a fresh `<verify><human-check>` to Plan 11 for exactly this re-check. Per this project's own verification standard (I18N-03 not satisfied while check 4 is open), SC#4/I18N-03 is `human_needed`, not verified — even though every machine signal is now clean. |
| SC#5 | `/ja/stable/` reachable the same way `/en/stable/` is, or the two-repository tag procedure is recorded as an explicit handoff | ✓ VERIFIED (discharged by handoff) | Unchanged since the prior pass: no tag exists yet (`stable` cannot exist before `v0.6.4`), and the two-repository release procedure is recorded in the translations repository's own `README.md` § "Release procedure" and restated in `30.1-EVIDENCE.md` § "Handoffs" item 6 — an explicit handoff into Phase 33's release procedure, not a false serving claim. |

**Score:** 4/5 roadmap success criteria VERIFIED (SC#1, SC#2, SC#3, SC#5). 1/5 (SC#4) is `human_needed` —
every machine-checkable signal is now a clean pass; only the owner's visual re-confirmation on the
corrected artifact remains open.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `typsphinx-doc-translations` GitHub repository (external) | Populated, current, and correctly building | ✓ VERIFIED | Live `.readthedocs.yaml` and `.github/workflows/update-pin.yml` on `main` byte-identical (SHA-256) to the staged copies in this repository; ja project's build list (7 builds) shows a healthy, current history with the newest build (`33762884`) matching Plan 11's fix commit; `translations/` API record still lists `typsphinx-ja` under the parent. |
| `.planning/phases/.../translations-repo/.readthedocs.yaml` (staged) | ja build manifest, current with the option-b font fix, no leftover diagnostic scaffolding | ✓ VERIFIED | Read directly: `build.jobs.build.pdf`/`post_create_environment`/`python.install` shape unchanged from Wave-2 verification; Plan 10's temporary diagnostic `pre_build` block is absent (confirmed by direct read — no `font_paths`/diagnostic step present); byte-identical to the live pushed copy. |
| `.planning/phases/.../translations-repo/.github/workflows/update-pin.yml` (staged) | D-08 automated pin-bump workflow, now actually reaching its commit/push steps | ✓ VERIFIED (upgraded from the prior pass's ⚠️ STUB) | The missing branch-fetch step is present (confirmed by direct read); an end-to-end run (Plan 09) actually moved the pin and propagated to `/ja/latest/`. The defect this verifier previously confirmed by inspection is now closed by the same method (direct file inspection) plus a live, independently-corroborated run. |
| `docs/source/_typst/custom_template.typ` (new, this round) | Custom template applying an explicit font fallback for the option-b fix | ✓ VERIFIED | Present, substantive (not a placeholder — a byte-for-byte copy of `typsphinx/templates/base.typ` except the documented `set text(...)` font-list line and its rationale comment block), wired (`docs/source/conf.py`'s `typst_template = "_typst/custom_template.typ"` references it), and its output verified to differ from the pre-fix font set on the actual served PDF (`NotoSerifCJKjp-*` replacing `HanaMinA`/`UnPen`/`STIX`). Its four `@preview` import lines still match `writer.py`/`template_engine.py`/`templates/base.typ` (independently re-grepped). |
| `docs/source/conf.py` (`typst_template` + `derive_typst_lang()` re-derivation) | Explicit re-derivation of Typst `lang` for the custom-template path | ✓ VERIFIED | Read directly: `typst_template = "_typst/custom_template.typ"` at line 122; `from typsphinx.template_engine import derive_typst_lang` + `typst_elements["lang"] = _typst_lang` immediately below, closing the gap that `TemplateEngine.uses_bundled_default_template()` returning `False` for any explicit `typst_template` would otherwise silently open (confirmed by code review and independently re-read here). `_resolve_language()` and the `language = _resolve_language()` assignment (Phase 29's seam) are unchanged. |
| `docs/source/contributing.rst` § "Translations" | Documents the translations repository workflow, including the WR-01 (old) `--recurse-submodules` fix | ✓ VERIFIED, with one standing quality Warning | The old WR-01 (missing `--recurse-submodules`) is fixed and re-confirmed live in the rendered `/ja/latest/contributing.html`. A **new** WR-01 was found by this round's code review (`30.1-REVIEW.md`) and independently re-confirmed here: the section still has no install step before `make locale-update`, and the evidence's own "positive control" run for the *old* WR-01 fix only succeeded because it overrode `SPHINXBUILD`/`PATH` to point at this repository's **own** pre-existing dev venv — not something a genuinely fresh contributor with no other typsphinx checkout would have. This is a real, reproducible documentation gap (see Anti-Patterns below), not phase-blocking (no ROADMAP success criterion names contributor-doc self-sufficiency), but it is not fully resolved either. |
| `tests/test_readthedocs_config.py` (D-05 rationale amendment) | Comment/message-only rewording | ✓ VERIFIED (unchanged from prior pass) | `assert` count 39, `def test_` count 4, all green. |
| `docs/locale/ja/` in *this* repository | Untouched (relocation deletion deferred to Phase 30) | ✓ VERIFIED | `git status --porcelain docs/locale/` empty. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `update-pin.yml`'s checkout step | `update-pin.yml`'s `git submodule update --remote` step | Explicit `git -C typsphinx fetch --depth=1 origin refs/heads/$branch:refs/remotes/origin/$branch` | ✓ WIRED (was ✗ NOT_WIRED in the prior pass) | Fix confirmed present by direct read; the repaired chain was exercised end to end by an observed run, not merely inspected. |
| Parent milestone branch tip | Translations-repo gitlink | D-08 pin-bump workflow's `git submodule update --remote` + commit + push | ✓ WIRED | Gitlink hunk exhibited (`dcc6a523...`→`5297d38...`), matched against `origin/gsd/v0.6.4-read-the-docs-migration`'s tip at trigger time. |
| Translations-repo push | ja RTD project rebuild | GitHub webhook → RTD build trigger | ✓ WIRED (both directions now measured) | Push-to-translations-repo → rebuild confirmed positively for the first time this round (build created within 1 second of the push); push-to-parent → no rebuild confirmed negatively (as expected, RESEARCH A3). |
| `docs/source/conf.py`'s `typst_template` | `docs/source/_typst/custom_template.typ`'s explicit font list | Sphinx config → TemplateEngine → typst-py's `compile()` | ✓ WIRED | Confirmed both by direct file read and by the served PDF's embedded font list actually changing to the fonts named in the template (not merely a manifest edit that never reached the artifact). |
| `docs/source/contributing.rst` "Translations" section | A genuinely fresh contributor's ability to run `make locale-update` unaided | `git clone --recurse-submodules` → `cd` → `make locale-update` | ⚠️ PARTIAL | Submodule-population half fixed and proven (old WR-01). Toolchain-availability half (`sphinx-build`/`sphinx-intl` importable) is undocumented; the round's own positive-control run depended on an out-of-band venv from this repository, not from following the documented steps alone. |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| I18N-01 | 30.1-01..05, 07, 08, 09 | Reader can browse Japanese docs on RTD and see actual Japanese prose (not 100% English on a green build), staying current | ✓ SATISFIED | SC#1, SC#2, SC#3, SC#5 (its four constituent success criteria) are all now VERIFIED. REQUIREMENTS.md still lists I18N-01 as `Pending` — that traceability table is updated separately from this report and is expected to be updated to reflect this verification's outcome. |
| I18N-03 | 30.1-01, 03, 06, 10, 11 | Japanese PDF downloadable with CJK glyphs actually rendered, not silently substituted | ⚠️ human_needed | SC#4's three machine checks are a clean, independently-corroborated pass. The requirement's own literal bar ("actually rendered, not silently substituted") includes a human confirmation that a text-layer fix did not merely relocate the defect somewhere text-extraction cannot see it — that confirmation has not yet been taken against the corrected artifact. Not BLOCKED (no failing evidence); genuinely `human_needed`. |

No orphaned requirements: REQUIREMENTS.md's Traceability table maps exactly I18N-01 and I18N-03 to Phase
30.1 (I18N-02 is Phase 30's). Every plan's `requirements:` frontmatter field across all 11 plans draws
only from {I18N-01, I18N-03}; no plan declares an ID absent from REQUIREMENTS.md, and no ID mapped to
this phase in REQUIREMENTS.md is absent from every plan.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `docs/source/contributing.rst` | 301-311 | "Translations" section documents clone + `make locale-update` with no toolchain-install step; the round's own verification evidence shows a genuinely fresh clone fails with `sphinx-build: command not found` / `ApplicationError: Cannot find source directory` unless the reader happens to have another typsphinx checkout's venv on `PATH` | ⚠️ Warning | Code-review finding (this round's `30.1-REVIEW.md` WR-01, new form — distinct from the already-fixed old WR-01 about `--recurse-submodules`). Not phase-blocking: no ROADMAP success criterion asserts contributor-doc self-sufficiency for the locale tooling, and this is a documentation-completeness gap rather than a defect in what gets served. Independently re-confirmed by this verifier reading the plan's own "positive control" evidence, which relied on an environment override (`SPHINXBUILD`/`PATH` pointed at this repository's own dev venv) not documented anywhere in `contributing.rst`. |
| `docs/source/_typst/custom_template.typ` | whole file (new, this round) | Fourth, unguarded `@preview` version-lockstep site — `tests/test_preview_version_sync.py`'s path constants and `examples/**/*.typ` glob do not reach `docs/source/_typst/` | ⚠️ Warning | Code-review finding WR-02, independently re-confirmed here (`grep` for the constants and glob path confirms the gap). Versions currently match all three canonical sites; a future version bump applied only to the canonical three (or to `examples/`) would silently leave this file stale with zero CI signal — the exact failure class the version-sync guard exists to prevent, reintroduced through an uncovered fourth copy. Not phase-blocking (no milestone invariant or success criterion is violated today; this is a forward-looking drift risk). |
| `.planning/.../translations-repo/.readthedocs.yaml`, `.github/workflows/update-pin.yml` (now also live in `typsphinx-doc-translations`) | whole files | No structural test coverage anywhere in this repository's `tests/` tree for either file's shape (PDF-job structure, `post_create_environment` ordering, the pin-bump decision logic) | ⚠️ Warning | Code-review finding WR-03. A future edit to the parent's `.readthedocs.yaml` PDF job has no CI signal forcing the ja manifest to stay in lockstep. Not phase-blocking; explicitly scoped by the reviewer as a "close once promoted" item, and these files have now in fact been promoted to the live repository (Plans 03/08/09), which sharpens rather than resolves the gap — worth a follow-up, not a phase gate. |

Repo-wide scan for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` across every file this phase touches
(`docs/source/contributing.rst`, `docs/source/conf.py`, `docs/source/_typst/custom_template.typ`,
`tests/test_readthedocs_config.py`, and the entire staged `translations-repo/` tree) returned **zero
matches** — no unresolved debt markers.

### Behavioral Spot-Checks (this verifier's own, independent of EVIDENCE.md)

| Behavior | Command | Result | Status |
|---|---|---|---|
| Documentation root, `/en/latest/`, `/ja/latest/` all still serve | `curl -s -o /dev/null -w "code=%{http_code}"` (with `-L`) on all three | `200` / `200` / `200`, root redirects to `/en/latest/` | ✓ PASS |
| ja PDF is the corrected, post-fix artifact (no undisclosed rebuild since the evidence was recorded) | fresh download + `sha256sum` | `23885dcdab16955b62a7110d62f234493f7b53fe96bb07b4452fc7267368355c` — exact match to `30.1-EVIDENCE.md`'s recorded post-fix value | ✓ PASS |
| en PDF unregressed post-fix | fresh download + `sha256sum` | `c4416251151a17b4bbda3e645661427102dd989d7713acd212567bc7492e2915` — exact match to the evidence's recorded post-fix value | ✓ PASS |
| ja PDF: 94 pages, 0 NUL bytes, target codepoints present as real characters | independent `pypdf` script (separate implementation from the evidence's own scripts) | `pages=94 nul=0`; 発×5, 単×3, 釈×2 all present as real Unicode | ✓ PASS |
| ja PDF embedded fonts match the fix's expected post-change set | independent `pypdf` `/BaseFont` enumeration | `NotoSerifCJKjp-{Regular,Bold,SemiBold}`, `UnDotum`, no `HanaMinA`/`UnPen`/`STIX` | ✓ PASS |
| Staged/live translations-repo files still byte-identical | GitHub Contents API SHA-256 vs. local `sha256sum`, both `.readthedocs.yaml` and `update-pin.yml` | Exact match on both files | ✓ PASS |
| `update-pin.yml`'s branch-fetch fix still present | `grep` on the staged file | `git -C typsphinx fetch --depth=1 origin "refs/heads/$branch:refs/remotes/origin/$branch"` present, precedes `git submodule update --remote` | ✓ PASS |
| `docs/source/conf.py` wiring for the option-b fix | direct read | `typst_template` set, `derive_typst_lang()` re-derivation present, `_resolve_language()` seam unchanged | ✓ PASS |
| Full non-slow test suite green | `uv run python -m pytest -q -m "not slow"` | `633 passed, 29 deselected` | ✓ PASS |
| No debt markers introduced by this round | repo-wide grep over touched files | No matches | ✓ PASS |
| `typsphinx/` untouched by this phase | `git status --porcelain typsphinx/`; `git show --stat` on the one font-fix commit | Clean; the fix commit touches only `docs/source/conf.py` + `docs/source/_typst/custom_template.typ` | ✓ PASS |

### Human Verification Required

See frontmatter `human_verification` — one item: D-03 check 4 (SC#4/I18N-03), the owner's visual
inspection of the **corrected** Japanese PDF (current SHA-256 `23885dcd...`) to confirm 発/単/釈 render
as real Han ideographs rather than a substitution that only survived visually while the text layer
happened to break. This is legitimately human-only — every machine-checkable signal (page count, text
equality, NUL count, font-family presence) is now clean, and this project's own verification-culture
standard explicitly treats that pattern (all mechanical signals green) as exactly the shape a silent
font-substitution defect would also produce, which is why the human look stays load-bearing rather than
optional.

**Why SC#2's human item is different and is not repeated here:** the prior verification pass's other
open human item (SC#2's flyout-widget visual confirmation) was resolved by a direct, fresh owner
observation collected in Plan 07 (`flyout_on_en=yes`, `flyout_on_ja=yes`, `linked_under_parent=yes`) that
this verifier judges as unstale (the flyout widget's behavior does not depend on which PDF artifact is
currently served, unlike D-03 check 4) — see the SC#2 row above for the full reasoning. It is therefore
counted VERIFIED, not carried forward as a second open human item.

### Gaps Summary

**No gaps remain that block the phase goal.** Both of the prior verification pass's FAILED criteria are
now closed with evidence this pass independently re-derived rather than merely cited:

1. **SC#3** was completely unmet (workflow died before it could push anything). Plan 08 fixed the
   missing branch-fetch step; Plan 09 exercised the repaired workflow end to end and this verifier
   independently re-confirmed the fix is present in the live repository and that the ja project's most
   recent build reflects it, with no regression back to the earlier failure mode.

2. **SC#4's machine-checkable half** (page count, extracted text/NUL scan, embedded CJK font
   enumeration) found a genuine, specific content defect (10 NUL bytes replacing 発/単/釈) that is now a
   clean pass, independently re-verified: this verifier downloaded the currently-served PDF fresh,
   confirmed its SHA-256 matches the evidence's recorded post-fix artifact exactly (ruling out an
   undisclosed further rebuild), and independently re-extracted its text and font list with a separate
   script, reaching the same 94-page/0-NUL/correct-font-family result the evidence file records.

**One item remains open, and it is a legitimate human-verification item, not a gap:** the owner's visual
confirmation that the corrected artifact's 発/単/釈 actually render as real glyphs has not yet been taken
against this specific (post-fix) PDF — the only prior owner look at this defect was against a superseded
artifact. Per this phase's own stated standard, I18N-03 is not recorded as satisfied while that
confirmation is open, however clean the mechanical signals are.

Three quality-level findings from this round's code review (`30.1-REVIEW.md`) remain unresolved and are
carried forward as Warnings, not gaps: a documentation-completeness issue in `contributing.rst`'s
Translations section (no toolchain-install step, independently re-confirmed by this verifier reading the
evidence's own "positive control" run), an unguarded fourth `@preview` version-lockstep site
(`custom_template.typ`), and the staged/now-live `translations-repo` manifests having no structural test
coverage in this repository. None of these violate a ROADMAP success criterion or a milestone invariant;
they are follow-up material.

---

_Verified: 2026-07-26T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
