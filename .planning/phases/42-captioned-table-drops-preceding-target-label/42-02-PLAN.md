---
phase: 42-captioned-table-drops-preceding-target-label
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/figure_propagated_target_render_gate/conf.py
  - tests/fixtures/figure_propagated_target_render_gate/index.rst
  - tests/fixtures/figure_propagated_target_render_gate/image.png
  - tests/test_figure_propagated_target_render_gate.py
  - .planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-02.md
autonomous: true
requirements: [TBL-03]

must_haves:
  truths:
    - "A captioned figure preceded by a standalone target emits BOTH labels: the figure's own `<index:fig-name>` self-anchor postfix and the propagated `[#metadata(none) <index:fig-target>]` anchor — so SC#2's question ('do captioned figures share the drop?') is answered NO with a recorded, in-repo measurement rather than carried-over hearsay."
    - "`uv run pytest tests/test_figure_propagated_target_render_gate.py -v` passes on the UNFIXED tree — the figure path is already correct, so this gate is a permanent regression guard (D-09), not a RED-to-GREEN recording."
    - "The gate covers exactly D-10's three measured shapes — a `:name:`-carrying figure with a preceding target, a figure with no `:name:` and a preceding target, and a figure inside a bullet-list item. The two-consecutive-targets shape is deliberately NOT covered for figures."
    - "The new fixture uses standalone `.. _label:` target directives only. It contains no `:target:` directive option anywhere — that option produces a `reference`-wrapped figure through `visit_reference`, a different docutils mechanism that would not exercise `PropagateTargets` at all."
    - "`42-GATE-EVIDENCE-02.md` records the code-level reason figures are unaffected: `add_text` consults `self.in_table` and nothing else, so `depart_figure`'s own trailing `_emit_id_anchors` call firing before `self.in_figure = False` is harmless — there is no `in_figure`-gated buffer for it to be diverted into."
  artifacts:
    - tests/fixtures/figure_propagated_target_render_gate/conf.py
    - tests/fixtures/figure_propagated_target_render_gate/index.rst
    - tests/fixtures/figure_propagated_target_render_gate/image.png
    - tests/test_figure_propagated_target_render_gate.py
    - .planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-02.md
  key_links:
    - "`depart_figure`'s `[#figure(...) <label>]` self-anchor postfix (`ids[0]`) plus `_emit_id_anchors(node, skip_ids={ids[0]})` for the propagated remainder — the same two-mechanism pairing `depart_table` uses, which is why Phase 25 modelled the table path on it (D-04 of Phase 25)."
    - "`add_text`'s single-flag branch (`self.in_table` only) is the load-bearing asymmetry between the two paths; this gate is the tripwire if a future change ever adds an `in_figure`-gated buffer diversion."
  prohibitions:
    - "The existing fixture `tests/fixtures/figure_target_caption_render_gate/` must not be extended or reused as this gate's source. Its figures use the `:target:` directive option, a different docutils mechanism; reusing its index.rst would produce a gate that silently exercises none of D-10's three shapes while appearing to pass."
    - "A passing figure gate must not be presented as evidence that the table path is fixed. The two paths diverge in exactly one respect and this plan changes no production code."
---

<objective>
Add the permanent figure-side regression gate required by D-09/D-10, and record the SC#2 answer —
whether captioned figures share the table's propagated-target drop — as in-repo phase evidence.

Purpose: Phase 25 modelled the table path on the figure path (Phase 25 D-04). A durable gate stops
a future table-side change from being copied back into a broken figure path, and stops a future
`in_figure`-gated buffer diversion from silently reintroducing this phase's defect class on the
image side. The gate is GREEN from the first run: the figure path is already correct, so no RED
recording is required or possible for it (D-09).

Output: a new dedicated fixture covering D-10's three measured shapes, a permanent render-gate
module, and `42-GATE-EVIDENCE-02.md` discharging SC#2.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-CONTEXT.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-PATTERNS.md
@.planning/phases/42-captioned-table-drops-preceding-target-label/42-VALIDATION.md
@CLAUDE.md
</context>

<worktree_provisioning>
CLAUDE.md's worktree-isolated execution is the STANDING mode. Before ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Assert the shim landed: `for t in uv ruff; do readlink -f ".venv/bin/$t"; done` must print
   `/nix/store/...` paths. A self-pointing symlink silently reintroduces ~45 environmental
   integration-test failures and looks like the shim ran.
4. Run every subsequent command through `uv run`.
5. NOTE on the `<automated>` verify blocks in this plan: `&amp;&amp;` is XML-escaped
   `&&`. Unescape it before running the command in a shell. Same for `&amp;` on its own.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: Create the figure propagated-target fixture covering D-10's three shapes</name>
  <files>tests/fixtures/figure_propagated_target_render_gate/conf.py, tests/fixtures/figure_propagated_target_render_gate/index.rst, tests/fixtures/figure_propagated_target_render_gate/image.png</files>
  <read_first>
    - `tests/fixtures/paragraph_propagated_target_render_gate/conf.py` and `.../index.rst` — the conf shape to copy and, critically, the standalone `.. _label:`-target-directive-before-node shape this gate needs.
    - `tests/fixtures/figure_target_caption_render_gate/index.rst` — read it ONLY to confirm the trap: its figures use the `:target:` directive option (a `reference`-wrapped figure), which is a different mechanism. Do NOT copy this file and do NOT edit it.
    - `tests/fixtures/figure_target_caption_render_gate/image.png` — the 68-byte image asset to COPY into the new fixture directory.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md` § 2 (the figure path analogue) and Pitfall 3 — why a new fixture is mandatory.
    - `typsphinx/translator.py` lines 2480-2531 (`depart_figure`) — the self-anchor postfix and `skip_ids` pairing the fixture must exercise. READ ONLY.
  </read_first>
  <action>
Create a NEW fixture directory `tests/fixtures/figure_propagated_target_render_gate/`. Copy
`tests/fixtures/figure_target_caption_render_gate/image.png` into it as `image.png` — that asset
only. Do not reuse that fixture's `conf.py` or `index.rst`, and do not modify anything under
`tests/fixtures/figure_target_caption_render_gate/`.

`conf.py`: mirror `paragraph_propagated_target_render_gate/conf.py`. `project` is
"Figure Propagated Target Render Gate", `author` is "typsphinx tests", `release` is "0.0.0",
`extensions` is the single entry `typsphinx`, and `typst_documents` is a single four-tuple making
`index` a master document (without it `TypstPDFBuilder.finish()` never calls `typst.compile()` and
the gate proves nothing). Set `numfig = True` for the `:numref:` on the named shape. The module
docstring must state that this fixture exercises docutils' `PropagateTargets` transform via
standalone target directives, and must explicitly contrast that with the `:target:` directive
option used by `figure_target_caption_render_gate/`, so nobody later merges the two.

`index.rst`: exactly D-10's three measured shapes, in this order, each figure carrying a caption
with a distinct ALLCAPS sentinel:

- Shape 1: `.. _fig-target:` immediately followed by `.. figure:: image.png` carrying
  `:name: fig-name` and a caption containing `FIGTGTNAMEDSENTINEL`.
- Shape 2: `.. _fig-target-noname:` immediately followed by `.. figure:: image.png` with NO
  `:name:` option and a caption containing `FIGTGTNONAMESENTINEL`.
- Shape 3: a bullet-list item with lead-in text, then — indented inside that same item —
  `.. _fig-target-li:` followed by `.. figure:: image.png` carrying `:name: fig-name-li` and a
  caption containing `FIGTGTLISTSENTINEL`.

Do NOT add a two-consecutive-targets shape: D-10 explicitly scopes the figure gate to three shapes
because the fourth was never measured for figures.

Then a references section: `:numref:` to `fig-name`; `:ref:` with EXPLICIT link text to each of
`fig-target`, `fig-target-noname`, `fig-target-li`, and `fig-name-li`. Explicit link text is
mandatory — a bare `:ref:` to a captioned figure defaults to that figure's caption text and
duplicates the sentinel. The figure-hyperlink directive option that plan-level acceptance
negative-greps for must not appear anywhere in this file, not even inside an rST comment
explaining why it is excluded — put that explanation in `conf.py`'s module docstring instead.

<!-- planner-discipline-allow: :target: -->
  </action>
  <acceptance_criteria>
    - `tests/fixtures/figure_propagated_target_render_gate/` contains `conf.py`, `index.rst`, and `image.png`, and `image.png` is byte-identical to `tests/fixtures/figure_target_caption_render_gate/image.png`.
    - `conf.py` contains the literals `typst_documents`, `typsphinx`, and `numfig = True`.
    - `index.rst` contains `.. _fig-target:`, `.. _fig-target-noname:`, `.. _fig-target-li:`, exactly three `.. figure:: image.png` directives, and the sentinels `FIGTGTNAMEDSENTINEL`, `FIGTGTNONAMESENTINEL`, `FIGTGTLISTSENTINEL`.
    - `index.rst` contains `:name: fig-name` and `:name: fig-name-li`, and contains no `:target:` option (`grep -c ':target:' index.rst` returns 0).
    - `uv run python -m sphinx -b typst -q -E` over the fixture exits 0, and the emitted `index.typ` contains `[#metadata(none) <index:fig-target>]`, `[#metadata(none) <index:fig-target-noname>]`, and `[#metadata(none) <index:fig-target-li>]` — the propagated anchors the figure path already emits correctly.
    - Nothing under `tests/fixtures/figure_target_caption_render_gate/` is modified, and `git status --porcelain typsphinx/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>OUT=$(mktemp -d); cmp tests/fixtures/figure_propagated_target_render_gate/image.png tests/fixtures/figure_target_caption_render_gate/image.png &amp;&amp; test "$(grep -c ':target:' tests/fixtures/figure_propagated_target_render_gate/index.rst)" = "0" &amp;&amp; uv run python -m sphinx -b typst -q -E tests/fixtures/figure_propagated_target_render_gate "$OUT" &amp;&amp; grep -q '\[#metadata(none) <index:fig-target>\]' "$OUT/index.typ" &amp;&amp; grep -q '\[#metadata(none) <index:fig-target-noname>\]' "$OUT/index.typ" &amp;&amp; grep -q '\[#metadata(none) <index:fig-target-li>\]' "$OUT/index.typ" &amp;&amp; test -z "$(git status --porcelain typsphinx/ tests/fixtures/figure_target_caption_render_gate/)"</automated>
  </verify>
  <done>A dedicated figure fixture exists covering D-10's three shapes via standalone target directives only, with the image asset copied rather than shared, and a real `-b typst` build already emits all three propagated anchors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the permanent figure regression gate module</name>
  <files>tests/test_figure_propagated_target_render_gate.py</files>
  <read_first>
    - `tests/test_paragraph_propagated_target_render_gate.py` — read the WHOLE file; it is the skeleton (import guard, `_run_sphinx_build_typstpdf` via `sys.executable -m sphinx`, `@pytest.mark.skipif`, the five assertion families with the `raw("...")`-stripping sweep).
    - `typsphinx/translator.py` lines 423-437 (`add_text`) and 2480-2531 (`depart_figure`) — the emitted anchor forms and the single-flag branch that is the reason figures are safe. READ ONLY; this task changes no production code.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md` § 2 — what the gate must assert so it catches a FUTURE regression, not merely today's correct behavior.
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-PATTERNS.md` § "tests/test_figure_propagated_target_render_gate.py".
  </read_first>
  <behavior>
    - Build the fixture once per class through `-b typstpdf`; returncode is 0 and stderr contains neither `does not exist in the document` nor `Typst compilation failed`.
    - Shape 1: the emitted `index.typ` contains `[#metadata(none) <index:fig-target>]` AND the figure self-anchor postfix ` <index:fig-name>]`.
    - Shape 2: the emitted `index.typ` contains `[#metadata(none) <index:fig-target-noname>]`. No assertion names the docutils auto id owning `ids[0]` here.
    - Shape 3: the emitted `index.typ` contains `[#metadata(none) <index:fig-target-li>]` AND ` <index:fig-name-li>]`.
    - No `index:`-namespaced label DEFINITION occurs twice (same negative-lookbehind-on-`link(` scan as the table gate, over `raw("...")`-stripped text).
    - The set of `link(<NAME>` names minus the set of `[#metadata(none) <NAME>]` anchor names is empty.
    - `index.pdf` exists, is non-empty, and starts with `%PDF`.
  </behavior>
  <action>
Create `tests/test_figure_propagated_target_render_gate.py` with the same skeleton as
`tests/test_paragraph_propagated_target_render_gate.py`, pointed at the new fixture, with class
`TestFigurePropagatedTargetRenderGate` guarded by `@pytest.mark.skipif(not TYPST_AVAILABLE, ...)`
and a class-scoped artifacts fixture that runs the build once and asserts nothing itself.

Module docstring: this is the D-09 PERMANENT regression gate, not a RED-to-GREEN recording. State
that the figure path is already correct today and record why, in code terms: `add_text` consults
`self.in_table` and no other flag, so `depart_figure`'s trailing
`_emit_id_anchors(node, skip_ids=set(node.get("ids", [])[:1]))` firing before `self.in_figure` is
cleared is harmless — there is no `in_figure`-gated buffer to divert into. State the gate's real
purpose: Phase 25 modelled the table path on the figure path, so this gate fails loudly if a future
change ever introduces an `in_figure`-gated buffer diversion or copies a broken table-side ordering
back onto the figure side. Also state the fixture-scoping rule so nobody merges this with
`figure_target_caption_render_gate/`: `:target:` is a directive OPTION producing a
`reference`-wrapped figure, whereas this gate exercises the standalone `.. _label:` target directive
and docutils' `PropagateTargets` transform.

Seven thin test methods over the single build, one behavior each, named so a failure names the
shape: a compile-clean test; one per D-10 shape; a no-duplicate-label-definition test; a
dangling-reference sweep; and the PDF magic-byte test. Implement each against the `<behavior>` block.
Restrict the duplicate-definition scan to `index:`-prefixed labels with a negative lookbehind on
`link(`, and strip `raw("...")` segments before every scan — the same two load-bearing details the
table gate uses.

Never `uv run sphinx-build` and never a bare `sphinx-build`; use the `sys.executable -m sphinx` form.
Commit with a `test(42-02): ...` message.
  </action>
  <acceptance_criteria>
    - `tests/test_figure_propagated_target_render_gate.py` defines `TestFigurePropagatedTargetRenderGate` with seven test methods.
    - The module contains the literals `[#metadata(none) <index:fig-target>]`, `[#metadata(none) <index:fig-target-noname>]`, `[#metadata(none) <index:fig-target-li>]`, ` <index:fig-name>]`, and ` <index:fig-name-li>]`.
    - The module invokes Sphinx via `sys.executable` and contains neither `uv run sphinx-build` nor a bare `sphinx-build` invocation.
    - `uv run pytest tests/test_figure_propagated_target_render_gate.py -v` PASSES on the unfixed tree (all seven methods green, or skipped only if `typst-py` is unavailable).
    - `uv run black --check tests/test_figure_propagated_target_render_gate.py` and `uv run ruff check tests/test_figure_propagated_target_render_gate.py` both exit 0.
    - `git status --porcelain typsphinx/` is empty at this task's commit.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain typsphinx/)" &amp;&amp; uv run black --check tests/test_figure_propagated_target_render_gate.py &amp;&amp; uv run ruff check tests/test_figure_propagated_target_render_gate.py &amp;&amp; uv run pytest tests/test_figure_propagated_target_render_gate.py -v</automated>
  </verify>
  <done>The permanent figure regression gate exists, passes GREEN against unfixed production source, covers exactly D-10's three shapes, and its docstring records why the figure path is safe and what future change it is designed to catch.</done>
</task>

<task type="auto">
  <name>Task 3: Record the SC#2 answer in 42-GATE-EVIDENCE-02.md</name>
  <files>.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-02.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § Phase 42 success criterion 2 — the exact question this file answers ("whether captioned figures exhibit the same drop is answered either way, with the measurement recorded").
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-RESEARCH.md` § 2 — the code-level explanation to re-derive here rather than transcribe.
    - `typsphinx/translator.py` lines 423-437 (`add_text`), 2480-2531 (`depart_figure`), 3249-3368 (`depart_table`) — the three regions whose asymmetry the evidence file must state. READ ONLY.
    - `.planning/phases/36-shared-emission-seam-cleanup/36-GATE-EVIDENCE.md` — the evidence-file tone: verbatim commands, verbatim outputs, an explicit verdict table.
  </read_first>
  <action>
Write `42-GATE-EVIDENCE-02.md` under this phase's directory. Every measurement must come from a
command actually run in THIS worktree — do not transcribe 42-RESEARCH.md's or 42-CONTEXT.md's
figures, which are reference material only.

Sections, in order:

1. THE QUESTION. Quote ROADMAP SC#2 verbatim and state the answer up front: captioned figures do
   NOT exhibit the propagated-target drop.

2. THE MEASUREMENT. The verbatim command and exit status of
   `uv run python -m sphinx -b typstpdf -q -E tests/fixtures/figure_propagated_target_render_gate <build>`,
   the fact that stderr contained neither the dangling-label fatal nor a compilation failure, and
   the emitted `.typ` excerpts for each of D-10's three shapes showing BOTH label forms — the
   `figure(...)` self-anchor postfix carrying `ids[0]` and the `[#metadata(none) <...>]` anchor
   carrying the propagated remainder. Include the produced `index.pdf` size and its first four bytes.

3. THE CODE-LEVEL REASON. Re-derive it here rather than asserting it: quote `add_text`'s body and
   state that `self.in_table` is the only flag it consults; quote `depart_figure`'s trailing
   `_emit_id_anchors` call together with the `self.in_figure = False` line that follows it, and
   state that this superficially matches `depart_table`'s ordering but is harmless precisely because
   no `in_figure`-gated buffer exists. Record the current line numbers you actually observed (the
   file grows every phase, so do not copy line numbers from any other document).

4. THE PERMANENT GATE (D-09/D-10). Name the module and class added by task 2, list its seven test
   methods against D-10's three shapes, record the verbatim passing
   `uv run pytest tests/test_figure_propagated_target_render_gate.py -v` output, and state that
   the gate is green against UNFIXED production source — so it is a forward-looking guard, not a
   RED-to-GREEN recording.

5. SCOPE NOTE. State that `tests/fixtures/figure_target_caption_render_gate/` was deliberately NOT
   reused because its `:target:` directive option produces a `reference`-wrapped figure through a
   different code path, and that the two-consecutive-targets shape is deliberately out of scope for
   figures per D-10.

6. VERDICT. A one-row-per-criterion table: SC#2 MET (answer recorded, measurement in section 2);
   D-09 MET (permanent gate, section 4); D-10 MET (three shapes, no fourth).
  </action>
  <acceptance_criteria>
    - `.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-02.md` exists.
    - It contains the literals `fig-target`, `fig-target-noname`, `fig-target-li`, and `metadata(none)`.
    - It states in words that captioned figures do NOT exhibit the drop, and names `add_text` and `in_table` as the reason.
    - It contains the verbatim passing pytest output for `tests/test_figure_propagated_target_render_gate.py`.
    - It contains a verdict table naming SC#2, D-09, and D-10.
    - It states that `figure_target_caption_render_gate` was not reused and why.
    - `git status --porcelain typsphinx/` is still empty at this task's commit.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-02.md; test -f "$F" &amp;&amp; grep -q 'fig-target-noname' "$F" &amp;&amp; grep -q 'fig-target-li' "$F" &amp;&amp; grep -q 'metadata(none)' "$F" &amp;&amp; grep -q 'add_text' "$F" &amp;&amp; grep -q 'in_table' "$F" &amp;&amp; grep -q 'figure_target_caption_render_gate' "$F" &amp;&amp; grep -q 'D-10' "$F" &amp;&amp; test -z "$(git status --porcelain typsphinx/)"</automated>
  </verify>
  <done>`42-GATE-EVIDENCE-02.md` answers SC#2 with an in-repo measurement, re-derives the code-level reason from the live tree, records the permanent gate and its passing output, and documents the two deliberate scope exclusions.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created by Phase 42 as a whole (owner plan in brackets). The plan-review
source-grounding pass must treat every entry here as newly created by this phase, not as drift.

Fixtures and test modules:
- `tests/fixtures/captioned_table_propagated_target_render_gate/conf.py` [42-01]
- `tests/fixtures/captioned_table_propagated_target_render_gate/index.rst` [42-01]
- `tests/test_captioned_table_propagated_target_render_gate.py` [42-01]
- class `TestCaptionedTablePropagatedTargetRenderGate` [42-01]
- `tests/fixtures/figure_propagated_target_render_gate/conf.py` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/index.rst` [42-02]
- `tests/fixtures/figure_propagated_target_render_gate/image.png` [42-02]
- `tests/test_figure_propagated_target_render_gate.py` [42-02]
- class `TestFigurePropagatedTargetRenderGate` [42-02]

Production symbol:
- local `was_captioned` in `TypstTranslator.depart_table` (`typsphinx/translator.py`) [42-04]

rST label ids introduced by the new fixtures:
- `tbl-target`, `tbl-name`, `tbl-target-noname`, `tbl-target-li`, `tbl-name-li`, `tbl-target-a`,
  `tbl-target-b`, `tbl-name-two` [42-01]
- `fig-target`, `fig-name`, `fig-target-noname`, `fig-target-li`, `fig-name-li` [42-02]

Caption sentinels:
- `TBLTGTNAMEDSENTINEL`, `TBLTGTNONAMESENTINEL`, `TBLTGTLISTSENTINEL`, `TBLTGTTWOSENTINEL` [42-01]
- `FIGTGTNAMEDSENTINEL`, `FIGTGTNONAMESENTINEL`, `FIGTGTLISTSENTINEL` [42-02]

Evidence and planning artifacts (all under
`.planning/phases/42-captioned-table-drops-preceding-target-label/`):
- `42-GATE-EVIDENCE-01.md` — table-side RED, SC#1 + SC#5 [42-01]
- `42-GATE-EVIDENCE-02.md` — figure-side answer + permanent gate, SC#2 [42-02]
- `42-GATE-EVIDENCE-03.md` — repo-wide misrouting sweep, D-06 + D-07 [42-03]
- `42-GATE-EVIDENCE-04.md` — table-side GREEN + regression sweep, SC#3 [42-04]
- `42-GATE-EVIDENCE-05.md` — caption-less byte invariance, SC#4 + D-04 [42-05]
- `42-SC4-INVARIANTS.md` — Phase 41 SC#4 sweep re-measured over a range including Phase 42, with the
  change-site to RED manifest, SC#6 [42-06]
- `42-CLOSEOUT-GUARD.md` — REL-04/REL-05 checkbox-flip guard, SC#6 [42-06]

Todo:
- `.planning/todos/pending/<YYYY-MM-DD>-table-whitespace-only-title-anchor-divergence.md` — D-08 [42-03]

Modified (not created): `CHANGELOG.md` gains one bullet under `## [0.7.0]` -> `### Fixed` [42-06];
`typsphinx/translator.py` `depart_table` [42-04].
</artifacts_this_phase_produces>

<planner_assumptions>
Flagged planner assumptions for this plan (surfaced, never silently dropped):

- The spec-less edge probe's `empty` row (zero/one/many input) is COVERED for the figure side by
  D-10's own shapes: one propagated id (all three shapes) and zero (`ids[0]` alone, exercised by
  the shape-2 auto-id case). The many case is deliberately out of scope for figures per D-10, and
  that exclusion is a locked user decision rather than a planner omission.
- The spec-less edge probe's `encoding` row is handled as a BACKSTOP truth in plan 42-01 and is not
  duplicated here — exactly one producer per predicate.
- Shape 2's `ids[0]` is a docutils auto id whose spelling depends on how many auto ids the document
  has already consumed; no assertion may name it.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rST source -> translator | Author-controlled target ids, `:name:` values, and captions cross into emitted Typst markup. |
| emitted `.typ` -> `typst.compile()` | A malformed label graph aborts the entire document build. |
| bundled image asset -> build output | `image.png` is copied by the builder into the build tree and referenced from the emitted Typst. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-04 | Denial of Service | `depart_figure` label emission (`typsphinx/translator.py`) | medium | mitigate | A future `in_figure`-gated buffer diversion, or a broken table-side ordering copied onto the figure path, would reintroduce the dangling-label build fatal on the image side. Mitigated by the permanent gate this plan adds (D-09), which fails on any of D-10's three shapes. |
| T-42-05 | Tampering | bundled `image.png` test asset | low | accept | The asset is a 68-byte file copied byte-for-byte from an existing in-repo fixture and is never fetched from the network. Verified by `cmp` in task 1's acceptance criteria. |
| T-42-06 | Spoofing | gate that appears to cover D-10 but exercises a different docutils mechanism | medium | mitigate | Reusing `figure_target_caption_render_gate/`'s `:target:`-option fixture would produce a green gate covering none of D-10's shapes. Mitigated by a dedicated fixture plus the acceptance criterion that `:target:` does not appear in the new `index.rst`. |
| T-42-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package. 42-RESEARCH.md's Package Legitimacy Audit records "not applicable" — no `[ASSUMED]`/`[SUS]`/`[SLOP]` entry exists, so no legitimacy checkpoint is required. `git diff -- pyproject.toml uv.lock` must stay empty (milestone invariant #1), verified in plan 42-06's invariant sweep. |
</threat_model>

<verification>
- `uv run pytest tests/test_figure_propagated_target_render_gate.py -v` passes against unfixed production source.
- `uv run pytest tests/test_pdf_render_gate.py -q` still passes — the pre-existing figure/table gates are uncontaminated.
- `grep -c ':target:' tests/fixtures/figure_propagated_target_render_gate/index.rst` returns 0.
- `uv run black --check .` and `uv run ruff check .` exit 0.
- `42-GATE-EVIDENCE-02.md` answers SC#2 explicitly and names the code-level reason.
</verification>

<success_criteria>
- SC#2 discharged: whether captioned figures share the drop is answered NO, with an in-repo recorded measurement rather than carried-over reference material.
- D-09 satisfied: a permanent figure regression gate exists and is green against unfixed source.
- D-10 satisfied: exactly the three measured shapes are covered; the two-consecutive-targets shape is absent by design.
- No production source, and no pre-existing fixture or test module, is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/42-captioned-table-drops-preceding-target-label/42-02-SUMMARY.md` when done.
</output>