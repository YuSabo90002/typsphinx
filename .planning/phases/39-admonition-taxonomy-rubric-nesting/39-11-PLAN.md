---
phase: 39-admonition-taxonomy-rubric-nesting
plan: 11
type: execute
wave: 6
depends_on: ["39-09"]
files_modified:
  - typsphinx/translator.py
  - tests/test_admonitions.py
  - tests/test_pdf_render_gate.py
autonomous: true
gap_closure: true
gap_ids: [G-39-1]
requirements: [ADM-02]

must_haves:
  truths:
    - "The danger handler passes the gentle-clues danger id and the attention handler passes the memo id. Exactly two call sites change; every other admonition call site is byte-unchanged."
    - "Exactly one call site passes the error id after this plan -- the error handler's own. Before this plan three did."
    - "No accent colour argument is introduced anywhere. D-01 is not reversed by D-03-R: a bucket is still expressed as a function name and the emitted admonition still carries no colour literal."
    - "Every docstring the routing change falsifies is rewritten in the same commit, naming D-03-R and G-39-1 rather than D-03. A docstring stating a routing the code no longer has is the same defect class as a stale test name."
    - "The generic .. admonition:: and the non-contents .. topic:: routing is byte-unchanged by this gap -- ADM-03's two call sites keep passing the notify and abstract ids, proven by the shipped bucket gate staying green rather than by inspection."
    - "This gap touches no rubric handler. visit_rubric, depart_rubric, visit_strong, depart_strong, visit_desc_signature and depart_desc_signature are byte-unchanged, and every rubric fixture and golden file is untouched, so ADM-05 and Phase 37's completed golden file stay fixed points."
    - "The two falsified in-process assertions in tests/test_admonitions.py are migrated by hand-derived expected strings taken from 39-CONTEXT.md's D-03-R table and the gentle-clues sources, never by copying a failure message, and their test function names are renamed in the same edit as their bodies."
    - "The compiled-PDF admonition gate is strengthened rather than merely re-worded: it gains an assertion that gentle-clues' own English default title for the memo id does not appear in the extracted text, so a lost title argument fails there as well as in the source-tier gate."
    - "The full fast suite is green at the end of this plan. Every RED plan 39-09 recorded has flipped, and no test outside 39-09's recorded RED inventory changed state in either direction."
  artifacts:
    - typsphinx/translator.py
    - tests/test_admonitions.py
    - tests/test_pdf_render_gate.py
  key_links:
    - "visit_danger and visit_attention -> _visit_admonition, whose emission mechanism is NOT changed; only the id argument moves"
    - "the two changed ids -> the wildcard gentle-clues import at typsphinx/writer.py:158, typsphinx/template_engine.py:615 and typsphinx/templates/base.typ:19, all three of which already bring memo and danger into scope, so no import edit and no pin move is required"
    - "_visit_admonition's admonitionlabels lookup -> keyed on the docutils node class name, which is unchanged by this gap, so both retitled types keep their catalog titles for free -- confirm, do not assume"
    - "the two changed call sites -> plan 39-09's RED inventory in 39-GATE-EVIDENCE-05.md, which flips GREEN as a result"
  prohibitions:
    - requirement_id: ADM-02
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT reach the new red-family members by passing an accent colour argument. D-03-R sub-divides the family by function name and leaves D-01 standing; a colour argument would satisfy every assertion in this gap while permanently putting a colour literal into every emitted admonition and enlarging the exact-string surface of every future change."
    - requirement_id: ADM-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT let the catalog title stop reaching the emitted box for either retitled type. gentle-clues resolves its own default title for every predefined id, so a dropped title argument renames the box in the reader's document while every routing assertion still passes -- the highest-risk regression in this gap and the reason plan 39-09 gated both locales first."
    - requirement_id: ADM-05
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT touch any rubric or desc_signature handler while editing the admonition zone of the same file. Phase 37's golden file is a completed fixed point and Phase 39's own rubric fix shipped in plan 39-06; an incidental edit there would put two closed workstreams back in play under a gap that mentions neither."
    - requirement_id: ADM-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT migrate a falsified expected string by copying it out of a failing test's output. Re-deriving by hand from the decision table is SC#5's standing discipline for this phase, and a string copied from output records what the code does rather than what it was asked to do."
---

<objective>
Land the routing change gap G-39-1 asks for: the danger handler passes the gentle-clues `danger` id,
the attention handler passes the `memo` id, and the error handler is untouched. Then migrate the two
in-process assertions the change falsifies and strengthen the compiled-PDF admonition gate against a
lost title argument.

Purpose: this is the gap's product change. It flips every RED plan 39-09 recorded — three assertions
in the bucket gate plus four box-open assertions in the two-locale title gate — and it is deliberately
two lines of behaviour change surrounded by the documentation and test work that keeps those two lines
honest.

Output: an edited `typsphinx/translator.py` with exactly two changed call sites and their corrected
docstrings, two renamed and re-derived tests in `tests/test_admonitions.py`, and a corrected,
strengthened `test_admonitionbuckettitlegate` in `tests/test_pdf_render_gate.py`. This plan does NOT
touch `visit_rubric`, `depart_rubric`, `visit_strong` or `visit_desc_signature`, does NOT re-run the
slow corpus gate (plan 39-13 owns it), and does NOT re-render the ADM-04 artifact (plan 39-12 owns it).
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-UAT.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-PATTERNS.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-05-SUMMARY.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-09-SUMMARY.md
@.planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-05.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Step 1 is load-bearing twice over here: this worktree must carry plan 39-09's new gate module and
fixtures, AND its `typsphinx` import must resolve to the worktree's own copy. Without the sync, the
worktree's pytest imports the MAIN checkout's package, so a correct two-line fix reports the wrong
colour and the executor is left debugging code that was never loaded.
</environment_setup>

<artifacts_this_phase_produces>
New symbols created by THIS plan:

- Renamed test functions in `tests/test_admonitions.py`: `test_danger_converts_to_error` →
  `test_danger_converts_to_danger_function`; `test_attention_converts_to_error` →
  `test_attention_converts_to_memo_function`.
- One new assertion inside `tests/test_pdf_render_gate.py`'s existing
  `TestAdmonitionPdfRenderGate::test_admonitionbuckettitlegate` — a negative check on gentle-clues'
  own English default title for the memo id. No new test function, no new class, no new fixture.

No new file is created by this plan. The two changed call sites in `typsphinx/translator.py` introduce
no new function, no new import and no new module-level name.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-route the two red-family call sites</name>
  <read_first>
    - `typsphinx/translator.py` lines 4400-4600 — every admonition `visit_*` handler in full,
      including docstrings. Re-derive the two target line numbers by grep before editing; the numbers
      quoted in 39-UAT.md (4517, 4525) predate later edits and the current definitions are at
      `def visit_danger` line 4536 with its call at 4542, and `def visit_attention` line 4548 with its
      call at 4554. Confirm both by `grep -n 'def visit_danger\|def visit_attention' typsphinx/translator.py`
      before touching anything.
    - `typsphinx/translator.py` lines 4337-4400 — `_visit_admonition` and `_depart_admonition` in
      full, including the `admonitionlabels` lookup keyed on the node class name. This plan changes
      what reaches the helper, never the helper itself.
    - `typsphinx/translator.py` lines 296-315 — the `_custom_admonition_title` attribute declaration,
      whose inline comment names danger and attention among the catalog-sourced types. Check whether
      the comment is still accurate after the routing change and correct it only if it is not.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` § "Reversal — recorded
      2026-08-02", decision D-03-R and its red-family table, as written by plan 39-10.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-05.md` — the RED
      inventory this task must flip, so the flip is checked against a written list rather than against
      whatever happens to be red at run time.
    - `~/.cache/typst/packages/preview/gentle-clues/1.3.1/lib/predefined.typ` — that both target ids
      are exported top-level functions in the pinned version.
  </read_first>
  <behavior>
    - A `.. danger::` directive emits a box opened by the gentle-clues danger function, carrying the Sphinx catalog title for danger.
    - A `.. attention::` directive emits a box opened by the gentle-clues memo function, carrying the Sphinx catalog title for attention.
    - A `.. error::` directive is unchanged in every respect.
    - The seven control types, the todo path, the generic admonition and the topic path are all unchanged.
    - No emitted admonition anywhere carries an accent colour argument.
  </behavior>
  <files>typsphinx/translator.py</files>
  <action>
Change the gentle-clues id passed at exactly two call sites, per D-03-R's table. Change nothing else
about how the box is opened: add no keyword argument, and in particular introduce no accent colour
argument anywhere (D-01 stands).

- `visit_danger`: pass the gentle-clues `danger` id — the package's own function for that type, accent
  `#fe640b`, icon `danger.svg` — instead of the `error` id it passes today.
- `visit_attention`: pass the gentle-clues `memo` id — accent `#e64553`, icon `excl.svg` — instead of
  the `error` id it passes today.

Leave `visit_error` exactly as it is.

Rewrite both handlers' docstrings in the same commit. Each currently cites D-03 and states that the
type folds into the single error-bucket function; both statements are now false. Replace them with the
governing decision — name D-03-R and gap G-39-1 — and state the type's own function id and the fact
that it remains in the red family. When writing these docstrings, name the clue function by its bare
id only; do NOT spell the helper-call form verbatim in prose, because an acceptance criterion below
counts occurrences of that call form in this file and a docstring copy would make the count wrong for
reasons that have nothing to do with the routing.

Also check `visit_error`'s docstring and the `_custom_admonition_title` inline comment for statements
the change falsifies, and correct only what is actually false. Record in the plan summary which
docstrings you checked and found already correct — positively, not by omission.

No import change is required. All three `@preview` import sites use the wildcard form
(`typsphinx/writer.py:158`, `typsphinx/template_engine.py:615`,
`typsphinx/templates/base.typ:19`), which was re-checked during UAT, so both ids are already in scope.
No `@preview` version moves, so `tests/test_preview_version_sync.py` is unaffected — run it as an
explicit criterion rather than assuming that.

Do not touch `visit_rubric`, `depart_rubric`, `visit_strong`, `depart_strong`,
`visit_desc_signature` or `depart_desc_signature` while you are in this file.
  </action>
  <acceptance_criteria>
    - `grep -c '_visit_admonition(node, "danger")' typsphinx/translator.py` returns exactly 1 — the inversion of the guard plan 39-05 recorded, which asserted this count was 0.
    - `grep -c '_visit_admonition(node, "memo")' typsphinx/translator.py` returns exactly 1.
    - `grep -c '_visit_admonition(node, "error")' typsphinx/translator.py` returns exactly 1 — down from 3, leaving only the error handler's own call.
    - `grep -c '_visit_admonition(node, "clue")' typsphinx/translator.py` returns 0 — the base clue function is still passed by no call site, unchanged from the shipped phase.
    - `uv run pytest tests/test_admonition_bucket_render_gate.py -v` is fully green, including both renamed defect-case tests, the red-family invariant, all seven controls, the ten-row catalog title table and the base-clue guard.
    - `uv run pytest tests/test_admonition_locale_title_precedence_gate.py -v` is fully green, including all four box-open assertions and the English compiled-PDF half.
    - `uv run pytest tests/test_preview_version_sync.py -x` is green.
    - `git diff -- typsphinx/translator.py` touches only the two handlers, their docstrings and — if it was actually false — the `_custom_admonition_title` comment. Prove the rubric and signature handlers are untouched: `git diff -U0 -- typsphinx/translator.py | grep -c 'rubric\|desc_signature\|visit_strong'` returns 0.
    - `uv run mypy typsphinx/` passes, and `uv run black --check typsphinx/ && uv run ruff check typsphinx/` pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_admonition_bucket_render_gate.py tests/test_admonition_locale_title_precedence_gate.py tests/test_preview_version_sync.py -q &amp;&amp; uv run mypy typsphinx/ &amp;&amp; test "$(grep -c '_visit_admonition(node, "error")' typsphinx/translator.py)" = "1"</automated>
  </verify>
  <done>Two call sites carry the new red-family ids, exactly one call site still passes the error id, both docstrings name D-03-R, and every gate plan 39-09 recorded RED is green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the two falsified in-process assertions and strengthen the compiled-PDF gate</name>
  <read_first>
    - `tests/test_admonitions.py` in full — every assertion, so the ones that must NOT be edited are
      identified positively rather than by omission. Only the danger and attention tests are falsified
      by this gap; the seealso, generic-admonition, note, warning, tip, hint, important, caution,
      error and todo assertions are not.
    - `tests/test_pdf_render_gate.py`, class `TestAdmonitionPdfRenderGate` in full — its class-scoped
      compile fixture, `test_admonition_pdf_has_no_literal_source_leak`, and
      `test_admonitionbuckettitlegate` including its docstring, which narrates a RED that the shipped
      phase already flipped and that this gap changes again.
    - `tests/test_pdf_render_gate.py`, class `TestTopicLineBlockRenderGate` — specifically
      `test_admonitiontitleregression_multichild` and the census note in `39-TEST-CENSUS.md` row 3
      recording that this function lives in that class, NOT in `TestAdmonitionPdfRenderGate`. Do not
      edit it; its four assertions are body sentinels and a directive-supplied title, none of which
      this gap touches.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-CONTEXT.md` D-14 and the D-03-R
      table — the rule that expected strings are re-derived by hand and never copied from failing
      output, and the table they are derived from.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-TEST-CENSUS.md` in full — the
      measured exact-string blast radius the shipped phase recorded, which plan 39-13 amends and which
      this task's counts must be reconcilable against.
    - `~/.cache/typst/packages/preview/gentle-clues/1.3.1/lib/lang.toml` `[lang.en]` — the package's
      own default title for the memo id, which the new negative PDF assertion excludes.
  </read_first>
  <files>tests/test_admonitions.py, tests/test_pdf_render_gate.py</files>
  <action>
In `tests/test_admonitions.py`, migrate exactly two tests and rename both:

- The danger test: its positive box-open assertion moves to the danger id's box-open form and its
  companion negative assertion — the one guarding against the markup-mode bracket form — moves to the
  same id. Rename the function so its name states the function the type now routes to rather than the
  one it folded into, and rewrite its docstring, which currently cites D-03 and states that danger
  folds into the single error-bucket function.
- The attention test: the same three edits, targeting the memo id.

Derive both new expected strings by hand from D-03-R's table and from the box-open shape
`_visit_admonition` emits, which you can read directly in the helper. Do not run the test first and
copy what it prints.

Leave every other assertion in that file byte-unchanged. In particular, the error test's assertions
are unchanged by this gap even though they assert the same id danger and attention used to assert —
record in the plan summary that you checked it and that it needs no edit, so the reason is on record
rather than inferred from the diff.

In `tests/test_pdf_render_gate.py`, work only inside `TestAdmonitionPdfRenderGate::test_admonitionbuckettitlegate`:

- Rewrite its docstring. It narrates a pre-phase RED that no longer exists and it describes the
  attention and danger boxes as rendering gentle-clues' defaults through the warning and danger
  functions. Replace that narrative with what the test now asserts under D-03-R, and state which of
  its assertions are bucket-independent — the three body sentinels and the three catalog title strings
  are all independent of which function boxes them, which is exactly why this test needed no
  expected-string migration and why it is being strengthened instead.
- Add one negative assertion: gentle-clues' own English default title for the memo id does not appear
  anywhere in the extracted PDF text. Transcribe that literal from `lang.toml`'s `[lang.en]` table
  with a comment naming the file and the table. This is the assertion that makes a lost title argument
  fail here as well as in the source-tier gate, and it is meaningful only for attention: for danger,
  the Sphinx catalog value and the package default are the same string in both locales, so no negative
  assertion is possible there. Say so in a comment rather than leaving a reader to wonder why danger
  has no matching guard.
- Leave the three sentinel assertions, the three catalog title assertions and the leak-signature loop
  exactly as they are.

Change nothing in `TestTopicLineBlockRenderGate`. Record in the plan summary the exact count of
assertions edited and left untouched in each file, so plan 39-13's census amendment can be checked
against it rather than re-derived.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_admonitions.py -v` is fully green.
    - `uv run pytest tests/test_admonitions.py -k "danger or attention" -v` selects exactly two tests and both pass.
    - `grep -c 'test_danger_converts_to_error\|test_attention_converts_to_error' tests/test_admonitions.py` returns 0 — both old names are gone, renamed rather than duplicated.
    - Exactly two `def test_` lines changed in that file: `git diff -U0 -- tests/test_admonitions.py | grep -c '^[-+].*def test_'` returns 4 (two removed, two added).
    - `uv run pytest tests/test_pdf_render_gate.py -k AdmonitionBucketTitleGate -v` selects at least one test and passes.
    - `uv run pytest tests/test_pdf_render_gate.py -k AdmonitionTitleRegression -v` selects at least one test and passes, and `git diff -- tests/test_pdf_render_gate.py | grep -c 'ADMONITIONNOTESENTINEL\|ADMONITIONWARNINGSENTINEL\|ADMONITIONCUSTOMSENTINEL\|Custom Title'` returns 0 — the multi-child regression test's four assertions were not touched.
    - The new negative assertion exists and is real: temporarily drop the title argument in a scratch edit to `_depart_admonition`, observe `test_admonitionbuckettitlegate` fail, revert, and record both the command and the observed failure in the plan summary.
    - `uv run pytest -m "not slow" -q` is fully green.
    - `uv run black --check . && uv run ruff check .` pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_admonitions.py -v &amp;&amp; uv run pytest tests/test_pdf_render_gate.py -k AdmonitionBucketTitleGate -v &amp;&amp; uv run pytest tests/test_pdf_render_gate.py -k AdmonitionTitleRegression -v &amp;&amp; uv run pytest -m "not slow" -q</automated>
  </verify>
  <done>Two in-process assertions are migrated by hand-derivation with their names, the compiled-PDF gate is corrected and strengthened with a proven-effective negative assertion, and the fast suite is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `sphinx.locale.admonitionlabels` → emitted `title:` argument → gentle-clues' linguify default | The precedence boundary. Both retitled types now ride ids that carry their own package default title, so a dropped argument silently renames the box. |
| rST directive → `_visit_admonition` → emitted `.typ` → `typst.compile()` | The emission chain. An id that does not exist in the pinned package is a build fatal rather than a wrong colour. |
| admonition zone of `translator.py` → rubric and `desc_*` zones of the same file | A large single file whose other zones are completed fixed points from Phases 36, 37 and 39-06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-27 | Spoofing | gentle-clues' own linguify default replacing the Sphinx catalog title once attention rides the memo id — the reader sees a box titled with the package's word instead of Sphinx's, while every routing assertion still passes | high | mitigate | Plan 39-09's two-locale gate is required green as an acceptance criterion of Task 1, and Task 2 adds a compiled-PDF negative assertion on the package's English memo default whose effectiveness is proven by a temporary scratch regression, observed and recorded. |
| T-39-28 | Tampering | An accent colour argument introduced to reach the new family members, putting a colour literal into every emitted admonition and enlarging the exact-string surface permanently | high | mitigate | Task 1 forbids it explicitly; the acceptance criteria pin the call form by exact count, so an extra keyword argument changes the count and fails. Reinforced by the first `must_haves.prohibitions` entry. |
| T-39-29 | Tampering | An incidental edit to the rubric or `desc_signature` handlers while working in the same 2700-line file, re-opening Phase 37's completed golden file or Phase 39-06's shipped rubric fix | high | mitigate | Task 1 forbids it and gates on `git diff -U0` containing no rubric, `desc_signature` or `visit_strong` line. The full fast suite, which includes the rubric render gates and the golden-file byte-identity check, is required green in Task 2. Reinforced by the third `must_haves.prohibitions` entry. |
| T-39-30 | Repudiation | A docstring left stating that the type folds into the single error-bucket function, so the next reader trusts documentation the code contradicts | medium | mitigate | Both docstrings are rewritten in the same commit naming D-03-R and G-39-1, and the plan summary records which other docstrings were checked and found already correct. |
| T-39-31 | Tampering | A migrated expected string copied out of a failing test's output, recording what the code does rather than what it was asked to do | medium | mitigate | Hand-derivation from D-03-R's table is required and is SC#5's standing discipline for this phase. Reinforced by the fourth `must_haves.prohibitions` entry. |
| T-39-SC | Tampering | npm/pip/cargo installs — supply chain | high | mitigate | This plan installs nothing, adds no dependency, and moves no `@preview` pin: both new ids are already in scope through the wildcard import at all three sites. `tests/test_preview_version_sync.py` is an explicit acceptance criterion rather than an assumption, and `git diff -- pyproject.toml` is empty over this plan's commits. |

ASVS L1: V5 Input Validation is the only applicable category — a build-time Sphinx extension has no
runtime authentication, session, access-control or cryptography surface (`39-RESEARCH.md` § Security
Domain; `39-SECURITY.md` carries the phase's register, whose numbering this table continues).
Blocking threshold is `high`. T-39-27, T-39-28, T-39-29 and T-39-SC are `high`; all four are
`mitigate` and all four mitigations land inside this plan. Nothing blocks.
</threat_model>

<verification>
- `uv run pytest tests/test_admonition_bucket_render_gate.py -v` and `uv run pytest tests/test_admonition_locale_title_precedence_gate.py -v` are both fully green — every RED in `39-GATE-EVIDENCE-05.md`'s inventory has flipped, checked row by row against that document rather than against whatever happens to be red.
- `uv run pytest tests/test_admonitions.py tests/test_topics.py -v` is fully green.
- `uv run pytest tests/test_pdf_render_gate.py -v` is fully green.
- `uv run pytest tests/test_preview_version_sync.py -x` is green — no pin moved.
- `uv run pytest -m "not slow" -q` is fully green, and the plan summary compares the pass/fail counts against plan 39-09's recorded post-RED counts so no test changed state unexplained.
- `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` all pass.
- `git diff -U0 -- typsphinx/translator.py | grep -c 'rubric\|desc_signature\|visit_strong'` returns 0.
- `git diff --stat -- pyproject.toml typsphinx/writer.py typsphinx/template_engine.py typsphinx/templates/base.typ` is empty.
</verification>

<success_criteria>
- The danger type routes to the gentle-clues danger id and the attention type to the memo id, with exactly one call site still passing the error id.
- No colour literal was introduced and no `@preview` pin moved.
- Both retitled types still carry their `sphinx.locale.admonitionlabels` title, proven in both locales at the source tier and in English in the compiled PDF.
- Two in-process assertions and two test names are migrated by hand-derivation; every other assertion in that file is byte-unchanged.
- The compiled-PDF admonition gate is strengthened by an assertion proven to fail when the title argument is dropped.
- The rubric and `desc_*` zones of `translator.py` are byte-unchanged and the full fast suite is green.
</success_criteria>

<output>
Create `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-11-SUMMARY.md` when done.
</output>
