---
phase: 44.1-relative-heading-depth-for-toctree-nesting
plan: 02
type: execute
wave: 2
depends_on: [44.1-01]
files_modified:
  - tests/test_translator.py
  - tests/test_topics.py
  - tests/test_pdf_render_gate.py
  - tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ
  - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md
autonomous: true
requirements: [TOC-01]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC#6: every rewritten assertion whose expected value changed is re-proved failing against the pre-fix commit before it is accepted — no value observed in a post-fix run is transcribed into an assertion without that proof"
    - "D-04 is honoured exactly: all 12 enumerated sites are edited — 7 positive assertions, 4 negative assertions, and 1 rationale docstring — with each edit traceable to TOC-01"
    - "D-05 is honoured: the positive sites stay unit tests asserting on the emitted string; none is promoted to a build-and-compile test"
    - "The 4 negative assertions do not become vacuously true: each is proved to fire against a constructed violating string, so the absence it asserts is still detected after the keyword change"
    - "EDGE(adjacency): the clamp boundary is asserted inclusively at both ends — a title at section_level 0 emits depth 1 and never depth 0, and a title at section_level 1 emits depth 1 — with the discharging test named for each end"
    - "SC#7 (topic half): a plain topic body, a multi-child topic title, and a `.. contents::` topic emit no heading at all — asserted keyword-agnostically, so no future keyword change can make these guards vacuous again"
    - "The repository carries no remaining absolute-level heading literal under tests/"
  artifacts:
    - path: "tests/test_topics.py"
      provides: "the rewritten clamp assertions, the keyword-agnostic topic guards, and the rewritten clamp rationale docstring"
    - path: "tests/test_translator.py"
      provides: "the three rewritten section-heading string assertions"
    - path: "tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ"
      provides: "the golden byte-comparison target updated to the emitted heading shape"
    - path: ".planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md"
      provides: "the per-site SC#6 re-proof record and the per-site non-vacuity record"
  key_links:
    - from: "tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ"
      to: "tests/test_desc_rubric_decoupling_render_gate.py"
      via: "byte-for-byte comparison of the emitted .typ against the golden file"
      pattern: "golden"
    - from: ".planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md"
      to: ".planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-01.md"
      via: "the pre-fix SHA the re-proof worktree is created at is the RED-recording commit evidence-01 names"
      pattern: "GATE-EVIDENCE-01"
  prohibitions:
    - requirement_id: TOC-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT leave any rewritten negative assertion vacuously true — an assertion that cannot fail must never be presented as coverage; each absence guard is proved to fire against a constructed violating subject before it is accepted"
    - requirement_id: TOC-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT edit an assertion to make the suite green — every edit encodes the new contract and is justified by a measurement, and any test failing for a reason outside the D-04 enumeration is reported as a blocker rather than adjusted"
---

<objective>
Migrate the 12 test sites that encode the buggy contract as an absolute-level heading literal, with
per-site proof rather than mechanical find-and-replace.

Purpose: SC#6 and SC#7's topic half, plus the adjacency edge criterion. D-04 approved the rewrite as
a policy; this plan does the edits and produces the evidence that each one is a real assertion and
not a laundered one. The four negative assertions are the highest-risk item in the set: after the
repair the old literal never appears in any output, so left alone they go vacuously true and
silently stop detecting.

Output: the four edited test artifacts and `44.1-GATE-EVIDENCE-02.md`, carrying a per-site re-proof
record (for the assertions whose expected value changed) and a per-site non-vacuity record (for the
absence guards, whose expected value did not change).
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-CONTEXT.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-PATTERNS.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-01-SUMMARY.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-01.md
@CLAUDE.md
</context>

<planner_measurements>
Re-measured during planning on 2026-08-04 at `a16deee` with
`grep -rn 'heading(level:' tests/ --include=*.py --include=*.typ`: **13** occurrences across **4**
files. Two of them (`tests/test_topics.py:161` and `:162`) are inside a single docstring, giving the
**12** edit sites D-04 enumerates. The census matched D-04 exactly; no site was added or lost.

**Site classification, measured (not assumed):**

| # | Site | Class | Can it be re-proved failing pre-fix? |
|---|---|---|---|
| 1 | `tests/test_translator.py:83` | positive, exact-string | yes — expected value changes |
| 2 | `tests/test_translator.py:97` | positive, exact-string | yes |
| 3 | `tests/test_translator.py:2199` | positive, exact-string | yes |
| 4 | `tests/test_topics.py:176` | positive, substring | yes |
| 5 | `tests/test_topics.py:208` | positive, substring | yes |
| 6 | `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ:20` | golden byte target | yes — the byte comparison flips |
| 7 | `tests/test_pdf_render_gate.py:2766` | **inert** — a hand-written reconstruction string, not translator output | **no** — see below |
| 8 | `tests/test_topics.py:62` | negative, absence | no — absence holds on both sides |
| 9 | `tests/test_topics.py:96` | negative, absence | no |
| 10 | `tests/test_topics.py:141` | negative, absence | no |
| 11 | `tests/test_topics.py:177` | negative, absence (depth-0 guard) | no |
| 12 | `tests/test_topics.py:161-162` | rationale docstring | n/a |

**Site 7, resolved.** `44.1-PATTERNS.md` flagged it for re-examination; planning measured it. The
literal sits inside `reconstructed_pre_fix_shape`, a hand-written string constant in
`test_stale_buffer_basis_second_sentinel_count_is_zero`. The two assertions in that test reference
only `TBLCAP_FIRST_SENTINEL` and `TBLCAP_SECOND_SENTINEL`; **no assertion in the file touches the
heading keyword**, and the string is never compiled or compared to translator output. It is
therefore inert: editing it cannot go red pre-fix, and not editing it cannot go red post-fix. D-04
lists it among the 12 owner-approved sites, so it is edited — but its SC#6 obligation is discharged
by recording that measured inertness, not by a re-proof that is impossible to construct. Do not
manufacture a red for it.

**Sites 8-10, resolved on in-repo precedent.** `tests/test_admonitions.py:279`, `:323` and `:448`
already assert the same class of absence **keyword-agnostically** (`"heading(" not in output`). That
form is strictly stronger than a keyword-specific one and can never go vacuous from a future keyword
change — which is exactly the failure mode D-04 names for these three sites. Adopt it. Site 11 is
different and must stay keyword-specific: a heading **is** emitted there, and the guard is against a
zero-valued one.

**Adjacency edge, resolved against existing tests — no new test needed.** The `section_level == 0`
end is `tests/test_topics.py::TestTitleLevelClamp::test_title_at_section_level_zero_clamps_to_one`
(lines 155-177), which asserts `translator.section_level == 0` before walking. The
`section_level == 1` end is
`tests/test_topics.py::TestTitleLevelClamp::test_title_with_multiple_children_in_heading_form_does_not_concatenate`
(lines 179-208), which wraps the title in a `nodes.section()` so `visit_section` raises the level to
1 before the title is emitted. Both ends are already asserted; this plan records the mapping rather
than adding a redundant test.

**The pre-fix SHA for re-proofs** is the RED-recording commit plan 01 Task 1 names in
`44.1-GATE-EVIDENCE-01.md` (`test(44.1-01): record RED for toctree relative heading depth gate`). It
is the last ancestor of the fix that contains no production change. Read the SHA from that file; do
not guess it.
</planner_measurements>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the 7 positive sites and re-prove each failing against the pre-fix commit</name>

  <files>tests/test_translator.py, tests/test_topics.py, tests/test_pdf_render_gate.py, tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ, .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md</files>

  <read_first>
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-01.md (the pre-fix SHA and the post-repair suite census this task edits against)
    - tests/test_translator.py lines 70-100 and 2185-2205 (the three positive assertion sites and their surrounding test bodies)
    - tests/test_topics.py lines 152-209 (TestTitleLevelClamp: both clamp tests, the rationale docstring at 161-162, and the positive assertions at 176 and 208)
    - tests/test_pdf_render_gate.py lines 2748-2796 (test_stale_buffer_basis_second_sentinel_count_is_zero — confirm for yourself that neither assertion references the heading keyword before editing the reconstruction string)
    - tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ (the whole file — it is short; line 20 is the only heading line)
    - tests/test_desc_rubric_decoupling_render_gate.py (how the golden file is compared, so the re-proof runs the right node id)
    - .planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md sections 1-3 (the two-worktree method and its `typsphinx.__file__` isolation proof — reuse its shape)
    - CLAUDE.md "Worktree-isolated execution" section
  </read_first>

  <action>
    Edit sites 1-6 so each asserts the relative depth keyword the translator now emits, changing
    nothing else about the assertion: `tests/test_translator.py:83` and `:97` keep their full
    exact-string form including the `, {text("Level 1")})` / `, {text("Level 2")})` tails and only
    the named argument changes; `:2199` likewise keeps `, {text("Test Comments")})`;
    `tests/test_topics.py:176` keeps its open-ended substring form; `:208` keeps its trailing `, {`;
    `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ:20` keeps its bracket-wrapped
    markup form and its trailing ` <index:desc-rubric-decoupling-render-gate>]` label.

    Edit site 7, `tests/test_pdf_render_gate.py:2766`, per D-04. Before editing, confirm by reading
    that neither assertion in that test references the heading keyword. After editing, extend the
    comment immediately above the assertion at line 2779-2782 so it states that the reconstruction
    tracks the translator's current emission keyword under TOC-01 while still modelling the
    stale-buffer shape it was written for. Do not restate the superseded keyword in that comment.

    Then produce the SC#6 re-proof. Read the pre-fix SHA from `44.1-GATE-EVIDENCE-01.md`. Create a
    detached throwaway worktree at that SHA under the scratch directory:
    `git worktree add --detach <scratch>/44.1-prefix <SHA>`. Provision it with
    `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Record the isolation proof —
    `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"` —
    and confirm the printed path lies inside that throwaway worktree and not in the main checkout or
    the executing worktree. If it does not, the run is void: re-provision and re-record before
    proceeding.

    Bring the NEW tests onto the OLD production code inside that worktree with
    `git checkout <this-plan's-test-commit-SHA> -- tests/test_translator.py tests/test_topics.py tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ`,
    then run each of sites 1-6 by node id and record the verbatim failure. Sites 1-3 and 4-5 are
    pytest node ids; site 6 is re-proved by running the golden-comparison test in
    `tests/test_desc_rubric_decoupling_render_gate.py` and recording its byte-difference failure.

    Write `44.1-GATE-EVIDENCE-02.md` in the phase directory, following the section structure of
    `42-GATE-EVIDENCE-05.md`: (1) the pre-fix SHA with `git log -1 --oneline` output, (2) the
    worktree isolation proof with the resolved `typsphinx.__file__` path, (3) a per-site table with
    one row per site 1-6 giving the node id, the command, and the verbatim failure line, and (4) a
    row for site 7 recording its measured inertness — that no assertion in its file references the
    heading keyword, so it carries no red obligation — stated as a measurement, not an excuse.
    Remove the throwaway worktree with `git worktree remove --force` and record that it is gone.

    Commit as `test(44.1-02): migrate positive heading-depth assertions with pre-fix re-proof`.

    <!-- planner-discipline-allow: heading(level: -->
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest tests/test_translator.py tests/test_topics.py tests/test_desc_rubric_decoupling_render_gate.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_translator.py -q` exits 0 and `uv run pytest tests/test_topics.py -q` exits 0.
    - Behavior: `uv run pytest tests/test_desc_rubric_decoupling_render_gate.py -q` exits 0 (the golden byte comparison matches the emitted output again).
    - Source: `grep -c 'heading(level:' tests/test_translator.py` returns `0`; `grep -c 'heading(depth:' tests/test_translator.py` returns `3`.
    - Source: `grep -c 'heading(level:' tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ` returns `0`.
    - Source: `grep -c 'heading(level:' tests/test_pdf_render_gate.py` returns `0`.
    - Red proof: `44.1-GATE-EVIDENCE-02.md` contains one row per site 1-6, each carrying a pytest node id and a verbatim failure line captured from a run inside a throwaway worktree checked out at the pre-fix SHA named in `44.1-GATE-EVIDENCE-01.md`.
    - Integrity: `44.1-GATE-EVIDENCE-02.md` records a `typsphinx.__file__` path that lies inside the throwaway pre-fix worktree; a path inside the main checkout or the executing worktree voids the proof.
    - Discriminating command for an environment failure: if a site's pre-fix run errors instead of failing its assertion, re-run it with `uv run pytest <node_id> -x -q --tb=long` inside the same worktree and record the traceback verbatim; an error that is not the expected assertion failure is reported as a blocker, never counted as the red.
  </acceptance_criteria>

  <reversibility rating="reversible">All seven edits are test-text substitutions in tracked files; `git revert` restores them exactly and no production behaviour depends on them.</reversibility>

  <done>Sites 1-7 are edited, sites 1-6 are each recorded failing against the pre-fix commit inside a proven-isolated worktree, site 7's inertness is recorded as a measurement, and the three affected test modules pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the 4 negative assertions and the rationale docstring, each with a non-vacuity proof</name>

  <files>tests/test_topics.py, .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md</files>

  <read_first>
    - tests/test_topics.py lines 30-100 (the two topic tests carrying the absence guards at 62 and 96)
    - tests/test_topics.py lines 99-150 (TestContentsTopicConversion and its absence guard at 141, plus the 135-139 comment explaining why the sibling `clue({` guard was deliberately left unchanged — follow that precedent for how a deliberate assertion choice is documented in this file)
    - tests/test_topics.py lines 152-178 (the rationale docstring at 161-162 and the depth-0 guard at 177)
    - tests/test_admonitions.py lines 270-300 and 440-455 (the in-repo keyword-agnostic absence precedent this task adopts for the three topic guards)
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md (as written by Task 1 — append, do not overwrite)
    - typsphinx/translator.py lines 762-800 (the buffer-swap branch that makes the topic guards true, and the clamp the docstring describes)
  </read_first>

  <action>
    Rewrite the three absence guards at `tests/test_topics.py:62`, `:96` and `:141` in the
    keyword-agnostic form already used at `tests/test_admonitions.py:279`, `:323` and `:448` — assert
    that no heading call of any kind appears in the output. Add a short comment above the first of
    the three, in the style of the existing 135-139 comment in the same file, recording that the
    guard was deliberately widened under TOC-01 so a future change to the heading call's named
    argument cannot make it vacuous, and naming the admonition tests as the precedent.

    Rewrite `tests/test_topics.py:177` keyword-specifically — a heading **is** emitted at that site,
    and the invariant is that its depth argument is never zero, so the guard keeps the argument name
    and the zero value and only the argument name changes.

    Rewrite the class docstring / test docstring at `tests/test_topics.py:161-162` per SC#4: the
    clamp survives because Typst's relative heading depth is likewise at least 1 and a computed zero
    would be rejected — it is no longer justified as an absolute-level floor. State that this
    supersedes D-06's stated reason. Do not restate the superseded keyword in the docstring text.

    Then produce the non-vacuity proof for all four guards. For each, construct a violating string
    that contains the substring the guard forbids, evaluate the guard's condition against it, and
    record that the condition is False — i.e. the assertion would fire. Use the in-repo
    reconstruction technique from `tests/test_pdf_render_gate.py:2754-2795`
    (`test_stale_buffer_basis_second_sentinel_count_is_zero`), which proves a gate is genuinely
    fail-first against a hand-built violating subject. Run these as one-off `uv run python -c`
    checks and record the commands and outputs; do not add them as permanent tests.

    Append a "Non-vacuity proofs" section to `44.1-GATE-EVIDENCE-02.md` with one row per guard
    (site 8, 9, 10, 11): the file and line, the guard's condition as written, the violating string
    used, and the recorded result showing the guard fires. In the same section, record the adjacency
    mapping: which existing test discharges the `section_level == 0` end of the clamp boundary and
    which discharges the `section_level == 1` end, each by node id, with the line number of the
    assertion that carries it.

    Finally run the full suite once (`uv run pytest tests/ -q`) and append the totals plus any
    remaining failing node ids to the same section. A failing node id outside the D-04 enumeration is
    a blocker: name it and stop rather than editing it.

    Commit as `test(44.1-02): widen topic absence guards and rewrite clamp rationale`.

    <!-- planner-discipline-allow: heading(level: -->
  </action>

  <verify>
    <automated>env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev &amp;&amp; uv run pytest tests/test_topics.py tests/test_admonitions.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_topics.py -q` exits 0 with every test in `TestTopicConversion`, `TestContentsTopicConversion` and `TestTitleLevelClamp` collected and passing.
    - Source: `grep -c 'heading(level:' tests/test_topics.py` returns `0`.
    - Source: `grep -c 'heading(depth: 0' tests/test_topics.py` returns `1` (the depth-zero guard, site 11).
    - Source: `grep -c '"heading(" not in output' tests/test_topics.py` returns `3` (sites 8, 9, 10 in their widened keyword-agnostic form).
    - Source: `grep -rc 'heading(level:' tests/ --include=*.py --include=*.typ` sums to `0` across the whole `tests/` tree.
    - Non-vacuity: `44.1-GATE-EVIDENCE-02.md` contains one recorded proof per guard (4 rows), each naming the violating string used and showing the guard's condition evaluating False against it.
    - Adjacency: `44.1-GATE-EVIDENCE-02.md` names the node id and assertion line discharging the `section_level == 0` end and the node id and assertion line discharging the `section_level == 1` end.
    - Behavior: `uv run pytest tests/ -q` exits 0, and its totals are recorded in `44.1-GATE-EVIDENCE-02.md`.
    - Discriminating command: if a non-vacuity check reports the guard NOT firing, print the constructed violating string with `repr()` before concluding anything — a guard that will not fire against a genuinely violating subject is a defective assertion and must be reported as a blocker, not rationalised.
  </acceptance_criteria>

  <reversibility rating="reversible">Four assertion lines and one docstring in a single test module; fully restorable by `git revert`.</reversibility>

  <done>All four absence guards are rewritten in a form proved to fire against a violating subject, the clamp rationale is rewritten per SC#4, the adjacency mapping is recorded, and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executing worktree → throwaway pre-fix worktree | test files from the new commit are checked out onto old production code; `import typsphinx` must resolve inside the throwaway tree |
| hand-constructed violating string → assertion condition | the non-vacuity proof feeds attacker-shaped input to the guard being validated |
| golden `.typ` fixture → byte comparison | a golden file edited to match observed output can silently encode a wrong contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44.1-06 | Tampering | pre-fix re-proof worktree resolving `import typsphinx` to the main checkout | high | mitigate | Task 1 records the resolved `typsphinx.__file__` path and the acceptance criteria void the proof unless it lies inside the throwaway worktree |
| T-44.1-07 | Repudiation | an assertion edited to make the suite green rather than to encode the contract | high | mitigate | every value-changing site carries a verbatim pre-fix failure line, every absence guard carries a non-vacuity proof, and site 7's exemption is recorded as a measurement of its file rather than as a claim |
| T-44.1-08 | Tampering | `golden.typ` edited to match observed output | high | mitigate | the golden edit is re-proved by running the golden-comparison test inside the pre-fix worktree and recording its byte-difference failure, so the new bytes are proved to be a real change of contract |
| T-44.1-09 | Information Disclosure | evidence file recording absolute scratch worktree paths | low | accept | planning artifact in a private repo; abbreviate throwaway roots as `<scratch>` following `42-GATE-EVIDENCE-05.md` |
| T-44.1-10 | Denial of Service | full-suite run inside a throwaway worktree | low | accept | only the named node ids are run pre-fix; the full suite runs once, in the executing worktree |
| T-44.1-SC | Tampering | npm/pip/cargo package installs | high | accept | no package is added by this plan; `pyproject.toml` and `uv.lock` are untouched (`git diff --stat <base> -- pyproject.toml uv.lock` is empty) and `uv sync --extra dev` resolves only already-locked dev dependencies, so there is no install task to gate |

ASVS level 1; block on `high`.
</threat_model>

<artifacts_this_phase_produces>
Symbols and files created by this plan (new — not expected to pre-exist in the codebase):

- **New file:** `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md`

Everything else this plan touches is an **in-place edit** of an existing artifact: `tests/test_translator.py`,
`tests/test_topics.py`, `tests/test_pdf_render_gate.py`, `tests/fixtures/desc_rubric_decoupling_render_gate/golden.typ`.
No new test function, class, fixture, helper or pytest mark is introduced — D-05 keeps the positive
sites as the unit tests they already are, and the adjacency edge is discharged by two existing tests
rather than by a new one.
</artifacts_this_phase_produces>

<verification>
- `uv run pytest tests/ -q` exits 0.
- `grep -rc 'heading(level:' tests/ --include=*.py --include=*.typ` sums to 0.
- `44.1-GATE-EVIDENCE-02.md` carries 6 pre-fix failure rows, 1 measured-inert row, 4 non-vacuity
  rows, and the adjacency mapping.
- `uv run black --check .`, `uv run ruff check .` exit 0.
</verification>

<success_criteria>
SC#6 discharged for all 12 D-04 sites — 6 by recorded pre-fix failure, 4 by recorded non-vacuity, 1
by recorded measured inertness, 1 (the docstring) by SC#4's rewrite. SC#7's topic half discharged
keyword-agnostically. The adjacency edge criterion is discharged and its discharging tests are named.
</success_criteria>

<output>
Create `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-02-SUMMARY.md` when done.
</output>
