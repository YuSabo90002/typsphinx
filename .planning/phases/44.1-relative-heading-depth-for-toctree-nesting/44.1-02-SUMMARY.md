---
phase: 44.1-relative-heading-depth-for-toctree-nesting
plan: 02
subsystem: testing
tags: [pytest, typst, heading-depth, toctree, test-migration, positive-control]

# Dependency graph
requires:
  - phase: 44.1-01
    provides: "visit_title emits heading(depth: ...) instead of heading(level: ...); visit_toctree emits a context-relative offset increment; the pre-fix anchor SHA 9d279250648ba2be2ca77f072e8655e6d6a689e7; the measured 9-item worklist of assertions the fix invalidates"
provides:
  - "tests/test_topics.py migrated to the relative heading-depth contract: all seven D-04-approved sites rewritten, each with a discharged non-vacuity proof"
  - "_section_title_output(app) positive-control helper, reusable by any future guard on the topic/contents buffer-swap path"
  - "44.1-GATE-EVIDENCE-02.md — the measured census, vacuity proof, per-site proof-obligation table, and full pre-fix/post-fix discharge for this file"
  - "Confirmed the remaining 7-item worklist for plan 03 (GATE-EVIDENCE-01's 9 minus the 2 sites this plan fixed)"
affects: [44.1-03, 44.1-04]

# Actuals (#2632)
actuals:
  tokens: 8488
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Positive-control assertion pattern for negative guards on a path that emits no output on either side of a fix: prove the searched literal IS producible by the same code in the same session on a DIFFERENT path, instead of chasing an unobtainable pre-fix RED."
    - "Matched-pair + independently-recomputed-expression assertion for a floor/clamp boundary: assert the clamp's own arithmetic (max(1, translator.section_level)) against the emitted numeral, not just the numeral's literal value, so the assertion goes red if the clamp's mechanism changes without its literal changing."

key-files:
  created:
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md
  modified:
    - tests/test_topics.py

key-decisions:
  - "The four negative guards on the topic/contents buffer-swap path (D-02) are proven non-vacuous by a positive control (_section_title_output), not by a pre-fix revert -- measured that a translator revert cannot make any of the four fail, because the buffer-swap early return intercepts before the generic section-heading path is reached on either side of the level->depth rewrite."
  - "The clamp test's negative half (heading(depth: 0 not in output) is discharged as a matched pair with its positive half plus a new adjacency-edge assertion binding the emitted numeral to max(1, translator.section_level) computed independently in the test, rather than an independent pre-fix RED -- the sub-floor literal is unproducible by construction on both sides of the revert."
  - "Module/class docstrings and the clamp test's own docstring reworded to cite TOC-01 instead of the superseded D-06 label, mirroring the exact source-side rationale plan 01 wrote in typsphinx/translator.py:797-803 so the two cannot drift apart."
  - "This session's environment had a working uv on PATH (symlinked into .venv/bin from the nix-store standalone binary) before any test ran, so the ~45 previously-environmental uv-subprocess failures GATE-EVIDENCE-01 recorded do not appear in this session's full-suite count; reconciled by arithmetic in GATE-EVIDENCE-02 Section 6 rather than assumed away."

patterns-established:
  - "Positive-control helper (_section_title_output) as the honest substitute for an unobtainable pre-fix RED on a negative guard whose path emits nothing on either side of a fix."

requirements-completed: [TOC-01]

coverage:
  - id: D1
    description: "The three topic/contents negative guards (test_topic_converts_to_clue_box, test_topic_title_with_multiple_children_does_not_concatenate, test_contents_topic_renders_boxless_bold_label) are rewritten to heading(depth: and each proven non-vacuous by a same-session positive control."
    requirement: "TOC-01"
    verification:
      - kind: unit
        ref: "tests/test_topics.py::TestTopicConversion::test_topic_converts_to_clue_box"
        status: pass
      - kind: unit
        ref: "tests/test_topics.py::TestTopicConversion::test_topic_title_with_multiple_children_does_not_concatenate"
        status: pass
      - kind: unit
        ref: "tests/test_topics.py::TestContentsTopicConversion::test_contents_topic_renders_boxless_bold_label"
        status: pass
    human_judgment: false
  - id: D2
    description: "The clamp test (TestTitleLevelClamp::test_title_at_section_level_zero_clamps_to_one) asserts the matched positive/negative pair over heading(depth:, plus a new adjacency-edge assertion binding the emitted numeral to max(1, translator.section_level)."
    requirement: "TOC-01"
    verification:
      - kind: unit
        ref: "tests/test_topics.py::TestTitleLevelClamp::test_title_at_section_level_zero_clamps_to_one"
        status: pass
    human_judgment: false
  - id: D3
    description: "The remaining positive assertion (multi-child heading form) is rewritten mechanically to heading(depth:."
    requirement: "TOC-01"
    verification:
      - kind: unit
        ref: "tests/test_topics.py::TestTitleLevelClamp::test_title_with_multiple_children_in_heading_form_does_not_concatenate"
        status: pass
    human_judgment: false
  - id: D4
    description: "The clamp rationale docstring and module/class docstrings are reworded to cite TOC-01 and mirror the source-side rationale in typsphinx/translator.py, retiring the superseded D-06 label from this file."
    verification:
      - kind: other
        ref: "grep -c 'D-06' tests/test_topics.py == 0; side-by-side reading against typsphinx/translator.py:797-803 (GATE-EVIDENCE-02 Section 5)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Every rewritten site's proof obligation (pre-fix RED or positive control) is discharged and recorded against the plan-01 anchor commit, with typsphinx/translator.py restored byte-clean afterward."
    requirement: "TOC-01"
    verification:
      - kind: other
        ref: "44.1-GATE-EVIDENCE-02.md Section 4 (pre-fix revert + verbatim pytest output) and Section 5 (verdict table, discharged: yes for all 7 rows)"
        status: pass
    human_judgment: false

duration: ~7min
completed: 2026-08-05
status: complete
---

# Phase 44.1 Plan 02: Test Migration for tests/test_topics.py Summary

**All seven D-04-approved sites in `tests/test_topics.py` rewritten to the `heading(depth: ...)`
contract, with the four previously-vacuous negative guards each carrying a same-session positive
control and the clamp boundary carrying a new adjacency-edge assertion binding the emitted numeral
to its own clamp expression.**

## Performance

- **Duration:** ~7 min (03:12:15 → 03:19:10 JST, first to last task commit)
- **Started:** 2026-08-05T03:11:xx+09:00 (approx, first commit 03:12:15)
- **Completed:** 2026-08-05T03:19:10+09:00
- **Tasks:** 3
- **Files modified:** 2 (`tests/test_topics.py`, `44.1-GATE-EVIDENCE-02.md`)

## Accomplishments

- Measured, in-session, that `tests/test_topics.py` carries 8 `heading(level:` grep hits (4
  negative, 2 positive, 2 prose spanning one rationale-comment site) — reconciled against
  44.1-CONTEXT.md D-04's 7-site figure as a counting-unit difference (line-level grep vs.
  docstring-paragraph "site"), not a divergence.
- Proved the four negative guards currently vacuous by direct measurement:
  `grep -rl 'heading(level:' typsphinx/` returns nothing, so no input to any of these tests could
  make the guards' literal appear.
- Added `_section_title_output(app)`, a module-level positive-control helper, and wired one call
  per topic/contents negative guard (3 guards, 3 calls) proving the searched literal IS producible
  by the same translator on a different path in the same session.
- Rewrote the clamp test's matched positive/negative pair to `heading(depth:` and added a new
  adjacency-edge assertion binding the emitted numeral to `max(1, translator.section_level)`
  computed independently in the test.
- Rewrote the mechanical positive assertion (multi-child heading form) and both docstrings (clamp
  test + module) to cite TOC-01 and mirror the exact source-side rationale plan 01 wrote in
  `typsphinx/translator.py:797-803`, retiring the superseded `D-06` label.
- Discharged every proof obligation against the plan-01 pre-fix anchor commit
  (`9d279250648ba2be2ca77f072e8655e6d6a689e7`) inside this worktree: a genuine revert-and-measure
  pass, not an assumed result — and recorded a nuance the plan's own prediction did not anticipate
  (the positive-control tests report FAILED as a whole pre-fix, but the -v traceback shows the
  raised line is always the control's own assertion, never the guard's, independently confirming
  the guard component carries no discriminating power on its own).
- Ran the full suite post-restore: 7 failed, 860 passed, 1 skipped — exactly
  GATE-EVIDENCE-01's 9-item worklist minus the 2 sites this plan fixed, handed to plan 03 with the
  reconciling arithmetic recorded.

## Task Commits

Each task was committed atomically:

1. **Task 1: Measure the census and prove the four negative guards are currently vacuous** —
   `a483cab` (docs)
2. **Task 2: Rewrite the seven sites and add a per-guard non-vacuity control** — `74bd9b5` (test)
3. **Task 3: Discharge the per-site proof obligations against the pre-fix anchor commit** —
   `378847b` (docs)

**Plan metadata:** committed separately by the orchestrator after wave completion (worktree mode
does not commit STATE.md/ROADMAP.md from this agent).

## Files Created/Modified

- `tests/test_topics.py` — all seven D-04 sites rewritten to `heading(depth: ...)`; new
  `_section_title_output(app)` helper; three positive-control call sites; clamp-arithmetic
  adjacency-edge assertion; module/class/test docstrings reworded to cite TOC-01.
- `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md` —
  new evidence file, Sections 0-6: worktree isolation proof, measured census, vacuity proof,
  per-site proof-obligation table, full pre-fix/post-fix discharge (including the traceback
  nuance), verdict table (discharged: yes for all 7 rows), and the full-suite reconciliation
  handed to plan 03.

## Decisions Made

- The three topic/contents negative guards are proven non-vacuous by a positive control, not a
  pre-fix revert — measured, not assumed, that a translator revert cannot make any of them fail
  (the buffer-swap early return intercepts before the generic heading path on both sides of the
  fix).
- The clamp test's negative half is discharged as a matched pair with its positive half plus the
  new clamp-arithmetic assertion, rather than an independent pre-fix RED, since the sub-floor
  literal is unproducible by construction.
- Docstrings reworded to mirror the exact source-side TOC-01 rationale wording rather than
  paraphrasing independently, so the two cannot drift apart (per plan instruction).
- Applied the documented NixOS `uv`/`.venv/bin` symlink workaround before running any test this
  session, which changed the full-suite failure count from GATE-EVIDENCE-01's 54 to 7 (45 fewer
  environmental failures) — reconciled explicitly by arithmetic in the evidence file rather than
  silently adopting a different baseline.

## Deviations from Plan

None — plan executed exactly as written. The traceback nuance recorded in
`44.1-GATE-EVIDENCE-02.md` Section 4 (positive-control tests reporting FAILED as a whole pre-fix,
via the control's own assertion rather than the guard's) is a more precise measurement than the
plan's own prediction ("record that step 4 shows them still passing"), not a deviation from the
plan's required proof shape — the guard line itself was confirmed, by the traceback, to never
raise, which is exactly what the plan's proof obligation required.

## Issues Encountered

- `ruff` cannot execute directly in this NixOS worktree sandbox
  (`Could not start dynamically linked executable: ruff`); no standalone nix-store `ruff` binary
  was found to symlink (unlike `uv`, which was symlinked successfully from
  `/nix/store/cgvijxnmydknslkl368k4j4j43akvl8b-uv-0.11.25/bin/uv`). Used the documented
  `nix-shell -p ruff --run "ruff check ..."` fallback for both task 2's file-scoped check and the
  plan-level `tests/` directory check — both passed clean.
- This session's full-suite count (`7 failed, 860 passed, 1 skipped`) differs from
  `44.1-GATE-EVIDENCE-01.md`'s recorded `54 failed, 813 passed, 1 skipped` by more than the 2 sites
  this plan fixed. Investigated and reconciled by arithmetic (813 + 2 fixed + 45 now-passing
  environmental = 860) rather than assumed: this session's `uv` symlink workaround, applied before
  any test ran, made the five `subprocess.run(["uv", "run", "sphinx-build", ...])`-based test files
  succeed instead of hitting the NixOS PATH-shadowing `returncode 127` GATE-EVIDENCE-01 recorded.
  No new failure and no unexplained survivor — the remaining 7 are exactly GATE-EVIDENCE-01 § 8b's
  9-item worklist minus this plan's 2 fixes.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `tests/test_topics.py` is fully migrated and green; all seven sites carry a discharged,
  recorded proof of non-vacuity.
- Plan 03 inherits a confirmed, measured 7-item worklist (not the document's larger estimate):
  `tests/test_desc_rubric_decoupling_render_gate.py::test_emitted_typ_is_byte_identical_to_golden`,
  `tests/test_toctree_requirement13.py::test_toctree_with_heading_offset`,
  `tests/test_toctree_requirement13.py::test_toctree_heading_offset_appears_once`,
  `tests/test_toctree_requirement13.py::test_toctree_single_entry_with_single_block`,
  `tests/test_translator.py::test_translator_heading_level_generation`,
  `tests/test_translator.py::test_toctree_generates_outline`,
  `tests/test_translator.py::test_comment_skipped`.
- `typsphinx/translator.py` is restored byte-clean (confirmed empty
  `git status --porcelain typsphinx/` both mid-task-3 and at plan end) — this plan touched
  production source only transiently, under `git checkout`, exactly per the plan's scope fence.
- The `_section_title_output(app)` positive-control pattern established here is reusable by any
  future guard on the topic/contents buffer-swap path.

## Self-Check: PASSED

- FOUND: `tests/test_topics.py`
- FOUND: `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-02.md`
- FOUND commit: `a483cab` (docs: measure census and vacuity proof)
- FOUND commit: `74bd9b5` (test: migrate test_topics.py)
- FOUND commit: `378847b` (docs: discharge proof obligations)

---
*Phase: 44.1-relative-heading-depth-for-toctree-nesting*
*Completed: 2026-08-05*
