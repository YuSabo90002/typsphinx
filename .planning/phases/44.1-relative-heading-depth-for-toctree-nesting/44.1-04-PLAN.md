---
phase: 44.1-relative-heading-depth-for-toctree-nesting
plan: 04
type: execute
wave: 3
depends_on: ["44.1-01", "44.1-02", "44.1-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - tests/test_heading_depth_render_gate.py
  - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md
autonomous: true
requirements: [TOC-01]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC#3 half (a), per D-02: over the full `docs/source` corpus and every root under `tests/roots`, each master document compiles before and after the fix and its OWN headings return an identical `typst.query(…, 'heading', field='level')` sequence — the master renders at offset zero, so its relative depth equals its absolute level."
    - "SC#3 half (b), per D-02 and its D-07 addendum: the substitution set is derived from the actual `git diff <pre>..<post> -- typsphinx/` rather than assumed, contains exactly the emitted-form changes that diff shows, and applying it to every pre-fix `.typ` — included documents as well as masters — leaves zero difference against the post-fix files."
    - "Neither half is accepted alone (D-02): half (a) would miss a non-heading diff and half (b) would not show that the substituted text still resolves to the same level. Both are recorded, and a positive control proves the pre-substitution diff was non-empty so the zero-diff result is not vacuous."
    - "D-01 is carried into REQUIREMENTS.md: TOC-01's body no longer claims master output must be byte-identical — a claim measurement falsified, since masters emit the same heading call as included documents — and it names both parts of the repair rather than only the `visit_title` half."
    - "SC#8: neither `typsphinx/templates/base.typ` nor the template file `_write_template_file` writes introduces a heading offset of its own, asserted by a permanent guard test over the real emitted template rather than by inspection alone."
    - "SC#7 discharge: every non-section-title path — admonition, topic, `.. contents::` and table caption — is covered by a named, currently-passing assertion, and the map from path to assertion is recorded so the criterion is checkable rather than restated."
    - "D-03 fence holds to the end of the phase: `typsphinx/templates/base.typ` is byte-identical between plan 01's pre-fix anchor commit and this plan's HEAD, and no `outline(depth: …)` expression was touched."
  artifacts:
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md
  key_links:
    - "the substitution set -> the actual source diff it must be derived from, never a remembered rule"
    - "each corpus master's own heading sites -> their resolved levels before and after the fix"
    - "TOC-01's published text in REQUIREMENTS.md -> the SC#3 wording D-01 already corrected in ROADMAP.md"
  prohibitions:
    - statement: "MUST NOT declare an invariance proof green without first showing it could have gone red. A zero diff is meaningless unless the pre-substitution diff is shown non-empty, and a pre/post comparison is meaningless unless each side is shown to have run its own copy of `typsphinx` — an unprovisioned worktree resolves the import to the main checkout and produces an empty diff for the wrong reason."
      status: unverified
      verification: test
    - statement: "MUST NOT widen the substitution set beyond what the source diff shows. An over-broad rule can manufacture an empty diff by normalising away a real change, which converts the phase's central evidence into a tautology. The set is derived from `git diff … -- typsphinx/` and every rule in it is traced to a hunk in that diff."
      status: unverified
      verification: test
---

<objective>
Prove that the two-site repair moved exactly what it claimed to move and nothing else, then bring the
published requirement text into agreement with what was measured.

SC#3 is the phase's central invariance claim and D-02 makes it deliberately redundant: resolved-level
equality alone would miss a non-heading diff, and normalised byte equality alone would not show that
the substituted text still resolves to the same level. This plan produces both halves over the full
`docs/source` corpus and every root under `tests/roots`, with the positive controls that stop either
half from passing vacuously.

It also closes the phase's paper trail. REQUIREMENTS.md's TOC-01 still carries the "byte-identical"
claim D-01 corrected in ROADMAP.md — measurement falsified it, because masters emit the same heading
call as included documents — and ROADMAP.md still carries a planning note describing a blocking
decision that D-07 has since answered. Both are in scope.

Purpose: SC#3, SC#7 and SC#8 are the criteria a verifier cannot discharge from code alone; they need
recorded measurement. D-01's correction is the one place this phase's own requirement text still
disagrees with its own findings.

Output: `44.1-GATE-EVIDENCE-04.md` carrying both invariance halves with their controls, a permanent
SC#8 guard test, an SC#7 discharge map, and corrected TOC-01 / ROADMAP text.

Scope fence (restated from plan 01's prohibitions): this plan writes evidence, one guard test, and
planning-document text. It changes nothing under `typsphinx/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-CONTEXT.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-PATTERNS.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-01-SUMMARY.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-02-SUMMARY.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-03-SUMMARY.md
@CLAUDE.md
</context>

<worktree_provisioning>
This plan runs in a GSD worktree AND creates two further detached worktrees of its own (one at the
pre-fix anchor commit, one at the post-fix HEAD). Every one of them must be provisioned separately.

For this plan's own worktree:

- `test -f .git` confirms isolation.
- `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
- Everything through `uv run`.

For each nested measurement worktree, per `CLAUDE.md` and the Phase 42 precedent:

- `git worktree add --detach <scratch>/<side> <SHA>`
- `(cd <scratch>/<side> && env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev)`
- `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"`
  inside it must print a path **inside that worktree**.

The reason is recorded in `CLAUDE.md` and is the single biggest way this plan's evidence could be
false without looking false: the main `.venv` holds a PEP-660 editable finder that resolves
`import typsphinx` to the MAIN checkout, so an unprovisioned pre-fix worktree would be built by
post-fix code and every diff would be empty for the wrong reason. Record each side's resolved path in
`44.1-GATE-EVIDENCE-04.md` § 0 before any build runs.

Remove the scratch worktrees with `git worktree remove --force` when done, and record that
`git worktree list` afterwards shows only the expected entries.
</worktree_provisioning>

<tasks>

<task type="auto">
  <name>Task 1: SC#3 half (a) — resolved-level invariance of every master across the corpus</name>

  <files>.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md</files>

  <precondition>
    Plan 01's test-only anchor commit is reachable from HEAD and changes no path under `typsphinx/`
    (read the 40-hex SHA from `44.1-GATE-EVIDENCE-01.md` § 5), AND nothing after plan 01 changed
    production source: `git diff --stat &lt;plan 01's fix commit&gt;..HEAD -- typsphinx/` must be empty, so
    the post-fix side of this measurement is a single well-defined state. Halt if either fails.
  </precondition>

  <read_first>
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-01.md
      (§ 5 for the anchor SHA, §§ 2/6/7 for the three measured outlines this plan cross-checks against)
    - .planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md
      in full — the established two-build method: naming the two commits, the per-worktree isolation
      proof, and the positive control. D-02 explicitly reuses it.
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-CONTEXT.md `<decisions>`
      D-01 and D-02 including D-02's 2026-08-05 addendum, and `<specifics>` items 2 and 3
    - ROADMAP.md § "Phase 44.1" SC#3 verbatim (its wording was rewritten under D-01; this task
      discharges the rewritten text, not the superseded one)
    - docs/source/conf.py — the corpus's `typst_documents` entry, its custom template, and the
      `derive_typst_lang` import the build needs
    - tests/roots/test-basic/conf.py — the second corpus's `typst_documents` entry
    - tests/test_heading_depth_render_gate.py — the `_query_heading_levels` / `_query_heading_outline`
      helpers plan 01 wrote, reused here rather than reimplemented
  </read_first>

  <action>
Measure the corpus, do not assume it. Run `ls tests/roots/` and enumerate the `.rst` files under
`docs/source` in this session, and record both counts in `44.1-GATE-EVIDENCE-04.md` § 1 "Corpus,
measured". 44.1-CONTEXT.md D-02 states one root and thirteen source files; confirm or correct that
here. Note explicitly that `docs/_build/` is not tracked in git, so the corpus must be **built** on
both sides — no committed build output may be used as either side of the comparison.

Name the two commits in § 2, each with `git log -1 --oneline <sha>` output and the full 40-hex SHA:

- **PRE-FIX** — plan 01's test-only anchor commit (the last commit that is an ancestor of the fix and
  contains no production change).
- **POST-FIX** — the current HEAD of this phase branch, having just proved in the precondition that
  nothing after plan 01's fix commit touched `typsphinx/`.

Create and provision both scratch worktrees per `<worktree_provisioning>` and record each side's
resolved `typsphinx.__file__` in § 0. Then, in each worktree, build both corpora:

- `uv run python -m sphinx -b typst docs/source <scratch>/<side>/out-docs`
- `uv run python -m sphinx -b typst tests/roots/test-basic <scratch>/<side>/out-roots`

Use the module form, not a PATH lookup of `sphinx-build`. Record both commands' exit codes and any
warnings. Four builds total.

**Now the measurement, and its one interpretive decision — record the decision, do not apply it
silently.** SC#3(a) asks that each master "compiles and returns an identical level sequence before and
after". Read literally over a *composed* document that cannot hold: `typst.query` follows `include()`,
so the sequence for a master with a toctree necessarily contains the included documents' headings,
and those are exactly what this phase moves. The satisfiable reading — and the one SC#3's own next
sentence supplies, "the master renders at offset zero, so depth equals level there" — is that the
**master's own** headings are invariant. Discharge it in two parts and record both:

- **A1, the master's own headings.** In a scratch copy of each build output, replace every non-master
  `.typ` with an empty file, then query the master. This isolates the master's own heading sites; the
  toctree scope is closed after its block, so stubbing the includes cannot shift them. Apply the
  identical stubbing transform to both sides. The two queried sequences must be **identical**. For a
  master with no toctree at all — measure which corpus masters those are rather than assuming — also
  record the un-stubbed full-document comparison, which for those masters must be identical too, and
  say so: that is the literal reading of SC#3(a) holding where it can hold.
- **A2, the composed delta.** Query each un-stubbed master on both sides and record the full
  `(level, title)` outlines side by side. These are **expected to differ** for masters with toctrees,
  and the difference is the phase's deliverable. Show, per moved heading, that its new level equals its
  old level plus its include depth. A2 is not a pass/fail criterion; it is the evidence that A1's
  invariance is not invariance-by-nothing-happening.

Reuse plan 01's `_query_heading_levels` and `_query_heading_outline` helpers by importing them, so the
`root=` binding and the JSON decoding are the same code the gate uses rather than a second
implementation that could differ.

Write § 3 "SC#3(a) result": the corpus table (master, corpus, has-toctree, A1 pre sequence, A1 post
sequence, identical yes/no), followed by the A2 outlines. Every row carries the command that produced
it.

Keep both scratch build trees on disk — task 2 diffs the same artefacts and must not rebuild them.
  </action>

  <verify>
    <automated>ls tests/roots/ &amp;&amp; find docs/source -name '*.rst' | wc -l</automated>
    <automated>git diff --stat $(grep -oE '\b[0-9a-f]{40}\b' .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-01.md | head -1)..HEAD -- typsphinx/ | tail -3</automated>
    <automated>uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"</automated>
    <automated>uv run python -m sphinx -b typst docs/source /tmp/44dot1-smoke-docs 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - § 0 records a resolved `typsphinx.__file__` path for each of the two scratch worktrees, and each
      path lies inside its own worktree — not in the main checkout and not in the other worktree.
    - § 1 records the measured corpus: the output of `ls tests/roots/` and the enumerated `.rst` list
      under `docs/source`, with a note stating whether they match 44.1-CONTEXT.md D-02's figures.
    - § 1 states that `docs/_build/` is untracked and that both sides were built rather than read from
      committed output.
    - § 2 names both commits with full 40-hex SHAs and `git log -1 --oneline` output, and shows the
      precondition check that nothing after plan 01's fix commit touched `typsphinx/`.
    - All four builds are recorded with their exact command lines and exit codes, and every exit code
      is `0`.
    - § 3 contains one row per master in the measured corpus, and every row's `identical` column reads
      yes for the A1 comparison.
    - § 3 records, for every master with no toctree, the un-stubbed full-document comparison as well,
      and it is identical.
    - § 3's A2 section shows the composed outlines side by side and accounts for every level change by
      the moved heading's include depth.
    - The stubbing transform is described precisely enough to re-run, and the evidence shows it was
      applied identically to both sides.
    - Both scratch build trees still exist at the end of this task for task 2 to consume.
  </acceptance_criteria>

  <done>
    Every master in the measured corpus has its own heading levels proven identical across the fix,
    the composed delta is recorded beside it so the invariance is visibly not vacuous, and the one
    interpretive decision SC#3(a) forces is written down rather than applied silently.
  </done>
</task>

<task type="auto">
  <name>Task 2: SC#3 half (b) — normalised byte invariance from a diff-derived substitution set</name>

  <files>.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md</files>

  <precondition>
    Task 1's two scratch worktrees and their four build outputs still exist on disk and their
    per-worktree `typsphinx.__file__` isolation proofs are recorded in § 0. Rebuilding here would
    invalidate the pairing between the two halves of the SC#3 evidence.
  </precondition>

  <read_first>
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-CONTEXT.md `<decisions>`
      D-02's 2026-08-05 addendum in full — it is the instruction that the substitution set is derived
      from the real source diff and that an over-broad rule can manufacture an empty diff
    - .planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md
      §§ 3-5 (the pairwise diff and the positive control)
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md §§ 0-3
  </read_first>

  <action>
Derive the substitution set from the source diff, then apply it.

Run `git diff <PRE-FIX SHA>..<POST-FIX SHA> -- typsphinx/` and record it verbatim in
`44.1-GATE-EVIDENCE-04.md` § 4 "Source diff". Read it and enumerate, in § 5 "Substitution set", every
hunk that changes an **emitted** string — a string that reaches a `.typ` file — as opposed to a
comment, a docstring, a local variable name or a type annotation. Each substitution rule gets its own
row naming the hunk it came from and the exact before-form and after-form.

Under D-07 there are **two** emitted-form changes, not one: the heading call's named argument and the
toctree scope opener. Note that CONTEXT's original D-02 framing assumed a single substitution and its
own addendum corrects that — so a one-rule set is a symptom of reading the wrong document rather than
the diff. Conversely, a rule that normalises anything the diff does not show changing is over-broad:
state explicitly, per rule, which hunk licenses it, and record that no rule lacks a hunk.

Then apply, over **all four** build outputs from task 1:

1. Take the pre-fix side's `.typ` files — masters **and** included documents; SC#3(b) covers both,
   because the point is proving the change did not leak anywhere.
2. Record the **positive control first**: diff the untouched pre-fix tree against the post-fix tree
   and show the result is NON-empty, listing which files differ and how many lines. If this diff were
   empty, the zero-diff below would prove nothing, and the most likely cause would be that one of the
   worktrees was unprovisioned and both sides ran the same code. Do not proceed past a non-empty
   result being demonstrated.
3. Apply the substitution set from § 5 to a scratch copy of the pre-fix tree — read and write every
   file with an explicit `encoding="utf-8"`, so the comparison is over UTF-8 bytes and a platform's
   default codec cannot change the verdict.
4. Diff the substituted pre-fix tree against the post-fix tree, recursively, across both corpora. The
   result must be **empty**. Record the command and its output.
5. Record a file inventory on both sides: the set of `.typ` paths must be identical (same names, same
   count) — a file present on one side only would be invisible to a content diff of matching names.

Write § 6 "SC#3(b) result": the positive control's non-empty diff summary, the substituted zero diff,
the file inventory comparison, and a one-line statement of what each of the three establishes.

Finally, § 7 "Both halves, jointly": state in three sentences why neither half alone suffices —
half (a) compares resolved levels and would not notice a non-heading textual diff, half (b) compares
bytes after normalisation and would not show that the normalised text still resolves to the same
level, and the positive controls are what stop either from passing on an empty measurement. Cross-check
§ 3's A1 sequences against `44.1-GATE-EVIDENCE-01.md` §§ 2 and 7 and note any disagreement rather than
reconciling it silently.

Remove the scratch worktrees and record `git worktree list` afterwards.
  </action>

  <verify>
    <automated>git worktree list</automated>
    <automated>test -z "$(git status --porcelain typsphinx/)" &amp;&amp; echo "OK: typsphinx/ untouched by this plan"</automated>
    <automated>uv run pytest tests/test_heading_depth_render_gate.py -q</automated>
  </verify>

  <acceptance_criteria>
    - § 4 contains the verbatim output of `git diff <PRE>..<POST> -- typsphinx/` with both full 40-hex
      SHAs in the command line.
    - § 5 lists every substitution rule with the diff hunk that licenses it; the number of rules over
      emitted strings is exactly `2`, and no rule lacks a named hunk.
    - § 6 records the positive control diff FIRST and it is non-empty, with the differing file list and
      line counts.
    - § 6 records the post-substitution recursive diff over both corpora and it is empty, with the
      command shown.
    - § 6 records that the `.typ` file inventories on the two sides are identical in names and count.
    - The substitution script or command shown in § 6 reads and writes with an explicit `utf-8`
      encoding.
    - § 7 states why each half is insufficient alone and cross-checks § 3's A1 sequences against
      `44.1-GATE-EVIDENCE-01.md`, noting any disagreement.
    - `git worktree list` at the end shows no leftover scratch worktree.
    - `git status --porcelain typsphinx/` is empty — this plan changed no production source.
  </acceptance_criteria>

  <done>
    The substitution set is provably derived from the source diff and contains exactly the two emitted
    changes; the pre-fix tree normalised by it is byte-identical to the post-fix tree across masters and
    included documents in both corpora; and the positive control plus the file inventory show the zero
    diff was reachable from a non-empty starting point rather than manufactured.
  </done>
</task>

<task type="auto">
  <name>Task 3: SC#7 and SC#8 discharge, and the TOC-01 / ROADMAP text corrections (D-01)</name>

  <files>
    tests/test_heading_depth_render_gate.py,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md
  </files>

  <read_first>
    - .planning/REQUIREMENTS.md lines 52-70 — TOC-01's body in full, including the superseded
      byte-identity sentence and the one-part statement of the repair
    - ROADMAP.md § "Phase 44.1" in full — the Goal prose (already updated for the two-part repair),
      SC#3's rewritten wording, SC#7, SC#8, and the trailing "Planning finding, 2026-08-04" note that
      describes a blocking decision D-07 has since answered
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-CONTEXT.md `<decisions>`
      D-01 and D-07, and `<canonical_refs>` "Requirements and roadmap"
    - typsphinx/templates/base.typ in full (it is short) — the heading and outline sites
    - typsphinx/builder.py — `_write_template_file` and the code path that writes the shared template
      to the output root
    - tests/test_heading_depth_render_gate.py — plan 01's module, so the SC#8 guard is appended in its
      style with its existing availability flag and marks
    - tests/test_admonitions.py lines 270-330 and 440-455, tests/test_translator.py lines 275-305,
      tests/test_pdf_render_gate.py line 2640, tests/test_topics.py (post plan 02) — the assertions the
      SC#7 map will name
  </read_first>

  <action>
**SC#8 — a permanent guard, not an inspection.** Append a new test class to
`tests/test_heading_depth_render_gate.py` asserting that nothing outside `visit_toctree` introduces a
heading offset. Two legs:

- A static leg over the packaged template source: read `typsphinx/templates/base.typ` with an explicit
  `encoding="utf-8"` and assert it contains no heading-offset set rule at all. Locate the file through
  the installed package (an `importlib.resources`-style or `Path(typsphinx.__file__).parent` lookup),
  not by a relative path from the test file, so the guard follows the package rather than the checkout
  layout.
- A live leg over what the builder actually writes: build any fixture project that produces a template
  file — `tests/fixtures/integration_nested_toctree` already does — and assert the emitted
  `_template.typ` at the output root contains no heading-offset set rule either. This is the half a
  static grep cannot cover, because `_write_template_file` composes what it writes.

Both legs assert absence, so both need a non-vacuity control: assert in the same test that the emitted
master `.typ` from that same build DOES contain the offset expression `visit_toctree` emits, proving
the searched construct is producible in that build and its absence from the template is informative.
Mark and guard the class like plan 01's, and record in its docstring that it discharges SC#8.

**SC#7 — a discharge map, not a restatement.** Write § 8 "SC#7 discharge map" in
`44.1-GATE-EVIDENCE-04.md`: one row per non-section-title path — admonition title, topic title,
`.. contents::` topic label, table caption — naming the test file, the test function and the exact
assertion that covers it, plus the assertion's shape. Note which of them are argument-name-agnostic
(they assert on the bare heading call rather than on a named argument, so they survived this phase
untouched and are the strongest evidence available) and which were rewritten by plan 02 and therefore
carry a positive control. Run every named test in this session and record the passing output beside
the map. Also record the structural claim the map rests on — that `visit_title` is the only place a
section heading is emitted, and the non-section paths return before it — with the grep or source
reading that establishes it, so the map is anchored to the code rather than to a recollection.

**D-01 — correct TOC-01 in REQUIREMENTS.md.** Its body still states that master documents render at
offset zero where depth equals level "so their output must be byte-identical". Measurement falsified
that: masters emit the same heading call as included documents, so the keyword change lands in master
output too. Replace that sentence with what SC#3 now says — the resolved level is invariant, not the
byte sequence, and the invariance is proven by resolved-level equality plus normalised byte equality.
While in the same entry, correct the one-part statement of the repair: the requirement's body says
"The repair is to emit `depth:`", which D-07 makes incomplete; state both parts, matching the wording
ROADMAP.md's Goal already uses. Do not change TOC-01's id, its checkbox state, its position, or any
other requirement — use scoped `Edit` calls, never a whole-file rewrite.

**ROADMAP.md — retire the superseded planning note.** Phase 44.1 carries two trailing block quotes: a
short "Superseded, 2026-08-05" marker added at re-plan time, and below it the original planning note.
Both go, replaced by a single corrected note. The original says the
`context`-relative change "exceeds the scope 44.1-CONTEXT.md locks, so plan 44.1-03 puts it to the
owner as a blocking `checkpoint:decision`". That is no longer true: the question was answered before
planning as D-07 option-a, and no plan in this phase carries such a checkpoint. Rewrite the note to
record what it is now — a measurement that falsified the original one-part premise, resolved by owner
decision D-07 on 2026-08-05, with the measured `[1, 2, 2]` versus `[1, 2, 3]` finding preserved because
it remains the reason the repair has two parts. Keep the note's evidentiary content; retire only its
now-false forward-looking sentence, and fold the re-plan-time "Superseded" marker into the same
corrected note so only one block quote remains. Use scoped `Edit` calls; do not rewrite the ROADMAP
file and do not touch any other phase's section.

Finally write § 9 "Phase verdict": a row per success criterion SC#1 through SC#8, each with the
artefact that discharges it and where that artefact lives. For any criterion this plan cannot discharge
from evidence in hand, write `human_needed` and the reason — never a bare assertion.
  </action>

  <verify>
    <automated>uv run pytest tests/test_heading_depth_render_gate.py -v</automated>
    <automated>test -z "$(grep -n 'heading(offset' typsphinx/templates/base.typ)" &amp;&amp; echo "OK: base.typ carries no heading offset (SC#8 static half)"</automated>
    <automated>uv run pytest tests/test_admonitions.py tests/test_topics.py tests/test_translator.py -q</automated>
    <automated>grep -c 'byte-identical' .planning/REQUIREMENTS.md</automated>
    <automated>uv run pytest -q 2>&1 | tail -15</automated>
    <automated>uv run ruff check . &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_heading_depth_render_gate.py` gains a test class with a static leg over the packaged
      `base.typ`, a live leg over the emitted `_template.typ`, and a non-vacuity control asserting the
      offset expression IS present in the same build's master output.
    - `grep -c 'importlib' tests/test_heading_depth_render_gate.py` returns at least `1`, OR the
      template lookup goes through `Path(typsphinx.__file__).parent` — the guard resolves the template
      through the installed package, not through a checkout-relative path.
    - `uv run pytest tests/test_heading_depth_render_gate.py -v` exits 0 with every test passing,
      including the new SC#8 class.
    - `grep -n 'heading(offset' typsphinx/templates/base.typ` returns nothing.
    - § 8 has one row per non-section-title path, each naming a test file, test function and assertion,
      and each named test is shown passing in this session's captured output.
    - § 8 records the structural claim that `visit_title` is the sole section-heading emission point,
      with the command or source reading that establishes it.
    - REQUIREMENTS.md's TOC-01 no longer contains the word `byte-identical`:
      `sed -n '52,70p' .planning/REQUIREMENTS.md | grep -c 'byte-identical'` returns `0`.
    - REQUIREMENTS.md's TOC-01 names both parts of the repair — `sed -n '52,75p' .planning/REQUIREMENTS.md
      | grep -c 'visit_toctree'` returns at least `1`.
    - `git diff .planning/REQUIREMENTS.md` shows changes confined to the TOC-01 entry; no other
      requirement id, checkbox or line is touched.
    - ROADMAP.md's Phase 44.1 trailing note no longer describes a pending blocking decision:
      `grep -c 'checkpoint:decision' .planning/ROADMAP.md` returns `0`, `grep -c 'Superseded, 2026-08-05'
      .planning/ROADMAP.md` returns `0` (the re-plan-time marker is folded in, not left alongside), and
      exactly one block quote remains under Phase 44.1 — still containing the measured `[1, 2, 2]` and
      `[1, 2, 3]` sequences.
    - `git diff .planning/ROADMAP.md` shows changes confined to the Phase 44.1 section.
    - § 9 has one row per criterion SC#1 through SC#8 with its discharging artefact, and any row this
      plan cannot discharge reads `human_needed` with a reason.
    - `uv run pytest -q` exits 0 across the whole suite; `uv run ruff check .`,
      `uv run black --check .` and `uv run mypy typsphinx/` all exit 0.
  </acceptance_criteria>

  <done>
    SC#8 is carried by a permanent two-leg guard with its own non-vacuity control rather than by
    inspection; SC#7 is discharged by a map from each non-section-title path to a named, currently
    passing assertion anchored to the code; TOC-01's published text agrees with what this phase
    measured; ROADMAP's superseded planning note is retired without losing its measurements; and the
    phase carries a criterion-by-criterion verdict with `human_needed` wherever evidence is short.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a scratch worktree -> the `typsphinx` its interpreter actually imports | A PEP-660 editable finder can resolve the import to the main checkout, so both "sides" run the same code |
| a substitution rule -> the source hunk that licenses it | An over-broad rule normalises away a real change and manufactures an empty diff |
| a zero diff -> the claim it supports | A zero diff proves nothing unless the un-normalised diff was non-empty |
| published requirement text -> what was measured | A requirement asserting an unsatisfiable property makes a verifier return NOT-MET against correct work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44.1-15 | Repudiation | the two scratch measurement worktrees | high | mitigate | An unprovisioned worktree imports the main checkout, so pre and post are built by identical code and the diff is empty for the wrong reason. Mitigated by a per-worktree `uv sync` and a recorded `typsphinx.__file__` resolving inside each worktree, captured in § 0 before any build. |
| T-44.1-16 | Tampering | the SC#3(b) substitution set | high | mitigate | An over-broad rule can normalise a real change out of existence. Mitigated by deriving every rule from a named hunk of `git diff … -- typsphinx/`, recording the diff verbatim, and requiring the emitted-string rule count to be exactly two. |
| T-44.1-17 | Spoofing | the zero-diff result | high | mitigate | A zero diff spoofs invariance if the inputs were already identical. Mitigated by recording the un-normalised positive control diff FIRST and requiring it to be non-empty, plus a `.typ` file inventory comparison so a file present on only one side cannot hide. |
| T-44.1-18 | Spoofing | the SC#8 absence guard | high | mitigate | Both legs assert absence and would pass on an empty or unbuilt input. Mitigated by a control in the same test asserting the offset expression IS present in the same build's master output. |
| T-44.1-19 | Tampering | scoped edits to REQUIREMENTS.md and ROADMAP.md | medium | mitigate | A whole-file rewrite of either would silently destroy entries outside the edit window — a recorded hazard in this project. Mitigated by requiring scoped `Edit` calls and by acceptance criteria asserting `git diff` is confined to the TOC-01 entry and the Phase 44.1 section respectively. |
| T-44.1-20 | Information disclosure | scratch worktrees under a temp path | low | accept | Worktrees hold only repository content already on disk and are removed with `git worktree remove --force`, verified by a final `git worktree list`. |
| T-44.1-SC | Tampering | package-manager installs | medium | mitigate | The nested worktrees each run `uv sync --extra dev`, resolving the project's already-pinned dependency set. No new dependency is added and `pyproject.toml` is not modified, so no `[ASSUMED]`/`[SUS]` package enters the tree; the invariance is asserted by `git status --porcelain pyproject.toml uv.lock` being empty at plan end. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and paths created by this plan:

- `tests/test_heading_depth_render_gate.py`: a new SC#8 source-fence test class with a static leg over
  the packaged `typsphinx/templates/base.typ`, a live leg over the emitted `_template.typ`, and a
  producibility control
- `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md`

Modified, not created: `.planning/REQUIREMENTS.md` (TOC-01's body only) and `.planning/ROADMAP.md`
(the Phase 44.1 trailing planning note only).

Created by sibling plans and referenced here: `tests/test_heading_depth_render_gate.py` itself plus
`_run_sphinx_build_typst`, `_query_heading_levels`, `_query_heading_outline`,
`TestToctreeHeadingDepthRenderGate` and `44.1-GATE-EVIDENCE-01.md` (plan 01); `_section_title_output`
in `tests/test_topics.py` and `44.1-GATE-EVIDENCE-02.md` (plan 02); the empty-entries toctree test in
`tests/test_toctree_requirement13.py` and `44.1-GATE-EVIDENCE-03.md` (plan 03); `emitted_depth` in
`typsphinx/translator.py` (plan 01).
</artifacts_this_phase_produces>

<flagged_assumptions>
1. **SC#3(a) cannot be read literally over a composed document, and this plan says so on the record.**
   `typst.query` follows `include()`, so a master with a toctree necessarily reports its children's
   headings — the very levels this phase moves. Task 1 discharges the satisfiable reading (the master's
   own headings, isolated by stubbing includes) and records the composed delta alongside it. A verifier
   reading SC#3(a) literally should read § 3's interpretation note before returning NOT-MET.
2. **The corpus figures are re-measured, not inherited.** 44.1-CONTEXT.md D-02 states one root under
   `tests/roots` and thirteen `.rst` files under `docs/source`. Task 1 re-counts both and records
   agreement or divergence.
3. **Encoding is asserted at the comparison layer only.** The probe's encoding edge is discharged by
   requiring explicit `utf-8` on every read and write in the substitution step, so the byte comparison
   has a defined meaning. Nothing in this phase changes how `typsphinx` itself encodes output, and no
   claim is made about that.
</flagged_assumptions>

<verification>
- `44.1-GATE-EVIDENCE-04.md` carries §§ 0-9: isolation proofs, measured corpus, named commits,
  SC#3(a) result with the interpretation note, the verbatim source diff, the diff-derived substitution
  set, SC#3(b) result with its positive control and file inventory, the joint-sufficiency statement,
  the SC#7 discharge map, and the SC#1-SC#8 verdict.
- `uv run pytest -q` exits 0 across the whole suite, including plan 01's gate and the new SC#8 class.
- `grep -n 'heading(offset' typsphinx/templates/base.typ` returns nothing.
- `sed -n '52,70p' .planning/REQUIREMENTS.md | grep -c 'byte-identical'` returns `0`.
- `grep -c 'checkpoint:decision' .planning/ROADMAP.md` returns `0`.
- `git status --porcelain typsphinx/ pyproject.toml uv.lock` is empty.
- `git worktree list` shows no leftover scratch worktree.
- `uv run ruff check . && uv run black --check . && uv run mypy typsphinx/` all exit 0.
</verification>

<success_criteria>
- SC#3 met in both halves over the measured corpus, each with a positive control that stops it passing
  vacuously, and with D-02's joint-sufficiency argument recorded.
- SC#7 met: every non-section-title path maps to a named, currently passing assertion anchored to the
  code, not to a restatement of the criterion.
- SC#8 met: a permanent two-leg guard with its own producibility control replaces inspection.
- D-01 fully carried: ROADMAP.md (done previously) and REQUIREMENTS.md (done here) agree with what was
  measured, and the superseded ROADMAP planning note is retired without losing its measurements.
- D-03 fence proven to have held for the whole phase.
- Every criterion SC#1-SC#8 has a recorded verdict, with `human_needed` wherever the evidence is short.
</success_criteria>

<output>
Create `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-04-SUMMARY.md` when done.
Record in it: the two named commits, both SC#3 results with their controls, the SC#7 map, the SC#8
guard, the two document corrections, and the SC#1-SC#8 verdict table.
</output>
