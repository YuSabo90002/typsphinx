---
phase: 44.1-relative-heading-depth-for-toctree-nesting
plan: 04
type: execute
wave: 3
depends_on: [44.1-01, 44.1-02, 44.1-03]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md
autonomous: true
requirements: [TOC-01]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC#3(a) resolved-level invariance: every master document in the corpus compiles before and after, and its typst.query(..., 'heading', field='level') sequence is identical on both sides"
    - "SC#3(b) normalized byte invariance: applying the phase's mechanical source substitutions to the pre-fix .typ output leaves a byte-identical result against the post-fix output — for included documents as well as masters, so the change is proved not to have leaked anywhere else"
    - "Both halves of SC#3 are produced; neither alone is accepted — (a) alone would miss a non-heading diff, (b) alone would not show the substituted text still resolves the same (D-02)"
    - "The corpus is the measured one: docs/source (13 .rst files, one master) plus every root under tests/roots (exactly one root, test-basic, one master)"
    - "Literal byte-identity of master .typ files is not claimed anywhere in the evidence — D-01 established it is unachievable, and the whole-file diff is recorded as expected-non-empty before the normalized diff is recorded as empty"
    - "SC#8: neither typsphinx/templates/base.typ nor builder._write_template_file introduces a heading offset — asserted by a repo-scoped source check and corroborated by SC#3's invariance result"
    - "SC#7: all four non-section title paths — admonition, topic, table caption, and the `.. contents::` topic label — are confirmed unaffected by naming the test and assertion line that discharges each one; no claim is left as an assumption"
    - "REQUIREMENTS.md TOC-01 no longer carries the superseded byte-identity sentence, so REQUIREMENTS.md and the rewritten ROADMAP SC#3 agree"
    - "The two builds are proved to have run genuinely different code: the resolved typsphinx.__file__ path differs between the two throwaway worktrees and each lies inside its own"
  artifacts:
    - path: ".planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md"
      provides: "the SC#3 two-part invariance proof, the SC#7 discharge map, and the SC#8 source assertion"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TOC-01 with the superseded byte-identity sentence corrected"
      contains: "TOC-01"
  key_links:
    - from: ".planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md"
      to: "typsphinx/translator.py"
      via: "git diff --stat <pre>..<post> -- typsphinx/ scoping the production surface the normalization must cover"
      pattern: "translator.py"
    - from: ".planning/REQUIREMENTS.md TOC-01"
      to: ".planning/ROADMAP.md Phase 44.1 SC#3"
      via: "both must state the same invariance claim after the correction"
      pattern: "TOC-01"
  prohibitions:
    - requirement_id: TOC-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT present an empty diff as proof without first recording the non-empty whole-file diff that shows the two builds ran genuinely different code — an empty diff produced by two runs of the same code proves nothing and is the exact failure this evidence exists to exclude"
    - requirement_id: TOC-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT transcribe any figure into this evidence from a planning document, a prior summary, or recall — every command, SHA, path and diff recorded here is executed in this session and pasted verbatim, including the ones that were also measured during planning"
    - requirement_id: TOC-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT let the phase close without the accepted user-visible consequences being written down where the milestone's CHANGELOG work will find them — the heading hierarchy of existing users' PDFs shifts, and per D-03 a rendered table of contents at the default depth will show included document titles without their sections"
---

<objective>
Discharge SC#3, SC#7 and SC#8, and stop REQUIREMENTS.md and ROADMAP.md from disagreeing about what
SC#3 claims.

Purpose: SC#3 is the phase's invariance obligation and, per D-02, it stacks two proofs that are each
insufficient alone. SC#7 and SC#8 are "asserted, not assumed" criteria whose discharge is a mapping
from claim to a named test and line. The REQUIREMENTS.md correction closes a live contradiction:
TOC-01 still says master output "must be byte-identical", which D-01 established is unachievable and
which the ROADMAP SC#3 text was already rewritten to stop claiming.

Output: `44.1-GATE-EVIDENCE-04.md` and a scoped edit to REQUIREMENTS.md TOC-01. This plan writes no
production code and no test code.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-CONTEXT.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-01-SUMMARY.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-02-SUMMARY.md
@.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-03-SUMMARY.md
@.planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md
@CLAUDE.md
</context>

<planner_measurements>
Measured during planning on 2026-08-04 at `a16deee`. Re-take each one in the executing session; none
of these may be transcribed from here into the evidence file.

**The corpus is small and fully enumerated.** `find docs/source -name '*.rst'` returns **13** files.
`docs/source/conf.py:71-73` declares a single master: `typst_documents = [("index", "typsphinx", project, author, "typst")]`.
`ls tests/roots/` returns exactly **one** root, `test-basic`, whose `conf.py` declares a single
master `[("index", "output.typ", "Test Document", "Test Author")]` and whose `index.rst` contains no
toctree directive — so its emitted output is pure master content and its resolved levels cannot move.
That is two masters and thirteen source documents in total; no scope negotiation is needed.

**The pre-fix corpus shape, from the committed build output** (`44.1-CONTEXT.md` `<specifics>` item 2,
which planning re-checked against `docs/_build/pdf/`): the master `docs/_build/pdf/typsphinx.typ`
carries four heading sites at lines 23, 30, 86 and 140; `docs/_build/pdf/quickstart.typ` carries five
at 12, 16, 79, 126, 152 and is included at offset 1; `docs/_build/pdf/user_guide/index.typ` carries
three at 12, 20, 41 and itself opens a nested offset scope at `:17`. These are the sites the
normalization must reconcile.

**SC#8, measured at source.** `grep -rn 'heading(offset' typsphinx/` returns three sites, all in
`typsphinx/translator.py` (`:4724` and `:4729` docstring prose, `:4762` the emission).
`typsphinx/templates/base.typ` has no offset site — it sets `heading(numbering: "1.1")` at `:65` and
its only other heading call, `#heading(outlined: false)[#toctree_caption]` at `:82`, takes no level
or depth argument. `typsphinx/builder.py:571` `_write_template_file` writes that template unchanged.
So SC#8's template half is a source fact, corroborated rather than established by SC#3.

**SC#7, measured mapping.** The four claims and their existing discharging assertions:
admonition titles → `tests/test_admonitions.py:279`, `:323`, `:448` (keyword-agnostic, unaffected by
this phase); topic titles → `tests/test_topics.py:62` and `:96` (widened by plan 02);
`.. contents::` topic label → `tests/test_topics.py:141` (widened by plan 02) plus the
insertion-order assertion at `:147-149`; table-caption titles → `typsphinx/translator.py`'s
`_in_table_caption` branch in `depart_title`, whose behaviour is asserted by the caption render
gates (`tests/test_captioned_table_render_gate.py`,
`tests/test_table_empty_caption_anchor_render_gate.py`). Verify each of these in the executing
session before writing it down; the table-caption row in particular must be resolved to a specific
node id and assertion line, not left as a file reference.

**The stale sentence to correct.** `.planning/REQUIREMENTS.md` TOC-01 spans lines 54-69; the
superseded clause is at lines 65-67 and reads that master documents render at offset 0 "so their
output must be byte-identical". ROADMAP SC#3 was rewritten on 2026-08-04 (commit `a16deee`) to state
the opposite explicitly. The correction is a scoped `Edit`, never a whole-file `Write`.

**The two SHAs.** PRE-FIX is the RED-recording commit plan 01 Task 1 named in
`44.1-GATE-EVIDENCE-01.md` — the last ancestor of the fix containing no production change. POST-FIX
is this phase's final production commit: plan 01's fix commit under option-b, or plan 03's emission
commit under option-a. Read both from the evidence files and confirm with
`git log -1 --oneline <sha>`; do not guess either.
</planner_measurements>

<tasks>

<task type="auto">
  <name>Task 1: SC#3 — the two-part invariance proof over the measured corpus, in two isolated worktrees</name>

  <files>.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md</files>

  <precondition>A `uv` executable is resolvable from inside a freshly-created throwaway worktree's `.venv/bin` — the sandbox shim `42-GATE-EVIDENCE-05.md` section 2 records (`ln -sf <nix-store-uv> .venv/bin/uv`, then `readlink -f .venv/bin/uv`). If it is not, record the failure verbatim and halt rather than falling back to the main checkout's environment, which would void the isolation proof.</precondition>

  <read_first>
    - .planning/milestones/v0.7.0-phases/42-captioned-table-drops-preceding-target-label/42-GATE-EVIDENCE-05.md in full (the established method: named commits, per-worktree provisioning, the `typsphinx.__file__` isolation proof, the expected-non-empty diff recorded before the empty one, and the production-diff isolation section)
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-01.md (the PRE-FIX SHA)
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-03.md (which option was selected, hence how many substitutions the normalization must cover, and the POST-FIX SHA under option-a)
    - .planning/ROADMAP.md the `### Phase 44.1` SC#3 paragraph (the criterion this evidence discharges, as rewritten on 2026-08-04)
    - .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-CONTEXT.md D-01 and D-02
    - docs/source/conf.py lines 60-80 and tests/roots/test-basic/conf.py (the two masters in the corpus)
    - CLAUDE.md "Worktree-isolated execution" section
  </read_first>

  <action>
    Write `44.1-GATE-EVIDENCE-04.md` in the phase directory, following the section structure of
    `42-GATE-EVIDENCE-05.md`. Execute every command in this session and paste its output verbatim.

    Section 1 — the two named commits. Read the PRE-FIX SHA from `44.1-GATE-EVIDENCE-01.md` and the
    POST-FIX SHA from the phase's final production commit. Record `git log -1 --oneline` for each and
    both full 40-hex SHAs. Confirm with `git merge-base --is-ancestor <pre> <post>` that the pre-fix
    side really is an ancestor, and record the exit status.

    Section 2 — worktree isolation. For each side: `git worktree add --detach <scratch>/44.1-{pre,post} <SHA>`,
    then `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then the shim from the
    precondition, then
    `uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"`.
    Record both resolved paths and state explicitly that they differ and that each lies inside its own
    throwaway worktree — not in the main checkout and not in the executing worktree. If either path
    falls outside its worktree, stop: the rest of the file would be meaningless.

    Section 3 — builds. From each worktree, build both corpus members into that side's own output
    directory: `uv run python -m sphinx -b typst -q -E docs/source <scratch>/44.1-{pre,post}-docs`
    and `uv run python -m sphinx -b typst -q -E tests/roots/test-basic <scratch>/44.1-{pre,post}-root`.
    Record all four commands and all four exit statuses. Record the emitted file lists so the two
    sides are shown to have produced the same set of paths.

    Section 4a — the expected-non-empty diff, recorded FIRST. `diff -r` the pre-fix and post-fix docs
    output trees and paste the verbatim output. It must be non-empty; that is the positive proof the
    two worktrees executed different code, and it is what makes section 4c meaningful rather than
    vacuous. State in one sentence that literal byte-identity of master output is not claimed and why
    (D-01: masters emit the same heading call as included documents, so the keyword change lands in
    master output too).

    Section 4b — SC#3(a), resolved-level invariance. For each of the two masters, compile and query
    both sides with `typst.query(<master>.typ, 'heading', field='level', root=<that side's build dir>)`,
    `json.loads` the returned JSON string, and record the two level sequences. Assert they are
    identical, per master, and paste both sequences. Record the compile exit status on both sides —
    a master that fails to compile on either side is a blocker, not a skip.

    Section 4c — SC#3(b), normalized byte invariance. Derive the normalization set from the actual
    production diff rather than from memory: run `git diff <pre>..<post> -- typsphinx/` and read off
    every emitted-text change. Under option-b that is one mechanical substitution (the heading call's
    named argument); under option-a it is two (that one plus the toctree scope opener). Copy the
    pre-fix output trees to a scratch location, apply exactly those substitutions to the copies, and
    `diff -r` each against the corresponding post-fix tree. Record the substitution commands verbatim
    and the resulting diffs, which must be empty. Run this over **both** corpus members and over
    **included** documents as well as masters — record the file counts compared on each side so the
    coverage is visible, and name `docs/_build`-equivalent included files such as `quickstart.typ` and
    `user_guide/index.typ` explicitly among them.

    Section 5 — production-diff isolation. `git diff --stat <pre>..<post> -- typsphinx/` and state
    which files changed. Also record `git diff --stat <pre>..<post> -- pyproject.toml uv.lock`, which
    must be empty, so the evidence carries its own supply-chain assertion.

    Section 6 — verdict table mapping SC#3(a) and SC#3(b) to the sections that discharge them, with a
    status per row. Record what this file does NOT own (SC#1/SC#4/SC#5 → `44.1-GATE-EVIDENCE-01.md`;
    SC#6 → `44.1-GATE-EVIDENCE-02.md`; SC#2 → `44.1-GATE-EVIDENCE-03.md`).

    Remove both throwaway worktrees with `git worktree remove --force` and record `git worktree list`
    afterwards showing they are gone.

    Do not name this or any other plan-authored artifact `44.1-VERIFICATION.md` — that filename is
    overwritten wholesale at verify time and plan-authored evidence stored there would be destroyed.

    Commit as `docs(44.1-04): record SC#3 two-part heading-depth invariance evidence`.
  </action>

  <verify>
    <automated>test -f .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md &amp;&amp; grep -c 'typsphinx/__init__.py' .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md</automated>
  </verify>

  <acceptance_criteria>
    - Evidence: `44.1-GATE-EVIDENCE-04.md` records two full 40-hex SHAs and the `git merge-base --is-ancestor` exit status proving the pre-fix side is an ancestor of the post-fix side.
    - Integrity: the file records two **different** resolved `typsphinx.__file__` paths, each containing its own throwaway worktree directory name, and neither equal to a path under the main checkout `/home/yuta/Documents/typsphinx/typsphinx/`.
    - Evidence: four build commands are recorded (two corpus members x two sides) with exit status 0 for all four.
    - Evidence: section 4a records a **non-empty** `diff -r` output verbatim, before any empty diff appears in the file.
    - Behavior: section 4b records, per master, two `typst.query(..., field='level')` sequences that are equal — pasted, not summarised — for both `docs/source` and `tests/roots/test-basic`.
    - Behavior: section 4c records an **empty** `diff -r` after normalization, for both corpus members, with the file count compared on each side stated and with `quickstart.typ` and `user_guide/index.typ` named among the included documents covered.
    - Evidence: the normalization substitutions recorded in 4c are derived from `git diff <pre>..<post> -- typsphinx/` output that is also pasted in the file, so the substitution set is shown to match the real production change rather than an assumed one.
    - Source: `git diff --stat <pre>..<post> -- pyproject.toml uv.lock` is recorded and is empty.
    - Filename: `ls .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/ | grep -c 'VERIFICATION'` returns `0` for plan-authored files.
    - Discriminating command: if section 4a's diff comes back **empty**, do not proceed — run the `typsphinx.__file__` command in both worktrees again and paste both outputs; an empty whole-file diff means both sides ran the same code and the entire proof is void. Report it as a blocker.
  </acceptance_criteria>

  <reversibility rating="reversible">This task writes one planning document and no code; the throwaway worktrees are removed at the end.</reversibility>

  <done>`44.1-GATE-EVIDENCE-04.md` carries both halves of SC#3 over the full measured corpus, with the isolation proof, the expected-non-empty diff recorded before the empty one, and the production-diff isolation section.</done>
</task>

<task type="auto">
  <name>Task 2: SC#7 discharge map, SC#8 source assertion, and the REQUIREMENTS.md TOC-01 correction</name>

  <files>.planning/REQUIREMENTS.md, .planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md lines 52-70 (TOC-01 in full — the superseded byte-identity clause is at 65-67)
    - .planning/ROADMAP.md the `### Phase 44.1` SC#3 paragraph as rewritten on 2026-08-04 (the wording REQUIREMENTS.md must be brought into agreement with)
    - typsphinx/templates/base.typ lines 60-95 (the heading and outline setup — confirm for yourself there is no offset site and that the caption heading takes no level or depth argument)
    - typsphinx/builder.py lines 565-600 (`_write_template_file` — confirm it writes the template unchanged and introduces no heading directive of its own)
    - typsphinx/translator.py `depart_title` and its `_in_table_caption` branch (the table-caption path SC#7 claims returns before the heading path)
    - tests/test_admonitions.py lines 270-300 and 440-455, tests/test_topics.py lines 55-150 (the admonition and topic discharge sites, as they stand after plan 02)
    - tests/test_captioned_table_render_gate.py and tests/test_table_empty_caption_anchor_render_gate.py (locate the specific assertion that shows a table caption does not emit a section heading)
  </read_first>

  <action>
    Append two sections to `44.1-GATE-EVIDENCE-04.md`.

    "SC#7 — non-section title discharge map": a table with one row per claim (admonition title, topic
    title, table-caption title, `.. contents::` topic label). Each row names the discharging pytest
    node id, the file and line of the assertion, and the verbatim output of running that node id in
    this session. Resolve the table-caption row to a specific node id and assertion line rather than
    a file reference — if no existing assertion proves it, say so plainly and record what would be
    needed, rather than describing the source path as if it were a test. Add a short paragraph
    recording the structural reason the claim holds (the buffer-swap branch in `visit_title` and the
    `_in_table_caption` branch in `depart_title` both return before the section-heading path), and
    state that the paragraph explains the assertions rather than substituting for them.

    "SC#8 — no template-side heading offset": record the verbatim output of
    `grep -rn 'heading(offset' typsphinx/` and of `grep -n 'heading' typsphinx/templates/base.typ`.
    State which sites are prose and which are emission, and that `templates/base.typ` and
    `_write_template_file` introduce none. Cross-reference SC#3(a) in this same file as the
    behavioural corroboration: the masters' resolved-level sequences are identical across the fix,
    which could not hold if the template introduced its own offset.

    Then correct `.planning/REQUIREMENTS.md` TOC-01 with a scoped `Edit` — never a whole-file
    `Write`. Replace the superseded clause at lines 65-67 (that master documents render at offset 0
    "so their output must be byte-identical") with the claim the ROADMAP SC#3 rewrite already makes:
    masters render at offset 0 so `depth` equals `level` and their **resolved level** is unchanged,
    while their `.typ` bytes do change because a master emits the same heading call as an included
    document. Keep the rest of TOC-01 verbatim — the root cause, the measured typst-py figures, and
    the standing instruction about deliberate test updates with owner sign-off and a pre-fix re-proof
    all stay. Mark the edit as a documentation correction traceable to TOC-01 and to D-01, and record
    the before/after wording in `44.1-GATE-EVIDENCE-04.md` so the change is auditable.

    Finally, record the accepted user-visible consequences in the same evidence file, under a heading
    the milestone's CHANGELOG work will find: (1) the heading hierarchy of any existing multi-document
    project's PDF shifts, because toctree'd documents now nest; and (2) per D-03 the rendered table of
    contents at the default depth will show included document titles without their sections, which is
    accepted and deferred with its measurements in `44.1-CONTEXT.md` `<deferred>`. State plainly that
    these are user-visible changes inside a patch release.

    Commit as `docs(44.1-04): map SC#7/SC#8 discharge and correct TOC-01's superseded invariance claim`.
  </action>

  <verify>
    <automated>grep -c 'byte-identical' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -n 'byte-identical' .planning/REQUIREMENTS.md` returns no line inside the TOC-01 block (lines 52-70); if the phrase survives elsewhere in the file it belongs to a different requirement and is left alone.
    - Source: `git diff -- .planning/REQUIREMENTS.md` touches only lines inside the TOC-01 block — no other requirement's text appears in the diff.
    - Source: `grep -c 'TOC-01' .planning/REQUIREMENTS.md` is unchanged from before the edit.
    - Evidence: the SC#7 table has exactly four rows, each carrying a pytest node id and a file:line, and each with the verbatim output of running that node id in this session.
    - Evidence: the SC#8 section pastes the verbatim output of `grep -rn 'heading(offset' typsphinx/` and states that `typsphinx/templates/base.typ` contributes zero of those sites.
    - Evidence: the accepted-consequences section names both the heading-hierarchy shift and the D-03 table-of-contents depth consequence, and labels them user-visible.
    - Behavior: `uv run pytest tests/test_admonitions.py tests/test_topics.py tests/test_captioned_table_render_gate.py tests/test_table_empty_caption_anchor_render_gate.py -q` exits 0.
    - Discriminating command: if the table-caption claim has no existing assertion, run `uv run pytest tests/test_captioned_table_render_gate.py -q --collect-only` and paste the collected node ids before concluding — record the absence as a measured gap with the collected list as evidence, never as a satisfied row.
  </acceptance_criteria>

  <reversibility rating="reversible">One scoped planning-document edit and one appended evidence section; both restorable by `git revert`.</reversibility>

  <done>SC#7's four claims are each mapped to a run assertion, SC#8 is asserted at source and corroborated behaviourally, TOC-01 no longer contradicts the rewritten ROADMAP SC#3, and the accepted user-visible consequences are written down where the milestone CHANGELOG work will find them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| throwaway worktree at an old SHA → executing interpreter | this task checks out an old tree and runs code from it; which tree is on `sys.path` decides whether the evidence means anything |
| pre-fix build output → normalization script | text substitutions are applied to generated files before comparison, so a wrong substitution set can manufacture an empty diff |
| planning documents → evidence file | a figure recalled instead of executed would make the record unfalsifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44.1-16 | Tampering | `import typsphinx` resolving to the main checkout from a throwaway worktree | high | mitigate | section 2 records both resolved `typsphinx.__file__` paths; acceptance criteria require them to differ and each to lie inside its own worktree, and section 4a's non-empty diff independently corroborates that different code ran |
| T-44.1-17 | Repudiation | an empty normalized diff manufactured by an over-broad substitution set | high | mitigate | the substitution set is derived from a pasted `git diff <pre>..<post> -- typsphinx/`, so the normalization is shown to match the real production change; the non-empty whole-file diff is recorded first |
| T-44.1-18 | Spoofing | a figure transcribed from a planning document instead of executed | high | mitigate | an explicit prohibition in `must_haves`, plus acceptance criteria that require verbatim command output rather than summaries, plus the discriminating command for the empty-4a case |
| T-44.1-19 | Tampering | a whole-file `Write` to REQUIREMENTS.md destroying other requirements | high | mitigate | the action mandates a scoped `Edit`, and an acceptance criterion requires `git diff -- .planning/REQUIREMENTS.md` to touch only lines inside the TOC-01 block |
| T-44.1-20 | Information Disclosure | absolute scratch paths and Nix store paths recorded in the evidence | low | accept | planning artifact in a private repo; abbreviate throwaway roots as `<scratch>` following `42-GATE-EVIDENCE-05.md`, which recorded its own shim path the same way |
| T-44.1-21 | Denial of Service | two full `uv sync --extra dev` provisions plus four Sphinx builds | low | accept | the corpus is 13 source documents and one four-file root; the same method completed in Phase 42 |
| T-44.1-SC | Tampering | npm/pip/cargo package installs | high | accept | no package is added; the evidence file itself asserts `git diff --stat <pre>..<post> -- pyproject.toml uv.lock` is empty, and `uv sync --extra dev` in each worktree resolves only already-locked dev dependencies, so there is no install task to gate and no `[ASSUMED]`/`[SUS]` package to verify |

ASVS level 1; block on `high`.
</threat_model>

<artifacts_this_phase_produces>
Symbols and files created by this plan (new — not expected to pre-exist in the codebase):

- **New file:** `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-GATE-EVIDENCE-04.md`

No test function, class, fixture, helper, module or pytest mark is created by this plan, and no
production code is written. `.planning/REQUIREMENTS.md` is edited in place, scoped to the TOC-01
block.

Deliberately NOT produced: any file named `44.1-VERIFICATION.md` — that name is reserved by the
verifier, which overwrites it wholesale.
</artifacts_this_phase_produces>

<verification>
- `44.1-GATE-EVIDENCE-04.md` exists and carries sections 1-6 plus the SC#7 map, the SC#8 assertion,
  and the accepted-consequences record.
- `git diff -- .planning/REQUIREMENTS.md` is confined to the TOC-01 block.
- `uv run pytest tests/ -q` exits 0 (unchanged by this plan — it writes no code).
- No plan-authored file in the phase directory is named `44.1-VERIFICATION.md`.
</verification>

<success_criteria>
SC#3 discharged by both required halves over the measured corpus, with the isolation proof and the
expected-non-empty diff recorded first. SC#7 discharged by a four-row map from claim to run
assertion. SC#8 discharged at source and corroborated by SC#3(a). REQUIREMENTS.md and ROADMAP.md
agree about what is invariant.
</success_criteria>

<output>
Create `.planning/phases/44.1-relative-heading-depth-for-toctree-nesting/44.1-04-SUMMARY.md` when done.
</output>
