---
phase: 48-compile-time-cross-reference-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md
  - .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md
  - tests/fixtures/xref_per_master_guard_gate/
  - tests/fixtures/citation_caption_dangling_label_gate/
  - tests/test_xref_compile_time_guard_render_gate.py
  - tests/test_citation_caption_dangling_label_gate.py
autonomous: true
requirements: [XREF-03, XREF-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "48-EXPECTED-STRUCTURE.md exists and states, for every assertion this phase flips, the exact new expected value derived from the fixture's conf.py and .rst read literally plus the D-07/D-08 guard contract — written and committed BEFORE the emitter changes (binding constraint #6)."
    - "48-RED-EVIDENCE.md records the verbatim pre-fix transcripts captured against the unfixed tree: the XREF-03 unguarded-form `label ... does not exist in the document` fatal, and the D-05 citation-in-caption fatal whose sphinx-build half exits 0 with no warning."
    - "48-RED-EVIDENCE.md carries an enumerated impossibility argument for D-04 (open question #1), naming the four measured source shapes and the Sphinx ReferencesResolver code path that replaces every pending_xref before the writer runs — an argument, not a 'not reproducible' report."
    - "Every new gate test that cannot pass until a later plan lands is marked pytest.mark.xfail(strict=True) with a reason naming the plan that flips it, so `pytest` exits 0 on the unfixed tree while an accidental early pass becomes XPASS (a failure)."
    - "The two new fixtures each carry a `Load-bearing properties -- do NOT touch any of these` comment block in conf.py naming exactly what would silently stop the fixture exercising its defect."
    - "The per-master guard fixture's two masters are BOTH well-formed 4-tuple typst_documents entries producing real wrapper files, and neither entry's resolved target casefold-equals any docname's own content path (no BLD-03 self-collision)."
    - { statement: "The per-master guard fixture is built on the current post-47 / pre-49 tree, where the include-dedup ledger is per-build, so it does NOT rely on one shared content file being included by both masters; the two masters' own content files carry a byte-identical reference line instead, and the byte-identity is asserted.", verification: backstop }
  artifacts:
    - .planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md
    - .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md
    - tests/fixtures/xref_per_master_guard_gate/conf.py
    - tests/fixtures/xref_per_master_guard_gate/index.rst
    - tests/fixtures/xref_per_master_guard_gate/target.rst
    - tests/fixtures/xref_per_master_guard_gate/bravo.rst
    - tests/fixtures/citation_caption_dangling_label_gate/conf.py
    - tests/fixtures/citation_caption_dangling_label_gate/index.rst
    - tests/test_xref_compile_time_guard_render_gate.py
    - tests/test_citation_caption_dangling_label_gate.py
  key_links:
    - "48-EXPECTED-STRUCTURE.md -> tests/test_xref_compile_time_guard_render_gate.py: every asserted post-fix string in the gate module is traceable to a row in the expected-value table, never to emitter output."
    - "48-RED-EVIDENCE.md -> tests/test_citation_caption_dangling_label_gate.py: the xfail reason= paraphrases the verbatim transcript recorded in the evidence artifact, per the Phase 47 plan-13 convention."
    - "tests/fixtures/xref_per_master_guard_gate/target.rst -> the guarded label: the section label is the only supply-side anchor, and only master alpha's toctree reaches it."
  prohibitions:
    - "typsphinx must not swallow or suppress Sphinx's own broken-reference diagnostics; after the build-time warning is deleted those Sphinx warnings are the only remaining build-time signal that a reference is broken."
    - "A reference whose target IS present must not render any differently to the reader than it did before this phase — no visual marking, bracket, or spacing distinguishes the degraded branch from the linked branch."
    - "No second, competing degrade decision may survive anywhere — not under a new name, not as a per-master ledger, not as a defensive getattr fallback."
---

<objective>
Write down, from first principles and BEFORE any emitter code changes, exactly what each flipping
assertion must become — then capture the pre-fix RED for both currently-reachable fatals against the
unfixed tree, and record the enumerated impossibility argument for the one site whose RED cannot be
constructed.

Purpose: binding constraint #6 forbids laundered gates. Two-plus assertions flip direction this
phase, and their new expected values must be an INPUT to the tests, derived from each fixture's
`conf.py`/`.rst` plus the D-07/D-08 guard contract read literally — never copy-pasted from the new
emitter's output. Binding constraint #4 requires every closed defect to name its pre-fix RED before
implementation starts; research already reproduced both reachable REDs end to end, and this plan
turns those reproductions into committed test assets.

Output: two new fixture projects, two new gate modules whose post-fix assertions are recorded as
`xfail(strict=True)`, `48-EXPECTED-STRUCTURE.md` (the first-principles derivation) and
`48-RED-EVIDENCE.md` (the captured pre-fix transcripts plus D-04's impossibility argument). No file
under `typsphinx/` is touched by this plan.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-compile-time-cross-reference-guard/48-CONTEXT.md
@.planning/phases/48-compile-time-cross-reference-guard/48-RESEARCH.md
@.planning/phases/48-compile-time-cross-reference-guard/48-PATTERNS.md
@.planning/phases/48-compile-time-cross-reference-guard/48-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files and identifiers **created** by Phase 48 (new — not drift; do not flag as unknown
symbols during source-grounding):

**New production symbols (created in 48-02):**
- `TypstTranslator._label_existence_guard()` — the single D-07 shared guard-string derivation point
  in `typsphinx/translator.py`
- `_LabelGuardStrings` — module-level `NamedTuple` in `typsphinx/translator.py` with fields
  `open_str` and `close_str`
- `TypstTranslator._reference_guard_close` — the per-reference close-string slot carried from
  `visit_reference` to `depart_reference` (mirrors the existing `_reference_own_anchor` slot)
- `__tsx_body` — the emitted Typst `let`-bound identifier inside the guard expression (Claude's
  Discretion in `48-CONTEXT.md` leaves the name unfixed; it is fixed to this spelling here so all
  four plans and all three call sites agree)

**New test modules:** `tests/test_xref_compile_time_guard_render_gate.py`,
`tests/test_citation_caption_dangling_label_gate.py`

**New fixture projects:** `tests/fixtures/xref_per_master_guard_gate/`,
`tests/fixtures/citation_caption_dangling_label_gate/`

**New planning artifacts:** `48-EXPECTED-STRUCTURE.md`, `48-RED-EVIDENCE.md`, `48-EVIDENCE.md`

**Symbols this phase DELETES** (the removals are intended, not accidental):
- `TypstBuilder._compute_master_included_docnames()` (`typsphinx/builder.py`)
- `TypstBuilder.master_included_docnames` attribute (`typsphinx/builder.py`)
- the `write()` call site that populates it (`typsphinx/builder.py`)
- `_ReferenceAnchorDecision.degrade_xref_to_text` field and its docstring paragraph
  (`typsphinx/translator.py`)
- `visit_reference`'s cross-document degrade branch, including its `logger.warning` (D-01) and its
  `_skip_link_wrapper` early return
- four unit tests in `tests/test_master_include_set_predicate_gate.py` (D-10)
- the `_StubBuilder` class attribute in `tests/test_citation_degradation_gate.py` that seeds the
  deleted builder state

<!-- planner-discipline-allow: master_included_docnames -->
<!-- planner-discipline-allow: degrade_xref_to_text -->
<!-- planner-discipline-allow: _compute_master_included_docnames -->

## Edge Coverage Ledger (spec-less probe fallback, §C)

All 6 probe rows are accounted for; none is silently dropped.

| # | Req | Category | Disposition | Where |
|---|-----|----------|-------------|-------|
| 1 | XREF-03 | empty | resolved (explicit) | 48-02 `must_haves.truths` |
| 2 | XREF-03 | encoding | resolved (explicit) | 48-02 `must_haves.truths` |
| 3 | XREF-04 | adjacency | resolved (explicit) | 48-03 `must_haves.truths` |
| 4 | XREF-04 | empty | resolved (explicit) | 48-03 `must_haves.truths` |
| 5 | XREF-04 | encoding | resolved (explicit) | 48-03 `must_haves.truths` |
| 6 | XREF-04 | ordering | resolved (explicit) | 48-03 `must_haves.truths` |

Sum check: 6 authored into `must_haves` + 0 flagged assumptions = 6 probe rows. No silent drops.

## Flagged planner assumptions

- **The per-build include-dedup ledger makes a single shared content file unusable for SC#1 on this
  tree.** `typsphinx/builder.py:750` resets `_included_docnames` once per build, not once per
  master, so a document toctree'd by two masters is `#include()`d into only one of them (defect A,
  owned by Phase 49). The SC#1 fixture therefore proves per-master divergence with two content
  files carrying a byte-identical reference line, and asserts that byte-identity explicitly, rather
  than with one shared file. This is recorded as a backstop truth above.

<tasks>

<task type="auto">
  <name>Task 1: Build the two new fixture projects and capture the pre-fix RED transcripts against the unfixed tree</name>
  <files>
    tests/fixtures/xref_per_master_guard_gate/conf.py,
    tests/fixtures/xref_per_master_guard_gate/index.rst,
    tests/fixtures/xref_per_master_guard_gate/target.rst,
    tests/fixtures/xref_per_master_guard_gate/bravo.rst,
    tests/fixtures/citation_caption_dangling_label_gate/conf.py,
    tests/fixtures/citation_caption_dangling_label_gate/index.rst,
    .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md
  </files>
  <read_first>
    - tests/fixtures/bld03_ghost_entry_xref_gate/conf.py (the verbatim "Load-bearing properties -- do NOT touch any of these" comment-block convention, lines 1-25, and the `:ref:`-into-a-conditionally-included-document content shape)
    - tests/fixtures/two_layer_root_master_gate/conf.py and tests/fixtures/two_layer_nested_master_gate/conf.py (the clean "two well-formed typst_documents entries, each a real compiling master" structural shape)
    - tests/fixtures/xref_orphan_degrade_render_gate/conf.py (the fixture de-collision note showing why an entry target must not casefold-equal a docname's own content path)
    - .planning/phases/48-compile-time-cross-reference-guard/48-RESEARCH.md sections "Common Pitfalls" 4 and 5 (the D-04 four-shape enumeration and the D-05 verbatim TypstError transcript) and "Code Examples"
    - .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-GAP2-RED-EVIDENCE.md (the RED-EVIDENCE artifact shape: one verbatim transcript block per failure mode, headed by exact reproduction commands and worktree-isolation provenance)
    - typsphinx/builder.py lines 730-770 (the write() phase, including the per-build include-dedup ledger reset that makes a shared content file unusable here)
    - typsphinx/translator.py lines 4780-4824 (visit_toctree's per-build dedup, same reason)
  </read_first>
  <precondition>The working tree carries no Phase 48 source change yet: `git status --porcelain typsphinx/` prints nothing.</precondition>
  <action>
Provision the worktree first — `.git` is a FILE in an isolated worktree, so run
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and then run every command through
`uv run` (per CLAUDE.md's mandatory worktree-isolated execution protocol). Confirm isolation with
`uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"` and
record that path in the evidence artifact's provenance header.

Create fixture project `tests/fixtures/xref_per_master_guard_gate/` — the SC#1 two-master acceptance
fixture (XREF-03):

- `conf.py`: `project = "Xref Per Master Guard Gate"`, `author = "Probe Author"`,
  `release = "1.0"`, `extensions = ["typsphinx"]`, and
  `typst_documents = [("index", "alpha.typ", "Alpha Master", "Probe Author"), ("bravo", "bravo_master.typ", "Bravo Master", "Probe Author")]`.
  Both entries are well-formed 4-tuples producing real wrapper files. Note in the comment block that
  `bravo`'s target is deliberately `bravo_master.typ` and NOT `bravo.typ`, because a target whose
  resolved path casefold-equals the docname's own content path is a BLD-03 self-collision that
  Phase 47's validator refuses.
- `index.rst` (root doc, master alpha): a `toctree` listing `target`, plus a body paragraph whose
  final sentence is a `:ref:` to the label defined in `target.rst`.
- `bravo.rst` (master bravo): carries an `:orphan:` field so Sphinx emits no
  "isn't included in any toctree" consistency warning, NO toctree of its own (so its wrapper's
  compiled PDF carries no outline-derived link annotations at all), and a body paragraph whose final
  sentence is the SAME `:ref:` line as `index.rst`'s — byte-identical reStructuredText.
- `target.rst`: a labelled section — an explicit label directive immediately above a section title —
  whose title text is a distinctive all-caps-free but unique phrase so `pypdf` text extraction can
  find it, and whose body carries a unique all-caps marker token.
- `conf.py` carries a "Load-bearing properties -- do NOT touch any of these" comment block, in the
  exact bulleted style of `tests/fixtures/bld03_ghost_entry_xref_gate/conf.py`, naming at minimum:
  (a) only `index`'s toctree may list `target` — adding `target` to a toctree in `bravo.rst`
  collapses the fixture into a single-outcome case; (b) `bravo.rst` must keep `:orphan:` AND must
  keep no toctree; (c) the `:ref:` sentence must stay byte-identical between `index.rst` and
  `bravo.rst`, because the gate asserts the two emitted content files carry the identical guarded
  expression; (d) neither entry target may be renamed onto a docname's own content path.

Create fixture project `tests/fixtures/citation_caption_dangling_label_gate/` — the D-05 pre-fix RED
fixture (XREF-04):

- `conf.py`: single well-formed entry `[("index", "manual.typ", "Citation Caption Gate", "Probe Author")]`,
  plus the same load-bearing comment block naming: the citing reference MUST live inside a
  `code-block` directive's `:caption:` option (that is the only route where `visit_caption` raises
  `SkipNode` while `document.findall` still finds the citing node), and the citation definition MUST
  stay in the same document.
- `index.rst`: a `code-block` directive with a `:caption:` option whose caption text contains a
  citation reference, a short code body, and below it the corresponding citation definition.

Then capture the pre-fix REDs and write
`.planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md`, following
`47-GAP2-RED-EVIDENCE.md`'s shape exactly (a `## Failure mode N` section per defect, each with an
exact reproduction command line, a verbatim raw-output fenced block, and a short reading of what the
transcript proves). Record three sections:

1. **Failure mode 1 — XREF-03, the unguarded cross-document form.** Run
   `uv run python -m sphinx -b typstpdf tests/fixtures/xref_per_master_guard_gate <tmp>` against the
   unfixed tree and paste the verbatim output. On the unfixed tree the build-time union suppresses
   the link for `bravo` and emits it for `index`, so the observable RED is a CONTENT-level
   measurement, not an exit code: paste the verbatim emitted `index.typ` and `bravo.typ` reference
   lines side by side and state, in one sentence, that the two files DISAGREE at write time — which
   is precisely the per-master decision this phase moves to compile time. Additionally, prove the
   unguarded form is a real fatal: copy the emitted `bravo.typ` to a scratch directory, hand-edit
   the degraded reference back into the plain `link(<label>, ...)` form the guard replaces, compile
   it with `uv run python -c "import typst; typst.compile(...)"`, and paste the verbatim
   `label ... does not exist in the document` error. State explicitly that this hand-edited
   reconstruction is the RED the guard prevents.
2. **Failure mode 2 — D-05, citation reference inside a captioned code block.** Run
   `uv run python -m sphinx -b typst tests/fixtures/citation_caption_dangling_label_gate <tmp>` and
   paste the verbatim output showing a clean success with no warning; then run
   `uv run python -m sphinx -b typstpdf ...` on the same fixture and paste the verbatim
   `label ... does not exist in the document` failure. State that the `-b typst` half exiting 0 with
   zero warnings is the reason this defect is invisible until the Typst compile.
3. **D-04 — enumerated impossibility argument (open question #1).** Not a transcript-of-a-failure
   section: an argument, per the Phase 40.1 D-01 precedent. Reproduce the four plausible source
   shapes research measured (an unresolvable `:ref:`, an unresolvable `:doc:`, an unresolvable
   `:any:`, and an unknown role), paste research's verbatim `sphinx-build` transcript showing each
   producing a Sphinx WARNING and plain-text or `raw()`-wrapped output, and name the mechanism:
   Sphinx 9.1.0's `ReferencesResolver` post-transform declares empty `builders`/`formats` tuples so
   `is_supported()` is unconditionally true for the `typst`/`typstpdf` builders, and its `run()`
   calls `node.replace_self(new_nodes)` unconditionally, falling back to the node's own deep-copied
   `contnode` when resolution fails. Conclude in one explicit sentence that no `pending_xref` node
   survives to the writer through the normal pipeline, that the RED is therefore unconstructible
   rather than merely un-reproduced, and that per D-04 the site is still brought under the guard as
   defence in depth. Record research assumption A2 verbatim as the argument's own stated limit (a
   third-party extension emitting a `pending_xref` after `ReferencesResolver` runs was not tested).

Do not modify any file under `typsphinx/`.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; p=pathlib.Path('tests/fixtures'); need=['xref_per_master_guard_gate/conf.py','xref_per_master_guard_gate/index.rst','xref_per_master_guard_gate/bravo.rst','xref_per_master_guard_gate/target.rst','citation_caption_dangling_label_gate/conf.py','citation_caption_dangling_label_gate/index.rst']; sys.exit(1 if [n for n in need if not (p/n).exists()] else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/fixtures/xref_per_master_guard_gate/conf.py tests/fixtures/xref_per_master_guard_gate/index.rst tests/fixtures/xref_per_master_guard_gate/bravo.rst tests/fixtures/xref_per_master_guard_gate/target.rst tests/fixtures/citation_caption_dangling_label_gate/conf.py tests/fixtures/citation_caption_dangling_label_gate/index.rst` exits 0.
    - `grep -c 'Load-bearing properties' tests/fixtures/xref_per_master_guard_gate/conf.py` returns at least 1, and the same command against `tests/fixtures/citation_caption_dangling_label_gate/conf.py` returns at least 1.
    - `grep -c 'bravo_master.typ' tests/fixtures/xref_per_master_guard_gate/conf.py` returns at least 1 and `grep -Ec '"bravo\.typ"' tests/fixtures/xref_per_master_guard_gate/conf.py` returns 0.
    - `grep -c ':orphan:' tests/fixtures/xref_per_master_guard_gate/bravo.rst` returns 1 and `grep -c 'toctree' tests/fixtures/xref_per_master_guard_gate/bravo.rst` returns 0.
    - The `:ref:` sentence is byte-identical between the two masters: `uv run python -c "import pathlib,re,sys; g=lambda p:[l for l in pathlib.Path(p).read_text().splitlines() if ':ref:' in l]; a=g('tests/fixtures/xref_per_master_guard_gate/index.rst'); b=g('tests/fixtures/xref_per_master_guard_gate/bravo.rst'); sys.exit(0 if a and a==b else 1)"` exits 0.
    - `48-RED-EVIDENCE.md` contains the literal string `does not exist in the document` inside a fenced transcript block for BOTH failure mode 1 and failure mode 2.
    - `48-RED-EVIDENCE.md` contains a section for D-04 naming `ReferencesResolver` and `replace_self`, enumerating four source shapes, and stating the conclusion as unconstructible rather than un-reproduced.
    - `git status --porcelain typsphinx/` prints nothing (no production code touched by this task).
  </acceptance_criteria>
  <done>Both fixtures exist with load-bearing comment blocks, and 48-RED-EVIDENCE.md carries verbatim pre-fix transcripts for both reachable fatals plus the enumerated D-04 impossibility argument, all captured against the unfixed tree.</done>
</task>

<task type="auto">
  <name>Task 2: Derive and write 48-EXPECTED-STRUCTURE.md — the new expected value for every assertion this phase flips</name>
  <files>
    .planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md
  </files>
  <read_first>
    - .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-EXPECTED-STRUCTURE.md (the artifact shape to imitate: per-fixture sections, an explicit "Derivation arithmetic" paragraph, a CURRENT-vs-EXPECTED table pair)
    - .planning/phases/48-compile-time-cross-reference-guard/48-CONTEXT.md (D-02, D-03, D-06, D-07, D-09, D-10 and the `<specifics>` guard sketch)
    - .planning/phases/48-compile-time-cross-reference-guard/48-RESEARCH.md "Architecture Patterns" Pattern 1 and "Common Pitfalls" Pitfall 1 (the corrected guard shape; the unbroken `if` line)
    - tests/test_xref_orphan_degrade_render_gate.py (all 196 lines — three of its assertions flip)
    - tests/test_master_include_set_predicate_gate.py lines 1-135 (the module docstring, TestGhostEntryXrefRenderGate, and the line-103 test that flips)
    - tests/test_citation_degradation_gate.py lines 590-620 (_StubBuilder), 850-925 (TestWr03DegradedCitingSiteAnchor), 995-1110 (case iii and the parametrized eligibility table), 1110-1164 (TestWr03XrefResolutionAndWarningFireOnce)
    - tests/fixtures/bld03_ghost_entry_xref_gate/conf.py and its index.rst / ghost.rst / ghost_child.rst (the source the line-103 expected value must be derived FROM)
    - tests/fixtures/xref_orphan_degrade_render_gate/conf.py and its index.rst / included.rst / orphan.rst
    - typsphinx/translator.py lines 4940-5024 (visit_reference's three branches) and 5025-5074 (depart_reference)
  </read_first>
  <action>
Write `.planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md`, following
`47-EXPECTED-STRUCTURE.md`'s shape. Derive every expected value from two inputs only: each fixture's
`conf.py`/`.rst` read literally, and the D-07/D-08 guard contract as corrected by research. **Run no
builder and read no emitter output while writing this document** — that is what binding constraint
#6 forbids.

Open with a `## Guard contract` section fixing the exact strings all later plans emit, so every
expected value in the rest of the document is a substitution into them. State them as two Python
f-string templates plus one worked substitution:

- the shared helper is `TypstTranslator._label_existence_guard(label, *, prefix="", code_mode_body=False)`,
  returning a `_LabelGuardStrings` `NamedTuple` with fields `open_str` and `close_str`;
- the bound identifier is `__tsx_body`;
- when `code_mode_body` is true, `open_str` ends with `= [#{` and `close_str` begins with `}];`;
  when false, `open_str` ends with `= [` and `close_str` begins with `];`;
- `close_str`'s conditional is one unbroken statement — the condition and its opening brace are
  never separated by a newline, because Typst's parser rejects that with `expected block`
  (research Pitfall 1). State this as a hard property of the emitted bytes, and note that the
  illustrative line break in `48-CONTEXT.md`'s `<specifics>` sketch is the shape that does NOT
  compile;
- show the fully substituted close string for one concrete label so the exact byte sequence is on
  the page: for label `target:xref-guard-target`, `close_str` is
  `}]; if query(<target:xref-guard-target>).len() > 0 { link(<target:xref-guard-target>, __tsx_body) } else { __tsx_body } }`.

Then one `## Fixture: <name>` section per fixture whose assertions change, each with a CURRENT
(pre-fix) table, an EXPECTED (post-fix) table, and a "Derivation arithmetic" paragraph. Cover at
minimum:

1. `tests/fixtures/xref_per_master_guard_gate` — for each of the two masters, the exact expected
   guarded expression in the emitted content file (identical in both, since the `:ref:` line is
   byte-identical and the target label is derived from the TARGET docname), the expected `/Link`
   annotation count in each compiled PDF (at least one for alpha's, exactly zero for bravo's — and
   state why zero is derivable: bravo has no toctree, so its wrapper's template emits no outline and
   the document contains no other link), and the expected `pypdf`-extracted visible text (the same
   reference text present in BOTH PDFs, per D-02).
2. `tests/fixtures/citation_caption_dangling_label_gate` — expected post-fix: `-b typstpdf` exits 0
   and the compiled PDF exists; no `does not exist in the document` text anywhere in the combined
   build output; the emitted `index.typ` carries the guarded form around the citation
   back-reference rather than a bare label link.
3. `tests/test_xref_orphan_degrade_render_gate.py` — three assertions flip. Give each its new
   expected value and the derivation. The negative assertion that no label link to the orphan
   document is emitted INVERTS: post-fix the emitted content file DOES carry the orphan document's
   label, inside the guard's `if`/`else` expression, because the write-time suppression is gone.
   The assertion that the reference text renders as a code-mode `text("...")` call must be re-derived
   from the guard contract's `code_mode_body` choice rather than assumed — state which form the
   contract produces for this site and why. The assertion that the same-document reference still
   emits its plain link is UNCHANGED and must be restated as an explicit invariance guard (D-06).
   Also state that the whole-project `-b typstpdf` build must still exit 0 with no dangling-label
   error — that assertion does not flip, it just changes mechanism from build-time suppression to
   compile-time guard, and the module's docstring must be rewritten to say so.
4. `tests/test_master_include_set_predicate_gate.py` — the ghost-entry test at line 103 flips.
   Derive from `bld03_ghost_entry_xref_gate`'s own sources: `index.rst` carries a `:ref:` into
   `ghost_child`'s label; `ghost` is a 1-tuple entry producing no wrapper, so `ghost_child.typ` is
   never included in the compiled `manual.typ`. Post-fix the emitted `index.typ` DOES carry the
   `ghost_child`-namespaced label inside the guard, and the compiled PDF still shows the reference's
   text with no link annotation and no `TypstError`. State that the three end-to-end tests in this
   module (the no-dangling-label `-b typstpdf` test, the unhashable-docname `-b typst` test, and the
   unhashable-docname `finish()` test) keep their exact current expected values (D-10), and that the
   four unit tests calling the deleted builder method are removed rather than rewritten.
5. `tests/test_citation_degradation_gate.py` — state the new expected values for: the parametrized
   eligibility case whose reference resolves to a document outside the old union (its
   `expected_eligible` becomes `True` under D-09, because `opens_wrapper` becomes unconditional and
   its anchor is `_current_docname()`-derived and therefore always same-document); the
   `TestWr03DegradedCitingSiteAnchor` class whose entire premise was that the reference degraded
   (re-derive what the assembled doctree now emits — every link target still has a matching attached
   anchor, but now via the eligible path rather than via the degrade path); and
   `TestWr03XrefResolutionAndWarningFireOnce`, whose warning-fires-once assertion has no subject at
   all after D-01 (state the replacement expected value: `_resolve_xref_docname` is still called
   exactly once per reference, and NO warning is emitted for a cross-document reference).

Close with a `## Assertions that must NOT change` section listing the D-06 negative assertions: the
two same-document `visit_reference` branches (bare-refid, and `#`-prefixed internal refuri) keep
emitting the plain unguarded `link(<label>, ` form, and every existing same-document-anchor test in
the corpus keeps its current expected value.

Finally, add a `## How to find any assertion I missed` section giving the exact repo-wide grep
commands a later plan must run to prove the enumeration is complete (a grep for the deleted symbols
across `tests/`, and a grep for assertions on cross-document link emission), so the phase does not
rely on this document's completeness alone.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; p=pathlib.Path('.planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md'); t=p.read_text() if p.exists() else ''; need=['## Guard contract','__tsx_body','.len() > 0','## Assertions that must NOT change','## How to find any assertion I missed']; sys.exit(1 if [n for n in need if n not in t] else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `48-EXPECTED-STRUCTURE.md` exists and contains the headings `## Guard contract`, `## Assertions that must NOT change` and `## How to find any assertion I missed`.
    - It contains the fully substituted close string for a concrete label, on ONE line, with the conditional's opening brace on the same line as its condition: `grep -Ec 'if query\(<[^>]+>\)\.len\(\) > 0 \{' .planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md` returns at least 1.
    - It names all five change sites in `## Fixture:` / per-module sections: the two new fixtures, `tests/test_xref_orphan_degrade_render_gate.py`, `tests/test_master_include_set_predicate_gate.py`, and `tests/test_citation_degradation_gate.py`.
    - It states an expected `/Link` annotation count for each master's compiled PDF in the per-master guard fixture, with the zero-count case justified from the fixture's own sources (no toctree, therefore no outline).
    - `git status --porcelain typsphinx/ tests/` prints nothing (this task writes only the planning artifact).
    - The document states in its own header that no builder was run to produce it.
  </acceptance_criteria>
  <done>Every assertion this phase flips has its new expected value written down and traceable to fixture sources plus the guard contract, before any emitter change exists.</done>
</task>

<task type="auto">
  <name>Task 3: Author the two new gate modules, recording the post-fix assertions as strict xfails</name>
  <files>
    tests/test_xref_compile_time_guard_render_gate.py,
    tests/test_citation_caption_dangling_label_gate.py
  </files>
  <read_first>
    - tests/test_pdf_render_gate.py lines 1-60 (module docstring style, the TYPST_AVAILABLE / PYPDF_AVAILABLE import guards) and lines 130-200 (fixtures_dir / temp_build_dir fixtures and _run_sphinx_build_typst, invoked as sys.executable -m sphinx)
    - tests/test_citation_render_gate.py lines 465-495 (_link_rect_x0_values — the /Annots readback idiom to adapt into a count-and-destination readback)
    - tests/test_xref_orphan_degrade_render_gate.py (the whole module — the closest existing single-project degrade gate, including _strip_raw_literals and the _run_sphinx_build_typstpdf helper)
    - tests/test_master_include_set_predicate_gate.py lines 1-95 (the module docstring convention and the xfail(strict=True) pre-fix RED recording style)
    - .planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md (written by task 2 — the ONLY source of the asserted post-fix values)
    - .planning/phases/48-compile-time-cross-reference-guard/48-RED-EVIDENCE.md (written by task 1 — the transcripts each xfail reason= paraphrases)
  </read_first>
  <action>
Author two new gate modules. Every asserted value comes from `48-EXPECTED-STRUCTURE.md`; no value is
read off a build. Every test that cannot pass until plan 48-02 or 48-03 lands carries
`@pytest.mark.xfail(strict=True, reason=...)` whose `reason` paraphrases the matching
`48-RED-EVIDENCE.md` transcript and names the plan that flips it — so `uv run pytest` exits 0 on this
plan's own tree while an accidental early pass surfaces as XPASS.

`tests/test_xref_compile_time_guard_render_gate.py` (XREF-03 / SC#1):

- Module docstring explaining the per-master divergence the gate proves, and stating explicitly why
  two content files carrying a byte-identical reference line are used instead of one shared file
  (the include-dedup ledger is per-build on this tree; defect A is Phase 49's).
- Reuse the established scaffold: module-level `TYPST_AVAILABLE` / `PYPDF_AVAILABLE` booleans built
  from guarded imports, a `_run_sphinx_build_typstpdf` helper invoking
  `[sys.executable, "-m", "sphinx", "-b", "typstpdf", src, out]` (never `uv run sphinx-build`), a
  `fixtures_dir`-style fixture, and a class-scoped fixture that builds the project ONCE and returns
  both compiled PDFs' bytes plus both emitted content files' text.
- Add a module-level `_link_annotation_dests(pdf_bytes)` helper adapted from
  `tests/test_citation_render_gate.py`'s `/Annots` readback: for every page, for every annotation
  whose `/Subtype` is `/Link`, return its destination name — reading `/Dest` directly and falling
  back to the action dictionary's destination entry — as a list of strings.
- Gate class decorated `@pytest.mark.slow` and
  `@pytest.mark.skipif(not (TYPST_AVAILABLE and PYPDF_AVAILABLE), ...)` with the same skip-reason
  wording the other GATE-01 classes use.
- Tests, each asserting a value taken from `48-EXPECTED-STRUCTURE.md`:
  1. Both masters' content files carry the IDENTICAL guarded expression for the reference — extract
     the guard expression from each emitted content file with one regex and assert the two extracted
     strings are equal, and assert the extracted string contains the target-docname-namespaced label
     and the unbroken conditional.
  2. Alpha's compiled PDF carries at least one `/Link` annotation whose destination names the
     target's namespaced label.
  3. Bravo's compiled PDF carries exactly zero `/Link` annotations.
  4. Neither compile raised a Typst error: the build exits 0 and the combined output contains no
     dangling-label error text.
  5. D-02 invariance: the reference's visible text appears in BOTH PDFs' `pypdf`-extracted text.
  Tests 1-3 and 5 are strict xfails naming plan 48-02; test 4 already passes today and must be
  written as a plain (non-xfail) invariance guard.

`tests/test_citation_caption_dangling_label_gate.py` (XREF-04 / D-05):

- Module docstring recording the two-traversal-mechanism root cause in one paragraph: the walker is
  interrupted by `SkipNode` inside the captioned-code-block caption handler, while the citation
  definition's back-reference loop scans the document with `findall`, which does not consult walker
  reachability — so an anchor that is never emitted is still linked to.
- Same scaffold and markers as above.
- Tests: (1) `-b typst` exits 0 with no warning naming this defect — a plain invariance guard that
  passes today and documents why the defect is invisible pre-compile; (2) `-b typstpdf` exits 0, the
  wrapper PDF exists and starts with the PDF magic bytes, and the combined build output contains no
  `does not exist in the document` text — a strict xfail naming plan 48-03; (3) the emitted
  `index.typ` carries the guarded form around the citation back-reference rather than a bare label
  link — a strict xfail naming plan 48-03.

Run `uv run black`, `uv run ruff check` and `uv run mypy typsphinx/` before finishing; both new
modules must be formatter- and linter-clean at 88 columns.
  </action>
  <verify>
    <automated>uv run pytest tests/test_xref_compile_time_guard_render_gate.py tests/test_citation_caption_dangling_label_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_xref_compile_time_guard_render_gate.py tests/test_citation_caption_dangling_label_gate.py -q` exits 0, reporting xfailed tests and zero failures and zero XPASS.
    - `grep -c 'xfail(strict=True' tests/test_xref_compile_time_guard_render_gate.py` returns at least 4 and the same command against `tests/test_citation_caption_dangling_label_gate.py` returns at least 2.
    - Every `reason=` string in both modules names either `48-02` or `48-03`: `grep -Ec 'reason=' tests/test_xref_compile_time_guard_render_gate.py` equals the count of lines in that file matching `48-0`.
    - Both modules invoke Sphinx as `sys.executable -m sphinx`: `grep -c 'uv", "run' tests/test_xref_compile_time_guard_render_gate.py` returns 0.
    - `uv run black --check tests/test_xref_compile_time_guard_render_gate.py tests/test_citation_caption_dangling_label_gate.py` exits 0 and `uv run ruff check tests/` exits 0.
    - `uv run pytest -m "not slow" -q` exits 0 (nothing pre-existing regressed).
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>Both gate modules exist, record the pre-fix RED as strict xfails traceable to 48-RED-EVIDENCE.md, and assert only values written down first in 48-EXPECTED-STRUCTURE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` / `.rst` author input → emitted Typst source | User-authored docnames, ids and section labels are interpolated into `.typ` text this phase's fixtures exercise |
| Emitted `.typ` → `typst.compile()` | The Typst compiler consumes text this project generates |

## STRIDE Threat Register

ASVS level 1, blocking threshold `high`. `48-RESEARCH.md` §Security Domain and `48-VALIDATION.md`
§Security Domain both record that ASVS **V2 (Authentication)**, **V3 (Session Management)**,
**V4 (Access Control)** and **V6 (Cryptography)** are **structurally inapplicable** — typsphinx is a
local Sphinx/Typst document-build tool with no network service, no authentication, no session and no
cryptographic surface. **V5 (Input Validation)** is already covered by the existing
`escape_typst_string` / `_sanitize_label` machinery, which this phase does not change. This block is
recorded rather than omitted, per the phase's security contract.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | Label interpolated into emitted Typst source by the new guard helper | low | mitigate | The guard helper receives a label ALREADY produced by `_namespace_label()`, which routes through `_sanitize_label()`; the helper never accepts a raw docutils id and never re-derives a label. Asserted at unit level in plan 48-03 |
| T-48-02 | Denial of Service | Per-reference `query()` introspection pass at Typst compile time | low | accept | D-11 fixes the cost tiers before measuring; the full-corpus baseline is recorded (28.93s / 28.56s) and the after-number is measured in plan 48-04 against those pre-fixed tiers |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no packages of any kind. `48-RESEARCH.md` §Package Legitimacy Audit records "no new PyPI dependency, no new `@preview` package"; binding constraint #7 forbids both |
</threat_model>

<verification>
- `uv run pytest tests/test_xref_compile_time_guard_render_gate.py tests/test_citation_caption_dangling_label_gate.py -q` exits 0 with xfails and no XPASS.
- `uv run pytest -m "not slow" -q` exits 0.
- `git status --porcelain typsphinx/` prints nothing for the whole plan.
- `48-EXPECTED-STRUCTURE.md` and `48-RED-EVIDENCE.md` both exist and are committed before any plan
  touches `typsphinx/`.
</verification>

<success_criteria>
- Two new fixture projects exist, each carrying a load-bearing-properties comment block.
- Two new gate modules exist with strict-xfail-recorded post-fix assertions.
- Both currently-reachable pre-fix REDs are recorded verbatim against the unfixed tree.
- D-04's unconstructible RED is recorded as an enumerated impossibility argument.
- Every value the later plans will assert is written down here first.
</success_criteria>

<output>
Create `.planning/phases/48-compile-time-cross-reference-guard/48-01-SUMMARY.md` when done
</output>
