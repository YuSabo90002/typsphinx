---
phase: 40-citations-full-round-trip
plan: 04
type: execute
wave: 3
depends_on: [40-03]
files_modified:
  - tests/test_desc_break_marker_buffer_swap_gate.py
  - .planning/phases/40-citations-full-round-trip/40-NONREGRESSION.md
autonomous: true
requirements: [CIT-01, CIT-03, CIT-04, CIT-05]

must_haves:
  truths:
    - "D-14's non-regression evidence is the full-corpus `-b typstpdf` gate ACTUALLY RUN green, not skipped: `uv run pytest tests/test_corpus_gate.py -m slow` executed at least once for real, its collected-and-passed counts recorded verbatim, and an explicit statement that no test in it reported `skipped`. A skip is not a pass."
    - "`tests/test_examples_charged_ieee_gate.py` is GREEN after the handlers landed, and `git log`/`git diff` show the module itself was never edited across the whole phase -- the RED recorded in `40-GATE-EVIDENCE-02.md` and this GREEN are the same assertions over the same file, which is what makes the flip meaningful."
    - "`tests/test_desc_break_marker_buffer_swap_gate.py`'s comment naming Phase 40's citation work as the milestone's sole `does not compile` exception is re-read against the finished phase and either confirmed still accurate or corrected in place -- it is not left to rot as a stale forward reference to work that has now shipped."
    - "`tests/test_corpus_gate.py` is confirmed unchanged and confirmed to have needed no change: both of its citation mentions are synthetic unit-test strings feeding its unknown-node warning-parser, not live-build assertions, so the live corpus no longer emitting that warning cannot make them stale."
    - "The milestone's standing invariants are re-proven over this phase's whole diff: zero new runtime dependencies, the `@preview` package count still four with no new version-lockstep site, and `tests/test_preview_version_sync.py` green and untouched."
    - "Every one of `40-VALIDATION.md`'s ten Per-Task Verification Map rows has a recorded, executed command and a recorded status, so the phase's validation contract is closed rather than left seeded."
    - "This plan modifies no file under `typsphinx/`, no file under `examples/`, and no test module other than the stale-comment fix."
  artifacts:
    - tests/test_desc_break_marker_buffer_swap_gate.py
    - .planning/phases/40-citations-full-round-trip/40-NONREGRESSION.md
  key_links:
    - "tests/test_corpus_gate.py -m slow -> the network-dependent real corpus; it skips gracefully offline, so the recorded evidence must distinguish `passed` from `skipped` explicitly or the phase closes on a gate that never ran."
    - "40-NONREGRESSION.md -> 40-GATE-EVIDENCE-01.md and 40-GATE-EVIDENCE-02.md; the GREEN half is only evidence if it names the RED half it flipped and the assertions are shown to be identical."
    - "tests/test_desc_break_marker_buffer_swap_gate.py's Phase-40 comment -> ROADMAP binding constraint #3, which is what makes Phase 40 the milestone's sole classic-RED exception; if the comment is corrected, the correction must stay consistent with that constraint rather than contradict it."
---

<objective>
Close Phase 40 with proof rather than assumption: run the full-corpus `-b typstpdf` gate for real and
record that it executed rather than skipped, re-run the untouched shipped-sample gate green, settle
the one stale forward reference this phase's landing creates, and record every green flip against the
REDs that 40-01 and 40-02 captured.

Purpose: D-14 says explicitly that non-regression must be PROVEN, not inferred from the `ids`
heuristic, and `40-VALIDATION.md` says explicitly that the corpus gate skipping offline is not the
gate passing. Both of those are only satisfiable after the handlers land, which is why they are their
own plan in their own wave rather than a footnote on 40-03.

Output: a possibly-corrected comment in one existing test module and
`40-NONREGRESSION.md`. This plan touches no file under `typsphinx/` and no file under `examples/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-citations-full-round-trip/40-CONTEXT.md
@.planning/phases/40-citations-full-round-trip/40-RESEARCH.md
@.planning/phases/40-citations-full-round-trip/40-VALIDATION.md
@.planning/phases/40-citations-full-round-trip/40-03-SUMMARY.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Skipping step 1 makes the worktree's pytest import the MAIN checkout's `typsphinx` package. In THIS
plan that failure mode is especially dangerous: the gates would report the PRE-fix colour and the
non-regression evidence this plan exists to produce would be a fabrication.
</environment_setup>

<flagged_assumptions>
The spec-less probe fallback is active for this phase. The full 11-row probe report, its disposition
table, the no-silent-drop equality statement and the prohibition-recall breadcrumbs are recorded once,
in `40-01-PLAN.md`'s `<flagged_assumptions>` block; they are not repeated here. Nine rows are authored
as plain `must_haves.truths` strings in `40-03-PLAN.md`; rows 10 (`CIT-05 / unclassified`) and 11
(`CIT-06 / unclassified`) remain `unresolved` and flagged, never auto-covered, never auto-backstopped
and never auto-dismissed.

This plan surfaces no new assumption of its own: every claim it makes is a recorded command output.
</flagged_assumptions>

<artifacts_this_phase_produces>
Phase-wide list of every symbol Phase 40 creates, so the plan-review source-grounding pass does not
flag a newly-created symbol as drift. Every plan in this phase carries this same list.

**Source symbols (`typsphinx/translator.py`)** -- all in plan 40-03

- `visit_citation(self, node)` -- NEW method (run-scoped grid open plus this row's two cells).
- `depart_citation(self, node)` -- NEW method (body-cell close, row comma, run-scoped grid close).
- `visit_label(self, node)` -- NEW method, raises `nodes.SkipNode`; fires for citations only.
- `_citation_run_neighbour(self, node, offset)` -- NEW private helper (working name) for run-adjacency
  scanning, scanning through siblings that emit nothing (`nodes.comment`, `nodes.system_message`).
- `visit_reference` / `depart_reference` -- EXISTING, gained the guarded own-`ids` bracket-wrap and
  its matching close plus label attachment (D-14).
- One NEW instance attribute slot in `__init__` carrying the D-14 bracket-wrap state between visit and
  depart (working name `_reference_own_anchor`); never a fourth consumer of the shared `_strong_was_*`
  slots (Phase 36 D-01/D-02).
- `_namespace_label`, `_sanitize_label`, `escape_typst_string`, `_emit_id_anchors`,
  `_visit_admonition`, `_depart_admonition`, `visit_footnote`, `visit_footnote_reference`,
  `depart_term`, `visit_target` -- **NOT modified**.

**Test symbols**

- `tests/fixtures/citation_render_gate/{conf.py,index.rst,second.rst}` -- NEW (plan 40-01).
- `tests/test_citation_render_gate.py` -- NEW (plan 40-01).
- The sentinel literals `CITORDERALPHA`, `CITORDERBRAVO`, `CITORDERCHARLIE`, `CITORDERDELTA`,
  `CITORDERECHO`, `CITNESTEDSENTINEL`, `CITBREAKONESENTINEL`, `CITBREAKTWOSENTINEL`,
  `CITSECONDDOCSENTINEL` -- NEW (plan 40-01).
- `tests/test_desc_break_marker_buffer_swap_gate.py` -- its Phase-40 sole-exception comment re-checked
  and corrected only if stale (THIS plan). No assertion in it changes.
- `tests/test_examples_charged_ieee_gate.py` -- **re-run, never edited** (plans 40-02 and this plan).
- `tests/test_corpus_gate.py` and `tests/test_preview_version_sync.py` -- **not modified**.

**Content**

- `examples/charged-ieee/{approach1,approach2}/source/index.rst` -- restored (plan 40-02).

**Planning artifacts**

- `40-GATE-EVIDENCE-01.md` (plan 40-01), `40-GATE-EVIDENCE-02.md` (plan 40-02),
  `40-NONREGRESSION.md` (THIS plan).
- `.planning/ROADMAP.md` -- Phase 40 SC#3 corrected plus a Roadmap Evolution entry (plan 40-02, D-09).

**Dependencies**

- None added; no `@preview` pin moves. Milestone invariants #1 and #2 hold.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Settle the stale Phase-40 forward reference and re-run the untouched shipped-sample gate</name>
  <read_first>
    - `tests/test_desc_break_marker_buffer_swap_gate.py` lines 225-260 -- the
      `TestDescBreakMarkerBufferSwapCompileGate` docstring naming Phase 40's citation work as the
      milestone's sole "does not compile" exception, and the assertions around it.
    - `.planning/ROADMAP.md` § "Binding constraints this roadmap is built on", item 3 -- the milestone
      rule that makes Phase 40 that exception, so any correction stays consistent with it rather than
      contradicting it.
    - `tests/test_corpus_gate.py` lines 205-245 and lines 485-510 -- both `citation` mentions, so the
      "needs no change" conclusion is re-derived from the code rather than taken on trust from
      RESEARCH.
    - `tests/test_examples_charged_ieee_gate.py` in full -- read, do not edit.
    - `.planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-02.md` -- the RED this task's
      green run must flip.
  </read_first>
  <files>tests/test_desc_break_marker_buffer_swap_gate.py</files>
  <action>
First, re-read `tests/test_desc_break_marker_buffer_swap_gate.py`'s compile-gate docstring and decide
whether its parenthetical about Phase 40's citation work is still accurate now that the phase has
shipped. Two things could have gone stale: the tense (it refers to Phase 40 as future work) and the
claim itself (the milestone rule says every OTHER phase's RED is structural and Phase 40's is the sole
classic compile fatal -- that remains true as a historical statement about the milestone, but the
sentence may read as though the exception is still pending). Correct it in place if it has gone
stale, keeping it consistent with ROADMAP binding constraint #3, and change nothing else in the file
-- no assertion, no fixture, no marker. If after reading it you conclude it is still accurate as
written, leave the file untouched and record that conclusion, with the quoted sentence, in the
summary; an unchanged file is a valid outcome for this task and must not be forced into an edit.

Second, re-derive the `tests/test_corpus_gate.py` conclusion for yourself. Read both of its citation
mentions and confirm each is a synthetic string literal fed to its unknown-node warning-parser rather
than an assertion over a live build's output. Confirm the module is unmodified. Do NOT edit it. If
either mention turns out to be a live-build assertion after all, stop and record it as a finding
rather than editing it here -- that would be a scope change RESEARCH explicitly measured as
unnecessary, and it needs to be surfaced rather than absorbed.

Third, re-run `tests/test_examples_charged_ieee_gate.py` and capture its full green output for Task 2.
Then prove the module was never edited across the whole phase: show that `git diff` between the
phase-start commit and now reports nothing for that path. Both approaches must build clean, with zero
warnings, and produce non-empty PDFs -- the same assertions that were RED in `40-GATE-EVIDENCE-02.md`.
  </action>
  <acceptance_criteria>
    - Either `tests/test_desc_break_marker_buffer_swap_gate.py`'s Phase-40 comment is corrected in place and the diff for that file contains only comment or docstring lines, or the file is unmodified and the summary quotes the sentence and states why it is still accurate.
    - No assertion, fixture, marker or import in `tests/test_desc_break_marker_buffer_swap_gate.py` changes; `uv run pytest tests/test_desc_break_marker_buffer_swap_gate.py -v` is green.
    - `tests/test_corpus_gate.py` is unmodified, and the summary states, from a fresh read, which lines hold the two synthetic citation strings and why neither is a live-build assertion.
    - `uv run pytest tests/test_examples_charged_ieee_gate.py -v` is fully green with zero warnings reported by its own zero-warning helper for both approaches.
    - `git diff --stat <phase-start-commit>..HEAD -- tests/test_examples_charged_ieee_gate.py tests/test_corpus_gate.py` is empty.
    - `git diff --stat -- typsphinx/ examples/` is empty over this plan's commits.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_examples_charged_ieee_gate.py -v &amp;&amp; uv run pytest tests/test_desc_break_marker_buffer_swap_gate.py -q &amp;&amp; test -z "$(git diff --stat -- typsphinx/ examples/ tests/test_corpus_gate.py tests/test_examples_charged_ieee_gate.py)"</automated>
  </verify>
  <done>The shipped-sample gate is green and provably unedited, the corpus gate is confirmed to have needed no change, and the one forward reference this phase's landing could have staled is settled either way.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full-corpus gate for real and record the whole phase's non-regression evidence</name>
  <read_first>
    - `tests/test_corpus_gate.py` -- how it is marked `slow`, what it fetches, and under what
      conditions it skips, so the evidence file can distinguish a real pass from a graceful skip.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-GAP-G39-1-CLOSEOUT.md` -- the
      in-repo precedent for recording a corpus-gate run as close-out evidence, including how it
      distinguished executed from skipped.
    - `.planning/phases/40-citations-full-round-trip/40-VALIDATION.md` § "Per-Task Verification Map"
      and § "Validation Sign-Off" -- the ten rows and the eight sign-off boxes this evidence closes.
    - `.planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md` and
      `40-GATE-EVIDENCE-02.md` -- the two REDs this file flips.
    - `pyproject.toml` and `uv.lock` -- to state the zero-new-dependency invariant from the real
      files rather than from memory.
    - `tests/test_preview_version_sync.py` -- the three declaration sites it keeps in lockstep, so
      the four-package invariant is stated from the test's own contract.
  </read_first>
  <files>.planning/phases/40-citations-full-round-trip/40-NONREGRESSION.md</files>
  <action>
Run `uv run pytest tests/test_corpus_gate.py -m slow -v` and let it complete. This is D-14's required
evidence and `40-VALIDATION.md`'s phase gate. If it SKIPS -- it is network-dependent and skips
gracefully offline -- do not record the run as satisfying the gate. Retry once network is available,
and if it genuinely cannot run in this environment, say so explicitly and in those words in the
evidence file and in the summary, and mark the phase gate as NOT satisfied rather than describing a
skip as a pass. Never paraphrase a skip into a pass.

Then write `.planning/phases/40-citations-full-round-trip/40-NONREGRESSION.md` recording, against a
named commit hash of the finished phase:

1. The commit hash, and the diffstat of the whole phase -- every file this phase touched and nothing
   else.
2. The full-corpus gate: the verbatim `uv run pytest tests/test_corpus_gate.py -m slow -v` output,
   with the passed/failed/skipped counts stated in plain words underneath and an explicit sentence
   naming which tests EXECUTED. If any test in it skipped, name it and say why.
3. The GREEN flip for the phase's own gate module: the verbatim
   `uv run pytest tests/test_citation_render_gate.py -v` output, mapped selector-by-selector back to
   the RED recorded in `40-GATE-EVIDENCE-01.md`, with a statement that the assertions are byte-identical
   between the two runs -- the tests flipped, they were not rewritten.
4. The GREEN flip for the shipped samples: the verbatim
   `uv run pytest tests/test_examples_charged_ieee_gate.py -v` output, mapped back to
   `40-GATE-EVIDENCE-02.md`, plus the `git diff` proof that the module was never edited.
5. The D-14 corpus byte-diff from 40-03 Task 1, restated with its command and its empty result, so the
   two independent non-regression proofs -- the `.typ` byte-diff and the full-corpus compile gate --
   sit side by side.
6. The milestone invariants, each with the command that proves it: `git diff` over `pyproject.toml`
   and `uv.lock` across the phase being empty (zero new runtime dependencies); the `@preview` package
   count still four across all three declaration sites, with `tests/test_preview_version_sync.py`
   green and unmodified; and `uv run black --check .`, `uv run ruff check .` and
   `uv run mypy typsphinx/` all passing.
7. A closed-out copy of `40-VALIDATION.md`'s Per-Task Verification Map: all ten rows, each with the
   plan and task that owns it, the command that was actually run, and a real status. Note in the
   summary that `40-VALIDATION.md`'s own frontmatter `wave_0_complete` and `nyquist_compliant` flags
   are owned by `/gsd-validate-phase`, not by this plan, so this file records the evidence rather than
   flipping those flags.
8. The stale-comment decision from Task 1, recorded either way with the quoted sentence.
9. An explicit statement of which tests actually executed versus which were skipped across every
   command in this file. A skip is not a pass, and this sentence is what a later reader checks first.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_corpus_gate.py -m slow -v` was executed and its verbatim output is in the evidence file; if any test skipped, the file names it and states the phase gate is not satisfied.
    - `.planning/phases/40-citations-full-round-trip/40-NONREGRESSION.md` exists and contains all nine recorded items, including the selector-by-selector RED-to-GREEN mapping for `tests/test_citation_render_gate.py`.
    - The file contains the phase diffstat and the `git diff` proof that `tests/test_examples_charged_ieee_gate.py` and `tests/test_corpus_gate.py` were never edited.
    - The file records `git diff -- pyproject.toml uv.lock` across the phase as empty and `tests/test_preview_version_sync.py` as green and unmodified.
    - The file contains the closed-out ten-row verification map with a real status in every row.
    - The file contains the executed-versus-skipped statement.
    - `uv run pytest` (full suite, slow included) is green.
    - `uv run black --check .`, `uv run ruff check .` and `uv run mypy typsphinx/` all pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_corpus_gate.py -m slow -v; uv run pytest -q &amp;&amp; test -s .planning/phases/40-citations-full-round-trip/40-NONREGRESSION.md &amp;&amp; test -z "$(git diff --stat -- pyproject.toml uv.lock)" &amp;&amp; uv run black --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy typsphinx/</automated>
  </verify>
  <done>The full-corpus gate has actually run green, both REDs are mapped to their GREENs with the assertions shown unchanged, and every milestone invariant is re-proven with its command recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the real documentation corpus -> `-b typstpdf` -> `typst.compile()` | The widest input this project ever compiles, and the only place a `visit_reference` regression on the hot path would surface at full scale. |
| a network-dependent gate -> the phase-close decision | A gate that skips silently and is recorded as green is indistinguishable from one that ran, unless the evidence separates the two. |
| this phase's diff -> the milestone invariants | Zero new dependencies and a `@preview` count of four are standing milestone rules; a phase closes against them or it does not close. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | Citation label-token derivation reaching a Typst label token | high | mitigate | Mitigated in 40-03 by routing every new label through the existing `_namespace_label`/`_sanitize_label`. Verified at full scale HERE by the full-corpus `-b typstpdf` gate, which compiles the real documentation corpus including its C-domain ids -- the input class that originally produced the unclosed-label fatal those helpers exist for. |
| T-40-02 | Denial of service | Separator miscounting merging or splitting a rendered construct | high | mitigate | Mitigated in 40-03 by the shared run-adjacency helper and asserted there by exact grid counts. Verified HERE by the full suite plus the full-corpus gate: any separator regression anywhere in the translator surfaces as a compile fatal in one of them. |
| T-40-03 | Denial of service | A link emitted to a label that was never attached, or a label defined twice | high | mitigate | Mitigated in 40-03 by three named guards. Verified HERE by the full-corpus gate, which is the only run in this project that would surface a `label does not exist` or `label occurs multiple times` fatal arising from a document combination no fixture reproduces. |
| T-40-SC | Tampering | npm/pip/cargo installs -- supply chain | high | mitigate | This plan installs nothing and this phase adds no dependency. Enforced HERE as an acceptance criterion: `git diff -- pyproject.toml uv.lock` across the whole phase is empty, the `@preview` count stays four across all three declaration sites, and `tests/test_preview_version_sync.py` is green and unmodified. |

ASVS L1: V5 Input Validation is the only applicable category -- a build-time Sphinx extension has no
runtime authentication, session, access-control or cryptography surface (`40-RESEARCH.md` § Security
Domain). Blocking threshold is `high`. All four threats are `high` and all four are `mitigate`; every
mitigation landed in an earlier plan and is verified here at full scale. Nothing blocks this plan.
</threat_model>

<verification>
- `uv run pytest tests/test_corpus_gate.py -m slow -v` ran and reported passes, not skips, and the
  distinction is stated explicitly in `40-NONREGRESSION.md`.
- `uv run pytest tests/test_citation_render_gate.py -v` is green across all eight selectors and its
  assertions are unchanged from the RED recorded in `40-GATE-EVIDENCE-01.md`.
- `uv run pytest tests/test_examples_charged_ieee_gate.py -v` is green and the module is provably
  unedited across the phase.
- `uv run pytest` (full suite, slow included) is green.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
- `git diff --stat -- pyproject.toml uv.lock` across the phase is empty and
  `tests/test_preview_version_sync.py` is green and unmodified.
- `git diff --stat -- typsphinx/ examples/` is empty for this plan's commits.
</verification>

<success_criteria>
- The full-corpus `-b typstpdf` gate has ACTUALLY run green at least once; a skip is recorded as a
  skip and never as a pass.
- Both GATE-01 REDs are mapped to their GREENs selector by selector, with the assertions shown to be
  unchanged between the two runs.
- The shipped-sample gate and the corpus gate are both proven unedited across the whole phase.
- The one comment that this phase's landing could have staled is settled, either by an in-place
  correction confined to comment lines or by a recorded conclusion that it is still accurate.
- Every milestone invariant is re-proven with the command that proves it recorded alongside it.
- All ten rows of `40-VALIDATION.md`'s verification map carry an executed command and a real status.
</success_criteria>

<output>
Create `.planning/phases/40-citations-full-round-trip/40-04-SUMMARY.md` when done.
</output>
