---
phase: 40-citations-full-round-trip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/citation_render_gate/conf.py
  - tests/fixtures/citation_render_gate/index.rst
  - tests/fixtures/citation_render_gate/second.rst
  - tests/test_citation_render_gate.py
  - .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md
autonomous: true
requirements: [CIT-01, CIT-02, CIT-03, CIT-04, CIT-06]

must_haves:
  truths:
    - "One two-document fixture Sphinx project exercises, in a single build, every citation scenario this phase must decide: a forward reference (definition after first use), two citations of one key (D-03 multi-backref shape), a single-citation key (D-03 single shape), an uncited definition (D-07), a duplicate key defined in both documents (D-10), a cross-document citing site (D-10a), a dangling citing reference (T-40-03), a citation defined and cited inside a list item (SC#5), a citing reference inside a code-mode concat context (SC#5), a citation run broken by a paragraph (D-06), and a citation run whose members are separated only by RST comments (which emit nothing and must NOT break the run)."
    - "CIT-01's RED is the CLASSIC compile fatal and is captured as a genuine RED-to-GREEN flip: the SAME test, over the SAME fixture, fails today because `sphinx-build -b typstpdf` exits non-zero with the verbatim `TypstError: expected semicolon or line break`, and passes after the handlers land. It is NOT written in the durable-mechanical-reconstruction shape of `tests/test_typst_elements_pass_through_gate.py`."
    - "Every expected label token in the gate module is EXTRACTED from the emitted `.typ` on both sides and compared for byte equality (definition anchor versus citing link target), never hard-coded against an assumed value -- so a wrong duplicate-key resolution direction (RESEARCH Pitfall 3) cannot be laundered into a false RED or a false GREEN."
    - "The D-10 assertion is authored against the DEFINITION-side namespacing property (two distinct, non-colliding anchors `index:same2020` and `second:same2020` both exist) and never against a presumed reference-resolution direction, because Sphinx's citation domain resolves a duplicate key last-registered-wins across the whole build."
    - "Every entry-body ordering and layout assertion is anchored on a unique per-entry sentinel token placed at the head of the entry body, so an assertion can never be satisfied by the label text appearing at an unrelated citing site earlier in the document."
    - "The layout claim (CIT-02) is a `pypdf` `extraction_mode=\"layout\"` leading-column measurement, and the back-reference claim (CIT-04) is a compiled-PDF `/Annots` link-rectangle assertion plus an ordered `.typ`-string assertion -- neither is an eye check, per ROADMAP SC#2."
    - "`40-GATE-EVIDENCE-01.md` records, against a named commit of the untouched translator: the fixture source, the verbatim pre-fix emitted `.typ` citation fragment, the verbatim `ExtensionError` / `TypstError` text, and the verbatim RED pytest output -- so the RED survives the fix landing."
    - "This plan modifies no file under `typsphinx/`."
  artifacts:
    - tests/fixtures/citation_render_gate/conf.py
    - tests/fixtures/citation_render_gate/index.rst
    - tests/fixtures/citation_render_gate/second.rst
    - tests/test_citation_render_gate.py
    - .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md
  key_links:
    - "tests/test_citation_render_gate.py -> tests/fixtures/citation_render_gate/ via a module-scoped build fixture that runs the build ONCE and returns the CompletedProcess plus the build dir WITHOUT asserting on the return code (pre-fix the build fails; an asserting fixture would error every test instead of producing readable per-requirement REDs)."
    - "tests/test_citation_render_gate.py -> `sys.executable -m sphinx` (never a bare `sphinx-build`, never `uv run sphinx-build`) -- the NixOS PATH-shadowing hazard restated in every render-gate module in this repo."
    - "the per-entry sentinel tokens -> both the fixture `.rst` bodies and every ordering/layout assertion; a sentinel renamed in one file and not the other silently un-anchors the assertion built on it."
    - "tests/fixtures/citation_render_gate/index.rst toctree -> second.rst; if `second` leaves the toctree it is no longer in the master include-set and every cross-document assertion degrades to plain text instead of a link."
  prohibitions:
    - requirement_id: CIT-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT author any expected value in this gate module by reading what the translator emits -- neither its current pre-fix output nor a post-fix sample. Every expectation is derived from 40-CONTEXT.md's locked decisions and from cross-comparing two independently-emitted halves of the same build, so a wrong implementation cannot be laundered into a green gate."
    - requirement_id: CIT-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT let a SKIPPED test be reported or recorded as a passing gate. Every compiled-PDF assertion in this module sits behind a `TYPST_AVAILABLE` guard, and a run in which the guard skipped is evidence of nothing -- the evidence file must state which tests actually executed."
---

<objective>
Stand up the whole Wave 0 test surface for Phase 40 and record the milestone's sole classic GATE-01
RED before a single line of handler code exists: a two-document fixture Sphinx project covering every
citation scenario this phase decides, a new render-gate module whose expectations come from the
locked decisions (D-03, D-05, D-06, D-07, D-08, D-10, D-13, D-14) rather than from emitted output,
and `40-GATE-EVIDENCE-01.md` recording the verbatim `TypstError: expected semicolon or line break`
against a named commit.

Purpose: CIT-01 is the one requirement in the v0.7.0 milestone where "does not compile" is available
as the RED state (ROADMAP binding constraint #3). That RED is only worth anything if it is captured
against the UNFIXED translator, so this plan is strictly earlier in the wave order than any plan that
touches `typsphinx/`.

Output: `tests/fixtures/citation_render_gate/` (three files), `tests/test_citation_render_gate.py`,
and `40-GATE-EVIDENCE-01.md`. This plan does NOT touch `typsphinx/`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-citations-full-round-trip/40-CONTEXT.md
@.planning/phases/40-citations-full-round-trip/40-RESEARCH.md
@.planning/phases/40-citations-full-round-trip/40-PATTERNS.md
@.planning/phases/40-citations-full-round-trip/40-VALIDATION.md
@CLAUDE.md
</context>

<environment_setup>
Worktree isolation is this project's STANDING execution mode (`CLAUDE.md` § "Worktree-isolated
execution"). Before running ANY command in this plan:

1. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
2. `for t in uv ruff; do [ -x "$(command -v $t)" ] && ln -sf "$(command -v $t)" ".venv/bin/$t"; done`
3. Run everything through `uv run` from then on.

Skipping step 1 makes the worktree's pytest import the MAIN checkout's `typsphinx` package, so gates
report the wrong colour.
</environment_setup>

<flagged_assumptions>
This phase has no `40-SPEC.md`, so `## Edge Coverage` and `## Prohibitions` were both absent and the
spec-less probe fallback is active. The orchestrator ran the deterministic edge probe over the six
CIT requirement texts and returned **11 rows, all `unresolved`, two of them `unclassified`**.

Nine rows are resolved to `covered` here and authored as plain `must_haves.truths` strings in
**40-03-PLAN.md** (the plan whose implementation makes them true). The two `unclassified` rows are
NEVER auto-`covered`, NEVER auto-`backstop`ped and NEVER auto-dismissed -- they are surfaced below as
explicit flagged assumptions. **No row is authored as a `backstop`**: every one of the nine has a
defensible acceptance criterion drawn from a measurement already recorded in `40-CONTEXT.md`.

**No-silent-drop equality: 11 probe-surfaced rows == 9 authored into `must_haves` (40-03-PLAN.md
truths) + 2 surfaced as flagged assumptions.**

| # | Req | Probe category | Disposition | Where it lands |
|---|-----|----------------|-------------|----------------|
| 1 | CIT-01 | empty | covered | 40-03 truth: a document with zero citations opens no grid and its emitted `.typ` is byte-identical to pre-phase; a document with exactly one citation opens exactly one grid holding exactly one row, and compiles. |
| 2 | CIT-01 | encoding | covered | 40-03 truth: label text and entry-body text reach the `.typ` only through the normal visitor chain, so the single existing `escape_typst_string` path applies; a citation body carrying a double quote and a non-ASCII character compiles and survives into extracted PDF text. No second escaping routine is introduced. |
| 3 | CIT-02 | empty | covered | 40-03 truth: D-07 -- an uncited definition (`backrefs == []`) still renders as a row, with a plain non-linked label and no back-reference marker. |
| 4 | CIT-02 | encoding | covered | 40-03 truth: D-05 -- "past the label" is Typst's own rendered-glyph width of the widest label cell (the grid's `auto` column), asserted from `pypdf` leading-column positions, never from a Python character count. |
| 5 | CIT-03 | empty | covered | 40-03 truth: T-40-03 -- a citing reference whose key has no definition anywhere never emits a `link(<...>)` to a label that was never attached; its text renders as plain inline content and Sphinx's own `citation not found` warning is the only signal. |
| 6 | CIT-03 | encoding | covered | 40-03 truth: D-13 -- the definition's anchor token and the citing site's link-target token are computed at one derivation point (`_namespace_label` / `_sanitize_label`) and are asserted byte-equal by extracting BOTH from the emitted `.typ`, never by hard-coding either. |
| 7 | CIT-04 | empty | covered | 40-03 truth: D-03 -- `len(backrefs) == 1` emits no `(1)` marker; the bracketed label itself becomes the back-link. `len(backrefs) == 0` emits a plain non-linked label. |
| 8 | CIT-04 | adjacency | covered | 40-03 truth: D-05/D-06 -- adjacent citation siblings share exactly one grid; a paragraph between two citations breaks the run into two grids; a node that emits nothing (an RST comment, a system message) does NOT break the run. |
| 9 | CIT-04 | ordering | covered | 40-03 truth: D-08 -- back-reference markers appear in docutils' own `backrefs` order, and the marker list contains only same-document citing sites. |
| 10 | CIT-05 | unclassified | **unresolved (flagged)** | The engine could not classify CIT-05. Planning's honest read: CIT-05 is a verbatim content restore (D-11/D-12) of two shipped `.rst` files -- there is no input domain, no boundary and no ordering for the edge taxonomy to reach. Its correctness axis is "the diff is exactly the reverse of `8bed1a3`/`c014a0b` and nothing more", which is carried as a prohibition and as byte-equality acceptance criteria in 40-02-PLAN.md, not as an edge predicate. Left `unresolved`; deliberately NOT converted into a probe-derived `must_haves` entry. |
| 11 | CIT-06 | unclassified | **unresolved (flagged)** | The engine could not classify CIT-06. Planning's honest read: "document order, unsorted" is satisfied by DOING NOTHING -- docutils' depth-first traversal order IS document order, and the design contains no sort step to have an ordering edge about (40-RESEARCH.md § Don't Hand-Roll). ROADMAP SC#4 nonetheless gives it a real, mechanical assertion (extracted-PDF sentinel order), which is carried as an ordinary `must_haves.truths` entry in 40-03-PLAN.md -- that entry is authored from SC#4, NOT from this probe row. Left `unresolved` as a probe row. |

**Prohibition recall (section B, run in-prompt -- there is no compiled engine).** Stage 1
over-produced ~10 must-NOT candidates per requirement; Stage 2 dropped the routine-engineering ones
(must not mutate emitted bytes of unrelated references, must not throw on an empty `backrefs` list,
must not leak a build directory) because they are ordinary correctness owned by the edge probe and by
this plan's acceptance criteria. Four survivors are authored **descriptor-less** into the
`must_haves.prohibitions:` sibling block of the plan they bind: two here, one in 40-02, one in 40-03.

Canon-referral breadcrumbs (kept by Stage 2, then DROPPED without minting, per ADR-550 D6):

- *"MUST NOT interpolate a citation key or entry body into a Typst string literal or a `<label>`
  token without sanitizing"* -- injection is canon; covered by `/gsd-secure-phase` and carried as
  threat `T-40-01` with a named one-line mitigation (route every label through the existing
  `_namespace_label`/`_sanitize_label`). Not minted as a prohibition.
</flagged_assumptions>

<artifacts_this_phase_produces>
Phase-wide list of every symbol Phase 40 creates, so the plan-review source-grounding pass does not
flag a newly-created symbol as drift. Every plan in this phase carries this same list.

**Source symbols (`typsphinx/translator.py`)** -- all in plan 40-03

- `visit_citation(self, node)` -- NEW method. Run-scoped: opens `grid(columns: (auto, 1fr), ...)` on
  the first citation of a run, emits this citation's two grid cells, and never opens a second grid
  inside a run.
- `depart_citation(self, node)` -- NEW method. Closes the body cell, emits the row-trailing comma, and
  closes the grid on the last citation of a run.
- `visit_label(self, node)` -- NEW method, raises `nodes.SkipNode`. Fires for citations only (measured:
  `visit_footnote` raises `SkipNode` before its own `label` child is reached, and
  `visit_footnote_reference` walks `footnote_node.children[1:]`). Its job is to keep the citation's
  already-consumed `label` child from rendering a second time into the body cell, and it removes the
  second unknown-node warning the shipped samples currently emit.
- `_citation_run_neighbour(self, node, offset)` -- NEW private helper (working name; the executor may
  choose another) implementing the run-adjacency scan: `node.parent.index(node)` plus a bounds-checked
  step of `offset`, skipping siblings that emit nothing (`nodes.comment`, `nodes.system_message`) and
  returning whether the first emitting neighbour is another `nodes.citation`.
- `visit_reference(self, node)` -- EXISTING, gains a guarded own-`ids` bracket-wrap open (D-14).
- `depart_reference(self, node)` -- EXISTING, gains the matching close plus the `<label>` attachment.
- One new instance attribute slot initialised in `__init__` to carry the D-14 bracket-wrap state
  between `visit_reference` and `depart_reference` (working name `_reference_own_anchor`). It is a NEW
  slot, never a fourth consumer of the shared `_strong_was_*` slots (Phase 36 D-01/D-02).
- `_namespace_label`, `_sanitize_label`, `escape_typst_string`, `_emit_id_anchors`,
  `_visit_admonition`, `_depart_admonition`, `visit_footnote`, `visit_footnote_reference`,
  `depart_term` -- **NOT modified**. They are read as precedent and reused unchanged.

**Test symbols**

- `tests/fixtures/citation_render_gate/conf.py`, `index.rst`, `second.rst` -- NEW fixture directory
  (this plan).
- `tests/test_citation_render_gate.py` -- NEW module (this plan), carrying the standard render-gate
  scaffolding (`TYPST_AVAILABLE` import guard, `temp_build_dir`, `_run_sphinx_build_typstpdf`), a
  module-scoped non-asserting build fixture, and layout/annotation helper functions copied from
  `tests/test_rubric_indent_invariance.py`.
- The per-entry sentinel tokens `CITORDERALPHA`, `CITORDERBRAVO`, `CITORDERCHARLIE`, `CITORDERDELTA`,
  `CITORDERECHO`, `CITNESTEDSENTINEL`, `CITBREAKONESENTINEL`, `CITBREAKTWOSENTINEL`,
  `CITSECONDDOCSENTINEL` -- NEW literals shared between the fixture and the module (this plan).
- `tests/test_desc_break_marker_buffer_swap_gate.py` -- its Phase-40 "sole exception" comment is
  re-checked for accuracy and updated if it has gone stale (plan 40-04). No other change.
- `tests/test_examples_charged_ieee_gate.py` -- **re-run, never edited** (plans 40-02 and 40-04).
- `tests/test_corpus_gate.py` -- **not modified**. Both of its `citation` mentions are synthetic
  unit-test strings feeding the unknown-node warning-parser, not live-build assertions (verified by
  RESEARCH at lines 210-241 and 490-503).
- `tests/test_preview_version_sync.py` -- **not modified**; this phase adds no package and moves no pin.

**Content**

- `examples/charged-ieee/approach1/source/index.rst` and
  `examples/charged-ieee/approach2/source/index.rst` -- restored (plan 40-02).

**Planning artifacts**

- `40-GATE-EVIDENCE-01.md` (this plan) -- the classic compile-fatal RED.
- `40-GATE-EVIDENCE-02.md` (plan 40-02) -- the shipped-sample RED.
- `40-NONREGRESSION.md` (plan 40-04) -- the GREEN flip plus the actually-ran full-corpus gate.
- `.planning/ROADMAP.md` -- SC#3 wording corrected and a Roadmap Evolution entry added (plan 40-02,
  D-09).

**Dependencies**

- None added. No entry changes in `[project] dependencies` or
  `[project.optional-dependencies] dev`; no `@preview` pin moves. Milestone invariants #1 and #2 hold.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build the two-document citation render-gate fixture</name>
  <read_first>
    - `tests/fixtures/cross_doc_label_namespace_render_gate/conf.py` and its `index.rst` /
      `pagea.rst` / `pageb.rst` -- the two-document fixture layout this one mirrors, and the
      convention that every fixture `conf.py` opens with a comment block naming the root cause it
      reproduces.
    - `tests/fixtures/confval_field_body_render_gate/conf.py` -- the single-master `typst_documents`
      declaration shape and the comment explaining why `index` must be a master document (only the
      master compile path surfaces the fatal).
    - `tests/fixtures/admonition_render_gate/index.rst` -- this repo's sentinel-token convention (one
      uppercase run, no underscores, unique in the repository) and the per-construct `..` comment
      naming requirement / expected behaviour / CONTROL-versus-DEFECT-CASE label.
    - `.planning/phases/40-citations-full-round-trip/40-VALIDATION.md` § "Wave 0 Requirements" -- the
      eight scenarios this fixture must carry.
    - `.planning/phases/40-citations-full-round-trip/40-RESEARCH.md` § "Common Pitfalls" (all three)
      and § "Current pre-phase failure inside a list item" -- the list-item case fails today with a
      DIFFERENT error than the top-level case, and the duplicate-key resolution direction is
      last-registered-wins.
    - `.planning/phases/40-citations-full-round-trip/40-CONTEXT.md` § "Implementation Decisions"
      (D-03, D-05, D-06, D-07, D-08, D-10, D-13).
  </read_first>
  <files>tests/fixtures/citation_render_gate/conf.py, tests/fixtures/citation_render_gate/index.rst, tests/fixtures/citation_render_gate/second.rst</files>
  <action>
Create a new fixture directory `tests/fixtures/citation_render_gate/` with exactly three files. Do
not copy any existing fixture wholesale; build it to the scenario list below.

`conf.py`: project `"Citation Render Gate"`, author `"Test Author"`, release `"1.0.0"`,
`extensions = ["typsphinx"]`, and a single-master `typst_documents` entry naming `index` as both
source and target. Open the file with a comment block stating the root cause this fixture reproduces:
`citation` and `label` have no handler, so the emitted `.typ` juxtaposes two code-mode expressions
and the master compile aborts; `index` must be a master document because that fatal only fires inside
`TypstPDFBuilder.finish()`.

`index.rst` -- a document title, then a `toctree` with `:maxdepth: 1` listing `second`, then these
sections in this order. Put a `..` comment above every construct naming the decision or requirement
it serves and whether it is a DEFECT CASE or a CONTROL.

Section "Citing Sites" -- six paragraphs, each a separate paragraph:
  1. cites `Krizhevsky2012` (a FORWARD reference: its definition is in the References section far
     below). SC#3 / CIT-03.
  2. cites `Krizhevsky2012` a second time. Together with paragraph 1 this gives that definition two
     entries in `backrefs`, which is D-03's multi-marker shape and CIT-04's proof.
  3. cites `Solo1998` exactly once -- D-03's single-marker shape.
  4. cites `Cross2019`, which is defined ONLY in `second.rst` -- the cross-document citing site
     (D-10a). This paragraph must not also define anything.
  5. cites `Same2020`, the key defined in BOTH documents (D-10b). Add a `..` comment recording that
     Sphinx's citation domain resolves a duplicate key last-registered-wins, so which document this
     reference lands in is decided by Sphinx and must never be hard-coded in an assertion.
  6. cites `Nosuchkey`, for which no definition exists anywhere -- the dangling-citing-reference case
     behind threat T-40-03. Add a `..` comment recording that Sphinx itself warns
     `citation not found` and leaves the reference unresolved before the translator runs, so the
     expected emission is plain text and no link call.

Section "Concat Protocol" -- a definition list whose TERM carries a citing reference to `Concat2000`,
with a short definition body. This is SC#5's code-mode-concat boundary: a term is one of this
translator's five concat contexts, where adjacent inline expressions are `+`-joined. Comment it as
such.

Section "Nested Protocol" -- a bullet list with two items. The first is a plain paragraph. The second
contains, in order: an introductory paragraph, a citation definition for `Nested2021` whose body
begins with the sentinel `CITNESTEDSENTINEL`, and a closing paragraph that cites `Nested2021`. This is
SC#5's list-item boundary and RESEARCH's independently-reproduced second failure mode -- comment it
with the fact that this construct fails today with `label ... does not exist in the document`, a
different fatal from the top-level syntax one.

Section "References" -- five citation definitions, consecutive siblings, in this order. Each body
STARTS with its own sentinel token so a layout or ordering assertion anchors on the body's own left
edge and can never be satisfied by the label text appearing at a citing site earlier in the document:
  1. `Krizhevsky2012`, sentinel `CITORDERALPHA`, then the real Krizhevsky/Sutskever/Hinton 2012
     reference text padded out so the entry body WRAPS onto at least a second visual line -- CIT-02's
     continuation-line measurement needs a wrapped body.
  2. `Solo1998`, sentinel `CITORDERBRAVO`, one short line.
  3. `Never1999`, sentinel `CITORDERCHARLIE`, one short line. This entry is cited by nothing -- D-07's
     uncited case, and the reason Sphinx will log a `is not referenced` warning for this fixture.
  4. `Same2020`, sentinel `CITORDERDELTA`, one short line. D-10's duplicate key.
  5. `Concat2000`, sentinel `CITORDERECHO`, one short line whose text also contains a literal double
     quote character and a non-ASCII character, so the existing `escape_typst_string` path is
     exercised by the body cell.
Put a `..` comment BETWEEN two of these five definitions (not just above the first). An RST comment
emits nothing, so it must NOT break the run -- these five must still land in ONE grid. Comment that
requirement explicitly, because it is the difference between a correct run scan and one that silently
splits a reference list in half.

Section "Run Break" -- a citation definition for `Break2021` whose body starts with sentinel
`CITBREAKONESENTINEL`, then a real PARAGRAPH of prose, then a citation definition for `Break2022`
whose body starts with sentinel `CITBREAKTWOSENTINEL`. D-06: the paragraph breaks the run, so these
two must land in TWO separate grids.

`second.rst` -- a document title, then a paragraph citing `Krizhevsky2012` (defined in `index.rst`):
the cross-document citing site that, per D-08, gets a working forward link and NO back-reference in
`index.rst`'s definition. Then a "References" section with two consecutive definitions: `Cross2019`
(cited only from `index.rst`) and `Same2020` (the duplicate key), the second one's body starting with
sentinel `CITSECONDDOCSENTINEL`. Comment both with the decision they serve.

Do not add a `conf.py` `suppress_warnings` entry, do not add `nitpicky`, and do not attempt to make
the fixture warning-free -- the dangling reference and the uncited definition are deliberate and their
warnings are part of what the module asserts on.
  </action>
  <acceptance_criteria>
    - The three files exist at `tests/fixtures/citation_render_gate/{conf.py,index.rst,second.rst}`.
    - `uv run python -m sphinx -b typst tests/fixtures/citation_render_gate /tmp/cit40fix` exits 0 and writes both `/tmp/cit40fix/index.typ` and `/tmp/cit40fix/second.typ`.
    - That same build's combined output contains at least one `unknown node type: <citation` warning and at least one `unknown node type: <label` warning -- the pre-fix signature this phase removes.
    - `grep -c 'CITORDER' tests/fixtures/citation_render_gate/index.rst` is 5, and each of `CITORDERALPHA`, `CITORDERBRAVO`, `CITORDERCHARLIE`, `CITORDERDELTA`, `CITORDERECHO` appears exactly once in the file.
    - `grep -c '^\.\. \[' tests/fixtures/citation_render_gate/index.rst` counts the top-level citation definitions; the References section contributes 5 consecutive ones and the Run Break section contributes 2 non-consecutive ones.
    - `index.rst` contains a citation definition indented inside a bullet-list item and a citing reference to that same key inside the same item.
    - `index.rst` contains a definition-list term that carries a citing reference.
    - `second.rst` defines `Cross2019` and `Same2020` and cites `Krizhevsky2012`; `index.rst` cites `Cross2019` and never defines it.
    - `uv run python -c "import typst; typst.compile('/tmp/cit40fix/index.typ')"` raises, and the raised message contains `expected semicolon or line break` -- the classic RED is reachable from this fixture.
  </acceptance_criteria>
  <verify>
    <automated>rm -rf /tmp/cit40fix &amp;&amp; uv run python -m sphinx -b typst tests/fixtures/citation_render_gate /tmp/cit40fix 2&gt;&amp;1 | tee /tmp/cit40fix.log &amp;&amp; test -f /tmp/cit40fix/index.typ &amp;&amp; test -f /tmp/cit40fix/second.typ &amp;&amp; grep -q 'unknown node type: &lt;citation' /tmp/cit40fix.log &amp;&amp; grep -q 'unknown node type: &lt;label' /tmp/cit40fix.log &amp;&amp; test "$(grep -c 'CITORDER' tests/fixtures/citation_render_gate/index.rst)" = "5"</automated>
  </verify>
  <done>A two-document fixture builds under `-b typst`, emits both unknown-node warnings, and its emitted `index.typ` fails a real `typst.compile()` with the verbatim classic syntax fatal.</done>
</task>

<task type="auto">
  <name>Task 2: Write the .typ-string and real-compile half of the gate module, RED against the untouched translator</name>
  <read_first>
    - `tests/test_confval_field_body_render_gate.py` lines 1-200 -- the RED-to-GREEN real-compile gate
      shape this module must mirror exactly (build, assert `returncode == 0`, assert the fatal text is
      absent from stderr, assert the `.typ` structure, assert the PDF exists and starts with the PDF
      magic bytes).
    - `tests/test_cross_doc_label_namespace_render_gate.py` in full -- the `TYPST_AVAILABLE` import
      guard, the `temp_build_dir` fixture, `_run_sphinx_build_typstpdf` with its `sys.executable -m
      sphinx` argv and NixOS rationale docstring, `_strip_raw_literals`, and above all the technique
      of grepping BOTH emitted `.typ` files and asserting a citing site's link target equals the
      target document's own anchor rather than a hard-coded guess.
    - `typsphinx/translator.py` lines 3519-3613 (`_sanitize_label`, `_current_docname`,
      `_namespace_label`) -- the exact token derivation both sides of every label assertion must
      agree on.
    - `typsphinx/translator.py` lines 3820-4031 (`visit_reference` / `depart_reference`) -- the four
      emission branches, so the expected shape of each citing site in this fixture is derived from
      the code that will produce it rather than guessed.
    - `.planning/phases/40-citations-full-round-trip/40-RESEARCH.md` § "GATE-01 RED capture" -- the
      explicit instruction that this must be a genuine RED-to-GREEN flip and NOT the durable
      mechanical-reconstruction pattern of `tests/test_typst_elements_pass_through_gate.py`.
    - `tests/fixtures/citation_render_gate/index.rst` and `second.rst` as created by Task 1.
  </read_first>
  <files>tests/test_citation_render_gate.py</files>
  <action>
Create `tests/test_citation_render_gate.py`. Open it with a module docstring that states the phase,
the six requirements, the classic-RED status of CIT-01, and the RED-versus-GREEN split of every test
in the file against the untouched translator.

Scaffolding, copied verbatim in shape from `tests/test_cross_doc_label_namespace_render_gate.py`: the
`try: import typst / except ImportError: TYPST_AVAILABLE = False` guard, a `temp_build_dir` fixture,
a `citation_render_gate_dir` fixture resolving the fixture path from `Path(__file__).parent`, and
`_run_sphinx_build_typstpdf` built from `sys.executable` and the `-m sphinx` module form with the
NixOS PATH-shadowing rationale restated in its docstring. Never invoke `sphinx-build` as an argv
element and never invoke it through `uv run`.

Add ONE module-scoped build fixture that runs `-b typstpdf` over the citation fixture exactly once and
returns a small record carrying the `CompletedProcess`, the build directory, and the text of
`index.typ` and `second.typ` when they exist. This fixture must NOT assert on the return code: pre-fix
the build fails, and an asserting fixture would turn every test in the module into a fixture error
instead of the readable per-requirement RED this gate exists to record. It must, however, raise a
clear `AssertionError` naming the missing file if a `.typ` it is asked for was never emitted.

Write these tests. Each name must contain the `-k` selector token that `40-VALIDATION.md`'s Per-Task
Verification Map already fixes, as one unbroken lowercase run so a `-k` substring match selects it:
`compile`, `link`, `namespace`, `separator`, `uncited`. Name every failure message with the decision
or requirement it enforces.

`compile` -- CIT-01, the classic RED. Assert the build's `returncode` is 0; assert
`expected semicolon or line break` does not appear in the combined stdout/stderr; assert
`Typst compilation failed` does not appear; assert `index.pdf` exists, is non-empty, and begins with
the PDF magic bytes. Pre-fix all of these fail because `TypstPDFBuilder.finish()` raises
`ExtensionError`. This is the SAME test that must pass post-fix, never deleted and never rewritten.

`link` -- CIT-03 plus D-14. From `index.typ`, extract the link-target token of the first citing site
for `Krizhevsky2012` and, separately, the anchor token attached to that key's definition row; assert
the two strings are equal. Do the same for the cross-document direction: extract the link target
`second.typ`'s citing site uses for `Krizhevsky2012` and assert it equals the anchor `index.typ`
emitted. Never write either token as a literal in the test. Add the D-14 half: assert every
citation-derived citing site in `index.typ` carries its own attached anchor token derived from that
site's docutils id, and add the negative control that a reference which is NOT citation-derived (the
toctree-generated link to `second`) carries no such attached anchor -- this is the own-`ids` guard
whose non-regression 40-03 must prove. Add the T-40-03 negative: no link call in `index.typ` targets
an anchor derived from the undefined key `Nosuchkey`, and that key's text still appears as ordinary
content. Run `_strip_raw_literals`-equivalent filtering before any label scan so a label form quoted
as prose in the fixture cannot masquerade as a real label expression.

`namespace` -- D-13 and D-10. Assert `index.typ` emits an anchor for its own `Same2020` definition and
`second.typ` emits an anchor for its own `Same2020` definition, that the two tokens are DIFFERENT, and
that neither is a bare un-namespaced token. Assert both tokens equal what `_namespace_label` would
produce for their respective docnames -- compute the expectation by importing the translator's own
helper, not by transcribing a string. Do NOT assert which of the two the `index.rst` citing site
resolves to: record in the test docstring that Sphinx's citation domain resolves a duplicate key
last-registered-wins across the build, and assert only that whichever it chose is one of the two
emitted anchors.

`separator` -- SC#5's three protocols, checked explicitly rather than by analogy to the footnote
handlers. (a) Paragraph boundary: the grid opening in the References section is emitted as its own
statement, with the preceding paragraph's emission terminated, and the whole document compiles. (b)
Code-mode concat boundary: inside the definition-list term's emission region, the citing reference is
joined to its sibling inline content with the concat operator and no operator is left without a right
operand. (c) List-item boundary: inside the bullet list's second item, the nested citation's grid is
newline-separated from the paragraph before it and the paragraph after it. Express each as a
region-scoped assertion over a slice of the emitted `.typ` located from the sentinel or the term text
-- never as a document-wide substring search and never as a global occurrence count, either of which
greens whenever any unrelated construct in the file happens to produce the same token.

`uncited` -- D-07, the deliberate inverse of the footnote precedent. Assert the `CITORDERCHARLIE`
sentinel is present in `index.typ` (the entry renders at all), that its row's label cell contains no
link call (a plain, non-linked label), and that the entry occupies a row in the SAME grid as its four
References siblings. Add the explicit contrast in the docstring: Phase 14's D-09 drops an unreferenced
footnote; D-07 requires the opposite for citations.

Add one grid-structure test covering D-05 and D-06 (its name may carry any of the tokens above or a
new one): the References run's five consecutive definitions, separated in the source only by an RST
comment, produce exactly ONE grid opening; the Run Break section's two definitions, separated by a
paragraph, produce exactly TWO. Derive the expected counts from the fixture's authored structure and
state that derivation in the test docstring.

This half of the module makes only `.typ`-string assertions plus the build-level compile assertion, so
mark nothing `slow`. The compile-dependent tests sit behind the `TYPST_AVAILABLE` skip guard; the
pure `.typ`-string tests must NOT, so they still run where `typst-py` is unavailable.
  </action>
  <acceptance_criteria>
    - `tests/test_citation_render_gate.py` exists and collects cleanly under `uv run pytest tests/test_citation_render_gate.py --collect-only`.
    - Each of `uv run pytest tests/test_citation_render_gate.py -k compile --collect-only`, `-k link`, `-k namespace`, `-k separator`, `-k uncited` selects at least one test.
    - The module's subprocess helper builds its argv from `sys.executable` and the `-m sphinx` module form; neither the string `sphinx-build` nor the string `uv run` appears in any argv list in the file.
    - Run against the untouched translator, the module reports failures for the compile, link, namespace, separator and uncited selectors, and every failure is an assertion mismatch or a non-zero build exit carrying `expected semicolon or line break` -- no failure is a Python `TypeError`, `KeyError` or fixture error.
    - No expected label token is written as a string literal in the file: every label comparison is between two values extracted from emitted output, or between an extracted value and one computed by importing `typsphinx.translator`'s own `_namespace_label`/`_sanitize_label`.
    - `grep -c '_namespace_label\|_sanitize_label' tests/test_citation_render_gate.py` is at least 1.
    - `uv run black --check tests/test_citation_render_gate.py` and `uv run ruff check tests/test_citation_render_gate.py` both pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_citation_render_gate.py -v; uv run pytest tests/test_citation_render_gate.py -k compile --collect-only -q &amp;&amp; uv run pytest tests/test_citation_render_gate.py -k link --collect-only -q &amp;&amp; uv run pytest tests/test_citation_render_gate.py -k namespace --collect-only -q &amp;&amp; uv run pytest tests/test_citation_render_gate.py -k separator --collect-only -q &amp;&amp; uv run pytest tests/test_citation_render_gate.py -k uncited --collect-only -q &amp;&amp; uv run black --check tests/test_citation_render_gate.py &amp;&amp; uv run ruff check tests/test_citation_render_gate.py</automated>
  </verify>
  <done>The gate module exists, its five `.typ`/compile selectors are all RED against the untouched translator for structural reasons, and no expected label token is hard-coded anywhere in it.</done>
</task>

<task type="auto">
  <name>Task 3: Add the compiled-PDF structural half and record the GATE-01 RED evidence</name>
  <read_first>
    - `tests/test_rubric_indent_invariance.py` lines 1-230 -- `_strip_zwsp`, `_layout_lines`,
      `_leading_columns`, `_find_page_and_column`, and the module header's finding that
      `extraction_mode="layout"` reconstructs left-edge indentation as leading spaces. Copy these
      helpers rather than reinventing them.
    - `tests/test_desc_content_indent_render_gate.py` lines 200-240 -- the recorded finding that
      `pypdf`'s per-glyph `visitor_text` reports `tm[4]`/`tm[5]` as zero on this project's PDFs.
    - `.planning/phases/40-citations-full-round-trip/40-RESEARCH.md` § "Code Examples" -- the verified
      probe showing `extraction_mode="layout"` reconstructs the hanging indent, that
      `page.get('/Annots')` yields real `/Link` rectangles, and that `cm[4]`/`cm[5]` (NOT
      `tm[4]`/`tm[5]`) carry usable per-glyph positions for this construct.
    - `.planning/phases/39-admonition-taxonomy-rubric-nesting/39-GATE-EVIDENCE-01.md` -- the evidence
      file format: named commit, verbatim RED output, an explicit RED-versus-CONTROL table, and a
      closing statement backed by `git diff --stat`.
    - `tests/test_citation_render_gate.py` as written by Task 2.
  </read_first>
  <files>tests/test_citation_render_gate.py, .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md</files>
  <action>
Extend `tests/test_citation_render_gate.py` with the compiled-PDF structural half, reusing the
module-scoped build fixture from Task 2 rather than compiling the fixture a second time. Every test
here sits behind the `TYPST_AVAILABLE` skip guard. Name each with the `-k` selector token
`40-VALIDATION.md` fixes: `layout`, `backref`, `order`.

`layout` -- CIT-02 and D-05. Using `extraction_mode="layout"` over the page holding the References
grid: locate the line containing `CITORDERALPHA` and record the column at which that sentinel starts;
assert the immediately following line -- the wrapped continuation of the same entry body -- has
exactly that many leading spaces, which is the hanging indent; then assert the sentinel start column
is identical for all five of `CITORDERALPHA`, `CITORDERBRAVO`, `CITORDERCHARLIE`, `CITORDERDELTA` and
`CITORDERECHO`, which is D-05's single-grid, widest-label alignment. Assert that column is strictly
greater than zero, so the body genuinely sits past the label rather than under it. Add the D-06
counterpart: `CITBREAKONESENTINEL` and `CITBREAKTWOSENTINEL` are in two independently-aligned runs, so
their start columns are permitted to differ and the test asserts only that each is greater than zero
-- record in the docstring that this is expected behaviour, not a bug.

`backref` -- CIT-04, D-01, D-02, D-03 and D-08. First the ordered `.typ` assertion: in the
`Krizhevsky2012` row's label cell, exactly two back-reference link calls appear and their target
tokens are, in order, the anchors of the first and second citing sites -- extracted from the citing
sites themselves, never written as literals. Assert the marker separator between them is a bare comma
with no space (D-03). Then D-03's single-marker case: the `Solo1998` row's label cell contains exactly
one link call and no parenthesised marker digit. Then D-08: no back-reference link in any row of
`index.typ` targets an anchor belonging to `second`, even though `second.rst` cites
`Krizhevsky2012`. Then the compiled-PDF half: on the page containing `CITORDERALPHA`, read
`page.get('/Annots')`, keep the `/Link` subtype entries, and assert at least one has a `/Rect` whose
horizontal extent lies to the LEFT of `CITORDERALPHA`'s own x position -- i.e. the back-reference
marker is a real clickable link living in the grid's left column, which is D-02's placement claim.
Obtain `CITORDERALPHA`'s x position with `extract_text(visitor_text=...)` reading `cm[4]`/`cm[5]`; if
those come back as zero on this project's PDFs, do NOT silently fall back to a weaker assertion --
record the discrepancy in the evidence file and derive the x position from the layout-mode column
instead, stating that substitution in the test docstring.

`order` -- CIT-06 and ROADMAP SC#4. Over the extracted text of the page holding the References grid,
assert the five sentinels appear in exactly their source order: ALPHA before BRAVO before CHARLIE
before DELTA before ECHO. Assert on the sentinels, never on the label text, because a label also
appears at its citing sites earlier in the document. State in the docstring that this order is
document order by construction -- docutils' own depth-first traversal -- and that the design contains
no sort step to defeat.

Then write `.planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md`, recording against a
named commit hash of the untouched translator:

1. The commit hash and a one-line statement that `typsphinx/` is byte-identical between the phase-start
   commit and the commit this evidence was captured at, backed by the `git diff --stat -- typsphinx/`
   output.
2. The fixture source, verbatim: `conf.py`, `index.rst` and `second.rst`.
3. The verbatim pre-fix emitted `.typ` fragment for a citation -- the two juxtaposed code-mode
   expressions with no separator between them, quoted exactly as emitted.
4. The verbatim exception text from a real `-b typstpdf` run over the fixture, including the
   `ExtensionError` line naming the failed master document and the `TypstError` line naming the
   syntax fatal.
5. The verbatim exception text from the SECOND, independently-reproduced failure mode: compiling the
   list-item-nested case, which aborts at Typst's semantic pass with a missing-label error rather
   than the syntax fatal. Record explicitly that these are two distinct pre-fix failure shapes.
6. The verbatim `uv run pytest tests/test_citation_render_gate.py -v` output, with every failing test
   labelled by the decision or requirement it enforces.
7. An explicit statement of which tests actually EXECUTED versus which were skipped by the
   `TYPST_AVAILABLE` guard on this run -- a skip is not evidence.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_citation_render_gate.py -k layout --collect-only`, `-k backref` and `-k order` each select at least one test.
    - The layout, backref and order tests are all RED against the untouched translator, and each fails on a missing artifact or an assertion mismatch rather than on an unhandled exception.
    - The module copies `_strip_zwsp`, `_layout_lines`, `_leading_columns` and `_find_page_and_column` (or equivalents with the same behaviour) from `tests/test_rubric_indent_invariance.py`; `grep -c 'extraction_mode' tests/test_citation_render_gate.py` is at least 1.
    - The module reads `page.get('/Annots')` at least once and reads `cm[4]`/`cm[5]` (or records in its docstring why the layout-mode column was substituted).
    - No back-reference target token, anchor token or namespaced label appears as a string literal in any assertion.
    - `40-GATE-EVIDENCE-01.md` exists and contains all seven recorded items above, including both distinct pre-fix failure shapes and the executed-versus-skipped statement.
    - `git diff --stat -- typsphinx/` is empty over this plan's commits.
    - `uv run black --check tests/test_citation_render_gate.py` and `uv run ruff check tests/test_citation_render_gate.py` both pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_citation_render_gate.py -v; uv run pytest tests/test_citation_render_gate.py -k layout --collect-only -q &amp;&amp; uv run pytest tests/test_citation_render_gate.py -k backref --collect-only -q &amp;&amp; uv run pytest tests/test_citation_render_gate.py -k order --collect-only -q &amp;&amp; test -s .planning/phases/40-citations-full-round-trip/40-GATE-EVIDENCE-01.md &amp;&amp; test -z "$(git diff --stat -- typsphinx/)" &amp;&amp; uv run black --check tests/test_citation_render_gate.py &amp;&amp; uv run ruff check tests/test_citation_render_gate.py</automated>
  </verify>
  <done>The compiled-PDF half of CIT-02, CIT-04 and CIT-06 exists and is RED, and the milestone's sole classic compile fatal is recorded verbatim against a named commit with both of its distinct failure shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.rst` fixture source -> translator | Author-controlled build-time text; the only untrusted-input analog in a build-time tool. A citation key and an entry body both cross here. |
| translator -> emitted `.typ` -> `typst.compile()` | Where a mis-derived label token or a miscounted separator becomes a build fatal rather than a cosmetic defect. |
| docutils `backrefs` / `ids` -> emitted `link()` target | A link whose target label was never attached is a whole-document compile abort at Typst's semantic pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | Citation label-token derivation -- a key or docname carrying a character outside Typst's label set would produce an unclosed label and abort the entire compile | high | mitigate | Not this plan's edit; this plan authors the assertion that observes it. The `namespace` test computes its expectation by importing the translator's own `_namespace_label`/`_sanitize_label` rather than transcribing a token, so a second sanitizer introduced in 40-03 would show up as a mismatch. No new sanitizer is introduced anywhere in this phase. |
| T-40-02 | Denial of service | Grid-open / grid-close and list-item leading-newline separators silently miscounted, merging two reference lists into one or splitting one into two | high | mitigate | The fixture carries both directions on purpose: five definitions separated only by an RST comment (which emits nothing and must NOT break the run) and two definitions separated by a paragraph (which must). The grid-structure test asserts the exact expected grid counts, derived from the fixture's authored structure, not from emitted output. |
| T-40-03 | Denial of service | A citing reference whose key has no definition emitting a `link()` to a label that was never attached -- a fatal `label ... does not exist` abort | high | mitigate | The fixture carries an undefined key on purpose, and the `link` test asserts no link call targets an anchor derived from it while its text still renders as ordinary content. The residual defensive guard mirrors `visit_footnote_reference`'s dangling-refid warn-and-skip and lands in 40-03. |
| T-40-SC | Tampering | npm/pip/cargo installs -- supply chain | high | mitigate | This plan installs nothing and this phase adds no dependency. `40-RESEARCH.md` § "Package Legitimacy Audit" records the phase as not applicable: `typst-py` and `pypdf` are already pinned and already used by every render gate in this suite. Verified at phase close by `git diff -- pyproject.toml uv.lock` over the phase's commits being empty. |

ASVS L1: V5 Input Validation is the only applicable category -- a build-time Sphinx extension has no
runtime authentication, session, access-control or cryptography surface (`40-RESEARCH.md` § Security
Domain). Blocking threshold is `high`. All four threats are `high` and all four are `mitigate`, with
T-40-01's, T-40-02's and T-40-03's implementation-side mitigations landing in 40-03. Nothing blocks
this plan.
</threat_model>

<verification>
- `uv run pytest tests/test_citation_render_gate.py -v` collects cleanly and reports every one of the
  eight selectors RED against the untouched translator.
- Every RED is a structural assertion mismatch or the classic build fatal; none is a Python type
  error, a fixture error, or a fixture-authoring mistake.
- `uv run pytest -m "not slow"` shows no NEW failure outside this module.
- `uv run black --check .`, `uv run ruff check .`, `uv run mypy typsphinx/` all pass.
- `uv run pytest tests/test_preview_version_sync.py` is green and untouched.
- `git diff --stat -- typsphinx/` is empty for this plan's commits.
</verification>

<success_criteria>
- One two-document fixture carries all eleven citation scenarios this phase decides, each labelled
  with its decision and its CONTROL-or-DEFECT-CASE status.
- The gate module's eight `-k` selectors match `40-VALIDATION.md`'s Per-Task Verification Map exactly.
- No expected label token, anchor token or link target is hard-coded anywhere in the module.
- The classic compile fatal is recorded verbatim against a named commit, together with the second,
  independently-reproduced list-item failure shape.
- The evidence file states which tests executed and which were skipped -- a skip is not a pass.
- `typsphinx/` is not modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/40-citations-full-round-trip/40-01-SUMMARY.md` when done.
</output>
