---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 13
type: execute
wave: 6
depends_on: ["70-10", "70-11", "70-12"]
files_modified:
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CI-EVIDENCE.md
autonomous: true
requirements: [QUA-09]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Wave gate: the canonical ref `gsd/v0.9.4-typing-modernization` carries 70-10, 70-11 and 70-12''s evidence, with SC1_VERDICT, SC2_VERDICT, LEG_A_VERDICT, LEG_C_VERDICT, LEG_E_VERDICT, LEG_B_VERDICT, LEG_D_VERDICT and DOC23_VERDICT all MET and no HALT heading, before anything is pushed.'
    - 'SC#5 local half: the gate quartet passes in this provisioned worktree on the tip about to be pushed: `uv run ruff check .`, `uv run black --check .`, `uv run mypy typsphinx/` and the full `LC_ALL=C` pytest suite, each exiting 0. `uv lock --check` exits 0 too, since every CI job syncs with `--locked`.'
    - 'Branch census, re-measured immediately before the push (ROADMAP constraint 9), records the local `gsd/v0.9.4*` refs, the origin heads and the main checkout''s HEAD. A `gsd/v0.9.4-milestone` decoy that carries commits is handled in the order constraint 9 binds: fast-forward the canonical ref to it, re-point the main checkout''s HEAD with `git symbolic-ref`, then delete it. Every other decoy shape halts. A decoy is never pushed.'
    - 'The first push is `git push --no-follow-tags -u origin gsd/v0.9.4-typing-modernization`. It creates the origin head at PUSHED_SHA, or fast-forwards it if it exists, with no force, tag or other ref. The branch''s upstream is origin/gsd/v0.9.4-typing-modernization.'
    - 'PUSHED_SHA''s product tree equals this worktree''s HEAD outside .planning/. pyproject.toml still reads `version = "0.9.2"`, `.github/workflows/` is unchanged since PHASE_BASE_SHA, no tag points at PUSHED_SHA, and origin has no v0.9.4 tag (positive control: the v0.9.2 tag is listed).'
    - 'Exactly one `gh workflow run CI --ref gsd/v0.9.4-typing-modernization` is issued. RUN_ID has headSha PUSHED_SHA, event workflow_dispatch and workflowName CI, and was created after PUSH_AT. It is observed in the foreground to status completed.'
    - 'SC#5 CI half: every job conclusion is transcribed literally. `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest` are named individually. Every job is success, and a skipped, cancelled or missing job never counts as success.'
    - 'The ruff verdict is quoted from the `Lint and Format Check` job, step `Run lint with tox`, never from this machine. CI_RUFF_VERSION equals LOCK_RUFF_VERSION and the ruff version in uv.lock at PUSHED_SHA.'
    - 'Phase roll-up: SC#1..SC#5 are tabulated from the committed verdict keys and this run. PHASE_SC_ROLLUP = ALL_MET only if every row is MET.'
  artifacts:
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CI-EVIDENCE.md
      provides: 'Wave gate, gate quartet, tip identity, census and decoy handling, push, the single dispatch, the completed run with every job, ruff verdict, SC roll-up'
      contains: 'PUSHED_SHA = '
  key_links:
    - from: 'local ref gsd/v0.9.4-typing-modernization'
      to: 'origin/gsd/v0.9.4-typing-modernization'
      via: 'git push --no-follow-tags -u; refs are shared between this worktree and the main checkout'
      pattern: 'ORIGIN_BEFORE = '
    - from: 'gh workflow run CI --ref gsd/v0.9.4-typing-modernization'
      to: 'ci.yml jobs on ubuntu, windows and macos'
      via: 'workflow_dispatch, since the push and pull_request triggers are scoped to main and develop'
      pattern: 'RUN_ID = '
  prohibitions:
    - statement: 'MUST NOT create or push a tag, open or comment on a pull request, trigger release.yml or update-pin.yml, force-push, push any ref other than the canonical branch, or push a decoy.'
      requirement_id: QUA-09
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT dispatch a second CI run, rerun jobs, or edit any file to turn a red job green; MUST NOT cite any run whose headSha differs from PUSHED_SHA, such as main''s baseline run, as this phase''s green.'
      requirement_id: QUA-09
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT move any main-checkout ref from this worktree except the single constraint-9 sequence for a decoy that carries commits (fast-forward, then symbolic-ref, then delete). MUST NOT merge origin/main into the branch.'
      requirement_id: QUA-09
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Put the milestone branch on `origin` for the first time and obtain CI's verdict on the post-flip tip (SC#5; ROADMAP constraints 8 and 9; milestone invariant #5).

Steps, in order:
1. Gate on wave 5's evidence.
2. Run the gate quartet on the tip.
3. Re-measure the branch census and correct any decoy.
4. Push with tracking.
5. Dispatch `ci.yml` exactly once and wait in the foreground until the run completes.
6. Transcribe every job, naming both `windows-latest` and both `macos-latest` lanes, and quote ruff's verdict from `Lint and Format Check`.
7. Roll SC#1..SC#5 up from the committed verdict keys.

Purpose: CI holds lint authority, and only CI reaches Windows and macOS. `ci.yml`'s push and pull_request triggers are scoped to `main`/`develop`, so the run must be dispatched. This plan runs in the last wave, so the pushed branch carries every evidence file and no plan audits a measurement made in its own wave.

Output: `70-CI-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every verify
command with the Bash tool `timeout` set to 600000. Run pytest under `LC_ALL=C`.

**Refs are shared.** This worktree's HEAD is on its own worktree branch, but
`refs/heads/gsd/v0.9.4-typing-modernization` is the same ref the main checkout uses. The main
checkout is `MAIN="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")"`. Touch
main-checkout refs only through the constraint-9 sequence in Task 1.

**Waiting on CI.** Wait in the foreground. Set the Bash tool's `timeout` to 600000 and never use
`run_in_background`. Wrap each watch in `timeout 590` so it returns inside the tool limit, then poll
`gh run view "$RUNID" --json status,conclusion`. Repeat until `status` is `completed`. Do not end the
turn while the run is in progress. If this executor is resumed mid-wait, reuse the recorded `RUN_ID`
and never dispatch again.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value.

**Commits.** Stage the evidence file by explicit path. Its commits land after the push and touch only
`.planning/`, which no CI job reads. On any failure path, write `## HALT: <reason>` into the evidence
file, commit it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: gsd/v0.9.4-milestone -->
<!-- planner-discipline-allow: gsd/v0\.9\.4-milestone -->
<!-- planner-discipline-allow: refs/tags/v0\.9\.4 -->
<!-- planner-discipline-allow: Run linters -->
The decoy name appears because the census looks for it and the verify requires it absent from origin.

## Planning-time census (2026-09-13; re-measure, never trust)

| Fact | Value at planning | Source |
|------|-------------------|--------|
| Local `gsd/v0.9.4*` refs | only `gsd/v0.9.4-typing-modernization` (HEAD of the main checkout); no decoy | `git branch -a` |
| Origin heads for `gsd/v0.9.4*` | none; the branch has never been pushed | ROADMAP constraint 9 |
| `ci.yml` jobs | `Test Python {3.12,3.13} on {ubuntu,windows,macos}-latest`, `Lint and Format Check` (step `Run lint with tox`), `Type Check`, `Code Coverage`, `Build Package`, `Integration Test - basic`, `Integration Test - advanced` | `.github/workflows/ci.yml` |
| `tox -e lint` commands | `black --check .` then `ruff check .` | `tox.ini` |
| Pre-conversion code baseline | `main`'s push run on the tree this branch forked from | ROADMAP constraint 8 (context only; never cited as this phase's green) |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: gate on wave 5, run the gate quartet on the tip, re-measure the census and correct any decoy, push with tracking, and dispatch CI exactly once</name>
  <precondition>`gh auth status` succeeds, and `git ls-remote --heads origin refs/heads/main` returns one line. `git cat-file -e gsd/v0.9.4-typing-modernization:<phase dir>/70-DOCS-DIFF-EVIDENCE.md` succeeds, and the same holds for `70-AFTER-STATIC-EVIDENCE.md` and `70-AFTER-RUNTIME-EVIDENCE.md`. Otherwise write `## HALT: canonical tip lacks wave 5` and stop without pushing.</precondition>
  <reversibility rating="costly">This is the branch's first publication to a public remote, with upstream tracking set. Undoing it needs a remote branch deletion, and the dispatched run stays in Actions history. No tag, release or pull request is created, and the owner authorised this push and dispatch in ROADMAP constraint 8 and SC#5, so it is flagged but not gated.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-04-PLAN.md Task 1 (the census, push and dispatch procedure this task adapts; the branch there already existed on origin, and here it does not)
    - .github/workflows/ci.yml (`name: CI`, the triggers, the job names, and `Run lint with tox`)
    - .planning/ROADMAP.md (binding constraints 8, 9 and 12; Phase 70 SC#5)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-AFTER-STATIC-EVIDENCE.md, 70-AFTER-RUNTIME-EVIDENCE.md and 70-DOCS-DIFF-EVIDENCE.md (the verdict keys)
  </read_first>
  <action>
1. Create `70-CI-EVIDENCE.md` with the heading `# Phase 70 — Push, CI and SC Roll-up`. Before any commit, record `BASE_70_13 = ` from `git rev-parse HEAD`. Under `## Head check and provisioning`, quote `date -u +%FT%TZ`, `pwd -P`, `test -f .git`, the shim check and the provisioning command. Record `VENV_VERSION_INFO = `, which must equal 70-02's.

2. **`## Wave-5 gate`.** From the evidence files at HEAD, quote these keys:
   - `SC1_VERDICT`, `SC2_VERDICT`, `LEG_A_VERDICT`, `LEG_C_VERDICT` and `LEG_E_VERDICT` from 70-10
   - `LEG_B_VERDICT` and `LEG_D_VERDICT` from 70-11
   - `DOC23_VERDICT` from 70-12

   All must be MET, and none of the three files may have a HALT heading.

3. **`## Gate quartet on the tip`.** Quote each with its exit code:
   - `uv lock --check`
   - `uv run ruff check .`
   - `uv run black --check .`
   - `uv run mypy typsphinx/`
   - `LC_ALL=C uv run pytest -q -p no:cacheprovider`, with its summary line

   All must exit 0. Record `QUARTET = PASS`.

4. **`## Census and decoy (constraint 9)`.** Run this immediately before the push, with only step 5's measurements in between. Record:
   - `git branch --list 'gsd/v0.9.4*' -v`
   - `git ls-remote --heads origin 'refs/heads/gsd/v0.9.4*'`
   - `git -C "$MAIN" symbolic-ref HEAD`

   Then handle the decoy:
   - If `gsd/v0.9.4-milestone` is on origin, write `## HALT: decoy on origin` and stop.
   - If it is absent locally, record `DECOY = ABSENT`.
   - If it exists locally and `git merge-base --is-ancestor gsd/v0.9.4-milestone gsd/v0.9.4-typing-modernization` exits 0, it has no unique commits. If the main checkout's HEAD is not on it, delete it with `git branch -d` and record `DECOY = DELETED-NO-UNIQUE`. If HEAD is on it, write `## HALT: main checkout on a stale decoy` and stop.
   - If it exists locally with unique commits, check that `git merge-base --is-ancestor gsd/v0.9.4-typing-modernization gsd/v0.9.4-milestone` exits 0 (a fast-forward is possible) and that the main checkout's HEAD is `refs/heads/gsd/v0.9.4-milestone`. If both hold, run constraint 9's sequence in exactly this order:
     1. `git update-ref refs/heads/gsd/v0.9.4-typing-modernization <decoy sha> <old canonical sha>`
     2. `git -C "$MAIN" symbolic-ref HEAD refs/heads/gsd/v0.9.4-typing-modernization`
     3. `git branch -d gsd/v0.9.4-milestone`

     Record each step and `DECOY = FAST-FORWARDED-AND-DELETED`. If either condition fails, write `## HALT: decoy needs the owner` and stop.

5. **`## Tip identity`.** Record:
   - `PUSHED_SHA = ` with `git rev-parse gsd/v0.9.4-typing-modernization`
   - `git diff --name-only HEAD "$PUSHED_SHA" -- . ':(exclude).planning'`, which must be empty
   - the `version = ` line of `git show "$PUSHED_SHA:pyproject.toml"`, which must be `version = "0.9.2"`
   - `git diff --quiet "$PHASE_BASE_SHA" "$PUSHED_SHA" -- .github/workflows`, which must exit 0
   - `git tag --points-at "$PUSHED_SHA"`, which must be empty
   - `LOCK_RUFF_VERSION_TIP = `, the version under `name = "ruff"` in `git show "$PUSHED_SHA:uv.lock"`, which must equal 70-02's `LOCK_RUFF_VERSION`

6. **`## Push`.**
   - Record `ORIGIN_BEFORE = ` as the first field of `git ls-remote --heads origin refs/heads/gsd/v0.9.4-typing-modernization`, or `ABSENT` if it prints nothing. If it is present, `git merge-base --is-ancestor "$ORIGIN_BEFORE" "$PUSHED_SHA"` must exit 0; otherwise write `## HALT: origin diverged` and stop. A force-push is forbidden.
   - Record `PUSH_AT = ` with `date -u +%FT%TZ`.
   - Run `git push --no-follow-tags -u origin gsd/v0.9.4-typing-modernization` and record its output. Do not act on GitHub's pull-request hint.
   - Record that the origin head now equals `PUSHED_SHA`, that `git config --get branch.gsd/v0.9.4-typing-modernization.remote` is `origin`, and that `.merge` is `refs/heads/gsd/v0.9.4-typing-modernization`.
   - Record that `git ls-remote --tags origin` lists `refs/tags/v0.9.2` and no `refs/tags/v0.9.4`.

7. **`## Dispatch`.**
   - Run `gh workflow run CI --ref gsd/v0.9.4-typing-modernization` exactly once.
   - Locate the run with `gh run list --workflow=ci.yml --branch gsd/v0.9.4-typing-modernization --event workflow_dispatch --limit 5 --json databaseId,headSha,status,createdAt,url`, taking the row whose `headSha` is `PUSHED_SHA` and whose `createdAt` follows `PUSH_AT`. Registration lag means querying again, never dispatching again.
   - Record `RUN_ID = ` and `RUN_URL = `.

8. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CI-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.4-typing-modernization &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; for kk in SC1_VERDICT SC2_VERDICT LEG_A_VERDICT LEG_C_VERDICT LEG_E_VERDICT; do [ "$(k $kk "$D/70-AFTER-STATIC-EVIDENCE.md")" = MET ] || { echo "not met: $kk"; exit 1; }; done &amp;&amp; for kk in LEG_B_VERDICT LEG_D_VERDICT; do [ "$(k $kk "$D/70-AFTER-RUNTIME-EVIDENCE.md")" = MET ] || { echo "not met: $kk"; exit 1; }; done &amp;&amp; [ "$(k DOC23_VERDICT "$D/70-DOCS-DIFF-EVIDENCE.md")" = MET ] &amp;&amp; [ "$(k QUARTET)" = PASS ] &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "${#P}" = 40 ] &amp;&amp; [ "$(git ls-remote --heads origin "refs/heads/$BR" | cut -f1)" = "$P" ] &amp;&amp; OB="$(k ORIGIN_BEFORE)" &amp;&amp; [ -n "$OB" ] &amp;&amp; { [ "$OB" = ABSENT ] || git merge-base --is-ancestor "$OB" "$P"; } &amp;&amp; [ "$(git config --get "branch.$BR.remote")" = origin ] &amp;&amp; [ "$(git config --get "branch.$BR.merge")" = "refs/heads/$BR" ] &amp;&amp; HL="$(git ls-remote --heads origin)" &amp;&amp; [ -n "$HL" ] &amp;&amp; ! printf '%s\n' "$HL" | grep -q 'gsd/v0\.9\.4-milestone' &amp;&amp; case "$(k DECOY)" in ABSENT|DELETED-NO-UNIQUE|FAST-FORWARDED-AND-DELETED) true ;; *) false ;; esac &amp;&amp; [ -z "$(git branch --list gsd/v0.9.4-milestone)" ] &amp;&amp; [ -z "$(git tag --points-at "$P")" ] &amp;&amp; TL="$(git ls-remote --tags origin)" &amp;&amp; printf '%s\n' "$TL" | grep -q 'refs/tags/v0\.9\.2$' &amp;&amp; ! printf '%s\n' "$TL" | grep -q 'refs/tags/v0\.9\.4' &amp;&amp; [ "$(git show "$P:pyproject.toml" | grep -m1 '^version = ')" = 'version = "0.9.2"' ] &amp;&amp; git diff --quiet "$B" "$P" -- .github/workflows &amp;&amp; [ -z "$(git diff --name-only HEAD "$P" -- . ':(exclude).planning')" ] &amp;&amp; [ "$(k LOCK_RUFF_VERSION_TIP)" = "$(k LOCK_RUFF_VERSION "$BE")" ] &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json workflowName -q .workflowName)" = CI ] &amp;&amp; [ "$(gh run view "$RUNID" --json event -q .event)" = workflow_dispatch ] &amp;&amp; PA="$(k PUSH_AT)" &amp;&amp; [ -n "$PA" ] &amp;&amp; [[ "$(gh run view "$RUNID" --json createdAt -q .createdAt)" &gt; "$PA" ]] &amp;&amp; for s in '## Head check and provisioning' '## Wave-5 gate' '## Gate quartet on the tip' '## Census and decoy (constraint 9)' '## Tip identity' '## Push' '## Dispatch'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$(k BASE_70_13)" HEAD | grep -vxF -e "$F" -e "$D/70-13-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or a `not met:` or `missing section:` line printed. Each of these makes it fail:
- a wave-5 verdict not MET, or the quartet not PASS
- `PUSHED_SHA` malformed, or the origin head not equal to it
- `ORIGIN_BEFORE` missing, or present and not an ancestor
- the upstream tracking config wrong
- the decoy on origin or still local, or a `DECOY` value outside the three allowed
- a tag at the pushed tip, no v0.9.2 tag on origin (a broken probe), or a v0.9.4 tag
- the version not 0.9.2, the workflows changed, or the pushed product tree differing from HEAD
- the lock's ruff version moved
- no `RUN_ID`, or the run's headSha, workflow, event or creation time wrong
- a section missing
- another tracked file changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - Every wave-5 verdict key is MET, and `QUARTET = PASS` with `uv lock --check`, ruff, black, mypy and pytest each exiting 0 on the tip.
    - `## Census and decoy (constraint 9)` records all three listings immediately before the push and a `DECOY` value from the allowed set. No decoy exists locally or on origin afterwards.
    - The origin head equals `PUSHED_SHA`, the upstream is set, no tag points at it, and origin lists v0.9.2 but no v0.9.4.
    - Exactly one dispatch is recorded, with `RUN_ID` bound to `PUSHED_SHA` and created after `PUSH_AT`.
  </acceptance_criteria>
  <done>The post-flip tip passed the gate quartet locally, is on origin with tracking, and has one CI run dispatched against exactly that SHA.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the run to completion in the foreground, transcribe every job with both windows-latest and both macos-latest lanes, quote ruff's CI verdict, and roll up SC#1..SC#5</name>
  <precondition>The `RUN_ID` recorded by Task 1 resolves with `gh run view <id> --json databaseId`. A resumed executor reuses it; exactly one `workflow_dispatch` CI run on the branch carries `headSha` equal to `PUSHED_SHA`.</precondition>
  <reversibility rating="reversible">Read-only observation of a run already dispatched, plus evidence.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CI-EVIDENCE.md (Task 1: `RUN_ID`, `PUSHED_SHA`, `LOCK_RUFF_VERSION_TIP`)
    - .github/workflows/ci.yml (lines 10-72 for the matrix and names; lines 183-200 for the integration matrix)
    - .planning/milestones/v0.9.3-phases/69-v0-9-3-close-prep-prep-only-unpublished/69-04-PLAN.md Task 2 (the transcript shape to mirror)
  </read_first>
  <action>
1. **Wait in the foreground.** Run `timeout 590 gh run watch "$RUNID" --interval 30` in a Bash call with `timeout` 600000 and no `run_in_background`. After each return, read `gh run view "$RUNID" --json status,conclusion`, and repeat until `status` is `completed`. The overall cap is 90 minutes. If it is reached, record the current status and return a blocking checkpoint. Never dispatch a second run.

2. **`## Run`.** Record `gh run view "$RUNID" --json status,conclusion,workflowName,headSha,url,createdAt,updatedAt`, then the keys `RUN_HEAD_SHA` and `RUN_CONCLUSION`.

3. **`## Job census`.**
   - Derive `EXPECTED_JOB_COUNT = ` from `ci.yml` at `PUSHED_SHA`: the test matrix's os entries times its python-version entries, plus `lint`, `type-check`, `coverage` and `build`, plus the integration matrix's `example` entries. Show the arithmetic on the next line.
   - Record `gh run view "$RUNID" --json jobs --jq '.jobs[] | [.name, .conclusion] | @tsv'` as a numbered table with every row.
   - Record `JOB_COUNT = ` and `NON_SUCCESS_JOBS = ` (the jobs whose conclusion is not success; skipped and cancelled count as not success).

4. **`## windows-latest lanes`** and **`## macos-latest lanes`.** Two sub-tables naming `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each with its conclusion.

5. **`## ruff's verdict`.** Take the `Lint and Format Check` job id and quote from `gh run view --job <id> --log`:
   - the `+ ruff==` line of its `Install dependencies` step, recorded as `CI_RUFF_VERSION`, which must equal `LOCK_RUFF_VERSION_TIP`
   - the `Run lint with tox` step's `black --check .` command line and verdict
   - its `ruff check .` command line and verdict
   - the `lint: OK` line

   CI is the lint authority. `release.yml` has a differently named lint step, and this plan never searches or triggers it.

6. **`## Dispatch count and no release run`.** Record `DISPATCH_COUNT = 1`, from the `workflow_dispatch` runs on the branch whose `headSha` is `PUSHED_SHA`, and `RELEASE_RUNS_AT_PUSHED = 0`, from `release.yml` runs at that SHA.

7. **`## SC#5 verdict`.** Write `SC5_VERDICT = MET` only when all of these hold: the run is completed and success, `JOB_COUNT` equals `EXPECTED_JOB_COUNT`, `NON_SUCCESS_JOBS = 0`, and the four named lanes and `Lint and Format Check` are success. On any non-success job:
   - transcribe every job literally and keep the run
   - do not rerun, dispatch again or edit anything
   - write `SC5_VERDICT = NOT-MET`, and return a blocking checkpoint naming each failing job and step

8. **`## Phase SC roll-up`.** Tabulate each SC against its evidence:
   - SC#1: 70-10 `SC1_VERDICT`
   - SC#2: 70-10 `SC2_VERDICT`
   - SC#3: 70-04 pilot keys, and 70-10 `LEG_A_VERDICT`, `LEG_C_VERDICT` and `LEG_E_VERDICT`
   - SC#4: 70-11 `LEG_B_VERDICT` and `LEG_D_VERDICT`, and 70-12 `DOC23_VERDICT`
   - SC#5: this file's `SC5_VERDICT`

   Record `PHASE_SC_ROLLUP = ALL_MET` only if every row is MET.

9. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CI-EVIDENCE.md" &amp;&amp; BR=gsd/v0.9.4-typing-modernization &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; RUNID="$(k RUN_ID)" &amp;&amp; [ -n "$RUNID" ] &amp;&amp; P="$(k PUSHED_SHA)" &amp;&amp; [ "$(gh run view "$RUNID" --json status -q .status)" = completed ] &amp;&amp; [ "$(gh run view "$RUNID" --json conclusion -q .conclusion)" = success ] &amp;&amp; [ "$(gh run view "$RUNID" --json headSha -q .headSha)" = "$P" ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '.jobs | length')" = "$(k EXPECTED_JOB_COUNT)" ] &amp;&amp; [ "$(k JOB_COUNT)" = "$(k EXPECTED_JOB_COUNT)" ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.conclusion != "success")] | length')" = 0 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("windows-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name | test("macos-latest"))] | length')" = 2 ] &amp;&amp; [ "$(gh run view "$RUNID" --json jobs -q '[.jobs[] | select(.name == "Lint and Format Check" and .conclusion == "success")] | length')" = 1 ] &amp;&amp; for n in 'Test Python 3.12 on windows-latest' 'Test Python 3.13 on windows-latest' 'Test Python 3.12 on macos-latest' 'Test Python 3.13 on macos-latest' 'Lint and Format Check' 'Run lint with tox' 'Type Check' 'Code Coverage' 'Build Package' 'Integration Test - basic' 'Integration Test - advanced' 'lint: OK'; do grep -qF "$n" "$F" || { echo "missing: $n"; exit 1; }; done &amp;&amp; CR="$(k CI_RUFF_VERSION)" &amp;&amp; [ -n "$CR" ] &amp;&amp; [ "$CR" = "$(k LOCK_RUFF_VERSION_TIP)" ] &amp;&amp; [ "$(git show "$P:uv.lock" | grep -A1 -xF 'name = "ruff"' | sed -n 's/^version = "\(.*\)"$/\1/p')" = "$CR" ] &amp;&amp; [ "$(gh run list --workflow=ci.yml --branch "$BR" --event workflow_dispatch --limit 50 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 1 ] &amp;&amp; [ "$(gh run list --workflow=release.yml --limit 20 --json headSha -q '[.[] | select(.headSha == "'"$P"'")] | length')" = 0 ] &amp;&amp; [ "$(grep -c 'Run linters' "$F" || true)" = 0 ] &amp;&amp; [ "$(k NON_SUCCESS_JOBS)" = 0 ] &amp;&amp; [ "$(k DISPATCH_COUNT)" = 1 ] &amp;&amp; [ "$(k RELEASE_RUNS_AT_PUSHED)" = 0 ] &amp;&amp; [ "$(k SC5_VERDICT)" = MET ] &amp;&amp; [ "$(k PHASE_SC_ROLLUP)" = ALL_MET ] &amp;&amp; for s in '## Run' '## Job census' '## windows-latest lanes' '## macos-latest lanes' "## ruff's verdict" '## Dispatch count and no release run' '## SC#5 verdict' '## Phase SC roll-up'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or a `missing:` or `missing section:` line printed. Each of these makes it fail:
- the run not completed, or its conclusion not success
- its headSha differing from `PUSHED_SHA`
- a job count differing from `EXPECTED_JOB_COUNT`, or any job not success
- a windows-latest or macos-latest count other than 2
- `Lint and Format Check` not success
- a lane, job, step or `lint: OK` absent from the evidence
- `CI_RUFF_VERSION` empty or differing from the lock at the pushed tip
- more than one dispatched run at `PUSHED_SHA`, or a release.yml run at it
- the release-only lint step name in the evidence
- a count, verdict or roll-up key wrong
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Job census` holds a numbered table of every job. `JOB_COUNT` equals `EXPECTED_JOB_COUNT` as derived from `ci.yml`, and `NON_SUCCESS_JOBS = 0`.
    - The lane sub-tables name `Test Python 3.12 on windows-latest`, `Test Python 3.13 on windows-latest`, `Test Python 3.12 on macos-latest` and `Test Python 3.13 on macos-latest`, each success.
    - `## ruff's verdict` quotes `Run lint with tox`'s black and ruff lines and `lint: OK`, with `CI_RUFF_VERSION` equal to the lock's ruff version.
    - `DISPATCH_COUNT = 1`, `RELEASE_RUNS_AT_PUSHED = 0`, `SC5_VERDICT = MET` and `PHASE_SC_ROLLUP = ALL_MET`.
  </acceptance_criteria>
  <done>One completed, all-green CI run on the post-flip tip is transcribed job by job, ruff's verdict comes from CI, and all five success criteria are rolled up MET from committed evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local refs → origin | a push publishes refs; a decoy, a force, another ref or a tag would publish something this phase must not |
| this worktree → main checkout refs | the constraint-9 decoy sequence moves the main checkout's HEAD |
| gh CLI → GitHub Actions | a dispatch starts remote work; the wrong workflow would begin a publish pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-29 | Tampering | pushing the decoy, a force-push, another ref, or a tag riding along | high | mitigate | Census immediately before the push; `git push --no-follow-tags -u origin gsd/v0.9.4-typing-modernization` only; `ORIGIN_BEFORE` must be ABSENT or an ancestor; the verify requires no decoy on origin or locally, no tag at the tip and no v0.9.4 tag on origin. |
| T-70-30 | Tampering | orphaning commits by deleting a decoy first | high | mitigate | Constraint 9's order is fixed (update-ref with an old-value guard, then symbolic-ref, then delete), and it runs only when a fast-forward is possible and the main checkout sits on the decoy; every other shape halts. |
| T-70-31 | Elevation of Privilege | triggering release.yml or update-pin.yml | high | mitigate | Only `gh workflow run CI` is issued; the verify asserts no release.yml run at `PUSHED_SHA`; a prohibition forbids every route. |
| T-70-32 | Repudiation | citing main's baseline run, or a second dispatch masking a red first | high | mitigate | `RUN_ID`'s headSha must equal `PUSHED_SHA` and its createdAt must follow `PUSH_AT`; exactly one dispatched run at that SHA; a red run is kept and escalated. |
| T-70-33 | Information Disclosure | CI log excerpts in committed evidence | low | accept | The repository and its Actions logs are public, and only tool-output lines (versions, lint verdicts) are quoted. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning and `uv lock --check` touch only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan creates
`70-CI-EVIDENCE.md`, with the keys `BASE_70_13`, `VENV_VERSION_INFO`, `QUARTET`, `DECOY`,
`PUSHED_SHA`, `LOCK_RUFF_VERSION_TIP`, `ORIGIN_BEFORE`, `PUSH_AT`, `RUN_ID`, `RUN_URL`,
`RUN_HEAD_SHA`, `RUN_CONCLUSION`, `EXPECTED_JOB_COUNT`, `JOB_COUNT`, `NON_SUCCESS_JOBS`,
`CI_RUFF_VERSION`, `DISPATCH_COUNT`, `RELEASE_RUNS_AT_PUSHED`, `SC5_VERDICT` and
`PHASE_SC_ROLLUP`. Outside the repository it creates `origin/gsd/v0.9.4-typing-modernization` with
upstream tracking, and one `workflow_dispatch` CI run. It modifies no product-tree file.
</artifacts_this_phase_produces>

<verification>
- The canonical branch is on origin at `PUSHED_SHA`, with tracking, no decoy and no tag.
- One `workflow_dispatch` CI run at `PUSHED_SHA` completed with every job green, both
  `windows-latest` and both `macos-latest` lanes named, and ruff's verdict quoted from `Run lint with
  tox` at the lock's version.
- All five success criteria are rolled up MET.
</verification>

<success_criteria>
SC#5 and milestone invariant #5: the branch reaches origin in the milestone's first phase, evidenced
by a completed three-OS CI run on the post-flip tip.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-13-SUMMARY.md` when done.
- Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
- The first section states `PUSHED_SHA`, `ORIGIN_BEFORE`, `DECOY`, `RUN_ID`, `RUN_URL`, `JOB_COUNT`, `NON_SUCCESS_JOBS`, `CI_RUFF_VERSION`, `SC5_VERDICT` and `PHASE_SC_ROLLUP`. If the run was not all green, that section names the failing jobs first and states that SC#5 awaits the owner.
</output>
