---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 06
type: execute
wave: 3
depends_on: ["70-01", "70-02", "70-04"]
files_modified:
  - typsphinx/template_engine.py
  - typsphinx/template_registry.py
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-TEMPLATE-EVIDENCE.md
autonomous: true
requirements: [QUA-11, QUA-12]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - '`typsphinx/template_engine.py` and `typsphinx/template_registry.py` are converted by the two scoped ruff passes while the config ignores are still present. Afterwards `uv run ruff check <both> --select UP006,UP035` and `uv run ruff check <both>` both exit 0.'
    - 'Both files'' typing import lines read `from typing import Any`.'
    - 'Leg (a), automated: each file''s masked hash equals the hash of its `git show PHASE_BASE_SHA:<path>`, computed with the hash-pinned harness from `70-MASK-PILOT-EVIDENCE.md`. That includes function-local annotated assignments such as the registry''s dict-typed local.'
    - 'Every changed non-blank line carries a typing name. The `@preview` import strings near template_engine.py''s imports are untouched. Docstring prose such as "List of directories" and "Tuple of author names" stays byte-identical.'
    - 'Edge QUA-11/concurrency: this plan runs in parallel with 70-05, 70-07 and 70-08 on disjoint files. Its branch changes only these two files and its own evidence and SUMMARY.'
    - 'mypy stdout hashes equal to MYPY_STDOUT_SHA256_BEFORE, and the full pytest result equals PYTEST_RESULT_BEFORE.'
  artifacts:
    - path: typsphinx/template_engine.py
      provides: 'Builtin generics throughout'
      contains: 'from typing import Any'
    - path: typsphinx/template_registry.py
      provides: 'Builtin generics throughout'
      contains: 'from typing import Any'
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-TEMPLATE-EVIDENCE.md
      provides: 'Conversion transcript, automated leg (a) hashes, changed-line census, gates'
      contains: 'TEMPLATE_ENGINE_MASK = EQUAL'
  key_links:
    - from: '70-MASK-PILOT-EVIDENCE.md ~~~python mask-harness'
      to: 'this plan''s leg (a) check'
      via: 'sed extraction, sha256 equal to MASK_HARNESS_SHA256, uv run python -c'
      pattern: 'MASK_HARNESS_SHA256'
  prohibitions:
    - statement: 'MUST NOT use sed or any text substitution on source, pass --preview or --unsafe-fixes, add `from __future__ import annotations`, rewrite any Optional/Union/`X | None`, edit an `@preview` version-sync line, or run ruff --fix or black in write mode on any file but these two.'
      requirement_id: QUA-11
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Convert `typsphinx/template_engine.py` and `typsphinx/template_registry.py` onto builtin generics under the still-present ignores (ROADMAP constraints 1(ii) and 5; QUA-11). The plan proves leg (a) with the pinned harness and gates on mypy and the full suite.

Purpose: this is one of four file-disjoint wave-3 conversions. template_engine.py sits next to one of the three `@preview` version-sync sites, so the census explicitly checks that no such line moves.

Output: the two converted files, committed alone, and `70-CONV-TEMPLATE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every verify
command with the Bash tool `timeout` set to 600000. Run pytest under `LC_ALL=C`.

**Harness.** Extract it exactly as 70-04 defined:
`H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')"`, where `PE` is
`70-MASK-PILOT-EVIDENCE.md`. Check that `printf '%s\n' "$H" | sha256sum` equals
`MASK_HARNESS_SHA256`, then run it with `sk() { uv run python -c "$H"; }`. Never retype it.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value.

**Commits.** Stage files by explicit path, never `git add -A`. The conversion commit touches only
the two source files. On any failure path, write `## HALT: <reason>` into the evidence file, commit
it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: assert -->
<!-- planner-discipline-allow: @preview -->
<!-- planner-discipline-allow: --preview -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: convert template_engine.py and template_registry.py with the two scoped ruff passes and prove leg (a) for both with the pinned mask harness</name>
  <precondition>`70-MASK-PILOT-EVIDENCE.md` holds `PILOT_TRANSLATOR = EQUAL`, `PILOT_LEDGER = EQUAL`, `CONTROL_RENAME = DIFFER` and `CONTROL_IMPORT = DIFFER` with no HALT heading, and its harness block hashes to `MASK_HARNESS_SHA256`. The single CLAUDE.md commit since `PHASE_BASE_SHA` is an ancestor of HEAD. `git diff --quiet "$PHASE_BASE_SHA" HEAD -- typsphinx/template_engine.py typsphinx/template_registry.py` exits 0. Otherwise write `## HALT: preconditions` and stop.</precondition>
  <reversibility rating="reversible">Annotation and import rewrites in two git-tracked files, proven structure-preserving by the harness.</reversibility>
  <files>typsphinx/template_engine.py, typsphinx/template_registry.py, .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-TEMPLATE-EVIDENCE.md</files>
  <read_first>
    - typsphinx/template_engine.py (line 13's typing import, lines 700-710 with the `@preview` import strings, lines 274 and 819 with the docstring prose)
    - typsphinx/template_registry.py (line 29's typing import, line 469's local annotated assignment)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md (§ Mask harness)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md (§ translator.py, writer.py, template_engine.py, template_registry.py)
  </read_first>
  <action>
1. Create `70-CONV-TEMPLATE-EVIDENCE.md` with the heading `# Phase 70 — Conversion: template_engine.py and template_registry.py`. Before any commit, record `BASE_70_06 = ` from `git rev-parse HEAD`. Then record `VENV_HOME = ` and `VENV_VERSION_INFO = ` after provisioning; both must equal 70-02's. Quote the precondition checks under `## Head check and provisioning`.

2. **`## Conversion`.** With `P="typsphinx/template_engine.py typsphinx/template_registry.py"`:
   - Record `uv run ruff check $P --select UP006,UP035 --output-format=concise` and its non-zero count.
   - Run pass 1, `uv run ruff check $P --select UP006,UP035 --fix`, then pass 2, `uv run ruff check $P --fix`. Never use `sed`, `--preview` or `--unsafe-fixes`.
   - Record that both `uv run ruff check $P --select UP006,UP035` and `uv run ruff check $P` exit 0.
   - Quote both typing import lines.
   - Commit the two files alone, with the message `refactor(70-06): move template_engine.py and template_registry.py onto builtin generics (QUA-11)`.

3. **`## Leg (a)`.** For each file, quote the base hash and the current hash from `sk`. If each pair is equal, record `TEMPLATE_ENGINE_MASK = EQUAL` and `TEMPLATE_REGISTRY_MASK = EQUAL`. Any difference: write `## HALT: mask` and stop.

4. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CONV-TEMPLATE-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; PE="$D/70-MASK-PILOT-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = "$(k VENV_VERSION_INFO "$BE")" ] &amp;&amp; C="$(git log --format=%H "$B"..HEAD -- CLAUDE.md)" &amp;&amp; [ "$(printf '%s\n' "$C" | grep -c .)" = 1 ] &amp;&amp; git merge-base --is-ancestor "$C" "$(k BASE_70_06)" &amp;&amp; [ "$(k PILOT_LEDGER "$PE")" = EQUAL ] &amp;&amp; [ "$(k CONTROL_RENAME "$PE")" = DIFFER ] &amp;&amp; H="$(sed -n '/^~~~python mask-harness$/,/^~~~$/p' "$PE" | sed '1d;$d')" &amp;&amp; [ -n "$H" ] &amp;&amp; [ "$(printf '%s\n' "$H" | sha256sum | cut -d' ' -f1)" = "$(k MASK_HARNESS_SHA256 "$PE")" ] &amp;&amp; sk() { uv run python -c "$H"; } &amp;&amp; for f in typsphinx/template_engine.py typsphinx/template_registry.py; do a="$(sk &lt; "$f")" &amp;&amp; [ "${#a}" = 64 ] &amp;&amp; [ "$a" = "$(git show "$B:$f" | sk)" ] || { echo "mask differs: $f"; exit 1; }; ! git diff --quiet "$B" HEAD -- "$f" || { echo "unchanged: $f"; exit 1; }; grep -qxF 'from typing import Any' "$f" || { echo "typing line: $f"; exit 1; }; done &amp;&amp; [ "$(k TEMPLATE_ENGINE_MASK)" = EQUAL ] &amp;&amp; [ "$(k TEMPLATE_REGISTRY_MASK)" = EQUAL ] &amp;&amp; uv run ruff check typsphinx/template_engine.py typsphinx/template_registry.py --select UP006,UP035 &amp;&amp; uv run ruff check typsphinx/template_engine.py typsphinx/template_registry.py &amp;&amp; for s in '## Head check and provisioning' '## Conversion' '## Leg (a)'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or a `mask differs:`, `unchanged:`, `typing line:` or `missing section:` line printed. Each of these makes it fail:
- the interpreter differing from 70-02's
- the CLAUDE.md rewrite not an ancestor
- the pilot keys wrong, or the harness not hashing to its key
- either file's masked hash differing from base, either file unchanged, or either typing line not `from typing import Any`
- a mask key wrong
- ruff reporting UP006/UP035 or any config-rule finding
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check` of both files, with and without `--select UP006,UP035`, exits 0.
    - Both files have the typing line `from typing import Any`.
    - Both masked hashes equal base's, with `TEMPLATE_ENGINE_MASK = EQUAL` and `TEMPLATE_REGISTRY_MASK = EQUAL`.
  </acceptance_criteria>
  <done>The template engine and registry are on builtin generics and structurally identical to the base under the mask.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the conversion: changed-line census, no assert or @preview line, black, repo-wide ruff, mypy stdout and the full pytest result against the baseline</name>
  <precondition>Task 1 is committed with both mask keys EQUAL and no HALT heading.</precondition>
  <reversibility rating="reversible">Measurement and evidence only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONV-TEMPLATE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PYTEST_RESULT_BEFORE`, `MYPY_STDOUT_SHA256_BEFORE`)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-MASK-PILOT-EVIDENCE.md (§ Changed-line census and § Gates)
  </read_first>
  <action>
1. **`## Changed-line census`.** Over `git diff "$PHASE_BASE_SHA" HEAD` for the two files, with headers and blank lines excluded, quote each check:
   - Every added and removed line matches `(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)`. Record `NON_TYPING_LINES_70_06 = 0`.
   - No changed line contains `assert` or `@preview`.
   - `git grep -n '@preview/' -- typsphinx/template_engine.py` is byte-identical at `PHASE_BASE_SHA` and HEAD.

2. **`## Gates`.** Quote each command with its exit code:
   - `uv run black --check typsphinx/template_engine.py typsphinx/template_registry.py`
   - `uv run ruff check .`
   - `uv run mypy typsphinx/ 2>/dev/null`, recording `MYPY_STDOUT_SHA256_70_06 = `, which must equal `MYPY_STDOUT_SHA256_BEFORE`
   - `LC_ALL=C uv run pytest -q -rs -p no:cacheprovider`, which must exit 0, recording `PYTEST_RESULT_70_06 = `, which must equal `PYTEST_RESULT_BEFORE`

3. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-CONV-TEMPLATE-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; P="typsphinx/template_engine.py typsphinx/template_registry.py" &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -vE '^[+-][[:space:]]*$' | grep -qvE '(Dict|List|Set|Tuple|Iterator|dict|list|set|tuple|typing|collections\.abc)' &amp;&amp; ! git diff "$B" HEAD -- $P | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)( |$)' | grep -qwE 'assert|@preview' &amp;&amp; [ "$(git grep -n '@preview/' "$B" -- typsphinx/template_engine.py | cut -d: -f2-)" = "$(git grep -n '@preview/' HEAD -- typsphinx/template_engine.py | cut -d: -f2-)" ] &amp;&amp; uv run black --check $P &amp;&amp; uv run ruff check . &amp;&amp; [ "$(uv run mypy typsphinx/ 2&gt;/dev/null | sha256sum | cut -d' ' -f1)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; [ "$(k MYPY_STDOUT_SHA256_70_06)" = "$(k MYPY_STDOUT_SHA256_BEFORE "$BE")" ] &amp;&amp; R="$(LC_ALL=C uv run pytest -q -p no:cacheprovider 2&gt;&amp;1 | tail -n 1 | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" &amp;&amp; [ -n "$R" ] &amp;&amp; [ "$R" = "$(k PYTEST_RESULT_BEFORE "$BE")" ] &amp;&amp; [ "$(k PYTEST_RESULT_70_06)" = "$R" ] &amp;&amp; [ "$(k NON_TYPING_LINES_70_06)" = 0 ] &amp;&amp; for s in '## Changed-line census' '## Gates'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$(k BASE_70_06)" HEAD | grep -vxF -e typsphinx/template_engine.py -e typsphinx/template_registry.py -e "$F" -e "$D/70-06-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- a changed non-blank line without a typing name, or containing `assert` or `@preview`
- template_engine.py's `@preview/` lines differing from base
- black or ruff reporting anything
- the mypy stdout hash or the pytest result differing from base
- a key wrong
- a section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `NON_TYPING_LINES_70_06 = 0`. No `assert` or `@preview` line is changed, and template_engine.py's `@preview/` lines are identical to base.
    - `uv run black --check` on both files and `uv run ruff check .` exit 0.
    - `MYPY_STDOUT_SHA256_70_06` and `PYTEST_RESULT_70_06` equal their base keys.
  </acceptance_criteria>
  <done>The template modules' conversion is lint-green under the ignores and leaves mypy, the suite and the `@preview` sync lines exactly as they were.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruff --fix → template modules | an automated rewrite reaches the code that renders every master document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-13 | Tampering | a non-annotation change in the template modules | medium | mitigate | Masked hashes equal to base; changed-line census; mypy stdout and full pytest equal to base. |
| T-70-14 | Tampering | the `@preview` version-sync strings in template_engine.py | medium | mitigate | `@preview/` lines compared byte-for-byte with base; `tests/test_preview_version_sync.py` runs in the suite. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan modifies
`typsphinx/template_engine.py` and `typsphinx/template_registry.py`, and creates
`70-CONV-TEMPLATE-EVIDENCE.md`, with the keys `BASE_70_06`, `VENV_HOME`, `VENV_VERSION_INFO`,
`TEMPLATE_ENGINE_MASK`, `TEMPLATE_REGISTRY_MASK`, `NON_TYPING_LINES_70_06`,
`MYPY_STDOUT_SHA256_70_06` and `PYTEST_RESULT_70_06`.
</artifacts_this_phase_produces>

<verification>
Both template modules carry zero UP006/UP035 findings, their masked hashes equal base, and mypy,
pytest and the `@preview` lines are unchanged.
</verification>

<success_criteria>
One of the four file-disjoint wave-3 conversions lands lint-green, ready for 70-09's flip.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-06-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
