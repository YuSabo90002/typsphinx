---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOC22-EVIDENCE.md
autonomous: true
requirements: [DOC-22]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'D-01: CLAUDE.md line 75 is the single bullet that starts `- **Python 3.12+ is required.**`. It keeps the Python-floor fact and adds one standing instruction: annotations use builtin generics (`dict[str, Any]`, `list[str]`, `set[str]`, `tuple[str, ...]`) and take abstract types such as `Iterator` from `collections.abc`, not `typing.Dict`/`List`/`Set`/`Tuple`/`Iterator`.'
    - 'D-02: the rewritten bullet carries no claim about which ruff rules the config suppresses or enforces, no pointer to the 2026-07-22 todo or any todos path, no instruction to hold the rewrite back, and no history note. It therefore reads true on both sides of the pyproject.toml flip (ROADMAP constraint 1(i)).'
    - 'D-03: the bullet adds no standing prohibition on `from __future__ import annotations` or on PEP 604 unions. The tokens `__future__` and `604` do not appear in it.'
    - 'D-04: only line 75 changes. CLAUDE.md with line 75 deleted hashes identically to the base file with line 75 deleted, and the line count is unchanged. Exactly one commit in the phase touches CLAUDE.md, and that commit touches nothing else.'
    - 'After this plan, the todo pending path is cited by exactly one tracked file outside .planning/ (pyproject.toml). That file and the pending todo itself leave together in 70-09 (ROADMAP constraint 2).'
    - 'D-04 test-neutral: the three tests/*.py files that mention CLAUDE pass with this edit, and `uv run ruff check .` and `uv run black --check .` exit 0.'
  artifacts:
    - path: CLAUDE.md
      provides: 'The rewritten line-75 bullet (DOC-22 first half)'
      contains: 'collections.abc'
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOC22-EVIDENCE.md
      provides: 'Before/after bullet, one-line diff, token checks, pending-path census, test-neutral run, D-01..D-03 reading'
      contains: 'BASE_70_01 = '
  key_links:
    - from: 'CLAUDE.md:75 (rewritten)'
      to: 'every conversion executor in waves 2-3 (70-04..70-08)'
      via: 'Claude Code auto-loads CLAUDE.md from each worktree; the rewrite must be merged before any conversion plan forks'
      pattern: 'collections\.abc'
  prohibitions:
    - statement: 'MUST NOT add a history note ("was deferred until …"), a claim about the ruff ignore list, or any todo path to CLAUDE.md, and MUST NOT edit any CLAUDE.md line other than 75 (D-02, D-04).'
      requirement_id: DOC-22
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT touch any file under typsphinx/, tests/, docs/ or pyproject.toml in this plan. The conversion starts only after this rewrite has merged.'
      requirement_id: DOC-22
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Rewrite `CLAUDE.md:75` so that it no longer forbids the typing modernization, before any conversion
plan runs (DOC-22 first half; ROADMAP constraint 1(i); D-01, D-02, D-03, D-04).

Purpose: every executor auto-loads `CLAUDE.md` as a standing instruction. Without this rewrite, each
conversion executor in waves 2-3 would be told not to do its own task. The new text must be true
both before and after the `pyproject.toml` ignore flip in 70-09, so it states how to write
annotations and says nothing about what ruff suppresses.

Output: the one-line CLAUDE.md rewrite, committed alone, plus `70-DOC22-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md
@CLAUDE.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every
verify command with the Bash tool `timeout` set to 600000.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value, because readers compare with
`sed -n "s/^KEY = //p"`.

**Commits.** Stage files by explicit path (`git add <path>`), never `git add -A`. On any failure
path, write `## HALT: <reason>` into the evidence file, commit it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: UP006 -->
<!-- planner-discipline-allow: UP035 -->
<!-- planner-discipline-allow: ignore -->
<!-- planner-discipline-allow: deferral -->
<!-- planner-discipline-allow: todos/pending -->
<!-- planner-discipline-allow: odernize -->
<!-- planner-discipline-allow: __future__ -->
<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
The tokens above are named because the verify checks that the rewritten bullet no longer carries them.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: rewrite the CLAUDE.md:75 bullet to D-01's annotation-style instruction and commit it alone</name>
  <reversibility rating="reversible">One documentation line, in one commit that touches nothing else.</reversibility>
  <files>CLAUDE.md, .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOC22-EVIDENCE.md</files>
  <read_first>
    - CLAUDE.md (line 75 and the surrounding "Conventions & gotchas" section)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md (D-01..D-04, and the Specific Ideas note that the bullet is read at two commits)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-PATTERNS.md (§ CLAUDE.md:75)
    - .planning/ROADMAP.md (binding constraints 1 and 2; Phase 70 SC#1)
  </read_first>
  <action>
1. Create `70-DOC22-EVIDENCE.md` with the heading `# Phase 70 — DOC-22 Evidence (CLAUDE.md:75)`. Before any commit, record `BASE_70_01 = ` followed by the output of `git rev-parse HEAD`, then `## Head check`, which quotes `pwd -P`, `test -f .git; echo "exit:$?"` and `grep -c typsphinx-fhs-run "$(command -v uv)"`.

2. Quote the current line with `sed -n 75p CLAUDE.md` under `## Before`. It must start with `- **Python 3.12+ is required.**`. If it does not, write `## HALT: line 75 moved` and stop.

3. Use the Edit tool to replace the whole of line 75 with this single physical line. The wording is Claude's discretion within D-01..D-03; it keeps the floor fact (D-01), drops every claim about the ruff config, the todo path, the instruction to hold the work back and any history (D-02), and adds no `__future__` or PEP 604 rule (D-03):

   - **Python 3.12+ is required.** Annotations use builtin generics (`dict[str, Any]`, `list[str]`, `set[str]`, `tuple[str, ...]`) and take abstract types such as `Iterator` from `collections.abc`, not `typing.Dict`/`List`/`Set`/`Tuple`/`Iterator`.

   The line starts with `- **Python 3.12+ is required.**`, exactly as today. Do not touch any other line (D-04). Do not use `sed -i`.

4. Commit CLAUDE.md alone, with the message `docs(70-01): rewrite CLAUDE.md typing bullet as an annotation-style instruction (DOC-22)`. This must be the only commit in the phase that touches CLAUDE.md, and it must touch no other file.

5. Under `## After`, quote `sed -n 75p CLAUDE.md` and `git diff -U0 "$BASE_70_01" HEAD -- CLAUDE.md`. The diff has exactly one removed line and one added line, both at line 75.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-DOC22-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_70_01)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; L="$(grep -n '^- \*\*Python 3\.12+ is required\.\*\*' CLAUDE.md)" &amp;&amp; [ "$(printf '%s\n' "$L" | wc -l)" = 1 ] &amp;&amp; [ "${L%%:*}" = 75 ] &amp;&amp; T="${L#*:}" &amp;&amp; for t in 'dict[str, Any]' 'list[str]' 'set[str]' 'tuple[str, ...]' 'collections.abc' 'Iterator' 'typing.Dict'; do case "$T" in *"$t"*) ;; *) echo "missing token: $t"; exit 1 ;; esac; done &amp;&amp; for t in UP006 UP035 ignore deferral 'todos/pending' odernize __future__ 604; do case "$T" in *"$t"*) echo "forbidden token: $t"; exit 1 ;; esac; done &amp;&amp; [ "$(sed '75d' CLAUDE.md | sha256sum)" = "$(git show "$B:CLAUDE.md" | sed '75d' | sha256sum)" ] &amp;&amp; [ "$(wc -l &lt; CLAUDE.md)" = "$(git show "$B:CLAUDE.md" | wc -l)" ] &amp;&amp; [ "$(git log --format=%H "$B"..HEAD -- CLAUDE.md | wc -l)" = 1 ] &amp;&amp; C="$(git log --format=%H "$B"..HEAD -- CLAUDE.md)" &amp;&amp; [ "$(git show --name-only --format= "$C")" = CLAUDE.md ] &amp;&amp; git diff --quiet HEAD -- CLAUDE.md</automated>
    <fails_when>Non-zero exit, or a `missing token:` / `forbidden token:` line printed. Each of these makes it fail:
- `BASE_70_01` missing or not an ancestor
- the Python bullet not found exactly once, or not on line 75
- a required token absent, or a flip-dependent, todo-path, `__future__` or `604` token present
- any line other than 75 changed, or the line count changed
- zero or several commits touching CLAUDE.md, or that commit touching another file
- an uncommitted CLAUDE.md change</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n 75p CLAUDE.md` starts with `- **Python 3.12+ is required.**` and contains `dict[str, Any]`, `list[str]`, `collections.abc`, `Iterator` and `typing.Dict`.
    - The bullet contains none of `UP006`, `UP035`, `ignore`, `deferral`, `todos/pending`, `odernize`, `__future__` or `604`.
    - `sed '75d'` of CLAUDE.md hashes identically to the base; the line count is unchanged.
    - Exactly one commit in `BASE_70_01..HEAD` touches CLAUDE.md, and `git show --name-only` for it lists only `CLAUDE.md`.
  </acceptance_criteria>
  <done>CLAUDE.md:75 now tells a contributor to write builtin generics and `collections.abc` types, carries no flip-dependent claim, and landed in a commit of its own.</done>
</task>

<task type="auto">
  <name>Task 2: Record D-04's lint and test neutrality, the pending-path census and the two-sided reading, then commit the evidence</name>
  <precondition>Task 1's CLAUDE.md commit exists, and `70-DOC22-EVIDENCE.md` holds `BASE_70_01` with no HALT heading.</precondition>
  <reversibility rating="reversible">Evidence file only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOC22-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-DOC22-EVIDENCE.md (Task 1 sections)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md (D-01..D-04)
    - pyproject.toml lines 120-135 (the `[tool.ruff.lint]` ignore array that still cites the todo)
  </read_first>
  <action>
1. Provision the worktree with the command in `<worktree_provisioning>`. Under `## Provisioning`, record `VENV_HOME = ` with the `home` value of `.venv/pyvenv.cfg`, and `VENV_VERSION_INFO = ` with its `version_info` value. The version must be `3.13.13`. Otherwise write `## HALT: interpreter` and stop.

2. **`## D-04 neutrality`.** Record each command with its output and exit code:
   - `git grep -l CLAUDE -- 'tests/*.py'`. The list is the files whose prose mentions CLAUDE. None of them reads CLAUDE.md, as measured at discussion time.
   - `LC_ALL=C uv run pytest $(git grep -l CLAUDE -- 'tests/*.py') -q -p no:cacheprovider`. Record the summary line and `CLAUDE_TESTS_EXIT = ` with the exit code, which must be 0.
   - `uv run ruff check .` and `uv run black --check .`, each with exit 0.

3. **`## Pending-path census`.** Record `git grep -n 'todos/pending/2026-07-22-modernize' -- . ':!.planning'`. It must list exactly one line, in `pyproject.toml`. Record `PENDING_REFS_AFTER_70_01 = 1`. The positive control is `git grep -n 'todos/pending/2026-07-22-modernize' "$BASE_70_01" -- . ':!.planning'`, which must list two lines (CLAUDE.md and pyproject.toml).

4. **`## Reading against D-01..D-03`.** Write one short paragraph for each side of the flip:
   - Before the flip (ignores still present, code not yet converted): the bullet is an instruction about how to write annotations, so it authorises the conversion plans and makes no claim that can be false.
   - After the flip (ignores removed): ruff enforces the same shape the bullet asks for, and the text still makes no claim about ruff.

   70-10 re-reads the bullet at the last pre-flip commit and at the post-flip tip.

5. Commit the evidence file alone.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-DOC22-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k BASE_70_01)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; [ "$(k VENV_VERSION_INFO)" = 3.13.13 ] &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = 3.13.13 ] &amp;&amp; [ "$(git grep -n 'todos/pending/2026-07-22-modernize' -- . ':!.planning' | wc -l)" = 1 ] &amp;&amp; git grep -q 'todos/pending/2026-07-22-modernize' -- pyproject.toml &amp;&amp; [ "$(git grep -n 'todos/pending/2026-07-22-modernize' "$B" -- . ':!.planning' | wc -l)" = 2 ] &amp;&amp; LC_ALL=C uv run pytest $(git grep -l CLAUDE -- 'tests/*.py') -q -p no:cacheprovider &amp;&amp; uv run ruff check . &amp;&amp; uv run black --check . &amp;&amp; [ "$(k CLAUDE_TESTS_EXIT)" = 0 ] &amp;&amp; for s in '## Head check' '## Before' '## After' '## Provisioning' '## D-04 neutrality' '## Pending-path census' '## Reading against D-01..D-03'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! git diff --name-only "$B" HEAD | grep -vxF -e CLAUDE.md -e "$F" -e "$D/70-01-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- the interpreter not 3.13.13
- the pending path cited by anything other than exactly `pyproject.toml` outside .planning/, or the base control not showing two citations
- the CLAUDE-mentioning tests failing, or ruff or black reporting anything
- a section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `CLAUDE_TESTS_EXIT = 0`, and `uv run ruff check .` and `uv run black --check .` exit 0.
    - `git grep -n 'todos/pending/2026-07-22-modernize' -- . ':!.planning'` prints exactly one line, in `pyproject.toml`. At `BASE_70_01` it prints two.
    - `## Reading against D-01..D-03` holds one paragraph for each side of the flip.
  </acceptance_criteria>
  <done>The rewrite is shown to be lint- and test-neutral, and it is recorded as true on both sides of the flip.</done>
</task>

</tasks>

<flagged_assumptions>
**Spec-less edge accounting for the whole phase.** No SPEC exists, so the probe fallback surfaced 19
edge items. 18 are resolved `explicit` and lifted as plain `must_haves.truths` strings, in the plan
that measures each. One stays unresolved and is flagged here:

- **DOC-22 / unclassified — unresolved, flagged.** The probe could not classify DOC-22. This plan
  reads it as a two-commit truth: the bullet must be true at the last pre-flip commit and at the
  post-flip tip. 70-10 checks that reading (SC#1). It stays flagged because the classifier gave it no
  category.

| Req | Category | Home plan | Disposition |
|-----|----------|-----------|-------------|
| QUA-09 | boundary, precision, ordering | 70-02 | explicit truths |
| QUA-09 | adjacency, empty | 70-09 | explicit truths |
| QUA-09 | concurrency | 70-10 | explicit truth |
| QUA-11 | adjacency, empty | 70-10 | explicit truths |
| QUA-11 | ordering | 70-04 | explicit truth |
| QUA-11 | concurrency | 70-05..70-08 | explicit truth |
| QUA-12 | boundary, empty (mask) | 70-04 | explicit truths |
| QUA-12 | empty (corpus), concurrency | 70-03 | explicit truths |
| QUA-12 | encoding, precision | 70-11 | explicit truths |
| DOC-22 | unclassified | 70-01 | flagged (above) |
| DOC-23 | empty, encoding, concurrency | 70-12 | explicit truths |

Accounting: 19 surfaced = 18 truths + 1 flagged + 0 dismissed + 0 dropped.

**Recalled prohibitions** are authored without a check descriptor and flagged for judgment. Each
lives in the `must_haves.prohibitions` of the plan that could violate it:

- no history or ruff-state claim in CLAUDE.md: 70-01
- no hard-coded research-time value: 70-02
- no `--preview`, `sed`, `from __future__ import annotations`, PEP 604 sweep or MSG-06 absorption: 70-04..70-08
- no narrowed corpus or dropped failing fixture: 70-03 and 70-11
- no difference written off as nondeterminism: 70-11 and 70-12
- no tag, pull request, release run or force-push: 70-13
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md → every executor | CLAUDE.md is loaded as a standing instruction; a wrong sentence misdirects every later plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-01 | Tampering | CLAUDE.md lines other than 75 | medium | mitigate | `sed '75d'` hash equal to base, line count unchanged, and a single commit touching only CLAUDE.md. |
| T-70-02 | Repudiation | a flip-dependent claim that turns false after 70-09 | medium | mitigate | The verify rejects the tokens `UP006`, `UP035`, `ignore`, `deferral`, `todos/pending` and `odernize`, and 70-10 re-reads the bullet at the pre-flip and post-flip commits. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins; no new package. |
</threat_model>

<artifacts_this_phase_produces>
Phase-wide list (every Phase 70 plan points here):

- **Modified product files, annotations and typing imports only:** `typsphinx/translator.py`,
  `typsphinx/builder.py`, `typsphinx/template_engine.py`, `typsphinx/template_registry.py`,
  `typsphinx/writer.py`, `typsphinx/__init__.py`, `tests/conftest.py`,
  `tests/test_bundle_layout_sweep_gate.py`, `tests/test_include_edge_derivation_unit.py`,
  `tests/test_include_ledger_removal_gate.py`.
- **Modified config and docs:** `CLAUDE.md` (line 75), and `pyproject.toml` (the two
  `[tool.ruff.lint] ignore` lines are deleted).
- **Moved:** `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md`
  moves to `.planning/todos/completed/` with its content unchanged.
- **New evidence files** in the phase directory: `70-DOC22-EVIDENCE.md`, `70-BASELINE-EVIDENCE.md`,
  `70-CORPUS-DOCS-BASE-EVIDENCE.md`, `70-MASK-PILOT-EVIDENCE.md`, `70-CONV-BUILDER-EVIDENCE.md`,
  `70-CONV-TEMPLATE-EVIDENCE.md`, `70-CONV-WRITER-INIT-EVIDENCE.md`, `70-CONV-TESTS-EVIDENCE.md`,
  `70-FLIP-EVIDENCE.md`, `70-AFTER-STATIC-EVIDENCE.md`, `70-AFTER-RUNTIME-EVIDENCE.md`,
  `70-DOCS-DIFF-EVIDENCE.md`, `70-CI-EVIDENCE.md`. No file is named `70-VERIFICATION.md`.
- **Symbols recorded in evidence, never committed as code:** the `Mask` class, an
  `ast.NodeTransformer` subclass with `visit_ImportFrom`, `visit_FunctionDef`,
  `visit_AsyncFunctionDef`, `_mask_func` and `visit_AnnAssign`, held in the `~~~python mask-harness`
  block of `70-MASK-PILOT-EVIDENCE.md`; and the `sk` shell helper that runs it.
- **Evidence keys** are defined by each plan's action. No new function, class, config value,
  CLI flag, test or committed script enters the product tree.
- **Outside the repository:** `origin/gsd/v0.9.4-typing-modernization` with upstream tracking, and
  one `workflow_dispatch` CI run (70-13).

This plan creates `70-DOC22-EVIDENCE.md` and modifies `CLAUDE.md` line 75.
</artifacts_this_phase_produces>

<verification>
- CLAUDE.md:75 carries the D-01 instruction, no D-02 flip-dependent content and no D-03 prohibition;
  the rest of the file is byte-identical (D-04).
- The rewrite is a single commit touching only CLAUDE.md, and it merges before any conversion plan
  forks.
</verification>

<success_criteria>
DOC-22 first half and SC#1's ordering precondition: the CLAUDE.md rewrite exists as a commit that
every later conversion commit will descend from, and it reads true before and after the flip.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-01-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
