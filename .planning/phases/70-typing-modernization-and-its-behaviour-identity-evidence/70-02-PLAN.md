---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
autonomous: true
requirements: [QUA-09, QUA-12]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'PHASE_BASE_SHA is this worktree''s fork point, recorded before any commit. Its product tree is the pre-conversion code, and every later plan reads it from `70-BASELINE-EVIDENCE.md`.'
    - 'The interpreter is recorded: VENV_HOME and VENV_VERSION_INFO from `.venv/pyvenv.cfg`, and VENV_VERSION_INFO is 3.13.13 (ROADMAP constraint 6). Every later plan asserts it matches the same values.'
    - 'Edge QUA-09/boundary: the fresh repo-wide `uv run ruff check . --select UP006,UP035` count at PHASE_BASE_SHA is recorded as UP_TOTAL_BASE and must be non-zero. Zero is the only passing value after the flip, and one finding anywhere fails.'
    - 'Edge QUA-09/precision: UP_TOTAL_BASE counts only diagnostic lines whose code is UP006 or UP035; F401 and every other code are excluded. It equals UP006_BASE + UP035_BASE and the sum of the two `--statistics` rows.'
    - 'Edge QUA-09/ordering: the per-file breakdown is recorded `LC_ALL=C` sorted, and the planned-file-set comparison is order-independent. The fresh file set must equal the ten files the conversion plans own (UP_FILES_MATCH_PLAN = YES); a difference halts the phase before any conversion.'
    - 'RUFF_VERSION_BASE equals `ruff <LOCK_RUFF_VERSION>`, the version `uv.lock` pins. It is recorded fresh at execution time, never copied from ROADMAP or research (ROADMAP constraint 3).'
    - 'The SC#2 base counts are recorded over typsphinx/ and tests/ *.py at PHASE_BASE_SHA: FUTURE_ANNOTATIONS_BASE, OPTIONAL_BASE, UNION_BASE and PIPE_NONE_BASE. 70-10 requires the same four values at the post-flip tip.'
    - 'Leg (e) before: `uv run mypy typsphinx/ 2>/dev/null` stdout is hashed as MYPY_STDOUT_SHA256_BEFORE (stdout only, because uv sync chatter goes to stderr), with MYPY_EXIT_BEFORE = 0 and MYPY_VERSION_BASE.'
    - 'Leg (b) before: PYTEST_COLLECTED_BEFORE comes from the unanchored `[0-9]+ tests? collected` extraction. PYTEST_RESULT_BEFORE is the passed/failed/skipped/error/xfail/xpass tokens of the full `LC_ALL=C` run, with PYTEST_EXIT_BEFORE = 0; the tokens sum to the collected count.'
  artifacts:
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
      provides: 'PHASE_BASE_SHA, interpreter, ruff version and fresh UP006/UP035 census, SC#2 base counts, leg (e) and leg (b) before sides'
      contains: 'PHASE_BASE_SHA = '
  key_links:
    - from: '70-BASELINE-EVIDENCE.md keys'
      to: '70-04..70-13 verify blocks'
      via: 'sed -n "s/^KEY = //p" exact comparison; PHASE_BASE_SHA, PYTEST_RESULT_BEFORE, MYPY_STDOUT_SHA256_BEFORE, VENV_* and the SC#2 counts'
      pattern: '^PHASE_BASE_SHA = [0-9a-f]{40}$'
  prohibitions:
    - statement: 'MUST NOT copy any research-time or roadmap-time number or SHA (the 113/93/20 counts, 1548/1547 test counts, cbd448bf, 9 source files) as a recorded or expected value; every value here is measured fresh at PHASE_BASE_SHA.'
      requirement_id: QUA-09
      category: transparency
      status: resolved
      verification: judgment
    - statement: 'MUST NOT run ruff --fix, black in write mode, or any edit under typsphinx/, tests/, docs/, pyproject.toml or CLAUDE.md in this plan; the baseline measures, it never changes.'
      requirement_id: QUA-12
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Capture the pre-conversion baseline on the untouched tree (ROADMAP constraint 1, first wave; constraint 3; constraint 6). The captures are:
- PHASE_BASE_SHA and the interpreter record
- the fresh repo-wide `UP006`/`UP035` census and the ruff version
- SC#2's base counts
- the before sides of QUA-12 leg (e), mypy, and leg (b), pytest

Purpose: every after-side comparison in 70-10 and 70-11 is made against values in this file, so they must be measured here, under the same lock, extras and interpreter the after side will use. The fresh census also confirms that the ten files the conversion plans own are exactly the files carrying findings.

Output: `70-BASELINE-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`.
Both extras are required: without `--extra docs` the CHANGELOG page gate skips four tests, and
without `--python 3.13.13` a fresh sync picks uv-managed 3.14. Run every tool through `uv run`. Never
run `uv sync` in the main checkout. Run every verify command with the Bash tool `timeout` set to
600000.

**Scratch.** Make one scratch directory with `mktemp -d` outside the repository and record it as
`SCRATCH_70_02 = <path>`. Keep it until this plan's last verify passes.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value, because readers compare with
`sed -n "s/^KEY = //p"`.

**Commits.** Stage files by explicit path, never `git add -A`. On any failure path, write
`## HALT: <reason>` into the evidence file, commit it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: record PHASE_BASE_SHA, interpreter, ruff version, the fresh UP006/UP035 census against the planned file set, the SC#2 base counts and mypy's before stdout</name>
  <reversibility rating="reversible">Measurement and one evidence file.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md (§ Leg (b), § Leg (e), § Same-Interpreter Mechanism, Pitfall 7)
    - .planning/ROADMAP.md (constraints 3, 5 and 6; Phase 70 SC#2)
    - pyproject.toml lines 118-135 (the ignore array this baseline runs under)
    - uv.lock (the `name = "ruff"` entry)
  </read_first>
  <action>
1. Create `70-BASELINE-EVIDENCE.md` with the heading `# Phase 70 — Pre-conversion Baseline`. Before any commit, record:
   - `BASE_70_02 = ` and `PHASE_BASE_SHA = `, both the output of `git rev-parse HEAD`
   - under `## Head check`: `date -u +%FT%TZ`, `pwd -P`, `test -f .git; echo "exit:$?"`, `grep -c typsphinx-fhs-run "$(command -v uv)"`, and `git diff --name-only main HEAD -- . ':(exclude).planning'`. The last must be empty: the branch outside `.planning/` equals `main`.

2. **`## Provisioning`.** Run the provisioning command and record its exit. Then record `VENV_HOME = ` (the `home` line of `.venv/pyvenv.cfg`) and `VENV_VERSION_INFO = ` (its `version_info`). If the version is not `3.13.13`, write `## HALT: interpreter` and stop.

3. **`## Ruff census`.**
   - Record `LOCK_RUFF_VERSION = ` with the version under `name = "ruff"` in `uv.lock`, and `RUFF_VERSION_BASE = ` with the output of `uv run ruff --version 2>/dev/null`. The second must equal `ruff ` followed by the first.
   - Run `uv run ruff check . --select UP006,UP035 --output-format=concise > "$S/ruff-base.txt"; echo "exit:$?"`. Expect `exit:1`, because findings exist. The CLI `--select` overrides the config ignore.
   - From the lines matching `: UP0(06|35) `, record `UP_TOTAL_BASE`, `UP006_BASE`, `UP035_BASE` and `UP_FILE_COUNT_BASE` (distinct paths). Put the `LC_ALL=C` sorted `uniq -c` per-file breakdown in a `~~~text up-files` block, one line per file.
   - Quote `uv run ruff check . --select UP006,UP035 --statistics`. Its two rows must sum to `UP_TOTAL_BASE`.
   - Compare the distinct file set with the ten files the conversion plans own: `tests/conftest.py`, `tests/test_bundle_layout_sweep_gate.py`, `tests/test_include_edge_derivation_unit.py`, `tests/test_include_ledger_removal_gate.py`, `typsphinx/__init__.py`, `typsphinx/builder.py`, `typsphinx/template_engine.py`, `typsphinx/template_registry.py`, `typsphinx/translator.py` and `typsphinx/writer.py`. Record `UP_FILES_MATCH_PLAN = YES` only if the sets are equal. Otherwise record `NO`, list the difference, write `## HALT: census differs from the plan's file set`, and stop. Discovery is repo-wide (constraint 3), and no plan may convert a file this census does not name (constraint 5).
   - `UP_TOTAL_BASE` must be non-zero. Do not copy any number from ROADMAP or research.

4. **`## SC#2 base counts`.** Over `git grep` at `PHASE_BASE_SHA`, pathspecs `'typsphinx/*.py' 'tests/*.py'`, record each command and its count:
   - `FUTURE_ANNOTATIONS_BASE`: occurrences of `from __future__ import annotations`
   - `OPTIONAL_BASE`: occurrences of `Optional\[`
   - `UNION_BASE`: occurrences of `Union\[`
   - `PIPE_NONE_BASE`: occurrences of `[|] None`

   Use `git grep -hoE '<pattern>' "$PHASE_BASE_SHA" -- 'typsphinx/*.py' 'tests/*.py' | wc -l` for each.

5. **`## mypy before (leg e)`.** Record `MYPY_VERSION_BASE = ` with `uv run mypy --version 2>/dev/null`. Run `uv run mypy typsphinx/ 2>/dev/null > "$S/mypy-before.out"; echo "exit:$?"`, then record `MYPY_EXIT_BEFORE = ` (it must be 0; otherwise HALT) and `MYPY_STDOUT_SHA256_BEFORE = ` (the first field of `sha256sum < "$S/mypy-before.out"`). Quote the stdout in a `~~~text mypy-before` block. Compare stdout only.

6. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA)" &amp;&amp; [ "${#B}" = 40 ] &amp;&amp; [ "$(k BASE_70_02)" = "$B" ] &amp;&amp; git merge-base --is-ancestor "$B" HEAD &amp;&amp; git diff --quiet "$B" HEAD -- typsphinx tests docs pyproject.toml CLAUDE.md &amp;&amp; [ "$(k VENV_VERSION_INFO)" = 3.13.13 ] &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = 3.13.13 ] &amp;&amp; [ "$(sed -n 's/^home = //p' .venv/pyvenv.cfg)" = "$(k VENV_HOME)" ] &amp;&amp; LR="$(grep -A1 -xF 'name = "ruff"' uv.lock | sed -n 's/^version = "\(.*\)"$/\1/p')" &amp;&amp; [ -n "$LR" ] &amp;&amp; [ "$(k LOCK_RUFF_VERSION)" = "$LR" ] &amp;&amp; [ "$(uv run ruff --version 2&gt;/dev/null)" = "ruff $LR" ] &amp;&amp; [ "$(k RUFF_VERSION_BASE)" = "ruff $LR" ] &amp;&amp; O="$(uv run ruff check . --select UP006,UP035 --output-format=concise 2&gt;/dev/null | grep -E ': UP0(06|35) ')" &amp;&amp; N="$(printf '%s\n' "$O" | grep -c .)" &amp;&amp; [ "$N" -gt 0 ] &amp;&amp; [ "$N" = "$(k UP_TOTAL_BASE)" ] &amp;&amp; [ "$(printf '%s\n' "$O" | grep -c ': UP006 ' || true)" = "$(k UP006_BASE)" ] &amp;&amp; [ "$(printf '%s\n' "$O" | grep -c ': UP035 ' || true)" = "$(k UP035_BASE)" ] &amp;&amp; [ "$(( $(k UP006_BASE) + $(k UP035_BASE) ))" = "$N" ] &amp;&amp; FS="$(printf '%s\n' "$O" | cut -d: -f1 | LC_ALL=C sort -u)" &amp;&amp; [ "$(printf '%s\n' "$FS" | wc -l)" = "$(k UP_FILE_COUNT_BASE)" ] &amp;&amp; [ "$FS" = "$(printf '%s\n' tests/conftest.py tests/test_bundle_layout_sweep_gate.py tests/test_include_edge_derivation_unit.py tests/test_include_ledger_removal_gate.py typsphinx/__init__.py typsphinx/builder.py typsphinx/template_engine.py typsphinx/template_registry.py typsphinx/translator.py typsphinx/writer.py | LC_ALL=C sort)" ] &amp;&amp; [ "$(k UP_FILES_MATCH_PLAN)" = YES ] &amp;&amp; [ "$(git grep -hoE 'from __future__ import annotations' "$B" -- 'typsphinx/*.py' 'tests/*.py' | wc -l)" = "$(k FUTURE_ANNOTATIONS_BASE)" ] &amp;&amp; [ "$(git grep -hoE 'Optional\[' "$B" -- 'typsphinx/*.py' 'tests/*.py' | wc -l)" = "$(k OPTIONAL_BASE)" ] &amp;&amp; [ "$(git grep -hoE 'Union\[' "$B" -- 'typsphinx/*.py' 'tests/*.py' | wc -l)" = "$(k UNION_BASE)" ] &amp;&amp; [ "$(git grep -hoE '[|] None' "$B" -- 'typsphinx/*.py' 'tests/*.py' | wc -l)" = "$(k PIPE_NONE_BASE)" ] &amp;&amp; [ "$(k MYPY_EXIT_BEFORE)" = 0 ] &amp;&amp; [ "$(uv run mypy typsphinx/ 2&gt;/dev/null | sha256sum | cut -d' ' -f1)" = "$(k MYPY_STDOUT_SHA256_BEFORE)" ] &amp;&amp; [ -n "$(k MYPY_VERSION_BASE)" ] &amp;&amp; for s in '## Head check' '## Provisioning' '## Ruff census' '## SC#2 base counts' '## mypy before (leg e)'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; grep -qxF '~~~text up-files' "$F" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- `PHASE_BASE_SHA` missing, not 40 characters, or not this worktree's fork point
- a product file changed in this worktree
- the interpreter not 3.13.13 or `VENV_HOME` differing from `.venv/pyvenv.cfg`
- the ruff version differing from `uv.lock`
- a zero UP count, a count key differing from a fresh re-count, or UP006 + UP035 not equal to the total
- the fresh file set differing from the ten planned files
- an SC#2 count key differing from a fresh `git grep`
- a mypy exit other than 0, or the stdout hash differing
- a section or the `up-files` block missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `PHASE_BASE_SHA` is a 40-character SHA equal to `BASE_70_02`, and no product file differs from it in this worktree.
    - `UP_TOTAL_BASE` is non-zero, equals `UP006_BASE + UP035_BASE`, and a fresh re-count matches it. `UP_FILES_MATCH_PLAN = YES`.
    - `RUFF_VERSION_BASE` equals `ruff <LOCK_RUFF_VERSION>`.
    - `FUTURE_ANNOTATIONS_BASE`, `OPTIONAL_BASE`, `UNION_BASE` and `PIPE_NONE_BASE` each equal a fresh `git grep` count at `PHASE_BASE_SHA`.
    - `MYPY_EXIT_BEFORE = 0`, and `MYPY_STDOUT_SHA256_BEFORE` equals a fresh stdout-only hash.
  </acceptance_criteria>
  <done>The base SHA, interpreter, ruff census and mypy before side are on record, and the census matches the file set the conversion plans own.</done>
</task>

<task type="auto">
  <name>Task 2: Record leg (b)'s before side: pytest collected count and full-suite result under LC_ALL=C, with skip reasons</name>
  <precondition>Task 1 is committed, and `70-BASELINE-EVIDENCE.md` holds `PHASE_BASE_SHA` and `UP_FILES_MATCH_PLAN = YES` with no HALT heading.</precondition>
  <reversibility rating="reversible">Measurement only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (Task 1 keys)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-RESEARCH.md (§ Leg (b): the `=`-decorated summary line and the extras hazard; Assumption A2)
  </read_first>
  <action>
1. **`## pytest before (leg b)`.**
   - Run `LC_ALL=C uv run pytest --collect-only -q -p no:cacheprovider 2>/dev/null | grep -oE '[0-9]+ tests? collected' | grep -oE '^[0-9]+'` and record `PYTEST_COLLECTED_BEFORE = `. The first grep is unanchored, because the summary line is wrapped in `=`.
   - Run `LC_ALL=C uv run pytest -q -rs -p no:cacheprovider > "$S/pytest-before.out" 2>&1; echo "exit:$?"`. Record `PYTEST_EXIT_BEFORE = ` with the exit code. If it is not 0, write `## HALT: base suite not green` and stop.
   - Record `PYTEST_RESULT_BEFORE = ` with the output of `tail -n 1 "$S/pytest-before.out" | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' '`. Warnings are excluded on purpose. Also record `PYTEST_WARNINGS_BEFORE = ` with the warnings count, or `0`. That value is for information only.
   - Quote the summary line, and quote every `SKIPPED` line of the `-rs` short summary in a `~~~text pytest-skips-before` block. 70-11 compares against these reasons, so a skip-count difference can be explained rather than assumed.
   - State that the counts in `PYTEST_RESULT_BEFORE` sum to `PYTEST_COLLECTED_BEFORE`.

2. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "$F" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA)" &amp;&amp; S="$(k SCRATCH_70_02)" &amp;&amp; [ -s "$S/pytest-before.out" ] &amp;&amp; C="$(LC_ALL=C uv run pytest --collect-only -q -p no:cacheprovider 2&gt;/dev/null | grep -oE '[0-9]+ tests? collected' | grep -oE '^[0-9]+')" &amp;&amp; [ -n "$C" ] &amp;&amp; [ "$C" = "$(k PYTEST_COLLECTED_BEFORE)" ] &amp;&amp; R="$(tail -n 1 "$S/pytest-before.out" | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" &amp;&amp; [ -n "$R" ] &amp;&amp; [ "$R" = "$(k PYTEST_RESULT_BEFORE)" ] &amp;&amp; case "$R" in *failed*|*error*) false ;; *passed*) true ;; *) false ;; esac &amp;&amp; [ "$(printf '%s\n' "$R" | grep -oE '[0-9]+' | awk '{s+=$1} END{print s}')" = "$C" ] &amp;&amp; [ "$(k PYTEST_EXIT_BEFORE)" = 0 ] &amp;&amp; grep -qxF '## pytest before (leg b)' "$F" &amp;&amp; grep -qxF '~~~text pytest-skips-before' "$F" &amp;&amp; ! git diff --name-only "$B" HEAD | grep -vxF -e "$F" -e "$D/70-02-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- the scratch output missing or empty
- the collected count empty or differing from `PYTEST_COLLECTED_BEFORE`
- the result tokens empty, differing from the key, containing `failed` or `error`, or lacking `passed`
- the result tokens not summing to the collected count
- `PYTEST_EXIT_BEFORE` not 0
- the section or skip block missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `PYTEST_COLLECTED_BEFORE` equals a fresh unanchored collect extraction.
    - `PYTEST_RESULT_BEFORE` equals the tokens of the recorded run's last line, contains `passed` and no `failed` or `error`, and sums to the collected count.
    - `PYTEST_EXIT_BEFORE = 0`, and the skip reasons are quoted.
  </acceptance_criteria>
  <done>Leg (b)'s before side is recorded at PHASE_BASE_SHA under the same extras and interpreter every later plan uses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline keys → every later comparison | a wrong or copied baseline value makes every "identical" verdict meaningless |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-03 | Repudiation | a research-time number recorded as if measured | medium | mitigate | Each key's verify re-derives the value from a fresh command at `PHASE_BASE_SHA` and compares exactly. |
| T-70-04 | Tampering | measuring through another tree's `.venv` (the editable finder resolves to the main checkout) | medium | mitigate | Worktree-only execution is asserted by `test -f .git` and the worktree path check; the interpreter keys must equal `.venv/pyvenv.cfg`. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan creates
`70-BASELINE-EVIDENCE.md`, with the keys `BASE_70_02`, `PHASE_BASE_SHA`, `SCRATCH_70_02`,
`VENV_HOME`, `VENV_VERSION_INFO`, `LOCK_RUFF_VERSION`, `RUFF_VERSION_BASE`, `UP_TOTAL_BASE`,
`UP006_BASE`, `UP035_BASE`, `UP_FILE_COUNT_BASE`, `UP_FILES_MATCH_PLAN`,
`FUTURE_ANNOTATIONS_BASE`, `OPTIONAL_BASE`, `UNION_BASE`, `PIPE_NONE_BASE`, `MYPY_VERSION_BASE`,
`MYPY_EXIT_BEFORE`, `MYPY_STDOUT_SHA256_BEFORE`, `PYTEST_COLLECTED_BEFORE`,
`PYTEST_EXIT_BEFORE`, `PYTEST_RESULT_BEFORE` and `PYTEST_WARNINGS_BEFORE`, and the blocks
`~~~text up-files`, `~~~text mypy-before` and `~~~text pytest-skips-before`.
</artifacts_this_phase_produces>

<verification>
- The baseline is measured at PHASE_BASE_SHA on nixpkgs CPython 3.13.13 with the dev and docs extras.
- The fresh census is non-zero and names exactly the ten planned files.
</verification>

<success_criteria>
The before sides of QUA-12 legs (b) and (e), and SC#2's base values, exist as committed keys that
70-10 and 70-11 compare against.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-02-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
