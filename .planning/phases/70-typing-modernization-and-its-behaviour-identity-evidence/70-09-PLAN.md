---
phase: 70-typing-modernization-and-its-behaviour-identity-evidence
plan: 09
type: execute
wave: 4
depends_on: ["70-04", "70-05", "70-06", "70-07", "70-08"]
files_modified:
  - pyproject.toml
  - .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md
  - .planning/todos/completed/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md
  - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-FLIP-EVIDENCE.md
autonomous: true
requirements: [QUA-09, DOC-22]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'Before the flip, a repo-wide `uv run ruff check . --select UP006,UP035` on the merged tree exits 0. Every file is already converted, so removing the ignores cannot turn any merged state red (ROADMAP constraint 1(ii)). A residue in any file, including one no plan touched, halts the plan and is never fixed opportunistically.'
    - 'One commit, the flip commit, holds exactly two changes: `pyproject.toml` loses the `"UP035"` and `"UP006"` ignore lines together with their deferral comments, and the 2026-07-22 todo is renamed from `todos/pending/` to `todos/completed/` with its content unchanged (R100). `git show --name-status -M` of that commit lists nothing else (ROADMAP constraint 2).'
    - 'Edge QUA-09/adjacency: the two deleted ignore lines are adjacent. `git diff` of pyproject.toml removes exactly those two lines and adds none, and the neighbouring `B017` and `UP028` entries remain.'
    - 'Edge QUA-09/empty: after the flip, pyproject.toml parses with `tomllib`, and `[tool.ruff.lint] ignore` is a non-empty list that contains neither `UP006` nor `UP035`. Its length is the pre-flip length minus 2.'
    - 'After the flip commit, `git grep` over tracked files outside .planning/ finds zero references to the todo''s pending path. The same grep at the pre-flip commit finds exactly one, in pyproject.toml, as a positive control.'
    - 'Post-flip, `uv run ruff check . --select UP006,UP035` and bare `uv run ruff check .` both exit 0, with no `--preview` anywhere.'
    - 'The gate quartet passes on the post-flip tree in this worktree: `uv run ruff check .`, `uv run black --check .`, `uv run mypy typsphinx/` with exit 0, and the full `LC_ALL=C` pytest run with exit 0 and a result equal to PYTEST_RESULT_BEFORE.'
  artifacts:
    - path: pyproject.toml
      provides: '[tool.ruff.lint] ignore without UP006/UP035'
    - path: .planning/todos/completed/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md
      provides: 'The folded todo, moved with the flip'
      contains: 'resolves_phase: 70'
    - path: .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-FLIP-EVIDENCE.md
      provides: 'Pre-flip residue check, the flip commit''s shape, the pending-path census, post-flip ruff, gate quartet'
      contains: 'PRE_FLIP_SHA = '
  key_links:
    - from: 'pyproject.toml [tool.ruff.lint] ignore'
      to: 'CI Lint and Format Check / tox -e lint'
      via: 'ruff check . now enforces UP006/UP035'
      pattern: 'UP028'
  prohibitions:
    - statement: 'MUST NOT edit any other pyproject.toml line (dependency specifiers, version, the rest of the ignore array), touch uv.lock, run ruff --fix or black in write mode anywhere, add a `preview` setting, or absorb SEED-003.'
      requirement_id: QUA-09
      category: safety
      status: resolved
      verification: judgment
    - statement: 'MUST NOT fix a residue found by the pre-flip repo-wide measure inside this plan; a residue halts the plan for replanning.'
      requirement_id: QUA-09
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Flip the lint gate once every file is converted (QUA-09; DOC-22's second half; ROADMAP constraints 1(ii), 2 and 3). This plan:
- deletes the two ignore lines from `pyproject.toml`
- moves the 2026-07-22 todo to `todos/completed/` in the same commit
- re-measures repo-wide, expecting zero
- runs the gate quartet on the post-flip tree

Purpose: the ignores leave only after nothing is left for them to hide, so no merged state of the branch is ever red. The todo and the last tracked reference to its pending path leave together, in one commit.

Output: the flip commit and `70-FLIP-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-CONTEXT.md
@.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md
@pyproject.toml
</context>

<worktree_provisioning>
You are in an isolated worktree (`test -f .git`). Provision it with
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev --extra docs --python 3.13.13`
and run every tool through `uv run`. Never run `uv sync` in the main checkout. Run every verify
command with the Bash tool `timeout` set to 600000. Run pytest under `LC_ALL=C`.

**Evidence key lines.** Every `KEY = value` line holds the bare value alone on its line. Put any
explanation on the next line, never after the value.

**Commits.** Stage files by explicit path, never `git add -A`. The flip commit holds only the
pyproject.toml edit and the todo rename. On any failure path, write `## HALT: <reason>` into the
evidence file, commit it, and stop.
</worktree_provisioning>

<!-- planner-discipline-allow: ^## HALT -->
<!-- planner-discipline-allow: ## HALT -->
<!-- planner-discipline-allow: todos/pending/2026-07-22-modernize -->
<!-- planner-discipline-allow: UP006 -->
<!-- planner-discipline-allow: UP035 -->
<!-- planner-discipline-allow: --preview -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: confirm zero repo-wide residue, then in one commit delete the two ignore lines and move the todo, and prove the post-flip tree is ruff-clean</name>
  <precondition>`70-CONV-BUILDER-EVIDENCE.md`, `70-CONV-TEMPLATE-EVIDENCE.md`, `70-CONV-WRITER-INIT-EVIDENCE.md`, `70-CONV-TESTS-EVIDENCE.md` and `70-MASK-PILOT-EVIDENCE.md` all exist at HEAD with their mask keys EQUAL and no HALT heading. `pyproject.toml` still carries both ignore lines. Otherwise write `## HALT: conversions incomplete` and stop.</precondition>
  <reversibility rating="reversible">Two config lines and one file rename, in one commit that `git revert` undoes cleanly.</reversibility>
  <files>pyproject.toml, .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md, .planning/todos/completed/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md, .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-FLIP-EVIDENCE.md</files>
  <read_first>
    - pyproject.toml (lines 118-135: the `[tool.ruff.lint]` ignore array; the lines to delete begin `"UP035",` and `"UP006",`)
    - .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md (moved unchanged)
    - .planning/ROADMAP.md (constraints 1, 2, 3 and 4; Phase 70 SC#1 and SC#2)
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PHASE_BASE_SHA`, `RUFF_VERSION_BASE`)
  </read_first>
  <action>
1. Create `70-FLIP-EVIDENCE.md` with the heading `# Phase 70 — The Ignore Flip`. Before any commit, record `BASE_70_09 = ` and `PRE_FLIP_SHA = `, both from `git rev-parse HEAD`. Then record `VENV_HOME = ` and `VENV_VERSION_INFO = ` after provisioning; both must equal 70-02's. Also record `RUFF_VERSION_FLIP = ` from `uv run ruff --version 2>/dev/null`, which must equal `RUFF_VERSION_BASE` (constraint 4: no mid-flight ruff bump).

2. **`## Pre-flip residue check`.** Record `uv run ruff check . --select UP006,UP035; echo "exit:$?"`, which must print `exit:0`. If it does not, record every remaining file and line, write `## HALT: residue before the flip`, and stop. Also record `IGNORE_LEN_PRE = ` as the length of `[tool.ruff.lint] ignore`, read with `uv run python -c` and `tomllib`.

3. **`## The flip commit`.**
   - Use the Edit tool to delete the whole `"UP035",` line and the whole `"UP006",` line, each with its trailing comment, from the ignore array. Change nothing else in pyproject.toml.
   - Run `git mv .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md .planning/todos/completed/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md`. Do not edit the file's content.
   - Commit exactly those two changes together, with the message `chore(70-09): drop the UP006/UP035 ignores and move the typing todo to completed (QUA-09, DOC-22)`.
   - Quote `git show --name-status -M --format= HEAD`. It must list exactly `M pyproject.toml` and one `R100` rename from pending to completed.
   - Quote `git diff -U0 "$PRE_FLIP_SHA" HEAD -- pyproject.toml`: two removed lines, zero added.

4. **`## Post-flip checks`.**
   - Record `IGNORE_LEN_POST = `, which must equal `IGNORE_LEN_PRE` minus 2. The list still contains `B017` and `UP028`, and contains neither `UP006` nor `UP035`.
   - Record `git grep -n 'todos/pending/2026-07-22-modernize' -- . ':!.planning'`, which must be empty. Its positive control is the same grep at `PRE_FLIP_SHA`, which must print one line, in pyproject.toml.
   - Record `uv run ruff check . --select UP006,UP035` and bare `uv run ruff check .`; both must exit 0.

5. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-FLIP-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; T=2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md &amp;&amp; test -f .git &amp;&amp; case "$(pwd -P)" in /home/yuta/Documents/typsphinx/.claude/worktrees/*) true ;; *) false ;; esac &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; B="$(k PHASE_BASE_SHA "$BE")" &amp;&amp; PF="$(k PRE_FLIP_SHA)" &amp;&amp; [ "${#PF}" = 40 ] &amp;&amp; git merge-base --is-ancestor "$B" "$PF" &amp;&amp; [ "$(sed -n 's/^version_info = //p' .venv/pyvenv.cfg)" = "$(k VENV_VERSION_INFO "$BE")" ] &amp;&amp; [ "$(uv run ruff --version 2&gt;/dev/null)" = "$(k RUFF_VERSION_BASE "$BE")" ] &amp;&amp; FL="$(git log --format=%H "$PF"..HEAD -- pyproject.toml)" &amp;&amp; [ "$(printf '%s\n' "$FL" | grep -c .)" = 1 ] &amp;&amp; [ "$(git show --name-status -M --format= "$FL" | LC_ALL=C sort)" = "$(printf 'M\tpyproject.toml\nR100\t.planning/todos/pending/%s\t.planning/todos/completed/%s\n' "$T" "$T" | LC_ALL=C sort)" ] &amp;&amp; [ "$(git diff --numstat "$PF" "$FL" -- pyproject.toml)" = "$(printf '0\t2\tpyproject.toml')" ] &amp;&amp; ! git diff -U0 "$PF" "$FL" -- pyproject.toml | grep '^-[^-]' | grep -qvE '"UP0(06|35)",' &amp;&amp; git diff -U0 "$PF" "$FL" -- pyproject.toml | grep -q '^-.*"UP006",' &amp;&amp; git diff -U0 "$PF" "$FL" -- pyproject.toml | grep -q '^-.*"UP035",' &amp;&amp; test -f ".planning/todos/completed/$T" &amp;&amp; test ! -e ".planning/todos/pending/$T" &amp;&amp; uv run python -c 'import sys,tomllib;i=tomllib.load(open("pyproject.toml","rb"))["tool"]["ruff"]["lint"]["ignore"];sys.exit(0 if i and "UP006" not in i and "UP035" not in i and "B017" in i and "UP028" in i and len(i)==int(sys.argv[1])-2 else 1)' "$(k IGNORE_LEN_PRE)" &amp;&amp; [ "$(k IGNORE_LEN_POST)" = "$(( $(k IGNORE_LEN_PRE) - 2 ))" ] &amp;&amp; [ -z "$(git grep -n 'todos/pending/2026-07-22-modernize' -- . ':!.planning')" ] &amp;&amp; [ "$(git grep -n 'todos/pending/2026-07-22-modernize' "$PF" -- . ':!.planning' | wc -l)" = 1 ] &amp;&amp; git grep -q 'todos/pending/2026-07-22-modernize' "$PF" -- pyproject.toml &amp;&amp; uv run ruff check . --select UP006,UP035 &amp;&amp; uv run ruff check . &amp;&amp; ! grep -qE '^[[:space:]]*preview[[:space:]]*=' pyproject.toml &amp;&amp; for s in '## Pre-flip residue check' '## The flip commit' '## Post-flip checks'; do grep -qxF "$s" "$F" || { echo "missing section: $s"; exit 1; }; done &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit, or `missing section: <name>` printed. Each of these makes it fail:
- `PRE_FLIP_SHA` missing or not descended from `PHASE_BASE_SHA`
- the interpreter or the ruff version differing from the baseline
- zero or several commits touching pyproject.toml since `PRE_FLIP_SHA`
- the flip commit's name-status not exactly `M pyproject.toml` plus one R100 todo rename
- the pyproject diff not exactly two removed UP lines and zero added lines
- the todo not in completed/, or still in pending/
- the ignore list failing the tomllib check or its length check
- any tracked reference to the pending path outside .planning/, or the positive control not showing one
- ruff reporting anything
- a `preview` setting in pyproject.toml
- a section missing
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - The flip commit's `git show --name-status -M` is exactly `M pyproject.toml` and `R100 .planning/todos/pending/2026-07-22-… .planning/todos/completed/2026-07-22-…`.
    - pyproject.toml's ignore list has no `UP006` or `UP035`, keeps `B017` and `UP028`, and is two entries shorter.
    - `git grep -n 'todos/pending/2026-07-22-modernize' -- . ':!.planning'` is empty at HEAD and has one hit at `PRE_FLIP_SHA`.
    - `uv run ruff check .` and `uv run ruff check . --select UP006,UP035` exit 0.
  </acceptance_criteria>
  <done>ruff now enforces UP006/UP035 on a tree with nothing left to enforce, and the todo left `pending/` in the same commit as the last reference to it.</done>
</task>

<task type="auto">
  <name>Task 2: Run the gate quartet on the post-flip tree: ruff, black, mypy and the full pytest suite</name>
  <precondition>Task 1 is committed, and `70-FLIP-EVIDENCE.md` has no HALT heading.</precondition>
  <reversibility rating="reversible">Measurement and evidence only.</reversibility>
  <files>.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-FLIP-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-BASELINE-EVIDENCE.md (`PYTEST_RESULT_BEFORE`, `MYPY_EXIT_BEFORE`)
    - tox.ini (`[testenv:lint]`: `black --check .` then `ruff check .`)
  </read_first>
  <action>
1. **`## Gate quartet (post-flip)`.** Quote each command with its output tail and exit code:
   - `uv run ruff check .`
   - `uv run black --check .`
   - `uv run mypy typsphinx/ 2>/dev/null`, which must exit 0; record `MYPY_EXIT_FLIP = 0`
   - `LC_ALL=C uv run pytest -q -rs -p no:cacheprovider`, which must exit 0; record `PYTEST_RESULT_FLIP = ` with 70-02's token extraction, which must equal `PYTEST_RESULT_BEFORE`

   This is the flip plan's own gate. The formal after-side evidence for legs (b) and (e) is re-measured in wave 5 by 70-10 and 70-11, never in the plan whose work it audits.

2. Commit the evidence file.
  </action>
  <verify>
    <automated>D=.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence &amp;&amp; F="$D/70-FLIP-EVIDENCE.md" &amp;&amp; BE="$D/70-BASELINE-EVIDENCE.md" &amp;&amp; test -f .git &amp;&amp; grep -q typsphinx-fhs-run "$(command -v uv)" &amp;&amp; k() { sed -n "s/^$1 = //p" "${2:-$F}" | head -n 1; } &amp;&amp; uv run ruff check . &amp;&amp; uv run black --check . &amp;&amp; uv run mypy typsphinx/ &gt;/dev/null 2&gt;&amp;1 &amp;&amp; [ "$(k MYPY_EXIT_FLIP)" = 0 ] &amp;&amp; R="$(LC_ALL=C uv run pytest -q -p no:cacheprovider 2&gt;&amp;1 | tail -n 1 | grep -oE '[0-9]+ (passed|failed|skipped|errors?|xfailed|xpassed)' | paste -sd' ')" &amp;&amp; [ -n "$R" ] &amp;&amp; [ "$R" = "$(k PYTEST_RESULT_BEFORE "$BE")" ] &amp;&amp; [ "$(k PYTEST_RESULT_FLIP)" = "$R" ] &amp;&amp; grep -qxF '## Gate quartet (post-flip)' "$F" &amp;&amp; ! git diff --name-only "$(k BASE_70_09)" HEAD | grep -vxF -e pyproject.toml -e .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md -e .planning/todos/completed/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md -e "$F" -e "$D/70-09-SUMMARY.md" &amp;&amp; ! grep -q '^## HALT' "$F"</automated>
    <fails_when>Non-zero exit. Each of these makes it fail:
- ruff, black or mypy reporting anything
- `MYPY_EXIT_FLIP` not 0
- the pytest result empty or differing from `PYTEST_RESULT_BEFORE`
- the section missing
- a file outside this plan's set changed
- a HALT heading</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check .`, `uv run black --check .` and `uv run mypy typsphinx/` exit 0 on the post-flip tree.
    - `PYTEST_RESULT_FLIP` equals `PYTEST_RESULT_BEFORE`, from a run that exited 0.
  </acceptance_criteria>
  <done>The post-flip tree passes the gate quartet with the stricter lint config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml → CI lint and every contributor's ruff | the ignore list decides what CI accepts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-19 | Tampering | other pyproject.toml lines (dependency specifiers, version) | high | mitigate | The diff must be exactly two removed UP lines and zero added; the tomllib check pins the remaining entries. |
| T-70-20 | Denial of Service | flipping onto an unconverted remainder (red CI) | medium | mitigate | The repo-wide pre-flip measure must be zero, or the plan halts. |
| T-70-21 | Repudiation | a dangling pending-path reference, or a todo moved separately | low | mitigate | One commit holds both changes, checked by name-status; the grep is empty after and has one hit before. |
| T-70-SC | Tampering | package installs | low | accept | Provisioning installs only what `uv.lock` pins; uv.lock is untouched. |
</threat_model>

<artifacts_this_phase_produces>
See `70-01-PLAN.md` § artifacts_this_phase_produces for the phase-wide list. This plan modifies
`pyproject.toml`, renames the 2026-07-22 todo into `.planning/todos/completed/`, and creates
`70-FLIP-EVIDENCE.md`, with the keys `BASE_70_09`, `PRE_FLIP_SHA`, `VENV_HOME`,
`VENV_VERSION_INFO`, `RUFF_VERSION_FLIP`, `IGNORE_LEN_PRE`, `IGNORE_LEN_POST`, `MYPY_EXIT_FLIP` and
`PYTEST_RESULT_FLIP`.
</artifacts_this_phase_produces>

<verification>
- The ignores are gone in the same commit that moves the todo.
- The tree is ruff-clean repo-wide with the rules enforced, and the gate quartet passes.
</verification>

<success_criteria>
QUA-09's config half and DOC-22's todo move are done, in the order constraint 1 binds, and SC#1's
same-commit property is observable on the flip commit.
</success_criteria>

<output>
Create `.planning/phases/70-typing-modernization-and-its-behaviour-identity-evidence/70-09-SUMMARY.md` when done. Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`.
</output>
