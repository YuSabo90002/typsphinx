---
phase: 49-per-master-include-graph-with-state-guarded-includes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
  - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md
autonomous: true
requirements: [COMP-05, COMP-06, COMP-10, COMP-12]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "49-EVIDENCE.md carries a `## State-syntax measurement` section recording real `typst.compile()` results for the DECIDED spellings (the namespaced state key of D-07 and the per-emission-site edge key of D-04), not for PROJECT.md's `\"inc\"` sketch — D-09 required this measured before any plan depends on it, and this plan is where the dependency starts."
    - "The array-literal rule is measured at every arity that matters: 0 keys renders `()`, 1 key renders a one-element array, 2+ keys render a multi-element array, and for EACH arity a compiled probe reports the Typst TYPE as `array` and the length equal to the input count. The 1-key case is the load-bearing one (D-09 / RESEARCH Pitfall 1: a missing trailing comma is NOT a syntax error, it silently degrades `in` from array membership to substring containment)."
    - "49-EXPECTED-STRUCTURE.md fixes the emission contract as substitutable templates — the state key string, the edge-key format, the array-literal rendering rule, the one-line guard shape, and the DFS rule — so every expected value in this phase is a substitution into them rather than an independent derivation (D-05: one judgement, one derivation point)."
    - "The edge key is unique per emission site within its parent document (D-04). The contract states the occurrence rule explicitly: the graph side always emits occurrence 0, so only occurrence-0 keys can ever be present in a published edge set, which is what makes a duplicate emission site (occurrence >= 1) structurally dark instead of firing a second physical include."
    - "Both the graph side and the emission side iterate `includefiles`, never `entries` (D-03) — the contract states this as a property of both sides and names the consequence it closes (external-URL and `self` entries never produce a guard, so the currently-live `file not found ... self.typ` fatal disappears as a structural consequence)."
    - "The heading-offset emission is unchanged (D-08): one `set heading(offset: heading.offset + 1)` per toctree inside the existing `context { ... }` block, with the per-entry guard INSIDE that block rather than wrapped around it. The contract records this as an assertion that must NOT change."
    - "The traversal rule is written down as a recursive walk with an ordered `traversed` threaded through recursion, seeded with the master's own docname, iterating each parent's children in source order — and the contract explicitly names the LIFO work-stack shape as forbidden, with its measured defect (the last-listed child is processed first, reversing sibling order with no compile error) (COMP-05)."
    - "For the mirror pair `[zmid, shared]` versus `[shared, zmid]` the contract states both expected edge sets and both expected resolved heading-level sequences, derived from the traversal rule alone before any fixture exists — order of the parent's entry list is the ONLY input that decides which position claims a multiply-reachable document, never a `prefer-deeper` heuristic and never Sphinx's lexicographic `selecting: X <- Y` tiebreak (COMP-10)."
    - "The published array's key ORDER is irrelevant to correctness (membership test, not sequence match) but is fixed to DFS discovery order so wrapper bytes are deterministic; two builds of the same project produce a byte-identical state line (COMP-06, ordering)."
    - "Every docname interpolated into an emitted Typst string literal — on both the publish side and the guard side — is routed through the existing `escape_typst_string` helper, so a docname containing a quote or a backslash yields a valid literal that still matches exactly on both sides; neither side applies Unicode normalization, so equality is exact code-point equality (COMP-06, encoding)."
    - "49-EXPECTED-STRUCTURE.md carries a `## Fixture specification` section fixing, for every fixture this phase adds, its exact docnames, toctree entry ORDER, marker strings, `typst_documents` entries and root_doc — and the expected edge set for each master derived by hand from that specification. The fixtures authored in later plans are built TO this specification; the specification is not read back off them (binding constraint #6)."
    - "49-EXPECTED-STRUCTURE.md carries a `## Degenerate-shape outcome table` stating, for each of the five D-06 shapes plus the `self`/external-URL shape, the DECIDED outcome (include / skip / degrade-to-text) and the traversal fact that produces it — decided here, at plan time, never discovered as a test failure (SC#2)."
    - "49-EXPECTED-STRUCTURE.md carries a `## Assertion census` produced by repo-wide grep, listing every existing test assertion that reads a toctree-emitted `include(` or the wrapper body, with a per-assertion verdict of SURVIVES or FLIPS and, for each FLIPS row, its new expected value. The census also lists every test that calls the per-document write path WITHOUT the builder's own `write()`, because those paths see an empty edge map."
    - "49-EXPECTED-STRUCTURE.md states in its own header that no builder was run to produce it and that the Typst probes in 49-EVIDENCE.md are hand-written files exercising the Typst language, not emitter output."
    - { statement: "Typst's multi-pass layout converges for a corpus-scale state/context graph (the Sphinx `doc/` tree, 154 documents) with no oscillation between passes. This research measured convergence only on small fixtures; corpus scale is measured in 49-06, and a failure there is a design-level finding escalated per D-02, never worked around by narrowing the corpus.", verification: backstop }
  artifacts:
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md
  key_links:
    - "49-EVIDENCE.md `## State-syntax measurement` -> 49-EXPECTED-STRUCTURE.md `## Emission contract`: the contract's array-literal rule and guard line are substitutions of measured outcomes, so no later plan carries an unmeasured Typst-syntax question."
    - "49-EXPECTED-STRUCTURE.md `## Fixture specification` -> the fixtures authored in 49-02 and 49-03: every docname, entry order and marker string in those fixtures is a transcription of this section, and each fixture's expected edge set is read from here, never computed by the new emitter."
    - "49-EXPECTED-STRUCTURE.md `## Assertion census` -> 49-04's migration task: the set of test modules 49-04 must touch is exactly the census's FLIPS rows, so an unpredicted failure after the emitter lands is an unplanned regression rather than a migration item."
  prohibitions:
    - "A COMP-12 corpus convergence failure must not be worked around: the corpus must not be narrowed, and the gate must not be skipped, marked flaky, or left unrun. A convergence failure is escalated to the owner per D-02 and ROADMAP binding constraint #5."
    - "No expected value anywhere in this phase may be derived from the new emitter's own output. Every changed expected value traces to a written-first derivation from the fixture's own conf.py and .rst read literally plus the emission contract."
    - "This phase must not trade one silent omission for another: no configuration may end up producing a PDF that is missing content its own toctree names, with no error and no warning, and no reintroduced write-time include decision may sit alongside the compile-time state guard."
    - "The `:numref:` numbering behaviour must not be silently changed, renumbered, or hidden. Both measured cases are recorded verbatim and handed forward to Phase 51 and Phase 52 per D-01."
---

<objective>
Fix the emission contract by measurement, then write down — from first principles and BEFORE any
production code or fixture exists — the exact expected value of everything this phase changes: each
fixture's source shape and expected edge set, each degenerate shape's decided outcome, and every
existing assertion that flips.

Purpose: ROADMAP binding constraint #6 forbids laundered gates, and this phase moves more expected
values than any other in the milestone (19 test files carry a literal `include("` assertion today).
Binding constraint #4 requires each closed defect to name its pre-fix RED assertion before
implementation starts. D-09 additionally requires the state-guard Typst syntax to be verified against
a real compile before any plan depends on it — and the two spellings that syntax uses (D-04's edge key,
D-07's namespaced state key) are Claude's Discretion, so the measurement must be taken against the
DECIDED spellings, not against PROJECT.md's `"inc"` sketch.

Output: `49-EVIDENCE.md` with a `## State-syntax measurement` section, and
`49-EXPECTED-STRUCTURE.md` with the emission contract, the fixture specification, the
degenerate-shape outcome table and the assertion census. No file under `typsphinx/` or `tests/` is
touched by this plan.

**Tracer note:** this phase's tracer task is 49-04 Task 1 (the end-to-end two-master
`sphinx-build -b typstpdf` slice). It cannot lead the phase because binding constraints #4 and #6
require the pre-fix REDs and the written-first expected values to exist before the emitter changes.
This plan and 49-02/49-03 are that prerequisite; the tracer is the first task that writes production
code.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-CONTEXT.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-RESEARCH.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-PATTERNS.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files and identifiers **created** by Phase 49 (new — not drift; do not flag as unknown
symbols during source-grounding). The full list is repeated in every plan of this phase so any single
plan is self-sufficient for a source-grounding pass.

**New production symbols (all created in 49-04, all module-level in `typsphinx/translator.py` so that
`builder.py` and `writer.py` can both import them without a cycle — `builder.py` -> `writer.py` ->
`translator.py` is the existing import direction):**
- `INCLUDE_STATE_KEY` — the namespaced Typst `state` key string (D-07), fixed to
  `typsphinx:include-edges`
- `make_include_edge_key(parent_docname, child_docname, occurrence=0)` — the ONE shared edge-key
  derivation point (D-04/D-05)
- `derive_master_edge_keys(toctree_includes, master_docname)` — the fresh recursive document-order DFS
  with an ordered `traversed` (COMP-05)
- `render_include_edge_state(edge_keys)` — the wrapper's `#state(...).update((...))` line, including
  the uniform trailing-comma rule
- `render_include_guard(edge_key, include_relpath)` — the one-line `if <key> in state(...).get() {
  include(...) }` guard string
- `TypstTranslator._toctree_entry_occurrences` — the per-document occurrence counter (a fresh
  translator instance is constructed per `translate()` call, so this is naturally per-document)
- `TypstBuilder._build_include_edge_map()` and `TypstBuilder._master_include_edges` — the per-master
  edge-key mapping and the single function that derives it
- `TypstWriter.render_wrapper()` gains a new `edge_keys` keyword parameter

**New test modules:** `tests/test_state_guard_composition_gate.py`,
`tests/test_state_guard_shapes_gate.py`, `tests/test_include_edge_derivation_unit.py`,
`tests/test_include_ledger_removal_gate.py`, `tests/test_state_guard_numref_gate.py`

**New fixture projects under `tests/fixtures/`:** `state_guard_two_master_gate`,
`state_guard_mirror_pair_gate`, `state_guard_self_and_url_gate`, `state_guard_cycle_gate`,
`state_guard_selfref_gate`, `state_guard_glob_gate`, `state_guard_orphan_ref_gate`,
`state_guard_three_master_gate`, `state_guard_substring_key_gate`,
`state_guard_numref_two_case_gate`

**New planning artifacts:** `49-EVIDENCE.md`, `49-EXPECTED-STRUCTURE.md`, `49-RED-EVIDENCE.md`,
`49-SHAPES-RED-EVIDENCE.md`

**Symbols this phase DELETES** (the removals are intended, not accidental):
- `TypstBuilder._included_docnames` (the declaration in `init()`)
- the per-`write()` reset of that ledger
- `visit_toctree`'s ledger read, its membership check and its `.add()` write

<!-- planner-discipline-allow: _included_docnames -->
<!-- planner-discipline-allow: stack.pop -->

## Edge Coverage Ledger (spec-less probe fallback, §C)

No SPEC exists for this phase, so the deterministic edge probe's 26 proposed edges are resolved here.
All 26 rows are accounted for; none is silently dropped.

| # | Req | Category | Disposition | Where |
|---|-----|----------|-------------|-------|
| 1 | COMP-05 | adjacency | resolved (explicit) | 49-03 `must_haves.truths` |
| 2 | COMP-05 | empty | resolved (explicit) | 49-03 `must_haves.truths` |
| 3 | COMP-05 | ordering | resolved (explicit) | 49-01 `must_haves.truths` |
| 4 | COMP-06 | adjacency | resolved (explicit) | 49-03 `must_haves.truths` |
| 5 | COMP-06 | empty | resolved (explicit) | 49-01 `must_haves.truths` |
| 6 | COMP-06 | encoding | resolved (explicit) | 49-01 `must_haves.truths` |
| 7 | COMP-06 | ordering | resolved (explicit) | 49-01 `must_haves.truths` |
| 8 | COMP-06 | idempotency | resolved (explicit) | 49-04 `must_haves.truths` |
| 9 | COMP-06 | concurrency | resolved (explicit) | 49-04 `must_haves.truths` |
| 10 | COMP-07 | adjacency | resolved (explicit) | 49-02 `must_haves.truths` |
| 11 | COMP-07 | empty | resolved (explicit) | 49-02 `must_haves.truths` |
| 12 | COMP-07 | encoding | resolved (explicit) | 49-02 `must_haves.truths` |
| 13 | COMP-07 | ordering | resolved (explicit) | 49-02 `must_haves.truths` |
| 14 | COMP-08 | empty | resolved (explicit) | 49-02 `must_haves.truths` |
| 15 | COMP-08 | encoding | resolved (explicit) | 49-02 `must_haves.truths` |
| 16 | COMP-09 | adjacency | resolved (explicit) | 49-02 `must_haves.truths` |
| 17 | COMP-09 | empty | resolved (explicit) | 49-02 `must_haves.truths` |
| 18 | COMP-09 | encoding | resolved (explicit) | 49-02 `must_haves.truths` |
| 19 | COMP-09 | ordering | resolved (explicit) | 49-02 `must_haves.truths` |
| 20 | COMP-09 | concurrency | resolved (explicit) | 49-02 `must_haves.truths` |
| 21 | COMP-10 | adjacency | resolved (explicit) | 49-02 `must_haves.truths` |
| 22 | COMP-10 | empty | resolved (explicit) | 49-02 `must_haves.truths` |
| 23 | COMP-10 | ordering | resolved (explicit) | 49-02 `must_haves.truths` |
| 24 | COMP-11 | unclassified | **unresolved — flagged assumption** | 49-05 `## Flagged planner assumptions` |
| 25 | COMP-12 | boundary | resolved (explicit) | 49-06 `must_haves.truths` |
| 26 | COMP-12 | precision | resolved (**backstop**) | 49-01 `must_haves.truths` |

Sum check: 25 authored into `must_haves` + 1 surfaced as a flagged assumption = 26 probe rows. No
silent drops. Row 24 is `unclassified` and is therefore never auto-`backstop`ped (it stays
`unresolved` and is surfaced explicitly).

## Prohibition probe (spec-less probe fallback, §B)

Two-stage recall/precision pass over COMP-05..COMP-12. Stage 1 over-produced ~11 raw must-NOT
candidates; Stage 2 dropped the routine-engineering ones (key mismatch, sibling-order reversal, a new
config value, a new runtime dependency — the first two are edge-probe territory and the last two are
already ROADMAP binding constraints #7). Four survivors are authored into `must_haves.prohibitions`,
descriptor-less, in every plan of this phase.

**Canon-referral drop (one breadcrumb, not minted):** interpolating docnames into emitted Typst string
literals is injection canon — covered by `/gsd-secure-phase` and by the existing
`escape_typst_string` / `_sanitize_label` machinery this phase routes through. Not minted as a
prohibition.

## Assumption-delta decision

The `assumption-delta` detector fired with kind `pluralization`, and this is a genuine fire: the phase
replaces one build-scoped include ledger (a single global include set) with a per-master include graph
(N include sets).

- **Noun that is now primary:** the *per-master include edge set* — identified by
  `(master_docname, edge_key)`, not by docname alone. The generalized identity is the **edge**
  (parent, child, occurrence) rather than the **document**.
- **Decision: `promote`.** The build-scoped ledger is deleted, not kept alongside. This is not
  re-opened here — the locked design already implies it, and COMP-11 makes the deletion a requirement.
  Recorded for the record.
- **Suggested contract test** (authored in 49-05): assert that the builder's edge mapping is keyed by
  master docname and that, for the two-master fixture, the mapping has at least two keys whose values
  are DIFFERENT. A future change that reintroduces one global include set collapses those values to
  equal and turns that assertion red.

<tasks>

<task type="auto">
  <name>Task 1: Settle the state-guard Typst syntax by real compiles against the DECIDED spellings, and write 49-EVIDENCE.md `## State-syntax measurement`</name>
  <files>
    .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
  </files>
  <read_first>
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-RESEARCH.md sections "Architecture Patterns" Pattern 1, "Common Pitfalls" 1 and 2, and "Code Examples" (the seven compiled probes, the trailing-comma hazard's three fixtures, and the Phase 48 D-08 line-break rule re-confirmed for this phase's `if` chains)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-CONTEXT.md `<decisions>` D-04, D-07, D-08, D-09 and `<specifics>` (the reviewed output shape, and the explicit instruction to treat `"inc"` and `"index>zmid"` as sketches rather than decided spellings)
    - .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md `## Body-mode measurement` (the artifact shape to imitate: verbatim probe source, verbatim compile result, an explicit statement of what was adopted, and an explicit statement that the probes are hand-written Typst exercising the language rather than emitter output)
    - typsphinx/translator.py lines 5016-5121 (`visit_toctree`, the current unconditional-include loop and the existing `context {` / `set heading(offset: heading.offset + 1)` lines the guard goes inside)
    - typsphinx/translator.py lines 141-180 (`escape_typst_string`, the single source of truth for string-literal escaping every emitted key must route through)
    - tests/test_pdf_render_gate.py lines 227-262 (the `typst.compile()` + `%PDF` magic-byte + `pypdf` readback idiom the probes reuse)
  </read_first>
  <precondition>`typst-py` is importable and reports version 0.15.0: `uv run python -c "import importlib.metadata as m; print(m.version('typst'))"` prints `0.15.0`.</precondition>
  <action>
Provision the worktree first. `.git` is a FILE in an isolated worktree, so run
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` and then run every command through
`uv run`, per CLAUDE.md's mandatory worktree-isolated execution protocol. Confirm isolation with
`uv run python -c "import typsphinx, pathlib; print(pathlib.Path(typsphinx.__file__).resolve())"` and
record that path in the evidence artifact's provenance header. Do not shell out to
`uv run sphinx-build` anywhere; `sys.executable -m sphinx` is the only permitted Sphinx invocation
form in this phase, and this task needs no Sphinx build at all.

The two spellings this phase's syntax depends on are Claude's Discretion and are DECIDED here, so the
measurement is taken against them rather than against PROJECT.md's sketch:

- **Namespaced state key (D-07):** the literal string `typsphinx:include-edges`. Rationale to record:
  a user-supplied `typst_template` is arbitrary Typst and may legitimately call `state("inc")`; a
  project-prefixed key with a separator no bare identifier would use makes a silent collision
  implausible. The bare `"inc"` from PROJECT.md is a sketch and is superseded.
- **Edge key (D-04/D-05):** the format `<parent_docname>#<occurrence>><child_docname>` — parent
  docname, a literal `#`, the 0-based occurrence index of this emission site among the emission sites
  in that parent naming that same child, a literal `>`, then the child docname. For a document whose
  toctree entries are `zmid`, `shared` the keys are `index#0>zmid` and `index#0>shared`; for a
  toctree listing `child` twice they are `index#0>child` and `index#1>child`. Rationale to record: the
  graph side always emits occurrence 0 (it claims a child at that child's FIRST non-traversed
  appearance in the parent's ordered list, which is always the first appearance), so only
  occurrence-0 keys can ever appear in a published edge set — which is exactly what makes a duplicate
  emission site structurally dark rather than firing a second physical include.

Write throwaway `.typ` probes under the scratch directory and compile each with
`uv run python -c "import typst; ..."`. These are hand-written Typst files exercising the LANGUAGE, not
output read off an emitter, so the measurement does not violate binding constraint #6 — state that
explicitly in the recorded section. Probe set:

1. **Arity 0.** A wrapper publishing an empty edge set with the empty-array literal, including a
   content file with one guard that must not fire. Assert the compile succeeds and the child's marker
   is absent.
2. **Arity 1.** A wrapper publishing exactly one edge key with a trailing comma, and a content file
   with that one guard. Assert the compile succeeds and the child's marker is present exactly once.
3. **Arity 2 and arity 3, both with an unconditional trailing comma after the last element.** This is
   the uniform rendering rule the contract adopts: `()` for zero keys and a parenthesized
   comma-separated key list with a trailing comma after the LAST element for one or more. Measure
   that a trailing comma on a 2- and 3-element literal is accepted, so the rule needs no
   `len == 1` special case and the RESEARCH Pitfall 1 hazard cannot arise by construction.
4. **Type-and-length readback at every arity.** For arities 0, 1, 2, 3, a probe whose only body is a
   context block rendering the `repr` of the state value's TYPE and of its length. Assert the type
   reads as Typst's array type at every arity including 1, and the length equals the input count.
   Record the arity-1 result as the load-bearing one.
5. **The no-trailing-comma counter-probe, to record the hazard rather than to adopt it.** The same
   arity-1 wrapper with the trailing comma removed. Record verbatim that it compiles with NO error,
   that the type readback reports a string rather than an array, and that a second, unrelated key that
   is a proper substring of the published one satisfies the membership test under that degradation.
   Use the decided key format for both keys so the recorded hazard is stated in this phase's own
   spelling.
6. **The dark-guard substring case, in the decided key format.** A wrapper publishing two keys, one of
   which is a proper substring of the other, plus a content file emitting a THIRD guard whose key is a
   proper substring of a published key and which must NOT fire. Assert the dark child's marker is
   absent from the compiled PDF. This is the semantics proof that the guard tests array membership,
   not string containment.
7. **The line-break rule (RESEARCH Pitfall 2 / Phase 48 D-08).** One probe with the guard's condition
   and its opening brace on ONE unbroken line (expected: compiles) and one with a newline between them
   (expected: the parser's expected-block error). Record both verbatim so the emitted-bytes property
   is measured rather than assumed.
8. **Interleaving and outline reachability, restated in the decided spellings.** A content file with
   prose before and after the guarded block, whose guarded child carries a labelled heading, and a
   wrapper that places an outline call BEFORE the include. Assert the extracted text order puts the
   prose-before marker, then the child's body, then the prose-after marker; assert the outline lists
   the conditionally included heading; and assert a heading query finds the child's label.
9. **Standalone content-file compile.** Compile a content file directly with no wrapper. Assert the
   compile succeeds and no child is included, because the state default is the empty array and every
   guard is false. This is the documented standalone behaviour Phase 51 writes up.

Then write `49-EVIDENCE.md` (create the file; later plans APPEND to it, never overwrite) with a
provenance header naming the resolved `typsphinx` package path, the three dependency versions read
live, and an explicit statement that no Sphinx build and no typsphinx emitter ran. Under
`## State-syntax measurement`, record every probe's verbatim source and verbatim compile result, the
two decided spellings with their rationale, and a short closing paragraph stating that D-09 is closed
for this phase's own spellings.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; p=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md'); t=p.read_text(encoding='utf-8') if p.exists() else ''; need=['## State-syntax measurement','typsphinx:include-edges','#0>','arity']; sys.exit(1 if [n for n in need if n not in t] else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `49-EVIDENCE.md` exists and contains the heading `## State-syntax measurement`.
    - It contains the decided state key literal `typsphinx:include-edges` and the decided edge-key separator pair, and states in prose that PROJECT.md's `inc` is a superseded sketch: `grep -c 'typsphinx:include-edges' .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md` returns at least 4.
    - The section records a verbatim compile result for arity 0, 1, 2 and 3 — at least four separate fenced probe blocks whose sources differ only in the number of published keys.
    - The type-and-length readback is recorded for arity 1 specifically, and the recorded type is Typst's array type. Behaviour: re-running the recorded arity-1 probe source through `uv run python -c "import typst; ..."` reproduces the recorded type string.
    - The no-trailing-comma counter-probe is recorded and its recorded outcome is an exit-0 compile whose type readback is a string, NOT an error: the evidence section states in its own prose that the omission is not a syntax error.
    - The dark-guard substring probe is recorded and its recorded outcome is that the dark child's marker is ABSENT from the compiled PDF's extracted text.
    - Both line-break probes are recorded, one compiling and one failing, and the failing one's verbatim output is present in a fenced block.
    - The standalone content-file probe is recorded with an exit-0 compile and no child content.
    - The artifact's header names the resolved `typsphinx` package path (worktree provenance) and states that no Sphinx build and no typsphinx emitter ran.
    - `git status --porcelain typsphinx/ tests/` prints nothing.
  </acceptance_criteria>
  <done>D-09's syntax question is closed for this phase's own decided spellings: every arity of the array-literal rule, the trailing-comma hazard, the dark-guard substring semantics, the line-break rule, the interleaving/outline behaviour and the standalone-compile behaviour are all recorded as verbatim real-compile transcripts in 49-EVIDENCE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Write 49-EXPECTED-STRUCTURE.md — the emission contract, the fixture specification, and the degenerate-shape outcome table</name>
  <files>
    .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md
  </files>
  <read_first>
    - .planning/phases/47-two-layer-output-content-wrapper-split-target-as-path-collis/47-EXPECTED-STRUCTURE.md (the artifact shape to imitate at a larger scale: a contract section whose templates every later expected value substitutes into, then per-fixture sections with a CURRENT table, an EXPECTED table and a "Derivation arithmetic" paragraph)
    - .planning/phases/48-compile-time-cross-reference-guard/48-EXPECTED-STRUCTURE.md (the second instance of the same procedure, including its `## Assertions that must NOT change` and `## How to find any assertion I missed` sections)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md `## State-syntax measurement` (written by Task 1 — the ONLY source of the syntax this contract fixes)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-CONTEXT.md `<decisions>` D-03..D-08 and `<canonical_refs>`
    - .planning/PROJECT.md lines 74-104 and 121-146 (the measured defect-A baseline, the composition rule and the explicit instruction not to port the lexicographic `selecting: X <- Y` tiebreak, the two rejected designs, the standalone-content-file behaviour, the expected test blast radius)
    - .venv/lib/python3.13/site-packages/sphinx/util/nodes.py lines 485-524 (`inline_all_toctrees` — the traversal being mirrored: document-order `findall`, `includefiles` iteration, `traversed` appended BEFORE recursing)
    - .venv/lib/python3.13/site-packages/sphinx/builders/latex/__init__.py lines 389-391 and .venv/lib/python3.13/site-packages/sphinx/builders/singlehtml.py line 95 (the callers that seed `traversed` with the master's own docname)
    - .venv/lib/python3.13/site-packages/sphinx/directives/other.py lines 42-183 (`TocTree.parse_content` — where `entries` and `includefiles` diverge for `self` and external URLs, where `:glob:` expands to sorted docnames, where a duplicate entry warns but still reaches both lists, and where `:reversed:` reverses both)
    - typsphinx/writer.py lines 25-60 (`compute_content_include_path`) and 262-311 (`render_wrapper`, whose one-line body is where the state publication goes)
    - typsphinx/translator.py lines 4592-4620 (`_compute_relative_include_path`, which survives unchanged)
    - tests/fixtures/template_named_dir_master/conf.py (the only existing fixture with two genuinely distinct `typst_documents` masters — the multi-master `conf.py` shape to specify)
    - tests/fixtures/bld02_template_clobber_gate/conf.py lines 1-25 (the verbatim "Load-bearing properties -- do NOT touch any of these" comment-block convention every new fixture must carry)
  </read_first>
  <reversibility rating="costly">The edge-key format (D-04) and the namespaced state key (D-07) are both emitted strings that appear in every wrapper and every content file, so changing them later means moving both sides in lockstep across the whole test corpus. Fixing them in this contract is the cheapest possible moment to fix them.</reversibility>
  <action>
Write `.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md`.
Run no builder and read no emitter output while writing it — that is exactly what binding constraint
#6 forbids. State that in the document's own header, together with the fact that Task 1's Typst probes
are hand-written language probes rather than emitter output.

**`## Emission contract`.** Fix, as substitutable templates with one worked substitution each, every
string this phase emits, so every expected value in the rest of the phase is a substitution rather
than an independent derivation (D-05):

- The state key literal `typsphinx:include-edges` (D-07), and the statement that it appears in
  exactly two emitted shapes: the wrapper's publication call and each guard's read.
- The edge-key format `<parent_docname>#<occurrence>><child_docname>` (D-04), with the occurrence rule
  stated as a property of BOTH sides: the emission side numbers each emission site by how many earlier
  sites in the SAME document named the same child; the graph side always emits occurrence 0. Include
  the derivation of why that makes duplicate sites dark, and the worked example for a toctree listing
  the same child twice.
- The array-literal rendering rule: the empty-array literal for zero keys, and for one or more keys a
  parenthesized comma-separated list of double-quoted keys with a trailing comma after the LAST
  element, unconditionally. State that the uniform rule is what removes the `len == 1` special case
  RESEARCH Pitfall 1 warns about, and cite Task 1's arity-2 and arity-3 probes as the measurement that
  licenses it.
- The wrapper body shape: the publication line first, then the existing content include line, with
  nothing between them. Show the fully substituted two-line body for a master with two edge keys and
  for a master with one.
- The guard line shape: one line per emission site, emitted INSIDE the existing `context { ... }`
  block after the existing heading-offset line (D-08), with the condition and its opening brace on
  ONE unbroken line (Task 1 probe 7 measured the alternative as a parser error). Show the fully
  substituted line for one concrete key and relative path.
- The escaping rule: every docname interpolated into any of these literals routes through the existing
  `escape_typst_string` helper, on both the publication side and the guard side, so a docname
  containing a quote or backslash yields a valid literal that still matches exactly. Note that no
  Unicode normalization is applied on either side, so equality is exact code-point equality.
- The `includefiles` rule (D-03): both the graph side and the emission side iterate the toctree node's
  include-file list, never its entry list. State the two consequences: `self` and external-URL entries
  never produce a guard (closing the currently-live compile fatal), and the emptiness check that
  currently short-circuits on an empty entry list must switch to the include-file list so an
  entries-only toctree emits no block at all.
- The traversal rule (COMP-05): a recursive walk over each parent's ordered include-file list, with an
  ordered `traversed` threaded through recursion, seeded with the master's own docname and appended to
  BEFORE recursing. State the forbidden shape by name and by defect: a last-in-first-out work stack
  fed by forward iteration processes the LAST-listed child first, reversing sibling order with no
  compile error. State that Sphinx's lexicographic `selecting: X <- Y` message comes from a different
  function that governs none of this and must not be ported.
- The unchanged surfaces: `_compute_relative_include_path` and `compute_content_include_path` both
  survive untouched, because only the DECISION to emit moves, not the path computation.

**`## Degenerate-shape outcome table`.** One row per shape, each with the DECIDED outcome and the
traversal fact that produces it (these are transcriptions of D-06 plus D-03, not new judgements):

- 2-node toctree cycle (master `A` lists `B`; `B` lists `A`): `A#0>B` is published; `B`'s own guard for
  `A` is dark because `A` is in `traversed` from the seed. Outcome: **skip** the back edge; compile
  succeeds; each body appears exactly once; no unbounded recursion.
- Self-referencing toctree (a document listing its own docname): the self entry is dark for the same
  reason. Outcome: **skip**, silently.
- `self` in a toctree and external-URL entries: never present in the include-file list, so no guard is
  emitted at all. Outcome: **skip, silently, with no new diagnostic** — typsphinx adds no warning
  Sphinx does not have.
- `:glob:` toctree: expanded at parse time into sorted docnames present in both lists, so it is
  indistinguishable from an explicit toctree by the time the writer sees it. Outcome: **no special
  handling**; guards emitted in the expanded sorted order.
- `:orphan:` document referenced but not toctree'd: in no edge set, so no wrapper includes it; a
  cross-reference to it **degrades to plain text** through Phase 48's compile-time guard, per compiled
  wrapper. Outcome: **not included, reference degrades**; no new mechanism.
- Three or more masters sharing two or more overlapping children: the same algorithm with no special
  case, each master's `traversed` freshly seeded. Outcome: **each master's PDF contains each shared
  child exactly once**, at the position its own traversal order dictates. This is a coverage
  obligation proving the fix is not 2-master-specific, not a design question.
- Duplicate entry inside one toctree directive: warned by Sphinx, still present twice in the
  include-file list, so two guards are emitted and only the occurrence-0 key is ever published.
  Outcome: **included exactly once**.

**`## Fixture specification`.** For each of the ten fixture projects this phase adds, fix the exact
source shape so 49-02, 49-03 and 49-06 build TO this specification rather than the specification being
read back off them. Per fixture give: the directory name, every docname and its `.rst` marker strings,
the exact toctree entry ORDER per document, the `typst_documents` entries verbatim, the `root_doc`
value where it is not the default, which documents carry an orphan field, and the load-bearing
properties the fixture's own comment block must name. Then derive, by hand from the traversal rule,
the expected edge key set for every master, and the expected compiled-PDF observations. The ten
fixtures and their required shapes:

1. `state_guard_two_master_gate` — the defect-A / diamond / interleaving fixture, serving COMP-07,
   COMP-08 and COMP-09 from one project. Docnames `index` (root, master A), `zmid`, `shared`,
   `bmaster` (master B, carries an orphan field so Sphinx emits no not-in-any-toctree warning).
   `index` is shaped like Sphinx's own default index: a prose paragraph carrying a
   `PROSE-BEFORE`-style marker, then one toctree listing `zmid` then `shared` in that order, then an
   Indices-and-tables-shaped section carrying a `PROSE-AFTER`-style marker. `zmid` toctrees `shared`.
   `shared` carries the marker `SHARED-CHAPTER-MARKER`, chosen to match the measured 2026-08-11
   baseline verbatim so the pre-fix RED is directly comparable. `bmaster` toctrees `shared`.
   `typst_documents` has two entries, `index` targeting one wrapper name and `bmaster` targeting
   another; neither target may casefold-equal any docname's own content path (a Phase 47 BLD-03
   self-collision the validator refuses). Derive both masters' edge sets, and state the expected
   observation that `shared`'s marker appears exactly once in EACH master's PDF, that master A's PDF
   text order is prose-before, `zmid`, `shared`, prose-after, and that `shared.typ` is byte-identical
   on disk for both compiles.
2. `state_guard_mirror_pair_gate` — COMP-10's mirror pair. One project, two masters,
   `root_doc` set to the first master. Master A toctrees `zmid` then `shared`; master B toctrees
   `shared` then `zmid` (and carries an orphan field); `zmid` toctrees `shared`. Derive both edge sets
   and both expected resolved heading-level sequences from the traversal rule.
3. `state_guard_self_and_url_gate` — D-03/D-10. One master, one toctree whose entries are, in order, a
   `self` entry, an external-URL entry with a title, then the same child docname listed twice. Record
   the pre-fix behaviour to expect (the emitted content file carries an include of a nonexistent
   `self`-named file and of a URL-named file, and the compile aborts) and the post-fix expectation (no
   guard for either navigation entry, the child included exactly once).
4. `state_guard_cycle_gate` — the 2-node cycle.
5. `state_guard_selfref_gate` — the self-referencing toctree, plus one ordinary child so the document
   is not otherwise empty.
6. `state_guard_glob_gate` — a `:glob:` toctree over three documents whose docnames sort in a
   different order than they appear on disk, so the sorted expansion is observable.
7. `state_guard_orphan_ref_gate` — an orphaned document carrying a labelled section, referenced from
   the master by a cross-reference and toctree'd by nobody.
8. `state_guard_three_master_gate` — three masters sharing two overlapping children plus one mid-level
   document, arranged so that at least one shared child is claimed by a different parent in each
   master. Specify the entry orders explicitly and derive all three edge sets.
9. `state_guard_substring_key_gate` — the substring-overlap fixture. Master `index` toctrees, in
   order, a document whose docname is a proper prefix-extension of another (so that one edge key is a
   proper substring of another) and then that shorter-named document; the longer-named document itself
   toctrees the shorter-named one. Derive the edge set and show explicitly which key is a proper
   substring of which, and which guard is DARK. State the expected observation: the shorter-named
   document's body appears exactly ONCE in the compiled PDF and at the nested heading level, proving
   the dark guard did not fire.
10. `state_guard_numref_two_case_gate` — the two-case `:numref:` fixture (49-06). Two masters with
    figure numbering enabled: one figure reachable from BOTH the root document and the second master
    at DIFFERENT traversal positions, and one figure reachable ONLY from the non-root master. Specify
    both figures' names and captions, and both `:numref:` reference sites.

**`## Assertions that must NOT change`.** The heading-offset emission (D-08), the two path helpers,
the wrapper's template application, the four `@preview` import lines, and every existing assertion
about a wrapper's `#include(` line other than the addition of one preceding publication line.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; p=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md'); t=p.read_text(encoding='utf-8') if p.exists() else ''; need=['## Emission contract','## Degenerate-shape outcome table','## Fixture specification','## Assertions that must NOT change','typsphinx:include-edges','state_guard_two_master_gate','state_guard_mirror_pair_gate','state_guard_self_and_url_gate','state_guard_cycle_gate','state_guard_selfref_gate','state_guard_glob_gate','state_guard_orphan_ref_gate','state_guard_three_master_gate','state_guard_substring_key_gate','state_guard_numref_two_case_gate']; sys.exit(1 if [n for n in need if n not in t] else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `49-EXPECTED-STRUCTURE.md` exists and contains the headings `## Emission contract`, `## Degenerate-shape outcome table`, `## Fixture specification` and `## Assertions that must NOT change`.
    - Its header states that no builder was run to produce it.
    - The contract's guard line appears as a fully substituted single line with the condition's opening brace on the same physical line as the condition: `grep -Ec 'if "[^"]+" in state\("typsphinx:include-edges", \(\)\)\.get\(\) \{' .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md` returns at least 1.
    - The contract's array-literal rule is stated at three arities and the one-or-more form carries a trailing comma after the last element: `grep -Ec '",\)\)' .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md` returns at least 1.
    - The contract names the forbidden traversal shape and its defect, and states that the lexicographic parent-selection message must not be ported: `grep -c 'selecting' .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md` returns at least 1.
    - The degenerate-shape table has at least seven rows and every row carries an explicit outcome word from the set include / skip / degrade: `uv run python -c "import pathlib,re,sys; t=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md').read_text(encoding='utf-8'); s=t.split('## Degenerate-shape outcome table')[1].split('## ')[0]; rows=[l for l in s.splitlines() if l.strip().startswith('|') and '---' not in l]; sys.exit(0 if len(rows)>=8 else 1)"` exits 0.
    - All ten fixture directory names appear in the `## Fixture specification` section, each with an explicitly derived expected edge set per master.
    - The substring fixture's specification names, verbatim, which edge key is a proper substring of which other edge key and which guard is dark.
    - `git status --porcelain typsphinx/ tests/` prints nothing.
  </acceptance_criteria>
  <done>The emission contract, the seven degenerate-shape outcomes and the full ten-fixture source specification with hand-derived expected edge sets all exist on disk, written from Sphinx's own sources plus Task 1's measured syntax, with no builder run.</done>
</task>

<task type="auto">
  <name>Task 3: Produce the repo-wide assertion census with a per-assertion SURVIVES/FLIPS verdict and new expected values</name>
  <files>
    .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md
  </files>
  <read_first>
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md `## Emission contract` (written by Task 2 — the only source of each new expected value)
    - tests/test_toctree_requirement13.py (the whole module — the largest single concentration of toctree-emission assertions)
    - tests/test_integration_nested_toctree.py (the whole module — the second largest, and the module whose nested fixture exercises multi-level heading offsets)
    - tests/test_nested_master_render_gate.py (the whole module)
    - tests/test_duplicate_include_label_render_gate.py (the whole module — its entire premise is the ledger's dedup, which the state guard replaces)
    - tests/test_xref_orphan_degrade_render_gate.py and tests/test_label_existence_guard_unit.py (the two Phase 48 modules that mention the deleted ledger by name)
    - tests/test_two_layer_output_gate.py, tests/test_default_typst_documents_gate.py, tests/test_builder_requirement13.py, tests/test_translator.py, tests/test_builder.py (the remaining modules with a literal include assertion)
    - typsphinx/builder.py lines 612-700 (`write()` — the only site that will populate the edge map) and lines 859-921 (`_write_typst_files`, the per-document write path some unit tests call directly, bypassing `write()`)
    - ROADMAP.md lines 385-395 (binding constraint #6 verbatim, including the instruction to prefer structural/regex assertions over exact-string diffs and to reserve exact strings for what is deterministic by construction)
  </read_first>
  <action>
Append a `## Assertion census` section to `49-EXPECTED-STRUCTURE.md`. This section is the definitive
list of what 49-04's migration task may touch: after the emitter lands, a failing test module NOT on
this list is an unplanned regression to investigate, not a migration item.

Build the census by repo-wide grep, never by reading the emitter. ROADMAP milestone invariant #4
requires "anywhere under X" criteria to be checked by repo-wide grep at discovery time rather than
against the files a requirement happens to name, so search the whole tree, including
`tests/fixtures/**/conf.py` comment blocks, `examples/`, and `docs/`, not just the modules named in
`<read_first>`. Run at minimum: a search for a literal Typst include call in every Python file under
`tests/`; a search for the deleted ledger's name across `typsphinx/`, `tests/` and `docs/`; a search
for direct calls to the per-document write path and to `write_doc`; a search for assertions that count
include occurrences rather than test membership; and a search for tests that construct a synthetic
toctree node.

For every hit, record a row with: file, line, the assertion as written, and a verdict:

- **SURVIVES** — the assertion still holds post-fix without edit. The largest class: an assertion that
  merely checks a literal include call appears somewhere in a file's text still passes, because the
  guard line CONTAINS that call as a substring. Say so explicitly per row rather than assuming it;
  each SURVIVES verdict must name why (substring containment, wrapper-side assertion unaffected by the
  one added publication line, etc.).
- **FLIPS** — the assertion changes. For each, give the new expected value as a substitution into the
  contract, and prefer a structural or regex assertion over an exact-string diff per binding
  constraint #6. Expect this class to include: assertions that pin an exact emitted line or an exact
  emitted block; assertions that count include occurrences and would now also match the guard;
  assertions whose premise is the deleted ledger's dedup (the duplicate-include module's whole
  purpose, which must be MIGRATED to assert the same invariant through the state guard, never
  deleted); and any assertion over a wrapper's full body text, which gains one preceding line.
- **NEEDS-SEEDING** — a test that reaches the per-document write path WITHOUT the builder's own
  `write()`. Those paths see an empty edge map, so every guard is dark and every wrapper publishes the
  empty array. For each, state the chosen remedy: call the builder's `write()`, or seed the edge map
  explicitly with the value the contract derives. State the remedy per row; do not leave it open.
- **SYNTHETIC-NODE** — a test that constructs a toctree node by hand. Post-fix the emission side reads
  the include-file list, so a synthetic node carrying only an entry list emits nothing. For each,
  state that the node must gain an include-file list matching its entries.
- **STALE-PROSE** — a docstring or fixture comment that names the deleted ledger and will become a
  false statement. List every one with its replacement wording. This class exists specifically because
  Phase 47 shipped exactly this defect (its review Warning WR-02: a module docstring still claiming a
  marker the fix had removed, still live at UAT close). Do not repeat it.

Close the section with a `## How to find any assertion I missed` subsection giving the exact
reproducible grep command lines a later plan must re-run to prove the enumeration is still complete,
and a numeric summary: the total hit count, and the count per verdict. State the summary as the
prediction 49-04 will be measured against.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md').read_text(encoding='utf-8'); need=['## Assertion census','SURVIVES','FLIPS','NEEDS-SEEDING','SYNTHETIC-NODE','STALE-PROSE','## How to find any assertion I missed']; sys.exit(1 if [n for n in need if n not in t] else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `49-EXPECTED-STRUCTURE.md` contains a `## Assertion census` section and a `## How to find any assertion I missed` subsection.
    - The census names all five verdict labels and every row carries exactly one of them.
    - The census covers at least the 19 test modules that currently contain a literal Typst include call: `uv run python -c "import pathlib,subprocess,sys; t=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md').read_text(encoding='utf-8'); out=subprocess.run(['grep','-rl','include(\"','--include=*.py','tests/'],capture_output=True,text=True).stdout.split(); missing=[f for f in out if f.split('/')[-1] not in t]; print(missing); sys.exit(1 if missing else 0)"` exits 0.
    - Every FLIPS row carries a new expected value that is a substitution into the `## Emission contract`, and no FLIPS row's expected value is presented as having been read from a build.
    - The STALE-PROSE list is non-empty and includes at least the two fixture `conf.py` files and the two Phase 48 test modules that name the deleted ledger: `uv run python -c "import pathlib,subprocess,sys; t=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md').read_text(encoding='utf-8'); out=subprocess.run(['grep','-rl','_included_docnames','tests/'],capture_output=True,text=True).stdout.split(); missing=[f for f in out if f.split('/')[-1] not in t]; print(missing); sys.exit(1 if missing else 0)"` exits 0.
    - The section closes with a numeric summary giving the total hit count and a count per verdict, stated as the prediction 49-04 is measured against.
    - `git status --porcelain typsphinx/ tests/` prints nothing.
  </acceptance_criteria>
  <done>Every existing assertion this phase could disturb is enumerated by repo-wide grep with a per-assertion verdict and, where it flips, a new expected value derived from the contract — so 49-04's migration has a written-first, countable scope and any unpredicted failure is visible as a regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `conf.py` / `.rst` author input → emitted Typst source | User-authored docnames are interpolated into the edge-key string literals this phase's contract fixes |
| Emitted `.typ` → `typst.compile()` | The Typst compiler consumes text this project generates, including the new state publication and guard lines |

## STRIDE Threat Register

ASVS level 1, blocking threshold `high`. `49-RESEARCH.md` §Security Domain and `49-VALIDATION.md`
§Security Domain both record that ASVS **V2 (Authentication)**, **V3 (Session Management)**,
**V4 (Access Control)** and **V6 (Cryptography)** are **structurally inapplicable** — typsphinx is a
local Sphinx/Typst document-generation tool with no network service, no authentication, no session and
no cryptographic surface. **V5 (Input Validation)** is the one real seam this phase introduces: the new
edge keys are generated from docnames and interpolated into Typst string literals. This block is
recorded rather than omitted, per the phase's security contract.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01 | Tampering | Docname interpolated into an emitted Typst string literal by the new edge-key derivation and by the state publication | low | mitigate | The contract written in Task 2 requires every docname to route through the existing `escape_typst_string` helper on BOTH the publication side and the guard side; asserted at unit level in 49-04 with a docname carrying a quote and a backslash |
| T-49-02 | Tampering | The guard's condition string, if spelled differently on the graph side and the emission side | low | mitigate | D-05: one shared derivation function, fixed by the contract in Task 2 and asserted by a two-sided agreement test in 49-04. A mismatch does not fail the build — content silently vanishes — so the mitigation is structural (one function) rather than diagnostic |
| T-49-03 | Denial of Service | Corpus-scale Typst multi-pass layout convergence over the new state/context graph | medium | accept | Measured in 49-06 against the existing GATE-02 corpus gate. A convergence failure is a design-level finding escalated per D-02 and binding constraint #5, never worked around. Accepted for this plan because this plan emits no Typst |
| T-49-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no packages of any kind. `49-RESEARCH.md` §Package Legitimacy Audit records no new PyPI dependency and no new `@preview` package; ROADMAP binding constraint #7 forbids both |
</threat_model>

<verification>
- `uv run python -c "import pathlib,sys; d=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes'); sys.exit(0 if (d/'49-EVIDENCE.md').exists() and (d/'49-EXPECTED-STRUCTURE.md').exists() else 1)"` exits 0.
- `git status --porcelain typsphinx/ tests/` prints nothing for the whole plan.
- `uv run pytest -m "not slow" -q` exits 0 (nothing regressed; this plan changes no code).
- Both planning artifacts are committed before any plan touches `typsphinx/`.
</verification>

<success_criteria>
- The state-guard Typst syntax is measured at four arities against the DECIDED state key and edge-key
  spellings, and the trailing-comma hazard, the dark-guard substring semantics, the line-break rule
  and the standalone-compile behaviour are all recorded verbatim (D-09 closed for this phase).
- The emission contract fixes every emitted string as a substitutable template with one worked
  substitution, so no later plan derives a spelling independently (D-04, D-05, D-07, D-08).
- All seven degenerate-shape outcomes are decided at plan time, with the traversal fact that produces
  each (D-03, D-06).
- All ten fixture projects have a complete source specification and a hand-derived expected edge set
  per master, written before any fixture exists (binding constraint #6).
- The repo-wide assertion census exists with a per-assertion verdict, a new expected value for every
  FLIPS row, and a numeric prediction 49-04 is measured against.
</success_criteria>

<output>
Create `.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-01-SUMMARY.md` when done
</output>
