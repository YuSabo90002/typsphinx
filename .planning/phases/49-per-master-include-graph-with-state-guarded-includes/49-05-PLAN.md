---
phase: 49-per-master-include-graph-with-state-guarded-includes
plan: 05
type: execute
wave: 4
depends_on: ["49-04"]
files_modified:
  - tests/test_include_ledger_removal_gate.py
  - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
autonomous: true
requirements: [COMP-05, COMP-06, COMP-11]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A committed structural gate proves the build-scoped include-dedup ledger is gone: a repo-wide search over the production package returns zero matches for its attribute name, and the gate fails loudly if any future change reintroduces it (COMP-11, SC#4)."
    - "The same gate proves the toctree visitor emits no unconditional include: every include call the visitor's emission loop produces is routed through the shared guard-rendering function, asserted structurally against the visitor's own source rather than against a build (COMP-11)."
    - "Exactly one spelling of the namespaced Typst state key exists in the production package, and it is the one the emission contract fixes; a second spelling anywhere is the drift class D-07's namespacing decision exists to reject (COMP-06, D-07)."
    - "Exactly one state publication line is emitted per wrapper file — asserted over a real multi-master build's emitted wrappers, so a duplicated publication (which would silently replace the first) cannot ship (COMP-06)."
    - "The assumption-delta contract test holds: the builder's include mapping is keyed by MASTER DOCNAME, and for the two-master fixture it carries at least two keys whose values are DIFFERENT. A future change that collapses the per-master graph back to one global include set makes those values equal and turns this test red. This is the promoted-plural identity made falsifiable."
    - "The SC#4 repo-wide sweep is recorded: every 'anywhere under X' claim this phase makes is checked by a repo-wide grep at verification time over the whole tree — production package, tests, fixtures, docs and examples — never against only the files a requirement happens to name (ROADMAP milestone invariant #4)."
    - "49-EVIDENCE.md records, per degenerate shape, the observed post-fix outcome next to the outcome DECIDED at plan time in 49-EXPECTED-STRUCTURE.md's outcome table, with an explicit match or divergence verdict per row. A divergence is reported as a finding, never resolved by amending the decided outcome after the fact (SC#2, D-06)."
    - "49-EVIDENCE.md records the standalone-content-file behaviour as measured: a content file compiled directly, outside any wrapper, sees the state default and therefore includes no children — the compile succeeds and produces only that document's own body. This is sane but user-visible, and it is handed to Phase 51 as a documentation obligation rather than left implicit."
    - "49-EVIDENCE.md records the two-layer output-shape change in user-visible terms — with the wrapper target no longer being the whole document and the body moving to the docname-named content file, now carrying state-guarded includes — as the handoff Phase 51 documents and Phase 52 announces in the CHANGELOG."
    - "No diagnostic Sphinx emits today was silently removed: for every Phase 49 fixture, the post-fix Sphinx warning list still contains every warning recorded in the pre-fix baselines, and the comparison is recorded per fixture."
    - "`uv run pytest` is green, `black --check`, `ruff check` and `mypy typsphinx/` all pass, and the phase adds no new runtime dependency, no new `typst_*` config value, and no fifth `@preview` package (ROADMAP binding constraint #7)."
  artifacts:
    - tests/test_include_ledger_removal_gate.py
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
  key_links:
    - "tests/test_include_ledger_removal_gate.py -> typsphinx/: the gate reads the production package's own source and a real build's emitted output, so the COMP-11 removal claim is asserted by a committed test rather than by a one-time grep in a summary."
    - "The assumption-delta contract test -> TypstBuilder's include mapping: the test's subject is the mapping's SHAPE (keyed by master, values differing), which is precisely the singular-to-plural generalization this phase performs, so a regression to one global set is caught structurally."
    - "49-EXPECTED-STRUCTURE.md `## Degenerate-shape outcome table` -> 49-EVIDENCE.md `## Degenerate-shape closure`: each observed outcome is recorded beside its plan-time decision with a match verdict, which is what makes SC#2's 'decided during planning rather than discovered as a test failure' auditable after the fact."
    - "49-EVIDENCE.md `## Handoff to Phase 51 and Phase 52` -> the two later phases: the standalone-compile behaviour and the output-shape change are named as documentation and CHANGELOG obligations rather than left as tribal knowledge."
  prohibitions:
    - "A COMP-12 corpus convergence failure must not be worked around: the corpus must not be narrowed, and the gate must not be skipped, marked flaky, or left unrun. A convergence failure is escalated to the owner per D-02 and ROADMAP binding constraint #5."
    - "No expected value anywhere in this phase may be derived from the new emitter's own output. Every changed expected value traces to a written-first derivation from the fixture's own conf.py and .rst read literally plus the emission contract."
    - "This phase must not trade one silent omission for another: no configuration may end up producing a PDF that is missing content its own toctree names, with no error and no warning, and no reintroduced write-time include decision may sit alongside the compile-time state guard."
    - "The `:numref:` numbering behaviour must not be silently changed, renumbered, or hidden. Both measured cases are recorded verbatim and handed forward to Phase 51 and Phase 52 per D-01."
---

<objective>
Turn this phase's removal and per-master-shape claims into committed, falsifiable tests, run the SC#4
repo-wide sweep, and record the degenerate-shape closure plus the two user-visible behaviours Phase 51
and Phase 52 owe their readers.

Purpose: COMP-11 is a removal requirement, and a removal asserted only in a summary regresses silently
the first time someone reaches for a convenient ledger again. SC#4 requires the removal to be verified by
repo-wide grep, and milestone invariant #4 requires "anywhere under X" claims to be checked over the whole
tree rather than the files a requirement names. SC#2 requires each degenerate shape's outcome to have been
decided at plan time — which is only auditable if the observed outcome is recorded beside the decision.
And the assumption-delta checkpoint asked for a contract test that goes red if a future phase reintroduces
the singular assumption; this plan is where that test lands.

Output: one new structural gate module carrying the removal assertions and the assumption-delta contract
test, and three new sections in `49-EVIDENCE.md`. No file under `typsphinx/` is touched by this plan.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-CONTEXT.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-RESEARCH.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-PATTERNS.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-VALIDATION.md
@.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files and identifiers **created** by Phase 49 (new — not drift; do not flag as unknown
symbols during source-grounding).

**New production symbols (all created in 49-04, all module-level in `typsphinx/translator.py`):**
`INCLUDE_STATE_KEY`, `make_include_edge_key`, `derive_master_edge_keys`,
`render_include_edge_state`, `render_include_guard`,
`TypstTranslator._toctree_entry_occurrences`, `TypstBuilder._build_include_edge_map`,
`TypstBuilder._master_include_edges`, and a new `edge_keys` keyword parameter on
`TypstWriter.render_wrapper`.

**New test modules:** `tests/test_state_guard_composition_gate.py`,
`tests/test_state_guard_shapes_gate.py`, `tests/test_include_edge_derivation_unit.py`,
`tests/test_include_ledger_removal_gate.py`, `tests/test_state_guard_numref_gate.py`

**New fixture projects under `tests/fixtures/`:** `state_guard_two_master_gate`,
`state_guard_mirror_pair_gate`, `state_guard_self_and_url_gate`, `state_guard_cycle_gate`,
`state_guard_selfref_gate`, `state_guard_glob_gate`, `state_guard_orphan_ref_gate`,
`state_guard_three_master_gate`, `state_guard_substring_key_gate`,
`state_guard_numref_two_case_gate`

**New planning artifacts:** `49-EVIDENCE.md`, `49-EXPECTED-STRUCTURE.md`, `49-RED-EVIDENCE.md`,
`49-SHAPES-RED-EVIDENCE.md`

**Symbols this phase DELETES:** the build-scoped include-dedup ledger attribute declared in
`TypstBuilder.init()`, its per-`write()` reset, and the toctree visitor's ledger lookup, membership
check, skipped-duplicate debug log and set-add write.

<!-- planner-discipline-allow: _included_docnames -->

## Edge coverage authored in this plan

Row 24 of the ledger in 49-01 is the COMP-11 `unclassified` probe row. Per the fallback protocol an
`unclassified` row is never auto-`backstop`ped: it stays **`unresolved`** and is surfaced here as an
explicit flagged assumption rather than silently dropped or converted into a truth. See
`## Flagged planner assumptions` below.

## Flagged planner assumptions

- **UNRESOLVED probe row 24 (COMP-11, `unclassified`).** The edge probe classified COMP-11 as
  `unclassified — review manually` and proposed no shape question. The planner's reading is that COMP-11
  is a pure removal requirement with no data or behaviour shape of its own: it has no input domain, no
  ordering, no encoding and no boundary, so the eight-category shape taxonomy has nothing to say about
  it. The removal is nonetheless covered behaviourally in this plan (the structural gate) and
  structurally in 49-04 (the deletion itself), so the unresolved classification does not leave the
  requirement unverified — only unclassified. **This row is deliberately left `unresolved` rather than
  converted to a backstop truth, and is surfaced here for a human reader to confirm or correct.**
- **The structural gate asserts against source text, which is a weaker instrument than a behavioural
  assertion.** A structural assertion over the visitor's own source can be defeated by a refactor that
  moves the emission out of that function. The gate therefore pairs each structural assertion with a
  behavioural one wherever a behavioural form exists (the emitted wrapper's publication count, the
  mapping's per-master shape), and the module's docstring states which assertions are structural and why.
- **The no-lost-diagnostic comparison depends on the pre-fix baselines being complete.** 49-02 and 49-03
  recorded each fixture's FULL Sphinx warning list, not only the warnings their own sections cared about,
  specifically so this comparison is possible. If a baseline turns out to have been partial, record that
  as a finding rather than narrowing the comparison to whatever the baseline happens to contain.

<tasks>

<task type="auto">
  <name>Task 1: Author tests/test_include_ledger_removal_gate.py — the COMP-11 removal gate and the assumption-delta contract test</name>
  <files>
    tests/test_include_ledger_removal_gate.py
  </files>
  <read_first>
    - tests/test_master_include_set_predicate_gate.py (the whole module — the closest existing precedent for a test that inspects builder internals structurally rather than through a compiled artifact, and the module whose own history shows how a structural claim drifts if it is not asserted)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md `## Emission contract` (the single permitted state-key spelling and the publication line's shape)
    - typsphinx/translator.py as it stands after 49-04 — the five new module-level derivation functions and the rewritten toctree visitor (the gate's structural assertions read this file)
    - typsphinx/builder.py as it stands after 49-04 — the include-mapping attribute, its single derivation method, the write-phase assignment and the lazy initialisation in the shared per-document write path
    - tests/test_pdf_render_gate.py lines 130-200 (the fixtures-dir fixture and the `sys.executable -m sphinx` build helper)
    - tests/test_state_guard_composition_gate.py as it stands after 49-04 (so the new gate does not duplicate assertions the composition gate already owns)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-CONTEXT.md `<code_context>` "Integration Points" (the enumeration of the deleted ledger's exactly one reader, one writer, declaration and reset — the reason a single change could achieve a repo-wide grep of zero)
  </read_first>
  <precondition>49-04 has landed and the fast suite is green on this tree: `uv run pytest -m "not slow" -q` exits 0.</precondition>
  <action>
Provision the worktree first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then
run every command through `uv run`. Never invoke Sphinx through the `uv` executable; use the running
interpreter's module form.

Author `tests/test_include_ledger_removal_gate.py`. Its docstring names COMP-11 and SC#4, states which
of its assertions are structural (reading the production package's own source) and which are
behavioural (reading a real build's emitted output), and explains why the structural ones exist anyway: a
removal asserted only once in a summary regresses silently, and this phase's whole subject is a
mechanism that fails without any diagnostic.

Tests:

1. **The ledger is absent from the production package.** Walk every Python file under the production
   package and assert none contains the deleted attribute's name. Implement the walk in Python over the
   package directory rather than shelling out, so the test is portable to the Windows CI lanes. Include
   the attribute name in the test as a single module-level constant with a comment stating it is the
   deleted symbol, so the test's own text is the only place the name appears.
2. **The ledger is absent from the repository's own prose.** The same walk extended over the tests
   directory, the fixtures tree, the docs source tree and the examples tree — the repo-wide form
   milestone invariant #4 requires — excluding the planning directory, whose history legitimately records
   the deleted symbol. State that exclusion explicitly in a comment.
3. **The toctree visitor emits no unconditional include.** Extract the visitor's own function body from
   the translator module's source, collect every non-comment line that contains an include call, and
   assert every such line is produced through the shared guard-rendering function or the surviving
   relative-path helper. Assert also that the visitor's body contains no membership test against a
   builder attribute — the shape of the deleted dedup read.
4. **Exactly one state-key spelling exists.** Assert the production package contains the contract's state
   key string, and that the total number of distinct string literals in the package that are passed as the
   first argument to a Typst state call is exactly one. Implement this by collecting the literals rather
   than by counting occurrences of one known string, so a second spelling is detected rather than assumed
   absent.
5. **Exactly one publication line per wrapper.** Build the three-master fixture for real, locate every
   emitted wrapper file, and assert each contains exactly one state publication line and that the
   publication line precedes that wrapper's content include line with nothing between them.
6. **The assumption-delta contract test.** Build the two-master fixture through the builder's own write
   phase, then assert on the include mapping directly: it is keyed by master docname; it has at least two
   keys for this fixture; and the values under those two keys are DIFFERENT. Add a docstring stating
   exactly what a failure means — that the per-master include graph has collapsed back to one global
   include set, which is the singular assumption this phase promoted away from and the failure mode
   defect A grew out of. Assert also that no attribute on the builder holds a single flat set of
   docnames spanning masters, by asserting the mapping's values are tuples of edge keys rather than a
   union of docnames.
7. **The mapping is not mutated during writing.** Derive the mapping, snapshot it, run the write phase
   over every document of the two-master fixture, and assert the mapping is unchanged — the property the
   deleted ledger did not have, because it was mutated inside the toctree visitor while documents were
   being written.

Mark any test that needs a real Typst compile with the project's slow marker; the structural tests must
stay in the fast suite so a regression is caught on every commit. Do not modify any file under the
production package.
  </action>
  <verify>
    <automated>uv run pytest tests/test_include_ledger_removal_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_include_ledger_removal_gate.py -q` exits 0 with zero failures.
    - The gate genuinely fails on a reintroduction: temporarily add the deleted attribute name to a scratch line in a production module, confirm the gate goes red, then revert. Record the red output in the task summary — a removal gate that cannot go red is not a gate.
    - The module contains at least seven test functions, each with a docstring naming its requirement id.
    - The repo-wide walk excludes only the planning directory: the module names that exclusion in a comment and the walk covers the production package, tests, fixtures, docs source and examples.
    - The walk is implemented in Python rather than by shelling out: `grep -Ec 'subprocess.*grep' tests/test_include_ledger_removal_gate.py` returns 0.
    - The state-key test collects literals rather than counting one known string: the module contains no assertion whose only subject is an occurrence count of the contract's state key.
    - The assumption-delta test asserts the mapping's values DIFFER for the two-master fixture, and its docstring states what a failure means in terms of the collapsed global include set.
    - The module invokes Sphinx through the running interpreter: `grep -c 'uv", "run' tests/test_include_ledger_removal_gate.py` returns 0 and `grep -c 'sys.executable' tests/test_include_ledger_removal_gate.py` returns at least 1.
    - `uv run black --check tests/test_include_ledger_removal_gate.py` exits 0 and `uv run python -m ruff check tests/` exits 0.
    - `uv run pytest -m "not slow" -q` exits 0.
    - `git status --porcelain typsphinx/` prints nothing.
  </acceptance_criteria>
  <done>A committed gate asserts the ledger's absence from both the production package and the repository's prose, asserts the visitor emits no unconditional include, asserts a single state-key spelling and a single publication per wrapper, and carries the assumption-delta contract test whose failure means the per-master graph collapsed back to one global set — and the gate is proven able to go red.</done>
</task>

<task type="auto">
  <name>Task 2: Run the SC#4 repo-wide sweep and record it, together with the no-lost-diagnostic comparison, in 49-EVIDENCE.md</name>
  <files>
    .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
  </files>
  <read_first>
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md as it stands (written by 49-01; this task APPENDS a new section and must not overwrite the state-syntax measurement)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md `## Assertion census` and its `## How to find any assertion I missed` subsection (the exact reproducible grep command lines this sweep re-runs to prove the enumeration is still complete)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-SHAPES-RED-EVIDENCE.md (the per-fixture pre-fix Sphinx warning baselines this task compares against)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-RED-EVIDENCE.md (the two-master and mirror-pair baselines, including their warning lists)
    - .planning/ROADMAP.md lines 396-411 (binding constraints #7, #8 and #9 verbatim: zero new runtime dependencies, four `@preview` packages with no new version-lockstep site, no new `typst_*` config value, every phase closes green, "anywhere under X" checked by repo-wide grep, and the two forbidden opportunistic changes)
    - tests/test_preview_version_sync.py (the existing gate that pins the four `@preview` package versions across every declaring surface — the instrument for the no-fifth-package half of binding constraint #7)
    - typsphinx/__init__.py (the config-value registrations — the instrument for the no-new-config-value half of binding constraint #7)
  </read_first>
  <action>
Append a `## Removal and invariant sweep` section to `49-EVIDENCE.md`. Do not overwrite the existing
`## State-syntax measurement` section; append after it.

Run the sweep over the WHOLE tree, not over the files a requirement happens to name — milestone invariant
#4 makes that the standing rule and this phase's SC#4 restates it. Paste each command line and its
verbatim output. Cover at minimum:

- The deleted ledger attribute's name, over the production package alone (must be empty), then over the
  production package plus tests, fixtures, docs source and examples (must be empty), then over the
  planning directory (expected non-empty — the history legitimately records it, and recording that
  distinction is what stops a future reader misreading the planning hits as a live reference).
- Every include call inside the toctree visitor's own body, showing each is guard-routed.
- Every distinct state-key literal in the production package, showing exactly one.
- The `@preview` package count across every declaring surface — the writer module, the template engine,
  the bundled template and the examples tree — showing the count is still four and that no new
  version-lockstep site was introduced. Cite the existing sync gate as the enforcing instrument and note
  that this phase added no new surface for it to watch.
- The registered config values, showing no new `typst_*` value was added.
- The runtime dependency set, showing no new runtime dependency was added.
- The two forbidden opportunistic changes: no typing-import modernization anywhere in this phase's diff,
  and no link-check job added. Show the diff-scoped commands that prove it.
- The census's own `## How to find any assertion I missed` commands, re-run, showing the enumeration is
  still complete after the migration.

Then add a `## No lost diagnostics` subsection. For every Phase 49 fixture, rebuild it and capture the
full Sphinx warning list, then compare it item by item against the pre-fix baseline recorded in
`49-RED-EVIDENCE.md` or `49-SHAPES-RED-EVIDENCE.md`. Record a table with one row per fixture: the
baseline warning count, the post-fix warning count, and an explicit verdict — every baseline warning
still present, or a named warning that disappeared. A disappeared warning is a FINDING and must be
reported as such, with the mechanism that removed it, rather than accepted because the build is green.
Note in the section's prose that new warnings appearing post-fix are not automatically a problem (the
duplicated-entry warning, for instance, is Sphinx's own and must still fire), but a DISAPPEARED warning
is, because this phase's whole subject is a class of failure that produces no diagnostic.

Close the section by stating, in one paragraph, which of the phase's success criteria this sweep
discharges and which remain owed to 49-06.
  </action>
  <verify>
    <automated>uv run python -c "
import pathlib,subprocess,sys
p=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md')
t=p.read_text(encoding='utf-8')
need=['## State-syntax measurement','## Removal and invariant sweep','## No lost diagnostics']
missing=[n for n in need if n not in t]
r=subprocess.run(['grep','-rn','_included_docnames','typsphinx/'],capture_output=True,text=True)
if r.stdout.strip(): missing.append('ledger still present in typsphinx/')
print(missing); sys.exit(1 if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `49-EVIDENCE.md` contains all three headings: the original `## State-syntax measurement` (not overwritten), plus the new `## Removal and invariant sweep` and `## No lost diagnostics`.
    - The sweep records the ledger grep at three scopes — production package, whole tree excluding planning, and planning only — with the first two empty and the third explicitly annotated as legitimate history.
    - Behaviour: `grep -rn '_included_docnames' typsphinx/ tests/ docs/ examples/` returns no matches; the verify command asserts the production-package half.
    - The sweep records exactly one distinct state-key literal in the production package, shown as collected literals rather than as a count of one known string.
    - The sweep records the `@preview` package count as four across every declaring surface and states that no new lockstep site was added: `uv run pytest tests/test_preview_version_sync.py -q` exits 0, and that result is pasted in the section.
    - The sweep records that no new `typst_*` config value was registered and no new runtime dependency was added, each with the command line that proves it.
    - The sweep records the two forbidden opportunistic changes as absent, with diff-scoped commands.
    - The census's `## How to find any assertion I missed` commands are re-run and their verbatim output is pasted, showing the enumeration is still complete.
    - The `## No lost diagnostics` table has one row per Phase 49 fixture with a baseline count, a post-fix count and an explicit verdict; any disappeared warning is labelled a finding with its mechanism named.
    - The section closes by naming which success criteria the sweep discharges and which remain owed to 49-06.
    - `git status --porcelain typsphinx/ tests/` prints nothing.
  </acceptance_criteria>
  <done>The SC#4 repo-wide sweep is recorded verbatim at every scope the milestone's invariants require, the four standing invariants of binding constraint #7 are re-measured and shown intact, and every fixture's post-fix warning list is compared item by item against its recorded pre-fix baseline with an explicit verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Record the degenerate-shape closure, the standalone-content-file behaviour, and the Phase 51 / Phase 52 handoff in 49-EVIDENCE.md</name>
  <files>
    .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md
  </files>
  <read_first>
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md as it stands after Task 2 (this task APPENDS; it must not overwrite either earlier section)
    - .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md `## Degenerate-shape outcome table` (the plan-time decisions each observed outcome is recorded beside)
    - tests/test_state_guard_shapes_gate.py as it stands after 49-04 (the gate whose passing assertions are the observed outcomes)
    - .planning/PROJECT.md lines 134-146 (the known residual risk, the standalone-content-file behaviour Phase 51 documents, and the user-visible output-shape change Phase 52 announces)
    - .planning/ROADMAP.md the Phase 51 and Phase 52 entries (so the handoff names the requirement each obligation lands against rather than describing it loosely)
    - .planning/phases/48-compile-time-cross-reference-guard/48-EVIDENCE.md (the artifact-shape precedent for a phase evidence file carrying several appended measurement sections plus an accepted-limit section)
  </read_first>
  <action>
Append two sections to `49-EVIDENCE.md`, after the sections 49-01 and Task 2 wrote.

**`## Degenerate-shape closure`.** One row per shape in the same order as
`49-EXPECTED-STRUCTURE.md`'s outcome table, with four columns: the shape, the outcome DECIDED at plan
time, the outcome OBSERVED post-fix, and a verdict of match or divergence. Take each observed outcome
from the shapes gate's passing assertions and paste the concrete observation the assertion made (a
marker count, a published key list, a resolved heading level) rather than restating the assertion's
name. Cover all seven rows: the 2-node cycle, the self-referencing toctree, the `self` and
external-URL entries, the `:glob:` toctree, the orphan reference, the three-master overlap, and the
duplicate entry inside one directive.

A divergence is a FINDING. Record it as one, naming the shape, the decided outcome, the observed
outcome and the mechanism responsible — and do NOT amend the decided outcome to match the observation.
SC#2's requirement is that each outcome was decided during planning rather than discovered as a test
failure, and rewriting a decision after the measurement would make that criterion unverifiable. If every
row matches, say so explicitly and state that SC#2's decided-in-advance requirement is therefore
discharged on the record rather than by assertion.

**`## Handoff to Phase 51 and Phase 52`.** Record the two user-visible behaviours this phase creates,
each as a numbered obligation naming the phase and the requirement it lands against:

1. **The standalone-content-file behaviour.** Measure it rather than assert it: take one content file
   from a real build of the two-master fixture, compile it directly with no wrapper, and paste the
   verbatim outcome — the compile succeeds and only that document's own body appears, because the state
   default is the empty array and every guard is therefore false. State the user-facing consequence in one
   sentence: a markup-builder user should compile the wrapper, not a content file, and a content file
   compiled alone is a valid document that simply contains no children. Name this as Phase 51's
   documentation obligation.
2. **The two-layer output-shape change, in its now-complete form.** State what a reader of the emitted
   output sees: the wrapper target is no longer the whole document but a template application plus a state
   publication plus one include, and the body lives in the docname-named content file, which now carries
   state-guarded includes at each toctree's own position. Note that Phase 47 introduced the split and this
   phase completed it by moving the include decision into the wrapper's published state. Name this as
   Phase 51's documentation obligation and Phase 52's CHANGELOG obligation, and note that PROJECT.md
   already asks for it to be explained together with the earlier default-derivation rename.

Also record, as a third numbered item, that the `:numref:` measurement and its fix-or-document decision
are owed by 49-06 under D-01 and are NOT discharged here — so a reader of this artifact cannot mistake
the handoff list for complete.

Close by confirming the phase's standing green bar for this plan: paste the verbatim results of the full
test suite, the formatter check, the linter check and the type check.
  </action>
  <verify>
    <automated>uv run python -c "
import pathlib,sys
t=pathlib.Path('.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EVIDENCE.md').read_text(encoding='utf-8')
need=['## State-syntax measurement','## Removal and invariant sweep','## No lost diagnostics','## Degenerate-shape closure','## Handoff to Phase 51 and Phase 52']
missing=[n for n in need if n not in t]
sec=t.split('## Degenerate-shape closure')[1].split('## Handoff')[0]
rows=[l for l in sec.splitlines() if l.strip().startswith('|') and '---' not in l]
if len(rows)<8: missing.append('degenerate closure table has fewer than 7 shape rows')
print(missing); sys.exit(1 if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `49-EVIDENCE.md` contains all five headings, with none of the earlier sections overwritten.
    - The degenerate-shape closure table has one row per shape (at least seven), each carrying the decided outcome, the observed outcome and a match-or-divergence verdict.
    - Every observed outcome is a concrete pasted observation — a marker count, a published key list or a resolved heading level — not a restatement of an assertion's name.
    - Any divergence is recorded as a finding with the responsible mechanism named, and no decided outcome in `49-EXPECTED-STRUCTURE.md` was amended after the measurement: `git diff --name-only HEAD -- .planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-EXPECTED-STRUCTURE.md` prints nothing for this plan.
    - The standalone-content-file behaviour is recorded as a verbatim measured outcome, not an assertion: the section contains a pasted compile result for a content file compiled with no wrapper.
    - The handoff section carries three numbered obligations, naming Phase 51, Phase 52 and the still-owed `:numref:` decision in 49-06, and names the requirement each lands against.
    - The section closes with verbatim results for the full test suite, the formatter check, the linter check and the type check.
    - `uv run pytest -q` exits 0, `uv run black --check .` exits 0, `uv run python -m ruff check .` exits 0, and `uv run python -m mypy typsphinx/` exits 0.
    - `git status --porcelain typsphinx/ tests/` prints nothing.
  </acceptance_criteria>
  <done>Every degenerate shape's observed outcome is recorded beside its plan-time decision with a verdict, the standalone-compile behaviour is measured rather than asserted, and the two user-visible changes are handed forward as named obligations against Phase 51 and Phase 52 with the still-owed `:numref:` item explicitly excluded from the handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production package source → the structural gate | The gate reads the production package's own source text as its subject; a refactor that relocates the emission can defeat a source-text assertion, which is why each structural assertion is paired with a behavioural one where a behavioural form exists |
| Emitted `.typ` → `typst.compile()` | The standalone-content-file measurement compiles a content file directly, outside any wrapper, exercising the state default path |

## STRIDE Threat Register

ASVS level 1, blocking threshold `high`. ASVS **V2 (Authentication)**, **V3 (Session Management)**,
**V4 (Access Control)** and **V6 (Cryptography)** are **structurally inapplicable** — typsphinx is a
local Sphinx/Typst document-generation tool with no network service, no authentication, no session and
no cryptographic surface. **V5 (Input Validation)** is the one real seam this phase introduces, and it is
mitigated in 49-04; this plan verifies the mitigation held. Recorded rather than omitted, per the phase's
security contract.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-02 | Tampering | A second spelling of the state key or of the edge key entering the production package later | low | mitigate | This plan's structural gate collects every state-key literal in the package and requires exactly one, so a second spelling is detected rather than assumed absent |
| T-49-08 | Repudiation | A removal claimed in a summary but not enforced by any test, regressing silently | low | mitigate | The removal becomes a committed gate that is proven able to go red, so the claim is falsifiable at every future commit rather than resting on a one-time grep |
| T-49-09 | Information Disclosure | A Sphinx diagnostic silently disappearing under the new mechanism, hiding a broken project from its author | medium | mitigate | Every fixture's post-fix warning list is compared item by item against its recorded pre-fix baseline, and a disappeared warning is reported as a finding rather than accepted because the build is green |
| T-49-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no packages of any kind. `49-RESEARCH.md` §Package Legitimacy Audit records no new PyPI dependency and no new `@preview` package; ROADMAP binding constraint #7 forbids both, and this plan re-measures both |
</threat_model>

<verification>
- `uv run pytest tests/test_include_ledger_removal_gate.py -q` exits 0, and the gate is demonstrated able
  to go red against a temporary reintroduction.
- `grep -rn '_included_docnames' typsphinx/ tests/ docs/ examples/` returns no matches.
- `uv run pytest tests/test_preview_version_sync.py -q` exits 0 and the `@preview` package count is still
  four with no new lockstep site.
- `uv run pytest -q` exits 0, `uv run black --check .` exits 0, `uv run python -m ruff check .` exits 0,
  and `uv run python -m mypy typsphinx/` exits 0.
- `49-EVIDENCE.md` carries five sections with none of the earlier ones overwritten.
- `git status --porcelain typsphinx/` prints nothing for the whole plan.
</verification>

<success_criteria>
- COMP-11's removal is enforced by a committed gate over both the production package and the
  repository's prose, and the gate is proven able to go red.
- The toctree visitor is asserted to emit no unconditional include, exactly one state-key spelling exists,
  and exactly one publication line is emitted per wrapper.
- The assumption-delta contract test exists and its failure means the per-master graph collapsed back to
  one global include set.
- The SC#4 repo-wide sweep is recorded at every scope milestone invariant #4 requires, and binding
  constraint #7's four standing invariants are re-measured intact.
- Every fixture's post-fix warning list is compared against its recorded baseline, with any disappeared
  warning reported as a finding.
- Every degenerate shape's observed outcome is recorded beside its plan-time decision with a verdict, and
  no decision was amended after the measurement.
- The standalone-compile behaviour and the completed output-shape change are handed forward as named
  Phase 51 and Phase 52 obligations, with the `:numref:` decision explicitly still owed to 49-06.
</success_criteria>

<output>
Create `.planning/phases/49-per-master-include-graph-with-state-guarded-includes/49-05-SUMMARY.md` when done
</output>
