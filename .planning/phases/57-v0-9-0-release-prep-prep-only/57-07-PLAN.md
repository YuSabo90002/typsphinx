---
phase: 57-v0-9-0-release-prep-prep-only
plan: 07
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md
autonomous: true
requirements: [REL-08]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC#3's multi-template claim is discharged by RE-RUNNING the existing permanent gate `tests/test_two_key_selection_gate.py` on the post-bump tree, not by authoring a new one (D-14) — the gate already drives a real `-b typstpdf` build over a fixture with three `typst_documents` entries across two registry keys."
    - "The gate's own assertion is a byte-inequality between the two produced PDFs; the two-way typographic difference lives in the fixture templates themselves, which differ in paper size and text size. That distinction is recorded rather than blurred, so the evidence does not overstate what the assertion proves."
    - "SC#3 asks literally for \"two differently-typeset PDFs\". A one-off measurement transcript reads each produced PDF's page geometry back and shows the two differ in the direction the fixture templates dictate — turning \"the bytes differ\" into \"they are typeset differently\". This is a TRANSCRIPT, not a new committed gate: no test module is added or modified (D-14)."
    - "The gate ran against the post-bump tree — the version read-back and the fixture build both happen in the same provisioned worktree — so this is a re-proof after the bump rather than an inherited result from the phase that authored the gate."
    - "The gate's run is recorded with zero skips: a skipped `typstpdf` build proves nothing about the multi-template claim, and the module's PDF-producing class is availability-gated."
    - "Nothing under `typsphinx/` or `tests/` changed in this plan; it runs, measures and records."
  artifacts:
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md"
      provides: "SC#3's goal-claim half — the existing gate's post-bump transcript plus a page-geometry read-back showing the two PDFs are typeset differently"
      contains: "D-14"
  key_links:
    - from: "tests/fixtures/two_key_selection_gate/'s conf.py"
      to: "two differently-typeset PDFs produced by one -b typstpdf build"
      via: "three typst_documents entries across two registry keys, whose two bundled templates deliberately differ in paper size and text size; the builder selects per entry via element [4], which is this milestone's headline feature"
      pattern: "typst_document_templates"
    - from: "the produced PDFs"
      to: "SC#3's \"differently typeset\" wording"
      via: "a pypdf page-geometry read-back run as a one-off transcript, because the committed gate asserts byte-inequality only and a byte comparison cannot judge typesetting"
      pattern: "pypdf"
  prohibitions:
    - statement: "No new test module, test class or test function is authored for the goal claim; D-14 discharges it by re-running the existing permanent gate."
      status: unverified
      verification: flagged
    - statement: "The existing gate module and its fixture are not edited — no assertion strengthened, weakened, renamed or removed."
      status: unverified
      verification: flagged
    - statement: "A pytest.skip is never reported as a pass; the gate's run must show zero skips or the outcome is recorded as unproven."
      status: unverified
      verification: flagged
    - statement: "No git tag named v0.9.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered, nothing is uploaded to PyPI, and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed."
      status: unverified
      verification: flagged
    - statement: "REL-08's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped."
      status: unverified
      verification: flagged
    - statement: "The page-geometry values are read from the produced PDFs at execution time, never transcribed from the fixture templates' source or from a planning document."
      status: unverified
      verification: flagged
  flagged_assumptions: []
---

<objective>
Re-prove this milestone's headline claim on the post-bump tree, on generated evidence: one
`-b typstpdf` build over a fixture whose `typst_documents` entries select two different registry keys
produces two PDFs that are typeset differently.

Purpose: discharges ROADMAP Phase 57 SC#3's goal-claim half. Unlike the previous release-prep phase,
which had to author a new gate for its goal claim, this milestone already ships a permanent one —
D-14 therefore requires a re-run rather than new authoring. The one thing the committed gate does
not do is judge typesetting: its assertion is a byte comparison, and the typographic difference is
baked into the fixture's two templates. This plan adds a one-off measurement transcript that reads
the produced PDFs' page geometry back, so SC#3's literal wording is supported by a measurement rather
than by an inference from the fixture source.

Output: `57-GOAL-CLAIM-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-level list, repeated in every plan of this phase so each is readable standalone.

**Source tree (modified files and new symbols):**

| Artifact | Plan | Shape |
|---|---|---|
| `pyproject.toml` line 7 | 57-01 | the `version` key's value becomes the string `0.9.0` |
| `README.md` line 347 | 57-01 | the Status line reads `**Status**: Stable (v0.9.0) - Production ready` |
| `uv.lock` | 57-01 | regenerated; its first-party `typsphinx` block carries the new version |
| `CHANGELOG.md` — new `## [0.9.0] - <date>` section (lead paragraph + `### Added` / `### Changed` / `### Fixed` / `### Removed` / `### Verified`) | 57-03 | new section; `## [Unreleased]` keeps only its `### Planned for Future Releases` list |
| `CHANGELOG.md` tail link block | 57-03 | one inserted `[0.9.0]:` release-tag line above the existing topmost release line, and the `[Unreleased]` compare base advanced |
| `tests/test_changelog_page_gate.py` — `RELEASE_VERSIONS` gains a `"0.9.0"` entry (14 → 15) and its count/range comment moves | 57-03 | tuple + comment edit only |
| `docs/source/changelog.rst` — new `Migrating from 0.8.x to 0.9.0` subsection under `Migration Guides` | 57-04 | new prose section, deliberately ungated (D-07) |
| `docs/source/installation.rst`, `docs/source/contributing.rst`, `docs/source/examples/advanced.rst`, `examples/basic/README.md`, `examples/advanced/README.md` | 57-04 | **conditional** — edited ONLY if the execution-time D-10 discovery grep returns a hit; commit `70e24958` already corrected all five, so the expected diff is empty |
| `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` | 57-09 | dated evidence annotation appended; the record **stays in `pending/`** by owner decision |

**No new todo record is created by this phase, and no new test module is authored** — D-14 discharges
the goal claim by re-running the gate that already exists.

**Planning artifacts:**

| Path (under `.planning/phases/57-v0-9-0-release-prep-prep-only/`) | Plan |
|---|---|
| `57-BUMP-EVIDENCE.md` | 57-01 |
| `57-CLOSEOUT-GUARD.md` | 57-01 (baseline) — re-verified by 57-08 and 57-09 |
| `COVERAGE.md` | 57-01 |
| `57-CI-EVIDENCE.md` | 57-02 (pre-bump check run) → 57-05 (post-bump authority run) |
| `57-CHANGELOG-EVIDENCE.md` | 57-03 |
| `57-MIGRATION-EVIDENCE.md` | 57-04 |
| `57-GREEN-TREE-EVIDENCE.md` | 57-06 |
| `57-GOAL-CLAIM-EVIDENCE.md` | 57-07 |
| `57-SC4-INVARIANTS.md` | 57-08 |
| `57-HANDOFF.md` | 57-09 |

**Reserved name — never produced:** `57-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: Re-run the existing multi-template gate on the post-bump tree and measure the two PDFs' page geometry</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md</files>
  <precondition>Plan 57-01 has merged back, so `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints `0.9.0` in this worktree. If it prints anything else, this is not a post-bump re-proof and the plan is running out of order.</precondition>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — the mandatory provisioning and `uv run` rule.
    - `tests/test_two_key_selection_gate.py` in full — the build fixture, the names of the produced PDF paths, every test in the module, and specifically the test whose assertion compares the two PDFs' bytes plus the docstring explaining why a byte comparison cannot judge "visibly different" rendering. READ ONLY; this module must not be edited.
    - `tests/fixtures/two_key_selection_gate/conf.py` — the `typst_document_templates` registry with its two keys and the three `typst_documents` entries that select between them.
    - `tests/fixtures/two_key_selection_gate/_typst/report/base.typ` and `tests/fixtures/two_key_selection_gate/_typst/memo/base.typ` — the two bundled templates and the specific `paper:` and `set text(size: ...)` values that make the two outputs differ. Note that neither declares an `@preview` import, deliberately, so this fixture creates no new version-lockstep site.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md` § "Pattern 5" — D-14's exact gate, the byte-inequality nuance, and the note that the planner owns whether an additional standalone transcript is worth the cost.
  </read_first>
  <action>
Provision first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`. Run everything
through `uv run`.

**Step 1 — confirm this is the post-bump tree.** Run
`uv run python -c "import typsphinx; print(typsphinx.__version__, typsphinx.__file__)"` and record it.
The version must be `0.9.0` and the path must lie inside this worktree. If not, STOP.

**Step 2 — re-run the permanent gate.** Run
`uv run pytest tests/test_two_key_selection_gate.py -v --junit-xml="${TMPDIR:-/tmp}/57-07-gate.xml"`
and capture the complete transcript plus the JUnit `testsuite` attributes. Every test must pass and
the skip count must be `0` — the PDF-producing class is availability-gated, so a skipped run proves
nothing about the multi-template claim and must be reported as unproven rather than as a pass. Do not
edit the module, do not add a test, do not force a marker.

**Step 3 — build the same fixture standalone and measure the geometry.** The committed gate asserts
the two PDFs' bytes differ; SC#3 asks for two *differently typeset* PDFs. Close that gap with a
one-off measurement, not a new gate.

Run `uv run sphinx-build -b typstpdf tests/fixtures/two_key_selection_gate "${TMPDIR:-/tmp}/57-07-build"`
and capture the build's own summary lines. Then locate the produced PDFs — read their target
filenames from the fixture's `typst_documents` configuration rather than assuming them — and read
each one's page geometry back with an inline `pypdf` invocation that prints, per file: the filename,
the page count, and page 1's mediabox width and height. Print the two rows side by side and add a
computed line stating whether the two geometries are equal or different.

The two bundled templates set different `paper:` values, so the two mediaboxes must differ. If they
come back equal, do NOT adjust the measurement to make the claim work — report it: either the
templates are no longer differing as the fixture intends, or the per-entry key selection is not
taking effect, and both are real findings that belong in the record rather than being smoothed over.

Also capture the emitted `.typ` tree with `find "${TMPDIR:-/tmp}/57-07-build" -type f -name '*.typ' | sort`,
so the per-key bundle directories under the output tree are visible alongside the PDFs — that is the
milestone's output rule made concrete.

This inline measurement is a **transcript, not a gate**. Do not write it into `tests/`, do not commit
a script for it, and do not add it to any test module: D-14 explicitly discharges the goal claim by
re-running the existing permanent gate, and the fixture, the module and the assertion all stay
exactly as they are.

Nothing under `typsphinx/` or `tests/` may change. Take no irreversible action.
  </action>
  <verify>
    <automated>uv run python -c "import typsphinx, sys; sys.exit(0 if typsphinx.__version__ == '0.9.0' else 1)" && uv run pytest tests/test_two_key_selection_gate.py -q --junit-xml="${TMPDIR:-/tmp}/57-07-gate.xml" && grep -q 'skipped="0"' "${TMPDIR:-/tmp}/57-07-gate.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/57-07-gate.xml" && grep -q 'errors="0"' "${TMPDIR:-/tmp}/57-07-gate.xml" && test -z "$(git diff --name-only -- typsphinx/ tests/)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import typsphinx; print(typsphinx.__version__, typsphinx.__file__)"` printed `0.9.0` and a path inside this worktree.
    - The gate module's JUnit `testsuite` element carries `skipped="0"`, `failures="0"` and `errors="0"`.
    - A standalone `uv run sphinx-build -b typstpdf` over the fixture completed, and its summary lines are captured.
    - Two PDFs were located by reading their target filenames from the fixture configuration, and a `pypdf` read-back printed each one's page count and page-1 mediabox dimensions, followed by an explicit equal/different line.
    - The two mediaboxes are recorded as different; if they are not, the discrepancy is reported as a finding rather than adjusted away.
    - The emitted `.typ` tree listing shows per-key bundle directories under the output tree.
    - `git diff --name-only -- typsphinx/ tests/` produces no output — no test module, test class, test function or fixture was added or edited.
    - No file was created under `tests/` or `scripts/` for the geometry measurement.
  </acceptance_criteria>
  <done>The existing permanent multi-template gate passes on the post-bump tree with zero skips, and a one-off page-geometry read-back shows the two produced PDFs differ in page size in the direction the fixture's two templates dictate — with no new gate authored and no existing one touched.</done>
</task>

<task type="auto">
  <name>Task 2: Write the goal-claim evidence record</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md</files>
  <read_first>
    - `.planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-GOAL-CLAIM-EVIDENCE.md` — the heading structure and register to match, noting that the previous phase had to author its gate while this one re-runs an existing one.
    - `.planning/PROJECT.md` § "Current Milestone: v0.9.0 per-document templates" — the goal sentence this evidence supports.
    - `.planning/ROADMAP.md` § "Phase 57" success criterion 3 — the literal wording "a real multi-template `-b typstpdf` build producing two differently-typeset PDFs".
  </read_first>
  <action>
Write `.planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md` in English with
the heading `# Phase 57 — Goal-Claim Evidence (SC#3, multi-template half)` and these sections:

- `## The claim` — quote ROADMAP Phase 57 SC#3's multi-template clause verbatim, and beside it the
  milestone goal sentence from `PROJECT.md`. One sentence stating what would have to be true for the
  claim to be false.

- `## D-14 — why no new gate was authored` — one short paragraph: the previous release-prep phase had
  to author a new gate for its goal claim; this milestone already ships a permanent one that drives a
  real `-b typstpdf` build over a fixture with three `typst_documents` entries across two registry
  keys, so D-14 discharges the claim by re-running it. State plainly that no test module, class,
  function or fixture was added or edited, and cite `git diff --name-only -- tests/` returning empty.

- `## Post-bump re-proof` — the version and `__file__` read-back, then the full
  `uv run pytest tests/test_two_key_selection_gate.py -v` transcript and its JUnit `testsuite`
  attributes, with the skip count stated as `0` and one sentence on why a skip here would not have
  been evidence.

- `## What the committed assertion does and does not prove` — one short paragraph recording, precisely,
  that the gate's assertion is a byte-inequality between the two produced PDFs, that the typographic
  difference is baked into the fixture's two bundled templates (quote their differing `paper:` and
  `set text(size: ...)` lines with their file paths and line numbers), and that a byte comparison
  cannot itself judge typesetting. Do not overstate the assertion.

- `## Page-geometry measurement (transcript, not a gate)` — the standalone `sphinx-build -b typstpdf`
  command and summary, the emitted `.typ` tree listing, and the `pypdf` read-back output showing each
  PDF's filename, page count and page-1 mediabox, followed by the explicit equal/different line. Add
  one sentence stating that this closes the gap between "the bytes differ" and SC#3's "differently
  typeset", and one stating that it is deliberately a transcript rather than a committed gate, per
  D-14.

- `## Division of authority` — two lines: this file carries the multi-template goal claim; the
  toolchain half lives in `57-CI-EVIDENCE.md` (plan 57-05) and `57-GREEN-TREE-EVIDENCE.md`
  (plan 57-06).

Never assert a command's result from memory — every section quotes a transcript produced in Task 1.
Where a result is genuinely unavailable, record `human_needed` honestly.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md && grep -q '^## D-14 — why no new gate was authored' .planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md && grep -q '^## What the committed assertion does and does not prove' .planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md && grep -q '^## Page-geometry measurement (transcript, not a gate)' .planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md && grep -q 'skipped="0"' .planning/phases/57-v0-9-0-release-prep-prep-only/57-GOAL-CLAIM-EVIDENCE.md && test -z "$(git diff --name-only -- typsphinx/ tests/ .planning/REQUIREMENTS.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `57-GOAL-CLAIM-EVIDENCE.md` exists, is written entirely in English, and contains the headings `## The claim`, `## D-14 — why no new gate was authored`, `## Post-bump re-proof`, `## What the committed assertion does and does not prove`, `## Page-geometry measurement (transcript, not a gate)`, and `## Division of authority`.
    - The `## Post-bump re-proof` section contains the literal `skipped="0"` and a version read-back printing `0.9.0`.
    - The `## What the committed assertion does and does not prove` section quotes both fixture templates' differing lines with file paths and line numbers, and states that the committed assertion is a byte comparison.
    - The `## Page-geometry measurement` section contains two PDF filenames, two page-1 mediabox readings, and an explicit line stating the two are different.
    - The `## Division of authority` section names `57-CI-EVIDENCE.md` and `57-GREEN-TREE-EVIDENCE.md`.
    - `git diff --name-only -- typsphinx/ tests/ .planning/REQUIREMENTS.md` produces no output.
  </acceptance_criteria>
  <done>SC#3's multi-template goal claim is on disk as generated evidence: the existing gate re-run post-bump with zero skips, an honest statement of what its byte-inequality assertion does and does not prove, and a page-geometry measurement closing the gap to "differently typeset" — with no new gate authored.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the produced PDFs → SC#3's published goal claim | This is the generated evidence the milestone's headline feature rests on; an overstated reading of the gate's assertion would publish an unproven claim. |
| the committed gate module and fixture → future regression detection | Editing either to make this phase's evidence easier would remove the permanent guard the previous phases installed. |
| a `pytest.skip` → a claim of proof | The PDF-producing class is availability-gated and skips rather than fails. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-37 | Repudiation | claiming "two differently-typeset PDFs" on the strength of a byte-inequality assertion alone | high | mitigate | The evidence file must state precisely what the committed assertion proves, and the page-geometry read-back supplies the typesetting measurement separately. |
| T-57-38 | Repudiation | a skipped gate reported as a pass | high | mitigate | `<verify>` and acceptance both assert `skipped="0"`; a prohibition forbids conflating a skip with a pass. |
| T-57-39 | Tampering | the permanent gate or its fixture edited to ease this phase's evidence | high | mitigate | `git diff --name-only -- tests/` asserted empty in `<verify>` and both tasks' acceptance; prohibitions name the gate module and fixture explicitly. |
| T-57-40 | Spoofing | evidence produced against the pre-bump tree and presented as a post-bump re-proof | medium | mitigate | The version and `__file__` read-back is a precondition and an acceptance criterion. |
| T-57-41 | Repudiation | geometry values copied from the fixture templates rather than read from the produced PDFs | medium | mitigate | A prohibition forbids transcribing them; acceptance requires the `pypdf` read-back output with filenames and page counts beside each reading. |
| T-57-SC | Tampering | npm/pip/cargo installs | low | accept | Only `uv sync --extra dev` runs, against the existing lock; `pypdf` and `typst-py` are already dev-extra dependencies. `57-RESEARCH.md` records the Package Legitimacy Gate as not applicable. |
</threat_model>

<verification>
- `uv run python -c "import typsphinx; print(typsphinx.__version__)"` prints `0.9.0` in this worktree.
- The gate module's JUnit result carries `skipped="0"`, `failures="0"` and `errors="0"`.
- A standalone `-b typstpdf` build produced two PDFs whose page-1 mediaboxes are read back and shown to differ.
- `git diff --name-only -- typsphinx/ tests/ .planning/REQUIREMENTS.md` produces no output.
</verification>

<success_criteria>
- SC#3's multi-template claim is re-proven on the post-bump tree by re-running the existing permanent gate, with zero skips (D-14).
- No new gate, test module, test function or fixture was authored or edited.
- The gap between the gate's byte-inequality assertion and SC#3's "differently typeset" wording is closed by a page-geometry measurement recorded as a transcript.
- The evidence states precisely what each observation does and does not prove.
</success_criteria>

<output>
Create `.planning/phases/57-v0-9-0-release-prep-prep-only/57-07-SUMMARY.md` when done.
</output>
