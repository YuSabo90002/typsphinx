---
phase: 57-v0-9-0-release-prep-prep-only
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/changelog.rst
  - docs/source/installation.rst
  - docs/source/contributing.rst
  - docs/source/examples/advanced.rst
  - examples/basic/README.md
  - examples/advanced/README.md
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md
autonomous: true
requirements: [REL-08]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/source/changelog.rst` carries a new `Migrating from 0.8.x to 0.9.0` subsection, inserted directly under the `Migration Guides` heading and above the existing `Migrating from 0.7.x to 0.8.0` section so the list stays most-recent-first (D-06). Before this plan the file contained zero occurrences of the new version string, while the release section's lead paragraph points readers at exactly this guide."
    - "The guide's \"before\" side is MEASURED, not derived from records: a second, disposable `git worktree add --detach` checkout at the prior release tag, provisioned with its OWN `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, produced a real `-b typst` build whose emitted file tree is transcribed verbatim into the evidence artifact and quoted in the guide's before-blocks (D-08)."
    - "The \"before\" build used the prior release's own `typsphinx`, not the current one: the second worktree's independent provisioning is recorded, and the emitted tree shows the OLD single shared template-file layout rather than the current per-key bundle directory layout — the direct falsification test for Pitfall 4."
    - "The same fixture built at HEAD in the executor's own worktree produces the \"after\" side, so the two code-blocks are the same configuration before and after, not two different configurations."
    - "The new guide is deliberately NOT bound by a test gate (D-07), carrying forward the line that historical release notes and migration guides record what was true at the version they document; the pre-existing 0.7.x→0.8.0 guide is equally unbound today."
    - "D-10, as amended, is discharged as VERIFICATION rather than as a fix: a repo-wide discovery grep re-run at execution time — never trusting the amendment's own hit set, which is itself only a floor — returns zero stale-prerequisite and zero dead-configuration-link hits outside `.planning/` and `CHANGELOG.md`'s historical entries, measured against the truth source `pyproject.toml`'s `requires-python` and `sphinx` pin. Commit `70e24958` is recorded as the closing evidence."
    - "If the execution-time grep surfaces a hit the amendment missed, fixing it IS in scope and is done here — prose only, in the same commit, with the hit and its fix recorded; the discovery is run-time and the amendment's five-file list is a floor, not a census (milestone invariants #4 and #11)."
    - "No version-sync gate is added for the prerequisites prose (D-11) — the owner declined a `tests/test_readme_version_sync.py`-shaped module reading `requires-python` and the `sphinx` pin and sweeping the published pages, accepting the recurrence risk knowingly after being shown its measured history."
    - "Nothing under `typsphinx/` changed in this plan, and no test module was added or modified."
  artifacts:
    - path: "docs/source/changelog.rst"
      provides: "the `Migrating from 0.8.x to 0.9.0` guide the release section's lead paragraph points at"
      contains: "Migrating from 0.8.x to 0.9.0"
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md"
      provides: "D-08's real before/after build transcripts, the second worktree's independent provisioning record, and D-10's execution-time discovery grep with its disposition"
      contains: "D-08"
  key_links:
    - from: "the `## [0.9.0]` lead paragraph's pointer (plan 57-03)"
      to: "the `Migrating from 0.8.x to 0.9.0` section in docs/source/changelog.rst"
      via: "the release section names the guide by title and the published page carries it; Phase 56 wrote no equivalent, so without this plan the pointer has no destination"
      pattern: "Migrating from 0\\.8\\.x to 0\\.9\\.0"
    - from: "the prior release tag's own source tree"
      to: "the guide's before-blocks"
      via: "a second `git worktree add --detach` checkout with its own uv provisioning and one real `sphinx-build -b typst` run — the project's established `checkout_pre_fix_translator` idiom, never a git-archive export and never the executor's own .venv"
      pattern: "git worktree add --detach"
    - from: "pyproject.toml's requires-python and sphinx pin"
      to: "every published prerequisites statement outside .planning/"
      via: "a repo-wide discovery grep run live at execution time; D-11 declined a machine gate, so this grep IS the whole verification and it must be run rather than inherited from the amendment's list"
      pattern: "requires-python"
  prohibitions:
    - statement: "No git tag named v0.9.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged."
      status: unverified
      verification: flagged
    - statement: "No test module is added or modified for the migration guide; D-07 declined a gate over it and the pre-existing guides stay equally unbound."
      status: unverified
      verification: flagged
    - statement: "No prerequisites version-sync gate is added; D-11 declined it and the recurrence risk is accepted knowingly."
      status: unverified
      verification: flagged
    - statement: "No historical migration guide section is edited; only the new section is added."
      status: unverified
      verification: flagged
    - statement: "The second worktree is removed after the before-build and no stray checkout is left behind; no branch is created for it."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed."
      status: unverified
      verification: flagged
    - statement: "REL-08's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped."
      status: unverified
      verification: flagged
    - statement: "No before-block content is reconstructed from planning documents, test assertions or memory; every line of it comes from the transcribed build output."
      status: unverified
      verification: flagged
  flagged_assumptions: []
---

<objective>
Write the `Migrating from 0.8.x to 0.9.0` guide from a real build at the prior release tag, and
discharge D-10 as verification against a repo-wide discovery grep run live.

Purpose: completes ROADMAP Phase 57 SC#2's documentation half. The release section's lead paragraph
points readers at this guide, and Phase 56 wrote no equivalent — so without this plan the pointer has
no destination. Because D-07 deliberately leaves the guide ungated, the only accuracy guarantee is
that its content was measured when written: D-08 therefore requires a second, disposable worktree at
the prior tag with its own provisioning, and one real `-b typst` build. Alongside it, D-10's already-
landed prerequisites correction is re-verified rather than re-done, with the grep re-run at execution
time because a written file list is a floor and never a census.

Output: the new guide section in `docs/source/changelog.rst`; `57-MIGRATION-EVIDENCE.md` with both
build transcripts and the D-10 disposition; and — only if the live grep finds a hit — a prose fix to
whichever published surface it names.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-level list, repeated in every plan of this phase so each is readable standalone.

**Source tree (modified files and new symbols):**

| Artifact | Plan | Shape |
|---|---|---|
| `pyproject.toml` line 7 | 57-01 | the `version` key's value becomes the string `0.9.0` |
| `README.md` line 347 | 57-01 | the Status line reads `**Status**: Stable (v0.9.0) - Production ready` |
| `uv.lock` | 57-01 | regenerated; its first-party `typsphinx` block carries the new version |
| `CHANGELOG.md` — new `## [0.9.0] - <date>` section (lead paragraph + `### Added` / `### Changed` / `### Fixed` / `### Removed` / `### Verified`) | 57-03 | new section; `## [Unreleased]` keeps only its `### Planned for Future Releases` list |
| `CHANGELOG.md` tail link block | 57-03 | one inserted `[0.9.0]:` release-tag line above the existing topmost release line, and the `[Unreleased]` compare base advanced |
| `tests/test_changelog_page_gate.py` — `RELEASE_VERSIONS` gains a `"0.9.0"` entry (14 → 15) and its count/range comment moves | 57-03 | tuple + comment edit only |
| `docs/source/changelog.rst` — new `Migrating from 0.8.x to 0.9.0` subsection under `Migration Guides` | 57-04 | new prose section, deliberately ungated (D-07) |
| `docs/source/installation.rst`, `docs/source/contributing.rst`, `docs/source/examples/advanced.rst`, `examples/basic/README.md`, `examples/advanced/README.md` | 57-04 | **conditional** — edited ONLY if the execution-time D-10 discovery grep returns a hit; commit `70e24958` already corrected all five, so the expected diff is empty |
| `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` | 57-09 | dated evidence annotation appended; the record **stays in `pending/`** by owner decision |

**No new todo record is created by this phase.** The 56-REVIEW filing already exists in
`.planning/todos/completed/`; the CONTEXT's "re-file the missing todo" item was retracted as a method
artifact (a content grep cannot see a slug that appears only in a filename).

**Planning artifacts:**

| Path (under `.planning/phases/57-v0-9-0-release-prep-prep-only/`) | Plan |
|---|---|
| `57-BUMP-EVIDENCE.md` | 57-01 |
| `57-CLOSEOUT-GUARD.md` | 57-01 (baseline) — re-verified by 57-08 and 57-09 |
| `COVERAGE.md` | 57-01 |
| `57-CI-EVIDENCE.md` | 57-02 (pre-bump check run) → 57-05 (post-bump authority run) |
| `57-CHANGELOG-EVIDENCE.md` | 57-03 |
| `57-MIGRATION-EVIDENCE.md` | 57-04 |
| `57-GREEN-TREE-EVIDENCE.md` | 57-06 |
| `57-GOAL-CLAIM-EVIDENCE.md` | 57-07 |
| `57-SC4-INVARIANTS.md` | 57-08 |
| `57-HANDOFF.md` | 57-09 |

**Reserved name — never produced:** `57-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: Build the guide's "before" side in a second, independently-provisioned worktree at the prior release tag, and the "after" side at HEAD</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md</files>
  <precondition>`git rev-parse v0.8.0^{commit}` resolves and `git merge-base --is-ancestor v0.8.0 HEAD` exits 0 — the prior release tag exists locally and is an ancestor of this branch, so a detached worktree at it is reachable. `uv` is on PATH.</precondition>
  <reversibility rating="reversible">A detached worktree is created and removed within the task; no branch, no commit, no remote interaction.</reversibility>
  <read_first>
    - `CLAUDE.md` § "Worktree-isolated execution" — the mandatory per-worktree provisioning rule and the reason behind it (the editable-install finder resolves `import typsphinx` to whichever checkout provisioned the `.venv`). This rule applies ONCE PER WORKTREE and must be applied a second time inside the tag checkout.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md` § "Pattern 3" and § "Pitfall 4" — the exact worktree mechanism, the fixture recommendation, and the failure mode this task exists to avoid.
    - `tests/roots/test-basic/conf.py` — the recommended fixture's configuration: a default `typst_documents` entry with no custom template, which exercises the OLD default shared-template layout at the tag and the NEW per-key bundle layout at HEAD.
    - `docs/source/user_guide/output_layout.rst` — the current output-layout contract the guide's closing cross-reference should point at, and the vocabulary to reuse for the "after" side.
  </read_first>
  <action>
Provision the executor's OWN worktree first:
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then run every command in this
worktree through `uv run`.

**Step 1 — the "after" side, from the current tree.** Pick the fixture. `tests/roots/test-basic/` is
the recommended candidate: confirm it first with
`git diff --stat v0.8.0..HEAD -- tests/roots/test-basic/` producing no output, which proves the same
source tree is being built on both sides. If it produces output, or if the guide's chosen
presentation needs more content than that fixture's single bare document provides, substitute a
different fixture and record the substitution and its own byte-identity check in the evidence file.

Build it: `uv run sphinx-build -b typst tests/roots/test-basic "${TMPDIR:-/tmp}/57-after-build"`, then
capture `find "${TMPDIR:-/tmp}/57-after-build" -type f | sort` and the full text of the emitted
top-level wrapper file. Record the build's own warning/summary lines too.

**Step 2 — the "before" side, in a SECOND worktree with its OWN provisioning.** This is the step the
whole decision rests on, and the one with a named failure mode.

Create the checkout: `git worktree add --detach "${TMPDIR:-/tmp}/typsphinx-v080-before" v0.8.0`. This
is a separate working directory carrying the prior release's own `typsphinx/` source tree. Then,
**inside that directory**, run `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`
again. `CLAUDE.md`'s rule applies once per worktree; the executor's own provisioning does not reach
into this one, and reusing the executor's `.venv` would build the "before" side with the CURRENT
code, producing a before-tree that is actually the after-behaviour and silently defeating D-08.

Prove the isolation before building: inside the tag checkout run
`uv run python -c "import typsphinx; print(typsphinx.__version__, typsphinx.__file__)"` and confirm
the printed path is under `${TMPDIR:-/tmp}/typsphinx-v080-before`, not under the executor's worktree.
Record that transcript — it is the falsification test for Pitfall 4. If the path points anywhere
else, STOP: the provisioning did not isolate and any build from here is worthless.

Then build the same fixture from that checkout:
`uv run sphinx-build -b typst tests/roots/test-basic "${TMPDIR:-/tmp}/57-before-build"` and capture
`find "${TMPDIR:-/tmp}/57-before-build" -type f | sort` plus the full text of the emitted top-level
wrapper file.

Clean up: `git worktree remove --force "${TMPDIR:-/tmp}/typsphinx-v080-before"` and confirm with
`git worktree list` that only the expected worktrees remain.

**Step 3 — sanity-check the two trees actually differ in the expected direction.** The before tree
must show the old layout — a single shared template file written at the output root — and the after
tree must show the per-key bundle directory under the output tree. If they look the same, the wrong
`.venv` was used; go back to Step 2 rather than writing the guide.

**Step 4 — start the evidence file.** Create
`.planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md` in English with the
heading `# Phase 57 — Migration Guide Evidence (D-06, D-07, D-08, D-10, D-11)` and these sections so
far:

- `## D-08 — the "before" side is a real build`, holding: the fixture choice and its byte-identity
  check; the `git worktree add --detach` command; the second worktree's own provisioning command;
  the `import typsphinx` isolation transcript with the printed path; the `sphinx-build` command and
  its summary lines; the `find ... | sort` output verbatim; and the emitted wrapper file's full text.
- `## The "after" side, built at HEAD`, holding the same four items for the current tree.
- `## Why a second worktree`, one short paragraph naming the failure mode explicitly: reusing the
  executor's own `.venv` would build the tag checkout's sources against the current editable install,
  so the "before" tree would show the current layout. Cite the isolation transcript as the check that
  this did not happen.

Do not write the guide prose yet — that is Task 2, and it must quote these transcripts rather than
reconstruct them. Nothing under `typsphinx/` may change. Take no irreversible action.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md && grep -q '^## D-08 — the "before" side is a real build' .planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md && grep -q 'git worktree add --detach' .planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md && grep -q 'typsphinx-v080-before' .planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md && test "$(git worktree list | grep -c 'typsphinx-v080-before')" = "0" && test -z "$(git diff --name-only -- typsphinx/)"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat v0.8.0..HEAD -- tests/roots/test-basic/` produced no output (or a substitute fixture's equivalent check is recorded with its own transcript).
    - `57-MIGRATION-EVIDENCE.md` contains the headings `## D-08 — the "before" side is a real build`, `## The "after" side, built at HEAD`, and `## Why a second worktree`.
    - The `## D-08` section contains an `import typsphinx` transcript whose printed `__file__` path lies under the second worktree's directory, and the printed version is the prior release, not `0.9.0`.
    - Both sections contain a `find ... -type f | sort` output block and the full text of an emitted top-level wrapper file.
    - The two recorded file trees differ: the before tree names a template file at the output root and the after tree names a path under an output-tree bundle directory.
    - `git worktree list` no longer lists the temporary checkout; `git branch --list` gained no branch.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>Two real `-b typst` builds of the same fixture — one from an independently-provisioned checkout at the prior release tag, one from the current tree — are transcribed verbatim, with the isolation of the second worktree proven by an `import typsphinx` path read rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Write the `Migrating from 0.8.x to 0.9.0` guide from the transcribed builds</name>
  <files>docs/source/changelog.rst</files>
  <precondition>`57-MIGRATION-EVIDENCE.md` carries both build transcripts from Task 1. Every before-block line in the guide comes from those transcripts; none may be reconstructed from records, test assertions or memory.</precondition>
  <read_first>
    - `docs/source/changelog.rst` lines 1-95 — the `.. include::` of the repo-root file, the `Migration Guides` heading, and the whole `Migrating from 0.7.x to 0.8.0` section (83 lines) as the shape to copy: a lead-in sentence explaining the block convention, one bullet per breaking change each with a before/after `code-block:: text` pair, one non-breaking item stated with "no action needed" prose, a closing disambiguation paragraph, and a closing `:doc:` cross-reference. Note the section that follows it, so the new section is inserted above both.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md` — Task 1's transcripts, which are the only permitted source for the before-block content.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md` § "`docs/source/changelog.rst`" — the quoted shape and the "no action needed" fourth-bullet pattern.
    - `docs/source/user_guide/configuration.rst` § "Removed Configuration Values" — the already-published migration sentence for the removed config value; the guide must agree with it.
    - `.planning/ROADMAP.md` § "Phase 57" preamble — the two breaking axes stated in the project's own words.
  </read_first>
  <action>
Insert a new `Migrating from 0.8.x to 0.9.0` subsection into `docs/source/changelog.rst`, directly
under the `Migration Guides` heading and ABOVE the existing `Migrating from 0.7.x to 0.8.0` section,
so the guides stay in most-recent-first order. Use the same `~~~~` underline style as its siblings,
with the underline at least as long as the title.

Follow the existing 0.7.x→0.8.0 section's shape:

1. **A lead-in sentence** stating how many breaking changes this release carries and explaining the
   block convention — that each item shows the `conf.py` fragment the reader has today, unchanged,
   and what it used to write versus what it writes now, with the two `code-block:: text` blocks
   labelled by version comment lines.

2. **One bullet per breaking change**, each `**Breaking:**`-prefixed and each carrying a before/after
   `code-block:: text` pair. The presentation order is yours; a defensible order is to lead with the
   change that stops a build loudest. The four are the same four the release section marks:
   - the output relocation — the single shared template file at the output root becomes a per-key
     bundle directory under the output tree, with the whole bundle copied wholesale; the migration is
     that a template referencing an asset by relative path must keep that asset in its own bundle
     directory;
   - the removed config value — the setting is now ignored with a warning, and the migration is to
     delete it, stated in agreement with the Removed Configuration Values table Phase 56 published;
   - the shadow-template route relocation — the migration is to move the file into the new
     source-tree subdirectory, and the bullet must say there is no build-time warning for it;
   - the pre-write template-layout validation — the three configurations that now stop the build, and
     what to change for each.

   **The before-blocks must be copied from Task 1's transcripts**, not written from the description
   above. Where a transcript line does not exist for a claim, do not invent one — either build for it
   or drop the claim.

3. **One non-breaking item stated with "no action is needed" prose** — the registry itself is
   additive: an untouched `conf.py` keeps working, and `typst_documents` element [4] only starts
   selecting a template once a project declares one.

4. **A closing disambiguation paragraph** distinguishing this release's relocation from the
   differently-shaped rename an earlier release performed, in the same spirit as the existing
   section's own "these two renames are easy to confuse" paragraph.

5. **A closing `:doc:` cross-reference** to the current output-layout contract page, matching the
   sibling section's closing line.

Do not edit any existing migration guide section. Do not add a test module for this guide: D-07
declined a gate over migration guides, and the pre-existing guides are equally unbound today —
`grep -rn "Migrating" tests/*.py` returns zero hits. Nothing under `typsphinx/` may change.
  </action>
  <verify>
    <automated>test "$(grep -c '^Migrating from 0\.8\.x to 0\.9\.0$' docs/source/changelog.rst)" = "1" && grep -n '^Migrating from' docs/source/changelog.rst | head -1 | grep -q '0\.8\.x to 0\.9\.0' && test "$(awk '/^Migrating from 0\.8\.x to 0\.9\.0$/{f=1;next} /^Migrating from 0\.7\.x to 0\.8\.0$/{f=0} f' docs/source/changelog.rst | grep -c 'code-block:: text')" -ge 8 && test -z "$(grep -rn 'Migrating' tests/ 2>/dev/null)" && uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -q && test -z "$(git diff --name-only -- typsphinx/ tests/)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^Migrating from 0\.8\.x to 0\.9\.0$' docs/source/changelog.rst` returns `1`, and `grep -n '^Migrating from' docs/source/changelog.rst | head -1` names that section — it is the first, most-recent-first.
    - The new section (extracted with `awk '/^Migrating from 0\.8\.x to 0\.9\.0$/{f=1;next} /^Migrating from 0\.7\.x to 0\.8\.0$/{f=0} f' docs/source/changelog.rst`) contains at least 8 `code-block:: text` directives — four before/after pairs.
    - The same extracted section contains exactly 4 lines matching `^- \*\*Breaking:\*\*`, plus at least one bullet containing the phrase `no action is needed`.
    - The same extracted section contains a `:doc:` role pointing at the output-layout page, and mentions `typst_documents` element [4].
    - Every `code-block:: text` before-block line in the new section also appears in `57-MIGRATION-EVIDENCE.md` — spot-check at least the emitted file paths from each before-block against the transcribed `find` output and record the check.
    - `git diff -U0 -- docs/source/changelog.rst | grep -c '^-'` returns `1` (the diff header only) — no existing line was removed.
    - `grep -rn "Migrating" tests/*.py` returns no output — no gate was added (D-07).
    - `git diff --name-only -- typsphinx/ tests/` produces no output.
  </acceptance_criteria>
  <done>The published changelog page carries a `Migrating from 0.8.x to 0.9.0` guide in most-recent-first position, with four before/after pairs whose before-blocks are quoted from a real build at the prior release tag, a "no action is needed" item for the additive registry, a disambiguation paragraph, and a closing cross-reference — and with no test gate added.</done>
</task>

<task type="auto">
  <name>Task 3: Discharge D-10 by re-running the discovery grep live, fixing any hit it surfaces, and recording the disposition</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md, docs/source/installation.rst, docs/source/contributing.rst, docs/source/examples/advanced.rst, examples/basic/README.md, examples/advanced/README.md</files>
  <read_first>
    - `pyproject.toml` lines 8-30 — `requires-python` and the `sphinx` pin. These two lines are the TRUTH SOURCE every published prerequisites statement must match; they are read here, never edited.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md` § D-10 including its AMENDED block, and § D-11 — why this is verification rather than a fix, and why no gate is added.
    - `.planning/todos/completed/2026-08-16-stale-version-prerequisites-and-dead-config-link-in-published-docs.md` — the filed record, including the four-file table of original hits, so the disposition can name what was fixed and by which commit.
    - `.planning/phases/56-per-document-template-documentation/56-REVIEW.md` — the two documentation findings this discharges.
  </read_first>
  <action>
This task is VERIFICATION, not a fix. The correction already landed on the live tree in commit
`70e24958`, across five files — two of them beyond the floor `57-CONTEXT.md` originally enumerated.
Your job is to prove it still holds, by measurement, at execution time.

**Step 1 — read the truth source.** Run `grep -n 'requires-python\|"sphinx>=' pyproject.toml` and
record the two lines verbatim. Every claim below is measured against these, not against any figure
written in a planning document.

**Step 2 — run the discovery grep repo-wide, live.** Do not derive the search set from the five files
the amendment names — that list is a floor, and the standing rule (milestone invariants #4 and #11)
is that an "anywhere under X" claim is settled by the grep, not by the enumeration. Run at least:

<!-- planner-discipline-allow: Python 3.9 -->
<!-- planner-discipline-allow: Sphinx 5.0 -->
<!-- planner-discipline-allow: docs/configuration.rst -->

- a stale-prerequisites sweep for `Python 3.9`, `Python 3.10`, `Python 3.11` and `Sphinx 5.0` through
  `Sphinx 8` shaped statements, across `*.rst`, `*.md` and `*.txt`, over the whole repository;
- a dead-link sweep for a `docs/configuration.rst` reference that does not resolve to a real file,
  and more generally for any `../../docs/` relative link from `examples/` whose target does not
  exist.

Exclude `.planning/` (planning prose deliberately quotes the stale strings, including this plan) and
`CHANGELOG.md`'s own historical entries (editing historical entries is explicitly out of scope). Show
the exclusion in the command itself — for example by piping through `grep -v '^\./\.planning/'` and
`grep -v '^\./CHANGELOG\.md'` — so the reader can see what was and was not searched. Paste every
command and its complete output.

**Step 3 — dispose of each hit.** If the sweep returns zero hits outside the excluded areas, record
that, and cite commit `70e24958` (`git show --stat 70e24958`, pasted) as the closing evidence:
the correction is landed, the todo record is in `.planning/todos/completed/`, and D-10 is discharged
by verification.

If the sweep returns a hit the amendment missed, **fixing it is in scope and is done here.** Replace
the stale prerequisite with the real floor read from Step 1's truth source (not a value copied from
any planning document), or repair the dead link to the path that actually exists. This is a prose fix
only — do not add a test, a gate or a sync module (D-11 declined exactly that, knowingly, after being
shown the drift's measured recurrence history). Re-run the sweep after the fix and paste the
now-empty result.

**Step 4 — record the disposition.** Append to
`.planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md`:

- `## D-10 — discovery grep, run live`, holding Step 1's truth-source lines, every Step 2 command with
  its complete output, and an explicit sentence naming what was excluded and why;
- `## D-10 — disposition`, with one line per hit found (or the sentence "zero hits outside the
  excluded areas"), the `git show --stat 70e24958` output as closing evidence, and one sentence
  stating that the amendment's five-file list was treated as a floor rather than a census;
- `## D-11 — no gate added`, one short paragraph recording that a `requires-python`/`sphinx`-pin
  sweeping test module was proposed and declined by the owner with the recurrence history on the
  table, and that the risk is accepted knowingly rather than overlooked. State plainly that this
  plan added no test module.

Nothing under `typsphinx/` may change. No test module may be added or modified. Take no irreversible
action.
  </action>
  <verify>
    <automated>test "$(grep -rn 'Python 3\.9\|Sphinx 5\.0' --include='*.rst' --include='*.md' --include='*.txt' . | grep -v '^\./\.planning/' | grep -v '^\./CHANGELOG\.md' | wc -l)" = "0" && grep -q '^## D-10 — discovery grep, run live' .planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md && grep -q '^## D-11 — no gate added' .planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md && grep -q '70e24958' .planning/phases/57-v0-9-0-release-prep-prep-only/57-MIGRATION-EVIDENCE.md && test -z "$(git diff --name-only -- typsphinx/ tests/)"</automated>
  </verify>
  <acceptance_criteria>
    - A repo-wide grep over `*.rst`, `*.md` and `*.txt`, excluding `.planning/` and `CHANGELOG.md`, returns zero stale-prerequisite hits — asserted by the `<verify>` command above and pasted in the evidence file with its exclusions visible.
    - `sed -n '270p' examples/advanced/README.md` (or the current location of that link, located by grep) names a path that `test -f` confirms exists.
    - `57-MIGRATION-EVIDENCE.md` contains the headings `## D-10 — discovery grep, run live`, `## D-10 — disposition`, and `## D-11 — no gate added`.
    - The `## D-10 — discovery grep, run live` section quotes `pyproject.toml`'s `requires-python` and `sphinx` pin lines verbatim as the truth source, and shows at least two distinct grep commands with their complete output.
    - The `## D-10 — disposition` section contains the output of `git show --stat 70e24958` and the sentence stating the amendment's file list was treated as a floor.
    - The `## D-11 — no gate added` section states that no test module was added; `git diff --name-only -- tests/` produces no output for this task's commit.
    - If any conditional prose fix was made, `git diff -U0` for it touches only `.rst`/`.md` prose lines and the replacement value matches the truth source read in Step 1.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>D-10 is discharged by a live repo-wide discovery grep that returns zero hits against the truth source, with commit `70e24958` cited as closing evidence, the amendment's file list explicitly treated as a floor, and D-11's declined gate recorded as a knowing acceptance rather than an omission.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the second worktree's `.venv` → the "before" build output | If the executor's own `.venv` is used, the tag checkout's sources are built against the current editable install and the guide documents the wrong "before". |
| `pyproject.toml`'s `requires-python` / `sphinx` pin → every published prerequisites statement | A published page claiming a lower floor sends readers into an immediate install failure at the moment the release ships. |
| the published migration guide → a user's working `conf.py` | The guide is the only migration instruction a reader who does not follow a link ever sees; D-07 leaves it ungated, so measurement at write time is the whole guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-18 | Spoofing | a "before" tree that is actually the after-behaviour, produced by the wrong `.venv` | high | mitigate | Task 1 provisions the tag checkout independently and proves it with an `import typsphinx` `__file__` path read, asserted in acceptance; Step 3 additionally requires the two trees to differ in the expected direction before any prose is written. |
| T-57-19 | Repudiation | guide content reconstructed from records rather than measured | high | mitigate | A prohibition forbids reconstructed before-blocks; acceptance requires each before-block's emitted file paths to be found in the transcribed `find` output. |
| T-57-20 | Information disclosure | published prerequisites announcing an unsupported floor at release time | high | mitigate | The D-10 sweep is re-run live against `pyproject.toml`'s own lines, the search set is derived by grep rather than from a written list, and `<verify>` asserts zero hits outside the excluded areas. |
| T-57-21 | Tampering | a stale amendment's file list accepted as a census, hiding a sixth surface | medium | mitigate | The action forbids deriving the search set from the amendment; acceptance requires the grep commands and their exclusions to be visible in the evidence file. |
| T-57-22 | Tampering | a test gate quietly added or removed under cover of documentation work | medium | mitigate | `git diff --name-only -- tests/` asserted empty; `grep -rn "Migrating" tests/*.py` asserted to return nothing; prohibitions name D-07 and D-11 by decision. |
| T-57-23 | Elevation of privilege | a `typsphinx/` source change riding in under a documentation plan | medium | mitigate | `git diff --name-only -- typsphinx/` asserted empty in all three tasks. |
| T-57-SC | Tampering | npm/pip/cargo installs | medium | accept | Both worktrees provision only from an existing lockfile; no package is added. The second worktree's `uv sync` resolves the PRIOR release's lock, which is a historical, already-shipped dependency set. `57-RESEARCH.md` records the Package Legitimacy Gate as not applicable. |
</threat_model>

<verification>
- `git worktree list` shows no leftover temporary checkout, and `git branch --list` gained no branch.
- The second worktree's `import typsphinx` transcript prints a path under that worktree, not under the executor's.
- The new guide section is first under `Migration Guides` and carries at least four before/after `code-block:: text` pairs, four `**Breaking:**` bullets and one "no action is needed" item.
- `grep -rn "Migrating" tests/*.py` returns no output.
- The repo-wide D-10 sweep returns zero hits outside `.planning/` and `CHANGELOG.md`.
- `git diff --name-only -- typsphinx/ tests/` produces no output.
</verification>

<success_criteria>
- `Migrating from 0.8.x to 0.9.0` is published in `docs/source/changelog.rst`, most-recent-first, with before-blocks quoted from a real build at the prior release tag (SC#2, D-06, D-08).
- The second worktree's isolation is proven by measurement rather than assumed (D-08, Pitfall 4).
- No test gate was added over the guide (D-07) and none over the prerequisites prose (D-11).
- D-10 is discharged as verification by a live repo-wide grep with commit `70e24958` cited as closing evidence, with any newly-surfaced hit fixed in scope.
</success_criteria>

<output>
Create `.planning/phases/57-v0-9-0-release-prep-prep-only/57-04-SUMMARY.md` when done.
</output>
