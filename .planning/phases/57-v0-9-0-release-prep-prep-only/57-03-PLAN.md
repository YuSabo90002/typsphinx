---
phase: 57-v0-9-0-release-prep-prep-only
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - tests/test_changelog_page_gate.py
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-CHANGELOG-EVIDENCE.md
autonomous: true
requirements: [REL-08]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`CHANGELOG.md` carries a curated `## [0.9.0]` section whose lead paragraph names the `typst_document_templates` registry as the headline, declares the release breaking in the second half of the same paragraph, and states what is NOT breaking — the registry is additive, so no existing `conf.py` needs editing (ROADMAP SC#2, D-04)."
    - "Exactly FOUR bullets inside the `## [0.9.0]` section carry a `**Breaking` marker (D-01): the two already written under `## [Unreleased]` (the `<srcdir>` shadow-template relocation, OUT-04; the pre-write template-layout validation, WR-01/CR-01) plus the two ROADMAP SC#2 names and that were absent from the file entirely — the `_template.typ` → `_template/<key>/<file>` output relocation and the `typst_template_assets` removal. Each carries its own migration sentence."
    - "The seven bullets that already existed under `## [Unreleased]` are promoted substantially as written, not compressed and not re-authored (D-02) — every one of the five `### Fixed` requirement IDs (XREF-05, BLD-07, BLD-08, BLD-09, IMG-03) and both existing `### Changed` bullets survive into `## [0.9.0]`, and `## [Unreleased]` is left holding only its `### Planned for Future Releases` list."
    - "The `typst_template_assets` removal is a `### Removed` bullet, not a fifth `### Changed` one (D-03), keeps its `**Breaking` prefix, gives its reason in one sentence, cross-references the output-relocation bullet, and states that a `config-inited` warning shim DOES exist — deliberately inverting `## [0.7.1]`'s `typst_authors` \"there is no deprecation shim\" clause — in agreement with the already-published Removed Configuration Values table."
    - "`### Verified` carries the same three items as 0.7.0 / 0.7.1 / 0.8.0, unchanged in wording (D-05); the built-wheel content check is NOT promoted to a fourth item — it is recorded in this phase's evidence artifacts instead."
    - "The tail link block is rolled over as a routine two-line edit: a `[0.9.0]` release-tag line inserted immediately above the existing topmost release line, and the `[Unreleased]` compare base advanced to the new tag. There is no missing historical entry to repair — the earlier CONTEXT claim to that effect was retracted, and the block's release lines stay in strictly descending order with `[Unreleased]` last (ROADMAP SC#2)."
    - "Edge probe (category: empty, REL-08) RESOLVED explicit: `scripts/extract_changelog_section.py 0.9.0` exits 0 and writes a NON-EMPTY body — the exact check `release.yml`'s `validate` job makes before any tag is ever pushed — while the same script exits non-zero for an absent version. The complementary empty case, `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` producing no output, is observed by plans 57-01, 57-08 and 57-09."
    - "`tests/test_changelog_page_gate.py`'s `RELEASE_VERSIONS` tuple gains a `\"0.9.0\"` entry and its count/range comment moves with it — appended only AFTER the `## [0.9.0]` heading exists, because the gate asserts every listed release appears in the built page and would otherwise go red."
    - "The published-page gate runs with the docs extra and reports zero skips, so the two build-driving classes actually executed rather than silently skipping on a missing `myst_parser`/`typst`."
    - "Nothing under `typsphinx/` changed in this plan; `54.1-REVIEW.md` WR-02 ships silent per D-09 — the CHANGELOG's \"validated before anything is written\" wording is kept unconditional and NO `### Known Limitations` section is added."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "the curated `## [0.9.0]` release section that becomes the GitHub Release body verbatim, plus the rolled-over tail link block"
      contains: "## [0.9.0]"
    - path: "tests/test_changelog_page_gate.py"
      provides: "the published-page coverage gate, extended to the new release"
      contains: "0.9.0"
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-CHANGELOG-EVIDENCE.md"
      provides: "SC#2's transcripts — the extractor run in both directions, the page-gate run with its skip count, and the structural census of the new section"
      contains: "SC#2"
  key_links:
    - from: "CHANGELOG.md's `## [0.9.0]` heading and body"
      to: "release.yml's `validate` job and its `create-release` job"
      via: "scripts/extract_changelog_section.py does positional extraction from the heading to the next `## [` heading and raises if the body is empty; `validate` fails the whole release before `build`/`publish-pypi` if it exits non-zero, and `create-release` calls it again to build the Release body"
      pattern: "extract_changelog_section"
    - from: "CHANGELOG.md"
      to: "docs/source/changelog.rst's published page"
      via: "the page `.. include::`s the repo-root file through myst_parser, so the release history propagates automatically (DOC-12) and only the gate's version tuple is hand-maintained"
      pattern: "include:: \\.\\./\\.\\./CHANGELOG\\.md"
    - from: "the `### Removed` bullet"
      to: "typsphinx/removed_config.py's REMOVED_CONFIG_VALUES warning string and configuration.rst's Removed Configuration Values table"
      via: "the shipped warning already says the value was removed in this release and explains that the whole bundle is copied wholesale, and Phase 56 published a migration sentence for it — the bullet must agree with both rather than derive a third reason"
      pattern: "typst_template_assets"
  prohibitions:
    - statement: "No git tag named v0.9.0 is created locally or pushed to any remote during this phase."
      status: unverified
      verification: flagged
    - statement: "No run of the release workflow is triggered — not by a tag push and not by workflow_dispatch."
      status: unverified
      verification: flagged
    - statement: "Nothing is uploaded to PyPI and no GitHub Release is created."
      status: unverified
      verification: flagged
    - statement: "No pull request is opened or merged."
      status: unverified
      verification: flagged
    - statement: "No historical CHANGELOG entry or historical migration guide is edited; only the Unreleased block, the new release section and the tail link block move."
      status: unverified
      verification: flagged
    - statement: "No `### Known Limitations` section is added to CHANGELOG.md and no GitHub issue is filed for the WR-02 confdir carve-out; D-09 declined both, and the reviewer's own recommended minimum remediation is declined with it."
      status: unverified
      verification: flagged
    - statement: "The `### Verified` section is not extended past its standing three items; the built-wheel content check stays in the evidence artifacts."
      status: unverified
      verification: flagged
    - statement: "No file under typsphinx/ is changed."
      status: unverified
      verification: flagged
    - statement: "REL-08's checkbox and its Traceability row in .planning/REQUIREMENTS.md are not flipped."
      status: unverified
      verification: flagged
    - statement: "No test gate is weakened — the page gate's assertions and skip guards are untouched; only the RELEASE_VERSIONS tuple and its adjacent comment move."
      status: unverified
      verification: flagged
    - statement: "No figure or quoted line is transcribed from 57-CONTEXT.md or 57-RESEARCH.md into the CHANGELOG or into an acceptance criterion; the Unreleased bullets are copied from CHANGELOG.md itself."
      status: unverified
      verification: flagged
  flagged_assumptions: []
---

<objective>
Author the curated `## [0.9.0]` release section, roll the tail link block over, and extend the
published-page gate to the new release.

Purpose: discharges ROADMAP Phase 57 SC#2. This section is not a document that later becomes the
GitHub Release body — it **is** that body, byte for byte, through
`scripts/extract_changelog_section.py`, so a reader who never follows a link must still get the
migration information from it. Unlike Phase 52, `## [Unreleased]` is not empty: it holds seven real
bullets written by Phases 54 / 54.1 / 55 immediately after each measured its own change, and D-02
promotes them substantially as written rather than re-authoring from requirements.

Output: `CHANGELOG.md` with a curated `## [0.9.0]` section and a rolled-over tail block;
`tests/test_changelog_page_gate.py` with the new release in its coverage tuple; and
`57-CHANGELOG-EVIDENCE.md`.
</objective>

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md
@.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-level list, repeated in every plan of this phase so each is readable standalone.

**Source tree (modified files and new symbols):**

| Artifact | Plan | Shape |
|---|---|---|
| `pyproject.toml` line 7 | 57-01 | the `version` key's value becomes the string `0.9.0` |
| `README.md` line 347 | 57-01 | the Status line reads `**Status**: Stable (v0.9.0) - Production ready` |
| `uv.lock` | 57-01 | regenerated; its first-party `typsphinx` block carries the new version |
| `CHANGELOG.md` — new `## [0.9.0] - <date>` section (lead paragraph + `### Added` / `### Changed` / `### Fixed` / `### Removed` / `### Verified`) | 57-03 | new section; `## [Unreleased]` keeps only its `### Planned for Future Releases` list |
| `CHANGELOG.md` tail link block | 57-03 | one inserted `[0.9.0]:` release-tag line above the existing topmost release line, and the `[Unreleased]` compare base advanced |
| `tests/test_changelog_page_gate.py` — `RELEASE_VERSIONS` gains a `"0.9.0"` entry (14 → 15) and its count/range comment moves | 57-03 | tuple + comment edit only |
| `docs/source/changelog.rst` — new `Migrating from 0.8.x to 0.9.0` subsection under `Migration Guides` | 57-04 | new prose section, deliberately ungated (D-07) |
| `docs/source/installation.rst`, `docs/source/contributing.rst`, `docs/source/examples/advanced.rst`, `examples/basic/README.md`, `examples/advanced/README.md` | 57-04 | **conditional** — edited ONLY if the execution-time D-10 discovery grep returns a hit; commit `70e24958` already corrected all five, so the expected diff is empty |
| `.planning/todos/pending/2026-08-11-ruff-generic-linux-elf-unrunnable-on-nixos.md` | 57-09 | dated evidence annotation appended; the record **stays in `pending/`** by owner decision |

**No new todo record is created by this phase.**

**Planning artifacts:**

| Path (under `.planning/phases/57-v0-9-0-release-prep-prep-only/`) | Plan |
|---|---|
| `57-BUMP-EVIDENCE.md` | 57-01 |
| `57-CLOSEOUT-GUARD.md` | 57-01 (baseline) — re-verified by 57-08 and 57-09 |
| `COVERAGE.md` | 57-01 |
| `57-CI-EVIDENCE.md` | 57-02 (pre-bump check run) → 57-05 (post-bump authority run) |
| `57-CHANGELOG-EVIDENCE.md` | 57-03 |
| `57-MIGRATION-EVIDENCE.md` | 57-04 |
| `57-GREEN-TREE-EVIDENCE.md` | 57-06 |
| `57-GOAL-CLAIM-EVIDENCE.md` | 57-07 |
| `57-SC4-INVARIANTS.md` | 57-08 |
| `57-HANDOFF.md` | 57-09 |

**Reserved name — never produced:** `57-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: Author the curated `## [0.9.0]` section — promote the seven existing bullets, add the three missing ones, mark four Breaking</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1-160 — the `## [Unreleased]` block in full (the two `### Changed` bullets and the five `### Fixed` bullets D-02 promotes, plus the `### Planned for Future Releases` list that stays behind), and `## [0.8.0]` in full as the structural model: lead paragraph → `### Added` / `### Changed` / `### Fixed` → `### Verified`.
    - `CHANGELOG.md`'s `## [0.7.1]` `### Removed` entry for `typst_authors` — the exact bullet model D-03 follows, including the "there is no deprecation shim" clause D-03 deliberately inverts. Locate it with `grep -n '^### Removed' CHANGELOG.md`.
    - `typsphinx/removed_config.py` around the `REMOVED_CONFIG_VALUES` dict — the shipped warning string for `typst_template_assets`, which already names this release and explains the wholesale bundle copy. READ ONLY; this file must not be edited. Note the three entries in that dict were removed in three DIFFERENT releases — only one of them belongs in this section.
    - `docs/source/user_guide/configuration.rst` § "Removed Configuration Values" — the migration sentence Phase 56 already published for `typst_template_assets`; the new bullet must agree with it, not derive a different reason.
    - `.planning/PROJECT.md` § "Current Milestone: v0.9.0 per-document templates" — the goal sentence D-04 makes the lead paragraph's headline, and the "one output rule, no exceptions" framing.
    - `.planning/ROADMAP.md` § "Phase 57" preamble and success criterion 2 — the two changes SC#2 names, and the sentence stating the registry itself is additive.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-PATTERNS.md` § "`CHANGELOG.md` — the `## [0.9.0]` section" — all five analogs with their quoted source text.
    - `.planning/phases/54.1-bundle-directory-safety-templates-path-collision-refusal-and/54.1-REVIEW.md` § WR-02 — the finding D-09 declines to disclose, so the decline is made knowingly rather than by omission.
  </read_first>
  <action>
Provision the worktree first: `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`, then
run every command through `uv run`.

Insert a new `## [0.9.0] - <today's date in YYYY-MM-DD>` section immediately above the existing
`## [0.8.0]` section heading, and leave `## [Unreleased]` in place above it holding ONLY its
`### Planned for Future Releases` list — its `### Changed` and `### Fixed` headings and all seven of
their bullets move down into the new section.

**Lead paragraph (D-04).** Name the registry as the headline: every `typst_documents` entry can now
name its own template, Typst Universe package and template-function arguments through the validated
`typst_document_templates` registry, instead of one globally-configured template being applied to
every master. In the second half of the same paragraph, declare the release breaking and point the
reader at the `Migrating from 0.8.x to 0.9.0` guide in the published documentation (plan 57-04 writes
that guide; refer to it by its exact title). Then state explicitly what is **not** breaking: the
registry is additive, so no existing `conf.py` needs editing. Follow `## [0.8.0]`'s lead-paragraph
register — declarative, no bullet list, one paragraph.

**Promote the seven existing bullets substantially as written (D-02).** Copy them from
`CHANGELOG.md` itself, not from any planning document. Edit only what promotion requires — remove
any framing that only made sense under an unreleased heading. They are longer than `## [0.8.0]`'s
longest bullet and that is deliberate: each was written immediately after the phase that measured it,
and compressing loses facts that exist nowhere else (for instance OUT-04's statement that there is no
build-time warning for that relocation, so the changelog entry is the only place it is announced).
Do not compress them to the 0.8.0 house length and do not re-author them from requirement text.

**Author the three missing bullets at the same granularity.**

1. *The registry itself* — an `### Added` bullet (section assignment is yours; `### Added` is the
   obvious home for an additive headline feature). State what a `conf.py` can now declare, that
   `typst_documents` element [4] is the registry key, that the built-in key is handled by the same
   rule rather than special-cased, and that an untouched `conf.py` keeps working. Attach the
   requirement IDs it closes in trailing parentheses, the house style since Phase 33.

2. *The output relocation* — a `**Breaking`-marked `### Changed` bullet. This is one of the two
   changes ROADMAP SC#2 names by hand, and it is currently written nowhere in `CHANGELOG.md`. State
   the old single shared template file at the output root, the new per-key bundle directory under
   the output tree, that every used key's bundle directory is copied wholesale rather than by an
   explicit asset list, and — as its migration sentence — that a template referencing an asset by
   relative path must now keep that asset in its own bundle directory. Attach its requirement IDs.

3. *The `typst_template_assets` removal* — a `### Removed` bullet, NOT a fifth `### Changed` one
   (D-03). `## [0.8.0]` has no `### Removed` section only because it had no candidate; a future
   reader seeing an absent `### Removed` concludes nothing was removed. Follow `## [0.7.1]`'s
   `typst_authors` bullet as the model: keep the `**Breaking` prefix, give the reason in one sentence
   (the whole bundle is copied wholesale, so no asset list is needed any more), cross-reference the
   output-relocation bullet in `### Changed`, and — inverting 0.7.1's closing clause — state that a
   `config-inited` warning shim DOES exist, so a `conf.py` that still sets the value warns rather
   than failing silently. Its reason and its migration advice must agree with the shipped warning
   string in `typsphinx/removed_config.py` and with the Removed Configuration Values table Phase 56
   published, rather than stating a third, independently-derived reason. Do not describe the other
   two entries of that dict as part of this release — they were removed in earlier releases.

**Breaking-mark census (D-01).** When you are done, exactly four bullets inside the `## [0.9.0]`
section carry a `**Breaking` marker: the two promoted ones and the two authored ones (#2 and #3
above). Each of the four states, in its own body, what a reader must do — that is the "migration
sentence" SC#2 requires. Do not fold the four into two axes to match the goal sentence's "two
independent axes" literally: burying "what stops your build" inside a longer bullet is what D-01
rejected.

**`### Verified` (D-05).** Reproduce the same three items 0.7.0, 0.7.1 and 0.8.0 all carry, wording
unchanged. Do not add a fourth item for the built-wheel content check — this section's standing
character is "here is what did *not* change", and its value comes partly from being comparable
across releases. Plan 57-08 supplies the hunk-level argument backing the first item; plan 57-06
records the wheel check.

**D-09 — WR-02 ships silent.** The promoted pre-write-validation bullet's claim that template layout
is validated before anything is written reads as unconditional. It is over-broad for a project using
`-c`/`--confdir`, because that validation resolves Sphinx's `templates_path` against the source
directory rather than the config directory. The owner decided, with the reviewer's own recommended
minimum remediation explicitly on the table, that this ships without a carve-out. **Do not add a
scoping clause, a `### Known Limitations` section, or a GitHub issue.** Plan 57-09 records the
decline in `57-HANDOFF.md`; your job here is to leave the wording alone.

Do not edit any historical section, any migration guide, or the tail link block in this task — the
tail block is Task 2. Nothing under `typsphinx/` may change.
  </action>
  <verify>
    <automated>uv run python scripts/extract_changelog_section.py 0.9.0 | tee "${TMPDIR:-/tmp}/57-03-body.txt" | head -1 && test -s "${TMPDIR:-/tmp}/57-03-body.txt" && test "$(awk '/^## \[0\.9\.0\]/{f=1;next} /^## \[0\.8\.0\]/{f=0} f' CHANGELOG.md | grep -c '\*\*Breaking')" = "4" && test "$(awk '/^## \[0\.9\.0\]/{f=1;next} /^## \[0\.8\.0\]/{f=0} f' CHANGELOG.md | grep -c '^### Removed$')" = "1" && test "$(awk '/^## \[Unreleased\]/{f=1;next} /^## \[0\.9\.0\]/{f=0} f' CHANGELOG.md | grep -c '^### ')" = "1" && test -z "$(git diff --name-only -- typsphinx/)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## \[0\.9\.0\] - [0-9]\{4\}-[0-9]\{2\}-[0-9]\{2\}$' CHANGELOG.md` returns `1`.
    - `uv run python scripts/extract_changelog_section.py 0.9.0` exits 0 and writes a non-empty body to stdout.
    - Inside the `## [0.9.0]` section (extracted with `awk '/^## \[0\.9\.0\]/{f=1;next} /^## \[0\.8\.0\]/{f=0} f' CHANGELOG.md`): `grep -c '\*\*Breaking'` returns exactly `4`; `grep -c '^### Removed$'` returns `1`; `grep -c '^### Verified$'` returns `1`; and the `### Verified` body has exactly 3 bullet lines.
    - The same extracted section contains every one of the literals `XREF-05`, `BLD-07`, `BLD-08`, `BLD-09`, `IMG-03`, `OUT-04`, `CONF-19`, `typst_document_templates`, and `typst_template_assets`.
    - The same extracted section contains the exact title `Migrating from 0.8.x to 0.9.0`.
    - The same extracted section contains the string `config-inited` (D-03's warning-shim statement) and does NOT contain the string `Known Limitations`.
    - Inside the residual `## [Unreleased]` block (extracted with `awk '/^## \[Unreleased\]/{f=1;next} /^## \[0\.9\.0\]/{f=0} f' CHANGELOG.md`): `grep -c '^### '` returns exactly `1`, and that heading is `### Planned for Future Releases`.
    - `git diff --stat -- CHANGELOG.md` shows deletions confined to the Unreleased block's promoted lines; `git diff -U0 -- CHANGELOG.md | grep -c '^-.*0\.7\.1\|^-.*0\.8\.0'` returns `0` — no historical section lost a line.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>A curated `## [0.9.0]` section exists whose lead paragraph names the registry, whose four `**Breaking` bullets each carry a migration sentence, whose `### Removed` bullet states the warning shim exists in agreement with the shipped warning string, and whose `### Verified` carries the standing three items unchanged — with `## [Unreleased]` reduced to its forward-looking list.</done>
</task>

<task type="auto">
  <name>Task 2: Roll over the tail link block and extend the published-page coverage tuple</name>
  <files>CHANGELOG.md, tests/test_changelog_page_gate.py</files>
  <precondition>`grep -c '^## \[0\.9\.0\]' CHANGELOG.md` returns 1 — Task 1's section exists. The `RELEASE_VERSIONS` append is invalid before that heading exists, because the page gate asserts every listed release appears in the built page and would go red on a missing heading.</precondition>
  <read_first>
    - `CHANGELOG.md`'s tail link-reference block, read in full with `tail -30 CHANGELOG.md` — the release lines in descending order with the current topmost line, and the `[Unreleased]` compare line last. **The block is complete**: it is not missing a historical release line, and an earlier CONTEXT claim to the contrary was retracted. Do not add a task to repair one.
    - `tests/test_changelog_page_gate.py` lines 40-70 — the count/range comment above `RELEASE_VERSIONS` and the tuple itself, plus the two consumers at the `missing = [v for v in RELEASE_VERSIONS ...]` lines that assert every listed release appears in the built HTML and in the PDF-extracted text.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-RESEARCH.md` § "Pitfall 3" — the recurring ordering dependency this task is sequenced against.
  </read_first>
  <action>
**Part A — the tail link block.** This is a routine two-line edit, not a repair.

<!-- planner-discipline-allow: compare/v0.8.0...HEAD -->

1. Insert a new line `[0.9.0]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0.9.0`
   immediately ABOVE the block's current topmost release line, so the release lines stay in strictly
   descending version order.
2. Change the `[Unreleased]` line's compare base so it reads
   `[Unreleased]: https://github.com/YuSabo90002/typsphinx/compare/v0.9.0...HEAD`. It stays the last
   line of the block.

Add exactly one line and change exactly one line. Do not reorder, deduplicate or reformat any other
line of the block, and do not add a second entry for a version that already has one.

**Part B — the published-page coverage tuple.** In `tests/test_changelog_page_gate.py`, append the
string `"0.9.0"` as the last element of `RELEASE_VERSIONS`, and update the comment above the tuple so
its release count and its version range both name the new release instead of the previous one. Change
nothing else in that file: no assertion, no skip guard, no fixture, no import.

Then run the gate with the docs extra so the two build-driving classes actually execute rather than
skipping:
`uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -v`. The skip count must be
`0`. A skip here is not evidence — the classes that build the HTML page and the PDF are gated on
`myst_parser` and `typst` availability and will silently skip under a `dev`-only environment,
producing a green run that proved nothing.

Nothing under `typsphinx/` may change. Take no irreversible action.
  </action>
  <verify>
    <automated>uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py -q --junit-xml="${TMPDIR:-/tmp}/57-03-page.xml" && grep -q 'skipped="0"' "${TMPDIR:-/tmp}/57-03-page.xml" && grep -q 'failures="0"' "${TMPDIR:-/tmp}/57-03-page.xml" && grep -q 'errors="0"' "${TMPDIR:-/tmp}/57-03-page.xml" && test "$(grep -c '^\[0\.9\.0\]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0\.9\.0$' CHANGELOG.md)" = "1" && test "$(grep -c '^\[Unreleased\]: https://github.com/YuSabo90002/typsphinx/compare/v0\.9\.0\.\.\.HEAD$' CHANGELOG.md)" = "1" && test -z "$(git diff --name-only -- typsphinx/)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\[0\.9\.0\]: https://github.com/YuSabo90002/typsphinx/releases/tag/v0\.9\.0$' CHANGELOG.md` returns `1`.
    - `grep -c '^\[Unreleased\]: https://github.com/YuSabo90002/typsphinx/compare/v0\.9\.0\.\.\.HEAD$' CHANGELOG.md` returns `1`.
    - `grep -c '^\[Unreleased\]: .*compare/v0\.8\.0\.\.\.HEAD$' CHANGELOG.md` returns `0` — the stale compare base is gone.
    - `grep -n '^\[0\.9\.0\]:' CHANGELOG.md` reports a line number exactly one less than `grep -n '^\[0\.8\.0\]:' CHANGELOG.md` — the new line sits immediately above the previous topmost release line.
    - `grep -c '^\[Unreleased\]:' CHANGELOG.md` returns `1` and `grep -n '^\[' CHANGELOG.md | tail -1` names the `[Unreleased]` line — it is still last in the block.
    - `git diff --numstat -- CHANGELOG.md` for this task's commit shows the tail-block change as exactly 2 added and 1 removed line beyond Task 1's section.
    - `grep -c '"0\.9\.0",' tests/test_changelog_page_gate.py` returns `1`, and the comment above `RELEASE_VERSIONS` names `0.9.0` and a release count one higher than before.
    - `git diff -- tests/test_changelog_page_gate.py` touches only lines inside the `RELEASE_VERSIONS` tuple and the comment block immediately above it — `git diff -U0 -- tests/test_changelog_page_gate.py | grep -c '^[+-]\s*\(def \|class \|assert \|import \|@pytest\)'` returns `0`.
    - The page-gate JUnit `testsuite` element carries `skipped="0"`, `failures="0"` and `errors="0"`.
    - `git diff --name-only -- typsphinx/` produces no output.
  </acceptance_criteria>
  <done>The tail link block carries a new release-tag line above the previous topmost one and an advanced `[Unreleased]` compare base with `[Unreleased]` still last; the page-coverage tuple names the new release; and the gate runs with zero skips, zero failures and zero errors.</done>
</task>

<task type="auto">
  <name>Task 3: Record SC#2's evidence — the extractor in both directions, the page gate, and a structural census of the new section</name>
  <files>.planning/phases/57-v0-9-0-release-prep-prep-only/57-CHANGELOG-EVIDENCE.md</files>
  <read_first>
    - `.planning/milestones/v0.8.0-phases/52-v0-8-0-release-prep-prep-only/52-BUMP-EVIDENCE.md` — the evidence-file register and heading discipline to match.
    - `scripts/extract_changelog_section.py` — its positional extraction rule and its non-zero exit on a missing heading or an empty body, so the two directions can be described accurately.
    - `.planning/phases/57-v0-9-0-release-prep-prep-only/57-VALIDATION.md` § "Manual-Only Verifications" — the editorial-quality row this evidence file supports but cannot replace.
  </read_first>
  <action>
Write `.planning/phases/57-v0-9-0-release-prep-prep-only/57-CHANGELOG-EVIDENCE.md` in English with the
heading `# Phase 57 — CHANGELOG Evidence (SC#2)` and these sections:

- `## SC#2 — the release body, in both directions`. Paste the full verbatim stdout of
  `uv run python scripts/extract_changelog_section.py 0.9.0` — this is exactly what the GitHub
  Release body will be, so it belongs in the record in full — followed by the transcript of
  `uv run python scripts/extract_changelog_section.py 9.9.9` showing a non-zero exit and its stderr
  message. Add one sentence naming this the **empty-input edge case** the phase resolves explicitly:
  `release.yml`'s `validate` job runs the first form and fails the whole release before `build` or
  `publish-pypi` if the section is missing or its body is empty, and the second form is the control
  proving the extractor really does reject that state rather than returning an empty string.

- `## Breaking-mark census (D-01)`. Paste the command and output of
  `awk '/^## \[0\.9\.0\]/{f=1;next} /^## \[0\.8\.0\]/{f=0} f' CHANGELOG.md | grep -n '\*\*Breaking'`
  and state, one line each, which of the four bullets is the promoted OUT-04 relocation, which is the
  promoted WR-01/CR-01 pre-write validation, which is the newly authored output relocation, and which
  is the newly authored removal — plus the migration sentence each carries, quoted.

- `## Section census (D-02, D-03, D-05)`. Paste the output of
  `awk '/^## \[0\.9\.0\]/{f=1;next} /^## \[0\.8\.0\]/{f=0} f' CHANGELOG.md | grep -n '^### '` and the
  residual-Unreleased equivalent, with one sentence recording that `### Removed` exists and that
  `### Verified` carries its standing three items with wording unchanged.

- `## Tail link block`. Paste `tail -25 CHANGELOG.md` verbatim and one sentence recording that the
  block was already complete — no historical release line was missing and none was repaired, the
  earlier CONTEXT claim to that effect having been retracted — so the change is the routine
  insert-plus-advance and nothing more.

- `## Published-page gate`. Paste the full `uv run --extra dev --extra docs pytest
  tests/test_changelog_page_gate.py -v` transcript and the JUnit `testsuite` attributes, with one
  sentence stating explicitly that the skip count is 0 and why a skip here would not have been
  evidence.

- `## D-09 — what is deliberately NOT in this section`. One short paragraph: `54.1-REVIEW.md` WR-02
  measured that the pre-write validation resolves `templates_path` against the source directory
  rather than the config directory, so a project using `-c`/`--confdir` still walks into the
  republication hole, and the promoted bullet's wording therefore reads as unconditional. The owner
  declined both a scoping clause and a `### Known Limitations` section, with the reviewer's own
  recommended minimum remediation explicitly on the table. Record the decline and name
  `57-HANDOFF.md` (plan 57-09) as the artifact that carries it forward. Do not restate it as a defect
  of this plan's execution.

- `## Human check owed`. One line stating that the editorial quality of the section — that it reads
  as curated prose rather than a generated dump — is a manual verification listed in
  `57-VALIDATION.md`, discharged at end-of-phase UAT against the pasted extractor output above, not
  by any assertion in this phase.

Never assert a command's result from memory — run it and paste the transcript.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-v0-9-0-release-prep-prep-only/57-CHANGELOG-EVIDENCE.md && grep -q '^## SC#2 — the release body, in both directions' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CHANGELOG-EVIDENCE.md && grep -q '^## Breaking-mark census (D-01)' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CHANGELOG-EVIDENCE.md && grep -q '^## D-09' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CHANGELOG-EVIDENCE.md && grep -q 'skipped="0"' .planning/phases/57-v0-9-0-release-prep-prep-only/57-CHANGELOG-EVIDENCE.md</automated>
    <human-check>Read the pasted `scripts/extract_changelog_section.py 0.9.0` output in `57-CHANGELOG-EVIDENCE.md` § SC#2 as if you were a user upgrading — this exact text becomes the GitHub Release body. Confirm it reads as curated prose rather than a generated dump: the lead paragraph names the registry as the headline and says what is not breaking, each of the four `**Breaking` bullets tells you what to change, and nothing reads as an internal note. `57-VALIDATION.md` lists this as a Manual-Only verification because editorial quality is not machine-checkable; the gate only proves the section exists and is non-empty.</human-check>
  </verify>
  <acceptance_criteria>
    - `57-CHANGELOG-EVIDENCE.md` exists, is written entirely in English, and contains the headings `## SC#2 — the release body, in both directions`, `## Breaking-mark census (D-01)`, `## Section census (D-02, D-03, D-05)`, `## Tail link block`, `## Published-page gate`, `## D-09 — what is deliberately NOT in this section`, and `## Human check owed`.
    - The `## SC#2` section contains both extractor transcripts and the literal `empty-input edge case`.
    - The `## Breaking-mark census (D-01)` section identifies four distinct bullets by name and quotes a migration sentence for each.
    - The `## Published-page gate` section contains the literal `skipped="0"`.
    - The `## D-09` section names `57-HANDOFF.md` and states that both a scoping clause and a limitations section were declined.
    - `git diff --name-only -- typsphinx/ .planning/REQUIREMENTS.md` produces no output.
  </acceptance_criteria>
  <done>SC#2's evidence is on disk: the exact GitHub Release body, both extractor directions with the empty-input control, the four-Breaking census with each migration sentence, the section and tail-block censuses, a zero-skip page-gate transcript, and an explicit record of what D-09 leaves unsaid.</done>
</task>

</tasks>

<threat_model>
Configured level: OWASP ASVS L1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md`'s `## [0.9.0]` body → the published GitHub Release body | `create-release` copies this text verbatim through `scripts/extract_changelog_section.py`; whatever is written here is what users read. |
| `CHANGELOG.md` → `release.yml`'s `validate` job | A missing or empty section aborts the release before `build` and `publish-pypi`. |
| `RELEASE_VERSIONS` → the published documentation page gate | The tuple is the hand-maintained half of an otherwise automatic include; an append made too early turns a real gate red for a reason unrelated to the defect it guards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-12 | Repudiation | a breaking change shipped unannounced | high | mitigate | The Breaking-mark census asserts exactly four marked bullets inside the new section and the evidence file names each with its migration sentence; the two ROADMAP SC#2 names are asserted present by their requirement IDs and by the `typst_template_assets` / `typst_document_templates` literals. |
| T-57-13 | Denial of service | `release.yml`'s `validate` job aborting the real release on a missing or empty section | high | mitigate | `<verify>` runs the exact extractor call `validate` makes and asserts a non-empty body; the 9.9.9 control proves the failure direction is real rather than a silent empty return. |
| T-57-14 | Tampering | a historical CHANGELOG section or migration guide edited under cover of the new one | medium | mitigate | Acceptance asserts no removed line in the diff belongs to a historical section, and the prohibitions forbid editing historical entries. |
| T-57-15 | Tampering | a test gate weakened so the page gate reports green | high | mitigate | Acceptance asserts the `tests/test_changelog_page_gate.py` diff touches no `def`, `class`, `assert`, `import` or `@pytest` line, and that the JUnit result carries `skipped="0"`. |
| T-57-16 | Information disclosure | over-broad claim about pre-write validation misleading a `-c`/confdir project | medium | accept | Owner decision D-09, taken with the reviewer's own recommended minimum remediation explicitly on the table. The record is preserved in `57-CHANGELOG-EVIDENCE.md` § D-09 and carried into `57-HANDOFF.md`; the underlying republication hole is a `typsphinx/` change the prep-only fence excludes. |
| T-57-17 | Repudiation | REL-08 reported complete before the publish | high | mitigate | Prohibition plus `git diff --name-only -- .planning/REQUIREMENTS.md` asserted empty; `57-CLOSEOUT-GUARD.md` (plan 57-01) holds the digest baseline. |
| T-57-SC | Tampering | npm/pip/cargo installs | low | accept | No package is added; `uv run --extra dev --extra docs` installs only what `uv.lock` already pins. `57-RESEARCH.md` records the Package Legitimacy Gate as not applicable. |
</threat_model>

<verification>
- `uv run python scripts/extract_changelog_section.py 0.9.0` exits 0 with a non-empty body; the same script exits non-zero for an absent version.
- Exactly four `**Breaking` markers inside the `## [0.9.0]` section; exactly one `### Removed` heading; exactly three `### Verified` bullets.
- The residual `## [Unreleased]` block carries exactly one `### ` heading.
- The tail block's new release-tag line sits immediately above the previous topmost release line; `[Unreleased]` is last and compares against the new tag.
- `uv run --extra dev --extra docs pytest tests/test_changelog_page_gate.py` reports `skipped="0"`, `failures="0"`, `errors="0"`.
- `git diff --name-only -- typsphinx/ .planning/REQUIREMENTS.md` produces no output.
</verification>

<success_criteria>
- A curated `## [0.9.0]` section exists whose lead paragraph names the registry, whose four `**Breaking` bullets each carry a migration sentence, and whose `### Removed` bullet agrees with the shipped warning string and the published Removed Configuration Values table (SC#2, D-01…D-05).
- The seven pre-existing bullets are promoted substantially as written, and `## [Unreleased]` retains only its forward-looking list (D-02).
- The tail link block is rolled over as a routine two-line edit with no repair task invented (SC#2).
- The published-page coverage tuple names the new release and its gate is green with zero skips.
- SC#2's evidence — including the exact GitHub Release body and the empty-input control — is on disk.
</success_criteria>

<output>
Create `.planning/phases/57-v0-9-0-release-prep-prep-only/57-03-SUMMARY.md` when done.
</output>
