---
phase: 57-v0-9-0-release-prep-prep-only
plan: 11
type: execute
wave: 3
depends_on: []
files_modified:
  - typsphinx/builder.py
  - CHANGELOG.md
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md
  - .planning/phases/57-v0-9-0-release-prep-prep-only/57-MESSAGE-FIX-EVIDENCE.md
  - .planning/WINDOWS.md
  - .planning/todos/pending/2026-08-17-repr-escaped-paths-in-remaining-user-facing-messages.md
autonomous: true
requirements: [REL-08]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "**This plan deliberately breaks Phase 57's prep-only fence, by explicit owner decision taken 2026-08-17 after being told that is what the option costs.** It is the FIRST and ONLY intended change under `typsphinx/` in this phase. Every downstream fence check — 57-08's SC#4 sweep and the phase verifier — must read the amended decision block this plan writes into `57-CONTEXT.md`, not the original prep-only wording, or it will report a false violation."
    - "The root cause is `repr()` escaping, NOT a path separator. `typsphinx/builder.py` builds `bundle_dir` as a **str** (`path.dirname(str(resolution.path))`, builder.py:1288) and interpolates it with `{bundle_dir!r}`. On Windows `repr()` escapes each backslash, so the raised message carries TWO literal backslash characters where one separator belongs. Reproduced locally on this POSIX host by simulating a Windows-shaped string: the message's character sequence is `..., 's', '\\\\', '\\\\', 'n', ...`."
    - "After this fix the message carries the platform's separator UNESCAPED, which means plan 57-10's already-merged test change — `str(Path(\"_templates\") / \"nested\")`, one backslash on Windows — becomes CORRECT rather than needing a second revision. Do NOT revert or re-edit 57-10's assertion; verify it now matches and say so."
    - "On POSIX the emitted message is **byte-identical before and after** this change: `repr('_templates')` and `'{}'.format('_templates')` wrapped in single quotes both yield `'_templates'`, because there is nothing to escape. The full local suite passing unchanged is the proof, and any assertion that DOES change is a signal the fix over-reached."
    - "The search set is derived from the DEFECT PROPERTY — `a filesystem path interpolated with !r into a message a user is expected to read and act on` — swept across `typsphinx/`, NOT from the single line CI named. A whole-file `!r` census exists; every hit is classified path-valued vs identifier-valued (registry keys, docnames, config tuples), and the classification is recorded."
    - "Scope is bounded to the **pre-write template-path refusal family** — this milestone's own new error surface (the `templates_path` collision refusal, the srcdir-ancestor refusal, and the bundle-destination collision refusal in `typsphinx/builder.py`). Identifier-valued `!r` (registry keys, docnames) is CORRECTLY `!r` and is left alone; path-valued `!r` outside that family (v0.8.0-era output-path collision messages, debug/warning logs) is NOT changed here — it is filed as a todo, because widening a message rewrite across the codebase during release prep is the larger risk."
    - "A Windows-only escaping regression becomes detectable on this POSIX host. This phase has already lost TWO full CI matrix dispatches (`31956166848`, `31959060298`) to a defect no local run could see, so the fix ships with a check that feeds a Windows-shaped path through the real message-construction path and asserts no doubled separator survives."
    - "The change is user-visible on Windows and lands in a release, so `CHANGELOG.md`'s `## [0.9.0]` section carries a `### Fixed` bullet for it. `docs/source/changelog.rst`'s quoted example block needs NO edit — its illustrative paths are POSIX and therefore byte-identical before and after; confirm this by inspection rather than assuming."
    - "`.planning/WINDOWS.md` entries 9 and 10 are both transitioned to record that the real fix landed in 57-11, with Windows-lane confirmation still pending the fresh authority dispatch. Neither is closed by this plan — no Windows run has confirmed anything yet."
  artifacts:
    - path: "typsphinx/builder.py"
      provides: "the pre-write template-path refusal messages, with path values quoted rather than repr-escaped"
      contains: "templates_path"
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-MESSAGE-FIX-EVIDENCE.md"
      provides: "the `!r` census and classification, the local Windows-shape reproduction before and after, and the proof that POSIX output is unchanged"
      contains: "31959060298"
    - path: ".planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md"
      provides: "the AMENDED decision block recording the owner-approved prep-only fence exception, so SC#4 is evaluated against the amended rule"
      contains: "AMENDED"
    - path: "CHANGELOG.md"
      provides: "the `### Fixed` bullet for the Windows message change in the `## [0.9.0]` section"
      contains: "0.9.0"
  key_links:
    - from: "typsphinx/builder.py's refusal message"
      to: "tests/test_templates_path_collision_gate.py's beta assertion"
      via: "the assertion (fixed in 57-10) expects one native separator; only an unescaped message can supply it"
      pattern: "_templates"
---

<objective>
Fix the defect for real, on the side the owner chose. Two CI matrix dispatches have
now been spent on a single assertion, because the failure was twice diagnosed as a
path-separator problem when it is an **escaping** problem: the refusal message
interpolates a path *string* with `!r`, and `repr()` doubles every backslash.

Owner decision (2026-08-17), taken with the cost stated up front: fix the product
message rather than normalise it away in the test. That decision knowingly breaks
this phase's prep-only fence, and this plan is responsible for recording the
exception where every downstream fence check will read it.
</objective>

<execution_context>
Read `CLAUDE.md` first and provision this worktree per its "Worktree-isolated
execution" section before running anything.
</execution_context>

<context>
Measured, not assumed:

- `builder.py:1288` — `bundle_dir = path.dirname(str(resolution.path))` is a **str**.
- `builder.py:1295-1299` — the message interpolates `{key!r}`, `{bundle_dir!r}`,
  `{raw_tp_entry!r}`, `{resolved_tp_entry!r}`.
- CI run `31959060298` (headSha `bfcc6f6d`, dispatched by 57-05): 10/12, both
  `windows-latest` lanes failing at the same assertion as run `31956166848`.
- Local reproduction on this POSIX host, simulating a Windows-shaped `bundle_dir`:
  the message's characters around the tail are `'s', '\\', '\\', 'n'` — two literal
  backslashes. `str(Path(...))` (one backslash) cannot match; the doubled form can.

`{key!r}` on a REGISTRY KEY is correct and stays — keys are identifiers, quoting
them is the point, and they cannot contain separators (`template_registry.py:118`
rejects those). Only PATH values change.
</context>

<tasks>

<task number="1" name="Census every !r, classify it, and unescape the path values in the pre-write refusal family">

**Step 1 — census.** Enumerate EVERY `!r` interpolation under `typsphinx/`. For each,
classify as:

- **path-valued** — the interpolated value is a filesystem path or path fragment
  (it can contain `os.sep`, so `repr()` will escape it on Windows), or
- **identifier-valued** — a registry key, docname, config tuple, or similar, where
  `!r` is the correct and intended quoting.

Record the full table in the evidence file. Start from the property, not from
builder.py:1296.

**Step 2 — fix, bounded to the pre-write template-path refusal family.** In
`typsphinx/builder.py`, change the **path-valued** interpolations to explicit
quoting that does not escape — `'{value}'` in place of `{value!r}` — in these
three refusal sites only:

- the `templates_path` collision refusal (around builder.py:1295-1299),
- the srcdir-ancestor refusal (around builder.py:330-333),
- the bundle-destination collision refusal (around builder.py:2164-2165).

Leave `{key!r}` and every other identifier-valued interpolation untouched. Leave
path-valued `!r` OUTSIDE this family untouched — task 3 files it as a todo.

**Step 3 — prove POSIX output is unchanged.** The full local suite must pass with
**no test edit at all**. If any assertion needs changing to accommodate this fix,
STOP and report — that means the change altered POSIX output, which it must not.

Also confirm, and state in the evidence file, that plan 57-10's already-merged
assertion `str(Path("_templates") / "nested")` now matches what the fixed message
would produce on Windows. Do NOT revert or re-edit it.

<verify>
<automated>uv run python -m pytest -q && uv run ruff check . && uv run black --check . && uv run mypy typsphinx/ && test -z "$(git diff --name-only -- tests/)"</automated>
<manual>
- The census table covers every `!r` under `typsphinx/`, each row classified.
- `git diff -- typsphinx/` touches only the three named refusal sites.
- No test file was edited (the automated check asserts this) — POSIX output is unchanged by construction.
</manual>
</verify>
</task>

<task number="2" name="Make the Windows-only escaping regression detectable on this POSIX host">

Two full CI matrix dispatches have been lost to a defect that no local run can see.
Add a check that closes that blind spot: drive the real message-construction path
with a Windows-shaped path value and assert the emitted message contains **no
doubled separator**.

Keep it small and honest:

- Put it with the existing collision-gate coverage rather than inventing a new
  module, unless the existing module's structure makes that unnatural.
- It must exercise the REAL message construction, not a copy of the f-string
  pasted into the test — a duplicated format string would pass while the product
  regressed, which is precisely the failure mode being guarded.
- If the message construction cannot be driven directly without a Windows host,
  say so plainly in the evidence file and add the closest honest approximation,
  naming what it does and does not cover. Do not overstate the guard's reach.

<verify>
<automated>uv run python -m pytest -q && uv run ruff check . && uv run black --check .</automated>
<manual>
- The new check fails if `!r` is reintroduced at any of the three fixed sites — demonstrate this by temporarily reverting one site, capturing the RED, and restoring it. Record both transcripts.
- The check drives real product code, not a re-pasted format string.
</manual>
</verify>
</task>

<task number="3" name="Record the fence exception, the CHANGELOG entry, the ledger transition, and the deferred class">

1. **`57-CONTEXT.md` — AMENDED decision block.** Append a clearly-marked `AMENDED
   2026-08-17` block recording: that Phase 57 was scoped prep-only with SC#4
   requiring no `typsphinx/` change; that CI runs `31956166848` and `31959060298`
   proved a Windows-only defect in this milestone's own new error surface; that the
   owner was told fixing the product breaks the fence and chose it anyway; and that
   SC#4 must therefore be evaluated as "no UNINTENDED `typsphinx/` change, the
   `builder.py` message fix in 57-11 being the one intended and owner-approved
   exception". Name `57-08` and the phase verifier as the readers who depend on this.

2. **`CHANGELOG.md`** — add a `### Fixed` bullet under the existing `## [0.9.0]`
   section (authored by 57-03; do not restructure it) describing the Windows
   message change in user terms. Do not add a `**Breaking` mark — this is a
   message-formatting fix, and 57-03's section is asserted to carry exactly four
   `**Breaking` bullets.

3. **`.planning/WINDOWS.md`** — transition entries 9 and 10 to record that the real
   fix landed in 57-11 and that Windows-lane confirmation is pending the fresh
   authority dispatch. Close neither.

4. **File the deferred class as a todo** at
   `.planning/todos/pending/2026-08-17-repr-escaped-paths-in-remaining-user-facing-messages.md`,
   listing the path-valued `!r` sites left unchanged (from task 1's census) and why
   they were deferred out of a release-prep phase.

5. **`57-MESSAGE-FIX-EVIDENCE.md`** — the census table, the local Windows-shape
   reproduction before AND after the fix, the byte-identical-on-POSIX argument with
   its supporting transcript, the task-2 RED/GREEN demonstration, and an explicit
   "this plan has not observed a green Windows lane" statement.

<verify>
<automated>grep -q "AMENDED" .planning/phases/57-v0-9-0-release-prep-prep-only/57-CONTEXT.md && grep -q "57-11" .planning/WINDOWS.md && test -f .planning/todos/pending/2026-08-17-repr-escaped-paths-in-remaining-user-facing-messages.md && test -f .planning/phases/57-v0-9-0-release-prep-prep-only/57-MESSAGE-FIX-EVIDENCE.md && uv run python -m pytest tests/test_changelog_page_gate.py -q</automated>
<manual>
- The AMENDED block names SC#4, the owner decision, and the downstream readers by name.
- The CHANGELOG `**Breaking` count is still exactly four.
- The evidence file does not claim a green Windows lane.
</manual>
</verify>
</task>

</tasks>

<threat_model>

## Trust Boundaries

This plan edits runtime code for the first time in the phase. The edited code is
error-message construction on a refusal path — it changes what a user reads when a
build is already being refused, never whether it is refused. No network call, no
credential, no irreversible action.

## STRIDE Threat Register

| ID | Category | Threat | Severity | Response | Mitigation |
|----|----------|--------|----------|----------|------------|
| T-57-70 | Tampering | The message edit changes refusal BEHAVIOUR (which builds are refused), not just wording | high | mitigate | Change is confined to f-string interpolation of already-computed values; the full suite, including every collision-gate behavioural assertion, must pass with zero test edits |
| T-57-71 | Repudiation | SC#4's fence check later reads the original prep-only wording and reports a false violation, or a real future over-reach hides behind this exception | high | mitigate | Task 3 writes an AMENDED block that scopes the exception to exactly this fix and names `57-08` and the verifier as its readers |
| T-57-72 | Denial of service | The fix is wrong a third time and burns another full CI matrix | high | mitigate | Task 2 makes the defect locally detectable before any dispatch; task 1 step 3 requires zero test edits, which would otherwise mask a wrong fix |
| T-57-73 | Tampering | The fix over-reaches into unrelated messages during release prep, changing user-facing text nobody reviewed | medium | mitigate | Scope is bounded to three named refusal sites; the remainder of the census is filed as a todo rather than silently fixed |
| T-57-74 | Spoofing | The task-2 guard duplicates the product's format string and therefore passes while the product regresses | medium | mitigate | Task 2 explicitly requires driving real product code and forbids a re-pasted format string |
| T-57-75 | Information disclosure | The CHANGELOG bullet describes an internal implementation detail rather than user-visible behaviour | low | mitigate | Task 3 requires the bullet be written in user terms |

</threat_model>

<verification>
- `uv run python -m pytest -q` passes with no test file edited in task 1.
- `uv run ruff check .`, `uv run black --check .`, `uv run mypy typsphinx/` all clean.
- `git diff --name-only -- typsphinx/` lists `typsphinx/builder.py` and nothing else.
- `git tag -l v0.9.0` and `git ls-remote --tags origin v0.9.0` both produce no output.
</verification>

<success_criteria>
- [ ] Every `!r` under `typsphinx/` is censused and classified path-valued vs identifier-valued.
- [ ] The three pre-write refusal sites quote path values without `repr()` escaping; identifier-valued `!r` is untouched.
- [ ] The full suite passes with ZERO test edits in task 1, proving POSIX output byte-identical.
- [ ] 57-10's merged assertion is confirmed to now match the fixed message on Windows, and was not reverted.
- [ ] A locally-runnable check catches reintroduction of the escaping, demonstrated by a real revert-and-restore RED/GREEN transcript.
- [ ] `57-CONTEXT.md` carries an AMENDED block scoping the owner-approved fence exception, naming SC#4, 57-08 and the verifier.
- [ ] `CHANGELOG.md` `## [0.9.0]` gains a `### Fixed` bullet in user terms; the `**Breaking` count stays exactly four.
- [ ] `WINDOWS.md` entries 9 and 10 transitioned, neither closed.
- [ ] The deferred path-valued `!r` sites are filed as a pending todo.
</success_criteria>

<output>
`57-11-SUMMARY.md` in the phase directory.
</output>
