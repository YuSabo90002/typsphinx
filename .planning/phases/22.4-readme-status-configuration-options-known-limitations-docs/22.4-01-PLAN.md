---
phase: 22.4-readme-status-configuration-options-known-limitations-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-05]
must_haves:
  truths:
    - "README.md に検証機構を持たない数値クレーム（テスト件数・カバレッジ率・カバレッジ努力目標）が一つも残っていない — 再計測ではなく削除で解消する (D-01, D-02, D-04)"
    - "Configuration Options 節が「主要な設定の部分列」と読める文言になり、`typst_documents` を含む 6 件を列挙し、実際にビルドされる `docs/source/user_guide/configuration.rst` を指している (D-05, D-06, D-12)"
    - "Features / Known Limitations / Status / Acknowledgments の各主張がソース実測と一致する: citations は Features から外れ Known Limitations に未対応として載り、Glossary の未対応記述は消え、Status は実測版数を示し、方法論名は GSD である (D-09, D-10, D-11, D-13)"
    - "Advanced Usage の 2 つのコード例が「そのまま貼ると動く」状態になる: 出力に反映されないテンプレートパラメータ例は削られ、`typst_template_mapping` 例はキー/値の向きが実装と一致する (D-18, D-19)"
    - "実体の存在しない文書を指す一文が README から消えている (D-20)"
  artifacts:
    - "README.md（修正済み・新規ファイルなし）"
  key_links:
    - "README Configuration Options 節 → `docs/source/user_guide/configuration.rst`（toctree 到達性のある実ファイル）"
    - "README Known Limitations の Citation 記述 ↔ `typsphinx/translator.py` に `visit_citation` が存在しないという実測"
    - "README Status 行 ↔ `pyproject.toml` の `version`（Phase 23 のバンプ時に同時更新する申し送り、D-11）"
  prohibitions:
    - statement: "`typsphinx/` 配下のファイルを 1 バイトも変更しない（本フェーズは prose only）"
      status: required
      verification: "git diff --stat 240142a..HEAD -- typsphinx/ が空"
    - statement: "`docs/` 配下のファイルを削除・再編・変更しない（孤児 `docs/configuration.rst` も残す、D-16 は todo 側で扱う）"
      status: required
      verification: "git diff --stat 240142a..HEAD -- docs/ が空"
    - statement: "`@preview` パッケージのバージョン文字列を変更しない（`tests/test_preview_version_sync.py` が担保する機械検証済みの数値、D-03）"
      status: required
      verification: "grep -cE 'mitex:0\\.2\\.7|codly:1\\.3\\.0' README.md が 2"
    - statement: "Requirements 節（Python / Sphinx / typst-py の下限）を変更しない — `pyproject.toml` と一致済み (D-03)"
      status: required
      verification: "grep -cE '^- Python 3\\.12 or higher$|^- Sphinx 9\\.1 or higher$|^- typst-py 0\\.15\\.0 or higher$' README.md が 3"
    - statement: "Example projects の箇条書きを変更しない（`examples/charged-ieee/` を追記しない、D-14）"
      status: required
      verification: "grep -c 'examples/basic/` and `examples/advanced/' README.md が 1、grep -c 'charged-ieee' README.md が 0"
    - statement: "github.io リンク 7 本に `/en/` プレフィックスを足さない・URL を差し替えない（オーナー裁定により現状維持、D-17）"
      status: required
      verification: "grep -c 'yusabo90002.github.io' README.md が 10、grep -c 'yusabo90002.github.io/typsphinx/en/' README.md が 0"
    - statement: "`typst_*` 設定値を追加も削除もしない（README の列挙は既存の登録済み名のみを使う）"
      status: required
      verification: "README に現れる `typst_` 名がすべて `typsphinx/__init__.py` に登録済み"
    - statement: "`CHANGELOG.md` の過去バージョンエントリを書き換えない"
      status: required
      verification: "git diff --stat 240142a..HEAD -- CHANGELOG.md が空"
---

<objective>
`README.md` の全記述を実測に合わせる。本フェーズで `README.md` を触る唯一のプランであり、
D-01〜D-06 / D-09〜D-14 / D-18(例の剪定) / D-19 / D-20 のすべてがこの 1 ファイルに集約される。

Purpose: 「検証機構を持たないクレームは README に置かない」という Phase 22.2 由来の原則を
README 全文に適用し、todo が名指しした 3 件だけでなく討議・Research で洗い出した全乖離を解消する。

Output: 修正済みの `README.md`（新規ファイルなし、ソース変更ゼロ）。
</objective>

## Artifacts this phase produces

このプランが **新規に作成するファイルは無い**。`README.md` を in-place で修正するのみ。
新しいシンボル・クラス・関数・CLI フラグ・設定値も一切導入しない（`typst_documents` は
`typsphinx/__init__.py:44` に既存登録済みの設定名を README に**列挙する**だけであり、新設ではない）。

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-CONTEXT.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-RESEARCH.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-VALIDATION.md
@README.md

**Worktree 前提（CLAUDE.md §Worktree-isolated execution）:** 本プロジェクトは worktree 分離実行が
恒常モード。`test -f .git` が真なら、最初に
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` を実行し、
以降の `pytest` / `ruff` / `black` は必ず `uv run` 経由で呼ぶこと。`grep` は素で構わない。

**座標は信用しない。** CONTEXT.md / RESEARCH.md に載る `README:NNN` はすべて 2026-07-22 の
スナップショットであり、本プランの Task 1 の編集で Task 2/3 の座標がずれる。対象箇所は必ず
`grep -n '<文字列>' README.md` で現在位置を引き直してから編集すること。
</context>

<!-- 以下は acceptance criteria が 0 件を要求する文字列リテラルであり、
     本プラン内で対象箇所を一意に指し示すために不可避に登場する。 -->
<!-- planner-discipline-allow: 413 tests -->
<!-- planner-discipline-allow: 93% overall -->
<!-- planner-discipline-allow: 80%+ code coverage -->
<!-- planner-discipline-allow: and citations -->
<!-- planner-discipline-allow: Glossary generation -->
<!-- planner-discipline-allow: v0.5.0 -->
<!-- planner-discipline-allow: Kiro -->
<!-- planner-discipline-allow: full requirements verification -->
<!-- planner-discipline-allow: docs/configuration.rst -->
<!-- planner-discipline-allow: papersize -->
<!-- planner-discipline-allow: fontsize -->

<tasks>

<task type="auto">
  <name>Task 1: 検証機構を持たない数値クレームを README から除去する (D-01/D-02/D-04)</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — Setup Development Environment のコードブロック、Testing Strategy 節、Development Guidelines 節
    - `pyproject.toml` — `[tool.pytest.ini_options]` と dev extra。coverage の閾値強制設定（`fail_under`／`[tool.coverage]`）が存在しないことの正解源
    - `tox.ini` — `[testenv:cov]` がレポート出力のみで閾値を強制しないことの正解源
    - `.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-RESEARCH.md` — Task B（実測値と `fail_under` 不在の確認手順）
  </read_first>
  <action>
    README の数値クレームを **削除**する。実測値への書き換えは採らない（D-01: 413 → 577 → 589 と
    数日で二度ずれた実績があり、書き換えても同じ乖離が再発するため）。

    1. Setup Development Environment のコードブロック内、`uv run pytest` の直前にあるコメント行から
       括弧つきのテスト件数注記を落とし、コメントを `# Run tests` だけにする (D-01)。
    2. Testing Strategy 節の Unit tests 箇条書きから件数を落とす。目標文言は
       `- **Unit tests**: Cover all major components` (D-01)。
    3. Testing Strategy 節の Code coverage 箇条書き（カバレッジ率を述べる行）を**行ごと削除**する。
       置換ではなく削除でよい — `pyproject.toml`／`tox.ini`／`.coveragerc`／`setup.cfg` のいずれにも
       `fail_under` 相当の閾値強制が存在せず、いかなる数値も担保できないため (D-02)。
    4. Development Guidelines 節のカバレッジ努力目標を述べる箇条書きを**行ごと削除**する (D-04)。
       同節の他の箇条書き（TDD / black / Sphinx 拡張慣習 / テスト追加）は変更しない。

    **触ってはいけないもの（アンチリグレッション）:** Features 節の `@preview/mitex:0.2.7` と
    `@preview/codly:1.3.0` はバイト単位で不変（`tests/test_preview_version_sync.py` が
    `writer.py`/`template_engine.py`/`templates/base.typ` の 3 箇所との一致を担保している検証済み数値, D-03）。
    Requirements 節の Python/Sphinx/typst-py 下限 3 行も `pyproject.toml` と一致済みのため不変 (D-03)。
    Testing Strategy 節の Example projects 箇条書きも現状のまま — `examples/charged-ieee/` は追記しない (D-14)。
  </action>
  <verify>
    <automated>grep -cE '413 tests|93% overall|80%\+ code coverage' README.md; test "$(grep -cE '413 tests|93% overall|80%\+ code coverage' README.md)" = "0" &amp;&amp; test "$(grep -cE 'mitex:0\.2\.7|codly:1\.3\.0' README.md)" = "2" &amp;&amp; test "$(grep -cE '^- Python 3\.12 or higher$|^- Sphinx 9\.1 or higher$|^- typst-py 0\.15\.0 or higher$' README.md)" = "3" &amp;&amp; test "$(grep -c 'charged-ieee' README.md)" = "0" &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '413 tests|93% overall|80%\+ code coverage' README.md` が **0**
    - `grep -cE 'mitex:0\.2\.7|codly:1\.3\.0' README.md` が **2**（D-03 アンチリグレッション）
    - `grep -cE '^- Python 3\.12 or higher$|^- Sphinx 9\.1 or higher$|^- typst-py 0\.15\.0 or higher$' README.md` が **3**（D-03 アンチリグレッション）
    - `grep -c 'examples/basic/` and `examples/advanced/' README.md` が **1** かつ `grep -c 'charged-ieee' README.md` が **0**（D-14 アンチリグレッション）
    - `grep -c 'uv run pytest' README.md` が **2 以上**（コマンド自体は残っている＝行ごと消していない）
  </acceptance_criteria>
  <done>README に検証機構を持たない数値が 1 つも残らず、機械検証済みの数値（@preview 版数・依存下限）は無傷。</done>
</task>

<task type="auto">
  <name>Task 2: Configuration Options を部分列に改め、リンクを実ファイルへ張り替え、Advanced Usage の 2 例を実装と一致させる (D-05/D-06/D-12/D-18/D-19)</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — Advanced Usage（Custom Templates / Template Parameter Mapping）と Configuration Options 節
    - `typsphinx/__init__.py:44-62` — 登録済み `typst_*` 設定 12 件の唯一の正解源。`typst_documents` が先頭で登録されていること、README 未掲載の 6 件（`typst_package` / `typst_package_imports` / `typst_template_function` / `typst_authors` / `typst_debug` / `typst_template_assets`）を確認する
    - `typsphinx/template_engine.py` — `DEFAULT_PARAMETER_MAPPING`（`{"project": "title", "author": "authors", "release": "date"}`）と `map_parameters()`。マッピングの向きとマッピング外キーが捨てられる挙動の正解源
    - `tests/test_template_engine.py` — `test_map_parameters_custom_mapping`。正しい向きの実例（`{"project": "doc_title", "author": "doc_authors", "release": "version"}`）
    - `docs/source/user_guide/index.rst` — 張り替え先が toctree 到達可能であることの確認
    - `.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-RESEARCH.md` — Task C（2 リンク先の実測比較）、新規発見 #1／#2
  </read_first>
  <action>
    (a) **Configuration Options 節** — 節冒頭のリード文を、列挙が**網羅ではなく主要設定の部分列**だと
    読み手に明示する文言に改める (D-05)。例:
    `Below are the main configuration options. This is not the complete set — see [docs/source/user_guide/configuration.rst](docs/source/user_guide/configuration.rst) for the full reference:`
    リンク先は必ず `docs/source/user_guide/configuration.rst`（リポジトリルート相対、Markdown リンクの
    表示テキストとターゲット双方）。現行の孤児ファイル `docs/configuration.rst` への参照は完全に除去する
    — 当該ファイルは `docs/source/` の外・どの toctree からも参照されず、本文に誤パッケージ名
    `sphinxcontrib.typst` を含む (D-12)。孤児ファイルの実体削除は本プランでは**行わない**（D-16 の todo 行き）。

    (b) 列挙は **6 件**にする (D-06)。既存 5 件（`typst_use_mitex` / `typst_template` / `typst_elements` /
    `typst_template_mapping` / `typst_toctree_defaults`）は説明文もそのまま維持し、先頭に
    `typst_documents` を 1 件だけ追加する。追加行の目標文言:
    `- \`typst_documents\`: Master documents to build, as \`[(source, target, title, author), ...]\` — required for PDF output`。
    未掲載のまま残る 6 件を README に足さないこと（D-05 の趣旨は「件数を増やさない」）。

    (c) **Custom Templates のコード例** — `typst_elements` を設定する辞書リテラルを例から**丸ごと削除**し、
    例を `typst_template` の 1 行だけにする (D-18)。理由: 当該辞書のキー群は
    `template_engine.py` の `map_parameters()` がマッピング外キーを無条件に捨てるため出力に一切到達せず、
    実ビルドで再現済み（Phase 22.2 が塞いだ「登録だけして出力に効かない設定」の第 3 インスタンス）。
    ソース修正は本フェーズのスコープ外で、Plan 02 が todo として起票する。
    **Configuration Options 節の `typst_elements` の 1 行（(b) の 6 件構成）は変更しない** — オーナー裁定 D-18。

    (d) **Template Parameter Mapping のコード例** — 現行例はキーがテンプレートパラメータ名・値が
    Sphinx メタデータ名という**実装と逆向き**で、しかも値の一つがリストになっており、そのまま使うと
    `map_parameters()` が何もマッチせずサイレントに無視される (D-19)。例を実装と同じ
    `{Sphinx メタデータ名: テンプレートパラメータ名}` の向きに書き換える。目標の辞書内容は
    `tests/test_template_engine.py` の `test_map_parameters_custom_mapping` と同じ 3 組:
    `'project': 'doc_title'` / `'author': 'doc_authors'` / `'release': 'version'`（すべて値は文字列、リスト不可）。
    コード例の直下に向きを明示する 1 文を添える。例:
    `Keys are Sphinx metadata names; values are the template parameter names they map to.`
  </action>
  <verify>
    <automated>test "$(grep -c 'docs/configuration.rst' README.md)" = "0" &amp;&amp; test "$(grep -c 'docs/source/user_guide/configuration.rst' README.md)" -ge 1 &amp;&amp; test "$(grep -c 'typst_documents' README.md)" -ge 1 &amp;&amp; test "$(grep -c '^- `typst_' README.md)" = "6" &amp;&amp; test "$(grep -cE 'papersize|fontsize' README.md)" = "0" &amp;&amp; test "$(grep -c \"'title': 'project'\" README.md)" = "0" &amp;&amp; test "$(grep -c \"'project': 'doc_title'\" README.md)" = "1" &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'docs/configuration.rst' README.md` が **0**、`grep -c 'docs/source/user_guide/configuration.rst' README.md` が **2**（表示テキストとリンクターゲット）
    - `grep -c '^- `typst_' README.md` が **6**、かつ `grep -c 'typst_documents' README.md` が **1 以上**
    - `grep -cE 'papersize|fontsize' README.md` が **0**（Custom Templates 例の剪定、D-18）
    - `grep -c "'title': 'project'" README.md` が **0** かつ `grep -c "'project': 'doc_title'" README.md` が **1**（向きの反転、D-19）
    - README に現れる全 `typst_` 名が `typsphinx/__init__.py` に登録済みであること — 検証コマンド: `for n in $(grep -o 'typst_[a-z_]*' README.md | sort -u); do grep -q "\"$n\"" typsphinx/__init__.py || echo "UNREGISTERED: $n"; done` の出力が空
    - **【手動レビュー・機械検証不可】** Configuration Options 節のリード文を読んだ読者が「これが typsphinx の設定の全てだ」と結論しないこと。VALIDATION.md の Manual-Only 表に該当。SUMMARY にリード文の実際の文面を引用して報告すること
  </acceptance_criteria>
  <done>設定一覧が明示的な部分列になり `typst_documents` を含む 6 件を列挙、リンクは toctree 到達可能な実ファイルを指し、Advanced Usage の 2 コード例がコピペで機能する状態。</done>
</task>

<task type="auto">
  <name>Task 3: 能力・制限・Status・方法論の各主張をソース実測に一致させる (D-09/D-10/D-11/D-13/D-20)</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — Features 節、Known Limitations 節、Documentation 節、Acknowledgments 節、末尾の Status 行
    - `typsphinx/translator.py` — `visit_citation` が**存在しない**こと（`grep -c 'def visit_citation' typsphinx/translator.py` が 0）と `visit_glossary` が**存在する**こと（`grep -n 'def visit_glossary' typsphinx/translator.py`）の実測。bibliography のハンドリングが皆無であること（`grep -ric 'bibliography' typsphinx/*.py`）も確認する
    - `pyproject.toml` — `version` の実測値（Status 行の正解源）
    - `.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-RESEARCH.md` — Task E（GSD の公開名称と URL）、新規発見 #3／#4
  </read_first>
  <action>
    (a) **Features 節** — Cross-references の箇条書きから citations の語を落とし、
    `- **Cross-references**: Maintain document structure with internal links` にする (D-10)。
    `typsphinx/translator.py` に `visit_citation` ハンドラが存在せず、rST citation 構文は Typst 構文
    エラーを起こす（Phase 22.2 で確認済み）ため。

    (b) **Known Limitations 節** — Glossary の未対応を述べる箇条書きを**削除**する。
    `typsphinx/translator.py` の `visit_glossary` により Phase 12 (BLK-04) で対応済みだから (D-13)。
    Bibliography の箇条書きは**維持**する（ソース中に bibliography のハンドリングが一切存在しないことを実測確認済み, D-13）。
    Citation を未対応として**追加**する。目標文言:
    `- **Citations**: reStructuredText citation directives are not yet supported` (D-13)。
    Features 側の (a) と整合すること。

    (c) 同じ Known Limitations 節の直後にある、実体の無い「プロジェクト文書」を参照する 1 文を
    **削除**する。指し示す requirements-verification 文書はリポジトリのどこにも存在しない (D-20)。
    差し替え先を捏造せず、単に削除する。

    (d) **Acknowledgments 節** — 方法論名の箇条書きを GSD に差し替える (D-09)。`.kiro` ディレクトリは
    存在せず、運用実態は `.planning/` + GSD ワークフロー。目標文言（RESEARCH Task E で公開 npm
    レジストリ経由で URL を実測確認済み）:
    `- Developed with [Claude Code](https://claude.ai/code) and [GSD](https://github.com/open-gsd/gsd-core) (spec-driven development for AI coding agents)`。
    `CHANGELOG.md` の 0.4.x 履歴エントリ側の記述は**改変しない**（履歴のため）。

    (e) **末尾の Status 行** — 版数を実測値 `0.6.1` に直す。目標文言:
    `**Status**: Stable (v0.6.1) - Production ready` (D-11)。未リリースの 0.6.2 を先行して書かないこと
    — 実測乖離の再生産になる。同行の次行（Python/Sphinx/Typst の下限）は変更しない (D-03)。
    **Phase 23 への申し送り:** `pyproject.toml` を 0.6.2 にバンプする際、この Status 行も同時に
    更新すること。SUMMARY にこの申し送りを明記すること。

    (f) **明示的な不作為の記録** — RESEARCH の ★discovery #6（Overview 冒頭の Markdown 言及と、
    Third-Party Extensions 節の `unknown_visit()` がテキストを抽出すると読める説明）は、RESEARCH が
    「軽微・対応不要・記録のみ」と判定した表現上の不正確さである。本プランでは**意図的に変更しない**。
    黙って見落としたのではなく評価のうえ据え置いた、という事実を SUMMARY に明記すること
    （VALIDATION.md の Manual-Only「全文パスが実際に行われたこと」の裏付けになる）。

    **触ってはいけないもの（アンチリグレッション）:** Documentation 節の `yusabo90002.github.io`
    リンク 7 本は URL・順序ともに現状維持。実測では 7 本すべて 404 で到達には `/en/` プレフィックスが
    必要だが、リンクの一括変更は RTD 移行作業側に委ねるというオーナー裁定 (D-17)。この 404 の実測事実は
    Plan 02 が todo として起票する。README 側では `/en/` を足さないこと。
  </action>
  <verify>
    <automated>test "$(grep -c 'and citations' README.md)" = "0" &amp;&amp; test "$(grep -cE 'Glossary generation|Glossary.*not yet supported' README.md)" = "0" &amp;&amp; test "$(grep -c 'Citations' README.md)" -ge 1 &amp;&amp; test "$(grep -c 'Bibliography' README.md)" -ge 1 &amp;&amp; test "$(grep -c 'full requirements verification' README.md)" = "0" &amp;&amp; test "$(grep -c 'Kiro' README.md)" = "0" &amp;&amp; test "$(grep -c 'GSD' README.md)" -ge 1 &amp;&amp; test "$(grep -c 'Stable (v0.6.1)' README.md)" = "1" &amp;&amp; test "$(grep -cE 'v0\.5\.0|v0\.6\.2' README.md)" = "0" &amp;&amp; test "$(grep -c 'yusabo90002.github.io' README.md)" = "10" &amp;&amp; test "$(grep -c 'yusabo90002.github.io/typsphinx/en/' README.md)" = "0" &amp;&amp; echo TASK3_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'and citations' README.md` が **0**（D-10）
    - `grep -cE 'Glossary generation|Glossary.*not yet supported' README.md` が **0**、`grep -c 'Citations' README.md` が **1 以上**（Known Limitations 節内）、`grep -c 'Bibliography' README.md` が **1 以上**（D-13）
    - `grep -c 'full requirements verification' README.md` が **0**（D-20）
    - `grep -c 'Kiro' README.md` が **0** かつ `grep -c 'GSD' README.md` が **1 以上**（D-09）
    - `grep -c 'Stable (v0.6.1)' README.md` が **1** かつ `grep -cE 'v0\.5\.0|v0\.6\.2' README.md` が **0**（D-11）
    - `grep -c 'yusabo90002.github.io' README.md` が **10** かつ `grep -c 'yusabo90002.github.io/typsphinx/en/' README.md` が **0**（D-17 アンチリグレッション）
    - `git diff --stat 240142a..HEAD -- CHANGELOG.md` が空（履歴不改変）
    - **【手動レビュー・機械検証不可】** RESEARCH の 55 クレーム台帳の全行が「修正済み / 変更不要（実測一致）/ todo 起票済み」のいずれかに処理されたこと。★discovery #6 は「評価のうえ据え置き」として SUMMARY に明記されていること。VALIDATION.md の Manual-Only 表に該当
  </acceptance_criteria>
  <done>能力主張・制限一覧・Status・方法論名がすべてソース実測に一致し、ダングリング参照が消え、github.io リンクは手つかず。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （該当なし） | 本プランは `README.md` の散文のみを編集する。信頼境界を跨ぐ入力を新たに受け取るコードパスは追加も変更もされない |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| （なし） | — | — | — | — | 本プランはコードを一切変更せず、入力を解析せず、ネットワーク到達先を追加しない。唯一の外部参照追加は Acknowledgments の `https://github.com/open-gsd/gsd-core`（D-09）で、これは公開 npm レジストリ `@opengsd/gsd-core` の homepage フィールドで実測確認済みの既知の公開 URL であり、実行時に取得されるものではなく README の散文リンクにすぎない。ASVS L1 の V2/V3/V4/V5/V6 のいずれも適用対象を持たない。捏造した脅威を並べるより、適用なしと明記するのが誠実な結論である |

**Package installs:** なし（`npm`/`pip`/`cargo` のインストールを一切行わない）。よって
`T-22.4-SC` サプライチェーン脅威も適用外。
</threat_model>

<verification>
全タスク完了後、worktree で以下を実行する（`test -f .git` が真なら先に
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev`）。

1. `grep -cE '413 tests|93% overall|80%\+ code coverage' README.md` → 0
2. `grep -c 'docs/configuration.rst' README.md` → 0 / `grep -c 'docs/source/user_guide/configuration.rst' README.md` → 2
3. `grep -c '^- `typst_' README.md` → 6
4. `grep -c 'Stable (v0.6.1)' README.md` → 1 / `grep -cE 'v0\.5\.0|v0\.6\.2' README.md` → 0
5. `grep -c 'Kiro' README.md` → 0
6. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest tests/test_preview_version_sync.py` → exit 0
7. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest` → フェーズ開始前と同じパス件数（変化があれば regression）
8. スコープフェンス: `git diff --stat 240142a..HEAD -- typsphinx/ docs/ CHANGELOG.md` → 出力なし
9. 変更ファイルが `README.md` のみ: `git diff --name-only 240142a..HEAD` に `README.md` 以外の非 `.planning/` ファイルが現れない
</verification>

<success_criteria>
- ROADMAP SC#1（数値クレーム削除・機械検証済み数値は不変）が満たされる
- ROADMAP SC#2（Configuration Options が部分列＋6 件＋正しいリンク、github.io は現状維持）が満たされる
- ROADMAP SC#3（citations / Glossary / Status / Acknowledgments）が満たされる
- ROADMAP SC#6（スコープフェンス）が `git diff --stat` で確認できる
- 既存 pytest スイートがフェーズ開始前と同じパス件数を維持
</success_criteria>

<output>
Create `.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-01-SUMMARY.md` when done.

SUMMARY には必ず含めること:
- Task 2 の Configuration Options リード文の実際の文面（手動レビュー用の引用）
- D-11 の Phase 23 申し送り（`pyproject.toml` 0.6.2 バンプ時に README の Status 行も同時更新）
- `uv run pytest` のフェーズ前／後のパス件数
</output>
