---
phase: 22.4-readme-status-configuration-options-known-limitations-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md
  - .planning/todos/pending/2026-07-22-delete-orphan-docs-configuration-rst.md
  - .planning/todos/pending/2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md
  - .planning/todos/pending/2026-07-22-user-guide-configuration-phantom-config-names.md
  - .planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md
  - .planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md
  - .planning/todos/pending/2026-07-21-move-documentation-hosting-to-read-the-docs.md
autonomous: true
requirements: [DOC-05]
must_haves:
  truths:
    - "本フェーズのスコープ外に退避すると決めた作業が、黙って落ちるのではなく `.planning/todos/pending/` の実ファイルとして存在する (D-15, D-16)"
    - "RESEARCH.md の全文監査で台帳外に新規発見された乖離が、本フェーズで直すもの以外すべて todo として記録されている (D-17, D-18, D-21)"
    - "既存の RTD 移行 todo が依拠していた誤った前提（github.io リンクは実測上すべて有効）が、404 の実測事実で上書き・相互参照され、後続フェーズに残らない (D-17)"
    - "★discovery #3 を将来自動検出できたはずの仕組み（`sphinx-build -b linkcheck` の CI ジョブ）が提案として記録されている"
  artifacts:
    - ".planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md"
    - ".planning/todos/pending/2026-07-22-delete-orphan-docs-configuration-rst.md"
    - ".planning/todos/pending/2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md"
    - ".planning/todos/pending/2026-07-22-user-guide-configuration-phantom-config-names.md"
    - ".planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md"
    - ".planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md"
  key_links:
    - "D-15 の todo ファイルパス → Plan 03 が `CLAUDE.md` と `pyproject.toml` のコメントから参照する先（Plan 03 は本プランに depends_on する）"
    - "D-17 の新規 todo ↔ 既存 `2026-07-21-move-documentation-hosting-to-read-the-docs.md`（相互参照で誤前提を打ち消す）"
  prohibitions:
    - statement: "`typsphinx/` 配下のファイルを 1 バイトも変更しない。todo は「起票」であって「実施」ではない — D-15 の typing modernize も D-18 のソース修正もここでは行わない"
      status: required
      verification: "git diff --stat 240142a..HEAD -- typsphinx/ が空"
    - statement: "`docs/` 配下のファイルを削除・再編・変更しない。孤児 `docs/configuration.rst`（D-16）と phantom 設定名を含む `docs/source/user_guide/configuration.rst`（D-21）はいずれも todo に記録するのみで、実体には触れない"
      status: required
      verification: "git diff --stat 240142a..HEAD -- docs/ が空"
    - statement: "`README.md` を変更しない（本フェーズで README を編集するのは Plan 01 のみ）"
      status: required
      verification: "git diff --name-only 240142a..HEAD に README.md が本プランのコミットからは現れない"
    - statement: "`pyproject.toml` の ruff `ignore` リストから `UP006`/`UP035` を外さない（D-08 の維持方針）"
      status: required
      verification: "grep -cE '\"UP006\"|\"UP035\"' pyproject.toml が 2"
    - statement: "既存 todo ファイルを削除・改名しない（追記による相互参照のみ）"
      status: required
      verification: "ls .planning/todos/pending/ に既存 4 ファイルがすべて残っている"
---

<objective>
本フェーズのスコープ外に退避すると決めた作業と、RESEARCH.md の全文監査で台帳外に新規発見された
乖離を、`.planning/todos/pending/` の実ファイルとして起票する。DOC-05 の中核 —
「台帳外の発見は本フェーズで直すか pending todo に起票する。黙って落とさない」。

Purpose: Phase 22.2 の教訓（「登録だけして出力に効かない設定」が registration-only テストの陰に
隠れていた）と同型の発見が本 Research で 2 件（`typst_elements` 非マッピングキー・
`typst_toctree_defaults`）出ている。ソース修正は prose-only のスコープフェンス外なので、
記録を残さなければ第 3・第 4 のインスタンスが再び埋没する。

Output: 新規 pending todo 6 本 + 既存 RTD 移行 todo への相互参照追記。
</objective>

## Artifacts this phase produces

**新規作成ファイル（6 本、すべて `.planning/todos/pending/` 配下）:**

- `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` (D-15)
- `.planning/todos/pending/2026-07-22-delete-orphan-docs-configuration-rst.md` (D-16)
- `.planning/todos/pending/2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md` (D-18)
- `.planning/todos/pending/2026-07-22-user-guide-configuration-phantom-config-names.md` (D-21)
- `.planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md` (D-17)
- `.planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md`（RESEARCH Task F 推奨）

**既存ファイルへの追記（1 本）:** `.planning/todos/pending/2026-07-21-move-documentation-hosting-to-read-the-docs.md`

新しいシンボル・クラス・関数・CLI フラグ・設定値は**一切作成しない**。ファイル名は上記のとおり
確定であり、Plan 03 が D-15 の todo をパスで参照するため**改名してはならない**。

<execution_context>
@/home/yuta/Documents/typsphinx/.claude/gsd-core/workflows/execute-plan.md
@/home/yuta/Documents/typsphinx/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-CONTEXT.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-RESEARCH.md
@.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-VALIDATION.md
@.planning/todos/pending/2026-07-21-move-documentation-hosting-to-read-the-docs.md

**Worktree 前提（CLAUDE.md §Worktree-isolated execution）:** `test -f .git` が真なら最初に
`env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv sync --extra dev` を実行し、以降の
`pytest` は `uv run` 経由で呼ぶ。

**todo フロントマターの厳密な形式（RESEARCH Task F pitfall 3）:** 4 フィールド
`created`（ISO8601 + タイムゾーン。`date -Iseconds` の出力をそのまま使う）/ `title`（日本語可）/
`area` / `files`（配列、各要素は `path:行 (説明)` 形式）に続けて `## Problem` と `## Solution` の
2 セクション。既存例は `2026-07-21-move-documentation-hosting-to-read-the-docs.md`。
本文は日本語で書く（既存 todo と同じ）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 本フェーズが明示的に退避した 2 件の todo を起票する (D-15/D-16)</name>
  <files>.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md, .planning/todos/pending/2026-07-22-delete-orphan-docs-configuration-rst.md</files>
  <read_first>
    - `.planning/todos/pending/2026-07-21-move-documentation-hosting-to-read-the-docs.md` — フロントマター 4 フィールドと `## Problem` / `## Solution` 構成の実例。必ず先に読む
    - `pyproject.toml` — ruff `[tool.ruff.lint] ignore` の `UP035` / `UP006` の 2 行と、その現行コメント文言
    - `typsphinx/__init__.py`, `typsphinx/template_engine.py`, `typsphinx/builder.py`, `typsphinx/translator.py` — `from typing import` 行を `grep -n 'from typing import' typsphinx/*.py` で実測し、`Dict`/`List`/`Set`/`Tuple` の実出現数を数える（todo の `files` 欄に実測値を書くため）
    - `docs/configuration.rst` — 行数、誤パッケージ名 `sphinxcontrib.typst` の出現行、誤パス言及の行を実測
    - `docs/source/` 配下の各 `index.rst` — `docs/configuration.rst` がどの toctree からも参照されていないことの確認（`grep -rn 'configuration' docs/source/*/index.rst docs/source/index.rst`）
  </read_first>
  <action>
    (a) `.planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md` を新規作成する (D-15)。
    `area: source`。`title` は「typing の modernize（`UP006`/`UP035` の ignore 解除と組み込みジェネリクスへの書き換え）」相当。
    `files` には実測した行を列挙する: `pyproject.toml` の `UP035` / `UP006` の ignore 2 行（説明つき）と、
    `typsphinx/{__init__,template_engine,builder,translator}.py` の `from typing import` 行。
    `## Problem` には Research Task D が確定させた**正直な理由**を書く — `requires-python = ">=3.12"` である以上
    `dict[str, Any]` 等の組み込みジェネリクスは何の問題もなく使え、`Dict`/`List` を使い続ける技術的必然性は
    存在しない。ignore が残っているのは Phase 6 (v0.5.0) で Python フロアを 3.12 へ引き上げた際に
    3.10 互換用の ignore を一緒に外し損ねたメンテナンスの取りこぼしである、と明記する。技術的正当化を捏造しないこと。
    `## Solution` には (1) `pyproject.toml` から当該 2 つの ignore を除去、(2) `ruff check --fix` で
    4 ファイルの typing インポートを組み込み型に書き換え、(3) `mypy typsphinx/` と全テストが green であることを確認、
    (4) `CLAUDE.md` の該当記述（Plan 03 で「D-15 が着地するまでの意図的な先送り」と書き換えられる箇所）を
    todo 着地時に再度更新する、の 4 手順を書く。**Plan 03 がこのファイルパスを参照するため、ファイル名は上記のとおり厳守。**

    (b) `.planning/todos/pending/2026-07-22-delete-orphan-docs-configuration-rst.md` を新規作成する (D-16)。
    `area: docs`。`title` は「孤児の `docs/configuration.rst` を削除する」相当。
    `files` には `docs/configuration.rst` を実測した行数・誤パッケージ名の出現行・誤パス言及行つきで記載し、
    `README.md`（Plan 01 の D-12 で参照を既に外した旨）も併記する。
    `## Problem` には、当該ファイルが `docs/source/` の外にありどの toctree からも参照されない（＝Sphinx が
    一切ビルドしない）孤児であること、本文に誤パッケージ名 `sphinxcontrib.typst` を含むこと、
    README からの参照は Phase 22.4 Plan 01 で `docs/source/user_guide/configuration.rst` に張り替え済みで
    参照元がゼロになったことを書く。`## Solution` には削除前に (1) 現行の
    `docs/source/user_guide/configuration.rst` に無い有用な内容が孤児側に残っていないかの差分確認、
    (2) 参照元ゼロの再確認（`grep -rn 'docs/configuration' . --exclude-dir=.git`）、(3) 削除、を書く。

    **どちらの todo も「起票」であり「実施」ではない。** 本タスクで `typsphinx/` や `docs/` の
    実ファイルを変更してはならない。
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md &amp;&amp; test -f .planning/todos/pending/2026-07-22-delete-orphan-docs-configuration-rst.md &amp;&amp; for f in .planning/todos/pending/2026-07-22-modernize-typing-imports-drop-up006-up035-ignore.md .planning/todos/pending/2026-07-22-delete-orphan-docs-configuration-rst.md; do grep -q '^created: 20' "$f" &amp;&amp; grep -q '^title: ' "$f" &amp;&amp; grep -q '^area: ' "$f" &amp;&amp; grep -q '^files:' "$f" &amp;&amp; grep -q '^## Problem' "$f" &amp;&amp; grep -q '^## Solution' "$f" || { echo "BAD FRONTMATTER: $f"; exit 1; }; done &amp;&amp; test -z "$(git diff --name-only 240142a..HEAD -- typsphinx/ docs/)" &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <acceptance_criteria>
    - 2 ファイルがそれぞれ存在する（`test -f`、パスは `<files>` のとおり厳密一致）
    - 各ファイルが `created:` / `title:` / `area:` / `files:` の 4 フロントマターキーと `## Problem` / `## Solution` の 2 見出しをすべて持つ
    - `created:` の値が `+09:00` 等のタイムゾーンオフセットを含む: `grep -cE '^created: [0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9:]+[+-][0-9]{2}:[0-9]{2}$'` が各ファイルで 1
    - D-15 の todo が `UP006` と `UP035` の両方を本文で言及する（`grep -c 'UP006' <file>` ≥1 かつ `grep -c 'UP035' <file>` ≥1）
    - D-16 の todo が `sphinxcontrib.typst` を誤パッケージ名として言及する（`grep -c 'sphinxcontrib.typst' <file>` ≥1）
    - `git diff --name-only 240142a..HEAD -- typsphinx/ docs/` が空（起票であって実施ではない）
  </acceptance_criteria>
  <done>D-15 と D-16 の退避作業が実ファイルとして存在し、既存 todo と同一のフロントマター形式に適合している。</done>
</task>

<task type="auto">
  <name>Task 2: 「登録済みだが出力に効かない設定」と「存在しない設定名の文書化」を起票する (D-18/D-21)</name>
  <files>.planning/todos/pending/2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md, .planning/todos/pending/2026-07-22-user-guide-configuration-phantom-config-names.md</files>
  <read_first>
    - `.planning/todos/pending/2026-07-21-move-documentation-hosting-to-read-the-docs.md` — フロントマター形式の実例
    - `typsphinx/template_engine.py` — `DEFAULT_PARAMETER_MAPPING`、`map_parameters()`（マッピング外キーを無条件に捨てる箇所）、`render()`（`#show: project.with(...)` へ渡る引数の決定箇所）。行番号は `grep -n 'def map_parameters\|def render\|DEFAULT_PARAMETER_MAPPING' typsphinx/template_engine.py` で実測する
    - `typsphinx/writer.py` — `typst_elements` を `sphinx_metadata` へ `update()` するだけの箇所（`grep -n 'typst_elements' typsphinx/writer.py`）
    - `typsphinx/__init__.py` — `typst_toctree_defaults` の登録行（`grep -n 'typst_toctree_defaults' typsphinx/__init__.py`）と、それを消費する箇所がゼロであること（`grep -rn 'typst_toctree_defaults' typsphinx/`）
    - `typsphinx/templates/base.typ` — `project()` 関数のデフォルト引数（テンプレート側デフォルトが常に使われる証跡）
    - `tests/test_config.py`, `tests/test_config_template_mapping.py`, `tests/test_config_toctree_defaults.py` — registration-only な検証にとどまっていることの実測
    - `docs/source/user_guide/configuration.rst` — phantom な設定名 5 個（`typst_use_codly` / `typst_code_line_numbers` / `typst_author` / `typst_papersize` / `typst_fontsize`）の実出現行を `grep -n` で実測する
    - `.planning/phases/22.2-dead-config-value-sweep/22.2-CONTEXT.md` — 同型（第一・第二インスタンス）の先例
  </read_first>
  <action>
    (a) `.planning/todos/pending/2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md` を新規作成する (D-18)。
    `area: source, config`。`title` は「登録済みだが出力に反映されない `typst_*` 設定の第三・第四インスタンス
    （`typst_elements` の非マッピングキー / `typst_toctree_defaults`）」相当。
    `files` には実測した行つきで `typsphinx/template_engine.py`（`map_parameters` と `DEFAULT_PARAMETER_MAPPING`）、
    `typsphinx/writer.py`（`typst_elements` の `update()`）、`typsphinx/__init__.py`（`typst_toctree_defaults` 登録行）、
    `typsphinx/templates/base.typ`（`project()` のデフォルト引数）、および registration-only なテスト 3 本を列挙する。
    `## Problem` には (1) `typst_elements` に入れた非マッピングキーは `map_parameters()` が
    無条件に捨てるため `#show: project.with(...)` に到達せず、実ビルドで生成 `.typ` に文字列が
    一切現れないことを再現手順つきで記す、(2) `typst_toctree_defaults` は登録されているだけで
    `typsphinx/` のどこからも参照されず、実際の toctree オプションは
    `TemplateEngine.extract_toctree_options()` が doctree から直接読むため当該設定を一切経由しないことを記す、
    (3) これが Phase 22.2 が `typst_output_dir` / `typst_package` に対して塞いだのと同型の第三・第四の
    インスタンスであり、registration-only テストの陰に隠れていた点まで書く。
    `## Solution` には二択を明記する — (A) 非マッピングキーをテンプレート関数へ渡す経路を実装し
    `typst_toctree_defaults` を実際に消費させる、(B) 設定自体を削除する（Phase 22.2 CONF-01 の前例）。
    いずれを採る場合も **config→output の回帰フィクスチャ**（Phase 22.2 CONF-03 と同型）を同時に
    追加すること、を必須条件として書く。Phase 22.4 側の措置（README の Custom Templates 例から
    当該キーを剪定済み／Configuration Options の 6 件構成は不変）も記録する。

    (b) `.planning/todos/pending/2026-07-22-user-guide-configuration-phantom-config-names.md` を新規作成する (D-21)。
    `area: docs`。`title` は「`docs/source/user_guide/configuration.rst` が実在しない設定名 5 個を記載している」相当。
    `files` には `docs/source/user_guide/configuration.rst` を、実測した各 phantom 名の行番号つきで列挙し、
    正解源として `typsphinx/__init__.py` の設定登録範囲も併記する。
    `## Problem` には 5 個の名前を列挙し、いずれも `typsphinx/__init__.py` に一切登録されていない
    架空の設定名であること、読者がコピーしても静かに無視されることを書く。あわせて、Phase 22.4 D-12 が
    README のリンク先としてこのファイルを選んだ判断は（孤児 `docs/configuration.rst` より遥かに良く
    toctree 到達性もあるため）**依然として正しい**が、リンク先自体にも独立した誤りが残っている、と明記する。
    `## Solution` には (1) 5 個の記述の削除または実在する設定名への置換、(2) 記載されている全 `typst_*` 名を
    `typsphinx/__init__.py` の登録集合と突き合わせる確認手順、を書く。

    **どちらも起票のみ。** `typsphinx/` も `docs/` も本タスクで変更しない。
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md &amp;&amp; test -f .planning/todos/pending/2026-07-22-user-guide-configuration-phantom-config-names.md &amp;&amp; for f in .planning/todos/pending/2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md .planning/todos/pending/2026-07-22-user-guide-configuration-phantom-config-names.md; do grep -q '^created: 20' "$f" &amp;&amp; grep -q '^title: ' "$f" &amp;&amp; grep -q '^area: ' "$f" &amp;&amp; grep -q '^files:' "$f" &amp;&amp; grep -q '^## Problem' "$f" &amp;&amp; grep -q '^## Solution' "$f" || { echo "BAD FRONTMATTER: $f"; exit 1; }; done &amp;&amp; grep -q 'typst_toctree_defaults' .planning/todos/pending/2026-07-22-dead-config-typst-elements-keys-and-toctree-defaults.md &amp;&amp; for n in typst_use_codly typst_code_line_numbers typst_author typst_papersize typst_fontsize; do grep -q "$n" .planning/todos/pending/2026-07-22-user-guide-configuration-phantom-config-names.md || { echo "MISSING PHANTOM: $n"; exit 1; }; done &amp;&amp; test -z "$(git diff --name-only 240142a..HEAD -- typsphinx/ docs/)" &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <acceptance_criteria>
    - 2 ファイルが存在し、4 フロントマターキー + `## Problem` / `## Solution` をすべて備える
    - D-18 の todo が `typst_elements` と `typst_toctree_defaults` の両方を言及する（各 `grep -c` ≥1）
    - D-21 の todo が phantom 5 名すべてを言及する: `typst_use_codly` / `typst_code_line_numbers` / `typst_author` / `typst_papersize` / `typst_fontsize`（各 `grep -c` ≥1）
    - `git diff --name-only 240142a..HEAD -- typsphinx/ docs/` が空
    - `grep -cE '"UP006"|"UP035"' pyproject.toml` が **2**（無関係な ruff 設定を巻き込んでいないこと）
  </acceptance_criteria>
  <done>Phase 22.2 と同型の dead-config 2 件と、リンク先文書の phantom 設定名 5 個が、実測行つきで追跡可能な状態になっている。</done>
</task>

<task type="auto">
  <name>Task 3: github.io リンク 404 の実測を起票し、既存 RTD todo の誤前提を打ち消し、linkcheck CI を提案する (D-17)</name>
  <files>.planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md, .planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md, .planning/todos/pending/2026-07-21-move-documentation-hosting-to-read-the-docs.md</files>
  <read_first>
    - `.planning/todos/pending/2026-07-21-move-documentation-hosting-to-read-the-docs.md` — **全文**。フロントマター形式の実例であり、かつ本タスクで追記する対象そのもの
    - `README.md` — Documentation 節の `yusabo90002.github.io` リンク 7 本（Plan 01 が現状維持したもの）。`grep -n 'yusabo90002.github.io' README.md` で現在位置を引き直す
    - `docs/build_multilang.py` — `create_redirect_page()` と canonical URL のハードコード箇所。`/en/`・`/ja/` に分けて公開しルート直下は JS 言語判定リダイレクトのみである構成の正解源（`grep -n 'canonical\|def create_redirect_page\|/en/' docs/build_multilang.py`）
    - `tox.ini` — `docs-multilang` env の定義（`grep -n 'docs-multilang' -A 8 tox.ini`）
    - `.github/workflows/docs.yml` — Pages デプロイステップ
    - `.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-RESEARCH.md` — 新規発見 #3 の 7 URL 実測表と Task F の linkcheck 提案
  </read_first>
  <action>
    (a) `.planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md` を新規作成する (D-17)。
    `area: docs`。`title` は「README の github.io ドキュメントリンク 7 本が 404（`/en/` プレフィックス欠落）」相当。
    `files` には `README.md`（Documentation 節のリンク箇所。行番号は起票時点で `grep -n` して実測した値を使う）、
    `docs/build_multilang.py`（多言語ツリー生成と canonical URL）、`tox.ini`（`docs-multilang` env）、
    `.github/workflows/docs.yml`（Pages デプロイ）を実測行つきで列挙する。
    `## Problem` には RESEARCH の 7 URL 実測表（各リンクが 404、`/en/` 付きの同名パスが 200）をそのまま
    転記し、原因が `docs/build_multilang.py` の en/ja 分割公開構成でルート直下が JS リダイレクトのみである
    ことを書く。**あわせて、既存 todo `2026-07-21-move-documentation-hosting-to-read-the-docs.md` の
    検討時に前提とされていた「実測上リンクは全て有効」という判断が誤りだったことを明記し、当該 todo を
    ファイル名で相互参照する。** Phase 22.4 のオーナー裁定により README のリンク自体は**現状維持**とした
    経緯（リンクの一括変更は RTD 移行作業側に委ねる）も記録する。
    `## Solution` には二択を書く — (A) RTD 移行を待たず README の 7 リンクに `/en/` を足して即座に 404 を解消する
    （同一ホスト内のパス修正でありホスティング移行ではない）、(B) RTD 移行時に移行先 URL へ一括で張り替える。
    どちらを採るにせよ本件は RTD 移行 todo と同時に判断すべき、と結ぶ。

    (b) `.planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md` を新規作成する。
    `area: ci, docs`。`title` は「`sphinx-build -b linkcheck` の CI ジョブを追加する」相当。
    `files` には `.github/workflows/ci.yml`（またはドキュメント系ワークフロー）、`tox.ini`（新 env の追加先）、
    `docs/source/conf.py`（`linkcheck_ignore` の設定先）を挙げる。
    `## Problem` には、(a) の 404 が数か月にわたり誰にも気づかれずに残っていた事実を挙げ、
    これを自動検出できたはずの仕組みが存在しないことを書く。Phase 22.4 は grep ベースの手動検証が
    誠実な到達点であり、リンク到達性の自動化は別途必要だという RESEARCH Task F の結論を引用する。
    `## Solution` には `tox.ini` に `[testenv:linkcheck]` を追加し CI から呼ぶ案、レート制限や
    外部サイトの一時障害でジョブが不安定にならないよう `linkcheck_ignore` と
    「失敗しても main をブロックしない扱い」から始める案を書く。

    (c) 既存の `.planning/todos/pending/2026-07-21-move-documentation-hosting-to-read-the-docs.md` を
    **Edit で追記**する（Write で全文置換しないこと）。`## Problem` 節の末尾に、2026-07-22 実測で
    README の github.io サブページリンク 7 本がすべて 404 であったこと、したがって本 todo 検討時の
    「リンクは実測上すべて有効」という前提は成立しないこと、詳細は (a) で作成した todo ファイル名を
    参照すること、の 3 点を追記する。既存本文の削除・改変はしない。`files` 配列に (a) の todo を
    足してもよいが、既存要素は削らないこと。
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md &amp;&amp; test -f .planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md &amp;&amp; for f in .planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md .planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md; do grep -q '^created: 20' "$f" &amp;&amp; grep -q '^title: ' "$f" &amp;&amp; grep -q '^area: ' "$f" &amp;&amp; grep -q '^files:' "$f" &amp;&amp; grep -q '^## Problem' "$f" &amp;&amp; grep -q '^## Solution' "$f" || { echo "BAD FRONTMATTER: $f"; exit 1; }; done &amp;&amp; grep -q '404' .planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md &amp;&amp; grep -q '2026-07-21-move-documentation-hosting-to-read-the-docs' .planning/todos/pending/2026-07-22-github-io-doc-links-404-missing-en-prefix.md &amp;&amp; grep -q '404' .planning/todos/pending/2026-07-21-move-documentation-hosting-to-read-the-docs.md &amp;&amp; grep -q 'linkcheck' .planning/todos/pending/2026-07-22-add-sphinx-linkcheck-ci-job.md &amp;&amp; test "$(ls .planning/todos/pending/*.md | wc -l)" = "10" &amp;&amp; echo TASK3_OK</automated>
  </verify>
  <acceptance_criteria>
    - 新規 2 ファイルが存在し、4 フロントマターキー + `## Problem` / `## Solution` をすべて備える
    - D-17 の todo が `404` を言及し、既存 RTD todo をファイル名 `2026-07-21-move-documentation-hosting-to-read-the-docs` で相互参照する（各 `grep -c` ≥1）
    - 既存 RTD todo `2026-07-21-move-documentation-hosting-to-read-the-docs.md` が `404` を言及するようになる（`grep -c '404'` ≥1）— 誤前提が打ち消されたことの機械検証
    - 既存 RTD todo の元の本文が残っている: `grep -c 'peaceiris/actions-gh-pages@v4'` が **1**、`grep -c 'build_multilang.py'` が **1 以上**（Write 全置換していないことの検証）
    - linkcheck todo が `linkcheck` を言及する（`grep -c 'linkcheck'` ≥1）
    - `ls .planning/todos/pending/*.md | wc -l` が **10**（既存 4 + 新規 6）
    - `git diff --name-only 240142a..HEAD -- README.md typsphinx/ docs/ .github/ tox.ini` が空（提案であって実施ではない）
  </acceptance_criteria>
  <done>github.io の 404 が実測事実として記録され、それに依拠していた既存 todo の誤前提が打ち消され、再発防止の自動化が提案として残っている。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （該当なし） | 本プランは `.planning/todos/pending/` 配下の Markdown ファイルを作成・追記するのみ。実行されるコードも、解析される入力も、新規のネットワーク到達先も無い |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| （なし） | — | — | — | — | 生成物は計画メモであり、配布物にもビルド成果物にも含まれない。todo 本文が言及する `yusabo90002.github.io` は既存の自プロジェクト GitHub Pages ホストであり、新規ドメインの導入ではない。ASVS L1 の V2/V3/V4/V5/V6 いずれも適用対象を持たない。適用なしと明記するのが誠実な結論である |

**Package installs:** なし。`T-22.4-SC` サプライチェーン脅威も適用外。
</threat_model>

<verification>
全タスク完了後:

1. `ls .planning/todos/pending/*.md | wc -l` → 10
2. 新規 6 ファイルすべてでフロントマター 4 キー + 2 見出しが揃う（各タスクの automated verify を再実行）
3. VALIDATION.md の DOC-05 行が要求する ls-grep がすべてマッチする:
   - `ls .planning/todos/pending/ | grep -iE 'typing|modern'` → ≥1
   - `ls .planning/todos/pending/ | grep -i 'configuration'` → ≥1
   - `ls .planning/todos/pending/ | grep -iE 'link|404|github-io'` → ≥1
   - `ls .planning/todos/pending/ | grep -iE 'user-guide|phantom|configuration'` → ≥1
4. スコープフェンス: `git diff --stat 240142a..HEAD -- typsphinx/ docs/ .github/ tox.ini CHANGELOG.md` → 出力なし
5. `env -u VIRTUAL_ENV -u UV_PROJECT_ENVIRONMENT uv run pytest` → フェーズ開始前と同じパス件数
</verification>

<success_criteria>
- ROADMAP SC#5（D-15 / D-16 が pending todo として存在し、台帳外の発見が本フェーズで直されるか起票されるかのいずれかで処理されている）が満たされる
- ROADMAP SC#6（スコープフェンス）が `git diff --stat` で確認できる
- Plan 03 が参照する D-15 todo のパスが確定して存在する
</success_criteria>

<output>
Create `.planning/phases/22.4-readme-status-configuration-options-known-limitations-docs/22.4-02-SUMMARY.md` when done.

SUMMARY には必ず含めること:
- 作成した 6 ファイルの完全パス一覧
- D-15 todo の完全パス（Plan 03 が参照するため明示）
- 既存 RTD todo へ追記した文面の引用
</output>
