---
milestone: v0.9.5
audited: 2026-09-16T11:55:00Z
status: tech_debt
scores:
  requirements: 3/4 satisfied + 1 held open by design (REL-14)
  phases: 2/2 verified (passed)
  integration: 6/6
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
held_by_design:
  - id: "REL-14"
    status: "partial (by design — checked only at /gsd-complete-milestone on the observed merge)"
    phase: "73"
    claimed_by_plans: ["73-01", "73-02", "73-03", "73-04", "73-05", "73-06", "73-07"]
    completed_by_plans: []
    verification_status: "passed"
    evidence: "73-VERIFICATION.md SC#1-SC#4 passed (4/4, 0 overrides). Every 73 SUMMARY declares requirements-completed: [] as constraint 10 requires. REQUIREMENTS.md:22 reads `- [ ]`, :57 reads `Pending`. Fence re-run by this audit at HEAD 29baea5f: sha256 7a21a1e48d7abfe4f1e8696dbcb40c5ffe0da4fc95bc9a790ee8501a5812bcad, 70 lines — MATCH with REQ_SHA256_BASE, REQ_SHA256_CLOSE and REQ_SHA256_THIRD in 73-CLOSEOUT-GUARD.md."
tech_debt:
  - phase: 72-tox-e-linkcheck-and-root-toctree-deduplication
    items:
      - "Info IN-01 (72-REVIEW.md): tox.ini:92-98 `[testenv:linkcheck]` has no failure-tolerance override or accept-list, unlike links.yml's advisory lychee job. A single flaky external URL fails the env locally with no guidance in the surrounding docs. Optional follow-up: note transient-failure risk in the env's `description`."
      - "72-VERIFICATION.md frontmatter says `status: passed` while its body says `Status: human_needed`. The one human item (the rendered furo sidebar, SC#4) was closed by 72-UAT.md (1/1 pass, 2026-09-13), so the frontmatter is correct; the body line is stale. Same stale-body shape as v0.9.4's 70-VERIFICATION.md."
  - phase: 73-v0-9-5-close-prep-prep-only-unpublished
    items:
      - "Info IN-01 (73-REVIEW.md): CHANGELOG.md:57-60's DOC-18 Fixed bullet states the sidebar fix unconditionally and gives a changelog-only reader no signal that the narrower HTML-vs-Typst `examples/basic` parent question was a deliberate Non-Goal. Not an inaccuracy — the divergence was re-measured at the Phase 72 tip and did NOT survive (72-VERIFICATION.md: `DIVERGENCE_SURVIVES = no`, no todo filed). Optional trailing clause only."
      - "Nyquist: 73-VALIDATION.md is `status: draft` (NOT-VALIDATED). validate-phase never ran for Phase 73. `workflow.nyquist_validation` is true."
      - "Security: `workflow.security_enforcement` is true but Phase 73 has no 73-SECURITY.md. secure-phase never ran for Phase 73."
      - "The REL-14 auto-flip fired again: `phase.complete 73` flipped REQUIREMENTS.md:22/:57 to `[x]`/Complete against the CONTEXT decision. Caught by the constraint-10 fence, reverted before commit (29baea5f). This is 9 of the 10 prior release-prep closes; the one non-firing (v0.9.4's Phase 71) remains an outlier, not a fix."
nyquist:
  compliant_phases: ["72"]
  partial_phases: []
  not_validated_phases: ["73"]
  missing_phases: []
  overall: partial
---

# v0.9.5 Docs Link Check and Navigation — Milestone Audit

**Verdict: `tech_debt`.** No blocking gaps. All three work requirements (QUA-13, DOC-24, DOC-18)
are satisfied on every one of the three sources. REL-14 is held open deliberately and closes at
`/gsd-complete-milestone` on the observed merge. The debt is six non-blocking items; two of them
are the same pair of Phase-gate gaps v0.9.4's Phase 71 carried (Nyquist and security never ran on
the close-prep phase).

## Scope

| Item | Value |
|---|---|
| Milestone | v0.9.5 — Docs Link Check and Navigation |
| Phases | 72, 73 (both complete) |
| Plans / tasks | 13/13 (6 + 7), 31 tasks |
| Branch | `gsd/v0.9.5-docs-link-check-and-navigation`, local HEAD `29baea5f`; `origin` at `a54a2d8a` (CI-verified tip) |
| Local-only commits | `.planning/` only: `git diff --name-only origin/gsd/v0.9.5-…..HEAD -- . ':!.planning'` is empty |
| `origin/main` | `6e2b7899` — moved past the milestone base `098a8ff6` via Dependabot #146–#150, all merged 2026-09-14 |
| Decoy branch | **none**, locally or on `origin` (`git ls-remote origin 'refs/heads/gsd/v0.9.5*'` returns exactly one ref). The helper did not re-create one this milestone. |
| Tags | `git ls-remote --tags origin \| grep -c 'v0\.9\.[345]'` = 0 |
| Working tree | clean (`git status --porcelain` empty) |

## Requirements Coverage (3-source cross-reference)

Sources: (A) each phase's `VERIFICATION.md`; (B) each plan's `SUMMARY.md`
`requirements-completed` frontmatter; (C) `REQUIREMENTS.md`'s checkbox + traceability row.

| REQ | Phase | (A) VERIFICATION | (B) SUMMARY frontmatter | (C) REQUIREMENTS.md | Final |
|---|---|---|---|---|---|
| QUA-13 | 72 | passed | 72-02, 72-05, 72-06 | `[x]` / Complete | **satisfied** |
| DOC-24 | 72 | passed | 72-03, 72-06 | `[x]` / Complete | **satisfied** |
| DOC-18 | 72 | passed | 72-01, 72-04, 72-05, 72-06 | `[x]` / Complete | **satisfied** |
| REL-14 | 73 | passed (coverage-only) | all 7 declare `[]` — required | `[ ]` / Pending | **held by design** |

**Orphan detection:** none. `REQUIREMENTS.md`'s own line reads "Mapped to phases: 4 (Phase 72:
QUA-13, DOC-24, DOC-18 · Phase 73: REL-14)", and each of the four appears in the `requirements:`
frontmatter of at least one plan in its mapped phase. QUA-08 is in **Future**, maps to no phase,
and is correctly absent from both phases — the owner deferred it at roadmap review on 2026-09-13
because GitHub runs `schedule` and `workflow_dispatch` only for a workflow file on the default
branch, so it could not be proven on an unmerged milestone branch.

**REL-14 is not a gap.** Constraint 10 requires its checkbox to stay `[ ]` through every plan, and
`/gsd-complete-milestone` is the only thing that may check it, on the observed merge. Treating it
as `unsatisfied` here would invert the requirement. It is recorded under `held_by_design:` rather
than `gaps:` for the same reason REL-13 was at v0.9.4's audit.

### REL-14 fence, re-measured by this audit

| Observation | Where | sha256 | lines | Verdict |
|---|---|---|---|---|
| Phase head | `73-CLOSEOUT-GUARD.md` `REQ_SHA256_BASE` | `7a21a1e4…12bcad` | 70 | — |
| Phase close | `REQ_SHA256_CLOSE` | `7a21a1e4…12bcad` | 70 | MATCH |
| After `phase.complete` | `REQ_SHA256_THIRD` (commit `29baea5f`) | `7a21a1e4…12bcad` | 70 | MATCH |
| **This audit, at HEAD `29baea5f`** | live `sha256sum` | `7a21a1e4…12bcad` | 70 | **MATCH** |

`.planning/REQUIREMENTS.md:22` reads `- [ ] **REL-14**: …` and `:57` reads
`| REL-14 | Phase 73 | Pending |`, both read directly, not inferred.

**The auto-flip fired again.** `THIRD_OBS_FLIP_FIRED = yes`: `phase.complete 73` flipped REL-14 to
`[x]`/Complete, and the response was `git checkout -- .planning/REQUIREMENTS.md` before any commit.
That makes **9 of the 10** prior release-prep closes. v0.9.4's Phase 71 non-firing stays an
outlier, and constraint 10's decision to re-run the fence anyway was correct.

## Phase Verification

| Phase | VERIFICATION status | Score | Human items | Gaps |
|---|---|---|---|---|
| 72 | `passed` | 5/5 SC verified | 1 (rendered furo sidebar) — **closed** by `72-UAT.md`, 1/1 pass, 2026-09-13 | none |
| 73 | `passed` | 4/4 SC verified | none | none |

Both verifiers re-measured rather than trusting the plans' own SUMMARYs. Phase 72's re-ran the
clean HTML and `-b typst` builds, the linkcheck run (95/95 `working`), the local gate quartet
(1547 passed / 1 skipped) and CI run `34761445288` (12/12). Phase 73's re-ran the tag/PyPI/Release
probes with positive controls, CI run `35083828156` (12/12, both `windows-latest` and both
`macos-latest` lanes named and green), branch protection (6 contexts, `strict: true`) and the
non-committing trial merge.

**One caveat, recorded not scored:** `72-VERIFICATION.md`'s frontmatter says `status: passed` while
its body heading says `Status: human_needed`. The frontmatter is the correct one — the single
human item was closed by UAT the same day. The stale body line is the same shape v0.9.4's
`70-VERIFICATION.md` carried and is logged as debt, not as a gap.

## Cross-Phase Integration

Checked by `gsd-integration-checker` against the live tree. **6/6 connections wired, 0 gaps.**

| # | Connection | Verdict |
|---|---|---|
| 1 | Doc surfaces (`CLAUDE.md:34`, `README.md:264`, `contributing.rst:126`) → `tox.ini:92-98 [testenv:linkcheck]` | WIRED — each names an environment that exists, in the neighbours' own form (`tox -e …` vs `uv run tox -e …`), column-aligned; `linkcheck` absent from `env_list` |
| 2 | Root `index.rst` → `user_guide/index` (owns 4) and `examples/index` (owns 2) | WIRED — all 14 docs pages reachable, zero orphans, each of the five de-duplicated pages reachable through exactly one chain |
| 3 | Phase 72 deliverables → Phase 73 CHANGELOG bullets | WIRED — `### Added` names QUA-13/DOC-24, `### Fixed` names DOC-18; no CI/job/schedule vocabulary (QUA-08 is deferred), no version heading, compare base still `v0.9.2` |
| 4 | Scope fence | WIRED — `git diff --stat 098a8ff6..HEAD -- typsphinx/ .github/workflows/` empty; widened control shows exactly the 6 expected product files |
| 5 | `docs/source/conf.py` | WIRED — unchanged from base; no `linkcheck_*` key, matching D-02 (no transient failure was measured, so none was added speculatively) |
| 6 | `73-HANDOFF.md` → `/gsd-complete-milestone` | WIRED — standalone: 9 enumerated steps, the fence procedure inlined, the two not-applicable steps reasoned, Dependabot #146–#150 recorded as already merged |

### E2E Flows — 3/3 complete

1. **Contributor runs the link check.** CLAUDE.md/README/contributing → `tox -e linkcheck` →
   `[testenv:linkcheck]` → `sphinx-build -b linkcheck` → results. `tox list` puts it under
   *additional environments*, so a plain `tox` run is unaffected. No break.
2. **Reader browses the HTML docs.** Root lists only the two section indexes; each section index
   lists its children once; the furo sidebar renders 4 + 2 nested pages; Sphinx reports zero
   `document is referenced in multiple toctrees`. Confirmed structurally by the verifier's markup
   count (each page = 1, control `output_layout` = 1) and visually by the owner at UAT. No break.
3. **Release prep reaches the merge.** Phase 72 lands → Phase 73 adds the CHANGELOG bullets →
   branch on `origin` at `a54a2d8a` with CI `35083828156` all-green → PR to `main` → merge commit,
   version still `0.9.2`, no tag/PyPI/Release → REL-14 checked on the observed merge. No break.

> Two narrative details in the checker's flow-3 trace run ahead of the facts and are not adopted
> here: there are **no docstring changes** in this milestone's diff (the `visit_toctree` docstring
> warnings are pre-existing and untouched by constraint 2), and REL-14's checkbox has **not** been
> checked — it is checked at the merge, which has not happened. Neither affects the wiring verdict.

## Milestone Invariants

| Invariant | Measured | Verdict |
|---|---|---|
| Zero lines change under `typsphinx/` (constraint 2) | `git diff --stat 098a8ff6..HEAD -- typsphinx/` empty, with widened control | HELD |
| No workflow file added or edited (constraint 3) | `… -- .github/workflows/` empty; no new file under `.github/workflows/` | HELD |
| `env_list` unchanged | `py312, py313, lint, type, cov, docs` — byte-identical | HELD |
| `tox-uv~=1.35` pin and its comment untouched | byte-identical in the `tox.ini` diff | HELD |
| `@preview` package count stays 4, versions unchanged | `test_preview_version_sync.py` green | HELD |
| `pyproject.toml` stays `0.9.2` | line 7 read live | HELD |
| No tag / PyPI / GitHub Release for 0.9.3–0.9.5 | positively-controlled probes (`v0.9.2` present, `0.9.2` → 200) | HELD |
| `flake.nix` not touched | absent from the milestone diff | HELD |
| Required status checks: 6 contexts, `strict: true` | read at Phase 72 base, Phase 72 close, Phase 73 pre-flight and by the Phase 73 verifier — identical every time | HELD |
| Full pytest green at every phase close | 1547 passed / 1 skipped, incl. one `LC_ALL=C` run | HELD |

## Nyquist Coverage

| Phase | VALIDATION.md | `status` | `nyquist_compliant` | Classification | Action |
|---|---|---|---|---|---|
| 72 | exists | `validated` | `true` | **COMPLIANT** | none |
| 73 | exists | `draft` | `false` | **NOT-VALIDATED** | `/gsd-validate-phase 73` (coverage TODO, not a compliance failure) |

Per #2117, Phase 73's `draft` means validate-phase never reconciled the file, so
`nyquist_compliant: false` there is not authoritative. Overall: **partial**.

## Security Coverage

`workflow.security_enforcement` is `true`.

| Phase | SECURITY.md | Status | Open threats |
|---|---|---|---|
| 72 | exists | `verified` | 0 (ASVS L1, `block_on: high`; auditor short-circuited) |
| 73 | **absent** | — | — |

`/gsd-secure-phase 73` never ran. This is the same gap v0.9.4's Phase 71 carried, and on the same
kind of phase: a prep-only close whose single product change is a 15-line CHANGELOG addition. It is
logged as debt, not a blocker.

## Tech Debt — 6 items across 2 phases

**Phase 72**
1. `72-REVIEW.md` IN-01 (info): `[testenv:linkcheck]` has no failure-tolerance override, so one
   flaky external URL fails the env locally. Optional `description` note.
2. `72-VERIFICATION.md` frontmatter/body disagree on status; frontmatter is correct (UAT closed the
   human item), body line is stale.

**Phase 73**
3. `73-REVIEW.md` IN-01 (info): the CHANGELOG DOC-18 bullet does not signal that the narrower
   `examples/basic` parent question was a Non-Goal. It did not survive re-measurement, so nothing
   is open — this is changelog completeness only.
4. Nyquist: `73-VALIDATION.md` is `draft`; validate-phase never ran.
5. Security: no `73-SECURITY.md`; secure-phase never ran.
6. The REL-14 auto-flip fired again at `phase.complete 73` and was reverted before commit. The
   defect is in the tooling, not in this milestone; the fence did its job.

## Deferred / Out of Scope (carried forward, not gaps)

| Item | Where it lives |
|---|---|
| **QUA-08** — weekly advisory CI workflow running `tox -e linkcheck` | `REQUIREMENTS.md` § Future. A future pickup must plan a **side PR to `main`** from the start, as v0.9.3 did for `dependabot.yml` (#137). The 2026-07-22 todo stays in `todos/pending/` with a Phase 72 status note naming `[testenv:linkcheck]`, QUA-08 and the side-PR constraint. |
| Issue filing on a failed scheduled linkcheck run | Declined by the owner 2026-09-13; relevant only once QUA-08 is picked up |
| Per-push/PR or required linkcheck runs | Declined outright 2026-09-13 — lychee (`links.yml`) already covers raw URLs per push/PR |
| The 3 counted docs warnings (`visit_toctree` docstring rST errors) | Fixing them edits `typsphinx/`, which constraint 2 forbids. Both the fix and a warnings gate are Future requirements. |
| RTD `stable` still shows the pre-fix sidebar | Accepted visibility limit (constraint 5). The fix reaches `/en/latest/` and ja `latest`; `stable` stays on tag `v0.9.2` until the next published release. |

## Conclusion

Every success criterion of both phases holds under independent re-measurement, all six cross-phase
connections are wired, all three E2E flows complete, and every milestone invariant is measured
rather than assumed. The milestone is correctly shaped for a merge-only, unpublished close.

The only item standing between this audit and a closed milestone is REL-14 itself, which is exactly
where the roadmap put it: `/gsd-complete-milestone` runs `73-HANDOFF.md`'s nine steps, and step 2's
conditional branch update **is expected to run** — `origin/main` moved to `6e2b7899` on 2026-09-14,
so `git merge-base --is-ancestor origin/main HEAD` will exit 1.

---

_Audited: 2026-09-16_
_Auditor: Claude (gsd-audit-milestone orchestrator) + gsd-integration-checker_
